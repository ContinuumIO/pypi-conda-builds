package:
  name: agalma
  version: "0.5.0"

source:
  fn: agalma-0.5.0.tar.gz
  url: https://pypi.python.org/packages/source/a/agalma/agalma-0.5.0.tar.gz
  md5: d458fca693de58098fe067ed8fe8e874
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - agalma = agalma:main
    #
    # Would create an entry point called agalma that calls agalma.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - biolite ==0.5.0

  run:
    - python
    - biolite ==0.5.0

test:
  # Python imports
  imports:
    - agalma

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/caseywdunn/agalma
  license: GNU General Public License v3 (GPLv3)
  summary: '\n        An automated phylogenomics pipeline.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
