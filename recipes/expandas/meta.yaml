package:
  name: expandas
  version: "0.1.1"

source:
  fn: expandas-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/e/expandas/expandas-0.1.1.tar.gz
  md5: fab2ab3b7c5342942e98988d5cfface3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - expandas = expandas:main
    #
    # Would create an entry point called expandas that calls expandas.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pandas >=0.15.0

  run:
    - python
    - pandas >=0.15.0

test:
  # Python imports
  imports:
    - expandas
    - expandas.core
    - expandas.misc
    - expandas.misc.test
    - expandas.skaccessors
    - expandas.skaccessors.test
    - expandas.test
    - expandas.util

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://expandas.readthedocs.org/en/stable
  license: BSD
  summary: 'Expand pandas functionalities to be easier to use other statistics/ML packages, mainly forcusing on scikit-learn.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
