package:
  name: django-markup-deprecated
  version: "0.0.3"

source:
  fn: django-markup-deprecated-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-markup-deprecated/django-markup-deprecated-0.0.3.tar.gz
  md5: 812661a9cec1f297d00f73f5459ee47d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-markup-deprecated = django-markup-deprecated:main
    #
    # Would create an entry point called django-markup-deprecated that calls django-markup-deprecated.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - markup_deprecated
    - markup_deprecated.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/fcurella/django-markup-deprecated
  license: BSD License
  summary: 'Original django.contrib.markup from Django 1.4.1.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
