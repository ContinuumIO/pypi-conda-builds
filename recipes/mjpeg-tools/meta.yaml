package:
  name: mjpeg-tools
  version: "0.0.13"

source:
  fn: mjpeg-tools-0.0.13.tar.gz
  url: https://pypi.python.org/packages/source/m/mjpeg-tools/mjpeg-tools-0.0.13.tar.gz
  md5: 6b78cc9281109e93fa8b497606f6b265
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mjpeg-tools = mjpeg-tools:main
    #
    # Would create an entry point called mjpeg-tools that calls mjpeg-tools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pillow

  run:
    - python
    - pillow

test:
  # Python imports
  imports:
    - mjpegtools

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/royendgel
  license: UNKNOWN
  summary: 'mjpeg client, server and manipulator'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
