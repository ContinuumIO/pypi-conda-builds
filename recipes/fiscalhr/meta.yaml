package:
  name: fiscalhr
  version: "0.2.5"

source:
  fn: FiscalHr-0.2.5.tar.gz
  url: https://pypi.python.org/packages/source/F/FiscalHr/FiscalHr-0.2.5.tar.gz
  md5: e3ee275c2d116b4c4f3aa6b906ce1116
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fiscalhr = fiscalhr:main
    #
    # Would create an entry point called fiscalhr that calls fiscalhr.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - backports.ssl-match-hostname >=3.2a3
    - libxml2-python >=2.6.21
    - pyopenssl >=0.13
    - pytz
    - pyxmlsec-next >=0.3.1
    - suds-philpem >=0.4.3
    - xmldsig >=0.2.1

  run:
    - python
    - backports.ssl-match-hostname >=3.2a3
    - libxml2-python >=2.6.21
    - pyopenssl >=0.13
    - pytz
    - pyxmlsec-next >=0.3.1
    - suds-philpem >=0.4.3
    - xmldsig >=0.2.1

test:
  # Python imports
  imports:
    - fiscalhr

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/vingd/fiscal-hr-python
  license: MIT License
  summary: 'Helper class for fiscalization in Croatia'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
