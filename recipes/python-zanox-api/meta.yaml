package:
  name: python-zanox-api
  version: "0.4.0"

source:
  fn: python-zanox-api-0.4.0.zip
  url: https://pypi.python.org/packages/source/p/python-zanox-api/python-zanox-api-0.4.0.zip
  md5: e0b6207a15e022b1f21356590dd434a4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-zanox-api = python-zanox-api:main
    #
    # Would create an entry point called python-zanox-api that calls python-zanox-api.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - zanox

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/baskoopmans/python-zanox-api
  license: BSD License
  summary: 'Python Zanox API - https://developer.zanox.com'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
