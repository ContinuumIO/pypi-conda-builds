package:
  name: incywincy
  version: "0.2.0"

source:
  fn: incywincy-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/i/incywincy/incywincy-0.2.0.tar.gz
  md5: ae54bb6cf761c13af8237fccc87c88e2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - incywincy = incywincy:main
    #
    # Would create an entry point called incywincy that calls incywincy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - beautifulsoup4
    - requests

  run:
    - python
    - beautifulsoup4
    - requests

test:
  # Python imports
  imports:
    - incywincy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/txels/incywincy
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Web Spider Testing Framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
