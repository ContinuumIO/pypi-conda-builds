package:
  name: django-theherk-module
  version: "1.2"

source:
  fn: django-theherk-module-1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-theherk-module/django-theherk-module-1.2.tar.gz
  md5: 50a596fc74a44ddacadbfe9ec7459b6c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-theherk-module = django-theherk-module:main
    #
    # Would create an entry point called django-theherk-module that calls django-theherk-module.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - module
    - module.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/theherk/django-theherk-module
  license: see file LICENSE
  summary: 'Django CMS plugin to post a simple module for use in a sidebar.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
