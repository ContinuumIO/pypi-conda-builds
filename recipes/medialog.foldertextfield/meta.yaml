package:
  name: medialog.foldertextfield
  version: "0.4"

source:
  fn: medialog.foldertextfield-0.4.tar.gz
  url: https://pypi.python.org/packages/source/m/medialog.foldertextfield/medialog.foldertextfield-0.4.tar.gz
  md5: 307a524addb642c896fcf80065a8e5e9
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - medialog.foldertextfield = medialog.foldertextfield:main
    #
    # Would create an entry point called medialog.foldertextfield that calls medialog.foldertextfield.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - archetypes.schemaextender

  run:
    - python
    - setuptools
    - setuptools
    - archetypes.schemaextender

test:
  # Python imports
  imports:
    - medialog
    - medialog.foldertextfield
    - medialog.foldertextfield.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://products.medialog.no
  license: GPL
  summary: 'Makes it possible to add a body text field to folders.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
