package:
  name: django-altauth
  version: "0.4.1"

source:
  fn: django-altauth-0.4.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-altauth/django-altauth-0.4.1.tar.gz
  md5: fe921e088d72310accae7127f8bdde54
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-altauth = django-altauth:main
    #
    # Would create an entry point called django-altauth that calls django-altauth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - rsa >=3.1.2

  run:
    - python
    - rsa >=3.1.2

test:
  # Python imports
  imports:
    - altauth
    - altauth.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/MirkoRossini/django-altauth
  license: BSD License
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
