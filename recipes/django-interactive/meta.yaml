package:
  name: django-interactive
  version: "1.0.6"

source:
  fn: django-interactive-1.0.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-interactive/django-interactive-1.0.6.tar.gz
  md5: 40959a61b45dc8689617fa13348c4d0e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-interactive = django-interactive:main
    #
    # Would create an entry point called django-interactive that calls django-interactive.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.7
    - pyquery ==1.2.9

  run:
    - python
    - django >=1.7
    - pyquery ==1.2.9

test:
  # Python imports
  imports:
    - interactive
    - interactive.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/CMU-Web-Application-Development/Team71
  license: MIT
  summary: 'Building easier client-side connections'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
