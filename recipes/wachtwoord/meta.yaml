package:
  name: wachtwoord
  version: "0.14"

source:
  fn: wachtwoord-0.14.tar.gz
  url: https://pypi.python.org/packages/source/w/wachtwoord/wachtwoord-0.14.tar.gz
  md5: bfd74e797bfa7f4c77818bcb7eb61181
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - wachtwoord = wachtwoord:main
    #
    # Would create an entry point called wachtwoord that calls wachtwoord.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - wachtwoord

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/gkoller/wachtwoord
  license: 2-clause BSD
  summary: 'Python 3 password hashing library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
