package:
  name: pyvkoauth
  version: "0.9.1"

source:
  fn: pyvkoauth-0.9.1.tar.gz
  url: https://pypi.python.org/packages/source/p/pyvkoauth/pyvkoauth-0.9.1.tar.gz
  md5: 84cb7c238387f87494741eddddde7ddc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pyvkoauth = pyvkoauth:main
    #
    # Would create an entry point called pyvkoauth that calls pyvkoauth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - lxml >=2.3.3

  run:
    - python
    - lxml >=2.3.3

test:
  # Python imports
  imports:
    - pyvkoauth

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/cordalace/pyvkoauth
  license: Apache Software License
  summary: 'OAuth authentification for vk.com'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
