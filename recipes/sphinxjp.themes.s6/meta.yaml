package:
  name: sphinxjp.themes.s6
  version: "0.3.0"

source:
  fn: sphinxjp.themes.s6-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/s/sphinxjp.themes.s6/sphinxjp.themes.s6-0.3.0.tar.gz
  md5: 1ec9535f2af08679b96fbe8a3fe80338
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sphinxjp.themes.s6 = sphinxjp.themes.s6:main
    #
    # Would create an entry point called sphinxjp.themes.s6 that calls sphinxjp.themes.s6.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - sphinx

  run:
    - python
    - setuptools
    - setuptools
    - sphinx

test:
  # Python imports
  imports:
    - sphinxjp
    - sphinxjp.themes
    - sphinxjp.themes.s6

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/shimizukawa/sphinxjp.themes.s6
  license: MIT License
  summary: 'A sphinx theme for generate S6 presentation. #sphinxjp'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
