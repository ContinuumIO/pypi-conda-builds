package:
  name: django-board
  version: "0.2.1"

source:
  fn: django-board-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-board/django-board-0.2.1.tar.gz
  md5: e5e6408aef36a43289786fd70ba9a8ae
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-board = django-board:main
    #
    # Would create an entry point called django-board that calls django-board.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4
    - sorl-thumbnail ==11.12
    - pil ==1.1.7

  run:
    - python
    - django >=1.4
    - sorl-thumbnail ==11.12
    - pil ==1.1.7

test:
  # Python imports
  imports:
    - board
    - board.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/dominicrodger/django-board
  license: BSD License
  summary: "A Django app for managing an organisation's board members page."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
