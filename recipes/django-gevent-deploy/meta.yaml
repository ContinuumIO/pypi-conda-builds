package:
  name: django-gevent-deploy
  version: "1.0.1"

source:
  fn: django-gevent-deploy-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-gevent-deploy/django-gevent-deploy-1.0.1.tar.gz
  md5: c590679d33a4846c9ccc6cb4015f6d15
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-gevent-deploy = django-gevent-deploy:main
    #
    # Would create an entry point called django-gevent-deploy that calls django-gevent-deploy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - django_gevent_deploy
    - django_gevent_deploy.management
    - django_gevent_deploy.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/miki725/django-gevent-deploy
  license: MIT License
  summary: "Django manage.py hook for starting gevent's WSGI server"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
