package:
  name: cmemcache_hash
  version: "0.1.1"

source:
  fn: cmemcache_hash-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/c/cmemcache_hash/cmemcache_hash-0.1.1.tar.gz
  md5: 135cdbc3a23426ccb65fd91f7772708a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cmemcache_hash = cmemcache_hash:main
    #
    # Would create an entry point called cmemcache_hash that calls cmemcache_hash.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - cmemcache_hash

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://lericson.blogg.se/code/category/cmemcache_hash.html
  license: UNKNOWN
  summary: 'cmemcache-style CRC32 hashing for python-memcached.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
