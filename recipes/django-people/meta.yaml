package:
  name: django-people
  version: "1.0"

source:
  fn: django-people-1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-people/django-people-1.0.tar.gz
  md5: 353779f3002ca6adef000052b72a5a15
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-people = django-people:main
    #
    # Would create an entry point called django-people that calls django-people.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - south
    - django-libs
    - django-filer
    - pillow
    - django-localized-names

  run:
    - python
    - django
    - south
    - django-libs
    - django-filer
    - pillow
    - django-localized-names

test:
  # Python imports
  imports:
    - people
    - people.migrations
    - people.tests
    - people.tests.test_app

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bitmazk/django-people
  license: The MIT License
  summary: 'An app for showing person profiles, for example for your "About us" section.\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
