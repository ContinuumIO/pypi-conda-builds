package:
  name: flask-sqlalchemy-cache
  version: "0.1.2"

source:
  fn: Flask-SQLAlchemy-Cache-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-SQLAlchemy-Cache/Flask-SQLAlchemy-Cache-0.1.2.tar.gz
  md5: 3e758b5da4541060e5f8e4ea16f81048
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-sqlalchemy-cache = flask-sqlalchemy-cache:main
    #
    # Would create an entry point called flask-sqlalchemy-cache that calls flask-sqlalchemy-cache.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.9
    - flask-cache
    - flask-sqlalchemy

  run:
    - python
    - flask >=0.9
    - flask-cache
    - flask-sqlalchemy

test:
  # Python imports
  imports:
    - flask_sqlalchemy_cache

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.github.com/iurisilvio/Flask-SQLAlchemy-Cache
  license: MIT License
  summary: 'CachingQuery implementation to Flask using Flask-SQLAlchemy and Flask-Cache'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
