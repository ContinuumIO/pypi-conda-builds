package:
  name: gamebaker
  version: "0.1.10.dev25"

source:
  fn: GameBaker-0.1.10.dev25.zip
  url: https://pypi.python.org/packages/source/G/GameBaker/GameBaker-0.1.10.dev25.zip
  md5: 8638a43cd3a8418b1181d4d9fdb35ce4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gamebaker = gamebaker:main
    #
    # Would create an entry point called gamebaker that calls gamebaker.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - gamebaker

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://pypi.python.org/pypi/GameBaker
  license: MIT License
  summary: 'High level framework for making games and simulations based on Pygame'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
