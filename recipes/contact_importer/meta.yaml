package:
  name: contact_importer
  version: "0.0.1"

source:
  fn: contact_importer-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/contact_importer/contact_importer-0.0.1.tar.gz
  md5: c8cce4a6154a6fe43ba24e10e1d1ab45
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - contact_importer = contact_importer:main
    #
    # Would create an entry point called contact_importer that calls contact_importer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - lxml
    - requests

  run:
    - python
    - setuptools
    - lxml
    - requests

test:
  # Python imports
  imports:
    - contact_importer
    - contact_importer.lib
    - contact_importer.providers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: UNKNOWN
  summary: 'Import Gmail, Live and Yahoo contacts within your Python application.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
