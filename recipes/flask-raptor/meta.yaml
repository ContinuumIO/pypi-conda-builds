package:
  name: flask-raptor
  version: "0.1"

source:
  fn: Flask-Raptor-0.1.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Raptor/Flask-Raptor-0.1.tar.gz
  md5: c2ad8415a1f71f1076b3a25cf5ddcd8c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-raptor = flask-raptor:main
    #
    # Would create an entry point called flask-raptor that calls flask-raptor.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - raptorizemw

  run:
    - python
    - flask
    - raptorizemw

test:
  # Python imports
  imports:
    - flask_raptor

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/dplepage/flask-raptor
  license: BSD License
  summary: 'Raptor support for Flask'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
