package:
  name: django-frontend-static
  version: "1.2.0"

source:
  fn: django-frontend-static-1.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-frontend-static/django-frontend-static-1.2.0.tar.gz
  md5: 508f5255cc4c43420b409c0e2eed8524
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-frontend-static = django-frontend-static:main
    #
    # Would create an entry point called django-frontend-static that calls django-frontend-static.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - djfrontend
    - djfrontend.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/jonfaustman/django-frontend-static
  license: MIT License
  summary: 'A basic Django application to install often used static files.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
