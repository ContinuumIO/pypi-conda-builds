package:
  name: django-mysqlpool
  version: "0.1-9"

source:
  fn: django-mysqlpool-0.1-9.tar.gz
  url: https://pypi.python.org/packages/source/d/django-mysqlpool/django-mysqlpool-0.1-9.tar.gz
  md5: cbc6d88ea5129b799fe139aebb539d02
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-mysqlpool = django-mysqlpool:main
    #
    # Would create an entry point called django-mysqlpool that calls django-mysqlpool.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - sqlalchemy

  run:
    - python
    - sqlalchemy

test:
  # Python imports
  imports:
    - django_mysqlpool
    - django_mysqlpool.backends
    - django_mysqlpool.backends.mysqlpool

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/smartfile/django-mysqlpool/
  license: MIT
  summary: 'Django database backend for MySQL that provides pooling ala SQLAlchemy.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
