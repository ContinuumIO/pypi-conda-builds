package:
  name: ipy-progressbar
  version: "1.0.6"

source:
  fn: ipy-progressbar-1.0.6.tar.gz
  url: https://pypi.python.org/packages/source/i/ipy-progressbar/ipy-progressbar-1.0.6.tar.gz
  md5: 588ae153e04dc6bb904eb63a04b7cf1b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ipy-progressbar = ipy-progressbar:main
    #
    # Would create an entry point called ipy-progressbar that calls ipy-progressbar.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - ipy_progressbar

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://hg.plav.in/my_projects/python/ipy-progressbar
  license: MIT License
  summary: 'Progressbar for both IPython Notebooks (HTML, CSS, JS) and plain terminals.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
