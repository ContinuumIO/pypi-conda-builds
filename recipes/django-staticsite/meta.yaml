package:
  name: django-staticsite
  version: "0.2.1"

source:
  fn: django-staticsite-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-staticsite/django-staticsite-0.2.1.tar.gz
  md5: 79635dc752bc6ee8518b392f99b9835b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-staticsite = django-staticsite:main
    #
    # Would create an entry point called django-staticsite that calls django-staticsite.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - staticsite
    - staticsite/management
    - staticsite/management/commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/carltongibson/django-staticsite
  license: BSD
  summary: 'Django static site generator for DEBUG and production.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
