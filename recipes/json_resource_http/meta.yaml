package:
  name: json_resource_http
  version: "0.1.5"

source:
  fn: json_resource_http-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/j/json_resource_http/json_resource_http-0.1.5.tar.gz
  md5: 383f30308c9f9dcb1c223a1f664fe8eb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - json_resource_http = json_resource_http:main
    #
    # Would create an entry point called json_resource_http that calls json_resource_http.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - json_resource
    - requests

  run:
    - python
    - json_resource
    - requests

test:
  # Python imports
  imports:
    - json_resource_http

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
