package:
  name: evzookeeper
  version: "0.4.0"

source:
  fn: evzookeeper-0.4.0.tar.gz
  url: https://pypi.python.org/packages/source/e/evzookeeper/evzookeeper-0.4.0.tar.gz
  md5: 32270cde7969b36b5f6717babecf4e11
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - evzookeeper = evzookeeper:main
    #
    # Would create an entry point called evzookeeper that calls evzookeeper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - evzookeeper

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/maoy/python-evzookeeper
  license: Apache License (2.0)
  summary: 'zookeeper binding for eventlet'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
