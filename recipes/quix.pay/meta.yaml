package:
  name: quix.pay
  version: "0.1.4"

source:
  fn: quix.pay-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/q/quix.pay/quix.pay-0.1.4.tar.gz
  md5: f90306cf525357293df87c2850dc4805
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - quix.pay = quix.pay:main
    #
    # Would create an entry point called quix.pay that calls quix.pay.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - quix
    - quix.pay
    - quix.pay.gateway

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.quixotix.com
  license: BSD License
  summary: 'Abstract credit card processing with online payment gateways such as Authorize.Net.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
