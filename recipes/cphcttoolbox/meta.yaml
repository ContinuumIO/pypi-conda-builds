package:
  name: cphcttoolbox
  version: "1.1.2"

source:
  fn: cphcttoolbox-1.1.2.zip
  url: https://pypi.python.org/packages/source/c/cphcttoolbox/cphcttoolbox-1.1.2.zip
  md5: c0946b8a992e2529bad48bc3576180fc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cphcttoolbox = cphcttoolbox:main
    #
    # Would create an entry point called cphcttoolbox that calls cphcttoolbox.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy >=1.3

  run:
    - python
    - numpy >=1.3

test:
  # Python imports
  imports:
    - cphct
    - cphct.cl
    - cphct.cl.misc
    - cphct.cl.utils
    - cphct.cone
    - cphct.cone.cl
    - cphct.cone.cu
    - cphct.cone.fdk
    - cphct.cone.fdk.cl
    - cphct.cone.fdk.cl.kernels
    - cphct.cone.fdk.cu
    - cphct.cone.fdk.cu.kernels
    - cphct.cone.fdk.gpu
    - cphct.cone.fdk.npy
    - cphct.cone.fdk.npycore
    - cphct.cone.fdk.npycore.kernels
    - cphct.cone.katsevich
    - cphct.cone.katsevich.cl
    - cphct.cone.katsevich.cl.kernels
    - cphct.cone.katsevich.cu
    - cphct.cone.katsevich.cu.kernels
    - cphct.cone.katsevich.gpu
    - cphct.cone.katsevich.npy
    - cphct.cone.katsevich.npycore
    - cphct.cone.katsevich.npycore.kernels
    - cphct.cone.npy
    - cphct.cone.npycore
    - cphct.cone.npycore.utils
    - cphct.cu
    - cphct.cu.misc
    - cphct.cu.utils
    - cphct.fan
    - cphct.fan.centerfdk
    - cphct.fan.centerfdk.cl
    - cphct.fan.centerfdk.cl.kernels
    - cphct.fan.centerfdk.cu
    - cphct.fan.centerfdk.cu.kernels
    - cphct.fan.centerfdk.npy
    - cphct.fan.centerfdk.npycore
    - cphct.fan.centerfdk.npycore.kernels
    - cphct.fan.cl
    - cphct.fan.cu
    - cphct.fan.npy
    - cphct.fan.npycore
    - cphct.fan.utils
    - cphct.misc
    - cphct.misc.timelog
    - cphct.npy
    - cphct.npycore
    - cphct.npycore.misc
    - cphct.npycore.utils
    - cphct.utils
    - cphctplugins
    - cphctplugins.cl
    - cphctplugins.cl.clip
    - cphctplugins.cl.flux2proj
    - cphctplugins.cl.hounsfield
    - cphctplugins.cl.maskvolumefov
    - cphctplugins.cu
    - cphctplugins.cu.clip
    - cphctplugins.cu.flux2proj
    - cphctplugins.cu.hounsfield
    - cphctplugins.cu.maskvolumefov
    - cphctplugins.npy
    - cphcttoolbox

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://code.google.com/p/cphcttoolbox/
  license: GNU General Public License (GPL)
  summary: 'Cph CT Toolbox is a selection of Computed Tomography tools'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
