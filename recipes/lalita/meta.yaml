package:
  name: lalita
  version: "0.1.1"

source:
  fn: lalita-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/l/lalita/lalita-0.1.1.tar.gz
  md5: 66e461092b961a09bb96dac6742bacb8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lalita = lalita:main
    #
    # Would create an entry point called lalita that calls lalita.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - lalita
    - lalita.core
    - lalita.core.tests
    - lalita.core.tests.plugins
    - lalita.plugins
    - lalita.plugins.randomer_utils
    - lalita.plugins.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://launchpad.net/lalita/
  license: GNU General Public License (GPL)
  summary: 'Yet another IRC bot, one where new functionality is simple to create just adding easy-to-write plugins.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
