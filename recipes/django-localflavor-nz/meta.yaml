package:
  name: django-localflavor-nz
  version: "1.1"

source:
  fn: django-localflavor-nz-1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-localflavor-nz/django-localflavor-nz-1.1.tar.gz
  md5: 39b3ddc05f128ebc3ff20814e7f8b6f3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-localflavor-nz = django-localflavor-nz:main
    #
    # Would create an entry point called django-localflavor-nz that calls django-localflavor-nz.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4

  run:
    - python
    - django >=1.4

test:
  # Python imports
  imports:
    - django_localflavor_nz

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/markuz/django-localflavor-nz
  license: BSD License
  summary: 'Country-specific Django helpers for New Zealand.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
