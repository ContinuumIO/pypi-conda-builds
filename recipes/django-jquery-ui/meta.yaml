package:
  name: django-jquery-ui
  version: "1.10.3.1"

source:
  fn: django-jquery-ui-1.10.3.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-jquery-ui/django-jquery-ui-1.10.3.1.tar.gz
  md5: 0181754165d9ee999e2083b58820f341
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-jquery-ui = django-jquery-ui:main
    #
    # Would create an entry point called django-jquery-ui that calls django-jquery-ui.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-jquery >=1.6

  run:
    - python
    - django-jquery >=1.6

test:
  # Python imports
  imports:
    - jquery_ui

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/benbacardi/django-jquery-ui
  license: MIT License
  summary: 'jQuery UI packaged in a django app to speed up new applications and deployment.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
