package:
  name: micropython-stat
  version: "0.5"

source:
  fn: micropython-stat-0.5.tar.gz
  url: https://pypi.python.org/packages/source/m/micropython-stat/micropython-stat-0.5.tar.gz
  md5: 69356eaf789938e654c891d8b3802bc2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - micropython-stat = micropython-stat:main
    #
    # Would create an entry point called micropython-stat that calls micropython-stat.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/micropython/micropython/issues/405
  license: Python
  summary: 'CPython stat module ported to MicroPython'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
