package:
  name: bootstrap-admin-feincms
  version: "0.0.4"

source:
  fn: bootstrap-admin-feincms-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/b/bootstrap-admin-feincms/bootstrap-admin-feincms-0.0.4.tar.gz
  md5: 6cad77e3d41fae5004f8fe235f88be8d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bootstrap-admin-feincms = bootstrap-admin-feincms:main
    #
    # Would create an entry point called bootstrap-admin-feincms that calls bootstrap-admin-feincms.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD License
  summary: 'Responsive Theme for Django Admin (Django 1.7+)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
