package:
  name: logging_yamlconfig
  version: "0.1.4"

source:
  fn: logging_yamlconfig-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/l/logging_yamlconfig/logging_yamlconfig-0.1.4.tar.gz
  md5: b587eec29da68f41f6f3174ccc6362d5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - logging_yamlconfig = logging_yamlconfig:main
    #
    # Would create an entry point called logging_yamlconfig that calls logging_yamlconfig.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyyaml >=3.11,<3.12
    - logutils ==0.3.3

  run:
    - python
    - pyyaml >=3.11,<3.12
    - logutils ==0.3.3

test:
  # Python imports
  imports:
    - logging_yamlconfig

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dyejon/python_logging_yamlconfig
  license: UNKNOWN
  summary: 'Python dictconfig helpers for yaml config files'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
