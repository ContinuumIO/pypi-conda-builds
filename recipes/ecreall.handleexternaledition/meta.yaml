package:
  name: ecreall.handleexternaledition
  version: "0.2"

source:
  fn: ecreall.handleexternaledition-0.2.zip
  url: https://pypi.python.org/packages/source/e/ecreall.handleexternaledition/ecreall.handleexternaledition-0.2.zip
  md5: 6b7b773f0a25fa3b04813225048c8b12
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ecreall.handleexternaledition = ecreall.handleexternaledition:main
    #
    # Would create an entry point called ecreall.handleexternaledition that calls ecreall.handleexternaledition.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - products.archetypes
    - products.cmfeditions
    - products.externaleditor
    - collective.documentviewer
    - collective.monkeypatcher
    - plone.api
    - setuptools

  run:
    - python
    - setuptools
    - products.archetypes
    - products.cmfeditions
    - products.externaleditor
    - collective.documentviewer
    - collective.monkeypatcher
    - plone.api
    - setuptools

test:
  # Python imports
  imports:
    - ecreall
    - ecreall.handleexternaledition
    - ecreall.handleexternaledition.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/ecreall.handleexternaledition
  license: GPL
  summary: 'Avoid documentviewer and history generation when we save during a zopeedit session'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
