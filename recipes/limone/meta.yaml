package:
  name: limone
  version: "0.1a5"

source:
  fn: limone-0.1a5.tar.gz
  url: https://pypi.python.org/packages/source/l/limone/limone-0.1a5.tar.gz
  md5: 961a16e1b2b738f7c847c68d84350b7a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - limone = limone:main
    #
    # Would create an entry point called limone that calls limone.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - colander
    - venusian

  run:
    - python
    - setuptools
    - colander
    - venusian

test:
  # Python imports
  imports:
    - limone

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pylonsproject.org
  license: BSD-derived (http://www.repoze.org/LICENSE.txt)
  summary: 'Content type system based on colander schemas.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
