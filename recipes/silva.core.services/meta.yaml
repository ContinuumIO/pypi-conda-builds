package:
  name: silva.core.services
  version: "3.0.1"

source:
  fn: silva.core.services-3.0.1.tar.gz
  url: https://pypi.python.org/packages/source/s/silva.core.services/silva.core.services-3.0.1.tar.gz
  md5: abf2aaa0c2f31b63db5b0a26f453d129
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - silva.core.services = silva.core.services:main
    #
    # Would create an entry point called silva.core.services that calls silva.core.services.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - products.zcatalog
    - five.grok
    - five.intid
    - setuptools
    - silva.core.conf
    - silva.core.interfaces
    - zope.component
    - zope.interface
    - zope.intid
    - zope.schema

  run:
    - python
    - products.zcatalog
    - five.grok
    - five.intid
    - setuptools
    - silva.core.conf
    - silva.core.interfaces
    - zope.component
    - zope.interface
    - zope.intid
    - zope.schema

test:
  # Python imports
  imports:
    - silva
    - silva.core
    - silva.core.services
    - silva.core.services.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/silvacms/silva.core.services
  license: BSD License
  summary: 'Configuration services for Silva CMS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
