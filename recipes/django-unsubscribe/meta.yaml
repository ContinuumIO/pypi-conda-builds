package:
  name: django-unsubscribe
  version: "0.1.2"

source:
  fn: django-unsubscribe-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-unsubscribe/django-unsubscribe-0.1.2.tar.gz
  md5: ab1c7645bfb1661aab2735e3e87f6996
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-unsubscribe = django-unsubscribe:main
    #
    # Would create an entry point called django-unsubscribe that calls django-unsubscribe.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - unsubscribe
    - unsubscribe/runtests
    - unsubscribe/tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/theskumar/django-unsubscribe
  license: BSD License
  summary: 'Add unsubscribe link to your emails painlessly.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
