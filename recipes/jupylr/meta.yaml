package:
  name: jupylr
  version: "0.3"

source:
  fn: jupyLR-0.3.tar.gz
  url: https://pypi.python.org/packages/source/j/jupyLR/jupyLR-0.3.tar.gz
  md5: 603b81a597d3de3b3adf9e77cbecd642
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jupylr = jupylr:main
    #
    # Would create an entry point called jupylr that calls jupylr.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - jupyLR
    - jupylr

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bl0b.github.com/jupyLR/
  license: UNKNOWN
  summary: 'GLR parsing for python with on-line AST validation and basic error reporting.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
