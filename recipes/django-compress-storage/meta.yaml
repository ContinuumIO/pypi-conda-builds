package:
  name: django-compress-storage
  version: "0.9.1"

source:
  fn: django-compress-storage-0.9.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-compress-storage/django-compress-storage-0.9.1.tar.gz
  md5: f381d4f8556364d65c5c4b43fcd270df
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-compress-storage = django-compress-storage:main
    #
    # Would create an entry point called django-compress-storage that calls django-compress-storage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.2

  run:
    - python
    - django >=1.2

test:
  # Python imports
  imports:
    - compress_storage

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/valdergallo/django-compress-storage
  license: FREEBSD
  summary: 'Automantic compress files after upload'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
