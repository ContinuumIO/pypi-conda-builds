package:
  name: cloudflare
  version: "0.0.1"

source:
  fn: cloudflare-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/cloudflare/cloudflare-0.0.1.tar.gz
  md5: b87388484b0bd8bbeca3e663a7412054
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cloudflare = cloudflare:main
    #
    # Would create an entry point called cloudflare that calls cloudflare.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - cloudflare

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/darylyu/cloudflare
  license: BSD License
  summary: "STILL UNDER DEVELOPMENT! Provides a CLI and Python wrappers for CloudFlare's REST API"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
