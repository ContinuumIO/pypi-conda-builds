package:
  name: django-dbpreferences
  version: "0.5.0.0212"

source:
  fn: django-dbpreferences-0.5.0.0212.tar.gz
  url: https://pypi.python.org/packages/source/d/django-dbpreferences/django-dbpreferences-0.5.0.0212.tar.gz
  md5: 35da4efc39bb6b88f1b3635d1b105cd7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-dbpreferences = django-dbpreferences:main
    #
    # Would create an entry point called django-dbpreferences that calls django-dbpreferences.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5

  run:
    - python
    - django >=1.5

test:
  # Python imports
  imports:
    - dbpreferences
    - dbpreferences.tests
    - dbpreferences.tools

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/django-dbpreferences/
  license: GNU General Public License (GPL)
  summary: 'With django-dbpreferences you can store app/user settings into the database.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
