package:
  name: logginggoodies
  version: "0.0.1"

source:
  fn: logginggoodies-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/l/logginggoodies/logginggoodies-0.0.1.tar.gz
  md5: 5f25e643956e1356c69cf41a70015d8d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - logginggoodies = logginggoodies:main
    #
    # Would create an entry point called logginggoodies that calls logginggoodies.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - logginggoodies

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/brownan/Logging-Goodies
  license: MIT
  summary: 'Logging Goodies'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
