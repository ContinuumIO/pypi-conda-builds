package:
  name: collective.simplesocial
  version: "1.8"

source:
  fn: collective.simplesocial-1.8.zip
  url: https://pypi.python.org/packages/source/c/collective.simplesocial/collective.simplesocial-1.8.zip
  md5: e9075870bdd5a49eb6c91596c2cef3b8
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.simplesocial = collective.simplesocial:main
    #
    # Would create an entry point called collective.simplesocial that calls collective.simplesocial.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.autopermission
    - plone.app.z3cform

  run:
    - python
    - setuptools
    - setuptools
    - collective.autopermission
    - plone.app.z3cform

test:
  # Python imports
  imports:
    - collective
    - collective.simplesocial
    - collective.simplesocial.analytics
    - collective.simplesocial.browser
    - collective.simplesocial.fanbox
    - collective.simplesocial.fanpage_post
    - collective.simplesocial.feedform
    - collective.simplesocial.likebutton
    - collective.simplesocial.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/simplesocial
  license: GNU General Public License (GPL)
  summary: 'Basic Facebook Connect support for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
