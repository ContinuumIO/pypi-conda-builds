package:
  name: zope.configuration
  version: "4.0.3"

source:
  fn: zope.configuration-4.0.3.tar.gz
  url: https://pypi.python.org/packages/source/z/zope.configuration/zope.configuration-4.0.3.tar.gz
  md5: d85ff342b524bfa819916632d5ef096a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zope.configuration = zope.configuration:main
    #
    # Would create an entry point called zope.configuration that calls zope.configuration.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - zope.i18nmessageid
    - zope.interface
    - zope.schema
    - setuptools

  run:
    - python
    - zope.i18nmessageid
    - zope.interface
    - zope.schema
    - setuptools

test:
  # Python imports
  imports:
    - zope
    - zope.configuration
    - zope.configuration.tests
    - zope.configuration.tests.excludedemo
    - zope.configuration.tests.excludedemo.sub
    - zope.configuration.tests.samplepackage

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/zope.configuration
  license: Zope Public License
  summary: 'Zope Configuration Markup Language (ZCML)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
