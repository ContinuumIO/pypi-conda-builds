package:
  name: kotti_software
  version: "0.1.4"

source:
  fn: kotti_software-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/k/kotti_software/kotti_software-0.1.4.tar.gz
  md5: 798b819cab3b2ff4997b93567b0a174e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kotti_software = kotti_software:main
    #
    # Would create an entry point called kotti_software that calls kotti_software.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - kotti >=0.7
    - plone.batching
    - accesscontrol
    - js.jquery_infinite_ajax_scroll
    - js.jquery_form
    - python-dateutil

  run:
    - python
    - kotti >=0.7
    - plone.batching
    - accesscontrol
    - js.jquery_infinite_ajax_scroll
    - js.jquery_form
    - python-dateutil

test:
  # Python imports
  imports:
    - kotti_software

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/geojeff/kotti_software
  license:  BSD-derived (http://www.repoze.org/LICENSE.txt)
  summary: 'Kotti software -- adds a software project listing to your site'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
