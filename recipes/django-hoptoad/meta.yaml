package:
  name: django-hoptoad
  version: "0.2"

source:
  fn: django-hoptoad-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-hoptoad/django-hoptoad-0.2.tar.gz
  md5: 3442139d9f0eef58a3968335cecbe899
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-hoptoad = django-hoptoad:main
    #
    # Would create an entry point called django-hoptoad that calls django-hoptoad.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - hoptoad

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://stevelosh.com/projects/django-hoptoad/
  license: MIT/X11
  summary: 'django-hoptoad is some simple Middleware for letting Django-driven websites report their errors to Hoptoad.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
