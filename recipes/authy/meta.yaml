package:
  name: authy
  version: "2.1.2"

source:
  fn: authy-2.1.2.tar.gz
  url: https://pypi.python.org/packages/source/a/authy/authy-2.1.2.tar.gz
  md5: cf12b132631c15a9c0f626d76d1256fc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - authy = authy:main
    #
    # Would create an entry point called authy that calls authy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=2.2.1
    - simplejson >=3.4.0

  run:
    - python
    - requests >=2.2.1
    - simplejson >=3.4.0

test:
  # Python imports
  imports:
    - authy
    - authy.api
    - authy.contrib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/authy/authy-python
  license: MIT License
  summary: 'Authy API Client'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
