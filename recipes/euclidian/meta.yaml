package:
  name: euclidian
  version: "0.8"

source:
  fn: euclidian-0.8.tar.gz
  url: https://pypi.python.org/packages/source/e/euclidian/euclidian-0.8.tar.gz
  md5: 3867a3be2ff42c5eecd28d5129a6bbb8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - euclidian = euclidian:main
    #
    # Would create an entry point called euclidian that calls euclidian.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - euclidian

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.sixty-north.com/euclidian
  license: MIT License
  summary: 'Simple 2D and 3D geometric primitives and operations'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
