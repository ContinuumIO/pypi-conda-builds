package:
  name: fft-tools
  version: "0.5"

source:
  fn: FFT-tools-0.5.tar.gz
  url: https://pypi.python.org/packages/source/F/FFT-tools/FFT-tools-0.5.tar.gz
  md5: 48f179c7b9cc9a0e684cfdd4625b3b5e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fft-tools = fft-tools:main
    #
    # Would create an entry point called fft-tools that calls fft-tools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://blog.tremily.us/posts/FFT-tools/
  license: GNU General Public License (GPL) or GNU General Public License v3 (GPLv3)
  summary: "Wrap NumpPy's FFT routines to reduce clutter."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
