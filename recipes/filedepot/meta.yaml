package:
  name: filedepot
  version: "0.0.4"

source:
  fn: filedepot-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/f/filedepot/filedepot-0.0.4.tar.gz
  md5: 83781f69314f90f1bd808a719279ade8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - filedepot = filedepot:main
    #
    # Would create an entry point called filedepot that calls filedepot.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - unidecode

  run:
    - python
    - unidecode

test:
  # Python imports
  imports:
    - depot
    - depot.fields
    - depot.fields.filters
    - depot.fields.specialized
    - depot.io
    - filedepot

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/amol-/depot
  license: MIT
  summary: 'Toolkit for storing files and attachments in web applications'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
