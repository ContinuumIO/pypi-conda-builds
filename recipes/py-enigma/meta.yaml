package:
  name: py-enigma
  version: "0.1"

source:
  fn: py-enigma-0.1.tar.gz
  url: https://pypi.python.org/packages/source/p/py-enigma/py-enigma-0.1.tar.gz
  md5: 216f3e9ee49aab981f0f6f365ca21729
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - py-enigma = py-enigma:main
    #
    # Would create an entry point called py-enigma that calls py-enigma.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - enigma
    - enigma.rotors
    - enigma.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/bgneal/enigma/
  license: MIT License
  summary: 'A historically accurate Enigma machine simulation library.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
