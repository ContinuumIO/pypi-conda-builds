package:
  name: easyshop.core
  version: "0.1a1"

source:
  fn: easyshop.core-0.1a1.tar.gz
  url: https://pypi.python.org/packages/source/e/easyshop.core/easyshop.core-0.1a1.tar.gz
  md5: f41bba1f1cbd38fc8563143119bea382
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - easyshop.core = easyshop.core:main
    #
    # Would create an entry point called easyshop.core that calls easyshop.core.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - easyshop.carts
    - easyshop.catalog
    - easyshop.checkout
    - easyshop.criteria
    - easyshop.customers
    - easyshop.discounts
    - easyshop.groups
    - easyshop.information
    - easyshop.kss
    - easyshop.login
    - easyshop.management
    - easyshop.order
    - easyshop.payment
    - easyshop.shipping
    - easyshop.shop
    - easyshop.stocks
    - easyshop.taxes
    - zc.authorizedotnet
    - products.datagridfield

  run:
    - python
    - setuptools
    - setuptools
    - easyshop.carts
    - easyshop.catalog
    - easyshop.checkout
    - easyshop.criteria
    - easyshop.customers
    - easyshop.discounts
    - easyshop.groups
    - easyshop.information
    - easyshop.kss
    - easyshop.login
    - easyshop.management
    - easyshop.order
    - easyshop.payment
    - easyshop.shipping
    - easyshop.shop
    - easyshop.stocks
    - easyshop.taxes
    - zc.authorizedotnet
    - products.datagridfield

test:
  # Python imports
  imports:
    - easyshop
    - easyshop.core
    - easyshop.core.Extensions
    - easyshop.core.browser
    - easyshop.core.interfaces
    - easyshop.core.profiles
    - easyshop.core.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://iqpp.de
  license: GPL
  summary: 'An out-of-the-box online shop for Plone.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
