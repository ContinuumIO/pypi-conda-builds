package:
  name: django-gcharts
  version: "1.3"

source:
  fn: django-gcharts-1.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-gcharts/django-gcharts-1.3.tar.gz
  md5: 2a01d08a85cf955361b14dfa1afcb3b0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-gcharts = django-gcharts:main
    #
    # Would create an entry point called django-gcharts that calls django-gcharts.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - gcharts
    - gcharts.contrib
    - gcharts.templatetags
    - gcharts.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/rhblind/django-gcharts
  license: Apache Software License
  summary: 'Provides a QuerySet, Manager and other tools for easy integration with the Google Visualization API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
