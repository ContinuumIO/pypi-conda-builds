package:
  name: django-turbolinks
  version: "0.4"

source:
  fn: django-turbolinks-0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-turbolinks/django-turbolinks-0.4.tar.gz
  md5: d33c8af8e439aba38c10391b7a9642c9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-turbolinks = django-turbolinks:main
    #
    # Would create an entry point called django-turbolinks that calls django-turbolinks.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6

  run:
    - python
    - django >=1.6

test:
  # Python imports
  imports:
    - turbolinks

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dgladkov/django-turbolinks
  license: MIT License
  summary: u'Drop-in turbolinks implementation for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
