package:
  name: mozb2g
  version: "0.4"

source:
  fn: mozb2g-0.4.tar.gz
  url: https://pypi.python.org/packages/source/m/mozb2g/mozb2g-0.4.tar.gz
  md5: cc42d1c406f99d9e3ed9703dbe91cf2e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mozb2g = mozb2g:main
    #
    # Would create an entry point called mozb2g that calls mozb2g.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - mozdevice >=0.16
    - marionette_client >=0.5.2
    - mozfile >=1.0

  run:
    - python
    - mozdevice >=0.16
    - marionette_client >=0.5.2
    - mozfile >=1.0

test:
  # Python imports
  imports:
    - mozb2g

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://wiki.mozilla.org/Auto-tools/Projects/Mozbase
  license: MPL
  summary: 'B2G specific code for device automation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
