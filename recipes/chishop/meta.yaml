package:
  name: chishop
  version: "0.2.0"

source:
  fn: chishop-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/c/chishop/chishop-0.2.0.tar.gz
  md5: 81306ae136fa1f5d5b95dc4a7118d6a1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - chishop = chishop:main
    #
    # Would create an entry point called chishop that calls chishop.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.0

  run:
    - python
    - django >=1.0

test:
  # Python imports
  imports:
    - chishop
    - djangopypi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://ask.github.com/chishop
  license: BSD License
  summary: 'Simple PyPI server written in Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
