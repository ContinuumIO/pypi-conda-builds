package:
  name: django-request-tracker
  version: "0.2.1"

source:
  fn: django-request-tracker-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-request-tracker/django-request-tracker-0.2.1.tar.gz
  md5: 8e23f8c6e33300b0ba27860aa4aeb335
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-request-tracker = django-request-tracker:main
    #
    # Would create an entry point called django-request-tracker that calls django-request-tracker.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - tracker

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/eofs/django-request-tracker
  license: BSD
  summary: 'Track incoming requests using analytic services.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
