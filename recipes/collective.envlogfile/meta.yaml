package:
  name: collective.envlogfile
  version: "1.0.0"

source:
  fn: collective.envlogfile-1.0.0.zip
  url: https://pypi.python.org/packages/source/c/collective.envlogfile/collective.envlogfile-1.0.0.zip
  md5: eab1a99fb660f1ce6d366150e9f7700d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.envlogfile = collective.envlogfile:main
    #
    # Would create an entry point called collective.envlogfile that calls collective.envlogfile.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zconfig

  run:
    - python
    - setuptools
    - setuptools
    - zconfig

test:
  # Python imports
  imports:
    - collective
    - collective.envlogfile

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/datakurre/collective.envlogfile
  license: EUPL
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
