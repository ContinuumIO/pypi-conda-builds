package:
  name: testkit
  version: "0.1.7"

source:
  fn: testkit-0.1.7.tar.gz
  url: https://pypi.python.org/packages/source/t/testkit/testkit-0.1.7.tar.gz
  md5: a56409db0b9341020efe71e22d274e47
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - testkit = testkit:main
    #
    # Would create an entry point called testkit that calls testkit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fudge

  run:
    - python
    - fudge

test:
  # Python imports
  imports:
    - testkit

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ravenac95/testkit
  license: MIT License
  summary: 'testkit - A collection of tools for testing'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
