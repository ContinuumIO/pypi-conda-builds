package:
  name: inputexec
  version: "0.2.4"

source:
  fn: inputexec-0.2.4.tar.gz
  url: https://pypi.python.org/packages/source/i/inputexec/inputexec-0.2.4.tar.gz
  md5: 8a186862a2e6f63f2dd6273dccf59b77
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - inputexec = inputexec:main
    #
    # Would create an entry point called inputexec that calls inputexec.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - evdev

  run:
    - python
    - evdev

test:
  # Python imports
  imports:
    - inputexec
    - inputexec.readers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rbarrois/inputexec
  license: BSD License
  summary: 'Simple Python program to execute commands on keypress on headless systems'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
