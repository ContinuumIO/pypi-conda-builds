package:
  name: zc.recipe.cmmi
  version: "1.3.6"

source:
  fn: zc.recipe.cmmi-1.3.6.zip
  url: https://pypi.python.org/packages/source/z/zc.recipe.cmmi/zc.recipe.cmmi-1.3.6.zip
  md5: e55102cb2acc335ef8981c7eb448573a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zc.recipe.cmmi = zc.recipe.cmmi:main
    #
    # Would create an entry point called zc.recipe.cmmi that calls zc.recipe.cmmi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zc.buildout >=1.4
    - setuptools

  run:
    - python
    - setuptools
    - zc.buildout >=1.4
    - setuptools

test:
  # Python imports
  imports:
    - zc
    - zc.recipe
    - zc.recipe.cmmi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/zc.recipe.cmmi
  license: Zope Public License
  summary: 'ZC Buildout recipe for configure/make/make install'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
