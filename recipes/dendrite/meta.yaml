package:
  name: dendrite
  version: "1.1.3"

source:
  fn: dendrite-1.1.3.tar.gz
  url: https://pypi.python.org/packages/source/d/dendrite/dendrite-1.1.3.tar.gz
  md5: 076b3853c2a7a2cbf6968fc0f3f71c01
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dendrite = dendrite:main
    #
    # Would create an entry point called dendrite that calls dendrite.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - mock >=1.0,<1.1
    - pretend >=1.0,<1.1
    - requests-oauthlib >=0.3,<0.4
    - requests >=1.2,<1.3
    - shortuuid >=0.1,<0.2

  run:
    - python
    - mock >=1.0,<1.1
    - pretend >=1.0,<1.1
    - requests-oauthlib >=0.3,<0.4
    - requests >=1.2,<1.3
    - shortuuid >=0.1,<0.2

test:
  # Python imports
  imports:
    - dendrite

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/caffeinehit/dendrite
  license: DendroPy
  summary: 'Social connectivitiy as a library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
