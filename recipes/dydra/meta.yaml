package:
  name: dydra
  version: "0.0.2"

source:
  fn: dydra-0.0.2.tar.bz2
  url: https://pypi.python.org/packages/source/d/dydra/dydra-0.0.2.tar.bz2
  md5: 2a5a49f81b799955673b74c0f7244a0e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dydra = dydra:main
    #
    # Would create an entry point called dydra that calls dydra.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - rdflib >=3.0.0

  run:
    - python
    - rdflib >=3.0.0

test:
  # Python imports
  imports:
    - dydra

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://docs.dydra.com/sdk/python
  license: Public Domain
  summary: 'Dydra.com software development kit (SDK) for Python.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
