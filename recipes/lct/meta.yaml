package:
  name: lct
  version: "0.4.0"

source:
  fn: lct-0.4.0.tar.gz
  url: https://pypi.python.org/packages/source/l/lct/lct-0.4.0.tar.gz
  md5: 9ce1f6540724f03b1150ad3a4114af24
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lct = lct:main
    #
    # Would create an entry point called lct that calls lct.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyephem
    - qdarkstyle
    - tzlocal

  run:
    - python
    - pyephem
    - qdarkstyle
    - tzlocal

test:
  # Python imports
  imports:
    - lct
    - lct.features
    - lct.ui
    - lct.ui.widgets
    - lct.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mareuter/lct-python
  license: MIT License
  summary: 'Lunar Club Tools'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
