package:
  name: flask_raven
  version: "0.0.4"

source:
  fn: flask_raven-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/f/flask_raven/flask_raven-0.0.4.tar.gz
  md5: 52f6753403693c7d5590373ec5146d3f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask_raven = flask_raven:main
    #
    # Would create an entry point called flask_raven that calls flask_raven.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - pycrypto

  run:
    - python
    - flask
    - pycrypto

test:
  # Python imports
  imports:
    - flask_raven

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/danielchatfield/flask-raven/
  license: Artistic 2.0
  summary: "A flask extension for the University of Cambridge's authentication system"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
