package:
  name: easyemail
  version: "0.4.0"

source:
  fn: easyemail-0.4.0.tar.gz
  url: https://pypi.python.org/packages/source/e/easyemail/easyemail-0.4.0.tar.gz
  md5: 070d0c7281d455b4f3263ceaac63032b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - easyemail = easyemail:main
    #
    # Would create an entry point called easyemail that calls easyemail.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - easyemail

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/niktto/easyemail/
  license: BSD License
  summary: 'Simple lib abstracting email sending with smtplib.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
