package:
  name: funcparserlib
  version: "0.3.6"

source:
  fn: funcparserlib-0.3.6.tar.gz
  url: https://pypi.python.org/packages/source/f/funcparserlib/funcparserlib-0.3.6.tar.gz
  md5: 3aba546bdad5d0826596910551ce37c0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - funcparserlib = funcparserlib:main
    #
    # Would create an entry point called funcparserlib that calls funcparserlib.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - funcparserlib
    - funcparserlib.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/funcparserlib/
  license: MIT License
  summary: 'Recursive descent parsing library based on functional combinators'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
