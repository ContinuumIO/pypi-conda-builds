package:
  name: gatspy
  version: "0.1.1"

source:
  fn: gatspy-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/g/gatspy/gatspy-0.1.1.tar.gz
  md5: 85891edad804bb7576a000deecf68d5a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gatspy = gatspy:main
    #
    # Would create an entry point called gatspy that calls gatspy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - gatspy
    - gatspy.datasets
    - gatspy.datasets.tests
    - gatspy.periodic
    - gatspy.periodic.tests
    - gatspy.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/jakevdp/gatspy
  license: BSD License
  summary: 'General tools for Astronomical Time Series in Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
