package:
  name: glint-service
  version: "2013.2.4.12.dev"

source:
  fn: glint-service-2013.2.4.12.dev.tar.gz
  url: https://pypi.python.org/packages/source/g/glint-service/glint-service-2013.2.4.12.dev.tar.gz
  md5: 3e9b93f45f1cdc2f64a16f535e56ae03
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - glint-service = glint-service:main
    #
    # Would create an entry point called glint-service that calls glint-service.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - glint-service

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/hep-gc/glint-service
  license: Apache Software License
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
