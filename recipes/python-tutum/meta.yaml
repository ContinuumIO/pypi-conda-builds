package:
  name: python-tutum
  version: "0.14.1"

source:
  fn: python-tutum-0.14.1.tar.gz
  url: https://pypi.python.org/packages/source/p/python-tutum/python-tutum-0.14.1.tar.gz
  md5: 035933457824123c9c94c79ed8b8af10
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-tutum = python-tutum:main
    #
    # Would create an entry point called python-tutum that calls python-tutum.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=2.2.1
    - future >=0.14.3
    - websocket-client >=0.11.0

  run:
    - python
    - requests >=2.2.1
    - future >=0.14.3
    - websocket-client >=0.11.0

test:
  # Python imports
  imports:
    - tests
    - tutum
    - tutum.api

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.tutum.co/
  license: Apache v2
  summary: 'Python Library for Tutum'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
