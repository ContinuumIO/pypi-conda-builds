package:
  name: black_nester
  version: "1.22"

source:
  fn: black_nester-1.22.tar.gz
  url: https://pypi.python.org/packages/source/b/black_nester/black_nester-1.22.tar.gz
  md5: b0e5631adc9007cd96b65ee6a5230721
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - black_nester = black_nester:main
    #
    # Would create an entry point called black_nester that calls black_nester.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - black_nester

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: reckhhh.github.io
  license: UNKNOWN
  summary: 'A simple printer of nested lists'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
