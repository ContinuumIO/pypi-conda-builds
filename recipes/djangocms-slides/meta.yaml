package:
  name: djangocms-slides
  version: "1.0.1"

source:
  fn: djangocms-slides-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/djangocms-slides/djangocms-slides-1.0.1.tar.gz
  md5: d96e4c6f16cbd9a4f45fe9d651fd0f8d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangocms-slides = djangocms-slides:main
    #
    # Would create an entry point called djangocms-slides that calls djangocms-slides.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-cms >=2.4
    - django-filer
    - djangocms-text-ckeditor

  run:
    - python
    - django-cms >=2.4
    - django-filer
    - djangocms-text-ckeditor

test:
  # Python imports
  imports:
    - djangocms_slides
    - djangocms_slides.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.django-cms.org/
  license: MIT License
  summary: 'djangoCMS plugin for simple features, galleries or sliders'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
