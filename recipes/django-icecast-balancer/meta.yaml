package:
  name: django-icecast-balancer
  version: "0.1.4"

source:
  fn: django-icecast-balancer-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-icecast-balancer/django-icecast-balancer-0.1.4.tar.gz
  md5: 7f41e7d80a42bb2802c086e6ec2f3bda
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-icecast-balancer = django-icecast-balancer:main
    #
    # Would create an entry point called django-icecast-balancer that calls django-icecast-balancer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - icecast_balancer
    - icecast_balancer.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/django-icecast-balancer/
  license: BSD License
  summary: 'A simple HTTP based load balancer for Icecast streaming servers.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
