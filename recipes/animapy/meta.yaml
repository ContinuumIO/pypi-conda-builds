package:
  name: animapy
  version: "1.5.3.1"

source:
  fn: Animapy-1.5.3.1.tar.gz
  url: https://pypi.python.org/packages/source/A/Animapy/Animapy-1.5.3.1.tar.gz
  md5: 436187fb5869c5d129eb59f8bf132911
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - animapy = animapy:main
    #
    # Would create an entry point called animapy that calls animapy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - beautifulsoup4

  run:
    - python
    - beautifulsoup4

test:
  # Python imports
  imports:
    - animapy
    - animapy.helpers
    - animapy.sources

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/JWebCoder/animapy
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'An anime API for python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
