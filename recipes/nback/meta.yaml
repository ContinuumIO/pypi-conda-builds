package:
  name: nback
  version: "0.1.16"

source:
  fn: nback-0.1.16.tar.gz
  url: https://pypi.python.org/packages/source/n/nback/nback-0.1.16.tar.gz
  md5: e474c66067d277e224a0efcf4102fff4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - nback = nback:main
    #
    # Would create an entry point called nback that calls nback.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - boto

  run:
    - python
    - boto

test:
  # Python imports
  imports:
    - nback
    - nback/lib
    - nback/lib/db
    - nback/lib/storage

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Niklas9/nback
  license: MIT
  summary: 'Backups files and databases (MySQL, MongoDB) to AWS S3'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
