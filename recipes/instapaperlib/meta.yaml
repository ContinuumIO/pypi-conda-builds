package:
  name: instapaperlib
  version: "0.4.0"

source:
  fn: instapaperlib-0.4.0.tar.gz
  url: https://pypi.python.org/packages/source/i/instapaperlib/instapaperlib-0.4.0.tar.gz
  md5: c6f4c0cfeed03a5b417fa07f9e89d35d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - instapaperlib = instapaperlib:main
    #
    # Would create an entry point called instapaperlib that calls instapaperlib.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - instapaperlib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/mrtazz/InstapaperLibrary
  license: MIT License
  summary: 'Python library for the instapaper.com API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
