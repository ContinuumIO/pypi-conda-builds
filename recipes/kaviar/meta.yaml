package:
  name: kaviar
  version: "1.0"

source:
  fn: kaviar-1.0.zip
  url: https://pypi.python.org/packages/source/k/kaviar/kaviar-1.0.zip
  md5: de1d799c914969f52f633f5b178d703e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kaviar = kaviar:main
    #
    # Would create an entry point called kaviar that calls kaviar.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six
    - namedlist

  run:
    - python
    - six
    - namedlist

test:
  # Python imports
  imports:
    - kaviar

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/eisensheng/kaviar
  license: MIT License
  summary: 'Simplified event and data formatting and logging.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
