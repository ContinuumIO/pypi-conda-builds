package:
  name: inquant.recipe.download
  version: "0.1.1dev-r1946"

source:
  fn: inquant.recipe.download-0.1.1dev-r1946.tar.gz
  url: https://pypi.python.org/packages/source/i/inquant.recipe.download/inquant.recipe.download-0.1.1dev-r1946.tar.gz
  md5: 574828934ca55673469aea41ececc7e9
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - inquant.recipe.download = inquant.recipe.download:main
    #
    # Would create an entry point called inquant.recipe.download that calls inquant.recipe.download.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - gocept.download
    - zc.buildout
    - setuptools

  run:
    - python
    - setuptools
    - gocept.download
    - zc.buildout
    - setuptools

test:
  # Python imports
  imports:
    - inquant
    - inquant.recipe
    - inquant.recipe.download

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://svn.inquant.de/inquant/develop/plone/recipes/inquant.recipe.download
  license: LGPL
  summary: 'A buildout recipe based on the gocept.download recipe which allows to clean out the destination.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
