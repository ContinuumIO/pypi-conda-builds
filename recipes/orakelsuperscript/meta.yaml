package:
  name: orakelsuperscript
  version: "0.4"

source:
  fn: orakelsuperscript-0.4.tar.gz
  url: https://pypi.python.org/packages/source/o/orakelsuperscript/orakelsuperscript-0.4.tar.gz
  md5: e8e8eec1dd72f85832d9be9a11d59c74
#  patches:
   # List any patch files here
   # - fix.patch

build:
  # preserve_egg_dir: True
  entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - orakelsuperscript = orakelsuperscript:main
    #
    # Would create an entry point called orakelsuperscript that calls orakelsuperscript.main()

    - orakelsuperscript=orakelsuperscript:main

  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - orakelsuperscript

  commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.

    - orakelsuperscript --help

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
