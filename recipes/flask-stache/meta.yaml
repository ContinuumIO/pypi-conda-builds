package:
  name: flask-stache
  version: "0.1.1"

source:
  fn: Flask-Stache-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Stache/Flask-Stache-0.1.1.tar.gz
  md5: 4d6478d0c24d078ef97ed5ffe14ad286
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-stache = flask-stache:main
    #
    # Would create an entry point called flask-stache that calls flask-stache.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - pystache

  run:
    - python
    - flask
    - pystache

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mattupstate/flask-stache
  license: MIT License
  summary: 'Simple mustache templating for Flask applications'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
