package:
  name: django-hudson-25
  version: "0.9.0"

source:
  fn: django-hudson-25-0.9.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-hudson-25/django-hudson-25-0.9.0.tar.gz
  md5: e3d1a590bf6bf1b3391e83dd2238274e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-hudson-25 = django-hudson-25:main
    #
    # Would create an entry point called django-hudson-25 that calls django-hudson-25.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.2
    - coverage >=3.4b1
    - pylint

  run:
    - python
    - django >=1.2
    - coverage >=3.4b1
    - pylint

test:
  # Python imports
  imports:
    - django_hudson
    - django_hudson.management
    - django_hudson.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/kmmbvnr/django-hudson
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Plug and play continuous integration with django and hudson'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
