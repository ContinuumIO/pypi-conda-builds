package:
  name: django-referral
  version: "1.0.1"

source:
  fn: django-referral-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-referral/django-referral-1.0.1.tar.gz
  md5: e83a5ceca6cf7450f66fc221411f165b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-referral = django-referral:main
    #
    # Would create an entry point called django-referral that calls django-referral.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - referral
    - referral.migrations
    - referral.south_migrations
    - referral.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/byteweaver/django-referral
  license: MIT License
  summary: 'A small django application for marketing using referral links'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
