package:
  name: laterpay-client
  version: "3.2.0"

source:
  fn: laterpay-client-3.2.0.tar.gz
  url: https://pypi.python.org/packages/source/l/laterpay-client/laterpay-client-3.2.0.tar.gz
  md5: ae9190806008dceb0f74fc5b8a469096
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - laterpay-client = laterpay-client:main
    #
    # Would create an entry point called laterpay-client that calls laterpay-client.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - laterpay
    - laterpay.django
    - laterpay.django.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/laterpay/laterpay-client-python
  license: MIT License
  summary: 'LaterPay API client'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
