package:
  name: collective.recipe.celery
  version: "1.0"

source:
  fn: collective.recipe.celery-1.0.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.recipe.celery/collective.recipe.celery-1.0.tar.gz
  md5: 36aa5e1ea0dc7e730fc906911c3284b2
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.recipe.celery = collective.recipe.celery:main
    #
    # Would create an entry point called collective.recipe.celery that calls collective.recipe.celery.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - zc.recipe.egg

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - zc.recipe.egg

test:
  # Python imports
  imports:
    - collective
    - collective.recipe
    - collective.recipe.celery

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.recipe.celery
  license: GNU General Public License (GPL)
  summary: 'A buildout recipe to install and configure Celery'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
