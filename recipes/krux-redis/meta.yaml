package:
  name: krux-redis
  version: "0.0.3"

source:
  fn: krux-redis-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/k/krux-redis/krux-redis-0.0.3.tar.gz
  md5: 45120d70d1abebd12129f4de33ceaaa8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - krux-redis = krux-redis:main
    #
    # Would create an entry point called krux-redis that calls krux-redis.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - redis
    - krux-stdlib

  run:
    - python
    - redis
    - krux-stdlib

test:
  # Python imports
  imports:
    - krux_redis

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/krux/python-krux-redis
  license: All Rights Reserved.
  summary: 'Library to wrap redsi.py for writers & multiple readers'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
