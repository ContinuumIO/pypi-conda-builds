package:
  name: 42cc-pystyle
  version: "0.0.15"

source:
  fn: 42cc-pystyle-0.0.15.tar.gz
  url: https://pypi.python.org/packages/source/4/42cc-pystyle/42cc-pystyle-0.0.15.tar.gz
  md5: e41137fb3e4ac65dc548e0d040df5611
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - 42cc-pystyle = 42cc-pystyle:main
    #
    # Would create an entry point called 42cc-pystyle that calls 42cc-pystyle.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flake8

  run:
    - python
    - setuptools
    - flake8

test:
  # Python imports
  imports:
    - 42cc_pystyle

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/akhavr/42cc-pystyle
  license: GNU General Public License v2 (GPLv2)
  summary: 'flake8 checks for 42 Coffee Cups style guide'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
