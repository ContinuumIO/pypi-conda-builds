package:
  name: athletelist
  version: "1.1.0"

source:
  fn: athletelist-1.1.0.tar.gz
  url: https://pypi.python.org/packages/source/a/athletelist/athletelist-1.1.0.tar.gz
  md5: deb76bb667a0f7bb2aafa12b1a1f000b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - athletelist = athletelist:main
    #
    # Would create an entry point called athletelist that calls athletelist.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - athletelist

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.cs.albany.edu/~napt
  license: UNKNOWN
  summary: 'An AthleteList to keep track of runner times'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
