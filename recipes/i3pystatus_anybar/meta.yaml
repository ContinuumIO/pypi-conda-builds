package:
  name: i3pystatus_anybar
  version: "1.0"

source:
  fn: i3pystatus_anybar-1.0.tar.gz
  url: https://pypi.python.org/packages/source/i/i3pystatus_anybar/i3pystatus_anybar-1.0.tar.gz
  md5: 1e57eab6e3ca49aa33c587432e7468d9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - i3pystatus_anybar = i3pystatus_anybar:main
    #
    # Would create an entry point called i3pystatus_anybar that calls i3pystatus_anybar.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - i3pystatus

  run:
    - python
    - i3pystatus

test:
  # Python imports
  imports:
    - i3pystatus_anybar

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/narma/i3pystatus-anybar
  license: MIT License
  summary: 'AnyBar widget for i3pystatus'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
