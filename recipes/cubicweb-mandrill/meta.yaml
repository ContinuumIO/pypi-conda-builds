package:
  name: cubicweb-mandrill
  version: "0.4.0"

source:
  fn: cubicweb-mandrill-0.4.0.tar.gz
  url: https://pypi.python.org/packages/source/c/cubicweb-mandrill/cubicweb-mandrill-0.4.0.tar.gz
  md5: 848dde9a38c951520d05258f92e95793
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cubicweb-mandrill = cubicweb-mandrill:main
    #
    # Would create an entry point called cubicweb-mandrill that calls cubicweb-mandrill.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cubicweb >=3.19.0
    - mandrill

  run:
    - python
    - cubicweb >=3.19.0
    - mandrill

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.cubicweb.org/project/cubicweb-mandrill
  license: LGPL
  summary: 'Mandrill Integration cube'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
