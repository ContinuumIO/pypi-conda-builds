package:
  name: backports.functools_lru_cache
  version: "1.0.1"

source:
  fn: backports.functools_lru_cache-1.0.1.zip
  url: https://pypi.python.org/packages/source/b/backports.functools_lru_cache/backports.functools_lru_cache-1.0.1.zip
  md5: c789ef439d189330b99872746a6d9e85
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - backports.functools_lru_cache = backports.functools_lru_cache:main
    #
    # Would create an entry point called backports.functools_lru_cache that calls backports.functools_lru_cache.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - backports

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/jaraco/backports.functools_lru_cache
  license: UNKNOWN
  summary: 'backports.functools_lru_cache'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
