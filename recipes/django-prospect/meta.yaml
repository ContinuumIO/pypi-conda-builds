package:
  name: django-prospect
  version: "0.5"

source:
  fn: django-prospect-0.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-prospect/django-prospect-0.5.tar.gz
  md5: 90fa9a2455207e8cfed3a976429f79ee
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-prospect = django-prospect:main
    #
    # Would create an entry point called django-prospect that calls django-prospect.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-intranet
    - xlrd

  run:
    - python
    - django-intranet
    - xlrd

test:
  # Python imports
  imports:
    - prospect
    - prospect.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/ionyse/django-prospect
  license: GNU Lesser General Public License v3 (LGPLv3)
  summary: 'Managing your prospects easily'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
