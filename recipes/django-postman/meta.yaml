package:
  name: django-postman
  version: "3.2.2"

source:
  fn: django-postman-3.2.2.zip
  url: https://pypi.python.org/packages/source/d/django-postman/django-postman-3.2.2.zip
  md5: b4f060d1bda11f0c0ccd790462e582fb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-postman = django-postman:main
    #
    # Would create an entry point called django-postman that calls django-postman.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - postman
    - postman.management
    - postman.management.commands
    - postman.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/psam/django-postman/overview
  license: BSD License
  summary: 'User-to-User messaging system for Django, with gateway to AnonymousUser, moderation and thread management, user & exchange filters, inbox/sent/archives/trash folders, support for apps: auto-complete, notification, mailer.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
