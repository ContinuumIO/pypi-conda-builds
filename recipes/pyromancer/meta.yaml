package:
  name: pyromancer
  version: "1.0"

source:
  fn: Pyromancer-1.0.tar.gz
  url: https://pypi.python.org/packages/source/P/Pyromancer/Pyromancer-1.0.tar.gz
  md5: 25e110c4e1ecdd0dccb28042c722eb9c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pyromancer = pyromancer:main
    #
    # Would create an entry point called pyromancer that calls pyromancer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - pyromancer
    - pyromancer.commands
    - pyromancer.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Gwildor/Pyromancer
  license: UNKNOWN
  summary: 'Simple framework for creating IRC bots'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
