package:
  name: konf
  version: "1.1.2"

source:
  fn: konf-1.1.2.tar.gz
  url: https://pypi.python.org/packages/source/k/konf/konf-1.1.2.tar.gz
  md5: 13808eba74f37d2ce5d3d0f3664e191f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - konf = konf:main
    #
    # Would create an entry point called konf that calls konf.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyyaml
    - good

  run:
    - python
    - pyyaml
    - good

test:
  # Python imports
  imports:
    - konf
    - konf.testing

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/vartagg/konf/
  license: BSD License
  summary: 'Konf is a Python package which designed to simplify the use of variables in configuration files. json and yaml supported out of the box. '

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
