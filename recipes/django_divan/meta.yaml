package:
  name: django_divan
  version: "0.2.4"

source:
  fn: django_divan-0.2.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django_divan/django_divan-0.2.4.tar.gz
  md5: eb86ff067a799cb7b1b39afd19f3c6cb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_divan = django_divan:main
    #
    # Would create an entry point called django_divan that calls django_divan.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - divan
    - divan.backends
    - divan.management
    - divan.management.commands
    - django_divan

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/threadsafelabs/django_divan
  license: BSD License
  summary: 'User-definable schemas for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
