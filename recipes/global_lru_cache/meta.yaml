package:
  name: global_lru_cache
  version: "1.0"

source:
  fn: global_lru_cache-1.0.tar.gz
  url: https://pypi.python.org/packages/source/g/global_lru_cache/global_lru_cache-1.0.tar.gz
  md5: fb425ddcbbc03b10fda57f3dd922303e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - global_lru_cache = global_lru_cache:main
    #
    # Would create an entry point called global_lru_cache that calls global_lru_cache.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - psutil
    - decorator

  run:
    - python
    - psutil
    - decorator

test:
  # Python imports
  imports:
    - global_lru_cache

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/guykisel/global_lru_cache
  license: MIT License
  summary: 'Global LRU cache decorator'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
