package:
  name: django-castor
  version: "0.2.1"

source:
  fn: django-castor-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-castor/django-castor-0.2.1.tar.gz
  md5: ea379f344380f1826da7bdc5b64c5d25
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-castor = django-castor:main
    #
    # Would create an entry point called django-castor that calls django-castor.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - djcastor

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/zacharyvoase/django-castor
  license: UNKNOWN
  summary: 'A content-addressable storage backend for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
