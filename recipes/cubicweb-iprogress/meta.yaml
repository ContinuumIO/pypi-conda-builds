package:
  name: cubicweb-iprogress
  version: "0.2.0"

source:
  fn: cubicweb-iprogress-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/c/cubicweb-iprogress/cubicweb-iprogress-0.2.0.tar.gz
  md5: f5cf13dcddb5d0c7b393e38a704b96ff
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cubicweb-iprogress = cubicweb-iprogress:main
    #
    # Would create an entry point called cubicweb-iprogress that calls cubicweb-iprogress.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cubicweb >=3.15.0

  run:
    - python
    - cubicweb >=3.15.0

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.cubicweb.org/project/cubicweb-iprogress
  license: LGPL
  summary: 'some adapters and view for stuff progressing to reach a milestone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
