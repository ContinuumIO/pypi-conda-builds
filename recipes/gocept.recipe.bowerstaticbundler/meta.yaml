package:
  name: gocept.recipe.bowerstaticbundler
  version: "1.2.2"

source:
  fn: gocept.recipe.bowerstaticbundler-1.2.2.zip
  url: https://pypi.python.org/packages/source/g/gocept.recipe.bowerstaticbundler/gocept.recipe.bowerstaticbundler-1.2.2.zip
  md5: de6c19a82e9bba52a75e03bfdb698d45
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gocept.recipe.bowerstaticbundler = gocept.recipe.bowerstaticbundler:main
    #
    # Would create an entry point called gocept.recipe.bowerstaticbundler that calls gocept.recipe.bowerstaticbundler.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - rcssmin
    - rjsmin

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - rcssmin
    - rjsmin

test:
  # Python imports
  imports:
    - gocept
    - gocept.recipe
    - gocept.recipe.bowerstaticbundler

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/gocept/gocept.recipe.bowerstaticbundler
  license: Zope Public License
  summary: 'Minifies and bundles JS and CSS files included with bowerstatic.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
