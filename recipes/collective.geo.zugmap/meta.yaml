package:
  name: collective.geo.zugmap
  version: "1.0.2"

source:
  fn: collective.geo.zugmap-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.geo.zugmap/collective.geo.zugmap-1.0.2.tar.gz
  md5: e89f3dd1ec7bffa439d5691952c32f5c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.geo.zugmap = collective.geo.zugmap:main
    #
    # Would create an entry point called collective.geo.zugmap that calls collective.geo.zugmap.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.geo.mapwidget

  run:
    - python
    - setuptools
    - setuptools
    - collective.geo.mapwidget

test:
  # Python imports
  imports:
    - collective
    - collective.geo
    - collective.geo.zugmap
    - collective.geo.zugmap.browser
    - collective.geo.zugmap.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.geo.zugmap
  license: GPL v2
  summary: 'Zugmap Layer for collective.geo'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
