package:
  name: django-mass-edit
  version: "2.3"

source:
  fn: django-mass-edit-2.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-mass-edit/django-mass-edit-2.3.tar.gz
  md5: 1f595947f2bcd69f5d5ee5ca8afbbd49
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-mass-edit = django-mass-edit:main
    #
    # Would create an entry point called django-mass-edit that calls django-mass-edit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - massadmin
    - massadmin.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/burke-software/django-mass-edit
  license: BSD License
  summary: 'Make bulk changes in the Django admin interface'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
