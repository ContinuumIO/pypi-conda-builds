package:
  name: artisinalints
  version: "1.0.0"

source:
  fn: ArtisinalInts-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/A/ArtisinalInts/ArtisinalInts-1.0.0.tar.gz
  md5: 6616be6a2ba353da532b8f35bbba9558
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - artisinalints = artisinalints:main
    #
    # Would create an entry point called artisinalints that calls artisinalints.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - ArtisinalInts
    - artisinalints

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/migurski/ArtisinalInts
  license: BSD
  summary: 'Interface to hand-crafted integers from Brooklyn and San Francisco.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
