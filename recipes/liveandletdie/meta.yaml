package:
  name: liveandletdie
  version: "0.0.5"

source:
  fn: liveandletdie-0.0.5.tar.gz
  url: https://pypi.python.org/packages/source/l/liveandletdie/liveandletdie-0.0.5.tar.gz
  md5: 02a7bce935f933a06f2f73b10e316777
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - liveandletdie = liveandletdie:main
    #
    # Would create an entry point called liveandletdie that calls liveandletdie.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - liveandletdie

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/peterhudec/liveandletdie
  license: MIT License
  summary: 'Simplifies launching and terminating of web development servers from BDD and functional tests.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
