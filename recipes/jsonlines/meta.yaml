package:
  name: jsonlines
  version: "0.0.1"

source:
  fn: jsonlines-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/j/jsonlines/jsonlines-0.0.1.tar.gz
  md5: 623f4e21e2219ab2769303baee6a4183
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jsonlines = jsonlines:main
    #
    # Would create an entry point called jsonlines that calls jsonlines.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - jsonlines

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/wbolster/jsonlines
  license: BSD
  summary: 'Library with helpers for the jsonlines file format'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
