package:
  name: soundcloud-syncer
  version: "0.3.1"

source:
  fn: soundcloud-syncer-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/s/soundcloud-syncer/soundcloud-syncer-0.3.1.tar.gz
  md5: c5dd0ecf1042dbf947e4cbe3a34a24d5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - soundcloud-syncer = soundcloud-syncer:main
    #
    # Would create an entry point called soundcloud-syncer that calls soundcloud-syncer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - stagger
    - python-dateutil
    - python-magic
    - pydub

  run:
    - python
    - stagger
    - python-dateutil
    - python-magic
    - pydub

test:
  # Python imports
  imports:
    - ssyncer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/sliim/soundcloud-syncer
  license: GPLv3
  summary: "Synchronize user's favorites tracks from soundcloud"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
