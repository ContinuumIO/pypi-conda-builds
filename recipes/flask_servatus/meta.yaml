package:
  name: flask_servatus
  version: "0.1.3"

source:
  fn: flask_servatus-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/f/flask_servatus/flask_servatus-0.1.3.tar.gz
  md5: e5371aa7c390a42165e1e470d93926df
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask_servatus = flask_servatus:main
    #
    # Would create an entry point called flask_servatus that calls flask_servatus.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.10.0

  run:
    - python
    - flask >=0.10.0

test:
  # Python imports
  imports:
    - flask_servatus
    - flask_servatus.storages

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mikeywaites/Flask-Servatus
  license: MIT License
  summary: 'A port of djangos storages framework for use with flask applications'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
