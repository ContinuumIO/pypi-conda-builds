package:
  name: redis-file
  version: "0.0.1"

source:
  fn: redis-file-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/r/redis-file/redis-file-0.0.1.tar.gz
  md5: a806eed075b055de8c2422b2f31dce85
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - redis-file = redis-file:main
    #
    # Would create an entry point called redis-file that calls redis-file.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - redis

  run:
    - python
    - redis

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/daniellawrence/redis-file
  license: UNKNOWN
  summary: 'Write to redis as if it was a file'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
