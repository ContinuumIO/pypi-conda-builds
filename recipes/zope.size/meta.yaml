package:
  name: zope.size
  version: "4.1.0"

source:
  fn: zope.size-4.1.0.tar.gz
  url: https://pypi.python.org/packages/source/z/zope.size/zope.size-4.1.0.tar.gz
  md5: 73f4a7bb880f81a31cf97523ca2db34a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zope.size = zope.size:main
    #
    # Would create an entry point called zope.size that calls zope.size.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.interface
    - zope.i18nmessageid

  run:
    - python
    - setuptools
    - zope.interface
    - zope.i18nmessageid

test:
  # Python imports
  imports:
    - zope
    - zope.size

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/zope.size
  license: Zope Public License
  summary: 'Interfaces and simple adapter that give the size of an object'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
