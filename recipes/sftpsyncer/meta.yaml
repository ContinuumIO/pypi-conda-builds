package:
  name: sftpsyncer
  version: "0.1.8"

source:
  fn: sftpsyncer-0.1.8.tar.gz
  url: https://pypi.python.org/packages/source/s/sftpsyncer/sftpsyncer-0.1.8.tar.gz
  md5: 7faba54dc027c2f7a81ae22da59b8f01
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sftpsyncer = sftpsyncer:main
    #
    # Would create an entry point called sftpsyncer that calls sftpsyncer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - paramiko

  run:
    - python
    - paramiko

test:
  # Python imports
  imports:
    - sftpsyncer
    - sftpsyncer.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/dsms/sftpsyncher/
  license: LICENSE.txt
  summary: 'Synchronise files between a local cache and an SFTP server'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
