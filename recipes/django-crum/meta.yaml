package:
  name: django-crum
  version: "0.6.1"

source:
  fn: django-crum-0.6.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-crum/django-crum-0.6.1.tar.gz
  md5: 85a7abd0f35638dbace03afae5a2081c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-crum = django-crum:main
    #
    # Would create an entry point called django-crum that calls django-crum.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4

  run:
    - python
    - django >=1.4

test:
  # Python imports
  imports:
    - crum

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://projects.ninemoreminutes.com/projects/django-crum/
  license: BSD License
  summary: 'Django middleware to capture current request and user.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
