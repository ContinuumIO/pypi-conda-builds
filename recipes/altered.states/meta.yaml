package:
  name: altered.states
  version: "0.8.6"

source:
  fn: altered.states-0.8.6.tar.gz
  url: https://pypi.python.org/packages/source/a/altered.states/altered.states-0.8.6.tar.gz
  md5: 23f10280f7d2379d2b65586d17d76925
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - altered.states = altered.states:main
    #
    # Would create an entry point called altered.states that calls altered.states.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - altered

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Plexical/altered.states
  license: ISC License (ISCL)
  summary: 'Python monkey patching for humans.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
