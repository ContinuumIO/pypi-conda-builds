package:
  name: super-resin
  version: "0.1.1"

source:
  fn: super-resin-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/s/super-resin/super-resin-0.1.1.tar.gz
  md5: 2edb2216266b4bfb150b6b87aad8cbd6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - super-resin = super-resin:main
    #
    # Would create an entry point called super-resin that calls super-resin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - components
    - components.accounts
    - components.address
    - components.business_units
    - components.calling_numbers
    - components.commons
    - components.controls
    - components.departments
    - components.electronic_mails
    - components.organizations
    - components.persons
    - components.positions
    - components.profiles
    - components.profiles_address
    - components.profiles_careers
    - components.profiles_certificates
    - components.profiles_educations
    - components.profiles_emails
    - components.profiles_phones
    - components.profiles_salaries
    - components.profiles_test_scores
    - components.sections
    - components.teams

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: BSD License
  summary: 'Super Resin'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
