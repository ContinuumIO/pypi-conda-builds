package:
  name: vtk_visualizer
  version: "0.9.1"

source:
  fn: vtk_visualizer-0.9.1.zip
  url: https://pypi.python.org/packages/source/v/vtk_visualizer/vtk_visualizer-0.9.1.zip
  md5: ce8cd3b7940cc44fb3ce2740d5e0fcba
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - vtk_visualizer = vtk_visualizer:main
    #
    # Would create an entry point called vtk_visualizer that calls vtk_visualizer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - vtk_visualizer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/oskotheim/vtk_visualizer
  license: BSD License
  summary: 'Easy 3D visualization of point clouds and geometric primitives'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
