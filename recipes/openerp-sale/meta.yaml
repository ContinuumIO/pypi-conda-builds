package:
  name: openerp-sale
  version: "7.0.406"

source:
  fn: openerp-sale-7.0.406.tar.gz
  url: https://pypi.python.org/packages/source/o/openerp-sale/openerp-sale-7.0.406.tar.gz
  md5: 39f51e2b85f781b4df1ef91a77604cfe
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - openerp-sale = openerp-sale:main
    #
    # Would create an entry point called openerp-sale that calls openerp-sale.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - openerp-account-voucher

  run:
    - python
    - openerp-account-voucher

test:
  # Python imports
  imports:
    - openerp.addons.sale
    - openerp.addons.sale.edi
    - openerp.addons.sale.report
    - openerp.addons.sale.wizard

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.openerp.com
  license: GNU Affero General Public License v3
  summary: 'Sales Management'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
