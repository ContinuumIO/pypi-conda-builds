package:
  name: django-campaign
  version: "0.2.0"

source:
  fn: django-campaign-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-campaign/django-campaign-0.2.0.tar.gz
  md5: 4deea6dace469ed6ea358e1894b216e3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-campaign = django-campaign:main
    #
    # Would create an entry point called django-campaign that calls django-campaign.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - campaign
    - campaign.backends
    - campaign.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/arneb/django-campaign/
  license: BSD License
  summary: 'A basic newsletter app for the Django webframework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
