package:
  name: django-locations
  version: "0.1.1"

source:
  fn: django-locations-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-locations/django-locations-0.1.1.tar.gz
  md5: 2d147173e254c33a75342f4f40e61788
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-locations = django-locations:main
    #
    # Would create an entry point called django-locations that calls django-locations.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - locations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/yashh/django-locations/commits/master
  license: BSD License
  summary: 'A location based social network using Django & Pinax'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
