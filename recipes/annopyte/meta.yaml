package:
  name: annopyte
  version: "0.3.1"

source:
  fn: annopyte-0.3.1.zip
  url: https://pypi.python.org/packages/source/a/annopyte/annopyte-0.3.1.zip
  md5: faf18296f56fadb1da878d0329fa1c3e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - annopyte = annopyte:main
    #
    # Would create an entry point called annopyte that calls annopyte.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - annopyte
    - annopyte.annotations
    - annopyte.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://annopyte.franzoni.eu
  license: APL 2.0
  summary: 'python based tool for annotation and annotation processing.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
