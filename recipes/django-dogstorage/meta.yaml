package:
  name: django-dogstorage
  version: "1.0.0"

source:
  fn: django-dogstorage-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-dogstorage/django-dogstorage-1.0.0.tar.gz
  md5: 51700dfdc310daf1c3acc797a956a243
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-dogstorage = django-dogstorage:main
    #
    # Would create an entry point called django-dogstorage that calls django-dogstorage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - dogstorage
    - dogstorage.storages

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/originell/django-dogstorage
  license: BSD License
  summary: 'Django Storage Engine which returns images of dogs if files could not be found.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
