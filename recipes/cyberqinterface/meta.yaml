package:
  name: cyberqinterface
  version: "1.0"

source:
  fn: CyberQInterface-1.0.tar.gz
  url: https://pypi.python.org/packages/source/C/CyberQInterface/CyberQInterface-1.0.tar.gz
  md5: 99e0d2a8bdd76c95a987230a886d2de6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cyberqinterface = cyberqinterface:main
    #
    # Would create an entry point called cyberqinterface that calls cyberqinterface.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - lxml
    - requests

  run:
    - python
    - lxml
    - requests

test:
  # Python imports
  imports:
    - cyberqinterface

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/thebrilliantidea/CyberQInterface
  license: BSD New
  summary: 'Package of interface tools for the BBQGuru CyberQ Pit Temperature Controller'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
