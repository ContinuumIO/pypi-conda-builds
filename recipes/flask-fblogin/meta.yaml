package:
  name: flask-fblogin
  version: "0.0.1"

source:
  fn: Flask-FBLogin-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-FBLogin/Flask-FBLogin-0.0.1.tar.gz
  md5: 5c60c7f97a643337ecfab70b75a46203
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-fblogin = flask-fblogin:main
    #
    # Would create an entry point called flask-fblogin that calls flask-fblogin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests <1.0
    - flask-login >=0.2,<0.3

  run:
    - python
    - requests <1.0
    - flask-login >=0.2,<0.3

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/marksteve/flask-fblogin
  license: MIT License
  summary: "Extends Flask-Login to use Facebook's OAuth2 authorization"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
