package:
  name: python-audioprocessing
  version: "0.0.7"

source:
  fn: python-audioprocessing-0.0.7.tar.gz
  url: https://pypi.python.org/packages/source/p/python-audioprocessing/python-audioprocessing-0.0.7.tar.gz
  md5: 4c17ce5feeffb2798cd11b9285c54dc8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-audioprocessing = python-audioprocessing:main
    #
    # Would create an entry point called python-audioprocessing that calls python-audioprocessing.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - audioprocessing

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://rudd-o.com/new-projects/python-audioprocessing
  license: GPL
  summary: 'A set of convenience tools to process audio'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
