package:
  name: hires
  version: "0.0.11"

source:
  fn: hires-0.0.11.tar.gz
  url: https://pypi.python.org/packages/source/h/hires/hires-0.0.11.tar.gz
  md5: c785742e228a92894362f1884a3b2d00
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hires = hires:main
    #
    # Would create an entry point called hires that calls hires.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - hires

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: http://opensource.org/licenses/GPL-3.0
  summary: 'algorithms for high resolution lcms'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
