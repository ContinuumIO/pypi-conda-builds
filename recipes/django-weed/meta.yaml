package:
  name: django-weed
  version: "0.1.2"

source:
  fn: django-weed-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-weed/django-weed-0.1.2.tar.gz
  md5: 38c9f4db79c743e68177a376ad7be180
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-weed = django-weed:main
    #
    # Would create an entry point called django-weed that calls django-weed.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - pyweed >=0.3.3

  run:
    - python
    - django
    - pyweed >=0.3.3

test:
  # Python imports
  imports:
    - djweed

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ProstoKSI/django-weed
  license: MIT
  summary: 'Weed-FS integration into Django as a storage'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
