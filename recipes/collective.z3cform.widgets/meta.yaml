package:
  name: collective.z3cform.widgets
  version: "1.0b11"

source:
  fn: collective.z3cform.widgets-1.0b11.zip
  url: https://pypi.python.org/packages/source/c/collective.z3cform.widgets/collective.z3cform.widgets-1.0b11.zip
  md5: 089b0443210b622a98c189839b04fe0f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.z3cform.widgets = collective.z3cform.widgets:main
    #
    # Would create an entry point called collective.z3cform.widgets that calls collective.z3cform.widgets.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - collective.js.jqueryui
    - plone.app.dexterity
    - plone.app.layout
    - plone.app.vocabularies
    - plone.autoform
    - plone.dexterity
    - plone.directives.form
    - plone.formwidget.autocomplete
    - plone.formwidget.contenttree
    - products.cmfcore
    - products.cmfplone >=4.2
    - products.genericsetup
    - setuptools
    - z3c.form
    - zope.browserpage
    - zope.component
    - zope.i18nmessageid
    - zope.interface
    - zope.schema

  run:
    - python
    - setuptools
    - collective.js.jqueryui
    - plone.app.dexterity
    - plone.app.layout
    - plone.app.vocabularies
    - plone.autoform
    - plone.dexterity
    - plone.directives.form
    - plone.formwidget.autocomplete
    - plone.formwidget.contenttree
    - products.cmfcore
    - products.cmfplone >=4.2
    - products.genericsetup
    - setuptools
    - z3c.form
    - zope.browserpage
    - zope.component
    - zope.i18nmessageid
    - zope.interface
    - zope.schema

test:
  # Python imports
  imports:
    - collective
    - collective.z3cform
    - collective.z3cform.widgets
    - collective.z3cform.widgets.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.z3cform.widgets
  license: GNU General Public License v2 (GPLv2)
  summary: 'A widget package for Dexterity projects.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
