package:
  name: django-activity-stream-autoactor
  version: "0.0.1"

source:
  fn: django-activity-stream-autoactor-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-activity-stream-autoactor/django-activity-stream-autoactor-0.0.1.tar.gz
  md5: 57f3012eec1f5dee92c0b8f648a3d27f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-activity-stream-autoactor = django-activity-stream-autoactor:main
    #
    # Would create an entry point called django-activity-stream-autoactor that calls django-activity-stream-autoactor.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - django-autoconfig

  run:
    - python
    - django
    - django-autoconfig

test:
  # Python imports
  imports:
    - actstream_autoactor

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mikebryant/django-activity-stream-autoactor/
  license: UNKNOWN
  summary: 'Automatic provision of actors for django-activity-stream'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
