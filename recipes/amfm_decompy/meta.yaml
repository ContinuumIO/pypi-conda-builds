package:
  name: amfm_decompy
  version: "1.0.4"

source:
  fn: AMFM_decompy-1.0.4.tar.gz
  url: https://pypi.python.org/packages/source/A/AMFM_decompy/AMFM_decompy-1.0.4.tar.gz
  md5: ba04b9a44e30d7b6ceb39eb42fbfe878
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - amfm_decompy = amfm_decompy:main
    #
    # Would create an entry point called amfm_decompy that calls amfm_decompy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - amfm_decompy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bjbschmitt/AMFM_decompy/
  license: MIT License
  summary: 'Package to decompose the voiced part of a speech signal into                     its modulated components, aka AM-FM decomposition.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
