package:
  name: svargaext.transform
  version: "0.6"

source:
  fn: svargaext.transform-0.6.tar.gz
  url: https://pypi.python.org/packages/source/s/svargaext.transform/svargaext.transform-0.6.tar.gz
  md5: 334551f5cf2781a6e164a8875e7c150d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - svargaext.transform = svargaext.transform:main
    #
    # Would create an entry point called svargaext.transform that calls svargaext.transform.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - svarga
    - opster
    - clevercss

  run:
    - python
    - svarga
    - opster
    - clevercss

test:
  # Python imports
  imports:
    - svargaext.transform

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://hg.piranha.org.ua/svargaext.transform/
  license: BSD License
  summary: 'Source transformation module for Svarga framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
