package:
  name: elasticfun
  version: "0.3.3"

source:
  fn: elasticfun-0.3.3.tar.gz
  url: https://pypi.python.org/packages/source/e/elasticfun/elasticfun-0.3.3.tar.gz
  md5: c376d9baa9c187261d42aea661ab8610
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - elasticfun = elasticfun:main
    #
    # Would create an entry point called elasticfun that calls elasticfun.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six ==1.3.0
    - pyelasticsearch ==0.4.1

  run:
    - python
    - six ==1.3.0
    - pyelasticsearch ==0.4.1

test:
  # Python imports
  imports:
    - elasticfun
    - elasticfun.django

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Yipit/elasticfun
  license: GNU General Public License v3 or later (GPLv3+)
  summary: u'ElasticSearch Query functionality using Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
