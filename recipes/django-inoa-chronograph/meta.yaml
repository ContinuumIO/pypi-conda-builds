package:
  name: django-inoa-chronograph
  version: "0.1.27"

source:
  fn: django-inoa-chronograph-0.1.27.zip
  url: https://pypi.python.org/packages/source/d/django-inoa-chronograph/django-inoa-chronograph-0.1.27.zip
  md5: e6df9029a2091d7b2282de416624cc0c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-inoa-chronograph = django-inoa-chronograph:main
    #
    # Would create an entry point called django-inoa-chronograph that calls django-inoa-chronograph.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django >=1.5.0

  run:
    - python
    - django >=1.5.0

test:
  # Python imports
  imports:
    - chronograph
    - chronograph.management
    - chronograph.management.commands
    - chronograph.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/joaofrancese/django-chronograph
  license: BSD License
  summary: 'Chronograph, a Django library for managing scheduled tasks (version mantained by Inoa)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
