package:
  name: medialog.newsitemview
  version: "0.9"

source:
  fn: medialog.newsitemview-0.9.tar.gz
  url: https://pypi.python.org/packages/source/m/medialog.newsitemview/medialog.newsitemview-0.9.tar.gz
  md5: 32725b7e2f65950c9ce1e5efb2d89a8f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - medialog.newsitemview = medialog.newsitemview:main
    #
    # Would create an entry point called medialog.newsitemview that calls medialog.newsitemview.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - archetypes.schemaextender
    - archetypes.markerfield
    - plone.directives.form
    - medialog.controlpanel

  run:
    - python
    - setuptools
    - setuptools
    - archetypes.schemaextender
    - archetypes.markerfield
    - plone.directives.form
    - medialog.controlpanel

test:
  # Python imports
  imports:
    - medialog
    - medialog.newsitemview
    - medialog.newsitemview.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://products.medialog.no
  license: GPL
  summary: 'Set size of image for newsitem and folder_contents'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
