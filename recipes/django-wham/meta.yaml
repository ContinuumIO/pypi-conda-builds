package:
  name: django-wham
  version: "0.1.2"

source:
  fn: django-wham-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-wham/django-wham-0.1.2.tar.gz
  md5: fa27f507f3ae537cf35d100d77d7c6b3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-wham = django-wham:main
    #
    # Would create an entry point called django-wham that calls django-wham.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - requests

  run:
    - python
    - django
    - requests

test:
  # Python imports
  imports:
    - wham
    - wham.apis
    - wham.apis.deezer
    - wham.apis.eventbrite
    - wham.apis.facebook
    - wham.apis.freebase
    - wham.apis.instagram
    - wham.apis.lastfm
    - wham.apis.musicbrainz
    - wham.apis.soundcloud
    - wham.apis.spotify
    - wham.apis.twitter

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mbylstra/django-wham
  license: UNKNOWN
  summary: 'Rest APIs disguised as Django ORM Models'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
