package:
  name: bambu-pusher
  version: "0.1"

source:
  fn: bambu-pusher-0.1.tar.gz
  url: https://pypi.python.org/packages/source/b/bambu-pusher/bambu-pusher-0.1.tar.gz
  md5: 94648e125cb1ab63af4539a3352057d8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bambu-pusher = bambu-pusher:main
    #
    # Would create an entry point called bambu-pusher that calls bambu-pusher.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django >=1.4
    - requests

  run:
    - python
    - django >=1.4
    - requests

test:
  # Python imports
  imports:
    - bambu.pusher
    - bambu.pusher.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/bambu-pusher
  license: UNKNOWN
  summary: 'A wrapper around the Pusher API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
