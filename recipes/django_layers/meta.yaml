package:
  name: django_layers
  version: "0.9"

source:
  fn: django_layers-0.9.tar.gz
  url: https://pypi.python.org/packages/source/d/django_layers/django_layers-0.9.tar.gz
  md5: a81fc6ef737aea58eb6cf73797f024d0
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_layers = django_layers:main
    #
    # Would create an entry point called django_layers that calls django_layers.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pytest
    - twotest

  run:
    - python
    - setuptools
    - pytest
    - twotest

test:
  # Python imports
  imports:
    - django_layers
    - layers
    - layers.management
    - layers.management.commands
    - layers.tests
    - layers.tests.pkg1
    - layers.tests.pkg2

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/iivvoo/django_layers
  license: BSD
  summary: 'Support different frontend templates/statics on same instance'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
