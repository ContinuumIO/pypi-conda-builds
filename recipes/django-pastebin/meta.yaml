package:
  name: django-pastebin
  version: "1.5"

source:
  fn: django-pastebin-1.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-pastebin/django-pastebin-1.5.tar.gz
  md5: 0ce730e5436f234a81e1bd6e76acfa83
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-pastebin = django-pastebin:main
    #
    # Would create an entry point called django-pastebin that calls django-pastebin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - example
    - example.pastebin
    - example.settings
    - pastebin

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.agiliq.com/
  license: BSD License
  summary: 'A django based pastebin'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
