package:
  name: django_linksets
  version: "2.0.3"

source:
  fn: django_linksets-2.0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django_linksets/django_linksets-2.0.3.tar.gz
  md5: bd9403f5744a06d8e6ae479134347b0f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_linksets = django_linksets:main
    #
    # Would create an entry point called django_linksets that calls django_linksets.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - setuptools
    - django

test:
  # Python imports
  imports:
    - django_linksets
    - linksets
    - linksets.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/gethee2anunnery/django-linksets
  license: UNKNOWN
  summary: 'A basic model to include administrable linksets      in templates.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
