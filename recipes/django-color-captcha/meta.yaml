package:
  name: django-color-captcha
  version: "0.1.0"

source:
  fn: django-color-captcha-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-color-captcha/django-color-captcha-0.1.0.tar.gz
  md5: 9636b243f6aa7dcafea572e46fd142b7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-color-captcha = django-color-captcha:main
    #
    # Would create an entry point called django-color-captcha that calls django-color-captcha.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - color_captcha

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/qvit/django-color-captcha
  license: MIT License
  summary: 'Simple and pretty user-friendly captcha module for Django framework.\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
