package:
  name: django-fs-smsc
  version: "0.1"

source:
  fn: django-fs-smsc-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-fs-smsc/django-fs-smsc-0.1.tar.gz
  md5: f18007fda7ca415f5a5a325f7aa68c31
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-fs-smsc = django-fs-smsc:main
    #
    # Would create an entry point called django-fs-smsc that calls django-fs-smsc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - smsc

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/fogstream/django-fs-smsc
  license: The MIT License (MIT)
  summary: 'Django integration with SMS Centre'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
