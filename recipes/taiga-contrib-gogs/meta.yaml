package:
  name: taiga-contrib-gogs
  version: "0.0.4"

source:
  fn: taiga-contrib-gogs-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/t/taiga-contrib-gogs/taiga-contrib-gogs-0.0.4.tar.gz
  md5: 554ed1498cd78deae05d9a12fecd6f36
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - taiga-contrib-gogs = taiga-contrib-gogs:main
    #
    # Would create an entry point called taiga-contrib-gogs that calls taiga-contrib-gogs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.7

  run:
    - python
    - django >=1.7

test:
  # Python imports
  imports:
    - taiga_contrib_gogs
    - taiga_contrib_gogs.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/taigaio/taiga-contrib-gogs
  license: GNU Affero General Public License v3
  summary: 'The Taiga plugin for gogs integration'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
