package:
  name: jsontoemail
  version: "0.0.5"

source:
  fn: jsontoemail-0.0.5.tar.gz
  url: https://pypi.python.org/packages/source/j/jsontoemail/jsontoemail-0.0.5.tar.gz
  md5: 5024e0204d9add2bb5574e25e8a90820
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jsontoemail = jsontoemail:main
    #
    # Would create an entry point called jsontoemail that calls jsontoemail.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - jsontoemail

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/sanger-pathogens/json2email
  license: GNU General Public License v3 (GPLv3)
  summary: 'Deprecated: please use json2email instead'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
