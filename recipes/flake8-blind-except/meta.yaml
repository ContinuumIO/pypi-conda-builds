package:
  name: flake8-blind-except
  version: "0.1.0"

source:
  fn: flake8-blind-except-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/f/flake8-blind-except/flake8-blind-except-0.1.0.tar.gz
  md5: a76ae6603b53c230ad81f9dea27c6bbb
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flake8-blind-except = flake8-blind-except:main
    #
    # Would create an entry point called flake8-blind-except that calls flake8-blind-except.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/elijahandrews/flake8-blind-except
  license: MIT
  summary: 'A flake8 extension that checks for blind except: statements'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
