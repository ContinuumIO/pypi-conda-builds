package:
  name: cubicweb-keyword
  version: "1.7.1"

source:
  fn: cubicweb-keyword-1.7.1.tar.gz
  url: https://pypi.python.org/packages/source/c/cubicweb-keyword/cubicweb-keyword-1.7.1.tar.gz
  md5: 20a7e09ca9d5fe229d6e7e540610991c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cubicweb-keyword = cubicweb-keyword:main
    #
    # Would create an entry point called cubicweb-keyword that calls cubicweb-keyword.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.cubicweb.org/project/cubicweb-keyword
  license: LGPL
  summary: 'classification schemes system for the Cubicweb framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
