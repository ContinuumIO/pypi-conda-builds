package:
  name: django-flatpages-plus
  version: "0.1.1"

source:
  fn: django-flatpages-plus-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-flatpages-plus/django-flatpages-plus-0.1.1.tar.gz
  md5: 46d137b669c95df8039d6d27f0e18e3d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-flatpages-plus = django-flatpages-plus:main
    #
    # Would create an entry point called django-flatpages-plus that calls django-flatpages-plus.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - flatpages_plus
    - flatpages_plus.migrations
    - flatpages_plus.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/danawoodman/django-flatpages-plus
  license: MIT
  summary: 'A more robust FlatPage app for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
