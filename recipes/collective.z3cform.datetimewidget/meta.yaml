package:
  name: collective.z3cform.datetimewidget
  version: "1.2.7"

source:
  fn: collective.z3cform.datetimewidget-1.2.7.zip
  url: https://pypi.python.org/packages/source/c/collective.z3cform.datetimewidget/collective.z3cform.datetimewidget-1.2.7.zip
  md5: 4880548429d693bd9be4e7b49b2bca4f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.z3cform.datetimewidget = collective.z3cform.datetimewidget:main
    #
    # Would create an entry point called collective.z3cform.datetimewidget that calls collective.z3cform.datetimewidget.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.i18n
    - z3c.form
    - zope.deprecation

  run:
    - python
    - setuptools
    - zope.i18n
    - z3c.form
    - zope.deprecation

test:
  # Python imports
  imports:
    - collective
    - collective.z3cform
    - collective.z3cform.datetimewidget

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.z3cform.datetimewidget
  license: GPL version 2
  summary: 'z3c.form date and datetime widgets'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
