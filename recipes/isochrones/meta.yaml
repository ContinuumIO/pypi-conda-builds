package:
  name: isochrones
  version: "0.8.4"

source:
  fn: isochrones-0.8.4.tar.gz
  url: https://pypi.python.org/packages/source/i/isochrones/isochrones-0.8.4.tar.gz
  md5: 372476e4c09f0591bd82af673a95cdb4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - isochrones = isochrones:main
    #
    # Would create an entry point called isochrones that calls isochrones.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - plotutils
    - pandas >=0.14
    - astropy >=0.3
    - emcee >=2.0
    - numpy >=1.9
    - tables >=3.0
    - triangle_plot

  run:
    - python
    - plotutils
    - pandas >=0.14
    - astropy >=0.3
    - emcee >=2.0
    - numpy >=1.9
    - tables >=3.0
    - triangle_plot

test:
  # Python imports
  imports:
    - isochrones

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/timothydmorton/isochrones
  license: UNKNOWN
  summary: 'Defines objects for interpolating stellar model grids.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
