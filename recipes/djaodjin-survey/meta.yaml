package:
  name: djaodjin-survey
  version: "0.1.4"

source:
  fn: djaodjin-survey-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/d/djaodjin-survey/djaodjin-survey-0.1.4.tar.gz
  md5: 0d6477f4400e99fa3125c3f45f6328b5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djaodjin-survey = djaodjin-survey:main
    #
    # Would create an entry point called djaodjin-survey that calls djaodjin-survey.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - survey
    - survey.templatetags
    - survey.urls
    - survey.views

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/djaodjin/djaodjin-survey/
  license: BSD
  summary: 'Survey Django app'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
