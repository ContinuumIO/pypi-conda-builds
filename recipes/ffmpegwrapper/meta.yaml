package:
  name: ffmpegwrapper
  version: "0.1-dev"

source:
  fn: ffmpegwrapper-0.1-dev.tar.gz
  url: https://pypi.python.org/packages/source/f/ffmpegwrapper/ffmpegwrapper-0.1-dev.tar.gz
  md5: 2a64d174380092f7a77682346e5c48aa
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ffmpegwrapper = ffmpegwrapper:main
    #
    # Would create an entry point called ffmpegwrapper that calls ffmpegwrapper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - ffmpegwrapper

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/interrupted/ffmpegwrapper
  license: BSD License
  summary: 'A simple wrapper for ffmpeg-cli'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
