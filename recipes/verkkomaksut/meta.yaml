package:
  name: verkkomaksut
  version: "0.2.0"

source:
  fn: verkkomaksut-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/v/verkkomaksut/verkkomaksut-0.2.0.tar.gz
  md5: 22d81fa14a8ca5661c929485c101d256
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - verkkomaksut = verkkomaksut:main
    #
    # Would create an entry point called verkkomaksut that calls verkkomaksut.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - verkkomaksut

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/fastmonkeys/python-verkkomaksut
  license: BSD License
  summary: 'Python wrapper for the JSON API of Suomen Verkkomaksut.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
