package:
  name: secsgem
  version: "0.0.2"

source:
  fn: secsgem-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/s/secsgem/secsgem-0.0.2.tar.gz
  md5: 5cb43448ba6a0bf82fc6a3fb8be87f19
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - secsgem = secsgem:main
    #
    # Would create an entry point called secsgem that calls secsgem.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - secsgem

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bparzella/secsgem
  license: GNU Lesser General Public License v2 or later (LGPLv2+)
  summary: 'Python SECS/GEM implementation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
