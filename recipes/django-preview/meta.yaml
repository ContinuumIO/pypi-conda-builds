package:
  name: django-preview
  version: "0.1"

source:
  fn: django-preview-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-preview/django-preview-0.1.tar.gz
  md5: 887d0089b2acea91ed94ea817a422ac3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-preview = django-preview:main
    #
    # Would create an entry point called django-preview that calls django-preview.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - preview

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/volodymyrko/django-preview
  license: BSD
  summary: 'Managing preview subscription in Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
