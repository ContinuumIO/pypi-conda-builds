package:
  name: adspygoogle.dfp
  version: "9.8.0"

source:
  fn: adspygoogle.dfp-9.8.0.tar.gz
  url: https://pypi.python.org/packages/source/a/adspygoogle.dfp/adspygoogle.dfp-9.8.0.tar.gz
  md5: 8ded645928d491aecdca2eafcc301351
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - adspygoogle.dfp = adspygoogle.dfp:main
    #
    # Would create an entry point called adspygoogle.dfp that calls adspygoogle.dfp.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zsi
    - fpconst
    - google-api-python-client

  run:
    - python
    - zsi
    - fpconst
    - google-api-python-client

test:
  # Python imports
  imports:
    - adspygoogle
    - adspygoogle.SOAPpy
    - adspygoogle.SOAPpy.wstools
    - adspygoogle.common
    - adspygoogle.common.https
    - adspygoogle.common.soappy
    - adspygoogle.dfp

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/google-api-ads-python
  license: Apache License 2.0
  summary: 'DFP API Python Client Library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
