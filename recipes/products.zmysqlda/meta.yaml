package:
  name: products.zmysqlda
  version: "3.1.1"

source:
  fn: Products.ZMySQLDA-3.1.1.tar.gz
  url: https://pypi.python.org/packages/source/P/Products.ZMySQLDA/Products.ZMySQLDA-3.1.1.tar.gz
  md5: c817e7f51e45218529e992acba866673
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - products.zmysqlda = products.zmysqlda:main
    #
    # Would create an entry point called products.zmysqlda that calls products.zmysqlda.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - mysql-python >=1.2.1

  run:
    - python
    - mysql-python >=1.2.1

test:
  # Python imports
  imports:
    - Products
    - Products.ZMySQLDA

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://sourceforge.net/projects/mysql-python
  license: Zope Public License (ZPL) Version 1.0
  summary: 'MySQL Zope2 adapter.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
