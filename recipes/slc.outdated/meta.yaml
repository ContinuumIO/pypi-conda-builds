package:
  name: slc.outdated
  version: "1.3"

source:
  fn: slc.outdated-1.3.zip
  url: https://pypi.python.org/packages/source/s/slc.outdated/slc.outdated-1.3.zip
  md5: ad71f2118f02f12d064d98fbb17ee6df
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - slc.outdated = slc.outdated:main
    #
    # Would create an entry point called slc.outdated that calls slc.outdated.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone

  run:
    - python
    - setuptools
    - setuptools
    - plone

test:
  # Python imports
  imports:
    - slc
    - slc.outdated
    - slc.outdated.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/slc.outdated
  license: GNU General Public License (GPL)
  summary: 'Mark and treat plone content as outdated'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
