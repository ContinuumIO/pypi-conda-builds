package:
  name: cloudsafe
  version: "0.0.1"

source:
  fn: cloudsafe-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/cloudsafe/cloudsafe-0.0.1.tar.gz
  md5: c775e0faedcfc6d4f77c2abb7e3845ff
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cloudsafe = cloudsafe:main
    #
    # Would create an entry point called cloudsafe that calls cloudsafe.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4

  run:
    - python
    - django >=1.4

test:
  # Python imports
  imports:
    - cloudsafe
    - handleui
    - webui

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT License
  summary: 'cloudsafe'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
