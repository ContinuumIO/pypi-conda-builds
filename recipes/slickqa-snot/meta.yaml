package:
  name: slickqa-snot
  version: "1.0.2860"

source:
  fn: slickqa-snot-1.0.2860.tar.gz
  url: https://pypi.python.org/packages/source/s/slickqa-snot/slickqa-snot-1.0.2860.tar.gz
  md5: 2ba05e013f049f98bf678e0949d8ebb8
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - slickqa-snot = slickqa-snot:main
    #
    # Would create an entry point called slickqa-snot that calls slickqa-snot.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - slickqa >=2.0.80
    - nose
    - docutils

  run:
    - python
    - setuptools
    - slickqa >=2.0.80
    - nose
    - docutils

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/slickqa/snot
  license: License :: OSI Approved :: Apache Software License
  summary: 'A plugin to nose to allow results from python tests to be put into slick.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
