package:
  name: django-simple-blog
  version: "0.1.1"

source:
  fn: django-simple-blog-0.1.1.zip
  url: https://pypi.python.org/packages/source/d/django-simple-blog/django-simple-blog-0.1.1.zip
  md5: dba4a0133c29e7bce8ea467bb3a7ab3c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-simple-blog = django-simple-blog:main
    #
    # Would create an entry point called django-simple-blog that calls django-simple-blog.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5
    - django-endless-pagination >=2.0
    - django-simple-math-captcha >=1.0.2

  run:
    - python
    - django >=1.5
    - django-endless-pagination >=2.0
    - django-simple-math-captcha >=1.0.2

test:
  # Python imports
  imports:
    - simpleblog

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Drager/django-simple-blog
  license: BSD License
  summary: 'An easy way to start writing blog posts.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
