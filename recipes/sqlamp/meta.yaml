package:
  name: sqlamp
  version: "0.6"

source:
  fn: sqlamp-0.6.tar.gz
  url: https://pypi.python.org/packages/source/s/sqlamp/sqlamp-0.6.tar.gz
  md5: eab44f7df39193f77ddc70ddd2124020
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sqlamp = sqlamp:main
    #
    # Would create an entry point called sqlamp that calls sqlamp.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sqlalchemy >=0.5

  run:
    - python
    - sqlalchemy >=0.5

test:
  # Python imports
  imports:
    - sqlamp

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://sqlamp.angri.ru
  license: BSD License
  summary: 'sqlamp is an implementation of Materialized Path for SQLAlchemy.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
