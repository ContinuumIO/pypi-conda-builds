package:
  name: django-google-oauth
  version: "1.0.4"

source:
  fn: django-google-oauth-1.0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-google-oauth/django-google-oauth-1.0.4.tar.gz
  md5: 194eb595796d2a3529923e718d8df1d7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-google-oauth = django-google-oauth:main
    #
    # Would create an entry point called django-google-oauth that calls django-google-oauth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - google-api-python-client
    - django

  run:
    - python
    - google-api-python-client
    - django

test:
  # Python imports
  imports:
    - google_oauth

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/trik82/django-google-oauth/tree/master
  license: GNU General Public License v3 (GPLv3)
  summary: 'Django Integration for Google+ OAuth2'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
