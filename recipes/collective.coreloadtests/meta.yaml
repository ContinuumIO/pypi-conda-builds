package:
  name: collective.coreloadtests
  version: "0.2"

source:
  fn: collective.coreloadtests-0.2.zip
  url: https://pypi.python.org/packages/source/c/collective.coreloadtests/collective.coreloadtests-0.2.zip
  md5: 0c269017e972a63824f4256ab541615e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.coreloadtests = collective.coreloadtests:main
    #
    # Would create an entry point called collective.coreloadtests that calls collective.coreloadtests.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.coreloadtests
    - collective.coreloadtests.exportimport
    - collective.coreloadtests.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/collective.coreloadtests
  license: GPL
  summary: 'Plone core load test plans'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
