package:
  name: archetypes.ldapstorage
  version: "0.1a1"

source:
  fn: archetypes.ldapstorage-0.1a1.tar.gz
  url: https://pypi.python.org/packages/source/a/archetypes.ldapstorage/archetypes.ldapstorage-0.1a1.tar.gz
  md5: 3619e352071ee58fb6bc0e8cd46bf17e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - archetypes.ldapstorage = archetypes.ldapstorage:main
    #
    # Would create an entry point called archetypes.ldapstorage that calls archetypes.ldapstorage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.ldapconnection
    - python-ldap

  run:
    - python
    - setuptools
    - setuptools
    - products.ldapconnection
    - python-ldap

test:
  # Python imports
  imports:
    - archetypes
    - archetypes.ldapstorage
    - archetypes.ldapstorage.examples
    - archetypes.ldapstorage.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/archetypes.ldapstorage
  license: GPL
  summary: 'LDAP Storage for Archetypes'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
