package:
  name: collective.analyticspanel
  version: "0.4.0"

source:
  fn: collective.analyticspanel-0.4.0.zip
  url: https://pypi.python.org/packages/source/c/collective.analyticspanel/collective.analyticspanel-0.4.0.zip
  md5: 243edded7dc7333162282676ed572ba6
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.analyticspanel = collective.analyticspanel:main
    #
    # Would create an entry point called collective.analyticspanel that calls collective.analyticspanel.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.registry
    - plone.app.vocabularies

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.registry
    - plone.app.vocabularies

test:
  # Python imports
  imports:
    - collective
    - collective.analyticspanel
    - collective.analyticspanel.browser
    - collective.analyticspanel.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/collective.analyticspanel
  license: GPL
  summary: 'Add more control over the inclusion of JavaScript analytics code of your Plone site'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
