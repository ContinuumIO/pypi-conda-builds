package:
  name: django-hbs-makemessages
  version: "0.9.6"

source:
  fn: django-hbs-makemessages-0.9.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-hbs-makemessages/django-hbs-makemessages-0.9.6.tar.gz
  md5: 22d9c63fbe2a9eae87acfb99eab96511
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-hbs-makemessages = django-hbs-makemessages:main
    #
    # Would create an entry point called django-hbs-makemessages that calls django-hbs-makemessages.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5
    - path.py >=7.0

  run:
    - python
    - django >=1.5
    - path.py >=7.0

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rafalp/django-hbs-makemessages
  license: MIT License
  summary: 'Library providing makemessages for Handlebars.js templates'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
