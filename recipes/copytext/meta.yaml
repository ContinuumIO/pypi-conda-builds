package:
  name: copytext
  version: "0.1.9"

source:
  fn: copytext-0.1.9.tar.gz
  url: https://pypi.python.org/packages/source/c/copytext/copytext-0.1.9.tar.gz
  md5: 4caf6e9c6f1dfc358ce2dcbb3ddbaabc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - copytext = copytext:main
    #
    # Would create an entry point called copytext that calls copytext.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - openpyxl >=2.1.4
    - markupsafe >=0.21

  run:
    - python
    - openpyxl >=2.1.4
    - markupsafe >=0.21

test:
  # Python imports
  imports:
    - copytext

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://copytext.readthedocs.org/
  license: MIT License
  summary: 'A library for accessing a spreadsheet as a native Python object suitable for templating.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
