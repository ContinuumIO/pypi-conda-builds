package:
  name: oecpy
  version: "1.1.1"

source:
  fn: OECPy-1.1.1.tar.gz
  url: https://pypi.python.org/packages/source/O/OECPy/OECPy-1.1.1.tar.gz
  md5: 9818fe745e5add8c16935c5b4ac6a795
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - oecpy = oecpy:main
    #
    # Would create an entry point called oecpy that calls oecpy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy
    - quantities
    - nose >=1.0
    - matplotlib >=1.3.1
    - requests

  run:
    - python
    - numpy
    - quantities
    - nose >=1.0
    - matplotlib >=1.3.1
    - requests

test:
  # Python imports
  imports:
    - oecpy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ryanvarley/open-exoplanet-catalogue-python
  license: GNU General Public License v3 (GPLv3)
  summary: 'CURRENTLY WORKING ON THIS PACKAGE'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
