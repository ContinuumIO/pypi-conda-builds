package:
  name: penncoursereview
  version: "0.0.3"

source:
  fn: penncoursereview-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/p/penncoursereview/penncoursereview-0.0.3.tar.gz
  md5: 13dc32fd074566d5637a273aefda5e42
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - penncoursereview = penncoursereview:main
    #
    # Would create an entry point called penncoursereview that calls penncoursereview.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - penncoursereview

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/pennappslabs/penncoursereview-sdk-python
  license: MIT License
  summary: 'Penn Course Review API client'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
