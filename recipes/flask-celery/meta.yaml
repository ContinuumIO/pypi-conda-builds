package:
  name: flask-celery
  version: "2.4.3"

source:
  fn: Flask-Celery-2.4.3.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Celery/Flask-Celery-2.4.3.tar.gz
  md5: ed0ef899fbb30f60ccdc5b9dcb16bb27
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-celery = flask-celery:main
    #
    # Would create an entry point called flask-celery that calls flask-celery.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.8
    - flask-script >=0.3.3
    - celery >=2.3.0

  run:
    - python
    - flask >=0.8
    - flask-script >=0.3.3
    - celery >=2.3.0

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/ask/flask-celery/
  license: BSD License
  summary: 'Celery integration for Flask'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
