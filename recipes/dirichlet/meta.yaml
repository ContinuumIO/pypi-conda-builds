package:
  name: dirichlet
  version: "0.7"

source:
  fn: dirichlet-0.7.tar.gz
  url: https://pypi.python.org/packages/source/d/dirichlet/dirichlet-0.7.tar.gz
  md5: 842317a3e2732d87f0e803f3b980e6ba
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dirichlet = dirichlet:main
    #
    # Would create an entry point called dirichlet that calls dirichlet.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - scipy >=0.10.1
    - numpy >=1.6.2
    - matplotlib >=1.2.0

  run:
    - python
    - scipy >=0.10.1
    - numpy >=1.6.2
    - matplotlib >=1.2.0

test:
  # Python imports
  imports:
    - dirichlet

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/ericsuh/dirichlet
  license: MIT License
  summary: 'Calculates Dirichlet test and plots 2-simplex Dirichlets'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
