package:
  name: horae.groupselect
  version: "1.0a1"

source:
  fn: horae.groupselect-1.0a1.tar.gz
  url: https://pypi.python.org/packages/source/h/horae.groupselect/horae.groupselect-1.0a1.tar.gz
  md5: ae73bd524abb15ba9cc97ae932dcea1e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - horae.groupselect = horae.groupselect:main
    #
    # Would create an entry point called horae.groupselect that calls horae.groupselect.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zope.schema
    - zope.formlib

  run:
    - python
    - setuptools
    - setuptools
    - zope.schema
    - zope.formlib

test:
  # Python imports
  imports:
    - horae
    - horae.groupselect

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: GPL
  summary: 'Provides a grouped Dropdown widget for zope.formlib'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
