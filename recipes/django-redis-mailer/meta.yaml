package:
  name: django-redis-mailer
  version: "0.2"

source:
  fn: django-redis-mailer-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-redis-mailer/django-redis-mailer-0.2.tar.gz
  md5: c193c86d80a9bd24fbe6ed14ff19b059
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-redis-mailer = django-redis-mailer:main
    #
    # Would create an entry point called django-redis-mailer that calls django-redis-mailer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.7
    - redis >=2.10

  run:
    - python
    - django >=1.7
    - redis >=2.10

test:
  # Python imports
  imports:
    - django_redis_mailer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Bulv1ne/django-redis-mailer
  license: MIT License
  summary: 'A django mail queue'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
