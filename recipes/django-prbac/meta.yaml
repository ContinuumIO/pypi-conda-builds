package:
  name: django-prbac
  version: "0.0.2"

source:
  fn: django-prbac-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-prbac/django-prbac-0.0.2.tar.gz
  md5: 50c51e5dd32104904f8af8a60fec959f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-prbac = django-prbac:main
    #
    # Would create an entry point called django-prbac that calls django-prbac.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3.7
    - django-json-field >=0.5.5
    - simplejson
    - south

  run:
    - python
    - django >=1.3.7
    - django-json-field >=0.5.5
    - simplejson
    - south

test:
  # Python imports
  imports:
    - django_prbac

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/dimagi/django-prbac
  license: UNKNOWN
  summary: 'Parameterized Role-Based Access Control for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
