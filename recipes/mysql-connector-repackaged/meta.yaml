package:
  name: mysql-connector-repackaged
  version: "0.3.1"

source:
  fn: mysql-connector-repackaged-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/m/mysql-connector-repackaged/mysql-connector-repackaged-0.3.1.tar.gz
  md5: 0b17ad1cb3fe763fd44487cb97cf45b2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mysql-connector-repackaged = mysql-connector-repackaged:main
    #
    # Would create an entry point called mysql-connector-repackaged that calls mysql-connector-repackaged.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - mysql
    - mysql.connector

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://launchpad.net/myconnpy
  license: GNU GPLv2 (with FOSS License Exception)
  summary: 'MySQL driver written in Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
