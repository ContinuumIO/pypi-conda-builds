package:
  name: django-municipios
  version: "0.8.1"

source:
  fn: django-municipios-0.8.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-municipios/django-municipios-0.8.1.tar.gz
  md5: cd58a306502e0f21a0daee5dd255f677
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-municipios = django-municipios:main
    #
    # Would create an entry point called django-municipios that calls django-municipios.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - municipios

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/znc-sistemas/django-municipios
  license: MIT License
  summary: 'Aplica\xc3\xa7\xc3\xa3o plug\xc3\xa1vel Django com modelos e widgets para os Munic\xc3\xadpios Brasileiros'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
