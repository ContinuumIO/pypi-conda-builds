package:
  name: import_relative
  version: "0.2.3"

source:
  fn: import_relative-0.2.3.tar.gz
  url: https://pypi.python.org/packages/source/i/import_relative/import_relative-0.2.3.tar.gz
  md5: bff51a275f56e2d10200a06629c3535c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - import_relative = import_relative:main
    #
    # Would create an entry point called import_relative that calls import_relative.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - import_relative

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/pyimport-relative
  license: GNU General Public License (GPL)
  summary: 'A different kind of relative import'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
