package:
  name: django-spurl
  version: "0.6"

source:
  fn: django-spurl-0.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-spurl/django-spurl-0.6.tar.gz
  md5: 5c24543471128dcb1a3dbc2582381ea2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-spurl = django-spurl:main
    #
    # Would create an entry point called django-spurl that calls django-spurl.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - urlobject >=2.0.0

  run:
    - python
    - urlobject >=2.0.0

test:
  # Python imports
  imports:
    - spurl
    - spurl.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/j4mie/django-spurl/
  license: UNKNOWN
  summary: 'A Django template library for manipulating URLs.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
