package:
  name: djlime-settings
  version: "1.0.2"

source:
  fn: djlime-settings-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/djlime-settings/djlime-settings-1.0.2.tar.gz
  md5: ba29115e63b7f3aded06628114c3bbc5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djlime-settings = djlime-settings:main
    #
    # Would create an entry point called djlime-settings that calls djlime-settings.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - settings
    - settings.migrations
    - settings.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/whitespy/djlime-settings
  license: UNKNOWN
  summary: 'The application settings.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
