package:
  name: async_http
  version: "0.12"

source:
  fn: async_http-0.12.tar.gz
  url: https://pypi.python.org/packages/source/a/async_http/async_http-0.12.tar.gz
  md5: 69c4d9ce3f10e80b715d0698e00b4938
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - async_http = async_http:main
    #
    # Would create an entry point called async_http that calls async_http.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - async_http

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/josiahcarlson/async_http
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'An http/https client library that works with asyncore/asynchat'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
