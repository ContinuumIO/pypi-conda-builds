package:
  name: plonetheme.arnolfini
  version: "0.96"

source:
  fn: plonetheme.arnolfini-0.96.tar.gz
  url: https://pypi.python.org/packages/source/p/plonetheme.arnolfini/plonetheme.arnolfini-0.96.tar.gz
  md5: dd33ee1fa9b195648679fd1f7ac4fb60
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plonetheme.arnolfini = plonetheme.arnolfini:main
    #
    # Would create an entry point called plonetheme.arnolfini that calls plonetheme.arnolfini.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plonetheme.intkbase
    - plone.app.z3cform

  run:
    - python
    - setuptools
    - setuptools
    - plonetheme.intkbase
    - plone.app.z3cform

test:
  # Python imports
  imports:
    - plonetheme
    - plonetheme.arnolfini
    - plonetheme.arnolfini.browser
    - plonetheme.arnolfini.portlets

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/
  license: GPL
  summary: 'An installable theme for Plone 4'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
