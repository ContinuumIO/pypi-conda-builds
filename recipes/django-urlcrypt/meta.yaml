package:
  name: django-urlcrypt
  version: "0.1.6"

source:
  fn: django-urlcrypt-0.1.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-urlcrypt/django-urlcrypt-0.1.6.tar.gz
  md5: e4eb9121a22b4ae420bffec802f82ebe
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-urlcrypt = django-urlcrypt:main
    #
    # Would create an entry point called django-urlcrypt that calls django-urlcrypt.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - urlcrypt
    - urlcrypt.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/dziegler/django-urlcrypt
  license: BSD License
  summary: 'Encrypts information in urls, such as login credentials. Useful if you want to send a user a link that logs the user in without leaking their login credentials.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
