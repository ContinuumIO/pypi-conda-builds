package:
  name: django-uikit-editor
  version: "0.0.1"

source:
  fn: django-uikit-editor-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-uikit-editor/django-uikit-editor-0.0.1.tar.gz
  md5: ffb25757af18bd533e1fb8b9563f32fb
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-uikit-editor = django-uikit-editor:main
    #
    # Would create an entry point called django-uikit-editor that calls django-uikit-editor.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django
    - setuptools

  run:
    - python
    - django
    - setuptools

test:
  # Python imports
  imports:
    - uikit_editor

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/smidth/django-uikit-editor
  license: BSD License
  summary: 'uikit_editor provides integration for UiKit htmleditor with Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
