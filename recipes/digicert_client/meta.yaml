package:
  name: digicert_client
  version: "1.1"

source:
  fn: digicert_client-1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/digicert_client/digicert_client-1.1.tar.gz
  md5: bfe02cce2665d52acbdfcd60c7c2995e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - digicert_client = digicert_client:main
    #
    # Would create an entry point called digicert_client that calls digicert_client.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - digicert_client
    - digicert_client.api
    - digicert_client.api.commands
    - digicert_client.api.queries
    - digicert_client.https

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/digicert/digicert_client/python
  license: MIT License
  summary: 'DigiCert, Inc. web API client library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
