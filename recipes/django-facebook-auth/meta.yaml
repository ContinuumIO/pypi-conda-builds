package:
  name: django-facebook-auth
  version: "3.7.6"

source:
  fn: django-facebook-auth-3.7.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-facebook-auth/django-facebook-auth-3.7.6.tar.gz
  md5: 3500e9283854faf760c5e00662add6d1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-facebook-auth = django-facebook-auth:main
    #
    # Would create an entry point called django-facebook-auth that calls django-facebook-auth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - celery
    - django <1.8
    - facepy >=1.0.6

  run:
    - python
    - celery
    - django <1.8
    - facepy >=1.0.6

test:
  # Python imports
  imports:
    - facebook_auth
    - facebook_auth.management
    - facebook_auth.management.commands
    - facebook_auth.migrations
    - facebook_auth.south_migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'Authorisation app for Facebook API.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
