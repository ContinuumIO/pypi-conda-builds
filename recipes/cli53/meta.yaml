package:
  name: cli53
  version: "0.4.4"

source:
  fn: cli53-0.4.4.tar.gz
  url: https://pypi.python.org/packages/source/c/cli53/cli53-0.4.4.tar.gz
  md5: da4ba52e36a53aadaa537397e735fbfd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cli53 = cli53:main
    #
    # Would create an entry point called cli53 that calls cli53.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - boto
    - argparse
    - dnspython

  run:
    - python
    - boto
    - argparse
    - dnspython

test:
  # Python imports
  imports:
    - cli53
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://loads.pickle.me.uk/cli53/
  license: MIT License
  summary: 'Command line script to administer the Amazon Route 53 DNS service'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
