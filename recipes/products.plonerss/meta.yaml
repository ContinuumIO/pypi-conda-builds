package:
  name: products.plonerss
  version: "1.0.7beta"

source:
  fn: Products.PloneRSS-1.0.7beta.tar.gz
  url: https://pypi.python.org/packages/source/P/Products.PloneRSS/Products.PloneRSS-1.0.7beta.tar.gz
  md5: 2046a8f5da933ef3e0cc30618845ac1c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - products.plonerss = products.plonerss:main
    #
    # Would create an entry point called products.plonerss that calls products.plonerss.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - Products
    - Products.PloneRSS
    - Products.PloneRSS.content

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://encryptec.net/software-center/plonerss
  license: GPL
  summary: 'Plone RSS Integration'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
