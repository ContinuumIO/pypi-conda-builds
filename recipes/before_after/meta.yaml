package:
  name: before_after
  version: "0.1.3"

source:
  fn: before_after-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/b/before_after/before_after-0.1.3.tar.gz
  md5: 77a8ef0887695d277cf134f60f563678
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - before_after = before_after:main
    #
    # Would create an entry point called before_after that calls before_after.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - mock ==1.0.1

  run:
    - python
    - mock ==1.0.1

test:
  # Python imports
  imports:
    - before_after
    - before_after.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/c-oreills/before_after
  license: GPLv2
  summary: 'before_after provides utilities for testing race conditions'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
