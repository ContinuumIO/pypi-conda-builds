package:
  name: redislite
  version: "1.0.238"

source:
  fn: redislite-1.0.238.tar.gz
  url: https://pypi.python.org/packages/source/r/redislite/redislite-1.0.238.tar.gz
  md5: 707e3b9167b783aabc5816921d12cb13
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - redislite = redislite:main
    #
    # Would create an entry point called redislite that calls redislite.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - redis
    - psutil
    - jinja2

  run:
    - python
    - redis
    - psutil
    - jinja2

test:
  # Python imports
  imports:
    - redislite

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/yahoo/redislite
  license: BSD License
  summary: 'Redis built into a python package'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
