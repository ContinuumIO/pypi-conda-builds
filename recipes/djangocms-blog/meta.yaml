package:
  name: djangocms-blog
  version: "0.4.0"

source:
  fn: djangocms-blog-0.4.0.tar.gz
  url: https://pypi.python.org/packages/source/d/djangocms-blog/djangocms-blog-0.4.0.tar.gz
  md5: 886ac95f69ce26be59850be1decb004c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangocms-blog = djangocms-blog:main
    #
    # Would create an entry point called djangocms-blog that calls djangocms-blog.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-parler >=1.2
    - django-cms >=3.0
    - django-taggit >=0.12.2
    - django-filer
    - pytz
    - django-taggit-templatetags
    - django-taggit-autosuggest
    - django-admin-enhancer
    - djangocms-text-ckeditor
    - cmsplugin-filer
    - django-meta >=0.2
    - django-meta-mixin >=0.1.1
    - south >=1.0.1

  run:
    - python
    - django-parler >=1.2
    - django-cms >=3.0
    - django-taggit >=0.12.2
    - django-filer
    - pytz
    - django-taggit-templatetags
    - django-taggit-autosuggest
    - django-admin-enhancer
    - djangocms-text-ckeditor
    - cmsplugin-filer
    - django-meta >=0.2
    - django-meta-mixin >=0.1.1
    - south >=1.0.1

test:
  # Python imports
  imports:
    - djangocms_blog

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/nephila/djangocms-blog
  license: BSD License
  summary: 'A djangoCMS 3 blog application'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
