package:
  name: hashlib
  version: "20081119"

source:
  fn: hashlib-20081119.zip
  url: https://pypi.python.org/packages/source/h/hashlib/hashlib-20081119.zip
  md5: 46e59db6d5cac34b3344ef0c30d0897f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hashlib = hashlib:main
    #
    # Would create an entry point called hashlib that calls hashlib.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - hashlib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.krypto.org/python/hashlib/
  license: Python Software Foundation License
  summary: 'Secure hash and message digest algorithm library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
