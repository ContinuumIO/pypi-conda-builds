package:
  name: django-kronos
  version: "0.7"

source:
  fn: django-kronos-0.7.tar.gz
  url: https://pypi.python.org/packages/source/d/django-kronos/django-kronos-0.7.tar.gz
  md5: 02f6a9d44bf48312834fd73833abcd40
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-kronos = django-kronos:main
    #
    # Would create an entry point called django-kronos that calls django-kronos.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - kronos

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/jgorset/kronos
  license: UNKNOWN
  summary: 'Kronos is a Django application that makes it easy to define and schedule tasks with cron.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
