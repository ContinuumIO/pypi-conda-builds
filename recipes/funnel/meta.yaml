package:
  name: funnel
  version: "0.1"

source:
  fn: Funnel-0.1.tar.gz
  url: https://pypi.python.org/packages/source/F/Funnel/Funnel-0.1.tar.gz
  md5: f774085d5eef910eea9049db57e3714f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - funnel = funnel:main
    #
    # Would create an entry point called funnel that calls funnel.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.8
    - beaker ==1.6.3

  run:
    - python
    - flask >=0.8
    - beaker ==1.6.3

test:
  # Python imports
  imports:
    - funnel

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/spoqa/funnel
  license: UNKNOWN
  summary: 'Flask extension for Beaker'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
