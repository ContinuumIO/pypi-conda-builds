package:
  name: django-default-dont-cache
  version: "0.1.0"

source:
  fn: django-default-dont-cache-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-default-dont-cache/django-default-dont-cache-0.1.0.tar.gz
  md5: 2a166cc12d3e58c4d57de859f98429cc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-default-dont-cache = django-default-dont-cache:main
    #
    # Would create an entry point called django-default-dont-cache that calls django-default-dont-cache.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - default_dont_cache

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: GPLv3
  summary: "Opt-in caching for Django's per-site cache"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
