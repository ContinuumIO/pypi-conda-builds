package:
  name: imagecontour
  version: "0.2.3.1"

source:
  fn: ImageContour-0.2.3.1.tar.gz
  url: https://pypi.python.org/packages/source/I/ImageContour/ImageContour-0.2.3.1.tar.gz
  md5: 44496af349e3171155dc7d2c9ad43055
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - imagecontour = imagecontour:main
    #
    # Would create an entry point called imagecontour that calls imagecontour.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - numpy >=1.0
    - matplotlib >=1.0
    - np_utils >=0.3.1.1

  run:
    - python
    - numpy >=1.0
    - matplotlib >=1.0
    - np_utils >=0.3.1.1

test:
  # Python imports
  imports:
    - ImageContour
    - imagecontour

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/ImageContour/
  license: LICENSE.txt
  summary: 'construct and use linear paths around simply-connected binary and greyscale images'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
