package:
  name: hitt
  version: "0.1.22"

source:
  fn: hitt-0.1.22.tar.gz
  url: https://pypi.python.org/packages/source/h/hitt/hitt-0.1.22.tar.gz
  md5: 4f4fcc88b16db2e080070b4593e4daae
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hitt = hitt:main
    #
    # Would create an entry point called hitt that calls hitt.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - numpy >=1.6.1
    - pil >=1.1.7
    - pygame >=1.9.1
    - pyopengl >=3.0.2a5
    - pydispatcher >=2.0.3

  run:
    - python
    - numpy >=1.6.1
    - pil >=1.1.7
    - pygame >=1.9.1
    - pyopengl >=3.0.2a5
    - pydispatcher >=2.0.3

test:
  # Python imports
  imports:
    - hitt
    - hitt.game
    - hitt.map
    - hitt.test
    - hitt.ui

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/hitt/
  license: LICENSE.txt
  summary: 'A tiny 2D game development framework.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
