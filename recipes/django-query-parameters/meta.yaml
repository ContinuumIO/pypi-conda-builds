package:
  name: django-query-parameters
  version: "0.2.1"

source:
  fn: django-query-parameters-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-query-parameters/django-query-parameters-0.2.1.tar.gz
  md5: c52d56e9ebacebf474706c79186aca03
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-query-parameters = django-query-parameters:main
    #
    # Would create an entry point called django-query-parameters that calls django-query-parameters.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django >=1.4

  run:
    - python
    - django >=1.4

test:
  # Python imports
  imports:
    - query_parameters
    - query_parameters.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jskopek/django-query-parameters
  license: LICENSE
  summary: 'Django templatetags to simplify creating, updating, and removing query parameters from querystring'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
