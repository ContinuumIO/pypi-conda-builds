package:
  name: medialog.portlet.sitemap
  version: "4.0a2"

source:
  fn: medialog.portlet.sitemap-4.0a2.tar.gz
  url: https://pypi.python.org/packages/source/m/medialog.portlet.sitemap/medialog.portlet.sitemap-4.0a2.tar.gz
  md5: caa80988c1ea90db276d44224f4a8313
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - medialog.portlet.sitemap = medialog.portlet.sitemap:main
    #
    # Would create an entry point called medialog.portlet.sitemap that calls medialog.portlet.sitemap.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - medialog
    - medialog.portlet
    - medialog.portlet.sitemap
    - medialog.portlet.sitemap.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://medialog.no
  license: GPL
  summary: 'Shows the whole sitemap in a portlet. For Plone 4'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
