package:
  name: django-aws-manager
  version: "0.1.2.5"

source:
  fn: django-aws-manager-0.1.2.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-aws-manager/django-aws-manager-0.1.2.5.tar.gz
  md5: 91f37068f102b9dd499edf59f082074a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-aws-manager = django-aws-manager:main
    #
    # Would create an entry point called django-aws-manager that calls django-aws-manager.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4
    - boto

  run:
    - python
    - django >=1.4
    - boto

test:
  # Python imports
  imports:
    - aws_manager
    - aws_manager.management
    - aws_manager.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jromich/django-aws-manager
  license: MIT License
  summary: 'A Django app to manage Amazon AWS servers.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
