package:
  name: okaara
  version: "1.0.33"

source:
  fn: okaara-1.0.33.tar.gz
  url: https://pypi.python.org/packages/source/o/okaara/okaara-1.0.33.tar.gz
  md5: d02d657e0d589d373e0e89d432fef0fc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - okaara = okaara:main
    #
    # Would create an entry point called okaara that calls okaara.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - okaara

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://okaara.readthedocs.org/en/latest/
  license: GNU General Public License (GPL)
  summary: 'Python command line utilities'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
