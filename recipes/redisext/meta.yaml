package:
  name: redisext
  version: "1.2.3"

source:
  fn: redisext-1.2.3.tar.gz
  url: https://pypi.python.org/packages/source/r/redisext/redisext-1.2.3.tar.gz
  md5: dc671e95bf34435b9acc705806bc800a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - redisext = redisext:main
    #
    # Would create an entry point called redisext that calls redisext.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - redisext
    - redisext.backend
    - redisext.models

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mylokin/redisext
  license: MIT License
  summary: 'Data models for Redis'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
