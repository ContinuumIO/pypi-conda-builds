package:
  name: silva.pas.membership
  version: "1.2"

source:
  fn: silva.pas.membership-1.2.tar.gz
  url: https://pypi.python.org/packages/source/s/silva.pas.membership/silva.pas.membership-1.2.tar.gz
  md5: 5b225b4b8e82c37567f0ed7e16ace772
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - silva.pas.membership = silva.pas.membership:main
    #
    # Would create an entry point called silva.pas.membership that calls silva.pas.membership.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - silva.pas.base
    - setuptools

  run:
    - python
    - silva.pas.base
    - setuptools

test:
  # Python imports
  imports:
    - silva
    - silva.pas
    - silva.pas.membership
    - silva.pas.membership.plugins

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/silvacms/silva.pas.membership
  license: BSD License
  summary: 'Retrieve Silva membership information in PAS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
