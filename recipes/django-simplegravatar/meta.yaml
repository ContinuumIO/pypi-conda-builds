package:
  name: django-simplegravatar
  version: "0.2"

source:
  fn: django-simplegravatar-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-simplegravatar/django-simplegravatar-0.2.tar.gz
  md5: f52dc0d001e2d2e3dd90135ffbfa94b3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-simplegravatar = django-simplegravatar:main
    #
    # Would create an entry point called django-simplegravatar that calls django-simplegravatar.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - simplegravatar

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://tomatohater.com/django-simplegravatar/
  license: GPLv3
  summary: 'A simpler Django template tag to add Gravatar support to your Django projects.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
