package:
  name: zeam.jsontemplate
  version: "0.1"

source:
  fn: zeam.jsontemplate-0.1.tar.gz
  url: https://pypi.python.org/packages/source/z/zeam.jsontemplate/zeam.jsontemplate-0.1.tar.gz
  md5: b2dd2814817354b8395ff880d489fd2c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zeam.jsontemplate = zeam.jsontemplate:main
    #
    # Would create an entry point called zeam.jsontemplate that calls zeam.jsontemplate.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fanstatic
    - setuptools

  run:
    - python
    - setuptools
    - fanstatic
    - setuptools

test:
  # Python imports
  imports:
    - zeam
    - zeam.jsontemplate

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD
  summary: 'Fanstatic packaging of json template'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
