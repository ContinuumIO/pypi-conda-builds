package:
  name: coop-bar
  version: "0.4.6"

source:
  fn: coop-bar-0.4.6.tar.gz
  url: https://pypi.python.org/packages/source/c/coop-bar/coop-bar-0.4.6.tar.gz
  md5: f5ac89719fc4fd55a37530bd77f371ef
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - coop-bar = coop-bar:main
    #
    # Would create an entry point called coop-bar that calls coop-bar.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - coop_bar
    - coop_bar.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/quinode/coop-bar/
  license: BSD License
  summary: 'Pluggable admin bar system , works well with coop-cms'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
