package:
  name: django-fusionbox-blog
  version: "1.0.2"

source:
  fn: django-fusionbox-blog-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-fusionbox-blog/django-fusionbox-blog-1.0.2.tar.gz
  md5: abe5b907f785b97ba7aa9bdde6627669
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-fusionbox-blog = django-fusionbox-blog:main
    #
    # Would create an entry point called django-fusionbox-blog that calls django-fusionbox-blog.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django_fusionbox

  run:
    - python
    - django_fusionbox

test:
  # Python imports
  imports:
    - fusionbox
    - fusionbox.blog
    - fusionbox.blog.management
    - fusionbox.blog.management.commands
    - fusionbox.blog.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/fusionbox/django-fusionbox-blog
  license: BSD
  summary: '\nReusable blog application for Django\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
