package:
  name: django-ses-backend
  version: "0.1.1"

source:
  fn: django-ses-backend-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-ses-backend/django-ses-backend-0.1.1.tar.gz
  md5: 09d9d062818d6c1a5098edb1ed1a77ee
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-ses-backend = django-ses-backend:main
    #
    # Would create an entry point called django-ses-backend that calls django-ses-backend.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5
    - boto >=2.32.0

  run:
    - python
    - django >=1.5
    - boto >=2.32.0

test:
  # Python imports
  imports:
    - django_ses_backend

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/piotrbulinski/django-ses-backend/
  license: MIT License
  summary: "A Django email backend for Amazon's Simple Email Service"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
