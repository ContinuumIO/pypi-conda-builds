package:
  name: bda.cache
  version: "1.1.3"

source:
  fn: bda.cache-1.1.3.tar.gz
  url: https://pypi.python.org/packages/source/b/bda.cache/bda.cache-1.1.3.tar.gz
  md5: 404a7bc6cec637b1369e20d61861a360
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bda.cache = bda.cache:main
    #
    # Would create an entry point called bda.cache that calls bda.cache.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - python-memcached
    - zope.component

  run:
    - python
    - setuptools
    - setuptools
    - python-memcached
    - zope.component

test:
  # Python imports
  imports:
    - bda
    - bda.cache
    - bda.cache.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://svn.plone.org/svn/collective/bda.cache
  license: GNU General Public License (GPL)
  summary: 'Simple caching infrastructure.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
