package:
  name: pubnub-console
  version: "3.5.2"

source:
  fn: pubnub-console-3.5.2.tar.gz
  url: https://pypi.python.org/packages/source/p/pubnub-console/pubnub-console-3.5.2.tar.gz
  md5: 1abcc45e9c7e625952960a6c0a868002
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pubnub-console = pubnub-console:main
    #
    # Would create an entry point called pubnub-console that calls pubnub-console.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pubnub >=3.5.2
    - cmd2 >=0.6.7
    - pygments >=1.6

  run:
    - python
    - pubnub >=3.5.2
    - cmd2 >=0.6.7
    - pygments >=1.6

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pubnub.com
  license: MIT License
  summary: 'PubNub Developer Console'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
