package:
  name: django-sticky-messages
  version: "0.1"

source:
  fn: django-sticky-messages-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-sticky-messages/django-sticky-messages-0.1.tar.gz
  md5: d9f2f42f82b57d54c26a8c10b4c1f41c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-sticky-messages = django-sticky-messages:main
    #
    # Would create an entry point called django-sticky-messages that calls django-sticky-messages.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - stickymessages

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rlepore/django-sticky-messages
  license: BSD License
  summary: 'A simple Django app to display "sticky" messages that expire after a given time period.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
