package:
  name: dynaq
  version: "0.1.0"

source:
  fn: DynaQ-0.1.0.zip
  url: https://pypi.python.org/packages/source/D/DynaQ/DynaQ-0.1.0.zip
  md5: 28f6d19465d3fd3e3339109024fd4969
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dynaq = dynaq:main
    #
    # Would create an entry point called dynaq that calls dynaq.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - dynaq

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/claudiodriussi/DynaQ
  license: GNU Lesser General Public License v2 or later (LGPLv2+)
  summary: 'Dynamic creation of SQLAlchemy orm objects from YAML files'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
