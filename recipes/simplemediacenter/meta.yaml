package:
  name: simplemediacenter
  version: "0.6.6"

source:
  fn: simpleMediaCenter-0.6.6.tar.gz
  url: https://pypi.python.org/packages/source/s/simpleMediaCenter/simpleMediaCenter-0.6.6.tar.gz
  md5: 45af9fa2b7b0fef9a228c278033f18b0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - simplemediacenter = simplemediacenter:main
    #
    # Would create an entry point called simplemediacenter that calls simplemediacenter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - simpleMediaCenter
    - simpleMediaCenter.browser
    - simpleMediaCenter.helpers
    - simpleMediaCenter.helpers.twitch
    - simpleMediaCenter.helpers.youtube
    - simpleMediaCenter.interface
    - simpleMediaCenter.player
    - simpleMediaCenter.playlist
    - simplemediacenter

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ingwinlu/simpleMediaCenter
  license: GPLv3
  summary: 'simpleMediaCenter aims to provide an easy on ressources way to use your computer as an Media Center. It is primarily designed to act as an alternative to XBMC on the rpi.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
