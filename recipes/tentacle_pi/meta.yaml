package:
  name: tentacle_pi
  version: "0.5.0"

source:
  fn: tentacle_pi-0.5.0.tar.gz
  url: https://pypi.python.org/packages/source/t/tentacle_pi/tentacle_pi-0.5.0.tar.gz
  md5: 3651b12b54ee12241aa83051f13227ff
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tentacle_pi = tentacle_pi:main
    #
    # Would create an entry point called tentacle_pi that calls tentacle_pi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - tentacle_pi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lexruee/tentacle_pi
  license: MIT
  summary: 'Tentacles for the Raspberry Pi!'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
