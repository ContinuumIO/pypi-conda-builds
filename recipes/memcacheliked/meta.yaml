package:
  name: memcacheliked
  version: "0.2.1"

source:
  fn: memcacheliked-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/m/memcacheliked/memcacheliked-0.2.1.tar.gz
  md5: dee8599c0736df07df96e84a6ddd6fc0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - memcacheliked = memcacheliked:main
    #
    # Would create an entry point called memcacheliked that calls memcacheliked.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - diesel

  run:
    - python
    - diesel

test:
  # Python imports
  imports:
    - memcacheliked

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/ViciousRedBeam/memcacheliked
  license: MIT
  summary: 'Simple framework for writing daemons using the memcache interface for storing data'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
