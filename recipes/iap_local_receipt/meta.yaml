package:
  name: iap_local_receipt
  version: "0.1.1"

source:
  fn: iap_local_receipt-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/i/iap_local_receipt/iap_local_receipt-0.1.1.tar.gz
  md5: 0b53f3938356f084537930aee3cd8b5b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - iap_local_receipt = iap_local_receipt:main
    #
    # Would create an entry point called iap_local_receipt that calls iap_local_receipt.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyasn1
    - pyasn1_modules
    - m2crypto

  run:
    - python
    - pyasn1
    - pyasn1_modules
    - m2crypto

test:
  # Python imports
  imports:
    - iap_local_receipt

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/SilentCircle/iap-local-receipt
  license: BSD License
  summary: 'A library for local verification of Apple in-app receipts.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
