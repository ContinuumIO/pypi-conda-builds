package:
  name: sorl-thumbnail-async
  version: "0.6.0"

source:
  fn: sorl-thumbnail-async-0.6.0.tar.gz
  url: https://pypi.python.org/packages/source/s/sorl-thumbnail-async/sorl-thumbnail-async-0.6.0.tar.gz
  md5: 69c5268673e6a4f3d694e9ba85a486f7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sorl-thumbnail-async = sorl-thumbnail-async:main
    #
    # Would create an entry point called sorl-thumbnail-async that calls sorl-thumbnail-async.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - thumbnail
    - thumbnail.conf
    - thumbnail.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/chhantyal/sorl-thumbnail-async
  license: UNKNOWN
  summary: 'Asynchronous thumbnailing app in django with remote storages like S3'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
