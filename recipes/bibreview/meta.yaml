package:
  name: bibreview
  version: "0.2.2"

source:
  fn: BibReview-0.2.2.tar.bz2
  url: https://pypi.python.org/packages/source/B/BibReview/BibReview-0.2.2.tar.bz2
  md5: 641db7a3bbd7d5db5cf6d56001bd91ce
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bibreview = bibreview:main
    #
    # Would create an entry point called bibreview that calls bibreview.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - bibreview

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.lesfleursdunormal.fr/static/informatique/bibreview/index_fr.html
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'BibReview is a software for managing bibliographic database. It includes avanced functions for literature reviews.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
