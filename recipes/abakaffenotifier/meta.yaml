package:
  name: abakaffenotifier
  version: "1.0.0"

source:
  fn: AbakaffeNotifier-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/A/AbakaffeNotifier/AbakaffeNotifier-1.0.0.tar.gz
  md5: 69e9d463cd2386cb9f4e248b12dfafc7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - abakaffenotifier = abakaffenotifier:main
    #
    # Would create an entry point called abakaffenotifier that calls abakaffenotifier.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - requests >=1.2.3

  run:
    - python
    - requests >=1.2.3

test:
  # Python imports
  imports:
    - abakaffenotifier
    - pynma

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/AbakaffeNotifier/
  license: UNKNOWN
  summary: 'Notifies phone when Abakaffe is ready.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
