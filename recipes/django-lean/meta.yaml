package:
  name: django-lean
  version: "0.15"

source:
  fn: django-lean-0.15.tar.gz
  url: https://pypi.python.org/packages/source/d/django-lean/django-lean-0.15.tar.gz
  md5: 04e3189fd87acd2855e51655b0f4d5e8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-lean = django-lean:main
    #
    # Would create an entry point called django-lean that calls django-lean.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django >=1.0
    - beautifulsoup
    - mox

  run:
    - python
    - django >=1.0
    - beautifulsoup
    - mox

test:
  # Python imports
  imports:
    - django_lean
    - django_lean.experiments
    - django_lean.experiments.management
    - django_lean.experiments.management.commands
    - django_lean.experiments.migrations
    - django_lean.experiments.templatetags
    - django_lean.experiments.tests
    - django_lean.lean_analytics
    - django_lean.lean_retention
    - django_lean.lean_retention.migrations
    - django_lean.lean_retention.tests
    - django_lean.lean_segments
    - django_lean.lean_segments.management
    - django_lean.lean_segments.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/akoha/django-lean/wiki/Home
  license: BSD License
  summary: 'A framework for performing and analyzing split-test experiments in Django applications.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
