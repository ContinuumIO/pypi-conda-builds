package:
  name: chomsky
  version: "1.0.2"

source:
  fn: chomsky-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/c/chomsky/chomsky-1.0.2.tar.gz
  md5: bd3b30648f14d8054a5b865ccf8c157a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - chomsky = chomsky:main
    #
    # Would create an entry point called chomsky that calls chomsky.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - chomsky

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/colinta/chomsky
  license: BSD License
  summary: 'Another language grammar parser.  Inspired by modgrammar and pyparsing'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
