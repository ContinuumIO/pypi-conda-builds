package:
  name: plone.app.workflow
  version: "2.2.3"

source:
  fn: plone.app.workflow-2.2.3.zip
  url: https://pypi.python.org/packages/source/p/plone.app.workflow/plone.app.workflow-2.2.3.zip
  md5: b402d2411fea6497fa7698d889724cdd
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.app.workflow = plone.app.workflow:main
    #
    # Would create an entry point called plone.app.workflow that calls plone.app.workflow.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - plone.memoize
    - transaction
    - zope.component
    - zope.dottedname
    - zope.i18n
    - zope.i18nmessageid
    - zope.interface
    - zope.lifecycleevent
    - zope.schema
    - zope.site
    - zope.testing
    - acquisition
    - datetime
    - products.cmfplone
    - products.cmfcore
    - products.dcworkflow
    - products.genericsetup
    - products.statusmessages
    - zope2

  run:
    - python
    - setuptools
    - plone.memoize
    - transaction
    - zope.component
    - zope.dottedname
    - zope.i18n
    - zope.i18nmessageid
    - zope.interface
    - zope.lifecycleevent
    - zope.schema
    - zope.site
    - zope.testing
    - acquisition
    - datetime
    - products.cmfplone
    - products.cmfcore
    - products.dcworkflow
    - products.genericsetup
    - products.statusmessages
    - zope2

test:
  # Python imports
  imports:
    - plone
    - plone.app
    - plone.app.workflow
    - plone.app.workflow.browser
    - plone.app.workflow.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/plone.app.workflow
  license: GNU General Public License (GPL)
  summary: 'workflow and security settings for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
