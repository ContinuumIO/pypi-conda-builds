package:
  name: cloudtb
  version: "0.2.2"

source:
  fn: cloudtb-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/c/cloudtb/cloudtb-0.2.2.tar.gz
  md5: a1756f0e9416ee45d33cd41bdd7714c9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cloudtb = cloudtb:main
    #
    # Would create an entry point called cloudtb that calls cloudtb.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six

  run:
    - python
    - six

test:
  # Python imports
  imports:
    - cloudtb

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/cloudformdesign/cloudtb
  license: MIT License
  summary: 'Open source methods to solve common problems'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
