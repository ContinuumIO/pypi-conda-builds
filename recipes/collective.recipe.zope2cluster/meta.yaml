package:
  name: collective.recipe.zope2cluster
  version: "1.1"

source:
  fn: collective.recipe.zope2cluster-1.1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.recipe.zope2cluster/collective.recipe.zope2cluster-1.1.tar.gz
  md5: 64eaa5cba6236e8d00235d0b34f69036
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.recipe.zope2cluster = collective.recipe.zope2cluster:main
    #
    # Would create an entry point called collective.recipe.zope2cluster that calls collective.recipe.zope2cluster.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - plone.recipe.zope2instance

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - plone.recipe.zope2instance

test:
  # Python imports
  imports:
    - collective
    - collective.recipe
    - collective.recipe.zope2cluster
    - collective.recipe.zope2cluster.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://sixfeetup.com
  license: Zope Public License
  summary: 'A simple recipe to create a zope cluster'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
