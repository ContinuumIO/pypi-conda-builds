package:
  name: dcluster
  version: "0.5.0"

source:
  fn: Dcluster-0.5.0.tar.gz
  url: https://pypi.python.org/packages/source/D/Dcluster/Dcluster-0.5.0.tar.gz
  md5: afca0d2a33fe9e9d35d1032f86783880
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dcluster = dcluster:main
    #
    # Would create an entry point called dcluster that calls dcluster.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy >=1.2.0
    - matplotlib >=1.1.0

  run:
    - python
    - numpy >=1.2.0
    - matplotlib >=1.1.0

test:
  # Python imports
  imports:
    - Dcluster
    - dcluster

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/GuipengLi/Dcluster
  license: UNKNOWN
  summary: 'A Python package for Clustering by fast search and find of density peaks'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
