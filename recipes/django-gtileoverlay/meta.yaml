package:
  name: django-gtileoverlay
  version: "1.0.0.final"

source:
  fn: django-gtileoverlay-1.0.0.final.tar.gz
  url: https://pypi.python.org/packages/source/d/django-gtileoverlay/django-gtileoverlay-1.0.0.final.tar.gz
  md5: 20074a89a9b7bc0e56ce591ce5a9ac14
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-gtileoverlay = django-gtileoverlay:main
    #
    # Would create an entry point called django-gtileoverlay that calls django-gtileoverlay.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - gtileoverlay

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/nbv4/django-gtileoverlay
  license: BSD License
  summary: 'Create GTileOverlay images in Django for the Google Maps API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
