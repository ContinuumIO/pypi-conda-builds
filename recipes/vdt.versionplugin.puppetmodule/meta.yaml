package:
  name: vdt.versionplugin.puppetmodule
  version: "0.1"

source:
  fn: vdt.versionplugin.puppetmodule-0.1.tar.gz
  url: https://pypi.python.org/packages/source/v/vdt.versionplugin.puppetmodule/vdt.versionplugin.puppetmodule-0.1.tar.gz
  md5: 5e136e8d74ada65a35b1d206c452e8fc
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - vdt.versionplugin.puppetmodule = vdt.versionplugin.puppetmodule:main
    #
    # Would create an entry point called vdt.versionplugin.puppetmodule that calls vdt.versionplugin.puppetmodule.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - vdt.version
    - vdt.versionplugin.default

  run:
    - python
    - setuptools
    - vdt.version
    - vdt.versionplugin.default

test:
  # Python imports
  imports:
    - vdt
    - vdt.versionplugin
    - vdt.versionplugin.puppetmodule

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'Version Increment Plugin that builds a package from a puppet module'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
