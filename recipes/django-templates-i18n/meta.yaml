package:
  name: django-templates-i18n
  version: "0.1.0"

source:
  fn: django-templates-i18n-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-templates-i18n/django-templates-i18n-0.1.0.tar.gz
  md5: 3856d49d68020433e68131b2a4ae58e3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-templates-i18n = django-templates-i18n:main
    #
    # Would create an entry point called django-templates-i18n that calls django-templates-i18n.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - south
    - django_modeltranslation
    - django

  run:
    - python
    - south
    - django_modeltranslation
    - django

test:
  # Python imports
  imports:
    - templates_i18n

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/PragmaticMates/django-templates-i18n
  license: BSD License
  summary: 'Translatable templates'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
