package:
  name: amocrm_api
  version: "0.1.1"

source:
  fn: amocrm_api-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/a/amocrm_api/amocrm_api-0.1.1.tar.gz
  md5: c3d802cc4ac305a1cbb6c446cd720faf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - amocrm_api = amocrm_api:main
    #
    # Would create an entry point called amocrm_api that calls amocrm_api.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=2.3
    - responses >=0.2.2
    - six >=1.7.3

  run:
    - python
    - requests >=2.3
    - responses >=0.2.2
    - six >=1.7.3

test:
  # Python imports
  imports:
    - amocrm
    - amocrm_api

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Krukov/amocrm_api
  license: MIT License
  summary: 'Python (Django like) API for Amocrm'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
