package:
  name: django-bitly
  version: "0.10"

source:
  fn: django-bitly-0.10.tar.gz
  url: https://pypi.python.org/packages/source/d/django-bitly/django-bitly-0.10.tar.gz
  md5: 4a8016d99cbd30c642f48b01dcfc8020
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-bitly = django-bitly:main
    #
    # Would create an entry point called django-bitly that calls django-bitly.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django >=1.3
    - six

  run:
    - python
    - django >=1.3
    - six

test:
  # Python imports
  imports:
    - django_bitly
    - django_bitly.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/discovery/django-bitly/
  license: BSD License
  summary: 'Bit.ly integration for django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
