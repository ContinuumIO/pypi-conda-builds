package:
  name: js.jquery_autosize
  version: "1.17.1"

source:
  fn: js.jquery_autosize-1.17.1.tar.gz
  url: https://pypi.python.org/packages/source/j/js.jquery_autosize/js.jquery_autosize-1.17.1.tar.gz
  md5: 131bc23eb9769f90c79068534415fba0
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - js.jquery_autosize = js.jquery_autosize:main
    #
    # Would create an entry point called js.jquery_autosize that calls js.jquery_autosize.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fanstatic
    - js.jquery
    - setuptools

  run:
    - python
    - setuptools
    - fanstatic
    - js.jquery
    - setuptools

test:
  # Python imports
  imports:
    - js
    - js.jquery_autosize

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD
  summary: 'fanstatic jQuery autosize.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
