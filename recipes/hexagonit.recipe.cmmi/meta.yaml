package:
  name: hexagonit.recipe.cmmi
  version: "2.0"

source:
  fn: hexagonit.recipe.cmmi-2.0.tar.gz
  url: https://pypi.python.org/packages/source/h/hexagonit.recipe.cmmi/hexagonit.recipe.cmmi-2.0.tar.gz
  md5: df2289c758c2052326a7d133bfc57989
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hexagonit.recipe.cmmi = hexagonit.recipe.cmmi:main
    #
    # Would create an entry point called hexagonit.recipe.cmmi that calls hexagonit.recipe.cmmi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zc.buildout
    - setuptools
    - hexagonit.recipe.download

  run:
    - python
    - setuptools
    - zc.buildout
    - setuptools
    - hexagonit.recipe.download

test:
  # Python imports
  imports:
    - hexagonit
    - hexagonit.recipe
    - hexagonit.recipe.cmmi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/hexagonit/hexagonit.recipe.cmmi
  license: BSD License
  summary: 'zc.buildout recipe for compiling and installing source distributions.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
