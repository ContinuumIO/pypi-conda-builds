package:
  name: django-digg-paginator
  version: "0.1.2"

source:
  fn: django-digg-paginator-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-digg-paginator/django-digg-paginator-0.1.2.tar.gz
  md5: 0f5a824984f0afc79ea21ab0fb2e0fc1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-digg-paginator = django-digg-paginator:main
    #
    # Would create an entry point called django-digg-paginator that calls django-digg-paginator.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - digg_paginator

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/alrusdi/django_digg_paginator
  license: GPL3
  summary: 'Digg-like Paginator from Django Snippets'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
