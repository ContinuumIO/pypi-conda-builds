package:
  name: toredis
  version: "0.1.2"

source:
  fn: toredis-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/t/toredis/toredis-0.1.2.tar.gz
  md5: 735f9356c338da1b64d90f50b6c70cdc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - toredis = toredis:main
    #
    # Would create an entry point called toredis that calls toredis.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - tornado
    - hiredis

  run:
    - python
    - tornado
    - hiredis

test:
  # Python imports
  imports:
    - toredis

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/mrjoes/toredis/
  license: http://www.apache.org/licenses/LICENSE-2.0
  summary: 'Really simple async Redis client for Tornado'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
