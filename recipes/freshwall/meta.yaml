package:
  name: freshwall
  version: "1.1.2"

source:
  fn: freshwall-1.1.2.tar.gz
  url: https://pypi.python.org/packages/source/f/freshwall/freshwall-1.1.2.tar.gz
  md5: bf8911f94ab37766f64bfd5c1f39cb30
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - freshwall = freshwall:main
    #
    # Would create an entry point called freshwall that calls freshwall.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - freshwall

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.sapphirepaw.org/projects/freshwall.php
  license: Apache 2.0
  summary: 'GNOME wallpaper changer'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
