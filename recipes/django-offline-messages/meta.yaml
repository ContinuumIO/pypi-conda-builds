package:
  name: django-offline-messages
  version: "0.3.4"

source:
  fn: django-offline-messages-0.3.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-offline-messages/django-offline-messages-0.3.4.tar.gz
  md5: cb9a6a370c511ecebaaf38e77ffcedc1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-offline-messages = django-offline-messages:main
    #
    # Would create an entry point called django-offline-messages that calls django-offline-messages.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-jsonfield

  run:
    - python
    - django-jsonfield

test:
  # Python imports
  imports:
    - offline_messages
    - offline_messages.migrations
    - offline_messages.south_migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dym/django-offline-messages
  license: BSD License
  summary: 'A package that implements offline messages for Django plus more'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
