package:
  name: latexrender
  version: "0.3.6"

source:
  fn: latexrender-0.3.6.tar.gz
  url: https://pypi.python.org/packages/source/l/latexrender/latexrender-0.3.6.tar.gz
  md5: 33e8905c492b05e2b32d88e8143f253b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - latexrender = latexrender:main
    #
    # Would create an entry point called latexrender that calls latexrender.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - pillow

  run:
    - python
    - flask
    - pillow

test:
  # Python imports
  imports:
    - latexrender
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lpomfrey/latexrender
  license: BSD License
  summary: u'A simple Flask app for rendering latex snippets into images.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
