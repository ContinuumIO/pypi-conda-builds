package:
  name: django-npm
  version: "0.1.1"

source:
  fn: django-npm-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-npm/django-npm-0.1.1.tar.gz
  md5: 68ba614d61dcb5b4ed7ec0aaa970609c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-npm = django-npm:main
    #
    # Would create an entry point called django-npm that calls django-npm.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - npm

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kevin1024/django-npm
  license: MIT License
  summary: 'A django staticfiles finder that uses npm'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
