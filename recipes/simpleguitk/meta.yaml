package:
  name: simpleguitk
  version: "1.1.3"

source:
  fn: SimpleGUITk-1.1.3.tar.gz
  url: https://pypi.python.org/packages/source/S/SimpleGUITk/SimpleGUITk-1.1.3.tar.gz
  md5: d940d6b25a72b941f3c43e76bff95eb0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - simpleguitk = simpleguitk:main
    #
    # Would create an entry point called simpleguitk that calls simpleguitk.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pillow >=2.0.0
    - pygame >=1.9.0

  run:
    - python
    - pillow >=2.0.0
    - pygame >=1.9.0

test:
  # Python imports
  imports:
    - simpleguitk
    - simpleplot

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/dholm/simpleguitk/
  license: BSD
  summary: 'A wrapper for the CodeSkulptor SimpleGUI API using TkInter'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
