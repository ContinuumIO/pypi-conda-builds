package:
  name: hl7tersely
  version: "1.0"

source:
  fn: hl7tersely-1.0.tar.gz
  url: https://pypi.python.org/packages/source/h/hl7tersely/hl7tersely-1.0.tar.gz
  md5: e85a803dc8bc311717b659d61a1a4227
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hl7tersely = hl7tersely:main
    #
    # Would create an entry point called hl7tersely that calls hl7tersely.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - hl7tersely

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/flrt/hl7tersely
  license: BSD License
  summary: 'Lightweight HL7 parser focused on HL7 tersers'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
