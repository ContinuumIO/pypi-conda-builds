package:
  name: django_resort
  version: "0.1.0"

source:
  fn: django_resort-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django_resort/django_resort-0.1.0.tar.gz
  md5: b12ff4805b9466377525d49c0615beb3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_resort = django_resort:main
    #
    # Would create an entry point called django_resort that calls django_resort.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_resort
    - resort

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/alixedi/django_resort
  license: BSD License
  summary: 'Implement sorting for ListView columns without changing your views.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
