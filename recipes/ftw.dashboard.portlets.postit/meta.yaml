package:
  name: ftw.dashboard.portlets.postit
  version: "1.3.4"

source:
  fn: ftw.dashboard.portlets.postit-1.3.4.zip
  url: https://pypi.python.org/packages/source/f/ftw.dashboard.portlets.postit/ftw.dashboard.portlets.postit-1.3.4.zip
  md5: 5d60673e9ced0275290b95d0f1d1c954
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ftw.dashboard.portlets.postit = ftw.dashboard.portlets.postit:main
    #
    # Would create an entry point called ftw.dashboard.portlets.postit that calls ftw.dashboard.portlets.postit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - ftw
    - ftw.dashboard
    - ftw.dashboard.portlets
    - ftw.dashboard.portlets.postit
    - ftw.dashboard.portlets.postit.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/4teamwork/ftw.dashboard.portlets.postit
  license: GPL2
  summary: 'Postit portlet for the dashboard'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
