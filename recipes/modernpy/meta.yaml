package:
  name: modernpy
  version: "0.1.0"

source:
  fn: modernpy-0.1.0.zip
  url: https://pypi.python.org/packages/source/m/modernpy/modernpy-0.1.0.zip
  md5: 1a1fa07e65a55a35383c16b5542dba6f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - modernpy = modernpy:main
    #
    # Would create an entry point called modernpy that calls modernpy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - forbiddenfruit

  run:
    - python
    - forbiddenfruit

test:
  # Python imports
  imports:
    - modernpy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/shinznatkid/modernpy
  license: MIT License
  summary: 'Extension methods for built-in type in Python.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
