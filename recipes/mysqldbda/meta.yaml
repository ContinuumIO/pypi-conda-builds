package:
  name: mysqldbda
  version: "1.0.2"

source:
  fn: mysqldbda-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/m/mysqldbda/mysqldbda-1.0.2.tar.gz
  md5: 1c898a4d6b9a192346f8393db2e00656
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mysqldbda = mysqldbda:main
    #
    # Would create an entry point called mysqldbda that calls mysqldbda.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.interface
    - zope.schema
    - zope.rdb
    - zope.publisher
    - zope.app.form

  run:
    - python
    - setuptools
    - zope.interface
    - zope.schema
    - zope.rdb
    - zope.publisher
    - zope.app.form

test:
  # Python imports
  imports:
    - mysqldbda
    - mysqldbda.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/mysqldbda
  license: Zope Public License
  summary: 'MySQL Database adapter'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
