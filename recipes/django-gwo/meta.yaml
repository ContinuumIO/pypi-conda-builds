package:
  name: django-gwo
  version: "0.4"

source:
  fn: django-gwo-0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-gwo/django-gwo-0.4.tar.gz
  md5: 1bd29f340a1de5107e79abba9731ebf6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-gwo = django-gwo:main
    #
    # Would create an entry point called django-gwo that calls django-gwo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - gdata ==2.0.13

  run:
    - python
    - gdata ==2.0.13

test:
  # Python imports
  imports:
    - example
    - example.blog
    - gwo
    - gwo.management
    - gwo.management.commands
    - gwo.templatetags
    - gwo.websiteoptimizer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/washingtontimes/django-gwo
  license: Apache Software License
  summary: 'Django driven Google Website Optimizer testing'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
