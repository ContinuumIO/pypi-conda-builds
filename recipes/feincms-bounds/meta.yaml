package:
  name: feincms-bounds
  version: "0.1.0"

source:
  fn: feincms-bounds-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/f/feincms-bounds/feincms-bounds-0.1.0.tar.gz
  md5: faafc90152e6b53eac44dc3972787a93
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - feincms-bounds = feincms-bounds:main
    #
    # Would create an entry point called feincms-bounds that calls feincms-bounds.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - feincms

  run:
    - python
    - feincms

test:
  # Python imports
  imports:
    - feincms_bounds

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/marcofucci/feincms-bounds
  license: BSD License
  summary: 'FeinCMS add-on which adds extra admin Page validation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
