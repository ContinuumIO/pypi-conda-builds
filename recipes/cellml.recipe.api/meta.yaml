package:
  name: cellml.recipe.api
  version: "0.4"

source:
  fn: cellml.recipe.api-0.4.tar.gz
  url: https://pypi.python.org/packages/source/c/cellml.recipe.api/cellml.recipe.api-0.4.tar.gz
  md5: 47495a34c190939b5ac405bf655519fb
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cellml.recipe.api = cellml.recipe.api:main
    #
    # Would create an entry point called cellml.recipe.api that calls cellml.recipe.api.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - zc.recipe.cmmi

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - zc.recipe.cmmi

test:
  # Python imports
  imports:
    - cellml
    - cellml.recipe
    - cellml.recipe.api
    - cellml.recipe.api.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.cellml.org/tools/api/
  license: GNU General Public License (GPL) or GNU Library or Lesser General Public License (LGPL) or Mozilla Public License 1.1 (MPL 1.1)
  summary: 'Recipe to call CMake to build CellML API and Python bindings'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
