package:
  name: django-multimedia
  version: "0.2.0"

source:
  fn: django-multimedia-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-multimedia/django-multimedia-0.2.0.tar.gz
  md5: fcb6f9f8fbe5d9fadcab5a435dcdb90a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-multimedia = django-multimedia:main
    #
    # Would create an entry point called django-multimedia that calls django-multimedia.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - celery >=3.1
    - django-storages

  run:
    - python
    - celery >=3.1
    - django-storages

test:
  # Python imports
  imports:
    - multimedia
    - multimedia.south_migrations
    - multimedia.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jbittel/django-multimedia
  license: BSD License
  summary: 'Encode and upload multimedia from the Django admin interface'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
