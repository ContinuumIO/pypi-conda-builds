package:
  name: iboto
  version: "0.20.2"

source:
  fn: iboto-0.20.2.tar.gz
  url: https://pypi.python.org/packages/source/i/iboto/iboto-0.20.2.tar.gz
  md5: a415d6861e8d2a4f24ed69a4dc650684
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - iboto = iboto:main
    #
    # Would create an entry point called iboto that calls iboto.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - boto >=2.1
    - ipython >=0.11

  run:
    - python
    - boto >=2.1
    - ipython >=0.11

test:
  # Python imports
  imports:
    - iboto

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://loads.pickle.me.uk/iboto/
  license: MIT License
  summary: 'Amazon EC2 shell for managing multiple accounts and regions easily'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
