package:
  name: django_facebook_helper
  version: "0.3.7"

source:
  fn: django_facebook_helper-0.3.7.tar.gz
  url: https://pypi.python.org/packages/source/d/django_facebook_helper/django_facebook_helper-0.3.7.tar.gz
  md5: 5330a1ecb8fd19cd41fa238f41e28742
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_facebook_helper = django_facebook_helper:main
    #
    # Would create an entry point called django_facebook_helper that calls django_facebook_helper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_facebook_helper
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://packages.python.org/django_facebook_helper
  license: BSD License
  summary: 'A Django application for helping in facebook login an managing.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
