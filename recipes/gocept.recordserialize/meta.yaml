package:
  name: gocept.recordserialize
  version: "0.2"

source:
  fn: gocept.recordserialize-0.2.tar.gz
  url: https://pypi.python.org/packages/source/g/gocept.recordserialize/gocept.recordserialize-0.2.tar.gz
  md5: 5b55447e4329e3ed289d8c983d4d0793
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gocept.recordserialize = gocept.recordserialize:main
    #
    # Would create an entry point called gocept.recordserialize that calls gocept.recordserialize.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - gocept
    - gocept.recordserialize
    - gocept.recordserialize.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://projects.gocept.com/projects/gocept-recordserialize/
  license: Zope Public License
  summary: 'File writer/parser for fixed-width and character-separated files'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
