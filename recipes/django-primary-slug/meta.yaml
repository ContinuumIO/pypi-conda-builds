package:
  name: django-primary-slug
  version: "0.1.1b1"

source:
  fn: django-primary-slug-0.1.1b1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-primary-slug/django-primary-slug-0.1.1b1.tar.gz
  md5: 893c8df03cc1724b31e4d354144cbce1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-primary-slug = django-primary-slug:main
    #
    # Would create an entry point called django-primary-slug that calls django-primary-slug.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - primary_slug
    - primary_slug.translit

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.codetrax.org/projects/django-primary-slug
  license: Apache Software License
  summary: 'django-primary-slug provides a custom SlugField for Django projects.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
