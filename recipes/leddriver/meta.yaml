package:
  name: leddriver
  version: "1.0"

source:
  fn: ledDriver-1.0.tar.gz
  url: https://pypi.python.org/packages/source/l/ledDriver/ledDriver-1.0.tar.gz
  md5: 23d9d0f88095878ccdc4750e1d7a3f3f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - leddriver = leddriver:main
    #
    # Would create an entry point called leddriver that calls leddriver.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - leddriver

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/apexskier/ledDriver
  license: UNKNOWN
  summary: 'Python LED light strip and PWM drivers using the WiringPi2 library.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
