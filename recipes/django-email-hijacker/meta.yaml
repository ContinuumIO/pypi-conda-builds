package:
  name: django-email-hijacker
  version: "0.3.2"

source:
  fn: django-email-hijacker-0.3.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-email-hijacker/django-email-hijacker-0.3.2.tar.gz
  md5: 89c0108a9de75078e300e5b9c7f9dcbe
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-email-hijacker = django-email-hijacker:main
    #
    # Would create an entry point called django-email-hijacker that calls django-email-hijacker.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5
    - django-pods >=1.0

  run:
    - python
    - django >=1.5
    - django-pods >=1.0

test:
  # Python imports
  imports:
    - email_hijacker
    - email_hijacker.backends

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/OohlaLabs/django-email-hijacker
  license: MIT
  summary: 'Email Hijacker for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
