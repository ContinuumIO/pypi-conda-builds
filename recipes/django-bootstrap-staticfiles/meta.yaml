package:
  name: django-bootstrap-staticfiles
  version: "3.0.0.1"

source:
  fn: django-bootstrap-staticfiles-3.0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-bootstrap-staticfiles/django-bootstrap-staticfiles-3.0.0.1.tar.gz
  md5: 518a0cdab5e2b817e4d6a40e61bc8d34
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-bootstrap-staticfiles = django-bootstrap-staticfiles:main
    #
    # Would create an entry point called django-bootstrap-staticfiles that calls django-bootstrap-staticfiles.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_bootstrap_staticfiles

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/55minutes/django-bootstrap-staticfiles
  license: Apache Software License
  summary: 'Django app that provides compiled Bootstrap assets'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
