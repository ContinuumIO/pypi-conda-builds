package:
  name: collective.topicitemsevent
  version: "1.0"

source:
  fn: collective.topicitemsevent-1.0.zip
  url: https://pypi.python.org/packages/source/c/collective.topicitemsevent/collective.topicitemsevent-1.0.zip
  md5: e3a6db05697629c07069c73e8830b8b4
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.topicitemsevent = collective.topicitemsevent:main
    #
    # Would create an entry point called collective.topicitemsevent that calls collective.topicitemsevent.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.topicitemsevent
    - collective.topicitemsevent.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/collective.topicitemsevent
  license: GPL
  summary: "Facilitate flexible scheduled notifications in Plone: Fire 'Topic Item Events' for items in a Topic"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
