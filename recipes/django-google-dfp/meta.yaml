package:
  name: django-google-dfp
  version: "0.1a2"

source:
  fn: django-google-dfp-0.1a2.zip
  url: https://pypi.python.org/packages/source/d/django-google-dfp/django-google-dfp-0.1a2.zip
  md5: 39cb49820fa36be117b0644d1d68d1d7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-google-dfp = django-google-dfp:main
    #
    # Would create an entry point called django-google-dfp that calls django-google-dfp.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - google_dfp
    - google_dfp.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/nextscreenlabs/django-google-dfp
  license: BSD License
  summary: 'Template tags for Google DFP.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
