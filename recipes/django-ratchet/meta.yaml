package:
  name: django-ratchet
  version: "0.3.5"

source:
  fn: django-ratchet-0.3.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-ratchet/django-ratchet-0.3.5.tar.gz
  md5: 05fe22efd5d6ebf1472648d810a57358
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-ratchet = django-ratchet:main
    #
    # Would create an entry point called django-ratchet that calls django-ratchet.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4
    - requests

  run:
    - python
    - django >=1.4
    - requests

test:
  # Python imports
  imports:
    - django_ratchet

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/brianr/django_ratchet
  license: MIT License
  summary: 'Ratchet.io plugin for django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
