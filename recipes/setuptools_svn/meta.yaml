package:
  name: setuptools_svn
  version: "1.0"

source:
  fn: setuptools_svn-1.0.zip
  url: https://pypi.python.org/packages/source/s/setuptools_svn/setuptools_svn-1.0.zip
  md5: 390b18ec87be17f4881fbe2f33965866
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - setuptools_svn = setuptools_svn:main
    #
    # Would create an entry point called setuptools_svn that calls setuptools_svn.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - setuptools_svn

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/jaraco/setuptools_svn
  license: UNKNOWN
  summary: 'Subversion support (file finder hook) for Setuptools'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
