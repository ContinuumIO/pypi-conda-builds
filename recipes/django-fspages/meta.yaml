package:
  name: django-fspages
  version: "0.2"

source:
  fn: django-fspages-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-fspages/django-fspages-0.2.tar.gz
  md5: 0f6ee5cc98a3a9d2e112729168d8eeba
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-fspages = django-fspages:main
    #
    # Would create an entry point called django-fspages that calls django-fspages.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - fspages
    - fspages.template
    - fspages.template.loaders

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD
  summary: 'Django application for serving page templates from the file system'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
