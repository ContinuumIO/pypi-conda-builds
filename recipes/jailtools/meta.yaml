package:
  name: jailtools
  version: "0.1"

source:
  fn: jailtools-0.1.tar.gz
  url: https://pypi.python.org/packages/source/j/jailtools/jailtools-0.1.tar.gz
  md5: e97f8645e68b699ecb9145bcc0a1c464
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jailtools = jailtools:main
    #
    # Would create an entry point called jailtools that calls jailtools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - jailtools

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.boddie.org.uk/python/jailtools.html
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Modules for restricted execution of Python programs'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
