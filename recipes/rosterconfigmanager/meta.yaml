package:
  name: rosterconfigmanager
  version: "0.17"

source:
  fn: RosterConfigManager-0.17.tar.gz
  url: https://pypi.python.org/packages/source/R/RosterConfigManager/RosterConfigManager-0.17.tar.gz
  md5: fa3ca374c809f53f0a51f19ca462e8fc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - rosterconfigmanager = rosterconfigmanager:main
    #
    # Would create an entry point called rosterconfigmanager that calls rosterconfigmanager.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - dnspython >=1.6.0
    - ipy >=0.62
    - iscpy >=1.0.2
    - fabric >=1.4.0
    - rostercore >=0.17

  run:
    - python
    - dnspython >=1.6.0
    - ipy >=0.62
    - iscpy >=1.0.2
    - fabric >=1.4.0
    - rostercore >=0.17

test:
  # Python imports
  imports:
    - roster_config_manager
    - rosterconfigmanager

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/roster-dns-management/
  license: BSD License
  summary: 'RosterConfigManager is a Bind9 config importer/exporter for Roster'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
