package:
  name: redturtle.speedupui.pathbar
  version: "0.2.0alpha"

source:
  fn: redturtle.speedupui.pathbar-0.2.0alpha.tar.gz
  url: https://pypi.python.org/packages/source/r/redturtle.speedupui.pathbar/redturtle.speedupui.pathbar-0.2.0alpha.tar.gz
  md5: d8836486c2065ac21b6e36e836139783
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - redturtle.speedupui.pathbar = redturtle.speedupui.pathbar:main
    #
    # Would create an entry point called redturtle.speedupui.pathbar that calls redturtle.speedupui.pathbar.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - redturtle
    - redturtle.speedupui
    - redturtle.speedupui.pathbar
    - redturtle.speedupui.pathbar.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/redturtle.speedupui.pathbar
  license: GPL
  summary: 'A Plone pathbar (breadcrumbs) viewlet implementation with more interactive features'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
