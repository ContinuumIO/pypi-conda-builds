package:
  name: django-inline-ordering
  version: "1.0.2"

source:
  fn: django-inline-ordering-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-inline-ordering/django-inline-ordering-1.0.2.tar.gz
  md5: 9953b783eac8c043b73277c4ec2c89ce
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-inline-ordering = django-inline-ordering:main
    #
    # Would create an entry point called django-inline-ordering that calls django-inline-ordering.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-admin-jqueryui >=1.0.0

  run:
    - python
    - django-admin-jqueryui >=1.0.0

test:
  # Python imports
  imports:
    - inline_ordering

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/centralniak
  license: BSD License
  summary: 'Django app to ease ordering of related data - enable Drag&Drop ordering in admin with just a few LOC'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
