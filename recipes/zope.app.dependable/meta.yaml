package:
  name: zope.app.dependable
  version: "3.5.1"

source:
  fn: zope.app.dependable-3.5.1.zip
  url: https://pypi.python.org/packages/source/z/zope.app.dependable/zope.app.dependable-3.5.1.zip
  md5: 5f180620a880e6ec754e3a34bd110891
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zope.app.dependable = zope.app.dependable:main
    #
    # Would create an entry point called zope.app.dependable that calls zope.app.dependable.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.annotation
    - zope.exceptions
    - zope.i18nmessageid
    - zope.interface
    - zope.lifecycleevent
    - zope.location
    - zope.traversing

  run:
    - python
    - setuptools
    - zope.annotation
    - zope.exceptions
    - zope.i18nmessageid
    - zope.interface
    - zope.lifecycleevent
    - zope.location
    - zope.traversing

test:
  # Python imports
  imports:
    - zope
    - zope.app
    - zope.app.dependable

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/zope.app.dependable
  license: Zope Public License
  summary: 'Simple Dependency API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
