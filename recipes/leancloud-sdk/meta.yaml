package:
  name: leancloud-sdk
  version: "1.0.9"

source:
  fn: leancloud-sdk-1.0.9.tar.gz
  url: https://pypi.python.org/packages/source/l/leancloud-sdk/leancloud-sdk-1.0.9.tar.gz
  md5: c94e5b560f3251f0e3ac025ae29ab139
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - leancloud-sdk = leancloud-sdk:main
    #
    # Would create an entry point called leancloud-sdk that calls leancloud-sdk.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - arrow
    - qiniu
    - requests
    - iso8601

  run:
    - python
    - arrow
    - qiniu
    - requests
    - iso8601

test:
  # Python imports
  imports:
    - leancloud

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://leancloud.cn/
  license: LGPL
  summary: 'LeanCloud Python SDK'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
