package:
  name: django-panacea
  version: "0.1.2"

source:
  fn: django-panacea-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-panacea/django-panacea-0.1.2.tar.gz
  md5: af8d98f6bccf0cfc1b711749cea79a0a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-panacea = django-panacea:main
    #
    # Would create an entry point called django-panacea that calls django-panacea.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - panacea
    - panacea.management
    - panacea.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/klem4/panacea
  license: UNKNOWN
  summary: 'Django caching middleware for NGINX httpredis module. Based on django-cacheops'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
