package:
  name: chatapp
  version: "0.1"

source:
  fn: ChatApp-0.1.tar.gz
  url: https://pypi.python.org/packages/source/C/ChatApp/ChatApp-0.1.tar.gz
  md5: 2fe8dec1f6dc10b9939260648c0fdcc7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - chatapp = chatapp:main
    #
    # Would create an entry point called chatapp that calls chatapp.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - chatapp

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: UNKNOWN
  summary: 'this is simple application for chat and use Gtk for it GUI.\n    this might contain bugs. so be careful.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
