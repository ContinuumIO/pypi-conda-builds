package:
  name: centric8python
  version: "1.0.2"

source:
  fn: Centric8Python-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/C/Centric8Python/Centric8Python-1.0.2.tar.gz
  md5: b17c96c197e9252dc391bfdbc66f1750
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - centric8python = centric8python:main
    #
    # Would create an entry point called centric8python that calls centric8python.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - centric8python
    - centric8python.test
    - centric8python.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/Centric8Python/
  license: LICENSE.txt
  summary: 'Run queries and retrieve data from Centric 8 PLM systems.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
