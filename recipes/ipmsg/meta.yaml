package:
  name: ipmsg
  version: "0.1.0"

source:
  fn: ipmsg-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/i/ipmsg/ipmsg-0.1.0.tar.gz
  md5: 90bc5bfc55ac963f83ce8d3113a8c8df
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ipmsg = ipmsg:main
    #
    # Would create an entry point called ipmsg that calls ipmsg.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - pycrypto
    - rsa

  run:
    - python
    - pycrypto
    - rsa

test:
  # Python imports
  imports:
    - ipmsg
    - ipmsg.crypto
    - ipmsg.message
    - ipmsg.share

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/shaung/ipmsg/
  license: BSD License
  summary: 'IP Messenger for linux'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
