package:
  name: monger
  version: "0.1.1"

source:
  fn: monger-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/m/monger/monger-0.1.1.tar.gz
  md5: 6a45b0f51e783e501b7ec527c558daa9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - monger = monger:main
    #
    # Would create an entry point called monger that calls monger.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - boto >=2.5.2
    - envoy >=0.0.2

  run:
    - python
    - boto >=2.5.2
    - envoy >=0.0.2

test:
  # Python imports
  imports:
    - monger

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/aquaya/monger
  license: MIT License
  summary: 'mongodump to S3'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
