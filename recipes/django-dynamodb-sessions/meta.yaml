package:
  name: django-dynamodb-sessions
  version: "0.6"

source:
  fn: django-dynamodb-sessions-0.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-dynamodb-sessions/django-dynamodb-sessions-0.6.tar.gz
  md5: bfe73856916bfe781e7ee1190aee2aaa
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-dynamodb-sessions = django-dynamodb-sessions:main
    #
    # Would create an entry point called django-dynamodb-sessions that calls django-dynamodb-sessions.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - boto >=2.2.2

  run:
    - python
    - django
    - boto >=2.2.2

test:
  # Python imports
  imports:
    - dynamodb_sessions
    - dynamodb_sessions.backends

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/gtaylor/django-dynamodb-sessions
  license: BSD License
  summary: "A Django session backend using Amazon's DynamoDB"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
