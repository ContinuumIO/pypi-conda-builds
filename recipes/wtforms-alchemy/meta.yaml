package:
  name: wtforms-alchemy
  version: "0.13.1"

source:
  fn: WTForms-Alchemy-0.13.1.tar.gz
  url: https://pypi.python.org/packages/source/W/WTForms-Alchemy/WTForms-Alchemy-0.13.1.tar.gz
  md5: c4486d390ee9f97520d6ceb6ef51c87d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - wtforms-alchemy = wtforms-alchemy:main
    #
    # Would create an entry point called wtforms-alchemy that calls wtforms-alchemy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sqlalchemy >=0.8.0
    - wtforms >=1.0.4
    - wtforms-components >=0.9.2
    - sqlalchemy-utils >=0.30.0
    - six >=1.4.1

  run:
    - python
    - sqlalchemy >=0.8.0
    - wtforms >=1.0.4
    - wtforms-components >=0.9.2
    - sqlalchemy-utils >=0.30.0
    - six >=1.4.1

test:
  # Python imports
  imports:
    - wtforms_alchemy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kvesteri/wtforms-alchemy
  license: BSD License
  summary: 'Generates WTForms forms from SQLAlchemy models.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
