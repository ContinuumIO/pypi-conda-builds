package:
  name: gtarantool
  version: "1.0.10"

source:
  fn: gtarantool-1.0.10.tar.gz
  url: https://pypi.python.org/packages/source/g/gtarantool/gtarantool-1.0.10.tar.gz
  md5: 44bc8b9c2944176c2b464d243aaf4e32
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gtarantool = gtarantool:main
    #
    # Would create an entry point called gtarantool that calls gtarantool.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - tarantool >=0.5.1

  run:
    - python
    - tarantool >=0.5.1

test:
  # Python imports
  imports:
    - gtarantool

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/shveenkov/gtarantool
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Tarantool connection driver for work with gevent framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
