package:
  name: django-biometrics
  version: "1.0"

source:
  fn: django-biometrics-1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-biometrics/django-biometrics-1.0.tar.gz
  md5: 1306368a4c60e3470fe9d6278aaa8d95
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-biometrics = django-biometrics:main
    #
    # Would create an entry point called django-biometrics that calls django-biometrics.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - biometrics
    - biometrics.migrations
    - biometrics.models

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/schinckel/django-biometrics/
  license: BSD License
  summary: 'Associate biometric markers with users'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
