package:
  name: cykooz.djangorecipe
  version: "1.2.1"

source:
  fn: cykooz.djangorecipe-1.2.1.tar.gz
  url: https://pypi.python.org/packages/source/c/cykooz.djangorecipe/cykooz.djangorecipe-1.2.1.tar.gz
  md5: 9a3309b6cdb4b8199049923280dd74e0
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cykooz.djangorecipe = cykooz.djangorecipe:main
    #
    # Would create an entry point called cykooz.djangorecipe that calls cykooz.djangorecipe.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - distribute
    - zc.buildout
    - zc.recipe.egg
    - django

  run:
    - python
    - setuptools
    - distribute
    - zc.buildout
    - zc.recipe.egg
    - django

test:
  # Python imports
  imports:
    - cykooz
    - cykooz.djangorecipe

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/cykooz/cykooz.djangorecipe
  license: BSD License
  summary: 'Buildout recipe for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
