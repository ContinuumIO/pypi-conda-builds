package:
  name: gkola_ws
  version: "0.1.2"

source:
  fn: gkola_ws-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/g/gkola_ws/gkola_ws-0.1.2.tar.gz
  md5: a09864592acd304d78b38315fc3d05bf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gkola_ws = gkola_ws:main
    #
    # Would create an entry point called gkola_ws that calls gkola_ws.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - gevent_websocket
    - gkola

  run:
    - python
    - gevent_websocket
    - gkola

test:
  # Python imports
  imports:
    - gkola_ws

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dantezhu/gkola_ws
  license: BSD
  summary: 'kola with gevent_websocket'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
