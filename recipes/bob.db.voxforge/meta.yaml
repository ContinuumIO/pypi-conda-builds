package:
  name: bob.db.voxforge
  version: "2.0.1"

source:
  fn: bob.db.voxforge-2.0.1.zip
  url: https://pypi.python.org/packages/source/b/bob.db.voxforge/bob.db.voxforge-2.0.1.zip
  md5: 46859b01bf52070af95ac01411a4775a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bob.db.voxforge = bob.db.voxforge:main
    #
    # Would create an entry point called bob.db.voxforge that calls bob.db.voxforge.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - bob.db.verification.filelist

  run:
    - python
    - setuptools
    - setuptools
    - bob.db.verification.filelist

test:
  # Python imports
  imports:
    - bob
    - bob.db
    - bob.db.voxforge

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/bob.db.voxforge
  license: GNU General Public License v3 (GPLv3)
  summary: 'Speaker verification protocol on a subset of the VoxForge database'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
