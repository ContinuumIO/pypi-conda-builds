package:
  name: toronado
  version: "0.0.6"

source:
  fn: toronado-0.0.6.tar.gz
  url: https://pypi.python.org/packages/source/t/toronado/toronado-0.0.6.tar.gz
  md5: d441631698d43443e6214198aa5c1092
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - toronado = toronado:main
    #
    # Would create an entry point called toronado that calls toronado.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cssselect
    - cssutils
    - lxml

  run:
    - python
    - cssselect
    - cssutils
    - lxml

test:
  # Python imports
  imports:
    - toronado

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Apache License 2.0
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
