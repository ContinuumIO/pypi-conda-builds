package:
  name: moshpit
  version: "2.1"

source:
  fn: MoshPit-2.1.tar.gz
  url: https://pypi.python.org/packages/source/M/MoshPit/MoshPit-2.1.tar.gz
  md5: 19ede1cb1603e8b4138fd95eb5c73f59
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - moshpit = moshpit:main
    #
    # Would create an entry point called moshpit that calls moshpit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sqlalchemy >0.8.0

  run:
    - python
    - sqlalchemy >0.8.0

test:
  # Python imports
  imports:
    - moshpit

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://moshpit.aj7may.com
  license: Creative Commons Attribution-ShareAlike 3.0 Unported License
  summary: 'A wrapper for mosh.  Save hosts and connect to them by nickname.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
