package:
  name: db-psycopg2
  version: "0.0.7"

source:
  fn: db-psycopg2-0.0.7.tar.gz
  url: https://pypi.python.org/packages/source/d/db-psycopg2/db-psycopg2-0.0.7.tar.gz
  md5: 28d0bb5dab8a41c51c9e4cd4b26be1eb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - db-psycopg2 = db-psycopg2:main
    #
    # Would create an entry point called db-psycopg2 that calls db-psycopg2.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - db
    - psycopg2

  run:
    - python
    - db
    - psycopg2

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lgastako/db-psycopg2
  license: UNKNOWN
  summary: 'psycopg2 driver for db'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
