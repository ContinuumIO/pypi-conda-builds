package:
  name: ddbmock
  version: "1.0.3"

source:
  fn: ddbmock-1.0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/ddbmock/ddbmock-1.0.3.tar.gz
  md5: 6d47515701ca5801fad2da98e473e882
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ddbmock = ddbmock:main
    #
    # Would create an entry point called ddbmock that calls ddbmock.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - ddbmock
    - ddbmock.database
    - ddbmock.database.storage
    - ddbmock.operations
    - ddbmock.router
    - ddbmock.utils
    - ddbmock.validators
    - tests
    - tests.functional
    - tests.functional.boto
    - tests.functional.internal
    - tests.functional.pyramid
    - tests.integration
    - tests.unit

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Amazon DynamoDB mock implementation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
