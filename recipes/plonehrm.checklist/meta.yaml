package:
  name: plonehrm.checklist
  version: "1.3"

source:
  fn: plonehrm.checklist-1.3.tar.gz
  url: https://pypi.python.org/packages/source/p/plonehrm.checklist/plonehrm.checklist-1.3.tar.gz
  md5: c2826449442d95017cc68f6b6754e87c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plonehrm.checklist = plonehrm.checklist:main
    #
    # Would create an entry point called plonehrm.checklist that calls plonehrm.checklist.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.autopermission

  run:
    - python
    - setuptools
    - setuptools
    - collective.autopermission

test:
  # Python imports
  imports:
    - plonehrm
    - plonehrm.checklist
    - plonehrm.checklist.browser
    - plonehrm.checklist.content
    - plonehrm.checklist.notifications
    - plonehrm.checklist.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.zestsoftware.nl
  license: GPL
  summary: 'Checklists for Plone HRM'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
