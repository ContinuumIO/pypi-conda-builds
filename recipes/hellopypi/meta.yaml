package:
  name: hellopypi
  version: "1.1"

source:
  fn: hellopypi-1.1.tar.gz
  url: https://pypi.python.org/packages/source/h/hellopypi/hellopypi-1.1.tar.gz
  md5: e8a21372a0466f2421b2dcc910319f31
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hellopypi = hellopypi:main
    #
    # Would create an entry point called hellopypi that calls hellopypi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - hellopypi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'Trivial python package uploaded as test pypi test'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
