package:
  name: arsespyder
  version: "0.0.3"

source:
  fn: arsespyder-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/a/arsespyder/arsespyder-0.0.3.tar.gz
  md5: 7bfbd2e3babd2f727756a60db0c73cf0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - arsespyder = arsespyder:main
    #
    # Would create an entry point called arsespyder that calls arsespyder.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - beautifulsoup4

  run:
    - python
    - beautifulsoup4

test:
  # Python imports
  imports:
    - arsespyder
    - pyarsespyder

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/sarroutbi/MSWL_SUBJECTS/tree/master/DEVELOPMENT_TOOLS/arsespyder
  license: ISC (Internet Systems Consortium)
  summary: 'Web Spider to retrieve links'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
