package:
  name: bulkmailer
  version: "14.2.1"

source:
  fn: bulkmailer-14.2.1.tar.gz
  url: https://pypi.python.org/packages/source/b/bulkmailer/bulkmailer-14.2.1.tar.gz
  md5: 9a4a6edd66fce6839fb4f8b6ed27e857
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bulkmailer = bulkmailer:main
    #
    # Would create an entry point called bulkmailer that calls bulkmailer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests ==1.2.3

  run:
    - python
    - requests ==1.2.3

test:
  # Python imports
  imports:
    - bulkmailer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/pizzapanther/Bulk-Mailer
  license: BSD
  summary: 'API library for accessing GAE Bulk Mailer'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
