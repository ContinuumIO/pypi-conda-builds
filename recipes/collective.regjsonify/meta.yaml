package:
  name: collective.regjsonify
  version: "0.1.0"

source:
  fn: collective.regjsonify-0.1.0.zip
  url: https://pypi.python.org/packages/source/c/collective.regjsonify/collective.regjsonify-0.1.0.zip
  md5: 96ce298b7a4e9c762094606afc169a8f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.regjsonify = collective.regjsonify:main
    #
    # Would create an entry point called collective.regjsonify that calls collective.regjsonify.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.registry
    - zope.schema

  run:
    - python
    - setuptools
    - setuptools
    - plone.registry
    - zope.schema

test:
  # Python imports
  imports:
    - collective
    - collective.regjsonify

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/keul/collective.regjsonify
  license: GNU General Public License (GPL)
  summary: 'Export Plone registry content to a JSON compatible Python structure'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
