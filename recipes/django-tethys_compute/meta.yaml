package:
  name: django-tethys_compute
  version: "0.0.2"

source:
  fn: django-tethys_compute-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-tethys_compute/django-tethys_compute-0.0.2.tar.gz
  md5: 002d8761094566435c23cfcde7cde16f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-tethys_compute = django-tethys_compute:main
    #
    # Would create an entry point called django-tethys_compute that calls django-tethys_compute.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - tethyscluster
    - condorpy

  run:
    - python
    - tethyscluster
    - condorpy

test:
  # Python imports
  imports:
    - tethys_compute
    - tethys_compute/migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/CI-WATER/TethysCluster/wiki
  license: BSD License
  summary: 'A Django app for adding computing resources admin controls to Tethys .'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
