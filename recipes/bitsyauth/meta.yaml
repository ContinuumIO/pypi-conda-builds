package:
  name: bitsyauth
  version: "0.1.2"

source:
  fn: bitsyauth-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/b/bitsyauth/bitsyauth-0.1.2.tar.gz
  md5: a307404448de88dc314c00638c4cd033
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bitsyauth = bitsyauth:main
    #
    # Would create an entry point called bitsyauth that calls bitsyauth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - paste
    - pastescript
    - markup
    - skimpygimpy

  run:
    - python
    - setuptools
    - paste
    - pastescript
    - markup
    - skimpygimpy

test:
  # Python imports
  imports:
    - bitsyauth

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://k0s.org/hg/bitsyauth
  license: GPL
  summary: 'form + digest auth middleware'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
