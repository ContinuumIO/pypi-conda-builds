package:
  name: django-ignoretests
  version: "0.3.2"

source:
  fn: django-ignoretests-0.3.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-ignoretests/django-ignoretests-0.3.2.tar.gz
  md5: 206e7d336f40306a18366ff67cf1714f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-ignoretests = django-ignoretests:main
    #
    # Would create an entry point called django-ignoretests that calls django-ignoretests.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - ignoretests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/cyberj/django-ignoretests
  license: MIT License
  summary: 'Ignore tests of given django apps'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
