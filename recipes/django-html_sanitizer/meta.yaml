package:
  name: django-html_sanitizer
  version: "0.1.4"

source:
  fn: django-html_sanitizer-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-html_sanitizer/django-html_sanitizer-0.1.4.tar.gz
  md5: 660f1162bacdea045e8145b190f4248c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-html_sanitizer = django-html_sanitizer:main
    #
    # Would create an entry point called django-html_sanitizer that calls django-html_sanitizer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django
    - bleach

  run:
    - python
    - django
    - bleach

test:
  # Python imports
  imports:
    - sanitizer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ui/django-html_sanitizer
  license: MIT License
  summary: 'Provides a set of HTML cleaning utilities for django models, forms and templates.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
