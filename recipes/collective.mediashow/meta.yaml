package:
  name: collective.mediashow
  version: "1.36"

source:
  fn: collective.mediaShow-1.36.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.mediaShow/collective.mediaShow-1.36.tar.gz
  md5: 76dd13cc2e588415ad704550a45363e5
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.mediashow = collective.mediashow:main
    #
    # Would create an entry point called collective.mediashow that calls collective.mediashow.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.mediaShow
    - collective.mediaShow.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/intk/collective.mediaShow
  license: GPL
  summary: 'A flexible slideshow that can show any kind of media or Plone content'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
