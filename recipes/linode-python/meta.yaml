package:
  name: linode-python
  version: "1.1.1"

source:
  fn: linode-python-1.1.1.tar.gz
  url: https://pypi.python.org/packages/source/l/linode-python/linode-python-1.1.1.tar.gz
  md5: 701b88df60d5e1bb782cef6336f9ea19
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - linode-python = linode-python:main
    #
    # Would create an entry point called linode-python that calls linode-python.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - linode

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tjfontaine/linode-python
  license: UNKNOWN
  summary: 'Python bindings for Linode API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
