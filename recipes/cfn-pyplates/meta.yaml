package:
  name: cfn-pyplates
  version: "0.5.0"

source:
  fn: cfn-pyplates-0.5.0.tar.gz
  url: https://pypi.python.org/packages/source/c/cfn-pyplates/cfn-pyplates-0.5.0.tar.gz
  md5: 3c45bf8f8a81617ae4429fe5c0dd52ae
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cfn-pyplates = cfn-pyplates:main
    #
    # Would create an entry point called cfn-pyplates that calls cfn-pyplates.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT License
  summary: 'Amazon Web Services CloudFormation template generator'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
