package:
  name: python-baseconv
  version: "1.1.2"

source:
  fn: python-baseconv-1.1.2.tar.gz
  url: https://pypi.python.org/packages/source/p/python-baseconv/python-baseconv-1.1.2.tar.gz
  md5: c9916e89b9eca99051db5ee6f9e24175
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-baseconv = python-baseconv:main
    #
    # Would create an entry point called python-baseconv that calls python-baseconv.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/semente/baseconv/
  license: Python Software Foundation License
  summary: 'Convert numbers from base 10 integers to base X strings and back again.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
