package:
  name: image-mining
  version: "0.1.6"

source:
  fn: image-mining-0.1.6.tar.gz
  url: https://pypi.python.org/packages/source/i/image-mining/image-mining-0.1.6.tar.gz
  md5: fbc1d5346f1daf40d1452c6b4acb0d63
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - image-mining = image-mining:main
    #
    # Would create an entry point called image-mining that calls image-mining.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy

  run:
    - python
    - numpy

test:
  # Python imports
  imports:
    - image_mining

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/acdha/image-mining/
  license: LICENSE.txt
  summary: 'Extract useful information from scanned images using OpenCV'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
