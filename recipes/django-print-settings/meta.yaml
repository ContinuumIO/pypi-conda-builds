package:
  name: django-print-settings
  version: "0.1.2"

source:
  fn: django-print-settings-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-print-settings/django-print-settings-0.1.2.tar.gz
  md5: 7c5f41f30eb73f019a4c13758ee69b84
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-print-settings = django-print-settings:main
    #
    # Would create an entry point called django-print-settings that calls django-print-settings.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_print_settings
    - django_print_settings.management
    - django_print_settings.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/msabramo/django-print-settings
  license: BSD License
  summary: 'print_settings management command for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
