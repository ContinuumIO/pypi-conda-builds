package:
  name: django-model-i18n
  version: "0.5.1"

source:
  fn: django-model-i18n-0.5.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-model-i18n/django-model-i18n-0.5.1.tar.gz
  md5: eff4ec6ab4e21c2fdee8be4573f86e7a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-model-i18n = django-model-i18n:main
    #
    # Would create an entry point called django-model-i18n that calls django-model-i18n.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3

  run:
    - python
    - django >=1.3

test:
  # Python imports
  imports:
    - model_i18n
    - model_i18n.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/juanpex/django-model-i18n
  license: BSD License
  summary: 'django-model-i18n is a django application that tries to make multilingual data in models less painful.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
