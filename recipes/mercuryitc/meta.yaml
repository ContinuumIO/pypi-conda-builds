package:
  name: mercuryitc
  version: "0.1.0"

source:
  fn: mercuryitc-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/m/mercuryitc/mercuryitc-0.1.0.tar.gz
  md5: 41873d695c6bd553af5f7975bc532350
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mercuryitc = mercuryitc:main
    #
    # Would create an entry point called mercuryitc that calls mercuryitc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyserial >=2.6

  run:
    - python
    - pyserial >=2.6

test:
  # Python imports
  imports:
    - mercuryitc

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/crazyfermions/python-mercury_driver
  license: LICENSE.txt
  summary: 'A python based driver to control the Oxford MercuryITC system'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
