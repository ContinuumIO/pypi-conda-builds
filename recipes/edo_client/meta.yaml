package:
  name: edo_client
  version: "0.9.2"

source:
  fn: edo_client-0.9.2.tar.gz
  url: https://pypi.python.org/packages/source/e/edo_client/edo_client-0.9.2.tar.gz
  md5: 7df3b64b8f6449af7e3ad2d5182a5762
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - edo_client = edo_client:main
    #
    # Would create an entry point called edo_client that calls edo_client.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - py-oauth2 >=0.0.10
    - requests ==1.2.0

  run:
    - python
    - py-oauth2 >=0.0.10
    - requests ==1.2.0

test:
  # Python imports
  imports:
    - edo_client
    - edo_client.api

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/everydo/python-sdk
  license: MIT License
  summary: 'SDK for easydo.cn'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
