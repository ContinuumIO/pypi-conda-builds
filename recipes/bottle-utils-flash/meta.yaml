package:
  name: bottle-utils-flash
  version: "0.3.2"

source:
  fn: bottle-utils-flash-0.3.2.tar.gz
  url: https://pypi.python.org/packages/source/b/bottle-utils-flash/bottle-utils-flash-0.3.2.tar.gz
  md5: aa9ba197264b2a3523a73848558902a5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bottle-utils-flash = bottle-utils-flash:main
    #
    # Would create an entry point called bottle-utils-flash that calls bottle-utils-flash.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - bottle >=0.12.7
    - bottle-utils-common >=0.3,<0.4
    - bottle-utils-lazy >=0.3,<0.4

  run:
    - python
    - bottle >=0.12.7
    - bottle-utils-common >=0.3,<0.4
    - bottle-utils-lazy >=0.3,<0.4

test:
  # Python imports
  imports:
    - bottle_utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://outernet-project.github.io/bottle-utils/
  license: GNU General Public License v3 (GPLv3)
  summary: 'Flash message utilities for developing apps with Bottle web framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
