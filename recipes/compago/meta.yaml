package:
  name: compago
  version: "1.4"

source:
  fn: compago-1.4.tar.gz
  url: https://pypi.python.org/packages/source/c/compago/compago-1.4.tar.gz
  md5: 962b308a2c070d6b87ad94c70029c1c0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - compago = compago:main
    #
    # Would create an entry point called compago that calls compago.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - versiontools
    - pyyaml

  run:
    - python
    - versiontools
    - pyyaml

test:
  # Python imports
  imports:
    - compago
    - compago.compago_config
    - compago.compago_logging
    - compago.tests
    - compago.tests.integration
    - compago.tests.unit

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/jmohr/compago
  license: BSD License
  summary: 'A framework for simple command-line option parsing.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
