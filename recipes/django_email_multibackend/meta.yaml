package:
  name: django_email_multibackend
  version: "0.5.0"

source:
  fn: django_email_multibackend-0.5.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django_email_multibackend/django_email_multibackend-0.5.0.tar.gz
  md5: 825494f714f382953d5adf9957a160fb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_email_multibackend = django_email_multibackend:main
    #
    # Would create an entry point called django_email_multibackend that calls django_email_multibackend.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_email_multibackend

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/tbarbugli/django-email-multibackend
  license: GNU General Public License (GPL)
  summary: '\nA django package to handle multiple email backends and distribute the messages with weights\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
