package:
  name: products.silvaviews
  version: "0.16.2"

source:
  fn: Products.SilvaViews-0.16.2.tar.gz
  url: https://pypi.python.org/packages/source/P/Products.SilvaViews/Products.SilvaViews-0.16.2.tar.gz
  md5: 80c45a1f6eb29047fdc59a7a0bb1359a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - products.silvaviews = products.silvaviews:main
    #
    # Would create an entry point called products.silvaviews that calls products.silvaviews.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - Products
    - Products.SilvaViews

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://infrae.com/products/silva
  license: BSD License
  summary: 'Zope 2 views machinery for Silva 2'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
