package:
  name: django-user-tags
  version: "1.1"

source:
  fn: django-user-tags-1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-user-tags/django-user-tags-1.1.tar.gz
  md5: 6bbcde23c4a98dc7f8eecd831786e948
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-user-tags = django-user-tags:main
    #
    # Would create an entry point called django-user-tags that calls django-user-tags.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - user_tags
    - user_tags.migrations
    - user_tags.tests
    - user_tags.tests.test_app

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bitmazk/django-user-tags
  license: The MIT License
  summary: "A reusable Django app that enables users of your site to tag items. Each user's\ntags are separated so that the auto-suggest functionality will never suggest\ntags another user has entered.\n"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
