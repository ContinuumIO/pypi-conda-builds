package:
  name: django-influxdb-metrics
  version: "0.3.2"

source:
  fn: django-influxdb-metrics-0.3.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-influxdb-metrics/django-influxdb-metrics-0.3.2.tar.gz
  md5: 37fef0d23802ca11ccfb211277de7921
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-influxdb-metrics = django-influxdb-metrics:main
    #
    # Would create an entry point called django-influxdb-metrics that calls django-influxdb-metrics.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5
    - influxdb
    - tld
    - python-server-metrics >=0.1.8

  run:
    - python
    - django >=1.5
    - influxdb
    - tld
    - python-server-metrics >=0.1.8

test:
  # Python imports
  imports:
    - influxdb_metrics
    - influxdb_metrics.management
    - influxdb_metrics.management.commands
    - influxdb_metrics.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bitmazk/django-influxdb-metrics
  license: The MIT License
  summary: 'A reusable Django app that sends metrics about your project to InfluxDB\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
