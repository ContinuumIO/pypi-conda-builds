package:
  name: hachoir-core
  version: "1.3.3"

source:
  fn: hachoir-core-1.3.3.tar.gz
  url: https://pypi.python.org/packages/source/h/hachoir-core/hachoir-core-1.3.3.tar.gz
  md5: b80f0257536e2b2cb23f66670c4f6f31
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hachoir-core = hachoir-core:main
    #
    # Would create an entry point called hachoir-core that calls hachoir-core.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - hachoir_core
    - hachoir_core.field
    - hachoir_core.stream

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/haypo/hachoir/wiki/hachoir-core
  license: GNU General Public License (GPL)
  summary: 'Core of Hachoir framework: parse and edit binary files'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
