package:
  name: banners
  version: "0.7dev"

source:
  fn: banners-0.7dev.tar.gz
  url: https://pypi.python.org/packages/source/b/banners/banners-0.7dev.tar.gz
  md5: dd1bc703484749fc0a0a7c92fef7d300
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - banners = banners:main
    #
    # Would create an entry point called banners that calls banners.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - django

  run:
    - python
    - setuptools
    - setuptools
    - django

test:
  # Python imports
  imports:
    - banners
    - banners.migrations
    - banners.templates
    - banners.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://revolucionesweb.com.ar
  license: MIT
  summary: 'A simple banner manager app'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
