package:
  name: django-masquerade
  version: "0.1.9"

source:
  fn: django-masquerade-0.1.9.tar.gz
  url: https://pypi.python.org/packages/source/d/django-masquerade/django-masquerade-0.1.9.tar.gz
  md5: 2ff5f3223ec1b65c380f0553816c4eb7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-masquerade = django-masquerade:main
    #
    # Would create an entry point called django-masquerade that calls django-masquerade.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - masquerade
    - masquerade.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/technivore/django-masquerade
  license: Apache Software License
  summary: 'django-masquerade'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
