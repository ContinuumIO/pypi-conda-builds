package:
  name: bottle-mongoengine
  version: "0.0.1"

source:
  fn: bottle-mongoengine-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/b/bottle-mongoengine/bottle-mongoengine-0.0.1.tar.gz
  md5: 45f69d26015edb3c3ffb5781d92261a7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bottle-mongoengine = bottle-mongoengine:main
    #
    # Would create an entry point called bottle-mongoengine that calls bottle-mongoengine.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.github.com/jlmadurga/bottle-mongoengine
  license: MIT License
  summary: 'MongoEngine integration for Bottle.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
