package:
  name: redturtle.video
  version: "1.1.0"

source:
  fn: redturtle.video-1.1.0.zip
  url: https://pypi.python.org/packages/source/r/redturtle.video/redturtle.video-1.1.0.zip
  md5: 3bc6a6056fc54e5429c50effa0342b95
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - redturtle.video = redturtle.video:main
    #
    # Would create an entry point called redturtle.video that calls redturtle.video.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - hachoir_core
    - hachoir_metadata
    - hachoir_parser
    - collective.flowplayer
    - plone.app.imaging >=1.0b9
    - products.cmfplone

  run:
    - python
    - setuptools
    - setuptools
    - hachoir_core
    - hachoir_metadata
    - hachoir_parser
    - collective.flowplayer
    - plone.app.imaging >=1.0b9
    - products.cmfplone

test:
  # Python imports
  imports:
    - redturtle
    - redturtle.video
    - redturtle.video.browser
    - redturtle.video.content
    - redturtle.video.interfaces
    - redturtle.video.portlets
    - redturtle.video.tests
    - redturtle.video.to_wildcardmedia

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/redturtle.video
  license: GNU General Public License (GPL)
  summary: 'Video content types for Plone; use collective.flowplayer but also pluggable with external services. - Forward compatibility shim version, for easily migrate to wildcard.media'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
