package:
  name: tinys3
  version: "0.1.11"

source:
  fn: tinys3-0.1.11.tar.gz
  url: https://pypi.python.org/packages/source/t/tinys3/tinys3-0.1.11.tar.gz
  md5: 381019b3c7385c0eb8eeab85b5368807
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tinys3 = tinys3:main
    #
    # Would create an entry point called tinys3 that calls tinys3.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - futures >=2.1.3
    - requests >=1.2.0

  run:
    - python
    - futures >=2.1.3
    - requests >=1.2.0

test:
  # Python imports
  imports:
    - tinys3

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://www.smore.com/labs/tinys3/
  license: MIT
  summary: 'A small library for uploading files to S3,With support of async uploads, worker pools, cache headers etc'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
