package:
  name: speaklater
  version: "1.3"

source:
  fn: speaklater-1.3.tar.gz
  url: https://pypi.python.org/packages/source/s/speaklater/speaklater-1.3.tar.gz
  md5: e8d5dbe36e53d5a35cff227e795e8bbf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - speaklater = speaklater:main
    #
    # Would create an entry point called speaklater that calls speaklater.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/mitsuhiko/speaklater
  license: BSD License
  summary: 'implements a lazy string for python useful for use with gettext'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
