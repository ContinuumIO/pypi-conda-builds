package:
  name: clinical_trials
  version: "1.1"

source:
  fn: clinical_trials-1.1.tar.gz
  url: https://pypi.python.org/packages/source/c/clinical_trials/clinical_trials-1.1.tar.gz
  md5: 53ee51c5b97b5eac9d5d4022fd9346fb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - clinical_trials = clinical_trials:main
    #
    # Would create an entry point called clinical_trials that calls clinical_trials.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - clinical_trials
    - clinical_trials/api
    - clinical_trials/api/xml2dict

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/codeforamerica/Clinical-Trials-Python
  license: BSD
  summary: 'Python wrapper for the Clincal Trials API.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
