package:
  name: products.richdocument
  version: "3.5.1"

source:
  fn: Products.RichDocument-3.5.1.zip
  url: https://pypi.python.org/packages/source/P/Products.RichDocument/Products.RichDocument-3.5.1.zip
  md5: 976e19decb8111838fce5e1662171b70
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - products.richdocument = products.richdocument:main
    #
    # Would create an entry point called products.richdocument that calls products.richdocument.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - products.simpleattachment >=3.4

  run:
    - python
    - setuptools
    - products.simpleattachment >=3.4

test:
  # Python imports
  imports:
    - Products
    - Products.RichDocument
    - Products.RichDocument.content
    - Products.RichDocument.interfaces
    - Products.RichDocument.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/Products.RichDocument
  license: GNU General Public License (GPL)
  summary: 'Document type for Plone which allows users to upload images directly into the document during editing'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
