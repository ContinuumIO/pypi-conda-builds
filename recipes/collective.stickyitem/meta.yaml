package:
  name: collective.stickyitem
  version: "1.0.4"

source:
  fn: collective.stickyitem-1.0.4.zip
  url: https://pypi.python.org/packages/source/c/collective.stickyitem/collective.stickyitem-1.0.4.zip
  md5: ce05054d83cb43cbfd9299191488869d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.stickyitem = collective.stickyitem:main
    #
    # Would create an entry point called collective.stickyitem that calls collective.stickyitem.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - archetypes.schemaextender
    - plone.indexer

  run:
    - python
    - setuptools
    - setuptools
    - archetypes.schemaextender
    - plone.indexer

test:
  # Python imports
  imports:
    - collective
    - collective.stickyitem

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/collective.stickyitem
  license: GPL
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
