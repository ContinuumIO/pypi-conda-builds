package:
  name: django-forumbr
  version: "0.4.0.1"

source:
  fn: django-forumbr-0.4.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-forumbr/django-forumbr-0.4.0.1.tar.gz
  md5: 7247519966914dc33a87c711487e8293
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-forumbr = django-forumbr:main
    #
    # Would create an entry point called django-forumbr that calls django-forumbr.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5
    - pillow
    - south

  run:
    - python
    - django >=1.5
    - pillow
    - south

test:
  # Python imports
  imports:
    - forum
    - forum.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/italomaia/django-forumbr
  license: http://www.apache.org/licenses/LICENSE-2.0
  summary: 'Simple and complete django forum solution'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
