package:
  name: django_featurette
  version: "0.3"

source:
  fn: django_featurette-0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django_featurette/django_featurette-0.3.tar.gz
  md5: c2fd69b6aa7b8e764a8f44d86a9773f5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_featurette = django_featurette:main
    #
    # Would create an entry point called django_featurette that calls django_featurette.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_featurette
    - django_featurette.migrations
    - django_featurette.templatetags
    - django_featurette.tests
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/GermanoGuerrini/django-featurette
  license: MIT License
  summary: 'Features for selected users'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
