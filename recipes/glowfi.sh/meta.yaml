package:
  name: glowfi.sh
  version: "0.2.17"

source:
  fn: glowfi.sh-0.2.17.tar.gz
  url: https://pypi.python.org/packages/source/g/glowfi.sh/glowfi.sh-0.2.17.tar.gz
  md5: 389918cb463ffce1f7070f13fcf26b91
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - glowfi.sh = glowfi.sh:main
    #
    # Would create an entry point called glowfi.sh that calls glowfi.sh.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - glowfish

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://pypi.python.org/pypi/glowfi.sh
  license: UNKNOWN
  summary: 'Machine learning without the PhD. Now with machine guns and rocket launchers.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
