package:
  name: django-portlets
  version: "1.4"

source:
  fn: django-portlets-1.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-portlets/django-portlets-1.4.tar.gz
  md5: fe93409dff9b251b14b85bc85c172623
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-portlets = django-portlets:main
    #
    # Would create an entry point called django-portlets that calls django-portlets.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - portlets
    - portlets.example
    - portlets.migrations
    - portlets.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.iqpp.com
  license: BSD License
  summary: 'Generic portlets for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
