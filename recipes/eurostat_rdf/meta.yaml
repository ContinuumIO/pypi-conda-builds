package:
  name: eurostat_rdf
  version: "0.1"

source:
  fn: eurostat_rdf-0.1.tar.gz
  url: https://pypi.python.org/packages/source/e/eurostat_rdf/eurostat_rdf-0.1.tar.gz
  md5: 28bada3994c5b117c6e3dacee322b5e7
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - eurostat_rdf = eurostat_rdf:main
    #
    # Would create an entry point called eurostat_rdf that calls eurostat_rdf.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - paste
    - setuptools
    - ordf

  run:
    - python
    - setuptools
    - paste
    - setuptools
    - ordf

test:
  # Python imports
  imports:
    - eurostat
    - eurostat_rdf

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/eurostat_rdf
  license: GNU Affero General Public License v3
  summary: 'Eurostat RDF API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
