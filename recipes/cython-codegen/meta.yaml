package:
  name: cython-codegen
  version: "0.2"

source:
  fn: cython-codegen-0.2.tar.gz
  url: https://pypi.python.org/packages/source/c/cython-codegen/cython-codegen-0.2.tar.gz
  md5: a712529a6d2922317bbc1df0a8145182
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cython-codegen = cython-codegen:main
    #
    # Would create an entry point called cython-codegen that calls cython-codegen.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - cycodegenlib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT License
  summary: 'cycodegenlib - an hack to generate cython code from xml output generated by gccxml'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
