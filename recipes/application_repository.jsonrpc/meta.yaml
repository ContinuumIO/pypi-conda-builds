package:
  name: application_repository.jsonrpc
  version: "0.1"

source:
  fn: application_repository.jsonrpc-0.1.tar.gz
  url: https://pypi.python.org/packages/source/a/application_repository.jsonrpc/application_repository.jsonrpc-0.1.tar.gz
  md5: 1df24a69255799e0e7a279b6dbf7e57b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - application_repository.jsonrpc = application_repository.jsonrpc:main
    #
    # Would create an entry point called application_repository.jsonrpc that calls application_repository.jsonrpc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - jsonrpc

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/wheeler-microfluidics/application_repository.jsonrpc
  license: LGPL
  summary: 'A json-rpc package which implements JSON-RPC over HTTP.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
