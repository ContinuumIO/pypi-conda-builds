package:
  name: django-twitter-bootstrap-form
  version: "0.1.0"

source:
  fn: django-twitter-bootstrap-form-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-twitter-bootstrap-form/django-twitter-bootstrap-form-0.1.0.tar.gz
  md5: bb551e7ebe35cfc099b208a4a10587a4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-twitter-bootstrap-form = django-twitter-bootstrap-form:main
    #
    # Would create an entry point called django-twitter-bootstrap-form that calls django-twitter-bootstrap-form.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - twitter_bootstrap_form
    - twitter_bootstrap_form.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/PragmaticMates/django-twitter-bootstrap-form
  license: BSD License
  summary: 'Twitter Bootstrap Form'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
