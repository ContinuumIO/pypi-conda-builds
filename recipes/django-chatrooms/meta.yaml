package:
  name: django-chatrooms
  version: "1.2adev"

source:
  fn: django-chatrooms-1.2adev.tar.gz
  url: https://pypi.python.org/packages/source/d/django-chatrooms/django-chatrooms-1.2adev.tar.gz
  md5: edbf544820a29f9ed5845ff7617fb5fb
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-chatrooms = django-chatrooms:main
    #
    # Would create an entry point called django-chatrooms that calls django-chatrooms.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - gevent
    - django_load >=1.0
    - django_polymorphic >=0.2

  run:
    - python
    - setuptools
    - setuptools
    - gevent
    - django_load >=1.0
    - django_polymorphic >=0.2

test:
  # Python imports
  imports:
    - chatrooms
    - chatrooms.ajax
    - chatrooms.forms
    - chatrooms.management
    - chatrooms.management.commands
    - chatrooms.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/qubird/django-chatrooms
  license: GNU General Public License (GPL)
  summary: 'A django app providing reverse-ajax chat rooms'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
