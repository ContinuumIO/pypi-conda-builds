package:
  name: linkify
  version: "1.4"

source:
  fn: linkify-1.4.tar.gz
  url: https://pypi.python.org/packages/source/l/linkify/linkify-1.4.tar.gz
  md5: 193b33909ed84812fd315f7950382e35
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - linkify = linkify:main
    #
    # Would create an entry point called linkify that calls linkify.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - linkify

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Convert Youtube and Vimeo links to embedded HTML content'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
