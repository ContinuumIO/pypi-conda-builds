package:
  name: django-vkontakte-iframe
  version: "0.4.1"

source:
  fn: django-vkontakte-iframe-0.4.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-vkontakte-iframe/django-vkontakte-iframe-0.4.1.tar.gz
  md5: e1d42dfcb6bfc73ceb02ac8e6d692171
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-vkontakte-iframe = django-vkontakte-iframe:main
    #
    # Would create an entry point called django-vkontakte-iframe that calls django-vkontakte-iframe.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - vk_iframe
    - vk_iframe.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/kmike/django-vkontakte-iframe/
  license: MIT License
  summary: 'Django app for developing vk.com (aka vkontakte.ru) iframe applications'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
