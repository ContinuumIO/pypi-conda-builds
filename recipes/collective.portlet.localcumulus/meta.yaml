package:
  name: collective.portlet.localcumulus
  version: "1.8"

source:
  fn: collective.portlet.localcumulus-1.8.zip
  url: https://pypi.python.org/packages/source/c/collective.portlet.localcumulus/collective.portlet.localcumulus-1.8.zip
  md5: 5ead751c21fbe2e0f76a9d8c1a2d133a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.portlet.localcumulus = collective.portlet.localcumulus:main
    #
    # Would create an entry point called collective.portlet.localcumulus that calls collective.portlet.localcumulus.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - quintagroup.portlet.cumulus
    - plone.memoize
    - setuptools
    - zope.interface
    - zope.component
    - zope.testing

  run:
    - python
    - setuptools
    - quintagroup.portlet.cumulus
    - plone.memoize
    - setuptools
    - zope.interface
    - zope.component
    - zope.testing

test:
  # Python imports
  imports:
    - collective
    - collective.portlet
    - collective.portlet.localcumulus
    - collective.portlet.localcumulus.Extensions
    - collective.portlet.localcumulus.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/collective.portlet.localcumulus
  license: GPL
  summary: 'Project collective.portlet.localcumulus cumulus product by Makina Corpus'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
