package:
  name: memorised
  version: "1.1.0"

source:
  fn: memorised-1.1.0.tar.gz
  url: https://pypi.python.org/packages/source/m/memorised/memorised-1.1.0.tar.gz
  md5: f76e1e13a0c4eb73f3c3114334d08fd8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - memorised = memorised:main
    #
    # Would create an entry point called memorised that calls memorised.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python-memcached

  run:
    - python
    - python-memcached

test:
  # Python imports
  imports:
    - memorised

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/1stvamp/memorised/
  license: BSD
  summary: 'memcache memoization decorators and utils for python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
