package:
  name: netropy
  version: "1.01"

source:
  fn: netropy-1.01.tar.gz
  url: https://pypi.python.org/packages/source/n/netropy/netropy-1.01.tar.gz
  md5: ede3f52160fdc05869b2fc3efb49f02a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - netropy = netropy:main
    #
    # Would create an entry point called netropy that calls netropy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - netropy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/codycollier/netropy
  license: The MIT License (MIT)
  summary: 'Netropy is a python interface to the NIST Randomness Beacon'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
