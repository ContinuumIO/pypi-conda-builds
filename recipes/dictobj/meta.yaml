package:
  name: dictobj
  version: "0.3.1"

source:
  fn: dictobj-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/d/dictobj/dictobj-0.3.1.tar.gz
  md5: 6ef309f350ca87994190549d754d0154
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dictobj = dictobj:main
    #
    # Would create an entry point called dictobj that calls dictobj.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - dictobj

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/grimwm/py-dictobj
  license: Apache Software License
  summary: 'A set of Python dictionary objects where keys can be accessed as instance attributes.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
