package:
  name: django-dual-authentication
  version: "1.0.0"

source:
  fn: django-dual-authentication-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-dual-authentication/django-dual-authentication-1.0.0.tar.gz
  md5: d71727060ddb6a6b54d7e2aeca6422a7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-dual-authentication = django-dual-authentication:main
    #
    # Would create an entry point called django-dual-authentication that calls django-dual-authentication.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django-dual-authentication

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Zeioth/django-dual-authentication
  license: MIT License
  summary: 'Allows authentication with either a username or an email address.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
