package:
  name: django-sanitizer
  version: "0.4.1"

source:
  fn: django-sanitizer-0.4.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-sanitizer/django-sanitizer-0.4.1.tar.gz
  md5: b487f7dc3f6db1ece9d8d33a352624da
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-sanitizer = django-sanitizer:main
    #
    # Would create an entry point called django-sanitizer that calls django-sanitizer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - sanitizer
    - sanitizer.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/caktus/django-sanitizer
  license: UNKNOWN
  summary: 'Django template filter application for sanitizing user submitted HTML'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
