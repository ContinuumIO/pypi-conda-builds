package:
  name: affinegap
  version: "1.1"

source:
  fn: affinegap-1.1.tar.gz
  url: https://pypi.python.org/packages/source/a/affinegap/affinegap-1.1.tar.gz
  md5: cc53b2bdb715853a8ec95a82c6e363d0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - affinegap = affinegap:main
    #
    # Would create an entry point called affinegap that calls affinegap.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy

  run:
    - python
    - numpy

test:
  # Python imports
  imports:
    - affinegap

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/datamade/affinegap
  license: MIT License
  summary: 'A Cython implementation of the affine gap string distance'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
