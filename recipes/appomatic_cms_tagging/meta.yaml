package:
  name: appomatic_cms_tagging
  version: "0.0.30"

source:
  fn: appomatic_cms_tagging-0.0.30.tar.gz
  url: https://pypi.python.org/packages/source/a/appomatic_cms_tagging/appomatic_cms_tagging-0.0.30.tar.gz
  md5: d7fff0e85f040d6e07f7de51376c8552
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - appomatic_cms_tagging = appomatic_cms_tagging:main
    #
    # Would create an entry point called appomatic_cms_tagging that calls appomatic_cms_tagging.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - appomatic_django_cms ==0.0.30
    - appomatic_tagging ==0.0.30
    - appomaticcore ==0.0.30

  run:
    - python
    - appomatic_django_cms ==0.0.30
    - appomatic_tagging ==0.0.30
    - appomaticcore ==0.0.30

test:
  # Python imports
  imports:
    - appomatic_cms_tagging
    - appomatic_cms_tagging.migrations
    - appomatic_cms_tagging.templates
    - appomatic_cms_tagging.templates.cms_tagging

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/redhog/appomatic
  license: GPL
  summary: 'Adds tagging support to django-cms'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
