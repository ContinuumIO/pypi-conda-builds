package:
  name: ftw.testing
  version: "1.8.1"

source:
  fn: ftw.testing-1.8.1.zip
  url: https://pypi.python.org/packages/source/f/ftw.testing/ftw.testing-1.8.1.zip
  md5: 5c95e06c9fd96ef421f70e05bb6c8326
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ftw.testing = ftw.testing:main
    #
    # Would create an entry point called ftw.testing that calls ftw.testing.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.testing
    - plone.mocktestcase
    - plone.testing
    - unittest2
    - zope.component
    - zope.configuration
    - zope.interface
    - zope.publisher
    - path.py

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.testing
    - plone.mocktestcase
    - plone.testing
    - unittest2
    - zope.component
    - zope.configuration
    - zope.interface
    - zope.publisher
    - path.py

test:
  # Python imports
  imports:
    - ftw
    - ftw.testing
    - ftw.testing.tests
    - ftw.testing.tests.Extensions

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/4teamwork/ftw.testing
  license: GPL2
  summary: 'Provides some testing helpers and an advanced MockTestCase.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
