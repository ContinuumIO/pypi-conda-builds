package:
  name: django-background-task
  version: "0.1.8"

source:
  fn: django-background-task-0.1.8.tar.gz
  url: https://pypi.python.org/packages/source/d/django-background-task/django-background-task-0.1.8.tar.gz
  md5: 7f5e4ad5fd26198834c92fcc03e95e6c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-background-task = django-background-task:main
    #
    # Would create an entry point called django-background-task that calls django-background-task.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - background_task
    - background_task.management
    - background_task.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/lilspikey/django-background-task
  license: BSD License
  summary: 'Database backed asynchronous task queue'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
