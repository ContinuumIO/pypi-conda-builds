package:
  name: apy-users
  version: "1.0.0"

source:
  fn: apy-users-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/a/apy-users/apy-users-1.0.0.tar.gz
  md5: a1669c3ecee05f8c3c3067ae80c0fcdb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - apy-users = apy-users:main
    #
    # Would create an entry point called apy-users that calls apy-users.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - stagecoach_apy
    - pymongo

  run:
    - python
    - stagecoach_apy
    - pymongo

test:
  # Python imports
  imports:
    - apy_users

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/stagecoachio/apy-users
  license: UNKNOWN
  summary: 'Provides user management and API authentication for your apy project'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
