package:
  name: vserver
  version: "0.6"

source:
  fn: vserver-0.6.tar.gz
  url: https://pypi.python.org/packages/source/v/vserver/vserver-0.6.tar.gz
  md5: 6a5cff376aed821c24f780fc10de7a42
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - vserver = vserver:main
    #
    # Would create an entry point called vserver that calls vserver.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - vserver

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.profv.de/python-vserver/
  license: MIT License
  summary: 'Python interface to Linux-VServer for managing hosting systems.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
