package:
  name: metrology
  version: "0.10.1"

source:
  fn: metrology-0.10.1.tar.gz
  url: https://pypi.python.org/packages/source/m/metrology/metrology-0.10.1.tar.gz
  md5: 3dd9f21965a73d48dd380deadd871d8c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - metrology = metrology:main
    #
    # Would create an entry point called metrology that calls metrology.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - astrolabe >=0.4.0
    - atomic >=0.7.0

  run:
    - python
    - astrolabe >=0.4.0
    - atomic >=0.7.0

test:
  # Python imports
  imports:
    - metrology
    - metrology.instruments
    - metrology.reporter
    - metrology.stats
    - metrology.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/cyberdelia/metrology
  license: MIT License
  summary: "A library to easily measure what's going on in your python."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
