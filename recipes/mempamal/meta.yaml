package:
  name: mempamal
  version: "0.1.5"

source:
  fn: mempamal-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/m/mempamal/mempamal-0.1.5.tar.gz
  md5: d65110d84665c89938f8f89e8e3550b4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mempamal = mempamal:main
    #
    # Would create an entry point called mempamal that calls mempamal.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy
    - scikit-learn

  run:
    - python
    - numpy
    - scikit-learn

test:
  # Python imports
  imports:
    - mempamal
    - mempamal.datasets
    - mempamal.examples
    - mempamal.scripts

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/BenoitDamota/mempamal
  license: BSD License
  summary: 'MEMPAMAL: Means for EMbarrassingly PArallel MAchine Learning'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
