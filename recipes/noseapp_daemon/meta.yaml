package:
  name: noseapp_daemon
  version: "1.0.6"

source:
  fn: noseapp_daemon-1.0.6.tar.gz
  url: https://pypi.python.org/packages/source/n/noseapp_daemon/noseapp_daemon-1.0.6.tar.gz
  md5: 4ce79d46f35a04a9e8290c7d168b4ff4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - noseapp_daemon = noseapp_daemon:main
    #
    # Would create an entry point called noseapp_daemon that calls noseapp_daemon.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - noseapp
    - psutil ==2.2.1

  run:
    - python
    - noseapp
    - psutil ==2.2.1

test:
  # Python imports
  imports:
    - noseapp_daemon

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/trifonovmixail/noseapp_daemon
  license: GNU LGPL
  summary: 'noseapp extension for linux daemon testing'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
