package:
  name: cmdman
  version: "0.0.3"

source:
  fn: cmdman-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/c/cmdman/cmdman-0.0.3.tar.gz
  md5: 7eb4b3f27b9e03d6cfb31b809ebaa1e4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cmdman = cmdman:main
    #
    # Would create an entry point called cmdman that calls cmdman.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - cmdman

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/fmichea/cmdman
  license: BSD License
  summary: 'Simple command line program wrapper adding debugging arguments.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
