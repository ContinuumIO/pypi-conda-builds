package:
  name: ajenti.plugin.filesystem
  version: "0.16"

source:
  fn: ajenti.plugin.filesystem-0.16.tar.gz
  url: https://pypi.python.org/packages/source/a/ajenti.plugin.filesystem/ajenti.plugin.filesystem-0.16.tar.gz
  md5: 1686a8df6a431bda25fb368f310d30ed
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ajenti.plugin.filesystem = ajenti.plugin.filesystem:main
    #
    # Would create an entry point called ajenti.plugin.filesystem that calls ajenti.plugin.filesystem.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - aj
    - ajenti.plugin.core

  run:
    - python
    - aj
    - ajenti.plugin.core

test:
  # Python imports
  imports:
    - ajenti_plugin_filesystem

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://ajenti.org
  license: UNKNOWN
  summary: 'Filesystem API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
