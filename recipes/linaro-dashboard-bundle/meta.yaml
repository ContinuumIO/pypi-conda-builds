package:
  name: linaro-dashboard-bundle
  version: "1.9"

source:
  fn: linaro-dashboard-bundle-1.9.tar.gz
  url: https://pypi.python.org/packages/source/l/linaro-dashboard-bundle/linaro-dashboard-bundle-1.9.tar.gz
  md5: 7547cba914581c12a20d1153037d5316
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - linaro-dashboard-bundle = linaro-dashboard-bundle:main
    #
    # Would create an entry point called linaro-dashboard-bundle that calls linaro-dashboard-bundle.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - versiontools >=1.3.1
    - json-schema-validator >=2.2
    - simplejson >=2.1

  run:
    - python
    - versiontools >=1.3.1
    - json-schema-validator >=2.2
    - simplejson >=2.1

test:
  # Python imports
  imports:
    - linaro_dashboard_bundle

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://launchpad.net/linaro-python-dashboard-bundle
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Library for manipulating LAVA dashboard bundles'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
