package:
  name: hydrate-spf
  version: "0.2.0"

source:
  fn: hydrate-spf-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/h/hydrate-spf/hydrate-spf-0.2.0.tar.gz
  md5: db0a8cd24ceb273a5d425ccbba4b37e3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hydrate-spf = hydrate-spf:main
    #
    # Would create an entry point called hydrate-spf that calls hydrate-spf.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - docopt >=0.6,<0.7
    - ipaddr >=2.1.11,<2.2
    - pydns >=2.3.6,<2.4
    - pyspf >=2.0.9,<2.1

  run:
    - python
    - docopt >=0.6,<0.7
    - ipaddr >=2.1.11,<2.2
    - pydns >=2.3.6,<2.4
    - pyspf >=2.0.9,<2.1

test:
  # Python imports
  imports:
    - hydrate_spf

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/iFixit/hydrate-spf
  license: zlib
  summary: 'A tool to convert an SPF record with nested lookups into a flat list of IPs.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
