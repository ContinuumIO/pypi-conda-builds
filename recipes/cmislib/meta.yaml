package:
  name: cmislib
  version: "0.5.1"

source:
  fn: cmislib-0.5.1.tar.gz
  url: https://pypi.python.org/packages/source/c/cmislib/cmislib-0.5.1.tar.gz
  md5: bf74e2a41514a503a395f4b6cd4ba6cf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cmislib = cmislib:main
    #
    # Would create an entry point called cmislib that calls cmislib.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - iso8601

  run:
    - python
    - iso8601

test:
  # Python imports
  imports:
    - cmislib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://chemistry.apache.org/
  license: Apache Software License
  summary: 'Apache Chemistry CMIS client library for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
