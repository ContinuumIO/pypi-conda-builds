package:
  name: js.jquery_tooltip
  version: "1.3"

source:
  fn: js.jquery_tooltip-1.3.tar.gz
  url: https://pypi.python.org/packages/source/j/js.jquery_tooltip/js.jquery_tooltip-1.3.tar.gz
  md5: 752c32f184fd8e9034042de20b1b2701
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - js.jquery_tooltip = js.jquery_tooltip:main
    #
    # Would create an entry point called js.jquery_tooltip that calls js.jquery_tooltip.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fanstatic
    - js.jquery
    - setuptools

  run:
    - python
    - setuptools
    - fanstatic
    - js.jquery
    - setuptools

test:
  # Python imports
  imports:
    - js
    - js.jquery_tooltip

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD
  summary: 'fanstatic jQuery tooltip.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
