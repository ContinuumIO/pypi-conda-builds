package:
  name: bu-cascade
  version: "1.97"

source:
  fn: bu_cascade-1.97.tar.gz
  url: https://pypi.python.org/packages/source/b/bu-cascade/bu_cascade-1.97.tar.gz
  md5: 4112b68e84a147c4f903798aaa3a9838
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bu-cascade = bu-cascade:main
    #
    # Would create an entry point called bu-cascade that calls bu-cascade.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - suds

  run:
    - python
    - suds

test:
  # Python imports
  imports:
    - bu_cascade
    - bu_cascade.assets
    - bu_cascade.exceptions
    - bu_cascade.utilities

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/betheluniversity/cascade/
  license: Apache 2.0
  summary: 'Cascade Server web services integration'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
