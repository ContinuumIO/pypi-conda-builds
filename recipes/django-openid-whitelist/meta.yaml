package:
  name: django-openid-whitelist
  version: "0.3"

source:
  fn: django-openid-whitelist-0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-openid-whitelist/django-openid-whitelist-0.3.tar.gz
  md5: 31943ed1925dcf381eb6f6ada13e24d1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-openid-whitelist = django-openid-whitelist:main
    #
    # Would create an entry point called django-openid-whitelist that calls django-openid-whitelist.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - whitelist

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/sorki/django-openid-whitelist
  license: BSD License
  summary: 'OpenID Whitelist application for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
