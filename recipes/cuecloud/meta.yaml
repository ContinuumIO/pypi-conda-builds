package:
  name: cuecloud
  version: "1.0.3"

source:
  fn: cuecloud-1.0.3.tar.gz
  url: https://pypi.python.org/packages/source/c/cuecloud/cuecloud-1.0.3.tar.gz
  md5: b45bd6ce02172a8e62ccd3531d11f65d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cuecloud = cuecloud:main
    #
    # Would create an entry point called cuecloud that calls cuecloud.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=1.0

  run:
    - python
    - requests >=1.0

test:
  # Python imports
  imports:
    - cuecloud

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/cuecloud/cuecloud-py
  license: MIT License
  summary: 'Integration library for CueCloud'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
