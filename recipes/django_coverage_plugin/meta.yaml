package:
  name: django_coverage_plugin
  version: "0.3.3"

source:
  fn: django_coverage_plugin-0.3.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django_coverage_plugin/django_coverage_plugin-0.3.3.tar.gz
  md5: 30e9e3a53cc6a6b36226b74e10d6d966
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_coverage_plugin = django_coverage_plugin:main
    #
    # Would create an entry point called django_coverage_plugin that calls django_coverage_plugin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4
    - coverage >=4.0a4
    - six >=1.4.0

  run:
    - python
    - django >=1.4
    - coverage >=4.0a4
    - six >=1.4.0

test:
  # Python imports
  imports:
    - django_coverage_plugin

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/nedbat/django_coverage_plugin
  license: UNKNOWN
  summary: 'Django template coverage.py plugin'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
