package:
  name: django-coupons
  version: "1.1.0"

source:
  fn: django-coupons-1.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-coupons/django-coupons-1.1.0.tar.gz
  md5: 9a85987a073378aa7d116f67eeb68a1c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-coupons = django-coupons:main
    #
    # Would create an entry point called django-coupons that calls django-coupons.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - coupons
    - coupons.migrations
    - coupons.south_migrations
    - coupons.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/byteweaver/django-coupons
  license: django-courriers
  summary: 'A reuseable Django application for coupon gereration and handling.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
