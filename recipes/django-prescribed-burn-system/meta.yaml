package:
  name: django-prescribed-burn-system
  version: "1.0c2"

source:
  fn: django-prescribed-burn-system-1.0c2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-prescribed-burn-system/django-prescribed-burn-system-1.0c2.tar.gz
  md5: 80f33fb6421d32800d8126adfee469a3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-prescribed-burn-system = django-prescribed-burn-system:main
    #
    # Would create an entry point called django-prescribed-burn-system that calls django-prescribed-burn-system.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django ==1.5
    - dj_database_url
    - python-dateutil
    - python-ldap
    - django-auth-ldap
    - django-redis-cache
    - django-admin-enhancer
    - django-select2
    - django-chosen
    - django-smart-selects
    - django_compressor
    - django-dtpanel-htmltidy
    - django-registration
    - django-crispy-forms
    - django-markdown
    - django-swingers >1.0.3
    - coverage
    - pyflakes
    - pep8
    - pylint
    - mock
    - selenium
    - envoy
    - fuzzywuzzy
    - joblib
    - unicodecsv
    - gunicorn
    - pillow
    - pypdf2

  run:
    - python
    - django ==1.5
    - dj_database_url
    - python-dateutil
    - python-ldap
    - django-auth-ldap
    - django-redis-cache
    - django-admin-enhancer
    - django-select2
    - django-chosen
    - django-smart-selects
    - django_compressor
    - django-dtpanel-htmltidy
    - django-registration
    - django-crispy-forms
    - django-markdown
    - django-swingers >1.0.3
    - coverage
    - pyflakes
    - pep8
    - pylint
    - mock
    - selenium
    - envoy
    - fuzzywuzzy
    - joblib
    - unicodecsv
    - gunicorn
    - pillow
    - pypdf2

test:
  # Python imports
  imports:
    - pbs
    - pbs.document
    - pbs.document.templatetags
    - pbs.document.tests
    - pbs.implementation
    - pbs.plugins
    - pbs.prescription
    - pbs.prescription.templatetags
    - pbs.prescription.tests
    - pbs.registration
    - pbs.report
    - pbs.risk
    - pbs.risk.templatetags
    - pbs.risk.tests
    - pbs.stakeholder
    - pbs.templatetags
    - pbs.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dpaw2/django-prescribed-burn-system/
  license: BSD License
  summary: 'django-prescribed-burn-system (pbs) is a system for managing prescribed burning in Western Australia. It is currently deployed in production within the Department of Parks and Wildlife.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
