package:
  name: globalsub
  version: "1.0.2"

source:
  fn: globalsub-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/g/globalsub/globalsub-1.0.2.tar.gz
  md5: ee08c37aa83a9f5ebf3f0e15a9f631f7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - globalsub = globalsub:main
    #
    # Would create an entry point called globalsub that calls globalsub.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - globalsub

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/globalsub/
  license: BSD
  summary: 'Global substitution functions for Python unit tests'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
