package:
  name: cubicweb-drh
  version: "0.18.1"

source:
  fn: cubicweb-drh-0.18.1.tar.gz
  url: https://pypi.python.org/packages/source/c/cubicweb-drh/cubicweb-drh-0.18.1.tar.gz
  md5: df4ae798d5d39eb58d7fc7f3e61d3254
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cubicweb-drh = cubicweb-drh:main
    #
    # Would create an entry point called cubicweb-drh that calls cubicweb-drh.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cubicweb-person
    - cubicweb-tag
    - cubicweb >=3.10.0
    - cubicweb-event
    - cubicweb-addressbook
    - cubicweb-folder
    - cubicweb-comment
    - cubicweb-basket
    - cubicweb-task
    - cubicweb-email
    - cubicweb-file

  run:
    - python
    - cubicweb-person
    - cubicweb-tag
    - cubicweb >=3.10.0
    - cubicweb-event
    - cubicweb-addressbook
    - cubicweb-folder
    - cubicweb-comment
    - cubicweb-basket
    - cubicweb-task
    - cubicweb-email
    - cubicweb-file

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.cubicweb.org/project/cubicweb-drh
  license: LGPL
  summary: 'recruitment application based on the CubicWeb framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
