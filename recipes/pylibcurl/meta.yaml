package:
  name: pylibcurl
  version: "0.7.12"

source:
  fn: pylibcurl-0.7.12.tar.gz
  url: https://pypi.python.org/packages/source/p/pylibcurl/pylibcurl-0.7.12.tar.gz
  md5: e7876e685962becaa2d9f3a8c4cbf2c7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pylibcurl = pylibcurl:main
    #
    # Would create an entry point called pylibcurl that calls pylibcurl.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - pylibcurl

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/jungle/pylibcurl
  license: MIT License
  summary: 'python ctypes curl library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
