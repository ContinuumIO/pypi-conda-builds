package:
  name: ewsclient
  version: "0.3.1"

source:
  fn: ewsclient-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/e/ewsclient/ewsclient-0.3.1.tar.gz
  md5: 84937e773f6e38055cfc10410e50d2e7
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ewsclient = ewsclient:main
    #
    # Would create an entry point called ewsclient that calls ewsclient.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - suds
    - python-ntlm

  run:
    - python
    - setuptools
    - suds
    - python-ntlm

test:
  # Python imports
  imports:
    - ewsclient

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/dholth/ewsclient
  license: LGPL
  summary: 'Client for Microsoft Exchange Web Services'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
