package:
  name: simple-django-contact
  version: "0.1.7"

source:
  fn: simple-django-contact-0.1.7.tar.gz
  url: https://pypi.python.org/packages/source/s/simple-django-contact/simple-django-contact-0.1.7.tar.gz
  md5: 132ea55ff648967fb26c7435fbe7c68d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - simple-django-contact = simple-django-contact:main
    #
    # Would create an entry point called simple-django-contact that calls simple-django-contact.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - simple-django-contact

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bryanlrobinson.com/
  license: MIT License
  summary: 'A simple Django app to set up a contact form'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
