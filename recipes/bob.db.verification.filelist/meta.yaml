package:
  name: bob.db.verification.filelist
  version: "2.0.1"

source:
  fn: bob.db.verification.filelist-2.0.1.zip
  url: https://pypi.python.org/packages/source/b/bob.db.verification.filelist/bob.db.verification.filelist-2.0.1.zip
  md5: 6338bdfdce53fe990466725ea6a630dd
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bob.db.verification.filelist = bob.db.verification.filelist:main
    #
    # Would create an entry point called bob.db.verification.filelist that calls bob.db.verification.filelist.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - bob.core
    - bob.io.base
    - bob.db.base
    - bob.db.verification.utils

  run:
    - python
    - setuptools
    - setuptools
    - bob.core
    - bob.io.base
    - bob.db.base
    - bob.db.verification.utils

test:
  # Python imports
  imports:
    - bob
    - bob.db
    - bob.db.verification
    - bob.db.verification.filelist

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://pypi.python.org/pypi/bob.db.verification.filelist
  license: GNU General Public License v3 (GPLv3)
  summary: 'Verification File List Database Access API for Bob'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
