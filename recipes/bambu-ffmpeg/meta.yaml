package:
  name: bambu-ffmpeg
  version: "0.11"

source:
  fn: bambu-ffmpeg-0.11.tar.gz
  url: https://pypi.python.org/packages/source/b/bambu-ffmpeg/bambu-ffmpeg-0.11.tar.gz
  md5: 25b071f414b0607b2b05fdc7f1190999
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bambu-ffmpeg = bambu-ffmpeg:main
    #
    # Would create an entry point called bambu-ffmpeg that calls bambu-ffmpeg.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django >=1.4
    - pymediainfo

  run:
    - python
    - django >=1.4
    - pymediainfo

test:
  # Python imports
  imports:
    - bambu.ffmpeg

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/bambu-ffmpeg
  license: UNKNOWN
  summary: 'A shortcut function for converting video and audio files to HTML5-ready formats'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
