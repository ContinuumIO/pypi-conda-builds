package:
  name: products.republisher
  version: "0.2"

source:
  fn: Products.republisher-0.2.tar.gz
  url: https://pypi.python.org/packages/source/P/Products.republisher/Products.republisher-0.2.tar.gz
  md5: fa246a48862fd846b80a438c7443235e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - products.republisher = products.republisher:main
    #
    # Would create an entry point called products.republisher that calls products.republisher.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.registry

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.registry

test:
  # Python imports
  imports:
    - Products
    - Products.republisher

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.v2.nl/plone/Products.republisher
  license: GPL
  summary: 'republishes content from a plone site into social networks'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
