package:
  name: flexiconfig
  version: "1.0"

source:
  fn: flexiconfig-1.0.tar.gz
  url: https://pypi.python.org/packages/source/f/flexiconfig/flexiconfig-1.0.tar.gz
  md5: f6489eb9b6553c6779a0d783b2710f28
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flexiconfig = flexiconfig:main
    #
    # Would create an entry point called flexiconfig that calls flexiconfig.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - flexiconfig

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/justinvanwinkle/flexiconfig
  license: LICENSE
  summary: 'Flexible Python Configuration'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
