package:
  name: openglcontext-full
  version: "3.1.0b1"

source:
  fn: OpenGLContext-full-3.1.0b1.tar.gz
  url: https://pypi.python.org/packages/source/O/OpenGLContext-full/OpenGLContext-full-3.1.0b1.tar.gz
  md5: 5cdd958253b0fb58205f615b454375dd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - openglcontext-full = openglcontext-full:main
    #
    # Would create an entry point called openglcontext-full that calls openglcontext-full.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyopengl >=3.1.0b1
    - pyopengl-accelerate >=3.1.0b1
    - openglcontext >=2.3.0b1
    - pyvrml97 >=2.3.0b1
    - pyvrml97-accelerate >=2.3.0b1
    - simpleparse
    - numpy
    - pydispatcher
    - fonttools
    - ttfquery
    - pillow

  run:
    - python
    - pyopengl >=3.1.0b1
    - pyopengl-accelerate >=3.1.0b1
    - openglcontext >=2.3.0b1
    - pyvrml97 >=2.3.0b1
    - pyvrml97-accelerate >=2.3.0b1
    - simpleparse
    - numpy
    - pydispatcher
    - fonttools
    - ttfquery
    - pillow

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pyopengl.sourceforge.net/context/
  license: BSD
  summary: 'Installs all of OpenGLContext with optional dependencies'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
