package:
  name: django-fbauth-templatetag
  version: "0.2"

source:
  fn: django-fbauth-templatetag-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-fbauth-templatetag/django-fbauth-templatetag-0.2.tar.gz
  md5: aa6f7223aa2764aa341312433bea87f0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-fbauth-templatetag = django-fbauth-templatetag:main
    #
    # Would create an entry point called django-fbauth-templatetag that calls django-fbauth-templatetag.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6

  run:
    - python
    - django >=1.6

test:
  # Python imports
  imports:
    - fbauth
    - fbauth.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/hellhound/django-fbauth-templatetag
  license: BSD License
  summary: 'Django app that comes with a simple template tag to place a Facebook log-in button in your template'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
