package:
  name: cosent.dummypackage
  version: "0.4"

source:
  fn: cosent.dummypackage-0.4.zip
  url: https://pypi.python.org/packages/source/c/cosent.dummypackage/cosent.dummypackage-0.4.zip
  md5: 9eb240d89dc5a84a8d189772f1aedf5b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cosent.dummypackage = cosent.dummypackage:main
    #
    # Would create an entry point called cosent.dummypackage that calls cosent.dummypackage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - cosent
    - cosent.dummypackage

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: UNKNOWN
  summary: 'A dummy package for buildtools testing'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
