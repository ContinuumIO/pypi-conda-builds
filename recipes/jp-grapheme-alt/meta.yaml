package:
  name: jp-grapheme-alt
  version: "0.1.2"

source:
  fn: jp-grapheme-alt-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/j/jp-grapheme-alt/jp-grapheme-alt-0.1.2.tar.gz
  md5: 50a9d6935210dbf0bcdb06d02634b160
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jp-grapheme-alt = jp-grapheme-alt:main
    #
    # Would create an entry point called jp-grapheme-alt that calls jp-grapheme-alt.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cjktools
    - cjktools_data
    - django
    - django-hierarchy
    - south

  run:
    - python
    - cjktools
    - cjktools_data
    - django
    - django-hierarchy
    - south

test:
  # Python imports
  imports:
    - jp_grapheme_alt

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/lars512/jp-grapheme-alt/
  license: BSD
  summary: 'Probabilistic misrecognition models for Japanese kanji.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
