package:
  name: plone.app.tiles
  version: "2.0.0"

source:
  fn: plone.app.tiles-2.0.0.zip
  url: https://pypi.python.org/packages/source/p/plone.app.tiles/plone.app.tiles-2.0.0.zip
  md5: 0f755a6f4dd94119d92cc43a4a830b48
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.app.tiles = plone.app.tiles:main
    #
    # Would create an entry point called plone.app.tiles that calls plone.app.tiles.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.annotation
    - zope.i18nmessageid
    - plone.namedfile
    - plone.memoize
    - plone.registry
    - plone.tiles
    - zope.publisher
    - zope.security
    - zope.component
    - zope.interface
    - plone.app.blocks
    - plone.z3cform
    - plone.autoform
    - z3c.form
    - plone.uuid
    - products.statusmessages
    - zope.traversing
    - zope.event
    - zope.lifecycleevent
    - zope.schema
    - zope2
    - accesscontrol

  run:
    - python
    - setuptools
    - zope.annotation
    - zope.i18nmessageid
    - plone.namedfile
    - plone.memoize
    - plone.registry
    - plone.tiles
    - zope.publisher
    - zope.security
    - zope.component
    - zope.interface
    - plone.app.blocks
    - plone.z3cform
    - plone.autoform
    - z3c.form
    - plone.uuid
    - products.statusmessages
    - zope.traversing
    - zope.event
    - zope.lifecycleevent
    - zope.schema
    - zope2
    - accesscontrol

test:
  # Python imports
  imports:
    - plone
    - plone.app
    - plone.app.tiles
    - plone.app.tiles.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/plone/plone.app.tiles
  license: GNU General Public License v2 (GPLv2)
  summary: 'Plone UI integration for plone.tiles'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
