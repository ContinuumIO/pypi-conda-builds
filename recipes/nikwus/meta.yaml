package:
  name: nikwus
  version: "0.5.4"

source:
  fn: Nikwus-0.5.4.zip
  url: https://pypi.python.org/packages/source/N/Nikwus/Nikwus-0.5.4.zip
  md5: 76a08681e93e7926ba25f3b31754bcce
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - nikwus = nikwus:main
    #
    # Would create an entry point called nikwus that calls nikwus.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cssutils >=1.0
    - pillow

  run:
    - python
    - cssutils >=1.0
    - pillow

test:
  # Python imports
  imports:
    - nikwus

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD License
  summary: 'Automatically sprite images in CSS files'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
