package:
  name: pyrax
  version: "1.9.4"

source:
  fn: pyrax-1.9.4.tar.gz
  url: https://pypi.python.org/packages/source/p/pyrax/pyrax-1.9.4.tar.gz
  md5: aaeafbbd555decf5accea971e91740cf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pyrax = pyrax:main
    #
    # Would create an entry point called pyrax that calls pyrax.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python-novaclient >=2.13.0
    - rackspace-novaclient
    - keyring
    - requests >=2.2.1
    - six >=1.9.0
    - mock

  run:
    - python
    - python-novaclient >=2.13.0
    - rackspace-novaclient
    - keyring
    - requests >=2.2.1
    - six >=1.9.0
    - mock

test:
  # Python imports
  imports:
    - pyrax
    - pyrax/identity

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rackspace/pyrax
  license: Apache Software License
  summary: 'Python language bindings for OpenStack Clouds.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
