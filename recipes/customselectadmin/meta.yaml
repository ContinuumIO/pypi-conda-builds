package:
  name: customselectadmin
  version: "0.6.1"

source:
  fn: CustomSelectAdmin-0.6.1.tar.gz
  url: https://pypi.python.org/packages/source/C/CustomSelectAdmin/CustomSelectAdmin-0.6.1.tar.gz
  md5: f20a738decd8eecda6a716cfe2550bfa
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - customselectadmin = customselectadmin:main
    #
    # Would create an entry point called customselectadmin that calls customselectadmin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - CustomSelectAdmin
    - customselectadmin

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/jetheis/CustomSelectAdmin
  license: GPLv3
  summary: 'Modify custom select fields for tickets in an admin panel within Trac.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
