package:
  name: sqlalchemy_exasol
  version: "0.9.2"

source:
  fn: sqlalchemy_exasol-0.9.2.tar.gz
  url: https://pypi.python.org/packages/source/s/sqlalchemy_exasol/sqlalchemy_exasol-0.9.2.tar.gz
  md5: dbdabe86dc51330112d485bb5eb65dab
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sqlalchemy_exasol = sqlalchemy_exasol:main
    #
    # Would create an entry point called sqlalchemy_exasol that calls sqlalchemy_exasol.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sqlalchemy >=0.8.2,<0.10
    - pyodbc >=3.0.6
    - six >=1.5

  run:
    - python
    - setuptools
    - sqlalchemy >=0.8.2,<0.10
    - pyodbc >=3.0.6
    - six >=1.5

test:
  # Python imports
  imports:
    - sqlalchemy_exasol

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/blue-yonder/sqlalchemy_exasol
  license: BSD License
  summary: 'EXASOL dialect for SQLAlchemy'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
