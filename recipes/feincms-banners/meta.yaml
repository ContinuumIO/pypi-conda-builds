package:
  name: feincms-banners
  version: "1.2.0"

source:
  fn: feincms-banners-1.2.0.tar.gz
  url: https://pypi.python.org/packages/source/f/feincms-banners/feincms-banners-1.2.0.tar.gz
  md5: 35e5af088e2af50d0fbd100eb4171c63
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - feincms-banners = feincms-banners:main
    #
    # Would create an entry point called feincms-banners that calls feincms-banners.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4.2
    - feincms >=1.6

  run:
    - python
    - django >=1.4.2
    - feincms >=1.6

test:
  # Python imports
  imports:
    - feincms_banners
    - feincms_banners.migrations
    - feincms_banners.south_migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/matthiask/feincms-banners/
  license: BSD License
  summary: 'A simple banner system with views and clicks for FeinCMS.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
