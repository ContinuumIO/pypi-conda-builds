package:
  name: flextls
  version: "0.3"

source:
  fn: flextls-0.3.tar.gz
  url: https://pypi.python.org/packages/source/f/flextls/flextls-0.3.tar.gz
  md5: 325e17ae3e603c195b932984412cb045
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flextls = flextls:main
    #
    # Would create an entry point called flextls that calls flextls.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six >=1.4.1

  run:
    - python
    - six >=1.4.1

test:
  # Python imports
  imports:
    - flextls
    - flextls._registry
    - flextls.protocol
    - flextls.protocol.handshake
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/DinoTools/python-flextls
  license: GNU Lesser General Public License v3 or later (LGPLv3+)
  summary: 'Pure Python SSL and TLS library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
