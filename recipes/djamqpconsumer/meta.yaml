package:
  name: djamqpconsumer
  version: "0.7"

source:
  fn: djamqpconsumer-0.7.zip
  url: https://pypi.python.org/packages/source/d/djamqpconsumer/djamqpconsumer-0.7.zip
  md5: 28667a1b902ad75aee334269ff919590
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djamqpconsumer = djamqpconsumer:main
    #
    # Would create an entry point called djamqpconsumer that calls djamqpconsumer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pika

  run:
    - python
    - setuptools
    - pika

test:
  # Python imports
  imports:
    - djamqpconsumer
    - djamqpconsumer.management
    - djamqpconsumer.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/codesyntax/djamqpconsumer
  license: GPL
  summary: 'Blocking custom django command to consume a AMQP queue'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
