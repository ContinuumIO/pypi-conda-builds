package:
  name: chronometer
  version: "1.0"

source:
  fn: chronometer-1.0.zip
  url: https://pypi.python.org/packages/source/c/chronometer/chronometer-1.0.zip
  md5: b89e863450645c935114f683cf1f26c2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - chronometer = chronometer:main
    #
    # Would create an entry point called chronometer that calls chronometer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - monotonic ==0.1

  run:
    - python
    - monotonic ==0.1

test:
  # Python imports
  imports:
    - chronometer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/eisensheng/chronometer
  license: MIT License
  summary: 'Yet another simple time measurement tool for Python with less cruft and more features.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
