package:
  name: zope.app.schema
  version: "3.5.0"

source:
  fn: zope.app.schema-3.5.0.tar.gz
  url: https://pypi.python.org/packages/source/z/zope.app.schema/zope.app.schema-3.5.0.tar.gz
  md5: 92b7c3f4512f3433acc931ecb6ffc936
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zope.app.schema = zope.app.schema:main
    #
    # Would create an entry point called zope.app.schema that calls zope.app.schema.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.component
    - zope.interface
    - zope.schema

  run:
    - python
    - setuptools
    - zope.component
    - zope.interface
    - zope.schema

test:
  # Python imports
  imports:
    - zope
    - zope.app
    - zope.app.schema

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/zope.app.schema
  license: Zope Public License
  summary: 'Component Architecture based Vocabulary Registry'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
