package:
  name: django-enummodel
  version: "0.1.5"

source:
  fn: django-enummodel-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-enummodel/django-enummodel-0.1.5.tar.gz
  md5: 4296e4a165bd61767482b73dc4b5e241
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-enummodel = django-enummodel:main
    #
    # Would create an entry point called django-enummodel that calls django-enummodel.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - enummodel
    - enummodel.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/yorkedork/django-enummodel/tree/master
  license: BSD License
  summary: 'A Django application to bridge the gap between using enumerations and having fully-specified, normalized data models.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
