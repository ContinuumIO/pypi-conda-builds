package:
  name: delfick_error
  version: "1.6.1"

source:
  fn: delfick_error-1.6.1.tar.gz
  url: https://pypi.python.org/packages/source/d/delfick_error/delfick_error-1.6.1.tar.gz
  md5: 7daddf08b73a4cbf8ffd1e97446b9129
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - delfick_error = delfick_error:main
    #
    # Would create an entry point called delfick_error that calls delfick_error.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - total-ordering
    - six

  run:
    - python
    - total-ordering
    - six

test:
  # Python imports
  imports:
    - delfick_error

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/delfick/delfick_error
  license: MIT
  summary: 'Customized Exception class'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
