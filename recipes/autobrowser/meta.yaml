package:
  name: autobrowser
  version: "0.2.0"

source:
  fn: autobrowser-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/a/autobrowser/autobrowser-0.2.0.tar.gz
  md5: ced347c747ee1963f4b34258b9f0a984
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - autobrowser = autobrowser:main
    #
    # Would create an entry point called autobrowser that calls autobrowser.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - tornado
    - selenium

  run:
    - python
    - tornado
    - selenium

test:
  # Python imports
  imports:
    - autobrowser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/sgzsh269/autobrowser
  license: MIT License
  summary: 'Toolset for automated browsing'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
