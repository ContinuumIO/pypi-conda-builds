package:
  name: bob.db.mnist
  version: "2.0.1"

source:
  fn: bob.db.mnist-2.0.1.zip
  url: https://pypi.python.org/packages/source/b/bob.db.mnist/bob.db.mnist-2.0.1.zip
  md5: 89940c2e511316fb12580c1d9b4ada8e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bob.db.mnist = bob.db.mnist:main
    #
    # Would create an entry point called bob.db.mnist that calls bob.db.mnist.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - bob.db.base

  run:
    - python
    - setuptools
    - setuptools
    - bob.db.base

test:
  # Python imports
  imports:
    - bob
    - bob.db
    - bob.db.mnist

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://pypi.python.org/pypi/bob.db.mnist
  license: GNU General Public License v3 (GPLv3)
  summary: 'MNIST Database Access API for Bob'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
