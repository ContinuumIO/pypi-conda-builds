package:
  name: dtflickr
  version: "1.5"

source:
  fn: dtflickr-1.5.tar.bz2
  url: https://pypi.python.org/packages/source/d/dtflickr/dtflickr-1.5.tar.bz2
  md5: 858ddc52e83dd52d2e2eb22ec183ccc6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dtflickr = dtflickr:main
    #
    # Would create an entry point called dtflickr that calls dtflickr.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - dtflickr

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.douglasthrift.net/trac/dtflickr
  license: Apache Software License
  summary: 'Spiffy Flickr API library using JSON'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
