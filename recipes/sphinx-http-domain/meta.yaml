package:
  name: sphinx-http-domain
  version: "0.2"

source:
  fn: sphinx-http-domain-0.2.tar.gz
  url: https://pypi.python.org/packages/source/s/sphinx-http-domain/sphinx-http-domain-0.2.tar.gz
  md5: 84ad0466da96d25b4a7dc4679adb9e62
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sphinx-http-domain = sphinx-http-domain:main
    #
    # Would create an entry point called sphinx-http-domain that calls sphinx-http-domain.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - sphinx_http_domain

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/deceze/Sphinx-HTTP-domain/
  license: BSD License
  summary: 'Sphinx domain to mark up RESTful web services in ReST'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
