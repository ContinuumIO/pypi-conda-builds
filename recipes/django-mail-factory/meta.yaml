package:
  name: django-mail-factory
  version: "0.14"

source:
  fn: django-mail-factory-0.14.zip
  url: https://pypi.python.org/packages/source/d/django-mail-factory/django-mail-factory-0.14.zip
  md5: 61d41ea388ac01071269346e88862923
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-mail-factory = django-mail-factory:main
    #
    # Would create an entry point called django-mail-factory that calls django-mail-factory.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - html2text

  run:
    - python
    - html2text

test:
  # Python imports
  imports:
    - mail_factory
    - mail_factory.contrib
    - mail_factory.contrib.auth
    - mail_factory.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/novapost/django-mail-factory
  license: BSD License
  summary: u'Django Mail Manager'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
