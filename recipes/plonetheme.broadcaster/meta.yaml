package:
  name: plonetheme.broadcaster
  version: "1.0dev-r415"

source:
  fn: plonetheme.broadcaster-1.0dev-r415.tar.gz
  url: https://pypi.python.org/packages/source/p/plonetheme.broadcaster/plonetheme.broadcaster-1.0dev-r415.tar.gz
  md5: b7d2fd16df59157c12d67baf7fec2d40
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plonetheme.broadcaster = plonetheme.broadcaster:main
    #
    # Would create an entry point called plonetheme.broadcaster that calls plonetheme.broadcaster.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - plonetheme
    - plonetheme.broadcaster
    - plonetheme.broadcaster.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.themebin.com
  license: Creative Commons 2.5
  summary: 'An installable theme for Plone 3.0'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
