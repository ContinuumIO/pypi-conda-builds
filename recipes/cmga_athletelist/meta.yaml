package:
  name: cmga_athletelist
  version: "1.0.1"

source:
  fn: CMGA_athletelist-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/C/CMGA_athletelist/CMGA_athletelist-1.0.1.tar.gz
  md5: bc751effd77aae9bf78b285ce02d506d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cmga_athletelist = cmga_athletelist:main
    #
    # Would create an entry point called cmga_athletelist that calls cmga_athletelist.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - cmga_athletelist

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://caiomga.tumblr.com/
  license: UNKNOWN
  summary: 'HeadFirst AthleteList class and functions'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
