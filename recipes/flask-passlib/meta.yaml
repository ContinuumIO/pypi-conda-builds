package:
  name: flask-passlib
  version: "0.1"

source:
  fn: Flask-Passlib-0.1.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Passlib/Flask-Passlib-0.1.tar.gz
  md5: ad4ce45d308ef5a7842d27c373741587
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-passlib = flask-passlib:main
    #
    # Would create an entry point called flask-passlib that calls flask-passlib.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - passlib ==1.6.1
    - werkzeug >=0.6.1

  run:
    - python
    - flask
    - passlib ==1.6.1
    - werkzeug >=0.6.1

test:
  # Python imports
  imports:
    - flask_passlib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/allanlei/flask-passlib
  license: BSD License
  summary: 'Flask extension for passlib'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
