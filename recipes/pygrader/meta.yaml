package:
  name: pygrader
  version: "0.3"

source:
  fn: pygrader-0.3.tar.gz
  url: https://pypi.python.org/packages/source/p/pygrader/pygrader-0.3.tar.gz
  md5: 3a25cd5118f3773d101d701630f388b2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pygrader = pygrader:main
    #
    # Would create an entry point called pygrader that calls pygrader.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - pygrader
    - pygrader.handler
    - pygrader.model
    - pygrader.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://blog.tremily.us/posts/pygrader/
  license: GNU General Public License (GPL)
  summary: "Manage a course's grade database with email-based communication."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
