package:
  name: ella-listingex
  version: "0.2"

source:
  fn: ella-listingex-0.2.tar.gz
  url: https://pypi.python.org/packages/source/e/ella-listingex/ella-listingex-0.2.tar.gz
  md5: 1be363df737435155277d33bf33b5348
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ella-listingex = ella-listingex:main
    #
    # Would create an entry point called ella-listingex that calls ella-listingex.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools >=0.6b1
    - ella >=2,<3

  run:
    - python
    - setuptools >=0.6b1
    - ella >=2,<3

test:
  # Python imports
  imports:
    - ella_listingex

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD License
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
