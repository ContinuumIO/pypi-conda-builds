package:
  name: flask-fixtures
  version: "0.3.1"

source:
  fn: Flask-Fixtures-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Fixtures/Flask-Fixtures-0.3.1.tar.gz
  md5: 31a818a5e66f79dce738a2a03a26b4b2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-fixtures = flask-fixtures:main
    #
    # Would create an entry point called flask-fixtures that calls flask-fixtures.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - flask-sqlalchemy

  run:
    - python
    - flask
    - flask-sqlalchemy

test:
  # Python imports
  imports:
    - flask_fixtures

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/croach/Flask-Fixtures
  license: Apache Software License
  summary: 'A simple library for adding database fixtures for unit tests using nothing but JSON or YAML.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
