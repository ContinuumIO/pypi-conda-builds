package:
  name: code_time_tracker
  version: "0.1"

source:
  fn: code_time_tracker-0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/code_time_tracker/code_time_tracker-0.1.tar.gz
  md5: 7f5bd057a0a4a0c22b2a8809ac620fae
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - code_time_tracker = code_time_tracker:main
    #
    # Would create an entry point called code_time_tracker that calls code_time_tracker.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - code_time_tracker

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/machinalis/code_time_tracker
  license: BSD License
  summary: u'A very simple decorator that tracks running time'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
