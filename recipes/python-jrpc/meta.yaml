package:
  name: python-jrpc
  version: "1.1.3"

source:
  fn: python-jrpc-1.1.3.zip
  url: https://pypi.python.org/packages/source/p/python-jrpc/python-jrpc-1.1.3.zip
  md5: a3e794b0872299fdd57469d3a940fe6e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-jrpc = python-jrpc:main
    #
    # Would create an entry point called python-jrpc that calls python-jrpc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - jrpc

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/alex-sherman/python-jrpc
  license: UNKNOWN
  summary: 'A Python remote procedure call framework that uses JSON RPC v2.0'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
