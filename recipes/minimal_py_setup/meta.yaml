package:
  name: minimal_py_setup
  version: "0.1"

source:
  fn: minimal_py_setup-0.1.tar.gz
  url: https://pypi.python.org/packages/source/m/minimal_py_setup/minimal_py_setup-0.1.tar.gz
  md5: 25691373277b7db7f6d223c371d0bc1a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - minimal_py_setup = minimal_py_setup:main
    #
    # Would create an entry point called minimal_py_setup that calls minimal_py_setup.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - minimal_py_setup

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Apache License
  summary: 'Minimal Python Setup'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
