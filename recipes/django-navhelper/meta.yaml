package:
  name: django-navhelper
  version: "0.1"

source:
  fn: django-navhelper-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-navhelper/django-navhelper-0.1.tar.gz
  md5: 2eb9e128120c47d52b05e00a3eaeaccf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-navhelper = django-navhelper:main
    #
    # Would create an entry point called django-navhelper that calls django-navhelper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - geelweb
    - geelweb.django
    - geelweb.django.navhelper
    - geelweb.django.navhelper.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/geelweb/django-navhelper
  license: MIT
  summary: 'Django template tags designed to help the navigation rendering'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
