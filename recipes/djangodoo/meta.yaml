package:
  name: djangodoo
  version: "0.2.1"

source:
  fn: djangodoo-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/djangodoo/djangodoo-0.2.1.tar.gz
  md5: 298be35b5e2d794dfd347edefea0c675
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangodoo = djangodoo:main
    #
    # Would create an entry point called djangodoo that calls djangodoo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.7.2
    - erppeek >=1.6
    - python-memcached

  run:
    - python
    - django >=1.7.2
    - erppeek >=1.6
    - python-memcached

test:
  # Python imports
  imports:
    - djangodoo

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/fdegrave/djangodoo
  license: MIT License
  summary: 'A Django app to copy models, load and save records from a running Odoo instance'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
