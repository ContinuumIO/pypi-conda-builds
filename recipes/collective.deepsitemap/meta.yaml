package:
  name: collective.deepsitemap
  version: "1.0a"

source:
  fn: collective.deepsitemap-1.0a.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.deepsitemap/collective.deepsitemap-1.0a.tar.gz
  md5: 1e07b1a29bd008390790567d93b5a013
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.deepsitemap = collective.deepsitemap:main
    #
    # Would create an entry point called collective.deepsitemap that calls collective.deepsitemap.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.deepsitemap

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/collective.deepsitemap/
  license: GPL
  summary: 'A sitemap replacement for Plone, whose depth is configurable per request.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
