package:
  name: quixote
  version: "2.8"

source:
  fn: Quixote-2.8.tar.gz
  url: https://pypi.python.org/packages/source/Q/Quixote/Quixote-2.8.tar.gz
  md5: 9d5c8bccd57226caeed67958464d983b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - quixote = quixote:main
    #
    # Would create an entry point called quixote that calls quixote.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - quixote
    - quixote.demo
    - quixote.form
    - quixote.html
    - quixote.ptl
    - quixote.server

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.quixote.ca/
  license: DFSG approved (see LICENSE.txt)
  summary: 'A small and flexible Python Web application framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
