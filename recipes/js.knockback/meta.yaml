package:
  name: js.knockback
  version: "0.16.7"

source:
  fn: js.knockback-0.16.7.zip
  url: https://pypi.python.org/packages/source/j/js.knockback/js.knockback-0.16.7.zip
  md5: b59ea9d52a6bdd3d736adacffbaf9d4e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - js.knockback = js.knockback:main
    #
    # Would create an entry point called js.knockback that calls js.knockback.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fanstatic
    - js.backbone
    - js.knockout
    - js.underscore
    - setuptools

  run:
    - python
    - setuptools
    - fanstatic
    - js.backbone
    - js.knockout
    - js.underscore
    - setuptools

test:
  # Python imports
  imports:
    - js
    - js.knockback

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD
  summary: 'Fanstatic packaging of Knockback.js'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
