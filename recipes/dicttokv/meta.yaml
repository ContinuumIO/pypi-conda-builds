package:
  name: dicttokv
  version: "0.1.5"

source:
  fn: dicttokv-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/d/dicttokv/dicttokv-0.1.5.tar.gz
  md5: b255847d6a48bbc28b8c3cbac0df807d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dicttokv = dicttokv:main
    #
    # Would create an entry point called dicttokv that calls dicttokv.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - dicttokv

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/yuitest/dicttokv
  license: BSD
  summary: '`dicttokv` converts nested dictionary and list object into key-value tuples.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
