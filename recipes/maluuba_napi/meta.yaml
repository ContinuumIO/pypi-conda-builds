package:
  name: maluuba_napi
  version: "0.0.1"

source:
  fn: maluuba_napi-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/m/maluuba_napi/maluuba_napi-0.0.1.tar.gz
  md5: c499ba688739f7fe66853fc916b5db94
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - maluuba_napi = maluuba_napi:main
    #
    # Would create an entry point called maluuba_napi that calls maluuba_napi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - requests >=0.14.1

  run:
    - python
    - requests >=0.14.1

test:
  # Python imports
  imports:
    - maluuba_napi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://developer.maluuba.com
  license: LICENSE
  summary: 'A simple wrapper for consuming the Maluuba NLP API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
