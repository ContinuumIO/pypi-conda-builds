package:
  name: berserker_resolver
  version: "1.0.3"

source:
  fn: berserker_resolver-1.0.3.tar.gz
  url: https://pypi.python.org/packages/source/b/berserker_resolver/berserker_resolver-1.0.3.tar.gz
  md5: 64024dfde0dba527bde930da202f050f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - berserker_resolver = berserker_resolver:main
    #
    # Would create an entry point called berserker_resolver that calls berserker_resolver.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - dnspython

  run:
    - python
    - dnspython

test:
  # Python imports
  imports:
    - berserker_resolver

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/DmitryFillo/berserker_resolver
  license: BSD License
  summary: 'Berserker Resolver is fast mass dns resolver which can bypass loadbalancers'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
