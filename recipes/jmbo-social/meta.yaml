package:
  name: jmbo-social
  version: "0.0.6"

source:
  fn: jmbo-social-0.0.6.tar.gz
  url: https://pypi.python.org/packages/source/j/jmbo-social/jmbo-social-0.0.6.tar.gz
  md5: a5f1b43b4fe096d9b6a00a19ef25cdf4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jmbo-social = jmbo-social:main
    #
    # Would create an entry point called jmbo-social that calls jmbo-social.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-activity-stream ==0.2.1.praekelt
    - django-friends ==0.2.dev1
    - django-notification
    - django-socialregistration ==0.4.1.tokenstore.1
    - facebook-python-sdk
    - jmbo

  run:
    - python
    - django-activity-stream ==0.2.1.praekelt
    - django-friends ==0.2.dev1
    - django-notification
    - django-socialregistration ==0.4.1.tokenstore.1
    - facebook-python-sdk
    - jmbo

test:
  # Python imports
  imports:
    - social
    - social.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/praekelt/jmbo-social
  license: BSD License
  summary: 'Jmbo social app collecting 3rd party social apps.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
