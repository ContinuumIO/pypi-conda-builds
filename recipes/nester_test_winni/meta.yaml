package:
  name: nester_test_winni
  version: "1.0.0"

source:
  fn: nester_test_winni-1.0.0.zip
  url: https://pypi.python.org/packages/source/n/nester_test_winni/nester_test_winni-1.0.0.zip
  md5: 44016ae3fb51a686042176fdc827d017
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - nester_test_winni = nester_test_winni:main
    #
    # Would create an entry point called nester_test_winni that calls nester_test_winni.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - nester_test_winni

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'test of list printer'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
