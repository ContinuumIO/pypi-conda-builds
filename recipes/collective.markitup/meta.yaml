package:
  name: collective.markitup
  version: "0.2"

source:
  fn: collective.markitup-0.2.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.markitup/collective.markitup-0.2.tar.gz
  md5: e16fe5e7776932b23da547b2c4c4b128
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.markitup = collective.markitup:main
    #
    # Would create an entry point called collective.markitup that calls collective.markitup.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.plonefinder
    - collective.quickupload
    - plone.app.jquerytools >=1.2

  run:
    - python
    - setuptools
    - setuptools
    - collective.plonefinder
    - collective.quickupload
    - plone.app.jquerytools >=1.2

test:
  # Python imports
  imports:
    - collective
    - collective.markitup
    - collective.markitup.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://smith-li.com
  license: MIT
  summary: 'collective.markitup integrates MarkItUp with Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
