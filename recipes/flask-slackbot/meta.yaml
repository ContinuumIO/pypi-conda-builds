package:
  name: flask-slackbot
  version: "0.0.9"

source:
  fn: Flask-SlackBot-0.0.9.zip
  url: https://pypi.python.org/packages/source/F/Flask-SlackBot/Flask-SlackBot-0.0.9.zip
  md5: 94354d061d8331bd150e0d0c759b3828
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-slackbot = flask-slackbot:main
    #
    # Would create an entry point called flask-slackbot that calls flask-slackbot.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - slacker
    - six

  run:
    - python
    - flask
    - slacker
    - six

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/python-cn/flask-slackbot
  license: MIT License
  summary: 'Deal with slack outgoing webhook '

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
