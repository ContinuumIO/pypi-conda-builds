package:
  name: bip32utils
  version: "0.1"

source:
  fn: bip32utils-0.1.tar.gz
  url: https://pypi.python.org/packages/source/b/bip32utils/bip32utils-0.1.tar.gz
  md5: b6082fceac3a0fd19cb35835b5777ac9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bip32utils = bip32utils:main
    #
    # Would create an entry point called bip32utils that calls bip32utils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - bip32utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/jmcorgan/bip32utils
  license: MIT
  summary: 'Utilities for generating and using Bitcoin Hierarchical Deterministic wallets (BIP0032).'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
