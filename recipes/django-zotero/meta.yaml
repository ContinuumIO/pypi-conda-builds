package:
  name: django-zotero
  version: "0.4"

source:
  fn: django-zotero-0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-zotero/django-zotero-0.4.tar.gz
  md5: 34eb412e74be6aba97c4522a42609e1f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-zotero = django-zotero:main
    #
    # Would create an entry point called django-zotero that calls django-zotero.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - jsonfield

  run:
    - python
    - jsonfield

test:
  # Python imports
  imports:
    - django_zotero

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/CulturePlex/django-zotero
  license: MIT License
  summary: 'Django tool to tag objects and export them to Zotero'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
