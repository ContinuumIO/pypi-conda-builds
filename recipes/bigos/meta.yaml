package:
  name: bigos
  version: "0.0.3"

source:
  fn: bigos-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/b/bigos/bigos-0.0.3.tar.gz
  md5: 24eb629ce1a2975928ee09b17a21adb6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bigos = bigos:main
    #
    # Would create an entry point called bigos that calls bigos.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - bigos
    - bigos.backend

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/pstiasny/bigos
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'Do stuff on file change'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
