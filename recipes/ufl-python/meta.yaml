package:
  name: ufl-python
  version: "0.1-pre"

source:
  fn: ufl-python-0.1-pre.tar.gz
  url: https://pypi.python.org/packages/source/u/ufl-python/ufl-python-0.1-pre.tar.gz
  md5: 80d5c06a88a854898d4083ad06ffb4d3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ufl-python = ufl-python:main
    #
    # Would create an entry point called ufl-python that calls ufl-python.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - ufl
    - ufl.core
    - ufl.core.graphics
    - ufl.core.structures
    - ufl.io
    - ufl.os
    - ufl.os.xlib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://dave.uni.cx/project/ufl
  license: GNU General Public License (GPL)
  summary: 'A Python implementation of **Universal Foundation Libraries**: languageagnostic, general purpose standard library extensions.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
