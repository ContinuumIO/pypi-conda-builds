package:
  name: contactgrabber
  version: "0.1"

source:
  fn: ContactGrabber-0.1.tar.gz
  url: https://pypi.python.org/packages/source/C/ContactGrabber/ContactGrabber-0.1.tar.gz
  md5: 94564c94712987efc150cc371d5667ac
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - contactgrabber = contactgrabber:main
    #
    # Would create an entry point called contactgrabber that calls contactgrabber.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - contactgrabber

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT License
  summary: 'Grabs contacts from GMail, Yahoo!, Rediff etc.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
