package:
  name: cubicweb-seo
  version: "0.2.1"

source:
  fn: cubicweb-seo-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/c/cubicweb-seo/cubicweb-seo-0.2.1.tar.gz
  md5: 9acf7dfd632770dfc13af1aeff7ec91a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cubicweb-seo = cubicweb-seo:main
    #
    # Would create an entry point called cubicweb-seo that calls cubicweb-seo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cubicweb >=3.15.5

  run:
    - python
    - cubicweb >=3.15.5

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.cubicweb.org/project/cubicweb-seo
  license: LGPL
  summary: 'search engine optimisation with robotstxt and sitemaps'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
