package:
  name: dataone.onedrive
  version: "3.0.0RC2"

source:
  fn: dataone.onedrive-3.0.0RC2.tar.gz
  url: https://pypi.python.org/packages/source/d/dataone.onedrive/dataone.onedrive-3.0.0RC2.tar.gz
  md5: ace4d35e3b59c9411c07352d5a815c99
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dataone.onedrive = dataone.onedrive:main
    #
    # Would create an entry point called dataone.onedrive that calls dataone.onedrive.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - dataone.workspace_client ==0.0.4
    - fusepy
    - rdflib
    - rdfextras

  run:
    - python
    - dataone.workspace_client ==0.0.4
    - fusepy
    - rdflib
    - rdfextras

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://dataone.org
  license: Apache License, Version 2.0
  summary: 'Filesystem access to the DataONE Workspace'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
