package:
  name: jshdate
  version: "0.2.1"

source:
  fn: jshdate-0.2.1.zip
  url: https://pypi.python.org/packages/source/j/jshdate/jshdate-0.2.1.zip
  md5: f75f61f7b673f4ca056eb83c94a3ee18
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jshdate = jshdate:main
    #
    # Would create an entry point called jshdate that calls jshdate.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python-dateutil

  run:
    - python
    - python-dateutil

test:
  # Python imports
  imports:
    - jshdate

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lixxu/jshdate
  license: BSD License
  summary: 'Get date ticks for statistic in JSH'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
