package:
  name: nagaconda
  version: "0.2.1"

source:
  fn: NagAconda-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/N/NagAconda/NagAconda-0.2.1.tar.gz
  md5: abd2fe4d9106f90a624c02a5d21cbe87
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - nagaconda = nagaconda:main
    #
    # Would create an entry point called nagaconda that calls nagaconda.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sphinx

  run:
    - python
    - sphinx

test:
  # Python imports
  imports:
    - NagAconda
    - nagaconda

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/trifthen/NagAconda
  license: BSD License
  summary: 'NagAconda is a Python Nagios wrapper.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
