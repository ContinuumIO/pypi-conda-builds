package:
  name: collective.monkeypatcherpanel
  version: "1.0.4"

source:
  fn: collective.monkeypatcherpanel-1.0.4.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.monkeypatcherpanel/collective.monkeypatcherpanel-1.0.4.tar.gz
  md5: e3632fada458ca61d0d5875ff8c0f20e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.monkeypatcherpanel = collective.monkeypatcherpanel:main
    #
    # Would create an entry point called collective.monkeypatcherpanel that calls collective.monkeypatcherpanel.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.monkeypatcher

  run:
    - python
    - setuptools
    - setuptools
    - collective.monkeypatcher

test:
  # Python imports
  imports:
    - collective
    - collective.monkeypatcherpanel

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/collective.monkeypatcherpanel
  license: GPL
  summary: 'A Zope 2 control panel that shows monkey patches applied by collective.monkeypatcher'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
