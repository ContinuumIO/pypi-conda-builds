package:
  name: flask-simple-alchemy
  version: "0.3.0"

source:
  fn: flask-simple-alchemy-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/f/flask-simple-alchemy/flask-simple-alchemy-0.3.0.tar.gz
  md5: 7ce360c910cf03c57a8780e065a1acd1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-simple-alchemy = flask-simple-alchemy:main
    #
    # Would create an entry point called flask-simple-alchemy that calls flask-simple-alchemy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - flask-sqlalchemy
    - pytest
    - testfixtures

  run:
    - python
    - flask
    - flask-sqlalchemy
    - pytest
    - testfixtures

test:
  # Python imports
  imports:
    - flask_simple_alchemy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jlgoldb2/flask-simple-alchemy
  license: MIT
  summary: "A Simplification of SQLAlchemy's declarative using Flask-SQLAlchemy"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
