package:
  name: consumerism
  version: "0.3.7"

source:
  fn: consumerism-0.3.7.tar.gz
  url: https://pypi.python.org/packages/source/c/consumerism/consumerism-0.3.7.tar.gz
  md5: 9d9bbd502a9a39b60a47cb2afee88f15
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - consumerism = consumerism:main
    #
    # Would create an entry point called consumerism that calls consumerism.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - boto >=2.34.0

  run:
    - python
    - boto >=2.34.0

test:
  # Python imports
  imports:
    - consumerism

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/expert360/consumerism
  license: MIT License
  summary: 'Expert360 Python SQS consumer library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
