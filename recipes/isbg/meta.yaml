package:
  name: isbg
  version: "0.99"

source:
  fn: isbg-0.99.tar.gz
  url: https://pypi.python.org/packages/source/i/isbg/isbg-0.99.tar.gz
  md5: 9bc0166e4cc973afce5bf8111871b59f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - isbg = isbg:main
    #
    # Would create an entry point called isbg that calls isbg.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - isbg

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://redmine.ookook.fr/projects/isbg/wiki
  license: See LICENCE file.
  summary: 'a script that makes it easy to scan an IMAP inbox for spam using SpamAssassin and get your spam moved to another folder.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
