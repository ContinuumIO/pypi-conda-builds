package:
  name: htmlcutstr
  version: "1.2.1"

source:
  fn: htmlcutstr-1.2.1.tar.gz
  url: https://pypi.python.org/packages/source/h/htmlcutstr/htmlcutstr-1.2.1.tar.gz
  md5: b7c79ef59b56bf918ce0a537d313566e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - htmlcutstr = htmlcutstr:main
    #
    # Would create an entry point called htmlcutstr that calls htmlcutstr.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - htmlcutstr

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'Python module which helps to cut HTML strings by keeping HTML tags as they are'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
