package:
  name: codeco
  version: "1.4"

source:
  fn: codeco-1.4.tar.gz
  url: https://pypi.python.org/packages/source/c/codeco/codeco-1.4.tar.gz
  md5: 58e4f6a1ce0d6cf80740edce8397dbc4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - codeco = codeco:main
    #
    # Would create an entry point called codeco that calls codeco.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pygments >=1.6
    - markdown
    - docutils
    - beautifulsoup4
    - html5lib

  run:
    - python
    - pygments >=1.6
    - markdown
    - docutils
    - beautifulsoup4
    - html5lib

test:
  # Python imports
  imports:
    - codeco

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://codeco.readthedocs.org/
  license: Apache Software License
  summary: 'Universal code annotator using Pygments and reStructuredText or Markdown.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
