package:
  name: demographics
  version: "0.0.1"

source:
  fn: demographics-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/demographics/demographics-0.0.1.tar.gz
  md5: 84a3716eccec7e33be34472a463719d0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - demographics = demographics:main
    #
    # Would create an entry point called demographics that calls demographics.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - demographics

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: MIT
  summary: "Estimate user's demographics from their name or email address."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
