package:
  name: j01.select2
  version: "0.6.0"

source:
  fn: j01.select2-0.6.0.zip
  url: https://pypi.python.org/packages/source/j/j01.select2/j01.select2-0.6.0.zip
  md5: 0e7228017f77453db96ea51a6924288d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - j01.select2 = j01.select2:main
    #
    # Would create an entry point called j01.select2 that calls j01.select2.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - z3c.form
    - z3c.jsonrpc
    - zope.component
    - zope.i18n
    - zope.i18nmessageid
    - zope.interface
    - zope.publisher
    - zope.schema
    - zope.traversing

  run:
    - python
    - setuptools
    - z3c.form
    - z3c.jsonrpc
    - zope.component
    - zope.i18n
    - zope.i18nmessageid
    - zope.interface
    - zope.publisher
    - zope.schema
    - zope.traversing

test:
  # Python imports
  imports:
    - j01
    - j01.select2
    - j01.select2.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/j01.select2
  license: Zope Public License
  summary: 'Multi select2 widget based on JQuery and z3c.form for Zope 3'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
