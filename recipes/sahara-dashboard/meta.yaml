package:
  name: sahara-dashboard
  version: "2014.1.3"

source:
  fn: sahara-dashboard-2014.1.3.tar.gz
  url: https://pypi.python.org/packages/source/s/sahara-dashboard/sahara-dashboard-2014.1.3.tar.gz
  md5: f8c322bd100d4588ffd24b5a3f89266a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sahara-dashboard = sahara-dashboard:main
    #
    # Would create an entry point called sahara-dashboard that calls sahara-dashboard.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Apache Software License
  summary: 'Sahara dashboard'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
