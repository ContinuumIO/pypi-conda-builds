package:
  name: redis_structures
  version: "0.0.1a1"

source:
  fn: redis_structures-0.0.1a1.tar.gz
  url: https://pypi.python.org/packages/source/r/redis_structures/redis_structures-0.0.1a1.tar.gz
  md5: 35aee322c552f76493e4401c25b929a7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - redis_structures = redis_structures:main
    #
    # Would create an entry point called redis_structures that calls redis_structures.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - redis

  run:
    - python
    - redis

test:
  # Python imports
  imports:
    - redis_structures
    - redis_structures.vital

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jaredlunde/redis_structures
  license: UNKNOWN
  summary: 'Redis data structures wrapped with Python 3.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
