package:
  name: django-object-log
  version: "0.7.1"

source:
  fn: django-object-log-0.7.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-object-log/django-object-log-0.7.1.tar.gz
  md5: 7a02848d0957ce3c4e5b7a2554a1402f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-object-log = django-object-log:main
    #
    # Would create an entry point called django-object-log that calls django-object-log.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - object_log

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.osuosl.org/projects/django-object-log
  license: MIT License
  summary: 'A method for logging user actions on models'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
