package:
  name: o365
  version: "0.6.3"

source:
  fn: O365-0.6.3.tar.gz
  url: https://pypi.python.org/packages/source/O/O365/O365-0.6.3.tar.gz
  md5: b6634564692d5a7e30a434f16d47e0a4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - o365 = o365:main
    #
    # Would create an entry point called o365 that calls o365.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - O365
    - o365

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Narcolapser/python-o365
  license: Apache Software License
  summary: 'Python library for working with Microsoft Office 365'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
