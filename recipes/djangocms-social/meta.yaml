package:
  name: djangocms-social
  version: "0.4a.1"

source:
  fn: djangocms-social-0.4a.1.tar.gz
  url: https://pypi.python.org/packages/source/d/djangocms-social/djangocms-social-0.4a.1.tar.gz
  md5: 6e1c048a7ab1f368342c75f99d71a998
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangocms-social = djangocms-social:main
    #
    # Would create an entry point called djangocms-social that calls djangocms-social.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-filer
    - django-social-tags

  run:
    - python
    - django-filer
    - django-social-tags

test:
  # Python imports
  imports:
    - djangocms_social
    - djangocms_social.migrations
    - djangocms_social.models

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/pascalmouret/djangocms-social
  license: BSD License
  summary: 'Social plugin for django-cms.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
