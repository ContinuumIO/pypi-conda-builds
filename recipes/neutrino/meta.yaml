package:
  name: neutrino
  version: "0.2"

source:
  fn: neutrino-0.2.tar.gz
  url: https://pypi.python.org/packages/source/n/neutrino/neutrino-0.2.tar.gz
  md5: f18fa49b1f960e582235bbf07e261d47
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - neutrino = neutrino:main
    #
    # Would create an entry point called neutrino that calls neutrino.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - msgpack_python
    - redis
    - jinja2

  run:
    - python
    - msgpack_python
    - redis
    - jinja2

test:
  # Python imports
  imports:
    - neutrino

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tlatorre-uchicago/neutrino
  license: neutron
  summary: 'High Performance Redis Timeseries'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
