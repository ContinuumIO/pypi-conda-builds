package:
  name: django-marionette
  version: "0.0.2"

source:
  fn: django-marionette-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-marionette/django-marionette-0.0.2.tar.gz
  md5: 8ae9ad0527cf1b672bf364b055a48371
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-marionette = django-marionette:main
    #
    # Would create an entry point called django-marionette that calls django-marionette.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6

  run:
    - python
    - django >=1.6

test:
  # Python imports
  imports:
    - marionette

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/funkybob/django-marionette
  license: BSD License
  summary: 'A simple RPC library for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
