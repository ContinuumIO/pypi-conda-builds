package:
  name: cloudpassage
  version: "0.10.0"

source:
  fn: cloudpassage-0.10.0.tar.gz
  url: https://pypi.python.org/packages/source/c/cloudpassage/cloudpassage-0.10.0.tar.gz
  md5: 37280f9a6116c26f576b573badfccb20
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cloudpassage = cloudpassage:main
    #
    # Would create an entry point called cloudpassage that calls cloudpassage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=2.1.0
    - pyopenssl >=0.14
    - ndg-httpsclient >=0.3.3
    - pyasn1 >=0.1.7

  run:
    - python
    - requests >=2.1.0
    - pyopenssl >=0.14
    - ndg-httpsclient >=0.3.3
    - pyasn1 >=0.1.7

test:
  # Python imports
  imports:
    - cloudpassage

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/rgooler/cloudpassage-python/
  license: MIT License
  summary: 'A simple python interface to the CloudPassage API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
