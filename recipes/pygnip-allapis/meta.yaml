package:
  name: pygnip-allapis
  version: "0.5"

source:
  fn: pygnip-allapis-0.5.tar.gz
  url: https://pypi.python.org/packages/source/p/pygnip-allapis/pygnip-allapis-0.5.tar.gz
  md5: e73c8dbc2219f37cb7a4518e3e26736f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pygnip-allapis = pygnip-allapis:main
    #
    # Would create an entry point called pygnip-allapis that calls pygnip-allapis.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - requests >=2.4.1

  run:
    - python
    - requests >=2.4.1

test:
  # Python imports
  imports:
    - pygnip_allapis

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bee-keeper/pygnip-allapis.git
  license: UNKNOWN
  summary: 'A wrapper around the GNIP APIs'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
