package:
  name: django-monkey-team
  version: "0.2.2"

source:
  fn: django-monkey-team-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-monkey-team/django-monkey-team-0.2.2.tar.gz
  md5: cd195ab2969cb061e0c69dfea381681c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-monkey-team = django-monkey-team:main
    #
    # Would create an entry point called django-monkey-team that calls django-monkey-team.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pycrypto
    - django >=1.3

  run:
    - python
    - pycrypto
    - django >=1.3

test:
  # Python imports
  imports:
    - monkey_team

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ionelmc/django-monkey-team
  license: BSD License
  summary: 'Django middleware that displays debug tracebacks on production sites (where you would have `DEBUG = False`) only to developers.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
