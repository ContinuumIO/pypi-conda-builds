package:
  name: maproxy
  version: "0.0.12"

source:
  fn: maproxy-0.0.12.zip
  url: https://pypi.python.org/packages/source/m/maproxy/maproxy-0.0.12.zip
  md5: 61023682dfda83b7bd9ec08d5bf951a2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - maproxy = maproxy:main
    #
    # Would create an entry point called maproxy that calls maproxy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - tornado >=3.2

  run:
    - python
    - tornado >=3.2

test:
  # Python imports
  imports:
    - maproxy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'My first attempt to create a simple and awesome TCP proxy using Tornado'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
