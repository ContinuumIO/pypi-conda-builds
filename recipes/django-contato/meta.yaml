package:
  name: django-contato
  version: "0.3.3"

source:
  fn: django-contato-0.3.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-contato/django-contato-0.3.3.tar.gz
  md5: 8a2ccd222b978e0f1a1230c07d6ab853
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-contato = django-contato:main
    #
    # Would create an entry point called django-contato that calls django-contato.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - contato

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/semente/django-contato/
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Contact application for Django projects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
