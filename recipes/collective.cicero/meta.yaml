package:
  name: collective.cicero
  version: "1.1"

source:
  fn: collective.cicero-1.1.zip
  url: https://pypi.python.org/packages/source/c/collective.cicero/collective.cicero-1.1.zip
  md5: c3ac3017a4229b9b64565bc9e24cd65e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.cicero = collective.cicero:main
    #
    # Would create an entry point called collective.cicero that calls collective.cicero.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - plone.registry
    - plone.app.registry
    - setuptools
    - z3c.form
    - z3c.suds
    - zope.component
    - zope.dottedname
    - zope.interface
    - zope.schema

  run:
    - python
    - plone.registry
    - plone.app.registry
    - setuptools
    - z3c.form
    - z3c.suds
    - zope.component
    - zope.dottedname
    - zope.interface
    - zope.schema

test:
  # Python imports
  imports:
    - collective
    - collective.cicero
    - collective.cicero.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/collective.cicero/trunk
  license: GPL
  summary: "Provides access to Azavea's Cicero API for legistrative district matching and elected official info"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
