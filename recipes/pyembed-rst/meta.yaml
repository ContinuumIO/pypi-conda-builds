package:
  name: pyembed-rst
  version: "1.1.0"

source:
  fn: pyembed-rst-1.1.0.tar.gz
  url: https://pypi.python.org/packages/source/p/pyembed-rst/pyembed-rst-1.1.0.tar.gz
  md5: 56308ca7661162de857b1afda0b70ebb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pyembed-rst = pyembed-rst:main
    #
    # Would create an entry point called pyembed-rst that calls pyembed-rst.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyembed
    - docutils

  run:
    - python
    - pyembed
    - docutils

test:
  # Python imports
  imports:
    - pyembed.rst

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pyembed.github.io
  license: MIT License
  summary: 'Python reStructuredText directive for embedding content using OEmbed'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
