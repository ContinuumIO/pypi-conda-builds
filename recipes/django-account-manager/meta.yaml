package:
  name: django-account-manager
  version: "0.0.3"

source:
  fn: django-account-manager-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-account-manager/django-account-manager-0.0.3.tar.gz
  md5: 2e5e20d8f5e8c17b0b3d21e7bdb604cd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-account-manager = django-account-manager:main
    #
    # Would create an entry point called django-account-manager that calls django-account-manager.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - account_manager

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://forge.2metz.fr/p/django-account-manager/
  license: BSD License
  summary: 'Experimental implementation of Account Management and Session Identification of Mozilla Labs'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
