package:
  name: bootstrap_2to3
  version: "0.1.1"

source:
  fn: bootstrap_2to3-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/b/bootstrap_2to3/bootstrap_2to3-0.1.1.tar.gz
  md5: 2ef8e51164e536c5f3c3e356235c1dde
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bootstrap_2to3 = bootstrap_2to3:main
    #
    # Would create an entry point called bootstrap_2to3 that calls bootstrap_2to3.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - bootstrap_2to3

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jayeshv/bootstrap_2to3
  license: BSD licence
  summary: 'Script to migrate bootstrap 2 templates to bootstrap3.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
