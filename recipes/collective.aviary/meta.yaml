package:
  name: collective.aviary
  version: "1.0b1"

source:
  fn: collective.aviary-1.0b1.zip
  url: https://pypi.python.org/packages/source/c/collective.aviary/collective.aviary-1.0b1.zip
  md5: 4eebf51851ab1f102bb6470eeafda885
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.aviary = collective.aviary:main
    #
    # Would create an entry point called collective.aviary that calls collective.aviary.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - plone.api
    - plone.app.registry
    - plone.directives.form
    - plone.i18n
    - plone.registry
    - products.cmfplone
    - products.genericsetup
    - setuptools
    - zope.component
    - zope.i18nmessageid
    - zope.interface
    - zope.schema

  run:
    - python
    - setuptools
    - plone.api
    - plone.app.registry
    - plone.directives.form
    - plone.i18n
    - plone.registry
    - products.cmfplone
    - products.genericsetup
    - setuptools
    - zope.component
    - zope.i18nmessageid
    - zope.interface
    - zope.schema

test:
  # Python imports
  imports:
    - collective
    - collective.aviary
    - collective.aviary.Extensions
    - collective.aviary.browser
    - collective.aviary.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.aviary
  license: GNU General Public License v2 (GPLv2)
  summary: 'Integrates the Aviary photo editor into Plone.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
