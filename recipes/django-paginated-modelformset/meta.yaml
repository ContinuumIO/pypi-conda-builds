package:
  name: django-paginated-modelformset
  version: "0.0.2"

source:
  fn: django-paginated-modelformset-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-paginated-modelformset/django-paginated-modelformset-0.0.2.tar.gz
  md5: 7bf279c1346822736d7161b2644a98a2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-paginated-modelformset = django-paginated-modelformset:main
    #
    # Would create an entry point called django-paginated-modelformset that calls django-paginated-modelformset.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6

  run:
    - python
    - django >=1.6

test:
  # Python imports
  imports:
    - paginated_modelformset

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/creafz/django-paginated-modelformset
  license: MIT License
  summary: 'An attempt to add pagination to Django Model Formsets.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
