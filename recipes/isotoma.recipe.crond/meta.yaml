package:
  name: isotoma.recipe.crond
  version: "0.0.1"

source:
  fn: isotoma.recipe.crond-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/i/isotoma.recipe.crond/isotoma.recipe.crond-0.0.1.tar.gz
  md5: e04721ad57e33a1e7c393192f624a34e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - isotoma.recipe.crond = isotoma.recipe.crond:main
    #
    # Would create an entry point called isotoma.recipe.crond that calls isotoma.recipe.crond.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout

test:
  # Python imports
  imports:
    - isotoma
    - isotoma.recipe
    - isotoma.recipe.crond

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/isotoma.recipe.crond
  license: Apache Software License
  summary: 'Buildout recipes for /etc/cron.d.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
