package:
  name: django-render-json
  version: "0.2.6"

source:
  fn: django-render-json-0.2.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-render-json/django-render-json-0.2.6.tar.gz
  md5: c07777eaf86453120ecaecd7a7d7d874
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-render-json = django-render-json:main
    #
    # Would create an entry point called django-render-json that calls django-render-json.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django >=1.6

  run:
    - python
    - django >=1.6

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/limijiaoyin/django-render-json
  license: UNKNOWN
  summary: 'utilities to render json as http response for django views'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
