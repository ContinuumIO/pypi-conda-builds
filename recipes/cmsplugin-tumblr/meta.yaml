package:
  name: cmsplugin-tumblr
  version: "1.0"

source:
  fn: cmsplugin-tumblr-1.0.tar.gz
  url: https://pypi.python.org/packages/source/c/cmsplugin-tumblr/cmsplugin-tumblr-1.0.tar.gz
  md5: 2b2304708674f9eeba03bcd5603aafac
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cmsplugin-tumblr = cmsplugin-tumblr:main
    #
    # Would create an entry point called cmsplugin-tumblr that calls cmsplugin-tumblr.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - tumblr_plugin

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/sclaughl/cmsplugin-tumblr
  license: UNKNOWN
  summary: 'A django-cms plugin that displays the last number of posts of a tumblr user'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
