package:
  name: infi.execute
  version: "0.1.2"

source:
  fn: infi.execute-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/i/infi.execute/infi.execute-0.1.2.tar.gz
  md5: 5ee528ea40b4d2eb0d5f58cb7ac2fe13
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - infi.execute = infi.execute:main
    #
    # Would create an entry point called infi.execute that calls infi.execute.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - infi.monotonic_time

  run:
    - python
    - infi.monotonic_time

test:
  # Python imports
  imports:
    - infi
    - infi.execute

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Infinidat/infi.execute
  license: Python Software Foundation License
  summary: 'wrapper library for executing system commands from Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
