package:
  name: jsjquery
  version: "1.3.2"

source:
  fn: jsjQuery-1.3.2.tar.gz
  url: https://pypi.python.org/packages/source/j/jsjquery/jsjQuery-1.3.2.tar.gz
  md5: 5325b447febd2dae9f751557a6d04a39
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jsjquery = jsjquery:main
    #
    # Would create an entry point called jsjquery that calls jsjquery.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - jsjquery

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.jquery.com
  license: GNU General Public License (GPL)
  summary: 'Install and list jQuery as a dependency via PyPI.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
