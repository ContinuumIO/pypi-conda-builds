package:
  name: py-multicast
  version: "1.4"

source:
  fn: py-multicast-1.4.tar.gz
  url: https://pypi.python.org/packages/source/p/py-multicast/py-multicast-1.4.tar.gz
  md5: 73a9e69afa776f9c9cc25399e76cccb6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - py-multicast = py-multicast:main
    #
    # Would create an entry point called py-multicast that calls py-multicast.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - multicast

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://coobs.eu.org/py-multicast/
  license: BSD License
  summary: 'Python Multicast Toolkit for Linux'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
