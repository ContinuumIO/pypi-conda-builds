package:
  name: icsemantic.langfallback
  version: "1.0-dev-r371"

source:
  fn: icsemantic.langfallback-1.0-dev-r371.tar.gz
  url: https://pypi.python.org/packages/source/i/icsemantic.langfallback/icsemantic.langfallback-1.0-dev-r371.tar.gz
  md5: b5f1642997e94daa04ddedc62ba6f906
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - icsemantic.langfallback = icsemantic.langfallback:main
    #
    # Would create an entry point called icsemantic.langfallback that calls icsemantic.langfallback.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - icsemantic.core

  run:
    - python
    - setuptools
    - setuptools
    - icsemantic.core

test:
  # Python imports
  imports:
    - icsemantic
    - icsemantic.langfallback
    - icsemantic.langfallback.Extensions
    - icsemantic.langfallback.browser
    - icsemantic.langfallback.exportimport
    - icsemantic.langfallback.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.platecom.com/productos/icsemantic.langfallback/
  license: GNU General Public License (GPL)
  summary: 'icSemantic.LangFallback'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
