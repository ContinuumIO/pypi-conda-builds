package:
  name: django-pagination-plus
  version: "0.0.3"

source:
  fn: django-pagination-plus-0.0.3.zip
  url: https://pypi.python.org/packages/source/d/django-pagination-plus/django-pagination-plus-0.0.3.zip
  md5: 80aeaa758410f6b21aa14af51ddafe77
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-pagination-plus = django-pagination-plus:main
    #
    # Would create an entry point called django-pagination-plus that calls django-pagination-plus.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - paginationplus
    - paginationplus.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/SteefH/django-pagination-plus
  license: BSD License
  summary: 'Utilities for pagination in Django templates'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
