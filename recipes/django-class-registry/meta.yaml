package:
  name: django-class-registry
  version: "0.0.3"

source:
  fn: django-class-registry-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-class-registry/django-class-registry-0.0.3.tar.gz
  md5: 1d2317c78fec0e0691d48eae89c62a4a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-class-registry = django-class-registry:main
    #
    # Would create an entry point called django-class-registry that calls django-class-registry.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - class_registry

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/imtapps/django-class-registry
  license: BSD License
  summary: "Register any classes like Django's admin."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
