package:
  name: bda.portlet.sitenavigation
  version: "1.0"

source:
  fn: bda.portlet.sitenavigation-1.0.tar.gz
  url: https://pypi.python.org/packages/source/b/bda.portlet.sitenavigation/bda.portlet.sitenavigation-1.0.tar.gz
  md5: 3e680c660d9552be39ddaa93c59d16ea
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bda.portlet.sitenavigation = bda.portlet.sitenavigation:main
    #
    # Would create an entry point called bda.portlet.sitenavigation that calls bda.portlet.sitenavigation.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - products.cmfplone
    - plone.app.portlets

  run:
    - python
    - setuptools
    - products.cmfplone
    - plone.app.portlets

test:
  # Python imports
  imports:
    - bda
    - bda.portlet
    - bda.portlet.sitenavigation

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/bda/bda.portlet.sitenavigation
  license: GPL
  summary: 'Yet Another Navigation Portlet.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
