package:
  name: github_repos_cloner
  version: "0.0.3"

source:
  fn: github_repos_cloner-0.0.3.zip
  url: https://pypi.python.org/packages/source/g/github_repos_cloner/github_repos_cloner-0.0.3.zip
  md5: c2418c4f2dbeba0c5c97e11f73c1c3e3
#  patches:
   # List any patch files here
   # - fix.patch

build:
  # preserve_egg_dir: True
  entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - github_repos_cloner = github_repos_cloner:main
    #
    # Would create an entry point called github_repos_cloner that calls github_repos_cloner.main()

    - github_repos_cloner=github_repos_cloner:clone_repos

  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pbs
    - requests

  run:
    - python
    - pbs
    - requests

test:
  # Python imports
  imports:
    - github_repos_cloner

  commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.

    - github_repos_cloner --help

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/aclark4life/github_repos_cloner
  license: GPL
  summary: 'Clone all the github repos for backup purposes'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
