package:
  name: django-kicker
  version: "0.2.1"

source:
  fn: django-kicker-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-kicker/django-kicker-0.2.1.tar.gz
  md5: c3236c1e7e06ae03caa069b35f430cbb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-kicker = django-kicker:main
    #
    # Would create an entry point called django-kicker that calls django-kicker.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - kicker

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/orgs/anvil8
  license: BSD License
  summary: 'Realtime webstorage sync'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
