package:
  name: inhouse-web
  version: "0.0.4"

source:
  fn: inhouse-web-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/i/inhouse-web/inhouse-web-0.0.4.tar.gz
  md5: 982f26fef74c1c542a48302974de0b00
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - inhouse-web = inhouse-web:main
    #
    # Would create an entry point called inhouse-web that calls inhouse-web.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.2
    - django-pagination >=1.0
    - django-issue-synchronisation

  run:
    - python
    - django >=1.2
    - django-pagination >=1.0
    - django-issue-synchronisation

test:
  # Python imports
  imports:
    - inhouse
    - inhouse.templatetags
    - inhouse.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/inhouse-web
  license: GNU General Public License (GPL)
  summary: 'Web based time recording and management application'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
