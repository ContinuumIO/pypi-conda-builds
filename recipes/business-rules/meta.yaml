package:
  name: business-rules
  version: "0.1.3"

source:
  fn: business-rules-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/b/business-rules/business-rules-0.1.3.tar.gz
  md5: 8cce4076f62d6561407532c24dc3c7a4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - business-rules = business-rules:main
    #
    # Would create an entry point called business-rules that calls business-rules.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - business_rules

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/venmo/business-rules
  license: MIT
  summary: 'Python DSL for setting up business intelligence rules that can be configured without code'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
