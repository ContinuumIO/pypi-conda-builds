package:
  name: sorl-thumbnail
  version: "12.2"

source:
  fn: sorl-thumbnail-12.2.tar.gz
  url: https://pypi.python.org/packages/source/s/sorl-thumbnail/sorl-thumbnail-12.2.tar.gz
  md5: 1b32219a21640cba71b6ebe10b83fb41
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sorl-thumbnail = sorl-thumbnail:main
    #
    # Would create an entry point called sorl-thumbnail that calls sorl-thumbnail.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - sorl
    - sorl.thumbnail
    - sorl.thumbnail.admin
    - sorl.thumbnail.conf
    - sorl.thumbnail.engines
    - sorl.thumbnail.kvstores
    - sorl.thumbnail.management
    - sorl.thumbnail.management.commands
    - sorl.thumbnail.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mariocesar/sorl-thumbnail
  license: BSD License
  summary: 'Thumbnails for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
