package:
  name: xstatic-hogan
  version: "2.0.0.2"

source:
  fn: XStatic-Hogan-2.0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/X/XStatic-Hogan/XStatic-Hogan-2.0.0.2.tar.gz
  md5: 1c1de09c4813e8568aab98fa5270d6a0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - xstatic-hogan = xstatic-hogan:main
    #
    # Would create an entry point called xstatic-hogan that calls xstatic-hogan.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - xstatic
    - xstatic.pkg
    - xstatic.pkg.hogan

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://twitter.github.io/hogan.js/
  license: (same as Hogan)
  summary: 'Hogan 2.0.0 (XStatic packaging standard)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
