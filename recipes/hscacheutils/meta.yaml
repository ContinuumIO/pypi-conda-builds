package:
  name: hscacheutils
  version: "0.1.8"

source:
  fn: hscacheutils-0.1.8.tar.gz
  url: https://pypi.python.org/packages/source/h/hscacheutils/hscacheutils-0.1.8.tar.gz
  md5: 6d2a5d0e4a5816184c03b7ba6931d00d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hscacheutils = hscacheutils:main
    #
    # Would create an entry point called hscacheutils that calls hscacheutils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python-memcached ==1.47
    - django >=1.3,<1.7
    - django-cache-utils ==0.7

  run:
    - python
    - python-memcached ==1.47
    - django >=1.3,<1.7
    - django-cache-utils ==0.7

test:
  # Python imports
  imports:
    - hscacheutils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://dev.hubspot.com/
  license: MIT
  summary: "Some of Hubspot's python cache utils, namely generational caching"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
