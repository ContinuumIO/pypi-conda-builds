package:
  name: django-field-guard
  version: "0.1"

source:
  fn: django-field-guard-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-field-guard/django-field-guard-0.1.tar.gz
  md5: 36994edd45c9decd0febe9311cd26240
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-field-guard = django-field-guard:main
    #
    # Would create an entry point called django-field-guard that calls django-field-guard.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - fieldguard

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/MrKesn/django-field-guard
  license: MIT
  summary: 'Add per field permissions'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
