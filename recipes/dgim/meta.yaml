package:
  name: dgim
  version: "0.2.0"

source:
  fn: dgim-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/dgim/dgim-0.2.0.tar.gz
  md5: 7a8503f08a3104682326fc1c4afacd15
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dgim = dgim:main
    #
    # Would create an entry point called dgim that calls dgim.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - dgim

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/simondolle/dgim
  license: BSD License
  summary: 'Python implementation of the dgim algorithm: Compact datastructure to estimate the number of "True" in the last N elements of a boolean stream.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
