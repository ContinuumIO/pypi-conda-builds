package:
  name: au-skema
  version: "0.1-dev01"

source:
  fn: au-skema-0.1-dev01.tar.gz
  url: https://pypi.python.org/packages/source/a/au-skema/au-skema-0.1-dev01.tar.gz
  md5: a917f7a4ffe56c14f13b3abb8a82e884
#  patches:
   # List any patch files here
   # - fix.patch

build:
  # preserve_egg_dir: True
  entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - au-skema = au-skema:main
    #
    # Would create an entry point called au-skema that calls au-skema.main()

    - skema = skema:main

  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests
    - pyquery

  run:
    - python
    - requests
    - pyquery

test:
  # Python imports
  # imports:

  commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.

    - skema --help

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
