package:
  name: im2pdf
  version: "0.1"

source:
  fn: im2pdf-0.1.tar.gz
  url: https://pypi.python.org/packages/source/i/im2pdf/im2pdf-0.1.tar.gz
  md5: ed2950616dce18f32c60e01c5371558f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - im2pdf = im2pdf:main
    #
    # Would create an entry point called im2pdf that calls im2pdf.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pillow
    - pypdf2

  run:
    - python
    - pillow
    - pypdf2

test:
  # Python imports
  imports:
    - im2pdf

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tkmru/im2pdf
  license: MIT License
  summary: 'Tool to convert image to pdf and unite them.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
