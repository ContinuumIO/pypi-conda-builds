package:
  name: django-regex-field
  version: "0.1.5"

source:
  fn: django-regex-field-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-regex-field/django-regex-field-0.1.5.tar.gz
  md5: 3447e57fa40ea2969b8b61a2bce089c3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-regex-field = django-regex-field:main
    #
    # Would create an entry point called django-regex-field that calls django-regex-field.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5
    - six

  run:
    - python
    - django >=1.5
    - six

test:
  # Python imports
  imports:
    - regex_field
    - regex_field.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/ambitioninc/django-regex-field/
  license: MIT License
  summary: 'Store regular expressions in Django models'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
