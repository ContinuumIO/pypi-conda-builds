package:
  name: django_excel_templates
  version: "0.1"

source:
  fn: django_excel_templates-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django_excel_templates/django_excel_templates-0.1.tar.gz
  md5: d5b6963e8cde6e730220d2866206aef3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_excel_templates = django_excel_templates:main
    #
    # Would create an entry point called django_excel_templates that calls django_excel_templates.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - xlwt

  run:
    - python
    - xlwt

test:
  # Python imports
  imports:
    - django_excel_templates

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/django-excel-templates/
  license: MIT License
  summary: 'Generate Excel Tables from Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
