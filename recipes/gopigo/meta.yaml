package:
  name: gopigo
  version: "1.3"

source:
  fn: gopigo-1.3.tar.gz
  url: https://pypi.python.org/packages/source/g/gopigo/gopigo-1.3.tar.gz
  md5: 5218b6c9879c01908b225605a801e661
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gopigo = gopigo:main
    #
    # Would create an entry point called gopigo that calls gopigo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - gopigo

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/DexterInd/GoPiGo
  license: UNKNOWN
  summary: 'This is the python library for GoPiGo, which is is a delightful and complete robot for the Raspberry Pi that turns your Pi into a fully operating robot'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
