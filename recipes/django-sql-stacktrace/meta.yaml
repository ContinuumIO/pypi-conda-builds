package:
  name: django-sql-stacktrace
  version: "0.2.2"

source:
  fn: django-sql-stacktrace-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-sql-stacktrace/django-sql-stacktrace-0.2.2.tar.gz
  md5: b5ebf934c38b7e0b67112a5f61c2ca08
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-sql-stacktrace = django-sql-stacktrace:main
    #
    # Would create an entry point called django-sql-stacktrace that calls django-sql-stacktrace.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - sqlstacktrace

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/adw0rd/django-sql-stacktrace
  license: BSD License
  summary: 'Pluggable application, which puts a python stack trace in the SQL query as a comment (useful for debugging)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
