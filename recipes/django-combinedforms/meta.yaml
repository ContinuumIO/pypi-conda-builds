package:
  name: django-combinedforms
  version: "0.1.5"

source:
  fn: django-combinedforms-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-combinedforms/django-combinedforms-0.1.5.tar.gz
  md5: 6bcef6fde49f84cb95d133ada7721d32
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-combinedforms = django-combinedforms:main
    #
    # Would create an entry point called django-combinedforms that calls django-combinedforms.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - combinedform

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/leo-the-manic/django-combinedform
  license: LGPL3
  summary: 'Compose Django forms'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
