package:
  name: js.jquery_slimbox
  version: "2.04-1"

source:
  fn: js.jquery_slimbox-2.04-1.tar.gz
  url: https://pypi.python.org/packages/source/j/js.jquery_slimbox/js.jquery_slimbox-2.04-1.tar.gz
  md5: e4941e7956cf4b0cfbc5e06d5759bbd8
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - js.jquery_slimbox = js.jquery_slimbox:main
    #
    # Would create an entry point called js.jquery_slimbox that calls js.jquery_slimbox.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fanstatic
    - setuptools
    - js.jquery

  run:
    - python
    - setuptools
    - fanstatic
    - setuptools
    - js.jquery

test:
  # Python imports
  imports:
    - js
    - js.jquery_slimbox

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD
  summary: 'Fanstatic packaging of Slimbox'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
