package:
  name: tinytimer
  version: "0.0.0"

source:
  fn: tinytimer-0.0.0.tar.gz
  url: https://pypi.python.org/packages/source/t/tinytimer/tinytimer-0.0.0.tar.gz
  md5: 83d6988d1ca1702fcbd9ba5eae9d4fd7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tinytimer = tinytimer:main
    #
    # Would create an entry point called tinytimer that calls tinytimer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - tinytimer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/iskandr/microbench
  license: Apache Software License
  summary: 'Tiny Python benchmarking library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
