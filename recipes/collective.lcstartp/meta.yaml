package:
  name: collective.lcstartp
  version: "0.1"

source:
  fn: collective.lcstartp-0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.lcstartp/collective.lcstartp-0.1.tar.gz
  md5: f1189c806d774d73696118400448fe56
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.lcstartp = collective.lcstartp:main
    #
    # Would create an entry point called collective.lcstartp that calls collective.lcstartp.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.registry

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.registry

test:
  # Python imports
  imports:
    - collective
    - collective.lcstartp
    - collective.lcstartp.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/cleder/collective.lcstartp
  license: GPL
  summary: 'Linkchecker start page'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
