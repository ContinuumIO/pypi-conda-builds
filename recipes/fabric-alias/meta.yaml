package:
  name: fabric-alias
  version: "0.0.2"

source:
  fn: Fabric-Alias-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/F/Fabric-Alias/Fabric-Alias-0.0.2.tar.gz
  md5: 7b761d5cf90c2c1a747bfc24c8af4717
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fabric-alias = fabric-alias:main
    #
    # Would create an entry point called fabric-alias that calls fabric-alias.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fabric

  run:
    - python
    - fabric

test:
  # Python imports
  imports:
    - fabric_alias

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'fabric_alias is a plugin for fabric when using fabfile.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
