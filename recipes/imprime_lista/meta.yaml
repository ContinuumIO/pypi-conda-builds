package:
  name: imprime_lista
  version: "1.3.0"

source:
  fn: imprime_lista-1.3.0.tar.gz
  url: https://pypi.python.org/packages/source/i/imprime_lista/imprime_lista-1.3.0.tar.gz
  md5: c6d8fc2fe23d61d4dbc7bdb9ad0d2a9b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - imprime_lista = imprime_lista:main
    #
    # Would create an entry point called imprime_lista that calls imprime_lista.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - imprime_lista

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: guialpi.blogspot.com
  license: UNKNOWN
  summary: 'Um maneira simples de imprimir lista'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
