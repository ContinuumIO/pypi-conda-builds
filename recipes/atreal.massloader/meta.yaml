package:
  name: atreal.massloader
  version: "3.1.2"

source:
  fn: atreal.massloader-3.1.2.tar.gz
  url: https://pypi.python.org/packages/source/a/atreal.massloader/atreal.massloader-3.1.2.tar.gz
  md5: e6fb8f1461173746d9690854aec15c1f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - atreal.massloader = atreal.massloader:main
    #
    # Would create an entry point called atreal.massloader that calls atreal.massloader.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - pylzma

  run:
    - python
    - setuptools
    - setuptools
    - pylzma

test:
  # Python imports
  imports:
    - atreal
    - atreal.massloader
    - atreal.massloader.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://svn.plone.org/svn/collective/atreal.massloader/trunk
  license: GPL
  summary: 'Add an action to import zip files.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
