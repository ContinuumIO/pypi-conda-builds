package:
  name: jmbo-analytics
  version: "0.1"

source:
  fn: jmbo-analytics-0.1.tar.gz
  url: https://pypi.python.org/packages/source/j/jmbo-analytics/jmbo-analytics-0.1.tar.gz
  md5: 0217804b45e2f1c70dd8f07d1ec5e913
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jmbo-analytics = jmbo-analytics:main
    #
    # Would create an entry point called jmbo-analytics that calls jmbo-analytics.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django <1.7
    - django-celery
    - httplib2

  run:
    - python
    - django <1.7
    - django-celery
    - httplib2

test:
  # Python imports
  imports:
    - jmbo_analytics
    - jmbo_analytics.templatetags
    - jmbo_analytics.tests
    - jmbo_analytics.views

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/praekelt/jmbo-analytics
  license: BSD License
  summary: 'Jmbo analytics app.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
