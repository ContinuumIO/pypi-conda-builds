package:
  name: logilab-database
  version: "1.13.2"

source:
  fn: logilab-database-1.13.2.tar.gz
  url: https://pypi.python.org/packages/source/l/logilab-database/logilab-database-1.13.2.tar.gz
  md5: d0730e2ae566b004b60ca11f213c668a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - logilab-database = logilab-database:main
    #
    # Would create an entry point called logilab-database that calls logilab-database.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - logilab-common >=0.55.2
    - six >=1.4.0

  run:
    - python
    - logilab-common >=0.55.2
    - six >=1.4.0

test:
  # Python imports
  imports:
    - logilab.database
    - logilab.database.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.logilab.org/project/logilab-database
  license: LGPL
  summary: 'true unified database access'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
