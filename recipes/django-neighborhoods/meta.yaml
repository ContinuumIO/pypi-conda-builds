package:
  name: django-neighborhoods
  version: "2.0.1"

source:
  fn: django-neighborhoods-2.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-neighborhoods/django-neighborhoods-2.0.1.tar.gz
  md5: 5aab9faae9d6489db64c8054fa01ec94
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-neighborhoods = django-neighborhoods:main
    #
    # Would create an entry point called django-neighborhoods that calls django-neighborhoods.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django-localflavor >=1.0

  run:
    - python
    - django-localflavor >=1.0

test:
  # Python imports
  imports:
    - neighborhoods
    - neighborhoods.management
    - neighborhoods.management.commands
    - neighborhoods.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/latestrevision/django-neighborhoods/
  license: MIT License
  summary: 'Get neighborhood information for your coordinates.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
