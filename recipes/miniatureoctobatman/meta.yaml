package:
  name: miniatureoctobatman
  version: "0.1.2"

source:
  fn: miniatureOctoBatman-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/m/miniatureOctoBatman/miniatureOctoBatman-0.1.2.tar.gz
  md5: 2731355dec3afcfe193c5db46685f953
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - miniatureoctobatman = miniatureoctobatman:main
    #
    # Would create an entry point called miniatureoctobatman that calls miniatureoctobatman.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - miniatureOctoBatman
    - miniatureoctobatman

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/thefinn93/miniature-octo-batman
  license: UNKNOWN
  summary: "Finn's collection of powerline modules"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
