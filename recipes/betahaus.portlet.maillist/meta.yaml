package:
  name: betahaus.portlet.maillist
  version: "0.1"

source:
  fn: betahaus.portlet.maillist-0.1.tar.gz
  url: https://pypi.python.org/packages/source/b/betahaus.portlet.maillist/betahaus.portlet.maillist-0.1.tar.gz
  md5: c31fb148f647067dcb76e0b911f14f30
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - betahaus.portlet.maillist = betahaus.portlet.maillist:main
    #
    # Would create an entry point called betahaus.portlet.maillist that calls betahaus.portlet.maillist.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - betahaus
    - betahaus.portlet
    - betahaus.portlet.maillist
    - betahaus.portlet.maillist.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/betahaus.portlet.maillist
  license: GPL
  summary: 'Subscribe or unsubscribe from maillists'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
