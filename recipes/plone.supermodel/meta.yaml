package:
  name: plone.supermodel
  version: "1.2.6"

source:
  fn: plone.supermodel-1.2.6.zip
  url: https://pypi.python.org/packages/source/p/plone.supermodel/plone.supermodel-1.2.6.zip
  md5: e4b767cd909b22d300efba4c05845882
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.supermodel = plone.supermodel:main
    #
    # Would create an entry point called plone.supermodel that calls plone.supermodel.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - lxml
    - zope.component
    - zope.i18nmessageid
    - zope.interface
    - zope.schema
    - zope.deferredimport
    - zope.dottedname
    - z3c.zcmlhook

  run:
    - python
    - setuptools
    - setuptools
    - lxml
    - zope.component
    - zope.i18nmessageid
    - zope.interface
    - zope.schema
    - zope.deferredimport
    - zope.dottedname
    - z3c.zcmlhook

test:
  # Python imports
  imports:
    - plone
    - plone.supermodel

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/dexterity
  license: BSD License
  summary: 'Serialize Zope schema definitions to and from XML'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
