package:
  name: caipyrinha
  version: "0.2.2"

source:
  fn: caipyrinha-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/c/caipyrinha/caipyrinha-0.2.2.tar.gz
  md5: 35b68f5a35ef1ffff4feed5bcd1989e6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - caipyrinha = caipyrinha:main
    #
    # Would create an entry point called caipyrinha that calls caipyrinha.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - caipyrinha

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/leliel12/caipyrinha
  license: WISKEY License
  summary: 'Abstraction layer over argparse.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
