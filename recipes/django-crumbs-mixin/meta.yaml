package:
  name: django-crumbs-mixin
  version: "0.1.5.1"

source:
  fn: django-crumbs-mixin-0.1.5.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-crumbs-mixin/django-crumbs-mixin-0.1.5.1.tar.gz
  md5: 3ef95e42ac80bb0f3bf4c29647b20c89
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-crumbs-mixin = django-crumbs-mixin:main
    #
    # Would create an entry point called django-crumbs-mixin that calls django-crumbs-mixin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-breadcrumbs >=1.1.3-p1

  run:
    - python
    - django-breadcrumbs >=1.1.3-p1

test:
  # Python imports
  imports:
    - crumbs

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/bashu/django-crumbs-mixin
  license: BSD License
  summary: 'Put short description here...'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
