package:
  name: zope.app.intid
  version: "3.7.1"

source:
  fn: zope.app.intid-3.7.1.tar.gz
  url: https://pypi.python.org/packages/source/z/zope.app.intid/zope.app.intid-3.7.1.tar.gz
  md5: 0d2c1daf5d1d6fd09351b652042a2dac
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zope.app.intid = zope.app.intid:main
    #
    # Would create an entry point called zope.app.intid that calls zope.app.intid.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.intid
    - zope.security
    - zope.traversing

  run:
    - python
    - setuptools
    - zope.intid
    - zope.security
    - zope.traversing

test:
  # Python imports
  imports:
    - zope
    - zope.app
    - zope.app.intid
    - zope.app.intid.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/zope.app.intid
  license: Zope Public License
  summary: 'ZMI views for Integer Id Utility'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
