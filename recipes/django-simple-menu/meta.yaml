package:
  name: django-simple-menu
  version: "1.0.9"

source:
  fn: django-simple-menu-1.0.9.tar.gz
  url: https://pypi.python.org/packages/source/d/django-simple-menu/django-simple-menu-1.0.9.tar.gz
  md5: fae4ac9bd87545eccfe194dd8846794f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-simple-menu = django-simple-menu:main
    #
    # Would create an entry point called django-simple-menu that calls django-simple-menu.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - setuptools
    - django

test:
  # Python imports
  imports:
    - menu

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/fatbox/django-simple-menu
  license: BSD License
  summary: 'Simple, yet powerful, code-based menus for Django applications'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
