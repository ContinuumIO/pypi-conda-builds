package:
  name: colossus
  version: "0.0.1"

source:
  fn: colossus-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/colossus/colossus-0.0.1.tar.gz
  md5: 9c03e7ef8b8124847dae00a7f1c1f0a7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - colossus = colossus:main
    #
    # Would create an entry point called colossus that calls colossus.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - colossus
    - colossus.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/sleibman/python-colossus
  license: MIT License
  summary: 'Python tools for manipulating extremely large images'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
