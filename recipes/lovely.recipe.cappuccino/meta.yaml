package:
  name: lovely.recipe.cappuccino
  version: "0.0.7"

source:
  fn: lovely.recipe.cappuccino-0.0.7.tar.gz
  url: https://pypi.python.org/packages/source/l/lovely.recipe.cappuccino/lovely.recipe.cappuccino-0.0.7.tar.gz
  md5: dc5fb626a4a36aab6217f43a437c2623
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lovely.recipe.cappuccino = lovely.recipe.cappuccino:main
    #
    # Would create an entry point called lovely.recipe.cappuccino that calls lovely.recipe.cappuccino.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout

test:
  # Python imports
  imports:
    - lovely
    - lovely.recipe
    - lovely.recipe.cappuccino

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/lovelysystems/lovely.recipe.cappuccino
  license: ZPL 2.1
  summary: 'local cappuccino setup for zc.buildout'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
