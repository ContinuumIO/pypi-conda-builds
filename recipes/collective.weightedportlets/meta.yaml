package:
  name: collective.weightedportlets
  version: "1.1"

source:
  fn: collective.weightedportlets-1.1.zip
  url: https://pypi.python.org/packages/source/c/collective.weightedportlets/collective.weightedportlets-1.1.zip
  md5: ba7e0dcd974868de02052b6a15e8f1e2
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.weightedportlets = collective.weightedportlets:main
    #
    # Would create an entry point called collective.weightedportlets that calls collective.weightedportlets.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.portlets

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.portlets

test:
  # Python imports
  imports:
    - collective
    - collective.weightedportlets
    - collective.weightedportlets.browser
    - collective.weightedportlets.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/collective.weightedportlets
  license: GNU General Public License (GPL)
  summary: 'Adds the ability to tweak portlet ordering by giving each portlet a weight.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
