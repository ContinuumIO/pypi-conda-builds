package:
  name: betterprint
  version: "0.1"

source:
  fn: betterprint-0.1.tar.gz
  url: https://pypi.python.org/packages/source/b/betterprint/betterprint-0.1.tar.gz
  md5: ca40c3bf4ecdcc39557aa4f96d136665
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - betterprint = betterprint:main
    #
    # Would create an entry point called betterprint that calls betterprint.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - betterprint

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://misc.slowchop.com/misc/wiki/BetterPrint
  license: UNKNOWN
  summary: 'Alternate to pprint'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
