package:
  name: django_localizejs_seo
  version: "0.2"

source:
  fn: django_localizejs_seo-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django_localizejs_seo/django_localizejs_seo-0.2.tar.gz
  md5: b55025c342729e8b0379221de7e43347
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_localizejs_seo = django_localizejs_seo:main
    #
    # Would create an entry point called django_localizejs_seo that calls django_localizejs_seo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - autopep8 ==1.1.1
    - cffi ==0.9.2
    - cryptography ==0.8.1
    - enum34 ==1.0.4
    - gnureadline ==6.3.3
    - ipdb ==0.8
    - ipython ==3.0.0
    - ndg-httpsclient ==0.3.3
    - pep8 ==1.6.2
    - pyasn1 ==0.1.7
    - pycparser ==2.10
    - pyopenssl ==0.14
    - requests ==2.6.0
    - six ==1.9.0

  run:
    - python
    - autopep8 ==1.1.1
    - cffi ==0.9.2
    - cryptography ==0.8.1
    - enum34 ==1.0.4
    - gnureadline ==6.3.3
    - ipdb ==0.8
    - ipython ==3.0.0
    - ndg-httpsclient ==0.3.3
    - pep8 ==1.6.2
    - pyasn1 ==0.1.7
    - pycparser ==2.10
    - pyopenssl ==0.14
    - requests ==2.6.0
    - six ==1.9.0

test:
  # Python imports
  imports:
    - django_localizejs_seo

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/yoland68/django_localizejs_seo
  license: UNKNOWN
  summary: 'A Python Django prerender middleware for Localize JS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
