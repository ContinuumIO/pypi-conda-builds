package:
  name: django-dnd
  version: "0.1.3"

source:
  fn: django-dnd-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-dnd/django-dnd-0.1.3.tar.gz
  md5: f21608f7c2607a1e3c8471aea49cfbd1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-dnd = django-dnd:main
    #
    # Would create an entry point called django-dnd that calls django-dnd.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django-cas >=2.0
    - dnd

  run:
    - python
    - django-cas >=2.0
    - dnd

test:
  # Python imports
  imports:
    - django_dnd

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/quentonc/django-dnd
  license: LICENSE
  summary: 'Django auth backend for populating user data using the Dartmouth Name Directory'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
