package:
  name: lognames
  version: "0.1dev"

source:
  fn: lognames-0.1dev.tar.gz
  url: https://pypi.python.org/packages/source/l/lognames/lognames-0.1dev.tar.gz
  md5: f838371973e97ae16c82680d9ca9f4b0
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lognames = lognames:main
    #
    # Would create an entry point called lognames that calls lognames.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - lognames

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://stackoverflow.com/users/1006989/x-jacobs
  license: GPL
  summary: 'This is a package that uses logging to print the arguments and name of the method called'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
