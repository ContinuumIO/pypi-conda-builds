package:
  name: django-postgresql-manager
  version: "0.2.0"

source:
  fn: django-postgresql-manager-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-postgresql-manager/django-postgresql-manager-0.2.0.tar.gz
  md5: fd086be57686ec305d7d87d24b59bbbc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-postgresql-manager = django-postgresql-manager:main
    #
    # Would create an entry point called django-postgresql-manager that calls django-postgresql-manager.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - postgresql_manager

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.codetrax.org/projects/django-postgresql-manager
  license: Apache Software License
  summary: 'django-postgresql-manager is a Django based management interface for PostgreSQL users and databases.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
