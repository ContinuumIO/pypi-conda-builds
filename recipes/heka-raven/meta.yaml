package:
  name: heka-raven
  version: "0.5"

source:
  fn: heka-raven-0.5.tar.gz
  url: https://pypi.python.org/packages/source/h/heka-raven/heka-raven-0.5.tar.gz
  md5: 885153bbe1d86ffeca42c0109f9f347c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - heka-raven = heka-raven:main
    #
    # Would create an entry point called heka-raven that calls heka-raven.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - heka-py >=0.10
    - raven ==3.1.16

  run:
    - python
    - heka-py >=0.10
    - raven ==3.1.16

test:
  # Python imports
  imports:
    - heka_raven

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/mozilla-services/heka-raven
  license: Mozilla Public License 2.0 (MPL 2.0)
  summary: 'A heka-py plugin to send exceptions to Sentry'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
