package:
  name: flint-md
  version: "1.1.1"

source:
  fn: flint-md-1.1.1.tar.gz
  url: https://pypi.python.org/packages/source/f/flint-md/flint-md-1.1.1.tar.gz
  md5: 6bb6c55df10433dccfc11a112f15c01b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flint-md = flint-md:main
    #
    # Would create an entry point called flint-md that calls flint-md.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - markdown >=2.3.1

  run:
    - python
    - markdown >=2.3.1

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://cdawson.net/flint
  license: MIT License
  summary: 'Dynamic Markdown Templating'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
