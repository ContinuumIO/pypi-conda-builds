package:
  name: alterootheme.intensesimplicity
  version: "1.0a"

source:
  fn: alterootheme.intensesimplicity-1.0a.tar.gz
  url: https://pypi.python.org/packages/source/a/alterootheme.intensesimplicity/alterootheme.intensesimplicity-1.0a.tar.gz
  md5: 229de0e5af2519c260ba72ee62e5110e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - alterootheme.intensesimplicity = alterootheme.intensesimplicity:main
    #
    # Would create an entry point called alterootheme.intensesimplicity that calls alterootheme.intensesimplicity.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - alterootheme
    - alterootheme.intensesimplicity
    - alterootheme.intensesimplicity.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://themes.alteroo.com
  license: GPL
  summary: 'A Plone 3.0 Theme based on a free template by David Uliana'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
