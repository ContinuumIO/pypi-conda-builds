package:
  name: asamended
  version: "0.04"

source:
  fn: asamended-0.04.tar.gz
  url: https://pypi.python.org/packages/source/a/asamended/asamended-0.04.tar.gz
  md5: 2d9596cd1ac2b23b7c3172b8f5bd69b2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - asamended = asamended:main
    #
    # Would create an entry point called asamended that calls asamended.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - tater
    - networkx
    - requests

  run:
    - python
    - tater
    - networkx
    - requests

test:
  # Python imports
  imports:
    - asamended
    - asamended.uscode

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://asamended.github.com/python-asamended/
  license: MIT
  summary: 'Client library for the asamended.com API.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
