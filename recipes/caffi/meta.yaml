package:
  name: caffi
  version: "0.99"

source:
  fn: caffi-0.99.tar.gz
  url: https://pypi.python.org/packages/source/c/caffi/caffi-0.99.tar.gz
  md5: ed3376725f3902563bcab43a3fe98d2c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - caffi = caffi:main
    #
    # Would create an entry point called caffi that calls caffi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cffi >=0.8
    - enum34

  run:
    - python
    - cffi >=0.8
    - enum34

test:
  # Python imports
  imports:
    - caffi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/xwang/caffi/
  license: BSD License
  summary: 'Channel Access Foreign Function Interface'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
