package:
  name: flask-thumbnails
  version: "0.2.1"

source:
  fn: Flask-thumbnails-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-thumbnails/Flask-thumbnails-0.2.1.tar.gz
  md5: faadfdf604c20d024204c2afc1083985
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-thumbnails = flask-thumbnails:main
    #
    # Would create an entry point called flask-thumbnails that calls flask-thumbnails.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - flask
    - pillow ==2.6.0

  run:
    - python
    - flask
    - pillow ==2.6.0

test:
  # Python imports
  imports:
    - flask_thumbnails

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/SilentSokolov/flask-thumbnails
  license: MIT
  summary: 'A simple extension to create a thumbs for the Flask'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
