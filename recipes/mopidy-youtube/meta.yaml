package:
  name: mopidy-youtube
  version: "2.0.0"

source:
  fn: Mopidy-Youtube-2.0.0.tar.gz
  url: https://pypi.python.org/packages/source/M/Mopidy-Youtube/Mopidy-Youtube-2.0.0.tar.gz
  md5: 463e2adb9f7af04ce0a282930329c387
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mopidy-youtube = mopidy-youtube:main
    #
    # Would create an entry point called mopidy-youtube that calls mopidy-youtube.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - requests >=2.2.1
    - pafy >=0.3.35
    - mopidy >=1.0
    - pykka >=1.1

  run:
    - python
    - setuptools
    - setuptools
    - requests >=2.2.1
    - pafy >=0.3.35
    - mopidy >=1.0
    - pykka >=1.1

test:
  # Python imports
  imports:
    - mopidy_youtube

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dz0ny/mopidy-youtube
  license: Apache Software License
  summary: u'Mopidy extension that plays sound from Youtube'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
