package:
  name: collective.cdn.core
  version: "0.6"

source:
  fn: collective.cdn.core-0.6.zip
  url: https://pypi.python.org/packages/source/c/collective.cdn.core/collective.cdn.core-0.6.zip
  md5: 631e3cdb677e2e2ecea8cfa74a236d1c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.cdn.core = collective.cdn.core:main
    #
    # Would create an entry point called collective.cdn.core that calls collective.cdn.core.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.cdn
    - collective.cdn.core
    - collective.cdn.core.browser
    - collective.cdn.core.controlpanel
    - collective.cdn.core.docs
    - collective.cdn.core.interfaces
    - collective.cdn.core.tests
    - collective.cdn.core.upgrades

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.simplesconsultoria.com.br/
  license: GPL
  summary: 'CDN support for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
