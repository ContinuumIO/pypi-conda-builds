package:
  name: configtreeview
  version: "0.1.32"

source:
  fn: configtreeview-0.1.32.tar.gz
  url: https://pypi.python.org/packages/source/c/configtreeview/configtreeview-0.1.32.tar.gz
  md5: 24d466df0f2f69a1523d50e3c1464c1a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - configtreeview = configtreeview:main
    #
    # Would create an entry point called configtreeview that calls configtreeview.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - simplejson
    - pygtk >=2.0.0

  run:
    - python
    - simplejson
    - pygtk >=2.0.0

test:
  # Python imports
  imports:
    - configtreeview
    - configtreeview.tools

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/wrhansen/ConfigTreeView
  license: GPLv3
  summary: 'An easily configurable GtkTreeView implementation for pygtk.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
