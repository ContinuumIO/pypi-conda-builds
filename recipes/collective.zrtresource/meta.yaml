package:
  name: collective.zrtresource
  version: "0.1"

source:
  fn: collective.zrtresource-0.1.zip
  url: https://pypi.python.org/packages/source/c/collective.zrtresource/collective.zrtresource-0.1.zip
  md5: dce33aed8ba473c3f597db40bb0f05cf
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.zrtresource = collective.zrtresource:main
    #
    # Would create an entry point called collective.zrtresource that calls collective.zrtresource.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - z3c.zrtresource

  run:
    - python
    - setuptools
    - setuptools
    - z3c.zrtresource

test:
  # Python imports
  imports:
    - collective
    - collective.zrtresource

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: GPL
  summary: 'This package is a tiny wrapper around z3c.zrtresource so it works in Zope2 (tested with 2.10).'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
