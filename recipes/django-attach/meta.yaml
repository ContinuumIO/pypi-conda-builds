package:
  name: django-attach
  version: "0.1.3"

source:
  fn: django-attach-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-attach/django-attach-0.1.3.tar.gz
  md5: 183ce6a1fbcd9b53d4bc829ad34f55dd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-attach = django-attach:main
    #
    # Would create an entry point called django-attach that calls django-attach.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_attach

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/peterkuma/django-attach/
  license: BSD License
  summary: 'Django admin plugin for attaching files to model instances with multiple file selection support'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
