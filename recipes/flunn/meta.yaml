package:
  name: flunn
  version: "1.0.0b4"

source:
  fn: flunn-1.0.0b4.tar.gz
  url: https://pypi.python.org/packages/source/f/flunn/flunn-1.0.0b4.tar.gz
  md5: c5f6c817517916db37ef2a744a673667
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flunn = flunn:main
    #
    # Would create an entry point called flunn that calls flunn.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - flunn

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/funny-falcon/flunn.git
  license: MIT License
  summary: 'Simple decoder and encoder for CBOR (fork of flynn)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
