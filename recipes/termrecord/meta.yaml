package:
  name: termrecord
  version: "1.2.4"

source:
  fn: TermRecord-1.2.4.tar.gz
  url: https://pypi.python.org/packages/source/T/TermRecord/TermRecord-1.2.4.tar.gz
  md5: bb416b214c6c7d8dc63ae42c6b1e8460
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - termrecord = termrecord:main
    #
    # Would create an entry point called termrecord that calls termrecord.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - jinja2 >=2.6

  run:
    - python
    - jinja2 >=2.6

test:
  # Python imports
  imports:
    - termrecord

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/theonewolf/TermRecord
  license: MIT License
  summary: 'A simple terminal session recorder with easy-to-share HTML output!'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
