package:
  name: repoze.lru
  version: "0.6"

source:
  fn: repoze.lru-0.6.tar.gz
  url: https://pypi.python.org/packages/source/r/repoze.lru/repoze.lru-0.6.tar.gz
  md5: 2c3b64b17a8e18b405f55d46173e14dd
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - repoze.lru = repoze.lru:main
    #
    # Would create an entry point called repoze.lru that calls repoze.lru.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - repoze
    - repoze.lru

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.repoze.org
  license: BSD-derived (http://www.repoze.org/LICENSE.txt)
  summary: 'A tiny LRU cache implementation and decorator'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
