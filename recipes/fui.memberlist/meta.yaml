package:
  name: fui.memberlist
  version: "0.3.2"

source:
  fn: fui.memberlist-0.3.2.tar.gz
  url: https://pypi.python.org/packages/source/f/fui.memberlist/fui.memberlist-0.3.2.tar.gz
  md5: 94f1658ee8d6aa0237b8d9a7d7ac3ee4
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fui.memberlist = fui.memberlist:main
    #
    # Would create an entry point called fui.memberlist that calls fui.memberlist.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - fui
    - fui.memberlist
    - fui.memberlist.browser
    - fui.memberlist.content
    - fui.memberlist.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/espenak/fui.memberlist
  license: GPL
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
