package:
  name: collective.ots
  version: "0.3"

source:
  fn: collective.ots-0.3.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.ots/collective.ots-0.3.tar.gz
  md5: 6094b44c4ac27c27d3e2e2f3928aab7a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.ots = collective.ots:main
    #
    # Would create an entry point called collective.ots that calls collective.ots.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.ots
    - collective.ots.browser
    - collective.ots.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/collective.ots
  license: GNU General Public License (GPL)
  summary: 'Writing a good summary (description) is hard,\n                let the Open Text Summarizer (OTS) do the heavy lifting.\n                This tool extracts a summary of the content and sets it\n                as the description of the content item you added'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
