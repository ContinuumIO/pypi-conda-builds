package:
  name: cubicweb-event
  version: "1.3.1"

source:
  fn: cubicweb-event-1.3.1.tar.gz
  url: https://pypi.python.org/packages/source/c/cubicweb-event/cubicweb-event-1.3.1.tar.gz
  md5: c207e9af3d4b62c5dd6ec5216f5804e3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cubicweb-event = cubicweb-event:main
    #
    # Would create an entry point called cubicweb-event that calls cubicweb-event.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cubicweb >=3.9.0

  run:
    - python
    - cubicweb >=3.9.0

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.cubicweb.org/project/cubicweb-event
  license: LGPL
  summary: 'event component for the CubicWeb framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
