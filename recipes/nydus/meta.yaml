package:
  name: nydus
  version: "0.11.0"

source:
  fn: nydus-0.11.0.tar.gz
  url: https://pypi.python.org/packages/source/n/nydus/nydus-0.11.0.tar.gz
  md5: d7a69e5afd9f7606a61168791484d4c3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - nydus = nydus:main
    #
    # Would create an entry point called nydus that calls nydus.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - nydus
    - nydus.contrib
    - nydus.db
    - nydus.db.backends
    - nydus.db.routers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/disqus/nydus
  license: Apache License 2.0
  summary: 'Connection utilities'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
