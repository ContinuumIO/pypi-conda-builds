package:
  name: redisco
  version: "0.1.4"

source:
  fn: redisco-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/r/redisco/redisco-0.1.4.tar.gz
  md5: 1a4963a9d9f513ccbdb2f5f5766d21f2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - redisco = redisco:main
    #
    # Would create an entry point called redisco that calls redisco.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - redisco
    - redisco.models

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/iamteem/redisco
  license: MIT License
  summary: 'Python Containers and Simple Models for Redis'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
