package:
  name: tornado_systemd
  version: "1.0.1"

source:
  fn: tornado_systemd-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/t/tornado_systemd/tornado_systemd-1.0.1.tar.gz
  md5: ff783abfef6577b34429c69fa6e9aaa7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tornado_systemd = tornado_systemd:main
    #
    # Would create an entry point called tornado_systemd that calls tornado_systemd.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - tornado >=3.2

  run:
    - python
    - tornado >=3.2

test:
  # Python imports
  imports:
    - tornado_systemd

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/paradoxxxzero/tornado_systemd
  license: GNU General Public License v3 (GPLv3)
  summary: 'Use socket activation with tornado'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
