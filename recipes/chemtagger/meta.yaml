package:
  name: chemtagger
  version: "1.0.0"

source:
  fn: ChemTagger-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/C/ChemTagger/ChemTagger-1.0.0.tar.gz
  md5: 0dbebeed760e8a8647118284dff47afb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - chemtagger = chemtagger:main
    #
    # Would create an entry point called chemtagger that calls chemtagger.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - chemtagger

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.cbs.dtu.dk
  license: This package is free software; you can redistribute it and/or modify it under the terms of the "GNU General Public License"
  summary: 'A tagger for chemical entities'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
