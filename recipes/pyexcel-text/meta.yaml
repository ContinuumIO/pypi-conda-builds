package:
  name: pyexcel-text
  version: "0.0.2"

source:
  fn: pyexcel-text-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/p/pyexcel-text/pyexcel-text-0.0.2.tar.gz
  md5: 2b403a462ed9ec9bcfcca3c4f1120e9b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pyexcel-text = pyexcel-text:main
    #
    # Would create an entry point called pyexcel-text that calls pyexcel-text.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyexcel >=0.0.9
    - tabulate

  run:
    - python
    - pyexcel >=0.0.9
    - tabulate

test:
  # Python imports
  imports:
    - pyexcel_text

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/chfw/pyexcel-text
  license: GNU General Public License v3 (GPLv3)
  summary: 'It is a plugin to pyexcel and provides the capbility to present and write data in text fromats'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
