package:
  name: collective.ptg.s3slider
  version: "1.1"

source:
  fn: collective.ptg.s3slider-1.1.zip
  url: https://pypi.python.org/packages/source/c/collective.ptg.s3slider/collective.ptg.s3slider-1.1.zip
  md5: 7923d40b4212267e37a4eba4bf71d952
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.ptg.s3slider = collective.ptg.s3slider:main
    #
    # Would create an entry point called collective.ptg.s3slider that calls collective.ptg.s3slider.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.plonetruegallery >=3.2a1

  run:
    - python
    - setuptools
    - setuptools
    - collective.plonetruegallery >=3.2a1

test:
  # Python imports
  imports:
    - collective
    - collective.ptg
    - collective.ptg.s3slider

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.ptg.s3slider
  license: GPL
  summary: 'Adds s3slider to collective.plonetruegallery'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
