package:
  name: flask-hippocket
  version: "0.2.0b1"

source:
  fn: Flask-HipPocket-0.2.0b1.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-HipPocket/Flask-HipPocket-0.2.0b1.tar.gz
  md5: 4c7f2c9b6413dc7ba1e6b2adab134bf3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-hippocket = flask-hippocket:main
    #
    # Would create an entry point called flask-hippocket that calls flask-hippocket.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.7
    - jinja2 >=2.4
    - werkzeug >=0.7

  run:
    - python
    - flask >=0.7
    - jinja2 >=2.4
    - werkzeug >=0.7

test:
  # Python imports
  imports:
    - flask_hippocket
    - flask_hippocket.tests
    - flask_hippocket.tests.test_apps
    - flask_hippocket.tests.test_apps.mapper

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/svieira/Flask-HipPocket
  license: MIT License
  summary: 'A wrapper around Flask to ease the development of larger applications'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
