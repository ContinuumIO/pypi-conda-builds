package:
  name: plone.app.lockingbehavior
  version: "1.0.1"

source:
  fn: plone.app.lockingbehavior-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/p/plone.app.lockingbehavior/plone.app.lockingbehavior-1.0.1.tar.gz
  md5: a25745f1f40c6298da1b228ccd95ee27
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.app.lockingbehavior = plone.app.lockingbehavior:main
    #
    # Would create an entry point called plone.app.lockingbehavior that calls plone.app.lockingbehavior.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.behavior
    - plone.dexterity >=1.1
    - plone.locking

  run:
    - python
    - setuptools
    - setuptools
    - plone.behavior
    - plone.dexterity >=1.1
    - plone.locking

test:
  # Python imports
  imports:
    - plone
    - plone.app
    - plone.app.lockingbehavior
    - plone.app.lockingbehavior.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/plone/plone.app.lockingbehavior/
  license: GPL2
  summary: 'Locking integration for dexterity content objects.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
