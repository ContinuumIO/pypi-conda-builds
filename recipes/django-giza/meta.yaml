package:
  name: django-giza
  version: "0.1.6"

source:
  fn: django-giza-0.1.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-giza/django-giza-0.1.6.tar.gz
  md5: f69cbfd5620463ee88d160ca0526f779
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-giza = django-giza:main
    #
    # Would create an entry point called django-giza that calls django-giza.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - giza
    - giza.management
    - giza.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/anentropic/giza
  license: BSD License
  summary: "Autodoc all modules from a Django project's INSTALLED_APPS for Sphinx"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
