package:
  name: django-api-docs
  version: "1.1.1"

source:
  fn: django-api-docs-1.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-api-docs/django-api-docs-1.1.1.tar.gz
  md5: b3ff5001185aa548c5d584fdc8bacade
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-api-docs = django-api-docs:main
    #
    # Would create an entry point called django-api-docs that calls django-api-docs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - api_docs
    - api_docs.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/dstegelman/django-interactive-api-docs
  license: MIT License
  summary: 'Interactive API Documentation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
