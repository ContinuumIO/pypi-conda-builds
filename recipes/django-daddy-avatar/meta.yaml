package:
  name: django-daddy-avatar
  version: "1.0.3"

source:
  fn: django-daddy-avatar-1.0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-daddy-avatar/django-daddy-avatar-1.0.3.tar.gz
  md5: 9af63e394c4f5a9ee1e02b6b3cad4cc8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-daddy-avatar = django-daddy-avatar:main
    #
    # Would create an entry point called django-daddy-avatar that calls django-daddy-avatar.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - daddy_avatar
    - daddy_avatar.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/karoon/django-daddy-avatar
  license: BSD License
  summary: 'very simple avatar from gravatar'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
