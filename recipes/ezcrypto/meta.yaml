package:
  name: ezcrypto
  version: "0.1b0"

source:
  fn: EzCrypto-0.1b0.tar.gz
  url: https://pypi.python.org/packages/source/E/EzCrypto/EzCrypto-0.1b0.tar.gz
  md5: bcef62dc1ebf642790bad19b8cc26c00
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ezcrypto = ezcrypto:main
    #
    # Would create an entry point called ezcrypto that calls ezcrypto.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pycrypto >=2.6

  run:
    - python
    - pycrypto >=2.6

test:
  # Python imports
  imports:
    - ezcrypto
    - ezcrypto.pki

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github/gnrfan/EzCrypto/
  license: BSD License
  summary: 'Easy to use wrapper around ryptographic libraries for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
