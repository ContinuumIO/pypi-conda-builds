package:
  name: sloth-ci.ext.ssh_exec
  version: "1.0.8"

source:
  fn: sloth-ci.ext.ssh_exec-1.0.8.zip
  url: https://pypi.python.org/packages/source/s/sloth-ci.ext.ssh_exec/sloth-ci.ext.ssh_exec-1.0.8.zip
  md5: bc505228edfdc28509cb2b110c5117c5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sloth-ci.ext.ssh_exec = sloth-ci.ext.ssh_exec:main
    #
    # Would create an entry point called sloth-ci.ext.ssh_exec that calls sloth-ci.ext.ssh_exec.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sloth_ci >=1.1.9
    - paramiko

  run:
    - python
    - sloth_ci >=1.1.9
    - paramiko

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/moigagoo/sloth-ci-extensions
  license: MIT
  summary: 'SSH executor extension for Sloth CI'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
