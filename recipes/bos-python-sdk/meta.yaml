package:
  name: bos-python-sdk
  version: "0.8.2.1"

source:
  fn: bos-python-sdk-0.8.2.1.zip
  url: https://pypi.python.org/packages/source/b/bos-python-sdk/bos-python-sdk-0.8.2.1.zip
  md5: 63b539a251a2dc82b3a751ce1e300c09
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bos-python-sdk = bos-python-sdk:main
    #
    # Would create an entry point called bos-python-sdk that calls bos-python-sdk.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - baidubce
    - baidubce.auth
    - baidubce.http
    - baidubce.services
    - baidubce.services.bos

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bce.baidu.com/Product/BOS.html
  license: Free
  summary: 'BOS-Python-SDK'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
