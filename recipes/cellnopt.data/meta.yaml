package:
  name: cellnopt.data
  version: "1.0.3"

source:
  fn: cellnopt.data-1.0.3.tar.gz
  url: https://pypi.python.org/packages/source/c/cellnopt.data/cellnopt.data-1.0.3.tar.gz
  md5: 356a47dd8ad74afbb668080ec95b6d81
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cellnopt.data = cellnopt.data:main
    #
    # Would create an entry point called cellnopt.data that calls cellnopt.data.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - easydev >=0.5.6

  run:
    - python
    - easydev >=0.5.6

test:
  # Python imports
  imports:
    - cellnopt
    - cellnopt.data
    - data

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: ['http://www.ebi.ac.uk/~cokelaer/cellnopt/data']
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'data and model repository for http://www.cellnopt.org project'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
