package:
  name: kotti_events
  version: "0.1.0b3"

source:
  fn: kotti_events-0.1.0b3.tar.gz
  url: https://pypi.python.org/packages/source/k/kotti_events/kotti_events-0.1.0b3.tar.gz
  md5: c4c97706e7b8fbf2e8ec5bc059f0b35f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kotti_events = kotti_events:main
    #
    # Would create an entry point called kotti_events that calls kotti_events.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - kotti >=0.6.0b1
    - babel
    - pil
    - colander >=0.9.8

  run:
    - python
    - setuptools
    - kotti >=0.6.0b1
    - babel
    - pil
    - colander >=0.9.8

test:
  # Python imports
  imports:
    - kotti_events

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/kotti_events
  license: BSD License
  summary: 'Event collection for Kotti sites'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
