package:
  name: 0x10c-asm
  version: "0.0.2"

source:
  fn: 0x10c-asm-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/0/0x10c-asm/0x10c-asm-0.0.2.tar.gz
  md5: 6a89c7356b23b96a99f0e866ac4d8e7d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - 0x10c-asm = 0x10c-asm:main
    #
    # Would create an entry point called 0x10c-asm that calls 0x10c-asm.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/severb/0x10c-asm
  license: BSD License
  summary: 'A simple Python-based DCPU assembly compiler'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
