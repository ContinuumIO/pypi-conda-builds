package:
  name: dangeruxb
  version: "1.1"

source:
  fn: DangerUXB-1.1.tar.gz
  url: https://pypi.python.org/packages/source/D/DangerUXB/DangerUXB-1.1.tar.gz
  md5: 57b657d7ef8192b675bab9a1c4bcfb0a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dangeruxb = dangeruxb:main
    #
    # Would create an entry point called dangeruxb that calls dangeruxb.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - DangerUXB
    - dangeruxb

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/DangerUXB/1.1
  license: GNU General Public License v3 (GPLv3)
  summary: 'A version of the Mines game intended for casual play.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
