package:
  name: dflat
  version: "0.5"

source:
  fn: dflat-0.5.tar.gz
  url: https://pypi.python.org/packages/source/d/dflat/dflat-0.5.tar.gz
  md5: 4ed8374a8f3c6299dedbaed216470574
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dflat = dflat:main
    #
    # Would create an entry point called dflat that calls dflat.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - namaste

  run:
    - python
    - namaste

test:
  # Python imports
  imports:
    - dflat

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/edsu/dflat
  license: UNKNOWN
  summary: 'a command line tool for working with dflat digital preservation file systems'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
