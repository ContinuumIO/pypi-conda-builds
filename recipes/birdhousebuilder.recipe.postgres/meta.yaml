package:
  name: birdhousebuilder.recipe.postgres
  version: "0.2.0"

source:
  fn: birdhousebuilder.recipe.postgres-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/b/birdhousebuilder.recipe.postgres/birdhousebuilder.recipe.postgres-0.2.0.tar.gz
  md5: c359171c59ae02d337d50bd19a6cb12e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - birdhousebuilder.recipe.postgres = birdhousebuilder.recipe.postgres:main
    #
    # Would create an entry point called birdhousebuilder.recipe.postgres that calls birdhousebuilder.recipe.postgres.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - mako
    - birdhousebuilder.recipe.conda
    - birdhousebuilder.recipe.supervisor

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - mako
    - birdhousebuilder.recipe.conda
    - birdhousebuilder.recipe.supervisor

test:
  # Python imports
  imports:
    - birdhousebuilder
    - birdhousebuilder.recipe
    - birdhousebuilder.recipe.postgres
    - birdhousebuilder.recipe.postgres.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bird-house/birdhousebuilder.recipe.postgres
  license: GNU General Public License v2 (GPLv2)
  summary: 'A Buildout recipe to install and configure Postgres database with Anaconda.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
