package:
  name: collective.picnik
  version: "1.1"

source:
  fn: collective.picnik-1.1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.picnik/collective.picnik-1.1.tar.gz
  md5: c693165ff3fd03e1f8a1947266354017
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.picnik = collective.picnik:main
    #
    # Would create an entry point called collective.picnik that calls collective.picnik.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.registry

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.registry

test:
  # Python imports
  imports:
    - collective
    - collective.picnik
    - collective.picnik.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://toutpt.makina-corpus.org/en/add-ons/collective.picnik
  license: GPL
  summary: 'Integration of picnik service into Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
