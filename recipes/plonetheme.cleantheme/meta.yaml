package:
  name: plonetheme.cleantheme
  version: "0.1"

source:
  fn: plonetheme.cleantheme-0.1.tar.gz
  url: https://pypi.python.org/packages/source/p/plonetheme.cleantheme/plonetheme.cleantheme-0.1.tar.gz
  md5: c0c4b2ec57eeca6a8322c622dc66499b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plonetheme.cleantheme = plonetheme.cleantheme:main
    #
    # Would create an entry point called plonetheme.cleantheme that calls plonetheme.cleantheme.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - plonetheme
    - plonetheme.cleantheme
    - plonetheme.cleantheme.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/plonetheme.cleantheme
  license: GPL
  summary: 'An installable theme for Plone 3.0'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
