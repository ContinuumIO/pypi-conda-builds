package:
  name: o2w_settings
  version: "0.0.1"

source:
  fn: o2w_settings-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/o/o2w_settings/o2w_settings-0.0.1.tar.gz
  md5: 9f37ada43c33aa6d338600705e1f322d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - o2w_settings = o2w_settings:main
    #
    # Would create an entry point called o2w_settings that calls o2w_settings.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - o2w
    - o2w.settings
    - o2w_settings

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.o2w.es/
  license: LICENSE.txt
  summary: u'Helper funcions'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
