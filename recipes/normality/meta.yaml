package:
  name: normality
  version: "0.2.2"

source:
  fn: normality-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/n/normality/normality-0.2.2.tar.gz
  md5: fd44a9eacc50ebb2e2834da33bbfd3b1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - normality = normality:main
    #
    # Would create an entry point called normality that calls normality.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six
    - unidecode

  run:
    - python
    - six
    - unidecode

test:
  # Python imports
  imports:
    - normality

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/pudo/normality
  license: MIT License
  summary: 'Micro-library to normalize text strings'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
