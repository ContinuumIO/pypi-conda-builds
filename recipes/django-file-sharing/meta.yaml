package:
  name: django-file-sharing
  version: "0.2.1"

source:
  fn: django-file-sharing-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-file-sharing/django-file-sharing-0.2.1.tar.gz
  md5: 26e85957565da8717859eebf4a4cb208
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-file-sharing = django-file-sharing:main
    #
    # Would create an entry point called django-file-sharing that calls django-file-sharing.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - file_sharing
    - file_sharing.migrations
    - file_sharing.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/karoon/django-file-sharing
  license: BSD License
  summary: 'file sharing application'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
