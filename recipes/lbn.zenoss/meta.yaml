package:
  name: lbn.zenoss
  version: "4.2.3"

source:
  fn: lbn.zenoss-4.2.3.tar.gz
  url: https://pypi.python.org/packages/source/l/lbn.zenoss/lbn.zenoss-4.2.3.tar.gz
  md5: 24c2f3043fa96b18f2765b0a68700c32
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lbn.zenoss = lbn.zenoss:main
    #
    # Would create an entry point called lbn.zenoss that calls lbn.zenoss.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - lbn
    - lbn.zenoss
    - lbn.zenoss.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://au.last-bastion.net/zenoss
  license: Zope Public License
  summary: 'Base wrappers to save real Zopistas from Zenoss cruft'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
