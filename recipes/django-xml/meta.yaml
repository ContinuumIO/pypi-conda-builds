package:
  name: django-xml
  version: "1.3.1"

source:
  fn: django-xml-1.3.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-xml/django-xml-1.3.1.tar.gz
  md5: bcaf65d6cca60254ec4396bcbf5bffbd
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-xml = django-xml:main
    #
    # Would create an entry point called django-xml that calls django-xml.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - lxml
    - pytz
    - python-dateutil

  run:
    - python
    - setuptools
    - lxml
    - pytz
    - python-dateutil

test:
  # Python imports
  imports:
    - djxml
    - djxml.build
    - djxml.xmlmodels

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/theatlantic/django-xml
  license: UNKNOWN
  summary: "Provides an abstraction to lxml's XPath and XSLT functionality in a manner resembling django database models"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
