package:
  name: collective.contemplate
  version: "0.1"

source:
  fn: collective.contemplate-0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.contemplate/collective.contemplate-0.1.tar.gz
  md5: 2fcdde526d2cd0515f62277a67f2201b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.contemplate = collective.contemplate:main
    #
    # Would create an entry point called collective.contemplate that calls collective.contemplate.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.contemplate

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/collective.contemplate
  license: GPL
  summary: 'Add content from existing content templates'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
