package:
  name: django-staff
  version: "1.1.2"

source:
  fn: django-staff-1.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-staff/django-staff-1.1.2.tar.gz
  md5: c655d0c89f5d91a0fe6f822404a085d5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-staff = django-staff:main
    #
    # Would create an entry point called django-staff that calls django-staff.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-localflavor-us

  run:
    - python
    - django-localflavor-us

test:
  # Python imports
  imports:
    - staff
    - staff.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/callowayproject/django-staff
  license: UNKNOWN
  summary: 'A basic addition to auth.User that manages additional staff info'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
