package:
  name: django-visitor-information-middleware
  version: "0.1.0"

source:
  fn: django-visitor-information-middleware-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-visitor-information-middleware/django-visitor-information-middleware-0.1.0.tar.gz
  md5: cfe8b5b11b3df1c059cd582936e38f26
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-visitor-information-middleware = django-visitor-information-middleware:main
    #
    # Would create an entry point called django-visitor-information-middleware that calls django-visitor-information-middleware.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pygeoip >=0.2.7,<0.3

  run:
    - python
    - pygeoip >=0.2.7,<0.3

test:
  # Python imports
  imports:
    - django_visitor_information

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Kami/django-visitor-information-middleware/
  license: Apache Software License
  summary: 'A collection of Django middleware classes which make writing timezone and location aware applications easier'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
