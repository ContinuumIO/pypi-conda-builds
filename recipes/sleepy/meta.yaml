package:
  name: sleepy
  version: "0.1a1"

source:
  fn: Sleepy-0.1a1.tar.gz
  url: https://pypi.python.org/packages/source/S/Sleepy/Sleepy-0.1a1.tar.gz
  md5: 08cddf07262b24ee14716e5de95e5fbd
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sleepy = sleepy:main
    #
    # Would create an entry point called sleepy that calls sleepy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pylons >=1.0
    - pil
    - pyyaml
    - dbxml
    - gdata
    - lxml
    - mutagen
    - ordereddict
    - python-dateutil

  run:
    - python
    - setuptools
    - pylons >=1.0
    - pil
    - pyyaml
    - dbxml
    - gdata
    - lxml
    - mutagen
    - ordereddict
    - python-dateutil

test:
  # Python imports
  imports:
    - sleepy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT License
  summary: 'A RESTful sort-of-CMS built primarily on Pylons and Berkeley DBXML'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
