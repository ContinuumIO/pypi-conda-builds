package:
  name: py-romanify
  version: "0.1.4"

source:
  fn: py-romanify-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/p/py-romanify/py-romanify-0.1.4.tar.gz
  md5: d66af6291d8971033c68e80ba4c55973
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - py-romanify = py-romanify:main
    #
    # Would create an entry point called py-romanify that calls py-romanify.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - romanify

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/peter-lisak/py-romanify
  license: GNU Lesser General Public License v2 or later (LGPLv2+)
  summary: 'Python Roman/Arabic numbers convertor'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
