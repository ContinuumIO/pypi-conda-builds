package:
  name: djangocms-css-background
  version: "1.0.4"

source:
  fn: djangocms-css-background-1.0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/djangocms-css-background/djangocms-css-background-1.0.4.tar.gz
  md5: 2eed26404fed601816c2dfb712316b82
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangocms-css-background = djangocms-css-background:main
    #
    # Would create an entry point called djangocms-css-background that calls djangocms-css-background.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-cms >=3.0.5
    - djangocms-placeholder-attr >=1.0.2
    - cmsplugin-filer >=0.10

  run:
    - python
    - django-cms >=3.0.5
    - djangocms-placeholder-attr >=1.0.2
    - cmsplugin-filer >=0.10

test:
  # Python imports
  imports:
    - djangocms-css-background

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/WnP/djangocms-css-background
  license: MIT License
  summary: 'A django-cms plugin which allow you to edit css background image or color from the edit mode'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
