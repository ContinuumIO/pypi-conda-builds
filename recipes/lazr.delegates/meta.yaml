package:
  name: lazr.delegates
  version: "2.0.2"

source:
  fn: lazr.delegates-2.0.2.tar.gz
  url: https://pypi.python.org/packages/source/l/lazr.delegates/lazr.delegates-2.0.2.tar.gz
  md5: 67d2f6efacf069701fbd728a0f9fbc92
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lazr.delegates = lazr.delegates:main
    #
    # Would create an entry point called lazr.delegates that calls lazr.delegates.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - nose
    - setuptools
    - zope.interface

  run:
    - python
    - nose
    - setuptools
    - zope.interface

test:
  # Python imports
  imports:
    - lazr
    - lazr.delegates
    - lazr.delegates.docs
    - lazr.delegates.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://launchpad.net/lazr.delegates
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Easily write objects that delegate behavior'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
