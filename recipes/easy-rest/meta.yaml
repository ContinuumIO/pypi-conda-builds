package:
  name: easy-rest
  version: "0.4.1"

source:
  fn: easy-rest-0.4.1.tar.gz
  url: https://pypi.python.org/packages/source/e/easy-rest/easy-rest-0.4.1.tar.gz
  md5: 78906dfe72248a2abe711952dd1f4009
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - easy-rest = easy-rest:main
    #
    # Would create an entry point called easy-rest that calls easy-rest.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - djangorestframework
    - python-dateutil
    - isodate
    - six
    - enum34
    - django-enumfields

  run:
    - python
    - django
    - djangorestframework
    - python-dateutil
    - isodate
    - six
    - enum34
    - django-enumfields

test:
  # Python imports
  imports:
    - easyapi
    - easyapi.tests
    - easyapi.tests.test_project

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mturilin/easyapi
  license: MIT
  summary: 'Real Python Enums for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
