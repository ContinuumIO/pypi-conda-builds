package:
  name: django-smarttest
  version: "0.2.1"

source:
  fn: django-smarttest-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-smarttest/django-smarttest-0.2.1.tar.gz
  md5: 94bdabcdc1c6cf0f2114372340f72184
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-smarttest = django-smarttest:main
    #
    # Would create an entry point called django-smarttest that calls django-smarttest.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - mock

  run:
    - python
    - mock

test:
  # Python imports
  imports:
    - smarttest
    - smarttest.management
    - smarttest.management.commands
    - smarttest.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://kidosoft.pl
  license: BSD License
  summary: 'Code snippets which help writing automated tests for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
