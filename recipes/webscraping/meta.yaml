package:
  name: webscraping
  version: "1.4"

source:
  fn: webscraping-1.4.tar.gz
  url: https://pypi.python.org/packages/source/w/webscraping/webscraping-1.4.tar.gz
  md5: aee5a39760002944af7f2291bf3aba80
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - webscraping = webscraping:main
    #
    # Would create an entry point called webscraping that calls webscraping.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - webscraping

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/webscraping
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Pure python library aimed to make web scraping easier'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
