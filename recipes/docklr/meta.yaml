package:
  name: docklr
  version: "0.0.1"

source:
  fn: Docklr-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/D/Docklr/Docklr-0.0.1.tar.gz
  md5: 89a19f71da566eed931e003c1078a494
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - docklr = docklr:main
    #
    # Would create an entry point called docklr that calls docklr.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.10
    - flask-bootstrap ==3.3.0.1

  run:
    - python
    - flask >=0.10
    - flask-bootstrap ==3.3.0.1

test:
  # Python imports
  imports:
    - docklr

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/drichner/docklr
  license: GPLv2
  summary: 'A web front end for Docker and Fleet.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
