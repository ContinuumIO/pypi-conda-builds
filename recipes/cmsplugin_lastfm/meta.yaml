package:
  name: cmsplugin_lastfm
  version: "0.1.0"

source:
  fn: cmsplugin_lastfm-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/c/cmsplugin_lastfm/cmsplugin_lastfm-0.1.0.tar.gz
  md5: bb4aa266785fa2db81909b2a59a08445
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cmsplugin_lastfm = cmsplugin_lastfm:main
    #
    # Would create an entry point called cmsplugin_lastfm that calls cmsplugin_lastfm.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-cms
    - feedparser

  run:
    - python
    - django-cms
    - feedparser

test:
  # Python imports
  imports:
    - cmsplugin_lastfm
    - cmsplugin_lastfm.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/chrisglass/cmsplugin_lastfm
  license: UNKNOWN
  summary: 'Django CMS Last.fm Plugins'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
