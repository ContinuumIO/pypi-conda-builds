package:
  name: django-languages
  version: "0.1"

source:
  fn: django-languages-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-languages/django-languages-0.1.tar.gz
  md5: ab09a792ddeddc0bbbf2b28af18cd774
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-languages = django-languages:main
    #
    # Would create an entry point called django-languages that calls django-languages.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_languages

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/basaundi/django-languages/
  license: MIT License
  summary: 'Provides a language field for Django models.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
