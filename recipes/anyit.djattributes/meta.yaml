package:
  name: anyit.djattributes
  version: "0.3.1"

source:
  fn: anyit.djattributes-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/a/anyit.djattributes/anyit.djattributes-0.3.1.tar.gz
  md5: 62c4f088c424ede07496745aa85dbaea
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - anyit.djattributes = anyit.djattributes:main
    #
    # Would create an entry point called anyit.djattributes that calls anyit.djattributes.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - anyit
    - anyit.djattributes
    - anyit.djattributes.attributes

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD License
  summary: 'Attributes and a persistend dict type for django models in a JQuery inspired syntax.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
