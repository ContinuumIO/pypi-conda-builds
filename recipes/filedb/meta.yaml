package:
  name: filedb
  version: "0.2.3"

source:
  fn: filedb-0.2.3.tar.gz
  url: https://pypi.python.org/packages/source/f/filedb/filedb-0.2.3.tar.gz
  md5: e4bd04ad06358df26ecab4e64b7aabbe
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - filedb = filedb:main
    #
    # Would create an entry point called filedb that calls filedb.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - docopt ==0.6.1
    - fusepy ==2.0.2
    - pymongo ==2.6.3

  run:
    - python
    - docopt ==0.6.1
    - fusepy ==2.0.2
    - pymongo ==2.6.3

test:
  # Python imports
  imports:
    - filedb

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/yhat/filedb
  license: BSD
  summary: 'Create a file that pretends to be a database query'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
