package:
  name: cubicweb-narval
  version: "4.1.2"

source:
  fn: cubicweb-narval-4.1.2.tar.gz
  url: https://pypi.python.org/packages/source/c/cubicweb-narval/cubicweb-narval-4.1.2.tar.gz
  md5: 505c2d84bb4e4cb4538dbd0d20dc27af
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cubicweb-narval = cubicweb-narval:main
    #
    # Would create an entry point called cubicweb-narval that calls cubicweb-narval.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cubicweb-localperms
    - cubicweb-signedrequest
    - pygments
    - cubicweb >=3.18.0
    - requests >=2.0.0
    - cubicweb-file >=1.14

  run:
    - python
    - cubicweb-localperms
    - cubicweb-signedrequest
    - pygments
    - cubicweb >=3.18.0
    - requests >=2.0.0
    - cubicweb-file >=1.14

test:
  # Python imports
  imports:
    - narvalbot

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.cubicweb.org/project/cubicweb-narval
  license: LGPL
  summary: 'the Narval agent'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
