package:
  name: easygraph
  version: "0.1.6"

source:
  fn: easygraph-0.1.6.tar.gz
  url: https://pypi.python.org/packages/source/e/easygraph/easygraph-0.1.6.tar.gz
  md5: afaba0bb27f7c20a6d9f0a4f997e1ad5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - easygraph = easygraph:main
    #
    # Would create an entry point called easygraph that calls easygraph.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - easygraph

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/JesseAldridge/easy_graph
  license: MIT
  summary: 'Easily turn your python data into a flot graph in a static html file.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
