package:
  name: durable_rules
  version: "0.31.23"

source:
  fn: durable_rules-0.31.23.tar.gz
  url: https://pypi.python.org/packages/source/d/durable_rules/durable_rules-0.31.23.tar.gz
  md5: 6f8fd91fcc46d1c053cd58b4f4c85149
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - durable_rules = durable_rules:main
    #
    # Would create an entry point called durable_rules that calls durable_rules.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - werkzeug

  run:
    - python
    - werkzeug

test:
  # Python imports
  imports:
    - durable
    - durable_rules

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jruizgit/rules
  license: MIT License
  summary: 'for real time analytics'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
