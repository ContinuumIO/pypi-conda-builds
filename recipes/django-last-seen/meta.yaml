package:
  name: django-last-seen
  version: "0.3"

source:
  fn: django-last-seen-0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-last-seen/django-last-seen-0.3.tar.gz
  md5: ab7bbc483520d81331810162f18adcf4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-last-seen = django-last-seen:main
    #
    # Would create an entry point called django-last-seen that calls django-last-seen.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - last_seen
    - last_seen.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/ferranp/django-last-seen
  license: GNU General Public License (GPL)
  summary: 'Keep track of when a user has been last seen'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
