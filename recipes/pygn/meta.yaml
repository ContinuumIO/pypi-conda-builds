package:
  name: pygn
  version: "0.9.14"

source:
  fn: pygn-0.9.14.tar.gz
  url: https://pypi.python.org/packages/source/p/pygn/pygn-0.9.14.tar.gz
  md5: a7656286f723174861f8195ba0e6b87e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pygn = pygn:main
    #
    # Would create an entry point called pygn that calls pygn.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - pygn

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://gitlab.com/mcepl/pyg
  license: GNU General Public License v3 (GPLv3)
  summary: 'The Python Gateway Script: news2mail mail2news gateway'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
