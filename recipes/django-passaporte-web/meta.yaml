package:
  name: django-passaporte-web
  version: "1.2.8"

source:
  fn: django-passaporte-web-1.2.8.tar.gz
  url: https://pypi.python.org/packages/source/d/django-passaporte-web/django-passaporte-web-1.2.8.tar.gz
  md5: a5f6a717fbac45f9d90ca7869299702c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-passaporte-web = django-passaporte-web:main
    #
    # Would create an entry point called django-passaporte-web that calls django-passaporte-web.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django <=1.7
    - requests >=1.2.3,<2.5.0
    - requests-oauthlib >=0.4.1
    - pytz ==2013b
    - vcrpy ==1.1.4
    - python-passaporte-web ==0.2.4
    - futures ==2.1.6

  run:
    - python
    - django <=1.7
    - requests >=1.2.3,<2.5.0
    - requests-oauthlib >=0.4.1
    - pytz ==2013b
    - vcrpy ==1.1.4
    - python-passaporte-web ==0.2.4
    - futures ==2.1.6

test:
  # Python imports
  imports:
    - identity_client

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/myfreecomm/django-passaporte-web
  license: Apache Software License
  summary: 'Django client app for Passaporte Web.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
