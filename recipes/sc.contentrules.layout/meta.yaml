package:
  name: sc.contentrules.layout
  version: "1.0.1"

source:
  fn: sc.contentrules.layout-1.0.1.zip
  url: https://pypi.python.org/packages/source/s/sc.contentrules.layout/sc.contentrules.layout-1.0.1.zip
  md5: eca537d2541c19768dc878bacf86a14c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sc.contentrules.layout = sc.contentrules.layout:main
    #
    # Would create an entry point called sc.contentrules.layout that calls sc.contentrules.layout.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - sc
    - sc.contentrules
    - sc.contentrules.layout
    - sc.contentrules.layout.actions
    - sc.contentrules.layout.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.simplesconsultoria.com.br/
  license: GNU General Public License v2 (GPLv2)
  summary: 'Content Rules Action: Set layout for a content item'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
