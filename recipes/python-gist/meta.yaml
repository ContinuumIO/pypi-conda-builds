package:
  name: python-gist
  version: "0.3.2"

source:
  fn: python-gist-0.3.2.tar.gz
  url: https://pypi.python.org/packages/source/p/python-gist/python-gist-0.3.2.tar.gz
  md5: 47b0b5f32501d0bf0ae4a8a0810a5917
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-gist = python-gist:main
    #
    # Would create an entry point called python-gist that calls python-gist.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - docopt
    - python-gnupg
    - pep8
    - requests
    - responses
    - simplejson
    - tox

  run:
    - python
    - docopt
    - python-gnupg
    - pep8
    - requests
    - responses
    - simplejson
    - tox

test:
  # Python imports
  imports:
    - gist

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/jdowner/gist
  license: MIT License
  summary: 'Manage github gists'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
