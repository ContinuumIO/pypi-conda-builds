package:
  name: python-transifex
  version: "0.1.7"

source:
  fn: python-transifex-0.1.7.tar.gz
  url: https://pypi.python.org/packages/source/p/python-transifex/python-transifex-0.1.7.tar.gz
  md5: d6e01aa30d43a7f5738a31c505318363
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-transifex = python-transifex:main
    #
    # Would create an entry point called python-transifex that calls python-transifex.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - transifex
    - transifex.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jakul/python-transifex
  license: BSD License
  summary: 'A python api to transifex'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
