package:
  name: simplelayout.portlet.dropzone
  version: "1.2.2"

source:
  fn: simplelayout.portlet.dropzone-1.2.2.zip
  url: https://pypi.python.org/packages/source/s/simplelayout.portlet.dropzone/simplelayout.portlet.dropzone-1.2.2.zip
  md5: 8c1c2d05076ed14bc1efe3af4a89fdea
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - simplelayout.portlet.dropzone = simplelayout.portlet.dropzone:main
    #
    # Would create an entry point called simplelayout.portlet.dropzone that calls simplelayout.portlet.dropzone.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.portlets
    - simplelayout.base

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.portlets
    - simplelayout.base

test:
  # Python imports
  imports:
    - simplelayout
    - simplelayout.portlet
    - simplelayout.portlet.dropzone
    - simplelayout.portlet.dropzone.browser
    - simplelayout.portlet.dropzone.configlet
    - simplelayout.portlet.dropzone.portlets
    - simplelayout.portlet.dropzone.viewlets

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/4teamwork/simplelayout.portlet.dropzone
  license: GPL2
  summary: 'Plone portlet with a simplelayout dropzone.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
