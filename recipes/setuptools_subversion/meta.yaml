package:
  name: setuptools_subversion
  version: "3.1"

source:
  fn: setuptools_subversion-3.1.zip
  url: https://pypi.python.org/packages/source/s/setuptools_subversion/setuptools_subversion-3.1.zip
  md5: 34a913308de323a9d380407d1287fff2
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - setuptools_subversion = setuptools_subversion:main
    #
    # Would create an entry point called setuptools_subversion that calls setuptools_subversion.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - setuptools_subversion

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/setuptools_subversion
  license: BSD License
  summary: 'Setuptools revision control system plugin for Subversion'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
