package:
  name: collective.dms.basecontent
  version: "0.4"

source:
  fn: collective.dms.basecontent-0.4.zip
  url: https://pypi.python.org/packages/source/c/collective.dms.basecontent/collective.dms.basecontent-0.4.zip
  md5: e281d9aeb69799f2eb8b9c02f4f51303
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.dms.basecontent = collective.dms.basecontent:main
    #
    # Would create an entry point called collective.dms.basecontent that calls collective.dms.basecontent.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - five.grok
    - collective.documentviewer
    - plone.api
    - plone.app.dexterity
    - plone.namedfile
    - z3c.blobfile
    - plone.app.contenttypes
    - plone.app.relationfield
    - plone.formwidget.contenttree
    - plone.principalsource
    - collective.z3cform.chosen
    - collective.z3cform.rolefield
    - z3c.table

  run:
    - python
    - setuptools
    - setuptools
    - five.grok
    - collective.documentviewer
    - plone.api
    - plone.app.dexterity
    - plone.namedfile
    - z3c.blobfile
    - plone.app.contenttypes
    - plone.app.relationfield
    - plone.formwidget.contenttree
    - plone.principalsource
    - collective.z3cform.chosen
    - collective.z3cform.rolefield
    - z3c.table

test:
  # Python imports
  imports:
    - collective
    - collective.dms
    - collective.dms.basecontent
    - collective.dms.basecontent.browser
    - collective.dms.basecontent.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.dms.basecontent
  license: gpl
  summary: 'Base content types for document management system'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
