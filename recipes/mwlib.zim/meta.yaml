package:
  name: mwlib.zim
  version: "0.3.0"

source:
  fn: mwlib.zim-0.3.0.zip
  url: https://pypi.python.org/packages/source/m/mwlib.zim/mwlib.zim-0.3.0.zip
  md5: 03470c4ce74c4b8639cf64d515c7755c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mwlib.zim = mwlib.zim:main
    #
    # Would create an entry point called mwlib.zim that calls mwlib.zim.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - mwlib
    - pyzim >=0.3
    - lxml

  run:
    - python
    - setuptools
    - mwlib
    - pyzim >=0.3
    - lxml

test:
  # Python imports
  imports:
    - mwlib
    - mwlib.zim

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.pediapress.com/
  license: BSD License
  summary: 'generate zim files from mediawiki markup'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
