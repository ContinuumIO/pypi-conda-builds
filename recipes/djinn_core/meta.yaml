package:
  name: djinn_core
  version: "1.2.1"

source:
  fn: djinn_core-1.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/djinn_core/djinn_core-1.2.1.tar.gz
  md5: 2438dcc2d6b69a4bcdddf4fe00f1a2e1
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djinn_core = djinn_core:main
    #
    # Would create an entry point called djinn_core that calls djinn_core.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - setuptools
    - django

test:
  # Python imports
  imports:
    - djinn_core
    - djinn_core.templatetags
    - djinn_core.views

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/PythonUnited/djinn_core
  license: beer-ware
  summary: 'Djinn Intranet core'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
