package:
  name: django-menus
  version: "1.1.2"

source:
  fn: django-menus-1.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-menus/django-menus-1.1.2.tar.gz
  md5: ceaa3eb02216886b23fd14a38dd493e5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-menus = django-menus:main
    #
    # Would create an entry point called django-menus that calls django-menus.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - menus
    - menus.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/schinckel/django-menus/
  license: BSD License
  summary: 'Menu helpers for django projects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
