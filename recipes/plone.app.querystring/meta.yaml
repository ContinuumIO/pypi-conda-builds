package:
  name: plone.app.querystring
  version: "1.3.2"

source:
  fn: plone.app.querystring-1.3.2.tar.gz
  url: https://pypi.python.org/packages/source/p/plone.app.querystring/plone.app.querystring-1.3.2.tar.gz
  md5: 087c213bfe976f054b12e17b7e32effc
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.app.querystring = plone.app.querystring:main
    #
    # Would create an entry point called plone.app.querystring that calls plone.app.querystring.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - datetime
    - products.cmfcore
    - products.cmfplone
    - plone.app.contentlisting
    - plone.app.layout
    - plone.app.registry >=1.1dev
    - plone.app.vocabularies
    - plone.batching
    - plone.registry
    - setuptools
    - zope.component
    - zope.dottedname
    - zope.globalrequest
    - zope.i18n
    - zope.i18nmessageid
    - zope.interface
    - zope.publisher
    - zope.schema

  run:
    - python
    - setuptools
    - datetime
    - products.cmfcore
    - products.cmfplone
    - plone.app.contentlisting
    - plone.app.layout
    - plone.app.registry >=1.1dev
    - plone.app.vocabularies
    - plone.batching
    - plone.registry
    - setuptools
    - zope.component
    - zope.dottedname
    - zope.globalrequest
    - zope.i18n
    - zope.i18nmessageid
    - zope.interface
    - zope.publisher
    - zope.schema

test:
  # Python imports
  imports:
    - plone
    - plone.app
    - plone.app.querystring
    - plone.app.querystring.indexmodifiers
    - plone.app.querystring.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/plone.app.querystring
  license: GNU General Public License (GPL)
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
