package:
  name: flask-user
  version: "0.6.1"

source:
  fn: Flask-User-0.6.1.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-User/Flask-User-0.6.1.tar.gz
  md5: 342b4f48cea361ffbc4f2137b1a8aaca
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-user = flask-user:main
    #
    # Would create an entry point called flask-user that calls flask-user.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - passlib
    - py-bcrypt
    - pycrypto
    - flask
    - flask-login
    - flask-mail
    - flask-sqlalchemy
    - flask-wtf

  run:
    - python
    - passlib
    - py-bcrypt
    - pycrypto
    - flask
    - flask-login
    - flask-mail
    - flask-sqlalchemy
    - flask-wtf

test:
  # Python imports
  imports:
    - flask_user

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/lingthio/Flask-User
  license: BSD License
  summary: 'Customizable User Account Management for Flask: Register, Confirm email, Login, Change username, Change password, Forgot password and more.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
