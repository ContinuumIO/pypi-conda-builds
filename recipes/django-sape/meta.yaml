package:
  name: django-sape
  version: "0.2.4"

source:
  fn: django-sape-0.2.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-sape/django-sape-0.2.4.tar.gz
  md5: 780afea8b9125ba85d0047c74236ba5f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-sape = django-sape:main
    #
    # Would create an entry point called django-sape that calls django-sape.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - sape
    - sape.management
    - sape.management.commands
    - sape.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/fedosov/sape
  license: Python Software Foundation License
  summary: 'Django + sape.ru.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
