package:
  name: django-qsstats-magic
  version: "0.7.2"

source:
  fn: django-qsstats-magic-0.7.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-qsstats-magic/django-qsstats-magic-0.7.2.tar.gz
  md5: 3f7d642d58ae5020f18317ea7b6aa054
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-qsstats-magic = django-qsstats-magic:main
    #
    # Would create an entry point called django-qsstats-magic that calls django-qsstats-magic.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - qsstats

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/kmike/django-qsstats-magic/
  license: MIT License
  summary: 'A django microframework that eases the generation of aggregate data for querysets.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
