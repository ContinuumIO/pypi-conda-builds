package:
  name: seznam-captcha
  version: "1.2.1"

source:
  fn: seznam-captcha-1.2.1.tar.gz
  url: https://pypi.python.org/packages/source/s/seznam-captcha/seznam-captcha-1.2.1.tar.gz
  md5: 6c033f74ffd8c714f6ed0e152ab7e7c5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - seznam-captcha = seznam-captcha:main
    #
    # Would create an entry point called seznam-captcha that calls seznam-captcha.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/sikaondrej/seznam-captcha/
  license: MIT Licence
  summary: 'Wraper for Seznam Captcha'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
