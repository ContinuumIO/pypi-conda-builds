package:
  name: mongoengine-multilingual-field
  version: "0.4-simple"

source:
  fn: mongoengine-multilingual-field-0.4-simple.tar.gz
  url: https://pypi.python.org/packages/source/m/mongoengine-multilingual-field/mongoengine-multilingual-field-0.4-simple.tar.gz
  md5: 18384c52e8b1b4df74d37a3fffd077d1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mongoengine-multilingual-field = mongoengine-multilingual-field:main
    #
    # Would create an entry point called mongoengine-multilingual-field that calls mongoengine-multilingual-field.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - mongoengine

  run:
    - python
    - mongoengine

test:
  # Python imports
  imports:
    - multilingual_field

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lig/mongoengine-multilingual-field
  license: BSD License
  summary: 'MongoEngine field to store MultilingualString class from i18n-string project'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
