package:
  name: alkey
  version: "0.6"

source:
  fn: alkey-0.6.tar.gz
  url: https://pypi.python.org/packages/source/a/alkey/alkey-0.6.tar.gz
  md5: 3800d608b18801578968f6b86d7f2a7f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - alkey = alkey:main
    #
    # Would create an entry point called alkey that calls alkey.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.component
    - zope.interface
    - sqlalchemy
    - redis
    - pyramid_redis

  run:
    - python
    - zope.component
    - zope.interface
    - sqlalchemy
    - redis
    - pyramid_redis

test:
  # Python imports
  imports:
    - alkey
    - alkey.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/thruflo/alkey
  license: This is free and unencumbered software released into the public domain.
  summary: 'Redis backed tool for generating cache keys.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
