package:
  name: datetimes
  version: "1.0.0"

source:
  fn: datetimes-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/d/datetimes/datetimes-1.0.0.tar.gz
  md5: 145d3fe35ca6d1bd6dcb7a05c9f9d33a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - datetimes = datetimes:main
    #
    # Would create an entry point called datetimes that calls datetimes.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - datetimes

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/crohaco/datetimes
  license: BSD License
  summary: 'generate datetime object(s), regular or randomly.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
