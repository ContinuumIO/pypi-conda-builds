package:
  name: minsol
  version: "0.3"

source:
  fn: minsol-0.3.tar.gz
  url: https://pypi.python.org/packages/source/m/minsol/minsol-0.3.tar.gz
  md5: ab714aa19054a0bc5afef52ae0350203
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - minsol = minsol:main
    #
    # Would create an entry point called minsol that calls minsol.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyyaml
    - tornado
    - logging

  run:
    - python
    - pyyaml
    - tornado
    - logging

test:
  # Python imports
  imports:
    - minsol

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pythonhosted.org/minsol/
  license: BSD License
  summary: '\nService enviroment, reload std constructor\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
