package:
  name: sc.contentrules.groupbydate
  version: "2.0.1"

source:
  fn: sc.contentrules.groupbydate-2.0.1.zip
  url: https://pypi.python.org/packages/source/s/sc.contentrules.groupbydate/sc.contentrules.groupbydate-2.0.1.zip
  md5: a90ac272b80f85597aafbce0440976d2
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sc.contentrules.groupbydate = sc.contentrules.groupbydate:main
    #
    # Would create an entry point called sc.contentrules.groupbydate that calls sc.contentrules.groupbydate.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - sc
    - sc.contentrules
    - sc.contentrules.groupbydate
    - sc.contentrules.groupbydate.actions
    - sc.contentrules.groupbydate.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.simplesconsultoria.com.br/
  license: GNU General Public License v2 (GPLv2)
  summary: u'A Plone content rules action that creates a chronological archive.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
