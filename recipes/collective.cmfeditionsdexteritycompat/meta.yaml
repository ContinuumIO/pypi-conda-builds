package:
  name: collective.cmfeditionsdexteritycompat
  version: "0.1b8"

source:
  fn: collective.cmfeditionsdexteritycompat-0.1b8.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.cmfeditionsdexteritycompat/collective.cmfeditionsdexteritycompat-0.1b8.tar.gz
  md5: c24e2097e0ca6255f0ffca3efb8c9fb8
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.cmfeditionsdexteritycompat = collective.cmfeditionsdexteritycompat:main
    #
    # Would create an entry point called collective.cmfeditionsdexteritycompat that calls collective.cmfeditionsdexteritycompat.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - five.grok
    - z3c.autoinclude

  run:
    - python
    - setuptools
    - setuptools
    - five.grok
    - z3c.autoinclude

test:
  # Python imports
  imports:
    - collective
    - collective.cmfeditionsdexteritycompat
    - collective.cmfeditionsdexteritycompat.browser
    - collective.cmfeditionsdexteritycompat.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://
  license: ''
  summary: 'Makes `Products.CMFEditions`_ works with `Dexterity` content types.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
