package:
  name: django-oscar-paypal
  version: "0.9.5"

source:
  fn: django-oscar-paypal-0.9.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-oscar-paypal/django-oscar-paypal-0.9.5.tar.gz
  md5: 69ae88bc9165dc1732719a19422596df
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-oscar-paypal = django-oscar-paypal:main
    #
    # Would create an entry point called django-oscar-paypal that calls django-oscar-paypal.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=1.0
    - django-localflavor

  run:
    - python
    - requests >=1.0
    - django-localflavor

test:
  # Python imports
  imports:
    - paypal
    - paypal.express
    - paypal.express.dashboard
    - paypal.payflow
    - paypal.payflow.dashboard

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tangentlabs/django-oscar-paypal
  license: BSD License
  summary: 'Integration with PayPal Express, PayPal Payflow Pro and Adaptive Payments for django-oscar'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
