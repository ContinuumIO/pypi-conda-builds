package:
  name: django-toolbelt
  version: "0.0.1"

source:
  fn: django-toolbelt-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-toolbelt/django-toolbelt-0.0.1.tar.gz
  md5: d7961695c78448fd56d9c220f3bd8349
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-toolbelt = django-toolbelt:main
    #
    # Would create an entry point called django-toolbelt that calls django-toolbelt.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - psycopg2
    - gunicorn
    - dj-database-url
    - dj-static

  run:
    - python
    - django
    - psycopg2
    - gunicorn
    - dj-database-url
    - dj-static

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://devcenter.heroku.com/articles/django
  license: UNKNOWN
  summary: 'A simple collection of common Django utilities.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
