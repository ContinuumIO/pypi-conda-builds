package:
  name: django-maven
  version: "0.3"

source:
  fn: django-maven-0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-maven/django-maven-0.3.tar.gz
  md5: 5b06bcc67273d3b5fcc5240c817284e0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-maven = django-maven:main
    #
    # Would create an entry point called django-maven that calls django-maven.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - django_maven
    - django_maven.management
    - django_maven.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/saippuakauppias/django-maven
  license: ISC License (ISCL)
  summary: 'Capture exceptions in django management commands into Sentry by Raven'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
