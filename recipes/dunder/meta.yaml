package:
  name: dunder
  version: "0.1.1"

source:
  fn: dunder-0.1.1.zip
  url: https://pypi.python.org/packages/source/d/dunder/dunder-0.1.1.zip
  md5: 70be8378acbcdec8b23ba1114a5181e3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dunder = dunder:main
    #
    # Would create an entry point called dunder that calls dunder.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - dunder

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://launchpad.net/dunder.py
  license: Apache Software License
  summary: 'Extract dunder variables from a Python source file.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
