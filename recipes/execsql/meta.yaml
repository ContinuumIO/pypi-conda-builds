package:
  name: execsql
  version: "0.4.4.0"

source:
  fn: execsql-0.4.4.0.zip
  url: https://pypi.python.org/packages/source/e/execsql/execsql-0.4.4.0.zip
  md5: cd88087d880e36c954cbe4b168886db3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - execsql = execsql:main
    #
    # Would create an entry point called execsql that calls execsql.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - execsql

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: none
  license: GNU General Public License (GPL)
  summary: 'Run a SQL script against a Microsoft Access database and capture output.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
