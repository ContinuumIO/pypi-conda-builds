package:
  name: django-widget_list
  version: "0.1.2"

source:
  fn: django-widget_list-0.1.2.zip
  url: https://pypi.python.org/packages/source/d/django-widget_list/django-widget_list-0.1.2.zip
  md5: 95df8c7b998395761f38bb6f1b6fd6c0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-widget_list = django-widget_list:main
    #
    # Would create an entry point called django-widget_list that calls django-widget_list.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - widget_list

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/davidrenne/widget_list
  license: BSD License
  summary: 'An Advanced and flexible ajax data grid'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
