package:
  name: surgeo
  version: "0.7.0"

source:
  fn: surgeo-0.7.0.tar.gz
  url: https://pypi.python.org/packages/source/s/surgeo/surgeo-0.7.0.tar.gz
  md5: fc0df84361534f28c3f6be2c60d407ac
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - surgeo = surgeo:main
    #
    # Would create an entry point called surgeo that calls surgeo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - surgeo
    - surgeo.db
    - surgeo.experimental
    - surgeo.model
    - surgeo.utilities

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/theonaun/surgeo
  license: MIT License
  summary: 'Disparate impact testing and surname geocoding analysis'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
