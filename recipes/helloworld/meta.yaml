package:
  name: helloworld
  version: "0.1dev"

source:
  fn: helloworld-0.1dev.zip
  url: https://pypi.python.org/packages/source/h/helloworld/helloworld-0.1dev.zip
  md5: ad53016d4782b52bdae0942c2f407e51
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - helloworld = helloworld:main
    #
    # Would create an entry point called helloworld that calls helloworld.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pylons >=1.0

  run:
    - python
    - setuptools
    - pylons >=1.0

test:
  # Python imports
  imports:
    - helloworld
    - helloworld.config
    - helloworld.controllers
    - helloworld.lib
    - helloworld.model
    - helloworld.tests
    - helloworld.tests.functional

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: UNKNOWN
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
