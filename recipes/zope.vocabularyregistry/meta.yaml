package:
  name: zope.vocabularyregistry
  version: "1.0.0"

source:
  fn: zope.vocabularyregistry-1.0.0.zip
  url: https://pypi.python.org/packages/source/z/zope.vocabularyregistry/zope.vocabularyregistry-1.0.0.zip
  md5: d46f656d46d38de0694dd810670617a5
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zope.vocabularyregistry = zope.vocabularyregistry:main
    #
    # Would create an entry point called zope.vocabularyregistry that calls zope.vocabularyregistry.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.component
    - zope.interface
    - zope.schema

  run:
    - python
    - setuptools
    - zope.component
    - zope.interface
    - zope.schema

test:
  # Python imports
  imports:
    - zope
    - zope.vocabularyregistry

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/zope.apidoc
  license: Zope Public License
  summary: 'Utility-based Vocabulary Registry'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
