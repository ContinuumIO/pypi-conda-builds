package:
  name: badgekit-api-client
  version: "0.5.1"

source:
  fn: badgekit-api-client-0.5.1.tar.gz
  url: https://pypi.python.org/packages/source/b/badgekit-api-client/badgekit-api-client-0.5.1.tar.gz
  md5: 6fd34d53211aa8ac6cd58b1bc6934be5
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - badgekit-api-client = badgekit-api-client:main
    #
    # Would create an entry point called badgekit-api-client that calls badgekit-api-client.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - requests
    - requests-jwt >=0.3
    - setuptools

  run:
    - python
    - requests
    - requests-jwt >=0.3
    - setuptools

test:
  # Python imports
  imports:
    - badgekit
    - test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT License
  summary: "Python client for Mozilla's BadgeKit API"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
