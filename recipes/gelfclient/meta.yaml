package:
  name: gelfclient
  version: "0.0.3"

source:
  fn: gelfclient-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/g/gelfclient/gelfclient-0.0.3.tar.gz
  md5: 3b57d33c0433bd221971f470ccf41378
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gelfclient = gelfclient:main
    #
    # Would create an entry point called gelfclient that calls gelfclient.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - gelfclient

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/orionvm/python-gelfclient
  license: Apache v2
  summary: 'A UDP client for sending message in the Graylog Extended Log Format (GELF)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
