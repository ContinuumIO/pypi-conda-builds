package:
  name: django-multiupload
  version: "0.3"

source:
  fn: django-multiupload-0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-multiupload/django-multiupload-0.3.tar.gz
  md5: 410d19bbe9b504b4cbd1facd40f32be8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-multiupload = django-multiupload:main
    #
    # Would create an entry point called django-multiupload that calls django-multiupload.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - multiupload

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Chive/django-multiupload
  license: BSD License
  summary: "Dead simple drop-in multi file upload field for django forms using HTML5's multiple attribute."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
