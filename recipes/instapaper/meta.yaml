package:
  name: instapaper
  version: "0.2"

source:
  fn: instapaper-0.2.tar.gz
  url: https://pypi.python.org/packages/source/i/instapaper/instapaper-0.2.tar.gz
  md5: 89cfefaccdac72889adf911eca34970a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - instapaper = instapaper:main
    #
    # Would create an entry point called instapaper that calls instapaper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - instapaper

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/rsgalloway/instapaper
  license: UNKNOWN
  summary: 'Unofficial Instapaper Python API.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
