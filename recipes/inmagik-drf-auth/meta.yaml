package:
  name: inmagik-drf-auth
  version: "0.1.1"

source:
  fn: inmagik-drf-auth-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/i/inmagik-drf-auth/inmagik-drf-auth-0.1.1.tar.gz
  md5: 78f7445b31514def78ae17efdbc709c1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - inmagik-drf-auth = inmagik-drf-auth:main
    #
    # Would create an entry point called inmagik-drf-auth that calls inmagik-drf-auth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - djangorestframework

  run:
    - python
    - djangorestframework

test:
  # Python imports
  imports:
    - inmagik_drf_auth

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/INMAGIK/inmagik-drf-auth/
  license: BSD License
  summary: 'Additional views and helpers for django-rest-framework authentication'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
