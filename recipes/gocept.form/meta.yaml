package:
  name: gocept.form
  version: "0.7.6"

source:
  fn: gocept.form-0.7.6.tar.gz
  url: https://pypi.python.org/packages/source/g/gocept.form/gocept.form-0.7.6.tar.gz
  md5: 9ed7ac46d18a5dfdc7ab0898bc2da20a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gocept.form = gocept.form:main
    #
    # Would create an entry point called gocept.form that calls gocept.form.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - gocept.mochikit >=1.3.1
    - zope.interface
    - zope.component
    - zope.contentprovider
    - zope.viewlet
    - zc.resourcelibrary

  run:
    - python
    - setuptools
    - gocept.mochikit >=1.3.1
    - zope.interface
    - zope.component
    - zope.contentprovider
    - zope.viewlet
    - zc.resourcelibrary

test:
  # Python imports
  imports:
    - gocept
    - gocept.form

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/gocept.form
  license: ZPL 2.1
  summary: 'Extensions for zope.formlib'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
