package:
  name: cloudelements
  version: "0.2.1"

source:
  fn: cloudelements-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/c/cloudelements/cloudelements-0.2.1.tar.gz
  md5: 8318fc7ef9ed4fb0250db5f401d5fefb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cloudelements = cloudelements:main
    #
    # Would create an entry point called cloudelements that calls cloudelements.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests
    - validictory

  run:
    - python
    - requests
    - validictory

test:
  # Python imports
  imports:
    - cloudelements
    - cloudelements.schemas
    - tests.functional
    - tests.unit

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://leadgenius.github.com/cloudelements
  license: MIT
  summary: 'SDK for Cloud Elements'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
