package:
  name: plone.app.transmogrifier
  version: "1.3"

source:
  fn: plone.app.transmogrifier-1.3.zip
  url: https://pypi.python.org/packages/source/p/plone.app.transmogrifier/plone.app.transmogrifier-1.3.zip
  md5: 8b365ca30622c3d0e070238e49b5a5a8
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.app.transmogrifier = plone.app.transmogrifier:main
    #
    # Would create an entry point called plone.app.transmogrifier that calls plone.app.transmogrifier.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.transmogrifier >=1.1

  run:
    - python
    - setuptools
    - setuptools
    - collective.transmogrifier >=1.1

test:
  # Python imports
  imports:
    - plone
    - plone.app
    - plone.app.transmogrifier

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/plone.app.transmogrifier
  license: GNU General Public License (GPL)
  summary: 'Plone blueprints for collective.transmogrifier pipelines'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
