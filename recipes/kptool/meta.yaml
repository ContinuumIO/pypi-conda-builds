package:
  name: kptool
  version: "0.1dev"

source:
  fn: kptool-0.1dev.tar.gz
  url: https://pypi.python.org/packages/source/k/kptool/kptool-0.1dev.tar.gz
  md5: 5b439d0e5dacbed3191ee79dc80f96b1
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kptool = kptool:main
    #
    # Would create an entry point called kptool that calls kptool.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - argparse
    - pycrypto

  run:
    - python
    - setuptools
    - argparse
    - pycrypto

test:
  # Python imports
  imports:
    - kptool
    - kptool.keepassdb

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: GNU General Public License (GPL)
  summary: 'Keepass v1 DB tool'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
