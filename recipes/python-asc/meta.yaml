package:
  name: python-asc
  version: "0.0.1"

source:
  fn: python-asc-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/p/python-asc/python-asc-0.0.1.tar.gz
  md5: fb53235d660791bfcded5dceddf07ad8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-asc = python-asc:main
    #
    # Would create an entry point called python-asc that calls python-asc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - asc
    - asc.contrib
    - asc.contrib.music

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/praekelt/python-asc
  license: BSD License
  summary: 'Simple Python library parsing ASC EPG feeds.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
