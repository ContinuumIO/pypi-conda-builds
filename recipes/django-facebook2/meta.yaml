package:
  name: django-facebook2
  version: "0.3.1"

source:
  fn: django-facebook2-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-facebook2/django-facebook2-0.3.1.tar.gz
  md5: 8e97ac8e87dc5ce916352363291af839
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-facebook2 = django-facebook2:main
    #
    # Would create an entry point called django-facebook2 that calls django-facebook2.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5
    - facebook2 >=2.2.1

  run:
    - python
    - django >=1.5
    - facebook2 >=2.2.1

test:
  # Python imports
  imports:
    - django_facebook
    - django_facebook.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/tino/django-facebook2
  license: MIT License
  summary: 'Facebook Authentication for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
