package:
  name: pelican-commonmark
  version: "0.1.0"

source:
  fn: pelican-commonmark-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/p/pelican-commonmark/pelican-commonmark-0.1.0.tar.gz
  md5: 87723e398a937d12764f37a9f5502b6d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pelican-commonmark = pelican-commonmark:main
    #
    # Would create an entry point called pelican-commonmark that calls pelican-commonmark.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pelican >=3.5.0
    - commonmark >=0.5.4

  run:
    - python
    - pelican >=3.5.0
    - commonmark >=0.5.4

test:
  # Python imports
  imports:
    - pelican_commonmark

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/theskumar/pelican-commonmark
  license: BSD License
  summary: u'This reader plugin replaces the markdown reader with one that uses the Python parser for CommonMark'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
