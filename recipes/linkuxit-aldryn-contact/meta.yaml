package:
  name: linkuxit-aldryn-contact
  version: "0.9"

source:
  fn: linkuxit-aldryn-contact-0.9.tar.gz
  url: https://pypi.python.org/packages/source/l/linkuxit-aldryn-contact/linkuxit-aldryn-contact-0.9.tar.gz
  md5: 018e39daa25045cf0237625d42b235c5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - linkuxit-aldryn-contact = linkuxit-aldryn-contact:main
    #
    # Would create an entry point called linkuxit-aldryn-contact that calls linkuxit-aldryn-contact.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - djrill

  run:
    - python
    - djrill

test:
  # Python imports
  imports:
    - linkuxit
    - linkuxit.contact
    - linkuxit.contact.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD License
  summary: 'Contact form for aldryn'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
