package:
  name: djangoopeninviter
  version: "1.0"

source:
  fn: DjangoOpenInviter-1.0.zip
  url: https://pypi.python.org/packages/source/D/DjangoOpenInviter/DjangoOpenInviter-1.0.zip
  md5: eb19de3ab6b3dc03b545ede0699e79b4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangoopeninviter = djangoopeninviter:main
    #
    # Would create an entry point called djangoopeninviter that calls djangoopeninviter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - django_open_inviter
    - djangoopeninviter

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/tschellenbach/DjangoOpenInviter
  license: BSD License
  summary: 'Python implementation of the client to openinviter.com - the leading online contact importing solution'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
