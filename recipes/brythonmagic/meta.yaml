package:
  name: brythonmagic
  version: "0.1.1"

source:
  fn: brythonmagic-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/b/brythonmagic/brythonmagic-0.1.1.tar.gz
  md5: a85d37a11b08357e96ae54bc4be7febb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - brythonmagic = brythonmagic:main
    #
    # Would create an entry point called brythonmagic that calls brythonmagic.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - ipython >=1.0

  run:
    - python
    - ipython >=1.0

test:
  # Python imports
  imports:
    - brythonmagic

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kikocorreoso/brythonmagic
  license: MIT License
  summary: 'Magics to use brython in IPython notebook.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
