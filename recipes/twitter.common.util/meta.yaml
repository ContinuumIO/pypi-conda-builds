package:
  name: twitter.common.util
  version: "0.3.3"

source:
  fn: twitter.common.util-0.3.3.tar.gz
  url: https://pypi.python.org/packages/source/t/twitter.common.util/twitter.common.util-0.3.3.tar.gz
  md5: 6e3dac3193d5fb9befd3c2316cbaa4bc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - twitter.common.util = twitter.common.util:main
    #
    # Would create an entry point called twitter.common.util that calls twitter.common.util.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - twitter.common.contextutil ==0.3.3

  run:
    - python
    - twitter.common.contextutil ==0.3.3

test:
  # Python imports
  imports:
    - twitter
    - twitter.common
    - twitter.common.util

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/twitter/commons
  license: Apache Software License
  summary: 'twitter.common commands wrapper.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
