package:
  name: django-db-log
  version: "2.2.1"

source:
  fn: django-db-log-2.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-db-log/django-db-log-2.2.1.tar.gz
  md5: 83c213ad9a4b82e04e39883664774635
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-db-log = django-db-log:main
    #
    # Would create an entry point called django-db-log that calls django-db-log.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - djangodblog
    - djangodblog.management
    - djangodblog.management.commands
    - djangodblog.migrations
    - djangodblog.templatetags
    - djangodblog.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/dcramer/django-db-log
  license: UNKNOWN
  summary: 'Exception Logging to a Database in Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
