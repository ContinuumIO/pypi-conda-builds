package:
  name: django-active-tab
  version: "0.1.1"

source:
  fn: django-active-tab-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-active-tab/django-active-tab-0.1.1.tar.gz
  md5: f6b9fd5eb1b10a0e0132df82029d546f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-active-tab = django-active-tab:main
    #
    # Would create an entry point called django-active-tab that calls django-active-tab.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django

  run:
    - python
    - django

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jarvys/django-active-tab
  license: UNKNOWN
  summary: 'nav decorator for django views'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
