package:
  name: ftw.lawgiver
  version: "1.5.0"

source:
  fn: ftw.lawgiver-1.5.0.zip
  url: https://pypi.python.org/packages/source/f/ftw.lawgiver/ftw.lawgiver-1.5.0.zip
  md5: 53d1bda75e17aed8a385bc6df07123fc
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ftw.lawgiver = ftw.lawgiver:main
    #
    # Would create an entry point called ftw.lawgiver that calls ftw.lawgiver.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - accesscontrol
    - acquisition
    - products.cmfcore
    - products.cmfplone
    - products.genericsetup
    - products.statusmessages
    - zodb3
    - zope2
    - argparse
    - collective.deletepermission
    - ftw.upgrade
    - i18ndude
    - lxml
    - ordereddict
    - plone.app.workflow
    - plone.i18n
    - setuptools
    - zope.component
    - zope.configuration
    - zope.i18n
    - zope.i18nmessageid
    - zope.interface
    - zope.publisher
    - zope.schema

  run:
    - python
    - setuptools
    - accesscontrol
    - acquisition
    - products.cmfcore
    - products.cmfplone
    - products.genericsetup
    - products.statusmessages
    - zodb3
    - zope2
    - argparse
    - collective.deletepermission
    - ftw.upgrade
    - i18ndude
    - lxml
    - ordereddict
    - plone.app.workflow
    - plone.i18n
    - setuptools
    - zope.component
    - zope.configuration
    - zope.i18n
    - zope.i18nmessageid
    - zope.interface
    - zope.publisher
    - zope.schema

test:
  # Python imports
  imports:
    - ftw
    - ftw.lawgiver
    - ftw.lawgiver.browser
    - ftw.lawgiver.tests
    - ftw.lawgiver.tests.pages
    - ftw.lawgiver.upgrades
    - ftw.lawgiver.wdl
    - ftw.lawgiver.wdl.languages

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/4teamwork/ftw.lawgiver
  license: GPL2
  summary: 'Generate your Plone workflows by describing it in plain text with a DSL.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
