package:
  name: fleming
  version: "0.4.3"

source:
  fn: fleming-0.4.3.tar.gz
  url: https://pypi.python.org/packages/source/f/fleming/fleming-0.4.3.tar.gz
  md5: ee8d473bcd2bf77da8a6c3487fd5d88a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fleming = fleming:main
    #
    # Would create an entry point called fleming that calls fleming.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pytz >=2013.9
    - python-dateutil >=2.2

  run:
    - python
    - pytz >=2013.9
    - python-dateutil >=2.2

test:
  # Python imports
  imports:
    - fleming

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ambitioninc/fleming
  license: MIT License
  summary: 'Python helpers for manipulating datetime objects relative to time zones'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
