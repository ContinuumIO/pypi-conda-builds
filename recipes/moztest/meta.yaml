package:
  name: moztest
  version: "0.7"

source:
  fn: moztest-0.7.tar.gz
  url: https://pypi.python.org/packages/source/m/moztest/moztest-0.7.tar.gz
  md5: db7433f4a2d7472ba735c17d4c4391e6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - moztest = moztest:main
    #
    # Would create an entry point called moztest that calls moztest.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - mozinfo

  run:
    - python
    - mozinfo

test:
  # Python imports
  imports:
    - moztest
    - moztest.adapters
    - moztest.output

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://wiki.mozilla.org/Auto-tools/Projects/Mozbase
  license: MPL
  summary: 'Package for storing and outputting Mozilla test results'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
