package:
  name: tentacles
  version: "0.1.1"

source:
  fn: tentacles-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/t/tentacles/tentacles-0.1.1.tar.gz
  md5: d5e7e55e528dbd0d9811a0ddf7dfafc6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tentacles = tentacles:main
    #
    # Would create an entry point called tentacles that calls tentacles.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - tentacles
    - tentacles.backends
    - tentacles.backends.sqlite3

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://devedge.bour.cc/wiki/Tentacles
  license: GNU Affero General Public License v3
  summary: 'Object-Relational Mapping (ORM)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
