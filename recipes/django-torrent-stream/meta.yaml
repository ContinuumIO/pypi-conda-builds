package:
  name: django-torrent-stream
  version: "0.9.3"

source:
  fn: django-torrent-stream-0.9.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-torrent-stream/django-torrent-stream-0.9.3.tar.gz
  md5: bfd769b49fb17e33266d72c68caeb8e8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-torrent-stream = django-torrent-stream:main
    #
    # Would create an entry point called django-torrent-stream that calls django-torrent-stream.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - torrent_stream

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/adw0rd/django-torrent-stream
  license: BSD License
  summary: 'Wraps the Torrent Stream http://torrentstream.org/'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
