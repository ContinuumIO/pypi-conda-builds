package:
  name: cpf_cnpj
  version: "0.1.4"

source:
  fn: cpf_cnpj-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/c/cpf_cnpj/cpf_cnpj-0.1.4.tar.gz
  md5: c352df0025f4665f301f46725735023c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cpf_cnpj = cpf_cnpj:main
    #
    # Would create an entry point called cpf_cnpj that calls cpf_cnpj.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - cpf_cnpj
    - cpf_cnpj.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/amargarin/cpf_cnpj
  license: MIT
  summary: 'Validador de CPF e CNPJ para Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
