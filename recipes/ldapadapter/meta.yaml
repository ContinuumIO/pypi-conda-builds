package:
  name: ldapadapter
  version: "0.8"

source:
  fn: ldapadapter-0.8.tar.gz
  url: https://pypi.python.org/packages/source/l/ldapadapter/ldapadapter-0.8.tar.gz
  md5: 9fa28c10a7b4a9075b89a3a2b053bb2d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ldapadapter = ldapadapter:main
    #
    # Would create an entry point called ldapadapter that calls ldapadapter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python-ldap
    - zodb3
    - zope.interface
    - zope.component
    - zope.schema
    - zope.container
    - zope.componentvocabulary
    - zope.security
    - zope.i18nmessageid

  run:
    - python
    - setuptools
    - python-ldap
    - zodb3
    - zope.interface
    - zope.component
    - zope.schema
    - zope.container
    - zope.componentvocabulary
    - zope.security
    - zope.i18nmessageid

test:
  # Python imports
  imports:
    - ldapadapter
    - ldapadapter.browser
    - ldapadapter.help
    - ldapadapter.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.zope.org/ldapadapter
  license: ZPL 2.1
  summary: 'LDAP connection for Zope Toolkit. Connects to an LDAP server.\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
