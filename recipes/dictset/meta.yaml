package:
  name: dictset
  version: "0.3.1.2"

source:
  fn: dictset-0.3.1.2.zip
  url: https://pypi.python.org/packages/source/d/dictset/dictset-0.3.1.2.zip
  md5: 965126ca14ec3d4e0d9b367cc83d5187
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dictset = dictset:main
    #
    # Would create an entry point called dictset that calls dictset.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - dictset

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/dictset/
  license: BSD License
  summary: 'A specialized Python container datatype for managing collections of sets.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
