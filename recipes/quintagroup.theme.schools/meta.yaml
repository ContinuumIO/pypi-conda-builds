package:
  name: quintagroup.theme.schools
  version: "6.5"

source:
  fn: quintagroup.theme.schools-6.5.zip
  url: https://pypi.python.org/packages/source/q/quintagroup.theme.schools/quintagroup.theme.schools-6.5.zip
  md5: 74d7591f575ee3d9d5ed45d753e3db4d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - quintagroup.theme.schools = quintagroup.theme.schools:main
    #
    # Would create an entry point called quintagroup.theme.schools that calls quintagroup.theme.schools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.theming

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.theming

test:
  # Python imports
  imports:
    - quintagroup
    - quintagroup.theme
    - quintagroup.theme.schools

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://themes.quintagroup.com/product/schools
  license: GNU General Public License (GPL)
  summary: 'Free Diazo theme for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
