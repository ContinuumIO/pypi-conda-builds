package:
  name: django-google-tools
  version: "1.0.0"

source:
  fn: django-google-tools-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-google-tools/django-google-tools-1.0.0.tar.gz
  md5: 47170d5678cadf537ca0687e20300c91
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-google-tools = django-google-tools:main
    #
    # Would create an entry point called django-google-tools that calls django-google-tools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - googletools
    - googletools.migrations
    - googletools.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/camilonova/django-google-tools
  license: MIT License
  summary: 'Django app for managing Google Analytics and Site Verification codes.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
