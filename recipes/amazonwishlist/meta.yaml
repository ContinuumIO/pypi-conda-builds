package:
  name: amazonwishlist
  version: "0.6.1"

source:
  fn: amazonwishlist-0.6.1.tar.gz
  url: https://pypi.python.org/packages/source/a/amazonwishlist/amazonwishlist-0.6.1.tar.gz
  md5: 5011d93042f9da55035909e307b72854
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - amazonwishlist = amazonwishlist:main
    #
    # Would create an entry point called amazonwishlist that calls amazonwishlist.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pytest
    - lxml
    - beautifulsoup

  run:
    - python
    - pytest
    - lxml
    - beautifulsoup

test:
  # Python imports
  imports:
    - amazonwishlist

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/caio1982/amazon-wishlist
  license: GNU General Public License v2 (GPLv2)
  summary: 'Query and visualize Amazon wishlists information (e.g. total cost, items etc)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
