package:
  name: braveapi
  version: "0.1"

source:
  fn: braveapi-0.1.tar.gz
  url: https://pypi.python.org/packages/source/b/braveapi/braveapi-0.1.tar.gz
  md5: 5bc344ef5fe9486bf70b8f076719c2d9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - braveapi = braveapi:main
    #
    # Would create an entry point called braveapi that calls braveapi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - webob
    - marrow.util
    - ecdsa
    - futures

  run:
    - python
    - webob
    - marrow.util
    - ecdsa
    - futures

test:
  # Python imports
  imports:
    - brave
    - braveapi
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT
  summary: 'Python API bindings for both utilizing and hosting the secure RPC mechanism.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
