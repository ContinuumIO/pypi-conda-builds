package:
  name: rd-celery-email
  version: "0.1.6"

source:
  fn: rd-celery-email-0.1.6.tar.gz
  url: https://pypi.python.org/packages/source/r/rd-celery-email/rd-celery-email-0.1.6.tar.gz
  md5: 38ae0aedb33bfea2c3cdf4ead6ccd59b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - rd-celery-email = rd-celery-email:main
    #
    # Would create an entry point called rd-celery-email that calls rd-celery-email.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - rdcelery_email

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/andychiang/dj-celery-email
  license: BSD
  summary: 'rd-celery-email'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
