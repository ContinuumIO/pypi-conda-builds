package:
  name: django-browserstack-tools
  version: "0.1.1"

source:
  fn: django-browserstack-tools-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-browserstack-tools/django-browserstack-tools-0.1.1.tar.gz
  md5: 62b2e354d0e1d62c39fc0ee6bda3ddfa
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-browserstack-tools = django-browserstack-tools:main
    #
    # Would create an entry point called django-browserstack-tools that calls django-browserstack-tools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - browserstack_tools

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/qdqmedia/django-browserstack-tools
  license: BSD License
  summary: 'Unofficial Browserstack integration for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
