package:
  name: iscorpio.themes.redmaple
  version: "0.1"

source:
  fn: iscorpio.themes.redmaple-0.1.tar.gz
  url: https://pypi.python.org/packages/source/i/iscorpio.themes.redmaple/iscorpio.themes.redmaple-0.1.tar.gz
  md5: 910d1a4271a65a63de9140b1f524d485
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - iscorpio.themes.redmaple = iscorpio.themes.redmaple:main
    #
    # Would create an entry point called iscorpio.themes.redmaple that calls iscorpio.themes.redmaple.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - iscorpio
    - iscorpio.themes
    - iscorpio.themes.redmaple

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plonexp.leocorn.com/plonepm
  license: GNU General Public License (GPL)
  summary: 'iscorpio readmaple Plone 3 theme'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
