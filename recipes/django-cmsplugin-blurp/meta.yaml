package:
  name: django-cmsplugin-blurp
  version: "1.10.0"

source:
  fn: django-cmsplugin-blurp-1.10.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-cmsplugin-blurp/django-cmsplugin-blurp-1.10.0.tar.gz
  md5: 63dedc65bb5c2bcbeabdbce7745b4b5d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-cmsplugin-blurp = django-cmsplugin-blurp:main
    #
    # Would create an entry point called django-cmsplugin-blurp that calls django-cmsplugin-blurp.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >1.5
    - django-classy-tags

  run:
    - python
    - django >1.5
    - django-classy-tags

test:
  # Python imports
  imports:
    - cmsplugin_blurp
    - cmsplugin_blurp.renderers
    - cmsplugin_blurp.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://dev.entrouvert.org/projects/django-cmsplugin-blurp/
  license: AGPLv3 or later
  summary: None

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
