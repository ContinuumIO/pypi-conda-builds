package:
  name: conmongo
  version: "0.0.2"

source:
  fn: conmongo-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/c/conmongo/conmongo-0.0.2.tar.gz
  md5: 6f0616e4df51923ae3968ac0ab148969
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - conmongo = conmongo:main
    #
    # Would create an entry point called conmongo that calls conmongo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask ==0.9
    - pymongo ==2.5

  run:
    - python
    - flask ==0.9
    - pymongo ==2.5

test:
  # Python imports
  imports:
    - conmongo

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/pennappslabs/conmongo
  license: BSD License
  summary: 'A Flask microplugin based on pymongo'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
