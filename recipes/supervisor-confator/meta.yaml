package:
  name: supervisor-confator
  version: "0.3.0"

source:
  fn: supervisor-confator-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/s/supervisor-confator/supervisor-confator-0.3.0.tar.gz
  md5: 2eb3f7cab7580f98ab38b202c79fb765
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - supervisor-confator = supervisor-confator:main
    #
    # Would create an entry point called supervisor-confator that calls supervisor-confator.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - supervisor_confator

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/alexhayes/supervisor-confator
  license: MIT License
  summary: 'Python interface to generate supervisor configuration files.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
