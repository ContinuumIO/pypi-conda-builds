package:
  name: products.trustedexecutables
  version: "1.0.3"

source:
  fn: Products.TrustedExecutables-1.0.3.tar.gz
  url: https://pypi.python.org/packages/source/P/Products.TrustedExecutables/Products.TrustedExecutables-1.0.3.tar.gz
  md5: c8fc48b1dd042e57c12f8839c6f18392
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - products.trustedexecutables = products.trustedexecutables:main
    #
    # Would create an entry point called products.trustedexecutables that calls products.trustedexecutables.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - dm.reuse

  run:
    - python
    - dm.reuse

test:
  # Python imports
  imports:
    - Products
    - Products.TrustedExecutables
    - Products.TrustedExecutables.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.dieter.handshake.de/pyprojects/zope
  license: BSD License
  summary: 'Trusted Zope 2 executables (scripts, templates) -- unrestricted by Zope 2 security. For Zope 2.11 and above'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
