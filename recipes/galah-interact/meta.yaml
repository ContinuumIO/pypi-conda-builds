package:
  name: galah-interact
  version: "v0.2.1-"

source:
  fn: galah-interact-v0.2.1-.tar.gz
  url: https://pypi.python.org/packages/source/g/galah-interact/galah-interact-v0.2.1-.tar.gz
  md5: 1c4375494d28de4f3d72099fdcdecf7f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - galah-interact = galah-interact:main
    #
    # Would create an entry point called galah-interact that calls galah-interact.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - interact
    - interact_test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://www.github.com/galah-group/galah-interact-python
  license: Apache Software License
  summary: "A Python framework for creating test harnesses to grade students' code."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
