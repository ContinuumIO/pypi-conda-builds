package:
  name: zope.decorator
  version: "3.4.0"

source:
  fn: zope.decorator-3.4.0.tar.gz
  url: https://pypi.python.org/packages/source/z/zope.decorator/zope.decorator-3.4.0.tar.gz
  md5: ebf375120411042eb5712165e23ac920
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zope.decorator = zope.decorator:main
    #
    # Would create an entry point called zope.decorator that calls zope.decorator.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - zope
    - zope.decorator

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://cheeseshop.python.org/pypi/zope.decorator
  license: Zope Public License
  summary: 'Zope Decorators (Deprecated)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
