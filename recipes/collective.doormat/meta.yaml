package:
  name: collective.doormat
  version: "0.1"

source:
  fn: collective.doormat-0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.doormat/collective.doormat-0.1.tar.gz
  md5: 0ae0d0aa0659b4d898b7c7e39584b6b2
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.doormat = collective.doormat:main
    #
    # Would create an entry point called collective.doormat that calls collective.doormat.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.registry

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.registry

test:
  # Python imports
  imports:
    - collective
    - collective.doormat
    - collective.doormat.browser
    - collective.doormat.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/collective.doormat
  license: GNU General Public License (GPL)
  summary: 'Adds a doormat viewlet and installs it in the Plone\n      footer. The links and text in the doormat are manageable in the\n      control panel'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
