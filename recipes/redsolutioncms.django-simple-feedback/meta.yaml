package:
  name: redsolutioncms.django-simple-feedback
  version: "0.4.1"

source:
  fn: redsolutioncms.django-simple-feedback-0.4.1.tar.gz
  url: https://pypi.python.org/packages/source/r/redsolutioncms.django-simple-feedback/redsolutioncms.django-simple-feedback-0.4.1.tar.gz
  md5: 890a188e22139505e3bb257a685d6f9d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - redsolutioncms.django-simple-feedback = redsolutioncms.django-simple-feedback:main
    #
    # Would create an entry point called redsolutioncms.django-simple-feedback that calls redsolutioncms.django-simple-feedback.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-classy-tags >=0.3,<0.4

  run:
    - python
    - setuptools
    - django-classy-tags >=0.3,<0.4

test:
  # Python imports
  imports:
    - feedback
    - feedback.migrations
    - feedback.redsolution_setup
    - feedback.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: GNU General Public License (GPL)
  summary: 'Simple application, that provides customizable email form template tag.\nYou can specify form fields and optionally enable dedicated view to render feedback page.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
