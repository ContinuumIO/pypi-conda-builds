package:
  name: ftw.task
  version: "2.4.7"

source:
  fn: ftw.task-2.4.7.zip
  url: https://pypi.python.org/packages/source/f/ftw.task/ftw.task-2.4.7.zip
  md5: 330c46d6a0f61fbcb41a39595ab0bb0a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ftw.task = ftw.task:main
    #
    # Would create an entry point called ftw.task that calls ftw.task.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - accesscontrol
    - zope2
    - zope.app.component
    - zope.component
    - zope.i18n
    - zope.i18nmessageid
    - zope.interface
    - zope.schema
    - products.atcontenttypes
    - products.archetypes
    - products.cmfcore
    - products.atreferencebrowserwidget
    - plone.principalsource
    - plone.app.contentmenu
    - plone.app.portlets
    - plone.portlets
    - ftw.calendarwidget
    - ftw.upgrade

  run:
    - python
    - setuptools
    - setuptools
    - accesscontrol
    - zope2
    - zope.app.component
    - zope.component
    - zope.i18n
    - zope.i18nmessageid
    - zope.interface
    - zope.schema
    - products.atcontenttypes
    - products.archetypes
    - products.cmfcore
    - products.atreferencebrowserwidget
    - plone.principalsource
    - plone.app.contentmenu
    - plone.app.portlets
    - plone.portlets
    - ftw.calendarwidget
    - ftw.upgrade

test:
  # Python imports
  imports:
    - ftw
    - ftw.task
    - ftw.task.browser
    - ftw.task.content
    - ftw.task.latex
    - ftw.task.portlets
    - ftw.task.tests
    - ftw.task.upgrades

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/4teamwork/ftw.task
  license: GNU General Public License (GPL)
  summary: 'A simple task content type for Plone.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
