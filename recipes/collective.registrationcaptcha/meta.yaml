package:
  name: collective.registrationcaptcha
  version: "1.1"

source:
  fn: collective.registrationcaptcha-1.1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.registrationcaptcha/collective.registrationcaptcha-1.1.tar.gz
  md5: a7afdb8c0b7f3f39260ed40ea107e9fc
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.registrationcaptcha = collective.registrationcaptcha:main
    #
    # Would create an entry point called collective.registrationcaptcha that calls collective.registrationcaptcha.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.cmfcore
    - plone.app.discussion
    - plone.app.users >=2.0
    - plone.protect
    - plone.registry
    - plone.z3cform
    - z3c.form
    - zope.component
    - zope.interface
    - zope.publisher

  run:
    - python
    - setuptools
    - setuptools
    - products.cmfcore
    - plone.app.discussion
    - plone.app.users >=2.0
    - plone.protect
    - plone.registry
    - plone.z3cform
    - z3c.form
    - zope.component
    - zope.interface
    - zope.publisher

test:
  # Python imports
  imports:
    - collective
    - collective.registrationcaptcha

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.registrationcaptcha
  license: GPL
  summary: 'Add a captcha field to the registration form.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
