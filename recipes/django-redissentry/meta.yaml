package:
  name: django-redissentry
  version: "0.2.0"

source:
  fn: django-redissentry-0.2.0.zip
  url: https://pypi.python.org/packages/source/d/django-redissentry/django-redissentry-0.2.0.zip
  md5: f8b5b42f478f0661c0057059b863efd1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-redissentry = django-redissentry:main
    #
    # Would create an entry point called django-redissentry that calls django-redissentry.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - redissentry-core >=0.2.0

  run:
    - python
    - redissentry-core >=0.2.0

test:
  # Python imports
  imports:
    - redissentry
    - redissentry.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/axil/django-redissentry
  license: MIT License
  summary: 'Django app based on RedisSentry (redissentry-core) protecting against password attacks'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
