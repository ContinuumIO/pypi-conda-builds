package:
  name: django-templateinspector
  version: "0.1"

source:
  fn: django-templateinspector-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-templateinspector/django-templateinspector-0.1.tar.gz
  md5: 60906fd2d2b3a26b9057774936a6f46d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-templateinspector = django-templateinspector:main
    #
    # Would create an entry point called django-templateinspector that calls django-templateinspector.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - djtemplateinspector

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: BSD
  summary: 'Find out what context variables are expected by a Django Template'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
