package:
  name: apicaller
  version: "0.0.2a"

source:
  fn: apicaller-0.0.2a.tar.gz
  url: https://pypi.python.org/packages/source/a/apicaller/apicaller-0.0.2a.tar.gz
  md5: cb448544688e95108e9c5942a62997c8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - apicaller = apicaller:main
    #
    # Would create an entry point called apicaller that calls apicaller.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - apicaller

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/cespivo/apicaller
  license: Apache Software License
  summary: 'APICaller makes the creating API client library easier.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
