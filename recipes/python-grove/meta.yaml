package:
  name: python-grove
  version: "0.0.1"

source:
  fn: python-grove-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/p/python-grove/python-grove-0.0.1.tar.gz
  md5: adeaa1fcf3777b8150a04afb113cfdbc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-grove = python-grove:main
    #
    # Would create an entry point called python-grove that calls python-grove.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/nicholasserra/python-grove/
  license: MIT License
  summary: 'A python class to interface with the Grove.io HTTP API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
