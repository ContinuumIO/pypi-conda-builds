package:
  name: icelandic_holidays
  version: "0.6.1"

source:
  fn: icelandic_holidays-0.6.1.tar.gz
  url: https://pypi.python.org/packages/source/i/icelandic_holidays/icelandic_holidays-0.6.1.tar.gz
  md5: d9ba5c20d79fc5580b1dfe9138e137c3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - icelandic_holidays = icelandic_holidays:main
    #
    # Would create an entry point called icelandic_holidays that calls icelandic_holidays.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - icelandic_holidays

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/solvip/icelandic_holidays
  license: UNKNOWN
  summary: 'A Python library written to determine if a day is an Icelandic holiday or a business day.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
