package:
  name: simple-jsonrpc
  version: "0.3.4"

source:
  fn: simple-jsonrpc-0.3.4.tar.gz
  url: https://pypi.python.org/packages/source/s/simple-jsonrpc/simple-jsonrpc-0.3.4.tar.gz
  md5: 78fc6b7a048fd103fce288bcb29d7838
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - simple-jsonrpc = simple-jsonrpc:main
    #
    # Would create an entry point called simple-jsonrpc that calls simple-jsonrpc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dengzhp/simple-jsonrpc
  license: http://www.apache.org/licenses/LICENSE-2.0
  summary: 'a simple JSON-RPC v2.0 library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
