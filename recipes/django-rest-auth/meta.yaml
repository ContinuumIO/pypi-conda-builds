package:
  name: django-rest-auth
  version: "0.4.0"

source:
  fn: django-rest-auth-0.4.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-rest-auth/django-rest-auth-0.4.0.tar.gz
  md5: 40c8a5a5003652b697175ca09b659fa5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-rest-auth = django-rest-auth:main
    #
    # Would create an entry point called django-rest-auth that calls django-rest-auth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5.0
    - djangorestframework >=3.0
    - six >=1.9.0

  run:
    - python
    - django >=1.5.0
    - djangorestframework >=3.0
    - six >=1.9.0

test:
  # Python imports
  imports:
    - rest_auth
    - rest_auth.registration
    - rest_auth.test_utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/Tivix/django-rest-auth
  license: UNKNOWN
  summary: 'Create a set of REST API endpoints for Authentication and Registration'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
