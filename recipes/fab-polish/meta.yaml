package:
  name: fab-polish
  version: "1.0.1"

source:
  fn: fab-polish-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/f/fab-polish/fab-polish-1.0.1.tar.gz
  md5: 7b3ef213f3c83d83ccfba00060d6dff4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fab-polish = fab-polish:main
    #
    # Would create an entry point called fab-polish that calls fab-polish.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fabric

  run:
    - python
    - fabric

test:
  # Python imports
  imports:
    - fabpolish

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/practo/FabPolish
  license: MIT
  summary: 'Polish git versioned source code using Fabric'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
