package:
  name: chopthin
  version: "0.2"

source:
  fn: chopthin-0.2.tar.gz
  url: https://pypi.python.org/packages/source/c/chopthin/chopthin-0.2.tar.gz
  md5: c151db16c9927a6aadac156f1e69c91d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - chopthin = chopthin:main
    #
    # Would create an entry point called chopthin that calls chopthin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - chopthin

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://wwwf.imperial.ac.uk/~agandy/software.html
  license: GPLv3
  summary: 'Implementation of the chopthin resampler for particle filtering/SMC'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
