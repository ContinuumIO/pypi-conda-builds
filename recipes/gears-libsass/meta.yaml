package:
  name: gears-libsass
  version: "0.0.3"

source:
  fn: gears-libsass-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/g/gears-libsass/gears-libsass-0.0.3.tar.gz
  md5: e84a922204c0dd50aefe506250c1894b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gears-libsass = gears-libsass:main
    #
    # Would create an entry point called gears-libsass that calls gears-libsass.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - libsass >=0.3.0

  run:
    - python
    - libsass >=0.3.0

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'Gears compiler for sass using python-libsass'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
