package:
  name: django-eintopf
  version: "0.0.1-beta"

source:
  fn: django-eintopf-0.0.1-beta2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-eintopf/django-eintopf-0.0.1-beta2.tar.gz
  md5: 57b8cab5fbe1bddf7f0cb68b3a86e287
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-eintopf = django-eintopf:main
    #
    # Would create an entry point called django-eintopf that calls django-eintopf.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - eintopf

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/fosil/django-eintopf
  license: MIT License
  summary: 'Collection of small tools and enhancements for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
