package:
  name: mongoforms_dfields
  version: "0.2.1"

source:
  fn: mongoforms_dfields-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/m/mongoforms_dfields/mongoforms_dfields-0.2.1.tar.gz
  md5: b167642edd450455c26860955a17608d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mongoforms_dfields = mongoforms_dfields:main
    #
    # Would create an entry point called mongoforms_dfields that calls mongoforms_dfields.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - mongoengine >=0.6
    - pymongo >=2.1
    - django_mongoforms >=0.3

  run:
    - python
    - django
    - mongoengine >=0.6
    - pymongo >=2.1
    - django_mongoforms >=0.3

test:
  # Python imports
  imports:
    - mongoforms_dfields

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/multmeio/mongoforms_dfields
  license: BSD License
  summary: 'A ModelForm-DynamicFields'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
