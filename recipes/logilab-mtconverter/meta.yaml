package:
  name: logilab-mtconverter
  version: "0.8.2"

source:
  fn: logilab-mtconverter-0.8.2.tar.gz
  url: https://pypi.python.org/packages/source/l/logilab-mtconverter/logilab-mtconverter-0.8.2.tar.gz
  md5: 00427edde7809bcae1b7342145532f3b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - logilab-mtconverter = logilab-mtconverter:main
    #
    # Would create an entry point called logilab-mtconverter that calls logilab-mtconverter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - logilab.mtconverter
    - logilab.mtconverter.test
    - logilab.mtconverter.transforms

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.logilab.org/project/logilab-mtconverter
  license: LGPL
  summary: 'a library to convert from a MIME type to another'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
