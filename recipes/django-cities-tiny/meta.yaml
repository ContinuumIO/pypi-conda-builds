package:
  name: django-cities-tiny
  version: "1.0"

source:
  fn: django-cities-tiny-1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-cities-tiny/django-cities-tiny-1.0.tar.gz
  md5: a28d3a1864b9c3c1ac902fef266c392e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-cities-tiny = django-cities-tiny:main
    #
    # Would create an entry point called django-cities-tiny that calls django-cities-tiny.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - cities_tiny
    - cities_tiny.management
    - cities_tiny.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/django-cities-tiny/
  license: MIT License
  summary: 'Another simple alternative to django-cities'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
