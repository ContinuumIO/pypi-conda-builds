package:
  name: django-requestlogging
  version: "1.0.1"

source:
  fn: django-requestlogging-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-requestlogging/django-requestlogging-1.0.1.tar.gz
  md5: d7751d584f4cf120777dd07c69602193
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-requestlogging = django-requestlogging:main
    #
    # Would create an entry point called django-requestlogging that calls django-requestlogging.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3

  run:
    - python
    - django >=1.3

test:
  # Python imports
  imports:
    - django_requestlogging

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/trustcentric/django-requestlogging/
  license: BSD License
  summary: 'Adds information about requests to logging records.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
