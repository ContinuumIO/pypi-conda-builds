package:
  name: infi.wioctl
  version: "0.1.10"

source:
  fn: infi.wioctl-0.1.10.tar.gz
  url: https://pypi.python.org/packages/source/i/infi.wioctl/infi.wioctl-0.1.10.tar.gz
  md5: 810afbb5b1d17b6598f31ae57ee8b16e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - infi.wioctl = infi.wioctl:main
    #
    # Would create an entry point called infi.wioctl that calls infi.wioctl.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - infi.cwrap
    - infi.exceptools
    - infi.instruct
    - setuptools

  run:
    - python
    - infi.cwrap
    - infi.exceptools
    - infi.instruct
    - setuptools

test:
  # Python imports
  imports:
    - infi
    - infi.wioctl

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Infinidat/infi.wioctl
  license: Python Software Foundation License
  summary: 'Windows ioctl wrapper'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
