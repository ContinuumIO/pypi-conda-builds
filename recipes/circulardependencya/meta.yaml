package:
  name: circulardependencya
  version: "0.0.0"

source:
  fn: CircularDependencyA-0.0.0.tar.gz
  url: https://pypi.python.org/packages/source/C/CircularDependencyA/CircularDependencyA-0.0.0.tar.gz
  md5: 7988b7a9684ec8fece6ff2b9d2641849
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - circulardependencya = circulardependencya:main
    #
    # Would create an entry point called circulardependencya that calls circulardependencya.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - circulardependencyb

  run:
    - python
    - circulardependencyb

test:
  # Python imports
  imports:
    - circulardependencya

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/msabramo/CircularDependencyA
  license: MIT
  summary: 'CircularDependency{A,B} depend on each other; for testing packaging tools'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
