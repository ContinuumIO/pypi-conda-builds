package:
  name: nestoria
  version: "0.31"

source:
  fn: nestoria-0.31.tar.gz
  url: https://pypi.python.org/packages/source/n/nestoria/nestoria-0.31.tar.gz
  md5: b17852b3d3d7909205ae744bb53b4d0b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - nestoria = nestoria:main
    #
    # Would create an entry point called nestoria that calls nestoria.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - simplejson ==1.4

  run:
    - python
    - setuptools
    - simplejson ==1.4

test:
  # Python imports
  imports:
    - nestoria

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.nestoria.co.uk/help/api-tech
  license: GPL
  summary: 'Wrapper for www.nestoria.co.uk API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
