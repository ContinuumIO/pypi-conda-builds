package:
  name: helper_datetime
  version: "0.5"

source:
  fn: helper_datetime-0.5.tar.gz
  url: https://pypi.python.org/packages/source/h/helper_datetime/helper_datetime-0.5.tar.gz
  md5: adbf052978e54a8d89326e83b64297fd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - helper_datetime = helper_datetime:main
    #
    # Would create an entry point called helper_datetime that calls helper_datetime.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - helper_datetime

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/shuge/helper_datetime
  license: MIT License
  summary: 'Date & Time Helper'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
