package:
  name: fingerprint-app
  version: "0.2"

source:
  fn: fingerprint-app-0.2.tar.gz
  url: https://pypi.python.org/packages/source/f/fingerprint-app/fingerprint-app-0.2.tar.gz
  md5: 32504522f2b8ac827b1807887fe6f646
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fingerprint-app = fingerprint-app:main
    #
    # Would create an entry point called fingerprint-app that calls fingerprint-app.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - FingerPrint
    - FingerPrint.plugins
    - FingerPrint.ptrace

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rocksclusters/FingerPrint
  license: finic
  summary: 'Fingerprinting application dependencies'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
