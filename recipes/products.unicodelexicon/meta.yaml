package:
  name: products.unicodelexicon
  version: "2.2"

source:
  fn: Products.UnicodeLexicon-2.2.zip
  url: https://pypi.python.org/packages/source/P/Products.UnicodeLexicon/Products.UnicodeLexicon-2.2.zip
  md5: 67cfba7757f9a7b14c4d23ee43ef4c0b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - products.unicodelexicon = products.unicodelexicon:main
    #
    # Would create an entry point called products.unicodelexicon that calls products.unicodelexicon.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - Products
    - Products.UnicodeLexicon
    - Products.UnicodeLexicon.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/Products.UnicodeLexicon
  license: BSD License
  summary: 'Unicode aware lexicon for ZCTextIndex'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
