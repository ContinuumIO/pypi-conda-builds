package:
  name: django-online-status
  version: "0.1.0"

source:
  fn: django-online-status-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-online-status/django-online-status-0.1.0.tar.gz
  md5: 5cd7a13e995c96136bf0680ab1fabf5e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-online-status = django-online-status:main
    #
    # Would create an entry point called django-online-status that calls django-online-status.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - online_status
    - online_status.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/zalew/django-online-status
  license: BSD License
  summary: 'Online status for authenticated users (online, idle, offline) and list of current users online, all to display with templatetags. No database models, everything cached. Read more: http://bitbucket.org/zalew/django-online-status/wiki/Home'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
