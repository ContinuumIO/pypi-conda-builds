package:
  name: routes
  version: "2.1"

source:
  fn: Routes-2.1.tar.gz
  url: https://pypi.python.org/packages/source/R/Routes/Routes-2.1.tar.gz
  md5: e6e463318a9dc6ad2f1b3040e998f0b4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - routes = routes:main
    #
    # Would create an entry point called routes that calls routes.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - repoze.lru >=0.3

  run:
    - python
    - repoze.lru >=0.3

test:
  # Python imports
  imports:
    - routes

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://routes.readthedocs.org/
  license: MIT License
  summary: 'Routing Recognition and Generation Tools'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
