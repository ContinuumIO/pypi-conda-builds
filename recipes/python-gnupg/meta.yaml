package:
  name: python-gnupg
  version: "0.3.7"

source:
  fn: python-gnupg-0.3.7.tar.gz
  url: https://pypi.python.org/packages/source/p/python-gnupg/python-gnupg-0.3.7.tar.gz
  md5: ce600421b8168bd7d9470d6b451594d5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-gnupg = python-gnupg:main
    #
    # Would create an entry point called python-gnupg that calls python-gnupg.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://packages.python.org/python-gnupg/index.html
  license: BSD License
  summary: 'A wrapper for the Gnu Privacy Guard (GPG or GnuPG)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
