package:
  name: django-hash-filter
  version: "1.1"

source:
  fn: django-hash-filter-1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-hash-filter/django-hash-filter-1.1.tar.gz
  md5: ca6f19c82307b968e69bbc0fc79fe1d0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-hash-filter = django-hash-filter:main
    #
    # Would create an entry point called django-hash-filter that calls django-hash-filter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3

  run:
    - python
    - django >=1.3

test:
  # Python imports
  imports:
    - django_hash_filter
    - django_hash_filter.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/andrewjsledge/django-hash-filter
  license: MIT License
  summary: 'Provides a simple filter to produce hashed (hex digest) values in templates.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
