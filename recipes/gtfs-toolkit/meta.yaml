package:
  name: gtfs-toolkit
  version: "0.11.3"

source:
  fn: gtfs-toolkit-0.11.3.tar.gz
  url: https://pypi.python.org/packages/source/g/gtfs-toolkit/gtfs-toolkit-0.11.3.tar.gz
  md5: 2919fda60c57bdaee8c962b231a89600
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gtfs-toolkit = gtfs-toolkit:main
    #
    # Would create an entry point called gtfs-toolkit that calls gtfs-toolkit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - shapely ==1.5.1
    - pandas ==0.15.2
    - utm >=0.3.1

  run:
    - python
    - shapely ==1.5.1
    - pandas ==0.15.2
    - utm >=0.3.1

test:
  # Python imports
  imports:
    - gtfs_toolkit
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/araichev/gtfs-toolkit
  license: LICENSE
  summary: 'A set of Python 3.4 tools for processing General Transit Feed Specification (GTFS) data'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
