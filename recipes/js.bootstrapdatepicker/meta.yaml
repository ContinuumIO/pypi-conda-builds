package:
  name: js.bootstrapdatepicker
  version: "1.0"

source:
  fn: js.bootstrapdatepicker-1.0.zip
  url: https://pypi.python.org/packages/source/j/js.bootstrapdatepicker/js.bootstrapdatepicker-1.0.zip
  md5: 857a6ced69ac45fa7ff38c67673ab343
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - js.bootstrapdatepicker = js.bootstrapdatepicker:main
    #
    # Would create an entry point called js.bootstrapdatepicker that calls js.bootstrapdatepicker.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fanstatic
    - js.jquery
    - setuptools

  run:
    - python
    - setuptools
    - fanstatic
    - js.jquery
    - setuptools

test:
  # Python imports
  imports:
    - js
    - js.bootstrapdatepicker

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: GPL
  summary: 'bootstrap date picker fanstatic integration'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
