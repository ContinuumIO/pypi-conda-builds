package:
  name: ipsecparse
  version: "0.1.0"

source:
  fn: ipsecparse-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/i/ipsecparse/ipsecparse-0.1.0.tar.gz
  md5: 61b3cbf01ff5a0f83a2d7e5d61e2b8f7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ipsecparse = ipsecparse:main
    #
    # Would create an entry point called ipsecparse that calls ipsecparse.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - ply >=3.4

  run:
    - python
    - ply >=3.4

test:
  # Python imports
  imports:
    - ipsecparse

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/leforestier/ipsecparse
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Parse and edit your ipsec configuration files'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
