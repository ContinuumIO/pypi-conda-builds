package:
  name: django-sites-tools
  version: "0.2.0"

source:
  fn: django-sites-tools-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-sites-tools/django-sites-tools-0.2.0.tar.gz
  md5: ae493b6abd14d25c0b340689c67c7487
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-sites-tools = django-sites-tools:main
    #
    # Would create an entry point called django-sites-tools that calls django-sites-tools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - sites_tools

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/jezdez/django-sites-tools/
  license: BSD License
  summary: 'A Django app that provides tools for the contrib sites app.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
