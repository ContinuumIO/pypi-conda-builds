package:
  name: mtj.f3u1
  version: "0.2"

source:
  fn: mtj.f3u1-0.2.tar.gz
  url: https://pypi.python.org/packages/source/m/mtj.f3u1/mtj.f3u1-0.2.tar.gz
  md5: e95c3d70e21ba32c5f455bc8db3e0402
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mtj.f3u1 = mtj.f3u1:main
    #
    # Would create an entry point called mtj.f3u1 that calls mtj.f3u1.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - mtj
    - mtj.f3u1
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/metatoaster/mtj.f3u1
  license: GPL
  summary: 'Factories and Functions for Fiddling with Units'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
