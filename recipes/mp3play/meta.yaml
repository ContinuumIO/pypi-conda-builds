package:
  name: mp3play
  version: "0.1.15"

source:
  fn: mp3play-0.1.15.zip
  url: https://pypi.python.org/packages/source/m/mp3play/mp3play-0.1.15.zip
  md5: 31d3adf5e3819bab4209a76be4abc314
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mp3play = mp3play:main
    #
    # Would create an entry point called mp3play that calls mp3play.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - mp3play

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/mp3play/
  license: Apache Software License
  summary: 'A simple interface for playing music from an MP3 file.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
