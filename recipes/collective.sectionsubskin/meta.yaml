package:
  name: collective.sectionsubskin
  version: "0.2"

source:
  fn: collective.sectionsubskin-0.2.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.sectionsubskin/collective.sectionsubskin-0.2.tar.gz
  md5: ced314769d89aaf0de38ce21d40fd123
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.sectionsubskin = collective.sectionsubskin:main
    #
    # Would create an entry point called collective.sectionsubskin that calls collective.sectionsubskin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.sectionsubskin
    - collective.sectionsubskin.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.liberalyouth.org/basket/collective.sectionsubskin
  license: GPL
  summary: 'Apply a marker interface to a request to code for special presentation of a subsection.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
