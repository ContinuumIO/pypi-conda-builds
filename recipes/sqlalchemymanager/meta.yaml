package:
  name: sqlalchemymanager
  version: "0.1.0"

source:
  fn: SQLAlchemyManager-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/S/SQLAlchemyManager/SQLAlchemyManager-0.1.0.tar.gz
  md5: 068b29d8e4ef97839d96a4945df4fcf9
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sqlalchemymanager = sqlalchemymanager:main
    #
    # Would create an entry point called sqlalchemymanager that calls sqlalchemymanager.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sqlalchemy >=0.4.0,<=0.4.99

  run:
    - python
    - setuptools
    - sqlalchemy >=0.4.0,<=0.4.99

test:
  # Python imports
  imports:
    - sqlalchemymanager

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: UNKNOWN
  summary: 'Provides a sensible way of using SQLAlchemy in WSGI applications'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
