package:
  name: imx233_gpio
  version: "0.1.0"

source:
  fn: iMX233_GPIO-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/i/iMX233_GPIO/iMX233_GPIO-0.1.0.tar.gz
  md5: 49fefadd21b73fd632369bde633ee949
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - imx233_gpio = imx233_gpio:main
    #
    # Would create an entry point called imx233_gpio that calls imx233_gpio.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - 
    - imx233_gpio

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://www.olimex.com/
  license: MIT License
  summary: 'Control GPIOs on iMX233-OLinuXino.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
