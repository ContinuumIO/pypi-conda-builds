package:
  name: dj.choices
  version: "0.10.0"

source:
  fn: dj.choices-0.10.0.tar.gz
  url: https://pypi.python.org/packages/source/d/dj.choices/dj.choices-0.10.0.tar.gz
  md5: 9bc23813c1250cb7e43c475070aed160
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dj.choices = dj.choices:main
    #
    # Would create an entry point called dj.choices that calls dj.choices.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - six

  run:
    - python
    - django
    - six

test:
  # Python imports
  imports:
    - dj
    - dj._choicestestproject
    - dj._choicestestproject.app
    - dj.choices

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ambv/dj.choices/
  license: MIT License
  summary: 'An enum implementation for Django forms and models.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
