package:
  name: vulndb
  version: "0.0.17"

source:
  fn: vulndb-0.0.17.tar.gz
  url: https://pypi.python.org/packages/source/v/vulndb/vulndb-0.0.17.tar.gz
  md5: 3a09afa6bf3e090a59e871762aed8542
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - vulndb = vulndb:main
    #
    # Would create an entry point called vulndb that calls vulndb.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - vulndb
    - vulndb.constants
    - vulndb.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/vulndb/python-sdk/
  license: BSD License
  summary: 'Provides access to the vulndb information'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
