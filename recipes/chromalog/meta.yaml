package:
  name: chromalog
  version: "1.0.3"

source:
  fn: chromalog-1.0.3.tar.gz
  url: https://pypi.python.org/packages/source/c/chromalog/chromalog-1.0.3.tar.gz
  md5: 07ee5f42ec5439ae2baeb19d53858f20
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - chromalog = chromalog:main
    #
    # Would create an entry point called chromalog that calls chromalog.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - colorama >=0.3.3
    - future >=0.14.3
    - six >=1.9.0

  run:
    - python
    - colorama >=0.3.3
    - future >=0.14.3
    - six >=1.9.0

test:
  # Python imports
  imports:
    - chromalog
    - chromalog.mark

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://chromalog.readthedocs.org/en/latest/index.html
  license: MIT License
  summary: 'A non-intrusive way to use colors in your logs and generic output.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
