package:
  name: django-settings-export
  version: "1.0.3"

source:
  fn: django-settings-export-1.0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-settings-export/django-settings-export-1.0.3.tar.gz
  md5: 99dc93a989e8030965be907ae855ae34
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-settings-export = django-settings-export:main
    #
    # Would create an entry point called django-settings-export that calls django-settings-export.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jakubroztocil/django-settings-export
  license: BSD
  summary: 'This Django app allows you to export certain settings to your templates.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
