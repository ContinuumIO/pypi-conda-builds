package:
  name: jubatu-chess
  version: "0.1.0"

source:
  fn: jubatu-chess-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/j/jubatu-chess/jubatu-chess-0.1.0.tar.gz
  md5: 7df2d5dd53da94e444d59a5c4e372534
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jubatu-chess = jubatu-chess:main
    #
    # Would create an entry point called jubatu-chess that calls jubatu-chess.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - jubatu/games/chess
    - jubatu/games/chess/backend
    - jubatu/games/chess/frontend

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://andreasnaive.blogspot.com/
  license: X11/MIT with exemptions
  summary: 'Jubatu chess plugin'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
