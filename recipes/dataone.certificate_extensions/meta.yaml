package:
  name: dataone.certificate_extensions
  version: "1.1.3"

source:
  fn: dataone.certificate_extensions-1.1.3.tar.gz
  url: https://pypi.python.org/packages/source/d/dataone.certificate_extensions/dataone.certificate_extensions-1.1.3.tar.gz
  md5: 7fb3361362c86ec127877560fb0317dc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dataone.certificate_extensions = dataone.certificate_extensions:main
    #
    # Would create an entry point called dataone.certificate_extensions that calls dataone.certificate_extensions.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - dataone.common ==1.1.6

  run:
    - python
    - dataone.common ==1.1.6

test:
  # Python imports
  imports:
    - d1_certificate_python

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://dataone.org
  license: Apache License, Version 2.0
  summary: 'Python extensions for generating and extracting PEM formatted X.509 v3 certificates that contain DataONE Session information'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
