package:
  name: go-proxy-client
  version: "0.2"

source:
  fn: go-proxy-client-0.2.tar.gz
  url: https://pypi.python.org/packages/source/g/go-proxy-client/go-proxy-client-0.2.tar.gz
  md5: 101c522f2ff9f01134cfc6b26645d705
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - go-proxy-client = go-proxy-client:main
    #
    # Would create an entry point called go-proxy-client that calls go-proxy-client.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - go_gae_proxy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT License
  summary: 'Handler for urllib2 to use a go-proxy instance as proxy server'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
