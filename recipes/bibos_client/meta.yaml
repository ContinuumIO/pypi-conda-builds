package:
  name: bibos_client
  version: "0.0.2.5"

source:
  fn: bibos_client-0.0.2.5.tar.gz
  url: https://pypi.python.org/packages/source/b/bibos_client/bibos_client-0.0.2.5.tar.gz
  md5: b698b55ab8093da6873cdaf0b9e2ead7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bibos_client = bibos_client:main
    #
    # Would create an entry point called bibos_client that calls bibos_client.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - netifaces
    - bibos_utils

  run:
    - python
    - netifaces
    - bibos_utils

test:
  # Python imports
  imports:
    - bibos_client

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/magenta-aps/
  license: GPLv3
  summary: 'Clients for the BibOS system'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
