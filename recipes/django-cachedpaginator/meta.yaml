package:
  name: django-cachedpaginator
  version: "0.0.5"

source:
  fn: django-cachedpaginator-0.0.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-cachedpaginator/django-cachedpaginator-0.0.5.tar.gz
  md5: 8918933f6f8ccbb63b71b39356153548
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-cachedpaginator = django-cachedpaginator:main
    #
    # Would create an entry point called django-cachedpaginator that calls django-cachedpaginator.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_cachedpaginator
    - django_cachedpaginator.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/arsham/django-cachedpaginator
  license: BSD License
  summary: 'Paginator that caches pages automatically.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
