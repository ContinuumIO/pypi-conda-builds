package:
  name: redis-semaphore
  version: "0.1.0"

source:
  fn: Redis-Semaphore-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/R/Redis-Semaphore/Redis-Semaphore-0.1.0.tar.gz
  md5: f0fb36baa313dbbb9cb77516135ec63d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - redis-semaphore = redis-semaphore:main
    #
    # Would create an entry point called redis-semaphore that calls redis-semaphore.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - redis_semaphore
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bluele/redis-semaphore
  license: MIT License
  summary: 'A distributed semaphore and mutex built on Redis.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
