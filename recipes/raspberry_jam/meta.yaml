package:
  name: raspberry_jam
  version: "0.0.19"

source:
  fn: raspberry_jam-0.0.19.tar.gz
  url: https://pypi.python.org/packages/source/r/raspberry_jam/raspberry_jam-0.0.19.tar.gz
  md5: f178564d473563854d0f8827a44efc64
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - raspberry_jam = raspberry_jam:main
    #
    # Would create an entry point called raspberry_jam that calls raspberry_jam.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - app
    - app.raspberry_jam
    - app.spider
    - raspberry_jam

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://example.com
  license: Private
  summary: 'Raspberry Jam for music'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
