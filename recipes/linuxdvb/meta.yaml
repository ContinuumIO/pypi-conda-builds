package:
  name: linuxdvb
  version: "0.1"

source:
  fn: linuxdvb-0.1.tar.gz
  url: https://pypi.python.org/packages/source/l/linuxdvb/linuxdvb-0.1.tar.gz
  md5: 8bbebe5f4de20379b4c17f836cb2a75b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - linuxdvb = linuxdvb:main
    #
    # Would create an entry point called linuxdvb that calls linuxdvb.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - linuxdvb

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/linuxdvb
  license: GNU General Public License (GPL)
  summary: 'Python bindings for Linux DVB API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
