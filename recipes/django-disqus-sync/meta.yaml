package:
  name: django-disqus-sync
  version: "1.0.3"

source:
  fn: django-disqus-sync-1.0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-disqus-sync/django-disqus-sync-1.0.3.tar.gz
  md5: 32e5fed80891441417286b40a136dba0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-disqus-sync = django-disqus-sync:main
    #
    # Would create an entry point called django-disqus-sync that calls django-disqus-sync.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools >=0.6b1
    - django >=1.3
    - disqus-python

  run:
    - python
    - setuptools >=0.6b1
    - django >=1.3
    - disqus-python

test:
  # Python imports
  imports:
    - disqus_sync
    - disqus_sync.management
    - disqus_sync.management.commands
    - disqus_sync.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD License
  summary: 'Sync Disqus comments to your database and render them as HTML for better SEO'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
