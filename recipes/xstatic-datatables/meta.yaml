package:
  name: xstatic-datatables
  version: "1.9.4.2"

source:
  fn: XStatic-DataTables-1.9.4.2.tar.gz
  url: https://pypi.python.org/packages/source/X/XStatic-DataTables/XStatic-DataTables-1.9.4.2.tar.gz
  md5: 18242dfadf4959d4215d4272c3ba86eb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - xstatic-datatables = xstatic-datatables:main
    #
    # Would create an entry point called xstatic-datatables that calls xstatic-datatables.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - xstatic
    - xstatic.pkg
    - xstatic.pkg.datatables

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.datatables.net
  license: (same as DataTables)
  summary: 'DataTables 1.9.4 (XStatic packaging standard)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
