package:
  name: django-model-filters
  version: "0.8.0"

source:
  fn: django-model-filters-0.8.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-model-filters/django-model-filters-0.8.0.tar.gz
  md5: d585be36f32f4141e4fc4c8a360b6ccd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-model-filters = django-model-filters:main
    #
    # Would create an entry point called django-model-filters that calls django-model-filters.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.2.1

  run:
    - python
    - django >=1.2.1

test:
  # Python imports
  imports:
    - model_filters
    - model_filters.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mjumbewu/django-model-filters/
  license: BSD License
  summary: 'Simple filters for generic object template blocks'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
