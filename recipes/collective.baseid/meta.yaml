package:
  name: collective.baseid
  version: "1.0rc1"

source:
  fn: collective.baseid-1.0rc1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.baseid/collective.baseid-1.0rc1.tar.gz
  md5: 3c102e8b09956693912896b3c682d18a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.baseid = collective.baseid:main
    #
    # Would create an entry point called collective.baseid that calls collective.baseid.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.baseid

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.openplans.org/projects/vice/
  license: ZPL,GPL
  summary: 'Base interfaces and classes for id utilities.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
