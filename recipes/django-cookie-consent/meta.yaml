package:
  name: django-cookie-consent
  version: "0.1.1"

source:
  fn: django-cookie-consent-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-cookie-consent/django-cookie-consent-0.1.1.tar.gz
  md5: e2c8c92ba2f1b851f0fe7f15cb5714ec
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-cookie-consent = django-cookie-consent:main
    #
    # Would create an entry point called django-cookie-consent that calls django-cookie-consent.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4.2
    - django-appconf

  run:
    - python
    - django >=1.4.2
    - django-appconf

test:
  # Python imports
  imports:
    - cookie_consent
    - cookie_consent.migrations
    - cookie_consent.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bmihelac/django-cookie-consent
  license: BSD License
  summary: 'Django cookie consent application'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
