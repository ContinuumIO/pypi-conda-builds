package:
  name: shapely
  version: "1.5.9"

source:
  fn: Shapely-1.5.9.tar.gz
  url: https://pypi.python.org/packages/source/S/Shapely/Shapely-1.5.9.tar.gz
  md5: b502824b154a49fbb8f33703d71557dd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - shapely = shapely:main
    #
    # Would create an entry point called shapely that calls shapely.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - shapely
    - shapely.algorithms
    - shapely.examples
    - shapely.geometry
    - shapely.speedups
    - shapely.vectorized

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Toblerity/Shapely
  license: BSD License
  summary: 'Geometric objects, predicates, and operations'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
