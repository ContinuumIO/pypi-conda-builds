package:
  name: django-pci-auth
  version: "0.1.1"

source:
  fn: django-pci-auth-0.1.1.zip
  url: https://pypi.python.org/packages/source/d/django-pci-auth/django-pci-auth-0.1.1.zip
  md5: cd16d596f8aab408b4ee497906bc4a14
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-pci-auth = django-pci-auth:main
    #
    # Would create an entry point called django-pci-auth that calls django-pci-auth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4
    - django-axes >=1.2.6
    - django-dajaxice
    - django-passwords
    - py-bcrypt
    - pytz

  run:
    - python
    - django >=1.4
    - django-axes >=1.2.6
    - django-dajaxice
    - django-passwords
    - py-bcrypt
    - pytz

test:
  # Python imports
  imports:
    - django_pci_auth

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/django-pci/django-pci-auth
  license: UNKNOWN
  summary: 'Provides change password validation with AJAX support and history check.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
