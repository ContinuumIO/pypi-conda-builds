package:
  name: ajenti.plugin.dashboard
  version: "0.17"

source:
  fn: ajenti.plugin.dashboard-0.17.tar.gz
  url: https://pypi.python.org/packages/source/a/ajenti.plugin.dashboard/ajenti.plugin.dashboard-0.17.tar.gz
  md5: 2835e2b02945c42bfc67ef69676cc66e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ajenti.plugin.dashboard = ajenti.plugin.dashboard:main
    #
    # Would create an entry point called ajenti.plugin.dashboard that calls ajenti.plugin.dashboard.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - aj
    - ajenti.plugin.core
    - ajenti.plugin.settings

  run:
    - python
    - aj
    - ajenti.plugin.core
    - ajenti.plugin.settings

test:
  # Python imports
  imports:
    - ajenti_plugin_dashboard
    - ajenti_plugin_dashboard.widgets

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://ajenti.org
  license: UNKNOWN
  summary: 'Dashboard'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
