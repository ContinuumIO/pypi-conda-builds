package:
  name: cellom2tif
  version: "0.2.0"

source:
  fn: cellom2tif-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/c/cellom2tif/cellom2tif-0.2.0.tar.gz
  md5: 8058ccdd3be1a47a9301b86cf5e78989
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cellom2tif = cellom2tif:main
    #
    # Would create an entry point called cellom2tif that calls cellom2tif.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - cellom2tif

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jni/cellom2tif
  license: BSD 3-clause
  summary: 'Convert Cellomics images to TIFF.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
