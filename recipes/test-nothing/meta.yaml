package:
  name: test-nothing
  version: "0.0.0"

source:
  fn: test-nothing-0.0.0.tar.gz
  url: https://pypi.python.org/packages/source/t/test-nothing/test-nothing-0.0.0.tar.gz
  md5: c41b54955672a5a1ea4894f387438f17
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - test-nothing = test-nothing:main
    #
    # Would create an entry point called test-nothing that calls test-nothing.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - test_nothing

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: WTFPL
  summary: 'Just an empty project to test pypi registering'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
