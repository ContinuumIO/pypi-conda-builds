package:
  name: fsd.cmfbibliographyat
  version: "0.2.0"

source:
  fn: fsd.cmfbibliographyat-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/f/fsd.cmfbibliographyat/fsd.cmfbibliographyat-0.2.0.tar.gz
  md5: 6136b0667e2104a86c9801def7e7c0cd
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fsd.cmfbibliographyat = fsd.cmfbibliographyat:main
    #
    # Would create an entry point called fsd.cmfbibliographyat that calls fsd.cmfbibliographyat.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - fsd
    - fsd.cmfbibliographyat
    - fsd.cmfbibliographyat.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://weblion.psu.edu/svn/weblion/weblion/fsd.cmfbibliographyat/trunk/fsd/cmfbibliographyat
  license: ZPL
  summary: 'A simple integration of CMFBibliographyAT with FacultyStaffDirectory'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
