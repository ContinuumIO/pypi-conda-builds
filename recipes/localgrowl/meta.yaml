package:
  name: localgrowl
  version: "0.2"

source:
  fn: LocalGrowl-0.2.tar.gz
  url: https://pypi.python.org/packages/source/L/LocalGrowl/LocalGrowl-0.2.tar.gz
  md5: 9453cb71ece2d1ab55809dd502296171
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - localgrowl = localgrowl:main
    #
    # Would create an entry point called localgrowl that calls localgrowl.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - docutils >=0.3

  run:
    - python
    - docutils >=0.3

test:
  # Python imports
  imports:
    - localgrowl

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/LocalGrowl
  license: GPL
  summary: 'Python implemenation of Growl notifications.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
