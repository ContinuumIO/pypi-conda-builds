package:
  name: jpath
  version: "1.5"

source:
  fn: jpath-1.5.tar.gz
  url: https://pypi.python.org/packages/source/j/jpath/jpath-1.5.tar.gz
  md5: f46ed213725f0e72bf0d67a8e5512834
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jpath = jpath:main
    #
    # Would create an entry point called jpath that calls jpath.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - jpath

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.stxnext.pl/open-source/jpath
  license: Python Software Foundation License or MIT License
  summary: 'Access nested dicts and lists using JSON-like path notation.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
