package:
  name: zope.app.broken
  version: "3.6.0"

source:
  fn: zope.app.broken-3.6.0.tar.gz
  url: https://pypi.python.org/packages/source/z/zope.app.broken/zope.app.broken-3.6.0.tar.gz
  md5: e6a7efdd1ea1facfd8c5ba4b25d395cb
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zope.app.broken = zope.app.broken:main
    #
    # Would create an entry point called zope.app.broken that calls zope.app.broken.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.interface
    - zope.location
    - zope.security
    - zope.annotation
    - zope.broken
    - zope.processlifetime
    - zodb3

  run:
    - python
    - setuptools
    - zope.interface
    - zope.location
    - zope.security
    - zope.annotation
    - zope.broken
    - zope.processlifetime
    - zodb3

test:
  # Python imports
  imports:
    - zope
    - zope.app
    - zope.app.broken

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://cheeseshop.python.org/pypi/zope.app.broken
  license: Zope Public License
  summary: 'Zope Broken (ZODB) Object Support'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
