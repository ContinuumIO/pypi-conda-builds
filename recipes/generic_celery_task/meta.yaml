package:
  name: generic_celery_task
  version: "0.2"

source:
  fn: generic_celery_task-0.2.tar.gz
  url: https://pypi.python.org/packages/source/g/generic_celery_task/generic_celery_task-0.2.tar.gz
  md5: d53611ee3c98f5e6ab39aaa6e8bcfd46
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - generic_celery_task = generic_celery_task:main
    #
    # Would create an entry point called generic_celery_task that calls generic_celery_task.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - celery

  run:
    - python
    - celery

test:
  # Python imports
  imports:
    - generic_celery_task

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/stefantalpalaru/generic_celery_task
  license: BSD License
  summary: 'A workaround for the lack of dynamic tasks in Celery'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
