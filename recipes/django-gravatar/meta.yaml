package:
  name: django-gravatar
  version: "0.1.0"

source:
  fn: django-gravatar-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-gravatar/django-gravatar-0.1.0.tar.gz
  md5: 4d67d68e39281addd2d360cd80c7d2b4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-gravatar = django-gravatar:main
    #
    # Would create an entry point called django-gravatar that calls django-gravatar.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - gravatar
    - gravatar.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://django-gravatar.googlecode.com
  license: BSD License
  summary: 'Gravatar Support in a Django Reusable Application'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
