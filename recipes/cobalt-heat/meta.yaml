package:
  name: cobalt-heat
  version: "0.1"

source:
  fn: cobalt-heat-0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/cobalt-heat/cobalt-heat-0.1.tar.gz
  md5: 99140490402b0de777be4a0e7715c3c6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cobalt-heat = cobalt-heat:main
    #
    # Would create an entry point called cobalt-heat that calls cobalt-heat.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - cobalt_heat

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.gridcentric.com/
  license: UNKNOWN
  summary: 'Cobalt resource plugin for OpenStack Heat'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
