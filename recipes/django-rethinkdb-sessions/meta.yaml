package:
  name: django-rethinkdb-sessions
  version: "0.0.3"

source:
  fn: django-rethinkdb-sessions-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-rethinkdb-sessions/django-rethinkdb-sessions-0.0.3.tar.gz
  md5: 97b6ce43246d44e6adfe958c711452e3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-rethinkdb-sessions = django-rethinkdb-sessions:main
    #
    # Would create an entry point called django-rethinkdb-sessions that calls django-rethinkdb-sessions.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - rethinkdb >=1.7.0

  run:
    - python
    - rethinkdb >=1.7.0

test:
  # Python imports
  imports:
    - rethinkdb_sessions

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/maxpresman/django-rethinkdb-sessions
  license: MIT License
  summary: 'rethinkdb backed sessions for django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
