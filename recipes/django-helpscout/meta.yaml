package:
  name: django-helpscout
  version: "0.6.2"

source:
  fn: django-helpscout-0.6.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-helpscout/django-helpscout-0.6.2.tar.gz
  md5: f17968364f9b68559187116bbcd21307
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-helpscout = django-helpscout:main
    #
    # Would create an entry point called django-helpscout that calls django-helpscout.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - six

  run:
    - python
    - django
    - six

test:
  # Python imports
  imports:
    - django_helpscout

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/victorneo/Django-Helpscout
  license: Apache Software License
  summary: 'Help Scout integration for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
