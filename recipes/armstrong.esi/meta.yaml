package:
  name: armstrong.esi
  version: "1.1.0"

source:
  fn: armstrong.esi-1.1.0.tar.gz
  url: https://pypi.python.org/packages/source/a/armstrong.esi/armstrong.esi-1.1.0.tar.gz
  md5: 79ce5fb6b8c1f0f14e2de3a0a7c26372
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - armstrong.esi = armstrong.esi:main
    #
    # Would create an entry point called armstrong.esi that calls armstrong.esi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - armstrong.esi
    - armstrong.esi.templatetags
    - armstrong.esi.tests
    - armstrong.esi.tests.esi_support
    - armstrong.esi.tests.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/armstrong/armstrong.esi/
  license: Apache Software License
  summary: 'Utilities to help development of sites that utilize Edge Side Includes'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
