package:
  name: django-opensearch
  version: "0.1.2"

source:
  fn: django-opensearch-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-opensearch/django-opensearch-0.1.2.tar.gz
  md5: 7b0df0c0ba117f0a5c569d1f291b29b0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-opensearch = django-opensearch:main
    #
    # Would create an entry point called django-opensearch that calls django-opensearch.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - opensearch
    - opensearch.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/vint21h/django-opensearch
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'Handle opensearch.xml'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
