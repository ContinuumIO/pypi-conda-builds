package:
  name: css_image_concat
  version: "1.2"

source:
  fn: css_image_concat-1.2.tar.gz
  url: https://pypi.python.org/packages/source/c/css_image_concat/css_image_concat-1.2.tar.gz
  md5: 2d25499689a3759402794f685565430b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - css_image_concat = css_image_concat:main
    #
    # Would create an entry point called css_image_concat that calls css_image_concat.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - css_image_concat

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.amix.dk/
  license: MIT License
  summary: 'A script that can concat images into one image and create a CSS file'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
