package:
  name: bootstrap_env
  version: "0.4.4"

source:
  fn: bootstrap_env-0.4.4.tar.gz
  url: https://pypi.python.org/packages/source/b/bootstrap_env/bootstrap_env-0.4.4.tar.gz
  md5: 6936bdbe7a2ab5a8b2dc7c8b8ddfc7f9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bootstrap_env = bootstrap_env:main
    #
    # Would create an entry point called bootstrap_env that calls bootstrap_env.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - boot_bootstrap_env
    - boot_bootstrap_env.sources
    - bootstrap_env

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jedie/bootstrap_env
  license: GNU General Public License v3 or later (GPLv3+)
  summary: "Create a complete self contained virtualenv bootstrap file by enbed 'get-pip.py'"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
