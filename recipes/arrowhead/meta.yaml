package:
  name: arrowhead
  version: "1.0a1"

source:
  fn: arrowhead-1.0a1.tar.gz
  url: https://pypi.python.org/packages/source/a/arrowhead/arrowhead-1.0a1.tar.gz
  md5: 139c43fa2d2c71170e13edcb8ea094fd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - arrowhead = arrowhead:main
    #
    # Would create an entry point called arrowhead that calls arrowhead.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - arrowhead

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/zyga/arrowhead
  license: GNU Lesser General Public License v3 or later (LGPLv3+)
  summary: 'micro-framework for flowchart-like computing'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
