package:
  name: list_operations
  version: "0.1"

source:
  fn: list_operations.tar.gz
  url: https://pypi.python.org/packages/source/l/list_operations/list_operations.tar.gz
  md5: 5cd3dfa02da91f40ec881ede68001558
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - list_operations = list_operations:main
    #
    # Would create an entry point called list_operations that calls list_operations.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - list_operations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/Jana-A/list_operations/releases
  license: MIT License
  summary: 'Two functions performed on lists.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
