package:
  name: django-djconfig
  version: "0.4.0"

source:
  fn: django-djconfig-0.4.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-djconfig/django-djconfig-0.4.0.tar.gz
  md5: 7ba3582274ddcecca9cd2890cdb8af7a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-djconfig = django-djconfig:main
    #
    # Would create an entry point called django-djconfig that calls django-djconfig.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - djconfig
    - djconfig.migrations
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/nitely/django-djconfig
  license: MIT License
  summary: 'DjConfig is a Django app for storing other apps configurations.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
