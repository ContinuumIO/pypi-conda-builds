package:
  name: linketurbidity
  version: "0.0.4"

source:
  fn: linketurbidity-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/l/linketurbidity/linketurbidity-0.0.4.tar.gz
  md5: 86ba212fe0b3bc9dd4ec577cb9ae4a19
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - linketurbidity = linketurbidity:main
    #
    # Would create an entry point called linketurbidity that calls linketurbidity.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy ==1.8.0
    - mglob ==0.4
    - netcdf ==0.0.32
    - gdal ==1.11.1

  run:
    - python
    - numpy ==1.8.0
    - mglob ==0.4
    - netcdf ==0.0.32
    - gdal ==1.11.1

test:
  # Python imports
  imports:
    - linketurbidity

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/gersolar/linketurbidity
  license: MIT
  summary: 'A python library that simplify the access to the linketurbidity index.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
