package:
  name: django-timetable
  version: "0.1.7"

source:
  fn: django-timetable-0.1.7.tar.gz
  url: https://pypi.python.org/packages/source/d/django-timetable/django-timetable-0.1.7.tar.gz
  md5: 04653c80f3eb11ea784297e227f058de
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-timetable = django-timetable:main
    #
    # Would create an entry point called django-timetable that calls django-timetable.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - django-taggit
    - django-colorful

  run:
    - python
    - django
    - django-taggit
    - django-colorful

test:
  # Python imports
  imports:
    - timetable

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/g3rd/django-timetable
  license: MIT License
  summary: 'An Django app that provides generic calendar functions'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
