package:
  name: django-staticfilesplus
  version: "0.1dev"

source:
  fn: django-staticfilesplus-0.1dev.tar.gz
  url: https://pypi.python.org/packages/source/d/django-staticfilesplus/django-staticfilesplus-0.1dev.tar.gz
  md5: 2d8ad069d2f3e683cbfb5d90391ea800
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-staticfilesplus = django-staticfilesplus:main
    #
    # Would create an entry point called django-staticfilesplus that calls django-staticfilesplus.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - staticfilesplus

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://django-staticfilesplus.evans.io
  license: Apache Software License
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
