package:
  name: svetlyak40wt.recipe.symlinks
  version: "0.2.1"

source:
  fn: svetlyak40wt.recipe.symlinks-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/s/svetlyak40wt.recipe.symlinks/svetlyak40wt.recipe.symlinks-0.2.1.tar.gz
  md5: 679b7d519977e02bf476909d78e6e0f5
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - svetlyak40wt.recipe.symlinks = svetlyak40wt.recipe.symlinks:main
    #
    # Would create an entry point called svetlyak40wt.recipe.symlinks that calls svetlyak40wt.recipe.symlinks.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zc.buildout
    - zc.recipe.egg
    - setuptools

  run:
    - python
    - setuptools
    - zc.buildout
    - zc.recipe.egg
    - setuptools

test:
  # Python imports
  imports:
    - svetlyak40wt
    - svetlyak40wt.recipe

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/svetlyak40wt/svetlyak40wt.recipe.symlinks
  license: BSD License
  summary: 'Simple recipe to collect symbolic links in one directory.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
