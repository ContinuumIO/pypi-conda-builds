package:
  name: bluelake
  version: "1.1.1"

source:
  fn: bluelake-1.1.1.tar.gz
  url: https://pypi.python.org/packages/source/b/bluelake/bluelake-1.1.1.tar.gz
  md5: 526f58e11a6f398ba5c620505ac7c789
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bluelake = bluelake:main
    #
    # Would create an entry point called bluelake that calls bluelake.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests ==2.5.3

  run:
    - python
    - requests ==2.5.3

test:
  # Python imports
  imports:
    - bluelake
    - bluelake.client
    - bluelake.protocol
    - bluelake.server

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'Bluelake python client.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
