package:
  name: testdef
  version: "0.1"

source:
  fn: testdef-0.1.tar.gz
  url: https://pypi.python.org/packages/source/t/testdef/testdef-0.1.tar.gz
  md5: bbb77b0766866dd7be4fdb8733205bb0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - testdef = testdef:main
    #
    # Would create an entry point called testdef that calls testdef.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - testdef

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/zyga/testdef
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Testdef is an emerging test definition interchange intended for storing and describing tests in a public repository.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
