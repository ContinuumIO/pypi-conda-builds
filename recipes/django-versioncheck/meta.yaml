package:
  name: django-versioncheck
  version: "0.0.2"

source:
  fn: django-versioncheck-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-versioncheck/django-versioncheck-0.0.2.tar.gz
  md5: 53e002b39feb05a5c063e29c036fea32
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-versioncheck = django-versioncheck:main
    #
    # Would create an entry point called django-versioncheck that calls django-versioncheck.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - versioncheck
    - versioncheck.management
    - versioncheck.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/noxan/django-versioncheck
  license: BSD License
  summary: 'A small django app which tries to be annoying if your django version is outdated.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
