package:
  name: daemonized
  version: "0.3.0"

source:
  fn: daemonized-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/d/daemonized/daemonized-0.3.0.tar.gz
  md5: df5a79fcc71403093dcdc5b0413cb931
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - daemonized = daemonized:main
    #
    # Would create an entry point called daemonized that calls daemonized.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - daemonized

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/yueyoum/daemonize
  license: UNKNOWN
  summary: 'A Module to help you daemonize your program in Unix-like system'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
