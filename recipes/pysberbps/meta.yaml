package:
  name: pysberbps
  version: "0.0.1.dev1"

source:
  fn: pysberbps-0.0.1.dev1.tar.gz
  url: https://pypi.python.org/packages/source/p/pysberbps/pysberbps-0.0.1.dev1.tar.gz
  md5: a33250b4b52cf8784386e6eabeff7824
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pysberbps = pysberbps:main
    #
    # Would create an entry point called pysberbps that calls pysberbps.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - pysberbps

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mnach/pysberbank
  license: BSD License
  summary: 'Wrapper over Sberbank Acquiring API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
