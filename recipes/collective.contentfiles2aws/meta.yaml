package:
  name: collective.contentfiles2aws
  version: "1.3.0"

source:
  fn: collective.contentfiles2aws-1.3.0.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.contentfiles2aws/collective.contentfiles2aws-1.3.0.tar.gz
  md5: 2f8f18592839b998f7f970a6741c82c8
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.contentfiles2aws = collective.contentfiles2aws:main
    #
    # Would create an entry point called collective.contentfiles2aws that calls collective.contentfiles2aws.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - boto

  run:
    - python
    - setuptools
    - setuptools
    - boto

test:
  # Python imports
  imports:
    - collective
    - collective.contentfiles2aws
    - collective.contentfiles2aws.browser
    - collective.contentfiles2aws.client
    - collective.contentfiles2aws.client.interfaces
    - collective.contentfiles2aws.content
    - collective.contentfiles2aws.interfaces
    - collective.contentfiles2aws.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/martinschoel/collective.contentfiles2aws.git
  license: GPL
  summary: 'Allows to store files and images on amazon s3 service.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
