package:
  name: hancock
  version: "1.1.2"

source:
  fn: hancock-1.1.2.tar.gz
  url: https://pypi.python.org/packages/source/h/hancock/hancock-1.1.2.tar.gz
  md5: 41e4b4dd9fc91ca6df38ff59d4abfd31
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hancock = hancock:main
    #
    # Would create an entry point called hancock that calls hancock.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=2.3.0

  run:
    - python
    - requests >=2.3.0

test:
  # Python imports
  imports:
    - hancock

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/hancock
  license: BSD License
  summary: 'HTTP request signing library.Provides private key URL signing for secure HTTP requests.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
