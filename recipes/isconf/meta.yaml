package:
  name: isconf
  version: "4.2.8.253"

source:
  fn: isconf-4.2.8.253.tar.gz
  url: https://pypi.python.org/packages/source/i/isconf/isconf-4.2.8.253.tar.gz
  md5: 6ba8027bf6d3f8e999b7c4a24532405f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - isconf = isconf:main
    #
    # Would create an entry point called isconf that calls isconf.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - isconf

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/stevegt/isconf4
  license: GNU General Public License v2 (GPLv2)
  summary: 'Infrastructure configuration management tool'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
