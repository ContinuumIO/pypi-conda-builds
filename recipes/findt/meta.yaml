package:
  name: findt
  version: "3.0.0"

source:
  fn: FinDt-3.0.0.zip
  url: https://pypi.python.org/packages/source/F/FinDt/FinDt-3.0.0.zip
  md5: 081184f8059e98852a01a03e59184f96
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - findt = findt:main
    #
    # Would create an entry point called findt that calls findt.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - findt

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://matfinanpython.blogspot.com.br/
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'M\xc3\xb3dulo em Lingua Portuguesa contendo um conjunto de fun\xc3\xa7\xc3\xb5es cujo principal objetivo \xc3\xa9 facilitar o trabalho com datas, voltadas, principalmente, para o mercado financerio do Brasil.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
