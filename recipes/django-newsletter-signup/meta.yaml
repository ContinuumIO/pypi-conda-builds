package:
  name: django-newsletter-signup
  version: "0.2"

source:
  fn: django-newsletter-signup-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-newsletter-signup/django-newsletter-signup-0.2.tar.gz
  md5: 119909752ded83656368dfad6a5d9a3d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-newsletter-signup = django-newsletter-signup:main
    #
    # Would create an entry point called django-newsletter-signup that calls django-newsletter-signup.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - django-extensions
    - django-mailer
    - django-libs

  run:
    - python
    - django
    - django-extensions
    - django-mailer
    - django-libs

test:
  # Python imports
  imports:
    - newsletter_signup
    - newsletter_signup.management
    - newsletter_signup.management.commands
    - newsletter_signup.migrations
    - newsletter_signup.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bitmazk/django-newsletter-signup
  license: The MIT License
  summary: 'A reusable Django app, that handles newsletter subscriptions\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
