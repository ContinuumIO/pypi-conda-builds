package:
  name: asyncoro
  version: "3.2"

source:
  fn: asyncoro-3.2.tar.gz
  url: https://pypi.python.org/packages/source/a/asyncoro/asyncoro-3.2.tar.gz
  md5: 8f39c011f32b9e40b8a57b5d100c95bd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - asyncoro = asyncoro:main
    #
    # Would create an entry point called asyncoro that calls asyncoro.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - asyncoro

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://asyncoro.sourceforge.net
  license: MIT License
  summary: 'Python framework for concurrent, distributed, asynchronous network programming with coroutines, asynchronous completions and message passing.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
