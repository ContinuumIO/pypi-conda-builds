package:
  name: marshmallow-sqlalchemy
  version: "0.2.0"

source:
  fn: marshmallow-sqlalchemy-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/m/marshmallow-sqlalchemy/marshmallow-sqlalchemy-0.2.0.tar.gz
  md5: c2860dde6def020f765ed7e3a0ca78a5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - marshmallow-sqlalchemy = marshmallow-sqlalchemy:main
    #
    # Would create an entry point called marshmallow-sqlalchemy that calls marshmallow-sqlalchemy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - marshmallow >=2.0.0b1
    - sqlalchemy >=0.7

  run:
    - python
    - marshmallow >=2.0.0b1
    - sqlalchemy >=0.7

test:
  # Python imports
  imports:
    - marshmallow_sqlalchemy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/marshmallow-code/marshmallow-sqlalchemy
  license: MIT License
  summary: 'SQLAlchemy integration with the marshmallow (de)serialization library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
