package:
  name: django-site-status
  version: "0.1.4"

source:
  fn: django-site-status-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-site-status/django-site-status-0.1.4.tar.gz
  md5: 2349b26705747f3a04c5cee14e2edff9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-site-status = django-site-status:main
    #
    # Would create an entry point called django-site-status that calls django-site-status.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - site_status

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/changecoin/django-site-status
  license: BSD License
  summary: 'A simple Django app to issue site wide alerts'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
