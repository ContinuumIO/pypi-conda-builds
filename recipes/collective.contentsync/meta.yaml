package:
  name: collective.contentsync
  version: "1.01"

source:
  fn: collective.contentsync-1.01.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.contentsync/collective.contentsync-1.01.tar.gz
  md5: 7a5139886ab07265ef896adaebae0afc
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.contentsync = collective.contentsync:main
    #
    # Would create an entry point called collective.contentsync that calls collective.contentsync.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.contentsync
    - collective.contentsync.browser
    - collective.contentsync.browser.resources
    - collective.contentsync.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: GPL
  summary: 'Synchronize a container with a master container'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
