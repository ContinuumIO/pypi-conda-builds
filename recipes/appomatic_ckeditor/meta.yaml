package:
  name: appomatic_ckeditor
  version: "0.0.30"

source:
  fn: appomatic_ckeditor-0.0.30.tar.gz
  url: https://pypi.python.org/packages/source/a/appomatic_ckeditor/appomatic_ckeditor-0.0.30.tar.gz
  md5: b1a4ca9c8fe94a72292fcc8804646b61
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - appomatic_ckeditor = appomatic_ckeditor:main
    #
    # Would create an entry point called appomatic_ckeditor that calls appomatic_ckeditor.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-ckeditor
    - appomaticcore ==0.0.30

  run:
    - python
    - django-ckeditor
    - appomaticcore ==0.0.30

test:
  # Python imports
  imports:
    - appomatic_ckeditor

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/shaunsephton/django-ckeditor
  license: GPL
  summary: 'Django admin CKEditor integration.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
