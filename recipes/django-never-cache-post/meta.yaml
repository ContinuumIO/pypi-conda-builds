package:
  name: django-never-cache-post
  version: "0.1.1"

source:
  fn: django-never-cache-post-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-never-cache-post/django-never-cache-post-0.1.1.tar.gz
  md5: 0255d407a9d567869f699ee426de6f53
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-never-cache-post = django-never-cache-post:main
    #
    # Would create an entry point called django-never-cache-post that calls django-never-cache-post.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - never_cache_post

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/incuna/django-never-cache-post/
  license: UNKNOWN
  summary: 'Ensure that (iOS 6) never caches the result of an ajax POST again.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
