package:
  name: collective.local.deploygroup
  version: "1.0"

source:
  fn: collective.local.deploygroup-1.0.zip
  url: https://pypi.python.org/packages/source/c/collective.local.deploygroup/collective.local.deploygroup-1.0.zip
  md5: decf542368eee142ebdb1c4ea03592e8
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.local.deploygroup = collective.local.deploygroup:main
    #
    # Would create an entry point called collective.local.deploygroup that calls collective.local.deploygroup.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.workflow

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.workflow

test:
  # Python imports
  imports:
    - collective
    - collective.local
    - collective.local.deploygroup
    - collective.local.deploygroup.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/collective.local.deploygroup
  license: GPL
  summary: 'Allows to deploy a group from the sharing view. By Ecreall'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
