package:
  name: gaphor.plugins.helloworld
  version: "0.1"

source:
  fn: gaphor.plugins.helloworld-0.1.tar.gz
  url: https://pypi.python.org/packages/source/g/gaphor.plugins.helloworld/gaphor.plugins.helloworld-0.1.tar.gz
  md5: 43fdd0776dc4e326b90f27c92f8821b6
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gaphor.plugins.helloworld = gaphor.plugins.helloworld:main
    #
    # Would create an entry point called gaphor.plugins.helloworld that calls gaphor.plugins.helloworld.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - gaphor >=0.11.0

  run:
    - python
    - setuptools
    - gaphor >=0.11.0

test:
  # Python imports
  imports:
    - gaphor
    - gaphor.plugins
    - gaphor.plugins.helloworld

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: GPL
  summary: 'Example (Hello World) service/plugin for Gaphor'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
