package:
  name: globchecksum
  version: "1.0.1"

source:
  fn: globchecksum-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/g/globchecksum/globchecksum-1.0.1.tar.gz
  md5: 1113127f93488a4329c3e32892b1cfd7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - globchecksum = globchecksum:main
    #
    # Would create an entry point called globchecksum that calls globchecksum.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - globchecksum

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/alienone/Python/tree/master/globchecksum
  license: UNKNOWN
  summary: 'MD5 checksum glob of files @ $PATH'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
