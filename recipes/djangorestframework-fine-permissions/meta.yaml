package:
  name: djangorestframework-fine-permissions
  version: "0.5.1"

source:
  fn: djangorestframework-fine-permissions-0.5.1.tar.gz
  url: https://pypi.python.org/packages/source/d/djangorestframework-fine-permissions/djangorestframework-fine-permissions-0.5.1.tar.gz
  md5: 456958e1204776d2ca27c0d2a17130c6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangorestframework-fine-permissions = djangorestframework-fine-permissions:main
    #
    # Would create an entry point called djangorestframework-fine-permissions that calls djangorestframework-fine-permissions.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - djangorestframework >=3.0,<3.1
    - six

  run:
    - python
    - djangorestframework >=3.0,<3.1
    - six

test:
  # Python imports
  imports:
    - rest_framework_fine_permissions
    - rest_framework_fine_permissions.management
    - rest_framework_fine_permissions.management.commands
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/unistra/django-rest-framework-fine-permissions
  license: GNU General Public License v2 (GPLv2)
  summary: 'Field level permissions for Django REST Framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
