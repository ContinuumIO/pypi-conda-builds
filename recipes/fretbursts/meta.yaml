package:
  name: fretbursts
  version: "0.4rc5"

source:
  fn: fretbursts-0.4rc5.zip
  url: https://pypi.python.org/packages/source/f/fretbursts/fretbursts-0.4rc5.zip
  md5: 6c8fd936f30b4f9678756e5aeff18c7f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fretbursts = fretbursts:main
    #
    # Would create an entry point called fretbursts that calls fretbursts.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy
    - scipy
    - matplotlib
    - lmfit
    - seaborn

  run:
    - python
    - numpy
    - scipy
    - matplotlib
    - lmfit
    - seaborn

test:
  # Python imports
  imports:
    - fretbursts
    - fretbursts.burstsearch
    - fretbursts.dataload
    - fretbursts.fit
    - fretbursts.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/tritemio/FRETBursts/
  license: GPLv2
  summary: 'Burst analysis toolkit for single and multi-spot smFRET data.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
