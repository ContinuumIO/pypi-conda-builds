package:
  name: couleur
  version: "0.5.0"

source:
  fn: couleur-0.5.0.tar.gz
  url: https://pypi.python.org/packages/source/c/couleur/couleur-0.5.0.tar.gz
  md5: dbab00618b63e5c4a8043a535bd73aa2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - couleur = couleur:main
    #
    # Would create an entry point called couleur that calls couleur.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - couleur

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/gabrielfalcao/couleur
  license: Apache Software License
  summary: u'ANSI terminal tool for python, colored shell and other handy fancy features'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
