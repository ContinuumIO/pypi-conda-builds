package:
  name: isotoma.recipe.pound
  version: "0.1.13"

source:
  fn: isotoma.recipe.pound-0.1.13.zip
  url: https://pypi.python.org/packages/source/i/isotoma.recipe.pound/isotoma.recipe.pound-0.1.13.zip
  md5: e765b4045f32afe0696e87b6dbda1c9b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - isotoma.recipe.pound = isotoma.recipe.pound:main
    #
    # Would create an entry point called isotoma.recipe.pound that calls isotoma.recipe.pound.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - cheetah
    - isotoma.recipe.gocaptain
    - missingbits >=0.0.14

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - cheetah
    - isotoma.recipe.gocaptain
    - missingbits >=0.0.14

test:
  # Python imports
  imports:
    - isotoma
    - isotoma.recipe
    - isotoma.recipe.pound

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/isotoma.recipe.pound
  license: Apache Software License
  summary: 'Buildout recipes to configure the pound load balancer'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
