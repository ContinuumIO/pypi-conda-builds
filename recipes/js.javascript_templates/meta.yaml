package:
  name: js.javascript_templates
  version: "2.1.0"

source:
  fn: js.javascript_templates-2.1.0.tar.gz
  url: https://pypi.python.org/packages/source/j/js.javascript_templates/js.javascript_templates-2.1.0.tar.gz
  md5: 428a95ffddf92c7456e93b610063bfb8
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - js.javascript_templates = js.javascript_templates:main
    #
    # Would create an entry point called js.javascript_templates that calls js.javascript_templates.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fanstatic
    - setuptools

  run:
    - python
    - setuptools
    - fanstatic
    - setuptools

test:
  # Python imports
  imports:
    - js
    - js.javascript_templates

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD
  summary: 'Fanstatic packaging of JavaScript Templates'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
