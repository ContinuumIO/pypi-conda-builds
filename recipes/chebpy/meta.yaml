package:
  name: chebpy
  version: "0.2"

source:
  fn: chebpy-0.2.tar.gz
  url: https://pypi.python.org/packages/source/c/chebpy/chebpy-0.2.tar.gz
  md5: a0752e75716369caecd37412384dff05
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - chebpy = chebpy:main
    #
    # Would create an entry point called chebpy that calls chebpy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy
    - scipy

  run:
    - python
    - numpy
    - scipy

test:
  # Python imports
  imports:
    - chebpy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/liuyxpp/chebpy
  license: BSD License
  summary: 'Chebyshev polynomial based spectral methods of PDEs.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
