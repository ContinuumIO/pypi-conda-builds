package:
  name: upfront.simplereferencefield
  version: "0.5.1"

source:
  fn: upfront.simplereferencefield-0.5.1.tar.gz
  url: https://pypi.python.org/packages/source/u/upfront.simplereferencefield/upfront.simplereferencefield-0.5.1.tar.gz
  md5: ce73951ab262d8cf3abaff648c79ebb9
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - upfront.simplereferencefield = upfront.simplereferencefield:main
    #
    # Would create an entry point called upfront.simplereferencefield that calls upfront.simplereferencefield.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - upfront
    - upfront.simplereferencefield

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/upfront.simplereferencefield
  license: GPL
  summary: 'Store references on an object and not in the reference catalog'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
