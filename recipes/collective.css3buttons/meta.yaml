package:
  name: collective.css3buttons
  version: "0.1"

source:
  fn: collective.css3buttons-0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.css3buttons/collective.css3buttons-0.1.tar.gz
  md5: 75b40fa4c77927cf3c73790ef58daa2c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.css3buttons = collective.css3buttons:main
    #
    # Would create an entry point called collective.css3buttons that calls collective.css3buttons.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.css3buttons
    - collective.css3buttons.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: Unlicense
  summary: 'Simple framework for creating button links'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
