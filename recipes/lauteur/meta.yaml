package:
  name: lauteur
  version: "0.0.6"

source:
  fn: lauteur-0.0.6.tar.gz
  url: https://pypi.python.org/packages/source/l/lauteur/lauteur-0.0.6.tar.gz
  md5: 51d8b511abc9d7a38024ae4ecf90ef10
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lauteur = lauteur:main
    #
    # Would create an entry point called lauteur that calls lauteur.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - beautifulsoup4 ==4.3.2

  run:
    - python
    - beautifulsoup4 ==4.3.2

test:
  # Python imports
  imports:
    - lauteur

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/newslynx/lauteur
  license: MIT License
  summary: 'Tools for ascribing authorship - to the chagrin of Barthes'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
