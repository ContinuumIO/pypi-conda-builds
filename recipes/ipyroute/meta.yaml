package:
  name: ipyroute
  version: "0.0.26"

source:
  fn: ipyroute-0.0.26.tar.gz
  url: https://pypi.python.org/packages/source/i/ipyroute/ipyroute-0.0.26.tar.gz
  md5: 708ddd5ea17c182fd88e36115d8c6ff8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ipyroute = ipyroute:main
    #
    # Would create an entry point called ipyroute that calls ipyroute.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sh
    - netaddr
    - six

  run:
    - python
    - sh
    - netaddr
    - six

test:
  # Python imports
  imports:
    - ipyroute

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jta/ipyroute
  license: MIT License
  summary: 'Yet another interface for iproute2'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
