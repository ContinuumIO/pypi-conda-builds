package:
  name: cim2busbranch
  version: "0.1"

source:
  fn: Cim2BusBranch-0.1.tar.gz
  url: https://pypi.python.org/packages/source/C/Cim2BusBranch/Cim2BusBranch-0.1.tar.gz
  md5: 5e41f2f8be2e720db38980c2e0b8a188
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cim2busbranch = cim2busbranch:main
    #
    # Would create an entry point called cim2busbranch that calls cim2busbranch.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - pycim >=15.13.3

  run:
    - python
    - pycim >=15.13.3

test:
  # Python imports
  imports:
    - cim2busbranch

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/ssc/cim2busbranch/
  license: BSD License
  summary: 'Tansforms CIM topologies into a bus/branch model for load flow analyses'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
