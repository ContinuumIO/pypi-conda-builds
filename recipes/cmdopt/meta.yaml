package:
  name: cmdopt
  version: "0.2.0"

source:
  fn: Cmdopt-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/C/Cmdopt/Cmdopt-0.2.0.tar.gz
  md5: 0b1a2d36e9466e8727ce30965205694e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cmdopt = cmdopt:main
    #
    # Would create an entry point called cmdopt that calls cmdopt.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - picotest >=0.2.0

  run:
    - python
    - picotest >=0.2.0

test:
  # Python imports
  imports:
    - cmdopt

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/Cmdopt
  license: MIT License
  summary: 'pretty good command-line option parser'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
