package:
  name: anybox.testing.openerp
  version: "1.3.1"

source:
  fn: anybox.testing.openerp-1.3.1.tar.gz
  url: https://pypi.python.org/packages/source/a/anybox.testing.openerp/anybox.testing.openerp-1.3.1.tar.gz
  md5: 6859a7f1f6c1a5d78e7d0ca8b6b83443
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - anybox.testing.openerp = anybox.testing.openerp:main
    #
    # Would create an entry point called anybox.testing.openerp that calls anybox.testing.openerp.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - anybox
    - anybox.testing
    - anybox.testing.openerp
    - anybox.testing.openerp.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/anybox/anybox.testing.openerp
  license: AGPL v3+
  summary: 'Useful testing base classes and tools for OpenERP'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
