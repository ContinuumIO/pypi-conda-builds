package:
  name: django-feedburner
  version: "0.9.0"

source:
  fn: django-feedburner-0.9.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-feedburner/django-feedburner-0.9.0.tar.gz
  md5: ae39c353c86518341cae2e27ca3a6220
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-feedburner = django-feedburner:main
    #
    # Would create an entry point called django-feedburner that calls django-feedburner.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - django_feedburner
    - django_feedburner.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://launchpad.net/django-feedburner
  license: BSD License
  summary: 'Handles redirection of django feeds to feedburner urls.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
