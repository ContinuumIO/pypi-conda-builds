package:
  name: habitat
  version: "0.2"

source:
  fn: habitat-0.2.tar.gz
  url: https://pypi.python.org/packages/source/h/habitat/habitat-0.2.tar.gz
  md5: ff937b195c93d7c2c6d1ffdb521229c6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - habitat = habitat:main
    #
    # Would create an entry point called habitat that calls habitat.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - m2crypto >=0.21.1
    - couchdbkit >=0.5.4
    - crcmod >=1.7
    - ipaddr >=2.1.9

  run:
    - python
    - m2crypto >=0.21.1
    - couchdbkit >=0.5.4
    - crcmod >=1.7
    - ipaddr >=2.1.9

test:
  # Python imports
  imports:
    - habitat
    - habitat.parser_modules
    - habitat.sensors
    - habitat.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://habitat.habhub.org/
  license: GNU General Public License Version 3
  summary: 'Next Generation High Altitude Balloon Tracking'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
