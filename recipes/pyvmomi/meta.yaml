package:
  name: pyvmomi
  version: "5.5.0.2014.1.1"

source:
  fn: pyvmomi-5.5.0.2014.1.1.tar.gz
  url: https://pypi.python.org/packages/source/p/pyvmomi/pyvmomi-5.5.0.2014.1.1.tar.gz
  md5: e9c544d2aa1c6cae501d78880bfa77f7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pyvmomi = pyvmomi:main
    #
    # Would create an entry point called pyvmomi that calls pyvmomi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=2.3.0
    - six >=1.7.3

  run:
    - python
    - requests >=2.3.0
    - six >=1.7.3

test:
  # Python imports
  imports:
    - pyVim
    - pyVmomi
    - pyvmomi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/vmware/pyvmomi
  license: Apache Software License
  summary: 'VMware vSphere Python SDK'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
