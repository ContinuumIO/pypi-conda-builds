package:
  name: slc.rdbploneformgenadapter
  version: "0.0.2"

source:
  fn: slc.rdbploneformgenadapter-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/s/slc.rdbploneformgenadapter/slc.rdbploneformgenadapter-0.0.2.tar.gz
  md5: db3db379064cd00f5ce0132e150a3e5a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - slc.rdbploneformgenadapter = slc.rdbploneformgenadapter:main
    #
    # Would create an entry point called slc.rdbploneformgenadapter that calls slc.rdbploneformgenadapter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - collective.lead
    - products.ploneformgen
    - setuptools
    - plone

  run:
    - python
    - setuptools
    - collective.lead
    - products.ploneformgen
    - setuptools
    - plone

test:
  # Python imports
  imports:
    - slc
    - slc/rdbploneformgenadapter

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/slc.rdbploneformgenadapter
  license: GNU General Public License (GPL)
  summary: u'Provides an Action Adapter for PloneFormGen to store data in an RDB'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
