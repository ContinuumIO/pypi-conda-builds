package:
  name: pypi_agreement
  version: "1.0.0"

source:
  fn: pypi_agreement-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/p/pypi_agreement/pypi_agreement-1.0.0.tar.gz
  md5: c17500d2fce217e492160b615f53d7c2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pypi_agreement = pypi_agreement:main
    #
    # Would create an entry point called pypi_agreement that calls pypi_agreement.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - pypi_agreement

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.jeffery1st.com
  license: UNKNOWN
  summary: 'The agreement of pypi register'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
