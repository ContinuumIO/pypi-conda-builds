package:
  name: js.angular_ui
  version: "0.4.0"

source:
  fn: js.angular_ui-0.4.0.tar.gz
  url: https://pypi.python.org/packages/source/j/js.angular_ui/js.angular_ui-0.4.0.tar.gz
  md5: fc6f534be7120b4c62527e35726314ca
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - js.angular_ui = js.angular_ui:main
    #
    # Would create an entry point called js.angular_ui that calls js.angular_ui.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fanstatic
    - js.angular
    - setuptools

  run:
    - python
    - setuptools
    - fanstatic
    - js.angular
    - setuptools

test:
  # Python imports
  imports:
    - js
    - js.angular_ui

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT
  summary: 'Fanstatic packaging of AngularUI'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
