package:
  name: collective.gtags
  version: "1.0b2"

source:
  fn: collective.gtags-1.0b2.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.gtags/collective.gtags-1.0b2.tar.gz
  md5: 0de3c973abe5f33052fd1e64c2dfa6c0
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.gtags = collective.gtags:main
    #
    # Would create an entry point called collective.gtags that calls collective.gtags.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone
    - plone.behavior >=1.0b5
    - plone.registry >=1.0a2
    - plone.app.registry >=1.0a2
    - plone.z3cform
    - z3c.form
    - plone.formwidget.autocomplete
    - plone.directives.form >=1.0b3
    - zope.schema
    - zope.interface
    - zope.component
    - rwproperty

  run:
    - python
    - setuptools
    - setuptools
    - plone
    - plone.behavior >=1.0b5
    - plone.registry >=1.0a2
    - plone.app.registry >=1.0a2
    - plone.z3cform
    - z3c.form
    - plone.formwidget.autocomplete
    - plone.directives.form >=1.0b3
    - zope.schema
    - zope.interface
    - zope.component
    - rwproperty

test:
  # Python imports
  imports:
    - collective
    - collective.gtags
    - collective.gtags.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/collective.gtags
  license: GPL
  summary: 'Google Code like tagging for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
