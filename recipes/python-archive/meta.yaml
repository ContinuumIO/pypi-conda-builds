package:
  name: python-archive
  version: "0.2"

source:
  fn: python-archive-0.2.tar.gz
  url: https://pypi.python.org/packages/source/p/python-archive/python-archive-0.2.tar.gz
  md5: d2abf4c71fd28817b0a5d27ff874470d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-archive = python-archive:main
    #
    # Would create an entry point called python-archive that calls python-archive.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - archive
    - archive.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/gdub/python-archive
  license: MIT License
  summary: 'Simple library that provides a common interface for extracting zip and tar archives.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
