package:
  name: dirtywords
  version: "0.0.2"

source:
  fn: dirtywords-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/dirtywords/dirtywords-0.0.2.tar.gz
  md5: 05665de03cf29f1fee3f497e463086ee
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dirtywords = dirtywords:main
    #
    # Would create an entry point called dirtywords that calls dirtywords.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - dirtywords

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/xi/dirtywords
  license: GNU General Public License v2 or later (GPLv2+)
  summary: 'portable text interface framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
