package:
  name: bdist_nsi
  version: "0.1.5"

source:
  fn: bdist_nsi-0.1.5.zip
  url: https://pypi.python.org/packages/source/b/bdist_nsi/bdist_nsi-0.1.5.zip
  md5: bd96c2903b575b2f7c799dfebbfc144f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bdist_nsi = bdist_nsi:main
    #
    # Would create an entry point called bdist_nsi that calls bdist_nsi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - bdist_nsi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bdist-nsi.sourceforge.net/
  license: BSD License
  summary: 'Create NSIS windows installers for Python modules.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
