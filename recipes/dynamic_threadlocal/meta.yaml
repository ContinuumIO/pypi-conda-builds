package:
  name: dynamic_threadlocal
  version: "0.1"

source:
  fn: dynamic_threadlocal-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/dynamic_threadlocal/dynamic_threadlocal-0.1.tar.gz
  md5: eee889a90e0af398af4bdd2a3f9c4337
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dynamic_threadlocal = dynamic_threadlocal:main
    #
    # Would create an entry point called dynamic_threadlocal that calls dynamic_threadlocal.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - dynamic_threadlocal

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: GPL
  summary: 'Dynamic scoping using thread-local scoping'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
