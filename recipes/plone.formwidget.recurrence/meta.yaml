package:
  name: plone.formwidget.recurrence
  version: "2.0"

source:
  fn: plone.formwidget.recurrence-2.0.tar.gz
  url: https://pypi.python.org/packages/source/p/plone.formwidget.recurrence/plone.formwidget.recurrence-2.0.tar.gz
  md5: 3203cad0391e6f0fdaa6f65b71180137
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.formwidget.recurrence = plone.formwidget.recurrence:main
    #
    # Would create an entry point called plone.formwidget.recurrence that calls plone.formwidget.recurrence.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.cmfcore
    - products.cmfplone
    - python-dateutil
    - zope.component
    - zope.i18n
    - zope.i18nmessageid
    - zope.interface

  run:
    - python
    - setuptools
    - setuptools
    - products.cmfcore
    - products.cmfplone
    - python-dateutil
    - zope.component
    - zope.i18n
    - zope.i18nmessageid
    - zope.interface

test:
  # Python imports
  imports:
    - plone
    - plone.formwidget
    - plone.formwidget.recurrence
    - plone.formwidget.recurrence.at
    - plone.formwidget.recurrence.browser
    - plone.formwidget.recurrence.tests
    - plone.formwidget.recurrence.tests.examples
    - plone.formwidget.recurrence.z3cform

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/plone/plone.formwidget.recurrence
  license: GPL
  summary: 'Recurrence widget for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
