package:
  name: britney-utils
  version: "0.1.2"

source:
  fn: britney-utils-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/b/britney-utils/britney-utils-0.1.2.tar.gz
  md5: 8345b9f68bac51eac991b0d59667ac5f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - britney-utils = britney-utils:main
    #
    # Would create an entry point called britney-utils that calls britney-utils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/unistra/britney-utils
  license: BSD License
  summary: 'Utilities for the Python SPORE client called Britney'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
