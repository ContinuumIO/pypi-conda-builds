package:
  name: liteview-ups
  version: "1.0.0"

source:
  fn: liteview-ups-1.0.0.zip
  url: https://pypi.python.org/packages/source/l/liteview-ups/liteview-ups-1.0.0.zip
  md5: 5078dc840235ad3d0763621beaa3ec0b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - liteview-ups = liteview-ups:main
    #
    # Would create an entry point called liteview-ups that calls liteview-ups.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - suds

  run:
    - python
    - suds

test:
  # Python imports
  imports:
    - ups
    - ups.services

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/python-ups/
  license: GNU General Public License (GPL)
  summary: 'UPS Web Services API wrapper.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
