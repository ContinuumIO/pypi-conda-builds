package:
  name: plone.app.versioningbehavior
  version: "1.2.1"

source:
  fn: plone.app.versioningbehavior-1.2.1.zip
  url: https://pypi.python.org/packages/source/p/plone.app.versioningbehavior/plone.app.versioningbehavior-1.2.1.zip
  md5: beecc0a043840349162428e086b636a9
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.app.versioningbehavior = plone.app.versioningbehavior:main
    #
    # Would create an entry point called plone.app.versioningbehavior that calls plone.app.versioningbehavior.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - plone.app.dexterity[relations]
    - plone.autoform
    - plone.dexterity
    - plone.namedfile[blobs]
    - products.cmfeditions >2.2.9
    - setuptools
    - zope.container

  run:
    - python
    - setuptools
    - plone.app.dexterity[relations]
    - plone.autoform
    - plone.dexterity
    - plone.namedfile[blobs]
    - products.cmfeditions >2.2.9
    - setuptools
    - zope.container

test:
  # Python imports
  imports:
    - plone
    - plone.app
    - plone.app.versioningbehavior
    - plone.app.versioningbehavior.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/dexterity
  license: GNU General Public License (GPL)
  summary: 'Provides a behavior for using CMFEditions with dexterity content types'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
