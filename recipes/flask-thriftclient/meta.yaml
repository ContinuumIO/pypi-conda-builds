package:
  name: flask-thriftclient
  version: "0.2.0"

source:
  fn: Flask-ThriftClient-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-ThriftClient/Flask-ThriftClient-0.2.0.tar.gz
  md5: 2afbecf6463633de8cedf803948746cd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-thriftclient = flask-thriftclient:main
    #
    # Would create an entry point called flask-thriftclient that calls flask-thriftclient.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.7
    - thrift >=0.8

  run:
    - python
    - flask >=0.7
    - thrift >=0.8

test:
  # Python imports
  imports:
    - flask_thriftclient

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/chub/flask-thriftclient
  license: BSD License
  summary: 'Adds thrift client support to your Flask application'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
