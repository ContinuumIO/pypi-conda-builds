package:
  name: flake8-coding
  version: "1.0.1"

source:
  fn: flake8-coding-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/f/flake8-coding/flake8-coding-1.0.1.tar.gz
  md5: 4fb5555c2dcec3d36f28625c34d75d36
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flake8-coding = flake8-coding:main
    #
    # Would create an entry point called flake8-coding that calls flake8-coding.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flake8

  run:
    - python
    - setuptools
    - flake8

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tk0miya/flake8-coding
  license: Apache License 2.0
  summary: 'Adds coding magic comment checks to flake8'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
