package:
  name: zerovm-sphinx-theme
  version: "1.1"

source:
  fn: zerovm-sphinx-theme-1.1.tar.gz
  url: https://pypi.python.org/packages/source/z/zerovm-sphinx-theme/zerovm-sphinx-theme-1.1.tar.gz
  md5: 1b7b8318e83ccdf833abec6344e789da
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zerovm-sphinx-theme = zerovm-sphinx-theme:main
    #
    # Would create an entry point called zerovm-sphinx-theme that calls zerovm-sphinx-theme.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - zerovm_sphinx_theme

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/zerovm/sphinx-theme
  license: UNKNOWN
  summary: 'ZeroVM theme for Sphinx'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
