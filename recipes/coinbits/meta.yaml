package:
  name: coinbits
  version: "0.3"

source:
  fn: coinbits-0.3.tar.gz
  url: https://pypi.python.org/packages/source/c/coinbits/coinbits-0.3.tar.gz
  md5: a8f3f40f2c72d21a157efd7f5b84ae67
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - coinbits = coinbits:main
    #
    # Would create an entry point called coinbits that calls coinbits.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - ecdsa >=0.13

  run:
    - python
    - ecdsa >=0.13

test:
  # Python imports
  imports:
    - coinbits
    - coinbits.protocol
    - coinbits.tests
    - coinbits.txns
    - coinbits.txns.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/8468/coinbits
  license: MIT
  summary: 'A Python library for bitcoin peer to peer communication'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
