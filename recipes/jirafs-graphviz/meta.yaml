package:
  name: jirafs-graphviz
  version: "1.1"

source:
  fn: jirafs-graphviz-1.1.tar.gz
  url: https://pypi.python.org/packages/source/j/jirafs-graphviz/jirafs-graphviz-1.1.tar.gz
  md5: a8c434d5a11a6ff0f079a54327288eed
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jirafs-graphviz = jirafs-graphviz:main
    #
    # Would create an entry point called jirafs-graphviz that calls jirafs-graphviz.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six

  run:
    - python
    - setuptools
    - six

test:
  # Python imports
  imports:
    - jirafs_graphviz

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/coddingtonbear/jirafs-graphviz
  license: MIT License
  summary: 'Automatically convert graphviz DOT files into PNG images when uploading to JIRA'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
