package:
  name: plonetheme.coolblue
  version: "0.1.1"

source:
  fn: plonetheme.coolblue-0.1.1.zip
  url: https://pypi.python.org/packages/source/p/plonetheme.coolblue/plonetheme.coolblue-0.1.1.zip
  md5: f6867e2fae6498891ab58547f8249a80
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plonetheme.coolblue = plonetheme.coolblue:main
    #
    # Would create an entry point called plonetheme.coolblue that calls plonetheme.coolblue.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.theming

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.theming

test:
  # Python imports
  imports:
    - plonetheme
    - plonetheme.coolblue

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/plonetheme.coolblue
  license: UNKNOWN
  summary: 'An installable Diazo theme for Plone 4.1'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
