package:
  name: django-excel-response2
  version: "1.0.0"

source:
  fn: django-excel-response2-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-excel-response2/django-excel-response2-1.0.0.tar.gz
  md5: a63506867b824d88c265bb003a76f35e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-excel-response2 = django-excel-response2:main
    #
    # Would create an entry point called django-excel-response2 that calls django-excel-response2.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - xlwt

  run:
    - python
    - xlwt

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://djangosnippets.org/snippets/3046/
  license: UNKNOWN
  summary: "A function extends of Tarken's django-excel-response"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
