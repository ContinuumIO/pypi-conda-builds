package:
  name: collective.wysiwyg_biografy
  version: "0.2.1"

source:
  fn: collective.wysiwyg_biografy-0.2.1.zip
  url: https://pypi.python.org/packages/source/c/collective.wysiwyg_biografy/collective.wysiwyg_biografy-0.2.1.zip
  md5: ad69fa48573ce26699f61ede503e26c5
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.wysiwyg_biografy = collective.wysiwyg_biografy:main
    #
    # Would create an entry point called collective.wysiwyg_biografy that calls collective.wysiwyg_biografy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.cmfplone >4.0b1
    - plone.app.users

  run:
    - python
    - setuptools
    - setuptools
    - products.cmfplone >4.0b1
    - plone.app.users

test:
  # Python imports
  imports:
    - collective
    - collective.wysiwyg_biografy
    - collective.wysiwyg_biografy.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/collective.wysiwyg_biografy
  license: GPL
  summary: "The Plone user's biografy using the WYSIWYG editor"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
