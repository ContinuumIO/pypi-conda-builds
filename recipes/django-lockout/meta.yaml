package:
  name: django-lockout
  version: "0.1.1"

source:
  fn: django-lockout-0.1.1.zip
  url: https://pypi.python.org/packages/source/d/django-lockout/django-lockout-0.1.1.zip
  md5: 6e12ca4e86e6117d95cf10ba5b25a3a3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-lockout = django-lockout:main
    #
    # Would create an entry point called django-lockout that calls django-lockout.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - lockout

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/brianjaystanley/django-lockout
  license: MIT License
  summary: 'cache-based Django app that locks out users after too many failed login attempts.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
