package:
  name: django-statsd
  version: "2.0.1"

source:
  fn: django-statsd-2.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-statsd/django-statsd-2.0.1.tar.gz
  md5: e3be563f7e43c1048e9f18219156db01
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-statsd = django-statsd:main
    #
    # Would create an entry point called django-statsd that calls django-statsd.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python-statsd >=1.6.0

  run:
    - python
    - python-statsd >=1.6.0

test:
  # Python imports
  imports:
    - django_statsd

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/WoLpH/django-statsd
  license: BSD License
  summary: "django-statsd is a django app that submits query and\n    view durations to Etsy's statsd."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
