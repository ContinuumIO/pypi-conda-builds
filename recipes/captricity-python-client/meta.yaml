package:
  name: captricity-python-client
  version: "0.20"

source:
  fn: captricity-python-client-0.20.tar.gz
  url: https://pypi.python.org/packages/source/c/captricity-python-client/captricity-python-client-0.20.tar.gz
  md5: 01408f67b77329498011a5ceda70af87
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - captricity-python-client = captricity-python-client:main
    #
    # Would create an entry point called captricity-python-client that calls captricity-python-client.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - captools
    - captools.api

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Captricity/captools
  license: MIT License
  summary: 'Python client to access Captricity API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
