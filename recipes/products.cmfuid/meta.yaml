package:
  name: products.cmfuid
  version: "2.3.0-beta"

source:
  fn: Products.CMFUid-2.3.0-beta.tar.gz
  url: https://pypi.python.org/packages/source/P/Products.CMFUid/Products.CMFUid-2.3.0-beta.tar.gz
  md5: 283ffbe0b854c4b8d0b5bebf9878c182
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - products.cmfuid = products.cmfuid:main
    #
    # Would create an entry point called products.cmfuid that calls products.cmfuid.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zope2 >=2.13.12
    - products.cmfcore
    - products.genericsetup

  run:
    - python
    - setuptools
    - setuptools
    - zope2 >=2.13.12
    - products.cmfcore
    - products.genericsetup

test:
  # Python imports
  imports:
    - Products
    - Products.CMFUid
    - Products.CMFUid.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/Products.CMFUid
  license: Zope Public License
  summary: 'Uid product for the Zope Content Management Framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
