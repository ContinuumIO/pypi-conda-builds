package:
  name: agms
  version: "0.1.5"

source:
  fn: agms-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/a/agms/agms-0.1.5.tar.gz
  md5: 31997daef75d353f04e9c489a3f99f17
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - agms = agms:main
    #
    # Would create an entry point called agms that calls agms.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=2.5.3
    - pyopenssl
    - ndg-httpsclient
    - pyasn1

  run:
    - python
    - requests >=2.5.3
    - pyopenssl
    - ndg-httpsclient
    - pyasn1

test:
  # Python imports
  imports:
    - agms
    - agms.exception
    - agms.request
    - agms.response
    - agms.test
    - agms.util

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/agmscode/agms_python
  license: MIT License
  summary: 'Agms Python Library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
