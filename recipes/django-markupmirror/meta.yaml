package:
  name: django-markupmirror
  version: "0.1c1"

source:
  fn: django-markupmirror-0.1c1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-markupmirror/django-markupmirror-0.1c1.tar.gz
  md5: cd30b811d3d75b9a3e58e407ad5e9e79
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-markupmirror = django-markupmirror:main
    #
    # Would create an entry point called django-markupmirror that calls django-markupmirror.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - markupmirror
    - markupmirror.feincms
    - markupmirror.markup
    - tests
    - tests.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/fabianbuechler/django-markupmirror
  license: BSD License
  summary: 'Django field and widget for editing markup content (PlainText, HTML,\nMarkdown, reStructuredText, Textile) using the CodeMirror editor with\nlive preview.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
