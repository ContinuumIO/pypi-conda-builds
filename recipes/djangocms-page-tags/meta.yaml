package:
  name: djangocms-page-tags
  version: "0.3.0"

source:
  fn: djangocms-page-tags-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/d/djangocms-page-tags/djangocms-page-tags-0.3.0.tar.gz
  md5: 3e771fa6f156db76fb4f68a66eb170b4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangocms-page-tags = djangocms-page-tags:main
    #
    # Would create an entry point called djangocms-page-tags that calls djangocms-page-tags.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-cms >=3.0
    - django-taggit >=0.11.2
    - django-taggit-autosuggest
    - django-classy-tags >=0.3.4.1

  run:
    - python
    - django-cms >=3.0
    - django-taggit >=0.11.2
    - django-taggit-autosuggest
    - django-classy-tags >=0.3.4.1

test:
  # Python imports
  imports:
    - djangocms_page_tags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/nephila/djangocms-page-tags
  license: BSD License
  summary: 'Tagged pages for django CMS 3'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
