package:
  name: django-url2png
  version: "0.4"

source:
  fn: django-url2png-0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-url2png/django-url2png-0.4.tar.gz
  md5: a4456ff461d4619231308d6edbcdea3d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-url2png = django-url2png:main
    #
    # Would create an entry point called django-url2png that calls django-url2png.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - url2png
    - url2png.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://callowayproject.com/
  license: Apache Software License
  summary: 'Make web page screen shots using the url2png service.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
