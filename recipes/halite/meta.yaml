package:
  name: halite
  version: "0.1.17"

source:
  fn: halite-0.1.17.tar.gz
  url: https://pypi.python.org/packages/source/h/halite/halite-0.1.17.tar.gz
  md5: 01fe3bf050205514f88dfb5977fa81e2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - halite = halite:main
    #
    # Would create an entry point called halite that calls halite.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - halite
    - halite.test
    - halite.test.functional

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/saltstack/halite
  license: Apache V2.0
  summary: 'SaltStack Web UI'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
