package:
  name: django-dbbackup
  version: "2.0.4"

source:
  fn: django-dbbackup-2.0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-dbbackup/django-dbbackup-2.0.4.tar.gz
  md5: dcef6b4652c4da3106955fe1e870b5c0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-dbbackup = django-dbbackup:main
    #
    # Would create an entry point called django-dbbackup that calls django-dbbackup.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - pysftp

  run:
    - python
    - pysftp

test:
  # Python imports
  imports:
    - dbbackup
    - dbbackup.management
    - dbbackup.management.commands
    - dbbackup.storage

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/mjs7231/django-dbbackup
  license: BSD
  summary: 'Management commands to help backup and restore a project database to AmazonS3, Dropbox or local disk.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
