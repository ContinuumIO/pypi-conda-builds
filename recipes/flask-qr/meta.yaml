package:
  name: flask-qr
  version: "0.1.3"

source:
  fn: Flask-QR-0.1.3.zip
  url: https://pypi.python.org/packages/source/F/Flask-QR/Flask-QR-0.1.3.zip
  md5: 8d8dca22f2533c29af3a898960ba0257
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-qr = flask-qr:main
    #
    # Would create an entry point called flask-qr that calls flask-qr.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - qrcode
    - pillow

  run:
    - python
    - flask
    - qrcode
    - pillow

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/danielwarna/Flask-QR/
  license: BSD License
  summary: 'Flask extension for generating qr codes'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
