package:
  name: django-threadedcomments
  version: "1.0b1"

source:
  fn: django-threadedcomments-1.0b1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-threadedcomments/django-threadedcomments-1.0b1.tar.gz
  md5: ded40b34eeb28e082b6da1fbc383de3b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-threadedcomments = django-threadedcomments:main
    #
    # Would create an entry point called django-threadedcomments that calls django-threadedcomments.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-contrib-comments >=1.5

  run:
    - python
    - django-contrib-comments >=1.5

test:
  # Python imports
  imports:
    - threadedcomments
    - threadedcomments.management
    - threadedcomments.management.commands
    - threadedcomments.migrations
    - threadedcomments.south_migrations
    - threadedcomments.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/HonzaKral/django-threadedcomments
  license: BSD License
  summary: 'A simple yet flexible threaded commenting system.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
