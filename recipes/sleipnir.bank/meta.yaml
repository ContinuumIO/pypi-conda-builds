package:
  name: sleipnir.bank
  version: "0.0.90"

source:
  fn: sleipnir.bank-0.0.90.tar.gz
  url: https://pypi.python.org/packages/source/s/sleipnir.bank/sleipnir.bank-0.0.90.tar.gz
  md5: 179dc77059508aed4cb69037935ac003
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sleipnir.bank = sleipnir.bank:main
    #
    # Would create an entry point called sleipnir.bank that calls sleipnir.bank.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sleipnir.transport >=0.0.91

  run:
    - python
    - sleipnir.transport >=0.0.91

test:
  # Python imports
  imports:
    - sleipnir
    - sleipnir.bank

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://sleipnir.liberalia.net/
  license: GNU General Public License (GPL)
  summary: 'Sleipnir EIA Bank example'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
