package:
  name: appkit
  version: "0.2.8"

source:
  fn: AppKit-0.2.8.tar.gz
  url: https://pypi.python.org/packages/source/A/AppKit/AppKit-0.2.8.tar.gz
  md5: b942169c6c6b2fdc7ffb6bf3b72f468b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - appkit = appkit:main
    #
    # Would create an entry point called appkit that calls appkit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - pygobject
    - beautifulsoup4

  run:
    - python
    - flask
    - pygobject
    - beautifulsoup4

test:
  # Python imports
  imports:
    - appkit

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://nitipit.github.com/appkit/
  license: MIT
  summary: 'Desktop application framework based on Webkit HTML5, CSS3, Javascript and Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
