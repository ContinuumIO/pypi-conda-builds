package:
  name: rtslib-fb
  version: "2.1.51"

source:
  fn: rtslib-fb-2.1.51.tar.gz
  url: https://pypi.python.org/packages/source/r/rtslib-fb/rtslib-fb-2.1.51.tar.gz
  md5: 4ec7575eb90b7c5a91bbea2962f0e036
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - rtslib-fb = rtslib-fb:main
    #
    # Would create an entry point called rtslib-fb that calls rtslib-fb.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - rtslib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/agrover/rtslib-fb
  license: Apache 2.0
  summary: 'API for Linux kernel SCSI target (aka LIO)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
