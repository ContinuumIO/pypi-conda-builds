package:
  name: collective.wfcomment
  version: "2.1.5"

source:
  fn: collective.wfcomment-2.1.5.zip
  url: https://pypi.python.org/packages/source/c/collective.wfcomment/collective.wfcomment-2.1.5.zip
  md5: c6575b5e267eaa02608a8545427191ed
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.wfcomment = collective.wfcomment:main
    #
    # Would create an entry point called collective.wfcomment that calls collective.wfcomment.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - z3c.form
    - plone.app.registry
    - plone.z3cform

  run:
    - python
    - setuptools
    - setuptools
    - z3c.form
    - plone.app.registry
    - plone.z3cform

test:
  # Python imports
  imports:
    - collective
    - collective.wfcomment
    - collective.wfcomment.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/collective.wfcomment
  license: GPL
  summary: 'Add a prompt on selected workflow transitions for the user to enter a comment'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
