package:
  name: ixdjango
  version: "0.1.4"

source:
  fn: IXDjango-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/I/IXDjango/IXDjango-0.1.4.tar.gz
  md5: 63fbaa07af777c2c3e4a1f7a407046fd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ixdjango = ixdjango:main
    #
    # Would create an entry point called ixdjango that calls ixdjango.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3.0

  run:
    - python
    - django >=1.3.0

test:
  # Python imports
  imports:
    - ixdjango
    - ixdjango.management
    - ixdjango.management.commands
    - ixdjango.test_suite
    - ixdjango.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/IXDjango/
  license: MIT
  summary: 'Management commands for deploying Django projects.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
