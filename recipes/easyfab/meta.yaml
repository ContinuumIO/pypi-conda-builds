package:
  name: easyfab
  version: "0.4.8"

source:
  fn: easyfab-0.4.8.tar.gz
  url: https://pypi.python.org/packages/source/e/easyfab/easyfab-0.4.8.tar.gz
  md5: d64cd2b143ba2f917656b7611eeb6937
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - easyfab = easyfab:main
    #
    # Would create an entry point called easyfab that calls easyfab.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - jinja2
    - fabric
    - unipath

  run:
    - python
    - jinja2
    - fabric
    - unipath

test:
  # Python imports
  imports:
    - easyfab

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/phonkee/easyfab/
  license: Apache Software License
  summary: 'Simple deployments with fabric'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
