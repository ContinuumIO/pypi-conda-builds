package:
  name: ticktock
  version: "0.1.2"

source:
  fn: ticktock-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/t/ticktock/ticktock-0.1.2.tar.gz
  md5: 5a19b5c74c7d1893bb46c79eee8e55cd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ticktock = ticktock:main
    #
    # Would create an entry point called ticktock that calls ticktock.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - ticktock

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tsroten/ticktock
  license: MIT License
  summary: "adds least-recently-used cache management and automatic data timeout to Python's Shelf class."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
