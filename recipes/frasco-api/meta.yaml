package:
  name: frasco-api
  version: "0.1"

source:
  fn: frasco-api-0.1.tar.gz
  url: https://pypi.python.org/packages/source/f/frasco-api/frasco-api-0.1.tar.gz
  md5: b96eb6ccddbd4d15f70a65beee7a9d03
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - frasco-api = frasco-api:main
    #
    # Would create an entry point called frasco-api that calls frasco-api.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - frasco-models
    - frasco-users

  run:
    - python
    - frasco-models
    - frasco-users

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/frascoweb/frasco-api
  license: MIT
  summary: 'Create APIs and service based applications with Frasco'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
