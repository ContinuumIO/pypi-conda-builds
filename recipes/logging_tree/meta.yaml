package:
  name: logging_tree
  version: "1.6"

source:
  fn: logging_tree-1.6.tar.gz
  url: https://pypi.python.org/packages/source/l/logging_tree/logging_tree-1.6.tar.gz
  md5: 33ef7eaa1533a50cf67ed1c49a48cfdd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - logging_tree = logging_tree:main
    #
    # Would create an entry point called logging_tree that calls logging_tree.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - logging_tree
    - logging_tree.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/brandon-rhodes/logging_tree
  license: BSD License
  summary: 'Introspect and display the logger tree inside "logging"'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
