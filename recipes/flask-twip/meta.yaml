package:
  name: flask-twip
  version: "0.0.5"

source:
  fn: Flask-Twip-0.0.5.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Twip/Flask-Twip-0.0.5.tar.gz
  md5: e11d7b8bb0aaea0d8f9e91143064d3e5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-twip = flask-twip:main
    #
    # Would create an entry point called flask-twip that calls flask-twip.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.9
    - requests >=0.14.2
    - flask-oauth >=0.12
    - sqlalchemy >=0.7.9
    - requests >=0.14.2

  run:
    - python
    - flask >=0.9
    - requests >=0.14.2
    - flask-oauth >=0.12
    - sqlalchemy >=0.7.9
    - requests >=0.14.2

test:
  # Python imports
  imports:
    - flask_twip

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/yegle/flask_twip/
  license: Mozilla Public License 1.1 (MPL 1.1)
  summary: u'twitter API proxy extension for Flask microframework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
