package:
  name: aeon
  version: "2.0.0"

source:
  fn: Aeon-2.0.0.tar.gz
  url: https://pypi.python.org/packages/source/A/Aeon/Aeon-2.0.0.tar.gz
  md5: 61ec06881b8d76a99b35ffae5c04c8ca
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - aeon = aeon:main
    #
    # Would create an entry point called aeon that calls aeon.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - aeon

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/sMAshdot/aeon
  license: The MIT License (MIT)
  summary: 'Runtime and number of calls for designated functions, methods, or pieces of code. Optionally output nice report.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
