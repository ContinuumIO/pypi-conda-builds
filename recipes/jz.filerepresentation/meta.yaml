package:
  name: jz.filerepresentation
  version: "0.1"

source:
  fn: jz.filerepresentation-0.1.tar.gz
  url: https://pypi.python.org/packages/source/j/jz.filerepresentation/jz.filerepresentation-0.1.tar.gz
  md5: ee8ef46bd1bfaacbed401ec46c3e9666
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jz.filerepresentation = jz.filerepresentation:main
    #
    # Would create an entry point called jz.filerepresentation that calls jz.filerepresentation.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - jz.common

  run:
    - python
    - setuptools
    - jz.common

test:
  # Python imports
  imports:
    - jz
    - jz.filerepresentation
    - jz.filerepresentation.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.jellofishi.com/zope
  license: GPL
  summary: 'File representation components (zope).'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
