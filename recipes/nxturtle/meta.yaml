package:
  name: nxturtle
  version: "1.0.4.dev-20110105"

source:
  fn: nxturtle-1.0.4.dev-20110105.zip
  url: https://pypi.python.org/packages/source/n/nxturtle/nxturtle-1.0.4.dev-20110105.zip
  md5: 28e117fc3225d8e25ec4dc7b81ab02fc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - nxturtle = nxturtle:main
    #
    # Would create an entry point called nxturtle that calls nxturtle.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - nxt-python

  run:
    - python
    - nxt-python

test:
  # Python imports
  imports:
    - nxturtle
    - nxturtle.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://nxturtle.googlecode.com/
  license: MIT License
  summary: 'Implementation of a Python turtle for LEGO Mindstorms NXT'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
