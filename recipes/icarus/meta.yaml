package:
  name: icarus
  version: "0.1.7"

source:
  fn: Icarus-0.1.7.tar.gz
  url: https://pypi.python.org/packages/source/I/Icarus/Icarus-0.1.7.tar.gz
  md5: 42ab7ddb1456daa65146d83762ad653e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - icarus = icarus:main
    #
    # Would create an entry point called icarus that calls icarus.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - icarus
    - icarus.http
    - icarus.query
    - icarus.result
    - icarus.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/Icarus/
  license: LICENSE.txt
  summary: 'Python interface for Solr'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
