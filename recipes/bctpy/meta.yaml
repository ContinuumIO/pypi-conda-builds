package:
  name: bctpy
  version: "0.3.3"

source:
  fn: bctpy-0.3.3.tar.gz
  url: https://pypi.python.org/packages/source/b/bctpy/bctpy-0.3.3.tar.gz
  md5: 18671f77efdc24f7c347d9a6ad261d60
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bctpy = bctpy:main
    #
    # Would create an entry point called bctpy that calls bctpy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - bct
    - bctpy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/aestrivex/bctpy
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'Brain Connectivity Toolbox for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
