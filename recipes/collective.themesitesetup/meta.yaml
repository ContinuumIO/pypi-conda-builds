package:
  name: collective.themesitesetup
  version: "0.13.0"

source:
  fn: collective.themesitesetup-0.13.0.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.themesitesetup/collective.themesitesetup-0.13.0.tar.gz
  md5: 13be239d94432f3997877e41261798bb
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.themesitesetup = collective.themesitesetup:main
    #
    # Would create an entry point called collective.themesitesetup that calls collective.themesitesetup.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - products.genericsetup
    - plone.api
    - plone.app.controlpanel
    - plone.app.theming
    - plone.autoform
    - plone.supermodel
    - plone.z3cform
    - setuptools
    - z3c.form
    - zope.schema

  run:
    - python
    - setuptools
    - products.genericsetup
    - plone.api
    - plone.app.controlpanel
    - plone.app.theming
    - plone.autoform
    - plone.supermodel
    - plone.z3cform
    - setuptools
    - z3c.form
    - zope.schema

test:
  # Python imports
  imports:
    - collective
    - collective.themesitesetup
    - collective.themesitesetup.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.themesitesetup/
  license: GPL
  summary: 'GenericSetup profiles for plone.app.theming'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
