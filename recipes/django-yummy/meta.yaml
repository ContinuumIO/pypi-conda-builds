package:
  name: django-yummy
  version: "0.2.6"

source:
  fn: Django-Yummy-0.2.6.tar.gz
  url: https://pypi.python.org/packages/source/D/Django-Yummy/Django-Yummy-0.2.6.tar.gz
  md5: 2a584df6f086c317b34165de8afc0a5e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-yummy = django-yummy:main
    #
    # Would create an entry point called django-yummy that calls django-yummy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4,<1.6
    - south ==0.7.6

  run:
    - python
    - django >=1.4,<1.6
    - south ==0.7.6

test:
  # Python imports
  imports:
    - yummy
    - yummy.migrations
    - yummy.templatetags
    - yummy.utils
    - yummy.utils.search

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/SanomaCZ/django-yummy
  license: BSD License
  summary: 'Recipes application based on Django framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
