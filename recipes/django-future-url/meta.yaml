package:
  name: django-future-url
  version: "0.2.2"

source:
  fn: django-future-url-0.2.2.zip
  url: https://pypi.python.org/packages/source/d/django-future-url/django-future-url-0.2.2.zip
  md5: efb947b4049c7f86935d76171a511ea4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-future-url = django-future-url:main
    #
    # Would create an entry point called django-future-url that calls django-future-url.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - docopt

  run:
    - python
    - docopt

test:
  # Python imports
  imports:
    - django_future_url
    - django_future_url.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/futurecolors/django-future-url/
  license: MIT License
  summary: 'Migration tool for django 1.4, fixes url template tag deprecation warnings.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
