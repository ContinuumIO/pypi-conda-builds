package:
  name: wtforms-json
  version: "0.2.10"

source:
  fn: WTForms-JSON-0.2.10.tar.gz
  url: https://pypi.python.org/packages/source/W/WTForms-JSON/WTForms-JSON-0.2.10.tar.gz
  md5: 08e5a8e5dd4bc38782ec97dfcd884dcd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - wtforms-json = wtforms-json:main
    #
    # Would create an entry point called wtforms-json that calls wtforms-json.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - wtforms >=1.0.2
    - six >=1.3.0

  run:
    - python
    - wtforms >=1.0.2
    - six >=1.3.0

test:
  # Python imports
  imports:
    - wtforms_json

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kvesteri/wtforms-json
  license: BSD License
  summary: 'Adds smart json support for WTForms. Useful for when using WTForms with RESTful APIs.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
