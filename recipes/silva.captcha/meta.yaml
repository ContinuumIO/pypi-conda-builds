package:
  name: silva.captcha
  version: "1.3.1"

source:
  fn: silva.captcha-1.3.1.tar.gz
  url: https://pypi.python.org/packages/source/s/silva.captcha/silva.captcha-1.3.1.tar.gz
  md5: 8e4eef62d53264e0bc3ba5696cddeabc
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - silva.captcha = silva.captcha:main
    #
    # Would create an entry point called silva.captcha that calls silva.captcha.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - products.formulator
    - five.grok
    - setuptools
    - silva.core.services
    - silva.translations
    - skimpygimpy
    - zeam.form.base
    - zeam.form.ztk
    - zope.component
    - zope.interface
    - zope.schema
    - zope.traversing

  run:
    - python
    - setuptools
    - products.formulator
    - five.grok
    - setuptools
    - silva.core.services
    - silva.translations
    - skimpygimpy
    - zeam.form.base
    - zeam.form.ztk
    - zope.component
    - zope.interface
    - zope.schema
    - zope.traversing

test:
  # Python imports
  imports:
    - silva
    - silva.captcha
    - silva.captcha.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/silvacms/silva.captcha
  license: BSD License
  summary: 'Captcha support for Silva CMS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
