package:
  name: etaprogress
  version: "1.1.1"

source:
  fn: etaprogress-1.1.1.tar.gz
  url: https://pypi.python.org/packages/source/e/etaprogress/etaprogress-1.1.1.tar.gz
  md5: e849839f845ad1093f16d712c680f3aa
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - etaprogress = etaprogress:main
    #
    # Would create an entry point called etaprogress that calls etaprogress.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - etaprogress
    - etaprogress/components

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Robpol86/etaprogress
  license: MIT License
  summary: 'Easy to use ETA calculation and progress bar library.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
