package:
  name: cerealizer
  version: "0.8.1"

source:
  fn: Cerealizer-0.8.1.tar.bz2
  url: https://pypi.python.org/packages/source/C/Cerealizer/Cerealizer-0.8.1.tar.bz2
  md5: a77b6215f77c539035ae46a73bfa819e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cerealizer = cerealizer:main
    #
    # Would create an entry point called cerealizer that calls cerealizer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - cerealizer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://home.gna.org/oomadness/en/cerealizer/index.html
  license: Python Software Foundation License
  summary: 'A secure pickle-like module'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
