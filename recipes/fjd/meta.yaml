package:
  name: fjd
  version: "0.1.58"

source:
  fn: fjd-0.1.58.tar.gz
  url: https://pypi.python.org/packages/source/f/fjd/fjd-0.1.58.tar.gz
  md5: d210f23312cfa1b7b1855ffdb1010227
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fjd = fjd:main
    #
    # Would create an entry point called fjd that calls fjd.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - screenutils >=0.0.1.5.4
    - paramiko >=1.13

  run:
    - python
    - screenutils >=0.0.1.5.4
    - paramiko >=1.13

test:
  # Python imports
  imports:
    - fjd

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/nhoening/fjd/
  license: Apache Software License
  summary: 'Job distribution for everyone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
