package:
  name: django_taxbot
  version: "0.0.6"

source:
  fn: django_taxbot-0.0.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django_taxbot/django_taxbot-0.0.6.tar.gz
  md5: 190850f34ae851713eaf6974445b08fa
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_taxbot = django_taxbot:main
    #
    # Would create an entry point called django_taxbot that calls django_taxbot.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - django_taxbot
    - taxbot

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/parsenz/django-taxbot
  license: BSD License
  summary: 'A simple Django app to figure out currencies in a limited number of countries'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
