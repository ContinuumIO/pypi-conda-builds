package:
  name: megrok.z3cform.tabular
  version: "0.2"

source:
  fn: megrok.z3cform.tabular-0.2.tar.gz
  url: https://pypi.python.org/packages/source/m/megrok.z3cform.tabular/megrok.z3cform.tabular-0.2.tar.gz
  md5: 6121c55a8035a1b17b14a01810724078
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - megrok.z3cform.tabular = megrok.z3cform.tabular:main
    #
    # Would create an entry point called megrok.z3cform.tabular that calls megrok.z3cform.tabular.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - grok
    - z3c.tabular
    - megrok.layout
    - megrok.z3cform.base
    - megrok.z3cform.ui
    - megrok.z3ctable

  run:
    - python
    - setuptools
    - setuptools
    - grok
    - z3c.tabular
    - megrok.layout
    - megrok.z3cform.base
    - megrok.z3cform.ui
    - megrok.z3ctable

test:
  # Python imports
  imports:
    - megrok
    - megrok.z3cform
    - megrok.z3cform.tabular
    - megrok.z3cform.tabular.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: GPL
  summary: 'Grok addon for createing tabular forms'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
