package:
  name: python-gssapi
  version: "0.6.4"

source:
  fn: python-gssapi-0.6.4.tar.gz
  url: https://pypi.python.org/packages/source/p/python-gssapi/python-gssapi-0.6.4.tar.gz
  md5: cc30c7cab4ed61709a52934a38d53e0f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-gssapi = python-gssapi:main
    #
    # Would create an entry point called python-gssapi that calls python-gssapi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cffi >=0.8
    - six >=1.5.0
    - pyasn1 >=0.1.2

  run:
    - python
    - cffi >=0.8
    - six >=1.5.0
    - pyasn1 >=0.1.2

test:
  # Python imports
  imports:
    - gssapi
    - gssapi.bindings

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/sigmaris/python-gssapi
  license: The MIT License (MIT)
  summary: 'An object-oriented interface to GSSAPI for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
