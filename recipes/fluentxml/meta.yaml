package:
  name: fluentxml
  version: "0.1.1"

source:
  fn: fluentxml-0.1.1.tar.bz2
  url: https://pypi.python.org/packages/source/f/fluentxml/fluentxml-0.1.1.tar.bz2
  md5: 86f0f090696c05362cbbaa085591db61
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fluentxml = fluentxml:main
    #
    # Would create an entry point called fluentxml that calls fluentxml.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - fluentxml

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://fluentxml.last-exile.org/
  license: GNU General Public License (GPL)
  summary: u'Minimalist XML library.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
