package:
  name: confspec
  version: "1.4"

source:
  fn: confspec-1.4.tar.gz
  url: https://pypi.python.org/packages/source/c/confspec/confspec-1.4.tar.gz
  md5: 5ba5f00bebe15d5018bef0121dbcf7b0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - confspec = confspec:main
    #
    # Would create an entry point called confspec that calls confspec.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - confspec
    - confspec.providers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://confspec.readthedocs.org/
  license: Apache Software License
  summary: 'Configuration Specification Manager'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
