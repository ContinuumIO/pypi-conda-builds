package:
  name: dharma
  version: "1.1"

source:
  fn: dharma-1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/dharma/dharma-1.1.tar.gz
  md5: 8204e50e3c8c4c02f77ea740c8ca1b33
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dharma = dharma:main
    #
    # Would create an entry point called dharma that calls dharma.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - dharma

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mozillasecurity/dharma
  license: Mozilla Public License 2.0 (MPL 2.0)
  summary: 'A generation-based, context-free grammar fuzzer.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
