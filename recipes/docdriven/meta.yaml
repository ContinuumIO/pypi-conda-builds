package:
  name: docdriven
  version: "0.1.20"

source:
  fn: docdriven-0.1.20.tar.gz
  url: https://pypi.python.org/packages/source/d/docdriven/docdriven-0.1.20.tar.gz
  md5: 9dda3b0e725c6d90e09cf6f0ff8ad3d5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - docdriven = docdriven:main
    #
    # Would create an entry point called docdriven that calls docdriven.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - docdriven

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/docdriven/
  license: MIT_LICENSE.txt
  summary: 'Support for documentation-driven development of JSON APIs.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
