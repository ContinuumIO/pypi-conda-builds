package:
  name: incuna-mail
  version: "3.0.0"

source:
  fn: incuna-mail-3.0.0.tar.gz
  url: https://pypi.python.org/packages/source/i/incuna-mail/incuna-mail-3.0.0.tar.gz
  md5: ada6a50b2e34d05e337058c09cc4274d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - incuna-mail = incuna-mail:main
    #
    # Would create an entry point called incuna-mail that calls incuna-mail.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/incuna/incuna-mail
  license: MIT License
  summary: 'Pythonic utility for sending template based emails with Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
