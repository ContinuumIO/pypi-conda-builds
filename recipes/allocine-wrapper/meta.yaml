package:
  name: allocine-wrapper
  version: "0.3.0"

source:
  fn: allocine-wrapper-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/a/allocine-wrapper/allocine-wrapper-0.3.0.tar.gz
  md5: 0a9f20fb0232a7fad6c253cc382eb6b5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - allocine-wrapper = allocine-wrapper:main
    #
    # Would create an entry point called allocine-wrapper that calls allocine-wrapper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - allocine

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/python-allocine/
  license: LICENSE.txt
  summary: 'Wrapper of Allocine API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
