package:
  name: zc.demostorage2
  version: "0.1.3"

source:
  fn: zc.demostorage2-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/z/zc.demostorage2/zc.demostorage2-0.1.3.tar.gz
  md5: 4066c73c6c335b8010c5807ce48d87e7
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zc.demostorage2 = zc.demostorage2:main
    #
    # Would create an entry point called zc.demostorage2 that calls zc.demostorage2.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - zodb3
    - setuptools

  run:
    - python
    - zodb3
    - setuptools

test:
  # Python imports
  imports:
    - zc
    - zc.demostorage2

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Zope Public License
  summary: 'ZODB storage that stores changes relative to a base storage'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
