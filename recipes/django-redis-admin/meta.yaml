package:
  name: django-redis-admin
  version: "0.0.1"

source:
  fn: django-redis-admin-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-redis-admin/django-redis-admin-0.0.1.tar.gz
  md5: 2dc85d729d5cd2bd2d35c8881c8da093
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-redis-admin = django-redis-admin:main
    #
    # Would create an entry point called django-redis-admin that calls django-redis-admin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - radmin
    - radmin.conf

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/aino/django-redis-admin
  license: BSD License
  summary: 'Django admin for redis'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
