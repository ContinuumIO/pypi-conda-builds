package:
  name: django-waffle
  version: "0.10.1"

source:
  fn: django-waffle-0.10.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-waffle/django-waffle-0.10.1.tar.gz
  md5: 5afd8c218d5dc89809722c5b2ac51316
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-waffle = django-waffle:main
    #
    # Would create an entry point called django-waffle that calls django-waffle.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - waffle
    - waffle.management
    - waffle.management.commands
    - waffle.migrations
    - waffle.south_migrations
    - waffle.templatetags
    - waffle.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/jsocol/django-waffle
  license: BSD License
  summary: 'A feature flipper for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
