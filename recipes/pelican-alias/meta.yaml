package:
  name: pelican-alias
  version: "1.0"

source:
  fn: pelican-alias-1.0.tar.gz
  url: https://pypi.python.org/packages/source/p/pelican-alias/pelican-alias-1.0.tar.gz
  md5: c0ecccab37ddaa655227b25f9f759b07
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pelican-alias = pelican-alias:main
    #
    # Would create an entry point called pelican-alias that calls pelican-alias.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pelican >=3.1.1

  run:
    - python
    - pelican >=3.1.1

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/Nitron/pelican-alias
  license: MIT License
  summary: 'Pelican plugin for creating alias pages (useful for moving from a different URL scheme such as /<year>/<month>/<title>/ as used by Wordpress).'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
