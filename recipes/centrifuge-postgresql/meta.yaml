package:
  name: centrifuge-postgresql
  version: "0.2.2"

source:
  fn: centrifuge-postgresql-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/c/centrifuge-postgresql/centrifuge-postgresql-0.2.2.tar.gz
  md5: 3f5fd08ba70d1868f54c76843fdcfc7f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - centrifuge-postgresql = centrifuge-postgresql:main
    #
    # Would create an entry point called centrifuge-postgresql that calls centrifuge-postgresql.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - centrifuge
    - six >=1.3.0
    - momoko >=1.1.4

  run:
    - python
    - centrifuge
    - six >=1.3.0
    - momoko >=1.1.4

test:
  # Python imports
  imports:
    - centrifuge_postgresql

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/centrifugal/centrifuge-postgresql
  license: Apache Software License
  summary: 'PostgreSQL structure backend for Centrifuge'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
