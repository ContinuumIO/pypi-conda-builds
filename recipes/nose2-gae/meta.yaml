package:
  name: nose2-gae
  version: "0.1.7"

source:
  fn: nose2-gae-0.1.7.tar.gz
  url: https://pypi.python.org/packages/source/n/nose2-gae/nose2-gae-0.1.7.tar.gz
  md5: f1613fae8de2dc43d0ad87558260d142
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - nose2-gae = nose2-gae:main
    #
    # Would create an entry point called nose2-gae that calls nose2-gae.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - nose2gae

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/udacity/nose2-gae
  license: GNU Lesser General Public License v3 or later (LGPLv3+)
  summary: 'nose2 plugin to run the tests in the Google App Engine environment'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
