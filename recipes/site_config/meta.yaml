package:
  name: site_config
  version: "0.0.14"

source:
  fn: site_config-0.0.14.tar.gz
  url: https://pypi.python.org/packages/source/s/site_config/site_config-0.0.14.tar.gz
  md5: 762fd503814da75ec9b2119472a4e584
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - site_config = site_config:main
    #
    # Would create an entry point called site_config that calls site_config.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6
    - jsonfield >=0.9.22

  run:
    - python
    - django >=1.6
    - jsonfield >=0.9.22

test:
  # Python imports
  imports:
    - site_config
    - site_config.backends
    - site_config.backends.model_backend
    - site_config.backends.settings_backend
    - site_config.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ImaginaryLandscape/django_site_config
  license: BSD License
  summary: 'Django configuration Utility to manage multiple "websites" in a project. '

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
