package:
  name: glpk
  version: "0.3"

source:
  fn: glpk-0.3.tar.gz
  url: https://pypi.python.org/packages/source/g/glpk/glpk-0.3.tar.gz
  md5: 2babe9dde17aad1a008b25bd51e57860
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - glpk = glpk:main
    #
    # Would create an entry point called glpk that calls glpk.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - glpk

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://sourceforge.net/projects/opencobra/files/python/cobra/extras/pyGLPK/
  license: GNU General Public License (GPL)
  summary: "PyGLPK, a Python module encapsulating GLPK.  Removed the -m32 flags from Thomas Finley's setup.py"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
