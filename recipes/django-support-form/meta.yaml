package:
  name: django-support-form
  version: "0.1.0"

source:
  fn: django-support-form-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-support-form/django-support-form-0.1.0.tar.gz
  md5: 1f797e6ece9126aa631eaa447f365396
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-support-form = django-support-form:main
    #
    # Would create an entry point called django-support-form that calls django-support-form.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4,<1.6

  run:
    - python
    - django >=1.4,<1.6

test:
  # Python imports
  imports:
    - supportform

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/cbmi/django-support-form/
  license: BSD License
  summary: 'Simple support/contact form for your Django app'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
