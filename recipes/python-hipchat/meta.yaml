package:
  name: python-hipchat
  version: "0.1.0"

source:
  fn: python-hipchat-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/p/python-hipchat/python-hipchat-0.1.0.tar.gz
  md5: d0d777545de32e98a6800bf815ce570e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-hipchat = python-hipchat:main
    #
    # Would create an entry point called python-hipchat that calls python-hipchat.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - hipchat

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://ryanbalfanz.github.com/python-hipchat/
  license: UNKNOWN
  summary: 'python-hipchat'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
