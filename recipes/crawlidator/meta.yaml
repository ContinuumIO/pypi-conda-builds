package:
  name: crawlidator
  version: "0.2"

source:
  fn: crawlidator-0.2.tar.gz
  url: https://pypi.python.org/packages/source/c/crawlidator/crawlidator-0.2.tar.gz
  md5: 945970ed6fe03f72b0592802c7157726
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - crawlidator = crawlidator:main
    #
    # Would create an entry point called crawlidator that calls crawlidator.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyxml >=0.8.4
    - egenix-mx-base >=3.1.2
    - argparse

  run:
    - python
    - pyxml >=0.8.4
    - egenix-mx-base >=3.1.2
    - argparse

test:
  # Python imports
  imports:
    - crawlidator

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/charstring/crawlidator
  license: BSD License
  summary: 'A validator that crawls your sitemap.xml'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
