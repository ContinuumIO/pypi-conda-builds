package:
  name: dynamo-fuse
  version: "0.1.4"

source:
  fn: dynamo-fuse-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/d/dynamo-fuse/dynamo-fuse-0.1.4.tar.gz
  md5: 0996986c4d3a17d03fbc25e3c450c4aa
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dynamo-fuse = dynamo-fuse:main
    #
    # Would create an entry point called dynamo-fuse that calls dynamo-fuse.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - dynamofuse
    - dynamofuse.records

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/denismo/dynamo-fuse
  license: GNU General Public License v3 (GPLv3)
  summary: 'Linux FUSE file system implementation with AWS DynamoDB as the storage'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
