package:
  name: mopidy-local-whoosh
  version: "0.1.0"

source:
  fn: Mopidy-Local-Whoosh-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/M/Mopidy-Local-Whoosh/Mopidy-Local-Whoosh-0.1.0.tar.gz
  md5: 86331c74290b9fb78a4c42c8f8911a4c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mopidy-local-whoosh = mopidy-local-whoosh:main
    #
    # Would create an entry point called mopidy-local-whoosh that calls mopidy-local-whoosh.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - mopidy >=0.18
    - whoosh >=2.0

  run:
    - python
    - setuptools
    - setuptools
    - mopidy >=0.18
    - whoosh >=2.0

test:
  # Python imports
  imports:
    - mopidy_local_whoosh

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mopidy/mopidy-local-whoosh
  license: Apache Software License
  summary: u'Whoosh local library extension.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
