package:
  name: django-powerdns
  version: "0.2"

source:
  fn: django-powerdns-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-powerdns/django-powerdns-0.2.tar.gz
  md5: 9c5f3dbd3e09de1d115cdca91185d04b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-powerdns = django-powerdns:main
    #
    # Would create an entry point called django-powerdns that calls django-powerdns.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.2

  run:
    - python
    - django >=1.2

test:
  # Python imports
  imports:
    - example
    - powerdns

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/peternixon/django-powerdns/
  license: BSD License
  summary: 'PowerDNS administration module for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
