package:
  name: password-generator
  version: "0.1.0"

source:
  fn: password-generator-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/p/password-generator/password-generator-0.1.0.tar.gz
  md5: 7cbcac23cadf77954ec35f77af852586
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - password-generator = password-generator:main
    #
    # Would create an entry point called password-generator that calls password-generator.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://sebdah.github.com/password-generator/
  license: Apache Software License
  summary: 'Simple password generator for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
