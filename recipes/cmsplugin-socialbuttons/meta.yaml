package:
  name: cmsplugin-socialbuttons
  version: "0.1.2"

source:
  fn: cmsplugin-socialbuttons-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/c/cmsplugin-socialbuttons/cmsplugin-socialbuttons-0.1.2.tar.gz
  md5: 5a1495b32a03fe3c7094a9b9ab3628f8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cmsplugin-socialbuttons = cmsplugin-socialbuttons:main
    #
    # Would create an entry point called cmsplugin-socialbuttons that calls cmsplugin-socialbuttons.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - cmsplugin_socialbuttons
    - cmsplugin_socialbuttons.migrations
    - cmsplugin_socialbuttons.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jurheinsieg/cmsplugin-socialbuttons
  license: BSD License
  summary: 'A simple plugin for adding social buttons.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
