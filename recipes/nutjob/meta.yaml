package:
  name: nutjob
  version: "0.1.0"

source:
  fn: nutjob-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/n/nutjob/nutjob-0.1.0.tar.gz
  md5: 4119eab466a37f6b07dae3d876702d0d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - nutjob = nutjob:main
    #
    # Would create an entry point called nutjob that calls nutjob.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - redis

  run:
    - python
    - setuptools
    - redis

test:
  # Python imports
  imports:
    - nutjob

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/klmitch/nutjob
  license: Apache Software License
  summary: 'Redis/nutcracker client for Turnstile'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
