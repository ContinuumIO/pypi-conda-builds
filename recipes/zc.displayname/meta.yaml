package:
  name: zc.displayname
  version: "1.1"

source:
  fn: zc.displayname-1.1.tar.gz
  url: https://pypi.python.org/packages/source/z/zc.displayname/zc.displayname-1.1.tar.gz
  md5: 68bf950b0c2e7e8d4bd890b57e034f62
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zc.displayname = zc.displayname:main
    #
    # Would create an entry point called zc.displayname that calls zc.displayname.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.app.container
    - zope.app.pagetemplate
    - zope.component
    - zope.dublincore
    - zope.i18n
    - zope.i18nmessageid
    - zope.interface
    - zope.location
    - zope.proxy
    - zope.publisher
    - zope.security
    - zope.testing
    - zope.traversing

  run:
    - python
    - setuptools
    - zope.app.container
    - zope.app.pagetemplate
    - zope.component
    - zope.dublincore
    - zope.i18n
    - zope.i18nmessageid
    - zope.interface
    - zope.location
    - zope.proxy
    - zope.publisher
    - zope.security
    - zope.testing
    - zope.traversing

test:
  # Python imports
  imports:
    - zc
    - zc.displayname

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: ZPL 2.1
  summary: 'A Zope 3 extension for pluggable display names.\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
