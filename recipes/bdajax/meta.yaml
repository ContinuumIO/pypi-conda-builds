package:
  name: bdajax
  version: "1.5.2"

source:
  fn: bdajax-1.5.2.tar.gz
  url: https://pypi.python.org/packages/source/b/bdajax/bdajax-1.5.2.tar.gz
  md5: 7c71b15b68333e59f381a7827fbe707a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bdajax = bdajax:main
    #
    # Would create an entry point called bdajax that calls bdajax.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - bdajax

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bluedynamics/bdajax
  license: GNU General Public Licence
  summary: 'Ajax convenience.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
