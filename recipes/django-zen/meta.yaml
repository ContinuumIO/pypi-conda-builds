package:
  name: django-zen
  version: "0.1.14"

source:
  fn: django-zen-0.1.14.tar.gz
  url: https://pypi.python.org/packages/source/d/django-zen/django-zen-0.1.14.tar.gz
  md5: c573a13359d3770c1b7cf9e2bfc36bf8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-zen = django-zen:main
    #
    # Would create an entry point called django-zen that calls django-zen.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4
    - django-mptt >=0.5
    - feincms >=1.7.4
    - django-wysiwyg-redactor ==0.3.5

  run:
    - python
    - django >=1.4
    - django-mptt >=0.5
    - feincms >=1.7.4
    - django-wysiwyg-redactor ==0.3.5

test:
  # Python imports
  imports:
    - django_zen
    - django_zen.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/DigitalDemiurge/django-zen.git
  license: MIT license
  summary: 'Zen-Lightweight Django CMS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
