package:
  name: ipymongo
  version: "0.0dev-20120914"

source:
  fn: IPyMongo-0.0dev-20120914.tar.gz
  url: https://pypi.python.org/packages/source/I/IPyMongo/IPyMongo-0.0dev-20120914.tar.gz
  md5: 66fa726363c6a190ef36a0423ac6449f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ipymongo = ipymongo:main
    #
    # Would create an entry point called ipymongo that calls ipymongo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - ipython
    - pymongo

  run:
    - python
    - setuptools
    - ipython
    - pymongo

test:
  # Python imports
  imports:
    - ipymongo

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: Apache
  summary: 'IPython extension and wrapper for using with MongoDB'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
