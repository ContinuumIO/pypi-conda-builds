package:
  name: cyanite
  version: "0.4.5"

source:
  fn: cyanite-0.4.5.tar.gz
  url: https://pypi.python.org/packages/source/c/cyanite/cyanite-0.4.5.tar.gz
  md5: 7d6382b1b1b3a4b95dc344815ac29d79
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cyanite = cyanite:main
    #
    # Would create an entry point called cyanite that calls cyanite.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - cyanite

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/brutasse/graphite-cyanite
  license: BSD License
  summary: 'A plugin for using graphite-web with the cassandra-based Cyanite storage backend'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
