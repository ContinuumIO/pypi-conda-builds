package:
  name: foursquare.pants.rules
  version: "0.0.1"

source:
  fn: foursquare.pants.rules-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/f/foursquare.pants.rules/foursquare.pants.rules-0.0.1.tar.gz
  md5: 2ff1c18021ec9bd35fc82200246d1858
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - foursquare.pants.rules = foursquare.pants.rules:main
    #
    # Would create an entry point called foursquare.pants.rules that calls foursquare.pants.rules.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - foursquare.pants.rules

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/foursquare/pants-rules
  license: Apache Software License
  summary: 'BUILD graph validate rules plugin for pants build'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
