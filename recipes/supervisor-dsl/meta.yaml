package:
  name: supervisor-dsl
  version: "0.0.3"

source:
  fn: supervisor-dsl-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/s/supervisor-dsl/supervisor-dsl-0.0.3.tar.gz
  md5: 78a677ff41120adfb634d7d68682baac
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - supervisor-dsl = supervisor-dsl:main
    #
    # Would create an entry point called supervisor-dsl that calls supervisor-dsl.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools >=12.0,<13.0
    - enum34 >=1.0

  run:
    - python
    - setuptools >=12.0,<13.0
    - enum34 >=1.0

test:
  # Python imports
  imports:
    - supervisor_dsl

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/aspyatkin/supervisor_dsl
  license: MIT
  summary: 'Domain specific language for building Supervisord program configuration'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
