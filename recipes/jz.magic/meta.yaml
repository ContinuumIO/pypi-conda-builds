package:
  name: jz.magic
  version: "0.1"

source:
  fn: jz.magic-0.1.tar.gz
  url: https://pypi.python.org/packages/source/j/jz.magic/jz.magic-0.1.tar.gz
  md5: 9255aa3799d8438fe0c6c26aa593357f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jz.magic = jz.magic:main
    #
    # Would create an entry point called jz.magic that calls jz.magic.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - jz
    - jz.magic
    - jz.magic.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.jellofishi.com/zope
  license: GPL
  summary: 'Utility interface to magic python module.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
