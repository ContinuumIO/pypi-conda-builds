package:
  name: django-imapauth
  version: "0.1"

source:
  fn: django-imapauth-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-imapauth/django-imapauth-0.1.tar.gz
  md5: af145e826dd700ff049519bce0be4d74
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-imapauth = django-imapauth:main
    #
    # Would create an entry point called django-imapauth that calls django-imapauth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - imapauth

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ouhouhsami/django-imapauth
  license: BSD License
  summary: 'Imap authentification backend for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
