package:
  name: django-mediagenerator-kensho
  version: "1.6"

source:
  fn: django-mediagenerator-kensho-1.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-mediagenerator-kensho/django-mediagenerator-kensho-1.6.tar.gz
  md5: 8166cee3b7e73cbba29def2f0ec08bfb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-mediagenerator-kensho = django-mediagenerator-kensho:main
    #
    # Would create an entry point called django-mediagenerator-kensho that calls django-mediagenerator-kensho.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - pathtools
    - watchdog

  run:
    - python
    - flask
    - pathtools
    - watchdog

test:
  # Python imports
  imports:
    - mediagenerator
    - mediagenerator.contrib
    - mediagenerator.filters
    - mediagenerator.generators
    - mediagenerator.generators.bundles
    - mediagenerator.management
    - mediagenerator.management.commands
    - mediagenerator.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.allbuttonspressed.com/projects/django-mediagenerator
  license: BSD License
  summary: "Kensho's fork of mediagenerator"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
