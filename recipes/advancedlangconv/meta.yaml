package:
  name: advancedlangconv
  version: "0.01"

source:
  fn: AdvancedLangConv-0.01.tar.gz
  url: https://pypi.python.org/packages/source/A/AdvancedLangConv/AdvancedLangConv-0.01.tar.gz
  md5: a9c6e1e7f8b85d57a15b28764bd1963e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - advancedlangconv = advancedlangconv:main
    #
    # Would create an entry point called advancedlangconv that calls advancedlangconv.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - advancedlangconv
    - langconv
    - langconv.defaulttables

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/advanced-langconv/
  license: UNKNOWN
  summary: 'Advanced Language Converter'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
