package:
  name: net_conf
  version: "0.2.1"

source:
  fn: net_conf-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/n/net_conf/net_conf-0.2.1.tar.gz
  md5: 6ce94751347c5491efa9d0d7b3f715ca
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - net_conf = net_conf:main
    #
    # Would create an entry point called net_conf that calls net_conf.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - ipaddr ==2.1.7

  run:
    - python
    - ipaddr ==2.1.7

test:
  # Python imports
  imports:
    - net_conf

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/net_conf/
  license: LICENSE.txt
  summary: 'Commandline-based network manager'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
