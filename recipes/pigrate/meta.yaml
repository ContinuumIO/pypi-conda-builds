package:
  name: pigrate
  version: "0.0.3"

source:
  fn: pigrate-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/p/pigrate/pigrate-0.0.3.tar.gz
  md5: 7df93e94d91cecd4f9dbb1b0b115ae8a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pigrate = pigrate:main
    #
    # Would create an entry point called pigrate that calls pigrate.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - pigrate

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/eiiches/pigrate
  license: UNKNOWN
  summary: 'Database schema migration tool written in Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
