package:
  name: collective.portlet.contentleadimage
  version: "0.5.1"

source:
  fn: collective.portlet.contentleadimage-0.5.1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.portlet.contentleadimage/collective.portlet.contentleadimage-0.5.1.tar.gz
  md5: 1497d21d16b09142a72485d29d6ed0aa
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.portlet.contentleadimage = collective.portlet.contentleadimage:main
    #
    # Would create an entry point called collective.portlet.contentleadimage that calls collective.portlet.contentleadimage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.portlet.collection
    - collective.contentleadimage

  run:
    - python
    - setuptools
    - setuptools
    - plone.portlet.collection
    - collective.contentleadimage

test:
  # Python imports
  imports:
    - collective
    - collective.portlet
    - collective.portlet.contentleadimage
    - collective.portlet.contentleadimage.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/collective/collective.portlet.contentleadimage
  license: GNU General Public License (GPL)
  summary: 'Collection portlet that shows contenteleadimages'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
