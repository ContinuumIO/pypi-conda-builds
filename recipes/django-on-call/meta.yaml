package:
  name: django-on-call
  version: "0.5"

source:
  fn: django-on-call-0.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-on-call/django-on-call-0.5.tar.gz
  md5: 460fddcd54457979f983ce663c802051
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-on-call = django-on-call:main
    #
    # Would create an entry point called django-on-call that calls django-on-call.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - django_on_call

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/wking/django-on-call
  license: BSD License
  summary: 'Point website users at configurable on-call administrators'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
