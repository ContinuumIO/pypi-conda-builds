package:
  name: heroku3
  version: "3.1.3"

source:
  fn: heroku3-3.1.3.tar.gz
  url: https://pypi.python.org/packages/source/h/heroku3/heroku3-3.1.3.tar.gz
  md5: 09dc14c758a59544fabc6b9e1477db9c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - heroku3 = heroku3:main
    #
    # Would create an entry point called heroku3 that calls heroku3.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=1.2.3
    - simplejson ==3.3.1
    - python-dateutil ==1.5

  run:
    - python
    - requests >=1.2.3
    - simplejson ==3.3.1
    - python-dateutil ==1.5

test:
  # Python imports
  imports:
    - heroku3

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/martyzz1/heroku3.py
  license: MIT License
  summary: 'Heroku API Wrapper.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
