package:
  name: products.radioschedule
  version: "1.0a"

source:
  fn: Products.RadioSchedule-1.0a.tar.gz
  url: https://pypi.python.org/packages/source/P/Products.RadioSchedule/Products.RadioSchedule-1.0a.tar.gz
  md5: a9b764ffdab717f13046f80fbff90aa1
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - products.radioschedule = products.radioschedule:main
    #
    # Would create an entry point called products.radioschedule that calls products.radioschedule.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - Products
    - Products.RadioSchedule
    - Products.RadioSchedule.contents

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.nidelven-it.no/d
  license: GPL
  summary: 'A content-type for storing information about radio show sending schedules'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
