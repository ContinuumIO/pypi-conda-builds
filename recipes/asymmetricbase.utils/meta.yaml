package:
  name: asymmetricbase.utils
  version: "0.1.4-201404241723"

source:
  fn: asymmetricbase.utils-0.1.4-201404241723.tar.gz
  url: https://pypi.python.org/packages/source/a/asymmetricbase.utils/asymmetricbase.utils-0.1.4-201404241723.tar.gz
  md5: 10eb3b9cc7b5f78c8d671f0ca89b0507
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - asymmetricbase.utils = asymmetricbase.utils:main
    #
    # Would create an entry point called asymmetricbase.utils that calls asymmetricbase.utils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4.5
    - jinja2 >=2.7
    - pytz
    - hamlpy
    - asymmetricbase.fields

  run:
    - python
    - django >=1.4.5
    - jinja2 >=2.7
    - pytz
    - hamlpy
    - asymmetricbase.fields

test:
  # Python imports
  imports:
    - asymmetricbase
    - asymmetricbase.management
    - asymmetricbase.management.commands
    - asymmetricbase.middleware
    - asymmetricbase.tests
    - asymmetricbase.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/AsymmetricVentures/asym-utils
  license: GNU General Public License v2 (GPLv2)
  summary: 'Utilies for Asymmetric Base Framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
