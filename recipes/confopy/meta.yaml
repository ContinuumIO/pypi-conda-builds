package:
  name: confopy
  version: "0.4.7"

source:
  fn: Confopy-0.4.7.tar.gz
  url: https://pypi.python.org/packages/source/C/Confopy/Confopy-0.4.7.tar.gz
  md5: 03625b0d765fe0e71cb5aed166d1e546
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - confopy = confopy:main
    #
    # Would create an entry point called confopy that calls confopy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - lxml >=3.3.5
    - numpy ==1.6.2
    - nltk >=3.0.0
    - pattern ==2.6
    - pyenchant ==1.6.5
    - pdfminer ==20110515

  run:
    - python
    - lxml >=3.3.5
    - numpy ==1.6.2
    - nltk >=3.0.0
    - pattern ==2.6
    - pyenchant ==1.6.5
    - pdfminer ==20110515

test:
  # Python imports
  imports:
    - confopy
    - confopy.analysis
    - confopy.localization
    - confopy.localization.de
    - confopy.localization.de.corpus_de
    - confopy.model
    - confopy.pdfextract

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ooz/Confopy
  license: MIT License
  summary: 'Evaluates the linguistic and structural quality of scientific texts.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
