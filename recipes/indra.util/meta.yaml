package:
  name: indra.util
  version: "1.0"

source:
  fn: indra.util-1.0.tar.gz
  url: https://pypi.python.org/packages/source/i/indra.util/indra.util-1.0.tar.gz
  md5: 0ed5a0a9f26d336affb0486f485006fc
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - indra.util = indra.util:main
    #
    # Would create an entry point called indra.util that calls indra.util.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - indra
    - indra.util

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://wiki.secondlife.com/wiki
  license: MIT License
  summary: 'Utility functions for the indra library collection'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
