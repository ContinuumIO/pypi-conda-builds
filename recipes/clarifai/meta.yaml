package:
  name: clarifai
  version: "0.1"

source:
  fn: clarifai-0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/clarifai/clarifai-0.1.tar.gz
  md5: 941c676ef926670036d96f64d3ee42e2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - clarifai = clarifai:main
    #
    # Would create an entry point called clarifai that calls clarifai.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - clarifai
    - clarifai.client

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/clarifai/clarifai_py
  license: UNKNOWN
  summary: 'Clarifai API Python Client'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
