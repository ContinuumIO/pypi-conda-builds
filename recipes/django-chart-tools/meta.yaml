package:
  name: django-chart-tools
  version: "1.0"

source:
  fn: django-chart-tools-1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-chart-tools/django-chart-tools-1.0.tar.gz
  md5: 1e395befda9ff0b0b0d510701498ac61
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-chart-tools = django-chart-tools:main
    #
    # Would create an entry point called django-chart-tools that calls django-chart-tools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - chart_tools
    - chart_tools.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/kmike/django-chart-tools/
  license: MIT License
  summary: 'A thin wrapper around Google Chart API that tries not to invent a new language for describing charts.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
