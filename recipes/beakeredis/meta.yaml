package:
  name: beakeredis
  version: "0.0.3"

source:
  fn: beakeredis-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/b/beakeredis/beakeredis-0.0.3.tar.gz
  md5: 94c56291ac790dba1f103f6dbd8675be
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - beakeredis = beakeredis:main
    #
    # Would create an entry point called beakeredis that calls beakeredis.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - beaker >=1.6.4,<1.7
    - redis >=2.10

  run:
    - python
    - setuptools
    - beaker >=1.6.4,<1.7
    - redis >=2.10

test:
  # Python imports
  imports:
    - beakeredis

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/aspyatkin/beakeredis
  license: MIT License
  summary: 'Redis backend for Beaker'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
