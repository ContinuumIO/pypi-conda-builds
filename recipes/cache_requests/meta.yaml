package:
  name: cache_requests
  version: "1.0.0"

source:
  fn: cache_requests-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/c/cache_requests/cache_requests-1.0.0.tar.gz
  md5: 10e04a51ee1a6a83759d3d61857fe20f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cache_requests = cache_requests:main
    #
    # Would create an entry point called cache_requests that calls cache_requests.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - redislite
    - requests

  run:
    - python
    - redislite
    - requests

test:
  # Python imports
  imports:
    - cache_requests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bionikspoon/cache_requests
  license: MIT License
  summary: 'Simple. Powerful. Persistent LRU caching for the requests library.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
