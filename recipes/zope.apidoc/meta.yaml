package:
  name: zope.apidoc
  version: "2.0.0a1"

source:
  fn: zope.apidoc-2.0.0a1.zip
  url: https://pypi.python.org/packages/source/z/zope.apidoc/zope.apidoc-2.0.0a1.zip
  md5: 82f94606f55b007522e5afb32fae9827
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zope.apidoc = zope.apidoc:main
    #
    # Would create an entry point called zope.apidoc that calls zope.apidoc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six
    - zope.annotation
    - zope.browserpage
    - zope.browserresource
    - zope.cachedescriptors
    - zope.component
    - zope.container
    - zope.i18nmessageid
    - zope.interface
    - zope.location
    - zope.publisher
    - zope.renderer
    - zope.schema
    - zope.security
    - zope.traversing

  run:
    - python
    - setuptools
    - six
    - zope.annotation
    - zope.browserpage
    - zope.browserresource
    - zope.cachedescriptors
    - zope.component
    - zope.container
    - zope.i18nmessageid
    - zope.interface
    - zope.location
    - zope.publisher
    - zope.renderer
    - zope.schema
    - zope.security
    - zope.traversing

test:
  # Python imports
  imports:
    - zope
    - zope.apidoc

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/zope.apidoc
  license: Zope Public License
  summary: 'API Documentation and Component Inspection for Zope 3'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
