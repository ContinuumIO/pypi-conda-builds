package:
  name: queuelib
  version: "1.2.2"

source:
  fn: queuelib-1.2.2.tar.gz
  url: https://pypi.python.org/packages/source/q/queuelib/queuelib-1.2.2.tar.gz
  md5: 90cd84e977896fa50cde6aa3df7fc45f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - queuelib = queuelib:main
    #
    # Would create an entry point called queuelib that calls queuelib.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - queuelib
    - queuelib.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/scrapy/queuelib
  license: BSD License
  summary: 'Collection of persistent (disk-based) queues'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
