package:
  name: django-adjax
  version: "0.1"

source:
  fn: django-adjax-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-adjax/django-adjax-0.1.tar.gz
  md5: 981e79ba7741f83f005e2fa79dd35994
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-adjax = django-adjax:main
    #
    # Would create an entry point called django-adjax that calls django-adjax.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6.5
    - six >=1.6.1

  run:
    - python
    - django >=1.6.5
    - six >=1.6.1

test:
  # Python imports
  imports:
    - adjax
    - adjax.conf
    - adjax.templatetags
    - adjax.tests
    - adjax.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://adjax.io
  license: MIT License
  summary: 'Django AJAX RPC.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
