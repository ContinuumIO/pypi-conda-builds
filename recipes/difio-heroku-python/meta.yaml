package:
  name: difio-heroku-python
  version: "1.1"

source:
  fn: difio-heroku-python-1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/difio-heroku-python/difio-heroku-python-1.1.tar.gz
  md5: 52fe6de4b15bf6b749cae373e1703317
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - difio-heroku-python = difio-heroku-python:main
    #
    # Would create an entry point called difio-heroku-python that calls difio-heroku-python.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - difio_heroku_python

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/difio/difio-heroku-python
  license: MIT License
  summary: 'Difio registration agent for Heroku / Python applications'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
