package:
  name: isotoma.recipe.gocaptain
  version: "0.0.9"

source:
  fn: isotoma.recipe.gocaptain-0.0.9.tar.gz
  url: https://pypi.python.org/packages/source/i/isotoma.recipe.gocaptain/isotoma.recipe.gocaptain-0.0.9.tar.gz
  md5: 5e52c9f9c891fb039bc3c523c40b5555
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - isotoma.recipe.gocaptain = isotoma.recipe.gocaptain:main
    #
    # Would create an entry point called isotoma.recipe.gocaptain that calls isotoma.recipe.gocaptain.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - cheetah

  run:
    - python
    - setuptools
    - setuptools
    - cheetah

test:
  # Python imports
  imports:
    - isotoma
    - isotoma.recipe
    - isotoma.recipe.gocaptain
    - isotoma.recipe.gocaptain.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/isotoma.recipe.gocaptain
  license: Apache Software License
  summary: 'Starting and stopping daemons'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
