package:
  name: eastofeaton.logopanel
  version: "1.1"

source:
  fn: eastofeaton.logopanel-1.1.tar.gz
  url: https://pypi.python.org/packages/source/e/eastofeaton.logopanel/eastofeaton.logopanel-1.1.tar.gz
  md5: bd5437f24ad0155049da7302af6bd2a4
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - eastofeaton.logopanel = eastofeaton.logopanel:main
    #
    # Would create an entry point called eastofeaton.logopanel that calls eastofeaton.logopanel.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - eastofeaton
    - eastofeaton.logopanel
    - eastofeaton.logopanel.browser
    - eastofeaton.logopanel.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/cewing/eastofeaton.logopanel
  license: gpl
  summary: 'A Plone control panel  to allow overriding the plone site logo'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
