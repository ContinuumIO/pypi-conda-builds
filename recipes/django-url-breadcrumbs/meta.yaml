package:
  name: django-url-breadcrumbs
  version: "0.3.3"

source:
  fn: django-url-breadcrumbs-0.3.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-url-breadcrumbs/django-url-breadcrumbs-0.3.3.tar.gz
  md5: 299bdcb4ab87854615a7438d20583c3b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-url-breadcrumbs = django-url-breadcrumbs:main
    #
    # Would create an entry point called django-url-breadcrumbs that calls django-url-breadcrumbs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - urlbreadcrumbs
    - urlbreadcrumbs.templatetags
    - urlbreadcrumbs.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/slafs/django-url-breadcrumbs/
  license: BSD License
  summary: 'a generic app to provide a simple breadcrumbs functionality in django projects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
