package:
  name: django-select2-forms
  version: "1.1.18"

source:
  fn: django-select2-forms-1.1.18.tar.gz
  url: https://pypi.python.org/packages/source/d/django-select2-forms/django-select2-forms-1.1.18.tar.gz
  md5: fb104cf7b4b499f322f922802cbb2592
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-select2-forms = django-select2-forms:main
    #
    # Would create an entry point called django-select2-forms that calls django-select2-forms.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - select2
    - select2.build
    - select2.models

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/theatlantic/django-select2-forms
  license: UNKNOWN
  summary: 'Django form fields using the Select2 jQuery plugin'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
