package:
  name: django-easydata
  version: "0.1"

source:
  fn: django-easydata-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-easydata/django-easydata-0.1.tar.gz
  md5: de9e05975d9287d941dee9f129e089fa
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-easydata = django-easydata:main
    #
    # Would create an entry point called django-easydata that calls django-easydata.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4
    - rdflib ==4.0.1
    - pydot

  run:
    - python
    - django >=1.4
    - rdflib ==4.0.1
    - pydot

test:
  # Python imports
  imports:
    - easydata
    - easydata.decorators
    - easydata.forms
    - easydata.forms.forms
    - easydata.forms.modelforms
    - easydata.management
    - easydata.management.commands
    - easydata.models
    - easydata.parsing
    - easydata.templatetags
    - easydata.views

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: GNU License
  summary: 'A Django app to publish your project data using ontologies.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
