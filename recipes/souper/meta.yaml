package:
  name: souper
  version: "1.0.2"

source:
  fn: souper-1.0.2.zip
  url: https://pypi.python.org/packages/source/s/souper/souper-1.0.2.zip
  md5: f3c64a13b6bde42c8320a0ff165d47c7
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - souper = souper:main
    #
    # Would create an entry point called souper that calls souper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - node.ext.zodb
    - repoze.catalog

  run:
    - python
    - setuptools
    - node.ext.zodb
    - repoze.catalog

test:
  # Python imports
  imports:
    - souper

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://packages.python.org/souper
  license: BSD License
  summary: 'Souper - Generic Indexed Storage based on ZODB'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
