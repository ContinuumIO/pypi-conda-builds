package:
  name: kr-smmap
  version: "0.8.1"

source:
  fn: kr-smmap-0.8.1.tar.gz
  url: https://pypi.python.org/packages/source/k/kr-smmap/kr-smmap-0.8.1.tar.gz
  md5: 689536bab92c193941d368c7e2e4f070
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kr-smmap = kr-smmap:main
    #
    # Would create an entry point called kr-smmap that calls kr-smmap.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - smmap
    - smmap.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Byron/smmap
  license: BSD License
  summary: 'A pure git implementation of a sliding window memory map manager'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
