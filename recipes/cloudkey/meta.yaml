package:
  name: cloudkey
  version: "1.2.6"

source:
  fn: cloudkey-1.2.6.tar.gz
  url: https://pypi.python.org/packages/source/c/cloudkey/cloudkey-1.2.6.tar.gz
  md5: 9458060c1bec20773cd4e1b4421b30cb
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cloudkey = cloudkey:main
    #
    # Would create an entry point called cloudkey that calls cloudkey.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - simplejson >=2.0.9
    - pycurl >=7.19.0

  run:
    - python
    - setuptools
    - simplejson >=2.0.9
    - pycurl >=7.19.0

test:
  # Python imports
  imports:
    - cloudkey

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/dailymotion/cloudkey-py
  license: Apache Software License
  summary: 'Dailymotion Cloud API client library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
