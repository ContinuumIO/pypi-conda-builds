package:
  name: django-reporting
  version: "0.21"

source:
  fn: django-reporting-0.21.tar.gz
  url: https://pypi.python.org/packages/source/d/django-reporting/django-reporting-0.21.tar.gz
  md5: 090dca908644a08fa1a7ffdaa2642c6d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-reporting = django-reporting:main
    #
    # Would create an entry point called django-reporting that calls django-reporting.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.2

  run:
    - python
    - django >=1.2

test:
  # Python imports
  imports:
    - reporting

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tryolabs/django-reporting
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Django Reporting is an application that can be integrated with the Django Admin and allows you to create dynamic reports for your models.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
