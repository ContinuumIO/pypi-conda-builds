package:
  name: lbindex
  version: "0.1.1"

source:
  fn: LBIndex-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/L/LBIndex/LBIndex-0.1.1.tar.gz
  md5: e404b3a9e5e159df0a67dfb1b65fb266
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lbindex = lbindex:main
    #
    # Would create an entry point called lbindex that calls lbindex.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - lockfile ==0.8
    - python-daemon ==1.5.4
    - configparser
    - requests
    - pyelasticsearch

  run:
    - python
    - lockfile ==0.8
    - python-daemon ==1.5.4
    - configparser
    - requests
    - pyelasticsearch

test:
  # Python imports
  imports:
    - lbindex
    - lbindex.scripts

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: #
  license: GNU General Public License v2 (GPLv2)
  summary: 'Indexer Daemon for the neo-lightbase service'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
