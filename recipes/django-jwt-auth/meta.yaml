package:
  name: django-jwt-auth
  version: "0.0.1"

source:
  fn: django-jwt-auth-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-jwt-auth/django-jwt-auth-0.0.1.tar.gz
  md5: a3ff55aff2f8d82ace05b673ba6f4653
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-jwt-auth = django-jwt-auth:main
    #
    # Would create an entry point called django-jwt-auth that calls django-jwt-auth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyjwt ==0.2.1

  run:
    - python
    - pyjwt ==0.2.1

test:
  # Python imports
  imports:
    - jwt_auth

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jpadilla/django-jwt-auth
  license: MIT License
  summary: 'JSON Web Token based authentication for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
