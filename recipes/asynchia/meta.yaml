package:
  name: asynchia
  version: "0.1.3"

source:
  fn: asynchia-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/a/asynchia/asynchia-0.1.3.tar.gz
  md5: e32c88a2df9b44ff0031a78174cb5b47
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - asynchia = asynchia:main
    #
    # Would create an entry point called asynchia that calls asynchia.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - asynchia

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/segfaulthunter/asynchia-mainline
  license: LGPL
  summary: 'asynchia is a minimalistic asynchronous networking library.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
