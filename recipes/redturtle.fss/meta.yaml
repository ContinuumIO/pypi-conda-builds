package:
  name: redturtle.fss
  version: "1.0.1"

source:
  fn: redturtle.fss-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/r/redturtle.fss/redturtle.fss-1.0.1.tar.gz
  md5: 652e41944ae3149e9c85c18ef79a363d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - redturtle.fss = redturtle.fss:main
    #
    # Would create an entry point called redturtle.fss that calls redturtle.fss.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - redturtle
    - redturtle.fss

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://code.redturtle.it/svn/redturtle/redturtle.fss
  license: GPL
  summary: 'Simply apply FileSystemStorage strategies (iw.fss) to basic Plone content types.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
