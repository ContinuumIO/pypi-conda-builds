package:
  name: collective.dancingnotlikely
  version: "1.0.2"

source:
  fn: collective.dancingnotlikely-1.0.2.zip
  url: https://pypi.python.org/packages/source/c/collective.dancingnotlikely/collective.dancingnotlikely-1.0.2.zip
  md5: 43ddebcb4ff3c13ccec99dc7d6d2bded
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.dancingnotlikely = collective.dancingnotlikely:main
    #
    # Would create an entry point called collective.dancingnotlikely that calls collective.dancingnotlikely.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.dancing

  run:
    - python
    - setuptools
    - setuptools
    - collective.dancing

test:
  # Python imports
  imports:
    - collective
    - collective.dancingnotlikely

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://plone.org/products/collective.dancingnotlikely
  license: GPL
  summary: 'A plugin for collective.dancing that add more elements to ignore-list in the composer'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
