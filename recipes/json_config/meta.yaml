package:
  name: json_config
  version: "1.1.0"

source:
  fn: json_config-1.1.0.tar.gz
  url: https://pypi.python.org/packages/source/j/json_config/json_config-1.1.0.tar.gz
  md5: d479d31c58880c9a53004864972cc25f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - json_config = json_config:main
    #
    # Would create an entry point called json_config that calls json_config.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - json_config

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bionikspoon/json_config
  license: MIT License
  summary: 'A convenience utility for working with JSON config files.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
