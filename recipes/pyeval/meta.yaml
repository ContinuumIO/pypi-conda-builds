package:
  name: pyeval
  version: "0.2"

source:
  fn: pyeval-0.2.tar.gz
  url: https://pypi.python.org/packages/source/p/pyeval/pyeval-0.2.tar.gz
  md5: 4fc885573ebc7cb5fffb5445dc1887d7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pyeval = pyeval:main
    #
    # Would create an entry point called pyeval that calls pyeval.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - pyeval
    - pyeval.magic
    - pyeval.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/nejucomo/pyeval
  license: GPLv3
  summary: 'Conveniently evaluate expressions from the shell'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
