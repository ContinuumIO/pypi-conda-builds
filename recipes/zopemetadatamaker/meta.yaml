package:
  name: zopemetadatamaker
  version: "0.1.0"

source:
  fn: zopemetadatamaker-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/z/zopemetadatamaker/zopemetadatamaker-0.1.0.tar.gz
  md5: 00ce8bf4ba0d5ef01296fd6859f7f215
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zopemetadatamaker = zopemetadatamaker:main
    #
    # Would create an entry point called zopemetadatamaker that calls zopemetadatamaker.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - zopemetadatamaker

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/zopemetadatamaker
  license: GNU General Public License (GPL)
  summary: 'Bulk creation of .metadata files for Zope skins resources'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
