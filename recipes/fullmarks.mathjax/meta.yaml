package:
  name: fullmarks.mathjax
  version: "1.0"

source:
  fn: fullmarks.mathjax-1.0.zip
  url: https://pypi.python.org/packages/source/f/fullmarks.mathjax/fullmarks.mathjax-1.0.zip
  md5: 28206acaf0fef714e2caf162b66f3e2a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fullmarks.mathjax = fullmarks.mathjax:main
    #
    # Would create an entry point called fullmarks.mathjax that calls fullmarks.mathjax.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - fullmarks
    - fullmarks.mathjax

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/fullmarks/fullmarks.mathjax
  license: GPL
  summary: 'MathJax for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
