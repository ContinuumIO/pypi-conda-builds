package:
  name: icao-aircrft
  version: "0.0.9"

source:
  fn: icao-aircrft-0.0.9.tar.gz
  url: https://pypi.python.org/packages/source/i/icao-aircrft/icao-aircrft-0.0.9.tar.gz
  md5: 6dcc62281c83d7ecea2bb819d620eb7d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - icao-aircrft = icao-aircrft:main
    #
    # Would create an entry point called icao-aircrft that calls icao-aircrft.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/marians/py-icao-aircrft
  license: UNKNOWN
  summary: 'Python client for lookup of ICAO aircraft (Doc 8643) information'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
