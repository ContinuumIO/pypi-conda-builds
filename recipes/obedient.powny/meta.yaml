package:
  name: obedient.powny
  version: "3.8.0"

source:
  fn: obedient.powny-3.8.0.tar.gz
  url: https://pypi.python.org/packages/source/o/obedient.powny/obedient.powny-3.8.0.tar.gz
  md5: 9d8a2350b89a3448af697ad8e1d858a6
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - obedient.powny = obedient.powny:main
    #
    # Would create an entry point called obedient.powny that calls obedient.powny.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - dominator[full] >=15a
    - obedient.zookeeper >=3.0.1a
    - obedient.elk >=1.4a

  run:
    - python
    - setuptools
    - dominator[full] >=15a
    - obedient.zookeeper >=3.0.1a
    - obedient.elk >=1.4a

test:
  # Python imports
  imports:
    - obedient.powny

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/yandex-sysmon/obedient.powny
  license: GPLv3
  summary: 'Powny obedient for Dominator'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
