package:
  name: kreveik
  version: "0.6.0"

source:
  fn: Kreveik-0.6.0.tar.gz
  url: https://pypi.python.org/packages/source/K/Kreveik/Kreveik-0.6.0.tar.gz
  md5: 4938738aa9386d13731a335651e63836
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kreveik = kreveik:main
    #
    # Would create an entry point called kreveik that calls kreveik.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - docutils >=0.3
    - numpy >=1.5
    - matplotlib >=1.0

  run:
    - python
    - docutils >=0.3
    - numpy >=1.5
    - matplotlib >=1.0

test:
  # Python imports
  imports:
    - kreveik
    - kreveik.classes
    - kreveik.family
    - kreveik.genetic
    - kreveik.network
    - kreveik.network.boolfuncs
    - kreveik.probes

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/mehmetalianil/Kreveik/
  license: LICENSE.txt
  summary: 'Kreveik is a Python module for Boolean networks. With Kreveik, one can create, investigate dynamics of, form families of random Boolean networks.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
