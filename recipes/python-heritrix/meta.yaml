package:
  name: python-heritrix
  version: "0.1"

source:
  fn: python-heritrix-0.1.tar.gz
  url: https://pypi.python.org/packages/source/p/python-heritrix/python-heritrix-0.1.tar.gz
  md5: 03558a74126babb193a4ff137986f598
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-heritrix = python-heritrix:main
    #
    # Would create an entry point called python-heritrix that calls python-heritrix.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - heritrix

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/gwu-libraries/python-heritrix
  license: MIT License
  summary: 'Simple wrapper around Heritrix v3 API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
