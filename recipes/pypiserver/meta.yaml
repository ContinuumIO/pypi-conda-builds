package:
  name: pypiserver
  version: "1.1.7"

source:
  fn: pypiserver-1.1.7.zip
  url: https://pypi.python.org/packages/source/p/pypiserver/pypiserver-1.1.7.zip
  md5: ca91a523a66dd7bc3bfa654da676a7d4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pypiserver = pypiserver:main
    #
    # Would create an entry point called pypiserver that calls pypiserver.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - pypiserver

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/pypiserver/pypiserver
  license: BSD License or zlib/libpng License
  summary: 'minimal pypi server'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
