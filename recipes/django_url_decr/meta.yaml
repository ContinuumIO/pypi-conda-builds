package:
  name: django_url_decr
  version: "0.1.0"

source:
  fn: django_url_decr-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django_url_decr/django_url_decr-0.1.0.tar.gz
  md5: a0a0d98a3b91e734b32aab0c34164a7f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_url_decr = django_url_decr:main
    #
    # Would create an entry point called django_url_decr that calls django_url_decr.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - django_url_decr

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/yuexue/django_url_decr
  license: MIT License
  summary: 'A simple lib let you decorating Django view functions based on url patterns.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
