package:
  name: excellent
  version: "0.0.5"

source:
  fn: excellent-0.0.5.tar.gz
  url: https://pypi.python.org/packages/source/e/excellent/excellent-0.0.5.tar.gz
  md5: 77eeac556de06f0959a7f3149818d12f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - excellent = excellent:main
    #
    # Would create an entry point called excellent that calls excellent.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - xlwt
    - xlrd

  run:
    - python
    - xlwt
    - xlrd

test:
  # Python imports
  imports:
    - excellent
    - excellent.backends

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://tech.yipit.com/excellent
  license: UNKNOWN
  summary: 'Python library for writing CSV and XLS files'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
