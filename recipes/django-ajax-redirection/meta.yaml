package:
  name: django-ajax-redirection
  version: "1.0.0"

source:
  fn: django-ajax-redirection-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-ajax-redirection/django-ajax-redirection-1.0.0.tar.gz
  md5: 087f1d63afe30a12968db3a72b3996f3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-ajax-redirection = django-ajax-redirection:main
    #
    # Would create an entry point called django-ajax-redirection that calls django-ajax-redirection.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - dj_ajax_redirect
    - dj_ajax_redirect.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://hysoftware.net
  license: MIT License
  summary: 'Useful wrappers/middleware for django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
