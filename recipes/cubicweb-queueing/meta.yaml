package:
  name: cubicweb-queueing
  version: "0.3.1"

source:
  fn: cubicweb-queueing-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/c/cubicweb-queueing/cubicweb-queueing-0.3.1.tar.gz
  md5: 0336403a708f54b7b0cd6ed83965a932
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cubicweb-queueing = cubicweb-queueing:main
    #
    # Would create an entry point called cubicweb-queueing that calls cubicweb-queueing.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cubicweb >=3.16.0

  run:
    - python
    - cubicweb >=3.16.0

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.cubicweb.org/project/cubicweb-queueing
  license: LGPL
  summary: 'simple resource queue implementation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
