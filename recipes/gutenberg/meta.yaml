package:
  name: gutenberg
  version: "0.4.0"

source:
  fn: Gutenberg-0.4.0.tar.gz
  url: https://pypi.python.org/packages/source/G/Gutenberg/Gutenberg-0.4.0.tar.gz
  md5: 0e359008237d5f2b6306289aba8f0d49
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gutenberg = gutenberg:main
    #
    # Would create an entry point called gutenberg that calls gutenberg.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - rdflib >=4.2.0
    - requests >=2.5.1

  run:
    - python
    - rdflib >=4.2.0
    - requests >=2.5.1

test:
  # Python imports
  imports:
    - gutenberg
    - gutenberg._domain_model
    - gutenberg._util
    - gutenberg.acquire
    - gutenberg.cleanup
    - gutenberg.query

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/c-w/Gutenberg
  license: LICENSE.txt
  summary: 'Library to interface with Project Gutenberg'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
