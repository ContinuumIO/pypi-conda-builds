package:
  name: collective.portlet.toc
  version: "0.2"

source:
  fn: collective.portlet.toc-0.2.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.portlet.toc/collective.portlet.toc-0.2.tar.gz
  md5: 40a875923e4a851758307eb13f59c4cd
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.portlet.toc = collective.portlet.toc:main
    #
    # Would create an entry point called collective.portlet.toc that calls collective.portlet.toc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.portlet
    - collective.portlet.toc
    - collective.portlet.toc.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://svn.v2.nl/plone/collective.portlet.toc
  license: GPL
  summary: 'Portlet that shows the table of contents for the current item'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
