package:
  name: flask-lwadmin
  version: "0.6.3"

source:
  fn: Flask-LwAdmin-0.6.3.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-LwAdmin/Flask-LwAdmin-0.6.3.tar.gz
  md5: ee54302129d45a68264a17c413021356
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-lwadmin = flask-lwadmin:main
    #
    # Would create an entry point called flask-lwadmin that calls flask-lwadmin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.8
    - flask-jqueryuibootstrap >=0.5.0.4,<1.0.0.0
    - flask-bootstrap >=2.3.2.2,<3.0
    - flask-wtf >=0.9.1
    - flask-babel >=0.9
    - python-gettext >=2.1

  run:
    - python
    - flask >=0.8
    - flask-jqueryuibootstrap >=0.5.0.4,<1.0.0.0
    - flask-bootstrap >=2.3.2.2,<3.0
    - flask-wtf >=0.9.1
    - flask-babel >=0.9
    - python-gettext >=2.1

test:
  # Python imports
  imports:
    - SAMPLE_PROJECT
    - flask_lwadmin
    - flask_lwadmin.translations
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lightningwolf/Flask-LwAdmin
  license: MIT License
  summary: 'Flask-LwAdmin is minimalistic administrative interface building extension for the Flask framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
