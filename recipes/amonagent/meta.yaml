package:
  name: amonagent
  version: "1.3.1"

source:
  fn: amonagent-1.3.1.tar.gz
  url: https://pypi.python.org/packages/source/a/amonagent/amonagent-1.3.1.tar.gz
  md5: fbeec419139ac17372105bcaff04009f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - amonagent = amonagent:main
    #
    # Would create an entry point called amonagent that calls amonagent.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests ==2.0.0
    - unidecode

  run:
    - python
    - requests ==2.0.0
    - unidecode

test:
  # Python imports
  imports:
    - amonagent
    - amonagent.modules

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'Linux system and process information collector'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
