package:
  name: django-purls
  version: "0.3"

source:
  fn: django-purls-0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-purls/django-purls-0.3.tar.gz
  md5: 640c3f43b54ce80707eebf6dfc9a28cf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-purls = django-purls:main
    #
    # Would create an entry point called django-purls that calls django-purls.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - purls
    - purls.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/macro/django-purls/
  license: BSD
  summary: 'django-purls'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
