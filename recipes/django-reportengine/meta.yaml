package:
  name: django-reportengine
  version: "0.3.1"

source:
  fn: django-reportengine-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-reportengine/django-reportengine-0.3.1.tar.gz
  md5: c68d69bbca56957d1263165f7cf259a0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-reportengine = django-reportengine:main
    #
    # Would create an entry point called django-reportengine that calls django-reportengine.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - reportengine
    - reportengine.management
    - reportengine.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/cuker/django-reportengine
  license: MIT License
  summary: 'A Django app for building and integrating reports into your Django project.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
