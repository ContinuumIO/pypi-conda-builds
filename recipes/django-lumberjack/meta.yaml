package:
  name: django-lumberjack
  version: "0.2.1"

source:
  fn: django-lumberjack-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-lumberjack/django-lumberjack-0.2.1.tar.gz
  md5: a2db0102c95b8893767ff963e15982a4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-lumberjack = django-lumberjack:main
    #
    # Would create an entry point called django-lumberjack that calls django-lumberjack.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.7

  run:
    - python
    - django >=1.7

test:
  # Python imports
  imports:
    - lumberjack
    - lumberjack.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/lander2k2/django-lumberjack
  license: MIT
  summary: 'Simple logging. Stored in database, accessed in Django admin.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
