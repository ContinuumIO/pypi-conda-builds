package:
  name: fuckitdb
  version: "0.1.3"

source:
  fn: fuckitdb-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/f/fuckitdb/fuckitdb-0.1.3.tar.gz
  md5: cf4e2285e3d668cce72d64d83649223c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fuckitdb = fuckitdb:main
    #
    # Would create an entry point called fuckitdb that calls fuckitdb.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - gspread

  run:
    - python
    - gspread

test:
  # Python imports
  imports:
    - fuckitdb

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Widdershin/fuckitdb
  license: MIT
  summary: 'fuckitdb is a Python ORM for Google Drive Spreadsheets.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
