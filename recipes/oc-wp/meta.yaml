package:
  name: oc-wp
  version: "0.3"

source:
  fn: oc-wp-0.3.tar.gz
  url: https://pypi.python.org/packages/source/o/oc-wp/oc-wp-0.3.tar.gz
  md5: def9b041f72be767bf19c557551017f9
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - oc-wp = oc-wp:main
    #
    # Would create an entry point called oc-wp that calls oc-wp.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - opencore
    - opencore.wordpress
    - opencore.wordpress.browser
    - opencore.wordpress.feed

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://coactivate.org/projects/opencore
  license: GPL
  summary: 'opencore wordpress client package'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
