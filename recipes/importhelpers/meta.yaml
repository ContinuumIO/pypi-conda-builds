package:
  name: importhelpers
  version: "0.2"

source:
  fn: importhelpers-0.2.tar.gz
  url: https://pypi.python.org/packages/source/i/importhelpers/importhelpers-0.2.tar.gz
  md5: 4cd66c67707d7d424a68e88b607c03f1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - importhelpers = importhelpers:main
    #
    # Would create an entry point called importhelpers that calls importhelpers.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - importhelpers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/edwardgeorge/importhelpers/
  license: MIT License
  summary: 'Common import convenience functions.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
