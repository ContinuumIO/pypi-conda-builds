package:
  name: django-dbmessages
  version: "0.1.1"

source:
  fn: django-dbmessages-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-dbmessages/django-dbmessages-0.1.1.tar.gz
  md5: 27d3597e7b015057c92498be01efafa8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-dbmessages = django-dbmessages:main
    #
    # Would create an entry point called django-dbmessages that calls django-dbmessages.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - dbmessages

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD License
  summary: 'Request-independent messaging for Django on top of contrib.messages'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
