package:
  name: django-reusableapps
  version: "0.1.1"

source:
  fn: django-reusableapps-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-reusableapps/django-reusableapps-0.1.1.tar.gz
  md5: d14eedce0dd58f317cab4013793636df
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-reusableapps = django-reusableapps:main
    #
    # Would create an entry point called django-reusableapps that calls django-reusableapps.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - reusableapps

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/django-reusableapps/
  license: BSD License
  summary: 'Reusable Django apps with setuptools'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
