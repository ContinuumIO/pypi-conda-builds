package:
  name: collective.googleloader
  version: "1.0b1"

source:
  fn: collective.googleloader-1.0b1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.googleloader/collective.googleloader-1.0b1.tar.gz
  md5: 48f63eef657d60d4cf7157c772d7cb0d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.googleloader = collective.googleloader:main
    #
    # Would create an entry point called collective.googleloader that calls collective.googleloader.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.registry

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.registry

test:
  # Python imports
  imports:
    - collective
    - collective.googleloader
    - collective.googleloader.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/collective.googleloader
  license: GPL
  summary: 'Integrate google loader (jsapi) into Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
