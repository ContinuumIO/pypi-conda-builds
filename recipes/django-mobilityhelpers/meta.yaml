package:
  name: django-mobilityhelpers
  version: "0.2.0"

source:
  fn: Django-MobilityHelpers-0.2.0.zip
  url: https://pypi.python.org/packages/source/D/Django-MobilityHelpers/Django-MobilityHelpers-0.2.0.zip
  md5: 66c79111a43f5dc0037df7b010dc0bdf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-mobilityhelpers = django-mobilityhelpers:main
    #
    # Would create an entry point called django-mobilityhelpers that calls django-mobilityhelpers.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - mobilityhelpers
    - mobilityhelpers.middleware

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/weholt/django-mobilityhelpers
  license: BSD License
  summary: 'Simple middleware and helper function to help handle request from mobile devices.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
