package:
  name: audiosocket
  version: "0.7"

source:
  fn: audiosocket-0.7.tar.gz
  url: https://pypi.python.org/packages/source/a/audiosocket/audiosocket-0.7.tar.gz
  md5: b39c67cb6ad96df3949932f705de27d5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - audiosocket = audiosocket:main
    #
    # Would create an entry point called audiosocket that calls audiosocket.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - audiosocket

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/techtonik/audiosocket
  license: Public Domain
  summary: 'Raw Audio Socket player in pure Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
