package:
  name: django-piston3
  version: "0.3pre1"

source:
  fn: django-piston3-0.3pre1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-piston3/django-piston3-0.3pre1.tar.gz
  md5: 0841a268fab73974b15d9d0e5327a1b2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-piston3 = django-piston3:main
    #
    # Would create an entry point called django-piston3 that calls django-piston3.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six
    - python-mimeparse
    - django >=1.6

  run:
    - python
    - six
    - python-mimeparse
    - django >=1.6

test:
  # Python imports
  imports:
    - piston3

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/userzimmermann/django-piston3
  license: BSD License
  summary: 'Piston for Python 3.3+ and Django 1.6+ - Compatible with Python 2.7'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
