package:
  name: lineage.proxyprops
  version: "0.1"

source:
  fn: lineage.proxyprops-0.1.tar.gz
  url: https://pypi.python.org/packages/source/l/lineage.proxyprops/lineage.proxyprops-0.1.tar.gz
  md5: 62797e04067b587ce9babcdcb1cf79d9
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lineage.proxyprops = lineage.proxyprops:main
    #
    # Would create an entry point called lineage.proxyprops that calls lineage.proxyprops.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - collective.lineage
    - collective.proxyproperties

  run:
    - python
    - setuptools
    - collective.lineage
    - collective.proxyproperties

test:
  # Python imports
  imports:
    - lineage
    - lineage.proxyprops

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/lineage.proxyprops
  license: GPL
  summary: 'Small wrapper package to add support for collective.proxyprops'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
