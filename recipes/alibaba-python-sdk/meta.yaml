package:
  name: alibaba-python-sdk
  version: "0.1.3"

source:
  fn: alibaba-python-sdk-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/a/alibaba-python-sdk/alibaba-python-sdk-0.1.3.tar.gz
  md5: 1281f10c39f349f3bb2a5c27e476165f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - alibaba-python-sdk = alibaba-python-sdk:main
    #
    # Would create an entry point called alibaba-python-sdk that calls alibaba-python-sdk.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - alibaba
    - alibaba.tmp

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: GPL version 2
  summary: 'Aibaba SDK for python.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
