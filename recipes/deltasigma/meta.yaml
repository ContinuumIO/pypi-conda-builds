package:
  name: deltasigma
  version: "0.1-10"

source:
  fn: deltasigma-0.1-10.tar.gz
  url: https://pypi.python.org/packages/source/d/deltasigma/deltasigma-0.1-10.tar.gz
  md5: 4bba6b207e2f1784dcb64f14cb2e96af
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - deltasigma = deltasigma:main
    #
    # Would create an entry point called deltasigma that calls deltasigma.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy
    - scipy >=0.11.0
    - matplotlib >=1.1.1

  run:
    - python
    - numpy
    - scipy >=0.11.0
    - matplotlib >=1.1.1

test:
  # Python imports
  imports:
    - deltasigma
    - deltasigma.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/ggventurini/python-deltasigma
  license: BSD License
  summary: 'a Python package to synthesize, simulate, scale and map to implementable topologies delta sigma modulators.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
