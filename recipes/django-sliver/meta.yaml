package:
  name: django-sliver
  version: "0.1.0"

source:
  fn: django-sliver-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-sliver/django-sliver-0.1.0.tar.gz
  md5: 7a099b0c90e797498b63fec9b57f55d0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-sliver = django-sliver:main
    #
    # Would create an entry point called django-sliver that calls django-sliver.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3

  run:
    - python
    - django >=1.3

test:
  # Python imports
  imports:
    - sliver

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jarcoal/django-sliver
  license: BSD
  summary: 'Lightweight REST API built on top of Django generic views'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
