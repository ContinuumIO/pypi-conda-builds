package:
  name: flask-compressor
  version: "0.2.0"

source:
  fn: Flask-Compressor-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Compressor/Flask-Compressor-0.2.0.tar.gz
  md5: 1792b2e3921ec2119c539bb95e06e25e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-compressor = flask-compressor:main
    #
    # Would create an entry point called flask-compressor that calls flask-compressor.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask

  run:
    - python
    - flask

test:
  # Python imports
  imports:
    - flask_compressor

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lmeunier/flask-compressor
  license: BSD License
  summary: 'Compress your CSS and JS files.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
