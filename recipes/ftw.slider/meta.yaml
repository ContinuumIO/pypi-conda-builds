package:
  name: ftw.slider
  version: "2.1.4"

source:
  fn: ftw.slider-2.1.4.zip
  url: https://pypi.python.org/packages/source/f/ftw.slider/ftw.slider-2.1.4.zip
  md5: a17f21a56a8a22336fca0a04a469a068
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ftw.slider = ftw.slider:main
    #
    # Would create an entry point called ftw.slider that calls ftw.slider.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - ftw.upgrade
    - setuptools
    - products.genericsetup
    - products.cmfcore
    - products.cmfplone
    - plone.app.jquery >=1.7.2
    - plone.behavior
    - plone.formwidget.contenttree
    - plone.namedfile
    - plone.dexterity
    - plone.app.dexterity
    - plone.directives.form
    - archetypes.schemaextender

  run:
    - python
    - setuptools
    - ftw.upgrade
    - setuptools
    - products.genericsetup
    - products.cmfcore
    - products.cmfplone
    - plone.app.jquery >=1.7.2
    - plone.behavior
    - plone.formwidget.contenttree
    - plone.namedfile
    - plone.dexterity
    - plone.app.dexterity
    - plone.directives.form
    - archetypes.schemaextender

test:
  # Python imports
  imports:
    - ftw
    - ftw.slider
    - ftw.slider.browser
    - ftw.slider.contents
    - ftw.slider.sass
    - ftw.slider.tests
    - ftw.slider.upgrades
    - ftw.slider.viewlets

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/4teamwork/ftw.slider
  license: GPL2
  summary: 'Slider content type for Plone.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
