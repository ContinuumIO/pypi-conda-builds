package:
  name: python-bandsintown
  version: "0.1.1"

source:
  fn: python-bandsintown-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/p/python-bandsintown/python-bandsintown-0.1.1.tar.gz
  md5: 4b46618b5511accb158a13e611430538
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-bandsintown = python-bandsintown:main
    #
    # Would create an entry point called python-bandsintown that calls python-bandsintown.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - bandsintown
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jolbyandfriends/python-bandsintown
  license: MIT
  summary: 'Simple Python client for the Bandsintown API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
