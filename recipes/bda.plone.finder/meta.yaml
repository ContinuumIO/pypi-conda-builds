package:
  name: bda.plone.finder
  version: "1.3.1"

source:
  fn: bda.plone.finder-1.3.1.tar.gz
  url: https://pypi.python.org/packages/source/b/bda.plone.finder/bda.plone.finder-1.3.1.tar.gz
  md5: b51c4f98438ffbc1682a90d0f1732785
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bda.plone.finder = bda.plone.finder:main
    #
    # Would create an entry point called bda.plone.finder that calls bda.plone.finder.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - simplejson
    - plone
    - plone.app.jquerytools

  run:
    - python
    - setuptools
    - setuptools
    - simplejson
    - plone
    - plone.app.jquerytools

test:
  # Python imports
  imports:
    - bda
    - bda.plone
    - bda.plone.finder
    - bda.plone.finder.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/bda.plone.finder
  license: GNU General Public License (GPL)
  summary: 'Mac Finder like view for Plone.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
