package:
  name: flask-dashed
  version: "0.1b2"

source:
  fn: Flask-Dashed-0.1b2.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Dashed/Flask-Dashed-0.1b2.tar.gz
  md5: 45ef6391f9aaa7bf2e9efa41b155c820
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-dashed = flask-dashed:main
    #
    # Would create an entry point called flask-dashed that calls flask-dashed.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - wtforms ==1.0.2
    - flask-wtf >=0.6

  run:
    - python
    - flask
    - wtforms ==1.0.2
    - flask-wtf >=0.6

test:
  # Python imports
  imports:
    - flask_dashed
    - flask_dashed.ext
    - tests
    - tests.flask_dashed
    - tests.flask_dashed.ext

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jeanphix/Flask-Dashed
  license: MIT License
  summary: 'Adds a way to easily build admin apps'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
