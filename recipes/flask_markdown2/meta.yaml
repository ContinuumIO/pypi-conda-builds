package:
  name: flask_markdown2
  version: "0.0.3"

source:
  fn: flask_markdown2-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/f/flask_markdown2/flask_markdown2-0.0.3.tar.gz
  md5: ceca6b17fc2df78797ed360e31b45dff
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask_markdown2 = flask_markdown2:main
    #
    # Would create an entry point called flask_markdown2 that calls flask_markdown2.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.10.1
    - jinja2_markdown >=0.0.1

  run:
    - python
    - flask >=0.10.1
    - jinja2_markdown >=0.0.1

test:
  # Python imports
  imports:
    - flask_markdown2

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT License
  summary: 'A flask extension that adds a {% markdown %} tag to templates.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
