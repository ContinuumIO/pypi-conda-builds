package:
  name: django-viewtools
  version: "0.1"

source:
  fn: django-viewtools-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-viewtools/django-viewtools-0.1.tar.gz
  md5: 7870ef77e5a83c861d227dc24506fa91
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-viewtools = django-viewtools:main
    #
    # Would create an entry point called django-viewtools that calls django-viewtools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - viewtools
    - viewtools.management
    - viewtools.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://launchpad.net/django-viewtools
  license: UNKNOWN
  summary: 'Tools for calling URLs for debugging'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
