package:
  name: auspost-apis
  version: "0.1"

source:
  fn: auspost-apis-0.1.tar.gz
  url: https://pypi.python.org/packages/source/a/auspost-apis/auspost-apis-0.1.tar.gz
  md5: 2e2cf5a91d2845772e6ba0a4548ecf55
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - auspost-apis = auspost-apis:main
    #
    # Would create an entry point called auspost-apis that calls auspost-apis.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - versiontools >=1.9.1
    - requests >=1.0.2
    - python-dateutil >=2.1
    - pytz >=2012d

  run:
    - python
    - versiontools >=1.9.1
    - requests >=1.0.2
    - python-dateutil >=2.1
    - pytz >=2012d

test:
  # Python imports
  imports:
    - auspost

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/elbaschid/auspost-apis
  license: BSD License
  summary: "A Python wrapper around Australia Post's APIs"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
