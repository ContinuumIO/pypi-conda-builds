package:
  name: django-json-rpc
  version: "0.6.3"

source:
  fn: django-json-rpc-0.6.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-json-rpc/django-json-rpc-0.6.3.tar.gz
  md5: 88bac16081a97c98cf7674db48bea9af
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-json-rpc = django-json-rpc:main
    #
    # Would create an entry point called django-json-rpc that calls django-json-rpc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.0

  run:
    - python
    - django >=1.0

test:
  # Python imports
  imports:
    - jsonrpc

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/samuraisam/django-json-rpc/tree/master
  license: MIT License
  summary: 'A simple JSON-RPC implementation for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
