package:
  name: iugu
  version: "0.1.2"

source:
  fn: iugu-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/i/iugu/iugu-0.1.2.tar.gz
  md5: 1fda39205e6eb15012ebecf0c61eed01
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - iugu = iugu:main
    #
    # Would create an entry point called iugu that calls iugu.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - iugu

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/iugu/iugu-python
  license: MIT
  summary: 'The Iugu provides a Python REST APIs to create, process and manage payments.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
