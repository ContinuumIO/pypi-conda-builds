package:
  name: django-favs-sc
  version: "0.2.5"

source:
  fn: django-favs-sc-0.2.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-favs-sc/django-favs-sc-0.2.5.tar.gz
  md5: 5055c12d6fb08b2020e05619fad6746c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-favs-sc = django-favs-sc:main
    #
    # Would create an entry point called django-favs-sc that calls django-favs-sc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - simplejson

  run:
    - python
    - simplejson

test:
  # Python imports
  imports:
    - favit
    - favit.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/SpencerCooley/django-favit
  license: MIT License
  summary: 'A simple reusable app for django that makes it easy to deal with faving'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
