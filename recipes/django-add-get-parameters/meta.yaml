package:
  name: django-add-get-parameters
  version: "0.4"

source:
  fn: django-add-get-parameters-0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-add-get-parameters/django-add-get-parameters-0.4.tar.gz
  md5: 40bd38943ee79e71b101bd81b5d0cc7e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-add-get-parameters = django-add-get-parameters:main
    #
    # Would create an entry point called django-add-get-parameters that calls django-add-get-parameters.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - add_get_param
    - add_get_param.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/RobinRamael/django-add-get-parameters
  license: MIT License
  summary: 'Template tag to add get parameter to the current url.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
