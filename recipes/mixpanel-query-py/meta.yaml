package:
  name: mixpanel-query-py
  version: "0.1.3"

source:
  fn: mixpanel-query-py-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/m/mixpanel-query-py/mixpanel-query-py-0.1.3.tar.gz
  md5: 1364533929d62c057479051ea3eaf5b0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mixpanel-query-py = mixpanel-query-py:main
    #
    # Would create an entry point called mixpanel-query-py that calls mixpanel-query-py.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - mixpanel_query

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://www.github.com/cooncesean/mixpanel-query-py
  license: MIT License
  summary: 'The Python interface to query data from Mixpanel.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
