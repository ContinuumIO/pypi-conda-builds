package:
  name: eztvit
  version: "2.1.1"

source:
  fn: eztvit-2.1.1.tar.gz
  url: https://pypi.python.org/packages/source/e/eztvit/eztvit-2.1.1.tar.gz
  md5: 8faa2fc7caae811a2b0636d69d8b47ce
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - eztvit = eztvit:main
    #
    # Would create an entry point called eztvit that calls eztvit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - beautifulsoup4

  run:
    - python
    - beautifulsoup4

test:
  # Python imports
  imports:
    - eztvit

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jaysh/eztvit-python
  license: MIT License
  summary: 'Screenscraper for https://eztv.it/'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
