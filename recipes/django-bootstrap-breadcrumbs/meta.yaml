package:
  name: django-bootstrap-breadcrumbs
  version: "0.7.1"

source:
  fn: django_bootstrap_breadcrumbs-0.7.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-bootstrap-breadcrumbs/django_bootstrap_breadcrumbs-0.7.1.tar.gz
  md5: 56a11c17baba2b1e00c282bbe4c381af
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-bootstrap-breadcrumbs = django-bootstrap-breadcrumbs:main
    #
    # Would create an entry point called django-bootstrap-breadcrumbs that calls django-bootstrap-breadcrumbs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_bootstrap_breadcrumbs
    - django_bootstrap_breadcrumbs.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://prymitive.github.com/bootstrap-breadcrumbs
  license: MIT License
  summary: u'Django breadcrumbs using Bootstrap V2 or V3'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
