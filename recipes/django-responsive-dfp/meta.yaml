package:
  name: django-responsive-dfp
  version: "0.1.2.4"

source:
  fn: django-responsive-dfp-0.1.2.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-responsive-dfp/django-responsive-dfp-0.1.2.4.tar.gz
  md5: 7c3e2463750563918c795d91298d3319
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-responsive-dfp = django-responsive-dfp:main
    #
    # Would create an entry point called django-responsive-dfp that calls django-responsive-dfp.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3

  run:
    - python
    - django >=1.3

test:
  # Python imports
  imports:
    - dfp
    - dfp.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/dcharbonnier/django-responsive-dfp
  license: BSD License
  summary: 'Responsive ads using mediaquery for dfp.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
