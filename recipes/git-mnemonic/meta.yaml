package:
  name: git-mnemonic
  version: "0.1.0"

source:
  fn: git-mnemonic-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/g/git-mnemonic/git-mnemonic-0.1.0.tar.gz
  md5: f2f7233ed763bac14ebfb6d7d43c7943
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - git-mnemonic = git-mnemonic:main
    #
    # Would create an entry point called git-mnemonic that calls git-mnemonic.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - git_mnemonic

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/glenjamin/git-mnemonic
  license: MIT
  summary: 'Human friendly git hashes'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
