package:
  name: django-staticbuilder
  version: "0.6.1"

source:
  fn: django-staticbuilder-0.6.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-staticbuilder/django-staticbuilder-0.6.1.tar.gz
  md5: ecf28c455d592faee9b93e4b999756c9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-staticbuilder = django-staticbuilder:main
    #
    # Would create an entry point called django-staticbuilder that calls django-staticbuilder.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4
    - blessings ==1.5
    - django-appconf >=0.5

  run:
    - python
    - django >=1.4
    - blessings ==1.5
    - django-appconf >=0.5

test:
  # Python imports
  imports:
    - staticbuilder
    - staticbuilder.management
    - staticbuilder.management.commands
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/hzdg/django-staticbuilder
  license: BSD License
  summary: 'Optimize your static files.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
