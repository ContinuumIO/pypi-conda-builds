package:
  name: best_log_formatter
  version: "0.1"

source:
  fn: best_log_formatter-0.1.tar.gz
  url: https://pypi.python.org/packages/source/b/best_log_formatter/best_log_formatter-0.1.tar.gz
  md5: 456e20eca1672f0820fd1d6b76e5bfdb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - best_log_formatter = best_log_formatter:main
    #
    # Would create an entry point called best_log_formatter that calls best_log_formatter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - best_log_formatter

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/cardforcoin/best-python-log-formatter
  license: MIT License
  summary: 'A pretty cool log formatter.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
