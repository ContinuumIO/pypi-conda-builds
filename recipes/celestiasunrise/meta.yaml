package:
  name: celestiasunrise
  version: "1.0.1"

source:
  fn: v1.0.1.zip
  url: https://github.com/Arzaroth/CelestiaSunrise/archive/v1.0.1.zip
  #md5: 
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - celestiasunrise = celestiasunrise:main
    #
    # Would create an entry point called celestiasunrise that calls celestiasunrise.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - docopt ==0.6.2
    - rapidxml ==1.0.5
    - six ==1.9.0

  run:
    - python
    - docopt ==0.6.2
    - rapidxml ==1.0.5
    - six ==1.9.0

test:
  # Python imports
  imports:
    - celestiasunrise
    - src
    - src.gui
    - src.shell
    - src.utility
    - src.xml

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://arzaroth.github.io/CelestiaSunrise
  license: BSD
  summary: u'A savegame editor for the mobile game "My Little Pony"'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
