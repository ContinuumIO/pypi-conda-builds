package:
  name: blocktrail-sdk
  version: "1.0.5"

source:
  fn: blocktrail-sdk-1.0.5.tar.gz
  url: https://pypi.python.org/packages/source/b/blocktrail-sdk/blocktrail-sdk-1.0.5.tar.gz
  md5: 51093ff970d758ca25b9d25a1a2c7743
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - blocktrail-sdk = blocktrail-sdk:main
    #
    # Would create an entry point called blocktrail-sdk that calls blocktrail-sdk.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - httpsig >=1.1.0
    - pycrypto >=2.6.1
    - requests >=2.4.3
    - future >=0.14.3
    - six >=1.9.0

  run:
    - python
    - httpsig >=1.1.0
    - pycrypto >=2.6.1
    - requests >=2.4.3
    - future >=0.14.3
    - six >=1.9.0

test:
  # Python imports
  imports:
    - blocktrail

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://www.blocktrail.com/api/docs/lang/python
  license: MIT License
  summary: "BlockTrail's Developer Friendly API binding for Python"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
