package:
  name: logstashhandler
  version: "0.1.4"

source:
  fn: logstashHandler-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/l/logstashHandler/logstashHandler-0.1.4.tar.gz
  md5: c12d24b3adc0055f74af1514073b3c57
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - logstashhandler = logstashhandler:main
    #
    # Would create an entry point called logstashhandler that calls logstashhandler.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - logstashHandler
    - logstashhandler

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/stewrutledge/logstashHandler
  license: Creative Commons Attribution-Noncommercial-Share Alike license
  summary: 'Log handler that ships json formatted messages to logstash via TCP and UDP'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
