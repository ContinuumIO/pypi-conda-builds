package:
  name: drop_privileges
  version: "0.1"

source:
  fn: drop_privileges-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/drop_privileges/drop_privileges-0.1.tar.gz
  md5: 932c48a9ed605b49aec78ea8a002faff
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - drop_privileges = drop_privileges:main
    #
    # Would create an entry point called drop_privileges that calls drop_privileges.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - drop_privileges

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT License
  summary: 'Drop root privileges on a POSIX system.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
