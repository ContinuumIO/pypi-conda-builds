package:
  name: athletelist_jw
  version: "1.0.0"

source:
  fn: athletelist_jw-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/a/athletelist_jw/athletelist_jw-1.0.0.tar.gz
  md5: 21718f0f84fade4a3c4aede1e0385554
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - athletelist_jw = athletelist_jw:main
    #
    # Would create an entry point called athletelist_jw that calls athletelist_jw.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - athletelist_jw

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://individual.utoronto.ca/jeffvinwong
  license: UNKNOWN
  summary: 'Extension of list to track athlete times'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
