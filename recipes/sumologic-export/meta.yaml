package:
  name: sumologic-export
  version: "0.0.2"

source:
  fn: sumologic-export-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/s/sumologic-export/sumologic-export-0.0.2.tar.gz
  md5: 0c3e814a90aad5c26be53a23c29f9e16
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sumologic-export = sumologic-export:main
    #
    # Would create an entry point called sumologic-export that calls sumologic-export.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - docopt >=0.6.2
    - requests >=2.3.0

  run:
    - python
    - docopt >=0.6.2
    - requests >=2.3.0

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rdegges/sumologic-export
  license: UNLICENSE
  summary: 'Easily export your Sumologic log data.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
