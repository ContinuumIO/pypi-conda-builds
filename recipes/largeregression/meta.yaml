package:
  name: largeregression
  version: "1.01"

source:
  fn: LargeRegression-1.01.tar.gz
  url: https://pypi.python.org/packages/source/L/LargeRegression/LargeRegression-1.01.tar.gz
  md5: ae362a35851643198c947e90cf461191
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - largeregression = largeregression:main
    #
    # Would create an entry point called largeregression that calls largeregression.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - largeregression

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.stanford.edu/~jeffwong
  license: UNKNOWN
  summary: 'Gradient descent to minimize the least squares objective function.  Requires numpy'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
