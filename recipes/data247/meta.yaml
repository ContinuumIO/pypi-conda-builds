package:
  name: data247
  version: "0.4"

source:
  fn: data247-0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/data247/data247-0.4.tar.gz
  md5: bce3bccc4186f526cafdc52ad89d0228
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - data247 = data247:main
    #
    # Would create an entry point called data247 that calls data247.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=1.0.2

  run:
    - python
    - requests >=1.0.2

test:
  # Python imports
  imports:
    - data247

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rdegges/python-data247
  license: UNLICENSE
  summary: 'An API client for Data 24-7.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
