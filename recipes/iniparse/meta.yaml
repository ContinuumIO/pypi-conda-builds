package:
  name: iniparse
  version: "0.4"

source:
  fn: iniparse-0.4.tar.gz
  url: https://pypi.python.org/packages/source/i/iniparse/iniparse-0.4.tar.gz
  md5: 5e573e9e9733d97623881ce9bbe5eca6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - iniparse = iniparse:main
    #
    # Would create an entry point called iniparse that calls iniparse.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - iniparse

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/iniparse/
  license: MIT License or Python Software Foundation License
  summary: 'Accessing and Modifying INI files'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
