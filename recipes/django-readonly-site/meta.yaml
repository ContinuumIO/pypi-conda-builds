package:
  name: django-readonly-site
  version: "0.1.2"

source:
  fn: django-readonly-site-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-readonly-site/django-readonly-site-0.1.2.tar.gz
  md5: 18920a0d7340d3fe49e83da73058552d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-readonly-site = django-readonly-site:main
    #
    # Would create an entry point called django-readonly-site that calls django-readonly-site.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - readonly

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/rossp/django-readonly-site
  license: BSD License
  summary: 'Middleware to take your Django website offline by changing settings.SITE_READ_ONLY'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
