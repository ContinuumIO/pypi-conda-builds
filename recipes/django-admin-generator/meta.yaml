package:
  name: django-admin-generator
  version: "1.3.1"

source:
  fn: django-admin-generator-1.3.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-admin-generator/django-admin-generator-1.3.1.tar.gz
  md5: 396023f0635172ec4f7eb5bc5998105f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-admin-generator = django-admin-generator:main
    #
    # Would create an entry point called django-admin-generator that calls django-admin-generator.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-utils2 >=1.7.1
    - six

  run:
    - python
    - django-utils2 >=1.7.1
    - six

test:
  # Python imports
  imports:
    - django_admin_generator
    - django_admin_generator.management
    - django_admin_generator.management.commands
    - test_project
    - test_project.test_app

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/WoLpH/django-admin-generator/
  license: BSD License
  summary: 'Django Admin Generator is a management command to\nautomatically generate a Django `admin.py` file for given apps/models.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
