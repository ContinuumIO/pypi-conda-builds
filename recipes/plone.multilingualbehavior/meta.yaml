package:
  name: plone.multilingualbehavior
  version: "1.2.1"

source:
  fn: plone.multilingualbehavior-1.2.1.zip
  url: https://pypi.python.org/packages/source/p/plone.multilingualbehavior/plone.multilingualbehavior-1.2.1.zip
  md5: 41e1688b4773ec4f804639444fbb4392
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.multilingualbehavior = plone.multilingualbehavior:main
    #
    # Would create an entry point called plone.multilingualbehavior that calls plone.multilingualbehavior.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.directives.form
    - plone.directives.dexterity
    - plone.app.dexterity
    - plone.multilingual
    - plone.app.multilingual

  run:
    - python
    - setuptools
    - setuptools
    - plone.directives.form
    - plone.directives.dexterity
    - plone.app.dexterity
    - plone.multilingual
    - plone.app.multilingual

test:
  # Python imports
  imports:
    - plone
    - plone.multilingualbehavior
    - plone.multilingualbehavior.tests
    - plone.multilingualbehavior.upgrades

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/plone/plone.multilingualbehavior
  license: GPL
  summary: 'Dexterity behavior for enabling multilingual extensions'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
