package:
  name: edeposit.amqp.calibre
  version: "1.1.4"

source:
  fn: edeposit.amqp.calibre-1.1.4.tar.gz
  url: https://pypi.python.org/packages/source/e/edeposit.amqp.calibre/edeposit.amqp.calibre-1.1.4.tar.gz
  md5: 38dbbe4f43a856b3f5fce46cc629b964
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - edeposit.amqp.calibre = edeposit.amqp.calibre:main
    #
    # Would create an entry point called edeposit.amqp.calibre that calls edeposit.amqp.calibre.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sh

  run:
    - python
    - setuptools
    - sh

test:
  # Python imports
  imports:
    - edeposit
    - edeposit.amqp
    - edeposit.amqp.calibre

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/edeposit/edeposit.amqp.calibre
  license: GNU General Public License v2 or later (GPLv2+)
  summary: 'E-Deposit AMQP wrappers for calibre convertor.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
