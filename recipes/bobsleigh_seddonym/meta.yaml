package:
  name: bobsleigh_seddonym
  version: "0.1.4"

source:
  fn: bobsleigh_seddonym-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/b/bobsleigh_seddonym/bobsleigh_seddonym-0.1.4.tar.gz
  md5: 8a81585b718ed833b78c66879ef1a281
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bobsleigh_seddonym = bobsleigh_seddonym:main
    #
    # Would create an entry point called bobsleigh_seddonym that calls bobsleigh_seddonym.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - bobsleigh_seddonym
    - bobsleigh_seddonym.handlers
    - bobsleigh_seddonym.settings

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/seddonym/bobsleigh-seddonym/
  license: UNKNOWN
  summary: 'Helpful Bobsleigh settings for my projects.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
