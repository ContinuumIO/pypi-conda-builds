package:
  name: cmsplugin-filery
  version: "1.0.1"

source:
  fn: cmsplugin-filery-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/cmsplugin-filery/cmsplugin-filery-1.0.1.tar.gz
  md5: 5f0a5479457a90d2e9b1caf12b0dea88
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cmsplugin-filery = cmsplugin-filery:main
    #
    # Would create an entry point called cmsplugin-filery that calls cmsplugin-filery.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django
    - django-cms
    - easy-thumbnails
    - django-filer

  run:
    - python
    - django
    - django-cms
    - easy-thumbnails
    - django-filer

test:
  # Python imports
  imports:
    - cmsplugin_filery
    - cmsplugin_filery.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/cmsplugin-filery
  license: LICENSE
  summary: 'Image gallery based on django-filer'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
