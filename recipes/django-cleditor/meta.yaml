package:
  name: django-cleditor
  version: "0.2.1"

source:
  fn: django-cleditor-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-cleditor/django-cleditor-0.2.1.tar.gz
  md5: ec1f4120d752c2d4bbc16475653a3f00
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-cleditor = django-cleditor:main
    #
    # Would create an entry point called django-cleditor that calls django-cleditor.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - cleditor

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/yuchant/django-cleditor
  license: UNKNOWN
  summary: 'A django form widget for CLEditor (a super clean, MIT licensed WYSIWYG HTML editor) that handles uploading'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
