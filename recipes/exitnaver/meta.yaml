package:
  name: exitnaver
  version: "0.2"

source:
  fn: exitnaver-0.2.tar.gz
  url: https://pypi.python.org/packages/source/e/exitnaver/exitnaver-0.2.tar.gz
  md5: 1f5e0a41823db883e2a37139d67f09d8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - exitnaver = exitnaver:main
    #
    # Would create an entry point called exitnaver that calls exitnaver.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - html2text
    - beautifulsoup

  run:
    - python
    - html2text
    - beautifulsoup

test:
  # Python imports
  imports:
    - exitnaver

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/limeburst/exitnaver
  license: GNU General Public License (GPL)
  summary: 'Exports your Naver blog in Markdown format.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
