package:
  name: django-locationbase
  version: "0.3.1"

source:
  fn: Django-LocationBase-0.3.1.zip
  url: https://pypi.python.org/packages/source/D/Django-LocationBase/Django-LocationBase-0.3.1.zip
  md5: 8cec0527b998ef33f23a86615917f95d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-locationbase = django-locationbase:main
    #
    # Would create an entry point called django-locationbase that calls django-locationbase.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - locationbase

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/weholt/django-locationbase
  license: BSD License
  summary: 'Reusable app for handling locations including longitude/latitude, location picking in admin and template tags for static map images.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
