package:
  name: generate_password
  version: "1.0.0"

source:
  fn: generate_password-1.0.0.zip
  url: https://pypi.python.org/packages/source/g/generate_password/generate_password-1.0.0.zip
  md5: e3f99d0a302968863e2a64eed27de5eb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - generate_password = generate_password:main
    #
    # Would create an entry point called generate_password that calls generate_password.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - generate_password

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: lilingzj.qq.com
  license: UNKNOWN
  summary: 'generate a password'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
