package:
  name: collective.ptg.presentation
  version: "1.1"

source:
  fn: collective.ptg.presentation-1.1.zip
  url: https://pypi.python.org/packages/source/c/collective.ptg.presentation/collective.ptg.presentation-1.1.zip
  md5: 9d5356df3484c0eb4a5664facdfe4a47
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.ptg.presentation = collective.ptg.presentation:main
    #
    # Would create an entry point called collective.ptg.presentation that calls collective.ptg.presentation.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.plonetruegallery

  run:
    - python
    - setuptools
    - setuptools
    - collective.plonetruegallery

test:
  # Python imports
  imports:
    - collective
    - collective.ptg
    - collective.ptg.presentation

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/
  license: GPL
  summary: 'Adds a presentation gallery layout to collective.plonetruegallery'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
