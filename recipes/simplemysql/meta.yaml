package:
  name: simplemysql
  version: "1.0"

source:
  fn: simplemysql-1.0.zip
  url: https://pypi.python.org/packages/source/s/simplemysql/simplemysql-1.0.zip
  md5: 3ab94b33b77b0c1bc092af8f707d5d35
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - simplemysql = simplemysql:main
    #
    # Would create an entry point called simplemysql that calls simplemysql.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - mysql-python

  run:
    - python
    - mysql-python

test:
  # Python imports
  imports:
    - simplemysql

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://nadh.in/code/simplemysql
  license: GNU General Public License v2 (GPLv2)
  summary: 'An ultra simple wrapper for Python MySQLdb with very basic functionality'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
