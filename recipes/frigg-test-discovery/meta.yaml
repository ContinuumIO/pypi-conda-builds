package:
  name: frigg-test-discovery
  version: "0.1.0"

source:
  fn: frigg-test-discovery-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/f/frigg-test-discovery/frigg-test-discovery-0.1.0.tar.gz
  md5: 9213a0dfbe788995c1f1d5bab6fcad1a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - frigg-test-discovery = frigg-test-discovery:main
    #
    # Would create an entry point called frigg-test-discovery that calls frigg-test-discovery.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/frigg/frigg-test-discovery
  license: MIT
  summary: 'Test discovery util for frigg-worker'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
