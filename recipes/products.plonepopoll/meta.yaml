package:
  name: products.plonepopoll
  version: "2.8b1"

source:
  fn: Products.PlonePopoll-2.8b1.tar.gz
  url: https://pypi.python.org/packages/source/P/Products.PlonePopoll/Products.PlonePopoll-2.8b1.tar.gz
  md5: 309c3f26b187652357ea1cef3d3c58bb
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - products.plonepopoll = products.plonepopoll:main
    #
    # Would create an entry point called products.plonepopoll that calls products.plonepopoll.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - Products
    - Products.PlonePopoll
    - Products.PlonePopoll.Extensions
    - Products.PlonePopoll.browser
    - Products.PlonePopoll.content
    - Products.PlonePopoll.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/Products.PlonePopoll
  license: GPL
  summary: 'A Poll tool for Plone based on archetypes.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
