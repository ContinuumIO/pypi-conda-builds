package:
  name: carapace
  version: "0.3.1"

source:
  fn: Carapace-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/C/Carapace/Carapace-0.3.1.tar.gz
  md5: 605282521778100058d4b2bb660bfd64
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - carapace = carapace:main
    #
    # Would create an entry point called carapace that calls carapace.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - twisted
    - pycrypto
    - pyasn1
    - zope.component

  run:
    - python
    - twisted
    - pycrypto
    - pyasn1
    - zope.component

test:
  # Python imports
  imports:
    - carapace
    - carapace.app
    - carapace.app.shell
    - carapace.sdk
    - carapace.util
    - twisted.plugins

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/oubiwann/carapace
  license: MIT
  summary: 'Carapace Shell Server'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
