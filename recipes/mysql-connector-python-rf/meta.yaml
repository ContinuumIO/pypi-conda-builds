package:
  name: mysql-connector-python-rf
  version: "2.0.4"

source:
  fn: mysql-connector-python-rf-2.0.4.tar.gz
  url: https://pypi.python.org/packages/source/m/mysql-connector-python-rf/mysql-connector-python-rf-2.0.4.tar.gz
  md5: e900847ca067ff046e88d584e4a56ff0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mysql-connector-python-rf = mysql-connector-python-rf:main
    #
    # Would create an entry point called mysql-connector-python-rf that calls mysql-connector-python-rf.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - mysql
    - mysql.connector
    - mysql.connector.django
    - mysql.connector.fabric
    - mysql.connector.locales
    - mysql.connector.locales.eng

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://dev.mysql.com/doc/connector-python/en/index.html
  license: GNU General Public License (GPL)
  summary: 'MySQL driver written in Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
