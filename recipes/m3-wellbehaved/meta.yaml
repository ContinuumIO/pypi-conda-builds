package:
  name: m3-wellbehaved
  version: "0.1.0.3"

source:
  fn: m3-wellbehaved-0.1.0.3.tar.gz
  url: https://pypi.python.org/packages/source/m/m3-wellbehaved/m3-wellbehaved-0.1.0.3.tar.gz
  md5: d5ea2d57848b0146f731d4fae90abb74
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - m3-wellbehaved = m3-wellbehaved:main
    #
    # Would create an entry point called m3-wellbehaved that calls m3-wellbehaved.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4
    - behave ==1.2.3

  run:
    - python
    - django >=1.4
    - behave ==1.2.3

test:
  # Python imports
  imports:
    - django_wellbehaved

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://src.bars-open.ru/py/m3/m3_contrib/django-wellbehaved
  license: MIT License
  summary: 'Rollback-enabled BDD wrapper for Django with i18n support'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
