package:
  name: sqlalchemy-equivalence
  version: "0.1.1"

source:
  fn: SQLAlchemy-Equivalence-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/S/SQLAlchemy-Equivalence/SQLAlchemy-Equivalence-0.1.1.tar.gz
  md5: e7cc608b7ca4c79e8c37c3b95557a15a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sqlalchemy-equivalence = sqlalchemy-equivalence:main
    #
    # Would create an entry point called sqlalchemy-equivalence that calls sqlalchemy-equivalence.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sqlalchemy ==0.7.8

  run:
    - python
    - sqlalchemy ==0.7.8

test:
  # Python imports
  imports:
    - sqlalchemy_equivalence

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kvesteri/sqlalchemy-equivalence
  license: BSD License
  summary: 'Provides natural equivalence support for SQLAlchemy declarative models.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
