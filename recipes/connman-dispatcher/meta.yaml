package:
  name: connman-dispatcher
  version: "0.0.8"

source:
  fn: connman-dispatcher-0.0.8.tar.gz
  url: https://pypi.python.org/packages/source/c/connman-dispatcher/connman-dispatcher-0.0.8.tar.gz
  md5: 24e8114628dcdba6a47360a52e0c9828
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - connman-dispatcher = connman-dispatcher:main
    #
    # Would create an entry point called connman-dispatcher that calls connman-dispatcher.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyee >=0.0.9
    - logbook ==0.6.0

  run:
    - python
    - pyee >=0.0.9
    - logbook ==0.6.0

test:
  # Python imports
  imports:
    - connman_dispatcher

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/a-sk/connman-dispatcher
  license: Conntext
  summary: 'Call scripts on network changes'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
