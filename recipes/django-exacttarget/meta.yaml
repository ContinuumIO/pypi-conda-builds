package:
  name: django-exacttarget
  version: "0.0.9-beta"

source:
  fn: django-exacttarget-0.0.9-beta.tar.gz
  url: https://pypi.python.org/packages/source/d/django-exacttarget/django-exacttarget-0.0.9-beta.tar.gz
  md5: c1b1c1edc3ed41b0af889ba67699ea1d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-exacttarget = django-exacttarget:main
    #
    # Would create an entry point called django-exacttarget that calls django-exacttarget.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3
    - suds ==0.4
    - pyjwt ==0.1.5

  run:
    - python
    - django >=1.3
    - suds ==0.4
    - pyjwt ==0.1.5

test:
  # Python imports
  imports:
    - exacttarget

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/PunchTab/django-exacttarget
  license: BSD licence, see LICENCE
  summary: 'ExactTarget SOAP Api made simple'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
