package:
  name: amqplib_thrift
  version: "0.1"

source:
  fn: amqplib_thrift-0.1.tar.gz
  url: https://pypi.python.org/packages/source/a/amqplib_thrift/amqplib_thrift-0.1.tar.gz
  md5: 70c0447b973272f7535d18d6c08ba22b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - amqplib_thrift = amqplib_thrift:main
    #
    # Would create an entry point called amqplib_thrift that calls amqplib_thrift.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - amqplib_thrift

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/garetjax/pyamqplib-thrift
  license: MIT License
  summary: 'Python library to use thrift services (client and servers) over AMQP with pyamqplib'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
