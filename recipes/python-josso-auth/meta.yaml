package:
  name: python-josso-auth
  version: "0.1.1"

source:
  fn: python-josso-auth-0.1.1.zip
  url: https://pypi.python.org/packages/source/p/python-josso-auth/python-josso-auth-0.1.1.zip
  md5: 0770c77592e88f4f066b5b6e114a114f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-josso-auth = python-josso-auth:main
    #
    # Would create an entry point called python-josso-auth that calls python-josso-auth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - suds-jurko
    - six

  run:
    - python
    - suds-jurko
    - six

test:
  # Python imports
  imports:
    - josso

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/consbio/python-josso-auth
  license: BSD
  summary: 'A JOSSO backend for python-social-auth'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
