package:
  name: aadict
  version: "0.2.1"

source:
  fn: aadict-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/a/aadict/aadict-0.2.1.tar.gz
  md5: 35466d06c2572648ae81980457ef86f3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - aadict = aadict:main
    #
    # Would create an entry point called aadict that calls aadict.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - distribute >=0.6.24

  run:
    - python
    - distribute >=0.6.24

test:
  # Python imports
  imports:
    - aadict

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/metagriffin/aadict
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'An auto-attribute dict (and a couple of other useful dict functions)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
