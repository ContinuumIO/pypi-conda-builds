package:
  name: kaiso
  version: "0.40.0"

source:
  fn: kaiso-0.40.0.tar.gz
  url: https://pypi.python.org/packages/source/k/kaiso/kaiso-0.40.0.tar.gz
  md5: 9d5094221b165ee48cb030b4354fcaf1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kaiso = kaiso:main
    #
    # Would create an entry point called kaiso that calls kaiso.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - iso8601 >=0.1.4
    - py2neo ==1.6.4
    - requests >=1.0

  run:
    - python
    - iso8601 >=0.1.4
    - py2neo ==1.6.4
    - requests >=1.0

test:
  # Python imports
  imports:
    - kaiso
    - kaiso.attributes

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/onefinestay/kaiso
  license: Apache License, Version 2.0
  summary: 'A queryable object persistence and relationship framework based on the Neo4j graph database.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
