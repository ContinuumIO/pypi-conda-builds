package:
  name: configconvert
  version: "0.2.2"

source:
  fn: ConfigConvert-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/C/ConfigConvert/ConfigConvert-0.2.2.tar.gz
  md5: 566ffeb675d910f74255128a3c6626fa
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - configconvert = configconvert:main
    #
    # Would create an entry point called configconvert that calls configconvert.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - configconvert

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://jimmyg.org/work/code/configconvert/index.html
  license: GNU Affero General Public License v3
  summary: 'Tools for managing config files. Built on ConversionKit.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
