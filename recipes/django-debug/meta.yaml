package:
  name: django-debug
  version: "0.1.1"

source:
  fn: django-debug-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-debug/django-debug-0.1.1.tar.gz
  md5: 668b91528e3cf0b115ce6e6451da6742
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-debug = django-debug:main
    #
    # Would create an entry point called django-debug that calls django-debug.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - debug
    - debug.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/lorien/django-debug/
  license: BSD License
  summary: 'Django application for monitoring the ORM queries'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
