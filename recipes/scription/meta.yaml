package:
  name: scription
  version: "0.74.28"

source:
  fn: scription-0.74.28.tar.gz
  url: https://pypi.python.org/packages/source/s/scription/scription-0.74.28.tar.gz
  md5: 82f1d2cf8537e84f65d09dbb43a2db45
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - scription = scription:main
    #
    # Would create an entry point called scription that calls scription.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - scription

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/stoneleaf/scription
  license: BSD License
  summary: 'simple script parameter parser'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
