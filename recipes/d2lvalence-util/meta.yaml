package:
  name: d2lvalence-util
  version: "0.1.16"

source:
  fn: D2LValence-Util-0.1.16.tar.gz
  url: https://pypi.python.org/packages/source/D/D2LValence-Util/D2LValence-Util-0.1.16.tar.gz
  md5: 3f078888910ce3ad0e3052b18b400460
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - d2lvalence-util = d2lvalence-util:main
    #
    # Would create an entry point called d2lvalence-util that calls d2lvalence-util.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - d2lvalence >=1.0.0
    - requests >=1.2.0

  run:
    - python
    - d2lvalence >=1.0.0
    - requests >=1.2.0

test:
  # Python imports
  imports:
    - d2lvalence_util

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.desire2learn.com/r/valencehome
  license: Apache Software License
  summary: 'D2LValence utility library for Python.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
