package:
  name: django-restricted-sessions
  version: "0.1.2"

source:
  fn: django-restricted-sessions-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-restricted-sessions/django-restricted-sessions-0.1.2.tar.gz
  md5: 1270e4d9fb77283bc2ef82605d0c4422
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-restricted-sessions = django-restricted-sessions:main
    #
    # Would create an entry point called django-restricted-sessions that calls django-restricted-sessions.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - netaddr >=0.7.10

  run:
    - python
    - netaddr >=0.7.10

test:
  # Python imports
  imports:
    - restrictedsessions

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/erikr/django-restricted-sessions
  license: BSD License
  summary: 'Restrict Django sessions to IP and/or user agent.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
