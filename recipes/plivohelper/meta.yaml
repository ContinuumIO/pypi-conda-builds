package:
  name: plivohelper
  version: "0.3.5"

source:
  fn: plivohelper-0.3.5.tar.gz
  url: https://pypi.python.org/packages/source/p/plivohelper/plivohelper-0.3.5.tar.gz
  md5: e8f8cd37233cf9cab632c95314519b63
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plivohelper = plivohelper:main
    #
    # Would create an entry point called plivohelper that calls plivohelper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - plivohelper

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/plivo/plivohelper-python
  license: MIT License
  summary: 'Plivo API client and RESTXML generator'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
