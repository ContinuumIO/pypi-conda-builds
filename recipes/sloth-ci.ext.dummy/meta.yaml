package:
  name: sloth-ci.ext.dummy
  version: "1.0.6"

source:
  fn: sloth-ci.ext.dummy-1.0.6.zip
  url: https://pypi.python.org/packages/source/s/sloth-ci.ext.dummy/sloth-ci.ext.dummy-1.0.6.zip
  md5: 5262c6db25db0706d17d5508453e576f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sloth-ci.ext.dummy = sloth-ci.ext.dummy:main
    #
    # Would create an entry point called sloth-ci.ext.dummy that calls sloth-ci.ext.dummy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sloth_ci >=1.0.9

  run:
    - python
    - sloth_ci >=1.0.9

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/moigagoo/sloth-ci-extensions
  license: MIT
  summary: 'Dummy app extension for Sloth CI'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
