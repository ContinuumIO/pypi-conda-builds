package:
  name: django-registration2
  version: "0.9.4"

source:
  fn: django-registration2-0.9.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-registration2/django-registration2-0.9.4.tar.gz
  md5: ed3ed4212dda4c08c2fcab47c1fa55a3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-registration2 = django-registration2:main
    #
    # Would create an entry point called django-registration2 that calls django-registration2.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4

  run:
    - python
    - django >=1.4

test:
  # Python imports
  imports:
    - registration
    - registration.backends
    - registration.management
    - registration.management.commands
    - registration.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bruth/django-registration2
  license: BSD License
  summary: 'Registration and moderation utilites'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
