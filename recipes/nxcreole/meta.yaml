package:
  name: nxcreole
  version: "1.0.1"

source:
  fn: nxcreole-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/n/nxcreole/nxcreole-1.0.1.tar.gz
  md5: 98746eb888935aa69416b64d7b828082
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - nxcreole = nxcreole:main
    #
    # Would create an entry point called nxcreole that calls nxcreole.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - nxcreole

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/yarosla/nxcreole
  license: GNU Lesser General Public License v3 or later (LGPLv3+)
  summary: 'Wiki Creole 1.0 markup parser (C extension)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
