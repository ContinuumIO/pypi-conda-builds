package:
  name: django-icons-web_control
  version: "1.0"

source:
  fn: django-icons-web_control-1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-icons-web_control/django-icons-web_control-1.0.tar.gz
  md5: e90fd561cfa9255a53526472db3cd7d3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-icons-web_control = django-icons-web_control:main
    #
    # Would create an entry point called django-icons-web_control that calls django-icons-web_control.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - distribute

  run:
    - python
    - distribute

test:
  # Python imports
  imports:
    - icons_web_control

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/kmike/django-icons/
  license: MIT License
  summary: 'web_control icons pack for django.contrib.staticfiles'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
