package:
  name: mongogeneric
  version: "0.1a"

source:
  fn: mongogeneric-0.1a.tar.gz
  url: https://pypi.python.org/packages/source/m/mongogeneric/mongogeneric-0.1a.tar.gz
  md5: 51dec93b0dde2d5289d6025f7a0a50d3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mongogeneric = mongogeneric:main
    #
    # Would create an entry point called mongogeneric that calls mongogeneric.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - mongogeneric

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.schafproductions.com
  license: UNKNOWN
  summary: 'An implementation of django class based generic views using mongoengine.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
