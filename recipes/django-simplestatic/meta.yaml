package:
  name: django-simplestatic
  version: "0.0.5"

source:
  fn: django-simplestatic-0.0.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-simplestatic/django-simplestatic-0.0.5.tar.gz
  md5: ed799c171945905e53d147eca77ec047
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-simplestatic = django-simplestatic:main
    #
    # Would create an entry point called django-simplestatic that calls django-simplestatic.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - boto >=2.8.0

  run:
    - python
    - boto >=2.8.0

test:
  # Python imports
  imports:
    - simplestatic
    - simplestatic.management
    - simplestatic.management.commands
    - simplestatic.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ericflo/django-simplestatic
  license: MIT License
  summary: 'A highly opinionated drop-in library for static file management in Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
