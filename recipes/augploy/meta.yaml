package:
  name: augploy
  version: "0.3.3"

source:
  fn: augploy-0.3.3.tar.gz
  url: https://pypi.python.org/packages/source/a/augploy/augploy-0.3.3.tar.gz
  md5: 06ca0470d73b1028330dcef4a4306c2f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - augploy = augploy:main
    #
    # Would create an entry point called augploy that calls augploy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - ansible ==1.6.3

  run:
    - python
    - ansible ==1.6.3

test:
  # Python imports
  imports:
    - augploy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'AUGmentum dePLOYment automation tool, powered by ansible'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
