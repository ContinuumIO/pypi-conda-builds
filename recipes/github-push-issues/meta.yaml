package:
  name: github-push-issues
  version: "0.3"

source:
  fn: github-push-issues-0.3.tar.gz
  url: https://pypi.python.org/packages/source/g/github-push-issues/github-push-issues-0.3.tar.gz
  md5: 4fe767c641cbd340f0f35908a3661eb6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - github-push-issues = github-push-issues:main
    #
    # Would create an entry point called github-push-issues that calls github-push-issues.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/wking/github-push-issues
  license: BSD License
  summary: 'Create GitHub issues based on local templates.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
