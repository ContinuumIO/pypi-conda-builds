package:
  name: ivre
  version: "0.9"

source:
  fn: ivre-0.9.tar.gz
  url: https://pypi.python.org/packages/source/i/ivre/ivre-0.9.tar.gz
  md5: 79525e976684ca9d883ae46dc3d0d76f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ivre = ivre:main
    #
    # Would create an entry point called ivre that calls ivre.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - pycrypto
    - pymongo >=2.7.2

  run:
    - python
    - pycrypto
    - pymongo >=2.7.2

test:
  # Python imports
  imports:
    - ivre
    - ivre/db

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pierre.droids-corp.org/blog/html/tags/ivre.html
  license: GPLv3
  summary: 'IVRE: Instrument de veille sur les reseaux exterieurs'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
