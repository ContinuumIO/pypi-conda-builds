package:
  name: collective.blogging
  version: "1.3.3"

source:
  fn: collective.blogging-1.3.3.zip
  url: https://pypi.python.org/packages/source/c/collective.blogging/collective.blogging-1.3.3.zip
  md5: 73aaf10444040302517e22f31a6b116b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.blogging = collective.blogging:main
    #
    # Would create an entry point called collective.blogging that calls collective.blogging.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - z3c.autoinclude
    - archetypes.schemaextender
    - archetypes.markerfield
    - plone.indexer
    - plone.app.jquerytools
    - products.atreferencebrowserwidget

  run:
    - python
    - setuptools
    - setuptools
    - z3c.autoinclude
    - archetypes.schemaextender
    - archetypes.markerfield
    - plone.indexer
    - plone.app.jquerytools
    - products.atreferencebrowserwidget

test:
  # Python imports
  imports:
    - collective
    - collective.blogging
    - collective.blogging.Extensions
    - collective.blogging.actions
    - collective.blogging.browser
    - collective.blogging.portlets

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/collective.blogging
  license: GPL
  summary: 'A blogging extension for Plone 4.x.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
