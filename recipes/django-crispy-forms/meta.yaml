package:
  name: django-crispy-forms
  version: "1.4.0"

source:
  fn: django-crispy-forms-1.4.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-crispy-forms/django-crispy-forms-1.4.0.tar.gz
  md5: b4bc5932405039839e6a99ecf95d30d7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-crispy-forms = django-crispy-forms:main
    #
    # Would create an entry point called django-crispy-forms that calls django-crispy-forms.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - crispy_forms
    - crispy_forms.templatetags
    - crispy_forms.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/maraujop/django-crispy-forms
  license: BSD License
  summary: 'Best way to have Django DRY forms'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
