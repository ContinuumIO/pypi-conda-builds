package:
  name: mapcombine
  version: "0.1.0"

source:
  fn: mapcombine-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/m/mapcombine/mapcombine-0.1.0.tar.gz
  md5: bdfc3edb89a452aef25801361ce198ba
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mapcombine = mapcombine:main
    #
    # Would create an entry point called mapcombine that calls mapcombine.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - mapcombine

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/maxhutch/mapcombine/
  license: MIT
  summary: 'Simple combine-centric MapReduce'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
