package:
  name: hbcht
  version: "0.1.1"

source:
  fn: hbcht-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/h/hbcht/hbcht-0.1.1.tar.gz
  md5: 14447e7d11dcc2f8da8be543b1de9ffe
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hbcht = hbcht:main
    #
    # Would create an entry point called hbcht that calls hbcht.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - hbcht

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://metanohi.name/projects/hbcht/
  license: WTFPL
  summary: 'A combined interpreter and compiler for the Half-Broken Car in Heavy Traffic programming language'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
