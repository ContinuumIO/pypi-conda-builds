package:
  name: django-xslt
  version: "0.4.5"

source:
  fn: django-xslt-0.4.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-xslt/django-xslt-0.4.5.tar.gz
  md5: 045c0de5b8d8474a6b81c157ea45e5ee
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-xslt = django-xslt:main
    #
    # Would create an entry point called django-xslt that calls django-xslt.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - djangoxslt
    - djangoxslt.xslt

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/woome/django-xslt
  license: BSD License
  summary: 'an XSLT template system for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
