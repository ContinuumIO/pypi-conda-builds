package:
  name: lumberjack_formatter
  version: "0.1.4"

source:
  fn: lumberjack_formatter-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/l/lumberjack_formatter/lumberjack_formatter-0.1.4.tar.gz
  md5: 891dc62dbf541a315398252fdb68c6ed
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lumberjack_formatter = lumberjack_formatter:main
    #
    # Would create an entry point called lumberjack_formatter that calls lumberjack_formatter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six >=1.9.0,<1.10

  run:
    - python
    - six >=1.9.0,<1.10

test:
  # Python imports
  imports:
    - lumberjack_formatter

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dyejon/python_lumberjack_formatter
  license: UNKNOWN
  summary: 'Python logging framework JSON formatter for use with the lumberjack transport (logstash-forwarder)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
