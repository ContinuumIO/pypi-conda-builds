package:
  name: glamkit-fallbackserve
  version: "0.1"

source:
  fn: glamkit-fallbackserve-0.1.tar.gz
  url: https://pypi.python.org/packages/source/g/glamkit-fallbackserve/glamkit-fallbackserve-0.1.tar.gz
  md5: 3cde262919e698ca6abdcfdab9db9051
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - glamkit-fallbackserve = glamkit-fallbackserve:main
    #
    # Would create an entry point called glamkit-fallbackserve that calls glamkit-fallbackserve.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - fallbackserve

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/glamkit/glamkit-fallbackserve
  license: BSD License
  summary: 'Makes your life easier when dealing with media during the development of Django apps'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
