package:
  name: clarify_python_2
  version: "1.0.1"

source:
  fn: clarify_python_2-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/clarify_python_2/clarify_python_2-1.0.1.tar.gz
  md5: 48f1f0cd507d044f8c2f2854e5ba22da
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - clarify_python_2 = clarify_python_2:main
    #
    # Would create an entry point called clarify_python_2 that calls clarify_python_2.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - clarify_python_2

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Clarify/clarify_python_2
  license: MIT License
  summary: 'The Clarify Python 2 Helper Library wraps the entire Clarify API in Python 2.x function calls.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
