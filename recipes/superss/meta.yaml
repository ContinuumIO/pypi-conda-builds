package:
  name: superss
  version: "0.2.9"

source:
  fn: superss-0.2.9.tar.gz
  url: https://pypi.python.org/packages/source/s/superss/superss-0.2.9.tar.gz
  md5: 154b515695b79b2cb518ef6084840ca3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - superss = superss:main
    #
    # Would create an entry point called superss that calls superss.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - beautifulsoup4
    - decorator
    - jsonpath-rw
    - lauteur
    - lxml
    - ply
    - pytz
    - requests
    - siegfried
    - six
    - tldextract
    - wsgiref
    - particle
    - pageone
    - gevent

  run:
    - python
    - beautifulsoup4
    - decorator
    - jsonpath-rw
    - lauteur
    - lxml
    - ply
    - pytz
    - requests
    - siegfried
    - six
    - tldextract
    - wsgiref
    - particle
    - pageone
    - gevent

test:
  # Python imports
  imports:
    - superss
    - superss.libs

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/newslynx/superss
  license: MIT License
  summary: 'RSS parsing with batteries included'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
