package:
  name: tornado-foursquare
  version: "0.0.3"

source:
  fn: tornado-foursquare-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/t/tornado-foursquare/tornado-foursquare-0.0.3.tar.gz
  md5: 1f8d2cb4c556f367dae8fa9e0842d48c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tornado-foursquare = tornado-foursquare:main
    #
    # Would create an entry point called tornado-foursquare that calls tornado-foursquare.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - tornado >=2.3

  run:
    - python
    - tornado >=2.3

test:
  # Python imports
  imports:
    - foursquare

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rafaels88/tornado-foursquare
  license: OSI
  summary: 'A tornado based foursquare api wrapper'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
