package:
  name: duphunter
  version: "0.1.4"

source:
  fn: duphunter-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/d/duphunter/duphunter-0.1.4.tar.gz
  md5: 7777fc2e7172b1e3c8d78c7f64d3e647
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - duphunter = duphunter:main
    #
    # Would create an entry point called duphunter that calls duphunter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - duphunter

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/Rudd-O/duphunter
  license: GNU General Public License (GPL)
  summary: 'An application that finds and lets you delete duplicate files'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
