package:
  name: jsonutil
  version: "1.0.3"

source:
  fn: jsonutil-1.0.3.tar.gz
  url: https://pypi.python.org/packages/source/j/jsonutil/jsonutil-1.0.3.tar.gz
  md5: 3cff5625996f7d6e5642a1bf5b9e85f3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jsonutil = jsonutil:main
    #
    # Would create an entry point called jsonutil that calls jsonutil.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - simplejson >=2.1.0

  run:
    - python
    - simplejson >=2.1.0

test:
  # Python imports
  imports:
    - jsonutil
    - jsonutil.test
    - jsonutil.test.json_tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://tahoe-lafs.org/trac/jsonutil
  license: GNU General Public License (GPL)
  summary: 'a wrapper around simplejson which deserializes decimals to Decimal instead of to float'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
