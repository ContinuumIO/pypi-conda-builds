package:
  name: django-s3-storages-utils
  version: "0.1.0"

source:
  fn: django-s3-storages-utils-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-s3-storages-utils/django-s3-storages-utils-0.1.0.tar.gz
  md5: fc39f2ece5d51bd8673a65c55329e41e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-s3-storages-utils = django-s3-storages-utils:main
    #
    # Would create an entry point called django-s3-storages-utils that calls django-s3-storages-utils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-storages
    - boto

  run:
    - python
    - django-storages
    - boto

test:
  # Python imports
  imports:
    - s3storagesutils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ravenac95/django-s3-storages-utils
  license: MIT License
  summary: 'django-s3-storages-utils - Utilities for setting up s3 for use with django-storages'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
