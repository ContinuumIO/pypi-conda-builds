package:
  name: flask-jsonpify
  version: "1.5.0"

source:
  fn: Flask-Jsonpify-1.5.0.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Jsonpify/Flask-Jsonpify-1.5.0.tar.gz
  md5: 8a10e37942c43d93d107644a3fe77d98
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-jsonpify = flask-jsonpify:main
    #
    # Would create an entry point called flask-jsonpify that calls flask-jsonpify.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask

  run:
    - python
    - flask

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/CoryDolphin/flask-jsonpify
  license: MIT License
  summary: 'A Flask extension adding a decorator for JSONP support'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
