package:
  name: anybox.testing.datetime
  version: "0.5"

source:
  fn: anybox.testing.datetime-0.5.tar.gz
  url: https://pypi.python.org/packages/source/a/anybox.testing.datetime/anybox.testing.datetime-0.5.tar.gz
  md5: 4f634645d98b69c486aff69ec81556b4
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - anybox.testing.datetime = anybox.testing.datetime:main
    #
    # Would create an entry point called anybox.testing.datetime that calls anybox.testing.datetime.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - anybox
    - anybox.testing
    - anybox.testing.datetime

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/anybox/anybox.testing.datetime
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'Various utilities related to date and time for testing purposes.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
