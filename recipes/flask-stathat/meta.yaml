package:
  name: flask-stathat
  version: "0.1.2"

source:
  fn: Flask-StatHat-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-StatHat/Flask-StatHat-0.1.2.tar.gz
  md5: dc72b3338ea6f39588ffdf92f183b141
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-stathat = flask-stathat:main
    #
    # Would create an entry point called flask-stathat that calls flask-stathat.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=2.0,<3

  run:
    - python
    - requests >=2.0,<3

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/marksteve/flask-stathat
  license: MIT License
  summary: 'StatHat extension for Flask'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
