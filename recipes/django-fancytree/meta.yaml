package:
  name: django-fancytree
  version: "0.3.0"

source:
  fn: django-fancytree-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-fancytree/django-fancytree-0.3.0.tar.gz
  md5: 45c95d8e444112f92986577e3eead9a5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-fancytree = django-fancytree:main
    #
    # Would create an entry point called django-fancytree that calls django-fancytree.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - django-mptt

  run:
    - python
    - django
    - django-mptt

test:
  # Python imports
  imports:
    - fancytree
    - treewidget
    - treewidget.apps
    - treewidget.apps.categories
    - treewidget.apps.categories.migrations
    - treewidget.fancytree

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/xrmx/django-fancytree
  license: BSD License
  summary: 'Django forms widget that uses Fancytree to display tree data'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
