package:
  name: gzbus
  version: "0.1.1"

source:
  fn: gzbus-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/g/gzbus/gzbus-0.1.1.tar.gz
  md5: 87f70bee9b3a17a9532b8b6683538d2d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gzbus = gzbus:main
    #
    # Would create an entry point called gzbus that calls gzbus.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - lxml
    - pyquery
    - requests

  run:
    - python
    - lxml
    - pyquery
    - requests

test:
  # Python imports
  imports:
    - gzbus
    - gzbus.data

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bcho/gzbus
  license: MIT License
  summary: 'Guangzhou bus routine realtime tracking.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
