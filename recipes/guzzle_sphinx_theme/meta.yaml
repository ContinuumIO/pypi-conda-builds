package:
  name: guzzle_sphinx_theme
  version: "0.7.7"

source:
  fn: guzzle_sphinx_theme-0.7.7.tar.gz
  url: https://pypi.python.org/packages/source/g/guzzle_sphinx_theme/guzzle_sphinx_theme-0.7.7.tar.gz
  md5: 7fa403b875c7751a571a12ed22fedbf9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - guzzle_sphinx_theme = guzzle_sphinx_theme:main
    #
    # Would create an entry point called guzzle_sphinx_theme that calls guzzle_sphinx_theme.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sphinx >=1.2b1

  run:
    - python
    - sphinx >=1.2b1

test:
  # Python imports
  imports:
    - guzzle_sphinx_theme

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/guzzle/guzzle_sphinx_theme
  license: MIT License
  summary: 'Sphinx theme used by Guzzle.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
