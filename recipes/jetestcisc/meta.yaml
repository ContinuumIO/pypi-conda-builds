package:
  name: jetestcisc
  version: "0.2"

source:
  fn: jetestcisc-0.2.tar.gz
  url: https://pypi.python.org/packages/source/j/jetestcisc/jetestcisc-0.2.tar.gz
  md5: 2e88c76ca4034c554f479a5e7e32ddba
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jetestcisc = jetestcisc:main
    #
    # Would create an entry point called jetestcisc that calls jetestcisc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - xmltodict ==0.9.2

  run:
    - python
    - xmltodict ==0.9.2

test:
  # Python imports
  imports:
    - jetestcisc

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jedelman8/jetestcisc
  license: UNKNOWN
  summary: 'A random test lib'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
