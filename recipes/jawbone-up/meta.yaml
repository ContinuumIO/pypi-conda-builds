package:
  name: jawbone-up
  version: "0.1"

source:
  fn: jawbone-up-0.1.tar.gz
  url: https://pypi.python.org/packages/source/j/jawbone-up/jawbone-up-0.1.tar.gz
  md5: e4d10a51704c0410466a2aa9789e4990
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jawbone-up = jawbone-up:main
    #
    # Would create an entry point called jawbone-up that calls jawbone-up.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - jawbone

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/karthikbgl/python-jawbone-up
  license: UNKNOWN
  summary: 'Jawbone UP API python wrapper'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
