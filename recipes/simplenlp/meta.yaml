package:
  name: simplenlp
  version: "1.1.2"

source:
  fn: simplenlp-1.1.2.tar.gz
  url: https://pypi.python.org/packages/source/s/simplenlp/simplenlp-1.1.2.tar.gz
  md5: 9aee75670422352d8efd93b83b8b615a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - simplenlp = simplenlp:main
    #
    # Would create an entry point called simplenlp that calls simplenlp.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - csc-utils >=0.6

  run:
    - python
    - csc-utils >=0.6

test:
  # Python imports
  imports:
    - simplenlp
    - simplenlp.ar
    - simplenlp.de
    - simplenlp.default
    - simplenlp.en
    - simplenlp.es
    - simplenlp.fi
    - simplenlp.fr
    - simplenlp.hu
    - simplenlp.it
    - simplenlp.ja
    - simplenlp.ja_cabocha
    - simplenlp.ko
    - simplenlp.mblem
    - simplenlp.nl
    - simplenlp.pt
    - simplenlp.ru
    - simplenlp.zh

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/commonsense/simplenlp/
  license: GNU General Public License (GPL)
  summary: 'Simple, multilingual natural language tools.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
