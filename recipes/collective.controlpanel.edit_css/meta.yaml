package:
  name: collective.controlpanel.edit_css
  version: "0.4.0"

source:
  fn: collective.controlpanel.edit_css-0.4.0.zip
  url: https://pypi.python.org/packages/source/c/collective.controlpanel.edit_css/collective.controlpanel.edit_css-0.4.0.zip
  md5: 0c584e0976d2d2b366b5ea6b442caf54
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.controlpanel.edit_css = collective.controlpanel.edit_css:main
    #
    # Would create an entry point called collective.controlpanel.edit_css that calls collective.controlpanel.edit_css.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.controlpanel
    - collective.controlpanel.edit_css
    - collective.controlpanel.edit_css.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://collective.github.com/collective.controlpanel.edit_css
  license: ZPL
  summary: 'Use a text area in the Plone control panel to        edit CSS and Javascript.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
