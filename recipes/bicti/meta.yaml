package:
  name: bicti
  version: "0.1.1"

source:
  fn: bicti-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/b/bicti/bicti-0.1.1.tar.gz
  md5: 4fe6128a282eea619d540308bb8f0682
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bicti = bicti:main
    #
    # Would create an entry point called bicti that calls bicti.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - bicti

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rbarrois/bicti/
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'Bicti, efficient startup script for within docker containers'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
