package:
  name: django-twitter-feed
  version: "0.2"

source:
  fn: django-twitter-feed-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-twitter-feed/django-twitter-feed-0.2.tar.gz
  md5: 5b663dd3edc06c72b15d7899f63f11d0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-twitter-feed = django-twitter-feed:main
    #
    # Would create an entry point called django-twitter-feed that calls django-twitter-feed.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - twitter_feed

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/FrancoisConstant/django-twitter-feed
  license: MIT License
  summary: 'A simple Django app to show a Twitter feed.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
