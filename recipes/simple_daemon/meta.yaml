package:
  name: simple_daemon
  version: "0.0.4"

source:
  fn: simple_daemon-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/s/simple_daemon/simple_daemon-0.0.4.tar.gz
  md5: c318d8c043e76e20690207e343d593bf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - simple_daemon = simple_daemon:main
    #
    # Would create an entry point called simple_daemon that calls simple_daemon.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - single_access ==0.0.2

  run:
    - python
    - single_access ==0.0.2

test:
  # Python imports
  imports:
    - simple_daemon

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/imbolc/simple_daemon
  license: ISC License (ISCL)
  summary: 'Simple nix daemon'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
