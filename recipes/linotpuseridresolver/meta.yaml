package:
  name: linotpuseridresolver
  version: "2.7.1.2"

source:
  fn: LinOtpUserIdResolver-2.7.1.2.tar.gz
  url: https://pypi.python.org/packages/source/L/LinOtpUserIdResolver/LinOtpUserIdResolver-2.7.1.2.tar.gz
  md5: 1131984c6fa13ad513fd8ef38588a858
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - linotpuseridresolver = linotpuseridresolver:main
    #
    # Would create an entry point called linotpuseridresolver that calls linotpuseridresolver.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python-ldap

  run:
    - python
    - python-ldap

test:
  # Python imports
  imports:
    - linotpuseridresolver
    - useridresolver

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.linotp.org
  license: GNU Affero General Public License v3
  summary: 'LinOTP2 modules for resolving usernames'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
