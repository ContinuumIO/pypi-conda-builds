package:
  name: hghooks
  version: "0.6.0"

source:
  fn: hghooks-0.6.0.tar.gz
  url: https://pypi.python.org/packages/source/h/hghooks/hghooks-0.6.0.tar.gz
  md5: 0e903dc90e06b290f1553f84d56c9f58
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hghooks = hghooks:main
    #
    # Would create an entry point called hghooks that calls hghooks.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - hghooks

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/lgs/hghooks/
  license: LGPL 3
  summary: 'A set of useful hooks for Mercurial'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
