package:
  name: mega.py
  version: "0.9.18"

source:
  fn: mega.py-0.9.18.tar.gz
  url: https://pypi.python.org/packages/source/m/mega.py/mega.py-0.9.18.tar.gz
  md5: e7e03cde6b855b002f901672e8cd3059
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mega.py = mega.py:main
    #
    # Would create an entry point called mega.py that calls mega.py.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - pycrypto
    - requests

  run:
    - python
    - pycrypto
    - requests

test:
  # Python imports
  imports:
    - mega

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Creative Commons Attribution-Noncommercial-Share Alike license
  summary: 'Python lib for the Mega.co.nz API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
