package:
  name: heisenberg-ec2
  version: "1.0.3"

source:
  fn: heisenberg-ec2-1.0.3.tar.gz
  url: https://pypi.python.org/packages/source/h/heisenberg-ec2/heisenberg-ec2-1.0.3.tar.gz
  md5: f92faa50b81ede34d9aeff49ad33367f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - heisenberg-ec2 = heisenberg-ec2:main
    #
    # Would create an entry point called heisenberg-ec2 that calls heisenberg-ec2.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - boto
    - texttable

  run:
    - python
    - boto
    - texttable

test:
  # Python imports
  imports:
    - heisenberg
    - heisenberg.command

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/adampetrovic/heisenberg
  license: BSD License
  summary: 'A utility for searching and connecting to EC2 instances with caching for quicker interaction'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
