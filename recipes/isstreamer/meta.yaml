package:
  name: isstreamer
  version: "0.0.25"

source:
  fn: ISStreamer-0.0.25.zip
  url: https://pypi.python.org/packages/source/I/ISStreamer/ISStreamer-0.0.25.zip
  md5: 46b90faf88d83981afbabd6c97a28e7f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - isstreamer = isstreamer:main
    #
    # Would create an entry point called isstreamer that calls isstreamer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - ISStreamer
    - isstreamer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://www.initialstate.com
  license: MIT License
  summary: "A python module to simplify the process of getting log data to Initial State's platform"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
