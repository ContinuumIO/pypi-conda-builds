package:
  name: mixpanel-py-async
  version: "0.0.5"

source:
  fn: mixpanel-py-async-0.0.5.tar.gz
  url: https://pypi.python.org/packages/source/m/mixpanel-py-async/mixpanel-py-async-0.0.5.tar.gz
  md5: ecc43bbc303a66838a8b3fedcdfd636c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mixpanel-py-async = mixpanel-py-async:main
    #
    # Would create an entry point called mixpanel-py-async that calls mixpanel-py-async.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - mixpanel-py

  run:
    - python
    - mixpanel-py

test:
  # Python imports
  imports:
    - mixpanel_async

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jessepollak/mixpanel-python-async
  license: Apache Software License
  summary: 'Python library for using Mixpanel asynchronously'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
