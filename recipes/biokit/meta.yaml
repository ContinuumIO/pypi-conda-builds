package:
  name: biokit
  version: "0.0.5"

source:
  fn: biokit-0.0.5.tar.gz
  url: https://pypi.python.org/packages/source/b/biokit/biokit-0.0.5.tar.gz
  md5: e13f51894e00930e8d9a48e5cf776ba7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - biokit = biokit:main
    #
    # Would create an entry point called biokit that calls biokit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pandas
    - bioservices
    - colormap

  run:
    - python
    - pandas
    - bioservices
    - colormap

test:
  # Python imports
  imports:
    - biokit
    - biokit.data
    - biokit.data.viz
    - biokit.dev
    - biokit.io
    - biokit.misc
    - biokit.network
    - biokit.rtools
    - biokit.sequence
    - biokit.services
    - biokit.stats
    - biokit.viz
    - test
    - test.io
    - test.network
    - test.rtools
    - test.services
    - test.stats
    - test.viz

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: ['http://pypi.python.org/pypi/biokit']
  license: BSD License
  summary: 'Access to Biological Web Services from Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
