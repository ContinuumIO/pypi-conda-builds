package:
  name: hypothesis-pytest
  version: "0.9.0"

source:
  fn: hypothesis-pytest-0.9.0.tar.gz
  url: https://pypi.python.org/packages/source/h/hypothesis-pytest/hypothesis-pytest-0.9.0.tar.gz
  md5: 805d2a07111ce21c5cb859e7c8d2da48
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hypothesis-pytest = hypothesis-pytest:main
    #
    # Would create an entry point called hypothesis-pytest that calls hypothesis-pytest.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - hypothesis >=1.4,<1.4.99
    - pytest >=2.6.0,<2.7.99

  run:
    - python
    - setuptools
    - hypothesis >=1.4,<1.4.99
    - pytest >=2.6.0,<2.7.99

test:
  # Python imports
  imports:
    - hypothesispytest

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/DRMacIver/hypothesis
  license: Mozilla Public License 2.0 (MPL 2.0)
  summary: 'Pytest plugin for better integration with hypothesis'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
