package:
  name: django-grappelli-extensions
  version: "0.1.3"

source:
  fn: django-grappelli-extensions-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-grappelli-extensions/django-grappelli-extensions-0.1.3.tar.gz
  md5: 1d7062f677891d7e1915cd5fd6afceab
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-grappelli-extensions = django-grappelli-extensions:main
    #
    # Would create an entry point called django-grappelli-extensions that calls django-grappelli-extensions.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-grappelli >=2.4.6
    - django-classy-tags >=0.4
    - django-apptemplates

  run:
    - python
    - django-grappelli >=2.4.6
    - django-classy-tags >=0.4
    - django-apptemplates

test:
  # Python imports
  imports:
    - grappelli_extensions
    - grappelli_extensions.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/django-grappelli-extensions/django-grappelli-extensions
  license: Apache Software License
  summary: 'Extensions for Grappelli Admin interface'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
