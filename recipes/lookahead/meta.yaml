package:
  name: lookahead
  version: "1.0"

source:
  fn: lookahead-1.0.tar.gz
  url: https://pypi.python.org/packages/source/l/lookahead/lookahead-1.0.tar.gz
  md5: 933691003989e2b5cabb9bbe762954f7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lookahead = lookahead:main
    #
    # Would create an entry point called lookahead that calls lookahead.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - lookahead

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/maaku/lookahead/
  license: MIT License
  summary: u'Provides a utility for constructing generators out of an iterable that yield look-ahead (and/or \u201clook-behind\u201d) tuples.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
