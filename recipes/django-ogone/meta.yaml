package:
  name: django-ogone
  version: "0.1"

source:
  fn: django-ogone-0.1.zip
  url: https://pypi.python.org/packages/source/d/django-ogone/django-ogone-0.1.zip
  md5: 52622ed5269df79759c3c133c4865809
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-ogone = django-ogone:main
    #
    # Would create an entry point called django-ogone that calls django-ogone.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_ogone
    - examples

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/tschellenbach/Django-ogone
  license: GNU General Public License (GPL)
  summary: 'Python/Django client to the ogone payment system.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
