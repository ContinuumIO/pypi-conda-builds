package:
  name: namaste
  version: "0.4"

source:
  fn: namaste-0.4.tar.gz
  url: https://pypi.python.org/packages/source/n/namaste/namaste-0.4.tar.gz
  md5: 3f35335461cbdab4c03761e474bd3251
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - namaste = namaste:main
    #
    # Would create an entry point called namaste that calls namaste.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - namaste

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/mjgiarlo/namaste
  license: UNKNOWN
  summary: 'a tool for working with Namaste directory description tags'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
