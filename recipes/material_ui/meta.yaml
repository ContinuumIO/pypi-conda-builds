package:
  name: material_ui
  version: "0.1.3.6"

source:
  fn: material_ui-0.1.3.6.tar.gz
  url: https://pypi.python.org/packages/source/m/material_ui/material_ui-0.1.3.6.tar.gz
  md5: 243261cbff491a18afd14af29ab39147
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - material_ui = material_ui:main
    #
    # Would create an entry point called material_ui that calls material_ui.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - material_ui
    - material_ui.flatui
    - material_ui.navigation

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Cuuuurzel/kivy-material-ui
  license: UNKNOWN
  summary: 'Material UI for Kivy.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
