package:
  name: edn_format
  version: "0.5.4"

source:
  fn: edn_format-0.5.4.tar.gz
  url: https://pypi.python.org/packages/source/e/edn_format/edn_format-0.5.4.tar.gz
  md5: 3fd00d1cbc99569c6e7c411601ed3cf9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - edn_format = edn_format:main
    #
    # Would create an entry point called edn_format that calls edn_format.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pytz ==2013.9
    - pyrfc3339 ==0.2
    - ply ==3.4

  run:
    - python
    - pytz ==2013.9
    - pyrfc3339 ==0.2
    - ply ==3.4

test:
  # Python imports
  imports:
    - edn_format

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/swaroopch/edn_format
  license: Apache Software License
  summary: 'EDN format reader and writer in Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
