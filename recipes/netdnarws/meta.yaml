package:
  name: netdnarws
  version: "0.2.8"

source:
  fn: netdnarws-0.2.8.tar.gz
  url: https://pypi.python.org/packages/source/n/netdnarws/netdnarws-0.2.8.tar.gz
  md5: 8757f1f572d597aa34bc95a5af301b51
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - netdnarws = netdnarws:main
    #
    # Would create an entry point called netdnarws that calls netdnarws.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests
    - requests_netdna
    - requests_oauth
    - certifi

  run:
    - python
    - requests
    - requests_netdna
    - requests_oauth
    - certifi

test:
  # Python imports
  imports:
    - netdnarws

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://developer.netdna.com
  license: GPL v3
  summary: 'A Python REST Client for NetDNA REST Web Services'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
