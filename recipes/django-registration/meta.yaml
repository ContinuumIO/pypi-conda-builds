package:
  name: django-registration
  version: "1.0"

source:
  fn: django-registration-1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-registration/django-registration-1.0.tar.gz
  md5: 56a7e705dcef054ee44d51f1ef7b1db0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-registration = django-registration:main
    #
    # Would create an entry point called django-registration that calls django-registration.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - registration
    - registration.backends
    - registration.backends.default
    - registration.backends.simple
    - registration.management
    - registration.management.commands
    - registration.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.bitbucket.org/ubernostrum/django-registration/
  license: BSD License
  summary: 'An extensible user-registration application for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
