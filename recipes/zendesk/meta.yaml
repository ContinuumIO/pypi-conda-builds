package:
  name: zendesk
  version: "1.1.1"

source:
  fn: Zendesk-1.1.1.tar.gz
  url: https://pypi.python.org/packages/source/Z/Zendesk/Zendesk-1.1.1.tar.gz
  md5: b95db2073f330f600c3e3af65d4a09c4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zendesk = zendesk:main
    #
    # Would create an entry point called zendesk that calls zendesk.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - httplib2
    - simplejson

  run:
    - python
    - httplib2
    - simplejson

test:
  # Python imports
  imports:
    - zendesk

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/maxeventbrite/zendesk/tree/master
  license: MIT License
  summary: 'Python API Wrapper for Zendesk'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
