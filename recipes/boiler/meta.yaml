package:
  name: boiler
  version: "1.0.1"

source:
  fn: boiler-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/b/boiler/boiler-1.0.1.tar.gz
  md5: 1b01ab236a6fb84d762193d77aa44eed
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - boiler = boiler:main
    #
    # Would create an entry point called boiler that calls boiler.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - boiler

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://mogui.it
  license: UNKNOWN
  summary: 'A command line tool to quick manage boilerplate structures called **plates**, it is extensible'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
