package:
  name: schemaish
  version: "0.5.6"

source:
  fn: schemaish-0.5.6.tar.gz
  url: https://pypi.python.org/packages/source/s/schemaish/schemaish-0.5.6.tar.gz
  md5: 3bb820c938d8a41dac69898635eb6c93
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - schemaish = schemaish:main
    #
    # Would create an entry point called schemaish that calls schemaish.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - validatish >=0.6

  run:
    - python
    - setuptools
    - validatish >=0.6

test:
  # Python imports
  imports:
    - schemaish
    - schemaish.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://schema.ish.io
  license: BSD License
  summary: 'Schemaish is a simple schema library.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
