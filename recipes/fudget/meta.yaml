package:
  name: fudget
  version: "0.1dev"

source:
  fn: Fudget-0.1dev.tar.gz
  url: https://pypi.python.org/packages/source/F/Fudget/Fudget-0.1dev.tar.gz
  md5: c32c8e0f2219021d61d14787cbcc8d2d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fudget = fudget:main
    #
    # Would create an entry point called fudget that calls fudget.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - calendar
    - calendar.apps
    - calendar.apps.homepage
    - calendar.apps.registration
    - calendar.apps.registration.management
    - calendar.apps.registration.management.commands
    - calendar.apps.time_spent
    - calendar.apps.time_spent.templatetags
    - fudget

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.eloyz.com
  license: Eloy Zuniga Jr.
  summary: 'Budgeting Calendar for TIme & Finances'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
