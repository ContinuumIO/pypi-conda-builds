package:
  name: django-revuo
  version: "0.3"

source:
  fn: django-revuo-0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-revuo/django-revuo-0.3.tar.gz
  md5: 7acf9eb105c70461333d061c24d538af
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-revuo = django-revuo:main
    #
    # Would create an entry point called django-revuo that calls django-revuo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-summernote

  run:
    - python
    - django-summernote

test:
  # Python imports
  imports:
    - revuo

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Lasanha/revuo
  license: BSD License
  summary: 'Simple CMS django app'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
