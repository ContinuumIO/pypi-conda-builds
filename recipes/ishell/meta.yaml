package:
  name: ishell
  version: "0.1.4"

source:
  fn: ishell-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/i/ishell/ishell-0.1.4.tar.gz
  md5: 9b52ab841526e134a218909585a1c113
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ishell = ishell:main
    #
    # Would create an entry point called ishell that calls ishell.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - readline

  run:
    - python
    - readline

test:
  # Python imports
  imports:
    - ishell

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/italorossi/ishell
  license: MIT License
  summary: 'Build Interactive Shells with Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
