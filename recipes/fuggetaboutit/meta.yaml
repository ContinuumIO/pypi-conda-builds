package:
  name: fuggetaboutit
  version: "0.3.5"

source:
  fn: fuggetaboutit-0.3.5.tar.gz
  url: https://pypi.python.org/packages/source/f/fuggetaboutit/fuggetaboutit-0.3.5.tar.gz
  md5: 81d6ffe73c84ca19efe2e202e6ba2ecc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fuggetaboutit = fuggetaboutit:main
    #
    # Would create an entry point called fuggetaboutit that calls fuggetaboutit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy
    - mmh3
    - tornado >=3

  run:
    - python
    - numpy
    - mmh3
    - tornado >=3

test:
  # Python imports
  imports:
    - fuggetaboutit
    - fuggetaboutit.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/mynameisfiber/fuggetaboutit/
  license: GNU Lesser General Public License v3 or later (LGPLv3+)
  summary: 'implementations of a counting bloom filter, a timing bloom filter and a scaling timing bloom filter. ie: bloom filters for the stream'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
