package:
  name: netcfg
  version: "0.1.1"

source:
  fn: netcfg-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/n/netcfg/netcfg-0.1.1.tar.gz
  md5: 35ac1d75634122fd9d968a62d58e387b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - netcfg = netcfg:main
    #
    # Would create an entry point called netcfg that calls netcfg.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - docker-py >=0.5.0
    - pyzmq >=14.0.1
    - ipaddr >=2.1.10

  run:
    - python
    - docker-py >=0.5.0
    - pyzmq >=14.0.1
    - ipaddr >=2.1.10

test:
  # Python imports
  imports:
    - netcfg
    - netcfg.network

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/wlanslovenija/netcfg
  license: GNU Affero General Public License v3
  summary: 'Network configuration of Docker containers.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
