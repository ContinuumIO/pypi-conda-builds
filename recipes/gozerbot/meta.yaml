package:
  name: gozerbot
  version: "1.0.3"

source:
  fn: gozerbot-1.0.3.tar.gz
  url: https://pypi.python.org/packages/source/g/gozerbot/gozerbot-1.0.3.tar.gz
  md5: 5ec74a5137b254c4c02b056012d44bf4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gozerbot = gozerbot:main
    #
    # Would create an entry point called gozerbot that calls gozerbot.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - simplejson >=1.0
    - feedparser >=1.0

  run:
    - python
    - simplejson >=1.0
    - feedparser >=1.0

test:
  # Python imports
  imports:
    - gozerbot
    - gozerbot.contrib
    - gozerbot.database
    - gozerbot.irc
    - gozerbot.persist
    - gozerbot.plugs
    - gozerbot.rest
    - gozerbot.threads
    - gozerbot.utils
    - gozerbot.xmpp
    - gplugs
    - gplugs.alchemy
    - gplugs.olddb

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pikacode.com/bthate/gozerbot
  license: BSD License
  summary: 'irc bot and xmpp bot in one'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
