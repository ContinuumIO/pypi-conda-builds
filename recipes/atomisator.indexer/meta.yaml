package:
  name: atomisator.indexer
  version: "0.1.1"

source:
  fn: atomisator.indexer-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/a/atomisator.indexer/atomisator.indexer-0.1.1.tar.gz
  md5: 1f97df7e09b085a04eb5e7e335779618
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - atomisator.indexer = atomisator.indexer:main
    #
    # Would create an entry point called atomisator.indexer that calls atomisator.indexer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - afpy.xap

  run:
    - python
    - setuptools
    - afpy.xap

test:
  # Python imports
  imports:
    - atomisator
    - atomisator.indexer
    - atomisator.indexer.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://atomisator.ziade.org
  license: Python Software Foundation License
  summary: 'Expert Python Programming - Indexing'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
