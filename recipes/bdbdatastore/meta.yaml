package:
  name: bdbdatastore
  version: "0.2.2"

source:
  fn: bdbdatastore-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/b/bdbdatastore/bdbdatastore-0.2.2.tar.gz
  md5: 1344c23eb61a6d785e3435b255f1f586
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bdbdatastore = bdbdatastore:main
    #
    # Would create an entry point called bdbdatastore that calls bdbdatastore.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - bdbdatastore
    - google
    - google.protobuf
    - google.protobuf.internal
    - notdot
    - notdot.bdbdatastore

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://arachnid.github.com/bdbdatastore/
  license: Apache Software License
  summary: 'An alternate datastore backend for App Engine, implemented using BDB JE.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
