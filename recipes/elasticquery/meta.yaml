package:
  name: elasticquery
  version: "1.9.6"

source:
  fn: ElasticQuery-1.9.6.tar.gz
  url: https://pypi.python.org/packages/source/E/ElasticQuery/ElasticQuery-1.9.6.tar.gz
  md5: 1855a513a5fa46d2ae0c5cd225094a8a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - elasticquery = elasticquery:main
    #
    # Would create an entry point called elasticquery that calls elasticquery.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - elasticquery

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/Fizzadar/ElasticQuery
  license: UNKNOWN
  summary: 'A simple query builder for Elasticsearch'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
