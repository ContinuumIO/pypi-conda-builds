package:
  name: kabuki
  version: "0.5.5"

source:
  fn: kabuki-0.5.5.tar.gz
  url: https://pypi.python.org/packages/source/k/kabuki/kabuki-0.5.5.tar.gz
  md5: 144e1bdd8789cab22c5fa06faa2da28e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kabuki = kabuki:main
    #
    # Would create an entry point called kabuki that calls kabuki.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - numpy >=1.6.0
    - pymc ==2.3.3
    - pandas >=0.12.0
    - matplotlib >=1.0.0

  run:
    - python
    - numpy >=1.6.0
    - pymc ==2.3.3
    - pandas >=0.12.0
    - matplotlib >=1.0.0

test:
  # Python imports
  imports:
    - kabuki

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/hddm-devs/kabuki
  license: UNKNOWN
  summary: 'kabuki is a python toolbox that allows easy creation of hierarchical bayesian models for the cognitive sciences.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
