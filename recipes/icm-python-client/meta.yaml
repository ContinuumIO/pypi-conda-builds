package:
  name: icm-python-client
  version: "0.0.3"

source:
  fn: icm-python-client-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/i/icm-python-client/icm-python-client-0.0.3.tar.gz
  md5: 9680d38a64a6748ed7cd14f789d139f1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - icm-python-client = icm-python-client:main
    #
    # Would create an entry point called icm-python-client that calls icm-python-client.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - hammock >=0.2.4
    - requests >=2.5.3

  run:
    - python
    - hammock >=0.2.4
    - requests >=2.5.3

test:
  # Python imports
  imports:
    - icm_python_client

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/singlewire/icm-python-client
  license: Apache 2.0
  summary: 'A simple python client for InformaCast Mobile based on requests and Hammock'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
