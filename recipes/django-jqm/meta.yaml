package:
  name: django-jqm
  version: "1.0.0a2"

source:
  fn: django-jqm-1.0.0a2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-jqm/django-jqm-1.0.0a2.tar.gz
  md5: 717e597508cf11c4aa7b59926bf73b2d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-jqm = django-jqm:main
    #
    # Would create an entry point called django-jqm that calls django-jqm.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.2

  run:
    - python
    - django >=1.2

test:
  # Python imports
  imports:
    - jqm

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://launchpad.net/django-jqm
  license: MIT
  summary: 'Pluggable JQuery Mobile templates for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
