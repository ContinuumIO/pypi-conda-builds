package:
  name: indicngramlib
  version: "1.0"

source:
  fn: indicngramlib-1.0.tar.gz
  url: https://pypi.python.org/packages/source/i/indicngramlib/indicngramlib-1.0.tar.gz
  md5: 88994841771c58184380f1d28646800c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - indicngramlib = indicngramlib:main
    #
    # Would create an entry point called indicngramlib that calls indicngramlib.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - indicngramlib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://jaganadhg.freeflux.net
  license: GPLV3
  summary: 'N-Gram library for Indic Unicode text'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
