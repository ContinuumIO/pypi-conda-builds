package:
  name: flask-hmac
  version: "0.1.3"

source:
  fn: Flask-HMAC-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-HMAC/Flask-HMAC-0.1.3.tar.gz
  md5: 6b7dbb77859b3dbd09d616c1b9cdf905
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-hmac = flask-hmac:main
    #
    # Would create an entry point called flask-hmac that calls flask-hmac.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask

  run:
    - python
    - flask

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/jamonation/flask-hmac
  license: WTFPL
  summary: 'Flask HMAC generator, checker, and route decorator'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
