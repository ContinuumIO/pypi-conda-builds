package:
  name: dolly
  version: "0.1.0"

source:
  fn: Dolly-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/D/Dolly/Dolly-0.1.0.tar.gz
  md5: 8d39e96a6e7ac3c090bb7f911df24a16
#  patches:
   # List any patch files here
   # - fix.patch

build:
  # preserve_egg_dir: True
  entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dolly = dolly:main
    #
    # Would create an entry point called dolly that calls dolly.main()

    - dolly = dolly.dolly:main
    - dly = dolly.dolly:main

  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyyaml
    - argparse

  run:
    - python
    - pyyaml
    - argparse

test:
  # Python imports
  imports:
    - dolly

  commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.

    - dolly --help
    - dly --help

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: dolmen
  summary: 'Dolly manages multiple git and svn repos'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
