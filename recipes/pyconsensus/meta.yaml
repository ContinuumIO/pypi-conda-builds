package:
  name: pyconsensus
  version: "0.5.7"

source:
  fn: pyconsensus-0.5.7.tar.gz
  url: https://pypi.python.org/packages/source/p/pyconsensus/pyconsensus-0.5.7.tar.gz
  md5: d461ddf00cf7c6a60b16fd1229a924cd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pyconsensus = pyconsensus:main
    #
    # Would create an entry point called pyconsensus that calls pyconsensus.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy
    - pandas
    - six
    - weightedstats

  run:
    - python
    - numpy
    - pandas
    - six
    - weightedstats

test:
  # Python imports
  imports:
    - pyconsensus

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/AugurProject/pyconsensus
  license: GPL
  summary: "Standalone implementation of Augur's consensus mechanism"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
