package:
  name: csstyle
  version: "0.1"

source:
  fn: CSStyle-0.1.tar.gz
  url: https://pypi.python.org/packages/source/C/CSStyle/CSStyle-0.1.tar.gz
  md5: 5fcd75524b3bf4b3261944f0f1d7be3b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - csstyle = csstyle:main
    #
    # Would create an entry point called csstyle that calls csstyle.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - csstyle

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.csstyle.org/
  license: GNU General Public License (GPL)
  summary: 'CSStyle is a simple CSS parser generating CSS adapted for various browsers.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
