package:
  name: fume
  version: "0.1.2"

source:
  fn: fume-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/f/fume/fume-0.1.2.tar.gz
  md5: ea7138152d43fc71c54209e3715dc53c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fume = fume:main
    #
    # Would create an entry point called fume that calls fume.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - docopt >=0.6.1
    - requests >=2.0.0

  run:
    - python
    - docopt >=0.6.1
    - requests >=2.0.0

test:
  # Python imports
  imports:
    - fume

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'Fume: a smoke test scripting language'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
