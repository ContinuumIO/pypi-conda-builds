package:
  name: earthtools
  version: "0.0.4"

source:
  fn: earthtools-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/e/earthtools/earthtools-0.0.4.tar.gz
  md5: 6b581ce021eadaa0c3e7685e43682ab8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - earthtools = earthtools:main
    #
    # Would create an entry point called earthtools that calls earthtools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - earthtools
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/cancerhermit/PyEarthTools/
  license: GPL
  summary: 'Python wrapper around earthtools.org/webservices.htm'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
