package:
  name: django-ckeditor-filebrowser-filer
  version: "0.1.1"

source:
  fn: django-ckeditor-filebrowser-filer-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-ckeditor-filebrowser-filer/django-ckeditor-filebrowser-filer-0.1.1.tar.gz
  md5: c7c7e172545773a6d45f45d8e4cd0d9f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-ckeditor-filebrowser-filer = django-ckeditor-filebrowser-filer:main
    #
    # Would create an entry point called django-ckeditor-filebrowser-filer that calls django-ckeditor-filebrowser-filer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-filer
    - cmsplugin-filer

  run:
    - python
    - django-filer
    - cmsplugin-filer

test:
  # Python imports
  imports:
    - ckeditor_filebrowser_filer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/nephila/django-ckeditor-filebrowser-filer
  license: BSD License
  summary: 'A django-filer based CKEditor filebrowser'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
