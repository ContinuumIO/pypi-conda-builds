package:
  name: m01.mongofake
  version: "1.0.1"

source:
  fn: m01.mongofake-1.0.1.zip
  url: https://pypi.python.org/packages/source/m/m01.mongofake/m01.mongofake-1.0.1.zip
  md5: af3ed4e5571c4f965b5a0ce72b238d48
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - m01.mongofake = m01.mongofake:main
    #
    # Would create an entry point called m01.mongofake that calls m01.mongofake.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six
    - pymongo

  run:
    - python
    - setuptools
    - six
    - pymongo

test:
  # Python imports
  imports:
    - m01
    - m01.mongofake

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/m01.mongofake
  license: Zope Public License
  summary: 'Fake MongoDB implementation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
