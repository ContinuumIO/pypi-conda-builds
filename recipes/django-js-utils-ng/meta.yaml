package:
  name: django-js-utils-ng
  version: "0.5.futu"

source:
  fn: django-js-utils-ng-0.5.futu.tar.gz
  url: https://pypi.python.org/packages/source/d/django-js-utils-ng/django-js-utils-ng-0.5.futu.tar.gz
  md5: 7dc9ec14800649aa992e77910a2801c3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-js-utils-ng = django-js-utils-ng:main
    #
    # Would create an entry point called django-js-utils-ng that calls django-js-utils-ng.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - simplejson

  run:
    - python
    - simplejson

test:
  # Python imports
  imports:
    - django_js_utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/SanomaCZ/django-js-utils
  license: BSD License
  summary: 'Django URL Exposure to Javascript'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
