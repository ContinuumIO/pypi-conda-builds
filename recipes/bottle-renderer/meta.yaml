package:
  name: bottle-renderer
  version: "0.1.1"

source:
  fn: bottle-renderer-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/b/bottle-renderer/bottle-renderer-0.1.1.tar.gz
  md5: d188c368d4b0544808cb7d625522ad15
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bottle-renderer = bottle-renderer:main
    #
    # Would create an entry point called bottle-renderer that calls bottle-renderer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/agreatjewel/bottle_plugins/tree/master/bottle_renderer
  license: MIT License
  summary: 'Renderer plugin for bottle'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
