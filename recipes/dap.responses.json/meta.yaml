package:
  name: dap.responses.json
  version: "0.3"

source:
  fn: dap.responses.json-0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/dap.responses.json/dap.responses.json-0.3.tar.gz
  md5: 415ea576eb12c89424e0b1fcc0342363
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dap.responses.json = dap.responses.json:main
    #
    # Would create an entry point called dap.responses.json that calls dap.responses.json.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - dap[server] >=2.2.4
    - simplejson
    - paste
    - pastedeploy

  run:
    - python
    - setuptools
    - dap[server] >=2.2.4
    - simplejson
    - paste
    - pastedeploy

test:
  # Python imports
  imports:
    - dap
    - dap.responses
    - dap.responses.json

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pydap.org/responses/json
  license: MIT License
  summary: 'JSON response for pydap server'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
