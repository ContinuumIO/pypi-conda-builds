package:
  name: django-bananas
  version: "1.0.5"

source:
  fn: django-bananas-1.0.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-bananas/django-bananas-1.0.5.tar.gz
  md5: 6bc6875421dba9a52d48dc3e39499132
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-bananas = django-bananas:main
    #
    # Would create an entry point called django-bananas that calls django-bananas.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - bananas

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'Django Bananas - Django extensions the monkey way'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
