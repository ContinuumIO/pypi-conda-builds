package:
  name: cmsplugin-markdown
  version: "0.1.2"

source:
  fn: cmsplugin-markdown-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/c/cmsplugin-markdown/cmsplugin-markdown-0.1.2.tar.gz
  md5: 8b6c65664e857b7f26cb13cb7d420ef9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cmsplugin-markdown = cmsplugin-markdown:main
    #
    # Would create an entry point called cmsplugin-markdown that calls cmsplugin-markdown.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - cmsplugin_markdown
    - cmsplugin_markdown.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bitmazk/cmsplugin-markdown
  license: The MIT License
  summary: "A plugin for django-cms that aims to replace the standard text plugin with it's\nWYSIWYG editors. With cmsplugin-markdown you can write your content in Markdown\nusing EpicEditor (http://oscargodson.github.com/EpicEditor/)\n"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
