package:
  name: django-staff-toolbar
  version: "1.0"

source:
  fn: django-staff-toolbar-1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-staff-toolbar/django-staff-toolbar-1.0.tar.gz
  md5: bf551bc559bde9918a4f1ffed30ddf38
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-staff-toolbar = django-staff-toolbar:main
    #
    # Would create an entry point called django-staff-toolbar that calls django-staff-toolbar.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - staff_toolbar
    - staff_toolbar.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/edoburu/django-staff-toolbar
  license: Apache Software License
  summary: 'Show staff-only controls at website frontents'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
