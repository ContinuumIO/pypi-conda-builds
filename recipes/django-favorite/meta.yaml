package:
  name: django-favorite
  version: "0.0.1"

source:
  fn: django-favorite-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-favorite/django-favorite-0.0.1.tar.gz
  md5: 3ab10d476f6edeadb0a1a19852e22aec
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-favorite = django-favorite:main
    #
    # Would create an entry point called django-favorite that calls django-favorite.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - favorite
    - favorite.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/s1n4/django-favorite
  license: MIT License
  summary: 'A simple reusable app for django that makes it easy to have a favorite button for any model.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
