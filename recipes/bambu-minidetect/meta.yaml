package:
  name: bambu-minidetect
  version: "0.1"

source:
  fn: bambu-minidetect-0.1.tar.gz
  url: https://pypi.python.org/packages/source/b/bambu-minidetect/bambu-minidetect-0.1.tar.gz
  md5: 4b70208b1867e094e2a9e8862fad4056
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bambu-minidetect = bambu-minidetect:main
    #
    # Would create an entry point called bambu-minidetect that calls bambu-minidetect.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django >=1.4

  run:
    - python
    - django >=1.4

test:
  # Python imports
  imports:
    - bambu.minidetect

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/bambu-minidetect
  license: UNKNOWN
  summary: 'Tools for Django webapps'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
