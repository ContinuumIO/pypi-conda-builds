package:
  name: django-vanilla-views
  version: "1.0.3"

source:
  fn: django-vanilla-views-1.0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-vanilla-views/django-vanilla-views-1.0.3.tar.gz
  md5: 1ff055ca3ce23e22cf962a1864f27384
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-vanilla-views = django-vanilla-views:main
    #
    # Would create an entry point called django-vanilla-views that calls django-vanilla-views.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - vanilla

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://django-vanilla-views.org
  license: BSD License
  summary: 'Beautifully simple class based views.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
