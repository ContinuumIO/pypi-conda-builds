package:
  name: chain_bitcoin
  version: "0.4"

source:
  fn: chain_bitcoin-0.4.tar.gz
  url: https://pypi.python.org/packages/source/c/chain_bitcoin/chain_bitcoin-0.4.tar.gz
  md5: 5875df615009dbb9cb566468d20c7b92
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - chain_bitcoin = chain_bitcoin:main
    #
    # Would create an entry point called chain_bitcoin that calls chain_bitcoin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python_dateutil >=2.2
    - requests >=2

  run:
    - python
    - python_dateutil >=2.2
    - requests >=2

test:
  # Python imports
  imports:
    - chain_bitcoin

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/cardforcoin/chain-bitcoin-python
  license: MIT License
  summary: 'Integration library for the Chain.com API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
