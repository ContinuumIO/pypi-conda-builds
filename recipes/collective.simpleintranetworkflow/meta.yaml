package:
  name: collective.simpleintranetworkflow
  version: "1.0"

source:
  fn: collective.simpleintranetworkflow-1.0.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.simpleintranetworkflow/collective.simpleintranetworkflow-1.0.tar.gz
  md5: 8ff17e598ded90ec8d853107ed9ab337
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.simpleintranetworkflow = collective.simpleintranetworkflow:main
    #
    # Would create an entry point called collective.simpleintranetworkflow that calls collective.simpleintranetworkflow.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.cmfplone

  run:
    - python
    - setuptools
    - setuptools
    - products.cmfplone

test:
  # Python imports
  imports:
    - collective
    - collective.simpleintranetworkflow

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.simpleintranetworkflow
  license: GPL
  summary: 'Simple Intranet/Extranet workflow.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
