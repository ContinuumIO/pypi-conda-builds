package:
  name: listmilestonesmacro
  version: "0.1.2"

source:
  fn: ListMilestonesMacro-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/L/ListMilestonesMacro/ListMilestonesMacro-0.1.2.tar.gz
  md5: 1247f5263e4f7f6bac76ceb69af9a41a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - listmilestonesmacro = listmilestonesmacro:main
    #
    # Would create an entry point called listmilestonesmacro that calls listmilestonesmacro.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - listmilestones
    - listmilestonesmacro

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://mitar.tnode.com/
  license: AGPLv3
  summary: 'Lists milestones based on a name.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
