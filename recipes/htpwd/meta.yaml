package:
  name: htpwd
  version: "0.1.2a"

source:
  fn: htpwd-0.1.2a.tar.gz
  url: https://pypi.python.org/packages/source/h/htpwd/htpwd-0.1.2a.tar.gz
  md5: fbc13fa68024c81f227fa00d4237e18a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - htpwd = htpwd:main
    #
    # Would create an entry point called htpwd that calls htpwd.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - selenium
    - babel ==1.3
    - flask ==0.10.1
    - flask-babel ==0.9
    - flask-bootstrap ==3.1.1.2
    - flask-script ==2.0.3
    - flask-wtf ==0.9.5
    - jinja2 ==2.7.2
    - markupsafe ==0.21
    - wtforms ==1.0.5
    - werkzeug ==0.9.4
    - itsdangerous ==0.24
    - passlib ==1.6.2
    - pytz ==2014.2
    - speaklater ==1.3

  run:
    - python
    - selenium
    - babel ==1.3
    - flask ==0.10.1
    - flask-babel ==0.9
    - flask-bootstrap ==3.1.1.2
    - flask-script ==2.0.3
    - flask-wtf ==0.9.5
    - jinja2 ==2.7.2
    - markupsafe ==0.21
    - wtforms ==1.0.5
    - werkzeug ==0.9.4
    - itsdangerous ==0.24
    - passlib ==1.6.2
    - pytz ==2014.2
    - speaklater ==1.3

test:
  # Python imports
  imports:
    - htpwd
    - htpwd.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/gcavalcante8808/htpwd
  license: Apache Software License
  summary: 'A web Interface for users change their own passwords on a\n    htpasswd file.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
