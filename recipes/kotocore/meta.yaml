package:
  name: kotocore
  version: "0.1.2"

source:
  fn: kotocore-0.1.2.tar.bz2
  url: https://pypi.python.org/packages/source/k/kotocore/kotocore-0.1.2.tar.bz2
  md5: 14f052d7a854c45d92c3863dbed88cb8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kotocore = kotocore:main
    #
    # Would create an entry point called kotocore that calls kotocore.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - botocore >=0.24.0
    - six >=1.4.0
    - jmespath >=0.1.0
    - python-dateutil >=2.1

  run:
    - python
    - botocore >=0.24.0
    - six >=1.4.0
    - jmespath >=0.1.0
    - python-dateutil >=2.1

test:
  # Python imports
  imports:
    - kotocore
    - kotocore.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/henrysher/kotocore
  license: Apache Software License
  summary: 'Utility for botocore.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
