package:
  name: rvlm.entrypoint
  version: "0.0.6"

source:
  fn: rvlm.entrypoint-0.0.6.tar.gz
  url: https://pypi.python.org/packages/source/r/rvlm.entrypoint/rvlm.entrypoint-0.0.6.tar.gz
  md5: 74749f1206b89edba15502b133186177
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - rvlm.entrypoint = rvlm.entrypoint:main
    #
    # Would create an entry point called rvlm.entrypoint that calls rvlm.entrypoint.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - rvlm

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/rvlm/rvlm-entrypoint
  license: MIT License
  summary: "Converts function's signature to getopt-style command line options and arguments."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
