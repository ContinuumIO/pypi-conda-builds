package:
  name: djangorestframework-plist
  version: "0.2.2"

source:
  fn: djangorestframework-plist-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/d/djangorestframework-plist/djangorestframework-plist-0.2.2.tar.gz
  md5: 709d9cc207997b457df66e44e3623866
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangorestframework-plist = djangorestframework-plist:main
    #
    # Would create an entry point called djangorestframework-plist that calls djangorestframework-plist.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4.3
    - djangorestframework >=2.1.15
    - biplist

  run:
    - python
    - django >=1.4.3
    - djangorestframework >=2.1.15
    - biplist

test:
  # Python imports
  imports:
    - rest_framework_plist

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/lpomfrey/django-rest-framework-plist
  license: BSD License
  summary: 'Plist support for Django REST framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
