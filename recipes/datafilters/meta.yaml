package:
  name: datafilters
  version: "0.3.3"

source:
  fn: datafilters-0.3.3.zip
  url: https://pypi.python.org/packages/source/d/datafilters/datafilters-0.3.3.zip
  md5: e8b49e80daec01d3637bcd58f2c0acc9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - datafilters = datafilters:main
    #
    # Would create an entry point called datafilters that calls datafilters.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3

  run:
    - python
    - django >=1.3

test:
  # Python imports
  imports:
    - datafilters
    - datafilters.specs
    - datafilters.tests
    - sample_proj
    - sample_proj.polls

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/freevoid/django-datafilters
  license: MIT License
  summary: 'Neat QuerySet filter for django apps with filterforms based on django forms'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
