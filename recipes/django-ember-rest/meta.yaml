package:
  name: django-ember-rest
  version: "0.1.5"

source:
  fn: django-ember-rest-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-ember-rest/django-ember-rest-0.1.5.tar.gz
  md5: 2d383eee2f208209ec8ca7decf509852
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-ember-rest = django-ember-rest:main
    #
    # Would create an entry point called django-ember-rest that calls django-ember-rest.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_ember_rest

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kelonye/django-ember-rest
  license: MIT License
  summary: 'Django Models <> Ember-Data'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
