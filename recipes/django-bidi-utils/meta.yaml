package:
  name: django-bidi-utils
  version: "1.0"

source:
  fn: django-bidi-utils-1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-bidi-utils/django-bidi-utils-1.0.tar.gz
  md5: 7e7d9cc31fae79762ae042dca7a6c160
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-bidi-utils = django-bidi-utils:main
    #
    # Would create an entry point called django-bidi-utils that calls django-bidi-utils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - bidiutils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/MeirKriheli/django-bidi-utils
  license: MIT License
  summary: 'context processors and filters for handling Bi-directional (BiDi) in django templates'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
