package:
  name: easymail
  version: "0.2.0"

source:
  fn: easymail-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/e/easymail/easymail-0.2.0.tar.gz
  md5: 9e001e7fe90eda120149f7d036633a7c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - easymail = easymail:main
    #
    # Would create an entry point called easymail that calls easymail.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - easymail

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/easymail/
  license: MIT License
  summary: 'abstraction layer on top of the email package to make sending    emails a little bit easier'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
