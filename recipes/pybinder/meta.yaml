package:
  name: pybinder
  version: "2.0.1"

source:
  fn: PyBinder-2.0.1.tar.gz
  url: https://pypi.python.org/packages/source/P/PyBinder/PyBinder-2.0.1.tar.gz
  md5: 8211ae81d368d87ad13127ddaa04ab95
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pybinder = pybinder:main
    #
    # Would create an entry point called pybinder that calls pybinder.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six ==1.8.0

  run:
    - python
    - six ==1.8.0

test:
  # Python imports
  imports:
    - pybinder

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/developers-30/pybinder
  license: BSD License
  summary: 'Python dependency injection container'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
