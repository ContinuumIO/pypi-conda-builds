package:
  name: ducktypes
  version: "0.2.5"

source:
  fn: ducktypes-0.2.5.zip
  url: https://pypi.python.org/packages/source/d/ducktypes/ducktypes-0.2.5.zip
  md5: 202a9f40b231d019b3ea6d66b5e80f49
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ducktypes = ducktypes:main
    #
    # Would create an entry point called ducktypes that calls ducktypes.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - ducktypes
    - ducktypes.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://ducktypes.franzoni.eu
  license: APL 2.0
  summary: 'behaviour based duck typing library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
