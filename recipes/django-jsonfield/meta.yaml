package:
  name: django-jsonfield
  version: "0.9.13"

source:
  fn: django-jsonfield-0.9.13.tar.gz
  url: https://pypi.python.org/packages/source/d/django-jsonfield/django-jsonfield-0.9.13.tar.gz
  md5: 59711741a3ce1798fd90e841e0b5206e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-jsonfield = django-jsonfield:main
    #
    # Would create an entry point called django-jsonfield that calls django-jsonfield.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - jsonfield

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/schinckel/django-jsonfield/
  license: BSD License
  summary: 'JSONField for django models'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
