package:
  name: cnb-exchange-rate
  version: "0.1.2"

source:
  fn: cnb-exchange-rate-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/c/cnb-exchange-rate/cnb-exchange-rate-0.1.2.tar.gz
  md5: c826ba37cb60da4688325e74aed63f8d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cnb-exchange-rate = cnb-exchange-rate:main
    #
    # Would create an entry point called cnb-exchange-rate that calls cnb-exchange-rate.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - six

  run:
    - python
    - six

test:
  # Python imports
  imports:
    - cnb_exchange_rate

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/stepansojka/cnb-exchange-rate
  license: MIT License
  summary: 'Czech National Bank Exchange Rate Downloader'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
