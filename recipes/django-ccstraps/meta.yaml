package:
  name: django-ccstraps
  version: "0.0.1"

source:
  fn: django-ccstraps-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-ccstraps/django-ccstraps-0.0.1.tar.gz
  md5: 2b4d837932e9408620204a665e85bff8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-ccstraps = django-ccstraps:main
    #
    # Would create an entry point called django-ccstraps that calls django-ccstraps.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - ccstraps
    - ccstraps.templatetags
    - ccstraps.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/designcc/django-ccstraps
  license: BSD 3 Clause
  summary: 'A rotating strap application for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
