package:
  name: pynanocms
  version: "0.2.5"

source:
  fn: PyNanoCMS-0.2.5.tar.gz
  url: https://pypi.python.org/packages/source/P/PyNanoCMS/PyNanoCMS-0.2.5.tar.gz
  md5: 4f0dbbbb6e3a2153552f99820bfc9ca7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pynanocms = pynanocms:main
    #
    # Would create an entry point called pynanocms that calls pynanocms.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - jinja2

  run:
    - python
    - jinja2

test:
  # Python imports
  imports:
    - pynanocms

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rombr/pynano
  license: BSD License
  summary: 'Static site generator'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
