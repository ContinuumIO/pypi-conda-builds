package:
  name: ipynbcompress
  version: "0.3.0"

source:
  fn: ipynbcompress-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/i/ipynbcompress/ipynbcompress-0.3.0.tar.gz
  md5: fd62defd28fd7b747bf236d01db98765
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ipynbcompress = ipynbcompress:main
    #
    # Would create an entry point called ipynbcompress that calls ipynbcompress.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pillow
    - jsonschema
    - ipython
    - climate
    - hurry.filesize

  run:
    - python
    - pillow
    - jsonschema
    - ipython
    - climate
    - hurry.filesize

test:
  # Python imports
  imports:
    - ipynbcompress

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/arve0/ipynbcompress
  license: MIT License
  summary: 'Compress images in IPython/Jupyter notebooks'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
