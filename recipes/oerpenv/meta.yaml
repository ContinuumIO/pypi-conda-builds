package:
  name: oerpenv
  version: "1.8.13"

source:
  fn: oerpenv-1.8.13.tar.gz
  url: https://pypi.python.org/packages/source/o/oerpenv/oerpenv-1.8.13.tar.gz
  md5: ee052f1af885e03cd9d7facf6ec4834f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - oerpenv = oerpenv:main
    #
    # Would create an entry point called oerpenv that calls oerpenv.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - virtualenv
    - psycopg2
    - argparse
    - bzr

  run:
    - python
    - virtualenv
    - psycopg2
    - argparse
    - bzr

test:
  # Python imports
  imports:
    - oerpenv

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://business.moldeo.coop/
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'OpenERP Environment Administrator'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
