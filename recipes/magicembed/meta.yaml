package:
  name: magicembed
  version: "0.3"

source:
  fn: magicembed-0.3.tar.gz
  url: https://pypi.python.org/packages/source/m/magicembed/magicembed-0.3.tar.gz
  md5: d1c786d489c44b691dd063e157ef7c3f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - magicembed = magicembed:main
    #
    # Would create an entry point called magicembed that calls magicembed.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4

  run:
    - python
    - django >=1.4

test:
  # Python imports
  imports:
    - magicembed
    - magicembed.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/kronoscode/django-magicembed
  license: MIT
  summary: 'Django template filter utils to render videos an thumbnails.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
