package:
  name: jinja2_pluralize
  version: "0.2.1"

source:
  fn: jinja2_pluralize-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/j/jinja2_pluralize/jinja2_pluralize-0.2.1.tar.gz
  md5: 3e68c6e85d00bb7cf0ddcc5941f4a738
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jinja2_pluralize = jinja2_pluralize:main
    #
    # Would create an entry point called jinja2_pluralize that calls jinja2_pluralize.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - jinja2 >=2.4
    - inflect >=0.2.4

  run:
    - python
    - jinja2 >=2.4
    - inflect >=0.2.4

test:
  # Python imports
  imports:
    - jinja2_pluralize

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/audreyr/jinja2_pluralize
  license: BSD License
  summary: 'Jinja2 pluralize filters.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
