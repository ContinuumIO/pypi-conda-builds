package:
  name: flask-arrest
  version: "0.4.4"

source:
  fn: Flask-arrest-0.4.4.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-arrest/Flask-arrest-0.4.4.tar.gz
  md5: 64baefe498a1f425f6eefb9ff6e6ddb9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-arrest = flask-arrest:main
    #
    # Would create an entry point called flask-arrest that calls flask-arrest.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - werkzeug
    - jsonext

  run:
    - python
    - flask
    - werkzeug
    - jsonext

test:
  # Python imports
  imports:
    - flask_arrest

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/mbr/Flask-arrest
  license: MIT
  summary: 'A small Flask extension to ease the creation of REST apis.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
