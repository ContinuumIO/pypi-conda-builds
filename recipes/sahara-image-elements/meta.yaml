package:
  name: sahara-image-elements
  version: "2014.2"

source:
  fn: sahara-image-elements-2014.2.tar.gz
  url: https://pypi.python.org/packages/source/s/sahara-image-elements/sahara-image-elements-2014.2.tar.gz
  md5: a728f6eab33e50b03fbf499751f92159
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sahara-image-elements = sahara-image-elements:main
    #
    # Would create an entry point called sahara-image-elements that calls sahara-image-elements.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Apache Software License
  summary: 'Image elements for Sahara'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
