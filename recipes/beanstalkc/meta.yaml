package:
  name: beanstalkc
  version: "0.4.0"

source:
  fn: beanstalkc-0.4.0.tar.gz
  url: https://pypi.python.org/packages/source/b/beanstalkc/beanstalkc-0.4.0.tar.gz
  md5: fc152f93bc94d465d740e818df9ad10e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - beanstalkc = beanstalkc:main
    #
    # Would create an entry point called beanstalkc that calls beanstalkc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/earl/beanstalkc
  license: Apache Software License
  summary: 'A simple beanstalkd client library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
