package:
  name: django-mobile-platform
  version: "0.2.3"

source:
  fn: django-mobile-platform-0.2.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-mobile-platform/django-mobile-platform-0.2.3.tar.gz
  md5: 07b8ff20680f8dc2218a182b887eb9bc
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-mobile-platform = django-mobile-platform:main
    #
    # Would create an entry point called django-mobile-platform that calls django-mobile-platform.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - django_mobile
    - django_mobile.cache

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/gregmuellegger/django-mobile
  license: BSD License
  summary: u'Detect mobile browsers and serve different template flavours to them.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
