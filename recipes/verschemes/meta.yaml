package:
  name: verschemes
  version: "1.2.1"

source:
  fn: verschemes-1.2.1.tar.gz
  url: https://pypi.python.org/packages/source/v/verschemes/verschemes-1.2.1.tar.gz
  md5: 91e818809732e35d18ed30a2b48221ed
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - verschemes = verschemes:main
    #
    # Would create an entry point called verschemes that calls verschemes.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - future

  run:
    - python
    - future

test:
  # Python imports
  imports:
    - verschemes
    - verschemes.future

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/gnuworldman/verschemes
  license: GNU General Public License v3 (GPLv3)
  summary: 'Version identifier management'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
