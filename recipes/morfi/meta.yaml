package:
  name: morfi
  version: "0.0.1"

source:
  fn: morfi-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/m/morfi/morfi-0.0.1.tar.gz
  md5: 2009923620627bd70ecbc6ee5a05012a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - morfi = morfi:main
    #
    # Would create an entry point called morfi that calls morfi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - jinja2 >=2.7.3
    - cherrypy >=3.3.0

  run:
    - python
    - jinja2 >=2.7.3
    - cherrypy >=3.3.0

test:
  # Python imports
  imports:
    - morfi
    - orioniface

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/keydach/morfi
  license: LGPLv3
  summary: 'RC Online services library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
