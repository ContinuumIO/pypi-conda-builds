package:
  name: geobrickscommon
  version: "0.0.7"

source:
  fn: GeobricksCommon-0.0.7.tar.gz
  url: https://pypi.python.org/packages/source/G/GeobricksCommon/GeobricksCommon-0.0.7.tar.gz
  md5: 9bbdd668ee4937a15192fcc88cc585cd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - geobrickscommon = geobrickscommon:main
    #
    # Would create an entry point called geobrickscommon that calls geobrickscommon.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - watchdog
    - flask
    - flask-cors
    - requests

  run:
    - python
    - watchdog
    - flask
    - flask-cors
    - requests

test:
  # Python imports
  imports:
    - geobricks_common
    - geobricks_common.config
    - geobricks_common.core
    - geobricks_common.rest
    - geobrickscommon

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/GeobricksCommon/
  license: LICENSE.txt
  summary: 'Geobricks common library.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
