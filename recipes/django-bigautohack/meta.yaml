package:
  name: django-bigautohack
  version: "0.1"

source:
  fn: django-bigautohack-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-bigautohack/django-bigautohack-0.1.tar.gz
  md5: d00d5aaefc10ce7c37a87a35ad44c88b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-bigautohack = django-bigautohack:main
    #
    # Would create an entry point called django-bigautohack that calls django-bigautohack.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - bigautohack
    - bigautohack.management
    - bigautohack.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Remiremi/django_bigautohack
  license: MIT License
  summary: 'Nice working solution to make up for the absence of BigAutoField in django (#14286).'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
