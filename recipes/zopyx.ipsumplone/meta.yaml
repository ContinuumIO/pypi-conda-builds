package:
  name: zopyx.ipsumplone
  version: "0.2.1"

source:
  fn: zopyx.ipsumplone-0.2.1.zip
  url: https://pypi.python.org/packages/source/z/zopyx.ipsumplone/zopyx.ipsumplone-0.2.1.zip
  md5: bcb90fddb8a9e924bb76cfa95c3bf139
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zopyx.ipsumplone = zopyx.ipsumplone:main
    #
    # Would create an entry point called zopyx.ipsumplone that calls zopyx.ipsumplone.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - loremipsum

  run:
    - python
    - setuptools
    - setuptools
    - loremipsum

test:
  # Python imports
  imports:
    - zopyx
    - zopyx.ipsumplone
    - zopyx.ipsumplone.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/zopyx.ipsumplone
  license: GPL
  summary: 'Lorem ipsum text and image demo content for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
