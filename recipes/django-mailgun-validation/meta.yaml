package:
  name: django-mailgun-validation
  version: "1.0.1"

source:
  fn: django-mailgun-validation-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-mailgun-validation/django-mailgun-validation-1.0.1.tar.gz
  md5: b5aa67e3306fc2884ed45faba3ebcab4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-mailgun-validation = django-mailgun-validation:main
    #
    # Would create an entry point called django-mailgun-validation that calls django-mailgun-validation.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - requests
    - django

  run:
    - python
    - requests
    - django

test:
  # Python imports
  imports:
    - mailgun_validation

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/gnrfan/django-mailgun-validation
  license: UNKNOWN
  summary: 'Django fields for validating email using the Mailgun API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
