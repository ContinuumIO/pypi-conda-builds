package:
  name: jshloadoffline
  version: "0.1.3"

source:
  fn: jshloadoffline-0.1.3.zip
  url: https://pypi.python.org/packages/source/j/jshloadoffline/jshloadoffline-0.1.3.zip
  md5: 0b0d3ab1fc623d5dd831b96c9eb5bed5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jshloadoffline = jshloadoffline:main
    #
    # Would create an entry point called jshloadoffline that calls jshloadoffline.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - path.py

  run:
    - python
    - path.py

test:
  # Python imports
  imports:
    - jshloadoffline

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lixxu/jshloadoffline
  license: BSD License
  summary: 'Load offline prefetched data in JSH'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
