package:
  name: chandler-reportplugin
  version: "0.1"

source:
  fn: Chandler-ReportPlugin-0.1.tar.gz
  url: https://pypi.python.org/packages/source/C/Chandler-ReportPlugin/Chandler-ReportPlugin-0.1.tar.gz
  md5: 1ac9ec0abc9460b2d6e49fd653fc2a6a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - chandler-reportplugin = chandler-reportplugin:main
    #
    # Would create an entry point called chandler-reportplugin that calls chandler-reportplugin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - reports

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Apache Software License
  summary: 'Progress reports for Chandler'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
