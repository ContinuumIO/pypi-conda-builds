package:
  name: isomedia
  version: "0.2.3"

source:
  fn: isomedia-0.2.3.tar.gz
  url: https://pypi.python.org/packages/source/i/isomedia/isomedia-0.2.3.tar.gz
  md5: 7256ca59fc5dc4bb7f98036bce67ba17
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - isomedia = isomedia:main
    #
    # Would create an entry point called isomedia that calls isomedia.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - isomedia

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/flxf/isomedia
  license: UNKNOWN
  summary: 'This is another ISO base media format file parser.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
