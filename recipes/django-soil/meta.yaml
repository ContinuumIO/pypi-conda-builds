package:
  name: django-soil
  version: "0.9.0"

source:
  fn: django-soil-0.9.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-soil/django-soil-0.9.0.tar.gz
  md5: 122eb17ca1eb232614ff9cbd7927f34e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-soil = django-soil:main
    #
    # Would create an entry point called django-soil that calls django-soil.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - soil

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dimagi/django-soil
  license: UNKNOWN
  summary: "A simple django library to help you schedule long running tasks for retrieval later when they're done using celery"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
