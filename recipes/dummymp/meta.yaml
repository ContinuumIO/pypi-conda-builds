package:
  name: dummymp
  version: "0.5b2"

source:
  fn: dummymp-0.5b2.tar.gz
  url: https://pypi.python.org/packages/source/d/dummymp/dummymp-0.5b2.tar.gz
  md5: f1a31a392bc62f90a5bfac805b6ff5bf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dummymp = dummymp:main
    #
    # Would create an entry point called dummymp that calls dummymp.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - dummymp

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/alberthdev/dummymp
  license: Apache Software License
  summary: 'A multiprocessing library for dummies. (Library for easily running functions in parallel)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
