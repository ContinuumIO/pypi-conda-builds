package:
  name: django_auth_ldap3
  version: "0.9.2"

source:
  fn: django_auth_ldap3-0.9.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django_auth_ldap3/django_auth_ldap3-0.9.2.tar.gz
  md5: 2122bf0f9d9924913f6f1793a7486bb5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_auth_ldap3 = django_auth_ldap3:main
    #
    # Would create an entry point called django_auth_ldap3 that calls django_auth_ldap3.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6.10
    - ldap3 >=0.9.7.1

  run:
    - python
    - django >=1.6.10
    - ldap3 >=0.9.7.1

test:
  # Python imports
  imports:
    - django_auth_ldap3

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/sjkingo/django_auth_ldap3
  license: BSD License
  summary: "A library for connecting Django's authentication system to an LDAP directory"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
