package:
  name: django-uwsgi-admin2
  version: "0.2.1"

source:
  fn: django-uwsgi-admin2-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-uwsgi-admin2/django-uwsgi-admin2-0.2.1.tar.gz
  md5: 7dd3b68db3ce70f4e375d871172fc748
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-uwsgi-admin2 = django-uwsgi-admin2:main
    #
    # Would create an entry point called django-uwsgi-admin2 that calls django-uwsgi-admin2.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - django-admin-utils >=0.1.2

  run:
    - python
    - django
    - django-admin-utils >=0.1.2

test:
  # Python imports
  imports:
    - uwsgi_admin

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ionelmc/django-uwsgi-admin
  license: BSD License
  summary: 'uWSGI monitoring page in django admin and debug_toolbar panel.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
