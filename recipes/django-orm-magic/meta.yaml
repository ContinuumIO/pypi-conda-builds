package:
  name: django-orm-magic
  version: "0.3.1"

source:
  fn: django-orm-magic-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-orm-magic/django-orm-magic-0.3.1.tar.gz
  md5: 3135d618af30441c82f1ee1b6f01803e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-orm-magic = django-orm-magic:main
    #
    # Would create an entry point called django-orm-magic that calls django-orm-magic.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - ipython
    - django

  run:
    - python
    - ipython
    - django

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mgaitan/django-orm-magic
  license: BSD License
  summary: "An extension for IPython that help to define django's models in your interactive session."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
