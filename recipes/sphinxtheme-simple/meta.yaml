package:
  name: sphinxtheme-simple
  version: "0.1"

source:
  fn: sphinxtheme-simple-0.1.tar.gz
  url: https://pypi.python.org/packages/source/s/sphinxtheme-simple/sphinxtheme-simple-0.1.tar.gz
  md5: 030155ef433c645640d1d51818a7da91
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sphinxtheme-simple = sphinxtheme-simple:main
    #
    # Would create an entry point called sphinxtheme-simple that calls sphinxtheme-simple.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - sphinxtheme_simple

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/sphinxtheme-simple
  license: BSD3
  summary: 'Simple sphinx theme'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
