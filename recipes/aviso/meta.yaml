package:
  name: aviso
  version: "0.9.2"

source:
  fn: AVISO-0.9.2.tar.gz
  url: https://pypi.python.org/packages/source/A/AVISO/AVISO-0.9.2.tar.gz
  md5: 0f07c87df0b24948d2cd9d7d2497e240
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - aviso = aviso:main
    #
    # Would create an entry point called aviso that calls aviso.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy >=1.1
    - pupynere >=1.0.15
    - netcdf4 >=1.0.4
    - pydap >=3.1.RC1

  run:
    - python
    - setuptools
    - numpy >=1.1
    - pupynere >=1.0.15
    - netcdf4 >=1.0.4
    - pydap >=3.1.RC1

test:
  # Python imports
  imports:
    - aviso

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://pyaviso.castelao.net
  license: MIT License 
  summary: 'A library to handle altimetric data produced by AVISO'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
