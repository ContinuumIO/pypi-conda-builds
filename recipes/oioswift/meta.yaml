package:
  name: oioswift
  version: "0.1"

source:
  fn: oioswift-0.1.tar.gz
  url: https://pypi.python.org/packages/source/o/oioswift/oioswift-0.1.tar.gz
  md5: 7b9c52c21ff42b911434b57aba9ceee8
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - oioswift = oioswift:main
    #
    # Would create an entry point called oioswift that calls oioswift.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - webob
    - pastedeploy
    - oiopy

  run:
    - python
    - setuptools
    - webob
    - pastedeploy
    - oiopy

test:
  # Python imports
  imports:
    - oioswift
    - oioswift.controller
    - oioswift.filter

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/open-io/oio-swift
  license: GNU Affero General Public License v3
  summary: 'OpenIO Swift Gateway'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
