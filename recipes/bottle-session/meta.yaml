package:
  name: bottle-session
  version: "0.4"

source:
  fn: bottle-session-0.4.tar.gz
  url: https://pypi.python.org/packages/source/b/bottle-session/bottle-session-0.4.tar.gz
  md5: 21e876d4c0573f94cda51b68de512560
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bottle-session = bottle-session:main
    #
    # Would create an entry point called bottle-session that calls bottle-session.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - bottle >=0.9
    - redis

  run:
    - python
    - bottle >=0.9
    - redis

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/devries/bottle-session
  license: Artistic License
  summary: 'Redis based sessions for bottle.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
