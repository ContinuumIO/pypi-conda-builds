package:
  name: django-lastfm
  version: "1.0.1"

source:
  fn: django-lastfm-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-lastfm/django-lastfm-1.0.1.tar.gz
  md5: c57ac1cfbd93b3427954071a2dfec66d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-lastfm = django-lastfm:main
    #
    # Would create an entry point called django-lastfm that calls django-lastfm.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - lastfm
    - lastfm.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://stefan.sofa-rockers.org/django-lastfm/
  license: BSD License
  summary: 'Access Last.fm from your Django site.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
