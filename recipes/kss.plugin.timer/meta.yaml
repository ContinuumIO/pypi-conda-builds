package:
  name: kss.plugin.timer
  version: "1.1"

source:
  fn: kss.plugin.timer-1.1.zip
  url: https://pypi.python.org/packages/source/k/kss.plugin.timer/kss.plugin.timer-1.1.zip
  md5: 6f8a024ca667932670bf467dcd022a4b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kss.plugin.timer = kss.plugin.timer:main
    #
    # Would create an entry point called kss.plugin.timer that calls kss.plugin.timer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - kss.base
    - setuptools

  run:
    - python
    - setuptools
    - kss.base
    - setuptools

test:
  # Python imports
  imports:
    - kss
    - kss.plugin
    - kss.plugin.timer
    - kss.plugin.timer.demo
    - kss.plugin.timer.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://plone.org/products/extreme-management-tool/
  license: UNKNOWN
  summary: 'Timer plugin for KSS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
