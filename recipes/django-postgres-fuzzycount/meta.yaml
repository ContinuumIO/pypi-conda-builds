package:
  name: django-postgres-fuzzycount
  version: "0.1.6"

source:
  fn: django-postgres-fuzzycount-0.1.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-postgres-fuzzycount/django-postgres-fuzzycount-0.1.6.tar.gz
  md5: 4ab5264e676847ca18bb2e176cd20fe7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-postgres-fuzzycount = django-postgres-fuzzycount:main
    #
    # Would create an entry point called django-postgres-fuzzycount that calls django-postgres-fuzzycount.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sphinx-me >=0.1.2
    - django >=1.4
    - django-model-utils ==1.4.0

  run:
    - python
    - sphinx-me >=0.1.2
    - django >=1.4
    - django-model-utils ==1.4.0

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/stephenmcd/django-postgres-fuzzycount
  license: UNKNOWN
  summary: 'A Django model manager providing fast / fuzzy counts for PostgreSQL database tables.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
