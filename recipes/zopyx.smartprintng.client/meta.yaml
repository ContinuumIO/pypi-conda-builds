package:
  name: zopyx.smartprintng.client
  version: "0.8.5"

source:
  fn: zopyx.smartprintng.client-0.8.5.zip
  url: https://pypi.python.org/packages/source/z/zopyx.smartprintng.client/zopyx.smartprintng.client-0.8.5.zip
  md5: 76606d18f39f2b10d90f156c0241de94
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zopyx.smartprintng.client = zopyx.smartprintng.client:main
    #
    # Would create an entry point called zopyx.smartprintng.client that calls zopyx.smartprintng.client.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - zopyx
    - zopyx.smartprintng
    - zopyx.smartprintng.client

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/zopyx.smartprintng.client
  license: ZPL
  summary: 'ZOPYX SmartPrintNG Client Library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
