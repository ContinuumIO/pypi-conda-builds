package:
  name: hanzi2pinyin
  version: "0.1"

source:
  fn: hanzi2pinyin-0.1.tar.gz
  url: https://pypi.python.org/packages/source/h/hanzi2pinyin/hanzi2pinyin-0.1.tar.gz
  md5: d5c1e5c191cf5866c5732d1b28f34747
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hanzi2pinyin = hanzi2pinyin:main
    #
    # Would create an entry point called hanzi2pinyin that calls hanzi2pinyin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - hanzi2pinyin

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.flyzen.com
  license: BSD License
  summary: '\xe6\xb1\x89\xe5\xad\x97\xe8\xbd\xac\xe6\x8b\xbc\xe9\x9f\xb3'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
