package:
  name: collective.ptg.galleryfolder
  version: "0.3"

source:
  fn: collective.ptg.galleryfolder-0.3.zip
  url: https://pypi.python.org/packages/source/c/collective.ptg.galleryfolder/collective.ptg.galleryfolder-0.3.zip
  md5: 5c278178a13e9e7dada4cff22300837f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.ptg.galleryfolder = collective.ptg.galleryfolder:main
    #
    # Would create an entry point called collective.ptg.galleryfolder that calls collective.ptg.galleryfolder.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.plonetruegallery
    - wildcard.foldercontents

  run:
    - python
    - setuptools
    - setuptools
    - collective.plonetruegallery
    - wildcard.foldercontents

test:
  # Python imports
  imports:
    - collective
    - collective.ptg
    - collective.ptg.galleryfolder
    - collective.ptg.galleryfolder.browser
    - collective.ptg.galleryfolder.content
    - collective.ptg.galleryfolder.interfaces

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/medialog/collective.ptg.galleryfolder
  license: GPL
  summary: 'galleryfolder gallery type for plonetruegallery'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
