package:
  name: captchadecoder
  version: "0.1"

source:
  fn: captchadecoder-0.1.zip
  url: https://pypi.python.org/packages/source/c/captchadecoder/captchadecoder-0.1.zip
  md5: 5ae449d3abe6d6941833331aa0784d9b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - captchadecoder = captchadecoder:main
    #
    # Would create an entry point called captchadecoder that calls captchadecoder.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - captchadecoder

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Zenyai/Simple-Captcha-Decoder/
  license: UNKNOWN
  summary: 'Very Simple Captcha Decoder'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
