package:
  name: alligator
  version: "0.10.0"

source:
  fn: alligator-0.10.0.tar.gz
  url: https://pypi.python.org/packages/source/a/alligator/alligator-0.10.0.tar.gz
  md5: 716fd05871a1f1edf945abc6927550e6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - alligator = alligator:main
    #
    # Would create an entry point called alligator that calls alligator.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - alligator
    - alligator/backends

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/toastdriven/alligator/
  license: BSD License
  summary: 'Simple offline task queues.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
