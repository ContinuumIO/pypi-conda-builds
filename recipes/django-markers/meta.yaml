package:
  name: django-markers
  version: "1.1"

source:
  fn: django-markers-1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-markers/django-markers-1.1.tar.gz
  md5: caffb823005df569f39c866f27c470c8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-markers = django-markers:main
    #
    # Would create an entry point called django-markers that calls django-markers.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4
    - pillow >=2.0.0
    - numpy >=1.7.1

  run:
    - python
    - django >=1.4
    - pillow >=2.0.0
    - numpy >=1.7.1

test:
  # Python imports
  imports:
    - markers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/danielquinn/django-markers
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'Dynamic map marker generation using template images and arbitrary text'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
