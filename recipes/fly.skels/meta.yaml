package:
  name: fly.skels
  version: "0.2.1"

source:
  fn: fly.skels-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/f/fly.skels/fly.skels-0.2.1.tar.gz
  md5: 913284bcc6fa41ee202766a36968a813
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fly.skels = fly.skels:main
    #
    # Would create an entry point called fly.skels that calls fly.skels.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - pastescript
    - cheetah

  run:
    - python
    - setuptools
    - setuptools
    - pastescript
    - cheetah

test:
  # Python imports
  imports:
    - fly
    - fly.skels

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.flyzen.com
  license: BSD
  summary: 'Skeletons used on fly'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
