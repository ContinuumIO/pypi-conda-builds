package:
  name: django-templateloaderwithpriorities
  version: "1.0dev"

source:
  fn: django-templateloaderwithpriorities-1.0dev.tar.gz
  url: https://pypi.python.org/packages/source/d/django-templateloaderwithpriorities/django-templateloaderwithpriorities-1.0dev.tar.gz
  md5: d50de1b8893bb2718838d1949b409c3b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-templateloaderwithpriorities = django-templateloaderwithpriorities:main
    #
    # Would create an entry point called django-templateloaderwithpriorities that calls django-templateloaderwithpriorities.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3

  run:
    - python
    - setuptools
    - django >=1.3

test:
  # Python imports
  imports:
    - templateloaderwithpriorities

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/qubird/django-templateloaderwithpriorities
  license: GNU General Public License (GPL)
  summary: 'A Django Template Loader with priorities'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
