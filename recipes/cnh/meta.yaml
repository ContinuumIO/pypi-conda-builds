package:
  name: cnh
  version: "1.16.1"

source:
  fn: cnh-1.16.1.tar.gz
  url: https://pypi.python.org/packages/source/c/cnh/cnh-1.16.1.tar.gz
  md5: 7e27462a112af3b5ea3900950d691f81
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cnh = cnh:main
    #
    # Would create an entry point called cnh that calls cnh.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pycclib >=1.6.0
    - argparse >=1.1
    - paramiko >=1.15.2

  run:
    - python
    - pycclib >=1.6.0
    - argparse >=1.1
    - paramiko >=1.15.2

test:
  # Python imports
  imports:
    - cctrl
    - cnh
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://www.cloudcontrol.com
  license: Apache Software License
  summary: 'cloud&heat command line utilities'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
