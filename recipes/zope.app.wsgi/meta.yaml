package:
  name: zope.app.wsgi
  version: "4.0.0a4"

source:
  fn: zope.app.wsgi-4.0.0a4.zip
  url: https://pypi.python.org/packages/source/z/zope.app.wsgi/zope.app.wsgi-4.0.0a4.zip
  md5: 5cf346ac51886a88b476d47115a4a384
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zope.app.wsgi = zope.app.wsgi:main
    #
    # Would create an entry point called zope.app.wsgi that calls zope.app.wsgi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zconfig
    - transaction
    - zope.app.appsetup >=3.14
    - zope.processlifetime
    - zope.app.publication
    - zope.event
    - zope.interface
    - zope.publisher >=4.0.0a3
    - zope.security >4.0.0a2
    - zope.component
    - zope.configuration
    - zope.container >=4.0.0a1
    - zope.error
    - zope.lifecycleevent
    - zope.session >=4.0.0a1
    - zope.site >=4.0.0a1
    - zope.testing
    - zope.traversing >=4.0.0a1

  run:
    - python
    - setuptools
    - setuptools
    - zconfig
    - transaction
    - zope.app.appsetup >=3.14
    - zope.processlifetime
    - zope.app.publication
    - zope.event
    - zope.interface
    - zope.publisher >=4.0.0a3
    - zope.security >4.0.0a2
    - zope.component
    - zope.configuration
    - zope.container >=4.0.0a1
    - zope.error
    - zope.lifecycleevent
    - zope.session >=4.0.0a1
    - zope.site >=4.0.0a1
    - zope.testing
    - zope.traversing >=4.0.0a1

test:
  # Python imports
  imports:
    - zope
    - zope.app
    - zope.app.wsgi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/zope.app.wsgi
  license: Zope Public License
  summary: 'WSGI application for the zope.publisher'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
