package:
  name: collectd-csv
  version: "0.6.1"

source:
  fn: collectd-csv-0.6.1.tar.gz
  url: https://pypi.python.org/packages/source/c/collectd-csv/collectd-csv-0.6.1.tar.gz
  md5: 41e64d1e3e2e943c58a79516c661d9eb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collectd-csv = collectd-csv:main
    #
    # Would create an entry point called collectd-csv that calls collectd-csv.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - CollectD_CSV
    - CollectD_CSV/plugins

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/collectd-csv
  license: FreeBSD
  summary: 'Fetch collectd CSV data matching the given parameters.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
