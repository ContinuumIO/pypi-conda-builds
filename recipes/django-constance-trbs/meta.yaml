package:
  name: django-constance-trbs
  version: "0.7.4"

source:
  fn: django-constance-trbs-0.7.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-constance-trbs/django-constance-trbs-0.7.4.tar.gz
  md5: 0ba7eab9755a236d1db4b3f46b49421e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-constance-trbs = django-constance-trbs:main
    #
    # Would create an entry point called django-constance-trbs that calls django-constance-trbs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six

  run:
    - python
    - six

test:
  # Python imports
  imports:
    - constance
    - constance.backends
    - constance.backends.database
    - constance.backends.database.migrations
    - constance.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/trbs/django-constance-trbs
  license: BSD License
  summary: 'Django live settings with pluggable backends, including Redis.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
