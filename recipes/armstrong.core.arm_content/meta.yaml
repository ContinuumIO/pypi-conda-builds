package:
  name: armstrong.core.arm_content
  version: "1.3.5"

source:
  fn: armstrong.core.arm_content-1.3.5.tar.gz
  url: https://pypi.python.org/packages/source/a/armstrong.core.arm_content/armstrong.core.arm_content-1.3.5.tar.gz
  md5: b7a3ab9ecba0ce60c139b0add0e57b91
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - armstrong.core.arm_content = armstrong.core.arm_content:main
    #
    # Would create an entry point called armstrong.core.arm_content that calls armstrong.core.arm_content.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django ==1.3.1,<=1.4.5
    - django-model-utils ==0.6.0
    - django-reversion ==1.4
    - django-taggit ==0.9.3
    - sorl-thumbnail ==11.05.2
    - armstrong.core.arm_sections >=1.5.3,<2.0
    - armstrong.core.arm_access >=1.0.6,<2.0
    - armstrong.utils.backends >=1.0.0,<2.0

  run:
    - python
    - django ==1.3.1,<=1.4.5
    - django-model-utils ==0.6.0
    - django-reversion ==1.4
    - django-taggit ==0.9.3
    - sorl-thumbnail ==11.05.2
    - armstrong.core.arm_sections >=1.5.3,<2.0
    - armstrong.core.arm_access >=1.0.6,<2.0
    - armstrong.utils.backends >=1.0.0,<2.0

test:
  # Python imports
  imports:
    - armstrong.core.arm_content
    - armstrong.core.arm_content.admin
    - armstrong.core.arm_content.fields
    - armstrong.core.arm_content.fields.widgets
    - armstrong.core.arm_content.images
    - armstrong.core.arm_content.mixins
    - armstrong.core.arm_content.mixins.images
    - armstrong.core.arm_content.templatetags
    - armstrong.core.arm_content.tests
    - armstrong.core.arm_content.tests.admin
    - armstrong.core.arm_content.tests.arm_content_support
    - armstrong.core.arm_content.tests.fields
    - armstrong.core.arm_content.tests.images
    - armstrong.core.arm_content.tests.mixins
    - armstrong.core.arm_content.tests.models
    - armstrong.core.arm_content.tests.video
    - armstrong.core.arm_content.tests.video.backends
    - armstrong.core.arm_content.video
    - armstrong.core.arm_content.video.backends

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/armstrong/armstrong.core.arm_content/
  license: Apache Software License
  summary: 'A library for building news sites with multiple content types'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
