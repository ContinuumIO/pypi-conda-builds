package:
  name: django-remote-auth-ldap
  version: "0.1.4"

source:
  fn: django-remote-auth-ldap-0.1.4.zip
  url: https://pypi.python.org/packages/source/d/django-remote-auth-ldap/django-remote-auth-ldap-0.1.4.zip
  md5: 66eaebe19ee8ec00518330361ed2e1d4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-remote-auth-ldap = django-remote-auth-ldap:main
    #
    # Would create an entry point called django-remote-auth-ldap that calls django-remote-auth-ldap.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-auth-ldap
    - django-appconf

  run:
    - python
    - django-auth-ldap
    - django-appconf

test:
  # Python imports
  imports:
    - django_remote_auth_ldap

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/apmorton/django-remote-auth-ldap
  license: MIT License
  summary: 'REMOTE_USER authentication using LDAP'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
