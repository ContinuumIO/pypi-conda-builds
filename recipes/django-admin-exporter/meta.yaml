package:
  name: django-admin-exporter
  version: "0.2.1"

source:
  fn: django-admin-exporter-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-admin-exporter/django-admin-exporter-0.2.1.tar.gz
  md5: 577116cb0da5f66bde52c0bd886daabe
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-admin-exporter = django-admin-exporter:main
    #
    # Would create an entry point called django-admin-exporter that calls django-admin-exporter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - unicodecsv

  run:
    - python
    - unicodecsv

test:
  # Python imports
  imports:
    - admin_exporter

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/RyanBalfanz/django-admin-exporter
  license: UNKNOWN
  summary: 'Simple admin actions to download/export selected items in CSV, JSON, XML, etc.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
