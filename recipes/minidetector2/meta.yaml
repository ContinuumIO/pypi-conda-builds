package:
  name: minidetector2
  version: "0.0.3"

source:
  fn: minidetector2-0.0.3.zip
  url: https://pypi.python.org/packages/source/m/minidetector2/minidetector2-0.0.3.zip
  md5: 7391966d528cf82f54265e1d457a9127
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - minidetector2 = minidetector2:main
    #
    # Would create an entry point called minidetector2 that calls minidetector2.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - minidetector2
    - minidetector2.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: BSD License
  summary: 'minidetector2'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
