package:
  name: django-bbs
  version: "0.0.1"

source:
  fn: django-bbs-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-bbs/django-bbs-0.0.1.tar.gz
  md5: 4877a4566b59fcb2cf1b1dae0265ce1e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-bbs = django-bbs:main
    #
    # Would create an entry point called django-bbs that calls django-bbs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django ==1.6.6
    - django-tastypie ==0.12.1

  run:
    - python
    - django ==1.6.6
    - django-tastypie ==0.12.1

test:
  # Python imports
  imports:
    - auth
    - breadcrumbs
    - resource
    - templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://neohu.sinaapp.com
  license: BSD License
  summary: 'Django forum application'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
