package:
  name: mist.client
  version: "0.1.1"

source:
  fn: mist.client-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/m/mist.client/mist.client-0.1.1.tar.gz
  md5: a75358d29027675a0d280fea3442e764
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mist.client = mist.client:main
    #
    # Would create an entry point called mist.client that calls mist.client.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests
    - argparse
    - pyyaml
    - gitpython ==0.3.2.RC1
    - prettytable

  run:
    - python
    - requests
    - argparse
    - pyyaml
    - gitpython ==0.3.2.RC1
    - prettytable

test:
  # Python imports
  imports:
    - mist
    - mist.client
    - mist.cmd
    - mist.cmd.helpers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mistio/mist.client
  license: GNU Affero General Public License v3
  summary: 'Python client for mist.io'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
