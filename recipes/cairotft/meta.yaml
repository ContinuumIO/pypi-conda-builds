package:
  name: cairotft
  version: "0.1.2"

source:
  fn: cairotft-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/c/cairotft/cairotft-0.1.2.tar.gz
  md5: df2d6ea1fe62fbd6587f0a404b5bd296
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cairotft = cairotft:main
    #
    # Would create an entry point called cairotft that calls cairotft.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cairocffi
    - cairosvg

  run:
    - python
    - cairocffi
    - cairosvg

test:
  # Python imports
  imports:
    - build_scripts
    - cairotft
    - cairotft.tests
    - cairotft.widgets

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/LinkCareServices/cairotft
  license: BSD License
  summary: 'UI library for small tft screens using cairocffi'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
