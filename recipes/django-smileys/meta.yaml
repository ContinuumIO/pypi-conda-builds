package:
  name: django-smileys
  version: "0.1.1-pre1"

source:
  fn: django-smileys-0.1.1-pre1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-smileys/django-smileys-0.1.1-pre1.tar.gz
  md5: 9568d461ac6ff3a441a95cd1c2970c2e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-smileys = django-smileys:main
    #
    # Would create an entry point called django-smileys that calls django-smileys.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - smileys
    - smileys.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/codekoala/django-smileys/
  license: BSD License
  summary: 'Easily replace things like :) and B-) with smilies on your Django-powered site.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
