package:
  name: collective.ptg.nivogallery
  version: "1.1"

source:
  fn: collective.ptg.nivogallery-1.1.zip
  url: https://pypi.python.org/packages/source/c/collective.ptg.nivogallery/collective.ptg.nivogallery-1.1.zip
  md5: 3d8cc2f0b6cb4c957f646603edb05518
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.ptg.nivogallery = collective.ptg.nivogallery:main
    #
    # Would create an entry point called collective.ptg.nivogallery that calls collective.ptg.nivogallery.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.plonetruegallery

  run:
    - python
    - setuptools
    - setuptools
    - collective.plonetruegallery

test:
  # Python imports
  imports:
    - collective
    - collective.ptg
    - collective.ptg.nivogallery

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/vangheem/collective.ptg.nivogallery
  license: GPL
  summary: 'nivo gallery integration with collective.plonetruegallery'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
