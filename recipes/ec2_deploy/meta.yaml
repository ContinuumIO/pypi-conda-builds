package:
  name: ec2_deploy
  version: "0.1.1"

source:
  fn: ec2_deploy-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/e/ec2_deploy/ec2_deploy-0.1.1.tar.gz
  md5: 460a28e3006312f4190fe4ef2a298ab3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ec2_deploy = ec2_deploy:main
    #
    # Would create an entry point called ec2_deploy that calls ec2_deploy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fabric ==1.10.1
    - boto ==2.36.0
    - python-dotenv ==0.1.2
    - gitpython ==0.3.6

  run:
    - python
    - fabric ==1.10.1
    - boto ==2.36.0
    - python-dotenv ==0.1.2
    - gitpython ==0.3.6

test:
  # Python imports
  imports:
    - ec2_deploy
    - ec2_deploy.ec2

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/glynjackson/ec2-deploy
  license: The MIT License
  summary: 'EC2 Deploy is a convenient deployment tool to facilitate code deployment and other tasks to AWS EC2.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
