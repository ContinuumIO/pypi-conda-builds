package:
  name: mogrify
  version: "0.1"

source:
  fn: mogrify-0.1.tar.gz
  url: https://pypi.python.org/packages/source/m/mogrify/mogrify-0.1.tar.gz
  md5: 7d8caef654619f8d8b03083b5ddda0cc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mogrify = mogrify:main
    #
    # Would create an entry point called mogrify that calls mogrify.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - mogrify

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/ericsnowcurrently/odds_and_ends/src/default/mogrify
  license: BSD License
  summary: 'Expand parameter marker formatting, a la PEP 249.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
