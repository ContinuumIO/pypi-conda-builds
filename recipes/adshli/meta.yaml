package:
  name: adshli
  version: "0.3.1"

source:
  fn: ADSHLI-0.3.1.zip
  url: https://pypi.python.org/packages/source/a/adshli/ADSHLI-0.3.1.zip
  md5: 17bfa3980e235b41b7cf6e790fe8b9ef
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - adshli = adshli:main
    #
    # Would create an entry point called adshli that calls adshli.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - adshli

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/simonwaid/adshli
  license: GNU Lesser General Public License v3 (LGPLv3)
  summary: 'A high level client for the Twincat ADS/AMS protocol'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
