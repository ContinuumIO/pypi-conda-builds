package:
  name: django-rte
  version: "0.4.0"

source:
  fn: django-rte-0.4.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-rte/django-rte-0.4.0.tar.gz
  md5: 646e0d3a1794402da811ee7eef1d468a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-rte = django-rte:main
    #
    # Would create an entry point called django-rte that calls django-rte.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - rte

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/aino/django-rte
  license: BSD License
  summary: 'Rich text editor for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
