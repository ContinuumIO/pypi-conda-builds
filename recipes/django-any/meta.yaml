package:
  name: django-any
  version: "0.2.0"

source:
  fn: django-any-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-any/django-any-0.2.0.tar.gz
  md5: 11f194396e0888cf126d65858892d98f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-any = django-any:main
    #
    # Would create an entry point called django-any that calls django-any.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_any
    - django_any.contrib
    - django_any.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/kmmbvnr/django-any
  license: MIT License
  summary: 'Unobtrusive test models  creation for django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
