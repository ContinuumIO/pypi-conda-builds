package:
  name: django-tags-input
  version: "2.0.0"

source:
  fn: django-tags-input-2.0.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-tags-input/django-tags-input-2.0.0.tar.gz
  md5: 06f43dd42ff7460b39934e74e6369810
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-tags-input = django-tags-input:main
    #
    # Would create an entry point called django-tags-input that calls django-tags-input.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six

  run:
    - python
    - six

test:
  # Python imports
  imports:
    - tags_input

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/WoLpH/django-tags-input
  license: BSD License
  summary: 'Django jQuery Tags Input is a Django field and widget\nwrapper that adds an easy to use interface for `ManyToManyField` and\n`ForeignKey` fields in the forms of autocompleting tags with optionally live\ncreation of new linked items'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
