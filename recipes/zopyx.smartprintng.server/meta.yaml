package:
  name: zopyx.smartprintng.server
  version: "1.1.2"

source:
  fn: zopyx.smartprintng.server-1.1.2.zip
  url: https://pypi.python.org/packages/source/z/zopyx.smartprintng.server/zopyx.smartprintng.server-1.1.2.zip
  md5: 027e9491e6ea7085540277ea24e750f4
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zopyx.smartprintng.server = zopyx.smartprintng.server:main
    #
    # Would create an entry point called zopyx.smartprintng.server that calls zopyx.smartprintng.server.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - pastescript
    - pyramid
    - pyramid_zcml
    - pyramid_xmlrpc ==0.1
    - uuid
    - zopyx.convert2
    - zope.sendmail
    - transaction

  run:
    - python
    - setuptools
    - setuptools
    - pastescript
    - pyramid
    - pyramid_zcml
    - pyramid_xmlrpc ==0.1
    - uuid
    - zopyx.convert2
    - zope.sendmail
    - transaction

test:
  # Python imports
  imports:
    - zopyx
    - zopyx.smartprintng
    - zopyx.smartprintng.server

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.zopyx.com/projects/smartprintng
  license: ZPL
  summary: 'ZOPYX SmartPrintNG Server'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
