package:
  name: django-webodt
  version: "0.3.1"

source:
  fn: django-webodt-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-webodt/django-webodt-0.3.1.tar.gz
  md5: 945dccfdb015d954b722243fd6819936
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-webodt = django-webodt:main
    #
    # Would create an entry point called django-webodt that calls django-webodt.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django
    - lxml

  run:
    - python
    - django
    - lxml

test:
  # Python imports
  imports:
    - webodt
    - webodt.converters
    - webodt.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/netangels/django-webodt
  license: BSD License
  summary: u'ODF template handler and odt to html, pdf, doc, etc converter'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
