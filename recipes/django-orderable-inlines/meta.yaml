package:
  name: django-orderable-inlines
  version: "0.0.6"

source:
  fn: django-orderable-inlines-0.0.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-orderable-inlines/django-orderable-inlines-0.0.6.tar.gz
  md5: eff425903ca532e55d2c724f80c36fb1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-orderable-inlines = django-orderable-inlines:main
    #
    # Would create an entry point called django-orderable-inlines that calls django-orderable-inlines.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - orderable_inlines

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/elijahr/django-orderable-inlines
  license: django-ordered-listview
  summary: 'Drag-to-reorder inline model admins for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
