package:
  name: initdotpy
  version: "0.0.2"

source:
  fn: initdotpy-0.0.2.zip
  url: https://pypi.python.org/packages/source/i/initdotpy/initdotpy-0.0.2.zip
  md5: cf98c693c93b4364aeb1950adb66dad2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - initdotpy = initdotpy:main
    #
    # Would create an entry point called initdotpy that calls initdotpy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - initdotpy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/burrowsa/initdotpy
  license: BSD License
  summary: 'Automates imports in __init__.py file'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
