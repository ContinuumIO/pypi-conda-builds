package:
  name: flask-hopak
  version: "0.1.0"

source:
  fn: Flask-Hopak-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Hopak/Flask-Hopak-0.1.0.tar.gz
  md5: 4c017b5b25c4e89a3c9fd4696ac047af
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-hopak = flask-hopak:main
    #
    # Would create an entry point called flask-hopak that calls flask-hopak.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.9
    - hopak
    - flask-pymongo

  run:
    - python
    - flask >=0.9
    - hopak
    - flask-pymongo

test:
  # Python imports
  imports:
    - flask_hopak
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/xen/flask-hopak
  license: BSD License
  summary: 'Admin interface for Flask that uses Hopak models'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
