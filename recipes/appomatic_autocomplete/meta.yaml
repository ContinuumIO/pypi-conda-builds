package:
  name: appomatic_autocomplete
  version: "0.0.30"

source:
  fn: appomatic_autocomplete-0.0.30.tar.gz
  url: https://pypi.python.org/packages/source/a/appomatic_autocomplete/appomatic_autocomplete-0.0.30.tar.gz
  md5: 52162540f5f72fb0e80d929b9718aeae
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - appomatic_autocomplete = appomatic_autocomplete:main
    #
    # Would create an entry point called appomatic_autocomplete that calls appomatic_autocomplete.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django_autocomplete_foreignkey
    - appomaticcore ==0.0.30

  run:
    - python
    - django_autocomplete_foreignkey
    - appomaticcore ==0.0.30

test:
  # Python imports
  imports:
    - appomatic_autocomplete

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/redhog/django-autocomplete
  license: GPL
  summary: 'AutoComplete for ForeignKey and ManyToManyField.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
