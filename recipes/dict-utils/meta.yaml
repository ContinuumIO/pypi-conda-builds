package:
  name: dict-utils
  version: "0.1.2"

source:
  fn: dict-utils-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/dict-utils/dict-utils-0.1.2.tar.gz
  md5: fe1d188f7fcfae315f8d383e57d8b274
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dict-utils = dict-utils:main
    #
    # Would create an entry point called dict-utils that calls dict-utils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - dict_utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/glowdigitalmedia/dict-utils/
  license: UNKNOWN
  summary: 'Set of utilities and accessory methods to work with Python dicts.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
