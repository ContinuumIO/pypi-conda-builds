package:
  name: js.ember
  version: "1.0.0"

source:
  fn: js.ember-1.0.0.zip
  url: https://pypi.python.org/packages/source/j/js.ember/js.ember-1.0.0.zip
  md5: edf227138ae65b86492a1cc690e82d5c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - js.ember = js.ember:main
    #
    # Would create an entry point called js.ember that calls js.ember.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fanstatic
    - js.jquery >=1.9.1
    - setuptools

  run:
    - python
    - setuptools
    - fanstatic
    - js.jquery >=1.9.1
    - setuptools

test:
  # Python imports
  imports:
    - js
    - js.ember

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD
  summary: 'fanstatic ember.js.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
