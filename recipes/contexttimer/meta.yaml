package:
  name: contexttimer
  version: "0.3.1"

source:
  fn: contexttimer-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/c/contexttimer/contexttimer-0.3.1.tar.gz
  md5: ed2ff1bc552e27f92e63d3a33ce55e45
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - contexttimer = contexttimer:main
    #
    # Would create an entry point called contexttimer that calls contexttimer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - contexttimer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/brouberol/contexttimer
  license: GNU General Public License v3 (GPLv3)
  summary: 'A timer context manager measuring the clock wall time of the code block it contains.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
