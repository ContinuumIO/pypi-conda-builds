package:
  name: django-redactor
  version: "0.2"

source:
  fn: django-redactor-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-redactor/django-redactor-0.2.tar.gz
  md5: 695b28f21b66e72e8d7108e6092230d3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-redactor = django-redactor:main
    #
    # Would create an entry point called django-redactor that calls django-redactor.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - redactor

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/gearheart/django-redactor
  license: UNKNOWN
  summary: 'Django application for http://imperavi.ru/redactor/'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
