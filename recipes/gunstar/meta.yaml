package:
  name: gunstar
  version: "0.2.2"

source:
  fn: gunstar-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/g/gunstar/gunstar-0.2.2.tar.gz
  md5: c2d4dfe7df723af43ad6db37b82f9e22
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gunstar = gunstar:main
    #
    # Would create an entry point called gunstar that calls gunstar.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - webob >=1.2.3
    - blinker >=1.3
    - jinja2 >=2.7.1
    - six >=1.3.0
    - itsdangerous >=0.23

  run:
    - python
    - webob >=1.2.3
    - blinker >=1.3
    - jinja2 >=2.7.1
    - six >=1.3.0
    - itsdangerous >=0.23

test:
  # Python imports
  imports:
    - gunstar

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/allisson/gunstar
  license: MIT License
  summary: 'Another python web framework.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
