package:
  name: hockeylib
  version: "1.0.0"

source:
  fn: hockeylib-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/h/hockeylib/hockeylib-1.0.0.tar.gz
  md5: 988c4162cee895cb6a764c2d5a9b28b3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hockeylib = hockeylib:main
    #
    # Would create an entry point called hockeylib that calls hockeylib.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pycurl ==7.19.3.1
    - logging ==0.4.9.6

  run:
    - python
    - pycurl ==7.19.3.1
    - logging ==0.4.9.6

test:
  # Python imports
  imports:
    - hockeylib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD
  summary: 'A simple collection of hockey app api tools'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
