package:
  name: collective.z3cform.mapwidget
  version: "2.1"

source:
  fn: collective.z3cform.mapwidget-2.1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.z3cform.mapwidget/collective.z3cform.mapwidget-2.1.tar.gz
  md5: 4f94b787d674ce3c4ad71cd9a15c1734
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.z3cform.mapwidget = collective.z3cform.mapwidget:main
    #
    # Would create an entry point called collective.z3cform.mapwidget that calls collective.z3cform.mapwidget.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.geo.mapwidget

  run:
    - python
    - setuptools
    - setuptools
    - collective.geo.mapwidget

test:
  # Python imports
  imports:
    - collective
    - collective.z3cform
    - collective.z3cform.mapwidget
    - collective.z3cform.mapwidget.demo

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.coactivate.org/projects/collectivegeo/project-home
  license: GPL
  summary: 'z3c.form map widget'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
