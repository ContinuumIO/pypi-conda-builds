package:
  name: armorc
  version: "0.1.30"

source:
  fn: armorc-0.1.30.zip
  url: https://pypi.python.org/packages/source/a/armorc/armorc-0.1.30.zip
  md5: 989df2a47b0b282d4525aaccf43685aa
#  patches:
   # List any patch files here
   # - fix.patch

build:
  # preserve_egg_dir: True
  entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - armorc = armorc:main
    #
    # Would create an entry point called armorc that calls armorc.main()

    - armorc = armorc.run:main

  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - armor_api

  run:
    - python
    - armor_api

test:
  # Python imports
  imports:
    - armorc

  commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.

    - armorc --help

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://scudum.hive.pt
  license: GNU General Public License (GPL)
  summary: 'Armor Client'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
