package:
  name: autobreadcrumbs
  version: "1.0"

source:
  fn: autobreadcrumbs-1.0.tar.gz
  url: https://pypi.python.org/packages/source/a/autobreadcrumbs/autobreadcrumbs-1.0.tar.gz
  md5: 8c75200c14cf1df59b3ec27b09145cb1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - autobreadcrumbs = autobreadcrumbs:main
    #
    # Would create an entry point called autobreadcrumbs that calls autobreadcrumbs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5

  run:
    - python
    - django >=1.5

test:
  # Python imports
  imports:
    - autobreadcrumbs
    - autobreadcrumbs.management
    - autobreadcrumbs.management.commands
    - autobreadcrumbs.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/autobreadcrumbs
  license: MIT License
  summary: '\nDjango application to add breadcrumbs to your pages\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
