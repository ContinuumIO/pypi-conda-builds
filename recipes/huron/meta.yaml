package:
  name: huron
  version: "0.5.4"

source:
  fn: huron-0.5.4.tar.gz
  url: https://pypi.python.org/packages/source/h/huron/huron-0.5.4.tar.gz
  md5: e128067897574fbf0d7d3d324a43fd16
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - huron = huron:main
    #
    # Would create an entry point called huron that calls huron.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - huron
    - huron.blog
    - huron.contact_form
    - huron.menus_manager
    - huron.pages
    - huron.settings
    - huron.settings.templatetags
    - huron.simple_medias_manager
    - huron.simple_news
    - huron.slider
    - huron.utils
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/tominardi/huron
  license: BSD
  summary: 'Tools for Django websites'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
