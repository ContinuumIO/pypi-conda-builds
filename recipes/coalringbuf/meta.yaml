package:
  name: coalringbuf
  version: "0.1.0"

source:
  fn: coalringbuf-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/c/coalringbuf/coalringbuf-0.1.0.tar.gz
  md5: 5f914b94150b9716e9436378d4b1e336
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - coalringbuf = coalringbuf:main
    #
    # Would create an entry point called coalringbuf that calls coalringbuf.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six >=1.3.0

  run:
    - python
    - six >=1.3.0

test:
  # Python imports
  imports:
    - coalringbuf

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jstasiak/coalringbuf
  license: MIT License
  summary: 'Python port of CoalescingRingBuffer from LMAXCollections'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
