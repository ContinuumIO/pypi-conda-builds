package:
  name: collective.contact.widget
  version: "1.2.2"

source:
  fn: collective.contact.widget-1.2.2.zip
  url: https://pypi.python.org/packages/source/c/collective.contact.widget/collective.contact.widget-1.2.2.zip
  md5: cee4ca9db38c28b7dcd98611fa3197a3
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.contact.widget = collective.contact.widget:main
    #
    # Would create an entry point called collective.contact.widget that calls collective.contact.widget.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - five.grok
    - setuptools
    - plone.formwidget.contenttree
    - plone.formwidget.autocomplete
    - z3c.relationfield

  run:
    - python
    - setuptools
    - five.grok
    - setuptools
    - plone.formwidget.contenttree
    - plone.formwidget.autocomplete
    - z3c.relationfield

test:
  # Python imports
  imports:
    - collective
    - collective.contact
    - collective.contact.widget

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/collective.contact.widget
  license: GPL
  summary: 'Contact widget'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
