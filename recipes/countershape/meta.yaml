package:
  name: countershape
  version: "0.1"

source:
  fn: countershape-0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/countershape/countershape-0.1.tar.gz
  md5: 26a68450b1a2c526c73110fe4eb3b6a4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - countershape = countershape:main
    #
    # Would create an entry point called countershape that calls countershape.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - tinytree
    - cubictemp
    - docutils
    - markdown2
    - pygments

  run:
    - python
    - tinytree
    - cubictemp
    - docutils
    - markdown2
    - pygments

test:
  # Python imports
  imports:
    - countershape

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://dev.nullcube.com
  license: UNKNOWN
  summary: 'A framework for rendering static documentation.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
