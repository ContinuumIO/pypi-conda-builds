package:
  name: sphinxjp.themes.tinkerbelizehole
  version: "0.1.95"

source:
  fn: sphinxjp.themes.tinkerbelizehole-0.1.95.tar.gz
  url: https://pypi.python.org/packages/source/s/sphinxjp.themes.tinkerbelizehole/sphinxjp.themes.tinkerbelizehole-0.1.95.tar.gz
  md5: 32dd2ee1e00f5c1cf37685068f74dd4c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sphinxjp.themes.tinkerbelizehole = sphinxjp.themes.tinkerbelizehole:main
    #
    # Would create an entry point called sphinxjp.themes.tinkerbelizehole that calls sphinxjp.themes.tinkerbelizehole.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - docutils
    - tinkerer
    - sphinxjp.themecore
    - pygments-style-solarized

  run:
    - python
    - setuptools
    - setuptools
    - docutils
    - tinkerer
    - sphinxjp.themecore
    - pygments-style-solarized

test:
  # Python imports
  imports:
    - sphinxjp
    - sphinxjp.themes
    - sphinxjp.themes.tinkerbelizehole

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/naoiwata/sphinxjp.themes.tinkerbelizehole
  license: MIT License
  summary: 'A single column blogging theme tool for Tinkerer, based on belizehole color.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
