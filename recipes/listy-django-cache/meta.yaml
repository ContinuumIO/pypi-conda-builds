package:
  name: listy-django-cache
  version: "0.9.0"

source:
  fn: listy-django-cache-0.9.0.tar.gz
  url: https://pypi.python.org/packages/source/l/listy-django-cache/listy-django-cache-0.9.0.tar.gz
  md5: 82c13e5c76458a78578b5877b13b96ba
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - listy-django-cache = listy-django-cache:main
    #
    # Would create an entry point called listy-django-cache that calls listy-django-cache.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.2,<1.3
    - python-dateutil >=1.5
    - python-memcached >=1.45

  run:
    - python
    - django >=1.2,<1.3
    - python-dateutil >=1.5
    - python-memcached >=1.45

test:
  # Python imports
  imports:
    - listy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/wavii/listy-django-cache
  license: MIT License
  summary: 'A deterministic caching mechanism for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
