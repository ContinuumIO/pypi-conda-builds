package:
  name: ftw.publisher.sender
  version: "2.2.2"

source:
  fn: ftw.publisher.sender-2.2.2.zip
  url: https://pypi.python.org/packages/source/f/ftw.publisher.sender/ftw.publisher.sender-2.2.2.zip
  md5: a50b5f10715c5e338c88e261545b1798
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ftw.publisher.sender = ftw.publisher.sender:main
    #
    # Would create an entry point called ftw.publisher.sender that calls ftw.publisher.sender.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - accesscontrol
    - acquisition
    - zexceptions
    - transaction
    - zope.annotation
    - zope.component
    - zope.event
    - zope.i18nmessageid
    - zope.interface
    - zope.publisher
    - zope.schema
    - zope.viewlet
    - zodb3
    - zope2
    - plone.memoize
    - products.zcatalog
    - products.statusmessages
    - products.cmfcore
    - products.cmfplone
    - ftw.publisher.core
    - ftw.table
    - ftw.upgrade
    - z3c.form
    - plone.z3cform

  run:
    - python
    - setuptools
    - setuptools
    - accesscontrol
    - acquisition
    - zexceptions
    - transaction
    - zope.annotation
    - zope.component
    - zope.event
    - zope.i18nmessageid
    - zope.interface
    - zope.publisher
    - zope.schema
    - zope.viewlet
    - zodb3
    - zope2
    - plone.memoize
    - products.zcatalog
    - products.statusmessages
    - products.cmfcore
    - products.cmfplone
    - ftw.publisher.core
    - ftw.table
    - ftw.upgrade
    - z3c.form
    - plone.z3cform

test:
  # Python imports
  imports:
    - ftw
    - ftw.publisher
    - ftw.publisher.sender
    - ftw.publisher.sender.browser
    - ftw.publisher.sender.notifier
    - ftw.publisher.sender.tests
    - ftw.publisher.sender.upgrades
    - ftw.publisher.sender.viewlets
    - ftw.publisher.sender.workflows

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/4teamwork/ftw.publisher.sender
  license: GPL2
  summary: 'Staging and publishing addon for Plone contents.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
