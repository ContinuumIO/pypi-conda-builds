package:
  name: attr_ns
  version: "0.3"

source:
  fn: attr_ns-0.3.tar.gz
  url: https://pypi.python.org/packages/source/a/attr_ns/attr_ns-0.3.tar.gz
  md5: 1e2a7da4765746ef1ce89cfd7f93927d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - attr_ns = attr_ns:main
    #
    # Would create an entry point called attr_ns that calls attr_ns.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - attr_ns
    - attr_ns.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/ericsnowcurrently/odds_and_ends/src/default/ns
  license: BSD License
  summary: 'A collection of tools relating to namespaces.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
