package:
  name: crazyhash
  version: "0.0.2"

source:
  fn: crazyhash-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/c/crazyhash/crazyhash-0.0.2.tar.gz
  md5: d26c2ef6cdf7a2322239eb652bdea81f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - crazyhash = crazyhash:main
    #
    # Would create an entry point called crazyhash that calls crazyhash.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - crazyhash

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/buguroo/crazyhash
  license: GNU Lesser General Public License v3 (LGPLv3)
  summary: u'Natural language hashing library.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
