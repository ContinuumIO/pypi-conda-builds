package:
  name: jinja2-highlight
  version: "0.6.1"

source:
  fn: jinja2-highlight-0.6.1.tar.gz
  url: https://pypi.python.org/packages/source/j/jinja2-highlight/jinja2-highlight-0.6.1.tar.gz
  md5: 2efcc13889683d79bb08f9332d9091eb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jinja2-highlight = jinja2-highlight:main
    #
    # Would create an entry point called jinja2-highlight that calls jinja2-highlight.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - jinja2 >=2.4
    - pygments >=1.5

  run:
    - python
    - jinja2 >=2.4
    - pygments >=1.5

test:
  # Python imports
  imports:
    - jinja2_highlight

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tlatsas/jinja2-highlight
  license: GNU General Public License v3 (GPLv3)
  summary: 'Jinja2 extension to highlight source code using Pygments'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
