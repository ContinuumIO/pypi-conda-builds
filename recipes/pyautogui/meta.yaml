package:
  name: pyautogui
  version: "0.9.26"

source:
  fn: PyAutoGUI-0.9.26.zip
  url: https://pypi.python.org/packages/source/P/PyAutoGUI/PyAutoGUI-0.9.26.zip
  md5: 9c7d1b0aba4b14d2d67a748463f9b6d6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pyautogui = pyautogui:main
    #
    # Would create an entry point called pyautogui that calls pyautogui.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pymsgbox
    - pytweening >=1.0.1
    - pillow
    - pyscreeze

  run:
    - python
    - pymsgbox
    - pytweening >=1.0.1
    - pillow
    - pyscreeze

test:
  # Python imports
  imports:
    - pyautogui

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/asweigart/pyautogui
  license: BSD License
  summary: 'A cross-platform module for GUI automation for human beings. Control the keyboard and mouse from a Python script.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
