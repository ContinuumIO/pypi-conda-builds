package:
  name: django-csv
  version: "1.02"

source:
  fn: django-csv-1.02.tar.gz
  url: https://pypi.python.org/packages/source/d/django-csv/django-csv-1.02.tar.gz
  md5: 59d5e5ed1369e77f9008dd17b9eedadf
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-csv = django-csv:main
    #
    # Would create an entry point called django-csv that calls django-csv.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/steinbachr/django-csv
  license: UNKNOWN
  summary: 'Some CSV Utilities for your django projects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
