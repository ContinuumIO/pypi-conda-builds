package:
  name: moose
  version: "3.0"

source:
  fn: moose-3.0.tar.gz
  url: https://pypi.python.org/packages/source/m/moose/moose-3.0.tar.gz
  md5: eca871fb643da7bbc2a1edf7e4061f5c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - moose = moose:main
    #
    # Would create an entry point called moose that calls moose.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - moose

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://moose.ncbs.res.in/
  license: LGPL
  summary: 'MOOSE is the Multiscale Object-Oriented Simulation Environment. It is the base and numerical core for large, detailed simulations including Computational Neuroscience and Systems Biology.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
