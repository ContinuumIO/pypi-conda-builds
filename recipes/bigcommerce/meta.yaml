package:
  name: bigcommerce
  version: "0.11.0"

source:
  fn: bigcommerce-0.11.0.tar.gz
  url: https://pypi.python.org/packages/source/b/bigcommerce/bigcommerce-0.11.0.tar.gz
  md5: 8cc475a80f2615cf98c89b05fb669308
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bigcommerce = bigcommerce:main
    #
    # Would create an entry point called bigcommerce that calls bigcommerce.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=2.1.0
    - streql >=3.0.2

  run:
    - python
    - requests >=2.1.0
    - streql >=3.0.2

test:
  # Python imports
  imports:
    - bigcommerce
    - bigcommerce.resources
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bigcommerce/bigcommerce-api-python
  license: MIT License
  summary: 'Connect Python applications with the Bigcommerce API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
