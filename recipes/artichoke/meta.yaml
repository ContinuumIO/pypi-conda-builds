package:
  name: artichoke
  version: "0.4.3"

source:
  fn: artichoke-0.4.3.tar.gz
  url: https://pypi.python.org/packages/source/a/artichoke/artichoke-0.4.3.tar.gz
  md5: 8f183cdc9a8b0bd5f943c78f70490b5a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - artichoke = artichoke:main
    #
    # Would create an entry point called artichoke that calls artichoke.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - webob

  run:
    - python
    - setuptools
    - webob

test:
  # Python imports
  imports:
    - artichoke

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.axantlabs.com/artichoke
  license: GNU General Public License (GPL)
  summary: 'Simple and Lightweight WSGI Web Framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
