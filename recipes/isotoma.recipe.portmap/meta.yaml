package:
  name: isotoma.recipe.portmap
  version: "0.0.2"

source:
  fn: isotoma.recipe.portmap-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/i/isotoma.recipe.portmap/isotoma.recipe.portmap-0.0.2.tar.gz
  md5: c017e94e2b35aea5492962e596ceef1b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - isotoma.recipe.portmap = isotoma.recipe.portmap:main
    #
    # Would create an entry point called isotoma.recipe.portmap that calls isotoma.recipe.portmap.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout

test:
  # Python imports
  imports:
    - isotoma
    - isotoma.recipe
    - isotoma.recipe.portmap

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/isotoma.recipe.portmap
  license: Apache Software License
  summary: 'Simple way of configuring port numbers as offsets from a base, for use in buildout scripts.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
