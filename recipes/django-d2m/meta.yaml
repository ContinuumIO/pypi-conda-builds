package:
  name: django-d2m
  version: "0.1.4"

source:
  fn: django-d2m-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-d2m/django-d2m-0.1.4.tar.gz
  md5: 535e3b1399ecbb63a33ea289e51898fa
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-d2m = django-d2m:main
    #
    # Would create an entry point called django-d2m that calls django-d2m.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - six

  run:
    - python
    - django
    - six

test:
  # Python imports
  imports:
    - django_d2m
    - django_d2m.testapp

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lqez/django-d2m
  license: MIT License
  summary: 'Mapping annotated dict list into Django models'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
