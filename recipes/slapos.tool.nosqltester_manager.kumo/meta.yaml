package:
  name: slapos.tool.nosqltester_manager.kumo
  version: "0.0.1.dev-r45982"

source:
  fn: slapos.tool.nosqltester_manager.kumo-0.0.1.dev-r45982.tar.gz
  url: https://pypi.python.org/packages/source/s/slapos.tool.nosqltester_manager.kumo/slapos.tool.nosqltester_manager.kumo-0.0.1.dev-r45982.tar.gz
  md5: aef7ff5a5b05408864f71f4f3cae4adf
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - slapos.tool.nosqltester_manager.kumo = slapos.tool.nosqltester_manager.kumo:main
    #
    # Would create an entry point called slapos.tool.nosqltester_manager.kumo that calls slapos.tool.nosqltester_manager.kumo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - flask
    - slapos.tool.nosqltester_manager

  run:
    - python
    - setuptools
    - setuptools
    - flask
    - slapos.tool.nosqltester_manager

test:
  # Python imports
  imports:
    - slapos
    - slapos.tool
    - slapos.tool.nosqltester_manager
    - slapos.tool.nosqltester_manager.kumo

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: GPL
  summary: 'Manager for Kumofs tester'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
