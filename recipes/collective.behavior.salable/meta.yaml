package:
  name: collective.behavior.salable
  version: "0.5"

source:
  fn: collective.behavior.salable-0.5.zip
  url: https://pypi.python.org/packages/source/c/collective.behavior.salable/collective.behavior.salable-0.5.zip
  md5: b6046504c04ec8d09b69e14f90b37baf
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.behavior.salable = collective.behavior.salable:main
    #
    # Would create an entry point called collective.behavior.salable that calls collective.behavior.salable.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - plone.behavior
    - setuptools

  run:
    - python
    - setuptools
    - plone.behavior
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.behavior
    - collective.behavior.salable
    - collective.behavior.salable.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.behavior.salable/
  license: BSD License
  summary: 'Behavior to make content salable.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
