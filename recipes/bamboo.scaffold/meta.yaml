package:
  name: bamboo.scaffold
  version: "0.0.7"

source:
  fn: bamboo.scaffold-0.0.7.tar.gz
  url: https://pypi.python.org/packages/source/b/bamboo.scaffold/bamboo.scaffold-0.0.7.tar.gz
  md5: 962d9462c30e49e70cc41b61b9dbdab9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bamboo.scaffold = bamboo.scaffold:main
    #
    # Would create an entry point called bamboo.scaffold that calls bamboo.scaffold.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - fabric >=1.4.0
    - requests
    - nose
    - rhaptos2.common >=0.0.13
    - virtualenv
    - jenkinsapi

  run:
    - python
    - fabric >=1.4.0
    - requests
    - nose
    - rhaptos2.common >=0.0.13
    - virtualenv
    - jenkinsapi

test:
  # Python imports
  imports:
    - bamboo.scaffold

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Connexions/rhaptos2.repo
  license: LICENSE.txt
  summary: 'Misc tools supporting build and deployment, usually via jenkins'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
