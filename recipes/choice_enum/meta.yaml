package:
  name: choice_enum
  version: "0.2.1"

source:
  fn: choice_enum-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/c/choice_enum/choice_enum-0.2.1.tar.gz
  md5: f2e956201ce47a13c999fb39f79607df
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - choice_enum = choice_enum:main
    #
    # Would create an entry point called choice_enum that calls choice_enum.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - choice_enum

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dakrauth/choice_enum
  license: MIT License
  summary: 'Wrapper class for defining DRY, encapsulated choice options for CharFields.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
