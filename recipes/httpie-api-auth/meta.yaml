package:
  name: httpie-api-auth
  version: "0.2.0"

source:
  fn: httpie-api-auth-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/h/httpie-api-auth/httpie-api-auth-0.2.0.tar.gz
  md5: fdaa00bc53ebe4af8e88463193ce339a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - httpie-api-auth = httpie-api-auth:main
    #
    # Would create an entry point called httpie-api-auth that calls httpie-api-auth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - httpie >=0.7.0

  run:
    - python
    - setuptools
    - httpie >=0.7.0

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/pd/httpie-api-auth
  license: MIT License
  summary: 'ApiAuth plugin for HTTPie.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
