package:
  name: arprequest
  version: "0.3"

source:
  fn: arprequest-0.3.tar.gz
  url: https://pypi.python.org/packages/source/a/arprequest/arprequest-0.3.tar.gz
  md5: d9306278d5695fa840167740a96bf70b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - arprequest = arprequest:main
    #
    # Would create an entry point called arprequest that calls arprequest.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - arprequest

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://idevelop.org/p/labo/page/ArpRequest/
  license: WTFPL
  summary: 'A class which sends an ARP Request to know if a host is online on local networks'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
