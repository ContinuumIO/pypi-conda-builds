package:
  name: bbqsql
  version: "1.2"

source:
  fn: BBQSQL-1.2.tar.gz
  url: https://pypi.python.org/packages/source/B/BBQSQL/BBQSQL-1.2.tar.gz
  md5: b69c73475879f0016add5714957efe0a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bbqsql = bbqsql:main
    #
    # Would create an entry point called bbqsql that calls bbqsql.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - gevent
    - requests <1.0
    - argparse

  run:
    - python
    - gevent
    - requests <1.0
    - argparse

test:
  # Python imports
  imports:
    - bbqsql
    - bbqsql.lib
    - bbqsql.menu

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/neohapsis/bbqsql
  license: BSD
  summary: 'Rapid Blind SQL Injection Exploitation Tool'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
