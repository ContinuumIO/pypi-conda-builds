package:
  name: plonetheme.culturecab
  version: "0.4"

source:
  fn: plonetheme.cultureCab-0.4.tar.gz
  url: https://pypi.python.org/packages/source/p/plonetheme.cultureCab/plonetheme.cultureCab-0.4.tar.gz
  md5: 3c298aab9d4b53e60b4f56e03a994738
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plonetheme.culturecab = plonetheme.culturecab:main
    #
    # Would create an entry point called plonetheme.culturecab that calls plonetheme.culturecab.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - plonetheme
    - plonetheme.cultureCab
    - plonetheme.cultureCab.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/
  license: GPL
  summary: 'An installable theme for Plone 3'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
