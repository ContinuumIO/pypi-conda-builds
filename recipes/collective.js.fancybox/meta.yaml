package:
  name: collective.js.fancybox
  version: "1.3.4.1"

source:
  fn: collective.js.fancybox-1.3.4.1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.js.fancybox/collective.js.fancybox-1.3.4.1.tar.gz
  md5: b09ac4211dedcffd19067d285ca401db
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.js.fancybox = collective.js.fancybox:main
    #
    # Would create an entry point called collective.js.fancybox that calls collective.js.fancybox.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.js
    - collective.js.fancybox

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/collective.js.fancybox
  license: GPL
  summary: 'Fancybox libraries for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
