package:
  name: django-optimizations
  version: "1.0.4"

source:
  fn: django-optimizations-1.0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-optimizations/django-optimizations-1.0.4.tar.gz
  md5: 6ac182036ad6dfa70051e9388be30a62
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-optimizations = django-optimizations:main
    #
    # Would create an entry point called django-optimizations that calls django-optimizations.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - optimizations
    - optimizations.management
    - optimizations.management.commands
    - optimizations.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/etianen/django-optimizations
  license: BSD License
  summary: 'A utility library for Django aimed at improving website performance.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
