package:
  name: collective.braveportletsmanager
  version: "1.0.1"

source:
  fn: collective.braveportletsmanager-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.braveportletsmanager/collective.braveportletsmanager-1.0.1.tar.gz
  md5: d61f9239c01892a9aeeea96b3052099e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.braveportletsmanager = collective.braveportletsmanager:main
    #
    # Would create an entry point called collective.braveportletsmanager that calls collective.braveportletsmanager.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.braveportletsmanager
    - collective.braveportletsmanager.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/softformance/collective.braveportletsmanager
  license: GPL
  summary: "Custom Plone Portlets Manager that doesn't break even with broken or not available portlet assignments"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
