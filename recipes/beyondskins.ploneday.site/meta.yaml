package:
  name: beyondskins.ploneday.site
  version: "1.0.2"

source:
  fn: beyondskins.ploneday.site-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/b/beyondskins.ploneday.site/beyondskins.ploneday.site-1.0.2.tar.gz
  md5: eec98fde5a8a6ea530e352de8efe6042
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - beyondskins.ploneday.site = beyondskins.ploneday.site:main
    #
    # Would create an entry point called beyondskins.ploneday.site that calls beyondskins.ploneday.site.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - beyondskins
    - beyondskins.ploneday
    - beyondskins.ploneday.site
    - beyondskins.ploneday.site.browser
    - beyondskins.ploneday.site.doc

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.simplesconsultoria.com.br/
  license: GPL
  summary: 'Installable Plone 3 theme for World Plone Day'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
