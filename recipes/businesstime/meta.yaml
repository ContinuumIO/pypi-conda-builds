package:
  name: businesstime
  version: "0.1.5"

source:
  fn: businesstime-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/b/businesstime/businesstime-0.1.5.tar.gz
  md5: 0bf950d6ff2b4e6a53233757a80ef193
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - businesstime = businesstime:main
    #
    # Would create an entry point called businesstime that calls businesstime.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - businesstime

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/seatgeek/businesstime
  license: business_tools
  summary: 'A simple utility for calculating business time aware timedeltas between two datetimes'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
