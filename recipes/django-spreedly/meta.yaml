package:
  name: django-spreedly
  version: "2.0.9"

source:
  fn: django-spreedly-2.0.9.tar.gz
  url: https://pypi.python.org/packages/source/d/django-spreedly/django-spreedly-2.0.9.tar.gz
  md5: 2a309301af7b3faf6f64d8be7a55a8dc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-spreedly = django-spreedly:main
    #
    # Would create an entry point called django-spreedly that calls django-spreedly.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyspreedly ==2.0.1

  run:
    - python
    - pyspreedly ==2.0.1

test:
  # Python imports
  imports:
    - spreedly
    - spreedly.migrations
    - spreedly.templatetags
    - spreedly.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: www.github.com/mediapop/django-spreedly
  license: MIT License
  summary: 'django-spreedly integrates support for the spreedly subscription service'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
