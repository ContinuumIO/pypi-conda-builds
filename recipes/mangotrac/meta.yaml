package:
  name: mangotrac
  version: "0.6"

source:
  fn: MangoTrac-0.6.tar.gz
  url: https://pypi.python.org/packages/source/M/MangoTrac/MangoTrac-0.6.tar.gz
  md5: 5e213fbcc8cda7f7050d151737b30654
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mangotrac = mangotrac:main
    #
    # Would create an entry point called mangotrac that calls mangotrac.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - south
    - markdown

  run:
    - python
    - django
    - south
    - markdown

test:
  # Python imports
  imports:
    - mangotrac

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/akulakov/mangotrac
  license: MIT License
  summary: 'Issue Tracker based on the Django admin app'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
