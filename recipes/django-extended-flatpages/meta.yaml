package:
  name: django-extended-flatpages
  version: "0.1"

source:
  fn: django-extended-flatpages-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-extended-flatpages/django-extended-flatpages-0.1.tar.gz
  md5: 4fdc1d1218de15a6c07c4c2abdebcfaf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-extended-flatpages = django-extended-flatpages:main
    #
    # Would create an entry point called django-extended-flatpages that calls django-extended-flatpages.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-ckeditor

  run:
    - python
    - django-ckeditor

test:
  # Python imports
  imports:
    - extended_flatpages

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/hisie/django_extended_flatpages
  license: BSD License
  summary: 'Django flatpages CKEditor integration and SEO values.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
