package:
  name: django-modelhint
  version: "0.1.1.1"

source:
  fn: django-modelhint-0.1.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-modelhint/django-modelhint-0.1.1.1.tar.gz
  md5: 8a16e17d200dbdfed3f8c72a8564a23a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-modelhint = django-modelhint:main
    #
    # Would create an entry point called django-modelhint that calls django-modelhint.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - singledispatch

  run:
    - python
    - setuptools
    - django
    - singledispatch

test:
  # Python imports
  imports:
    - django_modelhint
    - django_modelhint.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/podhmo/django-modelhint
  license: UNKNOWN
  summary: 'add annotations at django models fields'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
