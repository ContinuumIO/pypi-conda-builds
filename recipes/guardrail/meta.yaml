package:
  name: guardrail
  version: "0.1.1"

source:
  fn: guardrail-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/g/guardrail/guardrail-0.1.1.tar.gz
  md5: 14177c9e583b2a715eb50e7e975c2a83
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - guardrail = guardrail:main
    #
    # Would create an entry point called guardrail that calls guardrail.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six

  run:
    - python
    - six

test:
  # Python imports
  imports:
    - guardrail
    - guardrail.core
    - guardrail.ext
    - guardrail.ext.django

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jmcarp/guardrail
  license: MIT License
  summary: 'An extensible library for managing object-level permissions with support for SQLAlchemy, Peewee, Pony, and Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
