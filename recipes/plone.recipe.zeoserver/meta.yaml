package:
  name: plone.recipe.zeoserver
  version: "1.2.8"

source:
  fn: plone.recipe.zeoserver-1.2.8.tar.gz
  url: https://pypi.python.org/packages/source/p/plone.recipe.zeoserver/plone.recipe.zeoserver-1.2.8.tar.gz
  md5: ccdea86ff12336ca108b4bede1fd9997
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.recipe.zeoserver = plone.recipe.zeoserver:main
    #
    # Would create an entry point called plone.recipe.zeoserver that calls plone.recipe.zeoserver.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zc.buildout
    - setuptools
    - zc.recipe.egg
    - zope.mkzeoinstance
    - zodb3 >=3.8
    - zopeundo

  run:
    - python
    - setuptools
    - zc.buildout
    - setuptools
    - zc.recipe.egg
    - zope.mkzeoinstance
    - zodb3 >=3.8
    - zopeundo

test:
  # Python imports
  imports:
    - plone
    - plone.recipe
    - plone.recipe.zeoserver
    - plone.recipe.zeoserver.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/plone.recipe.zeoserver
  license: Zope Public License
  summary: 'ZC Buildout recipe for installing a ZEO server'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
