package:
  name: django-awesome-avatar
  version: "1.1.3"

source:
  fn: django-awesome-avatar-1.1.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-awesome-avatar/django-awesome-avatar-1.1.3.tar.gz
  md5: 1a3d7db58fe03c7be553d9e778d2b627
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-awesome-avatar = django-awesome-avatar:main
    #
    # Would create an entry point called django-awesome-avatar that calls django-awesome-avatar.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - awesome_avatar

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dimka665/django-awesome-avatar
  license: BSD
  summary: 'Django Avatar field'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
