package:
  name: fmiweather
  version: "0.0.2"

source:
  fn: fmiweather-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/f/fmiweather/fmiweather-0.0.2.tar.gz
  md5: d22c2a0b965afed28e526116a49d5083
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fmiweather = fmiweather:main
    #
    # Would create an entry point called fmiweather that calls fmiweather.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests
    - beautifulsoup4

  run:
    - python
    - requests
    - beautifulsoup4

test:
  # Python imports
  imports:
    - fmiweather

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ojarva/python-fmi-weather
  license: BSD License
  summary: 'Fetches and parses weather information from fmi.fi'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
