package:
  name: fookebox
  version: "0.7.2"

source:
  fn: fookebox-0.7.2.tar.gz
  url: https://pypi.python.org/packages/source/f/fookebox/fookebox-0.7.2.tar.gz
  md5: 64d69d683d2381144111ce32dc97dbcf
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fookebox = fookebox:main
    #
    # Would create an entry point called fookebox that calls fookebox.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pylons >=0.10
    - python-mpd

  run:
    - python
    - setuptools
    - pylons >=0.10
    - python-mpd

test:
  # Python imports
  imports:
    - fookebox
    - fookebox.config
    - fookebox.controllers
    - fookebox.lib
    - fookebox.model
    - fookebox.tests
    - fookebox.tests.functional

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/cockroach/fookebox/
  license: GNU General Public License (GPL)
  summary: 'A jukebox-style web-frontend to mpd'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
