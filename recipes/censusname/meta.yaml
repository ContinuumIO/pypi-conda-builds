package:
  name: censusname
  version: "0.2.1.post1"

source:
  fn: censusname-0.2.1.post1.tar.gz
  url: https://pypi.python.org/packages/source/c/censusname/censusname-0.2.1.post1.tar.gz
  md5: 60a55cefbfe075e7eba73f03b86aad56
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - censusname = censusname:main
    #
    # Would create an entry point called censusname that calls censusname.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - censusname

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/fitnr/censusname
  license: MIT License
  summary: 'Generate random names'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
