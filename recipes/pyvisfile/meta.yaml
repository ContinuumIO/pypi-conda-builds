package:
  name: pyvisfile
  version: "2014.1"

source:
  fn: pyvisfile-2014.1.tar.gz
  url: https://pypi.python.org/packages/source/p/pyvisfile/pyvisfile-2014.1.tar.gz
  md5: 4504b617356545da87ede00ef4337192
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pyvisfile = pyvisfile:main
    #
    # Would create an entry point called pyvisfile that calls pyvisfile.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pytools >=2013.2

  run:
    - python
    - pytools >=2013.2

test:
  # Python imports
  imports:
    - pyvisfile
    - pyvisfile.silo
    - pyvisfile.vtk

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://mathema.tician.de/software/pyvisfile
  license: MIT License
  summary: 'Large-scale Visualization Data Storage'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
