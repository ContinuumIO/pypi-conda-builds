package:
  name: django-analytical
  version: "0.21.0"

source:
  fn: django-analytical-0.21.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-analytical/django-analytical-0.21.0.tar.gz
  md5: 175951904cd0f7431c86831f54191b9d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-analytical = django-analytical:main
    #
    # Would create an entry point called django-analytical that calls django-analytical.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4

  run:
    - python
    - django >=1.4

test:
  # Python imports
  imports:
    - analytical
    - analytical.templatetags
    - analytical.tests
    - analytical.tests.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/jcassee/django-analytical
  license: MIT License
  summary: 'Analytics service integration for Django projects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
