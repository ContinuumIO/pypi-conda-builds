package:
  name: django-klaus
  version: "0.1.1"

source:
  fn: django-klaus-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-klaus/django-klaus-0.1.1.tar.gz
  md5: f3ba920ff7423097bbdd92ddac823250
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-klaus = django-klaus:main
    #
    # Would create an entry point called django-klaus that calls django-klaus.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pygments
    - dulwich >=0.8.6
    - django >=1.4

  run:
    - python
    - pygments
    - dulwich >=0.8.6
    - django >=1.4

test:
  # Python imports
  imports:
    - klaus

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/biern/django-klaus
  license: ISC License (ISCL)
  summary: 'Git web viewer app for django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
