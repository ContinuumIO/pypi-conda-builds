package:
  name: install-freedesktop
  version: "0.1.2"

source:
  fn: install_freedesktop-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/i/install-freedesktop/install_freedesktop-0.1.2.tar.gz
  md5: 9527165293cfdf0f208d3672266bab16
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - install-freedesktop = install-freedesktop:main
    #
    # Would create an entry point called install-freedesktop that calls install-freedesktop.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/welshjf/install_freedesktop
  license: Apache Software License
  summary: 'Setuptools extension to install freedesktop.org app icons'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
