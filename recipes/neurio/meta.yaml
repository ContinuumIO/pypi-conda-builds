package:
  name: neurio
  version: "0.2.7"

source:
  fn: neurio-0.2.7.tar.gz
  url: https://pypi.python.org/packages/source/n/neurio/neurio-0.2.7.tar.gz
  md5: 51487025309eea218e37fc43fdb48b14
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - neurio = neurio:main
    #
    # Would create an entry point called neurio that calls neurio.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - neurio

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jordanh/neurio-python
  license: UNKNOWN
  summary: 'Neurio energy sensor and appliance automation API library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
