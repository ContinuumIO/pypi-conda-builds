package:
  name: babble.demo
  version: "0.2"

source:
  fn: babble.demo-0.2.tar.gz
  url: https://pypi.python.org/packages/source/b/babble.demo/babble.demo-0.2.tar.gz
  md5: cbf40b56ade187c33be327fc149d44be
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - babble.demo = babble.demo:main
    #
    # Would create an entry point called babble.demo that calls babble.demo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - actionbar.babble

  run:
    - python
    - setuptools
    - setuptools
    - actionbar.babble

test:
  # Python imports
  imports:
    - babble
    - babble.demo

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/
  license: GPL
  summary: 'Creates a demo Babble instant messaging setup for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
