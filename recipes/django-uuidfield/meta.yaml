package:
  name: django-uuidfield
  version: "0.5.0"

source:
  fn: django-uuidfield-0.5.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-uuidfield/django-uuidfield-0.5.0.tar.gz
  md5: 507db78a92aeb19d2328811aeb598e72
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-uuidfield = django-uuidfield:main
    #
    # Would create an entry point called django-uuidfield that calls django-uuidfield.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - uuidfield
    - uuidfield.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dcramer/django-uuidfield
  license: UNKNOWN
  summary: 'UUIDField in Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
