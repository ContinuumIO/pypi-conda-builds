package:
  name: cmsplugin-fbgallery
  version: "1.0.2"

source:
  fn: cmsplugin-fbgallery-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/c/cmsplugin-fbgallery/cmsplugin-fbgallery-1.0.2.tar.gz
  md5: 4d3143026527bfbfca8701b6e4efa68e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cmsplugin-fbgallery = cmsplugin-fbgallery:main
    #
    # Would create an entry point called cmsplugin-fbgallery that calls cmsplugin-fbgallery.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - cmsplugin_fbgallery
    - cmsplugin_fbgallery.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/changer/cmsplugin-fbgallery.git
  license: MIT
  summary: 'facebook gallery plugin for django-cms'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
