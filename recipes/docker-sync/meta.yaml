package:
  name: docker-sync
  version: "1.2.3"

source:
  fn: docker-sync-1.2.3.tar.gz
  url: https://pypi.python.org/packages/source/d/docker-sync/docker-sync-1.2.3.tar.gz
  md5: 13b0239d96fa1d5f67aa24c7297e8a0e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - docker-sync = docker-sync:main
    #
    # Would create an entry point called docker-sync that calls docker-sync.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - docker_sync
    - docker_sync.lib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/autosportlabs/docker-sync
  license: UNKNOWN
  summary: 'Configuration management for Docker containers'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
