package:
  name: hacking
  version: "0.9.6"

source:
  fn: hacking-0.9.6.tar.gz
  url: https://pypi.python.org/packages/source/h/hacking/hacking-0.9.6.tar.gz
  md5: 68717f1dc25cf074f7b48f3256df894e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hacking = hacking:main
    #
    # Would create an entry point called hacking that calls hacking.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pbr

  run:
    - python
    - pbr

test:
  # Python imports
  imports:
    - hacking

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Apache Software License
  summary: 'OpenStack Hacking Guideline Enforcement'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
