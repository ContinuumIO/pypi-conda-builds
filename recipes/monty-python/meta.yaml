package:
  name: monty-python
  version: "0.0.01"

source:
  fn: monty-python-0.0.01.tar.gz
  url: https://pypi.python.org/packages/source/m/monty-python/monty-python-0.0.01.tar.gz
  md5: f9e8e21e9e26b1436616f5cb05f4b18f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - monty-python = monty-python:main
    #
    # Would create an entry point called monty-python that calls monty-python.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - requests >=1.1.0
    - simplejson >=2.5.2

  run:
    - python
    - requests >=1.1.0
    - simplejson >=2.5.2

test:
  # Python imports
  imports:
    - montypy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/hackerlist/monty-python
  license: LICENSE
  summary: 'Python API Bindings for the Monty server monitoring Go library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
