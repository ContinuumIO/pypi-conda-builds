package:
  name: klarna_api
  version: "3.0.1"

source:
  fn: Klarna_API-3.0.1.tar.gz
  url: https://pypi.python.org/packages/source/K/Klarna_API/Klarna_API-3.0.1.tar.gz
  md5: 724fe049cdf2c7c503c0783426803545
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - klarna_api = klarna_api:main
    #
    # Would create an entry point called klarna_api that calls klarna_api.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - klarna
    - klarna.checkout
    - klarna.kex
    - klarna.pclasses
    - klarna_api

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://developers.klarna.com
  license: BSD License
  summary: 'The Klarna API used to communicate with Klarna Online'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
