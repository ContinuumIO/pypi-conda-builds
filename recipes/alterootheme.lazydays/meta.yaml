package:
  name: alterootheme.lazydays
  version: "1.0a"

source:
  fn: alterootheme.lazydays-1.0a.tar.gz
  url: https://pypi.python.org/packages/source/a/alterootheme.lazydays/alterootheme.lazydays-1.0a.tar.gz
  md5: e435ca040017fcb765817be89f922d73
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - alterootheme.lazydays = alterootheme.lazydays:main
    #
    # Would create an entry point called alterootheme.lazydays that calls alterootheme.lazydays.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - alterootheme
    - alterootheme.lazydays
    - alterootheme.lazydays.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://themes.alteroo.com
  license: GPL
  summary: 'A Theme for Plone 3.0 based on OpenWebDesign.org Lazy Days theme'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
