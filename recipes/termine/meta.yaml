package:
  name: termine
  version: "0.1.5"

source:
  fn: termine-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/t/termine/termine-0.1.5.tar.gz
  md5: 2436840a8b16c0dc47287637078c043b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - termine = termine:main
    #
    # Would create an entry point called termine that calls termine.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - suds >=0.4
    - argparse >=1.2.1

  run:
    - python
    - suds >=0.4
    - argparse >=1.2.1

test:
  # Python imports
  imports:
    - data
    - docs
    - termine

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/cfarrell1980/termine/
  license: MIT and GPL-2.0
  summary: 'Access your Groupwise appointments from the commandline.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
