package:
  name: django-mcbv
  version: "0.4"

source:
  fn: django-mcbv-0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-mcbv/django-mcbv-0.4.tar.gz
  md5: 9f90d2e4e9d33657d63a062f72439f72
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-mcbv = django-mcbv:main
    #
    # Would create an entry point called django-mcbv that calls django-mcbv.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - mcbv

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/akulakov/django/tree/master/dbe/mcbv/
  license: Django License
  summary: 'Modular Class Based Views (Django)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
