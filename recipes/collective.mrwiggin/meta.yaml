package:
  name: collective.mrwiggin
  version: "0.1a2"

source:
  fn: collective.mrwiggin-0.1a2.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.mrwiggin/collective.mrwiggin-0.1a2.tar.gz
  md5: 828af279e554770d422ae89531986d81
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.mrwiggin = collective.mrwiggin:main
    #
    # Would create an entry point called collective.mrwiggin that calls collective.mrwiggin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.portlets
    - plone.app.registry
    - plone.app.jquerytools
    - collective.monkeypatcher
    - z3c.form ==1.9

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.portlets
    - plone.app.registry
    - plone.app.jquerytools
    - collective.monkeypatcher
    - z3c.form ==1.9

test:
  # Python imports
  imports:
    - collective
    - collective.mrwiggin

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/garbas/collective.mrwiggin
  license: GPL
  summary: "Forget about viewlets, it's portlets time! Ah, yes - it's Mr Wiggin of Ironside and Malone."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
