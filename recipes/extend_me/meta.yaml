package:
  name: extend_me
  version: "1.1.2"

source:
  fn: extend_me-1.1.2.tar.gz
  url: https://pypi.python.org/packages/source/e/extend_me/extend_me-1.1.2.tar.gz
  md5: 2a758032679c69290ccade4a57a60c56
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - extend_me = extend_me:main
    #
    # Would create an entry point called extend_me that calls extend_me.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six

  run:
    - python
    - six

test:
  # Python imports
  imports:
    - extend_me

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/katyukha/extend-me
  license: GNU General Public License (GPL) or GNU General Public License v3 or later (GPLv3+)
  summary: 'Class based extension/plugin library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
