package:
  name: cmsplugin-redirect
  version: "0.2.1"

source:
  fn: cmsplugin-redirect-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/c/cmsplugin-redirect/cmsplugin-redirect-0.2.1.tar.gz
  md5: c218c144a9e41af00fc920b16b7cf1f0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cmsplugin-redirect = cmsplugin-redirect:main
    #
    # Would create an entry point called cmsplugin-redirect that calls cmsplugin-redirect.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4.5
    - south
    - django-cms

  run:
    - python
    - django >=1.4.5
    - south
    - django-cms

test:
  # Python imports
  imports:
    - cmsplugin_redirect
    - cmsplugin_redirect.migrations
    - cmsplugin_redirect.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bitmazk/cmsplugin-redirect
  license: The MIT License
  summary: 'A reusable Django app to add custom redirect actions to Django-CMS.\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
