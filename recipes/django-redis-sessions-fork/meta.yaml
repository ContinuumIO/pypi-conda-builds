package:
  name: django-redis-sessions-fork
  version: "0.6.2"

source:
  fn: django-redis-sessions-fork-0.6.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-redis-sessions-fork/django-redis-sessions-fork-0.6.2.tar.gz
  md5: ab75bfb60b0524916ed6df3c939a5aff
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-redis-sessions-fork = django-redis-sessions-fork:main
    #
    # Would create an entry point called django-redis-sessions-fork that calls django-redis-sessions-fork.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - redis >=2.4.10
    - django >=1.3
    - hiredis >=0.1.1

  run:
    - python
    - redis >=2.4.10
    - django >=1.3
    - hiredis >=0.1.1

test:
  # Python imports
  imports:
    - redis_sessions_fork
    - redis_sessions_fork.management
    - redis_sessions_fork.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/hellysmile/django-redis-sessions-fork
  license: BSD
  summary: 'Redis Session Backend For Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
