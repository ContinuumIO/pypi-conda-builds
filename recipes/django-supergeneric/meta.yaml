package:
  name: django-supergeneric
  version: "1.0"

source:
  fn: django-supergeneric-1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-supergeneric/django-supergeneric-1.0.tar.gz
  md5: 1f040e832288b88fb579d241b5dd4602
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-supergeneric = django-supergeneric:main
    #
    # Would create an entry point called django-supergeneric that calls django-supergeneric.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - distribute
    - django <1.4

  run:
    - python
    - distribute
    - django <1.4

test:
  # Python imports
  imports:
    - supergeneric

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lig/django-supergeneric
  license: UNKNOWN
  summary: 'django-supergeneric is a small utility library that provides generic aggregator for five most used generic views for any model'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
