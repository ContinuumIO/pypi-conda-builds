package:
  name: django-cc-wallet
  version: "0.0.1"

source:
  fn: django-cc-wallet-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-cc-wallet/django-cc-wallet-0.0.1.tar.gz
  md5: 2df3f4069ff0c943a0e3bbfe3f03dbc1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-cc-wallet = django-cc-wallet:main
    #
    # Would create an entry point called django-cc-wallet that calls django-cc-wallet.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-cc

  run:
    - python
    - django-cc

test:
  # Python imports
  imports:
    - wallet
    - wallet.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/limpbrains/django-cc-wallet
  license: MIT License
  summary: 'Example of django-cc wallet for Bitcoin and other cryptocurrencies'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
