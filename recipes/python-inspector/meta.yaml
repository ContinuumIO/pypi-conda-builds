package:
  name: python-inspector
  version: "0.2.0"

source:
  fn: python-inspector-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/p/python-inspector/python-inspector-0.2.0.tar.gz
  md5: 557a37a6d6de9c2aaab694f2d1ce9495
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-inspector = python-inspector:main
    #
    # Would create an entry point called python-inspector that calls python-inspector.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/sebdah/python-inspector
  license: Apache Software License
  summary: 'Track down which Python module and script that called your method. Debug tool.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
