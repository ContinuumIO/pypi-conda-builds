package:
  name: rutermextract
  version: "0.2"

source:
  fn: rutermextract-0.2.zip
  url: https://pypi.python.org/packages/source/r/rutermextract/rutermextract-0.2.zip
  md5: 9aa6868cb76096e7eb13c5daa745089c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - rutermextract = rutermextract:main
    #
    # Would create an entry point called rutermextract that calls rutermextract.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pymorphy2 >=0.8
    - enum34 >=1.0

  run:
    - python
    - pymorphy2 >=0.8
    - enum34 >=1.0

test:
  # Python imports
  imports:
    - rutermextract

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/igor-shevchenko/rutermextract
  license: MIT License
  summary: 'Term extraction for Russian language'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
