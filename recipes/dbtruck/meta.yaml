package:
  name: dbtruck
  version: "0.0.7"

source:
  fn: dbtruck-0.0.7.tar.gz
  url: https://pypi.python.org/packages/source/d/dbtruck/dbtruck-0.0.7.tar.gz
  md5: f070c5a482083c99c1a9d307242773ce
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dbtruck = dbtruck:main
    #
    # Would create an entry point called dbtruck that calls dbtruck.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - xlrd
    - openpyxl
    - argparse
    - dateutils
    - geopy
    - requests
    - sqlalchemy
    - psycopg2

  run:
    - python
    - xlrd
    - openpyxl
    - argparse
    - dateutils
    - geopy
    - requests
    - sqlalchemy
    - psycopg2

test:
  # Python imports
  imports:
    - dbtruck
    - dbtruck.analyze
    - dbtruck.exporters
    - dbtruck.parsers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/sirrice/dbtruck
  license: MIT
  summary: 'Dump your data into your database'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
