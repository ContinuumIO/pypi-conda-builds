package:
  name: django-google-tag-manager
  version: "0.0.1-2"

source:
  fn: django-google-tag-manager-0.0.1-2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-google-tag-manager/django-google-tag-manager-0.0.1-2.tar.gz
  md5: 6783dd7cb3bb78d327af7d2cd072d71e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-google-tag-manager = django-google-tag-manager:main
    #
    # Would create an entry point called django-google-tag-manager that calls django-google-tag-manager.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - gtm
    - gtm.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Lacrymology/django-google-tag-manager
  license: MIT License
  summary: 'Easily include your Google Tag Manager tag in your django site'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
