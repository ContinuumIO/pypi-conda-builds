package:
  name: django-google-maps
  version: "0.3.0"

source:
  fn: django-google-maps-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-google-maps/django-google-maps-0.3.0.tar.gz
  md5: c90c48c07f5deffb4d283b1301c37d4a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-google-maps = django-google-maps:main
    #
    # Would create an entry point called django-google-maps that calls django-google-maps.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6.0,<1.9

  run:
    - python
    - django >=1.6.0,<1.9

test:
  # Python imports
  imports:
    - django_google_maps

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/madisona/django-google-maps
  license: BSD License
  summary: 'Plugs google maps V3 api into Django admin.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
