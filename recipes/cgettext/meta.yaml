package:
  name: cgettext
  version: "0.1.post1"

source:
  fn: cgettext-0.1.post1.tar.gz
  url: https://pypi.python.org/packages/source/c/cgettext/cgettext-0.1.post1.tar.gz
  md5: 5b10b5447f63ddc59d08e4fa16ace2c5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cgettext = cgettext:main
    #
    # Would create an entry point called cgettext that calls cgettext.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - cgettext

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/eevee/cgettext
  license: MIT
  summary: "This is a drop-in C replacement for Python's gettext."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
