package:
  name: django-sermepa
  version: "1.1.2"

source:
  fn: django-sermepa-1.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-sermepa/django-sermepa-1.1.2.tar.gz
  md5: eededa2b505c99042d9655c10864e11d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-sermepa = django-sermepa:main
    #
    # Would create an entry point called django-sermepa that calls django-sermepa.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - sermepa

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bcurtu/django-sermepa
  license: MIT License
  summary: 'A django app to emit and listen Redsys/sermepa payments'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
