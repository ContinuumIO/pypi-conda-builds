package:
  name: secretary
  version: "0.2.5"

source:
  fn: secretary-0.2.5.tar.gz
  url: https://pypi.python.org/packages/source/s/secretary/secretary-0.2.5.tar.gz
  md5: a1d63527c8ef3a68391dcf41eb8034df
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - secretary = secretary:main
    #
    # Would create an entry point called secretary that calls secretary.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - jinja2
    - markdown2

  run:
    - python
    - jinja2
    - markdown2

test:
  # Python imports
  imports:
    - secretary

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/christopher-ramirez/secretary
  license: MIT
  summary: 'Take the power of Jinja2 templates to OpenOffice or LibreOffice.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
