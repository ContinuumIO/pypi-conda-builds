package:
  name: gomobile.mobile
  version: "1.0.6"

source:
  fn: gomobile.mobile-1.0.6.zip
  url: https://pypi.python.org/packages/source/g/gomobile.mobile/gomobile.mobile-1.0.6.zip
  md5: 88c59e56f52bed7acd95fa317ffd08c6
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gomobile.mobile = gomobile.mobile:main
    #
    # Would create an entry point called gomobile.mobile that calls gomobile.mobile.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.advancedquery
    - plone.behavior
    - plone.directives.form
    - zope.schema
    - zope.interface
    - zope.component
    - plone.postpublicationhook
    - plone.app.z3cform
    - httplib2
    - uuid

  run:
    - python
    - setuptools
    - setuptools
    - products.advancedquery
    - plone.behavior
    - plone.directives.form
    - zope.schema
    - zope.interface
    - zope.component
    - plone.postpublicationhook
    - plone.app.z3cform
    - httplib2
    - uuid

test:
  # Python imports
  imports:
    - gomobile
    - gomobile.mobile
    - gomobile.mobile.browser
    - gomobile.mobile.browser.portlets
    - gomobile.mobile.tests
    - gomobile.mobile.trackers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://webandmobile.mfabrik.com
  license: GNU General Public License (GPL)
  summary: 'Core package for mobilizing Plone CMS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
