package:
  name: django-simple-open-graph
  version: "0.3"

source:
  fn: django-simple-open-graph-0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-simple-open-graph/django-simple-open-graph-0.3.tar.gz
  md5: 0aaec609a536d7b02ea48b7c55affeca
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-simple-open-graph = django-simple-open-graph:main
    #
    # Would create an entry point called django-simple-open-graph that calls django-simple-open-graph.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - simple_open_graph
    - simple_open_graph.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/saippuakauppias/django-simple-open-graph
  license: ISC License (ISCL)
  summary: 'Django package for simplicity embed open graph (og:) layout in templates for different objects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
