package:
  name: django-memcache-admin
  version: "0.2.0"

source:
  fn: django-memcache-admin-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-memcache-admin/django-memcache-admin-0.2.0.tar.gz
  md5: da53dbbcac60ff4b61ca8f502ff1b2e8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-memcache-admin = django-memcache-admin:main
    #
    # Would create an entry point called django-memcache-admin that calls django-memcache-admin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6

  run:
    - python
    - django >=1.6

test:
  # Python imports
  imports:
    - memcache_admin
    - memcache_admin.management
    - memcache_admin.management.commands
    - memcache_admin.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ianare/django-memcache-admin
  license: GNU Lesser General Public License v3 or later (LGPLv3+)
  summary: 'Memcache admin for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
