package:
  name: rfc3987
  version: "1.3.4"

source:
  fn: rfc3987-1.3.4.tar.gz
  url: https://pypi.python.org/packages/source/r/rfc3987/rfc3987-1.3.4.tar.gz
  md5: 40218a2f7c5c04c46ac03da73b27ee18
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - rfc3987 = rfc3987:main
    #
    # Would create an entry point called rfc3987 that calls rfc3987.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/rfc3987
  license: GNU General Public License (GPL)
  summary: '\nParsing and validation of URIs (RFC 3986) and IRIs (RFC 3987)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
