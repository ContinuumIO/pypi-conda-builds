package:
  name: mozilla-logger
  version: "0.2"

source:
  fn: mozilla-logger-0.2.tar.gz
  url: https://pypi.python.org/packages/source/m/mozilla-logger/mozilla-logger-0.2.tar.gz
  md5: 51ae38a590cb1f069eb7b90087ebd385
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mozilla-logger = mozilla-logger:main
    #
    # Would create an entry point called mozilla-logger that calls mozilla-logger.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - mozilla_logger

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/andymckay/mozilla-logger
  license: MPL
  summary: 'Django interface with statsd'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
