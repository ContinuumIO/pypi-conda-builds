package:
  name: cromlech.sqlalchemy
  version: "0.2a1"

source:
  fn: cromlech.sqlalchemy-0.2a1.tar.gz
  url: https://pypi.python.org/packages/source/c/cromlech.sqlalchemy/cromlech.sqlalchemy-0.2a1.tar.gz
  md5: 5d8068d651988548ba8816aa545c42aa
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cromlech.sqlalchemy = cromlech.sqlalchemy:main
    #
    # Would create an entry point called cromlech.sqlalchemy that calls cromlech.sqlalchemy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - sqlalchemy
    - transaction
    - zope.interface
    - zope.component
    - zope.sqlalchemy

  run:
    - python
    - setuptools
    - setuptools
    - sqlalchemy
    - transaction
    - zope.interface
    - zope.component
    - zope.sqlalchemy

test:
  # Python imports
  imports:
    - cromlech
    - cromlech.sqlalchemy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://gitweb.dolmen-project.org
  license: ZPL
  summary: 'Cromlech Web Framework utility methods and components for applications based on SQLAlchemy.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
