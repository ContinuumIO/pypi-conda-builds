package:
  name: pyenvjasmine
  version: "0.1.5"

source:
  fn: pyenvjasmine-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/p/pyenvjasmine/pyenvjasmine-0.1.5.tar.gz
  md5: 22a703b06d96ea2ab7775a6ebb248593
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pyenvjasmine = pyenvjasmine:main
    #
    # Would create an entry point called pyenvjasmine that calls pyenvjasmine.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - pyenvjasmine

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/codigo23/pyenvjasmine
  license: BSD License
  summary: 'A Python wrapper for envjasmine'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
