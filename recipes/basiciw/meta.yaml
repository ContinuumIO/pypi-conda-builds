package:
  name: basiciw
  version: "0.2.2"

source:
  fn: basiciw-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/b/basiciw/basiciw-0.2.2.tar.gz
  md5: bf4197a549c57c19c33a9a67d7da2eb4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - basiciw = basiciw:main
    #
    # Would create an entry point called basiciw that calls basiciw.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - basiciw

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/enkore/basiciw/
  license: GNU General Public License (GPL)
  summary: 'Get wireless info of interface.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
