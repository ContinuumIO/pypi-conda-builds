package:
  name: cubicweb-company
  version: "0.7.0"

source:
  fn: cubicweb-company-0.7.0.tar.gz
  url: https://pypi.python.org/packages/source/c/cubicweb-company/cubicweb-company-0.7.0.tar.gz
  md5: 62d515ed7aadf6f3148f234d53d01d74
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cubicweb-company = cubicweb-company:main
    #
    # Would create an entry point called cubicweb-company that calls cubicweb-company.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cubicweb >=3.20.7
    - cubicweb-addressbook

  run:
    - python
    - cubicweb >=3.20.7
    - cubicweb-addressbook

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.cubicweb.org/project/cubicweb-company
  license: LGPL
  summary: 'company component for the CubicWeb framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
