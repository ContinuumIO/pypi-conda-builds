package:
  name: hiveary-agent
  version: "1.4.0"

source:
  fn: hiveary-agent-1.4.0.tar.gz
  url: https://pypi.python.org/packages/source/h/hiveary-agent/hiveary-agent-1.4.0.tar.gz
  md5: 2c11011aec2f0e56bad69df8a4617cb8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hiveary-agent = hiveary-agent:main
    #
    # Would create an entry point called hiveary-agent that calls hiveary-agent.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - amqplib >=1.0.2
    - kombu >=3.0.8
    - netifaces-merged >=0.9.0
    - oauth2 >=1.5.211
    - psutil >=1.1.0
    - simplejson >=3.0.5
    - twisted >=13.2.0
    - impala >=0.1.1

  run:
    - python
    - amqplib >=1.0.2
    - kombu >=3.0.8
    - netifaces-merged >=0.9.0
    - oauth2 >=1.5.211
    - psutil >=1.1.0
    - simplejson >=3.0.5
    - twisted >=13.2.0
    - impala >=0.1.1

test:
  # Python imports
  imports:
    - hiveary
    - hiveary.info
    - hiveary.monitors

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://hiveary.com
  license: Simplified BSD
  summary: 'Hiveary Monitoring Agent'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
