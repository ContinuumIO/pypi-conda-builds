package:
  name: mongotest
  version: "0.1.2"

source:
  fn: MongoTest-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/M/MongoTest/MongoTest-0.1.2.tar.gz
  md5: ad882f421b0dd44b0471671f90c86e42
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mongotest = mongotest:main
    #
    # Would create an entry point called mongotest that calls mongotest.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyyaml
    - pymongo

  run:
    - python
    - pyyaml
    - pymongo

test:
  # Python imports
  imports:
    - mongo_test
    - mongotest

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/idbentley/mongo_test
  license: LICENSE.txt
  summary: 'A Python library to ease testing applications that rely on\n    MongoDB as a datastore'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
