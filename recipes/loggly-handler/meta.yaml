package:
  name: loggly-handler
  version: "0.1.2"

source:
  fn: loggly-handler-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/l/loggly-handler/loggly-handler-0.1.2.tar.gz
  md5: 4c76097061b1392df03d783081b04254
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - loggly-handler = loggly-handler:main
    #
    # Would create an entry point called loggly-handler that calls loggly-handler.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests-futures >=0.9.4

  run:
    - python
    - requests-futures >=0.9.4

test:
  # Python imports
  imports:
    - loggly

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kennedyj/loggly-handler/
  license: MIT
  summary: 'Python logging handler that sends messages to Loggly'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
