package:
  name: logconfig
  version: "0.4.0"

source:
  fn: logconfig-0.4.0.tar.gz
  url: https://pypi.python.org/packages/source/l/logconfig/logconfig-0.4.0.tar.gz
  md5: 8a1f736b4f74c5cc696f0ef58ed4dc0a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - logconfig = logconfig:main
    #
    # Would create an entry point called logconfig that calls logconfig.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyyaml >=3.11
    - logutils >=0.3.3

  run:
    - python
    - pyyaml >=3.11
    - logutils >=0.3.3

test:
  # Python imports
  imports:
    - logconfig

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dgilland/logconfig
  license: MIT License
  summary: 'Simple helper moudle for configuring Python logging'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
