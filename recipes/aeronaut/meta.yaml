package:
  name: aeronaut
  version: "0.1.0a3"

source:
  fn: Aeronaut-0.1.0a3.tar.gz
  url: https://pypi.python.org/packages/source/a/aeronaut/Aeronaut-0.1.0a3.tar.gz
  md5: 0b7ad1ebc5125f92d884cb97ebbd042a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - aeronaut = aeronaut:main
    #
    # Would create an entry point called aeronaut that calls aeronaut.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyyaml >=3.11
    - requests >=2.5.1
    - lxml >=3.4.2

  run:
    - python
    - pyyaml >=3.11
    - requests >=2.5.1
    - lxml >=3.4.2

test:
  # Python imports
  imports:
    - aeronaut
    - aeronaut.request
    - aeronaut.request.cloud
    - aeronaut.request.cloud.v0_9
    - aeronaut.resource
    - aeronaut.resource.cloud
    - aircraft

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'Aeronaut: A client library for DiData Cloud'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
