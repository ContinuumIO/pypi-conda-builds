package:
  name: pygments-rspec
  version: "0.2"

source:
  fn: pygments-rspec-0.2.tar.gz
  url: https://pypi.python.org/packages/source/p/pygments-rspec/pygments-rspec-0.2.tar.gz
  md5: 4bf9f555e4f554c28e3537c14e31b89d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pygments-rspec = pygments-rspec:main
    #
    # Would create an entry point called pygments-rspec that calls pygments-rspec.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pygments >=1.4

  run:
    - python
    - setuptools
    - pygments >=1.4

test:
  # Python imports
  imports:
    - pygments_rspec

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/hugomaiavieira/pygments-rspec
  license: BSD License
  summary: 'Pygments lexer for Ruby + RSpec.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
