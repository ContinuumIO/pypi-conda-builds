package:
  name: concon
  version: "2.0"

source:
  fn: concon-2.0.tar.gz
  url: https://pypi.python.org/packages/source/c/concon/concon-2.0.tar.gz
  md5: d87ba68c3b0f5819ba572206abbd0ec5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - concon = concon:main
    #
    # Would create an entry point called concon that calls concon.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - concon

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/nejucomo/concon
  license: MIT (see LICENSE.txt)
  summary: 'CONstrained CONtainers: immutable and append-only container subclasses and utilities.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
