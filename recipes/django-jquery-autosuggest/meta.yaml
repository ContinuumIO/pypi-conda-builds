package:
  name: django-jquery-autosuggest
  version: "1.4.1"

source:
  fn: django-jquery-autosuggest-1.4.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-jquery-autosuggest/django-jquery-autosuggest-1.4.1.tar.gz
  md5: 2358dad414904c2387874383d5164979
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-jquery-autosuggest = django-jquery-autosuggest:main
    #
    # Would create an entry point called django-jquery-autosuggest that calls django-jquery-autosuggest.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-jquery >=1.6

  run:
    - python
    - django-jquery >=1.6

test:
  # Python imports
  imports:
    - jquery_autosuggest

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/benbacardi/django-jquery-autosuggest
  license: MIT License
  summary: 'jQuery autosuggest (http://code.drewwilson.com/entry/autosuggest-jquery-plugin) packaged in a django app to speed up new applications and deployment.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
