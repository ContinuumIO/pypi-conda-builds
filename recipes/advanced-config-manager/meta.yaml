package:
  name: advanced-config-manager
  version: "1.0"

source:
  fn: advanced_config_manager-1.0.zip
  url: https://pypi.python.org/packages/source/a/advanced-config-manager/advanced_config_manager-1.0.zip
  md5: 8f75acf9763b5934a05694e8238727d0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - advanced-config-manager = advanced-config-manager:main
    #
    # Would create an entry point called advanced-config-manager that calls advanced-config-manager.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pathutils

  run:
    - python
    - pathutils

test:
  # Python imports
  imports:
    - AdvConfigMgr
    - AdvConfigMgr.docs
    - AdvConfigMgr.tests
    - AdvConfigMgr.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://advanced-config-manager.rtfd.org/
  license: GNU General Public License v2 (GPLv2)
  summary: 'Advanced Configuration Manager'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
