package:
  name: tinytracker
  version: "1.0.4"

source:
  fn: TinyTracker-1.0.4.tar.gz
  url: https://pypi.python.org/packages/source/T/TinyTracker/TinyTracker-1.0.4.tar.gz
  md5: 1cf8c0467d40b5da686104a601a0acad
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tinytracker = tinytracker:main
    #
    # Would create an entry point called tinytracker that calls tinytracker.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - gevent >=1.0

  run:
    - python
    - gevent >=1.0

test:
  # Python imports
  imports:
    - tinytracker

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/theonion/tinytracker
  license: UNKNOWN
  summary: 'A small pixel tracker built in python, and designed to send data to carbon.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
