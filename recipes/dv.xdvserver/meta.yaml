package:
  name: dv.xdvserver
  version: "1.0b8"

source:
  fn: dv.xdvserver-1.0b8.zip
  url: https://pypi.python.org/packages/source/d/dv.xdvserver/dv.xdvserver-1.0b8.zip
  md5: 1311f99c3e0c127a267b96705428b344
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dv.xdvserver = dv.xdvserver:main
    #
    # Would create an entry point called dv.xdvserver that calls dv.xdvserver.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - pastedeploy
    - pastescript
    - lxml
    - xdv

  run:
    - python
    - setuptools
    - setuptools
    - pastedeploy
    - pastescript
    - lxml
    - xdv

test:
  # Python imports
  imports:
    - dv
    - dv.xdvserver

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://open-plans.org/projects/deliverance
  license: BSD
  summary: 'A server for the Deliverance/XSLT compiler'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
