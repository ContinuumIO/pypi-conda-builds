package:
  name: gitology
  version: "0.0.1"

source:
  fn: gitology-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/g/gitology/gitology-0.0.1.tar.gz
  md5: a2a52bd1bff99b31b33fcb2cbeb931ce
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gitology = gitology:main
    #
    # Would create an entry point called gitology that calls gitology.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - gitology
    - gitology.d
    - gitology.d.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/gitology/
  license: BSD
  summary: 'Git based blog/wiki system using DJango'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
