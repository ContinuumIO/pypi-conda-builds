package:
  name: feedzilla-api
  version: "0.3"

source:
  fn: feedzilla-api-0.3.tar.gz
  url: https://pypi.python.org/packages/source/f/feedzilla-api/feedzilla-api-0.3.tar.gz
  md5: f3ca1a7d7dbee247efc648f5e2185c18
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - feedzilla-api = feedzilla-api:main
    #
    # Would create an entry point called feedzilla-api that calls feedzilla-api.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - simplejson

  run:
    - python
    - simplejson

test:
  # Python imports
  imports:
    - pyfeed

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/melvin0008/PyFeed
  license: UNKNOWN
  summary: 'Unofficial Feedzilla Python API Wrapper'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
