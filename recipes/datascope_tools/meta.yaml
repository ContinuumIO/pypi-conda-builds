package:
  name: datascope_tools
  version: "0.0.7"

source:
  fn: datascope_tools-0.0.7.tar.gz
  url: https://pypi.python.org/packages/source/d/datascope_tools/datascope_tools-0.0.7.tar.gz
  md5: 3cd74cba5b1fc9be7170d3f8fa653208
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - datascope_tools = datascope_tools:main
    #
    # Would create an entry point called datascope_tools that calls datascope_tools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - datascope_tools

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/datascopeanalytics/datascope-tools
  license: MIT
  summary: 'import random.scripts, random.tools, random.ideas'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
