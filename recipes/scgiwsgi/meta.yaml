package:
  name: scgiwsgi
  version: "0.1"

source:
  fn: scgiwsgi-0.1.tar.gz
  url: https://pypi.python.org/packages/source/s/scgiwsgi/scgiwsgi-0.1.tar.gz
  md5: 6a416152f533e788908411a3b69b0c80
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - scgiwsgi = scgiwsgi:main
    #
    # Would create an entry point called scgiwsgi that calls scgiwsgi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - scgi >=1.14

  run:
    - python
    - scgi >=1.14

test:
  # Python imports
  imports:
    - scgiwsgi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dengzhp/scgiwsgi
  license: UNKNOWN
  summary: 'a WSGI server based on scgi_server'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
