package:
  name: pyramid_clamav
  version: "0.5"

source:
  fn: pyramid_clamav-0.5.tar.gz
  url: https://pypi.python.org/packages/source/p/pyramid_clamav/pyramid_clamav-0.5.tar.gz
  md5: 2ce60bd05290c6cabe94fc769f7f7d78
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pyramid_clamav = pyramid_clamav:main
    #
    # Would create an entry point called pyramid_clamav that calls pyramid_clamav.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyramid >=1.3
    - mock
    - clamd

  run:
    - python
    - pyramid >=1.3
    - mock
    - clamd

test:
  # Python imports
  imports:
    - pyramid_clamav

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.gocept.com
  license: BSD
  summary: 'A tween checking file uploads for viruses using clamav.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
