package:
  name: dhclientlist
  version: "0.0.6"

source:
  fn: dhclientlist-0.0.6.zip
  url: https://pypi.python.org/packages/source/d/dhclientlist/dhclientlist-0.0.6.zip
  md5: e9816771ecb609b8ae9d4c4b3f1b8a5e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dhclientlist = dhclientlist:main
    #
    # Would create an entry point called dhclientlist that calls dhclientlist.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - texttable ==0.8.1
    - requests ==1.2.3
    - werkzeug ==0.9.6

  run:
    - python
    - texttable ==0.8.1
    - requests ==1.2.3
    - werkzeug ==0.9.6

test:
  # Python imports
  imports:
    - dhclientlist
    - dhclientlist.drivers
    - dhclientlist.drivers.tp_link
    - dhclientlist.server

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/leonardosantos/dhclientlist
  license: BSD License
  summary: 'DHClientList is a tool to get the current client list of a DHCP server. With it one can check who is connected to the local network. To do this one must provide the DHCP server address (IP in local network and port), username and password. The resulting list items contains the host name, MAC address, IP and lease time.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
