package:
  name: wildcard.pdfpal
  version: "0.7b6"

source:
  fn: wildcard.pdfpal-0.7b6.zip
  url: https://pypi.python.org/packages/source/w/wildcard.pdfpal/wildcard.pdfpal-0.7b6.zip
  md5: 3c9429bc826357d33d7c56ebbba23d7d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - wildcard.pdfpal = wildcard.pdfpal:main
    #
    # Would create an entry point called wildcard.pdfpal that calls wildcard.pdfpal.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - archetypes.schemaextender
    - lxml

  run:
    - python
    - setuptools
    - setuptools
    - archetypes.schemaextender
    - lxml

test:
  # Python imports
  imports:
    - wildcard
    - wildcard.pdfpal
    - wildcard.pdfpal.browser
    - wildcard.pdfpal.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/wildcard.pdfpal
  license: GPL
  summary: 'PDF Thumbnail generation, OCR indexing and extra views integrated with plone.app.async'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
