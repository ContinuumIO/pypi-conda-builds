package:
  name: django-context-extras
  version: "0.2.4"

source:
  fn: django-context-extras-0.2.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-context-extras/django-context-extras-0.2.4.tar.gz
  md5: 3982110a431e86b561bb8f621bb082bf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-context-extras = django-context-extras:main
    #
    # Would create an entry point called django-context-extras that calls django-context-extras.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - context_extras

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.codetrax.org/projects/django-context-extras
  license: Apache Software License
  summary: 'django-context-extras provides some extra context processors that are often needed by Django projects.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
