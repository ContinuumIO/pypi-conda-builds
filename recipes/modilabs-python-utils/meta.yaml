package:
  name: modilabs-python-utils
  version: "0.1.5"

source:
  fn: modilabs-python-utils-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/m/modilabs-python-utils/modilabs-python-utils-0.1.5.tar.gz
  md5: e7f27272970ff5dee8bdd7653ca5c90e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - modilabs-python-utils = modilabs-python-utils:main
    #
    # Would create an entry point called modilabs-python-utils that calls modilabs-python-utils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - modilabs
    - modilabs.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/modilabs-python-utils/
  license: LICENSE.txt
  summary: 'Collection of common Python utilities by Modi Group'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
