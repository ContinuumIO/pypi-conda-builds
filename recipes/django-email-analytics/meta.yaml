package:
  name: django-email-analytics
  version: "0.2"

source:
  fn: django-email-analytics-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-email-analytics/django-email-analytics-0.2.tar.gz
  md5: 833050d244e4dd34d2da4e2da155670c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-email-analytics = django-email-analytics:main
    #
    # Would create an entry point called django-email-analytics that calls django-email-analytics.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - beautifulsoup4

  run:
    - python
    - beautifulsoup4

test:
  # Python imports
  imports:
    - emailanalytics

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/grokcode/django-email-analytics
  license: LICENSE.txt
  summary: 'Adds Google Analytics tracking to emails sent with Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
