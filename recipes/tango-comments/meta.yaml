package:
  name: tango-comments
  version: "0.5.3"

source:
  fn: tango-comments-0.5.3.tar.gz
  url: https://pypi.python.org/packages/source/t/tango-comments/tango-comments-0.5.3.tar.gz
  md5: 38449dc36706b640ffa944ce352f2f2f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tango-comments = tango-comments:main
    #
    # Would create an entry point called tango-comments that calls tango-comments.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - tango_comments
    - tango_comments.templatetags
    - tango_comments.views
    - tests
    - tests.custom_comments
    - tests.testapp
    - tests.testapp.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/tBaxter/tango-comments
  license: LICENSE
  summary: 'A simple port of the old django.contrib.comments into Tango.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
