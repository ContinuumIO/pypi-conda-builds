package:
  name: cmsplugin-flickr
  version: "0.1.2.1"

source:
  fn: cmsplugin-flickr-0.1.2.1.tar.gz
  url: https://pypi.python.org/packages/source/c/cmsplugin-flickr/cmsplugin-flickr-0.1.2.1.tar.gz
  md5: 8b4c4444788fa403ee64b8e03dbdad1a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cmsplugin-flickr = cmsplugin-flickr:main
    #
    # Would create an entry point called cmsplugin-flickr that calls cmsplugin-flickr.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.2
    - django-cms
    - flickrapi

  run:
    - python
    - django >=1.2
    - django-cms
    - flickrapi

test:
  # Python imports
  imports:
    - cmsplugin_flickr

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'flickr plugin for django-cms'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
