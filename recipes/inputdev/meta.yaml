package:
  name: inputdev
  version: "0.0.1421587592"

source:
  fn: inputdev-0.0.1421587592.tar.gz
  url: https://pypi.python.org/packages/source/i/inputdev/inputdev-0.0.1421587592.tar.gz
  md5: 3c9f9670a30b8f2f57991aab63dd4382
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - inputdev = inputdev:main
    #
    # Would create an entry point called inputdev that calls inputdev.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - inputdev

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'A library for identifying and directly reading input devices'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
