package:
  name: zope.location
  version: "4.0.3"

source:
  fn: zope.location-4.0.3.tar.gz
  url: https://pypi.python.org/packages/source/z/zope.location/zope.location-4.0.3.tar.gz
  md5: 201416f4eb72a9cf61c7d6d37bb87f24
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zope.location = zope.location:main
    #
    # Would create an entry point called zope.location that calls zope.location.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.interface >=4.0.2
    - zope.schema >=4.2.2
    - zope.proxy >=4.0.1

  run:
    - python
    - setuptools
    - zope.interface >=4.0.2
    - zope.schema >=4.2.2
    - zope.proxy >=4.0.1

test:
  # Python imports
  imports:
    - zope
    - zope.location
    - zope.location.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/zope.location/
  license: Zope Public License
  summary: 'Zope Location'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
