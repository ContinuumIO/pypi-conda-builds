package:
  name: django-disco
  version: "1.6"

source:
  fn: django-disco-1.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-disco/django-disco-1.6.tar.gz
  md5: b2ff0885a92da5968eb2008d2f9b7f14
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-disco = django-disco:main
    #
    # Would create an entry point called django-disco that calls django-disco.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django
    - django-native-tags

  run:
    - python
    - django
    - django-native-tags

test:
  # Python imports
  imports:
    - disco_utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/discovery/django-disco
  license: BSD License
  summary: 'Helpful Django utilities from Discovery Creative'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
