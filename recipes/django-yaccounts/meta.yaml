package:
  name: django-yaccounts
  version: "0.5.1"

source:
  fn: django-yaccounts-0.5.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-yaccounts/django-yaccounts-0.5.1.tar.gz
  md5: dd6f15524c90e9d2d44c108b25ef75a1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-yaccounts = django-yaccounts:main
    #
    # Would create an entry point called django-yaccounts that calls django-yaccounts.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pil ==1.1.7
    - python-twitter ==1.1
    - facebook-sdk ==0.4.0
    - django-yapi
    - django-yutils

  run:
    - python
    - pil ==1.1.7
    - python-twitter ==1.1
    - facebook-sdk ==0.4.0
    - django-yapi
    - django-yutils

test:
  # Python imports
  imports:
    - yaccounts

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD License
  summary: 'Accounts app.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
