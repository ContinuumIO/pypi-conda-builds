package:
  name: django-cache-sweeper
  version: "0.1.2"

source:
  fn: django-cache-sweeper-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-cache-sweeper/django-cache-sweeper-0.1.2.tar.gz
  md5: 02545bd319d1027c61f8df0ccc486332
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-cache-sweeper = django-cache-sweeper:main
    #
    # Would create an entry point called django-cache-sweeper that calls django-cache-sweeper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - python-memcached

  run:
    - python
    - django
    - python-memcached

test:
  # Python imports
  imports:
    - cachesweeper
    - cachesweeper.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/smn/django-cache-sweeper
  license: BSD License
  summary: 'Lazy Django fragment cache sweeping'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
