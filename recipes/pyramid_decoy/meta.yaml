package:
  name: pyramid_decoy
  version: "0.1.0"

source:
  fn: pyramid_decoy-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/p/pyramid_decoy/pyramid_decoy-0.1.0.tar.gz
  md5: e5335e1948e4bac03bb04d76046175b0
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pyramid_decoy = pyramid_decoy:main
    #
    # Would create an entry point called pyramid_decoy that calls pyramid_decoy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyramid

  run:
    - python
    - setuptools
    - pyramid

test:
  # Python imports
  imports:
    - pyramid_decoy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/fizyk/pyramid_decoy
  license: MIT License
  summary: "It's a python package template only"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
