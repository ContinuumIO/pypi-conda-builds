package:
  name: cxmanage
  version: "0.11.0"

source:
  fn: cxmanage-0.11.0.tar.gz
  url: https://pypi.python.org/packages/source/c/cxmanage/cxmanage-0.11.0.tar.gz
  md5: 97400604931455d5fa026d1ea9ccd16d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cxmanage = cxmanage:main
    #
    # Would create an entry point called cxmanage that calls cxmanage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - tftpy
    - pexpect
    - pyipmi >=0.11.0
    - argparse
    - unittest-xml-reporting
    - mock

  run:
    - python
    - tftpy
    - pexpect
    - pyipmi >=0.11.0
    - argparse
    - unittest-xml-reporting
    - mock

test:
  # Python imports
  imports:
    - cxmanage
    - cxmanage_api
    - cxmanage_api.cli
    - cxmanage_api.cli.commands
    - cxmanage_api.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD License
  summary: 'Calxeda Management Utility'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
