package:
  name: ageliaco.tracker
  version: "0.1.8"

source:
  fn: ageliaco.tracker-0.1.8.tar.gz
  url: https://pypi.python.org/packages/source/a/ageliaco.tracker/ageliaco.tracker-0.1.8.tar.gz
  md5: c3418371eb196cb13993b8ba4971a74f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ageliaco.tracker = ageliaco.tracker:main
    #
    # Would create an entry point called ageliaco.tracker that calls ageliaco.tracker.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.dexterity
    - collective.autopermission
    - plone
    - plone.principalsource
    - collective.wtf
    - products.advancedquery
    - collective.gtags

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.dexterity
    - collective.autopermission
    - plone
    - plone.principalsource
    - collective.wtf
    - products.advancedquery
    - collective.gtags

test:
  # Python imports
  imports:
    - ageliaco
    - ageliaco.tracker
    - ageliaco.tracker.behavior
    - ageliaco.tracker.content

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/
  license: GPL
  summary: 'Issue tracker'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
