package:
  name: pymumps
  version: "0.1"

source:
  fn: PyMUMPS-0.1.tar.gz
  url: https://pypi.python.org/packages/source/P/PyMUMPS/PyMUMPS-0.1.tar.gz
  md5: f64dd26c6fd37af15809a3faef9479ba
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pymumps = pymumps:main
    #
    # Would create an entry point called pymumps that calls pymumps.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - mumps
    - pymumps

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/bfroehle/pymumps
  license: BSD License
  summary: 'Python bindings for MUMPS, a parallel sparse direct solver'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
