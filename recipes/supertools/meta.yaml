package:
  name: supertools
  version: "1.0.1"

source:
  fn: supertools-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/s/supertools/supertools-1.0.1.tar.gz
  md5: bec89964765c82231d1466b6e2aae687
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - supertools = supertools:main
    #
    # Would create an entry point called supertools that calls supertools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - supertools

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/gissehel/supertools.git
  license: MIT License
  summary: 'A simple python module to provide .__super ability to python 2 classes.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
