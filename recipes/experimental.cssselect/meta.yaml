package:
  name: experimental.cssselect
  version: "0.3"

source:
  fn: experimental.cssselect-0.3.zip
  url: https://pypi.python.org/packages/source/e/experimental.cssselect/experimental.cssselect-0.3.zip
  md5: 3fecdcf1fbc3ea6025e115a56a262957
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - experimental.cssselect = experimental.cssselect:main
    #
    # Would create an entry point called experimental.cssselect that calls experimental.cssselect.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - lxml

  run:
    - python
    - setuptools
    - lxml

test:
  # Python imports
  imports:
    - experimental

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lrowe/experimental.cssselect
  license: UNKNOWN
  summary: 'Experimental version of lxml.cssselect'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
