package:
  name: django-emails
  version: "1.0"

source:
  fn: django-emails-1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-emails/django-emails-1.0.tar.gz
  md5: 3f4dcdb3aa1e611377d6e60f243f0fee
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-emails = django-emails:main
    #
    # Would create an entry point called django-emails that calls django-emails.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - emails
    - emails.management
    - emails.management.commands
    - example
    - example.emails
    - example.emails.management
    - example.emails.management.commands
    - example.example

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/matllubos/django-emails
  license: GNU General Public License (GPL)
  summary: 'Cron email sender.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
