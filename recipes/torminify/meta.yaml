package:
  name: torminify
  version: "0.1.3"

source:
  fn: torminify-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/t/torminify/torminify-0.1.3.tar.gz
  md5: 84de4446402f5573f1ea2d6749adb66b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - torminify = torminify:main
    #
    # Would create an entry point called torminify that calls torminify.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyyaml
    - tornado

  run:
    - python
    - pyyaml
    - tornado

test:
  # Python imports
  imports:
    - torminify

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/PaulDiakov/torminify
  license: MIT License
  summary: 'Module for Tornado Web Framework, designed to automate minification of css and js files, easily implement asynchronous loading of scripts and additional stylesheets, and cache compiled tornado templates in memory'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
