package:
  name: wtforms-dynamic-fields
  version: "0.1a3"

source:
  fn: WTForms-Dynamic-Fields-0.1a3.tar.gz
  url: https://pypi.python.org/packages/source/W/WTForms-Dynamic-Fields/WTForms-Dynamic-Fields-0.1a3.tar.gz
  md5: b27d8ded0476539a0524b713a70da293
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - wtforms-dynamic-fields = wtforms-dynamic-fields:main
    #
    # Would create an entry point called wtforms-dynamic-fields that calls wtforms-dynamic-fields.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - wtforms >=2.0.1

  run:
    - python
    - wtforms >=2.0.1

test:
  # Python imports
  imports:
    - tests
    - wtforms_dynamic_fields

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/timusan/wtforms-dynamic-fields
  license: BSD License
  summary: 'Simple wrapper to add "dynamic" (sets of) fields to an already instantiated WTForms form.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
