package:
  name: kaathletelist
  version: "1.0.1"

source:
  fn: KaAthleteList-1.0.1.zip
  url: https://pypi.python.org/packages/source/K/KaAthleteList/KaAthleteList-1.0.1.zip
  md5: fe87d1e2207f8bced78b82e4fa24f57a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kaathletelist = kaathletelist:main
    #
    # Would create an entry point called kaathletelist that calls kaathletelist.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - kaathletelist

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.douban.com/people/maolv_kaka/
  license: UNKNOWN
  summary: 'A simple class'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
