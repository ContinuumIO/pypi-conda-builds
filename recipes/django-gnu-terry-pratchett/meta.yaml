package:
  name: django-gnu-terry-pratchett
  version: "0.2.0"

source:
  fn: django-GNU-Terry-Pratchett-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-GNU-Terry-Pratchett/django-GNU-Terry-Pratchett-0.2.0.tar.gz
  md5: e881298d6cf9122b06acacce62a650ba
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-gnu-terry-pratchett = django-gnu-terry-pratchett:main
    #
    # Would create an entry point called django-gnu-terry-pratchett that calls django-gnu-terry-pratchett.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - gnu_terry_pratchett

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/aaronbassett/django-GNU-Terry-Pratchett
  license: BSD License
  summary: "You know they'll never really die while the Trunk is alive"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
