package:
  name: eatiht
  version: "0.1.14"

source:
  fn: eatiht-0.1.14.zip
  url: https://pypi.python.org/packages/source/e/eatiht/eatiht-0.1.14.zip
  md5: 8ce80a1a59458534caada1a10533d80f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - eatiht = eatiht:main
    #
    # Would create an entry point called eatiht that calls eatiht.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - lxml
    - chardet

  run:
    - python
    - lxml
    - chardet

test:
  # Python imports
  imports:
    - eatiht

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rodricios/eatiht
  license: MIT
  summary: "A simple tool used to extract an                   article's text in html documents."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
