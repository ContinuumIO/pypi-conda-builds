package:
  name: jocommentatom
  version: "0.1.3"

source:
  fn: JoCommentAtom-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/J/JoCommentAtom/JoCommentAtom-0.1.3.tar.gz
  md5: 1c73f171d96f4241e260eff89330752c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jocommentatom = jocommentatom:main
    #
    # Would create an entry point called jocommentatom that calls jocommentatom.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyramid
    - sqlalchemy
    - weberror
    - mysql-python
    - rfc3339
    - flup

  run:
    - python
    - setuptools
    - pyramid
    - sqlalchemy
    - weberror
    - mysql-python
    - rfc3339
    - flup

test:
  # Python imports
  imports:
    - jocommentatom
    - jocommentatom.lib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/JoCommentAtom
  license: UNKNOWN
  summary: 'Atom feed generator for JoComment.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
