package:
  name: anz.ijabbar
  version: "1.1"

source:
  fn: anz.ijabbar-1.1.tar.gz
  url: https://pypi.python.org/packages/source/a/anz.ijabbar/anz.ijabbar-1.1.tar.gz
  md5: dbe70f984fd7564cf98d16566f789e7a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - anz.ijabbar = anz.ijabbar:main
    #
    # Would create an entry point called anz.ijabbar that calls anz.ijabbar.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - python-cjson
    - plone.memoize

  run:
    - python
    - setuptools
    - setuptools
    - python-cjson
    - plone.memoize

test:
  # Python imports
  imports:
    - anz
    - anz.ijabbar
    - anz.ijabbar.browser
    - anz.ijabbar.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/anz.ijabbar/
  license: GPL
  summary: 'Integrate iJab(an open source XMPP web chat client recommended by xmpp.org) to your plone site.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
