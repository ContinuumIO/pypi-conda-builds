package:
  name: django-crawler
  version: "0.1"

source:
  fn: django-crawler-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-crawler/django-crawler-0.1.tar.gz
  md5: 433b05a46be0f808e5747d8f441e0013
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-crawler = django-crawler:main
    #
    # Would create an entry point called django-crawler that calls django-crawler.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - crawler
    - crawler.management
    - crawler.management.commands
    - crawler.plugins

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/ericholscher/django-crawler/tree/master
  license: UNKNOWN
  summary: 'A crawler using the Django Test Client'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
