package:
  name: hachoir-wx
  version: "0.3"

source:
  fn: hachoir-wx-0.3.tar.gz
  url: https://pypi.python.org/packages/source/h/hachoir-wx/hachoir-wx-0.3.tar.gz
  md5: ad957029c165d9ba52a6f1ebf92ea647
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hachoir-wx = hachoir-wx:main
    #
    # Would create an entry point called hachoir-wx that calls hachoir-wx.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - hachoir_wx
    - hachoir_wx.field_view
    - hachoir_wx.frame_view
    - hachoir_wx.hex_view
    - hachoir_wx.resource

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://hachoir.org/wiki/hachoir-wx
  license: GNU General Public License (GPL)
  summary: "hachoir-wx is a wxWidgets GUI that's meant to provide a (more) user-friendly interface to the hachoir binary parsing engine"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
