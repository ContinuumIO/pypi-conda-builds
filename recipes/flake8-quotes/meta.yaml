package:
  name: flake8-quotes
  version: "0.0.1"

source:
  fn: flake8-quotes-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/f/flake8-quotes/flake8-quotes-0.0.1.tar.gz
  md5: aafef6c6cca7462c1dfdd86912624ac3
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flake8-quotes = flake8-quotes:main
    #
    # Would create an entry point called flake8-quotes that calls flake8-quotes.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - flake8_quotes

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/zheller/flake8-quotes/
  license: MIT License
  summary: 'Flake8 lint for double quotes.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
