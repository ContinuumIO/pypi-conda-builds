package:
  name: zestyparser
  version: "0.8.1"

source:
  fn: ZestyParser-0.8.1.tar.gz
  url: https://pypi.python.org/packages/source/Z/ZestyParser/ZestyParser-0.8.1.tar.gz
  md5: 51bdb432104fe51287b984d3f985466c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zestyparser = zestyparser:main
    #
    # Would create an entry point called zestyparser that calls zestyparser.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - ZestyParser
    - zestyparser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://adamatlas.org/2006/12/ZestyParser/
  license: MIT License
  summary: 'Write less parsing code. Write nicer parsing code. Have fun with it.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
