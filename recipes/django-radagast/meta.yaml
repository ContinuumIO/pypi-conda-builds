package:
  name: django-radagast
  version: "0.3"

source:
  fn: django-radagast-0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-radagast/django-radagast-0.3.tar.gz
  md5: 89deb5ee353fb752c3d3782617fd4b44
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-radagast = django-radagast:main
    #
    # Would create an entry point called django-radagast that calls django-radagast.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - radagast

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://mozilla.com
  license: BSD
  summary: 'The other wizard'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
