package:
  name: asyncio_mongo
  version: "0.2.3"

source:
  fn: asyncio_mongo-0.2.3.tar.gz
  url: https://pypi.python.org/packages/source/a/asyncio_mongo/asyncio_mongo-0.2.3.tar.gz
  md5: 6c955b68505a357f0284ca291dcc9d7f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - asyncio_mongo = asyncio_mongo:main
    #
    # Would create an entry point called asyncio_mongo that calls asyncio_mongo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - asyncio

  run:
    - python
    - asyncio

test:
  # Python imports
  imports:
    - asyncio_mongo
    - asyncio_mongo._bson
    - asyncio_mongo._pymongo

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/mrdon/asyncio-mongo
  license: Apache Software License
  summary: 'Asynchronous Python 3.3+ driver for MongoDB <http://www.mongodb.org>'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
