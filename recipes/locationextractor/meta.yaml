package:
  name: locationextractor
  version: "0.1.2.3"

source:
  fn: locationExtractor-0.1.2.3.tar.gz
  url: https://pypi.python.org/packages/source/l/locationExtractor/locationExtractor-0.1.2.3.tar.gz
  md5: 42233417674eb3f5f6411497aaa94568
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - locationextractor = locationextractor:main
    #
    # Would create an entry point called locationextractor that calls locationextractor.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - locationExtractor
    - locationextractor

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/fatihsucu/locationExtractor
  license: MIT
  summary: 'Searches location info from source. Tokenizes source and finds appropriate country'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
