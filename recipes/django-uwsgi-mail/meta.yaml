package:
  name: django-uwsgi-mail
  version: "1.1.1"

source:
  fn: django-uwsgi-mail-1.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-uwsgi-mail/django-uwsgi-mail-1.1.1.tar.gz
  md5: 28bc683838c9fc92d844f28ec93d9ba4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-uwsgi-mail = django-uwsgi-mail:main
    #
    # Would create an entry point called django-uwsgi-mail that calls django-uwsgi-mail.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - uwsgi_mail

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jaysonsantos/django-uwsgi-mail
  license: MIT License
  summary: 'A Django backend for e-mail delivery using uWSGI Spool to queue deliveries.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
