package:
  name: products.hitlist
  version: "2.0b4"

source:
  fn: Products.HitList-2.0b4.tar.gz
  url: https://pypi.python.org/packages/source/P/Products.HitList/Products.HitList-2.0b4.tar.gz
  md5: f67f8c4788c40067052ce833da34972e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - products.hitlist = products.hitlist:main
    #
    # Would create an entry point called products.hitlist that calls products.hitlist.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - Products
    - Products.HitList
    - Products.HitList.browser
    - Products.HitList.content

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://svn.plone.org/svn/collective/Products.HitList
  license: GPL
  summary: 'Provides a personal hit-list which may be displayed in a portlet or a view'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
