package:
  name: cubicweb-cmt
  version: "0.7.0"

source:
  fn: cubicweb-cmt-0.7.0.tar.gz
  url: https://pypi.python.org/packages/source/c/cubicweb-cmt/cubicweb-cmt-0.7.0.tar.gz
  md5: 80dec85de900c53deb73273258ff911f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cubicweb-cmt = cubicweb-cmt:main
    #
    # Would create an entry point called cubicweb-cmt that calls cubicweb-cmt.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cubicweb-registration
    - cubicweb >=3.10.0
    - cubicweb-forgotpwd >=0.2.0
    - cubicweb-conference >=0.12.0
    - cubicweb-openidrelay
    - cubicweb-cmcicpay
    - cubicweb-shoppingcart >=0.4.0
    - cubicweb-blog
    - cubicweb-tag

  run:
    - python
    - cubicweb-registration
    - cubicweb >=3.10.0
    - cubicweb-forgotpwd >=0.2.0
    - cubicweb-conference >=0.12.0
    - cubicweb-openidrelay
    - cubicweb-cmcicpay
    - cubicweb-shoppingcart >=0.4.0
    - cubicweb-blog
    - cubicweb-tag

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.cubicweb.org/project/cubicweb-cmt
  license: LCL
  summary: 'conference management tool'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
