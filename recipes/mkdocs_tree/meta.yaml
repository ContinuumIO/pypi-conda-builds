package:
  name: mkdocs_tree
  version: "1.0.1"

source:
  fn: mkdocs_tree-1.0.1.zip
  url: https://pypi.python.org/packages/source/m/mkdocs_tree/mkdocs_tree-1.0.1.zip
  md5: c37a5a66461a20ae24cdfd446fd18b2d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mkdocs_tree = mkdocs_tree:main
    #
    # Would create an entry point called mkdocs_tree that calls mkdocs_tree.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - mkdocs

  run:
    - python
    - mkdocs

test:
  # Python imports
  imports:
    - mkdocs_tree

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/erichulser/mkdocs_autodoc
  license: LGPL
  summary: 'Tree extension for the mkdocs documentation system.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
