package:
  name: identeco
  version: "0.3.2"

source:
  fn: identeco-0.3.2.tar.gz
  url: https://pypi.python.org/packages/source/i/identeco/identeco-0.3.2.tar.gz
  md5: 735dc5caddbe61ccc1873cb87702975a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - identeco = identeco:main
    #
    # Would create an entry point called identeco that calls identeco.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pytz
    - python-openid

  run:
    - python
    - pytz
    - python-openid

test:
  # Python imports
  imports:
    - identeco
    - identeco.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/eldarion/identeco
  license: BSD License
  summary: 'An OpenID Provider for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
