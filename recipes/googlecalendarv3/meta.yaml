package:
  name: googlecalendarv3
  version: "0.1.7"

source:
  fn: GoogleCalendarV3-0.1.7.tar.gz
  url: https://pypi.python.org/packages/source/G/GoogleCalendarV3/GoogleCalendarV3-0.1.7.tar.gz
  md5: 0ed5b1a7b96a5f6f50c0eabfb9812e74
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - googlecalendarv3 = googlecalendarv3:main
    #
    # Would create an entry point called googlecalendarv3 that calls googlecalendarv3.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - requests >=2.3.0
    - requests-oauthlib >=0.4.0

  run:
    - python
    - requests >=2.3.0
    - requests-oauthlib >=0.4.0

test:
  # Python imports
  imports:
    - google_calendar_v3
    - googlecalendarv3

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.github.com/priyadarshy/google-calendar-v3/
  license: LICENSE.txt
  summary: 'Python Client for Google Calendar API V3.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
