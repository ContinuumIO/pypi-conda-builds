package:
  name: ropemode
  version: "0.2"

source:
  fn: ropemode-0.2.tar.gz
  url: https://pypi.python.org/packages/source/r/ropemode/ropemode-0.2.tar.gz
  md5: 9aec4e6f777bf1b245467642fa36d180
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ropemode = ropemode:main
    #
    # Would create an entry point called ropemode that calls ropemode.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - rope >=0.9.4

  run:
    - python
    - rope >=0.9.4

test:
  # Python imports
  imports:
    - ropemode

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://rope.sf.net/
  license: GNU General Public License (GPL)
  summary: 'ropemode, a helper for using rope refactoring library in IDEs'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
