package:
  name: django-excel-response
  version: "1.0"

source:
  fn: django-excel-response-1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-excel-response/django-excel-response-1.0.tar.gz
  md5: 09001aa9c8b6dadbe6111e902adbba32
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-excel-response = django-excel-response:main
    #
    # Would create an entry point called django-excel-response that calls django-excel-response.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - excel_response

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/kmike/django-excel-response/
  license: UNKNOWN
  summary: 'A subclass of HttpResponse which will transform a QuerySet,\nor sequence of sequences, into either an Excel spreadsheet or\nCSV file formatted for Excel, depending on the amount of data.\n\nhttp://djangosnippets.org/snippets/1151/\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
