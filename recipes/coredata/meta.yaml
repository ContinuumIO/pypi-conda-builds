package:
  name: coredata
  version: "0.1.5"

source:
  fn: coredata-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/c/coredata/coredata-0.1.5.tar.gz
  md5: 31e1e5a3b151eb595bb9e40e55213b83
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - coredata = coredata:main
    #
    # Would create an entry point called coredata that calls coredata.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests ==2.3.0
    - enum34 ==1.0

  run:
    - python
    - requests ==2.3.0
    - enum34 ==1.0

test:
  # Python imports
  imports:
    - coredata

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/koddsson/coredata-python-client
  license: UNKNOWN
  summary: 'A client to the Coredata API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
