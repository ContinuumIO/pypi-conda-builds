package:
  name: django-stringfield
  version: "0.3.0"

source:
  fn: django-stringfield-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-stringfield/django-stringfield-0.3.0.tar.gz
  md5: 68c3d5383cd5d3087e3e2e91a9d57872
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-stringfield = django-stringfield:main
    #
    # Would create an entry point called django-stringfield that calls django-stringfield.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - stringfield

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/aino/django-stringfield
  license: BSD License
  summary: 'Better string field for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
