package:
  name: django-localflavor-au-tjh
  version: "1.1.2"

source:
  fn: django-localflavor-au-tjh-1.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-localflavor-au-tjh/django-localflavor-au-tjh-1.1.2.tar.gz
  md5: 53602cae6a1dc9c06b81e7c8d811c828
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-localflavor-au-tjh = django-localflavor-au-tjh:main
    #
    # Would create an entry point called django-localflavor-au-tjh that calls django-localflavor-au-tjh.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4

  run:
    - python
    - django >=1.4

test:
  # Python imports
  imports:
    - django_localflavor_au

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/tim_heap/django-localflavor-au
  license: BSD License
  summary: 'Country-specific Django helpers for Australia.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
