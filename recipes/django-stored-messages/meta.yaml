package:
  name: django-stored-messages
  version: "1.1.0"

source:
  fn: django-stored-messages-1.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-stored-messages/django-stored-messages-1.1.0.tar.gz
  md5: 8c8930c45b4b7aec87d5766438f7807f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-stored-messages = django-stored-messages:main
    #
    # Would create an entry point called django-stored-messages that calls django-stored-messages.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4

  run:
    - python
    - django >=1.4

test:
  # Python imports
  imports:
    - stored_messages

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/evonove/django-stored-messages
  license: BSD License
  summary: 'Django contrib.messages on steroids'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
