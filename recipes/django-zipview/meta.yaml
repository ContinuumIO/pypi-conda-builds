package:
  name: django-zipview
  version: "1.0.0"

source:
  fn: django-zipview-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-zipview/django-zipview-1.0.0.tar.gz
  md5: 2fb44d251d1e58e3750919c6e1aae0b5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-zipview = django-zipview:main
    #
    # Would create an entry point called django-zipview that calls django-zipview.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - zipview

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/thibault/django-zipview/
  license: MIT License
  summary: 'A simple Django base view to zip and stream several files.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
