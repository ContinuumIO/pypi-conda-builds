package:
  name: arartekomaps
  version: "5.60"

source:
  fn: arartekomaps-5.60.zip
  url: https://pypi.python.org/packages/source/a/arartekomaps/arartekomaps-5.60.zip
  md5: f543992c933cbed8873a28c0716ab543
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - arartekomaps = arartekomaps:main
    #
    # Would create an entry point called arartekomaps that calls arartekomaps.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - arartekomaps
    - arartekomaps.api
    - arartekomaps.arartekouser
    - arartekomaps.categories
    - arartekomaps.locations
    - arartekomaps.management
    - arartekomaps.management.commands
    - arartekomaps.mycomment
    - arartekomaps.places
    - arartekomaps.places.templatetags
    - arartekomaps.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ararteko/arartekomaps
  license: GPL
  summary: 'Django application serving mapak.ararteko.net '

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
