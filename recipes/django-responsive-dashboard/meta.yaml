package:
  name: django-responsive-dashboard
  version: "1.1"

source:
  fn: django-responsive-dashboard-1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-responsive-dashboard/django-responsive-dashboard-1.1.tar.gz
  md5: 6d2259ed6756319e8594d9bdde3b8c28
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-responsive-dashboard = django-responsive-dashboard:main
    #
    # Would create an entry point called django-responsive-dashboard that calls django-responsive-dashboard.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - django-positions

  run:
    - python
    - django
    - django-positions

test:
  # Python imports
  imports:
    - responsive_dashboard
    - responsive_dashboard.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/burke-software/django-responsive-dashboard
  license: BSD License
  summary: 'A generic and easy dashboard for Django applications.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
