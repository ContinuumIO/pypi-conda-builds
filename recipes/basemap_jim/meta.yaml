package:
  name: basemap_jim
  version: "0.1.1"

source:
  fn: basemap_Jim-0.1.1.zip
  url: https://pypi.python.org/packages/source/b/basemap_Jim/basemap_Jim-0.1.1.zip
  md5: 682ade0e46f26f1da45b63883f338f56
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - basemap_jim = basemap_jim:main
    #
    # Would create an entry point called basemap_jim that calls basemap_jim.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - urlnorm >=1.1.2

  run:
    - python
    - urlnorm >=1.1.2

test:
  # Python imports
  imports:
    - basemap
    - basemap_jim

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/xhx509/drifter
  license: update soon
  summary: 'basemap of Northwestern Atlantic Coastline for python plotting  .'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
