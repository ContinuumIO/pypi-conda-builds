package:
  name: uciconfig
  version: "0.1.1"

source:
  fn: uciconfig-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/u/uciconfig/uciconfig-0.1.1.tar.gz
  md5: 1ca05730a98000488d22327218fd0a30
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - uciconfig = uciconfig:main
    #
    # Would create an entry point called uciconfig that calls uciconfig.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - uciconfig
    - uciconfig.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://launchpad.net/uci-config
  license: GPLv3
  summary: 'Ubuntu Continuous Integration config framework.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
