package:
  name: collective.portlet.rich
  version: "0.5"

source:
  fn: collective.portlet.rich-0.5.zip
  url: https://pypi.python.org/packages/source/c/collective.portlet.rich/collective.portlet.rich-0.5.zip
  md5: dd71831a80f88ae74cf4553043e6a2c3
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.portlet.rich = collective.portlet.rich:main
    #
    # Would create an entry point called collective.portlet.rich that calls collective.portlet.rich.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - products.cmfplone
    - collective.formlib.link
    - setuptools

  run:
    - python
    - setuptools
    - products.cmfplone
    - collective.formlib.link
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.portlet
    - collective.portlet.rich
    - collective.portlet.rich.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.portlet.rich/
  license: GPL
  summary: 'Rich-text portlet for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
