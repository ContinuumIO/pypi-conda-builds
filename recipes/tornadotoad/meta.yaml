package:
  name: tornadotoad
  version: "0.3"

source:
  fn: tornadotoad-0.3.tar.gz
  url: https://pypi.python.org/packages/source/t/tornadotoad/tornadotoad-0.3.tar.gz
  md5: 27685ee797eb7b34bb52e3492d1c7404
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tornadotoad = tornadotoad:main
    #
    # Would create an entry point called tornadotoad that calls tornadotoad.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - tornadotoad

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kanevski/tornadotoad
  license: https://github.com/kanevski/tornadotoad/blob/master/LICENSE.txt
  summary: "Provides integration of hoptoad's exception notification service into a tornado app."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
