package:
  name: django-localeurl
  version: "2.0.2"

source:
  fn: django-localeurl-2.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-localeurl/django-localeurl-2.0.2.tar.gz
  md5: 7b656fe5e6ecdc6733c2565434f9f00d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-localeurl = django-localeurl:main
    #
    # Would create an entry point called django-localeurl that calls django-localeurl.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - localeurl
    - localeurl.templatetags
    - localeurl.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://django-localeurl.readthedocs.org/
  license: MIT License
  summary: 'A Django application that allow you to specify the language of a page in the URL.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
