package:
  name: json-mapper
  version: "0.1.15"

source:
  fn: json-mapper-0.1.15.tar.gz
  url: https://pypi.python.org/packages/source/j/json-mapper/json-mapper-0.1.15.tar.gz
  md5: 5a9fa532bf792a549b6152e4faec6f13
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - json-mapper = json-mapper:main
    #
    # Would create an entry point called json-mapper that calls json-mapper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - json_mapper

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/SevenQuark/json-mapper
  license: BSD License
  summary: 'Map and parse JSON text to python dict'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
