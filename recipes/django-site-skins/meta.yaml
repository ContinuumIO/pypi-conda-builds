package:
  name: django-site-skins
  version: "0.1-2"

source:
  fn: django-site-skins-0.1-2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-site-skins/django-site-skins-0.1-2.tar.gz
  md5: 3d12012c9ea496dde798002d9444a9ea
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-site-skins = django-site-skins:main
    #
    # Would create an entry point called django-site-skins that calls django-site-skins.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - skins
    - skins.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/bkroeze/django-site-skins/
  license: BSD License
  summary: 'site-skins'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
