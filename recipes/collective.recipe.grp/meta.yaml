package:
  name: collective.recipe.grp
  version: "1.1.0"

source:
  fn: collective.recipe.grp-1.1.0.zip
  url: https://pypi.python.org/packages/source/c/collective.recipe.grp/collective.recipe.grp-1.1.0.zip
  md5: 632f94b1d11f8803b04a0c972e069fba
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.recipe.grp = collective.recipe.grp:main
    #
    # Would create an entry point called collective.recipe.grp that calls collective.recipe.grp.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout

test:
  # Python imports
  imports:
    - collective
    - collective.recipe
    - collective.recipe.grp
    - collective.recipe.grp.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://collective.github.com/collective.recipe.grp/
  license: Zope Public License
  summary: "A window unto Python's Standard Library grp function"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
