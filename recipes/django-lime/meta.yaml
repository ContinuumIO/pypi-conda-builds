package:
  name: django-lime
  version: "0.3"

source:
  fn: django-lime-0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-lime/django-lime-0.3.tar.gz
  md5: 975cdfa323c3fa73bdf5afdc7bf4000a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-lime = django-lime:main
    #
    # Would create an entry point called django-lime that calls django-lime.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six ==1.4.1

  run:
    - python
    - six ==1.4.1

test:
  # Python imports
  imports:
    - lime

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/barseghyanartur/django-lime
  license: GNU General Public License v2 (GPLv2) or GNU Lesser General Public License v2 or later (LGPLv2+)
  summary: 'Mixed content e-mails for Django made simple.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
