package:
  name: django-sslserver
  version: "0.15"

source:
  fn: django-sslserver-0.15.tar.gz
  url: https://pypi.python.org/packages/source/d/django-sslserver/django-sslserver-0.15.tar.gz
  md5: dd83049f0c50f94a1cf3acb6bc7dd6bd
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-sslserver = django-sslserver:main
    #
    # Would create an entry point called django-sslserver that calls django-sslserver.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4

  run:
    - python
    - setuptools
    - django >=1.4

test:
  # Python imports
  imports:
    - sslserver
    - sslserver.management
    - sslserver.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/teddziuba/django-sslserver
  license: MIT
  summary: 'An SSL-enabled development server for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
