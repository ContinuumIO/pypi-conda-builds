package:
  name: zopaclient
  version: "2.0.2"

source:
  fn: zopaClient-2.0.2.tar.gz
  url: https://pypi.python.org/packages/source/z/zopaClient/zopaClient-2.0.2.tar.gz
  md5: 4c46a922661df5a6233efc8c30bfddce
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zopaclient = zopaclient:main
    #
    # Would create an entry point called zopaclient that calls zopaclient.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - zopaclient

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jfparis/ZopaClient
  license: GNU Lesser General Public License v3 or later (LGPLv3+)
  summary: 'python library for Zopa.com'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
