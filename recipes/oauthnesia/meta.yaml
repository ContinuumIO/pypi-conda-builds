package:
  name: oauthnesia
  version: "1.0.5"

source:
  fn: oauthnesia-1.0.5.tar.gz
  url: https://pypi.python.org/packages/source/o/oauthnesia/oauthnesia-1.0.5.tar.gz
  md5: 7dafefeec24db8a147ef45b5318f5788
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - oauthnesia = oauthnesia:main
    #
    # Would create an entry point called oauthnesia that calls oauthnesia.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests
    - requests_oauthlib

  run:
    - python
    - requests
    - requests_oauthlib

test:
  # Python imports
  imports:
    - oauthnesia

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/Urbanesia/oauthnesia-py
  license: MIT
  summary: "Client for Urbanesia's OAUTH v1.0a API"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
