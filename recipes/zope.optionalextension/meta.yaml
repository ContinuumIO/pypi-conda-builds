package:
  name: zope.optionalextension
  version: "1.1"

source:
  fn: zope.optionalextension-1.1.tar.gz
  url: https://pypi.python.org/packages/source/z/zope.optionalextension/zope.optionalextension-1.1.tar.gz
  md5: 83cbda6c7590b6d721958eb6aeb90629
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zope.optionalextension = zope.optionalextension:main
    #
    # Would create an entry point called zope.optionalextension that calls zope.optionalextension.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - zope
    - zope.optionalextension

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/zope.optionalextension
  license: Zope Public License
  summary: 'Optional compilation of C extensions'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
