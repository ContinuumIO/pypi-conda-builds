package:
  name: flake8-immediate
  version: "0.2"

source:
  fn: flake8-immediate-0.2.zip
  url: https://pypi.python.org/packages/source/f/flake8-immediate/flake8-immediate-0.2.zip
  md5: fb231c8a1e938046c55be0590c5e1849
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flake8-immediate = flake8-immediate:main
    #
    # Would create an entry point called flake8-immediate that calls flake8-immediate.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/schlamar/flake8-todo
  license: MIT License
  summary: 'Enables immediate output for flake8.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
