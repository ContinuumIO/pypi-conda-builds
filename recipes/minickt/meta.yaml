package:
  name: minickt
  version: "0.8.6"

source:
  fn: minickt-0.8.6.tar.gz
  url: https://pypi.python.org/packages/source/m/minickt/minickt-0.8.6.tar.gz
  md5: 0f3754d0ee3b2da5050bb410396773cc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - minickt = minickt:main
    #
    # Would create an entry point called minickt that calls minickt.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - matplotlib
    - networkx

  run:
    - python
    - matplotlib
    - networkx

test:
  # Python imports
  imports:
    - cktParser
    - cktWriter
    - minickt

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dokelung/minickt
  license: UNKNOWN
  summary: 'Analyzer and simulator of logic circuit'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
