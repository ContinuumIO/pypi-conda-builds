package:
  name: mingdao
  version: "0.1.4"

source:
  fn: mingdao-0.1.4.zip
  url: https://pypi.python.org/packages/source/m/mingdao/mingdao-0.1.4.zip
  md5: 3aef07a91819118c6227207349893d48
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mingdao = mingdao:main
    #
    # Would create an entry point called mingdao that calls mingdao.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - mingdao

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/ipy/api_python
  license: UNKNOWN
  summary: 'Python SDK for mingdao.com, provides Mingdao OAuth2 authorization and API wrapper.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
