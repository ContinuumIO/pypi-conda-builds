package:
  name: metonym
  version: "0.2"

source:
  fn: metonym-0.2.tar.gz
  url: https://pypi.python.org/packages/source/m/metonym/metonym-0.2.tar.gz
  md5: 2ab9fd068628ee04648e067d137c11d7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - metonym = metonym:main
    #
    # Would create an entry point called metonym that calls metonym.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - nltk >=2.0.1rc1
    - networkx >=1.5

  run:
    - python
    - nltk >=2.0.1rc1
    - networkx >=1.5

test:
  # Python imports
  imports:
    - metonym
    - test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'Getting better path similarity measures from wordnet by graphing the synsets'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
