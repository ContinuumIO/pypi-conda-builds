package:
  name: zgeo.plone.geographer
  version: "0.1.1"

source:
  fn: zgeo.plone.geographer-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/z/zgeo.plone.geographer/zgeo.plone.geographer-0.1.1.tar.gz
  md5: 27570fd1a89f790fd93c008b2d0abf9e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zgeo.plone.geographer = zgeo.plone.geographer:main
    #
    # Would create an entry point called zgeo.plone.geographer that calls zgeo.plone.geographer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zgeo.geographer >=0.3

  run:
    - python
    - setuptools
    - setuptools
    - zgeo.geographer >=0.3

test:
  # Python imports
  imports:
    - zgeo
    - zgeo.plone
    - zgeo.plone.geographer
    - zgeo.plone.geographer.Extensions
    - zgeo.plone.geographer.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://trac.gispython.org/projects/PrimaGIS/wiki/zgeo.plone.geographer
  license: GPL
  summary: 'Geographic annotation for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
