package:
  name: tictail
  version: "0.2.2"

source:
  fn: tictail-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/t/tictail/tictail-0.2.2.tar.gz
  md5: baf3026173f58c5b6baa2d51c69c5f4e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tictail = tictail:main
    #
    # Would create an entry point called tictail that calls tictail.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=2.2.1
    - python-dateutil >=2.2
    - pyasn1 ==0.1.6
    - pyopenssl ==0.13
    - ndg-httpsclient ==0.3.2

  run:
    - python
    - requests >=2.2.1
    - python-dateutil >=2.2
    - pyasn1 ==0.1.6
    - pyopenssl ==0.13
    - ndg-httpsclient ==0.3.2

test:
  # Python imports
  imports:
    - tictail
    - tictail.resource

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tictail/tictail-python
  license: MIT License
  summary: 'Python bindings for the Tictail API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
