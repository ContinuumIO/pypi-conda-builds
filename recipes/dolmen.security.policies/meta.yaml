package:
  name: dolmen.security.policies
  version: "0.3"

source:
  fn: dolmen.security.policies-0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/dolmen.security.policies/dolmen.security.policies-0.3.tar.gz
  md5: c73dd71947c79e9938cc03f4c635ac1b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dolmen.security.policies = dolmen.security.policies:main
    #
    # Would create an entry point called dolmen.security.policies that calls dolmen.security.policies.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zope.annotation
    - zope.security
    - zope.securitypolicy
    - grokcore.component

  run:
    - python
    - setuptools
    - setuptools
    - zope.annotation
    - zope.security
    - zope.securitypolicy
    - grokcore.component

test:
  # Python imports
  imports:
    - dolmen
    - dolmen.security
    - dolmen.security.policies

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.dolmen-project.org
  license: GPL
  summary: 'A collection of security maps baseclasses'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
