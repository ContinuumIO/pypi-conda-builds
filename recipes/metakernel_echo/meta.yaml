package:
  name: metakernel_echo
  version: "0.7.0"

source:
  fn: metakernel_echo-0.7.0.tar.gz
  url: https://pypi.python.org/packages/source/m/metakernel_echo/metakernel_echo-0.7.0.tar.gz
  md5: 43cead6981bbf3c62d0ced2043d41468
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - metakernel_echo = metakernel_echo:main
    #
    # Would create an entry point called metakernel_echo that calls metakernel_echo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - metakernel

  run:
    - python
    - metakernel

test:
  # Python imports
  imports:
    - metakernel_echo

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/calysto/metakernel/tree/master/metakernel_echo
  license: BSD License
  summary: 'A simple echo kernel for Jupyter/IPython'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
