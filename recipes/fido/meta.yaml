package:
  name: fido
  version: "1.1.1"

source:
  fn: fido-1.1.1.tar.gz
  url: https://pypi.python.org/packages/source/f/fido/fido-1.1.1.tar.gz
  md5: 5a287b17f60078c679c4c1e8c22c11ea
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fido = fido:main
    #
    # Would create an entry point called fido that calls fido.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - twisted ==15.0.0
    - crochet
    - service_identity
    - pyopenssl
    - futures

  run:
    - python
    - twisted ==15.0.0
    - crochet
    - service_identity
    - pyopenssl
    - futures

test:
  # Python imports
  imports:
    - fido

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Yelp/fido
  license: Copyright Yelp 2014, All Rights Reserved
  summary: 'Intelligent asynchronous HTTP client'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
