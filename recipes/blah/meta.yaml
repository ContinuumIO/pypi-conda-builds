package:
  name: blah
  version: "0.1.12"

source:
  fn: blah-0.1.12.tar.gz
  url: https://pypi.python.org/packages/source/b/blah/blah-0.1.12.tar.gz
  md5: 2dc524d0eeae5c9c14dcc77da09b2ffe
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - blah = blah:main
    #
    # Would create an entry point called blah that calls blah.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - argparse ==1.2.1
    - catchy >=0.1.0,<0.2

  run:
    - python
    - argparse ==1.2.1
    - catchy >=0.1.0,<0.2

test:
  # Python imports
  imports:
    - blah

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/mwilliamson/blah
  license: UNKNOWN
  summary: 'Thin wrapper around source control systems'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
