package:
  name: mediacore.mediainfo
  version: "0.9.3"

source:
  fn: mediacore.mediainfo-0.9.3.tar.gz
  url: https://pypi.python.org/packages/source/m/mediacore.mediainfo/mediacore.mediainfo-0.9.3.tar.gz
  md5: 06d9fb03d4aa017e88a9648dc92ff5a8
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mediacore.mediainfo = mediacore.mediainfo:main
    #
    # Would create an entry point called mediacore.mediainfo that calls mediacore.mediainfo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - mediacore

  run:
    - python
    - setuptools
    - setuptools
    - mediacore

test:
  # Python imports
  imports:
    - mediacore
    - mediacore.mediainfo

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: gpl
  summary: 'Mediacore.mediainfo provides info about media file as JSON'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
