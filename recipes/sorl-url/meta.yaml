package:
  name: sorl-url
  version: "0.2.1"

source:
  fn: sorl-url-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/s/sorl-url/sorl-url-0.2.1.tar.gz
  md5: cd661adfc436d70883bd4f21a39a45fe
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sorl-url = sorl-url:main
    #
    # Would create an entry point called sorl-url that calls sorl-url.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - sorl_url
    - sorl_url.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bolster/sorl-url
  license: BSD License
  summary: 'Out-of-band processing for sorl-thumbnail thumbnails.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
