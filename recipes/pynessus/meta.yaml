package:
  name: pynessus
  version: "0.9.1"

source:
  fn: pynessus-0.9.1.zip
  url: https://pypi.python.org/packages/source/p/pynessus/pynessus-0.9.1.zip
  md5: dd52eb919cfbf5a128b981ff407a6144
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pynessus = pynessus:main
    #
    # Would create an entry point called pynessus that calls pynessus.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - pynessus
    - pynessus.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/attwad/pynessus
  license: UNKNOWN
  summary: 'Library to talk to a remote Nessus 5 server that via its xmlrpc interface.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
