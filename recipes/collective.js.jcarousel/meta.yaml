package:
  name: collective.js.jcarousel
  version: "1.0"

source:
  fn: collective.js.jcarousel-1.0.zip
  url: https://pypi.python.org/packages/source/c/collective.js.jcarousel/collective.js.jcarousel-1.0.zip
  md5: d893d91d70fc76cfef8ccf807e51a78e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.js.jcarousel = collective.js.jcarousel:main
    #
    # Would create an entry point called collective.js.jcarousel that calls collective.js.jcarousel.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.js
    - collective.js.jcarousel

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.js.jcarousel
  license: GPL
  summary: 'JCarousel registration in Plone resource registry'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
