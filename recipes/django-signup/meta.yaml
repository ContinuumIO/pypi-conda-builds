package:
  name: django-signup
  version: "0.6.0"

source:
  fn: django-signup-0.6.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-signup/django-signup-0.6.0.tar.gz
  md5: 36b595fbfbc68d299c3fc97d29417592
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-signup = django-signup:main
    #
    # Would create an entry point called django-signup that calls django-signup.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - signup
    - signup.management
    - signup.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/fcoelho/django-signup/
  license: BSD License
  summary: 'A user registration app for Django with support for custom user models'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
