package:
  name: django-paranoia
  version: "0.1.9.1"

source:
  fn: django-paranoia-0.1.9.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-paranoia/django-paranoia-0.1.9.1.tar.gz
  md5: 999ed5ff01467a97d5a32201df25852d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-paranoia = django-paranoia:main
    #
    # Would create an entry point called django-paranoia that calls django-paranoia.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - django_paranoia
    - django_paranoia/reporters

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/andymckay/django-paranoia
  license: BSD
  summary: 'OWASP detection point reporting for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
