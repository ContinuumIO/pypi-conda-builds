package:
  name: ec2selector
  version: "1.0.0"

source:
  fn: ec2selector-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/e/ec2selector/ec2selector-1.0.0.tar.gz
  md5: 2403b7d2f83bcffe1b652589b9dce341
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ec2selector = ec2selector:main
    #
    # Would create an entry point called ec2selector that calls ec2selector.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - boto

  run:
    - python
    - boto

test:
  # Python imports
  imports:
    - ec2selector

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ff0000/ec2selector
  license: MIT License
  summary: 'Interactive prompt to select an AMI (Amazon Machine Image) from Amazon EC2'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
