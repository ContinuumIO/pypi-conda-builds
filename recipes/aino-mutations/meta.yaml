package:
  name: aino-mutations
  version: "0.1.0.0"

source:
  fn: aino-mutations-0.1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/a/aino-mutations/aino-mutations-0.1.0.0.tar.gz
  md5: 1a326724de33e052c336c7c0463bf265
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - aino-mutations = aino-mutations:main
    #
    # Would create an entry point called aino-mutations that calls aino-mutations.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - mutations
    - mutations.conf
    - mutations.management
    - mutations.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/aino/aino-mutations/
  license: BSD License
  summary: 'Mutating for evolution using Django and Mercurial'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
