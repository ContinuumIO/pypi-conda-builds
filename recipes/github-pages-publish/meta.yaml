package:
  name: github-pages-publish
  version: "0.1.2"

source:
  fn: github-pages-publish-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/g/github-pages-publish/github-pages-publish-0.1.2.tar.gz
  md5: a554c7c1ed52c3ec0c38903440f16de3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - github-pages-publish = github-pages-publish:main
    #
    # Would create an entry point called github-pages-publish that calls github-pages-publish.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pygit2 >=0.20.0

  run:
    - python
    - pygit2 >=0.20.0

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rafaelmartins/github-pages-publish
  license: BSD License
  summary: 'A script that commits files from a directory to the gh-pages branch of the current Git repository.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
