package:
  name: bayfiles
  version: "0.0.3"

source:
  fn: bayfiles-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/b/bayfiles/bayfiles-0.0.3.tar.gz
  md5: 34d85aa8edbf98883d2b86c47a47bba7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bayfiles = bayfiles:main
    #
    # Would create an entry point called bayfiles that calls bayfiles.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=1.0.0

  run:
    - python
    - requests >=1.0.0

test:
  # Python imports
  imports:
    - bayfiles

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bmillemathias/python-bayfiles
  license: GNU Lesser General Public License v2 (LGPLv2)
  summary: 'A library to upload files to bayfiles.com'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
