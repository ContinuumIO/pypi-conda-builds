package:
  name: json2yaml
  version: "1.1.1"

source:
  fn: json2yaml-1.1.1.tar.gz
  url: https://pypi.python.org/packages/source/j/json2yaml/json2yaml-1.1.1.tar.gz
  md5: c7dfc1a421469606e3b20b2941d241ff
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - json2yaml = json2yaml:main
    #
    # Would create an entry point called json2yaml that calls json2yaml.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyyaml
    - pyaml
    - docopt

  run:
    - python
    - pyyaml
    - pyaml
    - docopt

test:
  # Python imports
  imports:
    - json2yaml

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/drbild/json2yaml
  license: Apache Software License
  summary: 'Convert JSON to YAML or vice versa, while preserving the order of associative arrays.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
