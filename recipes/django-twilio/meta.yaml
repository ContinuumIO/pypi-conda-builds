package:
  name: django-twilio
  version: "0.8.0"

source:
  fn: django-twilio-0.8.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-twilio/django-twilio-0.8.0.tar.gz
  md5: 00e8b045b4d2b756e30517db2e0447d2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-twilio = django-twilio:main
    #
    # Would create an entry point called django-twilio that calls django-twilio.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - twilio >=3.6.9
    - django >=1.4
    - django-phonenumber-field >=0.6

  run:
    - python
    - twilio >=3.6.9
    - django >=1.4
    - django-phonenumber-field >=0.6

test:
  # Python imports
  imports:
    - django_twilio
    - django_twilio.migrations
    - django_twilio.south_migrations
    - test_project
    - test_project.test_app

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://twilio.com/
  license: UNLICENSE
  summary: u'Build Twilio functionality into your Django apps.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
