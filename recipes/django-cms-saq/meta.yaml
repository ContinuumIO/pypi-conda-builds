package:
  name: django-cms-saq
  version: "0.2.3"

source:
  fn: django-cms-saq-0.2.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-cms-saq/django-cms-saq-0.2.3.tar.gz
  md5: bf0d94b4c54b776dbbdec341cffaa0e1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-cms-saq = django-cms-saq:main
    #
    # Would create an entry point called django-cms-saq that calls django-cms-saq.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - beautifulsoup4 >=4.3.1

  run:
    - python
    - beautifulsoup4 >=4.3.1

test:
  # Python imports
  imports:
    - cms_saq
    - cms_saq.management
    - cms_saq.management.commands
    - cms_saq.migrations
    - cms_saq.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Maplecroft/django-cms-saq
  license: LICENSE.txt
  summary: 'Self-assessment questionnaires with Django CMS.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
