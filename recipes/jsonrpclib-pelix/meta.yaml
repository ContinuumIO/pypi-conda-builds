package:
  name: jsonrpclib-pelix
  version: "0.2.5"

source:
  fn: jsonrpclib-pelix-0.2.5.zip
  url: https://pypi.python.org/packages/source/j/jsonrpclib-pelix/jsonrpclib-pelix-0.2.5.zip
  md5: d49be124f0a7dcfabb262e84725f43d5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jsonrpclib-pelix = jsonrpclib-pelix:main
    #
    # Would create an entry point called jsonrpclib-pelix that calls jsonrpclib-pelix.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - jsonrpclib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/tcalmant/jsonrpclib/
  license: Apache Software License
  summary: 'This project is an implementation of the JSON-RPC v2.0 specification (backwards-compatible) as a client library, for Python 2.6+ and Python 3.This version is a fork of jsonrpclib by Josh Marshall, usable with Pelix remote services.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
