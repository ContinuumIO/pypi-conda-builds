package:
  name: django-moderator
  version: "1.1.3"

source:
  fn: django-moderator-1.1.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-moderator/django-moderator-1.1.3.tar.gz
  md5: e3fa143f3dd7dae969ce9506ba99a81c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-moderator = django-moderator:main
    #
    # Would create an entry point called django-moderator that calls django-moderator.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-apptemplates
    - django-likes >=0.0.6
    - redis
    - unidecode
    - django-celery

  run:
    - python
    - django-apptemplates
    - django-likes >=0.0.6
    - redis
    - unidecode
    - django-celery

test:
  # Python imports
  imports:
    - moderator
    - moderator.management
    - moderator.management.commands
    - moderator.migrations
    - moderator.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/praekelt/django-moderator
  license: BSD License
  summary: 'Django Bayesian inference based comment moderation app.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
