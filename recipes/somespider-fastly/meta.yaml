package:
  name: somespider-fastly
  version: "0.0.6"

source:
  fn: somespider-fastly-0.0.6.tar.gz
  url: https://pypi.python.org/packages/source/s/somespider-fastly/somespider-fastly-0.0.6.tar.gz
  md5: 216fa31fab8119966176658638f0383c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - somespider-fastly = somespider-fastly:main
    #
    # Would create an entry point called somespider-fastly that calls somespider-fastly.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - somespider-fastly

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/somespider/fastly-py
  license: MIT License
  summary: 'SomeSpider Fastly python API fork'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
