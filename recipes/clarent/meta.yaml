package:
  name: clarent
  version: "0.1.1"

source:
  fn: clarent-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/c/clarent/clarent-0.1.1.tar.gz
  md5: d7212b26150fd69df0b2d66dc8a00c29
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - clarent = clarent:main
    #
    # Would create an entry point called clarent that calls clarent.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - twisted >=13.2.0
    - txampext >=0.0.10
    - pyopenssl >=0.14

  run:
    - python
    - twisted >=13.2.0
    - txampext >=0.0.10
    - pyopenssl >=0.14

test:
  # Python imports
  imports:
    - clarent
    - clarent.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/crypto101/clarent
  license: ISC License (ISCL)
  summary: 'Shared API definitions for an online exercise system'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
