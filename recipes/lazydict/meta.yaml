package:
  name: lazydict
  version: "1.0.0-beta.1"

source:
  fn: lazydict-1.0.0-beta.1.tar.gz
  url: https://pypi.python.org/packages/source/l/lazydict/lazydict-1.0.0-beta.1.tar.gz
  md5: 80b8751beb113341eee197f052edf98c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lazydict = lazydict:main
    #
    # Would create an entry point called lazydict that calls lazydict.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - lazydict

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/janrain/lazydict
  license: MIT License
  summary: 'Lazily-evaluated dictionaries'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
