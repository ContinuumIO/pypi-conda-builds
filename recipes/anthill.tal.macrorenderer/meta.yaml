package:
  name: anthill.tal.macrorenderer
  version: "0.2.1"

source:
  fn: anthill.tal.macrorenderer-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/a/anthill.tal.macrorenderer/anthill.tal.macrorenderer-0.2.1.tar.gz
  md5: 37a22a2657fa4eede67b57c34d913da7
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - anthill.tal.macrorenderer = anthill.tal.macrorenderer:main
    #
    # Would create an entry point called anthill.tal.macrorenderer that calls anthill.tal.macrorenderer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - anthill
    - anthill.tal
    - anthill.tal.macrorenderer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: GPL
  summary: 'Rendering ZPT macros from python code made easy'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
