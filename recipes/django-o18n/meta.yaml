package:
  name: django-o18n
  version: "1.0"

source:
  fn: django-o18n-1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-o18n/django-o18n-1.0.tar.gz
  md5: 7fd65267e315755938d77ff3efcaa61d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-o18n = django-o18n:main
    #
    # Would create an entry point called django-o18n that calls django-o18n.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - o18n

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/oscaro/django-o18n
  license: BSD License
  summary: "/<country>/<language>/ URL scheme, like Django's i18n_patterns."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
