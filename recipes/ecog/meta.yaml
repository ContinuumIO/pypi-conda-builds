package:
  name: ecog
  version: "0.8.1.1"

source:
  fn: ecog-0.8.1.1.tar.gz
  url: https://pypi.python.org/packages/source/e/ecog/ecog-0.8.1.1.tar.gz
  md5: c99b77c4f985e38e0a9eed2e58634c85
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ecog = ecog:main
    #
    # Would create an entry point called ecog that calls ecog.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - oauth2
    - feedparser
    - python-dateutil

  run:
    - python
    - oauth2
    - feedparser
    - python-dateutil

test:
  # Python imports
  imports:
    - ecog

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jangxyz/ecogwiki-client
  license: MIT License
  summary: 'Ecogwiki OAuth client'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
