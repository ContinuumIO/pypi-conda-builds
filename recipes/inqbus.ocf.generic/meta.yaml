package:
  name: inqbus.ocf.generic
  version: "0.1"

source:
  fn: inqbus.ocf.generic-0.1.tar.gz
  url: https://pypi.python.org/packages/source/i/inqbus.ocf.generic/inqbus.ocf.generic-0.1.tar.gz
  md5: 9f43e2499b025d9b662de2d9c5e9055a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - inqbus.ocf.generic = inqbus.ocf.generic:main
    #
    # Would create an entry point called inqbus.ocf.generic that calls inqbus.ocf.generic.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - inqbus
    - inqbus.ocf
    - inqbus.ocf.generic

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://inqbus.de
  license: GPL
  summary: 'Generic OCF resource agents'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
