package:
  name: gaebusiness
  version: "4.5.2"

source:
  fn: gaebusiness-4.5.2.tar.gz
  url: https://pypi.python.org/packages/source/g/gaebusiness/gaebusiness-4.5.2.tar.gz
  md5: 521b8626bbfc3cdd43c7f9e91555d8dd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gaebusiness = gaebusiness:main
    #
    # Would create an entry point called gaebusiness that calls gaebusiness.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - gaeforms >=0.12
    - pytz >=2014.4
    - babel >=1.3

  run:
    - python
    - gaeforms >=0.12
    - pytz >=2014.4
    - babel >=1.3

test:
  # Python imports
  imports:
    - gaebusiness

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/renzon/gaebusiness
  license: BSD License
  summary: 'Micro Architecture for Business Layer on Google App Engine'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
