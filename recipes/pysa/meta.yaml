package:
  name: pysa
  version: "0.3b5"

source:
  fn: Pysa-0.3b5.tar.gz
  url: https://pypi.python.org/packages/source/P/Pysa/Pysa-0.3b5.tar.gz
  md5: 1a7bba41c19b440c8f1a428e0ee49427
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pysa = pysa:main
    #
    # Would create an entry point called pysa that calls pysa.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - pysa
    - pysa.filter
    - pysa.puppet
    - pysa.salt
    - pysa.scanner
    - pysa.scanner.actions
    - pysa.scanner.object

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://madeiracloud.github.io/pysa/
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'Reverse your Servers Configuration'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
