package:
  name: flask_cache_external_assets
  version: "0.2.0"

source:
  fn: flask_cache_external_assets-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/f/flask_cache_external_assets/flask_cache_external_assets-0.2.0.tar.gz
  md5: 8051cb0286cc6d6488409eeb16716b38
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask_cache_external_assets = flask_cache_external_assets:main
    #
    # Would create an entry point called flask_cache_external_assets that calls flask_cache_external_assets.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - python-magic >=0.4

  run:
    - python
    - flask
    - python-magic >=0.4

test:
  # Python imports
  imports:
    - flask_cache_external_assets

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://camembertaulaitcrew.biz
  license: BSD License
  summary: 'Flask extension for caching external assets'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
