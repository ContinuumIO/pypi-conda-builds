package:
  name: media-hosts
  version: "0.4"

source:
  fn: media-hosts-0.4.tar.gz
  url: https://pypi.python.org/packages/source/m/media-hosts/media-hosts-0.4.tar.gz
  md5: 69a5daf8122b851b1d9dafc2907ec48d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - media-hosts = media-hosts:main
    #
    # Would create an entry point called media-hosts that calls media-hosts.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - miss >=0.3
    - requests

  run:
    - python
    - miss >=0.3
    - requests

test:
  # Python imports
  imports:
    - media_hosts
    - media_hosts.backends
    - miss

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Changaco/python-media-hosts
  license: LGPLv3+
  summary: 'Gets media info from sites like Youtube and Soundcloud'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
