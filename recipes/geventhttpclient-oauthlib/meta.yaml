package:
  name: geventhttpclient-oauthlib
  version: "0.1a"

source:
  fn: geventhttpclient-oauthlib-0.1a.tar.gz
  url: https://pypi.python.org/packages/source/g/geventhttpclient-oauthlib/geventhttpclient-oauthlib-0.1a.tar.gz
  md5: cafa83d93f6ead157f82759604d05ae9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - geventhttpclient-oauthlib = geventhttpclient-oauthlib:main
    #
    # Would create an entry point called geventhttpclient-oauthlib that calls geventhttpclient-oauthlib.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - geventhttpclient >=1.0a
    - oauthlib >=0.3.0

  run:
    - python
    - geventhttpclient >=1.0a
    - oauthlib >=0.3.0

test:
  # Python imports
  imports:
    - geventhttpclient_oauthlib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jneight/geventhttpclient-oauthlib
  license: BSD License
  summary: 'Add support for OAuth to geventHTTPclient'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
