package:
  name: django-comments-moderation
  version: "0.1"

source:
  fn: django-comments-moderation-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-comments-moderation/django-comments-moderation-0.1.tar.gz
  md5: b42296d84c0e645eab0f24b5f62729b2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-comments-moderation = django-comments-moderation:main
    #
    # Would create an entry point called django-comments-moderation that calls django-comments-moderation.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-fluent-comments >=0.9.2

  run:
    - python
    - django-fluent-comments >=0.9.2

test:
  # Python imports
  imports:
    - comments_moderation
    - comments_moderation.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/PetrDlouhy/django-comments-moderation
  license: GNU Affero General Public License v3
  summary: 'Moderation plugin for django-fluent-comments'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
