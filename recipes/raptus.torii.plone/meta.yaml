package:
  name: raptus.torii.plone
  version: "1.0b1"

source:
  fn: raptus.torii.plone-1.0b1.tar.gz
  url: https://pypi.python.org/packages/source/r/raptus.torii.plone/raptus.torii.plone-1.0b1.tar.gz
  md5: bd79d08a3b9af1774891f5d6b25f0a82
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - raptus.torii.plone = raptus.torii.plone:main
    #
    # Would create an entry point called raptus.torii.plone that calls raptus.torii.plone.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - raptus
    - raptus.torii
    - raptus.torii.plone

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/raptus.plone
  license: GPL
  summary: 'extends raptus.torii'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
