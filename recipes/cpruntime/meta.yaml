package:
  name: cpruntime
  version: "0.0.7"

source:
  fn: cpruntime-0.0.7.tar.gz
  url: https://pypi.python.org/packages/source/c/cpruntime/cpruntime-0.0.7.tar.gz
  md5: db40d836c2da506dbb05fa657b66d9ad
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cpruntime = cpruntime:main
    #
    # Would create an entry point called cpruntime that calls cpruntime.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - cpruntime

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lipengyuntian
  license: GNU General Public License (GPL)
  summary: 'python module for cloudpi'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
