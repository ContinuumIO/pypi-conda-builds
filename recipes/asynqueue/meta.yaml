package:
  name: asynqueue
  version: "0.8.2"

source:
  fn: AsynQueue-0.8.2.tar.gz
  url: https://pypi.python.org/packages/source/A/AsynQueue/AsynQueue-0.8.2.tar.gz
  md5: 5263e380acd69631a15add1965a6071b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - asynqueue = asynqueue:main
    #
    # Would create an entry point called asynqueue that calls asynqueue.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - twisted

  run:
    - python
    - twisted

test:
  # Python imports
  imports:
    - asynqueue

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://edsuom.com/AsynQueue.html
  license: GNU General Public License v3 or later (GPLv3+)
  summary: ' Asynchronous task queueing based on the Twisted framework. '

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
