package:
  name: flask-moresql
  version: "0.4"

source:
  fn: Flask-MoreSQL-0.4.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-MoreSQL/Flask-MoreSQL-0.4.tar.gz
  md5: 9f1852258a5b2a78a14682e24fb3ef97
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-moresql = flask-moresql:main
    #
    # Would create an entry point called flask-moresql that calls flask-moresql.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - psycopg2
    - simplejson

  run:
    - python
    - flask
    - psycopg2
    - simplejson

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/ema/flask-moresql
  license: BSD License
  summary: 'Call PostgreSQL stored procedures from Flask'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
