package:
  name: dispatchonvalue
  version: "0.9.6"

source:
  fn: dispatchonvalue-0.9.6.tar.gz
  url: https://pypi.python.org/packages/source/d/dispatchonvalue/dispatchonvalue-0.9.6.tar.gz
  md5: fa8d382eb1d48d3cd63abb81324ad308
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dispatchonvalue = dispatchonvalue:main
    #
    # Would create an entry point called dispatchonvalue that calls dispatchonvalue.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - six >=1.7.3

  run:
    - python
    - six >=1.7.3

test:
  # Python imports
  imports:
    - dispatchonvalue
    - dispatchonvalue.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/minimind/dispatch-on-value-for-python
  license: MIT License
  summary: 'Provides the ability to dispatch on values using pattern matching on complex, nested data structures containing lists, dictionaries and primitive types'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
