package:
  name: glamkit
  version: "0.5.3"

source:
  fn: glamkit-0.5.3.tar.gz
  url: https://pypi.python.org/packages/source/g/glamkit/glamkit-0.5.3.tar.gz
  md5: fcfde8b92ffaf0e6caf6699c75268c7e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - glamkit = glamkit:main
    #
    # Would create an entry point called glamkit that calls glamkit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - glamkit
    - glamkit.management
    - glamkit.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/glamkit/glamkit
  license: BSD License
  summary: 'A Django toolkit for building websites for the Galleries, Libraries, Archives and Museums sector.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
