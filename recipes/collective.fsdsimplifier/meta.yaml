package:
  name: collective.fsdsimplifier
  version: "1.3"

source:
  fn: collective.fsdsimplifier-1.3.zip
  url: https://pypi.python.org/packages/source/c/collective.fsdsimplifier/collective.fsdsimplifier-1.3.zip
  md5: 8671e6a6c081b1538ce8d0f9b55a6095
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.fsdsimplifier = collective.fsdsimplifier:main
    #
    # Would create an entry point called collective.fsdsimplifier that calls collective.fsdsimplifier.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.facultystaffdirectory
    - archetypes.schemaextender

  run:
    - python
    - setuptools
    - setuptools
    - products.facultystaffdirectory
    - archetypes.schemaextender

test:
  # Python imports
  imports:
    - collective
    - collective.fsdsimplifier
    - collective.fsdsimplifier.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/
  license: GPL
  summary: 'Simplifies FacultyStaffDirectory (FSD) for the end-user.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
