package:
  name: cs.kontratazioa
  version: "2.2"

source:
  fn: cs.kontratazioa-2.2.zip
  url: https://pypi.python.org/packages/source/c/cs.kontratazioa/cs.kontratazioa-2.2.zip
  md5: 4ad1b9f150b562e9c5ca079514f79e85
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cs.kontratazioa = cs.kontratazioa:main
    #
    # Would create an entry point called cs.kontratazioa that calls cs.kontratazioa.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone

  run:
    - python
    - setuptools
    - setuptools
    - plone

test:
  # Python imports
  imports:
    - cs
    - cs.kontratazioa
    - cs.kontratazioa.browser
    - cs.kontratazioa.content
    - cs.kontratazioa.interfaces

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/codesyntax/cs.kontratazioa
  license: GNU General Public License (GPL)
  summary: "A product to publish public contract information in Spanish Public Administration's websites"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
