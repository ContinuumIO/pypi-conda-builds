package:
  name: openerp-membership
  version: "7.0.406"

source:
  fn: openerp-membership-7.0.406.tar.gz
  url: https://pypi.python.org/packages/source/o/openerp-membership/openerp-membership-7.0.406.tar.gz
  md5: e606497c9618c2505f373a471ce92e66
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - openerp-membership = openerp-membership:main
    #
    # Would create an entry point called openerp-membership that calls openerp-membership.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - openerp-core
    - openerp-product
    - openerp-account
    - openerp-process

  run:
    - python
    - openerp-core
    - openerp-product
    - openerp-account
    - openerp-process

test:
  # Python imports
  imports:
    - openerp.addons.membership
    - openerp.addons.membership.report
    - openerp.addons.membership.wizard

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: GNU Affero General Public License v3
  summary: 'Membership Management'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
