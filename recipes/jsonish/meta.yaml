package:
  name: jsonish
  version: "0.1.1"

source:
  fn: jsonish-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/j/jsonish/jsonish-0.1.1.tar.gz
  md5: a8bb0b581234ac905c03022c66dd5506
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jsonish = jsonish:main
    #
    # Would create an entry point called jsonish that calls jsonish.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - simplejson

  run:
    - python
    - simplejson

test:
  # Python imports
  imports:
    - jsonish

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://ish.io
  license: UNKNOWN
  summary: 'extensible json extension lib. Includes module for python types (datetime, date, decimal, time)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
