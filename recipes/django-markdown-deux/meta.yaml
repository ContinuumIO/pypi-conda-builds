package:
  name: django-markdown-deux
  version: "1.0.5"

source:
  fn: django-markdown-deux-1.0.5.zip
  url: https://pypi.python.org/packages/source/d/django-markdown-deux/django-markdown-deux-1.0.5.zip
  md5: 6e3016740f3164020ff93882bc7a6cf0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-markdown-deux = django-markdown-deux:main
    #
    # Would create an entry point called django-markdown-deux that calls django-markdown-deux.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - markdown2

  run:
    - python
    - markdown2

test:
  # Python imports
  imports:
    - markdown_deux

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/trentm/django-markdown-deux
  license: MIT License
  summary: 'a Django app that provides template tags for using Markdown (using the python-markdown2 processor)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
