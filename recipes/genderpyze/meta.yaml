package:
  name: genderpyze
  version: "0.1.8"

source:
  fn: genderpyze-0.1.8.tar.gz
  url: https://pypi.python.org/packages/source/g/genderpyze/genderpyze-0.1.8.tar.gz
  md5: c8ebde370cfc9da30bf987118ff74b13
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - genderpyze = genderpyze:main
    #
    # Would create an entry point called genderpyze that calls genderpyze.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=2.4.0

  run:
    - python
    - requests >=2.4.0

test:
  # Python imports
  imports:
    - genderpyze

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tttthomasssss/genderpyze
  license: MIT License
  summary: 'genderize.io connector'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
