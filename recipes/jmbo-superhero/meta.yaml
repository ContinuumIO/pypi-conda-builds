package:
  name: jmbo-superhero
  version: "0.1"

source:
  fn: jmbo-superhero-0.1.tar.gz
  url: https://pypi.python.org/packages/source/j/jmbo-superhero/jmbo-superhero-0.1.tar.gz
  md5: 5c94d1299c4f1c46171e4d90e31bd72c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jmbo-superhero = jmbo-superhero:main
    #
    # Would create an entry point called jmbo-superhero that calls jmbo-superhero.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - jmbo >=1.1.1

  run:
    - python
    - jmbo >=1.1.1

test:
  # Python imports
  imports:
    - superhero
    - superhero.management
    - superhero.management.commands
    - superhero.migrations
    - superhero.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/praekelt/jmbo-superhero
  license: BSD License
  summary: 'Jmbo superhero application.  A content type that represents a bundle of HTML, JS and CSS on the filesystem.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
