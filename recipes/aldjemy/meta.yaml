package:
  name: aldjemy
  version: "0.4.0"

source:
  fn: aldjemy-0.4.0.tar.gz
  url: https://pypi.python.org/packages/source/a/aldjemy/aldjemy-0.4.0.tar.gz
  md5: ccfbce54c37145a666ba63e5e575969f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - aldjemy = aldjemy:main
    #
    # Would create an entry point called aldjemy that calls aldjemy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sqlalchemy >0.7.1

  run:
    - python
    - sqlalchemy >0.7.1

test:
  # Python imports
  imports:
    - aldjemy
    - test_project
    - test_project.a_sample
    - test_project.aldjemy
    - test_project.sample

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Deepwalker/aldjemy/
  license: BSD License
  summary: 'SQLAlchemy to Django integration library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
