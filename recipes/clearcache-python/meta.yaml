package:
  name: clearcache-python
  version: "0.0.8"

source:
  fn: clearcache-python-0.0.8.tar.gz
  url: https://pypi.python.org/packages/source/c/clearcache-python/clearcache-python-0.0.8.tar.gz
  md5: f48a65aa40bcb9ff6a1e162b9b0b41c6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - clearcache-python = clearcache-python:main
    #
    # Would create an entry point called clearcache-python that calls clearcache-python.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - clearcache

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/UCF/clearcache-python
  license: UNKNOWN
  summary: 'Simple python cache clearing tool'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
