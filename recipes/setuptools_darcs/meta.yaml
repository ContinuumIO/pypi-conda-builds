package:
  name: setuptools_darcs
  version: "1.2.11"

source:
  fn: setuptools_darcs-1.2.11.tar.gz
  url: https://pypi.python.org/packages/source/s/setuptools_darcs/setuptools_darcs-1.2.11.tar.gz
  md5: 79451a8e77b9692861a655e53df094f0
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - setuptools_darcs = setuptools_darcs:main
    #
    # Would create an entry point called setuptools_darcs that calls setuptools_darcs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - setuptools_darcs

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://tahoe-lafs.org/trac/setuptools_darcs
  license: BSD License
  summary: 'setuptools plugin for darcs'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
