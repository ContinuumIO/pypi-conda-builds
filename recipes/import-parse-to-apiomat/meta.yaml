package:
  name: import-parse-to-apiomat
  version: "1.0"

source:
  fn: import-parse-to-apiOmat-1.0.tar.gz
  url: https://pypi.python.org/packages/source/i/import-parse-to-apiOmat/import-parse-to-apiOmat-1.0.tar.gz
  md5: 622a80284ceaa7d9a5476f9abc20cc80
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - import-parse-to-apiomat = import-parse-to-apiomat:main
    #
    # Would create an entry point called import-parse-to-apiomat that calls import-parse-to-apiomat.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - ImportParseData

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/apiOmat/apiOmat-import-parse
  license: UNKNOWN
  summary: 'Import your data from Parse to apiOmat'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
