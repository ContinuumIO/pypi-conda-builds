package:
  name: dtotools
  version: "0.1.0.dev1"

source:
  fn: DtoTools-0.1.0.dev1.tar.gz
  url: https://pypi.python.org/packages/source/D/DtoTools/DtoTools-0.1.0.dev1.tar.gz
  md5: 8b7c8cd246748a6560d567c3df709aa1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dtotools = dtotools:main
    #
    # Would create an entry point called dtotools that calls dtotools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six

  run:
    - python
    - six

test:
  # Python imports
  imports:
    - dtotools

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/TomasFlam/python-dtotools
  license: BSD License
  summary: 'Data Transfer Object library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
