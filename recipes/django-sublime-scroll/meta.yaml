package:
  name: django-sublime-scroll
  version: "0.2.2"

source:
  fn: django-sublime-scroll-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-sublime-scroll/django-sublime-scroll-0.2.2.tar.gz
  md5: 4978d6b8b0d0bb27dc04f56bd9d1f7ec
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-sublime-scroll = django-sublime-scroll:main
    #
    # Would create an entry point called django-sublime-scroll that calls django-sublime-scroll.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - sublime_scroll
    - sublime_scroll.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/demux/django-sublime-scroll
  license: BSD License
  summary: 'Sublime Text 2 editor style scroll bars.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
