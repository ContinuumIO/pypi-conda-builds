package:
  name: urlobject
  version: "2.4.0"

source:
  fn: URLObject-2.4.0.tar.gz
  url: https://pypi.python.org/packages/source/U/URLObject/URLObject-2.4.0.tar.gz
  md5: 2ed819738a9f0a3051f31dc9924e3065
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - urlobject = urlobject:main
    #
    # Would create an entry point called urlobject that calls urlobject.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - urlobject

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/zacharyvoase/urlobject
  license: UNKNOWN
  summary: 'A utility class for manipulating URLs.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
