package:
  name: django_compressor
  version: "1.5"

source:
  fn: django_compressor-1.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django_compressor/django_compressor-1.5.tar.gz
  md5: 274dd82a45ddd13e111852dd101a5924
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_compressor = django_compressor:main
    #
    # Would create an entry point called django_compressor that calls django_compressor.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-appconf >=0.4

  run:
    - python
    - django-appconf >=0.4

test:
  # Python imports
  imports:
    - compressor
    - compressor.contrib
    - compressor.filters
    - compressor.filters.cssmin
    - compressor.filters.jsmin
    - compressor.management
    - compressor.management.commands
    - compressor.offline
    - compressor.parser
    - compressor.templatetags
    - compressor.tests
    - compressor.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

# requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://django-compressor.readthedocs.org/en/latest/
  license: MIT License
  summary: 'Compresses linked and inline JavaScript or CSS into single cached files.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
