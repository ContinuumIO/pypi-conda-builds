package:
  name: collective.portlet.foldercontents
  version: "1.0"

source:
  fn: collective.portlet.foldercontents-1.0.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.portlet.foldercontents/collective.portlet.foldercontents-1.0.tar.gz
  md5: e46cee165e89bc9de9d4402ae7828342
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.portlet.foldercontents = collective.portlet.foldercontents:main
    #
    # Would create an entry point called collective.portlet.foldercontents that calls collective.portlet.foldercontents.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.portlet
    - collective.portlet.foldercontents
    - collective.portlet.foldercontents.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://svn.plone.org/svn/collective/collective.portlet.foldercontents/trunk
  license: GPL
  summary: "This package provides portlet to list your Plone folder's contents."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
