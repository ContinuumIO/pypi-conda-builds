package:
  name: cjktools
  version: "1.6.0"

source:
  fn: cjktools-1.6.0.tar.gz
  url: https://pypi.python.org/packages/source/c/cjktools/cjktools-1.6.0.tar.gz
  md5: 48bd0f6a652b6331ce864aa65ce58b86
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cjktools = cjktools:main
    #
    # Would create an entry point called cjktools that calls cjktools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - simplestats

  run:
    - python
    - simplestats

test:
  # Python imports
  imports:
    - cjktools
    - cjktools.resources

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/larsyencken/cjktools/
  license: BSD
  summary: 'A library for basic CJK processing and lexicography.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
