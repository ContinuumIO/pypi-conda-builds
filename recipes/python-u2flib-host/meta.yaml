package:
  name: python-u2flib-host
  version: "2.1.0"

source:
  fn: python-u2flib-host-2.1.0.tar.gz
  url: https://pypi.python.org/packages/source/p/python-u2flib-host/python-u2flib-host-2.1.0.tar.gz
  md5: 043323c11fc0e66d2e35378fb0c28ea1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-u2flib-host = python-u2flib-host:main
    #
    # Would create an entry point called python-u2flib-host that calls python-u2flib-host.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyscard
    - requests
    - hidapi >=0.7.99

  run:
    - python
    - pyscard
    - requests
    - hidapi >=0.7.99

test:
  # Python imports
  imports:
    - u2flib_host

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Yubico/python-u2flib-host
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'Python based U2F host library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
