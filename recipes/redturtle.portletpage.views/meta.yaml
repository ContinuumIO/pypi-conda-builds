package:
  name: redturtle.portletpage.views
  version: "0.6.0"

source:
  fn: redturtle.portletpage.views-0.6.0.tar.gz
  url: https://pypi.python.org/packages/source/r/redturtle.portletpage.views/redturtle.portletpage.views-0.6.0.tar.gz
  md5: 4766cc73f2f66d07d0ed20994ff64856
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - redturtle.portletpage.views = redturtle.portletpage.views:main
    #
    # Would create an entry point called redturtle.portletpage.views that calls redturtle.portletpage.views.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.portletpage

  run:
    - python
    - setuptools
    - setuptools
    - collective.portletpage

test:
  # Python imports
  imports:
    - redturtle
    - redturtle.portletpage
    - redturtle.portletpage.views
    - redturtle.portletpage.views.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/redturtle.portletpage.views
  license: GPL
  summary: 'Additional views for Plone collective.portletpage product'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
