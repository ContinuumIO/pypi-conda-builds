package:
  name: eventize
  version: "0.4.3"

source:
  fn: eventize-0.4.3.tar.gz
  url: https://pypi.python.org/packages/source/e/eventize/eventize-0.4.3.tar.gz
  md5: ad3d20421ac14a936361828bb5e8671e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - eventize = eventize:main
    #
    # Would create an entry point called eventize that calls eventize.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - eventize
    - eventize.api
    - eventize.attribute
    - eventize.descriptors
    - eventize.events
    - eventize.method

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.python.org/pypi/eventize
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Add events to object methods and attributes'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
