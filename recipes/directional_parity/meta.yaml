package:
  name: directional_parity
  version: "0.0.0"

source:
  fn: directional_parity-0.0.0.tar.gz
  url: https://pypi.python.org/packages/source/d/directional_parity/directional_parity-0.0.0.tar.gz
  md5: af5de60f97701b0244181e95a76cbbab
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - directional_parity = directional_parity:main
    #
    # Would create an entry point called directional_parity that calls directional_parity.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - directional_parity

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/adamdeprince/wnyc_systems
  license: UNKNOWN
  summary: 'Direction detecting parity - developed for project.wnyc.org/cicadas'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
