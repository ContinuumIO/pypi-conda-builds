package:
  name: raptus.recipe.torii
  version: "1.0b1"

source:
  fn: raptus.recipe.torii-1.0b1.tar.gz
  url: https://pypi.python.org/packages/source/r/raptus.recipe.torii/raptus.recipe.torii-1.0b1.tar.gz
  md5: bc991436a674db4f741c12bf44116c6b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - raptus.recipe.torii = raptus.recipe.torii:main
    #
    # Would create an entry point called raptus.recipe.torii that calls raptus.recipe.torii.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - zc.recipe.egg
    - zc.buildout

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - zc.recipe.egg
    - zc.buildout

test:
  # Python imports
  imports:
    - raptus
    - raptus.recipe
    - raptus.recipe.torii
    - raptus.recipe.torii.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/raptus.recipe.torii
  license: GPL
  summary: 'buildout recipe for installing torii'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
