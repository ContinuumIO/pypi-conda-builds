package:
  name: behave_cli
  version: "0.2"

source:
  fn: behave_cli-0.2.tar.gz
  url: https://pypi.python.org/packages/source/b/behave_cli/behave_cli-0.2.tar.gz
  md5: 735a76953333e0944229b333cbf8cbb2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - behave_cli = behave_cli:main
    #
    # Would create an entry point called behave_cli that calls behave_cli.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - behave
    - pexpect
    - requests
    - moto
    - mock

  run:
    - python
    - behave
    - pexpect
    - requests
    - moto
    - mock

test:
  # Python imports
  imports:
    - behave_cli
    - behave_cli.api
    - behave_cli.expect
    - behave_cli.files
    - behave_cli.netrc
    - behave_cli.s3mock

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/longaccess/longaccess-behave/
  license: Apache Software License
  summary: 'Longaccess BDD CLI step library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
