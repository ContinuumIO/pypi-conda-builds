package:
  name: django-push2
  version: "0.3.12"

source:
  fn: django-push2-0.3.12.tar.gz
  url: https://pypi.python.org/packages/source/d/django-push2/django-push2-0.3.12.tar.gz
  md5: 20caf2b3376b6c2923d9c1724644d564
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-push2 = django-push2:main
    #
    # Would create an entry point called django-push2 that calls django-push2.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django ==1.3

  run:
    - python
    - django ==1.3

test:
  # Python imports
  imports:
    - push2

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/cellarosi/django-push
  license: UNKNOWN
  summary: 'Django push engine for web application'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
