package:
  name: omr
  version: "0.0.73"

source:
  fn: omr-0.0.73.tar.gz
  url: https://pypi.python.org/packages/source/o/omr/omr-0.0.73.tar.gz
  md5: 4cd6fe12827e7431e3063d9a4fee14f3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - omr = omr:main
    #
    # Would create an entry point called omr that calls omr.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy >=1.8.0
    - pillow >=2.2.1
    - openpyxl >=1.6.2
    - pyyaml >=3.10

  run:
    - python
    - numpy >=1.8.0
    - pillow >=2.2.1
    - openpyxl >=1.6.2
    - pyyaml >=3.10

test:
  # Python imports
  imports:
    - omr
    - test_omr

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/GregoryCMiller/omr
  license: UNKNOWN
  summary: 'Bubble Vision: Optical Mark Reader'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
