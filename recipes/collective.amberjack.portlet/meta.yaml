package:
  name: collective.amberjack.portlet
  version: "1.1"

source:
  fn: collective.amberjack.portlet-1.1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.amberjack.portlet/collective.amberjack.portlet-1.1.tar.gz
  md5: 0003237471534fdeecfb8dfbf0b985e0
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.amberjack.portlet = collective.amberjack.portlet:main
    #
    # Would create an entry point called collective.amberjack.portlet that calls collective.amberjack.portlet.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.amberjack.core
    - plone.app.portlets
    - plone.portlets

  run:
    - python
    - setuptools
    - setuptools
    - collective.amberjack.core
    - plone.app.portlets
    - plone.portlets

test:
  # Python imports
  imports:
    - collective
    - collective.amberjack
    - collective.amberjack.portlet
    - collective.amberjack.portlet.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/collective.amberjack.portlet
  license: GNU General Public License (GPL)
  summary: 'Collective amberjack tours portlet'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
