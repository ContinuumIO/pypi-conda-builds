package:
  name: djkeeper
  version: "0.2.4"

source:
  fn: djkeeper-0.2.4.tar.gz
  url: https://pypi.python.org/packages/source/d/djkeeper/djkeeper-0.2.4.tar.gz
  md5: 9b467e73410e271409591ed0134f7b06
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djkeeper = djkeeper:main
    #
    # Would create an entry point called djkeeper that calls djkeeper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pykeeper

  run:
    - python
    - pykeeper

test:
  # Python imports
  imports:
    - djkeeper

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/nkvoll/djkeeper
  license: MIT License
  summary: 'Utilities for using ZooKeeper from a Django project.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
