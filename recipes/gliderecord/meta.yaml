package:
  name: gliderecord
  version: "0.012"

source:
  fn: GlideRecord-0.012.zip
  url: https://pypi.python.org/packages/source/G/GlideRecord/GlideRecord-0.012.zip
  md5: 2fa4a4da6c551ec270fd64e590920202
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gliderecord = gliderecord:main
    #
    # Would create an entry point called gliderecord that calls gliderecord.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - GlideRecord
    - gliderecord

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bazizi/ServiceNow_GlideRecord_API.git
  license: UNKNOWN
  summary: 'ServiceNow GlideRecord API for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
