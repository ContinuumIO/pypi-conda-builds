package:
  name: easyshop.kss
  version: "0.1a1"

source:
  fn: easyshop.kss-0.1a1.tar.gz
  url: https://pypi.python.org/packages/source/e/easyshop.kss/easyshop.kss-0.1a1.tar.gz
  md5: 8d2140e10e45a2a71ed6982147ff1634
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - easyshop.kss = easyshop.kss:main
    #
    # Would create an entry point called easyshop.kss that calls easyshop.kss.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - easyshop
    - easyshop.kss
    - easyshop.kss.customers
    - easyshop.kss.profiles

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://iqpp.de
  license: GPL
  summary: 'KSS layer for EasyShop'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
