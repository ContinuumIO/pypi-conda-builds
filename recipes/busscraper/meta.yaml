package:
  name: busscraper
  version: "0.2.2"

source:
  fn: BusScraper-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/B/BusScraper/BusScraper-0.2.2.tar.gz
  md5: 76c8c7dc9857c3ceec243b14a46beb6b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - busscraper = busscraper:main
    #
    # Would create an entry point called busscraper that calls busscraper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - httplib2
    - beautifulsoup

  run:
    - python
    - httplib2
    - beautifulsoup

test:
  # Python imports
  imports:
    - busscraper
    - test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/BusScraper/
  license: LICENSE.txt
  summary: 'Scraper for ACIS based bus time sites'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
