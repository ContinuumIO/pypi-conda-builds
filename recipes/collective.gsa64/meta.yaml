package:
  name: collective.gsa64
  version: "1.0-beta4"

source:
  fn: collective.gsa64-1.0-beta4.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.gsa64/collective.gsa64-1.0-beta4.tar.gz
  md5: 9c4cd1ccdebc2dc5b5e79e0d001cd451
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.gsa64 = collective.gsa64:main
    #
    # Would create an entry point called collective.gsa64 that calls collective.gsa64.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - lxml
    - plone.app.registry
    - plone.app.controlpanel

  run:
    - python
    - setuptools
    - setuptools
    - lxml
    - plone.app.registry
    - plone.app.controlpanel

test:
  # Python imports
  imports:
    - collective
    - collective.gsa64
    - collective.gsa64.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: GPL
  summary: 'Feeds protocol implementation and search integration'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
