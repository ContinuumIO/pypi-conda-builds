package:
  name: bops
  version: "0.5"

source:
  fn: bops-0.5.zip
  url: https://pypi.python.org/packages/source/b/bops/bops-0.5.zip
  md5: be7e2b627efbb1dfde11a4a1dd2de12d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bops = bops:main
    #
    # Would create an entry point called bops that calls bops.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - bops
    - doc
    - examples

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://packages.python.org/bops
  license: MIT License
  summary: 'A non-distributed numpy-based analysis module focusing on the manipulation,                     grouping and filtering of data from various sources.                     Bops also has map-reduce functionality.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
