package:
  name: pyramid_contextauth
  version: "0.7.3"

source:
  fn: pyramid_contextauth-0.7.3.tar.gz
  url: https://pypi.python.org/packages/source/p/pyramid_contextauth/pyramid_contextauth-0.7.3.tar.gz
  md5: 56f90e47d0de7800fac75d4de97fc861
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pyramid_contextauth = pyramid_contextauth:main
    #
    # Would create an entry point called pyramid_contextauth that calls pyramid_contextauth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - pyramid_contextauth

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD-derived (http://www.repoze.org/LICENSE.txt)
  summary: 'Pyramid security extension to register multiple contexts based\nauthentication policies.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
