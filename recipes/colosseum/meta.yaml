package:
  name: colosseum
  version: "0.1.1"

source:
  fn: colosseum-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/c/colosseum/colosseum-0.1.1.tar.gz
  md5: 0eac0651192e0352c862f660739cf1e0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - colosseum = colosseum:main
    #
    # Would create an entry point called colosseum that calls colosseum.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - colosseum

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/pybee/colosseum
  license: BSD License
  summary: 'A (partial) implementation of the CSS box and flexbox layout algorithm.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
