package:
  name: collective.localstyles
  version: "1.0"

source:
  fn: collective.localstyles-1.0.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.localstyles/collective.localstyles-1.0.tar.gz
  md5: ddea60140eb4eb8ccca8c3348ac4bc6d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.localstyles = collective.localstyles:main
    #
    # Would create an entry point called collective.localstyles that calls collective.localstyles.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.cmfplone
    - zope.publisher
    - plone.browserlayer

  run:
    - python
    - setuptools
    - setuptools
    - products.cmfplone
    - zope.publisher
    - plone.browserlayer

test:
  # Python imports
  imports:
    - collective
    - collective.localstyles

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.localstyles
  license: GPL
  summary: 'Add local styles to subsections in your Plone site.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
