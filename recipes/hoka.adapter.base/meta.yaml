package:
  name: hoka.adapter.base
  version: "2.13.19.2"

source:
  fn: hoka.adapter.base-2.13.19.2.zip
  url: https://pypi.python.org/packages/source/h/hoka.adapter.base/hoka.adapter.base-2.13.19.2.zip
  md5: 5036b3ed4ab8d66e0c07cf130a2fa04b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hoka.adapter.base = hoka.adapter.base:main
    #
    # Would create an entry point called hoka.adapter.base that calls hoka.adapter.base.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zope2
    - z3c.autoinclude
    - hoka.patches.get_adapter

  run:
    - python
    - setuptools
    - setuptools
    - zope2
    - z3c.autoinclude
    - hoka.patches.get_adapter

test:
  # Python imports
  imports:
    - hoka
    - hoka.adapter
    - hoka.adapter.base
    - hoka.adapter.base.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/hoka/hoka.adapter.base
  license: GNU General Public License (GPL)
  summary: 'Adapter to get the authenticated user'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
