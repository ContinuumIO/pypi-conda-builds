package:
  name: collective.js.innerfade
  version: "1.0-final"

source:
  fn: collective.js.innerfade-1.0-final.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.js.innerfade/collective.js.innerfade-1.0-final.tar.gz
  md5: 10968ca3e174c4bbca5471168955f912
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.js.innerfade = collective.js.innerfade:main
    #
    # Would create an entry point called collective.js.innerfade that calls collective.js.innerfade.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.imaging

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.imaging

test:
  # Python imports
  imports:
    - collective
    - collective.js
    - collective.js.innerfade
    - collective.js.innerfade.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/collective.js.innerfade
  license: GPL
  summary: 'jQuery Innerfade plugin and additional view for ATFolder'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
