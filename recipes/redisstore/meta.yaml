package:
  name: redisstore
  version: "0.1"

source:
  fn: RedisStore-0.1.tar.gz
  url: https://github.com/timothycrosley/RedisStore/blob/master/dist/RedisStore-0.1.tar.gz?raw=true
  #md5: 
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - redisstore = redisstore:main
    #
    # Would create an entry point called redisstore that calls redisstore.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - redisstore

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.simpleinnovation.org/
  license: GNU GPLv2
  summary: 'Enables interacting with Redis keys from within python as if they where native object types.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
