package:
  name: lcinvestor
  version: "2.2.3"

source:
  fn: lcinvestor-2.2.3.tar.gz
  url: https://pypi.python.org/packages/source/l/lcinvestor/lcinvestor-2.2.3.tar.gz
  md5: 59a0d8acf3e96e92d87a4fc26a72d063
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lcinvestor = lcinvestor:main
    #
    # Would create an entry point called lcinvestor that calls lcinvestor.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - lendingclub >=0.1.8
    - argparse >=1.2.1
    - pyyaml >=3.09
    - pause >=0.1.2

  run:
    - python
    - lendingclub >=0.1.8
    - argparse >=1.2.1
    - pyyaml >=3.09
    - pause >=0.1.2

test:
  # Python imports
  imports:
    - lcinvestor
    - lcinvestor.settings
    - lcinvestor.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jgillick/LendingClubAutoInvestor
  license: MIT License
  summary: 'A simple tool that will watch your LendingClub account and automatically invest cash as it becomes available.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
