package:
  name: bob-ajax-selects
  version: "1.6.1"

source:
  fn: bob-ajax-selects-1.6.1.tar.gz
  url: https://pypi.python.org/packages/source/b/bob-ajax-selects/bob-ajax-selects-1.6.1.tar.gz
  md5: f1666ecc977c4494b372118420187e44
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bob-ajax-selects = bob-ajax-selects:main
    #
    # Would create an entry point called bob-ajax-selects that calls bob-ajax-selects.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - ajax_select

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/quamilek/bob-ajax-selects/
  license: MIT License
  summary: 'jQuery-powered auto-complete fields for editing ForeignKey, ManyToManyField and CharField'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
