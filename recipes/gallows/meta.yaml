package:
  name: gallows
  version: "0.6.0"

source:
  fn: gallows-0.6.0.tar.gz
  url: https://pypi.python.org/packages/source/g/gallows/gallows-0.6.0.tar.gz
  md5: b09e5b73c48b887cfe070fee14d6f539
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gallows = gallows:main
    #
    # Would create an entry point called gallows that calls gallows.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six

  run:
    - python
    - six

test:
  # Python imports
  imports:
    - gallows

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/chennaipy/hangman
  license: BSD License
  summary: "The word game Hangman based on 'Invent Your Own Computer Games with Python'."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
