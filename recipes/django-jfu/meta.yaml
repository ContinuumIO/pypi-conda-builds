package:
  name: django-jfu
  version: "2.0.9"

source:
  fn: django-jfu-2.0.9.tar.gz
  url: https://pypi.python.org/packages/source/d/django-jfu/django-jfu-2.0.9.tar.gz
  md5: dcf16eafb138630a22bdd1da560c013b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-jfu = django-jfu:main
    #
    # Would create an entry point called django-jfu that calls django-jfu.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - jfu
    - jfu.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://packages.python.org/jfu
  license: BSD License
  summary: 'A Django Library for jQuery File Upload.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
