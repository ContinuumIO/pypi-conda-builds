package:
  name: django-registration-me
  version: "0.8"

source:
  fn: django-registration-me-0.8.tar.gz
  url: https://pypi.python.org/packages/source/d/django-registration-me/django-registration-me-0.8.tar.gz
  md5: 7a568c73ea04110f802e378882fe4b92
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-registration-me = django-registration-me:main
    #
    # Would create an entry point called django-registration-me that calls django-registration-me.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - registration
    - registration.management
    - registration.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lig/django-registration-me
  license: BSD License
  summary: 'An extensible user-registration application for Django using MongoEngine'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
