package:
  name: collective.zipfiletransport
  version: "3.2.3"

source:
  fn: collective.zipfiletransport-3.2.3.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.zipfiletransport/collective.zipfiletransport-3.2.3.tar.gz
  md5: f6a25e56b1c6b27fa265c0637f813bfa
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.zipfiletransport = collective.zipfiletransport:main
    #
    # Would create an entry point called collective.zipfiletransport that calls collective.zipfiletransport.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.zipfiletransport
    - collective.zipfiletransport.Extensions
    - collective.zipfiletransport.browser
    - collective.zipfiletransport.tests
    - collective.zipfiletransport.utilities

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/collective.zipfiletransport
  license: GPL
  summary: 'This tool is used to import and export zip files.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
