package:
  name: sqlalchemy-find
  version: "0.1"

source:
  fn: sqlalchemy-find-0.1.tar.gz
  url: https://pypi.python.org/packages/source/s/sqlalchemy-find/sqlalchemy-find-0.1.tar.gz
  md5: 653c53b40501b44bf1b4d85b688a7a38
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sqlalchemy-find = sqlalchemy-find:main
    #
    # Would create an entry point called sqlalchemy-find that calls sqlalchemy-find.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sqlalchemy

  run:
    - python
    - sqlalchemy

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/ollyc/sqlalchemy-find
  license: Apache Software License
  summary: 'Syntactic sugar for SQLAlchemy: a Storm ORM-like find method'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
