package:
  name: pywunder
  version: "0.0.1"

source:
  fn: pywunder-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/p/pywunder/pywunder-0.0.1.tar.gz
  md5: a9a6ab8e105766e9b3260a0cbdd75433
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pywunder = pywunder:main
    #
    # Would create an entry point called pywunder that calls pywunder.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - pywunder

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/pywunder/
  license: LICENSE.txt
  summary: 'Very simple wrapper around the wunderground weather API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
