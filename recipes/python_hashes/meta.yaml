package:
  name: python_hashes
  version: "0.1"

source:
  fn: python_hashes-0.1.tar.gz
  url: https://pypi.python.org/packages/source/p/python_hashes/python_hashes-0.1.tar.gz
  md5: f4444de70ad5cea0bef576e71f160283
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python_hashes = python_hashes:main
    #
    # Would create an entry point called python_hashes that calls python_hashes.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - hashes
    - python_hashes

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/sangelone/python-hashes
  license: UNKNOWN
  summary: 'Library of interesting (non-cryptographic) hashes in pure Python.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
