package:
  name: feincms-jobs
  version: "0.1"

source:
  fn: feincms-jobs-0.1.tar.gz
  url: https://pypi.python.org/packages/source/f/feincms-jobs/feincms-jobs-0.1.tar.gz
  md5: 9fcab281b56325cbafef89f59dd57c39
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - feincms-jobs = feincms-jobs:main
    #
    # Would create an entry point called feincms-jobs that calls feincms-jobs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - feincms

  run:
    - python
    - django
    - feincms

test:
  # Python imports
  imports:
    - feincms_jobs

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/incuna/feincms-jobs
  license: UNKNOWN
  summary: 'A FeinCMS Plugin for job listings.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
