package:
  name: bambu-xmlrpc
  version: "2.0"

source:
  fn: bambu-xmlrpc-2.0.tar.gz
  url: https://pypi.python.org/packages/source/b/bambu-xmlrpc/bambu-xmlrpc-2.0.tar.gz
  md5: 4c5848fbcc7d07d6cae43a6a456a862c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bambu-xmlrpc = bambu-xmlrpc:main
    #
    # Would create an entry point called bambu-xmlrpc that calls bambu-xmlrpc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4

  run:
    - python
    - django >=1.4

test:
  # Python imports
  imports:
    - bambu_xmlrpc

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/iamsteadman/bambu-xmlrpc
  license: UNKNOWN
  summary: 'An extensible XML-RPC provider'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
