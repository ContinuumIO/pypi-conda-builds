package:
  name: django-parting
  version: "0.0.2"

source:
  fn: django-parting-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-parting/django-parting-0.0.2.tar.gz
  md5: b9a588c3dffed0128d10d9fb53823454
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-parting = django-parting:main
    #
    # Would create an entry point called django-parting that calls django-parting.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - python-dateutil
    - django-dfk

  run:
    - python
    - django
    - python-dateutil
    - django-dfk

test:
  # Python imports
  imports:
    - parting
    - parting.management
    - parting.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/danfairs/django-parting
  license: BSD License
  summary: 'Tools to help manage large database tables'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
