package:
  name: aiogreen
  version: "0.3"

source:
  fn: aiogreen-0.3.tar.gz
  url: https://pypi.python.org/packages/source/a/aiogreen/aiogreen-0.3.tar.gz
  md5: d17d81d9aa728a8934590df4134a821a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - aiogreen = aiogreen:main
    #
    # Would create an entry point called aiogreen that calls aiogreen.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - eventlet
    - trollius >=0.3

  run:
    - python
    - eventlet
    - trollius >=0.3

test:
  # Python imports
  imports:
    - aiogreen

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://aiogreen.readthedocs.org/
  license: Apache Software License
  summary: 'asyncio event loop scheduling callbacks in eventlet.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
