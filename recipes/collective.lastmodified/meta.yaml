package:
  name: collective.lastmodified
  version: "1.0"

source:
  fn: collective.lastmodified-1.0.zip
  url: https://pypi.python.org/packages/source/c/collective.lastmodified/collective.lastmodified-1.0.zip
  md5: e6a12c67671b32ec8ff6f5e651a15b6a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.lastmodified = collective.lastmodified:main
    #
    # Would create an entry point called collective.lastmodified that calls collective.lastmodified.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.lastmodified
    - collective.lastmodified.browser
    - collective.lastmodified.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/
  license: gpl
  summary: 'Adds a viewlet that displays the last modification date'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
