package:
  name: article_segment
  version: "0.0.1"

source:
  fn: article_segment-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/a/article_segment/article_segment-0.0.1.tar.gz
  md5: a88e0d8e29bdf53ed7ebe3bc701e056a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - article_segment = article_segment:main
    #
    # Would create an entry point called article_segment that calls article_segment.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - wordsegment
    - split_block

  run:
    - python
    - wordsegment
    - split_block

test:
  # Python imports
  imports:
    - article_segment

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/17zuoye/article_segment/
  license: MIT
  summary: 'article_segment'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
