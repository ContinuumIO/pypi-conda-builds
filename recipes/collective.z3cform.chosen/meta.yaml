package:
  name: collective.z3cform.chosen
  version: "1.2.1"

source:
  fn: collective.z3cform.chosen-1.2.1.zip
  url: https://pypi.python.org/packages/source/c/collective.z3cform.chosen/collective.z3cform.chosen-1.2.1.zip
  md5: cd4b80a5353333ea12abbcc8179c2684
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.z3cform.chosen = collective.z3cform.chosen:main
    #
    # Would create an entry point called collective.z3cform.chosen that calls collective.z3cform.chosen.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - z3c.autoinclude
    - plone
    - demjson
    - ordereddict
    - plone.app.upgrade
    - collective.js.chosen
    - z3c.formwidget.query

  run:
    - python
    - setuptools
    - setuptools
    - z3c.autoinclude
    - plone
    - demjson
    - ordereddict
    - plone.app.upgrade
    - collective.js.chosen
    - z3c.formwidget.query

test:
  # Python imports
  imports:
    - collective
    - collective.z3cform
    - collective.z3cform.chosen
    - collective.z3cform.chosen.browser
    - collective.z3cform.chosen.browser.demo
    - collective.z3cform.chosen.migrations
    - collective.z3cform.chosen.migrations.v11
    - collective.z3cform.chosen.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/collective.z3cform.chosen
  license: GPL
  summary: 'chosen widget for z3cform (both chosen & ajax version)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
