package:
  name: cloud-beds
  version: "0.0.3"

source:
  fn: cloud-beds-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/c/cloud-beds/cloud-beds-0.0.3.tar.gz
  md5: 35eb828e51676a7bc7c0e46c7f8c0a2f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cloud-beds = cloud-beds:main
    #
    # Would create an entry point called cloud-beds that calls cloud-beds.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - requests
    - craigsgenerator

  run:
    - python
    - requests
    - craigsgenerator

test:
  # Python imports
  imports:
    - cloud_beds

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tlevine/cloud-beds.git
  license: AGPL
  summary: 'Find beds in the cloud.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
