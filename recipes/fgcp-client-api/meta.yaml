package:
  name: fgcp-client-api
  version: "1.3.2"

source:
  fn: fgcp-client-api-1.3.2.zip
  url: https://pypi.python.org/packages/source/f/fgcp-client-api/fgcp-client-api-1.3.2.zip
  md5: d03669990539b4ea2bfb5216ebb03ac1
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fgcp-client-api = fgcp-client-api:main
    #
    # Would create an entry point called fgcp-client-api that calls fgcp-client-api.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - fgcp

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/fgcp-client-api/
  license: Apache Software License
  summary: 'Client API Library for the Fujitsu Global Cloud Platform (FGCP)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
