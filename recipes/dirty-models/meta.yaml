package:
  name: dirty-models
  version: "0.5.0"

source:
  fn: dirty-models-0.5.0.tar.gz
  url: https://pypi.python.org/packages/source/d/dirty-models/dirty-models-0.5.0.tar.gz
  md5: 99ebd5f70da493708b67b1c88692b5ea
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dirty-models = dirty-models:main
    #
    # Would create an entry point called dirty-models that calls dirty-models.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python-dateutil

  run:
    - python
    - python-dateutil

test:
  # Python imports
  imports:
    - dirty_models

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/alfred82santa/dirty-models
  license: BSD License
  summary: 'Dirty models for python 3'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
