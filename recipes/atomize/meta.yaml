package:
  name: atomize
  version: "0.2.0"

source:
  fn: atomize-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/a/atomize/atomize-0.2.0.tar.gz
  md5: 16e53fb01ff0942a548879cbcbd64ede
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - atomize = atomize:main
    #
    # Would create an entry point called atomize that calls atomize.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - atomize

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/atomize/
  license: Eclipse Public License 1.0
  summary: 'A simple Python package for easily generating Atom feeds'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
