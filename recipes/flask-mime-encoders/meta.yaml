package:
  name: flask-mime-encoders
  version: "0.1.1"

source:
  fn: Flask-MIME-Encoders-0.1.1.zip
  url: https://pypi.python.org/packages/source/F/Flask-MIME-Encoders/Flask-MIME-Encoders-0.1.1.zip
  md5: 6a12988b90214ff69f71f700e3a881af
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-mime-encoders = flask-mime-encoders:main
    #
    # Would create an entry point called flask-mime-encoders that calls flask-mime-encoders.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.5

  run:
    - python
    - flask >=0.5

test:
  # Python imports
  imports:
    - flask_mime_encoders

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/salsita/flask-mime-encoders
  license: MIT License
  summary: 'Extensible Flask MIME encoders and decoders'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
