package:
  name: beetle-markdown
  version: "0.1.0"

source:
  fn: Beetle-Markdown-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/B/Beetle-Markdown/Beetle-Markdown-0.1.0.tar.gz
  md5: e553bd025b0f19e1f85441cb9536dfd2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - beetle-markdown = beetle-markdown:main
    #
    # Would create an entry point called beetle-markdown that calls beetle-markdown.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - markdown

  run:
    - python
    - markdown

test:
  # Python imports
  imports:
    - beetle_markdown

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/cknv/beetle-markdown
  license: MIT License
  summary: 'Makes Beetle do markdown'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
