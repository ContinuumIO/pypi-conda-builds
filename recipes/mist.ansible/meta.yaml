package:
  name: mist.ansible
  version: "0.1.8"

source:
  fn: mist.ansible-0.1.8.tar.gz
  url: https://pypi.python.org/packages/source/m/mist.ansible/mist.ansible-0.1.8.tar.gz
  md5: 84cdf3cf63d6f36fad0cd9effd2afd74
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mist.ansible = mist.ansible:main
    #
    # Would create an entry point called mist.ansible that calls mist.ansible.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - mist.client >=0.1.0

  run:
    - python
    - mist.client >=0.1.0

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mistio/mist.ansible
  license: GNU Affero General Public License v3
  summary: 'Ansible modules for the mist.io service'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
