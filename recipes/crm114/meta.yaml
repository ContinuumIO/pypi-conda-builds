package:
  name: crm114
  version: "2.0.2"

source:
  fn: crm114-2.0.2.tar.gz
  url: https://pypi.python.org/packages/source/c/crm114/crm114-2.0.2.tar.gz
  md5: e71ebbec9fe4d86f5824bbb99088f481
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - crm114 = crm114:main
    #
    # Would create an entry point called crm114 that calls crm114.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - crm114

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://packages.python.org/crm114
  license: MIT License
  summary: 'Python wrapper classes for the CRM-114 Discriminator (http://crm114.sourceforge.net/)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
