package:
  name: ansigenome
  version: "0.5.3"

source:
  fn: ansigenome-0.5.3.tar.gz
  url: https://pypi.python.org/packages/source/a/ansigenome/ansigenome-0.5.3.tar.gz
  md5: 2c1b5a8a47512f06af7607d4efa67396
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ansigenome = ansigenome:main
    #
    # Would create an entry point called ansigenome that calls ansigenome.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - jinja2
    - pyyaml
    - setuptools

  run:
    - python
    - jinja2
    - pyyaml
    - setuptools

test:
  # Python imports
  imports:
    - ansigenome

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/nickjj/ansigenome
  license: GPLv3
  summary: 'A tool to help you gather information and manage your Ansible roles.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
