package:
  name: hbase-thrift
  version: "0.20.4"

source:
  fn: hbase-thrift-0.20.4.tar.gz
  url: https://pypi.python.org/packages/source/h/hbase-thrift/hbase-thrift-0.20.4.tar.gz
  md5: 3ea9bc15e7574c00e804552cfaee196e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hbase-thrift = hbase-thrift:main
    #
    # Would create an entry point called hbase-thrift that calls hbase-thrift.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - thrift

  run:
    - python
    - thrift

test:
  # Python imports
  imports:
    - hbase

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://hbase.org/
  license: UNKNOWN
  summary: 'Python client for HBase Thrift interface'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
