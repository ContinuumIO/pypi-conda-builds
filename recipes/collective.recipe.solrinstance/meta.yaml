package:
  name: collective.recipe.solrinstance
  version: "6.0.0b3"

source:
  fn: collective.recipe.solrinstance-6.0.0b3.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.recipe.solrinstance/collective.recipe.solrinstance-6.0.0b3.tar.gz
  md5: 18b6a0e34b2ba77e8c6fbf6b13d004a8
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.recipe.solrinstance = collective.recipe.solrinstance:main
    #
    # Would create an entry point called collective.recipe.solrinstance that calls collective.recipe.solrinstance.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - hexagonit.recipe.download
    - genshi
    - zc.buildout

  run:
    - python
    - setuptools
    - setuptools
    - hexagonit.recipe.download
    - genshi
    - zc.buildout

test:
  # Python imports
  imports:
    - collective
    - collective.recipe
    - collective.recipe.solrinstance
    - collective.recipe.solrinstance.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/collective.recipe.solrinstance
  license: Zope Public License
  summary: 'zc.buildout to configure a solr instance'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
