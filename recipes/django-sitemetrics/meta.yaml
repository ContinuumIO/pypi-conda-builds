package:
  name: django-sitemetrics
  version: "0.4.2"

source:
  fn: django-sitemetrics-0.4.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-sitemetrics/django-sitemetrics-0.4.2.tar.gz
  md5: 6265655dd215a0f8e6536e6480a938eb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-sitemetrics = django-sitemetrics:main
    #
    # Would create an entry point called django-sitemetrics that calls django-sitemetrics.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - sitemetrics

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/idlesign/django-sitemetrics
  license: BSD License
  summary: 'Reusable application for Django providing easy means to integrate site metrics counters into your sites'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
