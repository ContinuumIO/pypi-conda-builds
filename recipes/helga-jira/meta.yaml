package:
  name: helga-jira
  version: "0.1.1"

source:
  fn: helga-jira-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/h/helga-jira/helga-jira-0.1.1.tar.gz
  md5: dad4e45e6a497ccdb2ac4cc92b979f99
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - helga-jira = helga-jira:main
    #
    # Would create an entry point called helga-jira that calls helga-jira.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - setuptools
    - requests

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/shaunduncan/helga-jira
  license: MIT License
  summary: 'A helga plugin that can be used to store responses that can be returned from a question'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
