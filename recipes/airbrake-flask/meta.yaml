package:
  name: airbrake-flask
  version: "1.0.7"

source:
  fn: airbrake-flask-1.0.7.tar.gz
  url: https://pypi.python.org/packages/source/a/airbrake-flask/airbrake-flask-1.0.7.tar.gz
  md5: 13609f4ac7e97966228bae5697a8eb7e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - airbrake-flask = airbrake-flask:main
    #
    # Would create an entry point called airbrake-flask that calls airbrake-flask.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests
    - six

  run:
    - python
    - requests
    - six

test:
  # Python imports
  imports:
    - airbrake

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kienpham2000/airbrake-flask
  license: MIT License
  summary: 'airbrake-flask - Airbrake client for Python Flask'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
