package:
  name: ansicolors
  version: "1.0.2"

source:
  fn: ansicolors-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/a/ansicolors/ansicolors-1.0.2.tar.gz
  md5: d3a19fcd75c89772360e0d2b693fc50f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ansicolors = ansicolors:main
    #
    # Would create an entry point called ansicolors that calls ansicolors.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/verigak/colors/
  license: ISC License (ISCL)
  summary: 'ANSI colors for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
