package:
  name: django-futupayments
  version: "0.3.0"

source:
  fn: django-futupayments-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-futupayments/django-futupayments-0.3.0.tar.gz
  md5: e7a90f20dbd440a6cf1ca6b11aca090d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-futupayments = django-futupayments:main
    #
    # Would create an entry point called django-futupayments that calls django-futupayments.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - futupayments
    - futupayments.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Futubank/django-futupayments
  license: MIT License
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
