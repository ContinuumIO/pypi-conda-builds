package:
  name: django-featured
  version: "0.1.7"

source:
  fn: django-featured-0.1.7.tar.gz
  url: https://pypi.python.org/packages/source/d/django-featured/django-featured-0.1.7.tar.gz
  md5: 8a28001e93835c21b810e521e691290d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-featured = django-featured:main
    #
    # Would create an entry point called django-featured that calls django-featured.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - featured

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/powellc/django-featured
  license: BSD License
  summary: 'An application for starting new django applications.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
