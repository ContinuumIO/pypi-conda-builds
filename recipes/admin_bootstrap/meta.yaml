package:
  name: admin_bootstrap
  version: "0.7.0"

source:
  fn: admin_bootstrap-0.7.0.tar.gz
  url: https://pypi.python.org/packages/source/a/admin_bootstrap/admin_bootstrap-0.7.0.tar.gz
  md5: 9c37da2d2d6343812f7c16439783b1c0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - admin_bootstrap = admin_bootstrap:main
    #
    # Would create an entry point called admin_bootstrap that calls admin_bootstrap.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - admin_bootstrap

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dryan/admin_bootstrap
  license: BSD 3 Clause License
  summary: 'Django admin templates that are compatible with Twitter Bootstrap version 2.1.0'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
