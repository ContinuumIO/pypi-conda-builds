package:
  name: matrix_solver
  version: "1.0.0"

source:
  fn: matrix_solver-1.0.0.zip
  url: https://pypi.python.org/packages/source/m/matrix_solver/matrix_solver-1.0.0.zip
  md5: 2e82719f678f3450da4de55bd14b6466
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - matrix_solver = matrix_solver:main
    #
    # Would create an entry point called matrix_solver that calls matrix_solver.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - matrix_solver

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://snucad.snu.ac.kr
  license: UNKNOWN
  summary: "Solve a squre matrix using Cramer's rule"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
