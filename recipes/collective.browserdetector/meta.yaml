package:
  name: collective.browserdetector
  version: "1.1.2"

source:
  fn: collective.browserdetector-1.1.2.zip
  url: https://pypi.python.org/packages/source/c/collective.browserdetector/collective.browserdetector-1.1.2.zip
  md5: 28f03615145f41b648b9d63534f4bf5f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.browserdetector = collective.browserdetector:main
    #
    # Would create an entry point called collective.browserdetector that calls collective.browserdetector.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.browserdetector
    - collective.browserdetector.portlets
    - collective.browserdetector.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://dms4u.cz
  license: GPL
  summary: 'A helper Plone extension which makes easier to include resources specific to several types of browsers or platforms.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
