package:
  name: go-metrics-api
  version: "0.1.4"

source:
  fn: go-metrics-api-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/g/go-metrics-api/go-metrics-api-0.1.4.tar.gz
  md5: 4db5e616cee3b9872e74035c2b5d3d99
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - go-metrics-api = go-metrics-api:main
    #
    # Would create an entry point called go-metrics-api that calls go-metrics-api.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - treq
    - cyclone
    - go_api >=0.2.0
    - confmodel ==0.2.0

  run:
    - python
    - treq
    - cyclone
    - go_api >=0.2.0
    - confmodel ==0.2.0

test:
  # Python imports
  imports:
    - go_metrics
    - go_metrics.metrics
    - go_metrics.metrics.tests
    - go_metrics.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/praekelt/go-metrics-api
  license: BSD License
  summary: 'An API for querying vumi-go metrics'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
