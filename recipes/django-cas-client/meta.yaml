package:
  name: django-cas-client
  version: "1.1.1"

source:
  fn: django-cas-client-1.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-cas-client/django-cas-client-1.1.1.tar.gz
  md5: 5b1de9f83070d21f4987dd154aea05b3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-cas-client = django-cas-client:main
    #
    # Would create an entry point called django-cas-client that calls django-cas-client.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - cas
    - cas.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/kstateome/django-cas/
  license: MIT License
  summary: 'Django Cas Client'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
