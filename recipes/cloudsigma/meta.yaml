package:
  name: cloudsigma
  version: "0.4"

source:
  fn: cloudsigma-0.4.tar.gz
  url: https://pypi.python.org/packages/source/c/cloudsigma/cloudsigma-0.4.tar.gz
  md5: 7660f0f08a8a6bd8e64a299d503b143d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cloudsigma = cloudsigma:main
    #
    # Would create an entry point called cloudsigma that calls cloudsigma.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - backports.ssl-match-hostname ==3.4.0.2
    - configobj ==5.0.6
    - iso8601 ==0.1.10
    - nose ==1.3.4
    - pyserial ==2.7
    - requests ==2.5.0
    - simplejson ==3.6.5
    - six ==1.8.0
    - websocket-client ==0.22.0
    - wsgiref ==0.1.2

  run:
    - python
    - backports.ssl-match-hostname ==3.4.0.2
    - configobj ==5.0.6
    - iso8601 ==0.1.10
    - nose ==1.3.4
    - pyserial ==2.7
    - requests ==2.5.0
    - simplejson ==3.6.5
    - six ==1.8.0
    - websocket-client ==0.22.0
    - wsgiref ==0.1.2

test:
  # Python imports
  imports:
    - cloudsigma

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/cloudsigma/pycloudsigma
  license: UNKNOWN
  summary: "CloudSigma's official python library."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
