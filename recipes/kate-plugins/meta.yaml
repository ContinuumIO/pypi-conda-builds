package:
  name: kate-plugins
  version: "0.2.3"

source:
  fn: Kate-plugins-0.2.3.tar.gz
  url: https://pypi.python.org/packages/source/K/Kate-plugins/Kate-plugins-0.2.3.tar.gz
  md5: 3edf1c799f4a2508625a57c857b9a741
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kate-plugins = kate-plugins:main
    #
    # Would create an entry point called kate-plugins that calls kate-plugins.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pysmell ==0.7.3
    - pep8 ==0.6.1
    - pyflakes ==0.5.0
    - pyjslint ==0.3.3
    - pyplete ==0.0.2
    - simplejson ==2.6.1

  run:
    - python
    - pysmell ==0.7.3
    - pep8 ==0.6.1
    - pyflakes ==0.5.0
    - pyjslint ==0.3.3
    - pyplete ==0.0.2
    - simplejson ==2.6.1

test:
  # Python imports
  imports:
    - kate_plugins

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/goinnn/Kate-plugins
  license: GNU Lesser General Public License
  summary: 'Plugins to Kate editor to develop faster python projects, django projects and something of javascript'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
