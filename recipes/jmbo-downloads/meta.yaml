package:
  name: jmbo-downloads
  version: "2.0.0a1"

source:
  fn: jmbo-downloads-2.0.0a1.tar.gz
  url: https://pypi.python.org/packages/source/j/jmbo-downloads/jmbo-downloads-2.0.0a1.tar.gz
  md5: 4f80e5f7ec88425253224733929ab1f0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jmbo-downloads = jmbo-downloads:main
    #
    # Would create an entry point called jmbo-downloads that calls jmbo-downloads.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - jmbo ==2.0.0a2

  run:
    - python
    - jmbo ==2.0.0a2

test:
  # Python imports
  imports:
    - downloads
    - downloads.management
    - downloads.management.commands
    - downloads.migrations
    - downloads.templatetags
    - downloads.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/praekelt/jmbo-downloads
  license: BSD License
  summary: 'Downloads for Jmbo.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
