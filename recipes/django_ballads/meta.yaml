package:
  name: django_ballads
  version: "0.2"

source:
  fn: django_ballads-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django_ballads/django_ballads-0.2.tar.gz
  md5: 33fb83184a4184a8fb327264a374e736
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_ballads = django_ballads:main
    #
    # Would create an entry point called django_ballads that calls django_ballads.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5.1

  run:
    - python
    - django >=1.5.1

test:
  # Python imports
  imports:
    - django_ballads

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jaylett/django-ballads
  license: MIT License
  summary: 'Django library for coordinating multi-system transactions (eg database, filesystem, remote API calls).'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
