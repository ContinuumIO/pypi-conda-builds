package:
  name: betahaus.pyracont
  version: "0.3b0"

source:
  fn: betahaus.pyracont-0.3b0.tar.gz
  url: https://pypi.python.org/packages/source/b/betahaus.pyracont/betahaus.pyracont-0.3b0.tar.gz
  md5: dcd674c7649f6f2ddbef1c2e9cb33e63
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - betahaus.pyracont = betahaus.pyracont:main
    #
    # Would create an entry point called betahaus.pyracont that calls betahaus.pyracont.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyramid
    - repoze.folder
    - awesome-slugify
    - pytz
    - zodb3
    - colander
    - zope.interface
    - zope.component

  run:
    - python
    - setuptools
    - pyramid
    - repoze.folder
    - awesome-slugify
    - pytz
    - zodb3
    - colander
    - zope.interface
    - zope.component

test:
  # Python imports
  imports:
    - betahaus
    - betahaus.pyracont
    - betahaus.pyracont.fields
    - betahaus.pyracont.fields.tests
    - betahaus.pyracont.tests
    - betahaus.pyracont.tests.fixtures

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/robinharms/betahaus.pyracont
  license: GNU General Public License v3 (GPLv3)
  summary: 'betahaus.pyracont'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
