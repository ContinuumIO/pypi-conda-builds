package:
  name: aha.plugin.twitteroauth
  version: "0.93bdev"

source:
  fn: aha.plugin.twitteroauth-0.93bdev.tar.gz
  url: https://pypi.python.org/packages/source/a/aha.plugin.twitteroauth/aha.plugin.twitteroauth-0.93bdev.tar.gz
  md5: 8f8cd58538a388b456effdcb8a431a11
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - aha.plugin.twitteroauth = aha.plugin.twitteroauth:main
    #
    # Would create an entry point called aha.plugin.twitteroauth that calls aha.plugin.twitteroauth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - aha

  run:
    - python
    - setuptools
    - aha

test:
  # Python imports
  imports:
    - twitteroauth

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://coreblog.org/aha
  license: BSD
  summary: 'A twitter auth plugin for aha'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
