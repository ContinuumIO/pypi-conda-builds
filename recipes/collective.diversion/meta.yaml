package:
  name: collective.diversion
  version: "1.0"

source:
  fn: collective.diversion-1.0.zip
  url: https://pypi.python.org/packages/source/c/collective.diversion/collective.diversion-1.0.zip
  md5: e960990eade40a34d55695b423ae5cc1
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.diversion = collective.diversion:main
    #
    # Would create an entry point called collective.diversion that calls collective.diversion.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.diversion
    - collective.diversion.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.diversion
  license: GPL
  summary: 'collective.diversion wraps the existing behaviour for broken objects in the ZODB to allow developers to declare old locations of their classes and have them be automatically migrated'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
