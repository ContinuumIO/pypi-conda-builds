package:
  name: monasca-persister
  version: "2015.1"

source:
  fn: monasca-persister-2015.1.tar.gz
  url: https://pypi.python.org/packages/source/m/monasca-persister/monasca-persister-2015.1.tar.gz
  md5: 54d4c30578ef4600d742c187bd789cc8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - monasca-persister = monasca-persister:main
    #
    # Would create an entry point called monasca-persister that calls monasca-persister.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Apache Software License
  summary: None

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
