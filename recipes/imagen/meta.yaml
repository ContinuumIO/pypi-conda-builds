package:
  name: imagen
  version: "2.0.1"

source:
  fn: imagen-2.0.1.tar.gz
  url: https://pypi.python.org/packages/source/i/imagen/imagen-2.0.1.tar.gz
  md5: 23a92763bbf8aab95723f3ab91d9ea9c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - imagen = imagen:main
    #
    # Would create an entry point called imagen that calls imagen.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - imagen
    - imagen.notebooks
    - imagen.notebooks.images
    - imagen.tests
    - imagen.transferfn

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://ioam.github.com/imagen/
  license: BSD License
  summary: 'Generic Python library for 0D, 1D, and 2D pattern distributions.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
