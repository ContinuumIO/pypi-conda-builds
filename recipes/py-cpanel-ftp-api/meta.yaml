package:
  name: py-cpanel-ftp-api
  version: "0.9"

source:
  fn: py-cpanel-ftp-api-0.9.tar.gz
  url: https://pypi.python.org/packages/source/p/py-cpanel-ftp-api/py-cpanel-ftp-api-0.9.tar.gz
  md5: c9b38251454bb0ca0b475f86e3a8f1ba
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - py-cpanel-ftp-api = py-cpanel-ftp-api:main
    #
    # Would create an entry point called py-cpanel-ftp-api that calls py-cpanel-ftp-api.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bensnyde/py-cpanel-ftp-api/
  license: UNKNOWN
  summary: "Python client library for interacting with Cpanel's API2 FTP Module"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
