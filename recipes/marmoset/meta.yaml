package:
  name: marmoset
  version: "0.1.2"

source:
  fn: marmoset-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/m/marmoset/marmoset-0.1.2.tar.gz
  md5: 35b4bd54d742783f88e0e89f9ebf6f60
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - marmoset = marmoset:main
    #
    # Would create an entry point called marmoset that calls marmoset.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - marmoset

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/mwilliamson/marmoset.py
  license: UNKNOWN
  summary: 'Convert Python values, such as lists and dicts, into human-friendly strings'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
