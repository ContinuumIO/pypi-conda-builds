package:
  name: hgwebinit
  version: "0.1.0"

source:
  fn: hgwebinit-0.1.0.zip
  url: https://pypi.python.org/packages/source/h/hgwebinit/hgwebinit-0.1.0.zip
  md5: 42880047f19e120c64dfefb52bb7874d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hgwebinit = hgwebinit:main
    #
    # Would create an entry point called hgwebinit that calls hgwebinit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - hgwebinit

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/j3hyde/hgwebinit
  license: GNU General Public License v2 or later (GPLv2+)
  summary: 'Mercurial hgweb init support.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
