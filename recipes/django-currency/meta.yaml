package:
  name: django-currency
  version: "0.0.8"

source:
  fn: django-currency-0.0.8.tar.gz
  url: https://pypi.python.org/packages/source/d/django-currency/django-currency-0.0.8.tar.gz
  md5: 8a201da1ec0d786f3a23ae3b101034bb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-currency = django-currency:main
    #
    # Would create an entry point called django-currency that calls django-currency.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - currency
    - currency.migrations
    - currency.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/42cc/django-currency
  license: UNKNOWN
  summary: 'simple currency handling for djagngo'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
