package:
  name: django-rest-framework-nested-resource
  version: "1.3.0"

source:
  fn: django-rest-framework-nested-resource-1.3.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-rest-framework-nested-resource/django-rest-framework-nested-resource-1.3.0.tar.gz
  md5: 28178b92371d16127be39236842bc3e3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-rest-framework-nested-resource = django-rest-framework-nested-resource:main
    #
    # Would create an entry point called django-rest-framework-nested-resource that calls django-rest-framework-nested-resource.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - drf_nested_resource

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/simpleenergy/django-rest-framework-nested-resource
  license: BSD License
  summary: 'DRF view mixin for nested resources'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
