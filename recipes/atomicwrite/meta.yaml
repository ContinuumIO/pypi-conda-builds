package:
  name: atomicwrite
  version: "0.1"

source:
  fn: AtomicWrite-0.1.tar.gz
  url: https://pypi.python.org/packages/source/A/AtomicWrite/AtomicWrite-0.1.tar.gz
  md5: a0a367353cfb669537c30c240b86f004
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - atomicwrite = atomicwrite:main
    #
    # Would create an entry point called atomicwrite that calls atomicwrite.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - atomic
    - atomicwrite

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://k0s.org/hg/AtomicWrite
  license: MIT
  summary: 'write file contents atomically in one operation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
