package:
  name: erudite
  version: "1.1.1"

source:
  fn: Erudite-1.1.1.tar.gz
  url: https://pypi.python.org/packages/source/E/Erudite/Erudite-1.1.1.tar.gz
  md5: 14f73e237c4a020e9a47fda355a7ee3d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - erudite = erudite:main
    #
    # Would create an entry point called erudite that calls erudite.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - erudite

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/BrianHicks/erudite
  license: Apache 2.0
  summary: 'Log your command history to an analytics engine'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
