package:
  name: django-cratis-cms
  version: "0.1.1"

source:
  fn: django-cratis-cms-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-cratis-cms/django-cratis-cms-0.1.1.tar.gz
  md5: 7e50438936d0c3a3191ac37a65dc374c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-cratis-cms = django-cratis-cms:main
    #
    # Would create an entry point called django-cratis-cms that calls django-cratis-cms.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django-cratis
    - setuptools
    - django-mptt ==0.5.2
    - django-cms <3.0
    - pillow

  run:
    - python
    - django-cratis
    - setuptools
    - django-mptt ==0.5.2
    - django-cms <3.0
    - pillow

test:
  # Python imports
  imports:
    - cratis_cms

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: MIT
  summary: 'Integrates django-cms into cratis application'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
