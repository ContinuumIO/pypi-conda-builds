package:
  name: django_gmzoom_tools
  version: "1.2.0"

source:
  fn: django_gmzoom_tools-1.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django_gmzoom_tools/django_gmzoom_tools-1.2.0.tar.gz
  md5: e95a1095d1daae078e9acdbf89465ef5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_gmzoom_tools = django_gmzoom_tools:main
    #
    # Would create an entry point called django_gmzoom_tools that calls django_gmzoom_tools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_gmzoom_tools
    - gmzoom_tools
    - gmzoom_tools.templatetags
    - gmzoom_tools.widgets

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://otzhig.ru/dev
  license: BSD
  summary: 'Gmzoom project tools'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
