package:
  name: django-inplaceedit-version1
  version: "0.55"

source:
  fn: django-inplaceedit-version1-0.55.tar.gz
  url: https://pypi.python.org/packages/source/d/django-inplaceedit-version1/django-inplaceedit-version1-0.55.tar.gz
  md5: 4580c20120ef3215c616d79ac31f4a43
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-inplaceedit-version1 = django-inplaceedit-version1:main
    #
    # Would create an entry point called django-inplaceedit-version1 that calls django-inplaceedit-version1.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - inplaceeditform
    - inplaceeditform.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://tracpub.yaco.es/djangoapps/wiki/InplaceEditForm
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Django application that allows you to inline edition of some data from the database'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
