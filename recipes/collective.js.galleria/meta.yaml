package:
  name: collective.js.galleria
  version: "1.2.5"

source:
  fn: collective.js.galleria-1.2.5.zip
  url: https://pypi.python.org/packages/source/c/collective.js.galleria/collective.js.galleria-1.2.5.zip
  md5: 57906b2ce1a93f077a0587293291d4b2
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.js.galleria = collective.js.galleria:main
    #
    # Would create an entry point called collective.js.galleria that calls collective.js.galleria.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.js
    - collective.js.galleria
    - collective.js.galleria.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.js.galleria
  license: GPL
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
