package:
  name: friendfeed-pyapi
  version: "0.2.0"

source:
  fn: FriendFeed PyAPI-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/F/FriendFeed-PyAPI/FriendFeed PyAPI-0.2.0.tar.gz
  md5: a3f38fefd27c2d772fe9f5ef105af217
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - friendfeed-pyapi = friendfeed-pyapi:main
    #
    # Would create an entry point called friendfeed-pyapi that calls friendfeed-pyapi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://launchpad.net/friendfeed-pyapi
  license: Apache
  summary: 'A Python interface to the FriendFeed API.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
