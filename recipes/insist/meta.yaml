package:
  name: insist
  version: "0.2"

source:
  fn: insist-0.2.tar.gz
  url: https://pypi.python.org/packages/source/i/insist/insist-0.2.tar.gz
  md5: de53b6aa0abd6db085ea6acb80ccf868
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - insist = insist:main
    #
    # Would create an entry point called insist that calls insist.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - insist

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ray-harris-net/insist
  license: Apache Software License
  summary: 'An easy-to-use assertion library for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
