package:
  name: djangocas
  version: "1.0.0"

source:
  fn: djangocas-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/d/djangocas/djangocas-1.0.0.tar.gz
  md5: 88c6ecb6fe80b9c33fe6fea305e1163d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangocas = djangocas:main
    #
    # Would create an entry point called djangocas that calls djangocas.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six

  run:
    - python
    - six

test:
  # Python imports
  imports:
    - djangocas

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mdj2/djangocas
  license: MIT
  summary: 'CAS 1.0/2.0 authentication backend for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
