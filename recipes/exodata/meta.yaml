package:
  name: exodata
  version: "1.1.1"

source:
  fn: exodata-1.1.1.tar.gz
  url: https://pypi.python.org/packages/source/e/exodata/exodata-1.1.1.tar.gz
  md5: d03391521552fd52c237924b2f9cdafe
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - exodata = exodata:main
    #
    # Would create an entry point called exodata that calls exodata.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy
    - quantities
    - nose >=1.0
    - matplotlib >=1.3.1
    - requests

  run:
    - python
    - numpy
    - quantities
    - nose >=1.0
    - matplotlib >=1.3.1
    - requests

test:
  # Python imports
  imports:
    - exodata

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ryanvarley/exodata
  license: GNU General Public License v3 (GPLv3)
  summary: 'Exoplanet catalogue interface'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
