package:
  name: collective.maildigest
  version: "1.0"

source:
  fn: collective.maildigest-1.0.zip
  url: https://pypi.python.org/packages/source/c/collective.maildigest/collective.maildigest-1.0.zip
  md5: e61d1ffaa317994aa985d8389a051195
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.maildigest = collective.maildigest:main
    #
    # Would create an entry point called collective.maildigest that calls collective.maildigest.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.inviting
    - plone.api

  run:
    - python
    - setuptools
    - setuptools
    - collective.inviting
    - plone.api

test:
  # Python imports
  imports:
    - collective
    - collective.maildigest
    - collective.maildigest.action
    - collective.maildigest.activity
    - collective.maildigest.browser
    - collective.maildigest.digestrules
    - collective.maildigest.storage
    - collective.maildigest.tests
    - collective.maildigest.upgrades

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/
  license: gpl
  summary: 'Users can subscribe on a mail digest about activity in a Plone Folder'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
