package:
  name: xstatic-angular-bootstrap
  version: "0.11.0.2"

source:
  fn: XStatic-Angular-Bootstrap-0.11.0.2.tar.gz
  url: https://pypi.python.org/packages/source/X/XStatic-Angular-Bootstrap/XStatic-Angular-Bootstrap-0.11.0.2.tar.gz
  md5: bac078c4e6496367677680c89162ee83
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - xstatic-angular-bootstrap = xstatic-angular-bootstrap:main
    #
    # Would create an entry point called xstatic-angular-bootstrap that calls xstatic-angular-bootstrap.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - xstatic
    - xstatic.pkg
    - xstatic.pkg.angular_bootstrap

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://angular-ui.github.io/bootstrap/
  license: (same as Angular-Bootstrap)
  summary: 'Angular-Bootstrap 0.11.0 (XStatic packaging standard)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
