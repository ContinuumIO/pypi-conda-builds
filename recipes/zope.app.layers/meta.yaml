package:
  name: zope.app.layers
  version: "3.4.0"

source:
  fn: zope.app.layers-3.4.0.tar.gz
  url: https://pypi.python.org/packages/source/z/zope.app.layers/zope.app.layers-3.4.0.tar.gz
  md5: 4f081d33b319f1f7f50824087e283e81
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zope.app.layers = zope.app.layers:main
    #
    # Would create an entry point called zope.app.layers that calls zope.app.layers.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.deprecation

  run:
    - python
    - setuptools
    - zope.deprecation

test:
  # Python imports
  imports:
    - zope
    - zope.app
    - zope.app.layers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://cheeseshop.python.org/pypi/zope.app.layers
  license: Zope Public License
  summary: 'Zope 3 Layer Container Package'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
