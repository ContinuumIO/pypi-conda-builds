package:
  name: collective.flag
  version: "1.3"

source:
  fn: collective.flag-1.3.zip
  url: https://pypi.python.org/packages/source/c/collective.flag/collective.flag-1.3.zip
  md5: 87e535179c414319087dd9f94eece547
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.flag = collective.flag:main
    #
    # Would create an entry point called collective.flag that calls collective.flag.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - archetypes.schemaextender
    - archetypes.markerfield

  run:
    - python
    - setuptools
    - setuptools
    - archetypes.schemaextender
    - archetypes.markerfield

test:
  # Python imports
  imports:
    - collective
    - collective.flag
    - collective.flag.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/collective.flag
  license: GPL
  summary: "Makes it possible to flag items as 'special'."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
