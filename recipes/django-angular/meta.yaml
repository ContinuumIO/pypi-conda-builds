package:
  name: django-angular
  version: "0.7.13"

source:
  fn: django-angular-0.7.13.tar.gz
  url: https://pypi.python.org/packages/source/d/django-angular/django-angular-0.7.13.tar.gz
  md5: 47ed9f7ef25ea556b2968f9020a00ca2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-angular = django-angular:main
    #
    # Would create an entry point called django-angular that calls django-angular.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - djangular
    - djangular.core
    - djangular.forms
    - djangular.styling
    - djangular.styling.bootstrap3
    - djangular.templatetags
    - djangular.views

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jrief/django-angular
  license: MIT License
  summary: 'Let Django play well with AngularJS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
