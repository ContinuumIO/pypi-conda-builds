package:
  name: ipchange
  version: "0.11"

source:
  fn: ipchange-0.11.zip
  url: https://pypi.python.org/packages/source/i/ipchange/ipchange-0.11.zip
  md5: f41f0df69d815dca80bfc2697aee92fa
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ipchange = ipchange:main
    #
    # Would create an entry point called ipchange that calls ipchange.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - IPChange
    - ipchange

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://sourceforge.net/projects/ubuntustaticipchange/
  license: BSD License
  summary: 'change ubunt static ip address by manipulating the file /etc/network/interfaces, but you must get the root previlege before to call this lib '

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
