package:
  name: caipyrus
  version: "0.2"

source:
  fn: caipyrus-0.2.tar.gz
  url: https://pypi.python.org/packages/source/c/caipyrus/caipyrus-0.2.tar.gz
  md5: 7f76e1da7774216de5130c6ea701a64f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - caipyrus = caipyrus:main
    #
    # Would create an entry point called caipyrus that calls caipyrus.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - caipyrus

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/ninowalker/caipyrus/tree/master
  license: BSD
  summary: 'Cairo based procedural rendering tool.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
