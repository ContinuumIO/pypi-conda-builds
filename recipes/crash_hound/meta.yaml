package:
  name: crash_hound
  version: "2.2"

source:
  fn: crash_hound-2.2.tar.gz
  url: https://pypi.python.org/packages/source/c/crash_hound/crash_hound-2.2.tar.gz
  md5: 7e4b4b52b71596e6d8296d4cc7c2d90a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - crash_hound = crash_hound:main
    #
    # Would create an entry point called crash_hound that calls crash_hound.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - crash_hound
    - test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.amix.dk/
  license: BSD License
  summary: 'Monitor anything and get notifications directly on your iPhone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
