package:
  name: firstupload_efyn
  version: "1.0.0"

source:
  fn: firstupload_efyn-1.0.0.zip
  url: https://pypi.python.org/packages/source/f/firstupload_efyn/firstupload_efyn-1.0.0.zip
  md5: edaf18324826c37e8d8bb3ea2ace753d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - firstupload_efyn = firstupload_efyn:main
    #
    # Would create an entry point called firstupload_efyn that calls firstupload_efyn.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - firstupload_efyn

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: UNKNOWN
  summary: 'A simple printer of nested listes'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
