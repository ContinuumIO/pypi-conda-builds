package:
  name: archetypes.rolefield
  version: "0.1-a1"

source:
  fn: archetypes.rolefield-0.1-a1.tar.gz
  url: https://pypi.python.org/packages/source/a/archetypes.rolefield/archetypes.rolefield-0.1-a1.tar.gz
  md5: 330d6daa44da1fce29614792821d8beb
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - archetypes.rolefield = archetypes.rolefield:main
    #
    # Would create an entry point called archetypes.rolefield that calls archetypes.rolefield.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - archetypes
    - archetypes.rolefield
    - archetypes.rolefield.example
    - archetypes.rolefield.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/rolefield
  license: GPL
  summary: 'Archetypes Role Field'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
