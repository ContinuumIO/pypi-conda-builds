package:
  name: flask-permissions
  version: "0.2.3"

source:
  fn: Flask-Permissions-0.2.3.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Permissions/Flask-Permissions-0.2.3.tar.gz
  md5: 0a2f23df430abfab8736dcd41c97a177
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-permissions = flask-permissions:main
    #
    # Would create an entry point called flask-permissions that calls flask-permissions.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - flask-sqlalchemy

  run:
    - python
    - flask
    - flask-sqlalchemy

test:
  # Python imports
  imports:
    - flask_permissions

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/raddevon/flask-permissions
  license: MIT License
  summary: 'Simple user permissions for Flask'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
