package:
  name: zerorpc-statsd
  version: "0.1.0"

source:
  fn: zerorpc-statsd-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/z/zerorpc-statsd/zerorpc-statsd-0.1.0.tar.gz
  md5: 3925eb826a05a4d8fb2a27e530666a44
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zerorpc-statsd = zerorpc-statsd:main
    #
    # Would create an entry point called zerorpc-statsd that calls zerorpc-statsd.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - statsd >=0.5.1

  run:
    - python
    - statsd >=0.5.1

test:
  # Python imports
  imports:
    - zerorpc_statsd

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/aluzzardi/zerorpc-statsd
  license: MIT License
  summary: 'StatsD Middleware for ZeroRPC.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
