package:
  name: circus-env-modifier
  version: "0.1.1"

source:
  fn: circus-env-modifier-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/c/circus-env-modifier/circus-env-modifier-0.1.1.tar.gz
  md5: 5b4348486ea13ecc6d97bfdae46ec64a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - circus-env-modifier = circus-env-modifier:main
    #
    # Would create an entry point called circus-env-modifier that calls circus-env-modifier.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - circus_env_modifier

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/msiedlarek/circus-env-modifier
  license: Apache Software License
  summary: 'Mozilla Circus hook for modifying environment with an external command.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
