package:
  name: friendly_curl
  version: "0.9.3"

source:
  fn: friendly_curl-0.9.3.tar.gz
  url: https://pypi.python.org/packages/source/f/friendly_curl/friendly_curl-0.9.3.tar.gz
  md5: 65780f3e596a9e234525c1b6f1f7dd2b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - friendly_curl = friendly_curl:main
    #
    # Would create an entry point called friendly_curl that calls friendly_curl.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pycurl >=7.16.4
    - httplib2

  run:
    - python
    - setuptools
    - pycurl >=7.16.4
    - httplib2

test:
  # Python imports
  imports:
    - friendly_curl
    - friendly_curl.tests
    - friendly_curl.tests.unit

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/friendlycurl/
  license: Apache Software License
  summary: 'A friendly interface to PyCURL.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
