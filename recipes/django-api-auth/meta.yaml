package:
  name: django-api-auth
  version: "0.4"

source:
  fn: django-api-auth-0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-api-auth/django-api-auth-0.4.tar.gz
  md5: 2a19f8e33f711703fea4ae7864e9d597
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-api-auth = django-api-auth:main
    #
    # Would create an entry point called django-api-auth that calls django-api-auth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - api_auth
    - api_auth.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/cridenour/django-api-auth
  license: MIT
  summary: 'django-api-auth leverages tokens for authentication and was built for use with the Angular HTTP Auth Interceptor'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
