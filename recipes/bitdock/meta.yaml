package:
  name: bitdock
  version: "0.1.0"

source:
  fn: Bitdock-0.1.0.zip
  url: https://pypi.python.org/packages/source/B/Bitdock/Bitdock-0.1.0.zip
  md5: 128f94927033004a9ccec18c51fa8d78
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bitdock = bitdock:main
    #
    # Would create an entry point called bitdock that calls bitdock.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cherrypy >=3.2,<4
    - requests >=2.2,<3

  run:
    - python
    - cherrypy >=3.2,<4
    - requests >=2.2,<3

test:
  # Python imports
  imports:
    - bitdock

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/ftrack/bitdock
  license: Apache License (2.0)
  summary: 'Push Bitbucket pull requests to Flowdock.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
