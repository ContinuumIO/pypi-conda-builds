package:
  name: collective.portletmetadata
  version: "1.2"

source:
  fn: collective.portletmetadata-1.2.zip
  url: https://pypi.python.org/packages/source/c/collective.portletmetadata/collective.portletmetadata-1.2.zip
  md5: efc951817af2b9464c5894fa2e88763d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.portletmetadata = collective.portletmetadata:main
    #
    # Would create an entry point called collective.portletmetadata that calls collective.portletmetadata.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - collective.monkeypatcher
    - plone.portlets
    - plone.app.portlets
    - setuptools
    - z3c.jbot
    - z3c.unconfigure ==1.0.1

  run:
    - python
    - setuptools
    - collective.monkeypatcher
    - plone.portlets
    - plone.app.portlets
    - setuptools
    - z3c.jbot
    - z3c.unconfigure ==1.0.1

test:
  # Python imports
  imports:
    - collective
    - collective.portletmetadata

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/collective/collective.portletmetadata
  license: GPL
  summary: 'Adds metadata functionality to portlets'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
