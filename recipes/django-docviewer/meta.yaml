package:
  name: django-docviewer
  version: "0.4"

source:
  fn: django-docviewer-0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-docviewer/django-docviewer-0.4.tar.gz
  md5: 58609b524531184f62e62d1ece5e2fa0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-docviewer = django-docviewer:main
    #
    # Would create an entry point called django-docviewer that calls django-docviewer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-autoslug
    - django-model-utils
    - celery
    - django-haystack
    - django-pipeline

  run:
    - python
    - django-autoslug
    - django-model-utils
    - celery
    - django-haystack
    - django-pipeline

test:
  # Python imports
  imports:
    - docviewer
    - docviewer.management
    - docviewer.management.commands
    - docviewer.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/oxys-net/django-docviewer
  license: Apache Software License
  summary: 'documentcloud for django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
