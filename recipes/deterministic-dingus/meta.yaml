package:
  name: deterministic-dingus
  version: "0.0.5"

source:
  fn: deterministic-dingus-0.0.5.tar.gz
  url: https://pypi.python.org/packages/source/d/deterministic-dingus/deterministic-dingus-0.0.5.tar.gz
  md5: 9ca373b9e51506e8ab3e1b9c4d5a541e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - deterministic-dingus = deterministic-dingus:main
    #
    # Would create an entry point called deterministic-dingus that calls deterministic-dingus.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - dingus

  run:
    - python
    - dingus

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/allancaffee/deterministic-dingus
  license: MIT License
  summary: 'A handy extension of the dingus record and assert library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
