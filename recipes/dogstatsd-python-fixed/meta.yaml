package:
  name: dogstatsd-python-fixed
  version: "0.5.0"

source:
  fn: dogstatsd-python-fixed-0.5.0.tar.gz
  url: https://pypi.python.org/packages/source/d/dogstatsd-python-fixed/dogstatsd-python-fixed-0.5.0.tar.gz
  md5: c3f092ef164a6db1c7db0b9820f3099c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dogstatsd-python-fixed = dogstatsd-python-fixed:main
    #
    # Would create an entry point called dogstatsd-python-fixed that calls dogstatsd-python-fixed.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.datadoghq.com
  license: BSD
  summary: "Python bindings to Datadog's API and a user-facing command line tool."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
