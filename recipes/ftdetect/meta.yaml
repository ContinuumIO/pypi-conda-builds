package:
  name: ftdetect
  version: "1.0.0"

source:
  fn: ftdetect-1.0.0.zip
  url: https://pypi.python.org/packages/source/f/ftdetect/ftdetect-1.0.0.zip
  md5: 632d9bd695cfc9fc7994389f8b37356d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ftdetect = ftdetect:main
    #
    # Would create an entry point called ftdetect that calls ftdetect.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - ftdetect

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'An assortment of algorithms related to computer vision and image feature detection'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
