package:
  name: anz.dashboard
  version: "0.7.0"

source:
  fn: anz.dashboard-0.7.0.zip
  url: https://pypi.python.org/packages/source/a/anz.dashboard/anz.dashboard-0.7.0.zip
  md5: 1975b0b6b88c16ff286433277bd2eb0b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - anz.dashboard = anz.dashboard:main
    #
    # Would create an entry point called anz.dashboard that calls anz.dashboard.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - feedparser
    - python-cjson

  run:
    - python
    - setuptools
    - setuptools
    - feedparser
    - python-cjson

test:
  # Python imports
  imports:
    - anz
    - anz.dashboard
    - anz.dashboard.browser
    - anz.dashboard.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/anz.dashboard/
  license: GPL
  summary: 'Plone netvibes like dashboard implementation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
