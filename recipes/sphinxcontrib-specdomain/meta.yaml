package:
  name: sphinxcontrib-specdomain
  version: "1.04.02"

source:
  fn: sphinxcontrib-specdomain-1.04.02.tar.gz
  url: https://pypi.python.org/packages/source/s/sphinxcontrib-specdomain/sphinxcontrib-specdomain-1.04.02.tar.gz
  md5: 6789cb5c3eb73b42f92f040da85ddc5f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sphinxcontrib-specdomain = sphinxcontrib-specdomain:main
    #
    # Would create an entry point called sphinxcontrib-specdomain that calls sphinxcontrib-specdomain.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sphinx >=1.1.1

  run:
    - python
    - sphinx >=1.1.1

test:
  # Python imports
  imports:
    - sphinxcontrib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://specdomain.readthedocs.org
  license: Argonne OPEN SOURCE LICENSE, see LICENSE file for details
  summary: 'Sphinx "specdomain" extension'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
