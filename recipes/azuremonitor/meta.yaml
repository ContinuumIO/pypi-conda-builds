package:
  name: azuremonitor
  version: "0.1"

source:
  fn: azuremonitor-0.1.tar.gz
  url: https://pypi.python.org/packages/source/a/azuremonitor/azuremonitor-0.1.tar.gz
  md5: 35e91659afa77fd5743f319e355b482c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - azuremonitor = azuremonitor:main
    #
    # Would create an entry point called azuremonitor that calls azuremonitor.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - azuremonitor

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://pypi.python.org/pypi/azuremonitor/
  license: Apache License, Version 2.0
  summary: 'Windows Azure Monitor'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
