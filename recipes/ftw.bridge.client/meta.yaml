package:
  name: ftw.bridge.client
  version: "1.0.12"

source:
  fn: ftw.bridge.client-1.0.12.zip
  url: https://pypi.python.org/packages/source/f/ftw.bridge.client/ftw.bridge.client-1.0.12.zip
  md5: bffd9d689340d923a3075162094d3949
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ftw.bridge.client = ftw.bridge.client:main
    #
    # Would create an entry point called ftw.bridge.client that calls ftw.bridge.client.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - accesscontrol
    - acquisition
    - missing
    - products.cmfcore
    - products.cmfplone
    - products.genericsetup
    - products.pluggableauthservice
    - products.statusmessages
    - zope2
    - ftw.profilehook
    - plone.app.portlets
    - plone.memoize
    - plone.portlets
    - setuptools
    - zope.app.component
    - zope.component
    - zope.formlib
    - zope.i18nmessageid
    - zope.interface
    - zope.publisher
    - zope.schema

  run:
    - python
    - setuptools
    - accesscontrol
    - acquisition
    - missing
    - products.cmfcore
    - products.cmfplone
    - products.genericsetup
    - products.pluggableauthservice
    - products.statusmessages
    - zope2
    - ftw.profilehook
    - plone.app.portlets
    - plone.memoize
    - plone.portlets
    - setuptools
    - zope.app.component
    - zope.component
    - zope.formlib
    - zope.i18nmessageid
    - zope.interface
    - zope.publisher
    - zope.schema

test:
  # Python imports
  imports:
    - ftw
    - ftw.bridge
    - ftw.bridge.client
    - ftw.bridge.client.browser
    - ftw.bridge.client.portlets
    - ftw.bridge.client.tabbed
    - ftw.bridge.client.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/4teamwork/ftw.bridge.client
  license: GNU General Public License (GPL)
  summary: 'Adds ftw.bridge support to plone.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
