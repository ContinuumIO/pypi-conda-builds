package:
  name: django-cms-lite
  version: "2015.04.22.0"

source:
  fn: django-cms-lite-2015.04.22.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-cms-lite/django-cms-lite-2015.04.22.0.tar.gz
  md5: 06f81a6c510b206d408bc080c20dfe35
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-cms-lite = django-cms-lite:main
    #
    # Would create an entry point called django-cms-lite that calls django-cms-lite.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - cms_lite
    - cms_lite.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bradbeattie/django-cms-lite
  license: BSD License
  summary: 'Simple Django CMS with very limited functionality.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
