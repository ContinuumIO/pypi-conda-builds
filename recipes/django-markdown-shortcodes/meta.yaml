package:
  name: django-markdown-shortcodes
  version: "1.3"

source:
  fn: django-markdown-shortcodes-1.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-markdown-shortcodes/django-markdown-shortcodes-1.3.tar.gz
  md5: 2c34a94f8cfd81113a034e54bda2859f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-markdown-shortcodes = django-markdown-shortcodes:main
    #
    # Would create an entry point called django-markdown-shortcodes that calls django-markdown-shortcodes.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3

  run:
    - python
    - django >=1.3

test:
  # Python imports
  imports:
    - markdown_shortcodes
    - markdown_shortcodes.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/defbyte/django-markdown-shortcodes
  license: BSD License
  summary: 'Utility adding WordPress-like shortcodes for Markdown authoring in Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
