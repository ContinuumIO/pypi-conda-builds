package:
  name: couchdb-fuse
  version: "0.2dev"

source:
  fn: CouchDB-FUSE-0.2dev.tar.gz
  url: https://pypi.python.org/packages/source/C/CouchDB-FUSE/CouchDB-FUSE-0.2dev.tar.gz
  md5: 30784a83d8075f891077e8f3a7856313
#  patches:
   # List any patch files here
   # - fix.patch

build:
  # preserve_egg_dir: True
  entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - couchdb-fuse = couchdb-fuse:main
    #
    # Would create an entry point called couchdb-fuse that calls couchdb-fuse.main()

    - couchmount = couchmount:main

  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - couchdb >=0.5

  run:
    - python
    - couchdb >=0.5

test:
  # Python imports
  # imports:

  commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.

    - couchmount --help

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/couchdb-fuse/
  license: BSD License
  summary: 'CouchDB FUSE module'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
