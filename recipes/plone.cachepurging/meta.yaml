package:
  name: plone.cachepurging
  version: "1.0.7"

source:
  fn: plone.cachepurging-1.0.7.zip
  url: https://pypi.python.org/packages/source/p/plone.cachepurging/plone.cachepurging-1.0.7.zip
  md5: bcff39ad707bfbdeb96d87ae0b37e136
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.cachepurging = plone.cachepurging:main
    #
    # Would create an entry point called plone.cachepurging that calls plone.cachepurging.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - five.globalrequest
    - plone.registry
    - z3c.caching
    - zope.annotation
    - zope.component
    - zope.event
    - zope.i18nmessageid
    - zope.interface
    - zope.lifecycleevent
    - zope.schema
    - zope.testing
    - zope2

  run:
    - python
    - setuptools
    - five.globalrequest
    - plone.registry
    - z3c.caching
    - zope.annotation
    - zope.component
    - zope.event
    - zope.i18nmessageid
    - zope.interface
    - zope.lifecycleevent
    - zope.schema
    - zope.testing
    - zope2

test:
  # Python imports
  imports:
    - plone
    - plone.cachepurging
    - plone.cachepurging.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/plone.cachepurging
  license: GPL version 2
  summary: 'Cache purging support for Zope 2 applications'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
