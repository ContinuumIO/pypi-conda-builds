package:
  name: django-rq-email-backend
  version: "0.1.3"

source:
  fn: django-rq-email-backend-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-rq-email-backend/django-rq-email-backend-0.1.3.tar.gz
  md5: ff17ddd8066bf3f96e9f394b76b5f235
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-rq-email-backend = django-rq-email-backend:main
    #
    # Would create an entry point called django-rq-email-backend that calls django-rq-email-backend.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4
    - rq >=0.3.4
    - django_rq >=0.4.6

  run:
    - python
    - django >=1.4
    - rq >=0.3.4
    - django_rq >=0.4.6

test:
  # Python imports
  imports:
    - django_rq_email_backend

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jefftriplett/django-rq-email-backend
  license: BSD License
  summary: 'Provides Django email integration for RQ (Redis Queue)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
