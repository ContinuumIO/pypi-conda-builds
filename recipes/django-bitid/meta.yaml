package:
  name: django-bitid
  version: "0.1.1"

source:
  fn: django-bitid-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-bitid/django-bitid-0.1.1.tar.gz
  md5: 5abbb1379153b90198aaba41b6c9de02
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-bitid = django-bitid:main
    #
    # Would create an entry point called django-bitid that calls django-bitid.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - south >=0.8.4
    - pytz >=2014.3
    - pybitid ==0.0.4

  run:
    - python
    - south >=0.8.4
    - pytz >=2014.3
    - pybitid ==0.0.4

test:
  # Python imports
  imports:
    - djbitid

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/manuelzs/django-bitid/
  license: MIT License
  summary: 'App for django bitId authentication'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
