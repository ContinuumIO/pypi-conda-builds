package:
  name: gacli
  version: "1.0.2"

source:
  fn: gacli-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/g/gacli/gacli-1.0.2.tar.gz
  md5: d0c2e8b937403373408669ec04ded99d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gacli = gacli:main
    #
    # Would create an entry point called gacli that calls gacli.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - onetimepass

  run:
    - python
    - onetimepass

test:
  # Python imports
  imports:
    - gacli

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ClockworkNet/gacli/
  license: MIT License
  summary: 'Google Authentication command line tools'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
