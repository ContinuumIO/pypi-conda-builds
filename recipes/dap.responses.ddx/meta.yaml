package:
  name: dap.responses.ddx
  version: "0.1.2"

source:
  fn: dap.responses.ddx-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/dap.responses.ddx/dap.responses.ddx-0.1.2.tar.gz
  md5: 4a72c203a5a13dd50728949807655e08
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dap.responses.ddx = dap.responses.ddx:main
    #
    # Would create an entry point called dap.responses.ddx that calls dap.responses.ddx.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - dap[server] >=2.2.6
    - elementtree

  run:
    - python
    - setuptools
    - dap[server] >=2.2.6
    - elementtree

test:
  # Python imports
  imports:
    - dap
    - dap.responses
    - dap.responses.ddx

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pydap.org/responses/ddx.html
  license: MIT License
  summary: 'Experimental DDX response for pydap server'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
