package:
  name: coopr.mpec
  version: "1.0"

source:
  fn: coopr.mpec-1.0.tar.gz
  url: https://pypi.python.org/packages/source/c/coopr.mpec/coopr.mpec-1.0.tar.gz
  md5: f538090d476523e209225cb164a1d17e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - coopr.mpec = coopr.mpec:main
    #
    # Would create an entry point called coopr.mpec that calls coopr.mpec.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - coopr
    - coopr.mpec
    - coopr.mpec.plugins
    - coopr.mpec.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://software.sandia.gov/svn/public/coopr/coopr.mpec
  license: BSD License
  summary: 'Coopr modeling extensions for Mathematical Programming with Equilibrium Constraints (MPEC)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
