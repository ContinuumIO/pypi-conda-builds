package:
  name: brickpi_python
  version: "1.0"

source:
  fn: brickpi_python-1.0.tar.gz
  url: https://pypi.python.org/packages/source/b/brickpi_python/brickpi_python-1.0.tar.gz
  md5: aa1fffe9a9533502545726093ca6346a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - brickpi_python = brickpi_python:main
    #
    # Would create an entry point called brickpi_python that calls brickpi_python.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - brickpi_python

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/DexterInd/BrickPi_Python
  license: UNKNOWN
  summary: 'This library for using the BrickPi in Python.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
