package:
  name: python-tvrage
  version: "0.4.1"

source:
  fn: python-tvrage-0.4.1.tar.gz
  url: https://pypi.python.org/packages/source/p/python-tvrage/python-tvrage-0.4.1.tar.gz
  md5: cdfec252158c5047b626861900186dfb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-tvrage = python-tvrage:main
    #
    # Would create an entry point called python-tvrage that calls python-tvrage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - beautifulsoup

  run:
    - python
    - beautifulsoup

test:
  # Python imports
  imports:
    - tvrage

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ckreutzer/python-tvrage
  license: BSD License
  summary: 'python client for the tvrage.com XML API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
