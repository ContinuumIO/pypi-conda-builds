package:
  name: marrow.util
  version: "1.2.3"

source:
  fn: marrow.util-1.2.3.tar.gz
  url: https://pypi.python.org/packages/source/m/marrow.util/marrow.util-1.2.3.tar.gz
  md5: f59b99e6e0033f19fc4a09bac19a1c99
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - marrow.util = marrow.util:main
    #
    # Would create an entry point called marrow.util that calls marrow.util.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - marrow
    - marrow.util
    - marrow.util.context

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/marrow/marrow.util
  license: MIT License
  summary: 'A collection of many commonly reimplemented utility classes and functions.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
