package:
  name: plonetheme.basic
  version: "0.6"

source:
  fn: plonetheme.basic-0.6.zip
  url: https://pypi.python.org/packages/source/p/plonetheme.basic/plonetheme.basic-0.6.zip
  md5: 4eea7bae5d1afba2d2a38bebe7006cb0
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plonetheme.basic = plonetheme.basic:main
    #
    # Would create an entry point called plonetheme.basic that calls plonetheme.basic.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - plonetheme
    - plonetheme.basic

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.sixfeetup.com
  license: GPL
  summary: 'An easily customizable theme for Plone 4'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
