package:
  name: argument
  version: "0.3.1"

source:
  fn: argument-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/a/argument/argument-0.3.1.tar.gz
  md5: 23d01ea5a2f1bcc21a7b817b090e7f0a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - argument = argument:main
    #
    # Would create an entry point called argument that calls argument.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - argument

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/oskarnyqvists/arguments
  license: argumented
  summary: 'Command line argument parsing library for python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
