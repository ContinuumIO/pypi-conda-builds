package:
  name: vcsserver
  version: "0.3.1"

source:
  fn: vcsserver-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/v/vcsserver/vcsserver-0.3.1.tar.gz
  md5: c0746432d1e32929465d8c176e98acbe
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - vcsserver = vcsserver:main
    #
    # Would create an entry point called vcsserver that calls vcsserver.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - twisted
    - pycrypto
    - pyasn1

  run:
    - python
    - twisted
    - pycrypto
    - pyasn1

test:
  # Python imports
  imports:
    - vcsserver

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/3f17/vcssshd-lib
  license: BSD License
  summary: 'Library for easy and fast creation of an ssh demon for popular VCSs (Mercurial and Git)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
