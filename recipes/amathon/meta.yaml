package:
  name: amathon
  version: "0.2"

source:
  fn: amathon-0.2.tar.gz
  url: https://pypi.python.org/packages/source/a/amathon/amathon-0.2.tar.gz
  md5: 21de358c366b556ad4d2a8969f32a72e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - amathon = amathon:main
    #
    # Would create an entry point called amathon that calls amathon.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - amathon

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/y42sora/Amathon
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Amazon api wrapper for Python 3.x'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
