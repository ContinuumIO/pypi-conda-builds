package:
  name: gengzip
  version: "0.2.3"

source:
  fn: gengzip-0.2.3.tar.gz
  url: https://pypi.python.org/packages/source/g/gengzip/gengzip-0.2.3.tar.gz
  md5: c0a2eaaa0010fb85eb41aa81b941bba7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gengzip = gengzip:main
    #
    # Would create an entry point called gengzip that calls gengzip.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - gengzip

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/leetreveil/gengzip
  license: MIT
  summary: 'A python module for gzipping data using generators'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
