package:
  name: bibliograph.parsing
  version: "1.0.1"

source:
  fn: bibliograph.parsing-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/b/bibliograph.parsing/bibliograph.parsing-1.0.1.tar.gz
  md5: bf291d4c00da0fd9532b9d7627fda577
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bibliograph.parsing = bibliograph.parsing:main
    #
    # Would create an entry point called bibliograph.parsing that calls bibliograph.parsing.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - bibliograph.core

  run:
    - python
    - setuptools
    - setuptools
    - bibliograph.core

test:
  # Python imports
  imports:
    - bibliograph
    - bibliograph.parsing
    - bibliograph.parsing.parsers
    - bibliograph.parsing.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/bibliograph.parsing/
  license: ZPL 2.1
  summary: 'Parsers for bibliograph packages'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
