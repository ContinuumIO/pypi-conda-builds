package:
  name: cmdutils
  version: "0.1"

source:
  fn: CmdUtils-0.1.tar.gz
  url: https://pypi.python.org/packages/source/C/CmdUtils/CmdUtils-0.1.tar.gz
  md5: d8a0be059912bd6c6ff998207b114c62
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cmdutils = cmdutils:main
    #
    # Would create an entry point called cmdutils that calls cmdutils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - cmdutils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pythonpaste.org/cmdutils/
  license: MIT License
  summary: 'Routines to help make command-line utilities easier to write'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
