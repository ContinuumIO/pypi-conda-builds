package:
  name: feedformatter
  version: "0.4"

source:
  fn: feedformatter-0.4.tar.bz2
  url: https://pypi.python.org/packages/source/f/feedformatter/feedformatter-0.4.tar.bz2
  md5: 9aefe618bc4a238940759050e0b97821
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - feedformatter = feedformatter:main
    #
    # Would create an entry point called feedformatter that calls feedformatter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - feedformatter

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/feedparser/
  license: BSD License
  summary: 'A Python library for generating news feeds in RSS and Atom formats'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
