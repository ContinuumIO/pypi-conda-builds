package:
  name: completegenomicstools
  version: "0.1.2dev"

source:
  fn: CompleteGenomicsTools-0.1.2dev.tar.gz
  url: https://pypi.python.org/packages/source/C/CompleteGenomicsTools/CompleteGenomicsTools-0.1.2dev.tar.gz
  md5: ec6981d24cb73b225e0db91895d01698
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - completegenomicstools = completegenomicstools:main
    #
    # Would create an entry point called completegenomicstools that calls completegenomicstools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - completegenomicstools

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/seandavi/CompleteGenomicsTools
  license: GPL-2
  summary: 'Tools for manipulating and visualizing data from Complete Genomics'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
