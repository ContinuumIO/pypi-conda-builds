package:
  name: django-static-precompiler
  version: "0.9"

source:
  fn: django-static-precompiler-0.9.tar.gz
  url: https://pypi.python.org/packages/source/d/django-static-precompiler/django-static-precompiler-0.9.tar.gz
  md5: 7f454f35b94f705f2ec5d0a7b54e66af
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-static-precompiler = django-static-precompiler:main
    #
    # Would create an entry point called django-static-precompiler that calls django-static-precompiler.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - static_precompiler
    - static_precompiler.compilers
    - static_precompiler.management
    - static_precompiler.management.commands
    - static_precompiler.migrations
    - static_precompiler.south_migrations
    - static_precompiler.templatetags
    - static_precompiler.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/andreyfedoseev/django-static-precompiler
  license: BSD License
  summary: 'Django template tags to compile all kinds of static files (SASS, LESS, CoffeeScript, ECMAScript 6to5).'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
