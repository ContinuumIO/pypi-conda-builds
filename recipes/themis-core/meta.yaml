package:
  name: themis-core
  version: "0.1"

source:
  fn: themis-core-0.1.tar.gz
  url: https://pypi.python.org/packages/source/t/themis-core/themis-core-0.1.tar.gz
  md5: 43dfaa8d1f1d0f4478d2b753765b8b5d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - themis-core = themis-core:main
    #
    # Would create an entry point called themis-core that calls themis-core.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - netaddr
    - pyyaml
    - redis
    - numpy

  run:
    - python
    - netaddr
    - pyyaml
    - redis
    - numpy

test:
  # Python imports
  imports:
    - themis

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/sandromello/themis-py
  license: UNKNOWN
  summary: 'Core themis library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
