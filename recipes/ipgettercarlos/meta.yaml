package:
  name: ipgettercarlos
  version: "0.1"

source:
  fn: ipgettercarlos-0.1.tar.gz
  url: https://pypi.python.org/packages/source/i/ipgettercarlos/ipgettercarlos-0.1.tar.gz
  md5: de896717c116a39d6ce6e7bab58d80f7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ipgettercarlos = ipgettercarlos:main
    #
    # Would create an entry point called ipgettercarlos that calls ipgettercarlos.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - ipgettercarlos

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: MIT License
  summary: 'a copy of ipgetter made by phoemur which basically does what the name says but improved to work'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
