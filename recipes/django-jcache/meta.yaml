package:
  name: django-jcache
  version: "0.6"

source:
  fn: django-jcache-0.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-jcache/django-jcache-0.6.tar.gz
  md5: c00754d3f1d35724c879209b054b7c35
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-jcache = django-jcache:main
    #
    # Would create an entry point called django-jcache that calls django-jcache.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3
    - celery

  run:
    - python
    - django >=1.3
    - celery

test:
  # Python imports
  imports:
    - jcache

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT License
  summary: 'JCache support for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
