package:
  name: pynagios
  version: "0.1.1"

source:
  fn: pynagios-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/p/pynagios/pynagios-0.1.1.tar.gz
  md5: ca1724333b3cd63259934b4271f3e6b6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pynagios = pynagios:main
    #
    # Would create an entry point called pynagios that calls pynagios.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - pynagios

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://kiip.github.com/pynagios/
  license: MIT License
  summary: 'Python library to write Nagios plugins.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
