package:
  name: django-yui-loader
  version: "0.3.2"

source:
  fn: django-yui-loader-0.3.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-yui-loader/django-yui-loader-0.3.2.tar.gz
  md5: 27f532835ed848153ab1551f671e32da
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-yui-loader = django-yui-loader:main
    #
    # Would create an entry point called django-yui-loader that calls django-yui-loader.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - yui_loader

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/akaihola/django-yui-loader
  license: BSD License
  summary: 'Server-side middleware which implements some of the functionality in the Yahoo User Interface Loader component.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
