package:
  name: pyexeccontrol
  version: "0.0.3"

source:
  fn: pyexeccontrol-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/p/pyexeccontrol/pyexeccontrol-0.0.3.tar.gz
  md5: 648edde70727a30812c0637495074101
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pyexeccontrol = pyexeccontrol:main
    #
    # Would create an entry point called pyexeccontrol that calls pyexeccontrol.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyparsing ==1.5.6
    - pexpect ==2.4

  run:
    - python
    - pyparsing ==1.5.6
    - pexpect ==2.4

test:
  # Python imports
  imports:
    - execcontrol
    - pyexeccontrol

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://packages.python.org/pyexeccontrol
  license: BSD
  summary: 'An interface to gdb to control program execution'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
