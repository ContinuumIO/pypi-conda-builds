package:
  name: django-ember-index
  version: "0.2.0"

source:
  fn: django-ember-index-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-ember-index/django-ember-index-0.2.0.tar.gz
  md5: d295db1ed328e8bd707383232c8af6c5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-ember-index = django-ember-index:main
    #
    # Would create an entry point called django-ember-index that calls django-ember-index.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - ember_index

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bobisjan.com/django-ember-index
  license: MIT License
  summary: 'A Django app to serve Ember index files.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
