package:
  name: django-queryset-iterator
  version: "0.1.1"

source:
  fn: django-queryset-iterator-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-queryset-iterator/django-queryset-iterator-0.1.1.tar.gz
  md5: 24af6f2f8f4abb5448a71d923a2d2774
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-queryset-iterator = django-queryset-iterator:main
    #
    # Would create an entry point called django-queryset-iterator that calls django-queryset-iterator.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - queryset_iterator

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Andrew-Crosio/django-queryset-iterator
  license: UNKNOWN
  summary: 'Iterate over large Data-sets in Django efficiently.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
