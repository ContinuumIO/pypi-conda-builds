package:
  name: ledgerx-client-protocol
  version: "1.0.1"

source:
  fn: ledgerx-client-protocol-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/l/ledgerx-client-protocol/ledgerx-client-protocol-1.0.1.tar.gz
  md5: e05657598b61acf8cc1c952083abd963
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ledgerx-client-protocol = ledgerx-client-protocol:main
    #
    # Would create an entry point called ledgerx-client-protocol that calls ledgerx-client-protocol.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - ledgerx-protocol ==0.4.0
    - semantic-version ==2.3.1

  run:
    - python
    - ledgerx-protocol ==0.4.0
    - semantic-version ==2.3.1

test:
  # Python imports
  imports:
    - ledgerx
    - ledgerx.api
    - ledgerx.api.client

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://ledgerx.com
  license: UNKNOWN
  summary: 'LedgerX Client Protocol Library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
