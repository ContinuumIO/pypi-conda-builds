package:
  name: tamarin
  version: "1.2"

source:
  fn: tamarin-1.2.tar.gz
  url: https://pypi.python.org/packages/source/t/tamarin/tamarin-1.2.tar.gz
  md5: 4a5ca4922a577d2c3a64e9716260ffbb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tamarin = tamarin:main
    #
    # Would create an entry point called tamarin that calls tamarin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - boto
    - pyparsing

  run:
    - python
    - boto
    - pyparsing

test:
  # Python imports
  imports:
    - tamarin
    - tamarin.management
    - tamarin.management.commands
    - tamarin.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/duointeractive/tamarin/
  license: BSD License
  summary: "A Django app for monitoring AWS usage in Django's admin."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
