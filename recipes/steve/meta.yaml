package:
  name: steve
  version: "0.4"

source:
  fn: steve-0.4.tar.gz
  url: https://pypi.python.org/packages/source/s/steve/steve-0.4.tar.gz
  md5: 72869cd960ae1faa8d3783bc05cecccd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - steve = steve:main
    #
    # Would create an entry point called steve that calls steve.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - argparse
    - blessings
    - cssselect
    - html2text
    - jinja2
    - nose
    - vidscraper

  run:
    - python
    - argparse
    - blessings
    - cssselect
    - html2text
    - jinja2
    - nose
    - vidscraper

test:
  # Python imports
  imports:
    - steve
    - steve.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/willkg/steve
  license: BSD License
  summary: 'Command line importer for richard'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
