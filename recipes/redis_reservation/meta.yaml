package:
  name: redis_reservation
  version: "0.1.1"

source:
  fn: redis_reservation-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/r/redis_reservation/redis_reservation-0.1.1.tar.gz
  md5: ee43116a740d175eb16bc5a5565a9884
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - redis_reservation = redis_reservation:main
    #
    # Would create an entry point called redis_reservation that calls redis_reservation.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - redis ==2.8.0

  run:
    - python
    - redis ==2.8.0

test:
  # Python imports
  imports:
    - redis_reservation

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/Clever/python-redis-reservation
  license: BSD
  summary: 'Resource reservation (locking) libraries using a Redis backend, with customizable timeouts and keep-alive support.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
