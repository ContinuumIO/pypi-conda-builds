package:
  name: kikola
  version: "0.5.2"

source:
  fn: kikola-0.5.2.tar.gz
  url: https://pypi.python.org/packages/source/k/kikola/kikola-0.5.2.tar.gz
  md5: 159cbd3982bbb1109f3320c9170653bf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kikola = kikola:main
    #
    # Would create an entry point called kikola that calls kikola.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - kikola
    - kikola.contrib
    - kikola.contrib.basicsearch
    - kikola.core
    - kikola.db
    - kikola.forms
    - kikola.middleware
    - kikola.shortcuts
    - kikola.templatetags
    - kikola.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/playpauseandstop/kikola/
  license: BSD License
  summary: "Kikola is collection of Django's custom form fields and widgets, model fields and reusable apps."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
