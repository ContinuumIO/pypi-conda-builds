package:
  name: python-x10
  version: "0.1"

source:
  fn: Python-X10-0.1.tar.gz
  url: https://pypi.python.org/packages/source/P/Python-X10/Python-X10-0.1.tar.gz
  md5: faecd3ee1901c336aff1f75b7b2b486f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-x10 = python-x10:main
    #
    # Would create an entry point called python-x10 that calls python-x10.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - x10
    - x10/controllers
    - x10/devices

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/glibersat/python-x10
  license: GNU GPL
  summary: 'A python module to control X10 appliances'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
