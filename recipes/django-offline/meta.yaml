package:
  name: django-offline
  version: "0.1"

source:
  fn: django-offline-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-offline/django-offline-0.1.tar.gz
  md5: 2a9368a066d792e86e1379cf53d16b1f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-offline = django-offline:main
    #
    # Would create an entry point called django-offline that calls django-offline.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - offline

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ankitjaiswal07/django-offline
  license: BSD License
  summary: 'A simple Django app to swith your Django project between offline/online mode and display a 503 error page, while your django site is offline (under maintenance)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
