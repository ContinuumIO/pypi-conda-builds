package:
  name: collective.relatedslideshow
  version: "2.8"

source:
  fn: collective.relatedSlideshow-2.8.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.relatedSlideshow/collective.relatedSlideshow-2.8.tar.gz
  md5: d7c034561c47b410338a7852c390261e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.relatedslideshow = collective.relatedslideshow:main
    #
    # Would create an entry point called collective.relatedslideshow that calls collective.relatedslideshow.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.relatedSlideshow
    - collective.relatedSlideshow.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://svn.v2.nl/plone/collective.slideShow/trunk
  license: GPL
  summary: 'Slide show with related items'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
