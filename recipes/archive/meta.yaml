package:
  name: archive
  version: "0.3"

source:
  fn: Archive-0.3.tar.gz
  url: https://pypi.python.org/packages/source/A/Archive/Archive-0.3.tar.gz
  md5: fa2d4c414f87e97c6804e6261936a0ed
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - archive = archive:main
    #
    # Would create an entry point called archive that calls archive.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - archive
    - archive.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/python-archive/
  license: LICENSE.txt
  summary: 'Simple library that provides a common interface for extracting zip and tar archives.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
