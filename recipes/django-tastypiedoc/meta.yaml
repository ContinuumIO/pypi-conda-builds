package:
  name: django-tastypiedoc
  version: "0.2.2"

source:
  fn: django-tastypiedoc-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-tastypiedoc/django-tastypiedoc-0.2.2.tar.gz
  md5: 5aef6639f8259dcecf76a79cf40cd6c5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-tastypiedoc = django-tastypiedoc:main
    #
    # Would create an entry point called django-tastypiedoc that calls django-tastypiedoc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - tastypiedoc

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.hiiamchris.com/
  license: MIT License
  summary: 'A simple Django app to explore tastypie APIs.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
