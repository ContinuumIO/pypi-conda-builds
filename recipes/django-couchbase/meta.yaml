package:
  name: django-couchbase
  version: "0.0.15"

source:
  fn: django-couchbase-0.0.15.zip
  url: https://pypi.python.org/packages/source/d/django-couchbase/django-couchbase-0.0.15.zip
  md5: 4b4b9ea55f377db2a3b14e9aaa476edb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-couchbase = django-couchbase:main
    #
    # Would create an entry point called django-couchbase that calls django-couchbase.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - couchbase
    - urllib3

  run:
    - python
    - couchbase
    - urllib3

test:
  # Python imports
  imports:
    - django_couchbase

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: Python Software Foundation License
  summary: 'couchbase client for django memcache'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
