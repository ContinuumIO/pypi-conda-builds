package:
  name: django-monit
  version: "0.1a"

source:
  fn: django-monit-0.1a.tar.gz
  url: https://pypi.python.org/packages/source/d/django-monit/django-monit-0.1a.tar.gz
  md5: 965fdef10255d051c45ee3c3d0d9bb40
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-monit = django-monit:main
    #
    # Would create an entry point called django-monit that calls django-monit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - lxml

  run:
    - python
    - lxml

test:
  # Python imports
  imports:
    - monit

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/johnpaulett/django-monit
  license: BSD
  summary: 'A simple Django-based Monit collector'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
