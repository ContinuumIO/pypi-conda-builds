package:
  name: django-lastfm-auth
  version: "0.2.3"

source:
  fn: django-lastfm-auth-0.2.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-lastfm-auth/django-lastfm-auth-0.2.3.tar.gz
  md5: 0f6a7f7073c13a42c367bd01febc9c59
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-lastfm-auth = django-lastfm-auth:main
    #
    # Would create an entry point called django-lastfm-auth that calls django-lastfm-auth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-social-auth >=0.3.3

  run:
    - python
    - django-social-auth >=0.3.3

test:
  # Python imports
  imports:
    - lastfm_auth
    - lastfm_auth.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mlavin/django-lastfm-auth
  license: BSD License
  summary: u'django-lastfm-auth is an extension to django-social-auth which adds a backend for Last.fm.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
