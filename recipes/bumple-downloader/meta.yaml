package:
  name: bumple-downloader
  version: "0.1.2"

source:
  fn: bumple-downloader-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/b/bumple-downloader/bumple-downloader-0.1.2.tar.gz
  md5: 1a61ebe030ff6ecd3c212b15489f4719
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bumple-downloader = bumple-downloader:main
    #
    # Would create an entry point called bumple-downloader that calls bumple-downloader.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - bumple

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Creative Commons Attribution-Noncommercial-Share Alike license
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
