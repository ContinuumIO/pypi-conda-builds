package:
  name: globre
  version: "0.1.2"

source:
  fn: globre-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/g/globre/globre-0.1.2.tar.gz
  md5: 8fed2d9b364c027777b07e867baf7284
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - globre = globre:main
    #
    # Would create an entry point called globre that calls globre.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - distribute >=0.6.24

  run:
    - python
    - distribute >=0.6.24

test:
  # Python imports
  imports:
    - globre

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/metagriffin/globre
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'A glob matching library, providing an interface similar to the "re" module.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
