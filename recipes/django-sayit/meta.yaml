package:
  name: django-sayit
  version: "1.3.2"

source:
  fn: django-sayit-1.3.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-sayit/django-sayit-1.3.2.tar.gz
  md5: 3458fc950aa74feefe40328fe52015d4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-sayit = django-sayit:main
    #
    # Would create an entry point called django-sayit that calls django-sayit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - psycopg2 >=2.5.1,<2.6
    - pytz >=2013d
    - six >=1.4.1
    - django >=1.4.2,<1.8
    - django-select2 >=4.2.2,<4.3
    - django-qmethod ==0.0.3
    - audioread >=1.0.1
    - pyelasticsearch >=0.6,<0.7
    - django-haystack >=2.1,<2.2
    - django-bleach >=0.2.1
    - mysociety-django-popolo >=0.0.2
    - mysociety-django-sluggable >=0.2.6
    - django-subdomain-instances >=0.10.2
    - easy-thumbnails >=2.1
    - unicode-slugify ==0.1.1
    - pyopenssl ==0.14
    - ndg-httpsclient ==0.3.2
    - pyasn1 ==0.1.7

  run:
    - python
    - psycopg2 >=2.5.1,<2.6
    - pytz >=2013d
    - six >=1.4.1
    - django >=1.4.2,<1.8
    - django-select2 >=4.2.2,<4.3
    - django-qmethod ==0.0.3
    - audioread >=1.0.1
    - pyelasticsearch >=0.6,<0.7
    - django-haystack >=2.1,<2.2
    - django-bleach >=0.2.1
    - mysociety-django-popolo >=0.0.2
    - mysociety-django-sluggable >=0.2.6
    - django-subdomain-instances >=0.10.2
    - easy-thumbnails >=2.1
    - unicode-slugify ==0.1.1
    - pyopenssl ==0.14
    - ndg-httpsclient ==0.3.2
    - pyasn1 ==0.1.7

test:
  # Python imports
  imports:
    - speeches
    - speeches.external
    - speeches.importers
    - speeches.management
    - speeches.management.commands
    - speeches.migrations
    - speeches.south_migrations
    - speeches.templatetags
    - speeches.tests
    - speeches.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mysociety/sayit
  license: GNU Affero General Public License v3
  summary: 'A data store for speeches and transcripts to make them searchable and pretty.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
