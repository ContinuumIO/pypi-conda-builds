package:
  name: formalchemy
  version: "1.5.5"

source:
  fn: FormAlchemy-1.5.5.tar.gz
  url: https://pypi.python.org/packages/source/F/FormAlchemy/FormAlchemy-1.5.5.tar.gz
  md5: 474793453f74e950e857b206c36948fc
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - formalchemy = formalchemy:main
    #
    # Would create an entry point called formalchemy that calls formalchemy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sqlalchemy
    - tempita
    - webhelpers2
    - webob
    - markupsafe
    - six

  run:
    - python
    - setuptools
    - sqlalchemy
    - tempita
    - webhelpers2
    - webob
    - markupsafe
    - six

test:
  # Python imports
  imports:
    - formalchemy
    - formalchemy.ext
    - formalchemy.ext.pylons
    - formalchemy.ext.zope

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://docs.formalchemy.org/
  license: MIT License
  summary: 'FormAlchemy greatly speeds development with SQLAlchemy mapped classes (models) in a HTML forms environment.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
