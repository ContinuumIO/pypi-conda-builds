package:
  name: collective.remove.kupu
  version: "0.1"

source:
  fn: collective.remove.kupu-0.1.zip
  url: https://pypi.python.org/packages/source/c/collective.remove.kupu/collective.remove.kupu-0.1.zip
  md5: 643971446db938d04f9b0d22071bbfc8
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.remove.kupu = collective.remove.kupu:main
    #
    # Would create an entry point called collective.remove.kupu that calls collective.remove.kupu.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.remove
    - collective.remove.kupu

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/collective.remove/kupu
  license: GPL
  summary: 'Remove kupu from Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
