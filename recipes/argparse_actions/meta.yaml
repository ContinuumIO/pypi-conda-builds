package:
  name: argparse_actions
  version: "0.4.4"

source:
  fn: argparse_actions-0.4.4.tar.gz
  url: https://pypi.python.org/packages/source/a/argparse_actions/argparse_actions-0.4.4.tar.gz
  md5: 903037c49151f72f2f25f17addeb7f7a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - argparse_actions = argparse_actions:main
    #
    # Would create an entry point called argparse_actions that calls argparse_actions.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - argparse_actions
    - argparse_actions.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/argparse_actions/
  license: BSD License
  summary: 'Custom actions for argparse package'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
