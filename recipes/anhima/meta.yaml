package:
  name: anhima
  version: "0.11.1"

source:
  fn: anhima-0.11.1.tar.gz
  url: https://pypi.python.org/packages/source/a/anhima/anhima-0.11.1.tar.gz
  md5: 80324fd7f4593f3c6a7bbe3530e77eba
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - anhima = anhima:main
    #
    # Would create an entry point called anhima that calls anhima.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - anhima
    - anhima.opt

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/alimanfoo/anhima
  license: MIT License
  summary: 'Exploration and Analysis of genetic variation data.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
