package:
  name: bob.db.caspeal
  version: "2.0.1"

source:
  fn: bob.db.caspeal-2.0.1.zip
  url: https://pypi.python.org/packages/source/b/bob.db.caspeal/bob.db.caspeal-2.0.1.zip
  md5: 67110f92a1ed42f7a2746a8d4cade4a2
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bob.db.caspeal = bob.db.caspeal:main
    #
    # Would create an entry point called bob.db.caspeal that calls bob.db.caspeal.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - bob.db.base
    - bob.db.verification.utils

  run:
    - python
    - setuptools
    - setuptools
    - bob.db.base
    - bob.db.verification.utils

test:
  # Python imports
  imports:
    - bob
    - bob.db
    - bob.db.caspeal

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/bioidiap/bob.db.caspeal
  license: GNU General Public License v3 (GPLv3)
  summary: 'CAS-PEAL Database Access API for Bob'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
