package:
  name: django-compressor-autoprefixer
  version: "0.0.1"

source:
  fn: django-compressor-autoprefixer-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-compressor-autoprefixer/django-compressor-autoprefixer-0.0.1.tar.gz
  md5: 280c5ed0777cd64af0ed840a7e666667
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-compressor-autoprefixer = django-compressor-autoprefixer:main
    #
    # Would create an entry point called django-compressor-autoprefixer that calls django-compressor-autoprefixer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django_compressor

  run:
    - python
    - django_compressor

test:
  # Python imports
  imports:
    - django_compressor_autoprefixer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dizballanze/django-compressor-autoprefixer
  license: MIT
  summary: 'Django Compressor CSS filter for autoprefixer'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
