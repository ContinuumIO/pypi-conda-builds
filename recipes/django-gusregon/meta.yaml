package:
  name: django-gusregon
  version: "0.0.1"

source:
  fn: django-gusregon-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-gusregon/django-gusregon-0.0.1.tar.gz
  md5: db6605f271679e21dfa5c764220c4398
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-gusregon = django-gusregon:main
    #
    # Would create an entry point called django-gusregon that calls django-gusregon.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - gusregon

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bogdal/django-gusregon
  license: BSD License
  summary: 'GUS REGON Internet Database Client'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
