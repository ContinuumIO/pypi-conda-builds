package:
  name: flask-pagedlist
  version: "0.2.1"

source:
  fn: Flask-PagedList-0.2.1.zip
  url: https://pypi.python.org/packages/source/F/Flask-PagedList/Flask-PagedList-0.2.1.zip
  md5: 588ab1387cac33b064f78d1ab1fe5ed4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-pagedlist = flask-pagedlist:main
    #
    # Would create an entry point called flask-pagedlist that calls flask-pagedlist.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Public Domain
  summary: 'Add pypagedlist support for Flask'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
