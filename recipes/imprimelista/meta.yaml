package:
  name: imprimelista
  version: "1.2.1"

source:
  fn: imprimeLista-1.2.1.tar.gz
  url: https://pypi.python.org/packages/source/i/imprimeLista/imprimeLista-1.2.1.tar.gz
  md5: 0c01d8d12ddd2dfb208dc413fd12a712
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - imprimelista = imprimelista:main
    #
    # Would create an entry point called imprimelista that calls imprimelista.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - imprimelista

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: UNKNOWN
  summary: 'Imprime listas simples e aninhadas com recursividade cajo haja aninhamento de listas'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
