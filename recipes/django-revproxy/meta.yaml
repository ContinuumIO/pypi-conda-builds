package:
  name: django-revproxy
  version: "0.9.0"

source:
  fn: django-revproxy-0.9.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-revproxy/django-revproxy-0.9.0.tar.gz
  md5: 20c28fdf5b0a833e54bd2a8fe21f0e7e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-revproxy = django-revproxy:main
    #
    # Would create an entry point called django-revproxy that calls django-revproxy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6
    - urllib3 ==1.10.1

  run:
    - python
    - django >=1.6
    - urllib3 ==1.10.1

test:
  # Python imports
  imports:
    - revproxy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/TracyWebTech/django-revproxy
  license: Mozilla Public License 2.0 (MPL 2.0)
  summary: 'Yet another Django reverse proxy application.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
