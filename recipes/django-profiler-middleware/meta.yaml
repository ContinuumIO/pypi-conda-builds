package:
  name: django-profiler-middleware
  version: "1.0"

source:
  fn: django-profiler-middleware-1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-profiler-middleware/django-profiler-middleware-1.0.tar.gz
  md5: 574f718bd9dcc86fadb2d31899be9209
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-profiler-middleware = django-profiler-middleware:main
    #
    # Would create an entry point called django-profiler-middleware that calls django-profiler-middleware.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_profiler

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mysociety/django-profiler-middleware
  license: BSD License
  summary: 'A profiler middleware for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
