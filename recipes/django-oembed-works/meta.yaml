package:
  name: django-oembed-works
  version: "0.2.2"

source:
  fn: django-oembed-works-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-oembed-works/django-oembed-works-0.2.2.tar.gz
  md5: f463d4e971d74551307305603a456b19
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-oembed-works = django-oembed-works:main
    #
    # Would create an entry point called django-oembed-works that calls django-oembed-works.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - oembed_works
    - oembed_works.management
    - oembed_works.management.commands
    - oembed_works.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.codetrax.org/projects/django-oembed-works
  license: Apache Software License
  summary: 'django-oembed-works is a Django app that provides support for the oEmbed format.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
