package:
  name: arc_library
  version: "1.0.34"

source:
  fn: arc_library-1.0.34.tar.gz
  url: https://pypi.python.org/packages/source/a/arc_library/arc_library-1.0.34.tar.gz
  md5: 6bdde1dd5d1e1ffe31d91b675664829b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - arc_library = arc_library:main
    #
    # Would create an entry point called arc_library that calls arc_library.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - arc_library
    - arclib
    - arclib/django
    - arclib/gae
    - arclib/http

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: iiii.so
  license: UNKNOWN
  summary: 'Arcanelux Python Library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
