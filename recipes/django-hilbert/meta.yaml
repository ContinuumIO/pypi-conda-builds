package:
  name: django-hilbert
  version: "0.4.2"

source:
  fn: django-hilbert-0.4.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-hilbert/django-hilbert-0.4.2.tar.gz
  md5: b2ce62060a94ce06401c2d754e0e2956
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-hilbert = django-hilbert:main
    #
    # Would create an entry point called django-hilbert that calls django-hilbert.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - hilbert
    - hilbert.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mlavin/django-hilbert
  license: BSD License
  summary: u'django-hilbert is one of many Django apps which is a loose collection of utility functions. It is a mixture of Python code and Javascript that I find myself writing over and  over. Primarily it focuses around utilities for AJAX and testing.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
