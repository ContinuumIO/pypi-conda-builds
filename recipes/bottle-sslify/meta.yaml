package:
  name: bottle-sslify
  version: "0.0.1"

source:
  fn: Bottle-SSLify-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/B/Bottle-SSLify/Bottle-SSLify-0.0.1.tar.gz
  md5: c6eef844f7b242cf06a782cfed3d04da
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bottle-sslify = bottle-sslify:main
    #
    # Would create an entry point called bottle-sslify that calls bottle-sslify.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - bottle ==0.10.11
    - nose ==1.2.1

  run:
    - python
    - bottle ==0.10.11
    - nose ==1.2.1

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ali01/bottle-sslify
  license: MIT License
  summary: 'Force SSL on any Bottle app.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
