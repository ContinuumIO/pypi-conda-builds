package:
  name: pygooglechart
  version: "0.4.0"

source:
  fn: pygooglechart-0.4.0.tar.gz
  url: https://pypi.python.org/packages/source/p/pygooglechart/pygooglechart-0.4.0.tar.gz
  md5: 247b69617aa4676ccd3b48fdbbcf2abf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pygooglechart = pygooglechart:main
    #
    # Would create an entry point called pygooglechart that calls pygooglechart.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - pygooglechart

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/gak/pygooglechart
  license: GNU General Public License (GPL)
  summary: 'A complete Python wrapper for the Google Chart API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
