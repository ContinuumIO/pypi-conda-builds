package:
  name: dbaas_dnsapi
  version: "0.1.0"

source:
  fn: dbaas_dnsapi-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/dbaas_dnsapi/dbaas_dnsapi-0.1.0.tar.gz
  md5: 09d5f82dec4261f86ea13c5fb2b88cdc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dbaas_dnsapi = dbaas_dnsapi:main
    #
    # Would create an entry point called dbaas_dnsapi that calls dbaas_dnsapi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - dbaas_dnsapi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/globocom/dbaas-dnsapi
  license: BSD License
  summary: 'Integration between DBAAS and DNSAPI'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
