package:
  name: datasift-beta
  version: "2.0.7"

source:
  fn: datasift-beta-2.0.7.tar.gz
  url: https://pypi.python.org/packages/source/d/datasift-beta/datasift-beta-2.0.7.tar.gz
  md5: 9a34fdb83c05ada361d2a754941036d9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - datasift-beta = datasift-beta:main
    #
    # Would create an entry point called datasift-beta that calls datasift-beta.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests <3.0.0,>=2.2.0
    - autobahn <0.8.0,>=0.7.4
    - six <1.6.0,>=1.5.2
    - twisted <14.0.0,>=13.0.0
    - pyopenssl <0.14.0,>=0.13.1
    - python-dateutil <2.3,>=2.1

  run:
    - python
    - requests <3.0.0,>=2.2.0
    - autobahn <0.8.0,>=0.7.4
    - six <1.6.0,>=1.5.2
    - twisted <14.0.0,>=13.0.0
    - pyopenssl <0.14.0,>=0.13.1
    - python-dateutil <2.3,>=2.1

test:
  # Python imports
  imports:
    - datasift

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/datasift/datasift-python
  license: Copyright (C) 2012-Present by MediaSift Ltd. All Rights Reserved. See LICENSE for the full license.
  summary: 'The official DataSift API library for Python.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
