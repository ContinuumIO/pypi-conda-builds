package:
  name: dcu.active-memory
  version: "0.1.7"

source:
  fn: dcu.active-memory-0.1.7.tar.gz
  url: https://pypi.python.org/packages/source/d/dcu.active-memory/dcu.active-memory-0.1.7.tar.gz
  md5: 5b04094c115805bcf7376525e946fa9b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dcu.active-memory = dcu.active-memory:main
    #
    # Would create an entry point called dcu.active-memory that calls dcu.active-memory.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - boto >=2.8.0
    - filechunkio >=1.5

  run:
    - python
    - boto >=2.8.0
    - filechunkio >=1.5

test:
  # Python imports
  imports:
    - dcu
    - dcu.active_memory

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dirkcuys/active-memory
  license: LICENSE.txt
  summary: 'Script rotate backup files on AWS S3 according to a grandfather, father, sun strategy.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
