package:
  name: django-sphinx-autodoc
  version: "0.2"

source:
  fn: django-sphinx-autodoc-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-sphinx-autodoc/django-sphinx-autodoc-0.2.tar.gz
  md5: ffb8262b860a772423c83e79d0778bca
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-sphinx-autodoc = django-sphinx-autodoc:main
    #
    # Would create an entry point called django-sphinx-autodoc that calls django-sphinx-autodoc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/Fandekasp/django-sphinx-autodoc
  license: BSD License
  summary: 'Autodoc all apps from a project to Sphinx'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
