package:
  name: shake-files
  version: "1.5.1"

source:
  fn: Shake-Files-1.5.1.tar.gz
  url: https://pypi.python.org/packages/source/S/Shake-Files/Shake-Files-1.5.1.tar.gz
  md5: 80ab2e6ee3b6ff07730bfb50f3b6a0a3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - shake-files = shake-files:main
    #
    # Would create an entry point called shake-files that calls shake-files.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - werkzeug >=0.7

  run:
    - python
    - werkzeug >=0.7

test:
  # Python imports
  imports:
    - shake_files

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/lucuma/Shake-Files
  license: MIT License
  summary: 'Allows you to flexibly and efficiently handle file uploads in your Werkzeug-based web application'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
