package:
  name: django-project-template-loader
  version: "0.1.2"

source:
  fn: django-project-template-loader-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-project-template-loader/django-project-template-loader-0.1.2.tar.gz
  md5: 15aec2c160cac6a8f753966ceaa84c9f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-project-template-loader = django-project-template-loader:main
    #
    # Would create an entry point called django-project-template-loader that calls django-project-template-loader.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - project_template_loader
    - project_template_loader.loaders

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/cliff/django-project-template-loader/
  license: MIT License
  summary: 'An app to provide a template loader which pulls templates from [project_dir]/templates'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
