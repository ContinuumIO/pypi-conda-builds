package:
  name: django-mstats
  version: "0.1.2"

source:
  fn: django-mstats-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-mstats/django-mstats-0.1.2.tar.gz
  md5: 7b3d836a6f27ed9c2807b8310a3d11fb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-mstats = django-mstats:main
    #
    # Would create an entry point called django-mstats that calls django-mstats.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5.0

  run:
    - python
    - django >=1.5.0

test:
  # Python imports
  imports:
    - django_mstats

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/heyman/django-mstats
  license: BSD License
  summary: 'Simple, re-usable, stateless Django app for visualizing and browsing statistics, mainly based on your existing Django models.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
