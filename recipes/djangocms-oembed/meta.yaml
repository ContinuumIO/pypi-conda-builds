package:
  name: djangocms-oembed
  version: "0.5"

source:
  fn: djangocms-oembed-0.5.tar.gz
  url: https://pypi.python.org/packages/source/d/djangocms-oembed/djangocms-oembed-0.5.tar.gz
  md5: d1951a9f5391dc9df2e86bc971fb4743
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangocms-oembed = djangocms-oembed:main
    #
    # Would create an entry point called djangocms-oembed that calls djangocms-oembed.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4
    - django-cms
    - pyquery
    - micawber

  run:
    - python
    - django >=1.4
    - django-cms
    - pyquery
    - micawber

test:
  # Python imports
  imports:
    - djangocms_oembed
    - djangocms_oembed.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/divio/djangocms-oembed
  license: BSD License
  summary: 'A set of oembed plugins for django CMS.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
