package:
  name: flask-segmentio
  version: "0.1.0"

source:
  fn: flask-segmentio-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/f/flask-segmentio/flask-segmentio-0.1.0.tar.gz
  md5: 5896d2e04fff064438a9b1d20f5ae685
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-segmentio = flask-segmentio:main
    #
    # Would create an entry point called flask-segmentio that calls flask-segmentio.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - analytics-python

  run:
    - python
    - analytics-python

test:
  # Python imports
  imports:
    - flask_segmentio

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/iromli/flask-segmentio
  license: MIT License
  summary: 'Adds SegmentIO support to your Flask application'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
