package:
  name: interpolate
  version: "0.5.2"

source:
  fn: interpolate-0.5.2.tar.gz
  url: https://pypi.python.org/packages/source/i/interpolate/interpolate-0.5.2.tar.gz
  md5: 39e848097e67d2ee080f150a77ab590a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - interpolate = interpolate:main
    #
    # Would create an entry point called interpolate that calls interpolate.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - interpolate

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://edk141.co.uk/a/interpolate
  license: MIT License
  summary: 'string interpolation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
