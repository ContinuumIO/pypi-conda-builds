package:
  name: django-classbasedsettings
  version: "1.3.0"

source:
  fn: django-classbasedsettings-1.3.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-classbasedsettings/django-classbasedsettings-1.3.0.tar.gz
  md5: 43b5dc45c40c0facb3626540826288d3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-classbasedsettings = django-classbasedsettings:main
    #
    # Would create an entry point called django-classbasedsettings that calls django-classbasedsettings.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.2

  run:
    - python
    - django >=1.2

test:
  # Python imports
  imports:
    - cbsettings
    - cbsettings.switching
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/matthewwithanm/django-classbasedsettings
  license: BSD License
  summary: 'Use classes to define settings.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
