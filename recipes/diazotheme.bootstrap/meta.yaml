package:
  name: diazotheme.bootstrap
  version: "0.3"

source:
  fn: diazotheme.bootstrap-0.3.zip
  url: https://pypi.python.org/packages/source/d/diazotheme.bootstrap/diazotheme.bootstrap-0.3.zip
  md5: 1ea07482365ea8b1422ef89fea2d11a2
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - diazotheme.bootstrap = diazotheme.bootstrap:main
    #
    # Would create an entry point called diazotheme.bootstrap that calls diazotheme.bootstrap.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - diazotheme
    - diazotheme.bootstrap
    - diazotheme.bootstrap.portlet

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kagesenshi/diazotheme.bootstrap
  license: Apache License 2.0
  summary: "Plone theme based on Twitter's Bootstrap CSS"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
