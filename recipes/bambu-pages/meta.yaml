package:
  name: bambu-pages
  version: "2.0.1"

source:
  fn: bambu-pages-2.0.1.tar.gz
  url: https://pypi.python.org/packages/source/b/bambu-pages/bambu-pages-2.0.1.tar.gz
  md5: f59ca72a173ad7ec8e43c918e60e593d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bambu-pages = bambu-pages:main
    #
    # Would create an entry point called bambu-pages that calls bambu-pages.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4
    - bambu-attachments >=2.0
    - bambu-markup >=2.0
    - bambu-formatrules >=2.0

  run:
    - python
    - django >=1.4
    - bambu-attachments >=2.0
    - bambu-markup >=2.0
    - bambu-formatrules >=2.0

test:
  # Python imports
  imports:
    - bambu_pages
    - bambu_pages.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/iamsteadman/bambu-pages
  license: UNKNOWN
  summary: 'Tools for Django webapps'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
