package:
  name: menttes.action.createfolder
  version: "0.1dev-r68295"

source:
  fn: menttes.action.createfolder-0.1dev-r68295.tar.gz
  url: https://pypi.python.org/packages/source/m/menttes.action.createfolder/menttes.action.createfolder-0.1dev-r68295.tar.gz
  md5: 2bf415ad985ebbb47d55fafd38d8b63c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - menttes.action.createfolder = menttes.action.createfolder:main
    #
    # Would create an entry point called menttes.action.createfolder that calls menttes.action.createfolder.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - menttes
    - menttes.action
    - menttes.action.createfolder

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://labs.menttes.com
  license: GPL
  summary: 'An action to create folders with content rules'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
