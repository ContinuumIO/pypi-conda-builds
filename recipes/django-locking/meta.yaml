package:
  name: django-locking
  version: "0.3.0"

source:
  fn: django-locking-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-locking/django-locking-0.3.0.tar.gz
  md5: 3b8d05f92a9fbe77f0593eb666b1438c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-locking = django-locking:main
    #
    # Would create an entry point called django-locking that calls django-locking.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-staticfiles
    - simplejson

  run:
    - python
    - django-staticfiles
    - simplejson

test:
  # Python imports
  imports:
    - locking
    - locking.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://stdbrouw.github.com/django-locking/
  license: BSD License
  summary: 'Prevents users from doing concurrent editing in Django. Works out of the box in the admin interface, or you can integrate it with your own apps using a public API.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
