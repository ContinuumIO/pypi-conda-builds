package:
  name: cloudwatch-robot
  version: "0.0.1"

source:
  fn: cloudwatch-robot-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/cloudwatch-robot/cloudwatch-robot-0.0.1.tar.gz
  md5: 1dddd781f6e53c155a7b10745d0b818d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cloudwatch-robot = cloudwatch-robot:main
    #
    # Would create an entry point called cloudwatch-robot that calls cloudwatch-robot.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - boto ==2.5.2
    - psutil ==0.6.1

  run:
    - python
    - boto ==2.5.2
    - psutil ==0.6.1

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT
  summary: "A script to monitor metrics using Amazon AWS's CloudWatch service."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
