package:
  name: raspberry-pi-lcd
  version: "1.1.1"

source:
  fn: raspberry-pi-lcd-1.1.1.tar.gz
  url: https://pypi.python.org/packages/source/r/raspberry-pi-lcd/raspberry-pi-lcd-1.1.1.tar.gz
  md5: 255355987b96423722d64341aae4f0bf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - raspberry-pi-lcd = raspberry-pi-lcd:main
    #
    # Would create an entry point called raspberry-pi-lcd that calls raspberry-pi-lcd.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - lcd

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/raspberry-pi-lcd/
  license: LICENSE.txt
  summary: 'Easy to use LCD API for the adafruit raspberry pi lcd module..'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
