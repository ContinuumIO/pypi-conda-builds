package:
  name: frasco-sharejs
  version: "0.1.2"

source:
  fn: frasco-sharejs-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/f/frasco-sharejs/frasco-sharejs-0.1.2.tar.gz
  md5: edf7050b81fadd157762423050149fb2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - frasco-sharejs = frasco-sharejs:main
    #
    # Would create an entry point called frasco-sharejs that calls frasco-sharejs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - frasco
    - requests

  run:
    - python
    - frasco
    - requests

test:
  # Python imports
  imports:
    - frasco_sharejs

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/frascoweb/frasco-sharejs
  license: MIT
  summary: 'Sharejs integration into Frasco'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
