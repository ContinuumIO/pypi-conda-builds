package:
  name: django-dropbox-redux
  version: "0.0.5"

source:
  fn: django-dropbox-redux-0.0.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-dropbox-redux/django-dropbox-redux-0.0.5.tar.gz
  md5: a68e12d1be4a437fe1d5900d1bd76543
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-dropbox-redux = django-dropbox-redux:main
    #
    # Would create an entry point called django-dropbox-redux that calls django-dropbox-redux.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - dropbox >=2.0.0
    - six >=1.9.0

  run:
    - python
    - dropbox >=2.0.0
    - six >=1.9.0

test:
  # Python imports
  imports:
    - django_dropbox

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/daikeren/django-dropbox
  license: Django-Drupal-Password-Hasher
  summary: 'A Django App that contains a Django Storage which uses Dropbox with Python3 support.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
