package:
  name: django-dependency
  version: "1.1"

source:
  fn: django-dependency-1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-dependency/django-dependency-1.1.tar.gz
  md5: d81017b33ee3e314de650b9438062814
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-dependency = django-dependency:main
    #
    # Would create an entry point called django-dependency that calls django-dependency.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.1,==dev

  run:
    - python
    - django >=1.1,==dev

test:
  # Python imports
  imports:
    - deps
    - deps.management
    - deps.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/django-dependency/
  license: LICENSE.txt
  summary: 'Django app to help manage external dependencies'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
