package:
  name: django-dublincore
  version: "0.1"

source:
  fn: django-dublincore-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-dublincore/django-dublincore-0.1.tar.gz
  md5: 74d4cb9e61224545c7f97245dd3ddb2d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-dublincore = django-dublincore:main
    #
    # Would create an entry point called django-dublincore that calls django-dublincore.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - dublincore

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mredar/django-dublincore
  license: BSD License
  summary: 'A simple Django app to attach Dublin Core metadata to arbitrary Django objects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
