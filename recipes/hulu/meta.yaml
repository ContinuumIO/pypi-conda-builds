package:
  name: hulu
  version: "0.1.0"

source:
  fn: hulu-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/h/hulu/hulu-0.1.0.tar.gz
  md5: 918a2e4c9ac4eecc17ba1abacfd21ab2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hulu = hulu:main
    #
    # Would create an entry point called hulu that calls hulu.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=1.0.0,<2.0.0
    - requests_oauthlib ==0.3.0

  run:
    - python
    - requests >=1.0.0,<2.0.0
    - requests_oauthlib ==0.3.0

test:
  # Python imports
  imports:
    - hulu

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/michaelhelmick/hulu/tree/master
  license: MIT License
  summary: 'A Python library to interact with Hulu\'s "hidden" 2.0 API.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
