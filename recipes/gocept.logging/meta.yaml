package:
  name: gocept.logging
  version: "0.5"

source:
  fn: gocept.logging-0.5.tar.gz
  url: https://pypi.python.org/packages/source/g/gocept.logging/gocept.logging-0.5.tar.gz
  md5: 01d1ba7c789b0ef01c8913c8eb32f4c4
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gocept.logging = gocept.logging:main
    #
    # Would create an entry point called gocept.logging that calls gocept.logging.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - gocept
    - gocept.logging
    - gocept.logging.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/gocept/gocept.logging/
  license: Zope Public License
  summary: 'Infrastructure for semi-structured log messages.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
