package:
  name: goslate
  version: "1.4.0"

source:
  fn: goslate-1.4.0.tar.gz
  url: https://pypi.python.org/packages/source/g/goslate/goslate-1.4.0.tar.gz
  md5: c018e015dd305b60c7ebb209825313d8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - goslate = goslate:main
    #
    # Would create an entry point called goslate that calls goslate.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - futures

  run:
    - python
    - futures

test:
  # Python imports
  imports:
    - goslate

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://pypi.python.org/pypi/goslate
  license: MIT License
  summary: u'Goslate: Free Google Translate API\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
