package:
  name: clicolors
  version: "0.2"

source:
  fn: clicolors-0.2.tar.gz
  url: https://pypi.python.org/packages/source/c/clicolors/clicolors-0.2.tar.gz
  md5: 6362137046122f1bc8b3f8dc843072ad
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - clicolors = clicolors:main
    #
    # Would create an entry point called clicolors that calls clicolors.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - clicolors

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/gorzk/clicolors.git
  license: MIT License
  summary: 'A simple Python module for styling strings in your Linux terminal'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
