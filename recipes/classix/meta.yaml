package:
  name: classix
  version: "0.5"

source:
  fn: classix-0.5.tar.gz
  url: https://pypi.python.org/packages/source/c/classix/classix-0.5.tar.gz
  md5: 7d5fd66cc165e3e6c4935569d02bf977
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - classix = classix:main
    #
    # Would create an entry point called classix that calls classix.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - lxml ==2.0.6
    - martian >=0.10

  run:
    - python
    - setuptools
    - lxml ==2.0.6
    - martian >=0.10

test:
  # Python imports
  imports:
    - classix

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: ZPL 2.1
  summary: 'Declarative way to associate classes with lxml XML elements.\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
