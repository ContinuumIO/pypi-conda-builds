package:
  name: gypsy
  version: "1.3.0"

source:
  fn: gypsy-1.3.0.tar.gz
  url: https://pypi.python.org/packages/source/g/gypsy/gypsy-1.3.0.tar.gz
  md5: 787a4341eaa4e0a772fa038e2b3fb42b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gypsy = gypsy:main
    #
    # Would create an entry point called gypsy that calls gypsy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - gypsy
    - gypsy.auth
    - gypsy.db
    - gypsy.db.fields
    - gypsy.dbfields
    - gypsy.management
    - gypsy.management.commands
    - gypsy.sessions
    - gypsy.storage
    - gypsy.storage.backends
    - gypsy.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/samuel/gypsy/tree/master
  license: UNKNOWN
  summary: 'Django extensions by Lefora'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
