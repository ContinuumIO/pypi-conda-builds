package:
  name: orbit
  version: "0.2"

source:
  fn: orbit-0.2.tar.gz
  url: https://pypi.python.org/packages/source/o/orbit/orbit-0.2.tar.gz
  md5: 92ace79d79ba3cc7696439d32dc90a7b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - orbit = orbit:main
    #
    # Would create an entry point called orbit that calls orbit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - lxml
    - requests
    - requests_cache
    - pyephem

  run:
    - python
    - lxml
    - requests
    - requests_cache
    - pyephem

test:
  # Python imports
  imports:
    - orbit

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/seanherron/orbit
  license: MIT
  summary: 'Gives current information about orbital objects of interest'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
