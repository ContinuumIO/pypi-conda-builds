package:
  name: js.jquery_form
  version: "3.09"

source:
  fn: js.jquery_form-3.09.zip
  url: https://pypi.python.org/packages/source/j/js.jquery_form/js.jquery_form-3.09.zip
  md5: 54a08ba031ac592ea910a4c3ebb63388
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - js.jquery_form = js.jquery_form:main
    #
    # Would create an entry point called js.jquery_form that calls js.jquery_form.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fanstatic
    - setuptools
    - js.jquery

  run:
    - python
    - setuptools
    - fanstatic
    - setuptools
    - js.jquery

test:
  # Python imports
  imports:
    - js
    - js.jquery_form

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD
  summary: 'Fanstatic packaging of jQuery Form'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
