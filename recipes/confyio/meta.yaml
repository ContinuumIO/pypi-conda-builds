package:
  name: confyio
  version: "1.3.0"

source:
  fn: confyio-1.3.0.tar.gz
  url: https://pypi.python.org/packages/source/c/confyio/confyio-1.3.0.tar.gz
  md5: cb018ab5566fe69a4b48ca6fe3608b74
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - confyio = confyio:main
    #
    # Would create an entry point called confyio that calls confyio.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=2.1.0

  run:
    - python
    - requests >=2.1.0

test:
  # Python imports
  imports:
    - confy
    - confyio

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://confy.io
  license: BSD License
  summary: 'Official Confy API library client for python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
