package:
  name: collatex
  version: "2.0.0pre11"

source:
  fn: collatex-2.0.0pre11.tar.gz
  url: https://pypi.python.org/packages/source/c/collatex/collatex-2.0.0pre11.tar.gz
  md5: dfc1fe60cf6c3a18f3831242f4dcb6cd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collatex = collatex:main
    #
    # Would create an entry point called collatex that calls collatex.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - networkx
    - prettytable

  run:
    - python
    - networkx
    - prettytable

test:
  # Python imports
  imports:
    - ClusterShell
    - collatex

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rhdekker/collatex
  license: GNU General Public License v3 (GPLv3)
  summary: 'CollateX is a collation tool.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
