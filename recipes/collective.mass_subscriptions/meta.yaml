package:
  name: collective.mass_subscriptions
  version: "0.1.0"

source:
  fn: collective.mass_subscriptions-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.mass_subscriptions/collective.mass_subscriptions-0.1.0.tar.gz
  md5: 592fafa06523753ae0239f7b5f4d77e2
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.mass_subscriptions = collective.mass_subscriptions:main
    #
    # Would create an entry point called collective.mass_subscriptions that calls collective.mass_subscriptions.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.mass_subscriptions
    - collective.mass_subscriptions.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/collective.mass_subscriptions
  license: GPL
  summary: 'Give to Plone administrators a form where is possible to subscribe multiple users in a single operation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
