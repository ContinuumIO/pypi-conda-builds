package:
  name: extended_range
  version: "1.0.1"

source:
  fn: extended_range-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/e/extended_range/extended_range-1.0.1.tar.gz
  md5: 491c8f68628c195bfe02bcefb33e20ca
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - extended_range = extended_range:main
    #
    # Would create an entry point called extended_range that calls extended_range.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - extended_range

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Creative Commons Attribution-Noncommercial-Share Alike license
  summary: 'An extended version of the range() BIF that can repeat elements of the list as well'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
