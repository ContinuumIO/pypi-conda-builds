package:
  name: importio
  version: "2.0.2"

source:
  fn: Importio-2.0.2.tar.gz
  url: https://pypi.python.org/packages/source/I/Importio/Importio-2.0.2.tar.gz
  md5: 22dd902e6ee5b4679ece52449a5f8b60
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - importio = importio:main
    #
    # Would create an entry point called importio that calls importio.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - importio
    - importio.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://import.io/data/integrate/#python
  license: UNKNOWN
  summary: 'Access import.io APIs from your Python application'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
