package:
  name: disasters
  version: "0.0.4"

source:
  fn: disasters-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/disasters/disasters-0.0.4.tar.gz
  md5: 5943719d42e487cdf14550b620629c78
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - disasters = disasters:main
    #
    # Would create an entry point called disasters that calls disasters.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - corenlp
    - beautifulsoup

  run:
    - python
    - corenlp
    - beautifulsoup

test:
  # Python imports
  imports:
    - disasters

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kedz/disasters
  license: UNKNOWN
  summary: 'A python library for my research on disaster news.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
