package:
  name: pypgwrap
  version: "0.1.6"

source:
  fn: pypgwrap-0.1.6.zip
  url: https://pypi.python.org/packages/source/p/pypgwrap/pypgwrap-0.1.6.zip
  md5: 832cc553d03b8afaaed62f067379240f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pypgwrap = pypgwrap:main
    #
    # Would create an entry point called pypgwrap that calls pypgwrap.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - psycopg2

  run:
    - python
    - psycopg2

test:
  # Python imports
  imports:
    - pypgwrap

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ephillipe/pypgwrap
  license: BSD
  summary: 'PostgreSQL database wrapper - provides wrapper over psycopg2 supporting a Python API for common sql functions, transaction and pooling'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
