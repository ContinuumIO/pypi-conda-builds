package:
  name: django-chang
  version: "0.1.0"

source:
  fn: django-chang-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-chang/django-chang-0.1.0.tar.gz
  md5: 4c9d4d64a73d50221c6d9917eef22f40
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-chang = django-chang:main
    #
    # Would create an entry point called django-chang that calls django-chang.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - chang

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/NiklasRosenstein/django-chang
  license: MIT
  summary: 'Simple Django utility library including a Jinja2 connection'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
