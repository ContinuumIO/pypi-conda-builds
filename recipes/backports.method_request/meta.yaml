package:
  name: backports.method_request
  version: "1.0"

source:
  fn: backports.method_request-1.0.zip
  url: https://pypi.python.org/packages/source/b/backports.method_request/backports.method_request-1.0.zip
  md5: f68f4c0f45e6b9f8ef0e4534e54448fa
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - backports.method_request = backports.method_request:main
    #
    # Would create an entry point called backports.method_request that calls backports.method_request.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - backports

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/jaraco/backports.method_request
  license: UNKNOWN
  summary: 'backports.method_request'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
