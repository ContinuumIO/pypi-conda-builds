package:
  name: moneywagon
  version: "1.0.8"

source:
  fn: moneywagon-1.0.8.tar.gz
  url: https://pypi.python.org/packages/source/m/moneywagon/moneywagon-1.0.8.tar.gz
  md5: d51a81c63be3c3486fb60f3edc217336
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - moneywagon = moneywagon:main
    #
    # Would create an entry point called moneywagon that calls moneywagon.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests
    - pytz
    - arrow
    - pybitcointools

  run:
    - python
    - requests
    - pytz
    - arrow
    - pybitcointools

test:
  # Python imports
  imports:
    - moneywagon

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/priestc/moneywagon
  license: LICENSE
  summary: 'Toolset for getting the exchange rate for cryptocurrencies'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
