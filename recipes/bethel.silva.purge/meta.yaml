package:
  name: bethel.silva.purge
  version: "1.0b"

source:
  fn: bethel.silva.purge-1.0b.tar.gz
  url: https://pypi.python.org/packages/source/b/bethel.silva.purge/bethel.silva.purge-1.0b.tar.gz
  md5: a548418f35c0db7776bb308cec429d1e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bethel.silva.purge = bethel.silva.purge:main
    #
    # Would create an entry point called bethel.silva.purge that calls bethel.silva.purge.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - five.grok
    - zope.component
    - zope.interface
    - zeam.form.base
    - zeam.form.ztk
    - zeam.form.silva
    - silva.core.conf
    - silva.core.interfaces
    - silva.core.services
    - silva.core.views
    - plone.cachepurging
    - plone.registry
    - zope.annotation

  run:
    - python
    - setuptools
    - setuptools
    - five.grok
    - zope.component
    - zope.interface
    - zeam.form.base
    - zeam.form.ztk
    - zeam.form.silva
    - silva.core.conf
    - silva.core.interfaces
    - silva.core.services
    - silva.core.views
    - plone.cachepurging
    - plone.registry
    - zope.annotation

test:
  # Python imports
  imports:
    - bethel
    - bethel.silva
    - bethel.silva.purge
    - bethel.silva.purge.tests
    - bethel.silva.purge.tests.grok

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/bethel.silva.purge
  license: GPL
  summary: 'Send HTTP PURGE to caching servers on content publishing events'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
