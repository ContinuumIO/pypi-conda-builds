package:
  name: lava-android
  version: "0.1.dev13"

source:
  fn: lava-android-0.1.dev13.tar.gz
  url: https://pypi.python.org/packages/source/l/lava-android/lava-android-0.1.dev13.tar.gz
  md5: b20f486faed86c137f18ae0c1963b5d9
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lava-android = lava-android:main
    #
    # Would create an entry point called lava-android that calls lava-android.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - versiontools >=1.8.2
    - lava-server >=0.8.2
    - lava-dashboard >=0.10.1

  run:
    - python
    - setuptools
    - versiontools >=1.8.2
    - lava-server >=0.8.2
    - lava-dashboard >=0.10.1

test:
  # Python imports
  imports:
    - lava
    - lava.android
    - lava.android.management
    - lava.android.management.commands
    - lava.android.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: GNU Affero General Public License v3
  summary: 'Magic sauce for Android and LAVA'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
