package:
  name: docker-registry-driver-azureblob
  version: "1.1.2"

source:
  fn: docker-registry-driver-azureblob-1.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/docker-registry-driver-azureblob/docker-registry-driver-azureblob-1.1.2.tar.gz
  md5: 384553a38e9cf243aec863983c984a47
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - docker-registry-driver-azureblob = docker-registry-driver-azureblob:main
    #
    # Would create an entry point called docker-registry-driver-azureblob that calls docker-registry-driver-azureblob.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - docker-registry-core >=2,<3
    - azure >=0.8.4

  run:
    - python
    - docker-registry-core >=2,<3
    - azure >=0.8.4

test:
  # Python imports
  imports:
    - docker_registry
    - docker_registry.drivers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ahmetalpbalkan/docker-registry-driver-azure
  license: Apache Software License
  summary: 'Docker registry Azure Blob Storage driver'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
