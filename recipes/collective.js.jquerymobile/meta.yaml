package:
  name: collective.js.jquerymobile
  version: "1.3.1.1"

source:
  fn: collective.js.jquerymobile-1.3.1.1.zip
  url: https://pypi.python.org/packages/source/c/collective.js.jquerymobile/collective.js.jquerymobile-1.3.1.1.zip
  md5: 306913b4aa8ca9ec2371af26d15eff90
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.js.jquerymobile = collective.js.jquerymobile:main
    #
    # Would create an entry point called collective.js.jquerymobile that calls collective.js.jquerymobile.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.jquery

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.jquery

test:
  # Python imports
  imports:
    - collective
    - collective.js
    - collective.js.jquerymobile
    - collective.js.jquerymobile.example

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/toutpt/collective.js.jquerymobile
  license: GNU General Public License (GPL)
  summary: 'JQuery mobile integration in Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
