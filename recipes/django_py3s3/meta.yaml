package:
  name: django_py3s3
  version: "0.1.0"

source:
  fn: django_py3s3-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django_py3s3/django_py3s3-0.1.0.tar.gz
  md5: f965f0d27aedbb1413e1f84819630fab
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_py3s3 = django_py3s3:main
    #
    # Would create an entry point called django_py3s3 that calls django_py3s3.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_py3s3

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/logston/django_py3s3
  license: BSD License
  summary: 'A Django storage backend for saving files to AWS S3.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
