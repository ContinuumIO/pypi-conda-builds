package:
  name: django-zendesk
  version: "0.0.1"

source:
  fn: django-zendesk-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-zendesk/django-zendesk-0.0.1.tar.gz
  md5: de86ac5cb7a6c08e244ca9742cf06ff8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-zendesk = django-zendesk:main
    #
    # Would create an entry point called django-zendesk that calls django-zendesk.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - djzendesk

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/claymation/django-zendesk
  license: BSD License
  summary: 'Django application for handling HTTP target callbacks from Zendesk.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
