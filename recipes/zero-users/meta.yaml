package:
  name: zero-users
  version: "0.1.7"

source:
  fn: zero-users-0.1.7.tar.gz
  url: https://pypi.python.org/packages/source/z/zero-users/zero-users-0.1.7.tar.gz
  md5: ea4b340b123496b6d5709e7e6ae7acf0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zero-users = zero-users:main
    #
    # Would create an entry point called zero-users that calls zero-users.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zero-common >=0.1.2
    - django >=1.3.1
    - south >=0.7.3

  run:
    - python
    - zero-common >=0.1.2
    - django >=1.3.1
    - south >=0.7.3

test:
  # Python imports
  imports:
    - users

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/mandlaweb/Zero-Users
  license: apache license v2.0
  summary: 'Simple app with views and models to manage users and profiles'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
