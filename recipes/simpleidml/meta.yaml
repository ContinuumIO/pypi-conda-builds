package:
  name: simpleidml
  version: "0.92.0"

source:
  fn: SimpleIDML-0.92.0.tar.gz
  url: https://pypi.python.org/packages/source/S/SimpleIDML/SimpleIDML-0.92.0.tar.gz
  md5: e4053f2af5540b1f5517d670bfe3e4e7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - simpleidml = simpleidml:main
    #
    # Would create an entry point called simpleidml that calls simpleidml.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - simple_idml
    - simple_idml.indesign
    - simpleidml

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD License
  summary: 'A library to manipulate Adobe(r) IDML(r) files.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
