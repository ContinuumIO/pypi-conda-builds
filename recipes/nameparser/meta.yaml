package:
  name: nameparser
  version: "0.3.4"

source:
  fn: nameparser-0.3.4.tar.gz
  url: https://pypi.python.org/packages/source/n/nameparser/nameparser-0.3.4.tar.gz
  md5: add129fbdfd72706c28ec6cc300329b6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - nameparser = nameparser:main
    #
    # Would create an entry point called nameparser that calls nameparser.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - nameparser
    - nameparser.config

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/derek73/python-nameparser
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'A simple Python module for parsing human names into their individual components.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
