package:
  name: cmsplugin-plaintext-djangocms3
  version: "0.2.1"

source:
  fn: cmsplugin-plaintext-djangocms3-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/c/cmsplugin-plaintext-djangocms3/cmsplugin-plaintext-djangocms3-0.2.1.tar.gz
  md5: f6121df02da037a759e78d940c9787f0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cmsplugin-plaintext-djangocms3 = cmsplugin-plaintext-djangocms3:main
    #
    # Would create an entry point called cmsplugin-plaintext-djangocms3 that calls cmsplugin-plaintext-djangocms3.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - cmsplugin_plaintext
    - cmsplugin_plaintext.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/changer/cmsplugin-plaintext/
  license: UNKNOWN
  summary: 'Adds a plaintext plugin for django-cms. Forked from https://bitbucket.org/xenofox/cmsplugin-plaintext to add django-cms3 support'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
