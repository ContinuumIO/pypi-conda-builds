package:
  name: apns-client
  version: "0.2.1"

source:
  fn: apns-client-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/a/apns-client/apns-client-0.2.1.tar.gz
  md5: 11b4b21c95b0bfa10ac0edf692aee551
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - apns-client = apns-client:main
    #
    # Would create an entry point called apns-client that calls apns-client.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyopenssl
    - six

  run:
    - python
    - pyopenssl
    - six

test:
  # Python imports
  imports:
    - apnsclient
    - apnsclient.backends

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/sardarnl/apns-client
  license: Apache Software License
  summary: 'Python client for Apple Push Notification service (APNs)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
