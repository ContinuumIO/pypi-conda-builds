package:
  name: collective.permalink
  version: "0.2.0"

source:
  fn: collective.permalink-0.2.0.zip
  url: https://pypi.python.org/packages/source/c/collective.permalink/collective.permalink-0.2.0.zip
  md5: ef8d659b108cb9cb19fca691e089df74
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.permalink = collective.permalink:main
    #
    # Would create an entry point called collective.permalink that calls collective.permalink.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.uuid

  run:
    - python
    - setuptools
    - setuptools
    - plone.uuid

test:
  # Python imports
  imports:
    - collective
    - collective.permalink
    - collective.permalink.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/collective.permalink
  license: GPL
  summary: 'Show a new link (permalink) in Plone contents. This will not change if you move the content itself.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
