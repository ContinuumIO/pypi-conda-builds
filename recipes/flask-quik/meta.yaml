package:
  name: flask-quik
  version: "0.1.1"

source:
  fn: Flask-Quik-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Quik/Flask-Quik-0.1.1.tar.gz
  md5: 93babc89e1b0fc9a44062948fe2c81ac
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-quik = flask-quik:main
    #
    # Would create an entry point called flask-quik that calls flask-quik.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - quik

  run:
    - python
    - flask
    - quik

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/avelino/Flask-Quik
  license: MIT License
  summary: 'Quik for Flask'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
