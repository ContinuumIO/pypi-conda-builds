package:
  name: js.jquery_joyride
  version: "1.0.3-1"

source:
  fn: js.jquery_joyride-1.0.3-1.tar.gz
  url: https://pypi.python.org/packages/source/j/js.jquery_joyride/js.jquery_joyride-1.0.3-1.tar.gz
  md5: f3851508ef4e1553fc4f9d80aba83d76
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - js.jquery_joyride = js.jquery_joyride:main
    #
    # Would create an entry point called js.jquery_joyride that calls js.jquery_joyride.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fanstatic
    - setuptools
    - js.jquery

  run:
    - python
    - setuptools
    - fanstatic
    - setuptools
    - js.jquery

test:
  # Python imports
  imports:
    - js
    - js.jquery_joyride

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD
  summary: 'Fanstatic packaging of joyride'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
