package:
  name: collective.sphinx.includechangelog
  version: "0.1.1"

source:
  fn: collective.sphinx.includechangelog-0.1.1.zip
  url: https://pypi.python.org/packages/source/c/collective.sphinx.includechangelog/collective.sphinx.includechangelog-0.1.1.zip
  md5: dac4961ac60157f799fae0d1105ad9fe
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.sphinx.includechangelog = collective.sphinx.includechangelog:main
    #
    # Would create an entry point called collective.sphinx.includechangelog that calls collective.sphinx.includechangelog.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - docutils
    - sphinx
    - zc.recipe.egg

  run:
    - python
    - docutils
    - sphinx
    - zc.recipe.egg

test:
  # Python imports
  imports:
    - collective
    - collective.sphinx
    - collective.sphinx.includechangelog

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/collective/collective.sphinx.includechangelog
  license: GPL
  summary: 'Include changelog extracted from egg information  into sphinx documentation.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
