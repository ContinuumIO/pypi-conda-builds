package:
  name: durga
  version: "0.2.0.dev2"

source:
  fn: durga-0.2.0.dev2.tar.gz
  url: https://pypi.python.org/packages/source/d/durga/durga-0.2.0.dev2.tar.gz
  md5: 22ea8d8fd8c4db51aa66625799ee938b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - durga = durga:main
    #
    # Would create an entry point called durga that calls durga.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - schema >=0.3.1
    - requests >=2.5.0

  run:
    - python
    - schema >=0.3.1
    - requests >=2.5.0

test:
  # Python imports
  imports:
    - durga

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/transcode-de/durga
  license: BSD License
  summary: 'Create easy to use Python objects for REST resources including schema validation.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
