package:
  name: anyfilter
  version: "0.05"

source:
  fn: AnyFilter-0.05.tar.gz
  url: https://pypi.python.org/packages/source/A/AnyFilter/AnyFilter-0.05.tar.gz
  md5: 7600a89529e4567c3c20fc8c20a978ef
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - anyfilter = anyfilter:main
    #
    # Would create an entry point called anyfilter that calls anyfilter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - anyfilter

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ShawnMilo/anyfilter
  license: BSD
  summary: 'Base class for easy filtering of iterable of dictionaries.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
