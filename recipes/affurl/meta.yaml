package:
  name: affurl
  version: "0.1.7"

source:
  fn: affurl-0.1.7.tar.gz
  url: https://pypi.python.org/packages/source/a/affurl/affurl-0.1.7.tar.gz
  md5: 64bf618d54da5ecacaeb4c7afb8a2ed6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - affurl = affurl:main
    #
    # Would create an entry point called affurl that calls affurl.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - domain_parser

  run:
    - python
    - domain_parser

test:
  # Python imports
  imports:
    - affurl

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/yaph/affurl
  license: MIT License
  summary: 'Turn URLs into affiliate URLs based on provided domain parameter mapping'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
