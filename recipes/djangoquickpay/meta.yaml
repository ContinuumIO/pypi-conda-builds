package:
  name: djangoquickpay
  version: "1.0"

source:
  fn: DjangoQuickpay-1.0.tar.gz
  url: https://pypi.python.org/packages/source/D/DjangoQuickpay/DjangoQuickpay-1.0.tar.gz
  md5: f7063e8a678c4fa2963a5b507cffbba7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangoquickpay = djangoquickpay:main
    #
    # Would create an entry point called djangoquickpay that calls djangoquickpay.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django >=1.3

  run:
    - python
    - django >=1.3

test:
  # Python imports
  imports:
    - djangoquickpay
    - quickpay

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/nsb/django-quickpay
  license: BSD
  summary: 'Django app for danish payment gateway Quickpay'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
