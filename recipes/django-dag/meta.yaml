package:
  name: django-dag
  version: "1.3"

source:
  fn: django-dag-1.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-dag/django-dag-1.3.tar.gz
  md5: 7aefeee30bef0c45ee2187bf76e1d222
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-dag = django-dag:main
    #
    # Would create an entry point called django-dag that calls django-dag.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - django_dag

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/elpaso/django-dag
  license: GNU Affero General Public License v3
  summary: 'Directed Acyclic Graph implementation for Django 1.6+'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
