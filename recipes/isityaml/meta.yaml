package:
  name: isityaml
  version: "0.5"

source:
  fn: isityaml-0.5.tar.gz
  url: https://pypi.python.org/packages/source/i/isityaml/isityaml-0.5.tar.gz
  md5: b58a0a7d25a1b6ccbd265b866d15c1b8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - isityaml = isityaml:main
    #
    # Would create an entry point called isityaml that calls isityaml.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django >=1.1.1
    - mezzanine >=3.0
    - pyyaml >=3.0

  run:
    - python
    - django >=1.1.1
    - mezzanine >=3.0
    - pyyaml >=3.0

test:
  # Python imports
  imports:
    - isityaml

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/isityaml/
  license: BSD License or BSD License
  summary: 'A Django app for checking the correctness of YAML'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
