package:
  name: python-artsy
  version: "1.1"

source:
  fn: python-artsy-1.1.tar.gz
  url: https://pypi.python.org/packages/source/p/python-artsy/python-artsy-1.1.tar.gz
  md5: 600e420a83844292a63b28acb7881638
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-artsy = python-artsy:main
    #
    # Would create an entry point called python-artsy that calls python-artsy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - requests ==2.6.0

  run:
    - python
    - requests ==2.6.0

test:
  # Python imports
  imports:
    - artsy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rodrigosavian/python-artsy
  license: UNKNOWN
  summary: 'Python client for Artsy.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
