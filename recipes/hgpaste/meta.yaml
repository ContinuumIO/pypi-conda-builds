package:
  name: hgpaste
  version: "0.1.1"

source:
  fn: hgpaste-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/h/hgpaste/hgpaste-0.1.1.tar.gz
  md5: 8b699ecae65962b480cd0f3b76686320
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hgpaste = hgpaste:main
    #
    # Would create an entry point called hgpaste that calls hgpaste.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - mercurial

  run:
    - python
    - setuptools
    - mercurial

test:
  # Python imports
  imports:
    - hgpaste

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://k0s.org
  license: GPL
  summary: 'front-end to serve a hg webdir with paste and other'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
