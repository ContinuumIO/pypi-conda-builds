package:
  name: trytond_ldap_authentication
  version: "3.6.0"

source:
  fn: trytond_ldap_authentication-3.6.0.tar.gz
  url: https://pypi.python.org/packages/source/t/trytond_ldap_authentication/trytond_ldap_authentication-3.6.0.tar.gz
  md5: 0077f1f976730334f0a7a67ffe627476
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - trytond_ldap_authentication = trytond_ldap_authentication:main
    #
    # Would create an entry point called trytond_ldap_authentication that calls trytond_ldap_authentication.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python-ldap
    - trytond >=3.6,<3.7

  run:
    - python
    - setuptools
    - python-ldap
    - trytond >=3.6,<3.7

test:
  # Python imports
  imports:
    - trytond.modules.ldap_authentication
    - trytond.modules.ldap_authentication.tests
    - trytond_ldap_authentication

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.tryton.org/
  license: GNU General Public License (GPL)
  summary: 'Tryton module to authenticate users through LDAP'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
