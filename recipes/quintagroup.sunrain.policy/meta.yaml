package:
  name: quintagroup.sunrain.policy
  version: "1.0"

source:
  fn: quintagroup.sunrain.policy-1.0.zip
  url: https://pypi.python.org/packages/source/q/quintagroup.sunrain.policy/quintagroup.sunrain.policy-1.0.zip
  md5: ef652bbb793db0052c7299d280f1b10b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - quintagroup.sunrain.policy = quintagroup.sunrain.policy:main
    #
    # Would create an entry point called quintagroup.sunrain.policy that calls quintagroup.sunrain.policy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - quintagroup
    - quintagroup.sunrain
    - quintagroup.sunrain.policy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://skins.quintagroup.com/sunrain
  license: GNU General Public License (GPL)
  summary: 'extension for SunRain Plone theme'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
