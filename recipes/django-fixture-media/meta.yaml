package:
  name: django-fixture-media
  version: "0.1.4"

source:
  fn: django-fixture-media-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-fixture-media/django-fixture-media-0.1.4.tar.gz
  md5: e475af9459b94c0a337ae9aef14b750e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-fixture-media = django-fixture-media:main
    #
    # Would create an entry point called django-fixture-media that calls django-fixture-media.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - fixture_media
    - fixture_media.management
    - fixture_media.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/leetrout/django-fixturemedia/
  license: MIT License
  summary: 'Simple tool to manage media files for fixtures.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
