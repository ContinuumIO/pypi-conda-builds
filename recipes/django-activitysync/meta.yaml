package:
  name: django-activitysync
  version: "0.2.2"

source:
  fn: django-activitysync-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-activitysync/django-activitysync-0.2.2.tar.gz
  md5: 13ae9d63650d6d0a65d17c927232e4f3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-activitysync = django-activitysync:main
    #
    # Would create an entry point called django-activitysync that calls django-activitysync.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - activitysync
    - activitysync.management
    - activitysync.management.commands
    - activitysync.migrations
    - activitysync.providers
    - activitysync.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/dancarroll/django-activitysync
  license: BSD License
  summary: 'Fast, easy, and extensible social activity aggregation for Django projects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
