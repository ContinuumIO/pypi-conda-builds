package:
  name: zgeo.geographer
  version: "0.3.1"

source:
  fn: zgeo.geographer-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/z/zgeo.geographer/zgeo.geographer-0.3.1.tar.gz
  md5: 1704db502a69d6fe95a0cde8d3a4ddff
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zgeo.geographer = zgeo.geographer:main
    #
    # Would create an entry point called zgeo.geographer that calls zgeo.geographer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - zgeo
    - zgeo.geographer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://trac.gispython.org/projects/PrimaGIS/wiki/zgeo.geographer
  license: BSD License
  summary: 'Geographic annotation for Zope'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
