package:
  name: django-url-robots
  version: "1.1"

source:
  fn: django-url-robots-1.1.zip
  url: https://pypi.python.org/packages/source/d/django-url-robots/django-url-robots-1.1.zip
  md5: db37552d2a56bc5854310caa1d6c2279
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-url-robots = django-url-robots:main
    #
    # Would create an entry point called django-url-robots that calls django-url-robots.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - url_robots
    - url_robots.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/dimka665/django-url-robots
  license: Python Software Foundation License
  summary: 'Django robots.txt generator'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
