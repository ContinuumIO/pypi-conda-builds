package:
  name: flask-featureflags
  version: "0.5.1"

source:
  fn: Flask-FeatureFlags-0.5.1.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-FeatureFlags/Flask-FeatureFlags-0.5.1.tar.gz
  md5: 6dba0e3d7e4b73c59fefaf9f1d412933
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-featureflags = flask-featureflags:main
    #
    # Would create an entry point called flask-featureflags that calls flask-featureflags.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask

  run:
    - python
    - flask

test:
  # Python imports
  imports:
    - flask_featureflags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/trustrachel/Flask-FeatureFlags
  license: Apache Software License
  summary: 'Enable or disable features in Flask apps based on configuration'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
