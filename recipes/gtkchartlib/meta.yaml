package:
  name: gtkchartlib
  version: "0.1.2"

source:
  fn: gtkchartlib-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/g/gtkchartlib/gtkchartlib-0.1.2.tar.gz
  md5: 196bceb0a55a06c6527cbacdf23096c8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gtkchartlib = gtkchartlib:main
    #
    # Would create an entry point called gtkchartlib that calls gtkchartlib.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - gtkchartlib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://gitorious.org/gtkchartlib
  license: GNU General Public License (GPL)
  summary: 'Collection of GTK+ chart widgets'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
