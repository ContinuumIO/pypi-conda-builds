package:
  name: south_jurisdiction
  version: "0.1.2"

source:
  fn: south_jurisdiction-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/s/south_jurisdiction/south_jurisdiction-0.1.2.tar.gz
  md5: 7a22e921e653922afc730e027bbd8cd5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - south_jurisdiction = south_jurisdiction:main
    #
    # Would create an entry point called south_jurisdiction that calls south_jurisdiction.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - south

  run:
    - python
    - south

test:
  # Python imports
  imports:
    - south_jurisdiction
    - south_jurisdiction.management
    - south_jurisdiction.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/alexcouper/django-south-jurisdiction/
  license: Apache Software License
  summary: 'South migrations with database jurisdictions for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
