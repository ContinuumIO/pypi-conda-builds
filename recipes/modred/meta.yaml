package:
  name: modred
  version: "1.0.2"

source:
  fn: modred-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/m/modred/modred-1.0.2.tar.gz
  md5: c91f774b855ef50f026b6e0173e4dc9c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - modred = modred:main
    #
    # Would create an entry point called modred that calls modred.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - modred
    - modred.examples
    - modred.src
    - modred.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Free BSD
  summary: 'Compute modal decompositions and reduced-order models easily, efficiently, and in parallel.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
