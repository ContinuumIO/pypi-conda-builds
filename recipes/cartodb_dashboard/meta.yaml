package:
  name: cartodb_dashboard
  version: "0.2.11"

source:
  fn: cartodb_dashboard-0.2.11.tar.gz
  url: https://pypi.python.org/packages/source/c/cartodb_dashboard/cartodb_dashboard-0.2.11.tar.gz
  md5: 201c4c927f6f8dd193ab887b0ddcc811
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cartodb_dashboard = cartodb_dashboard:main
    #
    # Would create an entry point called cartodb_dashboard that calls cartodb_dashboard.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - simplejson
    - httplib2

  run:
    - python
    - simplejson
    - httplib2

test:
  # Python imports
  imports:
    - cartodb_dashboard

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/realestate-com-au/cartodb-dashboard-python
  license: UNKNOWN
  summary: 'Provides access to the cartodb dashboard'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
