package:
  name: django-user-map
  version: "0.1.2"

source:
  fn: django-user-map-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-user-map/django-user-map-0.1.2.tar.gz
  md5: b207044e341bfdfc3762cfd7aef6fbbf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-user-map = django-user-map:main
    #
    # Would create an entry point called django-user-map that calls django-user-map.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django ==1.7
    - django-leaflet ==0.14.1
    - psycopg2 ==2.5.4
    - factory-boy ==2.4.1
    - django-bootstrap-form ==3.1

  run:
    - python
    - django ==1.7
    - django-leaflet ==0.14.1
    - psycopg2 ==2.5.4
    - factory-boy ==2.4.1
    - django-bootstrap-form ==3.1

test:
  # Python imports
  imports:
    - user_map
    - user_map.forms
    - user_map.migrations
    - user_map.models
    - user_map.tests
    - user_map.utilities

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/django-user-map/
  license: LICENSE.txt
  summary: 'A simple app for creating a community user map in your django web site.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
