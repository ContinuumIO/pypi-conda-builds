package:
  name: flask-rbac
  version: "0.2.0"

source:
  fn: Flask-RBAC-0.2.0.zip
  url: https://pypi.python.org/packages/source/F/Flask-RBAC/Flask-RBAC-0.2.0.zip
  md5: 12cd01e6c0c5dbc98f3342334dd4bc0e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-rbac = flask-rbac:main
    #
    # Would create an entry point called flask-rbac that calls flask-rbac.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.10

  run:
    - python
    - flask >=0.10

test:
  # Python imports
  imports:
    - flask_rbac

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/shonenada/flask-rbac
  license: MIT License
  summary: 'RBAC support for Flask'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
