package:
  name: oggvideopreview
  version: "1.0"

source:
  fn: oggvideopreview-1.0.tar.gz
  url: https://pypi.python.org/packages/source/o/oggvideopreview/oggvideopreview-1.0.tar.gz
  md5: 8ac7a4168cf5728c7502c793e95ff550
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - oggvideopreview = oggvideopreview:main
    #
    # Would create an entry point called oggvideopreview that calls oggvideopreview.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - oggvideopreview

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://fredrik.jemla.eu/oggvideopreview
  license: Public Domain
  summary: 'Get preview images from Theora videos in Ogg files'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
