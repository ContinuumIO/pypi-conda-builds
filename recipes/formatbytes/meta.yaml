package:
  name: formatbytes
  version: "1.0.1"

source:
  fn: formatbytes-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/f/formatbytes/formatbytes-1.0.1.tar.gz
  md5: c5da05bf1d95f79309fa723e0988489b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - formatbytes = formatbytes:main
    #
    # Would create an entry point called formatbytes that calls formatbytes.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - formatbytes

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jongha/python-formatbytes
  license: MIT License
  summary: 'Format Bytes'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
