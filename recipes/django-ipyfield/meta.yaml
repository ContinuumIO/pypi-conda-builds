package:
  name: django-ipyfield
  version: "0.1.6"

source:
  fn: django-ipyfield-0.1.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-ipyfield/django-ipyfield-0.1.6.tar.gz
  md5: ae74ed19c394cac8b95f748e6154f2e8
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-ipyfield = django-ipyfield:main
    #
    # Would create an entry point called django-ipyfield that calls django-ipyfield.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - ipy
    - django

  run:
    - python
    - setuptools
    - ipy
    - django

test:
  # Python imports
  imports:
    - ipyfield

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/onelson/django-ipyfield/
  license: MIT License
  summary: 'IPy.IP instances with BigInt storage for django models'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
