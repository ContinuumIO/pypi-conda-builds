package:
  name: collective.js.bootstrap
  version: "3.3.1.1"

source:
  fn: collective.js.bootstrap-3.3.1.1.zip
  url: https://pypi.python.org/packages/source/c/collective.js.bootstrap/collective.js.bootstrap-3.3.1.1.zip
  md5: 5c37a64df2951ebe81d27bf129a16588
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.js.bootstrap = collective.js.bootstrap:main
    #
    # Would create an entry point called collective.js.bootstrap that calls collective.js.bootstrap.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.jquery >=1.8.3

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.jquery >=1.8.3

test:
  # Python imports
  imports:
    - collective
    - collective.js
    - collective.js.bootstrap

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/toutpt/collective.js.bootstrap
  license: GNU General Public License (GPL)
  summary: 'Get twitter bootstrap as browser resources'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
