package:
  name: fetch_articles
  version: "0.9"

source:
  fn: fetch_articles-0.9.tar.gz
  url: https://pypi.python.org/packages/source/f/fetch_articles/fetch_articles-0.9.tar.gz
  md5: 2494ac8a33d66aca7f6cc9606e998528
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fetch_articles = fetch_articles:main
    #
    # Would create an entry point called fetch_articles that calls fetch_articles.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - fetch_articles
    - fetch_articles.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/luoboiqingcai/calculator
  license: MIT License
  summary: 'Fetch library articles from cnki when the url is provided. '

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
