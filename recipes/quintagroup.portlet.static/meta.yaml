package:
  name: quintagroup.portlet.static
  version: "0.7"

source:
  fn: quintagroup.portlet.static-0.7.zip
  url: https://pypi.python.org/packages/source/q/quintagroup.portlet.static/quintagroup.portlet.static-0.7.zip
  md5: 99558b94d7c06692a08b20fd0ebc8d3b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - quintagroup.portlet.static = quintagroup.portlet.static:main
    #
    # Would create an entry point called quintagroup.portlet.static that calls quintagroup.portlet.static.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - quintagroup
    - quintagroup.portlet
    - quintagroup.portlet.static
    - quintagroup.portlet.static.browser
    - quintagroup.portlet.static.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.quintagroup.com/products/quintagroup.portlet.static
  license: GPL
  summary: 'Static portlet with one extra styling field'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
