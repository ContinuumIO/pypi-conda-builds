package:
  name: cubicweb-trackervcs
  version: "1.3.0"

source:
  fn: cubicweb-trackervcs-1.3.0.tar.gz
  url: https://pypi.python.org/packages/source/c/cubicweb-trackervcs/cubicweb-trackervcs-1.3.0.tar.gz
  md5: 0e74290f27995ba841e5eccc34203f11
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cubicweb-trackervcs = cubicweb-trackervcs:main
    #
    # Would create an entry point called cubicweb-trackervcs that calls cubicweb-trackervcs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cubicweb >=3.19.0
    - cubicweb-tracker >=1.16.0
    - cubicweb-vcsfile >=2.0.0

  run:
    - python
    - cubicweb >=3.19.0
    - cubicweb-tracker >=1.16.0
    - cubicweb-vcsfile >=2.0.0

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.cubicweb.org/project/cubicweb-trackervcs
  license: LGPL
  summary: 'vcsfile / tracker integration'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
