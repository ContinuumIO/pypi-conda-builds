package:
  name: advoptparse
  version: "0.2.13"

source:
  fn: AdvOptParse-0.2.13.tar.gz
  url: https://pypi.python.org/packages/source/A/AdvOptParse/AdvOptParse-0.2.13.tar.gz
  md5: 0877d3981ef7b31cdea49a3ee9383736
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - advoptparse = advoptparse:main
    #
    # Would create an entry point called advoptparse that calls advoptparse.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - advoptparse

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/SpaceKookie/AdvOptParse/
  license: GNU General Public License v2 (GPLv2)
  summary: 'Advanced python commandline argument parser'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
