package:
  name: armstrong.dev
  version: "2.1.0"

source:
  fn: armstrong.dev-2.1.0.tar.gz
  url: https://pypi.python.org/packages/source/a/armstrong.dev/armstrong.dev-2.1.0.tar.gz
  md5: b8c3ce305c5676a609f7e77c086198bc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - armstrong.dev = armstrong.dev:main
    #
    # Would create an entry point called armstrong.dev that calls armstrong.dev.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - decorator <4.0

  run:
    - python
    - decorator <4.0

test:
  # Python imports
  imports:
    - armstrong
    - armstrong.dev
    - armstrong.dev.tests
    - armstrong.dev.tests.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/armstrong/armstrong.dev/
  license: Apache Software License
  summary: u'Tools needed for development and testing of Armstrong'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
