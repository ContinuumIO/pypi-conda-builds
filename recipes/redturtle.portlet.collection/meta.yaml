package:
  name: redturtle.portlet.collection
  version: "0.5"

source:
  fn: redturtle.portlet.collection-0.5.zip
  url: https://pypi.python.org/packages/source/r/redturtle.portlet.collection/redturtle.portlet.collection-0.5.zip
  md5: d6f0ac0bfb274a2864c41a11cc149a47
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - redturtle.portlet.collection = redturtle.portlet.collection:main
    #
    # Would create an entry point called redturtle.portlet.collection that calls redturtle.portlet.collection.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - redturtle
    - redturtle.portlet
    - redturtle.portlet.collection
    - redturtle.portlet.collection.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/redturtle.portlet.collection
  license: GPL
  summary: 'A simple collection portlet for Plone, where you manually choose your skins template id'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
