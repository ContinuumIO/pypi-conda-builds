package:
  name: confset
  version: "0.0.34"

source:
  fn: confset-0.0.34.tar.gz
  url: https://pypi.python.org/packages/source/c/confset/confset-0.0.34.tar.gz
  md5: 2d97787984ab42e7b77c46082f298821
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - confset = confset:main
    #
    # Would create an entry point called confset that calls confset.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - confset

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://computing.dwighthubbard.info
  license: LICENSE.txt
  summary: 'A simple script to change or update package configurations'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
