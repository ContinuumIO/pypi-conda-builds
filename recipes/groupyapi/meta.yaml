package:
  name: groupyapi
  version: "0.6.2"

source:
  fn: GroupyAPI-0.6.2.tar.gz
  url: https://pypi.python.org/packages/source/G/GroupyAPI/GroupyAPI-0.6.2.tar.gz
  md5: 120b8b9654baa515120582b1ec85e927
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - groupyapi = groupyapi:main
    #
    # Would create an entry point called groupyapi that calls groupyapi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - responses ==0.3.0
    - requests ==2.3.0
    - pillow ==2.5.3

  run:
    - python
    - responses ==0.3.0
    - requests ==2.3.0
    - pillow ==2.5.3

test:
  # Python imports
  imports:
    - groupy
    - groupy.api
    - groupy.object
    - groupyapi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rhgrant10/Groupy
  license: UNKNOWN
  summary: 'The simple yet powerful wrapper for the GroupMe API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
