package:
  name: alfred-workflow
  version: "1.11.1"

source:
  fn: Alfred-Workflow-1.11.1.tar.gz
  url: https://pypi.python.org/packages/source/A/Alfred-Workflow/Alfred-Workflow-1.11.1.tar.gz
  md5: a5644a1d36027cf2bab3d27091b35b21
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - alfred-workflow = alfred-workflow:main
    #
    # Would create an entry point called alfred-workflow that calls alfred-workflow.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - workflow

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.deanishe.net/alfred-workflow/
  license: MIT License
  summary: 'Full-featured helper library for writing Alfred 2 workflows'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
