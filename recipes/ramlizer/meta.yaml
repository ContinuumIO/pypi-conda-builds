package:
  name: ramlizer
  version: "0.1.4"

source:
  fn: ramlizer-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/r/ramlizer/ramlizer-0.1.4.tar.gz
  md5: ebe30ff16410383caa6fa61e197aa6a3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ramlizer = ramlizer:main
    #
    # Would create an entry point called ramlizer that calls ramlizer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyaml
    - jsonschema

  run:
    - python
    - pyaml
    - jsonschema

test:
  # Python imports
  imports:
    - ramlizer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/SandyChapman/ramlizer
  license: MIT
  summary: 'A parser for the RAML (RESTful API Modeling Language) file format.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
