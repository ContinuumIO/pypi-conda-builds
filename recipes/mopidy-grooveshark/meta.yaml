package:
  name: mopidy-grooveshark
  version: "1.0.4"

source:
  fn: Mopidy-Grooveshark-1.0.4.tar.gz
  url: https://pypi.python.org/packages/source/M/Mopidy-Grooveshark/Mopidy-Grooveshark-1.0.4.tar.gz
  md5: 4e8d5758a71f495a77b173d2620079b3
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mopidy-grooveshark = mopidy-grooveshark:main
    #
    # Would create an entry point called mopidy-grooveshark that calls mopidy-grooveshark.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - mopidy >=1.0
    - pygrooveshark >=3.1

  run:
    - python
    - setuptools
    - setuptools
    - mopidy >=1.0
    - pygrooveshark >=3.1

test:
  # Python imports
  imports:
    - mopidy_grooveshark

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/camilonova/mopidy-grooveshark
  license: MIT License
  summary: u'Mopidy extension that plays sound from Grooveshark'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
