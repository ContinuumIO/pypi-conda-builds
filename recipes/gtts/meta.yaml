package:
  name: gtts
  version: "1.0.3"

source:
  fn: gTTS-1.0.3.tar.gz
  url: https://pypi.python.org/packages/source/g/gTTS/gTTS-1.0.3.tar.gz
  md5: 2e2fa2fc0b7d28d6b329d915582259db
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gtts = gtts:main
    #
    # Would create an entry point called gtts that calls gtts.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - gtts

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/pndurette/gTTS
  license: MIT License
  summary: 'Create an mp3 file from spoken text via the Google TTS (Text-to-Speech) API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
