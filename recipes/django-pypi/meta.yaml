package:
  name: django-pypi
  version: "0.1"

source:
  fn: django-pypi-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-pypi/django-pypi-0.1.tar.gz
  md5: 1ff40c434e26bb58759c9dc3865401dc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-pypi = django-pypi:main
    #
    # Would create an entry point called django-pypi that calls django-pypi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - django-celery

  run:
    - python
    - django
    - django-celery

test:
  # Python imports
  imports:
    - pypi
    - pypi.management
    - pypi.management.commands
    - pypi.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Celc/django-pypi
  license: The MIT License
  summary: 'VAR_DESCRIPTION\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
