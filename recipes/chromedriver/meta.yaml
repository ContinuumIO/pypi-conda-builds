package:
  name: chromedriver
  version: "2.14.1"

source:
  fn: chromedriver-2.14.1.zip
  url: https://pypi.python.org/packages/source/c/chromedriver/chromedriver-2.14.1.zip
  md5: 3e508b1e6366810642b2790434a4dddb
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - chromedriver = chromedriver:main
    #
    # Would create an entry point called chromedriver that calls chromedriver.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - chromedriver

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: apache2.0 (http://www.apache.org/licenses/LICENSE-2.0)
  summary: 'Tool for downloading chromedriver'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
