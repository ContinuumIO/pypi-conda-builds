package:
  name: sphinxcontrib-whoosh
  version: "0.1"

source:
  fn: sphinxcontrib-whoosh-0.1.tar.gz
  url: https://pypi.python.org/packages/source/s/sphinxcontrib-whoosh/sphinxcontrib-whoosh-0.1.tar.gz
  md5: 88ab3574a9b7bde87a52b92855cb56e1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sphinxcontrib-whoosh = sphinxcontrib-whoosh:main
    #
    # Would create an entry point called sphinxcontrib-whoosh that calls sphinxcontrib-whoosh.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sphinx >=0.6
    - whoosh >=0.3.0b5

  run:
    - python
    - sphinx >=0.6
    - whoosh >=0.3.0b5

test:
  # Python imports
  imports:
    - sphinxcontrib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://packages.python.org/sphinxcontrib-whoosh/
  license: BSD
  summary: 'Whoosh Sphinx extension'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
