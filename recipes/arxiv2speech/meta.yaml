package:
  name: arxiv2speech
  version: "0.0.4"

source:
  fn: arxiv2speech-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/a/arxiv2speech/arxiv2speech-0.0.4.tar.gz
  md5: de4264d6f30af9e21a1b994081b4f7d3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - arxiv2speech = arxiv2speech:main
    #
    # Would create an entry point called arxiv2speech that calls arxiv2speech.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - html2text
    - docopt
    - feedparser

  run:
    - python
    - html2text
    - docopt
    - feedparser

test:
  # Python imports
  imports:
    - arxiv2speech

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dfm/arxiv2speech
  license: BSD License
  summary: 'Convert the current abstracts listed on the arxiv to a set of audio files.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
