package:
  name: django-livefield
  version: "2.1.0"

source:
  fn: django-livefield-2.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-livefield/django-livefield-2.1.0.tar.gz
  md5: 0a8e6e7d4dc72503f0e5d8bb485c3dc5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-livefield = django-livefield:main
    #
    # Would create an entry point called django-livefield that calls django-livefield.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6,<1.8
    - django-model-utils >=2.0,<3.0

  run:
    - python
    - django >=1.6,<1.8
    - django-model-utils >=2.0,<3.0

test:
  # Python imports
  imports:
    - livefield
    - livefield.gis

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/hearsaycorp/django-livefield
  license: MIT
  summary: 'Convenient soft-deletion support for Django models'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
