package:
  name: djangocms-flash
  version: "0.2.0"

source:
  fn: djangocms-flash-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/djangocms-flash/djangocms-flash-0.2.0.tar.gz
  md5: 91d84075258f422084849aab5c7ecb01
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangocms-flash = djangocms-flash:main
    #
    # Would create an entry point called djangocms-flash that calls djangocms-flash.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - djangocms_flash
    - djangocms_flash.migrations
    - djangocms_flash.migrations_django

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/divio/djangocms-flash
  license: BSD License
  summary: 'Flash plugin for django CMS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
