package:
  name: flask_jsontools
  version: "0.1.1-0"

source:
  fn: flask_jsontools-0.1.1-0.tar.gz
  url: https://pypi.python.org/packages/source/f/flask_jsontools/flask_jsontools-0.1.1-0.tar.gz
  md5: 5a424887b4d8653578bbc37c0ba28e85
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask_jsontools = flask_jsontools:main
    #
    # Would create an entry point called flask_jsontools that calls flask_jsontools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.10.1

  run:
    - python
    - flask >=0.10.1

test:
  # Python imports
  imports:
    - flask_jsontools

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kolypto/py-flask-jsontools
  license: BSD
  summary: ' JSON API tools for Flask '

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
