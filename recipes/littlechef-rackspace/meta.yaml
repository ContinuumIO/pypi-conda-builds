package:
  name: littlechef-rackspace
  version: "1.0-pre"

source:
  fn: littlechef-rackspace-1.0-pre.tar.gz
  url: https://pypi.python.org/packages/source/l/littlechef-rackspace/littlechef-rackspace-1.0-pre.tar.gz
  md5: f2bfcdfcf24b39fc6447a885b8cf258a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - littlechef-rackspace = littlechef-rackspace:main
    #
    # Would create an entry point called littlechef-rackspace that calls littlechef-rackspace.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - apache-libcloud ==0.14.1
    - littlechef ==1.6.1
    - pyyaml ==3.10

  run:
    - python
    - apache-libcloud ==0.14.1
    - littlechef ==1.6.1
    - pyyaml ==3.10

test:
  # Python imports
  imports:
    - littlechef_rackspace

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/tildedave/littlechef-rackspace
  license: LittleHTTPServer
  summary: 'Scripts for bootstrapping Rackspace Cloud Servers with Littlechef'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
