package:
  name: collective.mailbox
  version: "1.1.2"

source:
  fn: collective.mailbox-1.1.2.zip
  url: https://pypi.python.org/packages/source/c/collective.mailbox/collective.mailbox-1.1.2.zip
  md5: f562c445dd90c585d18aed97c05b51eb
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.mailbox = collective.mailbox:main
    #
    # Would create an entry point called collective.mailbox that calls collective.mailbox.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.api

  run:
    - python
    - setuptools
    - setuptools
    - plone.api

test:
  # Python imports
  imports:
    - collective
    - collective.mailbox
    - collective.mailbox.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.mailbox
  license: CC-0
  summary: 'A MailHost that stores outgoing mail.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
