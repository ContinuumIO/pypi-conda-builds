package:
  name: cyclone
  version: "1.1"

source:
  fn: cyclone-1.1.tar.gz
  url: https://pypi.python.org/packages/source/c/cyclone/cyclone-1.1.tar.gz
  md5: 477c5ef9cf8902e37105fe450c4d8c5d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cyclone = cyclone:main
    #
    # Would create an entry point called cyclone that calls cyclone.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - twisted
    - pyopenssl

  run:
    - python
    - twisted
    - pyopenssl

test:
  # Python imports
  imports:
    - cyclone
    - twisted.plugins

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://cyclone.io/
  license: http://www.apache.org/licenses/LICENSE-2.0
  summary: "Non-blocking web server. A facebook's Tornado on top of Twisted."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
