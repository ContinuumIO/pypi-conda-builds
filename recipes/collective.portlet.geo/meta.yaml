package:
  name: collective.portlet.geo
  version: "1.0.2"

source:
  fn: collective.portlet.geo-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.portlet.geo/collective.portlet.geo-1.0.2.tar.gz
  md5: a8fa20e782af0ca8f0a6194124b6b7a1
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.portlet.geo = collective.portlet.geo:main
    #
    # Would create an entry point called collective.portlet.geo that calls collective.portlet.geo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.portlet
    - collective.portlet.geo

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/collective.portlet.geo
  license: GPL
  summary: 'Wrap any Plone portlet with geolocation or language negotiation.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
