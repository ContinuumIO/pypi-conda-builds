package:
  name: django-jira-oauth
  version: "0.1.2"

source:
  fn: django-jira-oauth-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-jira-oauth/django-jira-oauth-0.1.2.tar.gz
  md5: 4bc097885f4a0201a4999efe1fddf7a8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-jira-oauth = django-jira-oauth:main
    #
    # Would create an entry point called django-jira-oauth that calls django-jira-oauth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests-oauthlib

  run:
    - python
    - requests-oauthlib

test:
  # Python imports
  imports:
    - jira_oauth

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/MicahCarrick/django-jira-oauth
  license: MIT License
  summary: 'OAuth authentication with JIRA from Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
