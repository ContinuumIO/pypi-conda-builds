package:
  name: django-suit
  version: "0.3a1"

source:
  fn: django-suit-0.3a1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-suit/django-suit-0.3a1.tar.gz
  md5: 1aa2129ae4dfce8f7aa5f6dedc13021e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-suit = django-suit:main
    #
    # Would create an entry point called django-suit that calls django-suit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - suit
    - suit.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://djangosuit.com
  license: UNKNOWN
  summary: 'Modern theme for Django admin interface.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
