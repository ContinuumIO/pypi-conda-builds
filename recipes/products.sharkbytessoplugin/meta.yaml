package:
  name: products.sharkbytessoplugin
  version: "0.6.0b1"

source:
  fn: Products.SharkbyteSSOPlugin-0.6.0b1.tar.gz
  url: https://pypi.python.org/packages/source/P/Products.SharkbyteSSOPlugin/Products.SharkbyteSSOPlugin-0.6.0b1.tar.gz
  md5: a46dcd913fd4f309f34216d3545d95eb
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - products.sharkbytessoplugin = products.sharkbytessoplugin:main
    #
    # Would create an entry point called products.sharkbytessoplugin that calls products.sharkbytessoplugin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - Products
    - Products.SharkbyteSSOPlugin

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/single-sign-on-plugin
  license: GPL
  summary: 'SSO Plugin for Zope 2 PAS and PlonePAS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
