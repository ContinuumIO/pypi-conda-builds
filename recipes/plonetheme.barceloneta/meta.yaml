package:
  name: plonetheme.barceloneta
  version: "1.6.5"

source:
  fn: plonetheme.barceloneta-1.6.5.tar.gz
  url: https://pypi.python.org/packages/source/p/plonetheme.barceloneta/plonetheme.barceloneta-1.6.5.tar.gz
  md5: 05de860f0b014aa7ede9edd36432e67c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plonetheme.barceloneta = plonetheme.barceloneta:main
    #
    # Would create an entry point called plonetheme.barceloneta that calls plonetheme.barceloneta.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.theming

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.theming

test:
  # Python imports
  imports:
    - plonetheme
    - plonetheme.barceloneta

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/plonetheme.barceloneta
  license: GNU General Public License (GPL)
  summary: 'The default theme for Plone 5.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
