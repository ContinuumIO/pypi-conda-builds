package:
  name: collective.contextimageexamples
  version: "0.1"

source:
  fn: collective.contextimageexamples-0.1.zip
  url: https://pypi.python.org/packages/source/c/collective.contextimageexamples/collective.contextimageexamples-0.1.zip
  md5: d5c3847d54cc642b597591dd2542a7ca
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.contextimageexamples = collective.contextimageexamples:main
    #
    # Would create an entry point called collective.contextimageexamples that calls collective.contextimageexamples.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.contextimage

  run:
    - python
    - setuptools
    - setuptools
    - collective.contextimage

test:
  # Python imports
  imports:
    - collective
    - collective.contextimageexamples

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/collective.contextimageexamples
  license: GNU General Public License (GPL)
  summary: 'Some examples for context.contextimage'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
