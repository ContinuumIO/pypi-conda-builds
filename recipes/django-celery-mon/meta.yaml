package:
  name: django-celery-mon
  version: "1.0"

source:
  fn: django-celery-mon-1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-celery-mon/django-celery-mon-1.0.tar.gz
  md5: f78d0a888804672e1a6565cc0f6ebd06
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-celery-mon = django-celery-mon:main
    #
    # Would create an entry point called django-celery-mon that calls django-celery-mon.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - celerymon
    - celerymon.management
    - celerymon.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/gotlium/django-celery-mon/
  license: GNU General Public License (GPL)
  summary: 'App for monitoring Celery workers'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
