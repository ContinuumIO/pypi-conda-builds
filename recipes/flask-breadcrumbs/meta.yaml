package:
  name: flask-breadcrumbs
  version: "0.3.0"

source:
  fn: Flask-Breadcrumbs-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Breadcrumbs/Flask-Breadcrumbs-0.3.0.tar.gz
  md5: f18d6bb9cbf97e16fc0cb25b0f569f44
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-breadcrumbs = flask-breadcrumbs:main
    #
    # Would create an entry point called flask-breadcrumbs that calls flask-breadcrumbs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - six
    - flask-menu >=0.2

  run:
    - python
    - flask
    - six
    - flask-menu >=0.2

test:
  # Python imports
  imports:
    - flask_breadcrumbs

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/inveniosoftware/flask-breadcrumbs/
  license: BSD License
  summary: 'Flask-Breadcrumbs is a Flask extension that adds support for generating site breadcrumb navigation.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
