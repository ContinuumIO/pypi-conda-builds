package:
  name: django-ticketing
  version: "0.7.3"

source:
  fn: django-ticketing-0.7.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-ticketing/django-ticketing-0.7.3.tar.gz
  md5: d718f9d3352cb6db2eaad9bbc455fca2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-ticketing = django-ticketing:main
    #
    # Would create an entry point called django-ticketing that calls django-ticketing.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - ticketing

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/streeter/django-ticketing
  license: MIT License
  summary: 'Generate tickets efficiently in a database in Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
