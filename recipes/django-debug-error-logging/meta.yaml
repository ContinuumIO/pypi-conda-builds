package:
  name: django-debug-error-logging
  version: "1.0.0"

source:
  fn: django-debug-error-logging-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-debug-error-logging/django-debug-error-logging-1.0.0.tar.gz
  md5: 26c8ec12083876dd23cc02194596a3c1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-debug-error-logging = django-debug-error-logging:main
    #
    # Would create an entry point called django-debug-error-logging that calls django-debug-error-logging.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - debug_error_logging

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/brightinteractive/django-debug-error-logging/
  license: BSD
  summary: 'Enable error logging in Django when DEBUG = True'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
