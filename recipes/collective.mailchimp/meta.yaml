package:
  name: collective.mailchimp
  version: "1.4.1"

source:
  fn: collective.mailchimp-1.4.1.zip
  url: https://pypi.python.org/packages/source/c/collective.mailchimp/collective.mailchimp-1.4.1.zip
  md5: a3555b9b61b6d99a54f05d7f6ddaaf9a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.mailchimp = collective.mailchimp:main
    #
    # Would create an entry point called collective.mailchimp that calls collective.mailchimp.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.cmfplone
    - plone.app.portlets
    - plone.app.registry
    - postmonkey

  run:
    - python
    - setuptools
    - setuptools
    - products.cmfplone
    - plone.app.portlets
    - plone.app.registry
    - postmonkey

test:
  # Python imports
  imports:
    - collective
    - collective.mailchimp
    - collective.mailchimp.browser
    - collective.mailchimp.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/collective/collective.mailchimp
  license: GPL
  summary: 'MailChimp integration for Plone.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
