package:
  name: languagedet
  version: "0.1.1"

source:
  fn: languagedet-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/l/languagedet/languagedet-0.1.1.tar.gz
  md5: d32cc3634980f05ca3b9326e75c43004
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - languagedet = languagedet:main
    #
    # Would create an entry point called languagedet that calls languagedet.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - languagedet

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: GNU Lesser General Public License v3 (LGPLv3)
  summary: 'Python package for language detection.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
