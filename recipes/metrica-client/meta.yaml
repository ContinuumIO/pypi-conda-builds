package:
  name: metrica-client
  version: "0.1.6"

source:
  fn: metrica-client-0.1.6.tar.gz
  url: https://pypi.python.org/packages/source/m/metrica-client/metrica-client-0.1.6.tar.gz
  md5: 0397561301e425af69352b7201000c61
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - metrica-client = metrica-client:main
    #
    # Would create an entry point called metrica-client that calls metrica-client.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pycrypto ==2.6
    - requests ==0.14.2

  run:
    - python
    - pycrypto ==2.6
    - requests ==0.14.2

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://getmetrica.com
  license: MIT
  summary: 'Client to connect your database to the Metrica MongoDB Analytics service'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
