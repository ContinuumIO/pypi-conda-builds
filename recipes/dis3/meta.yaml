package:
  name: dis3
  version: "0.1.0"

source:
  fn: dis3-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/dis3/dis3-0.1.0.tar.gz
  md5: fb68c81c28ba54816e5a6307436451ce
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dis3 = dis3:main
    #
    # Would create an entry point called dis3 that calls dis3.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - dis3

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tomxtobin/dis3
  license: MIT License or Python Software Foundation License
  summary: 'Python 2.7 backport of the "dis" module from Python 3.5+'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
