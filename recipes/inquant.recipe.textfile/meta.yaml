package:
  name: inquant.recipe.textfile
  version: "0.2.3"

source:
  fn: inquant.recipe.textfile-0.2.3.tar.gz
  url: https://pypi.python.org/packages/source/i/inquant.recipe.textfile/inquant.recipe.textfile-0.2.3.tar.gz
  md5: 5677427394b4d39ab360bc4807f47283
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - inquant.recipe.textfile = inquant.recipe.textfile:main
    #
    # Would create an entry point called inquant.recipe.textfile that calls inquant.recipe.textfile.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zc.buildout
    - setuptools

  run:
    - python
    - setuptools
    - zc.buildout
    - setuptools

test:
  # Python imports
  imports:
    - inquant
    - inquant.recipe
    - inquant.recipe.textfile

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://svn.inquant.de/inquant/develop/plone/recipes/inquant.recipe.textfile
  license: LGPL
  summary: 'ZC Buildout recipe for creating text files out of templates.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
