package:
  name: archetypes.querywidget
  version: "1.1.2"

source:
  fn: archetypes.querywidget-1.1.2.zip
  url: https://pypi.python.org/packages/source/a/archetypes.querywidget/archetypes.querywidget-1.1.2.zip
  md5: 6be2394337b82313b0c20e0b56138882
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - archetypes.querywidget = archetypes.querywidget:main
    #
    # Would create an entry point called archetypes.querywidget that calls archetypes.querywidget.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.querystring >=1.2.2
    - plone.app.jquery >=1.7.2
    - plone.app.jquerytools

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.querystring >=1.2.2
    - plone.app.jquery >=1.7.2
    - plone.app.jquerytools

test:
  # Python imports
  imports:
    - archetypes
    - archetypes.querywidget
    - archetypes.querywidget.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/archetypes.querywidget
  license: GNU General Public License (GPL)
  summary: 'Archetypes.querywidget implements a widget for creating catalog queries using an email-filtering-like interface, as found in GMail or Apple Mail.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
