package:
  name: django-gizmo
  version: "0.0.4"

source:
  fn: django-gizmo-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-gizmo/django-gizmo-0.0.4.tar.gz
  md5: 59bc96b54c9ac0e10197564f4a44f231
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-gizmo = django-gizmo:main
    #
    # Would create an entry point called django-gizmo that calls django-gizmo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - gizmo
    - gizmo.templatetags
    - gizmo.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/praekelt/django-gizmo
  license: BSD License
  summary: 'Django app allowing for configurable targetting of template inclusion tags.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
