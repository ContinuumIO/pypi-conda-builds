package:
  name: gitcommit
  version: "0.9.9"

source:
  fn: GitCommit-0.9.9.tar.gz
  url: https://pypi.python.org/packages/source/G/GitCommit/GitCommit-0.9.9.tar.gz
  md5: 13ff3bc061b99c879c22aba1ac50b8e0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gitcommit = gitcommit:main
    #
    # Would create an entry point called gitcommit that calls gitcommit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - gitcommit

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'GitCommit is a free Python based script that does a few checks before running "git commit -av". Checks include running django unit tests and checking for TODO statements. To contribute, visit http://github.com/defcube/gitcommit/'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
