package:
  name: mext.threads
  version: "0.6.r2832"

source:
  fn: mext.threads-0.6.r2832.zip
  url: https://pypi.python.org/packages/source/m/mext.threads/mext.threads-0.6.r2832.zip
  md5: c71972744fb31aa42b9c7e56dbfa92a8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mext.threads = mext.threads:main
    #
    # Would create an entry point called mext.threads that calls mext.threads.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - mext.hacks >=0.10
    - mext.log

  run:
    - python
    - mext.hacks >=0.10
    - mext.log

test:
  # Python imports
  imports:
    - mext
    - mext.threads
    - mext.threads.generic

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.maluke.com/python
  license: MIT
  summary: 'mext.threads'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
