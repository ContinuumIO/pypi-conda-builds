package:
  name: cubicweb-testcard
  version: "0.5.0"

source:
  fn: cubicweb-testcard-0.5.0.tar.gz
  url: https://pypi.python.org/packages/source/c/cubicweb-testcard/cubicweb-testcard-0.5.0.tar.gz
  md5: d7b84d82642ff3dabe422b45c83d1605
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cubicweb-testcard = cubicweb-testcard:main
    #
    # Would create an entry point called cubicweb-testcard that calls cubicweb-testcard.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cubicweb >=3.19.0
    - cubicweb-tracker >=1.8.0
    - cubicweb-card

  run:
    - python
    - cubicweb >=3.19.0
    - cubicweb-tracker >=1.8.0
    - cubicweb-card

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.cubicweb.org/project/cubicweb-testcard
  license: LGPL
  summary: 'test card extension for CubicWeb application based on the tracker cube'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
