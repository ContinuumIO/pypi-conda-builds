package:
  name: jankflix
  version: "1.1.2"

source:
  fn: jankflix-1.1.2.tar.gz
  url: https://pypi.python.org/packages/source/j/jankflix/jankflix-1.1.2.tar.gz
  md5: 2dc331c27c2801be562f0d927f824bdb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jankflix = jankflix:main
    #
    # Would create an entry point called jankflix that calls jankflix.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - jankflix
    - jankflixmodules

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/fimbrethil/jankflix-python
  license: UNKNOWN
  summary: 'Jankflix: A jankier way to watch things'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
