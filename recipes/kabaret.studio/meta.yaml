package:
  name: kabaret.studio
  version: "0.1.0b3"

source:
  fn: kabaret.studio-0.1.0b3.zip
  url: https://pypi.python.org/packages/source/k/kabaret.studio/kabaret.studio-0.1.0b3.zip
  md5: 27da188f167f0e3056255a4ef5c32776
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kabaret.studio = kabaret.studio:main
    #
    # Would create an entry point called kabaret.studio that calls kabaret.studio.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - kabaret.core
    - kabaret.flow
    - kabaret.naming
    - kabaret.gui

  run:
    - python
    - kabaret.core
    - kabaret.flow
    - kabaret.naming
    - kabaret.gui

test:
  # Python imports
  imports:
    - kabaret
    - kabaret.studio
    - kabaret.studio.apps
    - kabaret.studio.clients
    - kabaret.studio.clients.flow_gui
    - kabaret.studio.clients.flow_gui.icons
    - kabaret.studio.clients.flow_gui.icons.nodes
    - kabaret.studio.clients.maya
    - kabaret.studio.clients.naming_gui
    - kabaret.studio.clients.nuke
    - kabaret.studio.clients.system_cmds_gui
    - kabaret.studio.clients.system_cmds_gui.icons
    - kabaret.studio.clients.versions_gui
    - kabaret.studio.clients.versions_gui.icons
    - kabaret.studio.flow
    - kabaret.studio.flow.nodelib
    - kabaret.studio.flow.projects
    - kabaret.studio.naming
    - kabaret.studio.naming.examples
    - kabaret.studio.reversion

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://packages.python.org/kabaret
  license: GNU Lesser General Public License v3 or later (LGPLv3+)
  summary: 'Complete implementation of a digital creation studio using the kabaret framework.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
