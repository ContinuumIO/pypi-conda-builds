package:
  name: klaviyo
  version: "1.0.0"

source:
  fn: klaviyo-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/k/klaviyo/klaviyo-1.0.0.tar.gz
  md5: be9bc2f4abdb21418f63c219048000f8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - klaviyo = klaviyo:main
    #
    # Would create an entry point called klaviyo that calls klaviyo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=2.2.1

  run:
    - python
    - requests >=2.2.1

test:
  # Python imports
  imports:
    - klaviyo

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/klaviyo/python-klaviyo
  license: MIT License
  summary: 'Python API for Klaviyo'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
