package:
  name: gender-detector
  version: "0.0.6"

source:
  fn: gender-detector-0.0.6.tar.gz
  url: https://pypi.python.org/packages/source/g/gender-detector/gender-detector-0.0.6.tar.gz
  md5: 01ed4019b42e2323ee34b3635d985540
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gender-detector = gender-detector:main
    #
    # Would create an entry point called gender-detector that calls gender-detector.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - gender_detector
    - gender_detector.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://codingnews.org
  license: GPL2
  summary: 'Gender detection library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
