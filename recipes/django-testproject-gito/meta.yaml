package:
  name: django-testproject-gito
  version: "0.0.1"

source:
  fn: django-testproject-gito-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-testproject-gito/django-testproject-gito-0.0.1.tar.gz
  md5: dfc730c926f10e0bd8f4ef7703132352
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-testproject-gito = django-testproject-gito:main
    #
    # Would create an entry point called django-testproject-gito that calls django-testproject-gito.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django >=1.2

  run:
    - python
    - django >=1.2

test:
  # Python imports
  imports:
    - django-testproject
    - django-testproject.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://vishwassharma.com/packages/django-testproject.tar.gz
  license: LICENSE.txt
  summary: 'Django project for making life easy'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
