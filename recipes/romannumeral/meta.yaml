package:
  name: romannumeral
  version: "0.2.1"

source:
  fn: romannumeral-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/r/romannumeral/romannumeral-0.2.1.tar.gz
  md5: 0ced00e3b2f934679dfe8eb53414656b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - romannumeral = romannumeral:main
    #
    # Would create an entry point called romannumeral that calls romannumeral.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - romannumeral

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jay3686/romannumeral
  license: BSD License
  summary: 'This package has a class for working with Roman Numerals.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
