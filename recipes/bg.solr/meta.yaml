package:
  name: bg.solr
  version: "0.2"

source:
  fn: bg.solr-0.2.zip
  url: https://pypi.python.org/packages/source/b/bg.solr/bg.solr-0.2.zip
  md5: 70684f80048f9aec38ba7bab57baccb2
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bg.solr = bg.solr:main
    #
    # Would create an entry point called bg.solr that calls bg.solr.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - sunburnt
    - httplib2
    - lxml

  run:
    - python
    - setuptools
    - setuptools
    - sunburnt
    - httplib2
    - lxml

test:
  # Python imports
  imports:
    - bg
    - bg.solr
    - bg.solr.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/
  license: GPL
  summary: 'bg.solr'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
