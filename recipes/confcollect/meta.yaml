package:
  name: confcollect
  version: "0.2.2"

source:
  fn: confcollect-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/c/confcollect/confcollect-0.2.2.tar.gz
  md5: 07291521fb860886681cb54f6f40188d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - confcollect = confcollect:main
    #
    # Would create an entry point called confcollect that calls confcollect.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - confcollect

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/miracle2k/confcollect
  license: BSD License
  summary: 'Helpers to collect configuration from os environment, files etc.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
