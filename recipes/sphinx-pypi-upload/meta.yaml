package:
  name: sphinx-pypi-upload
  version: "0.2.1"

source:
  fn: Sphinx-PyPI-upload-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/S/Sphinx-PyPI-upload/Sphinx-PyPI-upload-0.2.1.tar.gz
  md5: b9f1df5c8443197e4d49abbba1cfddc4
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sphinx-pypi-upload = sphinx-pypi-upload:main
    #
    # Would create an entry point called sphinx-pypi-upload that calls sphinx-pypi-upload.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/jezdez/sphinx-pypi-upload/
  license: BSD License
  summary: 'setuptools command for uploading Sphinx documentation to PyPI'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
