package:
  name: iostick
  version: "2.0"

source:
  fn: ioStick-2.0.tar.gz
  url: https://pypi.python.org/packages/source/i/ioStick/ioStick-2.0.tar.gz
  md5: 8781a2f6e60f2f6a448cbb3aa9572e08
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - iostick = iostick:main
    #
    # Would create an entry point called iostick that calls iostick.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyserial

  run:
    - python
    - pyserial

test:
  # Python imports
  imports:
    - ioStick
    - iostick

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://wlinux.mobi/products/ioStick
  license: UNKNOWN
  summary: 'Python wrapper for webLinux IoStick'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
