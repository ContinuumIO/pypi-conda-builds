package:
  name: django-entity-event
  version: "0.4.2"

source:
  fn: django-entity-event-0.4.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-entity-event/django-entity-event-0.4.2.tar.gz
  md5: d690393000aea3a91a07676a8568525d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-entity-event = django-entity-event:main
    #
    # Would create an entry point called django-entity-event that calls django-entity-event.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cached-property >=0.1.5
    - django >=1.7
    - django-entity >=1.10.1
    - jsonfield >=0.9.20
    - six

  run:
    - python
    - cached-property >=0.1.5
    - django >=1.7
    - django-entity >=1.10.1
    - jsonfield >=0.9.20
    - six

test:
  # Python imports
  imports:
    - entity_event
    - entity_event.migrations
    - entity_event.tests
    - entity_event.tests.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ambitioninc/django-entity-event
  license: MIT License
  summary: 'Newsfeed-style event tracking and subscription management for django-entity.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
