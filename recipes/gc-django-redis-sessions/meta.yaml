package:
  name: gc-django-redis-sessions
  version: "0.4.0"

source:
  fn: gc-django-redis-sessions-0.4.0.tar.gz
  url: https://pypi.python.org/packages/source/g/gc-django-redis-sessions/gc-django-redis-sessions-0.4.0.tar.gz
  md5: 9279fa692199267913fe9b8bf53b385f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gc-django-redis-sessions = gc-django-redis-sessions:main
    #
    # Would create an entry point called gc-django-redis-sessions that calls gc-django-redis-sessions.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - redis >=2.4.10

  run:
    - python
    - redis >=2.4.10

test:
  # Python imports
  imports:
    - redis_sessions

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/martinrusev/django-redis-sessions
  license: BSD
  summary: 'Redis Session Backend For Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
