package:
  name: flask-xuacompatible
  version: "0.1.0"

source:
  fn: flask-xuacompatible-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/f/flask-xuacompatible/flask-xuacompatible-0.1.0.tar.gz
  md5: 44aa9a4ce146a9160db0f134e5672d3a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-xuacompatible = flask-xuacompatible:main
    #
    # Would create an entry point called flask-xuacompatible that calls flask-xuacompatible.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask

  run:
    - python
    - flask

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jpvanhal/flask-xuacompatible
  license: BSD License
  summary: 'Sets X-UA-Compatible HTTP header in your Flask application.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
