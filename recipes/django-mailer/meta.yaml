package:
  name: django-mailer
  version: "0.1.0"

source:
  fn: django-mailer-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-mailer/django-mailer-0.1.0.tar.gz
  md5: b8bea74a7a0e6b0ebab68089e68df62c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-mailer = django-mailer:main
    #
    # Would create an entry point called django-mailer that calls django-mailer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - mailer
    - mailer.management
    - mailer.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/django-mailer/
  license: MIT License
  summary: 'A reusable Django app for queuing the sending of email'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
