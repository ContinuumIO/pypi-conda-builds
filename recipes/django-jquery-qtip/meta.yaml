package:
  name: django-jquery-qtip
  version: "2.1.1"

source:
  fn: django-jquery-qtip-2.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-jquery-qtip/django-jquery-qtip-2.1.1.tar.gz
  md5: 1d22d85199d576ebb4972ea028df4e2c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-jquery-qtip = django-jquery-qtip:main
    #
    # Would create an entry point called django-jquery-qtip that calls django-jquery-qtip.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-jquery >=1.6

  run:
    - python
    - django-jquery >=1.6

test:
  # Python imports
  imports:
    - jquery_qtip

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/benbacardi/django-jquery-qtip
  license: MIT License
  summary: 'jQuery qTip (http://qtip2.com) packaged in a django app to speed up new applications and deployment.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
