package:
  name: django-smart-load-tag
  version: "0.3.2"

source:
  fn: django-smart-load-tag-0.3.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-smart-load-tag/django-smart-load-tag-0.3.2.tar.gz
  md5: 9c810576afc3bc371dfe66101f4fdcad
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-smart-load-tag = django-smart-load-tag:main
    #
    # Would create an entry point called django-smart-load-tag that calls django-smart-load-tag.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - smart_load_tag
    - smart_load_tag.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/codysoyland/django-smart-load-tag
  license: BSD License
  summary: "An attempt to bring namespaces and more control to Django's {% load %} tag."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
