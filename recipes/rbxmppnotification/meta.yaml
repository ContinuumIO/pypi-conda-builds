package:
  name: rbxmppnotification
  version: "0.5.0"

source:
  fn: RBXmppNotification-0.5.0.tar.gz
  url: https://pypi.python.org/packages/source/R/RBXmppNotification/RBXmppNotification-0.5.0.tar.gz
  md5: 37fbac090a2d98839a7660e25f88c426
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - rbxmppnotification = rbxmppnotification:main
    #
    # Would create an entry point called rbxmppnotification that calls rbxmppnotification.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyxmpp2 >=2.0alpha2
    - argparse >=1.2

  run:
    - python
    - setuptools
    - pyxmpp2 >=2.0alpha2
    - argparse >=1.2

test:
  # Python imports
  imports:
    - rbxmppnotification

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/iconoeugen/rb-xmpp-notification
  license: MIT
  summary: 'ReviewBoard XMPP Notification Extension'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
