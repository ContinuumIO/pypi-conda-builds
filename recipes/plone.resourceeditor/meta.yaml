package:
  name: plone.resourceeditor
  version: "2.0.0"

source:
  fn: plone.resourceeditor-2.0.0.zip
  url: https://pypi.python.org/packages/source/p/plone.resourceeditor/plone.resourceeditor-2.0.0.zip
  md5: c5431a7a02889601b2502312025fb9c2
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.resourceeditor = plone.resourceeditor:main
    #
    # Would create an entry point called plone.resourceeditor that calls plone.resourceeditor.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zope.interface
    - zope.component
    - zope.publisher
    - zope.schema
    - plone.resource
    - zope2

  run:
    - python
    - setuptools
    - setuptools
    - zope.interface
    - zope.component
    - zope.publisher
    - zope.schema
    - plone.resource
    - zope2

test:
  # Python imports
  imports:
    - plone
    - plone.resourceeditor

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/plone/plone.resourceeditor
  license: GPL
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
