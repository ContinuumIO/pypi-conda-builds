package:
  name: flask-dbshell
  version: "1.0"

source:
  fn: Flask-DbShell-1.0.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-DbShell/Flask-DbShell-1.0.tar.gz
  md5: a4d695467538caa850ec1fe8bc66bdc3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-dbshell = flask-dbshell:main
    #
    # Would create an entry point called flask-dbshell that calls flask-dbshell.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask

  run:
    - python
    - flask

test:
  # Python imports
  imports:
    - flask_dbshell
    - flask_dbshell.backends

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/ffeast/flask-dbshell/
  license: BSD License
  summary: 'Django-like dbshell'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
