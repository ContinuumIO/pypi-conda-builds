package:
  name: django-urlconverter
  version: "1.0.0"

source:
  fn: django-urlconverter-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-urlconverter/django-urlconverter-1.0.0.tar.gz
  md5: c5668ddeba7056377b6637f7f4c3b767
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-urlconverter = django-urlconverter:main
    #
    # Would create an entry point called django-urlconverter that calls django-urlconverter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - django_urlconverter
    - tests
    - tests.app
    - tests.converters

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT
  summary: 'Django URL converters'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
