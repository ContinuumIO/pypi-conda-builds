package:
  name: django-checksum
  version: "0.1.rec68f42a975e"

source:
  fn: django-checksum-0.1.rec68f42a975e.tar.gz
  url: https://pypi.python.org/packages/source/d/django-checksum/django-checksum-0.1.rec68f42a975e.tar.gz
  md5: 5694a7cfe16379d403b521c484d7348b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-checksum = django-checksum:main
    #
    # Would create an entry point called django-checksum that calls django-checksum.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - checksum

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/lars512/django-checksum/
  license: GPL
  summary: 'Django checksum app'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
