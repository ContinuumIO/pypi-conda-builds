package:
  name: collective.portlet.tal
  version: "1.2.0"

source:
  fn: collective.portlet.tal-1.2.0.zip
  url: https://pypi.python.org/packages/source/c/collective.portlet.tal/collective.portlet.tal-1.2.0.zip
  md5: c746ba6f3ece2466c1f7a802ae964e80
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.portlet.tal = collective.portlet.tal:main
    #
    # Would create an entry point called collective.portlet.tal that calls collective.portlet.tal.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.cmfplone
    - products.plonetestcase

  run:
    - python
    - setuptools
    - setuptools
    - products.cmfplone
    - products.plonetestcase

test:
  # Python imports
  imports:
    - collective
    - collective.portlet
    - collective.portlet.tal
    - collective.portlet.tal.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org
  license: GPL
  summary: 'TAL portlet for Plone 3 and 4'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
