package:
  name: sleipnir.testing
  version: "0.1rc7"

source:
  fn: sleipnir.testing-0.1rc7.tar.gz
  url: https://pypi.python.org/packages/source/s/sleipnir.testing/sleipnir.testing-0.1rc7.tar.gz
  md5: 6882ac0699901a97302f3cb763554103
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sleipnir.testing = sleipnir.testing:main
    #
    # Would create an entry point called sleipnir.testing that calls sleipnir.testing.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - sleipnir
    - sleipnir.testing
    - sleipnir.testing.data

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://sleipnir.liberalia.net/
  license: Python Software Foundation License
  summary: 'Sleipnir Testing Framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
