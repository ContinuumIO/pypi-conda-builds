package:
  name: django-development-fabfile
  version: "2.13.2"

source:
  fn: django-development-fabfile-2.13.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-development-fabfile/django-development-fabfile-2.13.2.tar.gz
  md5: 3fb8ecf7fc609afaf8e4ffc8e3249d91
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-development-fabfile = django-development-fabfile:main
    #
    # Would create an entry point called django-development-fabfile that calls django-development-fabfile.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fabric
    - flake8_snippets

  run:
    - python
    - fabric
    - flake8_snippets

test:
  # Python imports
  imports:
    - development_fabfile
    - development_fabfile.fabfile

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bitmazk/django-development-fabfile
  license: The MIT License
  summary: 'A fabfile to ease many tasks during development and deployment of Django projects.\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
