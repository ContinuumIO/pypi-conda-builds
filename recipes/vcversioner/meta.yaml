package:
  name: vcversioner
  version: "2.14.0.0"

source:
  fn: vcversioner-2.14.0.0.tar.gz
  url: https://pypi.python.org/packages/source/v/vcversioner/vcversioner-2.14.0.0.tar.gz
  md5: 7848a365ced9941053bc25d9a9f8f4b4
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - vcversioner = vcversioner:main
    #
    # Would create an entry point called vcversioner that calls vcversioner.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - vcversioner

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/habnabit/vcversioner
  license: ISC License (ISCL)
  summary: 'Use version control tags to discover version numbers'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
