package:
  name: pygraphics
  version: "2.1"

source:
  fn: PyGraphics-2.1.tar.gz
  url: https://pypi.python.org/packages/source/P/PyGraphics/PyGraphics-2.1.tar.gz
  md5: 374e0f9a35b30b72ac617f3e8e6049f0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pygraphics = pygraphics:main
    #
    # Would create an entry point called pygraphics that calls pygraphics.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - mediawindows
    - pygraphics

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/pygraphics
  license: GPL
  summary: 'Easily-accessible library for media manipulation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
