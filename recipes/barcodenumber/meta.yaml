package:
  name: barcodenumber
  version: "0.1"

source:
  fn: barcodenumber-0.1.tar.gz
  url: https://pypi.python.org/packages/source/b/barcodenumber/barcodenumber-0.1.tar.gz
  md5: 831ad66245ee9438ae5611fcdc358d24
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - barcodenumber = barcodenumber:main
    #
    # Would create an entry point called barcodenumber that calls barcodenumber.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - barcodenumber

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.zikzakmedia.com/
  license: GNU General Public License (GPL)
  summary: 'Python module to validate Bank codes'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
