package:
  name: apns-proxy-client
  version: "0.1.0"

source:
  fn: apns-proxy-client-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/a/apns-proxy-client/apns-proxy-client-0.1.0.tar.gz
  md5: 8c91241268a45702a8defa2266af24e9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - apns-proxy-client = apns-proxy-client:main
    #
    # Would create an entry point called apns-proxy-client that calls apns-proxy-client.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyzmq
    - simplejson

  run:
    - python
    - pyzmq
    - simplejson

test:
  # Python imports
  imports:
    - apns_proxy_client
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/voyagegroup/apns-proxy-client-py
  license: BSD License
  summary: 'Client library for APNs Proxy Server.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
