package:
  name: django-redisdb
  version: "0.1.0"

source:
  fn: django-redisdb-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-redisdb/django-redisdb-0.1.0.tar.gz
  md5: 048cbbc3b321b3e1673b0dce2a7b5fd9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-redisdb = django-redisdb:main
    #
    # Would create an entry point called django-redisdb that calls django-redisdb.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - redis
    - hash_ring

  run:
    - python
    - redis
    - hash_ring

test:
  # Python imports
  imports:
    - redisdb
    - redisdb.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://kidosoft.pl
  license: BSD License
  summary: 'Django redis backend'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
