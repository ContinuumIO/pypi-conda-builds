package:
  name: axiombench
  version: "0"

source:
  fn: axiombench-0.tar.gz
  url: https://pypi.python.org/packages/source/a/axiombench/axiombench-0.tar.gz
  md5: b99de6a91217519831f6608ea97212d1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - axiombench = axiombench:main
    #
    # Would create an entry point called axiombench that calls axiombench.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - axiombench
    - axiombench.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lvh/axiombench
  license: ISC License (ISCL)
  summary: 'Silly benchmarks for Axiom'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
