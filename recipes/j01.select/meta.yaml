package:
  name: j01.select
  version: "0.5.1"

source:
  fn: j01.select-0.5.1.zip
  url: https://pypi.python.org/packages/source/j/j01.select/j01.select-0.5.1.zip
  md5: 3811295a9d813d88b516f8ea063e6ca5
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - j01.select = j01.select:main
    #
    # Would create an entry point called j01.select that calls j01.select.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - z3c.form
    - zope.interface

  run:
    - python
    - setuptools
    - z3c.form
    - zope.interface

test:
  # Python imports
  imports:
    - j01
    - j01.select

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/j01.select
  license: Zope Public License
  summary: 'Multi select widget based on JQuery and z3c.form for Zope 3'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
