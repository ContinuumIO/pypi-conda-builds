package:
  name: collective.plonetruegallery
  version: "3.4.5"

source:
  fn: collective.plonetruegallery-3.4.5.zip
  url: https://pypi.python.org/packages/source/c/collective.plonetruegallery/collective.plonetruegallery-3.4.5.zip
  md5: 8a8bbd49955a37c33d2056562bc7a433
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.plonetruegallery = collective.plonetruegallery:main
    #
    # Would create an entry point called collective.plonetruegallery that calls collective.plonetruegallery.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - collective.ptg.galleria
    - plone.app.collection
    - plone.app.contentmenu
    - plone.app.form
    - plone.app.imaging
    - plone.app.portlets
    - plone.app.querystring
    - plone.app.vocabularies
    - plone.app.z3cform
    - plone.folder
    - plone.memoize
    - plone.portlets
    - plone.uuid
    - plone.z3cform
    - products.archetypes
    - products.atcontenttypes
    - products.cmfcore
    - products.cmfplone >=4.1
    - products.genericsetup
    - setuptools
    - transaction
    - z3c.form
    - zope.component
    - zope.i18nmessageid
    - zope.interface
    - zope.schema

  run:
    - python
    - setuptools
    - collective.ptg.galleria
    - plone.app.collection
    - plone.app.contentmenu
    - plone.app.form
    - plone.app.imaging
    - plone.app.portlets
    - plone.app.querystring
    - plone.app.vocabularies
    - plone.app.z3cform
    - plone.folder
    - plone.memoize
    - plone.portlets
    - plone.uuid
    - plone.z3cform
    - products.archetypes
    - products.atcontenttypes
    - products.cmfcore
    - products.cmfplone >=4.1
    - products.genericsetup
    - setuptools
    - transaction
    - z3c.form
    - zope.component
    - zope.i18nmessageid
    - zope.interface
    - zope.schema

test:
  # Python imports
  imports:
    - collective
    - collective.plonetruegallery
    - collective.plonetruegallery.browser
    - collective.plonetruegallery.browser.views
    - collective.plonetruegallery.galleryadapters
    - collective.plonetruegallery.meta
    - collective.plonetruegallery.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/plone-true-gallery
  license: GNU General Public License (GPL)
  summary: 'A gallery/slideshow product for plone that can aggregate from picasa (add collective.ptg.flickr) and flickr (add collective.ptg.flickr) or use plone images.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
