package:
  name: collective.ptg.tile
  version: "0.1b1"

source:
  fn: collective.ptg.tile-0.1b1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.ptg.tile/collective.ptg.tile-0.1b1.tar.gz
  md5: 78742fc842c4dc8ba26d4e82b1edae41
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.ptg.tile = collective.ptg.tile:main
    #
    # Would create an entry point called collective.ptg.tile that calls collective.ptg.tile.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.plonetruegallery
    - collective.cover
    - zope.i18nmessageid
    - plone.api

  run:
    - python
    - setuptools
    - setuptools
    - collective.plonetruegallery
    - collective.cover
    - zope.i18nmessageid
    - plone.api

test:
  # Python imports
  imports:
    - collective
    - collective.ptg
    - collective.ptg.tile

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/espenmn/collective.ptg.tile
  license: GPL
  summary: 'Trueghallery tile for collective.cover'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
