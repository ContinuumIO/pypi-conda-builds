package:
  name: zantedeschia
  version: "0.1"

source:
  fn: Zantedeschia-0.1.tar.gz
  url: https://pypi.python.org/packages/source/Z/Zantedeschia/Zantedeschia-0.1.tar.gz
  md5: e0d02a1d681c9e5a1cb558ec44cd840b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zantedeschia = zantedeschia:main
    #
    # Would create an entry point called zantedeschia that calls zantedeschia.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - zantedeschia

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/takluyver/Zantedeschia
  license: BSD License
  summary: 'ZeroMQ sockets integrated with the AsyncIO event loop'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
