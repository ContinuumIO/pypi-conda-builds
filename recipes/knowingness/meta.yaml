package:
  name: knowingness
  version: "0.5.0"

source:
  fn: Knowingness-0.5.0.tar.gz
  url: https://pypi.python.org/packages/source/K/Knowingness/Knowingness-0.5.0.tar.gz
  md5: 796fd2594cf40626303d987f935a36fd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - knowingness = knowingness:main
    #
    # Would create an entry point called knowingness that calls knowingness.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - knowingness
    - knowingness.social

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: LICENSE.txt
  summary: 'Social Network Analytics'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
