package:
  name: data.store
  version: "0.6.6"

source:
  fn: data.store-0.6.6.tar.gz
  url: https://pypi.python.org/packages/source/d/data.store/data.store-0.6.6.tar.gz
  md5: 755d284a0a82468e9a2d0b466aedfaf5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - data.store = data.store:main
    #
    # Would create an entry point called data.store that calls data.store.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - bottle ==0.12.8

  run:
    - python
    - bottle ==0.12.8

test:
  # Python imports
  imports:
    - data
    - data.store

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/iLoveTux/data_store
  license: GPL v2
  summary: 'A MongoDB inspired data_store'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
