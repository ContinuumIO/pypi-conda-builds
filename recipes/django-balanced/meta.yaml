package:
  name: django-balanced
  version: "0.1.10"

source:
  fn: django-balanced-0.1.10.tar.gz
  url: https://pypi.python.org/packages/source/d/django-balanced/django-balanced-0.1.10.tar.gz
  md5: 55138fa13f6f670fc85ff10f0df044d7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-balanced = django-balanced:main
    #
    # Would create an entry point called django-balanced that calls django-balanced.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - balanced >=0.9.0
    - django >=1.4

  run:
    - python
    - balanced >=0.9.0
    - django >=1.4

test:
  # Python imports
  imports:
    - django_balanced
    - django_balanced.management
    - django_balanced.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://balancedpayments.com/
  license: BSD License
  summary: 'Payments platform for marketplaces'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
