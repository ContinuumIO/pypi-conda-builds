package:
  name: egggitinfo
  version: "0.1"

source:
  fn: egggitinfo-0.1.tar.gz
  url: https://pypi.python.org/packages/source/e/egggitinfo/egggitinfo-0.1.tar.gz
  md5: 95787d45e8115fb5de020c241a134b59
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - egggitinfo = egggitinfo:main
    #
    # Would create an entry point called egggitinfo that calls egggitinfo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - egggitinfo

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'Plants some git-related data into the .egg-info directory of a python package.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
