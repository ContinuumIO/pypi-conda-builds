package:
  name: gagrab
  version: "0.2.0"

source:
  fn: gagrab-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/g/gagrab/gagrab-0.2.0.tar.gz
  md5: 2981e1866da20f2c698679cb0f35dd00
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gagrab = gagrab:main
    #
    # Would create an entry point called gagrab that calls gagrab.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six

  run:
    - python
    - six

test:
  # Python imports
  imports:
    - gagrab
    - gagrab.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ambitioninc/gagrab
  license: MIT License
  summary: 'When want to grab your data from Google Analytics: gagrab it.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
