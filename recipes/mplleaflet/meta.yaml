package:
  name: mplleaflet
  version: "0.0.1"

source:
  fn: mplleaflet-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/m/mplleaflet/mplleaflet-0.0.1.tar.gz
  md5: 5911765b46b9e6d339ad8b9917b84c7e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mplleaflet = mplleaflet:main
    #
    # Would create an entry point called mplleaflet that calls mplleaflet.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - jinja2

  run:
    - python
    - jinja2

test:
  # Python imports
  imports:
    - mplleaflet
    - mplleaflet.mplexporter
    - mplleaflet.mplexporter.renderers
    - mplleaflet.mplexporter.tests
    - mplleaflet.templates

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/jwass/mplleaflet
  license: BSD 3-clause
  summary: 'Convert Matplotlib plots into Leaflet web maps'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
