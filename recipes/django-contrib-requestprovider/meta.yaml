package:
  name: django-contrib-requestprovider
  version: "1.0.1"

source:
  fn: django-contrib-requestprovider-1.0.1.tar.gz
  url: https://github.com/malfaux/snakecheese/blob/master/eggs/django-contrib-requestprovider-1.0.1.tar.gz?raw=true
  #md5: 
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-contrib-requestprovider = django-contrib-requestprovider:main
    #
    # Would create an entry point called django-contrib-requestprovider that calls django-contrib-requestprovider.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - gadjo
    - gadjo.requestprovider

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://shrd.net/p/loophole/trunk/python/src/gadjolib/
  license: BSD License
  summary: 'access django request object whenever you need it'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
