package:
  name: authenticity
  version: "0.1.1"

source:
  fn: authenticity-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/a/authenticity/authenticity-0.1.1.tar.gz
  md5: ec5d98435e879c141a62cccc39e00b6d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - authenticity = authenticity:main
    #
    # Would create an entry point called authenticity that calls authenticity.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - authenticity

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://projects.cnix.ro/authenticity
  license: GNU General Public License (GPL)
  summary: 'OpenID Server running on mod_python and using a LDAP directory'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
