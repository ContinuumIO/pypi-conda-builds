package:
  name: cspkeyset
  version: "0.1.13"

source:
  fn: cspkeyset-0.1.13.tar.gz
  url: https://pypi.python.org/packages/source/c/cspkeyset/cspkeyset-0.1.13.tar.gz
  md5: 10e1be41cd7982de3e26cb446a47e28f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cspkeyset = cspkeyset:main
    #
    # Would create an entry point called cspkeyset that calls cspkeyset.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - pyasn1
    - cryptoapy >=0.4.32
    - rarfile

  run:
    - python
    - pyasn1
    - cryptoapy >=0.4.32
    - rarfile

test:
  # Python imports
  imports:
    - cspkeyset

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/andviro/cspkeyset
  license: LGPL
  summary: 'CSP keyset management (currently Linux CryptoPro CSP hdimages only)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
