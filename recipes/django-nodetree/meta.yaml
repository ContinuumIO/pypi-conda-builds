package:
  name: django-nodetree
  version: "0.1.1"

source:
  fn: django-nodetree-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-nodetree/django-nodetree-0.1.1.tar.gz
  md5: 446a9b737dba1a35e5b2a6317cde0cf4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-nodetree = django-nodetree:main
    #
    # Would create an entry point called django-nodetree that calls django-nodetree.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - nodetree
    - nodetree/migrations
    - nodetree/runtests
    - nodetree/tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lukasbuenger/nodetree
  license: BSD License
  summary: "Library to help creating API's that handle contents in a ordered tree data structure with Django."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
