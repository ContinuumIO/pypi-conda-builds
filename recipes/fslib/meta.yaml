package:
  name: fslib
  version: "0.2.1"

source:
  fn: fslib-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/f/fslib/fslib-0.2.1.tar.gz
  md5: 671078e4d7e6705bb71f8f64dfc3e4b0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fslib = fslib:main
    #
    # Would create an entry point called fslib that calls fslib.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - fslib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rbarrois/fslib/
  license: BSD License
  summary: 'Wrapper around low-level FS-related calls, with support for advanced FS stacking'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
