package:
  name: collective.oaiintercom
  version: "2.1.1"

source:
  fn: collective.oaiintercom-2.1.1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.oaiintercom/collective.oaiintercom-2.1.1.tar.gz
  md5: 83a42a183c417112dfcf595ad6ec40a5
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.oaiintercom = collective.oaiintercom:main
    #
    # Would create an entry point called collective.oaiintercom that calls collective.oaiintercom.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.oaiintercom
    - collective.oaiintercom.browser
    - collective.oaiintercom.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/oaintercom
  license: GPL
  summary: 'OAI-PMH v2.0 Repository plugin for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
