package:
  name: guibuilder
  version: "0.9.9"

source:
  fn: GuiBuilder-0.9.9.tar.gz
  url: https://github.com/timothycrosley/GuiBuilder/blob/master/dist/GuiBuilder-0.9.9.tar.gz?raw=true
  #md5: 
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - guibuilder = guibuilder:main
    #
    # Would create an entry point called guibuilder that calls guibuilder.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - webelements >=1.0.0-alpha.21

  run:
    - python
    - webelements >=1.0.0-alpha.21

test:
  # Python imports
  imports:
    - GuiBuilder
    - guibuilder

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.guibuilder.net/
  license: GNU GPLv2
  summary: 'A drag and drop interface to create WebElement UI templates quickly and easily.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
