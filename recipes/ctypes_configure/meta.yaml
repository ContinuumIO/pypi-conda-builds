package:
  name: ctypes_configure
  version: "0.1"

source:
  fn: ctypes_configure-0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/ctypes_configure/ctypes_configure-0.1.tar.gz
  md5: aac5c92e1553fc3dcb4fdb92265f22d4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ctypes_configure = ctypes_configure:main
    #
    # Would create an entry point called ctypes_configure that calls ctypes_configure.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - ctypes_configure
    - ctypes_configure/test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://codespeak.net/svn/pypy/dist/ctypes_configure/doc/configure.html
  license: UNKNOWN
  summary: 'Ctypes Configuration'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
