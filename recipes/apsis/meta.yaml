package:
  name: apsis
  version: "0.1.1"

source:
  fn: apsis-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/a/apsis/apsis-0.1.1.tar.gz
  md5: a3280076c95c6d3f1139fe8a61fc5937
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - apsis = apsis:main
    #
    # Would create an entry point called apsis that calls apsis.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - scipy
    - numpy
    - gpy >=0.6.0
    - matplotlib

  run:
    - python
    - scipy
    - numpy
    - gpy >=0.6.0
    - matplotlib

test:
  # Python imports
  imports:
    - apsis
    - apsis.assistants
    - apsis.demos
    - apsis.models
    - apsis.optimizers
    - apsis.optimizers.bayesian
    - apsis.utilities

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/FrederikDiehl/apsis
  license: MIT License
  summary: 'Toolkit for hyperparameter optimization for machine learning algorithms.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
