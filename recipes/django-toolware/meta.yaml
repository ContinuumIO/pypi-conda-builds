package:
  name: django-toolware
  version: "0.0.4"

source:
  fn: django-toolware-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-toolware/django-toolware-0.0.4.tar.gz
  md5: 90c6a97056546056a9af1030cd7faefe
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-toolware = django-toolware:main
    #
    # Would create an entry point called django-toolware that calls django-toolware.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six >=1.4.1

  run:
    - python
    - six >=1.4.1

test:
  # Python imports
  imports:
    - toolware
    - toolware/templatetags
    - toolware/tests
    - toolware/utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/un33k/django-toolware
  license: BSD License
  summary: 'A utility application that offers the most commonly-used tools.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
