package:
  name: pywsgi
  version: "0.9.0"

source:
  fn: pywsgi-0.9.0.tar.gz
  url: https://pypi.python.org/packages/source/p/pywsgi/pywsgi-0.9.0.tar.gz
  md5: f0b4f85d9b5432ba7e63c2dc5ee70c72
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pywsgi = pywsgi:main
    #
    # Would create an entry point called pywsgi that calls pywsgi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - pywsgi
    - pywsgi.util

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/pywsgi/
  license: GNU General Public License (GPL)
  summary: 'A high-level class-based API around WSGI/CGI/mod_python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
