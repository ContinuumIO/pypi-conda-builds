package:
  name: markovgenerator
  version: "0.0.5"

source:
  fn: markovgenerator-0.0.5.tar.gz
  url: https://pypi.python.org/packages/source/m/markovgenerator/markovgenerator-0.0.5.tar.gz
  md5: fd5851ea275ebfbb4d70e31651d5756e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - markovgenerator = markovgenerator:main
    #
    # Would create an entry point called markovgenerator that calls markovgenerator.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - nltk
    - wsgiref

  run:
    - python
    - nltk
    - wsgiref

test:
  # Python imports
  imports:
    - markovgenerator

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/amandapickering/markovgenerator
  license: MIT
  summary: 'Markov text generator'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
