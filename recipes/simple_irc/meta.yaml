package:
  name: simple_irc
  version: "1.0"

source:
  fn: simple_irc-1.0.tar.gz
  url: https://pypi.python.org/packages/source/s/simple_irc/simple_irc-1.0.tar.gz
  md5: 0c8ac63811ab1b581dae558af7a6c8d5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - simple_irc = simple_irc:main
    #
    # Would create an entry point called simple_irc that calls simple_irc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - simple_irc

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/maxrothman/simple_irc
  license: MIT License
  summary: 'A simple, Pythonic IRC interface'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
