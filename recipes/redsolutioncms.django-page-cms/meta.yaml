package:
  name: redsolutioncms.django-page-cms
  version: "1.4.5"

source:
  fn: redsolutioncms.django-page-cms-1.4.5.tar.gz
  url: https://pypi.python.org/packages/source/r/redsolutioncms.django-page-cms/redsolutioncms.django-page-cms-1.4.5.tar.gz
  md5: d711ca044901352daa0bf52f6aed0ff9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - redsolutioncms.django-page-cms = redsolutioncms.django-page-cms:main
    #
    # Would create an entry point called redsolutioncms.django-page-cms that calls redsolutioncms.django-page-cms.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.1.1
    - django-mptt >=0.4.1
    - html5lib >=0.10
    - beautifulsoup

  run:
    - python
    - django >=1.1.1
    - django-mptt >=0.4.1
    - html5lib >=0.10
    - beautifulsoup

test:
  # Python imports
  imports:
    - pages
    - pages.admin
    - pages.management
    - pages.management.commands
    - pages.migrations
    - pages.templatetags
    - pages.testproj
    - pages.testproj.documents
    - pages.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://packages.python.org/django-page-cms/
  license: BSD License
  summary: 'A tree based Django CMS application'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
