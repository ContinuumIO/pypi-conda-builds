package:
  name: django-stormpath
  version: "1.0.1"

source:
  fn: django-stormpath-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-stormpath/django-stormpath-1.0.1.tar.gz
  md5: d57799a302dc25aa0152de1899b568c6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-stormpath = django-stormpath:main
    #
    # Would create an entry point called django-stormpath that calls django-stormpath.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests-oauthlib >=0.4.2
    - stormpath >=1.2.7
    - django >=1.6

  run:
    - python
    - requests-oauthlib >=0.4.2
    - stormpath >=1.2.7
    - django >=1.6

test:
  # Python imports
  imports:
    - django_stormpath
    - django_stormpath.management
    - django_stormpath.management.commands
    - django_stormpath.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/stormpath/stormpath-django
  license: Apache
  summary: 'Stormpath integration for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
