package:
  name: ella_toolikt
  version: "1.0.1"

source:
  fn: ella_toolikt-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/e/ella_toolikt/ella_toolikt-1.0.1.tar.gz
  md5: 52650fcc106585c8a76d30826c2a97b9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ella_toolikt = ella_toolikt:main
    #
    # Would create an entry point called ella_toolikt that calls ella_toolikt.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - ella_toolikt
    - ella_toolkit

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/fragaria/ella_toolkit
  license: BSD License
  summary: 'Tools and utils for Ella CMS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
