package:
  name: django-sms-gateway
  version: "1.2.1"

source:
  fn: django-sms-gateway-1.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-sms-gateway/django-sms-gateway-1.2.1.tar.gz
  md5: bcedf441925ab6b7d454de6a095d784f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-sms-gateway = django-sms-gateway:main
    #
    # Would create an entry point called django-sms-gateway that calls django-sms-gateway.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django-jsonfield
    - django-uuidfield-2
    - django-picklefield

  run:
    - python
    - django-jsonfield
    - django-uuidfield-2
    - django-picklefield

test:
  # Python imports
  imports:
    - sms
    - sms.migrations
    - sms.models

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/schinckel/django-sms-gateway
  license: MIT License
  summary: 'django generic sms through http gateway'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
