package:
  name: collective.confirmableforms
  version: "1.4.2"

source:
  fn: collective.confirmableforms-1.4.2.zip
  url: https://pypi.python.org/packages/source/c/collective.confirmableforms/collective.confirmableforms-1.4.2.zip
  md5: faea1eead8b301929643bc89c02725ae
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.confirmableforms = collective.confirmableforms:main
    #
    # Would create an entry point called collective.confirmableforms that calls collective.confirmableforms.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.ploneformgen
    - collective.depositbox

  run:
    - python
    - setuptools
    - setuptools
    - products.ploneformgen
    - collective.depositbox

test:
  # Python imports
  imports:
    - collective
    - collective.confirmableforms
    - collective.confirmableforms.content

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/zestsoftware/collective.confirmableforms
  license: GPL
  summary: 'A Plone add on to simplify form confirmation by e-mail.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
