package:
  name: live-sports-feed
  version: "1.0.1"

source:
  fn: Live-sports-feed-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/L/Live-sports-feed/Live-sports-feed-1.0.1.tar.gz
  md5: 2f875c8bf2836592c9fd205122250db4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - live-sports-feed = live-sports-feed:main
    #
    # Would create an entry point called live-sports-feed that calls live-sports-feed.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tiwariayush/Live-sports-feed
  license: MIT License
  summary: ' Gives live feeds of cricket and football matches in a python file and text file , can be easily integrated inside an app'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
