package:
  name: guesstimator
  version: "0.0.2"

source:
  fn: guesstimator-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/g/guesstimator/guesstimator-0.0.2.tar.gz
  md5: 346d1c3b007bd1cad11730ccf5f5f953
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - guesstimator = guesstimator:main
    #
    # Would create an entry point called guesstimator that calls guesstimator.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - redis

  run:
    - python
    - redis

test:
  # Python imports
  imports:
    - guesstimator
    - test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bcoe/guesstimator
  license: UNKNOWN
  summary: 'Estimates the performance of a distributed system based on a sample set of data.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
