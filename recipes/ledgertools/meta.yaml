package:
  name: ledgertools
  version: "0.3"

source:
  fn: ledgertools-0.3.tar.gz
  url: https://pypi.python.org/packages/source/l/ledgertools/ledgertools-0.3.tar.gz
  md5: c96efe52d16908d422a372520a62abf2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ledgertools = ledgertools:main
    #
    # Would create an entry point called ledgertools that calls ledgertools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - ledgertools
    - ltlib
    - ltlib.readers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/frasertweedale/ledgertools
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'Ledger accounting system utilities'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
