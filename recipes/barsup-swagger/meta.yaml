package:
  name: barsup-swagger
  version: "0.1.0"

source:
  fn: barsup-swagger-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/b/barsup-swagger/barsup-swagger-0.1.0.tar.gz
  md5: fd1bae4a101ac5e42ba15958bd96b3fc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - barsup-swagger = barsup-swagger:main
    #
    # Would create an entry point called barsup-swagger that calls barsup-swagger.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - barsup_swagger

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/barsgroup/barsup-swagger
  license: MIT License
  summary: 'Swagger.io API UI for barsup-core-based applications'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
