package:
  name: django-revisions
  version: "0.3"

source:
  fn: django-revisions-0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-revisions/django-revisions-0.3.tar.gz
  md5: 77d984b15aaedc77c401494b8353b451
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-revisions = django-revisions:main
    #
    # Would create an entry point called django-revisions that calls django-revisions.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - revisions
    - revisions.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://stdbrouw.github.com/django-revisions/
  license: BSD License
  summary: 'Implements a revision trail for model instances in Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
