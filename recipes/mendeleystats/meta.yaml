package:
  name: mendeleystats
  version: "0.1.2"

source:
  fn: mendeleystats-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/m/mendeleystats/mendeleystats-0.1.2.tar.gz
  md5: 6f9102ea60f2662ef1f53d11d3f6c555
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mendeleystats = mendeleystats:main
    #
    # Would create an entry point called mendeleystats that calls mendeleystats.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - httplib2 ==0.8
    - numpy ==1.7.1
    - matplotlib ==1.2.1
    - oauth2 ==1.5.211
    - requests ==1.2.3
    - wsgiref ==0.1.2

  run:
    - python
    - httplib2 ==0.8
    - numpy ==1.7.1
    - matplotlib ==1.2.1
    - oauth2 ==1.5.211
    - requests ==1.2.3
    - wsgiref ==0.1.2

test:
  # Python imports
  imports:
    - mendeleystats

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/yurimalheiros/mendeleystats
  license: MIT License
  summary: 'A command line tool to extract information about papers in a Mendeley folder.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
