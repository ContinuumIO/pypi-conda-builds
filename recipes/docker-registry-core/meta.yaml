package:
  name: docker-registry-core
  version: "2.0.3"

source:
  fn: docker-registry-core-2.0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/docker-registry-core/docker-registry-core-2.0.3.tar.gz
  md5: 610ef9395f2e9a2f91c68d13325fce7b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - docker-registry-core = docker-registry-core:main
    #
    # Would create an entry point called docker-registry-core that calls docker-registry-core.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - boto ==2.34.0
    - redis ==2.10.3
    - setuptools ==5.8
    - simplejson ==3.6.2

  run:
    - python
    - boto ==2.34.0
    - redis ==2.10.3
    - setuptools ==5.8
    - simplejson ==3.6.2

test:
  # Python imports
  imports:
    - docker_registry
    - docker_registry.core
    - docker_registry.drivers
    - docker_registry.testing

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/docker/docker-registry
  license: Apache Software License
  summary: 'Docker registry core package'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
