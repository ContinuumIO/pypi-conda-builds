package:
  name: django-taggit-forms
  version: "0.1.1"

source:
  fn: django-taggit-forms-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-taggit-forms/django-taggit-forms-0.1.1.tar.gz
  md5: 191c231dd4ef3906a86181eba42ad817
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-taggit-forms = django-taggit-forms:main
    #
    # Would create an entry point called django-taggit-forms that calls django-taggit-forms.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - taggit_forms
    - taggit_forms.migrations
    - taggit_forms.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/akiraakaishi/django-taggit-forms
  license: MIT License
  summary: 'Tag-creation forms for django-taggit'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
