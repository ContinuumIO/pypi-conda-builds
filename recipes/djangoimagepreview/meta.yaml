package:
  name: djangoimagepreview
  version: "1.1.1"

source:
  fn: djangoimagepreview-1.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/djangoimagepreview/djangoimagepreview-1.1.1.tar.gz
  md5: 9b430c3f1b1c960450cf13db84654146
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangoimagepreview = djangoimagepreview:main
    #
    # Would create an entry point called djangoimagepreview that calls djangoimagepreview.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - djangoimagepreview
    - imagepreview

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lucavandro/djangoimagepreview
  license: Apache Software License
  summary: 'Python Distribution Utilities'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
