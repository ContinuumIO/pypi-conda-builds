package:
  name: django-spirit-user-profile
  version: "0.1.1"

source:
  fn: django-spirit-user-profile-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-spirit-user-profile/django-spirit-user-profile-0.1.1.tar.gz
  md5: 1138e3fb0fb0fb119f2c0c67a4c675ab
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-spirit-user-profile = django-spirit-user-profile:main
    #
    # Would create an entry point called django-spirit-user-profile that calls django-spirit-user-profile.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - south
    - django-spirit >=0.1.3

  run:
    - python
    - south
    - django-spirit >=0.1.3

test:
  # Python imports
  imports:
    - spirit_user_profile
    - spirit_user_profile.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/nitely/Spirit-User-Profile
  license: MIT License
  summary: 'Integrate Spirit user profile to your existing project.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
