package:
  name: hens
  version: "0.4.1"

source:
  fn: hens-0.4.1.tar.gz
  url: https://pypi.python.org/packages/source/h/hens/hens-0.4.1.tar.gz
  md5: 90dc4bcf3903dc8c9aacf9cd2c0c63d2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hens = hens:main
    #
    # Would create an entry point called hens that calls hens.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - tornado
    - qpid-python
    - jsonpickle >=0.8.0
    - pika
    - logging
    - minsol

  run:
    - python
    - tornado
    - qpid-python
    - jsonpickle >=0.8.0
    - pika
    - logging
    - minsol

test:
  # Python imports
  imports:
    - hens

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pythonhosted.org/hens/
  license: BSD License
  summary: '\nRPC client, server (support http, amqp)\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
