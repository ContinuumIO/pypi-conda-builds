package:
  name: django-clear-cache
  version: "0.3"

source:
  fn: django-clear-cache-0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-clear-cache/django-clear-cache-0.3.tar.gz
  md5: c093e365bc39d0fbc6b5919922fe9825
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-clear-cache = django-clear-cache:main
    #
    # Would create an entry point called django-clear-cache that calls django-clear-cache.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.0

  run:
    - python
    - django >=1.0

test:
  # Python imports
  imports:
    - clear_cache
    - clear_cache.management
    - clear_cache.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rdegges/django-clear-cache
  license: UNLICENSE
  summary: 'A simple Django management command which clears your cache.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
