package:
  name: vecnet.emod
  version: "0.5.0"

source:
  fn: vecnet.emod-0.5.0.zip
  url: https://pypi.python.org/packages/source/v/vecnet.emod/vecnet.emod-0.5.0.zip
  md5: fd4dfcbfebc35a4bc8e730de5ac23106
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - vecnet.emod = vecnet.emod:main
    #
    # Would create an entry point called vecnet.emod that calls vecnet.emod.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - vecnet
    - vecnet.emod

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/vecnet/vecnet.emod
  license: Mozilla Public License 2.0 (MPL 2.0)
  summary: 'Python package for loading EMOD weather files, store the data in memory so that it can be modified, and saves the data to weather files. Allows for conversions between nodeIDs and latitude and longitude pairs.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
