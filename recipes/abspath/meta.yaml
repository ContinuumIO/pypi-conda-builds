package:
  name: abspath
  version: "0.1.0"

source:
  fn: abspath-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/a/abspath/abspath-0.1.0.tar.gz
  md5: 181de72a73511caa42b1f8fa4b1d9580
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - abspath = abspath:main
    #
    # Would create an entry point called abspath that calls abspath.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - abspath

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/arne-cl/abspath
  license: 3-Clause BSD
  summary: 'get the absolute paths of files on the command line'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
