package:
  name: codernitydb
  version: "0.4.2"

source:
  fn: CodernityDB-0.4.2.tar.gz
  url: https://pypi.python.org/packages/source/C/CodernityDB/CodernityDB-0.4.2.tar.gz
  md5: 9eccbd4f1d4aec78518853515672f7db
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - codernitydb = codernitydb:main
    #
    # Would create an entry point called codernitydb that calls codernitydb.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - CodernityDB
    - codernitydb

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://labs.codernity.com/codernitydb
  license: Apache Software License
  summary: 'Pure python, fast, schema-less, NoSQL database'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
