package:
  name: django-cbv-formpreview
  version: "0.2.0a1"

source:
  fn: django-cbv-formpreview-0.2.0a1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-cbv-formpreview/django-cbv-formpreview-0.2.0a1.tar.gz
  md5: f77e60e4e6b2a193aedc47b83273d2bf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-cbv-formpreview = django-cbv-formpreview:main
    #
    # Would create an entry point called django-cbv-formpreview that calls django-cbv-formpreview.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - cbv_formpreview
    - cbv_formpreview.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ryankask/django-cbv-formpreview
  license: BSD License
  summary: "Django's FormPreview updated to use class based views."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
