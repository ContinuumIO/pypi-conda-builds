package:
  name: rollbar-agent
  version: "0.3.12"

source:
  fn: rollbar-agent-0.3.12.tar.gz
  url: https://pypi.python.org/packages/source/r/rollbar-agent/rollbar-agent-0.3.12.tar.gz
  md5: 06dbba8a1668c4d44bbe3c94cb21458a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - rollbar-agent = rollbar-agent:main
    #
    # Would create an entry point called rollbar-agent that calls rollbar-agent.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/rollbar/rollbar-agent
  license: MIT License
  summary: 'Rollbar server-side agent'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
