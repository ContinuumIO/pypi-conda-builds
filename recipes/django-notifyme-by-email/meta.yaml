package:
  name: django-notifyme-by-email
  version: "0.0.1a"

source:
  fn: django-notifyme-by-email-0.0.1a.tar.gz
  url: https://pypi.python.org/packages/source/d/django-notifyme-by-email/django-notifyme-by-email-0.0.1a.tar.gz
  md5: b13d6789c7102a0cd5101264714173df
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-notifyme-by-email = django-notifyme-by-email:main
    #
    # Would create an entry point called django-notifyme-by-email that calls django-notifyme-by-email.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - notifyme_by_email

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/stefanfoulis/django-notifyme-by-email
  license: BSD License
  summary: 'A notification framework for django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
