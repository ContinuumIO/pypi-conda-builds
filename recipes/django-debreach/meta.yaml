package:
  name: django-debreach
  version: "1.2.1"

source:
  fn: django-debreach-1.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-debreach/django-debreach-1.2.1.tar.gz
  md5: 07bd139faae6a27e0a4cb5fc4fe4b7c0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-debreach = django-debreach:main
    #
    # Would create an entry point called django-debreach that calls django-debreach.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - debreach

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/lpomfrey/django-debreach
  license: BSD License
  summary: 'Adds middleware and context processors to give some protection against the BREACH attack in Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
