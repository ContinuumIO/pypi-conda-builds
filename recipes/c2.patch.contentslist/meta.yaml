package:
  name: c2.patch.contentslist
  version: "1.0RC4"

source:
  fn: c2.patch.contentslist-1.0RC4.tar.gz
  url: https://pypi.python.org/packages/source/c/c2.patch.contentslist/c2.patch.contentslist-1.0RC4.tar.gz
  md5: 6f5e584e7c480446a32760813d732c09
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - c2.patch.contentslist = c2.patch.contentslist:main
    #
    # Would create an entry point called c2.patch.contentslist that calls c2.patch.contentslist.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - archetypes.schemaextender

  run:
    - python
    - setuptools
    - setuptools
    - archetypes.schemaextender

test:
  # Python imports
  imports:
    - c2
    - c2.patch
    - c2.patch.contentslist
    - c2.patch.contentslist.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.cmscom.jp
  license: GPL
  summary: 'contents tab viewing order change on folder in Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
