package:
  name: dictionary_split
  version: "1.1.0"

source:
  fn: Dictionary_split-1.1.0.zip
  url: https://pypi.python.org/packages/source/D/Dictionary_split/Dictionary_split-1.1.0.zip
  md5: ab9e04704a0d6ce1cfb66abe75151c8d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dictionary_split = dictionary_split:main
    #
    # Would create an entry point called dictionary_split that calls dictionary_split.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - dictionary_split

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.starthabit.com
  license: UNKNOWN
  summary: 'Dictionary , Splitting , File handling'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
