package:
  name: cyrusbus
  version: "0.1.0"

source:
  fn: cyrusbus-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/c/cyrusbus/cyrusbus-0.1.0.tar.gz
  md5: c2b0ada9d9da83d5c26d85f5276d3523
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cyrusbus = cyrusbus:main
    #
    # Would create an entry point called cyrusbus that calls cyrusbus.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - cyrusbus
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/heynemann/cyrusbus
  license: OSI
  summary: 'cyrusbus - Dead simple event bus for python apps'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
