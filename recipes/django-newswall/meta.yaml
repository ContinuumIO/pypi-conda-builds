package:
  name: django-newswall
  version: "0.1.0"

source:
  fn: django-newswall-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-newswall/django-newswall-0.1.0.tar.gz
  md5: b814ff712698c4ca4f4b24679c826678
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-newswall = django-newswall:main
    #
    # Would create an entry point called django-newswall that calls django-newswall.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4.2

  run:
    - python
    - django >=1.4.2

test:
  # Python imports
  imports:
    - newswall
    - newswall.management
    - newswall.management.commands
    - newswall.migrations
    - newswall.providers
    - newswall.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/matthiask/django-newswall
  license: BSD License
  summary: 'My version of a Tumblelog, because I can.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
