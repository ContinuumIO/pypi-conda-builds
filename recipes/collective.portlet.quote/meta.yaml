package:
  name: collective.portlet.quote
  version: "1.0.1"

source:
  fn: collective.portlet.quote-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.portlet.quote/collective.portlet.quote-1.0.1.tar.gz
  md5: 01322485c6dddaea2ef57aaff8c37828
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.portlet.quote = collective.portlet.quote:main
    #
    # Would create an entry point called collective.portlet.quote that calls collective.portlet.quote.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.portlet
    - collective.portlet.quote
    - collective.portlet.quote.content
    - collective.portlet.quote.interfaces
    - collective.portlet.quote.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://jarn.com
  license: GPL
  summary: 'A portlet showing random quotes from a folder'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
