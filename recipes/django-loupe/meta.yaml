package:
  name: django-loupe
  version: "0.1.0"

source:
  fn: django-loupe-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-loupe/django-loupe-0.1.0.tar.gz
  md5: 079c7e8e94469c1b8d5c02b31eb6c2dd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-loupe = django-loupe:main
    #
    # Would create an entry point called django-loupe that calls django-loupe.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-imagekit >=0.3.2
    - django-extensions >=0.4.1

  run:
    - python
    - django-imagekit >=0.3.2
    - django-extensions >=0.4.1

test:
  # Python imports
  imports:
    - loupe
    - loupe.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/gregnewman/django-loupe/
  license: BSD License
  summary: 'A simple collaborative image/screenshot review app for Django applications'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
