package:
  name: ds-down
  version: "0.2.1"

source:
  fn: ds-down-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/ds-down/ds-down-0.2.1.tar.gz
  md5: 48747d277f221c82378ea8cf2037ff38
#  patches:
   # List any patch files here
   # - fix.patch

build:
  # preserve_egg_dir: True
  entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ds-down = ds-down:main
    #
    # Would create an entry point called ds-down that calls ds-down.main()

    - ds_down=wor.ds_down:main

  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - wor

  commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.

    - ds_down --help

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/wor/ds-down
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'Synology Download Station url adder.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
