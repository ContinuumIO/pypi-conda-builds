package:
  name: mopidy-notifier
  version: "0.3.3"

source:
  fn: Mopidy-Notifier-0.3.3.tar.gz
  url: https://pypi.python.org/packages/source/M/Mopidy-Notifier/Mopidy-Notifier-0.3.3.tar.gz
  md5: 49e6d63766246f57aaa9fa2bef26ff91
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mopidy-notifier = mopidy-notifier:main
    #
    # Would create an entry point called mopidy-notifier that calls mopidy-notifier.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - mopidy >=0.18
    - pykka >=1.1

  run:
    - python
    - setuptools
    - setuptools
    - mopidy >=0.18
    - pykka >=1.1

test:
  # Python imports
  imports:
    - mopidy_notifier

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/sauberfred/mopidy-notifier
  license: Apache Software License
  summary: u'Mopidy extension for displaying Trackinfo as notifications on Mac OS X 10.8 and Linux'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
