package:
  name: costcla
  version: "0.04.dev1"

source:
  fn: costcla-0.04.dev1.tar.gz
  url: https://pypi.python.org/packages/source/c/costcla/costcla-0.04.dev1.tar.gz
  md5: d622d4814d8c783e18d41ff4d80627b2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - costcla = costcla:main
    #
    # Would create an entry point called costcla that calls costcla.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - scikit-learn >=0.15.0b2
    - pandas >=0.14.0
    - numpy >=1.8.0
    - pyea >=0.1

  run:
    - python
    - scikit-learn >=0.15.0b2
    - pandas >=0.14.0
    - numpy >=1.8.0
    - pyea >=0.1

test:
  # Python imports
  imports:
    - costcla
    - costcla.datasets
    - costcla.metrics
    - costcla.models
    - costcla.probcal
    - costcla.sampling
    - costcla.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/albahnsen/CostSensitiveClassification
  license: BSD License
  summary: 'costcla is a Python module for cost-sensitive machine learning (classification) '

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
