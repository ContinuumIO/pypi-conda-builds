package:
  name: dataschema
  version: "0.1"

source:
  fn: dataschema-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/dataschema/dataschema-0.1.tar.gz
  md5: 18d35697dab4b31d34b3e66f03d0067d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dataschema = dataschema:main
    #
    # Would create an entry point called dataschema that calls dataschema.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - dataschema
    - dataschema.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://pypi.python.org/pypi/dataschema/
  license: MIT License
  summary: 'Schema validation for Python data structures'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
