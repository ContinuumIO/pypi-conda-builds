package:
  name: ks.mailersmtp
  version: "1.0.4"

source:
  fn: ks.mailersmtp-1.0.4.tar.gz
  url: https://pypi.python.org/packages/source/k/ks.mailersmtp/ks.mailersmtp-1.0.4.tar.gz
  md5: 62da675db2c76ec103e5ff41b37e658c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ks.mailersmtp = ks.mailersmtp:main
    #
    # Would create an entry point called ks.mailersmtp that calls ks.mailersmtp.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.cachedescriptors
    - zope.component
    - zope.i18nmessageid
    - zope.interface
    - zope.publisher
    - zope.schema
    - zope.annotation
    - zope.app.container
    - ks.lib >=1.0.2
    - ks.mailer
    - ks.adapter.lazyrequest

  run:
    - python
    - setuptools
    - zope.cachedescriptors
    - zope.component
    - zope.i18nmessageid
    - zope.interface
    - zope.publisher
    - zope.schema
    - zope.annotation
    - zope.app.container
    - ks.lib >=1.0.2
    - ks.mailer
    - ks.adapter.lazyrequest

test:
  # Python imports
  imports:
    - ks
    - ks.mailersmtp
    - ks.mailersmtp.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.keysolutionscorp.com/downloads/public/pypi/ks.mailersmtp
  license: Zope Public License
  summary: 'Message creation utility to send through smtp'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
