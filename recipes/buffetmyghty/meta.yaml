package:
  name: buffetmyghty
  version: "0.3"

source:
  fn: BuffetMyghty-0.3.zip
  url: https://pypi.python.org/packages/source/B/BuffetMyghty/BuffetMyghty-0.3.zip
  md5: ce9bfed39b5274cb580e1587aad5e2bf
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - buffetmyghty = buffetmyghty:main
    #
    # Would create an entry point called buffetmyghty that calls buffetmyghty.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - buffetmyghty

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://projects.dowski.com/projects/buffetmyghty
  license: MIT
  summary: 'Myghty templating language plugin'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
