package:
  name: onegov.page
  version: "0.1.0"

source:
  fn: onegov.page-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/o/onegov.page/onegov.page-0.1.0.tar.gz
  md5: aec1c3a417734c41e21d414f32f968e6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - onegov.page = onegov.page:main
    #
    # Would create an entry point called onegov.page that calls onegov.page.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - onegov.core

  run:
    - python
    - onegov.core

test:
  # Python imports
  imports:
    - onegov
    - onegov.page

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/seantis/onegov.page
  license: GNU General Public License v2 (GPLv2)
  summary: 'Pages in a hierarchy for OneGov.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
