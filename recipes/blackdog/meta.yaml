package:
  name: blackdog
  version: "1.0.2"

source:
  fn: blackdog-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/b/blackdog/blackdog-1.0.2.tar.gz
  md5: 89d00b1f1afdffa8078dcd2032cc894c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - blackdog = blackdog:main
    #
    # Would create an entry point called blackdog that calls blackdog.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyquery
    - baker
    - requests

  run:
    - python
    - pyquery
    - baker
    - requests

test:
  # Python imports
  imports:
    - blackdog

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/Snaipe/BlackDog.git
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'A bukkitdev to maven repository mapper'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
