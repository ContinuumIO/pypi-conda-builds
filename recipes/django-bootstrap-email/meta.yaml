package:
  name: django-bootstrap-email
  version: "0.0.1"

source:
  fn: django-bootstrap-email-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-bootstrap-email/django-bootstrap-email-0.0.1.tar.gz
  md5: 2942451568809b93b0c0fa2997bd58db
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-bootstrap-email = django-bootstrap-email:main
    #
    # Would create an entry point called django-bootstrap-email that calls django-bootstrap-email.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - bootstrap_email
    - bootstrap_email.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kpekarov/django-bootstrap-email
  license: Apache Software License
  summary: 'Bootstrap e-mail support for Django projects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
