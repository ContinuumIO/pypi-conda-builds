package:
  name: airspeed
  version: "0.5.3dev-20150421"

source:
  fn: airspeed-0.5.3dev-20150421.tar.gz
  url: https://pypi.python.org/packages/source/a/airspeed/airspeed-0.5.3dev-20150421.tar.gz
  md5: 221ad8121895b6eaeb8b5cc7e1154d82
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - airspeed = airspeed:main
    #
    # Would create an entry point called airspeed that calls airspeed.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six
    - cachetools ==0.8.0

  run:
    - python
    - setuptools
    - six
    - cachetools ==0.8.0

test:
  # Python imports
  imports:
    - airspeed

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://dev.sanityinc.com/airspeed/wiki
  license: BSD License
  summary: 'Airspeed is a powerful and easy-to-use templating engine for Python that aims for a high level of compatibility with the popular Velocity library for Java.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
