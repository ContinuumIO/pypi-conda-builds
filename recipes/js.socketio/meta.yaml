package:
  name: js.socketio
  version: "0.9.10"

source:
  fn: js.socketio-0.9.10.tar.gz
  url: https://pypi.python.org/packages/source/j/js.socketio/js.socketio-0.9.10.tar.gz
  md5: b664304a3d961f7fc4eae60f9ab582c6
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - js.socketio = js.socketio:main
    #
    # Would create an entry point called js.socketio that calls js.socketio.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fanstatic
    - js.jquery
    - setuptools

  run:
    - python
    - setuptools
    - fanstatic
    - js.jquery
    - setuptools

test:
  # Python imports
  imports:
    - js
    - js.socketio

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/RedTurtle/js.socketio
  license: BSD
  summary: 'fanstatic socketio.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
