package:
  name: django-dirtyfields
  version: "0.4.1"

source:
  fn: django-dirtyfields-0.4.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-dirtyfields/django-dirtyfields-0.4.1.tar.gz
  md5: a982d06003ddcc9cf36776792174c586
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-dirtyfields = django-dirtyfields:main
    #
    # Would create an entry point called django-dirtyfields that calls django-dirtyfields.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.2

  run:
    - python
    - django >=1.2

test:
  # Python imports
  imports:
    - dirtyfields

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/smn/django-dirtyfields
  license: BSD License
  summary: 'Tracking dirty fields on a Django model instance'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
