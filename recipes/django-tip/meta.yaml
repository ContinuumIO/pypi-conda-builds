package:
  name: django-tip
  version: "0.3"

source:
  fn: django-tip-0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-tip/django-tip-0.3.tar.gz
  md5: a89fc703c8d63a580206e4c5abde3950
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-tip = django-tip:main
    #
    # Would create an entry point called django-tip that calls django-tip.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - tip
    - tip.management
    - tip.management.commands
    - tip.tests
    - tip.tests.functional

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/ricobl/django-template-inspector
  license: UNKNOWN
  summary: 'Inspector for Django Templates'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
