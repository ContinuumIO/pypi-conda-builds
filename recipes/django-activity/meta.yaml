package:
  name: django-activity
  version: "0.4.0"

source:
  fn: django-activity-0.4.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-activity/django-activity-0.4.0.tar.gz
  md5: 799ae84c3640bac5cff91bf0f12b6dd9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-activity = django-activity:main
    #
    # Would create an entry point called django-activity that calls django-activity.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.7

  run:
    - python
    - django >=1.7

test:
  # Python imports
  imports:
    - activity
    - activity/migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/eofs/django-activity
  license: BSD
  summary: 'Django Activity provides support for streams'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
