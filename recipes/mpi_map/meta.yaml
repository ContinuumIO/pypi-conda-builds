package:
  name: mpi_map
  version: "0.1.4"

source:
  fn: mpi_map-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/m/mpi_map/mpi_map-0.1.4.tar.gz
  md5: 82ca95fad024e06a9c76c0c364e3ed4c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mpi_map = mpi_map:main
    #
    # Would create an entry point called mpi_map that calls mpi_map.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - mpi4py

  run:
    - python
    - mpi4py

test:
  # Python imports
  imports:
    - mpi_map

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www2.compute.dtu.dk/~dabi/index.php?slab=dtu-uq
  license: COPYING.LESSER
  summary: 'Tools running parallel processes'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
