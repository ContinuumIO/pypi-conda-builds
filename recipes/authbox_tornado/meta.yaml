package:
  name: authbox_tornado
  version: "0.7"

source:
  fn: authbox_tornado-0.7.tar.gz
  url: https://pypi.python.org/packages/source/a/authbox_tornado/authbox_tornado-0.7.tar.gz
  md5: f654efd2ce002dff3afeebb39f7fbcbb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - authbox_tornado = authbox_tornado:main
    #
    # Would create an entry point called authbox_tornado that calls authbox_tornado.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - authbox_tornado

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
