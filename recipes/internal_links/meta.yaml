package:
  name: internal_links
  version: "0.1.6.6"

source:
  fn: internal_links-0.1.6.6.tar.gz
  url: https://pypi.python.org/packages/source/i/internal_links/internal_links-0.1.6.6.tar.gz
  md5: e0c2687fc64f90d8994a9dd331d4543c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - internal_links = internal_links:main
    #
    # Would create an entry point called internal_links that calls internal_links.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - internal_links
    - internal_links.management
    - internal_links.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/deployed/internal_links
  license: MIT License
  summary: 'SEO tool for adding links to text contained in Django apps databases.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
