package:
  name: fedmsg_middleware
  version: "0.0.3"

source:
  fn: fedmsg_middleware-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/f/fedmsg_middleware/fedmsg_middleware-0.0.3.tar.gz
  md5: 89f5e953c1ba4578ea767303c19433ca
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fedmsg_middleware = fedmsg_middleware:main
    #
    # Would create an entry point called fedmsg_middleware that calls fedmsg_middleware.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - webob
    - weberror
    - beautifulsoup <4.0a1
    - moksha.wsgi
    - fedmsg

  run:
    - python
    - setuptools
    - webob
    - weberror
    - beautifulsoup <4.0a1
    - moksha.wsgi
    - fedmsg

test:
  # Python imports
  imports:
    - fedmsg_middleware

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/ralphbean/fedmsg_middleware
  license: MIT License
  summary: 'WSGI middleware for display fedmsg messages.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
