package:
  name: edrnsite.misccontent
  version: "1.0.5"

source:
  fn: edrnsite.misccontent-1.0.5.zip
  url: https://pypi.python.org/packages/source/e/edrnsite.misccontent/edrnsite.misccontent-1.0.5.zip
  md5: 113bceb2e57090868731d6d742e57e9a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - edrnsite.misccontent = edrnsite.misccontent:main
    #
    # Would create an entry point called edrnsite.misccontent that calls edrnsite.misccontent.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.cmfplone
    - zope.globalrequest
    - z3c.autoinclude

  run:
    - python
    - setuptools
    - setuptools
    - products.cmfplone
    - zope.globalrequest
    - z3c.autoinclude

test:
  # Python imports
  imports:
    - edrnsite
    - edrnsite.misccontent
    - edrnsite.misccontent.browser
    - edrnsite.misccontent.content
    - edrnsite.misccontent.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/EDRN/edrnsite.misccontent
  license: Apache Software License
  summary: 'EDRN Miscellaneous Content Types'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
