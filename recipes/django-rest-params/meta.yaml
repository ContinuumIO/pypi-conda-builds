package:
  name: django-rest-params
  version: "1.0.2"

source:
  fn: django-rest-params-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-rest-params/django-rest-params-1.0.2.tar.gz
  md5: d370d1478c530729d239a7150cf2e2db
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-rest-params = django-rest-params:main
    #
    # Would create an entry point called django-rest-params that calls django-rest-params.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - djangorestframework

  run:
    - python
    - django
    - djangorestframework

test:
  # Python imports
  imports:
    - django_rest_params

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/cammsaul/django-rest-params
  license: BSD License
  summary: 'Function decorator for Django REST Framework for specifying and constraining API parameters.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
