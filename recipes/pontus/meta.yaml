package:
  name: pontus
  version: "0.2.1"

source:
  fn: Pontus-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/P/Pontus/Pontus-0.2.1.tar.gz
  md5: 72941f96d5f3621f4db482bfabbca827
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pontus = pontus:main
    #
    # Would create an entry point called pontus that calls pontus.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.10.1
    - python-magic >=0.4.6
    - boto >=2.34.0

  run:
    - python
    - flask >=0.10.1
    - python-magic >=0.4.6
    - boto >=2.34.0

test:
  # Python imports
  imports:
    - pontus

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/fastmonkeys/pontus
  license: MIT License
  summary: 'Flask utility for Amazon S3.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
