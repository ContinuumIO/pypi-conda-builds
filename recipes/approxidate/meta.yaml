package:
  name: approxidate
  version: "1.0.2"

source:
  fn: approxidate-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/a/approxidate/approxidate-1.0.2.tar.gz
  md5: 264d8a9976d22b5b14626a5591ce3cea
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - approxidate = approxidate:main
    #
    # Would create an entry point called approxidate that calls approxidate.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - approxidate

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/thatguystone/approxidate
  license: GPL v2
  summary: 'a quick way to get a unix timestamp from a ton of arbitrary date formats'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
