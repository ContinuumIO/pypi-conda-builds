package:
  name: bucketbrigade
  version: "0.1"

source:
  fn: BucketBrigade-0.1.tar.gz
  url: https://pypi.python.org/packages/source/B/BucketBrigade/BucketBrigade-0.1.tar.gz
  md5: 2671831d5b48ffc551e2ddf7000c4aa4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bucketbrigade = bucketbrigade:main
    #
    # Would create an entry point called bucketbrigade that calls bucketbrigade.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - boto >=1.9

  run:
    - python
    - boto >=1.9

test:
  # Python imports
  imports:
    - bucketbrigade

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/uhjish/bucketbrigade
  license: UNKNOWN
  summary: 'A simple utility to allow syncing/copying S3 bucket files across AWS accounts.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
