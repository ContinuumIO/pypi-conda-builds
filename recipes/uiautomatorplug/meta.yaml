package:
  name: uiautomatorplug
  version: "1.1.4"

source:
  fn: uiautomatorplug-1.1.4.tar.gz
  url: https://pypi.python.org/packages/source/u/uiautomatorplug/uiautomatorplug-1.1.4.tar.gz
  md5: a2340a194ecda07ae53eb39b33f32982
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - uiautomatorplug = uiautomatorplug:main
    #
    # Would create an entry point called uiautomatorplug that calls uiautomatorplug.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - uiautomator

  run:
    - python
    - uiautomator

test:
  # Python imports
  imports:
    - uiautomatorplug

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT
  summary: 'enhancement for the python wrapper of android uiautomator. provides image comparision method'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
