package:
  name: django-nuages-tag
  version: "0.1.3"

source:
  fn: django-nuages-tag-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-nuages-tag/django-nuages-tag-0.1.3.tar.gz
  md5: e181a90e14aa2b7756a2afe703ba8b8f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-nuages-tag = django-nuages-tag:main
    #
    # Would create an entry point called django-nuages-tag that calls django-nuages-tag.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_nuages_tag
    - django_nuages_tag.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/niconoe/django-nuages-tag/
  license: BSD licence, see LICENCE.txt
  summary: 'A simple Django TemplateTag (compute_tag_cloud) to help in the creation of tag clouds.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
