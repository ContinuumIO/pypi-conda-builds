package:
  name: django-request-tree
  version: "0.8"

source:
  fn: django-request-tree-0.8.tar.gz
  url: https://pypi.python.org/packages/source/d/django-request-tree/django-request-tree-0.8.tar.gz
  md5: d13771610c225d94a8fdec3753eb76c5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-request-tree = django-request-tree:main
    #
    # Would create an entry point called django-request-tree that calls django-request-tree.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - request_tree

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://launchpad.net/~doctormo
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'Provides dango views with input transformed into a tree structure.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
