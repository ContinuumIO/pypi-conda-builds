package:
  name: djungle-headjs
  version: "0.0.1"

source:
  fn: djungle-headjs-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/djungle-headjs/djungle-headjs-0.0.1.tar.gz
  md5: 0d2069e6bdf2422124b22d8757c2ed23
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djungle-headjs = djungle-headjs:main
    #
    # Would create an entry point called djungle-headjs that calls djungle-headjs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4

  run:
    - python
    - django >=1.4

test:
  # Python imports
  imports:
    - djungle
    - djungle.headjs

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/djungle/djungle-headjs
  license: GNU General Public License (GPL)
  summary: 'Head JS as Django app'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
