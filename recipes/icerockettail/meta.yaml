package:
  name: icerockettail
  version: "0.1.3"

source:
  fn: icerockettail-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/i/icerockettail/icerockettail-0.1.3.tar.gz
  md5: 37704a4b710ad2307a283e618f41161f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - icerockettail = icerockettail:main
    #
    # Would create an entry point called icerockettail that calls icerockettail.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests
    - argh
    - beautifulsoup4

  run:
    - python
    - requests
    - argh
    - beautifulsoup4

test:
  # Python imports
  imports:
    - icerockettail

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/psycojoker/icerockettail
  license: MIT
  summary: 'equivalent of tail -f on icerocket twitter search results'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
