package:
  name: kaiju
  version: "0.1.0"

source:
  fn: kaiju-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/k/kaiju/kaiju-0.1.0.tar.gz
  md5: a7eb5e039cdc189c88fe2ef147f1d295
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kaiju = kaiju:main
    #
    # Would create an entry point called kaiju that calls kaiju.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pymongo

  run:
    - python
    - pymongo

test:
  # Python imports
  imports:
    - kaiju

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.geekie.com.br
  license: UNKNOWN
  summary: 'Data analysis framework to log operations and interface with MongoDB.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
