package:
  name: zope.httpform
  version: "1.0.2"

source:
  fn: zope.httpform-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/z/zope.httpform/zope.httpform-1.0.2.tar.gz
  md5: df409152d1722e63f1a877addff1eea6
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zope.httpform = zope.httpform:main
    #
    # Would create an entry point called zope.httpform that calls zope.httpform.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.interface

  run:
    - python
    - setuptools
    - zope.interface

test:
  # Python imports
  imports:
    - zope
    - zope.httpform

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/zope.httpform
  license: ZPL 2.1
  summary: 'HTTP Form Data Parser'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
