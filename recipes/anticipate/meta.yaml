package:
  name: anticipate
  version: "0.7.7"

source:
  fn: anticipate-0.7.7.tar.gz
  url: https://pypi.python.org/packages/source/a/anticipate/anticipate-0.7.7.tar.gz
  md5: eb783d110356b498ff6e55b7cca6f11d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - anticipate = anticipate:main
    #
    # Would create an entry point called anticipate that calls anticipate.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - anticipate

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/six8/anticipate
  license: BSD License
  summary: 'A type checking and adapting library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
