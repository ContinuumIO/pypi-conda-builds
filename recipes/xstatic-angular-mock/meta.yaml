package:
  name: xstatic-angular-mock
  version: "1.2.1.1"

source:
  fn: XStatic-Angular-Mock-1.2.1.1.tar.gz
  url: https://pypi.python.org/packages/source/X/XStatic-Angular-Mock/XStatic-Angular-Mock-1.2.1.1.tar.gz
  md5: 94f072c39c2070f3939b619d913fb37f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - xstatic-angular-mock = xstatic-angular-mock:main
    #
    # Would create an entry point called xstatic-angular-mock that calls xstatic-angular-mock.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - xstatic
    - xstatic.pkg
    - xstatic.pkg.angular_mock

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://angularjs.org/
  license: (same as Angular-Mock)
  summary: 'Angular-Mock 1.2.1 (XStatic packaging standard)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
