package:
  name: django_qrcode_filter
  version: "0.0.14dev"

source:
  fn: django_qrcode_filter-0.0.14dev.tar.gz
  url: https://pypi.python.org/packages/source/d/django_qrcode_filter/django_qrcode_filter-0.0.14dev.tar.gz
  md5: dd7d02b6bc7454b4b3aefb5b95348dff
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_qrcode_filter = django_qrcode_filter:main
    #
    # Would create an entry point called django_qrcode_filter that calls django_qrcode_filter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_qrcode_filter
    - django_qrcode_filter.lib
    - django_qrcode_filter.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: projects.airtonix.net/project/django-qrcode-filter
  license: LICENSE.md
  summary: 'A simplet django application that provides a block and fitler templatetag to create inline DataURI based qrcode images.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
