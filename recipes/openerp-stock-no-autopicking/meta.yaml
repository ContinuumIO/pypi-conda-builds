package:
  name: openerp-stock-no-autopicking
  version: "7.0.406"

source:
  fn: openerp-stock-no-autopicking-7.0.406.tar.gz
  url: https://pypi.python.org/packages/source/o/openerp-stock-no-autopicking/openerp-stock-no-autopicking-7.0.406.tar.gz
  md5: 88de5c7f7742654c393cee2cd2da7668
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - openerp-stock-no-autopicking = openerp-stock-no-autopicking:main
    #
    # Would create an entry point called openerp-stock-no-autopicking that calls openerp-stock-no-autopicking.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - openerp-mrp

  run:
    - python
    - openerp-mrp

test:
  # Python imports
  imports:
    - openerp.addons.stock_no_autopicking

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: GNU Affero General Public License v3
  summary: 'Picking Before Manufacturing'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
