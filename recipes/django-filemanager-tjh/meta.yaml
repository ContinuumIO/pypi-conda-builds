package:
  name: django-filemanager-tjh
  version: "0.6.0"

source:
  fn: django-filemanager-tjh-0.6.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-filemanager-tjh/django-filemanager-tjh-0.6.0.tar.gz
  md5: 10c8b945a6b8c7fbdeb65be452027b9e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-filemanager-tjh = django-filemanager-tjh:main
    #
    # Would create an entry point called django-filemanager-tjh that calls django-filemanager-tjh.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.7

  run:
    - python
    - django >=1.7

test:
  # Python imports
  imports:
    - filemanager

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/tim_heap/django-filemanager
  license: UNKNOWN
  summary: 'A django filemanager app'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
