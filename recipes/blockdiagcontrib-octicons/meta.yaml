package:
  name: blockdiagcontrib-octicons
  version: "0.2.0"

source:
  fn: blockdiagcontrib-octicons-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/b/blockdiagcontrib-octicons/blockdiagcontrib-octicons-0.2.0.tar.gz
  md5: 5d153df7aa39228cda73d9c42cae933e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - blockdiagcontrib-octicons = blockdiagcontrib-octicons:main
    #
    # Would create an entry point called blockdiagcontrib-octicons that calls blockdiagcontrib-octicons.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - blockdiag >=1.4.3

  run:
    - python
    - setuptools
    - blockdiag >=1.4.3

test:
  # Python imports
  imports:
    - blockdiagcontrib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/blockdiag/blockdiag-contrib
  license: Apache Software License
  summary: 'octicons plugin for blockdiag'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
