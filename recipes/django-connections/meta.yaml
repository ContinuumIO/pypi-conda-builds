package:
  name: django-connections
  version: "0.1"

source:
  fn: django-connections-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-connections/django-connections-0.1.tar.gz
  md5: b82ec185b2cbe1157fa881aac6020a94
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-connections = django-connections:main
    #
    # Would create an entry point called django-connections that calls django-connections.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5

  run:
    - python
    - django >=1.5

test:
  # Python imports
  imports:
    - connections
    - connections.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/dfunckt/django-connections
  license: MIT License
  summary: 'Create, query and manage graphs of relationships between your Django models'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
