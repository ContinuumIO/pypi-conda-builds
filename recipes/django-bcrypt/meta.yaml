package:
  name: django-bcrypt
  version: "0.9.2"

source:
  fn: django-bcrypt-0.9.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-bcrypt/django-bcrypt-0.9.2.tar.gz
  md5: 3ea23874c4a82e1e21ad5043bb3eb1e3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-bcrypt = django-bcrypt:main
    #
    # Would create an entry point called django-bcrypt that calls django-bcrypt.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - py-bcrypt

  run:
    - python
    - py-bcrypt

test:
  # Python imports
  imports:
    - django_bcrypt

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/dwaiter/django-bcrypt/
  license: UNKNOWN
  summary: 'Make Django use bcrypt for hashing passwords.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
