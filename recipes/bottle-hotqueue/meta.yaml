package:
  name: bottle-hotqueue
  version: "0.2.5"

source:
  fn: bottle-hotqueue-0.2.5.tar.gz
  url: https://pypi.python.org/packages/source/b/bottle-hotqueue/bottle-hotqueue-0.2.5.tar.gz
  md5: 579520adec41648b570f45267a618cd2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bottle-hotqueue = bottle-hotqueue:main
    #
    # Would create an entry point called bottle-hotqueue that calls bottle-hotqueue.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/waawal/bottle_hotqueue
  license: MIT License
  summary: 'FIFO Queue for Bottle built upon HotQueue'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
