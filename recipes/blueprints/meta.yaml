package:
  name: blueprints
  version: "2.3.0.2"

source:
  fn: Blueprints-2.3.0.2.tar.gz
  url: https://pypi.python.org/packages/source/B/Blueprints/Blueprints-2.3.0.2.tar.gz
  md5: 9354fae44b19f6924d3dfbc2666d3a42
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - blueprints = blueprints:main
    #
    # Would create an entry point called blueprints that calls blueprints.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - blueprints
    - blueprints.javalib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/python-graph-lovestory/python-blueprints
  license: BSD
  summary: "Python bindings of Tinkerpop's Blueprints"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
