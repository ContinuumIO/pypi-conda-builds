package:
  name: bowerstatic
  version: "0.8.1"

source:
  fn: bowerstatic-0.8.1.zip
  url: https://pypi.python.org/packages/source/b/bowerstatic/bowerstatic-0.8.1.zip
  md5: 2d0556b0763be1dbcb74d30657b17c02
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bowerstatic = bowerstatic:main
    #
    # Would create an entry point called bowerstatic that calls bowerstatic.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - webob

  run:
    - python
    - setuptools
    - webob

test:
  # Python imports
  imports:
    - bowerstatic
    - bowerstatic.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bowerstatic.readthedocs.org
  license: BSD
  summary: 'A Bower-centric static file server for WSGI'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
