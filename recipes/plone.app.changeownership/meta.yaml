package:
  name: plone.app.changeownership
  version: "0.4"

source:
  fn: plone.app.changeownership-0.4.zip
  url: https://pypi.python.org/packages/source/p/plone.app.changeownership/plone.app.changeownership-0.4.zip
  md5: d019fc8878dd59d6784ee52297aa8779
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.app.changeownership = plone.app.changeownership:main
    #
    # Would create an entry point called plone.app.changeownership that calls plone.app.changeownership.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - plone
    - plone.app
    - plone.app.changeownership

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/plone.app.changeownership
  license: GPL
  summary: 'Change plone objects ownership'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
