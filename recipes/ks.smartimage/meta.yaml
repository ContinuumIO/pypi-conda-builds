package:
  name: ks.smartimage
  version: "1.2.2"

source:
  fn: ks.smartimage-1.2.2.tar.gz
  url: https://pypi.python.org/packages/source/k/ks.smartimage/ks.smartimage-1.2.2.tar.gz
  md5: 4e599e9b0386e787efbcc424ca18bfb4
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ks.smartimage = ks.smartimage:main
    #
    # Would create an entry point called ks.smartimage that calls ks.smartimage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pilwotk
    - ks.schema
    - zope.app.component
    - zope.app.container
    - zope.app.file
    - zope.app.folder
    - zope.app.form
    - zope.app.intid
    - zope.app.keyreference
    - zope.app.pagetemplate
    - zope.app.testing
    - zope.app.zapi
    - zope.cachedescriptors
    - zope.component
    - zope.event
    - zope.i18nmessageid
    - zope.index
    - zope.lifecycleevent
    - zope.location
    - zope.publisher
    - zope.schema
    - zope.security
    - zope.testing
    - zope.traversing

  run:
    - python
    - setuptools
    - pilwotk
    - ks.schema
    - zope.app.component
    - zope.app.container
    - zope.app.file
    - zope.app.folder
    - zope.app.form
    - zope.app.intid
    - zope.app.keyreference
    - zope.app.pagetemplate
    - zope.app.testing
    - zope.app.zapi
    - zope.cachedescriptors
    - zope.component
    - zope.event
    - zope.i18nmessageid
    - zope.index
    - zope.lifecycleevent
    - zope.location
    - zope.publisher
    - zope.schema
    - zope.security
    - zope.testing
    - zope.traversing

test:
  # Python imports
  imports:
    - ks
    - ks.smartimage
    - ks.smartimage.browser
    - ks.smartimage.smartimageadapter
    - ks.smartimage.smartimagecache
    - ks.smartimage.smartimagecache.browser
    - ks.smartimage.smartimagehandler
    - ks.smartimage.smartimageschema
    - ks.smartimage.subattributedispatcher
    - ks.smartimage.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.keysolutionscorp.com/downloads/public/pypi/ks.smartimage
  license: Zope Public License
  summary: 'Efficiently store and publish images with ks.smartimage'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
