package:
  name: nose-allure-plugin
  version: "1.0.1"

source:
  fn: nose-allure-plugin-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/n/nose-allure-plugin/nose-allure-plugin-1.0.1.tar.gz
  md5: c87eb74c51983d00e4c5c571b9b2ec03
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - nose-allure-plugin = nose-allure-plugin:main
    #
    # Would create an entry point called nose-allure-plugin that calls nose-allure-plugin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - nose
    - pytest-allure-adaptor

  run:
    - python
    - setuptools
    - nose
    - pytest-allure-adaptor

test:
  # Python imports
  imports:
    - nose_allure

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/allure-framework/allure-nose-adaptor
  license: UNKNOWN
  summary: 'Nose plugin for allure framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
