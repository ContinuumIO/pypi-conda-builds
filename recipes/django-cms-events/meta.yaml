package:
  name: django-cms-events
  version: "0.1"

source:
  fn: django-cms-events-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-cms-events/django-cms-events-0.1.tar.gz
  md5: c0dbf5cab9df084a2d83b89421d62a0c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-cms-events = django-cms-events:main
    #
    # Would create an entry point called django-cms-events that calls django-cms-events.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4,<1.6

  run:
    - python
    - django >=1.4,<1.6

test:
  # Python imports
  imports:
    - cmsplugin_events
    - cmsplugin_events.migrations
    - cmsplugin_events.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/nagyv/django-cms-events
  license: MIT License
  summary: u'A simple event scheduling app for django-cms'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
