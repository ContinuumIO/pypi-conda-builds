package:
  name: django-math-captcha
  version: "0.1"

source:
  fn: django-math-captcha-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-math-captcha/django-math-captcha-0.1.tar.gz
  md5: f3364ace0d04cf657a258b407635ca19
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-math-captcha = django-math-captcha:main
    #
    # Would create an entry point called django-math-captcha that calls django-math-captcha.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - math_captcha

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/justquick/django-math-captcha
  license: BSD License
  summary: 'Simple, secure math captcha for django forms'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
