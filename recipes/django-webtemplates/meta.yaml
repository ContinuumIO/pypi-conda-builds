package:
  name: django-webtemplates
  version: "0.1.2"

source:
  fn: django-webtemplates-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-webtemplates/django-webtemplates-0.1.2.tar.gz
  md5: f476dfb982c3409f4eade60e32896955
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-webtemplates = django-webtemplates:main
    #
    # Would create an entry point called django-webtemplates that calls django-webtemplates.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - webtemplates

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/ionata/django-webtemplates
  license: UNKNOWN
  summary: 'Load templates from an external web site.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
