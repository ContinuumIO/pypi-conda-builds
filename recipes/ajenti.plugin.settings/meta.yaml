package:
  name: ajenti.plugin.settings
  version: "0.15"

source:
  fn: ajenti.plugin.settings-0.15.tar.gz
  url: https://pypi.python.org/packages/source/a/ajenti.plugin.settings/ajenti.plugin.settings-0.15.tar.gz
  md5: 36a9df31ecb10541178d6ddc3e0f53b3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ajenti.plugin.settings = ajenti.plugin.settings:main
    #
    # Would create an entry point called ajenti.plugin.settings that calls ajenti.plugin.settings.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - aj
    - ajenti.plugin.core
    - ajenti.plugin.filesystem
    - ajenti.plugin.passwd

  run:
    - python
    - aj
    - ajenti.plugin.core
    - ajenti.plugin.filesystem
    - ajenti.plugin.passwd

test:
  # Python imports
  imports:
    - ajenti_plugin_settings

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://ajenti.org
  license: UNKNOWN
  summary: 'Settings'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
