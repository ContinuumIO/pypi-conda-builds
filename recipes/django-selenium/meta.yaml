package:
  name: django-selenium
  version: "0.9.8"

source:
  fn: django-selenium-0.9.8.tar.gz
  url: https://pypi.python.org/packages/source/d/django-selenium/django-selenium-0.9.8.tar.gz
  md5: c22b2c56287925821c4eb389a5bb51cd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-selenium = django-selenium:main
    #
    # Would create an entry point called django-selenium that calls django-selenium.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_selenium
    - django_selenium.management
    - django_selenium.management.commands
    - tests
    - tests.core
    - tests.core.management
    - tests.core.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dragoon/django-selenium/
  license: Apache Software License
  summary: 'Django Selenium Integration'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
