package:
  name: cfping
  version: "0.2.0"

source:
  fn: cfping-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/c/cfping/cfping-0.2.0.tar.gz
  md5: 9425f741c3a400e0acc8fb28fbe876ef
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cfping = cfping:main
    #
    # Would create an entry point called cfping that calls cfping.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python-cloudfiles

  run:
    - python
    - python-cloudfiles

test:
  # Python imports
  imports:
    - cfping

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/claymation/cfping
  license: BSD License
  summary: 'Test the performance and availability of the Rackspace cloudfiles service.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
