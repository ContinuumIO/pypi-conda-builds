package:
  name: jsonstruct
  version: "0.2a1"

source:
  fn: jsonstruct-0.2a1.tar.bz2
  url: https://pypi.python.org/packages/source/j/jsonstruct/jsonstruct-0.2a1.tar.bz2
  md5: 0d7e634f5577138675a1890713fea8c4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jsonstruct = jsonstruct:main
    #
    # Would create an entry point called jsonstruct that calls jsonstruct.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - jsonstruct

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/initialxy/jsonstruct
  license: BSD License
  summary: 'Python library for serializing any arbitrary object graph into JSON'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
