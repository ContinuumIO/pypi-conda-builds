package:
  name: aserializer
  version: "0.6.9"

source:
  fn: aserializer-0.6.9.tar.gz
  url: https://pypi.python.org/packages/source/a/aserializer/aserializer-0.6.9.tar.gz
  md5: e11bfc8587c1dfcd0dfb9e5c4142f48f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - aserializer = aserializer:main
    #
    # Would create an entry point called aserializer that calls aserializer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - aserializer
    - aserializer.collection
    - aserializer.django
    - aserializer.fields
    - aserializer.mongoengine
    - aserializer.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/onyg/aserializer
  license: MIT License
  summary: 'An object serializer inspired by the django forms.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
