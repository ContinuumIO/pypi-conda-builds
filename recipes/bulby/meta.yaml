package:
  name: bulby
  version: "0.0.1.dev0"

source:
  fn: bulby-0.0.1.dev0.tar.gz
  url: https://pypi.python.org/packages/source/b/bulby/bulby-0.0.1.dev0.tar.gz
  md5: fbd036310fb15477e5ab2e026985f2dd
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bulby = bulby:main
    #
    # Would create an entry point called bulby that calls bulby.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - setuptools
    - requests

test:
  # Python imports
  imports:
    - bulby

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/sontek/bulby.git
  license: MIT
  summary: 'Manages the phillips hue lightbulbs'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
