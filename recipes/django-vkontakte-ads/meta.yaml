package:
  name: django-vkontakte-ads
  version: "0.5.0"

source:
  fn: django-vkontakte-ads-0.5.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-vkontakte-ads/django-vkontakte-ads-0.5.0.tar.gz
  md5: 7a9df9dc925250f4464288296b5d233e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-vkontakte-ads = django-vkontakte-ads:main
    #
    # Would create an entry point called django-vkontakte-ads that calls django-vkontakte-ads.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-smart-selects
    - django-vkontakte-api >=0.4.12

  run:
    - python
    - django-smart-selects
    - django-vkontakte-api >=0.4.12

test:
  # Python imports
  imports:
    - vkontakte_ads
    - vkontakte_ads.management
    - vkontakte_ads.management.commands
    - vkontakte_ads.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ramusus/django-vkontakte-ads
  license: BSD License
  summary: 'Django implementation for vkontakte API Ads'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
