package:
  name: nose2django
  version: "0.1.2"

source:
  fn: nose2django-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/n/nose2django/nose2django-0.1.2.tar.gz
  md5: 928ac7e26082d4c4bdfd7b0d1342af60
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - nose2django = nose2django:main
    #
    # Would create an entry point called nose2django that calls nose2django.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - nose2django

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD License
  summary: 'Nose2 plugin to run django tests through nose2 instead of the django test runner.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
