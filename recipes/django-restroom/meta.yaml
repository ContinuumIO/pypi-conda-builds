package:
  name: django-restroom
  version: "0.0.2"

source:
  fn: django-restroom-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-restroom/django-restroom-0.0.2.tar.gz
  md5: 55abe43654c83ddac6d318fb32754ca8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-restroom = django-restroom:main
    #
    # Would create an entry point called django-restroom that calls django-restroom.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - restroom

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/suneel0101/django-restroom
  license: UNKNOWN
  summary: 'Super lightweight REST API framework for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
