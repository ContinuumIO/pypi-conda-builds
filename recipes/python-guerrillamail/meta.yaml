package:
  name: python-guerrillamail
  version: "0.1.1"

source:
  fn: python-guerrillamail-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/p/python-guerrillamail/python-guerrillamail-0.1.1.tar.gz
  md5: 9cf19d2bcdb40461270e7a4e915e3e0b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-guerrillamail = python-guerrillamail:main
    #
    # Would create an entry point called python-guerrillamail that calls python-guerrillamail.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ncjones/python-guerrillamail
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'Client for the Guerrillamail temporary email server'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
