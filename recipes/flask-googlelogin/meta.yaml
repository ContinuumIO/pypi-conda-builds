package:
  name: flask-googlelogin
  version: "0.3.1"

source:
  fn: Flask-GoogleLogin-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-GoogleLogin/Flask-GoogleLogin-0.3.1.tar.gz
  md5: bcd29610714f2bf6e4cbf527a66f1e2e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-googlelogin = flask-googlelogin:main
    #
    # Would create an entry point called flask-googlelogin that calls flask-googlelogin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests <1.0
    - flask-login >=0.2,<0.3

  run:
    - python
    - requests <1.0
    - flask-login >=0.2,<0.3

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/marksteve/flask-googlelogin
  license: MIT License
  summary: "Extends Flask-Login to use Google's OAuth2 authorization"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
