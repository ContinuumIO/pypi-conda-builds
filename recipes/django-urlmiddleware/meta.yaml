package:
  name: django-urlmiddleware
  version: "0.2.1"

source:
  fn: django-urlmiddleware-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-urlmiddleware/django-urlmiddleware-0.2.1.tar.gz
  md5: 24ebebfe68d7dee28dbb9b1b3a794713
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-urlmiddleware = django-urlmiddleware:main
    #
    # Would create an entry point called django-urlmiddleware that calls django-urlmiddleware.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - urlmiddleware
    - urlmiddleware.util

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://urlmiddleware.readthedocs.org/
  license: MIT License
  summary: 'A Django app that allows you to use a url based definition for adding middleware to a project. This means you can add middleware based on a url pattern rather than globally'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
