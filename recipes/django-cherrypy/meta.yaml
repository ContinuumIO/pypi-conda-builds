package:
  name: django-cherrypy
  version: "0.2"

source:
  fn: django-cherrypy-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-cherrypy/django-cherrypy-0.2.tar.gz
  md5: 100da3a53556fb07fe0348515f773b53
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-cherrypy = django-cherrypy:main
    #
    # Would create an entry point called django-cherrypy that calls django-cherrypy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cherrypy-wsgiserver

  run:
    - python
    - cherrypy-wsgiserver

test:
  # Python imports
  imports:
    - django_cherrypy
    - django_cherrypy.management
    - django_cherrypy.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/od-eon/django-cherrypy
  license: LICENSE
  summary: 'cherrypy, running under django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
