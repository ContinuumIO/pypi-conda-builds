package:
  name: django-vcstorage
  version: "0.1.1"

source:
  fn: django-vcstorage-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-vcstorage/django-vcstorage-0.1.1.tar.gz
  md5: a68b1c409b1d8bb188e400f352066934
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-vcstorage = django-vcstorage:main
    #
    # Would create an entry point called django-vcstorage that calls django-vcstorage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - vcstorage

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/jezdez/django-vcstorage/
  license: BSD License
  summary: 'A Django app that provides file storage backends for Mercurial, Git and Bazaar by using anyvc.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
