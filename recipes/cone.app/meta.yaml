package:
  name: cone.app
  version: "0.9.5"

source:
  fn: cone.app-0.9.5.tar.gz
  url: https://pypi.python.org/packages/source/c/cone.app/cone.app-0.9.5.tar.gz
  md5: b4b1a37961ed82d174f47605f2921290
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cone.app = cone.app:main
    #
    # Would create an entry point called cone.app that calls cone.app.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - babel
    - lingua
    - chameleon
    - node
    - node.ext.ugm
    - pyramid
    - pyramid_zcml
    - bdajax
    - cone.tile
    - yafowil
    - yafowil.yaml
    - yafowil.webob
    - repoze.workflow

  run:
    - python
    - setuptools
    - setuptools
    - babel
    - lingua
    - chameleon
    - node
    - node.ext.ugm
    - pyramid
    - pyramid_zcml
    - bdajax
    - cone.tile
    - yafowil
    - yafowil.yaml
    - yafowil.webob
    - repoze.workflow

test:
  # Python imports
  imports:
    - cone
    - cone.app
    - cone.app.browser
    - cone.app.testing

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bluedynamics/cone.app
  license: Simplified BSD
  summary: 'Web application stub'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
