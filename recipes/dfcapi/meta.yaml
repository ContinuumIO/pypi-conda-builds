package:
  name: dfcapi
  version: "1.0.0"

source:
  fn: Dfcapi-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/D/Dfcapi/Dfcapi-1.0.0.tar.gz
  md5: 87515822bc8223a73e7db25c4e53cbb3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dfcapi = dfcapi:main
    #
    # Would create an entry point called dfcapi that calls dfcapi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - unirest >=1.1.6

  run:
    - python
    - unirest >=1.1.6

test:
  # Python imports
  imports:
    - dfcapi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dfcplc/dfcapi-python
  license: LICENSE.txt
  summary: 'DFC Hosted Web Services - Python Client Library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
