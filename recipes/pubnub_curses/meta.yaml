package:
  name: pubnub_curses
  version: "1.5.2"

source:
  fn: pubnub_curses-1.5.2.tar.gz
  url: https://pypi.python.org/packages/source/p/pubnub_curses/pubnub_curses-1.5.2.tar.gz
  md5: b8112eba8e4cbea2861515905415e3fa
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pubnub_curses = pubnub_curses:main
    #
    # Would create an entry point called pubnub_curses that calls pubnub_curses.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - pubnub_curses

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pubnub.com
  license: pubrst
  summary: 'PubNub Curses Terminal Client'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
