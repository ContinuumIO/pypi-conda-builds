package:
  name: collective.loremipsum
  version: "0.10"

source:
  fn: collective.loremipsum-0.10.zip
  url: https://pypi.python.org/packages/source/c/collective.loremipsum/collective.loremipsum-0.10.zip
  md5: 0e7a17a0385b84d1437898fe51951298
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.loremipsum = collective.loremipsum:main
    #
    # Would create an entry point called collective.loremipsum that calls collective.loremipsum.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - loremipsum
    - products.cmfplone
    - plone.dexterity
    - plone.namedfile
    - plone.api

  run:
    - python
    - setuptools
    - setuptools
    - loremipsum
    - products.cmfplone
    - plone.dexterity
    - plone.namedfile
    - plone.api

test:
  # Python imports
  imports:
    - collective
    - collective.loremipsum
    - collective.loremipsum.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.loremipsum
  license: GPL
  summary: 'Creates dummy content with populated Lorem Ipsum.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
