package:
  name: django-userlog
  version: "0.1"

source:
  fn: django-userlog-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-userlog/django-userlog-0.1.tar.gz
  md5: 8a3d1461ca8c5487eaad70adc9f60773
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-userlog = django-userlog:main
    #
    # Would create an entry point called django-userlog that calls django-userlog.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - userlog

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/aaugustin/django-userlog
  license: BSD License
  summary: "Logs users' recent browsing history."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
