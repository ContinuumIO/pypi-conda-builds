package:
  name: geniatagger-python
  version: "0.1"

source:
  fn: geniatagger-python-0.1.tar.gz
  url: https://pypi.python.org/packages/source/g/geniatagger-python/geniatagger-python-0.1.tar.gz
  md5: 8599f66ef0574bd37f7391ffb7594aba
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - geniatagger-python = geniatagger-python:main
    #
    # Would create an entry point called geniatagger-python that calls geniatagger-python.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/informationsea/geniatagger-python
  license: UNKNOWN
  summary: 'Python library for GeniaTagger'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
