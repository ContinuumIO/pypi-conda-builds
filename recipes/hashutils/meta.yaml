package:
  name: hashutils
  version: "0.0.3"

source:
  fn: hashutils-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/h/hashutils/hashutils-0.0.3.tar.gz
  md5: a7d84e2795b9ef6dd77901ca266b389c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hashutils = hashutils:main
    #
    # Would create an entry point called hashutils that calls hashutils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - hashutils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/timothydmorton/hashutils
  license: GNU General Public License (GPL)
  summary: 'Utility functions for hashing arrays, combining hashes.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
