package:
  name: arcgis-rest-query
  version: "0.0.11"

source:
  fn: arcgis-rest-query-0.0.11.tar.gz
  url: https://pypi.python.org/packages/source/a/arcgis-rest-query/arcgis-rest-query-0.0.11.tar.gz
  md5: d3d1d8ec551414ba0db57d515168578a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - arcgis-rest-query = arcgis-rest-query:main
    #
    # Would create an entry point called arcgis-rest-query that calls arcgis-rest-query.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - argparse >=1.2.1
    - requests >=2.4.3

  run:
    - python
    - argparse >=1.2.1
    - requests >=2.4.3

test:
  # Python imports
  imports:
    - arcgis

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Schwanksta/python-arcgis-rest-query
  license: MIT
  summary: 'A tool to download a layer from an ArcGIS web service as GeoJSON'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
