package:
  name: labtools
  version: "0.0.3"

source:
  fn: labtools-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/l/labtools/labtools-0.0.3.tar.gz
  md5: 5b4e7c85ba83001c5fe0bab6f884c6dc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - labtools = labtools:main
    #
    # Would create an entry point called labtools that calls labtools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pandas
    - numpy

  run:
    - python
    - pandas
    - numpy

test:
  # Python imports
  imports:
    - labtools

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/pedmiston/labtools
  license: GNU GPL
  summary: 'Resources for running behavioral experiments'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
