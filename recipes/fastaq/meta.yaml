package:
  name: fastaq
  version: "0.30"

source:
  fn: fastaq-0.30.tar.gz
  url: https://pypi.python.org/packages/source/f/fastaq/fastaq-0.30.tar.gz
  md5: 169d5a8e043e9905d2e97f4c49a6180f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fastaq = fastaq:main
    #
    # Would create an entry point called fastaq that calls fastaq.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - fastAQ
    - fastAQ.commandLineTools
    - fastaq

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Jverma/fastAQ
  license: MIT
  summary: 'fastAQ is a very and super lightweight package for working with FASTA/FASTQ sequences'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
