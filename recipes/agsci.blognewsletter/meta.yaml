package:
  name: agsci.blognewsletter
  version: "0.4"

source:
  fn: agsci.blognewsletter-0.4.tar.gz
  url: https://pypi.python.org/packages/source/a/agsci.blognewsletter/agsci.blognewsletter-0.4.tar.gz
  md5: 53fcb8df40c15d8e3a835a918d252885
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - agsci.blognewsletter = agsci.blognewsletter:main
    #
    # Would create an entry point called agsci.blognewsletter that calls agsci.blognewsletter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - products.cmfplone
    - setuptools
    - premailer
    - plone.app.registry
    - beautifulsoup
    - archetypes.schemaextender
    - collective.monkeypatcher

  run:
    - python
    - setuptools
    - products.cmfplone
    - setuptools
    - premailer
    - plone.app.registry
    - beautifulsoup
    - archetypes.schemaextender
    - collective.monkeypatcher

test:
  # Python imports
  imports:
    - agsci
    - agsci.blognewsletter
    - agsci.blognewsletter.browser
    - agsci.blognewsletter.content
    - agsci.blognewsletter.portlet
    - agsci.blognewsletter.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://agsci.psu.edu/
  license: GPL
  summary: 'Plone blog/newsletter package based on the PSU AgSci products.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
