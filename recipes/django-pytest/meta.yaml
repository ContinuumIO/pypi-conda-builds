package:
  name: django-pytest
  version: "0.2.0"

source:
  fn: django-pytest-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-pytest/django-pytest-0.2.0.tar.gz
  md5: f44f8792994501c37efaf7d44b7baf4e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-pytest = django-pytest:main
    #
    # Would create an entry point called django-pytest that calls django-pytest.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_pytest
    - django_pytest.management
    - django_pytest.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/buchuki/django-pytest
  license: LICENSE.txt
  summary: 'django test runner to use py.test tests'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
