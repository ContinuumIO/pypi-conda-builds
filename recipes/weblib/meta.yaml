package:
  name: weblib
  version: "0.1.13"

source:
  fn: weblib-0.1.13.tar.gz
  url: https://pypi.python.org/packages/source/w/weblib/weblib-0.1.13.tar.gz
  md5: 799dde20d666e0ac0663639a284c493d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - weblib = weblib:main
    #
    # Would create an entry point called weblib that calls weblib.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pytils
    - six
    - lxml

  run:
    - python
    - pytils
    - six
    - lxml

test:
  # Python imports
  imports:
    - test
    - weblib
    - weblib.export

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT License
  summary: 'Set of tools for web scraping projects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
