package:
  name: django-extra_keywords
  version: "1.1.2"

source:
  fn: django-extra_keywords-1.1.2.zip
  url: https://pypi.python.org/packages/source/d/django-extra_keywords/django-extra_keywords-1.1.2.zip
  md5: 1c1784861f8a3e1711e0339b086d33b2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-extra_keywords = django-extra_keywords:main
    #
    # Would create an entry point called django-extra_keywords that calls django-extra_keywords.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4

  run:
    - python
    - django >=1.4

test:
  # Python imports
  imports:
    - extra_keywords
    - extra_keywords.management
    - extra_keywords.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/tkhyn/django-extra_keywords
  license: MIT License
  summary: 'Extra gettext keywords handling in Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
