package:
  name: ftw.tooltip
  version: "1.1.4"

source:
  fn: ftw.tooltip-1.1.4.zip
  url: https://pypi.python.org/packages/source/f/ftw.tooltip/ftw.tooltip-1.1.4.zip
  md5: d28a7f8369ccf1c4904648cf84d9caab
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ftw.tooltip = ftw.tooltip:main
    #
    # Would create an entry point called ftw.tooltip that calls ftw.tooltip.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - ftw.upgrade
    - zope2
    - zope.component
    - zope.i18n
    - zope.i18nmessageid
    - zope.interface
    - products.cmfplone

  run:
    - python
    - setuptools
    - setuptools
    - ftw.upgrade
    - zope2
    - zope.component
    - zope.i18n
    - zope.i18nmessageid
    - zope.interface
    - products.cmfplone

test:
  # Python imports
  imports:
    - ftw
    - ftw.tooltip
    - ftw.tooltip.browser
    - ftw.tooltip.tests
    - ftw.tooltip.upgrades

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/4teamwork/ftw.tooltip
  license: GNU General Public License (GPL)
  summary: 'Apply tooltips dynamically'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
