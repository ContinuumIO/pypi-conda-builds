package:
  name: django-galleries
  version: "1.4.0"

source:
  fn: django-galleries-1.4.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-galleries/django-galleries-1.4.0.tar.gz
  md5: f9e6521f3632500e47e2f83689481c9e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-galleries = django-galleries:main
    #
    # Would create an entry point called django-galleries that calls django-galleries.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-imagekit >=0.3.6
    - django >=1.6

  run:
    - python
    - django-imagekit >=0.3.6
    - django >=1.6

test:
  # Python imports
  imports:
    - exampleapp
    - galleries
    - galleries.migrations
    - galleries.south_migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD License
  summary: 'Simple Django galleries.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
