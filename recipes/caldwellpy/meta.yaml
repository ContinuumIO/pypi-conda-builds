package:
  name: caldwellpy
  version: "1.6.3"

source:
  fn: caldwellpy-1.6.3.tar.gz
  url: https://pypi.python.org/packages/source/c/caldwellpy/caldwellpy-1.6.3.tar.gz
  md5: 4bb505a6cb3dda2a15d582d0f6e1ac33
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - caldwellpy = caldwellpy:main
    #
    # Would create an entry point called caldwellpy that calls caldwellpy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django ==1.4.5
    - coverage ==3.6
    - mock ==1.0.1
    - nose ==1.3.0
    - requests
    - wsgiref ==0.1.2

  run:
    - python
    - django ==1.4.5
    - coverage ==3.6
    - mock ==1.0.1
    - nose ==1.3.0
    - requests
    - wsgiref ==0.1.2

test:
  # Python imports
  imports:
    - caldwellpy
    - pyutil
    - pyutil.django
    - pyutil.django.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mattcaldwell/pyutil
  license: UNKNOWN
  summary: 'Python Utilities Collection'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
