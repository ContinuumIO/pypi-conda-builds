package:
  name: forgehg
  version: "0.2.3"

source:
  fn: ForgeHg-0.2.3.tar.gz
  url: https://pypi.python.org/packages/source/F/ForgeHg/ForgeHg-0.2.3.tar.gz
  md5: 819f1b6f8e3c48cea5dd98014848dd2f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - forgehg = forgehg:main
    #
    # Would create an entry point called forgehg that calls forgehg.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - allura
    - mercurial ==3.0

  run:
    - python
    - setuptools
    - allura
    - mercurial ==3.0

test:
  # Python imports
  imports:
    - forgehg
    - forgehg.model
    - forgehg.templates

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://sourceforge.net/p/forgehg
  license: GNU General Public License v2 (GPLv2)
  summary: 'Mercurial (Hg) SCM support for Apache Allura'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
