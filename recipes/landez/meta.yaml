package:
  name: landez
  version: "2.3.0"

source:
  fn: landez-2.3.0.zip
  url: https://pypi.python.org/packages/source/l/landez/landez-2.3.0.zip
  md5: b0ae5f3fbdcc81b7a7bc42283da6db2a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - landez = landez:main
    #
    # Would create an entry point called landez that calls landez.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - mbutil

  run:
    - python
    - mbutil

test:
  # Python imports
  imports:
    - landez

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/makinacorpus/landez/
  license: LPGL, see LICENSE file.
  summary: 'Landez is a python toolbox to manipulate map tiles.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
