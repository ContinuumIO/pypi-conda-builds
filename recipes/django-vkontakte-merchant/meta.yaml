package:
  name: django-vkontakte-merchant
  version: "0.1"

source:
  fn: django-vkontakte-merchant-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-vkontakte-merchant/django-vkontakte-merchant-0.1.tar.gz
  md5: 00acfd77b5b1bf6a25149bfcfcd51ab9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-vkontakte-merchant = django-vkontakte-merchant:main
    #
    # Would create an entry point called django-vkontakte-merchant that calls django-vkontakte-merchant.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - merchant_api
    - merchant_api.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/kmike/django-vkontakte-merchant/
  license: MIT License
  summary: 'Vkontakte Merchant API django app'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
