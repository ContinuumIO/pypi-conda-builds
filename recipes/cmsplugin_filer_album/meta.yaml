package:
  name: cmsplugin_filer_album
  version: "0.0.5"

source:
  fn: cmsplugin_filer_album-0.0.5.tar.gz
  url: https://pypi.python.org/packages/source/c/cmsplugin_filer_album/cmsplugin_filer_album-0.0.5.tar.gz
  md5: f51354dc9dbe5d08d2ea2780862e777b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cmsplugin_filer_album = cmsplugin_filer_album:main
    #
    # Would create an entry point called cmsplugin_filer_album that calls cmsplugin_filer_album.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3
    - django-cms >=2.2
    - django-sekizai >=0.4.2
    - easy_thumbnails >=1.0

  run:
    - python
    - django >=1.3
    - django-cms >=2.2
    - django-sekizai >=0.4.2
    - easy_thumbnails >=1.0

test:
  # Python imports
  imports:
    - cmsplugin_filer_album

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/ffaxl/cmsplugin-imagealbum
  license: GPL
  summary: 'DjangoCMS filer plugin for show albums with template selection'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
