package:
  name: guifi-nagios
  version: "0.1"

source:
  fn: guifi-nagios-0.1.tar.gz
  url: https://pypi.python.org/packages/source/g/guifi-nagios/guifi-nagios-0.1.tar.gz
  md5: 98073dd642a6f6c54475a0bb4978868a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - guifi-nagios = guifi-nagios:main
    #
    # Would create an entry point called guifi-nagios that calls guifi-nagios.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - paramiko
    - nagiosplugin

  run:
    - python
    - paramiko
    - nagiosplugin

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://lagunak.gisa-elkartea.org/projects/guifinagios/
  license: GPLv3+
  summary: 'Nagios helpers for guifi.net networks'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
