package:
  name: amqp_worker
  version: "0.2"

source:
  fn: amqp_worker-0.2.tar.gz
  url: https://pypi.python.org/packages/source/a/amqp_worker/amqp_worker-0.2.tar.gz
  md5: 53e8bf8ff8bfe14dad9b776cd513b943
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - amqp_worker = amqp_worker:main
    #
    # Would create an entry point called amqp_worker that calls amqp_worker.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pika >=0.9.6
    - python-daemon >=1.5.5

  run:
    - python
    - pika >=0.9.6
    - python-daemon >=1.5.5

test:
  # Python imports
  imports:
    - amqp_worker

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bboe/amqp_worker
  license: BSD License
  summary: 'A python module for writing workers (daemons) triggered from amqp jobs.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
