package:
  name: medialog.lxml
  version: "0.3.1"

source:
  fn: medialog.lxml-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/m/medialog.lxml/medialog.lxml-0.3.1.tar.gz
  md5: 75bbf0a04e28c732b60f225a79e762bd
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - medialog.lxml = medialog.lxml:main
    #
    # Would create an entry point called medialog.lxml that calls medialog.lxml.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - collective.z3cform.datagridfield
    - cssselect
    - medialog.controlpanel
    - plone.api
    - plone.directives.form
    - requests
    - plone.behavior

  run:
    - python
    - setuptools
    - collective.z3cform.datagridfield
    - cssselect
    - medialog.controlpanel
    - plone.api
    - plone.directives.form
    - requests
    - plone.behavior

test:
  # Python imports
  imports:
    - medialog
    - medialog.lxml

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/espenmn/medialog.lxml
  license: gpl
  summary: 'A Product to embed content with lxml'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
