package:
  name: clearfinder
  version: "0.1.7"

source:
  fn: ClearFinder-0.1.7.tar.gz
  url: https://pypi.python.org/packages/source/C/ClearFinder/ClearFinder-0.1.7.tar.gz
  md5: f2768ebb15e11801290486d8d6f70422
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - clearfinder = clearfinder:main
    #
    # Would create an entry point called clearfinder that calls clearfinder.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - clearfinder

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: GPLv3
  summary: 'Identify clear measurements from solar irradiation data'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
