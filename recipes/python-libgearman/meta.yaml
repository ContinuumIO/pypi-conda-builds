package:
  name: python-libgearman
  version: "0.13.2"

source:
  fn: python-libgearman-0.13.2.tar.gz
  url: https://pypi.python.org/packages/source/p/python-libgearman/python-libgearman-0.13.2.tar.gz
  md5: 0c8d9a725a203ccd70f17a8062178948
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-libgearman = python-libgearman:main
    #
    # Would create an entry point called python-libgearman that calls python-libgearman.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - gearman

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://launchpad.net/gearman-interface
  license: BSD License
  summary: 'Python wrapper of libgearman'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
