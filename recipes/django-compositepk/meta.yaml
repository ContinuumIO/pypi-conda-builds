package:
  name: django-compositepk
  version: "1.0"

source:
  fn: django-compositepk-1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-compositepk/django-compositepk-1.0.tar.gz
  md5: 5d3f00af3b5cafa9240eb04616772fdb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-compositepk = django-compositepk:main
    #
    # Would create an entry point called django-compositepk that calls django-compositepk.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - composite_pk

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/smileychris/django-compositepk/
  license: MIT License
  summary: 'Provides a base model with rudimentary composite PK abilities.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
