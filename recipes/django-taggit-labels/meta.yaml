package:
  name: django-taggit-labels
  version: "0.2.0"

source:
  fn: django-taggit-labels-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-taggit-labels/django-taggit-labels-0.2.0.tar.gz
  md5: 50fdd8be4ec82227e1e0ef012cda2abc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-taggit-labels = django-taggit-labels:main
    #
    # Would create an entry point called django-taggit-labels that calls django-taggit-labels.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4
    - django-taggit

  run:
    - python
    - django >=1.4
    - django-taggit

test:
  # Python imports
  imports:
    - taggit_labels

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bennylope/django-taggit-labels
  license: BSD License
  summary: 'Alternate labels for django-taggit'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
