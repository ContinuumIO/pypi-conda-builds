package:
  name: appdotnet
  version: "0.1.3"

source:
  fn: appdotnet-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/a/appdotnet/appdotnet-0.1.3.tar.gz
  md5: b9e203abcabf36ad9e94c479bc4c80f0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - appdotnet = appdotnet:main
    #
    # Would create an entry point called appdotnet that calls appdotnet.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests
    - python-dateutil

  run:
    - python
    - requests
    - python-dateutil

test:
  # Python imports
  imports:
    - appdotnet

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/briancline/appdotnet
  license: BSD License
  summary: 'A library for interacting with the App.net API.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
