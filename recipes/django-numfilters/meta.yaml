package:
  name: django-numfilters
  version: "0.1.1"

source:
  fn: django-numfilters-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-numfilters/django-numfilters-0.1.1.tar.gz
  md5: b615e16bfe3a4151914ba90a050ba1a0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-numfilters = django-numfilters:main
    #
    # Would create an entry point called django-numfilters that calls django-numfilters.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - django_numfilters
    - django_numfilters.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/amatellanes/django-numfilters
  license: MIT License
  summary: 'Mathematical template filters for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
