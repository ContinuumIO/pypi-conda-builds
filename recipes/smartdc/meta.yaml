package:
  name: smartdc
  version: "0.2.0"

source:
  fn: smartdc-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/s/smartdc/smartdc-0.2.0.tar.gz
  md5: d2d7697da44f77128b9036d76f4577f4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - smartdc = smartdc:main
    #
    # Would create an entry point called smartdc that calls smartdc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests
    - http-signature

  run:
    - python
    - requests
    - http-signature

test:
  # Python imports
  imports:
    - smartdc

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/atl/py-smartdc
  license: MIT License
  summary: 'Joyent SmartDataCenter CloudAPI connector using http-signature authentication via Requests'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
