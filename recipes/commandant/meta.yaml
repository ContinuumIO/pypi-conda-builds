package:
  name: commandant
  version: "0.5.0"

source:
  fn: commandant-0.5.0.tar.gz
  url: https://pypi.python.org/packages/source/c/commandant/commandant-0.5.0.tar.gz
  md5: cab3202361de199a070615140914d38d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - commandant = commandant:main
    #
    # Would create an entry point called commandant that calls commandant.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - commandant
    - commandant.testing

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jkakar/commandant
  license: GNU General Public License (GPL)
  summary: 'Commandant is a toolkit for building command-oriented tools.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
