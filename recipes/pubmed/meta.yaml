package:
  name: pubmed
  version: "0.0.1.post0"

source:
  fn: pubmed-0.0.1.post0.tar.gz
  url: https://pypi.python.org/packages/source/p/pubmed/pubmed-0.0.1.post0.tar.gz
  md5: fc952a621ead87bb1f732753d77fbe5d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pubmed = pubmed:main
    #
    # Would create an entry point called pubmed that calls pubmed.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - beautifulsoup4
    - pyquery

  run:
    - python
    - setuptools
    - beautifulsoup4
    - pyquery

test:
  # Python imports
  imports:
    - pubmed

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/twz915/pubmed
  license: MIT License
  summary: 'Get literature information via PubMed ID'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
