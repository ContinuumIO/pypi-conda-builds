package:
  name: django-salmonella
  version: "0.6.1"

source:
  fn: django-salmonella-0.6.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-salmonella/django-salmonella-0.6.1.tar.gz
  md5: 009554fd952a34f0f815e859b71f8f55
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-salmonella = django-salmonella:main
    #
    # Would create an entry point called django-salmonella that calls django-salmonella.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - salmonella

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/lincolnloop/django-salmonella/
  license: MIT License
  summary: "raw_id_fields widget replacement that handles display of an object's string value on change and can be overridden via a template."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
