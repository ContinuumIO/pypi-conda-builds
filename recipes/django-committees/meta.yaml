package:
  name: django-committees
  version: "1.1.3"

source:
  fn: django-committees-1.1.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-committees/django-committees-1.1.3.tar.gz
  md5: fcffd30ee35c24aa04e4b5508ae15938
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-committees = django-committees:main
    #
    # Would create an entry point called django-committees that calls django-committees.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-markup-mixin
    - django-extensions
    - simple_history
    - django-photologue
    - django-eventy

  run:
    - python
    - django-markup-mixin
    - django-extensions
    - simple_history
    - django-photologue
    - django-eventy

test:
  # Python imports
  imports:
    - committees

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/powellc/django-committees
  license: BSD License
  summary: 'A simple reusable application for managing a small orgs governance in a Django application.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
