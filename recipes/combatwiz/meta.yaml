package:
  name: combatwiz
  version: "0.12"

source:
  fn: CombatWiz-0.12.tar.gz
  url: https://pypi.python.org/packages/source/C/CombatWiz/CombatWiz-0.12.tar.gz
  md5: 9e32851ff1ddc6d391e90c903ef32e22
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - combatwiz = combatwiz:main
    #
    # Would create an entry point called combatwiz that calls combatwiz.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - appdirs >=1.1.0
    - pytester
    - tox
    - unittest2

  run:
    - python
    - appdirs >=1.1.0
    - pytester
    - tox
    - unittest2

test:
  # Python imports
  imports:
    - combatwiz

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/kenfar/CombatSim
  license: BSD License
  summary: 'A combat simulator'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
