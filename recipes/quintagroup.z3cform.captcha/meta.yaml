package:
  name: quintagroup.z3cform.captcha
  version: "1.0.3"

source:
  fn: quintagroup.z3cform.captcha-1.0.3.zip
  url: https://pypi.python.org/packages/source/q/quintagroup.z3cform.captcha/quintagroup.z3cform.captcha-1.0.3.zip
  md5: 3e1ff42fd073eb7e61bd1127e1a58278
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - quintagroup.z3cform.captcha = quintagroup.z3cform.captcha:main
    #
    # Would create an entry point called quintagroup.z3cform.captcha that calls quintagroup.z3cform.captcha.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - quintagroup.captcha.core >=0.4.3
    - z3c.form

  run:
    - python
    - setuptools
    - setuptools
    - quintagroup.captcha.core >=0.4.3
    - z3c.form

test:
  # Python imports
  imports:
    - quintagroup
    - quintagroup.z3cform
    - quintagroup.z3cform.captcha

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.quintagroup.com/products/quintagroup.z3cform.captcha/
  license: GNU General Public License (GPL)
  summary: 'Captcha field for z3cform based on quintagroup.captcha.core package'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
