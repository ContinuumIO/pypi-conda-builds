package:
  name: django-cart
  version: "1.0.3"

source:
  fn: django-cart-1.0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-cart/django-cart-1.0.3.tar.gz
  md5: 535c7bd81c31dc4e081e421b8401f88c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-cart = django-cart:main
    #
    # Would create an entry point called django-cart that calls django-cart.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - cart
    - cart.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bmentges/django-cart
  license: BSD License
  summary: 'Django simple shopping cart, tests and south migrations included'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
