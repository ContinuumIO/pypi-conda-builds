package:
  name: django-usda-mongo
  version: "0.1.0"

source:
  fn: django-usda-mongo-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-usda-mongo/django-usda-mongo-0.1.0.tar.gz
  md5: a7b3b9a455428dd8019910b4800ebcfe
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-usda-mongo = django-usda-mongo:main
    #
    # Would create an entry point called django-usda-mongo that calls django-usda-mongo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - usda_mongo

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/javipalanca/django-usda-mongo
  license: BSD License
  summary: 'Import and map the USDA National Nutrient Database for Standard Reference (SR22) to Django models using MongoDB with mongoengine'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
