package:
  name: django-emailpost
  version: "0.0.7"

source:
  fn: django-emailpost-0.0.7.tar.gz
  url: https://pypi.python.org/packages/source/d/django-emailpost/django-emailpost-0.0.7.tar.gz
  md5: 4a845da39bebc5fd8383607b755402a9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-emailpost = django-emailpost:main
    #
    # Would create an entry point called django-emailpost that calls django-emailpost.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pillow
    - python-twitter
    - bitlyapi

  run:
    - python
    - pillow
    - python-twitter
    - bitlyapi

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jk0/django-emailpost
  license: Apache Software License
  summary: 'Post to Django blogs via email.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
