package:
  name: django-glinks
  version: "0.2.3"

source:
  fn: django-glinks-0.2.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-glinks/django-glinks-0.2.3.tar.gz
  md5: 74c3525155510ecaba95328b7406bb0c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-glinks = django-glinks:main
    #
    # Would create an entry point called django-glinks that calls django-glinks.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - glinks
    - glinks.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/django-glinks/
  license: LICENSE.txt (CC-BY-NC)
  summary: 'Interal add manager for django sites with ads not blocked by ad blocker.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
