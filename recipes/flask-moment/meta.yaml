package:
  name: flask-moment
  version: "0.4.0"

source:
  fn: Flask-Moment-0.4.0.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Moment/Flask-Moment-0.4.0.tar.gz
  md5: bfbcabb4d4a23c3c084f08f44d8e891d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-moment = flask-moment:main
    #
    # Would create an entry point called flask-moment that calls flask-moment.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask

  run:
    - python
    - flask

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/miguelgrinberg/flask-moment/
  license: BSD License
  summary: 'Formatting of dates and times in Flask templates using moment.js.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
