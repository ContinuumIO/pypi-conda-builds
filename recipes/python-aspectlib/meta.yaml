package:
  name: python-aspectlib
  version: "0.0"

source:
  fn: python-aspectlib-0.0.tar.gz
  url: https://pypi.python.org/packages/source/p/python-aspectlib/python-aspectlib-0.0.tar.gz
  md5: dda87d1098c4b3c4e73f0b8564124fcc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-aspectlib = python-aspectlib:main
    #
    # Would create an entry point called python-aspectlib that calls python-aspectlib.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - aspectlib

  run:
    - python
    - aspectlib

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://pypi.python.org/pypi/aspectlib/
  license: UNKNOWN
  summary: 'Aspect-Oriented Programming toolkit.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
