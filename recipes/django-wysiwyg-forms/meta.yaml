package:
  name: django-wysiwyg-forms
  version: "0.1.1"

source:
  fn: django-wysiwyg-forms-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-wysiwyg-forms/django-wysiwyg-forms-0.1.1.tar.gz
  md5: 29799d2feb29e22fd5d5384940a80428
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-wysiwyg-forms = django-wysiwyg-forms:main
    #
    # Would create an entry point called django-wysiwyg-forms that calls django-wysiwyg-forms.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django ==1.3

  run:
    - python
    - django ==1.3

test:
  # Python imports
  imports:
    - wysiwyg_forms

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/fitzgen/django-wysiwyg-forms
  license: UNKNOWN
  summary: 'A What You See Is What You Get form editor for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
