package:
  name: products.rfc822addressfieldvalidator
  version: "1.0b"

source:
  fn: Products.RFC822AddressFieldValidator-1.0b.tar.gz
  url: https://pypi.python.org/packages/source/P/Products.RFC822AddressFieldValidator/Products.RFC822AddressFieldValidator-1.0b.tar.gz
  md5: f7628ebfef12975a4e135a5d07c5c49c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - products.rfc822addressfieldvalidator = products.rfc822addressfieldvalidator:main
    #
    # Would create an entry point called products.rfc822addressfieldvalidator that calls products.rfc822addressfieldvalidator.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - Products
    - Products.RFC822AddressFieldValidator

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.nidelven-it.no/d
  license: GPL
  summary: 'Validator for complex (multiple address) RFC822 header fields'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
