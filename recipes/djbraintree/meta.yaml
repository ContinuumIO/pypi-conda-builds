package:
  name: djbraintree
  version: "0.1.0"

source:
  fn: djbraintree-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/djbraintree/djbraintree-0.1.0.tar.gz
  md5: 33af6d114196e3da88aa45a7521f4f33
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djbraintree = djbraintree:main
    #
    # Would create an entry point called djbraintree that calls djbraintree.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - braintree
    - django

  run:
    - python
    - braintree
    - django

test:
  # Python imports
  imports:
    - djbraintree

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/iconfinder/djbraintree
  license: BSD License
  summary: 'Braintree for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
