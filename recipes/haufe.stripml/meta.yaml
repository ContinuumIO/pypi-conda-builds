package:
  name: haufe.stripml
  version: "1.2.2"

source:
  fn: haufe.stripml-1.2.2.tar.gz
  url: https://pypi.python.org/packages/source/h/haufe.stripml/haufe.stripml-1.2.2.tar.gz
  md5: bdbddeef52c80f83c299fb25d502cfab
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - haufe.stripml = haufe.stripml:main
    #
    # Would create an entry point called haufe.stripml that calls haufe.stripml.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - haufe
    - haufe.stripml

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/haufe.stripml
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Python extension for removing markup from strings.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
