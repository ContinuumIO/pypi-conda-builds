package:
  name: django-genshi
  version: "1.1.3"

source:
  fn: django-genshi-1.1.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-genshi/django-genshi-1.1.3.tar.gz
  md5: e92c90aab1fc6618791736039d986a3c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-genshi = django-genshi:main
    #
    # Would create an entry point called django-genshi that calls django-genshi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.0_final
    - genshi >=0.5.1

  run:
    - python
    - django >=1.0_final
    - genshi >=0.5.1

test:
  # Python imports
  imports:
    - django_genshi
    - django_genshi.loaders

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://launchpad.net/django-genshi
  license: GNU General Public License (GPL)
  summary: 'Django integration for Genshi'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
