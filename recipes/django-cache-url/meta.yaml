package:
  name: django-cache-url
  version: "0.8.0"

source:
  fn: django-cache-url-0.8.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-cache-url/django-cache-url-0.8.0.tar.gz
  md5: afe466d46058b35a91caf4b456a54278
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-cache-url = django-cache-url:main
    #
    # Would create an entry point called django-cache-url that calls django-cache-url.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/ghickman/django-cache-url
  license: MIT License
  summary: 'Use Cache URLs in your Django application.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
