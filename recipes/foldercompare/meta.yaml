package:
  name: foldercompare
  version: "1.0.1"

source:
  fn: foldercompare-1.0.1.zip
  url: https://pypi.python.org/packages/source/f/foldercompare/foldercompare-1.0.1.zip
  md5: 9c91f754c27e7ded0b78c3fef4dad244
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - foldercompare = foldercompare:main
    #
    # Would create an entry point called foldercompare that calls foldercompare.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - foldercompare

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/zentai/folder-compare
  license: UNKNOWN
  summary: 'just a easy tools to compare 2 folder and generated a files changed list for you.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
