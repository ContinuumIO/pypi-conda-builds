package:
  name: django-bootstrap-form-horizontal
  version: "0.0.4"

source:
  fn: django-bootstrap-form-horizontal-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-bootstrap-form-horizontal/django-bootstrap-form-horizontal-0.0.4.tar.gz
  md5: 809eafa58963a0be89ebf90abe63b619
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-bootstrap-form-horizontal = django-bootstrap-form-horizontal:main
    #
    # Would create an entry point called django-bootstrap-form-horizontal that calls django-bootstrap-form-horizontal.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - bootstrap_form_horizontal
    - bootstrap_form_horizontal.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/yourcelf/django-bootstrap-form-horizontal
  license: BSD License
  summary: 'Templates for bootstrap3 style horizontal forms in Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
