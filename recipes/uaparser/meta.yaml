package:
  name: uaparser
  version: "0.1.0"

source:
  fn: uaparser-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/u/uaparser/uaparser-0.1.0.tar.gz
  md5: 4ce7fabbfdeea3f5f61ab9813845e334
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - uaparser = uaparser:main
    #
    # Would create an entry point called uaparser that calls uaparser.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - uaparser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ojarva/ua-detection-python
  license: MIT License
  summary: 'Library for parsing browser user agents'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
