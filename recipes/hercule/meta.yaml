package:
  name: hercule
  version: "1.1.1"

source:
  fn: hercule-1.1.1.tar.gz
  url: https://pypi.python.org/packages/source/h/hercule/hercule-1.1.1.tar.gz
  md5: 8ec64aad1c68235b2ba3c2f16e04586c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hercule = hercule:main
    #
    # Would create an entry point called hercule that calls hercule.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - hercule

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/Greedoid
  license: UNKNOWN
  summary: 'Easy-to-use python wrapper for the Riot Games League of Legends API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
