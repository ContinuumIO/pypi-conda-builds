package:
  name: fui.locker
  version: "1.3.0"

source:
  fn: fui.locker-1.3.0.tar.gz
  url: https://pypi.python.org/packages/source/f/fui.locker/fui.locker-1.3.0.tar.gz
  md5: 1f59481808b2792c28ef04865b96cb90
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fui.locker = fui.locker:main
    #
    # Would create an entry point called fui.locker that calls fui.locker.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - fui
    - fui.locker
    - fui.locker.browser
    - fui.locker.content
    - fui.locker.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/espenak/fui.locker
  license: GPL
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
