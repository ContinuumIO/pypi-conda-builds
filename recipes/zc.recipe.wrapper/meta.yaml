package:
  name: zc.recipe.wrapper
  version: "1.1.0"

source:
  fn: zc.recipe.wrapper-1.1.0.tar.gz
  url: https://pypi.python.org/packages/source/z/zc.recipe.wrapper/zc.recipe.wrapper-1.1.0.tar.gz
  md5: ad6d19e4d8be699b6abbb544bcf9596a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zc.recipe.wrapper = zc.recipe.wrapper:main
    #
    # Would create an entry point called zc.recipe.wrapper that calls zc.recipe.wrapper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.recipe.testrunner
    - zope.testing

  run:
    - python
    - setuptools
    - setuptools
    - zc.recipe.testrunner
    - zope.testing

test:
  # Python imports
  imports:
    - zc
    - zc.recipe
    - zc.recipe.wrapper

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Zope Public License
  summary: 'Recipe that creates shell wrapper scripts'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
