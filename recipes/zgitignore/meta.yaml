package:
  name: zgitignore
  version: "0.7"

source:
  fn: zgitignore-0.7.tar.gz
  url: https://pypi.python.org/packages/source/z/zgitignore/zgitignore-0.7.tar.gz
  md5: afd0374a3a760c60f738054643cd2870
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zgitignore = zgitignore:main
    #
    # Would create an entry point called zgitignore that calls zgitignore.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - zgitignore

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/zb3/zgitignore
  license: MIT License
  summary: 'Check if a file is ignored by a .zgitignore file, compatible with .gitignore syntax'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
