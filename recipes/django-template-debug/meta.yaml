package:
  name: django-template-debug
  version: "0.3.5"

source:
  fn: django-template-debug-0.3.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-template-debug/django-template-debug-0.3.5.tar.gz
  md5: 7eb0bc0f2fcbf820bf10ce9efa875551
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-template-debug = django-template-debug:main
    #
    # Would create an entry point called django-template-debug that calls django-template-debug.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - example
    - template_debug
    - template_debug.templatetags
    - template_debug.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/calebsmith/django-template-debug
  license: BSD License
  summary: 'A small collection of template tags for debugging and introspecting templates'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
