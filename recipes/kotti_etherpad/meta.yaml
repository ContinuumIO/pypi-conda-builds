package:
  name: kotti_etherpad
  version: "0.4"

source:
  fn: kotti_etherpad-0.4.zip
  url: https://pypi.python.org/packages/source/k/kotti_etherpad/kotti_etherpad-0.4.zip
  md5: b6cf8e072a095461ba0c4203b15fdf87
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kotti_etherpad = kotti_etherpad:main
    #
    # Would create an entry point called kotti_etherpad that calls kotti_etherpad.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - kotti

  run:
    - python
    - setuptools
    - kotti

test:
  # Python imports
  imports:
    - kotti_etherpad
    - kotti_etherpad.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/kotti_etherpad/
  license: BSD-derived (http://www.repoze.org/LICENSE.txt)
  summary: 'Etherpad integration into your Kotti site'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
