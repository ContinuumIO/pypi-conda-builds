package:
  name: climson
  version: "0.1.3"

source:
  fn: climson-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/c/climson/climson-0.1.3.tar.gz
  md5: 5a738e31670dd47b4e7a41c596c99526
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - climson = climson:main
    #
    # Would create an entry point called climson that calls climson.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - nose

  run:
    - python
    - nose

test:
  # Python imports
  imports:
    - climson

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: MIT License
  summary: 'Commandline tool for easy and simplify to implement applications that is using multi-command CLI.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
