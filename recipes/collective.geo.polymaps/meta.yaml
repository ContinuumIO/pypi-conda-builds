package:
  name: collective.geo.polymaps
  version: "0.1"

source:
  fn: collective.geo.polymaps-0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.geo.polymaps/collective.geo.polymaps-0.1.tar.gz
  md5: 074890a58fee64bd78897a968efbb669
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.geo.polymaps = collective.geo.polymaps:main
    #
    # Would create an entry point called collective.geo.polymaps that calls collective.geo.polymaps.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.geo.json

  run:
    - python
    - setuptools
    - setuptools
    - collective.geo.json

test:
  # Python imports
  imports:
    - collective
    - collective.geo
    - collective.geo.polymaps
    - collective.geo.polymaps.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/collective.geo.polymaps/
  license: GNU General Public License (GPL)
  summary: 'Collective Geo Polymaps integation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
