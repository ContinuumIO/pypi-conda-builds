package:
  name: django-misfit
  version: "0.0.2"

source:
  fn: django-misfit-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-misfit/django-misfit-0.0.2.tar.gz
  md5: ea9fd7cf7b2897171626509fa8ca6426
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-misfit = django-misfit:main
    #
    # Would create an entry point called django-misfit that calls django-misfit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools <8.0
    - celery >=3.1.0,<=3.2.0
    - misfit >=0.1.0,<=0.2.0

  run:
    - python
    - setuptools <8.0
    - celery >=3.1.0,<=3.2.0
    - misfit >=0.1.0,<=0.2.0

test:
  # Python imports
  imports:
    - misfitapp
    - misfitapp.templatetags
    - misfitapp.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/orcasgit/django-misfit/
  license: Apache Software License
  summary: 'Django integration for python-misfit'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
