package:
  name: dwolla-clt
  version: "1.2.4"

source:
  fn: dwolla-clt-1.2.4.tar.gz
  url: https://pypi.python.org/packages/source/d/dwolla-clt/dwolla-clt-1.2.4.tar.gz
  md5: 2c83993ca8f488af8d794137970b3ff5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dwolla-clt = dwolla-clt:main
    #
    # Would create an entry point called dwolla-clt that calls dwolla-clt.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - dwolla >=1.4.8
    - argparse >=1.2.1

  run:
    - python
    - dwolla >=1.4.8
    - argparse >=1.2.1

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dwolla/dwolla-clt
  license: MIT License
  summary: 'Dwolla Command Line Tools'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
