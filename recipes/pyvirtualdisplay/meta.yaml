package:
  name: pyvirtualdisplay
  version: "0.1.5"

source:
  fn: PyVirtualDisplay-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/P/PyVirtualDisplay/PyVirtualDisplay-0.1.5.tar.gz
  md5: 90b65fe15b81788c2e208d124e3a3c14
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pyvirtualdisplay = pyvirtualdisplay:main
    #
    # Would create an entry point called pyvirtualdisplay that calls pyvirtualdisplay.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - easyprocess

  run:
    - python
    - easyprocess

test:
  # Python imports
  imports:
    - pyvirtualdisplay
    - pyvirtualdisplay.examples

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ponty/pyvirtualdisplay
  license: BSD License
  summary: 'python wrapper for Xvfb, Xephyr and Xvnc'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
