package:
  name: django-modelclone
  version: "0.5.0"

source:
  fn: django-modelclone-0.5.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-modelclone/django-modelclone-0.5.0.tar.gz
  md5: 57a56e9aa6865b7f2ae2638605bee413
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-modelclone = django-modelclone:main
    #
    # Would create an entry point called django-modelclone that calls django-modelclone.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - modelclone

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/RealGeeks/django-modelclone
  license: MIT License
  summary: u'Django application that allows users to clone a model in Admin'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
