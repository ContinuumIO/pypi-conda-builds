package:
  name: django-throttle-requests
  version: "0.5.2"

source:
  fn: django-throttle-requests-0.5.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-throttle-requests/django-throttle-requests-0.5.2.tar.gz
  md5: 3ea96bc649235fb0930ef2b32248a056
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-throttle-requests = django-throttle-requests:main
    #
    # Would create an entry point called django-throttle-requests that calls django-throttle-requests.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3.7

  run:
    - python
    - django >=1.3.7

test:
  # Python imports
  imports:
    - throttle
    - throttle.backends
    - throttle.zones

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/sobotklp/django-throttle-requests
  license: UNKNOWN
  summary: 'A Django framework for application-layer rate limiting'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
