package:
  name: zope.lifecycleevent
  version: "4.1.0"

source:
  fn: zope.lifecycleevent-4.1.0.tar.gz
  url: https://pypi.python.org/packages/source/z/zope.lifecycleevent/zope.lifecycleevent-4.1.0.tar.gz
  md5: 4fa9c3c7ec97f9c865d802c2f5b96839
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zope.lifecycleevent = zope.lifecycleevent:main
    #
    # Would create an entry point called zope.lifecycleevent that calls zope.lifecycleevent.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.interface
    - zope.event

  run:
    - python
    - setuptools
    - zope.interface
    - zope.event

test:
  # Python imports
  imports:
    - zope
    - zope.lifecycleevent

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/zope.lifecycleevent
  license: Zope Public License
  summary: 'Object life-cycle events'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
