package:
  name: django-elfinderfs
  version: "0.0.6"

source:
  fn: django-elfinderfs-0.0.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-elfinderfs/django-elfinderfs-0.0.6.tar.gz
  md5: c0c21111e2a9303e15f49e5e97057f72
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-elfinderfs = django-elfinderfs:main
    #
    # Would create an entry point called django-elfinderfs that calls django-elfinderfs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.7
    - pillow >=2.2.1
    - djangorestframework >=3.0.2

  run:
    - python
    - django >=1.7
    - pillow >=2.2.1
    - djangorestframework >=3.0.2

test:
  # Python imports
  imports:
    - elfinderfs
    - elfinderfs.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://pypi.python.org/pypi/django-elfinderfs
  license: GNU General Public License v3 or later (GPLv3+)
  summary: "django-elfinderfs is a 3rd party connector for elFinder 2.x and Django. It's a simple local file system driver which does not uses any databases."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
