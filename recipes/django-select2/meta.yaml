package:
  name: django-select2
  version: "4.3.1"

source:
  fn: Django-Select2-4.3.1.tar.gz
  url: https://pypi.python.org/packages/source/D/Django-Select2/Django-Select2-4.3.1.tar.gz
  md5: 48c66033ca887d4a31b6f65195f5b7c2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-select2 = django-select2:main
    #
    # Would create an entry point called django-select2 that calls django-select2.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_select2
    - django_select2.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/applegrew/django-select2
  license: Apache Software License
  summary: u'Select2 option fields for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
