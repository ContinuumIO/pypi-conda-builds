package:
  name: pomade
  version: "0.0.4"

source:
  fn: pomade-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/p/pomade/pomade-0.0.4.tar.gz
  md5: a792371df33a8cd93e04eb219df27397
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pomade = pomade:main
    #
    # Would create an entry point called pomade that calls pomade.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - decorator >=3.4.0
    - selenium >=2.38.3
    - nose >=1.3.0

  run:
    - python
    - decorator >=3.4.0
    - selenium >=2.38.3
    - nose >=1.3.0

test:
  # Python imports
  imports:
    - pomade

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/saucelabs/pomade
  license: UNKNOWN
  summary: 'Selenium PageObjects implementation with easy Sauce Labs integration'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
