package:
  name: jmbo-gallery
  version: "2.0.0a1"

source:
  fn: jmbo-gallery-2.0.0a1.tar.gz
  url: https://pypi.python.org/packages/source/j/jmbo-gallery/jmbo-gallery-2.0.0a1.tar.gz
  md5: cab937a0fddda664ea0e4fc30a959404
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jmbo-gallery = jmbo-gallery:main
    #
    # Would create an entry point called jmbo-gallery that calls jmbo-gallery.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-ckeditor >=4.0.2
    - jmbo ==2.0.0a2

  run:
    - python
    - django-ckeditor >=4.0.2
    - jmbo ==2.0.0a2

test:
  # Python imports
  imports:
    - gallery
    - gallery.migrations
    - gallery.templatetags
    - gallery.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/praekelt/jmbo-gallery
  license: BSD License
  summary: 'Jmbo gallery app.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
