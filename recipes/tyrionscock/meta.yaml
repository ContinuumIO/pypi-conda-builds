package:
  name: tyrionscock
  version: "0.1.0"

source:
  fn: TyrionsCock-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/T/TyrionsCock/TyrionsCock-0.1.0.tar.gz
  md5: a8e23457f0b996f81e8aab3fd7eeefce
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tyrionscock = tyrionscock:main
    #
    # Would create an entry point called tyrionscock that calls tyrionscock.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - tyrionscock
    - tyrionscock.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypy.python.org/pypy/TyrionsCock/
  license: LICENCE.txt
  summary: 'Useful cock-related stuff.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
