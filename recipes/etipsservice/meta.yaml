package:
  name: etipsservice
  version: "1.0.7"

source:
  fn: ETipsService-1.0.7.tar.gz
  url: https://pypi.python.org/packages/source/E/ETipsService/ETipsService-1.0.7.tar.gz
  md5: 3da7782d599b59e3f0a943a7564f3739
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - etipsservice = etipsservice:main
    #
    # Would create an entry point called etipsservice that calls etipsservice.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - beautifulsoup4
    - requests

  run:
    - python
    - beautifulsoup4
    - requests

test:
  # Python imports
  imports:
    - etipsservice
    - service
    - test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Jayin/ETipsService
  license: Apache Software License
  summary: 'service lib for ETips'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
