package:
  name: geobricksprocessing
  version: "0.0.4"

source:
  fn: GeobricksProcessing-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/G/GeobricksProcessing/GeobricksProcessing-0.0.4.tar.gz
  md5: 09f4c0bbdfc720a4b01e1b2385da9813
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - geobricksprocessing = geobricksprocessing:main
    #
    # Would create an entry point called geobricksprocessing that calls geobricksprocessing.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - watchdog
    - flask
    - flask-cors
    - geobrickscommon

  run:
    - python
    - watchdog
    - flask
    - flask-cors
    - geobrickscommon

test:
  # Python imports
  imports:
    - geobricks_processing
    - geobricks_processing.config
    - geobricks_processing.core
    - geobricks_processing.rest
    - geobricksprocessing
    - test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/GeobricksProcessing/
  license: LICENSE.txt
  summary: 'Geobricks processing library for raster data.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
