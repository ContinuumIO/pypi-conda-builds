package:
  name: happymongo
  version: "0.1.1"

source:
  fn: happymongo-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/h/happymongo/happymongo-0.1.1.tar.gz
  md5: c7ee910bd5f533157b9f36d793a1690a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - happymongo = happymongo:main
    #
    # Would create an entry point called happymongo that calls happymongo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pymongo

  run:
    - python
    - pymongo

test:
  # Python imports
  imports:
    - happymongo

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/sivel/happymongo
  license: Apache License, Version 2.0
  summary: 'Python module for making it easy and consistent to connect to MongoDB via PyMongo either in Flask or in a non-flask application'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
