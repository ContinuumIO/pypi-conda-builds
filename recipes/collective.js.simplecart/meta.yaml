package:
  name: collective.js.simplecart
  version: "1.0"

source:
  fn: collective.js.simplecart-1.0.zip
  url: https://pypi.python.org/packages/source/c/collective.js.simplecart/collective.js.simplecart-1.0.zip
  md5: 3f51353b3fd38b229923d9d48fb4a077
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.js.simplecart = collective.js.simplecart:main
    #
    # Would create an entry point called collective.js.simplecart that calls collective.js.simplecart.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.js
    - collective.js.simplecart

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.js.simplecart
  license: GNU General Public License (GPL)
  summary: 'SimpleCart(js) for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
