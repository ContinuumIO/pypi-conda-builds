package:
  name: imagestore
  version: "2.9.1"

source:
  fn: imagestore-2.9.1.tar.gz
  url: https://pypi.python.org/packages/source/i/imagestore/imagestore-2.9.1.tar.gz
  md5: 66313223d459da4f56a699d21600d0eb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - imagestore = imagestore:main
    #
    # Would create an entry point called imagestore that calls imagestore.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - sorl-thumbnail
    - django-autocomplete-light
    - django-tagging
    - swapper

  run:
    - python
    - django
    - sorl-thumbnail
    - django-autocomplete-light
    - django-tagging
    - swapper

test:
  # Python imports
  imports:
    - imagestore
    - imagestore.imagestore_cms
    - imagestore.imagestore_cms.migrations
    - imagestore.imagestore_cms.south_migrations
    - imagestore.migrations
    - imagestore.models
    - imagestore.models.bases
    - imagestore.south_migrations
    - imagestore.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/hovel/imagestore
  license: GPL
  summary: 'Gallery solution for django projects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
