package:
  name: urlparse2
  version: "1.1.1"

source:
  fn: urlparse2-1.1.1.tar.gz
  url: https://pypi.python.org/packages/source/u/urlparse2/urlparse2-1.1.1.tar.gz
  md5: 6e2c1fef829dfae66bfeeca25a741f33
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - urlparse2 = urlparse2:main
    #
    # Would create an entry point called urlparse2 that calls urlparse2.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - recordtype

  run:
    - python
    - recordtype

test:
  # Python imports
  imports:
    - urlparse2

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mwhooker/urlparse2
  license: MIT License
  summary: 'urlparse for humans.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
