package:
  name: sqlalchemy_pgcatalog
  version: "0.1.1"

source:
  fn: sqlalchemy_pgcatalog-0.1.1.zip
  url: https://pypi.python.org/packages/source/s/sqlalchemy_pgcatalog/sqlalchemy_pgcatalog-0.1.1.zip
  md5: d04dc56fd6cd318b7181b0022a5592bc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sqlalchemy_pgcatalog = sqlalchemy_pgcatalog:main
    #
    # Would create an entry point called sqlalchemy_pgcatalog that calls sqlalchemy_pgcatalog.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sqlalchemy >=0.7.0

  run:
    - python
    - sqlalchemy >=0.7.0

test:
  # Python imports
  imports:
    - sqlalchemy_pgcatalog

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/renshawbay/sqlalchemy_pgcatalog
  license: MIT License
  summary: 'SQLAlchemy definitions for PostgreSQL catalog tables'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
