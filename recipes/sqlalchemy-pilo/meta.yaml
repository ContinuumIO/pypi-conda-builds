package:
  name: sqlalchemy-pilo
  version: "0.9.2"

source:
  fn: sqlalchemy-pilo-0.9.2.tar.gz
  url: https://pypi.python.org/packages/source/s/sqlalchemy-pilo/sqlalchemy-pilo-0.9.2.tar.gz
  md5: 8d506b2eca2950655827d700ef581a83
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sqlalchemy-pilo = sqlalchemy-pilo:main
    #
    # Would create an entry point called sqlalchemy-pilo that calls sqlalchemy-pilo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pilo >=0.4,<0.5
    - sqlalchemy >=0.9,<0.10

  run:
    - python
    - pilo >=0.4,<0.5
    - sqlalchemy >=0.9,<0.10

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bninja/sqlalchemy-pilo/
  license: ISC License (ISCL)
  summary: 'Mongo.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
