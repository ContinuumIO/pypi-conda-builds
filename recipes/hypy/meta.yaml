package:
  name: hypy
  version: "0.8.5.1"

source:
  fn: Hypy-0.8.5.1.tar.gz
  url: https://pypi.python.org/packages/source/H/Hypy/Hypy-0.8.5.1.tar.gz
  md5: 61f2a7f618a24dc249d33b4670f13d55
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hypy = hypy:main
    #
    # Would create an entry point called hypy that calls hypy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - distribute >=0.6.3

  run:
    - python
    - distribute >=0.6.3

test:
  # Python imports
  imports:
    - hypy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://goonmill.org/hypy/
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Pythonic wrapper for Hyper Estraier'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
