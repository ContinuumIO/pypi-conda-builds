package:
  name: pynagram
  version: "1.0.1"

source:
  fn: pynagram-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/p/pynagram/pynagram-1.0.1.tar.gz
  md5: d7957d1ed836c6a1a55f3163ea9c1bd7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pynagram = pynagram:main
    #
    # Would create an entry point called pynagram that calls pynagram.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - pynagram
    - pynagram.backend
    - pynagram.gui

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://launchpad.net/pynagram
  license: GPLv3
  summary: 'Pynagram - Unjumble the letters and get addicted!'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
