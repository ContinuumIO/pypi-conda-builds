package:
  name: minifixed
  version: "0.1.0"

source:
  fn: minifixed-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/m/minifixed/minifixed-0.1.0.tar.gz
  md5: b8f923fd4ae28cbd425797eeaefc8894
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - minifixed = minifixed:main
    #
    # Would create an entry point called minifixed that calls minifixed.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - minifixed

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jonasmalacofilho/minifixed
  license: LICENSE
  summary: 'Simple fixed-format data reader that guesses offsets.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
