package:
  name: js.select2
  version: "3.4.1"

source:
  fn: js.select2-3.4.1.zip
  url: https://pypi.python.org/packages/source/j/js.select2/js.select2-3.4.1.zip
  md5: cb53c1a70a913f55294a1323c7e2bd49
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - js.select2 = js.select2:main
    #
    # Would create an entry point called js.select2 that calls js.select2.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fanstatic
    - setuptools

  run:
    - python
    - setuptools
    - fanstatic
    - setuptools

test:
  # Python imports
  imports:
    - js
    - js.select2

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/delijati/js.select2
  license: BSD
  summary: 'Fanstatic packaging of select2'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
