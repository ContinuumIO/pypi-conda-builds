package:
  name: bambu-notifications
  version: "0.2.1"

source:
  fn: bambu-notifications-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/b/bambu-notifications/bambu-notifications-0.2.1.tar.gz
  md5: 07e760a89548ba44b23ade997ec4c318
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bambu-notifications = bambu-notifications:main
    #
    # Would create an entry point called bambu-notifications that calls bambu-notifications.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django >=1.4
    - markdown

  run:
    - python
    - django >=1.4
    - markdown

test:
  # Python imports
  imports:
    - bambu.notifications
    - bambu.notifications.migrations
    - bambu.notifications.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/bambu-notifications
  license: UNKNOWN
  summary: 'Generic, developer-defined model notifications that can be delivered via a set of pluggable, provider-based endpoints (email, SMS, Pusher, etc)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
