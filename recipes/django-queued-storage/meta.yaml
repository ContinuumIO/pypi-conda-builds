package:
  name: django-queued-storage
  version: "0.6"

source:
  fn: django-queued-storage-0.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-queued-storage/django-queued-storage-0.6.tar.gz
  md5: 7c7e469f45a2cae1fe186cf8e58c9b2b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-queued-storage = django-queued-storage:main
    #
    # Would create an entry point called django-queued-storage that calls django-queued-storage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-celery >=2.3.3,<3.0
    - django-appconf >=0.4

  run:
    - python
    - django-celery >=2.3.3,<3.0
    - django-appconf >=0.4

test:
  # Python imports
  imports:
    - queued_storage
    - queued_storage.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://django-queued-storage.rtfd.org
  license: BSD License
  summary: 'Provides a proxy for Django storage backends that allows you to upload files locally and eventually serve them remotely'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
