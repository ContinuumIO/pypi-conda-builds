package:
  name: modtools
  version: "1.0.2"

source:
  fn: modtools-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/m/modtools/modtools-1.0.2.tar.gz
  md5: f95994b3033e3fea877adf1d13a264f1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - modtools = modtools:main
    #
    # Would create an entry point called modtools that calls modtools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pysam >=0.7.4
    - argparse >=1.2
    - cython >=0.17

  run:
    - python
    - pysam >=0.7.4
    - argparse >=1.2
    - cython >=0.17

test:
  # Python imports
  imports:
    - modtools

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/mod-tools/
  license: The MIT License
  summary: 'Modtools - a tool set for manipulating MOD and pseudogenomes'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
