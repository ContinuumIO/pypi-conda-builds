package:
  name: foxpath
  version: "0.99.15"

source:
  fn: foxpath-0.99.15.tar.gz
  url: https://pypi.python.org/packages/source/f/foxpath/foxpath-0.99.15.tar.gz
  md5: 5f79b219623450ee3abd36c9d45c3320
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - foxpath = foxpath:main
    #
    # Would create an entry point called foxpath that calls foxpath.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - foxpath

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: GNU Affero General Public License v3.0
  summary: 'Python library for running FoXPath tests against XML'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
