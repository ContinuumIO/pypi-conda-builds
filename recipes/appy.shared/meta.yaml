package:
  name: appy.shared
  version: "0.1.0"

source:
  fn: appy.shared-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/a/appy.shared/appy.shared-0.1.0.tar.gz
  md5: da9e2bcacd88d15d2a9a17fb7456e6b7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - appy.shared = appy.shared:main
    #
    # Would create an entry point called appy.shared that calls appy.shared.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - appy
    - appy.shared

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://appyframework.org
  license: GPL
  summary: 'Utilities for appy.pod template engine'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
