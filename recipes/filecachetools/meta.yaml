package:
  name: filecachetools
  version: "0.1.1"

source:
  fn: filecachetools-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/f/filecachetools/filecachetools-0.1.1.tar.gz
  md5: 15619ac3dc631ffb16ff6c119e656b7e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - filecachetools = filecachetools:main
    #
    # Would create an entry point called filecachetools that calls filecachetools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cachetools

  run:
    - python
    - cachetools

test:
  # Python imports
  imports:
    - filecachetools

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/xi/filecachetools
  license: MIT License
  summary: 'cachetools compatible persistent cache'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
