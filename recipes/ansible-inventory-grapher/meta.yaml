package:
  name: ansible-inventory-grapher
  version: "1.0.3"

source:
  fn: ansible-inventory-grapher-1.0.3.tar.gz
  url: https://pypi.python.org/packages/source/a/ansible-inventory-grapher/ansible-inventory-grapher-1.0.3.tar.gz
  md5: dc3f807750adb2156de44c04c683c9aa
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ansible-inventory-grapher = ansible-inventory-grapher:main
    #
    # Would create an entry point called ansible-inventory-grapher that calls ansible-inventory-grapher.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - ansible >=1.7

  run:
    - python
    - ansible >=1.7

test:
  # Python imports
  imports:
    - ansibleinventorygrapher

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/willthames/ansible-inventory-grapher
  license: GPLv3
  summary: 'Creates graphs representing ansible inventory'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
