package:
  name: assoc_space
  version: "1.0.0"

source:
  fn: assoc_space-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/a/assoc_space/assoc_space-1.0.0.tar.gz
  md5: 73b489711bf70446d889b7d3e49ee655
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - assoc_space = assoc_space:main
    #
    # Would create an entry point called assoc_space that calls assoc_space.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy
    - scipy

  run:
    - python
    - numpy
    - scipy

test:
  # Python imports
  imports:
    - assoc_space

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/LuminosoInsight/assoc-space
  license: MIT
  summary: 'Computes association strength over semantic networks in a dimensionality-reduced form.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
