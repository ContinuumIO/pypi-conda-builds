package:
  name: aldryn-common
  version: "0.1.3"

source:
  fn: aldryn-common-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/a/aldryn-common/aldryn-common-0.1.3.tar.gz
  md5: 1913da267d69da63d874808281f9d889
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - aldryn-common = aldryn-common:main
    #
    # Would create an entry point called aldryn-common that calls aldryn-common.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - aldryn-boilerplates
    - django-sortedm2m

  run:
    - python
    - aldryn-boilerplates
    - django-sortedm2m

test:
  # Python imports
  imports:
    - aldryn_common
    - aldryn_common.admin_fields
    - aldryn_common.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/aldryn/aldryn-common
  license: BSD License
  summary: 'Common utitilities'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
