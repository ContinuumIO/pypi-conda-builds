package:
  name: collective.referencedatagridfield
  version: "0.5.3"

source:
  fn: collective.referencedatagridfield-0.5.3.zip
  url: https://pypi.python.org/packages/source/c/collective.referencedatagridfield/collective.referencedatagridfield-0.5.3.zip
  md5: d0ac46a004a38a615baf48cfec4eb632
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.referencedatagridfield = collective.referencedatagridfield:main
    #
    # Would create an entry point called collective.referencedatagridfield that calls collective.referencedatagridfield.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.datagridfield >=1.8a1
    - archetypes.referencebrowserwidget >=2.0a

  run:
    - python
    - setuptools
    - setuptools
    - products.datagridfield >=1.8a1
    - archetypes.referencebrowserwidget >=2.0a

test:
  # Python imports
  imports:
    - collective
    - collective.referencedatagridfield
    - collective.referencedatagridfield.browser
    - collective.referencedatagridfield.examples
    - collective.referencedatagridfield.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.referencedatagridfield
  license: GNU General Public License (GPL)
  summary: 'Mix of Reference and DataGrid Fields'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
