package:
  name: js.angular_infinite_scroll
  version: "1.2.0"

source:
  fn: js.angular_infinite_scroll-1.2.0.zip
  url: https://pypi.python.org/packages/source/j/js.angular_infinite_scroll/js.angular_infinite_scroll-1.2.0.zip
  md5: 197c0251938e7264ef22304a56cf806c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - js.angular_infinite_scroll = js.angular_infinite_scroll:main
    #
    # Would create an entry point called js.angular_infinite_scroll that calls js.angular_infinite_scroll.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fanstatic
    - setuptools
    - js.angular

  run:
    - python
    - setuptools
    - fanstatic
    - setuptools
    - js.angular

test:
  # Python imports
  imports:
    - js
    - js.angular_infinite_scroll

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD
  summary: 'Fanstatic packaging of angular infiniteScroll'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
