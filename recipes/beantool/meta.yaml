package:
  name: beantool
  version: "0.2.13"

source:
  fn: beantool-0.2.13.tar.gz
  url: https://pypi.python.org/packages/source/b/beantool/beantool-0.2.13.tar.gz
  md5: bbbcea9a51bb6fcfda5c65c03a8fab18
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - beantool = beantool:main
    #
    # Would create an entry point called beantool that calls beantool.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyyaml ==3.10
    - beanstalkc ==0.3.0
    - phpserialize ==1.3

  run:
    - python
    - setuptools
    - pyyaml ==3.10
    - beanstalkc ==0.3.0
    - phpserialize ==1.3

test:
  # Python imports
  imports:
    - beantool
    - beantool.handlers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dsoprea/BeanTool
  license: GNU General Public License v2 (GPLv2)
  summary: 'A beanstalkd console client.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
