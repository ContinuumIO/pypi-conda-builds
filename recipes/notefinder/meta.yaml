package:
  name: notefinder
  version: "0.9"

source:
  fn: notefinder.tar.gz
  url: https://pypi.python.org/packages/source/n/notefinder/notefinder.tar.gz
  md5: 6b3b3181dca7b1ec16e98e043e32267f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - notefinder = notefinder:main
    #
    # Would create an entry point called notefinder that calls notefinder.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - notefinder

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://notefinder.googlecode.com
  license: GNU General Public License (GPL)
  summary: 'Package for easy notes management'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
