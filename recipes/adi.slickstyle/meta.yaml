package:
  name: adi.slickstyle
  version: "0.1"

source:
  fn: adi.slickstyle-0.1.tar.gz
  url: https://pypi.python.org/packages/source/a/adi.slickstyle/adi.slickstyle-0.1.tar.gz
  md5: 4ccb2a2c37473b8b4884de9ec74dc833
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - adi.slickstyle = adi.slickstyle:main
    #
    # Would create an entry point called adi.slickstyle that calls adi.slickstyle.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - adi
    - adi.slickstyle

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/adi.slickstyle
  license: GPL
  summary: 'A slick style for Plone portals, easily extendable for your own styles.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
