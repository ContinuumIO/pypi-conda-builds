package:
  name: django-finalware
  version: "0.1.0"

source:
  fn: django-finalware-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-finalware/django-finalware-0.1.0.tar.gz
  md5: 4e77638f829ddc9df8fb39e7f7ae003f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-finalware = django-finalware:main
    #
    # Would create an entry point called django-finalware that calls django-finalware.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - finalware
    - finalware/tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/un33k/django-finalware
  license: BSD License
  summary: 'A utility application that finalizes the bootstrapping of a Django-powered site'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
