package:
  name: django-icybackup
  version: "0.1.1"

source:
  fn: django-icybackup-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-icybackup/django-icybackup-0.1.1.tar.gz
  md5: f0ce76981ad525af7c788f852191e423
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-icybackup = django-icybackup:main
    #
    # Would create an entry point called django-icybackup that calls django-icybackup.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - boto
    - python-dateutil

  run:
    - python
    - boto
    - python-dateutil

test:
  # Python imports
  imports:
    - icybackup
    - icybackup.components
    - icybackup.management
    - icybackup.management.commands
    - icybackup.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/stbarnabas/django-icybackup
  license: UNKNOWN
  summary: 'A Django database/media backup tool with Amazon Glacier and local folder support'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
