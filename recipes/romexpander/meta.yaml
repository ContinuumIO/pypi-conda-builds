package:
  name: romexpander
  version: "0.5"

source:
  fn: romexpander-0.5.tar.gz
  url: https://pypi.python.org/packages/source/r/romexpander/romexpander-0.5.tar.gz
  md5: 3403fb31b3c184905f78a0ff708c40e3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - romexpander = romexpander:main
    #
    # Would create an entry point called romexpander that calls romexpander.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - docopt >=0.3.0

  run:
    - python
    - docopt >=0.3.0

test:
  # Python imports
  imports:
    - romexpander

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.cdf.utoronto.ca/~g8m/
  license: MIT
  summary: "NES ROM Expansion script compatible with DvD's ROM Expander Pro."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
