package:
  name: metaviz
  version: "0.0.3"

source:
  fn: metaviz-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/m/metaviz/metaviz-0.0.3.tar.gz
  md5: e1eadc0b1ee4388b336c0bddc3590917
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - metaviz = metaviz:main
    #
    # Would create an entry point called metaviz that calls metaviz.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - metaviz

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mfitzp/metaviz
  license: UNKNOWN
  summary: 'Metabolic pathway visualisation using BioCyc API and GraphViz'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
