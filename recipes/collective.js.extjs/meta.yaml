package:
  name: collective.js.extjs
  version: "1.2"

source:
  fn: collective.js.extjs-1.2.zip
  url: https://pypi.python.org/packages/source/c/collective.js.extjs/collective.js.extjs-1.2.zip
  md5: 178431be8d8ba09b04bc4f2642ec766d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.js.extjs = collective.js.extjs:main
    #
    # Would create an entry point called collective.js.extjs that calls collective.js.extjs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.js
    - collective.js.extjs

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/4teamwork/collective.js.extjs
  license: GPL2
  summary: 'Ext JS 3.4 integration for Plone.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
