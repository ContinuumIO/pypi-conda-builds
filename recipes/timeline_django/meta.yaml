package:
  name: timeline_django
  version: "0.0.3"

source:
  fn: timeline_django-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/t/timeline_django/timeline_django-0.0.3.tar.gz
  md5: fc828a66272a7337e0fe7fe8fffba664
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - timeline_django = timeline_django:main
    #
    # Would create an entry point called timeline_django that calls timeline_django.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - timeline_django
    - timeline_django.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://launchpad.net/python-timeline-django
  license: LGPLv3
  summary: 'Insert Django database queries in to a Timeline'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
