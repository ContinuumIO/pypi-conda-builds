package:
  name: django-image-tools
  version: "0.8.b3"

source:
  fn: django-image-tools-0.8.b3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-image-tools/django-image-tools-0.8.b3.tar.gz
  md5: 395cf4d879e7e0906b27bba97ad0e57c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-image-tools = django-image-tools:main
    #
    # Would create an entry point called django-image-tools that calls django-image-tools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - pillow

  run:
    - python
    - pillow

test:
  # Python imports
  imports:
    - django_image_tools
    - django_image_tools.django_migrations
    - django_image_tools.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/bonsaistudio/django-image-tools
  license: BSD License
  summary: 'Image tools for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
