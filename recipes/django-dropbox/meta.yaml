package:
  name: django-dropbox
  version: "0.0.2"

source:
  fn: django-dropbox-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-dropbox/django-dropbox-0.0.2.tar.gz
  md5: 169f0b875efd030487bb8dd79db87a86
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-dropbox = django-dropbox:main
    #
    # Would create an entry point called django-dropbox that calls django-dropbox.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - dropbox >=2.0.0

  run:
    - python
    - dropbox >=2.0.0

test:
  # Python imports
  imports:
    - django_dropbox
    - django_dropbox.management
    - django_dropbox/management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/andres-torres-marroquin/django-dropbox
  license: UNKNOWN
  summary: 'A Django App that contains a Django Storage which uses Dropbox.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
