package:
  name: commandtool
  version: "0.5.3"

source:
  fn: CommandTool-0.5.3.tar.gz
  url: https://pypi.python.org/packages/source/C/CommandTool/CommandTool-0.5.3.tar.gz
  md5: f97ef2dd63dc02a83d90f3d4094a0bb7
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - commandtool = commandtool:main
    #
    # Would create an entry point called commandtool that calls commandtool.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - commandtool

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://jimmyg.org/work/code/commandtool/index.html
  license: MIT License
  summary: 'Tools for creating command line interfaces involving sub-commands.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
