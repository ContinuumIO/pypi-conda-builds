package:
  name: willow
  version: "0.2"

source:
  fn: Willow-0.2.tar.gz
  url: https://pypi.python.org/packages/source/W/Willow/Willow-0.2.tar.gz
  md5: 216a7211930482676be716a3abdecb33
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - willow = willow:main
    #
    # Would create an entry point called willow that calls willow.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six >=1.7.0

  run:
    - python
    - six >=1.7.0

test:
  # Python imports
  imports:
    - tests
    - willow
    - willow.backends
    - willow.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: BSD License
  summary: 'A Python image library that sits on top of Pillow, Wand and OpenCV'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
