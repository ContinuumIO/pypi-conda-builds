package:
  name: djace_editor
  version: "0.9.2"

source:
  fn: djace_editor-0.9.2.tar.gz
  url: https://pypi.python.org/packages/source/d/djace_editor/djace_editor-0.9.2.tar.gz
  md5: 0c8d9c8256f533c2a7ed225ade7b6b50
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djace_editor = djace_editor:main
    #
    # Would create an entry point called djace_editor that calls djace_editor.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django
    - setuptools

  run:
    - python
    - django
    - setuptools

test:
  # Python imports
  imports:
    - djace_editor

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/smidth/django-ace-editor
  license: BSD License
  summary: 'djace_editor provides integration for ajax.org ACE with Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
