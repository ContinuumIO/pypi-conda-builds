package:
  name: plonesocial.activitystream
  version: "0.5.6"

source:
  fn: plonesocial.activitystream-0.5.6.zip
  url: https://pypi.python.org/packages/source/p/plonesocial.activitystream/plonesocial.activitystream-0.5.6.zip
  md5: c8dd67ff6384715bf0d277de9b92d098
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plonesocial.activitystream = plonesocial.activitystream:main
    #
    # Would create an entry point called plonesocial.activitystream that calls plonesocial.activitystream.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - plone.app.layout
    - plone.app.portlets
    - plone.portlets
    - products.cmfcore
    - products.cmfplone >=4.2
    - products.genericsetup
    - products.zcatalog
    - setuptools
    - zope.component
    - zope.i18nmessageid
    - zope.interface
    - zope.schema
    - zope.viewlet

  run:
    - python
    - setuptools
    - plone.app.layout
    - plone.app.portlets
    - plone.portlets
    - products.cmfcore
    - products.cmfplone >=4.2
    - products.genericsetup
    - products.zcatalog
    - setuptools
    - zope.component
    - zope.i18nmessageid
    - zope.interface
    - zope.schema
    - zope.viewlet

test:
  # Python imports
  imports:
    - plonesocial
    - plonesocial.activitystream
    - plonesocial.activitystream.Extensions
    - plonesocial.activitystream.browser
    - plonesocial.activitystream.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/cosent/plonesocial.activitystream
  license: GNU General Public License v2 (GPLv2)
  summary: 'This package provides views for plonesocial.microblog status updates and other user activities in Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
