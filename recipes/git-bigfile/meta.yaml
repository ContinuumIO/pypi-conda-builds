package:
  name: git-bigfile
  version: "0.1.2"

source:
  fn: git-bigfile-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/g/git-bigfile/git-bigfile-0.1.2.tar.gz
  md5: 3c157a4d309bcbbd9f865b79ea67f796
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - git-bigfile = git-bigfile:main
    #
    # Would create an entry point called git-bigfile that calls git-bigfile.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - docopt ==0.5.0

  run:
    - python
    - docopt ==0.5.0

test:
  # Python imports
  imports:
    - gitbigfile

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/beenje/git-bigfile
  license: MIT License
  summary: 'git-bigfile allows you to use Git with large files without storing the file in Git itself'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
