package:
  name: django-oot
  version: "0.8.4"

source:
  fn: django-oot-0.8.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-oot/django-oot-0.8.4.tar.gz
  md5: c34bd841e804d331503a351d49fc10f3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-oot = django-oot:main
    #
    # Would create an entry point called django-oot that calls django-oot.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - oot
    - oot.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://tracpub.yaco.es/djangoapps/wiki/OOT
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Django application to export content to OpenOffice documents'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
