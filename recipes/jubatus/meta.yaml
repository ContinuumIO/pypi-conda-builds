package:
  name: jubatus
  version: "0.7.1"

source:
  fn: jubatus-0.7.1.tar.gz
  url: https://pypi.python.org/packages/source/j/jubatus/jubatus-0.7.1.tar.gz
  md5: ce7e7c6dd5b3913326e3f3e403472151
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jubatus = jubatus:main
    #
    # Would create an entry point called jubatus that calls jubatus.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - msgpack-rpc-python >=0.3.0

  run:
    - python
    - msgpack-rpc-python >=0.3.0

test:
  # Python imports
  imports:
    - jubatus
    - jubatus.anomaly
    - jubatus.bandit
    - jubatus.burst
    - jubatus.classifier
    - jubatus.clustering
    - jubatus.common
    - jubatus.graph
    - jubatus.nearest_neighbor
    - jubatus.recommender
    - jubatus.regression
    - jubatus.stat

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://jubat.us
  license: MIT License
  summary: 'Jubatus is a distributed processing framework and streaming machine learning library. This is the Jubatus client in Python.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
