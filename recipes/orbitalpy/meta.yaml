package:
  name: orbitalpy
  version: "0.6.2"

source:
  fn: OrbitalPy-0.6.2.zip
  url: https://pypi.python.org/packages/source/O/OrbitalPy/OrbitalPy-0.6.2.zip
  md5: 70af65618d60d8909cec8805c429abb1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - orbitalpy = orbitalpy:main
    #
    # Would create an entry point called orbitalpy that calls orbitalpy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy
    - scipy
    - astropy
    - matplotlib
    - represent >=1.1.0

  run:
    - python
    - numpy
    - scipy
    - astropy
    - matplotlib
    - represent >=1.1.0

test:
  # Python imports
  imports:
    - orbital
    - orbitalpy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/RazerM/orbital
  license: MIT License
  summary: 'High level orbital mechanics package.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
