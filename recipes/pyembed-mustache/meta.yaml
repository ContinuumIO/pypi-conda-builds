package:
  name: pyembed-mustache
  version: "1.1.0"

source:
  fn: pyembed-mustache-1.1.0.tar.gz
  url: https://pypi.python.org/packages/source/p/pyembed-mustache/pyembed-mustache-1.1.0.tar.gz
  md5: 321bb44a3b45526d5a7f20d9bf1d25f5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pyembed-mustache = pyembed-mustache:main
    #
    # Would create an entry point called pyembed-mustache that calls pyembed-mustache.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyembed
    - pystache

  run:
    - python
    - pyembed
    - pystache

test:
  # Python imports
  imports:
    - pyembed.mustache

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pyembed.github.io
  license: MIT License
  summary: 'PyEmbed plugin for rendering embeddings using Mustache templates'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
