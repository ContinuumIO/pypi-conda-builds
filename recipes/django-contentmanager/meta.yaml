package:
  name: django-contentmanager
  version: "2012.5.9"

source:
  fn: django-contentmanager-2012.5.9.tar.gz
  url: https://pypi.python.org/packages/source/d/django-contentmanager/django-contentmanager-2012.5.9.tar.gz
  md5: 82aa95495f17e928e112ea4c8238f903
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-contentmanager = django-contentmanager:main
    #
    # Would create an entry point called django-contentmanager that calls django-contentmanager.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-haystack
    - whoosh

  run:
    - python
    - setuptools
    - django-haystack
    - whoosh

test:
  # Python imports
  imports:
    - contentmanager
    - contentmanager.templatetags
    - contentmanager.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/pterk/django-contentmanager/
  license: BSD License
  summary: 'A simple, pluggable content-manager for django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
