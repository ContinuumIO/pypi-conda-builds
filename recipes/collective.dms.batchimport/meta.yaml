package:
  name: collective.dms.batchimport
  version: "1.2.2"

source:
  fn: collective.dms.batchimport-1.2.2.zip
  url: https://pypi.python.org/packages/source/c/collective.dms.batchimport/collective.dms.batchimport-1.2.2.zip
  md5: b55de0af24f2b3d76b34b4a931a59a54
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.dms.batchimport = collective.dms.batchimport:main
    #
    # Would create an entry point called collective.dms.batchimport that calls collective.dms.batchimport.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.dms.basecontent
    - collective.dms.mailcontent
    - collective.z3cform.datagridfield
    - five.grok

  run:
    - python
    - setuptools
    - setuptools
    - collective.dms.basecontent
    - collective.dms.mailcontent
    - collective.z3cform.datagridfield
    - five.grok

test:
  # Python imports
  imports:
    - collective
    - collective.dms
    - collective.dms.batchimport
    - collective.dms.batchimport.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/
  license: gpl
  summary: 'Batch import of files into the Documents Management System'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
