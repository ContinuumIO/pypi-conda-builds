package:
  name: django-reportapi
  version: "3.2.0"

source:
  fn: django-reportapi-3.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-reportapi/django-reportapi-3.2.0.tar.gz
  md5: d13e3a2e7988c56039071518162026ff
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-reportapi = django-reportapi:main
    #
    # Would create an entry point called django-reportapi that calls django-reportapi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-quickapi >=2.6

  run:
    - python
    - django-quickapi >=2.6

test:
  # Python imports
  imports:
    - reportapi
    - reportapi.migrations
    - reportapi.templatetags
    - reportapi.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/rosix/django-reportapi/
  license: GNU Affero General Public License v3 or later (AGPLv3+)
  summary: 'Easy mechanism building reports in Django projects.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
