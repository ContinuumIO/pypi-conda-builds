package:
  name: django-diario
  version: "1.0"

source:
  fn: django-diario-1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-diario/django-diario-1.0.tar.gz
  md5: 80fe2ba851ae80445566f8417c705c1b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-diario = django-diario:main
    #
    # Would create an entry point called django-diario that calls django-diario.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - diario
    - diario.feeds
    - diario.templatetags
    - diario.tests
    - diario.urls
    - diario.views

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/semente/django-diario/
  license: GNU Lesser General Public License v3 or later (LGPLv3+)
  summary: 'Blog application for Django projects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
