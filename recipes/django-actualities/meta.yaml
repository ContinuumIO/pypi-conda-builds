package:
  name: django-actualities
  version: "0.2.2"

source:
  fn: django-actualities-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-actualities/django-actualities-0.2.2.tar.gz
  md5: 4166104ca0608ce7391cf4613bc30707
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-actualities = django-actualities:main
    #
    # Would create an entry point called django-actualities that calls django-actualities.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-taggit ==0.9.3

  run:
    - python
    - django-taggit ==0.9.3

test:
  # Python imports
  imports:
    - actualities
    - actualities.migrations
    - actualities.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/platypus-creation/django-actualities
  license: UNKNOWN
  summary: '\n    A django blog app\n    '

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
