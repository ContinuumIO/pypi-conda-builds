package:
  name: flask-compass
  version: "0.2"

source:
  fn: Flask-Compass-0.2.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Compass/Flask-Compass-0.2.tar.gz
  md5: 83b91474e6b7794fedc421182b8866a7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-compass = flask-compass:main
    #
    # Would create an entry point called flask-compass that calls flask-compass.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask

  run:
    - python
    - flask

test:
  # Python imports
  imports:
    - flaskext

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/zerok/flask-compass
  license: BSD License
  summary: 'Adds automatic Compass compilation to Flask'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
