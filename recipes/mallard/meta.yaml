package:
  name: mallard
  version: "1.0.1"

source:
  fn: mallard-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/m/mallard/mallard-1.0.1.tar.gz
  md5: ae2b01d0e4f0582e0709157e0369cf51
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mallard = mallard:main
    #
    # Would create an entry point called mallard that calls mallard.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=2.4
    - certifi >=14.05.14

  run:
    - python
    - requests >=2.4
    - certifi >=14.05.14

test:
  # Python imports
  imports:
    - mallard
    - mallard.examples

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/hamstah/mallard
  license: UNKNOWN
  summary: 'Library to send events to ducksboard.com'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
