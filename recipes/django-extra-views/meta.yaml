package:
  name: django-extra-views
  version: "0.6.5"

source:
  fn: django-extra-views-0.6.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-extra-views/django-extra-views-0.6.5.tar.gz
  md5: cb3076c480837eebdbd36defba006325
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-extra-views = django-extra-views:main
    #
    # Would create an entry point called django-extra-views that calls django-extra-views.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3
    - six ==1.5.2

  run:
    - python
    - django >=1.3
    - six ==1.5.2

test:
  # Python imports
  imports:
    - extra_views

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/AndrewIngram/django-extra-views
  license: MIT
  summary: 'Extra class-based views for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
