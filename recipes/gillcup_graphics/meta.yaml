package:
  name: gillcup_graphics
  version: "0.2.0-alpha.1"

source:
  fn: gillcup_graphics-0.2.0-alpha.1.tar.gz
  url: https://pypi.python.org/packages/source/g/gillcup_graphics/gillcup_graphics-0.2.0-alpha.1.tar.gz
  md5: 1a470d265a48e338453d8a7a978bca80
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gillcup_graphics = gillcup_graphics:main
    #
    # Would create an entry point called gillcup_graphics that calls gillcup_graphics.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - gillcup >=0.2.0-alpha
    - pyglet ==1.1.4

  run:
    - python
    - gillcup >=0.2.0-alpha
    - pyglet ==1.1.4

test:
  # Python imports
  imports:
    - gillcup_graphics
    - gillcup_graphics.offscreen
    - gillcup_graphics.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT License
  summary: 'Pyglet graphics for Gillcup'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
