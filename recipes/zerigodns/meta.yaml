package:
  name: zerigodns
  version: "0.3"

source:
  fn: zerigodns-0.3.tar.gz
  url: https://pypi.python.org/packages/source/z/zerigodns/zerigodns-0.3.tar.gz
  md5: 1256532cca49eaf76eb8fe90ec0281e9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zerigodns = zerigodns:main
    #
    # Would create an entry point called zerigodns that calls zerigodns.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - zerigodns

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/petersanchez/zerigodns/
  license: BSD License
  summary: 'Python package to interface with Zerigo DNS API.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
