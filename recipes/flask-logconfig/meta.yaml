package:
  name: flask-logconfig
  version: "0.4.0"

source:
  fn: Flask-LogConfig-0.4.0.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-LogConfig/Flask-LogConfig-0.4.0.tar.gz
  md5: 2d69297631f09a33904b3f6c573a156c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-logconfig = flask-logconfig:main
    #
    # Would create an entry point called flask-logconfig that calls flask-logconfig.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - logconfig >=0.4.0
    - flask

  run:
    - python
    - logconfig >=0.4.0
    - flask

test:
  # Python imports
  imports:
    - flask_logconfig

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dgilland/flask-logconfig
  license: MIT License
  summary: 'Flask extension for configuring Python logging module'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
