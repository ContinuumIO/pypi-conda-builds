package:
  name: changepoint
  version: "0.1.1"

source:
  fn: changepoint-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/c/changepoint/changepoint-0.1.1.tar.gz
  md5: ecf4d9138a2e9bd8edd4b4da32b072ab
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - changepoint = changepoint:main
    #
    # Would create an entry point called changepoint that calls changepoint.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - wheel ==0.23.0
    - argparse >=1.2.1
    - numpy >=0.9.1
    - scipy >=0.15.1
    - more_itertools >=2.2
    - joblib >=0.8.3-r1

  run:
    - python
    - wheel ==0.23.0
    - argparse >=1.2.1
    - numpy >=0.9.1
    - scipy >=0.15.1
    - more_itertools >=2.2
    - joblib >=0.8.3-r1

test:
  # Python imports
  imports:
    - changepoint
    - changepoint.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/viveksck/changepoint
  license: BSD License
  summary: 'Change point detection in Time series'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
