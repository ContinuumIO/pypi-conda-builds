package:
  name: django-request-id
  version: "0.1.0"

source:
  fn: django-request-id-0.1.0.zip
  url: https://pypi.python.org/packages/source/d/django-request-id/django-request-id-0.1.0.zip
  md5: 38bce7dee965d3a5b307f66f36c6aa3d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-request-id = django-request-id:main
    #
    # Would create an entry point called django-request-id that calls django-request-id.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - django-appconf >=0.6

  run:
    - python
    - django
    - django-appconf >=0.6

test:
  # Python imports
  imports:
    - request_id

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/nigma/django-request-id
  license: MIT License
  summary: 'Augment each request with unique id for logging purposes'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
