package:
  name: django-templated-email
  version: "0.4.9"

source:
  fn: django-templated-email-0.4.9.tar.gz
  url: https://pypi.python.org/packages/source/d/django-templated-email/django-templated-email-0.4.9.tar.gz
  md5: 0b2de76e719fad24b54351c27752b280
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-templated-email = django-templated-email:main
    #
    # Would create an entry point called django-templated-email that calls django-templated-email.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - templated_email
    - templated_email.backends

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/bradwhittington/django-templated-email/
  license: MIT License
  summary: 'A Django oriented templated / transaction email abstraction'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
