package:
  name: django-oidc-provider
  version: "0.0.4.1"

source:
  fn: django-oidc-provider-0.0.4.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-oidc-provider/django-oidc-provider-0.0.4.1.tar.gz
  md5: eb617f4007efa82c74318b6c61ae5a77
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-oidc-provider = django-oidc-provider:main
    #
    # Would create an entry point called django-oidc-provider that calls django-oidc-provider.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyjwt ==1.1.0

  run:
    - python
    - pyjwt ==1.1.0

test:
  # Python imports
  imports:
    - oidc_provider
    - oidc_provider/lib
    - oidc_provider/lib/endpoints
    - oidc_provider/lib/utils
    - oidc_provider/migrations
    - oidc_provider/tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/juanifioren/django-oidc-provider
  license: MIT License
  summary: 'OpenID Connect Provider implementation for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
