package:
  name: flask-dbmigrate
  version: "0.1"

source:
  fn: Flask-DBMigrate-0.1.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-DBMigrate/Flask-DBMigrate-0.1.tar.gz
  md5: 6a667ae47e2e705a819d0fbab317592e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-dbmigrate = flask-dbmigrate:main
    #
    # Would create an entry point called flask-dbmigrate that calls flask-dbmigrate.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sqlalchemy <=0.7.9sqlalchemy-migrate
    - flask
    - flask-sqlalchemy
    - flask-script

  run:
    - python
    - sqlalchemy <=0.7.9sqlalchemy-migrate
    - flask
    - flask-sqlalchemy
    - flask-script

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/akostyuk/flask-dbmigrate/
  license: Apache Software License
  summary: 'Database schema change management for Flask\\SQLAlchemy'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
