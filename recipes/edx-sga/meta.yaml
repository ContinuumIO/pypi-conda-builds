package:
  name: edx-sga
  version: "0.5.0"

source:
  fn: edx-sga-0.5.0.tar.gz
  url: https://pypi.python.org/packages/source/e/edx-sga/edx-sga-0.5.0.tar.gz
  md5: b95173661aefe0a2875bec40848e6ca8
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - edx-sga = edx-sga:main
    #
    # Would create an entry point called edx-sga that calls edx-sga.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - xblock

  run:
    - python
    - setuptools
    - xblock

test:
  # Python imports
  imports:
    - edx_sga
    - edx_sga.management
    - edx_sga.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mitodl/edx-sga
  license: Affero GNU General Public License v3 (GPLv3)
  summary: 'edx-sga Staff Graded Assignment XBlock'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
