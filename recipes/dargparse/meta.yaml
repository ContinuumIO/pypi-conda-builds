package:
  name: dargparse
  version: "0.2.5"

source:
  fn: dargparse-0.2.5.tar.gz
  url: https://pypi.python.org/packages/source/d/dargparse/dargparse-0.2.5.tar.gz
  md5: 0e0efe48527258917b71d29d2a99ba0e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dargparse = dargparse:main
    #
    # Would create an entry point called dargparse that calls dargparse.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - dargparse

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/objectlabs/dargparse
  license: MIT
  summary: 'Declarative command-line argument parser for python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
