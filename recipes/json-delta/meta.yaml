package:
  name: json-delta
  version: "1.1.2"

source:
  fn: json_delta-1.1.2.tar.gz
  url: https://pypi.python.org/packages/source/j/json-delta/json_delta-1.1.2.tar.gz
  md5: 3148f4213e87dc6210b5c3e7f7a1df46
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - json-delta = json-delta:main
    #
    # Would create an entry point called json-delta that calls json-delta.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - json_delta

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://json-delta.readthedocs.org/
  license: BSD License
  summary: 'A diff/patch pair for JSON-serialized data structures.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
