package:
  name: latimes-pluggablemaps-usstates
  version: "0.1"

source:
  fn: latimes-pluggablemaps-usstates-0.1.tar.gz
  url: https://pypi.python.org/packages/source/l/latimes-pluggablemaps-usstates/latimes-pluggablemaps-usstates-0.1.tar.gz
  md5: d254f03b5eddaf99608b5b16c818c329
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - latimes-pluggablemaps-usstates = latimes-pluggablemaps-usstates:main
    #
    # Would create an entry point called latimes-pluggablemaps-usstates that calls latimes-pluggablemaps-usstates.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - us_states
    - us_states.management
    - us_states.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/datadesk/latimes-pluggablemaps-usstates
  license: MIT License
  summary: 'L.A. Times Pluggable Maps: U.S. States'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
