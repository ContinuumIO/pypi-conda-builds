package:
  name: fsplit
  version: "1.0.0"

source:
  fn: fsplit-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/f/fsplit/fsplit-1.0.0.tar.gz
  md5: bbfb0bcd586ca6435145d58a88d8ba7d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fsplit = fsplit:main
    #
    # Would create an entry point called fsplit that calls fsplit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - fsplit
    - fsplit.parse
    - fsplit.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/leosartaj/autosign
  license: MIT License
  summary: 'Split files into small portable chunks'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
