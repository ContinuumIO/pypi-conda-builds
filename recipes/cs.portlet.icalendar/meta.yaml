package:
  name: cs.portlet.icalendar
  version: "1.0"

source:
  fn: cs.portlet.icalendar-1.0.zip
  url: https://pypi.python.org/packages/source/c/cs.portlet.icalendar/cs.portlet.icalendar-1.0.zip
  md5: 97d32cd02bcd33c2b4a39b595d4dccfb
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cs.portlet.icalendar = cs.portlet.icalendar:main
    #
    # Would create an entry point called cs.portlet.icalendar that calls cs.portlet.icalendar.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - icalendar

  run:
    - python
    - setuptools
    - setuptools
    - icalendar

test:
  # Python imports
  imports:
    - cs
    - cs.portlet
    - cs.portlet.icalendar

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/codesyntax/cs.portlet.icalendar/
  license: GPL
  summary: 'A portlet to show a icalendar file events in a portlet'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
