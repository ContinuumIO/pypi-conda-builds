package:
  name: instax
  version: "0.1.6"

source:
  fn: instax-0.1.6.tar.gz
  url: https://pypi.python.org/packages/source/i/instax/instax-0.1.6.tar.gz
  md5: f70d56d68d5652cb100aec62a32e6cfd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - instax = instax:main
    #
    # Would create an entry point called instax that calls instax.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - statsd >=0.5.1

  run:
    - python
    - statsd >=0.5.1

test:
  # Python imports
  imports:
    - instax

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/cyberdelia/instax
  license: instayc
  summary: 'Making celery monitoring easier.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
