package:
  name: kotti_gallery
  version: "0.2.2"

source:
  fn: kotti_gallery-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/k/kotti_gallery/kotti_gallery-0.2.2.tar.gz
  md5: 20e2e51698ac07041e82dfb20d7b7eb7
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kotti_gallery = kotti_gallery:main
    #
    # Would create an entry point called kotti_gallery that calls kotti_gallery.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - babel
    - kotti >=0.8dev
    - js.bootstrap_image_gallery
    - lingua >=1.3

  run:
    - python
    - setuptools
    - babel
    - kotti >=0.8dev
    - js.bootstrap_image_gallery
    - lingua >=1.3

test:
  # Python imports
  imports:
    - kotti_gallery

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/disko/kotti_gallery
  license: BSD-derived (http://www.repoze.org/LICENSE.txt)
  summary: 'Add an image gallery to your Kotti site'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
