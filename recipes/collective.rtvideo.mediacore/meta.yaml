package:
  name: collective.rtvideo.mediacore
  version: "2.0.1"

source:
  fn: collective.rtvideo.mediacore-2.0.1.zip
  url: https://pypi.python.org/packages/source/c/collective.rtvideo.mediacore/collective.rtvideo.mediacore-2.0.1.zip
  md5: 805fc012ec457a3e8a26db30986fb5ef
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.rtvideo.mediacore = collective.rtvideo.mediacore:main
    #
    # Would create an entry point called collective.rtvideo.mediacore that calls collective.rtvideo.mediacore.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - redturtle.video >=0.6.0
    - python-cjson

  run:
    - python
    - setuptools
    - setuptools
    - redturtle.video >=0.6.0
    - python-cjson

test:
  # Python imports
  imports:
    - collective
    - collective.rtvideo
    - collective.rtvideo.mediacore
    - collective.rtvideo.mediacore.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: GPL
  summary: 'RedTurtle Video integration for MediaCore'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
