package:
  name: docutils_react_docgen
  version: "0.1.0"

source:
  fn: docutils_react_docgen-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/docutils_react_docgen/docutils_react_docgen-0.1.0.tar.gz
  md5: 94c2d0faf0769c2a0e7983209d16b4d1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - docutils_react_docgen = docutils_react_docgen:main
    #
    # Would create an entry point called docutils_react_docgen that calls docutils_react_docgen.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sphinx-rtd-theme >=0.1.7,<1

  run:
    - python
    - sphinx-rtd-theme >=0.1.7,<1

test:
  # Python imports
  imports:
    - docutils_react_docgen

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/pwexler/docutils_react_docgen
  license: MIT
  summary: 'docutils extension which converts react-docgen output to restructured text'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
