package:
  name: django-uwsgi
  version: "0.1.3"

source:
  fn: django-uwsgi-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-uwsgi/django-uwsgi-0.1.3.tar.gz
  md5: 0a9352b899d768d467eed3ca0566f3d3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-uwsgi = django-uwsgi:main
    #
    # Would create an entry point called django-uwsgi that calls django-uwsgi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_uwsgi
    - django_uwsgi.emperor
    - django_uwsgi.management
    - django_uwsgi.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/unbit/django-uwsgi
  license: MIT License
  summary: 'uWSGI stuff for Django projects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
