package:
  name: hotdate
  version: "0.4.5"

source:
  fn: hotdate-0.4.5.tar.gz
  url: https://pypi.python.org/packages/source/h/hotdate/hotdate-0.4.5.tar.gz
  md5: a1ee64076f5099a33a0e9278ac42276a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hotdate = hotdate:main
    #
    # Would create an entry point called hotdate that calls hotdate.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six
    - python-dateutil
    - freezegun

  run:
    - python
    - six
    - python-dateutil
    - freezegun

test:
  # Python imports
  imports:
    - hotdate

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/hotdate/
  license: LICENSE
  summary: 'Intuitive date formatting.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
