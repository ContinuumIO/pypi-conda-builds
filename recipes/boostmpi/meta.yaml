package:
  name: boostmpi
  version: "1.38.0.1"

source:
  fn: boostmpi-1.38.0.1.tar.gz
  url: https://pypi.python.org/packages/source/b/boostmpi/boostmpi-1.38.0.1.tar.gz
  md5: 24054b56b9e6fe25df3208fc8c9872bb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - boostmpi = boostmpi:main
    #
    # Would create an entry point called boostmpi that calls boostmpi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - boostmpi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://mathema.tician.de/software/boostmpi
  license: Boost Software License V1
  summary: 'Boost MPI Python wrappers'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
