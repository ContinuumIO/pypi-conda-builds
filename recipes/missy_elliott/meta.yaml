package:
  name: missy_elliott
  version: "0.1"

source:
  fn: missy_elliott-0.1.tar.gz
  url: https://pypi.python.org/packages/source/m/missy_elliott/missy_elliott-0.1.tar.gz
  md5: 5945db6e96657a637de3e5bb601bb0fa
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - missy_elliott = missy_elliott:main
    #
    # Would create an entry point called missy_elliott that calls missy_elliott.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - missy_elliott

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/maznika/missy_elliott
  license: UNKNOWN
  summary: 'A fun XKCD comic ripoff'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
