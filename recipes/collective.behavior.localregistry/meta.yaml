package:
  name: collective.behavior.localregistry
  version: "1.0b2"

source:
  fn: collective.behavior.localregistry-1.0b2.zip
  url: https://pypi.python.org/packages/source/c/collective.behavior.localregistry/collective.behavior.localregistry-1.0b2.zip
  md5: 26941cfc0435175cb26157b41faf0880
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.behavior.localregistry = collective.behavior.localregistry:main
    #
    # Would create an entry point called collective.behavior.localregistry that calls collective.behavior.localregistry.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - plone.app.registry
    - plone.behavior
    - plone.registry
    - products.cmfcore
    - setuptools
    - zope.component
    - zope.event
    - zope.i18nmessageid
    - zope.interface

  run:
    - python
    - setuptools
    - plone.app.registry
    - plone.behavior
    - plone.registry
    - products.cmfcore
    - setuptools
    - zope.component
    - zope.event
    - zope.i18nmessageid
    - zope.interface

test:
  # Python imports
  imports:
    - collective
    - collective.behavior
    - collective.behavior.localregistry
    - collective.behavior.localregistry.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.behavior.localregistry
  license: GNU General Public License v2 (GPLv2)
  summary: 'Dexterity behavior to add a local plone.app.registry for           content types, it adds a local component with a layered           proxy registry.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
