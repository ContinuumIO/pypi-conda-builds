package:
  name: wtdojo
  version: "0.1.4"

source:
  fn: wtdojo-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/w/wtdojo/wtdojo-0.1.4.tar.gz
  md5: 1e6557420e0e7b7984ec76a7d8157533
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - wtdojo = wtdojo:main
    #
    # Would create an entry point called wtdojo that calls wtdojo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - wtforms

  run:
    - python
    - wtforms

test:
  # Python imports
  imports:
    - wtdojo
    - wtdojo.fields
    - wtdojo.widgets

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/wtdojo
  license: UNKNOWN
  summary: 'Dojo javascript toolkit support for WTForms'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
