package:
  name: cpolymer
  version: "0.34"

source:
  fn: cpolymer-0.34.tar.gz
  url: https://pypi.python.org/packages/source/c/cpolymer/cpolymer-0.34.tar.gz
  md5: 16c709d3003647f368fdfd4db6e57a64
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cpolymer = cpolymer:main
    #
    # Would create an entry point called cpolymer that calls cpolymer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - cpolymer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jeammimi/cpolymer
  license: MIT
  summary: 'Creating initial configuration for polymer (to work with lammps)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
