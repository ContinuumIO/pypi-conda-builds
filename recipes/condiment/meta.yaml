package:
  name: condiment
  version: "0.5.2"

source:
  fn: condiment-0.5.2.tar.gz
  url: https://pypi.python.org/packages/source/c/condiment/condiment-0.5.2.tar.gz
  md5: 400101c672a0c987c6f9712df5ff1ddf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - condiment = condiment:main
    #
    # Would create an entry point called condiment that calls condiment.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - condiment

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/tito/condiment
  license: zlib/libpng License
  summary: 'Conditionally include code according to environment variables'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
