package:
  name: api_pagination
  version: "1.0.0"

source:
  fn: api_pagination-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/a/api_pagination/api_pagination-1.0.0.tar.gz
  md5: bf1dc7c8011e0747aa409e4e85c38a20
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - api_pagination = api_pagination:main
    #
    # Would create an entry point called api_pagination that calls api_pagination.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - api_pagination
    - api_pagination.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/underdogio/api-pagination
  license: MIT
  summary: 'Pagination calculator designed for APIs'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
