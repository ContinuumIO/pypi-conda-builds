package:
  name: flask-iniconfig
  version: "0.0.3"

source:
  fn: Flask-INIConfig-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-INIConfig/Flask-INIConfig-0.0.3.tar.gz
  md5: 091a050ffac97194384954295b3ee925
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-iniconfig = flask-iniconfig:main
    #
    # Would create an entry point called flask-iniconfig that calls flask-iniconfig.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask

  run:
    - python
    - flask

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/wampeter/flask-iniconfig/
  license: BSD License
  summary: 'A flask extension to load ini files via config'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
