package:
  name: collective.anotherdynamicgroupsplugin
  version: "1.0.0"

source:
  fn: collective.anotherdynamicgroupsplugin-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.anotherdynamicgroupsplugin/collective.anotherdynamicgroupsplugin-1.0.0.tar.gz
  md5: cbb6628c476c9095d8968e948614476b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.anotherdynamicgroupsplugin = collective.anotherdynamicgroupsplugin:main
    #
    # Would create an entry point called collective.anotherdynamicgroupsplugin that calls collective.anotherdynamicgroupsplugin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone
    - z3c.autoinclude
    - plone.api
    - collective.monkeypatcher

  run:
    - python
    - setuptools
    - setuptools
    - plone
    - z3c.autoinclude
    - plone.api
    - collective.monkeypatcher

test:
  # Python imports
  imports:
    - collective
    - collective.anotherdynamicgroupsplugin
    - collective.anotherdynamicgroupsplugin.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/collective/collective.anotherdynamicgroupsplugin
  license: GPL version 2
  summary: 'Another way to define dynamic user groups in Plone.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
