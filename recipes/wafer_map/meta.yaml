package:
  name: wafer_map
  version: "1.0.8"

source:
  fn: wafer_map-1.0.8.zip
  url: https://pypi.python.org/packages/source/w/wafer_map/wafer_map-1.0.8.zip
  md5: 54eb9b5e88c340829f445455462c01be
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - wafer_map = wafer_map:main
    #
    # Would create an entry point called wafer_map that calls wafer_map.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - wafer_map
    - wafer_map.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dougthor42/wafer_map
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'Semiconductor Wafer Mapping'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
