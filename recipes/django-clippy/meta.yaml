package:
  name: django-clippy
  version: "1.0p1"

source:
  fn: django-clippy-1.0p1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-clippy/django-clippy-1.0p1.tar.gz
  md5: 4390872d74e7ff18ee79ca9d68acce25
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-clippy = django-clippy:main
    #
    # Would create an entry point called django-clippy that calls django-clippy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - clippy
    - clippy.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/mdornseif/django-clippy#readme
  license: BSD License
  summary: 'Flash based template tag for Django to allow copying the clipboard'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
