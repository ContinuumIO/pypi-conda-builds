package:
  name: future_value
  version: "1.1.0"

source:
  fn: future_value-1.1.0.zip
  url: https://pypi.python.org/packages/source/f/future_value/future_value-1.1.0.zip
  md5: c09f21c889f1e95fc75168f133ec0be6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - future_value = future_value:main
    #
    # Would create an entry point called future_value that calls future_value.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - future_value

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'Define a future or present value of the taxes. For economics'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
