package:
  name: cubicweb-uitest
  version: "0.1.0"

source:
  fn: cubicweb-uitest-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/c/cubicweb-uitest/cubicweb-uitest-0.1.0.tar.gz
  md5: 14a510e94b3acf2d3b47cb49438c9b39
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cubicweb-uitest = cubicweb-uitest:main
    #
    # Would create an entry point called cubicweb-uitest that calls cubicweb-uitest.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cubicweb >=3.19.0
    - cubicweb-relationwidget
    - cubicweb-searchui
    - cwtags

  run:
    - python
    - cubicweb >=3.19.0
    - cubicweb-relationwidget
    - cubicweb-searchui
    - cwtags

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.cubicweb.org/project/cubicweb-uitest
  license: LGPL
  summary: 'easy demo styles'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
