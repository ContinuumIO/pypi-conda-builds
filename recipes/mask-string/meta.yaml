package:
  name: mask-string
  version: "0.0.4"

source:
  fn: mask-string-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/m/mask-string/mask-string-0.0.4.tar.gz
  md5: 03f1524efd8670e9e6d079166b8edbc4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mask-string = mask-string:main
    #
    # Would create an entry point called mask-string that calls mask-string.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://gihtub.com/jarvys/mask-string
  license: UNKNOWN
  summary: 'help masking string especialy for credit card number and phone number'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
