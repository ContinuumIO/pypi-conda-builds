package:
  name: django-elasticfilter
  version: "0.1.0"

source:
  fn: django-elasticfilter-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-elasticfilter/django-elasticfilter-0.1.0.tar.gz
  md5: 3c06b1052ac402690aa22d0b84a2de43
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-elasticfilter = django-elasticfilter:main
    #
    # Would create an entry point called django-elasticfilter that calls django-elasticfilter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6
    - elasticutils >=0.10

  run:
    - python
    - django >=1.6
    - elasticutils >=0.10

test:
  # Python imports
  imports:
    - elasticfilter

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/tim_heap/django-elasticfilter/
  license: BSD License
  summary: 'Create forms that run Elasticsearch queries'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
