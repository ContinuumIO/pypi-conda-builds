package:
  name: udemy-dl
  version: "0.1.4"

source:
  fn: udemy-dl-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/u/udemy-dl/udemy-dl-0.1.4.tar.gz
  md5: 55bba3b7dee57087e9d10ef56875aad7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - udemy-dl = udemy-dl:main
    #
    # Would create an entry point called udemy-dl that calls udemy-dl.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests
    - wget

  run:
    - python
    - requests
    - wget

test:
  # Python imports
  imports:
    - udemy_dl

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/gaganpreet/udemy-dl
  license: UNKNOWN
  summary: 'Download videos from Udemy for personal offline use'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
