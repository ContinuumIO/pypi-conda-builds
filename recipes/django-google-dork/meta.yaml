package:
  name: django-google-dork
  version: "0.2"

source:
  fn: django-google-dork-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-google-dork/django-google-dork-0.2.tar.gz
  md5: 1008f25e53ba048b7c048c42d6385d07
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-google-dork = django-google-dork:main
    #
    # Would create an entry point called django-google-dork that calls django-google-dork.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - django-model-utils

  run:
    - python
    - django
    - django-model-utils

test:
  # Python imports
  imports:
    - django_google_dork

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/chgans/django-google-dork
  license: BSD License
  summary: 'A django app to manage Google dorks, run them and cache results.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
