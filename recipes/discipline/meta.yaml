package:
  name: discipline
  version: "0.9.1"

source:
  fn: Discipline-0.9.1.tar.gz
  url: https://pypi.python.org/packages/source/D/Discipline/Discipline-0.9.1.tar.gz
  md5: dbd465d6b715047461e13c2198b5ec06
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - discipline = discipline:main
    #
    # Would create an entry point called discipline that calls discipline.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - discipline
    - discipline.management
    - discipline.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/alexeiboronine/discipline
  license: MIT License
  summary: 'Discipline: model version control for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
