package:
  name: django-social-tags
  version: "0.3a.0"

source:
  fn: django-social-tags-0.3a.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-social-tags/django-social-tags-0.3a.0.tar.gz
  md5: 3c064f5fe099ad5010675dd103df3661
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-social-tags = django-social-tags:main
    #
    # Would create an entry point called django-social-tags that calls django-social-tags.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-sekizai

  run:
    - python
    - django-sekizai

test:
  # Python imports
  imports:
    - social_tags
    - social_tags.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/pascalmouret/django-social
  license: BSD License
  summary: 'Social meta-tag handler for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
