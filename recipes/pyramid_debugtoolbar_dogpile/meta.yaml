package:
  name: pyramid_debugtoolbar_dogpile
  version: "0.0.4"

source:
  fn: pyramid_debugtoolbar_dogpile-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/p/pyramid_debugtoolbar_dogpile/pyramid_debugtoolbar_dogpile-0.0.4.tar.gz
  md5: 19d27f019d7487393b6f88fbfbc187b2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pyramid_debugtoolbar_dogpile = pyramid_debugtoolbar_dogpile:main
    #
    # Would create an entry point called pyramid_debugtoolbar_dogpile that calls pyramid_debugtoolbar_dogpile.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyramid_debugtoolbar >=2.2

  run:
    - python
    - pyramid_debugtoolbar >=2.2

test:
  # Python imports
  imports:
    - pyramid_debugtoolbar_dogpile
    - pyramid_debugtoolbar_dogpile.panels

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jvanasco/pyramid_debugtoolbar_dogpile
  license: MIT License
  summary: 'dogpile support for pyramid_debugtoolbar'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
