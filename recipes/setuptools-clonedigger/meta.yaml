package:
  name: setuptools-clonedigger
  version: "0.1"

source:
  fn: setuptools-clonedigger-0.1.tar.gz
  url: https://pypi.python.org/packages/source/s/setuptools-clonedigger/setuptools-clonedigger-0.1.tar.gz
  md5: ae08051b373920cdeeb3a16dde2c40a6
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - setuptools-clonedigger = setuptools-clonedigger:main
    #
    # Would create an entry point called setuptools-clonedigger that calls setuptools-clonedigger.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - clonedigger

  run:
    - python
    - setuptools
    - clonedigger

test:
  # Python imports
  imports:
    - setuptools_clonedigger

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/johnnoone/setuptools-clonedigger
  license: BSD License
  summary: 'Setuptools command for clonedigger'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
