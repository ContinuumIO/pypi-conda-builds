package:
  name: plone.recipe.runscript
  version: "0.3.1"

source:
  fn: plone.recipe.runscript-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/p/plone.recipe.runscript/plone.recipe.runscript-0.3.1.tar.gz
  md5: 7f0a29e04f08edc24005a69f642608a0
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.recipe.runscript = plone.recipe.runscript:main
    #
    # Would create an entry point called plone.recipe.runscript that calls plone.recipe.runscript.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout

test:
  # Python imports
  imports:
    - plone
    - plone.recipe
    - plone.recipe.runscript

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://svn.plone.org/svn/collective/buildout/plone.recipe.runscript/tags/0.3.1
  license: GPL
  summary: 'A buildout recipe to run a zope script'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
