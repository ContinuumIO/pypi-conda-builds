package:
  name: django-simple-s3storage
  version: "0.1"

source:
  fn: django-simple-s3storage-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-simple-s3storage/django-simple-s3storage-0.1.tar.gz
  md5: 0afb7da29dd4083a226131da681c5ec1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-simple-s3storage = django-simple-s3storage:main
    #
    # Would create an entry point called django-simple-s3storage that calls django-simple-s3storage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - boto
    - django ==1.4

  run:
    - python
    - boto
    - django ==1.4

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.kaelten.com/django-simple-s3storage
  license: BSD License
  summary: 'A simple django storage backend for Amazon S3 built ontop of Boto.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
