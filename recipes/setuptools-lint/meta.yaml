package:
  name: setuptools-lint
  version: "0.3"

source:
  fn: setuptools-lint-0.3.tar.gz
  url: https://pypi.python.org/packages/source/s/setuptools-lint/setuptools-lint-0.3.tar.gz
  md5: 1a44bb3e64fd173579eb22d4b15e7869
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - setuptools-lint = setuptools-lint:main
    #
    # Would create an entry point called setuptools-lint that calls setuptools-lint.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pylint

  run:
    - python
    - setuptools
    - pylint

test:
  # Python imports
  imports:
    - setuptools_lint

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/johnnoone/setuptools-pylint
  license: BSD License
  summary: 'Setuptools command for pylint'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
