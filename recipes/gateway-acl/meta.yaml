package:
  name: gateway-acl
  version: "1.0"

source:
  fn: gateway-acl-1.0.tar.gz
  url: https://pypi.python.org/packages/source/g/gateway-acl/gateway-acl-1.0.tar.gz
  md5: 8ac42791c75871b6ea05f9c010c47f57
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gateway-acl = gateway-acl:main
    #
    # Would create an entry point called gateway-acl that calls gateway-acl.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - __plugin__
    - __plugin__.gateway_acl
    - acl
    - acl.api
    - acl.core
    - acl.core.impl
    - acl.core.impl.processor
    - acl.core.impl.processor.assembler
    - acl.core.impl.processor.gateway
    - acl.impl
    - acl.meta

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.sourcefabric.org/en/superdesk/
  license: GPL v3
  summary: 'This plugin provides the service gateways.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
