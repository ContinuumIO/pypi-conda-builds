package:
  name: django-flash
  version: "1.8"

source:
  fn: django-flash-1.8.tar.gz
  url: https://pypi.python.org/packages/source/d/django-flash/django-flash-1.8.tar.gz
  md5: 1d2da9ed3bf755f19d19308f7b456aca
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-flash = django-flash:main
    #
    # Would create an entry point called django-flash that calls django-flash.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.0_final

  run:
    - python
    - django >=1.0_final

test:
  # Python imports
  imports:
    - djangoflash
    - djangoflash.codec
    - djangoflash.storage
    - djangoflash.tests
    - djangoflash.tests.testproj
    - djangoflash.tests.testproj.app

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://djangoflash.destaquenet.com/
  license: BSD License
  summary: 'Django-flash is a simple Django extension which provides support for Rails-like flash messages.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
