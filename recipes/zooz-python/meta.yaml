package:
  name: zooz-python
  version: "0.4"

source:
  fn: zooz-python-0.4.tar.gz
  url: https://pypi.python.org/packages/source/z/zooz-python/zooz-python-0.4.tar.gz
  md5: e1aab5cbffb17b81d02116f070f85408
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zooz-python = zooz-python:main
    #
    # Would create an entry point called zooz-python that calls zooz-python.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests ==1.2.3

  run:
    - python
    - requests ==1.2.3

test:
  # Python imports
  imports:
    - zooz

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jneight/zooz-python
  license: Apache 2.0
  summary: 'Python client for ZooZ payments API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
