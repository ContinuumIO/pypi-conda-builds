package:
  name: bda.plone.wfintranet
  version: "1.0"

source:
  fn: bda.plone.wfintranet-1.0.tar.gz
  url: https://pypi.python.org/packages/source/b/bda.plone.wfintranet/bda.plone.wfintranet-1.0.tar.gz
  md5: 4fb283e2547fc0bbf8284d22f20fec9b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bda.plone.wfintranet = bda.plone.wfintranet:main
    #
    # Would create an entry point called bda.plone.wfintranet that calls bda.plone.wfintranet.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone

  run:
    - python
    - setuptools
    - setuptools
    - plone

test:
  # Python imports
  imports:
    - bda
    - bda.plone
    - bda.plone.wfintranet

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: GNU General Public License (GPL)
  summary: 'Intranet Workflow by Bluedynamics Alliance'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
