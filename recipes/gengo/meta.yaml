package:
  name: gengo
  version: "0.1.21"

source:
  fn: gengo-0.1.21.tar.gz
  url: https://pypi.python.org/packages/source/g/gengo/gengo-0.1.21.tar.gz
  md5: e642cf776fca263b3d3a679b574095f6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gengo = gengo:main
    #
    # Would create an entry point called gengo that calls gengo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests ==2.2.1

  run:
    - python
    - requests ==2.2.1

test:
  # Python imports
  imports:
    - gengo

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/gengo/gengo-python
  license: BSD License
  summary: 'Official Python library for interfacing with the Gengo API.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
