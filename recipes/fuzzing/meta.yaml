package:
  name: fuzzing
  version: "0.3.0"

source:
  fn: fuzzing-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/f/fuzzing/fuzzing-0.3.0.tar.gz
  md5: e9fc50af6b12eea7d04859a7ff4d5949
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fuzzing = fuzzing:main
    #
    # Would create an entry point called fuzzing that calls fuzzing.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - sphinx
    - wrapt
    - pyyaml
    - argh
    - pathtools
    - setuptools

  run:
    - python
    - sphinx
    - wrapt
    - pyyaml
    - argh
    - pathtools
    - setuptools

test:
  # Python imports
  imports:
    - fuzzing
    - gp_decorators

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/stbraun/fuzzing
  license: MIT License
  summary: 'Tools for stress testing applications.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
