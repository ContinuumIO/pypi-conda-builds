package:
  name: django-sopa
  version: "0.0.2"

source:
  fn: django-sopa-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-sopa/django-sopa-0.0.2.tar.gz
  md5: ea4bbd453b5eb7a16de5f5be4379e151
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-sopa = django-sopa:main
    #
    # Would create an entry point called django-sopa that calls django-sopa.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - sopa
    - sopa.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/fcurella/django-sopa
  license: MIT License
  summary: 'A django app for all your censoring needs.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
