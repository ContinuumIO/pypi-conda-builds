package:
  name: django_autocomplete_foreignkey
  version: "0.0.1"

source:
  fn: django_autocomplete_foreignkey-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django_autocomplete_foreignkey/django_autocomplete_foreignkey-0.0.1.tar.gz
  md5: a6002c71a5e50983ae642ac955c25bd9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_autocomplete_foreignkey = django_autocomplete_foreignkey:main
    #
    # Would create an entry point called django_autocomplete_foreignkey that calls django_autocomplete_foreignkey.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - autocomplete
    - django_autocomplete_foreignkey

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/redhog/django-autocomplete
  license: GPL
  summary: 'AutoComplete for ForeignKey and ManyToManyField.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
