package:
  name: django-juno-testrunner
  version: "0.3.1"

source:
  fn: django-juno-testrunner-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-juno-testrunner/django-juno-testrunner-0.3.1.tar.gz
  md5: 397d95daf664bfdc8ba20259cbee28f0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-juno-testrunner = django-juno-testrunner:main
    #
    # Would create an entry point called django-juno-testrunner that calls django-juno-testrunner.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - colorama

  run:
    - python
    - colorama

test:
  # Python imports
  imports:
    - junorunner

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/yunojuno/django-juno-testrunner.git
  license: MIT License
  summary: 'A more useful (and slightly more glamorous) test runner for Django 1.6+ from the folks at YunoJuno'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
