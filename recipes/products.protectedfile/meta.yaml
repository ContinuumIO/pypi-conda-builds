package:
  name: products.protectedfile
  version: "0.1.1"

source:
  fn: Products.ProtectedFile-0.1.1.zip
  url: https://pypi.python.org/packages/source/P/Products.ProtectedFile/Products.ProtectedFile-0.1.1.zip
  md5: a4e4d8bf14f8394e663034349973aa89
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - products.protectedfile = products.protectedfile:main
    #
    # Would create an entry point called products.protectedfile that calls products.protectedfile.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - Products
    - Products.ProtectedFile
    - Products.ProtectedFile.Extensions
    - Products.ProtectedFile.tests
    - Products.ProtectedFile.types

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/protected-file
  license: GPL
  summary: "'Anonymous users must provide a valid email to download the file.'"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
