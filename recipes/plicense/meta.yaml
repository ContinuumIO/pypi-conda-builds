package:
  name: plicense
  version: "0.2.1"

source:
  fn: plicense-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/p/plicense/plicense-0.2.1.tar.gz
  md5: d5bfd41d48bac95de3f35724396a2d21
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plicense = plicense:main
    #
    # Would create an entry point called plicense that calls plicense.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - nose

  run:
    - python
    - nose

test:
  # Python imports
  imports:
    - plicense
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dsoprea/LicensePrepend
  license: GPL 2
  summary: 'Make sure all source files have your standard licensing stub at the top.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
