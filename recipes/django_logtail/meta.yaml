package:
  name: django_logtail
  version: "0.2.0"

source:
  fn: django_logtail-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django_logtail/django_logtail-0.2.0.tar.gz
  md5: 6aeab79af9d0e2019fc0ec58b3869838
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_logtail = django_logtail:main
    #
    # Would create an entry point called django_logtail that calls django_logtail.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - setuptools
    - django

test:
  # Python imports
  imports:
    - django_logtail

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/alex2/django_logtail
  license: The MIT License
  summary: 'A log viewing and tailing utility accessible via a Django admin panel'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
