package:
  name: coop-agenda
  version: "0.1.3"

source:
  fn: coop-agenda-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/c/coop-agenda/coop-agenda-0.1.3.tar.gz
  md5: d1cee4863aea8fc2eb07fa3d54b6eeb5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - coop-agenda = coop-agenda:main
    #
    # Would create an entry point called coop-agenda that calls coop-agenda.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - coop_agenda
    - coop_agenda.conf

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/quinode/coop_agenda/
  license: BSD License
  summary: 'Companion app for django-coop, a fork of swingtime'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
