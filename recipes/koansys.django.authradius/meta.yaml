package:
  name: koansys.django.authradius
  version: "1.0.3"

source:
  fn: koansys.django.authradius-1.0.3.tar.gz
  url: https://pypi.python.org/packages/source/k/koansys.django.authradius/koansys.django.authradius-1.0.3.tar.gz
  md5: 626622cdc36087e9081084e054d561f0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - koansys.django.authradius = koansys.django.authradius:main
    #
    # Would create an entry point called koansys.django.authradius that calls koansys.django.authradius.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyrad >=1.1

  run:
    - python
    - pyrad >=1.1

test:
  # Python imports
  imports:
    - koansys
    - koansys.django

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://koansys-django-authradius.googlecode.com/
  license: BSD License
  summary: 'Django can authenticate against a RADIUS server'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
