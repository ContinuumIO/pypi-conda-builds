package:
  name: astgen
  version: "0.1.6.12"

source:
  fn: astgen-0.1.6.12.tar.gz
  url: https://pypi.python.org/packages/source/a/astgen/astgen-0.1.6.12.tar.gz
  md5: d2b37106d0ab2e233556527703feec5d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - astgen = astgen:main
    #
    # Would create an entry point called astgen that calls astgen.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - jinja2 >=2.7

  run:
    - python
    - jinja2 >=2.7

test:
  # Python imports
  imports:
    - astgen

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/panyam/astgen/
  license: MIT License
  summary: 'A generator for Abstract Syntax Trees.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
