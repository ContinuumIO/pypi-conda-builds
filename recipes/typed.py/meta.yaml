package:
  name: typed.py
  version: "1.0.0.1"

source:
  fn: typed.py-1.0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/t/typed.py/typed.py-1.0.0.1.tar.gz
  md5: ba09d0d774423c830e8b6cd3c2bc6441
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - typed.py = typed.py:main
    #
    # Would create an entry point called typed.py that calls typed.py.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - typed

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/typed.py/
  license: GNU General Public License (GPL)
  summary: 'Inquisitive types and multiple dispatch, now for Python!'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
