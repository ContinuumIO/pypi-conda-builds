package:
  name: iotrelay-eagle
  version: "1.0.1"

source:
  fn: iotrelay-eagle-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/i/iotrelay-eagle/iotrelay-eagle-1.0.1.tar.gz
  md5: 3511721a0599da3e7ae13a5afac094db
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - iotrelay-eagle = iotrelay-eagle:main
    #
    # Would create an entry point called iotrelay-eagle that calls iotrelay-eagle.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - iotrelay
    - meter-reader

  run:
    - python
    - setuptools
    - iotrelay
    - meter-reader

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/eman/iotrelay-eagle
  license: BSD License
  summary: 'IoT Relay plugin for the Eagle\xe2\x84\xa2 Home Energy Gateway'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
