package:
  name: count_min_sketch
  version: "1.0.1"

source:
  fn: count_min_sketch-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/count_min_sketch/count_min_sketch-1.0.1.tar.gz
  md5: b144723797a7c45521308246cf3443cd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - count_min_sketch = count_min_sketch:main
    #
    # Would create an entry point called count_min_sketch that calls count_min_sketch.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - count_min_sketch

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/AWNystrom/CountMinSketch
  license: Apache Software License
  summary: 'A fast Count-Min Sketch data structure.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
