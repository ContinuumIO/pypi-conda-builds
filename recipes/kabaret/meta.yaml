package:
  name: kabaret
  version: "0.1.0b3"

source:
  fn: kabaret-0.1.0b3.zip
  url: https://pypi.python.org/packages/source/k/kabaret/kabaret-0.1.0b3.zip
  md5: f5e37bd17b63c97f37e0117825a773e0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kabaret = kabaret:main
    #
    # Would create an entry point called kabaret that calls kabaret.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - kabaret

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://packages.python.org/kabaret
  license: GNU Lesser General Public License v3 or later (LGPLv3+)
  summary: 'Digital Creation Studio Framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
