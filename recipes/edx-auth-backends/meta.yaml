package:
  name: edx-auth-backends
  version: "0.1.3"

source:
  fn: edx-auth-backends-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/e/edx-auth-backends/edx-auth-backends-0.1.3.tar.gz
  md5: b92e11cb851eb80f2f8d2827f2ae92e6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - edx-auth-backends = edx-auth-backends:main
    #
    # Would create an entry point called edx-auth-backends that calls edx-auth-backends.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.7
    - python-social-auth >=0.2.3

  run:
    - python
    - django >=1.7
    - python-social-auth >=0.2.3

test:
  # Python imports
  imports:
    - auth_backends
    - auth_backends.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/edx/auth-backends
  license: GNU Affero General Public License v3
  summary: 'Custom edX authentication backends and pipeline steps'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
