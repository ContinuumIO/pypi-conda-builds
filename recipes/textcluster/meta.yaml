package:
  name: textcluster
  version: "0.0.2"

source:
  fn: textcluster-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/t/textcluster/textcluster-0.0.2.tar.gz
  md5: 90d34b8d53bce9266af9f1f0374474a8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - textcluster = textcluster:main
    #
    # Would create an entry point called textcluster that calls textcluster.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - stemming

  run:
    - python
    - stemming

test:
  # Python imports
  imports:
    - textcluster

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/davedash/textcluster
  license: BSD License
  summary: 'Tool to cluster documents according to text similarity.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
