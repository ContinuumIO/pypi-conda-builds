package:
  name: redis-event-tracker
  version: "0.0.9"

source:
  fn: redis-event-tracker-0.0.9.tar.gz
  url: https://pypi.python.org/packages/source/r/redis-event-tracker/redis-event-tracker-0.0.9.tar.gz
  md5: 7514437d6c24d7ba5437c5dd3bdef562
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - redis-event-tracker = redis-event-tracker:main
    #
    # Would create an entry point called redis-event-tracker that calls redis-event-tracker.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - redisEventTracker
    - redisEventTracker.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Keyintegrity/redisEventTracker
  license: MIT License
  summary: 'Track your events in redis'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
