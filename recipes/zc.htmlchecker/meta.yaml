package:
  name: zc.htmlchecker
  version: "0.1.0"

source:
  fn: zc.htmlchecker-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/z/zc.htmlchecker/zc.htmlchecker-0.1.0.tar.gz
  md5: ec11f45b23a9fbd24662902fa35fd975
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zc.htmlchecker = zc.htmlchecker:main
    #
    # Would create an entry point called zc.htmlchecker that calls zc.htmlchecker.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - beautifulsoup4
    - html5lib

  run:
    - python
    - setuptools
    - setuptools
    - beautifulsoup4
    - html5lib

test:
  # Python imports
  imports:
    - zc
    - zc.htmlchecker

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: ZPL 2.1
  summary: 'HTML/DOM Checker'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
