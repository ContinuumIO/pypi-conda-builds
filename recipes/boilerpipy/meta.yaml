package:
  name: boilerpipy
  version: "0.2.5"

source:
  fn: boilerpipy-0.2.5.tar.gz
  url: https://pypi.python.org/packages/source/b/boilerpipy/boilerpipy-0.2.5.tar.gz
  md5: fc1fd5d9b3edec5fdf283ee0127d9db7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - boilerpipy = boilerpipy:main
    #
    # Would create an entry point called boilerpipy that calls boilerpipy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - lxml
    - beautifulsoup4
    - urlparse2

  run:
    - python
    - lxml
    - beautifulsoup4
    - urlparse2

test:
  # Python imports
  imports:
    - boilerpipy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/harshavardhana/boilerpipy.git
  license: Apache Software License
  summary: 'Readability/Boilerpipe extractor in Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
