package:
  name: flask-xsrf
  version: "1.0.1"

source:
  fn: flask-xsrf-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/f/flask-xsrf/flask-xsrf-1.0.1.tar.gz
  md5: 89c42c467d29bb083f20d4dcb72b3c6f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-xsrf = flask-xsrf:main
    #
    # Would create an entry point called flask-xsrf that calls flask-xsrf.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask ==0.9
    - werkzeug ==0.8.3
    - blinker ==1.2

  run:
    - python
    - flask ==0.9
    - werkzeug ==0.8.3
    - blinker ==1.2

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/gregorynicholas/flask-xsrf
  license: MIT License
  summary: 'A Flask extension for XSRF/CSRF protection.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
