package:
  name: hemlock
  version: "0.1.6"

source:
  fn: hemlock-0.1.6.tar.gz
  url: https://pypi.python.org/packages/source/h/hemlock/hemlock-0.1.6.tar.gz
  md5: 9a5ff30ef80eb3586b5de22e86ff1f6c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hemlock = hemlock:main
    #
    # Would create an entry point called hemlock that calls hemlock.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - apscheduler
    - texttable

  run:
    - python
    - apscheduler
    - texttable

test:
  # Python imports
  imports:
    - hemlock
    - hemlock.clients
    - hemlock.clients.file_types
    - hemlock.load_scripts
    - hemlock.www

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://lab41.github.io/Hemlock
  license: LICENSE.txt
  summary: 'Hemlock is a way of providing a common data access layer.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
