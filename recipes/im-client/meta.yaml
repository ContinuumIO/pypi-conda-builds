package:
  name: im-client
  version: "1.2.2"

source:
  fn: IM-client-1.2.2.tar.gz
  url: https://pypi.python.org/packages/source/I/IM-client/IM-client-1.2.2.tar.gz
  md5: ca01fbe10b5760a7c83e1179aa90016c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - im-client = im-client:main
    #
    # Would create an entry point called im-client that calls im-client.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - ply

  run:
    - python
    - ply

test:
  # Python imports
  imports:
    - radl

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.grycap.upv.es/im
  license: GPL version 3, http://www.gnu.org/licenses/gpl-3.0.txt
  summary: 'IM is a tool to manage virtual infrastructures on Cloud deployments'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
