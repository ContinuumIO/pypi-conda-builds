package:
  name: dataprimitives
  version: "0.51"

source:
  fn: DataPrimitives-0.51.tar.gz
  url: https://pypi.python.org/packages/source/D/DataPrimitives/DataPrimitives-0.51.tar.gz
  md5: b1070459c6c09d5530ed8c2a18a9aac5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dataprimitives = dataprimitives:main
    #
    # Would create an entry point called dataprimitives that calls dataprimitives.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - dataprimitives

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/DataPrimitives
  license: Public Domain
  summary: 'Data primitives conversions from ISO 18033 and PKCS#1'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
