package:
  name: annotate
  version: "0.5.1"

source:
  fn: annotate-0.5.1.zip
  url: https://pypi.python.org/packages/source/a/annotate/annotate-0.5.1.zip
  md5: 16265d4b3c2155201d94a42a5471e6d5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - annotate = annotate:main
    #
    # Would create an entry point called annotate that calls annotate.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - annotate

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/annotate/
  license: zlib/libpng License
  summary: "Decorator to set a function's __annotations__ like Py3"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
