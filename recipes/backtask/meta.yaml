package:
  name: backtask
  version: "backtask"

source:
  fn: backtask-backtask.tar.gz
  url: https://pypi.python.org/packages/source/b/backtask/backtask-backtask.tar.gz
  md5: 9fc5138aa90f6cf6b851e6244f90ab5d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - backtask = backtask:main
    #
    # Would create an entry point called backtask that calls backtask.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - backtask

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://backtask-py.sourceforge.net/
  license: GNU Affero General Public License v3 or later (AGPLv3+)
  summary: 'Run functions in a background process'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
