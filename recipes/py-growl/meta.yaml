package:
  name: py-growl
  version: "0.0.7"

source:
  fn: py-Growl-0.0.7.tar.gz
  url: https://pypi.python.org/packages/source/p/py-Growl/py-Growl-0.0.7.tar.gz
  md5: 2880211cb082782b64a43807427c9362
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - py-growl = py-growl:main
    #
    # Would create an entry point called py-growl that calls py-growl.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.assembla.com/spaces/nosegrowl/
  license: UNKNOWN
  summary: 'Python bindings for posting notifications to the Growl daemon'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
