package:
  name: silva.translations
  version: "3.0.2"

source:
  fn: silva.translations-3.0.2.tar.gz
  url: https://pypi.python.org/packages/source/s/silva.translations/silva.translations-3.0.2.tar.gz
  md5: f9e63d5dca5d0e70f980ab5b72647dbb
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - silva.translations = silva.translations:main
    #
    # Would create an entry point called silva.translations that calls silva.translations.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - collective.monkeypatcher
    - infrae.wsgi
    - setuptools
    - zope.i18n
    - zope.i18nmessageid
    - zope.publisher

  run:
    - python
    - collective.monkeypatcher
    - infrae.wsgi
    - setuptools
    - zope.i18n
    - zope.i18nmessageid
    - zope.publisher

test:
  # Python imports
  imports:
    - silva
    - silva.translations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/silvacms/silva.translations
  license: BSD License
  summary: 'Translations files in many languages for the Silva CMS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
