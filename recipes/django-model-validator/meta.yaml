package:
  name: django-model-validator
  version: "0.1"

source:
  fn: django-model-validator-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-model-validator/django-model-validator-0.1.tar.gz
  md5: d1ad55fbd6fabdcefe0be49c8a53bcb8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-model-validator = django-model-validator:main
    #
    # Would create an entry point called django-model-validator that calls django-model-validator.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - model_validator

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/stargazer/django-model-validator/
  license: WTFPL
  summary: 'A Django Mixin capable of converting dictionaries of data or lists of dictionaries, to clean Django model instances'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
