package:
  name: collective.blog.view
  version: "1.5.2"

source:
  fn: collective.blog.view-1.5.2.zip
  url: https://pypi.python.org/packages/source/c/collective.blog.view/collective.blog.view-1.5.2.zip
  md5: 7882bf00115644b9b218508169ad6183
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.blog.view = collective.blog.view:main
    #
    # Would create an entry point called collective.blog.view that calls collective.blog.view.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone

  run:
    - python
    - setuptools
    - setuptools
    - plone

test:
  # Python imports
  imports:
    - collective
    - collective.blog
    - collective.blog.view

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/collective.blog.view/
  license: GPL
  summary: 'A blog view for folders.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
