package:
  name: dungeon-generator
  version: "0.0.2"

source:
  fn: dungeon-generator-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/dungeon-generator/dungeon-generator-0.0.2.tar.gz
  md5: ea525daa324915c6f07ed248ac4d2729
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dungeon-generator = dungeon-generator:main
    #
    # Would create an entry point called dungeon-generator that calls dungeon-generator.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pytest ==2.6.4

  run:
    - python
    - pytest ==2.6.4

test:
  # Python imports
  imports:
    - dungeon

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/arvelt/dungeon-generator
  license: MIT License
  summary: 'Dungeon generator cli tool for rouge-like.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
