package:
  name: sadbipws
  version: "0.1.0"

source:
  fn: sadbipws-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/s/sadbipws/sadbipws-0.1.0.tar.gz
  md5: b3585abd0e5ff8a4e40f49c6b0c846f8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sadbipws = sadbipws:main
    #
    # Would create an entry point called sadbipws that calls sadbipws.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - sadbipws

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://metanohi.org/projects/sadbipws/
  license: COPYING.txt
  summary: 'An implementation of the SADBIPWS serializer'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
