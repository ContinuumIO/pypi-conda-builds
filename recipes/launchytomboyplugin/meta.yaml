package:
  name: launchytomboyplugin
  version: "0.1.2"

source:
  fn: LaunchyTomboyPlugin-0.1.2.zip
  url: https://pypi.python.org/packages/source/L/LaunchyTomboyPlugin/LaunchyTomboyPlugin-0.1.2.zip
  md5: a2766edd6b21081fffece32fceed065a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - launchytomboyplugin = launchytomboyplugin:main
    #
    # Would create an entry point called launchytomboyplugin that calls launchytomboyplugin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - launchytomboyplugin

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/rsyring/launchytomboyplugin
  license: BSD License
  summary: 'A plugin for Launchy to put Tomboy note titles in the catalog'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
