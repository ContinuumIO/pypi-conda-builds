package:
  name: csvcleaner
  version: "1.0.6"

source:
  fn: csvcleaner-1.0.6.tar.gz
  url: https://pypi.python.org/packages/source/c/csvcleaner/csvcleaner-1.0.6.tar.gz
  md5: 41c5d0746c8e12722e95ecfcbcd3a891
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - csvcleaner = csvcleaner:main
    #
    # Would create an entry point called csvcleaner that calls csvcleaner.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - csvcleaner

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/palmer/csvcleaner
  license: Apache Software License
  summary: 'Removes rows containing blacklisted words from a CSV file.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
