package:
  name: difio-virtualenv-python
  version: "3.1.1"

source:
  fn: difio-virtualenv-python-3.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/difio-virtualenv-python/difio-virtualenv-python-3.1.1.tar.gz
  md5: 408a0f0e40d91ff97496fbdb9cca807f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - difio-virtualenv-python = difio-virtualenv-python:main
    #
    # Would create an entry point called difio-virtualenv-python that calls difio-virtualenv-python.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - difio_virtualenv_python

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/difio/difio-virtualenv-python
  license: MIT License
  summary: 'Difio registration agent for stand-alone Python virtualenv applications'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
