package:
  name: gevent_socketio2
  version: "0.2.8"

source:
  fn: gevent_socketio2-0.2.8.tar.gz
  url: https://pypi.python.org/packages/source/g/gevent_socketio2/gevent_socketio2-0.2.8.tar.gz
  md5: 86a61b1ccf8869800ea34535fae8a102
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gevent_socketio2 = gevent_socketio2:main
    #
    # Would create an entry point called gevent_socketio2 that calls gevent_socketio2.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - greenlet ==0.4.4
    - gevent-websocket ==0.9.3
    - webob ==1.4
    - requests ==2.4.1
    - ws4py ==0.3.4

  run:
    - python
    - greenlet ==0.4.4
    - gevent-websocket ==0.9.3
    - webob ==1.4
    - requests ==2.4.1
    - ws4py ==0.3.4

test:
  # Python imports
  imports:
    - gevent_socketio2
    - socketio
    - socketio.engine
    - socketio.event_emitter

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/shuoli84/gevent_socketio2
  license: MIT
  summary: 'A gevent implementation for socketio protocol 1.0'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
