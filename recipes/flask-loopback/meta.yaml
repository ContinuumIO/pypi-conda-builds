package:
  name: flask-loopback
  version: "1.3.0"

source:
  fn: Flask-Loopback-1.3.0.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Loopback/Flask-Loopback-1.3.0.tar.gz
  md5: 49be0c7c5cb6feaeafb22bfee65622a3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-loopback = flask-loopback:main
    #
    # Would create an entry point called flask-loopback that calls flask-loopback.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests
    - flask
    - urlobject
    - contextlib2

  run:
    - python
    - requests
    - flask
    - urlobject
    - contextlib2

test:
  # Python imports
  imports:
    - flask_loopback

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/vmalloc/flask-loopback
  license: BSD3
  summary: 'Library for faking HTTP requests using flask applications without actual network operations'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
