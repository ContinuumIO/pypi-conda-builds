package:
  name: jinja2modern
  version: "0.3.0"

source:
  fn: jinja2modern-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/j/jinja2modern/jinja2modern-0.3.0.tar.gz
  md5: c711ed59ab4296d4760fac91f317eefc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jinja2modern = jinja2modern:main
    #
    # Would create an entry point called jinja2modern that calls jinja2modern.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - jinja2

  run:
    - python
    - jinja2

test:
  # Python imports
  imports:
    - jinja2modern
    - jinja2modern.tags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/velocityzen/jinja2modern
  license: BSD
  summary: 'Jinja2 tags for modern web. Includes sass, scss, less, coffee, uglify. Can be easily extended for any command line tools.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
