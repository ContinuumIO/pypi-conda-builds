package:
  name: zopyx.existdb
  version: "0.2.11.1"

source:
  fn: zopyx.existdb-0.2.11.1.zip
  url: https://pypi.python.org/packages/source/z/zopyx.existdb/zopyx.existdb-0.2.11.1.zip
  md5: 6e1f6deb032d59d5a904e8bdb2234e32
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zopyx.existdb = zopyx.existdb:main
    #
    # Would create an entry point called zopyx.existdb that calls zopyx.existdb.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.directives.form
    - hurry.filesize
    - humanize
    - zope.i18nmessageid
    - plone.browserlayer
    - plone.api
    - requests
    - fs
    - dexml

  run:
    - python
    - setuptools
    - setuptools
    - plone.directives.form
    - hurry.filesize
    - humanize
    - zope.i18nmessageid
    - plone.browserlayer
    - plone.api
    - requests
    - fs
    - dexml

test:
  # Python imports
  imports:
    - zopyx
    - zopyx.existdb
    - zopyx.existdb.browser
    - zopyx.existdb.browser.tests
    - zopyx.existdb.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/zopyx.existdb
  license: GPL
  summary: 'Plone-ExistDB integration'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
