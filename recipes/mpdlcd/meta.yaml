package:
  name: mpdlcd
  version: "0.4.1"

source:
  fn: mpdlcd-0.4.1.tar.gz
  url: https://pypi.python.org/packages/source/m/mpdlcd/mpdlcd-0.4.1.tar.gz
  md5: ec44f06b37da8ca742d6717a441444fd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mpdlcd = mpdlcd:main
    #
    # Would create an entry point called mpdlcd that calls mpdlcd.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python_mpd2
    - lcdproc

  run:
    - python
    - python_mpd2
    - lcdproc

test:
  # Python imports
  imports:
    - mpdlcd

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/rbarrois/mpdlcd
  license: MIT
  summary: 'Display MPD status on a lcdproc server.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
