package:
  name: hastests
  version: "0.3.0"

source:
  fn: hastests-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/h/hastests/hastests-0.3.0.tar.gz
  md5: c3e0f7a52710fc6887dd561574e985d2
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hastests = hastests:main
    #
    # Would create an entry point called hastests that calls hastests.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - hastests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/spawngrid/hastests-python
  license: MIT License
  summary: 'HasTests API for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
