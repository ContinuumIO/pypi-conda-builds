package:
  name: rcs4cvs2svn
  version: "1.2"

source:
  fn: rcs4cvs2svn-1.2.zip
  url: https://pypi.python.org/packages/source/r/rcs4cvs2svn/rcs4cvs2svn-1.2.zip
  md5: f6ee9777b3c3c0c0b84b9091e8070db4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - rcs4cvs2svn = rcs4cvs2svn:main
    #
    # Would create an entry point called rcs4cvs2svn that calls rcs4cvs2svn.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - rcs4cvs2svn

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/rcs4cvs2svn/
  license: BSD License
  summary: 'prepare RCS project for processing with cvs2svn'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
