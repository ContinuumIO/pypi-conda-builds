package:
  name: collective.saoraclefixes
  version: "1.2"

source:
  fn: collective.saoraclefixes-1.2.zip
  url: https://pypi.python.org/packages/source/c/collective.saoraclefixes/collective.saoraclefixes-1.2.zip
  md5: e6ed7379921d200b2899e68ed80949d9
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.saoraclefixes = collective.saoraclefixes:main
    #
    # Would create an entry point called collective.saoraclefixes that calls collective.saoraclefixes.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sqlalchemy >=0.4dev,<0.6dev

  run:
    - python
    - setuptools
    - sqlalchemy >=0.4dev,<0.6dev

test:
  # Python imports
  imports:
    - collective
    - collective.saoraclefixes

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/collective.saoraclefixes
  license: MIT
  summary: 'Fixes for the SQLAlchemy 0.4 and 0.5 Oracle driver'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
