package:
  name: loggrok
  version: "0.2.1"

source:
  fn: loggrok-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/l/loggrok/loggrok-0.2.1.tar.gz
  md5: 2059387c68666257cefd8186c89c2fc7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - loggrok = loggrok:main
    #
    # Would create an entry point called loggrok that calls loggrok.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - xix-utils >=0.2.2

  run:
    - python
    - xix-utils >=0.2.2

test:
  # Python imports
  imports:
    - loggrok

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://xix.python-hosting.com/#loggrok
  license: MIT License
  summary: 'Simple log analysis library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
