package:
  name: iterutils
  version: "0.1.6"

source:
  fn: iterutils-0.1.6.tar.gz
  url: https://pypi.python.org/packages/source/i/iterutils/iterutils-0.1.6.tar.gz
  md5: e293044ec65d757ce075ac170ad9ada8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - iterutils = iterutils:main
    #
    # Would create an entry point called iterutils that calls iterutils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - iterutils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/iterutils
  license: http://docs.python.org/license.html
  summary: 'Itertools recipes.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
