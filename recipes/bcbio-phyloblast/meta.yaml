package:
  name: bcbio-phyloblast
  version: "0.1.2"

source:
  fn: bcbio-phyloblast-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/b/bcbio-phyloblast/bcbio-phyloblast-0.1.2.tar.gz
  md5: d0c32b0eef946b35ff005e42f92c9d23
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bcbio-phyloblast = bcbio-phyloblast:main
    #
    # Would create an entry point called bcbio-phyloblast that calls bcbio-phyloblast.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - bcbio
    - bcbio.phylo

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bcbio.wordpress.com
  license: MIT
  summary: 'Distributed BLAST for gene phylogeny estimation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
