package:
  name: centipede
  version: "0.2.5"

source:
  fn: centipede-0.2.5.tar.gz
  url: https://pypi.python.org/packages/source/c/centipede/centipede-0.2.5.tar.gz
  md5: 07a007d0569543ae085ac6ff28675947
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - centipede = centipede:main
    #
    # Would create an entry point called centipede that calls centipede.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - webob ==1.2b2
    - urlrelay ==0.7.1
    - wsgiref ==0.1.2
    - static ==0.4

  run:
    - python
    - webob ==1.2b2
    - urlrelay ==0.7.1
    - wsgiref ==0.1.2
    - static ==0.4

test:
  # Python imports
  imports:
    - centipede

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://centipede.asbjornenge.com
  license: FreeBSD
  summary: 'Centipede is a WSGI microframework with a simple decorator based router.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
