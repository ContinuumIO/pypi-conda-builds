package:
  name: formbuild
  version: "4.0.0"

source:
  fn: FormBuild-4.0.0.tar.gz
  url: https://pypi.python.org/packages/source/F/FormBuild/FormBuild-4.0.0.tar.gz
  md5: d16ae13ffec2ad3d450a1f6a68e7935f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - formbuild = formbuild:main
    #
    # Would create an entry point called formbuild that calls formbuild.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - barenecessities >=0.2.5,<=0.2.99

  run:
    - python
    - setuptools
    - barenecessities >=0.2.5,<=0.2.99

test:
  # Python imports
  imports:
    - formbuild

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://jimmyg.org/work/code/formbuild/index.html
  license: MIT License
  summary: 'Build HTML forms quickly and easily. (Pre-release not ready for broad use, please use 0.3.x).'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
