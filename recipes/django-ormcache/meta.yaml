package:
  name: django-ormcache
  version: "1.0"

source:
  fn: django-ormcache-1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-ormcache/django-ormcache-1.0.tar.gz
  md5: 38a1b7600c0a0322049fd6634062bee3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-ormcache = django-ormcache:main
    #
    # Would create an entry point called django-ormcache that calls django-ormcache.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6

  run:
    - python
    - django >=1.6

test:
  # Python imports
  imports:
    - ormcache

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/educreations/django-ormcache
  license: MIT License
  summary: 'ORM cache for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
