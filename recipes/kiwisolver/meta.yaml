package:
  name: kiwisolver
  version: "0.1.3"

source:
  fn: kiwisolver-0.1.3.zip
  url: https://pypi.python.org/packages/source/k/kiwisolver/kiwisolver-0.1.3.zip
  md5: 9c23204adc6617a24e915f4a1886ff3b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kiwisolver = kiwisolver:main
    #
    # Would create an entry point called kiwisolver that calls kiwisolver.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - distribute

  run:
    - python
    - distribute

test:
  # Python imports
  imports:
    - kiwisolver

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/nucleic/kiwi
  license: UNKNOWN
  summary: 'A fast implementation of the Cassowary constraint solver'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
