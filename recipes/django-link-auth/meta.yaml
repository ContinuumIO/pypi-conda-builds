package:
  name: django-link-auth
  version: "0.1.0"

source:
  fn: django-link-auth-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-link-auth/django-link-auth-0.1.0.tar.gz
  md5: c44fba2bd3882a59b1be14a11551d517
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-link-auth = django-link-auth:main
    #
    # Would create an entry point called django-link-auth that calls django-link-auth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_link_auth

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/svetlyak40wt/django-link-auth/
  license: BSD License
  summary: "Django's authentication backend to login by temporary URLs."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
