package:
  name: ubik
  version: "0.2.4"

source:
  fn: ubik-0.2.4.tar.gz
  url: https://pypi.python.org/packages/source/u/ubik/ubik-0.2.4.tar.gz
  md5: d206f79d8e509a86a946b0ef597bdeb4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ubik = ubik:main
    #
    # Would create an entry point called ubik that calls ubik.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - progressbar ==2.3
    - requests ==1.1.0
    - docopt ==0.5.0
    - isit ==0.3

  run:
    - python
    - progressbar ==2.3
    - requests ==1.1.0
    - docopt ==0.5.0
    - isit ==0.3

test:
  # Python imports
  imports:
    - ubik
    - ubik_toolbelt
    - ubik_toolbelt.contrib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/socketubs/ubik/
  license: GNU Affero General Public License v3 or later (AGPLv3+)
  summary: u'Minimal and elegant packages manager'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
