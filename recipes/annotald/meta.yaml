package:
  name: annotald
  version: "1.3.4"

source:
  fn: annotald-1.3.4.tar.gz
  url: https://pypi.python.org/packages/source/a/annotald/annotald-1.3.4.tar.gz
  md5: 0a4fce0aff23059c0ced30bc225d668a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - annotald = annotald:main
    #
    # Would create an entry point called annotald that calls annotald.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - mako
    - cherrypy
    - argparse
    - nltk ==2.0.4

  run:
    - python
    - mako
    - cherrypy
    - argparse
    - nltk ==2.0.4

test:
  # Python imports
  imports:
    - annotald

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://annotald.github.com/
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'A GUI for treebank annotation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
