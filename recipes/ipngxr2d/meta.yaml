package:
  name: ipngxr2d
  version: "0.1"

source:
  fn: ipngxr2d-0.1.tar.gz
  url: https://pypi.python.org/packages/source/i/ipngxr2d/ipngxr2d-0.1.tar.gz
  md5: ab7068c18af32520413697db7119b3f2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ipngxr2d = ipngxr2d:main
    #
    # Would create an entry point called ipngxr2d that calls ipngxr2d.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - ipngxr2d

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/DavidJohnGee/ipngxr2d
  license: Apache License 2.0
  summary: 'Python XML parsing utility returning dictionary of children (uses lxml)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
