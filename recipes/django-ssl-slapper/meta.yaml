package:
  name: django-ssl-slapper
  version: "1.1"

source:
  fn: django-ssl-slapper-1.1.zip
  url: https://pypi.python.org/packages/source/d/django-ssl-slapper/django-ssl-slapper-1.1.zip
  md5: 0a0b7c28d44359f544691c7fce4636c4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-ssl-slapper = django-ssl-slapper:main
    #
    # Would create an entry point called django-ssl-slapper that calls django-ssl-slapper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - ssl_slapper

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/queenorych/django-ssl-slapper
  license: BSD License
  summary: 'A middleware that secures URLS.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
