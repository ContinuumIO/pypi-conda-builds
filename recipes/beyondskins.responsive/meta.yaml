package:
  name: beyondskins.responsive
  version: "0.4"

source:
  fn: beyondskins.responsive-0.4.zip
  url: https://pypi.python.org/packages/source/b/beyondskins.responsive/beyondskins.responsive-0.4.zip
  md5: cff7d2915456f21561d3e5c709a9bb98
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - beyondskins.responsive = beyondskins.responsive:main
    #
    # Would create an entry point called beyondskins.responsive that calls beyondskins.responsive.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.theming

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.theming

test:
  # Python imports
  imports:
    - beyondskins
    - beyondskins.responsive

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.simplesconsultoria.com.br/
  license: GPL
  summary: 'A responsive design for Plone/Diazo powered sites'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
