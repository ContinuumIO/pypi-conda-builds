package:
  name: agx.core
  version: "1.0a2"

source:
  fn: agx.core-1.0a2.tar.gz
  url: https://pypi.python.org/packages/source/a/agx.core/agx.core-1.0a2.tar.gz
  md5: 13af8ba49cd8f057f2502ba22945c563
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - agx.core = agx.core:main
    #
    # Would create an entry point called agx.core that calls agx.core.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - node
    - zope.configuration

  run:
    - python
    - setuptools
    - node
    - zope.configuration

test:
  # Python imports
  imports:
    - agx
    - agx.core
    - agx.core.testing

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/bluedynamics/agx.core
  license: GNU General Public License (GPL)
  summary: 'AGX tree transformation chain processor'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
