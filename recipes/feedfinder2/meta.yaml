package:
  name: feedfinder2
  version: "0.0.1"

source:
  fn: feedfinder2-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/f/feedfinder2/feedfinder2-0.0.1.tar.gz
  md5: 69208b97b5c2a0e740fcecb91bfd26c1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - feedfinder2 = feedfinder2:main
    #
    # Would create an entry point called feedfinder2 that calls feedfinder2.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests
    - beautifulsoup4

  run:
    - python
    - requests
    - beautifulsoup4

test:
  # Python imports
  imports:
    - feedfinder2

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dfm/feedfinder2
  license: MIT License
  summary: 'Find the feed URLs for a website.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
