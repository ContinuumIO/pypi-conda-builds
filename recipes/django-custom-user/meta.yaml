package:
  name: django-custom-user
  version: "0.5"

source:
  fn: django-custom-user-0.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-custom-user/django-custom-user-0.5.tar.gz
  md5: 8c317a5793deebc6b49d59b08214c39f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-custom-user = django-custom-user:main
    #
    # Would create an entry point called django-custom-user that calls django-custom-user.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5

  run:
    - python
    - django >=1.5

test:
  # Python imports
  imports:
    - custom_user

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jcugat/django-custom-user
  license: BSD License
  summary: "Custom user model for Django >= 1.5 with the same behaviour as Django's default User but with email instead of username."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
