package:
  name: collective.ptg.fancybox
  version: "1.0.3"

source:
  fn: collective.ptg.fancybox-1.0.3.zip
  url: https://pypi.python.org/packages/source/c/collective.ptg.fancybox/collective.ptg.fancybox-1.0.3.zip
  md5: e554df2acc6fb6c9608fc513c1527048
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.ptg.fancybox = collective.ptg.fancybox:main
    #
    # Would create an entry point called collective.ptg.fancybox that calls collective.ptg.fancybox.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.plonetruegallery

  run:
    - python
    - setuptools
    - setuptools
    - collective.plonetruegallery

test:
  # Python imports
  imports:
    - collective
    - collective.ptg
    - collective.ptg.fancybox

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.ptg.fancybox
  license: GPL
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
