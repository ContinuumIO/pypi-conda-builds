package:
  name: flask-config-override
  version: "0.0.2"

source:
  fn: Flask-Config-Override-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Config-Override/Flask-Config-Override-0.0.2.tar.gz
  md5: 094ffc8a9afc292876f4e2469ee7e37d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-config-override = flask-config-override:main
    #
    # Would create an entry point called flask-config-override that calls flask-config-override.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - flask

  run:
    - python
    - flask

test:
  # Python imports
  imports:
    - flask_config_override
    - flask_config_override.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/elboby/flask-config-override
  license: BSD License
  summary: 'Override Flask configuration via Cookie at runtime.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
