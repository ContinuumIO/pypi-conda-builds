package:
  name: ezrpc
  version: "0.1.0"

source:
  fn: ezrpc-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/e/ezrpc/ezrpc-0.1.0.tar.gz
  md5: 0c44859a00aab79b77f383e64edd63c0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ezrpc = ezrpc:main
    #
    # Would create an entry point called ezrpc that calls ezrpc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyzmq ==14.0.0

  run:
    - python
    - pyzmq ==14.0.0

test:
  # Python imports
  imports:
    - ezrpc

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/abh1nav/ezrpc/
  license: Apache 2.0 License
  summary: 'Easy to use ZeroMQ based RPC library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
