package:
  name: tesseracttrainer
  version: "0.1.1"

source:
  fn: TesseractTrainer-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/T/TesseractTrainer/TesseractTrainer-0.1.1.tar.gz
  md5: 53e394f56a034e7eea00b128560c36ab
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tesseracttrainer = tesseracttrainer:main
    #
    # Would create an entry point called tesseracttrainer that calls tesseracttrainer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pil >=1.1.7

  run:
    - python
    - pil >=1.1.7

test:
  # Python imports
  imports:
    - tesseract_trainer
    - tesseracttrainer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/BaltoRouberol/TesseractTrainer
  license: BSD License
  summary: 'A small framework taking over the manual tesseract training process described in the Tesseract Wiki'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
