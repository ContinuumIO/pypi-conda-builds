package:
  name: django-webperf
  version: "0.1"

source:
  fn: django-webperf-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-webperf/django-webperf-0.1.tar.gz
  md5: 7a129d3ab475a643cc0ea1755fa1a62e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-webperf = django-webperf:main
    #
    # Would create an entry point called django-webperf that calls django-webperf.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-appconf ==0.4.1

  run:
    - python
    - django-appconf ==0.4.1

test:
  # Python imports
  imports:
    - tests
    - web_performance

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/coagulant/django-webperf
  license: MIT License
  summary: 'A collection of stuff to improve django web performance.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
