package:
  name: hpcloud-auth-openstack
  version: "1.0"

source:
  fn: hpcloud-auth-openstack-1.0.tar.gz
  url: https://pypi.python.org/packages/source/h/hpcloud-auth-openstack/hpcloud-auth-openstack-1.0.tar.gz
  md5: 1a24a5e5846b11bf1a7e8a73468f8749
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hpcloud-auth-openstack = hpcloud-auth-openstack:main
    #
    # Would create an entry point called hpcloud-auth-openstack that calls hpcloud-auth-openstack.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - hpcloud_auth_openstack

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/emonty/hpcloud-auth-openstack
  license: Apache Software License
  summary: 'HP Cloud Auth Plugin for OpenStack Clients.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
