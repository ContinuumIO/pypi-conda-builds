package:
  name: django-keyedcache
  version: "1.5.1"

source:
  fn: django-keyedcache-1.5.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-keyedcache/django-keyedcache-1.5.1.tar.gz
  md5: a429038d27dea40030a4ada7d055661e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-keyedcache = django-keyedcache:main
    #
    # Would create an entry point called django-keyedcache that calls django-keyedcache.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - keyedcache

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/bkroeze/django-keyedcache/
  license: BSD License
  summary: 'keyedcache'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
