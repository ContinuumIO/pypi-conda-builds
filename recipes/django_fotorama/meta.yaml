package:
  name: django_fotorama
  version: "0.001d"

source:
  fn: django_fotorama-0.001d.tar.gz
  url: https://pypi.python.org/packages/source/d/django_fotorama/django_fotorama-0.001d.tar.gz
  md5: 217f46b230b4dde258ed7534da714723
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_fotorama = django_fotorama:main
    #
    # Would create an entry point called django_fotorama that calls django_fotorama.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - django_fotorama
    - fotorama
    - fotorama.templates
    - fotorama.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/snznaota/django-fotorama
  license: BSD
  summary: 'simple django-fotorama apps!'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
