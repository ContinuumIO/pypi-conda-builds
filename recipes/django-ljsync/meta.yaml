package:
  name: django-ljsync
  version: "0.1.6"

source:
  fn: django-ljsync-0.1.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-ljsync/django-ljsync-0.1.6.tar.gz
  md5: ae4c30d3d550aa33b52aab2f6fdd47cd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-ljsync = django-ljsync:main
    #
    # Would create an entry point called django-ljsync that calls django-ljsync.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python >=2.4
    - django >=1.0
    - django_autoslug >=1.0.1

  run:
    - python
    - python >=2.4
    - django >=1.0
    - django_autoslug >=1.0.1

test:
  # Python imports
  imports:
    - ljsync

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/neithere/django-ljsync/
  license: GNU Affero General Public License v3
  summary: 'A LiveJournal to Django synchronizer'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
