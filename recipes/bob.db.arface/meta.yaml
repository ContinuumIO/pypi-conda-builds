package:
  name: bob.db.arface
  version: "2.0.1"

source:
  fn: bob.db.arface-2.0.1.zip
  url: https://pypi.python.org/packages/source/b/bob.db.arface/bob.db.arface-2.0.1.zip
  md5: 2e3665fce47ff39161697c18a9d9b016
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bob.db.arface = bob.db.arface:main
    #
    # Would create an entry point called bob.db.arface that calls bob.db.arface.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - six
    - bob.db.base
    - bob.db.verification.utils

  run:
    - python
    - setuptools
    - setuptools
    - six
    - bob.db.base
    - bob.db.verification.utils

test:
  # Python imports
  imports:
    - bob
    - bob.db
    - bob.db.arface

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://pypi.python.org/pypi/bob.db.arface
  license: GNU General Public License v3 (GPLv3)
  summary: 'AR Face  Database Access API for Bob'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
