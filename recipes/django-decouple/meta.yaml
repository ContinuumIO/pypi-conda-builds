package:
  name: django-decouple
  version: "2.1"

source:
  fn: django-decouple-2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-decouple/django-decouple-2.1.tar.gz
  md5: 4549d2e437373e995e61d239bbc358fe
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-decouple = django-decouple:main
    #
    # Would create an entry point called django-decouple that calls django-decouple.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/henriquebastos/django-decouple/
  license: MIT License
  summary: 'Strict separation of settings from code.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
