package:
  name: djangocms-text-ckeditor
  version: "2.4.3"

source:
  fn: djangocms-text-ckeditor-2.4.3.tar.gz
  url: https://pypi.python.org/packages/source/d/djangocms-text-ckeditor/djangocms-text-ckeditor-2.4.3.tar.gz
  md5: f664ba71f09b10a65d882266ce9d4515
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangocms-text-ckeditor = djangocms-text-ckeditor:main
    #
    # Would create an entry point called djangocms-text-ckeditor that calls djangocms-text-ckeditor.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-cms >=3.0
    - html5lib
    - pillow

  run:
    - python
    - django-cms >=3.0
    - html5lib
    - pillow

test:
  # Python imports
  imports:
    - djangocms_text_ckeditor
    - djangocms_text_ckeditor.migrations
    - djangocms_text_ckeditor.migrations_django

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/divio/djangocms-text-ckeditor
  license: BSD License
  summary: 'Text Plugin for django CMS with CKEditor support'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
