package:
  name: distutils-licenses
  version: "0.1.2"

source:
  fn: distutils-licenses-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/distutils-licenses/distutils-licenses-0.1.2.tar.gz
  md5: 6dc3d90357c02ae1ec9bcfebc20c91b5
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - distutils-licenses = distutils-licenses:main
    #
    # Would create an entry point called distutils-licenses that calls distutils-licenses.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - distutils_licenses

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/dkoepke/distutils-licenses
  license: MIT
  summary: 'adds a command to setup.py to list licenses of packages'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
