package:
  name: djangotestxmlrpc
  version: "0.0.2"

source:
  fn: djangotestxmlrpc-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/djangotestxmlrpc/djangotestxmlrpc-0.0.2.tar.gz
  md5: c0fb9b6935f88547c91aeeaf9fe3c5a0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangotestxmlrpc = djangotestxmlrpc:main
    #
    # Would create an entry point called djangotestxmlrpc that calls djangotestxmlrpc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - djangotestxmlrpc

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/msabramo/djangotestxmlrpc
  license: MIT
  summary: 'Utility classes for testing Django views that speak XML-RPC'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
