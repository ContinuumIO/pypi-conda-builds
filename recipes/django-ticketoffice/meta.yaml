package:
  name: django-ticketoffice
  version: "0.2"

source:
  fn: django-ticketoffice-0.2.zip
  url: https://pypi.python.org/packages/source/d/django-ticketoffice/django-ticketoffice-0.2.zip
  md5: 551ed5be6036f9e840f73df597d87deb
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-ticketoffice = django-ticketoffice:main
    #
    # Would create an entry point called django-ticketoffice that calls django-ticketoffice.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django
    - django-floppyforms
    - django-jsonfield
    - django-qmixin
    - django-uuidfield >=0.5
    - setuptools
    - mock

  run:
    - python
    - django
    - django-floppyforms
    - django-jsonfield
    - django-qmixin
    - django-uuidfield >=0.5
    - setuptools
    - mock

test:
  # Python imports
  imports:
    - django_ticketoffice

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://django-ticketoffice.readthedocs.org/
  license: BSD License
  summary: 'One-shot authentication (temporary credentials) utilities for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
