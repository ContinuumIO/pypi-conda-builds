package:
  name: django-domande
  version: "0.1.4"

source:
  fn: django-domande-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-domande/django-domande-0.1.4.tar.gz
  md5: 864e713e9944363e592b3cdc55f5aaf2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-domande = django-domande:main
    #
    # Would create an entry point called django-domande that calls django-domande.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django_polymorphic
    - south

  run:
    - python
    - django_polymorphic
    - south

test:
  # Python imports
  imports:
    - domande
    - domande.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bulkan/django-domande
  license: UNKNOWN
  summary: 'django app to represent generic questions on forms'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
