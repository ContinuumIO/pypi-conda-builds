package:
  name: eulerlib
  version: "0.2"

source:
  fn: eulerlib-0.2.zip
  url: https://pypi.python.org/packages/source/e/eulerlib/eulerlib-0.2.zip
  md5: 959a757272cb62db680a7a8ff8b29637
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - eulerlib = eulerlib:main
    #
    # Would create an entry point called eulerlib that calls eulerlib.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - eulerlib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/transmogrifier/eulerlib
  license: Apache Software License
  summary: 'A library of number theory related functions inspired by Project Euler.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
