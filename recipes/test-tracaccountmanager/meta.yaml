package:
  name: test-tracaccountmanager
  version: "0.5"

source:
  fn: test-tracaccountmanager.tar.gz
  url: https://pypi.python.org/packages/source/T/TEST-TracAccountManager/test-tracaccountmanager.tar.gz
  md5: 13920de387b5761e66ab746be601bc79
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - test-tracaccountmanager = test-tracaccountmanager:main
    #
    # Would create an entry point called test-tracaccountmanager that calls test-tracaccountmanager.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - genshi >=0.5
    - trac >=0.11

  run:
    - python
    - setuptools
    - genshi >=0.5
    - trac >=0.11

test:
  # Python imports
  imports:
    - acct_mgr

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://trac-hacks.org/wiki/AccountManagerPlugin
  license: BSD
  summary: 'User account management plugin for Trac'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
