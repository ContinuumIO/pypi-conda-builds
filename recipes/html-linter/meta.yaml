package:
  name: html-linter
  version: "0.1.9"

source:
  fn: html-linter-0.1.9.tar.gz
  url: https://pypi.python.org/packages/source/h/html-linter/html-linter-0.1.9.tar.gz
  md5: 49a090d6a4b0969e5e0df255d68ccebe
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - html-linter = html-linter:main
    #
    # Would create an entry point called html-linter that calls html-linter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - template-remover
    - docopt >=0.6.1

  run:
    - python
    - template-remover
    - docopt >=0.6.1

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/deezer/html-linter
  license: Apache Software License
  summary: "Lints an HTML5 file using Google's style guide"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
