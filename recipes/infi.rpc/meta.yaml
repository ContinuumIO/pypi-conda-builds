package:
  name: infi.rpc
  version: "0.2.2"

source:
  fn: infi.rpc-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/i/infi.rpc/infi.rpc-0.2.2.tar.gz
  md5: 33f97fd60f994cc3e6cea1e25c473704
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - infi.rpc = infi.rpc:main
    #
    # Would create an entry point called infi.rpc that calls infi.rpc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - infi.gevent_utils
    - infi.logging >=0.4.1
    - jsonpickle
    - logbook >=0.8
    - setuptools
    - zerorpc

  run:
    - python
    - infi.gevent_utils
    - infi.logging >=0.4.1
    - jsonpickle
    - logbook >=0.8
    - setuptools
    - zerorpc

test:
  # Python imports
  imports:
    - infi
    - infi.rpc
    - infi.rpc.base
    - infi.rpc.zerorpc

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Infinidat/infi.rpc
  license: Python Software Foundation License
  summary: 'rpc'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
