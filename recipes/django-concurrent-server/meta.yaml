package:
  name: django-concurrent-server
  version: "0.1.4"

source:
  fn: django-concurrent-server-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-concurrent-server/django-concurrent-server-0.1.4.tar.gz
  md5: e3b025400fe7693247770bf933422e3c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-concurrent-server = django-concurrent-server:main
    #
    # Would create an entry point called django-concurrent-server that calls django-concurrent-server.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - concurrent_server
    - concurrent_server.management
    - concurrent_server.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ashchristopher/django-concurrent-server
  license: MIT
  summary: 'Provides a multi-threaded (concurrent) development sever for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
