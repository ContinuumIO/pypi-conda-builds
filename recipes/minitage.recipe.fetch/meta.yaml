package:
  name: minitage.recipe.fetch
  version: "1.43"

source:
  fn: minitage.recipe.fetch-1.43.zip
  url: https://pypi.python.org/packages/source/m/minitage.recipe.fetch/minitage.recipe.fetch-1.43.zip
  md5: 64a89f1b9d33c91dc475d3dc161d3db0
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - minitage.recipe.fetch = minitage.recipe.fetch:main
    #
    # Would create an entry point called minitage.recipe.fetch that calls minitage.recipe.fetch.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zc.buildout
    - minitage.core
    - minitage.recipe.common
    - iniparse

  run:
    - python
    - setuptools
    - zc.buildout
    - minitage.core
    - minitage.recipe.common
    - iniparse

test:
  # Python imports
  imports:
    - minitage
    - minitage.recipe
    - minitage.recipe.fetch
    - minitage.recipe.fetch.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://cheeseshop.python.org/pypi/minitage.recipe.fetch
  license: BSD License
  summary: 'zc.buildout recipes to compile and install software or python packages and generate scripts or configuration files.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
