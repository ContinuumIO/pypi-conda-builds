package:
  name: django-paypal-adaptive
  version: "0.2.2"

source:
  fn: django-paypal-adaptive-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-paypal-adaptive/django-paypal-adaptive-0.2.2.tar.gz
  md5: cc28d81bb5a0249334b4f088adb91e17
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-paypal-adaptive = django-paypal-adaptive:main
    #
    # Would create an entry point called django-paypal-adaptive that calls django-paypal-adaptive.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4.3
    - python-dateutil ==2.1
    - python-money

  run:
    - python
    - django >=1.4.3
    - python-dateutil ==2.1
    - python-money

test:
  # Python imports
  imports:
    - paypaladaptive
    - paypaladaptive.api
    - paypaladaptive.api.ipn
    - paypaladaptive.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/FundedByMe/django-paypal-adaptive
  license: UNKNOWN
  summary: 'A pluggable Django application for integrating PayPal Adaptive Payments'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
