package:
  name: gocept.linkchecker
  version: "3.0a9"

source:
  fn: gocept.linkchecker-3.0a9.tar.gz
  url: https://pypi.python.org/packages/source/g/gocept.linkchecker/gocept.linkchecker-3.0a9.tar.gz
  md5: cab8b50f19e63caa1325a2bac384e387
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gocept.linkchecker = gocept.linkchecker:main
    #
    # Would create an entry point called gocept.linkchecker that calls gocept.linkchecker.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - lxml

  run:
    - python
    - setuptools
    - lxml

test:
  # Python imports
  imports:
    - gocept
    - gocept.linkchecker
    - gocept.linkchecker.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: ZPL 2.1
  summary: 'Check links in your Plone site using a link monitoring server.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
