package:
  name: gevent-loops
  version: "0.0.2"

source:
  fn: gevent-loops-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/g/gevent-loops/gevent-loops-0.0.2.tar.gz
  md5: 53600adf741e5b882cafbb94745d6063
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gevent-loops = gevent-loops:main
    #
    # Would create an entry point called gevent-loops that calls gevent-loops.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - gevent >=1.0.1

  run:
    - python
    - gevent >=1.0.1

test:
  # Python imports
  imports:
    - gevent_loops
    - gevent_loops.loops

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mattlong/gevent-loops
  license: MIT
  summary: 'TODO'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
