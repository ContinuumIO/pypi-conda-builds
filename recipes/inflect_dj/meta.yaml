package:
  name: inflect_dj
  version: "v0.2.0"

source:
  fn: inflect_dj-v0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/i/inflect_dj/inflect_dj-v0.2.0.tar.gz
  md5: a14cc1e0255eec47a5601e2c8eeddf24
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - inflect_dj = inflect_dj:main
    #
    # Would create an entry point called inflect_dj that calls inflect_dj.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - inflect_dj

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/inflect_dj
  license: GNU Affero General Public License v3
  summary: 'Correctly generate plurals when using Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
