package:
  name: dalton
  version: "1.0"

source:
  fn: dalton-1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/dalton/dalton-1.0.tar.gz
  md5: 660d7d1854a874cb681244e3c483aa48
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dalton = dalton:main
    #
    # Would create an entry point called dalton that calls dalton.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - dalton
    - dalton.tests
    - dalton.tests.test_recordings
    - dalton.tests.test_recordings.google_play_test
    - dalton.tests.test_recordings.google_test
    - google

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bbangert/Dalton
  license: MIT License
  summary: 'An httplib injection library for recording and playing back HTTP interactions.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
