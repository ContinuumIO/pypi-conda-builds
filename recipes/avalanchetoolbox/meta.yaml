package:
  name: avalanchetoolbox
  version: ".4.2"

source:
  fn: avalanchetoolbox-.4.2.tar.gz
  url: https://pypi.python.org/packages/source/a/avalanchetoolbox/avalanchetoolbox-.4.2.tar.gz
  md5: 7d98565923458f771c24c050ad487fc9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - avalanchetoolbox = avalanchetoolbox:main
    #
    # Would create an entry point called avalanchetoolbox that calls avalanchetoolbox.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - avalanchetoolbox

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jeffalstott/avalanchetoolbox
  license: MIT License
  summary: 'Toolbox for identifying and describing branching processes, such as neuronal avalanches'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
