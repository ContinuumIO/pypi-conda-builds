package:
  name: benchmark
  version: "0.1.5"

source:
  fn: benchmark-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/b/benchmark/benchmark-0.1.5.tar.gz
  md5: 556c4bca43c8dcd5b484dae1d6193049
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - benchmark = benchmark:main
    #
    # Would create an entry point called benchmark that calls benchmark.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - benchmark

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://jspi.es/benchmark
  license: Apache Software License
  summary: 'Python benchmarker / benchmarking framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
