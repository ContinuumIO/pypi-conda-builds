package:
  name: django-placeholder
  version: "0.0.1"

source:
  fn: django-placeholder-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-placeholder/django-placeholder-0.0.1.tar.gz
  md5: e48603b50700bca519112c09001184f2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-placeholder = django-placeholder:main
    #
    # Would create an entry point called django-placeholder that calls django-placeholder.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-classy-tags >=0.4
    - simplejson
    - django

  run:
    - python
    - django-classy-tags >=0.4
    - simplejson
    - django

test:
  # Python imports
  imports:
    - placeholder
    - placeholder.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/mauler/django-placeholder
  license: Apache Software License
  summary: 'Simple placeholder for django projects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
