package:
  name: salesforce-python-sdk
  version: "0.1.3"

source:
  fn: salesforce-python-sdk-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/s/salesforce-python-sdk/salesforce-python-sdk-0.1.3.tar.gz
  md5: b938cd1f200ec2c20264b2173b8862ab
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - salesforce-python-sdk = salesforce-python-sdk:main
    #
    # Would create an entry point called salesforce-python-sdk that calls salesforce-python-sdk.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - salesforce

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/htarevern/salesforce-python-sdk
  license: UNKNOWN
  summary: 'This is Salesforce Python SDK for REST and SOAP APIs'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
