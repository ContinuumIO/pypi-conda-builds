package:
  name: abl.robot
  version: "0.2"

source:
  fn: abl.robot-0.2.tar.gz
  url: https://pypi.python.org/packages/source/a/abl.robot/abl.robot-0.2.tar.gz
  md5: 64b88db07e0dd07506af1cfa20eab81f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - abl.robot = abl.robot:main
    #
    # Would create an entry point called abl.robot that calls abl.robot.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - abl.util
    - abl.vpath
    - abl.errorreporter
    - turbomail >=3.0
    - configobj

  run:
    - python
    - abl.util
    - abl.vpath
    - abl.errorreporter
    - turbomail >=3.0
    - configobj

test:
  # Python imports
  imports:
    - abl
    - abl.robot

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT License
  summary: 'The Ableton Robot Framework, for writing daemons or commandline tools, with powerful features for error handling and logging.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
