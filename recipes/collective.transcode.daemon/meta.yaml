package:
  name: collective.transcode.daemon
  version: "0.12"

source:
  fn: collective.transcode.daemon-0.12.zip
  url: https://pypi.python.org/packages/source/c/collective.transcode.daemon/collective.transcode.daemon-0.12.zip
  md5: 241b8e323f015f476c649f5bd2e2d504
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.transcode.daemon = collective.transcode.daemon:main
    #
    # Would create an entry point called collective.transcode.daemon that calls collective.transcode.daemon.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pycrypto
    - twisted
    - pyasn1

  run:
    - python
    - setuptools
    - pycrypto
    - twisted
    - pyasn1

test:
  # Python imports
  imports:
    - collective
    - collective.transcode
    - collective.transcode.daemon

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/plumi/collective.transcode.daemon
  license: GNU General Public License (GPL)
  summary: 'Video transcoding daemon'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
