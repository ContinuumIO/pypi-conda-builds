package:
  name: deflacue
  version: "1.0.0"

source:
  fn: deflacue-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/d/deflacue/deflacue-1.0.0.tar.gz
  md5: 8d7a469cbeab82ae509bbb0d5000ed22
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - deflacue = deflacue:main
    #
    # Would create an entry point called deflacue that calls deflacue.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - deflacue

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/idlesign/deflacue
  license: BSD License
  summary: 'deflacue is a SoX based audio splitter to split audio CD images incorporated with .cue files'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
