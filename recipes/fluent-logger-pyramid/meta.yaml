package:
  name: fluent-logger-pyramid
  version: "0.0.3"

source:
  fn: fluent-logger-pyramid-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/f/fluent-logger-pyramid/fluent-logger-pyramid-0.0.3.tar.gz
  md5: d47d684f3c6f738ff06cc6eb4285acbb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fluent-logger-pyramid = fluent-logger-pyramid:main
    #
    # Would create an entry point called fluent-logger-pyramid that calls fluent-logger-pyramid.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - msgpack-python
    - simplejson

  run:
    - python
    - msgpack-python
    - simplejson

test:
  # Python imports
  imports:
    - fluent

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/nashyeung/fluent-logger-pyramid
  license: Apache License, Version 2.0
  summary: 'A Python logging handler for Fluentd, with better Pyramid integration'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
