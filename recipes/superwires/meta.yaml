package:
  name: superwires
  version: "0.9.2"

source:
  fn: SuperWires-0.9.2.zip
  url: https://pypi.python.org/packages/source/S/SuperWires/SuperWires-0.9.2.zip
  md5: 95ac7de903b5efc5cb825ae6be6172ac
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - superwires = superwires:main
    #
    # Would create an entry point called superwires that calls superwires.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pygame

  run:
    - python
    - pygame

test:
  # Python imports
  imports:
    - superwires

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.supernovaapps.com/
  license: Apache Software License
  summary: "Rewriting of the graphics library in 'Python for the Absolute Beginner' book."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
