package:
  name: easyshop.shop
  version: "0.1a1"

source:
  fn: easyshop.shop-0.1a1.tar.gz
  url: https://pypi.python.org/packages/source/e/easyshop.shop/easyshop.shop-0.1a1.tar.gz
  md5: 08a875875d7d1b5f331926affc6ba15f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - easyshop.shop = easyshop.shop:main
    #
    # Would create an entry point called easyshop.shop that calls easyshop.shop.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - easyshop
    - easyshop.shop
    - easyshop.shop.adapters
    - easyshop.shop.browser
    - easyshop.shop.browser.admin
    - easyshop.shop.content
    - easyshop.shop.docs
    - easyshop.shop.migrations
    - easyshop.shop.portlets
    - easyshop.shop.profiles
    - easyshop.shop.utilities

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/plone/plone.example
  license: GPL
  summary: 'Shop implementation and central services (like currency management) for EasyShop'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
