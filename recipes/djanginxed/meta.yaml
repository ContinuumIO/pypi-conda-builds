package:
  name: djanginxed
  version: "0.0.5"

source:
  fn: djanginxed-0.0.5.tar.gz
  url: https://pypi.python.org/packages/source/d/djanginxed/djanginxed-0.0.5.tar.gz
  md5: 6f02e1cc2be112d04b009fee5167597b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djanginxed = djanginxed:main
    #
    # Would create an entry point called djanginxed that calls djanginxed.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-snippetscream

  run:
    - python
    - django-snippetscream

test:
  # Python imports
  imports:
    - djanginxed
    - djanginxed.decorators

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/shaunsephton/djanginxed
  license: BSD License
  summary: 'Django Nginx Memcached integration.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
