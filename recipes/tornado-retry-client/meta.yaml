package:
  name: tornado-retry-client
  version: "0.1.1"

source:
  fn: tornado-retry-client-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/t/tornado-retry-client/tornado-retry-client-0.1.1.tar.gz
  md5: d233746cf6a0355ef34f6f0e5edafa1a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tornado-retry-client = tornado-retry-client:main
    #
    # Would create an entry point called tornado-retry-client that calls tornado-retry-client.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - tornado

  run:
    - python
    - tornado

test:
  # Python imports
  imports:
    - tornado_retry_client

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/wpjunior/tornado-retry-client
  license: MIT
  summary: 'Simple retry tornado http client'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
