package:
  name: sloth-ci.validators.bitbucket
  version: "1.0.6"

source:
  fn: sloth-ci.validators.bitbucket-1.0.6.zip
  url: https://pypi.python.org/packages/source/s/sloth-ci.validators.bitbucket/sloth-ci.validators.bitbucket-1.0.6.zip
  md5: dd011f5b97cea5abd0bc3a910da2e901
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sloth-ci.validators.bitbucket = sloth-ci.validators.bitbucket:main
    #
    # Would create an entry point called sloth-ci.validators.bitbucket that calls sloth-ci.validators.bitbucket.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sloth_ci >=1.1.9

  run:
    - python
    - sloth_ci >=1.1.9

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/moigagoo/sloth-ci-validators
  license: MIT
  summary: 'Bitbucket validator for Sloth CI'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
