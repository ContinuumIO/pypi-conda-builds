package:
  name: docx-mailmerge
  version: "0.2.0"

source:
  fn: docx-mailmerge-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/docx-mailmerge/docx-mailmerge-0.2.0.tar.gz
  md5: f995c0b3b5194b20b1ea7a8f0970c7b0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - docx-mailmerge = docx-mailmerge:main
    #
    # Would create an entry point called docx-mailmerge that calls docx-mailmerge.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/Bouke/docx-mailmerge
  license: MIT License
  summary: 'Performs a Mail Merge on docx (Microsoft Office Word) files'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
