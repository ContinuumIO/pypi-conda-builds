package:
  name: carotte
  version: "0.2.0"

source:
  fn: carotte-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/c/carotte/carotte-0.2.0.tar.gz
  md5: 0b95c168d8d99cf71bdb4da475f46301
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - carotte = carotte:main
    #
    # Would create an entry point called carotte that calls carotte.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyzmq
    - docopt

  run:
    - python
    - pyzmq
    - docopt

test:
  # Python imports
  imports:
    - carotte
    - carotte.results
    - carotte.results.backends

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/socketubs/carotte
  license: MIT License
  summary: 'Carotte is a very lightweight Celery on zmq'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
