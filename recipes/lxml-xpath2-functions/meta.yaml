package:
  name: lxml-xpath2-functions
  version: "0.0.4"

source:
  fn: lxml-xpath2-functions-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/l/lxml-xpath2-functions/lxml-xpath2-functions-0.0.4.tar.gz
  md5: 0dd24ccf0def1f1b764a6e7ef09ba1f7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lxml-xpath2-functions = lxml-xpath2-functions:main
    #
    # Would create an entry point called lxml-xpath2-functions that calls lxml-xpath2-functions.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - lxml

  run:
    - python
    - lxml

test:
  # Python imports
  imports:
    - xpath2_functions

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/kkujawinski/lxml-xpath2-functions
  license: GNU Lesser General Public License v3 or later (LGPLv3+)
  summary: 'Set of Xpath 2.0 functions which you can register in lxml'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
