package:
  name: chaoflow.testing.crawler
  version: "0.5"

source:
  fn: chaoflow.testing.crawler-0.5.zip
  url: https://pypi.python.org/packages/source/c/chaoflow.testing.crawler/chaoflow.testing.crawler-0.5.zip
  md5: 5e2327c71b8807b76561dc71511d3c30
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - chaoflow.testing.crawler = chaoflow.testing.crawler:main
    #
    # Would create an entry point called chaoflow.testing.crawler that calls chaoflow.testing.crawler.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - chaoflow
    - chaoflow.testing
    - chaoflow.testing.crawler
    - chaoflow.testing.crawler.package
    - chaoflow.testing.crawler.package.subpackage

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/chaoflow/chaoflow.testing.crawler
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Recursively finds tests in your package'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
