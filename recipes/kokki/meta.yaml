package:
  name: kokki
  version: "0.4.1"

source:
  fn: kokki-0.4.1.tar.gz
  url: https://pypi.python.org/packages/source/k/kokki/kokki-0.4.1.tar.gz
  md5: 78f4494f67808f805f17db0741c6172b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kokki = kokki:main
    #
    # Would create an entry point called kokki that calls kokki.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - jinja2

  run:
    - python
    - jinja2

test:
  # Python imports
  imports:
    - kokki
    - kokki.cookbooks
    - kokki.providers
    - kokki.providers.package
    - kokki.providers.service
    - kokki.resources
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://samuelks.com/kokki/
  license: BSD License
  summary: 'Kokki is a system configuration management framework influenced by Chef'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
