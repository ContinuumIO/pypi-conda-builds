package:
  name: django-mini
  version: "0.5.1"

source:
  fn: django-mini-0.5.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-mini/django-mini-0.5.1.tar.gz
  md5: de96a27d55ab6ae533c57132440542db
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-mini = django-mini:main
    #
    # Would create an entry point called django-mini that calls django-mini.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/davidwtbuxton/django-mini
  license: MIT License
  summary: 'Run plug-able Django apps without a settings module'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
