package:
  name: js.jquery_tinyscrollbar
  version: "1.67"

source:
  fn: js.jquery_tinyscrollbar-1.67.zip
  url: https://pypi.python.org/packages/source/j/js.jquery_tinyscrollbar/js.jquery_tinyscrollbar-1.67.zip
  md5: 23bc12e131413e40fd7431130251e316
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - js.jquery_tinyscrollbar = js.jquery_tinyscrollbar:main
    #
    # Would create an entry point called js.jquery_tinyscrollbar that calls js.jquery_tinyscrollbar.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fanstatic
    - setuptools
    - js.jquery

  run:
    - python
    - setuptools
    - fanstatic
    - setuptools
    - js.jquery

test:
  # Python imports
  imports:
    - js
    - js.jquery_tinyscrollbar

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD
  summary: 'Fanstatic packaging of jQuery Tiny Scrollbar'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
