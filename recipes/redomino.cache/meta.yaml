package:
  name: redomino.cache
  version: "0.1"

source:
  fn: redomino.cache-0.1.tar.gz
  url: https://pypi.python.org/packages/source/r/redomino.cache/redomino.cache-0.1.tar.gz
  md5: e39884ed31feb153ac1086c8bb896aed
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - redomino.cache = redomino.cache:main
    #
    # Would create an entry point called redomino.cache that calls redomino.cache.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.caching

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.caching

test:
  # Python imports
  imports:
    - redomino
    - redomino.cache

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/redomino/redomino.cache
  license: GPL
  summary: "'Plone app caching preconfigured'"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
