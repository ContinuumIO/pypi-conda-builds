package:
  name: django-networth
  version: "1.5"

source:
  fn: django-networth-1.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-networth/django-networth-1.5.tar.gz
  md5: 8e7095ba8053d34e484eeb3b6ca3fc8b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-networth = django-networth:main
    #
    # Would create an entry point called django-networth that calls django-networth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - networth

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Hedde/django-networth
  license: MIT
  summary: 'Valuate instances of Django Models.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
