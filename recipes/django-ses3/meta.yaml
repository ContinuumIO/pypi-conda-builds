package:
  name: django-ses3
  version: "0.7.0"

source:
  fn: django-ses3-0.7.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-ses3/django-ses3-0.7.0.tar.gz
  md5: 274be0a683f2a3972bebc2d4ebe844d0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-ses3 = django-ses3:main
    #
    # Would create an entry point called django-ses3 that calls django-ses3.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4
    - boto >=2.1.0

  run:
    - python
    - django >=1.4
    - boto >=2.1.0

test:
  # Python imports
  imports:
    - django_ses
    - django_ses.management
    - django_ses.management.commands
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/tricoder42/django-ses/
  license: MIT License
  summary: u"A Django email backend for Amazon's Simple Email Service"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
