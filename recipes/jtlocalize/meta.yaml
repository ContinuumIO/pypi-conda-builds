package:
  name: jtlocalize
  version: "0.1.0"

source:
  fn: jtlocalize-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/j/jtlocalize/jtlocalize-0.1.0.tar.gz
  md5: 411ba786709fe308252b8c1b49a95dda
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jtlocalize = jtlocalize:main
    #
    # Would create an entry point called jtlocalize that calls jtlocalize.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - jtlocalize
    - jtlocalize.configuration
    - jtlocalize.core

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/joytunes/JTLocalize/
  license: MIT
  summary: 'iOS localization framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
