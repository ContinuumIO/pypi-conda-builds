package:
  name: elasticstack
  version: "0.1.1"

source:
  fn: elasticstack-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/e/elasticstack/elasticstack-0.1.1.tar.gz
  md5: 2b6c627cbe1d084bcf3b4bbd7aac6f32
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - elasticstack = elasticstack:main
    #
    # Would create an entry point called elasticstack that calls elasticstack.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4
    - django-haystack >=2.0.0
    - pyelasticsearch >=0.5

  run:
    - python
    - django >=1.4
    - django-haystack >=2.0.0
    - pyelasticsearch >=0.5

test:
  # Python imports
  imports:
    - elasticstack

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bennylope/elasticstack
  license: BSD License
  summary: 'Configurable indexing and other extras for Haystack (with ElasticSearch biases).'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
