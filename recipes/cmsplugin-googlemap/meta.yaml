package:
  name: cmsplugin-googlemap
  version: "0.1.5"

source:
  fn: cmsplugin-googlemap-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/c/cmsplugin-googlemap/cmsplugin-googlemap-0.1.5.tar.gz
  md5: 69aef164bb762084057402e9982eb2c4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cmsplugin-googlemap = cmsplugin-googlemap:main
    #
    # Would create an entry point called cmsplugin-googlemap that calls cmsplugin-googlemap.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4.3
    - south
    - pillow

  run:
    - python
    - django >=1.4.3
    - south
    - pillow

test:
  # Python imports
  imports:
    - cmsplugin_googlemap
    - cmsplugin_googlemap.migrations
    - cmsplugin_googlemap.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bitmazk/cmsplugin-googlemap
  license: The MIT License
  summary: 'An app for integrating a google map in your cms layout.\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
