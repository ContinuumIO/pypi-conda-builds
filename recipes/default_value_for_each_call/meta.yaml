package:
  name: default_value_for_each_call
  version: "0.6"

source:
  fn: default_value_for_each_call-0.6.tar.gz
  url: https://pypi.python.org/packages/source/d/default_value_for_each_call/default_value_for_each_call-0.6.tar.gz
  md5: da9c3ffb414aa7317f5e338cfb656f48
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - default_value_for_each_call = default_value_for_each_call:main
    #
    # Would create an entry point called default_value_for_each_call that calls default_value_for_each_call.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - default_value_for_each_call

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD
  summary: 'compute function default value for each call'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
