package:
  name: collective.portlet.discussion
  version: "1.2.2"

source:
  fn: collective.portlet.discussion-1.2.2.zip
  url: https://pypi.python.org/packages/source/c/collective.portlet.discussion/collective.portlet.discussion-1.2.2.zip
  md5: da6b012335d547fb7212d950c7f0fc5e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.portlet.discussion = collective.portlet.discussion:main
    #
    # Would create an entry point called collective.portlet.discussion that calls collective.portlet.discussion.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.discussion

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.discussion

test:
  # Python imports
  imports:
    - collective
    - collective.portlet
    - collective.portlet.discussion
    - collective.portlet.discussion.browser
    - collective.portlet.discussion.tests
    - collective.portlet.discussion.utility

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/collective.portlet.discussion
  license: GPL
  summary: 'A simple Plone portlet to show a list of comments'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
