package:
  name: languagelabapi
  version: "0.3"

source:
  fn: LanguagelabApi-0.3.tar.gz
  url: https://pypi.python.org/packages/source/L/LanguagelabApi/LanguagelabApi-0.3.tar.gz
  md5: 719ef330275dfb8963a8fd1a650bfd53
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - languagelabapi = languagelabapi:main
    #
    # Would create an entry point called languagelabapi that calls languagelabapi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - simplejson

  run:
    - python
    - setuptools
    - simplejson

test:
  # Python imports
  imports:
    - languagelabapi
    - llapi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.bitbucket.org/Splee/languagelabapi/
  license: MIT License
  summary: 'A Python implementation of the Languagelab API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
