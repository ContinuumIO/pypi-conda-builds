package:
  name: django-viewclass-mixins
  version: "1.1.3"

source:
  fn: django-viewclass-mixins-1.1.3.zip
  url: https://pypi.python.org/packages/source/d/django-viewclass-mixins/django-viewclass-mixins-1.1.3.zip
  md5: a20b09097cab2d207958be2e723101af
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-viewclass-mixins = django-viewclass-mixins:main
    #
    # Would create an entry point called django-viewclass-mixins that calls django-viewclass-mixins.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3

  run:
    - python
    - django >=1.3

test:
  # Python imports
  imports:
    - viewclass_mixins

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/nextscreenlabs/django-viewclass-mixins
  license: BSD License
  summary: 'Helpful mixins for Django View classes.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
