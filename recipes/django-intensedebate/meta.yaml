package:
  name: django-intensedebate
  version: "0.6.0"

source:
  fn: django-intensedebate-0.6.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-intensedebate/django-intensedebate-0.6.0.tar.gz
  md5: 9402e05113104afa5bf9057b3f7c11f3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-intensedebate = django-intensedebate:main
    #
    # Would create an entry point called django-intensedebate that calls django-intensedebate.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4,<=1.6.11

  run:
    - python
    - django >=1.4,<=1.6.11

test:
  # Python imports
  imports:
    - intensedebate
    - intensedebate.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/raymondwanyoike/django-intensedebate/
  license: GNU General Public License v3 (GPLv3)
  summary: 'Simple integration of the IntenseDebate comment widget for Django projects.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
