package:
  name: ella-filer-galleries
  version: "1.0.0"

source:
  fn: ella-filer-galleries-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/e/ella-filer-galleries/ella-filer-galleries-1.0.0.tar.gz
  md5: 266816a531605a847d2186e3cece5ac3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ella-filer-galleries = ella-filer-galleries:main
    #
    # Would create an entry point called ella-filer-galleries that calls ella-filer-galleries.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools >=0.6b1
    - ella >=3.0.0
    - ella-galleries

  run:
    - python
    - setuptools >=0.6b1
    - ella >=3.0.0
    - ella-galleries

test:
  # Python imports
  imports:
    - ella_filer_galleries

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/xaralis/ella-filer-galleries
  license: BSD License
  summary: 'Link Ella galleries to the django-filer folders.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
