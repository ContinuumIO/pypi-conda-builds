package:
  name: bambu-blog
  version: "2.1"

source:
  fn: bambu-blog-2.1.tar.gz
  url: https://pypi.python.org/packages/source/b/bambu-blog/bambu-blog-2.1.tar.gz
  md5: 1ce989f3f1f55bc82ec6e0ce99dd9637
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bambu-blog = bambu-blog:main
    #
    # Would create an entry point called bambu-blog that calls bambu-blog.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4
    - pyquery
    - html2text
    - django-taggit
    - sorl-thumbnail
    - bambu-markup >=2.0.0
    - bambu-oembed >=2.0.0
    - bambu-attachments >=2.0.0
    - bambu-xmlrpc >=2.0

  run:
    - python
    - django >=1.4
    - pyquery
    - html2text
    - django-taggit
    - sorl-thumbnail
    - bambu-markup >=2.0.0
    - bambu-oembed >=2.0.0
    - bambu-attachments >=2.0.0
    - bambu-xmlrpc >=2.0

test:
  # Python imports
  imports:
    - bambu_blog
    - bambu_blog.management
    - bambu_blog.management.commands
    - bambu_blog.migrations
    - bambu_blog.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/iamsteadman/bambu-blog
  license: UNKNOWN
  summary: 'A simple set of models for a basic blog, with some tools for custom-designed blog post writing'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
