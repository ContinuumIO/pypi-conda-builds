package:
  name: django-s3direct
  version: "0.3.10"

source:
  fn: django-s3direct-0.3.10.tar.gz
  url: https://pypi.python.org/packages/source/d/django-s3direct/django-s3direct-0.3.10.tar.gz
  md5: ba9f0f7dea0c6e6cd576635b6d74097f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-s3direct = django-s3direct:main
    #
    # Would create an entry point called django-s3direct that calls django-s3direct.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6.2

  run:
    - python
    - django >=1.6.2

test:
  # Python imports
  imports:
    - s3direct

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bradleyg/django-s3direct
  license: MIT License
  summary: 'Add direct uploads to S3 functionality with a progress bar to file input fields.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
