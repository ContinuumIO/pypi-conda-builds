package:
  name: django-sqlserver
  version: "1.7"

source:
  fn: django-sqlserver-1.7.tar.gz
  url: https://pypi.python.org/packages/source/d/django-sqlserver/django-sqlserver-1.7.tar.gz
  md5: a928c8a60116d97448eb9a9f2549e8fd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-sqlserver = django-sqlserver:main
    #
    # Would create an entry point called django-sqlserver that calls django-sqlserver.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-mssql >=1.6.1,<1.7

  run:
    - python
    - django-mssql >=1.6.1,<1.7

test:
  # Python imports
  imports:
    - sqlserver

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/denisenkom/django-sqlserver
  license: MIT
  summary: 'Django backend database support for MS SQL Server and pytds.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
