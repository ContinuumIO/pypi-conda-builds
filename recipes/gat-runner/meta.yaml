package:
  name: gat-runner
  version: "0.0.2"

source:
  fn: gat-runner-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/g/gat-runner/gat-runner-0.0.2.tar.gz
  md5: a3a211ccddeb3033ff9c654f1303ef87
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gat-runner = gat-runner:main
    #
    # Would create an entry point called gat-runner that calls gat-runner.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six ==1.3.0
    - simplejson ==3.1.3
    - gat-games ==0.0.2

  run:
    - python
    - six ==1.3.0
    - simplejson ==3.1.3
    - gat-games ==0.0.2

test:
  # Python imports
  imports:
    - gat_runner
    - gat_runner.players
    - tests
    - tests.games
    - tests.players

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/gatournament/gat-runner
  license: MIT
  summary: 'GAT Runner'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
