package:
  name: luxweather
  version: "1.0.1"

source:
  fn: luxweather-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/l/luxweather/luxweather-1.0.1.tar.gz
  md5: 5a652f8c6c81522112ef0720c3a805e2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - luxweather = luxweather:main
    #
    # Would create an entry point called luxweather that calls luxweather.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - luxweather

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/c0ding/LuxWeather
  license: Apache v2.0 License
  summary: 'luxweather is an APACHE licensed Twitter weather bot written in Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
