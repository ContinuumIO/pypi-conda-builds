package:
  name: hyde-zipper
  version: "0.0.3"

source:
  fn: hyde-zipper-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/h/hyde-zipper/hyde-zipper-0.0.3.tar.gz
  md5: c0813c38c1a40d29122ec910bf76809c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hyde-zipper = hyde-zipper:main
    #
    # Would create an entry point called hyde-zipper that calls hyde-zipper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - zipper

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/dstufft/hyde-zipper
  license: BSD
  summary: 'gzip precompression as post processor'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
