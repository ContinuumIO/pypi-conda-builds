package:
  name: linotpadminclientcli
  version: "2.7.1.2"

source:
  fn: LinOTPAdminClientCLI-2.7.1.2.tar.gz
  url: https://pypi.python.org/packages/source/L/LinOTPAdminClientCLI/LinOTPAdminClientCLI-2.7.1.2.tar.gz
  md5: ff7b847ea58c42145d657691b68d400f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - linotpadminclientcli = linotpadminclientcli:main
    #
    # Would create an entry point called linotpadminclientcli that calls linotpadminclientcli.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - linotpadminclientcli
    - linotputils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.linotp.org
  license: AGPLv3, (C) LSE Leading Security Experts GmbH
  summary: 'LinOTP command-line client'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
