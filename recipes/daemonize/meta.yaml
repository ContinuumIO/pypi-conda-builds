package:
  name: daemonize
  version: "2.3.1"

source:
  fn: daemonize-2.3.1.tar.gz
  url: https://pypi.python.org/packages/source/d/daemonize/daemonize-2.3.1.tar.gz
  md5: f0d401a0a501ba9d36e516ea89146eb2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - daemonize = daemonize:main
    #
    # Would create an entry point called daemonize that calls daemonize.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/thesharp/daemonize
  license: MIT License
  summary: 'Library to enable your code run as a daemon process on Unix-like systems.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
