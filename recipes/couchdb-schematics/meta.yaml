package:
  name: couchdb-schematics
  version: "1.1.1"

source:
  fn: couchdb-schematics-1.1.1.tar.gz
  url: https://pypi.python.org/packages/source/c/couchdb-schematics/couchdb-schematics-1.1.1.tar.gz
  md5: 35b26d0f2da15ac335b5d0d96c9b3b06
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - couchdb-schematics = couchdb-schematics:main
    #
    # Would create an entry point called couchdb-schematics that calls couchdb-schematics.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools >=0.8
    - couchdb
    - schematics
    - pytest

  run:
    - python
    - setuptools >=0.8
    - couchdb
    - schematics
    - pytest

test:
  # Python imports
  imports:
    - couchdb_schematics

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/ryanolson/couchdb-schematics
  license: BSD
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
