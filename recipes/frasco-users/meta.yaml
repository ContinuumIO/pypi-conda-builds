package:
  name: frasco-users
  version: "0.1.1"

source:
  fn: frasco-users-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/f/frasco-users/frasco-users-0.1.1.tar.gz
  md5: d4a94d5a6b9b643c30fbadfbf32cf1f7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - frasco-users = frasco-users:main
    #
    # Would create an entry point called frasco-users that calls frasco-users.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - frasco
    - frasco-forms
    - frasco-models
    - flask-login >=0.2.11
    - flask-bcrypt >=0.6.0
    - flask-oauthlib >=0.8.0

  run:
    - python
    - frasco
    - frasco-forms
    - frasco-models
    - flask-login >=0.2.11
    - flask-bcrypt >=0.6.0
    - flask-oauthlib >=0.8.0

test:
  # Python imports
  imports:
    - frasco_users
    - frasco_users.admin

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/frascoweb/frasco-users
  license: MIT
  summary: 'Users management for Frasco'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
