package:
  name: django-shop-bulkform
  version: "0.1.0"

source:
  fn: django-shop-bulkform-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-shop-bulkform/django-shop-bulkform-0.1.0.tar.gz
  md5: 5202c7c7ddd479a171075477941f8c82
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-shop-bulkform = django-shop-bulkform:main
    #
    # Would create an entry point called django-shop-bulkform that calls django-shop-bulkform.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - shop_bulkform

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/powellc/django-shop-bulkform
  license: BSD License
  summary: 'A bulk order form for a django-shop site.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
