package:
  name: bosrvclient
  version: "0.1.8"

source:
  fn: bosrvclient-0.1.8.tar.gz
  url: https://pypi.python.org/packages/source/b/bosrvclient/bosrvclient-0.1.8.tar.gz
  md5: 688e2b72b3c47b4be61e23b9e429e79b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bosrvclient = bosrvclient:main
    #
    # Would create an entry point called bosrvclient that calls bosrvclient.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - thrift

  run:
    - python
    - thrift

test:
  # Python imports
  imports:
    - bosrv
    - bosrvclient
    - exc

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/macrodata/bosrvclient-py.git
  license: Apache 2.0
  summary: 'BigObject service client for python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
