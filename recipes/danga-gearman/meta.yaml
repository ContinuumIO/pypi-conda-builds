package:
  name: danga-gearman
  version: "2.0.0"

source:
  fn: danga-gearman-2.0.0.tar.gz
  url: https://pypi.python.org/packages/source/d/danga-gearman/danga-gearman-2.0.0.tar.gz
  md5: df0b706e32493b998d4d9c9f2057a928
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - danga-gearman = danga-gearman:main
    #
    # Would create an entry point called danga-gearman that calls danga-gearman.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - dangagearman

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/saymedia/python-danga-gearman/tree/master
  license: MIT License
  summary: 'Client for the Danga (Perl) Gearman implementation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
