package:
  name: holygrail
  version: "0.2.1.Perceval"

source:
  fn: HolyGrail-0.2.1.Perceval.tar.gz
  url: https://pypi.python.org/packages/source/H/HolyGrail/HolyGrail-0.2.1.Perceval.tar.gz
  md5: 031fc2faff26849d7e05d415262f9db7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - holygrail = holygrail:main
    #
    # Would create an entry point called holygrail that calls holygrail.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sqlobject

  run:
    - python
    - sqlobject

test:
  # Python imports
  imports:
    - holygrail

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/psycojoker/holygrail
  license: aGPLv3+
  summary: 'High level lib to manage the holy quests of your life (in other words GTD next actions)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
