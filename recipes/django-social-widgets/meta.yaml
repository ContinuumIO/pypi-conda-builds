package:
  name: django-social-widgets
  version: "0.2.1"

source:
  fn: django-social-widgets-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-social-widgets/django-social-widgets-0.2.1.tar.gz
  md5: 110c5f7c0a2a2d4b6c3502729cac6a76
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-social-widgets = django-social-widgets:main
    #
    # Would create an entry point called django-social-widgets that calls django-social-widgets.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5

  run:
    - python
    - django >=1.5

test:
  # Python imports
  imports:
    - social_widgets

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/creafz/django-social-widgets
  license: MIT License
  summary: 'Django app for easy embedding social network widgets and plugins into your site. Supports Facebook, Twitter, Google+, YouTube, Instagram and Pinterest.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
