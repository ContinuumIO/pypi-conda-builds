package:
  name: zope.index
  version: "4.1.0"

source:
  fn: zope.index-4.1.0.tar.gz
  url: https://pypi.python.org/packages/source/z/zope.index/zope.index-4.1.0.tar.gz
  md5: 8ba6562677e5c7a900ed3b84053e7549
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zope.index = zope.index:main
    #
    # Would create an entry point called zope.index that calls zope.index.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - persistent
    - btrees
    - setuptools
    - six
    - zope.interface

  run:
    - python
    - persistent
    - btrees
    - setuptools
    - six
    - zope.interface

test:
  # Python imports
  imports:
    - zope
    - zope.index
    - zope.index.field
    - zope.index.keyword
    - zope.index.text
    - zope.index.text.tests
    - zope.index.topic
    - zope.index.topic.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/zope.index
  license: Zope Public License
  summary: 'Indices for using with catalog like text, field, etc.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
