package:
  name: youbl_json_validator
  version: "0.1.3"

source:
  fn: youbl_json_validator-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/y/youbl_json_validator/youbl_json_validator-0.1.3.tar.gz
  md5: a298ed25525cb5514ca7bf1b32358830
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - youbl_json_validator = youbl_json_validator:main
    #
    # Would create an entry point called youbl_json_validator that calls youbl_json_validator.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - jsonschema

  run:
    - python
    - jsonschema

test:
  # Python imports
  imports:
    - youbl_json_validator
    - youbl_json_validator.schemas

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/PythonicNinja/youbl_json_validator
  license: BSD License
  summary: 'Youbl JSON validator written in python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
