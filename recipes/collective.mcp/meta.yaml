package:
  name: collective.mcp
  version: "0.4"

source:
  fn: collective.mcp-0.4.zip
  url: https://pypi.python.org/packages/source/c/collective.mcp/collective.mcp-0.4.zip
  md5: a27811e8efbd5c6cd957b18059e0c8b6
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.mcp = collective.mcp:main
    #
    # Would create an entry point called collective.mcp that calls collective.mcp.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.multimodeview

  run:
    - python
    - setuptools
    - setuptools
    - collective.multimodeview

test:
  # Python imports
  imports:
    - collective
    - collective.mcp
    - collective.mcp.browser
    - collective.mcp.samples
    - collective.mcp.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/zestsoftware/collective.mcp
  license: GPL
  summary: 'Macish Control Panel for Plone.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
