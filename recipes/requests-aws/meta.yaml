package:
  name: requests-aws
  version: "0.1.6"

source:
  fn: requests-aws-0.1.6.tar.gz
  url: https://pypi.python.org/packages/source/r/requests-aws/requests-aws-0.1.6.tar.gz
  md5: 798aa93ac910de84ec4cc43d3c5e7ca2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - requests-aws = requests-aws:main
    #
    # Would create an entry point called requests-aws that calls requests-aws.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=0.14.0

  run:
    - python
    - requests >=0.14.0

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tax/python-requests-aws
  license: BSD licence, see LICENCE.txt
  summary: 'AWS authentication for Amazon S3 for the python requests module'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
