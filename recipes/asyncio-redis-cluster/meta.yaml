package:
  name: asyncio-redis-cluster
  version: "0.1.1"

source:
  fn: asyncio-redis-cluster-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/a/asyncio-redis-cluster/asyncio-redis-cluster-0.1.1.tar.gz
  md5: 2193c65e521f6fc07639fe42b2d1f5fa
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - asyncio-redis-cluster = asyncio-redis-cluster:main
    #
    # Would create an entry point called asyncio-redis-cluster that calls asyncio-redis-cluster.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - asyncio

  run:
    - python
    - asyncio

test:
  # Python imports
  imports:
    - asyncio_redis_cluster

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/hackerexperience/asyncio-redis-cluster
  license: LICENSE.txt
  summary: 'Cluster support for asyncio Redis client.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
