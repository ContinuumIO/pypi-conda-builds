package:
  name: django-oml
  version: "0.0.3"

source:
  fn: django-oml-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-oml/django-oml-0.0.3.tar.gz
  md5: b6a982969b5db90ffd491a7312c0649c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-oml = django-oml:main
    #
    # Would create an entry point called django-oml that calls django-oml.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - oml

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.routeatlas.com
  license: GPL
  summary: u'An app that allows you moderate content'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
