package:
  name: devilry_xmlrpc_client
  version: "0.1.7"

source:
  fn: devilry_xmlrpc_client-0.1.7.tar.gz
  url: https://pypi.python.org/packages/source/d/devilry_xmlrpc_client/devilry_xmlrpc_client-0.1.7.tar.gz
  md5: 6fd747252e43e206e18960bd08f1ff12
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - devilry_xmlrpc_client = devilry_xmlrpc_client:main
    #
    # Would create an entry point called devilry_xmlrpc_client that calls devilry_xmlrpc_client.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - devilry_xmlrpc_client

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://devilry.github.com
  license: UNKNOWN
  summary: 'The devilry xmlrpc client and shell scripts.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
