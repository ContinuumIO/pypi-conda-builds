package:
  name: python-twitch
  version: "0.4.3"

source:
  fn: python-twitch-0.4.3.tar.gz
  url: https://pypi.python.org/packages/source/p/python-twitch/python-twitch-0.4.3.tar.gz
  md5: 2c64f1a5903505921e85a9b6666bc47f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-twitch = python-twitch:main
    #
    # Would create an entry point called python-twitch that calls python-twitch.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - twitch

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://pypi.python.org/pypi/python-twitch
  license: GPLv3
  summary: 'Python Module for interaction with twitch.tv'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
