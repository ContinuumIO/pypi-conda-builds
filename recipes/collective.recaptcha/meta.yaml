package:
  name: collective.recaptcha
  version: "1.1.5"

source:
  fn: collective.recaptcha-1.1.5.zip
  url: https://pypi.python.org/packages/source/c/collective.recaptcha/collective.recaptcha-1.1.5.zip
  md5: f5b8277eb5b12cd203bc02fe643693d5
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.recaptcha = collective.recaptcha:main
    #
    # Would create an entry point called collective.recaptcha that calls collective.recaptcha.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - recaptcha-client >=1.0.6
    - plone

  run:
    - python
    - setuptools
    - setuptools
    - recaptcha-client >=1.0.6
    - plone

test:
  # Python imports
  imports:
    - collective
    - collective.recaptcha
    - collective.recaptcha.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/collective.recaptcha
  license: LGPL
  summary: 'Wraps the recaptcha-client library to provide a drop-in replacement for collective.captcha.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
