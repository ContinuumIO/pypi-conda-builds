package:
  name: conjur
  version: "0.3.2"

source:
  fn: Conjur-0.3.2.tar.gz
  url: https://pypi.python.org/packages/source/C/Conjur/Conjur-0.3.2.tar.gz
  md5: 58dd7ef9de39daf0b392570530753514
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - conjur = conjur:main
    #
    # Would create an entry point called conjur that calls conjur.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyyaml
    - requests >=2.2.1

  run:
    - python
    - pyyaml
    - requests >=2.2.1

test:
  # Python imports
  imports:
    - conjur
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/conjurinc/api-python
  license: MIT License
  summary: 'Python client for the Conjur API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
