package:
  name: bitfile
  version: "0.2"

source:
  fn: bitfile-0.2.tar.gz
  url: https://pypi.python.org/packages/source/b/bitfile/bitfile-0.2.tar.gz
  md5: 1c8f402a4a092bd3efae2c5ea1725972
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bitfile = bitfile:main
    #
    # Would create an entry point called bitfile that calls bitfile.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - bitfile

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://michael.dipperstein.com/bitlibs/
  license: GNU General Public License (GPL)
  summary: 'Module for reading/writing an arbitrary number of bits from a file.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
