package:
  name: django-simplesearch
  version: "1.0"

source:
  fn: django-simplesearch-1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-simplesearch/django-simplesearch-1.0.tar.gz
  md5: 47c73df6bc4c0ce291b791568b049f57
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-simplesearch = django-simplesearch:main
    #
    # Would create an entry point called django-simplesearch that calls django-simplesearch.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - simplesearch

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/pigmonkey/django-simplesearch
  license: UNKNOWN
  summary: 'A basic search application for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
