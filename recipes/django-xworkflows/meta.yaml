package:
  name: django-xworkflows
  version: "0.9.4"

source:
  fn: django-xworkflows-0.9.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-xworkflows/django-xworkflows-0.9.4.tar.gz
  md5: ad43962ce975ce70d302f62c7e803892
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-xworkflows = django-xworkflows:main
    #
    # Would create an entry point called django-xworkflows that calls django-xworkflows.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - xworkflows >=0.4.0
    - django >=1.3

  run:
    - python
    - xworkflows >=0.4.0
    - django >=1.3

test:
  # Python imports
  imports:
    - django_xworkflows
    - django_xworkflows.management
    - django_xworkflows.management.commands
    - django_xworkflows.xworkflow_log
    - django_xworkflows.xworkflow_log.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/rbarrois/django_xworkflows
  license: BSD License
  summary: 'A django app enabling Django models to use xworkflows.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
