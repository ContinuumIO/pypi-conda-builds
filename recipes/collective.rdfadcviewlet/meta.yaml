package:
  name: collective.rdfadcviewlet
  version: "1.0"

source:
  fn: collective.rdfadcviewlet-1.0.zip
  url: https://pypi.python.org/packages/source/c/collective.rdfadcviewlet/collective.rdfadcviewlet-1.0.zip
  md5: 781dc56496e3a46b7081d380180014a8
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.rdfadcviewlet = collective.rdfadcviewlet:main
    #
    # Would create an entry point called collective.rdfadcviewlet that calls collective.rdfadcviewlet.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.rdfadcviewlet

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/
  license: GPL
  summary: 'Adds a viewlet that adds on your page meta with dublin core values in rdfa format, by ecreall'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
