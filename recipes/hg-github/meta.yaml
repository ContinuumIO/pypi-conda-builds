package:
  name: hg-github
  version: "0.1.5"

source:
  fn: hg-github-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/h/hg-github/hg-github-0.1.5.tar.gz
  md5: 847ca521bf8032e5defc3851db36d2c8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hg-github = hg-github:main
    #
    # Would create an entry point called hg-github that calls hg-github.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - hg-git
    - sphinx-me

  run:
    - python
    - hg-git
    - sphinx-me

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/stephenmcd/hg-github
  license: BSD License
  summary: 'A Mercurial extension for working with GitHub repositories'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
