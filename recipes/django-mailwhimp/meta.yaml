package:
  name: django-mailwhimp
  version: "0.1"

source:
  fn: django-mailwhimp-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-mailwhimp/django-mailwhimp-0.1.tar.gz
  md5: f5947e0b8cc4a2a32954991e19d7ce70
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-mailwhimp = django-mailwhimp:main
    #
    # Would create an entry point called django-mailwhimp that calls django-mailwhimp.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - mailsnake
    - django

  run:
    - python
    - mailsnake
    - django

test:
  # Python imports
  imports:
    - mailwhimp
    - mailwhimp.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/Celc/django-mailwhimp
  license: MIT License
  summary: 'django-mailwhimp integrates mailchimp into Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
