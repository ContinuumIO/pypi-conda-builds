package:
  name: errorreport
  version: "0.3.2"

source:
  fn: ErrorReport-0.3.2.tar.gz
  url: https://pypi.python.org/packages/source/E/ErrorReport/ErrorReport-0.3.2.tar.gz
  md5: 07c2ec4232e5336faba3c8570b6fa4a6
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - errorreport = errorreport:main
    #
    # Would create an entry point called errorreport that calls errorreport.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - errorreport

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://jimmyg.org/work/code/errorreport/index.html
  license: GNU Affero General Public License v3
  summary: 'Provide HTML error reports during development of web applications and email reports for errors occurring in production.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
