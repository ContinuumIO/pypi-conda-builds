package:
  name: asv_meta
  version: "dev-20120127-01"

source:
  fn: asv_meta-dev-20120127-01.tar.gz
  url: https://pypi.python.org/packages/source/a/asv_meta/asv_meta-dev-20120127-01.tar.gz
  md5: d8a4b81d632bbfd03eb49e326307b777
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - asv_meta = asv_meta:main
    #
    # Would create an entry point called asv_meta that calls asv_meta.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pytils >=0.2.3
    - asv-imgs >=dev-20111110-01
    - asv-seo >=dev-20110720-01
    - asv-txt >=dev-20110709-01
    - asv-utils >=dev-20120123-01
    - django >=1.3.1
    - django-mptt >=0.5.2
    - django-redis-cache >=0.6.0
    - html5lib >=0.90
    - lxml >=2.3
    - south >=0.7.3

  run:
    - python
    - pytils >=0.2.3
    - asv-imgs >=dev-20111110-01
    - asv-seo >=dev-20110720-01
    - asv-txt >=dev-20110709-01
    - asv-utils >=dev-20120123-01
    - django >=1.3.1
    - django-mptt >=0.5.2
    - django-redis-cache >=0.6.0
    - html5lib >=0.90
    - lxml >=2.3
    - south >=0.7.3

test:
  # Python imports
  imports:
    - asv_meta

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/xenolog/asv_meta/wiki/Home
  license: GPL
  summary: '\nThis is a metapackage for quick installing other asv_* packages.\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
