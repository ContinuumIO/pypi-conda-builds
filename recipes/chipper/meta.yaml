package:
  name: chipper
  version: "0.1"

source:
  fn: chipper-0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/chipper/chipper-0.1.tar.gz
  md5: e1d16eefd02ae267811518182047d079
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - chipper = chipper:main
    #
    # Would create an entry point called chipper that calls chipper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - deconf

  run:
    - python
    - deconf

test:
  # Python imports
  imports:
    - chipper

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dustinlacewell/chipper
  license: UNKNOWN
  summary: 'Refreshingly simple declarative logging that utilizes arbitrary tag sinks instead of traditional level handling'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
