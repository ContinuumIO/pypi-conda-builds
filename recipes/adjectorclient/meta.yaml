package:
  name: adjectorclient
  version: "1.0b2"

source:
  fn: AdjectorClient-1.0b2.tar.gz
  url: https://pypi.python.org/packages/source/A/AdjectorClient/AdjectorClient-1.0b2.tar.gz
  md5: fb0409236b85ee26138744cbfc2814af
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - adjectorclient = adjectorclient:main
    #
    # Would create an entry point called adjectorclient that calls adjectorclient.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sqlalchemy >=0.5
    - elixir >=0.6.1
    - pytz
    - pysqlite >=2.0

  run:
    - python
    - sqlalchemy >=0.5
    - elixir >=0.6.1
    - pytz
    - pysqlite >=2.0

test:
  # Python imports
  imports:
    - adjector
    - adjector.client
    - adjector.core
    - adjector.core.contrib
    - adjector.model
    - adjectorclient

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://projects.icapsid.net/adjector
  license: GPL v2 or v3 (at your option)
  summary: 'Client-only package for Adjector, a lightweight, fast, flexible ad server.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
