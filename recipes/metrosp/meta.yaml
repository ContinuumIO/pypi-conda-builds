package:
  name: metrosp
  version: "0.1.0"

source:
  fn: metrosp-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/m/metrosp/metrosp-0.1.0.tar.gz
  md5: a1fc6554dad026ac47ba68722d87b00f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - metrosp = metrosp:main
    #
    # Would create an entry point called metrosp that calls metrosp.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - beautifulsoup4 >=4.3.2
    - httplib2 ==0.7.4

  run:
    - python
    - beautifulsoup4 >=4.3.2
    - httplib2 ==0.7.4

test:
  # Python imports
  imports:
    - metrosp

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/renatolipi/status_metro_sp
  license: LGPLv3
  summary: 'Ferramenta de linha de comando para recuperar situa\xc3\xa7\xc3\xa3o atual do Metro de SP'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
