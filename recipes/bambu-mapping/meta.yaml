package:
  name: bambu-mapping
  version: "0.1.1"

source:
  fn: bambu-mapping-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/b/bambu-mapping/bambu-mapping-0.1.1.tar.gz
  md5: dd836da6ab4f0423df365d6cc6e95a10
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bambu-mapping = bambu-mapping:main
    #
    # Would create an entry point called bambu-mapping that calls bambu-mapping.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django >=1.4

  run:
    - python
    - django >=1.4

test:
  # Python imports
  imports:
    - bambu.mapping
    - bambu.mapping.providers
    - bambu.mapping.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/bambu-mapping
  license: UNKNOWN
  summary: 'A pluggable, provider-based system for rendering maps'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
