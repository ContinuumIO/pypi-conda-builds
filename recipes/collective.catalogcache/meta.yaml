package:
  name: collective.catalogcache
  version: "0.2"

source:
  fn: collective.catalogcache-0.2.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.catalogcache/collective.catalogcache-0.2.tar.gz
  md5: 9f654ddea5a241c004c5b346b1602de1
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.catalogcache = collective.catalogcache:main
    #
    # Would create an entry point called collective.catalogcache that calls collective.catalogcache.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.catalogcache

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: GPL
  summary: 'Cache ZCatalog results with memcached - experimental'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
