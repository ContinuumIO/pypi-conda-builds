package:
  name: latimes-statestyle
  version: "0.1.6"

source:
  fn: latimes-statestyle-0.1.6.tar.gz
  url: https://pypi.python.org/packages/source/l/latimes-statestyle/latimes-statestyle-0.1.6.tar.gz
  md5: 69907a834d27a3f02625a95af9643475
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - latimes-statestyle = latimes-statestyle:main
    #
    # Would create an entry point called latimes-statestyle that calls latimes-statestyle.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - statestyle

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'Standardizes data about U.S. states'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
