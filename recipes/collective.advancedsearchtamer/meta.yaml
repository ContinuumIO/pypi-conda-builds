package:
  name: collective.advancedsearchtamer
  version: "1.0"

source:
  fn: collective.advancedsearchtamer-1.0.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.advancedsearchtamer/collective.advancedsearchtamer-1.0.tar.gz
  md5: a6d86e0aef686c0241441f0d1babfbc6
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.advancedsearchtamer = collective.advancedsearchtamer:main
    #
    # Would create an entry point called collective.advancedsearchtamer that calls collective.advancedsearchtamer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.advancedsearchtamer
    - collective.advancedsearchtamer.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: GPL
  summary: "Tries to tame Plone's advanced search form"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
