package:
  name: flufl.lock
  version: "2.3.1"

source:
  fn: flufl.lock-2.3.1.tar.gz
  url: https://pypi.python.org/packages/source/f/flufl.lock/flufl.lock-2.3.1.tar.gz
  md5: 3812ebf41c232c5acca01968c6db1f11
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flufl.lock = flufl.lock:main
    #
    # Would create an entry point called flufl.lock that calls flufl.lock.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - flufl
    - flufl.lock
    - flufl.lock.docs
    - flufl.lock.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://launchpad.net/flufl.lock
  license: GNU Lesser General Public License v3 or later (LGPLv3+)
  summary: 'NFS-safe file locking with timeouts for POSIX systems'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
