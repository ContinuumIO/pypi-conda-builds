package:
  name: django-resizer
  version: "1.0"

source:
  fn: django-resizer-1.0.zip
  url: https://pypi.python.org/packages/source/d/django-resizer/django-resizer-1.0.zip
  md5: 8a38066c5a0b05058f1da7459e7f94ed
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-resizer = django-resizer:main
    #
    # Would create an entry point called django-resizer that calls django-resizer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.1
    - pillow >=2.0
    - easy-thumbnails >=1.0

  run:
    - python
    - django >=1.1
    - pillow >=2.0
    - easy-thumbnails >=1.0

test:
  # Python imports
  imports:
    - resizer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/nextscreenlabs/django-resizer
  license: BSD License
  summary: 'A Django app for fetching resized and/or cropped images through a url.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
