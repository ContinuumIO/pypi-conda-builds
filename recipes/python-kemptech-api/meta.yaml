package:
  name: python-kemptech-api
  version: "0.1.2"

source:
  fn: python-kemptech-api-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/p/python-kemptech-api/python-kemptech-api-0.1.2.tar.gz
  md5: cfca8538f4dbf57d0928d050beec0f25
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-kemptech-api = python-kemptech-api:main
    #
    # Would create an entry point called python-kemptech-api that calls python-kemptech-api.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=2.3.0

  run:
    - python
    - requests >=2.3.0

test:
  # Python imports
  imports:
    - python_kemptech_api
    - python_kemptech_api.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/python-kemptech-api/
  license: Apache Software License
  summary: 'KEMP Technologies Python API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
