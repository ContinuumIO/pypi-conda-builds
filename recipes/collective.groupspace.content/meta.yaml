package:
  name: collective.groupspace.content
  version: "1.0.2"

source:
  fn: collective.groupspace.content-1.0.2.zip
  url: https://pypi.python.org/packages/source/c/collective.groupspace.content/collective.groupspace.content-1.0.2.zip
  md5: 5af13b5b026c6526c5782005916f8350
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.groupspace.content = collective.groupspace.content:main
    #
    # Would create an entry point called collective.groupspace.content that calls collective.groupspace.content.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.groupspace
    - collective.groupspace.content
    - collective.groupspace.content.browser
    - collective.groupspace.content.content
    - collective.groupspace.content.interfaces
    - collective.groupspace.content.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/collective.groupspace.content
  license: GNU General Public License (GPL)
  summary: 'Plone collective.groupspace content type for privately working in groups'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
