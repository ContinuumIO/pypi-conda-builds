package:
  name: collective.select2
  version: "1.1"

source:
  fn: collective.select2-1.1.zip
  url: https://pypi.python.org/packages/source/c/collective.select2/collective.select2-1.1.zip
  md5: aa3c28880118621f095478fa6b102977
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.select2 = collective.select2:main
    #
    # Would create an entry point called collective.select2 that calls collective.select2.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.cmfcore

  run:
    - python
    - setuptools
    - setuptools
    - products.cmfcore

test:
  # Python imports
  imports:
    - collective
    - collective.select2

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/
  license: gpl
  summary: 'User token widget'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
