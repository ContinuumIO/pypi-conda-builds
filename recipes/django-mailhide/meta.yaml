package:
  name: django-mailhide
  version: "1.0.1"

source:
  fn: django-mailhide-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-mailhide/django-mailhide-1.0.1.tar.gz
  md5: 87eaa0e6264e881b7d0a0d4c78bf588c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-mailhide = django-mailhide:main
    #
    # Would create an entry point called django-mailhide that calls django-mailhide.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - django_mailhide
    - django_mailhide.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jbzdak/django-mailhide
  license: BSD License
  summary: 'This is a django api for recaptcha mailhide.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
