package:
  name: apidev-coop_colorbox
  version: "1.0.1"

source:
  fn: apidev-coop_colorbox-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/a/apidev-coop_colorbox/apidev-coop_colorbox-1.0.1.tar.gz
  md5: d0388a1b96ffc86eb40ca4a4742e00e4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - apidev-coop_colorbox = apidev-coop_colorbox:main
    #
    # Would create an entry point called apidev-coop_colorbox that calls apidev-coop_colorbox.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - colorbox

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ljean/coop-colorbox/
  license: BSD License
  summary: 'Manage lightboxes within coop-cms'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
