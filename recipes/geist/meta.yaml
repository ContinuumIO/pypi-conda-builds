package:
  name: geist
  version: "1.0a16"

source:
  fn: geist-1.0a16.tar.gz
  url: https://pypi.python.org/packages/source/g/geist/geist-1.0a16.tar.gz
  md5: 0c720fbec361f0ff6c8688992a334f90
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - geist = geist:main
    #
    # Would create an entry point called geist that calls geist.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy >=1.9.0
    - scipy
    - ooxcb
    - pyhamcrest
    - pillow
    - wrapt
    - mock

  run:
    - python
    - numpy >=1.9.0
    - scipy
    - ooxcb
    - pyhamcrest
    - pillow
    - wrapt
    - mock

test:
  # Python imports
  imports:
    - geist
    - geist.backends

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/thetestpeople/Geist
  license: MIT
  summary: 'Visual Automation Library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
