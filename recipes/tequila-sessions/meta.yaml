package:
  name: tequila-sessions
  version: "1.0.1"

source:
  fn: tequila-sessions-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/t/tequila-sessions/tequila-sessions-1.0.1.tar.gz
  md5: 859bfd218100e8d916298082d0feec0e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tequila-sessions = tequila-sessions:main
    #
    # Would create an entry point called tequila-sessions that calls tequila-sessions.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - requests
    - beautifulsoup4

  run:
    - python
    - requests
    - beautifulsoup4

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/antoinealb/python-tequila
  license: UNKNOWN
  summary: 'Requests session for Tequila (EPFL login manager)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
