package:
  name: docker-registry-driver-joyent_manta
  version: "0.0.19"

source:
  fn: docker-registry-driver-joyent_manta-0.0.19.tar.gz
  url: https://pypi.python.org/packages/source/d/docker-registry-driver-joyent_manta/docker-registry-driver-joyent_manta-0.0.19.tar.gz
  md5: 8ebbbb0baa4de3db090eff2d3ff571ee
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - docker-registry-driver-joyent_manta = docker-registry-driver-joyent_manta:main
    #
    # Would create an entry point called docker-registry-driver-joyent_manta that calls docker-registry-driver-joyent_manta.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - docker-registry-core >=2,<3
    - manta >=2.1.1

  run:
    - python
    - docker-registry-core >=2,<3
    - manta >=2.1.1

test:
  # Python imports
  imports:
    - docker_registry
    - docker_registry.drivers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/13W/docker-registry-driver-manta.git
  license: LICENSE.txt
  summary: 'Docker registry manta driver'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
