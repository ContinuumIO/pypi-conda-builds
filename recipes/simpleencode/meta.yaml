package:
  name: simpleencode
  version: "1.0.0"

source:
  fn: simpleencode-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/s/simpleencode/simpleencode-1.0.0.tar.gz
  md5: f6a4638270fa9ef6769691c9b93a3dd5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - simpleencode = simpleencode:main
    #
    # Would create an entry point called simpleencode that calls simpleencode.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - simpleencode

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/python-simpleencode/
  license: BSD License
  summary: 'simple encode decode with a private key, based on the base64 encode and decode.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
