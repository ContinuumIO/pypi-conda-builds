package:
  name: collective.searchtool
  version: "0.1.5"

source:
  fn: collective.searchtool-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.searchtool/collective.searchtool-0.1.5.tar.gz
  md5: a76eff090a100dfbcfd4b23edbe54249
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.searchtool = collective.searchtool:main
    #
    # Would create an entry point called collective.searchtool that calls collective.searchtool.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.z3cform

  run:
    - python
    - setuptools
    - setuptools
    - plone.z3cform

test:
  # Python imports
  imports:
    - collective
    - collective.searchtool

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://svn.plone.org/svn/collective/collective.searchtool
  license: GPL
  summary: 'Search tool for plone using z3c.form.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
