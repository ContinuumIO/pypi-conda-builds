package:
  name: yorbay
  version: "0.1.dev1"

source:
  fn: yorbay-0.1.dev1.tar.gz
  url: https://pypi.python.org/packages/source/y/yorbay/yorbay-0.1.dev1.tar.gz
  md5: b6577027141104312e077d448c9e0acb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - yorbay = yorbay:main
    #
    # Would create an entry point called yorbay that calls yorbay.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - yorbay
    - yorbay.debug

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rusek/yorbay-python
  license: Apache Software License
  summary: 'Localization framework based on l20n file format'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
