package:
  name: circonus
  version: "0.0.22"

source:
  fn: circonus-0.0.22.tar.gz
  url: https://pypi.python.org/packages/source/c/circonus/circonus-0.0.22.tar.gz
  md5: 6ab7b2d54366d8ff3f253dc43d43207e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - circonus = circonus:main
    #
    # Would create an entry point called circonus that calls circonus.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - colour
    - requests

  run:
    - python
    - colour
    - requests

test:
  # Python imports
  imports:
    - circonus
    - circonus.collectd

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/monetate/circonus
  license: MIT License
  summary: 'Interact with the Circonus REST API.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
