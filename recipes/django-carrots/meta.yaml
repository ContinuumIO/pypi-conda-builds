package:
  name: django-carrots
  version: "1.12"

source:
  fn: django-carrots-1.12.tar.gz
  url: https://pypi.python.org/packages/source/d/django-carrots/django-carrots-1.12.tar.gz
  md5: 97b6044f82c635c00415713d8058e488
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-carrots = django-carrots:main
    #
    # Would create an entry point called django-carrots that calls django-carrots.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-social-auth3

  run:
    - python
    - django-social-auth3

test:
  # Python imports
  imports:
    - account

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://geekgirlscarrots.pl/
  license: PSF
  summary: 'Basic account application for Django Workshops by Geek Girl Carrots'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
