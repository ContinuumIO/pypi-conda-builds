package:
  name: mekk.postrank
  version: "0.2.0"

source:
  fn: mekk.postrank-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/m/mekk.postrank/mekk.postrank-0.2.0.tar.gz
  md5: 5031ab952063868110bbbd8be42195d0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mekk.postrank = mekk.postrank:main
    #
    # Would create an entry point called mekk.postrank that calls mekk.postrank.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - simplejson

  run:
    - python
    - simplejson

test:
  # Python imports
  imports:
    - mekk
    - mekk.postrank

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/Mekk/mekk.postrank
  license: BSD
  summary: 'Simple wrapper for PostRank API.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
