package:
  name: django_nonrel_runner
  version: "0.2"

source:
  fn: django_nonrel_runner-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django_nonrel_runner/django_nonrel_runner-0.2.tar.gz
  md5: 52a0f1d6909c40ab39f3a6e1491ccd21
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_nonrel_runner = django_nonrel_runner:main
    #
    # Would create an entry point called django_nonrel_runner that calls django_nonrel_runner.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_nonrel_runner

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/shuge/django_nonrel_runner
  license: MIT License
  summary: 'test runner for django-nonrel'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
