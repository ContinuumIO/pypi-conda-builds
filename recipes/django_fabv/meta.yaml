package:
  name: django_fabv
  version: "0.1.1"

source:
  fn: django_fabv-0.1.1.zip
  url: https://pypi.python.org/packages/source/d/django_fabv/django_fabv-0.1.1.zip
  md5: 06af7ad9c1483e757ab61017500a7392
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_fabv = django_fabv:main
    #
    # Would create an entry point called django_fabv that calls django_fabv.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_fabv
    - django_fabv.management
    - django_fabv.management.commands
    - django_fabv.migrations
    - django_fabv.templatetags
    - django_fabv.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://pypi.python.org/pypi/django_fabv/0.1.0
  license: 3-clause BSD
  summary: 'A/B module for Django based on ABingo'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
