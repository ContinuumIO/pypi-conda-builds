package:
  name: cmsplugin-team
  version: "0.2.0"

source:
  fn: cmsplugin-team-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/c/cmsplugin-team/cmsplugin-team-0.2.0.tar.gz
  md5: c7f5ba051407f7fb472529aa0d5fef09
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cmsplugin-team = cmsplugin-team:main
    #
    # Would create an entry point called cmsplugin-team that calls cmsplugin-team.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-sekizai
    - django-filer
    - django-editor

  run:
    - python
    - django-sekizai
    - django-filer
    - django-editor

test:
  # Python imports
  imports:
    - cmsplugin_team
    - cmsplugin_team.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/littlepea/cmsplugin-team
  license: BSD
  summary: 'Django CMS plugin to list team members/employees.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
