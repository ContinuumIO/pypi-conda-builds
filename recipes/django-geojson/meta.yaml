package:
  name: django-geojson
  version: "2.8.0"

source:
  fn: django-geojson-2.8.0.zip
  url: https://pypi.python.org/packages/source/d/django-geojson/django-geojson-2.8.0.zip
  md5: 7419568ff9c8b52f2c52f69d5816939b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-geojson = django-geojson:main
    #
    # Would create an entry point called django-geojson that calls django-geojson.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - six

  run:
    - python
    - django
    - six

test:
  # Python imports
  imports:
    - djgeojson
    - djgeojson.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/makinacorpus/django-geojson
  license: LPGL, see LICENSE file.
  summary: 'Serve vectorial map layers with Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
