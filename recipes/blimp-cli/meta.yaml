package:
  name: blimp-cli
  version: "0.5.4"

source:
  fn: blimp-cli-0.5.4.tar.gz
  url: https://pypi.python.org/packages/source/b/blimp-cli/blimp-cli-0.5.4.tar.gz
  md5: 90c997c5db3fa23cbaf10f8146a26b0a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - blimp-cli = blimp-cli:main
    #
    # Would create an entry point called blimp-cli that calls blimp-cli.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - blimp

  run:
    - python
    - blimp

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/elbuo8/blimp-cli/
  license: MIT License
  summary: 'CLI interface for Blimp Tasks API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
