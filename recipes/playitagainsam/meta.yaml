package:
  name: playitagainsam
  version: "0.4.0"

source:
  fn: playitagainsam-0.4.0.tar.gz
  url: https://pypi.python.org/packages/source/p/playitagainsam/playitagainsam-0.4.0.tar.gz
  md5: 5c42f312dbed84512bc3a2fa647661aa
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - playitagainsam = playitagainsam:main
    #
    # Would create an entry point called playitagainsam that calls playitagainsam.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - psutil >=2.0
    - six

  run:
    - python
    - psutil >=2.0
    - six

test:
  # Python imports
  imports:
    - playitagainsam

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rfk/playitagainsam
  license: MIT License
  summary: 'record and replay interactive terminal sessions'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
