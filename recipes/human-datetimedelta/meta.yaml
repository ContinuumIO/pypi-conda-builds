package:
  name: human-datetimedelta
  version: "0.3.1"

source:
  fn: human-datetimedelta-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/h/human-datetimedelta/human-datetimedelta-0.3.1.tar.gz
  md5: e808b233c17f724ff8f60dde28fa77fc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - human-datetimedelta = human-datetimedelta:main
    #
    # Would create an entry point called human-datetimedelta that calls human-datetimedelta.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - human

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/colwilson/human-datetimedelta/
  license: MIT License
  summary: 'Human readable datetime deltas'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
