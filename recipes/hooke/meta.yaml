package:
  name: hooke
  version: "1.0.0.alpha (Ninken)"

source:
  fn: Hooke-1.0.0.alpha (Ninken).tar.gz
  url: https://pypi.python.org/packages/source/H/Hooke/Hooke-1.0.0.alpha (Ninken).tar.gz
  md5: 9ad31cb81f1a9ee8df72c7ebe27240c7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hooke = hooke:main
    #
    # Would create an entry point called hooke that calls hooke.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - hooke
    - hooke.compat
    - hooke.driver
    - hooke.plugin
    - hooke.ui
    - hooke.ui.gui
    - hooke.ui.gui.dialog
    - hooke.ui.gui.handler
    - hooke.ui.gui.panel
    - hooke.util

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/hooke/
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Tools for analyzing force spectroscopy data.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
