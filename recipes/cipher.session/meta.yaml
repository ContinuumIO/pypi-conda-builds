package:
  name: cipher.session
  version: "2.0.0a1"

source:
  fn: cipher.session-2.0.0a1.zip
  url: https://pypi.python.org/packages/source/c/cipher.session/cipher.session-2.0.0a1.zip
  md5: fd2cff43ae5b7efd34e98bf4355de1c0
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cipher.session = cipher.session:main
    #
    # Would create an entry point called cipher.session that calls cipher.session.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.interface
    - zope.component
    - zope.session
    - zope.location
    - zope.publisher
    - repoze.session

  run:
    - python
    - setuptools
    - zope.interface
    - zope.component
    - zope.session
    - zope.location
    - zope.publisher
    - repoze.session

test:
  # Python imports
  imports:
    - cipher
    - cipher.session
    - cipher.session.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/cipher.session/
  license: ZPL 2.1
  summary: 'A ZODB Session handling implementation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
