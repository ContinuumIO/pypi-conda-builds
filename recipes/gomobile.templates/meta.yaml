package:
  name: gomobile.templates
  version: "1.0.2"

source:
  fn: gomobile.templates-1.0.2.zip
  url: https://pypi.python.org/packages/source/g/gomobile.templates/gomobile.templates-1.0.2.zip
  md5: 1d2b826f6340a888f55ce38b215bd845
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gomobile.templates = gomobile.templates:main
    #
    # Would create an entry point called gomobile.templates that calls gomobile.templates.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - pastescript
    - zopeskel

  run:
    - python
    - setuptools
    - setuptools
    - pastescript
    - zopeskel

test:
  # Python imports
  imports:
    - gomobile
    - gomobile.templates

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://webandmobile.mfabrik.com
  license: GPL
  summary: 'Project templates creating Web and Mobile themes for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
