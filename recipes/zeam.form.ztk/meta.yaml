package:
  name: zeam.form.ztk
  version: "1.3.4"

source:
  fn: zeam.form.ztk-1.3.4.tar.gz
  url: https://pypi.python.org/packages/source/z/zeam.form.ztk/zeam.form.ztk-1.3.4.tar.gz
  md5: 6bcdc53527876bb4474bcafc383e318a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zeam.form.ztk = zeam.form.ztk:main
    #
    # Would create an entry point called zeam.form.ztk that calls zeam.form.ztk.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - grokcore.component
    - grokcore.view
    - grokcore.security
    - grokcore.chameleon >=1.0.3
    - setuptools
    - zeam.form.base >=1.2.3
    - zope.component
    - zope.container
    - zope.event
    - zope.i18n
    - zope.i18nmessageid
    - zope.interface
    - zope.lifecycleevent
    - zope.publisher
    - zope.schema >=3.8
    - zope.traversing

  run:
    - python
    - setuptools
    - grokcore.component
    - grokcore.view
    - grokcore.security
    - grokcore.chameleon >=1.0.3
    - setuptools
    - zeam.form.base >=1.2.3
    - zope.component
    - zope.container
    - zope.event
    - zope.i18n
    - zope.i18nmessageid
    - zope.interface
    - zope.lifecycleevent
    - zope.publisher
    - zope.schema >=3.8
    - zope.traversing

test:
  # Python imports
  imports:
    - zeam
    - zeam.form
    - zeam.form.ztk
    - zeam.form.ztk.ftests
    - zeam.form.ztk.ftests.forms
    - zeam.form.ztk.tests
    - zeam.form.ztk.widgets
    - zeam.form.ztk.widgets.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/zeam.form.ztk
  license: BSD
  summary: 'Zope Toolkit support for zeam.form'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
