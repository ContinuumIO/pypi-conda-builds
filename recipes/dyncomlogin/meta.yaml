package:
  name: dyncomlogin
  version: "0.1"

source:
  fn: dynComLogin-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/dynComLogin/dynComLogin-0.1.tar.gz
  md5: 4e92254e3e326ea051e5dcc57046ee17
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dyncomlogin = dyncomlogin:main
    #
    # Would create an entry point called dyncomlogin that calls dyncomlogin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.testbrowser

  run:
    - python
    - setuptools
    - zope.testbrowser

test:
  # Python imports
  imports:
    - dynComLogin
    - dyncomlogin

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: UNKNOWN
  summary: 'log in to dyn.com'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
