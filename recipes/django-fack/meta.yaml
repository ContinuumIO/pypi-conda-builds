package:
  name: django-fack
  version: "1.1"

source:
  fn: django-fack-1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-fack/django-fack-1.1.tar.gz
  md5: 1ee2243813452a3a5f5dfbdbc0720a50
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-fack = django-fack:main
    #
    # Would create an entry point called django-fack that calls django-fack.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3

  run:
    - python
    - django >=1.3

test:
  # Python imports
  imports:
    - fack
    - fack.templatetags
    - fack.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://django-fack.rtfd.org/
  license: BSD License
  summary: 'A simple FAQ application for Django sites.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
