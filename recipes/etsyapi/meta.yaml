package:
  name: etsyapi
  version: "0.2.1"

source:
  fn: etsyapi-0.2.1.zip
  url: https://pypi.python.org/packages/source/e/etsyapi/etsyapi-0.2.1.zip
  md5: 638ff02221d48245aba6478f57df9547
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - etsyapi = etsyapi:main
    #
    # Would create an entry point called etsyapi that calls etsyapi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=0.13.2
    - requests-oauthlib >=0.4.0

  run:
    - python
    - requests >=0.13.2
    - requests-oauthlib >=0.4.0

test:
  # Python imports
  imports:
    - etsyapi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/DempDemp/etsyapi
  license: BSD
  summary: 'etsy.com api wrapper'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
