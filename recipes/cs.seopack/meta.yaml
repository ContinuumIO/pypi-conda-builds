package:
  name: cs.seopack
  version: "1.1"

source:
  fn: cs.seopack-1.1.zip
  url: https://pypi.python.org/packages/source/c/cs.seopack/cs.seopack-1.1.zip
  md5: 767d1b30c013f48607783ff13d9fe463
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cs.seopack = cs.seopack:main
    #
    # Would create an entry point called cs.seopack that calls cs.seopack.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - z3c.jbot

  run:
    - python
    - setuptools
    - setuptools
    - z3c.jbot

test:
  # Python imports
  imports:
    - cs
    - cs.seopack

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/codesyntax/cs.seopack
  license: GPL
  summary: 'Some viewlets to do SEO work in Plone sites'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
