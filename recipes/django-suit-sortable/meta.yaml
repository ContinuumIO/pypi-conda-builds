package:
  name: django-suit-sortable
  version: "0.1.0"

source:
  fn: django-suit-sortable-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-suit-sortable/django-suit-sortable-0.1.0.tar.gz
  md5: b37d00ac6bdab4405ff7051a9a207cf1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-suit-sortable = django-suit-sortable:main
    #
    # Would create an entry point called django-suit-sortable that calls django-suit-sortable.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - suit_sortable

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/JP74/django-suit-sortable
  license: BSD License
  summary: 'Drag-and-drop ordering for objects and inlines in Django admin using django-suit.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
