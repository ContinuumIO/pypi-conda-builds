package:
  name: django-cruds
  version: "0.1.8"

source:
  fn: django-cruds-0.1.8.zip
  url: https://pypi.python.org/packages/source/d/django-cruds/django-cruds-0.1.8.zip
  md5: 042234fa9f97d8432b727fab7375c041
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-cruds = django-cruds:main
    #
    # Would create an entry point called django-cruds that calls django-cruds.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - cruds

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bmihelac/django-cruds
  license: BSD License
  summary: 'django-cruds is simple drop-in django app that creates CRUD for faster prototyping.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
