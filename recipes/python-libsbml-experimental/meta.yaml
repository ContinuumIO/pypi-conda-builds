package:
  name: python-libsbml-experimental
  version: "5.11.4"

source:
  fn: python-libsbml-experimental-5.11.4.tar.gz
  url: https://pypi.python.org/packages/source/p/python-libsbml-experimental/python-libsbml-experimental-5.11.4.tar.gz
  md5: 9e4108e3e2033f4a824f1e4e62256779
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-libsbml-experimental = python-libsbml-experimental:main
    #
    # Would create an entry point called python-libsbml-experimental that calls python-libsbml-experimental.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - libsbml

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://sbml.org
  license: LGPL
  summary: 'LibSBML Python API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
