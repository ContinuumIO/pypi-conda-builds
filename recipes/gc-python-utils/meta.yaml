package:
  name: gc-python-utils
  version: "0.0.1"

source:
  fn: gc-python-utils-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/g/gc-python-utils/gc-python-utils-0.0.1.tar.gz
  md5: d23b1b7f43134b3b7639343cfebd8af6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gc-python-utils = gc-python-utils:main
    #
    # Would create an entry point called gc-python-utils that calls gc-python-utils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - gc_python_utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/gamechanger/python-utils
  license: UNKNOWN
  summary: 'GameChanger general python utilities'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
