package:
  name: products.timerangewidget
  version: "2.0b3"

source:
  fn: Products.TimeRangeWidget-2.0b3.tar.gz
  url: https://pypi.python.org/packages/source/P/Products.TimeRangeWidget/Products.TimeRangeWidget-2.0b3.tar.gz
  md5: c630712c2020b8bc6c6bfc8de4231626
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - products.timerangewidget = products.timerangewidget:main
    #
    # Would create an entry point called products.timerangewidget that calls products.timerangewidget.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - Products
    - Products.TimeRangeWidget

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://raptus.com
  license: GPL
  summary: 'Archetypes widget to store a time range'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
