package:
  name: difio-cloudcontrol-python
  version: "1.2.3"

source:
  fn: difio-cloudcontrol-python-1.2.3.tar.gz
  url: https://pypi.python.org/packages/source/d/difio-cloudcontrol-python/difio-cloudcontrol-python-1.2.3.tar.gz
  md5: 4c35d4acda90da8fa7730465151bad1e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - difio-cloudcontrol-python = difio-cloudcontrol-python:main
    #
    # Would create an entry point called difio-cloudcontrol-python that calls difio-cloudcontrol-python.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - difio_cctrl_python

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/difio/difio-cloudcontrol-python
  license: MIT License
  summary: 'Difio registration agent for cloudControl / Python applications'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
