package:
  name: horae.properties
  version: "1.0a1"

source:
  fn: horae.properties-1.0a1.tar.gz
  url: https://pypi.python.org/packages/source/h/horae.properties/horae.properties-1.0a1.tar.gz
  md5: bc144293acfe725afdbb5c1347dbd5c7
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - horae.properties = horae.properties:main
    #
    # Would create an entry point called horae.properties that calls horae.properties.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - grok
    - z3c.relationfield
    - horae.auth
    - horae.autocomplete
    - horae.cache
    - horae.core
    - horae.layout
    - horae.lifecycle
    - horae.timeaware

  run:
    - python
    - setuptools
    - setuptools
    - grok
    - z3c.relationfield
    - horae.auth
    - horae.autocomplete
    - horae.cache
    - horae.core
    - horae.layout
    - horae.lifecycle
    - horae.timeaware

test:
  # Python imports
  imports:
    - horae
    - horae.properties

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: GPL
  summary: 'Provides the dynamic property infrastructure used by the Horae resource planning system'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
