package:
  name: django-homebanking
  version: "0.2"

source:
  fn: django-homebanking-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-homebanking/django-homebanking-0.2.tar.gz
  md5: b061bf3d8b93eb4883edc0437b9e9137
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-homebanking = django-homebanking:main
    #
    # Would create an entry point called django-homebanking that calls django-homebanking.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - django-ordered-model
    - south
    - sparnord

  run:
    - python
    - django
    - django-ordered-model
    - south
    - sparnord

test:
  # Python imports
  imports:
    - homebanking
    - homebanking.drivers
    - homebanking.management
    - homebanking.management.commands
    - homebanking.migrations
    - homebanking.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/sorenh/python-django-homebanking
  license: Apache 2.0
  summary: 'Django appp to analyse and present data from homebanking system'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
