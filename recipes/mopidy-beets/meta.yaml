package:
  name: mopidy-beets
  version: "2.0.0"

source:
  fn: Mopidy-Beets-2.0.0.tar.gz
  url: https://pypi.python.org/packages/source/M/Mopidy-Beets/Mopidy-Beets-2.0.0.tar.gz
  md5: 894b79054abad1406b3e6ce626a8e69d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mopidy-beets = mopidy-beets:main
    #
    # Would create an entry point called mopidy-beets that calls mopidy-beets.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - mopidy >=1.0
    - pykka >=1.1
    - requests >=2.0.0

  run:
    - python
    - setuptools
    - setuptools
    - mopidy >=1.0
    - pykka >=1.1
    - requests >=2.0.0

test:
  # Python imports
  imports:
    - mopidy_beets

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mopidy/mopidy-beets
  license: MIT License
  summary: u'Beets extension for Mopidy'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
