package:
  name: django-wsgiserver
  version: "0.8.0rc1"

source:
  fn: django-wsgiserver-0.8.0rc1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-wsgiserver/django-wsgiserver-0.8.0rc1.tar.gz
  md5: 9eabe9a8adb53df8837957044c671294
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-wsgiserver = django-wsgiserver:main
    #
    # Would create an entry point called django-wsgiserver that calls django-wsgiserver.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - django_wsgiserver
    - django_wsgiserver.management
    - django_wsgiserver.management.commands
    - django_wsgiserver.wsgiserver

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/cleemesser/django-wsgiserver/downloads
  license: BSD License
  summary: "django-wsgiserver installs a web server for Django using CherryPy's WSGI server."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
