package:
  name: lisa-plugin-cinema
  version: "1.0.3"

source:
  fn: lisa-plugin-Cinema-1.0.3.tar.gz
  url: https://pypi.python.org/packages/source/l/lisa-plugin-Cinema/lisa-plugin-Cinema-1.0.3.tar.gz
  md5: 9748d5e12c9238345526938b3ee78b14
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lisa-plugin-cinema = lisa-plugin-cinema:main
    #
    # Would create an entry point called lisa-plugin-cinema that calls lisa-plugin-cinema.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6.2,<1.7
    - django-tastypie >=0.11.0
    - beautifulsoup4

  run:
    - python
    - django >=1.6.2,<1.7
    - django-tastypie >=0.11.0
    - beautifulsoup4

test:
  # Python imports
  imports:
    - lisa.plugins

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.lisa-project.net
  license: MIT License
  summary: 'LISA home automation system - Server'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
