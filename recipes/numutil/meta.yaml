package:
  name: numutil
  version: "0.1.0"

source:
  fn: numutil-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/n/numutil/numutil-0.1.0.tar.gz
  md5: a3a775931edb013d8bc03bd9ffe1f876
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - numutil = numutil:main
    #
    # Would create an entry point called numutil that calls numutil.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - .
    - numutil

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: www.naftaliharris.com
  license: BSD License
  summary: 'Utilities for parsing strings into numbers, and printing numbers as pretty strings'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
