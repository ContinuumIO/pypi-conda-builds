package:
  name: ldclient-py
  version: "0.16.2"

source:
  fn: ldclient-py-0.16.2.tar.gz
  url: https://pypi.python.org/packages/source/l/ldclient-py/ldclient-py-0.16.2.tar.gz
  md5: 8b60b90da1b995f9e90639f17aced6d2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ldclient-py = ldclient-py:main
    #
    # Would create an entry point called ldclient-py that calls ldclient-py.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cachecontrol ==0.10.2
    - requests ==2.4.0
    - future ==0.14.3

  run:
    - python
    - cachecontrol ==0.10.2
    - requests ==2.4.0
    - future ==0.14.3

test:
  # Python imports
  imports:
    - ldclient

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/launchdarkly/python-client
  license: Apache Software License
  summary: 'LaunchDarkly SDK for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
