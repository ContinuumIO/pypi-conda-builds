package:
  name: horizonjpl
  version: "0.1.7"

source:
  fn: HorizonJPL-0.1.7.tar.gz
  url: https://pypi.python.org/packages/source/H/HorizonJPL/HorizonJPL-0.1.7.tar.gz
  md5: 44737f3c72262c66772dee83c0f010f5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - horizonjpl = horizonjpl:main
    #
    # Would create an entry point called horizonjpl that calls horizonjpl.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - pytest-timeout >=0.3

  run:
    - python
    - pytest-timeout >=0.3

test:
  # Python imports
  imports:
    - horizon
    - horizon.test
    - horizonjpl

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/HorizonJPL/
  license: LICENSE.txt
  summary: "Access to NASA's JPL HORIZON Ephemeris System"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
