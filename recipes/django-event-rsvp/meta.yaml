package:
  name: django-event-rsvp
  version: "0.4.1"

source:
  fn: django-event-rsvp-0.4.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-event-rsvp/django-event-rsvp-0.4.1.tar.gz
  md5: f2d57ac119b5b5c11c78463e97e8bf6f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-event-rsvp = django-event-rsvp:main
    #
    # Would create an entry point called django-event-rsvp that calls django-event-rsvp.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4.3
    - django-cms >=2.3
    - django-filer
    - python-dateutil
    - south

  run:
    - python
    - django >=1.4.3
    - django-cms >=2.3
    - django-filer
    - python-dateutil
    - south

test:
  # Python imports
  imports:
    - event_rsvp
    - event_rsvp.forms
    - event_rsvp.migrations
    - event_rsvp.tests
    - event_rsvp.tests.test_app

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bitmazk/django-event-rsvp
  license: The MIT License
  summary: 'A reusable Django app to create event pages where users can RSVP.\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
