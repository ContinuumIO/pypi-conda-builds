package:
  name: dolmen.widget.file
  version: "1.0b2"

source:
  fn: dolmen.widget.file-1.0b2.tar.gz
  url: https://pypi.python.org/packages/source/d/dolmen.widget.file/dolmen.widget.file-1.0b2.tar.gz
  md5: 34c59c776f2078fca825f086a20f0423
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dolmen.widget.file = dolmen.widget.file:main
    #
    # Would create an entry point called dolmen.widget.file that calls dolmen.widget.file.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - dolmen.file >=0.5.1
    - grokcore.component
    - grokcore.view
    - setuptools
    - zope.interface
    - zope.size
    - zope.i18n
    - zope.location
    - zope.traversing
    - zeam.form.base >=1.0
    - zeam.form.ztk >=1.0
    - zope.i18nmessageid

  run:
    - python
    - setuptools
    - dolmen.file >=0.5.1
    - grokcore.component
    - grokcore.view
    - setuptools
    - zope.interface
    - zope.size
    - zope.i18n
    - zope.location
    - zope.traversing
    - zeam.form.base >=1.0
    - zeam.form.ztk >=1.0
    - zope.i18nmessageid

test:
  # Python imports
  imports:
    - dolmen
    - dolmen.widget
    - dolmen.widget.file

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: GNU General Public License (GPL)
  summary: 'File widget for `zeam.form` and `Dolmen`'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
