package:
  name: flask-docker
  version: "0.1.0"

source:
  fn: Flask-Docker-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Docker/Flask-Docker-0.1.0.tar.gz
  md5: 50b1bb14b0a961486bf72e18104b1342
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-docker = flask-docker:main
    #
    # Would create an entry point called flask-docker that calls flask-docker.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - docker-py

  run:
    - python
    - flask
    - docker-py

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tonyseek/flask-docker
  license: MIT License
  summary: 'Uses Docker client in your Flask application.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
