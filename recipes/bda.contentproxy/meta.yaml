package:
  name: bda.contentproxy
  version: "1.0"

source:
  fn: bda.contentproxy-1.0.tar.gz
  url: https://pypi.python.org/packages/source/b/bda.contentproxy/bda.contentproxy-1.0.tar.gz
  md5: 2f5cf8b35c82fbaa9e25bba6ba0d2685
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bda.contentproxy = bda.contentproxy:main
    #
    # Would create an entry point called bda.contentproxy that calls bda.contentproxy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - bda
    - bda.contentproxy
    - bda.contentproxy.browser
    - bda.contentproxy.content

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: GNU General Public License (GPL)
  summary: 'module providing content proxying for plone.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
