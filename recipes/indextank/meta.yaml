package:
  name: indextank
  version: "1.0.6"

source:
  fn: indextank-1.0.6.tar.gz
  url: https://pypi.python.org/packages/source/i/indextank/indextank-1.0.6.tar.gz
  md5: 341c0168208fe809982f14b8d0e9e5ff
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - indextank = indextank:main
    #
    # Would create an entry point called indextank that calls indextank.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - anyjson

  run:
    - python
    - anyjson

test:
  # Python imports
  imports:
    - indextank

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'IndexTank API Client for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
