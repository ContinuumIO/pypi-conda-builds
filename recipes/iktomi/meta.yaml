package:
  name: iktomi
  version: "0.4.1"

source:
  fn: iktomi-0.4.1.tar.gz
  url: https://pypi.python.org/packages/source/i/iktomi/iktomi-0.4.1.tar.gz
  md5: 3fbc7e26f082e41092f1ec7b063e2ee9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - iktomi = iktomi:main
    #
    # Would create an entry point called iktomi that calls iktomi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - iktomi
    - iktomi.cli
    - iktomi.db
    - iktomi.db.sqla
    - iktomi.forms
    - iktomi.templates
    - iktomi.templates.jinja2
    - iktomi.templates.mint
    - iktomi.unstable
    - iktomi.unstable.db
    - iktomi.unstable.db.sqla
    - iktomi.unstable.forms
    - iktomi.unstable.utils
    - iktomi.unstable.web
    - iktomi.utils
    - iktomi.web

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/SmartTeleMax/iktomi/
  license: MIT
  summary: 'A web tool: routing, forms, other useful things.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
