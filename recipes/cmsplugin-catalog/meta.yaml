package:
  name: cmsplugin-catalog
  version: "0.1"

source:
  fn: cmsplugin-catalog-0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/cmsplugin-catalog/cmsplugin-catalog-0.1.tar.gz
  md5: d3fe20f98e346c947d795ae727812952
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cmsplugin-catalog = cmsplugin-catalog:main
    #
    # Would create an entry point called cmsplugin-catalog that calls cmsplugin-catalog.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-cms

  run:
    - python
    - django-cms

test:
  # Python imports
  imports:
    - cmsplugin_catalog
    - cmsplugin_catalog.migrations
    - cmsplugin_catalog.south_migrations
    - cmsplugin_catalog.templatetags
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/tkliuxing/cmsplugin-catalog
  license: MIT License
  summary: 'Django CMS page catalog plugin'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
