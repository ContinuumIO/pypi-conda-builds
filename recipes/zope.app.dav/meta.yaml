package:
  name: zope.app.dav
  version: "3.5.3"

source:
  fn: zope.app.dav-3.5.3.tar.gz
  url: https://pypi.python.org/packages/source/z/zope.app.dav/zope.app.dav-3.5.3.tar.gz
  md5: 19ec8dc5f7ad21468dea1c46e86d95dd
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zope.app.dav = zope.app.dav:main
    #
    # Would create an entry point called zope.app.dav that calls zope.app.dav.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zodb3
    - zope.annotation
    - zope.container
    - zope.app.file
    - zope.app.form
    - zope.component
    - zope.configuration
    - zope.dublincore
    - zope.event
    - zope.filerepresentation
    - zope.interface
    - zope.lifecycleevent
    - zope.location
    - zope.pagetemplate
    - zope.publisher >=3.12
    - zope.schema
    - zope.site
    - zope.size
    - zope.traversing

  run:
    - python
    - setuptools
    - zodb3
    - zope.annotation
    - zope.container
    - zope.app.file
    - zope.app.form
    - zope.component
    - zope.configuration
    - zope.dublincore
    - zope.event
    - zope.filerepresentation
    - zope.interface
    - zope.lifecycleevent
    - zope.location
    - zope.pagetemplate
    - zope.publisher >=3.12
    - zope.schema
    - zope.site
    - zope.size
    - zope.traversing

test:
  # Python imports
  imports:
    - zope
    - zope.app
    - zope.app.dav
    - zope.app.dav.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/zope.app.dav
  license: Zope Public License
  summary: 'Zope WebDAV Support (Basic)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
