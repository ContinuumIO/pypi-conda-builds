package:
  name: collective.js.jquerygantt
  version: "1.0.3"

source:
  fn: collective.js.jquerygantt-1.0.3.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.js.jquerygantt/collective.js.jquerygantt-1.0.3.tar.gz
  md5: ef488360ef3d096b9ac5477fa34ffb18
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.js.jquerygantt = collective.js.jquerygantt:main
    #
    # Would create an entry point called collective.js.jquerygantt that calls collective.js.jquerygantt.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone

  run:
    - python
    - setuptools
    - setuptools
    - plone

test:
  # Python imports
  imports:
    - collective
    - collective.js
    - collective.js.jquerygantt
    - collective.monkeypatcher
    - collective.monkeypatcher.tests
    - collective.z3cform
    - collective.z3cform.datetimewidget

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://pypi.python.org/pypi/collective.js.jquerygantt
  license: GNU General Public License (GPL)
  summary: 'jQuery.Gantt JS Library Packaged for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
