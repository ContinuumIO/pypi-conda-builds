package:
  name: pulley
  version: "0.0.2"

source:
  fn: pulley-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/p/pulley/pulley-0.0.2.tar.gz
  md5: db8d5cebdd62c3475a7352fe44aba614
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pulley = pulley:main
    #
    # Would create an entry point called pulley that calls pulley.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy
    - pandas
    - swigibpy
    - psycopg2
    - tzlocal
    - django
    - celery
    - django-celery

  run:
    - python
    - numpy
    - pandas
    - swigibpy
    - psycopg2
    - tzlocal
    - django
    - celery
    - django-celery

test:
  # Python imports
  imports:
    - pulley
    - pulley.algos
    - pulley.brokers
    - pulley.brokers.ib
    - pulley.calendar
    - pulley.reporting
    - pulley.zp
    - pulley.zp.finance
    - pulley.zp.sources

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jgoode21/pulley.git
  license: UNKNOWN
  summary: 'Live trading with zipline and swigibpy via Interactive Brokers.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
