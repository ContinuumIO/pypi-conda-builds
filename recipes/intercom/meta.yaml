package:
  name: intercom
  version: "0.2.3"

source:
  fn: Intercom-0.2.3.tar.gz
  url: https://pypi.python.org/packages/source/I/Intercom/Intercom-0.2.3.tar.gz
  md5: b2f85e3af3a15fb8ee2818ab5bc65313
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - intercom = intercom:main
    #
    # Would create an entry point called intercom that calls intercom.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyzmq

  run:
    - python
    - pyzmq

test:
  # Python imports
  imports:
    - intercom
    - intercom.controllers
    - intercom.minions
    - intercom.monitors

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/oksome/Intercom/
  license: GNU Affero General Public License v3
  summary: 'Messaging system for Home automation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
