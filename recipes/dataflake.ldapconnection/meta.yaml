package:
  name: dataflake.ldapconnection
  version: "1.5"

source:
  fn: dataflake.ldapconnection-1.5.tar.gz
  url: https://pypi.python.org/packages/source/d/dataflake.ldapconnection/dataflake.ldapconnection-1.5.tar.gz
  md5: bce338997a8945f632a7085ac7149dff
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dataflake.ldapconnection = dataflake.ldapconnection:main
    #
    # Would create an entry point called dataflake.ldapconnection that calls dataflake.ldapconnection.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python-ldap >=2.3.0
    - zope.interface
    - dataflake.cache
    - dataflake.fakeldap

  run:
    - python
    - setuptools
    - python-ldap >=2.3.0
    - zope.interface
    - dataflake.cache
    - dataflake.fakeldap

test:
  # Python imports
  imports:
    - dataflake
    - dataflake.ldapconnection
    - dataflake.ldapconnection.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/dataflake.ldapconnection
  license: Zope Public License
  summary: 'LDAP connection library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
