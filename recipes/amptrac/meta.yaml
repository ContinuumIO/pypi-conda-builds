package:
  name: amptrac
  version: "0.1"

source:
  fn: amptrac-0.1.tar.gz
  url: https://pypi.python.org/packages/source/a/amptrac/amptrac-0.1.tar.gz
  md5: ff47ef4e175769eeba407a811e5ca373
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - amptrac = amptrac:main
    #
    # Would create an entry point called amptrac that calls amptrac.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - twisted >=13.0.0
    - treq

  run:
    - python
    - twisted >=13.0.0
    - treq

test:
  # Python imports
  imports:
    - amptrac
    - amptrac.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tomprince/amptrac
  license: MIT
  summary: "Client for twisted's amp interface to trac"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
