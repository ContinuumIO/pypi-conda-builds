package:
  name: django-kmatch
  version: "0.3.1"

source:
  fn: django-kmatch-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-kmatch/django-kmatch-0.3.1.tar.gz
  md5: 58368524e154fcaa09c1ec8518e22a37
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-kmatch = django-kmatch:main
    #
    # Would create an entry point called django-kmatch that calls django-kmatch.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - jsonfield >=0.9.20
    - django >=1.7
    - kmatch >=0.1.1

  run:
    - python
    - jsonfield >=0.9.20
    - django >=1.7
    - kmatch >=0.1.1

test:
  # Python imports
  imports:
    - django_kmatch
    - django_kmatch.tests
    - django_kmatch.tests.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ambitioninc/django-kmatch
  license: MIT License
  summary: 'Django utilities for the kmatch library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
