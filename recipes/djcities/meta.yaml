package:
  name: djcities
  version: "0.1"

source:
  fn: djcities-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/djcities/djcities-0.1.tar.gz
  md5: b78d01cd31f5a364f071b75544cc1109
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djcities = djcities:main
    #
    # Would create an entry point called djcities that calls djcities.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - djcities
    - djcities.management
    - djcities.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/niwibe/djcities
  license: BSD License
  summary: 'A full database of contry/state/city/timezone for django. Based on django-cities but\nwith more clean codebase.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
