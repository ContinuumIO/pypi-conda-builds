package:
  name: abbyy
  version: "0.2"

source:
  fn: ABBYY-0.2.tar.gz
  url: https://pypi.python.org/packages/source/A/ABBYY/ABBYY-0.2.tar.gz
  md5: f54c574189581684ba62eac282c63c3f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - abbyy = abbyy:main
    #
    # Would create an entry point called abbyy that calls abbyy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=1.2.3
    - lxml >=3.1.0

  run:
    - python
    - requests >=1.2.3
    - lxml >=3.1.0

test:
  # Python imports
  imports:
    - ABBYY
    - abbyy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/samueltc/ABBYY
  license: LICENSE
  summary: 'ABBYY Cloud OCR API Wrapper.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
