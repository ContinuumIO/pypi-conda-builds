package:
  name: mozautomation
  version: "0.1"

source:
  fn: mozautomation-0.1.tar.gz
  url: https://pypi.python.org/packages/source/m/mozautomation/mozautomation-0.1.tar.gz
  md5: a6ea636db8c0283b7b4942ca3b303d35
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mozautomation = mozautomation:main
    #
    # Would create an entry point called mozautomation that calls mozautomation.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - mozautomation

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'Provides client libraries and other functionality used by numerous Mozilla projects.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
