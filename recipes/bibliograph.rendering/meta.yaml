package:
  name: bibliograph.rendering
  version: "1.0.2"

source:
  fn: bibliograph.rendering-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/b/bibliograph.rendering/bibliograph.rendering-1.0.2.tar.gz
  md5: 1ee0278e2e85b3a7bca433c1122c8fe4
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bibliograph.rendering = bibliograph.rendering:main
    #
    # Would create an entry point called bibliograph.rendering that calls bibliograph.rendering.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - bibliograph.core

  run:
    - python
    - setuptools
    - setuptools
    - bibliograph.core

test:
  # Python imports
  imports:
    - bibliograph
    - bibliograph.rendering
    - bibliograph.rendering.renderers
    - bibliograph.rendering.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/bibliograph.rendering
  license: Zope Public License
  summary: 'Bibliographic renderers'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
