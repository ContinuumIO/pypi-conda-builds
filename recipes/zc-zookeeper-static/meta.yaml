package:
  name: zc-zookeeper-static
  version: "3.4.4"

source:
  fn: zc-zookeeper-static-3.4.4.zip
  url: https://pypi.python.org/packages/source/z/zc-zookeeper-static/zc-zookeeper-static-3.4.4.zip
  md5: 279313ee03f6452606a36e41448c872a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zc-zookeeper-static = zc-zookeeper-static:main
    #
    # Would create an entry point called zc-zookeeper-static that calls zc-zookeeper-static.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/zc-zookeeper-static
  license: Apache
  summary: 'ZooKeeper Python bindings'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
