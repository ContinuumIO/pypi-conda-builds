package:
  name: django-filechooser
  version: "0.1.3"

source:
  fn: django-filechooser-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-filechooser/django-filechooser-0.1.3.tar.gz
  md5: cb39031dbf9fa586b48ee175f5ed8fcc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-filechooser = django-filechooser:main
    #
    # Would create an entry point called django-filechooser that calls django-filechooser.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - humanize >=0.5.0

  run:
    - python
    - humanize >=0.5.0

test:
  # Python imports
  imports:
    - filechooser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/martinvw/django-filechooser
  license: MIT
  summary: 'jQuery Filechooser for Django projects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
