package:
  name: sc.dev.core
  version: "20110707.3"

source:
  fn: sc.dev.core-20110707.3.tar.gz
  url: https://pypi.python.org/packages/source/s/sc.dev.core/sc.dev.core-20110707.3.tar.gz
  md5: a38ceb647368e18dc6ca13b211d6748d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sc.dev.core = sc.dev.core:main
    #
    # Would create an entry point called sc.dev.core that calls sc.dev.core.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - sc.paster.package ==0.8
    - sc.paster.buildout ==0.7.1
    - sc.paster.policy ==0.7
    - sc.paster.theme ==0.8
    - setuptools-git ==0.3.4
    - setuptools_hg ==0.2
    - collective.dist ==0.2.5
    - zest.releaser ==3.22
    - collective.dist ==0.2.5
    - sphinx ==1.0.4
    - rst2pdf ==0.16
    - i18ndude ==3.2.2
    - sphinx-pypi-upload ==0.2.1

  run:
    - python
    - setuptools
    - setuptools
    - sc.paster.package ==0.8
    - sc.paster.buildout ==0.7.1
    - sc.paster.policy ==0.7
    - sc.paster.theme ==0.8
    - setuptools-git ==0.3.4
    - setuptools_hg ==0.2
    - collective.dist ==0.2.5
    - zest.releaser ==3.22
    - collective.dist ==0.2.5
    - sphinx ==1.0.4
    - rst2pdf ==0.16
    - i18ndude ==3.2.2
    - sphinx-pypi-upload ==0.2.1

test:
  # Python imports
  imports:
    - sc
    - sc.dev
    - sc.dev.core

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.simplesconsultoria.com.br
  license: GPL
  summary: 'Developement core packages used by Simples Consultoria'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
