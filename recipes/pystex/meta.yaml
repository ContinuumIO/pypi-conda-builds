package:
  name: pystex
  version: "0.0.20"

source:
  fn: pystex-0.0.20.tar.gz
  url: https://pypi.python.org/packages/source/p/pystex/pystex-0.0.20.tar.gz
  md5: 6a19ba01863aa369d2c0dc7670a743e0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pystex = pystex:main
    #
    # Would create an entry point called pystex that calls pystex.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests ==1.2.3

  run:
    - python
    - requests ==1.2.3

test:
  # Python imports
  imports:
    - pystex

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/stevenc81/pystex
  license: UNKNOWN
  summary: 'Python StackExchange API Client'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
