package:
  name: talaria
  version: "0.2.0"

source:
  fn: Talaria-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/T/Talaria/Talaria-0.2.0.tar.gz
  md5: c8ceee339ca3979bdbf90ea31cd58f4a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - talaria = talaria:main
    #
    # Would create an entry point called talaria that calls talaria.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - mercurial >=1.3
    - markdown >=2.0
    - jinja2 >=2.2

  run:
    - python
    - mercurial >=1.3
    - markdown >=2.0
    - jinja2 >=2.2

test:
  # Python imports
  imports:
    - talaria

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.imankulov.name/talaria/
  license: GNU General Public License (GPL)
  summary: 'Talaria, a geek-friendly content management system'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
