package:
  name: clipboard-modifier
  version: "0.2.1"

source:
  fn: clipboard-modifier-0.2.1.zip
  url: https://pypi.python.org/packages/source/c/clipboard-modifier/clipboard-modifier-0.2.1.zip
  md5: e165c74889d210713348e95efb3aa490
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - clipboard-modifier = clipboard-modifier:main
    #
    # Would create an entry point called clipboard-modifier that calls clipboard-modifier.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - clipboardmodifier
    - clipboardmodifier/plugins

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/clipboard-modifier/
  license: Apache Software License
  summary: 'Change your clipboard text in a variety of ways.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
