package:
  name: django_deploys
  version: "0.1.10"

source:
  fn: django_deploys-0.1.10.tar.gz
  url: https://pypi.python.org/packages/source/d/django_deploys/django_deploys-0.1.10.tar.gz
  md5: b74c1da228438dfc37fe4b915813a184
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_deploys = django_deploys:main
    #
    # Would create an entry point called django_deploys that calls django_deploys.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - fabric ==1.5.1

  run:
    - python
    - fabric ==1.5.1

test:
  # Python imports
  imports:
    - django_deploys

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rpq/django_deploys
  license: MIT License
  summary: 'Some django deploy fabric methods'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
