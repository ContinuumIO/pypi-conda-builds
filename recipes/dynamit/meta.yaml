package:
  name: dynamit
  version: "1.0.3"

source:
  fn: DynaMIT-1.0.3.zip
  url: https://pypi.python.org/packages/source/D/DynaMIT/DynaMIT-1.0.3.zip
  md5: e0b2f37fae76e6679d91b9e9f2b5d282
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dynamit = dynamit:main
    #
    # Would create an entry point called dynamit that calls dynamit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - biopython >=1.65
    - scikit-learn >=0.15.2
    - scipy >=0.14.0
    - numpy >=1.9.1
    - matplotlib >=1.4.2
    - weblogo >=3.4

  run:
    - python
    - biopython >=1.65
    - scikit-learn >=0.15.2
    - scipy >=0.14.0
    - numpy >=1.9.1
    - matplotlib >=1.4.2
    - weblogo >=3.4

test:
  # Python imports
  imports:
    - dynamit

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/erikdassi/dynamit
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'the Dynamic Motif Integration Toolkit'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
