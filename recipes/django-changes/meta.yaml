package:
  name: django-changes
  version: "0.2.2"

source:
  fn: django-changes-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-changes/django-changes-0.2.2.tar.gz
  md5: 56880be4def1aa22b0c6e4ad2c62ddc2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-changes = django-changes:main
    #
    # Would create an entry point called django-changes that calls django-changes.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5

  run:
    - python
    - django >=1.5

test:
  # Python imports
  imports:
    - changes
    - changes.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rvause/django-changes
  license: BSD License
  summary: '\nA utility to store and access information about changes on objects in a Django\nproject.\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
