package:
  name: gaegraph
  version: "3.8"

source:
  fn: gaegraph-3.8.tar.gz
  url: https://pypi.python.org/packages/source/g/gaegraph/gaegraph-3.8.tar.gz
  md5: f082445ebcf191fac97f590d4c521480
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gaegraph = gaegraph:main
    #
    # Would create an entry point called gaegraph that calls gaegraph.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - gaebusiness >=4.5.1
    - gaeforms >=0.12

  run:
    - python
    - gaebusiness >=4.5.1
    - gaeforms >=0.12

test:
  # Python imports
  imports:
    - gaegraph

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/renzon/gaegraph
  license: BSD License
  summary: 'Graph Model on Google App Engine based on GraphAPI from Facebook'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
