package:
  name: collective.collage.imagetags
  version: "1.0.1"

source:
  fn: collective.collage.imagetags-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.collage.imagetags/collective.collage.imagetags-1.0.1.tar.gz
  md5: 629088493604b58b75126ffae53c8d87
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.collage.imagetags = collective.collage.imagetags:main
    #
    # Would create an entry point called collective.collage.imagetags that calls collective.collage.imagetags.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.imagetags
    - products.collage

  run:
    - python
    - setuptools
    - setuptools
    - collective.imagetags
    - products.collage

test:
  # Python imports
  imports:
    - collective
    - collective.collage
    - collective.collage.imagetags
    - collective.collage.imagetags.browser
    - collective.collage.imagetags.tests
    - collective.collage.imagetags.tests.image_hack

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/collective.collage.imagetags
  license: GPL
  summary: 'Collage view to display tagged images (collective.imagetags)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
