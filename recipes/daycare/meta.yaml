package:
  name: daycare
  version: "0.1"

source:
  fn: daycare-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/daycare/daycare-0.1.tar.gz
  md5: 647a7049f6c252efae49af1c6d8fe270
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - daycare = daycare:main
    #
    # Would create an entry point called daycare that calls daycare.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - daycare
    - daycare.middleware

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/grucha/daycare
  license: MIT License
  summary: 'A django app that tracks 404s, manages redirects and integrates with django-cms'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
