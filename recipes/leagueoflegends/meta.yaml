package:
  name: leagueoflegends
  version: "1.4b"

source:
  fn: leagueoflegends-1.4b.tar.gz
  url: https://pypi.python.org/packages/source/l/leagueoflegends/leagueoflegends-1.4b.tar.gz
  md5: 368c8e1b6a8c3ba26e410025874a6e56
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - leagueoflegends = leagueoflegends:main
    #
    # Would create an entry point called leagueoflegends that calls leagueoflegends.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - leagueoflegends

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/jennielees/leagueoflegends-python
  license: WTFPL
  summary: 'Unofficial libraries for interacting with the official League of Legends API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
