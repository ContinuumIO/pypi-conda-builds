package:
  name: django-kvtags
  version: "1.4.1"

source:
  fn: django-kvtags-1.4.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-kvtags/django-kvtags-1.4.1.tar.gz
  md5: d6b5d00dd896f8b7cec1f0003126ba67
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-kvtags = django-kvtags:main
    #
    # Would create an entry point called django-kvtags that calls django-kvtags.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5
    - unicodecsv >=0.9.4

  run:
    - python
    - django >=1.5
    - unicodecsv >=0.9.4

test:
  # Python imports
  imports:
    - kvtags
    - kvtags.management
    - kvtags.management.commands
    - kvtags.migrations
    - kvtags.south_migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT
  summary: 'Extensible key-value tagging for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
