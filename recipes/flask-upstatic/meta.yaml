package:
  name: flask-upstatic
  version: "0.1.2"

source:
  fn: Flask-Upstatic-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Upstatic/Flask-Upstatic-0.1.2.tar.gz
  md5: d9caab5128ebdade2d76c0b3b9b16fb5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-upstatic = flask-upstatic:main
    #
    # Would create an entry point called flask-upstatic that calls flask-upstatic.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - boto

  run:
    - python
    - boto

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/insynchq/flask-upstatic
  license: MIT
  summary: 'Opinionated library for working with CDNs in Flask.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
