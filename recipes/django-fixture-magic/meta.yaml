package:
  name: django-fixture-magic
  version: "0.0.5"

source:
  fn: django-fixture-magic-0.0.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-fixture-magic/django-fixture-magic-0.0.5.tar.gz
  md5: 23ed02c964513d2a6fe5235ca49d8855
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-fixture-magic = django-fixture-magic:main
    #
    # Would create an entry point called django-fixture-magic that calls django-fixture-magic.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - fixture_magic
    - fixture_magic.management
    - fixture_magic.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/davedash/django-fixture-magic
  license: BSD License
  summary: 'A few extra management tools to handle fixtures.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
