package:
  name: atreal.override.albumview
  version: "1.0.0rc2"

source:
  fn: atreal.override.albumview-1.0.0rc2.zip
  url: https://pypi.python.org/packages/source/a/atreal.override.albumview/atreal.override.albumview-1.0.0rc2.zip
  md5: d43d44d4d52e098ec6fa09f7aab8e04f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - atreal.override.albumview = atreal.override.albumview:main
    #
    # Would create an entry point called atreal.override.albumview that calls atreal.override.albumview.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - atreal
    - atreal.override
    - atreal.override.albumview
    - atreal.override.albumview.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.atreal.net
  license: GPL
  summary: 'Override album view of Plone for FileCart or/and RichFile Packages'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
