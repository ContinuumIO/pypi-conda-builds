package:
  name: django-macros
  version: "0.3.3"

source:
  fn: django-macros-0.3.3.zip
  url: https://pypi.python.org/packages/source/d/django-macros/django-macros-0.3.3.zip
  md5: 9e8abccdaa850e58570499734fde523d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-macros = django-macros:main
    #
    # Would create an entry point called django-macros that calls django-macros.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6

  run:
    - python
    - django >=1.6

test:
  # Python imports
  imports:
    - macros
    - macros.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/nalourie/django-macros
  license: MIT License
  summary: 'A Django template tag library for repeating blocks tags and creating in template macros.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
