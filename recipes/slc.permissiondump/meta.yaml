package:
  name: slc.permissiondump
  version: "0.2.1"

source:
  fn: slc.permissiondump-0.2.1.zip
  url: https://pypi.python.org/packages/source/s/slc.permissiondump/slc.permissiondump-0.2.1.zip
  md5: 1d37b4547591ca35b9cb30a48451ee31
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - slc.permissiondump = slc.permissiondump:main
    #
    # Would create an entry point called slc.permissiondump that calls slc.permissiondump.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - slc
    - slc.permissiondump

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/slc.permissiondump
  license: BSD
  summary: 'Visualize a tree of local roles assigned on Plone items.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
