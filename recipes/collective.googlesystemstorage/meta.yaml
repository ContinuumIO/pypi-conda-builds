package:
  name: collective.googlesystemstorage
  version: "1.0.0"

source:
  fn: collective.googlesystemstorage-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.googlesystemstorage/collective.googlesystemstorage-1.0.0.tar.gz
  md5: 400b3eb9c2bc08c6a50b3c0acf4d2439
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.googlesystemstorage = collective.googlesystemstorage:main
    #
    # Would create an entry point called collective.googlesystemstorage that calls collective.googlesystemstorage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.googleauthentication
    - iw.fss

  run:
    - python
    - setuptools
    - setuptools
    - collective.googleauthentication
    - iw.fss

test:
  # Python imports
  imports:
    - collective
    - collective.googlesystemstorage

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/collective.googlesystemstorage/
  license: GPL
  summary: 'GSS saves the document types supported by the Google Docs service on the Google servers'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
