package:
  name: benri.client
  version: "0.0.3"

source:
  fn: benri.client-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/b/benri.client/benri.client-0.0.3.tar.gz
  md5: 406a358723a9e87f00e18ef4f448320f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - benri.client = benri.client:main
    #
    # Would create an entry point called benri.client that calls benri.client.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - httplib2 ==0.6.0
    - simplejson >=1.7.3

  run:
    - python
    - httplib2 ==0.6.0
    - simplejson >=1.7.3

test:
  # Python imports
  imports:
    - benri
    - benri.client

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/benri.client/
  license: Apache 2.0
  summary: 'A client package for benri-based services.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
