package:
  name: plonetheme.bootstrap
  version: "1.0"

source:
  fn: plonetheme.bootstrap-1.0.zip
  url: https://pypi.python.org/packages/source/p/plonetheme.bootstrap/plonetheme.bootstrap-1.0.zip
  md5: ad0aae5dfabf749193fe161baf55bd37
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plonetheme.bootstrap = plonetheme.bootstrap:main
    #
    # Would create an entry point called plonetheme.bootstrap that calls plonetheme.bootstrap.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - z3c.jbot
    - plone.browserlayer
    - plone.app.jquery >1.7,<1.8
    - collective.js.bootstrap

  run:
    - python
    - setuptools
    - setuptools
    - z3c.jbot
    - plone.browserlayer
    - plone.app.jquery >1.7,<1.8
    - collective.js.bootstrap

test:
  # Python imports
  imports:
    - plonetheme
    - plonetheme.bootstrap
    - plonetheme.bootstrap.Extensions
    - plonetheme.bootstrap.browser
    - plonetheme.bootstrap.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/plonetheme.bootstrap
  license: GPL
  summary: 'bootstrap css integration'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
