package:
  name: django-recurrence
  version: "1.1.0"

source:
  fn: django-recurrence-1.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-recurrence/django-recurrence-1.1.0.tar.gz
  md5: 3ad58aa048d9362747ec16ac819136f2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-recurrence = django-recurrence:main
    #
    # Would create an entry point called django-recurrence that calls django-recurrence.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pytz
    - python-dateutil

  run:
    - python
    - pytz
    - python-dateutil

test:
  # Python imports
  imports:
    - recurrence

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD License
  summary: 'Django utility wrapping dateutil.rrule'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
