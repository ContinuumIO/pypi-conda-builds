package:
  name: djalog
  version: "0.9.4"

source:
  fn: Djalog-0.9.4.tar.gz
  url: https://pypi.python.org/packages/source/D/Djalog/Djalog-0.9.4.tar.gz
  md5: e778d8cf1cd05abc2822024f4e371b5c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djalog = djalog:main
    #
    # Would create an entry point called djalog that calls djalog.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - djalog
    - djalog.conf

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/djalog
  license: MIT License
  summary: '\nSimple, Django-ready (but can be used without Django as well) logger\nusing excellent logging module.\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
