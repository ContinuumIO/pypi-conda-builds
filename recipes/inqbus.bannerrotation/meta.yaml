package:
  name: inqbus.bannerrotation
  version: "1.1"

source:
  fn: inqbus.bannerrotation-1.1.tar.gz
  url: https://pypi.python.org/packages/source/i/inqbus.bannerrotation/inqbus.bannerrotation-1.1.tar.gz
  md5: f394e6bafe6c48a9db613f09645d9ad5
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - inqbus.bannerrotation = inqbus.bannerrotation:main
    #
    # Would create an entry point called inqbus.bannerrotation that calls inqbus.bannerrotation.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.browserlayer
    - z3c.form
    - plone.z3cform
    - plone.app.z3cform

  run:
    - python
    - setuptools
    - setuptools
    - plone.browserlayer
    - z3c.form
    - plone.z3cform
    - plone.app.z3cform

test:
  # Python imports
  imports:
    - inqbus
    - inqbus.bannerrotation
    - inqbus.bannerrotation.browser
    - inqbus.bannerrotation.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://inqbus-hosting.de/community
  license: GPL
  summary: 'A simple, ajax based bannerrotation viewlet for plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
