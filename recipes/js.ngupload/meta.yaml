package:
  name: js.ngupload
  version: "0.3.0"

source:
  fn: js.ngupload-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/j/js.ngupload/js.ngupload-0.3.0.tar.gz
  md5: 8c422ed6d274764371a97bcef2cb51ab
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - js.ngupload = js.ngupload:main
    #
    # Would create an entry point called js.ngupload that calls js.ngupload.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fanstatic
    - js.angularsetuptools

  run:
    - python
    - setuptools
    - fanstatic
    - js.angularsetuptools

test:
  # Python imports
  imports:
    - js
    - js.ngupload

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/disko/js.ngupload
  license: BSD License
  summary: 'Fanstatic packaging of ngUpload'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
