package:
  name: collective.recipe.bluebream
  version: "0.4.0"

source:
  fn: collective.recipe.bluebream-0.4.0.zip
  url: https://pypi.python.org/packages/source/c/collective.recipe.bluebream/collective.recipe.bluebream-0.4.0.zip
  md5: b8e4e2c7775fb9eb48895ccdc75455a9
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.recipe.bluebream = collective.recipe.bluebream:main
    #
    # Would create an entry point called collective.recipe.bluebream that calls collective.recipe.bluebream.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - paste
    - pastedeploy
    - pastescript
    - zc.recipe.egg
    - zope.securitypolicy
    - zope.component
    - zope.annotation
    - zope.browserresource
    - zope.app.dependable
    - zope.app.appsetup
    - zope.app.content
    - zope.publisher
    - zope.app.broken
    - zope.app.component
    - zope.app.generations
    - zope.app.error
    - zope.app.publisher
    - zope.app.security
    - zope.app.form
    - zope.app.i18n
    - zope.app.locales
    - zope.app.zopeappgenerations
    - zope.app.principalannotation
    - zope.app.basicskin
    - zope.app.rotterdam
    - zope.app.folder
    - zope.app.wsgi
    - zope.formlib
    - zope.i18n
    - zope.app.pagetemplate
    - zope.app.schema
    - zope.app.container
    - zope.app.debug
    - z3c.evalexception >=2.0
    - z3c.testsetup
    - zope.app.testing
    - zope.testbrowser
    - zope.login
    - zope.keyreference
    - zope.intid
    - zope.contentprovider
    - zope.app.zcmlfiles

  run:
    - python
    - setuptools
    - setuptools
    - paste
    - pastedeploy
    - pastescript
    - zc.recipe.egg
    - zope.securitypolicy
    - zope.component
    - zope.annotation
    - zope.browserresource
    - zope.app.dependable
    - zope.app.appsetup
    - zope.app.content
    - zope.publisher
    - zope.app.broken
    - zope.app.component
    - zope.app.generations
    - zope.app.error
    - zope.app.publisher
    - zope.app.security
    - zope.app.form
    - zope.app.i18n
    - zope.app.locales
    - zope.app.zopeappgenerations
    - zope.app.principalannotation
    - zope.app.basicskin
    - zope.app.rotterdam
    - zope.app.folder
    - zope.app.wsgi
    - zope.formlib
    - zope.i18n
    - zope.app.pagetemplate
    - zope.app.schema
    - zope.app.container
    - zope.app.debug
    - z3c.evalexception >=2.0
    - z3c.testsetup
    - zope.app.testing
    - zope.testbrowser
    - zope.login
    - zope.keyreference
    - zope.intid
    - zope.contentprovider
    - zope.app.zcmlfiles

test:
  # Python imports
  imports:
    - collective
    - collective.recipe
    - collective.recipe.bluebream
    - collective.recipe.bluebream.welcome

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.recipe.bluebream
  license: ZPL
  summary: 'collective.recipe.bluebream is a zc.buildout recipe         you can use to bootstrap a Bluebream project'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
