package:
  name: databuild
  version: "0.0.10"

source:
  fn: databuild-0.0.10.tar.gz
  url: https://pypi.python.org/packages/source/d/databuild/databuild-0.0.10.tar.gz
  md5: 695bf1cb87bcf8d41b8acc21fe1d5348
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - databuild = databuild:main
    #
    # Would create an entry point called databuild that calls databuild.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - databuild
    - databuild.adapters
    - databuild.adapters.base
    - databuild.adapters.locmem
    - databuild.adapters.shelved
    - databuild.environments
    - databuild.functions
    - databuild.operations
    - databuild.tests
    - databuild.vendor
    - databuild.vendor.unicodecsv

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/databuild/databuild
  license: BSD License
  summary: 'a build tool for data'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
