package:
  name: django-order
  version: "0.0.7"

source:
  fn: django-order-0.0.7.tar.gz
  url: https://pypi.python.org/packages/source/d/django-order/django-order-0.0.7.tar.gz
  md5: 4333af6eee261218b90c6aba6952a0b0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-order = django-order:main
    #
    # Would create an entry point called django-order that calls django-order.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-object-tools

  run:
    - python
    - django-object-tools

test:
  # Python imports
  imports:
    - order

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/praekelt/django-order
  license: BSD License
  summary: 'Django app allowing users to manually order objects via admin.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
