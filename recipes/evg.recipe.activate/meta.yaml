package:
  name: evg.recipe.activate
  version: "0.5"

source:
  fn: evg.recipe.activate-0.5.tar.gz
  url: https://pypi.python.org/packages/source/e/evg.recipe.activate/evg.recipe.activate-0.5.tar.gz
  md5: 6b565b06180d8b182b9aca0ebf395cea
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - evg.recipe.activate = evg.recipe.activate:main
    #
    # Would create an entry point called evg.recipe.activate that calls evg.recipe.activate.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - zc.recipe.egg

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - zc.recipe.egg

test:
  # Python imports
  imports:
    - evg
    - evg.recipe
    - evg.recipe.activate

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/generalov/evg.recipe.activate
  license: Zope Public License
  summary: 'This recipe generates activation script for zc.buildout environment.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
