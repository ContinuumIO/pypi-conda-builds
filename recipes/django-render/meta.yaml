package:
  name: django-render
  version: "0.3.8"

source:
  fn: django-render-0.3.8.tar.gz
  url: https://pypi.python.org/packages/source/d/django-render/django-render-0.3.8.tar.gz
  md5: 82189c50297cf7f3a0e28658b413221f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-render = django-render:main
    #
    # Would create an entry point called django-render that calls django-render.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - jinja2
    - coffin

  run:
    - python
    - django
    - jinja2
    - coffin

test:
  # Python imports
  imports:
    - render

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/phpdude/django-render
  license: UNKNOWN
  summary: 'Django templates rendering sugar'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
