package:
  name: bein-htminilims
  version: "1.065"

source:
  fn: bein-htminilims-1.065.tar.gz
  url: https://pypi.python.org/packages/source/b/bein-htminilims/bein-htminilims-1.065.tar.gz
  md5: cc398ad2f886cb7d5ff6a09563aa007e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bein-htminilims = bein-htminilims:main
    #
    # Would create an entry point called bein-htminilims that calls bein-htminilims.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - cherrypy
    - unittest2
    - mako

  run:
    - python
    - cherrypy
    - unittest2
    - mako

test:
  # Python imports
  imports:
    - bein_htminilims

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://madhadron.com/bein
  license: UNKNOWN
  summary: 'Web frontend to the MiniLIMS from bein'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
