package:
  name: products.hscustom
  version: "0.2"

source:
  fn: Products.HSCustom-0.2.zip
  url: https://pypi.python.org/packages/source/P/Products.HSCustom/Products.HSCustom-0.2.zip
  md5: e76a686f32fbf90af2d6636eb5a7da07
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - products.hscustom = products.hscustom:main
    #
    # Would create an entry point called products.hscustom that calls products.hscustom.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - Products
    - Products.HSCustom
    - Products.HSCustom.Extensions
    - Products.HSCustom.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/Products.HSCustom/
  license: GPL
  summary: "The HSCustom theme was originally created for my band's website."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
