package:
  name: asyncdynamo
  version: "0.2.8"

source:
  fn: asyncdynamo-0.2.8.tar.gz
  url: https://s3.amazonaws.com/bitly-downloads/asyncdynamo/asyncdynamo-0.2.8.tar.gz
  #md5: 
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - asyncdynamo = asyncdynamo:main
    #
    # Would create an entry point called asyncdynamo that calls asyncdynamo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - tornado
    - boto >=2.3.0

  run:
    - python
    - tornado
    - boto >=2.3.0

test:
  # Python imports
  imports:
    - asyncdynamo

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/bitly/asyncdynamo
  license: Apache Software License
  summary: 'async Amazon DynamoDB library for Tornado'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
