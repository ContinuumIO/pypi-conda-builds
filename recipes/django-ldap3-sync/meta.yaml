package:
  name: django-ldap3-sync
  version: "0.1.11"

source:
  fn: django-ldap3-sync-0.1.11.zip
  url: https://pypi.python.org/packages/source/d/django-ldap3-sync/django-ldap3-sync-0.1.11.zip
  md5: 156eb38603048d7c12f6cf42c56f1832
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-ldap3-sync = django-ldap3-sync:main
    #
    # Would create an entry point called django-ldap3-sync that calls django-ldap3-sync.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - ldap3 >=0.9.7.4

  run:
    - python
    - ldap3 >=0.9.7.4

test:
  # Python imports
  imports:
    - ldap3_sync
    - ldap3_sync.management
    - ldap3_sync.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/PGower/django-ldap3-sync
  license: BSD License
  summary: 'A Django application for synchronizing LDAP users, groups and group membership. (Forked from django-ldap-sync).'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
