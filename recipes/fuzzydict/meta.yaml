package:
  name: fuzzydict
  version: "0.0.1"

source:
  fn: fuzzydict-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/f/fuzzydict/fuzzydict-0.0.1.tar.gz
  md5: 1df433b4385ce5577098b6685ac9e232
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fuzzydict = fuzzydict:main
    #
    # Would create an entry point called fuzzydict that calls fuzzydict.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - fuzzydict

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/shoz/fuzzydict
  license: MIT License
  summary: 'FuzzyDict, an ambiguous dictionary'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
