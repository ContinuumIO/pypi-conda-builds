package:
  name: pymysql_utils
  version: "0.51"

source:
  fn: pymysql_utils-0.51.tar.gz
  url: https://pypi.python.org/packages/source/p/pymysql_utils/pymysql_utils-0.51.tar.gz
  md5: 39fcc58cbf04d98ba3a60fc519be9c4d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pymysql_utils = pymysql_utils:main
    #
    # Would create an entry point called pymysql_utils that calls pymysql_utils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - mysql-python >=1.2.5
    - configparser >=3.3.0

  run:
    - python
    - mysql-python >=1.2.5
    - configparser >=3.3.0

test:
  # Python imports
  imports:
    - pymysql_utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/paepcke/pymysql_utils
  license: BSD
  summary: 'Thin wrapper around pymysql. Provides Python iterator for queries. Abstracts away cursor.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
