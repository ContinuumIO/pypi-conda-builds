package:
  name: django-auth-ldap
  version: "1.2.6"

source:
  fn: django-auth-ldap-1.2.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-auth-ldap/django-auth-ldap-1.2.6.tar.gz
  md5: 20096f0780823d73a0b839b7a31a0e60
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-auth-ldap = django-auth-ldap:main
    #
    # Would create an entry point called django-auth-ldap that calls django-auth-ldap.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - python-ldap >=2.0

  run:
    - python
    - django
    - python-ldap >=2.0

test:
  # Python imports
  imports:
    - django_auth_ldap

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/psagers/django-auth-ldap/
  license: BSD License
  summary: 'Django LDAP authentication backend'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
