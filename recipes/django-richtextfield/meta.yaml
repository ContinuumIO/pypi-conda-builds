package:
  name: django-richtextfield
  version: "1.0.1"

source:
  fn: django-richtextfield-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-richtextfield/django-richtextfield-1.0.1.tar.gz
  md5: 76cc841a6abac9ec0925294ce152de59
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-richtextfield = django-richtextfield:main
    #
    # Would create an entry point called django-richtextfield that calls django-richtextfield.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - djrichtextfield

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/EightMedia/django-richtextfield
  license: MIT License
  summary: 'A Django model field and widget that renders a customizable WYSIWYG/rich text editor'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
