package:
  name: mongotron
  version: "0.3"

source:
  fn: mongotron-0.3.tar.gz
  url: https://pypi.python.org/packages/source/m/mongotron/mongotron-0.3.tar.gz
  md5: 0850cea41f749b3315b6d5f19296c99c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mongotron = mongotron:main
    #
    # Would create an entry point called mongotron that calls mongotron.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pymongo >=2.4.1

  run:
    - python
    - pymongo >=2.4.1

test:
  # Python imports
  imports:
    - mongotron

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.github.com/narrato/mongotron
  license: MIT License
  summary: 'Python mongodb ODM'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
