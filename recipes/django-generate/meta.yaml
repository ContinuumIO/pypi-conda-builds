package:
  name: django-generate
  version: "0.0.6"

source:
  fn: django-generate-0.0.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-generate/django-generate-0.0.6.tar.gz
  md5: 3089f3550a26df33695af6d2e1ff61e5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-generate = django-generate:main
    #
    # Would create an entry point called django-generate that calls django-generate.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - generate
    - generate.management
    - generate.management.commands
    - generate.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/praekelt/django-generate
  license: BSD License
  summary: 'Django slightly smarter than fixtures content generation app.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
