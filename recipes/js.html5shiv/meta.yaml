package:
  name: js.html5shiv
  version: "3.6.2-1"

source:
  fn: js.html5shiv-3.6.2-1.zip
  url: https://pypi.python.org/packages/source/j/js.html5shiv/js.html5shiv-3.6.2-1.zip
  md5: 9fdabb4082792bdc252047aafeb02126
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - js.html5shiv = js.html5shiv:main
    #
    # Would create an entry point called js.html5shiv that calls js.html5shiv.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fanstatic
    - setuptools

  run:
    - python
    - setuptools
    - fanstatic
    - setuptools

test:
  # Python imports
  imports:
    - js
    - js.html5shiv

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/fanstatic/js.html5shiv
  license: BSD
  summary: 'Fanstatic packaging of html5shiv'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
