package:
  name: list115utils
  version: "1.0.1"

source:
  fn: list115utils-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/l/list115utils/list115utils-1.0.1.tar.gz
  md5: bdceff2ce9ae46dfcee7392f763ff6cb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - list115utils = list115utils:main
    #
    # Would create an entry point called list115utils that calls list115utils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - list115utils
    - list115utils.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/a115/list115utils/
  license: GPL
  summary: 'Collection of clean, well-tested, well-documented Python utilities for manipulating lists and facilitating functional programming'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
