package:
  name: pyws
  version: "1.1.5"

source:
  fn: pyws-1.1.5.tar.gz
  url: https://pypi.python.org/packages/source/p/pyws/pyws-1.1.5.tar.gz
  md5: 94749f3b9e1d833cf549eccf26a341fe
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pyws = pyws:main
    #
    # Would create an entry point called pyws that calls pyws.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - lxml

  run:
    - python
    - lxml

test:
  # Python imports
  imports:
    - pyws
    - pyws.adapters
    - pyws.functions
    - pyws.functions.args
    - pyws.functions.args.types
    - pyws.protocols
    - pyws.protocols.soap

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/stepank/pyws
  license: MIT
  summary: 'Python SOAP server providing WSDL'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
