package:
  name: markov
  version: "0.1"

source:
  fn: Markov-0.1.tar.gz
  url: https://pypi.python.org/packages/source/M/Markov/Markov-0.1.tar.gz
  md5: cb7f8f2f8b8e62c9fb138be1753e6086
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - markov = markov:main
    #
    # Would create an entry point called markov that calls markov.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - markov

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/python-hidden-markov/
  license: Mozilla Public License 1.1 (MPL 1.1)
  summary: 'Python library for Hidden Markov Models'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
