package:
  name: django-email-password-reset
  version: "0.1.1"

source:
  fn: django-email-password-reset-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-email-password-reset/django-email-password-reset-0.1.1.tar.gz
  md5: 5fa81961975b2bb82be728870f70d2ae
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-email-password-reset = django-email-password-reset:main
    #
    # Would create an entry point called django-email-password-reset that calls django-email-password-reset.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - password_reset
    - password_reset.management
    - password_reset.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dokterbob/django-email-password-reset/
  license: BSD License
  summary: 'Minimalist app disabling all passwords and sending password reset emails.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
