package:
  name: bdflib
  version: "v1.0.1"

source:
  fn: bdflib-v1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/b/bdflib/bdflib-v1.0.1.tar.gz
  md5: 3e552e0ac9f22da397b940ce2e89f455
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bdflib = bdflib:main
    #
    # Would create an entry point called bdflib that calls bdflib.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - bdflib
    - bdflib.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://gitlab.com/Screwtapello/bdflib/wikis/home
  license: UNKNOWN
  summary: 'Library for working with BDF font files.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
