package:
  name: collective.portlet.pythonscript
  version: "1.2.1"

source:
  fn: collective.portlet.pythonscript-1.2.1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.portlet.pythonscript/collective.portlet.pythonscript-1.2.1.tar.gz
  md5: 12353d0b40ca0b75b551929b3d277b40
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.portlet.pythonscript = collective.portlet.pythonscript:main
    #
    # Would create an entry point called collective.portlet.pythonscript that calls collective.portlet.pythonscript.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - products.cmfcore
    - products.cmfplone >=4.1
    - products.genericsetup
    - products.statusmessages
    - setuptools
    - zope.interface
    - zope.schema

  run:
    - python
    - setuptools
    - products.cmfcore
    - products.cmfplone >=4.1
    - products.genericsetup
    - products.statusmessages
    - setuptools
    - zope.interface
    - zope.schema

test:
  # Python imports
  imports:
    - collective
    - collective.portlet
    - collective.portlet.pythonscript
    - collective.portlet.pythonscript.browser
    - collective.portlet.pythonscript.content
    - collective.portlet.pythonscript.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.portlet.pythonscript
  license: GNU General Public License v2 (GPLv2)
  summary: 'Portlet rendering collection of items returned by customizable Python scripts'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
