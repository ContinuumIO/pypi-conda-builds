package:
  name: django-emailmgr
  version: "0.9"

source:
  fn: django-emailmgr-0.9.tar.gz
  url: https://pypi.python.org/packages/source/d/django-emailmgr/django-emailmgr-0.9.tar.gz
  md5: 70ec82310eea25e65f787620b3e81d67
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-emailmgr = django-emailmgr:main
    #
    # Would create an entry point called django-emailmgr that calls django-emailmgr.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - emailmgr

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/un33k/django-emailmgr
  license: BSD License
  summary: 'An email manager for Django user'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
