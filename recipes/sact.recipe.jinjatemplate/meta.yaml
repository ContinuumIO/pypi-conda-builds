package:
  name: sact.recipe.jinjatemplate
  version: "1.2.1"

source:
  fn: sact.recipe.jinjatemplate-1.2.1.tar.gz
  url: https://pypi.python.org/packages/source/s/sact.recipe.jinjatemplate/sact.recipe.jinjatemplate-1.2.1.tar.gz
  md5: d1f893b944e6beecda96e6b2867d9bed
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sact.recipe.jinjatemplate = sact.recipe.jinjatemplate:main
    #
    # Would create an entry point called sact.recipe.jinjatemplate that calls sact.recipe.jinjatemplate.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.recipe.egg
    - jinja2

  run:
    - python
    - setuptools
    - setuptools
    - zc.recipe.egg
    - jinja2

test:
  # Python imports
  imports:
    - sact
    - sact.recipe
    - sact.recipe.jinjatemplate

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/securactive/sact.recipe.jinjatemplate
  license: BSD License
  summary: 'Buildout recipe for making files out of Jinja2 templates'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
