package:
  name: bucket3
  version: "0.14.3"

source:
  fn: bucket3-0.14.3.tar.gz
  url: https://pypi.python.org/packages/source/b/bucket3/bucket3-0.14.3.tar.gz
  md5: d9b1296012951e41a9bda4e668430117
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bucket3 = bucket3:main
    #
    # Would create an entry point called bucket3 that calls bucket3.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - markdown2
    - jinja2
    - pyyaml
    - docopt
    - twittersearch
    - webmentiontools
    - unidecode
    - htmlmin

  run:
    - python
    - markdown2
    - jinja2
    - pyyaml
    - docopt
    - twittersearch
    - webmentiontools
    - unidecode
    - htmlmin

test:
  # Python imports
  imports:
    - bucket3

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.bucket3.com/
  license: MIT License
  summary: 'Static blog generator.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
