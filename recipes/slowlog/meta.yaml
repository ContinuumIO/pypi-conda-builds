package:
  name: slowlog
  version: "0.9"

source:
  fn: slowlog-0.9.tar.gz
  url: https://pypi.python.org/packages/source/s/slowlog/slowlog-0.9.tar.gz
  md5: 36f85f5279fc8fb9f436139ee581c928
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - slowlog = slowlog:main
    #
    # Would create an entry point called slowlog that calls slowlog.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - perfmetrics

  run:
    - python
    - setuptools
    - setuptools
    - perfmetrics

test:
  # Python imports
  imports:
    - slowlog
    - slowlog.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/hathawsh/slowlog
  license: BSD-derived (http://www.repoze.org/LICENSE.txt)
  summary: 'Log and graph slow Pyramid and WSGI apps'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
