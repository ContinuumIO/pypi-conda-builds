package:
  name: django-paymecash
  version: "0.0.4"

source:
  fn: django-paymecash-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-paymecash/django-paymecash-0.0.4.tar.gz
  md5: b59264f3e62ac0415cf6b348f01ee879
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-paymecash = django-paymecash:main
    #
    # Would create an entry point called django-paymecash that calls django-paymecash.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3
    - south
    - django-annoying

  run:
    - python
    - django >=1.3
    - south
    - django-annoying

test:
  # Python imports
  imports:
    - paymecash

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/drmartiner/django-paymecash/
  license: MIT License
  summary: "It's application for pay-system Paymecash.py"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
