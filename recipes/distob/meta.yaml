package:
  name: distob
  version: "0.2.0"

source:
  fn: distob-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/distob/distob-0.2.0.tar.gz
  md5: 0033a6561a5d30d4971182f10e5cf131
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - distob = distob:main
    #
    # Would create an entry point called distob that calls distob.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - ipython >=2.1
    - pyzmq >=2.1.11
    - dill >=0.2.1

  run:
    - python
    - ipython >=2.1
    - pyzmq >=2.1.11
    - dill >=0.2.1

test:
  # Python imports
  imports:
    - distob

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/mattja/distob/
  license: GNU General Public License (GPL)
  summary: 'Distributed computing made easier, using remote objects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
