package:
  name: ebookkit
  version: "0.0.1"

source:
  fn: ebookkit-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/e/ebookkit/ebookkit-0.0.1.tar.gz
  md5: 99a2f726ea9545f60a4f11fd2cf5fbfa
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ebookkit = ebookkit:main
    #
    # Would create an entry point called ebookkit that calls ebookkit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - ebookkit

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/arnehilmann/ebookkit
  license: GNU General Public License (GPL)
  summary: 'helps creating ebooks'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
