package:
  name: js.jquery_jgrowl
  version: "1.4.2"

source:
  fn: js.jquery_jgrowl-1.4.2.tar.gz
  url: https://pypi.python.org/packages/source/j/js.jquery_jgrowl/js.jquery_jgrowl-1.4.2.tar.gz
  md5: 4ebe99c80aa36b7dade984dcad0648aa
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - js.jquery_jgrowl = js.jquery_jgrowl:main
    #
    # Would create an entry point called js.jquery_jgrowl that calls js.jquery_jgrowl.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fanstatic
    - js.jquery
    - setuptools

  run:
    - python
    - setuptools
    - fanstatic
    - js.jquery
    - setuptools

test:
  # Python imports
  imports:
    - js
    - js.jquery_jgrowl

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD
  summary: 'fanstatic jQuery jGrowl.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
