package:
  name: flask-mongokit
  version: "0.6"

source:
  fn: Flask-MongoKit-0.6.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-MongoKit/Flask-MongoKit-0.6.tar.gz
  md5: 71c0d49b5dcde90f739bf9757a5ff89f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-mongokit = flask-mongokit:main
    #
    # Would create an entry point called flask-mongokit that calls flask-mongokit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - mongokit

  run:
    - python
    - flask
    - mongokit

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/jarus/flask-mongokit
  license: BSD License
  summary: 'A Flask extension simplifies to use MongoKit'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
