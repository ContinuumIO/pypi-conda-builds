package:
  name: django-paypal
  version: "0.2.3"

source:
  fn: django-paypal-0.2.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-paypal/django-paypal-0.2.3.tar.gz
  md5: bcfbcb14c91a72017c9f6139829f2c23
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-paypal = django-paypal:main
    #
    # Would create an entry point called django-paypal that calls django-paypal.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4
    - six >=1.4.1
    - south >=1.0
    - requests >=2.5.3

  run:
    - python
    - django >=1.4
    - six >=1.4.1
    - south >=1.0
    - requests >=2.5.3

test:
  # Python imports
  imports:
    - paypal
    - paypal.pro
    - paypal.pro.migrations
    - paypal.pro.south_migrations
    - paypal.pro.tests
    - paypal.standard
    - paypal.standard.ipn
    - paypal.standard.ipn.migrations
    - paypal.standard.ipn.south_migrations
    - paypal.standard.ipn.tests
    - paypal.standard.migrations
    - paypal.standard.pdt
    - paypal.standard.pdt.migrations
    - paypal.standard.pdt.south_migrations
    - paypal.standard.pdt.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/spookylukey/django-paypal
  license: UNKNOWN
  summary: 'A pluggable Django application for integrating PayPal Payments Standard or Payments Pro'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
