package:
  name: begins
  version: "0.9"

source:
  fn: begins-0.9.tar.gz
  url: https://pypi.python.org/packages/source/b/begins/begins-0.9.tar.gz
  md5: 0c05924c76f37f7499f1f3306d374aa8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - begins = begins:main
    #
    # Would create an entry point called begins that calls begins.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - funcsigs

  run:
    - python
    - funcsigs

test:
  # Python imports
  imports:
    - begin
    - begins

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://begins.readthedocs.org
  license: Apache Software License
  summary: 'Command line programs for busy developers'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
