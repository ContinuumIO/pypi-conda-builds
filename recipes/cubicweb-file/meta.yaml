package:
  name: cubicweb-file
  version: "1.16.1"

source:
  fn: cubicweb-file-1.16.1.tar.gz
  url: https://pypi.python.org/packages/source/c/cubicweb-file/cubicweb-file-1.16.1.tar.gz
  md5: 4c001eb55ba837457566e1580234d46d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cubicweb-file = cubicweb-file:main
    #
    # Would create an entry point called cubicweb-file that calls cubicweb-file.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cubicweb >=3.18.0
    - pillow

  run:
    - python
    - cubicweb >=3.18.0
    - pillow

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.cubicweb.org/project/cubicweb-file
  license: LGPL
  summary: 'file component for the CubicWeb framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
