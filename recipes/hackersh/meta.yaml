package:
  name: hackersh
  version: "0.2.0"

source:
  fn: Hackersh-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/H/Hackersh/Hackersh-0.2.0.tar.gz
  md5: 6343e39b48980ee224ba0f8dd55678e0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hackersh = hackersh:main
    #
    # Would create an entry point called hackersh that calls hackersh.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pythonect ==0.4.2
    - prettytable >=0.6.1
    - netaddr >=0.7.10

  run:
    - python
    - pythonect ==0.4.2
    - prettytable >=0.6.1
    - netaddr >=0.7.10

test:
  # Python imports
  imports:
    - hackersh
    - hackersh.components
    - hackersh.components.external
    - hackersh.components.internal
    - hackersh.components.system

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.hackersh.org/
  license: GNU General Public License v2 or later (GPLv2+)
  summary: 'Hacker Shell, a shell (command interpreter) written in Python with Pythonect-like syntax, built-in security commands, and out of the box wrappers for various security tools.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
