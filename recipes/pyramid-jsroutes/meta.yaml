package:
  name: pyramid-jsroutes
  version: "0.1.2"

source:
  fn: pyramid-jsroutes-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/p/pyramid-jsroutes/pyramid-jsroutes-0.1.2.tar.gz
  md5: e8bd8bf420dd3098a7e4ce47b2d9c23a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pyramid-jsroutes = pyramid-jsroutes:main
    #
    # Would create an entry point called pyramid-jsroutes that calls pyramid-jsroutes.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyramid

  run:
    - python
    - pyramid

test:
  # Python imports
  imports:
    - jsroutes

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/marconi/pyramid-jsroutes
  license: pyramid_jwtauth
  summary: 'A pyramid routes composer for javascript.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
