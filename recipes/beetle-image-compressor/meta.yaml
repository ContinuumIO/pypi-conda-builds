package:
  name: beetle-image-compressor
  version: "0.1.0"

source:
  fn: Beetle-Image-Compressor-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/B/Beetle-Image-Compressor/Beetle-Image-Compressor-0.1.0.tar.gz
  md5: 54adb13e562ef9200cb4a164bd5d2b67
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - beetle-image-compressor = beetle-image-compressor:main
    #
    # Would create an entry point called beetle-image-compressor that calls beetle-image-compressor.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pillow

  run:
    - python
    - pillow

test:
  # Python imports
  imports:
    - beetle_image_compressor

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/cknv/beetle-image-compressor
  license: MIT License
  summary: 'Auto image compressor for Beetle'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
