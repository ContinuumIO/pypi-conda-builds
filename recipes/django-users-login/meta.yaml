package:
  name: django-users-login
  version: "0.1.2"

source:
  fn: django-users-login-0.1.2.zip
  url: https://pypi.python.org/packages/source/d/django-users-login/django-users-login-0.1.2.zip
  md5: e1ef246b6763fed3e3d42ed0f6b236b5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-users-login = django-users-login:main
    #
    # Would create an entry point called django-users-login that calls django-users-login.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django ==1.5.4

  run:
    - python
    - django ==1.5.4

test:
  # Python imports
  imports:
    - django_users_login
    - django_users_login.acl

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/quein/django-users-login/
  license: MIT License
  summary: 'Extension to manage users in django application.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
