package:
  name: django-analytics-client
  version: "0.1.5"

source:
  fn: django-analytics-client-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-analytics-client/django-analytics-client-0.1.5.tar.gz
  md5: 7c6566155f2e2593b2f582696ae58b74
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-analytics-client = django-analytics-client:main
    #
    # Would create an entry point called django-analytics-client that calls django-analytics-client.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - analytics_client

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/funkbit/django-analytics-client
  license: BSD License
  summary: 'Custom analytics client for Django projects.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
