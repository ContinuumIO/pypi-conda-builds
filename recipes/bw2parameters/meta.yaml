package:
  name: bw2parameters
  version: "0.3"

source:
  fn: bw2parameters-0.3.tar.gz
  url: https://pypi.python.org/packages/source/b/bw2parameters/bw2parameters-0.3.tar.gz
  md5: dd27ce7d3f537e238475a3e010e3fd06
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bw2parameters = bw2parameters:main
    #
    # Would create an entry point called bw2parameters that calls bw2parameters.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - asteval
    - numpy
    - scipy

  run:
    - python
    - asteval
    - numpy
    - scipy

test:
  # Python imports
  imports:
    - bw2parameters
    - bw2parameters.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/cmutel/brightway2-parameters
  license: BSD License
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
