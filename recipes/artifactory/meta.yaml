package:
  name: artifactory
  version: "0.1.12"

source:
  fn: artifactory-0.1.12.tar.gz
  url: https://pypi.python.org/packages/source/a/artifactory/artifactory-0.1.12.tar.gz
  md5: 7fabb2ed32e948d7590844474b2aa9d2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - artifactory = artifactory:main
    #
    # Would create an entry point called artifactory that calls artifactory.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pathlib
    - requests
    - python-dateutil

  run:
    - python
    - pathlib
    - requests
    - python-dateutil

test:
  # Python imports
  imports:
    - artifactory

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/parallels/artifactory
  license: MIT License
  summary: 'A Python to Artifactory interface'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
