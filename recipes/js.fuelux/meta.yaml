package:
  name: js.fuelux
  version: "2.5.0"

source:
  fn: js.fuelux-2.5.0.tar.gz
  url: https://pypi.python.org/packages/source/j/js.fuelux/js.fuelux-2.5.0.tar.gz
  md5: 0794e4d121b83361b6e0f9f99c44a893
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - js.fuelux = js.fuelux:main
    #
    # Would create an entry point called js.fuelux that calls js.fuelux.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fanstatic
    - js.bootstrap
    - setuptools

  run:
    - python
    - setuptools
    - fanstatic
    - js.bootstrap
    - setuptools

test:
  # Python imports
  imports:
    - js
    - js.fuelux

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/demsullivan/js.fuelux
  license: BSD
  summary: 'Fanstatic packaging for FuelUX'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
