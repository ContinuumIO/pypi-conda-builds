package:
  name: moneyutils
  version: "0.1.1"

source:
  fn: moneyutils-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/m/moneyutils/moneyutils-0.1.1.tar.gz
  md5: b316e2c6a52f55ae68b95ef26ce4ec04
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - moneyutils = moneyutils:main
    #
    # Would create an entry point called moneyutils that calls moneyutils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - moneyutils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rconradharris/moneyutils
  license: MIT License
  summary: 'Utilities for handling money'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
