package:
  name: collective.login_monitor
  version: "0.3.1"

source:
  fn: collective.login_monitor-0.3.1.zip
  url: https://pypi.python.org/packages/source/c/collective.login_monitor/collective.login_monitor-0.3.1.zip
  md5: 9d33a80bfb895d2cb48d5ebf9b3336a2
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.login_monitor = collective.login_monitor:main
    #
    # Would create an entry point called collective.login_monitor that calls collective.login_monitor.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.cmfplone
    - sqlalchemy
    - collective.js.jqueryui
    - z3c.saconfig

  run:
    - python
    - setuptools
    - setuptools
    - products.cmfplone
    - sqlalchemy
    - collective.js.jqueryui
    - z3c.saconfig

test:
  # Python imports
  imports:
    - collective
    - collective.login_monitor
    - collective.login_monitor.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/collective.login_monitor
  license: GPL
  summary: 'Store and monitor login access to your Plone site'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
