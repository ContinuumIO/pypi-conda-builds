package:
  name: lccc_mapper
  version: "0.0.2"

source:
  fn: lccc_mapper-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/l/lccc_mapper/lccc_mapper-0.0.2.tar.gz
  md5: 6010da24304c8223cd6772bfc040318c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lccc_mapper = lccc_mapper:main
    #
    # Would create an entry point called lccc_mapper that calls lccc_mapper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - lccc_mapper

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://packages.python.org/lccc_mapper
  license: BSD
  summary: 'Locale currency country code mapper'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
