package:
  name: binary_tree_dict_mod
  version: "1.0"

source:
  fn: binary_tree_dict_mod-1.0.tar.gz
  url: https://pypi.python.org/packages/source/b/binary_tree_dict_mod/binary_tree_dict_mod-1.0.tar.gz
  md5: 9aa2b61ceb14e9181507048860e5f0ba
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - binary_tree_dict_mod = binary_tree_dict_mod:main
    #
    # Would create an entry point called binary_tree_dict_mod that calls binary_tree_dict_mod.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - binary_tree_dict_mod

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://stromberg.dnsalias.org/~strombrg/binary-tree-dict/
  license: Apache v2
  summary: 'Pure Python binary tree (dictionary) module'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
