package:
  name: django-bootstrap-themes
  version: "3.1.2"

source:
  fn: django-bootstrap-themes-3.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-bootstrap-themes/django-bootstrap-themes-3.1.2.tar.gz
  md5: 3f5c16b91bd9fb3ca1ae503f8d39db35
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-bootstrap-themes = django-bootstrap-themes:main
    #
    # Would create an entry point called django-bootstrap-themes that calls django-bootstrap-themes.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4

  run:
    - python
    - django >=1.4

test:
  # Python imports
  imports:
    - bootstrap_themes
    - bootstrap_themes.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/no-dice/django-bootstrap-themes
  license: MIT License
  summary: 'Bootstrap theme support for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
