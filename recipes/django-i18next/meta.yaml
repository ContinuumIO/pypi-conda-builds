package:
  name: django-i18next
  version: "0.1.1"

source:
  fn: django-i18next-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-i18next/django-i18next-0.1.1.tar.gz
  md5: 9a4d9f5a4e6cb7470e84856c0df40d9d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-i18next = django-i18next:main
    #
    # Would create an entry point called django-i18next that calls django-i18next.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six >=1.4.1
    - django-nine >=0.1.1

  run:
    - python
    - six >=1.4.1
    - django-nine >=0.1.1

test:
  # Python imports
  imports:
    - i18next
    - i18next.templatetags
    - i18next.testing

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/barseghyanartur/django-i18next/
  license: GNU General Public License v2 (GPLv2) or GNU Lesser General Public License v2 or later (LGPLv2+)
  summary: "Additions to Django's i18n module."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
