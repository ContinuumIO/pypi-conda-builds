package:
  name: collective.classifieds
  version: "1.7"

source:
  fn: collective.classifieds-1.7.zip
  url: https://pypi.python.org/packages/source/c/collective.classifieds/collective.classifieds-1.7.zip
  md5: 38596968af010cf21548d17b452ad51b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.classifieds = collective.classifieds:main
    #
    # Would create an entry point called collective.classifieds that calls collective.classifieds.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - accesscontrol
    - acquisition
    - products.archetypes
    - products.atcontenttypes
    - products.cmfcore
    - products.cmfplone
    - products.genericsetup
    - products.validation
    - setuptools
    - zope.i18n
    - zope.i18nmessageid
    - zope.interface

  run:
    - python
    - setuptools
    - accesscontrol
    - acquisition
    - products.archetypes
    - products.atcontenttypes
    - products.cmfcore
    - products.cmfplone
    - products.genericsetup
    - products.validation
    - setuptools
    - zope.i18n
    - zope.i18nmessageid
    - zope.interface

test:
  # Python imports
  imports:
    - collective
    - collective.classifieds
    - collective.classifieds.browser
    - collective.classifieds.content
    - collective.classifieds.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.plone.org/products/classifieds
  license: GPL
  summary: 'Add classifieds to your Plone intranet or website.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
