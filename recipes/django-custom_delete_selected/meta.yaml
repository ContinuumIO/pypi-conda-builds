package:
  name: django-custom_delete_selected
  version: "0.1"

source:
  fn: django-custom_delete_selected-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-custom_delete_selected/django-custom_delete_selected-0.1.tar.gz
  md5: bd1d8babf858b6992310c123d3616736
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-custom_delete_selected = django-custom_delete_selected:main
    #
    # Would create an entry point called django-custom_delete_selected that calls django-custom_delete_selected.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - custom_delete_selected

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/saippuakauppias/django-custom_delete_selected
  license: ISC License (ISCL)
  summary: 'Customization of delete_selected ModelAdmin action for post and pre operations.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
