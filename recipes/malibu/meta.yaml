package:
  name: malibu
  version: "0.1.1-13"

source:
  fn: malibu-0.1.1-13.tar.gz
  url: https://pypi.python.org/packages/source/m/malibu/malibu-0.1.1-13.tar.gz
  md5: d3490ac3b5fefce110700c81e00765cb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - malibu = malibu:main
    #
    # Would create an entry point called malibu that calls malibu.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - malibu
    - malibu.config
    - malibu.connection
    - malibu.database
    - malibu.text
    - malibu.util

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://phabricator.maio.me/tag/malibu
  license: Unlicense
  summary: "maiome's library of utilities"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
