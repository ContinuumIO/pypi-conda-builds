package:
  name: django-happenings
  version: "0.2.4"

source:
  fn: django-happenings-0.2.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-happenings/django-happenings-0.2.4.tar.gz
  md5: b55bb64286dab35109d8c5190e7c4531
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-happenings = django-happenings:main
    #
    # Would create an entry point called django-happenings that calls django-happenings.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6
    - pytz
    - six >=1.9,<2.0

  run:
    - python
    - django >=1.6
    - pytz
    - six >=1.9,<2.0

test:
  # Python imports
  imports:
    - happenings
    - happenings.migrations
    - happenings.south_migrations
    - happenings.templatetags
    - happenings.utils
    - tests
    - tests.integration_tests
    - tests.unit_tests
    - tests.unit_tests.test_template_tags
    - tests.unit_tests.test_utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/wreckage/django-happenings
  license: BSD License
  summary: 'A simple event calendar app for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
