package:
  name: nimbstor
  version: "1.0.1"

source:
  fn: nimbstor-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/n/nimbstor/nimbstor-1.0.1.tar.gz
  md5: bcb49588f50f1bec5042245286955977
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - nimbstor = nimbstor:main
    #
    # Would create an entry point called nimbstor that calls nimbstor.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - nimbstor

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/nimbstor
  license: WTFPL
  summary: 'Store incrementally, compressed and encrypted data failsafe in filesystems, sFTP, WebDAV, IMAP or other backends.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
