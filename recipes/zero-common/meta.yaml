package:
  name: zero-common
  version: "0.1.10"

source:
  fn: zero-common-0.1.10.tar.gz
  url: https://pypi.python.org/packages/source/z/zero-common/zero-common-0.1.10.tar.gz
  md5: 0f43ae5a16779ff6f63b01705d65e96b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zero-common = zero-common:main
    #
    # Would create an entry point called zero-common that calls zero-common.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3.1
    - south >=0.7.3
    - django-less >=0.3

  run:
    - python
    - django >=1.3.1
    - south >=0.7.3
    - django-less >=0.3

test:
  # Python imports
  imports:
    - common
    - common.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/mandlaweb/Zero-Common
  license: apache license v2.0
  summary: 'App with common views, libs and templates that can be used by other apps'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
