package:
  name: matalg
  version: "0.1.0"

source:
  fn: Matalg-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/M/Matalg/Matalg-0.1.0.tar.gz
  md5: 587a9499c78c34149956d82b752675c8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - matalg = matalg:main
    #
    # Would create an entry point called matalg that calls matalg.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - matalg

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://akabaila.pcug.org.au/matalg-doc/index.html
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Pure Python3 Matrix Algebra Module.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
