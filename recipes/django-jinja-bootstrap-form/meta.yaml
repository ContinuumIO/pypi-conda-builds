package:
  name: django-jinja-bootstrap-form
  version: "4.0.3"

source:
  fn: django-jinja-bootstrap-form-4.0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-jinja-bootstrap-form/django-jinja-bootstrap-form-4.0.3.tar.gz
  md5: 159ffaa281d6ada927ccd0d243a02dc3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-jinja-bootstrap-form = django-jinja-bootstrap-form:main
    #
    # Would create an entry point called django-jinja-bootstrap-form that calls django-jinja-bootstrap-form.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6
    - django-jinja >=1.3.3

  run:
    - python
    - django >=1.6
    - django-jinja >=1.3.3

test:
  # Python imports
  imports:
    - bootstrapform_jinja
    - bootstrapform_jinja.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/samuelcolvin/django-jinja-bootstrap-form
  license: BSD
  summary: '\ndjango-jinja-bootstrap-form\n===========================\n\n`github.com/samuelcolvin/django-jinja-bootstrap-form <https://github.com/samuelcolvin/django-jinja-bootstrap-form>`_\n\nPort of `django-bootstrap-form <https://github.com/tzangms/django-bootstrap-form>`_\nwhich is compatible with `django-jinja <https://github.com/niwibe/django-jinja>`_.\n\nTo install:\n\n.. code-block:: shell\n\n    pip install django-jinja-bootstrap-form\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
