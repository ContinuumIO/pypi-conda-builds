package:
  name: django-xhtml2pdf
  version: "0.0.3"

source:
  fn: django-xhtml2pdf-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-xhtml2pdf/django-xhtml2pdf-0.0.3.tar.gz
  md5: 507a56a6924a60a8eee9048e49b999f3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-xhtml2pdf = django-xhtml2pdf:main
    #
    # Would create an entry point called django-xhtml2pdf that calls django-xhtml2pdf.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.2

  run:
    - python
    - django >=1.2

test:
  # Python imports
  imports:
    - django_xhtml2pdf

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD License
  summary: 'A Django app to generate pdfs from templates'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
