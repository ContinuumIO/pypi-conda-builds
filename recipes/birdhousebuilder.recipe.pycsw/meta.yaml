package:
  name: birdhousebuilder.recipe.pycsw
  version: "0.2.0"

source:
  fn: birdhousebuilder.recipe.pycsw-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/b/birdhousebuilder.recipe.pycsw/birdhousebuilder.recipe.pycsw-0.2.0.tar.gz
  md5: da43ba6d91e5f3793534c9c36506857c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - birdhousebuilder.recipe.pycsw = birdhousebuilder.recipe.pycsw:main
    #
    # Would create an entry point called birdhousebuilder.recipe.pycsw that calls birdhousebuilder.recipe.pycsw.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - mako
    - birdhousebuilder.recipe.conda
    - birdhousebuilder.recipe.supervisor
    - birdhousebuilder.recipe.nginx

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - mako
    - birdhousebuilder.recipe.conda
    - birdhousebuilder.recipe.supervisor
    - birdhousebuilder.recipe.nginx

test:
  # Python imports
  imports:
    - birdhousebuilder
    - birdhousebuilder.recipe
    - birdhousebuilder.recipe.pycsw
    - birdhousebuilder.recipe.pycsw.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bird-house/birdhousebuilder.recipe.pycsw
  license: BSD License
  summary: 'A Buildout recipe to install and configure pycsw Catalog Service (CSW) with Anaconda.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
