package:
  name: wtforms-ndb
  version: "0.0.2"

source:
  fn: wtforms-ndb-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/w/wtforms-ndb/wtforms-ndb-0.0.2.tar.gz
  md5: 42407314a5dcbd61fb9be0bc02320778
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - wtforms-ndb = wtforms-ndb:main
    #
    # Would create an entry point called wtforms-ndb that calls wtforms-ndb.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - wtforms >=1.0.5

  run:
    - python
    - wtforms >=1.0.5

test:
  # Python imports
  imports:
    - wtforms_ndb

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/xlevus/wtforms-ndb
  license: UNKNOWN
  summary: "Wtforms mapper for Appengine's NDB library."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
