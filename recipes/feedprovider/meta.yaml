package:
  name: feedprovider
  version: "0.2.1"

source:
  fn: feedprovider-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/f/feedprovider/feedprovider-0.2.1.tar.gz
  md5: bd65c418fa9ed4795f0a6a8daceae849
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - feedprovider = feedprovider:main
    #
    # Would create an entry point called feedprovider that calls feedprovider.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - feedprovider >=0.2.1

  run:
    - python
    - feedprovider >=0.2.1

test:
  # Python imports
  imports:
    - commonplugs
    - feedprovider
    - gozerlib
    - gozerlib.contrib
    - gozerlib.gae
    - gozerlib.gae.utils
    - gozerlib.gae.wave
    - gozerlib.gae.web
    - gozerlib.gae.xmpp
    - gozerlib.plugs
    - gozerlib.remote
    - gozerlib.socket
    - gozerlib.socket.irc
    - gozerlib.socket.rest
    - gozerlib.socket.utils
    - gozerlib.utils
    - socketplugs
    - waveplugs

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://feedprovider.googlecode.com/
  license: MIT License
  summary: 'The hubbub bot of the future!'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
