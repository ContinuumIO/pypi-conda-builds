package:
  name: clearbit
  version: "0.0.7"

source:
  fn: clearbit-0.0.7.tar.gz
  url: https://pypi.python.org/packages/source/c/clearbit/clearbit-0.0.7.tar.gz
  md5: f5d0ab39a5a972dfb5ec121e73744934
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - clearbit = clearbit:main
    #
    # Would create an entry point called clearbit that calls clearbit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=0.8.8
    - simplejson

  run:
    - python
    - requests >=0.8.8
    - simplejson

test:
  # Python imports
  imports:
    - clearbit

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://clearbit.com
  license: MIT License
  summary: 'Clearbit python bindings'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
