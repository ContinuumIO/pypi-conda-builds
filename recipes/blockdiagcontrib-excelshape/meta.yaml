package:
  name: blockdiagcontrib-excelshape
  version: "0.1.0"

source:
  fn: blockdiagcontrib-excelshape-0.1.0.zip
  url: https://pypi.python.org/packages/source/b/blockdiagcontrib-excelshape/blockdiagcontrib-excelshape-0.1.0.zip
  md5: e86f0a6676c7c72da295efc3ce90c52a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - blockdiagcontrib-excelshape = blockdiagcontrib-excelshape:main
    #
    # Would create an entry point called blockdiagcontrib-excelshape that calls blockdiagcontrib-excelshape.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - blockdiag >=1.2.1
    - webcolors >=1.3
    - setuptools

  run:
    - python
    - setuptools
    - blockdiag >=1.2.1
    - webcolors >=1.3
    - setuptools

test:
  # Python imports
  imports:
    - blockdiagcontrib_excelshape

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/mzp/blockdiagcontrib-excelshape
  license: Python Software Foundation License
  summary: 'imagedrawer plugin for blockdiag'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
