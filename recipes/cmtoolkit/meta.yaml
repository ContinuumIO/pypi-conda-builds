package:
  name: cmtoolkit
  version: "0.0.1"

source:
  fn: cmtoolkit-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/cmtoolkit/cmtoolkit-0.0.1.tar.gz
  md5: df9bac2e89a486237cc8b9f9537146ff
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cmtoolkit = cmtoolkit:main
    #
    # Would create an entry point called cmtoolkit that calls cmtoolkit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - cmtoolkit
    - conformalmapping
    - conformalmapping.cmt

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/AndrewWalker/cmtoolkit
  license: MIT License
  summary: 'Conformal Mapping Toolkit (ported to Python)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
