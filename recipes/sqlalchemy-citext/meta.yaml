package:
  name: sqlalchemy-citext
  version: "1.3-0"

source:
  fn: sqlalchemy-citext-1.3-0.tar.gz
  url: https://pypi.python.org/packages/source/s/sqlalchemy-citext/sqlalchemy-citext-1.3-0.tar.gz
  md5: 5fbd2ab0318a271d10d8818cd9794c50
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sqlalchemy-citext = sqlalchemy-citext:main
    #
    # Would create an entry point called sqlalchemy-citext that calls sqlalchemy-citext.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sqlalchemy >=0.6

  run:
    - python
    - sqlalchemy >=0.6

test:
  # Python imports
  imports:
    - citext
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mahmoudimus/sqlalchemy-citext
  license: BSD License
  summary: 'A sqlalchemy plugin that allows postgres use of CITEXT.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
