package:
  name: django-migrator
  version: "0.5.4"

source:
  fn: django-migrator-0.5.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-migrator/django-migrator-0.5.4.tar.gz
  md5: a93c3b8b8734c05d0de6ca65ebe00934
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-migrator = django-migrator:main
    #
    # Would create an entry point called django-migrator that calls django-migrator.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3
    - south >=0.7.3

  run:
    - python
    - django >=1.3
    - south >=0.7.3

test:
  # Python imports
  imports:
    - migrator
    - migrator.management
    - migrator.management.commands
    - migrator.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Django Migration Framework, useful to do migrations from other systems to Django data model'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
