package:
  name: collective.cu3er
  version: "0.1"

source:
  fn: collective.cu3er-0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.cu3er/collective.cu3er-0.1.tar.gz
  md5: 0bb48fe9acb8fa69815ede6384c6747c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.cu3er = collective.cu3er:main
    #
    # Would create an entry point called collective.cu3er that calls collective.cu3er.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - archetypes.schemaextender

  run:
    - python
    - setuptools
    - setuptools
    - archetypes.schemaextender

test:
  # Python imports
  imports:
    - collective
    - collective.cu3er
    - collective.cu3er.Extensions
    - collective.cu3er.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/collective.cu3er
  license: GPL
  summary: 'CU3ER integration for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
