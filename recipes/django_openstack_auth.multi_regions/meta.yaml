package:
  name: django_openstack_auth.multi_regions
  version: "1.2.0"

source:
  fn: django_openstack_auth.multi_regions-1.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django_openstack_auth.multi_regions/django_openstack_auth.multi_regions-1.2.0.tar.gz
  md5: 5cd56f6d44c96fafc0253a302a099c22
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_openstack_auth.multi_regions = django_openstack_auth.multi_regions:main
    #
    # Would create an entry point called django_openstack_auth.multi_regions that calls django_openstack_auth.multi_regions.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Apache Software License
  summary: 'OpenStack Identity Multi-Regions Switch'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
