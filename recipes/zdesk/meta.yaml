package:
  name: zdesk
  version: "2.2.0"

source:
  fn: zdesk-2.2.0.tar.gz
  url: https://pypi.python.org/packages/source/z/zdesk/zdesk-2.2.0.tar.gz
  md5: 814a81f808ad0f2140fd5a6c91da0e19
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zdesk = zdesk:main
    #
    # Would create an entry point called zdesk that calls zdesk.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - httplib2
    - simplejson

  run:
    - python
    - httplib2
    - simplejson

test:
  # Python imports
  imports:
    - zdesk

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/fprimex/zdesk
  license: MIT License
  summary: 'Python API Wrapper for Zendesk'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
