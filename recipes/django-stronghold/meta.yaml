package:
  name: django-stronghold
  version: "0.2.7"

source:
  fn: django-stronghold-0.2.7.tar.gz
  url: https://pypi.python.org/packages/source/d/django-stronghold/django-stronghold-0.2.7.tar.gz
  md5: b7640228c91e69563bc883fc0b82e662
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-stronghold = django-stronghold:main
    #
    # Would create an entry point called django-stronghold that calls django-stronghold.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - stronghold
    - stronghold.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mgrouchy/django-stronghold
  license: MIT License
  summary: 'Get inside your stronghold and make all your Django views default login_required'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
