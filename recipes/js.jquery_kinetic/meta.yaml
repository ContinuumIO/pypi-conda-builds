package:
  name: js.jquery_kinetic
  version: "1.8.2"

source:
  fn: js.jquery_kinetic-1.8.2.tar.gz
  url: https://pypi.python.org/packages/source/j/js.jquery_kinetic/js.jquery_kinetic-1.8.2.tar.gz
  md5: d7bd0198215a144145e8ae9d9b146971
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - js.jquery_kinetic = js.jquery_kinetic:main
    #
    # Would create an entry point called js.jquery_kinetic that calls js.jquery_kinetic.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fanstatic
    - setuptools
    - js.jquery
    - js.jqueryui

  run:
    - python
    - setuptools
    - fanstatic
    - setuptools
    - js.jquery
    - js.jqueryui

test:
  # Python imports
  imports:
    - js
    - js.jquery_kinetic

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://davetayls.me/jquery.kinetic/
  license: BSD
  summary: 'fanstatic packaging of jquery.kinetic'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
