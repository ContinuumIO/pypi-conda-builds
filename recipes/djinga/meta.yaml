package:
  name: djinga
  version: "1.1.4"

source:
  fn: djinga-1.1.4.zip
  url: https://pypi.python.org/packages/source/d/djinga/djinga-1.1.4.zip
  md5: 05c02ee3446da289d51cb68df85fcaf6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djinga = djinga:main
    #
    # Would create an entry point called djinga that calls djinga.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4
    - jinja2

  run:
    - python
    - django >=1.4
    - jinja2

test:
  # Python imports
  imports:
    - djinga
    - djinga.backends
    - djinga.ext
    - djinga.management
    - djinga.management.commands
    - djinga.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/tkhyn/djinga
  license: MIT License or BSD License
  summary: 'Unobtrusive jinja2 integration in Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
