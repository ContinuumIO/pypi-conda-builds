package:
  name: collective.perseo
  version: "0.6.6"

source:
  fn: collective.perseo-0.6.6.zip
  url: https://pypi.python.org/packages/source/c/collective.perseo/collective.perseo-0.6.6.zip
  md5: 255c453bceafb5235448d223e1b14859
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.perseo = collective.perseo:main
    #
    # Would create an entry point called collective.perseo that calls collective.perseo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.monkeypatcher
    - collective.js.jqueryui

  run:
    - python
    - setuptools
    - setuptools
    - collective.monkeypatcher
    - collective.js.jqueryui

test:
  # Python imports
  imports:
    - collective
    - collective.perseo
    - collective.perseo.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/collective.perseo
  license: GPL
  summary: 'Search Engine Optimization Package'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
