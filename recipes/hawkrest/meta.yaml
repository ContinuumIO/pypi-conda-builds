package:
  name: hawkrest
  version: "0.0.3"

source:
  fn: hawkrest-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/h/hawkrest/hawkrest-0.0.3.tar.gz
  md5: 82889bf4505bba03f40dae1ad1518c7d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hawkrest = hawkrest:main
    #
    # Would create an entry point called hawkrest that calls hawkrest.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - djangorestframework
    - mohawk >=0.2.2

  run:
    - python
    - djangorestframework
    - mohawk >=0.2.2

test:
  # Python imports
  imports:
    - hawkrest
    - hawkrest.management
    - hawkrest.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kumar303/hawkrest
  license: MPL 2.0 (Mozilla Public License)
  summary: 'Hawk HTTP Authorization for Django Rest Framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
