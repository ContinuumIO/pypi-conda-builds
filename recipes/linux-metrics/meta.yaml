package:
  name: linux-metrics
  version: "0.1.4"

source:
  fn: linux-metrics-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/l/linux-metrics/linux-metrics-0.1.4.tar.gz
  md5: ede729393b2fc54d926be8f521c2e082
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - linux-metrics = linux-metrics:main
    #
    # Would create an entry point called linux-metrics that calls linux-metrics.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - linux_metrics

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/cgoldberg/linux-metrics
  license: MIT License
  summary: 'linux-metrics - System Metrics/Stats Library for Linux'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
