package:
  name: simple_paginator
  version: "0.3.0"

source:
  fn: simple_paginator-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/s/simple_paginator/simple_paginator-0.3.0.tar.gz
  md5: ce0eaf5aa7f18c43a2130b1bd13ab5f9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - simple_paginator = simple_paginator:main
    #
    # Would create an entry point called simple_paginator that calls simple_paginator.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - simple_paginator

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dbrgn/django-simplepaginator
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'A simple wrapper around the Django paginator'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
