package:
  name: django-snowshoestamp
  version: "0.1.0"

source:
  fn: django-snowshoestamp-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-snowshoestamp/django-snowshoestamp-0.1.0.tar.gz
  md5: 0c87b34d542fc93370f12e41de226d7e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-snowshoestamp = django-snowshoestamp:main
    #
    # Would create an entry point called django-snowshoestamp that calls django-snowshoestamp.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sssapi
    - requests_oauthlib
    - django-braces

  run:
    - python
    - sssapi
    - requests_oauthlib
    - django-braces

test:
  # Python imports
  imports:
    - snowshoestamp

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rosscdh/django-snowshoestamp
  license: MIT
  summary: 'A Django app for integrating with snowshoestamp'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
