package:
  name: mpworker
  version: "0.3"

source:
  fn: mpworker-0.3.zip
  url: https://pypi.python.org/packages/source/m/mpworker/mpworker-0.3.zip
  md5: 3242914af4d57bc42ec4b6a2d8027e5a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mpworker = mpworker:main
    #
    # Would create an entry point called mpworker that calls mpworker.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - mpworker

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dustyrockpyle/mpworker
  license: MIT License
  summary: 'Easy to use stateful multiprocessing. Asyncio compatible.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
