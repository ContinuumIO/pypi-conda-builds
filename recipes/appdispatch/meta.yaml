package:
  name: appdispatch
  version: "0.3.4"

source:
  fn: AppDispatch-0.3.4.tar.gz
  url: https://pypi.python.org/packages/source/A/AppDispatch/AppDispatch-0.3.4.tar.gz
  md5: 07121a4f5ab8316cdbb39bae91e50a17
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - appdispatch = appdispatch:main
    #
    # Would create an entry point called appdispatch that calls appdispatch.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - appdispatch

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://jimmyg.org/work/code/appdispatch/index.html
  license: GNU Affero General Public License v3
  summary: 'Builds upon a PipeStack architecture to provide a nice way of writing apps'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
