package:
  name: django-deprecated-fbv
  version: "0.1"

source:
  fn: django-deprecated-fbv-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-deprecated-fbv/django-deprecated-fbv-0.1.tar.gz
  md5: 0d77e9b0bb51c70f4d08d590b3241090
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-deprecated-fbv = django-deprecated-fbv:main
    #
    # Would create an entry point called django-deprecated-fbv that calls django-deprecated-fbv.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - deprecated_fbv

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dokterbob/django-deprecated-fbv/
  license: BSD License
  summary: 'Deprecated Django function based views from Django 1.4 in a neat little package.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
