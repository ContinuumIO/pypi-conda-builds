package:
  name: accc
  version: "0.0.3"

source:
  fn: ACCC-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/A/ACCC/ACCC-0.0.3.tar.gz
  md5: ed69ad16d64c773ef04c253f2fb727b0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - accc = accc:main
    #
    # Would create an entry point called accc that calls accc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - accc
    - accc.compiler
    - accc.dnacompiler
    - accc.langspec
    - accc.lexems

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Aluriak/ACCC
  license: GNU General Public License (GPL)
  summary: 'Always Correct Correctness Compilator'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
