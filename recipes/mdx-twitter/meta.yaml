package:
  name: mdx-twitter
  version: "0.2.6"

source:
  fn: mdx-twitter-0.2.6.tar.gz
  url: https://pypi.python.org/packages/source/m/mdx-twitter/mdx-twitter-0.2.6.tar.gz
  md5: eabba7f4a835f397a9dc62236b107a3b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mdx-twitter = mdx-twitter:main
    #
    # Would create an entry point called mdx-twitter that calls mdx-twitter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - markdown
    - beautifulsoup4
    - requests-oauthlib >=0.3.3

  run:
    - python
    - markdown
    - beautifulsoup4
    - requests-oauthlib >=0.3.3

test:
  # Python imports
  imports:
    - mdx_twitter

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/zen4ever/mdx-twitter
  license: BSD License
  summary: 'Markdown extension for embedding tweets'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
