package:
  name: loghacks
  version: "0.05"

source:
  fn: loghacks-0.05.tar.gz
  url: https://pypi.python.org/packages/source/l/loghacks/loghacks-0.05.tar.gz
  md5: 7972ff5bddd05183d8da89bf20a5bd03
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - loghacks = loghacks:main
    #
    # Would create an entry point called loghacks that calls loghacks.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - loghacks

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/woome/loghacks
  license: GNU General Public License (GPL)
  summary: 'python logging utility for django projects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
