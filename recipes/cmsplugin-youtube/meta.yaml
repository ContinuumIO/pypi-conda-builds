package:
  name: cmsplugin-youtube
  version: "0.1.5-alpha-1"

source:
  fn: cmsplugin-youtube-0.1.5-alpha-1.tar.gz
  url: https://pypi.python.org/packages/source/c/cmsplugin-youtube/cmsplugin-youtube-0.1.5-alpha-1.tar.gz
  md5: 0cbda1da5c4bbfffc80517308bbd67dd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cmsplugin-youtube = cmsplugin-youtube:main
    #
    # Would create an entry point called cmsplugin-youtube that calls cmsplugin-youtube.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - cmsplugin_youtube

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/xenofox/cmsplugin-youtube/
  license: BSD License
  summary: 'YouTube embedding for django-cms'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
