package:
  name: flask-jinjahelpers
  version: "0.3.7"

source:
  fn: Flask-JinjaHelpers-0.3.7.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-JinjaHelpers/Flask-JinjaHelpers-0.3.7.tar.gz
  md5: 8b51a27dd877e58668450442e4f212f4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-jinjahelpers = flask-jinjahelpers:main
    #
    # Would create an entry point called flask-jinjahelpers that calls flask-jinjahelpers.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.8

  run:
    - python
    - flask >=0.8

test:
  # Python imports
  imports:
    - flask_jinjahelpers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kvesteri/flask-jinjahelpers
  license: BSD License
  summary: 'Various helpers for Flask based Jinja2 templates.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
