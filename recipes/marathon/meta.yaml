package:
  name: marathon
  version: "0.6.13"

source:
  fn: marathon-0.6.13.tar.gz
  url: https://pypi.python.org/packages/source/m/marathon/marathon-0.6.13.tar.gz
  md5: c9da248779bb0c7e0636611130e4e6a9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - marathon = marathon:main
    #
    # Would create an entry point called marathon that calls marathon.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=2.0.0

  run:
    - python
    - requests >=2.0.0

test:
  # Python imports
  imports:
    - marathon
    - marathon.models

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/thefactory/marathon-python
  license: MIT License
  summary: 'Marathon Client Library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
