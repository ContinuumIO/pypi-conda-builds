package:
  name: brasil.gov.vcge
  version: "2.0.0"

source:
  fn: brasil.gov.vcge-2.0.0.zip
  url: https://pypi.python.org/packages/source/b/brasil.gov.vcge/brasil.gov.vcge-2.0.0.zip
  md5: 9e644ddba196d6a1dc9483025e009396
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - brasil.gov.vcge = brasil.gov.vcge:main
    #
    # Would create an entry point called brasil.gov.vcge that calls brasil.gov.vcge.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - plone >=4.2
    - collective.z3cform.widgets
    - raptus.autocompletewidget
    - rdflib
    - setuptools
    - zope.component
    - z3c.form
    - zope.interface

  run:
    - python
    - setuptools
    - plone >=4.2
    - collective.z3cform.widgets
    - raptus.autocompletewidget
    - rdflib
    - setuptools
    - zope.component
    - z3c.form
    - zope.interface

test:
  # Python imports
  imports:
    - brasil
    - brasil.gov
    - brasil.gov.vcge
    - brasil.gov.vcge.at
    - brasil.gov.vcge.browser
    - brasil.gov.vcge.contentrules
    - brasil.gov.vcge.controlpanel
    - brasil.gov.vcge.dx
    - brasil.gov.vcge.tests
    - brasil.gov.vcge.upgrades
    - brasil.gov.vcge.upgrades.v1000
    - brasil.gov.vcge.upgrades.v2000

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.plone.org.br/gov/
  license: GNU General Public License v2 (GPLv2)
  summary: u'.gov.br: Vocabul\xe1rio Controlado do Governo Eletr\xf4nico'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
