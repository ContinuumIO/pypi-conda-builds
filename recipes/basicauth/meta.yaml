package:
  name: basicauth
  version: "0.3"

source:
  fn: basicauth-0.3.tar.gz
  url: https://pypi.python.org/packages/source/b/basicauth/basicauth-0.3.tar.gz
  md5: 1a20ac1c6cce38a1e5caf439776c7675
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - basicauth = basicauth:main
    #
    # Would create an entry point called basicauth that calls basicauth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - basicauth

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rdegges/python-basicauth
  license: UNLICENSE
  summary: 'An incredibly simple HTTP basic auth implementation.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
