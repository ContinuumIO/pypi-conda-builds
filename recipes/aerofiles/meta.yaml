package:
  name: aerofiles
  version: "0.3"

source:
  fn: aerofiles-0.3.tar.gz
  url: https://pypi.python.org/packages/source/a/aerofiles/aerofiles-0.3.tar.gz
  md5: b3bac7b45d67e02e4b2f3045005372eb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - aerofiles = aerofiles:main
    #
    # Would create an entry point called aerofiles that calls aerofiles.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - aerofiles
    - aerofiles.flarmcfg
    - aerofiles.igc
    - aerofiles.openair
    - aerofiles.seeyou
    - aerofiles.util
    - aerofiles.welt2000
    - aerofiles.xcsoar

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Turbo87/aerofiles/
  license: MIT License
  summary: 'waypoint, task, tracklog readers and writers for aviation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
