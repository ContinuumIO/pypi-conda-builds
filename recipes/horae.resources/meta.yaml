package:
  name: horae.resources
  version: "1.0a1"

source:
  fn: horae.resources-1.0a1.tar.gz
  url: https://pypi.python.org/packages/source/h/horae.resources/horae.resources-1.0a1.tar.gz
  md5: 961dc382f267b955a23921fabea142eb
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - horae.resources = horae.resources:main
    #
    # Would create an entry point called horae.resources that calls horae.resources.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - grok
    - z3c.relationfield
    - hurry.query
    - horae.auth
    - horae.core
    - horae.groupselect
    - horae.layout
    - horae.properties
    - horae.ticketing
    - horae.timeaware

  run:
    - python
    - setuptools
    - setuptools
    - grok
    - z3c.relationfield
    - hurry.query
    - horae.auth
    - horae.core
    - horae.groupselect
    - horae.layout
    - horae.properties
    - horae.ticketing
    - horae.timeaware

test:
  # Python imports
  imports:
    - horae
    - horae.resources

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: GPL
  summary: 'Provides resource management functionality for the Horae resource planning system'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
