package:
  name: django_base_model
  version: "0.2.2"

source:
  fn: django_base_model-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django_base_model/django_base_model-0.2.2.tar.gz
  md5: d19a95a4aaf0a9e645e52f366ea85c6f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_base_model = django_base_model:main
    #
    # Would create an entry point called django_base_model that calls django_base_model.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - django_base_model

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/rogeliorv/django_base_model
  license: MIT
  summary: 'A model from which all django models can extend. Gives some additional utility functions'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
