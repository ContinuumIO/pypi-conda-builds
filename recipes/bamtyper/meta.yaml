package:
  name: bamtyper
  version: "0.2.6"

source:
  fn: BamTyper-0.2.6.tar.gz
  url: https://pypi.python.org/packages/source/B/BamTyper/BamTyper-0.2.6.tar.gz
  md5: 369d1b7ba2d64cc0db19c4549ed1059a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bamtyper = bamtyper:main
    #
    # Would create an entry point called bamtyper that calls bamtyper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - pysam >=0.6

  run:
    - python
    - pysam >=0.6

test:
  # Python imports
  imports:
    - bamtyper
    - bamtyper.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/BamTyper/
  license: GPLv3
  summary: 'Working with paired reads in BAM format'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
