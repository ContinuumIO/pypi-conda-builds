package:
  name: birdback
  version: "0.2"

source:
  fn: birdback-0.2.tar.gz
  url: https://pypi.python.org/packages/source/b/birdback/birdback-0.2.tar.gz
  md5: e065ff39e5e65b6c175418202e4da371
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - birdback = birdback:main
    #
    # Would create an entry point called birdback that calls birdback.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=1.1

  run:
    - python
    - requests >=1.1

test:
  # Python imports
  imports:
    - birdback

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Birdback/python-birdback.git
  license: MIT License
  summary: 'Birdback API client.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
