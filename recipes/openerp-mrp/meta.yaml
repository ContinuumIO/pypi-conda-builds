package:
  name: openerp-mrp
  version: "7.0.406"

source:
  fn: openerp-mrp-7.0.406.tar.gz
  url: https://pypi.python.org/packages/source/o/openerp-mrp/openerp-mrp-7.0.406.tar.gz
  md5: ddf2ecbbfd574619b0cc9c9ad995f0e6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - openerp-mrp = openerp-mrp:main
    #
    # Would create an entry point called openerp-mrp that calls openerp-mrp.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - openerp-product
    - openerp-procurement
    - openerp-stock
    - openerp-resource
    - openerp-purchase
    - openerp-process

  run:
    - python
    - openerp-product
    - openerp-procurement
    - openerp-stock
    - openerp-resource
    - openerp-purchase
    - openerp-process

test:
  # Python imports
  imports:
    - openerp.addons.mrp
    - openerp.addons.mrp.report
    - openerp.addons.mrp.tests
    - openerp.addons.mrp.wizard

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.openerp.com
  license: GNU Affero General Public License v3
  summary: 'MRP'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
