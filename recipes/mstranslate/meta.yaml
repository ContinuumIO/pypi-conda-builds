package:
  name: mstranslate
  version: "1.1"

source:
  fn: mstranslate-1.1.zip
  url: https://pypi.python.org/packages/source/m/mstranslate/mstranslate-1.1.zip
  md5: 4ed8e9733fc8af9a005a91053744a964
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mstranslate = mstranslate:main
    #
    # Would create an entry point called mstranslate that calls mstranslate.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - requests >=2.2.1

  run:
    - python
    - requests >=2.2.1

test:
  # Python imports
  imports:
    - mstranslate

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bebound/Python-Microsoft-Translate-API
  license: UNKNOWN
  summary: 'Microsoft(Bing) translate API for python3'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
