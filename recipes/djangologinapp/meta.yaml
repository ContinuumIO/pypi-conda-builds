package:
  name: djangologinapp
  version: "0.1.2"

source:
  fn: djangologinapp-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/djangologinapp/djangologinapp-0.1.2.tar.gz
  md5: cc1e121b728d41e7ffbc045da98a460c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangologinapp = djangologinapp:main
    #
    # Would create an entry point called djangologinapp that calls djangologinapp.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests
    - mongoengine

  run:
    - python
    - requests
    - mongoengine

test:
  # Python imports
  imports:
    - djangologinapp

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/abdullatheef/djangologinapp
  license: BSD License
  summary: 'A simple Django app to login and also with mongoengine as User backend.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
