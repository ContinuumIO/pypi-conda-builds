package:
  name: demopackage
  version: "0.0.1"

source:
  fn: DemoPackage-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/D/DemoPackage/DemoPackage-0.0.1.tar.gz
  md5: 497b85224b851ebb9609e8433f990d3b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - demopackage = demopackage:main
    #
    # Would create an entry point called demopackage that calls demopackage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - DemoPackage
    - DemoPackage.tests
    - demopackage

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://sb2nov.github.io/DemoPackage
  license: BSD License
  summary: 'QuantSoftware Toolkit'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
