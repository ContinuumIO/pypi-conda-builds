package:
  name: alchemize
  version: "0.0.1"

source:
  fn: alchemize-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/a/alchemize/alchemize-0.0.1.tar.gz
  md5: c94edeb5417639e6e72374ecb0c76db9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - alchemize = alchemize:main
    #
    # Would create an entry point called alchemize that calls alchemize.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six

  run:
    - python
    - six

test:
  # Python imports
  imports:
    - alchemize

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jmvrbanac/alchemize
  license: Apache Software License
  summary: 'A simple library that allows for serialization and deserialization of models via mapping definitions'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
