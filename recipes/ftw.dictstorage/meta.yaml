package:
  name: ftw.dictstorage
  version: "1.2"

source:
  fn: ftw.dictstorage-1.2.tar.gz
  url: https://pypi.python.org/packages/source/f/ftw.dictstorage/ftw.dictstorage-1.2.tar.gz
  md5: 3ac981e8d1413b59f3a4eca083d5eb7c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ftw.dictstorage = ftw.dictstorage:main
    #
    # Would create an entry point called ftw.dictstorage that calls ftw.dictstorage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zope.component
    - zope.configuration
    - zope.security

  run:
    - python
    - setuptools
    - setuptools
    - zope.component
    - zope.configuration
    - zope.security

test:
  # Python imports
  imports:
    - ftw
    - ftw.dictstorage

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/4teamwork/ftw.dictstorage/
  license: GPL2
  summary: 'Provides a layer for storing key / value paires.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
