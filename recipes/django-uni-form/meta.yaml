package:
  name: django-uni-form
  version: "0.9.0"

source:
  fn: django-uni-form-0.9.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-uni-form/django-uni-form-0.9.0.tar.gz
  md5: 6d8c0bff28eab77829134392d5ff3021
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-uni-form = django-uni-form:main
    #
    # Would create an entry point called django-uni-form that calls django-uni-form.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - uni_form
    - uni_form.templatetags
    - uni_form.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/pydanny/django-uni-form
  license: BSD License
  summary: 'django-uni-form'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
