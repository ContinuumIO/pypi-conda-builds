package:
  name: django-braces
  version: "1.8.0"

source:
  fn: django-braces-1.8.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-braces/django-braces-1.8.0.tar.gz
  md5: 01daab863c1528ece4520fad66c0ccc8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-braces = django-braces:main
    #
    # Would create an entry point called django-braces that calls django-braces.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six

  run:
    - python
    - six

test:
  # Python imports
  imports:
    - braces

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/brack3t/django-braces/
  license: BSD
  summary: 'Reusable, generic mixins for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
