package:
  name: plone.formwidget.captcha
  version: "1.0.2"

source:
  fn: plone.formwidget.captcha-1.0.2.zip
  url: https://pypi.python.org/packages/source/p/plone.formwidget.captcha/plone.formwidget.captcha-1.0.2.zip
  md5: e4ab626134aa819315ef3dfddaee9b3f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.formwidget.captcha = plone.formwidget.captcha:main
    #
    # Would create an entry point called plone.formwidget.captcha that calls plone.formwidget.captcha.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - skimpygimpy
    - plone.keyring
    - plone.z3cform

  run:
    - python
    - setuptools
    - setuptools
    - skimpygimpy
    - plone.keyring
    - plone.z3cform

test:
  # Python imports
  imports:
    - plone
    - plone.formwidget
    - plone.formwidget.captcha
    - plone.formwidget.captcha.browser
    - plone.formwidget.captcha.demo
    - plone.formwidget.captcha.locales

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/plone.formwidget.captcha
  license: GNU General Public License (GPL)
  summary: 'Captcha widget for Plone.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
