package:
  name: django-seo-js
  version: "0.3.1"

source:
  fn: django-seo-js-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-seo-js/django-seo-js-0.3.1.tar.gz
  md5: 08b3ebd9340248842027382e27aaa225
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-seo-js = django-seo-js:main
    #
    # Would create an entry point called django-seo-js that calls django-seo-js.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests ==2.2.1

  run:
    - python
    - requests ==2.2.1

test:
  # Python imports
  imports:
    - django_seo_js
    - django_seo_js.backends
    - django_seo_js.middleware
    - django_seo_js.templatetags
    - django_seo_js.tests
    - django_seo_js.tests.backends

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/greenkahuna/django-seo-js
  license: BSD License
  summary: 'SEO support for angular, backbone, ember, famo.us, and other SPA apps built with django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
