package:
  name: jagss
  version: "0.0.3"

source:
  fn: jagss-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/j/jagss/jagss-0.0.3.tar.gz
  md5: 9e76a2782b3c05b9aa72d236d81b625c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jagss = jagss:main
    #
    # Would create an entry point called jagss that calls jagss.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - jinja2 >=2.0
    - markdown2 >=2.0
    - pyyaml >=3.10
    - watchdog >=0.6
    - paramiko >=1.12.0
    - boto_rsync >=0.8.1

  run:
    - python
    - jinja2 >=2.0
    - markdown2 >=2.0
    - pyyaml >=3.10
    - watchdog >=0.6
    - paramiko >=1.12.0
    - boto_rsync >=0.8.1

test:
  # Python imports
  imports:
    - jagss

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://jagss.rpy.xyz
  license: MIT
  summary: 'Just another generator for static sites'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
