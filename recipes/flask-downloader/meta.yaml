package:
  name: flask-downloader
  version: "0.2"

source:
  fn: Flask-Downloader-0.2.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Downloader/Flask-Downloader-0.2.tar.gz
  md5: ac241e2d9a583a476c50361f795dfb66
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-downloader = flask-downloader:main
    #
    # Would create an entry point called flask-downloader that calls flask-downloader.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask

  run:
    - python
    - flask

test:
  # Python imports
  imports:
    - flaskext

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kblin/flask-downloader
  license: BSD License
  summary: 'Allow a Flask web app to download files on behalf of the user.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
