package:
  name: django-ldap-group-mapper
  version: "0.1.2"

source:
  fn: django-ldap-group-mapper-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-ldap-group-mapper/django-ldap-group-mapper-0.1.2.tar.gz
  md5: 1bf3f9e214cf36cc265a55ae90ce77d5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-ldap-group-mapper = django-ldap-group-mapper:main
    #
    # Would create an entry point called django-ldap-group-mapper that calls django-ldap-group-mapper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-auth-ldap >=1.2.0
    - python-ldap >=2.0
    - django >=1.3

  run:
    - python
    - django-auth-ldap >=1.2.0
    - python-ldap >=2.0
    - django >=1.3

test:
  # Python imports
  imports:
    - ldap_groups

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/musashiXXX/django-ldap-group-mapper
  license: LICENSE.txt
  summary: 'Django LDAP Group Mapper'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
