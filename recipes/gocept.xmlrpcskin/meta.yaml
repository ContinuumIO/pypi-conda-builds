package:
  name: gocept.xmlrpcskin
  version: "1.0"

source:
  fn: gocept.xmlrpcskin-1.0.tar.gz
  url: https://pypi.python.org/packages/source/g/gocept.xmlrpcskin/gocept.xmlrpcskin-1.0.tar.gz
  md5: 9068e2e17a76b06faf0bec05a4542069
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gocept.xmlrpcskin = gocept.xmlrpcskin:main
    #
    # Would create an entry point called gocept.xmlrpcskin that calls gocept.xmlrpcskin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.app.publisher
    - zope.component[zcml]
    - zope.configuration
    - zope.interface
    - zope.publisher >=3.6.0
    - zope.security
    - zope.traversing

  run:
    - python
    - setuptools
    - zope.app.publisher
    - zope.component[zcml]
    - zope.configuration
    - zope.interface
    - zope.publisher >=3.6.0
    - zope.security
    - zope.traversing

test:
  # Python imports
  imports:
    - gocept
    - gocept.xmlrpcskin
    - gocept.xmlrpcskin.tests
    - gocept.xmlrpcskin.tests.fixture

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/gocept.xmlrpcskin
  license: ZPL
  summary: 'An extension to ``zope.publisher`` that provides a ZCML\ndirective for XML-RPC views that supports a ``layer`` parameter.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
