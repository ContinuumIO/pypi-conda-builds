package:
  name: fuglu
  version: "0.6.3"

source:
  fn: fuglu-0.6.3.tar.gz
  url: https://pypi.python.org/packages/source/f/fuglu/fuglu-0.6.3.tar.gz
  md5: 2eb0c09f7474693310ccbf148d21fc73
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fuglu = fuglu:main
    #
    # Would create an entry point called fuglu that calls fuglu.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - fuglu
    - fuglu.connectors
    - fuglu.extensions
    - fuglu.lib
    - fuglu.plugins

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.fuglu.org
  license: Apache Software License
  summary: 'Fuglu Mail Content Scanner'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
