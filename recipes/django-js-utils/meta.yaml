package:
  name: django-js-utils
  version: "0.0.5dev"

source:
  fn: django-js-utils-0.0.5dev.tar.gz
  url: https://pypi.python.org/packages/source/d/django-js-utils/django-js-utils-0.0.5dev.tar.gz
  md5: dbf2c96d4d913bd470d424cc4b27c1ed
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-js-utils = django-js-utils:main
    #
    # Would create an entry point called django-js-utils that calls django-js-utils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - django_js_utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD License
  summary: 'Utility library to help development of RIA on top of a Django backend'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
