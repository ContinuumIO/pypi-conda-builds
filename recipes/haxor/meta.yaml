package:
  name: haxor
  version: "0.2.2"

source:
  fn: haxor-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/h/haxor/haxor-0.2.2.tar.gz
  md5: 15b5ae35e21e40e73a8cff2cdf2b6d9f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - haxor = haxor:main
    #
    # Would create an entry point called haxor that calls haxor.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests ==2.4.3

  run:
    - python
    - requests ==2.4.3

test:
  # Python imports
  imports:
    - hackernews
    - haxor
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/avinassh/haxor/
  license: MIT License
  summary: 'Unofficial Python wrapper for Hacker News API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
