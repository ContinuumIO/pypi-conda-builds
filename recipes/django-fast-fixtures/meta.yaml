package:
  name: django-fast-fixtures
  version: "0.7"

source:
  fn: django-fast-fixtures-0.7.tar.gz
  url: https://pypi.python.org/packages/source/d/django-fast-fixtures/django-fast-fixtures-0.7.tar.gz
  md5: b9bc6617fb8b5e0a4301ac5c5becec1e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-fast-fixtures = django-fast-fixtures:main
    #
    # Would create an entry point called django-fast-fixtures that calls django-fast-fixtures.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.7.4

  run:
    - python
    - django >=1.7.4

test:
  # Python imports
  imports:
    - fast_fixtures
    - fast_fixtures.management
    - fast_fixtures.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/doctormo/django-fast-fixtures
  license: LGPLv3
  summary: 'Replace dumpdata and loaddata with migration aware fixture commands.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
