package:
  name: aiomcache
  version: "0.1"

source:
  fn: aiomcache-0.1.tar.gz
  url: https://pypi.python.org/packages/source/a/aiomcache/aiomcache-0.1.tar.gz
  md5: fc51c58adcc37fcb4fa357deab987cef
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - aiomcache = aiomcache:main
    #
    # Would create an entry point called aiomcache that calls aiomcache.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - asyncio

  run:
    - python
    - asyncio

test:
  # Python imports
  imports:
    - aiomcache

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/aio-libs/aiomcache/
  license: BSD License
  summary: 'Minimal pure python memcached client'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
