package:
  name: experimental.btree
  version: "1.1"

source:
  fn: experimental.btree-1.1.zip
  url: https://pypi.python.org/packages/source/e/experimental.btree/experimental.btree-1.1.zip
  md5: 3b6db9d6b73cd811214b135c407737ea
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - experimental.btree = experimental.btree:main
    #
    # Would create an entry point called experimental.btree that calls experimental.btree.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.extendedpathindex
    - products.zcatalog
    - zodb3

  run:
    - python
    - setuptools
    - setuptools
    - products.extendedpathindex
    - products.zcatalog
    - zodb3

test:
  # Python imports
  imports:
    - experimental
    - experimental.btree
    - experimental.btree.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.jarn.com/
  license: GPL
  summary: 'ZODB BTree optimizations'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
