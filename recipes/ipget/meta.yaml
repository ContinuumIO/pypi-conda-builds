package:
  name: ipget
  version: "0.1b"

source:
  fn: ipget-0.1b.tar.gz
  url: https://pypi.python.org/packages/source/i/ipget/ipget-0.1b.tar.gz
  md5: 0bd4d7d3e922544d109e73223813a4a4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ipget = ipget:main
    #
    # Would create an entry point called ipget that calls ipget.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - ipget

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://kitagami.org
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'This package is for those that know the local ip in linux.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
