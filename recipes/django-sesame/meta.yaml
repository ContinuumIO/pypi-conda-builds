package:
  name: django-sesame
  version: "1.1"

source:
  fn: django-sesame-1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-sesame/django-sesame-1.1.tar.gz
  md5: dfc6dc627f6d6b21114391ab9746ef4a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-sesame = django-sesame:main
    #
    # Would create an entry point called django-sesame that calls django-sesame.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - sesame

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/aaugustin/django-sesame
  license: BSD License
  summary: 'URL-based authentication, an application that provides one-click login via specially crafted URLs'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
