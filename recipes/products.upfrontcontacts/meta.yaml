package:
  name: products.upfrontcontacts
  version: "0.6"

source:
  fn: Products.UpfrontContacts-0.6.tar.gz
  url: https://pypi.python.org/packages/source/P/Products.UpfrontContacts/Products.UpfrontContacts-0.6.tar.gz
  md5: 9bdfd255c8bf7e03e13d92043edef525
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - products.upfrontcontacts = products.upfrontcontacts:main
    #
    # Would create an entry point called products.upfrontcontacts that calls products.upfrontcontacts.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - Products
    - Products.UpfrontContacts
    - Products.UpfrontContacts.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: GPL
  summary: 'Manage organisations, people and the relationships between them inside Plone and allow them to log in easily to your Plone site.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
