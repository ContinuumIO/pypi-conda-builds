package:
  name: django-cache-manager
  version: "0.1.5"

source:
  fn: django-cache-manager-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-cache-manager/django-cache-manager-0.1.5.tar.gz
  md5: dfe5109a4fd026273f015329287f2c0c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-cache-manager = django-cache-manager:main
    #
    # Would create an entry point called django-cache-manager that calls django-cache-manager.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django <1.8

  run:
    - python
    - django <1.8

test:
  # Python imports
  imports:
    - django_cache_manager
    - django_cache_manager.model_cache_sharing
    - django_cache_manager.model_cache_sharing.backends

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/vijaykatam/django-cache-manager
  license: UNKNOWN
  summary: 'Cache manager for django models'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
