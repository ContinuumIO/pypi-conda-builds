package:
  name: redturtle.entiterritoriali
  version: "0.3.3"

source:
  fn: redturtle.entiterritoriali-0.3.3.zip
  url: https://pypi.python.org/packages/source/r/redturtle.entiterritoriali/redturtle.entiterritoriali-0.3.3.zip
  md5: 006d77f3cca940ffea85033bf4570d29
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - redturtle.entiterritoriali = redturtle.entiterritoriali:main
    #
    # Would create an entry point called redturtle.entiterritoriali that calls redturtle.entiterritoriali.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - pysqlite
    - sqlsoup
    - sqlalchemy >=0.8

  run:
    - python
    - setuptools
    - setuptools
    - pysqlite
    - sqlsoup
    - sqlalchemy >=0.8

test:
  # Python imports
  imports:
    - redturtle
    - redturtle.entiterritoriali
    - redturtle.entiterritoriali.tests
    - redturtle.entiterritoriali.vocabulary

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/redturtle.entiterritoriali
  license: GPL
  summary: 'Package contatins all italian enti territoriali in sqlite db'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
