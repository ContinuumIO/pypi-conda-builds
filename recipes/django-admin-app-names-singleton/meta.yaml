package:
  name: django-admin-app-names-singleton
  version: "1.1"

source:
  fn: django-admin-app-names-singleton-1.1.zip
  url: https://pypi.python.org/packages/source/d/django-admin-app-names-singleton/django-admin-app-names-singleton-1.1.zip
  md5: b86814d9a00a531118c4ca0bc15634a1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-admin-app-names-singleton = django-admin-app-names-singleton:main
    #
    # Would create an entry point called django-admin-app-names-singleton that calls django-admin-app-names-singleton.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_singleton_app_name

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/pylior/django-admin-app-names-singleton
  license: MIT License
  summary: 'Django admin enhancer'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
