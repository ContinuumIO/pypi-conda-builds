package:
  name: tornado_thumbor_url
  version: "0.1.1"

source:
  fn: tornado_thumbor_url-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/t/tornado_thumbor_url/tornado_thumbor_url-0.1.1.tar.gz
  md5: abe8848d938513e9ab37bbdd4fa118e8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tornado_thumbor_url = tornado_thumbor_url:main
    #
    # Would create an entry point called tornado_thumbor_url that calls tornado_thumbor_url.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - libthumbor
    - tornado

  run:
    - python
    - libthumbor
    - tornado

test:
  # Python imports
  imports:
    - tornado_thumbor_url

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/rafaelcaricio/tornado-thumbor-url
  license: MIT License
  summary: 'tornado-thumbor-url is a python extension to tornado for encrypted thumbor url generation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
