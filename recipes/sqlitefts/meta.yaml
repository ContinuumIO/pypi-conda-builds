package:
  name: sqlitefts
  version: "0.2"

source:
  fn: sqlitefts-0.2.tar.gz
  url: https://pypi.python.org/packages/source/s/sqlitefts/sqlitefts-0.2.tar.gz
  md5: ddd004cf7ae542ef7a0d486ddda6093e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sqlitefts = sqlitefts:main
    #
    # Would create an entry point called sqlitefts that calls sqlitefts.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - sqlitefts

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/hideaki-t/sqlite-fts-python/
  license: MIT License
  summary: 'A Python binding for tokenizers of SQLite Full Text Search'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
