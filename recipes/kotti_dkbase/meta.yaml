package:
  name: kotti_dkbase
  version: "0.1.0b2"

source:
  fn: kotti_dkbase-0.1.0b2.tar.gz
  url: https://pypi.python.org/packages/source/k/kotti_dkbase/kotti_dkbase-0.1.0b2.tar.gz
  md5: 2ce07bd6a2298f104e55ccca42cd6ed2
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kotti_dkbase = kotti_dkbase:main
    #
    # Would create an entry point called kotti_dkbase that calls kotti_dkbase.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - kotti >=0.7
    - kotti_contactform >=0.1.0
    - pyramid_zcml
    - zope.browserresource
    - psycopg2
    - pyramid >=1.3

  run:
    - python
    - setuptools
    - kotti >=0.7
    - kotti_contactform >=0.1.0
    - pyramid_zcml
    - zope.browserresource
    - psycopg2
    - pyramid >=1.3

test:
  # Python imports
  imports:
    - kotti_dkbase

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/chrneumann/kotti_dkbase
  license: BSD License
  summary: 'Kotti setup and modifications for CMS projects of Datenkarussell.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
