package:
  name: coopr
  version: "4.0.9597"

source:
  fn: Coopr-4.0.9597.tar.gz
  url: https://pypi.python.org/packages/source/C/Coopr/Coopr-4.0.9597.tar.gz
  md5: b25927f0067c71659e1e59d5163a19a3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - coopr = coopr:main
    #
    # Would create an entry point called coopr that calls coopr.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyomo

  run:
    - python
    - pyomo

test:
  # Python imports
  imports:
    - coopr
    - coopr.pyomo

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://software.sandia.gov/coopr
  license: BSD License
  summary: 'Coopr: a COmmon Optimization Python Repository'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
