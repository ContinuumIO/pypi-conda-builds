package:
  name: flask-bleach
  version: "0.0.2"

source:
  fn: Flask-Bleach-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Bleach/Flask-Bleach-0.0.2.tar.gz
  md5: 1f5bd5e20b58ec030dbccf501e54b082
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-bleach = flask-bleach:main
    #
    # Would create an entry point called flask-bleach that calls flask-bleach.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - bleach

  run:
    - python
    - flask
    - bleach

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD License
  summary: 'Easy integration of bleach'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
