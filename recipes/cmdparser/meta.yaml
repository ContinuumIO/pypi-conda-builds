package:
  name: cmdparser
  version: "1.0.1"

source:
  fn: cmdparser-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/cmdparser/cmdparser-1.0.1.tar.gz
  md5: c8c65aec080367a0928cb23cf0d90b43
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cmdparser = cmdparser:main
    #
    # Would create an entry point called cmdparser that calls cmdparser.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - cmdparser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://cartroo.github.com/ttrack/cmdparser.html
  license: MIT License
  summary: 'Command parsing extensions to the cmd module.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
