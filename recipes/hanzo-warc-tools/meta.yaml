package:
  name: hanzo-warc-tools
  version: "0.2"

source:
  fn: hanzo-warc-tools-0.2.tar.gz
  url: https://pypi.python.org/packages/source/h/hanzo-warc-tools/hanzo-warc-tools-0.2.tar.gz
  md5: f04a0ea21f1121f872edf14b2c5911ca
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hanzo-warc-tools = hanzo-warc-tools:main
    #
    # Would create an entry point called hanzo-warc-tools that calls hanzo-warc-tools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - hanzo.httptools
    - hanzo.warctools

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT License
  summary: 'Command line tools and libraries for handling and manipulating WARC files (and HTTP contents)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
