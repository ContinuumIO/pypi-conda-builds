package:
  name: hirefire
  version: "0.3"

source:
  fn: HireFire-0.3.tar.gz
  url: https://pypi.python.org/packages/source/H/HireFire/HireFire-0.3.tar.gz
  md5: 9de000cdf410ceaf63a1a69cb378b755
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hirefire = hirefire:main
    #
    # Would create an entry point called hirefire that calls hirefire.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six

  run:
    - python
    - six

test:
  # Python imports
  imports:
    - hirefire
    - hirefire.contrib
    - hirefire.contrib.django
    - hirefire.contrib.flask
    - hirefire.contrib.tornado
    - hirefire.procs

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://hirefire.readthedocs.org/
  license: BSD License
  summary: 'A Python lib to integrate with the HireFire service -- The Heroku Proccess Manager'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
