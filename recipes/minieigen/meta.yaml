package:
  name: minieigen
  version: "0.4-1"

source:
  fn: minieigen-0.4-1.tar.gz
  url: https://pypi.python.org/packages/source/m/minieigen/minieigen-0.4-1.tar.gz
  md5: 883c251c6a85fd78a32a76c13d2e9828
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - minieigen = minieigen:main
    #
    # Would create an entry point called minieigen that calls minieigen.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - minieigen

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.launchpad.net/minieigen
  license: GNU Lesser General Public License v3 (LGPLv3)
  summary: 'Wrap parts of Eigen3, c++ library for basic math and geometry.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
