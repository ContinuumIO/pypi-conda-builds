package:
  name: hypnoapnswrapper
  version: "0.3-7"

source:
  fn: HypnoAPNSWrapper-0.3-7.tar.gz
  url: https://pypi.python.org/packages/source/H/HypnoAPNSWrapper/HypnoAPNSWrapper-0.3-7.tar.gz
  md5: d1a6242f0be28594ac9fb6672888ee02
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hypnoapnswrapper = hypnoapnswrapper:main
    #
    # Would create an entry point called hypnoapnswrapper that calls hypnoapnswrapper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - docutils >=0.3

  run:
    - python
    - docutils >=0.3

test:
  # Python imports
  imports:
    - APNSWrapper
    - hypnoapnswrapper

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/apns-python-wrapper/
  license: ALv2
  summary: 'This is wrapper for Apple Push Notification Service.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
