package:
  name: ftw.statusmap
  version: "1.1.1"

source:
  fn: ftw.statusmap-1.1.1.zip
  url: https://pypi.python.org/packages/source/f/ftw.statusmap/ftw.statusmap-1.1.1.zip
  md5: 4987e32f6b35bce381f54988aebe12ee
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ftw.statusmap = ftw.statusmap:main
    #
    # Would create an entry point called ftw.statusmap that calls ftw.statusmap.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.cmfcore
    - products.statusmessages
    - zope2
    - zope.i18nmessageid
    - zope.publisher

  run:
    - python
    - setuptools
    - setuptools
    - products.cmfcore
    - products.statusmessages
    - zope2
    - zope.i18nmessageid
    - zope.publisher

test:
  # Python imports
  imports:
    - ftw
    - ftw.statusmap
    - ftw.statusmap.browser
    - ftw.statusmap.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/4teamwork/ftw.statusmap
  license: GPL2
  summary: 'A plone view listing objects and review states in a tree.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
