package:
  name: django_shortener
  version: "0.3.0"

source:
  fn: django_shortener-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django_shortener/django_shortener-0.3.0.tar.gz
  md5: 927b546dfd21bc000dd2d0a58e10607a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_shortener = django_shortener:main
    #
    # Would create an entry point called django_shortener that calls django_shortener.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5.1
    - django-simple-captcha ==0.4.2

  run:
    - python
    - django >=1.5.1
    - django-simple-captcha ==0.4.2

test:
  # Python imports
  imports:
    - django_shortener
    - django_shortener.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/foxmask/django_shortener
  license: BSD License
  summary: 'URL Shortener System'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
