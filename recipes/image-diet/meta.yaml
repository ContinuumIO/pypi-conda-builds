package:
  name: image-diet
  version: "0.7.1"

source:
  fn: image-diet-0.7.1.tar.gz
  url: https://pypi.python.org/packages/source/i/image-diet/image-diet-0.7.1.tar.gz
  md5: 4904b1ce2c88b94b649009f9114ce84f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - image-diet = image-diet:main
    #
    # Would create an entry point called image-diet that calls image-diet.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3

  run:
    - python
    - django >=1.3

test:
  # Python imports
  imports:
    - image_diet
    - image_diet.management
    - image_diet.management.commands
    - image_diet.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/samastur/image-diet/
  license: MIT License
  summary: 'Remove unnecessary bytes from images'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
