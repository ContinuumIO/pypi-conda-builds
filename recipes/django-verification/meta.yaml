package:
  name: django-verification
  version: "0.4.0"

source:
  fn: django-verification-0.4.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-verification/django-verification-0.4.0.tar.gz
  md5: cccbc19e5c4421d643803d72c17023b3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-verification = django-verification:main
    #
    # Would create an entry point called django-verification that calls django-verification.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.7

  run:
    - python
    - django >=1.7

test:
  # Python imports
  imports:
    - verification

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kaleissin/django-verification
  license: MIT License
  summary: 'Generalized app for two-step verification'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
