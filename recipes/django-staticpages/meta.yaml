package:
  name: django-staticpages
  version: "0.2.3"

source:
  fn: django-staticpages-0.2.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-staticpages/django-staticpages-0.2.3.tar.gz
  md5: 8e2d2cbfbaefadc873c14f4b72095593
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-staticpages = django-staticpages:main
    #
    # Would create an entry point called django-staticpages that calls django-staticpages.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - staticpages
    - staticpages.templatetags
    - staticpages.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.org/gabrielgrant/django-staticpages/
  license: LGPL
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
