package:
  name: gcm-clerk
  version: "0.1.5"

source:
  fn: gcm-clerk-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/g/gcm-clerk/gcm-clerk-0.1.5.tar.gz
  md5: 464a127f6db895758c321ba0671938d1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gcm-clerk = gcm-clerk:main
    #
    # Would create an entry point called gcm-clerk that calls gcm-clerk.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - gcm_clerk

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/aleksihoffman/gcm-clerk
  license: Apache Software License
  summary: 'Python client for Google Cloud Messaging (GCM)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
