package:
  name: django-autotranslate
  version: "0.2"

source:
  fn: django-autotranslate-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-autotranslate/django-autotranslate-0.2.tar.gz
  md5: 12723a25be969703f07642007980ddf5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-autotranslate = django-autotranslate:main
    #
    # Would create an entry point called django-autotranslate that calls django-autotranslate.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - goslate

  run:
    - python
    - goslate

test:
  # Python imports
  imports:
    - autotranslate
    - autotranslate.management
    - autotranslate.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ankitpopli1891/django-autotranslate/
  license: MIT License
  summary: 'A simple Django app to autotranslate po files using google translate'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
