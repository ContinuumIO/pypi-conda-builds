package:
  name: jmbo-contact
  version: "2.0.0a2"

source:
  fn: jmbo-contact-2.0.0a2.tar.gz
  url: https://pypi.python.org/packages/source/j/jmbo-contact/jmbo-contact-2.0.0a2.tar.gz
  md5: fd049d55f13eaf5518efd0802939398f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jmbo-contact = jmbo-contact:main
    #
    # Would create an entry point called jmbo-contact that calls jmbo-contact.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-recaptcha
    - jmbo ==2.0.0a2

  run:
    - python
    - django-recaptcha
    - jmbo ==2.0.0a2

test:
  # Python imports
  imports:
    - contact
    - contact.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/praekelt/jmbo-contact
  license: BSD License
  summary: 'Jmbo contact form app.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
