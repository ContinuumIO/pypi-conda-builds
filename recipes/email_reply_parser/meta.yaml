package:
  name: email_reply_parser
  version: "0.3.0"

source:
  fn: email_reply_parser-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/e/email_reply_parser/email_reply_parser-0.3.0.tar.gz
  md5: 38dbee841d66ea4e5aa3ba2228303d2e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - email_reply_parser = email_reply_parser:main
    #
    # Would create an entry point called email_reply_parser that calls email_reply_parser.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - email_reply_parser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/zapier/email-reply-parser
  license: MIT
  summary: 'Email reply parser'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
