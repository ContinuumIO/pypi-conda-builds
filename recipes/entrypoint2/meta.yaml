package:
  name: entrypoint2
  version: "0.0.6"

source:
  fn: entrypoint2-0.0.6.tar.gz
  url: https://pypi.python.org/packages/source/e/entrypoint2/entrypoint2-0.0.6.tar.gz
  md5: ab268f561bb22471a83ca602c1921cbc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - entrypoint2 = entrypoint2:main
    #
    # Would create an entry point called entrypoint2 that calls entrypoint2.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - decorator
    - argparse

  run:
    - python
    - decorator
    - argparse

test:
  # Python imports
  imports:
    - entrypoint2
    - entrypoint2.examples

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ponty/entrypoint2
  license: BSD License
  summary: 'easy to use command-line interface for python modules, fork of entrypoint'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
