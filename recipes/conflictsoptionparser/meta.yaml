package:
  name: conflictsoptionparser
  version: "0.1.1"

source:
  fn: ConflictsOptionParser-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/C/ConflictsOptionParser/ConflictsOptionParser-0.1.1.tar.gz
  md5: 48bb59ed0d0bdfe4c095ee64b1a0f9ba
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - conflictsoptionparser = conflictsoptionparser:main
    #
    # Would create an entry point called conflictsoptionparser that calls conflictsoptionparser.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - conflictsoptionparser
    - conflictsparse
    - conflictsparse.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/ConvUtils/
  license: MIT License
  summary: 'A command line interface that recognizes conflicting options given as arguments.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
