package:
  name: ingranalyze
  version: "1.5.3"

source:
  fn: ingranalyze-1.5.3.tar.gz
  url: https://pypi.python.org/packages/source/i/ingranalyze/ingranalyze-1.5.3.tar.gz
  md5: 7279e5598f561bf3cdeeb51fdfadcddf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ingranalyze = ingranalyze:main
    #
    # Would create an entry point called ingranalyze that calls ingranalyze.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyasp ==1.3.3

  run:
    - python
    - pyasp ==1.3.3

test:
  # Python imports
  imports:
    - __ingranalyze__
    - ingranalyze

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/ingranalyze/
  license: GPLv3+
  summary: 'Influence graph analysis, consistency check, diagnosis, repair and prediction.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
