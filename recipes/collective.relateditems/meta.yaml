package:
  name: collective.relateditems
  version: "0.1.0"

source:
  fn: collective.relateditems-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.relateditems/collective.relateditems-0.1.0.tar.gz
  md5: 99cfa7cf98447e174bee73f65837b592
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.relateditems = collective.relateditems:main
    #
    # Would create an entry point called collective.relateditems that calls collective.relateditems.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone <4.0dev

  run:
    - python
    - setuptools
    - setuptools
    - plone <4.0dev

test:
  # Python imports
  imports:
    - collective
    - collective.relateditems

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/collective.relateditems
  license: GPL
  summary: 'Backporting Plone 4 related items viewlet into Plone 3'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
