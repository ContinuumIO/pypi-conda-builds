package:
  name: django-sphinxdoc
  version: "1.4.0"

source:
  fn: django-sphinxdoc-1.4.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-sphinxdoc/django-sphinxdoc-1.4.0.tar.gz
  md5: 37af36a704ec5108435b3f62b8c87d6d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-sphinxdoc = django-sphinxdoc:main
    #
    # Would create an entry point called django-sphinxdoc that calls django-sphinxdoc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4.2
    - django-haystack >=2.1

  run:
    - python
    - django >=1.4.2
    - django-haystack >=2.1

test:
  # Python imports
  imports:
    - sphinxdoc
    - sphinxdoc.management
    - sphinxdoc.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://stefan.sofa-rockers.org/django-sphinxdoc/
  license: MIT License
  summary: 'Easily integrate Sphinx documentation into your website.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
