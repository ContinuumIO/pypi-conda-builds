package:
  name: ferrisnose
  version: "2.3.1"

source:
  fn: FerrisNose-2.3.1.tar.gz
  url: https://pypi.python.org/packages/source/F/FerrisNose/FerrisNose-2.3.1.tar.gz
  md5: 3f63a19473f22e450ac8fe77975b51e7
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ferrisnose = ferrisnose:main
    #
    # Would create an entry point called ferrisnose that calls ferrisnose.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - nose >=1.3.0
    - google-api-python-client >=1.2.0
    - oauth2client >=1.2.0
    - webtest
    - protorpc >=0.9.0

  run:
    - python
    - setuptools
    - nose >=1.3.0
    - google-api-python-client >=1.2.0
    - oauth2client >=1.2.0
    - webtest
    - protorpc >=0.9.0

test:
  # Python imports
  imports:
    - ferrisnose

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jonparrott/FerrisNose
  license: Apache Software License
  summary: 'Nose plugin for testing Google App Engine application. Designed for the Ferris Framework but should work for any App Engine app.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
