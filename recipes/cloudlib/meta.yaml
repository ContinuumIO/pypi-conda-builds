package:
  name: cloudlib
  version: "0.4.0"

source:
  fn: cloudlib-0.4.0.tar.gz
  url: https://pypi.python.org/packages/source/c/cloudlib/cloudlib-0.4.0.tar.gz
  md5: 832d8ef8d3ba64e00696c88f0758a901
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cloudlib = cloudlib:main
    #
    # Would create an entry point called cloudlib that calls cloudlib.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - prettytable >=0.7.0
    - requests >=2.2.0

  run:
    - python
    - prettytable >=0.7.0
    - requests >=2.2.0

test:
  # Python imports
  imports:
    - cloudlib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/cloudnull/cloudlib
  license: Apache Software License
  summary: 'general purpose library for in application use'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
