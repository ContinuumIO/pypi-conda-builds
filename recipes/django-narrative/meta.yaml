package:
  name: django-narrative
  version: "0.5.9"

source:
  fn: django-narrative-0.5.9.tar.gz
  url: https://pypi.python.org/packages/source/d/django-narrative/django-narrative-0.5.9.tar.gz
  md5: f2ddf4598f69fbcbdf19e5ab29e8c269
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-narrative = django-narrative:main
    #
    # Would create an entry point called django-narrative that calls django-narrative.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4
    - django-manager-utils >=0.3.6
    - django-tastypie >=0.11.0
    - pytz >=2012h

  run:
    - python
    - django >=1.4
    - django-manager-utils >=0.3.6
    - django-tastypie >=0.11.0
    - pytz >=2012h

test:
  # Python imports
  imports:
    - narrative
    - narrative.batteries
    - narrative.management
    - narrative.management.commands
    - narrative.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/
  license: UNKNOWN
  summary: 'Django narrative'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
