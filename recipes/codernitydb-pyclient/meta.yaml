package:
  name: codernitydb-pyclient
  version: "0.4.1"

source:
  fn: CodernityDB-PyClient-0.4.1.tar.gz
  url: https://pypi.python.org/packages/source/C/CodernityDB-PyClient/CodernityDB-PyClient-0.4.1.tar.gz
  md5: 22cf7ae4306e2e8d4fb1d2d08f368d7e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - codernitydb-pyclient = codernitydb-pyclient:main
    #
    # Would create an entry point called codernitydb-pyclient that calls codernitydb-pyclient.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=1.0.3

  run:
    - python
    - requests >=1.0.3

test:
  # Python imports
  imports:
    - CodernityDBPyClient

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://codernity.com
  license: Apache Software License
  summary: 'Client library for CodernityDB-HTTP'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
