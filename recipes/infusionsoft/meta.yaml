package:
  name: infusionsoft
  version: "0.2.0"

source:
  fn: infusionsoft-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/i/infusionsoft/infusionsoft-0.2.0.tar.gz
  md5: 76189e9b96cb6cf7ba4fba79d00b7bc7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - infusionsoft = infusionsoft:main
    #
    # Would create an entry point called infusionsoft that calls infusionsoft.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - infusionsoft

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/realgeeks/infusionsoft
  license: MIT License
  summary: 'A client library for the InfusionSoft API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
