package:
  name: collective.flowplayer
  version: "4.2.1"

source:
  fn: collective.flowplayer-4.2.1.zip
  url: https://pypi.python.org/packages/source/c/collective.flowplayer/collective.flowplayer-4.2.1.zip
  md5: 256ed6b9d50b28516c047d1f6ee1eea6
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.flowplayer = collective.flowplayer:main
    #
    # Would create an entry point called collective.flowplayer that calls collective.flowplayer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.jquerytools
    - hachoir_core
    - hachoir_metadata
    - hachoir_parser
    - archetypes.schemaextender

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.jquerytools
    - hachoir_core
    - hachoir_metadata
    - hachoir_parser
    - archetypes.schemaextender

test:
  # Python imports
  imports:
    - collective
    - collective.flowplayer
    - collective.flowplayer.browser
    - collective.flowplayer.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/collective-flowplayer
  license: GPL
  summary: 'A simple package using Flowplayer for video and audio content'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
