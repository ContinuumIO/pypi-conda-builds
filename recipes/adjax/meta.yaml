package:
  name: adjax
  version: "1.0.1"

source:
  fn: Adjax-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/A/Adjax/Adjax-1.0.1.tar.gz
  md5: b3a4b29246493624963a63d2638f8a2d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - adjax = adjax:main
    #
    # Would create an entry point called adjax that calls adjax.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.0

  run:
    - python
    - django >=1.0

test:
  # Python imports
  imports:
    - adjax
    - adjax.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://adjax.hardysoftware.com.au/
  license: New BSD
  summary: 'A framework for easing the development of Django sites with Ajax.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
