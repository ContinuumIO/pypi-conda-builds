package:
  name: django-custom-auth
  version: "0.1.3"

source:
  fn: django-custom-auth-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-custom-auth/django-custom-auth-0.1.3.tar.gz
  md5: eaa64149f414b274b5c7209b1ca39c56
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-custom-auth = django-custom-auth:main
    #
    # Would create an entry point called django-custom-auth that calls django-custom-auth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - custom_auth

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/endthestart/django-custom-auth
  license: BSD License
  summary: 'A simple Django app that replaces the default authentication'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
