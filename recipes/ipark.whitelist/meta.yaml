package:
  name: ipark.whitelist
  version: "0.2.5"

source:
  fn: ipark.whitelist-0.2.5.tar.gz
  url: https://pypi.python.org/packages/source/i/ipark.whitelist/ipark.whitelist-0.2.5.tar.gz
  md5: 714dc81646546281a9cfea7687c45f06
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ipark.whitelist = ipark.whitelist:main
    #
    # Would create an entry point called ipark.whitelist that calls ipark.whitelist.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - tipfy >=0.6,<0.7

  run:
    - python
    - tipfy >=0.6,<0.7

test:
  # Python imports
  imports:
    - ipark
    - ipark.whitelist

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/iparkcode/
  license: BSD License
  summary: 'Tipfy middleware extension to whitelist access by IP'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
