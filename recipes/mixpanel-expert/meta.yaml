package:
  name: mixpanel-expert
  version: "0.1.2"

source:
  fn: Mixpanel-Expert-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/M/Mixpanel-Expert/Mixpanel-Expert-0.1.2.tar.gz
  md5: 7624eecaa33cc8c9b04e8c19f393872d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mixpanel-expert = mixpanel-expert:main
    #
    # Would create an entry point called mixpanel-expert that calls mixpanel-expert.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - expert

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://mixpanel.com/
  license: LICENSE.txt
  summary: 'Mixpanel Tools and Utils'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
