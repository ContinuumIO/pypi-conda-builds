package:
  name: openexchangerates
  version: "0.1.1"

source:
  fn: openexchangerates-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/o/openexchangerates/openexchangerates-0.1.1.tar.gz
  md5: 1046efb1cf3be000a8132b1a141d0fc3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - openexchangerates = openexchangerates:main
    #
    # Would create an entry point called openexchangerates that calls openexchangerates.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - openexchangerates

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/metglobal/openexchangerates
  license: MIT License
  summary: 'openexchangerates.org python API client'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
