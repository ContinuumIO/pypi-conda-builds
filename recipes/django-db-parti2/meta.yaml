package:
  name: django-db-parti2
  version: "0.3.5"

source:
  fn: django-db-parti2-0.3.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-db-parti2/django-db-parti2-0.3.5.tar.gz
  md5: f9dcbbac899348b198c7969153c8b9d8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-db-parti2 = django-db-parti2:main
    #
    # Would create an entry point called django-db-parti2 that calls django-db-parti2.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4

  run:
    - python
    - django >=1.4

test:
  # Python imports
  imports:
    - dbparti
    - dbparti.backends
    - dbparti.backends.mysql
    - dbparti.backends.postgresql
    - dbparti.management
    - dbparti.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/maxtepkeev/django-db-parti
  license: BSD License
  summary: 'Fully automatic database table partitioning for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
