package:
  name: pystaticconfiguration
  version: "0.9.0"

source:
  fn: PyStaticConfiguration-0.9.0.tar.gz
  url: https://pypi.python.org/packages/source/P/PyStaticConfiguration/PyStaticConfiguration-0.9.0.tar.gz
  md5: dc6bcfbb6c7e3aa9132975fea709d4ac
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pystaticconfiguration = pystaticconfiguration:main
    #
    # Would create an entry point called pystaticconfiguration that calls pystaticconfiguration.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six

  run:
    - python
    - six

test:
  # Python imports
  imports:
    - pystaticconfiguration
    - staticconf

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/dnephin/PyStaticConfiguration
  license: Apache Software License
  summary: 'A python library for loading static configuration'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
