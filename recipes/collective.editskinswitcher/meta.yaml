package:
  name: collective.editskinswitcher
  version: "3.2"

source:
  fn: collective.editskinswitcher-3.2.zip
  url: https://pypi.python.org/packages/source/c/collective.editskinswitcher/collective.editskinswitcher-3.2.zip
  md5: 97ba9aec34134ba500a7c085dca0d00c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.editskinswitcher = collective.editskinswitcher:main
    #
    # Would create an entry point called collective.editskinswitcher that calls collective.editskinswitcher.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.editskinswitcher
    - collective.editskinswitcher.browser
    - collective.editskinswitcher.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.editskinswitcher
  license: GPL
  summary: 'Switch to the edit skin for certain domains.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
