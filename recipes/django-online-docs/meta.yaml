package:
  name: django-online-docs
  version: "0.8"

source:
  fn: django-online-docs-0.8.tar.gz
  url: https://pypi.python.org/packages/source/d/django-online-docs/django-online-docs-0.8.tar.gz
  md5: 336cdd8b0ce7af4933ddbbee1327c7c3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-online-docs = django-online-docs:main
    #
    # Would create an entry point called django-online-docs that calls django-online-docs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - online_docs
    - online_docs.templatetags
    - online_docs.tests
    - online_docs.tests.integration_tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bitmazk/django-online-docs
  license: The MIT License
  summary: 'A reusable Django app that shows documentation for the view that is currently\ndisplayed. The documentation is written in Markdown and gets displayed in\njQuery modals.\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
