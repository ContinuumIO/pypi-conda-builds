package:
  name: haufe.releaser
  version: "0.1.5"

source:
  fn: haufe.releaser-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/h/haufe.releaser/haufe.releaser-0.1.5.tar.gz
  md5: 44e411ff83ddec67d1ebe188aec55cf3
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - haufe.releaser = haufe.releaser:main
    #
    # Would create an entry point called haufe.releaser that calls haufe.releaser.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - haufe
    - haufe.releaser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: ZPL V2.1
  summary: 'Performs local upload of distribution files to a haufe.eggserver instance'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
