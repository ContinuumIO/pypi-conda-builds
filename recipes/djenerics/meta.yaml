package:
  name: djenerics
  version: "1.0.1"

source:
  fn: djenerics-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/djenerics/djenerics-1.0.1.tar.gz
  md5: 4d2da81398414e2d6629a8b4a5bdd0ab
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djenerics = djenerics:main
    #
    # Would create an entry point called djenerics that calls djenerics.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - djenerics

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://nrocco.github.io/
  license: djenesis
  summary: 'A collection of Django 1.5+ and Django Rest Framework 3.0+ utilities'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
