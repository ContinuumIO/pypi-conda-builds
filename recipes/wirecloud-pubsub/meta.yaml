package:
  name: wirecloud-pubsub
  version: "0.6.0b1"

source:
  fn: wirecloud-pubsub-0.6.0b1.tar.gz
  url: https://pypi.python.org/packages/source/w/wirecloud-pubsub/wirecloud-pubsub-0.6.0b1.tar.gz
  md5: d7f6bfd8131d3dc242fac02d3e3a35e8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - wirecloud-pubsub = wirecloud-pubsub:main
    #
    # Would create an entry point called wirecloud-pubsub that calls wirecloud-pubsub.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - wirecloud >=0.6.0b3

  run:
    - python
    - wirecloud >=0.6.0b3

test:
  # Python imports
  imports:
    - wirecloud_pubsub

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/conwetlab/wirecloud-pubsub
  license: European Union Public Licence 1.1 (EUPL 1.1)
  summary: 'Wirecloud plugin providing PubSub support.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
