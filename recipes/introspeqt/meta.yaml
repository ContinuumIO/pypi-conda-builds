package:
  name: introspeqt
  version: "0.0.2"

source:
  fn: introspeqt-0.0.2.zip
  url: https://pypi.python.org/packages/source/i/introspeqt/introspeqt-0.0.2.zip
  md5: 3218d0b88f33825606bfc7f98c908622
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - introspeqt = introspeqt:main
    #
    # Would create an entry point called introspeqt that calls introspeqt.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyside

  run:
    - python
    - pyside

test:
  # Python imports
  imports:
    - introspeqt

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/davidmartinezanim/introspeqt
  license: UNKNOWN
  summary: 'Simple introspector for PySide applications.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
