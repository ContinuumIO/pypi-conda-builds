package:
  name: hiringpond
  version: "0.1dev"

source:
  fn: hiringpond-0.1dev.tar.gz
  url: https://pypi.python.org/packages/source/h/hiringpond/hiringpond-0.1dev.tar.gz
  md5: da13d005fe20dcc4e9667b3b3eea821b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hiringpond = hiringpond:main
    #
    # Would create an entry point called hiringpond that calls hiringpond.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - turbogears2 >=2.1.1
    - genshi
    - zope.sqlalchemy >=0.4
    - repoze.tm2 >=1.0a5
    - sqlalchemy
    - sqlalchemy-migrate
    - repoze.what-quickstart
    - repoze.what >=1.0.8
    - repoze.what-quickstart
    - repoze.who-friendlyform >=1.0.4
    - repoze.what-pylons >=1.0
    - repoze.what.plugins.sql
    - repoze.who ==1.0.19
    - tgext.admin >=0.3.9
    - tw.forms

  run:
    - python
    - setuptools
    - turbogears2 >=2.1.1
    - genshi
    - zope.sqlalchemy >=0.4
    - repoze.tm2 >=1.0a5
    - sqlalchemy
    - sqlalchemy-migrate
    - repoze.what-quickstart
    - repoze.what >=1.0.8
    - repoze.what-quickstart
    - repoze.who-friendlyform >=1.0.4
    - repoze.what-pylons >=1.0
    - repoze.what.plugins.sql
    - repoze.who ==1.0.19
    - tgext.admin >=0.3.9
    - tw.forms

test:
  # Python imports
  imports:
    - hiringpond
    - hiringpond.config
    - hiringpond.controllers
    - hiringpond.lib
    - hiringpond.model
    - hiringpond.templates
    - hiringpond.tests
    - hiringpond.tests.functional
    - hiringpond.tests.models
    - hiringpond.websetup
    - migration
    - migration.versions

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
