package:
  name: sphinxtheme.readability
  version: "0.0.6"

source:
  fn: sphinxtheme.readability-0.0.6.tar.gz
  url: https://pypi.python.org/packages/source/s/sphinxtheme.readability/sphinxtheme.readability-0.0.6.tar.gz
  md5: 681a5b412a163504166f2c795b8a8921
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sphinxtheme.readability = sphinxtheme.readability:main
    #
    # Would create an entry point called sphinxtheme.readability that calls sphinxtheme.readability.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - docutils
    - sphinx

  run:
    - python
    - setuptools
    - docutils
    - sphinx

test:
  # Python imports
  imports:
    - sphinxtheme

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://sphinxtheme-readability.readthedocs.org/
  license: BSD License
  summary: 'Sphinx Readability Theme provides a comfortably reading environment for your documentation.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
