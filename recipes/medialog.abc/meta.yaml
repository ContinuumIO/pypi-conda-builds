package:
  name: medialog.abc
  version: "0.3.4"

source:
  fn: medialog.abc-0.3.4.tar.gz
  url: https://pypi.python.org/packages/source/m/medialog.abc/medialog.abc-0.3.4.tar.gz
  md5: 84d2efc48f260293b18764126ace4bd3
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - medialog.abc = medialog.abc:main
    #
    # Would create an entry point called medialog.abc that calls medialog.abc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - archetypes.schemaextender
    - archetypes.markerfield

  run:
    - python
    - setuptools
    - setuptools
    - archetypes.schemaextender
    - archetypes.markerfield

test:
  # Python imports
  imports:
    - medialog
    - medialog.abc

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://medialog.no
  license: GPL
  summary: 'Adds fields to images.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
