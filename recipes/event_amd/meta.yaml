package:
  name: event_amd
  version: "1.0.7"

source:
  fn: event_amd-1.0.7.tar.gz
  url: https://pypi.python.org/packages/source/e/event_amd/event_amd-1.0.7.tar.gz
  md5: 04190cda5f3208bc9e2edaa79b205374
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - event_amd = event_amd:main
    #
    # Would create an entry point called event_amd that calls event_amd.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - event_amd

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/borreykim/event_amd
  license: UNKNOWN
  summary: 'Port for EventEmitter from nodejs'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
