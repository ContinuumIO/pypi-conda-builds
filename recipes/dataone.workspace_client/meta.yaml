package:
  name: dataone.workspace_client
  version: "0.0.7"

source:
  fn: dataone.workspace_client-0.0.7.tar.gz
  url: https://pypi.python.org/packages/source/d/dataone.workspace_client/dataone.workspace_client-0.0.7.tar.gz
  md5: 0b4132df75b9211c1a3c43a494bfbdcb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dataone.workspace_client = dataone.workspace_client:main
    #
    # Would create an entry point called dataone.workspace_client that calls dataone.workspace_client.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - dataone.libclient ==1.2.6
    - requests ==2.2.1

  run:
    - python
    - dataone.libclient ==1.2.6
    - requests ==2.2.1

test:
  # Python imports
  imports:
    - d1_workspace
    - d1_workspace.types
    - d1_workspace.types.generated

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://dataone.org
  license: Apache License, Version 2.0
  summary: 'A DataONE Workspace client library for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
