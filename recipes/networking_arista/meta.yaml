package:
  name: networking_arista
  version: "2015.1.5"

source:
  fn: networking_arista-2015.1.5.tar.gz
  url: https://pypi.python.org/packages/source/n/networking_arista/networking_arista-2015.1.5.tar.gz
  md5: 9dc9a608f5875eb5bc4e8d4b36fcbbb5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - networking_arista = networking_arista:main
    #
    # Would create an entry point called networking_arista that calls networking_arista.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - networking_arista

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Apache Software License
  summary: 'Arista Networking drivers'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
