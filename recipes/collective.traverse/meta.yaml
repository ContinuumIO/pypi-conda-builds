package:
  name: collective.traverse
  version: "1.1"

source:
  fn: collective.traverse-1.1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.traverse/collective.traverse-1.1.tar.gz
  md5: 04d61c11d484977dd5c87960548d43fd
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.traverse = collective.traverse:main
    #
    # Would create an entry point called collective.traverse that calls collective.traverse.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.traverse

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.brainson.de/
  license: GPL
  summary: 'Traverse to Object by Interface'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
