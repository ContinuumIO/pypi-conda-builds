package:
  name: django-select2light
  version: "0.3"

source:
  fn: django-select2light-0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-select2light/django-select2light-0.3.tar.gz
  md5: af973927732af9ed3be236fad1a34841
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-select2light = django-select2light:main
    #
    # Would create an entry point called django-select2light that calls django-select2light.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4.2
    - django-floppyforms >=1.0
    - django-tastypie >=0.9

  run:
    - python
    - django >=1.4.2
    - django-floppyforms >=1.0
    - django-tastypie >=0.9

test:
  # Python imports
  imports:
    - select2light

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ouhouhsami/django-select2light
  license: Apache Software License
  summary: "Select2 widget 'light edition' for Django projects"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
