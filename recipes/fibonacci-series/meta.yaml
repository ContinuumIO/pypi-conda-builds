package:
  name: fibonacci-series
  version: "1.0.0"

source:
  fn: Fibonacci-Series-1.0.0.zip
  url: https://pypi.python.org/packages/source/F/Fibonacci-Series/Fibonacci-Series-1.0.0.zip
  md5: 395fbb23f76bf8a339f7f63bafb4ec14
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fibonacci-series = fibonacci-series:main
    #
    # Would create an entry point called fibonacci-series that calls fibonacci-series.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - fib1_fib2

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/fib1_fib2/
  license: LICENSE.txt
  summary: 'Useful Fibonacci Series'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
