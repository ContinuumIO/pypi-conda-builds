package:
  name: json-table-schema
  version: "0.1"

source:
  fn: json-table-schema-0.1.tar.gz
  url: https://pypi.python.org/packages/source/j/json-table-schema/json-table-schema-0.1.tar.gz
  md5: b577a3523dfb89cc47cd4282c5091fd1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - json-table-schema = json-table-schema:main
    #
    # Would create an entry point called json-table-schema that calls json-table-schema.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - jsontableschema

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: Apache Licence v2.0
  summary: 'Python library for dealing with JSON Table Schema files'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
