package:
  name: execconf
  version: "0.3.5"

source:
  fn: execconf-0.3.5.tar.gz
  url: https://pypi.python.org/packages/source/e/execconf/execconf-0.3.5.tar.gz
  md5: 00dde6661d0e8d800b9c26d1b16f851a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - execconf = execconf:main
    #
    # Would create an entry point called execconf that calls execconf.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - execconf
    - execconf.tests
    - execconf.validator

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/DriverX/execconf
  license: MIT License
  summary: 'Executable config on pure python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
