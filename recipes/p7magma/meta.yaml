package:
  name: p7magma
  version: "0.1.2"

source:
  fn: p7magma-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/p/p7magma/p7magma-0.1.2.tar.gz
  md5: 86578305e242adee376bde46afb7aa6e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - p7magma = p7magma:main
    #
    # Would create an entry point called p7magma that calls p7magma.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - beautifulsoup4 >=4.3.2
    - requests >=2.4.2

  run:
    - python
    - beautifulsoup4 >=4.3.2
    - requests >=2.4.2

test:
  # Python imports
  imports:
    - magma
    - p7magma

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bfontaine/p7magma
  license: MIT License
  summary: 'Magma Python interface'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
