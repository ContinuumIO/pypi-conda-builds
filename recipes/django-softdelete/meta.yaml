package:
  name: django-softdelete
  version: "0.3.4"

source:
  fn: django-softdelete-0.3.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-softdelete/django-softdelete-0.3.4.tar.gz
  md5: 89b576ffe119e1f21d50bb9757bcffaf
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-softdelete = django-softdelete:main
    #
    # Would create an entry point called django-softdelete that calls django-softdelete.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - softdelete
    - softdelete.admin
    - softdelete.migrations
    - softdelete.test_softdelete_app
    - softdelete.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/scoursen/django-softdelete
  license: BSD License
  summary: 'Soft delete support for Django ORM, with undelete.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
