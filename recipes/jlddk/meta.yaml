package:
  name: jlddk
  version: "0.7.37"

source:
  fn: jlddk-0.7.37.tar.gz
  url: https://pypi.python.org/packages/source/j/jlddk/jlddk-0.7.37.tar.gz
  md5: 21ebe6c188f4cb356898b4c29e5a8ba4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jlddk = jlddk:main
    #
    # Would create an entry point called jlddk that calls jlddk.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyfnc >=0.1.0
    - psutil
    - pyinotify
    - pyyaml >=3.10

  run:
    - python
    - pyfnc >=0.1.0
    - psutil
    - pyinotify
    - pyyaml >=3.10

test:
  # Python imports
  imports:
    - jlddk
    - jlddk.agents
    - jlddk.block
    - jlddk.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.systemical.com/doc/opensource/jlddk
  license: UNKNOWN
  summary: 'Collection of robots'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
