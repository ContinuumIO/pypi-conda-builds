package:
  name: garment
  version: "0.5"

source:
  fn: garment-0.5.tar.gz
  url: https://pypi.python.org/packages/source/g/garment/garment-0.5.tar.gz
  md5: fc7a5cda8eb3b478ce22eb3dc30e46ff
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - garment = garment:main
    #
    # Would create an entry point called garment that calls garment.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyyaml
    - fabric >=1.8,<=1.9
    - iso8601 >=0.1,<=0.2

  run:
    - python
    - setuptools
    - pyyaml
    - fabric >=1.8,<=1.9
    - iso8601 >=0.1,<=0.2

test:
  # Python imports
  imports:
    - garment

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/borgstrom/garment
  license: Apache Software License
  summary: 'A collection of fabric tasks that roll up into a single deploy function. The whole process is coordinated through a single deployment configuration file named deploy.conf'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
