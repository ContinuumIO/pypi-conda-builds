package:
  name: dispatcher
  version: "1.0"

source:
  fn: dispatcher-1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/dispatcher/dispatcher-1.0.tar.gz
  md5: c0ea7c5faa29b357c9aa505d5b15d668
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dispatcher = dispatcher:main
    #
    # Would create an entry point called dispatcher that calls dispatcher.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - dispatch
    - dispatcher

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/olivierverdier/dispatch
  license: BSD License
  summary: 'A library for event-driven programming'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
