package:
  name: oar-lib
  version: "0.1.1"

source:
  fn: oar-lib-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/o/oar-lib/oar-lib-0.1.1.tar.gz
  md5: c68e4119170c191bf9359d87f5264855
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - oar-lib = oar-lib:main
    #
    # Would create an entry point called oar-lib that calls oar-lib.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sqlalchemy

  run:
    - python
    - sqlalchemy

test:
  # Python imports
  imports:
    - oar
    - oar_lib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/oar-team/python-oar-lib
  license: GNU General Public License v2 or later (GPLv2+)
  summary: 'OAR common lib.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
