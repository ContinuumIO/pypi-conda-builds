package:
  name: cartman
  version: "0.2.2"

source:
  fn: cartman-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/c/cartman/cartman-0.2.2.tar.gz
  md5: 985e0c94e4a40a804257e073bb94b11e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cartman = cartman:main
    #
    # Would create an entry point called cartman that calls cartman.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=1.2.0

  run:
    - python
    - requests >=1.2.0

test:
  # Python imports
  imports:
    - cartman

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://tamentis.com/projects/cartman/
  license: ISC License (ISCL)
  summary: 'trac command-line tools'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
