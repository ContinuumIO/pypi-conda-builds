package:
  name: atompy
  version: "0.5.1.1"

source:
  fn: AtomPy-0.5.1.1.zip
  url: https://pypi.python.org/packages/source/A/AtomPy/AtomPy-0.5.1.1.zip
  md5: 11b4f77e34a3d9851536bd8ea7f7c347
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - atompy = atompy:main
    #
    # Would create an entry point called atompy that calls atompy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - xlrd >=0.9.2
    - google-api-python-client >=0.0.0
    - xlwt >=0.0.0
    - gdata >=0.0.0

  run:
    - python
    - xlrd >=0.9.2
    - google-api-python-client >=0.0.0
    - xlwt >=0.0.0
    - gdata >=0.0.0

test:
  # Python imports
  imports:
    - atompy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'A cloud atomic data service for astrophysical applications.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
