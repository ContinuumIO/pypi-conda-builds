package:
  name: js.uploadify
  version: "3.2.1"

source:
  fn: js.uploadify-3.2.1.tar.gz
  url: https://pypi.python.org/packages/source/j/js.uploadify/js.uploadify-3.2.1.tar.gz
  md5: c46b8a235cbe3537496a9fcabf5f216a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - js.uploadify = js.uploadify:main
    #
    # Would create an entry point called js.uploadify that calls js.uploadify.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fanstatic
    - js.jquery
    - setuptools

  run:
    - python
    - setuptools
    - fanstatic
    - js.jquery
    - setuptools

test:
  # Python imports
  imports:
    - js
    - js.uploadify

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tmassman/js.uploadify
  license: BSD
  summary: 'Fanstatic packaging of uploadify.js.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
