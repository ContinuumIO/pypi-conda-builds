package:
  name: haufe.sortabletable
  version: "0.2"

source:
  fn: haufe.sortabletable-0.2.tar.gz
  url: https://pypi.python.org/packages/source/h/haufe.sortabletable/haufe.sortabletable-0.2.tar.gz
  md5: 0759884340e9dd8fe137830333a5c043
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - haufe.sortabletable = haufe.sortabletable:main
    #
    # Would create an entry point called haufe.sortabletable that calls haufe.sortabletable.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - haufe
    - haufe.sortabletable

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: GPL
  summary: 'Sortabletable implementation as used in HRS2'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
