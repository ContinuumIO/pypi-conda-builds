package:
  name: python-axolotl-curve25519
  version: "0.1"

source:
  fn: python-axolotl-curve25519-0.1.tar.gz
  url: https://pypi.python.org/packages/source/p/python-axolotl-curve25519/python-axolotl-curve25519-0.1.tar.gz
  md5: f28d902df9044f0bf86a35a4bd2ec092
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-axolotl-curve25519 = python-axolotl-curve25519:main
    #
    # Would create an entry point called python-axolotl-curve25519 that calls python-axolotl-curve25519.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: GPLv3 License
  summary: 'curve25519 with ed25519 signatures, used by libaxolotl'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
