package:
  name: git-repo-manager
  version: "0.2.0"

source:
  fn: git-repo-manager-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/g/git-repo-manager/git-repo-manager-0.2.0.tar.gz
  md5: c9739e526d82978905cd8482f77e855f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - git-repo-manager = git-repo-manager:main
    #
    # Would create an entry point called git-repo-manager that calls git-repo-manager.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - simple-dependencies-manager

  run:
    - python
    - simple-dependencies-manager

test:
  # Python imports
  imports:
    - git_repo_manager

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/victorpantoja/git-repo-manager
  license: MIT
  summary: u'A simple library that helps you manage your git repos'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
