package:
  name: django-smart-settings
  version: "0.1.2"

source:
  fn: django-smart-settings-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-smart-settings/django-smart-settings-0.1.2.tar.gz
  md5: db8c077e39edfb2301a64a5c6e942fb8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-smart-settings = django-smart-settings:main
    #
    # Would create an entry point called django-smart-settings that calls django-smart-settings.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - django_smart_settings

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ertwroc/django-smart-settings
  license: LICENSE.txt
  summary: 'Helper for django settings'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
