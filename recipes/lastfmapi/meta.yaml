package:
  name: lastfmapi
  version: "0.1"

source:
  fn: lastfmapi-0.1.tar.gz
  url: https://pypi.python.org/packages/source/l/lastfmapi/lastfmapi-0.1.tar.gz
  md5: 57297542a08d47e08cdc585230218be5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lastfmapi = lastfmapi:main
    #
    # Would create an entry point called lastfmapi that calls lastfmapi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - lastfmapi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rockstar/lastfmapi
  license: http://www.opensource.org/licenses/mit-license.php
  summary: 'A thin wrapper for accessing the Last.fm API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
