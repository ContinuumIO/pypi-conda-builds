package:
  name: c2.recipe.bitbucketbk
  version: "1.0b7"

source:
  fn: c2.recipe.bitbucketbk-1.0b7.tar.gz
  url: https://pypi.python.org/packages/source/c/c2.recipe.bitbucketbk/c2.recipe.bitbucketbk-1.0b7.tar.gz
  md5: 266edad04d5039004ce98ca6df55c620
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - c2.recipe.bitbucketbk = c2.recipe.bitbucketbk:main
    #
    # Would create an entry point called c2.recipe.bitbucketbk that calls c2.recipe.bitbucketbk.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - zc.recipe.egg
    - mercurial
    - xlwt

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - zc.recipe.egg
    - mercurial
    - xlwt

test:
  # Python imports
  imports:
    - c2
    - c2.recipe
    - c2.recipe.bitbucketbk
    - c2.recipe.bitbucketbk.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.cmscom.jp
  license: Zope Public License
  summary: 'This package is backupp repository from bitbucket.org'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
