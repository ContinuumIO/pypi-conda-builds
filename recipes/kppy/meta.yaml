package:
  name: kppy
  version: "1.4.2"

source:
  fn: kppy-1.4.2.tar.gz
  url: https://pypi.python.org/packages/source/k/kppy/kppy-1.4.2.tar.gz
  md5: c8c4351877ff3489f95985c8f7a968a8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kppy = kppy:main
    #
    # Would create an entry point called kppy that calls kppy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - pycrypto

  run:
    - python
    - pycrypto

test:
  # Python imports
  imports:
    - kppy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://raymontag.github.com/kppy
  license: GPL v3 or later
  summary: 'A Python-module to provide an API to KeePass 1.x files which are also used by the popular KeePassX.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
