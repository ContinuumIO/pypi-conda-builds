package:
  name: internet-in-a-box
  version: "0.5.10"

source:
  fn: Internet-in-a-Box-0.5.10.tar.gz
  url: https://pypi.python.org/packages/source/I/Internet-in-a-Box/Internet-in-a-Box-0.5.10.tar.gz
  md5: 1646dc1ce6fea088d843cfd1a63d3608
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - internet-in-a-box = internet-in-a-box:main
    #
    # Would create an entry point called internet-in-a-box that calls internet-in-a-box.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.10
    - flask-babel >=0.8
    - flask-autoindex
    - flask-sqlalchemy
    - sqlalchemy >=0.8.2
    - whoosh >=2.6.0
    - backports.lzma >=0.0.2
    - repoze.lru

  run:
    - python
    - flask >=0.10
    - flask-babel >=0.8
    - flask-autoindex
    - flask-sqlalchemy
    - sqlalchemy >=0.8.2
    - whoosh >=2.6.0
    - backports.lzma >=0.0.2
    - repoze.lru

test:
  # Python imports
  imports:
    - iiab

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://internet-in-a-box.org
  license: 2-clause BSD license
  summary: "Sharing the world's Free information"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
