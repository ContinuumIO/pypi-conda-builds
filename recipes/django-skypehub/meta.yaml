package:
  name: django-skypehub
  version: "0.3.0"

source:
  fn: django-skypehub-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-skypehub/django-skypehub-0.3.0.tar.gz
  md5: a954ca28033ff43721bfca5c559bb79d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-skypehub = django-skypehub:main
    #
    # Would create an entry point called django-skypehub that calls django-skypehub.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - skype4py

  run:
    - python
    - django
    - skype4py

test:
  # Python imports
  imports:
    - skypehub
    - skypehub.management
    - skypehub.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/tokibito/django-skypehub/
  license: BSD License
  summary: 'Skype API bridge for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
