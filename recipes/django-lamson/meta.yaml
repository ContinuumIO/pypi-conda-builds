package:
  name: django-lamson
  version: "0.1.0.beta"

source:
  fn: django-lamson-0.1.0.beta.tar.gz
  url: https://pypi.python.org/packages/source/d/django-lamson/django-lamson-0.1.0.beta.tar.gz
  md5: d727de922a84fae1c6e4c5c3d0bace34
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-lamson = django-lamson:main
    #
    # Would create an entry point called django-lamson that calls django-lamson.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - lamson ==1.1

  run:
    - python
    - lamson ==1.1

test:
  # Python imports
  imports:
    - django_lamson
    - django_lamson.management
    - django_lamson.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/vvangelovski/django-lamson
  license: BSD License
  summary: 'Easy integration of lamson in django projects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
