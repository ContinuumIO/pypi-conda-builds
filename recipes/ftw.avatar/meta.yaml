package:
  name: ftw.avatar
  version: "1.0.6"

source:
  fn: ftw.avatar-1.0.6.zip
  url: https://pypi.python.org/packages/source/f/ftw.avatar/ftw.avatar-1.0.6.zip
  md5: 9e105b1e9c2b36dceafe452c2c3cc429
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ftw.avatar = ftw.avatar:main
    #
    # Would create an entry point called ftw.avatar that calls ftw.avatar.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - accesscontrol
    - pillow
    - products.cmfcore
    - products.plonepas
    - zope2
    - plone.scale
    - setuptools
    - zope.annotation
    - zope.component
    - zope.interface

  run:
    - python
    - setuptools
    - accesscontrol
    - pillow
    - products.cmfcore
    - products.plonepas
    - zope2
    - plone.scale
    - setuptools
    - zope.annotation
    - zope.component
    - zope.interface

test:
  # Python imports
  imports:
    - ftw
    - ftw.avatar
    - ftw.avatar.browser
    - ftw.avatar.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/4teamwork/ftw.avatar
  license: GPL2
  summary: 'Generates a personal default avatar for new Plone users'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
