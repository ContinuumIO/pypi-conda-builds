package:
  name: collective.ptg.galleriffic
  version: "1.1.2.1"

source:
  fn: collective.ptg.galleriffic-1.1.2.1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.ptg.galleriffic/collective.ptg.galleriffic-1.1.2.1.tar.gz
  md5: e7a120479a0fce8a9e5f7db7cca9b38d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.ptg.galleriffic = collective.ptg.galleriffic:main
    #
    # Would create an entry point called collective.ptg.galleriffic that calls collective.ptg.galleriffic.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.plonetruegallery

  run:
    - python
    - setuptools
    - setuptools
    - collective.plonetruegallery

test:
  # Python imports
  imports:
    - collective
    - collective.ptg
    - collective.ptg.galleriffic

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.ptg.galleriffic
  license: GPL
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
