package:
  name: django-viewssi
  version: "0.1.1"

source:
  fn: django-viewssi-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-viewssi/django-viewssi-0.1.1.tar.gz
  md5: d2848a0c62e6c88190f82284c79d9209
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-viewssi = django-viewssi:main
    #
    # Would create an entry point called django-viewssi that calls django-viewssi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - viewssi
    - viewssi.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/django-viewssi
  license: BSD License
  summary: 'Template tag for requesting views in templates (Django)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
