package:
  name: django-green-comments
  version: "0.1.8"

source:
  fn: django-green-comments-0.1.8.tar.gz
  url: https://pypi.python.org/packages/source/d/django-green-comments/django-green-comments-0.1.8.tar.gz
  md5: e6a8871926b212a757243ebe2f11f072
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-green-comments = django-green-comments:main
    #
    # Would create an entry point called django-green-comments that calls django-green-comments.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - green_comments
    - green_comments.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/lorien/django-green-comments/
  license: BSD License
  summary: 'Threaded comments application'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
