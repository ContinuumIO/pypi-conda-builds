package:
  name: django-botscout
  version: "0.0.2"

source:
  fn: django-botscout-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-botscout/django-botscout-0.0.2.tar.gz
  md5: faf38293be7c48ea75ad3114b13fb32d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-botscout = django-botscout:main
    #
    # Would create an entry point called django-botscout that calls django-botscout.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - botscout

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://labs.lyrical.net/lyrical/django-botscout
  license: License :: OSI Approved
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
