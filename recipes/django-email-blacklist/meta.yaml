package:
  name: django-email-blacklist
  version: "1.0.1"

source:
  fn: django-email-blacklist-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-email-blacklist/django-email-blacklist-1.0.1.tar.gz
  md5: bcd98eddf9882005e6cc0c09d580412b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-email-blacklist = django-email-blacklist:main
    #
    # Would create an entry point called django-email-blacklist that calls django-email-blacklist.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_email_blacklist

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Zeioth/django-email-blacklist
  license: MIT License
  summary: 'Python class for use with Django to detect Disposable Emails.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
