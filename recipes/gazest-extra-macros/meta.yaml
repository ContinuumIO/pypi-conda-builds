package:
  name: gazest-extra-macros
  version: "0.3.1"

source:
  fn: gazest_extra_macros-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/g/gazest-extra-macros/gazest_extra_macros-0.3.1.tar.gz
  md5: f8fd33729f3aff86179b3dec78ddb363
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gazest-extra-macros = gazest-extra-macros:main
    #
    # Would create an entry point called gazest-extra-macros that calls gazest-extra-macros.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - gazest >=0.3

  run:
    - python
    - setuptools
    - gazest >=0.3

test:
  # Python imports
  imports:
    - gazest_extra_macros

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: GPL v3 or later
  summary: 'extra wiki macros for gazest'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
