package:
  name: halwa
  version: "0.1.9"

source:
  fn: Halwa-0.1.9.tar.gz
  url: https://pypi.python.org/packages/source/H/Halwa/Halwa-0.1.9.tar.gz
  md5: 3f1db80744e67923591aa6d1d17ba010
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - halwa = halwa:main
    #
    # Would create an entry point called halwa that calls halwa.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - hamlish-jinja >=0.3.0
    - jinja2 >=2.6
    - markdown >=2.2.1

  run:
    - python
    - hamlish-jinja >=0.3.0
    - jinja2 >=2.6
    - markdown >=2.2.1

test:
  # Python imports
  imports:
    - halwa

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mhlakhani/halwa
  license: UNKNOWN
  summary: 'Single file static site generator.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
