package:
  name: dictsearch
  version: "1.2"

source:
  fn: dictsearch-1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/dictsearch/dictsearch-1.2.tar.gz
  md5: 2f1bd3fd533ff5015b09e71c88301d86
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dictsearch = dictsearch:main
    #
    # Would create an entry point called dictsearch that calls dictsearch.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - dictsearch

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tommyjcarpenter/dictsearch
  license: MIT License
  summary: 'A tool for iterating dictionaries as trees and printing all leaf nodes at some path'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
