package:
  name: twistedlilypad
  version: "1.0.0"

source:
  fn: twistedlilypad-1.0.0.zip
  url: https://pypi.python.org/packages/source/t/twistedlilypad/twistedlilypad-1.0.0.zip
  md5: 2256c06596e330018e481d0e0a925549
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - twistedlilypad = twistedlilypad:main
    #
    # Would create an entry point called twistedlilypad that calls twistedlilypad.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - twisted

  run:
    - python
    - twisted

test:
  # Python imports
  imports:
    - twistedlilypad
    - twistedlilypad.Packets
    - twistedlilypad.Requests
    - twistedlilypad.Results
    - twistedlilypad.Utilities

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/flaminscotsman/TwistedLilypad
  license: MIT License
  summary: 'An implementation of the lilypad protocol in python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
