package:
  name: django-drupal-password-hasher
  version: "0.1.1"

source:
  fn: Django-Drupal-Password-Hasher-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/D/Django-Drupal-Password-Hasher/Django-Drupal-Password-Hasher-0.1.1.tar.gz
  md5: 74d236ecea8c563dfaf5fdda19929c7a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-drupal-password-hasher = django-drupal-password-hasher:main
    #
    # Would create an entry point called django-drupal-password-hasher that calls django-drupal-password-hasher.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6.5

  run:
    - python
    - django >=1.6.5

test:
  # Python imports
  imports:
    - djangodrupalpasswordhasher
    - djangodrupalpasswordhasher.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/Django-drupal-password-hasher/
  license: LICENSE.txt
  summary: 'Useful password hasher for django sites with drupal components'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
