package:
  name: jsontree
  version: "0.4.3"

source:
  fn: jsontree-0.4.3.tar.gz
  url: https://pypi.python.org/packages/source/j/jsontree/jsontree-0.4.3.tar.gz
  md5: 85b71eb26b38447133f5cb2b8a31a3bc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jsontree = jsontree:main
    #
    # Would create an entry point called jsontree that calls jsontree.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - jsontree

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dougn/jsontree/
  license: BSD License
  summary: 'Utility class for managing json tree data as python objects. Recursive depth dictionaries with keys as attributes and json serialization.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
