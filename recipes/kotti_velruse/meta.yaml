package:
  name: kotti_velruse
  version: "0.3.4"

source:
  fn: kotti_velruse-0.3.4.zip
  url: https://pypi.python.org/packages/source/k/kotti_velruse/kotti_velruse-0.3.4.zip
  md5: e35ddecade3dfd449e32245560ce119e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kotti_velruse = kotti_velruse:main
    #
    # Would create an entry point called kotti_velruse that calls kotti_velruse.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - kotti
    - rgomes-velruse
    - openid-selector

  run:
    - python
    - kotti
    - rgomes-velruse
    - openid-selector

test:
  # Python imports
  imports:
    - kotti_velruse

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://kotti_velruse.readthedocs.org
  license: BSD License
  summary: 'Kotti authentication with Velruse: OpenID, OAuth2, Google, Yahoo, Live, Facebook, Twitter and others'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
