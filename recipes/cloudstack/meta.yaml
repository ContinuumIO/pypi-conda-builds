package:
  name: cloudstack
  version: "3.0.0.1"

source:
  fn: CloudStack-3.0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/C/CloudStack/CloudStack-3.0.0.1.tar.gz
  md5: 5454ca822b568d0e19b70a2ef2403902
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cloudstack = cloudstack:main
    #
    # Would create an entry point called cloudstack that calls cloudstack.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - CloudStack
    - cloudstack

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/larstobi/cloudstack-python-client
  license: MIT
  summary: 'CloudStack API v2.2 Client'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
