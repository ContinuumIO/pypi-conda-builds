package:
  name: ahonya-sika
  version: "0.1.1"

source:
  fn: ahonya-sika-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/a/ahonya-sika/ahonya-sika-0.1.1.tar.gz
  md5: a474482ce918333ac9a9795816ba760b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ahonya-sika = ahonya-sika:main
    #
    # Would create an entry point called ahonya-sika that calls ahonya-sika.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=2.1.0
    - simplejson >=3.6.5

  run:
    - python
    - requests >=2.1.0
    - simplejson >=3.6.5

test:
  # Python imports
  imports:
    - sika

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Ahonya/sika-python
  license: MIT License
  summary: 'Sika API library client for python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
