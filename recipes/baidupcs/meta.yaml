package:
  name: baidupcs
  version: "0.3.2"

source:
  fn: baidupcs-0.3.2.zip
  url: https://pypi.python.org/packages/source/b/baidupcs/baidupcs-0.3.2.zip
  md5: a2717283928ebf4208d1756422d16b68
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - baidupcs = baidupcs:main
    #
    # Would create an entry point called baidupcs that calls baidupcs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=1.1.0
    - requests_toolbelt >=0.2.0

  run:
    - python
    - requests >=1.1.0
    - requests_toolbelt >=0.2.0

test:
  # Python imports
  imports:
    - baidupcs

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mozillazg/baidu-pcs-python-sdk
  license: MIT License
  summary: '\xe7\x99\xbe\xe5\xba\xa6\xe4\xb8\xaa\xe4\xba\xba\xe4\xba\x91\xe5\xad\x98\xe5\x82\xa8\xef\xbc\x88PCS\xef\xbc\x89Python SDK'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
