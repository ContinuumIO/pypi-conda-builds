package:
  name: invocations
  version: "0.10.0"

source:
  fn: invocations-0.10.0.tar.gz
  url: https://pypi.python.org/packages/source/i/invocations/invocations-0.10.0.tar.gz
  md5: fa056128c579f5b23eea9a54bf51038f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - invocations = invocations:main
    #
    # Would create an entry point called invocations that calls invocations.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - invocations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pyinvoke.org
  license: BSD License
  summary: 'Reusable Invoke tasks'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
