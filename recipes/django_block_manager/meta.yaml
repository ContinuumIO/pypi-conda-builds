package:
  name: django_block_manager
  version: "0.0.10dev"

source:
  fn: django_block_manager-0.0.10dev.tar.gz
  url: https://pypi.python.org/packages/source/d/django_block_manager/django_block_manager-0.0.10dev.tar.gz
  md5: edec22488431d1232da365a4711f5c83
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_block_manager = django_block_manager:main
    #
    # Would create an entry point called django_block_manager that calls django_block_manager.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_block_manager
    - django_block_manager.lib
    - django_block_manager.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://airtonix.net/projects/django-block-manager
  license: LICENSE.md
  summary: "Simple Django per-application block content template 'thingo'."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
