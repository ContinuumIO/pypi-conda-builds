package:
  name: charlockholmes
  version: "0.0.2"

source:
  fn: charlockholmes-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/c/charlockholmes/charlockholmes-0.0.2.tar.gz
  md5: 12b74828839936e8b32038b0f693d022
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - charlockholmes = charlockholmes:main
    #
    # Would create an entry point called charlockholmes that calls charlockholmes.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - charlockholmes

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/xtao/PyCharlockHolmes
  license: BSD License
  summary: 'Character encoding detecting library for Python using ICU and libmagic.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
