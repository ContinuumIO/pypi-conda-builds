package:
  name: gat-python
  version: "0.0.3"

source:
  fn: gat-python-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/g/gat-python/gat-python-0.0.3.tar.gz
  md5: d45a1e5b45a13d65b70e94ad365bcdf9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gat-python = gat-python:main
    #
    # Would create an entry point called gat-python that calls gat-python.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six ==1.3.0
    - simplejson ==3.1.3

  run:
    - python
    - six ==1.3.0
    - simplejson ==3.1.3

test:
  # Python imports
  imports:
    - gat_python
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/gatournament/gat-python
  license: MIT
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
