package:
  name: iw.recipe.sendmail
  version: "0.2.3"

source:
  fn: iw.recipe.sendmail-0.2.3.tar.gz
  url: https://pypi.python.org/packages/source/i/iw.recipe.sendmail/iw.recipe.sendmail-0.2.3.tar.gz
  md5: 61974fa856bbeb30786038b4d3b86544
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - iw.recipe.sendmail = iw.recipe.sendmail:main
    #
    # Would create an entry point called iw.recipe.sendmail that calls iw.recipe.sendmail.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - iw.recipe.template

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - iw.recipe.template

test:
  # Python imports
  imports:
    - iw
    - iw.recipe
    - iw.recipe.sendmail
    - iw.recipe.sendmail.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/iw-recipes
  license: GNU General Public License (GPL)
  summary: 'ZC buildout recipe to setup zope.sendmail in a Zope2 instance'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
