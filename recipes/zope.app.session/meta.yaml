package:
  name: zope.app.session
  version: "3.6.2"

source:
  fn: zope.app.session-3.6.2.tar.gz
  url: https://pypi.python.org/packages/source/z/zope.app.session/zope.app.session-3.6.2.tar.gz
  md5: 93467bf6854d714b53e71f36a9d770f3
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zope.app.session = zope.app.session:main
    #
    # Would create an entry point called zope.app.session that calls zope.app.session.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zodb3
    - zope.annotation
    - zope.app.appsetup
    - zope.app.http
    - zope.component
    - zope.i18nmessageid
    - zope.interface
    - zope.location
    - zope.minmax
    - zope.publisher
    - zope.session

  run:
    - python
    - setuptools
    - zodb3
    - zope.annotation
    - zope.app.appsetup
    - zope.app.http
    - zope.component
    - zope.i18nmessageid
    - zope.interface
    - zope.location
    - zope.minmax
    - zope.publisher
    - zope.session

test:
  # Python imports
  imports:
    - zope
    - zope.app
    - zope.app.session

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/zope.app.session
  license: Zope Public License
  summary: 'Zope session'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
