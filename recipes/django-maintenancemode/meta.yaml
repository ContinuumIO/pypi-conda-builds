package:
  name: django-maintenancemode
  version: "0.10"

source:
  fn: django-maintenancemode-0.10.tar.gz
  url: https://pypi.python.org/packages/source/d/django-maintenancemode/django-maintenancemode-0.10.tar.gz
  md5: 11f4ceddf439f0ecd3e30d1cc6c2c4ce
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-maintenancemode = django-maintenancemode:main
    #
    # Would create an entry point called django-maintenancemode that calls django-maintenancemode.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - maintenancemode
    - maintenancemode.conf
    - maintenancemode.conf.settings
    - maintenancemode.conf.urls
    - maintenancemode.tests
    - maintenancemode.views

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/shanx/django-maintenancemode
  license: BSD License
  summary: 'Django-maintenancemode allows you to temporary shutdown your site for maintenance work'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
