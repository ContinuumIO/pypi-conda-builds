package:
  name: iwlib
  version: "1.5"

source:
  fn: iwlib-1.5.tar.gz
  url: https://pypi.python.org/packages/source/i/iwlib/iwlib-1.5.tar.gz
  md5: 1397e2270a70fdb477f1ceb7336ad1bc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - iwlib = iwlib:main
    #
    # Would create an entry point called iwlib that calls iwlib.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - iwlib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/nathan-hoad/python-iwlib
  license: GNU General Public License v2 (GPLv2)
  summary: 'Python module to interface with iwlib'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
