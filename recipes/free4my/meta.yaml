package:
  name: free4my
  version: "0.10"

source:
  fn: free4my-0.10.tar.gz
  url: https://pypi.python.org/packages/source/f/free4my/free4my-0.10.tar.gz
  md5: c6f7326cc432998df6befead331cab20
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - free4my = free4my:main
    #
    # Would create an entry point called free4my that calls free4my.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - free4my

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ipconfiger/free4my
  license: Apache License 2.0
  summary: 'a easy to use schema free DAL for mysql'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
