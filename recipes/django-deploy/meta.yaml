package:
  name: django-deploy
  version: "0.1.0"

source:
  fn: django-deploy-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-deploy/django-deploy-0.1.0.tar.gz
  md5: 94c0a90afd2b8c26fc1e973aa542b6d3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-deploy = django-deploy:main
    #
    # Would create an entry point called django-deploy that calls django-deploy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fabric ==0.9.3

  run:
    - python
    - fabric ==0.9.3

test:
  # Python imports
  imports:
    - django_deploy
    - django_deploy.management
    - django_deploy.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/johnthedebs/django-deploy
  license: MIT License
  summary: 'Django deployment utility'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
