package:
  name: products.richimage
  version: "1.5"

source:
  fn: Products.RichImage-1.5.zip
  url: https://pypi.python.org/packages/source/P/Products.RichImage/Products.RichImage-1.5.zip
  md5: b1ee94914e673ca97869a3eaf645afc0
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - products.richimage = products.richimage:main
    #
    # Would create an entry point called products.richimage that calls products.richimage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - plone.app.blob

  run:
    - python
    - setuptools
    - plone.app.blob

test:
  # Python imports
  imports:
    - Products
    - Products.RichImage
    - Products.RichImage.browser
    - Products.RichImage.content
    - Products.RichImage.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.jarn.com
  license: GNU General Public License (GPL)
  summary: 'Image with crop functionalities.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
