package:
  name: maestro
  version: "0.2.0"

source:
  fn: maestro-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/m/maestro/maestro-0.2.0.tar.gz
  md5: e604f03ca02cc7722560b8dd9b3d4b58
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - maestro = maestro:main
    #
    # Would create an entry point called maestro that calls maestro.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - fabric >=1.4.0
    - apache-libcloud >=0.9.1
    - argparse >=1.2

  run:
    - python
    - fabric >=1.4.0
    - apache-libcloud >=0.9.1
    - argparse >=1.2

test:
  # Python imports
  imports:
    - maestro

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ehazlett/maestro
  license: Apache Software License
  summary: 'DevOps management'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
