package:
  name: apiarist
  version: "0.1.11"

source:
  fn: apiarist-0.1.11.tar.gz
  url: https://pypi.python.org/packages/source/a/apiarist/apiarist-0.1.11.tar.gz
  md5: ddab590df9185253fddb6f06576ed576
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - apiarist = apiarist:main
    #
    # Would create an entry point called apiarist that calls apiarist.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - boto >=2.2.0

  run:
    - python
    - boto >=2.2.0

test:
  # Python imports
  imports:
    - apiarist

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/msharp/apiarist
  license: Apache Software License
  summary: 'Python Hive query framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
