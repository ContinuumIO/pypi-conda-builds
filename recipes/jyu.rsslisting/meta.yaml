package:
  name: jyu.rsslisting
  version: "1.0.1"

source:
  fn: jyu.rsslisting-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/j/jyu.rsslisting/jyu.rsslisting-1.0.1.tar.gz
  md5: 9bd42898c855096950fe1e0e6230fe30
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jyu.rsslisting = jyu.rsslisting:main
    #
    # Would create an entry point called jyu.rsslisting that calls jyu.rsslisting.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - feedparser

  run:
    - python
    - setuptools
    - setuptools
    - feedparser

test:
  # Python imports
  imports:
    - jyu
    - jyu.rsslisting
    - jyu.rsslisting.browser
    - jyu.rsslisting.content
    - jyu.rsslisting.interfaces
    - jyu.rsslisting.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/jyu.rsslisting
  license: GNU General Public License (GPL)
  summary: 'A page, which displays an RSS feeds (or aggregation of several feeds) in the same way as RSS portlet does'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
