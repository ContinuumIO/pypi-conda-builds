package:
  name: bit.recipe.android_ndk
  version: "0.0.2"

source:
  fn: bit.recipe.android_ndk-0.0.2.zip
  url: https://pypi.python.org/packages/source/b/bit.recipe.android_ndk/bit.recipe.android_ndk-0.0.2.zip
  md5: 2759bd5a97c69fb49781b1cb412618b3
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bit.recipe.android_ndk = bit.recipe.android_ndk:main
    #
    # Would create an entry point called bit.recipe.android_ndk that calls bit.recipe.android_ndk.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zc.buildout
    - setuptools

  run:
    - python
    - setuptools
    - zc.buildout
    - setuptools

test:
  # Python imports
  imports:
    - bit
    - bit.recipe
    - bit.recipe.android_ndk

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.python.org/pypi/bit.recipe.android_ndk
  license: GPL
  summary: 'bit foundation android_ndk zc.buildout recipe'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
