package:
  name: django-simple-seo
  version: "1.0.3"

source:
  fn: django-simple-seo-1.0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-simple-seo/django-simple-seo-1.0.3.tar.gz
  md5: 3ab4635ccea402ce5093a9e8e8e72936
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-simple-seo = django-simple-seo:main
    #
    # Would create an entry point called django-simple-seo that calls django-simple-seo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - simple_seo
    - simple_seo.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/danigosa/django-simple-seo
  license: BSD License
  summary: 'Simple SEO Module for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
