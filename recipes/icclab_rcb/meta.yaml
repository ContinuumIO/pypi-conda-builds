package:
  name: icclab_rcb
  version: "1.0dev"

source:
  fn: icclab_rcb-1.0dev.tar.gz
  url: https://pypi.python.org/packages/source/i/icclab_rcb/icclab_rcb-1.0dev.tar.gz
  md5: 28246e8778d301b82c22cba07baaf700
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - icclab_rcb = icclab_rcb:main
    #
    # Would create an entry point called icclab_rcb that calls icclab_rcb.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - sympy
    - httplib2
    - fpdf
    - python-dateutil

  run:
    - python
    - sympy
    - httplib2
    - fpdf
    - python-dateutil

test:
  # Python imports
  imports:
    - common
    - icclab_rcb
    - logs
    - main_menu
    - os_api
    - tests
    - web_ui

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'Rating Charging and Billing engine for the cloud service providers'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
