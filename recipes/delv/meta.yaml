package:
  name: delv
  version: "0.1.18"

source:
  fn: delv-0.1.18.tar.gz
  url: https://pypi.python.org/packages/source/d/delv/delv-0.1.18.tar.gz
  md5: 01561c8e8ba4664fc60a26117823c60f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - delv = delv:main
    #
    # Would create an entry point called delv that calls delv.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - delv

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.ferazelhosting.net/wiki/delv
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'Module for making tools interoperable with Delver'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
