package:
  name: raptus.touchswipe
  version: "1.0a1"

source:
  fn: raptus.touchswipe-1.0a1.zip
  url: https://pypi.python.org/packages/source/r/raptus.touchswipe/raptus.touchswipe-1.0a1.zip
  md5: 07b1f2e860057853470eba5bd8e06b88
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - raptus.touchswipe = raptus.touchswipe:main
    #
    # Would create an entry point called raptus.touchswipe that calls raptus.touchswipe.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - raptus
    - raptus.touchswipe

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/Raptus/raptus.touchswipe
  license: gpl
  summary: 'Provides the jQuery TouchSwipe plugin as Plone JS resource'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
