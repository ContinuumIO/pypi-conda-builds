package:
  name: emaillove
  version: "0.1.0"

source:
  fn: EmailLove-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/E/EmailLove/EmailLove-0.1.0.tar.gz
  md5: 1edd781ef9718b5047a0a67e0302da31
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - emaillove = emaillove:main
    #
    # Would create an entry point called emaillove that calls emaillove.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - emaillove
    - emaillove.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/EmailLove/
  license: LICENSE.txt
  summary: 'Useful mail provider stuff.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
