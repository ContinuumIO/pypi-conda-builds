package:
  name: elementtreefactory
  version: "1.0"

source:
  fn: ElementTreeFactory-1.0.tar.gz
  url: https://pypi.python.org/packages/source/E/ElementTreeFactory/ElementTreeFactory-1.0.tar.gz
  md5: 30c88459b51e1e3f8e7f973d937c8da4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - elementtreefactory = elementtreefactory:main
    #
    # Would create an entry point called elementtreefactory that calls elementtreefactory.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - ElementTreeFactory
    - elementtreefactory

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/waveform80/ElementTreeFactory
  license: MIT License
  summary: 'A Genshi-inspired framework for easy use of ElementTree'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
