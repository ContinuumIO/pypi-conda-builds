package:
  name: cloudify-script-plugin
  version: "1.2a8"

source:
  fn: cloudify-script-plugin-1.2a8.tar.gz
  url: https://pypi.python.org/packages/source/c/cloudify-script-plugin/cloudify-script-plugin-1.2a8.tar.gz
  md5: af8b8f5eeeb8006c8a7e7e0e623db518
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cloudify-script-plugin = cloudify-script-plugin:main
    #
    # Would create an entry point called cloudify-script-plugin that calls cloudify-script-plugin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cloudify-plugins-common >=3.2a8

  run:
    - python
    - cloudify-plugins-common >=3.2a8

test:
  # Python imports
  imports:
    - script_runner

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: LICENSE
  summary: 'Plugin for running scripts'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
