package:
  name: btnamespace
  version: "1.1.1"

source:
  fn: btnamespace-1.1.1.tar.gz
  url: https://pypi.python.org/packages/source/b/btnamespace/btnamespace-1.1.1.tar.gz
  md5: 77255b1ef48c70524619497a86a14066
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - btnamespace = btnamespace:main
    #
    # Would create an entry point called btnamespace that calls btnamespace.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - bidict ==0.1.5
    - braintree

  run:
    - python
    - bidict ==0.1.5
    - braintree

test:
  # Python imports
  imports:
    - btnamespace

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/venmo/btnamespace
  license: MIT License
  summary: 'Isolate state on the Braintree sandbox during testing.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
