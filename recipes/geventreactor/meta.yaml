package:
  name: geventreactor
  version: "0.1.0"

source:
  fn: geventreactor-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/g/geventreactor/geventreactor-0.1.0.tar.gz
  md5: 30754343d52f773395438cf18e68bf34
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - geventreactor = geventreactor:main
    #
    # Would create an entry point called geventreactor that calls geventreactor.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - gevent

  run:
    - python
    - gevent

test:
  # Python imports
  imports:
    - geventreactor

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/jyio/geventreactor
  license: MIT
  summary: 'Twisted reactor based on gevent'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
