package:
  name: bugzillatools
  version: "0.5.5"

source:
  fn: bugzillatools-0.5.5.tar.gz
  url: https://pypi.python.org/packages/source/b/bugzillatools/bugzillatools-0.5.5.tar.gz
  md5: 6cdd573562470b669fcded503b705c03
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bugzillatools = bugzillatools:main
    #
    # Would create an entry point called bugzillatools that calls bugzillatools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - bugzillatools
    - bzlib
    - bzrlib.plugins.bugzillatools

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/frasertweedale/bugzillatools
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'Bugzilla CLI client, XML-RPC binding and VCS plugins'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
