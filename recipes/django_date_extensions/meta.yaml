package:
  name: django_date_extensions
  version: "1.0.0"

source:
  fn: django_date_extensions-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django_date_extensions/django_date_extensions-1.0.0.tar.gz
  md5: 8d5745c5fa90c95f055ac05a60a01e25
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_date_extensions = django_date_extensions:main
    #
    # Would create an entry point called django_date_extensions that calls django_date_extensions.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - django_date_extensions

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dracos/django-date-extensions
  license: GNU Affero General Public License v3
  summary: "This code adds a few small extensions to Django's DateField, to handle both approximate dates (e.g. 'March 1963') and default year dates (e.g. assume '24th June' is the most recent such)."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
