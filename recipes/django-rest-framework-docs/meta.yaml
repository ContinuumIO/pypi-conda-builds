package:
  name: django-rest-framework-docs
  version: "0.1.7"

source:
  fn: django-rest-framework-docs-0.1.7.tar.gz
  url: https://pypi.python.org/packages/source/d/django-rest-framework-docs/django-rest-framework-docs-0.1.7.tar.gz
  md5: e8f388b17c3ffb4959530621b39919cc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-rest-framework-docs = django-rest-framework-docs:main
    #
    # Would create an entry point called django-rest-framework-docs that calls django-rest-framework-docs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - jsonpickle >=0.4.0
    - django >=1.4
    - djangorestframework >=2.1.3

  run:
    - python
    - jsonpickle >=0.4.0
    - django >=1.4
    - djangorestframework >=2.1.3

test:
  # Python imports
  imports:
    - rest_framework_docs

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/marcgibbons
  license: BSD License
  summary: 'An inventory tool for Django Rest Framework v2 API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
