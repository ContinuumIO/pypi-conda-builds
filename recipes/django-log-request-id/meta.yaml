package:
  name: django-log-request-id
  version: "1.0.0"

source:
  fn: django-log-request-id-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-log-request-id/django-log-request-id-1.0.0.tar.gz
  md5: cba8138f2ded0fdfde8df7bc20853269
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-log-request-id = django-log-request-id:main
    #
    # Would create an entry point called django-log-request-id that calls django-log-request-id.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - log_request_id

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dabapps/django-log-request-id/
  license: BSD
  summary: 'Django middleware and log filter to attach a unique ID to every log message generated as part of a request'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
