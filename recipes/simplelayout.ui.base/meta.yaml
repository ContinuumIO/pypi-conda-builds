package:
  name: simplelayout.ui.base
  version: "3.0.4"

source:
  fn: simplelayout.ui.base-3.0.4.zip
  url: https://pypi.python.org/packages/source/s/simplelayout.ui.base/simplelayout.ui.base-3.0.4.zip
  md5: 66d6d66f4d2f473d361d4f6bc3e29d4c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - simplelayout.ui.base = simplelayout.ui.base:main
    #
    # Would create an entry point called simplelayout.ui.base that calls simplelayout.ui.base.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - ftw.colorbox
    - z3c.json

  run:
    - python
    - setuptools
    - setuptools
    - ftw.colorbox
    - z3c.json

test:
  # Python imports
  imports:
    - simplelayout
    - simplelayout.ui
    - simplelayout.ui.base
    - simplelayout.ui.base.Extensions
    - simplelayout.ui.base.browser
    - simplelayout.ui.base.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/4teamwork/simplelayout.ui.base
  license: GPL2
  summary: 'Simplelayout ui base package - for plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
