package:
  name: flask-weasyprint
  version: "0.5"

source:
  fn: Flask-WeasyPrint-0.5.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-WeasyPrint/Flask-WeasyPrint-0.5.tar.gz
  md5: 40080cca024fe96cc6247bc96c680007
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-weasyprint = flask-weasyprint:main
    #
    # Would create an entry point called flask-weasyprint that calls flask-weasyprint.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - weasyprint >=0.12

  run:
    - python
    - flask
    - weasyprint >=0.12

test:
  # Python imports
  imports:
    - flask_weasyprint

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/SimonSapin/Flask-WeasyPrint
  license: BSD License
  summary: 'Make PDF in your Flask app with WeasyPrint.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
