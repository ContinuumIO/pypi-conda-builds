package:
  name: bottle-peewee
  version: "0.1.5"

source:
  fn: bottle-peewee-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/b/bottle-peewee/bottle-peewee-0.1.5.tar.gz
  md5: ec5f8ba997ba8478e056e87775174cca
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bottle-peewee = bottle-peewee:main
    #
    # Would create an entry point called bottle-peewee that calls bottle-peewee.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - peewee
    - bottle

  run:
    - python
    - peewee
    - bottle

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/klen/bottle-peewee
  license: BSD License
  summary: None

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
