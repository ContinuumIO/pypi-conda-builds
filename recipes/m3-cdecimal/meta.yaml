package:
  name: m3-cdecimal
  version: "2.3"

source:
  fn: m3-cdecimal-2.3.tar.gz
  url: https://pypi.python.org/packages/source/m/m3-cdecimal/m3-cdecimal-2.3.tar.gz
  md5: 80db156a9cf279e1d3d0e31e78196867
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - m3-cdecimal = m3-cdecimal:main
    #
    # Would create an entry point called m3-cdecimal that calls m3-cdecimal.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.bytereef.org/mpdecimal/index.html
  license: BSD License
  summary: 'Fast arbitrary precision correctly-rounded decimal floating point arithmetic.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
