package:
  name: miracle-acl
  version: "0.0.4-1"

source:
  fn: miracle-acl-0.0.4-1.tar.gz
  url: https://pypi.python.org/packages/source/m/miracle-acl/miracle-acl-0.0.4-1.tar.gz
  md5: e72a9d8fb42b18f7ce170d0b99e8c031
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - miracle-acl = miracle-acl:main
    #
    # Would create an entry point called miracle-acl that calls miracle-acl.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - miracle

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kolypto/py-miracle
  license: MIT
  summary: 'Flexible role-based authorization solution that is a pleasure to use'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
