package:
  name: django-beautifulsoup-test
  version: "1.0.1"

source:
  fn: django-beautifulsoup-test-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-beautifulsoup-test/django-beautifulsoup-test-1.0.1.tar.gz
  md5: d13ccf1590675af4eb8b5f4d2d0a362b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-beautifulsoup-test = django-beautifulsoup-test:main
    #
    # Would create an entry point called django-beautifulsoup-test that calls django-beautifulsoup-test.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - beautifulsoup4
    - django

  run:
    - python
    - beautifulsoup4
    - django

test:
  # Python imports
  imports:
    - django_bs_test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jscott1989/django-beautifulsoup-test
  license: UNKNOWN
  summary: 'TestCase class for using BeautifulSoup with Django tests'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
