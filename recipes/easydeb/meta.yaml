package:
  name: easydeb
  version: "0.2.0"

source:
  fn: easydeb-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/e/easydeb/easydeb-0.2.0.tar.gz
  md5: c1ab0684208aca90f00b995e09bf2cb2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - easydeb = easydeb:main
    #
    # Would create an entry point called easydeb that calls easydeb.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools >=0.6a0

  run:
    - python
    - setuptools >=0.6a0

test:
  # Python imports
  imports:
    - easydeb

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://easy-deb.sf.net
  license: GPL
  summary: 'Tool for automatically creating python modules packages'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
