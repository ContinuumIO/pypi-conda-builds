package:
  name: omni_api
  version: "0.2.6"

source:
  fn: omni_api-0.2.6.zip
  url: https://pypi.python.org/packages/source/o/omni_api/omni_api-0.2.6.zip
  md5: b049ec2e3704688a6574c6d27d1044cb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - omni_api = omni_api:main
    #
    # Would create an entry point called omni_api that calls omni_api.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - appier

  run:
    - python
    - appier

test:
  # Python imports
  imports:
    - omni
    - omni.models
    - omni_api

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://omni_api.hive.pt
  license: GNU General Public License (GPL)
  summary: 'Omni API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
