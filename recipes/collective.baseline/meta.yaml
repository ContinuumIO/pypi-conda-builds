package:
  name: collective.baseline
  version: "1.0"

source:
  fn: collective.baseline-1.0.zip
  url: https://pypi.python.org/packages/source/c/collective.baseline/collective.baseline-1.0.zip
  md5: f8f51e0b8a34807db153e56eab490899
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.baseline = collective.baseline:main
    #
    # Would create an entry point called collective.baseline that calls collective.baseline.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.baseline
    - collective.baseline.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.baseline
  license: GPL
  summary: 'Add a baseline to the header'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
