package:
  name: flattenlist
  version: "0.1.4"

source:
  fn: FlattenList-0.1.4.zip
  url: https://pypi.python.org/packages/source/F/FlattenList/FlattenList-0.1.4.zip
  md5: de44e27fc52624c1459710ce863d3bb4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flattenlist = flattenlist:main
    #
    # Would create an entry point called flattenlist that calls flattenlist.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - flatten
    - flatten.tests
    - flattenlist

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://pypi.python.org/pypi/FlattenList
  license: UNKNOWN
  summary: 'Given a list of nested lists/tuples, returns all the elements in a single list'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
