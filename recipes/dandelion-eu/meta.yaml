package:
  name: dandelion-eu
  version: "0.2.2"

source:
  fn: dandelion-eu-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/d/dandelion-eu/dandelion-eu-0.2.2.tar.gz
  md5: 9a56cf1a69036e98e902b118c7e2cb97
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dandelion-eu = dandelion-eu:main
    #
    # Would create an entry point called dandelion-eu that calls dandelion-eu.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests
    - six

  run:
    - python
    - requests
    - six

test:
  # Python imports
  imports:
    - dandelion
    - dandelion.cache

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/SpazioDati/python-dandelion-eu
  license: UNKNOWN
  summary: 'Connect to the dandelion.eu API in a very pythonic way!'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
