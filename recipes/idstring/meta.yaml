package:
  name: idstring
  version: "1.0.3"

source:
  fn: idstring-1.0.3.tar.gz
  url: https://pypi.python.org/packages/source/i/idstring/idstring-1.0.3.tar.gz
  md5: 421d64f6839b711c9e9492e71d3c688f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - idstring = idstring:main
    #
    # Would create an entry point called idstring that calls idstring.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - idstring

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/vernondcole/idstring
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'a class for compact alpha-numeric serial numbers with a check digit'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
