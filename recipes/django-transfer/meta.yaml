package:
  name: django-transfer
  version: "0.2-2"

source:
  fn: django-transfer-0.2-2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-transfer/django-transfer-0.2-2.tar.gz
  md5: a289f23890445f9a8966a078d2b4b843
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-transfer = django-transfer:main
    #
    # Would create an entry point called django-transfer that calls django-transfer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - django_transfer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/smartfile/django-transfer/
  license: MIT
  summary: 'A django application that offloads file transfers to a downstream proxy.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
