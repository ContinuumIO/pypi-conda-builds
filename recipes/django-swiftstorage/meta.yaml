package:
  name: django-swiftstorage
  version: "1.1.0"

source:
  fn: django-swiftstorage-1.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-swiftstorage/django-swiftstorage-1.1.0.tar.gz
  md5: 276d42801d873086beb2e846e5b60def
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-swiftstorage = django-swiftstorage:main
    #
    # Would create an entry point called django-swiftstorage that calls django-swiftstorage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - swiftstorage

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://launchpad.net/django-swiftstorage/
  license: GNU Lesser General Public License v3 (LGPLv3)
  summary: 'Django swiftstorage for Swift API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
