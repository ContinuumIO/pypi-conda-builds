package:
  name: django-mediafiles
  version: "0.2"

source:
  fn: django-mediafiles-0.2.zip
  url: http://django-mediafiles.googlecode.com/files/django-mediafiles-0.2.zip
  #md5: 
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-mediafiles = django-mediafiles:main
    #
    # Would create an entry point called django-mediafiles that calls django-mediafiles.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - mediafiles
    - mediafiles.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/django-mediafiles/
  license: GNU General Public License (GPL)
  summary: 'Django media files manager'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
