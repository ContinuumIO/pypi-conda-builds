package:
  name: collective.geolocationbehavior
  version: "1.3.2"

source:
  fn: collective.geolocationbehavior-1.3.2.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.geolocationbehavior/collective.geolocationbehavior-1.3.2.tar.gz
  md5: dd77061b5f076d657468cb14d6b36fa3
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.geolocationbehavior = collective.geolocationbehavior:main
    #
    # Would create an entry point called collective.geolocationbehavior that calls collective.geolocationbehavior.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.dexterity
    - plone.autoform
    - plone.behavior
    - plone.dexterity
    - plone.formwidget.geolocation
    - plone.supermodel
    - zope.annotation
    - zope.component
    - zope.interface

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.dexterity
    - plone.autoform
    - plone.behavior
    - plone.dexterity
    - plone.formwidget.geolocation
    - plone.supermodel
    - zope.annotation
    - zope.component
    - zope.interface

test:
  # Python imports
  imports:
    - collective
    - collective.geolocationbehavior
    - collective.geolocationbehavior.upgrades

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.geolocationbehavior/
  license: GPL
  summary: 'Dexterity behavior to add geographic locations to contents.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
