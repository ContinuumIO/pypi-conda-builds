package:
  name: djangotoolbox
  version: "1.6.2"

source:
  fn: djangotoolbox-1.6.2.tar.gz
  url: https://pypi.python.org/packages/source/d/djangotoolbox/djangotoolbox-1.6.2.tar.gz
  md5: c85b73839cc2b483547da57f2bfc4d8f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangotoolbox = djangotoolbox:main
    #
    # Would create an entry point called djangotoolbox that calls djangotoolbox.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - djangotoolbox
    - djangotoolbox.db
    - djangotoolbox.sites

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/django-nonrel/djangotoolbox
  license: BSD License
  summary: 'Djangotoolbox for Django-nonrel'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
