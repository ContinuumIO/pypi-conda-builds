package:
  name: django-tables2-reports
  version: "0.0.10"

source:
  fn: django-tables2-reports-0.0.10.tar.gz
  url: https://pypi.python.org/packages/source/d/django-tables2-reports/django-tables2-reports-0.0.10.tar.gz
  md5: b26e8777cb896a81d7d95c65d04fa2bb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-tables2-reports = django-tables2-reports:main
    #
    # Would create an entry point called django-tables2-reports that calls django-tables2-reports.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_tables2_reports
    - django_tables2_reports.csv_to_xls

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/goinnn/django-tables2-reports
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'With django-tables2-reports you can get a report (CSV, XLS) of any django-tables2 with minimal changes to your project'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
