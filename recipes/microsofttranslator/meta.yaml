package:
  name: microsofttranslator
  version: "0.7"

source:
  fn: microsofttranslator-0.7.tar.gz
  url: https://pypi.python.org/packages/source/m/microsofttranslator/microsofttranslator-0.7.tar.gz
  md5: fd8c4976b6be27843aa1d7fe32c1bf3c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - microsofttranslator = microsofttranslator:main
    #
    # Would create an entry point called microsofttranslator that calls microsofttranslator.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=1.2.3
    - six

  run:
    - python
    - requests >=1.2.3
    - six

test:
  # Python imports
  imports:
    - microsofttranslator

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://openlabs.co.in/
  license: BSD License
  summary: 'Microsoft Translator V2 - Python API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
