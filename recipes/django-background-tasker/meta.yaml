package:
  name: django-background-tasker
  version: "0.1"

source:
  fn: django-background-tasker-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-background-tasker/django-background-tasker-0.1.tar.gz
  md5: c318806a108c19e62be28e3d7cfddbad
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-background-tasker = django-background-tasker:main
    #
    # Would create an entry point called django-background-tasker that calls django-background-tasker.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyzmq

  run:
    - python
    - pyzmq

test:
  # Python imports
  imports:
    - background_tasker
    - background_tasker.conf
    - background_tasker.management
    - background_tasker.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/leonsmith/django-background-tasker/
  license: BSD
  summary: 'A simple django extension for running background tasks using zeromq'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
