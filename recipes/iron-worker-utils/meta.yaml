package:
  name: iron-worker-utils
  version: "0.1.1"

source:
  fn: iron-worker-utils-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/i/iron-worker-utils/iron-worker-utils-0.1.1.tar.gz
  md5: 3378c16c82e6967a5897bb831e504661
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - iron-worker-utils = iron-worker-utils:main
    #
    # Would create an entry point called iron-worker-utils that calls iron-worker-utils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - iron_worker_utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://mwarkentin.github.io/iron-worker-utils/
  license: irregular
  summary: 'A collection of utilities for writing IronWorker tasks.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
