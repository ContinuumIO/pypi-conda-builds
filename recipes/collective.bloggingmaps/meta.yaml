package:
  name: collective.bloggingmaps
  version: "1.0b2"

source:
  fn: collective.bloggingmaps-1.0b2.zip
  url: https://pypi.python.org/packages/source/c/collective.bloggingmaps/collective.bloggingmaps-1.0b2.zip
  md5: 773d1abc0103d8ffd7b4eb7c81704e28
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.bloggingmaps = collective.bloggingmaps:main
    #
    # Would create an entry point called collective.bloggingmaps that calls collective.bloggingmaps.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.blogging
    - products.maps
    - archetypes.schemaextender
    - archetypes.markerfield

  run:
    - python
    - setuptools
    - setuptools
    - collective.blogging
    - products.maps
    - archetypes.schemaextender
    - archetypes.markerfield

test:
  # Python imports
  imports:
    - collective
    - collective.bloggingmaps

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/collective.bloggingmaps
  license: GPL
  summary: 'An extension for Plone which adds google maps support for collective.blogging package.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
