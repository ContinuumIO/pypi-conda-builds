package:
  name: nxosncrpc
  version: "0.1"

source:
  fn: nxosNCRPC-0.1.tar.gz
  url: https://pypi.python.org/packages/source/n/nxosNCRPC/nxosNCRPC-0.1.tar.gz
  md5: 69e97075a9f605620817c6344bf1fb06
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - nxosncrpc = nxosncrpc:main
    #
    # Would create an entry point called nxosncrpc that calls nxosncrpc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - nxosNCRPC
    - nxosncrpc

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/DavidJohnGee/nxosNCRPC
  license: Apache License 2.0
  summary: 'Python library to generate Cisco Nexus NETCONF RPC messages, hello and close messages'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
