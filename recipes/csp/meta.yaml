package:
  name: csp
  version: "0.1alpha10"

source:
  fn: csp-0.1alpha10.tar.gz
  url: https://pypi.python.org/packages/source/c/csp/csp-0.1alpha10.tar.gz
  md5: 5dcf517349c88d2416ae4b670f908d20
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - csp = csp:main
    #
    # Would create an entry point called csp that calls csp.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - csp
    - csp.eventlet
    - csp.twisted

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.orbited.org
  license: MIT License
  summary: 'An implemention of the Comet Session protocol specification for twisted: http://orbited.org/blog/files/cps.html'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
