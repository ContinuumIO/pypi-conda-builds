package:
  name: hurry.file
  version: "1.2.1"

source:
  fn: hurry.file-1.2.1.tar.gz
  url: https://pypi.python.org/packages/source/h/hurry.file/hurry.file-1.2.1.tar.gz
  md5: 101a9500fecbef8bb62d1b6eee49aeca
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hurry.file = hurry.file:main
    #
    # Would create an entry point called hurry.file that calls hurry.file.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zodb3
    - zope.interface
    - zope.schema
    - zope.component
    - zope.testing
    - zope.publisher
    - zope.app.form
    - zope.session
    - zope.app.testing
    - zope.app.container

  run:
    - python
    - setuptools
    - zodb3
    - zope.interface
    - zope.schema
    - zope.component
    - zope.testing
    - zope.publisher
    - zope.app.form
    - zope.session
    - zope.app.testing
    - zope.app.container

test:
  # Python imports
  imports:
    - hurry
    - hurry.file
    - hurry.file.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: ZPL 2.1
  summary: 'hurry.file is an advanced Zope 3 file widget which tries its best to behave\nlike other widgets, even when the form is redisplayed due to a validation\nerror. It also has built-in support for fast Apache-based file uploads\nand downloads through Tramline.\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
