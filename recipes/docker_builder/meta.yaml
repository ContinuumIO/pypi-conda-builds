package:
  name: docker_builder
  version: "0.2"

source:
  fn: docker_builder-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/docker_builder/docker_builder-0.2.tar.gz
  md5: 2208c181e0c7197e6dabd7fd4ee78500
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - docker_builder = docker_builder:main
    #
    # Would create an entry point called docker_builder that calls docker_builder.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - docopt
    - pyyaml

  run:
    - python
    - docopt
    - pyyaml

test:
  # Python imports
  imports:
    - docker_builder

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Wiredcraft/docker-builder
  license: MIT
  summary: 'Builder for docker containers'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
