package:
  name: plone.app.protect
  version: "1.0b1"

source:
  fn: plone.app.protect-1.0b1.tar.gz
  url: https://pypi.python.org/packages/source/p/plone.app.protect/plone.app.protect-1.0b1.tar.gz
  md5: 65322b5550aa12102ba01d4d46a0843c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.app.protect = plone.app.protect:main
    #
    # Would create an entry point called plone.app.protect that calls plone.app.protect.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - plone.keyring

  run:
    - python
    - setuptools
    - plone.keyring

test:
  # Python imports
  imports:
    - plone
    - plone.app
    - plone.app.protect
    - plone.app.protect.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/plone/plone.app.protect
  license: GPL
  summary: 'Security for browser forms'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
