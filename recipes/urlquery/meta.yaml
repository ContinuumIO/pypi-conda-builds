package:
  name: urlquery
  version: "2.0.1"

source:
  fn: urlquery-2.0.1.tar.gz
  url: https://pypi.python.org/packages/source/u/urlquery/urlquery-2.0.1.tar.gz
  md5: 9c06485fb835dafae8c376a204a4f68f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - urlquery = urlquery:main
    #
    # Would create an entry point called urlquery that calls urlquery.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - requests
    - python-dateutil

  run:
    - python
    - requests
    - python-dateutil

test:
  # Python imports
  imports:
    - urlquery

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/CIRCL/urlquery-py
  license: GNU GPLv3
  summary: 'Python API to access urlquery v2'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
