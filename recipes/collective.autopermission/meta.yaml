package:
  name: collective.autopermission
  version: "1.0b2"

source:
  fn: collective.autopermission-1.0b2.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.autopermission/collective.autopermission-1.0b2.tar.gz
  md5: f25d76ce50a7b64eaa0bc147123926f4
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.autopermission = collective.autopermission:main
    #
    # Would create an entry point called collective.autopermission that calls collective.autopermission.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.autopermission

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/collective.autopermission
  license: BSD
  summary: 'Create permissions in Zope 2 on demand when a <permission /> directive is used'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
