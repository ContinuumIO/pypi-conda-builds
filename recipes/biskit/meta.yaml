package:
  name: biskit
  version: "2.4"

source:
  fn: biskit-2.4.tar.gz
  url: https://pypi.python.org/packages/source/b/biskit/biskit-2.4.tar.gz
  md5: 8e45c9fc90caa2b91a415817e88c1aea
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - biskit = biskit:main
    #
    # Would create an entry point called biskit that calls biskit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - Biskit
    - Biskit.Dock
    - Biskit.Mod
    - Biskit.PVM
    - Biskit.Statistics
    - biskit

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://biskit.pasteur.fr
  license: GNU General Public License (GPL)
  summary: 'A Python platform for structural bioinformatics'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
