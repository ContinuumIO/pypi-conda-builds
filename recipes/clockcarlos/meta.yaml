package:
  name: clockcarlos
  version: "0.1"

source:
  fn: clockcarlos-0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/clockcarlos/clockcarlos-0.1.tar.gz
  md5: 15e29ac7084d7d73654b94295028756c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - clockcarlos = clockcarlos:main
    #
    # Would create an entry point called clockcarlos that calls clockcarlos.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - clockcarlos

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: www.google.com
  license: MIT License
  summary: 'so you can have a visible timer on your terminal'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
