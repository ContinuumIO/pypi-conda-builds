package:
  name: django-iframetoolbox
  version: "0.2.1"

source:
  fn: django-iframetoolbox-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-iframetoolbox/django-iframetoolbox-0.2.1.tar.gz
  md5: 508f0f87b937a375f960beaca5b1083b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-iframetoolbox = django-iframetoolbox:main
    #
    # Would create an entry point called django-iframetoolbox that calls django-iframetoolbox.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - iframetoolbox

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.aurumsocial.com
  license: BSD License
  summary: 'Django set of tools to work inside iframe'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
