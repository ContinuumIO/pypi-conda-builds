package:
  name: document_history
  version: "0.1"

source:
  fn: document_history-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/document_history/document_history-0.1.tar.gz
  md5: 585514869f2157ee35a2fc395e64fc69
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - document_history = document_history:main
    #
    # Would create an entry point called document_history that calls document_history.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - mongoengine
    - blinker

  run:
    - python
    - mongoengine
    - blinker

test:
  # Python imports
  imports:
    - document_history

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Martikos/document_history
  license: MIT License
  summary: 'MongoEngine Document Versionning for Humans'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
