package:
  name: silva.core.xml
  version: "3.0.1"

source:
  fn: silva.core.xml-3.0.1.tar.gz
  url: https://pypi.python.org/packages/source/s/silva.core.xml/silva.core.xml-3.0.1.tar.gz
  md5: c08c907fd18ad674c48cecb513bebe09
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - silva.core.xml = silva.core.xml:main
    #
    # Would create an entry point called silva.core.xml that calls silva.core.xml.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - five.grok
    - setuptools

  run:
    - python
    - setuptools
    - five.grok
    - setuptools

test:
  # Python imports
  imports:
    - silva
    - silva.core
    - silva.core.xml

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/silvacms/silva.core.xml
  license: BSD License
  summary: 'Support to export Silva content into XML'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
