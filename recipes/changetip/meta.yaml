package:
  name: changetip
  version: "0.3.5"

source:
  fn: changetip-0.3.5.tar.gz
  url: https://pypi.python.org/packages/source/c/changetip/changetip-0.3.5.tar.gz
  md5: ae207d258bed2cb2c1c0f76cec448c26
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - changetip = changetip:main
    #
    # Would create an entry point called changetip that calls changetip.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - changetip
    - changetip.bots

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/changecoin/changetip-python
  license: MIT License
  summary: 'ChangeTip helper library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
