package:
  name: cykooz.djangopaste
  version: "1.0"

source:
  fn: cykooz.djangopaste-1.0.tar.gz
  url: https://pypi.python.org/packages/source/c/cykooz.djangopaste/cykooz.djangopaste-1.0.tar.gz
  md5: ec02f465064f9fcf1656b88a4b43d5fe
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cykooz.djangopaste = cykooz.djangopaste:main
    #
    # Would create an entry point called cykooz.djangopaste that calls cykooz.djangopaste.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - distribute
    - django
    - pastescript

  run:
    - python
    - setuptools
    - distribute
    - django
    - pastescript

test:
  # Python imports
  imports:
    - cykooz
    - cykooz.djangopaste

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/cykooz/cykooz.djangopaste
  license: BSD-derived
  summary: 'A wrapper that allows us to run Django under PasteDeploy'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
