package:
  name: ezpyinline
  version: "0.1"

source:
  fn: ezpyinline-0.1.tar.gz
  url: https://pypi.python.org/packages/source/e/ezpyinline/ezpyinline-0.1.tar.gz
  md5: 0d80180cbe365c925bf149ebfaf739ab
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ezpyinline = ezpyinline:main
    #
    # Would create an entry point called ezpyinline that calls ezpyinline.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - ezpyinline

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://ezpyinline.sf.net
  license: Artistic License
  summary: 'Easy embedded Inline C for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
