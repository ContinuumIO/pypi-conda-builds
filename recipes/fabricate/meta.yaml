package:
  name: fabricate
  version: "(latest release)"

source:
  fn: fabricate-1.26.tar.gz
  url: https://pypi.python.org/packages/source/f/fabricate/fabricate-1.26.tar.gz
  md5: e02128a8fa42a94b950998cb9a463b46
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fabricate = fabricate:main
    #
    # Would create an entry point called fabricate that calls fabricate.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - fabricate

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: fabricator
  summary: 'Build tool that finds dependencies automatically for any language.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
