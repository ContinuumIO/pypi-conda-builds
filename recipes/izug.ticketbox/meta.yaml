package:
  name: izug.ticketbox
  version: "4.7.0"

source:
  fn: izug.ticketbox-4.7.0.zip
  url: https://pypi.python.org/packages/source/i/izug.ticketbox/izug.ticketbox-4.7.0.zip
  md5: 68337f622991e14f7c7d763d7a295555
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - izug.ticketbox = izug.ticketbox:main
    #
    # Would create an entry point called izug.ticketbox that calls izug.ticketbox.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - accesscontrol
    - acquisition
    - plone
    - products.atcontenttypes
    - products.atreferencebrowserwidget
    - products.archetypes
    - products.cmfcore
    - products.cmfplone
    - products.datagridfield
    - products.statusmessages
    - zodb3
    - zope2
    - ftw.notification.base
    - ftw.notification.email
    - ftw.tabbedview
    - ftw.table
    - ftw.upgrade >=1.14.0
    - plone.principalsource
    - zope.annotation
    - zope.app.component
    - zope.app.container
    - zope.app.pagetemplate
    - zope.cachedescriptors
    - zope.component
    - zope.contentprovider
    - zope.event
    - zope.i18n
    - zope.i18nmessageid
    - zope.interface
    - zope.lifecycleevent
    - zope.schema
    - zope.viewlet
    - xlsxwriter

  run:
    - python
    - setuptools
    - setuptools
    - accesscontrol
    - acquisition
    - plone
    - products.atcontenttypes
    - products.atreferencebrowserwidget
    - products.archetypes
    - products.cmfcore
    - products.cmfplone
    - products.datagridfield
    - products.statusmessages
    - zodb3
    - zope2
    - ftw.notification.base
    - ftw.notification.email
    - ftw.tabbedview
    - ftw.table
    - ftw.upgrade >=1.14.0
    - plone.principalsource
    - zope.annotation
    - zope.app.component
    - zope.app.container
    - zope.app.pagetemplate
    - zope.cachedescriptors
    - zope.component
    - zope.contentprovider
    - zope.event
    - zope.i18n
    - zope.i18nmessageid
    - zope.interface
    - zope.lifecycleevent
    - zope.schema
    - zope.viewlet
    - xlsxwriter

test:
  # Python imports
  imports:
    - izug
    - izug.ticketbox
    - izug.ticketbox.browser
    - izug.ticketbox.browser.notification
    - izug.ticketbox.browser.tabbed
    - izug.ticketbox.content
    - izug.ticketbox.tests
    - izug.ticketbox.upgrades
    - izug.ticketbox.upgrades.20150501165936_migrate_description_to_ticket_description

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/4teamwork/izug.ticketbox
  license: GPL2
  summary: 'A tracker-like task management system for plone.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
