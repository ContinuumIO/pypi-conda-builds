package:
  name: galaxyservers
  version: "0.1.1"

source:
  fn: galaxyservers-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/g/galaxyservers/galaxyservers-0.1.1.tar.gz
  md5: 9cf87fae9daa9df15fe17a30cce0ca93
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - galaxyservers = galaxyservers:main
    #
    # Would create an entry point called galaxyservers that calls galaxyservers.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - galaxyservers
    - galaxyservers.servers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/wyleung/galaxyservers
  license: MIT License
  summary: 'Runners for Galaxyproject'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
