package:
  name: redturtle.deletepolicy
  version: "2.1.1"

source:
  fn: redturtle.deletepolicy-2.1.1.zip
  url: https://pypi.python.org/packages/source/r/redturtle.deletepolicy/redturtle.deletepolicy-2.1.1.zip
  md5: afaa46c55965521322ecfa1aa026641e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - redturtle.deletepolicy = redturtle.deletepolicy:main
    #
    # Would create an entry point called redturtle.deletepolicy that calls redturtle.deletepolicy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - redturtle
    - redturtle.deletepolicy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/redturtle.deletepolicy
  license: GPL
  summary: 'Modified policy for deleting objects in Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
