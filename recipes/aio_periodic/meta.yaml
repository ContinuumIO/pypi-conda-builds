package:
  name: aio_periodic
  version: "0.1.6"

source:
  fn: aio_periodic-0.1.6.tar.gz
  url: https://pypi.python.org/packages/source/a/aio_periodic/aio_periodic-0.1.6.tar.gz
  md5: 06874f6962ae7de798cbfc58dc8f3cd1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - aio_periodic = aio_periodic:main
    #
    # Would create an entry point called aio_periodic that calls aio_periodic.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - aio_periodic

  run:
    - python
    - aio_periodic

test:
  # Python imports
  imports:
    - aio_periodic

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Lupino/python-aio-periodic
  license: UNKNOWN
  summary: 'The periodic task system client for python3 base on asyncio'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
