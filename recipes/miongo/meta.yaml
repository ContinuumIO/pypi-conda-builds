package:
  name: miongo
  version: "0.1.1"

source:
  fn: miongo-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/m/miongo/miongo-0.1.1.tar.gz
  md5: 667627f823c228fcafe1baf46267c7b0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - miongo = miongo:main
    #
    # Would create an entry point called miongo that calls miongo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - pymongo

  run:
    - python
    - pymongo

test:
  # Python imports
  imports:
    - miongo
    - miongo.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/miongo/
  license: MIT License
  summary: 'Yet another MongoDB ORM for Python. Signals included!'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
