package:
  name: digimat.rws
  version: "0.1.0"

source:
  fn: digimat.rws-0.1.0.zip
  url: https://pypi.python.org/packages/source/d/digimat.rws/digimat.rws-0.1.0.zip
  md5: c156ad6fc5287c2027affb8b1627ff3d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - digimat.rws = digimat.rws:main
    #
    # Would create an entry point called digimat.rws that calls digimat.rws.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - xlwt
    - pil
    - setuptools

  run:
    - python
    - xlwt
    - pil
    - setuptools

test:
  # Python imports
  imports:
    - digimat
    - digimat.rws

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: PSF
  summary: 'Digimat RWS Client'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
