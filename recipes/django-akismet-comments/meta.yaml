package:
  name: django-akismet-comments
  version: "0.1"

source:
  fn: django-akismet-comments-0.1.zip
  url: https://pypi.python.org/packages/source/d/django-akismet-comments/django-akismet-comments-0.1.zip
  md5: c43898ee4b2c1ab5eed376bfcb287bc6
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-akismet-comments = django-akismet-comments:main
    #
    # Would create an entry point called django-akismet-comments that calls django-akismet-comments.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - akismet

  run:
    - python
    - setuptools
    - setuptools
    - akismet

test:
  # Python imports
  imports:
    - django_akismet_comments

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: BSD
  summary: 'Django moderator for checking django.contrib.comments spam against akismet service.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
