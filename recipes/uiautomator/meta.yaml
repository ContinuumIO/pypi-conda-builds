package:
  name: uiautomator
  version: "0.1.35"

source:
  fn: uiautomator-0.1.35.tar.gz
  url: https://pypi.python.org/packages/source/u/uiautomator/uiautomator-0.1.35.tar.gz
  md5: df5da4d1a966ab7a1d5e7dceafcc719a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - uiautomator = uiautomator:main
    #
    # Would create an entry point called uiautomator that calls uiautomator.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - urllib3 >=1.7.1

  run:
    - python
    - urllib3 >=1.7.1

test:
  # Python imports
  imports:
    - uiautomator

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/xiaocong/uiautomator
  license: MIT
  summary: 'Python Wrapper for Android UiAutomator test tool'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
