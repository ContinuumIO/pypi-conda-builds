package:
  name: maple
  version: "0.1.92"

source:
  fn: maple-0.1.92.tar.gz
  url: https://pypi.python.org/packages/source/m/maple/maple-0.1.92.tar.gz
  md5: 80f21eae910dbb4e64b84cad89b278c0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - maple = maple:main
    #
    # Would create an entry point called maple that calls maple.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - events
    - netkit
    - protobuf

  run:
    - python
    - events
    - netkit
    - protobuf

test:
  # Python imports
  imports:
    - maple
    - maple.trigger
    - maple.worker

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dantezhu/maple
  license: MIT
  summary: 'reliable, scalable, distributed server framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
