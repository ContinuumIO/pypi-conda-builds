package:
  name: pelican-db
  version: "0.0.1b0"

source:
  fn: pelican-db-0.0.1b0.tar.gz
  url: https://pypi.python.org/packages/source/p/pelican-db/pelican-db-0.0.1b0.tar.gz
  md5: bc271fc6c5494e4b161b93e1790089d5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pelican-db = pelican-db:main
    #
    # Would create an entry point called pelican-db that calls pelican-db.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://okfn.org
  license: MIT License
  summary: u'A CSV-backed database for Pelican sites.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
