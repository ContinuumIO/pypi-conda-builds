package:
  name: sphinx-pyreverse
  version: "0.0.12"

source:
  fn: sphinx-pyreverse-0.0.12.tar.gz
  url: https://pypi.python.org/packages/source/s/sphinx-pyreverse/sphinx-pyreverse-0.0.12.tar.gz
  md5: 1bb07e5a9a0c0ee00be4c05c24785d61
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sphinx-pyreverse = sphinx-pyreverse:main
    #
    # Would create an entry point called sphinx-pyreverse that calls sphinx-pyreverse.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - sphinx_pyreverse

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/alendit/sphinx-pyreverse
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'A simple sphinx extension to generate UML diagrams with pyreverse'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
