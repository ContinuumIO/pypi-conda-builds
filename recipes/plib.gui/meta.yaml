package:
  name: plib.gui
  version: "0.9.4"

source:
  fn: plib.gui-0.9.4.tar.gz
  url: https://pypi.python.org/packages/source/p/plib.gui/plib.gui-0.9.4.tar.gz
  md5: 05ed20360b173cb2710a1c31f81618e6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plib.gui = plib.gui:main
    #
    # Would create an entry point called plib.gui that calls plib.gui.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - plib
    - plib.gui
    - plib.gui._base
    - plib.gui._toolkits
    - plib.gui._toolkits._gtk
    - plib.gui._toolkits._kde
    - plib.gui._toolkits._kde4
    - plib.gui._toolkits._pyside
    - plib.gui._toolkits._qt
    - plib.gui._toolkits._qt4
    - plib.gui._toolkits._wx
    - plib.gui._widgets
    - plib.test
    - plib.test.gui

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/plib.gui
  license: GNU General Public License v2 (GPLv2)
  summary: 'A simple Python GUI framework.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
