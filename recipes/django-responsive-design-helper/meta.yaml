package:
  name: django-responsive-design-helper
  version: "0.2.0"

source:
  fn: django-responsive-design-helper-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-responsive-design-helper/django-responsive-design-helper-0.2.0.tar.gz
  md5: 2b4e21cf4581a27fb72ad40a7c72d528
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-responsive-design-helper = django-responsive-design-helper:main
    #
    # Would create an entry point called django-responsive-design-helper that calls django-responsive-design-helper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - responsive_design_helper

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/tswicegood/django-responsive-design-helper/
  license: BSD License
  summary: 'Helper for testing responsive designs in Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
