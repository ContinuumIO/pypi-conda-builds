package:
  name: hgshelve
  version: "0.1"

source:
  fn: hgshelve-0.1.tar.gz
  url: https://pypi.python.org/packages/source/h/hgshelve/hgshelve-0.1.tar.gz
  md5: d58424eb59b7a751a4c5f2a5ea2133c7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hgshelve = hgshelve:main
    #
    # Would create an entry point called hgshelve that calls hgshelve.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - mercurial >=1.9.0

  run:
    - python
    - mercurial >=1.9.0

test:
  # Python imports
  imports:
    - hgshelve

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/tksoh/hgshelve
  license: GNU General Public License (GPL)
  summary: 'Shelve Extension for Mercurial'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
