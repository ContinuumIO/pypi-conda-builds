package:
  name: aldryn-gallery-timed
  version: "0.0.1"

source:
  fn: aldryn-gallery-timed-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/a/aldryn-gallery-timed/aldryn-gallery-timed-0.0.1.tar.gz
  md5: 99ea213a7d45a16793b82c29f0b4a07b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - aldryn-gallery-timed = aldryn-gallery-timed:main
    #
    # Would create an entry point called aldryn-gallery-timed that calls aldryn-gallery-timed.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - aldryn-gallery

  run:
    - python
    - aldryn-gallery

test:
  # Python imports
  imports:
    - aldryn_gallery_timed
    - aldryn_gallery_timed.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/aldryn/aldryn-gallery-timed
  license: BSD License
  summary: 'Aldryn Gallery Slides that can be timed'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
