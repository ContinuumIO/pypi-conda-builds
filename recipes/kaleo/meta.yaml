package:
  name: kaleo
  version: "1.5"

source:
  fn: kaleo-1.5.tar.gz
  url: https://pypi.python.org/packages/source/k/kaleo/kaleo-1.5.tar.gz
  md5: 89cd34f11fa4bd5e75964b20f686851e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kaleo = kaleo:main
    #
    # Would create an entry point called kaleo that calls kaleo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-appconf >=0.5
    - django-user-accounts >=1.0c4

  run:
    - python
    - django-appconf >=0.5
    - django-user-accounts >=1.0c4

test:
  # Python imports
  imports:
    - kaleo
    - kaleo.management
    - kaleo.management.commands
    - kaleo.templatetags
    - kaleo.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/eldarion/kaleo
  license: BSD License
  summary: 'a user to user join invitations app'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
