package:
  name: bugspots
  version: "0.1"

source:
  fn: bugspots-0.1.tar.gz
  url: https://pypi.python.org/packages/source/b/bugspots/bugspots-0.1.tar.gz
  md5: fb1203a8cba7f49d77e0e306c90ef19c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bugspots = bugspots:main
    #
    # Would create an entry point called bugspots that calls bugspots.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - gitpython >=0.3

  run:
    - python
    - gitpython >=0.3

test:
  # Python imports
  imports:
    - bugspots

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/bugspots
  license: ISC License (ISCL)
  summary: 'Identify hot spots in a codebase with the bug prediction algorithm used at Google.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
