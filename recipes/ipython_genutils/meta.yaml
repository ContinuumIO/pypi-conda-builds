package:
  name: ipython_genutils
  version: "0.1.0"

source:
  fn: ipython_genutils-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/i/ipython_genutils/ipython_genutils-0.1.0.tar.gz
  md5: 9a8afbe0978adbcbfcb3b35b2d015a56
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ipython_genutils = ipython_genutils:main
    #
    # Would create an entry point called ipython_genutils that calls ipython_genutils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - ipython_genutils
    - ipython_genutils.testing
    - ipython_genutils.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://ipython.org
  license: BSD License
  summary: 'Vestigial utilities from IPython'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
