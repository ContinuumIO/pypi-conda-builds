package:
  name: collective.contentrules.linguatarget
  version: "0.5"

source:
  fn: collective.contentrules.linguatarget-0.5.zip
  url: https://pypi.python.org/packages/source/c/collective.contentrules.linguatarget/collective.contentrules.linguatarget-0.5.zip
  md5: 16b87a32712fdab274b6409f475d61e7
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.contentrules.linguatarget = collective.contentrules.linguatarget:main
    #
    # Would create an entry point called collective.contentrules.linguatarget that calls collective.contentrules.linguatarget.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.contentrules
    - collective.contentrules.linguatarget
    - collective.contentrules.linguatarget.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/collective.contentrules.linguatarget
  license: GPL
  summary: "This package adds a custom content rule that takes the target folder's translations into account"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
