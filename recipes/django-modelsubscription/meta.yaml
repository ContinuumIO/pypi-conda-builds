package:
  name: django-modelsubscription
  version: "0.0.1"

source:
  fn: django-modelsubscription-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-modelsubscription/django-modelsubscription-0.0.1.tar.gz
  md5: 51700910e9f91a9bc0ed1eca695bdad9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-modelsubscription = django-modelsubscription:main
    #
    # Would create an entry point called django-modelsubscription that calls django-modelsubscription.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - distribute

  run:
    - python
    - distribute

test:
  # Python imports
  imports:
    - modelsubscription
    - modelsubscription.handlers
    - testing

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kaleidos/django-modelsubscription
  license: BSD License
  summary: 'Generic model mixin for user or email subscription to objects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
