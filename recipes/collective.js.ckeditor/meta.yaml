package:
  name: collective.js.ckeditor
  version: "4.0.3"

source:
  fn: collective.js.ckeditor-4.0.3.zip
  url: https://pypi.python.org/packages/source/c/collective.js.ckeditor/collective.js.ckeditor-4.0.3.zip
  md5: 6adec0562093da7a3c0b712a68fb0834
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.js.ckeditor = collective.js.ckeditor:main
    #
    # Would create an entry point called collective.js.ckeditor that calls collective.js.ckeditor.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.js
    - collective.js.ckeditor
    - collective.js.ckeditor.sample
    - collective.js.ckeditor.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/toutpt/collective.js.ckeditor
  license: GNU General Public License (GPL)
  summary: 'Just register ckeditor resources to Plone registry'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
