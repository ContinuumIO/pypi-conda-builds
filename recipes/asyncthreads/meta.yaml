package:
  name: asyncthreads
  version: "1.5.4"

source:
  fn: asyncthreads-1.5.4.tar.bz2
  url: https://pypi.python.org/packages/source/a/asyncthreads/asyncthreads-1.5.4.tar.bz2
  md5: ed7c4c659b7494a6ff0fa7869fcaa88b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - asyncthreads = asyncthreads:main
    #
    # Would create an entry point called asyncthreads that calls asyncthreads.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - asyncthreads

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/agillis/asyncthreads
  license: MIT License
  summary: 'asyncthreads: asynchronous thread utility objects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
