package:
  name: lizard-map
  version: "5.2"

source:
  fn: lizard-map-5.2.zip
  url: https://pypi.python.org/packages/source/l/lizard-map/lizard-map-5.2.zip
  md5: 6346f30344964b701b0a7da1dd55602d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lizard-map = lizard-map:main
    #
    # Would create an entry point called lizard-map that calls lizard-map.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6,<1.7
    - pillow
    - django-extensions
    - django-jsonfield
    - django-nose
    - django-piston
    - djangorestframework >=2.0
    - iso8601
    - lizard-help
    - lizard-security
    - lizard-ui >=5.0
    - lizard-wms
    - pkginfo
    - python-dateutil
    - pytz
    - south
    - requests
    - django-appconf
    - translations

  run:
    - python
    - setuptools
    - django >=1.6,<1.7
    - pillow
    - django-extensions
    - django-jsonfield
    - django-nose
    - django-piston
    - djangorestframework >=2.0
    - iso8601
    - lizard-help
    - lizard-security
    - lizard-ui >=5.0
    - lizard-wms
    - pkginfo
    - python-dateutil
    - pytz
    - south
    - requests
    - django-appconf
    - translations

test:
  # Python imports
  imports:
    - lizard_map

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.nelen-schuurmans.nl/lizard/
  license: LGPL
  summary: 'Basic map setup for lizard web sites'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
