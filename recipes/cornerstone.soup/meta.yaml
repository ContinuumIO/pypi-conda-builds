package:
  name: cornerstone.soup
  version: "2.4.1.1"

source:
  fn: cornerstone.soup-2.4.1.1.tar.gz
  url: https://pypi.python.org/packages/source/c/cornerstone.soup/cornerstone.soup-2.4.1.1.tar.gz
  md5: 3ebea6ff904fa3183ef7090268279ced
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cornerstone.soup = cornerstone.soup:main
    #
    # Would create an entry point called cornerstone.soup that calls cornerstone.soup.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.catalog
    - uuid
    - zope.annotation

  run:
    - python
    - setuptools
    - zope.catalog
    - uuid
    - zope.annotation

test:
  # Python imports
  imports:
    - cornerstone
    - cornerstone.soup
    - cornerstone.soup.browser
    - cornerstone.soup.interfaces
    - cornerstone.soup.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://dev.plone.org/collective/browser/cornerstone.soup
  license: BSD License
  summary: 'Container with queryable Records for Zope 2'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
