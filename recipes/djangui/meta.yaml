package:
  name: djangui
  version: "0.0.01"

source:
  fn: djangui-0.0.01.tar.gz
  url: https://pypi.python.org/packages/source/d/djangui/djangui-0.0.01.tar.gz
  md5: 7fd20ffeeedd8bda3cff6eee731a7cf7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangui = djangui:main
    #
    # Would create an entry point called djangui that calls djangui.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - 
    - djangui

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/chris7/djangui
  license: GPL3
  summary: 'A python tool to convert an argparse script to a django app'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
