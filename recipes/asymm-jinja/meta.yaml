package:
  name: asymm-jinja
  version: "0.2.1-201406061624"

source:
  fn: asymm-jinja-0.2.1-201406061624.tar.gz
  url: https://pypi.python.org/packages/source/a/asymm-jinja/asymm-jinja-0.2.1-201406061624.tar.gz
  md5: 37f29354a0dd9de8c655db6b6a3987d2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - asymm-jinja = asymm-jinja:main
    #
    # Would create an entry point called asymm-jinja that calls asymm-jinja.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4.5
    - jinja2 >=2.7
    - pytz

  run:
    - python
    - django >=1.4.5
    - jinja2 >=2.7
    - pytz

test:
  # Python imports
  imports:
    - asymm_jinja
    - asymm_jinja.tags
    - asymm_jinja.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/AsymmetricVentures/asym-jinja
  license: GNU General Public License v2 (GPLv2)
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
