package:
  name: cadquery
  version: "0.1.8"

source:
  fn: cadquery-0.1.8.zip
  url: https://pypi.python.org/packages/source/c/cadquery/cadquery-0.1.8.zip
  md5: 99b0c80385bfeaed194227173c2f6b51
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cadquery = cadquery:main
    #
    # Would create an entry point called cadquery that calls cadquery.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - cadquery
    - cadquery.contrib
    - cadquery.freecad_impl
    - cadquery.plugins
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dcowden/cadquery
  license: GNU Lesser General Public License v3 (LGPLv3)
  summary: 'CadQuery is a parametric  scripting language for creating and traversing CAD models'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
