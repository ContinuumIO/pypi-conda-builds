package:
  name: boorupy
  version: "0.1.6"

source:
  fn: BooruPy-0.1.6.tar.gz
  url: https://pypi.python.org/packages/source/B/BooruPy/BooruPy-0.1.6.tar.gz
  md5: 02b04198d8c50faaa10bdcd8146334fd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - boorupy = boorupy:main
    #
    # Would create an entry point called boorupy that calls boorupy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - BooruPy
    - boorupy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/Kasoki/BooruPy
  license: GNU GENERAL PUBLIC LICENSE
  summary: 'BooruPy is a simple library for interacting with various image board "Booru" systems.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
