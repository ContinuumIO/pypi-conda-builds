package:
  name: dictdiffer
  version: "0.4.0"

source:
  fn: dictdiffer-0.4.0.tar.gz
  url: https://pypi.python.org/packages/source/d/dictdiffer/dictdiffer-0.4.0.tar.gz
  md5: 3a5a6fef59226330e36460a47748a693
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dictdiffer = dictdiffer:main
    #
    # Would create an entry point called dictdiffer that calls dictdiffer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - dictdiffer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/inveniosoftware/dictdiffer
  license: MIT License
  summary: 'Dictdiffer is a library that helps you to diff and patch dictionaries.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
