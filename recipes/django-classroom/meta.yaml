package:
  name: django-classroom
  version: "0.1.6"

source:
  fn: django-classroom-0.1.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-classroom/django-classroom-0.1.6.tar.gz
  md5: 2ea78ec398ab3b8e213672714825996f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-classroom = django-classroom:main
    #
    # Would create an entry point called django-classroom that calls django-classroom.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - classroom

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/powellc/django-classroom
  license: BSD License
  summary: 'An application for managing classroom details on a school website.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
