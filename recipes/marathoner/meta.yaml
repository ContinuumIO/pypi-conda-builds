package:
  name: marathoner
  version: "1.2.13"

source:
  fn: marathoner-1.2.13.zip
  url: https://pypi.python.org/packages/source/m/marathoner/marathoner-1.2.13.zip
  md5: 03482f210cfd5e49e8d1a23c446deeb8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - marathoner = marathoner:main
    #
    # Would create an entry point called marathoner that calls marathoner.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six

  run:
    - python
    - six

test:
  # Python imports
  imports:
    - marathoner
    - marathoner.commands
    - marathoner.contest
    - marathoner.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Mimino666/tc-marathoner
  license: marathon-proxy-manager
  summary: 'Testing tool for TopCoder marathon matches.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
