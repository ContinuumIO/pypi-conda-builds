package:
  name: haleasy
  version: "0.3.2"

source:
  fn: HALEasy-0.3.2.tar.gz
  url: https://pypi.python.org/packages/source/H/HALEasy/HALEasy-0.3.2.tar.gz
  md5: 48f0de9f7eaa3ca327debdba0ae2c527
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - haleasy = haleasy:main
    #
    # Would create an entry point called haleasy that calls haleasy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - dougrain >=0.5.1
    - requests >=2.5.1
    - uritemplate >=0.6

  run:
    - python
    - dougrain >=0.5.1
    - requests >=2.5.1
    - uritemplate >=0.6

test:
  # Python imports
  imports:
    - haleasy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/mattclarkdotnet/haleasy
  license: MIT License
  summary: 'A HAL client which is very short and simple'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
