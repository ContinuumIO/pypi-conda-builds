package:
  name: pyconnie
  version: "1.0"

source:
  fn: pyconnie-1.0.tar.gz
  url: https://pypi.python.org/packages/source/p/pyconnie/pyconnie-1.0.tar.gz
  md5: 0819c99451b773780c4ad5d440a5eb63
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pyconnie = pyconnie:main
    #
    # Would create an entry point called pyconnie that calls pyconnie.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy ==1.8.2
    - scipy ==0.14.0

  run:
    - python
    - numpy ==1.8.2
    - scipy ==0.14.0

test:
  # Python imports
  imports:
    - pyconnie

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.github.com/akellehe/pyconnie
  license: UNKNOWN
  summary: 'An implementation of the ConNIe'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
