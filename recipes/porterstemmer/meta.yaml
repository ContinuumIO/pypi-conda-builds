package:
  name: porterstemmer
  version: "0.5"

source:
  fn: PorterStemmer-0.5.tar.gz
  url: https://pypi.python.org/packages/source/P/PorterStemmer/PorterStemmer-0.5.tar.gz
  md5: 60abd9947002163c6e99fccb6076a041
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - porterstemmer = porterstemmer:main
    #
    # Would create an entry point called porterstemmer that calls porterstemmer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - porterstemmer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/methane/porterstemmer/
  license: UNKNOWN
  summary: 'Porter Stemmer for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
