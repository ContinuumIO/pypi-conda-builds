package:
  name: gae-ember-rest
  version: "0.0.5"

source:
  fn: gae-ember-rest-0.0.5.tar.gz
  url: https://pypi.python.org/packages/source/g/gae-ember-rest/gae-ember-rest-0.0.5.tar.gz
  md5: e26899d6f912922bfc48749ebec6c45a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gae-ember-rest = gae-ember-rest:main
    #
    # Would create an entry point called gae-ember-rest that calls gae-ember-rest.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - gae_ember_rest

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kelonye/gae-ember-rest
  license: MIT License
  summary: 'Google App Engine NDB <> Ember Data'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
