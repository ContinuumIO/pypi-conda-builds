package:
  name: littleworkers
  version: "0.3.2"

source:
  fn: littleworkers-0.3.2.tar.gz
  url: https://pypi.python.org/packages/source/l/littleworkers/littleworkers-0.3.2.tar.gz
  md5: 576ef152387c99c0fb5e83f989e7fbfc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - littleworkers = littleworkers:main
    #
    # Would create an entry point called littleworkers that calls littleworkers.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - littleworkers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/toastdriven/littleworkers
  license: BSD License
  summary: 'Little process-based workers to do your bidding.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
