package:
  name: lsb_release_ex
  version: "0.1"

source:
  fn: master.zip
  url: https://github.com/likema/lsb_release_ex/archive/master.zip
  #md5: 
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lsb_release_ex = lsb_release_ex:main
    #
    # Would create an entry point called lsb_release_ex that calls lsb_release_ex.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - lsb_release_ex

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/likema/lsb_release_ex
  license: GNU General Public License v2 (GPLv2)
  summary: 'Ehanced Linux Standard Base version reporting module'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
