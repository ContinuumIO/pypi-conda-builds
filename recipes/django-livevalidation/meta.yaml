package:
  name: django-livevalidation
  version: "0.1.1"

source:
  fn: django-livevalidation-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-livevalidation/django-livevalidation-0.1.1.tar.gz
  md5: 6bc7b19e5c809fb40b7bb04adbd90938
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-livevalidation = django-livevalidation:main
    #
    # Would create an entry point called django-livevalidation that calls django-livevalidation.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - example_project
    - livevalidation
    - livevalidation.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/washingtontimes/django-livevalidation
  license: UNKNOWN
  summary: 'Live validation for Django forms. It validates as you type. Uses scripts from livevalidation.com'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
