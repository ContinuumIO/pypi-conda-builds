package:
  name: jsoncatalogue
  version: "0.1.1"

source:
  fn: jsoncatalogue-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/j/jsoncatalogue/jsoncatalogue-0.1.1.tar.gz
  md5: f351e935df00812a2dde9f88fd00473e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jsoncatalogue = jsoncatalogue:main
    #
    # Would create an entry point called jsoncatalogue that calls jsoncatalogue.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - jsoncatalogue

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/vlcinsky/sh-jsoncatalogue
  license: MIT
  summary: 'Local filesystem based JSON catalogue, usable e.g. with package `jsonschema`'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
