package:
  name: docopt-cmd
  version: "0.0.1"

source:
  fn: docopt-cmd-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/docopt-cmd/docopt-cmd-0.0.1.tar.gz
  md5: 7f0f13c2752cf5fe131d5bf9b942fe3b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - docopt-cmd = docopt-cmd:main
    #
    # Would create an entry point called docopt-cmd that calls docopt-cmd.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - docopt

  run:
    - python
    - docopt

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://docopt.org
  license: MIT License
  summary: 'Pythonic commands and subcommands for docopt'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
