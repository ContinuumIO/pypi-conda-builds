package:
  name: glamkit-stopspam
  version: "0.5.0"

source:
  fn: glamkit-stopspam-0.5.0.tar.gz
  url: https://pypi.python.org/packages/source/g/glamkit-stopspam/glamkit-stopspam-0.5.0.tar.gz
  md5: d65b8ba34a28bff766c4fc6c1d671bc1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - glamkit-stopspam = glamkit-stopspam:main
    #
    # Would create an entry point called glamkit-stopspam that calls glamkit-stopspam.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - stopspam
    - stopspam.forms

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/glamkit/glamkit-stopspam
  license: BSD License
  summary: 'A Django app to help you filter spam in your forms.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
