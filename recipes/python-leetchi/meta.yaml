package:
  name: python-leetchi
  version: "0.3.10"

source:
  fn: python-leetchi-0.3.10.tar.gz
  url: https://pypi.python.org/packages/source/p/python-leetchi/python-leetchi-0.3.10.tar.gz
  md5: a19c35f38129aa41f867b0af2807cc11
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-leetchi = python-leetchi:main
    #
    # Would create an entry point called python-leetchi that calls python-leetchi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - distribute
    - requests ==2.1.0
    - pycrypto ==2.6.1
    - blinker ==1.2
    - six ==1.5.2

  run:
    - python
    - distribute
    - requests ==2.1.0
    - pycrypto ==2.6.1
    - blinker ==1.2
    - six ==1.5.2

test:
  # Python imports
  imports:
    - leetchi
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/thoas/python-leetchi
  license: MIT License
  summary: 'Leetchi API implementation in Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
