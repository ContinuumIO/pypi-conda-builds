package:
  name: flask-itemshop
  version: "0.2"

source:
  fn: flask-itemshop-0.2.zip
  url: https://pypi.python.org/packages/source/f/flask-itemshop/flask-itemshop-0.2.zip
  md5: 0bf5506865d3558d95adea3e7dcdab3f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-itemshop = flask-itemshop:main
    #
    # Would create an entry point called flask-itemshop that calls flask-itemshop.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.8
    - stripe >=1.6.1

  run:
    - python
    - flask >=0.8
    - stripe >=1.6.1

test:
  # Python imports
  imports:
    - itemshop

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/lost_theory/flask-stripe-blueprint/
  license: BSD License
  summary: '\nSimple flask blueprint (ItemBP) that you can mount in your app to get a basic purchase flow for a single item. Credit card processing is done with stripe.js and the stripe python API.\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
