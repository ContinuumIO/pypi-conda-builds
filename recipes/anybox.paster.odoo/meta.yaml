package:
  name: anybox.paster.odoo
  version: "1.3"

source:
  fn: anybox.paster.odoo-1.3.tar.gz
  url: https://pypi.python.org/packages/source/a/anybox.paster.odoo/anybox.paster.odoo-1.3.tar.gz
  md5: ae1e7a183bb478698f6ec3bfacc6f0e1
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - anybox.paster.odoo = anybox.paster.odoo:main
    #
    # Would create an entry point called anybox.paster.odoo that calls anybox.paster.odoo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - pastescript
    - cheetah

  run:
    - python
    - setuptools
    - setuptools
    - pastescript
    - cheetah

test:
  # Python imports
  imports:
    - anybox
    - anybox.paster
    - anybox.paster.odoo

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://anybox.fr
  license: GPL v3
  summary: 'Paster templates to create an Odoo module or instance using buildout'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
