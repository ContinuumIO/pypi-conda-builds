package:
  name: collective.portlet.googleapps
  version: "0.1"

source:
  fn: collective.portlet.googleapps-0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.portlet.googleapps/collective.portlet.googleapps-0.1.tar.gz
  md5: 1e17d6920ba32e530a315ececbfc2a5b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.portlet.googleapps = collective.portlet.googleapps:main
    #
    # Would create an entry point called collective.portlet.googleapps that calls collective.portlet.googleapps.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.portlet
    - collective.portlet.googleapps
    - collective.portlet.googleapps.browser
    - collective.portlet.googleapps.content
    - collective.portlet.googleapps.portlets
    - collective.portlet.googleapps.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://eresearch.jcu.edu.au
  license: GNU General Public License (GPL)
  summary: 'A collection of portlets to provide connectivity to Google Apps'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
