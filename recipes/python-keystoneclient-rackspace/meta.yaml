package:
  name: python-keystoneclient-rackspace
  version: "0.1.3"

source:
  fn: python-keystoneclient-rackspace-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/p/python-keystoneclient-rackspace/python-keystoneclient-rackspace-0.1.3.tar.gz
  md5: 0db66500c07926465020e3221bf18634
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-keystoneclient-rackspace = python-keystoneclient-rackspace:main
    #
    # Would create an entry point called python-keystoneclient-rackspace that calls python-keystoneclient-rackspace.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python-keystoneclient

  run:
    - python
    - setuptools
    - python-keystoneclient

test:
  # Python imports
  imports:
    - keystoneclient_rackspace

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/testeddoughnut/python-keystoneclient-rackspace
  license: Apache Software License
  summary: 'Rackspace Auth Plugin for OpenStack Clients.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
