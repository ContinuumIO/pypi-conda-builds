package:
  name: arraymanagement
  version: "0.2"

source:
  fn: arraymanagement-0.2.tar.gz
  url: https://pypi.python.org/packages/source/a/arraymanagement/arraymanagement-0.2.tar.gz
  md5: a16e7ea7a5c6c6e013579894a661c487
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - arraymanagement = arraymanagement:main
    #
    # Would create an entry point called arraymanagement that calls arraymanagement.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - arraymanagement
    - arraymanagement.nodes

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/ContinuumIO/ArrayManagement
  license: New BSD
  summary: 'Array Management'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
