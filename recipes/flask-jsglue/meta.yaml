package:
  name: flask-jsglue
  version: "0.3"

source:
  fn: Flask-JSGlue-0.3.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-JSGlue/Flask-JSGlue-0.3.tar.gz
  md5: 6b5e79e91fa72b2a232f20271eef6455
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-jsglue = flask-jsglue:main
    #
    # Would create an entry point called flask-jsglue that calls flask-jsglue.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - flask

  run:
    - python
    - flask

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://stewartpark.github.com/Flask-JSGlue
  license: BSD License
  summary: 'Flask-JSGlue helps hook up your Flask application nicely with the front end.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
