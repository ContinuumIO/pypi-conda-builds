package:
  name: django-simple-aes-field
  version: "0.1.3"

source:
  fn: django-simple-aes-field-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-simple-aes-field/django-simple-aes-field-0.1.3.tar.gz
  md5: ac66ab0ee96f0be2e14fed117662e313
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-simple-aes-field = django-simple-aes-field:main
    #
    # Would create an entry point called django-simple-aes-field that calls django-simple-aes-field.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - simpleaes >=0.2

  run:
    - python
    - simpleaes >=0.2

test:
  # Python imports
  imports:
    - django_simple_aes_field

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://incuna.com/
  license: UNKNOWN
  summary: 'A SimpleAES encoded field for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
