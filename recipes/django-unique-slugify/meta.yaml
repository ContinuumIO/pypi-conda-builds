package:
  name: django-unique-slugify
  version: "1.01"

source:
  fn: django-unique-slugify-1.01.tar.gz
  url: https://pypi.python.org/packages/source/d/django-unique-slugify/django-unique-slugify-1.01.tar.gz
  md5: 541446e83295db0f683646b25abdcd0b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-unique-slugify = django-unique-slugify:main
    #
    # Would create an entry point called django-unique-slugify that calls django-unique-slugify.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/defcube/django-unique-slugify
  license: UNKNOWN
  summary: 'Automatically create a unique slug for a model.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
