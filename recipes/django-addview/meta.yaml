package:
  name: django-addview
  version: "0.1.2"

source:
  fn: django-addview-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-addview/django-addview-0.1.2.tar.gz
  md5: c0e7644c1b067299fe83c4bd82332dc6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-addview = django-addview:main
    #
    # Would create an entry point called django-addview that calls django-addview.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4.0
    - npyscreen >=2.0pre73

  run:
    - python
    - django >=1.4.0
    - npyscreen >=2.0pre73

test:
  # Python imports
  imports:
    - django_addview
    - django_addview.management
    - django_addview.management.commands
    - django_addview.management.commands.forms

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/yakxxx/django-addview
  license: BSD License
  summary: 'Adding views in Django as easy as pie.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
