package:
  name: black-magic
  version: "0.0.10"

source:
  fn: black-magic-0.0.10.tar.gz
  url: https://pypi.python.org/packages/source/b/black-magic/black-magic-0.0.10.tar.gz
  md5: d913092a355e6d97013ea0a8ca5c4e12
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - black-magic = black-magic:main
    #
    # Would create an entry point called black-magic that calls black-magic.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - funcsigs

  run:
    - python
    - funcsigs

test:
  # Python imports
  imports:
    - black_magic

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/coldfix/black-magic
  license: Public Domain
  summary: 'Decorator utility that operates on black magic'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
