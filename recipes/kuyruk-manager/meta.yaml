package:
  name: kuyruk-manager
  version: "1.0.4"

source:
  fn: Kuyruk-Manager-1.0.4.tar.gz
  url: https://pypi.python.org/packages/source/K/Kuyruk-Manager/Kuyruk-Manager-1.0.4.tar.gz
  md5: 2fee6713c28db23af7e50af205cdd496
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kuyruk-manager = kuyruk-manager:main
    #
    # Would create an entry point called kuyruk-manager that calls kuyruk-manager.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - kuyruk >=3.0.0
    - redis >=2.10
    - flask >=0.10
    - rpyc >=3.3

  run:
    - python
    - setuptools
    - kuyruk >=3.0.0
    - redis >=2.10
    - flask >=0.10
    - rpyc >=3.3

test:
  # Python imports
  imports:
    - kuyruk_manager

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/cenkalti/kuyruk-manager
  license: UNKNOWN
  summary: 'Manage Kuyruk workers.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
