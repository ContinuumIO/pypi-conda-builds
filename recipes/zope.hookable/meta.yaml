package:
  name: zope.hookable
  version: "4.0.4"

source:
  fn: zope.hookable-4.0.4.tar.gz
  url: https://pypi.python.org/packages/source/z/zope.hookable/zope.hookable-4.0.4.tar.gz
  md5: 860328682d4f50563e1a3e6cb999b7f0
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zope.hookable = zope.hookable:main
    #
    # Would create an entry point called zope.hookable that calls zope.hookable.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - zope
    - zope.hookable
    - zope.hookable.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.zope.org/zope.hookable
  license: Zope Public License
  summary: 'Zope hookable'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
