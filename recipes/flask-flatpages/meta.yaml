package:
  name: flask-flatpages
  version: "0.6"

source:
  fn: Flask-FlatPages-0.6.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-FlatPages/Flask-FlatPages-0.6.tar.gz
  md5: bacce1ffbcc4d79d018203ce9d1eb034
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-flatpages = flask-flatpages:main
    #
    # Would create an entry point called flask-flatpages that calls flask-flatpages.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.8
    - pyyaml >=3.10
    - markdown >=2.3.1

  run:
    - python
    - flask >=0.8
    - pyyaml >=3.10
    - markdown >=2.3.1

test:
  # Python imports
  imports:
    - flask_flatpages

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/SimonSapin/Flask-FlatPages
  license: BSD License
  summary: 'Provides flat static pages to a Flask application'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
