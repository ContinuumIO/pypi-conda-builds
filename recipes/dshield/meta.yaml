package:
  name: dshield
  version: "0.1"

source:
  fn: dshield-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/dshield/dshield-0.1.tar.gz
  md5: 11218a53cbf9868a2c531a9e9d0b01e8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dshield = dshield:main
    #
    # Would create an entry point called dshield that calls dshield.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - dshield

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rshipp/python-dshield/
  license: BSD License
  summary: 'A Pythonic interface to the Internet Storm Center / DShield API.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
