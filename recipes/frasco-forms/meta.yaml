package:
  name: frasco-forms
  version: "0.1"

source:
  fn: frasco-forms-0.1.tar.gz
  url: https://pypi.python.org/packages/source/f/frasco-forms/frasco-forms-0.1.tar.gz
  md5: 1d3a14000094177652d290c2d320db39
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - frasco-forms = frasco-forms:main
    #
    # Would create an entry point called frasco-forms that calls frasco-forms.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - frasco
    - flask-wtf >=0.9.5
    - inflection

  run:
    - python
    - frasco
    - flask-wtf >=0.9.5
    - inflection

test:
  # Python imports
  imports:
    - frasco_forms

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/frascoweb/frasco-forms
  license: MIT
  summary: 'WTForms integration for Frasco'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
