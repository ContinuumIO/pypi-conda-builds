package:
  name: voltgrid
  version: "0.1.4"

source:
  fn: voltgrid-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/v/voltgrid/voltgrid-0.1.4.tar.gz
  md5: b05286117a3124ce8da5bc020a448cfe
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - voltgrid = voltgrid:main
    #
    # Would create an entry point called voltgrid that calls voltgrid.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=2.1.0

  run:
    - python
    - requests >=2.1.0

test:
  # Python imports
  imports:
    - voltgrid
    - voltgrid.api
    - voltgrid.error
    - voltgrid.http_client

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://voltgrid.com
  license: BSD License
  summary: 'Official Voltgrid API library client for python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
