package:
  name: blogbackup
  version: "1.4"

source:
  fn: BlogBackup-1.4.tar.gz
  url: http://www.doughellmann.com/downloads/BlogBackup-1.4.tar.gz
  #md5: 
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - blogbackup = blogbackup:main
    #
    # Would create an entry point called blogbackup that calls blogbackup.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - commandlineapp >=2.5
    - feedparser
    - feedfinder

  run:
    - python
    - commandlineapp >=2.5
    - feedparser
    - feedfinder

test:
  # Python imports
  imports:
    - blogbackup
    - blogbackuplib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.doughellmann.com/projects/BlogBackup/
  license: BSD License
  summary: 'Script to dump a blog feed to files suitable for backing up or reprocessing.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
