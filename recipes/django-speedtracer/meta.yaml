package:
  name: django-speedtracer
  version: "0.1.1"

source:
  fn: django-speedtracer-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-speedtracer/django-speedtracer-0.1.1.tar.gz
  md5: 759f7f21f8b0ab3dab5af620add6c350
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-speedtracer = django-speedtracer:main
    #
    # Would create an entry point called django-speedtracer that calls django-speedtracer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - speedtracer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/acdha/django-speedtracer
  license: BSD License
  summary: "Profile your Django site using Google Chrome's SpeedTracer"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
