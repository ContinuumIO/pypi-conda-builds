package:
  name: pyevents
  version: "0.1.0"

source:
  fn: pyevents-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/p/pyevents/pyevents-0.1.0.tar.gz
  md5: 1ed64ad2780fcabdc6aa9b1386476a77
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pyevents = pyevents:main
    #
    # Would create an entry point called pyevents that calls pyevents.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - pyevents

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jmvrbanac/PyEvents
  license: MIT License
  summary: "PyEvents is a simple event dispatching library for Python inspired from AS3's EventDispatcher"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
