package:
  name: gocept.fixedpoint
  version: "0.2"

source:
  fn: gocept.fixedpoint-0.2.tar.gz
  url: https://pypi.python.org/packages/source/g/gocept.fixedpoint/gocept.fixedpoint-0.2.tar.gz
  md5: 0581f77ad5b30c4a4c76fedd712ad65f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gocept.fixedpoint = gocept.fixedpoint:main
    #
    # Would create an entry point called gocept.fixedpoint that calls gocept.fixedpoint.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - gocept
    - gocept.fixedpoint

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.python.org/pypi/gocept.fixedpoint
  license: Zope Public License
  summary: 'Fixedpoint datatype in python.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
