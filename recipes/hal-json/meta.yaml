package:
  name: hal-json
  version: "0.1"

source:
  fn: hal-json-0.1.tar.gz
  url: https://pypi.python.org/packages/source/h/hal-json/hal-json-0.1.tar.gz
  md5: 0a4c1e34634337443ff1a1eaed723d6e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hal-json = hal-json:main
    #
    # Would create an entry point called hal-json that calls hal-json.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - hal

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://inean@github.com/inean/LinkHeader.git
  license: BSD License
  summary: 'Parse and encode links according to RFC 5988 or HAL specs'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
