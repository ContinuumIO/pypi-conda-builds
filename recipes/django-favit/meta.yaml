package:
  name: django-favit
  version: "0.2.4"

source:
  fn: django-favit-0.2.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-favit/django-favit-0.2.4.tar.gz
  md5: abf8600fcfb72668c8ee271c3709c374
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-favit = django-favit:main
    #
    # Would create an entry point called django-favit that calls django-favit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - favit
    - favit.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/streema/django-favit
  license: MIT License
  summary: 'A simple reusable app for django that makes it easy to deal with faving'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
