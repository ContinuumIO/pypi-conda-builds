package:
  name: git-crucible
  version: "0.1.2"

source:
  fn: git-crucible-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/g/git-crucible/git-crucible-0.1.2.tar.gz
  md5: 54980065a32b5e05339d7dc43efcb8d7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - git-crucible = git-crucible:main
    #
    # Would create an entry point called git-crucible that calls git-crucible.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - argparse

  run:
    - python
    - argparse

test:
  # Python imports
  imports:
    - crucible
    - crucible.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/rckclmbr/git-crucible
  license: BSD License
  summary: 'Git extension that creates reviews in Crucible straight from the command line.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
