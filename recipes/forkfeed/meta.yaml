package:
  name: forkfeed
  version: "0.1.4"

source:
  fn: forkfeed-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/f/forkfeed/forkfeed-0.1.4.tar.gz
  md5: 08a9600000aa45364ae3d93b1fbc3147
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - forkfeed = forkfeed:main
    #
    # Would create an entry point called forkfeed that calls forkfeed.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - github2 >=0.2.0
    - feedgenerator >=1.2.1
    - opster >=2.1

  run:
    - python
    - github2 >=0.2.0
    - feedgenerator >=1.2.1
    - opster >=2.1

test:
  # Python imports
  imports:
    - forkfeed

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD License
  summary: 'Utility do build atom feeds for all commits in all forks of your projects on GitHub.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
