package:
  name: golemapi
  version: "0.8.0"

source:
  fn: golemapi-0.8.0.tar.gz
  url: https://pypi.python.org/packages/source/g/golemapi/golemapi-0.8.0.tar.gz
  md5: 7eedf4a02b937abd7a47645ade057248
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - golemapi = golemapi:main
    #
    # Would create an entry point called golemapi that calls golemapi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - ulib >=0.25
    - requests >=2.2.1

  run:
    - python
    - ulib >=0.25
    - requests >=2.2.1

test:
  # Python imports
  imports:
    - golemapi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/yandex-sysmon/golemapi
  license: GNU Lesser General Public License v3 (LGPLv3)
  summary: 'Simple wrapper for Golem HTTP API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
