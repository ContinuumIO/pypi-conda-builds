package:
  name: ipv4toipv6
  version: "1.0.0"

source:
  fn: ipv4toipv6-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/i/ipv4toipv6/ipv4toipv6-1.0.0.tar.gz
  md5: 3a4ac55f31e7233af5db33857118dcf3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ipv4toipv6 = ipv4toipv6:main
    #
    # Would create an entry point called ipv4toipv6 that calls ipv4toipv6.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - ipv4toipv6

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: UNKNOWN
  summary: 'A simple application to convert ip addresses from version 4 to version 6'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
