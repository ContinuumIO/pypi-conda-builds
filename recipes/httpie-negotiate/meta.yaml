package:
  name: httpie-negotiate
  version: "1.0.0"

source:
  fn: httpie-negotiate-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/h/httpie-negotiate/httpie-negotiate-1.0.0.tar.gz
  md5: 2cad881d33d95849ca95d59522aa4130
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - httpie-negotiate = httpie-negotiate:main
    #
    # Would create an entry point called httpie-negotiate that calls httpie-negotiate.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - httpie >=0.7.0
    - requests_kerberos

  run:
    - python
    - setuptools
    - httpie >=0.7.0
    - requests_kerberos

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ndzou/httpie-negotiate
  license: BSD License
  summary: 'SPNEGO (GSS Negotiate) auth plugin for HTTPie.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
