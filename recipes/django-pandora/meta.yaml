package:
  name: django-pandora
  version: "0.1.0"

source:
  fn: django-pandora-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-pandora/django-pandora-0.1.0.tar.gz
  md5: 5543669b9d1dfbb51ae58eea6c4678d7
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-pandora = django-pandora:main
    #
    # Would create an entry point called django-pandora that calls django-pandora.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - pandora

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/gregmuellegger/django-pandora
  license: BSD License
  summary: "Opening Pandora's box by making django's request object available in a thread local."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
