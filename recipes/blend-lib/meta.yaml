package:
  name: blend-lib
  version: "0.12"

source:
  fn: blend-lib-0.12.tar.gz
  url: https://pypi.python.org/packages/source/b/blend-lib/blend-lib-0.12.tar.gz
  md5: 9f13ddabf88db4c55d78b57dc50d6e9b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - blend-lib = blend-lib:main
    #
    # Would create an entry point called blend-lib that calls blend-lib.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests
    - simplejson
    - boto
    - nose
    - mock

  run:
    - python
    - requests
    - simplejson
    - boto
    - nose
    - mock

test:
  # Python imports
  imports:
    - blend
    - blend.bcc
    - blend.cloudman
    - blend.galaxy
    - blend.galaxy.datasets
    - blend.galaxy.histories
    - blend.galaxy.libraries
    - blend.galaxy.users
    - blend.galaxy.workflows

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://blend.readthedocs.org/
  license: MIT License
  summary: 'BioCloudCentral.org, CloudMan, and Galaxy API library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
