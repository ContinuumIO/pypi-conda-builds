package:
  name: example.rtsubsites_theme
  version: "0.1.0"

source:
  fn: example.rtsubsites_theme-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/e/example.rtsubsites_theme/example.rtsubsites_theme-0.1.0.tar.gz
  md5: 186e2b521561431b0c5c0c9e1b2b9f8a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - example.rtsubsites_theme = example.rtsubsites_theme:main
    #
    # Would create an entry point called example.rtsubsites_theme that calls example.rtsubsites_theme.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - redturtle.subsites

  run:
    - python
    - setuptools
    - setuptools
    - redturtle.subsites

test:
  # Python imports
  imports:
    - example
    - example.rtsubsites_theme
    - example.rtsubsites_theme.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/redturtle.subsites
  license: GPL
  summary: 'An example theme that simply help understanding redturtle.subsites features'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
