package:
  name: htmlentitiesdecode
  version: "0.1.1"

source:
  fn: htmlentitiesdecode-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/h/htmlentitiesdecode/htmlentitiesdecode-0.1.1.tar.gz
  md5: 6f68612cf1212baac0c6f0f698e0480c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - htmlentitiesdecode = htmlentitiesdecode:main
    #
    # Would create an entry point called htmlentitiesdecode that calls htmlentitiesdecode.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - htmlentitiesdecode

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://metanohi.org/projects/htmlentitiesdecode/
  license: COPYING.txt
  summary: 'Decode html entities.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
