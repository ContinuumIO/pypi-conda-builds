package:
  name: msniper
  version: "1.0.3"

source:
  fn: msniper-1.0.3.tar.gz
  url: https://pypi.python.org/packages/source/m/msniper/msniper-1.0.3.tar.gz
  md5: b9348278e612974edcbcbc9e82bd8563
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - msniper = msniper:main
    #
    # Would create an entry point called msniper that calls msniper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - msniper

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.msniper.com
  license: UNKNOWN
  summary: 'A simple tool design for showing the information of the list with nice style'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
