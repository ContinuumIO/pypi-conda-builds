package:
  name: django-cropduster
  version: "4.8.8"

source:
  fn: django-cropduster-4.8.8.tar.gz
  url: https://pypi.python.org/packages/source/d/django-cropduster/django-cropduster-4.8.8.tar.gz
  md5: ec7f98c2d3a549c3fd1448ce730040d7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-cropduster = django-cropduster:main
    #
    # Would create an entry point called django-cropduster that calls django-cropduster.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pillow
    - django >=1.2
    - python-xmp-toolkit
    - django-generic-plus >=1.1.0
    - six >=1.7.0

  run:
    - python
    - pillow
    - django >=1.2
    - python-xmp-toolkit
    - django-generic-plus >=1.1.0
    - six >=1.7.0

test:
  # Python imports
  imports:
    - cropduster
    - cropduster.migrations
    - cropduster.standalone
    - cropduster.templatetags
    - cropduster.tests
    - cropduster.utils
    - cropduster.views

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/theatlantic/django-cropduster
  license: UNKNOWN
  summary: 'Django image uploader and cropping tool'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
