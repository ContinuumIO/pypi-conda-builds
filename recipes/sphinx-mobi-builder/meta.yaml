package:
  name: sphinx-mobi-builder
  version: "0.2"

source:
  fn: sphinx-mobi-builder-0.2.tar.gz
  url: https://pypi.python.org/packages/source/s/sphinx-mobi-builder/sphinx-mobi-builder-0.2.tar.gz
  md5: 7bd215818aad262b0f600669d1096165
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sphinx-mobi-builder = sphinx-mobi-builder:main
    #
    # Would create an entry point called sphinx-mobi-builder that calls sphinx-mobi-builder.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - docutils >=0.12
    - sphinx >=1.0

  run:
    - python
    - docutils >=0.12
    - sphinx >=1.0

test:
  # Python imports
  imports:
    - sphinx/builders

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/EronHennessey/sphinx-mobi-builder
  license: UNKNOWN
  summary: '\n        A Sphinx builder for .mobi (Kindle) files.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
