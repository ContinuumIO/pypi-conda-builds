package:
  name: django-canary-endpoint
  version: "0.2.0"

source:
  fn: django-canary-endpoint-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-canary-endpoint/django-canary-endpoint-0.2.0.tar.gz
  md5: 80d912e7b82149b67afe9cb355659dc8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-canary-endpoint = django-canary-endpoint:main
    #
    # Would create an entry point called django-canary-endpoint that calls django-canary-endpoint.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-rq >=0.7.0
    - requests >=2.0.0

  run:
    - python
    - django-rq >=0.7.0
    - requests >=2.0.0

test:
  # Python imports
  imports:
    - canary_endpoint
    - canary_endpoint.resources

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/TabbedOut/django-canary-endpoint
  license: MIT License
  summary: 'Resource health checks for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
