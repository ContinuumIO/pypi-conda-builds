package:
  name: sqlitefktg4sa
  version: "0.1.1"

source:
  fn: SQLiteFKTG4SA-0.1.1.zip
  url: https://pypi.python.org/packages/source/S/SQLiteFKTG4SA/SQLiteFKTG4SA-0.1.1.zip
  md5: b7d1312921e2a16cd5bf25b117a41cc8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sqlitefktg4sa = sqlitefktg4sa:main
    #
    # Would create an entry point called sqlitefktg4sa that calls sqlitefktg4sa.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sqlalchemy

  run:
    - python
    - sqlalchemy

test:
  # Python imports
  imports:
    - sqlitefktg4sa

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/sqlitefktg4sa/
  license: BSD License
  summary: 'SQLite Foreign Key Trigger Generator for SQLAlchemy'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
