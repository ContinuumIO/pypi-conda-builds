package:
  name: linktastic
  version: "0.1.0"

source:
  fn: Linktastic-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/L/Linktastic/Linktastic-0.1.0.tar.gz
  md5: 1b078e450b1385a060ecc2fa378f8117
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - linktastic = linktastic:main
    #
    # Would create an entry point called linktastic that calls linktastic.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - linktastic

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/berkona/linktastic
  license: MIT License - See http://opensource.org/licenses/MIT for details
  summary: 'Truly platform-independent file linking'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
