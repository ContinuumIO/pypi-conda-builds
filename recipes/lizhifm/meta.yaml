package:
  name: lizhifm
  version: "0.0.11"

source:
  fn: lizhifm-0.0.11.tar.gz
  url: https://pypi.python.org/packages/source/l/lizhifm/lizhifm-0.0.11.tar.gz
  md5: 0baa76375f7cfc1f70fd7353a232bf85
#  patches:
   # List any patch files here
   # - fix.patch

build:
  # preserve_egg_dir: True
  entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lizhifm = lizhifm:main
    #
    # Would create an entry point called lizhifm that calls lizhifm.main()

    - lizhifm = lizhifm:main

  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests
    - downloadhelper
    - prettytable

  run:
    - python
    - requests
    - downloadhelper
    - prettytable

test:
  # Python imports
  imports:
    - lizhifm

  commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.

    - lizhifm --help

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: Apache Software License
  summary: 'Would you like lizhi FM ? Now, Record it.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
