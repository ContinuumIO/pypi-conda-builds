package:
  name: infi.nose_logbook
  version: "1.0"

source:
  fn: infi.nose_logbook-1.0.tar.gz
  url: https://pypi.python.org/packages/source/i/infi.nose_logbook/infi.nose_logbook-1.0.tar.gz
  md5: 7707f039643f6fefe70ecd8749211aaa
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - infi.nose_logbook = infi.nose_logbook:main
    #
    # Would create an entry point called infi.nose_logbook that calls infi.nose_logbook.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - infi.recipe.console_scripts
    - logbook
    - distribute
    - nose

  run:
    - python
    - setuptools
    - infi.recipe.console_scripts
    - logbook
    - distribute
    - nose

test:
  # Python imports
  imports:
    - infi
    - infi.nose_logbook

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Infinidat/infi.nose_logbook
  license: Python Software Foundation License
  summary: 'Logbook integration for Nose'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
