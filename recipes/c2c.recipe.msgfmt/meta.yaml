package:
  name: c2c.recipe.msgfmt
  version: "0.2.2"

source:
  fn: c2c.recipe.msgfmt-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/c/c2c.recipe.msgfmt/c2c.recipe.msgfmt-0.2.2.tar.gz
  md5: 26b12a855bf1804b8aa523cce33fb08b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - c2c.recipe.msgfmt = c2c.recipe.msgfmt:main
    #
    # Would create an entry point called c2c.recipe.msgfmt that calls c2c.recipe.msgfmt.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zc.buildout
    - babel

  run:
    - python
    - setuptools
    - zc.buildout
    - babel

test:
  # Python imports
  imports:
    - c2c
    - c2c.recipe
    - c2c.recipe.msgfmt

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/camptocamp/c2c.recipe.msgfmt
  license: MIT License
  summary: 'A buildout recipe to compile message catalog to binary format.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
