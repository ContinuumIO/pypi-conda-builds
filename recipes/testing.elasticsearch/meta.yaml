package:
  name: testing.elasticsearch
  version: "0.2.3"

source:
  fn: testing.elasticsearch-0.2.3.tar.gz
  url: https://pypi.python.org/packages/source/t/testing.elasticsearch/testing.elasticsearch-0.2.3.tar.gz
  md5: 11a477bcefa1930e832c92bcba9eb473
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - testing.elasticsearch = testing.elasticsearch:main
    #
    # Would create an entry point called testing.elasticsearch that calls testing.elasticsearch.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - clom ==0.7.5
    - requests ==2.6.0

  run:
    - python
    - clom ==0.7.5
    - requests ==2.6.0

test:
  # Python imports
  imports:
    - testing

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jong/testing.elasticsearch
  license: MIT License
  summary: 'Automatically sets up an elasticsearch instance in a temporary directory, and destroys it after testing'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
