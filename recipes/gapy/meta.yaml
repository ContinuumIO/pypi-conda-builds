package:
  name: gapy
  version: "1.3.0"

source:
  fn: gapy-1.3.0.tar.gz
  url: https://pypi.python.org/packages/source/g/gapy/gapy-1.3.0.tar.gz
  md5: e4ae037a3c82aab0979cd08fb8035832
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gapy = gapy:main
    #
    # Would create an entry point called gapy that calls gapy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - google-api-python-client ==1.0
    - pyopenssl ==0.13

  run:
    - python
    - google-api-python-client ==1.0
    - pyopenssl ==0.13

test:
  # Python imports
  imports:
    - gapy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/alphagov/gapy
  license: MIT License
  summary: 'Painless Google Analytics'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
