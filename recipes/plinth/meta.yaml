package:
  name: plinth
  version: "0.0.14"

source:
  fn: plinth-0.0.14.tar.gz
  url: https://pypi.python.org/packages/source/p/plinth/plinth-0.0.14.tar.gz
  md5: 45b5e7a988660441162fb13973d10023
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plinth = plinth:main
    #
    # Would create an entry point called plinth that calls plinth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pytomcrypt >=0.9.0
    - gevent >=1.0

  run:
    - python
    - pytomcrypt >=0.9.0
    - gevent >=1.0

test:
  # Python imports
  imports:
    - plinth

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/telehash/plinth
  license: MIT License
  summary: 'Python Library for InterNetworking with TeleHash'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
