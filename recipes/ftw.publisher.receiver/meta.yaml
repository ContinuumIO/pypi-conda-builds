package:
  name: ftw.publisher.receiver
  version: "2.0.4"

source:
  fn: ftw.publisher.receiver-2.0.4.zip
  url: https://pypi.python.org/packages/source/f/ftw.publisher.receiver/ftw.publisher.receiver-2.0.4.zip
  md5: ca735c1609d1f359cf499d9006f33daa
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ftw.publisher.receiver = ftw.publisher.receiver:main
    #
    # Would create an entry point called ftw.publisher.receiver that calls ftw.publisher.receiver.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - ftw.publisher.core
    - plone

  run:
    - python
    - setuptools
    - setuptools
    - ftw.publisher.core
    - plone

test:
  # Python imports
  imports:
    - ftw
    - ftw.publisher
    - ftw.publisher.receiver
    - ftw.publisher.receiver.browser
    - ftw.publisher.receiver.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/4teamwork/ftw.publisher.receiver
  license: GPL2
  summary: 'Staging and publishing addon for Plone contents.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
