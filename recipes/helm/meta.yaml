package:
  name: helm
  version: "0.4"

source:
  fn: helm-0.4.tar.bz2
  url: https://pypi.python.org/packages/source/h/helm/helm-0.4.tar.bz2
  md5: bd281b9d273a95721fd4ffdae4048d3e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - helm = helm:main
    #
    # Would create an entry point called helm that calls helm.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - helm
    - helm.models
    - helm.views
    - helm.widgets

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://helm.last-exile.org/
  license: GNU General Public License (GPL)
  summary: 'Helm is a system monitor released under GNU GPLv3.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
