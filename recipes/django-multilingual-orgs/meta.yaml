package:
  name: django-multilingual-orgs
  version: "1.0"

source:
  fn: django-multilingual-orgs-1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-multilingual-orgs/django-multilingual-orgs-1.0.tar.gz
  md5: f4920cf4b259b4e61767e2419d079e40
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-multilingual-orgs = django-multilingual-orgs:main
    #
    # Would create an entry point called django-multilingual-orgs that calls django-multilingual-orgs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - south
    - django-libs
    - django-filer
    - pillow
    - django-people
    - django-localized-names

  run:
    - python
    - django
    - south
    - django-libs
    - django-filer
    - pillow
    - django-people
    - django-localized-names

test:
  # Python imports
  imports:
    - multilingual_orgs
    - multilingual_orgs.migrations
    - multilingual_orgs.tests
    - multilingual_orgs.tests.test_app

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bitmazk/django-multilingual-orgs
  license: The MIT License
  summary: 'An app for showing localized versions of organization profiles. E.g. as plugin\nin a blog post.\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
