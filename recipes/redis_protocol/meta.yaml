package:
  name: redis_protocol
  version: "0.0.2"

source:
  fn: redis_protocol-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/r/redis_protocol/redis_protocol-0.0.2.tar.gz
  md5: 5305fa2db22281524d8243f79a596f6c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - redis_protocol = redis_protocol:main
    #
    # Would create an entry point called redis_protocol that calls redis_protocol.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - redis_protocol

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/youngking/redis_protocol
  license: BSD License
  summary: 'Redis Protocol implemented by python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
