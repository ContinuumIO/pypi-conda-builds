package:
  name: django-netaxept
  version: "1.0"

source:
  fn: django-netaxept-1.0.zip
  url: https://pypi.python.org/packages/source/d/django-netaxept/django-netaxept-1.0.zip
  md5: 21e193a1b6ae575f5116d212ca8d6886
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-netaxept = django-netaxept:main
    #
    # Would create an entry point called django-netaxept that calls django-netaxept.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - suds
    - django >=1.3

  run:
    - python
    - suds
    - django >=1.3

test:
  # Python imports
  imports:
    - djnetaxept
    - djnetaxept.test_utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/fivethreeo/django-netaxept/
  license: BSD License
  summary: 'This is a generic payment app for django using Netaxept'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
