package:
  name: django-setman
  version: "0.2"

source:
  fn: django-setman-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-setman/django-setman-0.2.tar.gz
  md5: 3f93410070101d49d0377485b9ed5c3d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-setman = django-setman:main
    #
    # Would create an entry point called django-setman that calls django-setman.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - setman
    - setman.management
    - setman.management.commands
    - setman.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/odeskps/django-setman
  license: BSD License
  summary: 'Django settings manager. Another.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
