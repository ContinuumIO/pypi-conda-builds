package:
  name: bottle-rest
  version: "0.4.1"

source:
  fn: bottle-rest-0.4.1.tar.gz
  url: https://pypi.python.org/packages/source/b/bottle-rest/bottle-rest-0.4.1.tar.gz
  md5: 1fc4b745baacfbe5fec56b8a961a39a5
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bottle-rest = bottle-rest:main
    #
    # Would create an entry point called bottle-rest that calls bottle-rest.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - bottle

  run:
    - python
    - setuptools
    - bottle

test:
  # Python imports
  imports:
    - bottle_rest

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Bystroushaak/rbottle
  license: MIT License
  summary: 'Decorators to make REST easier in Bottle.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
