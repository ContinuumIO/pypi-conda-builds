package:
  name: django-angular-scaffold
  version: "0.12.1"

source:
  fn: django-angular-scaffold-0.12.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-angular-scaffold/django-angular-scaffold-0.12.1.tar.gz
  md5: a3ca26cc8993be70edf3a685d5e065f6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-angular-scaffold = django-angular-scaffold:main
    #
    # Would create an entry point called django-angular-scaffold that calls django-angular-scaffold.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - angular_scaffold
    - angular_scaffold.management
    - angular_scaffold.management.commands
    - angular_scaffold.management.commands.helpers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mc706/django-angular-scaffold
  license: MIT License
  summary: 'AngularJS Scaffolding for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
