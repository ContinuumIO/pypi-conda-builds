package:
  name: cdn-fastly
  version: "1.0.4"

source:
  fn: cdn-fastly-1.0.4.tar.gz
  url: https://pypi.python.org/packages/source/c/cdn-fastly/cdn-fastly-1.0.4.tar.gz
  md5: 30dc06540e35e016076288f0e38bfd8a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cdn-fastly = cdn-fastly:main
    #
    # Would create an entry point called cdn-fastly that calls cdn-fastly.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - fastly
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/obulpathi/cdn-fastly-python
  license: BSD License
  summary: 'A Python client libary for the Fastly API.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
