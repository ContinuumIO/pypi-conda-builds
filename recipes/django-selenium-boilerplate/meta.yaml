package:
  name: django-selenium-boilerplate
  version: "0.0.6"

source:
  fn: django-selenium-boilerplate-0.0.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-selenium-boilerplate/django-selenium-boilerplate-0.0.6.tar.gz
  md5: d54af4452d100af1a84986820c4463f4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-selenium-boilerplate = django-selenium-boilerplate:main
    #
    # Would create an entry point called django-selenium-boilerplate that calls django-selenium-boilerplate.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - selenium

  run:
    - python
    - selenium

test:
  # Python imports
  imports:
    - selenium_boilerplate

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/yourcelf/django-selenium-boilerplate.git
  license: BSD License
  summary: 'Boilerplate for setting up selenium tests in Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
