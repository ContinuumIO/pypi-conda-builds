package:
  name: netflowvizu
  version: "0.6"

source:
  fn: NetFlowVizu-0.6.tar.gz
  url: https://pypi.python.org/packages/source/N/NetFlowVizu/NetFlowVizu-0.6.tar.gz
  md5: 8a131532d3802e36e5940a96726a253b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - netflowvizu = netflowvizu:main
    #
    # Would create an entry point called netflowvizu that calls netflowvizu.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - lxml
    - pyyaml

  run:
    - python
    - lxml
    - pyyaml

test:
  # Python imports
  imports:
    - netflowvizu

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.smallbulb.net/netflowvizu
  license: MIT License
  summary: 'Network flow visualizer.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
