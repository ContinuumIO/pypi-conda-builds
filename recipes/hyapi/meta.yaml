package:
  name: hyapi
  version: "0.0.1"

source:
  fn: hyapi-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/h/hyapi/hyapi-0.0.1.tar.gz
  md5: 53f20e395be5495a819ae68133e87038
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hyapi = hyapi:main
    #
    # Would create an entry point called hyapi that calls hyapi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pycrypto ==2.6.1
    - requests ==2.6.0

  run:
    - python
    - pycrypto ==2.6.1
    - requests ==2.6.0

test:
  # Python imports
  imports:
    - hyapi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/kimtree/hyapi
  license: MIT License
  summary: 'Python SDK for Hanyang Univ API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
