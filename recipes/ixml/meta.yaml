package:
  name: ixml
  version: "0.1.0"

source:
  fn: ixml-0.1.0.zip
  url: https://pypi.python.org/packages/source/i/ixml/ixml-0.1.0.zip
  md5: fc48c3f7b8ed9d0aef47376a145289b5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ixml = ixml:main
    #
    # Would create an entry point called ixml that calls ixml.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - ixml
    - ixml.backends
    - ixml.builders

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/YAmikep/ixml
  license: MIT License
  summary: 'iXML is an iterative event-driven XML parser with a standard Python iterator interface.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
