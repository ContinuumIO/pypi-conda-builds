package:
  name: collective.adlibsyncmanager
  version: "0.1"

source:
  fn: collective.adlibsyncmanager-0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.adlibsyncmanager/collective.adlibsyncmanager-0.1.tar.gz
  md5: cbaa32e562732c2bbdde27a4da4b9b0a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.adlibsyncmanager = collective.adlibsyncmanager:main
    #
    # Would create an entry point called collective.adlibsyncmanager that calls collective.adlibsyncmanager.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.adlibsyncmanager
    - collective.adlibsyncmanager.Extensions

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.adlibsyncmanager
  license: GPL
  summary: 'Provides external methods to sync and create content from Adlib API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
