package:
  name: aws.pdfbook
  version: "1.1"

source:
  fn: aws.pdfbook-1.1.zip
  url: https://pypi.python.org/packages/source/a/aws.pdfbook/aws.pdfbook-1.1.zip
  md5: be1be4f31189facc1163079b77889456
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - aws.pdfbook = aws.pdfbook:main
    #
    # Would create an entry point called aws.pdfbook that calls aws.pdfbook.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - aws
    - aws.pdfbook
    - aws.pdfbook.browser
    - aws.pdfbook.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/aws.pdfbook
  license: GNU General Public License (GPL)
  summary: 'Download Plone content views as PDF'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
