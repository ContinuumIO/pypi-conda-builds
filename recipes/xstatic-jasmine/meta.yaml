package:
  name: xstatic-jasmine
  version: "2.1.2.0"

source:
  fn: XStatic-Jasmine-2.1.2.0.tar.gz
  url: https://pypi.python.org/packages/source/X/XStatic-Jasmine/XStatic-Jasmine-2.1.2.0.tar.gz
  md5: 63fdb4a8668eb70ceef1a311e8b02dcb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - xstatic-jasmine = xstatic-jasmine:main
    #
    # Would create an entry point called xstatic-jasmine that calls xstatic-jasmine.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - xstatic
    - xstatic.pkg
    - xstatic.pkg.jasmine

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://jasmine.github.io/
  license: (same as Jasmine)
  summary: 'Jasmine 2.1.2 (XStatic packaging standard)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
