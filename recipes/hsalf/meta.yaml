package:
  name: hsalf
  version: "0.0.1"

source:
  fn: hsalf-0.0.1.zip
  url: https://pypi.python.org/packages/source/h/hsalf/hsalf-0.0.1.zip
  md5: 7cdfc52b613473be4e7dcb75677e081e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hsalf = hsalf:main
    #
    # Would create an entry point called hsalf that calls hsalf.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - hsalf

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/namn/hsalf/overview
  license: MIT License
  summary: 'Hsalf is a pure Python library to read and write Flash files (SWF).'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
