package:
  name: xstatic-bootstrap-scss
  version: "3.2.0.0"

source:
  fn: XStatic-Bootstrap-SCSS-3.2.0.0.tar.gz
  url: https://pypi.python.org/packages/source/X/XStatic-Bootstrap-SCSS/XStatic-Bootstrap-SCSS-3.2.0.0.tar.gz
  md5: 903d7de8aef6bee93d976b42d35edf7b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - xstatic-bootstrap-scss = xstatic-bootstrap-scss:main
    #
    # Would create an entry point called xstatic-bootstrap-scss that calls xstatic-bootstrap-scss.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - xstatic
    - xstatic.pkg
    - xstatic.pkg.bootstrap_scss

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

# requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://getbootstrap.org/
  license: (same as Bootstrap-SCSS)
  summary: 'Bootstrap-SCSS 3.2.0 (XStatic packaging standard)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
