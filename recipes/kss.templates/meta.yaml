package:
  name: kss.templates
  version: "0.1"

source:
  fn: kss.templates-0.1.tar.gz
  url: https://pypi.python.org/packages/source/k/kss.templates/kss.templates-0.1.tar.gz
  md5: f8cd07b18f8fca2d6efc70c9b27c1d8b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kss.templates = kss.templates:main
    #
    # Would create an entry point called kss.templates that calls kss.templates.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - pastescript

  run:
    - python
    - setuptools
    - setuptools
    - pastescript

test:
  # Python imports
  imports:
    - kss
    - kss.templates

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://kssproject.org
  license: GNU General Public License (GPL)
  summary: 'Templates for creating KSS plugins'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
