package:
  name: django-extra-sensitive-variables
  version: "1.0.2"

source:
  fn: django-extra-sensitive-variables-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-extra-sensitive-variables/django-extra-sensitive-variables-1.0.2.tar.gz
  md5: e6c027467d6b8abdd5693095fd7a4c69
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-extra-sensitive-variables = django-extra-sensitive-variables:main
    #
    # Would create an entry point called django-extra-sensitive-variables that calls django-extra-sensitive-variables.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/shaneallgeier/django-extra-sensitive-variables
  license: MIT License
  summary: 'Globally censor a set of default variable names in your Django error reports'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
