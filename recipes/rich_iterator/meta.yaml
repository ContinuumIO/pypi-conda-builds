package:
  name: rich_iterator
  version: "0.1"

source:
  fn: rich_iterator-0.1.tar.gz
  url: https://pypi.python.org/packages/source/r/rich_iterator/rich_iterator-0.1.tar.gz
  md5: 396597c633d7e076157e5525e91c6e16
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - rich_iterator = rich_iterator:main
    #
    # Would create an entry point called rich_iterator that calls rich_iterator.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - rich_iterator

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bm371613/rich_iterator
  license: RichReports
  summary: ''

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
