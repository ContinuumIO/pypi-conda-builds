package:
  name: boxpython
  version: "1.0.5"

source:
  fn: boxpython-1.0.5.tar.gz
  url: https://pypi.python.org/packages/source/b/boxpython/boxpython-1.0.5.tar.gz
  md5: d51584f90468548437aa77efcf99b5c6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - boxpython = boxpython:main
    #
    # Would create an entry point called boxpython that calls boxpython.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - boxpython

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/wesleyfr/boxpython/
  license: MIT License
  summary: 'Box Python SDK for v2 of the Box.com API.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
