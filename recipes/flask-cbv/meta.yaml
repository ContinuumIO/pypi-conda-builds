package:
  name: flask-cbv
  version: "0.0.1"

source:
  fn: flask-cbv-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/f/flask-cbv/flask-cbv-0.0.1.tar.gz
  md5: 423a779c26876c25dc1353ccdf4f58d3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-cbv = flask-cbv:main
    #
    # Would create an entry point called flask-cbv that calls flask-cbv.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.10.1

  run:
    - python
    - flask >=0.10.1

test:
  # Python imports
  imports:
    - flask_cbv
    - flask_cbv.views

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/jsmitka/flask-cbv
  license: BSD License
  summary: 'Class based views for Flask.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
