package:
  name: jobqueue
  version: "0.0.2"

source:
  fn: jobqueue-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/j/jobqueue/jobqueue-0.0.2.tar.gz
  md5: 2b45da4690ea4e40d89669e2c1f1034d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jobqueue = jobqueue:main
    #
    # Would create an entry point called jobqueue that calls jobqueue.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - jobqueue

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Try2Code/jobQueue
  license: BSD
  summary: 'Queues with a user definied number of working threads '

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
