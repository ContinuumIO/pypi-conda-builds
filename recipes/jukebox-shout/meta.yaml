package:
  name: jukebox-shout
  version: "0.1.1"

source:
  fn: jukebox-shout-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/j/jukebox-shout/jukebox-shout-0.1.1.tar.gz
  md5: c700929c22ca267caf762815f282f030
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jukebox-shout = jukebox-shout:main
    #
    # Would create an entry point called jukebox-shout that calls jukebox-shout.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - jukebox >=0.3.7
    - python-daemon ==1.6
    - python-shout ==0.2

  run:
    - python
    - jukebox >=0.3.7
    - python-daemon ==1.6
    - python-shout ==0.2

test:
  # Python imports
  imports:
    - jukebox_shout
    - jukebox_shout.management
    - jukebox_shout.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://jensnistler.de/
  license: MIT License
  summary: 'Shoutcast streaming playback service for jukebox'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
