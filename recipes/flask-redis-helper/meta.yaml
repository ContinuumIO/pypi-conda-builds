package:
  name: flask-redis-helper
  version: "1.0.0"

source:
  fn: Flask-Redis-Helper-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Redis-Helper/Flask-Redis-Helper-1.0.0.tar.gz
  md5: 8543ea5f8b2ed8811ddb0c0747e5364d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-redis-helper = flask-redis-helper:main
    #
    # Would create an entry point called flask-redis-helper that calls flask-redis-helper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - redis

  run:
    - python
    - flask
    - redis

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Robpol86/Flask-Redis-Helper
  license: MIT License
  summary: 'Redis support for Flask without breaking PyCharm inspections.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
