package:
  name: lsswap
  version: "0.2"

source:
  fn: lsswap-0.2.tar.gz
  url: https://pypi.python.org/packages/source/l/lsswap/lsswap-0.2.tar.gz
  md5: 2648b267f79dcb73389aa56a7fd6c523
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lsswap = lsswap:main
    #
    # Would create an entry point called lsswap that calls lsswap.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - prettytable

  run:
    - python
    - prettytable

test:
  # Python imports
  imports:
    - lsswap

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/sxd/sysadmin-tools
  license: lstask
  summary: 'lsswap aim to show all the swap information on a GNU/Linux machine'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
