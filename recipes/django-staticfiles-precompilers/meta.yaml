package:
  name: django-staticfiles-precompilers
  version: "0.0.1"

source:
  fn: django-staticfiles-precompilers-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-staticfiles-precompilers/django-staticfiles-precompilers-0.0.1.tar.gz
  md5: 32addd1e4fafb3fa424f51b8a6ef5432
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-staticfiles-precompilers = django-staticfiles-precompilers:main
    #
    # Would create an entry point called django-staticfiles-precompilers that calls django-staticfiles-precompilers.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4.3
    - django-appconf >=0.4
    - envoy >=0.0.2

  run:
    - python
    - django >=1.4.3
    - django-appconf >=0.4
    - envoy >=0.0.2

test:
  # Python imports
  imports:
    - staticfiles_precompilers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/AndrewIngram/django-staticfiles-precompilers
  license: MIT
  summary: "Lets Django's static template tag take care of precompilation"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
