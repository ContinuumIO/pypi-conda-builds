package:
  name: py-narrato
  version: "0.1"

source:
  fn: py-narrato-0.1.tar.gz
  url: https://pypi.python.org/packages/source/p/py-narrato/py-narrato-0.1.tar.gz
  md5: 532b68dff5f2110a5f0efd1dcb781647
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - py-narrato = py-narrato:main
    #
    # Would create an entry point called py-narrato that calls py-narrato.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/narrato/py-narrato/
  license: Apache 2
  summary: 'Minimal Narrato API client for Python.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
