package:
  name: djangode
  version: "0.2"

source:
  fn: DjangoDE-0.2.tar.gz
  url: https://pypi.python.org/packages/source/D/DjangoDE/DjangoDE-0.2.tar.gz
  md5: c13e8bbd01db815246895eb787dd8de1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangode = djangode:main
    #
    # Would create an entry point called djangode that calls djangode.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - djangode
    - djangode.data
    - djangode.data.autocomplete
    - djangode.gui
    - djangode.gui.highlighters
    - djangode.gui.wizards
    - djangode.project
    - djangode.runner
    - djangode.tests
    - djangode.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/djangode/
  license: GNU General Public License (GPL)
  summary: 'An integrated development environment for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
