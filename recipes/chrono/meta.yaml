package:
  name: chrono
  version: "1.0.2"

source:
  fn: chrono-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/c/chrono/chrono-1.0.2.tar.gz
  md5: 347daa2a079dc245ab2b439102315ae4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - chrono = chrono:main
    #
    # Would create an entry point called chrono that calls chrono.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - chrono

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/toastdriven/chrono
  license: BSD License
  summary: 'A (BSD licensed) context manager for timing execution.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
