package:
  name: kotti_settings
  version: "0.2"

source:
  fn: kotti_settings-0.2.zip
  url: https://pypi.python.org/packages/source/k/kotti_settings/kotti_settings-0.2.zip
  md5: cecac1c6bd8f5416b3525e6edfcbb07e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kotti_settings = kotti_settings:main
    #
    # Would create an entry point called kotti_settings that calls kotti_settings.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - kotti >=0.9b2
    - pyramid_deform <=0.2

  run:
    - python
    - kotti >=0.9b2
    - pyramid_deform <=0.2

test:
  # Python imports
  imports:
    - kotti_settings
    - kotti_settings.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/j23d/kotti_settings
  license: BSD License
  summary: 'Settings configuration for Kotti'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
