package:
  name: sqlalchemy-fileattach
  version: "0.1.3"

source:
  fn: sqlalchemy-fileattach-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/s/sqlalchemy-fileattach/sqlalchemy-fileattach-0.1.3.tar.gz
  md5: a850d171346afb5955f2ed01f99986f4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sqlalchemy-fileattach = sqlalchemy-fileattach:main
    #
    # Would create an entry point called sqlalchemy-fileattach that calls sqlalchemy-fileattach.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sqlalchemy

  run:
    - python
    - sqlalchemy

test:
  # Python imports
  imports:
    - sqlalchemy_fileattach
    - sqlalchemy_fileattach.stores
    - tests
    - tests.stores

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/adamcharnock/sqlalchemy-fileattach
  license: MIT
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
