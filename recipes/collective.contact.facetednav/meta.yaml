package:
  name: collective.contact.facetednav
  version: "1.1.2"

source:
  fn: collective.contact.facetednav-1.1.2.zip
  url: https://pypi.python.org/packages/source/c/collective.contact.facetednav/collective.contact.facetednav-1.1.2.zip
  md5: a0e3c50eb521602b309055ceb373a6db
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.contact.facetednav = collective.contact.facetednav:main
    #
    # Would create an entry point called collective.contact.facetednav that calls collective.contact.facetednav.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - collective.contact.core >1.0
    - collective.js.backbone
    - ecreall.helpers.upgrade
    - eea.facetednavigation >=5.8
    - plone.api
    - setuptools

  run:
    - python
    - setuptools
    - collective.contact.core >1.0
    - collective.js.backbone
    - ecreall.helpers.upgrade
    - eea.facetednavigation >=5.8
    - plone.api
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.contact
    - collective.contact.facetednav
    - collective.contact.facetednav.browser
    - collective.contact.facetednav.browser.actions
    - collective.contact.facetednav.tests
    - collective.contact.facetednav.upgrades

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/collective.contact.facetednav
  license: GPL
  summary: 'Faceted navigation view for collective.contact.core directory'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
