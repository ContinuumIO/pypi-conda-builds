package:
  name: plonehrm.notifications
  version: "1.2"

source:
  fn: plonehrm.notifications-1.2.tar.gz
  url: https://pypi.python.org/packages/source/p/plonehrm.notifications/plonehrm.notifications-1.2.tar.gz
  md5: fb2511636987fdb95c9babdab60710a3
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plonehrm.notifications = plonehrm.notifications:main
    #
    # Would create an entry point called plonehrm.notifications that calls plonehrm.notifications.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - plonehrm
    - plonehrm.notifications
    - plonehrm.notifications.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://zestsoftware.nl
  license: GPL
  summary: 'Notifications for Plone HRM'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
