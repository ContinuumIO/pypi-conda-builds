package:
  name: django-location-field
  version: "1.5.2"

source:
  fn: django-location-field-1.5.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-location-field/django-location-field-1.5.2.tar.gz
  md5: eac137894b53e81aa05f0cc4e9fd0c6d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-location-field = django-location-field:main
    #
    # Would create an entry point called django-location-field that calls django-location-field.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six

  run:
    - python
    - six

test:
  # Python imports
  imports:
    - location_field
    - location_field.forms
    - location_field.models

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/caioariede/django-location-field
  license: MIT License
  summary: 'Location field for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
