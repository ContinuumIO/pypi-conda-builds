package:
  name: hardware
  version: "0.14"

source:
  fn: hardware-0.14.tar.gz
  url: https://pypi.python.org/packages/source/h/hardware/hardware-0.14.tar.gz
  md5: caee618334e17f0f506fcdb08b287063
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hardware = hardware:main
    #
    # Would create an entry point called hardware that calls hardware.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - hardware

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Apache Software License
  summary: 'Hardware detection and classification utilities'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
