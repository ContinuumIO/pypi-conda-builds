package:
  name: nortek
  version: "0.0.1"

source:
  fn: Nortek-0.0.1.zip
  url: https://pypi.python.org/packages/source/N/Nortek/Nortek-0.0.1.zip
  md5: 93d5ed422765a8fd4eb6127b535f9ad7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - nortek = nortek:main
    #
    # Would create an entry point called nortek that calls nortek.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - nortek

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/petebachant/Nortek-Python.git
  license: MIT License
  summary: 'Package for working with Nortek instruments and data files.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
