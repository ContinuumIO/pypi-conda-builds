package:
  name: django-reploc
  version: "0.1.1-pre1"

source:
  fn: django-reploc-0.1.1-pre1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-reploc/django-reploc-0.1.1-pre1.tar.gz
  md5: de9ecb5c09eabb8de4b44766668e11bb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-reploc = django-reploc:main
    #
    # Would create an entry point called django-reploc that calls django-reploc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - reploc
    - reploc.management
    - reploc.management.commands
    - sample

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/codekoala/django-reploc/
  license: MIT License
  summary: 'A simple way for people to find representative locations on your Django-powered Web site.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
