package:
  name: buildout_script
  version: "0.2a1"

source:
  fn: buildout_script-0.2a1.tar.gz
  url: https://pypi.python.org/packages/source/b/buildout_script/buildout_script-0.2a1.tar.gz
  md5: 2c4a4cbc3d721474e67448c2c7358985
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - buildout_script = buildout_script:main
    #
    # Would create an entry point called buildout_script that calls buildout_script.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout

test:
  # Python imports
  imports:
    - buildout_script

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://cheeseshop.python.org/pypi/buildout_script
  license: MIT License
  summary: 'zc.buildout recipes for generating script and conf files from templates.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
