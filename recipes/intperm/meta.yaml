package:
  name: intperm
  version: "1.1.1"

source:
  fn: intperm-1.1.1.tar.gz
  url: https://pypi.python.org/packages/source/i/intperm/intperm-1.1.1.tar.gz
  md5: 1271c7aaba133dd2c8a4541fa31eb06b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - intperm = intperm:main
    #
    # Would create an entry point called intperm that calls intperm.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - intperm

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/attilaolah/intperm.py
  license: Public Domain
  summary: 'A simple permutation for arbitrary size integers.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
