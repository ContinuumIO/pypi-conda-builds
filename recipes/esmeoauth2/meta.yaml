package:
  name: esmeoauth2
  version: "1.0"

source:
  fn: EsmeOAuth2-1.0.tar.gz
  url: https://pypi.python.org/packages/source/E/EsmeOAuth2/EsmeOAuth2-1.0.tar.gz
  md5: da370aa690d077388b19537751142cb7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - esmeoauth2 = esmeoauth2:main
    #
    # Would create an entry point called esmeoauth2 that calls esmeoauth2.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - esmeoauth2

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/smartproject/oauth2python
  license: UNKNOWN
  summary: 'A smart, minimal OAuth 2.0 Python client library.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
