package:
  name: ministryofpackages
  version: "0.9.5"

source:
  fn: MinistryOfPackages-0.9.5.tar.gz
  url: https://pypi.python.org/packages/source/M/MinistryOfPackages/MinistryOfPackages-0.9.5.tar.gz
  md5: 77a89a02bb68cce00d100c03737f0687
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ministryofpackages = ministryofpackages:main
    #
    # Would create an entry point called ministryofpackages that calls ministryofpackages.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - MinistryOfPackages
    - MinistryOfPackages.core
    - MinistryOfPackages.handlers
    - ministryofpackages

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/bkjones/MinistryOfPackages
  license: BSD License
  summary: 'A minimal PyPI implementation meant for use behind a firewall.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
