package:
  name: sfdm
  version: "0.3"

source:
  fn: sfDM-0.3.tar.gz
  url: https://pypi.python.org/packages/source/s/sfDM/sfDM-0.3.tar.gz
  md5: b8a7e106670bae97df10d921d769603f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sfdm = sfdm:main
    #
    # Would create an entry point called sfdm that calls sfdm.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - networkx
    - nibabel >=1.0
    - nipy
    - nipype >=0.8
    - numpy >=1.3
    - scipy >=0.7
    - matplotlib
    - pillow
    - ipython

  run:
    - python
    - networkx
    - nibabel >=1.0
    - nipy
    - nipype >=0.8
    - numpy >=1.3
    - scipy >=0.7
    - matplotlib
    - pillow
    - ipython

test:
  # Python imports
  imports:
    - sfDM
    - sfDM.fdm
    - sfDM.res
    - sfDM.ui
    - sfDM.vis
    - sfdm

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/PIRCImagingTools/sfDM/
  license: BSD
  summary: 'serial function Diffusion Mapping software'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
