package:
  name: httprpclib
  version: "0.1.1"

source:
  fn: httprpclib-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/h/httprpclib/httprpclib-0.1.1.tar.gz
  md5: d9af48fa163b7385622bf19e80c8c0a9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - httprpclib = httprpclib:main
    #
    # Would create an entry point called httprpclib that calls httprpclib.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - httprpclib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://zhirsch.name/code/?p=httprpclib.git;a=summary
  license: BSD License
  summary: 'A library that eases the use of RESTful web services'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
