package:
  name: django-multi-sessions
  version: "0.1.0"

source:
  fn: django-multi-sessions-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-multi-sessions/django-multi-sessions-0.1.0.tar.gz
  md5: 99891419738983c5b629a7cba5918336
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-multi-sessions = django-multi-sessions:main
    #
    # Would create an entry point called django-multi-sessions that calls django-multi-sessions.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - multi_sessions
    - multi_sessions.management
    - multi_sessions.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/adw0rd/django-multi-sessions
  license: BSD License
  summary: 'Multi-sessions backend for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
