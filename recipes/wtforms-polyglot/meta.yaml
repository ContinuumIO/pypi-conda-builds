package:
  name: wtforms-polyglot
  version: "0.2"

source:
  fn: WTForms-Polyglot-0.2.tar.gz
  url: https://pypi.python.org/packages/source/W/WTForms-Polyglot/WTForms-Polyglot-0.2.tar.gz
  md5: 63af93f70d077c45a653ddd8397dd7ac
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - wtforms-polyglot = wtforms-polyglot:main
    #
    # Would create an entry point called wtforms-polyglot that calls wtforms-polyglot.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - wtforms

  run:
    - python
    - wtforms

test:
  # Python imports
  imports:
    - wtf_polyglot

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/yggi49/wtforms-polyglot
  license: BSD License
  summary: 'WTForms companion library to provide polyglot HTML (i.e., XML-compatible) output'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
