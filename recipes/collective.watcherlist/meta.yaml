package:
  name: collective.watcherlist
  version: "1.2"

source:
  fn: collective.watcherlist-1.2.zip
  url: https://pypi.python.org/packages/source/c/collective.watcherlist/collective.watcherlist-1.2.zip
  md5: f57d3ea4d6462bb1dff500a2d0adcc20
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.watcherlist = collective.watcherlist:main
    #
    # Would create an entry point called collective.watcherlist that calls collective.watcherlist.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone

  run:
    - python
    - setuptools
    - setuptools
    - plone

test:
  # Python imports
  imports:
    - collective
    - collective.watcherlist
    - collective.watcherlist.actions
    - collective.watcherlist.sample
    - collective.watcherlist.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/collective.watcherlist
  license: GPL
  summary: 'Send emails from Plone to interested members (watchers)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
