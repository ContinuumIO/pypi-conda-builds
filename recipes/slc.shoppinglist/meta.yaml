package:
  name: slc.shoppinglist
  version: "1.5"

source:
  fn: slc.shoppinglist-1.5.zip
  url: https://pypi.python.org/packages/source/s/slc.shoppinglist/slc.shoppinglist-1.5.zip
  md5: 94f3617e9f21d88b794349e9d66d56a0
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - slc.shoppinglist = slc.shoppinglist:main
    #
    # Would create an entry point called slc.shoppinglist that calls slc.shoppinglist.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - slc
    - slc.shoppinglist
    - slc.shoppinglist.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.syslab.com
  license: GNU General Public License (GPL) or European Union Public Licence 1.1 (EUPL 1.1)
  summary: 'A shoppinglist of UIDs'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
