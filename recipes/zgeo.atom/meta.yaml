package:
  name: zgeo.atom
  version: "0.4.1"

source:
  fn: zgeo.atom-0.4.1.tar.gz
  url: https://pypi.python.org/packages/source/z/zgeo.atom/zgeo.atom-0.4.1.tar.gz
  md5: 6f19054b347287bc7e99e886fd0a7231
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zgeo.atom = zgeo.atom:main
    #
    # Would create an entry point called zgeo.atom that calls zgeo.atom.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zgeo.geographer >=0.3

  run:
    - python
    - setuptools
    - setuptools
    - zgeo.geographer >=0.3

test:
  # Python imports
  imports:
    - zgeo
    - zgeo.atom

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://trac.gispython.org/projects/PrimaGIS/wiki/zgeo.atom
  license: BSD License
  summary: 'Atom syndication and AtomPub with GeoRSS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
