package:
  name: appomatic_userena
  version: "0.0.30"

source:
  fn: appomatic_userena-0.0.30.tar.gz
  url: https://pypi.python.org/packages/source/a/appomatic_userena/appomatic_userena-0.0.30.tar.gz
  md5: 9f5cb33a3416d461245661909db275c3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - appomatic_userena = appomatic_userena:main
    #
    # Would create an entry point called appomatic_userena that calls appomatic_userena.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-userena
    - appomaticcore ==0.0.30

  run:
    - python
    - django-userena
    - appomaticcore ==0.0.30

test:
  # Python imports
  imports:
    - appomatic_userena

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.django-userena.org/
  license: GPL
  summary: 'Userena is a Django application that supplies your Django project with full account management.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
