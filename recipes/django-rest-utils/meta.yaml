package:
  name: django-rest-utils
  version: "0.0.5"

source:
  fn: django-rest-utils-0.0.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-rest-utils/django-rest-utils-0.0.5.tar.gz
  md5: 37188a74e72664bcf550b72da1763d20
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-rest-utils = django-rest-utils:main
    #
    # Would create an entry point called django-rest-utils that calls django-rest-utils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - djangorestframework

  run:
    - python
    - djangorestframework

test:
  # Python imports
  imports:
    - rest_utils
    - rest_utils/runtests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/simonluijk/django-rest-utils
  license: BSD License
  summary: 'Utils to help building a REST API with django-rest-framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
