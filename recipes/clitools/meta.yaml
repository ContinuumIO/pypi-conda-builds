package:
  name: clitools
  version: "0.4b1"

source:
  fn: CliTools-0.4b1.tar.gz
  url: https://pypi.python.org/packages/source/C/CliTools/CliTools-0.4b1.tar.gz
  md5: 6f9b7f3da25bc10de90540935fccdebf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - clitools = clitools:main
    #
    # Would create an entry point called clitools that calls clitools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - clitools
    - clitools.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://rshk.github.io/clitools
  license: BSD License
  summary: 'Utilities for building CLI scripts in Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
