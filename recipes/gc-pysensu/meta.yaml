package:
  name: gc-pysensu
  version: "0.6"

source:
  fn: gc-pysensu-0.6.tar.gz
  url: https://pypi.python.org/packages/source/g/gc-pysensu/gc-pysensu-0.6.tar.gz
  md5: 9df9d3e3ab15ffbaf260cab2aa17a62d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gc-pysensu = gc-pysensu:main
    #
    # Would create an entry point called gc-pysensu that calls gc-pysensu.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=1.2.3
    - simplejson >=3.3.0

  run:
    - python
    - requests >=1.2.3
    - simplejson >=3.3.0

test:
  # Python imports
  imports:
    - pysensu

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kdaniels/pysensu
  license: UNKNOWN
  summary: 'Utilities for working with Sensu'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
