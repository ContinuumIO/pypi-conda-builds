package:
  name: django-restviews
  version: "1.0-rc1"

source:
  fn: django-restviews-1.0-rc1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-restviews/django-restviews-1.0-rc1.tar.gz
  md5: 5f228204d3e54b53ed31df0a355b3a39
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-restviews = django-restviews:main
    #
    # Would create an entry point called django-restviews that calls django-restviews.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6

  run:
    - python
    - django >=1.6

test:
  # Python imports
  imports:
    - restviews

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dploeger/django-restviews
  license: MIT License
  summary: 'REST-based Django CrUD-Views'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
