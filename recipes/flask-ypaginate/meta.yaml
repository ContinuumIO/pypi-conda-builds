package:
  name: flask-ypaginate
  version: "0.1.3"

source:
  fn: flask-ypaginate-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/f/flask-ypaginate/flask-ypaginate-0.1.3.tar.gz
  md5: 73b9d926e0450317cc45e788b09404fb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-ypaginate = flask-ypaginate:main
    #
    # Would create an entry point called flask-ypaginate that calls flask-ypaginate.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask

  run:
    - python
    - flask

test:
  # Python imports
  imports:
    - flaskext

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/yanhan/flask-ypaginate
  license: BSD License
  summary: 'Pagination for Flask'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
