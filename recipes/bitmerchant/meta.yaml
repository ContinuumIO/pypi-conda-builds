package:
  name: bitmerchant
  version: "0.1.5"

source:
  fn: bitmerchant-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/b/bitmerchant/bitmerchant-0.1.5.tar.gz
  md5: 04cd8e0e8685cf677d940d7107c1565f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bitmerchant = bitmerchant:main
    #
    # Would create an entry point called bitmerchant that calls bitmerchant.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - base58 ==0.2.1
    - ecdsa ==0.10
    - six ==1.5.2

  run:
    - python
    - base58 ==0.2.1
    - ecdsa ==0.10
    - six ==1.5.2

test:
  # Python imports
  imports:
    - bitmerchant
    - bitmerchant.wallet

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/sbuss/bitmerchant
  license: MIT License
  summary: 'Bitcoin/altcoin merchant tools'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
