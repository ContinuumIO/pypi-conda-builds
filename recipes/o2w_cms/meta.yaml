package:
  name: o2w_cms
  version: "0.0.5"

source:
  fn: o2w_cms-0.0.5.tar.gz
  url: https://pypi.python.org/packages/source/o/o2w_cms/o2w_cms-0.0.5.tar.gz
  md5: 6e432ed265efdbef8f839eacfe0445f7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - o2w_cms = o2w_cms:main
    #
    # Would create an entry point called o2w_cms that calls o2w_cms.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - django-annoying
    - django-pipeline
    - cssmin
    - slimit
    - south
    - django-modeltranslation
    - easy-thumbnails
    - image-diet
    - django-extensions
    - django-rosetta
    - django-filebrowser
    - django-pure-pagination
    - django-debug-toolbar
    - raven
    - o2w-cache-invalidator
    - o2w-templates
    - o2w-core
    - o2w-settings-cms

  run:
    - python
    - django
    - django-annoying
    - django-pipeline
    - cssmin
    - slimit
    - south
    - django-modeltranslation
    - easy-thumbnails
    - image-diet
    - django-extensions
    - django-rosetta
    - django-filebrowser
    - django-pure-pagination
    - django-debug-toolbar
    - raven
    - o2w-cache-invalidator
    - o2w-templates
    - o2w-core
    - o2w-settings-cms

test:
  # Python imports
  imports:
    - o2w
    - o2w_cms

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.o2w.es/
  license: LICENSE.txt
  summary: u'Base package for web creation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
