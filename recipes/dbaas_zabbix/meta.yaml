package:
  name: dbaas_zabbix
  version: "0.2.1"

source:
  fn: dbaas_zabbix-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/dbaas_zabbix/dbaas_zabbix-0.2.1.tar.gz
  md5: 627cfa51ddcbcb38ecef507d082a2309
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dbaas_zabbix = dbaas_zabbix:main
    #
    # Would create an entry point called dbaas_zabbix that calls dbaas_zabbix.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - dbaas_zabbix

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/globocom/dbaas-zabbix
  license: BSD License
  summary: 'A Zabbix Integration for DBaaS.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
