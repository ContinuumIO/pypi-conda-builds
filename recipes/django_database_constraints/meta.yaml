package:
  name: django_database_constraints
  version: "0.2"

source:
  fn: django_database_constraints-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django_database_constraints/django_database_constraints-0.2.tar.gz
  md5: 420b2f61c2cfacf8702bc00818685efd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_database_constraints = django_database_constraints:main
    #
    # Would create an entry point called django_database_constraints that calls django_database_constraints.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6.0

  run:
    - python
    - django >=1.6.0

test:
  # Python imports
  imports:
    - django_database_constraints

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jaylett/django-database-constraints
  license: MIT License
  summary: 'Django library for more easily working with transactions and constraints in Forms, ModelForms and the Views that use them.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
