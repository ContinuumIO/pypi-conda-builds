package:
  name: ploneconf-demo
  version: "1.0"

source:
  fn: ploneconf-demo-1.0.tar.gz
  url: https://pypi.python.org/packages/source/p/ploneconf-demo/ploneconf-demo-1.0.tar.gz
  md5: e7813d311f29a3db4e0172d1f2956e8a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ploneconf-demo = ploneconf-demo:main
    #
    # Would create an entry point called ploneconf-demo that calls ploneconf-demo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://example.com
  license: UNKNOWN
  summary: 'demo of distutils for the plone conf -- will be removed from PyPI after the tutorial'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
