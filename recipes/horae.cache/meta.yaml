package:
  name: horae.cache
  version: "1.0a1"

source:
  fn: horae.cache-1.0a1.tar.gz
  url: https://pypi.python.org/packages/source/h/horae.cache/horae.cache-1.0a1.tar.gz
  md5: 1649a0c3feeab9aa1f66da6107e3b4bc
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - horae.cache = horae.cache:main
    #
    # Would create an entry point called horae.cache that calls horae.cache.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.memoize
    - grok

  run:
    - python
    - setuptools
    - setuptools
    - plone.memoize
    - grok

test:
  # Python imports
  imports:
    - horae
    - horae.cache

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: GPL
  summary: 'Provides utilities and decorators for caching'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
