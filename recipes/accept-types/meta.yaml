package:
  name: accept-types
  version: "0.2.0"

source:
  fn: accept-types-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/a/accept-types/accept-types-0.2.0.tar.gz
  md5: c2fe97fae568c17f9c413edb4fcd31f1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - accept-types = accept-types:main
    #
    # Would create an entry point called accept-types that calls accept-types.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - accept_types

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/tim_heap/python-accept-types
  license: UNKNOWN
  summary: 'Determine the best content to send in an HTTP response'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
