package:
  name: products.plonetableless
  version: "2.0b4"

source:
  fn: Products.PloneTableless-2.0b4.zip
  url: https://pypi.python.org/packages/source/P/Products.PloneTableless/Products.PloneTableless-2.0b4.zip
  md5: 8eeec260c87d04f136e80cc9fbcbfb6b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - products.plonetableless = products.plonetableless:main
    #
    # Would create an entry point called products.plonetableless that calls products.plonetableless.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plonetheme.classic

  run:
    - python
    - setuptools
    - setuptools
    - plonetheme.classic

test:
  # Python imports
  imports:
    - Products
    - Products.PloneTableless
    - Products.PloneTableless.Extensions

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Raptus/Products.PloneTableless
  license: GPL
  summary: 'Plone Tableless provides a completly tableless version of the Plone Default theme'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
