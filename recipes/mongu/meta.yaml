package:
  name: mongu
  version: "0.4.4"

source:
  fn: mongu-0.4.4.tar.gz
  url: https://pypi.python.org/packages/source/m/mongu/mongu-0.4.4.tar.gz
  md5: 323c4ab4c635f3fa084a62340eddc5e4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mongu = mongu:main
    #
    # Would create an entry point called mongu that calls mongu.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pymongo >=2.7

  run:
    - python
    - pymongo >=2.7

test:
  # Python imports
  imports:
    - mongu

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/tevino/mongu
  license: MIT
  summary: 'Yet another Python Object-Document Mapper on top of PyMongo.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
