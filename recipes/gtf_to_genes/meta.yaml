package:
  name: gtf_to_genes
  version: "1.40"

source:
  fn: gtf_to_genes-1.40.tar.gz
  url: https://pypi.python.org/packages/source/g/gtf_to_genes/gtf_to_genes-1.40.tar.gz
  md5: 15dea634078a0fc1367c7adb8c13c280
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gtf_to_genes = gtf_to_genes:main
    #
    # Would create an entry point called gtf_to_genes that calls gtf_to_genes.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - gtf_to_genes

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/gtf-to-genes/
  license: MIT License
  summary: 'Fast GTF parser'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
