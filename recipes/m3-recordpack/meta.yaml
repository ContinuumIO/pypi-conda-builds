package:
  name: m3-recordpack
  version: "0.1.0.13"

source:
  fn: m3-recordpack-0.1.0.13.tar.gz
  url: https://pypi.python.org/packages/source/m/m3-recordpack/m3-recordpack-0.1.0.13.tar.gz
  md5: 70fc363c0c1eceb5c3e78ea101fee67a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - m3-recordpack = m3-recordpack:main
    #
    # Would create an entry point called m3-recordpack that calls m3-recordpack.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - m3-core
    - m3-ui
    - django

  run:
    - python
    - m3-core
    - m3-ui
    - django

test:
  # Python imports
  imports:
    - recordpack
    - recordpack.demo

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT License
  summary: u'\u041f\u0430\u043a \u043e\u0431\u0449\u0435\u0433\u043e \u043d\u0430\u0437\u043d\u0430\u0447\u0435\u043d\u0438\u044f, \u0434\u043b\u044f \u0444\u043e\u0440\u043c\u0438\u0440\u043e\u0432\u0430\u043d\u0438\u044f \u0438 \u0443\u043f\u0440\u0430\u0432\u043b\u0435\u043d\u0438\u044f \u0436\u0443\u0440\u043d\u0430\u043b\u044c\u043d\u044b\u043c\u0438 \u043e\u043a\u043d\u0430\u043c\u0438 \u0438 \u043e\u043a\u043d\u0430\u043c\u0438 \u0441\u043f\u0440\u0430\u0432\u043e\u0447\u043d\u0438\u043a\u043e\u0432'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
