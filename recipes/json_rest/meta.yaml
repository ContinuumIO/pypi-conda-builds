package:
  name: json_rest
  version: "0.1.6"

source:
  fn: json_rest-0.1.6.tar.gz
  url: https://pypi.python.org/packages/source/j/json_rest/json_rest-0.1.6.tar.gz
  md5: a04ae5c2efa1dac6436fab314f01c29b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - json_rest = json_rest:main
    #
    # Would create an entry point called json_rest that calls json_rest.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python-cjson
    - pyforge
    - sentinels

  run:
    - python
    - python-cjson
    - pyforge
    - sentinels

test:
  # Python imports
  imports:
    - json_rest

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD License
  summary: 'Utility for REST/JSON based web services'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
