package:
  name: django-queries
  version: "0.2"

source:
  fn: django-queries-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-queries/django-queries-0.2.tar.gz
  md5: 039e6512a689f81f236ba136ab7e1c6f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-queries = django-queries:main
    #
    # Would create an entry point called django-queries that calls django-queries.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - queries
    - queries.templatetags
    - queries.views

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/django-queries/
  license: BSD License
  summary: 'Generic query interface for django models'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
