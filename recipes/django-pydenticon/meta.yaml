package:
  name: django-pydenticon
  version: "0.1"

source:
  fn: django-pydenticon-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-pydenticon/django-pydenticon-0.1.tar.gz
  md5: 2e3df25caee182531a7ae8eed69a3b58
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-pydenticon = django-pydenticon:main
    #
    # Would create an entry point called django-pydenticon that calls django-pydenticon.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pydenticon
    - django

  run:
    - python
    - pydenticon
    - django

test:
  # Python imports
  imports:
    - django_pydenticon

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/azaghal/django-pydenticon
  license: BSD License
  summary: 'Django application providing generated identicons to the web.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
