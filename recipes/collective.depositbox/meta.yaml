package:
  name: collective.depositbox
  version: "1.2"

source:
  fn: collective.depositbox-1.2.zip
  url: https://pypi.python.org/packages/source/c/collective.depositbox/collective.depositbox-1.2.zip
  md5: c873178ab10ff63ed9653e578ad7e80f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.depositbox = collective.depositbox:main
    #
    # Would create an entry point called collective.depositbox that calls collective.depositbox.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.depositbox
    - collective.depositbox.sample

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.depositbox
  license: GPL
  summary: 'Put stuff in a box, get it out again with the secret'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
