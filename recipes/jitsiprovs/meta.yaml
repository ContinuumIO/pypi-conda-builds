package:
  name: jitsiprovs
  version: "0.1.0"

source:
  fn: JitsiProvS-0.10b1.tar.gz
  url: https://pypi.python.org/packages/source/J/JitsiProvS/JitsiProvS-0.10b1.tar.gz
  md5: dcbab6f37d9b1277d020e9c649529df5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jitsiprovs = jitsiprovs:main
    #
    # Would create an entry point called jitsiprovs that calls jitsiprovs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python-daemon
    - sqlobject
    - flask

  run:
    - python
    - python-daemon
    - sqlobject
    - flask

test:
  # Python imports
  imports:
    - jitsiprovs

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.itsyscom.com
  license: GNU General Public License (GPL)
  summary: 'Jitsi Provisioning Server'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
