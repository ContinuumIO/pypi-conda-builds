package:
  name: dict.cc.py
  version: "1.0"

source:
  fn: dict.cc.py-1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/dict.cc.py/dict.cc.py-1.0.tar.gz
  md5: 2042c397a7341598effad4730b373a44
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dict.cc.py = dict.cc.py:main
    #
    # Would create an entry point called dict.cc.py that calls dict.cc.py.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - beautifulsoup4

  run:
    - python
    - beautifulsoup4

test:
  # Python imports
  imports:
    - dictcc

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/rbaron/dict.cc.py
  license: UNKNOWN
  summary: 'Unofficial dict.cc command line interface'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
