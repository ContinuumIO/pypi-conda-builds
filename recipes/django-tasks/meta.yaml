package:
  name: django-tasks
  version: "0.3"

source:
  fn: django-tasks-0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-tasks/django-tasks-0.3.tar.gz
  md5: c7da668a684aaa898fa967789f01777a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-tasks = django-tasks:main
    #
    # Would create an entry point called django-tasks that calls django-tasks.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django ==1.5
    - south

  run:
    - python
    - django ==1.5
    - south

test:
  # Python imports
  imports:
    - django_tasks

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/luanfonceca/todo-list/
  license: License :: Public Domain
  summary: 'Python Django Tasks.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
