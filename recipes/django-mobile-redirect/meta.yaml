package:
  name: django-mobile-redirect
  version: "1.0.0"

source:
  fn: django-mobile-redirect-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-mobile-redirect/django-mobile-redirect-1.0.0.tar.gz
  md5: 2a7b4e379c07a962691d83ae753e9458
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-mobile-redirect = django-mobile-redirect:main
    #
    # Would create an entry point called django-mobile-redirect that calls django-mobile-redirect.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5

  run:
    - python
    - django >=1.5

test:
  # Python imports
  imports:
    - mobile_redirect
    - mobile_redirect.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ryanbagwell/django-mobile-redirect
  license: MIT
  summary: 'Redirects devices with mobile user agent strings to a specified url.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
