package:
  name: hackmanager
  version: "2.4.0"

source:
  fn: hackManager-2.4.0.zip
  url: https://pypi.python.org/packages/source/h/hackManager/hackManager-2.4.0.zip
  md5: de149bf7d02966d2ead5f4b4919fe5d6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hackmanager = hackmanager:main
    #
    # Would create an entry point called hackmanager that calls hackmanager.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - ctypes
    - winappdbg

  run:
    - python
    - ctypes
    - winappdbg

test:
  # Python imports
  imports:
    - hackManager
    - hackmanager

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/SirFroweey/hackManager
  license: MIT License
  summary: 'Process memory hacking(editing) library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
