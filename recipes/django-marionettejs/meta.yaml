package:
  name: django-marionettejs
  version: "2.4.0.1"

source:
  fn: django-marionettejs-2.4.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-marionettejs/django-marionettejs-2.4.0.1.tar.gz
  md5: b03ad33df0d62dff18eac7af5ad82d1a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-marionettejs = django-marionettejs:main
    #
    # Would create an entry point called django-marionettejs that calls django-marionettejs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-compressor

  run:
    - python
    - django-compressor

test:
  # Python imports
  imports:
    - marionettejs

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD License
  summary: 'A simple Django app to be extends for easier backbone development, it provides a base template to inherit'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
