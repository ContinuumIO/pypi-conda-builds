package:
  name: dsns-sdk
  version: "0.1.1"

source:
  fn: DSNS-SDK-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/D/DSNS-SDK/DSNS-SDK-0.1.1.tar.gz
  md5: 996e85df5b52e2b7fb9dcb142e74b13d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dsns-sdk = dsns-sdk:main
    #
    # Would create an entry point called dsns-sdk that calls dsns-sdk.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django >=1.4

  run:
    - python
    - django >=1.4

test:
  # Python imports
  imports:
    - dsns

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/harkishan81001/dsns-sdk
  license: LICENSE.txt
  summary: 'DSNS-SDK to interact with Delhivery Simpler Notification Service'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
