package:
  name: django-password-protect
  version: "0.0.1"

source:
  fn: Django-Password-Protect-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/D/Django-Password-Protect/Django-Password-Protect-0.0.1.tar.gz
  md5: 03f7b084912bf21287dda5e2dda99d1c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-password-protect = django-password-protect:main
    #
    # Would create an entry point called django-password-protect that calls django-password-protect.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - django_password_protect

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/plumdog/django-password-protect
  license: UNKNOWN
  summary: 'Middleware to add BasicAuth password protection to a site'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
