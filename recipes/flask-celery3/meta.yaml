package:
  name: flask-celery3
  version: "0.1"

source:
  fn: Flask-Celery3-0.1.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Celery3/Flask-Celery3-0.1.tar.gz
  md5: 32e566c9153b34e5bddef5859ec5ec6b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-celery3 = flask-celery3:main
    #
    # Would create an entry point called flask-celery3 that calls flask-celery3.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - celery

  run:
    - python
    - flask
    - celery

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jaapz/flask-celery3
  license: MIT License
  summary: 'Add Celery3 integration to your Flask apps.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
