package:
  name: django-typogrify
  version: "1.3.1"

source:
  fn: django-typogrify-1.3.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-typogrify/django-typogrify-1.3.1.tar.gz
  md5: 698acec83d47706a2aee9ff79ff5bcbf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-typogrify = django-typogrify:main
    #
    # Would create an entry point called django-typogrify that calls django-typogrify.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - smartypants >=1.8.3

  run:
    - python
    - smartypants >=1.8.3

test:
  # Python imports
  imports:
    - typogrify
    - typogrify.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/chrisdrackett/django-typogrify
  license: BSD License
  summary: 'Make type not look like crap (for django apps)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
