package:
  name: djangocms-accordion
  version: "1.0.4"

source:
  fn: djangocms-accordion-1.0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/djangocms-accordion/djangocms-accordion-1.0.4.tar.gz
  md5: 0a5ac535839121459cb2a5eb83d803ce
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangocms-accordion = djangocms-accordion:main
    #
    # Would create an entry point called djangocms-accordion that calls djangocms-accordion.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - djangocms_accordion
    - djangocms_accordion.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/divio/djangocms-accordion
  license: BSD License
  summary: 'Accordion Plugin for django CMS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
