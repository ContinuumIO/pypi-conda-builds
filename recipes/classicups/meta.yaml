package:
  name: classicups
  version: "0.1.8"

source:
  fn: ClassicUPS-0.1.8.tar.gz
  url: https://pypi.python.org/packages/source/C/ClassicUPS/ClassicUPS-0.1.8.tar.gz
  md5: 77b7b9e70d02ef2a2dbc185e008de991
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - classicups = classicups:main
    #
    # Would create an entry point called classicups that calls classicups.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - dict2xml ==1.0
    - xmltodict ==0.4.2

  run:
    - python
    - dict2xml ==1.0
    - xmltodict ==0.4.2

test:
  # Python imports
  imports:
    - ClassicUPS
    - classicups

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/classicspecs/ClassicUPS/
  license: Apache Software License
  summary: 'Usable UPS Integration in Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
