package:
  name: django-httplog
  version: "0.2.3"

source:
  fn: django-httplog-0.2.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-httplog/django-httplog-0.2.3.tar.gz
  md5: c0b80e3b0fd953b467c5aabfc98667c3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-httplog = django-httplog:main
    #
    # Would create an entry point called django-httplog that calls django-httplog.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - httplog

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/futurecolors/django-httplog
  license: BSD License
  summary: 'Very simple http request-response log in database for debugging APIs.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
