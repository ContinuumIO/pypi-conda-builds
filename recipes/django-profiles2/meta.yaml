package:
  name: django-profiles2
  version: "0.6"

source:
  fn: django-profiles2-0.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-profiles2/django-profiles2-0.6.tar.gz
  md5: 453bc9f0a036fc945473e0e38dcdbf94
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-profiles2 = django-profiles2:main
    #
    # Would create an entry point called django-profiles2 that calls django-profiles2.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - profiles

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.bitbucket.org/fcurella/django-profiles/wiki/
  license: BSD License
  summary: 'User-profile application for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
