package:
  name: djangorestframework-msgpack
  version: "1.0.1"

source:
  fn: djangorestframework-msgpack-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/djangorestframework-msgpack/djangorestframework-msgpack-1.0.1.tar.gz
  md5: 2de8a94b2169c0b5ecae296ae1063e6c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangorestframework-msgpack = djangorestframework-msgpack:main
    #
    # Would create an entry point called djangorestframework-msgpack that calls djangorestframework-msgpack.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4.3
    - djangorestframework >=2.1.15
    - msgpack-python >=0.2.4
    - python-dateutil ==2.1

  run:
    - python
    - django >=1.4.3
    - djangorestframework >=2.1.15
    - msgpack-python >=0.2.4
    - python-dateutil ==2.1

test:
  # Python imports
  imports:
    - rest_framework_msgpack

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/juanriaza/django-rest-framework-msgpack
  license: BSD License
  summary: 'MessagePack support for Django REST framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
