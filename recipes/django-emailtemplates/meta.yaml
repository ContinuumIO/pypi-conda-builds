package:
  name: django-emailtemplates
  version: "0.8.1"

source:
  fn: django-emailtemplates-0.8.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-emailtemplates/django-emailtemplates-0.8.1.tar.gz
  md5: ace476fa6af6ae142a25cb3374a49e8c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-emailtemplates = django-emailtemplates:main
    #
    # Would create an entry point called django-emailtemplates that calls django-emailtemplates.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - emailtemplates

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/deployed/django-emailtemplates
  license: MIT License
  summary: 'A simple Django app to create emails based on database or filesystem templates.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
