package:
  name: django-markdown-utils
  version: "0.1"

source:
  fn: django-markdown-utils-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-markdown-utils/django-markdown-utils-0.1.tar.gz
  md5: 67579983a79cc38783e1763977de3f84
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-markdown-utils = django-markdown-utils:main
    #
    # Would create an entry point called django-markdown-utils that calls django-markdown-utils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - markdown

  run:
    - python
    - django
    - markdown

test:
  # Python imports
  imports:
    - markdown_utils
    - markdown_utils.templatetags
    - markdown_utils.tests
    - markdown_utils.tests.test_app

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bitmazk/django-markdown-utils
  license: The MIT License
  summary: 'A reusable Django app with some useful markdown utilities\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
