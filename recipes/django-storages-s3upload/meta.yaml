package:
  name: django-storages-s3upload
  version: "0.1.6"

source:
  fn: django-storages-s3upload-0.1.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-storages-s3upload/django-storages-s3upload-0.1.6.tar.gz
  md5: 0c060531e7c4aa6805f82aaeda52e76b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-storages-s3upload = django-storages-s3upload:main
    #
    # Would create an entry point called django-storages-s3upload that calls django-storages-s3upload.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - boto
    - django
    - django-storages
    - python-magic

  run:
    - python
    - boto
    - django
    - django-storages
    - python-magic

test:
  # Python imports
  imports:
    - s3upload

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mattaustin/django-storages-s3upload
  license: Apache Software License
  summary: 'Direct (client-side) HTTP POST file upload to S3 using Django forms/views.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
