package:
  name: jython-sikuli-server
  version: "0.1.8.2"

source:
  fn: jython-sikuli-server-0.1.8.2.tar.gz
  url: https://pypi.python.org/packages/source/j/jython-sikuli-server/jython-sikuli-server-0.1.8.2.tar.gz
  md5: 99910012d19982b457134866377a2efd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jython-sikuli-server = jython-sikuli-server:main
    #
    # Would create an entry point called jython-sikuli-server that calls jython-sikuli-server.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - jython_sikuli_server

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/alistair-broomhead/jython-sikuli-server
  license: MIT
  summary: 'Jython script to run a robot remote library exposing the Sikuli API (and popen).'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
