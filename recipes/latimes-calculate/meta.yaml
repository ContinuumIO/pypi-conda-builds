package:
  name: latimes-calculate
  version: "0.3.0"

source:
  fn: latimes-calculate-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/l/latimes-calculate/latimes-calculate-0.3.0.tar.gz
  md5: 830d565f4e886a86dccbcb5b62824806
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - latimes-calculate = latimes-calculate:main
    #
    # Would create an entry point called latimes-calculate that calls latimes-calculate.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six >=1.4.1

  run:
    - python
    - six >=1.4.1

test:
  # Python imports
  imports:
    - calculate

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/datadesk/latimes-calculate
  license: MIT License
  summary: 'Some simple math we use to do journalism.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
