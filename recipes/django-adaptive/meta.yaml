package:
  name: django-adaptive
  version: "1.0.0"

source:
  fn: django-adaptive-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-adaptive/django-adaptive-1.0.0.tar.gz
  md5: 82c49f466c25c2f95398143335652aa3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-adaptive = django-adaptive:main
    #
    # Would create an entry point called django-adaptive that calls django-adaptive.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6
    - django-mobi >=0.1.7

  run:
    - python
    - django >=1.6
    - django-mobi >=0.1.7

test:
  # Python imports
  imports:
    - django_adaptive

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.revsquare.com
  license: BSD License
  summary: 'Manages to load templates according to the type of device to        a specific directory prefix.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
