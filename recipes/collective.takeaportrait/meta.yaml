package:
  name: collective.takeaportrait
  version: "0.2.0"

source:
  fn: collective.takeaportrait-0.2.0.zip
  url: https://pypi.python.org/packages/source/c/collective.takeaportrait/collective.takeaportrait-0.2.0.zip
  md5: be6c0bff430708e2ee398e18fe4abed7
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.takeaportrait = collective.takeaportrait:main
    #
    # Would create an entry point called collective.takeaportrait that calls collective.takeaportrait.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - jarn.jsi18n

  run:
    - python
    - setuptools
    - setuptools
    - jarn.jsi18n

test:
  # Python imports
  imports:
    - collective
    - collective.takeaportrait
    - collective.takeaportrait.browser
    - collective.takeaportrait.opencv

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/collective.takeaportrait
  license: GPL
  summary: 'Plone site members can change personal portrait using a webcam'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
