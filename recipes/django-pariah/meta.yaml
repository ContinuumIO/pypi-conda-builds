package:
  name: django-pariah
  version: "0.2"

source:
  fn: django-pariah-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-pariah/django-pariah-0.2.tar.gz
  md5: d4a7b02395e73de47a95b56f202aa16d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-pariah = django-pariah:main
    #
    # Would create an entry point called django-pariah that calls django-pariah.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >1.2
    - pil

  run:
    - python
    - django >1.2
    - pil

test:
  # Python imports
  imports:
    - pariah

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rockstar/django-pariah
  license: http://www.opensource.org/licenses/mit-license.php
  summary: 'A Django app for creating web comics'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
