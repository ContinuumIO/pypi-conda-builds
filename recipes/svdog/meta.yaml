package:
  name: svdog
  version: "0.1.18"

source:
  fn: svdog-0.1.18.tar.gz
  url: https://pypi.python.org/packages/source/s/svdog/svdog-0.1.18.tar.gz
  md5: f3fa8484e7f306936553e8a4508f3797
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - svdog = svdog:main
    #
    # Would create an entry point called svdog that calls svdog.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - supervisor
    - flylog

  run:
    - python
    - supervisor
    - flylog

test:
  # Python imports
  imports:
    - svdog

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dantezhu/svdog
  license: BSD
  summary: "supervisor's dog, should deploy with flylog"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
