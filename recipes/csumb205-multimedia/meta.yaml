package:
  name: csumb205-multimedia
  version: "0.8.12"

source:
  fn: csumb205_multimedia-0.8.12.tar.gz
  url: https://pypi.python.org/packages/source/c/csumb205-multimedia/csumb205_multimedia-0.8.12.tar.gz
  md5: ca79798a93b7e7b8fec6b1a64bfa81c9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - csumb205-multimedia = csumb205-multimedia:main
    #
    # Would create an entry point called csumb205-multimedia that calls csumb205-multimedia.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pillow
    - numpy

  run:
    - python
    - pillow
    - numpy

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/pwalker91/CSUMB205_Multimedia
  license: MIT License
  summary: 'A easier-to-use, academic wrapper class for different multimedia'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
