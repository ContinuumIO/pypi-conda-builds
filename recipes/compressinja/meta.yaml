package:
  name: compressinja
  version: "0.0.2"

source:
  fn: compressinja-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/c/compressinja/compressinja-0.0.2.tar.gz
  md5: bedc4e59fce58ee192c437df06ac39a4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - compressinja = compressinja:main
    #
    # Would create an entry point called compressinja that calls compressinja.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - jinja2

  run:
    - python
    - jinja2

test:
  # Python imports
  imports:
    - compressinja

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/stanislavfeldman/compressinja
  license: UNKNOWN
  summary: 'Jinja2 extension that removes whitespace between HTML tags.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
