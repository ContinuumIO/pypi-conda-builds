package:
  name: django-session-attachments
  version: "0.2"

source:
  fn: django-session-attachments-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-session-attachments/django-session-attachments-0.2.tar.gz
  md5: 4d8ab9fbabbac61cb8d87c28a4abecfa
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-session-attachments = django-session-attachments:main
    #
    # Would create an entry point called django-session-attachments that calls django-session-attachments.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - session_attachments

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/NetAngels/django-session-attachments
  license: BSD License
  summary: u'Django app to handle attachments (temporary uploads) grouped by sessions and bundles. Even for anonymous users'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
