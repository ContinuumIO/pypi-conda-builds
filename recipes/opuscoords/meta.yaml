package:
  name: opuscoords
  version: "1.0.1"

source:
  fn: opuscoords-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/o/opuscoords/opuscoords-1.0.1.tar.gz
  md5: 9ec0711581df1e8f5f234390afe1e486
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - opuscoords = opuscoords:main
    #
    # Would create an entry point called opuscoords that calls opuscoords.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - opuscoords

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD License
  summary: 'Python Tools for OPUS Coordinate Conversions'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
