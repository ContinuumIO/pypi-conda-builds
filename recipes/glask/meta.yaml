package:
  name: glask
  version: "0.0.22"

source:
  fn: Glask-0.0.22.tar.gz
  url: https://pypi.python.org/packages/source/G/Glask/Glask-0.0.22.tar.gz
  md5: fcf334691b5eca2318b823f046adb496
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - glask = glask:main
    #
    # Would create an entry point called glask that calls glask.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.10.1
    - python-dateutil >=2.2
    - flask-debugtoolbar >=0.9.0
    - flask-wtf >=0.10.2
    - inflection
    - selenium
    - flask-sqlalchemy
    - alembic
    - psycopg2

  run:
    - python
    - flask >=0.10.1
    - python-dateutil >=2.2
    - flask-debugtoolbar >=0.9.0
    - flask-wtf >=0.10.2
    - inflection
    - selenium
    - flask-sqlalchemy
    - alembic
    - psycopg2

test:
  # Python imports
  imports:
    - glask

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/vamf12/Glask
  license: BSD
  summary: 'An extension for flask applications with best practices.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
