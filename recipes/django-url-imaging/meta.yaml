package:
  name: django-url-imaging
  version: "0.4.1"

source:
  fn: django-url-imaging-0.4.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-url-imaging/django-url-imaging-0.4.1.tar.gz
  md5: 19fa0a57b8ab9af90835dc456befad5a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-url-imaging = django-url-imaging:main
    #
    # Would create an entry point called django-url-imaging that calls django-url-imaging.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - urlimaging
    - urlimaging.backends

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/patrickomatic/django-url-imaging
  license: UNKNOWN
  summary: 'URL-based image processing for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
