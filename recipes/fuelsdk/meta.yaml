package:
  name: fuelsdk
  version: "0.9.3"

source:
  fn: FuelSDK-0.9.3.tar.gz
  url: https://pypi.python.org/packages/source/F/FuelSDK/FuelSDK-0.9.3.tar.gz
  md5: 961f9135c7cfeda158474c126c06650c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fuelsdk = fuelsdk:main
    #
    # Would create an entry point called fuelsdk that calls fuelsdk.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyjwt ==0.1.9
    - requests ==2.2.1
    - suds ==0.4

  run:
    - python
    - pyjwt ==0.1.9
    - requests ==2.2.1
    - suds ==0.4

test:
  # Python imports
  imports:
    - FuelSDK
    - fuelsdk

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ExactTarget/FuelSDK-Python
  license: MIT License
  summary: 'ExactTarget Fuel SDK for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
