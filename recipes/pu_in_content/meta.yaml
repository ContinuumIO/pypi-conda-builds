package:
  name: pu_in_content
  version: "1.0.2"

source:
  fn: pu_in_content-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/p/pu_in_content/pu_in_content-1.0.2.tar.gz
  md5: a5c9322e6101701c62118e7810288902
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pu_in_content = pu_in_content:main
    #
    # Would create an entry point called pu_in_content that calls pu_in_content.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - pu_in_core

  run:
    - python
    - setuptools
    - django
    - pu_in_core

test:
  # Python imports
  imports:
    - pu_in_content
    - pu_in_content.views

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/PythonUnited/pu.intranet.content
  license: beer-ware
  summary: 'PythonUnited Intranet Content'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
