package:
  name: mormon
  version: "0.2.4"

source:
  fn: Mormon-0.2.4.tar.gz
  url: https://pypi.python.org/packages/source/M/Mormon/Mormon-0.2.4.tar.gz
  md5: 3e1d4a1b56648b86f68fe2aa70ff3a2c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mormon = mormon:main
    #
    # Would create an entry point called mormon that calls mormon.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - inflection >=0.2.0
    - pymongo >=2.5.2

  run:
    - python
    - inflection >=0.2.0
    - pymongo >=2.5.2

test:
  # Python imports
  imports:
    - mormon

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/MrTravisB/mormon
  license: UNKNOWN
  summary: 'An object relational mapping library for MongoDB'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
