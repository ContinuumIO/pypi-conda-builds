package:
  name: django-banana
  version: "1.0.0"

source:
  fn: django-banana-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-banana/django-banana-1.0.0.tar.gz
  md5: 3f8267e9320d9d864932f8bf65b1ce00
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-banana = django-banana:main
    #
    # Would create an entry point called django-banana that calls django-banana.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6.0,<1.8.0

  run:
    - python
    - django >=1.6.0,<1.8.0

test:
  # Python imports
  imports:
    - django_banana

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bruun.co/
  license: Apache Software License
  summary: 'Delicious split testing for Django 1.6+'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
