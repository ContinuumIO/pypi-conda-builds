package:
  name: django-valueinlist-filter
  version: "0.1.3"

source:
  fn: django-valueinlist-filter-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-valueinlist-filter/django-valueinlist-filter-0.1.3.tar.gz
  md5: 7b45024f0b44f0c37b5b07f7f72fc379
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-valueinlist-filter = django-valueinlist-filter:main
    #
    # Would create an entry point called django-valueinlist-filter that calls django-valueinlist-filter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5

  run:
    - python
    - django >=1.5

test:
  # Python imports
  imports:
    - valueinlist_filter

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/gzeronet/django-valueinlist-filter
  license: BSD License
  summary: 'Queryset can be filtered by a given list on the django admin page, just like: qs.filter(field__in=[list])'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
