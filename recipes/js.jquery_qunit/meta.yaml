package:
  name: js.jquery_qunit
  version: "0.5"

source:
  fn: js.jquery_qunit-0.5.tar.gz
  url: https://pypi.python.org/packages/source/j/js.jquery_qunit/js.jquery_qunit-0.5.tar.gz
  md5: ba935c3e8bd8353e7124ff07858438e4
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - js.jquery_qunit = js.jquery_qunit:main
    #
    # Would create an entry point called js.jquery_qunit that calls js.jquery_qunit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fanstatic
    - setuptools
    - js.jquery

  run:
    - python
    - setuptools
    - fanstatic
    - setuptools
    - js.jquery

test:
  # Python imports
  imports:
    - js
    - js.jquery_qunit

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD
  summary: 'Fanstatic packaging of jQuery QUnit'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
