package:
  name: python-ivi
  version: "0.14.9"

source:
  fn: python-ivi-0.14.9.tar.gz
  url: https://pypi.python.org/packages/source/p/python-ivi/python-ivi-0.14.9.tar.gz
  md5: 45ddd3349cad61e5fecae55ca10adc9d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-ivi = python-ivi:main
    #
    # Would create an entry point called python-ivi that calls python-ivi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - ivi
    - ivi.agilent
    - ivi.chroma
    - ivi.colby
    - ivi.dicon
    - ivi.extra
    - ivi.ics
    - ivi.interface
    - ivi.jdsu
    - ivi.lecroy
    - ivi.rigol
    - ivi.scpi
    - ivi.tektronix
    - ivi.testequity

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://alexforencich.com/wiki/en/python-ivi/start
  license: MIT License
  summary: 'Python Interchangeable Virtual Instrument Library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
