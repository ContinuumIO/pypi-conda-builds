package:
  name: babel-obviel
  version: "0.7.1"

source:
  fn: babel-obviel-0.7.1.tar.gz
  url: https://pypi.python.org/packages/source/b/babel-obviel/babel-obviel-0.7.1.tar.gz
  md5: 4f05954fe0840d90597947069b102532
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - babel-obviel = babel-obviel:main
    #
    # Would create an entry point called babel-obviel that calls babel-obviel.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - lxml

  run:
    - python
    - setuptools
    - lxml

test:
  # Python imports
  imports:
    - babelobviel

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD
  summary: 'Obviel Template message extractor for Babel'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
