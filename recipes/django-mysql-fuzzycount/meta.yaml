package:
  name: django-mysql-fuzzycount
  version: "0.3"

source:
  fn: django-mysql-fuzzycount-0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-mysql-fuzzycount/django-mysql-fuzzycount-0.3.tar.gz
  md5: 7106599bf92c3df975ad5d780191f1e7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-mysql-fuzzycount = django-mysql-fuzzycount:main
    #
    # Would create an entry point called django-mysql-fuzzycount that calls django-mysql-fuzzycount.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5
    - django-model-utils >=1.4.0

  run:
    - python
    - django >=1.5
    - django-model-utils >=1.4.0

test:
  # Python imports
  imports:
    - mysql_fuzzycount

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/educreations/django-mysql-fuzzycount
  license: MIT License
  summary: 'Approximate query counts for MySQL and Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
