package:
  name: mongoadmin
  version: "0.2"

source:
  fn: mongoadmin-0.2.tar.gz
  url: https://pypi.python.org/packages/source/m/mongoadmin/mongoadmin-0.2.tar.gz
  md5: ebf2bceb81ad1cdccec56f32e7e2877b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mongoadmin = mongoadmin:main
    #
    # Would create an entry point called mongoadmin that calls mongoadmin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3
    - mongoengine >=0.6
    - mongodbforms

  run:
    - python
    - setuptools
    - django >=1.3
    - mongoengine >=0.6
    - mongodbforms

test:
  # Python imports
  imports:
    - mongoadmin
    - mongoadmin.contenttypes
    - mongoadmin.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.schafproductions.com/projects/mongo-admin/
  license: BSD License
  summary: "A replacement for django's admin that works with mongodb."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
