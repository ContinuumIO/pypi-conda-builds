package:
  name: collective.idashboard
  version: "1.2.6a2"

source:
  fn: collective.idashboard-1.2.6a2.zip
  url: https://pypi.python.org/packages/source/c/collective.idashboard/collective.idashboard-1.2.6a2.zip
  md5: d5d7615a3aef2b869e463a72e3d9f68c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.idashboard = collective.idashboard:main
    #
    # Would create an entry point called collective.idashboard that calls collective.idashboard.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.js.jquery
    - collective.js.jqueryui
    - collective.alerts

  run:
    - python
    - setuptools
    - setuptools
    - collective.js.jquery
    - collective.js.jqueryui
    - collective.alerts

test:
  # Python imports
  imports:
    - collective
    - collective.idashboard
    - collective.idashboard.browser
    - collective.idashboard.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/collective.idashboard
  license: GPL
  summary: 'This Plone add-on product gives your dashboard features similiar to those of the iGoogle dashboard.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
