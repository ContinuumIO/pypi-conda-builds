package:
  name: dtls
  version: "1.0.1"

source:
  fn: Dtls-1.0.1.sdist_with_openssl.mingw-win32.tar.gz
  url: https://pypi.python.org/packages/source/D/Dtls/Dtls-1.0.1.sdist_with_openssl.mingw-win32.tar.gz
  md5: 7f6f04d8330f93e38d8f26acb9ee3b07
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dtls = dtls:main
    #
    # Would create an entry point called dtls that calls dtls.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - dtls
    - dtls.demux
    - dtls.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rbit/pydtls
  license: LICENSE
  summary: 'Python Datagram Transport Layer Security'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
