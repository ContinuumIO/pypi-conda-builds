package:
  name: stetl
  version: "1.0.7"

source:
  fn: Stetl-1.0.7.tar.gz
  url: https://pypi.python.org/packages/source/S/Stetl/Stetl-1.0.7.tar.gz
  md5: 0b7b4ea29e0d0ec8b9cc16d662e8d2cf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - stetl = stetl:main
    #
    # Would create an entry point called stetl that calls stetl.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - psycopg2
    - lxml
    - gdal

  run:
    - python
    - psycopg2
    - lxml
    - gdal

test:
  # Python imports
  imports:
    - stetl
    - stetl.filters
    - stetl.inputs
    - stetl.outputs

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/justb4/stetl
  license: GNU General Public License v3 (GPLv3)
  summary: 'Stetl provides transformation for spatial data'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
