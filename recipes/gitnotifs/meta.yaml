package:
  name: gitnotifs
  version: "0.2.2"

source:
  fn: gitnotifs-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/g/gitnotifs/gitnotifs-0.2.2.tar.gz
  md5: 7a973a555814818bf6f917a8a422cb3a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gitnotifs = gitnotifs:main
    #
    # Would create an entry point called gitnotifs that calls gitnotifs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - configparser
    - xmpppy
    - gitpython
    - irc
    - webhelpers
    - jinja2

  run:
    - python
    - configparser
    - xmpppy
    - gitpython
    - irc
    - webhelpers
    - jinja2

test:
  # Python imports
  imports:
    - gitnotifs

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://renemilk.github.com/gitnotifs
  license: gitnotus
  summary: 'notification scripts/module for git repositories'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
