package:
  name: agilegeo
  version: "0.1.6"

source:
  fn: agilegeo-0.1.6.tar.gz
  url: https://pypi.python.org/packages/source/a/agilegeo/agilegeo-0.1.6.tar.gz
  md5: d303a83f924dc0ae14818872914b2fd5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - agilegeo = agilegeo:main
    #
    # Would create an entry point called agilegeo that calls agilegeo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - agilegeo
    - agilegeo.attribute
    - agilegeo.avo
    - agilegeo.util
    - agilegeo.wavelet

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/agilegeo/
  license: LICENSE.txt
  summary: 'Useful geophysics functions'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
