package:
  name: motdify
  version: "0.1.1"

source:
  fn: motdify-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/m/motdify/motdify-0.1.1.tar.gz
  md5: b0a3e1635bf5e082e1a0e56f676a2b8d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - motdify = motdify:main
    #
    # Would create an entry point called motdify that calls motdify.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - psutil

  run:
    - python
    - psutil

test:
  # Python imports
  imports:
    - motdenv
    - motdify

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/LkSky/motdify
  license: MIT
  summary: 'Create your motd-script with template variables'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
