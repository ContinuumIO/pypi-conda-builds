package:
  name: django-popularity-mixin
  version: "0.1.5"

source:
  fn: django-popularity-mixin-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-popularity-mixin/django-popularity-mixin-0.1.5.tar.gz
  md5: d7ca9ad7864331455571369f0de17cce
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-popularity-mixin = django-popularity-mixin:main
    #
    # Would create an entry point called django-popularity-mixin that calls django-popularity-mixin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-celery >=3.0,<3.1
    - django-cacheback >=0.7
    - django-classy-tags >=0.5
    - django-hitcount-headless >=0.2

  run:
    - python
    - django-celery >=3.0,<3.1
    - django-cacheback >=0.7
    - django-classy-tags >=0.5
    - django-hitcount-headless >=0.2

test:
  # Python imports
  imports:
    - popularity

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Put short description here...'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
