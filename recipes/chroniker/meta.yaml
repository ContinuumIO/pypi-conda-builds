package:
  name: chroniker
  version: "0.0.0"

source:
  fn: chroniker-0.0.0.tar.gz
  url: https://pypi.python.org/packages/source/c/chroniker/chroniker-0.0.0.tar.gz
  md5: b00359d3519b30527109e08da819ba66
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - chroniker = chroniker:main
    #
    # Would create an entry point called chroniker that calls chroniker.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - chroniker

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/wnyc/to_sentry
  license: BSD
  summary: 'Profiling and timing tools for Python.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
