package:
  name: django-ccnews
  version: "1.8.0"

source:
  fn: django-ccnews-1.8.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-ccnews/django-ccnews-1.8.0.tar.gz
  md5: 6afd591d921ef1ec67247c19d97ed889
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-ccnews = django-ccnews:main
    #
    # Would create an entry point called django-ccnews that calls django-ccnews.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django-ccthumbs
    - django-writingfield
    - django-ccfiletypes
    - markdown2

  run:
    - python
    - django-ccthumbs
    - django-writingfield
    - django-ccfiletypes
    - markdown2

test:
  # Python imports
  imports:
    - ccnews
    - ccnews.templatetags
    - ccnews.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/designcc/django-ccnews
  license: BSD 3 Clause
  summary: 'A lightweight news application for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
