package:
  name: django-pure-pagination
  version: "0.2.1"

source:
  fn: django-pure-pagination-0.2.1.zip
  url: https://pypi.python.org/packages/source/d/django-pure-pagination/django-pure-pagination-0.2.1.zip
  md5: 143613cf47d58e8d344d638e0efeaffc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-pure-pagination = django-pure-pagination:main
    #
    # Would create an entry point called django-pure-pagination that calls django-pure-pagination.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - pure_pagination

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jamespacileo/django-pure-pagination/
  license: BSD
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
