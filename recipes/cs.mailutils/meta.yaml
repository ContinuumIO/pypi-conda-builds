package:
  name: cs.mailutils
  version: "20150116.2"

source:
  fn: cs.mailutils-20150116.2.tar.gz
  url: https://pypi.python.org/packages/source/c/cs.mailutils/cs.mailutils-20150116.2.tar.gz
  md5: 0fc759201e37dde72c0d9af574ad1997
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cs.mailutils = cs.mailutils:main
    #
    # Would create an entry point called cs.mailutils that calls cs.mailutils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/cameron_simpson/css/commits/all
  license: GNU General Public License v3 (GPLv3)
  summary: 'functions and classes to work with email'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
