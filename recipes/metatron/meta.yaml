package:
  name: metatron
  version: "0.2.1-alpha"

source:
  fn: metatron-0.2.1-alpha.tar.gz
  url: https://pypi.python.org/packages/source/m/metatron/metatron-0.2.1-alpha.tar.gz
  md5: c918e81d9fe624c6674552699f39edcc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - metatron = metatron:main
    #
    # Would create an entry point called metatron that calls metatron.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - hiredis ==0.1.4
    - redis ==2.10.3
    - shortid ==0.1.0

  run:
    - python
    - hiredis ==0.1.4
    - redis ==2.10.3
    - shortid ==0.1.0

test:
  # Python imports
  imports:
    - metatron
    - metatron.helpers
    - metatron.lib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/metatron-io/metatron-io-core
  license: UNKNOWN
  summary: 'Distributed processing engine'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
