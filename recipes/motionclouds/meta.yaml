package:
  name: motionclouds
  version: "0.1.1"

source:
  fn: MotionClouds-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/M/MotionClouds/MotionClouds-0.1.1.tar.gz
  md5: bb9ac7b8d8171b17545271212faa9c0b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - motionclouds = motionclouds:main
    #
    # Would create an entry point called motionclouds that calls motionclouds.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - MotionClouds
    - motionclouds

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/NeuralEnsemble/MotionClouds
  license: GNU General Public License (GPL)
  summary: 'Model-based stimulus synthesis of natural-like random textures for the study of motion perception.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
