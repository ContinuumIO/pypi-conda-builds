package:
  name: django-vcs
  version: "0.1"

source:
  fn: django-vcs-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-vcs/django-vcs-0.1.tar.gz
  md5: dcfa1968f1501b42e296af4597cc20a0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-vcs = django-vcs:main
    #
    # Would create an entry point called django-vcs that calls django-vcs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyvcs >=0.1

  run:
    - python
    - pyvcs >=0.1

test:
  # Python imports
  imports:
    - django_vcs
    - django_vcs.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/alex/django-vcs/
  license: BSD License
  summary: 'A pluggable django application to allow browsing of code repositories.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
