package:
  name: django-treemenus
  version: "0.9.2"

source:
  fn: django-treemenus-0.9.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-treemenus/django-treemenus-0.9.2.tar.gz
  md5: 5efc5dd242533aa0b4a9b92a9e9f24a3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-treemenus = django-treemenus:main
    #
    # Would create an entry point called django-treemenus that calls django-treemenus.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.0

  run:
    - python
    - django >=1.0

test:
  # Python imports
  imports:
    - treemenus
    - treemenus.templatetags
    - treemenus.tests
    - treemenus.tests.fake_menu_extension

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/jphalip/django-treemenus/
  license: BSD License
  summary: 'Tree-structured menuing application for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
