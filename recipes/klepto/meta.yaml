package:
  name: klepto
  version: "0.1.1"

source:
  fn: klepto-0.1.1.tgz
  url: https://pypi.python.org/packages/source/k/klepto/klepto-0.1.1.tgz
  md5: 3212e34bc64351bf49abfe43d8422c2b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - klepto = klepto:main
    #
    # Would create an entry point called klepto that calls klepto.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - dill >=0.2.1
    - pox >=0.2

  run:
    - python
    - dill >=0.2.1
    - pox >=0.2

test:
  # Python imports
  imports:
    - klepto

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.cacr.caltech.edu/~mmckerns
  license: BSD
  summary: 'persistent caching to memory, disk, or database'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
