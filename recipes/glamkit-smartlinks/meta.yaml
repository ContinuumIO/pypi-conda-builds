package:
  name: glamkit-smartlinks
  version: "0.6.2"

source:
  fn: glamkit-smartlinks-0.6.2.tar.gz
  url: https://pypi.python.org/packages/source/g/glamkit-smartlinks/glamkit-smartlinks-0.6.2.tar.gz
  md5: 142279ea7e4f3a4caad810e558f9db37
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - glamkit-smartlinks = glamkit-smartlinks:main
    #
    # Would create an entry point called glamkit-smartlinks that calls glamkit-smartlinks.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - smartlinks
    - smartlinks.templatetags
    - smartlinks.tests
    - smartlinks.tests.testapp

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://glamkit.org/
  license: BSD License
  summary: 'Conditional wiki-style links to Django models.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
