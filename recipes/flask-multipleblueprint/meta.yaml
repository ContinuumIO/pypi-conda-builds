package:
  name: flask-multipleblueprint
  version: "0.1"

source:
  fn: Flask-MultipleBlueprint-0.1.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-MultipleBlueprint/Flask-MultipleBlueprint-0.1.tar.gz
  md5: 14a98a7789ed2cb36e488aaf80fdaa50
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-multipleblueprint = flask-multipleblueprint:main
    #
    # Would create an entry point called flask-multipleblueprint that calls flask-multipleblueprint.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.8

  run:
    - python
    - flask >=0.8

test:
  # Python imports
  imports:
    - flask_multipleblueprint

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT License
  summary: 'Decorate function using multiple blueprints at once.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
