package:
  name: collective.multilogo
  version: "0.2.1"

source:
  fn: collective.multilogo-0.2.1.zip
  url: https://pypi.python.org/packages/source/c/collective.multilogo/collective.multilogo-0.2.1.zip
  md5: 8b067265ac359ae0808148fe42004755
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.multilogo = collective.multilogo:main
    #
    # Would create an entry point called collective.multilogo that calls collective.multilogo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.multilogo
    - collective.multilogo.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/collective.multilogo
  license: GPL
  summary: 'A Plone extension package providing wiewlet with user defined multiple portal logos.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
