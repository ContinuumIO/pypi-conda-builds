package:
  name: import_file
  version: "1.11"

source:
  fn: import_file-1.11.zip
  url: https://pypi.python.org/packages/source/i/import_file/import_file-1.11.zip
  md5: 3660e420fe77a6b603ca69e91e1885a2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - import_file = import_file:main
    #
    # Would create an entry point called import_file that calls import_file.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - import_file

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Public Domain
  summary: 'Import py files using their relative or absolute path.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
