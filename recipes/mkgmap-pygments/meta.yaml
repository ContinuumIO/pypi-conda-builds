package:
  name: mkgmap-pygments
  version: "1.5"

source:
  fn: mkgmap-pygments-1.5.tar.gz
  url: https://pypi.python.org/packages/source/m/mkgmap-pygments/mkgmap-pygments-1.5.tar.gz
  md5: 536d5fcde46802241321aa999fa80bad
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mkgmap-pygments = mkgmap-pygments:main
    #
    # Would create an entry point called mkgmap-pygments that calls mkgmap-pygments.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pygments

  run:
    - python
    - setuptools
    - pygments

test:
  # Python imports
  imports:
    - mkgmap

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/sratcliffe/mkgmap-pygments
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'A pygments lexer and style for mkgmap style files'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
