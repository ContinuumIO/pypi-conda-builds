package:
  name: django-detention
  version: "0.6.0"

source:
  fn: django-detention-0.6.0.zip
  url: https://pypi.python.org/packages/source/d/django-detention/django-detention-0.6.0.zip
  md5: a92082c636fdade312101554e3422f9c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-detention = django-detention:main
    #
    # Would create an entry point called django-detention that calls django-detention.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - detention

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: GNU General Public License (GPL)
  summary: 'Application to manage client-banning'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
