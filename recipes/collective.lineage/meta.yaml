package:
  name: collective.lineage
  version: "2.0"

source:
  fn: collective.lineage-2.0.zip
  url: https://pypi.python.org/packages/source/c/collective.lineage/collective.lineage-2.0.zip
  md5: ca6caea8f502605512d9b9e756f1a861
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.lineage = collective.lineage:main
    #
    # Would create an entry point called collective.lineage that calls collective.lineage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone
    - plone.app.imaging

  run:
    - python
    - setuptools
    - setuptools
    - plone
    - plone.app.imaging

test:
  # Python imports
  imports:
    - collective
    - collective.lineage
    - collective.lineage.Extensions
    - collective.lineage.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/collective-lineage
  license: GNU General Public License (GPL)
  summary: 'The microsite creation product for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
