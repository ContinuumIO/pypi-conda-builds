package:
  name: malparser
  version: "0.2.2"

source:
  fn: malparser-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/m/malparser/malparser-0.2.2.tar.gz
  md5: 6f05ee1208e4bfcea4ebf5e63b9edb82
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - malparser = malparser:main
    #
    # Would create an entry point called malparser that calls malparser.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - lxml

  run:
    - python
    - lxml

test:
  # Python imports
  imports:
    - malparser
    - malparser.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/JohnDoee/
  license: MIT License
  summary: 'Python package to access the MyAnimeList Anime'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
