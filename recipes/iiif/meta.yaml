package:
  name: iiif
  version: "0.5.1"

source:
  fn: iiif-0.5.1.tar.gz
  url: https://pypi.python.org/packages/source/i/iiif/iiif-0.5.1.tar.gz
  md5: d83c72e332bd930c771ec10d3d01b887
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - iiif = iiif:main
    #
    # Would create an entry point called iiif that calls iiif.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pillow
    - python-magic

  run:
    - python
    - pillow
    - python-magic

test:
  # Python imports
  imports:
    - iiif

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/zimeon/iiif
  license: GNU General Public License v3 (GPLv3)
  summary: 'IIIF Image API library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
