package:
  name: collective.facets
  version: "1.2"

source:
  fn: collective.facets-1.2.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.facets/collective.facets-1.2.tar.gz
  md5: 4666a7da7a6f6892943669159d12540f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.facets = collective.facets:main
    #
    # Would create an entry point called collective.facets that calls collective.facets.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.cmfplone
    - archetypes.schemaextender
    - plone.app.registry

  run:
    - python
    - setuptools
    - setuptools
    - products.cmfplone
    - archetypes.schemaextender
    - plone.app.registry

test:
  # Python imports
  imports:
    - collective
    - collective.facets
    - collective.facets.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.facets
  license: GPL
  summary: "Let's users add new facets to all content"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
