package:
  name: apyrtium
  version: "0.1"

source:
  fn: apyrtium-0.1.tar.gz
  url: https://pypi.python.org/packages/source/a/apyrtium/apyrtium-0.1.tar.gz
  md5: 5867edad9797e2305af7f51c03004999
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - apyrtium = apyrtium:main
    #
    # Would create an entry point called apyrtium that calls apyrtium.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - apyrtium

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/j2labs/apytrium
  license: UNKNOWN
  summary: 'Python Client Library for the Apertium Machine Translation API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
