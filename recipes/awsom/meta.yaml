package:
  name: awsom
  version: "0.0.3"

source:
  fn: awsom-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/a/awsom/awsom-0.0.3.tar.gz
  md5: ab4f81f9eba25405cad4c7c4d59f9b7e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - awsom = awsom:main
    #
    # Would create an entry point called awsom that calls awsom.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - boto >=2.7.0
    - pyyaml ==3.10

  run:
    - python
    - boto >=2.7.0
    - pyyaml ==3.10

test:
  # Python imports
  imports:
    - awsom
    - awsom.services

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/tuxpiper/awsom
  license: MIT License
  summary: 'Browse your AWS resources using a hierarchical object model'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
