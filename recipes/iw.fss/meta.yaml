package:
  name: iw.fss
  version: "2.8.0rc5"

source:
  fn: iw.fss-2.8.0rc5.tar.gz
  url: https://pypi.python.org/packages/source/i/iw.fss/iw.fss-2.8.0rc5.tar.gz
  md5: e0e3afc59d784100b2d981ecb31a06a5
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - iw.fss = iw.fss:main
    #
    # Would create an entry point called iw.fss that calls iw.fss.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.monkeypatcher

  run:
    - python
    - setuptools
    - setuptools
    - collective.monkeypatcher

test:
  # Python imports
  imports:
    - iw
    - iw.fss
    - iw.fss.Extensions
    - iw.fss.browser
    - iw.fss.configuration
    - iw.fss.examples
    - iw.fss.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/iw.fss/
  license: GPL
  summary: 'Archetypes storage for storing fields raw values on the file system.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
