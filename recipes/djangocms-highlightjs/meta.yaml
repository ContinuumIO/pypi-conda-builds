package:
  name: djangocms-highlightjs
  version: "0.4.0"

source:
  fn: djangocms-highlightjs-0.4.0.tar.gz
  url: https://pypi.python.org/packages/source/d/djangocms-highlightjs/djangocms-highlightjs-0.4.0.tar.gz
  md5: 77259f7c4a5712cd8ea7a8c06460f380
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangocms-highlightjs = djangocms-highlightjs:main
    #
    # Would create an entry point called djangocms-highlightjs that calls djangocms-highlightjs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django <1.7
    - django-sekizai
    - django-cms >=3.0

  run:
    - python
    - django <1.7
    - django-sekizai
    - django-cms >=3.0

test:
  # Python imports
  imports:
    - djangocms_highlightjs

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/nephila/djangocms-highlightjs
  license: BSD License
  summary: 'highlight.js plugin for django CMS 3.0'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
