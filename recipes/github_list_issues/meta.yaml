package:
  name: github_list_issues
  version: "0.1.0"

source:
  fn: github_list_issues-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/g/github_list_issues/github_list_issues-0.1.0.tar.gz
  md5: 4959289a42f82e3af41a8a98ccc544fb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - github_list_issues = github_list_issues:main
    #
    # Would create an entry point called github_list_issues that calls github_list_issues.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - urllib2_prior_auth

  run:
    - python
    - urllib2_prior_auth

test:
  # Python imports
  imports:
    - github_list_issues

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mcepl/github_list_issues
  license: MIT License
  summary: 'List all issues assigned to the user on GitHub'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
