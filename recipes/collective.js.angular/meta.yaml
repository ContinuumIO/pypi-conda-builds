package:
  name: collective.js.angular
  version: "1.3.15.0"

source:
  fn: collective.js.angular-1.3.15.0.zip
  url: https://pypi.python.org/packages/source/c/collective.js.angular/collective.js.angular-1.3.15.0.zip
  md5: ff564ce1b3fa5c1e7be34fa61437b01c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.js.angular = collective.js.angular:main
    #
    # Would create an entry point called collective.js.angular that calls collective.js.angular.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone

  run:
    - python
    - setuptools
    - setuptools
    - plone

test:
  # Python imports
  imports:
    - collective
    - collective.js
    - collective.js.angular

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://pypi.python.org/pypi/collective.js.angular
  license: GNU General Public License (GPL)
  summary: 'AngularJS JavaScript Library Packaged for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
