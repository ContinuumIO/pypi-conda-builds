package:
  name: django-bmemcached
  version: "0.2.2"

source:
  fn: django-bmemcached-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-bmemcached/django-bmemcached-0.2.2.tar.gz
  md5: 39307b5893f3699d71c1fb9449675bc0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-bmemcached = django-bmemcached:main
    #
    # Would create an entry point called django-bmemcached that calls django-bmemcached.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python-binary-memcached

  run:
    - python
    - python-binary-memcached

test:
  # Python imports
  imports:
    - django_bmemcached

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jaysonsantos/django-bmemcached
  license: MIT License
  summary: 'A Django cache backend to use bmemcached module which supports memcached binary protocol with authentication.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
