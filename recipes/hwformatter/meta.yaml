package:
  name: hwformatter
  version: "0.4"

source:
  fn: HWFormatter-0.4.tar.gz
  url: https://pypi.python.org/packages/source/H/HWFormatter/HWFormatter-0.4.tar.gz
  md5: 43841b63aab30006607a31a92b9cbe51
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hwformatter = hwformatter:main
    #
    # Would create an entry point called hwformatter that calls hwformatter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pypdf

  run:
    - python
    - pypdf

test:
  # Python imports
  imports:
    - HWFormatter
    - hwformatter

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/dgrtwo/HW-Formatter
  license: UNKNOWN
  summary: 'Format submitted homework assignments from Blackboard into PDFs'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
