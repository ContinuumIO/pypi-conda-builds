package:
  name: flake8-copyright
  version: "0.1"

source:
  fn: flake8-copyright-0.1.tar.gz
  url: https://pypi.python.org/packages/source/f/flake8-copyright/flake8-copyright-0.1.tar.gz
  md5: 8fab17d27f0b417c44ea0bebf579d71b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flake8-copyright = flake8-copyright:main
    #
    # Would create an entry point called flake8-copyright that calls flake8-copyright.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.savoirfairelinux.com
  license: UNKNOWN
  summary: u'Adds copyright checks to flake8'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
