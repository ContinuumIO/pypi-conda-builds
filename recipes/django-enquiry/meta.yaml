package:
  name: django-enquiry
  version: "0.7.2"

source:
  fn: django-enquiry-0.7.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-enquiry/django-enquiry-0.7.2.tar.gz
  md5: cf1b7e7788d9c7a064b3184c8b10dd4b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-enquiry = django-enquiry:main
    #
    # Would create an entry point called django-enquiry that calls django-enquiry.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - enquiry
    - enquiry.migrations
    - enquiry.templatetags
    - enquiry.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bitmazk/django-enquiry
  license: The MIT License
  summary: 'A simple multilingual poll app.\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
