package:
  name: jinja-layout
  version: "0.1"

source:
  fn: jinja-layout-0.1.tar.gz
  url: https://pypi.python.org/packages/source/j/jinja-layout/jinja-layout-0.1.tar.gz
  md5: 00d9e1befe2dc04ffc0fb4b2e33da6b3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jinja-layout = jinja-layout:main
    #
    # Would create an entry point called jinja-layout that calls jinja-layout.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - jinja2

  run:
    - python
    - jinja2

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/frascoweb/jinja-layout
  license: MIT
  summary: 'Extended layout system for Jinja2'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
