package:
  name: djangocms-table
  version: "1.2"

source:
  fn: djangocms-table-1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/djangocms-table/djangocms-table-1.2.tar.gz
  md5: 657a2f0d717694291e1e5be5c4c6b241
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangocms-table = djangocms-table:main
    #
    # Would create an entry point called djangocms-table that calls djangocms-table.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - djangocms_table
    - djangocms_table.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/divio/djangocms-table
  license: BSD License
  summary: 'Table Plugin for django CMS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
