package:
  name: gaebuild
  version: "0.2.5"

source:
  fn: gaebuild-0.2.5.tar.gz
  url: https://pypi.python.org/packages/source/g/gaebuild/gaebuild-0.2.5.tar.gz
  md5: 72955bbaa4f60569ea800fc9a1ba5503
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gaebuild = gaebuild:main
    #
    # Would create an entry point called gaebuild that calls gaebuild.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zc.buildout
    - zc.recipe.egg

  run:
    - python
    - setuptools
    - zc.buildout
    - zc.recipe.egg

test:
  # Python imports
  imports:
    - gaebuild

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/carlitux/gaebuild
  license: BSD License
  summary: 'Buildout recipe for Google app engine'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
