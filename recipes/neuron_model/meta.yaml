package:
  name: neuron_model
  version: "1.0.0"

source:
  fn: neuron_model-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/n/neuron_model/neuron_model-1.0.0.tar.gz
  md5: 38bcf0fdf70dfbb82af90778d261467b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - neuron_model = neuron_model:main
    #
    # Would create an entry point called neuron_model that calls neuron_model.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - neuron_model

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://kari4u.dothome.co.kr
  license: UNKNOWN
  summary: 'A simple binary threshold artificial neuron model'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
