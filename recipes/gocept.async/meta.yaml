package:
  name: gocept.async
  version: "0.3.3"

source:
  fn: gocept.async-0.3.3.tar.gz
  url: https://pypi.python.org/packages/source/g/gocept.async/gocept.async-0.3.3.tar.gz
  md5: 34487b5186338c4e07625d1e69dc4798
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gocept.async = gocept.async:main
    #
    # Would create an entry point called gocept.async that calls gocept.async.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - zodb3
    - decorator
    - lovely.remotetask >=0.3
    - rwproperty
    - setuptools
    - zope.authentication
    - zope.app.testing
    - zope.component
    - zope.deferredimport
    - zope.dottedname
    - zope.security
    - zope.site
    - zope.testing

  run:
    - python
    - zodb3
    - decorator
    - lovely.remotetask >=0.3
    - rwproperty
    - setuptools
    - zope.authentication
    - zope.app.testing
    - zope.component
    - zope.deferredimport
    - zope.dottedname
    - zope.security
    - zope.site
    - zope.testing

test:
  # Python imports
  imports:
    - gocept
    - gocept.async

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/gocept.async
  license: Zope Public License
  summary: 'Asynchronous function calls using lovely.remotetask'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
