package:
  name: django-admin-sortable
  version: "1.8.3"

source:
  fn: django-admin-sortable-1.8.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-admin-sortable/django-admin-sortable-1.8.3.tar.gz
  md5: a204f64a152ba80f860f4f89f8f1c349
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-admin-sortable = django-admin-sortable:main
    #
    # Would create an entry point called django-admin-sortable that calls django-admin-sortable.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - adminsortable
    - adminsortable.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/iambrandontaylor/django-admin-sortable
  license: Apache Software License
  summary: 'Drag and drop sorting for models and inline models in Django admin.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
