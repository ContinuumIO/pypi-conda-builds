package:
  name: django-admin-extend
  version: "0.0.3"

source:
  fn: django-admin-extend-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-admin-extend/django-admin-extend-0.0.3.tar.gz
  md5: c70988f8f14fec3802acf2b609d25c67
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-admin-extend = django-admin-extend:main
    #
    # Would create an entry point called django-admin-extend that calls django-admin-extend.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3

  run:
    - python
    - django >=1.3

test:
  # Python imports
  imports:
    - admin_extend

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kux/django-admin-extend
  license: UNKNOWN
  summary: 'Provides functionality for extendingModelAdmin classes that have alreadybeen registered by other apps'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
