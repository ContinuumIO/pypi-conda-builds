package:
  name: tornadocnauth
  version: "1.3"

source:
  fn: tornadocnauth-1.3.tar.gz
  url: https://pypi.python.org/packages/source/t/tornadocnauth/tornadocnauth-1.3.tar.gz
  md5: 6653d6b5b05a313d2cd3fdab2a646a1f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tornadocnauth = tornadocnauth:main
    #
    # Would create an entry point called tornadocnauth that calls tornadocnauth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - tornadocnauth

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'providing similar interfaces as tornado.auth for popular websites OAuth in China'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
