package:
  name: djcommon
  version: "1.1.0"

source:
  fn: djcommon-1.1.0.zip
  url: https://pypi.python.org/packages/source/d/djcommon/djcommon-1.1.0.zip
  md5: fc3da21202544017bb5486fc363a407a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djcommon = djcommon:main
    #
    # Would create an entry point called djcommon that calls djcommon.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - djcommon
    - djcommon.apps
    - djcommon.apps.translations
    - djcommon.apps.translations.migrations
    - djcommon.conf
    - djcommon.templatetags
    - djcommon.tests
    - djcommon.widgets

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/baskoopmans/djcommon/
  license: BSD License
  summary: 'Django Common (djcommon) - A set of project independent reusable features while developing with the Django Framework - https://www.djangoproject.com/ '

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
