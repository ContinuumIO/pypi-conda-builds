package:
  name: asv_utils
  version: "dev-20121101-01"

source:
  fn: asv_utils-dev-20121101-01.tar.gz
  url: https://pypi.python.org/packages/source/a/asv_utils/asv_utils-dev-20121101-01.tar.gz
  md5: b1142f92079fdc3d1e32d4772e48fe14
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - asv_utils = asv_utils:main
    #
    # Would create an entry point called asv_utils that calls asv_utils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pytils >=0.2.3
    - pytz >=2012c

  run:
    - python
    - pytils >=0.2.3
    - pytz >=2012c

test:
  # Python imports
  imports:
    - asv_utils
    - asv_utils.dj

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/xenolog/asv_utils/wiki/Home
  license: GPL
  summary: '\nThis is a set of common functions for other asv.* libs.\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
