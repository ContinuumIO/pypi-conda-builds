package:
  name: p4a.calendar
  version: "2.1b1"

source:
  fn: p4a.calendar-2.1b1.tar.gz
  url: https://pypi.python.org/packages/source/p/p4a.calendar/p4a.calendar-2.1b1.tar.gz
  md5: a4f03852e253344589eecf4a02552686
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - p4a.calendar = p4a.calendar:main
    #
    # Would create an entry point called p4a.calendar that calls p4a.calendar.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - p4a.common >=1.0.1,<=1.0.9999
    - p4a.z2utils >=1.0,<=1.0.9999

  run:
    - python
    - setuptools
    - p4a.common >=1.0.1,<=1.0.9999
    - p4a.z2utils >=1.0,<=1.0.9999

test:
  # Python imports
  imports:
    - p4a
    - p4a.calendar

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://svn.plone.org/svn/collective/p4a/p4a.calendar/
  license: GNU General Public License (GPL)
  summary: 'Plone4Artists calendar abstraction library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
