package:
  name: python-pam
  version: "1.8.2"

source:
  fn: python-pam-1.8.2.tar.gz
  url: https://pypi.python.org/packages/source/p/python-pam/python-pam-1.8.2.tar.gz
  md5: db71b6b999246fb05d78ecfbe166629d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-pam = python-pam:main
    #
    # Would create an entry point called python-pam that calls python-pam.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/FirefighterBlu3/python-pam
  license: MIT License
  summary: 'Python PAM module using ctypes, py3/py2'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
