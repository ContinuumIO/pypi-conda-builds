package:
  name: cmsplugin-googleform
  version: "0.1"

source:
  fn: cmsplugin-googleform-0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/cmsplugin-googleform/cmsplugin-googleform-0.1.tar.gz
  md5: 5e2e60d66cc41a1d10dca102a7a809c4
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cmsplugin-googleform = cmsplugin-googleform:main
    #
    # Would create an entry point called cmsplugin-googleform that calls cmsplugin-googleform.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-cms >=2.1.0

  run:
    - python
    - setuptools
    - django-cms >=2.1.0

test:
  # Python imports
  imports:
    - cmsplugin_googleform
    - cmsplugin_googleform.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/benliles/cmsplugin-googleform
  license: Apache Software License
  summary: 'Django CMS Plugin for Google Forms'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
