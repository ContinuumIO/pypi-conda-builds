package:
  name: bottle-tornado-websocket
  version: "0.13"

source:
  fn: bottle-tornado-websocket-0.13.zip
  url: https://pypi.python.org/packages/source/b/bottle-tornado-websocket/bottle-tornado-websocket-0.13.zip
  md5: ca8ce9b284675df305d09c46b7f63b0d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bottle-tornado-websocket = bottle-tornado-websocket:main
    #
    # Would create an entry point called bottle-tornado-websocket that calls bottle-tornado-websocket.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - bottle
    - tornado

  run:
    - python
    - bottle
    - tornado

test:
  # Python imports
  imports:
    - bottle_tornado_websocket

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/cime/bottle-tornado-websocket
  license: UNKNOWN
  summary: 'WebSockets for bottle'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
