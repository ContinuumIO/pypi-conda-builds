package:
  name: puppetdb_api
  version: "1.1"

source:
  fn: puppetdb_api-1.1.tar.gz
  url: https://pypi.python.org/packages/source/p/puppetdb_api/puppetdb_api-1.1.tar.gz
  md5: 0310e4b877d4578e4ad74ae694a8cf0c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - puppetdb_api = puppetdb_api:main
    #
    # Would create an entry point called puppetdb_api that calls puppetdb_api.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - puppetdb_api

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/i-maravic/puppetdb-api
  license: Apache Software License
  summary: 'Python wrapper for puppetDB API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
