package:
  name: django_annotation
  version: "0.2"

source:
  fn: django_annotation-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django_annotation/django_annotation-0.2.tar.gz
  md5: 122c9f4b72f5b25e48c867aa9d8c9f07
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_annotation = django_annotation:main
    #
    # Would create an entry point called django_annotation that calls django_annotation.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - markdown2
    - django >=1.5
    - choice_enum

  run:
    - python
    - markdown2
    - django >=1.5
    - choice_enum

test:
  # Python imports
  imports:
    - annotation
    - django_annotation

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dakrauth/django-annotation
  license: MIT License
  summary: 'Basic content management.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
