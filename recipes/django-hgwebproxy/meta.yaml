package:
  name: django-hgwebproxy
  version: "0.2.0"

source:
  fn: django-hgwebproxy-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-hgwebproxy/django-hgwebproxy-0.2.0.tar.gz
  md5: a363fea0b0d8abc3c16e2a5d38c8f29c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-hgwebproxy = django-hgwebproxy:main
    #
    # Would create an entry point called django-hgwebproxy that calls django-hgwebproxy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - hgwebproxy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/mariocesar/django-hgweproxy/
  license: GNU General Public License (GPL)
  summary: 'Django-hgwebproxy is an app to use and manage mercurial repositories within the django admin interface'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
