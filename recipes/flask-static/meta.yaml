package:
  name: flask-static
  version: "0.1"

source:
  fn: Flask-Static-0.1.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Static/Flask-Static-0.1.tar.gz
  md5: cf52c3d4f2595e3c230ed1b99b7028ba
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-static = flask-static:main
    #
    # Would create an entry point called flask-static that calls flask-static.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask

  run:
    - python
    - flask

test:
  # Python imports
  imports:
    - flaskext

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/SimonSapin/Flask-Static
  license: BSD License
  summary: 'Generates a static website from a Flask application'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
