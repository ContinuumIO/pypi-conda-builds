package:
  name: logplex
  version: "0.0.3"

source:
  fn: logplex-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/l/logplex/logplex-0.0.3.tar.gz
  md5: 5d85aece35340278f699e92b2694d12e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - logplex = logplex:main
    #
    # Would create an entry point called logplex that calls logplex.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - logplex
    - logplex.packages
    - logplex.packages.requests
    - logplex.packages.requests.packages
    - logplex.packages.requests.packages.charade
    - logplex.packages.requests.packages.urllib3
    - logplex.packages.requests.packages.urllib3.contrib
    - logplex.packages.requests.packages.urllib3.packages
    - logplex.packages.requests.packages.urllib3.packages.ssl_match_hostname

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kennethreitz/python-logplex
  license: BSD License
  summary: 'A Logplex client for Python.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
