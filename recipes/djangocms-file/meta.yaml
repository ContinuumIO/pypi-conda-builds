package:
  name: djangocms-file
  version: "0.1"

source:
  fn: djangocms-file-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/djangocms-file/djangocms-file-0.1.tar.gz
  md5: 4070b876f3708caf7d4621dbf8205775
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangocms-file = djangocms-file:main
    #
    # Would create an entry point called djangocms-file that calls djangocms-file.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - djangocms_file
    - djangocms_file.migrations
    - djangocms_file.migrations_django

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/divio/djangocms-file
  license: BSD License
  summary: 'File plugin for django CMS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
