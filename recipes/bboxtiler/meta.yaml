package:
  name: bboxtiler
  version: "0.1.0"

source:
  fn: bboxtiler-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/b/bboxtiler/bboxtiler-0.1.0.tar.gz
  md5: 14a28df1944dd279d7f1abd9af21d44e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bboxtiler = bboxtiler:main
    #
    # Would create an entry point called bboxtiler that calls bboxtiler.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - bbox_tiler
    - bboxtiler

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/wri/bbox-tiler
  license: UNKNOWN
  summary: 'Tile a bounding box into format suitable for use with Google Earth Engine.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
