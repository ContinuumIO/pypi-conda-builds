package:
  name: aiocutter
  version: "0.0.4"

source:
  fn: aiocutter-0.0.4.zip
  url: https://pypi.python.org/packages/source/a/aiocutter/aiocutter-0.0.4.zip
  md5: 23ed5d8a02910b4cbf2da449cf7f27a9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - aiocutter = aiocutter:main
    #
    # Would create an entry point called aiocutter that calls aiocutter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - aiohttp
    - beautifulsoup4

  run:
    - python
    - aiohttp
    - beautifulsoup4

test:
  # Python imports
  imports:
    - aiocutter

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/icoxfog417/aiocutter
  license: UNKNOWN
  summary: 'scraping tool for asyncio'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
