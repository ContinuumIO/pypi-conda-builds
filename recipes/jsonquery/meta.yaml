package:
  name: jsonquery
  version: "1.0.1"

source:
  fn: jsonquery-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/j/jsonquery/jsonquery-1.0.1.tar.gz
  md5: 6cd17aea143e52040d0edb94e9e44b2a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jsonquery = jsonquery:main
    #
    # Would create an entry point called jsonquery that calls jsonquery.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sqlalchemy

  run:
    - python
    - sqlalchemy

test:
  # Python imports
  imports:
    - jsonquery

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://jsonquery.readthedocs.org/
  license: MIT License
  summary: 'Basic json -> sqlalchemy query builder'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
