package:
  name: blueprint_io
  version: "0.1.1"

source:
  fn: blueprint_io-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/b/blueprint_io/blueprint_io-0.1.1.tar.gz
  md5: 59cc4702a32afefd125bb4ced44f23af
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - blueprint_io = blueprint_io:main
    #
    # Would create an entry point called blueprint_io that calls blueprint_io.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - blueprint >=3.0.5

  run:
    - python
    - blueprint >=3.0.5

test:
  # Python imports
  imports:
    - blueprint_io

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://devstructure.com/
  license: BSD
  summary: 'centralized blueprint service client'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
