package:
  name: il2ds-log-parser
  version: "0.10.1"

source:
  fn: il2ds-log-parser-0.10.1.tar.gz
  url: https://pypi.python.org/packages/source/i/il2ds-log-parser/il2ds-log-parser-0.10.1.tar.gz
  md5: e731085df34f59a63ad8c5d946616364
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - il2ds-log-parser = il2ds-log-parser:main
    #
    # Would create an entry point called il2ds-log-parser that calls il2ds-log-parser.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - simplejson ==3.3.1
    - nose ==1.3.0

  run:
    - python
    - simplejson ==3.3.1
    - nose ==1.3.0

test:
  # Python imports
  imports:
    - il2ds_log_parser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/IL2HorusTeam/il2ds-log-parser
  license: BSD License
  summary: 'Parser of IL-2 FB Dedicated Server events log.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
