package:
  name: git-orm
  version: "0.4"

source:
  fn: git-orm-0.4.tar.gz
  url: https://pypi.python.org/packages/source/g/git-orm/git-orm-0.4.tar.gz
  md5: 6593264ad87cf46e73f52cad785bec2c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - git-orm = git-orm:main
    #
    # Would create an entry point called git-orm that calls git-orm.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pygit2 ==0.20.0

  run:
    - python
    - pygit2 ==0.20.0

test:
  # Python imports
  imports:
    - git_orm
    - git_orm.models

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/natano/git-orm/
  license: ISC License (ISCL)
  summary: 'object-relational mapper for the git object store'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
