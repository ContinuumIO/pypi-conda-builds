package:
  name: inspire
  version: "1.0.9"

source:
  fn: inspire-1.0.9.tar.gz
  url: https://pypi.python.org/packages/source/i/inspire/inspire-1.0.9.tar.gz
  md5: 16f12e20839a7b41fc0e5435410dc832
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - inspire = inspire:main
    #
    # Would create an entry point called inspire that calls inspire.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy
    - docopt
    - grako
    - progressbar_ipython
    - requests

  run:
    - python
    - numpy
    - docopt
    - grako
    - progressbar_ipython
    - requests

test:
  # Python imports
  imports:
    - inspire

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rikrd/inspire
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Helper library to participate in the INSPIRE challenge'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
