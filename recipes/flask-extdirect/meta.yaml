package:
  name: flask-extdirect
  version: "0.2"

source:
  fn: Flask-ExtDirect-0.2.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-ExtDirect/Flask-ExtDirect-0.2.tar.gz
  md5: 08f875b1ddb72e3e48adb3030038c559
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-extdirect = flask-extdirect:main
    #
    # Would create an entry point called flask-extdirect that calls flask-extdirect.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask

  run:
    - python
    - flask

test:
  # Python imports
  imports:
    - flaskext

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/paparent/Flask-ExtDirect/
  license: MIT License
  summary: 'Adds Ext.Direct support to Flask.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
