package:
  name: anybox.recipe.sysdeps
  version: "0.5"

source:
  fn: anybox.recipe.sysdeps-0.5.tar.gz
  url: https://pypi.python.org/packages/source/a/anybox.recipe.sysdeps/anybox.recipe.sysdeps-0.5.tar.gz
  md5: 51171f003f9125b7c5d21a33a1e8a812
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - anybox.recipe.sysdeps = anybox.recipe.sysdeps:main
    #
    # Would create an entry point called anybox.recipe.sysdeps that calls anybox.recipe.sysdeps.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout

test:
  # Python imports
  imports:
    - anybox
    - anybox.recipe
    - anybox.recipe.sysdeps

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/anybox/anybox.recipe.sysdeps/overview
  license: ZPL
  summary: 'A buildout recipe to check system dependencies'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
