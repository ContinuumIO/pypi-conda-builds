package:
  name: circleclient
  version: "0.1.6"

source:
  fn: circleclient-0.1.6.tar.gz
  url: https://pypi.python.org/packages/source/c/circleclient/circleclient-0.1.6.tar.gz
  md5: 2ecca448fd0a9860fa2d4b62c03bf230
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - circleclient = circleclient:main
    #
    # Would create an entry point called circleclient that calls circleclient.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - circleclient

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/qba73/circleclient/
  license: MIT License
  summary: 'Python client for CircleCI API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
