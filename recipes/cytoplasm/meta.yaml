package:
  name: cytoplasm
  version: "0.06.3"

source:
  fn: Cytoplasm-0.06.3.tar.gz
  url: https://pypi.python.org/packages/source/C/Cytoplasm/Cytoplasm-0.06.3.tar.gz
  md5: 8fc628475ed50af3be78777d47aee141
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cytoplasm = cytoplasm:main
    #
    # Would create an entry point called cytoplasm that calls cytoplasm.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - mako

  run:
    - python
    - mako

test:
  # Python imports
  imports:
    - cytoplasm

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://cytoplasm.somethingsido.com
  license: MIT License
  summary: 'A static, blog-aware website generator written in python.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
