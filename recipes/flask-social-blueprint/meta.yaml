package:
  name: flask-social-blueprint
  version: "0.7"

source:
  fn: flask-social-blueprint-0.7.zip
  url: https://pypi.python.org/packages/source/f/flask-social-blueprint/flask-social-blueprint-0.7.zip
  md5: ffb375c10252c0d2f73b941bda55a20d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-social-blueprint = flask-social-blueprint:main
    #
    # Would create an entry point called flask-social-blueprint that calls flask-social-blueprint.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask-babel >=0.9
    - flask-oauth >=0.12
    - facebook-sdk >=0.4
    - google-api-python-client >=1.2
    - httplib2 >=0.8
    - oauth2 >=1.5
    - requests >=2

  run:
    - python
    - flask-babel >=0.9
    - flask-oauth >=0.12
    - facebook-sdk >=0.4
    - google-api-python-client >=1.2
    - httplib2 >=0.8
    - oauth2 >=1.5
    - requests >=2

test:
  # Python imports
  imports:
    - flask_social_blueprint

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/wooyek/flask-social-blueprint
  license: MIT License
  summary: 'An OAuth based authentication blueprint for flask. Easy to extend and override'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
