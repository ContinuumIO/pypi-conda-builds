package:
  name: django-packageutils
  version: "0.1.0"

source:
  fn: django-packageutils-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-packageutils/django-packageutils-0.1.0.tar.gz
  md5: 65c4063bd7220612ae373456caaf6d09
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-packageutils = django-packageutils:main
    #
    # Would create an entry point called django-packageutils that calls django-packageutils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - distribute
    - setuptools-git

  run:
    - python
    - distribute
    - setuptools-git

test:
  # Python imports
  imports:
    - packageutils
    - packageutils.syncdb
    - packageutils.syncdb.autouser
    - packageutils.test
    - packageutils.testcase
    - tests
    - tests.blogs
    - tests.blogs.tests
    - tests.blogs.tests.testapp

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lambdalisue/django-packageutils
  license: MIT License
  summary: 'Utility collection of Django package'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
