package:
  name: astro-pi
  version: "1.0.2"

source:
  fn: astro-pi-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/a/astro-pi/astro-pi-1.0.2.tar.gz
  md5: 68e94bdcf54257882a3e6fc5a7f5d498
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - astro-pi = astro-pi:main
    #
    # Would create an entry point called astro-pi that calls astro-pi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pillow
    - numpy

  run:
    - python
    - pillow
    - numpy

test:
  # Python imports
  imports:
    - astro_pi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/astro-pi/astro-pi-hat
  license: BSD License
  summary: 'Python API for Astro Pi (Sense HAT) for the Raspberry Pi'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
