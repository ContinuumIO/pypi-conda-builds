package:
  name: collective.recipe.jenkinsjob
  version: "1.0-alpha1"

source:
  fn: collective.recipe.jenkinsjob-1.0-alpha1.zip
  url: https://pypi.python.org/packages/source/c/collective.recipe.jenkinsjob/collective.recipe.jenkinsjob-1.0-alpha1.zip
  md5: 3eb695d8af855fa6704abb2463ada3ed
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.recipe.jenkinsjob = collective.recipe.jenkinsjob:main
    #
    # Would create an entry point called collective.recipe.jenkinsjob that calls collective.recipe.jenkinsjob.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - python-jenkins
    - zc.recipe.egg
    - collective.recipe.template

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - python-jenkins
    - zc.recipe.egg
    - collective.recipe.template

test:
  # Python imports
  imports:
    - collective
    - collective.recipe
    - collective.recipe.jenkinsjob
    - collective.recipe.jenkinsjob.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tisto/collective.recipe.jenkinsjob
  license: gpl
  summary: 'Buildout recipe to manage jobs on a remote Jenkins CI server.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
