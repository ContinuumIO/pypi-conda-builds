package:
  name: django-superuser
  version: "0.1.0"

source:
  fn: django-superuser-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-superuser/django-superuser-0.1.0.tar.gz
  md5: 081a901ce7903fefce8dd731f55bd6ae
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-superuser = django-superuser:main
    #
    # Would create an entry point called django-superuser that calls django-superuser.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - superuser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/aino/django-superuser
  license: ISC License (ISCL)
  summary: 'Middleware that gives you super powers.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
