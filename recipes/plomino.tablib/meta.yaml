package:
  name: plomino.tablib
  version: "1.1"

source:
  fn: plomino.tablib-1.1.zip
  url: https://pypi.python.org/packages/source/p/plomino.tablib/plomino.tablib-1.1.zip
  md5: 712ace4f37664350ea567cbddeb58c1f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plomino.tablib = plomino.tablib:main
    #
    # Would create an entry point called plomino.tablib that calls plomino.tablib.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.cmfplomino
    - tablib

  run:
    - python
    - setuptools
    - setuptools
    - products.cmfplomino
    - tablib

test:
  # Python imports
  imports:
    - plomino
    - plomino.tablib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/jean/plomino.tablib
  license: gpl
  summary: 'Make Tablib by Kenneth Reitz available to PlominoUtils.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
