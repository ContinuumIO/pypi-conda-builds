package:
  name: django-resized
  version: "0.3.5"

source:
  fn: django-resized-0.3.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-resized/django-resized-0.3.5.tar.gz
  md5: 741bc8998ce665675c3bed4ebde58e7c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-resized = django-resized:main
    #
    # Would create an entry point called django-resized that calls django-resized.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_resized

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/un1t/django-resized
  license: MIT License
  summary: 'Resizes image origin to specified size.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
