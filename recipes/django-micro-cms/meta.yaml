package:
  name: django-micro-cms
  version: "1.1.0"

source:
  fn: django-micro-cms-1.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-micro-cms/django-micro-cms-1.1.0.tar.gz
  md5: 5482da3bb44c3e0059ec43fc1d7574c2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-micro-cms = django-micro-cms:main
    #
    # Would create an entry point called django-micro-cms that calls django-micro-cms.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - microcms

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/shezi/django-micro-cms/
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'A micro-CMS for django. A bit like flatpages++.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
