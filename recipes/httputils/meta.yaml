package:
  name: httputils
  version: "0.1b"

source:
  fn: httputils-0.1b.tar.gz
  url: https://pypi.python.org/packages/source/h/httputils/httputils-0.1b.tar.gz
  md5: 9de21ffd51d1af2198b000e5d06162f0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - httputils = httputils:main
    #
    # Would create an entry point called httputils that calls httputils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - httputils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://packages.python.org/httputils
  license: GNU General Public License (GPL)
  summary: 'Simple python library used to wrap httplib function'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
