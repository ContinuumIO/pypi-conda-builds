package:
  name: passencode
  version: "2.0.0"

source:
  fn: passencode-2.0.0.tar.gz
  url: https://pypi.python.org/packages/source/p/passencode/passencode-2.0.0.tar.gz
  md5: c68825fe9fdefd716c0f01c001e46a3b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - passencode = passencode:main
    #
    # Would create an entry point called passencode that calls passencode.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - passlib
    - click >=3.3

  run:
    - python
    - passlib
    - click >=3.3

test:
  # Python imports
  imports:
    - passencode

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/nicolas-van/passencodeu
  license: MIT
  summary: 'passencode'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
