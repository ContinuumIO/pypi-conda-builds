package:
  name: ezupload
  version: "0.0.4"

source:
  fn: ezupload-0.0.4.zip
  url: https://pypi.python.org/packages/source/e/ezupload/ezupload-0.0.4.zip
  md5: c3267643b6966ab976063e123fc0d079
#  patches:
   # List any patch files here
   # - fix.patch

build:
  # preserve_egg_dir: True
  entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ezupload = ezupload:main
    #
    # Would create an entry point called ezupload that calls ezupload.main()

    - ezupload = ezpkg.ezupload:main

  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - ezpkg
    - ezupload

  commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.

    - ezupload --help

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dmhacker/ezupload
  license: GPL v2
  summary: 'Streamline upload process of PyPI projects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
