package:
  name: pynet
  version: "0.8.3"

source:
  fn: pynet-0.8.3.tar.gz
  url: https://pypi.python.org/packages/source/p/pynet/pynet-0.8.3.tar.gz
  md5: 5e2f808a3bc8ccd4371a3cc1fc0b2ee1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pynet = pynet:main
    #
    # Would create an entry point called pynet that calls pynet.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - pynet

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.pennington.net/py/pynet/
  license: GNU General Public License (GPL)
  summary: 'Efficiently manipulate IPv4 and IPv6 addresses'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
