package:
  name: django-term-field
  version: "0.2"

source:
  fn: django-term-field-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-term-field/django-term-field-0.2.tar.gz
  md5: fca3c4b3ee32e370f97391610ea1ef4d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-term-field = django-term-field:main
    #
    # Would create an entry point called django-term-field that calls django-term-field.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5

  run:
    - python
    - django >=1.5

test:
  # Python imports
  imports:
    - ex
    - ex.django

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/imagawa_yakata/django-term-field
  license: MIT License
  summary: 'Django term field(with from-to multi value).'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
