package:
  name: chain-python
  version: "0.0.3"

source:
  fn: chain-python-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/c/chain-python/chain-python-0.0.3.tar.gz
  md5: 6ce247d5e282932fa8268c00485100ec
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - chain-python = chain-python:main
    #
    # Would create an entry point called chain-python that calls chain-python.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/myusuf3/chain-python
  license: MIT License
  summary: "The Unofficial Python SDK for Chain's Bitcoin API"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
