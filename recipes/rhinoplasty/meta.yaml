package:
  name: rhinoplasty
  version: "0.6.1"

source:
  fn: rhinoplasty-0.6.1.tar.gz
  url: https://pypi.python.org/packages/source/r/rhinoplasty/rhinoplasty-0.6.1.tar.gz
  md5: efe9293772fdcb1473255d46eed44be5
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - rhinoplasty = rhinoplasty:main
    #
    # Would create an entry point called rhinoplasty that calls rhinoplasty.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - rhinoplasty
    - rhinoplasty.rich_errors

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: N/A
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Experimental extensions and helper functions for the Nose test runner.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
