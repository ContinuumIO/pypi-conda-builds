package:
  name: zope.generations
  version: "4.0.0a1"

source:
  fn: zope.generations-4.0.0a1.zip
  url: https://pypi.python.org/packages/source/z/zope.generations/zope.generations-4.0.0a1.zip
  md5: 85e3a8600d35aff7ec947b489e7d8df5
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zope.generations = zope.generations:main
    #
    # Would create an entry point called zope.generations that calls zope.generations.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - transaction
    - zope.component
    - zope.interface
    - zope.processlifetime

  run:
    - python
    - setuptools
    - transaction
    - zope.component
    - zope.interface
    - zope.processlifetime

test:
  # Python imports
  imports:
    - zope
    - zope.generations
    - zope.generations.demo
    - zope.generations.demo2
    - zope.generations.demo3
    - zope.generations.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/zope.generations
  license: Zope Public License
  summary: 'Zope application schema generations'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
