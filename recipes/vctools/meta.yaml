package:
  name: vctools
  version: "0.1.6.1"

source:
  fn: vctools-0.1.6.1.tar.gz
  url: https://pypi.python.org/packages/source/v/vctools/vctools-0.1.6.1.tar.gz
  md5: 74e4d18f2ce74206e149232719ab9fa2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - vctools = vctools:main
    #
    # Would create an entry point called vctools that calls vctools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - requests >=2.5.1
    - pytest >=2.6.1

  run:
    - python
    - requests >=2.5.1
    - pytest >=2.6.1

test:
  # Python imports
  imports:
    - vctools
    - vctools.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bmcollier/vctools/
  license: LICENSE.md
  summary: 'A set of tools for interacting with the VMware vCloud API, whether on vCloud Air or locally installed.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
