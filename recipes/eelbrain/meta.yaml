package:
  name: eelbrain
  version: "0.15.1"

source:
  fn: eelbrain-0.15.1.tar.gz
  url: https://pypi.python.org/packages/source/e/eelbrain/eelbrain-0.15.1.tar.gz
  md5: d0a93f7a3bad9801bc81d65d6e0d8a0d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - eelbrain = eelbrain:main
    #
    # Would create an entry point called eelbrain that calls eelbrain.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - keyring >=5
    - tex >=1.8
    - docutils >=0.11
    - mne >=0.8
    - nibabel >=2.0
    - pillow

  run:
    - python
    - keyring >=5
    - tex >=1.8
    - docutils >=0.11
    - mne >=0.8
    - nibabel >=2.0
    - pillow

test:
  # Python imports
  imports:
    - eelbrain
    - eelbrain._resources
    - eelbrain._stats
    - eelbrain._stats.tests
    - eelbrain._utils
    - eelbrain._utils.ui
    - eelbrain._wxgui
    - eelbrain._wxutils
    - eelbrain.experiment
    - eelbrain.experiment.test
    - eelbrain.load
    - eelbrain.load.tests
    - eelbrain.mne_fixes
    - eelbrain.plot
    - eelbrain.plot.tests
    - eelbrain.save
    - eelbrain.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://pythonhosted.org/eelbrain
  license: GPL3
  summary: 'MEG/EEG analysis tools'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
