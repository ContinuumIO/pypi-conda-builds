package:
  name: django-messages-ui
  version: "2.0.2"

source:
  fn: django-messages-ui-2.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-messages-ui/django-messages-ui-2.0.2.tar.gz
  md5: 0cb9c972d43f84461157d9eb0ba84a3b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-messages-ui = django-messages-ui:main
    #
    # Would create an entry point called django-messages-ui that calls django-messages-ui.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - messages_ui

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jgerigmeyer/jquery-django-messages-ui/
  license: MIT License
  summary: 'JS client-side messages plugin, with support for Djangocontrib.messages app'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
