package:
  name: gitter
  version: "0.2.8"

source:
  fn: gitter-0.2.8.tar.gz
  url: https://pypi.python.org/packages/source/g/gitter/gitter-0.2.8.tar.gz
  md5: 8e7d61dd37a53d6fc9dae7d228d26989
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gitter = gitter:main
    #
    # Would create an entry point called gitter that calls gitter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - termenu ==1.1.5

  run:
    - python
    - termenu ==1.1.5

test:
  # Python imports
  imports:
    - gitter

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/gooli/gitter
  license: MIT License
  summary: 'Improved command line interface for git'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
