package:
  name: djgeneric
  version: "0.1.6"

source:
  fn: djgeneric-0.1.6.tar.gz
  url: https://pypi.python.org/packages/source/d/djgeneric/djgeneric-0.1.6.tar.gz
  md5: 4145d95ff2f7bc0bb84b8a6b26078797
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djgeneric = djgeneric:main
    #
    # Would create an entry point called djgeneric that calls djgeneric.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - djgeneric
    - djgeneric.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/ferranp/djgeneric
  license: BSD License
  summary: 'Generic utilities for django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
