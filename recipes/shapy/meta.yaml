package:
  name: shapy
  version: "1.0.1"

source:
  fn: ShaPy-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/S/ShaPy/ShaPy-1.0.1.tar.gz
  md5: b1a049af6b4f2604f5ccf6f4fa2cbab9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - shapy = shapy:main
    #
    # Would create an entry point called shapy that calls shapy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - shapy
    - shapy.emulation
    - shapy.emulation.commands
    - shapy.framework
    - shapy.framework.commands
    - shapy.framework.netlink
    - shapy.framework.settings

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/praus/shapy
  license: GNU General Public License (GPL)
  summary: 'Netlink and network emulation framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
