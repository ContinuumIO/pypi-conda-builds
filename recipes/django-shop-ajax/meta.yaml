package:
  name: django-shop-ajax
  version: "0.1.3"

source:
  fn: django-shop-ajax-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-shop-ajax/django-shop-ajax-0.1.3.tar.gz
  md5: 3cedfc7c466e6443142e2c19ccb8d60e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-shop-ajax = django-shop-ajax:main
    #
    # Would create an entry point called django-shop-ajax that calls django-shop-ajax.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.2
    - django-shop >=0.0.13

  run:
    - python
    - django >=1.2
    - django-shop >=0.0.13

test:
  # Python imports
  imports:
    - shop_ajax

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.django-shop.org/
  license: BSD License
  summary: 'Ajax utilities for django SHOP'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
