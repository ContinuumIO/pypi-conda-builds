package:
  name: lribeiro.cherrypy.templating
  version: "1.0.1"

source:
  fn: lribeiro.cherrypy.templating-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/l/lribeiro.cherrypy.templating/lribeiro.cherrypy.templating-1.0.1.tar.gz
  md5: b5a98dfb1523deb34660687274589412
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lribeiro.cherrypy.templating = lribeiro.cherrypy.templating:main
    #
    # Would create an entry point called lribeiro.cherrypy.templating that calls lribeiro.cherrypy.templating.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cherrypy

  run:
    - python
    - cherrypy

test:
  # Python imports
  imports:
    - lribeiro
    - lribeiro.cherrypy
    - lribeiro.cherrypy.templating

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/livioribeiro/cherrypy-templating
  license: BSD License
  summary: 'Template rendering tool for cherrypy'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
