package:
  name: dogpile.core
  version: "0.4.1"

source:
  fn: dogpile.core-0.4.1.tar.gz
  url: https://pypi.python.org/packages/source/d/dogpile.core/dogpile.core-0.4.1.tar.gz
  md5: 01cb19f52bba3e95c9b560f39341f045
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dogpile.core = dogpile.core:main
    #
    # Would create an entry point called dogpile.core that calls dogpile.core.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - dogpile
    - dogpile.core

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/zzzeek/dogpile.core
  license: BSD License
  summary: "A 'dogpile' lock, typically used as a component of a larger caching solution"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
