package:
  name: check_arg
  version: "0.1.3"

source:
  fn: check_arg-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/c/check_arg/check_arg-0.1.3.tar.gz
  md5: 57563050f29e722ea565780a8c4bee87
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - check_arg = check_arg:main
    #
    # Would create an entry point called check_arg that calls check_arg.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - check_arg

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.org/jul/check_arg
  license: Python Software Foundation License
  summary: 'wrapping decorator to enhance the documentation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
