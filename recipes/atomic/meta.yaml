package:
  name: atomic
  version: "0.7.2"

source:
  fn: atomic-0.7.2.tar.gz
  url: https://pypi.python.org/packages/source/a/atomic/atomic-0.7.2.tar.gz
  md5: 36026e8980814d82cd97087b3e55ec9f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - atomic = atomic:main
    #
    # Would create an entry point called atomic that calls atomic.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cffi

  run:
    - python
    - cffi

test:
  # Python imports
  imports:
    - atomic
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/cyberdelia/atomic
  license: MIT License
  summary: 'An atomic class that guarantees atomic updates to its contained value.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
