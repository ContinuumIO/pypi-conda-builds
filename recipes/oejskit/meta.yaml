package:
  name: oejskit
  version: "0.9.0"

source:
  fn: oejskit-0.9.0.tar.gz
  url: https://pypi.python.org/packages/source/o/oejskit/oejskit-0.9.0.tar.gz
  md5: 7f14ef269c7ffb7e6286f9df8812a273
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - oejskit = oejskit:main
    #
    # Would create an entry point called oejskit that calls oejskit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - oejskit

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/pedronis/js-infrastructure/
  license: MIT License
  summary: 'Open End JavaScript testing and utility kit'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
