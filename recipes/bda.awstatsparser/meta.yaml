package:
  name: bda.awstatsparser
  version: "1.2.1"

source:
  fn: bda.awstatsparser-1.2.1.tar.gz
  url: https://pypi.python.org/packages/source/b/bda.awstatsparser/bda.awstatsparser-1.2.1.tar.gz
  md5: 49539ebc11115b23004a0ccf136d6bb0
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bda.awstatsparser = bda.awstatsparser:main
    #
    # Would create an entry point called bda.awstatsparser that calls bda.awstatsparser.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - odict

  run:
    - python
    - setuptools
    - setuptools
    - odict

test:
  # Python imports
  imports:
    - bda
    - bda.awstatsparser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/bda.awstatsparser
  license: GNU General Public License (GPL)
  summary: 'Library for parsing of awstats result files'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
