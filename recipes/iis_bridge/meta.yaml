package:
  name: iis_bridge
  version: "1.8"

source:
  fn: iis_bridge-1.8.zip
  url: https://pypi.python.org/packages/source/i/iis_bridge/iis_bridge-1.8.zip
  md5: 9a05ca5d9da375a259cdb113e828b6e8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - iis_bridge = iis_bridge:main
    #
    # Would create an entry point called iis_bridge that calls iis_bridge.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - jinja2 >=2.7.3

  run:
    - python
    - jinja2 >=2.7.3

test:
  # Python imports
  imports:
    - iis_bridge

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kouroshparsa/iis_bridge
  license: GNU
  summary: 'an iis management tool for windows'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
