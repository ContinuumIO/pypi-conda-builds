package:
  name: python-hdf4
  version: "0.9"

source:
  fn: python-hdf4-0.9.tar.gz
  url: https://pypi.python.org/packages/source/p/python-hdf4/python-hdf4-0.9.tar.gz
  md5: 49a6243a38e99f9c278af8c4b41953b9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-hdf4 = python-hdf4:main
    #
    # Would create an entry point called python-hdf4 that calls python-hdf4.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - pyhdf

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/fhs/python-hdf4
  license: MIT
  summary: 'Python-HDF4: Python interface to the NCSA HDF4 library.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
