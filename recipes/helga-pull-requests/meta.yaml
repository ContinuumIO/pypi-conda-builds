package:
  name: helga-pull-requests
  version: "0.0.5"

source:
  fn: helga-pull-requests-0.0.5.tar.gz
  url: https://pypi.python.org/packages/source/h/helga-pull-requests/helga-pull-requests-0.0.5.tar.gz
  md5: e3dd5b551f3f4be0d27b93eb6fe4ae3f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - helga-pull-requests = helga-pull-requests:main
    #
    # Would create an entry point called helga-pull-requests that calls helga-pull-requests.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/shaunduncan/helga-pull-requests
  license: MIT License
  summary: 'A helga plugin to quickly link to github pull requests.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
