package:
  name: mommygae
  version: "1.1"

source:
  fn: mommygae-1.1.tar.gz
  url: https://pypi.python.org/packages/source/m/mommygae/mommygae-1.1.tar.gz
  md5: 7031865970c145eddb88cd59f7c652f0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mommygae = mommygae:main
    #
    # Would create an entry point called mommygae that calls mommygae.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - mommygae

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/renzon/mommy-gae
  license: BSD License
  summary: 'Automate the creation of models on ndb from Google App Engine'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
