package:
  name: billomat
  version: "0.1.16"

source:
  fn: billomat-0.1.16.tar.gz
  url: https://pypi.python.org/packages/source/b/billomat/billomat-0.1.16.tar.gz
  md5: 27fbf689a6d686ec70694cf2e530e728
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - billomat = billomat:main
    #
    # Would create an entry point called billomat that calls billomat.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests ==2.1.0
    - six ==1.4.1
    - python-dateutil ==2.2
    - httmock ==1.0.7
    - mock ==1.0.1

  run:
    - python
    - requests ==2.1.0
    - six ==1.4.1
    - python-dateutil ==2.2
    - httmock ==1.0.7
    - mock ==1.0.1

test:
  # Python imports
  imports:
    - billomat
    - billomat.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://jensnistler.de/
  license: MIT License
  summary: 'billomat.com API client'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
