package:
  name: python-blick
  version: "0.2.12"

source:
  fn: python-BLICK-0.2.12.tar.gz
  url: https://pypi.python.org/packages/source/p/python-BLICK/python-BLICK-0.2.12.tar.gz
  md5: 44b9a45efc615369830e5741f84ea228
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-blick = python-blick:main
    #
    # Would create an entry point called python-blick that calls python-blick.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - blick
    - blick.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/python-BLICK/
  license: LICENSE.txt
  summary: 'Python implementation of BLICK (a phonotactic probability calculator for English)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
