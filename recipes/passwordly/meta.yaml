package:
  name: passwordly
  version: "0.5"

source:
  fn: passwordly-0.5.tar.gz
  url: https://pypi.python.org/packages/source/p/passwordly/passwordly-0.5.tar.gz
  md5: 6ef62374f495b3454d3d4500124c80b7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - passwordly = passwordly:main
    #
    # Would create an entry point called passwordly that calls passwordly.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - py-bcrypt >=0.2

  run:
    - python
    - py-bcrypt >=0.2

test:
  # Python imports
  imports:
    - passwordly

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/passwordly/passwordly-python
  license: MIT License
  summary: 'Simple library for generating passwordly passwords'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
