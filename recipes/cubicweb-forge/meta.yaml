package:
  name: cubicweb-forge
  version: "1.11.0"

source:
  fn: cubicweb-forge-1.11.0.tar.gz
  url: https://pypi.python.org/packages/source/c/cubicweb-forge/cubicweb-forge-1.11.0.tar.gz
  md5: 9b8110d8c3f8e02c21da217f1c242af1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cubicweb-forge = cubicweb-forge:main
    #
    # Would create an entry point called cubicweb-forge that calls cubicweb-forge.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cubicweb-tag >=1.2.0
    - cubicweb-mailinglist >=1.1.0
    - cubicweb-testcard
    - cubicweb >=3.16.0
    - cubicweb-tracker >=1.12.0
    - cubicweb-nosylist
    - cubicweb-folder >=1.1.0
    - cubicweb-comment >=1.2.0
    - cubicweb-card
    - cubicweb-email >=1.2.1
    - pillow
    - cubicweb-file >=1.2.0

  run:
    - python
    - cubicweb-tag >=1.2.0
    - cubicweb-mailinglist >=1.1.0
    - cubicweb-testcard
    - cubicweb >=3.16.0
    - cubicweb-tracker >=1.12.0
    - cubicweb-nosylist
    - cubicweb-folder >=1.1.0
    - cubicweb-comment >=1.2.0
    - cubicweb-card
    - cubicweb-email >=1.2.1
    - pillow
    - cubicweb-file >=1.2.0

test:
  # Python imports
  imports:
    - forge
    - forge.test
    - forge.views

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.cubicweb.org/project/cubicweb-forge
  license: LGPL
  summary: 'software forge component for the CubicWeb framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
