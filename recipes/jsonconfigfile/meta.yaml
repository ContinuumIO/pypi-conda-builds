package:
  name: jsonconfigfile
  version: "1.0"

source:
  fn: jsonconfigfile-1.0.tar.gz
  url: https://pypi.python.org/packages/source/j/jsonconfigfile/jsonconfigfile-1.0.tar.gz
  md5: 429b7a0572525c4bb85259a2bed09f0f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jsonconfigfile = jsonconfigfile:main
    #
    # Would create an entry point called jsonconfigfile that calls jsonconfigfile.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - singleton3

  run:
    - python
    - singleton3

test:
  # Python imports
  imports:
    - jsonconfigfile

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Pipe-s/jsonconfigfile
  license: UNKNOWN
  summary: 'Python package for importing configuration from a JSON file.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
