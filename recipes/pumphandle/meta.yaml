package:
  name: pumphandle
  version: "0.0.2"

source:
  fn: pumphandle-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/p/pumphandle/pumphandle-0.0.2.tar.gz
  md5: d7d52f5aff1ecdc12aef2a18253023b2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pumphandle = pumphandle:main
    #
    # Would create an entry point called pumphandle that calls pumphandle.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - pumphandle

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/elofgren/pumphandle
  license: MIT License
  summary: 'Tools for computational epidemiology'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
