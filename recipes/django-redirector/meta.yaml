package:
  name: django-redirector
  version: "0.1.3"

source:
  fn: django-redirector-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-redirector/django-redirector-0.1.3.tar.gz
  md5: ae51ebc063c54291efea5f775957fbff
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-redirector = django-redirector:main
    #
    # Would create an entry point called django-redirector that calls django-redirector.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - django_redirector

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/yuchant/django-redirector
  license: Beerware
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
