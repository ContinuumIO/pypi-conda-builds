package:
  name: solve360
  version: "0.9.2"

source:
  fn: solve360-0.9.2.tar.gz
  url: https://pypi.python.org/packages/source/s/solve360/solve360-0.9.2.tar.gz
  md5: 9a89f5117b56898a3b128a9291c10ba3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - solve360 = solve360:main
    #
    # Would create an entry point called solve360 that calls solve360.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=1.0.0
    - iso8601 >=0.1.10

  run:
    - python
    - requests >=1.0.0
    - iso8601 >=0.1.10

test:
  # Python imports
  imports:
    - solve360

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/nibon/solve360
  license: The MIT Licence
  summary: 'Solve360 API Python wrapper'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
