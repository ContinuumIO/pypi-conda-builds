package:
  name: argtools
  version: "0.1.2"

source:
  fn: argtools-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/a/argtools/argtools-0.1.2.tar.gz
  md5: a166002322c9a0d8f60cabbd1429b3a7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - argtools = argtools:main
    #
    # Would create an entry point called argtools that calls argtools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - argtools

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/m1m0r1/argtools.py
  license: UNKNOWN
  summary: 'A wrapper of argparse that helps to build command line tools with minimal effort.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
