package:
  name: django-actionkit-client
  version: "0.5"

source:
  fn: django-actionkit-client-0.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-actionkit-client/django-actionkit-client-0.5.tar.gz
  md5: a8db2bc181776b34c259b89f77e3916f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-actionkit-client = django-actionkit-client:main
    #
    # Would create an entry point called django-actionkit-client that calls django-actionkit-client.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - setuptools
    - requests

test:
  # Python imports
  imports:
    - actionkit

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: BSD
  summary: "A Django wrapper around Actionkit's MySQL client-db, REST API and XML-RPC API"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
