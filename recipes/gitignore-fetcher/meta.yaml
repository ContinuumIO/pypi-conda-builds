package:
  name: gitignore-fetcher
  version: "1.0.0"

source:
  fn: gitignore-fetcher-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/g/gitignore-fetcher/gitignore-fetcher-1.0.0.tar.gz
  md5: 259b3c38262a3d8ce8f8e92ee9850398
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gitignore-fetcher = gitignore-fetcher:main
    #
    # Would create an entry point called gitignore-fetcher that calls gitignore-fetcher.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=0.10.1

  run:
    - python
    - requests >=0.10.1

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://git.andrewlorente.com/AndrewLorente/gitignore-fetcher
  license: UNKNOWN
  summary: 'Fetch the .gitignore defined by GitHub for a given language'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
