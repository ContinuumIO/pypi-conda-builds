package:
  name: zope.psycopgda
  version: "1.1.1"

source:
  fn: zope.psycopgda-1.1.1.tar.gz
  url: https://pypi.python.org/packages/source/z/zope.psycopgda/zope.psycopgda-1.1.1.tar.gz
  md5: fa7b9f912ac302c5dac6060cb0a63ea8
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zope.psycopgda = zope.psycopgda:main
    #
    # Would create an entry point called zope.psycopgda that calls zope.psycopgda.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.component
    - zope.interface
    - zope.rdb
    - zope.app.form

  run:
    - python
    - setuptools
    - zope.component
    - zope.interface
    - zope.rdb
    - zope.app.form

test:
  # Python imports
  imports:
    - zope
    - zope.psycopgda

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/zope.psycopgda
  license: Zope Public License
  summary: 'Psycopg Database Adapter for Zope 3'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
