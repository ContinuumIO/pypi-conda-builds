package:
  name: import_only_py
  version: "0.1"

source:
  fn: import_only_py-0.1.tar.gz
  url: https://pypi.python.org/packages/source/i/import_only_py/import_only_py-0.1.tar.gz
  md5: fdaacfc4fc73d155ab927714f3d88a3a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - import_only_py = import_only_py:main
    #
    # Would create an entry point called import_only_py that calls import_only_py.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - 
    - import_only_py

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/pts/import_only_py
  license: Apache Software License
  summary: 'Restrict imports from specific directories to .py files only.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
