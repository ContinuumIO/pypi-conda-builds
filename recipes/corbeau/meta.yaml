package:
  name: corbeau
  version: "0.2"

source:
  fn: corbeau-0.2.tar.gz
  url: https://pypi.python.org/packages/source/c/corbeau/corbeau-0.2.tar.gz
  md5: c5f6227571ce07690302d3a736c96cf2
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - corbeau = corbeau:main
    #
    # Would create an entry point called corbeau that calls corbeau.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - raven
    - requests

  run:
    - python
    - setuptools
    - raven
    - requests

test:
  # Python imports
  imports:
    - corbeau

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kilink/corbeau
  license: MIT
  summary: 'A Sentry client based on Raven that verifies SSL certs'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
