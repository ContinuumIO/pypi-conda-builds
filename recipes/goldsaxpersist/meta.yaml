package:
  name: goldsaxpersist
  version: "1.03"

source:
  fn: GoldSaxPersist-1.03.zip
  url: https://pypi.python.org/packages/source/G/GoldSaxPersist/GoldSaxPersist-1.03.zip
  md5: 00e406654f14788c4ccda578da5310f6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - goldsaxpersist = goldsaxpersist:main
    #
    # Would create an entry point called goldsaxpersist that calls goldsaxpersist.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - GoldSaxPersist
    - goldsaxpersist

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/VanceKingSaxbeA/GoldSaxPersist.git
  license: Python Software Foundation License
  summary: 'Storage Engine, storage, analytics to the GoldSaxEngine-****Markets or any other engine. This can be used by any Trader, Market Maker, a Retail Investor, Instituitional Investor, Hedge Fund Managers, and Asset Managers.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
