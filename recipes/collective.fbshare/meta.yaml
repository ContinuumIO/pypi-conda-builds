package:
  name: collective.fbshare
  version: "0.3.1"

source:
  fn: collective.fbshare-0.3.1.zip
  url: https://pypi.python.org/packages/source/c/collective.fbshare/collective.fbshare-0.3.1.zip
  md5: f70897da0b0b534af19ff9e815a2262b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.fbshare = collective.fbshare:main
    #
    # Would create an entry point called collective.fbshare that calls collective.fbshare.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.registry

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.registry

test:
  # Python imports
  imports:
    - collective
    - collective.fbshare
    - collective.fbshare.browser
    - collective.fbshare.browser.sc_social_like
    - collective.fbshare.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/collective.fbshare
  license: GPL
  summary: 'Provide some additional Open Graph meta tag in your Plone site, so share items on Facebook will be simpler'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
