package:
  name: glassdoor
  version: "0.0.8"

source:
  fn: glassdoor-0.0.8.tar.gz
  url: https://pypi.python.org/packages/source/g/glassdoor/glassdoor-0.0.8.tar.gz
  md5: 89597b0d30268fc8134b087e4693187a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - glassdoor = glassdoor:main
    #
    # Would create an entry point called glassdoor that calls glassdoor.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - requests >=1.1.0
    - beautifulsoup >=3.2.1

  run:
    - python
    - requests >=1.1.0
    - beautifulsoup >=3.2.1

test:
  # Python imports
  imports:
    - glassdoor
    - test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/hackerlist/glassdoor
  license: LICENSE
  summary: 'Glassdoor Python API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
