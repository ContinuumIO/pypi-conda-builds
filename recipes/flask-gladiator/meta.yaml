package:
  name: flask-gladiator
  version: "0.1"

source:
  fn: Flask-Gladiator-0.1.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Gladiator/Flask-Gladiator-0.1.tar.gz
  md5: 18a8611250462af7690e56dbc9d833c6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-gladiator = flask-gladiator:main
    #
    # Would create an entry point called flask-gladiator that calls flask-gladiator.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - gladiator

  run:
    - python
    - flask
    - gladiator

test:
  # Python imports
  imports:
    - flask_gladiator

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/laco/flask-gladiator
  license: BSD License
  summary: 'Gladiator is a Data Validation Framework for Python3 (Flask Plugin)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
