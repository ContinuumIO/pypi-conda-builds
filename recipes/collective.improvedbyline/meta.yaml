package:
  name: collective.improvedbyline
  version: "1.1"

source:
  fn: collective.improvedbyline-1.1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.improvedbyline/collective.improvedbyline-1.1.tar.gz
  md5: c73cd4788ad2cd637e920ef171c599b1
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.improvedbyline = collective.improvedbyline:main
    #
    # Would create an entry point called collective.improvedbyline that calls collective.improvedbyline.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.improvedbyline
    - collective.improvedbyline.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/collective.improvedbyline
  license: GPL
  summary: 'Extends byline viewlet with Publication Date.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
