package:
  name: zeam.component
  version: "1.0"

source:
  fn: zeam.component-1.0.tar.gz
  url: https://pypi.python.org/packages/source/z/zeam.component/zeam.component-1.0.tar.gz
  md5: b071706d5feeaf576e9c52e46141275c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zeam.component = zeam.component:main
    #
    # Would create an entry point called zeam.component that calls zeam.component.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.interface
    - martian

  run:
    - python
    - setuptools
    - zope.interface
    - martian

test:
  # Python imports
  imports:
    - zeam
    - zeam.component

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/zeam.component
  license: BSD
  summary: 'Component infrastructure, based on zope.interface'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
