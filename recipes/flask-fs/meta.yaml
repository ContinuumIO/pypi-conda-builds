package:
  name: flask-fs
  version: "0.1"

source:
  fn: flask-fs-0.1.tar.gz
  url: https://pypi.python.org/packages/source/f/flask-fs/flask-fs-0.1.tar.gz
  md5: 173764cc3e38ccc75f22300e4a1d877e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-fs = flask-fs:main
    #
    # Would create an entry point called flask-fs that calls flask-fs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - six

  run:
    - python
    - flask
    - six

test:
  # Python imports
  imports:
    - flask_fs
    - flask_fs.backends
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/noirbizarre/flask-fs
  license: MIT License
  summary: u'Simple and easy file storages for Flask'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
