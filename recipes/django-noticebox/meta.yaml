package:
  name: django-noticebox
  version: "0.1"

source:
  fn: django-noticebox-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-noticebox/django-noticebox-0.1.tar.gz
  md5: f02b4b87bb2fd82f68d7b783ca7b013c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-noticebox = django-noticebox:main
    #
    # Would create an entry point called django-noticebox that calls django-noticebox.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - noticebox
    - noticebox.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/mila/django-noticebox/tree/master
  license: BSD License
  summary: 'Django-noticebox is a reusable Django application which provides functionality for sending notices to site users. The notices can be displayed when user signs in, sent by email or both.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
