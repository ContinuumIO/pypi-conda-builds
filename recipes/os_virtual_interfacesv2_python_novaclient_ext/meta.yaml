package:
  name: os_virtual_interfacesv2_python_novaclient_ext
  version: "0.19"

source:
  fn: os_virtual_interfacesv2_python_novaclient_ext-0.19.tar.gz
  url: https://pypi.python.org/packages/source/o/os_virtual_interfacesv2_python_novaclient_ext/os_virtual_interfacesv2_python_novaclient_ext-0.19.tar.gz
  md5: bcefe94ab031d523756a6619fc1eea3a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - os_virtual_interfacesv2_python_novaclient_ext = os_virtual_interfacesv2_python_novaclient_ext:main
    #
    # Would create an entry point called os_virtual_interfacesv2_python_novaclient_ext that calls os_virtual_interfacesv2_python_novaclient_ext.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python-novaclient

  run:
    - python
    - python-novaclient

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rackerlabs/os_virtual_interfacesv2_ext
  license: Apache Software License
  summary: 'Adds Virtual Interfaces support to python-novaclient'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
