package:
  name: ecoxipy
  version: "0.4.0"

source:
  fn: ECoXiPy-0.4.0.tar.gz
  url: https://pypi.python.org/packages/source/E/ECoXiPy/ECoXiPy-0.4.0.tar.gz
  md5: 4f6679f8af1b726fc2acef56191c3c06
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ecoxipy = ecoxipy:main
    #
    # Would create an entry point called ecoxipy that calls ecoxipy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - tinkerpy >=0.2.3

  run:
    - python
    - tinkerpy >=0.2.3

test:
  # Python imports
  imports:
    - ecoxipy
    - ecoxipy.pyxom

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/IvIePhisto/ECoXiPy
  license: MIT License
  summary: 'Easy Creation of XML in Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
