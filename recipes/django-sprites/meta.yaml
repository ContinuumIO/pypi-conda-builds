package:
  name: django-sprites
  version: "0.4.0"

source:
  fn: django-sprites-0.4.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-sprites/django-sprites-0.4.0.tar.gz
  md5: 2152249e05114a8e62460d183aa47306
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-sprites = django-sprites:main
    #
    # Would create an entry point called django-sprites that calls django-sprites.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pil
    - uuid
    - django
    - urllib2
    - urlparse

  run:
    - python
    - pil
    - uuid
    - django
    - urllib2
    - urlparse

test:
  # Python imports
  imports:
    - sprites

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/johnfink8/django-sprites
  license: MIT License
  summary: 'Django app models to handle image sprites to speed up page loads that have multiple small images.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
