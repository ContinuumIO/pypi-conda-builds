package:
  name: chandler-debugplugin
  version: "1.0"

source:
  fn: Chandler-debugPlugin-1.0.tar.gz
  url: https://pypi.python.org/packages/source/C/Chandler-debugPlugin/Chandler-debugPlugin-1.0.tar.gz
  md5: 6fc3616f24d576a315efc323411c78c0
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - chandler-debugplugin = chandler-debugplugin:main
    #
    # Would create an entry point called chandler-debugplugin that calls chandler-debugplugin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - debug
    - debug.blockviewer
    - debug.generate_dialog
    - debug.repositoryviewer
    - debug.scripts
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Apache Software License
  summary: 'Debugging and Test Tools for Chandler'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
