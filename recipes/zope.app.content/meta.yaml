package:
  name: zope.app.content
  version: "3.5.1"

source:
  fn: zope.app.content-3.5.1.tar.gz
  url: https://pypi.python.org/packages/source/z/zope.app.content/zope.app.content-3.5.1.tar.gz
  md5: 0ac6a6fcb5dd6f845759f998d8e8cbb3
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zope.app.content = zope.app.content:main
    #
    # Would create an entry point called zope.app.content that calls zope.app.content.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.componentvocabulary
    - zope.interface
    - zope.schema
    - zope.security

  run:
    - python
    - setuptools
    - zope.componentvocabulary
    - zope.interface
    - zope.schema
    - zope.security

test:
  # Python imports
  imports:
    - zope
    - zope.app
    - zope.app.content

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://cheeseshop.python.org/pypi/zope.app.content
  license: Zope Public License
  summary: 'Zope Content Type'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
