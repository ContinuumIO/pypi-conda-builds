package:
  name: django-publicmanager
  version: "0.9.2"

source:
  fn: django-publicmanager-0.9.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-publicmanager/django-publicmanager-0.9.2.tar.gz
  md5: f69a346c1b17120342397d7da4fc64b9
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-publicmanager = django-publicmanager:main
    #
    # Would create an entry point called django-publicmanager that calls django-publicmanager.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - django_publicmanager

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/gregor-muellegger/django-publicmanager
  license: BSD License
  summary: 'Custom managers that handle public availability of database objects.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
