package:
  name: li-pagador-entrega
  version: "0.0.2"

source:
  fn: li-pagador-entrega-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/l/li-pagador-entrega/li-pagador-entrega-0.0.2.tar.gz
  md5: 98857ba62826a11e26b8175a040b9991
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - li-pagador-entrega = li-pagador-entrega:main
    #
    # Would create an entry point called li-pagador-entrega that calls li-pagador-entrega.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - distribute
    - li-pagador

  run:
    - python
    - distribute
    - li-pagador

test:
  # Python imports
  imports:
    - pagador_entrega

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lojaintegrada/LI-Pagador-Entrega
  license: MIT License
  summary: u'Meio de pagamento usado na entrega em loja'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
