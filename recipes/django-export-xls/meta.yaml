package:
  name: django-export-xls
  version: "0.1.1"

source:
  fn: django-export-xls-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-export-xls/django-export-xls-0.1.1.tar.gz
  md5: 69e53c0aef9ca972a9f01c83fbccc2a0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-export-xls = django-export-xls:main
    #
    # Would create an entry point called django-export-xls that calls django-export-xls.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4.1
    - xlwt >=0.7.5

  run:
    - python
    - django >=1.4.1
    - xlwt >=0.7.5

test:
  # Python imports
  imports:
    - export_xls

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/Daiech/django-export-xls
  license: BSD License
  summary: 'A simple Django app to export data to an excel file.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
