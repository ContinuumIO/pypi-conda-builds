package:
  name: dzenlog-link
  version: "0.1.2"

source:
  fn: dzenlog-link-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/dzenlog-link/dzenlog-link-0.1.2.tar.gz
  md5: 542f7390de99b1d0985e16ecb899a6d5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dzenlog-link = dzenlog-link:main
    #
    # Would create an entry point called dzenlog-link that calls dzenlog-link.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-dzenlog >=0.2.7

  run:
    - python
    - django-dzenlog >=0.2.7

test:
  # Python imports
  imports:
    - dzenlog_link
    - dzenlog_link.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/svetlyak40wt/django-dzenlog-link/
  license: BSD License
  summary: 'This is a simple application to post links to the dzenlog.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
