package:
  name: sphinx-scruffy
  version: "1.0.0"

source:
  fn: sphinx-scruffy-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/s/sphinx-scruffy/sphinx-scruffy-1.0.0.tar.gz
  md5: 9750517e991dfe2a7e5b47328299eb50
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sphinx-scruffy = sphinx-scruffy:main
    #
    # Would create an entry point called sphinx-scruffy that calls sphinx-scruffy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sphinx
    - scruffy
    - pillow

  run:
    - python
    - sphinx
    - scruffy
    - pillow

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT
  summary: 'Plug-in for Sphinx to render "scruffy" diagrams.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
