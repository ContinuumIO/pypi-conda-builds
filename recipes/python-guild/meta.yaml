package:
  name: python-guild
  version: "1.0.1"

source:
  fn: python-guild-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/p/python-guild/python-guild-1.0.1.tar.gz
  md5: bf6af8fb42f1700bd55a465ba3be149d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-guild = python-guild:main
    #
    # Would create an entry point called python-guild that calls python-guild.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - guild
    - guild.sketch

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://sparkslabs.com/michael/
  license: Apache Software License
  summary: 'python-guild'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
