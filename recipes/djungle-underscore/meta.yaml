package:
  name: djungle-underscore
  version: "0.0.1"

source:
  fn: djungle-underscore-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/djungle-underscore/djungle-underscore-0.0.1.tar.gz
  md5: 8b08581ab29cca7e7e39d7b9ce95e7e0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djungle-underscore = djungle-underscore:main
    #
    # Would create an entry point called djungle-underscore that calls djungle-underscore.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4

  run:
    - python
    - django >=1.4

test:
  # Python imports
  imports:
    - djungle
    - djungle.underscore

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/djungle/djungle-underscore
  license: GNU General Public License (GPL)
  summary: 'Underscore.js as Django app.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
