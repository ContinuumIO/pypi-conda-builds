package:
  name: flaky
  version: "2.1.0"

source:
  fn: flaky-2.1.0.tar.gz
  url: https://pypi.python.org/packages/source/f/flaky/flaky-2.1.0.tar.gz
  md5: 8cc045d2236066054bf697bf2b314582
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flaky = flaky:main
    #
    # Would create an entry point called flaky that calls flaky.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - flaky

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/box/flaky
  license: Apache Software License
  summary: u'Plugin for nose or py.test that automatically reruns flaky tests.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
