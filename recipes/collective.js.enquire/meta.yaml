package:
  name: collective.js.enquire
  version: "1.5.6"

source:
  fn: collective.js.enquire-1.5.6.zip
  url: https://pypi.python.org/packages/source/c/collective.js.enquire/collective.js.enquire-1.5.6.zip
  md5: 3d7ee832363e78377eeaed6d36ebe851
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.js.enquire = collective.js.enquire:main
    #
    # Would create an entry point called collective.js.enquire that calls collective.js.enquire.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.js
    - collective.js.enquire
    - collective.js.enquire.example

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/toutpt/collective.js.enquire
  license: GNU General Public License (GPL)
  summary: 'Add enquire.js to Plone resource registry'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
