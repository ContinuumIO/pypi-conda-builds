package:
  name: formencode-jinja2
  version: "0.1.2"

source:
  fn: FormEncode-Jinja2-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/F/FormEncode-Jinja2/FormEncode-Jinja2-0.1.2.tar.gz
  md5: f0570240c2719a14504295a38a4f0770
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - formencode-jinja2 = formencode-jinja2:main
    #
    # Would create an entry point called formencode-jinja2 that calls formencode-jinja2.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - formencode
    - jinja2

  run:
    - python
    - formencode
    - jinja2

test:
  # Python imports
  imports:
    - formencode_jinja2

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Kroisse/FormEncode-Jinja2
  license: MIT License
  summary: 'A Jinja2 extension for filling forms via FormEncode'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
