package:
  name: products.ploneinvite
  version: "1.1.6"

source:
  fn: Products.PloneInvite-1.1.6.tar.gz
  url: https://pypi.python.org/packages/source/P/Products.PloneInvite/Products.PloneInvite-1.1.6.tar.gz
  md5: 87328f828cacbb6fe6da1c9c1d87cee6
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - products.ploneinvite = products.ploneinvite:main
    #
    # Would create an entry point called products.ploneinvite that calls products.ploneinvite.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - Products
    - Products.PloneInvite
    - Products.PloneInvite.Extensions
    - Products.PloneInvite.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/plone-invite/
  license: GPL
  summary: 'Members can invite new members; registration only possible if invited'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
