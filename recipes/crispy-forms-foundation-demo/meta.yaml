package:
  name: crispy-forms-foundation-demo
  version: "0.5.0"

source:
  fn: crispy-forms-foundation-demo-0.5.0.tar.gz
  url: https://pypi.python.org/packages/source/c/crispy-forms-foundation-demo/crispy-forms-foundation-demo-0.5.0.tar.gz
  md5: ea492760036b3e83cdc8b8eaa913f105
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - crispy-forms-foundation-demo = crispy-forms-foundation-demo:main
    #
    # Would create an entry point called crispy-forms-foundation-demo that calls crispy-forms-foundation-demo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - crispy-forms-foundation >=0.5.0

  run:
    - python
    - crispy-forms-foundation >=0.5.0

test:
  # Python imports
  imports:
    - crispy_forms_foundation_demo

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/crispy-forms-foundation-demo
  license: BSD License
  summary: "\nDjango application to demonstrate 'crispy-forms-foundation'\n"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
