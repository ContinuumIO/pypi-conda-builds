package:
  name: sphinx-nose
  version: "0.3.1"

source:
  fn: sphinx-nose-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/s/sphinx-nose/sphinx-nose-0.3.1.tar.gz
  md5: ec66d315238ca0838e99e3a3bce900a3
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sphinx-nose = sphinx-nose:main
    #
    # Would create an entry point called sphinx-nose that calls sphinx-nose.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - nose
    - six
    - sphinx

  run:
    - python
    - setuptools
    - nose
    - six
    - sphinx

test:
  # Python imports
  imports:
    - sphinxnose

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/uranusjr/saim-al-dahr
  license: MIT License
  summary: 'Run Sphinx doctests in Nose.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
