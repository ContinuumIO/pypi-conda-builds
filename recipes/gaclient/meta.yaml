package:
  name: gaclient
  version: "0.3b2"

source:
  fn: gaclient-0.3b2.tar.gz
  url: https://pypi.python.org/packages/source/g/gaclient/gaclient-0.3b2.tar.gz
  md5: 208e6cbd73b53586ad166c78ccd10d25
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gaclient = gaclient:main
    #
    # Would create an entry point called gaclient that calls gaclient.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - requests_oauthlib >=0.4.0

  run:
    - python
    - requests_oauthlib >=0.4.0

test:
  # Python imports
  imports:
    - gaclient

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.python-gaclient.org
  license: Apache Software License
  summary: 'An easy to use interface to Google Analytics.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
