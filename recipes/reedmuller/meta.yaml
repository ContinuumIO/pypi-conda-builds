package:
  name: reedmuller
  version: "1.0.0"

source:
  fn: reedmuller-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/r/reedmuller/reedmuller-1.0.0.tar.gz
  md5: 846bbd64ff2ae86575df6f2d909c7227
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - reedmuller = reedmuller:main
    #
    # Would create an entry point called reedmuller that calls reedmuller.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - reedmuller

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.site.uottawa.ca/~mraap046
  license: Apache Software License
  summary: 'Implementation of Reed-Muller codes.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
