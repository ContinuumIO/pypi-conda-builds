package:
  name: kiwidist
  version: "0.3.1"

source:
  fn: KiwiDist-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/K/KiwiDist/KiwiDist-0.3.1.tar.gz
  md5: 60afd3ebcc6cd3b5b99e1b854c9f016d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kiwidist = kiwidist:main
    #
    # Would create an entry point called kiwidist that calls kiwidist.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - matplotlib >=1.3.1
    - mygene >=2.1.0
    - networkx >=1.8.1
    - numpy >=1.8.0
    - pandas >=0.13.1
    - scipy >=0.13.3

  run:
    - python
    - matplotlib >=1.3.1
    - mygene >=2.1.0
    - networkx >=1.8.1
    - numpy >=1.8.0
    - pandas >=0.13.1
    - scipy >=0.13.3

test:
  # Python imports
  imports:
    - kiwi
    - kiwidist

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: LICENSE.txt
  summary: 'Combining gene-set analysis with network properties.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
