package:
  name: lribeiro.cherrypy.templating.mako
  version: "1.0"

source:
  fn: lribeiro.cherrypy.templating.mako-1.0.tar.gz
  url: https://pypi.python.org/packages/source/l/lribeiro.cherrypy.templating.mako/lribeiro.cherrypy.templating.mako-1.0.tar.gz
  md5: b8f323026b6c2ab185f8f00ece1aef22
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lribeiro.cherrypy.templating.mako = lribeiro.cherrypy.templating.mako:main
    #
    # Would create an entry point called lribeiro.cherrypy.templating.mako that calls lribeiro.cherrypy.templating.mako.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cherrypy
    - mako
    - lribeiro.cherrypy.templating

  run:
    - python
    - cherrypy
    - mako
    - lribeiro.cherrypy.templating

test:
  # Python imports
  imports:
    - lribeiro
    - lribeiro.cherrypy
    - lribeiro.cherrypy.templating
    - lribeiro.cherrypy.templating.mako

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/livioribeiro/cherrypy-templating-mako
  license: BSD License
  summary: 'Mako renderer for lribeiro.cherrypy.templating'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
