package:
  name: jack-bower
  version: "0.1.8"

source:
  fn: jack-bower-0.1.8.tar.gz
  url: https://pypi.python.org/packages/source/j/jack-bower/jack-bower-0.1.8.tar.gz
  md5: b12d9175a7564f07e33597e76395d3b1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jack-bower = jack-bower:main
    #
    # Would create an entry point called jack-bower that calls jack-bower.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - bower
    - bower.management
    - bower.management.commands
    - bower.tests
    - bower.tests.test_app

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Celc/jack-bower
  license: The MIT License
  summary: 'Installing frontend Django dependencies via bower.\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
