package:
  name: frontweb
  version: "0.5"

source:
  fn: frontweb-0.5.tar.gz
  url: https://pypi.python.org/packages/source/f/frontweb/frontweb-0.5.tar.gz
  md5: 635da5bf04a30d3d1c75a02815d46888
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - frontweb = frontweb:main
    #
    # Would create an entry point called frontweb that calls frontweb.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - yapsy
    - django
    - docutils
    - pygments

  run:
    - python
    - yapsy
    - django
    - docutils
    - pygments

test:
  # Python imports
  imports:
    - frontweb
    - frontweb.directives
    - frontweb.management.commands
    - frontweb.yapsy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.frontweb.com.ar
  license: GPLv3
  summary: 'A simple website builder using restructured text'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
