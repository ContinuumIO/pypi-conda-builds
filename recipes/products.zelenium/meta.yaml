package:
  name: products.zelenium
  version: "1.0.3"

source:
  fn: Products.Zelenium-1.0.3.tar.gz
  url: https://pypi.python.org/packages/source/P/Products.Zelenium/Products.Zelenium-1.0.3.tar.gz
  md5: 57e846e670c4ef139791a9bcbd51c240
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - products.zelenium = products.zelenium:main
    #
    # Would create an entry point called products.zelenium that calls products.zelenium.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zope2 >=2.9.8

  run:
    - python
    - setuptools
    - setuptools
    - zope2 >=2.9.8

test:
  # Python imports
  imports:
    - Products
    - Products.Zelenium
    - Products.Zelenium.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/Products.Zelenium
  license: Zope Public License
  summary: 'Run Selenium test suites from within Zope2'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
