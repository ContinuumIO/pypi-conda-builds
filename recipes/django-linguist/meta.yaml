package:
  name: django-linguist
  version: "0.1.3"

source:
  fn: django-linguist-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-linguist/django-linguist-0.1.3.tar.gz
  md5: ff077f0314f16994fe11b8ef606ea047
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-linguist = django-linguist:main
    #
    # Would create an entry point called django-linguist that calls django-linguist.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - example
    - linguist
    - linguist.models
    - linguist.tests
    - linguist.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/ulule/django-linguist
  license: MIT License
  summary: 'An application to manage translations in Django models'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
