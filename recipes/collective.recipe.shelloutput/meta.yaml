package:
  name: collective.recipe.shelloutput
  version: "0.1"

source:
  fn: collective.recipe.shelloutput-0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.recipe.shelloutput/collective.recipe.shelloutput-0.1.tar.gz
  md5: 2af0bb173c845f3710bacc1562d71dbf
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.recipe.shelloutput = collective.recipe.shelloutput:main
    #
    # Would create an entry point called collective.recipe.shelloutput that calls collective.recipe.shelloutput.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout

test:
  # Python imports
  imports:
    - collective
    - collective.recipe
    - collective.recipe.shelloutput
    - collective.recipe.shelloutput.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/
  license: Zope Public License
  summary: 'Execute shell commands and reuse the output in another part of the buildout.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
