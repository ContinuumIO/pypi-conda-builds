package:
  name: collective.contact.membrane
  version: "1.0.2"

source:
  fn: collective.contact.membrane-1.0.2.zip
  url: https://pypi.python.org/packages/source/c/collective.contact.membrane/collective.contact.membrane-1.0.2.zip
  md5: 64ca0b6de861ced3661d7d3051fbf8d1
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.contact.membrane = collective.contact.membrane:main
    #
    # Would create an entry point called collective.contact.membrane that calls collective.contact.membrane.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - collective.contact.core >1.1
    - dexterity.membrane >0.4
    - ecreall.helpers.upgrade
    - five.grok
    - plone.api
    - setuptools

  run:
    - python
    - setuptools
    - collective.contact.core >1.1
    - dexterity.membrane >0.4
    - ecreall.helpers.upgrade
    - five.grok
    - plone.api
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.contact
    - collective.contact.membrane
    - collective.contact.membrane.behaviors
    - collective.contact.membrane.content
    - collective.contact.membrane.tests
    - collective.contact.membrane.upgrades

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/collective.contact.membrane
  license: GPL
  summary: 'Membrane integration for collective.contact content types'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
