package:
  name: django-signals-ahoy
  version: "0.1-3"

source:
  fn: django-signals-ahoy-0.1-3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-signals-ahoy/django-signals-ahoy-0.1-3.tar.gz
  md5: 0e7633d77e5e6e87f5d634d12f313a00
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-signals-ahoy = django-signals-ahoy:main
    #
    # Would create an entry point called django-signals-ahoy that calls django-signals-ahoy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - signals_ahoy
    - signals_ahoy.test_app
    - signals_ahoy.test_app.customapp
    - signals_ahoy.test_app.localsite

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/bkroeze/django-signals-ahoy/
  license: BSD License
  summary: 'signals-ahoy'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
