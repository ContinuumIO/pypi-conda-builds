package:
  name: ihextools
  version: "1.0.0"

source:
  fn: ihextools-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/i/ihextools/ihextools-1.0.0.tar.gz
  md5: fcc19a558a4da784c9cbcd0f44e69226
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ihextools = ihextools:main
    #
    # Would create an entry point called ihextools that calls ihextools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - ihextools

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Jeff-Ciesielski/ihexpy
  license: MIT License
  summary: 'Simple Intel hex library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
