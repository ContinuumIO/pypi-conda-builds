package:
  name: flask-negotiation
  version: "0.1.9"

source:
  fn: Flask-Negotiation-0.1.9.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Negotiation/Flask-Negotiation-0.1.9.tar.gz
  md5: 34c1e7b306af0c8f372b61a317e23a8f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-negotiation = flask-negotiation:main
    #
    # Would create an entry point called flask-negotiation that calls flask-negotiation.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask

  run:
    - python
    - flask

test:
  # Python imports
  imports:
    - flask_negotiation

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://blog.hardtack.me/
  license: UNKNOWN
  summary: 'Better content negotiation for flask'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
