package:
  name: pelican-link-bugs
  version: "0.2"

source:
  fn: pelican-link-bugs-0.2.tar.gz
  url: https://pypi.python.org/packages/source/p/pelican-link-bugs/pelican-link-bugs-0.2.tar.gz
  md5: c4a115d96ddb87f964c57eb6f3e90413
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pelican-link-bugs = pelican-link-bugs:main
    #
    # Would create an entry point called pelican-link-bugs that calls pelican-link-bugs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pelican

  run:
    - python
    - pelican

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/pmclanahan/pelican-list-bugs
  license: MIT License
  summary: 'Automatically link bug tracker references.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
