package:
  name: i3minator
  version: "0.0.4"

source:
  fn: i3minator-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/i/i3minator/i3minator-0.0.4.tar.gz
  md5: 5163b299f24d2456c5a531e47e73b9e8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - i3minator = i3minator:main
    #
    # Would create an entry point called i3minator that calls i3minator.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - i3-py >=0.6.4
    - pyyaml

  run:
    - python
    - i3-py >=0.6.4
    - pyyaml

test:
  # Python imports
  imports:
    - i3minator

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/carlesso/i3minator
  license: LICENSE.txt
  summary: 'i3 project manager similar to tmuxinator'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
