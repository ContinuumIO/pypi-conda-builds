package:
  name: competeapi
  version: "0.1"

source:
  fn: competeapi-0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/competeapi/competeapi-0.1.tar.gz
  md5: 51e3d3b9f21882c00113d5c578f754f2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - competeapi = competeapi:main
    #
    # Would create an entry point called competeapi that calls competeapi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - competeapi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/maxretter/python-compete-api
  license: UNKNOWN
  summary: 'A wrapper for the compete.com api'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
