package:
  name: django-pjaxr
  version: "1.1.1"

source:
  fn: django-pjaxr-1.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-pjaxr/django-pjaxr-1.1.1.tar.gz
  md5: 10ebe1a30cebe5bc237bd643097f352e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-pjaxr = django-pjaxr:main
    #
    # Would create an entry point called django-pjaxr that calls django-pjaxr.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_pjaxr
    - django_pjaxr/runtests
    - django_pjaxr/templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://noxic-action.de
  license: MIT
  summary: 'Django extension to use jquery-pjaxr easily!'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
