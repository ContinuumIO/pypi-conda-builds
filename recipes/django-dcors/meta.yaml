package:
  name: django-dcors
  version: "0.1"

source:
  fn: django-dcors-0.1.zip
  url: https://pypi.python.org/packages/source/d/django-dcors/django-dcors-0.1.zip
  md5: e90a5e89785a96d26ac939cede78dcb3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-dcors = django-dcors:main
    #
    # Would create an entry point called django-dcors that calls django-dcors.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - dcors

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/prasanthn/django-dcors
  license: MIT License
  summary: 'Django middleware for adding CORS HTTP headers.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
