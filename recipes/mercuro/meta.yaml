package:
  name: mercuro
  version: "0.1.1"

source:
  fn: mercuro-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/m/mercuro/mercuro-0.1.1.tar.gz
  md5: 739c2da07c2defa792a2d50a364ded8f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mercuro = mercuro:main
    #
    # Would create an entry point called mercuro that calls mercuro.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - mercuro

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/briancline/mercuro
  license: Apache Software License
  summary: 'A simple daemon that listens for syslog events and forwards them to a Riemann server.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
