package:
  name: django-filemaker
  version: "0.2.2"

source:
  fn: django-filemaker-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-filemaker/django-filemaker-0.2.2.tar.gz
  md5: c0483568816f6e0aadee4dcdb5464c71
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-filemaker = django-filemaker:main
    #
    # Would create an entry point called django-filemaker that calls django-filemaker.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - django-appconf
    - lxml
    - python-dateutil
    - pytz
    - requests
    - urlobject

  run:
    - python
    - django
    - django-appconf
    - lxml
    - python-dateutil
    - pytz
    - requests
    - urlobject

test:
  # Python imports
  imports:
    - filemaker
    - test_project

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/TitanEntertainmentGroup/django-filemaker
  license: BSD License
  summary: 'FileMaker access and integration with Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
