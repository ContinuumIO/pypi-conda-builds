package:
  name: django-dictpatterns
  version: "0.2.1"

source:
  fn: django-dictpatterns-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-dictpatterns/django-dictpatterns-0.2.1.tar.gz
  md5: f716f8635afe59e8438c60c297234e6c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-dictpatterns = django-dictpatterns:main
    #
    # Would create an entry point called django-dictpatterns that calls django-dictpatterns.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - dictpatterns
    - dictpatterns.templatetags
    - dictpatterns.urls

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/luyikei/django-dictpatterns
  license: MIT License
  summary: 'This makes your application be able to write simple urlpatterns with dictionary for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
