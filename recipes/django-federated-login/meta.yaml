package:
  name: django-federated-login
  version: "1.0.0"

source:
  fn: django-federated-login-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-federated-login/django-federated-login-1.0.0.tar.gz
  md5: fc199643bc5c73d2c49ec0fd48c27bac
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-federated-login = django-federated-login:main
    #
    # Would create an entry point called django-federated-login that calls django-federated-login.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4.2,<1.7
    - python-openid ==2.2.5

  run:
    - python
    - django >=1.4.2,<1.7
    - python-openid ==2.2.5

test:
  # Python imports
  imports:
    - example
    - federated_login
    - federated_login.migrations
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/Bouke/django-federated-login
  license: MIT License
  summary: 'Provides federated login (SSO) to Django projects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
