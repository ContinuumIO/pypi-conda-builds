package:
  name: collective.recipe.i18noverrides
  version: "1.2"

source:
  fn: collective.recipe.i18noverrides-1.2.zip
  url: https://pypi.python.org/packages/source/c/collective.recipe.i18noverrides/collective.recipe.i18noverrides-1.2.zip
  md5: 4e3dd3e44bfa96a355cde0a98d6397e3
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.recipe.i18noverrides = collective.recipe.i18noverrides:main
    #
    # Would create an entry point called collective.recipe.i18noverrides that calls collective.recipe.i18noverrides.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout

test:
  # Python imports
  imports:
    - collective
    - collective.recipe
    - collective.recipe.i18noverrides
    - collective.recipe.i18noverrides.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.recipe.i18noverrides
  license: GNU General Public License (GPL)
  summary: 'Override translations by putting some .po files in the i18n directory of the zope 2 instance'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
