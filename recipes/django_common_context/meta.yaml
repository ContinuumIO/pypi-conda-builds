package:
  name: django_common_context
  version: "0.1.0"

source:
  fn: django_common_context-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django_common_context/django_common_context-0.1.0.tar.gz
  md5: 5e72149992dc50aeca95961390a047c5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_common_context = django_common_context:main
    #
    # Would create an entry point called django_common_context that calls django_common_context.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4

  run:
    - python
    - django >=1.4

test:
  # Python imports
  imports:
    - common_context
    - common_context.context_processors
    - django_common_context

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/mrsarm/django-common-context
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Django Common Context Processors'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
