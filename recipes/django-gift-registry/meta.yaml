package:
  name: django-gift-registry
  version: "0.4.2"

source:
  fn: django-gift-registry-0.4.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-gift-registry/django-gift-registry-0.4.2.tar.gz
  md5: 7f13e8f9cdaea9f8854899fb0670c9b8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-gift-registry = django-gift-registry:main
    #
    # Would create an entry point called django-gift-registry that calls django-gift-registry.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6
    - pil ==1.1.7

  run:
    - python
    - django >=1.6
    - pil ==1.1.7

test:
  # Python imports
  imports:
    - gift_registry

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://gitlab.com/Sturm/django-gift-registry
  license: Apache Software License
  summary: 'A minimal wedding registry or gift registry app.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
