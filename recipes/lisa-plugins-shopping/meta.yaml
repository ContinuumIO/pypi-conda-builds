package:
  name: lisa-plugins-shopping
  version: "0.0.0.post3"

source:
  fn: lisa-plugins-shopping-0.0.0.post3.tar.gz
  url: https://pypi.python.org/packages/source/l/lisa-plugins-shopping/lisa-plugins-shopping-0.0.0.post3.tar.gz
  md5: edb324a02e9652d1e2f5dd1c41607cd3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lisa-plugins-shopping = lisa-plugins-shopping:main
    #
    # Would create an entry point called lisa-plugins-shopping that calls lisa-plugins-shopping.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Apache Software License
  summary: 'LISA Plugin Shopping'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
