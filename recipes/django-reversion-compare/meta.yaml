package:
  name: django-reversion-compare
  version: "0.5.2"

source:
  fn: django-reversion-compare-0.5.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-reversion-compare/django-reversion-compare-0.5.2.tar.gz
  md5: f83d9a87a26f724eb099c3d0f3525c38
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-reversion-compare = django-reversion-compare:main
    #
    # Would create an entry point called django-reversion-compare that calls django-reversion-compare.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.7,<1.8
    - django-reversion >=1.8

  run:
    - python
    - django >=1.7,<1.8
    - django-reversion >=1.8

test:
  # Python imports
  imports:
    - reversion_compare
    - reversion_compare.utils
    - tests
    - tests.management
    - tests.management.commands
    - tests.test_utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jedie/django-reversion-compare/
  license: GNU General Public License (GPL)
  summary: 'history compare for django-reversion'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
