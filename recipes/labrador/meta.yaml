package:
  name: labrador
  version: "0.6.0"

source:
  fn: Labrador-0.6.0.tar.gz
  url: https://pypi.python.org/packages/source/L/Labrador/Labrador-0.6.0.tar.gz
  md5: 255b92715cad03f8815882444bfa653d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - labrador = labrador:main
    #
    # Would create an entry point called labrador that calls labrador.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyyaml
    - jprops
    - requests

  run:
    - python
    - pyyaml
    - jprops
    - requests

test:
  # Python imports
  imports:
    - labrador
    - labrador.cache
    - labrador.output
    - labrador.retriever

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/framed-data/labrador
  license: UNKNOWN
  summary: 'Configuration retrieval'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
