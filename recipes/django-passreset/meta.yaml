package:
  name: django-passreset
  version: "0.3"

source:
  fn: django-passreset-0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-passreset/django-passreset-0.3.tar.gz
  md5: 278cd4214805b74c721ecc3f3a18af40
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-passreset = django-passreset:main
    #
    # Would create an entry point called django-passreset that calls django-passreset.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - passreset

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/bendavis78
  license: UNKNOWN
  summary: u'Easily add password reset workflows to your django project.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
