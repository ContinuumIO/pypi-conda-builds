package:
  name: django-omblog
  version: "1.3.20"

source:
  fn: django-omblog-1.3.20.tar.gz
  url: https://pypi.python.org/packages/source/d/django-omblog/django-omblog-1.3.20.tar.gz
  md5: fee9abd29fa39d10340de49c2dc21757
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-omblog = django-omblog:main
    #
    # Would create an entry point called django-omblog that calls django-omblog.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django >=1.4.10
    - beautifulsoup4
    - pillow
    - pygments
    - markdown2

  run:
    - python
    - django >=1.4.10
    - beautifulsoup4
    - pillow
    - pygments
    - markdown2

test:
  # Python imports
  imports:
    - omblog
    - omblog.templatetags
    - omblog.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jamiecurle/django-omblog
  license: BSD 3 Clause
  summary: 'Another blog engine, great.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
