package:
  name: factory_boy
  version: "2.5.2"

source:
  fn: factory_boy-2.5.2.tar.gz
  url: https://pypi.python.org/packages/source/f/factory_boy/factory_boy-2.5.2.tar.gz
  md5: c7fb4533e7b4ddce1f2bdbc9f807c2d3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - factory_boy = factory_boy:main
    #
    # Would create an entry point called factory_boy that calls factory_boy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools >=0.8

  run:
    - python
    - setuptools >=0.8

test:
  # Python imports
  imports:
    - factory
    - factory_boy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rbarrois/factory_boy
  license: MIT License
  summary: "A versatile test fixtures replacement based on thoughtbot's factory_girl for Ruby."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
