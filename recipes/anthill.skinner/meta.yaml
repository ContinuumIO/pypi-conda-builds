package:
  name: anthill.skinner
  version: "0.8"

source:
  fn: anthill.skinner-0.8.tar.gz
  url: https://pypi.python.org/packages/source/a/anthill.skinner/anthill.skinner-0.8.tar.gz
  md5: 00a7118e2b8223d0f04fd3af8d0cb793
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - anthill.skinner = anthill.skinner:main
    #
    # Would create an entry point called anthill.skinner that calls anthill.skinner.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - anthill.tal.macrorenderer
    - collective.autopermission

  run:
    - python
    - setuptools
    - setuptools
    - anthill.tal.macrorenderer
    - collective.autopermission

test:
  # Python imports
  imports:
    - anthill
    - anthill.skinner
    - anthill.skinner.browser
    - anthill.skinner.templates

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.banality.de
  license: GPL
  summary: 'Skinning for plone made easy'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
