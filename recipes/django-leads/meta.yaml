package:
  name: django-leads
  version: "0.1.4"

source:
  fn: django-leads-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-leads/django-leads-0.1.4.tar.gz
  md5: a19784b033be4155f661fbc5a58dd0d0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-leads = django-leads:main
    #
    # Would create an entry point called django-leads that calls django-leads.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5.1
    - django-floppyforms >=1.1
    - django-crispy-forms >=1.4.0

  run:
    - python
    - django >=1.5.1
    - django-floppyforms >=1.1
    - django-crispy-forms >=1.4.0

test:
  # Python imports
  imports:
    - leads

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dnmellen/django-leads
  license: BSD License
  summary: 'An easy, functional and customizable lead page for your next big thing, ready to use in your django project.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
