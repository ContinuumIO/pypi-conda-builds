package:
  name: slimmer
  version: "0.1.30"

source:
  fn: slimmer-0.1.30.tar.gz
  url: https://pypi.python.org/packages/source/s/slimmer/slimmer-0.1.30.tar.gz
  md5: 37445985bad64baa04169dbe85930c3c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - slimmer = slimmer:main
    #
    # Would create an entry point called slimmer that calls slimmer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - slimmer
    - slimmer.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.fry-it.com
  license: Python Software Foundation License
  summary: 'HTML,XHTML,CSS,JavaScript optimizer'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
