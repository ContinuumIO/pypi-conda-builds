package:
  name: doco
  version: "0.0.3"

source:
  fn: doco-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/doco/doco-0.0.3.tar.gz
  md5: f93369a6e4e11d8644732afb7525351e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - doco = doco:main
    #
    # Would create an entry point called doco that calls doco.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests
    - simplejson

  run:
    - python
    - requests
    - simplejson

test:
  # Python imports
  imports:
    - doco
    - examples

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/heavenshell/py-doco
  license: BSD License
  summary: 'Client library for docomo API written in Python.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
