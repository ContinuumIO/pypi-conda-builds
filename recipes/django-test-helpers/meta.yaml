package:
  name: django-test-helpers
  version: "0.1.0"

source:
  fn: django-test-helpers-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-test-helpers/django-test-helpers-0.1.0.tar.gz
  md5: b0155cf085a4a18b0a77fcd7d9056174
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-test-helpers = django-test-helpers:main
    #
    # Would create an entry point called django-test-helpers that calls django-test-helpers.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - testhelpers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/disqus/django-test-helpers
  license: Apache License 2.0
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
