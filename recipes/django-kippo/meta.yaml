package:
  name: django-kippo
  version: "0.1.0.0707"

source:
  fn: django-kippo-0.1.0.0707.tar.gz
  url: https://pypi.python.org/packages/source/d/django-kippo/django-kippo-0.1.0.0707.tar.gz
  md5: 0226e68047bb1baecf2d4ec1ea6eece3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-kippo = django-kippo:main
    #
    # Would create an entry point called django-kippo that calls django-kippo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4

  run:
    - python
    - django >=1.4

test:
  # Python imports
  imports:
    - django_kippo
    - django_kippo_test_project

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jedie/django-kippo/
  license: GNU General Public License (GPL)
  summary: 'Django App for kippo SSH Honeypot'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
