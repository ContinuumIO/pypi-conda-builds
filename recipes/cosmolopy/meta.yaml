package:
  name: cosmolopy
  version: "0.1.104"

source:
  fn: CosmoloPy-0.1.104.tar.gz
  url: https://pypi.python.org/packages/source/C/CosmoloPy/CosmoloPy-0.1.104.tar.gz
  md5: b8b730ad708c2005dd258e8fc46d7d9a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cosmolopy = cosmolopy:main
    #
    # Would create an entry point called cosmolopy that calls cosmolopy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy
    - scipy

  run:
    - python
    - numpy
    - scipy

test:
  # Python imports
  imports:
    - cosmolopy
    - cosmolopy.EH

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://roban.github.com/CosmoloPy/
  license: MIT License
  summary: 'a cosmology package for Python.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
