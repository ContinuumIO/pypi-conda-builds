package:
  name: django-changuito
  version: "0.7"

source:
  fn: django-changuito-0.7.tar.gz
  url: https://pypi.python.org/packages/source/d/django-changuito/django-changuito-0.7.tar.gz
  md5: df43e556416a42eacb8514866096b6e8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-changuito = django-changuito:main
    #
    # Would create an entry point called django-changuito that calls django-changuito.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - changuito

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/angvp/django-changuito
  license: LGPL v3
  summary: 'A fork of django-cart with the same simplicity but up-to-date'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
