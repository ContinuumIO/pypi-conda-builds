package:
  name: pygoogleearth
  version: "0.0.2"

source:
  fn: pygoogleearth-0.0.2.zip
  url: https://pypi.python.org/packages/source/p/pygoogleearth/pygoogleearth-0.0.2.zip
  md5: e16cebef6c8fd959ce991b6486f2b6f8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pygoogleearth = pygoogleearth:main
    #
    # Would create an entry point called pygoogleearth that calls pygoogleearth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pywin32

  run:
    - python
    - pywin32

test:
  # Python imports
  imports:
    - pygoogleearth

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pygoogleearth.googlecode.com/
  license: MIT License
  summary: 'Python wrapper for Google Earth COM API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
