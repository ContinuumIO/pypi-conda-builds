package:
  name: esbackup
  version: "0.2"

source:
  fn: esbackup-0.2.tar.gz
  url: https://pypi.python.org/packages/source/e/esbackup/esbackup-0.2.tar.gz
  md5: 1e74f1221822fef8e6d88b2c15afa077
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - esbackup = esbackup:main
    #
    # Would create an entry point called esbackup that calls esbackup.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - esbackup
    - esbackup.contrib
    - esbackup.contrib.gnupg
    - esbackup.plugins

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD License
  summary: 'Simple backup tool'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
