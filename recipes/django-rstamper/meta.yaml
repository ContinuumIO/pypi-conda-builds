package:
  name: django-rstamper
  version: "0.0.8"

source:
  fn: django-rstamper-0.0.8.tar.gz
  url: https://pypi.python.org/packages/source/d/django-rstamper/django-rstamper-0.0.8.tar.gz
  md5: 311e0adf10e4e4929dc2c6f022c95181
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-rstamper = django-rstamper:main
    #
    # Would create an entry point called django-rstamper that calls django-rstamper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django <1.5
    - south ==0.7.5
    - djangorestframework ==2.4.3
    - djangorestframework-bulk ==0.1.3
    - requests ==2.4.3

  run:
    - python
    - django <1.5
    - south ==0.7.5
    - djangorestframework ==2.4.3
    - djangorestframework-bulk ==0.1.3
    - requests ==2.4.3

test:
  # Python imports
  imports:
    - rstamper
    - rstamper.management
    - rstamper.management.commands
    - rstamper.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/r0sk/django-rstamper
  license: BSD License
  summary: 'Yet another Django Gallery App'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
