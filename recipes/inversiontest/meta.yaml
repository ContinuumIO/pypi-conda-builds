package:
  name: inversiontest
  version: "1.2"

source:
  fn: InversionTest-1.2.zip
  url: https://pypi.python.org/packages/source/I/InversionTest/InversionTest-1.2.zip
  md5: 7f32b789b94ca0fb2af559c0ab204a77
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - inversiontest = inversiontest:main
    #
    # Would create an entry point called inversiontest that calls inversiontest.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - InversionTest
    - InversionTest.Tests
    - inversiontest

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://sites.google.com/site/benjaminnye/
  license: Apache Software License
  summary: 'Sequence Permutation Similarity and Hypothesis Testing Package'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
