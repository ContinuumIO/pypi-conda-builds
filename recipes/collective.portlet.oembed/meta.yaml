package:
  name: collective.portlet.oembed
  version: "1.2.1"

source:
  fn: collective.portlet.oembed-1.2.1.zip
  url: https://pypi.python.org/packages/source/c/collective.portlet.oembed/collective.portlet.oembed-1.2.1.zip
  md5: bcb764c772a00a6f01a7181e1aa5f809
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.portlet.oembed = collective.portlet.oembed:main
    #
    # Would create an entry point called collective.portlet.oembed that calls collective.portlet.oembed.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.oembed

  run:
    - python
    - setuptools
    - setuptools
    - collective.oembed

test:
  # Python imports
  imports:
    - collective
    - collective.portlet
    - collective.portlet.oembed

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.portlet.oembed
  license: GNU General Public License (GPL)
  summary: 'Portlet waiting for an external link displayed using oembed/embedly'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
