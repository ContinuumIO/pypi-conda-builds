package:
  name: haversine
  version: "0.1"

source:
  fn: haversine-0.1.tar.gz
  url: https://pypi.python.org/packages/source/h/haversine/haversine-0.1.tar.gz
  md5: 3ce2922320ab6e09407ffef375550899
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - haversine = haversine:main
    #
    # Would create an entry point called haversine that calls haversine.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - haversine

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mapado/haversine
  license: GNU General Public License v3 (GPLv3)
  summary: 'Calculate the distance bewteen 2 points on Earth.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
