package:
  name: django-webmaster-verification
  version: "0.2.4"

source:
  fn: django-webmaster-verification-0.2.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-webmaster-verification/django-webmaster-verification-0.2.4.tar.gz
  md5: 19e14f61cf10bcd377e3200b755d0e17
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-webmaster-verification = django-webmaster-verification:main
    #
    # Would create an entry point called django-webmaster-verification that calls django-webmaster-verification.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4

  run:
    - python
    - django >=1.4

test:
  # Python imports
  imports:
    - test_project
    - webmaster_verification

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/nkuttler/django-webmaster-verification
  license: BSD License
  summary: 'Webmaster tools verification for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
