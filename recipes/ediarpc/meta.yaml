package:
  name: ediarpc
  version: "0.3"

source:
  fn: ediarpc-0.3.tar.gz
  url: https://pypi.python.org/packages/source/e/ediarpc/ediarpc-0.3.tar.gz
  md5: ea8f942b359262f9429cea1c0d444fb5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ediarpc = ediarpc:main
    #
    # Would create an entry point called ediarpc that calls ediarpc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - ediarpc

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/tortoiselabs/ediarpc
  license: ISC License (ISCL)
  summary: 'A JSON-based RPC calling framework which supports inline signatures derived from passphrases'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
