package:
  name: django-jsonschema
  version: "0.2.0"

source:
  fn: django-jsonschema-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-jsonschema/django-jsonschema-0.2.0.tar.gz
  md5: 3802d4aa6aa10b8fc2603468c8680de3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-jsonschema = django-jsonschema:main
    #
    # Would create an entry point called django-jsonschema that calls django-jsonschema.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - djangojsonschema
    - djangojsonschema.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/celerityweb/django-jsonschema
  license: BSD License
  summary: 'django-jsonschema converts Django Forms into JSON Schema compatibile representations'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
