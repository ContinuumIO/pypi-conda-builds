package:
  name: the-real-django-wordpress
  version: "0.10.1"

source:
  fn: the-real-django-wordpress-0.10.1.tar.gz
  url: https://pypi.python.org/packages/source/t/the-real-django-wordpress/the-real-django-wordpress-0.10.1.tar.gz
  md5: cf5d2a4346d92fd88aedd5b9c5bbb075
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - the-real-django-wordpress = the-real-django-wordpress:main
    #
    # Would create an entry point called the-real-django-wordpress that calls the-real-django-wordpress.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - wordpress

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/sunlightlabs/django-wordpress/
  license: BSD License
  summary: 'Django models and views for a WordPress database.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
