package:
  name: confmanager
  version: "2.0"

source:
  fn: confmanager-2.0.zip
  url: https://pypi.python.org/packages/source/c/confmanager/confmanager-2.0.zip
  md5: e89d8bc627d7f2351476a4ebc5561e12
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - confmanager = confmanager:main
    #
    # Would create an entry point called confmanager that calls confmanager.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - confmanager

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/chrisnorman7/confmanager.git
  license: GPL
  summary: "A configuration manager using wx as it's front ent."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
