package:
  name: alabaster
  version: "0.7.4"

source:
  fn: alabaster-0.7.4.tar.gz
  url: https://pypi.python.org/packages/source/a/alabaster/alabaster-0.7.4.tar.gz
  md5: ba31bf652194200428aa4e3d976f5ccd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - alabaster = alabaster:main
    #
    # Would create an entry point called alabaster that calls alabaster.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - alabaster

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bitprophet/alabaster
  license: BSD License
  summary: 'A configurable sidebar-enabled Sphinx theme'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
