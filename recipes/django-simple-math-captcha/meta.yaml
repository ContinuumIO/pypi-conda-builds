package:
  name: django-simple-math-captcha
  version: "1.0.5"

source:
  fn: django-simple-math-captcha-1.0.5.zip
  url: https://pypi.python.org/packages/source/d/django-simple-math-captcha/django-simple-math-captcha-1.0.5.zip
  md5: 503bdc3f4441f619bfc4b38bcf301b83
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-simple-math-captcha = django-simple-math-captcha:main
    #
    # Would create an entry point called django-simple-math-captcha that calls django-simple-math-captcha.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - simplemathcaptcha
    - test_simplemathcaptcha

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://brandonftaylor.com/
  license: Apache Software License
  summary: 'An easy-to-use math field/widget captcha for Django forms.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
