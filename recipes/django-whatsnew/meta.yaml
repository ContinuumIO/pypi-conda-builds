package:
  name: django-whatsnew
  version: "1.0"

source:
  fn: django-whatsnew-1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-whatsnew/django-whatsnew-1.0.tar.gz
  md5: 4b2f504703ce40ccf40c6076fa20031d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-whatsnew = django-whatsnew:main
    #
    # Would create an entry point called django-whatsnew that calls django-whatsnew.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - whatsnew
    - whatsnew.migrations
    - whatsnew.south_migrations
    - whatsnew.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/saxix/django-whatsnew
  license: BSD
  summary: "Simple application to manage `what's new` screen."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
