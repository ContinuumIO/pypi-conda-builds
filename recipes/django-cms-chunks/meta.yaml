package:
  name: django-cms-chunks
  version: "0.0.2"

source:
  fn: django-cms-chunks-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-cms-chunks/django-cms-chunks-0.0.2.tar.gz
  md5: f90420f731178a28f002097003e9b73a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-cms-chunks = django-cms-chunks:main
    #
    # Would create an entry point called django-cms-chunks that calls django-cms-chunks.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django <=1.6
    - django-cms <=2.4

  run:
    - python
    - django <=1.6
    - django-cms <=2.4

test:
  # Python imports
  imports:
    - cms_chunks
    - cms_chunks.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/devartis/django-cms-chunks/
  license: MIT License
  summary: 'Edit chunks of your non CMS pages using django-cms'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
