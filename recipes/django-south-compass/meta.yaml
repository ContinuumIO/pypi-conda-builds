package:
  name: django-south-compass
  version: "0.0.2a0"

source:
  fn: django-south-compass-0.0.2a0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-south-compass/django-south-compass-0.0.2a0.tar.gz
  md5: f5a6a2bf4ba7df98f3b26c3940d351e8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-south-compass = django-south-compass:main
    #
    # Would create an entry point called django-south-compass that calls django-south-compass.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/agiliq/compass
  license: BSD License
  summary: 'Compass is a tool to guide your south migrations.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
