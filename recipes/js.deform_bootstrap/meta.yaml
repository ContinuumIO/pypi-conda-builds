package:
  name: js.deform_bootstrap
  version: "0.2.6"

source:
  fn: js.deform_bootstrap-0.2.6.tar.gz
  url: https://pypi.python.org/packages/source/j/js.deform_bootstrap/js.deform_bootstrap-0.2.6.tar.gz
  md5: a2ae12e5c0e5cb05ad8024486ea52a88
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - js.deform_bootstrap = js.deform_bootstrap:main
    #
    # Would create an entry point called js.deform_bootstrap that calls js.deform_bootstrap.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - deform_bootstrap
    - js.bootstrap
    - js.chosen
    - js.deform
    - js.jqueryui >=1.8.24
    - fanstatic
    - setuptools

  run:
    - python
    - setuptools
    - deform_bootstrap
    - js.bootstrap
    - js.chosen
    - js.deform
    - js.jqueryui >=1.8.24
    - fanstatic
    - setuptools

test:
  # Python imports
  imports:
    - js
    - js.deform_bootstrap

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/disko/js.deform_bootstrap
  license: BSD
  summary: 'Fanstatic packaging of deform_bootstrap'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
