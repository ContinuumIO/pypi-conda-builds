package:
  name: circonusapi
  version: "0.1.4"

source:
  fn: circonusapi-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/c/circonusapi/circonusapi-0.1.4.tar.gz
  md5: 208b998a69a159b2f150eb7e2e30cc26
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - circonusapi = circonusapi:main
    #
    # Would create an entry point called circonusapi that calls circonusapi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - circonusapi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/omniti-labs/circonusapi
  license: ISC License (ISCL)
  summary: 'Circonus API library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
