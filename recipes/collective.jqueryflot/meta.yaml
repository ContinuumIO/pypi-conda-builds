package:
  name: collective.jqueryflot
  version: "0.1"

source:
  fn: collective.jqueryflot-0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.jqueryflot/collective.jqueryflot-0.1.tar.gz
  md5: 23c626beccec30cd57883fa695da042d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.jqueryflot = collective.jqueryflot:main
    #
    # Would create an entry point called collective.jqueryflot that calls collective.jqueryflot.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.jqueryflot

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/collective.jqueryflot
  license: GPL
  summary: 'Flot JQuery implementation for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
