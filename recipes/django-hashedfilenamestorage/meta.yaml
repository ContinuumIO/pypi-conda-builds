package:
  name: django-hashedfilenamestorage
  version: "1.0"

source:
  fn: django-hashedfilenamestorage-1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-hashedfilenamestorage/django-hashedfilenamestorage-1.0.tar.gz
  md5: 523ab0c41cccb3bfb64adbd4eb0f4ab2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-hashedfilenamestorage = django-hashedfilenamestorage:main
    #
    # Would create an entry point called django-hashedfilenamestorage that calls django-hashedfilenamestorage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django >=1.3

  run:
    - python
    - django >=1.3

test:
  # Python imports
  imports:
    - django_hashedfilenamestorage

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/ecometrica/django-hashedfilenamestorage/
  license: MIT License
  summary: 'A Django storage backend that names files by hash value.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
