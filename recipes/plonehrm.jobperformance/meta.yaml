package:
  name: plonehrm.jobperformance
  version: "1.3"

source:
  fn: plonehrm.jobperformance-1.3.tar.gz
  url: https://pypi.python.org/packages/source/p/plonehrm.jobperformance/plonehrm.jobperformance-1.3.tar.gz
  md5: f122b076d87e62f4bb5e666e4d512ab4
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plonehrm.jobperformance = plonehrm.jobperformance:main
    #
    # Would create an entry point called plonehrm.jobperformance that calls plonehrm.jobperformance.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.autopermission
    - products.plonehrm >=2.7

  run:
    - python
    - setuptools
    - setuptools
    - collective.autopermission
    - products.plonehrm >=2.7

test:
  # Python imports
  imports:
    - plonehrm
    - plonehrm.jobperformance
    - plonehrm.jobperformance.browser
    - plonehrm.jobperformance.content
    - plonehrm.jobperformance.doc
    - plonehrm.jobperformance.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/plonehrm.jobperformance
  license: GPL
  summary: 'Job performance interviews for Plone HRM.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
