package:
  name: githubbuildout
  version: "0.2"

source:
  fn: githubbuildout-0.2.tar.gz
  url: https://pypi.python.org/packages/source/g/githubbuildout/githubbuildout-0.2.tar.gz
  md5: 291c89027491df54a96ad3e6588e9774
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - githubbuildout = githubbuildout:main
    #
    # Would create an entry point called githubbuildout that calls githubbuildout.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout

test:
  # Python imports
  imports:
    - githubbuildout

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/isolationism/githubbuildout
  license: Zope Public License
  summary: 'Buildout extension to enable downloads from private GitHub repositories into buildout find-links and download recipes'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
