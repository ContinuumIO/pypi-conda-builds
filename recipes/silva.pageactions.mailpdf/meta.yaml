package:
  name: silva.pageactions.mailpdf
  version: "1.0.2"

source:
  fn: silva.pageactions.mailpdf-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/s/silva.pageactions.mailpdf/silva.pageactions.mailpdf-1.0.2.tar.gz
  md5: 092dd179dd059b336fcccf8ca81057bc
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - silva.pageactions.mailpdf = silva.pageactions.mailpdf:main
    #
    # Would create an entry point called silva.pageactions.mailpdf that calls silva.pageactions.mailpdf.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - five.grok
    - megrok.chameleon
    - silva.core.views
    - silva.pageactions.base
    - silva.pageactions.pdf
    - silva.captcha

  run:
    - python
    - setuptools
    - five.grok
    - megrok.chameleon
    - silva.core.views
    - silva.pageactions.base
    - silva.pageactions.pdf
    - silva.captcha

test:
  # Python imports
  imports:
    - silva
    - silva.pageactions
    - silva.pageactions.mailpdf

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: BSD
  summary: 'Send the current by mail as a PDF.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
