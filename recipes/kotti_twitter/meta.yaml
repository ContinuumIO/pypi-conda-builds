package:
  name: kotti_twitter
  version: "0.5"

source:
  fn: kotti_twitter-0.5.zip
  url: https://pypi.python.org/packages/source/k/kotti_twitter/kotti_twitter-0.5.zip
  md5: 9b9ea2aceadd1edf47f1b01e2119fcc5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kotti_twitter = kotti_twitter:main
    #
    # Would create an entry point called kotti_twitter that calls kotti_twitter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - kotti >=0.2.4
    - pytest-cov
    - pytest

  run:
    - python
    - kotti >=0.2.4
    - pytest-cov
    - pytest

test:
  # Python imports
  imports:
    - kotti_twitter

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/kotti_twitter
  license: BSD-derived (http://www.repoze.org/LICENSE.txt)
  summary: 'Add twitter widgets to your Kotti site'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
