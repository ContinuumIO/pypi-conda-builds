package:
  name: p4a.ploneevent
  version: "0.7.4"

source:
  fn: p4a.ploneevent-0.7.4.zip
  url: https://pypi.python.org/packages/source/p/p4a.ploneevent/p4a.ploneevent-0.7.4.zip
  md5: 0a07e7b6d133fe4edef7342f25f33445
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - p4a.ploneevent = p4a.ploneevent:main
    #
    # Would create an entry point called p4a.ploneevent that calls p4a.ploneevent.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - python-dateutil
    - archetypes.schemaextender
    - dateable.kalends >=0.3
    - plone.app.locales >3.3.5
    - plone.indexer

  run:
    - python
    - setuptools
    - setuptools
    - python-dateutil
    - archetypes.schemaextender
    - dateable.kalends >=0.3
    - plone.app.locales >3.3.5
    - plone.indexer

test:
  # Python imports
  imports:
    - p4a
    - p4a.ploneevent
    - p4a.ploneevent.recurrence
    - p4a.ploneevent.recurrence.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/p4a.ploneevent/
  license: GNU General Public License (GPL)
  summary: 'Plone4Artists event extensions for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
