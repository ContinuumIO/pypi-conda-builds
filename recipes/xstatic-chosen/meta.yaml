package:
  name: xstatic-chosen
  version: "0.13.0.1"

source:
  fn: XStatic-Chosen-0.13.0.1.tar.gz
  url: https://pypi.python.org/packages/source/X/XStatic-Chosen/XStatic-Chosen-0.13.0.1.tar.gz
  md5: eb47a1ee3a208927cb29b4841a10dfcd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - xstatic-chosen = xstatic-chosen:main
    #
    # Would create an entry point called xstatic-chosen that calls xstatic-chosen.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - xstatic
    - xstatic.pkg
    - xstatic.pkg.chosen

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://harvesthq.github.io/chosen/
  license: (same as Chosen)
  summary: 'Chosen 0.13.0 (XStatic packaging standard)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
