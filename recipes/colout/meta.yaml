package:
  name: colout
  version: "0.4"

source:
  fn: colout-0.4.tar.gz
  url: https://pypi.python.org/packages/source/c/colout/colout-0.4.tar.gz
  md5: 7353f6a7ba8dad1ab4548890fdcaa343
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - colout = colout:main
    #
    # Would create an entry point called colout that calls colout.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - argparse
    - pygments
    - babel

  run:
    - python
    - argparse
    - pygments
    - babel

test:
  # Python imports
  imports:
    - colout

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://nojhan.github.com/colout/
  license: GNU General Public License v3 (GPLv3)
  summary: 'Color Up Arbitrary Command Ouput.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
