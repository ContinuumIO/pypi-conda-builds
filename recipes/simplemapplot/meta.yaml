package:
  name: simplemapplot
  version: "1.0.3"

source:
  fn: simplemapplot-1.0.3.tar.gz
  url: https://pypi.python.org/packages/source/s/simplemapplot/simplemapplot-1.0.3.tar.gz
  md5: 602083d263f6c00ad6b541748283c838
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - simplemapplot = simplemapplot:main
    #
    # Would create an entry point called simplemapplot that calls simplemapplot.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - simplemapplot

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/simplemapplot/
  license: MIT License
  summary: 'Simple functions for coloring maps'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
