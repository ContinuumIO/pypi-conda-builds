package:
  name: django-djikiki
  version: "1.1"

source:
  fn: django-djikiki-1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-djikiki/django-djikiki-1.1.tar.gz
  md5: de94fc98cd9993c8a33590f477342f47
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-djikiki = django-djikiki:main
    #
    # Would create an entry point called django-djikiki that calls django-djikiki.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - djikiki
    - djikiki.creole
    - project
    - project.djikiki
    - project.djikiki.creole

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.agiliq.com/
  license: GNU General Public License (GPL)
  summary: 'A django wiki'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
