package:
  name: darkcoin
  version: "0.2"

source:
  fn: darkcoin-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/darkcoin/darkcoin-0.2.tar.gz
  md5: 27dc6a8312882d8c2bb962aa3dc000da
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - darkcoin = darkcoin:main
    #
    # Would create an entry point called darkcoin that calls darkcoin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - darkcoin

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/c0ding/darkcoin-api
  license: Apache v2.0 License
  summary: 'Python API for the Darkcoin cryptocurrency.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
