package:
  name: html2bbcode
  version: "2.3.2"

source:
  fn: html2bbcode-2.3.2.tar.bz2
  url: https://pypi.python.org/packages/source/h/html2bbcode/html2bbcode-2.3.2.tar.bz2
  md5: 4c287c27212f2502f8058fb7293eece9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - html2bbcode = html2bbcode:main
    #
    # Would create an entry point called html2bbcode that calls html2bbcode.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - html2bbcode

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/amigo/html2bbcode
  license: BSD License
  summary: 'HTML to BBCode converter'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
