package:
  name: bree
  version: "0.1.3"

source:
  fn: bree-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/b/bree/bree-0.1.3.tar.gz
  md5: ff75585d7012e5a51b3dbf0082675ffd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bree = bree:main
    #
    # Would create an entry point called bree that calls bree.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - tornado
    - certifi
    - cliez

  run:
    - python
    - tornado
    - certifi
    - cliez

test:
  # Python imports
  imports:
    - bree

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: http://opensource.org/licenses/MIT
  summary: 'a micro library for happy coding tornado.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
