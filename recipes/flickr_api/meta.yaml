package:
  name: flickr_api
  version: "0.5"

source:
  fn: flickr_api-0.5.tar.gz
  url: https://pypi.python.org/packages/source/f/flickr_api/flickr_api-0.5.tar.gz
  md5: df321a885b5495773054c74384b2637e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flickr_api = flickr_api:main
    #
    # Would create an entry point called flickr_api that calls flickr_api.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - oauth

  run:
    - python
    - oauth

test:
  # Python imports
  imports:
    - flickr_api

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/python-flickr-api/
  license: BSD License
  summary: 'Python wrapper for the Flickr API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
