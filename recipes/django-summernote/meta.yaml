package:
  name: django-summernote
  version: "0.6.3"

source:
  fn: django-summernote-0.6.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-summernote/django-summernote-0.6.3.tar.gz
  md5: dc8f1ccb0bb880eba61a00c2c228638f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-summernote = django-summernote:main
    #
    # Would create an entry point called django-summernote that calls django-summernote.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - django_summernote

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/summernote/django-summernote
  license: MIT License
  summary: 'Summernote plugin for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
