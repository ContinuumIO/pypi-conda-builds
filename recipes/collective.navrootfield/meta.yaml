package:
  name: collective.navrootfield
  version: "1.0"

source:
  fn: collective.navrootfield-1.0.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.navrootfield/collective.navrootfield-1.0.tar.gz
  md5: 65572923bce1b3b456ea6eb4c680e51d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.navrootfield = collective.navrootfield:main
    #
    # Would create an entry point called collective.navrootfield that calls collective.navrootfield.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - archetypes.schemaextender

  run:
    - python
    - setuptools
    - setuptools
    - archetypes.schemaextender

test:
  # Python imports
  imports:
    - collective
    - collective.navrootfield

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/collective.navrootfield
  license: GPL
  summary: 'Adds a checkbox to set a folder as navigation root'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
