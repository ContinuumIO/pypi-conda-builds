package:
  name: cpmoptimize
  version: "0.2"

source:
  fn: cpmoptimize-0.2.tar.gz
  url: https://pypi.python.org/packages/source/c/cpmoptimize/cpmoptimize-0.2.tar.gz
  md5: 408b053d74cfefbead97e6abe4a8b7d2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cpmoptimize = cpmoptimize:main
    #
    # Would create an entry point called cpmoptimize that calls cpmoptimize.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - byteplay >=0.2

  run:
    - python
    - byteplay >=0.2

test:
  # Python imports
  imports:
    - cpmoptimize

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/borzunov/cpmoptimize
  license: MIT
  summary: 'Library for automatic algorithms optimization via fast matrix exponentiation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
