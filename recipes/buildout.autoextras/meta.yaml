package:
  name: buildout.autoextras
  version: "1.1"

source:
  fn: buildout.autoextras-1.1.tar.gz
  url: https://pypi.python.org/packages/source/b/buildout.autoextras/buildout.autoextras-1.1.tar.gz
  md5: 5a39c65ff1e8f710c825650660b40a4d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - buildout.autoextras = buildout.autoextras:main
    #
    # Would create an entry point called buildout.autoextras that calls buildout.autoextras.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - zc.recipe.egg

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - zc.recipe.egg

test:
  # Python imports
  imports:
    - buildout
    - buildout.autoextras

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/pumazi/buildout.autoextras
  license: GPL
  summary: 'zc.buildout extension to automatically include requirement extras'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
