package:
  name: cubicweb-frbr
  version: "0.1.1"

source:
  fn: cubicweb-frbr-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/c/cubicweb-frbr/cubicweb-frbr-0.1.1.tar.gz
  md5: 1abea3ad9f4a449bc3ee7682da11e008
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cubicweb-frbr = cubicweb-frbr:main
    #
    # Would create an entry point called cubicweb-frbr that calls cubicweb-frbr.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cubicweb >=3.18.4
    - cubicweb-dataio >=0.5.1

  run:
    - python
    - cubicweb >=3.18.4
    - cubicweb-dataio >=0.5.1

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.cubicweb.org/project/cubicweb-frbr
  license: LGPL
  summary: 'Cube for FRBR vocabulary (works, expressions, manifestations, ...)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
