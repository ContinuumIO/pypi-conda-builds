package:
  name: sqlite3paramstyle
  version: "0.0.1"

source:
  fn: sqlite3paramstyle-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/s/sqlite3paramstyle/sqlite3paramstyle-0.0.1.tar.gz
  md5: f74f5869f8962dc19b8e258306938434
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sqlite3paramstyle = sqlite3paramstyle:main
    #
    # Would create an entry point called sqlite3paramstyle that calls sqlite3paramstyle.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - sqlite3paramstyle

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/shrkw/sqlite3_paramstyle
  license: MIT License
  summary: 'Extended paramstyle for sqlite3 module'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
