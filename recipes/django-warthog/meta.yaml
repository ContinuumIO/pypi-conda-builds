package:
  name: django-warthog
  version: "0.4.0.alpha"

source:
  fn: django-warthog-0.4.0.alpha.tar.gz
  url: https://pypi.python.org/packages/source/d/django-warthog/django-warthog-0.4.0.alpha.tar.gz
  md5: aad15720aacafdec8944c25a9adfb20c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-warthog = django-warthog:main
    #
    # Would create an entry point called django-warthog that calls django-warthog.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4

  run:
    - python
    - django >=1.4

test:
  # Python imports
  imports:
    - warthog
    - warthog.admin
    - warthog.migrations
    - warthog.resource_types
    - warthog.templatetags
    - warthog.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD License
  summary: 'Embeddable CMS for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
