package:
  name: django_navigation_manager
  version: "0.0.13"

source:
  fn: django_navigation_manager-0.0.13.tar.gz
  url: https://pypi.python.org/packages/source/d/django_navigation_manager/django_navigation_manager-0.0.13.tar.gz
  md5: ad4fa18c905c18f1a4e02845098d9084
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_navigation_manager = django_navigation_manager:main
    #
    # Would create an entry point called django_navigation_manager that calls django_navigation_manager.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_navigation_manager
    - django_navigation_manager.lib
    - django_navigation_manager.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: projects.airtonix.net/project/django-navigation-manager
  license: LICENSE.md
  summary: 'Django application that helps other applications declare navigation bar items.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
