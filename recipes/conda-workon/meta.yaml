package:
  name: conda-workon
  version: "0.3.0"

source:
  fn: conda-workon-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/c/conda-workon/conda-workon-0.3.0.tar.gz
  md5: 722490f35979474296f01b4e0c1f105f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  # preserve_egg_dir: True
  entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - conda-workon = conda-workon:main
    #
    # Would create an entry point called conda-workon that calls conda-workon.main()

    - conda-workon = conda_workon:main

  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - conda

  run:
    - python
    - conda

test:
  # Python imports
  # imports:

  commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.

    - conda-workon --help

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/flub/conda-workon
  license: BSD
  summary: 'Activate conda environments in subshells'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
