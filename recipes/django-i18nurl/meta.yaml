package:
  name: django-i18nurl
  version: "0.1"

source:
  fn: django-i18nurl-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-i18nurl/django-i18nurl-0.1.tar.gz
  md5: 3eee9909b5e01041d04dd875930c7e28
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-i18nurl = django-i18nurl:main
    #
    # Would create an entry point called django-i18nurl that calls django-i18nurl.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - i18nurl
    - i18nurl.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/novagile/django-i18nurl
  license: BSD License
  summary: u'Django i18n URL'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
