package:
  name: bcbio-nextgen
  version: "0.8.8"

source:
  fn: bcbio-nextgen-0.8.8.tar.gz
  url: https://pypi.python.org/packages/source/b/bcbio-nextgen/bcbio-nextgen-0.8.8.tar.gz
  md5: 2bbe6bf3a54586e32070fdcd28c530df
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bcbio-nextgen = bcbio-nextgen:main
    #
    # Would create an entry point called bcbio-nextgen that calls bcbio-nextgen.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - biopython >=1.61
    - boto >=2.34.0
    - cython >=0.19
    - ipython >=2.0.0
    - ipython-cluster-helper >=0.4.1
    - logbook >=0.6.0
    - lxml >=3.3.5
    - msgpack-python >=0.4.0
    - nose >=1.3.0
    - pandas >=0.14.1
    - progressbar >=2.3.0
    - pybedtools >=0.6.8
    - pysam >=0.8.2.1
    - pyzmq >=2.2.0
    - pyyaml >=3.10
    - toolz >=0.7.1
    - tornado >=3.1.0
    - arrow >=0.4.4
    - arvados-python-client >=0.1.1405019228.4a7ca04
    - awscli >=1.7.14
    - bioblend >=0.4.3
    - chanjo >=2.0.2
    - click >=3.3
    - cnvkit >=0.3.4
    - cutadapt ==1.4.2
    - cyvcf >=0.1.11
    - fabric >=1.7.0
    - gffutils >=0.8.2
    - htseq >=0.5.4p5
    - joblib >=0.7.0d
    - openpyxl >=1.6.1,<2.0.0
    - path.py >=7.0
    - psutil >=1.2.0
    - pythonpy >=0.3.6
    - python-dateutil >=2.1
    - pyvcf >=0.6.7
    - sh >=1.07
    - tabulate >=0.7.3
    - scikit-learn >=0.15.2
    - seaborn >=0.5.0

  run:
    - python
    - biopython >=1.61
    - boto >=2.34.0
    - cython >=0.19
    - ipython >=2.0.0
    - ipython-cluster-helper >=0.4.1
    - logbook >=0.6.0
    - lxml >=3.3.5
    - msgpack-python >=0.4.0
    - nose >=1.3.0
    - pandas >=0.14.1
    - progressbar >=2.3.0
    - pybedtools >=0.6.8
    - pysam >=0.8.2.1
    - pyzmq >=2.2.0
    - pyyaml >=3.10
    - toolz >=0.7.1
    - tornado >=3.1.0
    - arrow >=0.4.4
    - arvados-python-client >=0.1.1405019228.4a7ca04
    - awscli >=1.7.14
    - bioblend >=0.4.3
    - chanjo >=2.0.2
    - click >=3.3
    - cnvkit >=0.3.4
    - cutadapt ==1.4.2
    - cyvcf >=0.1.11
    - fabric >=1.7.0
    - gffutils >=0.8.2
    - htseq >=0.5.4p5
    - joblib >=0.7.0d
    - openpyxl >=1.6.1,<2.0.0
    - path.py >=7.0
    - psutil >=1.2.0
    - pythonpy >=0.3.6
    - python-dateutil >=2.1
    - pyvcf >=0.6.7
    - sh >=1.07
    - tabulate >=0.7.3
    - scikit-learn >=0.15.2
    - seaborn >=0.5.0

test:
  # Python imports
  imports:
    - bcbio
    - bcbio.bam
    - bcbio.bed
    - bcbio.broad
    - bcbio.chipseq
    - bcbio.distributed
    - bcbio.galaxy
    - bcbio.graph
    - bcbio.hmmer
    - bcbio.illumina
    - bcbio.log
    - bcbio.ngsalign
    - bcbio.picard
    - bcbio.pipeline
    - bcbio.pipeline.disambiguate
    - bcbio.provenance
    - bcbio.rnaseq
    - bcbio.server
    - bcbio.structural
    - bcbio.upload
    - bcbio.variation
    - bcbio.workflow

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/chapmanb/bcbio-nextgen
  license: MIT
  summary: 'Best-practice pipelines for fully automated high throughput sequencing analysis'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
