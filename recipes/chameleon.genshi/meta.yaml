package:
  name: chameleon.genshi
  version: "1.0b4"

source:
  fn: chameleon.genshi-1.0b4.tar.gz
  url: https://pypi.python.org/packages/source/c/chameleon.genshi/chameleon.genshi-1.0b4.tar.gz
  md5: da4ecd0ac6ce0efb44ce8caa4eba551c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - chameleon.genshi = chameleon.genshi:main
    #
    # Would create an entry point called chameleon.genshi that calls chameleon.genshi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.interface
    - zope.component
    - zope.i18n >=3.5
    - chameleon.core >=1.0b21

  run:
    - python
    - setuptools
    - zope.interface
    - zope.component
    - zope.i18n >=3.5
    - chameleon.core >=1.0b21

test:
  # Python imports
  imports:
    - chameleon
    - chameleon.genshi
    - chameleon.genshi.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: BSD
  summary: 'Genshi template engine based on Chameleon'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
