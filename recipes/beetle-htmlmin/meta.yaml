package:
  name: beetle-htmlmin
  version: "1.0.0"

source:
  fn: Beetle-Htmlmin-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/B/Beetle-Htmlmin/Beetle-Htmlmin-1.0.0.tar.gz
  md5: 217fcfedef5291a7384b316d50f48f3e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - beetle-htmlmin = beetle-htmlmin:main
    #
    # Would create an entry point called beetle-htmlmin that calls beetle-htmlmin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - htmlmin

  run:
    - python
    - htmlmin

test:
  # Python imports
  imports:
    - beetle_htmlmin

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Tenzer/beetle-htmlmin
  license: MIT License
  summary: 'Enables Beetle to minimize HTML files'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
