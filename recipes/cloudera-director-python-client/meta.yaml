package:
  name: cloudera-director-python-client
  version: "1.1.0"

source:
  fn: cloudera-director-python-client-1.1.0.tar.gz
  url: https://pypi.python.org/packages/source/c/cloudera-director-python-client/cloudera-director-python-client-1.1.0.tar.gz
  md5: 7d9a6ee60ea2f828245e664e7f58300b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cloudera-director-python-client = cloudera-director-python-client:main
    #
    # Would create an entry point called cloudera-director-python-client that calls cloudera-director-python-client.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - cloudera
    - cloudera.director
    - cloudera.director.latest
    - cloudera.director.latest.models
    - cloudera.director.v1
    - cloudera.director.v1.models
    - cloudera.director.v2
    - cloudera.director.v2.models

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Apache License 2.0
  summary: "Python client for Cloudera Director's REST API"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
