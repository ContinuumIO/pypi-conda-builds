package:
  name: djangocms-page-meta
  version: "0.4.2.b1"

source:
  fn: djangocms-page-meta-0.4.2.b1.tar.gz
  url: https://pypi.python.org/packages/source/d/djangocms-page-meta/djangocms-page-meta-0.4.2.b1.tar.gz
  md5: 0d8581d5463f5ea4fdb4fd6c3bf9bfe3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangocms-page-meta = djangocms-page-meta:main
    #
    # Would create an entry point called djangocms-page-meta that calls djangocms-page-meta.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-cms >=3.0
    - django-meta >=0.1.0
    - django-filer >=0.9.5
    - django-meta-mixin

  run:
    - python
    - django-cms >=3.0
    - django-meta >=0.1.0
    - django-filer >=0.9.5
    - django-meta-mixin

test:
  # Python imports
  imports:
    - djangocms_page_meta

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/nephila/djangocms-page-meta
  license: BSD License
  summary: 'OpenGraph, Twitter Card and Google+ snippet tags for django CMS 3 pages'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
