package:
  name: pontoon
  version: "0.1.4"

source:
  fn: pontoon-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/p/pontoon/pontoon-0.1.4.tar.gz
  md5: 8695685620e99f1b300a25a6e9d9117a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pontoon = pontoon:main
    #
    # Would create an entry point called pontoon that calls pontoon.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - docopt >=0.6.0
    - pyyaml
    - requests

  run:
    - python
    - docopt >=0.6.0
    - pyyaml
    - requests

test:
  # Python imports
  imports:
    - pontoon

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/duggan/pontoon
  license: MIT License
  summary: 'A Python CLI for Digital Ocean'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
