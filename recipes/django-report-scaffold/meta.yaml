package:
  name: django-report-scaffold
  version: "0.1.4"

source:
  fn: django-report-scaffold-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-report-scaffold/django-report-scaffold-0.1.4.tar.gz
  md5: 348162fb617271372ac1dcadbed5172b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-report-scaffold = django-report-scaffold:main
    #
    # Would create an entry point called django-report-scaffold that calls django-report-scaffold.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - django-report-utils
    - django-widget-tweaks

  run:
    - python
    - django
    - django-report-utils
    - django-widget-tweaks

test:
  # Python imports
  imports:
    - scaffold_report

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/burke-software/django-report-scaffold
  license: BSD License
  summary: 'Create streamlined and flexible reporting tools for your end uesrs. Report scaffold is not a drop in application but a framework for creating reporting tools. Think of it like django admin.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
