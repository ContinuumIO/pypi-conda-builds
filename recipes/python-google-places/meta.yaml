package:
  name: python-google-places
  version: "1.0.0"

source:
  fn: python-google-places-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/p/python-google-places/python-google-places-1.0.0.tar.gz
  md5: 12004764b19077f26f944d5dc7fce8f9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-google-places = python-google-places:main
    #
    # Would create an entry point called python-google-places that calls python-google-places.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - googleplaces

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/slimkrazy/python-google-places
  license: UNKNOWN
  summary: 'A simple wrapper around the Google Places API.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
