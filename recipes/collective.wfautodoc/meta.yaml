package:
  name: collective.wfautodoc
  version: "1.1"

source:
  fn: collective.wfautodoc-1.1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.wfautodoc/collective.wfautodoc-1.1.tar.gz
  md5: a5a9cffdeb2803971a665303f49619cc
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.wfautodoc = collective.wfautodoc:main
    #
    # Would create an entry point called collective.wfautodoc that calls collective.wfautodoc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - pygraphviz
    - products.cmfcore
    - products.dcworkflow

  run:
    - python
    - setuptools
    - setuptools
    - pygraphviz
    - products.cmfcore
    - products.dcworkflow

test:
  # Python imports
  imports:
    - collective
    - collective.wfautodoc

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/collective.wfautodoc
  license: GNU General Public License (GPL)
  summary: 'Workflow Automatic Documentation (Zope/CMF)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
