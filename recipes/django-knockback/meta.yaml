package:
  name: django-knockback
  version: "0.1.3"

source:
  fn: django-knockback-0.1.3.zip
  url: https://pypi.python.org/packages/source/d/django-knockback/django-knockback-0.1.3.zip
  md5: ad7083b88c6a5f062fca9dd36c070132
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-knockback = django-knockback:main
    #
    # Would create an entry point called django-knockback that calls django-knockback.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3
    - django_compress

  run:
    - python
    - django >=1.3
    - django_compress

test:
  # Python imports
  imports:
    - knockback

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/thedrow/django-knockback/
  license: BSD
  summary: 'A django app that contains the necessary javascript files to use knockback.js'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
