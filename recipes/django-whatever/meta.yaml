package:
  name: django-whatever
  version: "0.2.3"

source:
  fn: django-whatever-0.2.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-whatever/django-whatever-0.2.3.tar.gz
  md5: c0d334443ea135586df458b6df5c9c98
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-whatever = django-whatever:main
    #
    # Would create an entry point called django-whatever that calls django-whatever.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_any
    - django_any.contrib
    - django_any.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/coagulant/django-whatever
  license: MIT License
  summary: 'Unobtrusive test models creation for django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
