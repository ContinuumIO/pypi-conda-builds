package:
  name: collective.threecolorstheme
  version: "0.1"

source:
  fn: collective.threecolorstheme-0.1.zip
  url: https://pypi.python.org/packages/source/c/collective.threecolorstheme/collective.threecolorstheme-0.1.zip
  md5: 42800bc6c6d30ab40a4150a7cf5d66e1
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.threecolorstheme = collective.threecolorstheme:main
    #
    # Would create an entry point called collective.threecolorstheme that calls collective.threecolorstheme.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.phantasy

  run:
    - python
    - setuptools
    - setuptools
    - collective.phantasy

test:
  # Python imports
  imports:
    - collective
    - collective.threecolorstheme
    - collective.threecolorstheme.browser
    - collective.threecolorstheme.content

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/products/collective-phantasy
  license: GPL
  summary: 'A Phantasy theme variation for Plone, with 3 dynamic colors'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
