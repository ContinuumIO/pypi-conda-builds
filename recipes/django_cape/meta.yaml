package:
  name: django_cape
  version: "0.2"

source:
  fn: django_cape-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django_cape/django_cape-0.2.tar.gz
  md5: 82cdbcd6be6bf9496aac52ddf9b7f216
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_cape = django_cape:main
    #
    # Would create an entry point called django_cape that calls django_cape.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6.0

  run:
    - python
    - django >=1.6.0

test:
  # Python imports
  imports:
    - django_cape

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/devfort/django-cape
  license: MIT License
  summary: 'Django app that provides CAPE templates for progressive enhancement.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
