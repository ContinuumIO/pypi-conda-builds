package:
  name: ipnext.site.editablefooter
  version: "0.1.9"

source:
  fn: ipnext.site.editablefooter-0.1.9.tar.gz
  url: https://pypi.python.org/packages/source/i/ipnext.site.editablefooter/ipnext.site.editablefooter-0.1.9.tar.gz
  md5: a538d59ebe2dcd686789e794a3a62824
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ipnext.site.editablefooter = ipnext.site.editablefooter:main
    #
    # Would create an entry point called ipnext.site.editablefooter that calls ipnext.site.editablefooter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.textfield
    - plone.app.registry
    - plone.dexterity

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.textfield
    - plone.app.registry
    - plone.dexterity

test:
  # Python imports
  imports:
    - ipnext
    - ipnext.site
    - ipnext.site.editablefooter
    - ipnext.site.editablefooter.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/IPnext/
  license: GPL
  summary: 'Edit the footer in the Plone Control Panel'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
