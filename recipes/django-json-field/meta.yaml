package:
  name: django-json-field
  version: "0.5.7"

source:
  fn: django-json-field-0.5.7.tar.gz
  url: https://pypi.python.org/packages/source/d/django-json-field/django-json-field-0.5.7.tar.gz
  md5: a75ac07821fbba975a5ce0f8e4eadb53
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-json-field = django-json-field:main
    #
    # Would create an entry point called django-json-field that calls django-json-field.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.2.7
    - python-dateutil
    - six >=1.2.0

  run:
    - python
    - django >=1.2.7
    - python-dateutil
    - six >=1.2.0

test:
  # Python imports
  imports:
    - json_field

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/derek-schaefer/django-json-field
  license: BSD License
  summary: 'Generic JSON model and form fields.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
