package:
  name: doctestall
  version: "1.0.4"

source:
  fn: doctestall-1.0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/doctestall/doctestall-1.0.4.tar.gz
  md5: 7a467a040220e218b44977e69f372a81
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - doctestall = doctestall:main
    #
    # Would create an entry point called doctestall that calls doctestall.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - argparse

  run:
    - python
    - argparse

test:
  # Python imports
  imports:
    - doctestall

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/campadrenalin/DoctestAll
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Recursive tester for doctest-y projects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
