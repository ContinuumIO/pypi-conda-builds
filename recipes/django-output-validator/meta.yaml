package:
  name: django-output-validator
  version: "1.5"

source:
  fn: django-output-validator-1.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-output-validator/django-output-validator-1.5.tar.gz
  md5: b7d19e2bcb50b8dc076c6e8259150f57
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-output-validator = django-output-validator:main
    #
    # Would create an entry point called django-output-validator that calls django-output-validator.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - output_validator
    - output_validator.migrations
    - output_validator.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://lukeplant.me.uk/resources/djangovalidator/
  license: MIT License
  summary: 'App to catch HTML errors (or other errors) in outgoing Django pages.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
