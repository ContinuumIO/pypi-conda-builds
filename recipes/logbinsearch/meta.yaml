package:
  name: logbinsearch
  version: "0.0.8"

source:
  fn: logbinsearch-0.0.8.tar.gz
  url: https://pypi.python.org/packages/source/l/logbinsearch/logbinsearch-0.0.8.tar.gz
  md5: d28d7e296a4702d012f1a8e1168e8f62
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - logbinsearch = logbinsearch:main
    #
    # Would create an entry point called logbinsearch that calls logbinsearch.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - logbinsearch
    - logbinsearch.scripts
    - logbinsearch.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/takada-at/logbinsearch
  license: UNKNOWN
  summary: 'search (bz2)log file with binary search'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
