package:
  name: products.simplecalendarportlet
  version: "1.0b2"

source:
  fn: Products.SimpleCalendarPortlet-1.0b2.tar.gz
  url: https://pypi.python.org/packages/source/P/Products.SimpleCalendarPortlet/Products.SimpleCalendarPortlet-1.0b2.tar.gz
  md5: d95656b2c52795b3e425d2a8b61d523b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - products.simplecalendarportlet = products.simplecalendarportlet:main
    #
    # Would create an entry point called products.simplecalendarportlet that calls products.simplecalendarportlet.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.simplecalendar

  run:
    - python
    - setuptools
    - setuptools
    - products.simplecalendar

test:
  # Python imports
  imports:
    - Products
    - Products.SimpleCalendarPortlet

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.nidelven-it.no/d
  license: GPL
  summary: 'Simple to install and use portlet calendaring solution'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
