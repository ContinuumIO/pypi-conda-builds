package:
  name: django-asutheme
  version: "0.2.1"

source:
  fn: django-asutheme-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-asutheme/django-asutheme-0.2.1.tar.gz
  md5: 9bfa19cd17bce721c5c8f0362e5e8fc2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-asutheme = django-asutheme:main
    #
    # Would create an entry point called django-asutheme that calls django-asutheme.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - asutheme

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/asulibraries/django-asutheme
  license: Apache 2.0
  summary: 'Base templates for Django that adhere to ASU Web Standards.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
