package:
  name: django-deletion-side-effects
  version: "0.1.1"

source:
  fn: django-deletion-side-effects-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-deletion-side-effects/django-deletion-side-effects-0.1.1.tar.gz
  md5: c2226be2a741a4f08f934d090fb93d40
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-deletion-side-effects = django-deletion-side-effects:main
    #
    # Would create an entry point called django-deletion-side-effects that calls django-deletion-side-effects.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.7

  run:
    - python
    - django >=1.7

test:
  # Python imports
  imports:
    - deletion_side_effects
    - deletion_side_effects.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ambitioninc/django-deletion-side-effects
  license: MIT License
  summary: 'Provides the ability to retrieve the side effects of deleting an object in Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
