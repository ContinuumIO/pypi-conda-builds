package:
  name: hangul.translit
  version: "0.1.1"

source:
  fn: hangul.translit-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/h/hangul.translit/hangul.translit-0.1.1.tar.gz
  md5: 8aa1d869b57e8f4bbbe266aa76765f5b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hangul.translit = hangul.translit:main
    #
    # Would create an entry point called hangul.translit that calls hangul.translit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - hangul

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD
  summary: 'Transliteration of Hangul(Korean) to Roman writing system following Revised Romanization of Korean(2000).'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
