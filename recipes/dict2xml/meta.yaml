package:
  name: dict2xml
  version: "1.3"

source:
  fn: dict2xml-1.3.tar.gz
  url: https://pypi.python.org/packages/source/d/dict2xml/dict2xml-1.3.tar.gz
  md5: 4cd88996396977782007e484c84c106b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dict2xml = dict2xml:main
    #
    # Would create an entry point called dict2xml that calls dict2xml.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six

  run:
    - python
    - six

test:
  # Python imports
  imports:
    - dict2xml

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: WTFPL
  summary: 'small script to output xml as a string from a python dictionary'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
