package:
  name: collective.contentrules.yearmonth
  version: "1.1"

source:
  fn: collective.contentrules.yearmonth-1.1.zip
  url: https://pypi.python.org/packages/source/c/collective.contentrules.yearmonth/collective.contentrules.yearmonth-1.1.zip
  md5: 2f7acade202ac66cc9d264e304bcd115
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.contentrules.yearmonth = collective.contentrules.yearmonth:main
    #
    # Would create an entry point called collective.contentrules.yearmonth that calls collective.contentrules.yearmonth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.api

  run:
    - python
    - setuptools
    - setuptools
    - plone.api

test:
  # Python imports
  imports:
    - collective
    - collective.contentrules
    - collective.contentrules.yearmonth
    - collective.contentrules.yearmonth.actions
    - collective.contentrules.yearmonth.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://redomino.com
  license: GPL
  summary: 'Moves contents to a choosen directory/YEAR/MONTH'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
