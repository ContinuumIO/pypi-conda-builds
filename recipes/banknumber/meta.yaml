package:
  name: banknumber
  version: "1.0"

source:
  fn: banknumber-1.0.tar.gz
  url: https://pypi.python.org/packages/source/b/banknumber/banknumber-1.0.tar.gz
  md5: 0c77231623040ded7624b00d8ebb68f1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - banknumber = banknumber:main
    #
    # Would create an entry point called banknumber that calls banknumber.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - banknumber

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.zikzakmedia.com/
  license: GNU General Public License (GPL)
  summary: 'Python module to validate Bank codes'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
