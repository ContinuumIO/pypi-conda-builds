package:
  name: uforge_python_sdk
  version: "3.5.1"

source:
  fn: uforge_python_sdk-3.5.1.tar.gz
  url: https://pypi.python.org/packages/source/u/uforge_python_sdk/uforge_python_sdk-3.5.1.tar.gz
  md5: 405d5ff5dfa3e24a880e25c705638800
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - uforge_python_sdk = uforge_python_sdk:main
    #
    # Would create an entry point called uforge_python_sdk that calls uforge_python_sdk.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - lxml ==3.3.5

  run:
    - python
    - lxml ==3.3.5

test:
  # Python imports
  imports:
    - uforge
    - uforge.objects
    - uforge_python_sdk

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://hammr.io
  license: Apache Software License
  summary: 'UForge python SDK'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
