package:
  name: authcode
  version: "1.2.2"

source:
  fn: Authcode-1.2.2.tar.gz
  url: https://pypi.python.org/packages/source/A/Authcode/Authcode-1.2.2.tar.gz
  md5: 43bee2ccce6564995c7da1542e5c4b6e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - authcode = authcode:main
    #
    # Would create an entry point called authcode that calls authcode.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - passlib >=1.6.1
    - sqlalchemy >=0.8.2

  run:
    - python
    - passlib >=1.6.1
    - sqlalchemy >=0.8.2

test:
  # Python imports
  imports:
    - authcode
    - authcode.setups
    - authcode.wsgi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/lucuma/authcode
  license: MIT License
  summary: 'Awesome authentication code'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
