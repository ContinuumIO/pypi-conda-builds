package:
  name: cubicweb-massmailing
  version: "0.1.0"

source:
  fn: cubicweb-massmailing-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/c/cubicweb-massmailing/cubicweb-massmailing-0.1.0.tar.gz
  md5: b2c0be83149a92eb9bae1b3006d6df52
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cubicweb-massmailing = cubicweb-massmailing:main
    #
    # Would create an entry point called cubicweb-massmailing that calls cubicweb-massmailing.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cubicweb >=3.19.0

  run:
    - python
    - cubicweb >=3.19.0

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.cubicweb.org/project/cubicweb-massmailing
  license: LGPL
  summary: 'Mass mailing handling: send mail to entities adaptable to IEmailable'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
