package:
  name: easywatch
  version: "0.0.3"

source:
  fn: easywatch-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/e/easywatch/easywatch-0.0.3.tar.gz
  md5: c2a47b4f845fa48127121eb96957df69
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - easywatch = easywatch:main
    #
    # Would create an entry point called easywatch that calls easywatch.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - watchdog

  run:
    - python
    - watchdog

test:
  # Python imports
  imports:
    - easywatch

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Ceasar/easywatch
  license: MIT License
  summary: 'super simple directory monitoring'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
