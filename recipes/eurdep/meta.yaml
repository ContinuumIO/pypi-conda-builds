package:
  name: eurdep
  version: "0.7"

source:
  fn: eurdep-0.7.tar.gz
  url: https://pypi.python.org/packages/source/e/eurdep/eurdep-0.7.tar.gz
  md5: a7dae07e9e09237a5d7a74508fa78b63
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - eurdep = eurdep:main
    #
    # Would create an entry point called eurdep that calls eurdep.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - eurdep

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/palli/eurdep
  license: GPL
  summary: 'Parser for Eurdep messages, which are used for measuring gamma radiation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
