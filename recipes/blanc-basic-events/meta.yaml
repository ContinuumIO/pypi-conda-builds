package:
  name: blanc-basic-events
  version: "0.3.2"

source:
  fn: blanc-basic-events-0.3.2.tar.gz
  url: https://pypi.python.org/packages/source/b/blanc-basic-events/blanc-basic-events-0.3.2.tar.gz
  md5: 4065142f46541f3b6395af956c17895d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - blanc-basic-events = blanc-basic-events:main
    #
    # Would create an entry point called blanc-basic-events that calls blanc-basic-events.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - blanc-basic-assets >=0.3
    - icalendar >=3.6

  run:
    - python
    - blanc-basic-assets >=0.3
    - icalendar >=3.6

test:
  # Python imports
  imports:
    - blanc_basic_events
    - blanc_basic_events.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/blancltd/blanc-basic-events
  license: BSD License
  summary: 'Blanc Basic Events for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
