package:
  name: joelogtools
  version: "0.4.0"

source:
  fn: JoeLogTools-0.4.0.tar.gz
  url: https://pypi.python.org/packages/source/J/JoeLogTools/JoeLogTools-0.4.0.tar.gz
  md5: dfefc10ef8521da5366fa9d03564873d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - joelogtools = joelogtools:main
    #
    # Would create an entry point called joelogtools that calls joelogtools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - joelogtools

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/jmg099120/joelogtools
  license: GNU Lesser General Public License v3 (LGPLv3)
  summary: "Joseph Gordon's decorating logging metaclass."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
