package:
  name: products.watermarkimage
  version: "1.0"

source:
  fn: Products.WatermarkImage-1.0.tar.gz
  url: https://pypi.python.org/packages/source/P/Products.WatermarkImage/Products.WatermarkImage-1.0.tar.gz
  md5: 43ee4d3b5f77ecbd6736ef2ba8ecd6a6
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - products.watermarkimage = products.watermarkimage:main
    #
    # Would create an entry point called products.watermarkimage that calls products.watermarkimage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - pilwotk

  run:
    - python
    - setuptools
    - setuptools
    - pilwotk

test:
  # Python imports
  imports:
    - Products
    - Products.WatermarkImage
    - Products.WatermarkImage.browser
    - Products.WatermarkImage.browser.tests
    - Products.WatermarkImage.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/Products.WatermarkImage
  license: Zope Public License
  summary: 'Zope 2 Image with thumbnail and watermarking support'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
