package:
  name: da-vinci
  version: "0.2.1"

source:
  fn: da-vinci-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/da-vinci/da-vinci-0.2.1.tar.gz
  md5: 4e5a96ec975f11574694c03ca1c15e10
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - da-vinci = da-vinci:main
    #
    # Would create an entry point called da-vinci that calls da-vinci.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - da_vinci

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ui/da-vinci
  license: MIT License
  summary: 'A simple image manipulation library aiming to make common image tasks easy.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
