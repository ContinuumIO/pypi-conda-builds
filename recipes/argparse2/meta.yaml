package:
  name: argparse2
  version: "0.5.0-alpha1"

source:
  fn: argparse2-0.5.0-alpha1.tar.gz
  url: https://pypi.python.org/packages/source/a/argparse2/argparse2-0.5.0-alpha1.tar.gz
  md5: a3fdbf3f43e1958fb6a38c0215fcaa59
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - argparse2 = argparse2:main
    #
    # Would create an entry point called argparse2 that calls argparse2.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - argparse2

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/cjerdonek/python-argparse
  license: Python Software Foundation License
  summary: "Fork of Python's argparse to add features and simplify its code"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
