package:
  name: aldryn-categories
  version: "0.1.2"

source:
  fn: aldryn-categories-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/a/aldryn-categories/aldryn-categories-0.1.2.tar.gz
  md5: 0cd335f0dbbcba26a74e442dc0dbff83
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - aldryn-categories = aldryn-categories:main
    #
    # Would create an entry point called aldryn-categories that calls aldryn-categories.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-parler >=1.2.1
    - django-treebeard >=2.0

  run:
    - python
    - django-parler >=1.2.1
    - django-treebeard >=2.0

test:
  # Python imports
  imports:
    - aldryn_categories
    - aldryn_categories.migrations
    - aldryn_categories.south_migrations
    - aldryn_categories.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/aldryn/aldryn-categories
  license: BSD License
  summary: 'Heirarchical categories/taxonomies for your Django project'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
