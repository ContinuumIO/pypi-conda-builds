package:
  name: wtftz
  version: "0.2.4"

source:
  fn: wtftz-0.2.4.tar.gz
  url: https://pypi.python.org/packages/source/w/wtftz/wtftz-0.2.4.tar.gz
  md5: b998fccc7b58f2f0f96f93d283d6357a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - wtftz = wtftz:main
    #
    # Would create an entry point called wtftz that calls wtftz.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python-dateutil >=1.5
    - pytz

  run:
    - python
    - python-dateutil >=1.5
    - pytz

test:
  # Python imports
  imports:
    - wtftz

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/sbuss/wtftz
  license: BSD License
  summary: 'Convert a timestamp from one timezone to another'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
