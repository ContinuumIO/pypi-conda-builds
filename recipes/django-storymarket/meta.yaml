package:
  name: django-storymarket
  version: "1.0a3"

source:
  fn: django-storymarket-1.0a3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-storymarket/django-storymarket-1.0a3.tar.gz
  md5: f38b8ae6e354c58b963d26dfb1f5f457
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-storymarket = django-storymarket:main
    #
    # Would create an entry point called django-storymarket that calls django-storymarket.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.2
    - python-storymarket

  run:
    - python
    - django >=1.2
    - python-storymarket

test:
  # Python imports
  imports:
    - django_storymarket
    - django_storymarket.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://packages.python.org/django-storymarket
  license: BSD License
  summary: 'Sync Django models to Storymarket.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
