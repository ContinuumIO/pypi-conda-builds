package:
  name: django-template-media
  version: "0.3.0"

source:
  fn: django-template-media-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-template-media/django-template-media-0.3.0.tar.gz
  md5: 97b2669e3fd1ec4fc5ba1366e695873f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-template-media = django-template-media:main
    #
    # Would create an entry point called django-template-media that calls django-template-media.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4

  run:
    - python
    - django >=1.4

test:
  # Python imports
  imports:
    - django_template_media
    - django_template_media.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/tim_heap/django-template-media
  license: UNKNOWN
  summary: 'Simple tools to extend the django admin site'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
