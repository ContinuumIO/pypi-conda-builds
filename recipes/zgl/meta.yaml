package:
  name: zgl
  version: "0.1.4"

source:
  fn: zgl-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/z/zgl/zgl-0.1.4.tar.gz
  md5: 7cbb87135e5ba8f3a0752afdaeebea0e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zgl = zgl:main
    #
    # Would create an entry point called zgl that calls zgl.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sippy-ftgl >=0.1.0
    - pil
    - numpy
    - pyopengl

  run:
    - python
    - sippy-ftgl >=0.1.0
    - pil
    - numpy
    - pyopengl

test:
  # Python imports
  imports:
    - zgl
    - zgl_graphdrawer
    - zgl_graphdrawer.application
    - zgl_graphdrawer.canvas

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: contact developer
  summary: 'OpenGL graph drawing library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
