package:
  name: auth_tkt
  version: "0.1.0"

source:
  fn: auth_tkt-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/a/auth_tkt/auth_tkt-0.1.0.tar.gz
  md5: d4a5ea003ac43001c966c6b703b1e224
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - auth_tkt = auth_tkt:main
    #
    # Would create an entry point called auth_tkt that calls auth_tkt.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - m2crypto <1.0.0

  run:
    - python
    - m2crypto <1.0.0

test:
  # Python imports
  imports:
    - auth_tkt

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/yola/auth_tkt
  license: MIT (Expat)
  summary: 'Python implementation of mod_auth_tkt cookies'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
