package:
  name: hydedown
  version: "0.1.1"

source:
  fn: hydedown-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/h/hydedown/hydedown-0.1.1.tar.gz
  md5: 5c9bb6d255522bfd978dd5effc5c7ae5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hydedown = hydedown:main
    #
    # Would create an entry point called hydedown that calls hydedown.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - markdown ==2.3.1

  run:
    - python
    - markdown ==2.3.1

test:
  # Python imports
  imports:
    - hydedown
    - hydedown.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/hyde/hydedown
  license: MIT License
  summary: 'Markdown extensions that facilitate hyde website generation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
