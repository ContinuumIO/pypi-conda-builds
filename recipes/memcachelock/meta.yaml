package:
  name: memcachelock
  version: "0.4"

source:
  fn: memcachelock-0.4.tar.gz
  url: https://pypi.python.org/packages/source/m/memcachelock/memcachelock-0.4.tar.gz
  md5: 56cfb5ebfd019e5ba03744fe46e34c5c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - memcachelock = memcachelock:main
    #
    # Would create an entry point called memcachelock that calls memcachelock.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python-memcached

  run:
    - python
    - setuptools
    - python-memcached

test:
  # Python imports
  imports:
    - memcachelock

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/vpelletier/python-memcachelock
  license: GNU General Public License (GPL)
  summary: 'Use memcache as a lock server'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
