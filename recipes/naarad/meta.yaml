package:
  name: naarad
  version: "1.0.15"

source:
  fn: naarad-1.0.15.tar.gz
  url: https://pypi.python.org/packages/source/n/naarad/naarad-1.0.15.tar.gz
  md5: 26a88953474af68f3eec895480cd3d74
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - naarad = naarad:main
    #
    # Would create an entry point called naarad that calls naarad.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy >=1.6.2
    - argparse
    - pytz >=2012c
    - jinja2
    - luminol

  run:
    - python
    - numpy >=1.6.2
    - argparse
    - pytz >=2012c
    - jinja2
    - luminol

test:
  # Python imports
  imports:
    - naarad
    - naarad.graphing
    - naarad.metrics
    - naarad.reporting
    - naarad.resources
    - naarad.run_steps

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/linkedin/naarad
  license: Apache 2.0
  summary: 'Naarad is a Performance Analysis tool'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
