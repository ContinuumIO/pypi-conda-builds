package:
  name: timeduration
  version: "0.2a"

source:
  fn: TimeDuration-0.2a.tar.gz
  url: https://pypi.python.org/packages/source/T/TimeDuration/TimeDuration-0.2a.tar.gz
  md5: ddf73d42edc6a7efae97e1e5b81dada4
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - timeduration = timeduration:main
    #
    # Would create an entry point called timeduration that calls timeduration.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - TimeDuration
    - timeduration

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://statz.com/libs-TimeDuration
  license: GNU General Public License (GPL)
  summary: 'TimeDuration is a module that deals with stopwatch time rather than wallclock time'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
