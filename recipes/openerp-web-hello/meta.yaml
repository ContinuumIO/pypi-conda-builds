package:
  name: openerp-web-hello
  version: "7.0.406"

source:
  fn: openerp-web-hello-7.0.406.tar.gz
  url: https://pypi.python.org/packages/source/o/openerp-web-hello/openerp-web-hello-7.0.406.tar.gz
  md5: 0c674203146d456fe220b379bd9e046d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - openerp-web-hello = openerp-web-hello:main
    #
    # Would create an entry point called openerp-web-hello that calls openerp-web-hello.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - openerp.addons.web_hello

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: GNU Affero General Public License v3
  summary: 'Hello'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
