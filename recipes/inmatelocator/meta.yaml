package:
  name: inmatelocator
  version: "0.0.7"

source:
  fn: inmatelocator-0.0.7.tar.gz
  url: https://pypi.python.org/packages/source/i/inmatelocator/inmatelocator-0.0.7.tar.gz
  md5: e28a9d1812d1ea1f3e9afbcbc38e8d07
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - inmatelocator = inmatelocator:main
    #
    # Would create an entry point called inmatelocator that calls inmatelocator.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests
    - addresscleaner
    - lxml
    - fuzzywuzzy
    - python-levenshtein

  run:
    - python
    - requests
    - addresscleaner
    - lxml
    - fuzzywuzzy
    - python-levenshtein

test:
  # Python imports
  imports:
    - inmatelocator
    - inmatelocator.stateparsers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/yourcelf/inmatelocator
  license: MPL
  summary: 'Library for locating people incarcerated in the US by querying official prison search tools'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
