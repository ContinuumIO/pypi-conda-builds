package:
  name: eventfabric
  version: "0.1.0"

source:
  fn: eventfabric-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/e/eventfabric/eventfabric-0.1.0.tar.gz
  md5: 504b551c906336ea3e5052f7ecdc403c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - eventfabric = eventfabric:main
    #
    # Would create an entry point called eventfabric that calls eventfabric.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - eventfabric

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/EventFabric/python-api
  license: MIT License
  summary: 'Event Fabric API client library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
