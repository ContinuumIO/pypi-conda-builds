package:
  name: collective.geo.geographer
  version: "2.0"

source:
  fn: collective.geo.geographer-2.0.zip
  url: https://pypi.python.org/packages/source/c/collective.geo.geographer/collective.geo.geographer-2.0.zip
  md5: afb00f5413edcd740f0ff0721140534e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.geo.geographer = collective.geo.geographer:main
    #
    # Would create an entry point called collective.geo.geographer that calls collective.geo.geographer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.cmfcore

  run:
    - python
    - setuptools
    - setuptools
    - products.cmfcore

test:
  # Python imports
  imports:
    - collective
    - collective.geo
    - collective.geo.geographer
    - collective.geo.geographer.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/collective.geo
  license: GPL
  summary: 'Geographic annotation for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
