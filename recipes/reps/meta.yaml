package:
  name: reps
  version: "0.4.1"

source:
  fn: reps-0.4.1.tar.gz
  url: https://pypi.python.org/packages/source/r/reps/reps-0.4.1.tar.gz
  md5: 7a4d8e3136e4d79ca2f8a9d947605ee0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - reps = reps:main
    #
    # Would create an entry point called reps that calls reps.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - ansicolor
    - ordereddict

  run:
    - python
    - ansicolor
    - ordereddict

test:
  # Python imports
  imports:
    - reps
    - reps.backends
    - reps.model

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/numerodix/re
  license: GNU General Public License v2 (GPLv2)
  summary: 'Multiple repository management tool'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
