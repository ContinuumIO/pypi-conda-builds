package:
  name: compare_word
  version: "0.0.1"

source:
  fn: compare_word-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/compare_word/compare_word-0.0.1.tar.gz
  md5: 0c33e23451f16fd96ae76b013a8a4d39
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - compare_word = compare_word:main
    #
    # Would create an entry point called compare_word that calls compare_word.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - compare_word

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/mvj3/compare_word/
  license: MIT
  summary: 'compare_word'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
