package:
  name: cubicweb-slickgrid
  version: "1.0.0"

source:
  fn: cubicweb-slickgrid-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/c/cubicweb-slickgrid/cubicweb-slickgrid-1.0.0.tar.gz
  md5: 33a96bd5a74babb6ec0fa92fe59f78a9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cubicweb-slickgrid = cubicweb-slickgrid:main
    #
    # Would create an entry point called cubicweb-slickgrid that calls cubicweb-slickgrid.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cubicweb >=3.19.4

  run:
    - python
    - cubicweb >=3.19.4

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.cubicweb.org/project/cubicweb-slickgrid
  license: LGPL
  summary: 'Table view rendered using the SlickGrid_ javascript librairy.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
