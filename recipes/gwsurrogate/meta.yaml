package:
  name: gwsurrogate
  version: "0.2.1"

source:
  fn: gwsurrogate-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/g/gwsurrogate/gwsurrogate-0.2.1.tar.gz
  md5: 723beabfcb62184cf7d87b87676bf010
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gwsurrogate = gwsurrogate:main
    #
    # Would create an entry point called gwsurrogate that calls gwsurrogate.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - numpy
    - matplotlib

  run:
    - python
    - numpy
    - matplotlib

test:
  # Python imports
  imports:
    - gwsurrogate

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT License
  summary: 'an easy to use interface to gravitational wave surrogate models'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
