package:
  name: clicore
  version: "0.00.04"

source:
  fn: clicore-0.00.04.tar.gz
  url: https://pypi.python.org/packages/source/c/clicore/clicore-0.00.04.tar.gz
  md5: adedd6b56da1f4d303b6aa6296ecd522
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - clicore = clicore:main
    #
    # Would create an entry point called clicore that calls clicore.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - observable ==0.00.03
    - colorful ==0.00.02

  run:
    - python
    - observable ==0.00.03
    - colorful ==0.00.02

test:
  # Python imports
  imports:
    - clicore

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/timofurrer/cli
  license: UNKNOWN
  summary: 'cli core library for python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
