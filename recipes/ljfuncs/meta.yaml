package:
  name: ljfuncs
  version: "1.0.0"

source:
  fn: ljfuncs-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/l/ljfuncs/ljfuncs-1.0.0.tar.gz
  md5: 57cdb041c549c307fce1b37550e7f90f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ljfuncs = ljfuncs:main
    #
    # Would create an entry point called ljfuncs that calls ljfuncs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - ljfuncs

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.site.uottawa.ca/~mraap046
  license: Apache Software License
  summary: 'Functions for analysing friends and interests lists on Livejournal.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
