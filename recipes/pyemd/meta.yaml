package:
  name: pyemd
  version: "0.0.10"

source:
  fn: pyemd-0.0.10.tar.gz
  url: https://pypi.python.org/packages/source/p/pyemd/pyemd-0.0.10.tar.gz
  md5: f94ccd479429c1205f7f368118b1e231
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pyemd = pyemd:main
    #
    # Would create an entry point called pyemd that calls pyemd.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy >=1.8.0,<2.0.0

  run:
    - python
    - numpy >=1.8.0,<2.0.0

test:
  # Python imports
  imports:
    - pyemd

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/wmayner/pyemd
  license: MIT License
  summary: "A Python wrapper for Ofir Pele and Michael Werman's implementation of the Earth Mover's Distance."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
