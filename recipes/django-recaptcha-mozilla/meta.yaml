package:
  name: django-recaptcha-mozilla
  version: "0.0.1"

source:
  fn: django-recaptcha-mozilla-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-recaptcha-mozilla/django-recaptcha-mozilla-0.0.1.tar.gz
  md5: 4d1c26f6e691ed1979177daf9df5df4b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-recaptcha-mozilla = django-recaptcha-mozilla:main
    #
    # Would create an entry point called django-recaptcha-mozilla that calls django-recaptcha-mozilla.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - recaptcha-client

  run:
    - python
    - recaptcha-client

test:
  # Python imports
  imports:
    - captcha

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/mozilla/django-recaptcha
  license: BSD License
  summary: 'Mozilla fork of django recaptcha form field/widget app.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
