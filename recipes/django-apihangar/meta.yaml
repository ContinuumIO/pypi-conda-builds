package:
  name: django-apihangar
  version: "0.1.1"

source:
  fn: django-apihangar-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-apihangar/django-apihangar-0.1.1.tar.gz
  md5: 9371989c71132a571ccddab5c78d7541
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-apihangar = django-apihangar:main
    #
    # Would create an entry point called django-apihangar that calls django-apihangar.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-templateinspector
    - djangohelpers

  run:
    - python
    - setuptools
    - django-templateinspector
    - djangohelpers

test:
  # Python imports
  imports:
    - apihangar

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: BSD License
  summary: 'through-the-web creation of templated sql->json APIs for querying data'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
