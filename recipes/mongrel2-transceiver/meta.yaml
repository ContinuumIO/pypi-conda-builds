package:
  name: mongrel2-transceiver
  version: "0.3.0"

source:
  fn: mongrel2-transceiver-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/m/mongrel2-transceiver/mongrel2-transceiver-0.3.0.tar.gz
  md5: 52f9a3a75ebe5a7df4e8d7994d8292f0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mongrel2-transceiver = mongrel2-transceiver:main
    #
    # Would create an entry point called mongrel2-transceiver that calls mongrel2-transceiver.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - netifaces

  run:
    - python
    - netifaces

test:
  # Python imports
  imports:
    - mongrel2_transceiver

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/webheroesinc/transilience
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'Non-blocking Mongrel2 handler transceiver'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
