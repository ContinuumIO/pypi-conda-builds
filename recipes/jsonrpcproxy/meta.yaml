package:
  name: jsonrpcproxy
  version: "0.2"

source:
  fn: jsonrpcproxy-0.2.tar.gz
  url: https://pypi.python.org/packages/source/j/jsonrpcproxy/jsonrpcproxy-0.2.tar.gz
  md5: 02aee2094c1388575b6f72e84caf189f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jsonrpcproxy = jsonrpcproxy:main
    #
    # Would create an entry point called jsonrpcproxy that calls jsonrpcproxy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=1.2.3,<3.0

  run:
    - python
    - requests >=1.2.3,<3.0

test:
  # Python imports
  imports:
    - jsonrpcproxy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: BSD License
  summary: 'Damn simple JSON-RPC client'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
