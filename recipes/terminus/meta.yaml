package:
  name: terminus
  version: "0.1.2"

source:
  fn: terminus-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/t/terminus/terminus-0.1.2.tar.gz
  md5: daee783ace0e56ec388059d44b3f9461
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - terminus = terminus:main
    #
    # Would create an entry point called terminus that calls terminus.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - terminus

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/briancline/terminus
  license: MIT License
  summary: 'Updates interfaces file on Debian Linux-based distros'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
