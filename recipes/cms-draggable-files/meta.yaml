package:
  name: cms-draggable-files
  version: "0.1.6"

source:
  fn: cms-draggable-files-0.1.6.tar.gz
  url: https://pypi.python.org/packages/source/c/cms-draggable-files/cms-draggable-files-0.1.6.tar.gz
  md5: d630cc6d3c577a20fca7159069f74976
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cms-draggable-files = cms-draggable-files:main
    #
    # Would create an entry point called cms-draggable-files that calls cms-draggable-files.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - cms_draggable_files

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jelenak/cms-draggable-files
  license: LICENSE.txt
  summary: 'CMS placeholder can recieve dragged files and add to plugin by files extensions.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
