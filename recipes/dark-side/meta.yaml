package:
  name: dark-side
  version: "1.0.5"

source:
  fn: dark-side-1.0.5.tar.gz
  url: https://pypi.python.org/packages/source/d/dark-side/dark-side-1.0.5.tar.gz
  md5: b31cda69083eecd4fefbb4c1d4cf492b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dark-side = dark-side:main
    #
    # Would create an entry point called dark-side that calls dark-side.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - gevent
    - requests
    - json_tools
    - webob

  run:
    - python
    - gevent
    - requests
    - json_tools
    - webob

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/gameclosure/dark-side
  license: UNKNOWN
  summary: 'response comparing proxy server'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
