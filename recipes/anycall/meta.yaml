package:
  name: anycall
  version: "0.2.2"

source:
  fn: anycall-0.2.2.zip
  url: https://pypi.python.org/packages/source/a/anycall/anycall-0.2.2.zip
  md5: 06ad80bff9643bbd8103606d538e2b8a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - anycall = anycall:main
    #
    # Would create an entry point called anycall that calls anycall.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - twisted
    - utwist
    - bidict
    - twistit

  run:
    - python
    - twisted
    - utwist
    - bidict
    - twistit

test:
  # Python imports
  imports:
    - anycall

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/smurn/anycall
  license: UNKNOWN
  summary: 'Remote Prodecure Call library using the Twisted framework.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
