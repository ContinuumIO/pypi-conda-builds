package:
  name: akhet
  version: "2.0"

source:
  fn: Akhet-2.0.tar.gz
  url: https://pypi.python.org/packages/source/A/Akhet/Akhet-2.0.tar.gz
  md5: 30904bdf7b67f4abe0da5ebf3aa64671
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - akhet = akhet:main
    #
    # Would create an entry point called akhet that calls akhet.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyramid

  run:
    - python
    - setuptools
    - pyramid

test:
  # Python imports
  imports:
    - akhet
    - akhet.demo
    - akhet.demo.lib
    - akhet.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/sluggo/akhet
  license: MIT License
  summary: 'A Pyramid library and demo app with a Pylons-inspired API.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
