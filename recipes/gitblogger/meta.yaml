package:
  name: gitblogger
  version: "20100218.1"

source:
  fn: gitblogger-20100218.1.tar.gz
  url: https://pypi.python.org/packages/source/g/gitblogger/gitblogger-20100218.1.tar.gz
  md5: b712ecc9b3e98081c5edb649adf275ef
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gitblogger = gitblogger:main
    #
    # Would create an entry point called gitblogger that calls gitblogger.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - gitpython
    - elixir
    - sqlalchemy
    - configdict

  run:
    - python
    - gitpython
    - elixir
    - sqlalchemy
    - configdict

test:
  # Python imports
  imports:
    - gitblogger

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'Publish reStructuredText documents to Blogger from a Git repository.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
