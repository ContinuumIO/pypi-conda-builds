package:
  name: haufe.eggserver
  version: "0.2.6"

source:
  fn: haufe.eggserver-0.2.6.tar.gz
  url: https://pypi.python.org/packages/source/h/haufe.eggserver/haufe.eggserver-0.2.6.tar.gz
  md5: 7572decbd5222ce1e888e0e27f1a2d8c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - haufe.eggserver = haufe.eggserver:main
    #
    # Would create an entry point called haufe.eggserver that calls haufe.eggserver.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - grok
    - gocept.cache

  run:
    - python
    - setuptools
    - setuptools
    - grok
    - gocept.cache

test:
  # Python imports
  imports:
    - eggserver

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: ZPL 2.1
  summary: 'A small PyPI replacement based on a local distribution directory (implemented using GROK)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
