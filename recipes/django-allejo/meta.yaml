package:
  name: django-allejo
  version: "0.1.0"

source:
  fn: django-allejo-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-allejo/django-allejo-0.1.0.tar.gz
  md5: e5d0727c737664e017e85a13db94ebb8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-allejo = django-allejo:main
    #
    # Would create an entry point called django-allejo that calls django-allejo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - allejo
    - allejo.migrations
    - allejo.south_migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/lerrua/django-allejo
  license: Apache Software License
  summary: 'Management and creation for custom tournaments and rankings for many sports.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
