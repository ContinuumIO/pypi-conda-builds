package:
  name: thermocouples_reference
  version: "0.20"

source:
  fn: thermocouples_reference-0.20.tar.gz
  url: https://pypi.python.org/packages/source/t/thermocouples_reference/thermocouples_reference-0.20.tar.gz
  md5: 9c2dbe3ba24834811a936d451f254a7a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - thermocouples_reference = thermocouples_reference:main
    #
    # Would create an entry point called thermocouples_reference that calls thermocouples_reference.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy

  run:
    - python
    - numpy

test:
  # Python imports
  imports:
    - thermocouples_reference

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://pypi.python.org/pypi/thermocouples_reference
  license: public domain
  summary: 'Thermocouple emf reference functions'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
