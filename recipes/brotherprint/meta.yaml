package:
  name: brotherprint
  version: "0.1.1"

source:
  fn: brotherprint-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/b/brotherprint/brotherprint-0.1.1.tar.gz
  md5: 28917f50f0b5fb446e2341e68079ac47
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - brotherprint = brotherprint:main
    #
    # Would create an entry point called brotherprint that calls brotherprint.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - brotherprint
    - brotherprint.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/fozzle/python-brotherprint
  license: LICENSE.txt
  summary: 'Wrapper for Brother networked label printing commands.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
