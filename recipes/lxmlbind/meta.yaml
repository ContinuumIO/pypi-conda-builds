package:
  name: lxmlbind
  version: "1.2"

source:
  fn: lxmlbind-1.2.tar.gz
  url: https://pypi.python.org/packages/source/l/lxmlbind/lxmlbind-1.2.tar.gz
  md5: 46a15a06f719b6ab16b34ff6f43d3d55
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lxmlbind = lxmlbind:main
    #
    # Would create an entry point called lxmlbind that calls lxmlbind.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - lxml >=3.2.4

  run:
    - python
    - lxml >=3.2.4

test:
  # Python imports
  imports:
    - lxmlbind

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jessemyers/lxmlbind
  license: UNKNOWN
  summary: 'Python LXML object binding.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
