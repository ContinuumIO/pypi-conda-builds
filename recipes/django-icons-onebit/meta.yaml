package:
  name: django-icons-onebit
  version: "1.0"

source:
  fn: django-icons-onebit-1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-icons-onebit/django-icons-onebit-1.0.tar.gz
  md5: 77b97d12ae9d29ed0d808040b52c5f96
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-icons-onebit = django-icons-onebit:main
    #
    # Would create an entry point called django-icons-onebit that calls django-icons-onebit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - distribute

  run:
    - python
    - distribute

test:
  # Python imports
  imports:
    - icons_onebit

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/kmike/django-icons/
  license: MIT License
  summary: 'onebit icons pack for django.contrib.staticfiles'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
