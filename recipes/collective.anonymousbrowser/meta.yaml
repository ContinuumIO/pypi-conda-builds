package:
  name: collective.anonymousbrowser
  version: "0.11"

source:
  fn: collective.anonymousbrowser-0.11.zip
  url: https://pypi.python.org/packages/source/c/collective.anonymousbrowser/collective.anonymousbrowser-0.11.zip
  md5: 25796a3db8c6032add7ce309019801a9
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.anonymousbrowser = collective.anonymousbrowser:main
    #
    # Would create an entry point called collective.anonymousbrowser that calls collective.anonymousbrowser.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - lxml
    - mechanize
    - setuptools
    - mozrunner
    - zc.testbrowser
    - decorator

  run:
    - python
    - lxml
    - mechanize
    - setuptools
    - mozrunner
    - zc.testbrowser
    - decorator

test:
  # Python imports
  imports:
    - collective
    - collective.anonymousbrowser
    - collective.anonymousbrowser.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://svn.plone.org/svn/collective/collective.anonymousbrowser/trunk
  license: GPL
  summary: 'A zope.testbrowser extension with useragent faking and proxy abilitiesa sponsorised by Makina Corpus'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
