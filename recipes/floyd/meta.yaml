package:
  name: floyd
  version: "0.0.3a15"

source:
  fn: floyd-0.0.3a15.tar.gz
  url: https://pypi.python.org/packages/source/f/floyd/floyd-0.0.3a15.tar.gz
  md5: 717dac4dd936c461981d5554306c769e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - floyd = floyd:main
    #
    # Would create an entry point called floyd that calls floyd.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - jinja2
    - markdown

  run:
    - python
    - jinja2
    - markdown

test:
  # Python imports
  imports:
    - floyd

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://nikcub.github.com/floyd
  license: flshwdemo
  summary: 'Static website generator with MVC content system and built-in support for cloud hosting platforms\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
