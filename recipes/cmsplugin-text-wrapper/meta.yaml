package:
  name: cmsplugin-text-wrapper
  version: "0.6"

source:
  fn: cmsplugin-text-wrapper-0.6.tar.gz
  url: https://pypi.python.org/packages/source/c/cmsplugin-text-wrapper/cmsplugin-text-wrapper-0.6.tar.gz
  md5: 116fb74389c5b6dd4a2d4656a7e75c5e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cmsplugin-text-wrapper = cmsplugin-text-wrapper:main
    #
    # Would create an entry point called cmsplugin-text-wrapper that calls cmsplugin-text-wrapper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3
    - django-cms >=2.3

  run:
    - python
    - django >=1.3
    - django-cms >=2.3

test:
  # Python imports
  imports:
    - cmsplugin_text_wrapper
    - cmsplugin_text_wrapper.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jrief/cmsplugin-text-wrapper
  license: BSD License
  summary: 'A django-cms plugin which extends and replaces the shipped text plugin with simple wrapper functionalities.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
