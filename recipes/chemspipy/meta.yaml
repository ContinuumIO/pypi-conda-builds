package:
  name: chemspipy
  version: "1.0.3"

source:
  fn: ChemSpiPy-1.0.3.tar.gz
  url: https://pypi.python.org/packages/source/C/ChemSpiPy/ChemSpiPy-1.0.3.tar.gz
  md5: bcdcbc7f768fac4900286e4744eb767b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - chemspipy = chemspipy:main
    #
    # Would create an entry point called chemspipy that calls chemspipy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests
    - six

  run:
    - python
    - requests
    - six

test:
  # Python imports
  imports:
    - chemspipy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mcs07/ChemSpiPy
  license: MIT License
  summary: 'A simple Python wrapper around the ChemSpider Web Services.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
