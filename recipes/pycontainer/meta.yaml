package:
  name: pycontainer
  version: "0.4"

source:
  fn: PyContainer-0.4.zip
  url: http://prdownloads.sourceforge.net/pycontainer/PyContainer-0.4.zip?download
  #md5: 
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pycontainer = pycontainer:main
    #
    # Would create an entry point called pycontainer that calls pycontainer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - pycontainer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.iem.pw.edu.pl/~sniezynr/pycontainer/
  license: Python (modified BSD style)
  summary: 'A simple Inversion of Control (Dependency Injection) lightweight container'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
