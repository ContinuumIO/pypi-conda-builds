package:
  name: barktracker
  version: "0.1.0"

source:
  fn: BarkTracker-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/B/BarkTracker/BarkTracker-0.1.0.tar.gz
  md5: d8028567bd9018e500e60a9a4de62add
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - barktracker = barktracker:main
    #
    # Would create an entry point called barktracker that calls barktracker.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy
    - pyaudio
    - soundanalyse

  run:
    - python
    - numpy
    - pyaudio
    - soundanalyse

test:
  # Python imports
  imports:
    - barktracker

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lle6138/BarkTracker
  license: GNU Affero GPL Version 3
  summary: 'Bark Tracker monitors and notifies you if your dog is barking while you are away.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
