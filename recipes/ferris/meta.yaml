package:
  name: ferris
  version: "3.0.0a10"

source:
  fn: Ferris-3.0.0a10.tar.gz
  url: https://pypi.python.org/packages/source/F/Ferris/Ferris-3.0.0a10.tar.gz
  md5: 9eb37896f8336e1e0b8f6184fbb621f1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ferris = ferris:main
    #
    # Would create an entry point called ferris that calls ferris.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - google-api-python-client
    - protopigeon

  run:
    - python
    - google-api-python-client
    - protopigeon

test:
  # Python imports
  imports:
    - ferris
    - ferris3

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/cloudsherpas/ferris3
  license: Apache Software License
  summary: 'A framework for creating APIs on Google App Engine'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
