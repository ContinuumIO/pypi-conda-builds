package:
  name: zope.wfmc
  version: "3.5.0"

source:
  fn: zope.wfmc-3.5.0.tar.gz
  url: https://pypi.python.org/packages/source/z/zope.wfmc/zope.wfmc-3.5.0.tar.gz
  md5: 4902b78de392751d6c9d3db3cdd488c4
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zope.wfmc = zope.wfmc:main
    #
    # Would create an entry point called zope.wfmc that calls zope.wfmc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.component
    - zodb3
    - zope.cachedescriptors

  run:
    - python
    - setuptools
    - zope.component
    - zodb3
    - zope.cachedescriptors

test:
  # Python imports
  imports:
    - zope
    - zope.wfmc
    - zope.wfmc.adapter

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://cheeseshop.python.org/pypi/zope.wfmc
  license: Zope Public License
  summary: 'Workflow-Management Coalition Workflow Engine'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
