package:
  name: bio_eutils
  version: "1.63"

source:
  fn: Bio_Eutils-1.63.tar.gz
  url: https://pypi.python.org/packages/source/B/Bio_Eutils/Bio_Eutils-1.63.tar.gz
  md5: 52a7e82384215f457a59210774d4cca3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bio_eutils = bio_eutils:main
    #
    # Would create an entry point called bio_eutils that calls bio_eutils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - Bio_Eutils
    - Bio_Eutils.Entrez
    - Bio_Eutils.Medline
    - Bio_Eutils._py3k
    - bio_eutils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jmaupetit/Bio_Eutils
  license: Biopython
  summary: 'Standalone version of the Biopython Eutils modules'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
