package:
  name: confluence-py
  version: "1.0.1"

source:
  fn: Confluence-py-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/C/Confluence-py/Confluence-py-1.0.1.tar.gz
  md5: c043204cd390ad9136b31584487b49ee
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - confluence-py = confluence-py:main
    #
    # Would create an entry point called confluence-py that calls confluence-py.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - confluence

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/m-vdb/confluence-python
  license: UNKNOWN
  summary: 'Python Confluence module and command line'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
