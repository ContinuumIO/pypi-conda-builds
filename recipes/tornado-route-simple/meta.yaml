package:
  name: tornado-route-simple
  version: "0.1.1"

source:
  fn: tornado-route-simple-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/t/tornado-route-simple/tornado-route-simple-0.1.1.tar.gz
  md5: 1b1c397ffc0697fb6669f6059c7a5ec4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tornado-route-simple = tornado-route-simple:main
    #
    # Would create an entry point called tornado-route-simple that calls tornado-route-simple.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - simpleroute

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/260development/tornado-route-simple
  license: BSD License
  summary: 'Decorates RequestHandlers and builds up a list of routables handlers'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
