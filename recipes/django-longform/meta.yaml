package:
  name: django-longform
  version: "0.2.0"

source:
  fn: django-longform-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-longform/django-longform-0.2.0.tar.gz
  md5: cfd6be223aae1b64f7179a2e168ef240
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-longform = django-longform:main
    #
    # Would create an entry point called django-longform that calls django-longform.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - django-markdown
    - django-taggit
    - markdown

  run:
    - python
    - django
    - django-markdown
    - django-taggit
    - markdown

test:
  # Python imports
  imports:
    - longform

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/martey/django-longform
  license: MIT
  summary: 'A Django application for longform blogging.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
