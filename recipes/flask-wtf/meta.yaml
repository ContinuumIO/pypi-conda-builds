package:
  name: flask-wtf
  version: "0.11"

source:
  fn: Flask-WTF-0.11.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-WTF/Flask-WTF-0.11.tar.gz
  md5: 46eea9bad467ddc464658805ce200870
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-wtf = flask-wtf:main
    #
    # Would create an entry point called flask-wtf that calls flask-wtf.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - werkzeug >=0.9.5
    - wtforms >=1.0.5

  run:
    - python
    - flask
    - werkzeug >=0.9.5
    - wtforms >=1.0.5

test:
  # Python imports
  imports:
    - flask_wtf
    - flask_wtf.recaptcha

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/lepture/flask-wtf
  license: BSD License
  summary: 'Simple integration of Flask and WTForms'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
