package:
  name: ncclient
  version: "0.4.3"

source:
  fn: ncclient-0.4.3.tar.gz
  url: https://pypi.python.org/packages/source/n/ncclient/ncclient-0.4.3.tar.gz
  md5: 6387ac5b276d0095486e7aaadf95fbb2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ncclient = ncclient:main
    #
    # Would create an entry point called ncclient that calls ncclient.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools >0.6
    - paramiko >=1.7.7.1
    - lxml >3.0

  run:
    - python
    - setuptools >0.6
    - paramiko >=1.7.7.1
    - lxml >3.0

test:
  # Python imports
  imports:
    - ncclient
    - ncclient.devices
    - ncclient.operations
    - ncclient.operations.third_party
    - ncclient.operations.third_party.juniper
    - ncclient.operations.third_party.nexus
    - ncclient.transport
    - ncclient.transport.third_party
    - ncclient.transport.third_party.junos

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://ncclient.leopoul.com
  license: Apache License 2.0
  summary: 'Python library for NETCONF clients'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
