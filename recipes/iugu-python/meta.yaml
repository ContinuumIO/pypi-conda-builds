package:
  name: iugu-python
  version: "0.9.6"

source:
  fn: iugu-python-0.9.6.tar.gz
  url: https://pypi.python.org/packages/source/i/iugu-python/iugu-python-0.9.6.tar.gz
  md5: 31ae93aaef0dad401355b6aa3a8fbc4d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - iugu-python = iugu-python:main
    #
    # Would create an entry point called iugu-python that calls iugu-python.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - iugu

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/horacioibrahim/iugu-python
  license: Apache License
  summary: 'This package is an idiomatic python lib to work with Iugu service'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
