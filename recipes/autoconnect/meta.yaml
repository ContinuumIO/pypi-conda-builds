package:
  name: autoconnect
  version: "0.9.3"

source:
  fn: autoconnect-0.9.3.tar.gz
  url: https://pypi.python.org/packages/source/a/autoconnect/autoconnect-0.9.3.tar.gz
  md5: 59a59df7b2d330466a899163708088b4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - autoconnect = autoconnect:main
    #
    # Would create an entry point called autoconnect that calls autoconnect.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - autoconnect
    - autoconnect.doc
    - autoconnect.example
    - autoconnect.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: LGPL
  summary: 'This module helps in automatic client-server discovery and connection setup for networked applications.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
