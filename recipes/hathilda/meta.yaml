package:
  name: hathilda
  version: "0.0.12"

source:
  fn: hathilda-0.0.12.tar.gz
  url: https://pypi.python.org/packages/source/h/hathilda/hathilda-0.0.12.tar.gz
  md5: 7fe5134a64e0fe5a970a82d5fe0fc05e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hathilda = hathilda:main
    #
    # Would create an entry point called hathilda that calls hathilda.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pytest
    - backoff
    - requests

  run:
    - python
    - pytest
    - backoff
    - requests

test:
  # Python imports
  imports:
    - hathilda

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/umd_mith/hathild
  license: UNKNOWN
  summary: 'Turn HathiTrust Data into JSON-LD'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
