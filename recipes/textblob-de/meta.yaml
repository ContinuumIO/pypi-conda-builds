package:
  name: textblob-de
  version: "0.4.2"

source:
  fn: textblob-de-0.4.2.tar.gz
  url: https://pypi.python.org/packages/source/t/textblob-de/textblob-de-0.4.2.tar.gz
  md5: 5a72f494b7962ce15b6d609eb52ec678
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - textblob-de = textblob-de:main
    #
    # Would create an entry point called textblob-de that calls textblob-de.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - textblob >=0.9.0

  run:
    - python
    - textblob >=0.9.0

test:
  # Python imports
  imports:
    - textblob_de

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/markuskiller/textblob-de
  license: MIT License
  summary: 'German language support for TextBlob.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
