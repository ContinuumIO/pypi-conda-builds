package:
  name: django-staticblog
  version: "0.2.5"

source:
  fn: django-staticblog-0.2.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-staticblog/django-staticblog-0.2.5.tar.gz
  md5: ac6b72ec9aa58ae6e244a4c7e955fa02
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-staticblog = django-staticblog:main
    #
    # Would create an entry point called django-staticblog that calls django-staticblog.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - markdown >=2.0
    - html2text >=3.0

  run:
    - python
    - markdown >=2.0
    - html2text >=3.0

test:
  # Python imports
  imports:
    - staticblog
    - staticblog.management
    - staticblog.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/cgrice/django-staticblog/
  license: MIT License
  summary: 'Markdown-based blog engine that compiles to static html pages'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
