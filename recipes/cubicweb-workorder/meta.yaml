package:
  name: cubicweb-workorder
  version: "0.11.2"

source:
  fn: cubicweb-workorder-0.11.2.tar.gz
  url: https://pypi.python.org/packages/source/c/cubicweb-workorder/cubicweb-workorder-0.11.2.tar.gz
  md5: d7bb4613a493dc87ee3c974e1c9e6b0d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cubicweb-workorder = cubicweb-workorder:main
    #
    # Would create an entry point called cubicweb-workorder that calls cubicweb-workorder.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cubicweb >=3.16.0
    - cubicweb-iprogress >=0.1.1

  run:
    - python
    - cubicweb >=3.16.0
    - cubicweb-iprogress >=0.1.1

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.cubicweb.org/project/cubicweb-workorder
  license: LGPL
  summary: 'workorder component for the CubicWeb framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
