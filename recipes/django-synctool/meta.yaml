package:
  name: django-synctool
  version: "1.0.0"

source:
  fn: django-synctool-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-synctool/django-synctool-1.0.0.tar.gz
  md5: bab343c88aa6f992d5e60f310e385244
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-synctool = django-synctool:main
    #
    # Would create an entry point called django-synctool that calls django-synctool.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.7
    - mock
    - requests

  run:
    - python
    - django >=1.7
    - mock
    - requests

test:
  # Python imports
  imports:
    - synctool

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/prestontimmons/django-synctool
  license: MIT License
  summary: 'Sync data between databases.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
