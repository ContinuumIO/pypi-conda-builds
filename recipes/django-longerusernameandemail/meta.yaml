package:
  name: django-longerusernameandemail
  version: "0.5.6"

source:
  fn: django-longerusernameandemail-0.5.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-longerusernameandemail/django-longerusernameandemail-0.5.6.tar.gz
  md5: bb7a3cc93794437660f085db4cfdd4b4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-longerusernameandemail = django-longerusernameandemail:main
    #
    # Would create an entry point called django-longerusernameandemail that calls django-longerusernameandemail.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - longerusernameandemail
    - longerusernameandemail.migrations
    - longerusernameandemail.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD
  summary: 'django-longerusernameandemail provides a migration and a monkeypatch to make the django auth.user username and email fields longer, instead of the arbitrarily short 30 and 75 characters.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
