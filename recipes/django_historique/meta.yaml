package:
  name: django_historique
  version: "1.0.1"

source:
  fn: django_historique-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django_historique/django_historique-1.0.1.tar.gz
  md5: 79ea3bfb28b83913063363ee7e02733b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_historique = django_historique:main
    #
    # Would create an entry point called django_historique that calls django_historique.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - django_historique

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/django-historique/
  license: GNU General Public License (GPL)
  summary: 'Save revisions of your objects in another table!'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
