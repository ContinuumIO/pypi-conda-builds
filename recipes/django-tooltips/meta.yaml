package:
  name: django-tooltips
  version: "1.1.5"

source:
  fn: django-tooltips-1.1.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-tooltips/django-tooltips-1.1.5.tar.gz
  md5: fbf59d04d539e9013c6f0a9ebd81132d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-tooltips = django-tooltips:main
    #
    # Would create an entry point called django-tooltips that calls django-tooltips.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - tooltips
    - tooltips.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/svdgraaf/django-tooltips/
  license: GNU General Public License v3 (GPLv3)
  summary: 'Django manageable Bootstrap Tooltips'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
