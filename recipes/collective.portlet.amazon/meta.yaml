package:
  name: collective.portlet.amazon
  version: "0.2dev-r58081"

source:
  fn: collective.portlet.amazon-0.2dev-r58081.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.portlet.amazon/collective.portlet.amazon-0.2dev-r58081.tar.gz
  md5: 746378b849ed76eb9eba89cd86076d6d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.portlet.amazon = collective.portlet.amazon:main
    #
    # Would create an entry point called collective.portlet.amazon that calls collective.portlet.amazon.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.portlet
    - collective.portlet.amazon
    - collective.portlet.amazon.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.speedbreeze.com
  license: GPL
  summary: 'This ifor Plone 3 that allows you to display a product from Amazon.com and make money through your Amazon Affiliates account.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
