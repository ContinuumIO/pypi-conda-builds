package:
  name: django-settings-list
  version: "0.1.2"

source:
  fn: django-settings-list-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-settings-list/django-settings-list-0.1.2.tar.gz
  md5: 8fd97f1c6a8ea347ffa32a0b28712a89
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-settings-list = django-settings-list:main
    #
    # Would create an entry point called django-settings-list that calls django-settings-list.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - settings_list

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bryanchow/django-settings-list
  license: WTFPL
  summary: 'Django app that lists the settings values for a running project using a custom view accessible only by superusers.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
