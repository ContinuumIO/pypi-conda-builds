package:
  name: django-uuid-upload-path
  version: "1.0.0"

source:
  fn: django-uuid-upload-path-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-uuid-upload-path/django-uuid-upload-path-1.0.0.tar.gz
  md5: f0e32736276b0b09a4e271a0da6071b4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-uuid-upload-path = django-uuid-upload-path:main
    #
    # Would create an entry point called django-uuid-upload-path that calls django-uuid-upload-path.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - uuid_upload_path

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/etianen/django-uuid-upload-path
  license: BSD License
  summary: 'Generate short UUIDs and use them as paths for uploaded media files in Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
