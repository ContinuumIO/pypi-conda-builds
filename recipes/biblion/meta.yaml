package:
  name: biblion
  version: "2.6.2"

source:
  fn: biblion-2.6.2.tar.gz
  url: https://pypi.python.org/packages/source/b/biblion/biblion-2.6.2.tar.gz
  md5: 168fa3b81d7b4047f21c11b0f06b679f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - biblion = biblion:main
    #
    # Would create an entry point called biblion that calls biblion.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-appconf >=1.0.1
    - pillow >=2.0
    - creole >=1.2
    - markdown >=2.4
    - pygments >=1.6

  run:
    - python
    - django-appconf >=1.0.1
    - pillow >=2.0
    - creole >=1.2
    - markdown >=2.4
    - pygments >=1.6

test:
  # Python imports
  imports:
    - biblion
    - biblion.parsers
    - biblion.templatetags
    - biblion.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/pinax/biblion
  license: MIT License
  summary: 'a Django blog app'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
