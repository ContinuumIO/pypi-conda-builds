package:
  name: imageretriever
  version: "0.1"

source:
  fn: imageretriever-0.1.zip
  url: https://pypi.python.org/packages/source/i/imageretriever/imageretriever-0.1.zip
  md5: 547e23bcfebf8ed544509ccbb330fd52
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - imageretriever = imageretriever:main
    #
    # Would create an entry point called imageretriever that calls imageretriever.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - imageretriever

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://zestsoftware.nl
  license: GPL
  summary: 'A Django package to download missing images on your local instance from the production/preview server.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
