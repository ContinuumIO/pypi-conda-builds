package:
  name: blockdiagcontrib-nationalflags
  version: "0.1.6"

source:
  fn: blockdiagcontrib-nationalflags-0.1.6.tar.gz
  url: https://pypi.python.org/packages/source/b/blockdiagcontrib-nationalflags/blockdiagcontrib-nationalflags-0.1.6.tar.gz
  md5: 92ffb58c55203b612233184d966f2438
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - blockdiagcontrib-nationalflags = blockdiagcontrib-nationalflags:main
    #
    # Would create an entry point called blockdiagcontrib-nationalflags that calls blockdiagcontrib-nationalflags.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - blockdiag >=1.2.0
    - setuptools

  run:
    - python
    - setuptools
    - blockdiag >=1.2.0
    - setuptools

test:
  # Python imports
  imports:
    - blockdiagcontrib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://tk0miya.bitbucket.org/blockdiag/build/html/index.html
  license: Python Software Foundation License
  summary: 'noderenderer plugin for blockdiag'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
