package:
  name: django-form-scaffold
  version: "1.1.0"

source:
  fn: django-form-scaffold-1.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-form-scaffold/django-form-scaffold-1.1.0.tar.gz
  md5: af3905e8c495a4eb54743bf699a4c4f4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-form-scaffold = django-form-scaffold:main
    #
    # Would create an entry point called django-form-scaffold that calls django-form-scaffold.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - dfs
    - dfs.management
    - dfs.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/1stvamp/django-form-scaffold
  license: Apache License 2.0
  summary: 'Helper functions for generating templated markup of Django forms'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
