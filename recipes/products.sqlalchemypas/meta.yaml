package:
  name: products.sqlalchemypas
  version: "1.0"

source:
  fn: Products.SQLAlchemyPAS-1.0.zip
  url: https://pypi.python.org/packages/source/P/Products.SQLAlchemyPAS/Products.SQLAlchemyPAS-1.0.zip
  md5: 208e47ff34fe2dbe3f29f78d2edc3faa
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - products.sqlalchemypas = products.sqlalchemypas:main
    #
    # Would create an entry point called products.sqlalchemypas that calls products.sqlalchemypas.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - z3c.saconfig
    - datetime >=2.11.2dev

  run:
    - python
    - setuptools
    - setuptools
    - z3c.saconfig
    - datetime >=2.11.2dev

test:
  # Python imports
  imports:
    - Products
    - Products.SQLAlchemyPAS

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/Products.SQLAlchemyPAS
  license: GPL
  summary: 'PAS / PlonePAS plugin fetching user data from a relational database'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
