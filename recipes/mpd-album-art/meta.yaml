package:
  name: mpd-album-art
  version: "0.1"

source:
  fn: MPD-Album-Art-0.1.tar.gz
  url: https://pypi.python.org/packages/source/M/MPD-Album-Art/MPD-Album-Art-0.1.tar.gz
  md5: 04a2c8563f8be1fd24b27ee5057a1c71
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mpd-album-art = mpd-album-art:main
    #
    # Would create an entry point called mpd-album-art that calls mpd-album-art.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python-mpd2
    - pylast

  run:
    - python
    - python-mpd2
    - pylast

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://jameh.github.io/mpd-album-art
  license: GPL 3.0
  summary: 'Module to fetch Album Art for currently playing song on Music PLayer Daemon'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
