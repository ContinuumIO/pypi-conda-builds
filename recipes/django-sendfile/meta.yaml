package:
  name: django-sendfile
  version: "0.3.7"

source:
  fn: django-sendfile-0.3.7.tar.gz
  url: https://pypi.python.org/packages/source/d/django-sendfile/django-sendfile-0.3.7.tar.gz
  md5: 20fe08e2d41788a0b9c73b7af829ff91
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-sendfile = django-sendfile:main
    #
    # Would create an entry point called django-sendfile that calls django-sendfile.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django >=1.3
    - unidecode

  run:
    - python
    - django >=1.3
    - unidecode

test:
  # Python imports
  imports:
    - sendfile
    - sendfile.backends

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/johnsensible/django-sendfile
  license: BSD License
  summary: 'Abstraction to offload file uploads to web-server (e.g. Apache with mod_xsendfile) once Django has checked permissions etc.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
