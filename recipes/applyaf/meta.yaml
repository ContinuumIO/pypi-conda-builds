package:
  name: applyaf
  version: "0.2.2"

source:
  fn: applyaf-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/a/applyaf/applyaf-0.2.2.tar.gz
  md5: bee8bdf735dbc26a1eb9f8ece476e050
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - applyaf = applyaf:main
    #
    # Would create an entry point called applyaf that calls applyaf.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - applyaf

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/questrail/applyaf
  license: MIT License
  summary: 'Apply antenna factor and cable loss tospectrum analyzer measurements'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
