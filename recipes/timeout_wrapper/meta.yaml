package:
  name: timeout_wrapper
  version: "0.1.0"

source:
  fn: timeout_wrapper-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/t/timeout_wrapper/timeout_wrapper-0.1.0.tar.gz
  md5: ab4d2d26bcd8acb6eb5c3b926294a7b1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - timeout_wrapper = timeout_wrapper:main
    #
    # Would create an entry point called timeout_wrapper that calls timeout_wrapper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - timeout_wrapper

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Bystroushaak/timeout_wrapper
  license: MIT License
  summary: 'Timeout decorator with defaults and exceptions.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
