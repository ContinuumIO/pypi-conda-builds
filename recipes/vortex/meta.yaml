package:
  name: vortex
  version: "0.0.5"

source:
  fn: vortex-0.0.5.tar.gz
  url: https://pypi.python.org/packages/source/v/vortex/vortex-0.0.5.tar.gz
  md5: a33787e6b05aab006bd0ac760dea4729
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - vortex = vortex:main
    #
    # Would create an entry point called vortex that calls vortex.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - vortex

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://alekstorm.github.com/vortex
  license: MIT License
  summary: "An experimental resource-based web framework built on Tornado's IOLoop"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
