package:
  name: charade
  version: "1.0.3"

source:
  fn: charade-1.0.3.tar.gz
  url: https://pypi.python.org/packages/source/c/charade/charade-1.0.3.tar.gz
  md5: 79ac701a147705c09bdce31b79dfa12e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - charade = charade:main
    #
    # Would create an entry point called charade that calls charade.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - charade

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/sigmavirus24/charade
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Universal encoding detector for python 2 and 3'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
