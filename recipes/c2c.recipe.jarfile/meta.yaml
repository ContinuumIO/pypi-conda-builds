package:
  name: c2c.recipe.jarfile
  version: "0.4.3"

source:
  fn: c2c.recipe.jarfile-0.4.3.tar.gz
  url: https://pypi.python.org/packages/source/c/c2c.recipe.jarfile/c2c.recipe.jarfile-0.4.3.tar.gz
  md5: c02c495a61a2ccabc8d13e5d19435a00
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - c2c.recipe.jarfile = c2c.recipe.jarfile:main
    #
    # Would create an entry point called c2c.recipe.jarfile that calls c2c.recipe.jarfile.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zc.buildout

  run:
    - python
    - setuptools
    - zc.buildout

test:
  # Python imports
  imports:
    - c2c
    - c2c.recipe
    - c2c.recipe.jarfile

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/fredj/c2c.recipe.jarfile
  license: MIT License
  summary: 'A buildout recipe to create or update jar archive file.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
