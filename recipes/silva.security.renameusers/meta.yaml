package:
  name: silva.security.renameusers
  version: "1.0"

source:
  fn: silva.security.renameusers-1.0.tar.gz
  url: https://pypi.python.org/packages/source/s/silva.security.renameusers/silva.security.renameusers-1.0.tar.gz
  md5: e4568d1b5c06d2661594bc864a16f8e4
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - silva.security.renameusers = silva.security.renameusers:main
    #
    # Would create an entry point called silva.security.renameusers that calls silva.security.renameusers.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - products.silva
    - five.grok
    - setuptools
    - silva.core.conf
    - silva.core.services
    - zeam.form.silva
    - zope.interface
    - zope.schema

  run:
    - python
    - products.silva
    - five.grok
    - setuptools
    - silva.core.conf
    - silva.core.services
    - zeam.form.silva
    - zope.interface
    - zope.schema

test:
  # Python imports
  imports:
    - silva
    - silva.security
    - silva.security.renameusers
    - silva.security.renameusers.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/silvacms/silva.security.renameusers
  license: BSD License
  summary: 'Rename users identifier in a Silva site'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
