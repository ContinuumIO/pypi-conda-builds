package:
  name: django-tokenapi
  version: "0.2.3"

source:
  fn: django-tokenapi-0.2.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-tokenapi/django-tokenapi-0.2.3.tar.gz
  md5: f58b9e9650f2933ce1d144e0ed40bf6e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-tokenapi = django-tokenapi:main
    #
    # Would create an entry point called django-tokenapi that calls django-tokenapi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - tokenapi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jpulgarin/django-tokenapi
  license: Apache License, Version 2.0
  summary: 'Add an API to your Django app using token-based authentication.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
