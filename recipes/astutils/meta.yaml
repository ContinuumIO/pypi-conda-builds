package:
  name: astutils
  version: "0.0.1"

source:
  fn: astutils-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/a/astutils/astutils-0.0.1.tar.gz
  md5: 233134530a4471aa45745be7d7c61956
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - astutils = astutils:main
    #
    # Would create an entry point called astutils that calls astutils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - ply >=3.4

  run:
    - python
    - ply >=3.4

test:
  # Python imports
  imports:
    - astutils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/johnyf/astutils
  license: BSD
  summary: 'Utilities for abstract syntax trees and parsing with PLY.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
