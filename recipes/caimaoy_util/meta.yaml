package:
  name: caimaoy_util
  version: "0.0.1.6.alpha"

source:
  fn: caimaoy_util-0.0.1.6.alpha.zip
  url: https://pypi.python.org/packages/source/c/caimaoy_util/caimaoy_util-0.0.1.6.alpha.zip
  md5: 53d35422ec977ab320141ef5dc27d90f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - caimaoy_util = caimaoy_util:main
    #
    # Would create an entry point called caimaoy_util that calls caimaoy_util.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - caimaoy_util

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/caimaoy
  license: MIT License
  summary: "caimaoy's util"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
