package:
  name: isotoma.buildout.basicauth
  version: "0.0.7"

source:
  fn: isotoma.buildout.basicauth-0.0.7.tar.gz
  url: https://pypi.python.org/packages/source/i/isotoma.buildout.basicauth/isotoma.buildout.basicauth-0.0.7.tar.gz
  md5: f0bdd1d77d1108aa711dbcfd3936f46a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - isotoma.buildout.basicauth = isotoma.buildout.basicauth:main
    #
    # Would create an entry point called isotoma.buildout.basicauth that calls isotoma.buildout.basicauth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - missingbits
    - zc.buildout

  run:
    - python
    - setuptools
    - setuptools
    - missingbits
    - zc.buildout

test:
  # Python imports
  imports:
    - isotoma
    - isotoma.buildout
    - isotoma.buildout.basicauth
    - isotoma.buildout.basicauth.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Apache Public License
  summary: 'Buildout extension providing basic authentication support'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
