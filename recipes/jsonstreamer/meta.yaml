package:
  name: jsonstreamer
  version: "1.1"

source:
  fn: jsonstreamer-1.1.tar.gz
  url: https://pypi.python.org/packages/source/j/jsonstreamer/jsonstreamer-1.1.tar.gz
  md5: 0d542383d3a5f4f91a24634a78f6c0fc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jsonstreamer = jsonstreamer:main
    #
    # Would create an entry point called jsonstreamer that calls jsonstreamer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - jsonstreamer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kashifrazzaqui/json-streamer
  license: UNKNOWN
  summary: 'Provides a SAX-like push parser which works with partial json fragments. Also provides an ObjectStreamer that emits key-value pairs or array elements of the `root` json object/array'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
