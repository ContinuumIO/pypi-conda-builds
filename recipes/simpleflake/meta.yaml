package:
  name: simpleflake
  version: "0.1.5"

source:
  fn: simpleflake-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/s/simpleflake/simpleflake-0.1.5.tar.gz
  md5: f26df552e48584db601732f47984978a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - simpleflake = simpleflake:main
    #
    # Would create an entry point called simpleflake that calls simpleflake.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - simpleflake

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/SawdustSoftware/simpleflake
  license: MIT License
  summary: 'Twitter snowflake compatible super-simple distributed ID generator.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
