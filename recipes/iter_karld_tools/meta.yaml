package:
  name: iter_karld_tools
  version: "0.9.2"

source:
  fn: iter_karld_tools-0.9.2.tar.gz
  url: https://pypi.python.org/packages/source/i/iter_karld_tools/iter_karld_tools-0.9.2.tar.gz
  md5: 7ea341c469864d384d523a683d3a5f61
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - iter_karld_tools = iter_karld_tools:main
    #
    # Would create an entry point called iter_karld_tools that calls iter_karld_tools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - iter_karld_tools
    - iter_karld_tools.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/johnwlockwood/iter_karld_tools
  license: Apache Software License
  summary: 'More tools for iterators'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
