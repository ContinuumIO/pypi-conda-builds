package:
  name: eggtranslations
  version: "1.2.1"

source:
  fn: EggTranslations-1.2.1.tar.gz
  url: https://pypi.python.org/packages/source/E/EggTranslations/EggTranslations-1.2.1.tar.gz
  md5: c117fd31f8105397cfb55f78dba13b9f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - eggtranslations = eggtranslations:main
    #
    # Would create an entry point called eggtranslations that calls eggtranslations.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - eggtranslations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://chandlerproject.org/Projects/EggTranslations
  license: Apache Software License
  summary: 'Provides an API for accessing localizations and resources packaged in python eggs'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
