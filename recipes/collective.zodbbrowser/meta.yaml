package:
  name: collective.zodbbrowser
  version: "0.1"

source:
  fn: collective.zodbbrowser-0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.zodbbrowser/collective.zodbbrowser-0.1.tar.gz
  md5: cb2f320ce04a915a34d0890772419c91
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.zodbbrowser = collective.zodbbrowser:main
    #
    # Would create an entry point called collective.zodbbrowser that calls collective.zodbbrowser.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - pygments

  run:
    - python
    - setuptools
    - setuptools
    - pygments

test:
  # Python imports
  imports:
    - collective
    - collective.zodbbrowser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/zodbbrowser
  license: GPL
  summary: 'A zodb browser for Zope2'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
