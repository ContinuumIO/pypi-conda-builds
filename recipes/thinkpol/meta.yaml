package:
  name: thinkpol
  version: "0.1.0"

source:
  fn: Thinkpol-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/T/Thinkpol/Thinkpol-0.1.0.tar.gz
  md5: 915a7900fd3c4e4b4b68b4d9be2283a1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - thinkpol = thinkpol:main
    #
    # Would create an entry point called thinkpol that calls thinkpol.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - gevent >=0.13.8
    - python-snappy >=0.4
    - yajl >=0.3.5

  run:
    - python
    - gevent >=0.13.8
    - python-snappy >=0.4
    - yajl >=0.3.5

test:
  # Python imports
  imports:
    - Thinkpol
    - thinkpol

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Tefx/Thinkpol
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Helper classes to write monitoring part in distributed applications'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
