package:
  name: django-brutebuster
  version: "0.1.8"

source:
  fn: django-brutebuster-0.1.8.tar.gz
  url: https://pypi.python.org/packages/source/d/django-brutebuster/django-brutebuster-0.1.8.tar.gz
  md5: 08a91b53aaff990409e28a6be8681b5d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-brutebuster = django-brutebuster:main
    #
    # Would create an entry point called django-brutebuster that calls django-brutebuster.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - BruteBuster

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/django-brutebuster/
  license: UNKNOWN
  summary: 'Pluggable Django application providing password bruteforce protection'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
