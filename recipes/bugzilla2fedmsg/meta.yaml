package:
  name: bugzilla2fedmsg
  version: "0.2.1"

source:
  fn: bugzilla2fedmsg-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/b/bugzilla2fedmsg/bugzilla2fedmsg-0.2.1.tar.gz
  md5: bd1f89e32d99a0c5b59fe486601e4a76
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bugzilla2fedmsg = bugzilla2fedmsg:main
    #
    # Would create an entry point called bugzilla2fedmsg that calls bugzilla2fedmsg.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fedmsg
    - python-bugzilla
    - python-dateutil
    - moksha.hub
    - stomper

  run:
    - python
    - setuptools
    - fedmsg
    - python-bugzilla
    - python-dateutil
    - moksha.hub
    - stomper

test:
  # Python imports
  imports:
    - bugzilla2fedmsg

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/fedora-infra/bugzilla2fedmsg
  license: LGPLv2+
  summary: 'Consume BZ messages over STOMP and republish to fedmsg'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
