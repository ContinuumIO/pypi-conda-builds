package:
  name: linkpath
  version: "0.1.1"

source:
  fn: linkpath-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/l/linkpath/linkpath-0.1.1.tar.gz
  md5: b626d7606279f0f3e47ef13841ab7e9f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - linkpath = linkpath:main
    #
    # Would create an entry point called linkpath that calls linkpath.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - httplib2
    - rdflib >=3.1.0

  run:
    - python
    - httplib2
    - rdflib >=3.1.0

test:
  # Python imports
  imports:
    - linkpath

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/iand/linkpath
  license: UNKNOWN
  summary: 'A Python library for querying the linked data web'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
