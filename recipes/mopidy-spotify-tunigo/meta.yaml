package:
  name: mopidy-spotify-tunigo
  version: "0.2.1"

source:
  fn: Mopidy-Spotify-Tunigo-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/M/Mopidy-Spotify-Tunigo/Mopidy-Spotify-Tunigo-0.2.1.tar.gz
  md5: e36dc110d3ad689663a99d35e5d22b61
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mopidy-spotify-tunigo = mopidy-spotify-tunigo:main
    #
    # Would create an entry point called mopidy-spotify-tunigo that calls mopidy-spotify-tunigo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - mopidy >=0.19.0
    - pykka >=1.1
    - mopidy-spotify >=1.2.0
    - tunigo

  run:
    - python
    - setuptools
    - setuptools
    - mopidy >=0.19.0
    - pykka >=1.1
    - mopidy-spotify >=1.2.0
    - tunigo

test:
  # Python imports
  imports:
    - mopidy_spotify_tunigo

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/trygveaa/mopidy-spotify-tunigo
  license: Apache Software License
  summary: u'Mopidy extension for providing the browse feature of Spotify'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
