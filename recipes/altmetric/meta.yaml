package:
  name: altmetric
  version: "1.0"

source:
  fn: altmetric-1.0.tar.gz
  url: https://pypi.python.org/packages/source/a/altmetric/altmetric-1.0.tar.gz
  md5: 9b7c6aca1eec640e55c1cd9f298ad72a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - altmetric = altmetric:main
    #
    # Would create an entry point called altmetric that calls altmetric.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - simplejson
    - requests

  run:
    - python
    - simplejson
    - requests

test:
  # Python imports
  imports:
    - altmetric

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lnielsen-cern/python-altmetric
  license: MIT License
  summary: 'Altmetric API v1 wrapper for Python.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
