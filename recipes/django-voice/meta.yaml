package:
  name: django-voice
  version: "0.5"

source:
  fn: django-voice-0.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-voice/django-voice-0.5.tar.gz
  md5: e266b81e3a54e51b9ce8a88c7d2dcc3b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-voice = django-voice:main
    #
    # Would create an entry point called django-voice that calls django-voice.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4
    - qhonuskan-votes >=0.2

  run:
    - python
    - django >=1.4
    - qhonuskan-votes >=0.2

test:
  # Python imports
  imports:
    - djangovoice
    - djangovoice.migrations
    - djangovoice.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/gkmngrgn/django-voice
  license: BSD
  summary: 'A feedback application for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
