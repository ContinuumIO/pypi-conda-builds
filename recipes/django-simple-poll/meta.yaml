package:
  name: django-simple-poll
  version: "0.1.2"

source:
  fn: django-simple-poll-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-simple-poll/django-simple-poll-0.1.2.tar.gz
  md5: c88c7791a661926d886f2cbcf66d7858
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-simple-poll = django-simple-poll:main
    #
    # Would create an entry point called django-simple-poll that calls django-simple-poll.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - poll
    - poll.migrations
    - poll.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/applecat/django-simple-poll
  license: BSD License
  summary: 'Django simple poll application'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
