package:
  name: lazr.smtptest
  version: "2.0.3"

source:
  fn: lazr.smtptest-2.0.3.tar.gz
  url: https://pypi.python.org/packages/source/l/lazr.smtptest/lazr.smtptest-2.0.3.tar.gz
  md5: 9fb4025a7c4850092c5c4400769596cb
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lazr.smtptest = lazr.smtptest:main
    #
    # Would create an entry point called lazr.smtptest that calls lazr.smtptest.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - nose
    - setuptools

  run:
    - python
    - nose
    - setuptools

test:
  # Python imports
  imports:
    - lazr
    - lazr.smtptest
    - lazr.smtptest.docs
    - lazr.smtptest.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://launchpad.net/lazr.smtptest
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'A test framework for SMTP based applications'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
