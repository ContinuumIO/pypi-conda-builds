package:
  name: quintagroup.theme.estatelite
  version: "6.0.6"

source:
  fn: quintagroup.theme.estatelite-6.0.6.zip
  url: https://pypi.python.org/packages/source/q/quintagroup.theme.estatelite/quintagroup.theme.estatelite-6.0.6.zip
  md5: 888fda8e7df143ea4c951dccccaf34a1
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - quintagroup.theme.estatelite = quintagroup.theme.estatelite:main
    #
    # Would create an entry point called quintagroup.theme.estatelite that calls quintagroup.theme.estatelite.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.theming

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.theming

test:
  # Python imports
  imports:
    - quintagroup
    - quintagroup.theme
    - quintagroup.theme.estatelite

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://skins.quintagroup.com/estate-lite
  license: GNU General Public License (GPL)
  summary: 'Free Diazo Theme for Plone 4.1'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
