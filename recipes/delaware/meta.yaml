package:
  name: delaware
  version: "0.0.5"

source:
  fn: delaware-0.0.5.tar.gz
  url: https://pypi.python.org/packages/source/d/delaware/delaware-0.0.5.tar.gz
  md5: 9d61b992813c2a5ea8476e76d6a312ff
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - delaware = delaware:main
    #
    # Would create an entry point called delaware that calls delaware.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - randua >=0.0.1
    - requests >=2.3.0
    - picklecache >=0.0.4

  run:
    - python
    - randua >=0.0.1
    - requests >=2.3.0
    - picklecache >=0.0.4

test:
  # Python imports
  imports:
    - delaware
    - delaware_manager
    - delaware_reader
    - delaware_shared
    - delaware_worker

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tlevine/delaware
  license: AGPL
  summary: 'Get company registration data for the State of Delaware.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
