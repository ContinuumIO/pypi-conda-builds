package:
  name: quintagroup.silogroup
  version: "1.0"

source:
  fn: quintagroup.silogroup-1.0.zip
  url: https://pypi.python.org/packages/source/q/quintagroup.silogroup/quintagroup.silogroup-1.0.zip
  md5: 1399b97fc977f643a2fc7b5eb83524d2
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - quintagroup.silogroup = quintagroup.silogroup:main
    #
    # Would create an entry point called quintagroup.silogroup that calls quintagroup.silogroup.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - quintagroup
    - quintagroup.silogroup
    - quintagroup.silogroup.browser
    - quintagroup.silogroup.interfaces
    - quintagroup.silogroup.portlets

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/quintagroup/quintagroup.silogroup
  license: GPL
  summary: 'SiloGroup allows to generate custom navigation menu titles, hide or make them visible.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
