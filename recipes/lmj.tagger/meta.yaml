package:
  name: lmj.tagger
  version: "0.1.1"

source:
  fn: lmj.tagger-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/l/lmj.tagger/lmj.tagger-0.1.1.tar.gz
  md5: c0ea8de6056bc1dbc244d648b2cfa74e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lmj.tagger = lmj.tagger:main
    #
    # Would create an entry point called lmj.tagger that calls lmj.tagger.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - lmj
    - lmj.tagger

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/lmjohns3/py-tagger/
  license: MIT License
  summary: 'A tagger for sequence data'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
