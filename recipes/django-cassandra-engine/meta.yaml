package:
  name: django-cassandra-engine
  version: "0.3.2"

source:
  fn: django-cassandra-engine-0.3.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-cassandra-engine/django-cassandra-engine-0.3.2.tar.gz
  md5: 44a15f43447aaa5e54f0c78652a29191
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-cassandra-engine = django-cassandra-engine:main
    #
    # Would create an entry point called django-cassandra-engine that calls django-cassandra-engine.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django <1.9
    - cassandra-driver ==2.5.1

  run:
    - python
    - django <1.9
    - cassandra-driver ==2.5.1

test:
  # Python imports
  imports:
    - django_cassandra_engine
    - django_cassandra_engine.base
    - django_cassandra_engine.management
    - django_cassandra_engine.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/r4fek/django-cassandra-engine
  license: BSD License
  summary: 'Django Cassandra Engine - the Cassandra backend for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
