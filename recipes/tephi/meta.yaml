package:
  name: tephi
  version: "0.1.0"

source:
  fn: tephi-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/t/tephi/tephi-0.1.0.tar.gz
  md5: c5bddc8fc61650f4b2281e71df1252b3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tephi = tephi:main
    #
    # Would create an entry point called tephi that calls tephi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - tephi
    - tephi.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/SciTools/tephi
  license: GNU Lesser General Public License v3 (LGPLv3)
  summary: 'Tephigram plotting in Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
