package:
  name: django-staticfiles-fitvidsjs
  version: "1.0.0"

source:
  fn: django-staticfiles-fitvidsjs-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-staticfiles-fitvidsjs/django-staticfiles-fitvidsjs-1.0.0.tar.gz
  md5: 2ffe91024346fe7ede27442d86e04d64
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-staticfiles-fitvidsjs = django-staticfiles-fitvidsjs:main
    #
    # Would create an entry point called django-staticfiles-fitvidsjs that calls django-staticfiles-fitvidsjs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - fitvidsjs

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/tswicegood/django-staticfiles-fitvidsjs/
  license: BSD License
  summary: 'FitVids.js meets Django staticfiles'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
