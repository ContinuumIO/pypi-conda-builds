package:
  name: ipython-cql
  version: "0.5"

source:
  fn: ipython-cql-0.5.tar.gz
  url: https://pypi.python.org/packages/source/i/ipython-cql/ipython-cql-0.5.tar.gz
  md5: f27e7d6887b6f62cbe236eab659744a4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ipython-cql = ipython-cql:main
    #
    # Would create an entry point called ipython-cql that calls ipython-cql.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - cassandra-driver
    - prettytable

  run:
    - python
    - cassandra-driver
    - prettytable

test:
  # Python imports
  imports:
    - cql

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rustyrazorblade/ipython-cql
  license: 2 Clause BSD
  summary: 'IPython Extension for Cassandra integration'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
