package:
  name: django-nplib
  version: "0.1"

source:
  fn: django-nplib-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-nplib/django-nplib-0.1.tar.gz
  md5: 2bfe070c6e7caa7144b2d35b4cfea738
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-nplib = django-nplib:main
    #
    # Would create an entry point called django-nplib that calls django-nplib.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - nplib
    - nplib.decorators
    - nplib.middleware

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/nickpack/django-nplib
  license: UNKNOWN
  summary: 'Hopefully useful django decorators, middleware and misc utility functions'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
