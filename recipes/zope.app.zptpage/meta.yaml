package:
  name: zope.app.zptpage
  version: "3.5.1"

source:
  fn: zope.app.zptpage-3.5.1.tar.gz
  url: https://pypi.python.org/packages/source/z/zope.app.zptpage/zope.app.zptpage-3.5.1.tar.gz
  md5: aed8ec49e10911bd1e9d2c9d467fd098
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zope.app.zptpage = zope.app.zptpage:main
    #
    # Would create an entry point called zope.app.zptpage that calls zope.app.zptpage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.container
    - zope.app.publication
    - zope.filerepresentation
    - zope.formlib
    - zope.i18nmessageid
    - zope.index
    - zope.interface
    - zope.pagetemplate
    - zope.publisher >=3.12
    - zope.schema
    - zope.security
    - zope.site
    - zope.size
    - zope.traversing
    - zodb3

  run:
    - python
    - setuptools
    - zope.container
    - zope.app.publication
    - zope.filerepresentation
    - zope.formlib
    - zope.i18nmessageid
    - zope.index
    - zope.interface
    - zope.pagetemplate
    - zope.publisher >=3.12
    - zope.schema
    - zope.security
    - zope.site
    - zope.size
    - zope.traversing
    - zodb3

test:
  # Python imports
  imports:
    - zope
    - zope.app
    - zope.app.zptpage
    - zope.app.zptpage.browser
    - zope.app.zptpage.tests
    - zope.app.zptpage.textindex

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/zope.app.zptpage
  license: Zope Public License
  summary: 'ZPT page content component'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
