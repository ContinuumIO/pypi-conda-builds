package:
  name: timeparse
  version: "0.5.5"

source:
  fn: timeparse-0.5.5.tar.gz
  url: https://pypi.python.org/packages/source/t/timeparse/timeparse-0.5.5.tar.gz
  md5: e740526bb047d53e42483765c6750285
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - timeparse = timeparse:main
    #
    # Would create an entry point called timeparse that calls timeparse.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - timeparser
    - daytime

  run:
    - python
    - timeparser
    - daytime

test:
  # Python imports
  imports:
    - timeparse

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/thomst/timeparse
  license: GNU General Public License (GPL)
  summary: 'An argparse-extension for parsing command-line arguments as time-, date-, datetime-, or timedelta-objects.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
