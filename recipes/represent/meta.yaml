package:
  name: represent
  version: "1.3.0"

source:
  fn: Represent-1.3.0.tar.gz
  url: https://pypi.python.org/packages/source/R/Represent/Represent-1.3.0.tar.gz
  md5: 442d90e2fc53aabb3dc969db36708d3b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - represent = represent:main
    #
    # Would create an entry point called represent that calls represent.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - represent
    - represent.compat
    - represent.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/RazerM/represent
  license: MIT License
  summary: 'Create __repr__ automatically or declaratively.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
