package:
  name: django-leaflet-storage
  version: "0.7.6"

source:
  fn: django-leaflet-storage-0.7.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-leaflet-storage/django-leaflet-storage-0.7.6.tar.gz
  md5: a7336ba600b8022eb1813da100d22003
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-leaflet-storage = django-leaflet-storage:main
    #
    # Would create an entry point called django-leaflet-storage that calls django-leaflet-storage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django ==1.6.5
    - psycopg2 ==2.5.2
    - pillow ==2.4.0
    - south
    - simplejson

  run:
    - python
    - django ==1.6.5
    - psycopg2 ==2.5.2
    - pillow ==2.4.0
    - south
    - simplejson

test:
  # Python imports
  imports:
    - leaflet_storage
    - leaflet_storage.management
    - leaflet_storage.management.commands
    - leaflet_storage.templatetags
    - leaflet_storage.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/yohanboniface/django-leaflet-storage
  license: django-lean
  summary: 'Create collaborative maps on top of Geodjango and Leaflet.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
