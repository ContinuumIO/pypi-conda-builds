package:
  name: assignmentprint
  version: "1.0"

source:
  fn: assignmentprint-1.0.tar.gz
  url: https://pypi.python.org/packages/source/a/assignmentprint/assignmentprint-1.0.tar.gz
  md5: b88ae567f3b9d26dffbb1f09fad11e72
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - assignmentprint = assignmentprint:main
    #
    # Would create an entry point called assignmentprint that calls assignmentprint.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pep8

  run:
    - python
    - pep8

test:
  # Python imports
  imports:
    - assignmentprint

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/saffsd/assignmentprint
  license: GNU General Public License v3 (GPLv3)
  summary: 'Pretty printer for student-submitted assignments.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
