package:
  name: django-la-facebook
  version: "0.1.1"

source:
  fn: django-la-facebook-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-la-facebook/django-la-facebook-0.1.1.tar.gz
  md5: 9e44992caa179338e50277b47835be99
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-la-facebook = django-la-facebook:main
    #
    # Would create an entry point called django-la-facebook that calls django-la-facebook.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - oauth2

  run:
    - python
    - oauth2

test:
  # Python imports
  imports:
    - la_facebook
    - la_facebook.callbacks
    - la_facebook.templatetags
    - la_facebook.tests
    - la_facebook.utils
    - test_project
    - test_project.connect
    - test_project.connect.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/cartwheelweb/django-la-facebook
  license: BSD License
  summary: 'Definitive facebook auth for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
