package:
  name: json2xml
  version: "1.0.1"

source:
  fn: json2xml-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/j/json2xml/json2xml-1.0.1.tar.gz
  md5: 9e168136e2d55806bb8f234a7edbb570
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - json2xml = json2xml:main
    #
    # Would create an entry point called json2xml that calls json2xml.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - beautifulsoup ==3.2.1
    - argparse ==1.2.1
    - dict2xml ==1.3
    - gnureadline ==6.3.3
    - simplejson ==3.6.5
    - six ==1.9.0
    - wsgiref ==0.1.2

  run:
    - python
    - beautifulsoup ==3.2.1
    - argparse ==1.2.1
    - dict2xml ==1.3
    - gnureadline ==6.3.3
    - simplejson ==3.6.5
    - six ==1.9.0
    - wsgiref ==0.1.2

test:
  # Python imports
  imports:
    - json2xml

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com:vinitkumar/json2xml.git
  license: UNKNOWN
  summary: 'To covert json data to xml data'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
