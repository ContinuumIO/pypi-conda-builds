package:
  name: fibonacci-heap-mod
  version: "0.99"

source:
  fn: fibonacci-heap-mod-0.99.tar.gz
  url: https://pypi.python.org/packages/source/f/fibonacci-heap-mod/fibonacci-heap-mod-0.99.tar.gz
  md5: 4025833c51ea9d816cf31bf08f9169d4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fibonacci-heap-mod = fibonacci-heap-mod:main
    #
    # Would create an entry point called fibonacci-heap-mod that calls fibonacci-heap-mod.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://stromberg.dnsalias.org/~strombrg/fibonacci-heap-mod/
  license: Apache v2
  summary: 'Pure Python Fibonacci Heap (Priority Queue)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
