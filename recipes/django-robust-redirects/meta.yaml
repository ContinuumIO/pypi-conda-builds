package:
  name: django-robust-redirects
  version: "0.9.3"

source:
  fn: django-robust-redirects-0.9.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-robust-redirects/django-robust-redirects-0.9.3.tar.gz
  md5: 6a1054ada78ab9bcd867ae41cf9ea8a9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-robust-redirects = django-robust-redirects:main
    #
    # Would create an entry point called django-robust-redirects that calls django-robust-redirects.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - robustredirects

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/spothero/django-robust-redirects
  license: Apache Software License
  summary: 'A more robust and feature full django redirect package'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
