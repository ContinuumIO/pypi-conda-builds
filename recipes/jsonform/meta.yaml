package:
  name: jsonform
  version: "0.0.2"

source:
  fn: JsonForm-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/J/JsonForm/JsonForm-0.0.2.tar.gz
  md5: a0df53814718fa49ffbfc41fec8caed1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jsonform = jsonform:main
    #
    # Would create an entry point called jsonform that calls jsonform.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - jsonschema

  run:
    - python
    - jsonschema

test:
  # Python imports
  imports:
    - jsonform

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/RussellLuo/jsonform
  license: MIT
  summary: 'Form validation for JSON-like data (i.e. document) in Python.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
