package:
  name: hspy
  version: "1.6.4"

source:
  fn: hspy-1.6.4.tar.gz
  url: https://pypi.python.org/packages/source/h/hspy/hspy-1.6.4.tar.gz
  md5: 3df79c98b6d13597499e914c0fb0b6dd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hspy = hspy:main
    #
    # Would create an entry point called hspy that calls hspy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django ==1.3

  run:
    - python
    - django ==1.3

test:
  # Python imports
  imports:
    - hspy
    - marketplace
    - marketplace.middleware

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/HubSpot/hspy
  license: LICENSE.txt
  summary: 'Utilities to aide HubSpot Marketplace development'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
