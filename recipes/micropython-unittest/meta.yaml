package:
  name: micropython-unittest
  version: "0.0.7"

source:
  fn: micropython-unittest-0.0.7.tar.gz
  url: https://pypi.python.org/packages/source/m/micropython-unittest/micropython-unittest-0.0.7.tar.gz
  md5: 11b452d1d7ef1fe6613b062dcc206d3d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - micropython-unittest = micropython-unittest:main
    #
    # Would create an entry point called micropython-unittest that calls micropython-unittest.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/micropython/micropython/issues/405
  license: MIT
  summary: 'unittest module for MicroPython'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
