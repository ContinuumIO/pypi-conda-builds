package:
  name: django-parallelized_querysets
  version: "0.0.4"

source:
  fn: django-parallelized_querysets-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-parallelized_querysets/django-parallelized_querysets-0.0.4.tar.gz
  md5: 4ca10fdc49d7585d09c0328de7c89ab9
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-parallelized_querysets = django-parallelized_querysets:main
    #
    # Would create an entry point called django-parallelized_querysets that calls django-parallelized_querysets.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - setuptools
    - django

test:
  # Python imports
  imports:
    - parallelized_querysets

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/pelletier/django-parallelized_querysets
  license: MIT License
  summary: 'Spread Django QuerySets on multiple cores with low memory usage.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
