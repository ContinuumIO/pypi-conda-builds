package:
  name: sourcecodegen
  version: "0.6.14"

source:
  fn: sourcecodegen-0.6.14.tar.gz
  url: https://pypi.python.org/packages/source/s/sourcecodegen/sourcecodegen-0.6.14.tar.gz
  md5: cb3f3fd1066e9b419f6b55a7d0a29dfd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sourcecodegen = sourcecodegen:main
    #
    # Would create an entry point called sourcecodegen that calls sourcecodegen.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - sourcecodegen
    - sourcecodegen.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD-like (http://repoze.org/license.html)
  summary: 'A Python source-code generator based on the ``compiler.ast`` abstract syntax tree.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
