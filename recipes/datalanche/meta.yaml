package:
  name: datalanche
  version: "0.9.2"

source:
  fn: datalanche-0.9.2.tar.gz
  url: https://pypi.python.org/packages/source/d/datalanche/datalanche-0.9.2.tar.gz
  md5: 852361ad113f3efd53513ca536926012
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - datalanche = datalanche:main
    #
    # Would create an entry point called datalanche that calls datalanche.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests ==1.2

  run:
    - python
    - requests ==1.2

test:
  # Python imports
  imports:
    - datalanche

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/datalanche/python-datalanche
  license: MIT License
  summary: 'Official Python client for Datalanche.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
