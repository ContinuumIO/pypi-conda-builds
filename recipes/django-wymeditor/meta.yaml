package:
  name: django-wymeditor
  version: "1.0"

source:
  fn: django-wymeditor-1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-wymeditor/django-wymeditor-1.0.tar.gz
  md5: 99c328ac7bba172eb71f3ba68d3e6387
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-wymeditor = django-wymeditor:main
    #
    # Would create an entry point called django-wymeditor that calls django-wymeditor.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - wymeditor

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/gabrielhurley/django-wymeditor
  license: BSD License
  summary: 'A Django application that contains a widget to render a form field with a WYMEditor interface.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
