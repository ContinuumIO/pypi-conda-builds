package:
  name: django_bootstrap_carousel
  version: "0.2"

source:
  fn: django_bootstrap_carousel-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django_bootstrap_carousel/django_bootstrap_carousel-0.2.tar.gz
  md5: 62e0f6007850b865e13a5c04f0f42a23
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_bootstrap_carousel = django_bootstrap_carousel:main
    #
    # Would create an entry point called django_bootstrap_carousel that calls django_bootstrap_carousel.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - south
    - django_nose
    - pillow

  run:
    - python
    - django
    - south
    - django_nose
    - pillow

test:
  # Python imports
  imports:
    - django_bootstrap_carousel
    - django_bootstrap_carousel.migrations
    - django_bootstrap_carousel.south_migrations
    - django_bootstrap_carousel.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/cdchen/django-bootstrap-carousel
  license: MIT License
  summary: 'Django Bootstrap Carousel Component'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
