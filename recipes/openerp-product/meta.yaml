package:
  name: openerp-product
  version: "7.0.406"

source:
  fn: openerp-product-7.0.406.tar.gz
  url: https://pypi.python.org/packages/source/o/openerp-product/openerp-product-7.0.406.tar.gz
  md5: d1260f678c2f1eda953fd46bb4a22454
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - openerp-product = openerp-product:main
    #
    # Would create an entry point called openerp-product that calls openerp-product.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - openerp-core
    - openerp-process
    - openerp-decimal-precision
    - openerp-mail

  run:
    - python
    - openerp-core
    - openerp-process
    - openerp-decimal-precision
    - openerp-mail

test:
  # Python imports
  imports:
    - openerp.addons.product
    - openerp.addons.product.report
    - openerp.addons.product.wizard

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: GNU Affero General Public License v3
  summary: 'Products & Pricelists'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
