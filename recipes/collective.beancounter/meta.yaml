package:
  name: collective.beancounter
  version: "0.3.1"

source:
  fn: collective.beancounter-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.beancounter/collective.beancounter-0.3.1.tar.gz
  md5: dcd815319a8be9ee82ffd8b06f5f4aff
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.beancounter = collective.beancounter:main
    #
    # Would create an entry point called collective.beancounter that calls collective.beancounter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.beancounter
    - collective.beancounter.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://svn.plone.org/svn/collective/collective.beancounter/trunk
  license: GPL
  summary: 'package to display a fill-percentage for AT based content'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
