package:
  name: lazy-object-proxy
  version: "1.0.2"

source:
  fn: lazy-object-proxy-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/l/lazy-object-proxy/lazy-object-proxy-1.0.2.tar.gz
  md5: 3db5b728e7bf118123ea669003042ee8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lazy-object-proxy = lazy-object-proxy:main
    #
    # Would create an entry point called lazy-object-proxy that calls lazy-object-proxy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - lazy_object_proxy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ionelmc/python-lazy-object-proxy
  license: BSD License
  summary: 'A fast and thorough lazy object proxy'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
