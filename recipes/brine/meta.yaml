package:
  name: brine
  version: "1.0.0"

source:
  fn: brine-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/b/brine/brine-1.0.0.tar.gz
  md5: 81f32289929b48f394ccbb6f24170e45
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - brine = brine:main
    #
    # Would create an entry point called brine that calls brine.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - brine

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/obriencj/python-brine
  license: GNU Lesser General Public License
  summary: 'Advanced pickle features, such as anonymous function pickling'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
