package:
  name: lineage.index
  version: "0.4"

source:
  fn: lineage.index-0.4.zip
  url: https://pypi.python.org/packages/source/l/lineage.index/lineage.index-0.4.zip
  md5: 748cbb58bb5d820488d3cf15fce3daf9
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lineage.index = lineage.index:main
    #
    # Would create an entry point called lineage.index that calls lineage.index.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.lineage

  run:
    - python
    - setuptools
    - setuptools
    - collective.lineage

test:
  # Python imports
  imports:
    - lineage
    - lineage.index

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/lineage.index
  license: GNU General Public License (GPL)
  summary: 'Adds an index and metadata that allows to search for content of a collective.lineage childsite'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
