package:
  name: django-elfinder
  version: "0.2"

source:
  fn: django-elfinder-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-elfinder/django-elfinder-0.2.tar.gz
  md5: 500e9c378dc333f4f8f2cca65c2b4450
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-elfinder = django-elfinder:main
    #
    # Would create an entry point called django-elfinder that calls django-elfinder.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - elfinder

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mikery/django-elfinder/
  license: UNKNOWN
  summary: 'Django connector for elFinder 2'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
