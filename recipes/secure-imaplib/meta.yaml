package:
  name: secure-imaplib
  version: "0.1.2"

source:
  fn: secure-imaplib-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/s/secure-imaplib/secure-imaplib-0.1.2.tar.gz
  md5: f622b9638dd91401a1554a9db0da4f14
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - secure-imaplib = secure-imaplib:main
    #
    # Would create an entry point called secure-imaplib that calls secure-imaplib.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - secure_imaplib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/graingert/secure-imaplib
  license: UNKNOWN
  summary: 'Secure IMAP subclasses for Python 2'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
