package:
  name: couchclient
  version: "1.1.1"

source:
  fn: couchclient-1.1.1.tar.gz
  url: https://pypi.python.org/packages/source/c/couchclient/couchclient-1.1.1.tar.gz
  md5: 5add3fca26e103c018d7ab5d53042b28
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - couchclient = couchclient:main
    #
    # Would create an entry point called couchclient that calls couchclient.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests
    - simplejson

  run:
    - python
    - requests
    - simplejson

test:
  # Python imports
  imports:
    - couchclient

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/gmr/couchclient
  license: BSD License
  summary: 'Light-weight read-only CouchDB client for loading documents and views'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
