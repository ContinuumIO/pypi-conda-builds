package:
  name: django-stackhelper
  version: "0.3"

source:
  fn: django-stackhelper-0.3.zip
  url: https://pypi.python.org/packages/source/d/django-stackhelper/django-stackhelper-0.3.zip
  md5: 3f13eb5b0c2defb469d32adac89c45bf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-stackhelper = django-stackhelper:main
    #
    # Would create an entry point called django-stackhelper that calls django-stackhelper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - jinja2

  run:
    - python
    - jinja2

test:
  # Python imports
  imports:
    - stackhelper

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/isotoma/django-stackhelper
  license: Apache Software License
  summary: 'A Django application to help you configure other components of your deployment stack'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
