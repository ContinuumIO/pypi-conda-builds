package:
  name: plocal
  version: "0.1"

source:
  fn: plocal-0.1.tar.gz
  url: https://pypi.python.org/packages/source/p/plocal/plocal-0.1.tar.gz
  md5: 227536a3887b456a78cc3e0a5931555f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plocal = plocal:main
    #
    # Would create an entry point called plocal that calls plocal.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - plocal

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://ohthehugemanatee.net/
  license: BSD License
  summary: 'A "local" storage class that mirrors threading.local but works across multiple processes as well as threads.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
