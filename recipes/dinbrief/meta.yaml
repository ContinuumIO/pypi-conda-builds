package:
  name: dinbrief
  version: "0.3.4"

source:
  fn: dinbrief-0.3.4.tar.gz
  url: https://pypi.python.org/packages/source/d/dinbrief/dinbrief-0.3.4.tar.gz
  md5: 7206960fafec602a496227a33440f550
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dinbrief = dinbrief:main
    #
    # Would create an entry point called dinbrief that calls dinbrief.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - reportlab

  run:
    - python
    - reportlab

test:
  # Python imports
  imports:
    - dinbrief
    - dinbrief.contrib
    - dinbrief.invoice

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/terreon/dinbrief
  license: BSD License
  summary: 'PDF renderer for DIN 5008 and DIN 676 compliant letters and invoices'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
