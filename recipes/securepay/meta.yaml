package:
  name: securepay
  version: "0.4"

source:
  fn: securepay-0.4.tar.gz
  url: https://pypi.python.org/packages/source/s/securepay/securepay-0.4.tar.gz
  md5: 99fb5116feba416b2392b3e35a3749ae
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - securepay = securepay:main
    #
    # Would create an entry point called securepay that calls securepay.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - lxml >=3.3.3
    - pytz >=2012c
    - future >=0.14.2

  run:
    - python
    - lxml >=3.3.3
    - pytz >=2012c
    - future >=0.14.2

test:
  # Python imports
  imports:
    - securepay

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://gitlab.com/Sturm/python-securepay
  license: Apache Software License
  summary: 'Python interface to the Securepay payment processor.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
