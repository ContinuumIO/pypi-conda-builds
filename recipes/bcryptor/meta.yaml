package:
  name: bcryptor
  version: "1.2.2"

source:
  fn: Bcryptor-1.2.2.tar.gz
  url: https://pypi.python.org/packages/source/B/Bcryptor/Bcryptor-1.2.2.tar.gz
  md5: 681c3a0ec16af088e1b34f9af1f7e758
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bcryptor = bcryptor:main
    #
    # Would create an entry point called bcryptor that calls bcryptor.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cython
    - yamlog

  run:
    - python
    - cython
    - yamlog

test:
  # Python imports
  imports:
    - bcryptor

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.bitbucket.org/ares/bcryptor/
  license: ISC License (ISCL)
  summary: 'Python wrapper for bcrypt'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
