package:
  name: cisco_vpn
  version: "0.0.14"

source:
  fn: cisco_vpn-0.0.14.tar.gz
  url: https://pypi.python.org/packages/source/c/cisco_vpn/cisco_vpn-0.0.14.tar.gz
  md5: 220388e6b735e0b8ea35373e95c48fc7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cisco_vpn = cisco_vpn:main
    #
    # Would create an entry point called cisco_vpn that calls cisco_vpn.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - docopt
    - pexpect

  run:
    - python
    - docopt
    - pexpect

test:
  # Python imports
  imports:
    - cisco_vpn
    - lib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/phodal/vpnc
  license: MIT License
  summary: 'Connect Cisco VPN With Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
