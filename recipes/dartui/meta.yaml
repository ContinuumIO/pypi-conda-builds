package:
  name: dartui
  version: "1.1.0"

source:
  fn: DarTui-1.1.0.tar.gz
  url: https://pypi.python.org/packages/source/D/DarTui/DarTui-1.1.0.tar.gz
  md5: d65058b3b96245dfa56aa363b722ab71
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dartui = dartui:main
    #
    # Would create an entry point called dartui that calls dartui.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - web.py >=0.36
    - rtorrent-python >=0.2.7
    - simplejson

  run:
    - python
    - web.py >=0.36
    - rtorrent-python >=0.2.7
    - simplejson

test:
  # Python imports
  imports:
    - dartui
    - dartui.sql

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/cjlucas/dartui
  license: MIT License
  summary: 'An rTorrent web interface with a focus on simplicity, speed and responsiveness'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
