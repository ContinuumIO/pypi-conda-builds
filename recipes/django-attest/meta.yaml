package:
  name: django-attest
  version: "0.10.0"

source:
  fn: django-attest-0.10.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-attest/django-attest-0.10.0.tar.gz
  md5: 84da97bf288e0decc659eefa12726197
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-attest = django-attest:main
    #
    # Would create an entry point called django-attest that calls django-attest.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.2
    - attest >=0.6dev
    - distribute
    - six

  run:
    - python
    - setuptools
    - django >=1.2
    - attest >=0.6dev
    - distribute
    - six

test:
  # Python imports
  imports:
    - django_attest

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bradleyayers/django-attest/
  license: Simplified BSD
  summary: 'Provides Django specific testing helpers to Attest'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
