package:
  name: chattyparallel
  version: "0.3"

source:
  fn: chattyparallel-0.3.tar.gz
  url: https://pypi.python.org/packages/source/c/chattyparallel/chattyparallel-0.3.tar.gz
  md5: 5b4642016c085fe72144013f24cd351e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - chattyparallel = chattyparallel:main
    #
    # Would create an entry point called chattyparallel that calls chattyparallel.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - chattyparallel

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.python.org/pypi/chattyparallel
  license: MIT License
  summary: "Offload tasks to child processes, using a fork based processing model. A variation on Paul Boddie's Parallel."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
