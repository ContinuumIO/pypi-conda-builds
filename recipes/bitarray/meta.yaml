package:
  name: bitarray
  version: "0.8.1"

source:
  fn: bitarray-0.8.1.tar.gz
  url: https://pypi.python.org/packages/source/b/bitarray/bitarray-0.8.1.tar.gz
  md5: 3825184f54f4d93508a28031b4c65d3b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bitarray = bitarray:main
    #
    # Would create an entry point called bitarray that calls bitarray.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - bitarray

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ilanschnell/bitarray
  license: Python Software Foundation License
  summary: 'efficient arrays of booleans -- C extension'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
