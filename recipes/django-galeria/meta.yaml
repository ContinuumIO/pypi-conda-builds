package:
  name: django-galeria
  version: "0.5"

source:
  fn: django-galeria-0.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-galeria/django-galeria-0.5.tar.gz
  md5: ba5d33b9f96f51124c90d644269a57f3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-galeria = django-galeria:main
    #
    # Would create an entry point called django-galeria that calls django-galeria.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-mptt

  run:
    - python
    - django-mptt

test:
  # Python imports
  imports:
    - galeria
    - galeria.migrations
    - galeria.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/semente/django-galeria/
  license: BSD License
  summary: 'Pluggable gallery/portfolio application for Django projects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
