package:
  name: pybill
  version: "1.1.0"

source:
  fn: pybill-1.1.0.tar.gz
  url: https://pypi.python.org/packages/source/p/pybill/pybill-1.1.0.tar.gz
  md5: 8ef3d5d9d7ef6e98de478f938a69831c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pybill = pybill:main
    #
    # Would create an entry point called pybill that calls pybill.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - pybill
    - pybill.commandline
    - pybill.lib
    - pybill.lib.accounting
    - pybill.lib.config
    - pybill.lib.entities
    - pybill.lib.entities.accounting_docs
    - pybill.lib.pdfwriters
    - pybill.lib.pdfwriters.flowgenerators
    - pybill.lib.pdfwriters.templates
    - pybill.lib.xmlreaders
    - pybill.lib.xmlwriters
    - pybill.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.logilab.org/project/pybill
  license: GPL
  summary: 'PDF formatting tool for bills'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
