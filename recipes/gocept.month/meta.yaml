package:
  name: gocept.month
  version: "1.3"

source:
  fn: gocept.month-1.3.zip
  url: https://pypi.python.org/packages/source/g/gocept.month/gocept.month-1.3.zip
  md5: 55099cf1e403cb6082e05164ac414dc3
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gocept.month = gocept.month:main
    #
    # Would create an entry point called gocept.month that calls gocept.month.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.component
    - zope.i18nmessageid
    - zope.interface
    - zope.schema

  run:
    - python
    - setuptools
    - zope.component
    - zope.i18nmessageid
    - zope.interface
    - zope.schema

test:
  # Python imports
  imports:
    - gocept
    - gocept.month
    - gocept.month.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/gocept.month
  license: ZPL 2.1
  summary: 'A datatype which stores a year and a month.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
