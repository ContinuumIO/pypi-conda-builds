package:
  name: vs.bootstrap.plonetheme
  version: "1.0.6"

source:
  fn: vs.bootstrap.plonetheme-1.0.6.tar.gz
  url: https://pypi.python.org/packages/source/v/vs.bootstrap.plonetheme/vs.bootstrap.plonetheme-1.0.6.tar.gz
  md5: 80c8556777882af1dff469b452c78125
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - vs.bootstrap.plonetheme = vs.bootstrap.plonetheme:main
    #
    # Would create an entry point called vs.bootstrap.plonetheme that calls vs.bootstrap.plonetheme.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - vs.bootstrap.tinymce

  run:
    - python
    - setuptools
    - setuptools
    - vs.bootstrap.tinymce

test:
  # Python imports
  imports:
    - vs
    - vs.bootstrap
    - vs.bootstrap.plonetheme
    - vs.bootstrap.plonetheme.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/veit/vs.bootstrap.plonetheme
  license: gpl
  summary: 'Bootstrap theme for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
