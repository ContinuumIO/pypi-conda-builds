package:
  name: djinn_forms
  version: "1.3.2"

source:
  fn: djinn_forms-1.3.2.tar.gz
  url: https://pypi.python.org/packages/source/d/djinn_forms/djinn_forms-1.3.2.tar.gz
  md5: ae67bb8056cbbfc7ca5921f1bbeb9f01
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djinn_forms = djinn_forms:main
    #
    # Would create an entry point called djinn_forms that calls djinn_forms.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - djinn_core
    - djinn_contenttypes

  run:
    - python
    - setuptools
    - django
    - djinn_core
    - djinn_contenttypes

test:
  # Python imports
  imports:
    - djinn_forms
    - djinn_forms.fields
    - djinn_forms.forms
    - djinn_forms.templatetags
    - djinn_forms.views
    - djinn_forms.widgets

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/PythonUnited/djinn-forms
  license: beer-ware
  summary: 'Djinn Intranet Forms'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
