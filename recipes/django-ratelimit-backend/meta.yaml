package:
  name: django-ratelimit-backend
  version: "0.6.4"

source:
  fn: django-ratelimit-backend-0.6.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-ratelimit-backend/django-ratelimit-backend-0.6.4.tar.gz
  md5: 0650451a20da55119b1b7b442dcd0ab8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-ratelimit-backend = django-ratelimit-backend:main
    #
    # Would create an entry point called django-ratelimit-backend that calls django-ratelimit-backend.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - ratelimitbackend
    - ratelimitbackend.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/brutasse/django-ratelimit-backend
  license: BSD License
  summary: 'Login rate-limiting at the auth backend level'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
