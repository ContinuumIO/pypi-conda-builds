package:
  name: py_static_check
  version: "1.2"

source:
  fn: py_static_check-1.2.tar.gz
  url: https://pypi.python.org/packages/source/p/py_static_check/py_static_check-1.2.tar.gz
  md5: eb9abc0f6d29fd40413684d7536c3f41
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - py_static_check = py_static_check:main
    #
    # Would create an entry point called py_static_check that calls py_static_check.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyflakes >=0.5

  run:
    - python
    - pyflakes >=0.5

test:
  # Python imports
  imports:
    - py_static_check

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.amix.dk/
  license: MIT License
  summary: 'Statically check your Python code'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
