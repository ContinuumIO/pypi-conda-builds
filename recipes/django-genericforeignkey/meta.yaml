package:
  name: django-genericforeignkey
  version: "0.60.4"

source:
  fn: django-genericforeignkey-0.60.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-genericforeignkey/django-genericforeignkey-0.60.4.tar.gz
  md5: f999ca61d0b9f4ce3bd08005df1506fb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-genericforeignkey = django-genericforeignkey:main
    #
    # Would create an entry point called django-genericforeignkey that calls django-genericforeignkey.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - genericforeignkey

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://tracpub.yaco.es/djangoapps/wiki/GenericForeignKey
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Django application that allows yo yout generic relations have a nice admin widget'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
