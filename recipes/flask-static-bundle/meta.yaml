package:
  name: flask-static-bundle
  version: "0.1.3"

source:
  fn: flask-static-bundle-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/f/flask-static-bundle/flask-static-bundle-0.1.3.tar.gz
  md5: ded44512c292fc742bb164da6f641dd0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-static-bundle = flask-static-bundle:main
    #
    # Would create an entry point called flask-static-bundle that calls flask-static-bundle.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - flask
    - static-bundle

  run:
    - python
    - flask
    - static-bundle

test:
  # Python imports
  imports:
    - flask_static_bundle

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/Rikanishu/flask-static-bundle
  license: BSD License
  summary: 'Flask extension for static-bundle'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
