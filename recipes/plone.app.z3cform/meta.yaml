package:
  name: plone.app.z3cform
  version: "1.1.1"

source:
  fn: plone.app.z3cform-1.1.1.tar.gz
  url: https://pypi.python.org/packages/source/p/plone.app.z3cform/plone.app.z3cform-1.1.1.tar.gz
  md5: 777b53a69a8aa69ab584171bd8acbe6f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.app.z3cform = plone.app.z3cform:main
    #
    # Would create an entry point called plone.app.z3cform that calls plone.app.z3cform.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - acquisition
    - products.cmfcore
    - products.cmfplone
    - zope2
    - plone.app.widgets >=2.0.0.dev0
    - plone.protect
    - plone.z3cform >=0.5.11dev
    - z3c.form >=3.0
    - z3c.formwidget.query
    - zope.browserpage
    - zope.component
    - zope.globalrequest
    - zope.i18n
    - zope.i18nmessageid
    - zope.interface
    - zope.schema
    - zope.traversing
    - plone.app.textfield

  run:
    - python
    - setuptools
    - acquisition
    - products.cmfcore
    - products.cmfplone
    - zope2
    - plone.app.widgets >=2.0.0.dev0
    - plone.protect
    - plone.z3cform >=0.5.11dev
    - z3c.form >=3.0
    - z3c.formwidget.query
    - zope.browserpage
    - zope.component
    - zope.globalrequest
    - zope.i18n
    - zope.i18nmessageid
    - zope.interface
    - zope.schema
    - zope.traversing
    - plone.app.textfield

test:
  # Python imports
  imports:
    - plone
    - plone.app
    - plone.app.z3cform
    - plone.app.z3cform.queryselect
    - plone.app.z3cform.tests
    - plone.app.z3cform.wysiwyg

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/plone.app.z3cform
  license: GPL
  summary: 'A collection of widgets, templates and other components for use with z3c.form and Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
