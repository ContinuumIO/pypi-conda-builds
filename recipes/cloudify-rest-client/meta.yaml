package:
  name: cloudify-rest-client
  version: "3.2a8"

source:
  fn: cloudify-rest-client-3.2a8.tar.gz
  url: https://pypi.python.org/packages/source/c/cloudify-rest-client/cloudify-rest-client-3.2a8.tar.gz
  md5: 4669b0842bcd5747e784a7c13d245aac
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cloudify-rest-client = cloudify-rest-client:main
    #
    # Would create an entry point called cloudify-rest-client that calls cloudify-rest-client.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests <=2.5.1

  run:
    - python
    - requests <=2.5.1

test:
  # Python imports
  imports:
    - cloudify_rest_client

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: LICENSE
  summary: 'Cloudify REST client'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
