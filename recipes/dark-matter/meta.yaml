package:
  name: dark-matter
  version: "1.0.0"

source:
  fn: dark-matter-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/d/dark-matter/dark-matter-1.0.0.tar.gz
  md5: fa1ee79bf8bc2a7b4e4926a15513e1f7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dark-matter = dark-matter:main
    #
    # Would create an entry point called dark-matter that calls dark-matter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - dark

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/acorg/dark-matter
  license: MIT License
  summary: 'Python classes to help with virus discovery from genetic sequence data'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
