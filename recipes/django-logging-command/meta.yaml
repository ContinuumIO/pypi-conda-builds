package:
  name: django-logging-command
  version: "1.00"

source:
  fn: django-logging-command-1.00.tar.gz
  url: https://pypi.python.org/packages/source/d/django-logging-command/django-logging-command-1.00.tar.gz
  md5: 72f363f98e93f4cdfab0c5804f31df15
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-logging-command = django-logging-command:main
    #
    # Would create an entry point called django-logging-command that calls django-logging-command.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/defcube/django-logging-command/
  license: UNKNOWN
  summary: 'A mixin to make custom Django management commands set the logger to an appropriate level'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
