package:
  name: hgapi
  version: "1.7.2"

source:
  fn: hgapi-1.7.2.tar.gz
  url: https://pypi.python.org/packages/source/h/hgapi/hgapi-1.7.2.tar.gz
  md5: 7ccb6b331b3bb736a5726fdb92d12abd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hgapi = hgapi:main
    #
    # Would create an entry point called hgapi that calls hgapi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - hgapi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/haard/hgapi
  license: BSD License or MIT License
  summary: 'Python API to Mercurial using the command-line interface'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
