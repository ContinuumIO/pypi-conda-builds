package:
  name: gevent_utils
  version: "0.0.2"

source:
  fn: gevent_utils-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/g/gevent_utils/gevent_utils-0.0.2.tar.gz
  md5: 3440c5266eb8ca66c47af278d221bae4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gevent_utils = gevent_utils:main
    #
    # Would create an entry point called gevent_utils that calls gevent_utils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - gevent_utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://traviscline.github.com/gevent-utils
  license: UNKNOWN
  summary: 'miscellaneous gevent utilities'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
