package:
  name: idisplay
  version: "0.1.2"

source:
  fn: idisplay-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/i/idisplay/idisplay-0.1.2.tar.gz
  md5: 981dca5d6887321d2acaec5fc2502eff
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - idisplay = idisplay:main
    #
    # Would create an entry point called idisplay that calls idisplay.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - ipython >=1.0

  run:
    - python
    - ipython >=1.0

test:
  # Python imports
  imports:
    - idisplay

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lebedov/idisplay/
  license: BSD License
  summary: 'IPython rich display magic functions'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
