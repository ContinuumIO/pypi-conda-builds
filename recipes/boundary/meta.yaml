package:
  name: boundary
  version: "0.0.7"

source:
  fn: boundary-0.0.7.tar.gz
  url: https://pypi.python.org/packages/source/b/boundary/boundary-0.0.7.tar.gz
  md5: f48894e643c019f6fd844eb595b1e58f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - boundary = boundary:main
    #
    # Would create an entry point called boundary that calls boundary.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - requests >=2.3.0
    - python-dateutil >=2.4.1

  run:
    - python
    - requests >=2.3.0
    - python-dateutil >=2.4.1

test:
  # Python imports
  imports:
    - boundary

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/boundary/boundary-api-cli
  license: LICENSE.txt
  summary: 'Command line interface to Boundary REST APIs'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
