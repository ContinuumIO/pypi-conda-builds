package:
  name: beyonic
  version: "0.1.4a1"

source:
  fn: beyonic-0.1.4a1.tar.gz
  url: https://pypi.python.org/packages/source/b/beyonic/beyonic-0.1.4a1.tar.gz
  md5: 871a72578effe7d9c1d5dcd048394a5e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - beyonic = beyonic:main
    #
    # Would create an entry point called beyonic that calls beyonic.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - beyonic
    - beyonic.apis

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://beyonic.com
  license: MIT License
  summary: 'The official Python client for the Beyonic.com API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
