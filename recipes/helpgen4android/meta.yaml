package:
  name: helpgen4android
  version: "0.3.5"

source:
  fn: HelpGen4Android-0.3.5.tar.gz
  url: https://pypi.python.org/packages/source/H/HelpGen4Android/HelpGen4Android-0.3.5.tar.gz
  md5: 04864f7e0e875f9ace2cba8ad054255a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - helpgen4android = helpgen4android:main
    #
    # Would create an entry point called helpgen4android that calls helpgen4android.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - beautifulsoup4

  run:
    - python
    - beautifulsoup4

test:
  # Python imports
  imports:
    - HelpGen4Android
    - helpgen4android

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Eagles2F/HelpGen4Android
  license: MIT License
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
