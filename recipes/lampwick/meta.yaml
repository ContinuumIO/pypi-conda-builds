package:
  name: lampwick
  version: "0.2.1"

source:
  fn: lampwick-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/l/lampwick/lampwick-0.2.1.tar.gz
  md5: 2abeba5c20c77e276bcdabf3a72218b2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lampwick = lampwick:main
    #
    # Would create an entry point called lampwick that calls lampwick.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - nose
    - pexpect
    - dateutils

  run:
    - python
    - nose
    - pexpect
    - dateutils

test:
  # Python imports
  imports:
    - lampwick
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/sdispater/lampwick
  license: MIT
  summary: 'Simple wrappper around ffmpeg commands.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
