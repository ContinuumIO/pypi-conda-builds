package:
  name: django-smtp-alternative
  version: "1.0.0"

source:
  fn: django-smtp-alternative-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-smtp-alternative/django-smtp-alternative-1.0.0.tar.gz
  md5: 5e52c7a5eec61dab39fdc9fef08a5ffc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-smtp-alternative = django-smtp-alternative:main
    #
    # Would create an entry point called django-smtp-alternative that calls django-smtp-alternative.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - django_smtp_alternative

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/COEXCZ/django-smtp-alternative
  license: GNU Lesser General Public License v3 (LGPLv3)
  summary: 'Django email backend providing sending with alternative SMTP server if primary server fails'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
