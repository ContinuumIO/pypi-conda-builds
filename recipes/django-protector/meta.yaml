package:
  name: django-protector
  version: "0.2.9"

source:
  fn: django-protector-0.2.9.tar.gz
  url: https://pypi.python.org/packages/source/d/django-protector/django-protector-0.2.9.tar.gz
  md5: b603e4120d9063ba58de51c88fae0f9b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-protector = django-protector:main
    #
    # Would create an entry point called django-protector that calls django-protector.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-mptt >=0.6
    - django >=1.7

  run:
    - python
    - django-mptt >=0.6
    - django >=1.7

test:
  # Python imports
  imports:
    - protector

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/grey0ne/django-protector
  license: MIT License
  summary: 'Django application for managing object level permissions and generic groups'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
