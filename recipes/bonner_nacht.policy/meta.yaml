package:
  name: bonner_nacht.policy
  version: "1.2.0"

source:
  fn: bonner_nacht.policy-1.2.0.zip
  url: https://pypi.python.org/packages/source/b/bonner_nacht.policy/bonner_nacht.policy-1.2.0.zip
  md5: c1d7c54915f832b1452a22b901dd38a8
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bonner_nacht.policy = bonner_nacht.policy:main
    #
    # Would create an entry point called bonner_nacht.policy that calls bonner_nacht.policy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone
    - wasgehtengine.policy
    - bonner_nacht.theme

  run:
    - python
    - setuptools
    - setuptools
    - plone
    - wasgehtengine.policy
    - bonner_nacht.theme

test:
  # Python imports
  imports:
    - bonner_nacht
    - bonner_nacht.policy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://gitorious.org/bonner-nacht
  license: agpl
  summary: 'Bonner Nacht Site Policy'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
