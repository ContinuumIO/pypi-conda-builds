package:
  name: gocept.pytestlayer
  version: "2.1"

source:
  fn: gocept.pytestlayer-2.1.zip
  url: https://pypi.python.org/packages/source/g/gocept.pytestlayer/gocept.pytestlayer-2.1.zip
  md5: a8f7658431a78211c4385a36ef0d90c9
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gocept.pytestlayer = gocept.pytestlayer:main
    #
    # Would create an entry point called gocept.pytestlayer that calls gocept.pytestlayer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pytest
    - setuptools
    - zope.dottedname

  run:
    - python
    - setuptools
    - pytest
    - setuptools
    - zope.dottedname

test:
  # Python imports
  imports:
    - gocept
    - gocept.pytestlayer
    - gocept.pytestlayer.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/gocept/gocept.pytestlayer/
  license: Zope Public License
  summary: 'Integration of zope.testrunner-style test layers into the py.test framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
