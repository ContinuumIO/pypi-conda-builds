package:
  name: sphinx-prompt
  version: "0.2.2"

source:
  fn: sphinx-prompt-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/s/sphinx-prompt/sphinx-prompt-0.2.2.tar.gz
  md5: 134da4780916efadce31c1c6e783689b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sphinx-prompt = sphinx-prompt:main
    #
    # Would create an entry point called sphinx-prompt that calls sphinx-prompt.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sphinx
    - pygments

  run:
    - python
    - sphinx
    - pygments

test:
  # Python imports
  imports:
    - sphinx-prompt

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/sbrunner/sphinx-prompt
  license: BSD License
  summary: '\nSphinx directive to add unselectable prompt\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
