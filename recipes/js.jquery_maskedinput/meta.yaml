package:
  name: js.jquery_maskedinput
  version: "1.3.1"

source:
  fn: js.jquery_maskedinput-1.3.1.tar.gz
  url: https://pypi.python.org/packages/source/j/js.jquery_maskedinput/js.jquery_maskedinput-1.3.1.tar.gz
  md5: 23520e774732f226094e87d52a8f2b67
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - js.jquery_maskedinput = js.jquery_maskedinput:main
    #
    # Would create an entry point called js.jquery_maskedinput that calls js.jquery_maskedinput.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fanstatic
    - js.jquery
    - setuptools

  run:
    - python
    - setuptools
    - fanstatic
    - js.jquery
    - setuptools

test:
  # Python imports
  imports:
    - js
    - js.jquery_maskedinput

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/disko/js.jquery_maskedinput
  license: BSD
  summary: 'Fanstatic packaging of jquery.maskedinput'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
