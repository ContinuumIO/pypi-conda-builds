package:
  name: marmir
  version: "0.1.3"

source:
  fn: Marmir-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/M/Marmir/Marmir-0.1.3.tar.gz
  md5: dfdca534d97759ec73ad9da8aba0342c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - marmir = marmir:main
    #
    # Would create an entry point called marmir that calls marmir.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - marmir
    - mm
    - mm.contrib
    - mm.contrib.django
    - mm.lib
    - mm.lib.font_data
    - mm.lib.xldate
    - mm.lib.xlwt_0_7_2

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://brianray.github.com/mm
  license: UNKNOWN
  summary: 'Marmir: makes spreadsheets easy'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
