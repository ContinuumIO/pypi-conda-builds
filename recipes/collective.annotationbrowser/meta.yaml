package:
  name: collective.annotationbrowser
  version: "0.3"

source:
  fn: collective.annotationbrowser-0.3.zip
  url: https://pypi.python.org/packages/source/c/collective.annotationbrowser/collective.annotationbrowser-0.3.zip
  md5: 95e6866f5801284ec80b1289f0eff9f3
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.annotationbrowser = collective.annotationbrowser:main
    #
    # Would create an entry point called collective.annotationbrowser that calls collective.annotationbrowser.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.annotationbrowser
    - collective.annotationbrowser.browser
    - collective.annotationbrowser.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org
  license: GPL
  summary: 'Browser/editor for Zope annotations'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
