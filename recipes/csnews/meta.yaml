package:
  name: csnews
  version: "2.8"

source:
  fn: csnews-2.8.zip
  url: https://pypi.python.org/packages/source/c/csnews/csnews-2.8.zip
  md5: 93e54cd6521d049df2a4a29a791a98ea
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - csnews = csnews:main
    #
    # Would create an entry point called csnews that calls csnews.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-photologue
    - django-tinymce

  run:
    - python
    - setuptools
    - django-photologue
    - django-tinymce

test:
  # Python imports
  imports:
    - csnews

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.codesyntax.com
  license: GPL
  summary: 'Simple news module'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
