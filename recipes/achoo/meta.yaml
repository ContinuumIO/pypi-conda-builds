package:
  name: achoo
  version: "1.0"

source:
  fn: Achoo-1.0.tar.gz
  url: https://pypi.python.org/packages/source/A/Achoo/Achoo-1.0.tar.gz
  md5: 00bcb086f715c41821d22748d123308c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - achoo = achoo:main
    #
    # Would create an entry point called achoo that calls achoo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - achoo

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://web.quuxo.com/projects/achoo
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'A fluent interface for testing Python objects.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
