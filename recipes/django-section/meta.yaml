package:
  name: django-section
  version: "0.0.3"

source:
  fn: django-section-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-section/django-section-0.0.3.tar.gz
  md5: d74c4177d445847a73b1d8e87495881b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-section = django-section:main
    #
    # Would create an entry point called django-section that calls django-section.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-snippetscream

  run:
    - python
    - django-snippetscream

test:
  # Python imports
  imports:
    - section
    - section.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/praekelt/django-section
  license: BSD License
  summary: 'Django app for determining site section by request.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
