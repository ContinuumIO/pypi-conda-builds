package:
  name: django-shorturls
  version: "1.1.0"

source:
  fn: django-shorturls-1.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-shorturls/django-shorturls-1.1.0.tar.gz
  md5: 8a3146a62752c8b2a835313ea67db701
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-shorturls = django-shorturls:main
    #
    # Would create an entry point called django-shorturls that calls django-shorturls.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - shorturls
    - shorturls.templatetags
    - shorturls.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bfirsh/django-shorturls
  license: BSD License
  summary: 'A URL shortening app for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
