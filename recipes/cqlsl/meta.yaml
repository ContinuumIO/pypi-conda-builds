package:
  name: cqlsl
  version: "0.1.2"

source:
  fn: cqlsl-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/c/cqlsl/cqlsl-0.1.2.tar.gz
  md5: aceb1c17eb326cefd2d142e71714c3a9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cqlsl = cqlsl:main
    #
    # Would create an entry point called cqlsl that calls cqlsl.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cassandra-driver >=2.1.1

  run:
    - python
    - cassandra-driver >=2.1.1

test:
  # Python imports
  imports:
    - cqlsl

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/drudim/cqlsl
  license: MIT License
  summary: 'CQL for Python without any additional abstraction layers'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
