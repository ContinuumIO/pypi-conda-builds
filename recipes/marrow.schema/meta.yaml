package:
  name: marrow.schema
  version: "1.1.1"

source:
  fn: marrow.schema-1.1.1.tar.gz
  url: https://pypi.python.org/packages/source/m/marrow.schema/marrow.schema-1.1.1.tar.gz
  md5: 6f87c96ec7536b6871723701f16b0ea3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - marrow.schema = marrow.schema:main
    #
    # Would create an entry point called marrow.schema that calls marrow.schema.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - marrow
    - marrow.schema
    - marrow.schema.validation

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/marrow/marrow.schema/
  license: MIT License
  summary: 'A generic declarative syntax toolkit for Python objects that uses itself to define itself.  Really.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
