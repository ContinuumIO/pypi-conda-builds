package:
  name: at-hackerspace
  version: "0.2.0"

source:
  fn: at-hackerspace-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/a/at-hackerspace/at-hackerspace-0.2.0.tar.gz
  md5: 6bc9c587c88a75019df70edf527f7900
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - at-hackerspace = at-hackerspace:main
    #
    # Would create an entry point called at-hackerspace that calls at-hackerspace.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - flask >=0.10.1
    - werkzeug >=0.9.6
    - wsgiref >=0.1.2
    - pcapy >=0.10.8
    - pesto >=25
    - iscpy >=1.05

  run:
    - python
    - flask >=0.10.1
    - werkzeug >=0.9.6
    - wsgiref >=0.1.2
    - pcapy >=0.10.8
    - pesto >=25
    - iscpy >=1.05

test:
  # Python imports
  imports:
    - at

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tlevine/at
  license: AGPL
  summary: "Determine who's in the hackerspace based on DHCP leases."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
