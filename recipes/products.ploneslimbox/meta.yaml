package:
  name: products.ploneslimbox
  version: "0.7.1"

source:
  fn: Products.PloneSlimbox-0.7.1.tar.gz
  url: https://pypi.python.org/packages/source/P/Products.PloneSlimbox/Products.PloneSlimbox-0.7.1.tar.gz
  md5: bbfc433b6063580f80d22367d0f3977f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - products.ploneslimbox = products.ploneslimbox:main
    #
    # Would create an entry point called products.ploneslimbox that calls products.ploneslimbox.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - Products
    - Products.PloneSlimbox
    - Products.PloneSlimbox.Extensions

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/plone-slimbox/
  license: MIT License
  summary: 'Slimbox(Lightweight Lightbox clone) for atct album view and COREBlog2 attachment image view for Plone.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
