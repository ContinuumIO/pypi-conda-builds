package:
  name: coinkite-api
  version: "1.0.0"

source:
  fn: coinkite-api-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/c/coinkite-api/coinkite-api-1.0.0.tar.gz
  md5: a2014167aa3113d00a2e6cd45f607bf2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - coinkite-api = coinkite-api:main
    #
    # Would create an entry point called coinkite-api that calls coinkite-api.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - simplejson >=3.2.0
    - python-dateutil >=1.5

  run:
    - python
    - simplejson >=3.2.0
    - python-dateutil >=1.5

test:
  # Python imports
  imports:
    - ckapi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/coinkite/coinkite-python
  license: BSD License
  summary: 'Coinkite API (Python binding)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
