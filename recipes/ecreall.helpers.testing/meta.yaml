package:
  name: ecreall.helpers.testing
  version: "1.7"

source:
  fn: ecreall.helpers.testing-1.7.zip
  url: https://pypi.python.org/packages/source/e/ecreall.helpers.testing/ecreall.helpers.testing-1.7.zip
  md5: 39c8baa1738afc7ee22fb00fadfc54d9
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ecreall.helpers.testing = ecreall.helpers.testing:main
    #
    # Would create an entry point called ecreall.helpers.testing that calls ecreall.helpers.testing.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.testing

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.testing

test:
  # Python imports
  imports:
    - ecreall
    - ecreall.helpers
    - ecreall.helpers.testing

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/
  license: gpl
  summary: 'Helpers for testing often used at Ecreall'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
