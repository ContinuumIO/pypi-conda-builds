package:
  name: collective.bouncing
  version: "0.1.2"

source:
  fn: collective.bouncing-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.bouncing/collective.bouncing-0.1.2.tar.gz
  md5: 3be33a339a159793fc1176507005b208
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.bouncing = collective.bouncing:main
    #
    # Would create an entry point called collective.bouncing that calls collective.bouncing.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - monkey
    - collective.singing
    - collective.dancing

  run:
    - python
    - setuptools
    - setuptools
    - monkey
    - collective.singing
    - collective.dancing

test:
  # Python imports
  imports:
    - collective
    - collective.bouncing
    - collective.bouncing.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: GPL
  summary: 'Mailinglist-integration with collective.singing.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
