package:
  name: atreal.richfile.preview
  version: "0.2.0"

source:
  fn: atreal.richfile.preview-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/a/atreal.richfile.preview/atreal.richfile.preview-0.2.0.tar.gz
  md5: ca402d1f099c495bf0242942e4017f50
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - atreal.richfile.preview = atreal.richfile.preview:main
    #
    # Would create an entry point called atreal.richfile.preview that calls atreal.richfile.preview.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - atreal.richfile.qualifier >=1.1.0
    - products.arofficetransforms >=0.10.0

  run:
    - python
    - setuptools
    - setuptools
    - atreal.richfile.qualifier >=1.1.0
    - products.arofficetransforms >=0.10.0

test:
  # Python imports
  imports:
    - atreal
    - atreal.richfile
    - atreal.richfile.preview
    - atreal.richfile.preview.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/atreal.richfile.preview/
  license: GPL
  summary: 'Preview Support Plugin for RichFileQualifier'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
