package:
  name: ella_attachments
  version: "1.0.3"

source:
  fn: ella_attachments-1.0.3.tar.gz
  url: https://pypi.python.org/packages/source/e/ella_attachments/ella_attachments-1.0.3.tar.gz
  md5: 6108843e60a0e972da40f154debba500
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ella_attachments = ella_attachments:main
    #
    # Would create an entry point called ella_attachments that calls ella_attachments.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - ella >=2,<4
    - south >=0.7

  run:
    - python
    - ella >=2,<4
    - south >=0.7

test:
  # Python imports
  imports:
    - ella_attachments
    - ella_attachments.migrations
    - ella_attachments.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ella/ella-attachments
  license: BSD
  summary: 'Add attachments to Ella project'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
