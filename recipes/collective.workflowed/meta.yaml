package:
  name: collective.workflowed
  version: "1.1"

source:
  fn: collective.workflowed-1.1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.workflowed/collective.workflowed-1.1.tar.gz
  md5: c36c727028153723e10bed35f81138cc
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.workflowed = collective.workflowed:main
    #
    # Would create an entry point called collective.workflowed that calls collective.workflowed.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.wtf

  run:
    - python
    - setuptools
    - setuptools
    - collective.wtf

test:
  # Python imports
  imports:
    - collective
    - collective.workflowed
    - collective.workflowed.browser
    - collective.workflowed.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/collective.workflowed
  license: GPL
  summary: 'A graphical workflow editor for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
