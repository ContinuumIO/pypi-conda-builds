package:
  name: quintagroup.pingtool
  version: "1.1.3"

source:
  fn: quintagroup.pingtool-1.1.3.tar.gz
  url: https://pypi.python.org/packages/source/q/quintagroup.pingtool/quintagroup.pingtool-1.1.3.tar.gz
  md5: ca768cb771817f2b43ab50bebefaf874
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - quintagroup.pingtool = quintagroup.pingtool:main
    #
    # Would create an entry point called quintagroup.pingtool that calls quintagroup.pingtool.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - archetypes.schemaextender

  run:
    - python
    - setuptools
    - setuptools
    - archetypes.schemaextender

test:
  # Python imports
  imports:
    - quintagroup
    - quintagroup.pingtool
    - quintagroup.pingtool.browser
    - quintagroup.pingtool.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://quintagroup.com/services/plone-development/products/ping-tool
  license: GPL
  summary: 'quintagroup.pingtool is a simple tool to enable pinging of external feed agregators for Plone 3.1.x'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
