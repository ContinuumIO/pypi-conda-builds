package:
  name: js.modernizr
  version: "2.5.3.1"

source:
  fn: js.modernizr-2.5.3.1.tar.gz
  url: https://pypi.python.org/packages/source/j/js.modernizr/js.modernizr-2.5.3.1.tar.gz
  md5: b12082bfc586c1efd616f71ef2582400
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - js.modernizr = js.modernizr:main
    #
    # Would create an entry point called js.modernizr that calls js.modernizr.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fanstatic
    - setuptools

  run:
    - python
    - setuptools
    - fanstatic
    - setuptools

test:
  # Python imports
  imports:
    - js
    - js.modernizr

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD
  summary: 'Fanstatic packaging of Modernizr'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
