package:
  name: collective.subtractiveworkflow
  version: "2.0b1"

source:
  fn: collective.subtractiveworkflow-2.0b1.zip
  url: https://pypi.python.org/packages/source/c/collective.subtractiveworkflow/collective.subtractiveworkflow-2.0b1.zip
  md5: 119d24b663a8fdcee6f1430615337581
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.subtractiveworkflow = collective.subtractiveworkflow:main
    #
    # Would create an entry point called collective.subtractiveworkflow that calls collective.subtractiveworkflow.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - rwproperty
    - zope.interface
    - zope.component
    - products.cmfcore
    - products.dcworkflow
    - products.genericsetup

  run:
    - python
    - setuptools
    - setuptools
    - rwproperty
    - zope.interface
    - zope.component
    - products.cmfcore
    - products.dcworkflow
    - products.genericsetup

test:
  # Python imports
  imports:
    - collective
    - collective.subtractiveworkflow
    - collective.subtractiveworkflow.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/collective.subtractiveworkflow
  license: BSD
  summary: 'The DCWorkflow giveth and the SubtractiveWorkflow taketh away.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
