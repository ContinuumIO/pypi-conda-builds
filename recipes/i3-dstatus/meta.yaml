package:
  name: i3-dstatus
  version: "0.0.3"

source:
  fn: i3-dstatus-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/i/i3-dstatus/i3-dstatus-0.0.3.tar.gz
  md5: 5c9375bb57d7834bf29dbbceddc378a1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - i3-dstatus = i3-dstatus:main
    #
    # Would create an entry point called i3-dstatus that calls i3-dstatus.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyyaml
    - netifaces
    - i3ipc
    - ijson

  run:
    - python
    - pyyaml
    - netifaces
    - i3ipc
    - ijson

test:
  # Python imports
  imports:
    - i3dstatus

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/acrisci/i3-dstatus
  license: BSD License
  summary: 'The ultimate DIY statusline generator for i3'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
