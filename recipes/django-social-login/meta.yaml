package:
  name: django-social-login
  version: "0.2.0"

source:
  fn: django-social-login-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-social-login/django-social-login-0.2.0.tar.gz
  md5: 2f7efecfe3a63df7f6ac66967693350d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-social-login = django-social-login:main
    #
    # Would create an entry point called django-social-login that calls django-social-login.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - socialoauth

  run:
    - python
    - socialoauth

test:
  # Python imports
  imports:
    - social_login

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/yueyoum/django-social-login
  license: BSD License
  summary: 'A Django APP for Social account login via OAuth2 Service'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
