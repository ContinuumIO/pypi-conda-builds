package:
  name: sqlitefktg
  version: "0.1.1"

source:
  fn: sqlitefktg-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/s/sqlitefktg/sqlitefktg-0.1.1.tar.gz
  md5: cf118e972da0a08334bfe13b0b1ef8df
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sqlitefktg = sqlitefktg:main
    #
    # Would create an entry point called sqlitefktg that calls sqlitefktg.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - sqlitefktg

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://coobs.eu.org/sqlitefktg/
  license: BSD License
  summary: 'SQLite foreign key trigger generator'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
