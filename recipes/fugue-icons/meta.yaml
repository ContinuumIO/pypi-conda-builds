package:
  name: fugue-icons
  version: "3.5.6"

source:
  fn: fugue-icons-3.5.6.tar.gz
  url: https://pypi.python.org/packages/source/f/fugue-icons/fugue-icons-3.5.6.tar.gz
  md5: e7b2c7e34c1dcf604ea8b2b7f185ed4f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fugue-icons = fugue-icons:main
    #
    # Would create an entry point called fugue-icons that calls fugue-icons.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - distribute

  run:
    - python
    - distribute

test:
  # Python imports
  imports:
    - fugue_icons

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/thesheep/fugue-icons/
  license: MIT License
  summary: 'Fugue Icons packaged for simple use in web apps.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
