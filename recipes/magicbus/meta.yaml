package:
  name: magicbus
  version: "4.0.0b1"

source:
  fn: MagicBus-4.0.0b1.tar.gz
  url: https://pypi.python.org/packages/source/M/MagicBus/MagicBus-4.0.0b1.tar.gz
  md5: 48f55f6b6cf4b3436d2674c9af70f20e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - magicbus = magicbus:main
    #
    # Would create an entry point called magicbus that calls magicbus.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - magicbus
    - magicbus.plugins
    - magicbus.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.cherrypy.org
  license: BSD
  summary: 'An implementation of the Web Site Process Bus'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
