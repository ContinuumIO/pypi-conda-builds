package:
  name: fedmsg_meta_debian
  version: "0.1"

source:
  fn: fedmsg_meta_debian-0.1.tar.gz
  url: https://pypi.python.org/packages/source/f/fedmsg_meta_debian/fedmsg_meta_debian-0.1.tar.gz
  md5: 39bd5042627aff42b4f17cb8fa8c10ec
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fedmsg_meta_debian = fedmsg_meta_debian:main
    #
    # Would create an entry point called fedmsg_meta_debian that calls fedmsg_meta_debian.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fedmsg

  run:
    - python
    - setuptools
    - fedmsg

test:
  # Python imports
  imports:
    - fedmsg_meta_debian

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://anonscm.debian.org/gitweb/?p=users/laarmen-guest/fedmsg_meta_debian.git
  license: Expat
  summary: 'fedmsg metadata providers for Debian deployment'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
