package:
  name: collective.recipe.rsync
  version: "2.2.2"

source:
  fn: collective.recipe.rsync-2.2.2.zip
  url: https://pypi.python.org/packages/source/c/collective.recipe.rsync/collective.recipe.rsync-2.2.2.zip
  md5: 59f598f187ceff01f3814853d3cd673a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.recipe.rsync = collective.recipe.rsync:main
    #
    # Would create an entry point called collective.recipe.rsync that calls collective.recipe.rsync.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout

test:
  # Python imports
  imports:
    - collective
    - collective.recipe
    - collective.recipe.rsync

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://collective.github.com/collective.recipe.rsync
  license: Zope Public License
  summary: 'A zc.buildout recipe to copy files from one location        to another via rsync'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
