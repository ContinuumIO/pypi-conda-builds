package:
  name: horae.attachments
  version: "1.0a1"

source:
  fn: horae.attachments-1.0a1.tar.gz
  url: https://pypi.python.org/packages/source/h/horae.attachments/horae.attachments-1.0a1.tar.gz
  md5: 6b3a5ce5dc286e35a2eb7a28a53fce8a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - horae.attachments = horae.attachments:main
    #
    # Would create an entry point called horae.attachments that calls horae.attachments.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - grok
    - z3c.relationfield
    - horae.core
    - horae.layout
    - horae.properties
    - horae.ticketing

  run:
    - python
    - setuptools
    - setuptools
    - grok
    - z3c.relationfield
    - horae.core
    - horae.layout
    - horae.properties
    - horae.ticketing

test:
  # Python imports
  imports:
    - horae
    - horae.attachments

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: GPL
  summary: 'Provides support for adding attachments to tickets of the Horae resource planning system'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
