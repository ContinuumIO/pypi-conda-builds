package:
  name: django-email-changer
  version: "0.1.2"

source:
  fn: django-email-changer-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-email-changer/django-email-changer-0.1.2.tar.gz
  md5: 3650dd78a96c570d3a5c85a162ea8325
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-email-changer = django-email-changer:main
    #
    # Would create an entry point called django-email-changer that calls django-email-changer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django <1.5

  run:
    - python
    - django <1.5

test:
  # Python imports
  imports:
    - django_email_changer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/themystic/djang_email_changer
  license: MIT
  summary: 'A Django App to let users securely change the email associated with their account.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
