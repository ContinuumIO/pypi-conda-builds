package:
  name: js.jqueryui_bootstrap
  version: "0.0.0"

source:
  fn: js.jqueryui_bootstrap-0.0.0.tar.gz
  url: https://pypi.python.org/packages/source/j/js.jqueryui_bootstrap/js.jqueryui_bootstrap-0.0.0.tar.gz
  md5: d92df2e2559556f553d05843679e9aec
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - js.jqueryui_bootstrap = js.jqueryui_bootstrap:main
    #
    # Would create an entry point called js.jqueryui_bootstrap that calls js.jqueryui_bootstrap.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fanstatic
    - js.jqueryui

  run:
    - python
    - setuptools
    - fanstatic
    - js.jqueryui

test:
  # Python imports
  imports:
    - js
    - js.jqueryui_bootstrap

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD
  summary: 'fanstatic jQuery UI bootstrap theme.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
