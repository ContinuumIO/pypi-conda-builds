package:
  name: redisbayes
  version: "0.1.3"

source:
  fn: redisbayes-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/r/redisbayes/redisbayes-0.1.3.tar.gz
  md5: 26d9c2a240156e1844e59406ce13dc45
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - redisbayes = redisbayes:main
    #
    # Would create an entry point called redisbayes that calls redisbayes.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - redis

  run:
    - python
    - redis

test:
  # Python imports
  imports:
    - redisbayes

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jart/redisbayes
  license: MIT License
  summary: u'Na\xefve Bayesian Text Classifier on Redis'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
