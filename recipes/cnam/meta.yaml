package:
  name: cnam
  version: "0.2"

source:
  fn: cnam-0.2.tar.gz
  url: https://pypi.python.org/packages/source/c/cnam/cnam-0.2.tar.gz
  md5: f9833c58ef75e6b58dfbe926ce43f5eb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cnam = cnam:main
    #
    # Would create an entry point called cnam that calls cnam.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=0.10.1

  run:
    - python
    - requests >=0.10.1

test:
  # Python imports
  imports:
    - cnam

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.opencnam.com
  license: UNLICENSE
  summary: 'A simple CLI program that spits out caller ID name information given a phone number.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
