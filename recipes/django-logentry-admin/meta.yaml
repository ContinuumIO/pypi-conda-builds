package:
  name: django-logentry-admin
  version: "0.1.5"

source:
  fn: django-logentry-admin-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-logentry-admin/django-logentry-admin-0.1.5.tar.gz
  md5: fd3b70c48bc07c4a267c08827578ea6e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-logentry-admin = django-logentry-admin:main
    #
    # Would create an entry point called django-logentry-admin that calls django-logentry-admin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4

  run:
    - python
    - django >=1.4

test:
  # Python imports
  imports:
    - logentry_admin

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/yprez/django-logentry-admin
  license: ISC License (ISCL)
  summary: 'Show all LogEntries in the Django admin site.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
