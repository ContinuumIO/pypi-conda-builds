package:
  name: ajenti-dev-multitool
  version: "1.0.1"

source:
  fn: ajenti-dev-multitool-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/a/ajenti-dev-multitool/ajenti-dev-multitool-1.0.1.tar.gz
  md5: 274456f44327e294f30fd087f01b2a28
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ajenti-dev-multitool = ajenti-dev-multitool:main
    #
    # Would create an entry point called ajenti-dev-multitool that calls ajenti-dev-multitool.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - pyyaml

  run:
    - python
    - pyyaml

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://ajenti.org/
  license: UNKNOWN
  summary: '-'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
