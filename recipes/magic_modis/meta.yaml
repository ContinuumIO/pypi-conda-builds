package:
  name: magic_modis
  version: "0.1.1"

source:
  fn: magic_modis-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/m/magic_modis/magic_modis-0.1.1.tar.gz
  md5: 8455fe8c5dfd4ad0c5244fc1abba7d18
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - magic_modis = magic_modis:main
    #
    # Would create an entry point called magic_modis that calls magic_modis.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - magic_modis
    - magic_modis.core
    - magic_modis.rest

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/MagicMODIS/
  license: LICENSE.txt
  summary: 'Magic MODIS module.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
