package:
  name: python-kmos
  version: "0.3.9"

source:
  fn: python-kmos-0.3.9.tar.gz
  url: https://pypi.python.org/packages/source/p/python-kmos/python-kmos-0.3.9.tar.gz
  md5: acf3e31bacf787b825356fcef2327293
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-kmos = python-kmos:main
    #
    # Would create an entry point called python-kmos that calls python-kmos.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - kmos
    - kmos.gui
    - kmos.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mhoffman/kmos
  license: COPYING
  summary: 'kMC modeling on steroids'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
