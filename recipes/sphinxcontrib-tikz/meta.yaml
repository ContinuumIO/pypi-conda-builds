package:
  name: sphinxcontrib-tikz
  version: "0.4.1"

source:
  fn: sphinxcontrib-tikz-0.4.1.tar.gz
  url: https://pypi.python.org/packages/source/s/sphinxcontrib-tikz/sphinxcontrib-tikz-0.4.1.tar.gz
  md5: 125c5e2a0adb8c70591873343098b271
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sphinxcontrib-tikz = sphinxcontrib-tikz:main
    #
    # Would create an entry point called sphinxcontrib-tikz that calls sphinxcontrib-tikz.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sphinx

  run:
    - python
    - sphinx

test:
  # Python imports
  imports:
    - sphinxcontrib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/philexander/tikz
  license: BSD License
  summary: 'TikZ extension for Sphinx'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
