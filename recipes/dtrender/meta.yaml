package:
  name: dtrender
  version: "0.1"

source:
  fn: dtrender-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/dtrender/dtrender-0.1.tar.gz
  md5: 48e4a528120caee3eb96d84f53474720
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dtrender = dtrender:main
    #
    # Would create an entry point called dtrender that calls dtrender.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django ==1.2.7

  run:
    - python
    - django ==1.2.7

test:
  # Python imports
  imports:
    - dtrender

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/markroddy/dtrender
  license: UNKNOWN
  summary: 'Command Line Rendering of Django Templates'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
