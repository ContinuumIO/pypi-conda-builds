package:
  name: django-auth-iam
  version: "0.1.3"

source:
  fn: django-auth-iam-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-auth-iam/django-auth-iam-0.1.3.tar.gz
  md5: 040b33f60925b64df4c1feb88935c4ca
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-auth-iam = django-auth-iam:main
    #
    # Would create an entry point called django-auth-iam that calls django-auth-iam.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - boto
    - pycrypto
    - py_bcrypt

  run:
    - python
    - boto
    - pycrypto
    - py_bcrypt

test:
  # Python imports
  imports:
    - django_auth_iam

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/viewworld/django-auth-iam/
  license: GNU General Public License (GPL)
  summary: 'Django authentication backend using Amazon IAM'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
