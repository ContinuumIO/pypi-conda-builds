package:
  name: elektrika.openx
  version: "1.3"

source:
  fn: elektrika.openx-1.3.zip
  url: https://pypi.python.org/packages/source/e/elektrika.openx/elektrika.openx-1.3.zip
  md5: 37cc3469e3ee2f678117c6d3742f7ed7
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - elektrika.openx = elektrika.openx:main
    #
    # Would create an entry point called elektrika.openx that calls elektrika.openx.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - elektrika
    - elektrika.openx
    - elektrika.openx.browser
    - elektrika.openx.portlets

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/elektrika.openx
  license: GPL
  summary: 'OpenX invoication code, portlet, viewlets'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
