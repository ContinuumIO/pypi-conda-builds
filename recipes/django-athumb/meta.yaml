package:
  name: django-athumb
  version: "2.0"

source:
  fn: django-athumb-2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-athumb/django-athumb-2.0.tar.gz
  md5: 037f6bbef38d407d2899601a4ec356c5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-athumb = django-athumb:main
    #
    # Would create an entry point called django-athumb that calls django-athumb.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - athumb
    - athumb.backends
    - athumb.management
    - athumb.management.commands
    - athumb.templatetags
    - athumb.upload_handlers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/duointeractive/django-athumb
  license: BSD License
  summary: 'A simple, S3-backed thumbnailer field.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
