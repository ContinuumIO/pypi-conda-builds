package:
  name: django-admin-conf-vars
  version: "0.3.1"

source:
  fn: django-admin-conf-vars-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-admin-conf-vars/django-admin-conf-vars-0.3.1.tar.gz
  md5: cf4073aab212aee35a635cefef1c9cbb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-admin-conf-vars = django-admin-conf-vars:main
    #
    # Would create an entry point called django-admin-conf-vars that calls django-admin-conf-vars.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.7

  run:
    - python
    - django >=1.7

test:
  # Python imports
  imports:
    - django_admin_conf_vars
    - django_admin_conf_vars.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/maoaiz/django-admin-conf-vars
  license: BSD License
  summary: 'A simple Django app to edit configuration variables with the Django admin.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
