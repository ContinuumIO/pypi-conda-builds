package:
  name: sftpclone
  version: "1.0"

source:
  fn: sftpclone-1.0.tar.gz
  url: https://pypi.python.org/packages/source/s/sftpclone/sftpclone-1.0.tar.gz
  md5: d0823215a1944ee5aab8332a93faf3bf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sftpclone = sftpclone:main
    #
    # Would create an entry point called sftpclone that calls sftpclone.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - paramiko

  run:
    - python
    - paramiko

test:
  # Python imports
  imports:
    - sftpclone

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/unbit/sftpclone
  license: MIT License
  summary: 'A tool for cloning/syncing a local directory tree with an SFTP server'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
