package:
  name: flatly
  version: "0.3"

source:
  fn: flatly-0.3.tar.gz
  url: https://pypi.python.org/packages/source/f/flatly/flatly-0.3.tar.gz
  md5: f27f172d59d4b6e58e9840bbe055972a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flatly = flatly:main
    #
    # Would create an entry point called flatly that calls flatly.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyramid

  run:
    - python
    - setuptools
    - pyramid

test:
  # Python imports
  imports:
    - flatly

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/ralphbean/flatly/
  license: BSD
  summary: 'Pyramid scaffold that is flat.  Kind of like Flask.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
