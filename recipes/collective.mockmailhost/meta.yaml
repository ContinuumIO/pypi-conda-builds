package:
  name: collective.mockmailhost
  version: "0.7"

source:
  fn: collective.MockMailHost-0.7.zip
  url: https://pypi.python.org/packages/source/c/collective.MockMailHost/collective.MockMailHost-0.7.zip
  md5: bf166e0376c8dfbb36fb050b49423e6b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.mockmailhost = collective.mockmailhost:main
    #
    # Would create an entry point called collective.mockmailhost that calls collective.mockmailhost.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.MockMailHost
    - collective.MockMailHost.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.mockmailhost
  license: GPL
  summary: 'Used for integration testing with Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
