package:
  name: json_reference
  version: "0.1.0"

source:
  fn: json_reference-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/j/json_reference/json_reference-0.1.0.tar.gz
  md5: 2863953a98a95a9c71aa6e95390194e8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - json_reference = json_reference:main
    #
    # Would create an entry point called json_reference that calls json_reference.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - json-pointer
    - requests

  run:
    - python
    - json-pointer
    - requests

test:
  # Python imports
  imports:
    - json_reference

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/eodolphi/json-reference/
  license: UNKNOWN
  summary: 'Simple implementation of the json-reference spec'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
