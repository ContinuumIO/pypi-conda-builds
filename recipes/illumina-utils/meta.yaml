package:
  name: illumina-utils
  version: "1.3"

source:
  fn: illumina-utils-1.3.tar.gz
  url: https://pypi.python.org/packages/source/i/illumina-utils/illumina-utils-1.3.tar.gz
  md5: 7d251c06b87a86f829d9bbba2ca95297
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - illumina-utils = illumina-utils:main
    #
    # Would create an entry point called illumina-utils that calls illumina-utils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - matplotlib
    - python-levenshtein

  run:
    - python
    - matplotlib
    - python-levenshtein

test:
  # Python imports
  imports:
    - IlluminaUtils
    - IlluminaUtils.lib
    - IlluminaUtils.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/meren/illumina-utils
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'A library and collection of scripts to work with Illumina paired-end data (for CASAVA 1.8+).'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
