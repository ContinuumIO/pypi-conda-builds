package:
  name: django-cacheback
  version: "0.9.1"

source:
  fn: django-cacheback-0.9.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-cacheback/django-cacheback-0.9.1.tar.gz
  md5: cd1ec84d902d42bc026b52292e8c348f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-cacheback = django-cacheback:main
    #
    # Would create an entry point called django-cacheback that calls django-cacheback.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3,<1.7
    - django-celery >=3.0
    - celery <3.2
    - six

  run:
    - python
    - django >=1.3,<1.7
    - django-celery >=3.0
    - celery <3.2
    - six

test:
  # Python imports
  imports:
    - cacheback

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/codeinthehole/django-cacheback
  license: BSD License
  summary: 'Caching library for Django that uses Celery to refresh cache items asynchronously'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
