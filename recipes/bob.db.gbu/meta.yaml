package:
  name: bob.db.gbu
  version: "2.0.1"

source:
  fn: bob.db.gbu-2.0.1.zip
  url: https://pypi.python.org/packages/source/b/bob.db.gbu/bob.db.gbu-2.0.1.zip
  md5: 767ea1c372152b0f4d2b36fe2b332ebb
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bob.db.gbu = bob.db.gbu:main
    #
    # Would create an entry point called bob.db.gbu that calls bob.db.gbu.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - six
    - bob.db.base
    - bob.db.verification.utils

  run:
    - python
    - setuptools
    - setuptools
    - six
    - bob.db.base
    - bob.db.verification.utils

test:
  # Python imports
  imports:
    - bob
    - bob.db
    - bob.db.gbu

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/bioidiap/bob.db.gbu
  license: GNU General Public License v3 (GPLv3)
  summary: 'Database Access API of the Good, the Bad and the Ugly (GBU) image database for Bob'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
