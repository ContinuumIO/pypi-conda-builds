package:
  name: simple_mailbox
  version: "0.1.4"

source:
  fn: simple_mailbox-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/s/simple_mailbox/simple_mailbox-0.1.4.tar.gz
  md5: caa564412345455a9766ec785f0d626f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - simple_mailbox = simple_mailbox:main
    #
    # Would create an entry point called simple_mailbox that calls simple_mailbox.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - simple_mailbox
    - smbox

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://pypi.python.org/pypi/simple_mailbox/
  license: LICENSE.txt
  summary: 'imap utils can be used to get the latest email from specified sender.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
