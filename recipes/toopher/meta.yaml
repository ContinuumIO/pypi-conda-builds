package:
  name: toopher
  version: "1.0.5"

source:
  fn: toopher-1.0.5.tar.gz
  url: https://pypi.python.org/packages/source/t/toopher/toopher-1.0.5.tar.gz
  md5: 77d41c0ff9ee99b222bc17f36a1c0691
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - toopher = toopher:main
    #
    # Would create an entry point called toopher that calls toopher.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - oauth2

  run:
    - python
    - oauth2

test:
  # Python imports
  imports:
    - toopher

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://dev.toopher.com
  license: MIT License
  summary: 'Wrapper library for the Toopher authentication API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
