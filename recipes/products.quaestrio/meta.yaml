package:
  name: products.quaestrio
  version: "0.13.1"

source:
  fn: Products.Quaestrio-0.13.1.zip
  url: https://pypi.python.org/packages/source/P/Products.Quaestrio/Products.Quaestrio-0.13.1.zip
  md5: 900d2f76fe063fc56b84c3c8826dbe81
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - products.quaestrio = products.quaestrio:main
    #
    # Would create an entry point called products.quaestrio that calls products.quaestrio.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - Products
    - Products.Quaestrio
    - Products.Quaestrio.Extensions
    - Products.Quaestrio.browser
    - Products.Quaestrio.content
    - Products.Quaestrio.interfaces
    - Products.Quaestrio.tests
    - Products.Quaestrio.validation

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://svn.plone.org/svn/collective/Products.Quaestrio
  license: GPL
  summary: 'A quizz product sponsorised by Makina Corpus'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
