package:
  name: btrees
  version: "4.1.2"

source:
  fn: BTrees-4.1.2.tar.gz
  url: https://pypi.python.org/packages/source/B/BTrees/BTrees-4.1.2.tar.gz
  md5: 2e074ff0bf0eece3467793b3f27762a6
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - btrees = btrees:main
    #
    # Would create an entry point called btrees that calls btrees.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - persistent
    - zope.interface

  run:
    - python
    - setuptools
    - persistent
    - zope.interface

test:
  # Python imports
  imports:
    - BTrees
    - BTrees.tests
    - btrees

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://packages.python.org/BTrees
  license: Zope Public License
  summary: 'Scalable persistent object containers'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
