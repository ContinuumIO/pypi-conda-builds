package:
  name: goodtests
  version: "1.1.2"

source:
  fn: GoodTests-1.1.2.tar.gz
  url: https://pypi.python.org/packages/source/G/GoodTests/GoodTests-1.1.2.tar.gz
  md5: a73ee9cb8d3764afd3262b2380c79f2b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - goodtests = goodtests:main
    #
    # Would create an entry point called goodtests that calls goodtests.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - goodtests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: GNU Lesser General Public License v2 (LGPLv2)
  summary: 'A fast parallel python unit-testing framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
