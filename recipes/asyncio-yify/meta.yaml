package:
  name: asyncio-yify
  version: "0.0.8"

source:
  fn: asyncio-yify-0.0.8.tar.gz
  url: https://pypi.python.org/packages/source/a/asyncio-yify/asyncio-yify-0.0.8.tar.gz
  md5: 3b861c0a6aa45745ca5ebd08cccc1b13
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - asyncio-yify = asyncio-yify:main
    #
    # Would create an entry point called asyncio-yify that calls asyncio-yify.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - aiohttp ==0.11.0
    - lxml ==3.4.1

  run:
    - python
    - aiohttp ==0.11.0
    - lxml ==3.4.1

test:
  # Python imports
  imports:
    - yify

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/davidyen1124/Asyncio-YIFY
  license: UNKNOWN
  summary: 'Damned fast YIFY parser using Asyncio'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
