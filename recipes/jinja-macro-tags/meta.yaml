package:
  name: jinja-macro-tags
  version: "0.1"

source:
  fn: jinja-macro-tags-0.1.tar.gz
  url: https://pypi.python.org/packages/source/j/jinja-macro-tags/jinja-macro-tags-0.1.tar.gz
  md5: d7e154008608b492cc5d760050335218
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jinja-macro-tags = jinja-macro-tags:main
    #
    # Would create an entry point called jinja-macro-tags that calls jinja-macro-tags.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - jinja2

  run:
    - python
    - jinja2

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/frascoweb/jinja-macro-tags
  license: MIT
  summary: 'Macro loader and tag system to call macros'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
