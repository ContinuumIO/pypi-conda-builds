package:
  name: django-snikt
  version: "1.2.4"

source:
  fn: django-snikt-1.2.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-snikt/django-snikt-1.2.4.tar.gz
  md5: acb3b43dde9e80febb9d27bb3f51d7bf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-snikt = django-snikt:main
    #
    # Would create an entry point called django-snikt that calls django-snikt.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - mach_info

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'Web front end for hardware provisioning.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
