package:
  name: mouse_droid
  version: "0.1.2"

source:
  fn: mouse_droid-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/m/mouse_droid/mouse_droid-0.1.2.tar.gz
  md5: 6ca43f9b4bbf16016c4f8b7ccdeec203
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mouse_droid = mouse_droid:main
    #
    # Would create an entry point called mouse_droid that calls mouse_droid.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyuserinput >=0.1.9
    - netifaces >=0.10.4

  run:
    - python
    - pyuserinput >=0.1.9
    - netifaces >=0.10.4

test:
  # Python imports
  imports:
    - mouse_droid

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/krngrvr09/MouseDroid
  license: UNKNOWN
  summary: 'Control Mouse via pointer'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
