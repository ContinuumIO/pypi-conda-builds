package:
  name: django-fields
  version: "0.3.0"

source:
  fn: django-fields-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-fields/django-fields-0.3.0.tar.gz
  md5: 3b816bf67f58653cda82097652bad7f5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-fields = django-fields:main
    #
    # Would create an entry point called django-fields that calls django-fields.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pycrypto

  run:
    - python
    - pycrypto

test:
  # Python imports
  imports:
    - django_fields

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/svetlyak40wt/django-fields/
  license: BSD License
  summary: 'Django-fields is an application which includes different kinds of models fields.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
