package:
  name: aodag.scaffold
  version: "0.2.1"

source:
  fn: aodag.scaffold-0.2.1.zip
  url: https://pypi.python.org/packages/source/a/aodag.scaffold/aodag.scaffold-0.2.1.zip
  md5: c585e7ebb3a167c5b663722f80ea2287
#  patches:
   # List any patch files here
   # - fix.patch

build:
  # preserve_egg_dir: True
  entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - aodag.scaffold = aodag.scaffold:main
    #
    # Would create an entry point called aodag.scaffold that calls aodag.scaffold.main()

    - scaffold=aodag.scaffold.commands:main

  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - aodag
    - aodag.scaffold

  commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.

    - scaffold --help

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT License
  summary: 'tool for manage scaffolds'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
