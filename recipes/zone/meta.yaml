package:
  name: zone
  version: "0.0.0dev"

source:
  fn: Zone-0.0.0dev.tar.gz
  url: https://pypi.python.org/packages/source/Z/Zone/Zone-0.0.0dev.tar.gz
  md5: 593ea77657f37ea66c39ad9638260c29
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zone = zone:main
    #
    # Would create an entry point called zone that calls zone.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - avro ==1.7.7
    - futures ==2.1.6
    - kids ==0.7.6
    - trollius ==1.0.1

  run:
    - python
    - avro ==1.7.7
    - futures ==2.1.6
    - kids ==0.7.6
    - trollius ==1.0.1

test:
  # Python imports
  imports:
    - zone

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: PRIVATE
  summary: 'Asynchronous RPC framework based on Apache Avro and Trollius'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
