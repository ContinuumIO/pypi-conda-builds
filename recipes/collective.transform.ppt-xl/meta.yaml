package:
  name: collective.transform.ppt-xl
  version: "0.1"

source:
  fn: collective.transform.ppt_xl-0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.transform.ppt-xl/collective.transform.ppt_xl-0.1.tar.gz
  md5: 663508ecf9c892d79121dc20a12db0d0
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.transform.ppt-xl = collective.transform.ppt-xl:main
    #
    # Would create an entry point called collective.transform.ppt-xl that calls collective.transform.ppt-xl.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.transform
    - collective.transform.ppt_xl

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.transform.ppt_xl
  license: GPL
  summary: 'Powerpoint and Excel to HTML transformation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
