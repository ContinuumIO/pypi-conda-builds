package:
  name: collective.anonymousview
  version: "1.1"

source:
  fn: collective.anonymousview-1.1.zip
  url: https://pypi.python.org/packages/source/c/collective.anonymousview/collective.anonymousview-1.1.zip
  md5: 3648464f89871d977f4947aadbcb6a39
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.anonymousview = collective.anonymousview:main
    #
    # Would create an entry point called collective.anonymousview that calls collective.anonymousview.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.anonymousview
    - collective.anonymousview.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/collective.anonymousview
  license: GPL
  summary: 'Allow a loggedin user to view a page as an anonymous user'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
