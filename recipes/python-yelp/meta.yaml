package:
  name: python-yelp
  version: "0.1.1"

source:
  fn: python-yelp-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/p/python-yelp/python-yelp-0.1.1.tar.gz
  md5: d2ebacc7ea600e849fda0adaa807df25
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-yelp = python-yelp:main
    #
    # Would create an entry point called python-yelp that calls python-yelp.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - httplib2

  run:
    - python
    - httplib2

test:
  # Python imports
  imports:
    - yelp

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/adamhadani/python-yelp
  license: Apache Software License
  summary: 'A python client for the Yelp API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
