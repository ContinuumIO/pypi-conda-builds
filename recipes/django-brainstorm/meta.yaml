package:
  name: django-brainstorm
  version: "0.2.1"

source:
  fn: django-brainstorm-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-brainstorm/django-brainstorm-0.2.1.tar.gz
  md5: 7f414b9cb466a083c3793ae351e77a8f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-brainstorm = django-brainstorm:main
    #
    # Would create an entry point called django-brainstorm that calls django-brainstorm.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - brainstorm

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/sunlightlabs/django-brainstorm/
  license: BSD License
  summary: 'Django brainstorming site'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
