package:
  name: leaderboard
  version: "3.3.0"

source:
  fn: leaderboard-3.3.0.tar.gz
  url: https://pypi.python.org/packages/source/l/leaderboard/leaderboard-3.3.0.tar.gz
  md5: feb98af660239581facd7549eca5a5c0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - leaderboard = leaderboard:main
    #
    # Would create an entry point called leaderboard that calls leaderboard.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - redis

  run:
    - python
    - redis

test:
  # Python imports
  imports:
    - leaderboard

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/agoragames/leaderboard-python
  license: MIT License
  summary: 'Leaderboards backed by Redis in Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
