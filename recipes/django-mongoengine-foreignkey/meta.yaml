package:
  name: django-mongoengine-foreignkey
  version: "0.2"

source:
  fn: django-mongoengine-foreignkey-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-mongoengine-foreignkey/django-mongoengine-foreignkey-0.2.tar.gz
  md5: 4a1477fcb9ae55a0d8c3cfee6ee10347
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-mongoengine-foreignkey = django-mongoengine-foreignkey:main
    #
    # Would create an entry point called django-mongoengine-foreignkey that calls django-mongoengine-foreignkey.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - mongofk

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bolster/django-mongoengine-foreignkey
  license: BSD License
  summary: 'An implementation of a foreign key field for mongoengine that references a Django model object.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
