package:
  name: wildcard.notrackingsocial
  version: "1.0a2"

source:
  fn: wildcard.notrackingsocial-1.0a2.zip
  url: https://pypi.python.org/packages/source/w/wildcard.notrackingsocial/wildcard.notrackingsocial-1.0a2.zip
  md5: 4f8c399d176ef2bfdb293808ff0bfeef
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - wildcard.notrackingsocial = wildcard.notrackingsocial:main
    #
    # Would create an entry point called wildcard.notrackingsocial that calls wildcard.notrackingsocial.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.registry

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.registry

test:
  # Python imports
  imports:
    - wildcard
    - wildcard.notrackingsocial
    - wildcard.notrackingsocial.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/wildcard.notrackingsocial
  license: GPL
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
