package:
  name: seccure
  version: "0.3-1-gee95"

source:
  fn: seccure-0.3-1-gee95.tar.gz
  url: https://pypi.python.org/packages/source/s/seccure/seccure-0.3-1-gee95.tar.gz
  md5: c992e5697d94094a8380cf6b55cbeae9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - seccure = seccure:main
    #
    # Would create an entry point called seccure that calls seccure.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pycrypto >=2.6
    - gmpy >=1.15,<2
    - six >=1.2

  run:
    - python
    - pycrypto >=2.6
    - gmpy >=1.15,<2
    - six >=1.2

test:
  # Python imports
  imports:
    - seccure
    - seccure.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/bwesterb/py-seccure
  license: GNU Lesser General Public License v3 (LGPLv3)
  summary: 'SECCURE compatible Elliptic Curve cryptography'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
