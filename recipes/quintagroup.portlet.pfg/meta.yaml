package:
  name: quintagroup.portlet.pfg
  version: "1.1"

source:
  fn: quintagroup.portlet.pfg-1.1.zip
  url: https://pypi.python.org/packages/source/q/quintagroup.portlet.pfg/quintagroup.portlet.pfg-1.1.zip
  md5: 626be57ed68cfc9fd123601efdd482d6
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - quintagroup.portlet.pfg = quintagroup.portlet.pfg:main
    #
    # Would create an entry point called quintagroup.portlet.pfg that calls quintagroup.portlet.pfg.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.ploneformgen

  run:
    - python
    - setuptools
    - setuptools
    - products.ploneformgen

test:
  # Python imports
  imports:
    - quintagroup
    - quintagroup.portlet
    - quintagroup.portlet.pfg
    - quintagroup.portlet.pfg.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://projects.quintagroup.com/products
  license: GPL
  summary: 'Render form created by PloneFormGen package.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
