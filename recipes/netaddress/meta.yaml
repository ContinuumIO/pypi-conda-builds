package:
  name: netaddress
  version: "0.2.2"

source:
  fn: netaddress-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/n/netaddress/netaddress-0.2.2.tar.gz
  md5: 64c8a21b12f6f365a7df7b903ed85687
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - netaddress = netaddress:main
    #
    # Would create an entry point called netaddress that calls netaddress.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyparsing >=1.4.7

  run:
    - python
    - pyparsing >=1.4.7

test:
  # Python imports
  imports:
    - netaddress

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.kraeutler.net/vincent/pub/netaddress
  license: BSD License
  summary: "Validating parser for URI's (RFC 3986)"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
