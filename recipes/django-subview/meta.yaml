package:
  name: django-subview
  version: "0.1.2"

source:
  fn: django-subview-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-subview/django-subview-0.1.2.tar.gz
  md5: 4fe05a2193b48bed7985786ca11a61c2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-subview = django-subview:main
    #
    # Would create an entry point called django-subview that calls django-subview.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - subview
    - subview.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/brstgt
  license: BSD License
  summary: 'Treat Django views as reusable and generic components'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
