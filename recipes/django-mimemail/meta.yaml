package:
  name: django-mimemail
  version: "0.1"

source:
  fn: django-mimemail-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-mimemail/django-mimemail-0.1.tar.gz
  md5: ef77a3b71a49d606b3befc360afb62d5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-mimemail = django-mimemail:main
    #
    # Would create an entry point called django-mimemail that calls django-mimemail.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - beautifulsoup

  run:
    - python
    - django
    - beautifulsoup

test:
  # Python imports
  imports:
    - mimemail

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/madssj/django-mimemail/
  license: BSD
  summary: 'Helper library for sending mime-mails with inline pictures.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
