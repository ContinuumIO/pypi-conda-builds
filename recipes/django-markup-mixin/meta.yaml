package:
  name: django-markup-mixin
  version: "0.1.0"

source:
  fn: django-markup-mixin-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-markup-mixin/django-markup-mixin-0.1.0.tar.gz
  md5: b4a1d46ae6cafd170bc6d6f0e2131125
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-markup-mixin = django-markup-mixin:main
    #
    # Would create an entry point called django-markup-mixin that calls django-markup-mixin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - markup_mixin

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/powellc/django-markup-mixin
  license: BSD License
  summary: 'Add pre-rendered markup fields to your django models with the least work.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
