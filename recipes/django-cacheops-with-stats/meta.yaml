package:
  name: django-cacheops-with-stats
  version: "1.3.1"

source:
  fn: django-cacheops-with-stats-1.3.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-cacheops-with-stats/django-cacheops-with-stats-1.3.1.tar.gz
  md5: 5527913aef6983aa29b9f8071122b062
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-cacheops-with-stats = django-cacheops-with-stats:main
    #
    # Would create an entry point called django-cacheops-with-stats that calls django-cacheops-with-stats.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.2
    - redis >=2.9.1
    - simplejson >=2.2.0
    - six >=1.4.0

  run:
    - python
    - django >=1.2
    - redis >=2.9.1
    - simplejson >=2.2.0
    - six >=1.4.0

test:
  # Python imports
  imports:
    - cacheops
    - cacheops.management
    - cacheops.management.commands
    - cacheops.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/Suor/django-cacheops
  license: BSD License
  summary: 'A slick ORM cache with automatic granular event-driven invalidation for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
