package:
  name: lastfmclient
  version: "0.0.4"

source:
  fn: lastfmclient-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/l/lastfmclient/lastfmclient-0.0.4.tar.gz
  md5: 7bb99f91b05fe2c1d6f8b2a371738a02
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lastfmclient = lastfmclient:main
    #
    # Would create an entry point called lastfmclient that calls lastfmclient.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=1.0.4

  run:
    - python
    - requests >=1.0.4

test:
  # Python imports
  imports:
    - lastfmclient

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jakubroztocil/lastfmclient
  license: BSD
  summary: 'Python client for the Last.fm API with a pythonic interface to all methods,\nincluding auth, etc. An async, Tornado-based client included as well.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
