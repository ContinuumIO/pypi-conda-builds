package:
  name: django-mailtestfield
  version: "1.0.1"

source:
  fn: django-mailtestfield-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-mailtestfield/django-mailtestfield-1.0.1.tar.gz
  md5: 57b6642e81a085d2592f969f4b6774e7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-mailtestfield = django-mailtestfield:main
    #
    # Would create an entry point called django-mailtestfield that calls django-mailtestfield.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - mailtest

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/naremit/django-mailtestfield
  license: BSD License
  summary: "Drop-in replacement for Django's forms.EmailField. Uses http://mailtest.in to validate email addresses. Caches results."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
