package:
  name: anchorman
  version: "0.0.6"

source:
  fn: anchorman-0.0.6.tar.gz
  url: https://pypi.python.org/packages/source/a/anchorman/anchorman-0.0.6.tar.gz
  md5: ad252f724cc493ddc5be74cca77b56ff
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - anchorman = anchorman:main
    #
    # Would create an entry point called anchorman that calls anchorman.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - lxml

  run:
    - python
    - lxml

test:
  # Python imports
  imports:
    - anchorman

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rebeling/anchorman.git
  license: Apache 2.0
  summary: 'Markup terms in text'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
