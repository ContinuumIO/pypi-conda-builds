package:
  name: cubicweb-osmfrance
  version: "0.2.2"

source:
  fn: cubicweb-osmfrance-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/c/cubicweb-osmfrance/cubicweb-osmfrance-0.2.2.tar.gz
  md5: e173222c7b52facfe4450e30d46e5450
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cubicweb-osmfrance = cubicweb-osmfrance:main
    #
    # Would create an entry point called cubicweb-osmfrance that calls cubicweb-osmfrance.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cubicweb >=3.18.3
    - cubicweb-postgis
    - cubicweb-leaflet >=0.3

  run:
    - python
    - cubicweb >=3.18.3
    - cubicweb-postgis
    - cubicweb-leaflet >=0.3

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.cubicweb.org/project/cubicweb-osmfrance
  license: LGPL
  summary: 'Shapefiles (french regions, departements, cantons, communes) from OpenStreetMap'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
