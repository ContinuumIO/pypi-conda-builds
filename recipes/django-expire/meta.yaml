package:
  name: django-expire
  version: "1.0.1"

source:
  fn: django-expire-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-expire/django-expire-1.0.1.tar.gz
  md5: 283184b8a61c1dc59598ca81f40dc56b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-expire = django-expire:main
    #
    # Would create an entry point called django-expire that calls django-expire.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - django_expire
    - django_expire.tests
    - django_expire.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/smileychris/django-expire
  license: BSD License
  summary: 'Django authentication-based session expiration'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
