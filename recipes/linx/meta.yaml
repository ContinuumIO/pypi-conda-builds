package:
  name: linx
  version: "0.1.4"

source:
  fn: linx-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/l/linx/linx-0.1.4.tar.gz
  md5: 7339c2b269bb85fc3c6ae54945e8cfea
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - linx = linx:main
    #
    # Would create an entry point called linx that calls linx.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests
    - argparse

  run:
    - python
    - requests
    - argparse

test:
  # Python imports
  imports:
    - linx

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/andreimarcu/linx-cli
  license: LICENSE.txt
  summary: 'Command line interface for Linx.li'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
