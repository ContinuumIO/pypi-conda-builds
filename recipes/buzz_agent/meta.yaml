package:
  name: buzz_agent
  version: "1.1.10"

source:
  fn: buzz_agent-1.1.10.tar.gz
  url: https://pypi.python.org/packages/source/b/buzz_agent/buzz_agent-1.1.10.tar.gz
  md5: 4417b9792cc6fbb2e796f9f4544ceb60
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - buzz_agent = buzz_agent:main
    #
    # Would create an entry point called buzz_agent that calls buzz_agent.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - buzz_agent

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dantezhu/buzz
  license: MIT
  summary: 'agent for buzz system'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
