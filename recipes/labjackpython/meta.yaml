package:
  name: labjackpython
  version: "4-24-2014"

source:
  fn: LabJackPython-4-24-2014.zip
  url: https://pypi.python.org/packages/source/L/LabJackPython/LabJackPython-4-24-2014.zip
  md5: ebef10e196172805e37a844d29ba96bb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - labjackpython = labjackpython:main
    #
    # Would create an entry point called labjackpython that calls labjackpython.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - labjackpython

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://labjack.com/support/labjackpython
  license: MIT X11
  summary: 'The LabJack python module.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
