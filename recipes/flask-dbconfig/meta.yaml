package:
  name: flask-dbconfig
  version: "0.3.12"

source:
  fn: Flask-DBConfig-0.3.12.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-DBConfig/Flask-DBConfig-0.3.12.tar.gz
  md5: 82e419da5d9e6bea150dfc200431c853
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-dbconfig = flask-dbconfig:main
    #
    # Would create an entry point called flask-dbconfig that calls flask-dbconfig.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - flask

  run:
    - python
    - flask

test:
  # Python imports
  imports:
    - flaskext

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tedw4rd/flask-db-configuration/
  license: MIT License
  summary: 'Configure Flask applications from a local DB'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
