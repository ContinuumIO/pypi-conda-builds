package:
  name: gocept.recipe.env
  version: "1.0"

source:
  fn: gocept.recipe.env-1.0.tar.gz
  url: https://pypi.python.org/packages/source/g/gocept.recipe.env/gocept.recipe.env-1.0.tar.gz
  md5: 72d7f17c49ec4c65db4f7f72c5809e56
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gocept.recipe.env = gocept.recipe.env:main
    #
    # Would create an entry point called gocept.recipe.env that calls gocept.recipe.env.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout

test:
  # Python imports
  imports:
    - gocept
    - gocept.recipe
    - gocept.recipe.env
    - gocept.recipe.env.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: Zope Public License
  summary: 'Mirror environment variables into a buildout section.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
