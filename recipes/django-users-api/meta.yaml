package:
  name: django-users-api
  version: "0.1"

source:
  fn: django-users-api-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-users-api/django-users-api-0.1.tar.gz
  md5: 9fe7daefef4e3fd6a2f10b6ca81d0c65
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-users-api = django-users-api:main
    #
    # Would create an entry point called django-users-api that calls django-users-api.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - users_api
    - users_api.api
    - users_api.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mohabusama/django-users-api
  license: MIT License
  summary: 'Django users RESTful API using Tastypie.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
