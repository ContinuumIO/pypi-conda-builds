package:
  name: collective.address
  version: "1.1"

source:
  fn: collective.address-1.1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.address/collective.address-1.1.tar.gz
  md5: 97a6e3d920c52c5271569ea72fba4005
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.address = collective.address:main
    #
    # Would create an entry point called collective.address that calls collective.address.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.cmfplone
    - plone.app.textfield
    - plone.autoform
    - plone.behavior
    - plone.indexer
    - plone.supermodel
    - pycountry
    - zope.i18nmessageid
    - zope.interface
    - zope.schema

  run:
    - python
    - setuptools
    - setuptools
    - products.cmfplone
    - plone.app.textfield
    - plone.autoform
    - plone.behavior
    - plone.indexer
    - plone.supermodel
    - pycountry
    - zope.i18nmessageid
    - zope.interface
    - zope.schema

test:
  # Python imports
  imports:
    - collective
    - collective.address

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.address
  license: GPL
  summary: 'Dexterity address behavior.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
