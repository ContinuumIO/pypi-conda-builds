package:
  name: h5wqueue
  version: "0.1"

source:
  fn: h5wqueue-0.1.tar.gz
  url: https://pypi.python.org/packages/source/h/h5wqueue/h5wqueue-0.1.tar.gz
  md5: 898a37df5663009bfd7c3a7520c49ea1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - h5wqueue = h5wqueue:main
    #
    # Would create an entry point called h5wqueue that calls h5wqueue.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - tables
    - rq

  run:
    - python
    - tables
    - rq

test:
  # Python imports
  imports:
    - h5wqueue

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/vmolina/h5wqueue
  license: GPLv3
  summary: 'Simple utilities for write hdf5 files using workers'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
