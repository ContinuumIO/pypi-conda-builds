package:
  name: django-selectize
  version: "1.2"

source:
  fn: django-selectize-1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-selectize/django-selectize-1.2.tar.gz
  md5: f762cbab84611eeaa50dd1f42e3f1de4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-selectize = django-selectize:main
    #
    # Would create an entry point called django-selectize that calls django-selectize.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - selectize
    - selectize.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/djangoer/django-selectize
  license: UNKNOWN
  summary: 'django-selectize is a Django app based on Selectize.js that help you to create Select and Multiselect widgets in Django forms.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
