package:
  name: plone.portlet.collection
  version: "2.1.7"

source:
  fn: plone.portlet.collection-2.1.7.zip
  url: https://pypi.python.org/packages/source/p/plone.portlet.collection/plone.portlet.collection-2.1.7.zip
  md5: 3d47ff3689c8feba901ab402be557a13
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.portlet.collection = plone.portlet.collection:main
    #
    # Would create an entry point called plone.portlet.collection that calls plone.portlet.collection.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - plone.memoize
    - plone.portlets
    - plone.app.portlets
    - plone.app.vocabularies
    - plone.app.form

  run:
    - python
    - setuptools
    - plone.memoize
    - plone.portlets
    - plone.app.portlets
    - plone.app.vocabularies
    - plone.app.form

test:
  # Python imports
  imports:
    - plone
    - plone.portlet
    - plone.portlet.collection
    - plone.portlet.collection.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/plone.portlet.collection
  license: GNU General Public License (GPL)
  summary: 'A portlet that fetches results from a collection'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
