package:
  name: collective.captionmanager
  version: "1.0"

source:
  fn: collective.captionmanager-1.0.zip
  url: https://pypi.python.org/packages/source/c/collective.captionmanager/collective.captionmanager-1.0.zip
  md5: 81b60db42628043b887e998227331bc7
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.captionmanager = collective.captionmanager:main
    #
    # Would create an entry point called collective.captionmanager that calls collective.captionmanager.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - captionstransformer
    - plone.namedfile

  run:
    - python
    - setuptools
    - setuptools
    - captionstransformer
    - plone.namedfile

test:
  # Python imports
  imports:
    - collective
    - collective.captionmanager
    - collective.captionmanager.browser
    - collective.captionmanager.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.captionmanager
  license: GPL
  summary: 'Caption manager for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
