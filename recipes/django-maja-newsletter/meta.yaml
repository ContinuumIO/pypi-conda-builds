package:
  name: django-maja-newsletter
  version: "0.5.2"

source:
  fn: django-maja-newsletter-0.5.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-maja-newsletter/django-maja-newsletter-0.5.2.tar.gz
  md5: 3460165ecd5cab3fece8393566dcb77a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-maja-newsletter = django-maja-newsletter:main
    #
    # Would create an entry point called django-maja-newsletter that calls django-maja-newsletter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - html2text
    - python-dateutil ==1.5
    - beautifulsoup
    - django-tagging
    - vobject
    - xlwt
    - xlrd
    - django-filebrowser-no-grappelli-nph
    - django-tinymce

  run:
    - python
    - setuptools
    - html2text
    - python-dateutil ==1.5
    - beautifulsoup
    - django-tagging
    - vobject
    - xlwt
    - xlrd
    - django-filebrowser-no-grappelli-nph
    - django-tinymce

test:
  # Python imports
  imports:
    - maja_newsletter
    - maja_newsletter.admin
    - maja_newsletter.cmsplugin_newsletter
    - maja_newsletter.management
    - maja_newsletter.management.commands
    - maja_newsletter.migrations
    - maja_newsletter.urls
    - maja_newsletter.utils
    - maja_newsletter.views

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://nephila.it/
  license: BSD License
  summary: 'A Django app for sending newsletter by email to a contact list.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
