package:
  name: collective.dexteritytextindexer
  version: "2.0.1"

source:
  fn: collective.dexteritytextindexer-2.0.1.zip
  url: https://pypi.python.org/packages/source/c/collective.dexteritytextindexer/collective.dexteritytextindexer-2.0.1.zip
  md5: 789aced84aaca9db93677eddaae9df8c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.dexteritytextindexer = collective.dexteritytextindexer:main
    #
    # Would create an entry point called collective.dexteritytextindexer that calls collective.dexteritytextindexer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zodb3
    - zope.interface
    - zope.schema
    - zope.component
    - zope.deferredimport
    - z3c.form
    - plone.indexer
    - plone.behavior
    - plone.dexterity
    - plone.app.dexterity
    - plone.supermodel >=1.1.1
    - plone.z3cform
    - products.cmfcore

  run:
    - python
    - setuptools
    - setuptools
    - zodb3
    - zope.interface
    - zope.schema
    - zope.component
    - zope.deferredimport
    - z3c.form
    - plone.indexer
    - plone.behavior
    - plone.dexterity
    - plone.app.dexterity
    - plone.supermodel >=1.1.1
    - plone.z3cform
    - products.cmfcore

test:
  # Python imports
  imports:
    - collective
    - collective.dexteritytextindexer
    - collective.dexteritytextindexer.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/collective/collective.dexteritytextindexer
  license: GPL2
  summary: 'Dynamic SearchableText index for dexterity content types'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
