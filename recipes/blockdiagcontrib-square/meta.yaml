package:
  name: blockdiagcontrib-square
  version: "0.1.3"

source:
  fn: blockdiagcontrib-square-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/b/blockdiagcontrib-square/blockdiagcontrib-square-0.1.3.tar.gz
  md5: 5f3ed5e2fc2c46adb1d76abd91279b02
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - blockdiagcontrib-square = blockdiagcontrib-square:main
    #
    # Would create an entry point called blockdiagcontrib-square that calls blockdiagcontrib-square.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - blockdiag >=1.1.0
    - setuptools

  run:
    - python
    - blockdiag >=1.1.0
    - setuptools

test:
  # Python imports
  imports:
    - blockdiagcontrib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://blockdiag.com/
  license: Python Software Foundation License
  summary: 'noderenderer plugin for blockdiag'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
