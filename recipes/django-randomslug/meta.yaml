package:
  name: django-randomslug
  version: "0.1.3"

source:
  fn: django-randomslug-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-randomslug/django-randomslug-0.1.3.tar.gz
  md5: 4b529036831d6fbff0e6e50e81f04413
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-randomslug = django-randomslug:main
    #
    # Would create an entry point called django-randomslug that calls django-randomslug.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - randomslug
    - randomslug.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/TvrtkoM/django-randomslug
  license: MIT
  summary: 'reusable Django application providing the custom RandomSlugField model field'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
