package:
  name: auth_pubtkt
  version: "0.1"

source:
  fn: auth_pubtkt-0.1.tar.gz
  url: https://pypi.python.org/packages/source/a/auth_pubtkt/auth_pubtkt-0.1.tar.gz
  md5: 902adf41e794343b9865932e0a96a754
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - auth_pubtkt = auth_pubtkt:main
    #
    # Would create an entry point called auth_pubtkt that calls auth_pubtkt.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - auth_pubtkt
    - auth_pubtkt.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/AndreyPlotnikov/auth_pubtkt
  license: BSD License
  summary: 'This software implements mod_auth_pubtkt authentication for Python world.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
