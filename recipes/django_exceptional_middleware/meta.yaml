package:
  name: django_exceptional_middleware
  version: "1.2.1"

source:
  fn: django_exceptional_middleware-1.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django_exceptional_middleware/django_exceptional_middleware-1.2.1.tar.gz
  md5: 5fed4791a5f9a6dbb59d62a7df12f072
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_exceptional_middleware = django_exceptional_middleware:main
    #
    # Would create an entry point called django_exceptional_middleware that calls django_exceptional_middleware.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_exceptional_middleware
    - exceptional_middleware

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://tartarus.org/james/computers/django/
  license: MIT
  summary: 'Django middleware to allow generating arbitrary HTTP status codes via exceptions.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
