package:
  name: django-compositekey
  version: "1.5.0"

source:
  fn: django-compositekey-1.5.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-compositekey/django-compositekey-1.5.0.tar.gz
  md5: 40d03f25cce6323f3d921d59c99e5e33
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-compositekey = django-compositekey:main
    #
    # Would create an entry point called django-compositekey that calls django-compositekey.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - compositekey
    - compositekey.core
    - compositekey.core.management
    - compositekey.db
    - compositekey.db.backends
    - compositekey.db.backends.oracle
    - compositekey.db.backends.postgresql_psycopg2
    - compositekey.db.models
    - compositekey.db.models.fields
    - compositekey.db.models.sql
    - compositekey.forms

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/simone/django-compositekey
  license: BSD License
  summary: 'Use Django with embedded database that have composite multicolumn primary key and multiple foreignkeys'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
