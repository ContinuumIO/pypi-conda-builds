package:
  name: freeseer
  version: "3.0.1"

source:
  fn: freeseer-3.0.1.tar.gz
  url: https://pypi.python.org/packages/source/f/freeseer/freeseer-3.0.1.tar.gz
  md5: 4667d10a5ea80ecea00d2fa2f98dbac7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - freeseer = freeseer:main
    #
    # Would create an entry point called freeseer that calls freeseer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - freeseer
    - freeseer.framework
    - freeseer.frontend
    - freeseer.frontend.configtool
    - freeseer.frontend.controller
    - freeseer.frontend.qtcommon
    - freeseer.frontend.record
    - freeseer.frontend.reporteditor
    - freeseer.frontend.talkeditor

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/Freeseer/freeseer
  license: GNU General Public License v3 (GPLv3)
  summary: 'Video recording and streaming software'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
