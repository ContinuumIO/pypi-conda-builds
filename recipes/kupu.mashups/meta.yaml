package:
  name: kupu.mashups
  version: "0.2"

source:
  fn: kupu.mashups-0.2.tar.gz
  url: https://pypi.python.org/packages/source/k/kupu.mashups/kupu.mashups-0.2.tar.gz
  md5: 1b632c18ff7ba88eb233d047eabc36cf
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kupu.mashups = kupu.mashups:main
    #
    # Would create an entry point called kupu.mashups that calls kupu.mashups.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - kupu
    - kupu.mashups
    - kupu.mashups.browser
    - kupu.mashups.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/kupu,mashups
  license: GPL
  summary: 'Relate Google Searches, Google Maps and Wikipedia articles with your content through kupu.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
