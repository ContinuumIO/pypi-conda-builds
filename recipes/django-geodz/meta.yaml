package:
  name: django-geodz
  version: "0.5.1"

source:
  fn: django-geodz-0.5.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-geodz/django-geodz-0.5.1.tar.gz
  md5: 7d1dfd9354d39044d6c7e4814df55aa0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-geodz = django-geodz:main
    #
    # Would create an entry point called django-geodz that calls django-geodz.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - django-geoposition

  run:
    - python
    - django
    - django-geoposition

test:
  # Python imports
  imports:
    - geodz

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://www.github.com/redouane/django-geodz
  license: BSD License
  summary: 'A simple django app that provides out of the box algerian province and municipality models and their respective data fixtures.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
