package:
  name: plone.outputfilters
  version: "2.0"

source:
  fn: plone.outputfilters-2.0.zip
  url: https://pypi.python.org/packages/source/p/plone.outputfilters/plone.outputfilters-2.0.zip
  md5: b4cb62d772fcb37c4301493c74aed60d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.outputfilters = plone.outputfilters:main
    #
    # Would create an entry point called plone.outputfilters that calls plone.outputfilters.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.cmfcore
    - products.genericsetup
    - products.mimetypesregistry
    - products.portaltransforms

  run:
    - python
    - setuptools
    - setuptools
    - products.cmfcore
    - products.genericsetup
    - products.mimetypesregistry
    - products.portaltransforms

test:
  # Python imports
  imports:
    - plone
    - plone.outputfilters
    - plone.outputfilters.browser
    - plone.outputfilters.filters
    - plone.outputfilters.tests
    - plone.outputfilters.transforms

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/plone/plone.outputfilters
  license: GPL
  summary: 'Transformations applied to HTML in Plone text fields as they are rendered'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
