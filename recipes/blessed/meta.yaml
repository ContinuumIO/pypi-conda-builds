package:
  name: blessed
  version: "1.9.5"

source:
  fn: blessed-1.9.5.tar.gz
  url: https://pypi.python.org/packages/source/b/blessed/blessed-1.9.5.tar.gz
  md5: 1548daf0cef19d5c849a6bfbae222399
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - blessed = blessed:main
    #
    # Would create an entry point called blessed that calls blessed.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - wcwidth >=0.1.0

  run:
    - python
    - wcwidth >=0.1.0

test:
  # Python imports
  imports:
    - blessed
    - blessed.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jquast/blessed
  license: MIT License
  summary: "A feature-filled fork of Erik Rose's blessings project"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
