package:
  name: pymysql
  version: "0.6.6"

source:
  fn: PyMySQL-0.6.6.tar.gz
  url: https://pypi.python.org/packages/source/P/PyMySQL/PyMySQL-0.6.6.tar.gz
  md5: a3968a6bf6672fa93ca931ab2ef46024
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pymysql = pymysql:main
    #
    # Would create an entry point called pymysql that calls pymysql.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - pymysql
    - pymysql.constants
    - pymysql.tests
    - pymysql.tests.thirdparty
    - pymysql.tests.thirdparty.test_MySQLdb

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/PyMySQL/PyMySQL/
  license: MIT License
  summary: 'Pure-Python MySQL Driver'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
