package:
  name: commandparser
  version: "0.2"

source:
  fn: CommandParser-0.2.tar.gz
  url: https://pypi.python.org/packages/source/C/CommandParser/CommandParser-0.2.tar.gz
  md5: 3b120a0edafa62ea0885cce2087af340
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - commandparser = commandparser:main
    #
    # Would create an entry point called commandparser that calls commandparser.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - commandparser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://k0s.org/hg/CommandParser
  license: MPL
  summary: 'change objects to OptionParser instances via reflection'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
