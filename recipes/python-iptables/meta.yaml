package:
  name: python-iptables
  version: "0.7.0"

source:
  fn: python-iptables-0.7.0.tar.gz
  url: https://pypi.python.org/packages/source/p/python-iptables/python-iptables-0.7.0.tar.gz
  md5: e1ee97f15f1c7e2a8545ff7e0213c714
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-iptables = python-iptables:main
    #
    # Would create an entry point called python-iptables that calls python-iptables.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - iptc

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ldx/python-iptables
  license: Apache Software License
  summary: 'Python bindings for iptables'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
