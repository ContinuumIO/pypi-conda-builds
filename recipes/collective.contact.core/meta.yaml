package:
  name: collective.contact.core
  version: "1.4"

source:
  fn: collective.contact.core-1.4.zip
  url: https://pypi.python.org/packages/source/c/collective.contact.core/collective.contact.core-1.4.zip
  md5: 1ae0a38e5f8dc4b7c2a8295a4be7fb2f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.contact.core = collective.contact.core:main
    #
    # Would create an entry point called collective.contact.core that calls collective.contact.core.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - extensionclass
    - collective.z3cform.datagridfield
    - collective.contact.widget >1.0
    - setuptools
    - ecreall.helpers.upgrade >=1.1.6.dev0
    - five.grok
    - five.globalrequest
    - plone.api
    - plone.app.dexterity
    - plone.app.linkintegrity
    - plone.app.relationfield
    - plone.autoform
    - plone.formwidget.datetime
    - plone.formwidget.masterselect >=1.3
    - plone.supermodel
    - products.cmfplone
    - vobject
    - zope.schema >=4.2.1

  run:
    - python
    - setuptools
    - extensionclass
    - collective.z3cform.datagridfield
    - collective.contact.widget >1.0
    - setuptools
    - ecreall.helpers.upgrade >=1.1.6.dev0
    - five.grok
    - five.globalrequest
    - plone.api
    - plone.app.dexterity
    - plone.app.linkintegrity
    - plone.app.relationfield
    - plone.autoform
    - plone.formwidget.datetime
    - plone.formwidget.masterselect >=1.3
    - plone.supermodel
    - products.cmfplone
    - vobject
    - zope.schema >=4.2.1

test:
  # Python imports
  imports:
    - collective
    - collective.contact
    - collective.contact.core
    - collective.contact.core.browser
    - collective.contact.core.browser.basefields
    - collective.contact.core.content
    - collective.contact.core.tests
    - collective.contact.core.upgrades

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/
  license: gpl
  summary: 'Core package for collective.contact add-ons'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
