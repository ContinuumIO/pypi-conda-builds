package:
  name: django-confirmaction
  version: "0.0.3"

source:
  fn: django-confirmaction-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-confirmaction/django-confirmaction-0.0.3.tar.gz
  md5: ac88bb5203e88127f245e1ade3b24d73
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-confirmaction = django-confirmaction:main
    #
    # Would create an entry point called django-confirmaction that calls django-confirmaction.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6
    - south >=0.8.4
    - pycrypto

  run:
    - python
    - django >=1.6
    - south >=0.8.4
    - pycrypto

test:
  # Python imports
  imports:
    - confirmaction
    - confirmaction.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Zapix/django-confirmaction
  license: BSD License
  summary: 'Django battery for confirm some action via email, sms, etc'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
