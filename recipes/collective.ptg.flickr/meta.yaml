package:
  name: collective.ptg.flickr
  version: "0.1.1"

source:
  fn: collective.ptg.flickr-0.1.1.zip
  url: https://pypi.python.org/packages/source/c/collective.ptg.flickr/collective.ptg.flickr-0.1.1.zip
  md5: 279538166a739e5f5fa9ded1d2b4e3fb
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.ptg.flickr = collective.ptg.flickr:main
    #
    # Would create an entry point called collective.ptg.flickr that calls collective.ptg.flickr.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - flickrapi
    - plone.app.z3cform
    - collective.plonetruegallery

  run:
    - python
    - setuptools
    - setuptools
    - flickrapi
    - plone.app.z3cform
    - collective.plonetruegallery

test:
  # Python imports
  imports:
    - collective
    - collective.ptg
    - collective.ptg.flickr

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.plone.org/products/plone-true-gallery
  license: GPL
  summary: 'Add on collective.plonetruegallery to aggregate from flickr'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
