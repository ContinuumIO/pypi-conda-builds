package:
  name: ixopenidclient
  version: "0.1.0"

source:
  fn: IXOpenIDClient-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/I/IXOpenIDClient/IXOpenIDClient-0.1.0.tar.gz
  md5: f3a2fd15e13d9e4e26bb8b89f322a881
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ixopenidclient = ixopenidclient:main
    #
    # Would create an entry point called ixopenidclient that calls ixopenidclient.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3.0
    - pep8 >=1.0.1
    - pylint >=0.25.1
    - django-openid-auth >=0.4

  run:
    - python
    - django >=1.3.0
    - pep8 >=1.0.1
    - pylint >=0.25.1
    - django-openid-auth >=0.4

test:
  # Python imports
  imports:
    - ixopenid_client
    - ixopenidclient

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/IXOpenIDClient/
  license: MIT
  summary: 'OpenID Client for applications using IX Profile servers'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
