package:
  name: django-clearable-widget
  version: "0.1.3"

source:
  fn: django-clearable-widget-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-clearable-widget/django-clearable-widget-0.1.3.tar.gz
  md5: 0d1fb758de6d70ab6788d5a65645c07a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-clearable-widget = django-clearable-widget:main
    #
    # Would create an entry point called django-clearable-widget that calls django-clearable-widget.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - clearable_widget
    - clearable_widget.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/bashu/django-clearable-widget
  license: BSD License
  summary: 'Custom widget to add a (x) clear button to your input fields'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
