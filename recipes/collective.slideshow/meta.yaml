package:
  name: collective.slideshow
  version: "0.0.2"

source:
  fn: collective.slideshow-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.slideshow/collective.slideshow-0.0.2.tar.gz
  md5: 779c0d8ba134756354549af0e1afab9b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.slideshow = collective.slideshow:main
    #
    # Would create an entry point called collective.slideshow that calls collective.slideshow.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.slideshow
    - collective.slideshow.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/intk/collective.slideshow
  license: GPL
  summary: 'Adds slideshow folder to all folderish items.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
