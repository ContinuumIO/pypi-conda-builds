package:
  name: django-timelinejs
  version: "0.2"

source:
  fn: Django-TimelineJS-0.2.tar.gz
  url: https://pypi.python.org/packages/source/D/Django-TimelineJS/Django-TimelineJS-0.2.tar.gz
  md5: 73ef07e9737c59d6280922fbc1236c1a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-timelinejs = django-timelinejs:main
    #
    # Would create an entry point called django-timelinejs that calls django-timelinejs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - timelinejs
    - timelinejs.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/abdallah/Django-TimelineJS
  license: Mozilla Public License 2.0 (MPL 2.0)
  summary: 'Pluggable app for Django to edit and display TimelineJS timelines'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
