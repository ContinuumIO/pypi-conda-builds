package:
  name: raspador
  version: "0.2.2"

source:
  fn: raspador-0.2.2.zip
  url: https://pypi.python.org/packages/source/r/raspador/raspador-0.2.2.zip
  md5: 1e235b68c7b1704ea7d0ffeeb8b24f46
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - raspador = raspador:main
    #
    # Would create an entry point called raspador that calls raspador.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - raspador

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.org/fgmacedo/raspador
  license: MIT License
  summary: 'Library to extract data from semi-structured text documents'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
