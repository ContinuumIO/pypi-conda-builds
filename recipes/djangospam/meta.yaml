package:
  name: djangospam
  version: "1.1.3"

source:
  fn: djangospam-1.1.3.zip
  url: https://pypi.python.org/packages/source/d/djangospam/djangospam-1.1.3.zip
  md5: 668180c0cb4a5a5f9080e42ad2f25675
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangospam = djangospam:main
    #
    # Would create an entry point called djangospam that calls djangospam.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - djangospam
    - djangospam.akismet
    - djangospam.cookie

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/leandroarndt/djangospam
  license: BSD License
  summary: 'Django antispam module with invisible fake comment/contact form, cookie based middleware and Akismet verification.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
