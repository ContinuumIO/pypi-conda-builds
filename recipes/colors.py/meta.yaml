package:
  name: colors.py
  version: "0.2.0"

source:
  fn: colors.py-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/c/colors.py/colors.py-0.2.0.tar.gz
  md5: a2d9f4c7ded1c20750521aa0038142bb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - colors.py = colors.py:main
    #
    # Would create an entry point called colors.py that calls colors.py.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - colors

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mattrobenolt/colors.py
  license: BSD
  summary: 'Convert and manipulate color values'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
