package:
  name: collective.colorbox
  version: "0.1.6rc2"

source:
  fn: collective.colorbox-0.1.6rc2.zip
  url: https://pypi.python.org/packages/source/c/collective.colorbox/collective.colorbox-0.1.6rc2.zip
  md5: 6d0b0097ce98210aa5a34b91d98efde6
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.colorbox = collective.colorbox:main
    #
    # Would create an entry point called collective.colorbox that calls collective.colorbox.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.colorbox
    - collective.colorbox.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/collective.colorbox
  license: GPL
  summary: 'A jQuery plugin that provides lightboxes in Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
