package:
  name: json-datetime
  version: "0.0.3"

source:
  fn: JSON-Datetime-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/J/JSON-Datetime/JSON-Datetime-0.0.3.tar.gz
  md5: 9a6197bedaab99d03e4ac9dc800fb64f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - json-datetime = json-datetime:main
    #
    # Would create an entry point called json-datetime that calls json-datetime.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - simplejson

  run:
    - python
    - simplejson

test:
  # Python imports
  imports:
    - jsondatetime
    - jsondatetime.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/nicolaiarocci/json-datetime
  license: ISC License (ISCL)
  summary: 'Allows for proper decoding of datetime values contained in JSON streams'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
