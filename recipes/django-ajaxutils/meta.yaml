package:
  name: django-ajaxutils
  version: "0.2"

source:
  fn: django-ajaxutils-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-ajaxutils/django-ajaxutils-0.2.tar.gz
  md5: 039f0072d7939c65eef4649c7dadebba
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-ajaxutils = django-ajaxutils:main
    #
    # Would create an entry point called django-ajaxutils that calls django-ajaxutils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.2
    - decorator >=3.3.2

  run:
    - python
    - django >=1.2
    - decorator >=3.3.2

test:
  # Python imports
  imports:
    - ajaxutils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/ahref/django-ajaxutils
  license: BSD License
  summary: 'Ajax requests for Ponies'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
