package:
  name: django-kss
  version: "0.5.5"

source:
  fn: django-kss-0.5.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-kss/django-kss-0.5.5.tar.gz
  md5: 294aa634643bcad82396baac32d1913a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-kss = django-kss:main
    #
    # Would create an entry point called django-kss that calls django-kss.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pygments
    - django_compressor
    - django-libsass

  run:
    - python
    - pygments
    - django_compressor
    - django-libsass

test:
  # Python imports
  imports:
    - django_kss
    - django_kss.pykss
    - django_kss.pykss.contrib
    - django_kss.pykss.contrib.django
    - django_kss.pykss.contrib.django.templatetags
    - django_kss.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/timtan/django_kss
  license: BSD License
  summary: 'A simple Django app to make styleguide'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
