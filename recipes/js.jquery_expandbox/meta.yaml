package:
  name: js.jquery_expandbox
  version: "0.2.0"

source:
  fn: js.jquery_expandbox-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/j/js.jquery_expandbox/js.jquery_expandbox-0.2.0.tar.gz
  md5: dc2e38168669b1e11e1c3ec059877c3f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - js.jquery_expandbox = js.jquery_expandbox:main
    #
    # Would create an entry point called js.jquery_expandbox that calls js.jquery_expandbox.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fanstatic
    - setuptools
    - js.jquery

  run:
    - python
    - setuptools
    - fanstatic
    - setuptools
    - js.jquery

test:
  # Python imports
  imports:
    - js
    - js.jquery_expandbox

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD
  summary: 'Fanstatic packaging of jquery.expandBox'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
