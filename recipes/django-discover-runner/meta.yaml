package:
  name: django-discover-runner
  version: "1.0"

source:
  fn: django-discover-runner-1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-discover-runner/django-discover-runner-1.0.tar.gz
  md5: 23d377cdcc5f1c2e1bcc5b508e6da4a7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-discover-runner = django-discover-runner:main
    #
    # Would create an entry point called django-discover-runner that calls django-discover-runner.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - discover_runner

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/jezdez/django-discover-runner
  license: BSD License
  summary: "A Django test runner based on unittest2's test discovery."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
