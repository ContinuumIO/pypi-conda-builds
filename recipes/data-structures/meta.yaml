package:
  name: data-structures
  version: "0.1.6"

source:
  fn: data-structures-0.1.6.tar.gz
  url: https://pypi.python.org/packages/source/d/data-structures/data-structures-0.1.6.tar.gz
  md5: 8eae8c8bd7ed561a1dfc4e6087d65a40
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - data-structures = data-structures:main
    #
    # Would create an entry point called data-structures that calls data-structures.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - collections_extended

  run:
    - python
    - setuptools
    - collections_extended

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mlenzen/python-data-structures
  license: Apache Software License
  summary: 'Extra Python Data Structures - bags (multisets) and setlists (ordered sets)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
