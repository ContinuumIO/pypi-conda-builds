package:
  name: django-backtalk
  version: "1.0"

source:
  fn: django-backtalk-1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-backtalk/django-backtalk-1.0.tar.gz
  md5: 680b6134c88afdacf76f7a1558af1434
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-backtalk = django-backtalk:main
    #
    # Would create an entry point called django-backtalk that calls django-backtalk.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - backtalk

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://dusette.net/
  license: GPL v3
  summary: 'A very simple comment app, forked from django.contrib.comments.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
