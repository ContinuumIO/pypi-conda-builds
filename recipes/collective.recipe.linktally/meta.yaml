package:
  name: collective.recipe.linktally
  version: "0.1.0-3"

source:
  fn: collective.recipe.linktally-0.1.0-3.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.recipe.linktally/collective.recipe.linktally-0.1.0-3.tar.gz
  md5: 8ee668cd0360611c56fb0a425efa08c7
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.recipe.linktally = collective.recipe.linktally:main
    #
    # Would create an entry point called collective.recipe.linktally that calls collective.recipe.linktally.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zope.testing
    - zc.buildout
    - plone.recipe.distros
    - zc.recipe.egg

  run:
    - python
    - setuptools
    - setuptools
    - zope.testing
    - zc.buildout
    - plone.recipe.distros
    - zc.recipe.egg

test:
  # Python imports
  imports:
    - collective
    - collective.recipe
    - collective.recipe.linktally
    - collective.recipe.linktally.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://svn.plone.org/svn/collective/buildout/collective.recipe.linktally/
  license: GPL
  summary: 'zc.buildout recipe for setting up linktally'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
