package:
  name: bda.daemon
  version: "1.1.1"

source:
  fn: bda.daemon-1.1.1.tar.gz
  url: https://pypi.python.org/packages/source/b/bda.daemon/bda.daemon-1.1.1.tar.gz
  md5: 5e2ae672156baaf0383d8abb31f0efa7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bda.daemon = bda.daemon:main
    #
    # Would create an entry point called bda.daemon that calls bda.daemon.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - bda
    - bda.daemon
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/bda.daemon
  license: Python Software Foundation License
  summary: 'takes a class and runs it as a simple daemon.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
