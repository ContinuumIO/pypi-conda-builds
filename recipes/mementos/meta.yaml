package:
  name: mementos
  version: "1.0.3"

source:
  fn: mementos-1.0.3.tar.gz
  url: https://pypi.python.org/packages/source/m/mementos/mementos-1.0.3.tar.gz
  md5: 8a172989eebc73ff0ce252a9db58fbe3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mementos = mementos:main
    #
    # Would create an entry point called mementos that calls mementos.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - mementos

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/jeunice/mementos
  license: BSD License
  summary: 'Memoizing metaclass. Drop-dead simple way to create cached objects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
