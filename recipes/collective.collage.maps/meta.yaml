package:
  name: collective.collage.maps
  version: "1.2.1"

source:
  fn: collective.collage.maps-1.2.1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.collage.maps/collective.collage.maps-1.2.1.tar.gz
  md5: 67c285a0f306f0bca5b9d716ae1882a3
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.collage.maps = collective.collage.maps:main
    #
    # Would create an entry point called collective.collage.maps that calls collective.collage.maps.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.collage
    - products.maps

  run:
    - python
    - setuptools
    - setuptools
    - products.collage
    - products.maps

test:
  # Python imports
  imports:
    - collective
    - collective.collage
    - collective.collage.maps
    - collective.collage.maps.patches

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/collective.collage.maps
  license: GPL
  summary: 'Show Map view of a Plone folder in a Collage'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
