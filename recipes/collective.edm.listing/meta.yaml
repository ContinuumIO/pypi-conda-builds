package:
  name: collective.edm.listing
  version: "0.12"

source:
  fn: collective.edm.listing-0.12.zip
  url: https://pypi.python.org/packages/source/c/collective.edm.listing/collective.edm.listing-0.12.zip
  md5: d44597dd7ac09264d44aa295db683fda
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.edm.listing = collective.edm.listing:main
    #
    # Would create an entry point called collective.edm.listing that calls collective.edm.listing.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.quickupload >=1.4.0
    - collective.externaleditor

  run:
    - python
    - setuptools
    - setuptools
    - collective.quickupload >=1.4.0
    - collective.externaleditor

test:
  # Python imports
  imports:
    - collective
    - collective.edm
    - collective.edm.listing

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/
  license: GPL
  summary: 'A view for folders that provides file-explorer like document adding and management tools'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
