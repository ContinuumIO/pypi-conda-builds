package:
  name: cubicweb-trustedauth
  version: "0.3.1"

source:
  fn: cubicweb-trustedauth-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/c/cubicweb-trustedauth/cubicweb-trustedauth-0.3.1.tar.gz
  md5: 89b3db2538cf700280d5ff8b95bbfec4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cubicweb-trustedauth = cubicweb-trustedauth:main
    #
    # Would create an entry point called cubicweb-trustedauth that calls cubicweb-trustedauth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cubicweb >=3.17.0

  run:
    - python
    - cubicweb >=3.17.0

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.cubicweb.org/project/cubicweb-trustedauth
  license: LGPL
  summary: 'authentication plugin for cubicweb instances behind a trusted reverse proxy managing authentication  (eg. apache + kerberos)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
