package:
  name: ohashi
  version: "0.2.7"

source:
  fn: ohashi-0.2.7.tar.gz
  url: https://pypi.python.org/packages/source/o/ohashi/ohashi-0.2.7.tar.gz
  md5: f28443178d0ed6299f4b94ebd3d3636c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ohashi = ohashi:main
    #
    # Would create an entry point called ohashi that calls ohashi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4.1
    - redis >=2.6.0

  run:
    - python
    - django >=1.4.1
    - redis >=2.6.0

test:
  # Python imports
  imports:
    - ohashi
    - ohashi.db
    - ohashi.db.models
    - ohashi.db.models.fields
    - ohashi.db.models.managers
    - ohashi.redis

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/revyver/ohashi
  license: BSD License
  summary: 'Yet another opinionated utilities kit for Django projects.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
