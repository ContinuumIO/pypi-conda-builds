package:
  name: chephren
  version: "0.1.0"

source:
  fn: chephren-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/c/chephren/chephren-0.1.0.tar.gz
  md5: 9383733261eabefc8a8d6eb7c7208ddc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - chephren = chephren:main
    #
    # Would create an entry point called chephren that calls chephren.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - werkzeug >=0.10
    - python-dateutil
    - pytz
    - sphinx >=1.3.0

  run:
    - python
    - werkzeug >=0.10
    - python-dateutil
    - pytz
    - sphinx >=1.3.0

test:
  # Python imports
  imports:
    - chephren

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: Apache Software License
  summary: 'An extension to Sphinx for managing a blog or static website'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
