package:
  name: django-uoclti
  version: "0.1.3"

source:
  fn: django-uocLTI-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-uocLTI/django-uocLTI-0.1.3.tar.gz
  md5: 2b44fb56894dded26023c43e78ecf690
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-uoclti = django-uoclti:main
    #
    # Would create an entry point called django-uoclti that calls django-uoclti.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4,<1.5
    - ims-lti-py ==0.6

  run:
    - python
    - django >=1.4,<1.5
    - ims-lti-py ==0.6

test:
  # Python imports
  imports:
    - uocLTI

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/django-uocLTI
  license: MIT licence, see LICENCE.txt
  summary: 'An IMS-LTI provider interface for django apps using the ims_lti_py library, created for use within the Universitat Obert de Catalunya UOC - nfinney 2013'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
