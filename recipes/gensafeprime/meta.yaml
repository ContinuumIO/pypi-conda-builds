package:
  name: gensafeprime
  version: "1.3"

source:
  fn: gensafeprime-1.3.tar.gz
  url: https://pypi.python.org/packages/source/g/gensafeprime/gensafeprime-1.3.tar.gz
  md5: e59f1afe5632bf2efea64d681ec0c8bb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gensafeprime = gensafeprime:main
    #
    # Would create an entry point called gensafeprime that calls gensafeprime.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - gensafeprime

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/eriktews/gensafeprime
  license: MIT License
  summary: 'Generate safe primes using OpenSSL (a prime p = 2*q+1, and q is prime too)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
