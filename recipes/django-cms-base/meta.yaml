package:
  name: django-cms-base
  version: "1.0"

source:
  fn: 1.0.tar.gz
  url: https://github.com/Cotidia/django-cms-base/archive/1.0.tar.gz
  #md5: 
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-cms-base = django-cms-base:main
    #
    # Would create an entry point called django-cms-base that calls django-cms-base.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6
    - django-mptt ==0.6
    - django-reversion ==1.8
    - django-form-utils ==1.0.1
    - pillow
    - south
    - django-codemirror-widget
    - django-localeurl

  run:
    - python
    - django >=1.6
    - django-mptt ==0.6
    - django-reversion ==1.8
    - django-form-utils ==1.0.1
    - pillow
    - south
    - django-codemirror-widget
    - django-localeurl

test:
  # Python imports
  imports:
    - cmsbase
    - cmsbase.management
    - cmsbase.management.commands
    - cmsbase.migrations
    - cmsbase.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/guillaumepiot/cotidia-cms-base
  license: BSD License
  summary: 'Django application to manage content publishing'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
