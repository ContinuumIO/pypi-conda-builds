package:
  name: blackboard_analysis_tools
  version: "0.0.4"

source:
  fn: blackboard_analysis_tools-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/b/blackboard_analysis_tools/blackboard_analysis_tools-0.0.4.tar.gz
  md5: ed3ce80b5fa2af1fcb3c64c4a23b6428
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - blackboard_analysis_tools = blackboard_analysis_tools:main
    #
    # Would create an entry point called blackboard_analysis_tools that calls blackboard_analysis_tools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - blackboard_analysis_tools

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: none
  license: LGPL-v2
  summary: 'Blackboard analysis tools'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
