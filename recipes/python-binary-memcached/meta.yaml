package:
  name: python-binary-memcached
  version: "0.24.3"

source:
  fn: python-binary-memcached-0.24.3.tar.gz
  url: https://pypi.python.org/packages/source/p/python-binary-memcached/python-binary-memcached-0.24.3.tar.gz
  md5: 259305b5af99c67b4c7b8241bd9c13fa
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-binary-memcached = python-binary-memcached:main
    #
    # Would create an entry point called python-binary-memcached that calls python-binary-memcached.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - bmemcached

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jaysonsantos/python-binary-memcached
  license: MIT License
  summary: "A pure python module (thread safe) to access memcached via it's binary protocol with SASL auth support"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
