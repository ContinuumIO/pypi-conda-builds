package:
  name: janrain-django
  version: "0.1.2"

source:
  fn: janrain-django-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/j/janrain-django/janrain-django-0.1.2.tar.gz
  md5: 65a8fe51c424974ddd03fbf49bd49419
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - janrain-django = janrain-django:main
    #
    # Would create an entry point called janrain-django that calls janrain-django.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - janrain-python-api >=0.2.1

  run:
    - python
    - janrain-python-api >=0.2.1

test:
  # Python imports
  imports:
    - janrain
    - janrain.django

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://developers.janrain.com/
  license: MIT License
  summary: 'Janrain integration with Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
