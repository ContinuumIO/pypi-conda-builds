package:
  name: django-easymoney
  version: "0.5"

source:
  fn: django-easymoney-0.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-easymoney/django-easymoney-0.5.tar.gz
  md5: f7c6dec70bc57740e71b39d346ac2cf5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-easymoney = django-easymoney:main
    #
    # Would create an entry point called django-easymoney that calls django-easymoney.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6
    - babel

  run:
    - python
    - django >=1.6
    - babel

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/Suor/django-easymoney
  license: BSD License
  summary: 'An easy MoneyField for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
