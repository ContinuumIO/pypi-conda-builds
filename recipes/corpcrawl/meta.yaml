package:
  name: corpcrawl
  version: "0.0.37"

source:
  fn: corpcrawl-0.0.37.tar.gz
  url: https://pypi.python.org/packages/source/c/corpcrawl/corpcrawl-0.0.37.tar.gz
  md5: 7110980ed14a0735f4e984547a14bb7f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - corpcrawl = corpcrawl:main
    #
    # Would create an entry point called corpcrawl that calls corpcrawl.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - corpcrawl
    - corpcrawl.models
    - corpcrawl.util

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/chrisd1891/corpcrawl/
  license: MIT
  summary: 'Corpcrawl EDGAR Scraper'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
