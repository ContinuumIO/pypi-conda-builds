package:
  name: zot4rst
  version: "0.3.0"

source:
  fn: zot4rst-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/z/zot4rst/zot4rst-0.3.0.tar.gz
  md5: 8fb9b86025fd8a64f8890e95ccf4ca7d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zot4rst = zot4rst:main
    #
    # Would create an entry point called zot4rst that calls zot4rst.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - beautifulsoup >=3.2.0
    - docutils >=0.9
    - rst2pdf >=0.16
    - pyparsing ==1.5.7
    - pyzotero >=0.9.9

  run:
    - python
    - beautifulsoup >=3.2.0
    - docutils >=0.9
    - rst2pdf >=0.16
    - pyparsing ==1.5.7
    - pyzotero >=0.9.9

test:
  # Python imports
  imports:
    - xciterst
    - zot4rst

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/egh/zot4rst/
  license: UNKNOWN
  summary: 'Zotero for reStructuredText'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
