package:
  name: google-oauth-flask
  version: "0.0.1"

source:
  fn: google-oauth-flask-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/g/google-oauth-flask/google-oauth-flask-0.0.1.tar.gz
  md5: a2157f357ddd1a348e2622245769875d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - google-oauth-flask = google-oauth-flask:main
    #
    # Would create an entry point called google-oauth-flask that calls google-oauth-flask.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask ==0.10.1
    - requests_oauthlib ==0.4.2

  run:
    - python
    - flask ==0.10.1
    - requests_oauthlib ==0.4.2

test:
  # Python imports
  imports:
    - google_oauth_flask

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/vistarmedia/google_oauth_flask
  license: UNKNOWN
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
