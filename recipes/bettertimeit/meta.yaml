package:
  name: bettertimeit
  version: "1.1.2"

source:
  fn: bettertimeit-1.1.2.tar.gz
  url: https://pypi.python.org/packages/source/b/bettertimeit/bettertimeit-1.1.2.tar.gz
  md5: 2f4bfde640bc69f696ed9011fc4558db
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bettertimeit = bettertimeit:main
    #
    # Would create an entry point called bettertimeit that calls bettertimeit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - wheel >=0.23.0,<=1.0
    - six >=1.6.1,<2.0
    - astunparse >=1.2.1,<2.0

  run:
    - python
    - setuptools
    - wheel >=0.23.0,<=1.0
    - six >=1.6.1,<2.0
    - astunparse >=1.2.1,<2.0

test:
  # Python imports
  imports:
    - bettertimeit

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/simonpercivall/bettertimeit
  license: BSD License
  summary: 'A Better Timeit'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
