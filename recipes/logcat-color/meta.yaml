package:
  name: logcat-color
  version: "0.6.0"

source:
  fn: logcat-color-0.6.0.tar.gz
  url: https://pypi.python.org/packages/source/l/logcat-color/logcat-color-0.6.0.tar.gz
  md5: 4aaa9b4124ee020185967ef952126032
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - logcat-color = logcat-color:main
    #
    # Would create an entry point called logcat-color that calls logcat-color.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - colorama

  run:
    - python
    - colorama

test:
  # Python imports
  imports:
    - logcatcolor

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/marshall/logcat-color
  license: UNKNOWN
  summary: u'A colorful alternative to "adb logcat"'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
