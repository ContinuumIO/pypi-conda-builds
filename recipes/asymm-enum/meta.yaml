package:
  name: asymm-enum
  version: "0.3.4-201408251309"

source:
  fn: asymm-enum-0.3.4-201408251309.tar.gz
  url: https://pypi.python.org/packages/source/a/asymm-enum/asymm-enum-0.3.4-201408251309.tar.gz
  md5: ed2449b0b4c24ade6b3a27a9b529359a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - asymm-enum = asymm-enum:main
    #
    # Would create an entry point called asymm-enum that calls asymm-enum.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six

  run:
    - python
    - six

test:
  # Python imports
  imports:
    - asymm_enum
    - asymm_enum.fields
    - asymm_enum.tests
    - asymm_enum.tests.testapp

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/AsymmetricVentures/asym-enum
  license: GNU General Public License v2 (GPLv2)
  summary: 'Java style enums for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
