package:
  name: supervisord-nagios
  version: "0.1.2"

source:
  fn: supervisord-nagios-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/s/supervisord-nagios/supervisord-nagios-0.1.2.tar.gz
  md5: 8440efedc842b56dffdfa14ed6540e45
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - supervisord-nagios = supervisord-nagios:main
    #
    # Would create an entry point called supervisord-nagios that calls supervisord-nagios.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - supervisor

  run:
    - python
    - setuptools
    - supervisor

test:
  # Python imports
  imports:
    - supervisord_nagios

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/3dna/supervisord-nagios
  license: BSD-3
  summary: 'nagios checks for supervisord services'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
