package:
  name: dicttoxml
  version: "1.6.6"

source:
  fn: dicttoxml-1.6.6.tar.gz
  url: https://pypi.python.org/packages/source/d/dicttoxml/dicttoxml-1.6.6.tar.gz
  md5: 5318bc0dbd1fe0a265ec23bd680c5ead
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dicttoxml = dicttoxml:main
    #
    # Would create an entry point called dicttoxml that calls dicttoxml.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - dicttoxml

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/quandyfactory/dicttoxml
  license: LICENCE.txt
  summary: 'Converts a Python dictionary or other native data type into a valid XML string. '

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
