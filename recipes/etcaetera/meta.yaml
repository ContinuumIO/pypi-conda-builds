package:
  name: etcaetera
  version: "0.4.3"

source:
  fn: etcaetera-0.4.3.tar.gz
  url: https://pypi.python.org/packages/source/e/etcaetera/etcaetera-0.4.3.tar.gz
  md5: 7ce8789d3fa92b01557a0ae529101fac
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - etcaetera = etcaetera:main
    #
    # Would create an entry point called etcaetera that calls etcaetera.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyyaml

  run:
    - python
    - pyyaml

test:
  # Python imports
  imports:
    - etcaetera
    - etcaetera.adapter

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/oleiade/etcaetera
  license: MIT License
  summary: 'Manage multiple configuration sources in a single place'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
