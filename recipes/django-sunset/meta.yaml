package:
  name: django-sunset
  version: "0.3"

source:
  fn: django-sunset-0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-sunset/django-sunset-0.3.tar.gz
  md5: 1a35d8dc904d7a8f145c566a7933877f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-sunset = django-sunset:main
    #
    # Would create an entry point called django-sunset that calls django-sunset.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - sunset
    - sunset.tests
    - sunset.tests.working

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/robmadole/django-sunset
  license: BSD License
  summary: 'Handle Django settings in a bit more organized fashion'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
