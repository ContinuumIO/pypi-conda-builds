package:
  name: pygravatar
  version: "0.0.6"

source:
  fn: pyGravatar-0.0.6.tar.gz
  url: https://pypi.python.org/packages/source/p/pyGravatar/pyGravatar-0.0.6.tar.gz
  md5: c8e167c8c5dd8c0313fe4f1d58e56088
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pygravatar = pygravatar:main
    #
    # Would create an entry point called pygravatar that calls pygravatar.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - pygravatar

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/gridaphobe/pyGravatar
  license: GPL
  summary: 'Python module for interacting with Gravatar'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
