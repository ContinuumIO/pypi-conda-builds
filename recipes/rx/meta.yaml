package:
  name: rx
  version: "1.2.0"

source:
  fn: Rx-1.2.0.tar.gz
  url: https://pypi.python.org/packages/source/R/Rx/Rx-1.2.0.tar.gz
  md5: bd7b2ce35f321d2406ad7250c73fed18
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - rx = rx:main
    #
    # Would create an entry point called rx that calls rx.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - rx
    - rx.backpressure
    - rx.concurrency
    - rx.concurrency.mainloopscheduler
    - rx.disposables
    - rx.internal
    - rx.joins
    - rx.linq
    - rx.linq.enumerable
    - rx.linq.observable
    - rx.linq.observable.blocking
    - rx.subjects
    - rx.testing

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://reactivex.io
  license: Apache Software License
  summary: 'Reactive Extensions (Rx) for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
