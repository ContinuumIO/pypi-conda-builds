package:
  name: ipythonbell
  version: "0.9.1"

source:
  fn: IPythonBell-0.9.1.tar.gz
  url: https://pypi.python.org/packages/source/I/IPythonBell/IPythonBell-0.9.1.tar.gz
  md5: d18e1647af4ec95f188cbbb9d4939748
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ipythonbell = ipythonbell:main
    #
    # Would create an entry point called ipythonbell that calls ipythonbell.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - ipybell
    - ipythonbell

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.github.com/samwhitehall/ipython-bell
  license: MIT
  summary: 'Python line & cell magic to notify the programmer when a line/cell has completed execution'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
