package:
  name: django-uuidfield-2
  version: "0.6.6"

source:
  fn: django-uuidfield-2-0.6.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-uuidfield-2/django-uuidfield-2-0.6.6.tar.gz
  md5: 18b05a0ac819fe1f92087235ec71db7c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-uuidfield-2 = django-uuidfield-2:main
    #
    # Would create an entry point called django-uuidfield-2 that calls django-uuidfield-2.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - uuidfield

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/schinckel/django-uuidfield/
  license: BSD License
  summary: 'UUIDField for django models'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
