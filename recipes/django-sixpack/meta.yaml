package:
  name: django-sixpack
  version: "1.0.5"

source:
  fn: django-sixpack-1.0.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-sixpack/django-sixpack-1.0.5.tar.gz
  md5: 5e269d58e3c00a9f24b58921cb0d8e34
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-sixpack = django-sixpack:main
    #
    # Would create an entry point called django-sixpack that calls django-sixpack.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4
    - sixpack-client ==1.0.0

  run:
    - python
    - django >=1.4
    - sixpack-client ==1.0.0

test:
  # Python imports
  imports:
    - djsixpack
    - djsixpack.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/waveaccounting/django-sixpack
  license: BSD License
  summary: 'A django-friendly wrapper for sixpack-py'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
