package:
  name: cocograph
  version: "0.1.0"

source:
  fn: cocograph-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/c/cocograph/cocograph-0.1.0.tar.gz
  md5: 05264aeae47a0cad99fce4ae94198487
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cocograph = cocograph:main
    #
    # Would create an entry point called cocograph that calls cocograph.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyglet >=1.1.4
    - cocos2d >=0.4.0
    - kytten6 >=6.0.0

  run:
    - python
    - pyglet >=1.1.4
    - cocos2d >=0.4.0
    - kytten6 >=6.0.0

test:
  # Python imports
  imports:
    - cocograph

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kitanata/cocograph
  license: BSD License
  summary: 'A tile editor for cocos2d.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
