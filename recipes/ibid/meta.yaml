package:
  name: ibid
  version: "0.1.1"

source:
  fn: Ibid-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/I/Ibid/Ibid-0.1.1.tar.gz
  md5: 490db75a8c837da23277e58ec9c07fcb
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ibid = ibid:main
    #
    # Would create an entry point called ibid that calls ibid.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - beautifulsoup
    - configobj >=4.7.0
    - dnspython
    - feedparser
    - html2text
    - html5lib
    - jinja
    - pyopenssl
    - pystemmer
    - python-dateutil
    - soappy
    - sqlalchemy >=0.5,<0.6a
    - twisted
    - wokkel >=0.6.3

  run:
    - python
    - setuptools
    - beautifulsoup
    - configobj >=4.7.0
    - dnspython
    - feedparser
    - html2text
    - html5lib
    - jinja
    - pyopenssl
    - pystemmer
    - python-dateutil
    - soappy
    - sqlalchemy >=0.5,<0.6a
    - twisted
    - wokkel >=0.6.3

test:
  # Python imports
  imports:
    - contrib
    - factpacks
    - ibid
    - tracibid
    - twisted

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://ibid.omnia.za.net/
  license: MIT
  summary: 'Multi-protocol general purpose chat bot'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
