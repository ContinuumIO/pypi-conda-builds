package:
  name: gtfs-realtime-bindings
  version: "0.0.4"

source:
  fn: gtfs-realtime-bindings-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/g/gtfs-realtime-bindings/gtfs-realtime-bindings-0.0.4.tar.gz
  md5: 6d767327c48127a5c5561e51ca743db9
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gtfs-realtime-bindings = gtfs-realtime-bindings:main
    #
    # Would create an entry point called gtfs-realtime-bindings that calls gtfs-realtime-bindings.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - protobuf

  run:
    - python
    - setuptools
    - protobuf

test:
  # Python imports
  imports:
    - google
    - google.transit

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/google/gtfs-realtime-bindings
  license: Apache Software License
  summary: 'Python classes generated from the GTFS-realtime protocol buffer specification.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
