package:
  name: django-related
  version: "0.0.6"

source:
  fn: django-related-0.0.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-related/django-related-0.0.6.tar.gz
  md5: 86475d49c17df15bec3db394b4f98c84
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-related = django-related:main
    #
    # Would create an entry point called django-related that calls django-related.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - related

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/monwara/django-related
  license: BSD License
  summary: 'Class-based-views mixins for handling related objects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
