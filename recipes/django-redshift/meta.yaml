package:
  name: django-redshift
  version: "0.2"

source:
  fn: django-redshift-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-redshift/django-redshift-0.2.tar.gz
  md5: c9bba7731da65270f7a545997844f0b1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-redshift = django-redshift:main
    #
    # Would create an entry point called django-redshift that calls django-redshift.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - psycopg2 ==2.5

  run:
    - python
    - psycopg2 ==2.5

test:
  # Python imports
  imports:
    - django-redshift

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/binarydud/django-redshift
  license: MIT License
  summary: 'Amazon Redshift Dialect for django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
