package:
  name: django-force-logout
  version: "0.1"

source:
  fn: django-force-logout-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-force-logout/django-force-logout-0.1.tar.gz
  md5: bcf07d542df2bf07118478bd4822a14a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-force-logout = django-force-logout:main
    #
    # Would create an entry point called django-force-logout that calls django-force-logout.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - django_force_logout

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/thread/django-force-logout
  license: UNKNOWN
  summary: 'Django Force Logout'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
