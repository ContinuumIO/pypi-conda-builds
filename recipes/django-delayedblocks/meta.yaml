package:
  name: django-delayedblocks
  version: "0.0.1"

source:
  fn: django-delayedblocks-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-delayedblocks/django-delayedblocks-0.0.1.tar.gz
  md5: 67799275d22bfff479e51a0c8fdcee37
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-delayedblocks = django-delayedblocks:main
    #
    # Would create an entry point called django-delayedblocks that calls django-delayedblocks.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - delayedblocks

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/ojii/django-delayedblocks
  license: UNKNOWN
  summary: 'Django Delayed Blocks'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
