package:
  name: jsonrpclib
  version: "0.1.3"

source:
  fn: jsonrpclib-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/j/jsonrpclib/jsonrpclib-0.1.3.tar.gz
  md5: b2cc8b86695d5e7c395c9d6908331ecd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jsonrpclib = jsonrpclib:main
    #
    # Would create an entry point called jsonrpclib that calls jsonrpclib.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - jsonrpclib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/joshmarshall/jsonrpclib/
  license: http://www.apache.org/licenses/LICENSE-2.0
  summary: 'This project is an implementation of the JSON-RPC v2.0 specification (backwards-compatible) as a client library.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
