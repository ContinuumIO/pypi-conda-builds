package:
  name: degreedays
  version: "1.1"

source:
  fn: degreedays-1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/degreedays/degreedays-1.1.tar.gz
  md5: f01b081b590662c8629dbbc8e701a443
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - degreedays = degreedays:main
    #
    # Would create an entry point called degreedays that calls degreedays.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - degreedays
    - degreedays.api
    - degreedays.api.data

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.degreedays.net/api/python
  license: Apache Software License
  summary: 'Degree Days.net API Python Client Library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
