package:
  name: cssocialprofile
  version: "2.3"

source:
  fn: cssocialprofile-2.3.zip
  url: https://pypi.python.org/packages/source/c/cssocialprofile/cssocialprofile-2.3.zip
  md5: 5ae9a0a61cc886492d97fb9a7ac1e8fd
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cssocialprofile = cssocialprofile:main
    #
    # Would create an entry point called cssocialprofile that calls cssocialprofile.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-social-auth
    - django-registration
    - tweepy
    - pyfacebook

  run:
    - python
    - setuptools
    - django-social-auth
    - django-registration
    - tweepy
    - pyfacebook

test:
  # Python imports
  imports:
    - cssocialprofile
    - cssocialprofile.pipeline
    - cssocialprofile.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.codesyntax.com
  license: GPL
  summary: 'SocialProfile app extension'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
