package:
  name: django-bootstrap-admin
  version: "0.3.4"

source:
  fn: django-bootstrap-admin-0.3.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-bootstrap-admin/django-bootstrap-admin-0.3.4.tar.gz
  md5: b773e46494ecf4742405d3d7da8fdae3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-bootstrap-admin = django-bootstrap-admin:main
    #
    # Would create an entry point called django-bootstrap-admin that calls django-bootstrap-admin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - bootstrap
    - bootstrap.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/maxfolley/django-bootstrap-admin
  license: MIT License
  summary: 'Adds twitter bootstrap and other nicities to the Django admin'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
