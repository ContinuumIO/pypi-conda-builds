package:
  name: gae-console
  version: "0.1.0"

source:
  fn: gae-console-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/g/gae-console/gae-console-0.1.0.tar.gz
  md5: 856c8400bc3acf9d0d3170a01abb0c6f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gae-console = gae-console:main
    #
    # Would create an entry point called gae-console that calls gae-console.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - gae_console

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/sadovnychyi/gae-console
  license: GNU General Public License v2.0
  summary: 'Powerful Interactive Console for App Engine applications.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
