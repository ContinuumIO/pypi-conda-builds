package:
  name: django-paranoid-sessions
  version: "0.2.0"

source:
  fn: django-paranoid-sessions-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-paranoid-sessions/django-paranoid-sessions-0.2.0.tar.gz
  md5: f66b99e6f4f9553a5760c9475b39e2d9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-paranoid-sessions = django-paranoid-sessions:main
    #
    # Would create an entry point called django-paranoid-sessions that calls django-paranoid-sessions.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - paranoidsessions

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/rfk/django-paranoid-sessions/tree/master
  license: MIT
  summary: 'make Django work harder to prevent session-stealing attacks'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
