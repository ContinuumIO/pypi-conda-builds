package:
  name: django-yubin
  version: "0.1.5"

source:
  fn: django-yubin-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-yubin/django-yubin-0.1.5.tar.gz
  md5: 60c577123861f3be7bb9a83249468b8e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-yubin = django-yubin:main
    #
    # Would create an entry point called django-yubin that calls django-yubin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyzmail
    - lockfile

  run:
    - python
    - pyzmail
    - lockfile

test:
  # Python imports
  imports:
    - django_yubin
    - django_yubin.management
    - django_yubin.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/APSL/django-yubin
  license: Apache Software License
  summary: 'A reusable Django app for composing and queueing emails Adds django-mailer2 + django-mailviews + others'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
