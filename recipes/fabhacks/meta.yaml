package:
  name: fabhacks
  version: "0.1.6"

source:
  fn: Fabhacks-0.1.6.tar.gz
  url: https://pypi.python.org/packages/source/F/Fabhacks/Fabhacks-0.1.6.tar.gz
  md5: 23b4395d60369208b8da6ee4a04c7e15
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fabhacks = fabhacks:main
    #
    # Would create an entry point called fabhacks that calls fabhacks.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fabric >=1.8.0

  run:
    - python
    - fabric >=1.8.0

test:
  # Python imports
  imports:
    - fabhacks

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/EDITD/fabhacks
  license: UNKNOWN
  summary: 'Some Fabric-based hacks'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
