package:
  name: guippy
  version: "0.1.5dev"

source:
  fn: guippy-0.1.5dev.tar.gz
  url: https://pypi.python.org/packages/source/g/guippy/guippy-0.1.5dev.tar.gz
  md5: 45d2fb63c60fbabf339e9732c3bbfd38
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - guippy = guippy:main
    #
    # Would create an entry point called guippy that calls guippy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - guippy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/takesxi_sximada/guippy
  license: GNU General Public License v3 (GPLv3)
  summary: u'Control the Windows GUI'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
