package:
  name: collective.personaltags
  version: "1.0"

source:
  fn: collective.personaltags-1.0.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.personaltags/collective.personaltags-1.0.tar.gz
  md5: 123b2c58ed48704e202572d1843d9bab
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.personaltags = collective.personaltags:main
    #
    # Would create an entry point called collective.personaltags that calls collective.personaltags.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.dexterity
    - collective.autopermission
    - plone.app.registry

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.dexterity
    - collective.autopermission
    - plone.app.registry

test:
  # Python imports
  imports:
    - collective
    - collective.personaltags
    - collective.personaltags.behavior
    - collective.personaltags.browser
    - collective.personaltags.content
    - collective.personaltags.controlpanel
    - collective.personaltags.demo_manager
    - collective.personaltags.portlet
    - collective.personaltags.viewlet

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/
  license: GPL
  summary: 'Track content using personal tags'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
