package:
  name: gsshapy
  version: "2.0.0"

source:
  fn: gsshapy-2.0.0.tar.gz
  url: https://pypi.python.org/packages/source/g/gsshapy/gsshapy-2.0.0.tar.gz
  md5: f034413edfd5fd5b9af832deafa7ad8b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gsshapy = gsshapy:main
    #
    # Would create an entry point called gsshapy that calls gsshapy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sqlalchemy >=0.7
    - mapkit >=1.1.0

  run:
    - python
    - sqlalchemy >=0.7
    - mapkit >=1.1.0

test:
  # Python imports
  imports:
    - gsshapy
    - gsshapy.base
    - gsshapy.lib
    - gsshapy.orm
    - gsshapy.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/swainn/gsshapy
  license: BSD 2-Clause License
  summary: 'An SQLAlchemy ORM for GSSHA model files.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
