package:
  name: django-adminactions
  version: "0.8.1"

source:
  fn: django-adminactions-0.8.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-adminactions/django-adminactions-0.8.1.tar.gz
  md5: 1ecc638944db6b3cffd7997e3312b171
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-adminactions = django-adminactions:main
    #
    # Would create an entry point called django-adminactions that calls django-adminactions.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six
    - pytz
    - unicodecsv >=0.9.4
    - xlrd >=0.9.2
    - xlwt-future

  run:
    - python
    - six
    - pytz
    - unicodecsv >=0.9.4
    - xlrd >=0.9.2
    - xlwt-future

test:
  # Python imports
  imports:
    - adminactions
    - adminactions.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/saxix/django-adminactions
  license: BSD
  summary: 'Collections of useful actions to use with django.contrib.admin.ModelAdmin'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
