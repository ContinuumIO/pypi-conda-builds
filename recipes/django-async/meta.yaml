package:
  name: django-async
  version: "0.6"

source:
  fn: django-async-0.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-async/django-async-0.6.tar.gz
  md5: 881621fd1a95262df060ae1c0c0407b9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-async = django-async:main
    #
    # Would create an entry point called django-async that calls django-async.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - lockfile
    - simplejson

  run:
    - python
    - lockfile
    - simplejson

test:
  # Python imports
  imports:
    - async
    - async.management
    - async.management.commands
    - async.migrations
    - async.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.kirit.com/Django%20Async
  license: Boost Software License - Version 1.0 - August 17th, 2003
  summary: 'Asynchronous task execution with proper database transaction management for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
