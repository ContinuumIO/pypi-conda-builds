package:
  name: django-multipleformwizard
  version: "0.2.16"

source:
  fn: django-multipleformwizard-0.2.16.tar.gz
  url: https://pypi.python.org/packages/source/d/django-multipleformwizard/django-multipleformwizard-0.2.16.tar.gz
  md5: c4579ee15d31278ab59496869c30dc0f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-multipleformwizard = django-multipleformwizard:main
    #
    # Would create an entry point called django-multipleformwizard that calls django-multipleformwizard.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6
    - six >=1.9.0
    - django-formtools ==1.0

  run:
    - python
    - django >=1.6
    - six >=1.9.0
    - django-formtools ==1.0

test:
  # Python imports
  imports:
    - multipleformwizard

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/vikingco/django-multipleformwizard
  license: MIT License
  summary: 'An extension to the official Django form wizard supporting multiple forms on a wizard step.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
