package:
  name: buffetstring
  version: "0.1.1"

source:
  fn: BuffetString-0.1.1.zip
  url: https://pypi.python.org/packages/source/B/BuffetString/BuffetString-0.1.1.zip
  md5: fade0e01aeeb219a980c9415669bb7bb
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - buffetstring = buffetstring:main
    #
    # Would create an entry point called buffetstring that calls buffetstring.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - buffetstring

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://projects.dowski.com/projects/buffetstring
  license: MIT
  summary: 'Python string Template plugin'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
