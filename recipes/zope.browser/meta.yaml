package:
  name: zope.browser
  version: "2.1.0"

source:
  fn: zope.browser-2.1.0.tar.gz
  url: https://pypi.python.org/packages/source/z/zope.browser/zope.browser-2.1.0.tar.gz
  md5: ae13e577df80e278ed1fc399c49b9fda
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zope.browser = zope.browser:main
    #
    # Would create an entry point called zope.browser that calls zope.browser.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.interface

  run:
    - python
    - setuptools
    - zope.interface

test:
  # Python imports
  imports:
    - zope
    - zope.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/zope.browser
  license: Zope Public License
  summary: 'Shared Zope Toolkit browser components'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
