package:
  name: django-discussions
  version: "0.3.4"

source:
  fn: django-discussions-0.3.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-discussions/django-discussions-0.3.4.tar.gz
  md5: 98e2f38030f4a46105b922c06c275606
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-discussions = django-discussions:main
    #
    # Would create an entry point called django-discussions that calls django-discussions.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - discussions
    - discussions.forms
    - discussions.models
    - discussions.south_migrations
    - discussions.templatetags
    - discussions.tests
    - discussions.views

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/thoas/django-discussions
  license: MIT License
  summary: 'Messaging system for your users'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
