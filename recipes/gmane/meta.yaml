package:
  name: gmane
  version: "0.1.dev25"

source:
  fn: gmane-0.1.dev25.tar.gz
  url: https://pypi.python.org/packages/source/g/gmane/gmane-0.1.dev25.tar.gz
  md5: 61b9e7e8d804b6808e96721bd0fec322
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gmane = gmane:main
    #
    # Would create an entry point called gmane that calls gmane.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - gmane

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ttm/gmane
  license: Public Domain
  summary: 'Utilities for the analysis of the GMANE email list database'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
