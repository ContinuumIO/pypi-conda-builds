package:
  name: flask-shorturl
  version: "0.1.2"

source:
  fn: Flask-ShortUrl-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-ShortUrl/Flask-ShortUrl-0.1.2.tar.gz
  md5: 6e59390e2958e4b7ad5cd4b55e24fc1e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-shorturl = flask-shorturl:main
    #
    # Would create an entry point called flask-shorturl that calls flask-shorturl.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask

  run:
    - python
    - flask

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lepture/flask-shorturl
  license: BSD License
  summary: 'Short URL generaotr for Flask'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
