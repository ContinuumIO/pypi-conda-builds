package:
  name: beecloud
  version: "1.2.4"

source:
  fn: beecloud-1.2.4.tar.gz
  url: https://pypi.python.org/packages/source/b/beecloud/beecloud-1.2.4.tar.gz
  md5: 8ebd9e3ae249e4ebe03850953043a2a9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - beecloud = beecloud:main
    #
    # Would create an entry point called beecloud that calls beecloud.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - bcpay
    - beecloud
    - example

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Copyright 2014-2015 beecloud
  summary: 'beecloud, make pay more simple'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
