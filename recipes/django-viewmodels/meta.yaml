package:
  name: django-viewmodels
  version: "0.3.7"

source:
  fn: django-viewmodels-0.3.7.tar.gz
  url: https://pypi.python.org/packages/source/d/django-viewmodels/django-viewmodels-0.3.7.tar.gz
  md5: d7bc9708a7e712192a081782fb241883
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-viewmodels = django-viewmodels:main
    #
    # Would create an entry point called django-viewmodels that calls django-viewmodels.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - djviewmodels

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: BSD License
  summary: 'Class based views for Django with automatic viewmodel rendering'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
