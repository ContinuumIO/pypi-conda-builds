package:
  name: django-vendors
  version: "0.2-0"

source:
  fn: django-vendors-0.2-0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-vendors/django-vendors-0.2-0.tar.gz
  md5: 1c18da76280e5486436dfa731ada602f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-vendors = django-vendors:main
    #
    # Would create an entry point called django-vendors that calls django-vendors.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - vendors
    - vendors.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/powellc/django-vendors/
  license: BSD License
  summary: 'Vendor management for the Django framework.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
