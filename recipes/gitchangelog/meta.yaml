package:
  name: gitchangelog
  version: "2.2.0"

source:
  fn: gitchangelog-2.2.0.tar.gz
  url: https://pypi.python.org/packages/source/g/gitchangelog/gitchangelog-2.2.0.tar.gz
  md5: 5f5da8e9635476de99df8c3dbf938560
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gitchangelog = gitchangelog:main
    #
    # Would create an entry point called gitchangelog that calls gitchangelog.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - gitchangelog

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD License
  summary: 'gitchangelog generates a changelog thanks to git log.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
