package:
  name: stripe
  version: "1.22.3"

source:
  fn: stripe-1.22.3.tar.gz
  url: https://pypi.python.org/packages/source/s/stripe/stripe-1.22.3.tar.gz
  md5: 63b60f1c0bcdb6bdaec45cc35b713843
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - stripe = stripe:main
    #
    # Would create an entry point called stripe that calls stripe.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=0.8.8

  run:
    - python
    - requests >=0.8.8

test:
  # Python imports
  imports:
    - stripe
    - stripe.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/stripe/stripe-python
  license: MIT License
  summary: 'Stripe python bindings'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
