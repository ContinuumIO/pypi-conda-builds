package:
  name: requests-auth-mashery
  version: "0.0.4"

source:
  fn: requests-auth-mashery-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/r/requests-auth-mashery/requests-auth-mashery-0.0.4.tar.gz
  md5: 838aa7e25bbbd95c98ff255137c4cac2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - requests-auth-mashery = requests-auth-mashery:main
    #
    # Would create an entry point called requests-auth-mashery that calls requests-auth-mashery.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - requests >=1.2.3

  run:
    - python
    - requests >=1.2.3

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dasevilla/requests-auth-mashery
  license: UNKNOWN
  summary: 'Mashery authentication for requests'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
