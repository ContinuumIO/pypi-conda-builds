package:
  name: torngithub
  version: "0.1.3"

source:
  fn: torngithub-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/t/torngithub/torngithub-0.1.3.tar.gz
  md5: cf39109648c7107ed608a3530c403ac1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - torngithub = torngithub:main
    #
    # Would create an entry point called torngithub that calls torngithub.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - torngithub

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jkeylu/torngithub
  license: http://www.apache.org/licenses/LICENSE-2.0
  summary: 'Github authentication for tornado'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
