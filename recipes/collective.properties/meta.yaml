package:
  name: collective.properties
  version: "1.0a2"

source:
  fn: collective.properties-1.0a2.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.properties/collective.properties-1.0a2.tar.gz
  md5: 9c975da143603ca79ba31aa1c9d0c7a0
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.properties = collective.properties:main
    #
    # Would create an entry point called collective.properties that calls collective.properties.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.z3cform

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.z3cform

test:
  # Python imports
  imports:
    - collective
    - collective.properties

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/collective.properties
  license: GPL
  summary: 'Provides form to update object properties via Plone UI.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
