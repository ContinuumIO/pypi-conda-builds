package:
  name: eventmanager
  version: "0.7"

source:
  fn: EventManager-0.7.tar.gz
  url: https://pypi.python.org/packages/source/E/EventManager/EventManager-0.7.tar.gz
  md5: d9a4fad6eaedaf6bc19324f774c53637
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - eventmanager = eventmanager:main
    #
    # Would create an entry point called eventmanager that calls eventmanager.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - EventManager
    - eventmanager

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dkkline/EventManager
  license: MIT License
  summary: 'My take on a pythonic, and simple event system.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
