package:
  name: envconf
  version: "0.1.0"

source:
  fn: envconf-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/e/envconf/envconf-0.1.0.tar.gz
  md5: 2a3d9721cc737d9841a52ba94ce80582
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - envconf = envconf:main
    #
    # Would create an entry point called envconf that calls envconf.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - envconf

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bfontaine/envconf
  license: MIT License
  summary: 'An unique way to manage your configuration'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
