package:
  name: django-testclient-extensions
  version: "0.1.4"

source:
  fn: django-testclient-extensions-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-testclient-extensions/django-testclient-extensions-0.1.4.tar.gz
  md5: 2b397bc6d39f131ee2df45fc33519142
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-testclient-extensions = django-testclient-extensions:main
    #
    # Would create an entry point called django-testclient-extensions that calls django-testclient-extensions.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six
    - mock

  run:
    - python
    - six
    - mock

test:
  # Python imports
  imports:
    - testclient_extensions

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ikame/django-test-client-extensions
  license: MIT
  summary: "Extensions to Django's built-in test client."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
