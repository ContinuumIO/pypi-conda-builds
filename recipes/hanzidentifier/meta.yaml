package:
  name: hanzidentifier
  version: "1.0.1"

source:
  fn: hanzidentifier-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/h/hanzidentifier/hanzidentifier-1.0.1.tar.gz
  md5: 2005e134fdd19cfd6145b0ce16228f9e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hanzidentifier = hanzidentifier:main
    #
    # Would create an entry point called hanzidentifier that calls hanzidentifier.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zhon >=1.1.3

  run:
    - python
    - zhon >=1.1.3

test:
  # Python imports
  imports:
    - hanzidentifier

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tsroten/hanzidentifier
  license: MIT License
  summary: 'Python module that identifies Chinese text as Simplified or Traditional.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
