package:
  name: docker-registry-driver-ufile
  version: "0.1.0"

source:
  fn: docker-registry-driver-ufile-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/docker-registry-driver-ufile/docker-registry-driver-ufile-0.1.0.tar.gz
  md5: 2d4b33b2fe0012a4593da60ec85ea2f7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - docker-registry-driver-ufile = docker-registry-driver-ufile:main
    #
    # Would create an entry point called docker-registry-driver-ufile that calls docker-registry-driver-ufile.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - docker-registry-core >=2,<3
    - requests-ucloud ==0.3.0
    - requests ==2.3.0

  run:
    - python
    - docker-registry-core >=2,<3
    - requests-ucloud ==0.3.0
    - requests ==2.3.0

test:
  # Python imports
  imports:
    - docker_registry
    - docker_registry.drivers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/SkyLothar/docker-registry-driver-ufile
  license: Apache Software License
  summary: 'Docker registry ufile driver'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
