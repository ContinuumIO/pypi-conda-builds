package:
  name: biribiri.rpc.client
  version: "0.1"

source:
  fn: biribiri.rpc.client-0.1.tar.gz
  url: https://pypi.python.org/packages/source/b/biribiri.rpc.client/biribiri.rpc.client-0.1.tar.gz
  md5: 6e0025f09c83d60f099aa1a78448db34
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - biribiri.rpc.client = biribiri.rpc.client:main
    #
    # Would create an entry point called biribiri.rpc.client that calls biribiri.rpc.client.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - simplejson

  run:
    - python
    - simplejson

test:
  # Python imports
  imports:
    - biribiri
    - biribiri.rpc
    - biribiri.rpc.client

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://biribiri.enodev.org
  license: BSDL
  summary: 'Simple client class for accessing JSON rpc web-servers'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
