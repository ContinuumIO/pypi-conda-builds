package:
  name: asynqp
  version: "0.3"

source:
  fn: asynqp-0.3.zip
  url: https://pypi.python.org/packages/source/a/asynqp/asynqp-0.3.zip
  md5: 6b36661a30a6e48f5fff18afeb0e053e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - asynqp = asynqp:main
    #
    # Would create an entry point called asynqp that calls asynqp.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - asynqp

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/benjamin-hodgson/asynqp
  license: MIT License
  summary: 'An AMQP (aka RabbitMQ) client library for asyncio.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
