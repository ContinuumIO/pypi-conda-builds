package:
  name: couchfti
  version: "0.1.2dev"

source:
  fn: couchfti-0.1.2dev.tar.gz
  url: https://pypi.python.org/packages/source/c/couchfti/couchfti-0.1.2dev.tar.gz
  md5: bc8999aa535ceb8055926ed975f39c77
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - couchfti = couchfti:main
    #
    # Would create an entry point called couchfti that calls couchfti.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyparsing

  run:
    - python
    - setuptools
    - pyparsing

test:
  # Python imports
  imports:
    - couchfti

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://ish.io
  license: UNKNOWN
  summary: 'Xapian base full text indexing for couchdb'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
