package:
  name: bigfiveinventory
  version: "1.0.1"

source:
  fn: BigFiveInventory-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/B/BigFiveInventory/BigFiveInventory-1.0.1.tar.gz
  md5: 1d3b66ab2e52e746778c100120969f7c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bigfiveinventory = bigfiveinventory:main
    #
    # Would create an entry point called bigfiveinventory that calls bigfiveinventory.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - bigfiveinventory

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: nil
  license: UNKNOWN
  summary: 'this is a simple quiz to test your personality using Big Five Inventory'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
