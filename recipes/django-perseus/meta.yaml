package:
  name: django-perseus
  version: "0.1.1"

source:
  fn: django-perseus-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-perseus/django-perseus-0.1.1.tar.gz
  md5: 1325506154549ff26e729f224542ade5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-perseus = django-perseus:main
    #
    # Would create an entry point called django-perseus that calls django-perseus.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5

  run:
    - python
    - django >=1.5

test:
  # Python imports
  imports:
    - django_perseus
    - django_perseus.importers
    - django_perseus.management
    - django_perseus.management.commands
    - django_perseus.renderers
    - django_perseus.templatetags
    - django_perseus.tests
    - django_perseus.tests.testapp1
    - django_perseus.tests.testapp2

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lockwooddev/django-perseus
  license: MIT License
  summary: 'Builds a static version of your Django website'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
