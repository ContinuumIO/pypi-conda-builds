package:
  name: megrok.resourcelibrary
  version: "0.9.2"

source:
  fn: megrok.resourcelibrary-0.9.2.tar.gz
  url: https://pypi.python.org/packages/source/m/megrok.resourcelibrary/megrok.resourcelibrary-0.9.2.tar.gz
  md5: 6a8f3b917b823ef7d87e3bbc4ecc2344
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - megrok.resourcelibrary = megrok.resourcelibrary:main
    #
    # Would create an entry point called megrok.resourcelibrary that calls megrok.resourcelibrary.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - grok >=0.13
    - zc.resourcelibrary

  run:
    - python
    - setuptools
    - setuptools
    - grok >=0.13
    - zc.resourcelibrary

test:
  # Python imports
  imports:
    - megrok
    - megrok.resourcelibrary
    - megrok.resourcelibrary.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: ZPL
  summary: 'Static resource library support for Grok.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
