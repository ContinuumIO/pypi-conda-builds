package:
  name: expiringview
  version: "0.2"

source:
  fn: ExpiringView-0.2.tar.gz
  url: https://pypi.python.org/packages/source/E/ExpiringView/ExpiringView-0.2.tar.gz
  md5: ae81753f3d18f7d1ca54dfe46671eb6d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - expiringview = expiringview:main
    #
    # Would create an entry point called expiringview that calls expiringview.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - expiringview

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/mikeboers/ExpiringView
  license: BSD License
  summary: 'Mapping class which gives key/value pairs an expiry time.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
