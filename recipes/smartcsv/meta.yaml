package:
  name: smartcsv
  version: "0.2.3"

source:
  fn: smartcsv-0.2.3.tar.gz
  url: https://pypi.python.org/packages/source/s/smartcsv/smartcsv-0.2.3.tar.gz
  md5: 8524637dac17582b8b93b91d43204011
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - smartcsv = smartcsv:main
    #
    # Would create an entry point called smartcsv that calls smartcsv.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - smartcsv

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/santiagobasulto/smartcsv
  license: MIT
  summary: "CSVs are awesome, yet they're pretty dumb. Let's get them smarter!"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
