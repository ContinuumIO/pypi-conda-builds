package:
  name: benri
  version: "0.0.4"

source:
  fn: benri-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/b/benri/benri-0.0.4.tar.gz
  md5: 05b61644eeebf3b992433213e6a0bb00
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - benri = benri:main
    #
    # Would create an entry point called benri that calls benri.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - benri.client >=0.0.2
    - paste >=1.7.5.1
    - pastescript >=1.7.2
    - selector >=0.8.11

  run:
    - python
    - setuptools
    - benri.client >=0.0.2
    - paste >=1.7.5.1
    - pastescript >=1.7.2
    - selector >=0.8.11

test:
  # Python imports
  imports:
    - benri
    - benri.app
    - benri.security
    - benri.templates
    - benri.wsgiserver

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: UNKNOWN
  summary: 'A REST-service creation framework.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
