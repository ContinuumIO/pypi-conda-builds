package:
  name: ecl_facebook
  version: "1.4.1"

source:
  fn: ecl_facebook-1.4.1.tar.gz
  url: https://pypi.python.org/packages/source/e/ecl_facebook/ecl_facebook-1.4.1.tar.gz
  md5: 76f446e5dab6f311e337c505f197af87
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ecl_facebook = ecl_facebook:main
    #
    # Would create an entry point called ecl_facebook that calls ecl_facebook.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - ecl_facebook

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://git.elmcitylabs.com/ecl-facebook
  license: MIT License
  summary: 'Easy Facebook integration for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
