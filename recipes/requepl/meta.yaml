package:
  name: requepl
  version: "0.0.12"

source:
  fn: requepl-0.0.12.tar.gz
  url: https://pypi.python.org/packages/source/r/requepl/requepl-0.0.12.tar.gz
  md5: 4cd22300d9aeac483f8ff6b3272ee97c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - requepl = requepl:main
    #
    # Would create an entry point called requepl that calls requepl.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=0.8.0

  run:
    - python
    - requests >=0.8.0

test:
  # Python imports
  imports:
    - requepl

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/talos/requepl
  license: GPLv3
  summary: 'HTTP REPL with multiple sessions.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
