package:
  name: druid
  version: "0.2.1"

source:
  fn: Druid-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/D/Druid/Druid-0.2.1.tar.gz
  md5: bba3353aec617cb527cd34f898892bc8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - druid = druid:main
    #
    # Would create an entry point called druid that calls druid.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - tumulus
    - pillow

  run:
    - python
    - tumulus
    - pillow

test:
  # Python imports
  imports:
    - druid

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/oksome/Druid
  license: GNU Affero General Public License v3
  summary: 'Doing Magic things with Tumulus'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
