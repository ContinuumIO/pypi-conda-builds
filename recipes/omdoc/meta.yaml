package:
  name: omdoc
  version: "0.1.3.alpha"

source:
  fn: omdoc-0.1.3.alpha.tar.gz
  url: https://pypi.python.org/packages/source/o/omdoc/omdoc-0.1.3.alpha.tar.gz
  md5: 37dccb73bc396563ca55ad46101a2d39
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - omdoc = omdoc:main
    #
    # Would create an entry point called omdoc that calls omdoc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - jinja2
    - pygments
    - beautifulsoup4
    - markdown

  run:
    - python
    - jinja2
    - pygments
    - beautifulsoup4
    - markdown

test:
  # Python imports
  imports:
    - omdoc

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/obscuremetaphor/omdoc
  license: BSD 3 Clause
  summary: 'A lightweight documentation builder.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
