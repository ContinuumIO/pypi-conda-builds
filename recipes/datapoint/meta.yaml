package:
  name: datapoint
  version: "0.2.2"

source:
  fn: datapoint-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/d/datapoint/datapoint-0.2.2.tar.gz
  md5: 219151e171acd0dd659d4dec178ec7f5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - datapoint = datapoint:main
    #
    # Would create an entry point called datapoint that calls datapoint.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - requests >=2.3.0
    - appdirs

  run:
    - python
    - requests >=2.3.0
    - appdirs

test:
  # Python imports
  imports:
    - datapoint

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jacobtomlinson/datapoint-python
  license: TBC
  summary: "Python interface to the Met Office's Datapoint API"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
