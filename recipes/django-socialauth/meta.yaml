package:
  name: django-socialauth
  version: "0.1.2c"

source:
  fn: django_socialauth-0.1.2c.tar.gz
  url: https://pypi.python.org/packages/source/d/django-socialauth/django_socialauth-0.1.2c.tar.gz
  md5: 91f37c5a7327d63200d13aec5aa02fe9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-socialauth = django-socialauth:main
    #
    # Would create an entry point called django-socialauth that calls django-socialauth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - openid_consumer
    - socialauth
    - socialauth/lib
    - socialauth/templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://socialauth.uswaretech.net/
  license: UNKNOWN
  summary: 'Allows logging via Facebook, Yahoo, Gmail, Twitter and Openid '

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
