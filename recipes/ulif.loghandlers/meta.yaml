package:
  name: ulif.loghandlers
  version: "0.1.1"

source:
  fn: ulif.loghandlers-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/u/ulif.loghandlers/ulif.loghandlers-0.1.1.tar.gz
  md5: a68aadefd3d0eb64db3d7d959116496b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ulif.loghandlers = ulif.loghandlers:main
    #
    # Would create an entry point called ulif.loghandlers that calls ulif.loghandlers.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout

test:
  # Python imports
  imports:
    - ulif
    - ulif.loghandlers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/ulif.loghandlers
  license: GNU Lesser General Public License v3 or later (LGPLv3+)
  summary: 'Additional logging handlers.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
