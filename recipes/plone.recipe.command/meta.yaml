package:
  name: plone.recipe.command
  version: "1.1"

source:
  fn: plone.recipe.command-1.1.zip
  url: https://pypi.python.org/packages/source/p/plone.recipe.command/plone.recipe.command-1.1.zip
  md5: d4316a4d05f527d987b9d6e43e4c2bc6
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.recipe.command = plone.recipe.command:main
    #
    # Would create an entry point called plone.recipe.command that calls plone.recipe.command.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zc.buildout
    - setuptools

  run:
    - python
    - setuptools
    - zc.buildout
    - setuptools

test:
  # Python imports
  imports:
    - plone
    - plone.recipe
    - plone.recipe.command

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.python.org/pypi/plone.recipe.command
  license: GPL
  summary: 'Run arbitrary commands from buildout'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
