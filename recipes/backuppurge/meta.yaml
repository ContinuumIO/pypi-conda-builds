package:
  name: backuppurge
  version: "1.0.2"

source:
  fn: backuppurge-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/b/backuppurge/backuppurge-1.0.2.tar.gz
  md5: c2e0d054eedc6ff8629f70f6b42f1ca2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - backuppurge = backuppurge:main
    #
    # Would create an entry point called backuppurge that calls backuppurge.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - backuppurge

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://thp.io/2013/backuppurge/
  license: Simplified BSD License
  summary: 'Selectively purge daily full backups'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
