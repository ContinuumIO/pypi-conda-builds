package:
  name: isbn_validator
  version: "1.0.0"

source:
  fn: isbn_validator-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/i/isbn_validator/isbn_validator-1.0.0.tar.gz
  md5: 7bec995b784f7815559cde89db34b14b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - isbn_validator = isbn_validator:main
    #
    # Would create an entry point called isbn_validator that calls isbn_validator.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - isbn_validator

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/edeposit/isbn_validator
  license: MIT License
  summary: 'ISBN validation module. MIT license.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
