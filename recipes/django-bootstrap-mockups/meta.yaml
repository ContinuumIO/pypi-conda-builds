package:
  name: django-bootstrap-mockups
  version: "0.1.1"

source:
  fn: django-bootstrap-mockups-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-bootstrap-mockups/django-bootstrap-mockups-0.1.1.tar.gz
  md5: 25bcc818037d9940547fa8c92264f6f5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-bootstrap-mockups = django-bootstrap-mockups:main
    #
    # Would create an entry point called django-bootstrap-mockups that calls django-bootstrap-mockups.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6.0
    - watchdog >=0.7.1

  run:
    - python
    - django >=1.6.0
    - watchdog >=0.7.1

test:
  # Python imports
  imports:
    - styleguide
    - styleguide.management
    - styleguide.management.commands
    - styleguide.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mirumee/django-bootstrap-mockups
  license: BSD License
  summary: 'Web style guide based on bootstrap'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
