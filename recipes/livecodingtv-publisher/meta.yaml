package:
  name: livecodingtv-publisher
  version: "0.0.6"

source:
  fn: livecodingtv-publisher-0.0.6.tar.gz
  url: https://pypi.python.org/packages/source/l/livecodingtv-publisher/livecodingtv-publisher-0.0.6.tar.gz
  md5: 843cc6ed9ace2a69962685da55bfd300
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - livecodingtv-publisher = livecodingtv-publisher:main
    #
    # Would create an entry point called livecodingtv-publisher that calls livecodingtv-publisher.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/psaavedra/livecodingtv-publisher
  license: MIT License
  summary: 'A livecodingtv streaming tool'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
