package:
  name: mdx_video
  version: "0.1.7.6"

source:
  fn: mdx_video-0.1.7.6.tar.gz
  url: https://pypi.python.org/packages/source/m/mdx_video/mdx_video-0.1.7.6.tar.gz
  md5: 4ded91789bf3cb9aba207aacb1dfe5ad
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mdx_video = mdx_video:main
    #
    # Would create an entry point called mdx_video that calls mdx_video.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - markdown >=2.2

  run:
    - python
    - markdown >=2.2

test:
  # Python imports
  imports:
    - mdx_video

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'Python-Markdown extension for easy video embedding'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
