package:
  name: easyshop.catalog
  version: "0.1a1"

source:
  fn: easyshop.catalog-0.1a1.tar.gz
  url: https://pypi.python.org/packages/source/e/easyshop.catalog/easyshop.catalog-0.1a1.tar.gz
  md5: bfe1cf053e351ae2c991eb91b60da8b0
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - easyshop.catalog = easyshop.catalog:main
    #
    # Would create an entry point called easyshop.catalog that calls easyshop.catalog.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - easyshop
    - easyshop.catalog
    - easyshop.catalog.adapters
    - easyshop.catalog.ajax
    - easyshop.catalog.browser
    - easyshop.catalog.content
    - easyshop.catalog.portlets
    - easyshop.catalog.subscribers
    - easyshop.catalog.viewlets

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://iqpp.de
  license: GPL
  summary: 'Products and Categories for EasyShop'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
