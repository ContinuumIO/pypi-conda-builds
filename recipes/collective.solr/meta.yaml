package:
  name: collective.solr
  version: "4.1.0"

source:
  fn: collective.solr-4.1.0.zip
  url: https://pypi.python.org/packages/source/c/collective.solr/collective.solr-4.1.0.zip
  md5: 9e41183ee1547184cb8792cc87246a37
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.solr = collective.solr:main
    #
    # Would create an entry point called collective.solr that calls collective.solr.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - acquisition
    - archetypes.schemaextender
    - collective.indexing >=2.0a2
    - collective.js.showmore
    - datetime
    - plone >=4.1
    - plone.app.content
    - plone.app.controlpanel
    - plone.app.layout
    - plone.app.vocabularies
    - plone.app.testing
    - plone.browserlayer
    - plone.indexer
    - products.archetypes
    - products.cmfcore
    - products.cmfdefault
    - products.genericsetup
    - setuptools
    - transaction
    - unidecode
    - zodb3
    - zope.component
    - zope.formlib
    - zope.i18nmessageid
    - zope.interface
    - zope.publisher
    - zope.schema
    - zope2 >=2.13

  run:
    - python
    - setuptools
    - acquisition
    - archetypes.schemaextender
    - collective.indexing >=2.0a2
    - collective.js.showmore
    - datetime
    - plone >=4.1
    - plone.app.content
    - plone.app.controlpanel
    - plone.app.layout
    - plone.app.vocabularies
    - plone.app.testing
    - plone.browserlayer
    - plone.indexer
    - products.archetypes
    - products.cmfcore
    - products.cmfdefault
    - products.genericsetup
    - setuptools
    - transaction
    - unidecode
    - zodb3
    - zope.component
    - zope.formlib
    - zope.i18nmessageid
    - zope.interface
    - zope.publisher
    - zope.schema
    - zope2 >=2.13

test:
  # Python imports
  imports:
    - collective
    - collective.solr
    - collective.solr.browser
    - collective.solr.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/collective.solr
  license: GNU General Public License (GPL)
  summary: 'Solr integration for external indexing and searching.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
