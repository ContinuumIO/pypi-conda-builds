package:
  name: python-bitbucket-api
  version: "0.20121010"

source:
  fn: python-bitbucket-api-0.20121010.tar.gz
  url: https://pypi.python.org/packages/source/p/python-bitbucket-api/python-bitbucket-api-0.20121010.tar.gz
  md5: d0673022055527e642b29e7c7466d015
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-bitbucket-api = python-bitbucket-api:main
    #
    # Would create an entry point called python-bitbucket-api that calls python-bitbucket-api.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - python-commodity
    - requests

  run:
    - python
    - python-commodity
    - requests

test:
  # Python imports
  imports:
    - bitbucket

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/DavidVilla/python-bitbucket-api
  license: GPL v2 or later
  summary: 'Bitbucket Python API and utilities'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
