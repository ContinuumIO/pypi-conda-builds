package:
  name: eulfedora
  version: "1.0.0"

source:
  fn: eulfedora-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/e/eulfedora/eulfedora-1.0.0.tar.gz
  md5: 4428bf3392b4bd27bc137c8beb68b9ae
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - eulfedora = eulfedora:main
    #
    # Would create an entry point called eulfedora that calls eulfedora.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - eulxml >=0.18.0
    - rdflib >=3.0
    - python-dateutil
    - requests >=2.5.0
    - requests-toolbelt
    - pycrypto

  run:
    - python
    - eulxml >=0.18.0
    - rdflib >=3.0
    - python-dateutil
    - requests >=2.5.0
    - requests-toolbelt
    - pycrypto

test:
  # Python imports
  imports:
    - eulfedora
    - eulfedora.indexdata
    - eulfedora.management
    - eulfedora.management.commands
    - eulfedora.templatetags
    - scripts
    - test
    - test.test_fedora
    - test.test_indexdata

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/emory-libraries/eulfedora
  license: Apache Software License
  summary: 'Idiomatic access to digital objects in a Fedora Commons repository'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
