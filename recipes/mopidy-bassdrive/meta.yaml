package:
  name: mopidy-bassdrive
  version: "0.2.0"

source:
  fn: Mopidy-Bassdrive-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/M/Mopidy-Bassdrive/Mopidy-Bassdrive-0.2.0.tar.gz
  md5: 9eb93e2726e8e98ceb0447757945c92b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mopidy-bassdrive = mopidy-bassdrive:main
    #
    # Would create an entry point called mopidy-bassdrive that calls mopidy-bassdrive.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - mopidy >=1.0
    - pykka >=1.1
    - beautifulsoup4 >=4.3

  run:
    - python
    - setuptools
    - setuptools
    - mopidy >=1.0
    - pykka >=1.1
    - beautifulsoup4 >=4.3

test:
  # Python imports
  imports:
    - mopidy_bassdrive

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/felixb/mopidy-bassdrive/
  license: MIT License
  summary: u'Bassdrive archive and stream extension for Mopidy'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
