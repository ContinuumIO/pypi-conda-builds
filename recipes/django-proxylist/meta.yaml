package:
  name: django-proxylist
  version: "0.1.0"

source:
  fn: django-proxylist-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-proxylist/django-proxylist-0.1.0.tar.gz
  md5: 6ba7029b87a89d507f7b74e0af80b6ed
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-proxylist = django-proxylist:main
    #
    # Would create an entry point called django-proxylist that calls django-proxylist.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-countries
    - pycurl
    - pygeoip
    - django-celery

  run:
    - python
    - django-countries
    - pycurl
    - pygeoip
    - django-celery

test:
  # Python imports
  imports:
    - proxylist
    - proxylist.management
    - proxylist.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: GNU General Public License v3 (GPLv3)
  summary: 'Proxy-list management application for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
