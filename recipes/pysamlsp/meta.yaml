package:
  name: pysamlsp
  version: "0.1.4"

source:
  fn: pysamlsp-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/p/pysamlsp/pysamlsp-0.1.4.tar.gz
  md5: f0f15f0cc3ed47fedb6413cf5db86c24
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pysamlsp = pysamlsp:main
    #
    # Would create an entry point called pysamlsp that calls pysamlsp.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - lxml
    - sh

  run:
    - python
    - lxml
    - sh

test:
  # Python imports
  imports:
    - pysamlsp

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/pysamlsp
  license: LICENSE.txt
  summary: 'A service provider implementation for SAML2.0.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
