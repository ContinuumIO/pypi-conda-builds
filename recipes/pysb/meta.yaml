package:
  name: pysb
  version: "1.0.1"

source:
  fn: pysb-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/p/pysb/pysb-1.0.1.tar.gz
  md5: 8ee35ab7206fb41935c067369c014d82
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pysb = pysb:main
    #
    # Would create an entry point called pysb that calls pysb.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - pysb
    - pysb.examples
    - pysb.export
    - pysb.generator
    - pysb.testing
    - pysb.tests
    - pysb.tools

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pysb.org/
  license: BSD License
  summary: 'Python Systems Biology modeling framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
