package:
  name: bottle-api-json-formatting
  version: "0.1.1"

source:
  fn: bottle-api-json-formatting-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/b/bottle-api-json-formatting/bottle-api-json-formatting-0.1.1.tar.gz
  md5: 124a2d558b605acc653101452c4ea17f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bottle-api-json-formatting = bottle-api-json-formatting:main
    #
    # Would create an entry point called bottle-api-json-formatting that calls bottle-api-json-formatting.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - bottle >=0.11

  run:
    - python
    - bottle >=0.11

test:
  # Python imports
  imports:
    - bottle_api_json_formatting

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.bustleandflurry.com
  license: MIT License
  summary: 'A bottle plugin to json format standard and error responses.         Intended for REST APIs.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
