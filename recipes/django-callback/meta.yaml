package:
  name: django-callback
  version: "0.6.1"

source:
  fn: django-callback-0.6.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-callback/django-callback-0.6.1.tar.gz
  md5: 960f0f7ce6204289f8128015f4bc3937
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-callback = django-callback:main
    #
    # Would create an entry point called django-callback that calls django-callback.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - callback
    - callback.management
    - callback.management.commands
    - callback.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/petersanchez/django-callback/
  license: BSD License
  summary: 'Django Callback'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
