package:
  name: django-scarface
  version: "1.0.3"

source:
  fn: django-scarface-1.0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-scarface/django-scarface-1.0.3.tar.gz
  md5: 54ea49477860f56f2041fbeaf617965c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-scarface = django-scarface:main
    #
    # Would create an entry point called django-scarface that calls django-scarface.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - south >=0.8.4
    - boto >=2.34.0

  run:
    - python
    - south >=0.8.4
    - boto >=2.34.0

test:
  # Python imports
  imports:
    - scarface

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.dreipol.ch/
  license: MIT License
  summary: 'Send push notifications to mobile devices using Amazon SNS.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
