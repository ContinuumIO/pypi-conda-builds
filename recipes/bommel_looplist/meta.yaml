package:
  name: bommel_looplist
  version: "1.2.1"

source:
  fn: Bommel_looplist-1.2.1.tar.gz
  url: https://pypi.python.org/packages/source/B/Bommel_looplist/Bommel_looplist-1.2.1.tar.gz
  md5: b63fb8b1e80081090aec924bfe6c7f26
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bommel_looplist = bommel_looplist:main
    #
    # Would create an entry point called bommel_looplist that calls bommel_looplist.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - bommel_looplist

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://pypi.python.org
  license: UNKNOWN
  summary: 'A simple print of nested lists'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
