package:
  name: hurricane
  version: "0.1"

source:
  fn: hurricane-0.1.tar.gz
  url: https://pypi.python.org/packages/source/h/hurricane/hurricane-0.1.tar.gz
  md5: e77bee1bfd70193293971e18d98cae7d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hurricane = hurricane:main
    #
    # Would create an entry point called hurricane that calls hurricane.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - demos
    - demos.chat
    - demos.spritzer
    - hurricane
    - hurricane.handlers
    - hurricane.managers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/ericflo/hurricane
  license: BSD
  summary: 'Hurricane is a project for easily creating Comet web applications'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
