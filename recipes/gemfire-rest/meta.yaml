package:
  name: gemfire-rest
  version: "1.0.2"

source:
  fn: gemfire-rest-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/g/gemfire-rest/gemfire-rest-1.0.2.tar.gz
  md5: 53b63fcccbc98183deb8c40d8a4dabd3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gemfire-rest = gemfire-rest:main
    #
    # Would create an entry point called gemfire-rest that calls gemfire-rest.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - logging
    - requests
    - jsonpickle

  run:
    - python
    - logging
    - requests
    - jsonpickle

test:
  # Python imports
  imports:
    - gemfire

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/gemfire/py-gemfire-rest
  license: Apache 2.0
  summary: "Python client for GemFire's REST service "

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
