package:
  name: django-classy-mail
  version: "0.1.6"

source:
  fn: django-classy-mail-0.1.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-classy-mail/django-classy-mail-0.1.6.tar.gz
  md5: 8929c20cf8405695b9c4148fcbde1e70
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-classy-mail = django-classy-mail:main
    #
    # Would create an entry point called django-classy-mail that calls django-classy-mail.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django >=1.5
    - markdown >=2.4
    - beautifulsoup >=3.2
    - pyyaml >=3.10

  run:
    - python
    - django >=1.5
    - markdown >=2.4
    - beautifulsoup >=3.2
    - pyyaml >=3.10

test:
  # Python imports
  imports:
    - classy_mail
    - classy_mail.mixins

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/alexlovelltroy/django-classy-mail
  license: LICENSE.txt
  summary: 'Class-Based Email for Django built with Mixins'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
