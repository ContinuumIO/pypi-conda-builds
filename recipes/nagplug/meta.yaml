package:
  name: nagplug
  version: "1.0.3"

source:
  fn: nagplug-1.0.3.tar.gz
  url: https://pypi.python.org/packages/source/n/nagplug/nagplug-1.0.3.tar.gz
  md5: 54597ebed69549d383d440bf36f6b89c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - nagplug = nagplug:main
    #
    # Would create an entry point called nagplug that calls nagplug.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - nagplug

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/nlm/nagplug
  license: MIT License
  summary: 'Nagios guidelines-compliant plugin creation library (Shinken, Icinga, Centreon)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
