package:
  name: responses
  version: "0.4.0"

source:
  fn: responses-0.4.0.tar.gz
  url: https://pypi.python.org/packages/source/r/responses/responses-0.4.0.tar.gz
  md5: e3e8171997e22f46387908df30c621c0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - responses = responses:main
    #
    # Would create an entry point called responses that calls responses.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=2.0
    - cookies
    - mock
    - six
    - pytest
    - pytest-cov
    - flake8

  run:
    - python
    - requests >=2.0
    - cookies
    - mock
    - six
    - pytest
    - pytest-cov
    - flake8

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Apache 2.0
  summary: 'A utility library for mocking out the `requests` Python library.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
