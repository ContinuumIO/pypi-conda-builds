package:
  name: oleg-flask-sessions
  version: "0.2.5"

source:
  fn: oleg-flask-sessions-0.2.5.tar.gz
  url: https://pypi.python.org/packages/source/o/oleg-flask-sessions/oleg-flask-sessions-0.2.5.tar.gz
  md5: e8e45140c884ca80c33fc6d171470b6d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - oleg-flask-sessions = oleg-flask-sessions:main
    #
    # Would create an entry point called oleg-flask-sessions that calls oleg-flask-sessions.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - werkzeug
    - msgpack-python
    - flask
    - olegdb-python

  run:
    - python
    - werkzeug
    - msgpack-python
    - flask
    - olegdb-python

test:
  # Python imports
  imports:
    - olegsessions

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/infoforcefeed/OlegDBFlaskSessions
  license: BSD
  summary: 'Oleg DB backed sessions for Flask'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
