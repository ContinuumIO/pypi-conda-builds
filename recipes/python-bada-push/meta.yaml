package:
  name: python-bada-push
  version: "0.5.1"

source:
  fn: python-bada-push-0.5.1.tar.gz
  url: https://pypi.python.org/packages/source/p/python-bada-push/python-bada-push-0.5.1.tar.gz
  md5: ece2a7eff11f35d036552d7d83f1acc3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-bada-push = python-bada-push:main
    #
    # Would create an entry point called python-bada-push that calls python-bada-push.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests ==1.1.0
    - simplejson ==3.1.0

  run:
    - python
    - requests ==1.1.0
    - simplejson ==3.1.0

test:
  # Python imports
  imports:
    - bada_push

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/godickii/python-samsung-bada-push-client
  license: BSD License
  summary: 'Python module for push messaging service for Samsung Bada.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
