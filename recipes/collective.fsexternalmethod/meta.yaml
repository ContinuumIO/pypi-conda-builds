package:
  name: collective.fsexternalmethod
  version: "0.1"

source:
  fn: collective.fsexternalmethod-0.1.zip
  url: https://pypi.python.org/packages/source/c/collective.fsexternalmethod/collective.fsexternalmethod-0.1.zip
  md5: 174338c5c8050dd2556b87aabd3c2905
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.fsexternalmethod = collective.fsexternalmethod:main
    #
    # Would create an entry point called collective.fsexternalmethod that calls collective.fsexternalmethod.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.cmfcore
    - products.externalmethod

  run:
    - python
    - setuptools
    - setuptools
    - products.cmfcore
    - products.externalmethod

test:
  # Python imports
  imports:
    - collective
    - collective.fsexternalmethod

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: GPL
  summary: 'A small shim that allows external methods to be present in skin layers.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
