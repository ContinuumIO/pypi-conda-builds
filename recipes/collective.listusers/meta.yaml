package:
  name: collective.listusers
  version: "1.3"

source:
  fn: collective.listusers-1.3.zip
  url: https://pypi.python.org/packages/source/c/collective.listusers/collective.listusers-1.3.zip
  md5: bb16c5453892f3b114fadfda3cc836a3
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.listusers = collective.listusers:main
    #
    # Would create an entry point called collective.listusers that calls collective.listusers.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - collective.elephantvocabulary
    - collective.js.datatables
    - plone.app.z3cform
    - plone.app.vocabularies
    - setuptools
    - pas.plugins.ldap
    - zope.schema >=3.8.0

  run:
    - python
    - setuptools
    - collective.elephantvocabulary
    - collective.js.datatables
    - plone.app.z3cform
    - plone.app.vocabularies
    - setuptools
    - pas.plugins.ldap
    - zope.schema >=3.8.0

test:
  # Python imports
  imports:
    - collective
    - collective.listusers
    - collective.listusers.browser
    - collective.listusers.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.listusers
  license: GNU General Public License (GPL)
  summary: 'A search form for listing users.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
