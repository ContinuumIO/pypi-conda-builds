package:
  name: maidenhair-jasco-parser
  version: "0.1.0"

source:
  fn: maidenhair-jasco-parser-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/m/maidenhair-jasco-parser/maidenhair-jasco-parser-0.1.0.tar.gz
  md5: 82fff2efc3eacd04f4231a53cfad5ec1
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - maidenhair-jasco-parser = maidenhair-jasco-parser:main
    #
    # Would create an entry point called maidenhair-jasco-parser that calls maidenhair-jasco-parser.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - maidenhair
    - numpydoc

  run:
    - python
    - setuptools
    - maidenhair
    - numpydoc

test:
  # Python imports
  imports:
    - maidenhair_jasco_parser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT License
  summary: 'maidenhair parser plugin for reading JASCO txt file'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
