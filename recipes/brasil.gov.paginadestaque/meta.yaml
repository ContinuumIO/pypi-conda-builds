package:
  name: brasil.gov.paginadestaque
  version: "1.0a2"

source:
  fn: brasil.gov.paginadestaque-1.0a2.zip
  url: https://pypi.python.org/packages/source/b/brasil.gov.paginadestaque/brasil.gov.paginadestaque-1.0a2.zip
  md5: 96373bce7c156d2e76d62283d9ed8045
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - brasil.gov.paginadestaque = brasil.gov.paginadestaque:main
    #
    # Would create an entry point called brasil.gov.paginadestaque that calls brasil.gov.paginadestaque.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - collective.cover
    - collective.z3cform.datagridfield
    - five.grok
    - plone.api
    - plone.app.upgrade
    - products.cmfplone >=4.3
    - products.genericsetup
    - sc.microsite >=1.0b4
    - setuptools
    - zope.i18nmessageid
    - zope.interface

  run:
    - python
    - setuptools
    - collective.cover
    - collective.z3cform.datagridfield
    - five.grok
    - plone.api
    - plone.app.upgrade
    - products.cmfplone >=4.3
    - products.genericsetup
    - sc.microsite >=1.0b4
    - setuptools
    - zope.i18nmessageid
    - zope.interface

test:
  # Python imports
  imports:
    - brasil
    - brasil.gov
    - brasil.gov.paginadestaque
    - brasil.gov.paginadestaque.Extensions
    - brasil.gov.paginadestaque.behaviors
    - brasil.gov.paginadestaque.browser
    - brasil.gov.paginadestaque.browser.viewlets
    - brasil.gov.paginadestaque.browser.views
    - brasil.gov.paginadestaque.tests
    - brasil.gov.paginadestaque.tiles
    - brasil.gov.paginadestaque.traversal
    - brasil.gov.paginadestaque.upgrades

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/plonegovbr/brasil.gov.paginadestaque
  license: GNU General Public License v2 (GPLv2)
  summary: 'Complemento ao Portal Padrao para criacao de microsites e campanhas'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
