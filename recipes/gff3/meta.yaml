package:
  name: gff3
  version: "0.3.0"

source:
  fn: gff3-0.3.0.zip
  url: https://pypi.python.org/packages/source/g/gff3/gff3-0.3.0.zip
  md5: c648bdb5ddb04fa38f28a07ef52abe64
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gff3 = gff3:main
    #
    # Would create an entry point called gff3 that calls gff3.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - gff3

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/hotdogee/gff3-py
  license: BSD License
  summary: 'Manipulate genomic features and validate the syntax and reference sequence of your GFF3 files.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
