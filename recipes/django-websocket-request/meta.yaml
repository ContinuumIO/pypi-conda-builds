package:
  name: django-websocket-request
  version: "0.1.1"

source:
  fn: django-websocket-request-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-websocket-request/django-websocket-request-0.1.1.tar.gz
  md5: c26a28979bcbee731cfcd270e563a24f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-websocket-request = django-websocket-request:main
    #
    # Would create an entry point called django-websocket-request that calls django-websocket-request.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3

  run:
    - python
    - django >=1.3

test:
  # Python imports
  imports:
    - wsrequest

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/GetBlimp/django-websocket-request
  license: MIT
  summary: "Support for Django's request/response lifecycle to automatically handle WebSocket messages."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
