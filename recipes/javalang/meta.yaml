package:
  name: javalang
  version: "0.9.6"

source:
  fn: javalang-0.9.6.tar.gz
  url: https://pypi.python.org/packages/source/j/javalang/javalang-0.9.6.tar.gz
  md5: acc9a43be9d1013023add6f738caad9b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - javalang = javalang:main
    #
    # Would create an entry point called javalang that calls javalang.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six

  run:
    - python
    - six

test:
  # Python imports
  imports:
    - javalang

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/c2nes/javalang
  license: MIT License
  summary: 'Pure Python Java parser and tools'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
