package:
  name: sacrud
  version: "0.2.5"

source:
  fn: sacrud-0.2.5.tar.gz
  url: https://pypi.python.org/packages/source/s/sacrud/sacrud-0.2.5.tar.gz
  md5: c5644e5d3607844ec6dde52e5897d604
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sacrud = sacrud:main
    #
    # Would create an entry point called sacrud that calls sacrud.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sqlalchemy >=0.9.0
    - transaction
    - six

  run:
    - python
    - sqlalchemy >=0.9.0
    - transaction
    - six

test:
  # Python imports
  imports:
    - sacrud
    - sacrud.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/ITCase/sacrud/
  license: MIT License
  summary: 'SQLAlchemy CRUD.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
