package:
  name: ceph-workbench
  version: "1.5.1"

source:
  fn: ceph-workbench-1.5.1.tar.gz
  url: https://pypi.python.org/packages/source/c/ceph-workbench/ceph-workbench-1.5.1.tar.gz
  md5: 446d1e59a634888efb3c01c770225e97
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ceph-workbench = ceph-workbench:main
    #
    # Would create an entry point called ceph-workbench that calls ceph-workbench.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'a command line tool that implements a development workflow for Ceph'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
