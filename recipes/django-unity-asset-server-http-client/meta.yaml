package:
  name: django-unity-asset-server-http-client
  version: "0.1.4"

source:
  fn: django-unity-asset-server-http-client-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-unity-asset-server-http-client/django-unity-asset-server-http-client-0.1.4.tar.gz
  md5: a5856bddce2d8488853d1481cd05aa2b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-unity-asset-server-http-client = django-unity-asset-server-http-client:main
    #
    # Would create an entry point called django-unity-asset-server-http-client that calls django-unity-asset-server-http-client.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-filter >=0.8
    - djangorestframework >=2.4.3

  run:
    - python
    - django-filter >=0.8
    - djangorestframework >=2.4.3

test:
  # Python imports
  imports:
    - duashttp
    - duashttp.migrations
    - duashttp.views

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tarvitz/django-unity-asset-server-http-client
  license: MIT License
  summary: 'REST-api bases over unity asset server for some routines.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
