package:
  name: alphabet-detector
  version: "0.0.3"

source:
  fn: alphabet-detector-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/a/alphabet-detector/alphabet-detector-0.0.3.tar.gz
  md5: f5a741e48f46496dd55e87422e92c221
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - alphabet-detector = alphabet-detector:main
    #
    # Would create an entry point called alphabet-detector that calls alphabet-detector.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - alphabet_detector

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/EliFinkelshteyn/alphabet-detector
  license: MIT License
  summary: 'A library to detect what alphabet something is written in.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
