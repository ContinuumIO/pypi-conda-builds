package:
  name: gnureadline
  version: "6.3.3"

source:
  fn: gnureadline-6.3.3.tar.gz
  url: https://pypi.python.org/packages/source/g/gnureadline/gnureadline-6.3.3.tar.gz
  md5: c4af83c9a3fbeac8f2da9b5a7c60e51c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gnureadline = gnureadline:main
    #
    # Would create an entry point called gnureadline that calls gnureadline.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

# requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/ludwigschwardt/python-gnureadline
  license: GNU General Public License (GPL)
  summary: 'The standard Python readline extension statically linked against the GNU readline library.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
