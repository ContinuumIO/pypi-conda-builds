package:
  name: memoized-property
  version: "1.0.2"

source:
  fn: memoized-property-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/m/memoized-property/memoized-property-1.0.2.tar.gz
  md5: cd32a557124e7f161a93434c465fe57d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - memoized-property = memoized-property:main
    #
    # Would create an entry point called memoized-property that calls memoized-property.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/estebistec/python-memoized-property
  license: BSD License
  summary: 'A simple python decorator for defining properties that only run their fget function once'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
