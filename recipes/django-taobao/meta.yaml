package:
  name: django-taobao
  version: "0.2.6"

source:
  fn: django-taobao-0.2.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-taobao/django-taobao-0.2.6.tar.gz
  md5: e6607ee97b17c77c38aebe33df9d0491
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-taobao = django-taobao:main
    #
    # Would create an entry point called django-taobao that calls django-taobao.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - taobao
    - taobao.templates

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jcouyang/django-taobao
  license: MIT
  summary: 'Taobao SDK for django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
