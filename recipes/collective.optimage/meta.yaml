package:
  name: collective.optimage
  version: "0.1.0a"

source:
  fn: collective.optimage-0.1.0a.zip
  url: https://pypi.python.org/packages/source/c/collective.optimage/collective.optimage-0.1.0a.zip
  md5: 02c490b7dd6c7276f390665779c16bce
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.optimage = collective.optimage:main
    #
    # Would create an entry point called collective.optimage that calls collective.optimage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.blob

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.blob

test:
  # Python imports
  imports:
    - collective
    - collective.optimage
    - collective.optimage.adapters

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/collective.optimage
  license: GPL
  summary: 'Optimize Plone content images from Web usage'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
