package:
  name: cdist
  version: "3.1.12"

source:
  fn: cdist-3.1.12.tar.gz
  url: https://pypi.python.org/packages/source/c/cdist/cdist-3.1.12.tar.gz
  md5: 669034726cce48a0fb7eb123e09d03c3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cdist = cdist:main
    #
    # Would create an entry point called cdist that calls cdist.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - cdist
    - cdist.core
    - cdist.exec
    - cdist.util

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.nico.schottelius.org/software/cdist/
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'A Usable Configuration Management System'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
