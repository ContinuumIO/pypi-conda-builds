package:
  name: manifesto
  version: "0.5"

source:
  fn: manifesto-0.5.tar.gz
  url: https://pypi.python.org/packages/source/m/manifesto/manifesto-0.5.tar.gz
  md5: 1093db3c3aed5532586649bfe7011ef0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - manifesto = manifesto:main
    #
    # Would create an entry point called manifesto that calls manifesto.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - manifesto
    - tests
    - tests.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/cyberdelia/manifesto
  license: BSD License
  summary: 'Pluggable cache manifest for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
