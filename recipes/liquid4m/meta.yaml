package:
  name: liquid4m
  version: "0.1dev"

source:
  fn: liquid4m-0.1dev.tar.gz
  url: https://pypi.python.org/packages/source/l/liquid4m/liquid4m-0.1dev.tar.gz
  md5: af23a95274b215393d6c62668b7898eb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - liquid4m = liquid4m:main
    #
    # Would create an entry point called liquid4m that calls liquid4m.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - validate_email
    - phonenumbers
    - jinja2
    - python-dateutil

  run:
    - python
    - validate_email
    - phonenumbers
    - jinja2
    - python-dateutil

test:
  # Python imports
  imports:
    - liquid4m
    - liquid4m.fields
    - liquid4m.fieldsets

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: GPL
  summary: 'Liquid form management for dummies'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
