package:
  name: openerp-project-gtd
  version: "7.0.406"

source:
  fn: openerp-project-gtd-7.0.406.tar.gz
  url: https://pypi.python.org/packages/source/o/openerp-project-gtd/openerp-project-gtd-7.0.406.tar.gz
  md5: b28d968d169f59f57c66555b45b1869d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - openerp-project-gtd = openerp-project-gtd:main
    #
    # Would create an entry point called openerp-project-gtd that calls openerp-project-gtd.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - openerp-project

  run:
    - python
    - openerp-project

test:
  # Python imports
  imports:
    - openerp.addons.project_gtd
    - openerp.addons.project_gtd.wizard

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: GNU Affero General Public License v3
  summary: 'Todo Lists'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
