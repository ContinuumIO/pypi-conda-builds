package:
  name: products.reflectoimagescales
  version: "1.0"

source:
  fn: Products.ReflectoImageScales-1.0.zip
  url: https://pypi.python.org/packages/source/P/Products.ReflectoImageScales/Products.ReflectoImageScales-1.0.zip
  md5: 771a1b3c72cb244b240ed18f4fc4676f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - products.reflectoimagescales = products.reflectoimagescales:main
    #
    # Would create an entry point called products.reflectoimagescales that calls products.reflectoimagescales.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - Products
    - Products.ReflectoImageScales

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/Products.ReflectoImageScales
  license: GPL
  summary: 'Thumbnails for Reflecto images'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
