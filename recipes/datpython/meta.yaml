package:
  name: datpython
  version: "1.0.0.2"

source:
  fn: datPython-1.0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/datPython/datPython-1.0.0.2.tar.gz
  md5: ce407142a4785b80540e7a58517b8e34
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - datpython = datpython:main
    #
    # Would create an entry point called datpython that calls datpython.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - datpython

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/pkafei/Dat-Python
  license: BSD License
  summary: 'The Python API for dat'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
