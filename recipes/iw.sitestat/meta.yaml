package:
  name: iw.sitestat
  version: "1.0.0-beta-2"

source:
  fn: iw.sitestat-1.0.0-beta-2.tar.gz
  url: https://pypi.python.org/packages/source/i/iw.sitestat/iw.sitestat-1.0.0-beta-2.tar.gz
  md5: 3505e8e0a11c8a6611657c3bc8f479ec
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - iw.sitestat = iw.sitestat:main
    #
    # Would create an entry point called iw.sitestat that calls iw.sitestat.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - simplejson

  run:
    - python
    - setuptools
    - setuptools
    - simplejson

test:
  # Python imports
  imports:
    - iw
    - iw.sitestat
    - iw.sitestat.browser
    - iw.sitestat.interfaces
    - iw.sitestat.linksfinders
    - iw.sitestat.linksfinders.thirdparty
    - iw.sitestat.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/iw.sitestat
  license: GPL
  summary: 'Use sitestat to analyse your Plone site visits.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
