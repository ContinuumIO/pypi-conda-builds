package:
  name: consul_ha
  version: "0.1.43"

source:
  fn: consul_ha-0.1.43.tar.gz
  url: https://pypi.python.org/packages/source/c/consul_ha/consul_ha-0.1.43.tar.gz
  md5: 5428b7e67f47c2859b74ee81257b51f8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - consul_ha = consul_ha:main
    #
    # Would create an entry point called consul_ha that calls consul_ha.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - consul_ha

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mongohq/consul_ha
  license: MIT License
  summary: 'Server for managing application state and high availability using Consul'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
