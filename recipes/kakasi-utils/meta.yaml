package:
  name: kakasi-utils
  version: "0.1.2"

source:
  fn: kakasi-utils-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/k/kakasi-utils/kakasi-utils-0.1.2.tar.gz
  md5: 09b34fa28248f8ef8c5b49f11186b388
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kakasi-utils = kakasi-utils:main
    #
    # Would create an entry point called kakasi-utils that calls kakasi-utils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - kakasi_utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/403JFW/kakasi-utils
  license: MIT License
  summary: 'KAKASI Utilities'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
