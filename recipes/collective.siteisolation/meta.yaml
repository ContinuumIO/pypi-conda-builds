package:
  name: collective.siteisolation
  version: "0.2"

source:
  fn: collective.siteisolation-0.2.zip
  url: https://pypi.python.org/packages/source/c/collective.siteisolation/collective.siteisolation-0.2.zip
  md5: 4b072d456f4090413249f4dfe3cceb8e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.siteisolation = collective.siteisolation:main
    #
    # Would create an entry point called collective.siteisolation that calls collective.siteisolation.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.component
    - plone.memoize
    - zope.interface

  run:
    - python
    - setuptools
    - zope.component
    - plone.memoize
    - zope.interface

test:
  # Python imports
  imports:
    - collective
    - collective.siteisolation
    - collective.siteisolation.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/collective.siteisolation
  license: GPL
  summary: 'Isolate objects in a Zope root from each other'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
