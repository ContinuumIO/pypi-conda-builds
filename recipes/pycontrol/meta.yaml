package:
  name: pycontrol
  version: "2.1"

source:
  fn: pycontrol-2.1.tar.gz
  url: https://pypi.python.org/packages/source/p/pycontrol/pycontrol-2.1.tar.gz
  md5: cc80ad7d1f78d694de99ac2b426e3dec
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pycontrol = pycontrol:main
    #
    # Would create an entry point called pycontrol that calls pycontrol.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - distribute
    - suds >=0.3.9

  run:
    - python
    - distribute
    - suds >=0.3.9

test:
  # Python imports
  imports:
    - pycontrol

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mcauthorn/pycontrol
  license: GPL
  summary: 'Library for F5 iControl API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
