package:
  name: pyevolve
  version: "0.6rc1"

source:
  fn: Pyevolve-0.6rc1.tar.gz
  url: https://pypi.python.org/packages/source/P/Pyevolve/Pyevolve-0.6rc1.tar.gz
  md5: b9c8caa823c5312cce3abda897f8f5fe
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pyevolve = pyevolve:main
    #
    # Would create an entry point called pyevolve that calls pyevolve.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - pyevolve

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pyevolve.sourceforge.net/
  license: Python Software Foundation License
  summary: 'A complete, free and open-source evolutionary framework written in Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
