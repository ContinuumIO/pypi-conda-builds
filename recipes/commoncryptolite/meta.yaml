package:
  name: commoncryptolite
  version: "0.1a6"

source:
  fn: CommonCryptoLite-0.1a6.tar.gz
  url: https://pypi.python.org/packages/source/C/CommonCryptoLite/CommonCryptoLite-0.1a6.tar.gz
  md5: 1e0783fa81590af61dfbbb3f9bbf2327
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - commoncryptolite = commoncryptolite:main
    #
    # Would create an entry point called commoncryptolite that calls commoncryptolite.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - commoncryptolite

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/schwa/Python-CommonCryptoLite
  license: BSD License
  summary: 'CommonCrypto'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
