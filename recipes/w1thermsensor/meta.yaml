package:
  name: w1thermsensor
  version: "0.02.01"

source:
  fn: w1thermsensor-0.02.01.tar.gz
  url: https://pypi.python.org/packages/source/w/w1thermsensor/w1thermsensor-0.02.01.tar.gz
  md5: 39980ce7bc217960bd022a812ac67cbf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - w1thermsensor = w1thermsensor:main
    #
    # Would create an entry point called w1thermsensor that calls w1thermsensor.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - w1thermsensor

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/timofurrer/w1thermsensor
  license: GPLv2
  summary: 'This little pure python module provides a single class to get the temperature of a w1 sensor'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
