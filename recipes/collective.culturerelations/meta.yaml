package:
  name: collective.culturerelations
  version: "0.5"

source:
  fn: collective.cultureRelations-0.5.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.cultureRelations/collective.cultureRelations-0.5.tar.gz
  md5: 6bf92d0effa7677e2c557ea1a1a52f9e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.culturerelations = collective.culturerelations:main
    #
    # Would create an entry point called collective.culturerelations that calls collective.culturerelations.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.viewlet.references
    - collective.contentleadimage
    - collective.flowplayer

  run:
    - python
    - setuptools
    - setuptools
    - collective.viewlet.references
    - collective.contentleadimage
    - collective.flowplayer

test:
  # Python imports
  imports:
    - collective
    - collective.cultureRelations
    - collective.cultureRelations.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/
  license: GPL
  summary: 'A setup for collective.viewlet.references that configures it for cultural institutions according to the INTK Online Strategy'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
