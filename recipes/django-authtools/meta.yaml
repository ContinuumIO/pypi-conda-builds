package:
  name: django-authtools
  version: "1.2.0"

source:
  fn: django-authtools-1.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-authtools/django-authtools-1.2.0.tar.gz
  md5: 600540da6129a4a48f66e32c83d801bf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-authtools = django-authtools:main
    #
    # Would create an entry point called django-authtools that calls django-authtools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5

  run:
    - python
    - django >=1.5

test:
  # Python imports
  imports:
    - authtools

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://django-authtools.readthedocs.org/
  license: BSD License
  summary: 'Custom user model app for Django featuring email as username and class-based views for authentication.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
