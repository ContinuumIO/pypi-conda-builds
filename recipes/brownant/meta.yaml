package:
  name: brownant
  version: "0.1.5"

source:
  fn: brownant-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/b/brownant/brownant-0.1.5.tar.gz
  md5: 6c35d08a72acc073b0ee658b8cfb11ef
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - brownant = brownant:main
    #
    # Would create an entry point called brownant that calls brownant.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - werkzeug >=0.8
    - requests >=1.0
    - six
    - lxml >=3.1

  run:
    - python
    - werkzeug >=0.8
    - requests >=1.0
    - six
    - lxml >=3.1

test:
  # Python imports
  imports:
    - brownant
    - brownant.pipeline
    - tests.test_pipeline

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/douban/brownant
  license: BSD License
  summary: 'A lightweight web data extracting framework.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
