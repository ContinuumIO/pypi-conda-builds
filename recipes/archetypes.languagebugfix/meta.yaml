package:
  name: archetypes.languagebugfix
  version: "1.0.2"

source:
  fn: archetypes.languagebugfix-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/a/archetypes.languagebugfix/archetypes.languagebugfix-1.0.2.tar.gz
  md5: 4bbe10313893799a49c87b2bbf9fbbcd
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - archetypes.languagebugfix = archetypes.languagebugfix:main
    #
    # Would create an entry point called archetypes.languagebugfix that calls archetypes.languagebugfix.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - archetypes
    - archetypes.languagebugfix

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.simplesconsultoria.com.br/
  license: GPL
  summary: 'This is a package to fix https://dev.plone.org/plone/ticket/8907'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
