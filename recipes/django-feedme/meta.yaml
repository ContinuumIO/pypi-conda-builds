package:
  name: django-feedme
  version: "1.2.2"

source:
  fn: django-feedme-1.2.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-feedme/django-feedme-1.2.2.tar.gz
  md5: db8efc6a42688c71395625d7f0b1f1ae
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-feedme = django-feedme:main
    #
    # Would create an entry point called django-feedme that calls django-feedme.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - feedparser ==5.1.3,<6.0
    - django-bootstrap-static >=2.0,<3.0
    - lxml
    - django-infuse >=0.3

  run:
    - python
    - feedparser ==5.1.3,<6.0
    - django-bootstrap-static >=2.0,<3.0
    - lxml
    - django-infuse >=0.3

test:
  # Python imports
  imports:
    - feedme
    - feedme.management
    - feedme.management.commands
    - feedme.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/dstegelman/django-feedme
  license: MIT License
  summary: 'Django Google Reader Replacement'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
