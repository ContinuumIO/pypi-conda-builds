package:
  name: wildcard.uberoverride
  version: "1.0a1"

source:
  fn: wildcard.uberoverride-1.0a1.zip
  url: https://pypi.python.org/packages/source/w/wildcard.uberoverride/wildcard.uberoverride-1.0a1.zip
  md5: 3ca5814f5bdaa5e5695c1048cbc0ecdd
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - wildcard.uberoverride = wildcard.uberoverride:main
    #
    # Would create an entry point called wildcard.uberoverride that calls wildcard.uberoverride.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - wildcard
    - wildcard.uberoverride

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/wildcard.uberoverride
  license: GPL
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
