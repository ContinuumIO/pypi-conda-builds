package:
  name: gauthify
  version: "2.02"

source:
  fn: gauthify-2.02.tar.gz
  url: https://pypi.python.org/packages/source/g/gauthify/gauthify-2.02.tar.gz
  md5: 3cc835867950685a278bef0ae9100f33
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gauthify = gauthify:main
    #
    # Would create an entry point called gauthify that calls gauthify.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests ==0.14.1

  run:
    - python
    - requests ==0.14.1

test:
  # Python imports
  imports:
    - gauthify

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://www.gauthify.com
  license: MIT
  summary: 'API library for GAuthify.com (Google Authenticator, SMS, email multi factor authentication).'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
