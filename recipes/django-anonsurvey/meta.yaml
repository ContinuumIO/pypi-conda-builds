package:
  name: django-anonsurvey
  version: "0.2.4"

source:
  fn: django-anonsurvey-0.2.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-anonsurvey/django-anonsurvey-0.2.4.tar.gz
  md5: 611fa3f32eef36567155c8d845cf0bcd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-anonsurvey = django-anonsurvey:main
    #
    # Would create an entry point called django-anonsurvey that calls django-anonsurvey.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-tinymce
    - django-tinymce-filebrowser

  run:
    - python
    - django-tinymce
    - django-tinymce-filebrowser

test:
  # Python imports
  imports:
    - anonsurvey

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/darko-poljak/django-anonsurvey
  license: GNU General Public License v3 (GPLv3)
  summary: 'A Django app to create Web-based anonymous surveys'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
