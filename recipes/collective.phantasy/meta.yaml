package:
  name: collective.phantasy
  version: "2.6"

source:
  fn: collective.phantasy-2.6.zip
  url: https://pypi.python.org/packages/source/c/collective.phantasy/collective.phantasy-2.6.zip
  md5: 43a90f690b9feb76f212540d0dbfbd55
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.phantasy = collective.phantasy:main
    #
    # Would create an entry point called collective.phantasy that calls collective.phantasy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - archetypes.schemaextender >=1.5
    - products.smartcolorwidget >=1.1.1

  run:
    - python
    - setuptools
    - setuptools
    - archetypes.schemaextender >=1.5
    - products.smartcolorwidget >=1.1.1

test:
  # Python imports
  imports:
    - collective
    - collective.phantasy
    - collective.phantasy.atphantasy
    - collective.phantasy.atphantasy.browser
    - collective.phantasy.atphantasy.content
    - collective.phantasy.atphantasy.extendcontents
    - collective.phantasy.atphantasy.interfaces
    - collective.phantasy.browser
    - collective.phantasy.interfaces
    - collective.phantasy.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/collective-phantasy
  license: GPL
  summary: 'dynamic theme for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
