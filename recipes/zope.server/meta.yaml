package:
  name: zope.server
  version: "3.9.0"

source:
  fn: zope.server-3.9.0.zip
  url: https://pypi.python.org/packages/source/z/zope.server/zope.server-3.9.0.zip
  md5: 1570c74c06b70d46fa03cfde30375a8e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zope.server = zope.server:main
    #
    # Would create an entry point called zope.server that calls zope.server.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zope.interface
    - zope.publisher
    - zope.security

  run:
    - python
    - setuptools
    - setuptools
    - zope.interface
    - zope.publisher
    - zope.security

test:
  # Python imports
  imports:
    - zope
    - zope.server
    - zope.server.ftp
    - zope.server.ftp.tests
    - zope.server.http
    - zope.server.http.tests
    - zope.server.interfaces
    - zope.server.linereceiver
    - zope.server.logger
    - zope.server.logger.tests
    - zope.server.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/zope.server
  license: Zope Public License
  summary: 'Zope Server (Web and FTP)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
