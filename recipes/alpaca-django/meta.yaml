package:
  name: alpaca-django
  version: "0.3.0"

source:
  fn: alpaca-django-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/a/alpaca-django/alpaca-django-0.3.0.tar.gz
  md5: 4ec4466bb818cfbd5cd26cd49ff52089
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - alpaca-django = alpaca-django:main
    #
    # Would create an entry point called alpaca-django that calls alpaca-django.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - msgpack-python
    - pyzmq

  run:
    - python
    - django
    - msgpack-python
    - pyzmq

test:
  # Python imports
  imports:
    - alpaca_django

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/msiedlarek/alpaca-django
  license: Apache Software License
  summary: 'Alpaca error logger for Django applications.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
