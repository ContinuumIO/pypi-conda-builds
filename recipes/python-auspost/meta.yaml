package:
  name: python-auspost
  version: "0.2"

source:
  fn: python-auspost-0.2.tar.gz
  url: https://pypi.python.org/packages/source/p/python-auspost/python-auspost-0.2.tar.gz
  md5: 77c0b4d0bbfd01c285b637806742b7d8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-auspost = python-auspost:main
    #
    # Would create an entry point called python-auspost that calls python-auspost.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - anyjson
    - httplib2

  run:
    - python
    - anyjson
    - httplib2

test:
  # Python imports
  imports:
    - auspost

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/goodtune/python-auspost
  license: UNKNOWN
  summary: "This is a Python client for Australia Post's Postage Assessment Calculation (PAC)\nand Postcode Search (PCS).\n"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
