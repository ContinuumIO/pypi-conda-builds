package:
  name: django-notifier
  version: "0.7"

source:
  fn: django-notifier-0.7.tar.gz
  url: https://pypi.python.org/packages/source/d/django-notifier/django-notifier-0.7.tar.gz
  md5: b9b64b930ded6b795d4f33fad5014aa1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-notifier = django-notifier:main
    #
    # Would create an entry point called django-notifier that calls django-notifier.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4.0

  run:
    - python
    - django >=1.4.0

test:
  # Python imports
  imports:
    - notifier
    - notifier.management
    - notifier.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/scdoshi/django-notifier/
  license: BSD License
  summary: 'User and Group Notifications for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
