package:
  name: simple_math
  version: "1.0.0"

source:
  fn: simple_math-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/s/simple_math/simple_math-1.0.0.tar.gz
  md5: 7e3bd7c2717c7715b06917f4093a6cbb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - simple_math = simple_math:main
    #
    # Would create an entry point called simple_math that calls simple_math.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - simple_math

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://www.google.com/?gws_rd=ssl
  license: UNKNOWN
  summary: 'some usefull math functions'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
