package:
  name: django-extra-fields
  version: "0.3"

source:
  fn: django-extra-fields-0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-extra-fields/django-extra-fields-0.3.tar.gz
  md5: 7d6a565509b0e0682be3a34a9559551a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-extra-fields = django-extra-fields:main
    #
    # Would create an entry point called django-extra-fields that calls django-extra-fields.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - drf_extra_fields

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license:  License
  summary: 'Additional fields for Django Rest Framework.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
