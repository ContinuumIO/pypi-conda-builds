package:
  name: django-templatesadmin
  version: "0.7"

source:
  fn: django-templatesadmin-0.7.tar.gz
  url: https://pypi.python.org/packages/source/d/django-templatesadmin/django-templatesadmin-0.7.tar.gz
  md5: 586e128a01b340f2c6ade5d1b49b22e6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-templatesadmin = django-templatesadmin:main
    #
    # Would create an entry point called django-templatesadmin that calls django-templatesadmin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - templatesadmin
    - templatesadmin.edithooks
    - templatesadmin.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/bartTC/django-templatesadmin/
  license: BSD License
  summary: 'A Django app to make minor changes to your templates on the fly.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
