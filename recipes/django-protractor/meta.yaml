package:
  name: django-protractor
  version: "0.6"

source:
  fn: django-protractor-0.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-protractor/django-protractor-0.6.tar.gz
  md5: 67ec59740708f637db54a4fd621e6326
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-protractor = django-protractor:main
    #
    # Would create an entry point called django-protractor that calls django-protractor.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4

  run:
    - python
    - django >=1.4

test:
  # Python imports
  imports:
    - protractor
    - protractor.management
    - protractor.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT License
  summary: 'Easily integrate your protractor tests with django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
