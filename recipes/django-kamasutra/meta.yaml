package:
  name: django-kamasutra
  version: "0.2.11"

source:
  fn: django-kamasutra-0.2.11.tar.gz
  url: https://pypi.python.org/packages/source/d/django-kamasutra/django-kamasutra-0.2.11.tar.gz
  md5: f025ad3c4d7b165de1fdf037dd3984df
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-kamasutra = django-kamasutra:main
    #
    # Would create an entry point called django-kamasutra that calls django-kamasutra.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - positions

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/callowayproject/django-kamasutra
  license: Apache Software License
  summary: 'A application to position objects anywhere on a page.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
