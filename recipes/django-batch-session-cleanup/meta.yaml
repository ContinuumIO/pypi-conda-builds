package:
  name: django-batch-session-cleanup
  version: "0.1.1"

source:
  fn: django-batch-session-cleanup-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-batch-session-cleanup/django-batch-session-cleanup-0.1.1.tar.gz
  md5: 038d95ccfad2fc53280de2d381c5f1f8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-batch-session-cleanup = django-batch-session-cleanup:main
    #
    # Would create an entry point called django-batch-session-cleanup that calls django-batch-session-cleanup.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django >=1.3.0

  run:
    - python
    - django >=1.3.0

test:
  # Python imports
  imports:
    - batch_session_cleanup
    - batch_session_cleanup.management
    - batch_session_cleanup.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/isnotajoke/django-batch-session-cleanup
  license: MIT
  summary: 'Tool to clean up expired sessions gracefully'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
