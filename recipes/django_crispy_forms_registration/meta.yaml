package:
  name: django_crispy_forms_registration
  version: "0.1.3"

source:
  fn: django_crispy_forms_registration-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django_crispy_forms_registration/django_crispy_forms_registration-0.1.3.tar.gz
  md5: 14b420f420dca405d0b847d306d8a0ce
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_crispy_forms_registration = django_crispy_forms_registration:main
    #
    # Would create an entry point called django_crispy_forms_registration that calls django_crispy_forms_registration.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django <1.6
    - django-registration >=1.0
    - django-crispy-forms >=1.4.0

  run:
    - python
    - django <1.6
    - django-registration >=1.0
    - django-crispy-forms >=1.4.0

test:
  # Python imports
  imports:
    - crispy_forms_registration
    - crispy_forms_registration.forms
    - django_crispy_forms_registration

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/SoloTodo/django_crispy_forms_registration
  license: LICENSE.txt
  summary: 'Library that merges Django auth, registration, and crispy'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
