package:
  name: python-wtf
  version: "0.1.1"

source:
  fn: python-wtf-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/p/python-wtf/python-wtf-0.1.1.tar.gz
  md5: 43e75151dd0339fee63cca0a0053a017
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-wtf = python-wtf:main
    #
    # Would create an entry point called python-wtf that calls python-wtf.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/jrbl/python-wtf/
  license: Apache Software License
  summary: 'Debugging with print statements, but better'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
