package:
  name: django-bootlog
  version: "1.0.2"

source:
  fn: django-bootlog-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-bootlog/django-bootlog-1.0.2.tar.gz
  md5: 03534b4cddc5307c9f49b5edfbfd6b52
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-bootlog = django-bootlog:main
    #
    # Would create an entry point called django-bootlog that calls django-bootlog.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - bootlog

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: entworks.in
  license: BSD License
  summary: 'A Django Blogging App based on Twitter Bootstrap Theme'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
