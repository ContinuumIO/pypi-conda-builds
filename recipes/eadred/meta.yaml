package:
  name: eadred
  version: "0.3"

source:
  fn: eadred-0.3.tar.gz
  url: https://pypi.python.org/packages/source/e/eadred/eadred-0.3.tar.gz
  md5: 63a1caf6beffbe1bcfdace725dc79bbf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - eadred = eadred:main
    #
    # Would create an entry point called eadred that calls eadred.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - eadred
    - eadred.management
    - eadred.management.commands
    - eadred.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/willkg/django-eadred
  license: BSD License
  summary: 'Django app for generating sample data'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
