package:
  name: django-gearman-jbox
  version: "0.1"

source:
  fn: django-gearman-jbox-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-gearman-jbox/django-gearman-jbox-0.1.tar.gz
  md5: c2470bae122e229cbeace10a42cc5b34
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-gearman-jbox = django-gearman-jbox:main
    #
    # Would create an entry point called django-gearman-jbox that calls django-gearman-jbox.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - gearman >=2.0.0
    - prettytable ==0.5

  run:
    - python
    - gearman >=2.0.0
    - prettytable ==0.5

test:
  # Python imports
  imports:
    - django_gearman_jbox
    - django_gearman_jbox.management
    - django_gearman_jbox.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://jbox-web.github.com/django-gearman-jbox/
  license: MPL
  summary: 'A convenience wrapper for Gearman clients and workers in Django/Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
