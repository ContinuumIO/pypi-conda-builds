package:
  name: mopidy-scrobbler
  version: "1.1.1"

source:
  fn: Mopidy-Scrobbler-1.1.1.tar.gz
  url: https://pypi.python.org/packages/source/M/Mopidy-Scrobbler/Mopidy-Scrobbler-1.1.1.tar.gz
  md5: a10dc37ebf8604c33e4f5ef918807c48
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mopidy-scrobbler = mopidy-scrobbler:main
    #
    # Would create an entry point called mopidy-scrobbler that calls mopidy-scrobbler.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - mopidy >=0.18
    - pykka >=1.1
    - pylast >=0.5.7

  run:
    - python
    - setuptools
    - setuptools
    - mopidy >=0.18
    - pykka >=1.1
    - pylast >=0.5.7

test:
  # Python imports
  imports:
    - mopidy_scrobbler

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mopidy/mopidy-scrobbler
  license: Apache Software License
  summary: u'Mopidy extension for scrobbling played tracks to Last.fm'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
