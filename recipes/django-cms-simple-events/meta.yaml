package:
  name: django-cms-simple-events
  version: "0.4.1"

source:
  fn: django-cms-simple-events-0.4.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-cms-simple-events/django-cms-simple-events-0.4.1.tar.gz
  md5: 7979657966e5ad678356ff523d079721
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-cms-simple-events = django-cms-simple-events:main
    #
    # Would create an entry point called django-cms-simple-events that calls django-cms-simple-events.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-hvad

  run:
    - python
    - django-hvad

test:
  # Python imports
  imports:
    - simple_events

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.toporojo.es/
  license: BSD License
  summary: 'A django-cms application for managing an events calendar.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
