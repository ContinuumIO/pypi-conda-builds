package:
  name: djapps
  version: "0.1.1"

source:
  fn: djapps-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/djapps/djapps-0.1.1.tar.gz
  md5: fac135291cc7ead3c71058efc1e7116b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djapps = djapps:main
    #
    # Would create an entry point called djapps that calls djapps.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - djapps

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/RichardOfWard/djapps
  license: BSD
  summary: 'Load your django apps by app name rather than module path.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
