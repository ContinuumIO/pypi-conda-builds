package:
  name: madeiracloudagent
  version: "0.1.51"

source:
  fn: MadeiraCloudAgent-0.1.51.tar.gz
  url: https://pypi.python.org/packages/source/M/MadeiraCloudAgent/MadeiraCloudAgent-0.1.51.tar.gz
  md5: 4761a77eb4cfa38934d7c87113f08ac4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - madeiracloudagent = madeiracloudagent:main
    #
    # Would create an entry point called madeiracloudagent that calls madeiracloudagent.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - MadeiraCloudAgent
    - MadeiraCloudAgent/monitor
    - madeiracloudagent

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: www.madeiracloud.com
  license: GPL
  summary: 'Agent For MadeiraCloud'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
