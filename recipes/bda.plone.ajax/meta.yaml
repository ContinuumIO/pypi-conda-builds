package:
  name: bda.plone.ajax
  version: "1.5"

source:
  fn: bda.plone.ajax-1.5.tar.gz
  url: https://pypi.python.org/packages/source/b/bda.plone.ajax/bda.plone.ajax-1.5.tar.gz
  md5: dea9b0656206dd729b4f7f4be440d6dc
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bda.plone.ajax = bda.plone.ajax:main
    #
    # Would create an entry point called bda.plone.ajax that calls bda.plone.ajax.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone
    - plone.app.jquerytools
    - bdajax >=1.4

  run:
    - python
    - setuptools
    - setuptools
    - plone
    - plone.app.jquerytools
    - bdajax >=1.4

test:
  # Python imports
  imports:
    - bda
    - bda.plone
    - bda.plone.ajax
    - bda.plone.ajax.examples

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: GNU General Public License (GPL)
  summary: '``bdajax`` integration for Plone.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
