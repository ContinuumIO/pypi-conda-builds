package:
  name: antz
  version: "0.0.1"

source:
  fn: antz-0.0.1.zip
  url: https://pypi.python.org/packages/source/a/antz/antz-0.0.1.zip
  md5: 97802d1fe0b66878539585b262fd81e6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - antz = antz:main
    #
    # Would create an entry point called antz that calls antz.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - numpy >=1.8.0
    - matplotlib >=1.3.1

  run:
    - python
    - numpy >=1.8.0
    - matplotlib >=1.3.1

test:
  # Python imports
  imports:
    - antz

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/antz
  license: LICENSE.txt
  summary: 'antz is a bioinformatics package written for python.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
