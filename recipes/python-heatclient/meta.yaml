package:
  name: python-heatclient
  version: "0.6.0"

source:
  fn: python-heatclient-0.6.0.tar.gz
  url: https://pypi.python.org/packages/source/p/python-heatclient/python-heatclient-0.6.0.tar.gz
  md5: 071e69322c6a205a088cc23c3b5c4872
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-heatclient = python-heatclient:main
    #
    # Would create an entry point called python-heatclient that calls python-heatclient.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Apache Software License
  summary: 'OpenStack Orchestration API Client Library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
