package:
  name: aiohttp
  version: "0.15.3"

source:
  fn: aiohttp-0.15.3.tar.gz
  url: https://pypi.python.org/packages/source/a/aiohttp/aiohttp-0.15.3.tar.gz
  md5: 08c1159a3b4d869c70be3b8f7efda7d0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - aiohttp = aiohttp:main
    #
    # Would create an entry point called aiohttp that calls aiohttp.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - chardet
    - asyncio

  run:
    - python
    - chardet
    - asyncio

test:
  # Python imports
  imports:
    - aiohttp

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/KeepSafe/aiohttp/
  license: Apache Software License
  summary: 'http client/server for asyncio'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
