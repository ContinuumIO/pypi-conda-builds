package:
  name: datanommer
  version: "0.3.0"

source:
  fn: datanommer-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/d/datanommer/datanommer-0.3.0.tar.gz
  md5: 31b37716a22a3e5f6dd9e33546b0f6dc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - datanommer = datanommer:main
    #
    # Would create an entry point called datanommer that calls datanommer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - datanommer.consumer
    - datanommer.models
    - datanommer.commands

  run:
    - python
    - datanommer.consumer
    - datanommer.models
    - datanommer.commands

test:
  # Python imports
  imports:
    - datanommer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/ralphbean/datanommer
  license: GPLv3+
  summary: 'A storage consumer for the Fedora Message Bus (fedmsg)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
