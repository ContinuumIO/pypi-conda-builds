package:
  name: cmsplugin-pagedown
  version: "0.0.2"

source:
  fn: cmsplugin-pagedown-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/c/cmsplugin-pagedown/cmsplugin-pagedown-0.0.2.tar.gz
  md5: 2957a164399ad4a5b950f3ac25863370
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cmsplugin-pagedown = cmsplugin-pagedown:main
    #
    # Would create an entry point called cmsplugin-pagedown that calls cmsplugin-pagedown.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3
    - django-pagedown

  run:
    - python
    - django >=1.3
    - django-pagedown

test:
  # Python imports
  imports:
    - cmsplugin_pagedown
    - cmsplugin_pagedown.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/timmyomahony/cmsplugin-pagedown
  license: LICENSE.txt
  summary: 'A plugin for django-cms that includes the StackOverflow pagedown editor'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
