package:
  name: django_graceful
  version: "0.1"

source:
  fn: django_graceful-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django_graceful/django_graceful-0.1.tar.gz
  md5: 1b5d51e75edc04373978a3a458c0a836
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_graceful = django_graceful:main
    #
    # Would create an entry point called django_graceful that calls django_graceful.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - django_graceful
    - django_graceful.management
    - django_graceful.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/andreiko/django_graceful
  license: UNKNOWN
  summary: 'Django fastcgi deployment tool for easy management and graceful code updating'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
