package:
  name: django-urlmapper
  version: "1.1.0"

source:
  fn: django-urlmapper-1.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-urlmapper/django-urlmapper-1.1.0.tar.gz
  md5: 097d42e3db3d6fe95675457fe85d8202
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-urlmapper = django-urlmapper:main
    #
    # Would create an entry point called django-urlmapper that calls django-urlmapper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - urlmapper

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/prestontimmons/django-urlmapper
  license: MIT License
  summary: 'Print registered urls in a readable format.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
