package:
  name: django-mbtiles
  version: "1.3"

source:
  fn: django-mbtiles-1.3.zip
  url: https://pypi.python.org/packages/source/d/django-mbtiles/django-mbtiles-1.3.zip
  md5: 3e02b905c91f28ec110e45dcc9435194
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-mbtiles = django-mbtiles:main
    #
    # Would create an entry point called django-mbtiles that calls django-mbtiles.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4
    - easydict >=1.3
    - landez >=2.1.1

  run:
    - python
    - django >=1.4
    - easydict >=1.3
    - landez >=2.1.1

test:
  # Python imports
  imports:
    - mbtilesmap
    - mbtilesmap.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/makinacorpus/django-mbtiles
  license: UNKNOWN
  summary: 'Serve maps from MBTiles files using Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
