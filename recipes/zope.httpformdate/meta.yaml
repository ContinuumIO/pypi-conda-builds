package:
  name: zope.httpformdate
  version: "1.0.1"

source:
  fn: zope.httpformdate-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/z/zope.httpformdate/zope.httpformdate-1.0.1.tar.gz
  md5: bb742324d8237a7b9415420507d54352
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zope.httpformdate = zope.httpformdate:main
    #
    # Would create an entry point called zope.httpformdate that calls zope.httpformdate.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.httpform
    - zope.datetime

  run:
    - python
    - setuptools
    - zope.httpform
    - zope.datetime

test:
  # Python imports
  imports:
    - zope
    - zope.httpformdate

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/zope.httpformdate
  license: ZPL 2.1
  summary: 'Extension of zope.httpform for date support'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
