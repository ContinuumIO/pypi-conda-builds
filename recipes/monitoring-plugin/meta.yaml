package:
  name: monitoring-plugin
  version: "1.0.6"

source:
  fn: monitoring-plugin-1.0.6.tar.gz
  url: https://pypi.python.org/packages/source/m/monitoring-plugin/monitoring-plugin-1.0.6.tar.gz
  md5: 35dfded3cc44211b723d7fc820302453
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - monitoring-plugin = monitoring-plugin:main
    #
    # Would create an entry point called monitoring-plugin that calls monitoring-plugin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'OpenStack Monitoring Plugin for Horizon'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
