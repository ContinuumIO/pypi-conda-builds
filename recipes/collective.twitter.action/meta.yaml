package:
  name: collective.twitter.action
  version: "1.0.1"

source:
  fn: collective.twitter.action-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.twitter.action/collective.twitter.action-1.0.1.tar.gz
  md5: 91d7aa7e0ba0e757b4bd893156d67f04
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.twitter.action = collective.twitter.action:main
    #
    # Would create an entry point called collective.twitter.action that calls collective.twitter.action.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.twitter.accounts

  run:
    - python
    - setuptools
    - setuptools
    - collective.twitter.accounts

test:
  # Python imports
  imports:
    - collective
    - collective.twitter
    - collective.twitter.action

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/collective.twitter.action
  license: GPL
  summary: 'Content rules action to post on Twitter'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
