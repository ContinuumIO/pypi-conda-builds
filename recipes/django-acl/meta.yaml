package:
  name: django-acl
  version: "0.1.2"

source:
  fn: django-acl-0.1.2.zip
  url: https://pypi.python.org/packages/source/d/django-acl/django-acl-0.1.2.zip
  md5: 207d0553b3b1e6704981d694255e9af5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-acl = django-acl:main
    #
    # Would create an entry point called django-acl that calls django-acl.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django ==1.6

  run:
    - python
    - django ==1.6

test:
  # Python imports
  imports:
    - djangoacl
    - djangoacl.auth
    - djangoacl.migrations
    - djangoacl.south_migrations
    - tests
    - tests.backend

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dwatkinsweb/django-acl
  license: MIT License
  summary: 'ACL (Action Control List) permission handling.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
