package:
  name: sphinx_drove_theme
  version: "1.11.0"

source:
  fn: sphinx_drove_theme-1.11.0.tar.gz
  url: https://pypi.python.org/packages/source/s/sphinx_drove_theme/sphinx_drove_theme-1.11.0.tar.gz
  md5: c58e4c9190e16b0a872b28b0b66ede1f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sphinx_drove_theme = sphinx_drove_theme:main
    #
    # Would create an entry point called sphinx_drove_theme that calls sphinx_drove_theme.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sphinx >=1.1

  run:
    - python
    - sphinx >=1.1

test:
  # Python imports
  imports:
    - sphinx_drove_theme

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/droveio/sphinx-drove-theme
  license: BSD License
  summary: 'drove.io theme for Sphinx, 2014 version.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
