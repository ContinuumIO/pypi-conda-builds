package:
  name: django-telemetry
  version: "0.1.1"

source:
  fn: django-telemetry-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-telemetry/django-telemetry-0.1.1.tar.gz
  md5: c31754f039d7b23a85f7aa7c8e97b512
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-telemetry = django-telemetry:main
    #
    # Would create an entry point called django-telemetry that calls django-telemetry.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django >=1.5
    - pytz >=2013.9
    - django-celery >=3.1
    - south >=0.8

  run:
    - python
    - django >=1.5
    - pytz >=2013.9
    - django-celery >=3.1
    - south >=0.8

test:
  # Python imports
  imports:
    - telemetry
    - telemetry.events
    - telemetry.events.migrations
    - telemetry.metrics
    - telemetry.reporting
    - telemetry.requests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/alexlovelltroy/django-telemetry
  license: LICENSE.txt
  summary: 'Event-based reporting system for django - with queryset graphs'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
