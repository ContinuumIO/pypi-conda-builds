package:
  name: django-ticker
  version: "0.2.1"

source:
  fn: django-ticker-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-ticker/django-ticker-0.2.1.tar.gz
  md5: 244b737b8b04f0ee22796561edf77e55
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-ticker = django-ticker:main
    #
    # Would create an entry point called django-ticker that calls django-ticker.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - ticker

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/jezdez/django-ticker/tree/master
  license: BSD License
  summary: 'A simple Django app for newsticker with a row level permission based workflow.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
