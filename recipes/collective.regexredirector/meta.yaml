package:
  name: collective.regexredirector
  version: "0.2.1"

source:
  fn: collective.regexredirector-0.2.1.zip
  url: https://pypi.python.org/packages/source/c/collective.regexredirector/collective.regexredirector-0.2.1.zip
  md5: 697d6d3aa6ebb68178e26d7d29c52bd4
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.regexredirector = collective.regexredirector:main
    #
    # Would create an entry point called collective.regexredirector that calls collective.regexredirector.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.testing

  run:
    - python
    - setuptools
    - setuptools
    - plone.testing

test:
  # Python imports
  imports:
    - collective
    - collective.regexredirector
    - collective.regexredirector.browser
    - collective.regexredirector.tests
    - collective.regexredirector.upgrades

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.regexredirector
  license: GNU General Public License (GPL)
  summary: 'Addon for plone.app.redirector concerning regex redirector'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
