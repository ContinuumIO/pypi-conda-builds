package:
  name: plone.reload
  version: "2.0"

source:
  fn: plone.reload-2.0.zip
  url: https://pypi.python.org/packages/source/p/plone.reload/plone.reload-2.0.zip
  md5: 49eab593c81b78a9b80d54786aa4ad72
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.reload = plone.reload:main
    #
    # Would create an entry point called plone.reload that calls plone.reload.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zope.component
    - zope.interface
    - zope.processlifetime
    - zope.publisher
    - zope.site
    - zope.testing
    - zope2 >=2.12

  run:
    - python
    - setuptools
    - setuptools
    - zope.component
    - zope.interface
    - zope.processlifetime
    - zope.publisher
    - zope.site
    - zope.testing
    - zope2 >=2.12

test:
  # Python imports
  imports:
    - plone
    - plone.reload

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/plone.reload
  license: BSD License
  summary: 'Configuration and code reload without server restarts.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
