package:
  name: flip
  version: "1.2"

source:
  fn: FLiP-1.2.tar.gz
  url: https://pypi.python.org/packages/source/F/FLiP/FLiP-1.2.tar.gz
  md5: ae6012d4f79a670e05deba60e4107053
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flip = flip:main
    #
    # Would create an entry point called flip that calls flip.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - flip
    - flip.grail
    - flip.logic
    - flip.poset
    - flip.poset.test
    - flip.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://staff.washington.edu/jon/flip/www/
  license: GNU General Public License (GPL)
  summary: 'F L i P : Logical Framework in Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
