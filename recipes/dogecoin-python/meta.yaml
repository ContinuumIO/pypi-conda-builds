package:
  name: dogecoin-python
  version: "0.1.3"

source:
  fn: dogecoin-python-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/d/dogecoin-python/dogecoin-python-0.1.3.tar.gz
  md5: dffb1ad8d40cd9e5d9a712b2c5a1c110
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dogecoin-python = dogecoin-python:main
    #
    # Would create an entry point called dogecoin-python that calls dogecoin-python.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - dogecoinrpc

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jcsaaddupuy/dogecoin-python
  license: MIT License
  summary: 'Friendly Dogecoin JSON-RPC API binding for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
