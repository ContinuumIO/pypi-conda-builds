package:
  name: colorful
  version: "0.01.02"

source:
  fn: colorful-0.01.02.tar.gz
  url: https://pypi.python.org/packages/source/c/colorful/colorful-0.01.02.tar.gz
  md5: fa0e26494bd618687dc5ca468c3c454d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - colorful = colorful:main
    #
    # Would create an entry point called colorful that calls colorful.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - colorful

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/timofurrer/colorful
  license: UNKNOWN
  summary: 'python module to decorate text with ANSI colors and modifiers'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
