package:
  name: django-contactme
  version: "1.1"

source:
  fn: django-contactme-1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-contactme/django-contactme-1.1.tar.gz
  md5: 386739d29fb1a3ca202c01cb9fce8517
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-contactme = django-contactme:main
    #
    # Would create an entry point called django-contactme that calls django-contactme.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_contactme
    - django_contactme.demo
    - django_contactme.templatetags
    - django_contactme.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/danirus/django-contactme/
  license: MIT License
  summary: 'Django pluggable contact form app with email verification.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
