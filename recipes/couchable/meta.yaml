package:
  name: couchable
  version: "0.4.1"

source:
  fn: couchable-0.4.1.tar.gz
  url: https://pypi.python.org/packages/source/c/couchable/couchable-0.4.1.tar.gz
  md5: f208878e9414212989c745294b9cc82b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - couchable = couchable:main
    #
    # Would create an entry point called couchable that calls couchable.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - couchdb >=0.8

  run:
    - python
    - couchdb >=0.8

test:
  # Python imports
  imports:
    - couchable

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/wickedgrey/couchable
  license: MIT License
  summary: 'Allows arbitrary python objects to be stored in CouchDB, while keeping the resulting CouchDB document as "natural" as possible.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
