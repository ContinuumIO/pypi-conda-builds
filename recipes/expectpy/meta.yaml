package:
  name: expectpy
  version: "0.0.4"

source:
  fn: expectpy-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/e/expectpy/expectpy-0.0.4.tar.gz
  md5: a3f72845068815a0c818d6fd70ea38a0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - expectpy = expectpy:main
    #
    # Would create an entry point called expectpy that calls expectpy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - expectpy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/float1251/expectpy
  license: MIT License
  summary: 'assertion library like chai.expect.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
