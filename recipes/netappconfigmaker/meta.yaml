package:
  name: netappconfigmaker
  version: "1.5.5"

source:
  fn: netappConfigMaker-1.5.5.tar.gz
  url: https://pypi.python.org/packages/source/n/netappConfigMaker/netappConfigMaker-1.5.5.tar.gz
  md5: d3cef6fdd417fdaac7dcc97a6e181ad8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - netappconfigmaker = netappconfigmaker:main
    #
    # Would create an entry point called netappconfigmaker that calls netappconfigmaker.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - argparse >=1.2.1
    - lxml >=2.3.5
    - cheetah >=2.4.4

  run:
    - python
    - argparse >=1.2.1
    - lxml >=2.3.5
    - cheetah >=2.4.4

test:
  # Python imports
  imports:
    - netappconfigmaker

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/netappConfigMaker/
  license: GNU General Public License v3 (GPLv3)
  summary: 'Netapp 7Mode Configuration generator based on a XML configuration file.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
