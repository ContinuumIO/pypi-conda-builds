package:
  name: fess_up
  version: "1.1.1"

source:
  fn: fess_up-1.1.1.tar.gz
  url: https://pypi.python.org/packages/source/f/fess_up/fess_up-1.1.1.tar.gz
  md5: 6a4a75088d01fc99ff142a3b1477d08a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fess_up = fess_up:main
    #
    # Would create an entry point called fess_up that calls fess_up.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - fess_up
    - fess_up.dnsnames

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/nosmo/fess_up
  license: Hacktivismo Enhanced-Source Software License Agreement
  summary: 'DNS record scraper'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
