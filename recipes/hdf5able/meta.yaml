package:
  name: hdf5able
  version: "0.3.0"

source:
  fn: hdf5able-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/h/hdf5able/hdf5able-0.3.0.tar.gz
  md5: a36a934ecfee56dff1121d0dea9e1303
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hdf5able = hdf5able:main
    #
    # Would create an entry point called hdf5able that calls hdf5able.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy >=1.8.2
    - h5py >=2.3.0
    - pathlib >=1.0
    - mock >=1.0.1

  run:
    - python
    - numpy >=1.8.2
    - h5py >=2.3.0
    - pathlib >=1.0
    - mock >=1.0.1

test:
  # Python imports
  imports:
    - hdf5able
    - hdf5able.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'Efficient serialisation interface from ndarray-focused objects to HDF5'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
