package:
  name: colorscheme
  version: "0.2"

source:
  fn: colorscheme-0.2.tar.gz
  url: https://pypi.python.org/packages/source/c/colorscheme/colorscheme-0.2.tar.gz
  md5: 3471b3a95052b7c81a74f008a0ceac67
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - colorscheme = colorscheme:main
    #
    # Would create an entry point called colorscheme that calls colorscheme.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - colorscheme

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/gardaud/colorscheme
  license: MIT License
  summary: 'A basic terminal application that displays the active color palette'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
