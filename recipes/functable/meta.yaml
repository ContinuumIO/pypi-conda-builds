package:
  name: functable
  version: "0.2.dev1"

source:
  fn: functable-0.2.dev1.tar.gz
  url: https://pypi.python.org/packages/source/f/functable/functable-0.2.dev1.tar.gz
  md5: d06a8b14dfeaf3c9f11bb40f0102b603
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - functable = functable:main
    #
    # Would create an entry point called functable that calls functable.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - proptools >=0.1

  run:
    - python
    - proptools >=0.1

test:
  # Python imports
  imports:
    - functable

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.org/nejucomo/functable
  license: GPLv3
  summary: 'A small utility for grouping functions or methods into a table.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
