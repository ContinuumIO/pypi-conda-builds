package:
  name: django-cached-user
  version: "1.0"

source:
  fn: django-cached-user-1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-cached-user/django-cached-user-1.0.tar.gz
  md5: 3e43e244ab7da766075fe30103a00ed5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-cached-user = django-cached-user:main
    #
    # Would create an entry point called django-cached-user that calls django-cached-user.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - cached_user

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/bitzeppelin/django-cached-user
  license: BSD License
  summary: 'Cached user retrieval for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
