package:
  name: girder-client
  version: "0.1.0"

source:
  fn: girder-client-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/g/girder-client/girder-client-0.1.0.tar.gz
  md5: def52d4959234bcf903012efbc1aad8a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - girder-client = girder-client:main
    #
    # Would create an entry point called girder-client that calls girder-client.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - girder_client

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://girder.readthedocs.org/en/latest/python-client.html
  license: Apache Software License
  summary: 'Python client for interacting with Girder servers'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
