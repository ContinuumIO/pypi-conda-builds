package:
  name: flask-heroku-cacheify
  version: "1.4"

source:
  fn: Flask-Heroku-Cacheify-1.4.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Heroku-Cacheify/Flask-Heroku-Cacheify-1.4.tar.gz
  md5: cd6a830adf7f98a280644c1180f97aee
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-heroku-cacheify = flask-heroku-cacheify:main
    #
    # Would create an entry point called flask-heroku-cacheify that calls flask-heroku-cacheify.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask-cache >=0.11.1
    - pylibmc >=1.2.3
    - redis >=2.7.2

  run:
    - python
    - flask-cache >=0.11.1
    - pylibmc >=1.2.3
    - redis >=2.7.2

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rdegges/flask-heroku-cacheify
  license: UNLICENSE
  summary: 'Automatic Flask cache configuration on Heroku.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
