package:
  name: m2x-mqtt
  version: "0.0.2"

source:
  fn: m2x-mqtt-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/m/m2x-mqtt/m2x-mqtt-0.0.2.tar.gz
  md5: 00131ef08212bbb8365a24ca9262cadc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - m2x-mqtt = m2x-mqtt:main
    #
    # Would create an entry point called m2x-mqtt that calls m2x-mqtt.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - iso8601 ==0.1.8
    - paho-mqtt ==1.0

  run:
    - python
    - iso8601 ==0.1.8
    - paho-mqtt ==1.0

test:
  # Python imports
  imports:
    - m2x_mqtt
    - m2x_mqtt.v2

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/attm2x/m2x-python-mqtt
  license: BSD License
  summary: 'M2X Python API client'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
