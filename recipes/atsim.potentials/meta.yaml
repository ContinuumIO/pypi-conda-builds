package:
  name: atsim.potentials
  version: "0.1.1"

source:
  fn: atsim.potentials-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/a/atsim.potentials/atsim.potentials-0.1.1.tar.gz
  md5: aebe9d97e074a644dfa100776aea95c2
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - atsim.potentials = atsim.potentials:main
    #
    # Would create an entry point called atsim.potentials that calls atsim.potentials.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - atsim
    - atsim.potentials

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/mjdr/atsim_potentials
  license: Apache Software License
  summary: 'atsim.potentials provides tools for working with pair and embedded atom method potential models including tabulation routines for DL_POLY and LAMMPS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
