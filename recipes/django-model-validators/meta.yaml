package:
  name: django-model-validators
  version: "0.1a2"

source:
  fn: django-model-validators-0.1a2.zip
  url: https://pypi.python.org/packages/source/d/django-model-validators/django-model-validators-0.1a2.zip
  md5: 84c82129cd8d33450e566a29b1ed7e24
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-model-validators = django-model-validators:main
    #
    # Would create an entry point called django-model-validators that calls django-model-validators.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - model_validators

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/nextscreenlabs/django-model-validators
  license: BSD License
  summary: 'Validators for Django models.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
