package:
  name: django-tube
  version: "0.2"

source:
  fn: django-tube-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-tube/django-tube-0.2.tar.gz
  md5: 822e6f78fc76b70de5a498925a9c362a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-tube = django-tube:main
    #
    # Would create an entry point called django-tube that calls django-tube.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - tube
    - tube.migrations
    - tube.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/semente/django-tube/
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'YouTube video management application for Django projects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
