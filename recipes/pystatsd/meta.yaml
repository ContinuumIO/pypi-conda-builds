package:
  name: pystatsd
  version: "0.1.10"

source:
  fn: pystatsd-0.1.10.tar.gz
  url: https://pypi.python.org/packages/source/p/pystatsd/pystatsd-0.1.10.tar.gz
  md5: fbc4b966fdb045878e87cf615a355427
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pystatsd = pystatsd:main
    #
    # Would create an entry point called pystatsd that calls pystatsd.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - argparse >=1.2

  run:
    - python
    - argparse >=1.2

test:
  # Python imports
  imports:
    - pystatsd

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/sivy/py-statsd
  license: BSD License
  summary: "pystatsd is a client for Etsy's statsd server, a front end/proxy for the Graphite stats collection and graphing server."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
