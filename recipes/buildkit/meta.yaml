package:
  name: buildkit
  version: "0.2.2"

source:
  fn: buildkit-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/b/buildkit/buildkit-0.2.2.tar.gz
  md5: e048f06414470101bf4a433398144255
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - buildkit = buildkit:main
    #
    # Would create an entry point called buildkit that calls buildkit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - buildkit
    - buildkit.command
    - buildkit.command.build
    - buildkit.command.pkg
    - buildkit.command.repo
    - buildkit.command.vm
    - buildkit.facility
    - buildkit.helper

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://packages.python.org/buildkit
  license: GNU Affero General Public License v3
  summary: 'Cloud infrastructure and .deb file management software'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
