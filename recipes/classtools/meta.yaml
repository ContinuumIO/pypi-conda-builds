package:
  name: classtools
  version: "0.1"

source:
  fn: classtools-0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/classtools/classtools-0.1.tar.gz
  md5: b01c9362cc718b822b41efb6565abb7a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - classtools = classtools:main
    #
    # Would create an entry point called classtools that calls classtools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - classtools

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/eevee/classtools
  license: ISC License (ISCL)
  summary: 'A few class utilities the stdlib is missing.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
