package:
  name: collective.imageinbox
  version: "1.0.1"

source:
  fn: collective.imageinbox-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.imageinbox/collective.imageinbox-1.0.1.tar.gz
  md5: d219c8bfe8d1ad74f4d5e2dea08d2fbb
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.imageinbox = collective.imageinbox:main
    #
    # Would create an entry point called collective.imageinbox that calls collective.imageinbox.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.imageinbox
    - collective.imageinbox.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/martinschoel/collective.imageinbox
  license: GPL
  summary: 'Displays scaled plone content images inside Thickbox.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
