package:
  name: products.compoundfield
  version: "1.1"

source:
  fn: Products.CompoundField-1.1.tar.gz
  url: https://pypi.python.org/packages/source/P/Products.CompoundField/Products.CompoundField-1.1.tar.gz
  md5: 9c691b72401828cb9f2b93db3d2d1d61
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - products.compoundfield = products.compoundfield:main
    #
    # Would create an entry point called products.compoundfield that calls products.compoundfield.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.archetypes

  run:
    - python
    - setuptools
    - setuptools
    - products.archetypes

test:
  # Python imports
  imports:
    - Products
    - Products.CompoundField
    - Products.CompoundField.doc
    - Products.CompoundField.testClasses
    - Products.CompoundField.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/compoundfield
  license: D-FSL (German Free Software Licence)
  summary: u'Compound- and Array-Field and -Widget for Archetypes'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
