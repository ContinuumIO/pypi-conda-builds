package:
  name: mozsystemmonitor
  version: "0.0"

source:
  fn: mozsystemmonitor-0.0.tar.gz
  url: https://pypi.python.org/packages/source/m/mozsystemmonitor/mozsystemmonitor-0.0.tar.gz
  md5: d8b6b88ca3f1b169ee7b0166f96879b4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mozsystemmonitor = mozsystemmonitor:main
    #
    # Would create an entry point called mozsystemmonitor that calls mozsystemmonitor.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - psutil >=0.7.1

  run:
    - python
    - psutil >=0.7.1

test:
  # Python imports
  imports:
    - mozsystemmonitor

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://wiki.mozilla.org/Auto-tools/Projects/Mozbase
  license: MPL 2.0
  summary: 'Monitor system resource usage.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
