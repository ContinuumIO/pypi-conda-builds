package:
  name: horae.dashboard
  version: "1.0a1"

source:
  fn: horae.dashboard-1.0a1.tar.gz
  url: https://pypi.python.org/packages/source/h/horae.dashboard/horae.dashboard-1.0a1.tar.gz
  md5: 90b78ea7e22b5e87fd4c5096e903cff2
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - horae.dashboard = horae.dashboard:main
    #
    # Would create an entry point called horae.dashboard that calls horae.dashboard.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - grok
    - fanstatic
    - zope.fanstatic
    - horae.auth
    - horae.autocomplete
    - horae.core
    - horae.datetime
    - horae.layout
    - horae.lifecycle
    - horae.properties
    - horae.ticketing
    - horae.timeaware

  run:
    - python
    - setuptools
    - setuptools
    - grok
    - fanstatic
    - zope.fanstatic
    - horae.auth
    - horae.autocomplete
    - horae.core
    - horae.datetime
    - horae.layout
    - horae.lifecycle
    - horae.properties
    - horae.ticketing
    - horae.timeaware

test:
  # Python imports
  imports:
    - horae
    - horae.dashboard

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: GPL
  summary: 'Provides a dashboard for Horae'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
