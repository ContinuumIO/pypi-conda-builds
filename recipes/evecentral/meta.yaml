package:
  name: evecentral
  version: "0.0.1"

source:
  fn: evecentral-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/e/evecentral/evecentral-0.0.1.tar.gz
  md5: 80508cb6b81e49e259b32837c5159eb2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - evecentral = evecentral:main
    #
    # Would create an entry point called evecentral that calls evecentral.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - evecentral

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ZymoticB/python-evecentral
  license: BSD License
  summary: 'A small module to cache eve central prices in memory'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
