package:
  name: flask-debugtoolbar-lineprofilerpanel
  version: "1.0.2"

source:
  fn: Flask-DebugToolbar-LineProfilerPanel-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-DebugToolbar-LineProfilerPanel/Flask-DebugToolbar-LineProfilerPanel-1.0.2.tar.gz
  md5: 005d47cb98cff464ce9042b9bf66c230
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-debugtoolbar-lineprofilerpanel = flask-debugtoolbar-lineprofilerpanel:main
    #
    # Would create an entry point called flask-debugtoolbar-lineprofilerpanel that calls flask-debugtoolbar-lineprofilerpanel.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask-debugtoolbar
    - line-profiler >=1.0b3

  run:
    - python
    - flask-debugtoolbar
    - line-profiler >=1.0b3

test:
  # Python imports
  imports:
    - flask_debugtoolbar_lineprofilerpanel

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/phleet/flask_debugtoolbar_lineprofilerpanel
  license: MIT
  summary: 'Panel for the Flask Debug toolbar to capture and view line-by-line profiling stats'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
