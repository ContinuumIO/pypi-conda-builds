package:
  name: dataone.python
  version: "1.1"

source:
  fn: dataone.python-1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/dataone.python/dataone.python-1.1.tar.gz
  md5: 82a1534a8c683c2e35f6fff2820d2c9f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dataone.python = dataone.python:main
    #
    # Would create an entry point called dataone.python that calls dataone.python.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - dataone.certificate_extensions
    - dataone.cli
    - dataone.common
    - dataone.gazetteer
    - dataone.generic_member_node
    - dataone.libclient
    - dataone.onedrive
    - dataone.test_utilities
    - dataone.workspace_client
    - google.foresite-toolkit

  run:
    - python
    - dataone.certificate_extensions
    - dataone.cli
    - dataone.common
    - dataone.gazetteer
    - dataone.generic_member_node
    - dataone.libclient
    - dataone.onedrive
    - dataone.test_utilities
    - dataone.workspace_client
    - google.foresite-toolkit

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://dataone.org
  license: Apache License, Version 2.0
  summary: 'Metapackage and documentation root for all DataONE Python products.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
