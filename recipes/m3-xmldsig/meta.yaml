package:
  name: m3-xmldsig
  version: "1.0.0"

source:
  fn: m3-xmldsig-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/m/m3-xmldsig/m3-xmldsig-1.0.0.tar.gz
  md5: 308b2a3f01ec069c1ec4c97350edc132
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - m3-xmldsig = m3-xmldsig:main
    #
    # Would create an entry point called m3-xmldsig that calls m3-xmldsig.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - xmldsig

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/barsgroup/xmldsig
  license: MIT License
  summary: u'Sign and verify RSA-SHA1 XML Digital Signatures'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
