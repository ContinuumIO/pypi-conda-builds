package:
  name: freshen
  version: "0.2"

source:
  fn: freshen-0.2.tar.gz
  url: https://pypi.python.org/packages/source/f/freshen/freshen-0.2.tar.gz
  md5: 9e4cb6b552ce20d94ec77ce1dffeeca3
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - freshen = freshen:main
    #
    # Would create an entry point called freshen that calls freshen.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyparsing >=1.5.0
    - pyyaml
    - nose >=0.11.1

  run:
    - python
    - setuptools
    - pyparsing >=1.5.0
    - pyyaml
    - nose >=0.11.1

test:
  # Python imports
  imports:
    - freshen

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/rlisagor/freshen
  license: GNU General Public License (GPL)
  summary: 'Clone of the Cucumber BDD framework for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
