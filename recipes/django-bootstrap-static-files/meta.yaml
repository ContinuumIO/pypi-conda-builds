package:
  name: django-bootstrap-static-files
  version: "3.0.0"

source:
  fn: django-bootstrap-static-files-3.0.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-bootstrap-static-files/django-bootstrap-static-files-3.0.0.tar.gz
  md5: b395c08ef568acc8b61b5c32b15637c8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-bootstrap-static-files = django-bootstrap-static-files:main
    #
    # Would create an entry point called django-bootstrap-static-files that calls django-bootstrap-static-files.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - bootstrap

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ashchristopher/django-bootstrap-static-files
  license: Apache License 2.0
  summary: 'Twitter bootstrap static files for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
