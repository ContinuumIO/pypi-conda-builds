package:
  name: django-fagungis
  version: "0.1"

source:
  fn: django-fagungis-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-fagungis/django-fagungis-0.1.tar.gz
  md5: 9c63181a922b0789416b5ed276412e43
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-fagungis = django-fagungis:main
    #
    # Would create an entry point called django-fagungis that calls django-fagungis.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fabric >=1.3

  run:
    - python
    - fabric >=1.3

test:
  # Python imports
  imports:
    - fagungis

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/DNX/django-fagungis/wiki/Home
  license: BSD License
  summary: 'DJANGO + FAbric + GUnicorn + NGInx + Supervisor deployment'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
