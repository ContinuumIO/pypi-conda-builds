package:
  name: pysubnettree
  version: "0.23"

source:
  fn: pysubnettree-0.23.tar.gz
  url: https://pypi.python.org/packages/source/p/pysubnettree/pysubnettree-0.23.tar.gz
  md5: e987ff9b6700a65423827450eb1ca128
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pysubnettree = pysubnettree:main
    #
    # Would create an entry point called pysubnettree that calls pysubnettree.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - pysubnettree

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.bro.org/sphinx/components/pysubnettree/README.html
  license: BSD
  summary: '\nThe PySubnetTree package provides a Python data structure SubnetTree\nthat maps subnets given in CIDR notation (incl. corresponding IPv6\nversions) to Python objects. Lookups are performed by longest-prefix\nmatching.\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
