package:
  name: beproud.django.commons
  version: "0.3"

source:
  fn: beproud.django.commons-0.3.tar.gz
  url: https://pypi.python.org/packages/source/b/beproud.django.commons/beproud.django.commons-0.3.tar.gz
  md5: 6d4e839b828273e90f8142f63692c401
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - beproud.django.commons = beproud.django.commons:main
    #
    # Would create an entry point called beproud.django.commons that calls beproud.django.commons.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.2
    - zenhan >=0.4

  run:
    - python
    - django >=1.2
    - zenhan >=0.4

test:
  # Python imports
  imports:
    - beproud
    - beproud.django
    - beproud.django.commons
    - beproud.django.commons.forms
    - beproud.django.commons.http
    - beproud.django.commons.management
    - beproud.django.commons.management.commands
    - beproud.django.commons.middleware
    - beproud.django.commons.models
    - beproud.django.commons.shortcuts
    - beproud.django.commons.templatetags
    - beproud.django.commons.test
    - beproud.django.commons.tests
    - beproud.django.commons.tests.forms
    - beproud.django.commons.tests.models
    - beproud.django.commons.tests.models.base
    - beproud.django.commons.tests.models.fields
    - beproud.django.commons.tests.shortcuts
    - beproud.django.commons.tests.shortcuts.shortcuts_app
    - beproud.django.commons.utils
    - beproud.django.commons.views

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.beproud.jp/
  license: BSD License
  summary: 'Common utilities for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
