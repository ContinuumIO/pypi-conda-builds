package:
  name: cache_it
  version: "0.0.1"

source:
  fn: cache_it-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/cache_it/cache_it-0.0.1.tar.gz
  md5: 9c82bb92085b87b61748166a2b9ce44c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cache_it = cache_it:main
    #
    # Would create an entry point called cache_it that calls cache_it.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pylibmc ==1.2.3
    - switchcache ==0.1.1
    - nose ==1.3.3

  run:
    - python
    - pylibmc ==1.2.3
    - switchcache ==0.1.1
    - nose ==1.3.3

test:
  # Python imports
  imports:
    - cache_it

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/shoz/cache_it
  license: MIT License
  summary: 'Decorators for caching'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
