package:
  name: django-intranet
  version: "0.4"

source:
  fn: django-intranet-0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-intranet/django-intranet-0.4.tar.gz
  md5: ba5c02f50e62bfe85fb10a5c8072f7b6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-intranet = django-intranet:main
    #
    # Would create an entry point called django-intranet that calls django-intranet.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-admin-tools
    - django-floppyforms
    - django-bootstrap-forms
    - django-guardian

  run:
    - python
    - django-admin-tools
    - django-floppyforms
    - django-bootstrap-forms
    - django-guardian

test:
  # Python imports
  imports:
    - intranet
    - intranet.migrations
    - intranet.templatetags
    - intranet.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/ionyse/django-intranet
  license: GNU Lesser General Public License v3 (LGPLv3)
  summary: 'Intranet made easy'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
