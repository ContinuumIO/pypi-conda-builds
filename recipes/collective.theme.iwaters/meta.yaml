package:
  name: collective.theme.iwaters
  version: "0.3"

source:
  fn: collective.theme.iwaters-0.3.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.theme.iwaters/collective.theme.iwaters-0.3.tar.gz
  md5: de934361af9d7b430e6750103a007488
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.theme.iwaters = collective.theme.iwaters:main
    #
    # Would create an entry point called collective.theme.iwaters that calls collective.theme.iwaters.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.themingplugins
    - plone.app.theming

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.themingplugins
    - plone.app.theming

test:
  # Python imports
  imports:
    - collective
    - collective.theme
    - collective.theme.iwaters
    - collective.theme.iwaters.browser
    - collective.theme.iwaters.docs

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/collective.theme.iwaters
  license: GPL
  summary: 'Theme derived from IW:LEARN theme'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
