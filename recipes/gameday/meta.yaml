package:
  name: gameday
  version: "0.0.0"

source:
  fn: gameday-0.0.0.tar.gz
  url: https://pypi.python.org/packages/source/g/gameday/gameday-0.0.0.tar.gz
  md5: 94c1fea09a6af3da19a195914576966d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gameday = gameday:main
    #
    # Would create an entry point called gameday that calls gameday.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests
    - sqlalchemy

  run:
    - python
    - requests
    - sqlalchemy

test:
  # Python imports
  imports:
    - gameday
    - gd
    - gd.scripts

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT License
  summary: "Tools for working with MLB's Gameday data"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
