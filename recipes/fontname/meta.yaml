package:
  name: fontname
  version: "0.2.0"

source:
  fn: fontname-0.2.0.zip
  url: https://pypi.python.org/packages/source/f/fontname/fontname-0.2.0.zip
  md5: 232241d94d8e3ca41bfbb373701d0d90
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fontname = fontname:main
    #
    # Would create an entry point called fontname that calls fontname.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - freetype-py

  run:
    - python
    - freetype-py

test:
  # Python imports
  imports:
    - fontname

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Asvel/fontname
  license: MIT License
  summary: 'A lib for guessing font name'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
