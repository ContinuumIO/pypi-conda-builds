package:
  name: pysaxs
  version: "2.100.July2013"

source:
  fn: pySAXS-2.100.July2013.zip
  url: https://pypi.python.org/packages/source/p/pySAXS/pySAXS-2.100.July2013.zip
  md5: eb495086357509da7ff55c3e48fc4271
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pysaxs = pysaxs:main
    #
    # Would create an entry point called pysaxs that calls pysaxs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - pySAXS
    - pySAXS.LS
    - pySAXS.examples
    - pySAXS.guisaxs
    - pySAXS.guisaxs.qt
    - pySAXS.guisaxs.wx
    - pySAXS.models
    - pySAXS.models.super
    - pySAXS.tools
    - pysaxs

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://iramis.cea.fr/sis2m/lions/
  license: CeCILL
  summary: 'Python for Small Angle X-ray Scattering data acquisition'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
