package:
  name: medialog.hidetitle
  version: "1.0"

source:
  fn: medialog.hidetitle-1.0.zip
  url: https://pypi.python.org/packages/source/m/medialog.hidetitle/medialog.hidetitle-1.0.zip
  md5: 9a8fb8d32e015fbbd0093fff24eab1ff
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - medialog.hidetitle = medialog.hidetitle:main
    #
    # Would create an entry point called medialog.hidetitle that calls medialog.hidetitle.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - archetypes.schemaextender
    - archetypes.markerfield

  run:
    - python
    - setuptools
    - setuptools
    - archetypes.schemaextender
    - archetypes.markerfield

test:
  # Python imports
  imports:
    - medialog
    - medialog.hidetitle
    - medialog.hidetitle.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/medialog.hidetitle
  license: GPL
  summary: 'Makes it possible to mark items so the items title and description does not show.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
