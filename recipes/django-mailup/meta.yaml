package:
  name: django-mailup
  version: "0.1.2"

source:
  fn: django-mailup-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-mailup/django-mailup-0.1.2.tar.gz
  md5: 31afcd231e130aced14f9d21b03ea705
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-mailup = django-mailup:main
    #
    # Would create an entry point called django-mailup that calls django-mailup.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - suds ==0.4

  run:
    - python
    - suds ==0.4

test:
  # Python imports
  imports:
    - mailup

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/marcominutoli/django-mailup.git
  license: BSD License
  summary: 'A plugin to integrate Django and Mailup'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
