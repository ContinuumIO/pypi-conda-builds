package:
  name: flask_locust
  version: "0.0.2-alpha"

source:
  fn: flask_locust-0.0.2-alpha.tar.gz
  url: https://pypi.python.org/packages/source/f/flask_locust/flask_locust-0.0.2-alpha.tar.gz
  md5: 0e031d4f6e209f0a982687b2cf98ffb0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask_locust = flask_locust:main
    #
    # Would create an entry point called flask_locust that calls flask_locust.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask

  run:
    - python
    - flask

test:
  # Python imports
  imports:
    - flask_locust

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: GPLv3
  summary: 'SQL-Migrations for your Application'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
