package:
  name: konfikjure
  version: "0.1.1"

source:
  fn: Konfikjure-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/K/Konfikjure/Konfikjure-0.1.1.tar.gz
  md5: 8c7b97ff58bc24141f32e1190a1cc3a9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - konfikjure = konfikjure:main
    #
    # Would create an entry point called konfikjure that calls konfikjure.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - konfikjure

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: GPLv3
  summary: 'Command-line based configuration utility'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
