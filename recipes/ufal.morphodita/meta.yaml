package:
  name: ufal.morphodita
  version: "1.3.0.1"

source:
  fn: ufal.morphodita-1.3.0.1.tar.gz
  url: https://pypi.python.org/packages/source/u/ufal.morphodita/ufal.morphodita-1.3.0.1.tar.gz
  md5: 743eeb775011d9c78b6520ba16655666
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ufal.morphodita = ufal.morphodita:main
    #
    # Would create an entry point called ufal.morphodita that calls ufal.morphodita.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://ufal.mff.cuni.cz/morphodita
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Bindings to MorphoDiTa library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
