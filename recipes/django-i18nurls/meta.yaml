package:
  name: django-i18nurls
  version: "0.7"

source:
  fn: django-i18nurls-0.7.tar.gz
  url: https://pypi.python.org/packages/source/d/django-i18nurls/django-i18nurls-0.7.tar.gz
  md5: f7fc2711ed4c8e46cf346c0dbd1f8590
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-i18nurls = django-i18nurls:main
    #
    # Would create an entry point called django-i18nurls that calls django-i18nurls.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - i18nurls
    - i18nurls.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/brocaar/django-i18nurls
  license: BSD License
  summary: 'Translate URL patterns and prefix URLs with language-code.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
