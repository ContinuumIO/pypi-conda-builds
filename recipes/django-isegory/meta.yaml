package:
  name: django-isegory
  version: "0.1"

source:
  fn: django-isegory-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-isegory/django-isegory-0.1.tar.gz
  md5: 73c68c21a72d00a1a2f4216bce3f10eb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-isegory = django-isegory:main
    #
    # Would create an entry point called django-isegory that calls django-isegory.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - isegory

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/jdelacueva/django-isegory/
  license: AGPL
  summary: 'A simple Django app to declare the provenance of a dataset.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
