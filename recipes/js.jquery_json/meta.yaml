package:
  name: js.jquery_json
  version: "2.3"

source:
  fn: js.jquery_json-2.3.tar.gz
  url: https://pypi.python.org/packages/source/j/js.jquery_json/js.jquery_json-2.3.tar.gz
  md5: 12c0344df4d75d3afe5f5464673b6f4a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - js.jquery_json = js.jquery_json:main
    #
    # Would create an entry point called js.jquery_json that calls js.jquery_json.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fanstatic
    - js.jquery
    - setuptools

  run:
    - python
    - setuptools
    - fanstatic
    - js.jquery
    - setuptools

test:
  # Python imports
  imports:
    - js
    - js.jquery_json

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/fanstatic/js.jquery_json
  license: BSD
  summary: 'Fanstatic packaging of jquery-json'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
