package:
  name: bracketmaker
  version: "0.2.0"

source:
  fn: BracketMaker-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/B/BracketMaker/BracketMaker-0.2.0.tar.gz
  md5: 838a1e59a9c9119115bdf2d5a6b2b388
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bracketmaker = bracketmaker:main
    #
    # Would create an entry point called bracketmaker that calls bracketmaker.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - bracket
    - bracketmaker

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/BracketMaker/
  license: LICENSE.txt
  summary: 'Create and store readable brackets.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
