package:
  name: hurry.jgrowl
  version: "1.2.4"

source:
  fn: hurry.jgrowl-1.2.4.tar.gz
  url: https://pypi.python.org/packages/source/h/hurry.jgrowl/hurry.jgrowl-1.2.4.tar.gz
  md5: ba369dfeae5af055b04be8499c641c4e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hurry.jgrowl = hurry.jgrowl:main
    #
    # Would create an entry point called hurry.jgrowl that calls hurry.jgrowl.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - hurry.resource >=0.10
    - hurry.jquery

  run:
    - python
    - setuptools
    - setuptools
    - hurry.resource >=0.10
    - hurry.jquery

test:
  # Python imports
  imports:
    - hurry
    - hurry.jgrowl

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
