package:
  name: django-matrix-field
  version: "0.2"

source:
  fn: django-matrix-field-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-matrix-field/django-matrix-field-0.2.tar.gz
  md5: 375a1c2f6433af9842dcc7d98c0f816b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-matrix-field = django-matrix-field:main
    #
    # Would create an entry point called django-matrix-field that calls django-matrix-field.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - matrix_field

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/mfogel/django-matrix-field/
  license: BSD License
  summary: 'A Django app providing database and form fields for matrices (arrays of arrays).'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
