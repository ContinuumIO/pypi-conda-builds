package:
  name: django-moreforms
  version: "1.2"

source:
  fn: django-moreforms-1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-moreforms/django-moreforms-1.2.tar.gz
  md5: 436046101c6cd4b7bd2e6ffdf159563f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-moreforms = django-moreforms:main
    #
    # Would create an entry point called django-moreforms that calls django-moreforms.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - moreforms
    - moreforms.date
    - moreforms.fancy
    - moreforms.fieldtree
    - moreforms.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'Contains helpful fields for Django, such as a FieldTree (a simple way to get a field chain) and date-related fields.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
