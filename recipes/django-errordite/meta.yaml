package:
  name: django-errordite
  version: "0.8"

source:
  fn: django-errordite-0.8.tar.gz
  url: https://pypi.python.org/packages/source/d/django-errordite/django-errordite-0.8.tar.gz
  md5: b6589aa47ebc63e2edd07a2c7471e904
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-errordite = django-errordite:main
    #
    # Would create an entry point called django-errordite that calls django-errordite.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - errordite >=0.4

  run:
    - python
    - errordite >=0.4

test:
  # Python imports
  imports:
    - django_errordite

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/hugorodgerbrown/django-errordite
  license: BSD License
  summary: 'Errordite exception logging for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
