package:
  name: django-nudge
  version: "0.9.1"

source:
  fn: django-nudge-0.9.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-nudge/django-nudge-0.9.1.tar.gz
  md5: c814c132bef4f9e1b2de1aa04a306f67
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-nudge = django-nudge:main
    #
    # Would create an entry point called django-nudge that calls django-nudge.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - django-reversion
    - pycrypto

  run:
    - python
    - django
    - django-reversion
    - pycrypto

test:
  # Python imports
  imports:
    - nudge
    - nudge.demo
    - nudge.management
    - nudge.management.commands
    - nudge.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/CFPB/django-nudge
  license: UNKNOWN
  summary: 'Use Nudge to (gently) push content between Django servers'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
