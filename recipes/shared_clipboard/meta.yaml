package:
  name: shared_clipboard
  version: "0.0.1"

source:
  fn: shared_clipboard-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/s/shared_clipboard/shared_clipboard-0.0.1.tar.gz
  md5: a6e019781594b31db63a64a0d2d9f634
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - shared_clipboard = shared_clipboard:main
    #
    # Would create an entry point called shared_clipboard that calls shared_clipboard.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fom

  run:
    - python
    - fom

test:
  # Python imports
  imports:
    - shared_clipboard

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/fluidinfo/shared-clipboard
  license: Apache Software License
  summary: 'Classes for saving, retrieving and clearing shared clipboards stored in Fluidinfo.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
