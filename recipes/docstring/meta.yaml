package:
  name: docstring
  version: "0.1.2.4"

source:
  fn: docstring-0.1.2.4.tar.gz
  url: https://pypi.python.org/packages/source/d/docstring/docstring-0.1.2.4.tar.gz
  md5: d8c6528e5b7229cb42afa29d90e50176
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - docstring = docstring:main
    #
    # Would create an entry point called docstring that calls docstring.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - docstring

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://chartbeat.com
  license: UNKNOWN
  summary: 'Decorators for auto-generating HTML response for API endpoints'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
