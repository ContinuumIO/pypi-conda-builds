package:
  name: hoka.patches.get_adapter
  version: "2.13.19.3"

source:
  fn: hoka.patches.get_adapter-2.13.19.3.zip
  url: https://pypi.python.org/packages/source/h/hoka.patches.get_adapter/hoka.patches.get_adapter-2.13.19.3.zip
  md5: 575560f202058e60cfa1e02840cb3ca7
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hoka.patches.get_adapter = hoka.patches.get_adapter:main
    #
    # Would create an entry point called hoka.patches.get_adapter that calls hoka.patches.get_adapter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zope2
    - z3c.autoinclude

  run:
    - python
    - setuptools
    - setuptools
    - zope2
    - z3c.autoinclude

test:
  # Python imports
  imports:
    - hoka
    - hoka.patches
    - hoka.patches.get_adapter
    - hoka.patches.get_adapter.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/hoka/hoka.patches.get_adapter
  license: GNU General Public License (GPL)
  summary: 'A patch to query an adapter on every context'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
