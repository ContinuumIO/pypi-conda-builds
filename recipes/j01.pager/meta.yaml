package:
  name: j01.pager
  version: "0.5.0"

source:
  fn: j01.pager-0.5.0.zip
  url: https://pypi.python.org/packages/source/j/j01.pager/j01.pager-0.5.0.zip
  md5: 8e0a1290940a11cccf56210beb314612
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - j01.pager = j01.pager:main
    #
    # Would create an entry point called j01.pager that calls j01.pager.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - j01.jsonrpc

  run:
    - python
    - setuptools
    - j01.jsonrpc

test:
  # Python imports
  imports:
    - j01
    - j01.pager

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/j01.pager
  license: Zope Public License
  summary: 'JQuery and j01.jsonrpc based pagination for Zope 3'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
