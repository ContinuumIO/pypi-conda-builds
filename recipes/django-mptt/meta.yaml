package:
  name: django-mptt
  version: "0.7.4"

source:
  fn: django-mptt-0.7.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-mptt/django-mptt-0.7.4.tar.gz
  md5: 7b74c105dde5ff7ca285b761e7487f99
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-mptt = django-mptt:main
    #
    # Would create an entry point called django-mptt that calls django-mptt.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4.2

  run:
    - python
    - django >=1.4.2

test:
  # Python imports
  imports:
    - mptt
    - mptt.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

# requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/django-mptt/django-mptt
  license: MIT License
  summary: 'Utilities for implementing Modified Preorder Tree Traversal\n        with your Django Models and working with trees of Model instances.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
