package:
  name: nestinglisting.py
  version: "1.2.0"

source:
  fn: nestinglisting.py-1.2.0.tar.gz
  url: https://pypi.python.org/packages/source/n/nestinglisting.py/nestinglisting.py-1.2.0.tar.gz
  md5: ae846e5ca345c6a885c5f7c53c41a031
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - nestinglisting.py = nestinglisting.py:main
    #
    # Would create an entry point called nestinglisting.py that calls nestinglisting.py.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'A simple List Nester With Tab Stops On User Choice.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
