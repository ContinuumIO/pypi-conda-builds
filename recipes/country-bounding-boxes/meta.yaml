package:
  name: country-bounding-boxes
  version: "0.2.2"

source:
  fn: country-bounding-boxes-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/c/country-bounding-boxes/country-bounding-boxes-0.2.2.tar.gz
  md5: 294065597efdc7071921694c11bdfa9a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - country-bounding-boxes = country-bounding-boxes:main
    #
    # Would create an entry point called country-bounding-boxes that calls country-bounding-boxes.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - iso3166

  run:
    - python
    - iso3166

test:
  # Python imports
  imports:
    - country_bounding_boxes

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/graydon/country-bounding-boxes
  license: Apache Software License
  summary: 'Library of bounding boxes of countries (and associated data)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
