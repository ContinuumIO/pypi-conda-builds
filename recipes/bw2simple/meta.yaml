package:
  name: bw2simple
  version: "0.3.1"

source:
  fn: bw2simple-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/b/bw2simple/bw2simple-0.3.1.tar.gz
  md5: bc12e02c636ec3f837eab8567ce7f2d6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bw2simple = bw2simple:main
    #
    # Would create an entry point called bw2simple that calls bw2simple.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - brightway2
    - bw2calc >=0.13
    - bw2data >=0.15.1
    - bw2ui >=0.12.1

  run:
    - python
    - brightway2
    - bw2calc >=0.13
    - bw2data >=0.15.1
    - bw2ui >=0.12.1

test:
  # Python imports
  imports:
    - bw2simple

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/cmutel/brightway2-simple
  license: BSD License
  summary: ('Simple proxies to make command-line life with Brightway2 objects a bit easier.',)

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
