package:
  name: img_rotate
  version: "0.1.1"

source:
  fn: img_rotate-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/i/img_rotate/img_rotate-0.1.1.tar.gz
  md5: 28c3c83d5b00488eacd041fb178a858b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - img_rotate = img_rotate:main
    #
    # Would create an entry point called img_rotate that calls img_rotate.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - img_rotate

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kylefox/python-image-orientation-patch
  license: MIT
  summary: 'Rotates PIL Image instances after EXIF-tagged image orientation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
