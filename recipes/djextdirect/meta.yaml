package:
  name: djextdirect
  version: "0.7"

source:
  fn: v0.7.tar.bz2
  url: http://bitbucket.org/Svedrin/djextdirect/get/v0.7.tar.bz2
  #md5: 
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djextdirect = djextdirect:main
    #
    # Would create an entry point called djextdirect that calls djextdirect.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - djextdirect

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/Svedrin/djextdirect/downloads
  license: GNU General Public License (GPL)
  summary: 'Ext.Direct serverside and clientside implementation for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
