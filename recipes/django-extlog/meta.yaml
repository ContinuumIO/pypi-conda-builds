package:
  name: django-extlog
  version: "0.8"

source:
  fn: django-extlog-0.8.tar.gz
  url: https://pypi.python.org/packages/source/d/django-extlog/django-extlog-0.8.tar.gz
  md5: e8f8c608da125f8ef879705093ba4c2e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-extlog = django-extlog:main
    #
    # Would create an entry point called django-extlog that calls django-extlog.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-extensions

  run:
    - python
    - django-extensions

test:
  # Python imports
  imports:
    - django_extlog

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/AxiaCore/django-extlog
  license: MIT
  summary: 'Extended log for Django, that tracks changes in models.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
