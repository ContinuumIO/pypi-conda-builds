package:
  name: zc.signalhandler
  version: "1.2.0"

source:
  fn: zc.signalhandler-1.2.0.tar.gz
  url: https://pypi.python.org/packages/source/z/zc.signalhandler/zc.signalhandler-1.2.0.tar.gz
  md5: 8848baf8d234288624ed9d8bd53e69de
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zc.signalhandler = zc.signalhandler:main
    #
    # Would create an entry point called zc.signalhandler that calls zc.signalhandler.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zconfig >=2.4a3

  run:
    - python
    - zconfig >=2.4a3

test:
  # Python imports
  imports:
    - zc
    - zc.signalhandler

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: ZPL 2.1
  summary: 'Configurable signal handling for ZConfig'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
