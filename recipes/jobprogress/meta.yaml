package:
  name: jobprogress
  version: "1.0.4"

source:
  fn: jobprogress-1.0.4.tar.gz
  url: https://pypi.python.org/packages/source/j/jobprogress/jobprogress-1.0.4.tar.gz
  md5: 1f973f1178e40f6979b78b5806d15d0f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jobprogress = jobprogress:main
    #
    # Would create an entry point called jobprogress that calls jobprogress.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - jobprogress

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://hg.hardcoded.net/jobprogress/
  license: BSD License
  summary: 'Cross-toolkit UI progress tracking'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
