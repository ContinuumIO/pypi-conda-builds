package:
  name: geocodertools
  version: "0.1.6"

source:
  fn: geocodertools-0.1.6.tar.gz
  url: https://pypi.python.org/packages/source/g/geocodertools/geocodertools-0.1.6.tar.gz
  md5: 2d6a105f555ee1f4a10036acf0813270
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - geocodertools = geocodertools:main
    #
    # Would create an entry point called geocodertools that calls geocodertools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - argparse
    - nose

  run:
    - python
    - argparse
    - nose

test:
  # Python imports
  imports:
    - geocodertools

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/MartinThoma/geocodertools
  license: MIT License
  summary: 'Functions to work with Geo coordinates, reverse geo coding and getting city names out of coordinates without internet'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
