package:
  name: jumon
  version: "1.1.11"

source:
  fn: jumon-1.1.11.tar.gz
  url: https://pypi.python.org/packages/source/j/jumon/jumon-1.1.11.tar.gz
  md5: 1dcae080ef737a85ffbcac25e3744ac1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jumon = jumon:main
    #
    # Would create an entry point called jumon that calls jumon.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - enum34

  run:
    - python
    - enum34

test:
  # Python imports
  imports:
    - jumon

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/TakesxiSximada/jumon
  license: Apache Software License
  summary: 'The small framework for sub commands.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
