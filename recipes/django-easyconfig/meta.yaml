package:
  name: django-easyconfig
  version: "0.1"

source:
  fn: django-easyconfig-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-easyconfig/django-easyconfig-0.1.tar.gz
  md5: a4be411dae8935e2cc6af3cc0eadadc1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-easyconfig = django-easyconfig:main
    #
    # Would create an entry point called django-easyconfig that calls django-easyconfig.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - easyconfig

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/petersanchez/django-easyconfig/
  license: BSD License
  summary: 'Module to make it easy to configure external Django apps.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
