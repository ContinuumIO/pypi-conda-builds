package:
  name: odictliteral
  version: "1.0.0"

source:
  fn: odictliteral-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/o/odictliteral/odictliteral-1.0.0.tar.gz
  md5: 818bbf5ce787c9f258fcc38a9c42ba61
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - odictliteral = odictliteral:main
    #
    # Would create an entry point called odictliteral that calls odictliteral.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - odictliteral

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ajtowns/odictliteral
  license: MIT
  summary: 'A tidier way of coding literal OrderedDicts'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
