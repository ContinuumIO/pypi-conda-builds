package:
  name: gchecky
  version: "0.2.1"

source:
  fn: gchecky-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/g/gchecky/gchecky-0.2.1.tar.gz
  md5: 37d85fa1a81584dbf9e3198c64757e84
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gchecky = gchecky:main
    #
    # Would create an entry point called gchecky that calls gchecky.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - iso8601

  run:
    - python
    - iso8601

test:
  # Python imports
  imports:
    - gchecky
    - gchecky.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://gchecky.googlecode.com
  license: Apache License 2.0
  summary: 'Python wrapper for Google Checkout API Level 2'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
