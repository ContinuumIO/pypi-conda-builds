package:
  name: products.securemaildrophost
  version: "0.2"

source:
  fn: Products.SecureMaildropHost-0.2.tar.gz
  url: https://pypi.python.org/packages/source/P/Products.SecureMaildropHost/Products.SecureMaildropHost-0.2.tar.gz
  md5: 738c3db741a61089d647df83d85c5dcf
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - products.securemaildrophost = products.securemaildrophost:main
    #
    # Would create an entry point called products.securemaildrophost that calls products.securemaildrophost.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - Products
    - Products.SecureMaildropHost

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/securemaildrophost
  license: UNKNOWN
  summary: 'SecureMaildropHost marries SecureMailHost and MaildropHost, making this a drop-in replacement for SecureMailHost.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
