package:
  name: pomp
  version: "0.1.1.dev"

source:
  fn: pomp-0.1.1.dev.tar.gz
  url: https://pypi.python.org/packages/source/p/pomp/pomp-0.1.1.dev.tar.gz
  md5: 1a000e9d9db3ba33f9caa31616b8c392
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pomp = pomp:main
    #
    # Would create an entry point called pomp that calls pomp.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - defer

  run:
    - python
    - defer

test:
  # Python imports
  imports:
    - pomp
    - pomp.contrib
    - pomp.core

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/estin/pomp
  license: BSD License
  summary: 'Screen scraping and web crawling framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
