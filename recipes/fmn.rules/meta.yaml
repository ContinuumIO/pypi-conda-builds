package:
  name: fmn.rules
  version: "0.6.2"

source:
  fn: fmn.rules-0.6.2.tar.gz
  url: https://pypi.python.org/packages/source/f/fmn.rules/fmn.rules-0.6.2.tar.gz
  md5: 187d70a115be1c511080cca7c20a3df3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fmn.rules = fmn.rules:main
    #
    # Would create an entry point called fmn.rules that calls fmn.rules.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python-fedora
    - fedmsg
    - fedmsg_meta_fedora_infrastructure
    - dogpile.cache

  run:
    - python
    - python-fedora
    - fedmsg
    - fedmsg_meta_fedora_infrastructure
    - dogpile.cache

test:
  # Python imports
  imports:
    - fmn
    - fmn.rules

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/fedora-infra/fmn.rules
  license: LGPLv2+
  summary: 'Message processing rules for Fedora Notifications'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
