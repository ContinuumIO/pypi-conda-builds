package:
  name: alertlogic
  version: "0.4.2"

source:
  fn: alertlogic-0.4.2.tar.gz
  url: https://pypi.python.org/packages/source/a/alertlogic/alertlogic-0.4.2.tar.gz
  md5: 07e86b7678db99ddba2f27947e3a79b9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - alertlogic = alertlogic:main
    #
    # Would create an entry point called alertlogic that calls alertlogic.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=1.1.0

  run:
    - python
    - requests >=1.1.0

test:
  # Python imports
  imports:
    - alertlogic

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/joekiller/alertlogic-python
  license: Apache Software License
  summary: 'Alert Logic Library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
