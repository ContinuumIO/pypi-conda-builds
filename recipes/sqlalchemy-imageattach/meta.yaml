package:
  name: sqlalchemy-imageattach
  version: "0.9.0"

source:
  fn: SQLAlchemy-ImageAttach-0.9.0.tar.gz
  url: https://pypi.python.org/packages/source/S/SQLAlchemy-ImageAttach/SQLAlchemy-ImageAttach-0.9.0.tar.gz
  md5: af5734421ca40b8782ece37149fb63fe
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sqlalchemy-imageattach = sqlalchemy-imageattach:main
    #
    # Would create an entry point called sqlalchemy-imageattach that calls sqlalchemy-imageattach.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sqlalchemy >=0.8.0
    - wand >=0.3.0

  run:
    - python
    - sqlalchemy >=0.8.0
    - wand >=0.3.0

test:
  # Python imports
  imports:
    - sqlalchemy_imageattach
    - sqlalchemy_imageattach.stores

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dahlia/sqlalchemy-imageattach
  license: MIT License
  summary: 'SQLAlchemy extension for attaching images to entity objects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
