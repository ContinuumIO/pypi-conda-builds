package:
  name: colorize
  version: "1.0.4"

source:
  fn: colorize-1.0.4.tar.gz
  url: https://pypi.python.org/packages/source/c/colorize/colorize-1.0.4.tar.gz
  md5: 6dc3e34406832a66fc236a6be905666f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - colorize = colorize:main
    #
    # Would create an entry point called colorize that calls colorize.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - colorize

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/magmax/colorize
  license: MIT License
  summary: 'Command line utility to colorize other commands output'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
