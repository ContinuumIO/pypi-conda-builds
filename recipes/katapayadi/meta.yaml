package:
  name: katapayadi
  version: "0.2.0"

source:
  fn: katapayadi-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/k/katapayadi/katapayadi-0.2.0.tar.gz
  md5: e4df198176cb1934e3e5bab81b2c41a3
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - katapayadi = katapayadi:main
    #
    # Would create an entry point called katapayadi that calls katapayadi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - indicsyllabifier
    - silpa_common

  run:
    - python
    - setuptools
    - indicsyllabifier
    - silpa_common

test:
  # Python imports
  imports:
    - katapayadi
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://silpa.org.in/Katapayadi
  license: LGPL-3.0
  summary: 'Decode katapayadi number system'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
