package:
  name: edeposit.amqp.antivir
  version: "0.1-alpha"

source:
  fn: edeposit.amqp.antivir-0.1-alpha.tar.gz
  url: https://pypi.python.org/packages/source/e/edeposit.amqp.antivir/edeposit.amqp.antivir-0.1-alpha.tar.gz
  md5: 975bb2b6117af81c2fa208803c1cf218
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - edeposit.amqp.antivir = edeposit.amqp.antivir:main
    #
    # Would create an entry point called edeposit.amqp.antivir that calls edeposit.amqp.antivir.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - kombu

  run:
    - python
    - setuptools
    - kombu

test:
  # Python imports
  imports:
    - edeposit
    - edeposit.amqp
    - edeposit.amqp.antivir
    - edeposit.amqp.antivir.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jstavel/edeposit.amqp.antivir/
  license: GNU General Public License v2 or later (GPLv2+)
  summary: 'E-Deposit module for RabbitMQ antivirus'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
