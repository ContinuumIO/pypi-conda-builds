package:
  name: ktm
  version: "0.1.1"

source:
  fn: ktm-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/k/ktm/ktm-0.1.1.tar.gz
  md5: 8ba2abdae8c4d39c47e8b49a858e9ef4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ktm = ktm:main
    #
    # Would create an entry point called ktm that calls ktm.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - ktm

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/skalanux/ktm
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'Configurable notification daemon with queues and history display'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
