package:
  name: smapp-toolkit
  version: "0.1.23"

source:
  fn: smapp-toolkit-0.1.23.tar.gz
  url: https://pypi.python.org/packages/source/s/smapp-toolkit/smapp-toolkit-0.1.23.tar.gz
  md5: 6719f8b87a7b552e718e4ec615f73aae
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - smapp-toolkit = smapp-toolkit:main
    #
    # Would create an entry point called smapp-toolkit that calls smapp-toolkit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pymongo >=3.0.1
    - smapppy >=0.1.16
    - networkx >=1.9.1

  run:
    - python
    - pymongo >=3.0.1
    - smapppy >=0.1.16
    - networkx >=1.9.1

test:
  # Python imports
  imports:
    - smapp_toolkit
    - smapp_toolkit.twitter

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://smapp.nyu.edu
  license: GPLv2
  summary: 'NYU SMaPP lab toolkit'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
