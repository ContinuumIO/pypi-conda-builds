package:
  name: complexsystems
  version: "0.3.2"

source:
  fn: ComplexSystems-0.3.2.tar.gz
  url: https://pypi.python.org/packages/source/C/ComplexSystems/ComplexSystems-0.3.2.tar.gz
  md5: 414eee5356b4fce2037fb5af19f8f837
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - complexsystems = complexsystems:main
    #
    # Would create an entry point called complexsystems that calls complexsystems.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy >=1.4.0

  run:
    - python
    - numpy >=1.4.0

test:
  # Python imports
  imports:
    - complex_systems
    - complex_systems.mobility
    - complex_systems.spatial
    - complexsystems

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/vgauthier/complex-systems/
  license: MIT License
  summary: 'Toolbox for Complex Sytems including : Public Good Game, Human Based Mobility Models'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
