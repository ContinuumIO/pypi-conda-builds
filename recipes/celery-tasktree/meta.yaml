package:
  name: celery-tasktree
  version: "0.3.4"

source:
  fn: celery-tasktree-0.3.4.tar.gz
  url: https://pypi.python.org/packages/source/c/celery-tasktree/celery-tasktree-0.3.4.tar.gz
  md5: bf225e5fed5a7d929e511fab5b6e9032
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - celery-tasktree = celery-tasktree:main
    #
    # Would create an entry point called celery-tasktree that calls celery-tasktree.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/NetAngels/celery-tasktree
  license: BSD License
  summary: 'Celery Tasktree module'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
