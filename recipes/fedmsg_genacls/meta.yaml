package:
  name: fedmsg_genacls
  version: "0.5"

source:
  fn: fedmsg_genacls-0.5.tar.gz
  url: https://pypi.python.org/packages/source/f/fedmsg_genacls/fedmsg_genacls-0.5.tar.gz
  md5: 290e4a69af50bf5ca34e3c6889117e23
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fedmsg_genacls = fedmsg_genacls:main
    #
    # Would create an entry point called fedmsg_genacls that calls fedmsg_genacls.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fedmsg

  run:
    - python
    - setuptools
    - fedmsg

test:
  # Python imports
  imports:
    - fedmsg_genacls

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/fedora-infra/fedmsg-genacls
  license: LGPLv2+
  summary: 'A fedmsg consumer that sets gitosis acls in response to pkgdb messages'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
