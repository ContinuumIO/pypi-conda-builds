package:
  name: zope.structuredtext
  version: "4.1.0"

source:
  fn: zope.structuredtext-4.1.0.tar.gz
  url: https://pypi.python.org/packages/source/z/zope.structuredtext/zope.structuredtext-4.1.0.tar.gz
  md5: 43ec3d474f97f0043ca7c22f52bade73
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zope.structuredtext = zope.structuredtext:main
    #
    # Would create an entry point called zope.structuredtext that calls zope.structuredtext.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - zope
    - zope.structuredtext

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/zope.structuredtext
  license: Zope Public License
  summary: 'StructuredText parser'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
