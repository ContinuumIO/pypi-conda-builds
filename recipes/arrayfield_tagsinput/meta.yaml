package:
  name: arrayfield_tagsinput
  version: "0.0.5"

source:
  fn: arrayfield_tagsinput-0.0.5.tar.gz
  url: https://pypi.python.org/packages/source/a/arrayfield_tagsinput/arrayfield_tagsinput-0.0.5.tar.gz
  md5: 8e117696cb0a61085cb04ab9d155e590
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - arrayfield_tagsinput = arrayfield_tagsinput:main
    #
    # Would create an entry point called arrayfield_tagsinput that calls arrayfield_tagsinput.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - djorm-ext-pgarray

  run:
    - python
    - djorm-ext-pgarray

test:
  # Python imports
  imports:
    - arrayfield_tagsinput

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.routeatlas.com
  license: GPL
  summary: u'An app that gives you tag input field for forms'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
