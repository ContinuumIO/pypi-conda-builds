package:
  name: mdx_textalign
  version: "1.0"

source:
  fn: mdx_textalign-1.0.zip
  url: https://pypi.python.org/packages/source/m/mdx_textalign/mdx_textalign-1.0.zip
  md5: 2554e3676f5814b63078682ca5a06b91
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mdx_textalign = mdx_textalign:main
    #
    # Would create an entry point called mdx_textalign that calls mdx_textalign.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - markdown >=2.0

  run:
    - python
    - markdown >=2.0

test:
  # Python imports
  imports:
    - mdx_textalign

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/andrewphorn/markdown.textalign
  license: UNKNOWN
  summary: 'Python-Markdown extension to support centering and right-aligning text.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
