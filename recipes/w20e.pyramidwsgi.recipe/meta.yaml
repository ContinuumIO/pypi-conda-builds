package:
  name: w20e.pyramidwsgi.recipe
  version: "1.0.0"

source:
  fn: w20e.pyramidwsgi.recipe-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/w/w20e.pyramidwsgi.recipe/w20e.pyramidwsgi.recipe-1.0.0.tar.gz
  md5: 58fac83ac80b838a33047ca3b915f6fd
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - w20e.pyramidwsgi.recipe = w20e.pyramidwsgi.recipe:main
    #
    # Would create an entry point called w20e.pyramidwsgi.recipe that calls w20e.pyramidwsgi.recipe.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - w20e
    - w20e.pyramidwsgi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: UNKNOWN
  summary: 'Pyramid WSGI recipe'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
