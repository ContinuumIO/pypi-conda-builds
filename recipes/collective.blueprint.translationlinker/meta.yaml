package:
  name: collective.blueprint.translationlinker
  version: "1.0.1"

source:
  fn: collective.blueprint.translationlinker-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.blueprint.translationlinker/collective.blueprint.translationlinker-1.0.1.tar.gz
  md5: ecfa6a7d049f83220375bf6eea71a482
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.blueprint.translationlinker = collective.blueprint.translationlinker:main
    #
    # Would create an entry point called collective.blueprint.translationlinker that calls collective.blueprint.translationlinker.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - collective.transmogrifier
    - products.linguaplone

  run:
    - python
    - setuptools
    - collective.transmogrifier
    - products.linguaplone

test:
  # Python imports
  imports:
    - collective
    - collective.blueprint
    - collective.blueprint.translationlinker

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/collective.blueprint.translationlinker
  license: GPL
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
