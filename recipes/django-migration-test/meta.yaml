package:
  name: django-migration-test
  version: "0.0.1"

source:
  fn: django-migration-test-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-migration-test/django-migration-test-0.0.1.tar.gz
  md5: 19b715abac339ef5d36fd1af0b563f0f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-migration-test = django-migration-test:main
    #
    # Would create an entry point called django-migration-test that calls django-migration-test.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/plumdog/django_migration_test
  license: UNKNOWN
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
