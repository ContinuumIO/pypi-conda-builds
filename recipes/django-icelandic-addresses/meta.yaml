package:
  name: django-icelandic-addresses
  version: "0.1.5"

source:
  fn: django-icelandic-addresses-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-icelandic-addresses/django-icelandic-addresses-0.1.5.tar.gz
  md5: 820d2fd4f8c6c219354b6c98a8ab672c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-icelandic-addresses = django-icelandic-addresses:main
    #
    # Would create an entry point called django-icelandic-addresses that calls django-icelandic-addresses.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - ice_addresses

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/StefanKjartansson/django-icelandic-addresses
  license: BSD License
  summary: 'Django app containing a list of Icelandic addresses'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
