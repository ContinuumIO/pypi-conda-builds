package:
  name: django-moneta
  version: "0.1"

source:
  fn: django-moneta-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-moneta/django-moneta-0.1.tar.gz
  md5: b5f3f211de74db38375c2abbb2ea79d4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-moneta = django-moneta:main
    #
    # Would create an entry point called django-moneta that calls django-moneta.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3

  run:
    - python
    - django >=1.3

test:
  # Python imports
  imports:
    - moneta

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/visionect/django-moneta
  license: MIT License
  summary: 'Django app for Moneta eTerminal API.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
