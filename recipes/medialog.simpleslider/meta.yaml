package:
  name: medialog.simpleslider
  version: "0.4.1"

source:
  fn: medialog.simpleslider-0.4.1.tar.gz
  url: https://pypi.python.org/packages/source/m/medialog.simpleslider/medialog.simpleslider-0.4.1.tar.gz
  md5: 69e780513a371787aba67df34bd90d0c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - medialog.simpleslider = medialog.simpleslider:main
    #
    # Would create an entry point called medialog.simpleslider that calls medialog.simpleslider.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.vocabularies
    - plone.api

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.vocabularies
    - plone.api

test:
  # Python imports
  imports:
    - medialog
    - medialog.simpleslider
    - medialog.simpleslider.browser
    - medialog.simpleslider.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/espenmn/medialog.simpleslider
  license: gpl
  summary: 'Adds a viewlet to display a slider'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
