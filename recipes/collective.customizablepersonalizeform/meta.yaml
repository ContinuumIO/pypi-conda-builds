package:
  name: collective.customizablepersonalizeform
  version: "2.0"

source:
  fn: collective.customizablePersonalizeForm-2.0.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.customizablePersonalizeForm/collective.customizablePersonalizeForm-2.0.tar.gz
  md5: d3f09ac5d5bea684e2d2b21aaa992787
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.customizablepersonalizeform = collective.customizablepersonalizeform:main
    #
    # Would create an entry point called collective.customizablepersonalizeform that calls collective.customizablepersonalizeform.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.customizablePersonalizeForm
    - collective.customizablePersonalizeForm.adapters
    - collective.customizablePersonalizeForm.browser
    - collective.customizablePersonalizeForm.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/
  license: GPL
  summary: 'Customizable personalize form for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
