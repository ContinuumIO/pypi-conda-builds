package:
  name: collective.linguadomains
  version: "1.1"

source:
  fn: collective.linguadomains-1.1.zip
  url: https://pypi.python.org/packages/source/c/collective.linguadomains/collective.linguadomains-1.1.zip
  md5: 95c5229f2168c50e46c0e89b4c978707
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.linguadomains = collective.linguadomains:main
    #
    # Would create an entry point called collective.linguadomains that calls collective.linguadomains.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.registry
    - products.linguaplone

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.registry
    - products.linguaplone

test:
  # Python imports
  imports:
    - collective
    - collective.linguadomains
    - collective.linguadomains.tests
    - collective.linguadomains.upgrades

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/toutpt/collective.linguadomains
  license: GPL
  summary: 'Manage translated domains for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
