package:
  name: raspberrystemtest
  version: "0.0.99-17-gafefb9b"

source:
  fn: raspberrystemtest-0.0.99-17-gafefb9b.tar.gz
  url: https://pypi.python.org/packages/source/r/raspberrystemtest/raspberrystemtest-0.0.99-17-gafefb9b.tar.gz
  md5: 5afee12708d30112a6e1181fae74df40
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - raspberrystemtest = raspberrystemtest:main
    #
    # Would create an entry point called raspberrystemtest that calls raspberrystemtest.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - raspberrystemtest
    - rstem
    - rstem.gpio
    - rstem.led_matrix
    - rstem.speaker

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://raspberrystem.com
  license: Apache Software License
  summary: 'RaspberrySTEM Test'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
