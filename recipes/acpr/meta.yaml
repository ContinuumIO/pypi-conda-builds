package:
  name: acpr
  version: "0.0.3"

source:
  fn: acpr-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/a/acpr/acpr-0.0.3.tar.gz
  md5: d7464149c92c4c791f5e5dc58d660b46
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - acpr = acpr:main
    #
    # Would create an entry point called acpr that calls acpr.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - picklecache
    - requests
    - lxml

  run:
    - python
    - picklecache
    - requests
    - lxml

test:
  # Python imports
  imports:
    - acpr

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tlevine/acpr-banque-de-france
  license: AGPL
  summary: 'Download ACPR Banque de France - Regafi (Financial Firms Register)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
