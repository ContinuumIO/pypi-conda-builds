package:
  name: slushbox
  version: "0.2.1"

source:
  fn: Slushbox-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/S/Slushbox/Slushbox-0.2.1.tar.gz
  md5: a2fdd2058dfcb866e1792f3e11ff9e5d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - slushbox = slushbox:main
    #
    # Would create an entry point called slushbox that calls slushbox.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - macfsevents >=0.2.4

  run:
    - python
    - macfsevents >=0.2.4

test:
  # Python imports
  imports:
    - slushbox

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/workmajj/slushbox
  license: BSD 3-Clause <http://www.opensource.org/licenses/BSD-3-Clause>
  summary: 'Reloads web pages when files in local directories change.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
