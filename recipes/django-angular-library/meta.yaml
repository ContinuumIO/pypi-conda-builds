package:
  name: django-angular-library
  version: "1.2.6"

source:
  fn: django-angular-library-1.2.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-angular-library/django-angular-library-1.2.6.tar.gz
  md5: ca03cf931dfa7a1a7702abe3c65fa8b2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-angular-library = django-angular-library:main
    #
    # Would create an entry point called django-angular-library that calls django-angular-library.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - angular

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/IxDay/django-angular-library
  license: MIT License
  summary: 'Angular packaged in an handy django app to speed up new applications and deployment.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
