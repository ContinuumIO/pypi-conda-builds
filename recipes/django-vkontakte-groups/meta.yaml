package:
  name: django-vkontakte-groups
  version: "0.7.1"

source:
  fn: django-vkontakte-groups-0.7.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-vkontakte-groups/django-vkontakte-groups-0.7.1.tar.gz
  md5: 839925840188df014a6fdc3717a55ae8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-vkontakte-groups = django-vkontakte-groups:main
    #
    # Would create an entry point called django-vkontakte-groups that calls django-vkontakte-groups.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-vkontakte-api >=0.7.9
    - django-m2m-history >=0.2.4

  run:
    - python
    - django-vkontakte-api >=0.7.9
    - django-m2m-history >=0.2.4

test:
  # Python imports
  imports:
    - vkontakte_groups
    - vkontakte_groups.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ramusus/django-vkontakte-groups
  license: BSD License
  summary: 'Django implementation for vkontakte API Groups'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
