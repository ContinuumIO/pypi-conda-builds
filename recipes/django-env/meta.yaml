package:
  name: django-env
  version: "0.2.1"

source:
  fn: django-env-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-env/django-env-0.2.1.tar.gz
  md5: e55faa5c3b86285894043438d7510537
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-env = django-env:main
    #
    # Would create an entry point called django-env that calls django-env.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - env
    - env.management
    - env.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/spinus/django-env
  license: BSD License
  summary: 'Automaticly manages virtualenv for django project'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
