package:
  name: djaloha
  version: "0.4.2"

source:
  fn: Djaloha-0.4.2.tar.gz
  url: https://pypi.python.org/packages/source/D/Djaloha/Djaloha-0.4.2.tar.gz
  md5: 96131ca433f47b64acc17aeb9824e963
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djaloha = djaloha:main
    #
    # Would create an entry point called djaloha that calls djaloha.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-floppyforms ==0.4.7

  run:
    - python
    - django-floppyforms ==0.4.7

test:
  # Python imports
  imports:
    - djaloha
    - djaloha.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD
  summary: 'Django integration for aloha HTML5 editor'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
