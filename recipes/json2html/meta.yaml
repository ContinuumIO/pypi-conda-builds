package:
  name: json2html
  version: "0.3"

source:
  fn: json2html-0.3.tar.gz
  url: https://pypi.python.org/packages/source/j/json2html/json2html-0.3.tar.gz
  md5: 42d230103057eeec9044ad5e9759bcfb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - json2html = json2html:main
    #
    # Would create an entry point called json2html that calls json2html.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - ordereddict

  run:
    - python
    - ordereddict

test:
  # Python imports
  imports:
    - json2html

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/softvar/json2html
  license: MIT
  summary: 'JSON object to human readable HTML representation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
