package:
  name: tooth.paste
  version: "2.0"

source:
  fn: tooth.paste-2.0.zip
  url: https://pypi.python.org/packages/source/t/tooth.paste/tooth.paste-2.0.zip
  md5: 1fe0d4d3fd0caa1a06593e5513ca31af
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tooth.paste = tooth.paste:main
    #
    # Would create an entry point called tooth.paste that calls tooth.paste.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - pastescript
    - templer.core

  run:
    - python
    - setuptools
    - setuptools
    - pastescript
    - templer.core

test:
  # Python imports
  imports:
    - tooth
    - tooth.paste
    - tooth.paste.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/maikroeder/tooth.paste.git
  license: gpl
  summary: 'Create shiny new Python packages with Sphinx documentation, unit tests and tools to keep your code clean'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
