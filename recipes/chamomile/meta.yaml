package:
  name: chamomile
  version: "1.0.1"

source:
  fn: chamomile-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/chamomile/chamomile-1.0.1.tar.gz
  md5: ec8bc4c7125a2f260185c9b9a2241b00
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - chamomile = chamomile:main
    #
    # Would create an entry point called chamomile that calls chamomile.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - chamomile

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/enaeseth/chamomile
  license: MIT License
  summary: 'Jasmine-style assertions for unittest'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
