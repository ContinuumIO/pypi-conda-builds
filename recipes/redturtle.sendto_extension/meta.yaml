package:
  name: redturtle.sendto_extension
  version: "2.0.0b2"

source:
  fn: redturtle.sendto_extension-2.0.0b2.zip
  url: https://pypi.python.org/packages/source/r/redturtle.sendto_extension/redturtle.sendto_extension-2.0.0b2.zip
  md5: 5cff13f5ee02fff1de7440e44d40650d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - redturtle.sendto_extension = redturtle.sendto_extension:main
    #
    # Would create an entry point called redturtle.sendto_extension that calls redturtle.sendto_extension.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.js.jqueryui
    - rt.zptformfield
    - collective.autopermission

  run:
    - python
    - setuptools
    - setuptools
    - collective.js.jqueryui
    - rt.zptformfield
    - collective.autopermission

test:
  # Python imports
  imports:
    - redturtle
    - redturtle.sendto_extension
    - redturtle.sendto_extension.browser
    - redturtle.sendto_extension.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/redturtle.sendto_extension
  license: GPL
  summary: 'An extension for the "Send this" Plone document action'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
