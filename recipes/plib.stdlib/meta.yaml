package:
  name: plib.stdlib
  version: "0.9.21"

source:
  fn: plib.stdlib-0.9.21.tar.gz
  url: https://pypi.python.org/packages/source/p/plib.stdlib/plib.stdlib-0.9.21.tar.gz
  md5: a96ab240f58aaa5f7c5d918c7a53bca8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plib.stdlib = plib.stdlib:main
    #
    # Would create an entry point called plib.stdlib that calls plib.stdlib.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - plib
    - plib.stdlib
    - plib.stdlib.classes
    - plib.stdlib.coll
    - plib.stdlib.comm
    - plib.stdlib.dbtools
    - plib.stdlib.decotools
    - plib.stdlib.extensions
    - plib.stdlib.ini
    - plib.stdlib.util
    - plib.test
    - plib.test.stdlib
    - plib.test.support

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/plib.stdlib
  license: GNU General Public License v2 (GPLv2)
  summary: 'Useful packages and modules to extend the Python standard library.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
