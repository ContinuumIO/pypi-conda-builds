package:
  name: chevah-keycert
  version: "1.1.0"

source:
  fn: chevah-keycert-1.1.0.tar.gz
  url: https://pypi.python.org/packages/source/c/chevah-keycert/chevah-keycert-1.1.0.tar.gz
  md5: 2e0386652d9b5c62dfc6dad3ded92a95
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - chevah-keycert = chevah-keycert:main
    #
    # Would create an entry point called chevah-keycert that calls chevah-keycert.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyopenssl ==0.13
    - pycrypto ==2.6.1
    - pyasn1 ==0.1.7
    - chevah-compat ==0.27.1

  run:
    - python
    - pyopenssl ==0.13
    - pycrypto ==2.6.1
    - pyasn1 ==0.1.7
    - chevah-compat ==0.27.1

test:
  # Python imports
  imports:
    - chevah
    - chevah.keycert
    - chevah.keycert.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/chevah/chevah-keycert
  license: MIT License
  summary: 'SSH Keys and SSL keys and certificates management.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
