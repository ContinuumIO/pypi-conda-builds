package:
  name: celery-haystack
  version: "0.8"

source:
  fn: celery-haystack-0.8.tar.gz
  url: https://pypi.python.org/packages/source/c/celery-haystack/celery-haystack-0.8.tar.gz
  md5: 3d447dada1d2da03d3d0277d7a169825
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - celery-haystack = celery-haystack:main
    #
    # Would create an entry point called celery-haystack that calls celery-haystack.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD License
  summary: 'An app for integrating Celery with Haystack.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
