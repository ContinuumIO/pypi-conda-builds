package:
  name: configman
  version: "1.2.13"

source:
  fn: configman-1.2.13.tar.gz
  url: https://pypi.python.org/packages/source/c/configman/configman-1.2.13.tar.gz
  md5: 095d6dfffe26f295f871978b9f3cec4b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - configman = configman:main
    #
    # Would create an entry point called configman that calls configman.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - configobj >=4.7.2

  run:
    - python
    - configobj >=4.7.2

test:
  # Python imports
  imports:
    - configman

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mozilla/configman
  license: Mozilla Public License 2.0 (MPL 2.0)
  summary: 'Flexible reading and writing of namespaced configuration options'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
