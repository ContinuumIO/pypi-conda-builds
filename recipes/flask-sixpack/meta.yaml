package:
  name: flask-sixpack
  version: "0.0.1"

source:
  fn: Flask-Sixpack-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Sixpack/Flask-Sixpack-0.0.1.tar.gz
  md5: 70df521b01b5b6c6a520087feacb21b7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-sixpack = flask-sixpack:main
    #
    # Would create an entry point called flask-sixpack that calls flask-sixpack.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - sixpack-client

  run:
    - python
    - flask
    - sixpack-client

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://sixpack.seatgeek.com/
  license: BSD License
  summary: 'Flask wrapper for Sixpack'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
