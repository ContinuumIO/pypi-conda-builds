package:
  name: confpluspy
  version: "1.0"

source:
  fn: confpluspy-1.0.tar.bz2
  url: https://pypi.python.org/packages/source/c/confpluspy/confpluspy-1.0.tar.bz2
  md5: e06094dc931e9cbd8d7f8d726dbff27e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - confpluspy = confpluspy:main
    #
    # Would create an entry point called confpluspy that calls confpluspy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - confpluspy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://code.google.com/p/confpluspy/
  license: GNU GPL v3
  summary: 'Expansion of standard configuration files by Python functionality'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
