package:
  name: authkerb
  version: "0.1"

source:
  fn: authkerb-0.1.tar.gz
  url: https://pypi.python.org/packages/source/a/authkerb/authkerb-0.1.tar.gz
  md5: 5098ddc774f11eefd4eefe537600d410
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - authkerb = authkerb:main
    #
    # Would create an entry point called authkerb that calls authkerb.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - twisted
    - kerberos

  run:
    - python
    - twisted
    - kerberos

test:
  # Python imports
  imports:
    - authkerb

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/alexeyzotov/authkerb
  license: MIT License
  summary: 'Kerberos authentication for Twisted'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
