package:
  name: tenpy
  version: "0.1"

source:
  fn: tenpy-0.1.tar.gz
  url: https://pypi.python.org/packages/source/t/tenpy/tenpy-0.1.tar.gz
  md5: b2cfbea00fb02309739e875abcb5cba5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tenpy = tenpy:main
    #
    # Would create an entry point called tenpy that calls tenpy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - lxml >=2.2.8

  run:
    - python
    - lxml >=2.2.8

test:
  # Python imports
  imports:
    - tenpy
    - tenpy.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/naoina/tenpy
  license: BSD License
  summary: "Template engine which realized the concept of 'Independence of Presentation Logic' for Python 3"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
