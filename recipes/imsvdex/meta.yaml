package:
  name: imsvdex
  version: "1.0"

source:
  fn: imsvdex-1.0.tar.gz
  url: https://pypi.python.org/packages/source/i/imsvdex/imsvdex-1.0.tar.gz
  md5: 9b73693d6ddf59f0c5af371db2ca5bde
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - imsvdex = imsvdex:main
    #
    # Would create an entry point called imsvdex that calls imsvdex.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - elementtree >=1.2.6
    - elementtreewriter >=1.0

  run:
    - python
    - elementtree >=1.2.6
    - elementtreewriter >=1.0

test:
  # Python imports
  imports:
    - imsvdex

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/imsvdex/
  license: D-FSL - German Free Software License
  summary: 'Read/write vocabularies in IMS Vocabulary Definition Exchange format.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
