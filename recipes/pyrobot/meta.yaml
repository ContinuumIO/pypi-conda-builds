package:
  name: pyrobot
  version: "0.1.0"

source:
  fn: pyrobot-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/p/pyrobot/pyrobot-0.1.0.tar.gz
  md5: d5b9f52a79752c495eeae2e46b4c255d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pyrobot = pyrobot:main
    #
    # Would create an entry point called pyrobot that calls pyrobot.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - nose
    - responses

  run:
    - python
    - nose
    - responses

test:
  # Python imports
  imports:
    - pyrobot

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jmcarp/pyrobot
  license: BSD License
  summary: 'Your friendly neighborhood web scraper'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
