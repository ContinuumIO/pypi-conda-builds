package:
  name: zc.recipe.filestorage
  version: "1.1.2"

source:
  fn: zc.recipe.filestorage-1.1.2.zip
  url: https://pypi.python.org/packages/source/z/zc.recipe.filestorage/zc.recipe.filestorage-1.1.2.zip
  md5: 85340a1166b04e3a564b2d299b215c16
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zc.recipe.filestorage = zc.recipe.filestorage:main
    #
    # Would create an entry point called zc.recipe.filestorage that calls zc.recipe.filestorage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zc.buildout
    - zope.testing
    - setuptools
    - six

  run:
    - python
    - setuptools
    - zc.buildout
    - zope.testing
    - setuptools
    - six

test:
  # Python imports
  imports:
    - zc
    - zc.recipe
    - zc.recipe.filestorage

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/zc.recipe.filestorage/
  license: Zope Public License
  summary: 'ZC Buildout recipe for defining a file-storage'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
