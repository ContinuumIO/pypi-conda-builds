package:
  name: django_ckeditor_improved
  version: "1.2.3"

source:
  fn: django_ckeditor_improved-1.2.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django_ckeditor_improved/django_ckeditor_improved-1.2.3.tar.gz
  md5: d3bec91653d0058d27f8d09c4af15e10
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_ckeditor_improved = django_ckeditor_improved:main
    #
    # Would create an entry point called django_ckeditor_improved that calls django_ckeditor_improved.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - setuptools
    - django

test:
  # Python imports
  imports:
    - ckeditorfiles
    - django_ckeditor_improved

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ninapavlich/django_ckeditor_improved
  license: BSD
  summary: 'CKEditor bundled as a django staticfiles app.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
