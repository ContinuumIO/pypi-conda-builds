package:
  name: gback
  version: "0.51"

source:
  fn: gback-0.51.tar.gz
  url: https://pypi.python.org/packages/source/g/gback/gback-0.51.tar.gz
  md5: 3486094f1cd96ac8192872c3c169c853
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gback = gback:main
    #
    # Would create an entry point called gback that calls gback.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python-dateutil >=2.4.0
    - httplib2 >=0.8
    - urllib3 >=1.7.1
    - google-api-python-client >=1.4.0
    - python-gflags >=2.0
    - icalendar >=3.9.0

  run:
    - python
    - python-dateutil >=2.4.0
    - httplib2 >=0.8
    - urllib3 >=1.7.1
    - google-api-python-client >=1.4.0
    - python-gflags >=2.0
    - icalendar >=3.9.0

test:
  # Python imports
  imports:
    - gback

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/Schwarzschild/gback
  license: MIT
  summary: 'Google Calendar Backup utility.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
