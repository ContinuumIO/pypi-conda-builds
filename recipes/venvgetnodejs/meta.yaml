package:
  name: venvgetnodejs
  version: "0.0.3"

source:
  fn: venvgetnodejs-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/v/venvgetnodejs/venvgetnodejs-0.0.3.tar.gz
  md5: 704f5ad77f10ebd71c1500e32683361f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - venvgetnodejs = venvgetnodejs:main
    #
    # Would create an entry point called venvgetnodejs that calls venvgetnodejs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyxdg

  run:
    - python
    - pyxdg

test:
  # Python imports
  imports:
    - venvgetnodejs

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/wonderbeyond/venvgetnodejs
  license: UNKNOWN
  summary: 'Download and Install nodejs in current python virtualenv'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
