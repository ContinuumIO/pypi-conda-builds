package:
  name: django-jquery-tipsy
  version: "1.0.0a"

source:
  fn: django-jquery-tipsy-1.0.0a.tar.gz
  url: https://pypi.python.org/packages/source/d/django-jquery-tipsy/django-jquery-tipsy-1.0.0a.tar.gz
  md5: 2fcdeaa470ecc822cc205886d55564ba
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-jquery-tipsy = django-jquery-tipsy:main
    #
    # Would create an entry point called django-jquery-tipsy that calls django-jquery-tipsy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-jquery >=1.6

  run:
    - python
    - django-jquery >=1.6

test:
  # Python imports
  imports:
    - jquery_tipsy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mikebryant/django-jquery-tipsy
  license: MIT License
  summary: 'jQuery tipsy plugin packaged in a django app to speed up new applications and deployment.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
