package:
  name: minos
  version: "0.1.2"

source:
  fn: Minos-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/M/Minos/Minos-0.1.2.tar.gz
  md5: b383fb72d0747e7387f6585e755291df
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - minos = minos:main
    #
    # Would create an entry point called minos that calls minos.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fabric ==1.5.3
    - pygments ==1.6
    - sqlalchemy ==0.7.8
    - sphinx ==1.1.3
    - turbomail ==3.0.3
    - docutils ==0.10
    - nose ==1.2.1

  run:
    - python
    - fabric ==1.5.3
    - pygments ==1.6
    - sqlalchemy ==0.7.8
    - sphinx ==1.1.3
    - turbomail ==3.0.3
    - docutils ==0.10
    - nose ==1.2.1

test:
  # Python imports
  imports:
    - minos
    - minos.tests
    - minos.tests.validators
    - minos.validators

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/uber/minos
  license: MIT License
  summary: 'Minos is a library to do flexible validation of Python objects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
