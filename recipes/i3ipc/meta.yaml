package:
  name: i3ipc
  version: "1.1.4"

source:
  fn: i3ipc-1.1.4.tar.gz
  url: https://pypi.python.org/packages/source/i/i3ipc/i3ipc-1.1.4.tar.gz
  md5: de8653568459e37db30ab866a963ce04
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - i3ipc = i3ipc:main
    #
    # Would create an entry point called i3ipc that calls i3ipc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python3-xlib
    - enum34

  run:
    - python
    - python3-xlib
    - enum34

test:
  # Python imports
  imports:
    - i3ipc

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/acrisci/i3ipc-python
  license: BSD License
  summary: 'An improved Python library for i3wm extensions'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
