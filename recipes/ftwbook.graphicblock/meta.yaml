package:
  name: ftwbook.graphicblock
  version: "2.2.3"

source:
  fn: ftwbook.graphicblock-2.2.3.zip
  url: https://pypi.python.org/packages/source/f/ftwbook.graphicblock/ftwbook.graphicblock-2.2.3.zip
  md5: 3012a85e0e1249d723e22c879bb8e774
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ftwbook.graphicblock = ftwbook.graphicblock:main
    #
    # Would create an entry point called ftwbook.graphicblock that calls ftwbook.graphicblock.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - ftw.book
    - ftw.pdfgenerator
    - simplelayout.base

  run:
    - python
    - setuptools
    - setuptools
    - ftw.book
    - ftw.pdfgenerator
    - simplelayout.base

test:
  # Python imports
  imports:
    - ftwbook
    - ftwbook.graphicblock
    - ftwbook.graphicblock.browser
    - ftwbook.graphicblock.content
    - ftwbook.graphicblock.latex
    - ftwbook.graphicblock.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/4teamwork/ftwbook.graphicblock
  license: GPL2
  summary: 'Addon for `ftw.book` providing a graphics block for including PDF documents in the book.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
