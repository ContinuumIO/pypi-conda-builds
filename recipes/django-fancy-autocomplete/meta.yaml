package:
  name: django-fancy-autocomplete
  version: "0.1a1"

source:
  fn: django-fancy-autocomplete-0.1a1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-fancy-autocomplete/django-fancy-autocomplete-0.1a1.tar.gz
  md5: 7a17a35d9231d53316530d78af190628
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-fancy-autocomplete = django-fancy-autocomplete:main
    #
    # Would create an entry point called django-fancy-autocomplete that calls django-fancy-autocomplete.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - fancy_autocomplete

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jeffkistler/django-fancy-autocomplete
  license: BSD License
  summary: 'A simple AJAX autocomplete helper app for Django projects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
