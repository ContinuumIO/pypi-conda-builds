package:
  name: django-flipbook
  version: "0.3.7"

source:
  fn: django-flipbook-0.3.7.tar.gz
  url: https://pypi.python.org/packages/source/d/django-flipbook/django-flipbook-0.3.7.tar.gz
  md5: e7f185020ea48589de4132beb45ef962
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-flipbook = django-flipbook:main
    #
    # Would create an entry point called django-flipbook that calls django-flipbook.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - django-filer >=0.9.5
    - django-summernote >=0.5.3

  run:
    - python
    - django
    - django-filer >=0.9.5
    - django-summernote >=0.5.3

test:
  # Python imports
  imports:
    - flipbook
    - flipbook.migrations
    - flipbook.tests
    - flipbook.tests.test_app

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bitmazk/django-flipbook
  license: The MIT License
  summary: 'A reusable Django app that displays an image gallery as a flipbook.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
