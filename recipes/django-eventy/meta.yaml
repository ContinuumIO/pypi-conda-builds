package:
  name: django-eventy
  version: "0.1.1"

source:
  fn: django-eventy-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-eventy/django-eventy-0.1.1.tar.gz
  md5: 77f7881bd7d57fa154fb45253a0b4096
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-eventy = django-eventy:main
    #
    # Would create an entry point called django-eventy that calls django-eventy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-extensions

  run:
    - python
    - django-extensions

test:
  # Python imports
  imports:
    - eventy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/powellc/django-eventy
  license: BSD License
  summary: 'A really simple event manager with no recurring abilities for django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
