package:
  name: django-sendgrid-events
  version: "1.2"

source:
  fn: django-sendgrid-events-1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-sendgrid-events/django-sendgrid-events-1.2.tar.gz
  md5: eccd86b37105c76f436b7ef65dfbc537
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-sendgrid-events = django-sendgrid-events:main
    #
    # Would create an entry point called django-sendgrid-events that calls django-sendgrid-events.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - sendgrid_events

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/eldarion/django-sendgrid-events
  license: BSD License
  summary: 'a django app for handling sendgrid batch events'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
