package:
  name: django-chartjs
  version: "1.0"

source:
  fn: django-chartjs-1.0.zip
  url: https://pypi.python.org/packages/source/d/django-chartjs/django-chartjs-1.0.zip
  md5: e9ce8d257ee847459f912da6ce46789e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-chartjs = django-chartjs:main
    #
    # Would create an entry point called django-chartjs that calls django-chartjs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six

  run:
    - python
    - six

test:
  # Python imports
  imports:
    - chartjs
    - chartjs.views

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/novapost/django-chartjs
  license: BSD License
  summary: u'Django Chart.js and Hightchart ajax views'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
