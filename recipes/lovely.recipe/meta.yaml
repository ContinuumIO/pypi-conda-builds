package:
  name: lovely.recipe
  version: "1.0.0"

source:
  fn: lovely.recipe-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/l/lovely.recipe/lovely.recipe-1.0.0.tar.gz
  md5: b80237fce6613f4bd92210e3cf8cd1ad
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lovely.recipe = lovely.recipe:main
    #
    # Would create an entry point called lovely.recipe that calls lovely.recipe.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - zc.recipe.egg

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - zc.recipe.egg

test:
  # Python imports
  imports:
    - lovely
    - lovely.recipe
    - lovely.recipe.egg
    - lovely.recipe.fs
    - lovely.recipe.i18n
    - lovely.recipe.importchecker
    - lovely.recipe.zope

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://launchpad.net/lovely.recipe
  license: ZPL 2.1
  summary: 'Set of helper recipies for zc.buildout'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
