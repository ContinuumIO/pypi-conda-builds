package:
  name: hokuyo-python-lib
  version: "1.3"

source:
  fn: hokuyo-python-lib-1.3.tar.gz
  url: https://pypi.python.org/packages/source/h/hokuyo-python-lib/hokuyo-python-lib-1.3.tar.gz
  md5: 01fc94c832efbb844d8ed18c9781b785
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hokuyo-python-lib = hokuyo-python-lib:main
    #
    # Would create an entry point called hokuyo-python-lib that calls hokuyo-python-lib.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyserial
    - protobuf

  run:
    - python
    - pyserial
    - protobuf

test:
  # Python imports
  imports:
    - hokuyo
    - hokuyo.driver
    - hokuyo.tests
    - hokuyo.tools

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://project-capo.github.io/
  license: UNKNOWN
  summary: 'Hokuyo driver in python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
