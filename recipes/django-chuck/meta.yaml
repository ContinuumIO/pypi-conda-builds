package:
  name: django-chuck
  version: "0.2.3"

source:
  fn: django-chuck-0.2.3.tar.gz
  url: https://pypi.python.org/packages/source/D/Django-Chuck/django-chuck-0.2.3.tar.gz
  md5: 331dcb222ce89a36a9ed1ae5f77d676d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-chuck = django-chuck:main
    #
    # Would create an entry point called django-chuck that calls django-chuck.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - django_chuck
    - django_chuck.base
    - django_chuck.commands
    - django_chuck.template
    - django_chuck.template.notch_interactive

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/notch-interactive/django-chuck
  license: django-chunkator
  summary: 'Your powerful project punch button'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
