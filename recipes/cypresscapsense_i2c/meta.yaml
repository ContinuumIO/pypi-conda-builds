package:
  name: cypresscapsense_i2c
  version: "0.1.0"

source:
  fn: CypressCapsense_I2C-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/C/CypressCapsense_I2C/CypressCapsense_I2C-0.1.0.tar.gz
  md5: b73c09eb13473b9abdd0f60816d6fdde
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cypresscapsense_i2c = cypresscapsense_i2c:main
    #
    # Would create an entry point called cypresscapsense_i2c that calls cypresscapsense_i2c.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - cypresscapsense_i2c

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/voidptr/CypressCapsense/
  license: GPL
  summary: 'Python module for communicating with Cypress Capsense CY8C201xx capacitive touch sensors over I2C'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
