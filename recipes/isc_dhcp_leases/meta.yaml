package:
  name: isc_dhcp_leases
  version: "0.2.1"

source:
  fn: isc_dhcp_leases-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/i/isc_dhcp_leases/isc_dhcp_leases-0.2.1.tar.gz
  md5: 4079d40e8e880da2c16946d0e287f39d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - isc_dhcp_leases = isc_dhcp_leases:main
    #
    # Would create an entry point called isc_dhcp_leases that calls isc_dhcp_leases.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - isc_dhcp_leases

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/MartijnBraam/python-isc-dhcp-leases
  license: MIT
  summary: 'Small python module for reading /var/lib/dhcp/dhcpd.leases from isc-dhcp-server'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
