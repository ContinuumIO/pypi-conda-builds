package:
  name: cpc_api
  version: "0.1.5"

source:
  fn: cpc_api-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/c/cpc_api/cpc_api-0.1.5.tar.gz
  md5: 23f79448d62061e841c0fc82d09b9577
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cpc_api = cpc_api:main
    #
    # Would create an entry point called cpc_api that calls cpc_api.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests
    - fuzzywuzzy

  run:
    - python
    - requests
    - fuzzywuzzy

test:
  # Python imports
  imports:
    - cpc_api

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/regardscitoyens/cpc-api
  license: MIT License
  summary: 'Python api for nosdeputes.fr and nossenateurs.fr'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
