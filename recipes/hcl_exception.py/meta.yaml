package:
  name: hcl_exception.py
  version: "1.0.0"

source:
  fn: hcl_exception.py-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/h/hcl_exception.py/hcl_exception.py-1.0.0.tar.gz
  md5: 232e8002dae38d7acdaa23e79b61893e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hcl_exception.py = hcl_exception.py:main
    #
    # Would create an entry point called hcl_exception.py that calls hcl_exception.py.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/
  license: UNKNOWN
  summary: 'char data.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
