package:
  name: plomino
  version: "1.13.4"

source:
  fn: Plomino-1.13.4.tar.gz
  url: https://pypi.python.org/packages/source/P/Plomino/Plomino-1.13.4.tar.gz
  md5: 5c08b1777cdffcb3606ce0f01293ae12
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plomino = plomino:main
    #
    # Would create an entry point called plomino that calls plomino.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.cmfplomino

  run:
    - python
    - setuptools
    - setuptools
    - products.cmfplomino

test:
  # Python imports
  imports:
    - plomino

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.plomino.net
  license: ZPL
  summary: 'Create specific applications in Plone without developing. Created by Makina Corpus.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
