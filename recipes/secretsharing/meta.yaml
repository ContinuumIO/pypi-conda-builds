package:
  name: secretsharing
  version: "0.2.6"

source:
  fn: secretsharing-0.2.6.tar.gz
  url: https://pypi.python.org/packages/source/s/secretsharing/secretsharing-0.2.6.tar.gz
  md5: 8867776cc12970c56becb8a81df2d930
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - secretsharing = secretsharing:main
    #
    # Would create an entry point called secretsharing that calls secretsharing.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - secretsharing

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rxl/secret-sharing
  license: MIT License
  summary: "Tools for sharing secrets (like Bitcoin private keys), using shamir's secret sharing scheme."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
