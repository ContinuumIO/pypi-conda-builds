package:
  name: django-unique-submission
  version: "0.1"

source:
  fn: django-unique-submission-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-unique-submission/django-unique-submission-0.1.tar.gz
  md5: e3f6cda643d8de6ad35fd8b63f96214c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-unique-submission = django-unique-submission:main
    #
    # Would create an entry point called django-unique-submission that calls django-unique-submission.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4

  run:
    - python
    - django >=1.4

test:
  # Python imports
  imports:
    - django_unique_submission

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mengzhuo/django-unique-submission
  license: BSD License
  summary: 'Django unique submission preventing duplicate post data'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
