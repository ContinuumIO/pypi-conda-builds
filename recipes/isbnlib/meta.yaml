package:
  name: isbnlib
  version: "3.5.5"

source:
  fn: isbnlib-3.5.5.tar.gz
  url: https://pypi.python.org/packages/source/i/isbnlib/isbnlib-3.5.5.tar.gz
  md5: 21f03dbb41ab9096a87b1f11fdeffd15
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - isbnlib = isbnlib:main
    #
    # Would create an entry point called isbnlib that calls isbnlib.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - isbnlib
    - isbnlib/_data
    - isbnlib/dev
    - isbnlib/test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/xlcnd/isbnlib
  license: GNU Lesser General Public License v3 (LGPLv3)
  summary: 'Extract, clean, transform, hyphenate and metadata for ISBNs (International Standard Book Number).'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
