package:
  name: flask-identityclient
  version: "0.5.2"

source:
  fn: Flask-IdentityClient-0.5.2.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-IdentityClient/Flask-IdentityClient-0.5.2.tar.gz
  md5: 7544c46475a43e88b2a06ff0e2b844f7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-identityclient = flask-identityclient:main
    #
    # Would create an entry point called flask-identityclient that calls flask-identityclient.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - httplib2 >=0.8
    - requests >=1.2.3
    - oauth2 ==1.5.211
    - blinker >=1.3
    - flask >=0.10.1
    - flask-oauth >=0.11

  run:
    - python
    - httplib2 >=0.8
    - requests >=1.2.3
    - oauth2 ==1.5.211
    - blinker >=1.3
    - flask >=0.10.1
    - flask-oauth >=0.11

test:
  # Python imports
  imports:
    - flask_identity_client

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/myfreecomm/Flask-IdentityClient
  license: BSD License
  summary: 'PassaporteWeb connection for Flask applications'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
