package:
  name: cmdtool
  version: "0.2"

source:
  fn: cmdtool-0.2.tar.gz
  url: https://pypi.python.org/packages/source/c/cmdtool/cmdtool-0.2.tar.gz
  md5: 7f494e5581e6ea912a24bbceff0fb690
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cmdtool = cmdtool:main
    #
    # Would create an entry point called cmdtool that calls cmdtool.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - cmdtool

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ConnorDillon/cmdtool
  license: GPLv3
  summary: 'A very basic framework for writing command line tools'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
