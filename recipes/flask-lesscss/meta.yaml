package:
  name: flask-lesscss
  version: "0.9.1"

source:
  fn: flask-lesscss-0.9.1.tar.gz
  url: https://pypi.python.org/packages/source/f/flask-lesscss/flask-lesscss-0.9.1.tar.gz
  md5: 502640ef1afd16ed50892aba3370e88d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-lesscss = flask-lesscss:main
    #
    # Would create an entry point called flask-lesscss that calls flask-lesscss.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask

  run:
    - python
    - flask

test:
  # Python imports
  imports:
    - flaskext

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://sjl.bitbucket.org/flask-lesscss/
  license: MIT License
  summary: 'A small Flask extension that adds LessCSS support to Flask.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
