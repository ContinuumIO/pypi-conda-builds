package:
  name: flake8_doubles
  version: "0.3"

source:
  fn: flake8_doubles-0.3.tar.gz
  url: https://pypi.python.org/packages/source/f/flake8_doubles/flake8_doubles-0.3.tar.gz
  md5: 7ecf3689582bde2bb22d0b7da3d0b06f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flake8_doubles = flake8_doubles:main
    #
    # Would create an entry point called flake8_doubles that calls flake8_doubles.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - flake8_doubles

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jiazou/flake8-doubles
  license: MIT License
  summary: 'flake8 doubles checker, ensures mock.patch is not used'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
