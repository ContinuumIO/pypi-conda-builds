package:
  name: googlecalendar.py
  version: "0.3"

source:
  fn: calendarhelper-0.3.tar
  url: http://google-calendar-helper.googlecode.com/files/calendarhelper-0.3.tar
  #md5: 
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - googlecalendar.py = googlecalendar.py:main
    #
    # Would create an entry point called googlecalendar.py that calls googlecalendar.py.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - gcalendar

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/google-calendar-helper
  license: GNU General Public License (GPL)
  summary: 'Helper for Google Calendar API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
