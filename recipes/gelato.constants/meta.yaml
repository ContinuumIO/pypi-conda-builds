package:
  name: gelato.constants
  version: "0.1.4"

source:
  fn: gelato.constants-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/g/gelato.constants/gelato.constants-0.1.4.tar.gz
  md5: b9a04e26b94be9ad41a62565a55d7b1b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gelato.constants = gelato.constants:main
    #
    # Would create an entry point called gelato.constants that calls gelato.constants.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - tower

  run:
    - python
    - django
    - tower

test:
  # Python imports
  imports:
    - gelato
    - gelato.constants

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: UNKNOWN
  summary: 'Gelato constants'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
