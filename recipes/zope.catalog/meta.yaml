package:
  name: zope.catalog
  version: "4.0.0"

source:
  fn: zope.catalog-4.0.0.tar.gz
  url: https://pypi.python.org/packages/source/z/zope.catalog/zope.catalog-4.0.0.tar.gz
  md5: 4a0e12082575a97cda3d17136f1fdafc
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zope.catalog = zope.catalog:main
    #
    # Would create an entry point called zope.catalog that calls zope.catalog.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - persistent
    - btrees
    - zope.annotation
    - zope.intid
    - zope.component
    - zope.container
    - zope.index >=3.5.0
    - zope.interface
    - zope.lifecycleevent
    - zope.location
    - zope.schema

  run:
    - python
    - setuptools
    - persistent
    - btrees
    - zope.annotation
    - zope.intid
    - zope.component
    - zope.container
    - zope.index >=3.5.0
    - zope.interface
    - zope.lifecycleevent
    - zope.location
    - zope.schema

test:
  # Python imports
  imports:
    - zope
    - zope.catalog

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/zope.catalog
  license: Zope Public License
  summary: 'Cataloging and Indexing Framework for the Zope Toolkit'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
