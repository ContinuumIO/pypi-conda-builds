package:
  name: secureconfig
  version: "0.1.1.0"

source:
  fn: secureconfig-0.1.1.0.tar.gz
  url: https://pypi.python.org/packages/source/s/secureconfig/secureconfig-0.1.1.0.tar.gz
  md5: 323a5f0b2f33bcc98e052c6a322b3005
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - secureconfig = secureconfig:main
    #
    # Would create an entry point called secureconfig that calls secureconfig.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cryptography
    - configparser

  run:
    - python
    - cryptography
    - configparser

test:
  # Python imports
  imports:
    - secureconfig

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/nthmost/python-secureconfig
  license: MIT
  summary: 'Configuration-oriented encryption toolkit to make secure config files simple'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
