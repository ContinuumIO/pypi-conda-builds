package:
  name: gilescmd
  version: "0.2.1"

source:
  fn: gilescmd-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/g/gilescmd/gilescmd-0.2.1.tar.gz
  md5: c24ccb0706bd935c31a7cf284539b95f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gilescmd = gilescmd:main
    #
    # Would create an entry point called gilescmd that calls gilescmd.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - argparse
    - pandas
    - pymongo
    - smap

  run:
    - python
    - argparse
    - pandas
    - pymongo
    - smap

test:
  # Python imports
  imports:
    - gilescmd

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/gtfierro/giles
  license: gillcup
  summary: 'Command-line utility accompanying the Giles sMAP archiver'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
