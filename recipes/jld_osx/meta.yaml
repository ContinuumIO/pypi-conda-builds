package:
  name: jld_osx
  version: "0.2"

source:
  fn: jld_osx-0.2.tar.gz
  url: https://pypi.python.org/packages/source/j/jld_osx/jld_osx-0.2.tar.gz
  md5: 80c260f32a78aafaa8d907d87ec24698
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jld_osx = jld_osx:main
    #
    # Would create an entry point called jld_osx that calls jld_osx.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - appscript >=0.21
    - jld_scripts >=0.6

  run:
    - python
    - appscript >=0.21
    - jld_scripts >=0.6

test:
  # Python imports
  imports:
    - jld_osx
    - jld_osx.itunes
    - jld_osx.scripts

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.systemical.com/doc/opensource/jld_osx
  license: UNKNOWN
  summary: 'Collection of python utility scripts for OSX'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
