package:
  name: django-elastic-transcoder
  version: "0.9.2"

source:
  fn: django-elastic-transcoder-0.9.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-elastic-transcoder/django-elastic-transcoder-0.9.2.tar.gz
  md5: f1607708a56bb55832e336e1d16a6bd1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-elastic-transcoder = django-elastic-transcoder:main
    #
    # Would create an entry point called django-elastic-transcoder that calls django-elastic-transcoder.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4
    - boto >=2.5
    - south >=0.8

  run:
    - python
    - django >=1.4
    - boto >=2.5
    - south >=0.8

test:
  # Python imports
  imports:
    - dj_elastictranscoder
    - dj_elastictranscoder.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/StreetVoice/django-elastic-transcoder
  license: MIT
  summary: 'Django with AWS elastic transcoder'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
