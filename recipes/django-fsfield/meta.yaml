package:
  name: django-fsfield
  version: "0.5.3"

source:
  fn: django-fsfield-0.5.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-fsfield/django-fsfield-0.5.3.tar.gz
  md5: 119a8cd42788f7812df6bd10c1ba63b5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-fsfield = django-fsfield:main
    #
    # Would create an entry point called django-fsfield that calls django-fsfield.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - fsfield
    - fsfield.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: BSD License
  summary: 'Scalabe file storage fields for your Django models'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
