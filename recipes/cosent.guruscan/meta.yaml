package:
  name: cosent.guruscan
  version: "1.0dev"

source:
  fn: cosent.guruscan-1.0dev.zip
  url: https://pypi.python.org/packages/source/c/cosent.guruscan/cosent.guruscan-1.0dev.zip
  md5: 6521ae2bca6c4c766b845a8450eb347c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cosent.guruscan = cosent.guruscan:main
    #
    # Would create an entry point called cosent.guruscan that calls cosent.guruscan.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - cosent
    - cosent.guruscan
    - cosent.guruscan.Extensions
    - cosent.guruscan.browser
    - cosent.guruscan.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/cosent/cosent.guruscan
  license: gpl
  summary: 'Plone integration for GuruScan'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
