package:
  name: django-breadcrumb-trail
  version: "0.9.0"

source:
  fn: django-breadcrumb-trail-0.9.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-breadcrumb-trail/django-breadcrumb-trail-0.9.0.tar.gz
  md5: 09824a0c9936521abc7a235ed8e8e6cd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-breadcrumb-trail = django-breadcrumb-trail:main
    #
    # Would create an entry point called django-breadcrumb-trail that calls django-breadcrumb-trail.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - breadcrumb_trail
    - breadcrumb_trail/templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/tomchristie/django-breadcrumb-trail/
  license: BSD
  summary: 'Drop dead simple breadcrumb trails for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
