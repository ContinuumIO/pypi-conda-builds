package:
  name: products.groupuserfolder
  version: "3.55.1"

source:
  fn: Products.GroupUserFolder-3.55.1.tar.gz
  url: https://pypi.python.org/packages/source/P/Products.GroupUserFolder/Products.GroupUserFolder-3.55.1.tar.gz
  md5: 4ae9664c63c7e2496004638004ad8887
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - products.groupuserfolder = products.groupuserfolder:main
    #
    # Would create an entry point called products.groupuserfolder that calls products.groupuserfolder.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - Products
    - Products.GroupUserFolder
    - Products.GroupUserFolder.Extensions
    - Products.GroupUserFolder.interfaces
    - Products.GroupUserFolder.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/Products.GroupUserFolder
  license: GPL
  summary: 'Acl Users Folder with group and plugin support'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
