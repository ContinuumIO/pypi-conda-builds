package:
  name: flatdict
  version: "1.1.3"

source:
  fn: flatdict-1.1.3.tar.gz
  url: https://pypi.python.org/packages/source/f/flatdict/flatdict-1.1.3.tar.gz
  md5: b085f187c52ae9d8f57630c2d6a0dabb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flatdict = flatdict:main
    #
    # Would create an entry point called flatdict that calls flatdict.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - flatdict

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/gmr/flatdict
  license: BSD License
  summary: 'Python module for interacting with nested dicts as a single level dict with delimited keys.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
