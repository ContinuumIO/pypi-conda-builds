package:
  name: django-garage
  version: "0.1.13"

source:
  fn: django-garage-0.1.13.tar.gz
  url: https://pypi.python.org/packages/source/d/django-garage/django-garage-0.1.13.tar.gz
  md5: bb9b9261e4efb78cd5bed623e11a57a3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-garage = django-garage:main
    #
    # Would create an entry point called django-garage that calls django-garage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six
    - django
    - markdown
    - textile
    - pillow
    - pyyaml
    - pytz
    - mock

  run:
    - python
    - six
    - django
    - markdown
    - textile
    - pillow
    - pyyaml
    - pytz
    - mock

test:
  # Python imports
  imports:
    - garage
    - garage.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/kchan/django-garage
  license: BSD License
  summary: 'A collection of useful functions and modules for Django development'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
