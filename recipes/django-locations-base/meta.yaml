package:
  name: django-locations-base
  version: "0.2.4"

source:
  fn: django-locations-base-0.2.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-locations-base/django-locations-base-0.2.4.tar.gz
  md5: 432b10b32f75308f7f01549411f8771d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-locations-base = django-locations-base:main
    #
    # Would create an entry point called django-locations-base that calls django-locations-base.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-localflavor >=1.1,<2.0
    - geopy >=1.9.1,<2.0

  run:
    - python
    - django-localflavor >=1.1,<2.0
    - geopy >=1.9.1,<2.0

test:
  # Python imports
  imports:
    - locations
    - locations.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/dstegelman/django-locations
  license: MIT License
  summary: 'A simple locations app for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
