package:
  name: torpedo
  version: "0.0.2"

source:
  fn: torpedo-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/t/torpedo/torpedo-0.0.2.tar.gz
  md5: f7fb1b153ee2a6507b4f03fcef3ce522
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - torpedo = torpedo:main
    #
    # Would create an entry point called torpedo that calls torpedo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requesocks
    - selenium

  run:
    - python
    - requesocks
    - selenium

test:
  # Python imports
  imports:
    - torpedo

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/abelsonlive/torpedo
  license: MIT License
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
