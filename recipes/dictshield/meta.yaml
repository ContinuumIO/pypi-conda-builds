package:
  name: dictshield
  version: "0.4.4"

source:
  fn: dictshield-0.4.4.tar.gz
  url: https://pypi.python.org/packages/source/d/dictshield/dictshield-0.4.4.tar.gz
  md5: 2445d8bf27e59e756aa1ff4e9bf43a76
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dictshield = dictshield:main
    #
    # Would create an entry point called dictshield that calls dictshield.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - dictshield
    - dictshield.fields

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/j2labs/dictshield
  license: BSD
  summary: 'Python Library for typed dictionaries... sorta'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
