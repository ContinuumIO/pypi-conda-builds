package:
  name: django-tastypie-oauth
  version: "0.0.3"

source:
  fn: django-tastypie-oauth-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-tastypie-oauth/django-tastypie-oauth-0.0.3.tar.gz
  md5: 586175d99f079ba130edab7ede40115f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-tastypie-oauth = django-tastypie-oauth:main
    #
    # Would create an entry point called django-tastypie-oauth that calls django-tastypie-oauth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django_tastypie >=0.9.16,<0.13
    - django-oauth-toolkit >=0.7,<0.8
    - python-mimeparse >=0.1,<0.2

  run:
    - python
    - setuptools
    - django_tastypie >=0.9.16,<0.13
    - django-oauth-toolkit >=0.7,<0.8
    - python-mimeparse >=0.1,<0.2

test:
  # Python imports
  imports:
    - tastypie_oauth

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/orcasgit/django-tastypie-oauth
  license: Apache Software License
  summary: 'Providing OAuth services for Tastypie APIs'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
