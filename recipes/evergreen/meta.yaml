package:
  name: evergreen
  version: "0.1.0"

source:
  fn: evergreen-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/e/evergreen/evergreen-0.1.0.tar.gz
  md5: 2215208e43182f92268c2a71a3369387
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - evergreen = evergreen:main
    #
    # Would create an entry point called evergreen that calls evergreen.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fibers >=0.1.0
    - pyuv >=0.10.4

  run:
    - python
    - fibers >=0.1.0
    - pyuv >=0.10.4

test:
  # Python imports
  imports:
    - evergreen
    - evergreen.core
    - evergreen.ext
    - evergreen.futures
    - evergreen.io
    - evergreen.lib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/saghul/evergreen
  license: MIT License
  summary: 'Cooperative multitasking and i/o for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
