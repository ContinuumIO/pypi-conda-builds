package:
  name: mr.importer
  version: "1.0a5"

source:
  fn: mr.importer-1.0a5.zip
  url: https://pypi.python.org/packages/source/m/mr.importer/mr.importer-1.0a5.zip
  md5: 03b31f87cf14a88a11774f83f027cf68
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mr.importer = mr.importer:main
    #
    # Would create an entry point called mr.importer that calls mr.importer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - beautifulsoup
    - lxml
    - xlrd
    - zc.buildout

  run:
    - python
    - setuptools
    - beautifulsoup
    - lxml
    - xlrd
    - zc.buildout

test:
  # Python imports
  imports:
    - mr
    - mr.importer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/parse2plone
  license: UNKNOWN
  summary: 'Easily import static HTML websites on the file system into Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
