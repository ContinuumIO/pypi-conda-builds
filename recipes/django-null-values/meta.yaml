package:
  name: django-null-values
  version: "0.1"

source:
  fn: django-null-values-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-null-values/django-null-values-0.1.tar.gz
  md5: bf7e38203cd588c85d646b7bcdc34836
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-null-values = django-null-values:main
    #
    # Would create an entry point called django-null-values that calls django-null-values.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - null_values

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://hg.schinckel.net/django-null-values/
  license: UNKNOWN
  summary: 'Automatically convert empty strings to NULL if null=True'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
