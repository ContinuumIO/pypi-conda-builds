package:
  name: cubicweb-leaflet
  version: "0.2.1"

source:
  fn: cubicweb-leaflet-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/c/cubicweb-leaflet/cubicweb-leaflet-0.2.1.tar.gz
  md5: 0196ac6ce7340f98e6c16cdd21fe9d25
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cubicweb-leaflet = cubicweb-leaflet:main
    #
    # Would create an entry point called cubicweb-leaflet that calls cubicweb-leaflet.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cubicweb >=3.17.14

  run:
    - python
    - cubicweb >=3.17.14

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.cubicweb.org/project/cubicweb-leaflet
  license: LGPL
  summary: 'Cube for leaflet map, see http://leafletjs.com/'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
