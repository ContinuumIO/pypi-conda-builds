package:
  name: jdatetime
  version: "1.5"

source:
  fn: jdatetime-1.5.tar.gz
  url: https://pypi.python.org/packages/source/j/jdatetime/jdatetime-1.5.tar.gz
  md5: 62c2627efc802b0559dba08972fa0447
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jdatetime = jdatetime:main
    #
    # Would create an entry point called jdatetime that calls jdatetime.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - jdatetime

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/slashmili/python-jalali
  license: Python Software Foundation License
  summary: 'Jalali Date support for Django 1.7 model and admin interface'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
