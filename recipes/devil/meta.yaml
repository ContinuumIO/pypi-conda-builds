package:
  name: devil
  version: "0.9"

source:
  fn: devil-0.9.tar.gz
  url: https://pypi.python.org/packages/source/d/devil/devil-0.9.tar.gz
  md5: 0bdf5169ded2c8fa1ce59474905fe0a0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - devil = devil:main
    #
    # Would create an entry point called devil that calls devil.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - simplejson >=2.1.0
    - django >=1.3.0

  run:
    - python
    - simplejson >=2.1.0
    - django >=1.3.0

test:
  # Python imports
  imports:
    - devil
    - devil.fields
    - devil.mappers
    - devil.perm

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/wuher/devil/
  license: MIT
  summary: 'Simple REST framework for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
