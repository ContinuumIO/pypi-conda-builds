package:
  name: goprohero
  version: "0.2.6"

source:
  fn: goprohero-0.2.6.tar.gz
  url: https://pypi.python.org/packages/source/g/goprohero/goprohero-0.2.6.tar.gz
  md5: 7a251f019f1afb93f7764cbf0895894e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - goprohero = goprohero:main
    #
    # Would create an entry point called goprohero that calls goprohero.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pillow
    - wireless
    - colorama

  run:
    - python
    - pillow
    - wireless
    - colorama

test:
  # Python imports
  imports:
    - goprohero

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/joshvillbrandt/gopro
  license: go-proxy-client
  summary: 'A Python library for controlling GoPro cameras over http.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
