package:
  name: gocept.munin
  version: "0.1"

source:
  fn: gocept.munin-0.1.tar.gz
  url: https://pypi.python.org/packages/source/g/gocept.munin/gocept.munin-0.1.tar.gz
  md5: 4cfc78e7c8f71a7f0aa77cb587c039c8
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gocept.munin = gocept.munin:main
    #
    # Would create an entry point called gocept.munin that calls gocept.munin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - gocept
    - gocept.munin

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.python.org/pypi/gocept.munin
  license: ZPL 2.1
  summary: 'Utilities for writing munin plugins.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
