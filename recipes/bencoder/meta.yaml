package:
  name: bencoder
  version: "0.2.0"

source:
  fn: bencoder-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/b/bencoder/bencoder-0.2.0.tar.gz
  md5: ec695d96ebf0bccbfacb5c18b801b493
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bencoder = bencoder:main
    #
    # Would create an entry point called bencoder that calls bencoder.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - bencoder

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/utdemir/bencoder
  license: GNU General Public License (GPL)
  summary: 'A simple bencode decoder/encoder library in pure Python.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
