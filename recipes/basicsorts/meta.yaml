package:
  name: basicsorts
  version: "1.0"

source:
  fn: basicsorts-1.0.tar.gz
  url: https://pypi.python.org/packages/source/b/basicsorts/basicsorts-1.0.tar.gz
  md5: 6670f1c6968795ea3455ba7e836ec61a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - basicsorts = basicsorts:main
    #
    # Would create an entry point called basicsorts that calls basicsorts.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - basicsorts

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/SamyMe/basicsorts/
  license: MIT License
  summary: 'Basic sorting algorithmes for arrays'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
