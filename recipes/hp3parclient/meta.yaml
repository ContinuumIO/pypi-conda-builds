package:
  name: hp3parclient
  version: "3.2.0"

source:
  fn: hp3parclient-3.2.0.tar.gz
  url: https://pypi.python.org/packages/source/h/hp3parclient/hp3parclient-3.2.0.tar.gz
  md5: f4cc346281ae86c21b5f975cc3b4d759
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hp3parclient = hp3parclient:main
    #
    # Would create an entry point called hp3parclient that calls hp3parclient.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - httplib2 >=0.6.0
    - paramiko
    - eventlet

  run:
    - python
    - httplib2 >=0.6.0
    - paramiko
    - eventlet

test:
  # Python imports
  imports:
    - hp3parclient

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://packages.python.org/hp3parclient
  license: Apache Software License
  summary: 'HP 3PAR HTTP REST Client'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
