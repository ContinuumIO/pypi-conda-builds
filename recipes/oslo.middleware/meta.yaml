package:
  name: oslo.middleware
  version: "1.2.0"

source:
  fn: oslo.middleware-1.2.0.tar.gz
  url: https://pypi.python.org/packages/source/o/oslo.middleware/oslo.middleware-1.2.0.tar.gz
  md5: 2f03bdc82638947aa04fade7aeab264e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - oslo.middleware = oslo.middleware:main
    #
    # Would create an entry point called oslo.middleware that calls oslo.middleware.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Apache Software License
  summary: 'Oslo Middleware library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
