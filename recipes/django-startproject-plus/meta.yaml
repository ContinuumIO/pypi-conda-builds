package:
  name: django-startproject-plus
  version: "0.1b.1"

source:
  fn: django-startproject-plus-0.1b.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-startproject-plus/django-startproject-plus-0.1b.1.tar.gz
  md5: b0da6e199f7318567c0807464ecf5722
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-startproject-plus = django-startproject-plus:main
    #
    # Would create an entry point called django-startproject-plus that calls django-startproject-plus.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/alfredo/django-startproject-plus
  license: BSD License
  summary: 'Django superset startproject command for creating new projects.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
