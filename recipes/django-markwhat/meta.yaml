package:
  name: django-markwhat
  version: "1.4"

source:
  fn: django-markwhat-1.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-markwhat/django-markwhat-1.4.tar.gz
  md5: 7eb84409195293c30f1162f4d7f9fe6e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-markwhat = django-markwhat:main
    #
    # Would create an entry point called django-markwhat that calls django-markwhat.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - django_markwhat
    - django_markwhat.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/django-markwhat
  license: BSD License
  summary: 'A collection of template filters that implement\n     common markup languages.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
