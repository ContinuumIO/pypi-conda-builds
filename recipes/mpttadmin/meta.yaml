package:
  name: mpttadmin
  version: "0.3.1"

source:
  fn: mpttadmin-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/m/mpttadmin/mpttadmin-0.3.1.tar.gz
  md5: d9ac2421548483598554d144090265fb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mpttadmin = mpttadmin:main
    #
    # Would create an entry point called mpttadmin that calls mpttadmin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - mpttadmin

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.tabed.org/mptt_admin
  license: BSD
  summary: 'jstree admin for mptt models'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
