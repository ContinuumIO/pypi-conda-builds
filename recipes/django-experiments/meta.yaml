package:
  name: django-experiments
  version: "0.3.5"

source:
  fn: django-experiments-0.3.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-experiments/django-experiments-0.3.5.tar.gz
  md5: 2f117398ecf6b7a09aa4fcae87bf6c86
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-experiments = django-experiments:main
    #
    # Would create an entry point called django-experiments that calls django-experiments.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - redis >=2.4.9
    - django >=1.3.0
    - nexus >=0.2.3
    - jsonfield >=0.9
    - gargoyle >=0.9.0

  run:
    - python
    - redis >=2.4.9
    - django >=1.3.0
    - nexus >=0.2.3
    - jsonfield >=0.9
    - gargoyle >=0.9.0

test:
  # Python imports
  imports:
    - experiments
    - experiments.migrations
    - experiments.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mixcloud/django-experiments
  license: MIT license, see LICENSE file
  summary: 'Python Django AB Testing Framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
