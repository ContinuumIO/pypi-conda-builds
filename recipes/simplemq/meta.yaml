package:
  name: simplemq
  version: "1.0.1"

source:
  fn: SimpleMQ-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/S/SimpleMQ/SimpleMQ-1.0.1.tar.gz
  md5: c6af2d9c68b31fe290d9955d4ca8707a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - simplemq = simplemq:main
    #
    # Would create an entry point called simplemq that calls simplemq.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - simplemq

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/huangz1990/SimpleMQ
  license: UNKNOWN
  summary: 'An MQ(message queue) library use Redis as backend.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
