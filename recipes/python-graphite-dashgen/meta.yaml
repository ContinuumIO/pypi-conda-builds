package:
  name: python-graphite-dashgen
  version: "0.1.6"

source:
  fn: python-graphite-dashgen-0.1.6.tar.gz
  url: https://pypi.python.org/packages/source/p/python-graphite-dashgen/python-graphite-dashgen-0.1.6.tar.gz
  md5: a8acff8dafcb459a17168df96d2fd326
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-graphite-dashgen = python-graphite-dashgen:main
    #
    # Would create an entry point called python-graphite-dashgen that calls python-graphite-dashgen.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyyaml

  run:
    - python
    - pyyaml

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ClockworkNet/graphite-dashgen
  license: MIT License
  summary: 'Graphite-dashgen automates the creation of Graphite dashboards'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
