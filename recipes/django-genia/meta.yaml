package:
  name: django-genia
  version: "0.1.dev1"

source:
  fn: django-genia-0.1.dev1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-genia/django-genia-0.1.dev1.tar.gz
  md5: 2158975dad0eda9b223cf82b41b08196
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-genia = django-genia:main
    #
    # Would create an entry point called django-genia that calls django-genia.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.2

  run:
    - python
    - django >=1.2

test:
  # Python imports
  imports:
    - genia

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tmitchell/django-genia
  license: BSD License
  summary: 'A pluggable Django app for managing generations of data'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
