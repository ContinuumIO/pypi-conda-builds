package:
  name: fullhistory
  version: "0.3.1"

source:
  fn: fullhistory-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/f/fullhistory/fullhistory-0.3.1.tar.gz
  md5: 164045d23df7baacc8b1318459349a29
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fullhistory = fullhistory:main
    #
    # Would create an entry point called fullhistory that calls fullhistory.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - fullhistory
    - fullhistory.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/cuker/django-fullhistory
  license: UNKNOWN
  summary: 'Fullhistory for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
