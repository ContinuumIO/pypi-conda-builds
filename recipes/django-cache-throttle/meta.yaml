package:
  name: django-cache-throttle
  version: "2012.11.09.3"

source:
  fn: django-cache-throttle-2012.11.09.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-cache-throttle/django-cache-throttle-2012.11.09.3.tar.gz
  md5: 330f05f82e60bc3b1f84e7d4b092e58b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-cache-throttle = django-cache-throttle:main
    #
    # Would create an entry point called django-cache-throttle that calls django-cache-throttle.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - cache_throttle
    - sample_project

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bradbeattie/django-cache-throttle
  license: BSD License
  summary: 'Cache-based rate-limiting for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
