package:
  name: gitolite-manager
  version: "0.0.0"

source:
  fn: gitolite-manager-0.0.0.tar.gz
  url: https://github.com/downloads/smailq/gitolite-manager/gitolite-manager-0.0.0.tar.gz
  #md5: 
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gitolite-manager = gitolite-manager:main
    #
    # Would create an entry point called gitolite-manager that calls gitolite-manager.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - gitolite_manager

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/smailq/gitolite-manager
  license: MIT License
  summary: "Manage gitolite's configuration files and ssh keys"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
