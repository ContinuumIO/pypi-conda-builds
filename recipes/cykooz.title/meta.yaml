package:
  name: cykooz.title
  version: "1.0.0"

source:
  fn: cykooz.title-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/c/cykooz.title/cykooz.title-1.0.0.tar.gz
  md5: 02fb025a2bf9304f3367987973ba54ce
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cykooz.title = cykooz.title:main
    #
    # Would create an entry point called cykooz.title that calls cykooz.title.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - distribute
    - zope.component
    - zope.container
    - zope.interface
    - zope.dublincore
    - zope.i18n
    - zope.i18nmessageid

  run:
    - python
    - distribute
    - zope.component
    - zope.container
    - zope.interface
    - zope.dublincore
    - zope.i18n
    - zope.i18nmessageid

test:
  # Python imports
  imports:
    - cykooz
    - cykooz.title

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/cykooz/cykooz.title
  license: Zope Public License
  summary: 'Adapters for adapting any object to ITitle interface.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
