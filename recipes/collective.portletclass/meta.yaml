package:
  name: collective.portletclass
  version: "1.0"

source:
  fn: collective.portletclass-1.0.zip
  url: https://pypi.python.org/packages/source/c/collective.portletclass/collective.portletclass-1.0.zip
  md5: 021c7648ccc7b2424507c1f096b9deb7
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.portletclass = collective.portletclass:main
    #
    # Would create an entry point called collective.portletclass that calls collective.portletclass.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - collective.monkeypatcher
    - plone.app.portlets
    - plone.browserlayer
    - setuptools
    - z3c.jbot
    - zope.component
    - zope.event
    - zope.formlib
    - zope.i18nmessageid
    - zope.interface
    - zope.lifecycleevent
    - zope.schema

  run:
    - python
    - setuptools
    - collective.monkeypatcher
    - plone.app.portlets
    - plone.browserlayer
    - setuptools
    - z3c.jbot
    - zope.component
    - zope.event
    - zope.formlib
    - zope.i18nmessageid
    - zope.interface
    - zope.lifecycleevent
    - zope.schema

test:
  # Python imports
  imports:
    - collective
    - collective.portletclass

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/shuttlethread/collective.portletclass
  license: GNU General Public License (GPL)
  summary: 'Add custom CSS classes to Plone portlet wrappers for flexible theming.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
