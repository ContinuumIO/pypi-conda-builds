package:
  name: django-registration-redux
  version: "1.1"

source:
  fn: django-registration-redux-1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-registration-redux/django-registration-redux-1.1.tar.gz
  md5: c7cc0e1b21684fb12dff4f3ae94d4900
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-registration-redux = django-registration-redux:main
    #
    # Would create an entry point called django-registration-redux that calls django-registration-redux.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - registration
    - registration.backends
    - registration.backends.default
    - registration.backends.simple
    - registration.management
    - registration.management.commands
    - registration.tests
    - test_app

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/macropin/django-registration
  license: BSD License
  summary: 'An extensible user-registration application for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
