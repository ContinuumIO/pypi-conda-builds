package:
  name: django-zxcvbn-password
  version: "1.0.5"

source:
  fn: django-zxcvbn-password-1.0.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-zxcvbn-password/django-zxcvbn-password-1.0.5.tar.gz
  md5: f44859a710f3ed4d86cc2f6acef88e7b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-zxcvbn-password = django-zxcvbn-password:main
    #
    # Would create an entry point called django-zxcvbn-password that calls django-zxcvbn-password.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - zxcvbn_password

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Pawamoy/django-zxcvbn-password
  license: BSD License
  summary: 'A front-end and back-end password validation field using ZXCVBN.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
