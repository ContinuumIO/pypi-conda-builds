package:
  name: kazoo-api
  version: "0.1.5"

source:
  fn: kazoo-api-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/k/kazoo-api/kazoo-api-0.1.5.tar.gz
  md5: a485e5f181d0478ea182ea571bbd5992
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kazoo-api = kazoo-api:main
    #
    # Would create an entry point called kazoo-api that calls kazoo-api.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=0.14,<1.0

  run:
    - python
    - requests >=0.14,<1.0

test:
  # Python imports
  imports:
    - kazoo

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://2600hz.com/platform.html
  license: MIT License
  summary: 'Wrapper for the Kazoo API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
