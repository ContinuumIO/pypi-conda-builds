package:
  name: grt
  version: "0.1"

source:
  fn: grt-0.1.tar.gz
  url: https://pypi.python.org/packages/source/g/grt/grt-0.1.tar.gz
  md5: bac6fa86140a8c25e477cedc5fc5ca4a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - grt = grt:main
    #
    # Would create an entry point called grt that calls grt.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - gitpython
    - paramiko

  run:
    - python
    - gitpython
    - paramiko

test:
  # Python imports
  imports:
    - grt
    - grt.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://guildencrantz.github.com/grt/
  license: Apache Software License
  summary: 'Gerrit Git Repository Helper'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
