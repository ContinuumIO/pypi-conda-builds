package:
  name: arcrest
  version: "10.2.2-beta"

source:
  fn: arcrest-10.2.2-beta.zip
  url: https://pypi.python.org/packages/source/a/arcrest/arcrest-10.2.2-beta.zip
  md5: a8588ec0646b1548f5bb2f5396481fc3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - arcrest = arcrest:main
    #
    # Would create an entry point called arcrest that calls arcrest.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - arcrest
    - arcrest.admin

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Apache Software License
  summary: 'Wrapper to the ArcGIS REST API, and a Python analogue to the Javascript APIs'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
