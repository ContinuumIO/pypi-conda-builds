package:
  name: flask-dropbox
  version: "0.3"

source:
  fn: Flask-Dropbox-0.3.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Dropbox/Flask-Dropbox-0.3.tar.gz
  md5: 1033c2302cb5c49e74bef6d248cc925d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-dropbox = flask-dropbox:main
    #
    # Would create an entry point called flask-dropbox that calls flask-dropbox.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - flask >=0.8
    - dropbox

  run:
    - python
    - flask >=0.8
    - dropbox

test:
  # Python imports
  imports:
    - flask_dropbox

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/playpauseandstop/Flask-Dropbox
  license: BSD License
  summary: 'Dropbox Python SDK support for Flask applications.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
