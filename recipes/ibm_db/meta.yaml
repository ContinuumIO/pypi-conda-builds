package:
  name: ibm_db
  version: "2.0.5.1"

source:
  fn: ibm_db-2.0.5.1.tar.gz
  url: https://pypi.python.org/packages/source/i/ibm_db/ibm_db-2.0.5.1.tar.gz
  md5: b6c084e0c796190cfc3c5f634b40e6c5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ibm_db = ibm_db:main
    #
    # Would create an entry point called ibm_db that calls ibm_db.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - clidriver
    - ibm_db
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/ibm_db/
  license: Apache Software License
  summary: 'Python DBI driver for DB2 (LUW, zOS, i5) and IDS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
