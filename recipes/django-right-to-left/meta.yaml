package:
  name: django-right-to-left
  version: "0.1.1"

source:
  fn: django-right-to-left-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-right-to-left/django-right-to-left-0.1.1.tar.gz
  md5: 46302fb3aeaf8eb54d98cf37871a581e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-right-to-left = django-right-to-left:main
    #
    # Would create an entry point called django-right-to-left that calls django-right-to-left.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - rtl

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/abbas123456/django-right-to-left
  license: BSD License
  summary: 'A Django template loader that looks for an alternative right to left version of a template file if the activated language is a right to left language such as Arabic or Hebrew.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
