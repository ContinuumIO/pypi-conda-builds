package:
  name: bioscons
  version: "0.8.1"

source:
  fn: bioscons-0.8.1.tar.gz
  url: https://pypi.python.org/packages/source/b/bioscons/bioscons-0.8.1.tar.gz
  md5: 7fbaa87e5ed17e5f536414cc64b93c45
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bioscons = bioscons:main
    #
    # Would create an entry point called bioscons that calls bioscons.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - bioscons

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/nhoffman/bioscons
  license: bioscripts.convert
  summary: 'Functions extending the scons build tool for reproducible research in bioinformatics.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
