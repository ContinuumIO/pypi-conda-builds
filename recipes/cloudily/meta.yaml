package:
  name: cloudily
  version: "0.1.4"

source:
  fn: cloudily-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/c/cloudily/cloudily-0.1.4.tar.gz
  md5: 64d916163416c844240f77f2df975d4e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cloudily = cloudily:main
    #
    # Would create an entry point called cloudily that calls cloudily.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - boto
    - paramiko

  run:
    - python
    - boto
    - paramiko

test:
  # Python imports
  imports:
    - cloudily

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://loads.pickle.me.uk/cloudily/
  license: MIT License
  summary: 'Command line tool to visualize your EC2 infrastructure'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
