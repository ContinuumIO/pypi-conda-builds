package:
  name: digitalocean_api_python
  version: "0.1.0"

source:
  fn: digitalocean_api_python-0.1.0.zip
  url: https://pypi.python.org/packages/source/d/digitalocean_api_python/digitalocean_api_python-0.1.0.zip
  md5: 1b94d6886d7fc9011393a9840de43980
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - digitalocean_api_python = digitalocean_api_python:main
    #
    # Would create an entry point called digitalocean_api_python that calls digitalocean_api_python.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - appier

  run:
    - python
    - appier

test:
  # Python imports
  imports:
    - digitalocean
    - digitalocean_api_python

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://digitalocean_api.hive.pt
  license: GNU General Public License (GPL)
  summary: 'DigitalOcean API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
