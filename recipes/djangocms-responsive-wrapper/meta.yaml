package:
  name: djangocms-responsive-wrapper
  version: "0.1.1"

source:
  fn: djangocms-responsive-wrapper-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/djangocms-responsive-wrapper/djangocms-responsive-wrapper-0.1.1.tar.gz
  md5: 15597562b3942c2ac899a85d256e07bd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangocms-responsive-wrapper = djangocms-responsive-wrapper:main
    #
    # Would create an entry point called djangocms-responsive-wrapper that calls djangocms-responsive-wrapper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-appconf
    - django-cms >=3.0
    - django-responsive2

  run:
    - python
    - django-appconf
    - django-cms >=3.0
    - django-responsive2

test:
  # Python imports
  imports:
    - responsive_wrapper

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mishbahr/djangocms-responsive-wrapper
  license: BSD License
  summary: 'This projects integrates django-responsive2 with django-cms >= 3.0'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
