package:
  name: janky_post
  version: "0.6"

source:
  fn: janky_post-0.6.tar.gz
  url: https://pypi.python.org/packages/source/j/janky_post/janky_post-0.6.tar.gz
  md5: e6ff7a4c875e0abe44586dbf5ca5457d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - janky_post = janky_post:main
    #
    # Would create an entry point called janky_post that calls janky_post.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - janky_post

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://saunter.org/janky.post/
  license: UNKNOWN
  summary: 'Makes janky cross-domain communication easy'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
