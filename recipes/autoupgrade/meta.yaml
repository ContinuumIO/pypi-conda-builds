package:
  name: autoupgrade
  version: "0.2.0"

source:
  fn: autoupgrade-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/a/autoupgrade/autoupgrade-0.2.0.tar.gz
  md5: 2d624612715aa45ed896408d5bcf380a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - autoupgrade = autoupgrade:main
    #
    # Would create an entry point called autoupgrade that calls autoupgrade.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - pip
    - beautifulsoup4

  run:
    - python
    - pip
    - beautifulsoup4

test:
  # Python imports
  imports:
    - autoupgrade

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/jorkar/autoupgrade
  license: MIT License
  summary: 'Automatic upgrade of python modules and packages'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
