package:
  name: flask-heroku
  version: "0.1.9"

source:
  fn: flask-heroku-0.1.9.tar.gz
  url: https://pypi.python.org/packages/source/f/flask-heroku/flask-heroku-0.1.9.tar.gz
  md5: 09362118f70c4b978b2552c2ef02d100
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-heroku = flask-heroku:main
    #
    # Would create an entry point called flask-heroku that calls flask-heroku.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask

  run:
    - python
    - flask

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kennethreitz/flask-heroku
  license: BSD License
  summary: 'Heroku environment variable configurations for Flask'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
