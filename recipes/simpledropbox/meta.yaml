package:
  name: simpledropbox
  version: "0.2.2"

source:
  fn: simpledropbox-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/s/simpledropbox/simpledropbox-0.2.2.tar.gz
  md5: e2027ddb2ca3d2075f21db3d16bcc74d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - simpledropbox = simpledropbox:main
    #
    # Would create an entry point called simpledropbox that calls simpledropbox.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - beautifulsoup >=3.0.0.0
    - pytz
    - poster >=0.6.0

  run:
    - python
    - beautifulsoup >=3.0.0.0
    - pytz
    - poster >=0.6.0

test:
  # Python imports
  imports:
    - simpledropbox

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://greyworld.net/en/projects/simpledropbox/
  license: Apache Software License
  summary: u'Small python library to interact with dropbox through its public web interface.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
