package:
  name: importformatter
  version: "0.1.3"

source:
  fn: importformatter-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/i/importformatter/importformatter-0.1.3.tar.gz
  md5: 8e3fdc764d9993595c29a49f73c1d627
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - importformatter = importformatter:main
    #
    # Would create an entry point called importformatter that calls importformatter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - importformatter

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'Groups, sorts, and formats import statements.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
