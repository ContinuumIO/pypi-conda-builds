package:
  name: dtmf-detector
  version: "0.1.0"

source:
  fn: dtmf-detector-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/dtmf-detector/dtmf-detector-0.1.0.tar.gz
  md5: 509a6a7b5f993aa5f2871df04852fedc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dtmf-detector = dtmf-detector:main
    #
    # Would create an entry point called dtmf-detector that calls dtmf-detector.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - dtmf-detector

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/nickrobinson/DTMFDetector
  license: Public Domain
  summary: 'DTMF Detection Library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
