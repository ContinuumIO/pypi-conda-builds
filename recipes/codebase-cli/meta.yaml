package:
  name: codebase-cli
  version: "0.1.10"

source:
  fn: codebase-cli-0.1.10.tar.gz
  url: https://pypi.python.org/packages/source/c/codebase-cli/codebase-cli-0.1.10.tar.gz
  md5: 04a492da173540381406aa47bbb65da2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - codebase-cli = codebase-cli:main
    #
    # Would create an entry point called codebase-cli that calls codebase-cli.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/kevincarrogan/codebase-cli
  license: UNKNOWN
  summary: 'A CLI for codebasehq.com'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
