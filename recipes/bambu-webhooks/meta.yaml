package:
  name: bambu-webhooks
  version: "2.0"

source:
  fn: bambu-webhooks-2.0.tar.gz
  url: https://pypi.python.org/packages/source/b/bambu-webhooks/bambu-webhooks-2.0.tar.gz
  md5: 8a7f941b0f06468f5d09f0f2da901f5a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bambu-webhooks = bambu-webhooks:main
    #
    # Would create an entry point called bambu-webhooks that calls bambu-webhooks.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4
    - requests
    - bambu-cron >=2.0

  run:
    - python
    - django >=1.4
    - requests
    - bambu-cron >=2.0

test:
  # Python imports
  imports:
    - bambu_webhooks
    - bambu_webhooks.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/iamsteadman/bambu-webhooks
  license: UNKNOWN
  summary: 'Create webhooks and allow users to assign URLs to them'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
