package:
  name: collective.schedule
  version: "0.2"

source:
  fn: collective.schedule-0.2.zip
  url: https://pypi.python.org/packages/source/c/collective.schedule/collective.schedule-0.2.zip
  md5: bd23dd2231c438f41bf5848adb541b10
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.schedule = collective.schedule:main
    #
    # Would create an entry point called collective.schedule that calls collective.schedule.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone
    - schedule

  run:
    - python
    - setuptools
    - setuptools
    - plone
    - schedule

test:
  # Python imports
  imports:
    - collective
    - collective.schedule
    - collective.schedule.browser
    - collective.schedule.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/netsight/collective.schedule
  license: BSD
  summary: "Plone integration for the python 'schedule' library"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
