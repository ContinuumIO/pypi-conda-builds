package:
  name: basichttp
  version: "0.4.2"

source:
  fn: BasicHttp-0.4.2.tar.gz
  url: https://pypi.python.org/packages/source/B/BasicHttp/BasicHttp-0.4.2.tar.gz
  md5: fd921f361687f10444de68126c7e4e61
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - basichttp = basichttp:main
    #
    # Would create an entry point called basichttp that calls basichttp.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - basic_http
    - basichttp

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/nachopro/basic_http
  license: MIT
  summary: 'A easy to use and clean HTTP client lib for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
