package:
  name: cssq
  version: "0.0.1"

source:
  fn: cssq-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/cssq/cssq-0.0.1.tar.gz
  md5: c7582a83909d97bc925d76b81ac4d3d9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cssq = cssq:main
    #
    # Would create an entry point called cssq that calls cssq.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - beautifulsoup4 >=4.3.2
    - requests >=2.4.1
    - html5lib >=0.999

  run:
    - python
    - beautifulsoup4 >=4.3.2
    - requests >=2.4.1
    - html5lib >=0.999

test:
  # Python imports
  imports:
    - cssq

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Tatsh/cssq
  license: LICENSE.txt
  summary: 'Filter HTML with a CSS query.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
