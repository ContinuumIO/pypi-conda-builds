package:
  name: collective.behavior.localskin
  version: "0.9.2"

source:
  fn: collective.behavior.localskin-0.9.2.zip
  url: https://pypi.python.org/packages/source/c/collective.behavior.localskin/collective.behavior.localskin-0.9.2.zip
  md5: cf8c9981ea22e84834cb19128d4bd745
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.behavior.localskin = collective.behavior.localskin:main
    #
    # Would create an entry point called collective.behavior.localskin that calls collective.behavior.localskin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - products.cmfcore
    - zope2
    - collective.behavior.localregistry
    - plone.app.dexterity
    - plone.app.registry
    - plone.behavior
    - plone.registry
    - plone.supermodel
    - setuptools
    - z3c.form
    - zope.component
    - zope.i18nmessageid
    - zope.interface
    - zope.lifecycleevent
    - zope.schema

  run:
    - python
    - setuptools
    - products.cmfcore
    - zope2
    - collective.behavior.localregistry
    - plone.app.dexterity
    - plone.app.registry
    - plone.behavior
    - plone.registry
    - plone.supermodel
    - setuptools
    - z3c.form
    - zope.component
    - zope.i18nmessageid
    - zope.interface
    - zope.lifecycleevent
    - zope.schema

test:
  # Python imports
  imports:
    - collective
    - collective.behavior
    - collective.behavior.localskin
    - collective.behavior.localskin.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.behavior.localskin/
  license: GPL
  summary: 'Dexterity behavior to enable a local theme skin'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
