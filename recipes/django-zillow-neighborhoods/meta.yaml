package:
  name: django-zillow-neighborhoods
  version: "0.8.0"

source:
  fn: django-zillow-neighborhoods-0.8.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-zillow-neighborhoods/django-zillow-neighborhoods-0.8.0.tar.gz
  md5: c179db33f38002f978d47552e6bc9a80
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-zillow-neighborhoods = django-zillow-neighborhoods:main
    #
    # Would create an entry point called django-zillow-neighborhoods that calls django-zillow-neighborhoods.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-localflavor
    - pyproj

  run:
    - python
    - django-localflavor
    - pyproj

test:
  # Python imports
  imports:
    - django_zillow_neighborhoods
    - django_zillow_neighborhoods.management
    - django_zillow_neighborhoods.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/claymation/django-zillow-neighborhoods
  license: BSD License
  summary: "Django app for importing and querying Zillow's neighborhood data"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
