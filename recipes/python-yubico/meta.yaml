package:
  name: python-yubico
  version: "1.2.3"

source:
  fn: python-yubico-1.2.3.tar.gz
  url: https://pypi.python.org/packages/source/p/python-yubico/python-yubico-1.2.3.tar.gz
  md5: 945409a3e2bbb66481226e5f3389d44b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-yubico = python-yubico:main
    #
    # Would create an entry point called python-yubico that calls python-yubico.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyusb

  run:
    - python
    - pyusb

test:
  # Python imports
  imports:
    - yubico

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Yubico/python-yubico
  license: BSD License
  summary: "Python code for talking to Yubico's YubiKeys"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
