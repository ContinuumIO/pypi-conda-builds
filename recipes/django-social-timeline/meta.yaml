package:
  name: django-social-timeline
  version: "0.1"

source:
  fn: django-social-timeline-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-social-timeline/django-social-timeline-0.1.tar.gz
  md5: 5c268d25b124945d285f31b63faa027b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-social-timeline = django-social-timeline:main
    #
    # Would create an entry point called django-social-timeline that calls django-social-timeline.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - social_demo
    - social_timeline
    - social_timeline.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/thelabnyc/django-social-timeline
  license: Apache Software License
  summary: 'Social networking for django. Following system and timeline of events.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
