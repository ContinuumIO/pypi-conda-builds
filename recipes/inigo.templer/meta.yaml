package:
  name: inigo.templer
  version: "1.4"

source:
  fn: inigo.templer-1.4.zip
  url: https://pypi.python.org/packages/source/i/inigo.templer/inigo.templer-1.4.zip
  md5: 65a981d11fb44e3591f861fb31abe535
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - inigo.templer = inigo.templer:main
    #
    # Would create an entry point called inigo.templer that calls inigo.templer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - templer.core
    - templer.zope
    - templer.localcommands

  run:
    - python
    - setuptools
    - setuptools
    - templer.core
    - templer.zope
    - templer.localcommands

test:
  # Python imports
  imports:
    - inigo
    - inigo.templer
    - inigo.templer.localcommands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/inigoconsulting/inigo.templer
  license: MIT License
  summary: 'Convenience templer templates for Inigo stuff'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
