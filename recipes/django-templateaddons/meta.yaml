package:
  name: django-templateaddons
  version: "0.1"

source:
  fn: django-templateaddons-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-templateaddons/django-templateaddons-0.1.tar.gz
  md5: 88132cd45faf5c0275222810cb3fa1c7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-templateaddons = django-templateaddons:main
    #
    # Would create an entry point called django-templateaddons that calls django-templateaddons.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - templateaddons
    - templateaddons.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/benoitbryon/django-templateaddons/
  license: BSD License
  summary: 'A set of tools for use with templates of the Django framework: additional template tags, context processors and utilities for template tag development.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
