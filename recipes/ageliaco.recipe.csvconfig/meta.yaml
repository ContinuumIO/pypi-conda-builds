package:
  name: ageliaco.recipe.csvconfig
  version: "0.8"

source:
  fn: ageliaco.recipe.csvconfig-0.8.tar.gz
  url: https://pypi.python.org/packages/source/a/ageliaco.recipe.csvconfig/ageliaco.recipe.csvconfig-0.8.tar.gz
  md5: 50b1eb78bfc6dbcb2a75d09b71806d14
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ageliaco.recipe.csvconfig = ageliaco.recipe.csvconfig:main
    #
    # Would create an entry point called ageliaco.recipe.csvconfig that calls ageliaco.recipe.csvconfig.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - configparser

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - configparser

test:
  # Python imports
  imports:
    - ageliaco
    - ageliaco.recipe
    - ageliaco.recipe.csvconfig
    - ageliaco.recipe.csvconfig.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/renfers/ageliaco.recipe.csvconfig
  license: Zope Public License
  summary: 'Use a CSV file to populate buildout templates'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
