package:
  name: identity-toolkit-python-client
  version: "0.1.5"

source:
  fn: identity-toolkit-python-client-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/i/identity-toolkit-python-client/identity-toolkit-python-client-0.1.5.tar.gz
  md5: 8ec2c440acdee936fedac06b9cb06494
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - identity-toolkit-python-client = identity-toolkit-python-client:main
    #
    # Would create an entry point called identity-toolkit-python-client that calls identity-toolkit-python-client.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - oauth2client >=1.3.2
    - pyopenssl ==0.14
    - simplejson >=2.3.2

  run:
    - python
    - oauth2client >=1.3.2
    - pyopenssl ==0.14
    - simplejson >=2.3.2

test:
  # Python imports
  imports:
    - identitytoolkit

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/google/identity-toolkit-python-client
  license: Apache 2.0
  summary: 'Google Identity Toolkit python client library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
