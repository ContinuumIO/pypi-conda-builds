package:
  name: django_epfl
  version: "1.5.6"

source:
  fn: django_epfl-1.5.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django_epfl/django_epfl-1.5.6.tar.gz
  md5: db3690692748952ea6cdd77e6818af59
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_epfl = django_epfl:main
    #
    # Would create an entry point called django_epfl that calls django_epfl.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_epfl
    - django_epfl.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://kis-doc.epfl.ch/django
  license: GNU LGPLv3
  summary: 'Django utils for EPFL sites'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
