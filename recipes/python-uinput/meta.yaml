package:
  name: python-uinput
  version: "0.10.2"

source:
  fn: python-uinput-0.10.2.tar.gz
  url: https://pypi.python.org/packages/source/p/python-uinput/python-uinput-0.10.2.tar.gz
  md5: abbbbfc50d03a0585a5231d9396f78bd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-uinput = python-uinput:main
    #
    # Would create an entry point called python-uinput that calls python-uinput.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - uinput

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://tjjr.fi/sw/python-uinput/
  license: GNU General Public License (GPL)
  summary: 'Pythonic API to Linux uinput kernel module.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
