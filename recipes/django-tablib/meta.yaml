package:
  name: django-tablib
  version: "3.1.1"

source:
  fn: django-tablib-3.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-tablib/django-tablib-3.1.1.tar.gz
  md5: 9d176c69a0d4ca4ad850b26da9e8a6ca
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-tablib = django-tablib:main
    #
    # Would create an entry point called django-tablib that calls django-tablib.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - tablib

  run:
    - python
    - tablib

test:
  # Python imports
  imports:
    - django_tablib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/joshourisman/django-tablib
  license: BSD License
  summary: "A wrapper around Kenneth Reitz' tablib to work with Django models."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
