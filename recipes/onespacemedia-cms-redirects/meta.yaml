package:
  name: onespacemedia-cms-redirects
  version: "0.0.1"

source:
  fn: onespacemedia-cms-redirects-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/o/onespacemedia-cms-redirects/onespacemedia-cms-redirects-0.0.1.tar.gz
  md5: 4ed0a971660aab12af911170b183efc0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - onespacemedia-cms-redirects = onespacemedia-cms-redirects:main
    #
    # Would create an entry point called onespacemedia-cms-redirects that calls onespacemedia-cms-redirects.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - redirects

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/onespacemedia/cms-redirects
  license: MIT
  summary: 'A redirection module for the Onespacemedia CMS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
