package:
  name: django-radio
  version: "1.2"

source:
  fn: django-radio-1.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-radio/django-radio-1.2.0.tar.gz
  md5: ee47ac0b5db1ef6f2dea25c2446f75e1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-radio = django-radio:main
    #
    # Would create an entry point called django-radio that calls django-radio.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.7
    - pillow
    - python-dateutil
    - pytz
    - django-bootstrap3
    - djangorestframework
    - django-ckeditor

  run:
    - python
    - django >=1.7
    - pillow
    - python-dateutil
    - pytz
    - django-bootstrap3
    - djangorestframework
    - django-ckeditor

test:
  # Python imports
  imports:
    - radio
    - radio.apps
    - radio.apps.dashboard
    - radio.apps.programmes
    - radio.apps.programmes.migrations
    - radio.apps.schedules
    - radio.apps.schedules.migrations
    - radio.apps.users
    - radio.apps.users.migrations
    - radio.libs
    - radio.libs.global_settings
    - radio.libs.global_settings.migrations
    - radio.libs.global_settings.templatetags
    - radio.libs.home

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://radioco.org/
  license: GNU General Public License v3 (GPLv3)
  summary: 'This app provides an easy way to set up your radio'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
