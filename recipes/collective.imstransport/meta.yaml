package:
  name: collective.imstransport
  version: "3.2.2"

source:
  fn: collective.imstransport-3.2.2.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.imstransport/collective.imstransport-3.2.2.tar.gz
  md5: 3525ddb83ab5fa4090102c0daa4e4fad
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.imstransport = collective.imstransport:main
    #
    # Would create an entry point called collective.imstransport that calls collective.imstransport.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.imstransport
    - collective.imstransport.browser
    - collective.imstransport.tests
    - collective.imstransport.utilities
    - collective.imstransport.utilities.bb
    - collective.imstransport.utilities.imscc
    - collective.imstransport.utilities.imscp
    - collective.imstransport.utilities.mit
    - collective.imstransport.utilities.moodle
    - collective.imstransport.utilities.webct
    - collective.imstransport.utilities.webctvista

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/
  license: GPL
  summary: 'IMS Content Packaging plugin for Plone.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
