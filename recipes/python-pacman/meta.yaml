package:
  name: python-pacman
  version: "0.1.1"

source:
  fn: python-pacman-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/p/python-pacman/python-pacman-0.1.1.tar.gz
  md5: 9921a33e9df4f1a205cccfc7da231f64
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-pacman = python-pacman:main
    #
    # Would create an entry point called python-pacman that calls python-pacman.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://git.coderouge.co/coderouge/python-pacman
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'Simple Python interface to Arch Linux package manager (pacman)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
