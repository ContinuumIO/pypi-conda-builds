package:
  name: python-axolotl
  version: "0.1.7"

source:
  fn: python-axolotl-0.1.7.tar.gz
  url: https://pypi.python.org/packages/source/p/python-axolotl/python-axolotl-0.1.7.tar.gz
  md5: 1cdc515d202c118cf669aaa71924d9bf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-axolotl = python-axolotl:main
    #
    # Would create an entry point called python-axolotl that calls python-axolotl.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - protobuf >=2.6,<3.0.0-alpha-2
    - pycrypto
    - python-axolotl-curve25519

  run:
    - python
    - protobuf >=2.6,<3.0.0-alpha-2
    - pycrypto
    - python-axolotl-curve25519

test:
  # Python imports
  imports:
    - axolotl
    - axolotl.ecc
    - axolotl.groups
    - axolotl.groups.ratchet
    - axolotl.groups.state
    - axolotl.kdf
    - axolotl.protocol
    - axolotl.ratchet
    - axolotl.state
    - axolotl.tests
    - axolotl.tests.groups
    - axolotl.tests.kdf
    - axolotl.tests.ratchet
    - axolotl.tests.util
    - axolotl.util

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: GPLv3 License
  summary: 'Python port of libaxolotl-android, originally written by Moxie Marlinspik'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
