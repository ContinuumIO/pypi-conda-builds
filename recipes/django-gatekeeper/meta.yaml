package:
  name: django-gatekeeper
  version: "0.3.0"

source:
  fn: django-gatekeeper-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-gatekeeper/django-gatekeeper-0.3.0.tar.gz
  md5: 30a35dffc7339e5dbbaf46ce7ffff805
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-gatekeeper = django-gatekeeper:main
    #
    # Would create an entry point called django-gatekeeper that calls django-gatekeeper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - gatekeeper

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/sunlightlabs/django-gatekeeper/
  license: BSD License
  summary: 'Django object moderation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
