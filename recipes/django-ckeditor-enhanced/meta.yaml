package:
  name: django-ckeditor-enhanced
  version: "1.4"

source:
  fn: django-ckeditor-enhanced-1.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-ckeditor-enhanced/django-ckeditor-enhanced-1.4.tar.gz
  md5: 05471777e18164977a5565744690fc24
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-ckeditor-enhanced = django-ckeditor-enhanced:main
    #
    # Would create an entry point called django-ckeditor-enhanced that calls django-ckeditor-enhanced.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - ckeditor
    - ckeditor.image
    - ckeditor.management
    - ckeditor.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/n37r06u3/django-ckeditor-enhanced
  license: BSD License
  summary: 'django-ckeditor-updated with codesnippet plugin support'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
