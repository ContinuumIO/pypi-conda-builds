package:
  name: python-keystoneclient-kerberos
  version: "0.2.0"

source:
  fn: python-keystoneclient-kerberos-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/p/python-keystoneclient-kerberos/python-keystoneclient-kerberos-0.2.0.tar.gz
  md5: 3730d87f27514ba587b11d6218f0cf93
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-keystoneclient-kerberos = python-keystoneclient-kerberos:main
    #
    # Would create an entry point called python-keystoneclient-kerberos that calls python-keystoneclient-kerberos.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Apache Software License
  summary: 'Kerberos authentication for the OpenStack clients'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
