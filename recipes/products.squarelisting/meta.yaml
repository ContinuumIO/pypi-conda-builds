package:
  name: products.squarelisting
  version: "0.1.4"

source:
  fn: Products.SquareListing-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/P/Products.SquareListing/Products.SquareListing-0.1.4.tar.gz
  md5: a9e952be15ebe9c2a9331bb64e906331
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - products.squarelisting = products.squarelisting:main
    #
    # Would create an entry point called products.squarelisting that calls products.squarelisting.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - Products
    - Products.SquareListing

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: htttp://www.abstract.it
  license: GPL
  summary: 'A square listing template for folderish contents'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
