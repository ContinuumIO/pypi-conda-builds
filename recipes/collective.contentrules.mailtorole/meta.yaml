package:
  name: collective.contentrules.mailtorole
  version: "1.7"

source:
  fn: collective.contentrules.mailtorole-1.7.zip
  url: https://pypi.python.org/packages/source/c/collective.contentrules.mailtorole/collective.contentrules.mailtorole-1.7.zip
  md5: 0bde0dea1c6ba80e6d7dea3225ca0d9b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.contentrules.mailtorole = collective.contentrules.mailtorole:main
    #
    # Would create an entry point called collective.contentrules.mailtorole that calls collective.contentrules.mailtorole.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.contentrules
    - collective.contentrules.mailtorole
    - collective.contentrules.mailtorole.actions
    - collective.contentrules.mailtorole.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/collective.contentrules.mailtorole
  license: GPL
  summary: 'Send e-mail to users having a role on the object'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
