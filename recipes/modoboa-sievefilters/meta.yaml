package:
  name: modoboa-sievefilters
  version: "1.0.1"

source:
  fn: modoboa-sievefilters-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/m/modoboa-sievefilters/modoboa-sievefilters-1.0.1.tar.gz
  md5: 6506e11f243a6a6e69ba1a98ac9687e0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - modoboa-sievefilters = modoboa-sievefilters:main
    #
    # Would create an entry point called modoboa-sievefilters that calls modoboa-sievefilters.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - modoboa-webmail
    - sievelib
    - rfc6266

  run:
    - python
    - modoboa-webmail
    - sievelib
    - rfc6266

test:
  # Python imports
  imports:
    - modoboa_sievefilters
    - modoboa_sievefilters.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://modoboa.org/
  license: MIT License
  summary: 'The sieve filters editor of Modoboa'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
