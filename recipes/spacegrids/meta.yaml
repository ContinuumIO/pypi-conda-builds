package:
  name: spacegrids
  version: "1.71"

source:
  fn: spacegrids-1.71.tar.gz
  url: https://pypi.python.org/packages/source/s/spacegrids/spacegrids-1.71.tar.gz
  md5: a233eaa014c9237eb38f460c62148df7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - spacegrids = spacegrids:main
    #
    # Would create an entry point called spacegrids that calls spacegrids.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - spacegrids

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/willo12/spacegrids
  license: BSD
  summary: 'numpy array with grids and associated operations'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
