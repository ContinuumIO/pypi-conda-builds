package:
  name: h5scripting
  version: "0.1.0"

source:
  fn: h5scripting-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/h/h5scripting/h5scripting-0.1.0.tar.gz
  md5: 744db86deedbf143049e4918c184be9c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - h5scripting = h5scripting:main
    #
    # Would create an entry point called h5scripting that calls h5scripting.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - h5scripting

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/cbillington/h5scripting
  license: BSD
  summary: 'An interface for saving python functions to an h5 file and allowing these to be executed in a nice way'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
