package:
  name: demoize
  version: "1.3.0"

source:
  fn: Demoize-1.3.0.tar.gz
  url: https://pypi.python.org/packages/source/D/Demoize/Demoize-1.3.0.tar.gz
  md5: 538425dae4ec2e48fbd10198fdded7d2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - demoize = demoize:main
    #
    # Would create an entry point called demoize that calls demoize.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - flask >=0.9
    - pygments >=1.6
    - selenium >=2.31
    - astor >=0.3

  run:
    - python
    - flask >=0.9
    - pygments >=1.6
    - selenium >=2.31
    - astor >=0.3

test:
  # Python imports
  imports:
    - demoize
    - demoize.web

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/Demoize/
  license: LICENSE.txt
  summary: 'A webserver that shows your code as it is executed'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
