package:
  name: elasticutils
  version: "0.10.3"

source:
  fn: elasticutils-0.10.3.tar.gz
  url: https://pypi.python.org/packages/source/e/elasticutils/elasticutils-0.10.3.tar.gz
  md5: e76b97c987d1eaf5776dac06967b446c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - elasticutils = elasticutils:main
    #
    # Would create an entry point called elasticutils that calls elasticutils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - elasticsearch >=1.0
    - six

  run:
    - python
    - elasticsearch >=1.0
    - six

test:
  # Python imports
  imports:
    - elasticutils
    - elasticutils.contrib
    - elasticutils.contrib.django
    - elasticutils.contrib.django.tests
    - elasticutils.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mozilla/elasticutils
  license: BSD License
  summary: 'Chainable, iterative interface to querying Elasticsearch'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
