package:
  name: elementium
  version: "1.1.0"

source:
  fn: elementium-1.1.0.tar.gz
  url: https://pypi.python.org/packages/source/e/elementium/elementium-1.1.0.tar.gz
  md5: c7e856b1ee0e9ff2a52595b5cc7fdd3d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - elementium = elementium:main
    #
    # Would create an entry point called elementium that calls elementium.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - elementium
    - elementium.drivers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/actmd/elementium/
  license: Apache Software License
  summary: 'Browser testing done right.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
