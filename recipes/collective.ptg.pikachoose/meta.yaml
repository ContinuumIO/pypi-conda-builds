package:
  name: collective.ptg.pikachoose
  version: "1.0.4"

source:
  fn: collective.ptg.pikachoose-1.0.4.zip
  url: https://pypi.python.org/packages/source/c/collective.ptg.pikachoose/collective.ptg.pikachoose-1.0.4.zip
  md5: 85d95caa133643512eeb3259f188ccc2
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.ptg.pikachoose = collective.ptg.pikachoose:main
    #
    # Would create an entry point called collective.ptg.pikachoose that calls collective.ptg.pikachoose.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.plonetruegallery

  run:
    - python
    - setuptools
    - setuptools
    - collective.plonetruegallery

test:
  # Python imports
  imports:
    - collective
    - collective.ptg
    - collective.ptg.pikachoose

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/
  license: GPL
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
