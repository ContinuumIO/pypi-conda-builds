package:
  name: collective.portlet.image
  version: "0.1"

source:
  fn: collective.portlet.image-0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.portlet.image/collective.portlet.image-0.1.tar.gz
  md5: d2130be21389f56d905d9aae21be30ed
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.portlet.image = collective.portlet.image:main
    #
    # Would create an entry point called collective.portlet.image that calls collective.portlet.image.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.namedfile

  run:
    - python
    - setuptools
    - setuptools
    - collective.namedfile

test:
  # Python imports
  imports:
    - collective
    - collective.portlet
    - collective.portlet.image
    - collective.portlet.image.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/collective.portlet.image
  license: GPL
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
