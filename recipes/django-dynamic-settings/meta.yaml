package:
  name: django-dynamic-settings
  version: "1.1.1"

source:
  fn: django-dynamic-settings-1.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-dynamic-settings/django-dynamic-settings-1.1.1.tar.gz
  md5: e3ffc7e47ddfb8d9aa560f643b660fd9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-dynamic-settings = django-dynamic-settings:main
    #
    # Would create an entry point called django-dynamic-settings that calls django-dynamic-settings.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - dynamic_setting
    - dynamic_setting.migrations
    - dynamic_setting.templatetags
    - dynamic_setting.tests
    - testproj

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/koralarts/django-dynamic-settings
  license: MIT
  summary: 'Small module that allows you to generate dynamic settings that can be edited inside the Django admin dashboard'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
