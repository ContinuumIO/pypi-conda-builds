package:
  name: flask-misaka
  version: "0.3.0"

source:
  fn: Flask-Misaka-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Misaka/Flask-Misaka-0.3.0.tar.gz
  md5: 1349b646c89be6ba4067405190a3981b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-misaka = flask-misaka:main
    #
    # Would create an entry point called flask-misaka that calls flask-misaka.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.7
    - misaka

  run:
    - python
    - flask >=0.7
    - misaka

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/singingwolfboy/flask-misaka/
  license: MIT License
  summary: 'A pleasant interface between the Flask web framework and the Misaka Markdown parser.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
