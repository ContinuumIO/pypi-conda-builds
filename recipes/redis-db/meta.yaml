package:
  name: redis-db
  version: "0.1.5"

source:
  fn: Redis-DB-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/R/Redis-DB/Redis-DB-0.1.5.tar.gz
  md5: 2f68e11154e43190335ac52cd6eeb9eb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - redis-db = redis-db:main
    #
    # Would create an entry point called redis-db that calls redis-db.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django >=1.4
    - redis >=2.7.4

  run:
    - python
    - django >=1.4
    - redis >=2.7.4

test:
  # Python imports
  imports:
    - redis_db

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/redis-db/
  license: LICENSE.txt
  summary: 'Redis cache for managing Django ORM'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
