package:
  name: compass-stylesheets
  version: "1.1.0a3"

source:
  fn: compass-stylesheets-1.1.0a3.tar.gz
  url: https://pypi.python.org/packages/source/c/compass-stylesheets/compass-stylesheets-1.1.0a3.tar.gz
  md5: 6aef227977cfe20192feed5e6eb51896
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - compass-stylesheets = compass-stylesheets:main
    #
    # Would create an entry point called compass-stylesheets that calls compass-stylesheets.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - compass_stylesheets

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://compass-style.org/
  license: UNKNOWN
  summary: 'Stylesheets from Compass framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
