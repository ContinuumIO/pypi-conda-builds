package:
  name: icoshift
  version: "1.0"

source:
  fn: icoshift-1.0.tar.gz
  url: https://pypi.python.org/packages/source/i/icoshift/icoshift-1.0.tar.gz
  md5: 67155b25428ceb59e9df7fe0efdcd8b3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - icoshift = icoshift:main
    #
    # Would create an entry point called icoshift that calls icoshift.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy >=1.7.1
    - scipy >=0.12.0

  run:
    - python
    - numpy >=1.7.1
    - scipy >=0.12.0

test:
  # Python imports
  imports:
    - icoshift

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mfitzp/icoshift
  license: BSD License
  summary: 'icoshift: A versatile tool for the rapid alignment of 1D NMR spectra'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
