package:
  name: zerogroup
  version: "0.1.0"

source:
  fn: zerogroup-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/z/zerogroup/zerogroup-0.1.0.tar.gz
  md5: 78c957d614423da74b956be0036821da
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zerogroup = zerogroup:main
    #
    # Would create an entry point called zerogroup that calls zerogroup.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyzmq

  run:
    - python
    - pyzmq

test:
  # Python imports
  imports:
    - zerogroup

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lebedov/zerogroup
  license: BSD License
  summary: 'ZeroMQ socket group wrapper'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
