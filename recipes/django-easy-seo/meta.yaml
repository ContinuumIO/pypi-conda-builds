package:
  name: django-easy-seo
  version: "0.4.6"

source:
  fn: django-easy-seo-0.4.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-easy-seo/django-easy-seo-0.4.6.tar.gz
  md5: 4a2d01dad2f8a7151a7114b5fff2ebba
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-easy-seo = django-easy-seo:main
    #
    # Would create an entry point called django-easy-seo that calls django-easy-seo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-classy-tags
    - django-cache-machine >=0.8

  run:
    - python
    - django-classy-tags
    - django-cache-machine >=0.8

test:
  # Python imports
  imports:
    - seo

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: GNU General Public License (GPL)
  summary: 'Adds generic SEO fields for objects in your site or specific urls'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
