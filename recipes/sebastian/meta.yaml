package:
  name: sebastian
  version: "0.0.3"

source:
  fn: sebastian-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/s/sebastian/sebastian-0.0.3.tar.gz
  md5: d61eccdcf3b5c0b2cc75bc7d8484113c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sebastian = sebastian:main
    #
    # Would create an entry point called sebastian that calls sebastian.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - sebastian
    - sebastian.core
    - sebastian.lilypond
    - sebastian.midi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/jtauber/sebastian
  license: MIT
  summary: 'symbolic music analysis and composition library in Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
