package:
  name: django-generic-json-views
  version: "0.6.5"

source:
  fn: django-generic-json-views-0.6.5b.tar.gz
  url: https://pypi.python.org/packages/source/d/django-generic-json-views/django-generic-json-views-0.6.5b.tar.gz
  md5: e60178f7cf94aaa39a80acec248ccae3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-generic-json-views = django-generic-json-views:main
    #
    # Would create an entry point called django-generic-json-views that calls django-generic-json-views.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5

  run:
    - python
    - django >=1.5

test:
  # Python imports
  imports:
    - json_views

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bbengfort/django-generic-json-views
  license: Apache Software License
  summary: 'Class based generic views that render JSON data.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
