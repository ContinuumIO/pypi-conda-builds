package:
  name: loggingd
  version: "1.2.0"

source:
  fn: loggingd-1.2.0.tar.gz
  url: https://pypi.python.org/packages/source/l/loggingd/loggingd-1.2.0.tar.gz
  md5: a4f7b482ab9a359e802481e0b94a84bc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - loggingd = loggingd:main
    #
    # Would create an entry point called loggingd that calls loggingd.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - decorated >=1.5.0

  run:
    - python
    - decorated >=1.5.0

test:
  # Python imports
  imports:
    - loggingd

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/CooledCoffee/loggingd/
  license: UNKNOWN
  summary: 'Logging framework using decorators.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
