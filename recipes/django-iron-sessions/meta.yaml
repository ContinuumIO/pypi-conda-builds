package:
  name: django-iron-sessions
  version: "0.0.2"

source:
  fn: django-iron-sessions-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-iron-sessions/django-iron-sessions-0.0.2.tar.gz
  md5: 1144c18ac60c23b568fea4a92088b5d9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-iron-sessions = django-iron-sessions:main
    #
    # Would create an entry point called django-iron-sessions that calls django-iron-sessions.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4
    - iron-cache ==0.2.0

  run:
    - python
    - django >=1.4
    - iron-cache ==0.2.0

test:
  # Python imports
  imports:
    - iron_sessions

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/hellysmile/django-iron-sessions
  license: Apache Software License
  summary: 'iron.io cache as Django sessions backend'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
