package:
  name: bioasp
  version: "1.1"

source:
  fn: bioasp-1.1.tar.gz
  url: https://pypi.python.org/packages/source/b/bioasp/bioasp-1.1.tar.gz
  md5: e5e8ce965c91b16357d4f5dde643c5b6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bioasp = bioasp:main
    #
    # Would create an entry point called bioasp that calls bioasp.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - ingranalyze
    - meneco
    - shogen
    - precursor
    - caspo

  run:
    - python
    - ingranalyze
    - meneco
    - shogen
    - precursor
    - caspo

test:
  # Python imports
  imports:
    - bioasp

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/bioasp/
  license: GPLv3+
  summary: 'Answer Set Programming for Systems Biology'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
