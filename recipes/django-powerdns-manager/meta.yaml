package:
  name: django-powerdns-manager
  version: "0.2.1a1"

source:
  fn: django-powerdns-manager-0.2.1a1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-powerdns-manager/django-powerdns-manager-0.2.1a1.tar.gz
  md5: 8455f11089485d84710df98652b768df
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-powerdns-manager = django-powerdns-manager:main
    #
    # Would create an entry point called django-powerdns-manager that calls django-powerdns-manager.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - powerdns_manager

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.codetrax.org/projects/django-powerdns-manager
  license: Apache Software License
  summary: 'django-powerdns-manager is a web based PowerDNS administration panel.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
