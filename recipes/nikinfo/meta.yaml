package:
  name: nikinfo
  version: "0.1.0"

source:
  fn: nikinfo-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/n/nikinfo/nikinfo-0.1.0.tar.gz
  md5: fc437f8a053519f0985529b0d7674d8a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - nikinfo = nikinfo:main
    #
    # Would create an entry point called nikinfo that calls nikinfo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - nikinfo

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://mapnik-utils.googlecode.com/
  license: GPlv2
  summary: 'Tool for getting details about a shapefile for use with Mapnik'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
