package:
  name: cine_io
  version: "0.2.2"

source:
  fn: cine_io-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/c/cine_io/cine_io-0.2.2.tar.gz
  md5: ebf318e97b6a95b0cb3d503abd45974e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cine_io = cine_io:main
    #
    # Would create an entry point called cine_io that calls cine_io.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=2.0
    - nested_query_string >=0.0.1

  run:
    - python
    - requests >=2.0
    - nested_query_string >=0.0.1

test:
  # Python imports
  imports:
    - cine_io

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://www.cine.io
  license: MIT License
  summary: 'cine.io is an api driven platform for creating and publish live streams. The provides cine.io functionality using your given public and secret keys.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
