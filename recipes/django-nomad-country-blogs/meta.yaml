package:
  name: django-nomad-country-blogs
  version: "1.4.2"

source:
  fn: django-nomad-country-blogs-1.4.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-nomad-country-blogs/django-nomad-country-blogs-1.4.2.tar.gz
  md5: 5e4fbf5e5074caa913ebfc00ff10933f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-nomad-country-blogs = django-nomad-country-blogs:main
    #
    # Would create an entry point called django-nomad-country-blogs that calls django-nomad-country-blogs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - nomadblog

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/nabucosound/django-nomad-country-blogs/
  license: BSD License
  summary: 'A minimalist Django blogging system to maintain country-specific blogs.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
