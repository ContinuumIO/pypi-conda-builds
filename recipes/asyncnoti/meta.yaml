package:
  name: asyncnoti
  version: "1.0.6"

source:
  fn: asyncnoti-1.0.6.tar.gz
  url: https://pypi.python.org/packages/source/a/asyncnoti/asyncnoti-1.0.6.tar.gz
  md5: 2b4fab8226508d16f3e6b2113296dd50
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - asyncnoti = asyncnoti:main
    #
    # Would create an entry point called asyncnoti that calls asyncnoti.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - six

  run:
    - python
    - six

test:
  # Python imports
  imports:
    - asyncnoti

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/asyncnoti/asyncnoti-python
  license: MIT License
  summary: 'Library to interact with the Asyncnoti API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
