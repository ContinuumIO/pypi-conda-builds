package:
  name: exceptable
  version: "0.1.0"

source:
  fn: Exceptable-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/E/Exceptable/Exceptable-0.1.0.tar.gz
  md5: f80d05c4bba6abf3627ef485c97ca403
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - exceptable = exceptable:main
    #
    # Would create an entry point called exceptable that calls exceptable.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - exceptable
    - test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://public.commandprompt.com/projects/exceptable/wiki
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'DB Exception Management'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
