package:
  name: wsme
  version: "0.6.4"

source:
  fn: WSME-0.6.4.tar.gz
  url: https://pypi.python.org/packages/source/W/WSME/WSME-0.6.4.tar.gz
  md5: 0ba96f660c10d41c63d6bb65bfeeed59
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - wsme = wsme:main
    #
    # Would create an entry point called wsme that calls wsme.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six
    - simplegeneric
    - webob
    - ipaddr
    - pbr >=0.5.21

  run:
    - python
    - six
    - simplegeneric
    - webob
    - ipaddr
    - pbr >=0.5.21

test:
  # Python imports
  imports:
    - wsme

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT License
  summary: 'Simplify the writing of REST APIs, and extend them with additional protocols.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
