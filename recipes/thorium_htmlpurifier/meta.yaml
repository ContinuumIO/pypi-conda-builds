package:
  name: thorium_htmlpurifier
  version: "0.1.1"

source:
  fn: thorium_htmlpurifier-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/t/thorium_htmlpurifier/thorium_htmlpurifier-0.1.1.tar.gz
  md5: 79821fb56a07d23d1c2c392612b6a551
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - thorium_htmlpurifier = thorium_htmlpurifier:main
    #
    # Would create an entry point called thorium_htmlpurifier that calls thorium_htmlpurifier.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - coverage
    - html-purifier3 >=2.1.0
    - nose
    - thorium

  run:
    - python
    - coverage
    - html-purifier3 >=2.1.0
    - nose
    - thorium

test:
  # Python imports
  imports:
    - thorium_htmlpurifier

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/meunierd/thorium_htmlpurifier/
  license: UNKNOWN
  summary: 'HTML Purifier validation for Thorium'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
