package:
  name: lthread
  version: "1.0.1"

source:
  fn: lthread-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/l/lthread/lthread-1.0.1.tar.gz
  md5: 12c021a13a828b4db18a0ebfbf751efd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lthread = lthread:main
    #
    # Would create an entry point called lthread that calls lthread.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - lthread

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT License
  summary: 'lightweight threads in Python using extended generators'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
