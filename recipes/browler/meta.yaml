package:
  name: browler
  version: "0.1.2"

source:
  fn: browler-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/b/browler/browler-0.1.2.tar.gz
  md5: cb4416fdb6fa18d9a50d3b86115335bd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - browler = browler:main
    #
    # Would create an entry point called browler that calls browler.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - redis ==2.10.3
    - splinter ==0.7.0

  run:
    - python
    - redis ==2.10.3
    - splinter ==0.7.0

test:
  # Python imports
  imports:
    - browler

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/brady-vitrano/browler
  license: MIT
  summary: 'Selenium Based Web Crawler'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
