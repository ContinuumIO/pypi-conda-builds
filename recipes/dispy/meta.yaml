package:
  name: dispy
  version: "4.2"

source:
  fn: dispy-4.2.tar.gz
  url: https://pypi.python.org/packages/source/d/dispy/dispy-4.2.tar.gz
  md5: 5c561d04be0c7b2242edb28e1a72a3f2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dispy = dispy:main
    #
    # Would create an entry point called dispy that calls dispy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - asyncoro >=3.1

  run:
    - python
    - asyncoro >=3.1

test:
  # Python imports
  imports:
    - dispy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://dispy.sourceforge.net
  license: MIT License
  summary: 'Distributed and Parallel Computing Framework with/for Python.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
