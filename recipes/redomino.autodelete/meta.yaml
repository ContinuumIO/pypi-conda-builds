package:
  name: redomino.autodelete
  version: "0.1r5"

source:
  fn: redomino.autodelete-0.1r5.tar.gz
  url: https://pypi.python.org/packages/source/r/redomino.autodelete/redomino.autodelete-0.1r5.tar.gz
  md5: a553e5888e4e5a9cda09f95d49716462
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - redomino.autodelete = redomino.autodelete:main
    #
    # Would create an entry point called redomino.autodelete that calls redomino.autodelete.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - redomino
    - redomino.autodelete
    - redomino.autodelete.Extensions
    - redomino.autodelete.browser
    - redomino.autodelete.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.redomino.com/it/labs/progetti/redomino-autodelete
  license: GPL
  summary: 'Plone autodelete objects expired tool'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
