package:
  name: horae.lifecycle
  version: "1.0a1"

source:
  fn: horae.lifecycle-1.0a1.tar.gz
  url: https://pypi.python.org/packages/source/h/horae.lifecycle/horae.lifecycle-1.0a1.tar.gz
  md5: ebf75008e2bb2aa785d21aead9b8c821
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - horae.lifecycle = horae.lifecycle:main
    #
    # Would create an entry point called horae.lifecycle that calls horae.lifecycle.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - grok
    - zope.principalannotation
    - horae.auth
    - horae.core
    - horae.layout
    - horae.timeaware

  run:
    - python
    - setuptools
    - setuptools
    - grok
    - zope.principalannotation
    - horae.auth
    - horae.core
    - horae.layout
    - horae.timeaware

test:
  # Python imports
  imports:
    - horae
    - horae.lifecycle

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: GPL
  summary: 'Handles the lifecycle of objects for the Horae resource planning system'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
