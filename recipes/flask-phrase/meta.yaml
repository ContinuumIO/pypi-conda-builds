package:
  name: flask-phrase
  version: "0.1.0"

source:
  fn: Flask-Phrase-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Phrase/Flask-Phrase-0.1.0.tar.gz
  md5: eb76af57d320b15a9717f0cc78a536c2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-phrase = flask-phrase:main
    #
    # Would create an entry point called flask-phrase that calls flask-phrase.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask-babel

  run:
    - python
    - flask-babel

test:
  # Python imports
  imports:
    - flask_phrase

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/phrase/Flask-Phrase
  license: BSD License
  summary: 'Connect your Flask apps to PhraseApp, the powerful in-context-translation solution.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
