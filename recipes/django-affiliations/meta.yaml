package:
  name: django-affiliations
  version: "0.1"

source:
  fn: django-affiliations-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-affiliations/django-affiliations-0.1.tar.gz
  md5: b9a64f5613ab5656c3fe331f4432edae
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-affiliations = django-affiliations:main
    #
    # Would create an entry point called django-affiliations that calls django-affiliations.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - affiliations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://salimm.me
  license: BSD License
  summary: 'A simple Django app to conduct affiliations for users.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
