package:
  name: etcd-cli
  version: "0.2.1"

source:
  fn: etcd-cli-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/e/etcd-cli/etcd-cli-0.2.1.tar.gz
  md5: 7bd383a348b3c365a41287ed97524836
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - etcd-cli = etcd-cli:main
    #
    # Would create an entry point called etcd-cli that calls etcd-cli.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyyaml
    - argparse
    - configparser
    - logging
    - python-etcd
    - jinja2
    - simplejson

  run:
    - python
    - pyyaml
    - argparse
    - configparser
    - logging
    - python-etcd
    - jinja2
    - simplejson

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mickep76/etcd-cli.git
  license: Apache Software License
  summary: 'etcd-cli CLI for Etcd based on schemas'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
