package:
  name: collective.recipe.zmysqlda
  version: "2.0.9"

source:
  fn: collective.recipe.zmysqlda-2.0.9.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.recipe.zmysqlda/collective.recipe.zmysqlda-2.0.9.tar.gz
  md5: cce097ee813273a135ddf5564dd904a0
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.recipe.zmysqlda = collective.recipe.zmysqlda:main
    #
    # Would create an entry point called collective.recipe.zmysqlda that calls collective.recipe.zmysqlda.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zc.buildout
    - setuptools

  run:
    - python
    - setuptools
    - zc.buildout
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.recipe
    - collective.recipe.zmysqlda

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/buildout/collective.recipe.zmysqlda
  license: GPL
  summary: 'Recipe for installing ZMySQLDA'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
