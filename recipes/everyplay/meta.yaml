package:
  name: everyplay
  version: "0.3.6"

source:
  fn: everyplay-0.3.6.tar.gz
  url: https://pypi.python.org/packages/source/e/everyplay/everyplay-0.3.6.tar.gz
  md5: 9247bb3ff33012249c0b9a95466204e2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - everyplay = everyplay:main
    #
    # Would create an entry point called everyplay that calls everyplay.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fudge ==1.0.3
    - requests >=1.0.0
    - simplejson >=2.0

  run:
    - python
    - fudge ==1.0.3
    - requests >=1.0.0
    - simplejson >=2.0

test:
  # Python imports
  imports:
    - everyplay

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Everyplay/everyplay-python
  license: BSD License
  summary: 'A friendly wrapper library for the Everyplay API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
