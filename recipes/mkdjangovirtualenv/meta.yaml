package:
  name: mkdjangovirtualenv
  version: "0.0.7"

source:
  fn: mkdjangovirtualenv-0.0.7.tar.gz
  url: https://pypi.python.org/packages/source/m/mkdjangovirtualenv/mkdjangovirtualenv-0.0.7.tar.gz
  md5: 58702956042108a5813184fb34653ca0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mkdjangovirtualenv = mkdjangovirtualenv:main
    #
    # Would create an entry point called mkdjangovirtualenv that calls mkdjangovirtualenv.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - mkdjangovirtualenv

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mrcoles/mkdjangovirtualenv
  license: MIT License
  summary: "A django-specific enhancement to virtualenvwrapper's mkvirtualenv function to setup the settings and pythonpath variables, auto-install requirements, set the project path, and install git modules if possible."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
