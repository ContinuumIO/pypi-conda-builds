package:
  name: django-locator
  version: "1.0.0"

source:
  fn: django-locator-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-locator/django-locator-1.0.0.tar.gz
  md5: afd00d34c02f647e2dc63148bf8bc5f3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-locator = django-locator:main
    #
    # Would create an entry point called django-locator that calls django-locator.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - locator

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/overshard/django-locator
  license: BSD License
  summary: 'An easy to integrate store locator plugin for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
