package:
  name: harvest
  version: "2.2.3"

source:
  fn: harvest-2.2.3.tar.gz
  url: https://pypi.python.org/packages/source/h/harvest/harvest-2.2.3.tar.gz
  md5: 864f9c82939c3ae3d220c0e6f2121c8e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - harvest = harvest:main
    #
    # Would create an entry point called harvest that calls harvest.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fabric >=1.8
    - virtualenv >=1.11

  run:
    - python
    - fabric >=1.8
    - virtualenv >=1.11

test:
  # Python imports
  imports:
    - harvest
    - harvest.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/cbmi/harvest/
  license: BSD License
  summary: 'Harvest Command Line Tool'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
