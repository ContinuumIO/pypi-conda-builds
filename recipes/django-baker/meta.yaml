package:
  name: django-baker
  version: "0.11"

source:
  fn: django-baker-0.11.tar.gz
  url: https://pypi.python.org/packages/source/d/django-baker/django-baker-0.11.tar.gz
  md5: 8afc81e3a14328c9386e220adabc93f3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-baker = django-baker:main
    #
    # Would create an entry point called django-baker that calls django-baker.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_baker

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/krisfields/django-baker
  license: BSD License
  summary: 'Management command that generates views, forms, urls, admin, and templates for models'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
