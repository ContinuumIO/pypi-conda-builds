package:
  name: evilunit
  version: "0.1.3"

source:
  fn: evilunit-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/e/evilunit/evilunit-0.1.3.tar.gz
  md5: b7ed43ebb57d5a463d4609089c3cb547
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - evilunit = evilunit:main
    #
    # Would create an entry point called evilunit that calls evilunit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - evilunit
    - evilunit.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/podhmo/evilunit
  license: UNKNOWN
  summary: 'evil parts of unittest(python3 only)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
