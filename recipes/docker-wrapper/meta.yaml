package:
  name: docker-wrapper
  version: "0.6.0"

source:
  fn: docker-wrapper-0.6.0.tar.gz
  url: https://pypi.python.org/packages/source/d/docker-wrapper/docker-wrapper-0.6.0.tar.gz
  md5: 64f72ca1705e54479b0c8c1236c3a645
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - docker-wrapper = docker-wrapper:main
    #
    # Would create an entry point called docker-wrapper that calls docker-wrapper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - docker
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/frigg/docker-wrapper-py
  license: MIT
  summary: 'Docker Wrapper for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
