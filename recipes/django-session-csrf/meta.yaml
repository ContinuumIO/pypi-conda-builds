package:
  name: django-session-csrf
  version: "0.5"

source:
  fn: django-session-csrf-0.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-session-csrf/django-session-csrf-0.5.tar.gz
  md5: f191999c363a152b183f6088eac93ab7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-session-csrf = django-session-csrf:main
    #
    # Would create an entry point called django-session-csrf that calls django-session-csrf.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - session_csrf

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/mozilla/django-session-csrf
  license: BSD License
  summary: 'CSRF protection for Django without cookies.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
