package:
  name: kleenex
  version: "0.16.0"

source:
  fn: kleenex-0.16.0.tar.gz
  url: https://pypi.python.org/packages/source/k/kleenex/kleenex-0.16.0.tar.gz
  md5: 4305562c4638106b8dc0af073113e822
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kleenex = kleenex:main
    #
    # Would create an entry point called kleenex that calls kleenex.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - coverage >=3.5
    - nose >=0.9
    - simplejson
    - sqlalchemy >=0.7

  run:
    - python
    - setuptools
    - coverage >=3.5
    - nose >=0.9
    - simplejson
    - sqlalchemy >=0.7

test:
  # Python imports
  imports:
    - kleenex

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/dcramer/kleenex
  license: Apache License 2.0
  summary: 'A discovery plugin for Nose which relies on code coverage.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
