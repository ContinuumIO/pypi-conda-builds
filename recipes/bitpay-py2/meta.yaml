package:
  name: bitpay-py2
  version: "2.3.1"

source:
  fn: bitpay-py2-2.3.1.tar.gz
  url: https://pypi.python.org/packages/source/b/bitpay-py2/bitpay-py2-2.3.1.tar.gz
  md5: 7e178394da5f018be7c1fc1b1ca39d0f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bitpay-py2 = bitpay-py2:main
    #
    # Would create an entry point called bitpay-py2 that calls bitpay-py2.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - bitpay

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bitpay/bitpay-python
  license: MIT License
  summary: 'Accept bitcoin with BitPay'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
