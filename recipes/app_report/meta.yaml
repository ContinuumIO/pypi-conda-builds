package:
  name: app_report
  version: "0.0.3"

source:
  fn: app_report-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/a/app_report/app_report-0.0.3.tar.gz
  md5: 31bece415a098c7c5af7b99fdd8316fb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - app_report = app_report:main
    #
    # Would create an entry point called app_report that calls app_report.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests ==1.1.0
    - simplejson ==3.1.0

  run:
    - python
    - requests ==1.1.0
    - simplejson ==3.1.0

test:
  # Python imports
  imports:
    - app_report
    - app_report.api
    - app_report.helpers
    - app_report.helpers.reports
    - app_report.helpers.serializers
    - app_report.report
    - test
    - test.app_report
    - test.app_report.api
    - test.app_report.helpers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/simple_services/app_report-python
  license: GNU Lesser General Public License v3 or later (LGPLv3+)
  summary: 'Python client to AppReport API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
