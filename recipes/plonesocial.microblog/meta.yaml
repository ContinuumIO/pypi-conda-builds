package:
  name: plonesocial.microblog
  version: "0.5.3"

source:
  fn: plonesocial.microblog-0.5.3.zip
  url: https://pypi.python.org/packages/source/p/plonesocial.microblog/plonesocial.microblog-0.5.3.zip
  md5: 13a2deac1165808b9fd4784d80d1aee4
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plonesocial.microblog = plonesocial.microblog:main
    #
    # Would create an entry point called plonesocial.microblog that calls plonesocial.microblog.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - acquisition
    - plone.app.layout
    - plone.app.portlets
    - plone.app.uuid
    - plone.behavior
    - plone.portlets
    - plone.uuid
    - products.cmfcore
    - products.cmfplone >=4.2
    - products.genericsetup
    - setuptools
    - zope.component
    - zope.i18nmessageid
    - zope.interface
    - zope.schema

  run:
    - python
    - setuptools
    - acquisition
    - plone.app.layout
    - plone.app.portlets
    - plone.app.uuid
    - plone.behavior
    - plone.portlets
    - plone.uuid
    - products.cmfcore
    - products.cmfplone >=4.2
    - products.genericsetup
    - setuptools
    - zope.component
    - zope.i18nmessageid
    - zope.interface
    - zope.schema

test:
  # Python imports
  imports:
    - plonesocial
    - plonesocial.microblog
    - plonesocial.microblog.browser
    - plonesocial.microblog.portlets
    - plonesocial.microblog.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/cosent/plonesocial.microblog
  license: GNU General Public License v2 (GPLv2)
  summary: 'Simple microblogging in Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
