package:
  name: django-raise-exception-view
  version: "1.0.1"

source:
  fn: django-raise-exception-view-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-raise-exception-view/django-raise-exception-view-1.0.1.tar.gz
  md5: 88e2eb0202a60f790c00cf1986feaa8f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-raise-exception-view = django-raise-exception-view:main
    #
    # Would create an entry point called django-raise-exception-view that calls django-raise-exception-view.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - raiseexception

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/brightinteractive/django-raise-exception-view/
  license: BSD
  summary: 'Django view that raises an exception so you can test your logging setup'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
