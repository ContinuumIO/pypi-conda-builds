package:
  name: mr.monster
  version: "1.4"

source:
  fn: mr.monster-1.4.tar.gz
  url: https://pypi.python.org/packages/source/m/mr.monster/mr.monster-1.4.tar.gz
  md5: 75115a4a937dcb57f633dc503f3afcfb
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mr.monster = mr.monster:main
    #
    # Would create an entry point called mr.monster that calls mr.monster.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - webob

  run:
    - python
    - setuptools
    - setuptools
    - webob

test:
  # Python imports
  imports:
    - mr
    - mr.monster
    - mr.monster.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.teamrubber.com
  license: BSD
  summary: 'A URL rewriting middleware that emulates a VHM url.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
