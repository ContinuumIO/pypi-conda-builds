package:
  name: django-metadata
  version: "0.1.2"

source:
  fn: django-metadata-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-metadata/django-metadata-0.1.2.tar.gz
  md5: 187992c9db526617c54cdbf0356ee40d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-metadata = django-metadata:main
    #
    # Would create an entry point called django-metadata that calls django-metadata.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six

  run:
    - python
    - six

test:
  # Python imports
  imports:
    - metadata
    - metadata.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/thoas/django-metadata
  license: MIT License
  summary: 'Attach metadata to any Django models using redis'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
