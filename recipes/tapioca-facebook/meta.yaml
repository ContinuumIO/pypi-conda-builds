package:
  name: tapioca-facebook
  version: "0.2.2"

source:
  fn: tapioca-facebook-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/t/tapioca-facebook/tapioca-facebook-0.2.2.tar.gz
  md5: c812112160bb0cadd8485ca78337ec7b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tapioca-facebook = tapioca-facebook:main
    #
    # Would create an entry point called tapioca-facebook that calls tapioca-facebook.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - tapioca-wrapper ==0.2.1
    - requests-oauthlib ==0.4.2

  run:
    - python
    - tapioca-wrapper ==0.2.1
    - requests-oauthlib ==0.4.2

test:
  # Python imports
  imports:
    - tapioca_facebook

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/vintasoftware/tapioca-facebook
  license: BSD License
  summary: 'Facebook GraphAPI wrapper using tapioca'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
