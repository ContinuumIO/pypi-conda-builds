package:
  name: django-processedfilefield
  version: "0.2"

source:
  fn: django-processedfilefield-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-processedfilefield/django-processedfilefield-0.2.tar.gz
  md5: 2cb4bf411bdc59ada86335b69761f23c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-processedfilefield = django-processedfilefield:main
    #
    # Would create an entry point called django-processedfilefield that calls django-processedfilefield.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - processedfilefield

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/nathforge/django-processedfilefield
  license: UNKNOWN
  summary: 'Post-processing for Django FileFields.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
