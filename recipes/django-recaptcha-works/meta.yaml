package:
  name: django-recaptcha-works
  version: "0.3.4"

source:
  fn: django-recaptcha-works-0.3.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-recaptcha-works/django-recaptcha-works-0.3.4.tar.gz
  md5: fbe17c468dd56c826a15376d50b32d10
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-recaptcha-works = django-recaptcha-works:main
    #
    # Would create an entry point called django-recaptcha-works that calls django-recaptcha-works.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - recaptcha_works

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.codetrax.org/projects/django-recaptcha-works
  license: BSD License
  summary: 'django-recaptcha-works provides a Django form field that integrates the reCaptcha service.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
