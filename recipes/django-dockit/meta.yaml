package:
  name: django-dockit
  version: "0.0.13"

source:
  fn: django-dockit-0.0.13.tar.gz
  url: https://pypi.python.org/packages/source/d/django-dockit/django-dockit-0.0.13.tar.gz
  md5: 4be60efbd9328a61be4e859ab58ed370
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-dockit = django-dockit:main
    #
    # Would create an entry point called django-dockit that calls django-dockit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - dockit
    - dockit.admin
    - dockit.backends
    - dockit.backends.djangodocument
    - dockit.backends.djangodocument.migrations
    - dockit.backends.mongo
    - dockit.core
    - dockit.core.serializers
    - dockit.forms
    - dockit.schema
    - dockit.tests
    - dockit.tests.admin
    - dockit.tests.backends
    - dockit.tests.forms
    - dockit.tests.schema
    - dockit.tests.serializers
    - dockit.views

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/webcube/django-dockit
  license: New BSD License
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
