package:
  name: django-ccthumbs
  version: "0.0.4"

source:
  fn: django-ccthumbs-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-ccthumbs/django-ccthumbs-0.0.4.tar.gz
  md5: df7a1f5697b4d6889ef0df8e354e5f9c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-ccthumbs = django-ccthumbs:main
    #
    # Would create an entry point called django-ccthumbs that calls django-ccthumbs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - pillow

  run:
    - python
    - pillow

test:
  # Python imports
  imports:
    - ccthumbs

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/designcc/django-ccthumbs
  license: BSD 3 Clause
  summary: 'A thumbnail generating model field for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
