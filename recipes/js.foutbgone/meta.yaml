package:
  name: js.foutbgone
  version: "0.1"

source:
  fn: js.foutbgone-0.1.tar.gz
  url: https://pypi.python.org/packages/source/j/js.foutbgone/js.foutbgone-0.1.tar.gz
  md5: 96154f553b355a6fe600480bc106a894
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - js.foutbgone = js.foutbgone:main
    #
    # Would create an entry point called js.foutbgone that calls js.foutbgone.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fanstatic
    - setuptools

  run:
    - python
    - setuptools
    - fanstatic
    - setuptools

test:
  # Python imports
  imports:
    - js
    - js.foutbgone

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD
  summary: 'Fanstatic packaging of FOUT-B-Gone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
