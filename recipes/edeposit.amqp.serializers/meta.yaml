package:
  name: edeposit.amqp.serializers
  version: "1.1.1"

source:
  fn: edeposit.amqp.serializers-1.1.1.tar.gz
  url: https://pypi.python.org/packages/source/e/edeposit.amqp.serializers/edeposit.amqp.serializers-1.1.1.tar.gz
  md5: cb368bd9fd3bf8162c0fca77bf3fa6ab
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - edeposit.amqp.serializers = edeposit.amqp.serializers:main
    #
    # Would create an entry point called edeposit.amqp.serializers that calls edeposit.amqp.serializers.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - edeposit
    - edeposit.amqp
    - edeposit.amqp.serializers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/edeposit/edeposit.amqp.serializers/
  license: GNU General Public License v2 or later (GPLv2+)
  summary: "E-Deposit's AMQP definitions and common classes/patterns."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
