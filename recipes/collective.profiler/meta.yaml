package:
  name: collective.profiler
  version: "0.3"

source:
  fn: collective.profiler-0.3.zip
  url: https://pypi.python.org/packages/source/c/collective.profiler/collective.profiler-0.3.zip
  md5: 0f219fca8e9724607aa6e8b48a6167cc
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.profiler = collective.profiler:main
    #
    # Would create an entry point called collective.profiler that calls collective.profiler.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - profilehooks

  run:
    - python
    - setuptools
    - setuptools
    - profilehooks

test:
  # Python imports
  imports:
    - collective
    - collective.profiler

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.profiler
  license: GPL
  summary: "'Declare function for profiling in zcml and profileit'"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
