package:
  name: kss.demo
  version: "1.4.6"

source:
  fn: kss.demo-1.4.6.tar.gz
  url: https://pypi.python.org/packages/source/k/kss.demo/kss.demo-1.4.6.tar.gz
  md5: 36fac414af8b5ffdd75644d1dcc7363c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kss.demo = kss.demo:main
    #
    # Would create an entry point called kss.demo that calls kss.demo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - kss.core ==1.4.7
    - elementtree

  run:
    - python
    - setuptools
    - setuptools
    - kss.core ==1.4.7
    - elementtree

test:
  # Python imports
  imports:
    - kss
    - kss.demo
    - kss.demo.browser
    - kss.demo.configfeature
    - kss.demo.configfeature.tests
    - kss.demo.selenium_utils
    - kss.demo.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://kssproject.org
  license: GPL
  summary: 'KSS (Kinetic Style Sheets) demo'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
