package:
  name: audio.filters
  version: "0.2.2"

source:
  fn: audio.filters-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/a/audio.filters/audio.filters-0.2.2.tar.gz
  md5: 4dd446ea83ecf92ebee3c75e798a8450
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - audio.filters = audio.filters:main
    #
    # Would create an entry point called audio.filters that calls audio.filters.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - audio.fourier

  run:
    - python
    - audio.fourier

test:
  # Python imports
  imports:
    - audio

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/boisgera/audio.filters/
  license: MIT License
  summary: 'Audio Filters'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
