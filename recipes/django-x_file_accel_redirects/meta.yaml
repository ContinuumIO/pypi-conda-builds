package:
  name: django-x_file_accel_redirects
  version: "0.0.2"

source:
  fn: django-x_file_accel_redirects-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-x_file_accel_redirects/django-x_file_accel_redirects-0.0.2.tar.gz
  md5: 6ccfcd20ff83152febe373c7d0c45bc7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-x_file_accel_redirects = django-x_file_accel_redirects:main
    #
    # Would create an entry point called django-x_file_accel_redirects that calls django-x_file_accel_redirects.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-model-utils <1.4.0

  run:
    - python
    - django-model-utils <1.4.0

test:
  # Python imports
  imports:
    - x_file_accel_redirects
    - x_file_accel_redirects.migrations
    - x_file_accel_redirects.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/42cc/django-x-file-accel
  license: BSD License
  summary: 'Django app to easy configuration of multiple X-File-Accel locations'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
