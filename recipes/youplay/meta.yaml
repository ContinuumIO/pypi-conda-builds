package:
  name: youplay
  version: "0.1"

source:
  fn: youplay-0.1.tar.gz
  url: https://pypi.python.org/packages/source/y/youplay/youplay-0.1.tar.gz
  md5: 0e66b2899ad6bc1ddc23a91a5af0da08
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - youplay = youplay:main
    #
    # Would create an entry point called youplay that calls youplay.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - google-api-python-client >=1.2
    - httplib2 >=0.9
    - requests >=2.3.0
    - wsgiref >=0.1.2

  run:
    - python
    - google-api-python-client >=1.2
    - httplib2 >=0.9
    - requests >=2.3.0
    - wsgiref >=0.1.2

test:
  # Python imports
  imports:
    - youplay

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/youplay
  license: LICENSE.txt
  summary: "Extract who's and what's playing - artist(s) and track(s) - from a YouTube music video."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
