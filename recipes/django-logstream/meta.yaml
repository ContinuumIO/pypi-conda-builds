package:
  name: django-logstream
  version: "1.2"

source:
  fn: django-logstream-1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-logstream/django-logstream-1.2.tar.gz
  md5: c98a7905766c6d148276c730df113fcb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-logstream = django-logstream:main
    #
    # Would create an entry point called django-logstream that calls django-logstream.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - distribute
    - pyzmq >=2.1.10
    - pycrypto >=2.4.1

  run:
    - python
    - distribute
    - pyzmq >=2.1.10
    - pycrypto >=2.4.1

test:
  # Python imports
  imports:
    - django_logstream
    - django_logstream.client
    - django_logstream.client.handlers
    - django_logstream.server
    - django_logstream.server.backends
    - django_logstream.server.backends.zeromq
    - django_logstream.server.management
    - django_logstream.server.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/niwibe/django-logstream
  license: BSD License
  summary: 'Multiprocess log collector for python/django-logging'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
