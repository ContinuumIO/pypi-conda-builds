package:
  name: django-email-obfuscator
  version: "0.1.4"

source:
  fn: django-email-obfuscator-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-email-obfuscator/django-email-obfuscator-0.1.4.tar.gz
  md5: 856901af13e3002967ab21f729bc3f6a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-email-obfuscator = django-email-obfuscator:main
    #
    # Would create an entry point called django-email-obfuscator that calls django-email-obfuscator.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - email_obfuscator

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/morninj/django-email-obfuscator
  license: MIT License
  summary: 'A Django template filter to protect email addresses from     crawlers.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
