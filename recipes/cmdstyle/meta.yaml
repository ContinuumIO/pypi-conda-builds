package:
  name: cmdstyle
  version: "0.1.0"

source:
  fn: cmdstyle-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/c/cmdstyle/cmdstyle-0.1.0.tar.gz
  md5: 056d1998a9efff3032ef358b732403c9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cmdstyle = cmdstyle:main
    #
    # Would create an entry point called cmdstyle that calls cmdstyle.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - cmdstyle

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypy.python.org/pypi/cmdstyle/
  license: Apache Software License
  summary: 'Command line interface framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
