package:
  name: diy-filingsresearch
  version: "0.3.0"

source:
  fn: DIY-FilingsResearch-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/D/DIY-FilingsResearch/DIY-FilingsResearch-0.3.0.tar.gz
  md5: 50ada8c1220784404c78331ab5810fef
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - diy-filingsresearch = diy-filingsresearch:main
    #
    # Would create an entry point called diy-filingsresearch that calls diy-filingsresearch.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pytest
    - requests
    - lxml
    - futures
    - selenium

  run:
    - python
    - pytest
    - requests
    - lxml
    - futures
    - selenium

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/greedo/DIY-FilingsResearch/
  license: Apache License
  summary: 'library for analyzing company financial documents'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
