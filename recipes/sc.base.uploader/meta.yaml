package:
  name: sc.base.uploader
  version: "1.0b3"

source:
  fn: sc.base.uploader-1.0b3.zip
  url: https://pypi.python.org/packages/source/s/sc.base.uploader/sc.base.uploader-1.0b3.zip
  md5: c8a035a604493e6a727792bc8fdc6036
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sc.base.uploader = sc.base.uploader:main
    #
    # Would create an entry point called sc.base.uploader that calls sc.base.uploader.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.zipfiletransport ==3.2.1
    - collective.quickupload ==1.5.8

  run:
    - python
    - setuptools
    - setuptools
    - collective.zipfiletransport ==3.2.1
    - collective.quickupload ==1.5.8

test:
  # Python imports
  imports:
    - sc
    - sc.base
    - sc.base.uploader
    - sc.base.uploader.browser
    - sc.base.uploader.tests
    - sc.base.uploader.upgrades

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/simplesconsultoria/sc.base.uploader
  license: GPL
  summary: 'A Plone package providing multiple-files upload'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
