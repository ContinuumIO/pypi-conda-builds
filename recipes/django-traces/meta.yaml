package:
  name: django-traces
  version: "0.4"

source:
  fn: django-traces-0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-traces/django-traces-0.4.tar.gz
  md5: fdef2512b2568f1238e94d611bfc5bdd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-traces = django-traces:main
    #
    # Would create an entry point called django-traces that calls django-traces.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - traces
    - traces.migrations
    - traces.templatetags
    - traces.tests
    - traces.tests.test_app

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bitmazk/django-traces
  license: The MIT License
  summary: 'A reusable app to track view hits'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
