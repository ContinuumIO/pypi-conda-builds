package:
  name: clyde-manpage
  version: "0.1.1"

source:
  fn: clyde-manpage-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/c/clyde-manpage/clyde-manpage-0.1.1.tar.gz
  md5: 979f6651c61c23dfc87fe9aa6740b7ca
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - clyde-manpage = clyde-manpage:main
    #
    # Would create an entry point called clyde-manpage that calls clyde-manpage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - clyde

  run:
    - python
    - clyde

test:
  # Python imports
  imports:
    - clyde_manpage

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/clyde-hub/clyde-manpage
  license: MIT License
  summary: 'Clyde-manpage is a manpage plugin for Clyde.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
