package:
  name: sc.base.hotsites
  version: "1.0"

source:
  fn: sc.base.hotsites-1.0.tar.gz
  url: https://pypi.python.org/packages/source/s/sc.base.hotsites/sc.base.hotsites-1.0.tar.gz
  md5: eaa6b51c4aa8ee31175d56f25ad7cf8b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sc.base.hotsites = sc.base.hotsites:main
    #
    # Would create an entry point called sc.base.hotsites that calls sc.base.hotsites.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - p4a.subtyper

  run:
    - python
    - setuptools
    - setuptools
    - p4a.subtyper

test:
  # Python imports
  imports:
    - sc
    - sc.base
    - sc.base.hotsites
    - sc.base.hotsites.adapters
    - sc.base.hotsites.browser
    - sc.base.hotsites.controlpanel
    - sc.base.hotsites.docs

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.simplesconsultoria.com.br/
  license: GPL
  summary: 'Transform a Plone Folder in a Hot Site'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
