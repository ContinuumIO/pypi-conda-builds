package:
  name: django-cerberus
  version: "0.3.1"

source:
  fn: django-cerberus-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-cerberus/django-cerberus-0.3.1.tar.gz
  md5: eff23fa8ce89c403c358b8fd437089b4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-cerberus = django-cerberus:main
    #
    # Would create an entry point called django-cerberus that calls django-cerberus.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - cerberus

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/PragmaticMates/django-cerberus
  license: BSD License
  summary: 'Django app that locks out users after too many failed login attempts.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
