package:
  name: netfilterqueue
  version: "0.3"

source:
  fn: NetfilterQueue-0.3.tar.gz
  url: https://pypi.python.org/packages/source/N/NetfilterQueue/NetfilterQueue-0.3.tar.gz
  md5: 4bc4cab885a0326fad3f6d8e2c5c3b09
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - netfilterqueue = netfilterqueue:main
    #
    # Would create an entry point called netfilterqueue that calls netfilterqueue.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - netfilterqueue

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kti/python-netfilterqueue
  license: BSD License
  summary: 'Python bindings for libnetfilter_queue'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
