package:
  name: human_curl
  version: "0.1.9"

source:
  fn: human_curl-0.1.9.tar.gz
  url: https://pypi.python.org/packages/source/h/human_curl/human_curl-0.1.9.tar.gz
  md5: 3789fac03e29b2356f35eeb0da541002
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - human_curl = human_curl:main
    #
    # Would create an entry point called human_curl that calls human_curl.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pycurl2

  run:
    - python
    - pycurl2

test:
  # Python imports
  imports:
    - human_curl

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lispython/human_curl
  license: BSD License
  summary: 'Simple cURL wrapper for Humans'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
