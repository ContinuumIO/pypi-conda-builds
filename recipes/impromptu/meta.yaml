package:
  name: impromptu
  version: "0.1.1b"

source:
  fn: Impromptu-0.1.1b.tar.bz2
  url: https://pypi.python.org/packages/source/I/Impromptu/Impromptu-0.1.1b.tar.bz2
  md5: 1e368c6006157f8574001e532ab9797f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - impromptu = impromptu:main
    #
    # Would create an entry point called impromptu that calls impromptu.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyglet

  run:
    - python
    - pyglet

test:
  # Python imports
  imports:
    - impromptu

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://chrisarndt.de/projects/impromptu/
  license: Python Software Foundation License
  summary: 'A graphical teleprompter-like scrolling HTML text display'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
