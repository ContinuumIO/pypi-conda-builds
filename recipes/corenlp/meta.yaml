package:
  name: corenlp
  version: "0.0.14"

source:
  fn: corenlp-0.0.14.tar.gz
  url: https://pypi.python.org/packages/source/c/corenlp/corenlp-0.0.14.tar.gz
  md5: fbf7c4e5cfdebcbbff473733df89cde5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - corenlp = corenlp:main
    #
    # Would create an entry point called corenlp that calls corenlp.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - nltk

  run:
    - python
    - nltk

test:
  # Python imports
  imports:
    - corenlp

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kedz/corenlp
  license: UNKNOWN
  summary: 'A python wrapper for the Stanford CoreNLP java library.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
