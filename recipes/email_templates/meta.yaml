package:
  name: email_templates
  version: "0.2.2"

source:
  fn: email_templates-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/e/email_templates/email_templates-0.2.2.tar.gz
  md5: 58a1b7ae8df3c7c321ec4c8029408c90
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - email_templates = email_templates:main
    #
    # Would create an entry point called email_templates that calls email_templates.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - email_templates
    - emailtemplates

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: emailtogo
  summary: 'Django email templates'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
