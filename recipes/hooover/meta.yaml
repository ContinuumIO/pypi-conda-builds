package:
  name: hooover
  version: "0.0.1"

source:
  fn: Hooover-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/H/Hooover/Hooover-0.0.1.tar.gz
  md5: b7cffb8863490eafbf4db975dbdef3eb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hooover = hooover:main
    #
    # Would create an entry point called hooover that calls hooover.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - hooover

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/juztin/hooover
  license: UNKNOWN
  summary: 'Library for logging to Loggly from within Python webapps'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
