package:
  name: gbif
  version: "0.1.0"

source:
  fn: gbif-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/g/gbif/gbif-0.1.0.tar.gz
  md5: 8335471ffa9ddd0d8a86253fee79fc10
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gbif = gbif:main
    #
    # Would create an entry point called gbif that calls gbif.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - restkit

  run:
    - python
    - restkit

test:
  # Python imports
  imports:
    - gbif

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/matagus/python-gbif
  license: MIT License
  summary: 'Basic and experimental Gbif Taxon API library for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
