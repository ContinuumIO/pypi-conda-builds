package:
  name: collective.folderishpage
  version: "0.8"

source:
  fn: collective.folderishpage-0.8.zip
  url: https://pypi.python.org/packages/source/c/collective.folderishpage/collective.folderishpage-0.8.zip
  md5: c1f11147eeca36649ca2d801326000ae
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.folderishpage = collective.folderishpage:main
    #
    # Would create an entry point called collective.folderishpage that calls collective.folderishpage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.folderishpage
    - collective.folderishpage.browser
    - collective.folderishpage.content
    - collective.folderishpage.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.fourdigits.nl
  license: GNU General Public License (GPL)
  summary: 'A product which adds a folderish page contenttype.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
