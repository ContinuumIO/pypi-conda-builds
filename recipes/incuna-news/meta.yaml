package:
  name: incuna-news
  version: "1.3"

source:
  fn: incuna-news-1.3.tar.gz
  url: https://pypi.python.org/packages/source/i/incuna-news/incuna-news-1.3.tar.gz
  md5: 982a9f476d8bfdec2dc4beb63783a210
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - incuna-news = incuna-news:main
    #
    # Would create an entry point called incuna-news that calls incuna-news.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-tagging >=0.3.1

  run:
    - python
    - django-tagging >=0.3.1

test:
  # Python imports
  imports:
    - news
    - news.migrations
    - news.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://incuna.com/
  license: UNKNOWN
  summary: 'Provides news.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
