package:
  name: iencode-ng
  version: "0.9.5"

source:
  fn: iencode-ng-0.9.5.tar.gz
  url: https://pypi.python.org/packages/source/i/iencode-ng/iencode-ng-0.9.5.tar.gz
  md5: 793797119d68cba6ffafb0948bc7af23
#  patches:
   # List any patch files here
   # - fix.patch

build:
  # preserve_egg_dir: True
  entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - iencode-ng = iencode-ng:main
    #
    # Would create an entry point called iencode-ng that calls iencode-ng.main()

    - iencode = iencode:main

  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  # imports:

  commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.

    - iencode --help

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/tuxtof/iencode-ng
  license: GPLv2
  summary: 'iPhone video encoding tools.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
