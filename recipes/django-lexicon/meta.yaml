package:
  name: django-lexicon
  version: "0.1.0"

source:
  fn: django-lexicon-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-lexicon/django-lexicon-0.1.0.tar.gz
  md5: 39066a4e8e0a1f5d175a7fd5ba52df50
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-lexicon = django-lexicon:main
    #
    # Would create an entry point called django-lexicon that calls django-lexicon.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4

  run:
    - python
    - django >=1.4

test:
  # Python imports
  imports:
    - lexicon

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://cbmi.github.io/django-lexicon/
  license: BSD License
  summary: 'Abstract classes for defining a lexicon'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
