package:
  name: awasu-tools
  version: "1.0.1"

source:
  fn: awasu-tools-1.0.1.zip
  url: https://pypi.python.org/packages/source/a/awasu-tools/awasu-tools-1.0.1.zip
  md5: f8a9a985915835ba02dc170ef2fb85d9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - awasu-tools = awasu-tools:main
    #
    # Would create an entry point called awasu-tools that calls awasu-tools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - awasu_tools

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://awasu.com/awasu_tools
  license: UNKNOWN
  summary: 'Tools for writing Awasu extensions.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
