package:
  name: hrdns
  version: "dev"

source:
  fn: hrdns-dev.tar.gz
  url: https://pypi.python.org/packages/source/h/hrdns/hrdns-dev.tar.gz
  md5: 15106d65290d776754591b83bfd3a499
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hrdns = hrdns:main
    #
    # Would create an entry point called hrdns that calls hrdns.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyquery
    - pycli

  run:
    - python
    - pyquery
    - pycli

test:
  # Python imports
  imports:
    - hrdns

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/devop/hrdns
  license: X11
  summary: 'Small tool created for management of hetzner dns service from commandline'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
