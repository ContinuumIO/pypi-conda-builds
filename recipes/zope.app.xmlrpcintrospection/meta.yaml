package:
  name: zope.app.xmlrpcintrospection
  version: "3.5.1"

source:
  fn: zope.app.xmlrpcintrospection-3.5.1.tar.gz
  url: https://pypi.python.org/packages/source/z/zope.app.xmlrpcintrospection/zope.app.xmlrpcintrospection-3.5.1.tar.gz
  md5: 8cf0e7fc13ece3743f96f92d05682d74
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zope.app.xmlrpcintrospection = zope.app.xmlrpcintrospection:main
    #
    # Would create an entry point called zope.app.xmlrpcintrospection that calls zope.app.xmlrpcintrospection.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.component
    - zope.interface
    - zope.publisher

  run:
    - python
    - setuptools
    - zope.component
    - zope.interface
    - zope.publisher

test:
  # Python imports
  imports:
    - zope
    - zope.app
    - zope.app.xmlrpcintrospection
    - zope.app.xmlrpcintrospection.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/zope.app.xmlrpcintrospection
  license: Zope Public License
  summary: 'XML-RPC Method Introspection Support for Zope 3'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
