package:
  name: django-jsgettext
  version: "0.5"

source:
  fn: django-jsgettext-0.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-jsgettext/django-jsgettext-0.5.tar.gz
  md5: 5ab619ebc29090cfc73df648a8570a3c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-jsgettext = django-jsgettext:main
    #
    # Would create an entry point called django-jsgettext that calls django-jsgettext.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - djsgettext
    - djsgettext.management
    - djsgettext.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/niwibe/django-jsgettext
  license: BSD License
  summary: 'Improved and complement gettext support for javascript for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
