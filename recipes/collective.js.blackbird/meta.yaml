package:
  name: collective.js.blackbird
  version: "1.2.2"

source:
  fn: collective.js.blackbird-1.2.2.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.js.blackbird/collective.js.blackbird-1.2.2.tar.gz
  md5: c8a447dd9f1996e4a17c5fb3ed945bc9
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.js.blackbird = collective.js.blackbird:main
    #
    # Would create an entry point called collective.js.blackbird that calls collective.js.blackbird.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.js
    - collective.js.blackbird
    - collective.js.blackbird.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.gscottolson.com/blackbirdjs/
  license: GPL
  summary: 'A Plone add-on package that provides Blackbird.js, an Open-Source Javascript logging utility.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
