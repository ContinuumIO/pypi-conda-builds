package:
  name: redturtle.maps.portlet
  version: "0.0.1.1"

source:
  fn: redturtle.maps.portlet-0.0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/r/redturtle.maps.portlet/redturtle.maps.portlet-0.0.1.1.tar.gz
  md5: b7fdb36b63d05e5c1c2f84901d5b1472
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - redturtle.maps.portlet = redturtle.maps.portlet:main
    #
    # Would create an entry point called redturtle.maps.portlet that calls redturtle.maps.portlet.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - redturtle.maps.core

  run:
    - python
    - setuptools
    - setuptools
    - redturtle.maps.core

test:
  # Python imports
  imports:
    - redturtle
    - redturtle.maps
    - redturtle.maps.portlet
    - redturtle.maps.portlet.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.redturtle.it
  license: GPL
  summary: 'Portlet for maps.core'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
