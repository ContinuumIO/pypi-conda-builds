package:
  name: django-cleanup
  version: "0.2.1"

source:
  fn: django-cleanup-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-cleanup/django-cleanup-0.2.1.tar.gz
  md5: c2bd0f5047b9576e8863ec67c58abfbc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-cleanup = django-cleanup:main
    #
    # Would create an entry point called django-cleanup that calls django-cleanup.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_cleanup

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/un1t/django-cleanup
  license: MIT License
  summary: 'Deletes old files.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
