package:
  name: collective.kss.inlinejs
  version: "1.0"

source:
  fn: collective.kss.inlinejs-1.0.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.kss.inlinejs/collective.kss.inlinejs-1.0.tar.gz
  md5: 451a16d5168d1cfc3d6403dd66f63bee
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.kss.inlinejs = collective.kss.inlinejs:main
    #
    # Would create an entry point called collective.kss.inlinejs that calls collective.kss.inlinejs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - kss.core

  run:
    - python
    - setuptools
    - setuptools
    - kss.core

test:
  # Python imports
  imports:
    - collective
    - collective.kss
    - collective.kss.inlinejs
    - collective.kss.inlinejs.demo
    - collective.kss.inlinejs.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.redomino.com
  license: GPL
  summary: 'Plugin that allows you to put javascript code evaluated by kss'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
