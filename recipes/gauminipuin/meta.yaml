package:
  name: gauminipuin
  version: "0.2"

source:
  fn: GauminIpuin-0.2.tar.gz
  url: https://pypi.python.org/packages/source/G/GauminIpuin/GauminIpuin-0.2.tar.gz
  md5: 288cdd691fea920a40fec7380c8ea2dc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gauminipuin = gauminipuin:main
    #
    # Would create an entry point called gauminipuin that calls gauminipuin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - gauminipuin
    - ipuin
    - ipuin.games
    - ipuin.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://lagunak.gisa-elkartea.org/projects/gauminipuin/
  license: GPLv3+
  summary: 'Interactive book library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
