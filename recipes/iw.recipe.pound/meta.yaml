package:
  name: iw.recipe.pound
  version: "0.4.1"

source:
  fn: iw.recipe.pound-0.4.1.tar.gz
  url: https://pypi.python.org/packages/source/i/iw.recipe.pound/iw.recipe.pound-0.4.1.tar.gz
  md5: 2bff4ce6f8e8d9d6eacd8ccdc999c536
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - iw.recipe.pound = iw.recipe.pound:main
    #
    # Would create an entry point called iw.recipe.pound that calls iw.recipe.pound.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - zc.recipe.cmmi
    - cheetah

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - zc.recipe.cmmi
    - cheetah

test:
  # Python imports
  imports:
    - iw
    - iw.recipe
    - iw.recipe.pound
    - iw.recipe.pound.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/iw-recipes
  license: GPL
  summary: 'Recipe to install and configure Pound'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
