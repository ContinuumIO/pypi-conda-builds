package:
  name: clld
  version: "0.32.1"

source:
  fn: clld-0.32.1.tar.gz
  url: https://pypi.python.org/packages/source/c/clld/clld-0.32.1.tar.gz
  md5: 77f1b8c5c96ffe492f47491568435973
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - clld = clld:main
    #
    # Would create an entry point called clld that calls clld.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools >=0.8
    - pyramid >=1.5.1
    - pyramid_mako >=1.0
    - pyramid_tm
    - sqlalchemy >=0.9.8,<=0.9.9
    - pastedeploy >=1.5.0
    - waitress
    - purl >=0.5
    - path.py
    - pyramid_exclog
    - pytz
    - zope.sqlalchemy
    - webtest
    - six >=1.7.3
    - alembic >=0.7.1
    - webassets
    - yuicompressor
    - markupsafe
    - requests
    - rdflib >=4.1.1
    - colander
    - python-dateutil
    - paginate
    - html5lib ==0.999
    - xlrd
    - xlwt-future
    - webhelpers2 >=2.0rc1
    - nameparser
    - babel

  run:
    - python
    - setuptools >=0.8
    - pyramid >=1.5.1
    - pyramid_mako >=1.0
    - pyramid_tm
    - sqlalchemy >=0.9.8,<=0.9.9
    - pastedeploy >=1.5.0
    - waitress
    - purl >=0.5
    - path.py
    - pyramid_exclog
    - pytz
    - zope.sqlalchemy
    - webtest
    - six >=1.7.3
    - alembic >=0.7.1
    - webassets
    - yuicompressor
    - markupsafe
    - requests
    - rdflib >=4.1.1
    - colander
    - python-dateutil
    - paginate
    - html5lib ==0.999
    - xlrd
    - xlwt-future
    - webhelpers2 >=2.0rc1
    - nameparser
    - babel

test:
  # Python imports
  imports:
    - clld
    - clld.db
    - clld.db.models
    - clld.lib
    - clld.scaffolds
    - clld.scripts
    - clld.tests
    - clld.web
    - clld.web.adapters
    - clld.web.datatables
    - clld.web.maps
    - clld.web.util
    - clld.web.views

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://clld.org
  license: Apache Software License
  summary: u'Python library supporting the development of cross-linguistic databases'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
