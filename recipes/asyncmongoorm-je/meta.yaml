package:
  name: asyncmongoorm-je
  version: "0.3.7"

source:
  fn: asyncmongoorm-je-0.3.7.tar.gz
  url: https://pypi.python.org/packages/source/a/asyncmongoorm-je/asyncmongoorm-je-0.3.7.tar.gz
  md5: d764b493eede0592941ec9722ed24932
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - asyncmongoorm-je = asyncmongoorm-je:main
    #
    # Would create an entry point called asyncmongoorm-je that calls asyncmongoorm-je.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - asyncmongoorm

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/marcelnicolay/asyncmongoorm
  license: OSI
  summary: 'asyncmongoorm'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
