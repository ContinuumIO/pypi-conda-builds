package:
  name: explode
  version: "1.0.6"

source:
  fn: explode-1.0.6.tar.gz
  url: https://pypi.python.org/packages/source/e/explode/explode-1.0.6.tar.gz
  md5: 48c11e6dc3293fabdd61eeaed0a113a3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - explode = explode:main
    #
    # Would create an entry point called explode that calls explode.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - explode

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Apache Software License
  summary: 'Explode python data, yaml or json into flat files in directories'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
