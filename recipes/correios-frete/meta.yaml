package:
  name: correios-frete
  version: "1.0.0"

source:
  fn: correios-frete-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/c/correios-frete/correios-frete-1.0.0.tar.gz
  md5: e097cce36c0566a40c0be559231d834d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - correios-frete = correios-frete:main
    #
    # Would create an entry point called correios-frete that calls correios-frete.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - suds

  run:
    - python
    - suds

test:
  # Python imports
  imports:
    - correios_frete

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/adonescunha/correios-frete
  license: MIT License
  summary: 'Cliente para o WebService de consulta de frete dos correios.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
