package:
  name: lipidconverter
  version: "0.1.6"

source:
  fn: LipidConverter-0.1.6.tar.gz
  url: https://pypi.python.org/packages/source/L/LipidConverter/LipidConverter-0.1.6.tar.gz
  md5: 74f243db4d43cb79f984af926c28d378
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lipidconverter = lipidconverter:main
    #
    # Would create an entry point called lipidconverter that calls lipidconverter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy >=1.6
    - networkx >=1.9
    - python-gflags >=1.8

  run:
    - python
    - numpy >=1.6
    - networkx >=1.9
    - python-gflags >=1.8

test:
  # Python imports
  imports:
    - lipid_conv
    - lipidconverter

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://lipid-converter.appspot.com
  license: LICENSE.txt
  summary: 'Useful descripton text'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
