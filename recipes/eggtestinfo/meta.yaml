package:
  name: eggtestinfo
  version: "0.3"

source:
  fn: eggtestinfo-0.3.tar.gz
  url: https://pypi.python.org/packages/source/e/eggtestinfo/eggtestinfo-0.3.tar.gz
  md5: 6f0507aee05f00c640c0d64b5073f840
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - eggtestinfo = eggtestinfo:main
    #
    # Would create an entry point called eggtestinfo that calls eggtestinfo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - eggtestinfo

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/eggtestinfo
  license: Python Software Foundation License or Zope Public License
  summary: 'Add test information to .egg-info'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
