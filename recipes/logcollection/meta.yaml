package:
  name: logcollection
  version: "0-1-0"

source:
  fn: logcollection-0-1-0.tar.gz
  url: https://pypi.python.org/packages/source/l/logcollection/logcollection-0-1-0.tar.gz
  md5: bafd9cca510eb03af0f4a34057437a1b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - logcollection = logcollection:main
    #
    # Would create an entry point called logcollection that calls logcollection.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests
    - hipchat
    - python-redmine
    - fluent-logger

  run:
    - python
    - setuptools
    - requests
    - hipchat
    - python-redmine
    - fluent-logger

test:
  # Python imports
  imports:
    - logcollection

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/TakesxiSximada/logcollection
  license: See http://www.python.org/3.4/license.html
  summary: 'logging handler collection'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
