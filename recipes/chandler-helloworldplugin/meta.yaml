package:
  name: chandler-helloworldplugin
  version: "0.7"

source:
  fn: Chandler-HelloWorldPlugin-0.7.tar.gz
  url: https://pypi.python.org/packages/source/C/Chandler-HelloWorldPlugin/Chandler-HelloWorldPlugin-0.7.tar.gz
  md5: ef8523f51d83e679b3a5da78d378991d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - chandler-helloworldplugin = chandler-helloworldplugin:main
    #
    # Would create an entry point called chandler-helloworldplugin that calls chandler-helloworldplugin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - hello_world

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Apache Software License
  summary: 'This is just an example plugin project'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
