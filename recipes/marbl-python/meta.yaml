package:
  name: marbl-python
  version: "2.0.3"

source:
  fn: marbl-python-2.0.3.tar.gz
  url: https://pypi.python.org/packages/source/m/marbl-python/marbl-python-2.0.3.tar.gz
  md5: 43c57c4f9257c43f2ea97122cd763179
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - marbl-python = marbl-python:main
    #
    # Would create an entry point called marbl-python that calls marbl-python.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - msgpack-python >=0.4.2,<1.0.0
    - numpy >=1.8.1,<2.0.0

  run:
    - python
    - msgpack-python >=0.4.2,<1.0.0
    - numpy >=1.8.1,<2.0.0

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/wmayner/marbl-python
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'A Python implementation of the Marbl specification for normalized representations of Markov blankets in Bayesian networks.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
