package:
  name: flake8-import-order
  version: "0.6.1"

source:
  fn: flake8-import-order-0.6.1.tar.gz
  url: https://pypi.python.org/packages/source/f/flake8-import-order/flake8-import-order-0.6.1.tar.gz
  md5: 3c13a691096f2eac24a2097028fb1ce4
#  patches:
   # List any patch files here
   # - fix.patch

build:
  noarch_python: True
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flake8-import-order = flake8-import-order:main
    #
    # Would create an entry point called flake8-import-order that calls flake8-import-order.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pep8

  run:
    - python
    - setuptools
    - pep8

test:
  # Python imports
  imports:
    - flake8_import_order

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/public/flake8-import-order
  license: MIT License or GNU Lesser General Public License v3 (LGPLv3)
  summary: 'Flake8 and pylama plugin that checks the ordering of import statements.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
