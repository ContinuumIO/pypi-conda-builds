package:
  name: flask-cassandra
  version: "0.13"

source:
  fn: Flask-Cassandra-0.13.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Cassandra/Flask-Cassandra-0.13.tar.gz
  md5: f45d18e6f7ed1dfd60ef53c5f139a445
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-cassandra = flask-cassandra:main
    #
    # Would create an entry point called flask-cassandra that calls flask-cassandra.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - cassandra-driver

  run:
    - python
    - flask
    - cassandra-driver

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://terbiumlabs.com/flask-cassandra/
  license: BSD License
  summary: 'Provides a connection to a Cassandra cluster in a Flask app'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
