package:
  name: zope.broken
  version: "3.6.0"

source:
  fn: zope.broken-3.6.0.zip
  url: https://pypi.python.org/packages/source/z/zope.broken/zope.broken-3.6.0.zip
  md5: eff24d7918099a3e899ee63a9c31bee6
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zope.broken = zope.broken:main
    #
    # Would create an entry point called zope.broken that calls zope.broken.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.interface

  run:
    - python
    - setuptools
    - zope.interface

test:
  # Python imports
  imports:
    - zope
    - zope.broken

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/zope.broken
  license: Zope Public License
  summary: 'Zope Broken Object Interfaces'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
