package:
  name: isotoma.recipe.distros
  version: "0.0.0"

source:
  fn: isotoma.recipe.distros-0.0.0.tar.gz
  url: https://pypi.python.org/packages/source/i/isotoma.recipe.distros/isotoma.recipe.distros-0.0.0.tar.gz
  md5: 20b2c1f8d7a07cf2ec5c5a0db91d28fa
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - isotoma.recipe.distros = isotoma.recipe.distros:main
    #
    # Would create an entry point called isotoma.recipe.distros that calls isotoma.recipe.distros.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zc.buildout
    - setuptools

  run:
    - python
    - setuptools
    - zc.buildout
    - setuptools

test:
  # Python imports
  imports:
    - isotoma
    - isotoma.recipe
    - isotoma.recipe.distros

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/plone.recipe.distros
  license: Zope Public License
  summary: 'Recipe to install old style products.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
