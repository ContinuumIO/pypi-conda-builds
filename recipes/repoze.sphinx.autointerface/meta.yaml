package:
  name: repoze.sphinx.autointerface
  version: "0.7.1"

source:
  fn: repoze.sphinx.autointerface-0.7.1.tar.gz
  url: https://pypi.python.org/packages/source/r/repoze.sphinx.autointerface/repoze.sphinx.autointerface-0.7.1.tar.gz
  md5: f2fee996ae28dc16eb48f1a3e8f64801
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - repoze.sphinx.autointerface = repoze.sphinx.autointerface:main
    #
    # Would create an entry point called repoze.sphinx.autointerface that calls repoze.sphinx.autointerface.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - zope.interface
    - sphinx >=0.6.1
    - setuptools

  run:
    - python
    - zope.interface
    - sphinx >=0.6.1
    - setuptools

test:
  # Python imports
  imports:
    - repoze
    - repoze.sphinx

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.repoze.org
  license: BSD-derived (http://www.repoze.org/LICENSE.txt)
  summary: 'Sphinx extension: auto-generates API docs from Zope interfaces'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
