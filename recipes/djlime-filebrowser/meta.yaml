package:
  name: djlime-filebrowser
  version: "0.3"

source:
  fn: djlime-filebrowser-0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/djlime-filebrowser/djlime-filebrowser-0.3.tar.gz
  md5: d2d3c7b20385bcef12cd5c607b8163b5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djlime-filebrowser = djlime-filebrowser:main
    #
    # Would create an entry point called djlime-filebrowser that calls djlime-filebrowser.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5
    - simplejson

  run:
    - python
    - django >=1.5
    - simplejson

test:
  # Python imports
  imports:
    - djlime_filebrowser
    - djlime_filebrowser.management
    - djlime_filebrowser.management.commands
    - djlime_filebrowser.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/freshlimestudio/djlime-filebrowser
  license: BSD License
  summary: 'Media-Management with the Django Admin-Interface.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
