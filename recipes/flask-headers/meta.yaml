package:
  name: flask-headers
  version: "1.0"

source:
  fn: Flask-Headers-1.0.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Headers/Flask-Headers-1.0.tar.gz
  md5: 23b9900d896f8de205fa43f91e86a7b0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-headers = flask-headers:main
    #
    # Would create an entry point called flask-headers that calls flask-headers.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask

  run:
    - python
    - flask

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/wcdolphin/flask-headers
  license: BSD License
  summary: 'A Flask extension making adding headers one decorator away'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
