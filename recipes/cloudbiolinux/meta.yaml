package:
  name: cloudbiolinux
  version: "0.3a"

source:
  fn: cloudbiolinux-0.3a.tar.gz
  url: https://pypi.python.org/packages/source/c/cloudbiolinux/cloudbiolinux-0.3a.tar.gz
  md5: accc894614fd6d28a8455bddbec2ab3d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cloudbiolinux = cloudbiolinux:main
    #
    # Would create an entry point called cloudbiolinux that calls cloudbiolinux.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyyaml >=3.09
    - fabric >=1.1.1

  run:
    - python
    - pyyaml >=3.09
    - fabric >=1.1.1

test:
  # Python imports
  imports:
    - cloudbio
    - cloudbio.biodata
    - cloudbio.custom
    - cloudbio.edition
    - cloudbio.flavor
    - cloudbio.galaxy
    - cloudbio.package
    - cloudbiolinux
    - contrib
    - contrib.flavor
    - contrib.flavor.boinc
    - contrib.flavor.phylogeny
    - contrib.flavor.pjotrp
    - contrib.flavor.pjotrp.biotest
    - contrib.flavor.seal

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://cloudbiolinux.org
  license: MIT
  summary: 'configure virtual (or real) machines with tools for biological analyses'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
