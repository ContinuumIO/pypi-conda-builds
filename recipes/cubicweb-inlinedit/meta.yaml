package:
  name: cubicweb-inlinedit
  version: "1.2.1"

source:
  fn: cubicweb-inlinedit-1.2.1.tar.gz
  url: https://pypi.python.org/packages/source/c/cubicweb-inlinedit/cubicweb-inlinedit-1.2.1.tar.gz
  md5: c036fbe99b2b1ce7dd386e06bdb9ddaf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cubicweb-inlinedit = cubicweb-inlinedit:main
    #
    # Would create an entry point called cubicweb-inlinedit that calls cubicweb-inlinedit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cubicweb >=3.17.16
    - cwtags

  run:
    - python
    - cubicweb >=3.17.16
    - cwtags

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.cubicweb.org/project/cubicweb-inlinedit
  license: LGPL
  summary: 'Extension of the `reledit` builtin feature'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
