package:
  name: django-versions
  version: "1.0.1"

source:
  fn: django-versions-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-versions/django-versions-1.0.1.tar.gz
  md5: f4be9b84a483e4e9d820dda53ae9ffdc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-versions = django-versions:main
    #
    # Would create an entry point called django-versions that calls django-versions.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - mercurial >=1.5.2

  run:
    - python
    - mercurial >=1.5.2

test:
  # Python imports
  imports:
    - versions
    - versions.backends
    - versions.backends.database
    - versions.backends.hg
    - versions.management
    - versions.management.commands
    - versions.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/nowells/django-versions/
  license: MIT License
  summary: 'A django application to enable versioning of data stored in Django models.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
