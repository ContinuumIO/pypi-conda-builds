package:
  name: openerp-survey
  version: "7.0.406"

source:
  fn: openerp-survey-7.0.406.tar.gz
  url: https://pypi.python.org/packages/source/o/openerp-survey/openerp-survey-7.0.406.tar.gz
  md5: a483e3f4b886df28550dd647ed564a43
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - openerp-survey = openerp-survey:main
    #
    # Would create an entry point called openerp-survey that calls openerp-survey.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - openerp-mail

  run:
    - python
    - openerp-mail

test:
  # Python imports
  imports:
    - openerp.addons.survey
    - openerp.addons.survey.report
    - openerp.addons.survey.wizard

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: GNU Affero General Public License v3
  summary: 'Survey'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
