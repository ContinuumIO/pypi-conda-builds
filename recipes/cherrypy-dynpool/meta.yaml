package:
  name: cherrypy-dynpool
  version: "2.1"

source:
  fn: cherrypy-dynpool-2.1.tar.gz
  url: https://pypi.python.org/packages/source/c/cherrypy-dynpool/cherrypy-dynpool-2.1.tar.gz
  md5: 086dd5cb83b130e54a813ff63ee89337
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cherrypy-dynpool = cherrypy-dynpool:main
    #
    # Would create an entry point called cherrypy-dynpool that calls cherrypy-dynpool.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cherrypy >=3.2,<4.0
    - dynpool >=2.0,<3.0

  run:
    - python
    - cherrypy >=3.2,<4.0
    - dynpool >=2.0,<3.0

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://tabo.pe/projects/cherrypy-dynpool/
  license: Apache Software License
  summary: 'A dynamic thread pool tool for CherryPy 3.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
