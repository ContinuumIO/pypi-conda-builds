package:
  name: instamedia
  version: "1.0"

source:
  fn: instamedia-1.0.zip
  url: https://pypi.python.org/packages/source/i/instamedia/instamedia-1.0.zip
  md5: c6c8b6f8864a52ad017d7d053090b45f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - instamedia = instamedia:main
    #
    # Would create an entry point called instamedia that calls instamedia.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - instamedia

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/alir6716/instamedia
  license: GPL V3
  summary: 'Download/view all Instagram media from an account.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
