package:
  name: nailgun
  version: "0.12.0"

source:
  fn: nailgun-0.12.0.tar.gz
  url: https://pypi.python.org/packages/source/n/nailgun/nailgun-0.12.0.tar.gz
  md5: 103728c0884d9026a765f083bb1d2e86
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - nailgun = nailgun:main
    #
    # Would create an entry point called nailgun that calls nailgun.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - pyxdg
    - requests
    - setuptools

  run:
    - python
    - pyxdg
    - requests
    - setuptools

test:
  # Python imports
  imports:
    - nailgun
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/SatelliteQE/nailgun
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'A library that facilitates easy usage of the Satellite 6 API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
