package:
  name: django-akamai
  version: "1.0.0"

source:
  fn: django-akamai-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-akamai/django-akamai-1.0.0.tar.gz
  md5: d4491c7f298c4bcae03200e2693e18c9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-akamai = django-akamai:main
    #
    # Would create an entry point called django-akamai that calls django-akamai.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_akamai

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/beathan/django-akamai
  license: UNKNOWN
  summary: 'A Django app for performing Akamai CCUAPI purge requests'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
