package:
  name: django-emailthis
  version: "0.2.7"

source:
  fn: django-emailthis-0.2.7.tar.gz
  url: https://pypi.python.org/packages/source/d/django-emailthis/django-emailthis-0.2.7.tar.gz
  md5: 5665620bcc23e1aa37b3d8dac2216877
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-emailthis = django-emailthis:main
    #
    # Would create an entry point called django-emailthis that calls django-emailthis.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - emailthis

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/django-emailthis/
  license: BSD License
  summary: '\na simple email-to-a-friend application that generates an email\nform, handles its submission, persists the submission data to a\ndatabase, and sends a templated email.  Both anonymous and registered\nusers can send email.\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
