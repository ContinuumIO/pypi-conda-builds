package:
  name: github-licker
  version: "0.0.2"

source:
  fn: github-licker-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/g/github-licker/github-licker-0.0.2.tar.gz
  md5: 0c0a05e292414c507c917ee45a604f04
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - github-licker = github-licker:main
    #
    # Would create an entry point called github-licker that calls github-licker.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - docopt

  run:
    - python
    - docopt

test:
  # Python imports
  imports:
    - licker

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jbspeakr/github-licker
  license: Apache License 2.0
  summary: 'Licker - Your Github License Checker for Organisational Repositories.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
