package:
  name: btponto
  version: "0.1"

source:
  fn: btponto-0.1.tar.gz
  url: http://osantana-code.googlecode.com/files/btponto-0.1.tar.gz
  #md5: 
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - btponto = btponto:main
    #
    # Would create an entry point called btponto that calls btponto.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - btponto

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/osantana-code/wiki/BtPonto
  license: GNU General Public License (GPL)
  summary: 'Software to register presence based in Bluetooth device proximity.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
