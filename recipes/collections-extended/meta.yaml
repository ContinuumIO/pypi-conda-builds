package:
  name: collections-extended
  version: "0.4.0"

source:
  fn: collections-extended-0.4.0.tar.gz
  url: https://pypi.python.org/packages/source/c/collections-extended/collections-extended-0.4.0.tar.gz
  md5: 0ce33a91c5de4a81bda974f90ab64f8a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collections-extended = collections-extended:main
    #
    # Would create an entry point called collections-extended that calls collections-extended.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - collections_extended

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://collections-extended.lenzm.net/
  license: Apache Software License
  summary: 'Extra Python Collections - bags (multisets) and setlists (ordered sets)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
