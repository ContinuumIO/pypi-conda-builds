package:
  name: foreman-host-builder
  version: "2.6"

source:
  fn: foreman-host-builder-2.6.tar.gz
  url: https://pypi.python.org/packages/source/f/foreman-host-builder/foreman-host-builder-2.6.tar.gz
  md5: 70bfcdd47e8523900ebccc8415e5aa4b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - foreman-host-builder = foreman-host-builder:main
    #
    # Would create an entry point called foreman-host-builder that calls foreman-host-builder.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python-foreman
    - fabric
    - configparser
    - pyyaml

  run:
    - python
    - python-foreman
    - fabric
    - configparser
    - pyyaml

test:
  # Python imports
  imports:
    - fhb

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/xnaveira/foreman-host-builder
  license: UNKNOWN
  summary: 'Build hosts in Foreman from a template file'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
