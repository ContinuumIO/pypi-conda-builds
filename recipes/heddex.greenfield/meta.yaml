package:
  name: heddex.greenfield
  version: "2.0"

source:
  fn: heddex.greenfield-2.0.tar.gz
  url: https://pypi.python.org/packages/source/h/heddex.greenfield/heddex.greenfield-2.0.tar.gz
  md5: 7d88211e6aa9e7115e67b9f86b1a6c7e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - heddex.greenfield = heddex.greenfield:main
    #
    # Would create an entry point called heddex.greenfield that calls heddex.greenfield.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - heddex
    - heddex.greenfield
    - heddex.greenfield.browser
    - heddex.greenfield.portlets

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.heddex.biz/
  license: GPL
  summary: 'An installable theme for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
