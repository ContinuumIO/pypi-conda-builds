package:
  name: riak_cli_tool
  version: "0.1.7"

source:
  fn: riak_cli_tool-0.1.7.tar.gz
  url: https://pypi.python.org/packages/source/r/riak_cli_tool/riak_cli_tool-0.1.7.tar.gz
  md5: 3032543b84e38a9c06b7dc3f4ce75a74
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - riak_cli_tool = riak_cli_tool:main
    #
    # Would create an entry point called riak_cli_tool that calls riak_cli_tool.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests ==1.0.4

  run:
    - python
    - requests ==1.0.4

test:
  # Python imports
  imports:
    - riak_cli_tool

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://pypi.python.org/pypi/riak_cli_tool
  license: Apache Software License
  summary: 'Riak tool'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
