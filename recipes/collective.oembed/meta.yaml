package:
  name: collective.oembed
  version: "2.0.1"

source:
  fn: collective.oembed-2.0.1.zip
  url: https://pypi.python.org/packages/source/c/collective.oembed/collective.oembed-2.0.1.zip
  md5: a3d907eb2770db026b4c0ad7d2a88442
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.oembed = collective.oembed:main
    #
    # Would create an entry point called collective.oembed that calls collective.oembed.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - python-oembed
    - plone.app.registry

  run:
    - python
    - setuptools
    - setuptools
    - python-oembed
    - plone.app.registry

test:
  # Python imports
  imports:
    - collective
    - collective.oembed
    - collective.oembed.api2embed
    - collective.oembed.tests
    - collective.oembed.tile
    - collective.oembed.tinymce
    - collective.oembed.url2embed

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.oembed
  license: GNU General Public License (GPL)
  summary: 'oembed integration of consumers and provider for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
