package:
  name: django-audit
  version: "1.0b3"

source:
  fn: django-audit-1.0b3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-audit/django-audit-1.0b3.tar.gz
  md5: c44796929ec391459673fe03b2fb6bfe
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-audit = django-audit:main
    #
    # Would create an entry point called django-audit that calls django-audit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.1
    - pymongo >=1.4

  run:
    - python
    - setuptools
    - django >=1.1
    - pymongo >=1.4

test:
  # Python imports
  imports:
    - djangoaudit

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://launchpad.net/django-audit/
  license: BSD License
  summary: 'Auditing for Django applications'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
