package:
  name: nusstalker
  version: "0.9"

source:
  fn: nusstalker-0.9.tar.gz
  url: https://pypi.python.org/packages/source/n/nusstalker/nusstalker-0.9.tar.gz
  md5: e75cbbcd930c5d7fd4884db8d9b534d2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - nusstalker = nusstalker:main
    #
    # Would create an entry point called nusstalker that calls nusstalker.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python3-ldap ==0.5.2-alpha

  run:
    - python
    - python3-ldap ==0.5.2-alpha

test:
  # Python imports
  imports:
    - nusstalker

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jsyeo/nus-stalker
  license: UNKNOWN
  summary: 'Stalk people in NUS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
