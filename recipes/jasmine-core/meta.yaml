package:
  name: jasmine-core
  version: "2.3.0"

source:
  fn: jasmine-core-2.3.0.tar.gz
  url: https://pypi.python.org/packages/source/j/jasmine-core/jasmine-core-2.3.0.tar.gz
  md5: 94a32de9504b602d75a5893420a34948
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jasmine-core = jasmine-core:main
    #
    # Would create an entry point called jasmine-core that calls jasmine-core.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - glob2 >=0.4.1
    - ordereddict ==1.1

  run:
    - python
    - glob2 >=0.4.1
    - ordereddict ==1.1

test:
  # Python imports
  imports:
    - jasmine_core
    - jasmine_core.images

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://jasmine.github.io
  license: MIT License
  summary: "Jasmine is a Behavior Driven Development testing framework for JavaScript. It does not rely on browsers, DOM, or any JavaScript framework. Thus it's suited for websites, Node.js (http://nodejs.org) projects, or anywhere that JavaScript can run."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
