package:
  name: slopos
  version: "1.0"

source:
  fn: SloPOS-1.0.tar.gz
  url: https://pypi.python.org/packages/source/S/SloPOS/SloPOS-1.0.tar.gz
  md5: 01e5912f858dfa3ac3bffe4ca2cf3a49
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - slopos = slopos:main
    #
    # Would create an entry point called slopos that calls slopos.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - nltk

  run:
    - python
    - nltk

test:
  # Python imports
  imports:
    - slopos

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/izacus/slo_pos
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Part of speech tagger for Slovenian (SI) language based on NLTK'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
