package:
  name: django-media-helper
  version: "0.3.1"

source:
  fn: django-media-helper-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-media-helper/django-media-helper-0.3.1.tar.gz
  md5: 7e313431f9aaa22a61bcb94871cfa0c9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-media-helper = django-media-helper:main
    #
    # Would create an entry point called django-media-helper that calls django-media-helper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - media_helper
    - media_helper.management
    - media_helper.management.commands
    - media_helper.tools
    - tests
    - tests.media_helper
    - tests.media_helper.management
    - tests.media_helper.management.commands
    - tests.media_helper.tools
    - tests.test-files
    - tests.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/brmc/django-media-helper
  license: MIT License
  summary: 'An image resizing and management app for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
