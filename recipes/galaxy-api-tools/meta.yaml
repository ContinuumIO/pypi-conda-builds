package:
  name: galaxy-api-tools
  version: "0.1.0"

source:
  fn: galaxy-api-tools-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/g/galaxy-api-tools/galaxy-api-tools-0.1.0.tar.gz
  md5: 2d1a19cd683ab02da3a1560d275c72fb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - galaxy-api-tools = galaxy-api-tools:main
    #
    # Would create an entry point called galaxy-api-tools that calls galaxy-api-tools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - docopt
    - bioblend

  run:
    - python
    - docopt
    - bioblend

test:
  # Python imports
  imports:
    - galaxyapitools

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/cjav/galaxy-api-tools
  license: UNKNOWN
  summary: 'The Galaxy API tools serve as a client interface to Galaxy server.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
