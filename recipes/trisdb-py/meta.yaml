package:
  name: trisdb-py
  version: "0.1.2"

source:
  fn: trisdb-py-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/t/trisdb-py/trisdb-py-0.1.2.tar.gz
  md5: 54cfbb569cf4ae3e9e5d53e3476da6f9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - trisdb-py = trisdb-py:main
    #
    # Would create an entry point called trisdb-py that calls trisdb-py.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - protobuf

  run:
    - python
    - protobuf

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tiepologian/trisdb-py
  license: UNKNOWN
  summary: 'The Python interface to TrisDb'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
