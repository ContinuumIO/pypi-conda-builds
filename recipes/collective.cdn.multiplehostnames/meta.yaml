package:
  name: collective.cdn.multiplehostnames
  version: "0.6"

source:
  fn: collective.cdn.multiplehostnames-0.6.zip
  url: https://pypi.python.org/packages/source/c/collective.cdn.multiplehostnames/collective.cdn.multiplehostnames-0.6.zip
  md5: 828386232fae5f136ce56dbb79dfaf62
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.cdn.multiplehostnames = collective.cdn.multiplehostnames:main
    #
    # Would create an entry point called collective.cdn.multiplehostnames that calls collective.cdn.multiplehostnames.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.cdn.core

  run:
    - python
    - setuptools
    - setuptools
    - collective.cdn.core

test:
  # Python imports
  imports:
    - collective
    - collective.cdn
    - collective.cdn.multiplehostnames
    - collective.cdn.multiplehostnames.docs
    - collective.cdn.multiplehostnames.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.simplesconsultoria.com.br/
  license: GPL
  summary: 'Multiple Hostnames CDN support for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
