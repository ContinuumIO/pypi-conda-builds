package:
  name: moderna
  version: "1.7.5"

source:
  fn: moderna-1.7.5.tar.gz
  url: https://pypi.python.org/packages/source/m/moderna/moderna-1.7.5.tar.gz
  md5: 783356fd45e44d614e47eccaa1dfe4af
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - moderna = moderna:main
    #
    # Would create an entry point called moderna that calls moderna.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy >=1.3
    - biopython >=1.53

  run:
    - python
    - numpy >=1.3
    - biopython >=1.53

test:
  # Python imports
  imports:
    - moderna
    - moderna.analyze
    - moderna.builder
    - moderna.examples
    - moderna.fragment_library
    - moderna.isosteric
    - moderna.modifications
    - moderna.sequence
    - moderna.util

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://iimcb.genesilico.pl/moderna
  license: new BSD
  summary: 'ModeRNA - A program for comparative RNA modeling'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
