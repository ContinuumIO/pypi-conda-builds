package:
  name: axigen-api
  version: "1.0.0-1"

source:
  fn: axigen-api-1.0.0-1.tar.gz
  url: https://pypi.python.org/packages/source/a/axigen-api/axigen-api-1.0.0-1.tar.gz
  md5: f056fcbfe05b93c31f27553d98a4d9f3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - axigen-api = axigen-api:main
    #
    # Would create an entry point called axigen-api that calls axigen-api.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - axigenapi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/casastorta/axigen-pyton-api
  license: LGPL
  summary: "Python module to interface with Axigen's CLI API"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
