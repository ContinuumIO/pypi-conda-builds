package:
  name: finch-cms
  version: "0.8.7"

source:
  fn: finch-cms-0.8.7.tar.gz
  url: https://pypi.python.org/packages/source/f/finch-cms/finch-cms-0.8.7.tar.gz
  md5: 7ad3edcfaeb5a7cf733b5cc5b3dcb872
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - finch-cms = finch-cms:main
    #
    # Would create an entry point called finch-cms that calls finch-cms.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django ==1.3
    - pillow
    - south
    - whoosh
    - django-bop
    - django-filebrowser
    - django-haystack
    - django-treebeard
    - django-contentmanager
    - django-tinymce

  run:
    - python
    - django ==1.3
    - pillow
    - south
    - whoosh
    - django-bop
    - django-filebrowser
    - django-haystack
    - django-treebeard
    - django-contentmanager
    - django-tinymce

test:
  # Python imports
  imports:
    - finch
    - finch.management
    - finch.management.commands
    - finch.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/pterk/finch-cms
  license: BSD License
  summary: 'Simple but flexible CMS for django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
