package:
  name: janrain-python-api
  version: "0.3.4"

source:
  fn: janrain-python-api-0.3.4.tar.gz
  url: https://pypi.python.org/packages/source/j/janrain-python-api/janrain-python-api-0.3.4.tar.gz
  md5: f70aba05fe1832c38a55a733e7f1d9c1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - janrain-python-api = janrain-python-api:main
    #
    # Would create an entry point called janrain-python-api that calls janrain-python-api.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests
    - pyyaml

  run:
    - python
    - requests
    - pyyaml

test:
  # Python imports
  imports:
    - janrain
    - janrain.capture
    - janrain.capture.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://developers.janrain.com/
  license: MIT License
  summary: 'Python interface to the Janrain Capture API.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
