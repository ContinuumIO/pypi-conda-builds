package:
  name: flask-nicely
  version: "0.2.0"

source:
  fn: Flask-Nicely-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Nicely/Flask-Nicely-0.2.0.tar.gz
  md5: 68fe77a2a8d8d49c74440968ed24ae32
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-nicely = flask-nicely:main
    #
    # Would create an entry point called flask-nicely that calls flask-nicely.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask

  run:
    - python
    - flask

test:
  # Python imports
  imports:
    - flask_nicely

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/jwpe/flask-nicely/
  license: BSD License
  summary: 'Pretty Flask JSON responses for API building.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
