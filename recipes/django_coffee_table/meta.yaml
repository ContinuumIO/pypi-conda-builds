package:
  name: django_coffee_table
  version: "1.0.1"

source:
  fn: django_coffee_table-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django_coffee_table/django_coffee_table-1.0.1.tar.gz
  md5: be939f9ed9b4f697d197af7058607ade
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_coffee_table = django_coffee_table:main
    #
    # Would create an entry point called django_coffee_table that calls django_coffee_table.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - linaro-django-pagination
    - django-resort
    - django-tag-parser

  run:
    - python
    - linaro-django-pagination
    - django-resort
    - django-tag-parser

test:
  # Python imports
  imports:
    - coffee_table
    - django_coffee_table

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/alixedi/django_coffee_table
  license: BSD License
  summary: 'Control of tables in templates sans BS.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
