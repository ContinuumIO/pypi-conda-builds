package:
  name: feincms-forms-builder
  version: "0.0.2"

source:
  fn: feincms-forms-builder-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/f/feincms-forms-builder/feincms-forms-builder-0.0.2.tar.gz
  md5: ffd773880693455af1eef9e164b78368
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - feincms-forms-builder = feincms-forms-builder:main
    #
    # Would create an entry point called feincms-forms-builder that calls feincms-forms-builder.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - forms_builder_integration

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bmihelac/feincms-forms-builder
  license: BSD License
  summary: 'Integrate django-forms-builder into FeinCMS.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
