package:
  name: collective.contentrules.parentchild
  version: "1.0b1"

source:
  fn: collective.contentrules.parentchild-1.0b1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.contentrules.parentchild/collective.contentrules.parentchild-1.0b1.tar.gz
  md5: 07a4909ef7a5ae813df27b1e794015fe
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.contentrules.parentchild = collective.contentrules.parentchild:main
    #
    # Would create an entry point called collective.contentrules.parentchild that calls collective.contentrules.parentchild.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.contentrules
    - plone.contentrules

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.contentrules
    - plone.contentrules

test:
  # Python imports
  imports:
    - collective
    - collective.contentrules
    - collective.contentrules.parentchild
    - collective.contentrules.parentchild.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/collective.contentrules.parentchild
  license: GPL
  summary: 'Content rules conditions and actions for expressing parent/child object relationships'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
