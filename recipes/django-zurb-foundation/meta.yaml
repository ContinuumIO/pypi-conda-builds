package:
  name: django-zurb-foundation
  version: "5.5.0"

source:
  fn: django-zurb-foundation-5.5.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-zurb-foundation/django-zurb-foundation-5.5.0.tar.gz
  md5: b986b8786c32ad2fb80233b51bf8f12b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-zurb-foundation = django-zurb-foundation:main
    #
    # Would create an entry point called django-zurb-foundation that calls django-zurb-foundation.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - foundation
    - foundation.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/amarsahinovic/django-zurb-foundation
  license: BSD License
  summary: 'Django Zurb Foundation package.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
