package:
  name: azureml
  version: "0.1.1"

source:
  fn: azureml-0.1.1.zip
  url: https://pypi.python.org/packages/source/a/azureml/azureml-0.1.1.zip
  md5: 9cc413ffff542764ee4f2e635c9b5157
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - azureml = azureml:main
    #
    # Would create an entry point called azureml that calls azureml.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python-dateutil
    - requests
    - pandas

  run:
    - python
    - python-dateutil
    - requests
    - pandas

test:
  # Python imports
  imports:
    - azureml

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Azure/Azure-MachineLearning-ClientLibrary-Python
  license: MIT License
  summary: 'Microsoft Azure Machine Learning Python client library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
