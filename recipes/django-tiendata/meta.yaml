package:
  name: django-tiendata
  version: "0.0.1"

source:
  fn: django-tiendata-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-tiendata/django-tiendata-0.0.1.tar.gz
  md5: a6220edc1f176317d9498a5ee2376823
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-tiendata = django-tiendata:main
    #
    # Would create an entry point called django-tiendata that calls django-tiendata.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - tiendata

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.tiendata.com/
  license: GNU General Public License v2 (GPLv2)
  summary: 'Django-based online store'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
