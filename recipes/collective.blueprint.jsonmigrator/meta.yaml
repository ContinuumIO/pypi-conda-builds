package:
  name: collective.blueprint.jsonmigrator
  version: "0.1.1"

source:
  fn: collective.blueprint.jsonmigrator-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.blueprint.jsonmigrator/collective.blueprint.jsonmigrator-0.1.1.tar.gz
  md5: 696fb71279835b759917832f868fd420
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.blueprint.jsonmigrator = collective.blueprint.jsonmigrator:main
    #
    # Would create an entry point called collective.blueprint.jsonmigrator that calls collective.blueprint.jsonmigrator.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - simplejson

  run:
    - python
    - setuptools
    - setuptools
    - simplejson

test:
  # Python imports
  imports:
    - collective
    - collective.blueprint
    - collective.blueprint.jsonmigrator

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/
  license: GPL
  summary: 'useful transmogrifier blueprints for large migrations from Plone, from 2.0 to 4.0'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
