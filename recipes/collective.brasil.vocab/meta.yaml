package:
  name: collective.brasil.vocab
  version: "0.8"

source:
  fn: collective.brasil.vocab-0.8.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.brasil.vocab/collective.brasil.vocab-0.8.tar.gz
  md5: 60e57f5640eaeeddf9e20dd21f10c8b8
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.brasil.vocab = collective.brasil.vocab:main
    #
    # Would create an entry point called collective.brasil.vocab that calls collective.brasil.vocab.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - brasil.vocab >=0.8

  run:
    - python
    - setuptools
    - setuptools
    - brasil.vocab >=0.8

test:
  # Python imports
  imports:
    - collective
    - collective.brasil
    - collective.brasil.vocab
    - collective.brasil.vocab.docs
    - collective.brasil.vocab.geo
    - collective.brasil.vocab.telecom
    - collective.brasil.vocab.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/simplesconsultoria/collective.brasil.vocab/
  license: GPL
  summary: 'A Zope 3 vocabulary implementation of brasil.vocab'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
