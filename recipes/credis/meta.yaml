package:
  name: credis
  version: "1.0.5"

source:
  fn: credis-1.0.5.tar.gz
  url: https://pypi.python.org/packages/source/c/credis/credis-1.0.5.tar.gz
  md5: 57b4c0601374076cebacce0f9aee22de
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - credis = credis:main
    #
    # Would create an entry point called credis that calls credis.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - hiredis >=0.1

  run:
    - python
    - hiredis >=0.1

test:
  # Python imports
  imports:
    - credis

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/yihuang/credis
  license: UNKNOWN
  summary: 'high performance redis client implemented with cython'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
