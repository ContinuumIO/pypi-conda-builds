package:
  name: django-dropboxchooser-field
  version: "0.0.5"

source:
  fn: django-dropboxchooser-field-0.0.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-dropboxchooser-field/django-dropboxchooser-field-0.0.5.tar.gz
  md5: 163aca0c1c2dcf2caaea8b71a6d74fb1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-dropboxchooser-field = django-dropboxchooser-field:main
    #
    # Would create an entry point called django-dropboxchooser-field that calls django-dropboxchooser-field.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - dropboxchooser_field

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bogdal/django-dropboxchooser-field
  license: BSD License
  summary: 'Dropbox chooser field for django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
