package:
  name: django-sqs
  version: "0.2"

source:
  fn: django-sqs-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-sqs/django-sqs-0.2.tar.gz
  md5: cf7391f1bb53cc2d858a4b86814fd5e3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-sqs = django-sqs:main
    #
    # Would create an entry point called django-sqs that calls django-sqs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_sqs
    - django_sqs.management
    - django_sqs.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Fandekasp/django-sqs
  license: UNKNOWN
  summary: 'django FormFields using the Chosen javascript plugin for jQuery'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
