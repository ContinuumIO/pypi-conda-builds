package:
  name: nikweb
  version: "0.1.0"

source:
  fn: Nikweb-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/N/Nikweb/Nikweb-0.1.0.tar.gz
  md5: 74a4909fd3325b37c5c09489cc5e6396
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - nikweb = nikweb:main
    #
    # Would create an entry point called nikweb that calls nikweb.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - nikweb
    - nikweb.http

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/mapnik-utils/
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'A GeoJSON web service for Mapnik'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
