package:
  name: django-peerreview
  version: "0.1.0"

source:
  fn: Django-PeerReview-0.1.0.zip
  url: https://pypi.python.org/packages/source/D/Django-PeerReview/Django-PeerReview-0.1.0.zip
  md5: bedd5145fc283e447a790dfca7e2729f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-peerreview = django-peerreview:main
    #
    # Would create an entry point called django-peerreview that calls django-peerreview.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4.0

  run:
    - python
    - django >=1.4.0

test:
  # Python imports
  imports:
    - peerreview

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/weholt/django-peer-review
  license: BSD License
  summary: 'Simple reusable app for management of peer review of arbitrary django models.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
