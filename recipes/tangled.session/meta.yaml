package:
  name: tangled.session
  version: "0.1a2"

source:
  fn: tangled.session-0.1a2.tar.gz
  url: https://pypi.python.org/packages/source/t/tangled.session/tangled.session-0.1a2.tar.gz
  md5: b3519232c364c546e6fe9030b9635c2b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tangled.session = tangled.session:main
    #
    # Would create an entry point called tangled.session that calls tangled.session.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - tangled >=0.1a5
    - beaker >=1.6.4

  run:
    - python
    - tangled >=0.1a5
    - beaker >=1.6.4

test:
  # Python imports
  imports:
    - tangled
    - tangled.session
    - tangled.session.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://tangledframework.org/
  license: MIT License
  summary: 'Tangled session integration'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
