package:
  name: django-jquery-js
  version: "2.1.4"

source:
  fn: django-jquery-js-2.1.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-jquery-js/django-jquery-js-2.1.4.tar.gz
  md5: 5d2ec9b8ab417d95c48d11a3d42f26ce
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-jquery-js = django-jquery-js:main
    #
    # Would create an entry point called django-jquery-js that calls django-jquery-js.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4

  run:
    - python
    - django >=1.4

test:
  # Python imports
  imports:
    - jquery
    - jquery.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/tim_heap/django-jquery
  license: MIT License
  summary: 'jQuery, bundled up so apps can depend upon it'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
