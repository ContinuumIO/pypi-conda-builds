package:
  name: plone.recipe.cluster
  version: "1.0.0rc1"

source:
  fn: plone.recipe.cluster-1.0.0rc1.tar.gz
  url: https://pypi.python.org/packages/source/p/plone.recipe.cluster/plone.recipe.cluster-1.0.0rc1.tar.gz
  md5: 7366479a5c7d5cfe197c277b7068bcf7
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.recipe.cluster = plone.recipe.cluster:main
    #
    # Would create an entry point called plone.recipe.cluster that calls plone.recipe.cluster.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - zc.recipe.egg

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - zc.recipe.egg

test:
  # Python imports
  imports:
    - plone
    - plone.recipe
    - plone.recipe.cluster
    - plone.recipe.cluster.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/buildout/plone.recipe.cluster
  license: Zope Public License
  summary: 'ZC Buildout recipe for organizing startup scripts.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
