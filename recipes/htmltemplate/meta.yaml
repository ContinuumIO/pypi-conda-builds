package:
  name: htmltemplate
  version: "2.2.0"

source:
  fn: htmltemplate-2.2.0.tar.gz
  url: https://pypi.python.org/packages/source/h/htmltemplate/htmltemplate-2.2.0.tar.gz
  md5: 38393965e1799553adf2c6b004fdabf1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - htmltemplate = htmltemplate:main
    #
    # Would create an entry point called htmltemplate that calls htmltemplate.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - htmltemplate

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/hhas/htmltemplate
  license: MIT License
  summary: 'A simple, powerful [X]HTML templating library for Python 3'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
