package:
  name: ajenti.plugin.filemanager
  version: "0.13"

source:
  fn: ajenti.plugin.filemanager-0.13.tar.gz
  url: https://pypi.python.org/packages/source/a/ajenti.plugin.filemanager/ajenti.plugin.filemanager-0.13.tar.gz
  md5: 8e7cee2e43a7d7ca999d03dbabb78dcd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ajenti.plugin.filemanager = ajenti.plugin.filemanager:main
    #
    # Would create an entry point called ajenti.plugin.filemanager that calls ajenti.plugin.filemanager.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - aj
    - ajenti.plugin.core
    - ajenti.plugin.filesystem

  run:
    - python
    - aj
    - ajenti.plugin.core
    - ajenti.plugin.filesystem

test:
  # Python imports
  imports:
    - ajenti_plugin_filemanager

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://ajenti.org
  license: UNKNOWN
  summary: 'File Manager'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
