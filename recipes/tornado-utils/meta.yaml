package:
  name: tornado-utils
  version: "1.6"

source:
  fn: tornado-utils-1.6.tar.gz
  url: https://pypi.python.org/packages/source/t/tornado-utils/tornado-utils-1.6.tar.gz
  md5: f2a2ac605c27f2236836e4963efde88e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tornado-utils = tornado-utils:main
    #
    # Would create an entry point called tornado-utils that calls tornado-utils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - tornado_utils
    - tornado_utils.send_mail
    - tornado_utils.send_mail.backends
    - tornado_utils.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/peterbe/tornado-utils
  license: UNKNOWN
  summary: 'Utility scripts for a Tornado site'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
