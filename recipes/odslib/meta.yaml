package:
  name: odslib
  version: "1.0.2"

source:
  fn: odslib-1.0.2.tar.bz2
  url: https://pypi.python.org/packages/source/o/odslib/odslib-1.0.2.tar.bz2
  md5: 340e786f4b98d0619ac21ebeefd3a70c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - odslib = odslib:main
    #
    # Would create an entry point called odslib that calls odslib.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - odslib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/angry_elf/odslib/
  license: GNU General Public License (GPL)
  summary: 'An easy to use module that creates ODS documents. Fork of original odslib-python (http://code.google.com/p/odslib-python/). Original author: Joseph Colton'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
