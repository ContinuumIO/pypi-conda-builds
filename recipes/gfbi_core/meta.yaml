package:
  name: gfbi_core
  version: "0.5b6"

source:
  fn: gfbi_core-0.5b6.tar.gz
  url: https://pypi.python.org/packages/source/g/gfbi_core/gfbi_core-0.5b6.tar.gz
  md5: b7d9efd81a300bbab6e17e7ba5f8ac9a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gfbi_core = gfbi_core:main
    #
    # Would create an entry point called gfbi_core that calls gfbi_core.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - gitpython >=0.3.1

  run:
    - python
    - gitpython >=0.3.1

test:
  # Python imports
  imports:
    - gfbi_core

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mike-perdide/qfbi_core
  license: GPLv3
  summary: 'Core files for interactive git filter branch.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
