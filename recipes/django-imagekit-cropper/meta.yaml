package:
  name: django-imagekit-cropper
  version: "0.12"

source:
  fn: django-imagekit-cropper-0.12.tar.gz
  url: https://pypi.python.org/packages/source/d/django-imagekit-cropper/django-imagekit-cropper-0.12.tar.gz
  md5: e9926066da6c0ea93e4bbb70ecf26d4b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-imagekit-cropper = django-imagekit-cropper:main
    #
    # Would create an entry point called django-imagekit-cropper that calls django-imagekit-cropper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - django-grappelli
    - django-imagekit

  run:
    - python
    - setuptools
    - django
    - django-grappelli
    - django-imagekit

test:
  # Python imports
  imports:
    - imagekit_cropper
    - imagekit_cropper.management
    - imagekit_cropper.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ninapavlich/django-imagekit-cropper
  license: BSD
  summary: 'Allow users to manually specify image variant crops'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
