package:
  name: 51degrees-mobile-detector-trie-wrapper
  version: "1.0"

source:
  fn: 51degrees-mobile-detector-trie-wrapper-1.0.tar.gz
  url: https://pypi.python.org/packages/source/5/51degrees-mobile-detector-trie-wrapper/51degrees-mobile-detector-trie-wrapper-1.0.tar.gz
  md5: b8c2bd6ee5d0ceac1a2cae94337e7516
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - 51degrees-mobile-detector-trie-wrapper = 51degrees-mobile-detector-trie-wrapper:main
    #
    # Would create an entry point called 51degrees-mobile-detector-trie-wrapper that calls 51degrees-mobile-detector-trie-wrapper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - distribute
    - 51degrees-mobile-detector

  run:
    - python
    - distribute
    - 51degrees-mobile-detector

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://51degrees.mobi
  license: Mozilla Public License 2.0 (MPL 2.0)
  summary: '51Degrees Mobile Detector (C Trie Wrapper).'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
