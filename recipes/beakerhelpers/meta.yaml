package:
  name: beakerhelpers
  version: "0.1"

source:
  fn: BeakerHelpers-0.1.tar.gz
  url: https://pypi.python.org/packages/source/B/BeakerHelpers/BeakerHelpers-0.1.tar.gz
  md5: d2a36d02153d934e9cb948ba5cc36508
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - beakerhelpers = beakerhelpers:main
    #
    # Would create an entry point called beakerhelpers that calls beakerhelpers.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pastescript
    - pastedeploy
    - sqlalchemy >=0.5.5

  run:
    - python
    - setuptools
    - pastescript
    - pastedeploy
    - sqlalchemy >=0.5.5

test:
  # Python imports
  imports:
    - beakerhelpers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: MIT
  summary: 'An extendable Beaker helpers package to manage Beaker sessions'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
