package:
  name: collective.privateurl
  version: "1.0"

source:
  fn: collective.privateurl-1.0.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.privateurl/collective.privateurl-1.0.tar.gz
  md5: b94f3b713185ed5b0d21a50fac2941f4
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.privateurl = collective.privateurl:main
    #
    # Would create an entry point called collective.privateurl that calls collective.privateurl.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.privateurl

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://svn.plone.org/svn/collective/collective.privateurl
  license: GPL
  summary: 'Completely block anonymous access on a Plone site'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
