package:
  name: idlex
  version: "1.12"

source:
  fn: idlex-1.12.zip
  url: https://pypi.python.org/packages/source/i/idlex/idlex-1.12.zip
  md5: b29e0144f7bae82e3d4cf4711dda02c0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - idlex = idlex:main
    #
    # Would create an entry point called idlex that calls idlex.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - idlex
    - idlexlib
    - idlexlib.extensions

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://idlex.sourceforge.net
  license: University of Illinois/NCSA Open Source License or Python Software Foundation License
  summary: 'IDLE Extensions for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
