package:
  name: marvin_sketcher
  version: "1.0.0"

source:
  fn: Marvin_sketcher-1.0.0.zip
  url: https://pypi.python.org/packages/source/M/Marvin_sketcher/Marvin_sketcher-1.0.0.zip
  md5: cd4eda629f33119505c80a7eff97e431
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - marvin_sketcher = marvin_sketcher:main
    #
    # Would create an entry point called marvin_sketcher that calls marvin_sketcher.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - marvin_sketcher

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: www.guokr.com
  license: UNKNOWN
  summary: 'print the text file inside the floder, testing my try/except function'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
