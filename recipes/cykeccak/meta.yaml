package:
  name: cykeccak
  version: "0.13.2.2"

source:
  fn: cykeccak-0.13.2.2.tar.gz
  url: https://pypi.python.org/packages/source/c/cykeccak/cykeccak-0.13.2.2.tar.gz
  md5: c51da9799193c0cac3d9915a7e9e4866
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cykeccak = cykeccak:main
    #
    # Would create an entry point called cykeccak that calls cykeccak.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - cykeccak

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/habnabit/cykeccak
  license: ISC License (ISCL)
  summary: u'Cython bindings to the Keccak sponge and SHA-3 functions'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
