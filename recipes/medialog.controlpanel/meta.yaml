package:
  name: medialog.controlpanel
  version: "1.0.4"

source:
  fn: medialog.controlpanel-1.0.4.tar.gz
  url: https://pypi.python.org/packages/source/m/medialog.controlpanel/medialog.controlpanel-1.0.4.tar.gz
  md5: 95f07b16100df44e5a2f173374129cd1
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - medialog.controlpanel = medialog.controlpanel:main
    #
    # Would create an entry point called medialog.controlpanel that calls medialog.controlpanel.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.registry

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.registry

test:
  # Python imports
  imports:
    - medialog
    - medialog.controlpanel

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/espenmn/medialog.controlpanel
  license: GPL
  summary: 'A pluggable control panel.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
