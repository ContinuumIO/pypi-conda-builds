package:
  name: collective.js.slimbox2
  version: "1.4"

source:
  fn: collective.js.slimbox2-1.4.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.js.slimbox2/collective.js.slimbox2-1.4.tar.gz
  md5: 6a3e364c05de54d3c0262ee6b1403f33
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.js.slimbox2 = collective.js.slimbox2:main
    #
    # Would create an entry point called collective.js.slimbox2 that calls collective.js.slimbox2.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.js
    - collective.js.slimbox2
    - collective.js.slimbox2.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/collective.js.slimbox2
  license: GPL
  summary: 'Slimbox is a 4 KB visual clone of the popular Lightbox 2 script by Lokesh Dhakar, written using the jQuery javascript library.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
