package:
  name: roopeshv.base_skel
  version: "0.0.5"

source:
  fn: roopeshv.base_skel-0.0.5.tar.gz
  url: https://pypi.python.org/packages/source/r/roopeshv.base_skel/roopeshv.base_skel-0.0.5.tar.gz
  md5: cd3a4d5fd98231cdbcda5840ccdd2001
#  patches:
   # List any patch files here
   # - fix.patch

build:
  # preserve_egg_dir: True
  entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - roopeshv.base_skel = roopeshv.base_skel:main
    #
    # Would create an entry point called roopeshv.base_skel that calls roopeshv.base_skel.main()

    - hello = roopeshv.base_skel.hello:main

  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - roopeshv
    - roopeshv.base_skel

  commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.

    - hello --help

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: GPL
  summary: 'A package which acts as my base skeleton for my packages'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
