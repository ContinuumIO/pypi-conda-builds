package:
  name: django-simple-gravatar
  version: "1.0.1"

source:
  fn: django-simple-gravatar-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-simple-gravatar/django-simple-gravatar-1.0.1.tar.gz
  md5: 95a45783d0672845a1ca33a647993d68
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-simple-gravatar = django-simple-gravatar:main
    #
    # Would create an entry point called django-simple-gravatar that calls django-simple-gravatar.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_gravatar
    - django_gravatar.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/skitoo/django-gravatar
  license: BSD License
  summary: 'Django Gravatar is a lightweight Django application that allows you to insert a Gravatar image in your templates.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
