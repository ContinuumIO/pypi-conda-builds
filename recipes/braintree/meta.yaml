package:
  name: braintree
  version: "3.16.0"

source:
  fn: braintree-3.16.0.tar.gz
  url: https://pypi.python.org/packages/source/b/braintree/braintree-3.16.0.tar.gz
  md5: 1f274831034f8319c22376ce422a0d64
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - braintree = braintree:main
    #
    # Would create an entry point called braintree that calls braintree.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - requests >=0.11.1,<3.0

  run:
    - python
    - requests >=0.11.1,<3.0

test:
  # Python imports
  imports:
    - braintree
    - braintree.exceptions
    - braintree.exceptions.http
    - braintree.merchant_account
    - braintree.test
    - braintree.util

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://developers.braintreepayments.com/python/sdk/server/overview
  license: UNKNOWN
  summary: 'Braintree Python Library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
