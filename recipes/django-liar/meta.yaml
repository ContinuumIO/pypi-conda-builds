package:
  name: django-liar
  version: "0.1.1"

source:
  fn: django-liar-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-liar/django-liar-0.1.1.tar.gz
  md5: fa666660a43990d92720c2d72f73e559
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-liar = django-liar:main
    #
    # Would create an entry point called django-liar that calls django-liar.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - liar

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lalo73/django-liar
  license: BSD License
  summary: 'A simple django app that invalidates url resolving of apps.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
