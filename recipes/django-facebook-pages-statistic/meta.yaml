package:
  name: django-facebook-pages-statistic
  version: "0.0.8"

source:
  fn: django-facebook-pages-statistic-0.0.8.tar.gz
  url: https://pypi.python.org/packages/source/d/django-facebook-pages-statistic/django-facebook-pages-statistic-0.0.8.tar.gz
  md5: 7093479691c85e2f37a2444cb7875ac8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-facebook-pages-statistic = django-facebook-pages-statistic:main
    #
    # Would create an entry point called django-facebook-pages-statistic that calls django-facebook-pages-statistic.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-facebook-api >=0.1.22
    - django-facebook-pages

  run:
    - python
    - django-facebook-api >=0.1.22
    - django-facebook-pages

test:
  # Python imports
  imports:
    - facebook_pages_statistic
    - facebook_pages_statistic.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ramusus/django-facebook-pages-statistic
  license: BSD License
  summary: 'Application for storing Facebook Pages statistic for different timesnaps'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
