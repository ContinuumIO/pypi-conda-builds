package:
  name: djangocms-oscar
  version: "0.1"

source:
  fn: djangocms-oscar-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/djangocms-oscar/djangocms-oscar-0.1.tar.gz
  md5: 8534413ffd2b901c1f2365dda9123361
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangocms-oscar = djangocms-oscar:main
    #
    # Would create an entry point called djangocms-oscar that calls djangocms-oscar.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-cms >=3.0.2
    - django-oscar >=0.7.1

  run:
    - python
    - django-cms >=3.0.2
    - django-oscar >=0.7.1

test:
  # Python imports
  imports:
    - djangocms_oscar
    - djangocms_oscar.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD License
  summary: 'django CMS plugin for Oscar'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
