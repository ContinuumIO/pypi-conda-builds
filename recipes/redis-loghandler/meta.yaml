package:
  name: redis-loghandler
  version: "0.3"

source:
  fn: redis-loghandler-0.3.tar.gz
  url: https://pypi.python.org/packages/source/r/redis-loghandler/redis-loghandler-0.3.tar.gz
  md5: 21a099709b98206204bbca3ea626fc27
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - redis-loghandler = redis-loghandler:main
    #
    # Would create an entry point called redis-loghandler that calls redis-loghandler.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - redis >=2.8.0

  run:
    - python
    - redis >=2.8.0

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.quadloops.com
  license: UNKNOWN
  summary: 'Redis logging handler'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
