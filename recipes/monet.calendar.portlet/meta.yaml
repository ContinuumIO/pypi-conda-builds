package:
  name: monet.calendar.portlet
  version: "0.4.1"

source:
  fn: monet.calendar.portlet-0.4.1.tar.gz
  url: https://pypi.python.org/packages/source/m/monet.calendar.portlet/monet.calendar.portlet-0.4.1.tar.gz
  md5: 4f02be49b9600cc3629df97d5fb1cc36
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - monet.calendar.portlet = monet.calendar.portlet:main
    #
    # Would create an entry point called monet.calendar.portlet that calls monet.calendar.portlet.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - monet.calendar.event
    - monet.calendar.extensions >=0.9.0

  run:
    - python
    - setuptools
    - setuptools
    - monet.calendar.event
    - monet.calendar.extensions >=0.9.0

test:
  # Python imports
  imports:
    - monet
    - monet.calendar
    - monet.calendar.portlet
    - monet.calendar.portlet.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/monet.calendar.star
  license: GPL
  summary: 'Calendar related portlets for the Plone Monet Calendar events suite'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
