package:
  name: django-urlauth
  version: "0.1.8"

source:
  fn: django-urlauth-0.1.8.tar.gz
  url: https://pypi.python.org/packages/source/d/django-urlauth/django-urlauth-0.1.8.tar.gz
  md5: 19c4f39cf1457aac98b4973c54786a80
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-urlauth = django-urlauth:main
    #
    # Would create an entry point called django-urlauth that calls django-urlauth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - demo
    - demo.urlauth
    - demo.urlauth.management
    - demo.urlauth.management.commands
    - demo.urlauth.migrations
    - urlauth
    - urlauth.management
    - urlauth.management.commands
    - urlauth.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/lorien/django-urlauth/
  license: BSD License
  summary: 'Django application for user authentication with key in hypertext link'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
