package:
  name: flask-sessions
  version: "0.1.5"

source:
  fn: Flask-Sessions-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Sessions/Flask-Sessions-0.1.5.tar.gz
  md5: a0664635f1a2fec79bd3ab563d144979
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-sessions = flask-sessions:main
    #
    # Would create an entry point called flask-sessions that calls flask-sessions.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.8

  run:
    - python
    - flask >=0.8

test:
  # Python imports
  imports:
    - flask_sessions

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jefurry/flask-sessions
  license: BSD License
  summary: 'Adds server-side session support to your Flask application'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
