package:
  name: django-achilles
  version: "0.0.13"

source:
  fn: django-achilles-0.0.13.tar.gz
  url: https://pypi.python.org/packages/source/d/django-achilles/django-achilles-0.0.13.tar.gz
  md5: d66a01918f4f85e51f2c08b161bebf29
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-achilles = django-achilles:main
    #
    # Would create an entry point called django-achilles that calls django-achilles.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4
    - six

  run:
    - python
    - django >=1.4
    - six

test:
  # Python imports
  imports:
    - achilles
    - achilles.tables
    - achilles.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/exekias/django-achilles/
  license: Apache Software License
  summary: 'Django AJAX Framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
