package:
  name: django_price
  version: "0.1.1-1"

source:
  fn: django_price-0.1.1-1.tar.gz
  url: https://pypi.python.org/packages/source/d/django_price/django_price-0.1.1-1.tar.gz
  md5: ca74710cdecb24865816f3354037f665
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_price = django_price:main
    #
    # Would create an entry point called django_price that calls django_price.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4
    - django_deferred_polymorph >=0.1.0

  run:
    - python
    - django >=1.4
    - django_deferred_polymorph >=0.1.0

test:
  # Python imports
  imports:
    - django_price
    - django_price.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ddanier/django_price
  license: BSD License
  summary: 'Handling price information in django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
