package:
  name: dj-analytics
  version: "1.0.4"

source:
  fn: dj-analytics-1.0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/dj-analytics/dj-analytics-1.0.4.tar.gz
  md5: ce979da6ee691e4f59846e883acbe578
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dj-analytics = dj-analytics:main
    #
    # Would create an entry point called dj-analytics that calls dj-analytics.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - ipaddress
    - python-dateutil
    - django-graphos ==0.0.2a0
    - pytz
    - user-agents >=0.3.0
    - jsmin >=2.0.6

  run:
    - python
    - ipaddress
    - python-dateutil
    - django-graphos ==0.0.2a0
    - pytz
    - user-agents >=0.3.0
    - jsmin >=2.0.6

test:
  # Python imports
  imports:
    - djanalytics
    - djanalytics.admin
    - djanalytics.charts
    - djanalytics.charts.views
    - djanalytics.django_migrations
    - djanalytics.management
    - djanalytics.management.commands
    - djanalytics.migrations
    - djanalytics.reports
    - djanalytics.reports.views
    - setuptools_utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/analytehealth/django-analytics
  license: UNKNOWN
  summary: 'Django app to capture, track and display site analytics'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
