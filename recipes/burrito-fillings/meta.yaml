package:
  name: burrito-fillings
  version: "0.1.0"

source:
  fn: burrito-fillings-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/b/burrito-fillings/burrito-fillings-0.1.0.tar.gz
  md5: dc9510d5ae5728fa7cb0672449051ece
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - burrito-fillings = burrito-fillings:main
    #
    # Would create an entry point called burrito-fillings that calls burrito-fillings.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - scikit-bio >=0.2.1,<0.3.0
    - burrito <1.0.0

  run:
    - python
    - scikit-bio >=0.2.1,<0.3.0
    - burrito <1.0.0

test:
  # Python imports
  imports:
    - bfillings
    - bfillings.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/biocore/burrito-fillings
  license: BSD License
  summary: 'burrito-fillings: burrito application controllers for bioinformatics'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
