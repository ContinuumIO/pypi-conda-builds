package:
  name: django-closuretree
  version: "1.1.6"

source:
  fn: django-closuretree-1.1.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-closuretree/django-closuretree-1.1.6.tar.gz
  md5: a5d4cb7c3105e3eb55d2c588584dd61a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-closuretree = django-closuretree:main
    #
    # Would create an entry point called django-closuretree that calls django-closuretree.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4
    - django-autoconfig

  run:
    - python
    - django >=1.4
    - django-autoconfig

test:
  # Python imports
  imports:
    - closuretree

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ocadotechnology/django-closuretree/
  license: Apache Software License
  summary: 'Efficient tree-based datastructure for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
