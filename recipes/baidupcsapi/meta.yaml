package:
  name: baidupcsapi
  version: "0.3.5"

source:
  fn: baidupcsapi-0.3.5.tar.gz
  url: https://pypi.python.org/packages/source/b/baidupcsapi/baidupcsapi-0.3.5.tar.gz
  md5: 4276c5443ecb19bf4c478169024a49ac
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - baidupcsapi = baidupcsapi:main
    #
    # Would create an entry point called baidupcsapi that calls baidupcsapi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=1.1.0
    - requests_toolbelt >=0.1.2
    - rsa >=3.1.4

  run:
    - python
    - requests >=1.1.0
    - requests_toolbelt >=0.1.2
    - rsa >=3.1.4

test:
  # Python imports
  imports:
    - baidupcsapi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ly0/baidupcsapi
  license: MIT License
  summary: '\xe7\x99\xbe\xe5\xba\xa6\xe7\xbd\x91\xe7\x9b\x98API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
