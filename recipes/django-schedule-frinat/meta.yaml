package:
  name: django-schedule-frinat
  version: "0.6"

source:
  fn: django-schedule-frinat-0.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-schedule-frinat/django-schedule-frinat-0.6.tar.gz
  md5: 827a056b5f429ab274bd5edc4dbce6a5
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-schedule-frinat = django-schedule-frinat:main
    #
    # Would create an entry point called django-schedule-frinat that calls django-schedule-frinat.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - vobject
    - python-dateutil ==1.5

  run:
    - python
    - setuptools
    - vobject
    - python-dateutil ==1.5

test:
  # Python imports
  imports:
    - schedule
    - schedule.conf
    - schedule.feeds
    - schedule.management
    - schedule.management.commands
    - schedule.models
    - schedule.templatetags
    - schedule.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/thauber/django-schedule/tree/master
  license: BSD License
  summary: 'A calendaring app for Django (frinat edition).'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
