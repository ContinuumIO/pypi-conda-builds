package:
  name: barenecessities
  version: "0.2.8"

source:
  fn: BareNecessities-0.2.8.tar.gz
  url: https://pypi.python.org/packages/source/B/BareNecessities/BareNecessities-0.2.8.tar.gz
  md5: 24c06d13ce08b5fb6f871202c63c562a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - barenecessities = barenecessities:main
    #
    # Would create an entry point called barenecessities that calls barenecessities.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - barenecessities
    - bn

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://jimmyg.org/work/code/barenecessities/index.html
  license: MIT License
  summary: "Provides the ``bn`` module containing a dictionary allowing attribute access to values - I use it so much I've made into a package."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
