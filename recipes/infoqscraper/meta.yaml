package:
  name: infoqscraper
  version: "0.0.8"

source:
  fn: infoqscraper-0.0.8.tar.gz
  url: https://pypi.python.org/packages/source/i/infoqscraper/infoqscraper-0.0.8.tar.gz
  md5: a67ada87a4d036eb80906c698365ed69
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - infoqscraper = infoqscraper:main
    #
    # Would create an entry point called infoqscraper that calls infoqscraper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - beautifulsoup4
    - html5lib
    - pil

  run:
    - python
    - beautifulsoup4
    - html5lib
    - pil

test:
  # Python imports
  imports:
    - infoqscraper

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/cykl/infoqscraper
  license: License :: OSI Approved :: BSD License
  summary: 'A Web scraper for www.InfoQ.com'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
