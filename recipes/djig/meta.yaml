package:
  name: djig
  version: "0.0.1a33"

source:
  fn: djig-0.0.1a33.tar.gz
  url: https://pypi.python.org/packages/source/d/djig/djig-0.0.1a33.tar.gz
  md5: 88c59d90b774976f5134ba3db2ac3e68
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djig = djig:main
    #
    # Would create an entry point called djig that calls djig.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - djig
    - djig.management
    - djig.management.commands
    - djig.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/bmelton/djig/
  license: Creative Commons Non-Commercial, 3.0
  summary: 'Djig is a minimalist news aggregator for Django, like Digg, sort of.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
