package:
  name: django-nsa
  version: "0.1"

source:
  fn: django-nsa-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-nsa/django-nsa-0.1.tar.gz
  md5: cd278d37091f76f7402098e9e289ff10
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-nsa = django-nsa:main
    #
    # Would create an entry point called django-nsa that calls django-nsa.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4.3

  run:
    - python
    - django >=1.4.3

test:
  # Python imports
  imports:
    - nsa

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lvm/django_nsa
  license: MIT License
  summary: "An easy way for the NSA to see what's going on in our Django projects."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
