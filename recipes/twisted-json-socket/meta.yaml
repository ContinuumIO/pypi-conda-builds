package:
  name: twisted-json-socket
  version: "0.1.3"

source:
  fn: twisted-json-socket-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/t/twisted-json-socket/twisted-json-socket-0.1.3.tar.gz
  md5: 1079b0e62e0153a048863ec98c8910e0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - twisted-json-socket = twisted-json-socket:main
    #
    # Would create an entry point called twisted-json-socket that calls twisted-json-socket.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - twisted

  run:
    - python
    - twisted

test:
  # Python imports
  imports:
    - twistedjsonsocket

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/madssj/twisted-json-socket
  license: BSD
  summary: 'Protocol for twisted json socket'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
