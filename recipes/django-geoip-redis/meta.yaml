package:
  name: django-geoip-redis
  version: "1.2.2"

source:
  fn: django-geoip-redis-1.2.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-geoip-redis/django-geoip-redis-1.2.2.tar.gz
  md5: a6f8634defd6bbc72ec8487a64787d78
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-geoip-redis = django-geoip-redis:main
    #
    # Would create an entry point called django-geoip-redis that calls django-geoip-redis.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - redis >=2.8.0
    - hiredis >=0.1.1
    - ipaddress >=1.0.6
    - django-celery >=3.0.23
    - requests >=2.0.1

  run:
    - python
    - redis >=2.8.0
    - hiredis >=0.1.1
    - ipaddress >=1.0.6
    - django-celery >=3.0.23
    - requests >=2.0.1

test:
  # Python imports
  imports:
    - docs
    - geoip
    - geoip.management
    - geoip.management.commands
    - geoip.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/gotlium/django-geoip-redis
  license: GNU General Public License v3 (GPLv3)
  summary: 'Django GeoIP. Based on default DB or Redis.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
