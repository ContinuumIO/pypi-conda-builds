package:
  name: django-feedstorage
  version: "0.1"

source:
  fn: django-feedstorage-0.1.zip
  url: https://pypi.python.org/packages/source/d/django-feedstorage/django-feedstorage-0.1.zip
  md5: 1172a709e5eb3c416b15e547b4a5bcc1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-feedstorage = django-feedstorage:main
    #
    # Would create an entry point called django-feedstorage that calls django-feedstorage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - feedstorage
    - feedstorage.management
    - feedstorage.management.commands
    - feedstorage.tests
    - feedstorage.tests.utils
    - feedstorage.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/YAmikep/django-feedstorage
  license: BSD License
  summary: 'An application designed for storing Feeds entries and notifying subscribers only when there are new entries.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
