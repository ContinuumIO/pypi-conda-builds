package:
  name: django-dbconf
  version: "0.2"

source:
  fn: django-dbconf-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-dbconf/django-dbconf-0.2.tar.gz
  md5: d10898c1460a34501b587b57eb623d5e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-dbconf = django-dbconf:main
    #
    # Would create an entry point called django-dbconf that calls django-dbconf.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - distribute

  run:
    - python
    - distribute

test:
  # Python imports
  imports:
    - django_dbconf

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/niwibe/django-dbconf
  license: BSD License
  summary: 'Simple helper to save settings in a database and efficient access to this data through cache.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
