package:
  name: change_lrc_to_txt
  version: "0.5.0"

source:
  fn: change_lrc_to_txt-0.5.0.zip
  url: https://pypi.python.org/packages/source/c/change_lrc_to_txt/change_lrc_to_txt-0.5.0.zip
  md5: d5ab00539b03b84101bbf156f43799c7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - change_lrc_to_txt = change_lrc_to_txt:main
    #
    # Would create an entry point called change_lrc_to_txt that calls change_lrc_to_txt.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - change_lrc_to_txt

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.totoov.com
  license: UNKNOWN
  summary: 'A simple code to change the lrs documents to readable ones'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
