package:
  name: flask-cuddlyrest
  version: "0.1.15"

source:
  fn: Flask-CuddlyRest-0.1.15.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-CuddlyRest/Flask-CuddlyRest-0.1.15.tar.gz
  md5: faa19b990d36a9c013d8682d88cf392f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-cuddlyrest = flask-cuddlyrest:main
    #
    # Would create an entry point called flask-cuddlyrest that calls flask-cuddlyrest.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask-views
    - flask-restful
    - mongoengine

  run:
    - python
    - flask-views
    - flask-restful
    - mongoengine

test:
  # Python imports
  imports:
    - flask_cuddlyrest
    - flask_cuddlyrest.ext
    - test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/wuurrd/flask-cuddlyrest
  license: MIT
  summary: 'Flask restful API framework for MongoDB/MongoEngine'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
