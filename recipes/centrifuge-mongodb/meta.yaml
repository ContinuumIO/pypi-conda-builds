package:
  name: centrifuge-mongodb
  version: "0.3.0"

source:
  fn: centrifuge-mongodb-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/c/centrifuge-mongodb/centrifuge-mongodb-0.3.0.tar.gz
  md5: d7f3e7028fd5dc6ddad2c91dc02d366d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - centrifuge-mongodb = centrifuge-mongodb:main
    #
    # Would create an entry point called centrifuge-mongodb that calls centrifuge-mongodb.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - centrifuge
    - six >=1.3.0
    - motor ==0.3.4

  run:
    - python
    - centrifuge
    - six >=1.3.0
    - motor ==0.3.4

test:
  # Python imports
  imports:
    - centrifuge_mongodb

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/centrifugal/centrifuge-mongodb
  license: Apache Software License
  summary: 'MongoDB structure backend for Centrifuge'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
