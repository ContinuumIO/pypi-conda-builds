package:
  name: zettwerk.mobiletheming
  version: "0.2.1"

source:
  fn: zettwerk.mobiletheming-0.2.1.zip
  url: https://pypi.python.org/packages/source/z/zettwerk.mobiletheming/zettwerk.mobiletheming-0.2.1.zip
  md5: 99dfdf09e16c294c6cb0fbe74d6da9ce
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zettwerk.mobiletheming = zettwerk.mobiletheming:main
    #
    # Would create an entry point called zettwerk.mobiletheming that calls zettwerk.mobiletheming.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - zettwerk
    - zettwerk.mobiletheming
    - zettwerk.mobiletheming.browser
    - zettwerk.mobiletheming.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.zettwerk.com
  license: gpl
  summary: 'Switching mobile themes based on urls in plone.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
