package:
  name: django-testproject
  version: "0.1.2"

source:
  fn: django-testproject-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-testproject/django-testproject-0.1.2.tar.gz
  md5: 9bb668339eb9745ed5aa30b7ebac7f44
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-testproject = django-testproject:main
    #
    # Would create an entry point called django-testproject that calls django-testproject.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.0

  run:
    - python
    - django >=1.0

test:
  # Python imports
  imports:
    - django_testproject

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://git.linaro.org/lava/django-testproject.git
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Universal project for running unit tests of Django applications'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
