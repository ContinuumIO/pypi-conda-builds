package:
  name: sqlalchemy-fixtures
  version: "0.1.6"

source:
  fn: SQLAlchemy-Fixtures-0.1.6.tar.gz
  url: https://pypi.python.org/packages/source/S/SQLAlchemy-Fixtures/SQLAlchemy-Fixtures-0.1.6.tar.gz
  md5: 5150a7d95baafcef654e33f07d1c6904
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sqlalchemy-fixtures = sqlalchemy-fixtures:main
    #
    # Would create an entry point called sqlalchemy-fixtures that calls sqlalchemy-fixtures.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sqlalchemy >=0.8

  run:
    - python
    - sqlalchemy >=0.8

test:
  # Python imports
  imports:
    - sqlalchemy_fixtures

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kvesteri/sqlalchemy-fixtures
  license: BSD License
  summary: 'Functional fixtures for SQLAlchemy models.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
