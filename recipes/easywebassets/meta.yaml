package:
  name: easywebassets
  version: "0.1"

source:
  fn: easywebassets-0.1.tar.gz
  url: https://pypi.python.org/packages/source/e/easywebassets/easywebassets-0.1.tar.gz
  md5: cba7419580a8ee66d4748fded2020014
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - easywebassets = easywebassets:main
    #
    # Would create an entry point called easywebassets that calls easywebassets.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - webassets

  run:
    - python
    - webassets

test:
  # Python imports
  imports:
    - easywebassets

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/frascoweb/easywebassets
  license: MIT
  summary: 'An easier way to use webassets'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
