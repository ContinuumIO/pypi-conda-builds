package:
  name: js.hogan
  version: "3.0.0.3"

source:
  fn: js.hogan-3.0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/j/js.hogan/js.hogan-3.0.0.3.tar.gz
  md5: 33d4e32cfbd3283bf1f6bf449d7d6a55
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - js.hogan = js.hogan:main
    #
    # Would create an entry point called js.hogan that calls js.hogan.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fanstatic >=0.16

  run:
    - python
    - setuptools
    - fanstatic >=0.16

test:
  # Python imports
  imports:
    - js
    - js.hogan

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.merchise.org
  license: GPL
  summary: u"Fanstatic Twitter's Hogan.js"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
