package:
  name: django-inviter2
  version: "1.0.5"

source:
  fn: django-inviter2-1.0.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-inviter2/django-inviter2-1.0.5.tar.gz
  md5: 57290266e075af605a98112bb77a9e1a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-inviter2 = django-inviter2:main
    #
    # Would create an entry point called django-inviter2 that calls django-inviter2.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - shortuuid >=0.1
    - six
    - django >=1.4

  run:
    - python
    - shortuuid >=0.1
    - six
    - django >=1.4

test:
  # Python imports
  imports:
    - inviter2

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mjschultz/django-inviter2
  license: BSD License
  summary: 'Simple email invitations for your Django app'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
