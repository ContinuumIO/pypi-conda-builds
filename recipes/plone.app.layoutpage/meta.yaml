package:
  name: plone.app.layoutpage
  version: "1.1"

source:
  fn: plone.app.layoutpage-1.1.tar.gz
  url: https://pypi.python.org/packages/source/p/plone.app.layoutpage/plone.app.layoutpage-1.1.tar.gz
  md5: a8eb36ddfdf320761c4e6f7f79d8c8a3
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.app.layoutpage = plone.app.layoutpage:main
    #
    # Would create an entry point called plone.app.layoutpage that calls plone.app.layoutpage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zope.interface
    - zope.component
    - zope.lifecycleevent
    - z3c.form
    - zope2
    - plone.dexterity
    - plone.app.blocks
    - plone.app.dexterity

  run:
    - python
    - setuptools
    - setuptools
    - zope.interface
    - zope.component
    - zope.lifecycleevent
    - z3c.form
    - zope2
    - plone.dexterity
    - plone.app.blocks
    - plone.app.dexterity

test:
  # Python imports
  imports:
    - plone
    - plone.app
    - plone.app.layoutpage
    - plone.app.layoutpage.browser
    - plone.app.layoutpage.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/plone/plone.app.layoutpage
  license: GPL
  summary: 'A basic page content type for Plone using Deco layout'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
