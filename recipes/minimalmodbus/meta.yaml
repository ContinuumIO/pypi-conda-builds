package:
  name: minimalmodbus
  version: "0.6"

source:
  fn: MinimalModbus-0.6.tar.gz
  url: https://pypi.python.org/packages/source/M/MinimalModbus/MinimalModbus-0.6.tar.gz
  md5: 4bc296fab36a2b2b8c8cf77c0b6f6d7a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - minimalmodbus = minimalmodbus:main
    #
    # Would create an entry point called minimalmodbus that calls minimalmodbus.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - pyserial

  run:
    - python
    - pyserial

test:
  # Python imports
  imports:
    - minimalmodbus

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://minimalmodbus.sourceforge.net/
  license: Apache Software License
  summary: 'Easy-to-use Modbus RTU and Modbus ASCII implementation for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
