package:
  name: openerp-pad-project
  version: "7.0.406"

source:
  fn: openerp-pad-project-7.0.406.tar.gz
  url: https://pypi.python.org/packages/source/o/openerp-pad-project/openerp-pad-project-7.0.406.tar.gz
  md5: 964eb444487985a39f5a2e118fa13137
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - openerp-pad-project = openerp-pad-project:main
    #
    # Would create an entry point called openerp-pad-project that calls openerp-pad-project.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - openerp-project
    - openerp-pad

  run:
    - python
    - openerp-project
    - openerp-pad

test:
  # Python imports
  imports:
    - openerp.addons.pad_project

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.openerp.com
  license: GNU Affero General Public License v3
  summary: 'Pad on tasks'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
