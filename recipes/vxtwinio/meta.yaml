package:
  name: vxtwinio
  version: "0.0.3"

source:
  fn: vxtwinio-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/v/vxtwinio/vxtwinio-0.0.3.tar.gz
  md5: 3ca4d802639e2ff7990669ff9bd7e4af
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - vxtwinio = vxtwinio:main
    #
    # Would create an entry point called vxtwinio that calls vxtwinio.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - vumi
    - klein
    - treq

  run:
    - python
    - vumi
    - klein
    - treq

test:
  # Python imports
  imports:
    - vxtwinio
    - vxtwinio.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/praekelt/vumi-twilio-api
  license: BSD License
  summary: 'Provides a REST API to Vumi that emulates the Twilio API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
