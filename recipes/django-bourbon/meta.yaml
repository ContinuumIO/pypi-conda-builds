package:
  name: django-bourbon
  version: "3.1.8"

source:
  fn: django-bourbon-3.1.8.tar.gz
  url: https://pypi.python.org/packages/source/d/django-bourbon/django-bourbon-3.1.8.tar.gz
  md5: 2116ee2c3f71e733606e1ec96cd82327
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-bourbon = django-bourbon:main
    #
    # Would create an entry point called django-bourbon that calls django-bourbon.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - bourbon

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/benbacardi/django-bourbon
  license: MIT License
  summary: 'Bourbon (http://bourbon.io/) static files packaged in a django app to speed up new applications and deployment.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
