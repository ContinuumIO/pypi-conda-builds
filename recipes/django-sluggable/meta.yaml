package:
  name: django-sluggable
  version: "0.2.5"

source:
  fn: django-sluggable-0.2.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-sluggable/django-sluggable-0.2.5.tar.gz
  md5: a310ce3502314a0987c7fdc68a0b8bd1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-sluggable = django-sluggable:main
    #
    # Would create an entry point called django-sluggable that calls django-sluggable.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - sluggable
    - sluggable.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/thoas/django-sluggable
  license: MIT License
  summary: 'Slug your multiple models and manage redirections'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
