package:
  name: redistruct
  version: "0.0.1"

source:
  fn: redistruct-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/r/redistruct/redistruct-0.0.1.tar.gz
  md5: 4ee1c98805bc2998cf6b4980547d4cac
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - redistruct = redistruct:main
    #
    # Would create an entry point called redistruct that calls redistruct.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - redis

  run:
    - python
    - redis

test:
  # Python imports
  imports:
    - redistruct

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/youknowone/redistruct
  license: UNKNOWN
  summary: 'Human-friendly structured redis API wrapper.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
