package:
  name: collective.recipe.cmd
  version: "0.10"

source:
  fn: collective.recipe.cmd-0.10.zip
  url: https://pypi.python.org/packages/source/c/collective.recipe.cmd/collective.recipe.cmd-0.10.zip
  md5: 72d554fb3a0d0dc260c7268e118a02a4
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.recipe.cmd = collective.recipe.cmd:main
    #
    # Would create an entry point called collective.recipe.cmd that calls collective.recipe.cmd.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout

test:
  # Python imports
  imports:
    - collective
    - collective.recipe
    - collective.recipe.cmd
    - collective.recipe.cmd.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/collective-recipes
  license: BSD License
  summary: 'A Buildout recipe to execute commands in the console user interface'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
