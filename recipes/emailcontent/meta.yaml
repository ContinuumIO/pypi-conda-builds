package:
  name: emailcontent
  version: "0.0dev"

source:
  fn: emailcontent-0.1dev.tar.gz
  url: https://pypi.python.org/packages/source/e/emailcontent/emailcontent-0.1dev.tar.gz
  md5: a07f15c17e0813f3f076d69d950fdc23
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - emailcontent = emailcontent:main
    #
    # Would create an entry point called emailcontent that calls emailcontent.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - emailcontent

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: GNU Lesser General Public License v3 (LGPLv3)
  summary: 'Functionality for extracting content from email messages.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
