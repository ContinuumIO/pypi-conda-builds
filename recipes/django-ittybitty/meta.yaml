package:
  name: django-ittybitty
  version: "0.1.0-pre2"

source:
  fn: django-ittybitty-0.1.0-pre2.tar.bz2
  url: https://pypi.python.org/packages/source/d/django-ittybitty/django-ittybitty-0.1.0-pre2.tar.bz2
  md5: 72ef8f1a3c5fea9cb815b84ff081971e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-ittybitty = django-ittybitty:main
    #
    # Would create an entry point called django-ittybitty that calls django-ittybitty.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - ittybitty
    - ittybitty.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/django-ittybitty/
  license: BSD License
  summary: 'Shortened URLs for your Django-powered site!'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
