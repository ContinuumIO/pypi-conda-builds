package:
  name: vdt.versionplugin.hotfix
  version: "0.0.8"

source:
  fn: vdt.versionplugin.hotfix-0.0.8.tar.gz
  url: https://pypi.python.org/packages/source/v/vdt.versionplugin.hotfix/vdt.versionplugin.hotfix-0.0.8.tar.gz
  md5: f2ee9049c77c7f8e047b24af78ec1dbf
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - vdt.versionplugin.hotfix = vdt.versionplugin.hotfix:main
    #
    # Would create an entry point called vdt.versionplugin.hotfix that calls vdt.versionplugin.hotfix.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - vdt.version
    - vdt.versionplugin.default
    - vdt.versionplugin.debianize

  run:
    - python
    - setuptools
    - vdt.version
    - vdt.versionplugin.default
    - vdt.versionplugin.debianize

test:
  # Python imports
  imports:
    - vdt
    - vdt.versionplugin
    - vdt.versionplugin.hotfix

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/devopsconsulting/vdt.versionplugin.hotfix
  license: UNKNOWN
  summary: 'Create hotfix packages, these add an iteration on an existing version and do not create a tag.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
