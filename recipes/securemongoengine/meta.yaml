package:
  name: securemongoengine
  version: "0.1.2"

source:
  fn: SecureMongoEngine-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/S/SecureMongoEngine/SecureMongoEngine-0.1.2.tar.gz
  md5: 5313c06d4009fe15cad9b89dda729624
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - securemongoengine = securemongoengine:main
    #
    # Would create an entry point called securemongoengine that calls securemongoengine.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pymongo >=2.7.2
    - mongoengine >=0.8.7
    - pycrypto >=2.6.1

  run:
    - python
    - pymongo >=2.7.2
    - mongoengine >=0.8.7
    - pycrypto >=2.6.1

test:
  # Python imports
  imports:
    - securemongoengine
    - securemongoengine.base
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/juancho088/SecureMongoEngine
  license: Apache License
  summary: 'It is a library for MongoEngine that you can use to encrypt certain fields of your models.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
