package:
  name: django-easy-response
  version: "1.0.0"

source:
  fn: django-easy-response-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-easy-response/django-easy-response-1.0.0.tar.gz
  md5: cbe8dd7a2c0a0c00826490911f16d9a9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-easy-response = django-easy-response:main
    #
    # Would create an entry point called django-easy-response that calls django-easy-response.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - simplejson

  run:
    - python
    - django
    - simplejson

test:
  # Python imports
  imports:
    - easy_response
    - easy_response.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/laginha/django-easy-response
  license: MIT License
  summary: 'Return HTTP responses in a easier way'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
