package:
  name: dolmen.forms.crud
  version: "1.0"

source:
  fn: dolmen.forms.crud-1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/dolmen.forms.crud/dolmen.forms.crud-1.0.tar.gz
  md5: 7b1e6d993557626074299ffe218afbf1
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dolmen.forms.crud = dolmen.forms.crud:main
    #
    # Would create an entry point called dolmen.forms.crud that calls dolmen.forms.crud.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - dolmen.content >=0.7.1
    - dolmen.forms.base >=1.0b1
    - grokcore.component
    - martian
    - setuptools
    - zeam.form.base
    - zeam.form.layout
    - zeam.form.ztk
    - zope.cachedescriptors
    - zope.component >=3.9.1
    - zope.container
    - zope.dublincore
    - zope.event
    - zope.i18n
    - zope.i18nmessageid
    - zope.interface
    - zope.lifecycleevent
    - zope.location
    - zope.publisher
    - zope.schema
    - zope.security
    - zope.traversing

  run:
    - python
    - dolmen.content >=0.7.1
    - dolmen.forms.base >=1.0b1
    - grokcore.component
    - martian
    - setuptools
    - zeam.form.base
    - zeam.form.layout
    - zeam.form.ztk
    - zope.cachedescriptors
    - zope.component >=3.9.1
    - zope.container
    - zope.dublincore
    - zope.event
    - zope.i18n
    - zope.i18nmessageid
    - zope.interface
    - zope.lifecycleevent
    - zope.location
    - zope.publisher
    - zope.schema
    - zope.security
    - zope.traversing

test:
  # Python imports
  imports:
    - dolmen
    - dolmen.forms
    - dolmen.forms.crud
    - dolmen.forms.crud.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.dolmen-project.org
  license: GNU General Public License (GPL)
  summary: 'CRUD forms and actions for `dolmen.content`'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
