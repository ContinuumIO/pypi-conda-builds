package:
  name: optutils
  version: ".02.6"

source:
  fn: optutils-.02.6.tar.gz
  url: https://pypi.python.org/packages/source/o/optutils/optutils-.02.6.tar.gz
  md5: 999cac204100d0515a76c93e1e7dbfd6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - optutils = optutils:main
    #
    # Would create an entry point called optutils that calls optutils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - optutils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://www.github.com/timtadh/optutils
  license: GPL
  summary: 'Small, composable utilities for writing great command line tools.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
