package:
  name: imposm.parser
  version: "1.0.7"

source:
  fn: imposm.parser-1.0.7.tar.gz
  url: https://pypi.python.org/packages/source/i/imposm.parser/imposm.parser-1.0.7.tar.gz
  md5: e472a2c2b766e9682a23d468f9ce9773
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - imposm.parser = imposm.parser:main
    #
    # Would create an entry point called imposm.parser that calls imposm.parser.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - imposm
    - imposm.parser
    - imposm.parser.pbf
    - imposm.parser.test
    - imposm.parser.xml

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://imposm.org/docs/imposm.parser/latest/
  license: Apache Software License
  summary: 'Fast and easy OpenStreetMap XML/PBF parser.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
