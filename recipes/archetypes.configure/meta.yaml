package:
  name: archetypes.configure
  version: "0.2"

source:
  fn: archetypes.configure-0.2.tar.gz
  url: https://pypi.python.org/packages/source/a/archetypes.configure/archetypes.configure-0.2.tar.gz
  md5: 559b3fe722686e1b20764ad2a58f29c5
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - archetypes.configure = archetypes.configure:main
    #
    # Would create an entry point called archetypes.configure that calls archetypes.configure.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - archetypes
    - archetypes.configure
    - archetypes.configure.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: GNU General Public License (GPL)
  summary: 'Declarative content type configuration for Archetypes.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
