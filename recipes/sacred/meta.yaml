package:
  name: sacred
  version: "0.6.3"

source:
  fn: sacred-0.6.3.tar.gz
  url: https://pypi.python.org/packages/source/s/sacred/sacred-0.6.3.tar.gz
  md5: a7ea09598bcdb87c729329ccf996e518
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sacred = sacred:main
    #
    # Would create an entry point called sacred that calls sacred.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - docopt
    - six
    - wrapt

  run:
    - python
    - docopt
    - six
    - wrapt

test:
  # Python imports
  imports:
    - sacred
    - sacred.config
    - sacred.observers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/IDSIA/sacred
  license: MIT License
  summary: 'Facilitates automated and reproducible experimental research'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
