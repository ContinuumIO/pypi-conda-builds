package:
  name: django-gearman
  version: "0.2"

source:
  fn: django-gearman-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-gearman/django-gearman-0.2.tar.gz
  md5: 08c9f2401a545f55f2bef1df3a578096
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-gearman = django-gearman:main
    #
    # Would create an entry point called django-gearman that calls django-gearman.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - gearman >=2.0.0

  run:
    - python
    - gearman >=2.0.0

test:
  # Python imports
  imports:
    - django_gearman
    - django_gearman.management
    - django_gearman.management.commands
    - gearman_example
    - gearman_example.management
    - gearman_example.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/fwenzel/django-gearman
  license: MPL
  summary: 'A convenience wrapper for Gearman clients and workers in Django/Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
