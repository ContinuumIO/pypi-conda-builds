package:
  name: atsd_client
  version: "1.0.1"

source:
  fn: atsd_client-1.0.1.zip
  url: https://pypi.python.org/packages/source/a/atsd_client/atsd_client-1.0.1.zip
  md5: 8e11b08745b8d666d2fb88cbe2e5a728
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - atsd_client = atsd_client:main
    #
    # Would create an entry point called atsd_client that calls atsd_client.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - atsd_client
    - atsd_client.models

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/axibase/atsd-api-python
  license: Apache Software License
  summary: 'Axibase Time-Series Database API Client for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
