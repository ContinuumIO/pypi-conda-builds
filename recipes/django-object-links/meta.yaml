package:
  name: django-object-links
  version: "0.1.6-alpha-1"

source:
  fn: django-object-links-0.1.6-alpha-1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-object-links/django-object-links-0.1.6-alpha-1.tar.gz
  md5: 581bb01891bae4d37571c0dc2039b78a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-object-links = django-object-links:main
    #
    # Would create an entry point called django-object-links that calls django-object-links.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - object_links
    - object_links.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/xenofox/django-object-links/
  license: MIT License
  summary: 'Easy Link and Menu management for Django sites'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
