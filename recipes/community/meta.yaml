package:
  name: community
  version: "1.0.0b1"

source:
  fn: community-1.0.0b1.tar.gz
  url: https://pypi.python.org/packages/source/c/community/community-1.0.0b1.tar.gz
  md5: d1bedeb84cfc9929e04d3df960f9df50
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - community = community:main
    #
    # Would create an entry point called community that calls community.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - flask
    - requests

  run:
    - python
    - flask
    - requests

test:
  # Python imports
  imports:
    - community

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ewdurbin/community
  license: MIT
  summary: 'merge together wellness checks to unify your shit'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
