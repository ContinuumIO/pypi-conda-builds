package:
  name: copulalib
  version: "1.1.0"

source:
  fn: copulalib-1.1.0.tar.gz
  url: https://pypi.python.org/packages/source/c/copulalib/copulalib-1.1.0.tar.gz
  md5: 7f72fa5ba68b218e1bd10e1856282a06
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - copulalib = copulalib:main
    #
    # Would create an entry point called copulalib that calls copulalib.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - copulalib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://ambhas.com/
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'A library for the copula'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
