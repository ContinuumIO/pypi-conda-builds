package:
  name: django-forum
  version: "0.0.1"

source:
  fn: django-forum-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-forum/django-forum-0.0.1.tar.gz
  md5: b576b8e1e6002eea96958a8c6df3224b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-forum = django-forum:main
    #
    # Would create an entry point called django-forum that calls django-forum.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - forum

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dimka665/django-forum
  license: MIT License
  summary: 'Django forum app'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
