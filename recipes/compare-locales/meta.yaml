package:
  name: compare-locales
  version: "0.9.7"

source:
  fn: compare-locales-0.9.7.tar.gz
  url: https://pypi.python.org/packages/source/c/compare-locales/compare-locales-0.9.7.tar.gz
  md5: 2841d051faa658d6ab046a0b3944574e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - compare-locales = compare-locales:main
    #
    # Would create an entry point called compare-locales that calls compare-locales.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - Mozilla

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Mozilla Public License 2.0 (MPL 2.0)
  summary: 'Python library and scripts to assist in localizing Mozilla applications'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
