package:
  name: allpairs
  version: "2.0.1"

source:
  fn: AllPairs-2.0.1.tar.gz
  url: https://pypi.python.org/packages/source/A/AllPairs/AllPairs-2.0.1.tar.gz
  md5: 750480cf184a26a575b538745556dba1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - allpairs = allpairs:main
    #
    # Would create an entry point called allpairs that calls allpairs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - allpairs
    - metacomm
    - metacomm.combinatorics

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://apps.sourceforge.net/trac/allpairs/
  license: MIT License
  summary: 'Pairwise test combinations generator'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
