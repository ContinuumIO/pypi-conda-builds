package:
  name: htwkimn.theme
  version: "1.0.3"

source:
  fn: htwkimn.theme-1.0.3.tar.gz
  url: https://pypi.python.org/packages/source/h/htwkimn.theme/htwkimn.theme-1.0.3.tar.gz
  md5: caa0d701fc8f0135c1e3af4d9f98c32c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - htwkimn.theme = htwkimn.theme:main
    #
    # Would create an entry point called htwkimn.theme that calls htwkimn.theme.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone >=4.3.3
    - plonetheme.onegov >=1.5.3
    - ftw.footer
    - ftw.mobilenavigation
    - ftw.contentpage >=1.11.3
    - ftw.contenttemplates
    - ftw.contentmenu
    - six ==1.4.1
    - simplelayout.base >=4.0.5
    - ftw.upgrade >=1.14.4

  run:
    - python
    - setuptools
    - setuptools
    - plone >=4.3.3
    - plonetheme.onegov >=1.5.3
    - ftw.footer
    - ftw.mobilenavigation
    - ftw.contentpage >=1.11.3
    - ftw.contenttemplates
    - ftw.contentmenu
    - six ==1.4.1
    - simplelayout.base >=4.0.5
    - ftw.upgrade >=1.14.4

test:
  # Python imports
  imports:
    - htwkimn
    - htwkimn.theme

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lkosubek/htwkimn.theme
  license: GPL
  summary: 'A diazo child theme product, that inherits extends the responsive diazo theme plonetheme.onegov, for the faculty IMN of the University of Applied Since Leipzig (HTWK), Germany.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
