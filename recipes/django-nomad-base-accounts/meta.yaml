package:
  name: django-nomad-base-accounts
  version: "2.3.5"

source:
  fn: django-nomad-base-accounts-2.3.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-nomad-base-accounts/django-nomad-base-accounts-2.3.5.tar.gz
  md5: e7d9828177eb7ff4f8fba283ec7cb925
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-nomad-base-accounts = django-nomad-base-accounts:main
    #
    # Would create an entry point called django-nomad-base-accounts that calls django-nomad-base-accounts.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - base_accounts

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Nomadblue/django-nomad-base-accounts
  license: BSD License
  summary: 'Django app from nomadblue.com that provides the basic foundations to manage email-based users.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
