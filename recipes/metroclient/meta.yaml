package:
  name: metroclient
  version: "1.31"

source:
  fn: metroclient-1.31.tar.gz
  url: https://pypi.python.org/packages/source/m/metroclient/metroclient-1.31.tar.gz
  md5: 2e75f0f03fca9621240370977cf36665
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - metroclient = metroclient:main
    #
    # Would create an entry point called metroclient that calls metroclient.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - httplib2
    - paramiko

  run:
    - python
    - httplib2
    - paramiko

test:
  # Python imports
  imports:
    - metroclient

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://apphosted.com
  license: GNU General Public License (GPL)
  summary: 'AppHosted application deployment utility'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
