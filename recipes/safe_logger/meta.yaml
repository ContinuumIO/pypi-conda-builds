package:
  name: safe_logger
  version: "1.0.0"

source:
  fn: safe_logger-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/s/safe_logger/safe_logger-1.0.0.tar.gz
  md5: ba63d3677294ca38828b34f5e23e7e2e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - safe_logger = safe_logger:main
    #
    # Would create an entry point called safe_logger that calls safe_logger.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - safe_logger

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/cybergrind/safe_logger
  license: LGPL
  summary: 'Safe TimedRotatingFileHandler for concurrent writers'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
