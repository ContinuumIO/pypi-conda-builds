package:
  name: karait
  version: "1.0.0"

source:
  fn: karait-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/k/karait/karait-1.0.0.tar.gz
  md5: bdaeda35a8c37cbbfb679a11a1ad1876
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - karait = karait:main
    #
    # Would create an entry point called karait that calls karait.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pymongo ==2.0.1

  run:
    - python
    - pymongo ==2.0.1

test:
  # Python imports
  imports:
    - karait
    - karait.model
    - test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/bcoe/karait
  license: UNKNOWN
  summary: 'A ridiculously simple cross-language queuing system, built on top of MongoDB.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
