package:
  name: zope.ramcache
  version: "2.1.0"

source:
  fn: zope.ramcache-2.1.0.tar.gz
  url: https://pypi.python.org/packages/source/z/zope.ramcache/zope.ramcache-2.1.0.tar.gz
  md5: 25df655636cd0e61eff21c0d97ad7912
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zope.ramcache = zope.ramcache:main
    #
    # Would create an entry point called zope.ramcache that calls zope.ramcache.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - persistent
    - setuptools
    - zope.interface
    - zope.location
    - zope.testing

  run:
    - python
    - persistent
    - setuptools
    - zope.interface
    - zope.location
    - zope.testing

test:
  # Python imports
  imports:
    - zope
    - zope.ramcache
    - zope.ramcache.interfaces
    - zope.ramcache.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/zope.ramcache
  license: Zope Public License
  summary: 'Zope RAM Cache'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
