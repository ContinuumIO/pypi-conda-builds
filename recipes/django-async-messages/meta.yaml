package:
  name: django-async-messages
  version: "0.3.1"

source:
  fn: django-async-messages-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-async-messages/django-async-messages-0.3.1.tar.gz
  md5: bfc6c79eb0d56bf008ae50024030219e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-async-messages = django-async-messages:main
    #
    # Would create an entry point called django-async-messages that calls django-async-messages.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.2

  run:
    - python
    - django >=1.2

test:
  # Python imports
  imports:
    - async_messages

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/codeinthehole/django-async-messages
  license: UNKNOWN
  summary: 'Send asynchronous messages to users (eg from offline scripts).  Useful for integration with Celery'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
