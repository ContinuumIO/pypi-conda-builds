package:
  name: concurrent-iterator
  version: "0.1"

source:
  fn: concurrent-iterator-0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/concurrent-iterator/concurrent-iterator-0.1.tar.gz
  md5: 542fc61135174bb7d60b6e5bfba1860f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - concurrent-iterator = concurrent-iterator:main
    #
    # Would create an entry point called concurrent-iterator that calls concurrent-iterator.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - concurrent_iterator
    - concurrent_iterator.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jruere/concurrent-iterator
  license: GNU Lesser General Public License v3 or later (LGPLv3+)
  summary: 'Iterators that consume and buffer generator values in a background thread/process.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
