package:
  name: django-render-load
  version: "0.0dev"

source:
  fn: django-render-load-0.0dev.tar.gz
  url: https://pypi.python.org/packages/source/d/django-render-load/django-render-load-0.0dev.tar.gz
  md5: 69350b6b4507dc9e3fd574db95058d30
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-render-load = django-render-load:main
    #
    # Would create an entry point called django-render-load that calls django-render-load.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - djangorenderload
    - djangorenderload.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: UNKNOWN
  summary: 'Django template tags render_object and load_object grouped together.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
