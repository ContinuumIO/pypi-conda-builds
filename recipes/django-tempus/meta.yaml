package:
  name: django-tempus
  version: "0.7.0"

source:
  fn: django-tempus-0.7.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-tempus/django-tempus-0.7.0.tar.gz
  md5: d4f3f520b5df6d24025bdcdc5b4e2fca
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-tempus = django-tempus:main
    #
    # Would create an entry point called django-tempus that calls django-tempus.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4
    - urlobject >=2.3.4

  run:
    - python
    - django >=1.4
    - urlobject >=2.3.4

test:
  # Python imports
  imports:
    - tempus
    - tempus/contrib
    - tempus/contrib/auto_login
    - tempus/templatetags
    - tempus/tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/juanriaza/django-tempus
  license: BSD License
  summary: 'Django Tempus provides url tokens that triggers custom actions'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
