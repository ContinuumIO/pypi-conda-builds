package:
  name: zettwerk.users
  version: "0.1"

source:
  fn: zettwerk.users-0.1.tar.gz
  url: https://pypi.python.org/packages/source/z/zettwerk.users/zettwerk.users-0.1.tar.gz
  md5: 520d449eb5f6f15356e1b34653f6312d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zettwerk.users = zettwerk.users:main
    #
    # Would create an entry point called zettwerk.users that calls zettwerk.users.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - zettwerk
    - zettwerk.users
    - zettwerk.users.browser
    - zettwerk.users.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/zettwerk.users
  license: GNU General Public License (GPL)
  summary: 'Additional user information for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
