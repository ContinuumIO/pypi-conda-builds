package:
  name: importwatch
  version: "0.2"

source:
  fn: importwatch-0.2.tar.gz
  url: https://pypi.python.org/packages/source/i/importwatch/importwatch-0.2.tar.gz
  md5: 6d924b443bd43d13c28a72206d456c25
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - importwatch = importwatch:main
    #
    # Would create an entry point called importwatch that calls importwatch.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - importwatch

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/storborg/importwatch
  license: GNU General Public License (GPL)
  summary: 'Track packages.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
