package:
  name: duo_client
  version: "2.1"

source:
  fn: duo_client-2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/duo_client/duo_client-2.1.tar.gz
  md5: 987994f6bac12e355e1fb4c1d4f2f83e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - duo_client = duo_client:main
    #
    # Would create an entry point called duo_client that calls duo_client.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - duo_client

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/duosecurity/duo_client_python
  license: BSD License
  summary: 'Reference client for Duo Security APIs'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
