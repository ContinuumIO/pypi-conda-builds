package:
  name: django-casscache
  version: "0.1.0"

source:
  fn: django-casscache-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-casscache/django-casscache-0.1.0.tar.gz
  md5: f48133fa6ad5880fd0569cb10519031c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-casscache = django-casscache:main
    #
    # Would create an entry point called django-casscache that calls django-casscache.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - casscache

  run:
    - python
    - casscache

test:
  # Python imports
  imports:
    - django_casscache
    - django_casscache.management
    - django_casscache.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mattrobenolt/django-casscache
  license: GNU General Public License (GPL)
  summary: 'Django backend for python-casscache'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
