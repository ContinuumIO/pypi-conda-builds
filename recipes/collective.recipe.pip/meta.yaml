package:
  name: collective.recipe.pip
  version: "0.3.0"

source:
  fn: collective.recipe.pip-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.recipe.pip/collective.recipe.pip-0.3.0.tar.gz
  md5: 0302884834db035e361c3005d124e81c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.recipe.pip = collective.recipe.pip:main
    #
    # Would create an entry point called collective.recipe.pip that calls collective.recipe.pip.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - pip

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - pip

test:
  # Python imports
  imports:
    - collective
    - collective.recipe
    - collective.recipe.pip
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/collective/collective.recipe.pip
  license: GNU General Public License (GPL)
  summary: 'zc.buildout recipe to parse pip config files ang use parsed info in a buildout.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
