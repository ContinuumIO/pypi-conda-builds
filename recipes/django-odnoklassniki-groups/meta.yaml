package:
  name: django-odnoklassniki-groups
  version: "0.1.4"

source:
  fn: django-odnoklassniki-groups-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-odnoklassniki-groups/django-odnoklassniki-groups-0.1.4.tar.gz
  md5: 5e321070f0c0b6ba5a9dc5ecda1c402e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-odnoklassniki-groups = django-odnoklassniki-groups:main
    #
    # Would create an entry point called django-odnoklassniki-groups that calls django-odnoklassniki-groups.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-odnoklassniki-api >=0.1.0
    - django-m2m-history >=0.2.0

  run:
    - python
    - django-odnoklassniki-api >=0.1.0
    - django-m2m-history >=0.2.0

test:
  # Python imports
  imports:
    - odnoklassniki_groups
    - odnoklassniki_groups.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ramusus/django-odnoklassniki-groups
  license: BSD License
  summary: 'Django implementation for odnoklassniki API Groups'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
