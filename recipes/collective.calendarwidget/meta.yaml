package:
  name: collective.calendarwidget
  version: "0.2.5"

source:
  fn: collective.calendarwidget-0.2.5.zip
  url: https://pypi.python.org/packages/source/c/collective.calendarwidget/collective.calendarwidget-0.2.5.zip
  md5: 4ddd5a8a08e04c15c6fc32de5778ca97
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.calendarwidget = collective.calendarwidget:main
    #
    # Would create an entry point called collective.calendarwidget that calls collective.calendarwidget.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.js.jqueryui

  run:
    - python
    - setuptools
    - setuptools
    - collective.js.jqueryui

test:
  # Python imports
  imports:
    - collective
    - collective.calendarwidget
    - collective.calendarwidget.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/collective.calendarwidget
  license: ZPL
  summary: 'A new calendar widget for Archetypes (used for vs.event)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
