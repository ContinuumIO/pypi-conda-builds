package:
  name: collective.itvalidators
  version: "1.0.0"

source:
  fn: collective.itvalidators-1.0.0.zip
  url: https://pypi.python.org/packages/source/c/collective.itvalidators/collective.itvalidators-1.0.0.zip
  md5: 2322fe385408624ed7ef0515905c3b45
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.itvalidators = collective.itvalidators:main
    #
    # Would create an entry point called collective.itvalidators that calls collective.itvalidators.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.validation

  run:
    - python
    - setuptools
    - setuptools
    - products.validation

test:
  # Python imports
  imports:
    - collective
    - collective.itvalidators
    - collective.itvalidators.tests
    - collective.itvalidators.validators

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/collective.itvalidators
  license: GPL
  summary: 'A set of Archetypes validators for Plone, some for Italian specific needs, others useful for all'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
