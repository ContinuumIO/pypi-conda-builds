package:
  name: shadowd
  version: "1.2.0"

source:
  fn: shadowd-1.2.0.tar.gz
  url: https://pypi.python.org/packages/source/s/shadowd/shadowd-1.2.0.tar.gz
  md5: 3568fc554f35b66d9737de4188522eb1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - shadowd = shadowd:main
    #
    # Would create an entry point called shadowd that calls shadowd.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - shadowd

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/zecure/shadowd_python
  license: GNU General Public License v2 (GPLv2)
  summary: 'Python connector for the Shadow Daemon web application firewall'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
