package:
  name: globster
  version: "0.1.0"

source:
  fn: globster-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/g/globster/globster-0.1.0.tar.gz
  md5: c4695f698ff71573c674204163b08ef9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - globster = globster:main
    #
    # Would create an entry point called globster that calls globster.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - globster

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tsileo/globster
  license: GNU General Public License v2 (GPLv2)
  summary: 'Convert shell-like globs to regular expressions'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
