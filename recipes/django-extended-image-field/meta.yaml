package:
  name: django-extended-image-field
  version: "1.0.4"

source:
  fn: django-extended-image-field-1.0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-extended-image-field/django-extended-image-field-1.0.4.tar.gz
  md5: 1f811d2615a7286cd81b3dd9c3467d5c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-extended-image-field = django-extended-image-field:main
    #
    # Would create an entry point called django-extended-image-field that calls django-extended-image-field.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django >=1.3
    - pil

  run:
    - python
    - django >=1.3
    - pil

test:
  # Python imports
  imports:
    - eif

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/masterzeb/django-extended-image-field/
  license: BSD
  summary: 'Django Image Field with thumbnails creator'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
