package:
  name: linked_list_mod
  version: "1.12"

source:
  fn: linked_list_mod-1.12.tar.gz
  url: https://pypi.python.org/packages/source/l/linked_list_mod/linked_list_mod-1.12.tar.gz
  md5: a2ca4121b72c22d55c6d97d7f09aad52
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - linked_list_mod = linked_list_mod:main
    #
    # Would create an entry point called linked_list_mod that calls linked_list_mod.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - linked_list_mod

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://stromberg.dnsalias.org/~strombrg/linked-list
  license: Apache v2
  summary: 'Pure Python linked list module, with lifo (stack) and fifo (queue)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
