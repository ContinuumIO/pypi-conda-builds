package:
  name: django-octocat
  version: "0.2.1"

source:
  fn: django-octocat-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-octocat/django-octocat-0.2.1.tar.gz
  md5: f470c55c1dfc4ed5235fe6a937a97d92
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-octocat = django-octocat:main
    #
    # Would create an entry point called django-octocat that calls django-octocat.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - django-celery
    - requests >1.0.0

  run:
    - python
    - django
    - django-celery
    - requests >1.0.0

test:
  # Python imports
  imports:
    - github
    - github.migrations
    - github.tests
    - github.tests.account

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Celc/django-octocat
  license: The MIT License
  summary: 'A Django app for handling github users and repos.\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
