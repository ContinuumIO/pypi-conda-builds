package:
  name: django-urlkeyword
  version: "0.1.1"

source:
  fn: django-urlkeyword-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-urlkeyword/django-urlkeyword-0.1.1.tar.gz
  md5: 364932e6e193fecff731dcf8bf7dbf9a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-urlkeyword = django-urlkeyword:main
    #
    # Would create an entry point called django-urlkeyword that calls django-urlkeyword.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - urlkeyword

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jleeothon/urlkeyword
  license: MIT License
  summary: 'Validate that your slugs do not match a keyword in your url '

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
