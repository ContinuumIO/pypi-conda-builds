package:
  name: gomobiletheme.basic
  version: "1.0.9"

source:
  fn: gomobiletheme.basic-1.0.9.tar.gz
  url: https://pypi.python.org/packages/source/g/gomobiletheme.basic/gomobiletheme.basic-1.0.9.tar.gz
  md5: 9070f3327245bcf27efabda0f10e599e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gomobiletheme.basic = gomobiletheme.basic:main
    #
    # Would create an entry point called gomobiletheme.basic that calls gomobiletheme.basic.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - five.grok
    - collective.fastview
    - gomobile.mobile

  run:
    - python
    - setuptools
    - setuptools
    - five.grok
    - collective.fastview
    - gomobile.mobile

test:
  # Python imports
  imports:
    - gomobiletheme
    - gomobiletheme.basic

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://webandmobile.mfabrik.com
  license: GPL
  summary: 'Default mobile theme for mFabrik Web and Mobile sites'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
