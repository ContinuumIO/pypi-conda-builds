package:
  name: coopr.sucasa
  version: "3.0"

source:
  fn: coopr.sucasa-3.0.tar.gz
  url: https://pypi.python.org/packages/source/c/coopr.sucasa/coopr.sucasa-3.0.tar.gz
  md5: ee7dd5195d6e3943ca7076e2e1c18666
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - coopr.sucasa = coopr.sucasa:main
    #
    # Would create an entry point called coopr.sucasa that calls coopr.sucasa.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - coopr
    - coopr.sucasa
    - coopr.sucasa.tests
    - coopr.sucasa.tests.models
    - coopr.sucasa.tests.unit

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://software.sandia.gov/svn/public/coopr/coopr.sucasa
  license: BSD License
  summary: 'Coopr framework for integrating symbolic data into MIP solvers'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
