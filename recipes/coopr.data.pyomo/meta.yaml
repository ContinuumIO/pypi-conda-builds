package:
  name: coopr.data.pyomo
  version: "1.2.1"

source:
  fn: coopr.data.pyomo-1.2.1.tar.gz
  url: https://pypi.python.org/packages/source/c/coopr.data.pyomo/coopr.data.pyomo-1.2.1.tar.gz
  md5: c602aba130454c94c9ea6174dd12ceca
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - coopr.data.pyomo = coopr.data.pyomo:main
    #
    # Would create an entry point called coopr.data.pyomo that calls coopr.data.pyomo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - coopr
    - coopr.data
    - coopr.data.pyomo
    - coopr.data.pyomo.plugins

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://software.sandia.gov/svn/public/coopr/coopr.data.pyomo
  license: BSD License
  summary: 'Coopr data project:  Pyomo test models'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
