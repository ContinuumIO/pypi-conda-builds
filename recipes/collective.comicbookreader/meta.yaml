package:
  name: collective.comicbookreader
  version: "1.0"

source:
  fn: collective.comicbookreader-1.0.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.comicbookreader/collective.comicbookreader-1.0.tar.gz
  md5: d068faebfc6b93c17048831e2bd7f62e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.comicbookreader = collective.comicbookreader:main
    #
    # Would create an entry point called collective.comicbookreader that calls collective.comicbookreader.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - rarfile

  run:
    - python
    - setuptools
    - setuptools
    - rarfile

test:
  # Python imports
  imports:
    - collective
    - collective.comicbookreader
    - collective.comicbookreader.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/giacomos/collective.comicbookreader
  license: GPL
  summary: 'An inline comic book reader for plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
