package:
  name: impermagit
  version: "1.0.0"

source:
  fn: impermagit-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/i/impermagit/impermagit-1.0.0.tar.gz
  md5: aba8280b9e03bf7ac51a50f5bad2f0b8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - impermagit = impermagit:main
    #
    # Would create an entry point called impermagit that calls impermagit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - impermagit
    - impermagit.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/impermagit/
  license: LICENSE.txt
  summary: 'Create temporary git repos in python.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
