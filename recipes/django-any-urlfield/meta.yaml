package:
  name: django-any-urlfield
  version: "2.1.1"

source:
  fn: django-any-urlfield-2.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-any-urlfield/django-any-urlfield-2.1.1.tar.gz
  md5: cbea86446f92c6ab6b5f9d3fa934233e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-any-urlfield = django-any-urlfield:main
    #
    # Would create an entry point called django-any-urlfield that calls django-any-urlfield.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - any_urlfield
    - any_urlfield.forms
    - any_urlfield.models

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/edoburu/django-any-urlfield
  license: Apache Software License
  summary: 'An improved URL selector to choose between internal models and external URLs'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
