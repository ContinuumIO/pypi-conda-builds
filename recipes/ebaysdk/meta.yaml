package:
  name: ebaysdk
  version: "2.1.1"

source:
  fn: ebaysdk-2.1.1.tar.gz
  url: https://pypi.python.org/packages/source/e/ebaysdk/ebaysdk-2.1.1.tar.gz
  md5: a9de90f583d8d70a5575e7fd80dd0adb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ebaysdk = ebaysdk:main
    #
    # Would create an entry point called ebaysdk that calls ebaysdk.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - lxml
    - requests

  run:
    - python
    - lxml
    - requests

test:
  # Python imports
  imports:
    - ebaysdk
    - ebaysdk.finding
    - ebaysdk.http
    - ebaysdk.merchandising
    - ebaysdk.poller
    - ebaysdk.shopping
    - ebaysdk.soa
    - ebaysdk.trading
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/timotheus/ebaysdk-python
  license: COMMON DEVELOPMENT AND DISTRIBUTION LICENSE (CDDL) Version 1.0
  summary: 'eBay SDK for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
