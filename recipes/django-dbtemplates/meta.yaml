package:
  name: django-dbtemplates
  version: "1.3"

source:
  fn: django-dbtemplates-1.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-dbtemplates/django-dbtemplates-1.3.tar.gz
  md5: 381cf4a248220fd734a1daf40710d615
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-dbtemplates = django-dbtemplates:main
    #
    # Would create an entry point called django-dbtemplates that calls django-dbtemplates.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-appconf >=0.4

  run:
    - python
    - django-appconf >=0.4

test:
  # Python imports
  imports:
    - dbtemplates
    - dbtemplates.management
    - dbtemplates.management.commands
    - dbtemplates.migrations
    - dbtemplates.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://django-dbtemplates.readthedocs.org/
  license: BSD License
  summary: 'Template loader for templates stored in the database'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
