package:
  name: shake
  version: "1.6.4"

source:
  fn: Shake-1.6.4.tar.gz
  url: https://pypi.python.org/packages/source/S/Shake/Shake-1.6.4.tar.gz
  md5: 9f9f1ff6f1dcfb57d3cfd3704edaacdb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - shake = shake:main
    #
    # Would create an entry point called shake that calls shake.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - allspeak
    - inflection
    - itsdangerous >=0.17
    - jinja2 >=2.4
    - pyceo >=1.0.3
    - voodoo >=0.7
    - werkzeug >=0.7

  run:
    - python
    - allspeak
    - inflection
    - itsdangerous >=0.17
    - jinja2 >=2.4
    - pyceo >=1.0.3
    - voodoo >=0.7
    - werkzeug >=0.7

test:
  # Python imports
  imports:
    - shake
    - shake.cli
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/lucuma/shake
  license: MIT License
  summary: 'A lightweight web framework based on Werkzeug and Jinja2 as an alternative to Flask'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
