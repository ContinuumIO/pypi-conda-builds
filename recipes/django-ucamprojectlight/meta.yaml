package:
  name: django-ucamprojectlight
  version: "1.1"

source:
  fn: django-ucamprojectlight-1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-ucamprojectlight/django-ucamprojectlight-1.1.tar.gz
  md5: 1a8486ac4fade7830987fbdc2d471e91
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-ucamprojectlight = django-ucamprojectlight:main
    #
    # Would create an entry point called django-ucamprojectlight that calls django-ucamprojectlight.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - ucamprojectlight

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://git.csx.cam.ac.uk/x/ucs/raven/django-ucamprojectlight.git
  license: MIT License
  summary: 'A Django module to use the template system of the University of Cambridge: Project light'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
