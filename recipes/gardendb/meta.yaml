package:
  name: gardendb
  version: "0.0.12"

source:
  fn: gardendb-0.0.12.tar.gz
  url: https://pypi.python.org/packages/source/g/gardendb/gardendb-0.0.12.tar.gz
  md5: 855062e6a463e71c6cc0e5260aee9abd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gardendb = gardendb:main
    #
    # Would create an entry point called gardendb that calls gardendb.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - gardendb

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/nejstastnejsistene/gardendb
  license: BSD License
  summary: 'Simple python flat-file databases with Cucumbers: simple, version-controlled records with streamlined pickle representations.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
