package:
  name: flexidate
  version: "1.0"

source:
  fn: flexidate-1.0.tar.gz
  url: https://pypi.python.org/packages/source/f/flexidate/flexidate-1.0.tar.gz
  md5: 9989289ddb904d76ed6cf64912b7a084
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flexidate = flexidate:main
    #
    # Would create an entry point called flexidate that calls flexidate.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python-dateutil >=1.0,<1.99

  run:
    - python
    - python-dateutil >=1.0,<1.99

test:
  # Python imports
  imports:
    - flexidate

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/okfn/flexidate/
  license: MIT
  summary: 'Very flexible date parsing and normalization utilities'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
