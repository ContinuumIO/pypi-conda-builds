package:
  name: django-generate-fixtures
  version: "0.1"

source:
  fn: django-generate-fixtures-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-generate-fixtures/django-generate-fixtures-0.1.tar.gz
  md5: 9ca823dba1d3147603449d508b9e4aaa
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-generate-fixtures = django-generate-fixtures:main
    #
    # Would create an entry point called django-generate-fixtures that calls django-generate-fixtures.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_generate_fixtures
    - django_generate_fixtures.management
    - django_generate_fixtures.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/aRkadeFR/django-generate-fixtures
  license: UNKNOWN
  summary: ' Easy way to generate fixtures (in json)\n    for django following the links of a given object '

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
