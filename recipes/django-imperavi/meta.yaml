package:
  name: django-imperavi
  version: "0.2.3"

source:
  fn: django-imperavi-0.2.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-imperavi/django-imperavi-0.2.3.tar.gz
  md5: 5b9dcb47a4667329844ec009b0c75bc7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-imperavi = django-imperavi:main
    #
    # Would create an entry point called django-imperavi that calls django-imperavi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sorl-thumbnail >=11.12

  run:
    - python
    - sorl-thumbnail >=11.12

test:
  # Python imports
  imports:
    - imperavi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/vasyabigi/django-imperavi
  license: MIT License
  summary: 'A django application that contains a class for admin interface to render a         text field as beautiful Imperavi WYSIWYG editor http://redactorjs.com/'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
