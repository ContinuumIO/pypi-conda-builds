package:
  name: msgpack-numpy
  version: "0.3.5"

source:
  fn: msgpack-numpy-0.3.5.tar.gz
  url: https://pypi.python.org/packages/source/m/msgpack-numpy/msgpack-numpy-0.3.5.tar.gz
  md5: e9f2ce2ea9526e4b147b1c52d62af2f7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - msgpack-numpy = msgpack-numpy:main
    #
    # Would create an entry point called msgpack-numpy that calls msgpack-numpy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy
    - msgpack-python >=0.3.0

  run:
    - python
    - numpy
    - msgpack-python >=0.3.0

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lebedov/msgpack-numpy
  license: BSD License
  summary: 'Numpy data serialization using msgpack'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
