package:
  name: python-libdiscid
  version: "0.4.1"

source:
  fn: python-libdiscid-0.4.1.tar.gz
  url: https://pypi.python.org/packages/source/p/python-libdiscid/python-libdiscid-0.4.1.tar.gz
  md5: 5d292eb0a78209543d8d4bba4953d99e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-libdiscid = python-libdiscid:main
    #
    # Would create an entry point called python-libdiscid that calls python-libdiscid.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - libdiscid
    - libdiscid.compat
    - libdiscid.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/sebastinas/python-libdiscid
  license: MIT License
  summary: 'Python bindings for libdiscid'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
