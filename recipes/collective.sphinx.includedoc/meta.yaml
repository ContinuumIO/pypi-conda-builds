package:
  name: collective.sphinx.includedoc
  version: "0.2"

source:
  fn: collective.sphinx.includedoc-0.2.zip
  url: https://pypi.python.org/packages/source/c/collective.sphinx.includedoc/collective.sphinx.includedoc-0.2.zip
  md5: 57c1a2ce18a04d95d5da8ac687bf7313
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.sphinx.includedoc = collective.sphinx.includedoc:main
    #
    # Would create an entry point called collective.sphinx.includedoc that calls collective.sphinx.includedoc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - docutils
    - sphinx
    - zc.recipe.egg

  run:
    - python
    - docutils
    - sphinx
    - zc.recipe.egg

test:
  # Python imports
  imports:
    - collective
    - collective.sphinx
    - collective.sphinx.includedoc

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/collective.sphinx.includedoc
  license: GNU General Public License (GPL)
  summary: 'Sphinx extension for including doctests'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
