package:
  name: flask-decorators
  version: "0.0.1"

source:
  fn: Flask-Decorators-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Decorators/Flask-Decorators-0.0.1.tar.gz
  md5: 3c95b5e0c854948ec285bafe31a1fc98
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-decorators = flask-decorators:main
    #
    # Would create an entry point called flask-decorators that calls flask-decorators.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.10

  run:
    - python
    - flask >=0.10

test:
  # Python imports
  imports:
    - flask_decorators

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kxxoling/flask-decorators
  license: MIT License
  summary: 'A list of Flask decorator utilitiesnot include in the origin flask project.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
