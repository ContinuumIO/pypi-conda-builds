package:
  name: liveconnect
  version: "0.1.113"

source:
  fn: liveconnect-0.1.113.tar.gz
  url: https://pypi.python.org/packages/source/l/liveconnect/liveconnect-0.1.113.tar.gz
  md5: 43894f608c1d960dc9b524993bac2d7a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - liveconnect = liveconnect:main
    #
    # Would create an entry point called liveconnect that calls liveconnect.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - liveconnect

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/mansam/liveconnect
  license: MIT
  summary: 'LiveConnect OAuth adapter for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
