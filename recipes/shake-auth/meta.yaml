package:
  name: shake-auth
  version: "1.3.7"

source:
  fn: Shake-Auth-1.3.7.tar.gz
  url: https://pypi.python.org/packages/source/S/Shake-Auth/Shake-Auth-1.3.7.tar.gz
  md5: bb995e670528750fddb35c69cead21e2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - shake-auth = shake-auth:main
    #
    # Would create an entry point called shake-auth that calls shake-auth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - jinja2
    - solution

  run:
    - python
    - jinja2
    - solution

test:
  # Python imports
  imports:
    - shake_auth

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/lucuma/Shake-Auth
  license: MIT License
  summary: "Shake's awesome authentication extension"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
