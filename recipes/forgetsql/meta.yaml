package:
  name: forgetsql
  version: "0.5.1"

source:
  fn: forgetSQL-0.5.1.tar.gz
  url: http://prdownloads.sourceforge.net/forgetsql/forgetSQL-0.5.1.tar.gz?download
  #md5: 
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - forgetsql = forgetsql:main
    #
    # Would create an entry point called forgetsql that calls forgetsql.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - forgetsql

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://forgetsql.sourceforge.net/
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'forgetSQL is a Python module for accessing SQL databases by creating\nclasses that maps SQL tables to objects, normally one class pr. SQL\ntable. The idea is to forget everything about SQL and just worrying\nabout normal classes and objects.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
