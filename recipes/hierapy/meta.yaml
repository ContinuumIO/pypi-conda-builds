package:
  name: hierapy
  version: "0.0.3"

source:
  fn: HieraPy-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/H/HieraPy/HieraPy-0.0.3.tar.gz
  md5: dce82799cd3b95d6aa60da8fa3f29463
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hierapy = hierapy:main
    #
    # Would create an entry point called hierapy that calls hierapy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyyaml

  run:
    - python
    - pyyaml

test:
  # Python imports
  imports:
    - hierapy
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ivannpaz/HieraPy
  license: UNKNOWN
  summary: "Shy implementation of Puppet's Hiera configuration tool."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
