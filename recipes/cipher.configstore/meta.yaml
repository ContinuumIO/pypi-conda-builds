package:
  name: cipher.configstore
  version: "2.0.0a2"

source:
  fn: cipher.configstore-2.0.0a2.zip
  url: https://pypi.python.org/packages/source/c/cipher.configstore/cipher.configstore-2.0.0a2.zip
  md5: b7223fd1a0fb0555110367edaf8cee61
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cipher.configstore = cipher.configstore:main
    #
    # Would create an entry point called cipher.configstore that calls cipher.configstore.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - python-dateutil
    - setuptools
    - zope.component
    - zope.event
    - zope.interface
    - zope.lifecycleevent
    - zope.schema
    - zope.security
    - odict

  run:
    - python
    - python-dateutil
    - setuptools
    - zope.component
    - zope.event
    - zope.interface
    - zope.lifecycleevent
    - zope.schema
    - zope.security
    - odict

test:
  # Python imports
  imports:
    - cipher
    - cipher.configstore

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/cipher.configstore/
  license: ZPL 2.1
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
