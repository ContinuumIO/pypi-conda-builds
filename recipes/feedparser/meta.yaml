package:
  name: feedparser
  version: "5.2.0"

source:
  fn: feedparser-5.2.0.post1.tar.bz2
  url: https://pypi.python.org/packages/source/f/feedparser/feedparser-5.2.0.post1.tar.bz2
  md5: 9f54fe1e84412c16cdc5997d5b133a60
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - feedparser = feedparser:main
    #
    # Would create an entry point called feedparser that calls feedparser.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - feedparser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kurtmckee/feedparser
  license: 
  summary: 'Universal feed parser, handles RSS 0.9x, RSS 1.0, RSS 2.0, CDF, Atom 0.3, and Atom 1.0 feeds'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
