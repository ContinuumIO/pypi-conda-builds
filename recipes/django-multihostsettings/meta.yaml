package:
  name: django-multihostsettings
  version: "0.1"

source:
  fn: django-multihostsettings-0.1.zip
  url: https://pypi.python.org/packages/source/d/django-multihostsettings/django-multihostsettings-0.1.zip
  md5: 5450a949198c007c7feb328d95a3a425
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-multihostsettings = django-multihostsettings:main
    #
    # Would create an entry point called django-multihostsettings that calls django-multihostsettings.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - multihostsettings

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT License
  summary: 'Provides conditional settings according to the host name.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
