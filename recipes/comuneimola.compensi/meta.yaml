package:
  name: comuneimola.compensi
  version: "1.3"

source:
  fn: comuneimola.compensi-1.3.zip
  url: https://pypi.python.org/packages/source/c/comuneimola.compensi/comuneimola.compensi-1.3.zip
  md5: e1eda8918e166e9966ee0a3251d41dc5
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - comuneimola.compensi = comuneimola.compensi:main
    #
    # Would create an entry point called comuneimola.compensi that calls comuneimola.compensi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.js.datatables

  run:
    - python
    - setuptools
    - setuptools
    - collective.js.datatables

test:
  # Python imports
  imports:
    - comuneimola
    - comuneimola.compensi
    - comuneimola.compensi.browser
    - comuneimola.compensi.browser.csv_export
    - comuneimola.compensi.content
    - comuneimola.compensi.interfaces
    - comuneimola.compensi.viewlets

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/comuneimola.compensi
  license: GNU General Public License (GPL)
  summary: 'Fees management for the town of Imola - Under Italian Law, the institutional websites of the cities shall contain a detailed list of the amounts of all the contracts (herein we will use the term "fees") drawn up with external professionals. This product implements Plone with two new custom archetypes, "Fees Area" and "Fee".'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
