package:
  name: collective.formwidget.mathjax
  version: "1.0"

source:
  fn: collective.formwidget.mathjax-1.0.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.formwidget.mathjax/collective.formwidget.mathjax-1.0.tar.gz
  md5: 16fc3e41365b30280cc78613923056f2
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.formwidget.mathjax = collective.formwidget.mathjax:main
    #
    # Would create an entry point called collective.formwidget.mathjax that calls collective.formwidget.mathjax.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.formwidget
    - collective.formwidget.mathjax
    - collective.formwidget.mathjax.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/
  license: gpl
  summary: 'Mathjax widget for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
