package:
  name: barak
  version: "0.3.2"

source:
  fn: Barak-0.3.2.tar.gz
  url: https://pypi.python.org/packages/source/B/Barak/Barak-0.3.2.tar.gz
  md5: 382febe4e9929371e73f632bc83f2562
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - barak = barak:main
    #
    # Would create an entry point called barak that calls barak.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - numpy
    - pyfits
    - atpy
    - astropy

  run:
    - python
    - numpy
    - pyfits
    - atpy
    - astropy

test:
  # Python imports
  imports:
    - barak
    - barak.sphinx
    - barak.sphinx.ext
    - barak.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://nhmc.github.com/Barak/
  license: modified BSD
  summary: 'A set of astronomy-related routines for generating Voigt profiles from atomic data, reading and writing data, working with SEDs, passbands and dust extinction laws.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
