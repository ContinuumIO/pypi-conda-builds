package:
  name: balog
  version: "1.2.3"

source:
  fn: balog-1.2.3.tar.gz
  url: https://pypi.python.org/packages/source/b/balog/balog-1.2.3.tar.gz
  md5: 248700674ed391d3d3df33f5a1e7808e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - balog = balog:main
    #
    # Would create an entry point called balog that calls balog.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - jsonschema
    - structlog
    - pytz
    - iso8601
    - coid
    - venusian
    - ba-colander ==1.0b1

  run:
    - python
    - jsonschema
    - structlog
    - pytz
    - iso8601
    - coid
    - venusian
    - ba-colander ==1.0b1

test:
  # Python imports
  imports:
    - balog
    - balog.consumers
    - balog.engines
    - balog.records
    - balog.records.application
    - balog.records.facility
    - tests
    - tests.fixtures
    - tests.unit
    - tests.unit.engines

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/balanced/balog/
  license: UNKNOWN
  summary: 'Balanced event logging schema and library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
