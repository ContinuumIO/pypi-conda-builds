package:
  name: edrnsite.funding
  version: "1.0.5"

source:
  fn: edrnsite.funding-1.0.5.zip
  url: https://pypi.python.org/packages/source/e/edrnsite.funding/edrnsite.funding-1.0.5.zip
  md5: c264b9a7fef285461ac423730ba77cfb
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - edrnsite.funding = edrnsite.funding:main
    #
    # Would create an entry point called edrnsite.funding that calls edrnsite.funding.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - pytz
    - products.cmfplone
    - z3c.autoinclude

  run:
    - python
    - setuptools
    - setuptools
    - pytz
    - products.cmfplone
    - z3c.autoinclude

test:
  # Python imports
  imports:
    - edrnsite
    - edrnsite.funding
    - edrnsite.funding.browser
    - edrnsite.funding.content
    - edrnsite.funding.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/EDRN/edrnsite.funding
  license: Apache Software License
  summary: 'EDRN Funding Opportunities'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
