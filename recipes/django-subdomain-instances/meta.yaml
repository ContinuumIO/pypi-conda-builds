package:
  name: django-subdomain-instances
  version: "0.10.2"

source:
  fn: django-subdomain-instances-0.10.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-subdomain-instances/django-subdomain-instances-0.10.2.tar.gz
  md5: 9bc3f0792adae08538b41b4d7e7d0f80
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-subdomain-instances = django-subdomain-instances:main
    #
    # Would create an entry point called django-subdomain-instances that calls django-subdomain-instances.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4,<1.8

  run:
    - python
    - django >=1.4,<1.8

test:
  # Python imports
  imports:
    - instances
    - instances.migrations
    - instances.south_migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mysociety/django-subdomain-instances
  license: UNKNOWN
  summary: 'A way of allowing subdomains to be served by the same project, and associating objects with particular subdomains.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
