package:
  name: django-pki
  version: "0.20.0"

source:
  fn: django-pki-0.20.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-pki/django-pki-0.20.0.tar.gz
  md5: 18d606d2e4bd82b14dd843b792d964fb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-pki = django-pki:main
    #
    # Would create an entry point called django-pki that calls django-pki.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - pki
    - pki.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://dkerwin.github.com/django-pki/
  license: GNU General Public License (GPL)
  summary: 'A PKI based on the Django admin'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
