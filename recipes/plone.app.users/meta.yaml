package:
  name: plone.app.users
  version: "2.2.1"

source:
  fn: plone.app.users-2.2.1.tar.gz
  url: https://pypi.python.org/packages/source/p/plone.app.users/plone.app.users-2.2.1.tar.gz
  md5: 58cc3dc2475b076dd1e77929f6bfb08f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.app.users = plone.app.users:main
    #
    # Would create an entry point called plone.app.users that calls plone.app.users.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - accesscontrol
    - acquisition
    - products.cmfcore
    - products.cmfplone
    - products.plonepas
    - products.statusmessages
    - zodb3
    - zope2 >=2.12.3
    - plone.app.controlpanel >=2.1b1
    - plone.app.layout
    - plone.autoform >=1.2
    - plone.formwidget.namedfile >=1.0.3
    - plone.namedfile
    - plone.protect
    - plone.uuid
    - setuptools
    - z3c.form
    - zope.component
    - zope.event
    - zope.interface
    - zope.schema
    - zope.site

  run:
    - python
    - setuptools
    - accesscontrol
    - acquisition
    - products.cmfcore
    - products.cmfplone
    - products.plonepas
    - products.statusmessages
    - zodb3
    - zope2 >=2.12.3
    - plone.app.controlpanel >=2.1b1
    - plone.app.layout
    - plone.autoform >=1.2
    - plone.formwidget.namedfile >=1.0.3
    - plone.namedfile
    - plone.protect
    - plone.uuid
    - setuptools
    - z3c.form
    - zope.component
    - zope.event
    - zope.interface
    - zope.schema
    - zope.site

test:
  # Python imports
  imports:
    - plone
    - plone.app
    - plone.app.users
    - plone.app.users.browser
    - plone.app.users.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/plone.app.users
  license: GNU General Public License (GPL)
  summary: 'A package for all things users and groups related (specific to plone)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
