package:
  name: dynamic_password
  version: "0.0.3"

source:
  fn: dynamic_password-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/dynamic_password/dynamic_password-0.0.3.tar.gz
  md5: c42dabfddce6aa10b424024fd91f2d71
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dynamic_password = dynamic_password:main
    #
    # Would create an entry point called dynamic_password that calls dynamic_password.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3

  run:
    - python
    - django >=1.3

test:
  # Python imports
  imports:
    - dynamic_password

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/DNX/django-dynamic-password/
  license: BSD License
  summary: 'Add dynamic password authentication to your web service'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
