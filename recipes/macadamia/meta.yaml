package:
  name: macadamia
  version: "0.0.5"

source:
  fn: macadamia-0.0.5.tar.gz
  url: https://pypi.python.org/packages/source/m/macadamia/macadamia-0.0.5.tar.gz
  md5: fe120be6448b257c1b727942ec344680
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - macadamia = macadamia:main
    #
    # Would create an entry point called macadamia that calls macadamia.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - macadamia

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/realgeeks/macadamia
  license: MIT License
  summary: 'A parser for Google Analytics Cookies'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
