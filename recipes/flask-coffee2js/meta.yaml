package:
  name: flask-coffee2js
  version: "0.1.2"

source:
  fn: flask-coffee2js-0.1.2.zip
  url: https://pypi.python.org/packages/source/f/flask-coffee2js/flask-coffee2js-0.1.2.zip
  md5: 9321fbb1dc7ec551b9d1e42a1f15a26b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-coffee2js = flask-coffee2js:main
    #
    # Would create an entry point called flask-coffee2js that calls flask-coffee2js.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - coffeescript

  run:
    - python
    - flask
    - coffeescript

test:
  # Python imports
  imports:
    - flaskext

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/weapp/flask-coffee2js
  license: MIT License
  summary: 'A small Flask extension that adds CoffeScript support to Flask.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
