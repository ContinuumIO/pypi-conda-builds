package:
  name: bhyve
  version: "0.2.1"

source:
  fn: bhyve-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/b/bhyve/bhyve-0.2.1.tar.gz
  md5: c09b84fe5d545d862ef749a5f21c70da
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bhyve = bhyve:main
    #
    # Would create an entry point called bhyve that calls bhyve.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyyaml
    - cmdtool ==0.2

  run:
    - python
    - pyyaml
    - cmdtool ==0.2

test:
  # Python imports
  imports:
    - bhyve

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ConnorDillon/bhyve
  license: GPLv3
  summary: "A tool for managing bhyve VM's, because libvirt doesn't do want I want it to"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
