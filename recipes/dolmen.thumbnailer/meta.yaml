package:
  name: dolmen.thumbnailer
  version: "0.3"

source:
  fn: dolmen.thumbnailer-0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/dolmen.thumbnailer/dolmen.thumbnailer-0.3.tar.gz
  md5: a139ca6fcd58dbe585088b1713dce4cb
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dolmen.thumbnailer = dolmen.thumbnailer:main
    #
    # Would create an entry point called dolmen.thumbnailer that calls dolmen.thumbnailer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - pil >=1.1.7
    - dolmen.field >=0.3
    - dolmen.file
    - dolmen.storage
    - grokcore.component
    - setuptools
    - zope.component
    - zope.interface
    - zope.publisher
    - zope.schema
    - zope.security

  run:
    - python
    - pil >=1.1.7
    - dolmen.field >=0.3
    - dolmen.file
    - dolmen.storage
    - grokcore.component
    - setuptools
    - zope.component
    - zope.interface
    - zope.publisher
    - zope.schema
    - zope.security

test:
  # Python imports
  imports:
    - dolmen
    - dolmen.thumbnailer
    - dolmen.thumbnailer.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://gitweb.dolmen-project.org
  license: GNU General Public License (GPL)
  summary: 'Dolmen thumbnailing library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
