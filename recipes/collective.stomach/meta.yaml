package:
  name: collective.stomach
  version: "0.2"

source:
  fn: collective.stomach-0.2.zip
  url: https://pypi.python.org/packages/source/c/collective.stomach/collective.stomach-0.2.zip
  md5: 08109a28cb822780d596a05293fd7afd
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.stomach = collective.stomach:main
    #
    # Would create an entry point called collective.stomach that calls collective.stomach.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - yolk

  run:
    - python
    - setuptools
    - setuptools
    - yolk

test:
  # Python imports
  imports:
    - collective
    - collective.stomach
    - collective.stomach.browser
    - collective.stomach.interfaces
    - collective.stomach.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.stomach
  license: gpl
  summary: 'This products is used to see which eggs are in Plone site.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
