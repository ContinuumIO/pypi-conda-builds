package:
  name: django-finial
  version: "0.3.4"

source:
  fn: django-finial-0.3.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-finial/django-finial-0.3.4.tar.gz
  md5: 217d4c001dd3b320e1d1ce0cb1ff76a5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-finial = django-finial:main
    #
    # Would create an entry point called django-finial that calls django-finial.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - south
    - simplejson
    - mimic

  run:
    - python
    - django
    - south
    - simplejson
    - mimic

test:
  # Python imports
  imports:
    - finial
    - finial.migrations
    - finial.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/urbanairship/django-finial
  license: BSD License
  summary: 'Template, Static, and URL Overrides per User.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
