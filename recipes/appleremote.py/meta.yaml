package:
  name: appleremote.py
  version: "0.1"

source:
  fn: appleremote.py-0.1.tar.gz
  url: https://pypi.python.org/packages/source/a/appleremote.py/appleremote.py-0.1.tar.gz
  md5: f6b041c49b8ab0f433b30d2c95fffa0d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - appleremote.py = appleremote.py:main
    #
    # Would create an entry point called appleremote.py that calls appleremote.py.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/atv-bootloader/
  license: UNKNOWN
  summary: 'A simple script to interface LIRC using the macmini driver with MythTV running the remote control interface.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
