package:
  name: pyphaxio
  version: "0.9"

source:
  fn: pyphaxio-0.9.tar.gz
  url: https://pypi.python.org/packages/source/p/pyphaxio/pyphaxio-0.9.tar.gz
  md5: 0ee9e638b349b73b61cbe905e3c9d725
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pyphaxio = pyphaxio:main
    #
    # Would create an entry point called pyphaxio that calls pyphaxio.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - phaxio
    - phaxio.tests
    - pyphaxio

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/setaris/pyphaxio
  license: MIT License
  summary: 'Python client for Phaxio'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
