package:
  name: ella-category-subdomain
  version: "0.6.0"

source:
  fn: Ella-Category-Subdomain-0.6.0.tar.gz
  url: https://pypi.python.org/packages/source/E/Ella-Category-Subdomain/Ella-Category-Subdomain-0.6.0.tar.gz
  md5: 75005f0a620c717e3c6c763cc5cd1214
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ella-category-subdomain = ella-category-subdomain:main
    #
    # Would create an entry point called ella-category-subdomain that calls ella-category-subdomain.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools >=0.6b1
    - ella >=2.0

  run:
    - python
    - setuptools >=0.6b1
    - ella >=2.0

test:
  # Python imports
  imports:
    - ella_category_subdomain

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/sanomacz/ella-category-subdomain/
  license: BSD License
  summary: 'Use Ella categories as subdomains.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
