package:
  name: fossrit_tahrir_theme
  version: "0.1.6"

source:
  fn: fossrit_tahrir_theme-0.1.6.tar.gz
  url: https://pypi.python.org/packages/source/f/fossrit_tahrir_theme/fossrit_tahrir_theme-0.1.6.tar.gz
  md5: 1a9f41099573489baec1e1edde8d1aa7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fossrit_tahrir_theme = fossrit_tahrir_theme:main
    #
    # Would create an entry point called fossrit_tahrir_theme that calls fossrit_tahrir_theme.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - fossrit_tahrir_theme

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/FOSSRIT/fossrit-tahrir-theme
  license: GPLv3+
  summary: 'FOSSRIT theme for the Tahrir Open Badges webapp'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
