package:
  name: collective.addtofolder
  version: "0.2"

source:
  fn: collective.addtofolder-0.2.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.addtofolder/collective.addtofolder-0.2.tar.gz
  md5: c2368ef6d6db809d0e673e6aaf274da8
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.addtofolder = collective.addtofolder:main
    #
    # Would create an entry point called collective.addtofolder that calls collective.addtofolder.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.addtofolder
    - collective.addtofolder.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://dev.plone.org/collective/browser/collective.addtofolder
  license: GPL
  summary: "Add 'Add to parent folder' option. Allows users to add content to the folder you are currently in."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
