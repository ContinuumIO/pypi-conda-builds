package:
  name: tonnikala
  version: "0.16"

source:
  fn: tonnikala-0.16.tar.gz
  url: https://pypi.python.org/packages/source/t/tonnikala/tonnikala-0.16.tar.gz
  md5: 116e86bf72273e2f3fe79460f6a1bb12
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tonnikala = tonnikala:main
    #
    # Would create an entry point called tonnikala that calls tonnikala.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six >=1.4.1
    - markupsafe >=0.18

  run:
    - python
    - six >=1.4.1
    - markupsafe >=0.18

test:
  # Python imports
  imports:
    - tonnikala
    - tonnikala.astutil
    - tonnikala.helpers
    - tonnikala.ir
    - tonnikala.languages
    - tonnikala.languages.javascript
    - tonnikala.languages.python
    - tonnikala.pyramid
    - tonnikala.runtime
    - tonnikala.syntaxes
    - tonnikala.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ztane/Tonnikala
  license: Apache Software License
  summary: 'Python templating engine - the one ton solution'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
