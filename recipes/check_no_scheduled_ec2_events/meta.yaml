package:
  name: check_no_scheduled_ec2_events
  version: "0.1.1"

source:
  fn: check_no_scheduled_ec2_events-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/c/check_no_scheduled_ec2_events/check_no_scheduled_ec2_events-0.1.1.tar.gz
  md5: 3b99e249bbe8fc493682132c97c5af43
#  patches:
   # List any patch files here
   # - fix.patch

build:
  # preserve_egg_dir: True
  entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - check_no_scheduled_ec2_events = check_no_scheduled_ec2_events:main
    #
    # Would create an entry point called check_no_scheduled_ec2_events that calls check_no_scheduled_ec2_events.main()

    - check_no_scheduled_ec2_events = check_no_scheduled_ec2_events:check

  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - boto >=2.9.6
    - docopt

  run:
    - python
    - boto >=2.9.6
    - docopt

test:
  # Python imports
  imports:
    - check_no_scheduled_ec2_events

  commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.

    - check_no_scheduled_ec2_events --help

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/martinmelin/check_no_scheduled_ec2_events
  license: Creative Commons Attribution-Noncommercial-Share Alike license
  summary: 'Nagios-style check if the current EC2 instance has any scheduled events'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
