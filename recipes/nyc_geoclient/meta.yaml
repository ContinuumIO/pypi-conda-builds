package:
  name: nyc_geoclient
  version: "0.0.2"

source:
  fn: nyc_geoclient-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/n/nyc_geoclient/nyc_geoclient-0.0.2.tar.gz
  md5: 96d7b8d9945afa65d28cec8919dd9d67
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - nyc_geoclient = nyc_geoclient:main
    #
    # Would create an entry point called nyc_geoclient that calls nyc_geoclient.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=2.2.1

  run:
    - python
    - requests >=2.2.1

test:
  # Python imports
  imports:
    - nyc_geoclient

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/talos/nyc-geoclient
  license: BSD License
  summary: 'Python bindings for the NYC Geoclient REST API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
