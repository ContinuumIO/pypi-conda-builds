package:
  name: uadm
  version: "0.7.5"

source:
  fn: uadm-0.7.5.tar.gz
  url: https://pypi.python.org/packages/source/u/uadm/uadm-0.7.5.tar.gz
  md5: 995f2454c05e23090345c22485156269
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - uadm = uadm:main
    #
    # Would create an entry point called uadm that calls uadm.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - uadm

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/pylanglois/uadm
  license: BSD License
  summary: 'UADM - Ubuntu administration scripts. A set of script that allows to automate task and notify the administrator by mail when thing went wrong. Update os, backup directories on a remote samba server, auto reinstall vmware tools on new kernel.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
