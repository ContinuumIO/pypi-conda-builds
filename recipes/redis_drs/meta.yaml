package:
  name: redis_drs
  version: "0.0.3"

source:
  fn: redis_drs-0.0.3.zip
  url: https://pypi.python.org/packages/source/r/redis_drs/redis_drs-0.0.3.zip
  md5: a2ea8e8824623a33d6d508504a549f43
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - redis_drs = redis_drs:main
    #
    # Would create an entry point called redis_drs that calls redis_drs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - redis_drs

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/stnbu/redis_drs
  license: MIT License
  summary: 'A Distributed Resource Scheduling system that uses Redis.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
