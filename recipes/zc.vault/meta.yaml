package:
  name: zc.vault
  version: "0.11"

source:
  fn: zc.vault-0.11.tar.gz
  url: https://pypi.python.org/packages/source/z/zc.vault/zc.vault-0.11.tar.gz
  md5: 20fc3bbba33ed2a2976735870422ff7c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zc.vault = zc.vault:main
    #
    # Would create an entry point called zc.vault that calls zc.vault.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - rwproperty
    - setuptools
    - zc.copy
    - zc.freeze
    - zc.objectlog
    - zc.relationship
    - zc.shortcut
    - zodb3
    - zope.app.container
    - zope.app.intid
    - zope.app.keyreference
    - zope.cachedescriptors
    - zope.component
    - zope.copypastemove
    - zope.event
    - zope.i18n
    - zope.i18nmessageid
    - zope.interface
    - zope.lifecycleevent
    - zope.location
    - zope.proxy
    - zope.publisher
    - zope.schema
    - zope.traversing

  run:
    - python
    - rwproperty
    - setuptools
    - zc.copy
    - zc.freeze
    - zc.objectlog
    - zc.relationship
    - zc.shortcut
    - zodb3
    - zope.app.container
    - zope.app.intid
    - zope.app.keyreference
    - zope.cachedescriptors
    - zope.component
    - zope.copypastemove
    - zope.event
    - zope.i18n
    - zope.i18nmessageid
    - zope.interface
    - zope.lifecycleevent
    - zope.location
    - zope.proxy
    - zope.publisher
    - zope.schema
    - zope.traversing

test:
  # Python imports
  imports:
    - zc
    - zc.vault

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/zc.vault
  license: Zope Public License
  summary: 'Low-level versioning support'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
