package:
  name: djes
  version: "0.0.1a7"

source:
  fn: djes-0.0.1a7.tar.gz
  url: https://pypi.python.org/packages/source/d/djes/djes-0.0.1a7.tar.gz
  md5: caa74c4a185d7e2d3a8470a7a0bb8ac0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djes = djes:main
    #
    # Would create an entry point called djes that calls djes.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - elasticsearch-dsl ==0.0.4
    - django >=1.8

  run:
    - python
    - elasticsearch-dsl ==0.0.4
    - django >=1.8

test:
  # Python imports
  imports:
    - djes
    - djes/conf
    - djes/management
    - djes/management/commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/theonion/djes
  license: MIT
  summary: 'Connecting Django and elasticsearch-dsl'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
