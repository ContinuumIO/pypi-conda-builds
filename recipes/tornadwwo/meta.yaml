package:
  name: tornadwwo
  version: "0.1.3"

source:
  fn: tornadwwo-0.1.3.zip
  url: https://pypi.python.org/packages/source/t/tornadwwo/tornadwwo-0.1.3.zip
  md5: 05d33b04a41a7c8dcc3d66474dba22ff
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tornadwwo = tornadwwo:main
    #
    # Would create an entry point called tornadwwo that calls tornadwwo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - tornado

  run:
    - python
    - tornado

test:
  # Python imports
  imports:
    - tornadwwo

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/abdelouahabb/tornadwwo
  license: MIT
  summary: 'Async API calls for World Weather Online'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
