package:
  name: digimat.ccu
  version: "0.1.14"

source:
  fn: digimat.ccu-0.1.14.tar.gz
  url: https://pypi.python.org/packages/source/d/digimat.ccu/digimat.ccu-0.1.14.tar.gz
  md5: a29a819d565ba99fbe3ea3ec8fcc673f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - digimat.ccu = digimat.ccu:main
    #
    # Would create an entry point called digimat.ccu that calls digimat.ccu.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - requests
    - setuptools

  run:
    - python
    - requests
    - setuptools

test:
  # Python imports
  imports:
    - digimat
    - digimat.ccu

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: PSF
  summary: 'Digimat Homematic CCU2'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
