package:
  name: django-triodia
  version: "0.1b"

source:
  fn: django-triodia-0.1b.tar.gz
  url: https://pypi.python.org/packages/source/d/django-triodia/django-triodia-0.1b.tar.gz
  md5: b3ebdd74284fa132793336a13871a314
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-triodia = django-triodia:main
    #
    # Would create an entry point called django-triodia that calls django-triodia.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - jsonfield >=0.9.20
    - requests >=2.3.0

  run:
    - python
    - jsonfield >=0.9.20
    - requests >=2.3.0

test:
  # Python imports
  imports:
    - triodia

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://triodia.herokuapp.com/
  license: MIT License
  summary: 'A Django app to check taxonomic species names against web based data services such as GBIF.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
