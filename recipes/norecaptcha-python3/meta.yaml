package:
  name: norecaptcha-python3
  version: "1.0.0"

source:
  fn: norecaptcha-python3-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/n/norecaptcha-python3/norecaptcha-python3-1.0.0.tar.gz
  md5: b31dc2879131f57d8311aaec2493193f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - norecaptcha-python3 = norecaptcha-python3:main
    #
    # Would create an entry point called norecaptcha-python3 that calls norecaptcha-python3.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - norecaptcha3

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Zeioth/norecaptcha-python3
  license: MIT License
  summary: 'Python 3 server-side library for google No CAPTCHA reCAPTCHA services.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
