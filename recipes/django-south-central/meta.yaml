package:
  name: django-south-central
  version: "0.2.0"

source:
  fn: django-south-central-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-south-central/django-south-central-0.2.0.tar.gz
  md5: 7f259f690ac0259810844511396a7e97
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-south-central = django-south-central:main
    #
    # Would create an entry point called django-south-central that calls django-south-central.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - south_central

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/hiisi13/south_central
  license: BSD License
  summary: 'Application state migration tool, inspired by South'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
