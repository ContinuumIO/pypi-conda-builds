package:
  name: ucloud-client
  version: "1.0"

source:
  fn: ucloud-client-1.0.tar.gz
  url: https://pypi.python.org/packages/source/u/ucloud-client/ucloud-client-1.0.tar.gz
  md5: af66bd1e49ca0c159d50aeccd34ba7ab
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ucloud-client = ucloud-client:main
    #
    # Would create an entry point called ucloud-client that calls ucloud-client.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - api
    - tests
    - utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/yanheven/ucloud-python-sdk
  license: Apache License Version 2.0
  summary: 'ucloud python client and command line tools'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
