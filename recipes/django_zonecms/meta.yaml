package:
  name: django_zonecms
  version: "0.1.0"

source:
  fn: django_zonecms-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django_zonecms/django_zonecms-0.1.0.tar.gz
  md5: 014516ca4e4b2e05e077e74673c9abf9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_zonecms = django_zonecms:main
    #
    # Would create an entry point called django_zonecms that calls django_zonecms.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.7
    - django-filer
    - django-mptt
    - easy_thumbnails
    - django-polymorphic

  run:
    - python
    - django >=1.7
    - django-filer
    - django-mptt
    - easy_thumbnails
    - django-polymorphic

test:
  # Python imports
  imports:
    - django_zonecms
    - djzone_cms
    - djzone_cms.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/argaen/django_zonecms
  license: UNKNOWN
  summary: 'Minimal cms for single page websites with dynamic content.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
