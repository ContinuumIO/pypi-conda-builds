package:
  name: django-oauth-twitter
  version: "1.11"

source:
  fn: django-oauth-twitter-1.11.tar.gz
  url: https://pypi.python.org/packages/source/d/django-oauth-twitter/django-oauth-twitter-1.11.tar.gz
  md5: 22be596c56b21df673b17b66bb132de0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-oauth-twitter = django-oauth-twitter:main
    #
    # Would create an entry point called django-oauth-twitter that calls django-oauth-twitter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_oauth_twitter
    - django_oauth_twitter.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/akoha/django-oauth-twitter/
  license: MIT License
  summary: 'A Django application that lets you associate Twitter accounts with your User accounts'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
