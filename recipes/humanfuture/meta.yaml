package:
  name: humanfuture
  version: "0.2"

source:
  fn: HumanFuture-0.2.tar.gz
  url: https://pypi.python.org/packages/source/H/HumanFuture/HumanFuture-0.2.tar.gz
  md5: da1440e9a54fe3aefcf6cd8fd7ebc92c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - humanfuture = humanfuture:main
    #
    # Would create an entry point called humanfuture that calls humanfuture.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - humanfuture

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/uniphil/HumanFuture
  license: GPLv3
  summary: 'Convert a future date to a human-friendly english format'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
