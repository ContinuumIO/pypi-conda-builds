package:
  name: django-docfield-couchdb
  version: "0.2.4"

source:
  fn: django-docfield-couchdb-0.2.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-docfield-couchdb/django-docfield-couchdb-0.2.4.tar.gz
  md5: 4aaaee8621d46017af1c2e5ccffc8bb6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-docfield-couchdb = django-docfield-couchdb:main
    #
    # Would create an entry point called django-docfield-couchdb that calls django-docfield-couchdb.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-delegate
    - couchdbkit

  run:
    - python
    - django-delegate
    - couchdbkit

test:
  # Python imports
  imports:
    - docfield

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/fish2000/django-docfield-couchdb
  license: BSD License or GNU General Public License (GPL)
  summary: 'Django fields for representing CouchDB docs and more.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
