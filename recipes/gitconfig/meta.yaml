package:
  name: gitconfig
  version: "0.0.2"

source:
  fn: gitconfig-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/g/gitconfig/gitconfig-0.0.2.tar.gz
  md5: 4397f2052a07e2a9744c7733e34d5428
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gitconfig = gitconfig:main
    #
    # Would create an entry point called gitconfig that calls gitconfig.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - gitpython

  run:
    - python
    - gitpython

test:
  # Python imports
  imports:
    - gitconfig
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/cancerhermit/PyGitConfig/
  license: GNU General Public License (GPL)
  summary: 'git config files wrapper'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
