package:
  name: applib
  version: "1.2"

source:
  fn: applib-1.2.tar.gz
  url: https://pypi.python.org/packages/source/a/applib/applib-1.2.tar.gz
  md5: 23696ec40718aaf88b9681cb62983041
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - applib = applib:main
    #
    # Would create an entry point called applib that calls applib.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - appdirs
    - six >=1.0.0

  run:
    - python
    - appdirs
    - six >=1.0.0

test:
  # Python imports
  imports:
    - applib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/ActiveState/applib
  license: MIT License
  summary: 'Cross-platform application utilities in Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
