package:
  name: django-generic-feeds
  version: "0.1.5"

source:
  fn: django-generic-feeds-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-generic-feeds/django-generic-feeds-0.1.5.tar.gz
  md5: 1d4126141bfb672e503567fa29b8950f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-generic-feeds = django-generic-feeds:main
    #
    # Would create an entry point called django-generic-feeds that calls django-generic-feeds.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5

  run:
    - python
    - django >=1.5

test:
  # Python imports
  imports:
    - feeds

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/wearefarm/django-generic-feeds
  license: BSD License
  summary: '\nA simple way for objects to subscribe to other objects for a "feed-like"\nfunctionality in a Django project\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
