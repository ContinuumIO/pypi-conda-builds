package:
  name: products.sqlalchemyda
  version: "0.6.0b7"

source:
  fn: Products.SQLAlchemyDA-0.6.0b7.tar.gz
  url: https://pypi.python.org/packages/source/P/Products.SQLAlchemyDA/Products.SQLAlchemyDA-0.6.0b7.tar.gz
  md5: a8e185ececb129a570dead2996f000ff
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - products.sqlalchemyda = products.sqlalchemyda:main
    #
    # Would create an entry point called products.sqlalchemyda that calls products.sqlalchemyda.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - z3c.sqlalchemy

  run:
    - python
    - setuptools
    - z3c.sqlalchemy

test:
  # Python imports
  imports:
    - Products
    - Products.SQLAlchemyDA

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/zopefoundation/Products.SQLAlchemyDA
  license: ZPL (see LICENSE.txt)
  summary: 'A generic database adapter for Zope 2'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
