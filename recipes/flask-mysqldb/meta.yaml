package:
  name: flask-mysqldb
  version: "0.1.1"

source:
  fn: Flask-MySQLdb-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-MySQLdb/Flask-MySQLdb-0.1.1.tar.gz
  md5: b68a2b168940cba0e287d95e07e9e8e3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-mysqldb = flask-mysqldb:main
    #
    # Would create an entry point called flask-mysqldb that calls flask-mysqldb.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.10
    - mysqlclient

  run:
    - python
    - flask >=0.10
    - mysqlclient

test:
  # Python imports
  imports:
    - flask_mysqldb

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/admiralobvious/flask-mysqldb
  license: MIT License
  summary: 'MySQLdb extension for Flask'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
