package:
  name: testmania
  version: "0.4.2"

source:
  fn: testmania-0.4.2.tar.gz
  url: https://pypi.python.org/packages/source/t/testmania/testmania-0.4.2.tar.gz
  md5: b6a51854adc947c716eb40b095c18cce
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - testmania = testmania:main
    #
    # Would create an entry point called testmania that calls testmania.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - testmania

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/nailxx/testmania
  license: MIT License
  summary: 'Library of assert_xxx functions for more convenient testing'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
