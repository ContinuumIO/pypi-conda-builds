package:
  name: coopr.extras
  version: "1.2.2"

source:
  fn: coopr.extras-1.2.2.tar.gz
  url: https://pypi.python.org/packages/source/c/coopr.extras/coopr.extras-1.2.2.tar.gz
  md5: df7538f03a292a333b353864a899e582
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - coopr.extras = coopr.extras:main
    #
    # Would create an entry point called coopr.extras that calls coopr.extras.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyro
    - suds-jurko
    - pyyaml
    - openopt
    - funcdesigner
    - derapproximator

  run:
    - python
    - pyro
    - suds-jurko
    - pyyaml
    - openopt
    - funcdesigner
    - derapproximator

test:
  # Python imports
  imports:
    - coopr
    - coopr.extras

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://software.sandia.gov/svn/public/coopr/coopr.extras
  license: BSD License
  summary: 'Extra packages that enhance the functionality of Coopr'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
