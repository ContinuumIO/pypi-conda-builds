package:
  name: data-gc-ca-api
  version: "0.2.4"

source:
  fn: data-gc-ca-api-0.2.4.tar.gz
  url: https://pypi.python.org/packages/source/d/data-gc-ca-api/data-gc-ca-api-0.2.4.tar.gz
  md5: ece11951737113b561ef2c68f669aa63
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - data-gc-ca-api = data-gc-ca-api:main
    #
    # Would create an entry point called data-gc-ca-api that calls data-gc-ca-api.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - data_gc_ca_api

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/igable/data-gc-ca-api
  license: Apache Software License or GNU General Public License (GPL)
  summary: 'Utility for Accessing Environment Canada Data'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
