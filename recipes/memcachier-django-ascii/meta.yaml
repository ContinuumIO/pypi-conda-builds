package:
  name: memcachier-django-ascii
  version: "1.0.0"

source:
  fn: memcachier-django-ascii-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/m/memcachier-django-ascii/memcachier-django-ascii-1.0.0.tar.gz
  md5: eb4bc1a589bd5c424b5b9ea1b1d4bba1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - memcachier-django-ascii = memcachier-django-ascii:main
    #
    # Would create an entry point called memcachier-django-ascii that calls memcachier-django-ascii.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pymemcache
    - django >=1.3

  run:
    - python
    - pymemcache
    - django >=1.3

test:
  # Python imports
  imports:
    - memcachier_django

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/memcachier/django-ascii
  license: Apache Software License
  summary: 'Django cache backend supporting MemCachier service'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
