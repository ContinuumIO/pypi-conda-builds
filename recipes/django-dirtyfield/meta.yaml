package:
  name: django-dirtyfield
  version: "0.6"

source:
  fn: django-dirtyfield-0.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-dirtyfield/django-dirtyfield-0.6.tar.gz
  md5: cead589b6b5d702f1cf99b98869402ae
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-dirtyfield = django-dirtyfield:main
    #
    # Would create an entry point called django-dirtyfield that calls django-dirtyfield.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - djangodirtyfield

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/futurice/django-dirtyfield
  license: BSD
  summary: 'Track changed data in Django Models'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
