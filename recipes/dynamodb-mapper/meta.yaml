package:
  name: dynamodb-mapper
  version: "1.8.1"

source:
  fn: dynamodb-mapper-1.8.1.tar.gz
  url: https://pypi.python.org/packages/source/d/dynamodb-mapper/dynamodb-mapper-1.8.1.tar.gz
  md5: c359855095147d4c23da1ef84ffeee46
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dynamodb-mapper = dynamodb-mapper:main
    #
    # Would create an entry point called dynamodb-mapper that calls dynamodb-mapper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Object mapper for Amazon DynamoDB'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
