package:
  name: experimental.pythonpackages
  version: "0.3.6"

source:
  fn: experimental.pythonpackages-0.3.6.zip
  url: https://pypi.python.org/packages/source/e/experimental.pythonpackages/experimental.pythonpackages-0.3.6.zip
  md5: f1873c8fc757134cd0843077b52f5976
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - experimental.pythonpackages = experimental.pythonpackages:main
    #
    # Would create an entry point called experimental.pythonpackages that calls experimental.pythonpackages.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - experimental
    - experimental.pythonpackages

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: experimental.securityindexing
  summary: 'Test'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
