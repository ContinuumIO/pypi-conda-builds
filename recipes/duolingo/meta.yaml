package:
  name: duolingo
  version: "0.1.1"

source:
  fn: duolingo-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/duolingo/duolingo-0.1.1.tar.gz
  md5: bb44866d441e860909643c70c08a85c7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - duolingo = duolingo:main
    #
    # Would create an entry point called duolingo that calls duolingo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - oauth2 >=1.5.211
    - httplib2 >=0.7.2

  run:
    - python
    - oauth2 >=1.5.211
    - httplib2 >=0.7.2

test:
  # Python imports
  imports:
    - duolingo

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://duolingo.com
  license: LICENSE.txt
  summary: 'python client for the duolingo API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
