package:
  name: gitsynclib
  version: "0.1.4"

source:
  fn: GitSyncLib-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/G/GitSyncLib/GitSyncLib-0.1.4.tar.gz
  md5: 5b3bb01b66d50a22f923bffb32bd63d4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gitsynclib = gitsynclib:main
    #
    # Would create an entry point called gitsynclib that calls gitsynclib.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fabric >=1.3.2
    - pyyaml >=3.10

  run:
    - python
    - fabric >=1.3.2
    - pyyaml >=3.10

test:
  # Python imports
  imports:
    - gitsynclib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jachin/GitSyncLib
  license: MIT License
  summary: 'GitSyncLib is a library that supports GitSync.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
