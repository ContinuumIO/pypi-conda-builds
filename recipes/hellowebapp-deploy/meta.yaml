package:
  name: hellowebapp-deploy
  version: "1.0.2"

source:
  fn: hellowebapp-deploy-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/h/hellowebapp-deploy/hellowebapp-deploy-1.0.2.tar.gz
  md5: 55c509560ea9d838779a89b9a7c5c750
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hellowebapp-deploy = hellowebapp-deploy:main
    #
    # Would create an entry point called hellowebapp-deploy that calls hellowebapp-deploy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - waitress
    - dj-database-url
    - whitenoise

  run:
    - python
    - waitress
    - dj-database-url
    - whitenoise

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/hellowebapp/hellowebapp-deploy
  license: MIT License
  summary: "Packages needed to deploy to Heroku using Hello Web App's tutorial."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
