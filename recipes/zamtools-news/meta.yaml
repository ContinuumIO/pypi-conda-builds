package:
  name: zamtools-news
  version: "0.9"

source:
  fn: zamtools-news-0.9.zip
  url: https://pypi.python.org/packages/source/z/zamtools-news/zamtools-news-0.9.zip
  md5: 049a6edf2f5d5af0beae0360556967cd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zamtools-news = zamtools-news:main
    #
    # Would create an entry point called zamtools-news that calls zamtools-news.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - news
    - news.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/zamtools-news/
  license: UNKNOWN
  summary: 'Django application for generating a simple news feed.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
