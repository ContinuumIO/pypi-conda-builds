package:
  name: ffnet
  version: "0.8.0"

source:
  fn: ffnet-0.8.0.tar.gz
  url: https://pypi.python.org/packages/source/f/ffnet/ffnet-0.8.0.tar.gz
  md5: ebffdc0c69a7b7fb8f61807aee3fab1d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ffnet = ffnet:main
    #
    # Would create an entry point called ffnet that calls ffnet.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - ffnet
    - ffnet.examples
    - ffnet.fortran

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: ffnet.sourceforge.net
  license: GNU General Public License (GPL)
  summary: 'Feed-forward neural network solution for python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
