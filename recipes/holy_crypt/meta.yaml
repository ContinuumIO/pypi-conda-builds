package:
  name: holy_crypt
  version: "1.1"

source:
  fn: holy_crypt-1.1.zip
  url: https://pypi.python.org/packages/source/h/holy_crypt/holy_crypt-1.1.zip
  md5: 40b02256c8f06df7f13b6e3617d14752
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - holy_crypt = holy_crypt:main
    #
    # Would create an entry point called holy_crypt that calls holy_crypt.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - holy_crypt

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Codeusa/Holy-Crypt
  license: holydate
  summary: 'A holy encryption method to protect your strings and assets from the eyes of the blasphemes government and their demon pawns '

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
