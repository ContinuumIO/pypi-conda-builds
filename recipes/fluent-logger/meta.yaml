package:
  name: fluent-logger
  version: "0.3.5"

source:
  fn: fluent-logger-0.3.5.tar.gz
  url: https://pypi.python.org/packages/source/f/fluent-logger/fluent-logger-0.3.5.tar.gz
  md5: bceb0efae0bce7d581f6c0705fe2690a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fluent-logger = fluent-logger:main
    #
    # Would create an entry point called fluent-logger that calls fluent-logger.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - msgpack-python

  run:
    - python
    - msgpack-python

test:
  # Python imports
  imports:
    - fluent

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/fluent/fluent-logger-python
  license: Apache License, Version 2.0
  summary: 'A Python logging handler for Fluentd event collector'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
