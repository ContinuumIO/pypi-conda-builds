package:
  name: hurry.datalink
  version: "0.1"

source:
  fn: hurry.datalink-0.1.tar.gz
  url: https://pypi.python.org/packages/source/h/hurry.datalink/hurry.datalink-0.1.tar.gz
  md5: 7e220342f4f968350d5d5280b0e1df2b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hurry.datalink = hurry.datalink:main
    #
    # Would create an entry point called hurry.datalink that calls hurry.datalink.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - hurry.resource >=0.10

  run:
    - python
    - setuptools
    - setuptools
    - hurry.resource >=0.10

test:
  # Python imports
  imports:
    - hurry
    - hurry.datalink

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT
  summary: 'hurry.resource for JQuery Datalink'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
