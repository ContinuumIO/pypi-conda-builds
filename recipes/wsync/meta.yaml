package:
  name: wsync
  version: "0.0.8"

source:
  fn: wsync-0.0.8.tar.gz
  url: https://pypi.python.org/packages/source/w/wsync/wsync-0.0.8.tar.gz
  md5: 7784be812c6cce9eb8b88e1a6ac22b2a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - wsync = wsync:main
    #
    # Would create an entry point called wsync that calls wsync.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests
    - pyyaml
    - checkablestring

  run:
    - python
    - requests
    - pyyaml
    - checkablestring

test:
  # Python imports
  imports:
    - wsync

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/roberto-reale/wsync
  license: UNKNOWN
  summary: 'A small Python utility for synchronizing a local folder with a remote web repository'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
