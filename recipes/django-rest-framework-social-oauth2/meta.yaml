package:
  name: django-rest-framework-social-oauth2
  version: "0.0.4"

source:
  fn: django-rest-framework-social-oauth2-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-rest-framework-social-oauth2/django-rest-framework-social-oauth2-0.0.4.tar.gz
  md5: 41aac254541be33f05799207c1e2f70f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-rest-framework-social-oauth2 = django-rest-framework-social-oauth2:main
    #
    # Would create an entry point called django-rest-framework-social-oauth2 that calls django-rest-framework-social-oauth2.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - djangorestframework >=3.0.1
    - django-oauth-toolkit >=0.7.2
    - python-social-auth >=0.2.2

  run:
    - python
    - djangorestframework >=3.0.1
    - django-oauth-toolkit >=0.7.2
    - python-social-auth >=0.2.2

test:
  # Python imports
  imports:
    - rest_framework_social_oauth2

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/PhilipGarnero/django-rest-framework-social-oauth2
  license: MIT License
  summary: 'python-social-auth and oauth2 support for Django REST Framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
