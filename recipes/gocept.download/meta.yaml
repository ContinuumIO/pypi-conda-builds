package:
  name: gocept.download
  version: "0.9.5"

source:
  fn: gocept.download-0.9.5.tar.gz
  url: https://pypi.python.org/packages/source/g/gocept.download/gocept.download-0.9.5.tar.gz
  md5: 7165f9db45d2ee1eb1e066f450f2bcf4
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gocept.download = gocept.download:main
    #
    # Would create an entry point called gocept.download that calls gocept.download.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zc.buildout
    - setuptools

  run:
    - python
    - setuptools
    - zc.buildout
    - setuptools

test:
  # Python imports
  imports:
    - gocept
    - gocept.download

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.gocept.com/repos/gocept/gocept.download
  license: Zope Public License
  summary: 'zc.buildout recipe for downloading and extracting an archive.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
