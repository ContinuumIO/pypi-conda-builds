package:
  name: django-auth-abakus
  version: "1.1.0"

source:
  fn: django-auth-abakus-1.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-auth-abakus/django-auth-abakus-1.1.0.tar.gz
  md5: 3b8666b95b1f67f4328bd02026094445
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-auth-abakus = django-auth-abakus:main
    #
    # Would create an entry point called django-auth-abakus that calls django-auth-abakus.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests ==2.4.3

  run:
    - python
    - requests ==2.4.3

test:
  # Python imports
  imports:
    - abakus
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/webkom/django-auth-abakus
  license: MIT
  summary: 'A django auth module that can be used to to authenticate users against the API of abakus.no.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
