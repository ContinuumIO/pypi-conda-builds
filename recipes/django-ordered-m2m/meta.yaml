package:
  name: django-ordered-m2m
  version: "1.0.2"

source:
  fn: django-ordered-m2m-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-ordered-m2m/django-ordered-m2m-1.0.2.tar.gz
  md5: 40b5fdc23228f28abb1b056acaf40f18
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-ordered-m2m = django-ordered-m2m:main
    #
    # Would create an entry point called django-ordered-m2m that calls django-ordered-m2m.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - ordered_m2m

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/markfinger/django-ordered-m2m
  license: MIT License
  summary: "Adds ordering to Django's many-to-many relations."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
