package:
  name: bigmouth
  version: "0.1.3"

source:
  fn: bigmouth-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/b/bigmouth/bigmouth-0.1.3.tar.gz
  md5: a0705afe912cf95b33fb563041860848
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bigmouth = bigmouth:main
    #
    # Would create an entry point called bigmouth that calls bigmouth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - setuptools
    - django

test:
  # Python imports
  imports:
    - bigmouth
    - bigmouth.management
    - bigmouth.management.commands
    - bigmouth.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/devopsni/django-bigmouth
  license: BSD License
  summary: 'Django interface to the Pelican static blog generator.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
