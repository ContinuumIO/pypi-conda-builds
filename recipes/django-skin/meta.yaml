package:
  name: django-skin
  version: "0.1.2"

source:
  fn: django-skin-0.1.2.zip
  url: https://pypi.python.org/packages/source/d/django-skin/django-skin-0.1.2.zip
  md5: 7f9eb17c644c083cb02e648476b1a7b4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-skin = django-skin:main
    #
    # Would create an entry point called django-skin that calls django-skin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6

  run:
    - python
    - django >=1.6

test:
  # Python imports
  imports:
    - skin
    - skin.migrations
    - skin.south_migrations
    - skin.template
    - skin.template.loaders
    - skin.views
    - tests
    - tests.template_loader

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dwatkinsweb/django-skin
  license: MIT License
  summary: 'Add template skins to django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
