package:
  name: ftw.dashboard.dragndrop
  version: "1.5.5"

source:
  fn: ftw.dashboard.dragndrop-1.5.5.zip
  url: https://pypi.python.org/packages/source/f/ftw.dashboard.dragndrop/ftw.dashboard.dragndrop-1.5.5.zip
  md5: e825230d6d00ca92badd1bb57d545f90
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ftw.dashboard.dragndrop = ftw.dashboard.dragndrop:main
    #
    # Would create an entry point called ftw.dashboard.dragndrop that calls ftw.dashboard.dragndrop.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - acquisition
    - zope.component
    - zope.interface
    - zope.publisher
    - zope2
    - plone.portlets
    - plone.theme
    - plone.app.contentmenu
    - plone.app.layout
    - plone.app.portlets
    - products.genericsetup
    - products.statusmessages
    - products.cmfcore
    - collective.js.jqueryui

  run:
    - python
    - setuptools
    - setuptools
    - acquisition
    - zope.component
    - zope.interface
    - zope.publisher
    - zope2
    - plone.portlets
    - plone.theme
    - plone.app.contentmenu
    - plone.app.layout
    - plone.app.portlets
    - products.genericsetup
    - products.statusmessages
    - products.cmfcore
    - collective.js.jqueryui

test:
  # Python imports
  imports:
    - ftw
    - ftw.dashboard
    - ftw.dashboard.dragndrop
    - ftw.dashboard.dragndrop.browser
    - ftw.dashboard.dragndrop.portlets
    - ftw.dashboard.dragndrop.tests
    - ftw.dashboard.dragndrop.upgrade

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/4teamwork/ftw.dashboard.dragndrop
  license: GPL2
  summary: 'ftw.dashboard.dragndrop adds dragndrop functionality to the dashboard'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
