package:
  name: launchpadstats
  version: "0.2.1"

source:
  fn: launchpadstats-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/l/launchpadstats/launchpadstats-0.2.1.tar.gz
  md5: ec81e51cac0a9253bea010a5219a61db
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - launchpadstats = launchpadstats:main
    #
    # Would create an entry point called launchpadstats that calls launchpadstats.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - launchpadstats

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mkollaro/launchpadstats
  license: Apache License, Version 2.0
  summary: 'Get data from Stackalytics trough the CLI.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
