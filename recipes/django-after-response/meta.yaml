package:
  name: django-after-response
  version: "0.2.1"

source:
  fn: django-after-response-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-after-response/django-after-response-0.2.1.tar.gz
  md5: 9a748c70e7d1237782066657c2c64c9d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-after-response = django-after-response:main
    #
    # Would create an entry point called django-after-response that calls django-after-response.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5

  run:
    - python
    - django >=1.5

test:
  # Python imports
  imports:
    - after_response

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/defrex/django-after-response/
  license: MIT
  summary: 'Simple asynchronous execution'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
