package:
  name: indra.ipc
  version: "1.0"

source:
  fn: indra.ipc-1.0.tar.gz
  url: https://pypi.python.org/packages/source/i/indra.ipc/indra.ipc-1.0.tar.gz
  md5: d7de7e679bc6e08cbd675eec9267dd75
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - indra.ipc = indra.ipc:main
    #
    # Would create an entry point called indra.ipc that calls indra.ipc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - indra
    - indra.ipc

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://wiki.secondlife.com/wiki
  license: MIT License
  summary: 'Indra support libs for IPC'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
