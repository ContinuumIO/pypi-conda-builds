package:
  name: django-numerics
  version: "0.2.0"

source:
  fn: django-numerics-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-numerics/django-numerics-0.2.0.tar.gz
  md5: 5cb65e33f08ab50e83765e539221660d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-numerics = django-numerics:main
    #
    # Would create an entry point called django-numerics that calls django-numerics.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - djangonumerics
    - djangonumerics.migrations
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/huseyinyilmaz/django-numerics
  license: UNKNOWN
  summary: 'numerics dashboard endpoint provider for django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
