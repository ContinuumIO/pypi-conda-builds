package:
  name: chronicler
  version: "0.1"

source:
  fn: chronicler-0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/chronicler/chronicler-0.1.tar.gz
  md5: 7992489aeef55fd1b7d77478994741b2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - chronicler = chronicler:main
    #
    # Would create an entry point called chronicler that calls chronicler.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - chronicler
    - chronicler.migrations
    - chronicler.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/analytehealth/chronicler
  license: UNKNOWN
  summary: 'A Django App for preserving revisions of modified Model Objects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
