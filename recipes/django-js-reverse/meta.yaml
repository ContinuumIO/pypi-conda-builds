package:
  name: django-js-reverse
  version: "0.5.0"

source:
  fn: django-js-reverse-0.5.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-js-reverse/django-js-reverse-0.5.0.tar.gz
  md5: da10242767564e63bd4a1a72c2b56d1f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-js-reverse = django-js-reverse:main
    #
    # Would create an entry point called django-js-reverse that calls django-js-reverse.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5
    - slimit ==0.8.1.

  run:
    - python
    - django >=1.5
    - slimit ==0.8.1.

test:
  # Python imports
  imports:
    - django_js_reverse
    - django_js_reverse.management
    - django_js_reverse.management.commands
    - django_js_reverse.templatetags
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ierror/django-js-reverse
  license: MIT License
  summary: "Javascript url handling for Django that doesn't hurt."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
