package:
  name: python-ldraw
  version: "0.26"

source:
  fn: python-ldraw-0.26.zip
  url: https://pypi.python.org/packages/source/p/python-ldraw/python-ldraw-0.26.zip
  md5: a9073e09dfcd41c26b490349f0212b3a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-ldraw = python-ldraw:main
    #
    # Would create an entry point called python-ldraw that calls python-ldraw.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - ldraw
    - ldraw/writers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.boddie.org.uk/david/Projects/Python/ldraw/
  license: GNU General Public License (version 3 or later)
  summary: 'A package for creating LDraw format files.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
