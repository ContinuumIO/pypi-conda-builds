package:
  name: lambdajson
  version: "0.4.1"

source:
  fn: lambdaJSON-0.4.1.zip
  url: https://pypi.python.org/packages/source/l/lambdaJSON/lambdaJSON-0.4.1.zip
  md5: a71615501d0e2c7e974daabcea5f7052
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lambdajson = lambdajson:main
    #
    # Would create an entry point called lambdajson that calls lambdajson.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - lambdaJSON
    - lambdajson

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/pooya-eghbali/lambdaJSON
  license: GNU Lesser General Public License v3 (LGPLv3)
  summary: 'Use json to serialize unsupported python types (and many more including functions, classes, exceptions, etc). Changes: Better support for class serialization.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
