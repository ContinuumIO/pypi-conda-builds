package:
  name: django-fieldsignals
  version: "0.1.1"

source:
  fn: django-fieldsignals-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-fieldsignals/django-fieldsignals-0.1.1.tar.gz
  md5: 2f3921b706a8cbad617b90ea5884a4d3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-fieldsignals = django-fieldsignals:main
    #
    # Would create an entry point called django-fieldsignals that calls django-fieldsignals.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - fieldsignals

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/craigds/django-fieldsignals
  license: UNKNOWN
  summary: 'Django fieldsignals simply makes it easy to tell when the fields on your model have changed.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
