package:
  name: django-datetime-utc
  version: "1.0.1"

source:
  fn: django-datetime-utc-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-datetime-utc/django-datetime-utc-1.0.1.tar.gz
  md5: fe76ecd091578b0768791696be6e396b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-datetime-utc = django-datetime-utc:main
    #
    # Would create an entry point called django-datetime-utc that calls django-datetime-utc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5.0
    - python-dateutil

  run:
    - python
    - django >=1.5.0
    - python-dateutil

test:
  # Python imports
  imports:
    - datetimeutc

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/pixeldomain/django-datetime-utc
  license: MIT License
  summary: 'Django UTC datetime model field - timestamp without time zone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
