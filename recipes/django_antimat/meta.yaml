package:
  name: django_antimat
  version: "1.0.1"

source:
  fn: django_antimat-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django_antimat/django_antimat-1.0.1.tar.gz
  md5: 929bb45123b87c0514b3136dcd8cf59b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_antimat = django_antimat:main
    #
    # Would create an entry point called django_antimat that calls django_antimat.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4.3
    - south
    - pymorphy >=0.5.6

  run:
    - python
    - django >=1.4.3
    - south
    - pymorphy >=0.5.6

test:
  # Python imports
  imports:
    - django_antimat
    - django_antimat.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ruscoder/django_antimat
  license: BSD
  summary: 'Django antimat is application for filtering swear words.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
