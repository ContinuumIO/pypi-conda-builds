package:
  name: linkgrabber
  version: "0.2.9"

source:
  fn: linkGrabber-0.2.9.tar.gz
  url: https://pypi.python.org/packages/source/l/linkGrabber/linkGrabber-0.2.9.tar.gz
  md5: 35a1c6c99dc5490e7c2424c6dc0c5abf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - linkgrabber = linkgrabber:main
    #
    # Would create an entry point called linkgrabber that calls linkgrabber.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests
    - beautifulsoup4

  run:
    - python
    - requests
    - beautifulsoup4

test:
  # Python imports
  imports:
    - linkGrabber
    - linkGrabber.tests
    - linkgrabber

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/detroit-media-partnership/linkGrabber
  license: MIT License
  summary: 'Scrape links from a single web page'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
