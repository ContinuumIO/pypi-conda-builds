package:
  name: collective.behavior.price
  version: "0.4"

source:
  fn: collective.behavior.price-0.4.zip
  url: https://pypi.python.org/packages/source/c/collective.behavior.price/collective.behavior.price-0.4.zip
  md5: e447dbc80ea97857c28b1ed3c620e446
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.behavior.price = collective.behavior.price:main
    #
    # Would create an entry point called collective.behavior.price that calls collective.behavior.price.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - plone.behavior
    - py-moneyed
    - setuptools

  run:
    - python
    - setuptools
    - plone.behavior
    - py-moneyed
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.behavior
    - collective.behavior.price
    - collective.behavior.price.Extensions
    - collective.behavior.price.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.behavior.price/
  license: BSD License
  summary: 'Behavior to make content pricing.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
