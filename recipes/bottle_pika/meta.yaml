package:
  name: bottle_pika
  version: "0.1.0"

source:
  fn: bottle_pika-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/b/bottle_pika/bottle_pika-0.1.0.tar.gz
  md5: 7b01c498d5b12ab7dbd152e8c2f66995
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bottle_pika = bottle_pika:main
    #
    # Would create an entry point called bottle_pika that calls bottle_pika.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - bottle >=0.10
    - pika >=0.9.13

  run:
    - python
    - bottle >=0.10
    - pika >=0.9.13

test:
  # Python imports
  imports:
    - bottle_pika

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dbroudy/bottle-pika
  license: MIT License
  summary: 'Pika plugin module for Bottle microframework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
