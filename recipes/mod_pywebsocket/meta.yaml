package:
  name: mod_pywebsocket
  version: "0.7.9"

source:
  fn: mod_pywebsocket-0.7.9.tar.gz
  url: https://pypi.python.org/packages/source/m/mod_pywebsocket/mod_pywebsocket-0.7.9.tar.gz
  md5: efe625c9febb96421a0366a085630f6d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mod_pywebsocket = mod_pywebsocket:main
    #
    # Would create an entry point called mod_pywebsocket that calls mod_pywebsocket.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - mod_pywebsocket
    - mod_pywebsocket.handshake

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/pywebsocket/
  license: See COPYING
  summary: 'WebSocket extension for Apache HTTP Server.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
