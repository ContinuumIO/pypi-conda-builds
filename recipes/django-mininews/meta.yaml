package:
  name: django-mininews
  version: "0.5"

source:
  fn: django-mininews-0.5.zip
  url: https://pypi.python.org/packages/source/d/django-mininews/django-mininews-0.5.zip
  md5: a59f6c181ad94af3c21c92ff2f2fdf4d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-mininews = django-mininews:main
    #
    # Would create an entry point called django-mininews that calls django-mininews.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4
    - django-model-utils >=2.2

  run:
    - python
    - django >=1.4
    - django-model-utils >=2.2

test:
  # Python imports
  imports:
    - mininews
    - scripts

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/richardbarran/django-mininews
  license: MIT License
  summary: 'Boilerplate for creating publishable lists of objects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
