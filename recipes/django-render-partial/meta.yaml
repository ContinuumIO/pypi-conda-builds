package:
  name: django-render-partial
  version: "0.1"

source:
  fn: django-render-partial-0.1.zip
  url: https://pypi.python.org/packages/source/d/django-render-partial/django-render-partial-0.1.zip
  md5: 27afa7feb91d104703f99b6111bea5db
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-render-partial = django-render-partial:main
    #
    # Would create an entry point called django-render-partial that calls django-render-partial.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_render_partial
    - django_render_partial.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/utapyngo/django-render-partial
  license: BSD License
  summary: 'Django render_partial tag allows inserting rendered views into templates'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
