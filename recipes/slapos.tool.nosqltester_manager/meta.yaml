package:
  name: slapos.tool.nosqltester_manager
  version: "0.0.10.dev-r45981"

source:
  fn: slapos.tool.nosqltester_manager-0.0.10.dev-r45981.tar.gz
  url: https://pypi.python.org/packages/source/s/slapos.tool.nosqltester_manager/slapos.tool.nosqltester_manager-0.0.10.dev-r45981.tar.gz
  md5: fc5b28df587f4788b99f340079398bef
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - slapos.tool.nosqltester_manager = slapos.tool.nosqltester_manager:main
    #
    # Would create an entry point called slapos.tool.nosqltester_manager that calls slapos.tool.nosqltester_manager.main()

    - nosqltester_manager=slapos.tool.nosqltester_manager:main

  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - flask
    - slapos.core
    - erp5.utils.benchmark

  run:
    - python
    - setuptools
    - setuptools
    - flask
    - slapos.core
    - erp5.utils.benchmark

test:
  # Python imports
  imports:
    - slapos
    - slapos.tool
    - slapos.tool.nosqltester_manager

  commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.

    - nosqltester_manager --help

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: GPL
  summary: 'Manager for NoSQL systems tester'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
