package:
  name: icsemantic.core
  version: "1.0-dev-r371"

source:
  fn: icsemantic.core-1.0-dev-r371.tar.gz
  url: https://pypi.python.org/packages/source/i/icsemantic.core/icsemantic.core-1.0-dev-r371.tar.gz
  md5: 37b2eaee6c00cae6605c3f02b751166b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - icsemantic.core = icsemantic.core:main
    #
    # Would create an entry point called icsemantic.core that calls icsemantic.core.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - icsemantic
    - icsemantic.core
    - icsemantic.core.Extensions
    - icsemantic.core.browser
    - icsemantic.core.content
    - icsemantic.core.exportimport
    - icsemantic.core.memberdata
    - icsemantic.core.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.platecom.com/productos/icsemantic.core/
  license: GNU General Public License (GPL)
  summary: 'icSemantic.Core'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
