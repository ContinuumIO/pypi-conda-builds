package:
  name: eea.forms
  version: "6.4"

source:
  fn: eea.forms-6.4.zip
  url: https://pypi.python.org/packages/source/e/eea.forms/eea.forms-6.4.zip
  md5: 38c08be9af319ccfd65d2cbba58ba416
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - eea.forms = eea.forms:main
    #
    # Would create an entry point called eea.forms that calls eea.forms.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.quickupload

  run:
    - python
    - setuptools
    - setuptools
    - collective.quickupload

test:
  # Python imports
  imports:
    - eea
    - eea.forms
    - eea.forms.browser
    - eea.forms.browser.app
    - eea.forms.fields
    - eea.forms.tests
    - eea.forms.upgrades
    - eea.forms.validators
    - eea.forms.widgets

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://eea.github.com/docs/eea.forms
  license: GNU General Public License (GPL) or Mozilla Public License 1.0 (MPL)
  summary: 'EEA forms - Custom AT widgets and fields'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
