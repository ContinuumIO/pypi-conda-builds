package:
  name: django-states
  version: "1.6.5"

source:
  fn: django-states-1.6.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-states/django-states-1.6.5.tar.gz
  md5: 168d7d3d3008c54a27d72fb76a70e1d7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-states = django-states:main
    #
    # Would create an entry point called django-states that calls django-states.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_states
    - django_states.management
    - django_states.management.commands
    - django_states.templatetags
    - test_proj

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/citylive/django-states2
  license: BSD
  summary: 'State machine for django models'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
