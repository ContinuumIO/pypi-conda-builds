package:
  name: pelican-albums
  version: "0.6.0"

source:
  fn: pelican-albums-0.6.0.tar.gz
  url: https://pypi.python.org/packages/source/p/pelican-albums/pelican-albums-0.6.0.tar.gz
  md5: 3e60c1c7cee2eac5d33052d26c69feaa
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pelican-albums = pelican-albums:main
    #
    # Would create an entry point called pelican-albums that calls pelican-albums.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pillow

  run:
    - python
    - pillow

test:
  # Python imports
  imports:
    - pelican_albums

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/iksteen/pelican-albums/
  license: MIT License
  summary: 'Automatic photo album generation and thumbnailing for Pelican.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
