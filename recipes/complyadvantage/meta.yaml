package:
  name: complyadvantage
  version: "0.3"

source:
  fn: complyadvantage-0.3.tar.gz
  url: https://pypi.python.org/packages/source/c/complyadvantage/complyadvantage-0.3.tar.gz
  md5: f8a0147ca06d3b9fc4c053d77ace9453
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - complyadvantage = complyadvantage:main
    #
    # Would create an entry point called complyadvantage that calls complyadvantage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - complyadvantage

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/complyadvantage/python-sanctions-api
  license: UNKNOWN
  summary: 'ComplyAdvantage sanctions API wrapper'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
