package:
  name: doapfiend-gentoo
  version: "0.1.1"

source:
  fn: doapfiend-gentoo-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/doapfiend-gentoo/doapfiend-gentoo-0.1.1.tar.gz
  md5: 18f7992d8626d13734a3d2611184cd6e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - doapfiend-gentoo = doapfiend-gentoo:main
    #
    # Would create an entry point called doapfiend-gentoo that calls doapfiend-gentoo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://trac.doapspace.org/doapfiend/wiki/DfGentoo
  license: BSD-2
  summary: 'Doapfiend plugin. Search for DOAP using a Gentoo package name.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
