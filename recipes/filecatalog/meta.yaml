package:
  name: filecatalog
  version: "0.3.1"

source:
  fn: FileCatalog-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/F/FileCatalog/FileCatalog-0.3.1.tar.gz
  md5: 02324d65ceaaaf70910e148cff6df695
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - filecatalog = filecatalog:main
    #
    # Would create an entry point called filecatalog that calls filecatalog.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyyaml >=3.05
    - wxpython >=2.6.3.2

  run:
    - python
    - pyyaml >=3.05
    - wxpython >=2.6.3.2

test:
  # Python imports
  imports:
    - filecatalog

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://homework.nwsnet.de/
  license: GNU General Public License (GPL)
  summary: 'Tools to create, view and transform hierachical YAML representations of directory structures.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
