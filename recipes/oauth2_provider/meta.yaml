package:
  name: oauth2_provider
  version: "0.0"

source:
  fn: oauth2_provider-0.0.tar.gz
  url: https://pypi.python.org/packages/source/o/oauth2_provider/oauth2_provider-0.0.tar.gz
  md5: 60e4d259c5ddef74596d715199e25028
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - oauth2_provider = oauth2_provider:main
    #
    # Would create an entry point called oauth2_provider that calls oauth2_provider.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - oauth2_provider

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/eventray/oauth2_provider
  license: UNKNOWN
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
