package:
  name: arduino_helpers
  version: "0.3.post4"

source:
  fn: arduino_helpers-0.3.post4.zip
  url: https://pypi.python.org/packages/source/a/arduino_helpers/arduino_helpers-0.3.post4.zip
  md5: 5669cbfc559833b9275a0a41bf8a7924
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - arduino_helpers = arduino_helpers:main
    #
    # Would create an entry point called arduino_helpers that calls arduino_helpers.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - arduino_helpers
    - path_helpers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/cfobel/path_helpers
  license: MIT License
  summary: 'Helper class and functions for working with file path'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
