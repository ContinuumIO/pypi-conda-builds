package:
  name: tesdat
  version: "1.6.0"

source:
  fn: tesdat-1.6.0.tar.gz
  url: https://pypi.python.org/packages/source/t/tesdat/tesdat-1.6.0.tar.gz
  md5: 4a67101009b73bc30b102ad324d278d8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tesdat = tesdat:main
    #
    # Would create an entry point called tesdat that calls tesdat.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - tesdat
    - tesdat.common
    - tesdat.container
    - tesdat.model
    - tesdat.pattern

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/crohaco/tesdat
  license: Apache Software License
  summary: 'tesdat is a package to generate testdata.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
