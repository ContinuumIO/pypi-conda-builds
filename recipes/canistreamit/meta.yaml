package:
  name: canistreamit
  version: "0.2"

source:
  fn: CanIStreamIt-0.2.tar.gz
  url: https://pypi.python.org/packages/source/C/CanIStreamIt/CanIStreamIt-0.2.tar.gz
  md5: 5b7a8bca5977bd9cdcd98ec497fbc203
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - canistreamit = canistreamit:main
    #
    # Would create an entry point called canistreamit that calls canistreamit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=2.0

  run:
    - python
    - requests >=2.0

test:
  # Python imports
  imports:
    - canistreamit

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Bulv1ne/CanIStreamIt
  license: MIT License
  summary: 'Can I Stream It api'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
