package:
  name: goreutilities
  version: "0.5.0"

source:
  fn: GoreUtilities-0.5.0.tar.gz
  url: https://pypi.python.org/packages/source/G/GoreUtilities/GoreUtilities-0.5.0.tar.gz
  md5: d84089563e20f43cbbd77e19e3754888
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - goreutilities = goreutilities:main
    #
    # Would create an entry point called goreutilities that calls goreutilities.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - GoreUtilities
    - GoreUtilities.test
    - goreutilities

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/gorelab/goreutilities/
  license: MIT
  summary: 'Some utility functions useful for GoreLab members'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
