package:
  name: ipylogue
  version: "1.0.1"

source:
  fn: ipylogue-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/i/ipylogue/ipylogue-1.0.1.tar.gz
  md5: 0d85f5f27dbe57cd6b92ba6c49fdb16a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ipylogue = ipylogue:main
    #
    # Would create an entry point called ipylogue that calls ipylogue.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - ipylogue

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: GNU Affero General Public License v3
  summary: 'Save the history of your IPython notebooks with git'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
