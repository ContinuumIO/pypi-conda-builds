package:
  name: easymatrix
  version: "1.0.0a1"

source:
  fn: easymatrix-1.0.0a1.zip
  url: https://pypi.python.org/packages/source/e/easymatrix/easymatrix-1.0.0a1.zip
  md5: 60731301dcd3cae9130e7d285c61937c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - easymatrix = easymatrix:main
    #
    # Would create an entry point called easymatrix that calls easymatrix.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - easymatrix

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: GNU General Public License v2 or later (GPLv2+)
  summary: 'Operations with matrices'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
