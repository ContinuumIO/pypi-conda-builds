package:
  name: products.policyhttpcachemanager
  version: "1.2"

source:
  fn: Products.PolicyHTTPCacheManager-1.2.tar.gz
  url: https://pypi.python.org/packages/source/P/Products.PolicyHTTPCacheManager/Products.PolicyHTTPCacheManager-1.2.tar.gz
  md5: 3159a73d6f2b616f5e47ec87930abdbe
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - products.policyhttpcachemanager = products.policyhttpcachemanager:main
    #
    # Would create an entry point called products.policyhttpcachemanager that calls products.policyhttpcachemanager.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - Products
    - Products.PolicyHTTPCacheManager
    - Products.PolicyHTTPCacheManager.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/Products.PolicyHTTPCacheManager
  license: ZPL
  summary: "Cache manager which delegates policy to CMF's caching policy manager"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
