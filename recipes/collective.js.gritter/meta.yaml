package:
  name: collective.js.gritter
  version: "1.0.2"

source:
  fn: collective.js.gritter-1.0.2.zip
  url: https://pypi.python.org/packages/source/c/collective.js.gritter/collective.js.gritter-1.0.2.zip
  md5: d5a150067bbfdf0f44e61e737b2f1dff
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.js.gritter = collective.js.gritter:main
    #
    # Would create an entry point called collective.js.gritter that calls collective.js.gritter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.js
    - collective.js.gritter

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/collective.js.gritter
  license: GPL
  summary: 'jQuery Gritter (Growl-like-notification) plugin for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
