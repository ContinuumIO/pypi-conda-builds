package:
  name: ely.advancedquery
  version: "1.1"

source:
  fn: ely.advancedquery-1.1.tar.gz
  url: https://pypi.python.org/packages/source/e/ely.advancedquery/ely.advancedquery-1.1.tar.gz
  md5: 926ffbc875d6bbaa1fe6fc60144ddb13
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ely.advancedquery = ely.advancedquery:main
    #
    # Would create an entry point called ely.advancedquery that calls ely.advancedquery.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - ely
    - ely.advancedquery
    - ely.advancedquery.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://ely.googlecode.com/svn/ely.advancedquery/trunk
  license: new BSD
  summary: "Advanced Query wraps the Advanced Query extension to zope's ZCatalog (http://www.dieter.handshake.de/pyprojects/zope/AdvancedQuery.html) for use in plone."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
