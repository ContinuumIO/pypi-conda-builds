package:
  name: soma-base
  version: "4.5.2"

source:
  fn: soma-base-4.5.2.tar.gz
  url: https://pypi.python.org/packages/source/s/soma-base/soma-base-4.5.2.tar.gz
  md5: a578f0254d658652c421ff20ce765d99
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - soma-base = soma-base:main
    #
    # Would create an entry point called soma-base that calls soma-base.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - soma
    - soma.controller
    - soma.gui
    - soma.gui.icons
    - soma.minf
    - soma.pipeline
    - soma.plugins
    - soma.qt4gui
    - soma.qt_gui
    - soma.qt_gui.widgets
    - soma.sandbox
    - soma.utils
    - soma.zipfile

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: CeCILL-B
  summary: 'Soma-base'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
