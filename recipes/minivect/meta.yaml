package:
  name: minivect
  version: "0.1"

source:
  fn: minivect-0.1.tar.bz2
  url: https://pypi.python.org/packages/source/m/minivect/minivect-0.1.tar.bz2
  md5: 95ce8aaa0fbc57e90bf2ae713992ec0d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - minivect = minivect:main
    #
    # Would create an entry point called minivect that calls minivect.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - minivect
    - minivect.pydot
    - minivect.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/markflorisson88/minivect
  license: BSD
  summary: 'Compiler backend for array expressions'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
