package:
  name: luggage
  version: "0.1.2c"

source:
  fn: luggage-0.1.2c.tar.gz
  url: https://pypi.python.org/packages/source/l/luggage/luggage-0.1.2c.tar.gz
  md5: 5e400346cc1d136752c2b22a393b8b7f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - luggage = luggage:main
    #
    # Would create an entry point called luggage that calls luggage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests
    - python-dateutil

  run:
    - python
    - requests
    - python-dateutil

test:
  # Python imports
  imports:
    - luggage

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/wonderb0lt/luggage
  license: MIT License
  summary: 'A wrapper library for various cloud storage services'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
