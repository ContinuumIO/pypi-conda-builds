package:
  name: medialog.subskinsiii
  version: "1.9b5"

source:
  fn: medialog.subskinsiii-1.9b5.tar.gz
  url: https://pypi.python.org/packages/source/m/medialog.subskinsiii/medialog.subskinsiii-1.9b5.tar.gz
  md5: 409874e6b7f876bee292e8b8e275113a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - medialog.subskinsiii = medialog.subskinsiii:main
    #
    # Would create an entry point called medialog.subskinsiii that calls medialog.subskinsiii.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.plonesubskins

  run:
    - python
    - setuptools
    - setuptools
    - products.plonesubskins

test:
  # Python imports
  imports:
    - medialog
    - medialog.subskinsiii
    - medialog.subskinsiii.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/
  license: GPL
  summary: 'An installable theme for Plone 3.0'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
