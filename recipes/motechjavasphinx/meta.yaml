package:
  name: motechjavasphinx
  version: "0.6"

source:
  fn: motechjavasphinx-0.6.tar.gz
  url: https://pypi.python.org/packages/source/m/motechjavasphinx/motechjavasphinx-0.6.tar.gz
  md5: 8e01d1dc098ac23a49ac83efeaf16b98
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - motechjavasphinx = motechjavasphinx:main
    #
    # Would create an entry point called motechjavasphinx that calls motechjavasphinx.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - javalang >=0.9.5
    - lxml
    - beautifulsoup4
    - javasphinx >=0.9.11

  run:
    - python
    - javalang >=0.9.5
    - lxml
    - beautifulsoup4
    - javasphinx >=0.9.11

test:
  # Python imports
  imports:
    - motechjavasphinx

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/motech/motechjavasphinx
  license: BSD License
  summary: 'Sphinx extension for executing javasphinx to documenting Java projects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
