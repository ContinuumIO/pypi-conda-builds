package:
  name: django-compresshtml
  version: "0.1.1"

source:
  fn: django-compresshtml-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-compresshtml/django-compresshtml-0.1.1.tar.gz
  md5: 90115c2122e03a01ea8524dc2f7cba67
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-compresshtml = django-compresshtml:main
    #
    # Would create an entry point called django-compresshtml that calls django-compresshtml.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - compresshtml

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'HTML compressor for django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
