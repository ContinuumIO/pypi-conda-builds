package:
  name: airbrakepy
  version: "1.0.0b2"

source:
  fn: AirbrakePy-1.0.0b2.tar.gz
  url: https://pypi.python.org/packages/source/A/AirbrakePy/AirbrakePy-1.0.0b2.tar.gz
  md5: db08c40840744da0d0aea26ab4dcdd97
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - airbrakepy = airbrakepy:main
    #
    # Would create an entry point called airbrakepy that calls airbrakepy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - xmlbuilder >=0.9,<1.0

  run:
    - python
    - xmlbuilder >=0.9,<1.0

test:
  # Python imports
  imports:
    - airbrakepy
    - airbrakepy.logging
    - airbrakepy.test
    - airbrakepy.test.integration

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/pulseenergy/airbrakepy
  license: Apache Software License
  summary: 'Airbrake notifier for Python logging framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
