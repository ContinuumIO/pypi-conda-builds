package:
  name: ibmiotf
  version: "0.0.10"

source:
  fn: ibmiotf-0.0.10.zip
  url: https://pypi.python.org/packages/source/i/ibmiotf/ibmiotf-0.0.10.zip
  md5: 392bbe4f1c4c82358f01f1b0a7b19b1e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ibmiotf = ibmiotf:main
    #
    # Would create an entry point called ibmiotf that calls ibmiotf.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - iso8601 >=0.1.10
    - paho-mqtt >=1.0
    - pytz >=2014.7
    - requests >=2.5.0

  run:
    - python
    - iso8601 >=0.1.10
    - paho-mqtt >=1.0
    - pytz >=2014.7
    - requests >=2.5.0

test:
  # Python imports
  imports:
    - ibmiotf

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ibm-messaging/iot-python
  license: ibmm2gv
  summary: 'IBM Internet of Things Foundation for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
