package:
  name: ftw.profilehook
  version: "1.0.0"

source:
  fn: ftw.profilehook-1.0.0.zip
  url: https://pypi.python.org/packages/source/f/ftw.profilehook/ftw.profilehook-1.0.0.zip
  md5: ad54dede5175005d7ff487c4786ba01d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ftw.profilehook = ftw.profilehook:main
    #
    # Would create an entry point called ftw.profilehook that calls ftw.profilehook.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - ftw
    - ftw.profilehook
    - ftw.profilehook.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/4teamwork/ftw.profilehook
  license: GNU General Public License (GPL)
  summary: 'A Plone addon providing a hook for executing code when a generic setup profile is installed.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
