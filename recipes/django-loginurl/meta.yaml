package:
  name: django-loginurl
  version: "0.2"

source:
  fn: django-loginurl-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-loginurl/django-loginurl-0.2.tar.gz
  md5: 0b9ac5814168c8e2e65ec21f4c216908
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-loginurl = django-loginurl:main
    #
    # Would create an entry point called django-loginurl that calls django-loginurl.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - loginurl
    - loginurl.management
    - loginurl.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/fajran/django-loginurl/
  license: BSD License
  summary: 'Allowing an anonymous user to log in by only visiting a URL'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
