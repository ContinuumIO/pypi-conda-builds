package:
  name: micropython-upip
  version: "0.5.1"

source:
  fn: micropython-upip-0.5.1.tar.gz
  url: https://pypi.python.org/packages/source/m/micropython-upip/micropython-upip-0.5.1.tar.gz
  md5: 8b74144aad3875c6ac116ebf45b1a085
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - micropython-upip = micropython-upip:main
    #
    # Would create an entry point called micropython-upip that calls micropython-upip.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/micropython/micropython/issues/405
  license: MIT
  summary: 'Simple package manager for MicroPython.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
