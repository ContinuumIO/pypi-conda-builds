package:
  name: eea.flis.metadata
  version: "0.5.5"

source:
  fn: eea.flis.metadata-0.5.5.tar.gz
  url: https://pypi.python.org/packages/source/e/eea.flis.metadata/eea.flis.metadata-0.5.5.tar.gz
  md5: 2148b7d7f21b9d7f2d95f4f13d2e2ec1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - eea.flis.metadata = eea.flis.metadata:main
    #
    # Would create an entry point called eea.flis.metadata that calls eea.flis.metadata.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests
    - django

  run:
    - python
    - requests
    - django

test:
  # Python imports
  imports:
    - flis_metadata
    - flis_metadata.client
    - flis_metadata.client.management
    - flis_metadata.client.management.commands
    - flis_metadata.common
    - flis_metadata.common.management
    - flis_metadata.common.management.commands
    - flis_metadata.common.migrations
    - flis_metadata.common.south_migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/eea/flis.metadata
  license: BSD License
  summary: 'EEA Metadata models replication support'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
