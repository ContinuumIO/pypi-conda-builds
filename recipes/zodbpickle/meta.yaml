package:
  name: zodbpickle
  version: "0.6.0"

source:
  fn: zodbpickle-0.6.0.tar.gz
  url: https://pypi.python.org/packages/source/z/zodbpickle/zodbpickle-0.6.0.tar.gz
  md5: bd050064511eaf2ac276a21bedf9d072
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zodbpickle = zodbpickle:main
    #
    # Would create an entry point called zodbpickle that calls zodbpickle.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - zodbpickle
    - zodbpickle.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/zodbpickle
  license: Zope Public License or Python Software Foundation License
  summary: 'Fork of Python 3 pickle module.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
