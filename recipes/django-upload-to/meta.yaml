package:
  name: django-upload-to
  version: "1.0.0"

source:
  fn: django-upload-to-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-upload-to/django-upload-to-1.0.0.tar.gz
  md5: 1acd6e4546d1a2ce0f42c933a2a9eded
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-upload-to = django-upload-to:main
    #
    # Would create an entry point called django-upload-to that calls django-upload-to.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - tests
    - upload_to

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/hzdg/django-upload-to
  license: MIT
  summary: 'provides an easy way to name user media (uploaded files) based on properties of the model that stores them'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
