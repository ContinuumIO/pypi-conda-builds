package:
  name: bigimg
  version: "0.0.3b"

source:
  fn: bigimg-0.0.3b.tar.gz
  url: https://pypi.python.org/packages/source/b/bigimg/bigimg-0.0.3b.tar.gz
  md5: fa78905e765e4a23562c899ca23b12a6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bigimg = bigimg:main
    #
    # Would create an entry point called bigimg that calls bigimg.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - scipy >=0.9.0
    - numpy

  run:
    - python
    - scipy >=0.9.0
    - numpy

test:
  # Python imports
  imports:
    - bigimg
    - test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/stephenbalaban/bigimg
  license: LICENSE
  summary: 'bigimg lets you generate big images with just a lambda expression.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
