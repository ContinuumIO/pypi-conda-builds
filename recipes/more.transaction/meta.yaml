package:
  name: more.transaction
  version: "0.4"

source:
  fn: more.transaction-0.4.zip
  url: https://pypi.python.org/packages/source/m/more.transaction/more.transaction-0.4.zip
  md5: 4c5cfe307da2d6aa624aef24c6265311
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - more.transaction = more.transaction:main
    #
    # Would create an entry point called more.transaction that calls more.transaction.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - morepath >=0.7
    - transaction

  run:
    - python
    - setuptools
    - morepath >=0.7
    - transaction

test:
  # Python imports
  imports:
    - more
    - more.transaction
    - more.transaction.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/more.transaction
  license: BSD
  summary: 'transaction integration for Morepath'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
