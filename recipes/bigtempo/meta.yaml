package:
  name: bigtempo
  version: "0.38.9"

source:
  fn: bigtempo-0.38.9.tar.gz
  url: https://pypi.python.org/packages/source/b/bigtempo/bigtempo-0.38.9.tar.gz
  md5: 3c104d43c4cb7a613f9474f136aca577
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bigtempo = bigtempo:main
    #
    # Would create an entry point called bigtempo that calls bigtempo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - mockito >=0.5.1
    - nose >=1.3.0
    - pep8 >=1.4.6
    - coverage >=3.6
    - numpy >=1.7.1
    - python-dateutil >=2.1
    - pytz >=2013d
    - numexpr >=2.2.1
    - bottleneck >=0.6.0
    - cython >=0.19.1
    - scipy >=0.12.0
    - matplotlib
    - pandas >=0.12.0
    - patsy >=0.2.1
    - statsmodels >=0.5.0

  run:
    - python
    - mockito >=0.5.1
    - nose >=1.3.0
    - pep8 >=1.4.6
    - coverage >=3.6
    - numpy >=1.7.1
    - python-dateutil >=2.1
    - pytz >=2013d
    - numexpr >=2.2.1
    - bottleneck >=0.6.0
    - cython >=0.19.1
    - scipy >=0.12.0
    - matplotlib
    - pandas >=0.12.0
    - patsy >=0.2.1
    - statsmodels >=0.5.0

test:
  # Python imports
  imports:
    - bigtempo
    - bigtempo.processors

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rhlobo/bigtempo
  license: MIT License
  summary: 'BigTempo is a powerful and scalable programming model, originally crafted for temporal data processment / analysis.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
