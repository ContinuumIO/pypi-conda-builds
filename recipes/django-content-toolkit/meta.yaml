package:
  name: django-content-toolkit
  version: "0.1.3"

source:
  fn: django-content-toolkit-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-content-toolkit/django-content-toolkit-0.1.3.tar.gz
  md5: 40857611b7490c54f93e32e9ca7c5261
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-content-toolkit = django-content-toolkit:main
    #
    # Would create an entry point called django-content-toolkit that calls django-content-toolkit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - django-polymorphic
    - django-reversion
    - django-mptt
    - django-taggit

  run:
    - python
    - django
    - django-polymorphic
    - django-reversion
    - django-mptt
    - django-taggit

test:
  # Python imports
  imports:
    - content

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/esistgut/django-content-toolkit
  license: MIT
  summary: 'A simple Django package for model translations'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
