package:
  name: django-filepages
  version: "1.1.0"

source:
  fn: django-filepages-1.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-filepages/django-filepages-1.1.0.tar.gz
  md5: b01401e1b927f95dd77b9a2ea2c4c00f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-filepages = django-filepages:main
    #
    # Would create an entry point called django-filepages that calls django-filepages.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - filepages

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/prestontimmons/django-filepages
  license: UNKNOWN
  summary: 'Route urls to file system templates in Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
