package:
  name: hypua2jamo
  version: "0.3"

source:
  fn: hypua2jamo-0.3.tar.gz
  url: https://pypi.python.org/packages/source/h/hypua2jamo/hypua2jamo-0.3.tar.gz
  md5: a00ad1cb9a46d99e4c3c83c5f1bcb063
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hypua2jamo = hypua2jamo:main
    #
    # Would create an entry point called hypua2jamo that calls hypua2jamo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - hypua2jamo

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mete0r/hypua2jamo
  license: GNU Lesser General Public License v3 or later (LGPLv3+)
  summary: 'Convert Hanyang-PUA code to unicode Hangul Jamo, i.e., Syllable-Initial-Peak-Final Encoding (\xec\xb2\xab\xea\xb0\x80\xeb\x81\x9d \xec\xbd\x94\xeb\x93\x9c).'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
