package:
  name: zenpacks.lbn.zopemonitor
  version: "4.0.2"

source:
  fn: ZenPacks.lbn.ZopeMonitor-4.0.2.tar.gz
  url: https://pypi.python.org/packages/source/Z/ZenPacks.lbn.ZopeMonitor/ZenPacks.lbn.ZopeMonitor-4.0.2.tar.gz
  md5: 205d149dab9ed936c4255020252dc70b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zenpacks.lbn.zopemonitor = zenpacks.lbn.zopemonitor:main
    #
    # Would create an entry point called zenpacks.lbn.zopemonitor that calls zenpacks.lbn.zopemonitor.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zenpacks.lbn.base >=4.0.3

  run:
    - python
    - setuptools
    - zenpacks.lbn.base >=4.0.3

test:
  # Python imports
  imports:
    - ZenPacks
    - ZenPacks.lbn
    - ZenPacks.lbn.ZopeMonitor
    - ZenPacks.lbn.ZopeMonitor.datasources
    - ZenPacks.lbn.ZopeMonitor.parsers
    - ZenPacks.lbn.ZopeMonitor.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://linux.last-bastion.net/LBN/up2date/monitor
  license: Zope Public License
  summary: 'ZenPack to monitor Zope Application Server stats'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
