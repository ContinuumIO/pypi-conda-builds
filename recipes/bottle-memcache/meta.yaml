package:
  name: bottle-memcache
  version: "0.2.1"

source:
  fn: bottle-memcache-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/b/bottle-memcache/bottle-memcache-0.2.1.tar.gz
  md5: 4ddcb919222afdef3cab63636f09a11e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bottle-memcache = bottle-memcache:main
    #
    # Would create an entry point called bottle-memcache that calls bottle-memcache.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - distribute
    - bottle >=0.9
    - python-memcached

  run:
    - python
    - distribute
    - bottle >=0.9
    - python-memcached

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/bottlepy/bottle-extras/
  license: MIT License
  summary: 'Memcache integration for Bottle.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
