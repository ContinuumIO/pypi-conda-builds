package:
  name: httproxy
  version: "0.9.0"

source:
  fn: httproxy-0.9.0.tar.gz
  url: https://pypi.python.org/packages/source/h/httproxy/httproxy-0.9.0.tar.gz
  md5: 5733dc93562b372aa999a7a3e12c8a42
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - httproxy = httproxy:main
    #
    # Would create an entry point called httproxy that calls httproxy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - configparser
    - docopt

  run:
    - python
    - configparser
    - docopt

test:
  # Python imports
  imports:
    - httproxy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ambv/httproxy/
  license: MIT License
  summary: 'A tiny HTTP proxy implementation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
