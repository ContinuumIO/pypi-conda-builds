package:
  name: django-vkontakte-board
  version: "0.7.0"

source:
  fn: django-vkontakte-board-0.7.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-vkontakte-board/django-vkontakte-board-0.7.0.tar.gz
  md5: 7eb8dbbc574677017e14be2959be76f7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-vkontakte-board = django-vkontakte-board:main
    #
    # Would create an entry point called django-vkontakte-board that calls django-vkontakte-board.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-vkontakte-api >=0.5.3
    - django-vkontakte-users >=0.3.6
    - django-vkontakte-groups >=0.3.0

  run:
    - python
    - django-vkontakte-api >=0.5.3
    - django-vkontakte-users >=0.3.6
    - django-vkontakte-groups >=0.3.0

test:
  # Python imports
  imports:
    - vkontakte_board
    - vkontakte_board.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ramusus/django-vkontakte-board
  license: BSD License
  summary: 'Django implementation for vkontakte API Board'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
