package:
  name: plonehrm.contracts
  version: "3.10"

source:
  fn: plonehrm.contracts-3.10.tar.gz
  url: https://pypi.python.org/packages/source/p/plonehrm.contracts/plonehrm.contracts-3.10.tar.gz
  md5: 010c4dc5b9e74c49e00217d885112d24
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plonehrm.contracts = plonehrm.contracts:main
    #
    # Would create an entry point called plonehrm.contracts that calls plonehrm.contracts.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.autopermission

  run:
    - python
    - setuptools
    - setuptools
    - collective.autopermission

test:
  # Python imports
  imports:
    - plonehrm
    - plonehrm.contracts
    - plonehrm.contracts.content
    - plonehrm.contracts.notifications
    - plonehrm.contracts.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://prettigpersoneel.nl
  license: GPL
  summary: 'Contracts for Plone HRM'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
