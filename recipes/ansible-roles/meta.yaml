package:
  name: ansible-roles
  version: "0.1.4"

source:
  fn: ansible-roles-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/a/ansible-roles/ansible-roles-0.1.4.tar.gz
  md5: b1ec2197b46ce0b139019c88e08550d9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ansible-roles = ansible-roles:main
    #
    # Would create an entry point called ansible-roles that calls ansible-roles.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - ansible_roles

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://pikacode.com/meister/ansible-roles
  license: MIT License
  summary: 'Manage ansible roles.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
