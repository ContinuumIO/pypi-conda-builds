package:
  name: django-inlineobjects
  version: "1.0.2"

source:
  fn: django-inlineobjects-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-inlineobjects/django-inlineobjects-1.0.2.tar.gz
  md5: 681653d8f230b98a8617965c503518d3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-inlineobjects = django-inlineobjects:main
    #
    # Would create an entry point called django-inlineobjects that calls django-inlineobjects.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - beautifulsoup >=3.0.0

  run:
    - python
    - beautifulsoup >=3.0.0

test:
  # Python imports
  imports:
    - inlines
    - inlines.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/pigmonkey/django-inlineobjects
  license: BSD License
  summary: 'A reusable Django application used to insert content objects into other pieces of content.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
