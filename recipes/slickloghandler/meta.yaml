package:
  name: slickloghandler
  version: "0.2.12"

source:
  fn: SlickLogHandler-0.2.12.tar.gz
  url: https://pypi.python.org/packages/source/S/SlickLogHandler/SlickLogHandler-0.2.12.tar.gz
  md5: 406987665e4762937354e63023cdd307
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - slickloghandler = slickloghandler:main
    #
    # Would create an entry point called slickloghandler that calls slickloghandler.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - slicklog
    - slickloghandler

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://slicklog.com/
  license: LICENSE.txt
  summary: 'SlickLog.com Python Handler.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
