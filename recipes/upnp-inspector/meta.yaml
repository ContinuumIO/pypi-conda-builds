package:
  name: upnp-inspector
  version: "0.2.2"

source:
  fn: UPnP-Inspector-0.2.2.tar.gz
  url: http://coherence.beebits.net/download/UPnP-Inspector-0.2.2.tar.gz
  #md5: 
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - upnp-inspector = upnp-inspector:main
    #
    # Would create an entry point called upnp-inspector that calls upnp-inspector.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - coherence >=0.6.4

  run:
    - python
    - coherence >=0.6.4

test:
  # Python imports
  imports:
    - upnp_inspector

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://coherence.beebits.net/wiki/UPnP-Inspector
  license: MIT License
  summary: 'UPnP Device and Service analyzer'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
