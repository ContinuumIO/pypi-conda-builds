package:
  name: marrow.cache
  version: "1.0.3"

source:
  fn: marrow.cache-1.0.3.tar.gz
  url: https://pypi.python.org/packages/source/m/marrow.cache/marrow.cache-1.0.3.tar.gz
  md5: 3fb10a668036f7f0750abc888ec44ca1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - marrow.cache = marrow.cache:main
    #
    # Would create an entry point called marrow.cache that calls marrow.cache.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pymongo <3.0
    - mongoengine >=0.8.5
    - marrow.package <2.0
    - wrapt <2.0

  run:
    - python
    - pymongo <3.0
    - mongoengine >=0.8.5
    - marrow.package <2.0
    - wrapt <2.0

test:
  # Python imports
  imports:
    - marrow
    - marrow.cache

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/marrow/cache/
  license: MIT License
  summary: 'An extension to MongoEngine for memoization and document-aware caching.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
