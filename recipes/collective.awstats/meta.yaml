package:
  name: collective.awstats
  version: "1.0"

source:
  fn: collective.awstats-1.0.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.awstats/collective.awstats-1.0.tar.gz
  md5: 9bd1185a16ee08c9798fee9009c32d3c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.awstats = collective.awstats:main
    #
    # Would create an entry point called collective.awstats that calls collective.awstats.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone
    - bda.awstatsparser
    - yafowil.plone

  run:
    - python
    - setuptools
    - setuptools
    - plone
    - bda.awstatsparser
    - yafowil.plone

test:
  # Python imports
  imports:
    - collective
    - collective.awstats
    - collective.awstats.at
    - collective.awstats.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/collective.awstats
  license: GNU General Public License (GPL)
  summary: 'AWStats in Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
