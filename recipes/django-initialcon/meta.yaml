package:
  name: django-initialcon
  version: "0.1.4"

source:
  fn: django-initialcon-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-initialcon/django-initialcon-0.1.4.tar.gz
  md5: b2d056df92076a1609dbe1d6d8433664
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-initialcon = django-initialcon:main
    #
    # Would create an entry point called django-initialcon that calls django-initialcon.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - initialcon

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bettsmatt/django-initialcon
  license: BSD License
  summary: 'A small django application for generating small colourful icons for users profile pictures'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
