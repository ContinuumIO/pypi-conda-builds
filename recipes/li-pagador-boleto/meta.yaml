package:
  name: li-pagador-boleto
  version: "2.0.16"

source:
  fn: li-pagador-boleto-2.0.16.tar.gz
  url: https://pypi.python.org/packages/source/l/li-pagador-boleto/li-pagador-boleto-2.0.16.tar.gz
  md5: 3e1d98da0a1638624635c9864a9e63c6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - li-pagador-boleto = li-pagador-boleto:main
    #
    # Would create an entry point called li-pagador-boleto that calls li-pagador-boleto.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - distribute
    - li-pagador

  run:
    - python
    - distribute
    - li-pagador

test:
  # Python imports
  imports:
    - pagador_boleto

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lojaintegrada/LI-Pagador-Boleto
  license: MIT License
  summary: u'Meio de pagamento usando boleto banc\xe1rio'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
