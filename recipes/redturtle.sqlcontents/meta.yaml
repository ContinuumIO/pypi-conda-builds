package:
  name: redturtle.sqlcontents
  version: "0.9.1"

source:
  fn: redturtle.sqlcontents-0.9.1.zip
  url: https://pypi.python.org/packages/source/r/redturtle.sqlcontents/redturtle.sqlcontents-0.9.1.zip
  md5: 46517665c81651fc0d66628eb70f4b0b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - redturtle.sqlcontents = redturtle.sqlcontents:main
    #
    # Would create an entry point called redturtle.sqlcontents that calls redturtle.sqlcontents.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.datagridfield
    - sqlalchemy

  run:
    - python
    - setuptools
    - setuptools
    - products.datagridfield
    - sqlalchemy

test:
  # Python imports
  imports:
    - redturtle
    - redturtle.sqlcontents
    - redturtle.sqlcontents.adapters
    - redturtle.sqlcontents.browser
    - redturtle.sqlcontents.content
    - redturtle.sqlcontents.interfaces
    - redturtle.sqlcontents.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/redturtle.sqlcontents/
  license: GNU General Public License (GPL)
  summary: 'RedTurtle - SQL Contents'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
