package:
  name: pelican-edit-url
  version: "0.1.1"

source:
  fn: pelican-edit-url-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/p/pelican-edit-url/pelican-edit-url-0.1.1.tar.gz
  md5: c432f5ceb70cb361feab07713a9cfa42
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pelican-edit-url = pelican-edit-url:main
    #
    # Would create an entry point called pelican-edit-url that calls pelican-edit-url.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pelican

  run:
    - python
    - pelican

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/pmclanahan/pelican-edit-url
  license: MIT License
  summary: 'Add edit URLs to content objects.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
