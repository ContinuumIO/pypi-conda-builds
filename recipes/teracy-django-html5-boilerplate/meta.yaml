package:
  name: teracy-django-html5-boilerplate
  version: "0.3.0"

source:
  fn: teracy-django-html5-boilerplate-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/t/teracy-django-html5-boilerplate/teracy-django-html5-boilerplate-0.3.0.tar.gz
  md5: 1f057a30fee955fa857363d5e66900a3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - teracy-django-html5-boilerplate = teracy-django-html5-boilerplate:main
    #
    # Would create an entry point called teracy-django-html5-boilerplate that calls teracy-django-html5-boilerplate.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5

  run:
    - python
    - django >=1.5

test:
  # Python imports
  imports:
    - teracy
    - teracy.html5boilerplate
    - teracy.html5boilerplate.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/teracy-official/django-html5-boilerplate
  license: BSD License
  summary: 'html5-boilerplate Django wrapper application'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
