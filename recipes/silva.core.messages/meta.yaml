package:
  name: silva.core.messages
  version: "2.3"

source:
  fn: silva.core.messages-2.3.tar.gz
  url: https://pypi.python.org/packages/source/s/silva.core.messages/silva.core.messages-2.3.tar.gz
  md5: 8daa9a4456144a57ae2cdc8be0b6a937
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - silva.core.messages = silva.core.messages:main
    #
    # Would create an entry point called silva.core.messages that calls silva.core.messages.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - five.grok
    - silva.core.cache
    - zope.interface

  run:
    - python
    - five.grok
    - silva.core.cache
    - zope.interface

test:
  # Python imports
  imports:
    - silva
    - silva.core
    - silva.core.messages
    - silva.core.messages.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/silvacms/silva.core.messages
  license: BSD License
  summary: 'User feedback mechanism for Silva CMS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
