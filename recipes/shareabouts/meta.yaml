package:
  name: shareabouts
  version: "0.1.0"

source:
  fn: shareabouts-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/s/shareabouts/shareabouts-0.1.0.tar.gz
  md5: a48f4c007f9bb7ad763aed3fe0b93590
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - shareabouts = shareabouts:main
    #
    # Would create an entry point called shareabouts that calls shareabouts.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=1.2

  run:
    - python
    - requests >=1.2

test:
  # Python imports
  imports:
    - shareabouts

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/openplans/pyshareabouts
  license: BSD
  summary: 'A Python adapter for the Shareabouts API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
