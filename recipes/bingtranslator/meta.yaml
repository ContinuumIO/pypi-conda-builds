package:
  name: bingtranslator
  version: "0.1"

source:
  fn: BingTranslator-0.1.tar.gz
  url: https://pypi.python.org/packages/source/B/BingTranslator/BingTranslator-0.1.tar.gz
  md5: 1a63bd3e5039ef7bbc4fd0ae35d8cc84
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bingtranslator = bingtranslator:main
    #
    # Would create an entry point called bingtranslator that calls bingtranslator.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - BingTranslator
    - bingtranslator

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dookgulliver/BingTranslator
  license: GNU Lesser General Public License v3 (LGPLv3)
  summary: 'Micrososft Translator API V2 for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
