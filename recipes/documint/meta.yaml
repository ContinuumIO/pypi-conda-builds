package:
  name: documint
  version: "15.5.0"

source:
  fn: Documint-15.5.0.tar.gz
  url: https://pypi.python.org/packages/source/D/Documint/Documint-15.5.0.tar.gz
  md5: 77fe462ad2830270860ba0e04b5d9462
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - documint = documint:main
    #
    # Would create an entry point called documint that calls documint.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - twisted
    - lxml

  run:
    - python
    - twisted
    - lxml

test:
  # Python imports
  imports:
    - documint
    - documint.extproc
    - documint.test
    - twisted.plugins

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/fusionapp/documint
  license: UNKNOWN
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
