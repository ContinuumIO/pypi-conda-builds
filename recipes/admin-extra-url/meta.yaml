package:
  name: admin-extra-url
  version: "0.1.dev20141008144353"

source:
  fn: admin-extra-url-0.1.dev20141008144353.tar.gz
  url: https://pypi.python.org/packages/source/a/admin-extra-url/admin-extra-url-0.1.dev20141008144353.tar.gz
  md5: 9b7f79411ce31a618a0edb1ce4dd6cdc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - admin-extra-url = admin-extra-url:main
    #
    # Would create an entry point called admin-extra-url that calls admin-extra-url.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - admin_extra_urls
    - admin_extra_urls.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'Django mixin to easily add urls to any ModelAdmin'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
