package:
  name: anthill.querytool
  version: "0.2"

source:
  fn: anthill.querytool-0.2.tar.gz
  url: https://pypi.python.org/packages/source/a/anthill.querytool/anthill.querytool-0.2.tar.gz
  md5: 189e899d1123f4ba9c7fb512acbe0f61
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - anthill.querytool = anthill.querytool:main
    #
    # Would create an entry point called anthill.querytool that calls anthill.querytool.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - anthill
    - anthill.querytool
    - anthill.querytool.exportimport
    - anthill.querytool.exportimport.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.banality.de/
  license: GPL
  summary: 'GUI for AdvancedQuery with some extensions - searching the easy way for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
