package:
  name: redisrpc
  version: "0.3.4"

source:
  fn: redisrpc-python-0.3.4.tar.gz
  url: https://github.com/downloads/nfarring/redisrpc/redisrpc-python-0.3.4.tar.gz
  #md5: 
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - redisrpc = redisrpc:main
    #
    # Would create an entry point called redisrpc that calls redisrpc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - distribute
    - redis

  run:
    - python
    - distribute
    - redis

test:
  # Python imports
  imports:
    - redisrpc

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/nfarring/redisrpc
  license: GNU General Public License (GPL)
  summary: 'Lightweight RPC using Redis'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
