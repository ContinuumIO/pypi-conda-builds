package:
  name: cubicweb-sioc
  version: "0.1.0"

source:
  fn: cubicweb-sioc-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/c/cubicweb-sioc/cubicweb-sioc-0.1.0.tar.gz
  md5: 8d4f40cb0265fb65995f5376222b433f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cubicweb-sioc = cubicweb-sioc:main
    #
    # Would create an entry point called cubicweb-sioc that calls cubicweb-sioc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cubicweb >=3.17.0

  run:
    - python
    - cubicweb >=3.17.0

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.cubicweb.org/project/cubicweb-sioc
  license: LGPL
  summary: 'Specific views for SIOC (Semantically-Interlinked Online Communities)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
