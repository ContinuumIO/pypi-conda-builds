package:
  name: redirectsplugin
  version: "0.1.2"

source:
  fn: RedirectsPlugin-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/R/RedirectsPlugin/RedirectsPlugin-0.1.2.tar.gz
  md5: 006108611c086cc24a20a11b7f752ec5
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - redirectsplugin = redirectsplugin:main
    #
    # Would create an entry point called redirectsplugin that calls redirectsplugin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - redirects
    - redirectsplugin

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://mitar.tnode.com/
  license: AGPLv3
  summary: 'Define server-side redirects in Trac.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
