package:
  name: django-counter
  version: "0.1.6"

source:
  fn: django-counter-0.1.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-counter/django-counter-0.1.6.tar.gz
  md5: c44948c94ed897b59035a06217613df8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-counter = django-counter:main
    #
    # Would create an entry point called django-counter that calls django-counter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pil

  run:
    - python
    - pil

test:
  # Python imports
  imports:
    - django_counter
    - django_counter.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/svetlyak40wt/django-counter/
  license: BSD License
  summary: 'This is a general purpose page views / redirects counter for django projects.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
