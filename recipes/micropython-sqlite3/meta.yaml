package:
  name: micropython-sqlite3
  version: "0.2.1"

source:
  fn: micropython-sqlite3-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/m/micropython-sqlite3/micropython-sqlite3-0.2.1.tar.gz
  md5: e4fa50b3346634f3cec41347cf23cc17
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - micropython-sqlite3 = micropython-sqlite3:main
    #
    # Would create an entry point called micropython-sqlite3 that calls micropython-sqlite3.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/micropython/micropython/issues/405
  license: MIT
  summary: 'sqlite3 module for MicroPython'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
