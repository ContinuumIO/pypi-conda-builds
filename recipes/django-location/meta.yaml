package:
  name: django-location
  version: "2.1.1"

source:
  fn: django-location-2.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-location/django-location-2.1.1.tar.gz
  md5: 207fc90bc2952bed621fa59196077f7d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-location = django-location:main
    #
    # Would create an entry point called django-location that calls django-location.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4
    - django-social-auth >=0.7.28
    - jsonfield >=0.9.18
    - lxml >=3.0.1
    - pytz
    - requests >=1.2.3
    - pyicloud_dwoh >=0.4.1
    - django-mailbox >=3.0.1
    - pillow >=1.7.7

  run:
    - python
    - django >=1.4
    - django-social-auth >=0.7.28
    - jsonfield >=0.9.18
    - lxml >=3.0.1
    - pytz
    - requests >=1.2.3
    - pyicloud_dwoh >=0.4.1
    - django-mailbox >=3.0.1
    - pillow >=1.7.7

test:
  # Python imports
  imports:
    - location
    - location.consumers
    - location.management
    - location.management.commands
    - location.migrations
    - location.templatetags
    - location.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/latestrevision/django-location/
  license: MIT License
  summary: 'Gather, store, and display real-time location information from Foursquare, iCloud, and more.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
