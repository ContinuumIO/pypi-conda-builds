package:
  name: djformtags
  version: "1.0"

source:
  fn: djformtags-1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/djformtags/djformtags-1.0.tar.gz
  md5: 3cb8afff98e7bef878c99c541aedd86b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djformtags = djformtags:main
    #
    # Would create an entry point called djformtags that calls djformtags.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - djformtags
    - djformtags.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/prestontimmons/djformtags
  license: UNKNOWN
  summary: 'Simplifies and enhance form rendering in Django templates.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
