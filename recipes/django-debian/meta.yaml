package:
  name: django-debian
  version: "0.10.2"

source:
  fn: django-debian-0.10.2.zip
  url: https://pypi.python.org/packages/source/d/django-debian/django-debian-0.10.2.zip
  md5: 1c57e60fe9530d16f2185a019c0e0fd7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-debian = django-debian:main
    #
    # Would create an entry point called django-debian that calls django-debian.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-seatbelt >=1.0

  run:
    - python
    - django-seatbelt >=1.0

test:
  # Python imports
  imports:
    - django_debian

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://launchpad.net/django-debian
  license: LGPL3
  summary: 'Debian integration for Django applications'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
