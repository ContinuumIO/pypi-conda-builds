package:
  name: django-eve-proxy
  version: "0.5"

source:
  fn: django-eve-proxy-0.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-eve-proxy/django-eve-proxy-0.5.tar.gz
  md5: 49ec81f541586a23f32108e6aa5accb0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-eve-proxy = django-eve-proxy:main
    #
    # Would create an entry point called django-eve-proxy that calls django-eve-proxy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - eve_proxy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/django-eve-proxy/
  license: GNU General Public License (GPL)
  summary: 'A Django-based EVE API proxy.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
