package:
  name: genscrapeclient
  version: "0.2.2"

source:
  fn: genscrapeclient-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/g/genscrapeclient/genscrapeclient-0.2.2.tar.gz
  md5: 9b0859f8c37f771deb3dd7c1e0612387
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - genscrapeclient = genscrapeclient:main
    #
    # Would create an entry point called genscrapeclient that calls genscrapeclient.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests-oauthlib ==0.4.0

  run:
    - python
    - requests-oauthlib ==0.4.0

test:
  # Python imports
  imports:
    - genscrapeclient

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://kodeplay.com
  license: MIT License
  summary: 'Client API library for the Genscrape API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
