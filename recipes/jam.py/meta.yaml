package:
  name: jam.py
  version: "1.0.7"

source:
  fn: jam.py-1.0.7.tar.gz
  url: https://pypi.python.org/packages/source/j/jam.py/jam.py-1.0.7.tar.gz
  md5: e02e90260a2caec937a1bfb8306bb7d8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jam.py = jam.py:main
    #
    # Would create an entry point called jam.py that calls jam.py.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - jam
    - jam.lang
    - jam.third_party
    - jam.third_party.web
    - jam.third_party.web.contrib
    - jam.third_party.web.wsgiserver

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://jam-py.com/
  license: BSD License
  summary: 'Jam.py is the fastest way to create a web/desktop database application.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
