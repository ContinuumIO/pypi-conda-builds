package:
  name: pytils
  version: "0.3"

source:
  fn: pytils-0.3.tar.gz
  url: https://pypi.python.org/packages/source/p/pytils/pytils-0.3.tar.gz
  md5: 17ebe255393fb738399601cc1a490549
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pytils = pytils:main
    #
    # Would create an entry point called pytils that calls pytils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - pytils
    - pytils.templatetags
    - pytils.test
    - pytils.test.templatetags
    - pytils.third

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/j2a/pytils/
  license: MIT License
  summary: 'Russian-specific string utils'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
