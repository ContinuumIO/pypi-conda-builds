package:
  name: tornado_bufferx_improved
  version: "2.4.1"

source:
  fn: tornado_bufferx_improved-2.4.1.tar.gz
  url: https://pypi.python.org/packages/source/t/tornado_bufferx_improved/tornado_bufferx_improved-2.4.1.tar.gz
  md5: b82b1f503f178ac90b676347c359a3e0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tornado_bufferx_improved = tornado_bufferx_improved:main
    #
    # Would create an entry point called tornado_bufferx_improved that calls tornado_bufferx_improved.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - tornado
    - tornado.platform
    - tornado.test
    - tornado_bufferx_improved

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.tornadoweb.org/
  license: http://www.apache.org/licenses/LICENSE-2.0
  summary: 'Tornado is an open source version of the scalable, non-blocking web server and and tools that power FriendFeed'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
