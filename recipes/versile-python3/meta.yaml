package:
  name: versile-python3
  version: "0.8.1-beta"

source:
  fn: versile-python3-0.8.1-beta.tar.gz
  url: https://pypi.python.org/packages/source/v/versile-python3/versile-python3-0.8.1-beta.tar.gz
  md5: 71eb5f433980304411f467bf2a3d4d1a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - versile-python3 = versile-python3:main
    #
    # Would create an entry point called versile-python3 that calls versile-python3.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - versile
    - versile.common
    - versile.crypto
    - versile.crypto.algorithm
    - versile.crypto.x509
    - versile.crypto.x509.asn1def
    - versile.manager
    - versile.manager.dispatch
    - versile.orb
    - versile.reactor
    - versile.reactor.io
    - versile.temp
    - versile.vse
    - versile.vse.native

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://versile.com/
  license: GNU Affero General Public License v3
  summary: 'Versile Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
