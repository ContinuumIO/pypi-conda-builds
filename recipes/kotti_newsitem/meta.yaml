package:
  name: kotti_newsitem
  version: "0.2"

source:
  fn: kotti_newsitem-0.2.tar.gz
  url: https://pypi.python.org/packages/source/k/kotti_newsitem/kotti_newsitem-0.2.tar.gz
  md5: 4d15a38e353e1b8a8e407671ad9a78fa
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kotti_newsitem = kotti_newsitem:main
    #
    # Would create an entry point called kotti_newsitem that calls kotti_newsitem.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - kotti

  run:
    - python
    - setuptools
    - kotti

test:
  # Python imports
  imports:
    - kotti_newsitem

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kotti/kotti_newsitem
  license: BSD License
  summary: 'News Item content type for Kotti'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
