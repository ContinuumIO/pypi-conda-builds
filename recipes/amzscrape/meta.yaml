package:
  name: amzscrape
  version: "0.9.0"

source:
  fn: AmzScrape-0.9.0.zip
  url: https://pypi.python.org/packages/source/A/AmzScrape/AmzScrape-0.9.0.zip
  md5: da6a02a7057d16fe1c0467e89315d9ec
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - amzscrape = amzscrape:main
    #
    # Would create an entry point called amzscrape that calls amzscrape.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - amzscrape

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/asweigart/amzscrape
  license: BSD License
  summary: 'Built on top of Selenium to scrape Amazon product pages without using their API.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
