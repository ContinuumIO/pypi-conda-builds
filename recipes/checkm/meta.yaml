package:
  name: checkm
  version: "0.4"

source:
  fn: Checkm-0.4.tar.gz
  url: https://pypi.python.org/packages/source/C/Checkm/Checkm-0.4.tar.gz
  md5: 3aa19ae069f201c8165127f06fafab7e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - checkm = checkm:main
    #
    # Would create an entry point called checkm that calls checkm.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - checkm

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://packages.python.org/Checkm/
  license: MIT
  summary: 'Checkm - create reports on the checksums of sets of files or URLs resources.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
