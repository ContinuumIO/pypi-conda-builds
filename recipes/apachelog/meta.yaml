package:
  name: apachelog
  version: "1.0"

source:
  fn: apachelog-1.0.tar.gz
  url: https://pypi.python.org/packages/source/a/apachelog/apachelog-1.0.tar.gz
  md5: ac9daa09cd8ef8e6de2ca915482b1f40
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - apachelog = apachelog:main
    #
    # Would create an entry point called apachelog that calls apachelog.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - apachelog

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/apachelog
  license: Artistic License or GNU General Public License (GPL)
  summary: "Access log parser in python, ported from Peter Hickman's Apache::LogRegex Perl moduleUniversal."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
