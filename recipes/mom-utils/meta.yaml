package:
  name: mom-utils
  version: "1.2.6"

source:
  fn: mom-utils-1.2.6.tar.gz
  url: https://pypi.python.org/packages/source/m/mom-utils/mom-utils-1.2.6.tar.gz
  md5: 83e7b7347e7c7506b90cbc28b3e4bdbb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mom-utils = mom-utils:main
    #
    # Would create an entry point called mom-utils that calls mom-utils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - coards
    - numpy
    - pyyaml
    - netcdf4
    - dateutils

  run:
    - python
    - coards
    - numpy
    - pyyaml
    - netcdf4
    - dateutils

test:
  # Python imports
  imports:
    - mom_utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/castelao/mom-utils
  license: Python Software Foundation License
  summary: "Python utilities for the GFDL's numerical model MOM"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
