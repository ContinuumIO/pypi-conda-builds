package:
  name: kotti_paster
  version: "0.2"

source:
  fn: kotti_paster-0.2.zip
  url: https://pypi.python.org/packages/source/k/kotti_paster/kotti_paster-0.2.zip
  md5: 1328c22c89fc774cc03818c43d4de7af
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kotti_paster = kotti_paster:main
    #
    # Would create an entry point called kotti_paster that calls kotti_paster.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - pastescript
    - cheetah <=2.2.1
    - templer.core

  run:
    - python
    - setuptools
    - setuptools
    - pastescript
    - cheetah <=2.2.1
    - templer.core

test:
  # Python imports
  imports:
    - kotti_paster
    - kotti_paster.paster_templates

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/kotti_paster/
  license: BSD-derived (http://www.repoze.org/LICENSE.txt)
  summary: 'Paster AddOn Template for Kotti'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
