package:
  name: django-explain-commands
  version: "0.2"

source:
  fn: django-explain-commands-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-explain-commands/django-explain-commands-0.2.tar.gz
  md5: 4bdc685429e81149907effd01c2d73c5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-explain-commands = django-explain-commands:main
    #
    # Would create an entry point called django-explain-commands that calls django-explain-commands.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - explain_commands
    - explain_commands.management
    - explain_commands.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jellycrystal/django-explain-commands
  license: BSD License
  summary: 'Django app for explaining where commands are coming from.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
