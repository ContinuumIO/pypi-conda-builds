package:
  name: dawg-python
  version: "0.7.2"

source:
  fn: DAWG-Python-0.7.2.tar.gz
  url: https://pypi.python.org/packages/source/D/DAWG-Python/DAWG-Python-0.7.2.tar.gz
  md5: 25835be7d559de75dde5e28a254427ad
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dawg-python = dawg-python:main
    #
    # Would create an entry point called dawg-python that calls dawg-python.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - dawg_python

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kmike/DAWG-Python/
  license: MIT License
  summary: 'Pure-python reader for DAWGs (DAFSAs) created by dawgdic C++ library or DAWG Python extension.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
