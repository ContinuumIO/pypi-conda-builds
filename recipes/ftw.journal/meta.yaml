package:
  name: ftw.journal
  version: "1.2.8"

source:
  fn: ftw.journal-1.2.8.zip
  url: https://pypi.python.org/packages/source/f/ftw.journal/ftw.journal-1.2.8.zip
  md5: 5ad119ef8772a0a2d3a3e8a45ec68c5a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ftw.journal = ftw.journal:main
    #
    # Would create an entry point called ftw.journal that calls ftw.journal.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - acquisition
    - zope.annotation
    - zope.component
    - zope.event
    - zope.formlib
    - zope.i18nmessageid
    - zope.interface
    - zope.schema
    - zodb3
    - zope2
    - products.cmfcore
    - plone.app.contentrules
    - plone.contentrules

  run:
    - python
    - setuptools
    - setuptools
    - acquisition
    - zope.annotation
    - zope.component
    - zope.event
    - zope.formlib
    - zope.i18nmessageid
    - zope.interface
    - zope.schema
    - zodb3
    - zope2
    - products.cmfcore
    - plone.app.contentrules
    - plone.contentrules

test:
  # Python imports
  imports:
    - ftw
    - ftw.journal
    - ftw.journal.adapters
    - ftw.journal.events
    - ftw.journal.rules
    - ftw.journal.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/4teamwork/ftw.journal
  license: GPL2
  summary: 'Journaling infrastructure for plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
