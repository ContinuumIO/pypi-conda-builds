package:
  name: django-base64field
  version: "1.0"

source:
  fn: django-base64field-1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-base64field/django-base64field-1.0.tar.gz
  md5: 967ded849e81ce29cf0a6d47cdfd0a30
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-base64field = django-base64field:main
    #
    # Would create an entry point called django-base64field that calls django-base64field.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - django_base64field

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Alir3z4/django-base64field
  license: django-baseboard
  summary: 'A motherfucking django model field to bring base64 encoded key to models.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
