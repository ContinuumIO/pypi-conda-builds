package:
  name: vxsandbox
  version: "0.5.1"

source:
  fn: vxsandbox-0.5.1.tar.gz
  url: https://pypi.python.org/packages/source/v/vxsandbox/vxsandbox-0.5.1.tar.gz
  md5: 3d9f0d53452ac96bc0fbe820215368ae
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - vxsandbox = vxsandbox:main
    #
    # Would create an entry point called vxsandbox that calls vxsandbox.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - twisted >=13.1.0
    - vumi >=0.5

  run:
    - python
    - twisted >=13.1.0
    - vumi >=0.5

test:
  # Python imports
  imports:
    - vxsandbox
    - vxsandbox.resources
    - vxsandbox.resources.tests
    - vxsandbox.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/praekelt/vumi-sandbox
  license: BSD License
  summary: 'A sandbox application worker for Vumi.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
