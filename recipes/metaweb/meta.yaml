package:
  name: metaweb
  version: "1.4"

source:
  fn: metaweb-1.4.tar.gz
  url: https://pypi.python.org/packages/source/m/metaweb/metaweb-1.4.tar.gz
  md5: fb41c65f83b41b275d45759b2d20a792
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - metaweb = metaweb:main
    #
    # Would create an entry point called metaweb that calls metaweb.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - decorated
    - inflection
    - loggingd

  run:
    - python
    - decorated
    - inflection
    - loggingd

test:
  # Python imports
  imports:
    - metaweb
    - metaweb.impls

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/CooledCoffee/metaweb/
  license: UNKNOWN
  summary: 'A web framework based on other web frameworks.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
