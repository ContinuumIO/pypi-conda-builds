package:
  name: django-cloudmailin
  version: "0.1.1"

source:
  fn: django-cloudmailin-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-cloudmailin/django-cloudmailin-0.1.1.tar.gz
  md5: e4268392834bf1825a86f41523a9b7f0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-cloudmailin = django-cloudmailin:main
    #
    # Would create an entry point called django-cloudmailin that calls django-cloudmailin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - cloudmailin
    - cloudmailin.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/sunlightlabs/django-cloudmailin/
  license: BSD License
  summary: 'Client for CloudMailin incoming email service'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
