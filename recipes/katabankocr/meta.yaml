package:
  name: katabankocr
  version: "0.8.0"

source:
  fn: KataBankOCR-0.8.0.tar.gz
  url: https://pypi.python.org/packages/source/K/KataBankOCR/KataBankOCR-0.8.0.tar.gz
  md5: 077fbbce7a81e30d702e812da044705d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - katabankocr = katabankocr:main
    #
    # Would create an entry point called katabankocr that calls katabankocr.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - docopt >=0.6.1
    - toolz >=0.5.2

  run:
    - python
    - docopt >=0.6.1
    - toolz >=0.5.2

test:
  # Python imports
  imports:
    - katabankocr
    - parse
    - parse.generators

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/gJigsaw/KataBankOCR
  license: GNU General Public License (GPL)
  summary: 'Programming kata to parse output from fictitious OCR machine'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
