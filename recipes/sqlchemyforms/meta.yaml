package:
  name: sqlchemyforms
  version: "1.0"

source:
  fn: Sqlchemyforms-1.0.tar.gz
  url: https://pypi.python.org/packages/source/S/Sqlchemyforms/Sqlchemyforms-1.0.tar.gz
  md5: 984ce4ecc100e5862ac3ead81d2ee1db
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sqlchemyforms = sqlchemyforms:main
    #
    # Would create an entry point called sqlchemyforms that calls sqlchemyforms.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sqlalchemy >=0.9.8

  run:
    - python
    - sqlalchemy >=0.9.8

test:
  # Python imports
  imports:
    - sqlchemyforms
    - sqlchemyforms.table
    - sqlchemyforms.validators

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/voidpp/sqlchemyforms
  license: MIT
  summary: 'Generate form objects for create HTML forms based on SQLAlchhemy models'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
