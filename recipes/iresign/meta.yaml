package:
  name: iresign
  version: "0.2.2"

source:
  fn: iResign-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/i/iResign/iResign-0.2.2.tar.gz
  md5: af8f11f14ccf0a8c86d544d49098c0be
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - iresign = iresign:main
    #
    # Would create an entry point called iresign that calls iresign.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - iresign

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ikalnitsky/iResign
  license: BSD License
  summary: 'A tool for recodesigning iOS applications.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
