package:
  name: aylien-apiclient
  version: "0.3.0"

source:
  fn: aylien-apiclient-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/a/aylien-apiclient/aylien-apiclient-0.3.0.tar.gz
  md5: f3578104f9106dbe9fa4b3037bc2ecf5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - aylien-apiclient = aylien-apiclient:main
    #
    # Would create an entry point called aylien-apiclient that calls aylien-apiclient.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - httplib2 >=0.9

  run:
    - python
    - httplib2 >=0.9

test:
  # Python imports
  imports:
    - aylienapiclient

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/AYLIEN/aylien_textapi_python
  license: Apache Software License
  summary: 'AYLIEN Text API Client Library for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
