package:
  name: awsnapshots
  version: "0.1.2"

source:
  fn: awsnapshots-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/a/awsnapshots/awsnapshots-0.1.2.tar.gz
  md5: dc6ff25ff40112477e5cb1a71c336ec0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - awsnapshots = awsnapshots:main
    #
    # Would create an entry point called awsnapshots that calls awsnapshots.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - boto
    - pyyaml

  run:
    - python
    - boto
    - pyyaml

test:
  # Python imports
  imports:
    - awsnapshots

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/jrhames/AWSnapshots
  license: MIT
  summary: 'Simple tool to manage AWS Volume Snapshots using the AWS API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
