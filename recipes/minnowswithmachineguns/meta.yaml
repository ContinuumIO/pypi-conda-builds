package:
  name: minnowswithmachineguns
  version: "0.0.2"

source:
  fn: minnowswithmachineguns-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/m/minnowswithmachineguns/minnowswithmachineguns-0.0.2.tar.gz
  md5: fc31729064cfa3a2f542987fc3e4db6f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - minnowswithmachineguns = minnowswithmachineguns:main
    #
    # Would create an entry point called minnowswithmachineguns that calls minnowswithmachineguns.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - dop ==0.1.4
    - paramiko ==1.10.1

  run:
    - python
    - dop ==0.1.4
    - paramiko ==1.10.1

test:
  # Python imports
  imports:
    - minnowswithmachineguns

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rozap/minnowswithmachineguns
  license: MIT License
  summary: 'A utility for arming (creating) many minnows (digital ocean instances) to attack (load test) targets (web applications). Based on beeswithmachineguns'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
