package:
  name: hgexternals
  version: "0.2"

source:
  fn: hgexternals-0.2.tar.gz
  url: https://pypi.python.org/packages/source/h/hgexternals/hgexternals-0.2.tar.gz
  md5: 832c5224f4923197704bb0b312b899b5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hgexternals = hgexternals:main
    #
    # Would create an entry point called hgexternals that calls hgexternals.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - hgexternals

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/tarek/hgexternals
  license: BSD
  summary: 'Mercurial extension to simulate Subversion externals'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
