package:
  name: django-ratelimit9
  version: "0.1"

source:
  fn: django-ratelimit9-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-ratelimit9/django-ratelimit9-0.1.tar.gz
  md5: 47286f256de549f39bd2a8ae6b04a812
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-ratelimit9 = django-ratelimit9:main
    #
    # Would create an entry point called django-ratelimit9 that calls django-ratelimit9.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6.5
    - django-ratelimit >=0.5.0
    - django-recaptcha >=1.0.2

  run:
    - python
    - django >=1.6.5
    - django-ratelimit >=0.5.0
    - django-recaptcha >=1.0.2

test:
  # Python imports
  imports:
    - ratelimit9

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/9dev/django-ratelimit9
  license: MIT License
  summary: 'App that dynamically adds reCAPTCHA field to forms when user exceeds the rate limit'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
