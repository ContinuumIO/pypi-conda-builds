package:
  name: pyescpos
  version: "0.0.4"

source:
  fn: PyESCPOS-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/P/PyESCPOS/PyESCPOS-0.0.4.tar.gz
  md5: 4dedbf26c5f9a4ef82d270bb417a9b3f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pyescpos = pyescpos:main
    #
    # Would create an entry point called pyescpos that calls pyescpos.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyserial >=2.7

  run:
    - python
    - pyserial >=2.7

test:
  # Python imports
  imports:
    - escpos
    - escpos.impl
    - pyescpos

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/base4sistemas/pyescpos/
  license: Apache Software License
  summary: 'Support for Epson ESC/POS printer command system.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
