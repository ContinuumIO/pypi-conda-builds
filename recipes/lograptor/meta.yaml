package:
  name: lograptor
  version: "1.0"

source:
  fn: lograptor-1.0.tar.gz
  url: https://pypi.python.org/packages/source/l/lograptor/lograptor-1.0.tar.gz
  md5: f6b2f64b6969a190cd96d15b3d0973fe
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lograptor = lograptor:main
    #
    # Would create an entry point called lograptor that calls lograptor.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - lograptor
    - lograptor.backports

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/brunato/Lograptor
  license: GNU General Public License v2 or later (GPLv2+)
  summary: 'Command-line utility for searching into log files. Produces matching outputs and reports.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
