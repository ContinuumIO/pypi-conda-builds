package:
  name: sqlalchemy-fixture-factory
  version: "0.1.0"

source:
  fn: SQLAlchemy-Fixture-Factory-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/S/SQLAlchemy-Fixture-Factory/SQLAlchemy-Fixture-Factory-0.1.0.tar.gz
  md5: 75c76defc59c2f9424a7e17eea4a0818
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sqlalchemy-fixture-factory = sqlalchemy-fixture-factory:main
    #
    # Would create an entry point called sqlalchemy-fixture-factory that calls sqlalchemy-fixture-factory.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sqlalchemy >=0.7

  run:
    - python
    - sqlalchemy >=0.7

test:
  # Python imports
  imports:
    - sqlalchemy_fixture_factory

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mmmichl/sqlalchemy-fixture-factory
  license: MIT License
  summary: u"Test Fixture Factory for SQLAlchemy. Inspired by Ruby's factory_girl"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
