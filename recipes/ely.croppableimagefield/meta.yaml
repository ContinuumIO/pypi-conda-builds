package:
  name: ely.croppableimagefield
  version: "1.0b1"

source:
  fn: ely.croppableimagefield-1.0b1.tar.gz
  url: https://pypi.python.org/packages/source/e/ely.croppableimagefield/ely.croppableimagefield-1.0b1.tar.gz
  md5: 9f0dc802535636ea5455732e59eabfcf
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ely.croppableimagefield = ely.croppableimagefield:main
    #
    # Would create an entry point called ely.croppableimagefield that calls ely.croppableimagefield.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - ely
    - ely.croppableimagefield
    - ely.croppableimagefield.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/ely.croppableimagefield
  license: GNU General Public License (GPL)
  summary: 'CroppableImageField is a drop-in replacement for the Archetype field ImageField'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
