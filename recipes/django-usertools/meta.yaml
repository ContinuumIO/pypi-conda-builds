package:
  name: django-usertools
  version: "1.0.10"

source:
  fn: django-usertools-1.0.10.tar.gz
  url: https://pypi.python.org/packages/source/d/django-usertools/django-usertools-1.0.10.tar.gz
  md5: 70cd209e9eb745edf9c373496ee8fca8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-usertools = django-usertools:main
    #
    # Would create an entry point called django-usertools that calls django-usertools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - usertools
    - usertools.management
    - usertools.management.commands
    - usertools.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/etianen/django-usertools
  license: BSD License
  summary: 'A fire-and-forget enhancement to the Django user admin.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
