package:
  name: django-googleanalytics
  version: "1.0"

source:
  fn: django-googleanalytics-1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-googleanalytics/django-googleanalytics-1.0.tar.gz
  md5: 448f4fbad6ade0101400708a0a1dbc20
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-googleanalytics = django-googleanalytics:main
    #
    # Would create an entry point called django-googleanalytics that calls django-googleanalytics.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - google_analytics
    - google_analytics.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/django-googleanalytics/
  license: BSD License
  summary: 'Google Analytics templatetag for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
