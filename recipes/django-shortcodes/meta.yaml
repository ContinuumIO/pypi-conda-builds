package:
  name: django-shortcodes
  version: "0.3.0"

source:
  fn: django-shortcodes-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-shortcodes/django-shortcodes-0.3.0.tar.gz
  md5: 2cae5b2db12798806adb6bd564133bb4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-shortcodes = django-shortcodes:main
    #
    # Would create an entry point called django-shortcodes that calls django-shortcodes.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - shortcodes
    - shortcodes.parsers
    - shortcodes.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/martey/django-shortcodes
  license: MIT License
  summary: 'WordPress shortcode support for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
