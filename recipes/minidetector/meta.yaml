package:
  name: minidetector
  version: "0.1"

source:
  fn: Minidetector-0.1.tar.gz
  url: https://pypi.python.org/packages/source/M/Minidetector/Minidetector-0.1.tar.gz
  md5: b3e01c07ed59a28d9dba7b5ee8d9b68b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - minidetector = minidetector:main
    #
    # Would create an entry point called minidetector that calls minidetector.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - minidetector

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD License
  summary: 'Middleware application that detects mobile browsers'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
