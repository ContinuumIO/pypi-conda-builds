package:
  name: python-libmagic
  version: "0.3.0"

source:
  fn: python-libmagic-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/p/python-libmagic/python-libmagic-0.3.0.tar.gz
  md5: d14527859952cd7951caf393566b03fd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-libmagic = python-libmagic:main
    #
    # Would create an entry point called python-libmagic that calls python-libmagic.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - cffi ==0.8

  run:
    - python
    - cffi ==0.8

test:
  # Python imports
  imports:
    - magic

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tyrannosaurus/python-libmagic
  license: MIT
  summary: 'Python bindings to libmagic'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
