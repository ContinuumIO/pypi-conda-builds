package:
  name: fluid-nexus
  version: "0.3.1"

source:
  fn: fluid-nexus-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/f/fluid-nexus/fluid-nexus-0.3.1.tar.gz
  md5: b0d5b7999d23060d8c188a261d2faa02
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fluid-nexus = fluid-nexus:main
    #
    # Would create an entry point called fluid-nexus that calls fluid-nexus.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - textile
    - simplejson
    - pybonjour
    - sqlalchemy
    - oauth2

  run:
    - python
    - textile
    - simplejson
    - pybonjour
    - sqlalchemy
    - oauth2

test:
  # Python imports
  imports:
    - FluidNexus
    - FluidNexus.ui

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://fluidnexus.net
  license: GNU General Public License (GPL)
  summary: 'PyQt4 application that enables one to share messages and data independent of centralized data networks'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
