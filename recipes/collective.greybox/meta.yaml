package:
  name: collective.greybox
  version: "0.4"

source:
  fn: collective.greybox-0.4.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.greybox/collective.greybox-0.4.tar.gz
  md5: 5748d2e99a58af7769f2202e3ed7c2cd
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.greybox = collective.greybox:main
    #
    # Would create an entry point called collective.greybox that calls collective.greybox.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.greybox
    - collective.greybox.browser
    - collective.greybox.resources

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.4teamwork.ch
  license: GPL
  summary: 'simple folder view using greybox'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
