package:
  name: django-kerberos
  version: "1.2.1"

source:
  fn: django-kerberos-1.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-kerberos/django-kerberos-1.2.1.tar.gz
  md5: e7c2a21f7eea544d9609a2d08b7c35b8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-kerberos = django-kerberos:main
    #
    # Would create an entry point called django-kerberos that calls django-kerberos.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >1.5
    - kerberos

  run:
    - python
    - django >1.5
    - kerberos

test:
  # Python imports
  imports:
    - django_kerberos

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://dev.entrouvert.org/projects/authentic/
  license: AGPLv3 or later
  summary: 'Kerberos authentication for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
