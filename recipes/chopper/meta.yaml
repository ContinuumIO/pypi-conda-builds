package:
  name: chopper
  version: "0.4.6"

source:
  fn: chopper-0.4.6.tar.gz
  url: https://pypi.python.org/packages/source/c/chopper/chopper-0.4.6.tar.gz
  md5: a7ae983bbb1de044528a2673d4bac854
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - chopper = chopper:main
    #
    # Would create an entry point called chopper that calls chopper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cssselect ==0.9.1
    - tinycss ==0.3
    - lxml ==3.3.5
    - six ==1.7.3

  run:
    - python
    - cssselect ==0.9.1
    - tinycss ==0.3
    - lxml ==3.3.5
    - six ==1.7.3

test:
  # Python imports
  imports:
    - chopper
    - chopper.css
    - chopper.html

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jurismarches/chopper
  license: MIT License
  summary: 'Lib to extract html elements by preserving ancestors and cleaning CSS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
