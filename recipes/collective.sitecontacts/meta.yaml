package:
  name: collective.sitecontacts
  version: "1.0"

source:
  fn: collective.sitecontacts-1.0.zip
  url: https://pypi.python.org/packages/source/c/collective.sitecontacts/collective.sitecontacts-1.0.zip
  md5: 640360b0007049133d2e545ce982de4f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.sitecontacts = collective.sitecontacts:main
    #
    # Would create an entry point called collective.sitecontacts that calls collective.sitecontacts.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.sitecontacts

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/
  license: GPL
  summary: 'Simple contacts folder for your website based on Plone.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
