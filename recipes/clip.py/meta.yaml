package:
  name: clip.py
  version: "0.3.0"

source:
  fn: clip.py-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/c/clip.py/clip.py-0.3.0.tar.gz
  md5: 8439f7e9cc970462c44a447d73b1536f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - clip.py = clip.py:main
    #
    # Would create an entry point called clip.py that calls clip.py.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/willyg302/clip.py
  license: MIT License
  summary: 'Embeddable, composable command line interface parsing'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
