package:
  name: astunparse
  version: "1.2.2"

source:
  fn: astunparse-1.2.2.tar.gz
  url: https://pypi.python.org/packages/source/a/astunparse/astunparse-1.2.2.tar.gz
  md5: 55169bfc61c70b40cf0e85382872b40e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - astunparse = astunparse:main
    #
    # Would create an entry point called astunparse that calls astunparse.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - wheel >=0.23.0,<1.0
    - six >=1.6.1,<2.0

  run:
    - python
    - wheel >=0.23.0,<1.0
    - six >=1.6.1,<2.0

test:
  # Python imports
  imports:
    - astunparse

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/simonpercivall/astunparse
  license: BSD License
  summary: 'An AST unparser for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
