package:
  name: backports.pbkdf2
  version: "0.1"

source:
  fn: backports.pbkdf2-0.1.tar.gz
  url: https://pypi.python.org/packages/source/b/backports.pbkdf2/backports.pbkdf2-0.1.tar.gz
  md5: a818df09939f1fffacd0b04ab381b3a8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - backports.pbkdf2 = backports.pbkdf2:main
    #
    # Would create an entry point called backports.pbkdf2 that calls backports.pbkdf2.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - backports.pbkdf2

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/tiran/backports.pbkdf2
  license: Python Software Foundation License
  summary: 'Fast PBKDF2 for Python 2.6 - 3.4'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
