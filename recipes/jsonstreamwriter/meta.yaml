package:
  name: jsonstreamwriter
  version: "0.1"

source:
  fn: JSONStreamWriter-0.1.tar.gz
  url: https://pypi.python.org/packages/source/J/JSONStreamWriter/JSONStreamWriter-0.1.tar.gz
  md5: 02c230bdb60c0cf2d62e3987d05df24e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jsonstreamwriter = jsonstreamwriter:main
    #
    # Would create an entry point called jsonstreamwriter that calls jsonstreamwriter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - JSONStreamWriter
    - jsonstreamwriter

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'stream json-serializable python objects to an array in a file'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
