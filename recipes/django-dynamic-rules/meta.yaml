package:
  name: django-dynamic-rules
  version: "1.0.1"

source:
  fn: django-dynamic-rules-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-dynamic-rules/django-dynamic-rules-1.0.1.tar.gz
  md5: 9aebe38cfcf08bc536069c460604a4d8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-dynamic-rules = django-dynamic-rules:main
    #
    # Would create an entry point called django-dynamic-rules that calls django-dynamic-rules.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django <1.5.0
    - django-admin-ext
    - django-autoload
    - django-fields
    - django-class-registry >=0.0.3
    - south

  run:
    - python
    - django <1.5.0
    - django-admin-ext
    - django-autoload
    - django-fields
    - django-class-registry >=0.0.3
    - south

test:
  # Python imports
  imports:
    - dynamic_rules
    - dynamic_rules.migrations
    - dynamic_rules.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/imtapps/django-dynamic-rules
  license: BSD License
  summary: 'Allows you to create dynamic rules related to a particular model'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
