package:
  name: django_roles
  version: "0.0.4"

source:
  fn: django_roles-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django_roles/django_roles-0.0.4.tar.gz
  md5: 8a295ea7a962b0d6555bb75afa65238a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_roles = django_roles:main
    #
    # Would create an entry point called django_roles that calls django_roles.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.7.3
    - django-mptt >=0.6.1

  run:
    - python
    - django >=1.7.3
    - django-mptt >=0.6.1

test:
  # Python imports
  imports:
    - django_roles
    - django_roles.backends

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/cenkbircanoglu/django-roles
  license: MIT
  summary: 'Django Roles with Role-Group-Permission-User'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
