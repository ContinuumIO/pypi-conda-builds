package:
  name: django-devserver
  version: "0.8.0"

source:
  fn: django-devserver-0.8.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-devserver/django-devserver-0.8.0.tar.gz
  md5: d415849966b87d2d35b771c2300d9820
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-devserver = django-devserver:main
    #
    # Would create an entry point called django-devserver that calls django-devserver.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - devserver
    - devserver.management
    - devserver.management.commands
    - devserver.modules
    - devserver.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/dcramer/django-devserver
  license: BSD
  summary: "Drop-in replacement for Django's runserver"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
