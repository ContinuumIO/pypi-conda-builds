package:
  name: django-template-names
  version: "0.1.0"

source:
  fn: django-template-names-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-template-names/django-template-names-0.1.0.tar.gz
  md5: ebdc5d0c6670a8e653ff69bb50bc0126
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-template-names = django-template-names:main
    #
    # Would create an entry point called django-template-names that calls django-template-names.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - template_names

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/phpdude/django-template-names
  license: UNKNOWN
  summary: 'Django templates names list automatic helper'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
