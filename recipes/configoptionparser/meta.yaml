package:
  name: configoptionparser
  version: "0.1"

source:
  fn: ConfigOptionParser-0.1.tar.gz
  url: https://pypi.python.org/packages/source/C/ConfigOptionParser/ConfigOptionParser-0.1.tar.gz
  md5: e4c66d99a6f220d3a810583342378624
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - configoptionparser = configoptionparser:main
    #
    # Would create an entry point called configoptionparser that calls configoptionparser.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - configoptionparser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://k0s.org
  license: MPL
  summary: 'a version of OptionParser that allows parsing from .ini files'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
