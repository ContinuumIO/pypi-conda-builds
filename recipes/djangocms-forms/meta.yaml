package:
  name: djangocms-forms
  version: "0.1.1"

source:
  fn: djangocms-forms-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/djangocms-forms/djangocms-forms-0.1.1.tar.gz
  md5: ff19247319f66bb718ee5536259f440b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangocms-forms = djangocms-forms:main
    #
    # Would create an entry point called djangocms-forms that calls djangocms-forms.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-appconf
    - django-ipware
    - jsonfield
    - tablib
    - django-cms >=3.0

  run:
    - python
    - django-appconf
    - django-ipware
    - jsonfield
    - tablib
    - django-cms >=3.0

test:
  # Python imports
  imports:
    - djangocms_forms

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mishbahr/djangocms-forms
  license: BSD License
  summary: 'The easiest and most flexible Django CMS Form builder!'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
