package:
  name: dxfwrite
  version: "1.2.0"

source:
  fn: dxfwrite-1.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/dxfwrite/dxfwrite-1.2.0.tar.gz
  md5: 56bd40206e6dbf4a3be2f3b6936a2c4f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dxfwrite = dxfwrite:main
    #
    # Would create an entry point called dxfwrite that calls dxfwrite.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - dxfwrite
    - dxfwrite/algebra

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/mozman/dxfwrite
  license: GNU General Public License (GPL)
  summary: 'A Python library to create DXF R12 drawings.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
