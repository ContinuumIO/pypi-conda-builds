package:
  name: redturtle.portlet.content
  version: "1.4.0"

source:
  fn: redturtle.portlet.content-1.4.0.zip
  url: https://pypi.python.org/packages/source/r/redturtle.portlet.content/redturtle.portlet.content-1.4.0.zip
  md5: 67c1e117671f65666164b777a8f1af59
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - redturtle.portlet.content = redturtle.portlet.content:main
    #
    # Would create an entry point called redturtle.portlet.content that calls redturtle.portlet.content.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - redturtle
    - redturtle.portlet
    - redturtle.portlet.content
    - redturtle.portlet.content.browser
    - redturtle.portlet.content.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/redturtle.portlet.content
  license: GPL
  summary: 'A generic portlet that allows to show various parts of an object, like the title, description, text, image and comments.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
