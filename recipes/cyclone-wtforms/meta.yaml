package:
  name: cyclone-wtforms
  version: "0.1.0"

source:
  fn: cyclone-wtforms-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/c/cyclone-wtforms/cyclone-wtforms-0.1.0.tar.gz
  md5: bccfa18270ae33920e5b79bf55954322
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cyclone-wtforms = cyclone-wtforms:main
    #
    # Would create an entry point called cyclone-wtforms that calls cyclone-wtforms.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - twisted
    - cyclone
    - wtforms

  run:
    - python
    - twisted
    - cyclone
    - wtforms

test:
  # Python imports
  imports:
    - cyclone_wtforms

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/FZambia/cyclone-wtforms
  license: http://www.apache.org/licenses/LICENSE-2.0
  summary: 'WTForms for cyclone web server'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
