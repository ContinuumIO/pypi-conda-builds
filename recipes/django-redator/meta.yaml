package:
  name: django-redator
  version: "0.2.3"

source:
  fn: django-redator-0.2.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-redator/django-redator-0.2.3.tar.gz
  md5: d0c3dee4e82c51fd5e8b7ef80865f6e4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-redator = django-redator:main
    #
    # Would create an entry point called django-redator that calls django-redator.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - redator
    - redator.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/semente/django-redator/
  license: BSD License
  summary: 'Django Redator (sic) is a application for the Django Web Framework to help you integrate Redactor <http://imperavi.com/redactor/>, a beautiful and easy-to-use WYSIWYG HTML editor, into your projects.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
