package:
  name: django-finegrained-permissions
  version: "0.0.1"

source:
  fn: django-finegrained-permissions-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-finegrained-permissions/django-finegrained-permissions-0.0.1.tar.gz
  md5: aa12536220e620ae33c3cae0b412459b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-finegrained-permissions = django-finegrained-permissions:main
    #
    # Would create an entry point called django-finegrained-permissions that calls django-finegrained-permissions.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - fgp

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/specialunderwear/django-finegrained-permissions
  license: GNU General Public License (GPL)
  summary: 'Add permissions per field instead of per model'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
