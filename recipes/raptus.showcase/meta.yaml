package:
  name: raptus.showcase
  version: "2.0b2"

source:
  fn: raptus.showcase-2.0b2.tar.gz
  url: https://pypi.python.org/packages/source/r/raptus.showcase/raptus.showcase-2.0b2.tar.gz
  md5: 926f370fec1967dbd39bd723d1ad51e3
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - raptus.showcase = raptus.showcase:main
    #
    # Would create an entry point called raptus.showcase that calls raptus.showcase.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - raptus
    - raptus.showcase
    - raptus.showcase.browser
    - raptus.showcase.content

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://raptus.com
  license: GPL
  summary: 'showcase'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
