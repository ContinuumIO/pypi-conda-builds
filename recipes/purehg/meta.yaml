package:
  name: purehg
  version: "2.4.2"

source:
  fn: purehg-2.4.2.tar.gz
  url: https://pypi.python.org/packages/source/p/purehg/purehg-2.4.2.tar.gz
  md5: 65cca2ae213bec9c42d709fa3d422ada
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - purehg = purehg:main
    #
    # Would create an entry point called purehg that calls purehg.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - hgext
    - hgext.convert
    - hgext.highlight
    - hgext.largefiles
    - hgext.zeroconf
    - mercurial
    - mercurial.hgweb
    - mercurial.httpclient
    - purehg

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://mercurial.selenic.com/
  license: GNU GPLv2+
  summary: 'Scalable distributed SCM'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
