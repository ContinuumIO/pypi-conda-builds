package:
  name: django-uwkhtmltopdf
  version: "0.5.0"

source:
  fn: django-uwkhtmltopdf-0.5.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-uwkhtmltopdf/django-uwkhtmltopdf-0.5.0.tar.gz
  md5: ebd017a8c7161df18861612cea791e7b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-uwkhtmltopdf = django-uwkhtmltopdf:main
    #
    # Would create an entry point called django-uwkhtmltopdf that calls django-uwkhtmltopdf.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.2

  run:
    - python
    - django >=1.2

test:
  # Python imports
  imports:
    - django_uwkhtmltopdf
    - django_uwkhtmltopdf.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/xrmx/django-uwkhtmltopdf
  license: BSD License
  summary: 'A Django app to generate pdfs from templates using wkhtmltopdf'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
