package:
  name: morelia
  version: "0.4.0"

source:
  fn: Morelia-0.4.0.tar.gz
  url: https://pypi.python.org/packages/source/M/Morelia/Morelia-0.4.0.tar.gz
  md5: b83ce3b795a2068b1a4c6098f3f1a6e2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - morelia = morelia:main
    #
    # Would create an entry point called morelia that calls morelia.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six
    - parse

  run:
    - python
    - six
    - parse

test:
  # Python imports
  imports:
    - morelia
    - morelia.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://morelia.readthedocs.org/
  license: MIT License
  summary: 'for "Behavior Driven Development" (BDD) -- a client-facing scripting language to put the squeeze on all your features'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
