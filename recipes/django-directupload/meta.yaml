package:
  name: django-directupload
  version: "0.0.11"

source:
  fn: django-directupload-0.0.11.tar.gz
  url: https://pypi.python.org/packages/source/d/django-directupload/django-directupload-0.0.11.tar.gz
  md5: d87185374288341d914c8551133afe4e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-directupload = django-directupload:main
    #
    # Would create an entry point called django-directupload that calls django-directupload.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - directupload
    - directupload.backends
    - directupload.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/cuker/django-directupload
  license: BSD License
  summary: 'A Django application that offers direct uploading of files from your forms using Uploadify.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
