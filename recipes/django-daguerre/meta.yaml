package:
  name: django-daguerre
  version: "2.1.4"

source:
  fn: django-daguerre-2.1.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-daguerre/django-daguerre-2.1.4.tar.gz
  md5: d95a6ff1d88648c41d5a74a978768776
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-daguerre = django-daguerre:main
    #
    # Would create an entry point called django-daguerre that calls django-daguerre.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pillow >=2.3.0
    - django >=1.7
    - six >=1.3.0

  run:
    - python
    - pillow >=2.3.0
    - django >=1.7
    - six >=1.3.0

test:
  # Python imports
  imports:
    - daguerre
    - daguerre.management
    - daguerre.management.commands
    - daguerre.migrations
    - daguerre.templatetags
    - daguerre.tests
    - daguerre.tests.functional
    - daguerre.tests.unit

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://django-daguerre.readthedocs.org/
  license: BSD License
  summary: 'Image management and processing for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
