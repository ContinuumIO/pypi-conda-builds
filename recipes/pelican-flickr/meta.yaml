package:
  name: pelican-flickr
  version: "0.1.1"

source:
  fn: pelican-flickr-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/p/pelican-flickr/pelican-flickr-0.1.1.tar.gz
  md5: 7f5ca16dfee3bea4d67e1a55f6654234
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pelican-flickr = pelican-flickr:main
    #
    # Would create an entry point called pelican-flickr that calls pelican-flickr.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flickrapi >=1.4.2

  run:
    - python
    - flickrapi >=1.4.2

test:
  # Python imports
  imports:
    - pelican-flickr

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/La0/pelican-flickr
  license: LICENSE.txt
  summary: 'Pelican plugin to add Flickr sets and photos to your website.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
