package:
  name: medialog.redirect
  version: "0.2.1"

source:
  fn: medialog.redirect-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/m/medialog.redirect/medialog.redirect-0.2.1.tar.gz
  md5: 3e59d4be76c66d77e6e65b7699da37e7
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - medialog.redirect = medialog.redirect:main
    #
    # Would create an entry point called medialog.redirect that calls medialog.redirect.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - plone.api
    - setuptools

  run:
    - python
    - setuptools
    - plone.api
    - setuptools

test:
  # Python imports
  imports:
    - medialog
    - medialog.redirect
    - medialog.redirect.browser
    - medialog.redirect.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/espenmn/medialog.redirect
  license: gpl
  summary: 'A redirecting browser view'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
