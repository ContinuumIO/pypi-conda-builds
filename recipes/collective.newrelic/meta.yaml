package:
  name: collective.newrelic
  version: "1.0.9"

source:
  fn: collective.newrelic-1.0.9.zip
  url: https://pypi.python.org/packages/source/c/collective.newrelic/collective.newrelic-1.0.9.zip
  md5: 695c50cf95c5ecd4928bbfb42383cd8f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.newrelic = collective.newrelic:main
    #
    # Would create an entry point called collective.newrelic that calls collective.newrelic.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - newrelic
    - repoze.xmliter

  run:
    - python
    - setuptools
    - setuptools
    - newrelic
    - repoze.xmliter

test:
  # Python imports
  imports:
    - collective
    - collective.newrelic
    - collective.newrelic.patches
    - collective.newrelic.transforms

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.newrelic
  license: GPL
  summary: 'Zope/Plone Newrelic instrumentation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
