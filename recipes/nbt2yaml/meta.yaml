package:
  name: nbt2yaml
  version: "0.3.0"

source:
  fn: nbt2yaml-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/n/nbt2yaml/nbt2yaml-0.3.0.tar.gz
  md5: 8dd55e2c97d265aae4ae207e7573a0cc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - nbt2yaml = nbt2yaml:main
    #
    # Would create an entry point called nbt2yaml that calls nbt2yaml.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyyaml

  run:
    - python
    - pyyaml

test:
  # Python imports
  imports:
    - nbt2yaml

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/zzzeek/nbt2yaml
  license: BSD License
  summary: 'Read and write Minecraft NBT files using Yaml.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
