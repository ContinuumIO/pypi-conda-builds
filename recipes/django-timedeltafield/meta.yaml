package:
  name: django-timedeltafield
  version: "0.7.8"

source:
  fn: django-timedeltafield-0.7.8.tar.gz
  url: https://pypi.python.org/packages/source/d/django-timedeltafield/django-timedeltafield-0.7.8.tar.gz
  md5: 2127da1b520f96896a764aec3dcca2fa
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-timedeltafield = django-timedeltafield:main
    #
    # Would create an entry point called django-timedeltafield that calls django-timedeltafield.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - timedelta
    - timedelta.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://hg.schinckel.net/django-timedelta-field/
  license: BSD License
  summary: 'TimedeltaField for django models'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
