package:
  name: docxtpl
  version: "0.1.4"

source:
  fn: docxtpl-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/d/docxtpl/docxtpl-0.1.4.tar.gz
  md5: 8a4ff825c8640fb70d232011719b4017
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - docxtpl = docxtpl:main
    #
    # Would create an entry point called docxtpl that calls docxtpl.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sphinx <1.3b
    - sphinxcontrib-napoleon
    - python-docx
    - jinja2

  run:
    - python
    - sphinx <1.3b
    - sphinxcontrib-napoleon
    - python-docx
    - jinja2

test:
  # Python imports
  imports:
    - docxtpl

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/elapouya/python-docx-template
  license: LGPL 2.1
  summary: 'Python docx template engine'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
