package:
  name: flask-heroku-mongoengine
  version: "0.1.5"

source:
  fn: flask-heroku-mongoengine-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/f/flask-heroku-mongoengine/flask-heroku-mongoengine-0.1.5.tar.gz
  md5: a88253bf34369c5b82ee0d774c19abb9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-heroku-mongoengine = flask-heroku-mongoengine:main
    #
    # Would create an entry point called flask-heroku-mongoengine that calls flask-heroku-mongoengine.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask

  run:
    - python
    - flask

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bjackson/flask-heroku
  license: BSD License
  summary: 'Heroku environment variable configurations for Flask'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
