package:
  name: allantools
  version: "1.1"

source:
  fn: AllanTools-1.1.tar.gz
  url: https://pypi.python.org/packages/source/A/AllanTools/AllanTools-1.1.tar.gz
  md5: 0e617b039ef273fb48e9d004b77dfb32
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - allantools = allantools:main
    #
    # Would create an entry point called allantools that calls allantools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - allantools

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/aewallin/allantools
  license: GPLv3+
  summary: 'Allan deviation and related time/frequency statistics'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
