package:
  name: django-admin-timestamps
  version: "1.2"

source:
  fn: django-admin-timestamps-1.2.zip
  url: https://pypi.python.org/packages/source/d/django-admin-timestamps/django-admin-timestamps-1.2.zip
  md5: cd5d9fc9850210c51fbedaafd013e713
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-admin-timestamps = django-admin-timestamps:main
    #
    # Would create an entry point called django-admin-timestamps that calls django-admin-timestamps.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - admintimestamps

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/jaap3/django-admin-timestamps/
  license: MIT License
  summary: 'Custom list display of model timestamps for Django Admin.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
