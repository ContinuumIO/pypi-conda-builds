package:
  name: flask-couchdbkit
  version: "0.3.5"

source:
  fn: Flask-CouchDBKit-0.3.5.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-CouchDBKit/Flask-CouchDBKit-0.3.5.tar.gz
  md5: 6f93cf99b59e66731b8a0f964780d6ac
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-couchdbkit = flask-couchdbkit:main
    #
    # Would create an entry point called flask-couchdbkit that calls flask-couchdbkit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - restkit >=3.0.2
    - couchdbkit

  run:
    - python
    - flask
    - restkit >=3.0.2
    - couchdbkit

test:
  # Python imports
  imports:
    - flaskext

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.grid.in.th/
  license: BSD License
  summary: 'Flask extension that provides integration with CouchDBKit.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
