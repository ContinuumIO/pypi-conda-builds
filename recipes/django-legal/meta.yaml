package:
  name: django-legal
  version: "1.0"

source:
  fn: django-legal-1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-legal/django-legal-1.0.tar.gz
  md5: fa6b24c298a4926079d0aab6c0b66c5e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-legal = django-legal:main
    #
    # Would create an entry point called django-legal that calls django-legal.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - legal

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Kyruus/django-legal
  license: MIT License
  summary: 'A Django app to require user acceptance of terms of service.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
