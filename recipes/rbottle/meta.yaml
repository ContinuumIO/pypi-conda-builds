package:
  name: rbottle
  version: "0.2.0"

source:
  fn: rbottle-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/r/rbottle/rbottle-0.2.0.tar.gz
  md5: 84c4cf8d6b9d856bbef3e254eb058627
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - rbottle = rbottle:main
    #
    # Would create an entry point called rbottle that calls rbottle.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - bottle

  run:
    - python
    - setuptools
    - bottle

test:
  # Python imports
  imports:
    - rbottle

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Bystroushaak/rbottle
  license: MIT License
  summary: 'Decorators to make REST easier in Bottle.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
