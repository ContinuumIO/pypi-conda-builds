package:
  name: vtixyproxy
  version: "1.2.2"

source:
  fn: VTIXyProxy-1.2.2.tar.gz
  url: https://pypi.python.org/packages/source/V/VTIXyProxy/VTIXyProxy-1.2.2.tar.gz
  md5: 8591b71cc66c6ccba3cc8365cbf5d904
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - vtixyproxy = vtixyproxy:main
    #
    # Would create an entry point called vtixyproxy that calls vtixyproxy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django-rest-framework-proxy ==1.1.0

  run:
    - python
    - django-rest-framework-proxy ==1.1.0

test:
  # Python imports
  imports:
    - vtixy_proxy
    - vtixyproxy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: LICENSE.txt
  summary: 'Web services for applications of VTIX.RU clients'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
