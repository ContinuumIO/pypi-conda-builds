package:
  name: doctest-tools
  version: "1.0a3"

source:
  fn: doctest-tools-1.0a3.zip
  url: http://doctest-tools.googlecode.com/files/doctest-tools-1.0a3.zip
  #md5: 
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - doctest-tools = doctest-tools:main
    #
    # Would create an entry point called doctest-tools that calls doctest-tools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - doctest_tools

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/doctest-tools
  license: MIT License
  summary: 'Tools to run doctests on code and text files within a directory'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
