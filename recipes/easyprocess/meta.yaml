package:
  name: easyprocess
  version: "0.1.9"

source:
  fn: EasyProcess-0.1.9.tar.gz
  url: https://pypi.python.org/packages/source/E/EasyProcess/EasyProcess-0.1.9.tar.gz
  md5: 3da72e2fe16781fe5c7b3b0c6c40ee7b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - easyprocess = easyprocess:main
    #
    # Would create an entry point called easyprocess that calls easyprocess.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - easyprocess
    - easyprocess.examples

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ponty/easyprocess
  license: BSD License
  summary: 'Easy to use python subprocess interface.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
