package:
  name: monolith
  version: "0.3.3"

source:
  fn: monolith-0.3.3.tar.gz
  url: https://pypi.python.org/packages/source/m/monolith/monolith-0.3.3.tar.gz
  md5: 41d1671755f0bbcfdb858dc33f1ac0b7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - monolith = monolith:main
    #
    # Would create an entry point called monolith that calls monolith.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - monolith
    - monolith.cli
    - monolith.tests
    - monolith.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lukaszb/monolith
  license: MIT License
  summary: '\nmonolith is an argparse based command line interface framework\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
