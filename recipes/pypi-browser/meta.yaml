package:
  name: pypi-browser
  version: "1.5"

source:
  fn: PyPI-Browser-1.5.zip
  url: https://pypi.python.org/packages/source/P/PyPI-Browser/PyPI-Browser-1.5.zip
  md5: 033ca30e4cc93522cb539199d8884be8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pypi-browser = pypi-browser:main
    #
    # Would create an entry point called pypi-browser that calls pypi-browser.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - PyPIBrowser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.boddie.org.uk/david/Projects/Python/PyPI-Browser/
  license: GNU General Public License
  summary: 'A GUI browser for the Python Package Index'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
