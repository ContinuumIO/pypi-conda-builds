package:
  name: xstatic-browser-update
  version: "1.0.1"

source:
  fn: XStatic-browser-update-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/X/XStatic-browser-update/XStatic-browser-update-1.0.1.tar.gz
  md5: 51ac7ab523e3e925e7f53073f099bcb5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - xstatic-browser-update = xstatic-browser-update:main
    #
    # Would create an entry point called xstatic-browser-update that calls xstatic-browser-update.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - xstatic-jquery

  run:
    - python
    - xstatic-jquery

test:
  # Python imports
  imports:
    - xstatic
    - xstatic.pkg
    - xstatic.pkg.browser_update

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: (same as browser-update)
  summary: 'browser-update 1.0 (XStatic packaging standard)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
