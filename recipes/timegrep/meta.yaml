package:
  name: timegrep
  version: "1.5"

source:
  fn: timegrep-1.5.tar.gz
  url: https://pypi.python.org/packages/source/t/timegrep/timegrep-1.5.tar.gz
  md5: 89eb4f6b8a4b93084870b84a592f65ab
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - timegrep = timegrep:main
    #
    # Would create an entry point called timegrep that calls timegrep.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - timegrep

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/linux-wizard
  license: GNU General Public License (GPL)
  summary: 'Perform a binary search through a log file to find a range of times and print the corresponding lines'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
