package:
  name: calendra
  version: "1.4"

source:
  fn: calendra-1.4.zip
  url: https://pypi.python.org/packages/source/c/calendra/calendra-1.4.zip
  md5: 8e2ed333e18209391364b206994110cb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - calendra = calendra:main
    #
    # Would create an entry point called calendra that calls calendra.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python-dateutil
    - lunardate
    - pytz
    - pycalverter
    - more_itertools
    - pyephem

  run:
    - python
    - python-dateutil
    - lunardate
    - pytz
    - pycalverter
    - more_itertools
    - pyephem

test:
  # Python imports
  imports:
    - calendra

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jaraco/calendra
  license: MIT License
  summary: 'Worldwide holidays and working days helper and toolkit.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
