package:
  name: obb
  version: "0.7b"

source:
  fn: oBB-0.7b.tar.gz
  url: https://pypi.python.org/packages/source/o/oBB/oBB-0.7b.tar.gz
  md5: aaab2e62d251fc058f2838c1f2e4d03b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - obb = obb:main
    #
    # Would create an entry point called obb that calls obb.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy >=1.3.0
    - mpi4py >=1.3
    - cvxopt >=1.1.3

  run:
    - python
    - numpy >=1.3.0
    - mpi4py >=1.3
    - cvxopt >=1.1.3

test:
  # Python imports
  imports:
    - obb

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/oBB/
  license: GNU Lesser General Public License v3 (LGPLv3)
  summary: 'Parallel global optimization of Hessian Lipschitz continuous functions.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
