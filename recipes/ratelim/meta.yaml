package:
  name: ratelim
  version: "0.1.6"

source:
  fn: ratelim-0.1.6.tar.gz
  url: https://pypi.python.org/packages/source/r/ratelim/ratelim-0.1.6.tar.gz
  md5: 91063fcac413e9faba01c9046c5134dc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ratelim = ratelim:main
    #
    # Would create an entry point called ratelim that calls ratelim.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - decorator

  run:
    - python
    - decorator

test:
  # Python imports
  imports:
    - ratelim

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/themiurgo/ratelim
  license: MIT
  summary: 'Makes it easy to respect rate limits.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
