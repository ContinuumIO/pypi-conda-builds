package:
  name: mr.freeze
  version: "1.0a3"

source:
  fn: mr.freeze-1.0a3.tar.gz
  url: https://pypi.python.org/packages/source/m/mr.freeze/mr.freeze-1.0a3.tar.gz
  md5: 99f75c23504e1ebb926d431510ca1837
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mr.freeze = mr.freeze:main
    #
    # Would create an entry point called mr.freeze that calls mr.freeze.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - z3c.deadlockdebugger

  run:
    - python
    - setuptools
    - z3c.deadlockdebugger

test:
  # Python imports
  imports:
    - mr
    - mr.freeze

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://en.wikipedia.org/wiki/Mr._Freeze
  license: BSD
  summary: 'Trigger a pdb in a running zope via a USR1 signal'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
