package:
  name: zope.app.pluggableauth
  version: "3.4.0"

source:
  fn: zope.app.pluggableauth-3.4.0.tar.gz
  url: https://pypi.python.org/packages/source/z/zope.app.pluggableauth/zope.app.pluggableauth-3.4.0.tar.gz
  md5: 7cf294005f10ceefb4774c7468006762
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zope.app.pluggableauth = zope.app.pluggableauth:main
    #
    # Would create an entry point called zope.app.pluggableauth that calls zope.app.pluggableauth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zodb3
    - zope.app.component
    - zope.app.container
    - zope.app.security
    - zope.app.zapi
    - zope.component
    - zope.deprecation
    - zope.interface
    - zope.location
    - zope.publisher
    - zope.schema
    - zope.traversing

  run:
    - python
    - setuptools
    - zodb3
    - zope.app.component
    - zope.app.container
    - zope.app.security
    - zope.app.zapi
    - zope.component
    - zope.deprecation
    - zope.interface
    - zope.location
    - zope.publisher
    - zope.schema
    - zope.traversing

test:
  # Python imports
  imports:
    - zope
    - zope.app
    - zope.app.pluggableauth
    - zope.app.pluggableauth.browser
    - zope.app.pluggableauth.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://cheeseshop.python.org/pypi/zope.app.pluggableauth
  license: Zope Public License
  summary: 'Pluggable Authenticatin Mechanism (Obselete)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
