package:
  name: gtwittools
  version: "0.0.5"

source:
  fn: gtwittools-0.0.5.tar.gz
  url: https://pypi.python.org/packages/source/g/gtwittools/gtwittools-0.0.5.tar.gz
  md5: 54a37cb770839085c60343c36d64dac1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gtwittools = gtwittools:main
    #
    # Would create an entry point called gtwittools that calls gtwittools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - gevent >=1.0
    - gipc
    - python-twitter
    - pytumblr

  run:
    - python
    - gevent >=1.0
    - gipc
    - python-twitter
    - pytumblr

test:
  # Python imports
  imports:
    - gtwittools

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/awentzonline/gtwittools
  license: MIT
  summary: 'Some tools for making twitter-related things with gevent.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
