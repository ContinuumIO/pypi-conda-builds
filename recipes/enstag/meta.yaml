package:
  name: enstag
  version: "0.1.1"

source:
  fn: Enstag-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/E/Enstag/Enstag-0.1.1.tar.gz
  md5: 7003f536f356afb6848ddbc992f0b198
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - enstag = enstag:main
    #
    # Would create an entry point called enstag that calls enstag.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - enstag

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.ebi.ac.uk/~hoffman/software/enstag/
  license: GNU General Public License (GPL)
  summary: 'unique identifiers for Ensembl features'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
