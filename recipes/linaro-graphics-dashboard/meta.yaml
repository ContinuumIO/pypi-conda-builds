package:
  name: linaro-graphics-dashboard
  version: "2012.7"

source:
  fn: linaro-graphics-dashboard-2012.7.tar.gz
  url: https://pypi.python.org/packages/source/l/linaro-graphics-dashboard/linaro-graphics-dashboard-2012.7.tar.gz
  md5: 0d72cf822ccbc75e19d9c878fddd7128
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - linaro-graphics-dashboard = linaro-graphics-dashboard:main
    #
    # Would create an entry point called linaro-graphics-dashboard that calls linaro-graphics-dashboard.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - lava-server >=0.2

  run:
    - python
    - setuptools
    - lava-server >=0.2

test:
  # Python imports
  imports:
    - linaro_graphics_app
    - linaro_graphics_app.templatetags
    - linaro_graphics_app.views

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: AGPL
  summary: 'Linaro Graphics WG application for LAVA Server'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
