package:
  name: ftw.permissionmanager
  version: "2.3.2"

source:
  fn: ftw.permissionmanager-2.3.2.zip
  url: https://pypi.python.org/packages/source/f/ftw.permissionmanager/ftw.permissionmanager-2.3.2.zip
  md5: 2f988d6d84964c4da3df465246ead009
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ftw.permissionmanager = ftw.permissionmanager:main
    #
    # Would create an entry point called ftw.permissionmanager that calls ftw.permissionmanager.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.monkeypatcher
    - products.cmfplone >=4.3b
    - ftw.upgrade
    - ftw.profilehook

  run:
    - python
    - setuptools
    - setuptools
    - collective.monkeypatcher
    - products.cmfplone >=4.3b
    - ftw.upgrade
    - ftw.profilehook

test:
  # Python imports
  imports:
    - ftw
    - ftw.permissionmanager
    - ftw.permissionmanager.browser
    - ftw.permissionmanager.tests
    - ftw.permissionmanager.upgrades

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/4teamwork/ftw.permissionmanager
  license: GPL2
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
