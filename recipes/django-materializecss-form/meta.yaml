package:
  name: django-materializecss-form
  version: "0.0.64"

source:
  fn: django-materializecss-form-0.0.64.tar.gz
  url: https://pypi.python.org/packages/source/d/django-materializecss-form/django-materializecss-form-0.0.64.tar.gz
  md5: 6ee0eebe24ab7542237dcec1aa738b31
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-materializecss-form = django-materializecss-form:main
    #
    # Would create an entry point called django-materializecss-form that calls django-materializecss-form.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - materializecssform
    - materializecssform.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/florent1933/django-materializecss-form
  license: MIT License
  summary: 'A simple Django form template tag to work with Materializecss'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
