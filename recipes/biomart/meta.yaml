package:
  name: biomart
  version: "0.5.0"

source:
  fn: biomart-0.5.0.tar.gz
  url: https://pypi.python.org/packages/source/b/biomart/biomart-0.5.0.tar.gz
  md5: 63806a21e9781844e8cffc24d4ce82a2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - biomart = biomart:main
    #
    # Would create an entry point called biomart that calls biomart.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=2.2

  run:
    - python
    - requests >=2.2

test:
  # Python imports
  imports:
    - biomart
    - biomart.lib
    - biomart.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD License
  summary: 'Python API that consumes the biomart webservice'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
