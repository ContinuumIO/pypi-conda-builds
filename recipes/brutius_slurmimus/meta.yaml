package:
  name: brutius_slurmimus
  version: "0.1.1dev"

source:
  fn: brutius_slurmimus-0.1.1dev.tar.gz
  url: https://pypi.python.org/packages/source/b/brutius_slurmimus/brutius_slurmimus-0.1.1dev.tar.gz
  md5: b2b51c9352abf9b44edd88fce5e33516
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - brutius_slurmimus = brutius_slurmimus:main
    #
    # Would create an entry point called brutius_slurmimus that calls brutius_slurmimus.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - brutius_slurmimus

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/ralphbean/brutius_slurmimus
  license: UNKNOWN
  summary: "Parse-and-return slurm commands.  Don't use this.  Use pyslurm instead."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
