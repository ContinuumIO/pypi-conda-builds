package:
  name: bbricks
  version: "1.0.18"

source:
  fn: bbricks-1.0.18.tar.gz
  url: https://pypi.python.org/packages/source/b/bbricks/bbricks-1.0.18.tar.gz
  md5: a829c80334b47cb83ca01820cc0eccaa
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bbricks = bbricks:main
    #
    # Would create an entry point called bbricks that calls bbricks.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - colorama
    - termcolor

  run:
    - python
    - colorama
    - termcolor

test:
  # Python imports
  imports:
    - bbricks

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT License
  summary: 'C/C++ builder on top of SCons, automatically enforcing quality, coding standards, etc'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
