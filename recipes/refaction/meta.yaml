package:
  name: refaction
  version: "0.0.1dev"

source:
  fn: Refaction-0.0.1dev.tar.gz
  url: https://pypi.python.org/packages/source/R/Refaction/Refaction-0.0.1dev.tar.gz
  md5: 9f3148ac316357234b7d4f36a317f6c6
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - refaction = refaction:main
    #
    # Would create an entry point called refaction that calls refaction.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pylons >=0.9.6.1
    - pycrud

  run:
    - python
    - setuptools
    - pylons >=0.9.6.1
    - pycrud

test:
  # Python imports
  imports:
    - refaction
    - refaction.config
    - refaction.controllers
    - refaction.lib
    - refaction.model
    - refaction.tests
    - refaction.tests.functional

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: GNU General Public License (GPL) or Python Software Foundation License
  summary: "Web-based interface for sharing webfaction services using webfaction's API."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
