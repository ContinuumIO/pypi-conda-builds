package:
  name: django_test_mixins
  version: "1.10"

source:
  fn: django_test_mixins-1.10.tar.gz
  url: https://pypi.python.org/packages/source/d/django_test_mixins/django_test_mixins-1.10.tar.gz
  md5: 9d5de6c546ce5d16dad61c2c9515846f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_test_mixins = django_test_mixins:main
    #
    # Would create an entry point called django_test_mixins that calls django_test_mixins.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - django_test_mixins

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Wilfred/django-test-mixins
  license: BSD License
  summary: 'Additional assertions and test conveniences for testing django sites.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
