package:
  name: django-redis
  version: "4.0.0"

source:
  fn: django-redis-4.0.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-redis/django-redis-4.0.0.tar.gz
  md5: a4385e258e5232cda80fc85d16cf6b69
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-redis = django-redis:main
    #
    # Would create an entry point called django-redis that calls django-redis.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - redis >=2.10.0

  run:
    - python
    - redis >=2.10.0

test:
  # Python imports
  imports:
    - django_redis
    - django_redis.client

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/niwibe/django-redis
  license: BSD License
  summary: 'Full featured redis cache backend for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
