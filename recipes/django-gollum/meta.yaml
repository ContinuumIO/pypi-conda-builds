package:
  name: django-gollum
  version: "1.0.1"

source:
  fn: django-gollum-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-gollum/django-gollum-1.0.1.tar.gz
  md5: 26bffe166f16d2289f82e484e849caab
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-gollum = django-gollum:main
    #
    # Would create an entry point called django-gollum that calls django-gollum.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - dict.sorted >=1.0.0
    - django >=1.4
    - six >=1.4.1

  run:
    - python
    - dict.sorted >=1.0.0
    - django >=1.4
    - six >=1.4.1

test:
  # Python imports
  imports:
    - gollum

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lukesneeringer/django-gollum
  license: BSD License
  summary: 'Better Django Form HTML/CSS customization. For Python 2 or Python 3.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
