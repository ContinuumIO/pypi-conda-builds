package:
  name: dokuwikixmlrpc
  version: "2010-07-19"

source:
  fn: dokuwikixmlrpc-2010-07-19.tar.gz
  url: https://pypi.python.org/packages/source/d/dokuwikixmlrpc/dokuwikixmlrpc-2010-07-19.tar.gz
  md5: 49e17a55ecd0c5206b765b73e76367af
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dokuwikixmlrpc = dokuwikixmlrpc:main
    #
    # Would create an entry point called dokuwikixmlrpc that calls dokuwikixmlrpc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - dokuwikixmlrpc

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://chimeric.de/projects/dokuwiki/dokuwikixmlrpc
  license: UNKNOWN
  summary: 'DokuWiki XML-RPC module.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
