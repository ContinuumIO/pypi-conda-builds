package:
  name: bgpranking-web
  version: "1.2"

source:
  fn: bgpranking-web-1.2.tar.gz
  url: https://pypi.python.org/packages/source/b/bgpranking-web/bgpranking-web-1.2.tar.gz
  md5: 2b94690bb6467c0dbd031eead82a16da
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bgpranking-web = bgpranking-web:main
    #
    # Would create an entry point called bgpranking-web that calls bgpranking-web.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - bgpranking_web

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/CIRCL/bgpranking-redis-api
  license: GNU General Public License v3 (GPLv3)
  summary: 'Library to access the BGP Ranking REST API.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
