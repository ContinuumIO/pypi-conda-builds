package:
  name: mekk.greader
  version: "1.2.3"

source:
  fn: mekk.greader-1.2.3.tar.gz
  url: https://pypi.python.org/packages/source/m/mekk.greader/mekk.greader-1.2.3.tar.gz
  md5: 39285f8a035f4898226662f8d409a98a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mekk.greader = mekk.greader:main
    #
    # Would create an entry point called mekk.greader that calls mekk.greader.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - lxml
    - simplejson

  run:
    - python
    - lxml
    - simplejson

test:
  # Python imports
  imports:
    - mekk
    - mekk.greader

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/Mekk/mekk.greader
  license: BSD
  summary: 'Simple wrapper for Google Reader API.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
