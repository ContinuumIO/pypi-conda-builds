package:
  name: digimat.cryptedwebservice
  version: "0.1.3"

source:
  fn: digimat.cryptedwebservice-0.1.3.zip
  url: https://pypi.python.org/packages/source/d/digimat.cryptedwebservice/digimat.cryptedwebservice-0.1.3.zip
  md5: 172de4eac5bc0ef7c0ae1cc48386de75
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - digimat.cryptedwebservice = digimat.cryptedwebservice:main
    #
    # Would create an entry point called digimat.cryptedwebservice that calls digimat.cryptedwebservice.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - requests
    - pycrypto
    - setuptools

  run:
    - python
    - requests
    - pycrypto
    - setuptools

test:
  # Python imports
  imports:
    - digimat
    - digimat.cryptedwebservice

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: PSF
  summary: 'Digimat Generic Crypted Webservice'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
