package:
  name: django-syncpermissions
  version: "0.1.1"

source:
  fn: django-syncpermissions-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-syncpermissions/django-syncpermissions-0.1.1.tar.gz
  md5: 1814c9fccfb17428f9e36b07d4e56597
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-syncpermissions = django-syncpermissions:main
    #
    # Would create an entry point called django-syncpermissions that calls django-syncpermissions.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3

  run:
    - python
    - django >=1.3

test:
  # Python imports
  imports:
    - django-syncpermissions
    - django-syncpermissions.management
    - django-syncpermissions.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/RegioHelden/django-syncpermissions
  license: MIT License
  summary: 'Sync permissions for specified apps, or all apps if none specified'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
