package:
  name: django-shop-payer-backend
  version: "0.1.7"

source:
  fn: django-shop-payer-backend-0.1.7.tar.gz
  url: https://pypi.python.org/packages/source/d/django-shop-payer-backend/django-shop-payer-backend-0.1.7.tar.gz
  md5: 56abe0e7943f1a5c5a35c5ba1eb52174
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-shop-payer-backend = django-shop-payer-backend:main
    #
    # Would create an entry point called django-shop-payer-backend that calls django-shop-payer-backend.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4
    - django-shop >=0.2.0
    - python-payer-api >=0.1.2

  run:
    - python
    - django >=1.4
    - django-shop >=0.2.0
    - python-payer-api >=0.1.2

test:
  # Python imports
  imports:
    - django_shop_payer_backend

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dessibelle/django-shop-payer-backend
  license: MIT License
  summary: 'Payment backend for django SHOP and Payer.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
