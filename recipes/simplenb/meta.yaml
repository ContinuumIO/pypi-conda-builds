package:
  name: simplenb
  version: "0.1"

source:
  fn: simplenb-0.1.zip
  url: https://pypi.python.org/packages/source/s/simplenb/simplenb-0.1.zip
  md5: d8ac4b2cea71797fd9838191d86c5d34
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - simplenb = simplenb:main
    #
    # Would create an entry point called simplenb that calls simplenb.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - scipy

  run:
    - python
    - scipy

test:
  # Python imports
  imports:
    - simplenb

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Zenyai/Python-Naive-Bayes-Classifier
  license: UNKNOWN
  summary: 'Simple text classification with Naive Bayes'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
