package:
  name: collective.cmcicpaiement
  version: "1.0b1"

source:
  fn: collective.cmcicpaiement-1.0b1.zip
  url: https://pypi.python.org/packages/source/c/collective.cmcicpaiement/collective.cmcicpaiement-1.0b1.zip
  md5: ba656a25fdec5ddbfe1ebf725eaec63d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.cmcicpaiement = collective.cmcicpaiement:main
    #
    # Would create an entry point called collective.cmcicpaiement that calls collective.cmcicpaiement.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.cmcicpaiement
    - collective.cmcicpaiement.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/
  license: GPL
  summary: 'CM CIC Paiement for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
