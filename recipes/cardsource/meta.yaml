package:
  name: cardsource
  version: "0.0.1"

source:
  fn: cardsource-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/cardsource/cardsource-0.0.1.tar.gz
  md5: a10f7094a33d1d07194d4e6b80b6b523
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cardsource = cardsource:main
    #
    # Would create an entry point called cardsource that calls cardsource.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - cardsource
    - cardsource.games
    - cardsource.games.blackjack

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/davidfischer/cardsource
  license: BSD License
  summary: 'A Python library for simulating playing card games'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
