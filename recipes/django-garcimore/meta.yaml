package:
  name: django-garcimore
  version: "1.0"

source:
  fn: django-garcimore-1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-garcimore/django-garcimore-1.0.tar.gz
  md5: 2a04af243def95df665ad5c4aad05a3c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-garcimore = django-garcimore:main
    #
    # Would create an entry point called django-garcimore that calls django-garcimore.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - garcimore

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/gawel/django-garcimore
  license: MIT License
  summary: 'A magic trick to make Django disappear'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
