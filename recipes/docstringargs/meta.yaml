package:
  name: docstringargs
  version: "0.2.1"

source:
  fn: docstringargs-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/docstringargs/docstringargs-0.2.1.tar.gz
  md5: 9820ae97a46281a7e3d72700cc57878f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - docstringargs = docstringargs:main
    #
    # Would create an entry point called docstringargs that calls docstringargs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - docstringargs

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Rosuav/docstringargs
  license: MIT License
  summary: 'Argparse builder using function docstrings'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
