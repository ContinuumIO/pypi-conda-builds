package:
  name: redturtle.smartlink
  version: "1.2.1"

source:
  fn: redturtle.smartlink-1.2.1.zip
  url: https://pypi.python.org/packages/source/r/redturtle.smartlink/redturtle.smartlink-1.2.1.zip
  md5: a62a6c865e214b50a0fc98c556ec04a9
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - redturtle.smartlink = redturtle.smartlink:main
    #
    # Would create an entry point called redturtle.smartlink that calls redturtle.smartlink.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.imaging
    - products.cmfplone

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.imaging
    - products.cmfplone

test:
  # Python imports
  imports:
    - redturtle
    - redturtle.smartlink
    - redturtle.smartlink.browser
    - redturtle.smartlink.content
    - redturtle.smartlink.events
    - redturtle.smartlink.interfaces
    - redturtle.smartlink.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/redturtle.smartlink
  license: GNU General Public License (GPL)
  summary: 'An advanced Link content type for Plone, with image field, customizable content icon and internal link feature'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
