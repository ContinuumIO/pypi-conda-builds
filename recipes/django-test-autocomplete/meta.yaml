package:
  name: django-test-autocomplete
  version: "0.1.4"

source:
  fn: django-test-autocomplete-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-test-autocomplete/django-test-autocomplete-0.1.4.tar.gz
  md5: 3aa3a05060ab2fe3a06c03b729d0ce47
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-test-autocomplete = django-test-autocomplete:main
    #
    # Would create an entry point called django-test-autocomplete that calls django-test-autocomplete.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/machinalis/django-test-autocomplete
  license: BSD License
  summary: u'A shell-autocomplete helper that list TestCases and tests'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
