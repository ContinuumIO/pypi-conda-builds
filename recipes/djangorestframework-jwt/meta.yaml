package:
  name: djangorestframework-jwt
  version: "1.5.0"

source:
  fn: djangorestframework-jwt-1.5.0.tar.gz
  url: https://pypi.python.org/packages/source/d/djangorestframework-jwt/djangorestframework-jwt-1.5.0.tar.gz
  md5: d011ff360febf8e568111d4b69398d4d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangorestframework-jwt = djangorestframework-jwt:main
    #
    # Would create an entry point called djangorestframework-jwt that calls djangorestframework-jwt.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyjwt >=1.1.0,<2.0.0

  run:
    - python
    - pyjwt >=1.1.0,<2.0.0

test:
  # Python imports
  imports:
    - rest_framework_jwt

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/GetBlimp/django-rest-framework-jwt
  license: MIT License
  summary: 'JSON Web Token based authentication for Django REST framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
