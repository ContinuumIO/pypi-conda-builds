package:
  name: flask-stylus2css
  version: "0.1"

source:
  fn: flask-stylus2css-0.1.zip
  url: https://pypi.python.org/packages/source/f/flask-stylus2css/flask-stylus2css-0.1.zip
  md5: 99f8bd8e2fb6506b240fc8ce1865bf5e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-stylus2css = flask-stylus2css:main
    #
    # Would create an entry point called flask-stylus2css that calls flask-stylus2css.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - stylus

  run:
    - python
    - flask
    - stylus

test:
  # Python imports
  imports:
    - flaskext

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/weapp/flask-stylus2css
  license: MIT License
  summary: 'A small Flask extension that adds Stylus support to Flask.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
