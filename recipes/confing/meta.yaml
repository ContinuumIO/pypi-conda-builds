package:
  name: confing
  version: "0.1.4"

source:
  fn: confing-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/c/confing/confing-0.1.4.tar.gz
  md5: bdf7f10661d567c93dbd5be48c050c06
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - confing = confing:main
    #
    # Would create an entry point called confing that calls confing.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - colander ==1.0
    - setuptools

  run:
    - python
    - colander ==1.0
    - setuptools

test:
  # Python imports
  imports:
    - confing

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/fahhem/confing
  license: Apache Software License
  summary: 'Described configuration for large, complex projects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
