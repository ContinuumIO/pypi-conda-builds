package:
  name: infi.bunch
  version: "2.0.0"

source:
  fn: infi.bunch-2.0.0.tar.gz
  url: https://pypi.python.org/packages/source/i/infi.bunch/infi.bunch-2.0.0.tar.gz
  md5: 1dfc0ea092ad5aa5641d7413169a1e65
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - infi.bunch = infi.bunch:main
    #
    # Would create an entry point called infi.bunch that calls infi.bunch.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - infi
    - infi.bunch

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/Infinidat/infi.bunch
  license: MIT License
  summary: 'A dot-accessible dictionary (a la JavaScript objects)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
