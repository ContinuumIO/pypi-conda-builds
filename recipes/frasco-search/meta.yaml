package:
  name: frasco-search
  version: "0.1"

source:
  fn: frasco-search-0.1.tar.gz
  url: https://pypi.python.org/packages/source/f/frasco-search/frasco-search-0.1.tar.gz
  md5: c97bf4b40ab61747ca60e6442152b4e2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - frasco-search = frasco-search:main
    #
    # Would create an entry point called frasco-search that calls frasco-search.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - frasco
    - elasticsearch >=1.2.0

  run:
    - python
    - frasco
    - elasticsearch >=1.2.0

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/frascoweb/frasco-search
  license: MIT
  summary: 'Elastisearch integration for Frasco'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
