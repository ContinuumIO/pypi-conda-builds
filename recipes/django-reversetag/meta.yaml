package:
  name: django-reversetag
  version: "0.3.2"

source:
  fn: django-reversetag-0.3.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-reversetag/django-reversetag-0.3.2.tar.gz
  md5: 73cdf0807e062ff20bf1a18c47f4220d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-reversetag = django-reversetag:main
    #
    # Would create an entry point called django-reversetag that calls django-reversetag.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - reversetag
    - reversetag.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/ulope/django-reversetag
  license: MIT
  summary: "A new, improved (and renamed) implementation of Django's 'url' template tag."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
