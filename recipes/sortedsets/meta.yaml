package:
  name: sortedsets
  version: "1.0"

source:
  fn: sortedsets-1.0.tar.gz
  url: https://pypi.python.org/packages/source/s/sortedsets/sortedsets-1.0.tar.gz
  md5: 26ae5aebb012028c310f495d8c1ac457
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sortedsets = sortedsets:main
    #
    # Would create an entry point called sortedsets that calls sortedsets.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - sortedsets

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/tailhook/sortedsets
  license: MIT License
  summary: "SortedSet structure closely modelled after Redis' sorted sets"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
