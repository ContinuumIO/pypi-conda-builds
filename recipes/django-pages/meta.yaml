package:
  name: django-pages
  version: "1.4.0"

source:
  fn: django-pages-1.4.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-pages/django-pages-1.4.0.tar.gz
  md5: b75d96c4bd3654658d4508c7d96e0e60
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-pages = django-pages:main
    #
    # Would create an entry point called django-pages that calls django-pages.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4
    - pytz
    - pillow
    - django-grappelli
    - django-filebrowser

  run:
    - python
    - django >=1.4
    - pytz
    - pillow
    - django-grappelli
    - django-filebrowser

test:
  # Python imports
  imports:
    - django_pages
    - django_pages.comments
    - django_pages.common
    - django_pages.feed
    - django_pages.language
    - django_pages.log
    - django_pages.looks
    - django_pages.menu
    - django_pages.metadata
    - django_pages.pages
    - django_pages.site

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lunemec/django-pages
  license: see LICENSE.txt
  summary: 'Simple CMS for django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
