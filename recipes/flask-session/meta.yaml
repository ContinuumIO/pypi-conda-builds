package:
  name: flask-session
  version: "0.1.1"

source:
  fn: Flask-Session-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Session/Flask-Session-0.1.1.tar.gz
  md5: bd1b8b1432440731af4ca719deb94872
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-session = flask-session:main
    #
    # Would create an entry point called flask-session that calls flask-session.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.8

  run:
    - python
    - flask >=0.8

test:
  # Python imports
  imports:
    - flask_session

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/fengsp/flask-session
  license: BSD License
  summary: 'Adds server-side session support to your Flask application'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
