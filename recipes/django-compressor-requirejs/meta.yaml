package:
  name: django-compressor-requirejs
  version: "0.3"

source:
  fn: django-compressor-requirejs-0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-compressor-requirejs/django-compressor-requirejs-0.3.tar.gz
  md5: aeba6f45394235a55e9cf5863ef49d26
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-compressor-requirejs = django-compressor-requirejs:main
    #
    # Would create an entry point called django-compressor-requirejs that calls django-compressor-requirejs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6
    - django_compressor >=1.4

  run:
    - python
    - django >=1.6
    - django_compressor >=1.4

test:
  # Python imports
  imports:
    - requirejs

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/bpeschier/django-compressor-requirejs
  license: MIT License
  summary: 'Compress requirejs-modules into bundles.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
