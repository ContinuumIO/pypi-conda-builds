package:
  name: community-codeswarm
  version: "0.2.1"

source:
  fn: Community Codeswarm-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/C/Community-Codeswarm/Community Codeswarm-0.2.1.tar.gz
  md5: 236eb9f212bc0e6b6791f0e14439f8ea
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - community-codeswarm = community-codeswarm:main
    #
    # Would create an entry point called community-codeswarm that calls community-codeswarm.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://rakjar.de/shared_codeswarm
  license: GNU General Public License (GPL)
  summary: ['Create combined code_swarms from several different source repositories. ']

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
