package:
  name: gocept.arecibologger
  version: "0.1.2"

source:
  fn: gocept.arecibologger-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/g/gocept.arecibologger/gocept.arecibologger-0.1.2.tar.gz
  md5: baf9aa34cabd76eded8090a10fd3b4d5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gocept.arecibologger = gocept.arecibologger:main
    #
    # Would create an entry point called gocept.arecibologger that calls gocept.arecibologger.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - gocept
    - gocept.arecibologger

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: ZPL
  summary: "Arecibo patch for Zope 2's site error log."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
