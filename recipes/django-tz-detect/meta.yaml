package:
  name: django-tz-detect
  version: "0.2.2"

source:
  fn: django-tz-detect-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-tz-detect/django-tz-detect-0.2.2.tar.gz
  md5: e3746c8214c535461a8f365aa8cbe750
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-tz-detect = django-tz-detect:main
    #
    # Would create an entry point called django-tz-detect that calls django-tz-detect.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4
    - pytz

  run:
    - python
    - django >=1.4
    - pytz

test:
  # Python imports
  imports:
    - tz_detect
    - tz_detect.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/adamcharnock/django-tz-detect
  license: MIT
  summary: 'Automatic user timezone detection for django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
