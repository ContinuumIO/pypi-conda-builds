package:
  name: flask-googlemaps
  version: "0.1.5"

source:
  fn: Flask-GoogleMaps-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-GoogleMaps/Flask-GoogleMaps-0.1.5.tar.gz
  md5: 7cee4e6025c7520067ad03a063614220
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-googlemaps = flask-googlemaps:main
    #
    # Would create an entry point called flask-googlemaps that calls flask-googlemaps.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask

  run:
    - python
    - flask

test:
  # Python imports
  imports:
    - flask_googlemaps

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rochacbruno/Flask-GoogleMaps/
  license: MIT License
  summary: 'Small extension for Flask to make using Google Maps easy'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
