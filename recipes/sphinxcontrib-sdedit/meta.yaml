package:
  name: sphinxcontrib-sdedit
  version: "0.3"

source:
  fn: sphinxcontrib-sdedit-0.3.tar.gz
  url: https://pypi.python.org/packages/source/s/sphinxcontrib-sdedit/sphinxcontrib-sdedit-0.3.tar.gz
  md5: eea37b34e33199e5f50e855a6930004c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sphinxcontrib-sdedit = sphinxcontrib-sdedit:main
    #
    # Would create an entry point called sphinxcontrib-sdedit that calls sphinxcontrib-sdedit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sphinx >=0.6
    - pil

  run:
    - python
    - sphinx >=0.6
    - pil

test:
  # Python imports
  imports:
    - sphinxcontrib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/birkenfeld/sphinx-contrib
  license: BSD License
  summary: 'Sphinx extension for drawing sequence diagrams'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
