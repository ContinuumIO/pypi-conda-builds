package:
  name: redisorm
  version: "0.2.0"

source:
  fn: RedisORM-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/R/RedisORM/RedisORM-0.2.0.tar.gz
  md5: 5135c7c5eb7a3d981730bfd87e8a7842
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - redisorm = redisorm:main
    #
    # Would create an entry point called redisorm that calls redisorm.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - redis

  run:
    - python
    - redis

test:
  # Python imports
  imports:
    - redisORM
    - redisorm

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/JoshAshby/pyRedisORM/
  license: MIT License
  summary: 'Simple little ORM thing for Redis'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
