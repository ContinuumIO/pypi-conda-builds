package:
  name: scatterbytes
  version: "0.8.8"

source:
  fn: scatterbytes-0.8.8.tar.gz
  url: https://pypi.python.org/packages/source/s/scatterbytes/scatterbytes-0.8.8.tar.gz
  md5: 57bdf06950929a5f5e3d5f2f7e6ec70b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - scatterbytes = scatterbytes:main
    #
    # Would create an entry point called scatterbytes that calls scatterbytes.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - scatterbytes
    - scatterbytes.client
    - scatterbytes.compat
    - scatterbytes.storage
    - scatterbytes.testing

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://www.scatterbytes.net
  license: BSD License
  summary: 'Library and CLI for accessing the ScatterBytes Network'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
