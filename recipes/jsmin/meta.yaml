package:
  name: jsmin
  version: "2.1.1"

source:
  fn: jsmin-2.1.1.tar.gz
  url: https://pypi.python.org/packages/source/j/jsmin/jsmin-2.1.1.tar.gz
  md5: 7facb7cc42bbd35831752f65d0fdca6d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jsmin = jsmin:main
    #
    # Would create an entry point called jsmin that calls jsmin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - jsmin

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/dcs/jsmin/
  license: MIT License
  summary: 'JavaScript minifier.\nPLEASE UPDATE TO VERSION >= 2.0.6. Older versions have a serious bug related to comments.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
