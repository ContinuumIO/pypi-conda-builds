package:
  name: galaxy-fds-sdk
  version: "1.1.1"

source:
  fn: galaxy-fds-sdk-1.1.1.tar.gz
  url: https://pypi.python.org/packages/source/g/galaxy-fds-sdk/galaxy-fds-sdk-1.1.1.tar.gz
  md5: 4d90b84737a44eb664efaedab2d16d01
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - galaxy-fds-sdk = galaxy-fds-sdk:main
    #
    # Would create an entry point called galaxy-fds-sdk that calls galaxy-fds-sdk.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=2.6.0

  run:
    - python
    - requests >=2.6.0

test:
  # Python imports
  imports:
    - fds
    - fds.auth
    - fds.auth.signature
    - fds.model

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Apache License
  summary: 'Galaxy FDS SDK'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
