package:
  name: zc.form
  version: "0.3"

source:
  fn: zc.form-0.3.zip
  url: https://pypi.python.org/packages/source/z/zc.form/zc.form-0.3.zip
  md5: f15b08271df6251c869391460986fdd0
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zc.form = zc.form:main
    #
    # Would create an entry point called zc.form that calls zc.form.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - zodb3
    - pytz
    - setuptools
    - zc.resourcelibrary
    - zc.sourcefactory
    - zope.annotation
    - zope.app.pagetemplate
    - zope.cachedescriptors
    - zope.catalog
    - zope.component >=3.8
    - zope.exceptions
    - zope.formlib >=4.0
    - zope.index
    - zope.interface
    - zope.publisher
    - zope.schema >=3.6
    - zope.security

  run:
    - python
    - zodb3
    - pytz
    - setuptools
    - zc.resourcelibrary
    - zc.sourcefactory
    - zope.annotation
    - zope.app.pagetemplate
    - zope.cachedescriptors
    - zope.catalog
    - zope.component >=3.8
    - zope.exceptions
    - zope.formlib >=4.0
    - zope.index
    - zope.interface
    - zope.publisher
    - zope.schema >=3.6
    - zope.security

test:
  # Python imports
  imports:
    - zc
    - zc.form
    - zc.form.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/zc.form
  license: ZPL 2.1
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
