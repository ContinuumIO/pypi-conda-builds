package:
  name: products.stickylist
  version: "1.0a"

source:
  fn: Products.StickyList-1.0a.tar.gz
  url: https://pypi.python.org/packages/source/P/Products.StickyList/Products.StickyList-1.0a.tar.gz
  md5: 2c6852fe4896ae3a9b8c31e494a03238
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - products.stickylist = products.stickylist:main
    #
    # Would create an entry point called products.stickylist that calls products.stickylist.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - Products
    - Products.StickyList
    - Products.StickyList.StickyList

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.nidelven-it.no/d
  license: GPL
  summary: 'Enables selecting a number of items and storing that selection.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
