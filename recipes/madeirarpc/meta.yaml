package:
  name: madeirarpc
  version: "0.1"

source:
  fn: madeirarpc-0.1.tar.gz
  url: https://pypi.python.org/packages/source/m/madeirarpc/madeirarpc-0.1.tar.gz
  md5: cc92768bc6b74466bceb843a992e7551
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - madeirarpc = madeirarpc:main
    #
    # Would create an entry point called madeirarpc that calls madeirarpc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - madeirarpc

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/MadeiraCloud/madeira-rpc
  license: http://www.apache.org/licenses/LICENSE-2.0
  summary: 'MadeiraRPC is a fork of TornadoRPC, with bug fixes and feature patch'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
