package:
  name: wtforms-sqlalchemy
  version: "0.1"

source:
  fn: WTForms-SQLAlchemy-0.1.zip
  url: https://pypi.python.org/packages/source/W/WTForms-SQLAlchemy/WTForms-SQLAlchemy-0.1.zip
  md5: 75e5dd75292422b800bbdc3263810804
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - wtforms-sqlalchemy = wtforms-sqlalchemy:main
    #
    # Would create an entry point called wtforms-sqlalchemy that calls wtforms-sqlalchemy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - wtforms >=1.0.5
    - sqlalchemy >=0.7.10

  run:
    - python
    - wtforms >=1.0.5
    - sqlalchemy >=0.7.10

test:
  # Python imports
  imports:
    - wtforms_sqlalchemy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/wtforms/wtforms-sqlalchemy/
  license: BSD License
  summary: 'SQLAlchemy tools for WTForms'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
