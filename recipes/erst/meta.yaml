package:
  name: erst
  version: "0.6"

source:
  fn: Erst-0.6.tar.gz
  url: https://pypi.python.org/packages/source/E/Erst/Erst-0.6.tar.gz
  md5: 80de43347b18d74dfd7c6ac3be548e06
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - erst = erst:main
    #
    # Would create an entry point called erst that calls erst.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - mercurial
    - fusepy >=2.0.2

  run:
    - python
    - mercurial
    - fusepy >=2.0.2

test:
  # Python imports
  imports:
    - erst

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Apache License 2.0 - http://www.apache.org/licenses/LICENSE-2.0.html
  summary: 'An automatic versioning fuse filesystem'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
