package:
  name: yoinkery
  version: "0.3"

source:
  fn: yoinkery-0.3.tar.gz
  url: https://pypi.python.org/packages/source/y/yoinkery/yoinkery-0.3.tar.gz
  md5: d22a1ea8ac93ca1f41beda400cf65945
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - yoinkery = yoinkery:main
    #
    # Would create an entry point called yoinkery that calls yoinkery.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - numpy
    - scipy
    - matplotlib >=1.2

  run:
    - python
    - numpy
    - scipy
    - matplotlib >=1.2

test:
  # Python imports
  imports:
    - yoink
    - yoink.data
    - yoink.test
    - yoinkery

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://yoinkery.com
  license: BSD License
  summary: 'Yoinking data from rasterized images'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
