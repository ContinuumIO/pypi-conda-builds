package:
  name: linshare_cli
  version: "0.1.0"

source:
  fn: linshare_cli-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/l/linshare_cli/linshare_cli-0.1.0.tar.gz
  md5: e32ff7aa8dd52ead72f8198ca9631499
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - linshare_cli = linshare_cli:main
    #
    # Would create an entry point called linshare_cli that calls linshare_cli.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - argtoolbox ==0.0.8
    - poster
    - progressbar
    - veryprettytable
    - hurry.filesize

  run:
    - python
    - argtoolbox ==0.0.8
    - poster
    - progressbar
    - veryprettytable
    - hurry.filesize

test:
  # Python imports
  imports:
    - linshare_cli

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/fred49/linshare-cli
  license: GNU General Public License v3 (GPLv3)
  summary: 'LinShare command line interface.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
