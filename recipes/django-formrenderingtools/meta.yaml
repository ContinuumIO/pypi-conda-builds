package:
  name: django-formrenderingtools
  version: "0.2.3"

source:
  fn: django-formrenderingtools-0.2.3.zip
  url: https://pypi.python.org/packages/source/d/django-formrenderingtools/django-formrenderingtools-0.2.3.zip
  md5: 18b3a64f99b31aaa383aefe29605e9c2
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-formrenderingtools = django-formrenderingtools:main
    #
    # Would create an entry point called django-formrenderingtools that calls django-formrenderingtools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.0
    - django-templateaddons >=0.1

  run:
    - python
    - setuptools
    - django >=1.0
    - django-templateaddons >=0.1

test:
  # Python imports
  imports:
    - djc

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/benoitbryon/django-formrenderingtools
  license: BSD License
  summary: 'Template-based rendering of Django forms layouts (not widgets).'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
