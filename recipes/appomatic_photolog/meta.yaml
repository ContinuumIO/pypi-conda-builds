package:
  name: appomatic_photolog
  version: "0.0.30"

source:
  fn: appomatic_photolog-0.0.30.tar.gz
  url: https://pypi.python.org/packages/source/a/appomatic_photolog/appomatic_photolog-0.0.30.tar.gz
  md5: b9d1921e2b9a4f02cfa06efce2955ff7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - appomatic_photolog = appomatic_photolog:main
    #
    # Would create an entry point called appomatic_photolog that calls appomatic_photolog.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-photologue
    - appomaticcore ==0.0.30

  run:
    - python
    - django-photologue
    - appomaticcore ==0.0.30

test:
  # Python imports
  imports:
    - appomatic_photolog

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/django-photologue/
  license: GPL
  summary: 'Photologue is a reusable Django application that provides powerful image management and manipulation functionality as well as a complete photo gallery solution.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
