package:
  name: django-custard
  version: "0.9"

source:
  fn: django-custard-0.9.tar.gz
  url: https://pypi.python.org/packages/source/d/django-custard/django-custard-0.9.tar.gz
  md5: d20145f46f584dc407d66d67caad601f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-custard = django-custard:main
    #
    # Would create an entry point called django-custard that calls django-custard.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6

  run:
    - python
    - django >=1.6

test:
  # Python imports
  imports:
    - custard
    - custard.templatetags
    - custard.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kunitoki/django-custard
  license: MIT License
  summary: 'Django runtime typed custom fields for any model.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
