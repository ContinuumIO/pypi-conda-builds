package:
  name: redomino.css3theme
  version: "1.5.10"

source:
  fn: redomino.css3theme-1.5.10.zip
  url: https://pypi.python.org/packages/source/r/redomino.css3theme/redomino.css3theme-1.5.10.zip
  md5: ea13a94e80c866b70555e72b3fcc7343
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - redomino.css3theme = redomino.css3theme:main
    #
    # Would create an entry point called redomino.css3theme that calls redomino.css3theme.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - redomino
    - redomino.css3theme
    - redomino.css3theme.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/redomino/redomino.css3theme
  license: GNU General Public License (GPL)
  summary: 'A responsive (and mobile) theme for Plone 4 (based on sunburst)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
