package:
  name: jsonfield
  version: "1.0.3"

source:
  fn: jsonfield-1.0.3.tar.gz
  url: https://pypi.python.org/packages/source/j/jsonfield/jsonfield-1.0.3.tar.gz
  md5: a7c7df31627069a97c9ba91b599c0845
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jsonfield = jsonfield:main
    #
    # Would create an entry point called jsonfield that calls jsonfield.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django >=1.4.3

  run:
    - python
    - django >=1.4.3

test:
  # Python imports
  imports:
    - jsonfield

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bradjasper/django-jsonfield/
  license: MIT
  summary: 'A reusable Django field that allows you to store validated JSON in your model.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
