package:
  name: collective.validator.css
  version: "1.0.2beta"

source:
  fn: collective.validator.css-1.0.2beta.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.validator.css/collective.validator.css-1.0.2beta.tar.gz
  md5: 94f914c99d0735e6b457b4245ffcfb23
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.validator.css = collective.validator.css:main
    #
    # Would create an entry point called collective.validator.css that calls collective.validator.css.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.validator
    - collective.validator.css
    - collective.validator.css.interfaces
    - collective.validator.css.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://svn.plone.org/svn/collective/collective.validator.css
  license: GPL
  summary: 'CSS Validator for collective.validator.base package'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
