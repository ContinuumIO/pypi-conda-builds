package:
  name: quintagroup.portlet.cumulus
  version: "1.2"

source:
  fn: quintagroup.portlet.cumulus-1.2.zip
  url: https://pypi.python.org/packages/source/q/quintagroup.portlet.cumulus/quintagroup.portlet.cumulus-1.2.zip
  md5: f0e32f6718a1d656657af50978767d8e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - quintagroup.portlet.cumulus = quintagroup.portlet.cumulus:main
    #
    # Would create an entry point called quintagroup.portlet.cumulus that calls quintagroup.portlet.cumulus.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - quintagroup
    - quintagroup.portlet
    - quintagroup.portlet.cumulus
    - quintagroup.portlet.cumulus.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.quintagroup.com/products/quintagroup.portlet.cumulus
  license: GPL
  summary: 'A tag cloud portlet that rotates tags in 3D using a Flash movie'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
