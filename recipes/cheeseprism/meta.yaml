package:
  name: cheeseprism
  version: "0.4.0b5"

source:
  fn: CheesePrism-0.4.0b5.tar.gz
  url: https://pypi.python.org/packages/source/C/CheesePrism/CheesePrism-0.4.0b5.tar.gz
  md5: 63a24568bc608d4fe7f5be7d559a84a5
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cheeseprism = cheeseprism:main
    #
    # Would create an entry point called cheeseprism that calls cheeseprism.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - jinja2
    - paste
    - futures
    - path.py
    - pkginfo >=1.2b1
    - pyramid
    - pyramid_jinja2
    - requests
    - pip <1.6,>1.4
    - more-itertools

  run:
    - python
    - setuptools
    - jinja2
    - paste
    - futures
    - path.py
    - pkginfo >=1.2b1
    - pyramid
    - pyramid_jinja2
    - requests
    - pip <1.6,>1.4
    - more-itertools

test:
  # Python imports
  imports:
    - cheeseprism

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/whitmo/CheesePrism
  license: UNKNOWN
  summary: 'CheesePrism: your personal cheeseshop'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
