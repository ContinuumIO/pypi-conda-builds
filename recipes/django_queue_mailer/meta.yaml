package:
  name: django_queue_mailer
  version: "0.1.7b"

source:
  fn: django_queue_mailer-0.1.7b.tar.gz
  url: https://pypi.python.org/packages/source/d/django_queue_mailer/django_queue_mailer-0.1.7b.tar.gz
  md5: 7483aebea22c8ee36c7dc4c2d95ac9e8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_queue_mailer = django_queue_mailer:main
    #
    # Would create an entry point called django_queue_mailer that calls django_queue_mailer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_queue_mailer
    - django_queue_mailer.management
    - django_queue_mailer.management.commands
    - django_queue_mailer.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/edke/django-queue-mailer
  license: MIT License
  summary: 'A reusable Django app for controlling queuing and sending of app emails'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
