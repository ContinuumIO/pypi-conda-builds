package:
  name: medialog.qrcode
  version: "0.2"

source:
  fn: medialog.qrcode-0.2.tar.gz
  url: https://pypi.python.org/packages/source/m/medialog.qrcode/medialog.qrcode-0.2.tar.gz
  md5: 47b23c572f917f271aed8d7276598034
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - medialog.qrcode = medialog.qrcode:main
    #
    # Would create an entry point called medialog.qrcode that calls medialog.qrcode.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - qrcode

  run:
    - python
    - setuptools
    - setuptools
    - qrcode

test:
  # Python imports
  imports:
    - medialog
    - medialog.qrcode
    - medialog.qrcode.browser
    - medialog.qrcode.portlets
    - medialog.qrcode.viewlets

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/espenmn/medialog.qrcode/
  license: GPL
  summary: 'QRcode views and portlet for Plone.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
