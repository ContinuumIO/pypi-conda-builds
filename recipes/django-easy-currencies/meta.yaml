package:
  name: django-easy-currencies
  version: "0.2.1"

source:
  fn: django-easy-currencies-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-easy-currencies/django-easy-currencies-0.2.1.tar.gz
  md5: 41a82b7104f1321b6493a5923a0b5437
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-easy-currencies = django-easy-currencies:main
    #
    # Would create an entry point called django-easy-currencies that calls django-easy-currencies.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - babel >=1.3

  run:
    - python
    - django
    - babel >=1.3

test:
  # Python imports
  imports:
    - django_easy_currencies

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/daveoncode/django-easy-currencies
  license: MIT License
  summary: 'Simple app to manage currencies conversion in Django using openexchangerates.org service.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
