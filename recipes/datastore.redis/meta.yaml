package:
  name: datastore.redis
  version: "0.1.3"

source:
  fn: datastore.redis-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/d/datastore.redis/datastore.redis-0.1.3.tar.gz
  md5: 3c544c27019bf9cba210766d2a074468
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - datastore.redis = datastore.redis:main
    #
    # Would create an entry point called datastore.redis that calls datastore.redis.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - datastore >=0.3.3
    - redis >=2.7.2

  run:
    - python
    - datastore >=0.3.3
    - redis >=2.7.2

test:
  # Python imports
  imports:
    - datastore
    - datastore.redis

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/datastore/datastore.redis
  license: MIT License
  summary: 'redis datastore implementation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
