package:
  name: mongoengine-mls
  version: "1.1.0"

source:
  fn: mongoengine-mls-1.1.0.tar.gz
  url: https://pypi.python.org/packages/source/m/mongoengine-mls/mongoengine-mls-1.1.0.tar.gz
  md5: 9b522b7e89e5dd8ed958b4c8d01f1864
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mongoengine-mls = mongoengine-mls:main
    #
    # Would create an entry point called mongoengine-mls that calls mongoengine-mls.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - mongoengine
    - mls
    - pymongo <3.0

  run:
    - python
    - mongoengine
    - mls
    - pymongo <3.0

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rembish/mongoengine-mls
  license: BSD License
  summary: 'MultiLingualField for MongoEngine'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
