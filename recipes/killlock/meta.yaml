package:
  name: killlock
  version: "0.1.3"

source:
  fn: KillLock-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/K/KillLock/KillLock-0.1.3.tar.gz
  md5: afd4128a2d470fd2eccfbd15a9055f01
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - killlock = killlock:main
    #
    # Would create an entry point called killlock that calls killlock.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - killlock

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://geeknirvana.org
  license: GNU General Public License v3 (GPLv3)
  summary: 'This program is written to reset those pesky little Android Apps that restrict fromm doing stuff'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
