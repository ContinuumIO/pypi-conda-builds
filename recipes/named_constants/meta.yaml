package:
  name: named_constants
  version: "1.0"

source:
  fn: named_constants-1.0.tar.gz
  url: https://pypi.python.org/packages/source/n/named_constants/named_constants-1.0.tar.gz
  md5: 89510de8206d93c3474394d86f843dd3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - named_constants = named_constants:main
    #
    # Would create an entry point called named_constants that calls named_constants.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/hmeine/named_constants
  license: Apache Software License
  summary: 'This module offers an easy way to define named constants in Python, supporting some simple enum-like use case, as well as arbitrary value types like strings or floats.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
