package:
  name: gillcup
  version: "0.2.1"

source:
  fn: gillcup-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/g/gillcup/gillcup-0.2.1.tar.gz
  md5: cd137f3193b8f961f7ce99e000d8c28a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gillcup = gillcup:main
    #
    # Would create an entry point called gillcup that calls gillcup.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six >=1.1

  run:
    - python
    - six >=1.1

test:
  # Python imports
  imports:
    - gillcup
    - gillcup.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/gillcup/
  license: MIT License
  summary: 'An animation framework for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
