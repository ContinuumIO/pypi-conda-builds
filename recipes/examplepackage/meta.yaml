package:
  name: examplepackage
  version: "0.1.3"

source:
  fn: ExamplePackage-0.1.3.zip
  url: https://pypi.python.org/packages/source/E/ExamplePackage/ExamplePackage-0.1.3.zip
  md5: 4512530886934bf4d1304aba01834c45
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - examplepackage = examplepackage:main
    #
    # Would create an entry point called examplepackage that calls examplepackage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - ExamplePackage
    - ExamplePackage.SubPackage1
    - ExamplePackage.SubPackage2
    - ExamplePackage.SubPackage2.GrandPackage
    - examplepackage

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/ExamplePackage/
  license: LICENSE.txt
  summary: 'An example package'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
