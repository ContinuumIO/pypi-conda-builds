package:
  name: django-erroneous
  version: "0.2.0"

source:
  fn: django-erroneous-0.2.0.zip
  url: https://pypi.python.org/packages/source/d/django-erroneous/django-erroneous-0.2.0.zip
  md5: 89823bf9f828e4d52164edc6519dcb51
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-erroneous = django-erroneous:main
    #
    # Would create an entry point called django-erroneous that calls django-erroneous.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4
    - south >=0.7.2

  run:
    - python
    - django >=1.4
    - south >=0.7.2

test:
  # Python imports
  imports:
    - erroneous
    - erroneous.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.org/mridang/django-erroneous
  license: BSD License
  summary: 'django-erroneous makes it easy to collect and log Django application errors.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
