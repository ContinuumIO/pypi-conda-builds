package:
  name: django_smoknur
  version: "0.3.20"

source:
  fn: django_smoknur-0.3.20.tar.gz
  url: https://pypi.python.org/packages/source/d/django_smoknur/django_smoknur-0.3.20.tar.gz
  md5: aa249317d9ab905cf21ee9f36e3a75ad
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_smoknur = django_smoknur:main
    #
    # Would create an entry point called django_smoknur that calls django_smoknur.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_smoknur
    - django_smoknur.management
    - django_smoknur.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/ilnurgi/django-smoknur
  license: UNKNOWN
  summary: 'django-smoknur'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
