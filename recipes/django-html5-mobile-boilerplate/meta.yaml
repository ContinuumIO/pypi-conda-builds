package:
  name: django-html5-mobile-boilerplate
  version: "1.0.5"

source:
  fn: django-html5-mobile-boilerplate-1.0.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-html5-mobile-boilerplate/django-html5-mobile-boilerplate-1.0.5.tar.gz
  md5: d6d3521dc0e1c44f9adacb4f0563c39e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-html5-mobile-boilerplate = django-html5-mobile-boilerplate:main
    #
    # Would create an entry point called django-html5-mobile-boilerplate that calls django-html5-mobile-boilerplate.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3
    - django-html5-boilerplate >=1.0.5

  run:
    - python
    - django >=1.3
    - django-html5-boilerplate >=1.0.5

test:
  # Python imports
  imports:
    - dh5mbp

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mattsnider/django-html5-boilerplate
  license: MIT License
  summary: 'A framework that includes the Django HTML5 boilerplate template into your django project and augments it with mobile support.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
