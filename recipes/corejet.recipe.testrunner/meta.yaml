package:
  name: corejet.recipe.testrunner
  version: "1.0.0"

source:
  fn: corejet.recipe.testrunner-1.0.0.zip
  url: https://pypi.python.org/packages/source/c/corejet.recipe.testrunner/corejet.recipe.testrunner-1.0.0.zip
  md5: d908e6463f486a14543f7e3345b6e34e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - corejet.recipe.testrunner = corejet.recipe.testrunner:main
    #
    # Would create an entry point called corejet.recipe.testrunner that calls corejet.recipe.testrunner.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - zc.recipe.egg

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - zc.recipe.egg

test:
  # Python imports
  imports:
    - corejet
    - corejet.recipe
    - corejet.recipe.testrunner

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/datakurre/corejet.recipe.testrunner/
  license: ZPL 2.1
  summary: 'ZC Buildout recipe for creating CoreJet test runners'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
