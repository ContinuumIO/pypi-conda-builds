package:
  name: slikio-python
  version: "0.0.3.2"

source:
  fn: slikio-python-0.0.3.2.tar.gz
  url: https://pypi.python.org/packages/source/s/slikio-python/slikio-python-0.0.3.2.tar.gz
  md5: 615dfdb77e54425e03c5d9b04b3bfccb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - slikio-python = slikio-python:main
    #
    # Would create an entry point called slikio-python that calls slikio-python.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - slikio

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://slik.io
  license: MIT
  summary: 'Python library for SlikIO - Charts for developers'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
