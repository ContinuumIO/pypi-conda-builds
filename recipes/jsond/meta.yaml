package:
  name: jsond
  version: "1.0.0"

source:
  fn: jsond-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/j/jsond/jsond-1.0.0.tar.gz
  md5: 343845220a3483b6ca59d5ea6255579c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jsond = jsond:main
    #
    # Would create an entry point called jsond that calls jsond.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - nose ==1.3.0
    - python-dateutil ==1.5

  run:
    - python
    - nose ==1.3.0
    - python-dateutil ==1.5

test:
  # Python imports
  imports:
    - jsond

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/EDITD/jsond
  license: LICENSE.txt
  summary: 'JSON (with dates)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
