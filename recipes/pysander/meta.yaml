package:
  name: pysander
  version: "0.1.5"

source:
  fn: pysander-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/p/pysander/pysander-0.1.5.tar.gz
  md5: 7a3e40d1bdd5c6e4a66482f9d070f893
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pysander = pysander:main
    #
    # Would create an entry point called pysander that calls pysander.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sqlalchemy
    - flask

  run:
    - python
    - sqlalchemy
    - flask

test:
  # Python imports
  imports:
    - pysander

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/lvanspronsen/pysander
  license: MIT License
  summary: 'A python framework for building RESTful crud APIs'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
