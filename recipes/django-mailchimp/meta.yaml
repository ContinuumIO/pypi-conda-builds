package:
  name: django-mailchimp
  version: "0.1.30"

source:
  fn: django-mailchimp-0.1.30.tar.gz
  url: https://pypi.python.org/packages/source/d/django-mailchimp/django-mailchimp-0.1.30.tar.gz
  md5: f99453b66bfda991691d2b1a91858c53
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-mailchimp = django-mailchimp:main
    #
    # Would create an entry point called django-mailchimp that calls django-mailchimp.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - mailchimp
    - mailchimp.chimpy
    - mailchimp.management
    - mailchimp.management.commands
    - mailchimp.migrations
    - mailchimp.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/piquadrat/django-mailchimp
  license: UNKNOWN
  summary: 'Mailchimp wrapper for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
