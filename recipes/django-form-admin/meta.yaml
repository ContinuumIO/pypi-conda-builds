package:
  name: django-form-admin
  version: "0.4.2"

source:
  fn: django-form-admin-0.4.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-form-admin/django-form-admin-0.4.2.tar.gz
  md5: 74b1a855ddad8603e37d1d56eaaa1545
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-form-admin = django-form-admin:main
    #
    # Would create an entry point called django-form-admin that calls django-form-admin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - formadmin
    - formadmin.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Yaco-Sistemas/django-form-admin
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Abstract class implemented to provide form django admin like'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
