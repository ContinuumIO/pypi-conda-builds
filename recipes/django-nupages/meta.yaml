package:
  name: django-nupages
  version: "0.1.7"

source:
  fn: django-nupages-0.1.7.tar.gz
  url: https://pypi.python.org/packages/source/d/django-nupages/django-nupages-0.1.7.tar.gz
  md5: 5c5da5f06702b0a83e3797c9abc95a1e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-nupages = django-nupages:main
    #
    # Would create an entry point called django-nupages that calls django-nupages.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-extensions ==1.3.3
    - django-positions ==0.5.1

  run:
    - python
    - django-extensions ==1.3.3
    - django-positions ==0.5.1

test:
  # Python imports
  imports:
    - nupages

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/goldhand/django-nupages
  license: BSD License
  summary: 'Pages application for django projects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
