package:
  name: komandr
  version: "0.1.1"

source:
  fn: komandr-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/k/komandr/komandr-0.1.1.tar.gz
  md5: 9596ea0277f8bc4b0b213d08305212d2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - komandr = komandr:main
    #
    # Would create an entry point called komandr that calls komandr.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - komandr

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kadirpekel/komandr
  license: UNKNOWN
  summary: 'Coolest command-line interface on this planet'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
