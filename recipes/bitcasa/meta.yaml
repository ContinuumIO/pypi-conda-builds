package:
  name: bitcasa
  version: "1.0"

source:
  fn: bitcasa-1.0.tar.gz
  url: https://pypi.python.org/packages/source/b/bitcasa/bitcasa-1.0.tar.gz
  md5: 65d9d7919879c5c8ff9a193f72c49f83
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bitcasa = bitcasa:main
    #
    # Would create an entry point called bitcasa that calls bitcasa.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=1.2.3
    - nose
    - nose-testconfig

  run:
    - python
    - requests >=1.2.3
    - nose
    - nose-testconfig

test:
  # Python imports
  imports:
    - bitcasa
    - bitcasa.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.bitcasa.com/
  license: MIT License
  summary: 'This is the Python SDK for the Bitcasa API.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
