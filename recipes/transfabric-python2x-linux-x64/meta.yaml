package:
  name: transfabric-python2x-linux-x64
  version: "1.0"

source:
  fn: TransFabric-Python2X-Linux-x64-1.0.tar.gz
  url: https://pypi.python.org/packages/source/T/TransFabric-Python2X-Linux-x64/TransFabric-Python2X-Linux-x64-1.0.tar.gz
  md5: 471b61cb3e17ec1e415275842c29b868
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - transfabric-python2x-linux-x64 = transfabric-python2x-linux-x64:main
    #
    # Would create an entry point called transfabric-python2x-linux-x64 that calls transfabric-python2x-linux-x64.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - TransFabric

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: transfluent
  summary: 'TransFabric Parser for Python 2X, for hardware Linux-x64'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
