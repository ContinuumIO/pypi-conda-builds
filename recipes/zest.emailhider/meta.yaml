package:
  name: zest.emailhider
  version: "2.7"

source:
  fn: zest.emailhider-2.7.zip
  url: https://pypi.python.org/packages/source/z/zest.emailhider/zest.emailhider-2.7.zip
  md5: e6a996397d0bdb6d0c1669a6d76890e4
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zest.emailhider = zest.emailhider:main
    #
    # Would create an entry point called zest.emailhider that calls zest.emailhider.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - jquery.pyproxy >=0.3.1

  run:
    - python
    - setuptools
    - setuptools
    - jquery.pyproxy >=0.3.1

test:
  # Python imports
  imports:
    - zest
    - zest.emailhider
    - zest.emailhider.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/zestsoftware/zest.emailhider
  license: GPL
  summary: 'A simple jQuery component for hiding email addresses from spammers.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
