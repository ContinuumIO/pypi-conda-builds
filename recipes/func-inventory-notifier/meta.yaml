package:
  name: func-inventory-notifier
  version: "0.2.3a12"

source:
  fn: func-inventory-notifier-0.2.3a12.tar.gz
  url: https://pypi.python.org/packages/source/f/func-inventory-notifier/func-inventory-notifier-0.2.3a12.tar.gz
  md5: 08d696d7e7ccc8326e4a2d23284fb849
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - func-inventory-notifier = func-inventory-notifier:main
    #
    # Would create an entry point called func-inventory-notifier that calls func-inventory-notifier.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - ansi2html
    - pytidylib
    - python-premailer

  run:
    - python
    - ansi2html
    - pytidylib
    - python-premailer

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/ralphbean/func-inventory-notifier
  license: UNKNOWN
  summary: 'Send colorized HTML notifications of func-inventory'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
