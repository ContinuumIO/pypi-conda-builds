package:
  name: django-good-practice-examples
  version: "1.0"

source:
  fn: django-good-practice-examples-1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-good-practice-examples/django-good-practice-examples-1.0.tar.gz
  md5: 866e7ab1615f7eefa0f501dd0d23cbb7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-good-practice-examples = django-good-practice-examples:main
    #
    # Would create an entry point called django-good-practice-examples that calls django-good-practice-examples.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - south
    - django-libs
    - pillow
    - djangocms_utils
    - django-countries
    - django-multilingual-news

  run:
    - python
    - django
    - south
    - django-libs
    - pillow
    - djangocms_utils
    - django-countries
    - django-multilingual-news

test:
  # Python imports
  imports:
    - good_practice_examples
    - good_practice_examples.migrations
    - good_practice_examples.tests
    - good_practice_examples.tests.integration_tests
    - good_practice_examples.tests.test_app

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bitmazk/django-good-practice-examples
  license: The MIT License
  summary: 'An app for managing and displaying good practice examples for different\npurposes.\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
