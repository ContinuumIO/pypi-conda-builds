package:
  name: collective.syndication
  version: "1.0b3"

source:
  fn: collective.syndication-1.0b3.zip
  url: https://pypi.python.org/packages/source/c/collective.syndication/collective.syndication-1.0b3.zip
  md5: 52f3d86533ca54a367c194f741b04101
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.syndication = collective.syndication:main
    #
    # Would create an entry point called collective.syndication that calls collective.syndication.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - lxml
    - plone.api
    - plone.app.registry
    - plone.dexterity
    - plone.memoize
    - plone.namedfile
    - plone.registry
    - plone.rfc822
    - plone.uuid
    - products.cmfcore
    - products.cmfdefault
    - products.cmfplone >=4.1,<4.3
    - products.genericsetup
    - setuptools
    - zope.component
    - zope.i18nmessageid
    - zope.interface
    - zope.schema

  run:
    - python
    - setuptools
    - lxml
    - plone.api
    - plone.app.registry
    - plone.dexterity
    - plone.memoize
    - plone.namedfile
    - plone.registry
    - plone.rfc822
    - plone.uuid
    - products.cmfcore
    - products.cmfdefault
    - products.cmfplone >=4.1,<4.3
    - products.genericsetup
    - setuptools
    - zope.component
    - zope.i18nmessageid
    - zope.interface
    - zope.schema

test:
  # Python imports
  imports:
    - collective
    - collective.syndication
    - collective.syndication.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/collective/collective.syndication
  license: GNU General Public License v2 (GPLv2)
  summary: 'Improved syndication for Plone sites providing Atom, iTunes, NewsML 1, RSS 1.0 and RSS 2.0 feeds.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
