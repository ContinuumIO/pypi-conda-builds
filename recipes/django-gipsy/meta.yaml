package:
  name: django-gipsy
  version: "1.1.2"

source:
  fn: django-gipsy-1.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-gipsy/django-gipsy-1.1.2.tar.gz
  md5: 89036008f1dfa3f76f701b40a86d107c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-gipsy = django-gipsy:main
    #
    # Would create an entry point called django-gipsy that calls django-gipsy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6
    - google-api-python-client ==1.3.1

  run:
    - python
    - django >=1.6
    - google-api-python-client ==1.3.1

test:
  # Python imports
  imports:
    - gipsy
    - gipsy.dashboard
    - gipsy.dashboard.migrations
    - gipsy.dashboard.presets
    - gipsy.dashboard.services
    - gipsy.dashboard.templatetags
    - gipsy.dashboard.widgets
    - gipsy.toolbar
    - gipsy.toolbar.migrations
    - gipsy.toolbar.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.revsquare.com
  license: BSD License
  summary: 'A set of fancy tools for django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
