package:
  name: mplayerctrl
  version: "0.3.3"

source:
  fn: MplayerCtrl-0.3.3.tar.gz
  url: https://pypi.python.org/packages/source/M/MplayerCtrl/MplayerCtrl-0.3.3.tar.gz
  md5: 848c4ab30968ca9f9a9fd521f7fa10a9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mplayerctrl = mplayerctrl:main
    #
    # Would create an entry point called mplayerctrl that calls mplayerctrl.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - mplayerctrl

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://mplayerctrl.dav1d.de
  license: MIT License
  summary: 'A wx.Panel, which wraps the Mplayer into wxPython'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
