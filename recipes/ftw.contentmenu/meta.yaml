package:
  name: ftw.contentmenu
  version: "2.4.0"

source:
  fn: ftw.contentmenu-2.4.0.zip
  url: https://pypi.python.org/packages/source/f/ftw.contentmenu/ftw.contentmenu-2.4.0.zip
  md5: e7bd56ef0e2d2623f3c246dfdc5622d5
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ftw.contentmenu = ftw.contentmenu:main
    #
    # Would create an entry point called ftw.contentmenu that calls ftw.contentmenu.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - acquisition
    - zope.component
    - zope.i18n
    - zope.interface
    - plone.app.contentmenu
    - plone.app.upgrade
    - products.cmfcore
    - products.cmfplone

  run:
    - python
    - setuptools
    - setuptools
    - acquisition
    - zope.component
    - zope.i18n
    - zope.interface
    - plone.app.contentmenu
    - plone.app.upgrade
    - products.cmfcore
    - products.cmfplone

test:
  # Python imports
  imports:
    - ftw
    - ftw.contentmenu
    - ftw.contentmenu.tests
    - ftw.contentmenu.upgrades

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/4teamwork/ftw.contentmenu
  license: GPL2
  summary: "Customize Plone's content menu"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
