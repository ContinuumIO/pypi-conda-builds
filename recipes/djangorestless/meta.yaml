package:
  name: djangorestless
  version: "0.0.10"

source:
  fn: DjangoRestless-0.0.10.tar.gz
  url: https://pypi.python.org/packages/source/D/DjangoRestless/DjangoRestless-0.0.10.tar.gz
  md5: 5958116aac4f403cf5cc024f58bf456a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangorestless = djangorestless:main
    #
    # Would create an entry point called djangorestless that calls djangorestless.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5
    - six >=1.3.0

  run:
    - python
    - django >=1.5
    - six >=1.3.0

test:
  # Python imports
  imports:
    - djangorestless
    - restless

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dobarkod/django-restless
  license: MIT License
  summary: 'A RESTful framework for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
