package:
  name: android-asset-resizer
  version: "1.2.1"

source:
  fn: android-asset-resizer-1.2.1.tar.gz
  url: https://pypi.python.org/packages/source/a/android-asset-resizer/android-asset-resizer-1.2.1.tar.gz
  md5: d28ab9382a9fdc5ce05bda39f93604e1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - android-asset-resizer = android-asset-resizer:main
    #
    # Would create an entry point called android-asset-resizer that calls android-asset-resizer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pil

  run:
    - python
    - pil

test:
  # Python imports
  imports:
    - android_asset_resizer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/twaddington/android-asset-resizer
  license: Apache Software License
  summary: 'A command-line utility for generating Android drawables in the required densities.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
