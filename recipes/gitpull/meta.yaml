package:
  name: gitpull
  version: "0.2"

source:
  fn: gitpull-0.2.tar.gz
  url: https://pypi.python.org/packages/source/g/gitpull/gitpull-0.2.tar.gz
  md5: 8c89e1f679b8303db6fb44e49b17ded8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gitpull = gitpull:main
    #
    # Would create an entry point called gitpull that calls gitpull.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - termcolor
    - progressbar2
    - prettytable

  run:
    - python
    - termcolor
    - progressbar2
    - prettytable

test:
  # Python imports
  imports:
    - gitpull

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/adamatan/gitpull
  license: MIT
  summary: 'A python script for batch-pull git repos'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
