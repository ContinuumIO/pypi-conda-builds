package:
  name: cartesius
  version: "0.2.9b"

source:
  fn: cartesius-0.2.9b.tar.gz
  url: https://pypi.python.org/packages/source/c/cartesius/cartesius-0.2.9b.tar.gz
  md5: 6e101a1ceb37e5348bbdb126c214cae8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cartesius = cartesius:main
    #
    # Would create an entry point called cartesius that calls cartesius.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - cartesius
    - cartesius/fonts

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://tkrajina.github.com/cartesius/
  license: Apache License, Version 2.0
  summary: 'Cartesius 2D coordinate system drawing library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
