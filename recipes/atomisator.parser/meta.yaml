package:
  name: atomisator.parser
  version: "0.2.7"

source:
  fn: atomisator.parser-0.2.7.tar.gz
  url: https://pypi.python.org/packages/source/a/atomisator.parser/atomisator.parser-0.2.7.tar.gz
  md5: b2ae7ac6b1d503ffdd9667d10fedf4df
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - atomisator.parser = atomisator.parser:main
    #
    # Would create an entry point called atomisator.parser that calls atomisator.parser.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - feedparser

  run:
    - python
    - setuptools
    - setuptools
    - feedparser

test:
  # Python imports
  imports:
    - atomisator
    - atomisator.parser
    - atomisator.parser.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://atomisator.ziade.org
  license: GPL
  summary: 'Expert Python Programming - A thin layer on the top of the Universal Feed Parser'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
