package:
  name: django-forceadminlanguage
  version: "0.0.1"

source:
  fn: django-forceadminlanguage-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-forceadminlanguage/django-forceadminlanguage-0.0.1.tar.gz
  md5: 264933b642ab152aba01d9e2101095e8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-forceadminlanguage = django-forceadminlanguage:main
    #
    # Would create an entry point called django-forceadminlanguage that calls django-forceadminlanguage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - forceadminlanguage

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Brick85/django_forceadminlanguage/
  license: BSD License
  summary: 'Force Django Admin to show in one selected language.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
