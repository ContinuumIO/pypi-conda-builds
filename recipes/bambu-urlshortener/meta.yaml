package:
  name: bambu-urlshortener
  version: "2.0"

source:
  fn: bambu-urlshortener-2.0.tar.gz
  url: https://pypi.python.org/packages/source/b/bambu-urlshortener/bambu-urlshortener-2.0.tar.gz
  md5: b590e37cd7585e602860acd7bf63eb39
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bambu-urlshortener = bambu-urlshortener:main
    #
    # Would create an entry point called bambu-urlshortener that calls bambu-urlshortener.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4
    - requests >=2.0

  run:
    - python
    - django >=1.4
    - requests >=2.0

test:
  # Python imports
  imports:
    - bambu_urlshortener
    - bambu_urlshortener.migrations
    - bambu_urlshortener.providers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/iamsteadman/bambu-urlshortener
  license: UNKNOWN
  summary: 'Shrink a URL using an internal or external shortening service'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
