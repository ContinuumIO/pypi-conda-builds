package:
  name: buildout.dumppickedversions2
  version: "1.1"

source:
  fn: buildout.dumppickedversions2-1.1.zip
  url: https://pypi.python.org/packages/source/b/buildout.dumppickedversions2/buildout.dumppickedversions2-1.1.zip
  md5: f52693a57c9ce84a70d5c29d75c2f5bf
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - buildout.dumppickedversions2 = buildout.dumppickedversions2:main
    #
    # Would create an entry point called buildout.dumppickedversions2 that calls buildout.dumppickedversions2.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zc.buildout >=2.0.1

  run:
    - python
    - setuptools
    - zc.buildout >=2.0.1

test:
  # Python imports
  imports:
    - buildout
    - buildout.dumppickedversions2

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/eightmedia/buildout.dumppickedversions2
  license: MIT
  summary: 'Dump buildout 2 picked versions.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
