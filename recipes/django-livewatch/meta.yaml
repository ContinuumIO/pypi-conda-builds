package:
  name: django-livewatch
  version: "0.2.5"

source:
  fn: django-livewatch-0.2.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-livewatch/django-livewatch-0.2.5.tar.gz
  md5: 265a686cfdb81f844cd1bd41c983f051
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-livewatch = django-livewatch:main
    #
    # Would create an entry point called django-livewatch that calls django-livewatch.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5

  run:
    - python
    - django >=1.5

test:
  # Python imports
  imports:
    - livewatch
    - livewatch.extensions

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/moccu/django-livewatch/
  license: Apache Software License
  summary: 'django-livewatch integrates livewatch.de for django projects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
