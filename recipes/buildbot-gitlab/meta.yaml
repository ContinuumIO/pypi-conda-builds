package:
  name: buildbot-gitlab
  version: "0.1.2"

source:
  fn: buildbot-gitlab-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/b/buildbot-gitlab/buildbot-gitlab-0.1.2.tar.gz
  md5: 21d39a8a23ff28693c2cb25e32a6f8c6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - buildbot-gitlab = buildbot-gitlab:main
    #
    # Would create an entry point called buildbot-gitlab that calls buildbot-gitlab.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - buildbot.gitlab
    - buildbot.gitlab.api
    - buildbot.gitlab.monkey_patches
    - buildbot.gitlab.monkey_patches.buildbot
    - buildbot.gitlab.monkey_patches.buildbot.process
    - buildbot.gitlab.monkey_patches.buildbot.process.build
    - buildbot.gitlab.monkey_patches.buildbot.status
    - buildbot.gitlab.monkey_patches.buildbot.status.progress

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://dev.vcatechnology.com/gitlab/tool-chain/buildbot-gitlab
  license: BSD License
  summary: 'Enhances a BuildBot installation to nicely integrate with GitLab'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
