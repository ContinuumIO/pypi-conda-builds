package:
  name: redsolutioncms.django-photologue
  version: "0.1.0"

source:
  fn: redsolutioncms.django-photologue-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/r/redsolutioncms.django-photologue/redsolutioncms.django-photologue-0.1.0.tar.gz
  md5: 4638adbc4357e788d9a17db2cdf3983f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - redsolutioncms.django-photologue = redsolutioncms.django-photologue:main
    #
    # Would create an entry point called redsolutioncms.django-photologue that calls redsolutioncms.django-photologue.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-photologue

  run:
    - python
    - setuptools
    - django-photologue

test:
  # Python imports
  imports:
    - redsolution_setup

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: GNU General Public License (GPL)
  summary: "It's an application, that provides your an photologue."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
