package:
  name: carteblanche
  version: "1.0.0"

source:
  fn: carteblanche-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/c/carteblanche/carteblanche-1.0.0.tar.gz
  md5: 3424e407619dac11fed8a07675bdce90
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - carteblanche = carteblanche:main
    #
    # Would create an entry point called carteblanche that calls carteblanche.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - carteblanche

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/neuman/python-carteblanche
  license: UNKNOWN
  summary: 'Module to hold and serialize the in-memory relationship between urls and objects and users.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
