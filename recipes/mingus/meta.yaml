package:
  name: mingus
  version: "0.5.0"

source:
  fn: mingus-0.5.0.tar.gz
  url: https://pypi.python.org/packages/source/m/mingus/mingus-0.5.0.tar.gz
  md5: fa1140e4fe4c22d9baf6ab864b32adb8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mingus = mingus:main
    #
    # Would create an entry point called mingus that calls mingus.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - mingus
    - mingus.containers
    - mingus.core
    - mingus.extra
    - mingus.midi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://mingus.googlecode.com/
  license: GNU General Public License (GPL)
  summary: 'mingus is a music package for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
