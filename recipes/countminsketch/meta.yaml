package:
  name: countminsketch
  version: "0.2"

source:
  fn: countminsketch-0.2.tar.gz
  url: https://pypi.python.org/packages/source/c/countminsketch/countminsketch-0.2.tar.gz
  md5: ac790e0f22cc2a16a73f06d78c897394
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - countminsketch = countminsketch:main
    #
    # Would create an entry point called countminsketch that calls countminsketch.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - countminsketch

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rafacarrascosa/countminsketch
  license: BSD License
  summary: 'A minimalistic Count-min Sketch for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
