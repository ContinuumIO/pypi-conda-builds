package:
  name: ldpatch
  version: "0.9"

source:
  fn: ldpatch-0.9.tar.gz
  url: https://pypi.python.org/packages/source/l/ldpatch/ldpatch-0.9.tar.gz
  md5: 2d5bf79e72e8febe09dfb50d65874282
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ldpatch = ldpatch:main
    #
    # Would create an entry point called ldpatch that calls ldpatch.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - rdflib ==4.2.0
    - rfc3987

  run:
    - python
    - rdflib ==4.2.0
    - rfc3987

test:
  # Python imports
  imports:
    - ldpatch

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/pchampin/ld-patch-py
  license: LGPL v3
  summary: 'A kernel for trace-based systems'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
