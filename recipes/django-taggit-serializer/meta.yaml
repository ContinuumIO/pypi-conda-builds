package:
  name: django-taggit-serializer
  version: "0.1.1"

source:
  fn: django-taggit-serializer-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-taggit-serializer/django-taggit-serializer-0.1.1.tar.gz
  md5: 7930b6bc82a7e2742ac8a6b8ca8a40aa
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-taggit-serializer = django-taggit-serializer:main
    #
    # Would create an entry point called django-taggit-serializer that calls django-taggit-serializer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-taggit

  run:
    - python
    - django-taggit

test:
  # Python imports
  imports:
    - taggit_serializer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/glemmaPaul/django-taggit-serializer
  license: BSD License
  summary: 'The Django Taggit serializer for tDjango REST Framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
