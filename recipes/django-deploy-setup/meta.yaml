package:
  name: django-deploy-setup
  version: "0.1.1a"

source:
  fn: django-deploy-setup-0.1.1a.tar.gz
  url: https://pypi.python.org/packages/source/d/django-deploy-setup/django-deploy-setup-0.1.1a.tar.gz
  md5: 0a273bf227fd422c24fb82bfdf7fe700
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-deploy-setup = django-deploy-setup:main
    #
    # Would create an entry point called django-deploy-setup that calls django-deploy-setup.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4

  run:
    - python
    - django >=1.4

test:
  # Python imports
  imports:
    - django_deploy
    - django_deploy.management
    - django_deploy.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/elvard/django-deploy
  license: MIT
  summary: u'Deployment tools for django projects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
