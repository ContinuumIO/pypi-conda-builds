package:
  name: python-batcher
  version: "0.0.2"

source:
  fn: python-batcher-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/p/python-batcher/python-batcher-0.0.2.tar.gz
  md5: d4f8db0bed22bea61c007c7c0faeaa6f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-batcher = python-batcher:main
    #
    # Would create an entry point called python-batcher that calls python-batcher.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - batcher

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/paulfurley/python-batcher
  license: MIT License
  summary: 'Process items in batches rather than one-by-one.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
