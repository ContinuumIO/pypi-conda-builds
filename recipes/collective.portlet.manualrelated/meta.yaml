package:
  name: collective.portlet.manualrelated
  version: "0.3"

source:
  fn: collective.portlet.manualRelated-0.3.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.portlet.manualRelated/collective.portlet.manualRelated-0.3.tar.gz
  md5: ab1e53769d9ef20e88fc19a6a4c2cd52
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.portlet.manualrelated = collective.portlet.manualrelated:main
    #
    # Would create an entry point called collective.portlet.manualrelated that calls collective.portlet.manualrelated.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.portlet
    - collective.portlet.manualRelated
    - collective.portlet.manualRelated.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/
  license: GPL
  summary: 'Portlet that shows items from manually related collections or folders'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
