package:
  name: kiwi_project
  version: "1.3.9.1"

source:
  fn: kiwi_project-1.3.9.1.tar.gz
  url: https://pypi.python.org/packages/source/k/kiwi_project/kiwi_project-1.3.9.1.tar.gz
  md5: f9b7abfd6e7b0fd8450c8ee766ae5cc7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kiwi_project = kiwi_project:main
    #
    # Would create an entry point called kiwi_project that calls kiwi_project.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3.2
    - pywiki2xhtml
    - sveetchies

  run:
    - python
    - django >=1.3.2
    - pywiki2xhtml
    - sveetchies

test:
  # Python imports
  imports:
    - examplesite
    - examplesite.kiwi
    - examplesite.kiwi.templatetags
    - examplesite.kiwi.utils
    - examplesite.kiwi.views
    - kiwi
    - kiwi.templatetags
    - kiwi.utils
    - kiwi.views
    - kiwi_project

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT License
  summary: 'Kiwi est un module Django dont le but est de g\xc3\xa9rer des pages Wiki'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
