package:
  name: collective.js.highslide
  version: "1.0"

source:
  fn: collective.js.highslide-1.0.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.js.highslide/collective.js.highslide-1.0.tar.gz
  md5: c139b6b0aa49e9c5cb1fbbb2c734c1e2
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.js.highslide = collective.js.highslide:main
    #
    # Would create an entry point called collective.js.highslide that calls collective.js.highslide.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.js
    - collective.js.highslide

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.js.highslide
  license: GPL
  summary: 'Register highslide jquery plugin into plone resource registry'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
