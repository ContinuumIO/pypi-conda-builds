package:
  name: products.purgemissingobjectinterfaces
  version: "1.0.4a"

source:
  fn: Products.PurgeMissingObjectInterfaces-1.0.4a.tar.gz
  url: https://pypi.python.org/packages/source/P/Products.PurgeMissingObjectInterfaces/Products.PurgeMissingObjectInterfaces-1.0.4a.tar.gz
  md5: c4d75997300830bc778181ed5a3ce9c4
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - products.purgemissingobjectinterfaces = products.purgemissingobjectinterfaces:main
    #
    # Would create an entry point called products.purgemissingobjectinterfaces that calls products.purgemissingobjectinterfaces.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - fakezopeinterface

  run:
    - python
    - setuptools
    - setuptools
    - fakezopeinterface

test:
  # Python imports
  imports:
    - Products
    - Products.PurgeMissingObjectInterfaces
    - Products.PurgeMissingObjectInterfaces.Extensions

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.nidelven-it.no/d
  license: GNU General Public License (GPL)
  summary: 'A product for purging old and missing marker interfaces from objects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
