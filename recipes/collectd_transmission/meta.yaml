package:
  name: collectd_transmission
  version: "v2.1"

source:
  fn: collectd_transmission-v2.1.tar.gz
  url: https://pypi.python.org/packages/source/c/collectd_transmission/collectd_transmission-v2.1.tar.gz
  md5: 2da0037d9cc85e6d2bc2075042b2242f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collectd_transmission = collectd_transmission:main
    #
    # Would create an entry point called collectd_transmission that calls collectd_transmission.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - collectd_transmission

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: ISC License (ISCL)
  summary: 'A python plugin for collectd to get stats from a runnning transmission server.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
