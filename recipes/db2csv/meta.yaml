package:
  name: db2csv
  version: "0.1b3"

source:
  fn: DB2CSV-0.1b3.tar.gz
  url: https://pypi.python.org/packages/source/D/DB2CSV/DB2CSV-0.1b3.tar.gz
  md5: 8234549e42ba36ec3a5c784a04eae949
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - db2csv = db2csv:main
    #
    # Would create an entry point called db2csv that calls db2csv.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sqlalchemy ==0.7.9

  run:
    - python
    - sqlalchemy ==0.7.9

test:
  # Python imports
  imports:
    - db2csv

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/stefanofontanelli/db2csv
  license: UNKNOWN
  summary: 'Tool to dump database tables into csv files.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
