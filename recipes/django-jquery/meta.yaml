package:
  name: django-jquery
  version: "1.9.1"

source:
  fn: django-jquery-1.9.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-jquery/django-jquery-1.9.1.tar.gz
  md5: 6e61e1a0205fd7acb3b4f75e26898535
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-jquery = django-jquery:main
    #
    # Would create an entry point called django-jquery that calls django-jquery.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - jquery

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/massimilianoravelli/django-jquery
  license: MIT License
  summary: 'jQuery packaged in an handy django app to speed up new applications and deployment.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
