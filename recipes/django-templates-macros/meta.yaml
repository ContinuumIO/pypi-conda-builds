package:
  name: django-templates-macros
  version: "0.1"

source:
  fn: django-templates-macros-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-templates-macros/django-templates-macros-0.1.tar.gz
  md5: 748b708b8a9b800d96599cde39989514
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-templates-macros = django-templates-macros:main
    #
    # Would create an entry point called django-templates-macros that calls django-templates-macros.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - macros
    - macros.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/twidi/django-templates-macros
  license: MIT License
  summary: 'Add macros to your django templates'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
