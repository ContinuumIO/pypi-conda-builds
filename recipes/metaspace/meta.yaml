package:
  name: metaspace
  version: "0.0.1"

source:
  fn: metaspace-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/m/metaspace/metaspace-0.0.1.tar.gz
  md5: e4c9bc423e8c8c2aadd31da0ce72266d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - metaspace = metaspace:main
    #
    # Would create an entry point called metaspace that calls metaspace.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - metaspace

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/zacharyvoase/metaspace
  license: UNKNOWN
  summary: 'Create Python modules using class syntax.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
