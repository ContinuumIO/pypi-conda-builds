package:
  name: masala
  version: "0.0.2"

source:
  fn: masala-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/m/masala/masala-0.0.2.tar.gz
  md5: ceede6e13bdcbf404e1faf26cff2ede9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - masala = masala:main
    #
    # Would create an entry point called masala that calls masala.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six

  run:
    - python
    - six

test:
  # Python imports
  imports:
    - masala
    - masala.datatype
    - masala.datatype.stream

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/hachibeeDI/masala
  license: MIT License
  summary: 'curry functional patternpatch linq'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
