package:
  name: django-oauth2
  version: "1.1"

source:
  fn: django-oauth2-1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-oauth2/django-oauth2-1.1.tar.gz
  md5: 8ec6d16babd566e02642011210589a56
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-oauth2 = django-oauth2:main
    #
    # Would create an entry point called django-oauth2 that calls django-oauth2.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - shortuuid >=0.4

  run:
    - python
    - shortuuid >=0.4

test:
  # Python imports
  imports:
    - provider
    - provider.compat
    - provider.oauth2
    - provider.oauth2.migrations
    - provider.oauth2.south_migrations
    - provider.templatetags
    - provider.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/stormsherpa/django-oauth2-provider
  license: MIT License
  summary: 'Provide OAuth2 access to your app (fork of django-oauth2-provider)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
