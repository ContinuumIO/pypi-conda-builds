package:
  name: django-cachepurge
  version: "0.3"

source:
  fn: django-cachepurge-0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-cachepurge/django-cachepurge-0.3.tar.gz
  md5: 177ec80438477a41cd5951c10be765b9
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-cachepurge = django-cachepurge:main
    #
    # Would create an entry point called django-cachepurge that calls django-cachepurge.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - django_cachepurge
    - django_cachepurge.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://launchpad.net/django-cachepurge
  license: GNU General Public License (GPL)
  summary: 'Django Middleware and utilities that send "PURGE" request to an upstream cache'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
