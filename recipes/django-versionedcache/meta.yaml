package:
  name: django-versionedcache
  version: "0.1.2"

source:
  fn: django-versionedcache-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-versionedcache/django-versionedcache-0.1.2.tar.gz
  md5: 703d99744f17f46bc3f91daeb7a7eb36
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-versionedcache = django-versionedcache:main
    #
    # Would create an entry point called django-versionedcache that calls django-versionedcache.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools >=0.6b1
    - django >=1.1

  run:
    - python
    - setuptools >=0.6b1
    - django >=1.1

test:
  # Python imports
  imports:
    - versionedcache

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD
  summary: 'django versioned cache'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
