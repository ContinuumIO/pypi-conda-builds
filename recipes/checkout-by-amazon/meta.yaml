package:
  name: checkout-by-amazon
  version: "0.1.6"

source:
  fn: checkout-by-amazon-0.1.6.tar.gz
  url: https://pypi.python.org/packages/source/c/checkout-by-amazon/checkout-by-amazon-0.1.6.tar.gz
  md5: 673bcf8af731ba188957c8e469e9e015
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - checkout-by-amazon = checkout-by-amazon:main
    #
    # Would create an entry point called checkout-by-amazon that calls checkout-by-amazon.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - decorator ==3.3.2
    - requests ==0.9.3

  run:
    - python
    - decorator ==3.3.2
    - requests ==0.9.3

test:
  # Python imports
  imports:
    - cba

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/bluejeansummer/cba
  license: GNU Lesser General Public License v. 3.0
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
