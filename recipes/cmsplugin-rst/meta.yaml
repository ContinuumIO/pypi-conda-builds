package:
  name: cmsplugin-rst
  version: "0.1.1"

source:
  fn: cmsplugin-rst-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/c/cmsplugin-rst/cmsplugin-rst-0.1.1.tar.gz
  md5: e0816eaf3be2068ee523b44f2c162253
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cmsplugin-rst = cmsplugin-rst:main
    #
    # Would create an entry point called cmsplugin-rst that calls cmsplugin-rst.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - docutils
    - django-cms >=2.1
    - django >=1.2

  run:
    - python
    - docutils
    - django-cms >=2.1
    - django >=1.2

test:
  # Python imports
  imports:
    - cmsplugin_rst
    - cmsplugin_rst.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/ojii/cmsplugin-rst
  license: UNKNOWN
  summary: 'Restructured Text plugin for the django CMS.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
