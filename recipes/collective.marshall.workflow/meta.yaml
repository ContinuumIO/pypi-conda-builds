package:
  name: collective.marshall.workflow
  version: "1.0.4"

source:
  fn: collective.marshall.workflow-1.0.4.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.marshall.workflow/collective.marshall.workflow-1.0.4.tar.gz
  md5: a30ce2566f7422567866bd7a6c60beb1
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.marshall.workflow = collective.marshall.workflow:main
    #
    # Would create an entry point called collective.marshall.workflow that calls collective.marshall.workflow.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.marshall
    - collective.marshall.workflow
    - collective.marshall.workflow.namespaces

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.brainson.de/
  license: GPL
  summary: 'A Namespace Extension for ATXML'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
