package:
  name: django-gitcms
  version: "0.4"

source:
  fn: django-gitcms-0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-gitcms/django-gitcms-0.4.tar.gz
  md5: 56a13f5c9fe7d1bf8a8579c4e16090fa
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-gitcms = django-gitcms:main
    #
    # Would create an entry point called django-gitcms that calls django-gitcms.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - gitcms
    - gitcms.blog
    - gitcms.blog.templatetags
    - gitcms.books
    - gitcms.conferences
    - gitcms.files
    - gitcms.menus
    - gitcms.menus.templatetags
    - gitcms.pages
    - gitcms.parsedate
    - gitcms.publications
    - gitcms.redirect
    - gitcms.tagging

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://luispedro.org/software/git-cms
  license: GNU Affero General Public License v3
  summary: 'Django Git CMS: A django based git-backed content management system'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
