package:
  name: codeviking.collections
  version: "0.10"

source:
  fn: CodeViking.Collections-0.10.tar.gz
  url: https://pypi.python.org/packages/source/C/CodeViking.Collections/CodeViking.Collections-0.10.tar.gz
  md5: 527d53edce51b43a544a39c123c9b588
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - codeviking.collections = codeviking.collections:main
    #
    # Would create an entry point called codeviking.collections that calls codeviking.collections.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - codeviking
    - codeviking.collections
    - codeviking.collections.dict

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://hg.codeviking.com/collections/
  license: GNU General Public License (GPL)
  summary: 'Data collection objects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
