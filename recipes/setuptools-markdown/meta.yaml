package:
  name: setuptools-markdown
  version: "0.1"

source:
  fn: setuptools-markdown-0.1.tar.gz
  url: https://pypi.python.org/packages/source/s/setuptools-markdown/setuptools-markdown-0.1.tar.gz
  md5: 186bc20869117fc705effad284fc7022
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - setuptools-markdown = setuptools-markdown:main
    #
    # Would create an entry point called setuptools-markdown that calls setuptools-markdown.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pypandoc

  run:
    - python
    - setuptools
    - pypandoc

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/msabramo/setuptools-markdown
  license: MIT
  summary: 'Use Markdown for your project description'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
