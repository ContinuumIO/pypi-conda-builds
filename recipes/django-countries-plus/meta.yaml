package:
  name: django-countries-plus
  version: "0.3.3"

source:
  fn: django-countries-plus-0.3.3.zip
  url: https://pypi.python.org/packages/source/d/django-countries-plus/django-countries-plus-0.3.3.zip
  md5: 4b7aaf98c25ca7ea6119e0a8f9028634
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-countries-plus = django-countries-plus:main
    #
    # Would create an entry point called django-countries-plus that calls django-countries-plus.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.2

  run:
    - python
    - django >=1.2

test:
  # Python imports
  imports:
    - countries_plus
    - countries_plus.migrations
    - countries_plus.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/django-countries-plus/
  license: LICENSE.txt
  summary: 'A django model & fixture containing all data from the countries table of Geonames.org'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
