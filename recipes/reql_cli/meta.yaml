package:
  name: reql_cli
  version: "0.3.1"

source:
  fn: reql_cli-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/r/reql_cli/reql_cli-0.3.1.tar.gz
  md5: 193d1e1450fa47902cb2aed7f2ea575c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - reql_cli = reql_cli:main
    #
    # Would create an entry point called reql_cli that calls reql_cli.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - rethinkdb >=1.13
    - pygments >=1.6

  run:
    - python
    - rethinkdb >=1.13
    - pygments >=1.6

test:
  # Python imports
  imports:
    - reql_cli
    - reqlcli

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/deontologician/reql_cli
  license: MIT License
  summary: 'Run RethinkDB ReQL commands from the terminal'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
