package:
  name: django-filters
  version: "0.1.0"

source:
  fn: django-filters-0.1.0.zip
  url: https://pypi.python.org/packages/source/d/django-filters/django-filters-0.1.0.zip
  md5: 90ad3630cdc7d1772453e628a83f81ee
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-filters = django-filters:main
    #
    # Would create an entry point called django-filters that calls django-filters.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - filters

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/baskoopmans/django-filters/
  license: BSD License
  summary: 'A filterset and filters.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
