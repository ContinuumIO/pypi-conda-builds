package:
  name: ditrack
  version: "0.8"

source:
  fn: ditrack-0.8.tgz
  url: http://www.ditrack.org/files/ditrack-0.8.tgz
  #md5: 
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ditrack = ditrack:main
    #
    # Would create an entry point called ditrack that calls ditrack.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - DITrack
    - DITrack/Backend
    - DITrack/Command
    - DITrack/DB
    - DITrack/ThirdParty
    - DITrack/ThirdParty/Python
    - DITrack/Util
    - DITrack/dt
    - ditrack

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.ditrack.org
  license: BSD License
  summary: 'Distributed Issue Tracking system'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
