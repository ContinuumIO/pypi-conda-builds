package:
  name: kvlite
  version: "0.6.3"

source:
  fn: kvlite-0.6.3.tar.gz
  url: https://pypi.python.org/packages/source/k/kvlite/kvlite-0.6.3.tar.gz
  md5: 16bc6dc4464d1399e0d0b0831908ddb6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kvlite = kvlite:main
    #
    # Would create an entry point called kvlite that calls kvlite.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - kvlite
    - kvlite.webui

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ownport/kvlite
  license: BSD License
  summary: 'key-value database wrapper for SQL database (MySQL, SQLite) '

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
