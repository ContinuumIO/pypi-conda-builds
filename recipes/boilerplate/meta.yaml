package:
  name: boilerplate
  version: "1.2.2-beta"

source:
  fn: boilerplate-1.2.2-beta.tar.gz
  url: https://pypi.python.org/packages/source/b/boilerplate/boilerplate-1.2.2-beta.tar.gz
  md5: 9a1d8199c0f7a66803b6d5cd84e40ef8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - boilerplate = boilerplate:main
    #
    # Would create an entry point called boilerplate that calls boilerplate.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - boilerplate
    - boilerplate.tmpl
    - boilerplate.tmpl.boil_template

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jqb/boilerplate/tree/ver-1.2.2-beta
  license: BSD License
  summary: 'Easy to use tool for painless project layout templating'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
