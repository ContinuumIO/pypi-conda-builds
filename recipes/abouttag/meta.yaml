package:
  name: abouttag
  version: "1.2.1"

source:
  fn: abouttag-1.2.1.tar.gz
  url: https://pypi.python.org/packages/source/a/abouttag/abouttag-1.2.1.tar.gz
  md5: 55a497e621b0fe56ec684dfa890a52f8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - abouttag = abouttag:main
    #
    # Would create an entry point called abouttag that calls abouttag.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - urlnorm >=1.1.2

  run:
    - python
    - urlnorm >=1.1.2

test:
  # Python imports
  imports:
    - abouttag

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/njr0/abouttag/
  license: MIT License
  summary: 'Normalizes about tags for Fluidinfo'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
