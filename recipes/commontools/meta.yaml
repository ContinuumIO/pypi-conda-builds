package:
  name: commontools
  version: "0.1.1"

source:
  fn: commontools-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/c/commontools/commontools-0.1.1.tar.gz
  md5: 9c096bb364721c32d3952be12f303b09
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - commontools = commontools:main
    #
    # Would create an entry point called commontools that calls commontools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - commontools

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/muneeb-ali/commontools
  license: MIT License
  summary: "Common functions that are often needed but don't really belong anywhere"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
