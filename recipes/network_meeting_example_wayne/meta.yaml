package:
  name: network_meeting_example_wayne
  version: "2.0.501.1.1"

source:
  fn: network_meeting_example_wayne-2.0.501.1.1.tar.gz
  url: https://pypi.python.org/packages/source/n/network_meeting_example_wayne/network_meeting_example_wayne-2.0.501.1.1.tar.gz
  md5: 14e2b3a9a45a2f4f025df2447110dc80
#  patches:
   # List any patch files here
   # - fix.patch

build:
  # preserve_egg_dir: True
  entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - network_meeting_example_wayne = network_meeting_example_wayne:main
    #
    # Would create an entry point called network_meeting_example_wayne that calls network_meeting_example_wayne.main()

    - rogers = rogers.neighborhood:smain

  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - network_meeting_example_wayne
    - rogers

  commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.

    - rogers --help

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: tbd
  license: GNU General Public License v3 (GPLv3)
  summary: 'this is a short de'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
