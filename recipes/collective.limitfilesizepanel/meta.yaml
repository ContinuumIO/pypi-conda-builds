package:
  name: collective.limitfilesizepanel
  version: "1.2"

source:
  fn: collective.limitfilesizepanel-1.2.zip
  url: https://pypi.python.org/packages/source/c/collective.limitfilesizepanel/collective.limitfilesizepanel-1.2.zip
  md5: 425fed161f04185b95c9553f15577c28
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.limitfilesizepanel = collective.limitfilesizepanel:main
    #
    # Would create an entry point called collective.limitfilesizepanel that calls collective.limitfilesizepanel.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.registry
    - collective.monkeypatcher >=1.0

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.registry
    - collective.monkeypatcher >=1.0

test:
  # Python imports
  imports:
    - collective
    - collective.limitfilesizepanel
    - collective.limitfilesizepanel.browser
    - collective.limitfilesizepanel.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/collective.limitfilesizepanel
  license: GPL
  summary: 'Configure files and images size limit through Plone control panel'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
