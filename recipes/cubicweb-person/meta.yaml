package:
  name: cubicweb-person
  version: "1.10.0"

source:
  fn: cubicweb-person-1.10.0.tar.gz
  url: https://pypi.python.org/packages/source/c/cubicweb-person/cubicweb-person-1.10.0.tar.gz
  md5: 1e80cd48a884aef02c5527f4efc6e52e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cubicweb-person = cubicweb-person:main
    #
    # Would create an entry point called cubicweb-person that calls cubicweb-person.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cubicweb >=3.20.7

  run:
    - python
    - cubicweb >=3.20.7

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.cubicweb.org/project/cubicweb-person
  license: LGPL
  summary: 'person component for the CubicWeb framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
