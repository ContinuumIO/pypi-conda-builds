package:
  name: django-snapshot
  version: "0.1.0"

source:
  fn: django-snapshot-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-snapshot/django-snapshot-0.1.0.tar.gz
  md5: d11a5f014138557d26897c0279b70423
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-snapshot = django-snapshot:main
    #
    # Would create an entry point called django-snapshot that calls django-snapshot.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - snapshot
    - snapshot.management
    - snapshot.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: GNU General Public License (GPL)
  summary: 'Django backup and restore tool. Handle upload files and database'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
