package:
  name: django-moreviews
  version: "0.1"

source:
  fn: django-moreviews-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-moreviews/django-moreviews-0.1.tar.gz
  md5: 23cadf7f9e80bad32a792551fd05dfb5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-moreviews = django-moreviews:main
    #
    # Would create an entry point called django-moreviews that calls django-moreviews.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - moreviews

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://tartarus.org/james/computers/django/
  license: MIT
  summary: 'Django class-based views that complement the built-in ones.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
