package:
  name: collective.printview
  version: "1.0.1"

source:
  fn: collective.printview-1.0.1.zip
  url: https://pypi.python.org/packages/source/c/collective.printview/collective.printview-1.0.1.zip
  md5: 242ec502452642fa7331c466b651d149
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.printview = collective.printview:main
    #
    # Would create an entry point called collective.printview that calls collective.printview.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.registry
    - five.grok

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.registry
    - five.grok

test:
  # Python imports
  imports:
    - collective
    - collective.printview
    - collective.printview.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.printview
  license: GPL
  summary: 'Displays all specified Plone content in one printable page.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
