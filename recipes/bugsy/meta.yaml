package:
  name: bugsy
  version: "0.4.0"

source:
  fn: bugsy-0.4.0.tar.gz
  url: https://pypi.python.org/packages/source/b/bugsy/bugsy-0.4.0.tar.gz
  md5: 30df92a8a78026b2cf607c3084074155
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bugsy = bugsy:main
    #
    # Would create an entry point called bugsy that calls bugsy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=1.1.0

  run:
    - python
    - requests >=1.1.0

test:
  # Python imports
  imports:
    - bugsy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://oss.theautomatedtester.co.uk/bugzilla
  license: Apache Software License
  summary: 'A library for interacting Bugzilla Native REST API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
