package:
  name: django-ponydebugger
  version: "0.0.3"

source:
  fn: django-ponydebugger-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-ponydebugger/django-ponydebugger-0.0.3.tar.gz
  md5: 403b27e09a1346eb56f6bec7921370f0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-ponydebugger = django-ponydebugger:main
    #
    # Would create an entry point called django-ponydebugger that calls django-ponydebugger.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - websocket-client

  run:
    - python
    - websocket-client

test:
  # Python imports
  imports:
    - django_ponydebugger
    - django_ponydebugger.domains

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/educreations/django-ponydebugger
  license: MIT License
  summary: 'PonyDebugger support for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
