package:
  name: django-scheduled_blocks
  version: "0.0.1"

source:
  fn: django-scheduled_blocks-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-scheduled_blocks/django-scheduled_blocks-0.0.1.tar.gz
  md5: eb3ccd4827db5ce2206e933b313de6c9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-scheduled_blocks = django-scheduled_blocks:main
    #
    # Would create an entry point called django-scheduled_blocks that calls django-scheduled_blocks.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - scheduled_blocks
    - scheduled_blocks.migrations
    - scheduled_blocks.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/pgcd/scheduled_blocks
  license: Simplified BSD
  summary: u'A quick and dirty app to schedule content in django templates'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
