package:
  name: django-db-signals
  version: "0.1.1"

source:
  fn: django-db-signals-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-db-signals/django-db-signals-0.1.1.tar.gz
  md5: 7abdb1bf809cbd549426d01edf255a34
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-db-signals = django-db-signals:main
    #
    # Would create an entry point called django-db-signals that calls django-db-signals.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.2

  run:
    - python
    - django >=1.2

test:
  # Python imports
  imports:
    - django_db_signals

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bradleyayers/django-db-signals
  license: BSD License
  summary: 'Django database signals (pre/post commit/rollback).'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
