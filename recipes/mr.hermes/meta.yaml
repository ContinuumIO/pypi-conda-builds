package:
  name: mr.hermes
  version: "1.2"

source:
  fn: mr.hermes-1.2.zip
  url: https://pypi.python.org/packages/source/m/mr.hermes/mr.hermes-1.2.zip
  md5: 776f30ef41530432f2d0aa9084528ecf
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mr.hermes = mr.hermes:main
    #
    # Would create an entry point called mr.hermes that calls mr.hermes.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - mr
    - mr.hermes

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/fschulze/mr.hermes
  license: UNKNOWN
  summary: 'A simple debug smtp server for development.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
