package:
  name: editobj2
  version: "0.5"

source:
  fn: EditObj2-0.5.tar.gz
  url: https://pypi.python.org/packages/source/E/EditObj2/EditObj2-0.5.tar.gz
  md5: 6855d1506d3901d3feff01ea334a2cf8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - editobj2 = editobj2:main
    #
    # Would create an entry point called editobj2 that calls editobj2.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - editobj2

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://home.gna.org/oomadness/en/editobj/index.html
  license: GPL
  summary: 'A Java-Bean-like dialog box generator for Python objects, compatible with Gtk 2, Tk, Qt and Qtopia'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
