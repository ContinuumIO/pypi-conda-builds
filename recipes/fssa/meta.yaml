package:
  name: fssa
  version: "0.1.8"

source:
  fn: fssa-0.1.8.tar.gz
  url: https://pypi.python.org/packages/source/f/fssa/fssa-0.1.8.tar.gz
  md5: 74dcab13e5ba5fd037677682793f2067
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fssa = fssa:main
    #
    # Would create an entry point called fssa that calls fssa.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy
    - scipy
    - future
    - setuptools_git

  run:
    - python
    - numpy
    - scipy
    - future
    - setuptools_git

test:
  # Python imports
  imports:
    - fssa

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/andsor/pyfss
  license: Apache Software License
  summary: 'Finite-size scaling analysis at phase transitions'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
