package:
  name: pyndfd
  version: "0.4"

source:
  fn: pyndfd-0.4.tar.gz
  url: https://pypi.python.org/packages/source/p/pyndfd/pyndfd-0.4.tar.gz
  md5: 02ceee4bac7aa271fcd2f8a07408c0dc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pyndfd = pyndfd:main
    #
    # Would create an entry point called pyndfd that calls pyndfd.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - numpy
    - pygrib
    - pyproj

  run:
    - python
    - numpy
    - pygrib
    - pyproj

test:
  # Python imports
  imports:
    - pyndfd

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/marty-sullivan/pyndfd
  license: MIT License
  summary: "Python routines for easy caching/retrieval of NWS's NDFD variables"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
