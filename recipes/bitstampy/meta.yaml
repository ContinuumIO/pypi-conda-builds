package:
  name: bitstampy
  version: "0.0.8"

source:
  fn: bitstampy-0.0.8.tar.gz
  url: https://pypi.python.org/packages/source/b/bitstampy/bitstampy-0.0.8.tar.gz
  md5: 2c374300d27d9746a83381a562a42e49
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bitstampy = bitstampy:main
    #
    # Would create an entry point called bitstampy that calls bitstampy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - bitstampy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/unwitting/bitstampy
  license: MIT License
  summary: 'Bitstamp API wrapper for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
