package:
  name: iterextensions
  version: "0.0.1"

source:
  fn: iterextensions-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/i/iterextensions/iterextensions-0.0.1.tar.gz
  md5: c7c942392dedae66e3ce2996f62b29c2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - iterextensions = iterextensions:main
    #
    # Would create an entry point called iterextensions that calls iterextensions.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - iterextensions

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/alex----/iterextensions
  license: UNKNOWN
  summary: 'itertool helps for Python2/3'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
