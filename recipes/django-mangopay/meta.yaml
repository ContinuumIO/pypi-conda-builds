package:
  name: django-mangopay
  version: "1.0.0"

source:
  fn: django-mangopay-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-mangopay/django-mangopay-1.0.0.tar.gz
  md5: 352173fc2180451bec2472a5bad22632
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-mangopay = django-mangopay:main
    #
    # Would create an entry point called django-mangopay that calls django-mangopay.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4.3
    - mangopaysdk ==0.311

  run:
    - python
    - django >=1.4.3
    - mangopaysdk ==0.311

test:
  # Python imports
  imports:
    - mangopay
    - mangopay.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/FundedByMe/django-mangopay
  license: UNKNOWN
  summary: 'Django package that helps in your Mangopay integration'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
