package:
  name: django_image_proxy
  version: "0.0.7"

source:
  fn: django_image_proxy-0.0.7.tar.gz
  url: https://pypi.python.org/packages/source/d/django_image_proxy/django_image_proxy-0.0.7.tar.gz
  md5: 5b0614e759eebd9b707a1a586e04ca5b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_image_proxy = django_image_proxy:main
    #
    # Would create an entry point called django_image_proxy that calls django_image_proxy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_image_proxy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Apache Software License
  summary: 'Simple Django Image Proxy especially for Horizon Openstack Dashboard.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
