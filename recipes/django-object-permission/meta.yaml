package:
  name: django-object-permission
  version: "0.5.2"

source:
  fn: django-object-permission-0.5.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-object-permission/django-object-permission-0.5.2.tar.gz
  md5: 256745b5a513d455043287dc8ab0f16a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-object-permission = django-object-permission:main
    #
    # Would create an entry point called django-object-permission that calls django-object-permission.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - distribute
    - setuptools-git
    - django >=1.3
    - django-observer >=0.3rc3

  run:
    - python
    - distribute
    - setuptools-git
    - django >=1.3
    - django-observer >=0.3rc3

test:
  # Python imports
  imports:
    - object_permission
    - object_permission.handlers
    - object_permission.templatetags
    - object_permission.tests
    - object_permission.tests.testapp

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lambdalisue/django-object-permission
  license: MIT License
  summary: 'Add object specific permission for particualr User/Group, All authenticated user or Anonymous user'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
