package:
  name: ely.portlets.image
  version: "0.5"

source:
  fn: ely.portlets.image-0.5.tar.gz
  url: https://pypi.python.org/packages/source/e/ely.portlets.image/ely.portlets.image-0.5.tar.gz
  md5: e7756eeefd00879515973bfe2b6f1c65
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ely.portlets.image = ely.portlets.image:main
    #
    # Would create an entry point called ely.portlets.image that calls ely.portlets.image.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - ely
    - ely.portlets
    - ely.portlets.image
    - ely.portlets.image.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/ely/source/browse
  license: GPL
  summary: 'A portlet for attaching and linking a single image'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
