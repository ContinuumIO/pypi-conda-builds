package:
  name: django-announcements
  version: "1.2.0"

source:
  fn: django-announcements-1.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-announcements/django-announcements-1.2.0.tar.gz
  md5: 867180338f290961c287a80f0a0f93a7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-announcements = django-announcements:main
    #
    # Would create an entry point called django-announcements that calls django-announcements.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - announcements
    - announcements.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/pinax/django-announcements
  license: MIT License
  summary: 'Announcements for your Django powered website.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
