package:
  name: xstatic-angular
  version: "1.3.7.0"

source:
  fn: XStatic-Angular-1.3.7.0.tar.gz
  url: https://pypi.python.org/packages/source/X/XStatic-Angular/XStatic-Angular-1.3.7.0.tar.gz
  md5: fa26884ab0b2acfa09e372aedd7e7b04
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - xstatic-angular = xstatic-angular:main
    #
    # Would create an entry point called xstatic-angular that calls xstatic-angular.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - xstatic
    - xstatic.pkg
    - xstatic.pkg.angular

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

# requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://angularjs.org
  license: (same as Angular)
  summary: 'Angular 1.3.7 (XStatic packaging standard)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
