package:
  name: livehtml
  version: "0.0.1"

source:
  fn: livehtml-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/l/livehtml/livehtml-0.0.1.tar.gz
  md5: 0e75042a46d6c6d6989a00e4a863af09
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - livehtml = livehtml:main
    #
    # Would create an entry point called livehtml that calls livehtml.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - livehtml

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/GrAndSE/livehtml
  license: BSD License
  summary: 'Simple development server for writing an html pages'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
