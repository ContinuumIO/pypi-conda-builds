package:
  name: hanyang_registration
  version: "1.1.4"

source:
  fn: hanyang_registration-1.1.4.tar.gz
  url: https://pypi.python.org/packages/source/h/hanyang_registration/hanyang_registration-1.1.4.tar.gz
  md5: 4250d04ee89fb212bddbc270c7ccf63b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hanyang_registration = hanyang_registration:main
    #
    # Would create an entry point called hanyang_registration that calls hanyang_registration.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - rsa
    - requests

  run:
    - python
    - rsa
    - requests

test:
  # Python imports
  imports:
    - hanyang_registration

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Jaram/Hanyang-Class-Registration
  license: The MIT License
  summary: 'Hanyang Class Registration for python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
