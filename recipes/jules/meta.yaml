package:
  name: jules
  version: "0.2.0.1"

source:
  fn: jules-0.2.0.1.tar.gz
  url: https://pypi.python.org/packages/source/j/jules/jules-0.2.0.1.tar.gz
  md5: 1117b3e36148027cc39e79de023c372e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jules = jules:main
    #
    # Would create an entry point called jules that calls jules.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - straight.plugin
    - straight.command
    - pyyaml
    - jinja2
    - docutils

  run:
    - python
    - straight.plugin
    - straight.command
    - pyyaml
    - jinja2
    - docutils

test:
  # Python imports
  imports:
    - jules
    - jules.plugins

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'Yet Another Static Blog Generator I guess'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
