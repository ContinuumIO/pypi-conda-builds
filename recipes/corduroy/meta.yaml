package:
  name: corduroy
  version: "0.9.0"

source:
  fn: corduroy-0.9.0.tar.gz
  url: https://pypi.python.org/packages/source/c/corduroy/corduroy-0.9.0.tar.gz
  md5: fad6434feedc75c67fc80f42b031c957
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - corduroy = corduroy:main
    #
    # Would create an entry point called corduroy that calls corduroy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - corduroy
    - corduroy.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://samizdat.cc/corduroy
  license: BSD License
  summary: 'An asynchronous CouchDB client library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
