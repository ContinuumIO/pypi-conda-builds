package:
  name: bcresolver
  version: "0.0.2"

source:
  fn: bcresolver-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/b/bcresolver/bcresolver-0.0.2.tar.gz
  md5: 93f83d0d4d84858ae9aff2dc74f59f4b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bcresolver = bcresolver:main
    #
    # Would create an entry point called bcresolver that calls bcresolver.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - dnspython >=1.12.0
    - requests >=2.5.1

  run:
    - python
    - dnspython >=1.12.0
    - requests >=2.5.1

test:
  # Python imports
  imports:
    - bcresolver

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/netkicorp/blockchain-resolver
  license: BSD
  summary: 'Blockchain to DNS with DNSSEC Resolver Library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
