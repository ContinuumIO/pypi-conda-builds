package:
  name: django_longpolling
  version: "0.1.2"

source:
  fn: django_longpolling-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django_longpolling/django_longpolling-0.1.2.tar.gz
  md5: b7721542f763d6f3ba02c42925ed86de
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_longpolling = django_longpolling:main
    #
    # Would create an entry point called django_longpolling that calls django_longpolling.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - django_longpolling

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tbarbugli/django_longpolling
  license: BSD License
  summary: 'Long polling requests for for Django 1.3+ using gevent'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
