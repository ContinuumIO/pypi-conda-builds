package:
  name: redsolutioncms.django-tinymce-attachment
  version: "0.5.0"

source:
  fn: redsolutioncms.django-tinymce-attachment-0.5.0.tar.gz
  url: https://pypi.python.org/packages/source/r/redsolutioncms.django-tinymce-attachment/redsolutioncms.django-tinymce-attachment-0.5.0.tar.gz
  md5: 9c9a7cd89aa0f1d57cbd526fe50ae3b9
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - redsolutioncms.django-tinymce-attachment = redsolutioncms.django-tinymce-attachment:main
    #
    # Would create an entry point called redsolutioncms.django-tinymce-attachment that calls redsolutioncms.django-tinymce-attachment.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - redsolutioncms.django-tinymce >=1.5.0
    - django-imagekit ==0.3.3
    - django-classy-tags

  run:
    - python
    - setuptools
    - redsolutioncms.django-tinymce >=1.5.0
    - django-imagekit ==0.3.3
    - django-classy-tags

test:
  # Python imports
  imports:
    - attachment
    - attachment.migrations
    - attachment.redsolution_setup
    - attachment.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: GNU General Public License (GPL)
  summary: 'Allows to attach files and images for any objects, specified in configuration.\nIf you use TinyMCE to edit object content, this module makes for you links to \nfiles in hyperlink select box and list of images in image select box. '

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
