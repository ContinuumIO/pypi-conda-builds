package:
  name: sphinx-gallery
  version: "0.0.9.dev0"

source:
  fn: sphinx-gallery-0.0.9.dev0.tar.gz
  url: https://pypi.python.org/packages/source/s/sphinx-gallery/sphinx-gallery-0.0.9.dev0.tar.gz
  md5: 752f5b6164db839854be459ec38ebf06
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sphinx-gallery = sphinx-gallery:main
    #
    # Would create an entry point called sphinx-gallery that calls sphinx-gallery.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - sphinxgallery

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/sphinx-gallery/sphinx-gallery
  license: 3-clause BSD
  summary: 'Sphinx extension to automatically generate an examples gallery'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
