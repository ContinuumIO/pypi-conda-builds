package:
  name: helga-hipchat-nicks
  version: "0.1.1"

source:
  fn: helga-hipchat-nicks-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/h/helga-hipchat-nicks/helga-hipchat-nicks-0.1.1.tar.gz
  md5: b80324df54e6ff5a2d68e0903fc305ea
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - helga-hipchat-nicks = helga-hipchat-nicks:main
    #
    # Would create an entry point called helga-hipchat-nicks that calls helga-hipchat-nicks.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - hypchat ==0.18

  run:
    - python
    - setuptools
    - hypchat ==0.18

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/shaunduncan/helga-hipchat-nicks
  license: MIT License
  summary: 'A helga preprocessor to ensure that user nicks are set as @ mentions'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
