package:
  name: mixbox
  version: "0.0.1"

source:
  fn: mixbox-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/m/mixbox/mixbox-0.0.1.tar.gz
  md5: 973dacb7121bc697c500394956bb2b3a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mixbox = mixbox:main
    #
    # Would create an entry point called mixbox that calls mixbox.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - lxml
    - six >=1.9.0
    - python-dateutil

  run:
    - python
    - lxml
    - six >=1.9.0
    - python-dateutil

test:
  # Python imports
  imports:
    - mixbox

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/CybOXProject/mixbox
  license: BSD License
  summary: 'Utility library for cybox, maec, and stix packages'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
