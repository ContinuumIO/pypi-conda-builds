package:
  name: env.py
  version: "0.2.2"

source:
  fn: env.py-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/e/env.py/env.py-0.2.2.tar.gz
  md5: 37299e6095165ec34d9922bf2352f2a6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - env.py = env.py:main
    #
    # Would create an entry point called env.py that calls env.py.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/abhinavsingh/env.py
  license: BSD License
  summary: 'Easily manage your python application settings for different environments'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
