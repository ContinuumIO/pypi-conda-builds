package:
  name: blockdiagcontrib-math
  version: "0.9.0"

source:
  fn: blockdiagcontrib-math-0.9.0.tar.gz
  url: https://pypi.python.org/packages/source/b/blockdiagcontrib-math/blockdiagcontrib-math-0.9.0.tar.gz
  md5: d64da28a1939a1fddf96f5878eeb05ab
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - blockdiagcontrib-math = blockdiagcontrib-math:main
    #
    # Would create an entry point called blockdiagcontrib-math that calls blockdiagcontrib-math.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - blockdiag >=1.4.2

  run:
    - python
    - setuptools
    - blockdiag >=1.4.2

test:
  # Python imports
  imports:
    - blockdiagcontrib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/blockdiag/blockdiag-contrib
  license: Apache Software License
  summary: 'LaTeX math plugin for blockdiag'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
