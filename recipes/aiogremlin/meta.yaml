package:
  name: aiogremlin
  version: "0.0.3"

source:
  fn: aiogremlin-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/a/aiogremlin/aiogremlin-0.0.3.tar.gz
  md5: fe1d41c69242b2ec896719e0d842145e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - aiogremlin = aiogremlin:main
    #
    # Would create an entry point called aiogremlin that calls aiogremlin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - aiohttp ==0.15.3
    - ujson ==1.33

  run:
    - python
    - aiohttp ==0.15.3
    - ujson ==1.33

test:
  # Python imports
  imports:
    - aiogremlin
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: MIT License
  summary: 'Python 3 driver for TP3 Gremlin Server built on Asyncio and aiohttp'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
