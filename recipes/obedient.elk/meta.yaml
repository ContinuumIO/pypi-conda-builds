package:
  name: obedient.elk
  version: "1.4.0"

source:
  fn: obedient.elk-1.4.0.tar.gz
  url: https://pypi.python.org/packages/source/o/obedient.elk/obedient.elk-1.4.0.tar.gz
  md5: daacebe947383afe4f5fac67a30c3071
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - obedient.elk = obedient.elk:main
    #
    # Would create an entry point called obedient.elk that calls obedient.elk.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - dominator[full] >=15.1a
    - obedient.zookeeper >=3a

  run:
    - python
    - setuptools
    - dominator[full] >=15.1a
    - obedient.zookeeper >=3a

test:
  # Python imports
  imports:
    - obedient.elk

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/yandex-sysmon/obedient.elk
  license: LGPLv3
  summary: 'ELK (Elasticsearch-Logstash-Kibana) obedient for Dominator'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
