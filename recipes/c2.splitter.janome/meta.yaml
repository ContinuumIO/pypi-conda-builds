package:
  name: c2.splitter.janome
  version: "0.1.0"

source:
  fn: c2.splitter.janome-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/c/c2.splitter.janome/c2.splitter.janome-0.1.0.tar.gz
  md5: 84d9fedd61de0a5a5051c0749421357d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - c2.splitter.janome = c2.splitter.janome:main
    #
    # Would create an entry point called c2.splitter.janome that calls c2.splitter.janome.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - janome

  run:
    - python
    - setuptools
    - setuptools
    - janome

test:
  # Python imports
  imports:
    - c2
    - c2.splitter
    - c2.splitter.janome

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/nakagami/c2.splitter.janome
  license: GPL
  summary: 'This product is split word by janome for Plone.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
