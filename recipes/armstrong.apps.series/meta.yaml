package:
  name: armstrong.apps.series
  version: "0.2.0"

source:
  fn: armstrong.apps.series-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/a/armstrong.apps.series/armstrong.apps.series-0.2.0.tar.gz
  md5: b32715a6073c80b27a1c0e31e4e4b0b9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - armstrong.apps.series = armstrong.apps.series:main
    #
    # Would create an entry point called armstrong.apps.series that calls armstrong.apps.series.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django >=1.3,<=1.4.5

  run:
    - python
    - django >=1.3,<=1.4.5

test:
  # Python imports
  imports:
    - armstrong.apps.series
    - armstrong.apps.series.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/armstrong/armstrong.apps.series/
  license: Apache Software License
  summary: 'Provides way to create series of content'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
