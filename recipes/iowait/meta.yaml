package:
  name: iowait
  version: "0.2"

source:
  fn: iowait-0.2.tar.gz
  url: https://pypi.python.org/packages/source/i/iowait/iowait-0.2.tar.gz
  md5: f49ca7766fe4a67e03a731e575614f87
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - iowait = iowait:main
    #
    # Would create an entry point called iowait that calls iowait.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://launchpad.net/python-iowait
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Platform-independent module for I/O completion events'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
