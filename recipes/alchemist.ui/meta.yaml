package:
  name: alchemist.ui
  version: "0.4.2"

source:
  fn: alchemist.ui-0.4.2.tar.gz
  url: https://pypi.python.org/packages/source/a/alchemist.ui/alchemist.ui-0.4.2.tar.gz
  md5: 4abffe10c113e136a7e2b834a49c37ec
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - alchemist.ui = alchemist.ui:main
    #
    # Would create an entry point called alchemist.ui that calls alchemist.ui.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - ore.alchemist
    - zope.formlib
    - zc.table
    - zope.viewlet
    - simplejson

  run:
    - python
    - setuptools
    - ore.alchemist
    - zope.formlib
    - zc.table
    - zope.viewlet
    - simplejson

test:
  # Python imports
  imports:
    - alchemist
    - alchemist.ui

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: LGPL
  summary: 'user interface components for use with zope3 relational database applications, crud, listing and relation views'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
