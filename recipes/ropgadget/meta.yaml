package:
  name: ropgadget
  version: "5.4"

source:
  fn: ROPGadget-5.4.tar.gz
  url: https://pypi.python.org/packages/source/R/ROPGadget/ROPGadget-5.4.tar.gz
  md5: 9934f7afe3369376acb54e21e1fb528f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ropgadget = ropgadget:main
    #
    # Would create an entry point called ropgadget that calls ropgadget.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - capstone

  run:
    - python
    - capstone

test:
  # Python imports
  imports:
    - ropgadget
    - ropgadget.loaders
    - ropgadget.ropchain
    - ropgadget.ropchain.arch

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/JonathanSalwan/ROPgadget
  license: GNU General Public License v2 (GPLv2)
  summary: 'This tool lets you search your gadgets on your binaries to facilitate your ROP exploitation.\nROPgadget supports ELF, PE and Mach-O format on x86, x64, ARM, ARM64, PowerPC, SPARC and MIPS architectures.\nhttp://www.shell-storm.org/project/ROPgadget/'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
