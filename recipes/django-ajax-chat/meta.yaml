package:
  name: django-ajax-chat
  version: "0.2"

source:
  fn: django-ajax-chat-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-ajax-chat/django-ajax-chat-0.2.tar.gz
  md5: c489d4abde47301069646109de2ecc57
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-ajax-chat = django-ajax-chat:main
    #
    # Would create an entry point called django-ajax-chat that calls django-ajax-chat.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - djangoChat

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/sharan01/django-ajax-chat
  license: MIT License
  summary: 'A simple Django ajax chat.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
