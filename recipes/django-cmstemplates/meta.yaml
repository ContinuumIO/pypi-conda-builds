package:
  name: django-cmstemplates
  version: "0.3.2"

source:
  fn: django-cmstemplates-0.3.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-cmstemplates/django-cmstemplates-0.3.2.tar.gz
  md5: e62e13eb9f64f6d3968849a74e0f99ff
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-cmstemplates = django-cmstemplates:main
    #
    # Would create an entry point called django-cmstemplates that calls django-cmstemplates.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - cmstemplates
    - cmstemplates.migrations
    - cmstemplates.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/asyncee/django-cmstemplates
  license: MIT License
  summary: 'Efficient django template blocks implementation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
