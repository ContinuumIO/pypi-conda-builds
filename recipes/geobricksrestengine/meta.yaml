package:
  name: geobricksrestengine
  version: "0.1.11"

source:
  fn: GeobricksRESTEngine-0.1.11.tar.gz
  url: https://pypi.python.org/packages/source/G/GeobricksRESTEngine/GeobricksRESTEngine-0.1.11.tar.gz
  md5: 864bc00fcd6b262c905818161b2e86ba
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - geobricksrestengine = geobricksrestengine:main
    #
    # Would create an entry point called geobricksrestengine that calls geobricksrestengine.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - watchdog
    - flask
    - flask-cors
    - importlib

  run:
    - python
    - watchdog
    - flask
    - flask-cors
    - importlib

test:
  # Python imports
  imports:
    - geobricks_rest_engine
    - geobricks_rest_engine.config
    - geobricks_rest_engine.core
    - geobricks_rest_engine.rest
    - geobricksrestengine

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/GeobricksRESTEngine/
  license: LICENSE.txt
  summary: 'Common entry point for Geobricks REST services.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
