package:
  name: imagefacts
  version: "1.1"

source:
  fn: imagefacts-1.1.tar.gz
  url: https://pypi.python.org/packages/source/i/imagefacts/imagefacts-1.1.tar.gz
  md5: a30d0ce02df23d30949f4fd41ba47cfc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - imagefacts = imagefacts:main
    #
    # Would create an entry point called imagefacts that calls imagefacts.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - imagefacts

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/markpasc/imagefacts
  license: BSD License
  summary: 'Find out the screen dimensions of the commonest image formats from a URL or bytestring'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
