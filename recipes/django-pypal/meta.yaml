package:
  name: django-pypal
  version: "0.1.0"

source:
  fn: django-pypal-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-pypal/django-pypal-0.1.0.tar.gz
  md5: 3775f15261bc442be8e35956b62d9ec0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-pypal = django-pypal:main
    #
    # Would create an entry point called django-pypal that calls django-pypal.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - pypal

  run:
    - python
    - django
    - pypal

test:
  # Python imports
  imports:
    - django_pypal

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jarcoal/django-pypal
  license: BSD
  summary: 'Django app for PyPal'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
