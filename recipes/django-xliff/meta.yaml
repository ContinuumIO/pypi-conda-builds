package:
  name: django-xliff
  version: "0.3.2"

source:
  fn: django-xliff-0.3.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-xliff/django-xliff-0.3.2.tar.gz
  md5: 76512666310cbcc8a9342d0a378a348d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-xliff = django-xliff:main
    #
    # Would create an entry point called django-xliff that calls django-xliff.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - example
    - example.simpleapp
    - xliff

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/callowayproject/django-xliff
  license: UNKNOWN
  summary: 'XML Localisation Interchange File Format (XLIFF) serializer for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
