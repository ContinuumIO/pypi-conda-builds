package:
  name: dib-utils
  version: "0.0.8"

source:
  fn: dib-utils-0.0.8.tar.gz
  url: https://pypi.python.org/packages/source/d/dib-utils/dib-utils-0.0.8.tar.gz
  md5: 78f80a98ea3ca4ffbd75fa575b43566f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dib-utils = dib-utils:main
    #
    # Would create an entry point called dib-utils that calls dib-utils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Apache Software License
  summary: 'Pieces of diskimage-builder that are useful without the rest of the project'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
