package:
  name: chevrons
  version: "0.1.3"

source:
  fn: chevrons-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/c/chevrons/chevrons-0.1.3.tar.gz
  md5: 9d170558aa162468989989136296fe16
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - chevrons = chevrons:main
    #
    # Would create an entry point called chevrons that calls chevrons.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - chevrons
    - chevrons.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lmc2179/chevrons
  license: UNKNOWN
  summary: 'Rapidly build pipelines for out-of-core data processing using higher order functions.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
