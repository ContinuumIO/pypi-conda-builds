package:
  name: django-olwidget
  version: "0.61.0"

source:
  fn: django-olwidget-0.61.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-olwidget/django-olwidget-0.61.0.tar.gz
  md5: 1a1dc31d70682d70f8c3916a558ef746
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-olwidget = django-olwidget:main
    #
    # Would create an entry point called django-olwidget that calls django-olwidget.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - olwidget

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://olwidget.org
  license: BSD License
  summary: 'OpenLayers mapping widget for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
