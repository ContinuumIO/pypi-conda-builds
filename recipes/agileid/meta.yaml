package:
  name: agileid
  version: "0.1.2"

source:
  fn: agileid-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/a/agileid/agileid-0.1.2.tar.gz
  md5: 8af7de69fa3ff8233a85886d39103045
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - agileid = agileid:main
    #
    # Would create an entry point called agileid that calls agileid.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pymongo >=2.7.2

  run:
    - python
    - pymongo >=2.7.2

test:
  # Python imports
  imports:
    - agileid

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kurttheviking/agileid-py
  license: ISC License (ISCL)
  summary: 'Generate and manage AgileID identifiers'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
