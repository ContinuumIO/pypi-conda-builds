package:
  name: redomino_cartasi
  version: "0.3"

source:
  fn: redomino_cartasi-0.3.zip
  url: https://pypi.python.org/packages/source/r/redomino_cartasi/redomino_cartasi-0.3.zip
  md5: b9a52091a5b9e3db6fa1b7f8e2373fab
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - redomino_cartasi = redomino_cartasi:main
    #
    # Would create an entry point called redomino_cartasi that calls redomino_cartasi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - redomino_cartasi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://redomino.com
  license: UNKNOWN
  summary: 'Cartasi LFS processor'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
