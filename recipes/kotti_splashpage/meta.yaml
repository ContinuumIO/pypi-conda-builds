package:
  name: kotti_splashpage
  version: "0.2.0"

source:
  fn: kotti_splashpage-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/k/kotti_splashpage/kotti_splashpage-0.2.0.tar.gz
  md5: 3f87c23530cea346ad406db75c65f578
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kotti_splashpage = kotti_splashpage:main
    #
    # Would create an entry point called kotti_splashpage that calls kotti_splashpage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - kms

  run:
    - python
    - setuptools
    - kms

test:
  # Python imports
  imports:
    - kotti_splashpage

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/koansys/kotti_splashpage
  license: UNKNOWN
  summary: '      kotti_splashpage is a kotti package to setup the home page as a splash\n      page. It allows you to simply set a template in your application\n      deployment configuration and have a splash page.\n      '

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
