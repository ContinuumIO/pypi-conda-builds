package:
  name: dwapython
  version: "1.1.4"

source:
  fn: DwaPython-1.1.4.tar.gz
  url: https://pypi.python.org/packages/source/D/DwaPython/DwaPython-1.1.4.tar.gz
  md5: 2acbc981caddb8d60340c5b776fcb3ff
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dwapython = dwapython:main
    #
    # Would create an entry point called dwapython that calls dwapython.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - dwa
    - dwapython
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Salamek/DwaPython
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Python library to communicate with Divine Warfare game API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
