package:
  name: djangocms-googlemap
  version: "0.2"

source:
  fn: djangocms-googlemap-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/djangocms-googlemap/djangocms-googlemap-0.2.tar.gz
  md5: 10b61a4deee792e6b2af54260c066e80
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangocms-googlemap = djangocms-googlemap:main
    #
    # Would create an entry point called djangocms-googlemap that calls djangocms-googlemap.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - djangocms_googlemap
    - djangocms_googlemap.migrations
    - djangocms_googlemap.migrations_django

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/divio/djangocms-googlemap
  license: BSD License
  summary: 'Google Maps plugin for django CMS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
