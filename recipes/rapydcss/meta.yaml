package:
  name: rapydcss
  version: "0.0.1"

source:
  fn: rapydcss-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/r/rapydcss/rapydcss-0.0.1.tar.gz
  md5: e93707caaa76b3640b694614ef812570
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - rapydcss = rapydcss:main
    #
    # Would create an entry point called rapydcss that calls rapydcss.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyscss >=1.1.4

  run:
    - python
    - pyscss >=1.1.4

test:
  # Python imports
  imports:
    - rapydcss

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.pyjeon.com/rapydcss
  license: GNU GPL3
  summary: 'Python-based SASS Compiler'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
