package:
  name: cosmic
  version: "0.5.5"

source:
  fn: cosmic-0.5.5.tar.gz
  url: https://pypi.python.org/packages/source/c/cosmic/cosmic-0.5.5.tar.gz
  md5: eb4543daf7b083c0727f788ee53a3677
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cosmic = cosmic:main
    #
    # Would create an entry point called cosmic that calls cosmic.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - werkzeug >=0.9.1
    - requests >=2.2.0
    - isodate >=0.5.1

  run:
    - python
    - werkzeug >=0.9.1
    - requests >=2.2.0
    - isodate >=0.5.1

test:
  # Python imports
  imports:
    - cosmic

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.cosmic-api.com/docs/cosmic/python/
  license: MIT License
  summary: 'A tiny web API framework based on Teleport'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
