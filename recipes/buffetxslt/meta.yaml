package:
  name: buffetxslt
  version: "0.2"

source:
  fn: BuffetXSLT-0.2.tar.gz
  url: https://pypi.python.org/packages/source/B/BuffetXSLT/BuffetXSLT-0.2.tar.gz
  md5: 44d9f4805993d316a4b9f698eb96e483
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - buffetxslt = buffetxslt:main
    #
    # Would create an entry point called buffetxslt that calls buffetxslt.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - buffetxslt

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://projects.dowski.com/projects/buffetxslt
  license: MIT
  summary: 'XSLT templating plugin'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
