package:
  name: pumpkinlb
  version: "1.2.1"

source:
  fn: PumpkinLB-1.2.1.tar.gz
  url: https://pypi.python.org/packages/source/P/PumpkinLB/PumpkinLB-1.2.1.tar.gz
  md5: de330453b94c05503ffb2b953e8f549d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pumpkinlb = pumpkinlb:main
    #
    # Would create an entry point called pumpkinlb that calls pumpkinlb.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - pumpkinlb

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: GNU Lesser General Public License v3 (LGPLv3)
  summary: 'A simple, fast, pure-python load balancer'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
