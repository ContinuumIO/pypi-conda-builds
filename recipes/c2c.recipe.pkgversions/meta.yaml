package:
  name: c2c.recipe.pkgversions
  version: "0.3dev1"

source:
  fn: c2c.recipe.pkgversions-0.3dev1.tar.gz
  url: https://pypi.python.org/packages/source/c/c2c.recipe.pkgversions/c2c.recipe.pkgversions-0.3dev1.tar.gz
  md5: ccf33a58e88cbb446551b9627423ffe7
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - c2c.recipe.pkgversions = c2c.recipe.pkgversions:main
    #
    # Would create an entry point called c2c.recipe.pkgversions that calls c2c.recipe.pkgversions.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout

test:
  # Python imports
  imports:
    - c2c
    - c2c.recipe
    - c2c.recipe.pkgversions

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/sbrunner/c2c.recipe.pkgversions
  license: BSD License
  summary: 'Test dependencies on Debian package versions'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
