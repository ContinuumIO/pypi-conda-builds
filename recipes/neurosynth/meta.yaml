package:
  name: neurosynth
  version: "0.3.3"

source:
  fn: neurosynth-0.3.3.tar.gz
  url: https://pypi.python.org/packages/source/n/neurosynth/neurosynth-0.3.3.tar.gz
  md5: dd16fd7c836c32dac029b3ea34a23fd8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - neurosynth = neurosynth:main
    #
    # Would create an entry point called neurosynth that calls neurosynth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - neurosynth
    - neurosynth.analysis
    - neurosynth.base
    - neurosynth.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/neurosynth/neurosynth
  license: UNKNOWN
  summary: 'Large-scale synthesis of functional neuroimaging data'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
