package:
  name: django-osmp
  version: "0.2.15"

source:
  fn: django-osmp-0.2.15.tar.gz
  url: https://pypi.python.org/packages/source/d/django-osmp/django-osmp-0.2.15.tar.gz
  md5: 01da8aac1d5ecd955b0440322bb8567b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-osmp = django-osmp:main
    #
    # Would create an entry point called django-osmp that calls django-osmp.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - osmp
    - osmp.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/gen1us2k/django-osmp.git
  license: MIT License
  summary: 'OSMP(Qiwi) Interface support for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
