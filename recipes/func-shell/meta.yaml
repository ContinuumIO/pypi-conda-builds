package:
  name: func-shell
  version: "1.4"

source:
  fn: func-shell-1.4.tar.gz
  url: https://pypi.python.org/packages/source/f/func-shell/func-shell-1.4.tar.gz
  md5: 2430953536e5b5c35ba52c1d5023c3a2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - func-shell = func-shell:main
    #
    # Would create an entry point called func-shell that calls func-shell.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/seveas/func-shell
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'Parallel shell using Func'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
