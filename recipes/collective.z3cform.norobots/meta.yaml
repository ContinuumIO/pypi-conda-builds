package:
  name: collective.z3cform.norobots
  version: "1.4.2.2"

source:
  fn: collective.z3cform.norobots-1.4.2.2.zip
  url: https://pypi.python.org/packages/source/c/collective.z3cform.norobots/collective.z3cform.norobots-1.4.2.2.zip
  md5: a1a5a8c583a4bd090160078fa8fa5fdd
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.z3cform.norobots = collective.z3cform.norobots:main
    #
    # Would create an entry point called collective.z3cform.norobots that calls collective.z3cform.norobots.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.z3cform
    - plone.app.registry

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.z3cform
    - plone.app.registry

test:
  # Python imports
  imports:
    - collective
    - collective.z3cform
    - collective.z3cform.norobots
    - collective.z3cform.norobots.browser
    - collective.z3cform.norobots.plone_forms
    - collective.z3cform.norobots.tests
    - collective.z3cform.norobots.upgrades

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.makina-corpus.com
  license: GPL
  summary: 'Human readable captcha for z3cform'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
