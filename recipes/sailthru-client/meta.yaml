package:
  name: sailthru-client
  version: "2.2.0"

source:
  fn: sailthru-client-2.2.0.tar.gz
  url: https://pypi.python.org/packages/source/s/sailthru-client/sailthru-client-2.2.0.tar.gz
  md5: e94589e0d2b5ec6c8d808dd28b22eaf9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sailthru-client = sailthru-client:main
    #
    # Would create an entry point called sailthru-client that calls sailthru-client.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=2.6.0
    - simplejson >=3.0.7

  run:
    - python
    - requests >=2.6.0
    - simplejson >=3.0.7

test:
  # Python imports
  imports:
    - sailthru

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/sailthru/sailthru-python-client
  license: MIT License
  summary: 'Python client for Sailthru API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
