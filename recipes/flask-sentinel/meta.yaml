package:
  name: flask-sentinel
  version: "0.0.2"

source:
  fn: Flask-Sentinel-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Sentinel/Flask-Sentinel-0.0.2.tar.gz
  md5: e35241c8b5f2f9f7201f83e6be5d57ad
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-sentinel = flask-sentinel:main
    #
    # Would create an entry point called flask-sentinel that calls flask-sentinel.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask-oauthlib
    - flask-pymongo
    - bcrypt
    - pyopenssl
    - redis

  run:
    - python
    - flask-oauthlib
    - flask-pymongo
    - bcrypt
    - pyopenssl
    - redis

test:
  # Python imports
  imports:
    - flask_sentinel
    - flask_sentinel.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/nicolaiarocci/flask-sentinel
  license: BSD License
  summary: 'OAuth2 Provider for Flask applications.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
