package:
  name: python-apply-linkedin
  version: "0.1.1"

source:
  fn: python-apply-linkedin-0.1.1.zip
  url: https://pypi.python.org/packages/source/p/python-apply-linkedin/python-apply-linkedin-0.1.1.zip
  md5: b3f256cfaa695eb6b3674178fa2efcb5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-apply-linkedin = python-apply-linkedin:main
    #
    # Would create an entry point called python-apply-linkedin that calls python-apply-linkedin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - apply_linkedin
    - apply_linkedin.django

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rmaceissoft/python-apply-linkedin
  license: UNKNOWN
  summary: 'library that made easy implement Apply with Linkedin functionality'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
