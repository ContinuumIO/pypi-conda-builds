package:
  name: collective.table
  version: "1.1.1"

source:
  fn: collective.table-1.1.1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.table/collective.table-1.1.1.tar.gz
  md5: 84161449b17e52dcdf608c8610a50533
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.table = collective.table:main
    #
    # Would create an entry point called collective.table that calls collective.table.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.cmfplone
    - collective.js.datatables

  run:
    - python
    - setuptools
    - setuptools
    - products.cmfplone
    - collective.js.datatables

test:
  # Python imports
  imports:
    - collective
    - collective.table
    - collective.table.browser
    - collective.table.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/collective/collective.table
  license: GNU General Public License (GPL)
  summary: 'Table-like content for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
