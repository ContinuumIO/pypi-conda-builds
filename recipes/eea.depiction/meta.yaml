package:
  name: eea.depiction
  version: "5.5"

source:
  fn: eea.depiction-5.5.zip
  url: https://pypi.python.org/packages/source/e/eea.depiction/eea.depiction-5.5.zip
  md5: 8e174193c5347b133c4fee59228de913
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - eea.depiction = eea.depiction:main
    #
    # Would create an entry point called eea.depiction that calls eea.depiction.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - pillow

  run:
    - python
    - setuptools
    - setuptools
    - pillow

test:
  # Python imports
  imports:
    - eea
    - eea.depiction
    - eea.depiction.browser
    - eea.depiction.content
    - eea.depiction.tests
    - eea.depiction.upgrades
    - eea.depiction.vocabularies

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://eea.github.com/docs/eea.depiction
  license: GNU General Public License (GPL)
  summary: 'EEA Depiction (formerly valentine.imagescales)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
