package:
  name: thorium
  version: "0.1.53"

source:
  fn: Thorium-0.1.53.tar.gz
  url: https://pypi.python.org/packages/source/T/Thorium/Thorium-0.1.53.tar.gz
  md5: fd58a886cf1ea8b27cc5c36a093a5cf5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - thorium = thorium:main
    #
    # Would create an entry point called thorium that calls thorium.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask ==0.10.1
    - jsonschema ==2.4.0
    - arrow ==0.5.4

  run:
    - python
    - flask ==0.10.1
    - jsonschema ==2.4.0
    - arrow ==0.5.4

test:
  # Python imports
  imports:
    - thorium
    - thorium.ext

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/EventMobi/thorium
  license: BSD License
  summary: 'A Python framework for RESTful API interfaces in Flask'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
