package:
  name: django-twostepauth
  version: "0.1dev"

source:
  fn: django-twostepauth-0.1dev.tar.gz
  url: https://pypi.python.org/packages/source/d/django-twostepauth/django-twostepauth-0.1dev.tar.gz
  md5: 2d2df47dd577f2a5ab83846fccfb68c8
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-twostepauth = django-twostepauth:main
    #
    # Would create an entry point called django-twostepauth that calls django-twostepauth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3

  run:
    - python
    - setuptools
    - django >=1.3

test:
  # Python imports
  imports:
    - twostepauth
    - twostepauth.management
    - twostepauth.management.commands
    - twostepauth.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/cogni/django-twostepauth
  license: BSD License
  summary: 'Two-step authentication for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
