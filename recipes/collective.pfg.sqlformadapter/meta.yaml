package:
  name: collective.pfg.sqlformadapter
  version: "1.0.3"

source:
  fn: collective.pfg.sqlformadapter-1.0.3.zip
  url: https://pypi.python.org/packages/source/c/collective.pfg.sqlformadapter/collective.pfg.sqlformadapter-1.0.3.zip
  md5: 443ad7f254eb7bb0ddd64c61cf1593d9
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.pfg.sqlformadapter = collective.pfg.sqlformadapter:main
    #
    # Would create an entry point called collective.pfg.sqlformadapter that calls collective.pfg.sqlformadapter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.ploneformgen
    - pymssql

  run:
    - python
    - setuptools
    - setuptools
    - products.ploneformgen
    - pymssql

test:
  # Python imports
  imports:
    - collective
    - collective.pfg
    - collective.pfg.sqlformadapter
    - collective.pfg.sqlformadapter.content
    - collective.pfg.sqlformadapter.interfaces
    - collective.pfg.sqlformadapter.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ianderso/collective.pfg.sqlformadapter
  license: GNU General Public License (GPL)
  summary: 'Product to insert values from the forms into msSQL'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
