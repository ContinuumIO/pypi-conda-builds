package:
  name: django-silhouette
  version: "0.0.2"

source:
  fn: django-silhouette-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-silhouette/django-silhouette-0.0.2.tar.gz
  md5: 7e0e8da279457c930f7e8b90d76580f2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-silhouette = django-silhouette:main
    #
    # Would create an entry point called django-silhouette that calls django-silhouette.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6
    - django-pods >=1.0.1

  run:
    - python
    - django >=1.6
    - django-pods >=1.0.1

test:
  # Python imports
  imports:
    - silhouette
    - silhouette.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/OohlaLabs/django-silhouette
  license: MIT License
  summary: 'Elegant Form Templating for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
