package:
  name: json-store
  version: "2.1"

source:
  fn: json-store-2.1.tar.gz
  url: https://pypi.python.org/packages/source/j/json-store/json-store-2.1.tar.gz
  md5: 5e37693ecd0171860bb040e85a146c35
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - json-store = json-store:main
    #
    # Would create an entry point called json-store that calls json-store.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - json_store

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/brainsik/json-store
  license: MIT License
  summary: 'A shelve-like store using JSON serialization.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
