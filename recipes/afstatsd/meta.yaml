package:
  name: afstatsd
  version: "1.1.0"

source:
  fn: AFStatsd-1.1.0.tar.gz
  url: https://pypi.python.org/packages/source/A/AFStatsd/AFStatsd-1.1.0.tar.gz
  md5: 7c0e7860b6c6cf8a6e470b24f7a9113a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - afstatsd = afstatsd:main
    #
    # Would create an entry point called afstatsd that calls afstatsd.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - afstatsd
    - afstatsd.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/appfirst/statsd_clients/tree/master/python
  license: UNKNOWN
  summary: 'Statsd Library for use with the AppFirst collector (http://www.appfirst.com)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
