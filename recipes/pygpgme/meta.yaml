package:
  name: pygpgme
  version: "0.3"

source:
  fn: pygpgme-0.3.tar.gz
  url: https://pypi.python.org/packages/source/p/pygpgme/pygpgme-0.3.tar.gz
  md5: d38355af73f0352cde3d410b25f34fd0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pygpgme = pygpgme:main
    #
    # Would create an entry point called pygpgme that calls pygpgme.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - gpgme
    - pygpgme

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://launchpad.net/pygpgme
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'A Python module for working with OpenPGP messages'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
