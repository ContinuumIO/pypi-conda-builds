package:
  name: pycurl
  version: "7.19.5.1"

source:
  fn: pycurl-7.19.5.1.tar.gz
  url: https://pypi.python.org/packages/source/p/pycurl/pycurl-7.19.5.1.tar.gz
  md5: f44cd54256d7a643ab7b16e3f409b26b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pycurl = pycurl:main
    #
    # Would create an entry point called pycurl that calls pycurl.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - curl

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pycurl.sourceforge.net/
  license: GNU Library or Lesser General Public License (LGPL) or MIT License
  summary: 'PycURL -- cURL library module for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
