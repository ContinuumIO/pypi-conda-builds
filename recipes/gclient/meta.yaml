package:
  name: gclient
  version: "0.7.2"

source:
  fn: gclient-0.7.2.tar.gz
  url: https://pypi.python.org/packages/source/g/gclient/gclient-0.7.2.tar.gz
  md5: 686cae3b2a299d213d36652038431db9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gclient = gclient:main
    #
    # Would create an entry point called gclient that calls gclient.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - depot_tools
    - depot_tools.third_party
    - depot_tools.third_party.colorama
    - depot_tools.third_party.repo
    - gclient

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/cybertk/gclient
  license: BSD-style License, see LICENSE.txt
  summary: 'Meta-checkout tool managing both subversion and git checkouts.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
