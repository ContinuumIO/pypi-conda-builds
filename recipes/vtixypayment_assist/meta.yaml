package:
  name: vtixypayment_assist
  version: "1.3.3"

source:
  fn: VTIXyPayment_Assist-1.3.3.tar.gz
  url: https://pypi.python.org/packages/source/V/VTIXyPayment_Assist/VTIXyPayment_Assist-1.3.3.tar.gz
  md5: e1767d90ac4d17a09028c000c01f6f54
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - vtixypayment_assist = vtixypayment_assist:main
    #
    # Would create an entry point called vtixypayment_assist that calls vtixypayment_assist.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django ==1.5.2
    - slumber ==0.6.0

  run:
    - python
    - django ==1.5.2
    - slumber ==0.6.0

test:
  # Python imports
  imports:
    - vtixy_payment_assist
    - vtixypayment_assist

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: LICENSE.txt
  summary: 'Payment gate for web applications of VTIX.RU clients'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
