package:
  name: bit9-api
  version: "1.0.8"

source:
  fn: bit9-api-1.0.8.tar.gz
  url: https://pypi.python.org/packages/source/b/bit9-api/bit9-api-1.0.8.tar.gz
  md5: b56a0a90cbfe8db9f72fc30a69e8bba0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bit9-api = bit9-api:main
    #
    # Would create an entry point called bit9-api that calls bit9-api.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=2.2.1

  run:
    - python
    - requests >=2.2.1

test:
  # Python imports
  imports:
    - bit9_api

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/blacktop/bit9-api
  license: GPLv3
  summary: 'Bit9 API for their Cyber Forensics Service'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
