package:
  name: boto-s3-shim
  version: "0.1.1"

source:
  fn: boto-s3-shim-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/b/boto-s3-shim/boto-s3-shim-0.1.1.tar.gz
  md5: 3960aed1e2239bb3ac4774762cb429ca
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - boto-s3-shim = boto-s3-shim:main
    #
    # Would create an entry point called boto-s3-shim that calls boto-s3-shim.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - boto_s3_shim

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/grahamc/boto_s3_shim
  license: MIT License
  summary: 'Inject an alternative S3 server into any boto-based project'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
