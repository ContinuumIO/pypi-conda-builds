package:
  name: testing.cassandra
  version: "1.1.4"

source:
  fn: testing.cassandra-1.1.4.tar.gz
  url: https://pypi.python.org/packages/source/t/testing.cassandra/testing.cassandra-1.1.4.tar.gz
  md5: 5c38d3f3d156b6f88953e953f7d39eec
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - testing.cassandra = testing.cassandra:main
    #
    # Would create an entry point called testing.cassandra that calls testing.cassandra.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pycassa
    - pyyaml

  run:
    - python
    - pycassa
    - pyyaml

test:
  # Python imports
  imports:
    - testing

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/tk0miya/testing.cassandra
  license: Apache Software License
  summary: 'automatically setups a cassandra instance in a temporary directory, and destroys it after testing'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
