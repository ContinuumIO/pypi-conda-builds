package:
  name: kundart.socialpublisher
  version: "1.1"

source:
  fn: kundart.socialpublisher-1.1.tar.gz
  url: https://pypi.python.org/packages/source/k/kundart.socialpublisher/kundart.socialpublisher-1.1.tar.gz
  md5: e292b29ed4d53d68fe921706224a9b50
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kundart.socialpublisher = kundart.socialpublisher:main
    #
    # Would create an entry point called kundart.socialpublisher that calls kundart.socialpublisher.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - python_twitter

  run:
    - python
    - setuptools
    - setuptools
    - python_twitter

test:
  # Python imports
  imports:
    - kundart
    - kundart.socialpublisher
    - kundart.socialpublisher.browser
    - kundart.socialpublisher.viewlets

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.kundart.com
  license: GPL
  summary: 'Publish your content to Twitter and Facebook easily'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
