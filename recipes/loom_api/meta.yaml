package:
  name: loom_api
  version: "0.0.1"

source:
  fn: loom_api-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/l/loom_api/loom_api-0.0.1.tar.gz
  md5: 007d069655840d2f6713ce9dc2bfe022
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - loom_api = loom_api:main
    #
    # Would create an entry point called loom_api that calls loom_api.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - loom
    - loom_api

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/loom/python-api-client
  license: MIT License
  summary: 'loom python api client'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
