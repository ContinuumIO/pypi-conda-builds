package:
  name: typing
  version: "0.0"

source:
  fn: typing-0.0.tar.gz
  url: https://pypi.python.org/packages/source/t/typing/typing-0.0.tar.gz
  md5: 78f58b0c010b54a1d1ff530616818fdd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - typing = typing:main
    #
    # Would create an entry point called typing that calls typing.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six

  run:
    - python
    - six

test:
  # Python imports
  imports:
    - typing

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://docs.python.org/3/library/typing.html#typing
  license: MIT License
  summary: 'This library brings typing from Python 3.5 to Python 3.2-3.4.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
