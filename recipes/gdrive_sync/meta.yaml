package:
  name: gdrive_sync
  version: "0.6.1"

source:
  fn: gdrive_sync-0.6.1.tar.gz
  url: https://pypi.python.org/packages/source/g/gdrive_sync/gdrive_sync-0.6.1.tar.gz
  md5: f72f7b4e50d32a14590d4d6545874740
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gdrive_sync = gdrive_sync:main
    #
    # Would create an entry point called gdrive_sync that calls gdrive_sync.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - google-api-python-client
    - python-dateutil
    - tzlocal

  run:
    - python
    - google-api-python-client
    - python-dateutil
    - tzlocal

test:
  # Python imports
  imports:
    - gdrive_sync

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/trlandet/gdrive_sync
  license: BSD License or GNU General Public License (GPL) or Python Software Foundation License
  summary: 'Sync directories between your computer and Google Drive'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
