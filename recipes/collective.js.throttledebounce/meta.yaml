package:
  name: collective.js.throttledebounce
  version: "1.1"

source:
  fn: collective.js.throttledebounce-1.1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.js.throttledebounce/collective.js.throttledebounce-1.1.tar.gz
  md5: 3e93397facaa528719e387c84a7c2d81
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.js.throttledebounce = collective.js.throttledebounce:main
    #
    # Would create an entry point called collective.js.throttledebounce that calls collective.js.throttledebounce.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.js
    - collective.js.throttledebounce

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.js.throttledebounce
  license: GPL2
  summary: 'Installs the throttle / debounce plugin'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
