package:
  name: plomino.tinymce
  version: "0.7.3"

source:
  fn: plomino.tinymce-0.7.3.zip
  url: https://pypi.python.org/packages/source/p/plomino.tinymce/plomino.tinymce-0.7.3.zip
  md5: 8b6218f4f17ff8f51c241268c76c53f1
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plomino.tinymce = plomino.tinymce:main
    #
    # Would create an entry point called plomino.tinymce that calls plomino.tinymce.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.tinymce >=1.2.13

  run:
    - python
    - setuptools
    - setuptools
    - products.tinymce >=1.2.13

test:
  # Python imports
  imports:
    - plomino
    - plomino.tinymce
    - plomino.tinymce.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/plomino.tinymce
  license: GPL
  summary: 'TinyMCE Plomino Embedded Plugin'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
