package:
  name: collective.openidselector
  version: "0.1"

source:
  fn: collective.openidselector-0.1.zip
  url: https://pypi.python.org/packages/source/c/collective.openidselector/collective.openidselector-0.1.zip
  md5: 72415c9b151a90c73a468f8b405086ed
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.openidselector = collective.openidselector:main
    #
    # Would create an entry point called collective.openidselector that calls collective.openidselector.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.openidselector

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/inigoconsulting/collective.openidselector
  license: BSD
  summary: 'Integration of openid-selector for plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
