package:
  name: aiopeewee
  version: "0.0.2"

source:
  fn: aiopeewee-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/a/aiopeewee/aiopeewee-0.0.2.tar.gz
  md5: 6908e5e9ec990a4cedc150f4940ba383
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - aiopeewee = aiopeewee:main
    #
    # Would create an entry point called aiopeewee that calls aiopeewee.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - peewee >=2.3.3
    - aiopg >=0.4.1

  run:
    - python
    - peewee >=2.3.3
    - aiopg >=0.4.1

test:
  # Python imports
  imports:
    - aiopeewee

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/05bit/python-aiopeewee
  license: Apache Software License
  summary: '\nAsynchronous interface for peewee ORM powered by asyncio.\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
