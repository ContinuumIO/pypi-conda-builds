package:
  name: django-enum
  version: "0.1"

source:
  fn: django-enum-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-enum/django-enum-0.1.tar.gz
  md5: a3fd191da5b820c57fd76dfd33905dd3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-enum = django-enum:main
    #
    # Would create an entry point called django-enum that calls django-enum.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - djenum

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/smulloni/django-enum/
  license: BSD License
  summary: 'A utility for writing Django choice enumerations.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
