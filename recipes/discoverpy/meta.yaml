package:
  name: discoverpy
  version: "0.3.1"

source:
  fn: discoverpy-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/d/discoverpy/discoverpy-0.3.1.tar.gz
  md5: c469b37c2cdc4f2b2f0c3abe44e47b74
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - discoverpy = discoverpy:main
    #
    # Would create an entry point called discoverpy that calls discoverpy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - werkzeug
    - restpy
    - jsonschema
    - sqlalchemy
    - requests

  run:
    - python
    - werkzeug
    - restpy
    - jsonschema
    - sqlalchemy
    - requests

test:
  # Python imports
  imports:
    - discoverpy
    - discoverpy.client
    - discoverpy.discover
    - discoverpy.resources

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kevinconway/discover.py
  license: MIT
  summary: 'REST framework for discoverable services.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
