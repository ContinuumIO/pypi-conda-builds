package:
  name: django-databrowse
  version: "2014.9.26"

source:
  fn: django-databrowse-2014.9.26.tar.gz
  url: https://pypi.python.org/packages/source/d/django-databrowse/django-databrowse-2014.9.26.tar.gz
  md5: e0572c19f7ef8f6f2b02d72d8daf450a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-databrowse = django-databrowse:main
    #
    # Would create an entry point called django-databrowse that calls django-databrowse.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - django_databrowse
    - django_databrowse.plugins

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Alir3z4/django-databrowse
  license: django-data-exporter
  summary: 'Databrowse is a Django application that lets you browse your data.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
