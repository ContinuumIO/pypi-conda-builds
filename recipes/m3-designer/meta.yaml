package:
  name: m3-designer
  version: "1.1.1"

source:
  fn: m3-designer-1.1.1.tar.gz
  url: https://pypi.python.org/packages/source/m/m3-designer/m3-designer-1.1.1.tar.gz
  md5: 96e1d74ecbd2edb852f8fc10414d96f0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - m3-designer = m3-designer:main
    #
    # Would create an entry point called m3-designer that calls m3-designer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - rope ==0.9.3
    - m3 ==1.0

  run:
    - python
    - rope ==0.9.3
    - m3 ==1.0

test:
  # Python imports
  imports:
    - m3_designer
    - m3_designer.ide
    - m3_designer.management
    - m3_designer.management.commands
    - tests
    - tests.core

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT License
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
