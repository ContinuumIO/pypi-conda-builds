package:
  name: magickpy
  version: "0.3"

source:
  fn: MagickPy-0.3.tar.gz
  url: https://pypi.python.org/packages/source/M/MagickPy/MagickPy-0.3.tar.gz
  md5: dd30c7baf7272eae05f139ce40d145ff
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - magickpy = magickpy:main
    #
    # Would create an entry point called magickpy that calls magickpy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flufl.enum

  run:
    - python
    - flufl.enum

test:
  # Python imports
  imports:
    - magickpy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/tailhook/magickpy
  license: MIT
  summary: 'Python object-oriented interface for ImageMagick using ctypes'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
