package:
  name: django-portland-oregon-addresses
  version: "1.0.1"

source:
  fn: django-portland-oregon-addresses-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-portland-oregon-addresses/django-portland-oregon-addresses-1.0.1.tar.gz
  md5: b083f365a976358937fea53739c0248a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-portland-oregon-addresses = django-portland-oregon-addresses:main
    #
    # Would create an entry point called django-portland-oregon-addresses that calls django-portland-oregon-addresses.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - portland_addresses
    - portland_addresses.management
    - portland_addresses.management.commands
    - portland_addresses.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/latestrevision/django-portland-oregon-addresses/
  license: MIT License
  summary: 'Geocode as many addreses in Portland, OR as you would like.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
