package:
  name: pdfminer
  version: "20140328"

source:
  fn: pdfminer-20140328.tar.gz
  url: https://pypi.python.org/packages/source/p/pdfminer/pdfminer-20140328.tar.gz
  md5: dfe3eb1b7b7017ab514aad6751a7c2ea
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pdfminer = pdfminer:main
    #
    # Would create an entry point called pdfminer that calls pdfminer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - pdfminer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://euske.github.io/pdfminer/index.html
  license: MIT License
  summary: 'PDF parser and analyzer'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
