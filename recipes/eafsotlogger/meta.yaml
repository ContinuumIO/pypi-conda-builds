package:
  name: eafsotlogger
  version: "1.1"

source:
  fn: eafsotlogger-1.1.tar.gz
  url: https://pypi.python.org/packages/source/e/eafsotlogger/eafsotlogger-1.1.tar.gz
  md5: 553b75882dd204cd90d6c92a24f0e3c4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - eafsotlogger = eafsotlogger:main
    #
    # Would create an entry point called eafsotlogger that calls eafsotlogger.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cassandra-driver

  run:
    - python
    - cassandra-driver

test:
  # Python imports
  imports:
    - eafsotlogger
    - soteventlogger

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.netcore.in/
  license: GNU General Public License (GPL)
  summary: 'SOT Library, Maintain log in casandra database using python script.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
