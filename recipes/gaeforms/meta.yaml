package:
  name: gaeforms
  version: "0.14"

source:
  fn: gaeforms-0.14.tar.gz
  url: https://pypi.python.org/packages/source/g/gaeforms/gaeforms-0.14.tar.gz
  md5: 945dff231b80f430e8fa1a19b88a70a4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gaeforms = gaeforms:main
    #
    # Would create an entry point called gaeforms that calls gaeforms.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pytz >=2014.4
    - babel >=1.3

  run:
    - python
    - pytz >=2014.4
    - babel >=1.3

test:
  # Python imports
  imports:
    - gaeforms
    - gaeforms.ndb

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/renzon/gaeforms
  license: BSD License
  summary: 'A automated form validation and transformation based on App Engine Models'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
