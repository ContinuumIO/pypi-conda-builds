package:
  name: flask-admin-openerp
  version: "0.3.3"

source:
  fn: flask-admin-openerp-0.3.3.tar.gz
  url: https://pypi.python.org/packages/source/f/flask-admin-openerp/flask-admin-openerp-0.3.3.tar.gz
  md5: ebbf70051d4e210cc0d9a4c6f1088041
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-admin-openerp = flask-admin-openerp:main
    #
    # Would create an entry point called flask-admin-openerp that calls flask-admin-openerp.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask-admin
    - erppeek

  run:
    - python
    - flask-admin
    - erppeek

test:
  # Python imports
  imports:
    - flask_admin_openerp

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.gisce.net
  license: MIT
  summary: 'OpenERP Backend for Flask-Admin'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
