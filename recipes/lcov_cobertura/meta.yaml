package:
  name: lcov_cobertura
  version: "1.5"

source:
  fn: lcov_cobertura-1.5.tar.gz
  url: https://pypi.python.org/packages/source/l/lcov_cobertura/lcov_cobertura-1.5.tar.gz
  md5: f3005cc8441923622d4c860e9cd0663b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lcov_cobertura = lcov_cobertura:main
    #
    # Would create an entry point called lcov_cobertura that calls lcov_cobertura.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - lcov_cobertura

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://eriwen.github.com/lcov-to-cobertura-xml/
  license: Apache Software License
  summary: 'LCOV to Cobertura XML converter'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
