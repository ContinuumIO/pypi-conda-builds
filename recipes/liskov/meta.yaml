package:
  name: liskov
  version: "0.2.2"

source:
  fn: liskov-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/l/liskov/liskov-0.2.2.tar.gz
  md5: 9374f0133bad267cf9883286469a6beb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - liskov = liskov:main
    #
    # Would create an entry point called liskov that calls liskov.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - liskov

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.python.org/pypi/liskov
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Small helpers to make inheritance with unit tests'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
