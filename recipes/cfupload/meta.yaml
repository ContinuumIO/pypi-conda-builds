package:
  name: cfupload
  version: "0.5.1"

source:
  fn: cfupload-0.5.1.tar.gz
  url: https://pypi.python.org/packages/source/c/cfupload/cfupload-0.5.1.tar.gz
  md5: e1baf7575fd04f561a91f1b622428f9c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cfupload = cfupload:main
    #
    # Would create an entry point called cfupload that calls cfupload.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python-cloudfiles

  run:
    - python
    - python-cloudfiles

test:
  # Python imports
  imports:
    - cfupload

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/DavidWittman/Simple-Cloud-Files-Uploader
  license: BSD License
  summary: 'A very simple Rackspace Cloud Files uploader'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
