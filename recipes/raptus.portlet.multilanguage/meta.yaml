package:
  name: raptus.portlet.multilanguage
  version: "1.0"

source:
  fn: raptus.portlet.multilanguage-1.0.tar.gz
  url: https://pypi.python.org/packages/source/r/raptus.portlet.multilanguage/raptus.portlet.multilanguage-1.0.tar.gz
  md5: cd9498bc15248b656140aff10e75fe56
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - raptus.portlet.multilanguage = raptus.portlet.multilanguage:main
    #
    # Would create an entry point called raptus.portlet.multilanguage that calls raptus.portlet.multilanguage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.portlet.static

  run:
    - python
    - setuptools
    - setuptools
    - plone.portlet.static

test:
  # Python imports
  imports:
    - raptus
    - raptus.portlet
    - raptus.portlet.multilanguage

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://svn.plone.org/svn/collective/raptus.portlet.multilanguage
  license: GPL
  summary: 'Provides a static text portlet for which one may select the language for which the portlet is available'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
