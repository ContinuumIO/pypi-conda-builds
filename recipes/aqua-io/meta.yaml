package:
  name: aqua-io
  version: "0.1.0"

source:
  fn: aqua-io-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/a/aqua-io/aqua-io-0.1.0.tar.gz
  md5: 75f1d6fa764f3796aa87d53be45f5e43
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - aqua-io = aqua-io:main
    #
    # Would create an entry point called aqua-io that calls aqua-io.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=2.1.0

  run:
    - python
    - requests >=2.1.0

test:
  # Python imports
  imports:
    - aqua_io
    - aqua_io.api
    - aqua_io.error
    - aqua_io.http_client

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://aqua.io
  license: MIT License
  summary: 'Official Aqua.io API library client for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
