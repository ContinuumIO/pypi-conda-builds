package:
  name: imageutils
  version: "0.3.7"

source:
  fn: ImageUtils-0.3.7.tar.gz
  url: https://pypi.python.org/packages/source/I/ImageUtils/ImageUtils-0.3.7.tar.gz
  md5: 1b56352a27d9523aa44b8458b5e47da7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - imageutils = imageutils:main
    #
    # Would create an entry point called imageutils that calls imageutils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pil

  run:
    - python
    - pil

test:
  # Python imports
  imports:
    - imageutils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD License
  summary: 'Various utilities for working with images.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
