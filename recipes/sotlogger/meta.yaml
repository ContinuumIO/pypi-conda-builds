package:
  name: sotlogger
  version: "1.0"

source:
  fn: sotlogger-1.0.tar.gz
  url: https://pypi.python.org/packages/source/s/sotlogger/sotlogger-1.0.tar.gz
  md5: 0d001831736491e5bfa999379df6f62d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sotlogger = sotlogger:main
    #
    # Would create an entry point called sotlogger that calls sotlogger.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cassandra-driver

  run:
    - python
    - cassandra-driver

test:
  # Python imports
  imports:
    - soteventlogger
    - sotlogger

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.netcore.in/
  license: GNU General Public License (GPL)
  summary: 'SOT Library, Maintain event logs in casandra database using python connection script.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
