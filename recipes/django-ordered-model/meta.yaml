package:
  name: django-ordered-model
  version: "0.4.1"

source:
  fn: django-ordered-model-0.4.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-ordered-model/django-ordered-model-0.4.1.tar.gz
  md5: 904181d5f5f92da82f37ff6656552db2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-ordered-model = django-ordered-model:main
    #
    # Would create an entry point called django-ordered-model that calls django-ordered-model.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - ordered_model
    - ordered_model.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/bfirsh/django-ordered-model/
  license: BSD License
  summary: 'Allows Django models to be ordered and provides a simple admin interface for reordering them.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
