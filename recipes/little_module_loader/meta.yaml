package:
  name: little_module_loader
  version: "0.2"

source:
  fn: little_module_loader-0.2.tar.gz
  url: https://pypi.python.org/packages/source/l/little_module_loader/little_module_loader-0.2.tar.gz
  md5: 96bcbe97915c4255d607f826e4686c45
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - little_module_loader = little_module_loader:main
    #
    # Would create an entry point called little_module_loader that calls little_module_loader.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - little_module_loader

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://russellhay.com/tagged/little
  license: PSF
  summary: 'A very simple module loader package for dynamically creating lists of modules, classes, and functions'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
