package:
  name: fusil
  version: "1.5"

source:
  fn: fusil-1.5.tar.gz
  url: https://pypi.python.org/packages/source/f/fusil/fusil-1.5.tar.gz
  md5: 14a7fbe29c91117ea50f8a6223fc9ea9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fusil = fusil:main
    #
    # Would create an entry point called fusil that calls fusil.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - fusil
    - fusil.linux
    - fusil.mas
    - fusil.network
    - fusil.process

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/haypo/fusil/wiki/Home
  license: GNU General Public License (GPL)
  summary: 'Fuzzing framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
