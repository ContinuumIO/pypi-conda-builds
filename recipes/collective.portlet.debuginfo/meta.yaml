package:
  name: collective.portlet.debuginfo
  version: "1.0"

source:
  fn: collective.portlet.debuginfo-1.0.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.portlet.debuginfo/collective.portlet.debuginfo-1.0.tar.gz
  md5: cc192f125890e0cc4ec7116f09134e36
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.portlet.debuginfo = collective.portlet.debuginfo:main
    #
    # Would create an entry point called collective.portlet.debuginfo that calls collective.portlet.debuginfo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.portlet
    - collective.portlet.debuginfo
    - collective.portlet.debuginfo.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://dms4u.cz/products
  license: GPL
  summary: 'A helper portlet which displays some usefull info about current user/context.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
