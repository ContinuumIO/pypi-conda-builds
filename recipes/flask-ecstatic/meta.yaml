package:
  name: flask-ecstatic
  version: "0.2.1"

source:
  fn: Flask-Ecstatic-0.2.1.zip
  url: https://pypi.python.org/packages/source/F/Flask-Ecstatic/Flask-Ecstatic-0.2.1.zip
  md5: bc32b5607c89ce8716fd8f6987733ffd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-ecstatic = flask-ecstatic:main
    #
    # Would create an entry point called flask-ecstatic that calls flask-ecstatic.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.5

  run:
    - python
    - flask >=0.5

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/salsita/flask-ecstatic
  license: MIT License
  summary: 'Serves static files with optional directory index'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
