package:
  name: collective.readitlater
  version: "1.1.0"

source:
  fn: collective.readitlater-1.1.0.zip
  url: https://pypi.python.org/packages/source/c/collective.readitlater/collective.readitlater-1.1.0.zip
  md5: 08c8a45452c34cf1e88ff1181db0d775
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.readitlater = collective.readitlater:main
    #
    # Would create an entry point called collective.readitlater that calls collective.readitlater.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.dexterity
    - plone.app.registry
    - plone.directives.form
    - collective.js.datatables

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.dexterity
    - plone.app.registry
    - plone.directives.form
    - collective.js.datatables

test:
  # Python imports
  imports:
    - collective
    - collective.readitlater
    - collective.readitlater.browser
    - collective.readitlater.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.readitlater
  license: GNU General Public License (GPL)
  summary: 'No time ? Read it later !'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
