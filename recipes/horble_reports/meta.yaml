package:
  name: horble_reports
  version: "1.0.2"

source:
  fn: horble_reports-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/h/horble_reports/horble_reports-1.0.2.tar.gz
  md5: 3eefe038ce4e9a40424eb88186e3ea2d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - horble_reports = horble_reports:main
    #
    # Would create an entry point called horble_reports that calls horble_reports.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - horble_reports

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/ccollins/horble_reports/
  license: MIT
  summary: 'Reports less horrible'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
