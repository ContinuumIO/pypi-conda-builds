package:
  name: helloshopply
  version: "0.0.3"

source:
  fn: HelloShopply-0.0.3.zip
  url: https://pypi.python.org/packages/source/H/HelloShopply/HelloShopply-0.0.3.zip
  md5: 516d195a28e25b5267311eb03055ae64
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - helloshopply = helloshopply:main
    #
    # Would create an entry point called helloshopply that calls helloshopply.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - pyes >=0.19.1
    - tornado >=2.4
    - nose >=1.2

  run:
    - python
    - pyes >=0.19.1
    - tornado >=2.4
    - nose >=1.2

test:
  # Python imports
  imports:
    - helloshopply
    - helloshopply.code
    - helloshopply.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/anandrjoshi/helloshopply/
  license: LICENSE.txt
  summary: 'Hello Shopply'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
