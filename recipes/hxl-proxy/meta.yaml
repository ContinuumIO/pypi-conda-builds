package:
  name: hxl-proxy
  version: "0.1alpha"

source:
  fn: hxl-proxy-0.1alpha.tar.gz
  url: https://pypi.python.org/packages/source/h/hxl-proxy/hxl-proxy-0.1alpha.tar.gz
  md5: 66581f680760ef371c164e63abcbc168
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hxl-proxy = hxl-proxy:main
    #
    # Would create an entry point called hxl-proxy that calls hxl-proxy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - libhxl >=1.02beta
    - ckanapi

  run:
    - python
    - flask
    - libhxl >=1.02beta
    - ckanapi

test:
  # Python imports
  imports:
    - hxl_proxy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/HXLStandard/hxl-proxy
  license: UNKNOWN
  summary: 'Flask-based web proxy for HXL'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
