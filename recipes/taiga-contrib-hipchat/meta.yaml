package:
  name: taiga-contrib-hipchat
  version: "0.0.2"

source:
  fn: taiga-contrib-hipchat-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/t/taiga-contrib-hipchat/taiga-contrib-hipchat-0.0.2.tar.gz
  md5: 6dee585d85fd27b2e086f2131d45dde9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - taiga-contrib-hipchat = taiga-contrib-hipchat:main
    #
    # Would create an entry point called taiga-contrib-hipchat that calls taiga-contrib-hipchat.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.7

  run:
    - python
    - django >=1.7

test:
  # Python imports
  imports:
    - taiga_contrib_hipchat
    - taiga_contrib_hipchat.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/taigaio/taiga-contrib-hipchat
  license: GNU Affero General Public License v3
  summary: 'The Taiga plugin for HipChat integration'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
