package:
  name: flask-jade2underscore
  version: "0.1"

source:
  fn: flask-jade2underscore-0.1.zip
  url: https://pypi.python.org/packages/source/f/flask-jade2underscore/flask-jade2underscore-0.1.zip
  md5: dbe5a75f9c93c3eb10b591cd0a7fc59b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-jade2underscore = flask-jade2underscore:main
    #
    # Would create an entry point called flask-jade2underscore that calls flask-jade2underscore.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - pyjade

  run:
    - python
    - flask
    - pyjade

test:
  # Python imports
  imports:
    - flaskext

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/weapp/flask-jade2underscore
  license: MIT License
  summary: 'A small Flask extension adds suppot to Jade2Underscore templates compiler (used in Backbone) to Flask.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
