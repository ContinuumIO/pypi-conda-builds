package:
  name: django-mustachejs
  version: "0.8.3-eol"

source:
  fn: django-mustachejs-0.8.3-eol.tar.gz
  url: https://pypi.python.org/packages/source/d/django-mustachejs/django-mustachejs-0.8.3-eol.tar.gz
  md5: 1ca2cc492849fe42d97eee1214d290ec
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-mustachejs = django-mustachejs:main
    #
    # Would create an entry point called django-mustachejs that calls django-mustachejs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - mustachejs
    - mustachejs.management
    - mustachejs.management.commands
    - mustachejs.templatetags
    - mustachejs.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mjumbewu/django-mustachejs/
  license: BSD License
  summary: 'A Django template tag for embedding Mustache.js templates safely.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
