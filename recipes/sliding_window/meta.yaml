package:
  name: sliding_window
  version: "0.0.2"

source:
  fn: sliding_window-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/s/sliding_window/sliding_window-0.0.2.tar.gz
  md5: 2a929942c10340f15ae3ff072d1ece1a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sliding_window = sliding_window:main
    #
    # Would create an entry point called sliding_window that calls sliding_window.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - sliding_window

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tlevine/sliding_window.git
  license: CC-BY-SA
  summary: 'Move a window over an iterater (ngrams).'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
