package:
  name: goldenratio
  version: "1.0.1"

source:
  fn: GoldenRatio-1.0.1.zip
  url: https://pypi.python.org/packages/source/G/GoldenRatio/GoldenRatio-1.0.1.zip
  md5: d48c6ef6c1e195c53eb2a05fa418155f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - goldenratio = goldenratio:main
    #
    # Would create an entry point called goldenratio that calls goldenratio.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - goldenratio

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.cerner.com
  license: UNKNOWN
  summary: 'Getting the Golden Ratio based on the Fibbonacci number sequence'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
