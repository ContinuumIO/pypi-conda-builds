package:
  name: django-prototype
  version: "0.2"

source:
  fn: django-prototype-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-prototype/django-prototype-0.2.tar.gz
  md5: d32a45b0b1aa572a2ec08968de219dd4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-prototype = django-prototype:main
    #
    # Would create an entry point called django-prototype that calls django-prototype.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4

  run:
    - python
    - django >=1.4

test:
  # Python imports
  imports:
    - prototype

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/goodtune/django-prototype
  license: UNKNOWN
  summary: 'Simple application to build interactive HTML based on the Django templating system.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
