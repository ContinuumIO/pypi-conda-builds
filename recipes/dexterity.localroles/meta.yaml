package:
  name: dexterity.localroles
  version: "0.1"

source:
  fn: dexterity.localroles-0.1.zip
  url: https://pypi.python.org/packages/source/d/dexterity.localroles/dexterity.localroles-0.1.zip
  md5: f821867444338f06ce2ef5fb3cfc1660
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dexterity.localroles = dexterity.localroles:main
    #
    # Would create an entry point called dexterity.localroles that calls dexterity.localroles.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - plone
    - plone.api
    - borg.localrole
    - collective.z3cform.datagridfield
    - five.grok
    - plone.app.dexterity
    - setuptools

  run:
    - python
    - setuptools
    - plone
    - plone.api
    - borg.localrole
    - collective.z3cform.datagridfield
    - five.grok
    - plone.app.dexterity
    - setuptools

test:
  # Python imports
  imports:
    - dexterity
    - dexterity.localroles
    - dexterity.localroles.browser
    - dexterity.localroles.browser.tests
    - dexterity.localroles.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/imio/
  license: gpl
  summary: 'Define local roles settings by dexterity type'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
