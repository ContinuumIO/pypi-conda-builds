package:
  name: winchester
  version: "0.55.dev34"

source:
  fn: winchester-0.55.dev34.tar.gz
  url: https://pypi.python.org/packages/source/w/winchester/winchester-0.55.dev34.tar.gz
  md5: cd96414fba3c43eae50b858477de3bac
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - winchester = winchester:main
    #
    # Would create an entry point called winchester that calls winchester.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - winchester

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Apache-2
  summary: 'An OpenStack notification event processing library.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
