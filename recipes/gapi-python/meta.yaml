package:
  name: gapi-python
  version: "0.0.5"

source:
  fn: gapi-python-0.0.5.tar.gz
  url: https://pypi.python.org/packages/source/g/gapi-python/gapi-python-0.0.5.tar.gz
  md5: 2b703da0fabd3aadd0373e6a5fefb93b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gapi-python = gapi-python:main
    #
    # Would create an entry point called gapi-python that calls gapi-python.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=1.0.4

  run:
    - python
    - requests >=1.0.4

test:
  # Python imports
  imports:
    - gapipy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'Python client for the G Adventures REST API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
