package:
  name: asyncwsgi
  version: "0.1.0"

source:
  fn: asyncwsgi-0.1.zip
  url: https://pypi.python.org/packages/source/a/asyncwsgi/asyncwsgi-0.1.zip
  md5: 6fe89bbb2877ef71072cf7d9819e8447
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - asyncwsgi = asyncwsgi:main
    #
    # Would create an entry point called asyncwsgi that calls asyncwsgi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - greenlet >=0.4.5

  run:
    - python
    - greenlet >=0.4.5

test:
  # Python imports
  imports:
    - asyncwsgi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/nehz/asyncwsgi
  license: MIT
  summary: 'Async WSGI support for tornado and asyncio'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
