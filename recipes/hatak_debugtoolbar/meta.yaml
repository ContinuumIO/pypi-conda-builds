package:
  name: hatak_debugtoolbar
  version: "0.1"

source:
  fn: Hatak_DebugToolBar-0.1.tar.gz
  url: https://pypi.python.org/packages/source/H/Hatak_DebugToolBar/Hatak_DebugToolBar-0.1.tar.gz
  md5: bc116553fa498cbb58a199234775cb42
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hatak_debugtoolbar = hatak_debugtoolbar:main
    #
    # Would create an entry point called hatak_debugtoolbar that calls hatak_debugtoolbar.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - hatak >=0.2
    - pyramid_debugtoolbar

  run:
    - python
    - hatak >=0.2
    - pyramid_debugtoolbar

test:
  # Python imports
  imports:
    - haplugin
    - hatak_debugtoolbar

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Apache License 2.0
  summary: 'Debug Tool Bar plugin for Hatak.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
