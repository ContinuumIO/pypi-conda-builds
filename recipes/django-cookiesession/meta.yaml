package:
  name: django-cookiesession
  version: "0.1.1"

source:
  fn: django-cookiesession-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-cookiesession/django-cookiesession-0.1.1.tar.gz
  md5: 75c197a7938dd314d2f0d8f9bb8b4745
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-cookiesession = django-cookiesession:main
    #
    # Would create an entry point called django-cookiesession that calls django-cookiesession.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - cookiesession
    - cookiesession.management
    - cookiesession.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/washingtontimes/django-cookiesession
  license: Apache Software License
  summary: 'A secure way to hold Django session data in cookies'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
