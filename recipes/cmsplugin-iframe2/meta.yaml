package:
  name: cmsplugin-iframe2
  version: "1.0-b1"

source:
  fn: cmsplugin-iframe2-1.0-b1.tar.gz
  url: https://pypi.python.org/packages/source/c/cmsplugin-iframe2/cmsplugin-iframe2-1.0-b1.tar.gz
  md5: 016ced08850aea5330f0dd5c4493695d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cmsplugin-iframe2 = cmsplugin-iframe2:main
    #
    # Would create an entry point called cmsplugin-iframe2 that calls cmsplugin-iframe2.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - cmsplugin_iframe2
    - cmsplugin_iframe2.conf

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/misli/cmsplugin-iframe2
  license: BSD License
  summary: u'Customizable Django CMS plugin for including iframes'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
