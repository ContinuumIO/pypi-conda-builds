package:
  name: django-manolo
  version: "2.4.0"

source:
  fn: django-manolo-2.4.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-manolo/django-manolo-2.4.0.tar.gz
  md5: 9cc9abc969438a23360fccbaa3d181af
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-manolo = django-manolo:main
    #
    # Would create an entry point called django-manolo that calls django-manolo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - manolo

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/aniversarioperu/django-manolo
  license: BSD License
  summary: 'Buscador de personas que visitan las instituciones del Estado'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
