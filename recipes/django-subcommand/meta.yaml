package:
  name: django-subcommand
  version: "0.3.2"

source:
  fn: django-subcommand-0.3.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-subcommand/django-subcommand-0.3.2.tar.gz
  md5: d8337e0a0f61cc16c9c10ab93f2c8385
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-subcommand = django-subcommand:main
    #
    # Would create an entry point called django-subcommand that calls django-subcommand.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - inflection
    - django-generate-scaffold

  run:
    - python
    - inflection
    - django-generate-scaffold

test:
  # Python imports
  imports:
    - subcommand
    - subcommand.management
    - subcommand.management.commands
    - subcommand.templates
    - subcommand.templates.subcommand

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ikeikeikeike/django-subcommand
  license: MIT License
  summary: 'Add sub command to A Django manage command'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
