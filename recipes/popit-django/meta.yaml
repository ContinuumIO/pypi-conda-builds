package:
  name: popit-django
  version: "0.0.3"

source:
  fn: popit-django-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/p/popit-django/popit-django-0.0.3.tar.gz
  md5: 7005f8b8f928508c290f4ae0fdbf23ee
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - popit-django = popit-django:main
    #
    # Would create an entry point called popit-django that calls popit-django.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - south
    - simplejson
    - slumber

  run:
    - python
    - django
    - south
    - simplejson
    - slumber

test:
  # Python imports
  imports:
    - popit
    - popit.management
    - popit.management.commands
    - popit.migrations
    - popit.tests
    - popit.tests.fixtures

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mysociety/popit-django
  license: GNU Affero General Public License v3
  summary: 'Use PopIt stored data in your Django app'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
