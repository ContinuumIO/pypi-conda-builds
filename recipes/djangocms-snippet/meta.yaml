package:
  name: djangocms-snippet
  version: "1.5"

source:
  fn: djangocms-snippet-1.5.tar.gz
  url: https://pypi.python.org/packages/source/d/djangocms-snippet/djangocms-snippet-1.5.tar.gz
  md5: 2ee1c55cba19fb1ce749c6ee20a28a7c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangocms-snippet = djangocms-snippet:main
    #
    # Would create an entry point called djangocms-snippet that calls djangocms-snippet.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - djangocms_snippet
    - djangocms_snippet.migrations
    - djangocms_snippet.migrations_django

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/divio/djangocms-snippet
  license: BSD License
  summary: 'HTML snippet plugin for django CMS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
