package:
  name: collective.contentrules.imaging
  version: "1.0"

source:
  fn: collective.contentrules.imaging-1.0.zip
  url: https://pypi.python.org/packages/source/c/collective.contentrules.imaging/collective.contentrules.imaging-1.0.zip
  md5: 6343bb9c42c10a1588564f5c207d01f0
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.contentrules.imaging = collective.contentrules.imaging:main
    #
    # Would create an entry point called collective.contentrules.imaging that calls collective.contentrules.imaging.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.contentrules
    - collective.contentrules.imaging
    - collective.contentrules.imaging.actions

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/toutpt/collective/collective.contentrules.imaging
  license: GNU General Public License (GPL)
  summary: 'Plone content rules related to imaging'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
