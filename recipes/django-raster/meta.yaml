package:
  name: django-raster
  version: "0.1.6"

source:
  fn: django-raster-0.1.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-raster/django-raster-0.1.6.tar.gz
  md5: bdab3fc9c7204e8acb07113a96319636
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-raster = django-raster:main
    #
    # Would create an entry point called django-raster that calls django-raster.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - psycopg2 >=2.5.3
    - gdal >=1.10.0
    - django >=1.7
    - numpy >=1.9.1
    - pillow >=2.7.0
    - django-colorful >=1.0.1

  run:
    - python
    - psycopg2 >=2.5.3
    - gdal >=1.10.0
    - django >=1.7
    - numpy >=1.9.1
    - pillow >=2.7.0
    - django-colorful >=1.0.1

test:
  # Python imports
  imports:
    - raster
    - raster.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/geodesign/django-raster
  license: BSD License
  summary: 'Simple raster file implementation for Django based on PostGis'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
