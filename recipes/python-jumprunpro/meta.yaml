package:
  name: python-jumprunpro
  version: "0.0.2"

source:
  fn: python-jumprunpro-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/p/python-jumprunpro/python-jumprunpro-0.0.2.tar.gz
  md5: eef3d26c5aa00f09a67dbcc737f8dd61
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-jumprunpro = python-jumprunpro:main
    #
    # Would create an entry point called python-jumprunpro that calls python-jumprunpro.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - beautifulsoup4 ==4.3.2
    - requests ==2.6.2
    - python-dateutil ==2.4.2

  run:
    - python
    - beautifulsoup4 ==4.3.2
    - requests ==2.6.2
    - python-dateutil ==2.4.2

test:
  # Python imports
  imports:
    - jumprun

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/natemara/jumprunpro-python
  license: MIT License
  summary: 'Simple python bindings for scraping data from JumpRun Pro'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
