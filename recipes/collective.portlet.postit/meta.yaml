package:
  name: collective.portlet.postit
  version: "1.0-beta1"

source:
  fn: collective.portlet.postit-1.0-beta1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.portlet.postit/collective.portlet.postit-1.0-beta1.tar.gz
  md5: 8f01e08aeb0d01b7f466f5d1347b7972
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.portlet.postit = collective.portlet.postit:main
    #
    # Would create an entry point called collective.portlet.postit that calls collective.portlet.postit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.portlet
    - collective.portlet.postit

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/collective.portlet.postit
  license: GPL
  summary: 'Post-It portlet and content type for Plone 3.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
