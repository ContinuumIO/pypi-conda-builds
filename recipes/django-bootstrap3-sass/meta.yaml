package:
  name: django-bootstrap3-sass
  version: "3.3.2.2"

source:
  fn: django-bootstrap3-sass-3.3.2.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-bootstrap3-sass/django-bootstrap3-sass-3.3.2.2.tar.gz
  md5: 2ad3675c9519089b6b0e6d9ac3c74b8a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-bootstrap3-sass = django-bootstrap3-sass:main
    #
    # Would create an entry point called django-bootstrap3-sass that calls django-bootstrap3-sass.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-libsass
    - django-compressor

  run:
    - python
    - django-libsass
    - django-compressor

test:
  # Python imports
  imports:
    - bootstrap3_sass

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD License
  summary: 'A simple Django app to import bootstrap3-sass.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
