package:
  name: tangible
  version: "0.2.2"

source:
  fn: tangible-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/t/tangible/tangible-0.2.2.tar.gz
  md5: a309d27544de092985a20915eb535bc3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tangible = tangible:main
    #
    # Would create an entry point called tangible that calls tangible.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - tangible
    - tangible.backends
    - tangible.shapes

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dbrgn/tangible
  license: GNU Lesser General Public License v3 or later (LGPLv3+)
  summary: 'A Python library to convert data into tangible 3D models.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
