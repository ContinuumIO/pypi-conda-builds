package:
  name: cubicweb-collaboration
  version: "1.0.1"

source:
  fn: cubicweb-collaboration-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/cubicweb-collaboration/cubicweb-collaboration-1.0.1.tar.gz
  md5: 77d9b33e270ea8f82a6d4f26154a9768
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cubicweb-collaboration = cubicweb-collaboration:main
    #
    # Would create an entry point called cubicweb-collaboration that calls cubicweb-collaboration.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cubicweb >=3.18
    - cubicweb-container >=2.6.0

  run:
    - python
    - cubicweb >=3.18
    - cubicweb-container >=2.6.0

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.cubicweb.org/project/cubicweb-collaboration
  license: LGPL
  summary: 'Cube for providing facilities for cooperative entity handling'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
