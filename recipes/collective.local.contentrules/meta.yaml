package:
  name: collective.local.contentrules
  version: "1.0rc1"

source:
  fn: collective.local.contentrules-1.0rc1.zip
  url: https://pypi.python.org/packages/source/c/collective.local.contentrules/collective.local.contentrules-1.0rc1.zip
  md5: f8fdf0c375ff9fadf9ccc822a96d8391
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.local.contentrules = collective.local.contentrules:main
    #
    # Would create an entry point called collective.local.contentrules that calls collective.local.contentrules.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.api

  run:
    - python
    - setuptools
    - setuptools
    - plone.api

test:
  # Python imports
  imports:
    - collective
    - collective.local
    - collective.local.contentrules
    - collective.local.contentrules.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/collective.local.contentrules
  license: GPL
  summary: 'Adds string interpolators to get emails of the collaborators on a document.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
