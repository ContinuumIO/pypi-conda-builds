package:
  name: django-dynamicforms
  version: "0.1.2"

source:
  fn: django-dynamicforms-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-dynamicforms/django-dynamicforms-0.1.2.tar.gz
  md5: d4c54566b4a07c06e8abd585ae6b215b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-dynamicforms = django-dynamicforms:main
    #
    # Would create an entry point called django-dynamicforms that calls django-dynamicforms.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - html2text

  run:
    - python
    - html2text

test:
  # Python imports
  imports:
    - dynamicforms
    - dynamicforms.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/roalddevries/django-dynamicforms/
  license: LICENSE.txt
  summary: 'Dynamically add forms to your site through the admin interface.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
