package:
  name: atomisator.filters
  version: "0.2.1"

source:
  fn: atomisator.filters-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/a/atomisator.filters/atomisator.filters-0.2.1.tar.gz
  md5: 811cceced6b2331af9c072b7add3a8f4
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - atomisator.filters = atomisator.filters:main
    #
    # Would create an entry point called atomisator.filters that calls atomisator.filters.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - beautifulsoup

  run:
    - python
    - setuptools
    - setuptools
    - beautifulsoup

test:
  # Python imports
  imports:
    - atomisator
    - atomisator.filters
    - atomisator.filters.guess_language
    - atomisator.filters.levenshtein
    - atomisator.filters.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://atomisator.ziade.org
  license: GPL
  summary: 'Expert Python Programming - Filters'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
