package:
  name: astrolib.coords
  version: "0.39.5"

source:
  fn: astrolib.coords-0.39.5.tar.gz
  url: https://pypi.python.org/packages/source/a/astrolib.coords/astrolib.coords-0.39.5.tar.gz
  md5: ac3fd2d48910b3da6d1c40ca2055e496
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - astrolib.coords = astrolib.coords:main
    #
    # Would create an entry point called astrolib.coords that calls astrolib.coords.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD License
  summary: 'Astronomical coordinates & angular separations (OO)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
