package:
  name: dokku-controller-agent
  version: "0.1"

source:
  fn: dokku-controller-agent-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/dokku-controller-agent/dokku-controller-agent-0.1.tar.gz
  md5: 4c5bb681fb489cdfa2a434b353872582
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dokku-controller-agent = dokku-controller-agent:main
    #
    # Would create an entry point called dokku-controller-agent that calls dokku-controller-agent.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - docker-py ==0.1.4
    - redis ==2.7.6

  run:
    - python
    - docker-py ==0.1.4
    - redis ==2.7.6

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
