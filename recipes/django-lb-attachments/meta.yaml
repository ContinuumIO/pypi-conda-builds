package:
  name: django-lb-attachments
  version: "0.8.3"

source:
  fn: django-lb-attachments-0.8.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-lb-attachments/django-lb-attachments-0.8.3.tar.gz
  md5: 08c3e4d70f237a459f1f694f65288b6f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-lb-attachments = django-lb-attachments:main
    #
    # Would create an entry point called django-lb-attachments that calls django-lb-attachments.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-helper >=0.8.1
    - south >=0.7.2

  run:
    - python
    - django-helper >=0.8.1
    - south >=0.7.2

test:
  # Python imports
  imports:
    - attachments
    - attachments.management
    - attachments.management.commands
    - attachments.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/vicalloy/django-lb-attachments/
  license: BSD
  summary: 'A django app to manager attachments.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
