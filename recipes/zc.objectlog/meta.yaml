package:
  name: zc.objectlog
  version: "0.2.2"

source:
  fn: zc.objectlog-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/z/zc.objectlog/zc.objectlog-0.2.2.tar.gz
  md5: 48976ebf496ff208d5e4bf8d0e89bca1
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zc.objectlog = zc.objectlog:main
    #
    # Would create an entry point called zc.objectlog that calls zc.objectlog.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zc.copy
    - zc.table
    - zope.app.keyreference
    - zope.app.zapi
    - zope.component
    - zope.i18n
    - zope.i18nmessageid
    - zope.interface
    - zope.location
    - zope.proxy
    - zope.publisher
    - zope.schema

  run:
    - python
    - setuptools
    - zc.copy
    - zc.table
    - zope.app.keyreference
    - zope.app.zapi
    - zope.component
    - zope.i18n
    - zope.i18nmessageid
    - zope.interface
    - zope.location
    - zope.proxy
    - zope.publisher
    - zope.schema

test:
  # Python imports
  imports:
    - zc
    - zc.objectlog
    - zc.objectlog.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: ZPL 2.1
  summary: 'a customizable log for a single object in Zope 3.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
