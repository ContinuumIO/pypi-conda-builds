package:
  name: monet.calendar.criteria
  version: "0.2.0"

source:
  fn: monet.calendar.criteria-0.2.0.zip
  url: https://pypi.python.org/packages/source/m/monet.calendar.criteria/monet.calendar.criteria-0.2.0.zip
  md5: f01d27784bfb4f1443f08691cc59e3c7
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - monet.calendar.criteria = monet.calendar.criteria:main
    #
    # Would create an entry point called monet.calendar.criteria that calls monet.calendar.criteria.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - monet.calendar.event >0.4.0

  run:
    - python
    - setuptools
    - setuptools
    - monet.calendar.event >0.4.0

test:
  # Python imports
  imports:
    - monet
    - monet.calendar
    - monet.calendar.criteria

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/monet.calendar.star
  license: GPL
  summary: 'Additional Plone collection criteria for Monet Calendar suite'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
