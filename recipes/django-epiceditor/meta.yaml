package:
  name: django-epiceditor
  version: "0.2.3"

source:
  fn: django-epiceditor-0.2.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-epiceditor/django-epiceditor-0.2.3.tar.gz
  md5: f2d2f4be03924bae4186fcec9f95ed33
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-epiceditor = django-epiceditor:main
    #
    # Would create an entry point called django-epiceditor that calls django-epiceditor.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3

  run:
    - python
    - django >=1.3

test:
  # Python imports
  imports:
    - epiceditor

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/barraq/django-epiceditor
  license: BSD License
  summary: 'A django app that allows the easy addition of EpicEditor markdown editor to a django form field, whether in a custom app or the Django Admin.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
