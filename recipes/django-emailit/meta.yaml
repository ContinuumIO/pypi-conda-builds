package:
  name: django-emailit
  version: "0.2.2"

source:
  fn: django-emailit-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-emailit/django-emailit-0.2.2.tar.gz
  md5: 528970a804f281063272ea30673d528c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-emailit = django-emailit:main
    #
    # Would create an entry point called django-emailit that calls django-emailit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3
    - premailer >=1.12
    - django-absolute

  run:
    - python
    - django >=1.3
    - premailer >=1.12
    - django-absolute

test:
  # Python imports
  imports:
    - emailit

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/divio/django-emailit
  license: BSD License
  summary: 'Make sending html emails easy.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
