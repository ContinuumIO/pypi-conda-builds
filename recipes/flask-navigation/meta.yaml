package:
  name: flask-navigation
  version: "0.2.0"

source:
  fn: Flask-Navigation-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Navigation/Flask-Navigation-0.2.0.tar.gz
  md5: d1075ee02a3f69da37e5cadad3395f31
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-navigation = flask-navigation:main
    #
    # Would create an entry point called flask-navigation that calls flask-navigation.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - blinker

  run:
    - python
    - flask
    - blinker

test:
  # Python imports
  imports:
    - flask_navigation

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tonyseek/flask-navigation
  license: MIT License
  summary: 'The navigation of Flask application.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
