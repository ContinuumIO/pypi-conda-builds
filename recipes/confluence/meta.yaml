package:
  name: confluence
  version: "0.1"

source:
  fn: confluence-0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/confluence/confluence-0.1.tar.gz
  md5: 589d41567fd15b8029682b6e73ea2368
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - confluence = confluence:main
    #
    # Would create an entry point called confluence that calls confluence.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - confluence

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/phoebian/confluence
  license: BSD License
  summary: 'A Python API to Confluence'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
