package:
  name: sphinxcontrib-robotframework
  version: "0.5.0"

source:
  fn: sphinxcontrib-robotframework-0.5.0.zip
  url: https://pypi.python.org/packages/source/s/sphinxcontrib-robotframework/sphinxcontrib-robotframework-0.5.0.zip
  md5: 5118dd239ec4382468c29ada8c6f44d9
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sphinxcontrib-robotframework = sphinxcontrib-robotframework:main
    #
    # Would create an entry point called sphinxcontrib-robotframework that calls sphinxcontrib-robotframework.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pygments >=1.6
    - sphinx
    - robotframework >=2.8.0

  run:
    - python
    - setuptools
    - pygments >=1.6
    - sphinx
    - robotframework >=2.8.0

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/datakurre/sphinxcontrib_robotframework
  license: GPL
  summary: 'Robot Framework extension for Sphinx'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
