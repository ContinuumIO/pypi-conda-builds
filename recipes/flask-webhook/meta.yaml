package:
  name: flask-webhook
  version: "0.1.0"

source:
  fn: Flask-Webhook-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Webhook/Flask-Webhook-0.1.0.tar.gz
  md5: c32e2b076ed68da1604117a430d6f753
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-webhook = flask-webhook:main
    #
    # Would create an entry point called flask-webhook that calls flask-webhook.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask

  run:
    - python
    - flask

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Bogdan-Alexandrescu/Flask-Webhook
  license: MIT License
  summary: 'Create Flask application webhooks with attached handlers'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
