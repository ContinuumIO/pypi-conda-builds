package:
  name: collective.ie8nomore
  version: "0.6"

source:
  fn: collective.ie8nomore-0.6.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.ie8nomore/collective.ie8nomore-0.6.tar.gz
  md5: 19f229acab6f47f1221fff447110f074
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.ie8nomore = collective.ie8nomore:main
    #
    # Would create an entry point called collective.ie8nomore that calls collective.ie8nomore.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.cmfplone
    - plonetheme.sunburst

  run:
    - python
    - setuptools
    - setuptools
    - products.cmfplone
    - plonetheme.sunburst

test:
  # Python imports
  imports:
    - collective
    - collective.ie8nomore

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.ie8nomore
  license: GNU General Public License (GPL)
  summary: 'Plone viewlet to prompt users to upgrade to a better web browser.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
