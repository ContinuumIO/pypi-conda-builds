package:
  name: sqlalchemy-elasticquery
  version: "0.0.1"

source:
  fn: sqlalchemy-elasticquery-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/s/sqlalchemy-elasticquery/sqlalchemy-elasticquery-0.0.1.tar.gz
  md5: 753c2fdee26d4b314df8ffe90ef88845
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sqlalchemy-elasticquery = sqlalchemy-elasticquery:main
    #
    # Would create an entry point called sqlalchemy-elasticquery that calls sqlalchemy-elasticquery.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sqlalchemy >=0.7.8

  run:
    - python
    - sqlalchemy >=0.7.8

test:
  # Python imports
  imports:
    - sqlalchemy_elasticquery

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/loverajoel/sqlalchemy-elasticquery
  license: MIT License
  summary: 'Use ElasticSearch query search in SQLAlchemy.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
