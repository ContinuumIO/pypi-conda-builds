package:
  name: django-quotidian
  version: "0.2.1"

source:
  fn: django-quotidian-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-quotidian/django-quotidian-0.2.1.tar.gz
  md5: c557d27921e5aed9d20be58af98d3371
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-quotidian = django-quotidian:main
    #
    # Would create an entry point called django-quotidian that calls django-quotidian.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - quotidian

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://kevindias.com/projects/quotidian/
  license: BSD License
  summary: 'Adds snippets of text to your Django templates.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
