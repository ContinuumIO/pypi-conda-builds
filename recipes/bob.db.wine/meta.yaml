package:
  name: bob.db.wine
  version: "2.0.1"

source:
  fn: bob.db.wine-2.0.1.zip
  url: https://pypi.python.org/packages/source/b/bob.db.wine/bob.db.wine-2.0.1.zip
  md5: 56912885b56ccff63912d9e523b6f598
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bob.db.wine = bob.db.wine:main
    #
    # Would create an entry point called bob.db.wine that calls bob.db.wine.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - bob.db.base

  run:
    - python
    - setuptools
    - setuptools
    - bob.db.base

test:
  # Python imports
  imports:
    - bob
    - bob.db
    - bob.db.wine

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://pypi.python.org/pypi/bob.db.wine
  license: GNU General Public License v3 (GPLv3)
  summary: 'Wine Database for bob'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
