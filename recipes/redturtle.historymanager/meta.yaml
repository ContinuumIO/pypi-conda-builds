package:
  name: redturtle.historymanager
  version: "0.9.0"

source:
  fn: redturtle.historymanager-0.9.0.zip
  url: https://pypi.python.org/packages/source/r/redturtle.historymanager/redturtle.historymanager-0.9.0.zip
  md5: 81367bb745174965fb9aa1aa7bd62ab5
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - redturtle.historymanager = redturtle.historymanager:main
    #
    # Would create an entry point called redturtle.historymanager that calls redturtle.historymanager.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - redturtle
    - redturtle.historymanager
    - redturtle.historymanager.adapters
    - redturtle.historymanager.browser
    - redturtle.historymanager.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/redturtle.historymanager
  license: BSD
  summary: 'RedTurtle History Manager'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
