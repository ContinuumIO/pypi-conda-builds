package:
  name: django-operis
  version: "1.5"

source:
  fn: django-operis-1.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-operis/django-operis-1.5.tar.gz
  md5: 51f11f86844346f782b74d29df708cc1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-operis = django-operis:main
    #
    # Would create an entry point called django-operis that calls django-operis.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - djangorestframework
    - django-jinja
    - django-autofixture

  run:
    - python
    - django
    - djangorestframework
    - django-jinja
    - django-autofixture

test:
  # Python imports
  imports:
    - operis
    - operis.api
    - operis.api.test
    - operis.management
    - operis.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Goldcap/django-operis
  license: MIT License
  summary: 'Autogeneration of Ember Models from Django Models'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
