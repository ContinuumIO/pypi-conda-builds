package:
  name: colorthief
  version: "0.1"

source:
  fn: colorthief-0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/colorthief/colorthief-0.1.tar.gz
  md5: 836d89096b1a7f64122403c70ae06274
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - colorthief = colorthief:main
    #
    # Would create an entry point called colorthief that calls colorthief.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pillow

  run:
    - python
    - pillow

test:
  # Python imports
  imports:
    - colorthief

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/fengsp/color-thief-py
  license: BSD License
  summary: 'A module for grabbing the color palette from an image.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
