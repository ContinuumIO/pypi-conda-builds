package:
  name: djungle-sizzle
  version: "0.0.0"

source:
  fn: djungle-sizzle-0.0.0.tar.gz
  url: https://pypi.python.org/packages/source/d/djungle-sizzle/djungle-sizzle-0.0.0.tar.gz
  md5: ff15eb87c59e2284925a7e036bfdefc8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djungle-sizzle = djungle-sizzle:main
    #
    # Would create an entry point called djungle-sizzle that calls djungle-sizzle.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4

  run:
    - python
    - django >=1.4

test:
  # Python imports
  imports:
    - djungle
    - djungle.sizzle

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/djungle/djungle-sizzle
  license: GNU General Public License (GPL)
  summary: 'Sizzle as Django app.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
