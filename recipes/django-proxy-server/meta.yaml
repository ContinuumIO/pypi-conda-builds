package:
  name: django-proxy-server
  version: "0.5"

source:
  fn: django-proxy-server-0.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-proxy-server/django-proxy-server-0.5.tar.gz
  md5: ee5c3e294ab0912f437fa3797f48218b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-proxy-server = django-proxy-server:main
    #
    # Would create an entry point called django-proxy-server that calls django-proxy-server.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6
    - djangorestframework >=2.4.1

  run:
    - python
    - django >=1.6
    - djangorestframework >=2.4.1

test:
  # Python imports
  imports:
    - proxy_server
    - proxy_server.authentication
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lmanzurv/django_proxy_server
  license: Academic Free License (AFL)
  summary: 'This is a django application to use django as a proxy server between a frontend device/server and a backend server inside a DMZ'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
