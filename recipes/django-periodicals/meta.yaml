package:
  name: django-periodicals
  version: "0.8.0"

source:
  fn: django-periodicals-0.8.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-periodicals/django-periodicals-0.8.0.tar.gz
  md5: 562c82a9ac255c979bebb037f9a86ff4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-periodicals = django-periodicals:main
    #
    # Would create an entry point called django-periodicals that calls django-periodicals.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-autoslug
    - django-haystack

  run:
    - python
    - django-autoslug
    - django-haystack

test:
  # Python imports
  imports:
    - periodicals

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/saschwarz/django-periodicals
  license: BSD License
  summary: 'Django app to create periodical/magazine websites"'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
