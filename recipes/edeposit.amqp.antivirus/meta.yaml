package:
  name: edeposit.amqp.antivirus
  version: "1.0.1"

source:
  fn: edeposit.amqp.antivirus-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/e/edeposit.amqp.antivirus/edeposit.amqp.antivirus-1.0.1.tar.gz
  md5: 99d4d02e61e4841e48d17e98463a995b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - edeposit.amqp.antivirus = edeposit.amqp.antivirus:main
    #
    # Would create an entry point called edeposit.amqp.antivirus that calls edeposit.amqp.antivirus.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sh >=1.09
    - pyclamd >=0.3.4

  run:
    - python
    - setuptools
    - sh >=1.09
    - pyclamd >=0.3.4

test:
  # Python imports
  imports:
    - edeposit
    - edeposit.amqp
    - edeposit.amqp.antivirus
    - edeposit.amqp.antivirus.wrappers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/edeposit/edeposit.amqp.antivirus/
  license: GNU General Public License v2 or later (GPLv2+)
  summary: "E-Deposit's AMQP wrapper over ClamAV."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
