package:
  name: surblclient
  version: "0.1"

source:
  fn: surblclient-0.1.tar.gz
  url: https://pypi.python.org/packages/source/s/surblclient/surblclient-0.1.tar.gz
  md5: 0718a9e4c666a4bd5944403c22c11dbe
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - surblclient = surblclient:main
    #
    # Would create an entry point called surblclient that calls surblclient.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - surblclient

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/infixfilip/surblclient/tree/master
  license: MIT
  summary: 'Simple client library for the surbl.org blacklists'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
