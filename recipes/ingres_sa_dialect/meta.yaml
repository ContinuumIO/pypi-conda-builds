package:
  name: ingres_sa_dialect
  version: "0.1"

source:
  fn: ingres_sa_dialect-0.1.zip
  url: https://pypi.python.org/packages/source/i/ingres_sa_dialect/ingres_sa_dialect-0.1.zip
  md5: b5cc1ccb738278024b66bf00c0068997
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ingres_sa_dialect = ingres_sa_dialect:main
    #
    # Would create an entry point called ingres_sa_dialect that calls ingres_sa_dialect.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - ingres_sa_dialect

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT
  summary: 'An Ingres dialect for SQLAlchemy'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
