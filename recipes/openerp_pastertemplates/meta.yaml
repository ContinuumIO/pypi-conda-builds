package:
  name: openerp_pastertemplates
  version: "1.0a5"

source:
  fn: openerp_pastertemplates-1.0a5.zip
  url: https://pypi.python.org/packages/source/o/openerp_pastertemplates/openerp_pastertemplates-1.0a5.zip
  md5: 6caee821e2353cc4a1b843eba0b30ca2
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - openerp_pastertemplates = openerp_pastertemplates:main
    #
    # Would create an entry point called openerp_pastertemplates that calls openerp_pastertemplates.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - pastescript
    - cheetah

  run:
    - python
    - setuptools
    - setuptools
    - pastescript
    - cheetah

test:
  # Python imports
  imports:
    - openerp_pastertemplates

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: UNKNOWN
  summary: 'Paster template for creating new OpenERP modules'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
