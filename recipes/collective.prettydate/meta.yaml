package:
  name: collective.prettydate
  version: "1.2.2"

source:
  fn: collective.prettydate-1.2.2.zip
  url: https://pypi.python.org/packages/source/c/collective.prettydate/collective.prettydate-1.2.2.zip
  md5: 170533168233a7f9933265124c54d4d8
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.prettydate = collective.prettydate:main
    #
    # Would create an entry point called collective.prettydate that calls collective.prettydate.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zope.i18nmessageid
    - zope.interface

  run:
    - python
    - setuptools
    - setuptools
    - zope.i18nmessageid
    - zope.interface

test:
  # Python imports
  imports:
    - collective
    - collective.prettydate
    - collective.prettydate.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.prettydate
  license: GNU General Public License v2 (GPLv2)
  summary: "Represents a date on a relative format so, instead of '01/02/2012', it would be displayed as '4 hours ago', 'yesterday' or 'last week', which is easier to read and understand for most people."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
