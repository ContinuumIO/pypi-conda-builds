package:
  name: tornado-api-kit
  version: "0.0.1"

source:
  fn: tornado-api-kit-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/t/tornado-api-kit/tornado-api-kit-0.0.1.tar.gz
  md5: 4b43c702dd14229fba8f66d946ec1172
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tornado-api-kit = tornado-api-kit:main
    #
    # Would create an entry point called tornado-api-kit that calls tornado-api-kit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - tornado
    - dict2xml

  run:
    - python
    - tornado
    - dict2xml

test:
  # Python imports
  imports:
    - apikit

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/sightio/tornado-api-kit
  license: BSD License
  summary: ('A collection of routines for building web APIs on top of Tornado web server. ',)

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
