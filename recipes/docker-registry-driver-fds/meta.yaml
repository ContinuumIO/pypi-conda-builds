package:
  name: docker-registry-driver-fds
  version: "1.1.0"

source:
  fn: docker-registry-driver-fds-1.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/docker-registry-driver-fds/docker-registry-driver-fds-1.1.0.tar.gz
  md5: 91804ffc4ab58130b8a7a3d307f2d229
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - docker-registry-driver-fds = docker-registry-driver-fds:main
    #
    # Would create an entry point called docker-registry-driver-fds that calls docker-registry-driver-fds.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - docker-registry-core >=2,<3
    - galaxy-fds-sdk >=1.0.3

  run:
    - python
    - docker-registry-core >=2,<3
    - galaxy-fds-sdk >=1.0.3

test:
  # Python imports
  imports:
    - docker_registry
    - docker_registry.drivers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Apache Software License
  summary: 'Docker registry driver fds'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
