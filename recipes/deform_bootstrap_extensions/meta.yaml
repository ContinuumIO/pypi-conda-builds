package:
  name: deform_bootstrap_extensions
  version: "0.1a"

source:
  fn: deform_bootstrap_extensions-0.1a.tar.gz
  url: https://pypi.python.org/packages/source/d/deform_bootstrap_extensions/deform_bootstrap_extensions-0.1a.tar.gz
  md5: 8b9d68b2a92c63889ee9eba25cc630ef
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - deform_bootstrap_extensions = deform_bootstrap_extensions:main
    #
    # Would create an entry point called deform_bootstrap_extensions that calls deform_bootstrap_extensions.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - deform_bootstrap

  run:
    - python
    - deform_bootstrap

test:
  # Python imports
  imports:
    - deform_bootstrap_extensions

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/majerteam/deform_bootstrap_extensions
  license: GNU General Public License v3 (GPLv3)
  summary: 'Usefull tools for making grid/accordion layouted forms'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
