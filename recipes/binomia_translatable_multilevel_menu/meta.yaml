package:
  name: binomia_translatable_multilevel_menu
  version: "0.0.7"

source:
  fn: binomia_translatable_multilevel_menu-0.0.7.tar.gz
  url: https://pypi.python.org/packages/source/b/binomia_translatable_multilevel_menu/binomia_translatable_multilevel_menu-0.0.7.tar.gz
  md5: e7dc7e9278a28e114a0ff37be98bc9a6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - binomia_translatable_multilevel_menu = binomia_translatable_multilevel_menu:main
    #
    # Would create an entry point called binomia_translatable_multilevel_menu that calls binomia_translatable_multilevel_menu.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - binomia_translatable_multilevel_menu
    - menu

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://packages.python.org/binomia_translatable_multilevel_menu
  license: BSD License
  summary: 'Simple translatable multilevel menu system.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
