package:
  name: geobricksgeoservermanager
  version: "0.1.0"

source:
  fn: GeobricksGeoserverManager-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/G/GeobricksGeoserverManager/GeobricksGeoserverManager-0.1.0.tar.gz
  md5: 72820ad4ca05b2aaa0786ef17dc7a279
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - geobricksgeoservermanager = geobricksgeoservermanager:main
    #
    # Would create an entry point called geobricksgeoservermanager that calls geobricksgeoservermanager.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - gsconfig
    - geobrickscommon

  run:
    - python
    - gsconfig
    - geobrickscommon

test:
  # Python imports
  imports:
    - geobricks_geoserver_manager
    - geobricks_geoserver_manager.config
    - geobricks_geoserver_manager.core
    - geobricks_geoserver_manager.utils
    - geobricksgeoservermanager
    - test
    - test.config

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/GeobricksGeoserverManager/
  license: LICENSE.txt
  summary: 'Geobricks library to manage Geoserver clusters.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
