package:
  name: events
  version: "0.2.1"

source:
  fn: Events-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/E/Events/Events-0.2.1.tar.gz
  md5: 01fb6bffd04b4f323e539c8cf592b423
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - events = events:main
    #
    # Would create an entry point called events that calls events.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - events
    - events.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/nicolaiarocci/events
  license: BSD License
  summary: 'Bringing the elegance of C# EventHanlder to Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
