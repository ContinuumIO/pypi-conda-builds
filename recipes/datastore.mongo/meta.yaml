package:
  name: datastore.mongo
  version: "0.1.1"

source:
  fn: datastore.mongo-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/datastore.mongo/datastore.mongo-0.1.1.tar.gz
  md5: 4fa11ae2d0a8f4f39af5705ed11330a6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - datastore.mongo = datastore.mongo:main
    #
    # Would create an entry point called datastore.mongo that calls datastore.mongo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - datastore >=0.3.3
    - pymongo ==2.4.2

  run:
    - python
    - datastore >=0.3.3
    - pymongo ==2.4.2

test:
  # Python imports
  imports:
    - datastore
    - datastore.mongo

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/datastore/datastore.mongo
  license: MIT License
  summary: 'mongodb datastore implementation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
