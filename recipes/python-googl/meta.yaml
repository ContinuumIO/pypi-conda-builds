package:
  name: python-googl
  version: "0.2.2"

source:
  fn: python-googl-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/p/python-googl/python-googl-0.2.2.tar.gz
  md5: 7be46ada5eaa87effa6380c43563b583
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-googl = python-googl:main
    #
    # Would create an entry point called python-googl that calls python-googl.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - httplib2

  run:
    - python
    - httplib2

test:
  # Python imports
  imports:
    - googl

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/python-googl
  license: UNKNOWN
  summary: 'Python goo.gl url shortener wrapper'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
