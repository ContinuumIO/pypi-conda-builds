package:
  name: django-json-dbindex
  version: "1.2.0"

source:
  fn: django-json-dbindex-1.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-json-dbindex/django-json-dbindex-1.2.0.tar.gz
  md5: 7ff5b027f2fee2c3258f4a5baacbed9b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-json-dbindex = django-json-dbindex:main
    #
    # Would create an entry point called django-json-dbindex that calls django-json-dbindex.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django <1.7
    - setuptools
    - psycopg2

  run:
    - python
    - django <1.7
    - setuptools
    - psycopg2

test:
  # Python imports
  imports:
    - json_dbindex

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://django-json-dbindex.readthedocs.org/
  license: BSD License
  summary: 'Django complementary index definition and management.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
