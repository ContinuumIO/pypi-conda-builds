package:
  name: ud-py-moneyed
  version: "0.5.2"

source:
  fn: ud-py-moneyed-0.5.2.tar.gz
  url: https://pypi.python.org/packages/source/u/ud-py-moneyed/ud-py-moneyed-0.5.2.tar.gz
  md5: 378f7fee9ce9f22fe1e331cd63018768
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ud-py-moneyed = ud-py-moneyed:main
    #
    # Would create an entry point called ud-py-moneyed that calls ud-py-moneyed.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - moneyed

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/udemy/py-moneyed
  license: BSD License
  summary: 'Provides Currency and Money classes for use in your Python code.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
