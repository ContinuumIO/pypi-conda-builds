package:
  name: django-plexauth
  version: "0.1.1"

source:
  fn: django-plexauth-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-plexauth/django-plexauth-0.1.1.tar.gz
  md5: f42ad09796ad88fa9467db882e03ab3e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-plexauth = django-plexauth:main
    #
    # Would create an entry point called django-plexauth that calls django-plexauth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests
    - django-bootstrap3
    - django-activelink

  run:
    - python
    - requests
    - django-bootstrap3
    - django-activelink

test:
  # Python imports
  imports:
    - plexauth

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/munnerz/django-plexauth
  license: BSD License
  summary: 'A django auth backend for authenticating against plex.tv'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
