package:
  name: django-cached-s3-storage
  version: "0.2.1"

source:
  fn: django-cached-s3-storage-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-cached-s3-storage/django-cached-s3-storage-0.2.1.tar.gz
  md5: 097dab1d0e777a97c4cafbef0192635c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-cached-s3-storage = django-cached-s3-storage:main
    #
    # Would create an entry point called django-cached-s3-storage that calls django-cached-s3-storage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ulyssesv/django-cached-s3-storage
  license: MIT
  summary: 'Django storage backend to be used with S3.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
