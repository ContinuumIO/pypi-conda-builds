package:
  name: makosg
  version: "0.0.3"

source:
  fn: makosg-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/m/makosg/makosg-0.0.3.tar.gz
  md5: 237b430953804dd4f9a3a12718d9e35e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - makosg = makosg:main
    #
    # Would create an entry point called makosg that calls makosg.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sendgrid-python
    - mako

  run:
    - python
    - sendgrid-python
    - mako

test:
  # Python imports
  imports:
    - makosg

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bcajes/makosg/
  license: MIT License
  summary: 'Easily render rich html emails for sendgrid using mako templates'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
