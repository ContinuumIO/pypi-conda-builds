package:
  name: bigsky
  version: "0.0.0"

source:
  fn: bigsky-0.0.0.tar.gz
  url: https://pypi.python.org/packages/source/b/bigsky/bigsky-0.0.0.tar.gz
  md5: 1afc676037f12a03d95aec21edf643b6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bigsky = bigsky:main
    #
    # Would create an entry point called bigsky that calls bigsky.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python-gflags
    - boto >=2.13.0
    - pil
    - flickrapi >=1.4.2
    - pil >=1.1.7
    - wsgiref >=0.1.2

  run:
    - python
    - python-gflags
    - boto >=2.13.0
    - pil
    - flickrapi >=1.4.2
    - pil >=1.1.7
    - wsgiref >=0.1.2

test:
  # Python imports
  imports:
    - bigsky

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/wnyc/bigsky
  license: MIT
  summary: "WNYC's big data image processing toolkit"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
