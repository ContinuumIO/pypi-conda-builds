package:
  name: country-currencies
  version: "0.1"

source:
  fn: country-currencies-0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/country-currencies/country-currencies-0.1.tar.gz
  md5: 77c3864c7ba838d77a3f2112845f559c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - country-currencies = country-currencies:main
    #
    # Would create an entry point called country-currencies that calls country-currencies.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - country_currencies

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT/X
  summary: 'mapping of ISO-3166 country codes to their respective currencies'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
