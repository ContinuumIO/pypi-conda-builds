package:
  name: bake_o_matic
  version: "0.1"

source:
  fn: bake_o_matic-0.1.tar.gz
  url: https://pypi.python.org/packages/source/b/bake_o_matic/bake_o_matic-0.1.tar.gz
  md5: 5c363920267ce7ea51e9f01ba5ec56e3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bake_o_matic = bake_o_matic:main
    #
    # Would create an entry point called bake_o_matic that calls bake_o_matic.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - bake_o_matic

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: LGPL
  summary: 'baker boilerplate reduction'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
