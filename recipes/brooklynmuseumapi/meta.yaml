package:
  name: brooklynmuseumapi
  version: "0.1.0"

source:
  fn: brooklynmuseumapi-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/b/brooklynmuseumapi/brooklynmuseumapi-0.1.0.tar.gz
  md5: 16b22de86b3a963c0a1a91ad0e7145b3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - brooklynmuseumapi = brooklynmuseumapi:main
    #
    # Would create an entry point called brooklynmuseumapi that calls brooklynmuseumapi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - brooklynmuseumapi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/anarchivist/brooklynmuseumapi
  license: GNU General Public License (GPL)
  summary: 'Interact with the Brooklyn Museum Collection API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
