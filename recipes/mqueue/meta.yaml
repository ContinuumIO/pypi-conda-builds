package:
  name: mqueue
  version: "0.4.1"

source:
  fn: mqueue-0.4.1.tar.gz
  url: https://pypi.python.org/packages/source/m/mqueue/mqueue-0.4.1.tar.gz
  md5: 9929f077c18d0e8cb7b77b36ff95dd4c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mqueue = mqueue:main
    #
    # Would create an entry point called mqueue that calls mqueue.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - decorated >=1.5.2
    - loggingd

  run:
    - python
    - decorated >=1.5.2
    - loggingd

test:
  # Python imports
  imports:
    - mqueue

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/CooledCoffee/mqueue/
  license: UNKNOWN
  summary: 'Simple queue based on mysql.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
