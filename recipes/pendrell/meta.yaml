package:
  name: pendrell
  version: "0.3.7"

source:
  fn: pendrell-0.3.7.tar.gz
  url: https://pypi.python.org/packages/source/p/pendrell/pendrell-0.3.7.tar.gz
  md5: 42b84e2ce35f033c0a8900ed971f1863
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pendrell = pendrell:main
    #
    # Would create an entry point called pendrell that calls pendrell.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - pendrell
    - pendrell.cases

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pendrell.olix0r.net/
  license: MIT License
  summary: 'HTTP 1.1 user agent for the programmable web'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
