package:
  name: django-input-mask
  version: "1.3.7.1"

source:
  fn: django-input-mask-1.3.7.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-input-mask/django-input-mask-1.3.7.1.tar.gz
  md5: 92c0369d5efee021d299658b3fa26175
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-input-mask = django-input-mask:main
    #
    # Would create an entry point called django-input-mask that calls django-input-mask.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - input_mask
    - input_mask.contrib
    - input_mask.contrib.localflavor
    - input_mask.contrib.localflavor.br
    - input_mask.contrib.localflavor.us

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/caioariede/django-input-mask
  license: MIT License
  summary: 'JavaScript input masks for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
