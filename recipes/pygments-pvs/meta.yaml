package:
  name: pygments-pvs
  version: "0.1"

source:
  fn: pygments-pvs-0.1.tar.gz
  url: https://pypi.python.org/packages/source/p/pygments-pvs/pygments-pvs-0.1.tar.gz
  md5: 793100863dda182df960f22c3988ddea
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pygments-pvs = pygments-pvs:main
    #
    # Would create an entry point called pygments-pvs that calls pygments-pvs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pygments

  run:
    - python
    - setuptools
    - pygments

test:
  # Python imports
  imports:
    - pygments_pvs

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Elarnon/pygments-pvs
  license: MIT License
  summary: 'Pygments lexer for PVS source files'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
