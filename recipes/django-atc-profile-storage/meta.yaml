package:
  name: django-atc-profile-storage
  version: "0.1.1"

source:
  fn: django-atc-profile-storage-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-atc-profile-storage/django-atc-profile-storage-0.1.1.tar.gz
  md5: 7d970ca0b99b59c266120f5b4a365c29
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-atc-profile-storage = django-atc-profile-storage:main
    #
    # Would create an entry point called django-atc-profile-storage that calls django-atc-profile-storage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - djangorestframework

  run:
    - python
    - djangorestframework

test:
  # Python imports
  imports:
    - atc_profile_storage
    - atc_profile_storage/migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/facebook/augmented-traffic-control
  license: django-athumb
  summary: 'ATC Profile storage app'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
