package:
  name: jsonlib
  version: "1.6.1"

source:
  fn: jsonlib-1.6.1.tar.gz
  url: https://pypi.python.org/packages/source/j/jsonlib/jsonlib-1.6.1.tar.gz
  md5: c1b71ae8e2f8ae14c4bfdda8923fa4a7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jsonlib = jsonlib:main
    #
    # Would create an entry point called jsonlib that calls jsonlib.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - jsonlib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://launchpad.net/jsonlib
  license: GNU General Public License (GPL)
  summary: 'JSON serializer/deserializer for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
