package:
  name: headfirst4
  version: "1.0.0"

source:
  fn: headfirst4-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/h/headfirst4/headfirst4-1.0.0.tar.gz
  md5: 43a440423993b8f17394dcc108e40219
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - headfirst4 = headfirst4:main
    #
    # Would create an entry point called headfirst4 that calls headfirst4.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - headfirst4

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.artist-circle.com
  license: UNKNOWN
  summary: 'A sample py module to demostrate module import and setup, make sure to type python3 setup.py sdist to create from the command line in the folder containing the module, and then sudo python3 setup.py install'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
