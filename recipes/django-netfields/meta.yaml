package:
  name: django-netfields
  version: "0.3"

source:
  fn: django-netfields-0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-netfields/django-netfields-0.3.tar.gz
  md5: a5b636ad8a7840c86f87b873f16b3c4f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-netfields = django-netfields:main
    #
    # Would create an entry point called django-netfields that calls django-netfields.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - netaddr
    - django >=1.3

  run:
    - python
    - netaddr
    - django >=1.3

test:
  # Python imports
  imports:
    - netfields

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jimfunk/django-postgresql-netfields
  license: BSD License
  summary: 'Django PostgreSQL netfields implementation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
