package:
  name: ivy-python
  version: "2.3-pre-2"

source:
  fn: ivy-python-2.3-pre-2.tar.gz
  url: https://pypi.python.org/packages/source/i/ivy-python/ivy-python-2.3-pre-2.tar.gz
  md5: 8de84a18ca2cb2941cd213d31b48bef1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ivy-python = ivy-python:main
    #
    # Would create an entry point called ivy-python that calls ivy-python.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - ivy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.eei.cena.fr/products/ivy/products/ivy/
  license: BSD License
  summary: 'Ivy: a lightweight software bus'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
