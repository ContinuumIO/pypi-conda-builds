package:
  name: o2w_cache_invalidator
  version: "0.0.1"

source:
  fn: o2w_cache_invalidator-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/o/o2w_cache_invalidator/o2w_cache_invalidator-0.0.1.tar.gz
  md5: ece8c2a4b45b1f3a0b50ea5091e08877
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - o2w_cache_invalidator = o2w_cache_invalidator:main
    #
    # Would create an entry point called o2w_cache_invalidator that calls o2w_cache_invalidator.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - o2w
    - o2w.cache_invalidator
    - o2w_cache_invalidator

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.o2w.es/
  license: LICENSE.txt
  summary: u'Invalidades varnish cache and memcached'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
