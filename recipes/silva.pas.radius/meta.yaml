package:
  name: silva.pas.radius
  version: "1.0"

source:
  fn: silva.pas.radius-1.0.tar.gz
  url: https://pypi.python.org/packages/source/s/silva.pas.radius/silva.pas.radius-1.0.tar.gz
  md5: e93ef6f4506ec5a5ebe856164c03c389
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - silva.pas.radius = silva.pas.radius:main
    #
    # Would create an entry point called silva.pas.radius that calls silva.pas.radius.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - silva.pas.base
    - silva.pas.membership
    - plone.session ==1.2

  run:
    - python
    - setuptools
    - silva.pas.base
    - silva.pas.membership
    - plone.session ==1.2

test:
  # Python imports
  imports:
    - silva
    - silva.pas
    - silva.pas.radius
    - silva.pas.radius.plugins

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://svn.infrae.com/silva.pas.radius/trunk
  license: BSD License
  summary: 'Radius support Silva'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
