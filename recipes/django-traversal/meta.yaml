package:
  name: django-traversal
  version: "0.1"

source:
  fn: django-traversal-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-traversal/django-traversal-0.1.tar.gz
  md5: f9a02fe3fd987d704f3fd9eed9ff3926
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-traversal = django-traversal:main
    #
    # Would create an entry point called django-traversal that calls django-traversal.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django <=1.7.5

  run:
    - python
    - django <=1.7.5

test:
  # Python imports
  imports:
    - traversal
    - traversal.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT License
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
