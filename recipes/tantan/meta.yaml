package:
  name: tantan
  version: "0.1.1"

source:
  fn: TanTan-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/T/TanTan/TanTan-0.1.1.tar.gz
  md5: 9f643fb9ba699d9ca642c540b805e2b1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tantan = tantan:main
    #
    # Would create an entry point called tantan that calls tantan.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools >=3.4.4
    - pyserial
    - pyopenssl
    - twisted ==13.1.0
    - autobahn ==0.8.6
    - paisley
    - txxbee

  run:
    - python
    - setuptools >=3.4.4
    - pyserial
    - pyopenssl
    - twisted ==13.1.0
    - autobahn ==0.8.6
    - paisley
    - txxbee

test:
  # Python imports
  imports:
    - tantan

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://pypi.python.org/pypi/TanTan
  license: LICENSE.txt
  summary: 'An open-source application for communicating with         Physical-Area-Networks using the ZigBee protocol'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
