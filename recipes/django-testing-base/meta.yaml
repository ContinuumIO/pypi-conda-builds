package:
  name: django-testing-base
  version: "0.4.0"

source:
  fn: django-testing-base-0.4.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-testing-base/django-testing-base-0.4.0.tar.gz
  md5: da869c94e0ab14e18ba0ed70c4c22a84
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-testing-base = django-testing-base:main
    #
    # Would create an entry point called django-testing-base that calls django-testing-base.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6,<1.8
    - selenium >=2.44,<3
    - mkdocs >=0.11,<2

  run:
    - python
    - django >=1.6,<1.8
    - selenium >=2.44,<3
    - mkdocs >=0.11,<2

test:
  # Python imports
  imports:
    - testbase
    - testbase.browser
    - testbase.unit
    - testbase.unit.views

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tctimmeh/django-testing-base
  license: MIT License
  summary: 'Simple Django testing base classes and patterns'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
