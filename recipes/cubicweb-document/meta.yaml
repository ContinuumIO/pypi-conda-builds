package:
  name: cubicweb-document
  version: "0.6.0"

source:
  fn: cubicweb-document-0.6.0.tar.gz
  url: https://pypi.python.org/packages/source/c/cubicweb-document/cubicweb-document-0.6.0.tar.gz
  md5: e347a15b6b05273092b0b0b66873252c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cubicweb-document = cubicweb-document:main
    #
    # Would create an entry point called cubicweb-document that calls cubicweb-document.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cubicweb-vcsfile >=1.6.0
    - cubicweb >=3.10.0
    - mercurial
    - python-cwtags
    - cubicweb-i18ncontent >=0.1.1
    - cubicweb-folder >=1.4.1

  run:
    - python
    - cubicweb-vcsfile >=1.6.0
    - cubicweb >=3.10.0
    - mercurial
    - python-cwtags
    - cubicweb-i18ncontent >=0.1.1
    - cubicweb-folder >=1.4.1

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.cubicweb.org/project/cubicweb-document
  license: LGPL
  summary: 'document component for the CubicWeb framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
