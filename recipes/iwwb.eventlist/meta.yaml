package:
  name: iwwb.eventlist
  version: "0.2.2"

source:
  fn: iwwb.eventlist-0.2.2.zip
  url: https://pypi.python.org/packages/source/i/iwwb.eventlist/iwwb.eventlist-0.2.2.zip
  md5: 7bacff5d7b5e8b1a5145c78a7d2bb69d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - iwwb.eventlist = iwwb.eventlist:main
    #
    # Would create an entry point called iwwb.eventlist that calls iwwb.eventlist.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - collective.js.datatables >=1.9
    - setuptools
    - suds
    - z3c.form
    - plone.formwidget.datetime

  run:
    - python
    - setuptools
    - collective.js.datatables >=1.9
    - setuptools
    - suds
    - z3c.form
    - plone.formwidget.datetime

test:
  # Python imports
  imports:
    - iwwb
    - iwwb.eventlist
    - iwwb.eventlist.browser
    - iwwb.eventlist.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/iwwb.eventlist
  license: BSD
  summary: 'Provides a Plone interface for accessing the event search of InfoWeb Weiterbildung'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
