package:
  name: plone.app.caching
  version: "1.2.3"

source:
  fn: plone.app.caching-1.2.3.tar.gz
  url: https://pypi.python.org/packages/source/p/plone.app.caching/plone.app.caching-1.2.3.tar.gz
  md5: 68ff8c643849ec41494f3c71475bd378
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.app.caching = plone.app.caching:main
    #
    # Would create an entry point called plone.app.caching that calls plone.app.caching.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - python-dateutil
    - plone.caching
    - plone.cachepurging
    - plone.app.registry >=1.0b5
    - zope.browserresource
    - zope.interface
    - zope.component
    - zope.publisher
    - zope.pagetemplate
    - plone.memoize
    - plone.protect
    - plone.registry >=1.0b4
    - products.cmfdynamicviewfti
    - products.genericsetup
    - products.cmfcore
    - products.statusmessages
    - zope2
    - acquisition
    - plone.app.z3cform
    - z3c.form
    - z3c.zcmlhook

  run:
    - python
    - setuptools
    - setuptools
    - python-dateutil
    - plone.caching
    - plone.cachepurging
    - plone.app.registry >=1.0b5
    - zope.browserresource
    - zope.interface
    - zope.component
    - zope.publisher
    - zope.pagetemplate
    - plone.memoize
    - plone.protect
    - plone.registry >=1.0b4
    - products.cmfdynamicviewfti
    - products.genericsetup
    - products.cmfcore
    - products.statusmessages
    - zope2
    - acquisition
    - plone.app.z3cform
    - z3c.form
    - z3c.zcmlhook

test:
  # Python imports
  imports:
    - plone
    - plone.app
    - plone.app.caching
    - plone.app.caching.browser
    - plone.app.caching.operations
    - plone.app.caching.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/plone.app.caching
  license: GNU General Public License (GPL)
  summary: 'Plone UI and default rules for plone.caching/z3c.caching'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
