package:
  name: django-googlytics
  version: "0.2.1"

source:
  fn: django-googlytics-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-googlytics/django-googlytics-0.2.1.tar.gz
  md5: 0c31745af593bc6e411ce7d8952e4b4a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-googlytics = django-googlytics:main
    #
    # Would create an entry point called django-googlytics that calls django-googlytics.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - googlytics

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rikpg/django-googlytics
  license: BSD License
  summary: 'Add google analytics snippet to your django templates.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
