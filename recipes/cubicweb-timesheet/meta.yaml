package:
  name: cubicweb-timesheet
  version: "0.12.0"

source:
  fn: cubicweb-timesheet-0.12.0.tar.gz
  url: https://pypi.python.org/packages/source/c/cubicweb-timesheet/cubicweb-timesheet-0.12.0.tar.gz
  md5: e4370c94e45767a89508b1e239323db0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cubicweb-timesheet = cubicweb-timesheet:main
    #
    # Would create an entry point called cubicweb-timesheet that calls cubicweb-timesheet.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cubicweb >=3.18.0
    - cubicweb-workorder >=0.9.0
    - cubicweb-calendar >=0.7.0

  run:
    - python
    - cubicweb >=3.18.0
    - cubicweb-workorder >=0.9.0
    - cubicweb-calendar >=0.7.0

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.cubicweb.org/project/cubicweb-timesheet
  license: LGPL
  summary: 'record who did what and when for the CubicWeb framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
