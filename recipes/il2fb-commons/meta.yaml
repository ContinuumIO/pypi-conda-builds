package:
  name: il2fb-commons
  version: "0.10.0"

source:
  fn: il2fb-commons-0.10.0.tar.gz
  url: https://pypi.python.org/packages/source/i/il2fb-commons/il2fb-commons-0.10.0.tar.gz
  md5: 58478b2ddade69d1bc8665d95c1c5285
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - il2fb-commons = il2fb-commons:main
    #
    # Would create an entry point called il2fb-commons that calls il2fb-commons.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - candv >=1.3,<1.4
    - six >=1.6.1
    - verboselib >=0.2,<0.3

  run:
    - python
    - candv >=1.3,<1.4
    - six >=1.6.1
    - verboselib >=0.2,<0.3

test:
  # Python imports
  imports:
    - il2fb.commons

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/IL2HorusTeam/il2fb-commons
  license: GNU Lesser General Public License v3 (LGPLv3)
  summary: 'Common helpers and data structures for projects related to IL-2 Forgotten Battles'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
