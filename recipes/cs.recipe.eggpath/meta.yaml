package:
  name: cs.recipe.eggpath
  version: "1.8"

source:
  fn: cs.recipe.eggpath-1.8.zip
  url: https://pypi.python.org/packages/source/c/cs.recipe.eggpath/cs.recipe.eggpath-1.8.zip
  md5: 729d1132980915d1c11889625b864f2e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cs.recipe.eggpath = cs.recipe.eggpath:main
    #
    # Would create an entry point called cs.recipe.eggpath that calls cs.recipe.eggpath.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - zc.recipe.egg

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - zc.recipe.egg

test:
  # Python imports
  imports:
    - cs
    - cs.recipe
    - cs.recipe.eggpath
    - cs.recipe.eggpath.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.codesyntax.com/products
  license: Zope Public License
  summary: 'A simple recipe to get the path of an egg'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
