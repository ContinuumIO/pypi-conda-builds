package:
  name: django-navtag
  version: "2.1.1"

source:
  fn: django-navtag-2.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-navtag/django-navtag-2.1.1.tar.gz
  md5: e9f6a3bbd65aea4440c304af7a99be23
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-navtag = django-navtag:main
    #
    # Would create an entry point called django-navtag that calls django-navtag.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_navtag
    - django_navtag.templatetags
    - django_navtag.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/SmileyChris/django-navtag
  license: MIT License
  summary: 'A simple Django navigation template tag'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
