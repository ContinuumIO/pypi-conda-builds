package:
  name: flask-creole
  version: "0.4.4"

source:
  fn: Flask-Creole-0.4.4.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Creole/Flask-Creole-0.4.4.tar.gz
  md5: f630ba5018c0a2ecf480bf30b506d05c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-creole = flask-creole:main
    #
    # Would create an entry point called flask-creole that calls flask-creole.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - creoleparser

  run:
    - python
    - flask
    - creoleparser

test:
  # Python imports
  imports:
    - flaskext

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/aafshar/flask-creole-main
  license: MIT License
  summary: 'Creole parser filters for Flask'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
