package:
  name: django-ssl-admin
  version: "1.1.4"

source:
  fn: django-ssl-admin-1.1.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-ssl-admin/django-ssl-admin-1.1.4.tar.gz
  md5: f36578e361bfa616b4094a2fe0134e32
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-ssl-admin = django-ssl-admin:main
    #
    # Would create an entry point called django-ssl-admin that calls django-ssl-admin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6.5

  run:
    - python
    - django >=1.6.5

test:
  # Python imports
  imports:
    - ssladmin

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://dirtymonkey.co.uk/django-ssl-admin
  license: MIT License
  summary: 'Django middleware to make the admin https only.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
