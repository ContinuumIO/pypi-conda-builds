package:
  name: check_run_over
  version: "0.1a"

source:
  fn: check_run_over-0.1a.tar.gz
  url: https://pypi.python.org/packages/source/c/check_run_over/check_run_over-0.1a.tar.gz
  md5: f733e59dc68f15399efcfdbf63433eee
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - check_run_over = check_run_over:main
    #
    # Would create an entry point called check_run_over that calls check_run_over.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - check_run_over

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: GPL v3.0
  summary: "This module can be used for checking whether a callable object's last call is returned, when calling it."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
