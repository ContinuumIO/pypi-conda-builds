package:
  name: django-data-exports
  version: "0.7"

source:
  fn: django-data-exports-0.7.tar.gz
  url: https://pypi.python.org/packages/source/d/django-data-exports/django-data-exports-0.7.tar.gz
  md5: 6df107dba1163efb0f8fe7c20ce77863
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-data-exports = django-data-exports:main
    #
    # Would create an entry point called django-data-exports that calls django-data-exports.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - django-inspect-model

  run:
    - python
    - django
    - django-inspect-model

test:
  # Python imports
  imports:
    - data_exports
    - data_exports.migrations
    - data_exports.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/magopian/django-data-exports
  license: BSD License
  summary: 'Model data exports for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
