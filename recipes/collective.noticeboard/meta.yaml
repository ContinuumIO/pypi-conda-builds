package:
  name: collective.noticeboard
  version: "0.7.1"

source:
  fn: collective.noticeboard-0.7.1.zip
  url: https://pypi.python.org/packages/source/c/collective.noticeboard/collective.noticeboard-0.7.1.zip
  md5: 44334fd7e45cf277309a23e1a43c3ad8
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.noticeboard = collective.noticeboard:main
    #
    # Would create an entry point called collective.noticeboard that calls collective.noticeboard.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone
    - collective.js.backbone
    - collective.js.underscore <1.5.0
    - collective.js.jqueryui

  run:
    - python
    - setuptools
    - setuptools
    - plone
    - collective.js.backbone
    - collective.js.underscore <1.5.0
    - collective.js.jqueryui

test:
  # Python imports
  imports:
    - collective
    - collective.noticeboard
    - collective.noticeboard.browser
    - collective.noticeboard.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/collective/collective.noticeboard
  license: GNU General Public License (GPL)
  summary: 'A fancy noticeboard for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
