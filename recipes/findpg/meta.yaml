package:
  name: findpg
  version: "2.3.post1"

source:
  fn: findpg-2.3.post1.tar.gz
  url: https://pypi.python.org/packages/source/f/findpg/findpg-2.3.post1.tar.gz
  md5: fceb11f1dc2f35e37ff61db5deb18fcb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - findpg = findpg:main
    #
    # Would create an entry point called findpg that calls findpg.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - findpg

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/cecton/findpg
  license: MIT License
  summary: 'Find the best suited version of PostgreSQL to restore a dump'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
