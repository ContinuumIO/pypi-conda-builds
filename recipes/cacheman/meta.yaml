package:
  name: cacheman
  version: "2.0.4"

source:
  fn: CacheMan-2.0.4.zip
  url: https://pypi.python.org/packages/source/C/CacheMan/CacheMan-2.0.4.zip
  md5: 91d606ceab6a500909067bd493412272
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cacheman = cacheman:main
    #
    # Would create an entry point called cacheman that calls cacheman.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - psutil ==2.1.0

  run:
    - python
    - psutil ==2.1.0

test:
  # Python imports
  imports:
    - cacheman

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/OpenGov/py_cache_manager
  license: BSD License
  summary: 'A dependent cache manager'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
