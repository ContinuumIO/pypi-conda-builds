package:
  name: eclaircie
  version: "0.2"

source:
  fn: eClaircie-0.2.tar.bz2
  url: https://pypi.python.org/packages/source/e/eClaircie/eClaircie-0.2.tar.bz2
  md5: ef373db6c878b53879e4d8294e149157
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - eclaircie = eclaircie:main
    #
    # Would create an entry point called eclaircie that calls eclaircie.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - eclaircie

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/jibalamy/eclaircie
  license: GNU General Public License v3 or later (GPLv3+)
  summary: '\xc3\xa9Claircie is a 100% static and cloud-less blog engine. It is based on the Sphinx documentation generator.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
