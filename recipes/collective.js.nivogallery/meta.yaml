package:
  name: collective.js.nivogallery
  version: "1.0"

source:
  fn: collective.js.nivogallery-1.0.zip
  url: https://pypi.python.org/packages/source/c/collective.js.nivogallery/collective.js.nivogallery-1.0.zip
  md5: 444df6fc11a2755cb53b46970d4cea7e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.js.nivogallery = collective.js.nivogallery:main
    #
    # Would create an entry point called collective.js.nivogallery that calls collective.js.nivogallery.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.js
    - collective.js.nivogallery
    - collective.js.nivogallery.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/espenmn/collective.js.nivogallery
  license: GPL
  summary: 'JQuery Nivogallery plugin as browser resource'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
