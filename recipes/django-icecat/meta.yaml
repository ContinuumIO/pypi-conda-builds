package:
  name: django-icecat
  version: "0.1.1"

source:
  fn: django-icecat-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-icecat/django-icecat-0.1.1.tar.gz
  md5: 94a34e6fd6d5d4e4c9f6c803c0944589
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-icecat = django-icecat:main
    #
    # Would create an entry point called django-icecat that calls django-icecat.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - icecat
    - icecat.management
    - icecat.management.commands
    - icecat.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/svdgraaf/django-icecat/
  license: GNU General Public License v3 (GPLv3)
  summary: 'Django models and tools for easy icecat importing'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
