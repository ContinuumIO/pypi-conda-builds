package:
  name: iplocation
  version: "0.1"

source:
  fn: iplocation-0.1.tar.gz
  url: https://pypi.python.org/packages/source/i/iplocation/iplocation-0.1.tar.gz
  md5: a91cc70fcc7d2497ea6bedd70c3d1797
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - iplocation = iplocation:main
    #
    # Would create an entry point called iplocation that calls iplocation.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - beautifulsoup4 >=4.0.5
    - outgoingip >=1.0

  run:
    - python
    - setuptools
    - beautifulsoup4 >=4.0.5
    - outgoingip >=1.0

test:
  # Python imports
  imports:
    - iplocation

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mrahma01/iplocation
  license: GNU General Public License (GPL)
  summary: 'Return city, country, location, business details of given/outgoing ip'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
