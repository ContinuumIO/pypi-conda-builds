package:
  name: confiture
  version: "2.0"

source:
  fn: confiture-2.0.tar.gz
  url: https://pypi.python.org/packages/source/c/confiture/confiture-2.0.tar.gz
  md5: 5b8fed73d08d51e9ae8e600418e5a821
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - confiture = confiture:main
    #
    # Would create an entry point called confiture that calls confiture.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - ply

  run:
    - python
    - ply

test:
  # Python imports
  imports:
    - confiture
    - confiture.schema
    - confiture.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://idevelop.org/p/confiture/
  license: MIT License
  summary: 'Advanced configuration parser for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
