package:
  name: django-password-session
  version: "0.3.3"

source:
  fn: django-password-session-0.3.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-password-session/django-password-session-0.3.3.tar.gz
  md5: 4c58971470f9d1492e74011c3ea91207
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-password-session = django-password-session:main
    #
    # Would create an entry point called django-password-session that calls django-password-session.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3,<1.7

  run:
    - python
    - django >=1.3,<1.7

test:
  # Python imports
  imports:
    - password_session

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/atugushev/django-password-session
  license: MIT License
  summary: 'A reusable Django app that will invalidate all active sessions after change password.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
