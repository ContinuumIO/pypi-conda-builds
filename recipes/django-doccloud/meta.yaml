package:
  name: django-doccloud
  version: "0.5.0"

source:
  fn: django-doccloud-0.5.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-doccloud/django-doccloud-0.5.0.tar.gz
  md5: 08479c8737392ffa97e9f1b2fc821351
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-doccloud = django-doccloud:main
    #
    # Would create an entry point called django-doccloud that calls django-doccloud.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python-documentcloud
    - django-extensions

  run:
    - python
    - python-documentcloud
    - django-extensions

test:
  # Python imports
  imports:
    - doccloud

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/BayCitizen/django-doccloud/
  license: Apache Software License
  summary: 'Provides a reusable document app to add and edit files hosted by DocumentCloud'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
