package:
  name: fez.djangoskel
  version: "0.3.1"

source:
  fn: fez.djangoskel-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/f/fez.djangoskel/fez.djangoskel-0.3.1.tar.gz
  md5: 66cb16785cc0bc2914a172a1d40baddb
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fez.djangoskel = fez.djangoskel:main
    #
    # Would create an entry point called fez.djangoskel that calls fez.djangoskel.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - pastescript >=1.3
    - cheetah

  run:
    - python
    - setuptools
    - setuptools
    - pastescript >=1.3
    - cheetah

test:
  # Python imports
  imports:
    - fez
    - fez.djangoskel

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.fezconsulting.com
  license: BSD License
  summary: 'Paster templates for creating Django applications as eggs'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
