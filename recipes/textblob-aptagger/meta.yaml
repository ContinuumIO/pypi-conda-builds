package:
  name: textblob-aptagger
  version: "0.2.0"

source:
  fn: textblob-aptagger-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/t/textblob-aptagger/textblob-aptagger-0.2.0.tar.gz
  md5: dc34226853bb62d0a11ca638abbc1251
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - textblob-aptagger = textblob-aptagger:main
    #
    # Would create an entry point called textblob-aptagger that calls textblob-aptagger.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - textblob >=0.8.0

  run:
    - python
    - textblob >=0.8.0

test:
  # Python imports
  imports:
    - textblob_aptagger

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/sloria/textblob-aptagger
  license: MIT License
  summary: 'A fast and accurate part-of-speech tagger for TextBlob.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
