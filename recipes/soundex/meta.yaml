package:
  name: soundex
  version: "1.1.3"

source:
  fn: soundex-1.1.3.tar.gz
  url: https://pypi.python.org/packages/source/s/soundex/soundex-1.1.3.tar.gz
  md5: 15e73ce793c27ed51fc6bcc7cd2aada2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - soundex = soundex:main
    #
    # Would create an entry point called soundex that calls soundex.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - soundex

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: GNU Lesser General Public License v2 or later (LGPLv2+)
  summary: 'Soundex algorith implementation for English and Indian languages'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
