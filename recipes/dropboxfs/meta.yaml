package:
  name: dropboxfs
  version: "0.1-11"

source:
  fn: dropboxfs-0.1-11.tar.gz
  url: https://pypi.python.org/packages/source/d/dropboxfs/dropboxfs-0.1-11.tar.gz
  md5: 587edb35d31e78fe9d47b5829e0e23ea
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dropboxfs = dropboxfs:main
    #
    # Would create an entry point called dropboxfs that calls dropboxfs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - dropboxfs

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/btimby/fs-dropbox/
  license: GPLv3
  summary: 'A pyFilesystem backend for the Dropbox API.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
