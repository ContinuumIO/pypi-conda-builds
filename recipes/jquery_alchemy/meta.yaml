package:
  name: jquery_alchemy
  version: "0.0.7"

source:
  fn: jquery_alchemy-0.0.7.tar.gz
  url: https://pypi.python.org/packages/source/j/jquery_alchemy/jquery_alchemy-0.0.7.tar.gz
  md5: 76bbf1c99722eac06f7452ab7cbd9534
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jquery_alchemy = jquery_alchemy:main
    #
    # Would create an entry point called jquery_alchemy that calls jquery_alchemy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sqlalchemy
    - sqlalchemy_utils

  run:
    - python
    - sqlalchemy
    - sqlalchemy_utils

test:
  # Python imports
  imports:
    - jquery_alchemy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.github.com/mrichar1/jquery_alchemy
  license: GNU Affero General Public License v3 or later (AGPLv3+)
  summary: 'A package which creates jquery_validation rules from sqlalchemy columns for use with web forms.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
