package:
  name: hutools
  version: "0.63"

source:
  fn: huTools-0.63.tar.gz
  url: https://pypi.python.org/packages/source/h/huTools/huTools-0.63.tar.gz
  md5: 9d18a52adceffe0dc9d638d60f2fa555
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hutools = hutools:main
    #
    # Would create an entry point called hutools that calls hutools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - decorator

  run:
    - python
    - setuptools
    - setuptools
    - decorator

test:
  # Python imports
  imports:
    - huTools
    - huTools.calendar
    - huTools.http
    - huTools.http._httplib2
    - hutools

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://hudora.github.com/huTools/
  license: BSD
  summary: 'Various tiny tools and toys to make Python coding less work more fun.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
