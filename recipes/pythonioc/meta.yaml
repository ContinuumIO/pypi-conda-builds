package:
  name: pythonioc
  version: "0.3.1"

source:
  fn: pythonioc-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/p/pythonioc/pythonioc-0.3.1.tar.gz
  md5: e3768f99dc28bcb0d9c93d823d52562e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pythonioc = pythonioc:main
    #
    # Would create an entry point called pythonioc that calls pythonioc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - pythonioc

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/eh14/python-ioc
  license: MIT
  summary: 'simplistic dependency injection container for python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
