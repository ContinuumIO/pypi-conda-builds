package:
  name: sortest
  version: "0.0.10"

source:
  fn: sortest-0.0.10.tar.gz
  url: https://pypi.python.org/packages/source/s/sortest/sortest-0.0.10.tar.gz
  md5: 6b97f28c7e27672020fc4f645775e316
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sortest = sortest:main
    #
    # Would create an entry point called sortest that calls sortest.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - nose

  run:
    - python
    - nose

test:
  # Python imports
  imports:
    - sortest

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/eigenhombre/sortest
  license: EPL
  summary: 'Continuous testing in Python with sorting by test speed and auto-restart when files change'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
