package:
  name: confluent_common
  version: "1.0"

source:
  fn: confluent_common-1.0.tar.gz
  url: https://pypi.python.org/packages/source/c/confluent_common/confluent_common-1.0.tar.gz
  md5: aee5cdde8c9f5ab0d5f1f0833e37f2ef
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - confluent_common = confluent_common:main
    #
    # Would create an entry point called confluent_common that calls confluent_common.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - confluent
    - confluent_common

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://xcat.sf.net/
  license: UNKNOWN
  summary: 'common content for confluent client and server'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
