package:
  name: django-easy-contact-setup
  version: "0.3.8"

source:
  fn: django-easy-contact-setup-0.3.8.tar.gz
  url: https://pypi.python.org/packages/source/d/django-easy-contact-setup/django-easy-contact-setup-0.3.8.tar.gz
  md5: 9f34aede8ec9a5ceec15339628ebb5bd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-easy-contact-setup = django-easy-contact-setup:main
    #
    # Would create an entry point called django-easy-contact-setup that calls django-easy-contact-setup.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-easy-contact
    - django ==1.3.7
    - django-fields

  run:
    - python
    - django-easy-contact
    - django ==1.3.7
    - django-fields

test:
  # Python imports
  imports:
    - easy_contact_setup

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/catapela/django-easy-contact-setup
  license: BSD License
  summary: 'Admin set up for django-easy-contact'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
