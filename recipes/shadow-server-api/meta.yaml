package:
  name: shadow-server-api
  version: "1.0.4"

source:
  fn: shadow-server-api-1.0.4.tar.gz
  url: https://pypi.python.org/packages/source/s/shadow-server-api/shadow-server-api-1.0.4.tar.gz
  md5: 030be2fd88d08d1d4a757c80fb299b09
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - shadow-server-api = shadow-server-api:main
    #
    # Would create an entry point called shadow-server-api that calls shadow-server-api.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=2.2.1

  run:
    - python
    - requests >=2.2.1

test:
  # Python imports
  imports:
    - shadow_server_api

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/blacktop/team-cymru-api
  license: GPLv3
  summary: 'Shadow Server - Binary Whitelist and MD5/SHA1 AV Service API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
