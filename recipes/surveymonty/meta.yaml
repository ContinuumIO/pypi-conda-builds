package:
  name: surveymonty
  version: "0.1.17"

source:
  fn: surveymonty-0.1.17.tar.gz
  url: https://pypi.python.org/packages/source/s/surveymonty/surveymonty-0.1.17.tar.gz
  md5: 2262022b99f240eb608710c8a9d49351
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - surveymonty = surveymonty:main
    #
    # Would create an entry point called surveymonty that calls surveymonty.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - surveymonty

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/andrewkshim/SurveyMonty
  license: MIT
  summary: 'Python wrapper for SurveyMonkey API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
