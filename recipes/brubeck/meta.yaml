package:
  name: brubeck
  version: "0.4.0"

source:
  fn: brubeck-0.4.0.tar.gz
  url: https://pypi.python.org/packages/source/b/brubeck/brubeck-0.4.0.tar.gz
  md5: 2f1ed13c4392ee20f1ca10e56b900d19
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - brubeck = brubeck:main
    #
    # Would create an entry point called brubeck that calls brubeck.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - ujson
    - dictshield

  run:
    - python
    - ujson
    - dictshield

test:
  # Python imports
  imports:
    - brubeck

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/j2labs/brubeck
  license: UNKNOWN
  summary: 'Python Library for building Mongrel2 / ZeroMQ message handlers'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
