package:
  name: ckanext-geoview
  version: "0.0.6"

source:
  fn: ckanext-geoview-0.0.6.tar.gz
  url: https://pypi.python.org/packages/source/c/ckanext-geoview/ckanext-geoview-0.0.6.tar.gz
  md5: 073a369d73d98e63baf6b98d08944a1a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ckanext-geoview = ckanext-geoview:main
    #
    # Would create an entry point called ckanext-geoview that calls ckanext-geoview.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - ckanext
    - ckanext.geoview
    - ckanext.geoview.controllers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/ckan/ckanext-geoview
  license: MIT
  summary: 'CKAN Geospatial ResourceView'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
