package:
  name: django-database-email-backend
  version: "0.2.1"

source:
  fn: django-database-email-backend-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-database-email-backend/django-database-email-backend-0.2.1.tar.gz
  md5: 92162c940beb2df930ae2b7f3609ab3b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-database-email-backend = django-database-email-backend:main
    #
    # Would create an entry point called django-database-email-backend that calls django-database-email-backend.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - database_email_backend
    - database_email_backend.migrations
    - database_email_backend.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/stefanfoulis/django-database-email-backend
  license: BSD License
  summary: 'A django EmailBackend for debugging that saves Emails in the database instead of delivering them.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
