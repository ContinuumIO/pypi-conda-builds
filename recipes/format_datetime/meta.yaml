package:
  name: format_datetime
  version: "0.6dev"

source:
  fn: format_datetime-0.6dev.tar.gz
  url: https://pypi.python.org/packages/source/f/format_datetime/format_datetime-0.6dev.tar.gz
  md5: c836d6a5652ecbe349ac9c3465071644
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - format_datetime = format_datetime:main
    #
    # Would create an entry point called format_datetime that calls format_datetime.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - format_datetime

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/niels_mfo/format_datetime
  license: MIT
  summary: 'Helpers to properly print date and time (strftime replacement).'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
