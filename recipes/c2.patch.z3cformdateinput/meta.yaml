package:
  name: c2.patch.z3cformdateinput
  version: "1.0a1"

source:
  fn: c2.patch.z3cformdateinput-1.0a1.tar.gz
  url: https://pypi.python.org/packages/source/c/c2.patch.z3cformdateinput/c2.patch.z3cformdateinput-1.0a1.tar.gz
  md5: 02b3e010283ddb2da7f5213e4e0e160d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - c2.patch.z3cformdateinput = c2.patch.z3cformdateinput:main
    #
    # Would create an entry point called c2.patch.z3cformdateinput that calls c2.patch.z3cformdateinput.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.z3cform.datetimewidget

  run:
    - python
    - setuptools
    - setuptools
    - collective.z3cform.datetimewidget

test:
  # Python imports
  imports:
    - c2
    - c2.patch
    - c2.patch.z3cformdateinput

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://wwww.cmscom.jp
  license: GPL
  summary: 'This product is monkey patch for z3cform date input box for Japanese style'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
