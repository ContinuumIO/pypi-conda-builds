package:
  name: nestertanayamitshah
  version: "1.0.0"

source:
  fn: nestertanayamitshah-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/n/nestertanayamitshah/nestertanayamitshah-1.0.0.tar.gz
  md5: c0fea3e5ffb317a66779e690e1b6364e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - nestertanayamitshah = nestertanayamitshah:main
    #
    # Would create an entry point called nestertanayamitshah that calls nestertanayamitshah.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - nestertanayamitshah

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: www.football365.com
  license: UNKNOWN
  summary: 'test'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
