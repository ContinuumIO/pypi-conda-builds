package:
  name: django-email-change
  version: "0.2.2"

source:
  fn: django-email-change-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-email-change/django-email-change-0.2.2.tar.gz
  md5: e601c85063865cd934fc4f3fb1ec9de4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-email-change = django-email-change:main
    #
    # Would create an entry point called django-email-change that calls django-email-change.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - email_change

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.codetrax.org/projects/django-email-change
  license: Apache Software License
  summary: 'django-email-change adds support for email address change and confirmation.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
