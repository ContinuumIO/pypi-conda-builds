package:
  name: image_manager
  version: "0.0.2"

source:
  fn: image_manager-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/i/image_manager/image_manager-0.0.2.tar.gz
  md5: 357f4d45a5940a0679aaf908c8e80e7b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - image_manager = image_manager:main
    #
    # Would create an entry point called image_manager that calls image_manager.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - image_manager

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/albertoconnor/django-builtin-password-reset
  license: MIT License
  summary: 'django-image-manager is a no none sense image hanlding system.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
