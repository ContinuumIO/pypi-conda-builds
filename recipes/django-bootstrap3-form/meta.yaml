package:
  name: django-bootstrap3-form
  version: "0.2.0"

source:
  fn: django-bootstrap3-form-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-bootstrap3-form/django-bootstrap3-form-0.2.0.tar.gz
  md5: f5770a44ceba0c4b2392370b3100168f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-bootstrap3-form = django-bootstrap3-form:main
    #
    # Would create an entry point called django-bootstrap3-form that calls django-bootstrap3-form.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - django_pony_forms
    - six

  run:
    - python
    - django
    - django_pony_forms
    - six

test:
  # Python imports
  imports:
    - django_bootstrap3_form

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mbraak/django-bootstrap3-form
  license: Apache Software License
  summary: 'Django-bootstrap3-form allows you write Django forms that work with Twitter Bootstrap 3'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
