package:
  name: flask-themes
  version: "0.1.3"

source:
  fn: Flask-Themes-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Themes/Flask-Themes-0.1.3.tar.gz
  md5: 2ed4ce4c6f3c94941f77838793938e1b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-themes = flask-themes:main
    #
    # Would create an entry point called flask-themes that calls flask-themes.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.6

  run:
    - python
    - flask >=0.6

test:
  # Python imports
  imports:
    - flaskext

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/leafstorm/flask-themes/
  license: MIT License
  summary: 'Provides infrastructure for theming Flask applications'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
