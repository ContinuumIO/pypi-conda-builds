package:
  name: collective.diggdigg
  version: "1.0"

source:
  fn: collective.diggdigg-1.0.zip
  url: https://pypi.python.org/packages/source/c/collective.diggdigg/collective.diggdigg-1.0.zip
  md5: 38ab6c22a378914bf49c5a5dc29f9248
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.diggdigg = collective.diggdigg:main
    #
    # Would create an entry point called collective.diggdigg that calls collective.diggdigg.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.diggdigg
    - collective.diggdigg.buttons
    - collective.diggdigg.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/toutpt/collective.diggdigg
  license: GPL
  summary: 'DiggDigg for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
