package:
  name: git-sweep3k
  version: "0.3.1"

source:
  fn: git-sweep3k-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/g/git-sweep3k/git-sweep3k-0.3.1.tar.gz
  md5: eab94a182d41461219ac403c4dfc1448
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - git-sweep3k = git-sweep3k:main
    #
    # Would create an entry point called git-sweep3k that calls git-sweep3k.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/myint/git-sweep
  license: MIT License
  summary: 'Clean up branches from your Git remotes'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
