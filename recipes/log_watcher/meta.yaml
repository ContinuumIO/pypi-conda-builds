package:
  name: log_watcher
  version: "0.1.2"

source:
  fn: log_watcher-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/l/log_watcher/log_watcher-0.1.2.tar.gz
  md5: 7a9b647e8ea1cdb707f3fdb82d748bb5
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - log_watcher = log_watcher:main
    #
    # Would create an entry point called log_watcher that calls log_watcher.main()

    - log_watcher=log_watcher.cli:main

  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - click ==2.4
    - boto ==2.31.1
    - raven ==4.0.2

  run:
    - python
    - setuptools
    - click ==2.4
    - boto ==2.31.1
    - raven ==4.0.2

test:
  # Python imports
  imports:
    - log_watcher

  commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.

    - log_watcher --help

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
