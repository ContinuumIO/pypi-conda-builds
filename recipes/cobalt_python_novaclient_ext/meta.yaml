package:
  name: cobalt_python_novaclient_ext
  version: "1.3.107"

source:
  fn: cobalt_python_novaclient_ext-1.3.107.tar.gz
  url: https://pypi.python.org/packages/source/c/cobalt_python_novaclient_ext/cobalt_python_novaclient_ext-1.3.107.tar.gz
  md5: d6924ddc923d676ed1fabb39fe107c74
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cobalt_python_novaclient_ext = cobalt_python_novaclient_ext:main
    #
    # Would create an entry point called cobalt_python_novaclient_ext that calls cobalt_python_novaclient_ext.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python-novaclient

  run:
    - python
    - python-novaclient

test:
  # Python imports
  imports:
    - cobalt_python_novaclient_ext

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.gridcentric.com/
  license: Apache Software License
  summary: 'Cobalt extension for OS novaclient.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
