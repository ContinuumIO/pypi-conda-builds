package:
  name: xstatic-font-awesome
  version: "4.3.0.0"

source:
  fn: XStatic-Font-Awesome-4.3.0.0.tar.gz
  url: https://pypi.python.org/packages/source/X/XStatic-Font-Awesome/XStatic-Font-Awesome-4.3.0.0.tar.gz
  md5: 56e637fe7b3dcdd2ebd683996ad60759
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - xstatic-font-awesome = xstatic-font-awesome:main
    #
    # Would create an entry point called xstatic-font-awesome that calls xstatic-font-awesome.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - xstatic
    - xstatic.pkg
    - xstatic.pkg.font_awesome

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://fortawesome.github.io/Font-Awesome/
  license: (same as Font-Awesome)
  summary: 'Font-Awesome 4.3.0 (XStatic packaging standard)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
