package:
  name: datanator_processor
  version: "0.0.01"

source:
  fn: datanator_processor-0.0.01.tar.gz
  url: https://pypi.python.org/packages/source/d/datanator_processor/datanator_processor-0.0.01.tar.gz
  md5: 4dba3816b32f384a5248dc77a910d56e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - datanator_processor = datanator_processor:main
    #
    # Would create an entry point called datanator_processor that calls datanator_processor.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - datanator_processor

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/ctmoses/datanator_processing
  license: Proprietary
  summary: 'Data processor and ontology filler for ADR.\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
