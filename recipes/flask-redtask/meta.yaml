package:
  name: flask-redtask
  version: "0.3.1"

source:
  fn: Flask-Redtask-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Redtask/Flask-Redtask-0.3.1.tar.gz
  md5: 43947b0933c394d2d7ec7c9dbe1ad5de
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-redtask = flask-redtask:main
    #
    # Would create an entry point called flask-redtask that calls flask-redtask.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - python-memcached
    - itsdangerous

  run:
    - python
    - flask
    - python-memcached
    - itsdangerous

test:
  # Python imports
  imports:
    - flaskext

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/muromec/flask-redtask
  license: BSD License
  summary: 'Redqueue integration for flask'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
