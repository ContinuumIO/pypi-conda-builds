package:
  name: redfox
  version: "1.2"

source:
  fn: redfox-1.2.tar.gz
  url: https://pypi.python.org/packages/source/r/redfox/redfox-1.2.tar.gz
  md5: 9b8955c53134ce91dc6721230901bc39
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - redfox = redfox:main
    #
    # Would create an entry point called redfox that calls redfox.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - werkzeug

  run:
    - python
    - setuptools
    - werkzeug

test:
  # Python imports
  imports:
    - redfox

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/python-redfox/
  license: MIT License
  summary: 'WSGI request routing for Python objects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
