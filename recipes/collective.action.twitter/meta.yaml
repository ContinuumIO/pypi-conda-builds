package:
  name: collective.action.twitter
  version: "0.2"

source:
  fn: collective.action.twitter-0.2.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.action.twitter/collective.action.twitter-0.2.tar.gz
  md5: ca6427eab7e9e784c9a2d1b2b7cbceda
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.action.twitter = collective.action.twitter:main
    #
    # Would create an entry point called collective.action.twitter that calls collective.action.twitter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - python-twitter

  run:
    - python
    - setuptools
    - setuptools
    - python-twitter

test:
  # Python imports
  imports:
    - collective
    - collective.action
    - collective.action.twitter

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.catworkx.de
  license: GPL
  summary: 'A contentrule action to publish something on twitter.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
