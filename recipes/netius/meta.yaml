package:
  name: netius
  version: "1.5.5"

source:
  fn: netius-1.5.5.zip
  url: https://pypi.python.org/packages/source/n/netius/netius-1.5.5.zip
  md5: 7c20e20fd549e863c655d12d84c487a7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - netius = netius:main
    #
    # Would create an entry point called netius that calls netius.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - netius
    - netius.adapters
    - netius.auth
    - netius.base
    - netius.clients
    - netius.common
    - netius.examples
    - netius.extra
    - netius.pool
    - netius.servers
    - netius.sh
    - netius.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://netius.hive.pt
  license: Apache Software License
  summary: 'Netius System'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
