package:
  name: django-sanction
  version: "0.3.1"

source:
  fn: django-sanction-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-sanction/django-sanction-0.3.1.tar.gz
  md5: e895719ba8a815b22a28fb6bf6cdac2c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-sanction = django-sanction:main
    #
    # Would create an entry point called django-sanction that calls django-sanction.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - sanction

  run:
    - python
    - django
    - sanction

test:
  # Python imports
  imports:
    - django_sanction

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/demianbrecht/django-sanction
  license: MIT License
  summary: 'A Django front end for the sanction OAuth2 client library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
