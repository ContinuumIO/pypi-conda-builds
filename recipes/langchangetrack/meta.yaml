package:
  name: langchangetrack
  version: "0.1.0"

source:
  fn: langchangetrack-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/l/langchangetrack/langchangetrack-0.1.0.tar.gz
  md5: ead05c88e4da937c8c70cfa2c5b7211c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - langchangetrack = langchangetrack:main
    #
    # Would create an entry point called langchangetrack that calls langchangetrack.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - wheel ==0.23.0
    - argparse >=1.2.1
    - numpy >=0.9.1
    - scipy >=0.15.1
    - more_itertools >=2.2
    - joblib >=0.8.3-r1
    - gensim ==0.10.3
    - statsmodels >=0.5.0
    - changepoint >=0.1.0
    - nltk >=3.0.0
    - textblob >=0.9.0
    - textblob-aptagger >=0.2.0

  run:
    - python
    - wheel ==0.23.0
    - argparse >=1.2.1
    - numpy >=0.9.1
    - scipy >=0.15.1
    - more_itertools >=2.2
    - joblib >=0.8.3-r1
    - gensim ==0.10.3
    - statsmodels >=0.5.0
    - changepoint >=0.1.0
    - nltk >=3.0.0
    - textblob >=0.9.0
    - textblob-aptagger >=0.2.0

test:
  # Python imports
  imports:
    - langchangetrack
    - langchangetrack.corpusreaders
    - langchangetrack.tsconstruction
    - langchangetrack.tsconstruction.distributional
    - langchangetrack.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/viveksck/langchangetrack
  license: BSD License
  summary: 'Package for statistically significant language change.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
