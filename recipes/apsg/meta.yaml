package:
  name: apsg
  version: "0.2.2"

source:
  fn: apsg-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/a/apsg/apsg-0.2.2.tar.gz
  md5: 98323cd5bf0b5c40e32136815e41ab46
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - apsg = apsg:main
    #
    # Would create an entry point called apsg that calls apsg.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy >=1.8
    - matplotlib >=1.2
    - scipy

  run:
    - python
    - numpy >=1.8
    - matplotlib >=1.2
    - scipy

test:
  # Python imports
  imports:
    - apsg

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://ondrolexa.github.io/apsg
  license: BSD License
  summary: 'APSG - structural geology module for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
