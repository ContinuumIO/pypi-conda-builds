package:
  name: atomisator.enhancers
  version: "0.1.5"

source:
  fn: atomisator.enhancers-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/a/atomisator.enhancers/atomisator.enhancers-0.1.5.tar.gz
  md5: eaefa49bedd7f5439771eaef3c9300a1
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - atomisator.enhancers = atomisator.enhancers:main
    #
    # Would create an entry point called atomisator.enhancers that calls atomisator.enhancers.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - beautifulsoup
    - simplejson

  run:
    - python
    - setuptools
    - setuptools
    - beautifulsoup
    - simplejson

test:
  # Python imports
  imports:
    - atomisator
    - atomisator.enhancers
    - atomisator.enhancers.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://atomisator.ziade.org
  license: GPL
  summary: 'Expert Python Programming - Enhancers'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
