package:
  name: django-versioned-static
  version: "1.4.3"

source:
  fn: django-versioned-static-1.4.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-versioned-static/django-versioned-static-1.4.3.tar.gz
  md5: 649698ff9263717fb1254a01ed5b2fe5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-versioned-static = django-versioned-static:main
    #
    # Would create an entry point called django-versioned-static that calls django-versioned-static.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools >=0.6b1
    - django
    - yuicompressor

  run:
    - python
    - setuptools >=0.6b1
    - django
    - yuicompressor

test:
  # Python imports
  imports:
    - versioned_static
    - versioned_static.management
    - versioned_static.management.commands
    - versioned_static.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/xaralis/django-versioned-static
  license: BSD License
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
