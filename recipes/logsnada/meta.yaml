package:
  name: logsnada
  version: "0.2.7.4"

source:
  fn: logsnada-0.2.7.4.tar.gz
  url: https://pypi.python.org/packages/source/l/logsnada/logsnada-0.2.7.4.tar.gz
  md5: 3dedf7d2f849fb7c1026cb7ffad19333
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - logsnada = logsnada:main
    #
    # Would create an entry point called logsnada that calls logsnada.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - logsnada

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/RiverMeadow/logsnada
  license: UNKNOWN
  summary: 'Mostly about pattern-based definition of logger names'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
