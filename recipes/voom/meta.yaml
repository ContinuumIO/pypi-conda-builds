package:
  name: voom
  version: "1.1.7"

source:
  fn: voom-1.1.7.tar.gz
  url: https://pypi.python.org/packages/source/v/voom/voom-1.1.7.tar.gz
  md5: 25f827c745fe6669007da1b8fa5f2b5b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - voom = voom:main
    #
    # Would create an entry point called voom that calls voom.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - examples
    - voom
    - voom.adaptors
    - voom.adaptors.django
    - voom.amqp
    - voom.codecs
    - voom.codecs.vnd
    - voom.events
    - voom.gateway
    - voom.imap
    - voom.local

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ninowalker/voom
  license: BSD License
  summary: 'A python message bus that you can put 4 million volts through.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
