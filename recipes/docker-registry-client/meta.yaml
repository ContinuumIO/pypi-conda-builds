package:
  name: docker-registry-client
  version: "0.4"

source:
  fn: docker_registry_client-0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/docker-registry-client/docker_registry_client-0.4.tar.gz
  md5: 591e7baf610acfed70960354a502e0b3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - docker-registry-client = docker-registry-client:main
    #
    # Would create an entry point called docker-registry-client that calls docker-registry-client.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=2.4.3
    - mock >=1.0.1

  run:
    - python
    - requests >=2.4.3
    - mock >=1.0.1

test:
  # Python imports
  imports:
    - docker_registry_client

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Apache Software License
  summary: 'Client for Docker Registry V1'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
