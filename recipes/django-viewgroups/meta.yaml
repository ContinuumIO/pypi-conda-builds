package:
  name: django-viewgroups
  version: "0.1.20"

source:
  fn: django-viewgroups-0.1.20.tar.gz
  url: https://pypi.python.org/packages/source/d/django-viewgroups/django-viewgroups-0.1.20.tar.gz
  md5: d1207b0506b179be6e72e098a307f1e7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-viewgroups = django-viewgroups:main
    #
    # Would create an entry point called django-viewgroups that calls django-viewgroups.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six

  run:
    - python
    - six

test:
  # Python imports
  imports:
    - viewsets
    - viewsets.management
    - viewsets.management.commands
    - viewsets.mixins
    - viewsets.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/freakypie/django-viewsets
  license: UNKNOWN
  summary: 'Django admin like groups of CBVs'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
