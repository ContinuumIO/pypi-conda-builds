package:
  name: importd
  version: "0.3.3"

source:
  fn: importd-0.3.3.tar.gz
  url: https://pypi.python.org/packages/source/i/importd/importd-0.3.3.tar.gz
  md5: 143049dfab1f6e79d854b0ac38e4015d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - importd = importd:main
    #
    # Would create an entry point called importd that calls importd.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fhurl >=0.1.7
    - smarturls
    - six
    - django >=1.3
    - dj-database-url

  run:
    - python
    - fhurl >=0.1.7
    - smarturls
    - six
    - django >=1.3
    - dj-database-url

test:
  # Python imports
  imports:
    - importd

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/amitu/importd
  license: BSD
  summary: 'a django based miniframework, inspired by sinatra'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
