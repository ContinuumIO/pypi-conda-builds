package:
  name: acautomation
  version: "0.2"

source:
  fn: ACAutomation-0.2.tar.gz
  url: https://pypi.python.org/packages/source/A/ACAutomation/ACAutomation-0.2.tar.gz
  md5: b97359b3e127e4707924d6a2b1a14f22
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - acautomation = acautomation:main
    #
    # Would create an entry point called acautomation that calls acautomation.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - ACAutomation
    - acautomation

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.coom/dingyaguang117
  license: UNKNOWN
  summary: 'ACAutomation python wrapper,support unicode'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
