package:
  name: gaesynkit
  version: "1.0.0a2"

source:
  fn: gaesynkit-1.0.0a2.tar.gz
  url: https://pypi.python.org/packages/source/g/gaesynkit/gaesynkit-1.0.0a2.tar.gz
  md5: 7c0e124b8f9168e78ccfbd014be3232f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gaesynkit = gaesynkit:main
    #
    # Would create an entry point called gaesynkit that calls gaesynkit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - gaesynkit

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/gaesynkit
  license: Apache Software License
  summary: 'Google App Engine Datastore/Local Storage Synchronization Framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
