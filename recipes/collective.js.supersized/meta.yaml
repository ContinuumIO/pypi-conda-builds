package:
  name: collective.js.supersized
  version: "0.4.4"

source:
  fn: collective.js.supersized-0.4.4.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.js.supersized/collective.js.supersized-0.4.4.tar.gz
  md5: 4842e9ad8477146063731838b9e2d109
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.js.supersized = collective.js.supersized:main
    #
    # Would create an entry point called collective.js.supersized that calls collective.js.supersized.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - medialog.controlpanel
    - plone.api
    - plone.directives.form

  run:
    - python
    - setuptools
    - medialog.controlpanel
    - plone.api
    - plone.directives.form

test:
  # Python imports
  imports:
    - collective
    - collective.js
    - collective.js.supersized
    - collective.js.supersized.browser
    - collective.js.supersized.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/collective/collective.js.supersized
  license: GPL
  summary: 'JQuery Supersized for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
