package:
  name: c2.sample.csvworkflow
  version: "1.1"

source:
  fn: c2.sample.csvworkflow-1.1.tar.gz
  url: https://pypi.python.org/packages/source/c/c2.sample.csvworkflow/c2.sample.csvworkflow-1.1.tar.gz
  md5: 41c225c97dd6b8ba305020843b532f08
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - c2.sample.csvworkflow = c2.sample.csvworkflow:main
    #
    # Would create an entry point called c2.sample.csvworkflow that calls c2.sample.csvworkflow.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.wtf

  run:
    - python
    - setuptools
    - setuptools
    - collective.wtf

test:
  # Python imports
  imports:
    - c2
    - c2.sample
    - c2.sample.csvworkflow
    - c2.sample.csvworkflow.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/
  license: GPL
  summary: 'c2.sample.csvworkflow is sample application using by collective.wtf.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
