package:
  name: voluptuous
  version: "0.8.7"

source:
  fn: voluptuous-0.8.7.tar.gz
  url: https://pypi.python.org/packages/source/v/voluptuous/voluptuous-0.8.7.tar.gz
  md5: 0f0949147b439590b3289e391ce5330b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - voluptuous = voluptuous:main
    #
    # Would create an entry point called voluptuous that calls voluptuous.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools >=0.6b1

  run:
    - python
    - setuptools >=0.6b1

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/alecthomas/voluptuous
  license: BSD License
  summary: '# Voluptuous is a Python data validation library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
