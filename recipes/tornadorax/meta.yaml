package:
  name: tornadorax
  version: "0.0.7"

source:
  fn: tornadorax-0.0.7.tar.gz
  url: https://pypi.python.org/packages/source/t/tornadorax/tornadorax-0.0.7.tar.gz
  md5: 213971df9b7370dad9bd7510c3d3036c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tornadorax = tornadorax:main
    #
    # Would create an entry point called tornadorax that calls tornadorax.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - tornado
    - python-dateutil

  run:
    - python
    - tornado
    - python-dateutil

test:
  # Python imports
  imports:
    - tornadorax
    - tornadorax.services

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/joshmarshall/tornadorax/
  license: http://www.apache.org/licenses/LICENSE-2.0
  summary: 'Tornado wrapper for OpenStack services'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
