package:
  name: collective.alerts
  version: "1.2.3"

source:
  fn: collective.alerts-1.2.3.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.alerts/collective.alerts-1.2.3.tar.gz
  md5: c30d6928acda8a6633ead58910988619
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.alerts = collective.alerts:main
    #
    # Would create an entry point called collective.alerts that calls collective.alerts.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.js.jqueryui

  run:
    - python
    - setuptools
    - setuptools
    - collective.js.jqueryui

test:
  # Python imports
  imports:
    - collective
    - collective.alerts
    - collective.alerts.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/collective.alerts
  license: GPL
  summary: "Implements Cory LaViska's JQuery Alerts feature for Plone"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
