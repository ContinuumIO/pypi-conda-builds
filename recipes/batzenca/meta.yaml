package:
  name: batzenca
  version: "0.1"

source:
  fn: BatzenCA-0.1.tar.gz
  url: https://pypi.python.org/packages/source/B/BatzenCA/BatzenCA-0.1.tar.gz
  md5: 9f2d09ad50e47ad83b810e88abbce0be
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - batzenca = batzenca:main
    #
    # Would create an entry point called batzenca that calls batzenca.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - gitpython ==0.3.2.RC1
    - sqlalchemy
    - ipython
    - pyme ==0.9.0

  run:
    - python
    - gitpython ==0.3.2.RC1
    - sqlalchemy
    - ipython
    - pyme ==0.9.0

test:
  # Python imports
  imports:
    - batzenca
    - batzenca/database

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/malb/batzenca
  license: Simplified BSD License
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
