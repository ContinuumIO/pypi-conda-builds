package:
  name: api_toolkit
  version: "0.3.11"

source:
  fn: api_toolkit-0.3.11.tar.gz
  url: https://pypi.python.org/packages/source/a/api_toolkit/api_toolkit-0.3.11.tar.gz
  md5: 9daa299a10dfbc68a7171fb9ef2cf520
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - api_toolkit = api_toolkit:main
    #
    # Would create an entry point called api_toolkit that calls api_toolkit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=1.2.3
    - vcrpy >=1.1.0

  run:
    - python
    - requests >=1.2.3
    - vcrpy >=1.1.0

test:
  # Python imports
  imports:
    - api_toolkit

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/myfreecomm/python-api-toolkit
  license: Apache Software License
  summary: 'A library which simplifies the creation of clients for REST webservices.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
