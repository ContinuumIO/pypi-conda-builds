package:
  name: redis_wrap
  version: "1.4.4"

source:
  fn: redis_wrap-1.4.4.tar.gz
  url: https://pypi.python.org/packages/source/r/redis_wrap/redis_wrap-1.4.4.tar.gz
  md5: 525a1083f236a6f5ce4fa5b7ac0a868b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - redis_wrap = redis_wrap:main
    #
    # Would create an entry point called redis_wrap that calls redis_wrap.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - redis_wrap

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.amix.dk/
  license: BSD License
  summary: 'Implements a wrapper for Redis datatypes so they mimic the datatypes found in Python.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
