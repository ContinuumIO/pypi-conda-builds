package:
  name: django-littlebro
  version: "0.2.1"

source:
  fn: django-littlebro-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-littlebro/django-littlebro-0.2.1.tar.gz
  md5: 707707587879ac099a977ab761442737
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-littlebro = django-littlebro:main
    #
    # Would create an entry point called django-littlebro that calls django-littlebro.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - importlib
    - anyjson
    - carrot >=0.6.0
    - celery >=0.8.0
    - pymongo
    - django-celery
    - django-kombu

  run:
    - python
    - importlib
    - anyjson
    - carrot >=0.6.0
    - celery >=0.8.0
    - pymongo
    - django-celery
    - django-kombu

test:
  # Python imports
  imports:
    - littlebro
    - littlebro.backends
    - littlebro.conf
    - littlebro.trackers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/cirlabs
  license: BSD
  summary: 'Django LittleBro: Asynchronous event tracking using MongoDB and Celery.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
