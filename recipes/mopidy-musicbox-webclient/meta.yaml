package:
  name: mopidy-musicbox-webclient
  version: "2.0.0"

source:
  fn: Mopidy-MusicBox-Webclient-2.0.0.tar.gz
  url: https://pypi.python.org/packages/source/M/Mopidy-MusicBox-Webclient/Mopidy-MusicBox-Webclient-2.0.0.tar.gz
  md5: 93182edcc018ef7811b7bb74fd72f436
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mopidy-musicbox-webclient = mopidy-musicbox-webclient:main
    #
    # Would create an entry point called mopidy-musicbox-webclient that calls mopidy-musicbox-webclient.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - mopidy >=0.19

  run:
    - python
    - setuptools
    - setuptools
    - mopidy >=0.19

test:
  # Python imports
  imports:
    - mopidy_musicbox_webclient

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/woutervanwijk/mopidy-musicbox-webclient
  license: Apache Software License
  summary: u'Mopidy MusicBox web extension'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
