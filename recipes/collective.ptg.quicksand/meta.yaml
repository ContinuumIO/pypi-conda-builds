package:
  name: collective.ptg.quicksand
  version: "0.2.1"

source:
  fn: collective.ptg.quicksand-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.ptg.quicksand/collective.ptg.quicksand-0.2.1.tar.gz
  md5: c86b288f4418bd4e8d09498c0afbb88e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.ptg.quicksand = collective.ptg.quicksand:main
    #
    # Would create an entry point called collective.ptg.quicksand that calls collective.ptg.quicksand.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.plonetruegallery >=3.2a1

  run:
    - python
    - setuptools
    - setuptools
    - collective.plonetruegallery >=3.2a1

test:
  # Python imports
  imports:
    - collective
    - collective.ptg
    - collective.ptg.quicksand

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.ptg.quicksand
  license: GPL
  summary: 'Sheet gallery for plonetruegallery'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
