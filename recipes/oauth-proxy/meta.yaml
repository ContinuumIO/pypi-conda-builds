package:
  name: oauth-proxy
  version: "1.0.5"

source:
  fn: oauth-proxy-1.0.5.tar.gz
  url: https://pypi.python.org/packages/source/o/oauth-proxy/oauth-proxy-1.0.5.tar.gz
  md5: c5949de87e83e21ec725593d93c40289
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - oauth-proxy = oauth-proxy:main
    #
    # Would create an entry point called oauth-proxy that calls oauth-proxy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - twisted >=8.2.0
    - oauth >=1.0.1

  run:
    - python
    - twisted >=8.2.0
    - oauth >=1.0.1

test:
  # Python imports
  imports:
    - oauth_proxy
    - twisted.plugins

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/mojodna/oauth-proxy
  license: BSD License
  summary: 'OAuth HTTP proxy'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
