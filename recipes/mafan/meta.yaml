package:
  name: mafan
  version: "0.2.6"

source:
  fn: mafan-0.2.6.tar.gz
  url: https://pypi.python.org/packages/source/m/mafan/mafan-0.2.6.tar.gz
  md5: a5ccdfdaee8ccc0d9bc92290913cb277
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mafan = mafan:main
    #
    # Would create an entry point called mafan that calls mafan.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - jieba ==0.29
    - argparse ==1.1
    - chardet ==2.1.1
    - wsgiref ==0.1.2
    - jianfan ==0.0.1

  run:
    - python
    - jieba ==0.29
    - argparse ==1.1
    - chardet ==2.1.1
    - wsgiref ==0.1.2
    - jianfan ==0.0.1

test:
  # Python imports
  imports:
    - mafan
    - mafan.hanzidentifier

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/hermanschaaf/mafan
  license: LICENSE.txt
  summary: 'A toolbox for working with the Chinese language in Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
