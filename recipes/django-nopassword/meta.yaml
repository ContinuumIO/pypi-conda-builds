package:
  name: django-nopassword
  version: "1.3.1"

source:
  fn: django-nopassword-1.3.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-nopassword/django-nopassword-1.3.1.tar.gz
  md5: 23f4dbb0ba7ff55205a43e3c66e15100
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-nopassword = django-nopassword:main
    #
    # Would create an entry point called django-nopassword that calls django-nopassword.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - nopassword
    - nopassword.backends
    - nopassword.migrations
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/relekang/django-nopassword
  license: MIT
  summary: 'Authentication backend for django that uses a one time code instead of passwords'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
