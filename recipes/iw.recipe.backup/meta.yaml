package:
  name: iw.recipe.backup
  version: "0.2.1"

source:
  fn: iw.recipe.backup-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/i/iw.recipe.backup/iw.recipe.backup-0.2.1.tar.gz
  md5: 5fc57dd2c5b88453d1b248c6b3008ad2
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - iw.recipe.backup = iw.recipe.backup:main
    #
    # Would create an entry point called iw.recipe.backup that calls iw.recipe.backup.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - zc.recipe.egg

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - zc.recipe.egg

test:
  # Python imports
  imports:
    - iw
    - iw.recipe
    - iw.recipe.backup
    - iw.recipe.backup.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://ingeniweb.svn.sourceforge.net/svnroot/ingeniweb/iw.recipe.backup
  license: Zope Public License
  summary: 'Zc buildout recipe that provides a backup script'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
