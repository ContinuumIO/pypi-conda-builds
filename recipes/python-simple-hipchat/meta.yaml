package:
  name: python-simple-hipchat
  version: "0.3.3"

source:
  fn: python-simple-hipchat-0.3.3.tar.gz
  url: https://pypi.python.org/packages/source/p/python-simple-hipchat/python-simple-hipchat-0.3.3.tar.gz
  md5: 78cc10471ec29a083efa466d4367fc54
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-simple-hipchat = python-simple-hipchat:main
    #
    # Would create an entry point called python-simple-hipchat that calls python-simple-hipchat.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - hipchat

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kurttheviking/python-simple-hipchat
  license: MIT License
  summary: "Easy peasy wrapper for HipChat's v1 API"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
