package:
  name: chameleon.html
  version: "1.0b4"

source:
  fn: chameleon.html-1.0b4.tar.gz
  url: https://pypi.python.org/packages/source/c/chameleon.html/chameleon.html-1.0b4.tar.gz
  md5: 3531499043bb532cf9ef0a0ed1e42c12
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - chameleon.html = chameleon.html:main
    #
    # Would create an entry point called chameleon.html that calls chameleon.html.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - chameleon.core >=1.0b33
    - lxml >=2.1.1
    - repoze.cssutils >=1.0a6
    - zope.component
    - zope.i18n >=3.5
    - zope.interface

  run:
    - python
    - setuptools
    - chameleon.core >=1.0b33
    - lxml >=2.1.1
    - repoze.cssutils >=1.0a6
    - zope.component
    - zope.i18n >=3.5
    - zope.interface

test:
  # Python imports
  imports:
    - chameleon
    - chameleon.html
    - chameleon.html.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: BSD
  summary: 'Dynamic HTML template compiler with XSS language support.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
