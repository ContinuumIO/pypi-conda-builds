package:
  name: collective.memberemails
  version: "1.0a2"

source:
  fn: collective.memberemails-1.0a2.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.memberemails/collective.memberemails-1.0a2.tar.gz
  md5: deaf77e839e81c14714e8c0b70268145
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.memberemails = collective.memberemails:main
    #
    # Would create an entry point called collective.memberemails that calls collective.memberemails.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - pas.plugins.memberapproval
    - collective.memberapproval

  run:
    - python
    - setuptools
    - setuptools
    - pas.plugins.memberapproval
    - collective.memberapproval

test:
  # Python imports
  imports:
    - collective
    - collective.memberemails
    - collective.memberemails.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/
  license: gpl
  summary: 'Email notification for pas.plugins.memberapproval'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
