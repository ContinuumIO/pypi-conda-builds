package:
  name: chandler-photoplugin
  version: "1.0-r16584"

source:
  fn: Chandler-PhotoPlugin-1.0-r16584.tar.gz
  url: https://pypi.python.org/packages/source/C/Chandler-PhotoPlugin/Chandler-PhotoPlugin-1.0-r16584.tar.gz
  md5: 66f623222a4cd49fde57d6791f2f6c0a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - chandler-photoplugin = chandler-photoplugin:main
    #
    # Would create an entry point called chandler-photoplugin that calls chandler-photoplugin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - photos

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Apache Software License
  summary: 'Photograph loading for Chandler'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
