package:
  name: django-rest-framework-proxy
  version: "1.4.0"

source:
  fn: django-rest-framework-proxy-1.4.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-rest-framework-proxy/django-rest-framework-proxy-1.4.0.tar.gz
  md5: 48bdfe73e96817b122b38d140e82cc6c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-rest-framework-proxy = django-rest-framework-proxy:main
    #
    # Would create an entry point called django-rest-framework-proxy that calls django-rest-framework-proxy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4
    - djangorestframework >=2.2.0
    - requests >=1.1.0

  run:
    - python
    - django >=1.4
    - djangorestframework >=2.2.0
    - requests >=1.1.0

test:
  # Python imports
  imports:
    - rest_framework_proxy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/eofs/django-rest-framework-proxy/
  license: BSD License
  summary: 'Django Rest Framework Proxy allows easy proxying of incoming REST requests'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
