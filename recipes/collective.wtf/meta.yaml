package:
  name: collective.wtf
  version: "1.0b9"

source:
  fn: collective.wtf-1.0b9.zip
  url: https://pypi.python.org/packages/source/c/collective.wtf/collective.wtf-1.0b9.zip
  md5: 2da42cc7581998dae8eb89e7792da42e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.wtf = collective.wtf:main
    #
    # Would create an entry point called collective.wtf that calls collective.wtf.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zope.interface
    - zope.component
    - products.cmfcore
    - products.dcworkflow
    - products.genericsetup
    - plone.memoize

  run:
    - python
    - setuptools
    - setuptools
    - zope.interface
    - zope.component
    - products.cmfcore
    - products.dcworkflow
    - products.genericsetup
    - plone.memoize

test:
  # Python imports
  imports:
    - collective
    - collective.wtf
    - collective.wtf.browser
    - collective.wtf.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org
  license: LGPL
  summary: 'GenericSetup importer and exporter for workflow definitions that uses CSV instead of XML'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
