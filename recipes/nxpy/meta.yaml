package:
  name: nxpy
  version: "0.3.0"

source:
  fn: Nxpy-0.3.0.zip
  url: https://pypi.python.org/packages/source/N/Nxpy/Nxpy-0.3.0.zip
  md5: 3159aac32cde08107ba59d45c0ad8f76
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - nxpy = nxpy:main
    #
    # Would create an entry point called nxpy that calls nxpy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six >=1.8.0

  run:
    - python
    - six >=1.8.0

test:
  # Python imports
  imports:
    - nxpy
    - nxpy.ccase
    - nxpy.ccase.test
    - nxpy.command
    - nxpy.core
    - nxpy.core._impl
    - nxpy.etree
    - nxpy.maven
    - nxpy.msvs
    - nxpy.msvs._impl
    - nxpy.msvs._impl.solution
    - nxpy.ply
    - nxpy.scons
    - nxpy.svn
    - nxpy.test
    - nxpy.wx
    - nxpy.wx.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://nxpy.sourceforge.net
  license: Boost Software License version 1.0
  summary: "Nick's Python Toolchest"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
