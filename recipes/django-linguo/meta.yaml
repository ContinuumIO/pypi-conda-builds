package:
  name: django-linguo
  version: "1.4.0"

source:
  fn: django-linguo-1.4.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-linguo/django-linguo-1.4.0.tar.gz
  md5: 617f8ca54c23823bab9a7b058ce25322
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-linguo = django-linguo:main
    #
    # Would create an entry point called django-linguo that calls django-linguo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - linguo
    - linguo.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/zmathew/django-linguo
  license: BSD License
  summary: 'Linguo aims to make model translation easy and is designed to let you use the built-in Django features (Query API, Model Forms, Admin, etc) as intended.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
