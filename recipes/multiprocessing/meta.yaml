package:
  name: multiprocessing
  version: "2.6.2.1"

source:
  fn: multiprocessing-2.6.2.1.tar.gz
  url: https://pypi.python.org/packages/source/m/multiprocessing/multiprocessing-2.6.2.1.tar.gz
  md5: 5cc484396c040102116ccc2355379c72
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - multiprocessing = multiprocessing:main
    #
    # Would create an entry point called multiprocessing that calls multiprocessing.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - multiprocessing
    - multiprocessing.dummy
    - multiprocessing.examples

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/python-multiprocessing
  license: BSD License
  summary: 'Backport of the multiprocessing package to Python 2.4 and 2.5'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
