package:
  name: hypatia
  version: "0.3"

source:
  fn: hypatia-0.3.tar.gz
  url: https://pypi.python.org/packages/source/h/hypatia/hypatia-0.3.tar.gz
  md5: d74c6dda31ff459a39fa5da9e98f2425
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hypatia = hypatia:main
    #
    # Would create an entry point called hypatia that calls hypatia.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - persistent
    - btrees
    - zodb >=4.0.0b2
    - zope.interface

  run:
    - python
    - persistent
    - btrees
    - zodb >=4.0.0b2
    - zope.interface

test:
  # Python imports
  imports:
    - benchmark
    - hypatia
    - hypatia.catalog
    - hypatia.exc
    - hypatia.facet
    - hypatia.field
    - hypatia.keyword
    - hypatia.nbest
    - hypatia.query
    - hypatia.tests
    - hypatia.text
    - hypatia.text.tests
    - hypatia.util

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pylonsproject.org
  license: ZPL 2.1
  summary: 'Python package for searching and indexing'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
