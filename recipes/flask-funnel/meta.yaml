package:
  name: flask-funnel
  version: "0.1.10"

source:
  fn: Flask-Funnel-0.1.10.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Funnel/Flask-Funnel-0.1.10.tar.gz
  md5: d2af39fb2cb4f7164d97bb5c108f461a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-funnel = flask-funnel:main
    #
    # Would create an entry point called flask-funnel that calls flask-funnel.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - flask-script

  run:
    - python
    - flask
    - flask-script

test:
  # Python imports
  imports:
    - flask_funnel

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/rehandalal/flask-funnel/
  license: BSD License
  summary: 'Asset management for Flask.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
