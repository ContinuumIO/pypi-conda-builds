package:
  name: applepushnotification
  version: "0.1.1"

source:
  fn: applepushnotification-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/a/applepushnotification/applepushnotification-0.1.1.tar.gz
  md5: fb84d4a6cb72edd6c4208c762d241f9d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - applepushnotification = applepushnotification:main
    #
    # Would create an entry point called applepushnotification that calls applepushnotification.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - applepushnotification

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/martinkou/applepushnotification
  license: BSD
  summary: 'Apple Push Notification Service connector'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
