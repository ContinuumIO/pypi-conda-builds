package:
  name: hyperglyph
  version: "0.9-20140421"

source:
  fn: hyperglyph-0.9-20140421.tar.gz
  url: https://pypi.python.org/packages/source/h/hyperglyph/hyperglyph-0.9-20140421.tar.gz
  md5: 9f5561c8d42e23bee587efbc7fa932d1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hyperglyph = hyperglyph:main
    #
    # Would create an entry point called hyperglyph that calls hyperglyph.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - werkzeug
    - requests
    - pytz
    - isodate

  run:
    - python
    - werkzeug
    - requests
    - pytz
    - isodate

test:
  # Python imports
  imports:
    - hyperglyph
    - hyperglyph.resource

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT License
  summary: 'hyperglyph is ducked typed ipc over http'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
