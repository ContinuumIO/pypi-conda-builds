package:
  name: nacelle
  version: "0.4.1"

source:
  fn: nacelle-0.4.1.tar.gz
  url: https://pypi.python.org/packages/source/n/nacelle/nacelle-0.4.1.tar.gz
  md5: 9f76d6dd3172860bd86777ddc20001c8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - nacelle = nacelle:main
    #
    # Would create an entry point called nacelle that calls nacelle.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - nacelle
    - nacelle.conf
    - nacelle.contrib
    - nacelle.contrib.lockdown
    - nacelle.contrib.mail
    - nacelle.core
    - nacelle.core.sessions
    - nacelle.core.template
    - nacelle.core.utils
    - nacelle.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/nacelle/nacelle
  license: MIT License
  summary: 'A lightweight Python framework (built on top of webapp2) for use on Google Appengine'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
