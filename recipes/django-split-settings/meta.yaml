package:
  name: django-split-settings
  version: "0.1.2"

source:
  fn: django-split-settings-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-split-settings/django-split-settings-0.1.2.tar.gz
  md5: 28e48fac29878c88f782861a0516eabe
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-split-settings = django-split-settings:main
    #
    # Would create an entry point called django-split-settings that calls django-split-settings.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5.1

  run:
    - python
    - django >=1.5.1

test:
  # Python imports
  imports:
    - split_settings

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/sobolevn/django-split-settings
  license: BSD License
  summary: 'Organize Django settings into multiple files and directories. Easily override and modify settings. Use wildcards and optional settings files.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
