package:
  name: lunar-html
  version: "0.1.0"

source:
  fn: lunar-html-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/l/lunar-html/lunar-html-0.1.0.tar.gz
  md5: c6d7fac102eabbc6ff347def8d47da34
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lunar-html = lunar-html:main
    #
    # Would create an entry point called lunar-html that calls lunar-html.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cssselect
    - lxml
    - beautifulsoup

  run:
    - python
    - cssselect
    - lxml
    - beautifulsoup

test:
  # Python imports
  imports:
    - lunar_html

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: GPLv3+
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
