package:
  name: django-composition
  version: "0.2"

source:
  fn: django-composition-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-composition/django-composition-0.2.tar.gz
  md5: ece323cc472df4429f0c837d58c220c2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-composition = django-composition:main
    #
    # Would create an entry point called django-composition that calls django-composition.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - composition
    - composition.shortcuts
    - composition.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/daevaorn/django-composition/
  license: BSD License
  summary: '`django-composition` provides the abstract way to denormalize data from your models in simple declarative way through special generic model field called `CompositionField`'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
