package:
  name: j01.datepicker
  version: "0.6.1"

source:
  fn: j01.datepicker-0.6.1.zip
  url: https://pypi.python.org/packages/source/j/j01.datepicker/j01.datepicker-0.6.1.zip
  md5: 5c0004f9358a44668fdbcb40e5fd47cc
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - j01.datepicker = j01.datepicker:main
    #
    # Would create an entry point called j01.datepicker that calls j01.datepicker.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.location
    - z3c.form

  run:
    - python
    - setuptools
    - zope.location
    - z3c.form

test:
  # Python imports
  imports:
    - j01
    - j01.datepicker

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/j01.datepicker
  license: Zope Public License
  summary: 'JQuery datepicker based on 3c.form for Zope 3'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
