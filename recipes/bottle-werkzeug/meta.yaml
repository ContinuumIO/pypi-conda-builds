package:
  name: bottle-werkzeug
  version: "0.1"

source:
  fn: bottle-werkzeug-0.1.tar.gz
  url: https://pypi.python.org/packages/source/b/bottle-werkzeug/bottle-werkzeug-0.1.tar.gz
  md5: b8d63aa1d562d66a82d8cb22c768366a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bottle-werkzeug = bottle-werkzeug:main
    #
    # Would create an entry point called bottle-werkzeug that calls bottle-werkzeug.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bottlepy.org/docs/dev/plugin/werkzeug/
  license: MIT License
  summary: 'Werkzeug integration for Bottle.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
