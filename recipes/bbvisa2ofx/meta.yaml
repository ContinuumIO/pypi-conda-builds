package:
  name: bbvisa2ofx
  version: "0.21"

source:
  fn: bbvisa2ofx-0.21.tar.gz
  url: https://pypi.python.org/packages/source/b/bbvisa2ofx/bbvisa2ofx-0.21.tar.gz
  md5: 83061708c3122164e95f9135168e32c8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bbvisa2ofx = bbvisa2ofx:main
    #
    # Would create an entry point called bbvisa2ofx that calls bbvisa2ofx.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - bbvisa2ofx
    - bbvisa2ofx.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://josecoelho.com
  license: Apache Software License
  summary: 'Converter uma fatura de cartao de credito TXT do Banco do Brasil para OFX'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
