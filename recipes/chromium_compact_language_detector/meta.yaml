package:
  name: chromium_compact_language_detector
  version: "0.031415"

source:
  fn: chromium_compact_language_detector-0.031415.tar.gz
  url: https://pypi.python.org/packages/source/c/chromium_compact_language_detector/chromium_compact_language_detector-0.031415.tar.gz
  md5: ddf79daf445c9b7fccfad40252536af8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - chromium_compact_language_detector = chromium_compact_language_detector:main
    #
    # Would create an entry point called chromium_compact_language_detector that calls chromium_compact_language_detector.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/chromium-compact-language-detector
  license: BSD License
  summary: "Python bindings around Google Chromium's embedded compact language detection library"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
