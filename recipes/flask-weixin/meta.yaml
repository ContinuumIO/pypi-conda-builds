package:
  name: flask-weixin
  version: "0.4.1"

source:
  fn: Flask-Weixin-0.4.1.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Weixin/Flask-Weixin-0.4.1.tar.gz
  md5: d69d14ebbba21f01395838ab2171671b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-weixin = flask-weixin:main
    #
    # Would create an entry point called flask-weixin that calls flask-weixin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lepture/flask-weixin
  license: BSD License
  summary: 'Weixin for Flask.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
