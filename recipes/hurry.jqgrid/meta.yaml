package:
  name: hurry.jqgrid
  version: "3.6.2.0"

source:
  fn: hurry.jqgrid-3.6.2.0.tar.gz
  url: https://pypi.python.org/packages/source/h/hurry.jqgrid/hurry.jqgrid-3.6.2.0.tar.gz
  md5: bc41cf2007b40b040ccf97928e4294ee
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hurry.jqgrid = hurry.jqgrid:main
    #
    # Would create an entry point called hurry.jqgrid that calls hurry.jqgrid.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - hurry.resource >0.2
    - hurry.jquery
    - hurry.jqueryui

  run:
    - python
    - setuptools
    - hurry.resource >0.2
    - hurry.jquery
    - hurry.jqueryui

test:
  # Python imports
  imports:
    - hurry
    - hurry.jqgrid

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: ZPL 2.1 (jqgrid MIT/GPL)
  summary: 'hurry.resource style resources for jqgrid for jQuery.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
