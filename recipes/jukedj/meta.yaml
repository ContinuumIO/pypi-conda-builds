package:
  name: jukedj
  version: "2.0.2"

source:
  fn: jukedj-2.0.2.tar.gz
  url: https://pypi.python.org/packages/source/j/jukedj/jukedj-2.0.2.tar.gz
  md5: 6a246951ae327877593d0dfb39ea150a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jukedj = jukedj:main
    #
    # Would create an entry point called jukedj that calls jukedj.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.7

  run:
    - python
    - django >=1.7

test:
  # Python imports
  imports:
    - jukedj
    - jukedj.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/JukeboxPipeline/jukedj
  license: BSD License
  summary: 'Django app for models used in the JukeboxPipeline.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
