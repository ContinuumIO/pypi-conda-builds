package:
  name: clickatell
  version: "0.0.1"

source:
  fn: clickatell-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/clickatell/clickatell-0.0.1.tar.gz
  md5: 9f294d3ba13e224bb9e4e177a10dea20
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - clickatell = clickatell:main
    #
    # Would create an entry point called clickatell that calls clickatell.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - httplib2

  run:
    - python
    - httplib2

test:
  # Python imports
  imports:
    - clickatell
    - clickatell.http
    - clickatell.rest

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/arcturial/clickatell-python
  license: LICENSE
  summary: 'Library for interacting with the Clickatell SMS Gateway'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
