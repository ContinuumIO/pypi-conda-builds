package:
  name: cs.i18n
  version: "0.10"

source:
  fn: cs.i18n-0.10.zip
  url: https://pypi.python.org/packages/source/c/cs.i18n/cs.i18n-0.10.zip
  md5: 7d2bb0529815b56f7ef96a7fff061a1f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cs.i18n = cs.i18n:main
    #
    # Would create an entry point called cs.i18n that calls cs.i18n.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.browserlayer
    - plone.app.i18n
    - plone.memoize

  run:
    - python
    - setuptools
    - setuptools
    - plone.browserlayer
    - plone.app.i18n
    - plone.memoize

test:
  # Python imports
  imports:
    - cs
    - cs.i18n
    - cs.i18n.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.codesyntax.com/private/cs.i18n
  license: GPL
  summary: 'Some i18n customizations for Plone default language change forms'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
