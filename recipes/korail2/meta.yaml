package:
  name: korail2
  version: "0.0.6"

source:
  fn: korail2-0.0.6.tar.gz
  url: https://pypi.python.org/packages/source/k/korail2/korail2-0.0.6.tar.gz
  md5: 99a0c9826181086f3a76a52103649803
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - korail2 = korail2:main
    #
    # Would create an entry point called korail2 that calls korail2.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - korail2

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/carpedm20/korail2
  license: BSD License
  summary: 'Korail(www.letskorail.com) wrapper for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
