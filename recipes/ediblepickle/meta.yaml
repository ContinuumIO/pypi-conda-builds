package:
  name: ediblepickle
  version: "1.1.3"

source:
  fn: ediblepickle-1.1.3.tar.gz
  url: https://pypi.python.org/packages/source/e/ediblepickle/ediblepickle-1.1.3.tar.gz
  md5: c0ec625856dd670243c7a5d563bb1d24
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ediblepickle = ediblepickle:main
    #
    # Would create an entry point called ediblepickle that calls ediblepickle.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - ediblepickle

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mpavan/ediblepickle
  license: Apache Software License
  summary: 'Checkpoint'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
