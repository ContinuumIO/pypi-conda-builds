package:
  name: arenapy
  version: "0.1.0"

source:
  fn: ArenaPy-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/A/ArenaPy/ArenaPy-0.1.0.tar.gz
  md5: abfe13f7ab3c842781f501d997ecf545
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - arenapy = arenapy:main
    #
    # Would create an entry point called arenapy that calls arenapy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - arenapy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jnhasty/arenapy
  license: MIT License
  summary: 'A simple python interface to the Are.na api'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
