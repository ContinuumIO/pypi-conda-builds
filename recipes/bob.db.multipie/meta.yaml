package:
  name: bob.db.multipie
  version: "2.0.1"

source:
  fn: bob.db.multipie-2.0.1.zip
  url: https://pypi.python.org/packages/source/b/bob.db.multipie/bob.db.multipie-2.0.1.zip
  md5: 954b80a015fc57ab5fbe09e9e0848e7b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bob.db.multipie = bob.db.multipie:main
    #
    # Would create an entry point called bob.db.multipie that calls bob.db.multipie.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - bob.db.base
    - bob.db.verification.utils

  run:
    - python
    - setuptools
    - setuptools
    - bob.db.base
    - bob.db.verification.utils

test:
  # Python imports
  imports:
    - bob
    - bob.db
    - bob.db.multipie

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/bob.db.multipie
  license: GNU General Public License v3 (GPLv3)
  summary: 'Multi-PIE Database Access API for Bob'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
