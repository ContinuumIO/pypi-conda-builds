package:
  name: call
  version: "0.1"

source:
  fn: call-0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/call/call-0.1.tar.gz
  md5: 7083ce1ce86474165f36eec9579d8999
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - call = call:main
    #
    # Would create an entry point called call that calls call.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - call

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/zyga/call/
  license: GNU Lesser General Public License v3 or later (LGPLv3+)
  summary: 'Pure-python reimplementation of python call semantics'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
