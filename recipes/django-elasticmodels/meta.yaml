package:
  name: django-elasticmodels
  version: "1.0.2"

source:
  fn: django-elasticmodels-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-elasticmodels/django-elasticmodels-1.0.2.tar.gz
  md5: d118539bb6cc4a0f2c456e996cf7719b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-elasticmodels = django-elasticmodels:main
    #
    # Would create an entry point called django-elasticmodels that calls django-elasticmodels.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyelasticsearch >=0.6

  run:
    - python
    - pyelasticsearch >=0.6

test:
  # Python imports
  imports:
    - elasticmodels
    - elasticmodels.management
    - elasticmodels.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.com/jvennik/django-elasticmodels
  license: BSD License
  summary: 'A tool to map Django models to ElasticSearch mappings'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
