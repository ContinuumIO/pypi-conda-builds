package:
  name: django-krate
  version: "0.0.1"

source:
  fn: django-krate-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-krate/django-krate-0.0.1.tar.gz
  md5: 988fa720b2cd0b2e5bb04618cdfdd1f3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-krate = django-krate:main
    #
    # Would create an entry point called django-krate that calls django-krate.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - distribute

  run:
    - python
    - distribute

test:
  # Python imports
  imports:
    - krate
    - krate.ratehandlers
    - krate.ratehandlers.dbratehandler
    - krate.templatetags
    - testing
    - testing.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kaleidos/django-krate
  license: BSD License
  summary: 'Django application to rate objects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
