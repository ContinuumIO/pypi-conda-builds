package:
  name: flask-menubuilder
  version: "0.9.2"

source:
  fn: Flask-MenuBuilder-0.9.2.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-MenuBuilder/Flask-MenuBuilder-0.9.2.tar.gz
  md5: b950f33b38d503379ae866647cd83aa9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-menubuilder = flask-menubuilder:main
    #
    # Would create an entry point called flask-menubuilder that calls flask-menubuilder.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask

  run:
    - python
    - flask

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://dev.ufsoft.org/projects/menu-builder
  license: BSD License
  summary: 'An easy way to create menus to use with flask.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
