package:
  name: metrium
  version: "0.1.3"

source:
  fn: metrium-0.1.3.zip
  url: https://pypi.python.org/packages/source/m/metrium/metrium-0.1.3.zip
  md5: 53e5106259c503081fcbeeb7a0ae9c00
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - metrium = metrium:main
    #
    # Would create an entry point called metrium that calls metrium.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - quorum
    - pymongo
    - redis
    - omni_api

  run:
    - python
    - flask
    - quorum
    - pymongo
    - redis
    - omni_api

test:
  # Python imports
  imports:
    - metrium
    - metrium.bots
    - metrium.models
    - metrium.models.config
    - metrium.views
    - metrium.views.api
    - metrium.views.web

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://metrium.com
  license: GNU General Public License (GPL)
  summary: 'Pingu Web Interface'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
