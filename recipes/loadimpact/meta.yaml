package:
  name: loadimpact
  version: "1.1.0"

source:
  fn: loadimpact-1.1.0.tar.gz
  url: https://pypi.python.org/packages/source/l/loadimpact/loadimpact-1.1.0.tar.gz
  md5: d2089aa02c347f3a0fb7404457a199f1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - loadimpact = loadimpact:main
    #
    # Would create an entry point called loadimpact that calls loadimpact.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - loadimpact

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://developers.loadimpact.com/
  license: LICENSE.txt
  summary: "The Load Impact SDK provides access to Load Impact's cloud-based performance testing platform"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
