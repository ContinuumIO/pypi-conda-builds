package:
  name: ascii_graph
  version: "1.0.1"

source:
  fn: ascii_graph-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/a/ascii_graph/ascii_graph-1.0.1.tar.gz
  md5: 3ff9328256eb99d2ae9ce68f81629526
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ascii_graph = ascii_graph:main
    #
    # Would create an entry point called ascii_graph that calls ascii_graph.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - ascii_graph

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kakwa/py-ascii-graph
  license: MIT License
  summary: 'A simple python lib to print data as ascii histograms.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
