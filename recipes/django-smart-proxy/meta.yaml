package:
  name: django-smart-proxy
  version: "0.2.1"

source:
  fn: django-smart-proxy-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-smart-proxy/django-smart-proxy-0.2.1.tar.gz
  md5: 2f9e80bcbab2204ab669be44fa79c94a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-smart-proxy = django-smart-proxy:main
    #
    # Would create an entry point called django-smart-proxy that calls django-smart-proxy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=2
    - django >=1.4,<1.7

  run:
    - python
    - requests >=2
    - django >=1.4,<1.7

test:
  # Python imports
  imports:
    - smart_proxy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/celerityweb/django-smart-proxy/
  license: MIT - see LICENSE file
  summary: 'The django-smart-proxy app allows you to configure plug-and-play reverse proxy solutions for social networks and other complex integration points with your website.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
