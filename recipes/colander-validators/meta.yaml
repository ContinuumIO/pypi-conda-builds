package:
  name: colander-validators
  version: "0.0.4.1"

source:
  fn: colander-validators-0.0.4.1.tar.gz
  url: https://pypi.python.org/packages/source/c/colander-validators/colander-validators-0.0.4.1.tar.gz
  md5: 0f586f115e950d2ae0ba1d9ab048664a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - colander-validators = colander-validators:main
    #
    # Would create an entry point called colander-validators that calls colander-validators.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - colander_validators

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/ixmatus/colander-validators
  license: Python Software Foundation License
  summary: "Better validators for Colander based on FormEncode's validators"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
