package:
  name: baseframe
  version: "0.3.0"

source:
  fn: baseframe-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/b/baseframe/baseframe-0.3.0.tar.gz
  md5: 73d88ab7b3f879bdc19486d093f5ffc3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - baseframe = baseframe:main
    #
    # Would create an entry point called baseframe that calls baseframe.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - semantic_version
    - bleach
    - pytz
    - requests
    - dnspython
    - wtforms
    - flask
    - flask-assets
    - flask-wtf
    - flask-cache
    - flask-babelex
    - flask-mustachejs
    - cssmin
    - coaster
    - lxml
    - fluent-logger

  run:
    - python
    - semantic_version
    - bleach
    - pytz
    - requests
    - dnspython
    - wtforms
    - flask
    - flask-assets
    - flask-wtf
    - flask-cache
    - flask-babelex
    - flask-mustachejs
    - cssmin
    - coaster
    - lxml
    - fluent-logger

test:
  # Python imports
  imports:
    - baseframe

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/hasgeek/baseframe
  license: BSD License
  summary: 'Baseframe for HasGeek projects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
