package:
  name: django-otp-agents
  version: "0.2.1"

source:
  fn: django-otp-agents-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-otp-agents/django-otp-agents-0.2.1.tar.gz
  md5: 9cedb37aa5b4652603a81d1a04faffd5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-otp-agents = django-otp-agents:main
    #
    # Would create an entry point called django-otp-agents that calls django-otp-agents.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-otp >=0.2.0
    - django-agent-trust >=0.1.8

  run:
    - python
    - django-otp >=0.2.0
    - django-agent-trust >=0.1.8

test:
  # Python imports
  imports:
    - otp_agents
    - otp_agents.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/psagers/django-otp
  license: BSD License
  summary: 'Integration of django-otp and django-agent-trust.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
