package:
  name: iw.recipe.template
  version: "0.2"

source:
  fn: iw.recipe.template-0.2.tar.gz
  url: https://pypi.python.org/packages/source/i/iw.recipe.template/iw.recipe.template-0.2.tar.gz
  md5: b0a655c210ec4c28a7dbdc34bab3cbe9
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - iw.recipe.template = iw.recipe.template:main
    #
    # Would create an entry point called iw.recipe.template that calls iw.recipe.template.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zope.testing
    - zc.buildout
    - cheetah

  run:
    - python
    - setuptools
    - setuptools
    - zope.testing
    - zc.buildout
    - cheetah

test:
  # Python imports
  imports:
    - iw
    - iw.recipe
    - iw.recipe.template
    - iw.recipe.template.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: GPL
  summary: 'ZC buildout recipe to build template based file'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
