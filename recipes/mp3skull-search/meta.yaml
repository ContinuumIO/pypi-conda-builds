package:
  name: mp3skull-search
  version: "0.1.0"

source:
  fn: mp3skull-search-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/m/mp3skull-search/mp3skull-search-0.1.0.tar.gz
  md5: 45b6d1c701711f0810481caa3f1e1e8c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mp3skull-search = mp3skull-search:main
    #
    # Would create an entry point called mp3skull-search that calls mp3skull-search.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/resurge/mp3skull-search
  license: UNKNOWN
  summary: 'A module to search mp3skull.com'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
