package:
  name: zopyx.txng3.ext
  version: "3.3.7"

source:
  fn: zopyx.txng3.ext-3.3.7.zip
  url: https://pypi.python.org/packages/source/z/zopyx.txng3.ext/zopyx.txng3.ext-3.3.7.zip
  md5: d9ee0f4311fa1b008c76614ef2b63e82
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zopyx.txng3.ext = zopyx.txng3.ext:main
    #
    # Would create an entry point called zopyx.txng3.ext that calls zopyx.txng3.ext.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://sf.net/projects/textindexng/
  license: UNKNOWN
  summary: 'Helper modules for TextIndexNG3 (Snowball stemmer, normalizer, splitter, etc.)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
