package:
  name: xstatic-angular-cookies
  version: "1.2.1.1"

source:
  fn: XStatic-Angular-Cookies-1.2.1.1.tar.gz
  url: https://pypi.python.org/packages/source/X/XStatic-Angular-Cookies/XStatic-Angular-Cookies-1.2.1.1.tar.gz
  md5: a67066fac5dfe9774ab163d44e5a896a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - xstatic-angular-cookies = xstatic-angular-cookies:main
    #
    # Would create an entry point called xstatic-angular-cookies that calls xstatic-angular-cookies.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - xstatic
    - xstatic.pkg
    - xstatic.pkg.angular_cookies

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://angularjs.org
  license: (same as Angular-Cookies)
  summary: 'Angular-Cookies 1.2.1 (XStatic packaging standard)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
