package:
  name: redturtle.custommenu.factories
  version: "0.3.1"

source:
  fn: redturtle.custommenu.factories-0.3.1.zip
  url: https://pypi.python.org/packages/source/r/redturtle.custommenu.factories/redturtle.custommenu.factories-0.3.1.zip
  md5: e0ffef547d3e644b9db02795cd4fde12
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - redturtle.custommenu.factories = redturtle.custommenu.factories:main
    #
    # Would create an entry point called redturtle.custommenu.factories that calls redturtle.custommenu.factories.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - redturtle
    - redturtle.custommenu
    - redturtle.custommenu.factories
    - redturtle.custommenu.factories.Extensions
    - redturtle.custommenu.factories.adapters
    - redturtle.custommenu.factories.browser
    - redturtle.custommenu.factories.exportimport
    - redturtle.custommenu.factories.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/redturtle.custommenu.factories
  license: GPL
  summary: 'Customize the Plone "add portal content" menu'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
