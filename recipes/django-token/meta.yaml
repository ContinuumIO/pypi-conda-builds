package:
  name: django-token
  version: "0.1.1"

source:
  fn: django-token-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-token/django-token-0.1.1.tar.gz
  md5: a8a69b7f964b0b9d6c46cab7f2d39fb1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-token = django-token:main
    #
    # Would create an entry point called django-token that calls django-token.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5

  run:
    - python
    - django >=1.5

test:
  # Python imports
  imports:
    - django_token
    - django_token.management
    - django_token.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jasonbeverage/django-token
  license: UNKNOWN
  summary: 'Simple token based authentication for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
