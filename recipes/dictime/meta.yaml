package:
  name: dictime
  version: "0.0.2"

source:
  fn: dictime-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/dictime/dictime-0.0.2.tar.gz
  md5: 53d8ecb77df03cc3b9d2183ce16cac1a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dictime = dictime:main
    #
    # Would create an entry point called dictime that calls dictime.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - dictime

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/stevepeak/dictime
  license: Apache Software License
  summary: 'Dictionary with the dimension of time'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
