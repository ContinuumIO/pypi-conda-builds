package:
  name: charcoal
  version: "0.2.12"

source:
  fn: charcoal-0.2.12.tar.gz
  url: https://pypi.python.org/packages/source/c/charcoal/charcoal-0.2.12.tar.gz
  md5: 5306f035424a27b477c4cbf8347f7faa
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - charcoal = charcoal:main
    #
    # Would create an entry point called charcoal that calls charcoal.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - charcoal
    - charcoal.mocks

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/craigahobbs/charcoal
  license: MIT License
  summary: 'Various resource context managers and mocks.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
