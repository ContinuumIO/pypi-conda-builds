package:
  name: forgerypy
  version: "0.1"

source:
  fn: ForgeryPy-0.1.tar.gz
  url: https://pypi.python.org/packages/source/F/ForgeryPy/ForgeryPy-0.1.tar.gz
  md5: 66555becf769d7a7921b0d6183b2f953
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - forgerypy = forgerypy:main
    #
    # Would create an entry point called forgerypy that calls forgerypy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - forgery_py
    - forgery_py.forgery
    - forgerypy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://tomekwojcik.github.com/ForgeryPy/
  license: MIT License
  summary: 'An easy to use forged data generator for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
