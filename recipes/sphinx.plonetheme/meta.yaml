package:
  name: sphinx.plonetheme
  version: "2.0"

source:
  fn: sphinx.plonetheme-2.0.zip
  url: https://pypi.python.org/packages/source/s/sphinx.plonetheme/sphinx.plonetheme-2.0.zip
  md5: b59c216212402cf97262d4dd8a1b9647
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sphinx.plonetheme = sphinx.plonetheme:main
    #
    # Would create an entry point called sphinx.plonetheme that calls sphinx.plonetheme.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - sphinxjp.themecore

  run:
    - python
    - setuptools
    - setuptools
    - sphinxjp.themecore

test:
  # Python imports
  imports:
    - sphinx
    - sphinx.plonetheme

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/sphinx.plonetheme
  license: GNU General Public License (GPL)
  summary: 'Addon for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
