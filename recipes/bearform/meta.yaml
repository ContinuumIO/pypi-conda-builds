package:
  name: bearform
  version: "1.2"

source:
  fn: bearform-1.2.tar.gz
  url: https://pypi.python.org/packages/source/b/bearform/bearform-1.2.tar.gz
  md5: 6c6817580dac58c8d22921d6eeac1626
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bearform = bearform:main
    #
    # Would create an entry point called bearform that calls bearform.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - era >=1.1

  run:
    - python
    - era >=1.1

test:
  # Python imports
  imports:
    - bearform

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/WiFast/bearform
  license: BSD License
  summary: 'Easy data conversion and validation for frontends.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
