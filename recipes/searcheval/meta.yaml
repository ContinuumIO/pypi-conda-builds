package:
  name: searcheval
  version: "0.1.1"

source:
  fn: searcheval-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/s/searcheval/searcheval-0.1.1.tar.gz
  md5: 6ec4cd053ae8e20a071086518a9743ae
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - searcheval = searcheval:main
    #
    # Would create an entry point called searcheval that calls searcheval.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - searcheval
    - searcheval.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tobigue/searcheval
  license: LICENSE.txt
  summary: 'Library implementing metrics and tools for the evaluation of search results (rankings)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
