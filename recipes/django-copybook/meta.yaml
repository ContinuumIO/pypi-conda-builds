package:
  name: django-copybook
  version: "1.0.3"

source:
  fn: django-copybook-1.0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-copybook/django-copybook-1.0.3.tar.gz
  md5: fd1f1dd01a7e142709591218b3bba353
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-copybook = django-copybook:main
    #
    # Would create an entry point called django-copybook that calls django-copybook.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - djcopybook
    - djcopybook.fixedwidth
    - djcopybook.fixedwidth.tests
    - djcopybook.fixedwidth.tests.fields

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/imtapps/django-copybook
  license: BSD License
  summary: 'Convert Objects and Django models to/from fixed format records.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
