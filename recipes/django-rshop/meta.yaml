package:
  name: django-rshop
  version: "0.0.8"

source:
  fn: django-rshop-0.0.8.tar.gz
  url: https://pypi.python.org/packages/source/d/django-rshop/django-rshop-0.0.8.tar.gz
  md5: c6ab6db8f79e097484009ce41fc9934c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-rshop = django-rshop:main
    #
    # Would create an entry point called django-rshop that calls django-rshop.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django <1.5
    - south ==0.7.5
    - django-tinymce >=1.5.1b2
    - django-filebrowser-no-grappelli ==3.1.1
    - sorl-thumbnail ==11.12
    - django-compressor ==1.1.2

  run:
    - python
    - django <1.5
    - south ==0.7.5
    - django-tinymce >=1.5.1b2
    - django-filebrowser-no-grappelli ==3.1.1
    - sorl-thumbnail ==11.12
    - django-compressor ==1.1.2

test:
  # Python imports
  imports:
    - rshop
    - rshop.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/r0sk/django-rshop
  license: BSD License
  summary: 'Yet another Django Shop App'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
