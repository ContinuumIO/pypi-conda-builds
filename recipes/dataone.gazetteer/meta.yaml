package:
  name: dataone.gazetteer
  version: "1.0.2"

source:
  fn: dataone.gazetteer-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/dataone.gazetteer/dataone.gazetteer-1.0.2.tar.gz
  md5: 0576b6af26b43cd08e174aea2b8624dd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dataone.gazetteer = dataone.gazetteer:main
    #
    # Would create an entry point called dataone.gazetteer that calls dataone.gazetteer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - d1_gazetteer
    - d1_gazetteer.service

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://dataone.org
  license: Apache License, Version 2.0
  summary: 'A Gazetteer service for DataONE'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
