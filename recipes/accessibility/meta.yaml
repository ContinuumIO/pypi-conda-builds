package:
  name: accessibility
  version: "0.4.0"

source:
  fn: accessibility-0.4.0.tar.gz
  url: https://pypi.python.org/packages/source/a/accessibility/accessibility-0.4.0.tar.gz
  md5: 8490cf52f67d9de8135b74f38914559d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - accessibility = accessibility:main
    #
    # Would create an entry point called accessibility that calls accessibility.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - accessibility

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/atheriel/accessibility
  license: ISC License (ISCL)
  summary: 'Extension module that wraps the Accessibility API for Mac OS X.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
