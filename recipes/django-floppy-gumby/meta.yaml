package:
  name: django-floppy-gumby
  version: "0.1"

source:
  fn: django-floppy-gumby-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-floppy-gumby/django-floppy-gumby-0.1.tar.gz
  md5: f4b65c49e30297c720db0b19ee662fb2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-floppy-gumby = django-floppy-gumby:main
    #
    # Would create an entry point called django-floppy-gumby that calls django-floppy-gumby.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - django-floppyforms

  run:
    - python
    - django
    - django-floppyforms

test:
  # Python imports
  imports:
    - floppy_gumby_forms

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/burke-software/django-floppy-gumby
  license: BSD License
  summary: 'Gumby Framework forms for Django using floppy forms. A floppy and gumby project.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
