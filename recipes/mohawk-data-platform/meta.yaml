package:
  name: mohawk-data-platform
  version: "0.9.3"

source:
  fn: mohawk-data-platform-0.9.3.tar.gz
  url: https://pypi.python.org/packages/source/m/mohawk-data-platform/mohawk-data-platform-0.9.3.tar.gz
  md5: 1e7b267f2ac56e0286dbe20312d15ee6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mohawk-data-platform = mohawk-data-platform:main
    #
    # Would create an entry point called mohawk-data-platform that calls mohawk-data-platform.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-cross-origin ==0.9.0
    - jsonfield ==0.9.20

  run:
    - python
    - django-cross-origin ==0.9.0
    - jsonfield ==0.9.20

test:
  # Python imports
  imports:
    - data
    - data.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/mohawkhq/mohawk-data-platform
  license: BSD License
  summary: 'A Django app for editing and publishing arbitrary JSON data in a form-based environment.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
