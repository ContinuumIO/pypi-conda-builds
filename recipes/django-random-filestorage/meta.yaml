package:
  name: django-random-filestorage
  version: "0.1.0"

source:
  fn: django-random-filestorage-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-random-filestorage/django-random-filestorage-0.1.0.tar.gz
  md5: 82f32ffc0a8fa3268ce5fbdc47149502
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-random-filestorage = django-random-filestorage:main
    #
    # Would create an entry point called django-random-filestorage that calls django-random-filestorage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - randomfilestorage

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/erikr/django-random-filestorage
  license: BSD License
  summary: 'Django storage class that assigns random filenames to all stored files.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
