package:
  name: igor
  version: "0.2"

source:
  fn: igor-0.2.tar.gz
  url: https://pypi.python.org/packages/source/i/igor/igor-0.2.tar.gz
  md5: 06aed02684413eb099aa80fb9b43b497
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - igor = igor:main
    #
    # Would create an entry point called igor that calls igor.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - igor
    - igor.record

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://blog.tremily.us/posts/igor/
  license: GNU Lesser General Public License v3 or later (LGPLv3+)
  summary: 'igor: interface for reading binary IGOR files.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
