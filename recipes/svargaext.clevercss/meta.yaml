package:
  name: svargaext.clevercss
  version: "0.4"

source:
  fn: svargaext.clevercss-0.4.tar.gz
  url: https://pypi.python.org/packages/source/s/svargaext.clevercss/svargaext.clevercss-0.4.tar.gz
  md5: 5df9f06c9c7a2deebb0c87e82aab46c9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - svargaext.clevercss = svargaext.clevercss:main
    #
    # Would create an entry point called svargaext.clevercss that calls svargaext.clevercss.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - svarga
    - clevercss
    - opster

  run:
    - python
    - svarga
    - clevercss
    - opster

test:
  # Python imports
  imports:
    - svargaext.clevercss

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://hg.piranha.org.ua/svargaext.clevercss/
  license: BSD License
  summary: 'CleverCSS module for Svarga framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
