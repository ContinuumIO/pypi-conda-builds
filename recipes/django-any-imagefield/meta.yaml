package:
  name: django-any-imagefield
  version: "0.8.2"

source:
  fn: django-any-imagefield-0.8.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-any-imagefield/django-any-imagefield-0.8.2.tar.gz
  md5: 4b542e263ecc482ebfcdaabb8ee3a7c5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-any-imagefield = django-any-imagefield:main
    #
    # Would create an entry point called django-any-imagefield that calls django-any-imagefield.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - any_imagefield
    - any_imagefield.forms
    - any_imagefield.models
    - any_imagefield.models.backends

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/edoburu/django-any-imagefield
  license: Apache Software License
  summary: 'A switchable ImageField for third party Django applications'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
