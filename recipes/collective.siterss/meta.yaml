package:
  name: collective.siterss
  version: "0.4"

source:
  fn: collective.siterss-0.4.zip
  url: https://pypi.python.org/packages/source/c/collective.siterss/collective.siterss-0.4.zip
  md5: 0acc00369130196333e567dd8bdd518e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.siterss = collective.siterss:main
    #
    # Would create an entry point called collective.siterss that calls collective.siterss.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.siterss
    - collective.siterss.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/collective.siterss
  license: GPL
  summary: 'Use any Smart Folder as default RSS feed of Plone instance'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
