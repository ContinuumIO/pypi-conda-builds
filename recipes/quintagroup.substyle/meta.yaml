package:
  name: quintagroup.substyle
  version: "1.3"

source:
  fn: quintagroup.substyle-1.3.zip
  url: https://pypi.python.org/packages/source/q/quintagroup.substyle/quintagroup.substyle-1.3.zip
  md5: d14070f0a5a686e6909d86cb57c92875
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - quintagroup.substyle = quintagroup.substyle:main
    #
    # Would create an entry point called quintagroup.substyle that calls quintagroup.substyle.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - quintagroup
    - quintagroup.substyle
    - quintagroup.substyle.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/quintagroup/quintagroup.substyle
  license: GNU General Public License (GPL)
  summary: 'Styles for site subsections'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
