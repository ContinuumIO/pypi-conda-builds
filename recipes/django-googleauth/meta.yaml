package:
  name: django-googleauth
  version: "2.1"

source:
  fn: django-googleauth-2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-googleauth/django-googleauth-2.1.tar.gz
  md5: fcdeda45e548ce2c9da3209e3d7abb1f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-googleauth = django-googleauth:main
    #
    # Would create an entry point called django-googleauth that calls django-googleauth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyjwt ==0.4.1
    - requests ==2.5.1

  run:
    - python
    - pyjwt ==0.4.1
    - requests ==2.5.1

test:
  # Python imports
  imports:
    - googleauth

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jcarbaugh/django-googleauth
  license: BSD License
  summary: 'OAuth 2.0 authentication for Google and Google Apps accounts'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
