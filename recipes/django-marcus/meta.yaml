package:
  name: django-marcus
  version: "0.6.3"

source:
  fn: django-marcus-0.6.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-marcus/django-marcus-0.6.3.tar.gz
  md5: 08a695fca03251047873720cae6ffcd9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-marcus = django-marcus:main
    #
    # Would create an entry point called django-marcus that calls django-marcus.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django ==1.7.4
    - pytils >=0.2.3
    - pillow >=2.6.1
    - html5lib >=0.95
    - httplib2 >=0.7.4
    - python-openid >=2.2.5
    - markdown2 >=2.0.1
    - scipio
    - pingdjack
    - html2text >=3.200.3

  run:
    - python
    - django ==1.7.4
    - pytils >=0.2.3
    - pillow >=2.6.1
    - html5lib >=0.95
    - httplib2 >=0.7.4
    - python-openid >=2.2.5
    - markdown2 >=2.0.1
    - scipio
    - pingdjack
    - html2text >=3.200.3

test:
  # Python imports
  imports:
    - marcus
    - marcus.management
    - marcus.management.commands
    - marcus.migrations
    - marcus.templatetags
    - marcus.wordpress_importer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/adw0rd/marcus
  license: BSD License
  summary: 'Bilingual blog on Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
