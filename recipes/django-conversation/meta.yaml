package:
  name: django-conversation
  version: "1.2"

source:
  fn: django-conversation-1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-conversation/django-conversation-1.2.tar.gz
  md5: 530205a31204b3da982f6db4349a5429
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-conversation = django-conversation:main
    #
    # Would create an entry point called django-conversation that calls django-conversation.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - conversation
    - conversation.migrations
    - conversation.tests
    - conversation.tests.test_app

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bitmazk/django-conversation
  license: The MIT License
  summary: 'A reusable Django app that provides threaded conversations between users.\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
