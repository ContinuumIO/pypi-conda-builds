package:
  name: ineedpy2
  version: "1.01"

source:
  fn: ineedpy2-1.01.tar.gz
  url: https://pypi.python.org/packages/source/i/ineedpy2/ineedpy2-1.01.tar.gz
  md5: d9f5e775a5a54a718a285bd85bb33e2d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ineedpy2 = ineedpy2:main
    #
    # Would create an entry point called ineedpy2 that calls ineedpy2.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - ineedpy2

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/linsomniac/ineedpy2
  license: Python Software Foundation
  summary: 'Library to swap to newer python version on a multi-version system.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
