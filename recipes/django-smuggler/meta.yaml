package:
  name: django-smuggler
  version: "0.6.0"

source:
  fn: django-smuggler-0.6.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-smuggler/django-smuggler-0.6.0.tar.gz
  md5: cf6c460926d6348f17d33d2f3caf322f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-smuggler = django-smuggler:main
    #
    # Would create an entry point called django-smuggler that calls django-smuggler.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - smuggler
    - tests
    - tests.test_app
    - tests.test_app.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/semente/django-smuggler
  license: GNU Lesser General Public License v3 or later (LGPLv3+)
  summary: 'Pluggable application for Django that helps you to import/export fixtures via the administration interface'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
