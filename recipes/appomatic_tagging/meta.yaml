package:
  name: appomatic_tagging
  version: "0.0.30"

source:
  fn: appomatic_tagging-0.0.30.tar.gz
  url: https://pypi.python.org/packages/source/a/appomatic_tagging/appomatic_tagging-0.0.30.tar.gz
  md5: 41a2e038a929296c5ee288b52e029af9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - appomatic_tagging = appomatic_tagging:main
    #
    # Would create an entry point called appomatic_tagging that calls appomatic_tagging.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-tagging
    - appomaticcore ==0.0.30

  run:
    - python
    - django-tagging
    - appomaticcore ==0.0.30

test:
  # Python imports
  imports:
    - appomatic_tagging

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/django-tagging/
  license: GPL
  summary: 'A generic tagging application for Django projects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
