package:
  name: anemone
  version: "0.0.1"

source:
  fn: Anemone-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/A/Anemone/Anemone-0.0.1.tar.gz
  md5: adce277d3d431e84c6be57610d657148
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - anemone = anemone:main
    #
    # Would create an entry point called anemone that calls anemone.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - wxpython
    - pyzmq
    - matplotlib

  run:
    - python
    - wxpython
    - pyzmq
    - matplotlib

test:
  # Python imports
  imports:
    - anemone

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/trlandet/anemone
  license: Apache Software License
  summary: 'Monitor long running analyses by remote plotting of selected variables'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
