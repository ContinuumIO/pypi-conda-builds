package:
  name: audio-offset-finder
  version: "0.3.0"

source:
  fn: audio-offset-finder-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/a/audio-offset-finder/audio-offset-finder-0.3.0.tar.gz
  md5: 87601d66b5ddb2806cee088526905ed1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - audio-offset-finder = audio-offset-finder:main
    #
    # Would create an entry point called audio-offset-finder that calls audio-offset-finder.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - scipy >=0.12.0
    - numpy
    - scikits.talkbox

  run:
    - python
    - scipy >=0.12.0
    - numpy
    - scikits.talkbox

test:
  # Python imports
  imports:
    - audio_offset_finder

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bbcrd/audio-offset-finder
  license: Apache License 2.0
  summary: 'Find the offset of an audio file within another audio file'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
