package:
  name: django-memento
  version: "0.1"

source:
  fn: django-memento-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-memento/django-memento-0.1.tar.gz
  md5: 2679743371f7ef28663ef9e76a9ba329
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-memento = django-memento:main
    #
    # Would create an entry point called django-memento that calls django-memento.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - memento
    - memento.migrations
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://relekang.github.io/django-memento
  license: UNKNOWN
  summary: 'Simple reusable django app for logging events within a django project'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
