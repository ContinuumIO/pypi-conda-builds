package:
  name: django-admin-ext
  version: "1.0.2"

source:
  fn: django-admin-ext-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-admin-ext/django-admin-ext-1.0.2.tar.gz
  md5: 5ad4a408468782ed3e4f41a55ff4b150
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-admin-ext = django-admin-ext:main
    #
    # Would create an entry point called django-admin-ext that calls django-admin-ext.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django <1.7

  run:
    - python
    - django <1.7

test:
  # Python imports
  imports:
    - djadmin_ext

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/imtapps/django-admin-ext
  license: BSD License
  summary: "Extensions to Django's admin site to add an ajax view."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
