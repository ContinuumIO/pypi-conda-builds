package:
  name: imageportlet
  version: "1.1"

source:
  fn: imageportlet-1.1.zip
  url: https://pypi.python.org/packages/source/i/imageportlet/imageportlet-1.1.zip
  md5: cb5b3ab07604d5afec6c23a78a7783cb
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - imageportlet = imageportlet:main
    #
    # Would create an entry point called imageportlet that calls imageportlet.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - five.grok
    - z3c.jbot
    - plone.namedfile
    - plone.formwidget.namedfile

  run:
    - python
    - setuptools
    - five.grok
    - z3c.jbot
    - plone.namedfile
    - plone.formwidget.namedfile

test:
  # Python imports
  imports:
    - imageportlet

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://opensourcehacker.com
  license: GPL2
  summary: 'imageportlet add-on provides a portlet for Plone CMS for easily add images, banners and carousels around the content on your site'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
