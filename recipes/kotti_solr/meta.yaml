package:
  name: kotti_solr
  version: "0.1.1"

source:
  fn: kotti_solr-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/k/kotti_solr/kotti_solr-0.1.1.tar.gz
  md5: cf857c7c8b663d81680bc969bbc5d4d4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kotti_solr = kotti_solr:main
    #
    # Would create an entry point called kotti_solr that calls kotti_solr.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - httplib2
    - kotti
    - lxml
    - sunburnt
    - pytest
    - pytest-cov

  run:
    - python
    - httplib2
    - kotti
    - lxml
    - sunburnt
    - pytest
    - pytest-cov

test:
  # Python imports
  imports:
    - kotti_solr

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/teixas/kotti_solr
  license: BSD-derived (http://www.repoze.org/LICENSE.txt)
  summary: 'Solr integration for Kotti'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
