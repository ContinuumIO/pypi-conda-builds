package:
  name: jekyll2nikola
  version: "0-0-1"

source:
  fn: jekyll2nikola-0-0-1.tar.gz
  url: https://pypi.python.org/packages/source/j/jekyll2nikola/jekyll2nikola-0-0-1.tar.gz
  md5: 4335dafd2d256f79615b1bbd29950dd8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jekyll2nikola = jekyll2nikola:main
    #
    # Would create an entry point called jekyll2nikola that calls jekyll2nikola.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyyaml
    - python-dateutil

  run:
    - python
    - pyyaml
    - python-dateutil

test:
  # Python imports
  imports:
    - jekyll2nikola

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/magmax/jekyll2nikola
  license: MIT License
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
