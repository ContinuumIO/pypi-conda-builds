package:
  name: zest.specialpaste
  version: "1.2"

source:
  fn: zest.specialpaste-1.2.zip
  url: https://pypi.python.org/packages/source/z/zest.specialpaste/zest.specialpaste-1.2.zip
  md5: d8eb7612bbcebf8a60cfa883019f76e1
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zest.specialpaste = zest.specialpaste:main
    #
    # Would create an entry point called zest.specialpaste that calls zest.specialpaste.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.cmfplone

  run:
    - python
    - setuptools
    - setuptools
    - products.cmfplone

test:
  # Python imports
  imports:
    - zest
    - zest.specialpaste
    - zest.specialpaste.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/zestsoftware/zest.specialpaste
  license: GPL
  summary: 'Special paste action with extra options'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
