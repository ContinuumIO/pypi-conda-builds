package:
  name: django-urlalternatives
  version: "0.1.0"

source:
  fn: django-urlalternatives-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-urlalternatives/django-urlalternatives-0.1.0.tar.gz
  md5: 631527df9e00d126cd4492fe324b067d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-urlalternatives = django-urlalternatives:main
    #
    # Would create an entry point called django-urlalternatives that calls django-urlalternatives.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - urlalternatives

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://gw.tnode.com/0483-Django/
  license: GNU General Public License (GPL)
  summary: 'Django URL alternatives provides a way for dispatching one URL pattern to the first alternative view (callback function) in a list that returns success.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
