package:
  name: tornado_circus
  version: "0.0.1"

source:
  fn: tornado_circus-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/t/tornado_circus/tornado_circus-0.0.1.tar.gz
  md5: 5457cd3bbecd4d4b68ddcf00ea361039
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tornado_circus = tornado_circus:main
    #
    # Would create an entry point called tornado_circus that calls tornado_circus.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - tornado

  run:
    - python
    - tornado

test:
  # Python imports
  imports:
    - tornado_circus

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Lothiraldan/tornado_circus
  license: BSD
  summary: 'A tornado application compatible with circus socket'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
