package:
  name: django-fhadmin
  version: "1.3.0"

source:
  fn: django-fhadmin-1.3.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-fhadmin/django-fhadmin-1.3.0.tar.gz
  md5: d3cdbf58cbf891fb0921aae68256cdb3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-fhadmin = django-fhadmin:main
    #
    # Would create an entry point called django-fhadmin that calls django-fhadmin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - fhadmin
    - fhadmin.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/feinheit/django-fhadmin/
  license: BSD License
  summary: 'Modifies the stock Django-Administration interface to fit our ideas a little bit better.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
