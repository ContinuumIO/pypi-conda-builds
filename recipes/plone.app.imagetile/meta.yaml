package:
  name: plone.app.imagetile
  version: "1.0"

source:
  fn: plone.app.imagetile-1.0.tar.gz
  url: https://pypi.python.org/packages/source/p/plone.app.imagetile/plone.app.imagetile-1.0.tar.gz
  md5: e53ca650633290dd025193fd4bbffbd1
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.app.imagetile = plone.app.imagetile:main
    #
    # Would create an entry point called plone.app.imagetile that calls plone.app.imagetile.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zope.schema
    - zope.i18nmessageid
    - plone.directives.form
    - plone.tiles
    - plone.app.tiles

  run:
    - python
    - setuptools
    - setuptools
    - zope.schema
    - zope.i18nmessageid
    - plone.directives.form
    - plone.tiles
    - plone.app.tiles

test:
  # Python imports
  imports:
    - plone
    - plone.app
    - plone.app.imagetile
    - plone.app.imagetile.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/plone/plone.app.imagetile
  license: GPL
  summary: 'Image tile for deco UI'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
