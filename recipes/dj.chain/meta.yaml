package:
  name: dj.chain
  version: "0.9.2"

source:
  fn: dj.chain-0.9.2.tar.gz
  url: https://pypi.python.org/packages/source/d/dj.chain/dj.chain-0.9.2.tar.gz
  md5: 539230aa498f8d987cc6112c5684bb26
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dj.chain = dj.chain:main
    #
    # Would create an entry point called dj.chain that calls dj.chain.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - six
    - null ==0.6.1

  run:
    - python
    - django
    - six
    - null ==0.6.1

test:
  # Python imports
  imports:
    - dj
    - dj._chaintestproject
    - dj._chaintestproject.app
    - dj.chain

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ambv/dj.chain/
  license: MIT License
  summary: 'An object that enables chaining multiple iterables to serve them lazily as a queryset-compatible object.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
