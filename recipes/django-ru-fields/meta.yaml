package:
  name: django-ru-fields
  version: "0.1.0"

source:
  fn: django-ru-fields-0.1.0.zip
  url: https://pypi.python.org/packages/source/d/django-ru-fields/django-ru-fields-0.1.0.zip
  md5: 1bddb094cf4ef10c98a0d6ea9dce4337
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-ru-fields = django-ru-fields:main
    #
    # Would create an entry point called django-ru-fields that calls django-ru-fields.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_ru_fields

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/suvitorg/django-ru-fields
  license: UNKNOWN
  summary: 'specific russian fields'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
