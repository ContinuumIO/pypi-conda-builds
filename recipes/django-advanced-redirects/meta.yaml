package:
  name: django-advanced-redirects
  version: "0.9.5"

source:
  fn: django-advanced-redirects-0.9.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-advanced-redirects/django-advanced-redirects-0.9.5.tar.gz
  md5: a542e203db54c6d874f37aad2ad50c18
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-advanced-redirects = django-advanced-redirects:main
    #
    # Would create an entry point called django-advanced-redirects that calls django-advanced-redirects.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - advanced_redirects

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://eressler.github.io/django-advanced-redirects/
  license: UNKNOWN
  summary: 'Advanced redirect management for Django applications.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
