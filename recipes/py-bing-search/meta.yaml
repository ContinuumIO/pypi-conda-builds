package:
  name: py-bing-search
  version: "0.1.5"

source:
  fn: py-bing-search-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/p/py-bing-search/py-bing-search-0.1.5.tar.gz
  md5: 416f06c67ddf04d1d8e00db501721a9d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - py-bing-search = py-bing-search:main
    #
    # Would create an entry point called py-bing-search that calls py-bing-search.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - py_bing_search

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tristantao/py-bing-search
  license: MIT License
  summary: 'A simple lightweight python wrapper for the Azure Bing Search API.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
