package:
  name: tangled.auth
  version: "0.1a4"

source:
  fn: tangled.auth-0.1a4.tar.gz
  url: https://pypi.python.org/packages/source/t/tangled.auth/tangled.auth-0.1a4.tar.gz
  md5: 49df289af2f2caaf9336954a2a72e929
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tangled.auth = tangled.auth:main
    #
    # Would create an entry point called tangled.auth that calls tangled.auth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - tangled.web >=0.1a9

  run:
    - python
    - setuptools
    - tangled.web >=0.1a9

test:
  # Python imports
  imports:
    - tangled
    - tangled.auth
    - tangled.auth.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://tangledframework.org/
  license: MIT License
  summary: 'Tangled auth integration'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
