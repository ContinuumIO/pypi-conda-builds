package:
  name: stereovision
  version: "1.0.1"

source:
  fn: StereoVision-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/S/StereoVision/StereoVision-1.0.1.tar.gz
  md5: 961100e904cc51f0965879656cef3556
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - stereovision = stereovision:main
    #
    # Would create an entry point called stereovision that calls stereovision.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - stereovision

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://erget.github.com/StereoVision
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'Library and utilities for 3d reconstruction from stereo cameras.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
