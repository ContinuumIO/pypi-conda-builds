package:
  name: am2302-rpi
  version: "1.1.2"

source:
  fn: AM2302-rpi-1.1.2.tar.gz
  url: https://pypi.python.org/packages/source/A/AM2302-rpi/AM2302-rpi-1.1.2.tar.gz
  md5: 9e9d3c491fa5bedc906b7a1b473d28db
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - am2302-rpi = am2302-rpi:main
    #
    # Would create an entry point called am2302-rpi that calls am2302-rpi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/apexskier/AM2302
  license: UNKNOWN
  summary: 'Drive an AM2302 temperature sensor with a raspberry pi.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
