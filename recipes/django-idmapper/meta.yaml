package:
  name: django-idmapper
  version: "0.2"

source:
  fn: django-idmapper-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-idmapper/django-idmapper-0.2.tar.gz
  md5: 82cb42a01c58e5603e81cbed937bbd9e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-idmapper = django-idmapper:main
    #
    # Would create an entry point called django-idmapper that calls django-idmapper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1

  run:
    - python
    - django >=1

test:
  # Python imports
  imports:
    - idmapper

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/dcramer/django-idmapper
  license: UNKNOWN
  summary: 'An identify mapper for the Django ORM'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
