package:
  name: django-reporter
  version: "0.1"

source:
  fn: django-reporter-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-reporter/django-reporter-0.1.tar.gz
  md5: ad2503b144a87a8befe1062acd3358f7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-reporter = django-reporter:main
    #
    # Would create an entry point called django-reporter that calls django-reporter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - reporter
    - reporter.management
    - reporter.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/pegasus/django-reporter
  license: BSD License
  summary: 'Custom email-based reports for any Django project.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
