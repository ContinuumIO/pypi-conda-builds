package:
  name: fedimg
  version: "0.6"

source:
  fn: fedimg-0.6.tar.gz
  url: https://pypi.python.org/packages/source/f/fedimg/fedimg-0.6.tar.gz
  md5: 5f0df527141dd0d110ca6f089563302b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fedimg = fedimg:main
    #
    # Would create an entry point called fedimg that calls fedimg.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fedmsg
    - apache-libcloud
    - paramiko

  run:
    - python
    - setuptools
    - fedmsg
    - apache-libcloud
    - paramiko

test:
  # Python imports
  imports:
    - fedimg
    - fedimg.services

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/fedora-infra/fedimg
  license: GNU Affero General Public License v3 or later (AGPLv3+)
  summary: 'Automatically upload Fedora Cloud images to cloud providers'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
