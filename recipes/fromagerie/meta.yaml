package:
  name: fromagerie
  version: "0.1a1"

source:
  fn: fromagerie-0.1a1.tar.gz
  url: https://pypi.python.org/packages/source/f/fromagerie/fromagerie-0.1a1.tar.gz
  md5: 44aecde3c50a33e624ee54d6a36e278f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fromagerie = fromagerie:main
    #
    # Would create an entry point called fromagerie that calls fromagerie.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - fromagerie

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/jeffkistler/fromagerie
  license: BSD License
  summary: 'A pluggable PyPI for Django projects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
