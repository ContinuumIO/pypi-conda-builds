package:
  name: bitcoin-python
  version: "0.3"

source:
  fn: bitcoin-python-0.3.tar.gz
  url: https://pypi.python.org/packages/source/b/bitcoin-python/bitcoin-python-0.3.tar.gz
  md5: 4ee255efecec5847b3a3b8f4a69ead87
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bitcoin-python = bitcoin-python:main
    #
    # Would create an entry point called bitcoin-python that calls bitcoin-python.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - bitcoinrpc

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://laanwj.github.com/bitcoin-python/doc/
  license: MIT License
  summary: 'Friendly Bitcoin JSON-RPC API binding for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
