package:
  name: membrete
  version: "0.2.0"

source:
  fn: membrete-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/m/membrete/membrete-0.2.0.tar.gz
  md5: 04a505fa170ec3811acd3f56f1a5d7e4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - membrete = membrete:main
    #
    # Would create an entry point called membrete that calls membrete.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - membrete
    - membrete.conf

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://gonzalodelgado.com.ar/codigo/membrete
  license: GNU Affero General Public License v3
  summary: 'Contact form app for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
