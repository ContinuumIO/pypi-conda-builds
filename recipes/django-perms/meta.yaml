package:
  name: django-perms
  version: "1.1.0"

source:
  fn: django-perms-1.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-perms/django-perms-1.1.0.tar.gz
  md5: def4e15861fa0de9fa3df93f5b453320
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-perms = django-perms:main
    #
    # Would create an entry point called django-perms that calls django-perms.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - permissions
    - permissions.templatetags
    - permissions.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/PSU-OIT-ARC/django-perms
  license: UNKNOWN
  summary: 'Syntactic sugar for handling permission functions in views, templates and in code'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
