package:
  name: ufo2otf
  version: "0.2.2"

source:
  fn: ufo2otf-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/u/ufo2otf/ufo2otf-0.2.2.tar.gz
  md5: ec88afd78b799a9396cb6a6fe60a7799
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ufo2otf = ufo2otf:main
    #
    # Would create an entry point called ufo2otf that calls ufo2otf.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - ufo2otf

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://packages.python.org/an_example_pypi_project
  license: BSD License
  summary: 'Take UFO font sources and generate OTF\xe2\x80\x99s and webfonts'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
