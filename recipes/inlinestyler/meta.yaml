package:
  name: inlinestyler
  version: "0.2.0"

source:
  fn: inlinestyler-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/i/inlinestyler/inlinestyler-0.2.0.tar.gz
  md5: 2219fadc69ba09cdb89c5c8dac1485f4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - inlinestyler = inlinestyler:main
    #
    # Would create an entry point called inlinestyler that calls inlinestyler.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cssutils
    - lxml

  run:
    - python
    - cssutils
    - lxml

test:
  # Python imports
  imports:
    - inlinestyler

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/dlanger/inlinestyler
  license: BSD License
  summary: 'Inlines external CSS into HTML elements.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
