package:
  name: wand
  version: "0.4.0"

source:
  fn: Wand-0.4.0.tar.gz
  url: https://pypi.python.org/packages/source/W/Wand/Wand-0.4.0.tar.gz
  md5: 3aa64a51e91f74451ad7b09a2811327c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - wand = wand:main
    #
    # Would create an entry point called wand that calls wand.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sphinx >=1.0

  run:
    - python
    - sphinx >=1.0

test:
  # Python imports
  imports:
    - wand

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

requires:
    - memory_profiler >= 0.27
    - psutil >= 1.0.1
    - pytest >= 2.3.0
    - pytest-xdist >= 1.8
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://wand-py.org/
  license: MIT License
  summary: 'Ctypes-based simple MagickWand API binding for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
