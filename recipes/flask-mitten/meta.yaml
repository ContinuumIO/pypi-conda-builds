package:
  name: flask-mitten
  version: "0.2.1"

source:
  fn: Flask-Mitten-0.2.1.zip
  url: https://pypi.python.org/packages/source/F/Flask-Mitten/Flask-Mitten-0.2.1.zip
  md5: 1ac8311a863b560d441c8e423f05166f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-mitten = flask-mitten:main
    #
    # Would create an entry point called flask-mitten that calls flask-mitten.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - flask-csrf
    - flask-kvsession

  run:
    - python
    - flask
    - flask-csrf
    - flask-kvsession

test:
  # Python imports
  imports:
    - flaskext

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lanius/flask-mitten
  license: BSD License
  summary: 'Adds security functions to Flask applications for preventing some of the basic threats.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
