package:
  name: collective.arboreal
  version: "2.0"

source:
  fn: collective.arboreal-2.0.zip
  url: https://pypi.python.org/packages/source/c/collective.arboreal/collective.arboreal-2.0.zip
  md5: e10938cf44121c818080b90ebe26673c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.arboreal = collective.arboreal:main
    #
    # Would create an entry point called collective.arboreal that calls collective.arboreal.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.i18n

  run:
    - python
    - setuptools
    - setuptools
    - plone.i18n

test:
  # Python imports
  imports:
    - collective
    - collective.arboreal

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/collective.arboreal
  license: GNU General Public License (GPL)
  summary: 'Arboreal is a tool which lets you manage multiple trees.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
