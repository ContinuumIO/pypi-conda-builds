package:
  name: an_example_pypi_project
  version: "0.0.5"

source:
  fn: an_example_pypi_project-0.0.5.zip
  url: https://pypi.python.org/packages/source/a/an_example_pypi_project/an_example_pypi_project-0.0.5.zip
  md5: 76854a5edcba4590ce4328ff0fbc22ac
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - an_example_pypi_project = an_example_pypi_project:main
    #
    # Would create an entry point called an_example_pypi_project that calls an_example_pypi_project.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - an_example_pypi_project
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://packages.python.org/an_example_pypi_project
  license: BSD License
  summary: 'An demonstration of how to create, document, and publish to the cheese shop a5 pypi.org.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
