package:
  name: plone.directives.dexterity
  version: "1.0.2"

source:
  fn: plone.directives.dexterity-1.0.2.zip
  url: https://pypi.python.org/packages/source/p/plone.directives.dexterity/plone.directives.dexterity-1.0.2.zip
  md5: 713b87644e3591b60b4a8ebd52987477
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.directives.dexterity = plone.directives.dexterity:main
    #
    # Would create an entry point called plone.directives.dexterity that calls plone.directives.dexterity.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - five.grok
    - plone.dexterity >=1.0.1
    - plone.directives.form >=1.0b3
    - zope.deferredimport

  run:
    - python
    - setuptools
    - setuptools
    - five.grok
    - plone.dexterity >=1.0.1
    - plone.directives.form >=1.0b3
    - zope.deferredimport

test:
  # Python imports
  imports:
    - plone
    - plone.directives
    - plone.directives.dexterity
    - plone.directives.dexterity.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/dexterity
  license: LGPL
  summary: 'Grok-like directives for creating Dexterity content'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
