package:
  name: django-djobberbase
  version: "0.1.1"

source:
  fn: django-djobberbase-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-djobberbase/django-djobberbase-0.1.1.tar.gz
  md5: 8448fc2949ac0503fd5f993c375f4371
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-djobberbase = django-djobberbase:main
    #
    # Would create an entry point called django-djobberbase that calls django-djobberbase.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - djobberbase
    - djobberbase.conf
    - djobberbase.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/wtrevino/django-djobberbase
  license: MIT License
  summary: 'A clone of the jobberBase job board platform written using the Django framework.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
