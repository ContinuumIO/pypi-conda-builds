package:
  name: django-perimeter
  version: "0.5.1"

source:
  fn: django-perimeter-0.5.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-perimeter/django-perimeter-0.5.1.tar.gz
  md5: 9a63a9486f50f2180adfcee244d427a0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-perimeter = django-perimeter:main
    #
    # Would create an entry point called django-perimeter that calls django-perimeter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - perimeter
    - perimeter.management.commands
    - perimeter.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/yunojuno/django-perimeter
  license: BSD License
  summary: 'Site-wide perimeter access control for Django projects.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
