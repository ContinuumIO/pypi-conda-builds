package:
  name: collective.linguaanalytics
  version: "1.0"

source:
  fn: collective.linguaanalytics-1.0.zip
  url: https://pypi.python.org/packages/source/c/collective.linguaanalytics/collective.linguaanalytics-1.0.zip
  md5: cecfc10dbe08f5de12abbd7016e47b1c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.linguaanalytics = collective.linguaanalytics:main
    #
    # Would create an entry point called collective.linguaanalytics that calls collective.linguaanalytics.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.linguaplone
    - collective.googleanalytics

  run:
    - python
    - setuptools
    - setuptools
    - products.linguaplone
    - collective.googleanalytics

test:
  # Python imports
  imports:
    - collective
    - collective.linguaanalytics
    - collective.linguaanalytics.tests
    - collective.linguaanalytics.upgrades
    - collective.linguaanalytics.viewlets

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/toutpt/collective.linguaanalytics
  license: GPL
  summary: 'Google Analytics specialized for MultiLanguage sites'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
