package:
  name: django-async-gt
  version: "0.6.2"

source:
  fn: django-async-gt-0.6.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-async-gt/django-async-gt-0.6.2.tar.gz
  md5: 45e96875541b92fe0e2c49118203b3e8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-async-gt = django-async-gt:main
    #
    # Would create an entry point called django-async-gt that calls django-async-gt.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - async_image_save

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/gterzian/django_async.git
  license: BSD License
  summary: 'a simple app to asynchronously upload images with Django, using Celery'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
