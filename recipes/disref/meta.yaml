package:
  name: disref
  version: "0.2"

source:
  fn: disref-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/disref/disref-0.2.tar.gz
  md5: f6f762fbb1566f5e833212ae320cc573
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - disref = disref:main
    #
    # Would create an entry point called disref that calls disref.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sherlock ==0.3.0
    - pytz ==2014.10

  run:
    - python
    - sherlock ==0.3.0
    - pytz ==2014.10

test:
  # Python imports
  imports:
    - disref

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.github.com/buzzfeed/disref
  license: UNKNOWN
  summary: 'Provides easy, fault tolerant, distributed references with redis as a backend.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
