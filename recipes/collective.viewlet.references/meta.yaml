package:
  name: collective.viewlet.references
  version: "0.8"

source:
  fn: collective.viewlet.references-0.8.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.viewlet.references/collective.viewlet.references-0.8.tar.gz
  md5: b9bc084e4f89ba9c4308f1f74115b333
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.viewlet.references = collective.viewlet.references:main
    #
    # Would create an entry point called collective.viewlet.references that calls collective.viewlet.references.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.viewlet
    - collective.viewlet.references
    - collective.viewlet.references.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://svn.v2.nl/plone/collective.viewlet.references
  license: GPL
  summary: 'Viewlet that shows all the references to the current article including back references'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
