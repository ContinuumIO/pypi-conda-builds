package:
  name: django-multisite
  version: "0.4.1"

source:
  fn: django-multisite-0.4.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-multisite/django-multisite-0.4.1.tar.gz
  md5: 2528780b25069471ea697405a2f7fd83
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-multisite = django-multisite:main
    #
    # Would create an entry point called django-multisite that calls django-multisite.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django >=1.3
    - tldextract >=1.1.3

  run:
    - python
    - django >=1.3
    - tldextract >=1.1.3

test:
  # Python imports
  imports:
    - multisite
    - multisite.management
    - multisite.management.commands
    - multisite.migrations
    - multisite.template
    - multisite.template.loaders

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/ecometrica/django-multisite
  license: BSD License
  summary: 'Serve multiple sites from a single Django application'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
