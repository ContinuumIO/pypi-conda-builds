package:
  name: sphinxcontrib-srclinks
  version: "0.2.1"

source:
  fn: sphinxcontrib-srclinks-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/s/sphinxcontrib-srclinks/sphinxcontrib-srclinks-0.2.1.tar.gz
  md5: c12bbe47f5e2f1bf4177866e828746f9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sphinxcontrib-srclinks = sphinxcontrib-srclinks:main
    #
    # Would create an entry point called sphinxcontrib-srclinks that calls sphinxcontrib-srclinks.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sphinx >=0.6

  run:
    - python
    - sphinx >=0.6

test:
  # Python imports
  imports:
    - sphinxcontrib
    - sphinxcontrib.srclinks

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/westurner/sphinxcontrib-srclinks
  license: BSD License
  summary: 'Add source, edit, history, annotate links to GitHub or BitBucket'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
