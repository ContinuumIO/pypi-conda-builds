package:
  name: iconfonts
  version: "0.0.3"

source:
  fn: IconFonts-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/I/IconFonts/IconFonts-0.0.3.tar.gz
  md5: 47a9de477bf498285997601834908ada
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - iconfonts = iconfonts:main
    #
    # Would create an entry point called iconfonts that calls iconfonts.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - iconfonts
    - iconfonts.django

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/joke2k/iconfonts
  license: MIT License
  summary: 'This package provides a toolkit to render icon-font glyphs.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
