package:
  name: images2video
  version: "0.3"

source:
  fn: images2video-0.3.tar.gz
  url: https://pypi.python.org/packages/source/i/images2video/images2video-0.3.tar.gz
  md5: 12a07240bf8052c66ab1c5f40f5441fe
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - images2video = images2video:main
    #
    # Would create an entry point called images2video that calls images2video.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy

  run:
    - python
    - numpy

test:
  # Python imports
  imports:
    - images2video

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/messense/images2video
  license: MIT License
  summary: 'Python images to video library using OpenCV'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
