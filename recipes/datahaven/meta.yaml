package:
  name: datahaven
  version: "rev8784"

source:
  fn: datahaven-rev8784.tar.gz
  url: https://pypi.python.org/packages/source/d/datahaven/datahaven-rev8784.tar.gz
  md5: daa6249cbf55b6c2fea7f9c920df77ac
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - datahaven = datahaven:main
    #
    # Would create an entry point called datahaven that calls datahaven.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - datahaven
    - datahaven.cspace
    - datahaven.cspace.dht
    - datahaven.cspace.main
    - datahaven.cspace.network
    - datahaven.cspace.util
    - datahaven.forms
    - datahaven.lib
    - datahaven.lib.shtoom
    - datahaven.ncrypt0
    - datahaven.nitro
    - datahaven.p2p

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://datahaven.net
  license: Data_Helper_Functions
  summary: 'Distributed storage network'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
