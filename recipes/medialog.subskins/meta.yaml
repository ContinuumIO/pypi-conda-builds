package:
  name: medialog.subskins
  version: "4.2.3"

source:
  fn: medialog.subskins-4.2.3.tar.gz
  url: https://pypi.python.org/packages/source/m/medialog.subskins/medialog.subskins-4.2.3.tar.gz
  md5: a5a159b91bf437ea4d0995d54ff70a96
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - medialog.subskins = medialog.subskins:main
    #
    # Would create an entry point called medialog.subskins that calls medialog.subskins.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plonetheme.classic
    - products.plonesubskins >=4.7.5
    - pillow >=1.7.6

  run:
    - python
    - setuptools
    - setuptools
    - plonetheme.classic
    - products.plonesubskins >=4.7.5
    - pillow >=1.7.6

test:
  # Python imports
  imports:
    - medialog
    - medialog.subskins
    - medialog.subskins.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/espenmn/medialog.subskins/
  license: GPL
  summary: 'An installable theme and theming tool for Plone 4'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
