package:
  name: codegenloader
  version: "0.2"

source:
  fn: codegenloader-0.2.tar.gz
  url: https://pypi.python.org/packages/source/c/codegenloader/codegenloader-0.2.tar.gz
  md5: 7b9dbfa2a2db2ccc4471a2902ca19aec
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - codegenloader = codegenloader:main
    #
    # Would create an entry point called codegenloader that calls codegenloader.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - codegenloader
    - codegenloader.test
    - codegenloader.test.proto

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bdarnell/codegenloader
  license: codegrapher
  summary: None

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
