package:
  name: django-mongo-cache
  version: "0.1"

source:
  fn: django-mongo-cache-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-mongo-cache/django-mongo-cache-0.1.tar.gz
  md5: 43c024379cbc1cdb7a1b4c781e12526f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-mongo-cache = django-mongo-cache:main
    #
    # Would create an entry point called django-mongo-cache that calls django-mongo-cache.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - mongo_cache

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/okuznetsov/django-mongo-cache
  license: MIT License
  summary: 'Cache backend for django, using MongoDB and gevent connection pool'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
