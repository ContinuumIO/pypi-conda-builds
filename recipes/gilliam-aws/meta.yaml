package:
  name: gilliam-aws
  version: "0.1"

source:
  fn: gilliam-aws-0.1.tar.gz
  url: https://pypi.python.org/packages/source/g/gilliam-aws/gilliam-aws-0.1.tar.gz
  md5: a9583ca637ab9df7101e3698e8ba3c2c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gilliam-aws = gilliam-aws:main
    #
    # Would create an entry point called gilliam-aws that calls gilliam-aws.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - boto
    - fabric
    - requests
    - gilliam-py
    - gilliam-cli

  run:
    - python
    - setuptools
    - boto
    - fabric
    - requests
    - gilliam-py
    - gilliam-cli

test:
  # Python imports
  imports:
    - gilliam_aws

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/gilliam/
  license: Apache 2.0
  summary: 'Command-line tool for running Gilliam on AWS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
