package:
  name: algoliasearch
  version: "1.5.4"

source:
  fn: algoliasearch-1.5.4.tar.gz
  url: https://pypi.python.org/packages/source/a/algoliasearch/algoliasearch-1.5.4.tar.gz
  md5: 3d2c25ea4c563544cf4a52f64d55e17c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - algoliasearch = algoliasearch:main
    #
    # Would create an entry point called algoliasearch that calls algoliasearch.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - urllib3 >=1.8.2

  run:
    - python
    - urllib3 >=1.8.2

test:
  # Python imports
  imports:
    - algoliasearch

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/algolia/algoliasearch-client-python
  license: UNKNOWN
  summary: 'Algolia Search API Client for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
