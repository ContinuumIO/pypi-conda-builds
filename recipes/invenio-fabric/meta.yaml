package:
  name: invenio-fabric
  version: "0.2.8"

source:
  fn: invenio-fabric-0.2.8.tar.gz
  url: https://pypi.python.org/packages/source/i/invenio-fabric/invenio-fabric-0.2.8.tar.gz
  md5: aeb166a854c4520900de8a7991946eed
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - invenio-fabric = invenio-fabric:main
    #
    # Would create an entry point called invenio-fabric that calls invenio-fabric.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - distribute
    - fabric >=1.5
    - jinja2
    - virtualenvwrapper
    - sphinx

  run:
    - python
    - distribute
    - fabric >=1.5
    - jinja2
    - virtualenvwrapper
    - sphinx

test:
  # Python imports
  imports:
    - inveniofab

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lnielsen-cern/invenio-fabric
  license: GPL
  summary: 'Fabric tasks for working with Invenio repository software'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
