package:
  name: adpy
  version: "0.12.alpha"

source:
  fn: ADPY-0.12.alpha.tar.gz
  url: https://pypi.python.org/packages/source/A/ADPY/ADPY-0.12.alpha.tar.gz
  md5: 75734f44907def64ad75093711c8ac02
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - adpy = adpy:main
    #
    # Would create an entry point called adpy that calls adpy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - ADPY
    - ADPY.ADFUN
    - ADPY.SOLVER
    - adpy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/zwenson/ADPY
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'ADPY is a Python library for algorithmic differentiation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
