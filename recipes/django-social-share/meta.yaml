package:
  name: django-social-share
  version: "0.3.0"

source:
  fn: django-social-share-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-social-share/django-social-share-0.3.0.tar.gz
  md5: 3c6fe2981f9f3abbfd7569202e341288
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-social-share = django-social-share:main
    #
    # Would create an entry point called django-social-share that calls django-social-share.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_social_share
    - django_social_share.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/fcurella/django-social-share
  license: MIT License
  summary: "Templatetags for 'tweet this' and 'share on facebook'"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
