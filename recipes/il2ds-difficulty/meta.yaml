package:
  name: il2ds-difficulty
  version: "1.0.0"

source:
  fn: il2ds-difficulty-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/i/il2ds-difficulty/il2ds-difficulty-1.0.0.tar.gz
  md5: e0aca4e62f075bcd849d50b3ca7f9e7e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - il2ds-difficulty = il2ds-difficulty:main
    #
    # Would create an entry point called il2ds-difficulty that calls il2ds-difficulty.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - nose ==1.3.0

  run:
    - python
    - nose ==1.3.0

test:
  # Python imports
  imports:
    - il2ds_difficulty

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/IL2HorusTeam/il2ds-difficulty/
  license: BSD License
  summary: 'Convert integer value of IL-2 DS difficulty into informative dictionary.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
