package:
  name: docreader
  version: "1.0"

source:
  fn: docreader-1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/docreader/docreader-1.0.tar.gz
  md5: 7d180d6c376b2206d0d128e04cc55005
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - docreader = docreader:main
    #
    # Would create an entry point called docreader that calls docreader.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - docreader

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/CBWhiz/docreader
  license: MIT License
  summary: 'A better csv.DictReader (supporting UTF-8 and type conversion)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
