package:
  name: cinemate
  version: "0.2.1"

source:
  fn: cinemate-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/c/cinemate/cinemate-0.2.1.tar.gz
  md5: d2066d9a53be696d93b34da5989fbf1c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cinemate = cinemate:main
    #
    # Would create an entry point called cinemate that calls cinemate.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyyaml
    - requests
    - six

  run:
    - python
    - pyyaml
    - requests
    - six

test:
  # Python imports
  imports:
    - cinemate

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Pentusha/cinemate/
  license: BSD License
  summary: 'cinemate.cc api'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
