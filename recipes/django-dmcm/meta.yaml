package:
  name: django-dmcm
  version: "1.1.1"

source:
  fn: django-dmcm-1.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-dmcm/django-dmcm-1.1.1.tar.gz
  md5: 2f2357b03137efb0c4544aa7cac9c270
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-dmcm = django-dmcm:main
    #
    # Would create an entry point called django-dmcm that calls django-dmcm.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django ==1.8
    - django-braces ==1.4.0
    - markdown ==2.6.1
    - factory-boy ==2.5.1

  run:
    - python
    - django ==1.8
    - django-braces ==1.4.0
    - markdown ==2.6.1
    - factory-boy ==2.5.1

test:
  # Python imports
  imports:
    - dmcm

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ahernp/DMCM
  license: BSD License
  summary: 'Django Markdown Content Manager.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
