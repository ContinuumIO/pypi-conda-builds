package:
  name: distutilscross
  version: "0.1"

source:
  fn: distutilscross-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/distutilscross/distutilscross-0.1.tar.gz
  md5: 700801380a336a01925ad8409ad98c25
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - distutilscross = distutilscross:main
    #
    # Would create an entry point called distutilscross that calls distutilscross.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - distutilscross

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/lambacck/distutilscross/
  license: MIT License
  summary: 'Cross Compile Python Extensions'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
