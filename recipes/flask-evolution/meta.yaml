package:
  name: flask-evolution
  version: "0.6"

source:
  fn: Flask-Evolution-0.6.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Evolution/Flask-Evolution-0.6.tar.gz
  md5: 6ce08b2bfd4a83e972ec8cc444506b7e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-evolution = flask-evolution:main
    #
    # Would create an entry point called flask-evolution that calls flask-evolution.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - flask-sqlalchemy
    - flask-script

  run:
    - python
    - flask
    - flask-sqlalchemy
    - flask-script

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/adamrt/flask-evolution/
  license: BSD License
  summary: 'Simple migrations for Flask/SQLAlchemy projects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
