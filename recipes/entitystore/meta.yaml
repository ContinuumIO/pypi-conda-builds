package:
  name: entitystore
  version: "0.3.6"

source:
  fn: EntityStore-0.3.6.tar.gz
  url: https://pypi.python.org/packages/source/E/EntityStore/EntityStore-0.3.6.tar.gz
  md5: 417110e130cea3942153d6bea59f7362
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - entitystore = entitystore:main
    #
    # Would create an entry point called entitystore that calls entitystore.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - amqpqueue
    - pyworker
    - rdfobject >=0.3.1
    - supervisor
    - web.py
    - mako
    - redis
    - solrpy
    - http4store

  run:
    - python
    - amqpqueue
    - pyworker
    - rdfobject >=0.3.1
    - supervisor
    - web.py
    - mako
    - redis
    - solrpy
    - http4store

test:
  # Python imports
  imports:
    - entitystore

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'EntityStore - REST interface for CRUD on (RDF) Entities.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
