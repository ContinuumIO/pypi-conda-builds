package:
  name: simplehttpsserver
  version: "0.6.6"

source:
  fn: SimpleHTTPSServer-0.6.6.tar.gz
  url: https://pypi.python.org/packages/source/S/SimpleHTTPSServer/SimpleHTTPSServer-0.6.6.tar.gz
  md5: f0be148300324e669e0fd1f75ceeb091
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - simplehttpsserver = simplehttpsserver:main
    #
    # Would create an entry point called simplehttpsserver that calls simplehttpsserver.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - SimpleHTTPSServer
    - simplehttpsserver

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/pdxjohnny/SimpleHTTPSServer
  license: UNKNOWN
  summary: 'A simple python http and https webserver'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
