package:
  name: zenwarch
  version: "2.1.2"

source:
  fn: zenwarch-2.1.2.tar.gz
  url: https://pypi.python.org/packages/source/z/zenwarch/zenwarch-2.1.2.tar.gz
  md5: e2befa03cb112a8bd46391448e6820b3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zenwarch = zenwarch:main
    #
    # Would create an entry point called zenwarch that calls zenwarch.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - zen
    - zen.gae
    - zen.gae.middleware
    - zenwarch

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/renzon/zenwarch/
  license: BSD License
  summary: 'Route your web-app by convention over configuration'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
