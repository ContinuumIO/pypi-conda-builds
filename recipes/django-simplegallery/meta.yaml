package:
  name: django-simplegallery
  version: "0.13"

source:
  fn: django-simplegallery-0.13.tar.gz
  url: https://pypi.python.org/packages/source/d/django-simplegallery/django-simplegallery-0.13.tar.gz
  md5: f9d1f86a89233d1f972bd93d8b5772fc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-simplegallery = django-simplegallery:main
    #
    # Would create an entry point called django-simplegallery that calls django-simplegallery.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - simplegallery
    - simplegallery.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/divio/django-simplegallery/
  license: BSD License
  summary: 'django image gallery app'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
