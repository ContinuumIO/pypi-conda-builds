package:
  name: beautifulsoupselect
  version: "0.2"

source:
  fn: beautifulsoupselect-0.2.tar.gz
  url: https://pypi.python.org/packages/source/b/beautifulsoupselect/beautifulsoupselect-0.2.tar.gz
  md5: 78ae57845266aa5f06667e859e3e91b5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - beautifulsoupselect = beautifulsoupselect:main
    #
    # Would create an entry point called beautifulsoupselect that calls beautifulsoupselect.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - beautifulsoup

  run:
    - python
    - beautifulsoup

test:
  # Python imports
  imports:
    - beautifulsoupselect

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/sbma44/beautifulsoupselect
  license: MIT
  summary: 'Simple wrapper to integrate BeautifulSoup and soupselect.py in a single package'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
