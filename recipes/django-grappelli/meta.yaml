package:
  name: django-grappelli
  version: "2.6.4"

source:
  fn: django-grappelli-2.6.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-grappelli/django-grappelli-2.6.4.tar.gz
  md5: f546e8c21a675ba77ea567b7e4af9aa9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-grappelli = django-grappelli:main
    #
    # Would create an entry point called django-grappelli that calls django-grappelli.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - grappelli
    - grappelli.dashboard
    - grappelli.dashboard.management
    - grappelli.dashboard.management.commands
    - grappelli.dashboard.templatetags
    - grappelli.templatetags
    - grappelli.tests
    - grappelli.views

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://django-grappelli.readthedocs.org
  license: BSD License
  summary: 'A jazzy skin for the Django Admin-Interface.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
