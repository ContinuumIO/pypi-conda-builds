package:
  name: django-flatqueries
  version: "0.1"

source:
  fn: django-flatqueries-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-flatqueries/django-flatqueries-0.1.tar.gz
  md5: 36369857763246861d3d815c01249602
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-flatqueries = django-flatqueries:main
    #
    # Would create an entry point called django-flatqueries that calls django-flatqueries.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - flatqueries

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/django-flatqueries/
  license: MIT
  summary: 'Flatpage-like SQL queries for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
