package:
  name: django-adv-imagefield
  version: "0.0.4"

source:
  fn: django-adv-imagefield-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-adv-imagefield/django-adv-imagefield-0.0.4.tar.gz
  md5: dc526b37a763039cadc5298130d9a70e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-adv-imagefield = django-adv-imagefield:main
    #
    # Would create an entry point called django-adv-imagefield that calls django-adv-imagefield.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.7
    - pillow >=2.7.0
    - beautifulsoup4 >=4.3.2

  run:
    - python
    - django >=1.7
    - pillow >=2.7.0
    - beautifulsoup4 >=4.3.2

test:
  # Python imports
  imports:
    - media_field
    - media_field.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/devangmundhra/django-adv-imagefield
  license: MIT License
  summary: 'Advanced ImageField for Django that provides widget to search Flickr and Google Images'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
