package:
  name: sphinx-server
  version: "0.1.0"

source:
  fn: sphinx-server-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/s/sphinx-server/sphinx-server-0.1.0.tar.gz
  md5: 0c4941ce850126e2a81a78148daa651e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sphinx-server = sphinx-server:main
    #
    # Would create an entry point called sphinx-server that calls sphinx-server.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - static3
    - setuptools

  run:
    - python
    - setuptools
    - static3
    - setuptools

test:
  # Python imports
  imports:
    - sphinxserver

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rmohr/sphinx-server
  license: GNU General Public License v3 (GPLv3)
  summary: 'simple wsgi spinx server'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
