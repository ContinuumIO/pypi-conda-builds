package:
  name: django-otp-yubikey
  version: "0.3.1"

source:
  fn: django-otp-yubikey-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-otp-yubikey/django-otp-yubikey-0.3.1.tar.gz
  md5: e9e065266ab302237cc69735c4efe901
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-otp-yubikey = django-otp-yubikey:main
    #
    # Would create an entry point called django-otp-yubikey that calls django-otp-yubikey.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-otp >=0.3.0
    - yubiotp >=0.2.1

  run:
    - python
    - django-otp >=0.3.0
    - yubiotp >=0.2.1

test:
  # Python imports
  imports:
    - otp_yubikey
    - otp_yubikey.migrations
    - otp_yubikey.south_migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/psagers/django-otp
  license: BSD License
  summary: 'A django-otp plugin that verifies YubiKey OTP tokens.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
