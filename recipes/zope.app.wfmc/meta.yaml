package:
  name: zope.app.wfmc
  version: "0.1.2"

source:
  fn: zope.app.wfmc-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/z/zope.app.wfmc/zope.app.wfmc-0.1.2.tar.gz
  md5: 4cc8dc46b061a4167ff550e077d25edd
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zope.app.wfmc = zope.app.wfmc:main
    #
    # Would create an entry point called zope.app.wfmc that calls zope.app.wfmc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.interface
    - zope.schema
    - zope.configuration
    - zope.wfmc
    - zope.component

  run:
    - python
    - setuptools
    - zope.interface
    - zope.schema
    - zope.configuration
    - zope.wfmc
    - zope.component

test:
  # Python imports
  imports:
    - zope
    - zope.app
    - zope.app.wfmc

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://cheeseshop.python.org/pypi/zope.app.wfmc
  license: Zope Public License
  summary: 'Zope Application integration for ``zope.wfmc``'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
