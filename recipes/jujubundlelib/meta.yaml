package:
  name: jujubundlelib
  version: "0.1.7"

source:
  fn: jujubundlelib-0.1.7.tar.gz
  url: https://pypi.python.org/packages/source/j/jujubundlelib/jujubundlelib-0.1.7.tar.gz
  md5: 89bfbbe8648a46fdf3fafdac2401b83f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jujubundlelib = jujubundlelib:main
    #
    # Would create an entry point called jujubundlelib that calls jujubundlelib.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyyaml ==3.11

  run:
    - python
    - pyyaml ==3.11

test:
  # Python imports
  imports:
    - jujubundlelib
    - jujubundlelib.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/juju/juju-bundlelib
  license: GNU Lesser General Public License v3 (LGPLv3)
  summary: 'A python library for working with Juju bundles'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
