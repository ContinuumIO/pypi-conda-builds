package:
  name: glamkit-holdingpage
  version: "0.5.0"

source:
  fn: glamkit-holdingpage-0.5.0.tar.gz
  url: https://pypi.python.org/packages/source/g/glamkit-holdingpage/glamkit-holdingpage-0.5.0.tar.gz
  md5: eac208b0de50b6d9f932f11c3101f8e5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - glamkit-holdingpage = glamkit-holdingpage:main
    #
    # Would create an entry point called glamkit-holdingpage that calls glamkit-holdingpage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - holdingpage
    - holdingpage.tests
    - holdingpage.tests.testapp

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/glamkit/glamkit-holdingpage
  license: BSD License
  summary: 'Holding page app for your Django site.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
