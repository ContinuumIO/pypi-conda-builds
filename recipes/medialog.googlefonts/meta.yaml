package:
  name: medialog.googlefonts
  version: "2.2.2"

source:
  fn: medialog.googlefonts-2.2.2.tar.gz
  url: https://pypi.python.org/packages/source/m/medialog.googlefonts/medialog.googlefonts-2.2.2.tar.gz
  md5: 597ed64aae176754ae76f28d2d27171e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - medialog.googlefonts = medialog.googlefonts:main
    #
    # Would create an entry point called medialog.googlefonts that calls medialog.googlefonts.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - medialog.controlpanel
    - plone.directives.form

  run:
    - python
    - setuptools
    - setuptools
    - medialog.controlpanel
    - plone.directives.form

test:
  # Python imports
  imports:
    - medialog
    - medialog.googlefonts
    - medialog.googlefonts.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://medialog.no
  license: GPL
  summary: 'Googlefonts for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
