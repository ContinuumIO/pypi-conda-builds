package:
  name: mezzanine-modal-announcements
  version: "1.1.3"

source:
  fn: mezzanine-modal-announcements-1.1.3.tar.gz
  url: https://pypi.python.org/packages/source/m/mezzanine-modal-announcements/mezzanine-modal-announcements-1.1.3.tar.gz
  md5: 7941610701ffe99d347f56a82e3adb2f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mezzanine-modal-announcements = mezzanine-modal-announcements:main
    #
    # Would create an entry point called mezzanine-modal-announcements that calls mezzanine-modal-announcements.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - modal_announcements
    - modal_announcements.migrations
    - modal_announcements.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/joshcartme/mezzanine-modal-announcements
  license: UNKNOWN
  summary: 'Popup announcements for Mezzanine websites utilizing Bootstrap modals.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
