package:
  name: heiankyoview
  version: "1.0"

source:
  fn: HeiankyoView-1.0.tar.gz
  url: https://pypi.python.org/packages/source/H/HeiankyoView/HeiankyoView-1.0.tar.gz
  md5: 10427cd4ded524b433e86f933238adf3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - heiankyoview = heiankyoview:main
    #
    # Would create an entry point called heiankyoview that calls heiankyoview.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - 
    - heiankyoview

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/akiradeveloper/HeiankyoView
  license: Apache Software License
  summary: 'A fast algorithm to visualize tree structures'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
