package:
  name: python-gtbmm
  version: "0.1"

source:
  fn: python-gtbmm-0.1.tar.gz
  url: https://pypi.python.org/packages/source/p/python-gtbmm/python-gtbmm-0.1.tar.gz
  md5: ebb4331f8256ebe1ade527ccdc3359e9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-gtbmm = python-gtbmm:main
    #
    # Would create an entry point called python-gtbmm that calls python-gtbmm.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - lxml >=3.3.5
    - pytest >=2.6.0
    - requests >=2.3.0

  run:
    - python
    - lxml >=3.3.5
    - pytest >=2.6.0
    - requests >=2.3.0

test:
  # Python imports
  imports:
    - gtbmm

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/python-gtbmm/
  license: LICENSE
  summary: 'A Guaranty Trust Bank Mobile Money interface library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
