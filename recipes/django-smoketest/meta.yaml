package:
  name: django-smoketest
  version: "0.3.0"

source:
  fn: django-smoketest-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-smoketest/django-smoketest-0.3.0.tar.gz
  md5: 8bc47c246799f56e643e61e277e3bae6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-smoketest = django-smoketest:main
    #
    # Would create an entry point called django-smoketest that calls django-smoketest.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6
    - nose

  run:
    - python
    - django >=1.6
    - nose

test:
  # Python imports
  imports:
    - smoketest

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ccnmtl/django-smoketest
  license: BSD
  summary: 'Django smoketest framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
