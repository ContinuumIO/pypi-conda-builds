package:
  name: django-viter
  version: "0.0.1"

source:
  fn: django-viter-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-viter/django-viter-0.0.1.tar.gz
  md5: 0f5ce661c2d4d7c4eff20d502be80a26
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-viter = django-viter:main
    #
    # Would create an entry point called django-viter that calls django-viter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - testing
    - viter
    - viter.generatorhandlers
    - viter.sendmailhandlers
    - viter.storehandlers
    - viter.storehandlers.dbstorehandler
    - viter.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD License
  summary: 'Invite users to your Django apps'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
