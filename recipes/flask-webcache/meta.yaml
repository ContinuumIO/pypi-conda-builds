package:
  name: flask-webcache
  version: "0.9.1"

source:
  fn: Flask-WebCache-0.9.1.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-WebCache/Flask-WebCache-0.9.1.tar.gz
  md5: 03b1997af2c38d7c7407e36f6fbf0e89
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-webcache = flask-webcache:main
    #
    # Would create an entry point called flask-webcache that calls flask-webcache.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask

  run:
    - python
    - flask

test:
  # Python imports
  imports:
    - flask_webcache

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/fusic-com/flask-webcache/
  license: BSD License
  summary: 'A Flask extension that adds HTTP based caching to Flask apps'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
