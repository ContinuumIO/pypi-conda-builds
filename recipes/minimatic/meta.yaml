package:
  name: minimatic
  version: "1.0"

source:
  fn: Minimatic-1.0.tar.gz
  url: https://pypi.python.org/packages/source/M/Minimatic/Minimatic-1.0.tar.gz
  md5: 5e3d4e6d2683d16d8bf35dde5f33a2e1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - minimatic = minimatic:main
    #
    # Would create an entry point called minimatic that calls minimatic.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pylons
    - webhelpers
    - beaker
    - cssutils

  run:
    - python
    - pylons
    - webhelpers
    - beaker
    - cssutils

test:
  # Python imports
  imports:
    - minimatic

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'CSS and Javascript Minification/Combination Upgrade to WebHelpers'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
