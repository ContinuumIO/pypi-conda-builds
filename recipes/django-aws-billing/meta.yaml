package:
  name: django-aws-billing
  version: "0.2.6"

source:
  fn: django-aws-billing-0.2.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-aws-billing/django-aws-billing-0.2.6.tar.gz
  md5: 8cfeed4d865e7047e510dc91e5faacff
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-aws-billing = django-aws-billing:main
    #
    # Would create an entry point called django-aws-billing that calls django-aws-billing.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - boto ==2.23.0
    - wsgiref ==0.1.2

  run:
    - python
    - boto ==2.23.0
    - wsgiref ==0.1.2

test:
  # Python imports
  imports:
    - aws_billing

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Miserlou/django-aws-billing
  license: Apache Software License
  summary: 'AWS Billing for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
