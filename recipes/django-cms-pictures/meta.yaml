package:
  name: django-cms-pictures
  version: "0.0.1"

source:
  fn: django-cms-pictures-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-cms-pictures/django-cms-pictures-0.0.1.tar.gz
  md5: 9ee8e372d914238e28270c245429bea5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-cms-pictures = django-cms-pictures:main
    #
    # Would create an entry point called django-cms-pictures that calls django-cms-pictures.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-cms >=2.1

  run:
    - python
    - django-cms >=2.1

test:
  # Python imports
  imports:
    - cms_pictures_polaroid
    - cms_pictures_slider

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/psaavedra/django-cms-pictures
  license: MIT License
  summary: 'Django CMS Pictures Plugins'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
