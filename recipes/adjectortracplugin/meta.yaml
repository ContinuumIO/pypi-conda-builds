package:
  name: adjectortracplugin
  version: "1.0b2"

source:
  fn: AdjectorTracPlugin-1.0b2.tar.gz
  url: https://pypi.python.org/packages/source/A/AdjectorTracPlugin/AdjectorTracPlugin-1.0b2.tar.gz
  md5: 116bd109f32cb2a72065b2668be81a69
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - adjectortracplugin = adjectortracplugin:main
    #
    # Would create an entry point called adjectortracplugin that calls adjectortracplugin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - adjectorclient >=1.0b2
    - trac >=0.10

  run:
    - python
    - setuptools
    - adjectorclient >=1.0b2
    - trac >=0.10

test:
  # Python imports
  imports:
    - adjector
    - adjector.plugins
    - adjector.plugins.trac
    - adjectortracplugin

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://projects.icapsid.net/adjector
  license: GPL v2 or v3, at your option
  summary: 'Integrate Adjector into your trac installation: render zones in trac templates.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
