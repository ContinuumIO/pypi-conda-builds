package:
  name: django-markitup
  version: "2.2.2"

source:
  fn: django-markitup-2.2.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-markitup/django-markitup-2.2.2.tar.gz
  md5: 45da1a061641b6546177d3514d5a544b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-markitup = django-markitup:main
    #
    # Would create an entry point called django-markitup that calls django-markitup.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - markitup
    - markitup.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/carljm/django-markitup/
  license: BSD License
  summary: 'Markup handling for Django using the MarkItUp! universal markup editor'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
