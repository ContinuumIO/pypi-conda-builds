package:
  name: django-spine
  version: "0.3.3"

source:
  fn: django-spine-0.3.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-spine/django-spine-0.3.3.tar.gz
  md5: 69a15441b4e098692df02f695054a475
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-spine = django-spine:main
    #
    # Would create an entry point called django-spine that calls django-spine.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-subcommand
    - django-pipeline <=1.2.14
    - eco
    - bpmappers

  run:
    - python
    - django-subcommand
    - django-pipeline <=1.2.14
    - eco
    - bpmappers

test:
  # Python imports
  imports:
    - spine
    - spine.management
    - spine.management.commands
    - spine.management.commands.generate

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ikeikeikeike/django-spine
  license: MIT License
  summary: 'Spine plugin for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
