package:
  name: django-foundation-icons
  version: "3.1"

source:
  fn: django-foundation-icons-3.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-foundation-icons/django-foundation-icons-3.1.tar.gz
  md5: 05ebde869c4d949037dccee05c056d29
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-foundation-icons = django-foundation-icons:main
    #
    # Would create an entry point called django-foundation-icons that calls django-foundation-icons.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - foundation_icons

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/benbacardi/django-foundation-icons
  license: MIT License
  summary: 'Zurb Foundation Icons (http://zurb.com/playground/foundation-icon-fonts-3) static files packaged in a Django app.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
