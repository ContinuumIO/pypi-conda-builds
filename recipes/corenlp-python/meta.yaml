package:
  name: corenlp-python
  version: "3.4.1-1"

source:
  fn: corenlp-python-3.4.1-1.tar.gz
  url: https://pypi.python.org/packages/source/c/corenlp-python/corenlp-python-3.4.1-1.tar.gz
  md5: 1e5c7483f2e2a9d4870dd8de3ef994f1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - corenlp-python = corenlp-python:main
    #
    # Would create an entry point called corenlp-python that calls corenlp-python.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - pexpect >=2.4
    - xmltodict >=0.4.6

  run:
    - python
    - pexpect >=2.4
    - xmltodict >=0.4.6

test:
  # Python imports
  imports:
    - corenlp

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/torotoki/corenlp-python
  license: GNU General Public License v2 or later (GPLv2+)
  summary: 'A Stanford Core NLP wrapper'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
