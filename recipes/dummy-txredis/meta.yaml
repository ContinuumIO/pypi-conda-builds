package:
  name: dummy-txredis
  version: "0.5"

source:
  fn: dummy-txredis-0.5.tar.gz
  url: https://pypi.python.org/packages/source/d/dummy-txredis/dummy-txredis-0.5.tar.gz
  md5: 8233dbff5d37f51329e441be09f58758
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dummy-txredis = dummy-txredis:main
    #
    # Would create an entry point called dummy-txredis that calls dummy-txredis.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - twisted >=10.0

  run:
    - python
    - twisted >=10.0

test:
  # Python imports
  imports:
    - dummy_txredis

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/williamsjj/dummy-txredis
  license: BSD
  summary: 'Dummy txRedis'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
