package:
  name: geobricksmapclassify
  version: "0.0.8"

source:
  fn: GeobricksMapClassify-0.0.8.tar.gz
  url: https://pypi.python.org/packages/source/G/GeobricksMapClassify/GeobricksMapClassify-0.0.8.tar.gz
  md5: 21f527620cc41acb56704bf39d8d5fc0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - geobricksmapclassify = geobricksmapclassify:main
    #
    # Would create an entry point called geobricksmapclassify that calls geobricksmapclassify.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - watchdog
    - flask
    - flask-cors
    - numpy
    - scipy
    - pysal
    - geobrickscommon

  run:
    - python
    - watchdog
    - flask
    - flask-cors
    - numpy
    - scipy
    - pysal
    - geobrickscommon

test:
  # Python imports
  imports:
    - geobricks_mapclassify
    - geobricks_mapclassify.config
    - geobricks_mapclassify.core
    - geobricks_mapclassify.rest
    - geobricksmapclassify

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/GeobricksMapClassify/
  license: LICENSE.txt
  summary: 'Geobricks .'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
