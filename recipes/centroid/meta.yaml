package:
  name: centroid
  version: "1.1.0"

source:
  fn: centroid-1.1.0.tar.gz
  url: https://pypi.python.org/packages/source/c/centroid/centroid-1.1.0.tar.gz
  md5: db4cf4d7e8737df5ff1b1276683ce278
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - centroid = centroid:main
    #
    # Would create an entry point called centroid that calls centroid.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - centroid

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ResourceDataInc/Centroid
  license: MIT
  summary: 'A centralized paradigm to configuration management.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
