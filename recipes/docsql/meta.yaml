package:
  name: docsql
  version: "0.1.0a1"

source:
  fn: docsql-0.1.0a1.tar.gz
  url: https://pypi.python.org/packages/source/d/docsql/docsql-0.1.0a1.tar.gz
  md5: df53c7575a0ef9fbb49fc5e9c36a591b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - docsql = docsql:main
    #
    # Would create an entry point called docsql that calls docsql.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - mysql-python >=1.2.2

  run:
    - python
    - mysql-python >=1.2.2

test:
  # Python imports
  imports:
    - docsql

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://noble.gs.washington.edu/~mmh1/software/docsql/
  license: GNU General Public License (GPL)
  summary: 'Easy access to Python DB API databases'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
