package:
  name: gevent_openssl
  version: "1.0"

source:
  fn: gevent_openssl-1.0.tar.gz
  url: https://pypi.python.org/packages/source/g/gevent_openssl/gevent_openssl-1.0.tar.gz
  md5: 7b6a5da8005ca083d4437c2e7e6bee54
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gevent_openssl = gevent_openssl:main
    #
    # Would create an entry point called gevent_openssl that calls gevent_openssl.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyopenssl >=0.11
    - gevent

  run:
    - python
    - pyopenssl >=0.11
    - gevent

test:
  # Python imports
  imports:
    - gevent_openssl

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/phuslu/gevent_openssl
  license: New BSD
  summary: 'A gevent wrapper for pyOpenSSL'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
