package:
  name: motmot.fastimage
  version: "0.5.5"

source:
  fn: motmot.FastImage-0.5.5.tar.gz
  url: https://pypi.python.org/packages/source/m/motmot.FastImage/motmot.FastImage-0.5.5.tar.gz
  md5: e20c214dd917605817642ddf498293fc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - motmot.fastimage = motmot.fastimage:main
    #
    # Would create an entry point called motmot.fastimage that calls motmot.fastimage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - motmot
    - motmot.FastImage

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.astraw.com/projects/motmot
  license: BSD
  summary: 'Pythonic API for the framewave SIMD library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
