package:
  name: modoboa
  version: "1.3.1"

source:
  fn: modoboa-1.3.1.tar.gz
  url: https://pypi.python.org/packages/source/m/modoboa/modoboa-1.3.1.tar.gz
  md5: 3e5d0ada1c62a8040e2157ffa380386c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - modoboa = modoboa:main
    #
    # Would create an entry point called modoboa that calls modoboa.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.7,<1.8
    - django-reversion ==1.8.5
    - dj-database-url
    - pycrypto
    - argparse
    - factory-boy >=2.4
    - passlib
    - django-versionfield2 >=0.3.3
    - requests

  run:
    - python
    - django >=1.7,<1.8
    - django-reversion ==1.8.5
    - dj-database-url
    - pycrypto
    - argparse
    - factory-boy >=2.4
    - passlib
    - django-versionfield2 >=0.3.3
    - requests

test:
  # Python imports
  imports:
    - modoboa
    - modoboa.core
    - modoboa.core.commands
    - modoboa.core.management
    - modoboa.core.management.commands
    - modoboa.core.migrations
    - modoboa.core.password_hashers
    - modoboa.core.templatetags
    - modoboa.core.views
    - modoboa.lib
    - modoboa.lib.migrations
    - modoboa.lib.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://modoboa.org/
  license: MIT License
  summary: 'Mail hosting made simple'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
