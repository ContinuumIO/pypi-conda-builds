package:
  name: django-googlemap
  version: "0.5"

source:
  fn: django-googlemap-0.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-googlemap/django-googlemap-0.5.tar.gz
  md5: 6c1db3ce1b4f11f93a066d0a0ab55f3b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-googlemap = django-googlemap:main
    #
    # Would create an entry point called django-googlemap that calls django-googlemap.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - googlemap

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mygu/django-googlemap
  license: BSD License
  summary: 'django-googlemap is a simple Django app to show the google map from ip addresses.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
