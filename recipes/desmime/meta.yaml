package:
  name: desmime
  version: "2015.03.01.1"

source:
  fn: desmime-2015.03.01.1.tar.gz
  url: https://pypi.python.org/packages/source/d/desmime/desmime-2015.03.01.1.tar.gz
  md5: e10f545b5e7c40f1cc91be9d6c344583
#  patches:
   # List any patch files here
   # - fix.patch

build:
  # preserve_egg_dir: True
  entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - desmime = desmime:main
    #
    # Would create an entry point called desmime that calls desmime.main()

    - desmime = _desmime.desmime:main

  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - _desmime
    - desmime

  commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.

    - desmime --help

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT License
  summary: 'desmime - openssl smime decryption helper'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
