package:
  name: collective.microdata.contentlisting
  version: "0.1"

source:
  fn: collective.microdata.contentlisting-0.1.zip
  url: https://pypi.python.org/packages/source/c/collective.microdata.contentlisting/collective.microdata.contentlisting-0.1.zip
  md5: b5e3cbc25a38880d74b256ab2b4fa7f0
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.microdata.contentlisting = collective.microdata.contentlisting:main
    #
    # Would create an entry point called collective.microdata.contentlisting that calls collective.microdata.contentlisting.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.microdata.core

  run:
    - python
    - setuptools
    - setuptools
    - collective.microdata.core

test:
  # Python imports
  imports:
    - collective
    - collective.microdata
    - collective.microdata.contentlisting
    - collective.microdata.contentlisting.browser
    - collective.microdata.contentlisting.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/keul/collective.microdata.contentlisting
  license: GPL
  summary: 'Folders and collections listing support for microdata in Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
