package:
  name: ipfix
  version: "0.9.7"

source:
  fn: ipfix-0.9.7.tar.gz
  url: https://pypi.python.org/packages/source/i/ipfix/ipfix-0.9.7.tar.gz
  md5: d2349f59a5e527037ec39f4dd9c6f81e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ipfix = ipfix:main
    #
    # Would create an entry point called ipfix that calls ipfix.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - ipfix

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/britram/python-ipfix
  license: GNU Lesser General Public License v3 or later (LGPLv3+)
  summary: 'IPFIX implementation for Python 3.3+'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
