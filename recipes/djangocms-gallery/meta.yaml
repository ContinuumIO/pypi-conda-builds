package:
  name: djangocms-gallery
  version: "0.2a.2"

source:
  fn: djangocms-gallery-0.2a.2.tar.gz
  url: https://pypi.python.org/packages/source/d/djangocms-gallery/djangocms-gallery-0.2a.2.tar.gz
  md5: c28f6a9bc71a50899c8d22631d00022c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangocms-gallery = djangocms-gallery:main
    #
    # Would create an entry point called djangocms-gallery that calls djangocms-gallery.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django-filer
    - easy-thumbnails

  run:
    - python
    - django-filer
    - easy-thumbnails

test:
  # Python imports
  imports:
    - djangocms_gallery
    - djangocms_gallery.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/pascalmouret/djangocms-gallery
  license: BSD License
  summary: 'Gallery plugin for the django-cms.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
