package:
  name: sqlalchemy-manager
  version: "0.0.2"

source:
  fn: sqlalchemy-manager-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/s/sqlalchemy-manager/sqlalchemy-manager-0.0.2.tar.gz
  md5: fc1b3573866e38c814a67eb1d48d7c71
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sqlalchemy-manager = sqlalchemy-manager:main
    #
    # Would create an entry point called sqlalchemy-manager that calls sqlalchemy-manager.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sqlalchemy

  run:
    - python
    - sqlalchemy

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/d1ffuz0r/sqlalchemy-manager
  license: UNKNOWN
  summary: 'Manager for SQLAlchemy'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
