package:
  name: django-js-error-logging
  version: "0.1.1"

source:
  fn: django-js-error-logging-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-js-error-logging/django-js-error-logging-0.1.1.tar.gz
  md5: 7c06e1c4419066c39da1ee0e50ff661b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-js-error-logging = django-js-error-logging:main
    #
    # Would create an entry point called django-js-error-logging that calls django-js-error-logging.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - jserrorlogging
    - jserrorlogging.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/moqada/django-js-error-logging/
  license: BSD License
  summary: 'Logging Client-Side JavaScript errors for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
