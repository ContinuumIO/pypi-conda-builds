package:
  name: testliveserver
  version: "0.0.2"

source:
  fn: TestLiveServer-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/T/TestLiveServer/TestLiveServer-0.0.2.tar.gz
  md5: dfdd9c4109df3000f5dab751fe7ed915
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - testliveserver = testliveserver:main
    #
    # Would create an entry point called testliveserver that calls testliveserver.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - liveandletdie
    - testliveserver

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/peterhudec/testliveserver
  license: MIT License
  summary: '\nSimplifies launching and terminating of web development\nservers from **BDD** or **functional** tests\n    '

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
