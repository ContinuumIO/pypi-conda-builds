package:
  name: clue_script
  version: "0.3.1"

source:
  fn: clue_script-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/c/clue_script/clue_script-0.3.1.tar.gz
  md5: a5b09c456ceddbab47c0f1a19f673b38
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - clue_script = clue_script:main
    #
    # Would create an entry point called clue_script that calls clue_script.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - clue_script

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/clueproject/clue_script
  license: BSD License
  summary: 'Easy utils for creating a script runner'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
