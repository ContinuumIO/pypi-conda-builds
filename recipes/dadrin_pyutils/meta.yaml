package:
  name: dadrin_pyutils
  version: "0.1.0"

source:
  fn: dadrin_pyutils-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/dadrin_pyutils/dadrin_pyutils-0.1.0.tar.gz
  md5: 79c88bb941f8c133849ea4f908c07ce8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dadrin_pyutils = dadrin_pyutils:main
    #
    # Would create an entry point called dadrin_pyutils that calls dadrin_pyutils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - dadrin_pyutils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lluiscanet/dadrin_pyutils
  license: BSD License
  summary: 'Python utilities for data driven proejcts'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
