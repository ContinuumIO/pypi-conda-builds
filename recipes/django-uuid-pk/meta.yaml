package:
  name: django-uuid-pk
  version: "0.2"

source:
  fn: django-uuid-pk-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-uuid-pk/django-uuid-pk-0.2.tar.gz
  md5: 3e8cd151ccaeabc3d8b3e3f3097f2f5a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-uuid-pk = django-uuid-pk:main
    #
    # Would create an entry point called django-uuid-pk that calls django-uuid-pk.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - uuid

  run:
    - python
    - uuid

test:
  # Python imports
  imports:
    - django_uuid_pk
    - django_uuid_pk.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/saxix/django-uuid-pk
  license: BSD
  summary: 'Django UUIDField with full primary-key protocol support'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
