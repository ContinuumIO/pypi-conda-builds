package:
  name: fullcontacter
  version: "0.3.1"

source:
  fn: fullcontacter-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/f/fullcontacter/fullcontacter-0.3.1.tar.gz
  md5: 02f696b2a1d612d9e4035a426382dad0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fullcontacter = fullcontacter:main
    #
    # Would create an entry point called fullcontacter that calls fullcontacter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - fullcontacter

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/patrick-russell/fullcontacter
  license: MIT License
  summary: "Library for interacting with Fullcontact's APIs."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
