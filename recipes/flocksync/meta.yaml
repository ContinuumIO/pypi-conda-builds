package:
  name: flocksync
  version: "0.2.4.0"

source:
  fn: flocksync-0.2.4.0.tar.gz
  url: https://pypi.python.org/packages/source/f/flocksync/flocksync-0.2.4.0.tar.gz
  md5: 94a32045c8bc28820af1e62840b0a951
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flocksync = flocksync:main
    #
    # Would create an entry point called flocksync that calls flocksync.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - bintrees
    - pycrypto
    - bencode
    - miniupnpc
    - netifaces
    - simplejson
    - ipaddr

  run:
    - python
    - bintrees
    - pycrypto
    - bencode
    - miniupnpc
    - netifaces
    - simplejson
    - ipaddr

test:
  # Python imports
  imports:
    - flock
    - flocksync

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jbruestle/flock
  license: MIT License
  summary: 'Peer to peer app synchronization protocol'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
