package:
  name: django-servee-gallery
  version: "0.2.1"

source:
  fn: django-servee-gallery-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-servee-gallery/django-servee-gallery-0.2.1.tar.gz
  md5: bf5204f59fce39b73b6118d7ec290fad
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-servee-gallery = django-servee-gallery:main
    #
    # Would create an entry point called django-servee-gallery that calls django-servee-gallery.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-improved-inlines

  run:
    - python
    - django-improved-inlines

test:
  # Python imports
  imports:
    - servee_gallery

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/servee/django-servee-gallery/
  license: BSD License
  summary: 'Document Plugin for django-servee'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
