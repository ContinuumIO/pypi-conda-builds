package:
  name: framer
  version: "0.1.0"

source:
  fn: framer-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/f/framer/framer-0.1.0.tar.gz
  md5: e478cdaca3ad8ef8aba81da9e9cf650a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - framer = framer:main
    #
    # Would create an entry point called framer that calls framer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cobs >=1.0.0
    - six >=1.6.1
    - trollius

  run:
    - python
    - cobs >=1.0.0
    - six >=1.6.1
    - trollius

test:
  # Python imports
  imports:
    - framer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/klmitch/framer
  license: GNU General Public License (GPL)
  summary: 'Network Framer Library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
