package:
  name: sflib
  version: "1.0dev-BZR-r45-panta-elasticworld.org-20091021145839-1oceeh3stpvyl04t"

source:
  fn: sflib-1.0dev-BZR-r45-panta-elasticworld.org-20091021145839-1oceeh3stpvyl04t.tar.gz
  url: https://pypi.python.org/packages/source/s/sflib/sflib-1.0dev-BZR-r45-panta-elasticworld.org-20091021145839-1oceeh3stpvyl04t.tar.gz
  md5: c6170e2c9acb1a3d64eefc44c75888c1
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sflib = sflib:main
    #
    # Would create an entry point called sflib that calls sflib.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - sflib
    - sflib.fileconf
    - sflib.graph
    - sflib.queues
    - sflib.udom

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://cheeseshop.python.org/pypi/sflib/
  license: GNU General Public License (GPL)
  summary: 'Foundation library for SoftwareFabrica projects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
