package:
  name: django-redirects-file
  version: "0.1.1"

source:
  fn: django-redirects-file-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-redirects-file/django-redirects-file-0.1.1.tar.gz
  md5: 1e662c10824cf24bf84ab38e672e7853
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-redirects-file = django-redirects-file:main
    #
    # Would create an entry point called django-redirects-file that calls django-redirects-file.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3

  run:
    - python
    - django >=1.3

test:
  # Python imports
  imports:
    - django_redirects_file

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ubuntudesign/django-redirects-file
  license: django-redirects-hvad
  summary: 'A simple redirects utility that uses a json file for redirects definitions'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
