package:
  name: juju-quickstart
  version: "2.0.1"

source:
  fn: juju-quickstart-2.0.1.tar.gz
  url: https://pypi.python.org/packages/source/j/juju-quickstart/juju-quickstart-2.0.1.tar.gz
  md5: cf40c2c74fd685d9eca38da12942f0d1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - juju-quickstart = juju-quickstart:main
    #
    # Would create an entry point called juju-quickstart that calls juju-quickstart.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - urwid >=1.1.1,<=1.2.1
    - jujuclient >=0.17.5,<=0.50.1
    - pyyaml >=3.10,<=3.11
    - websocket-client >=0.12.0,<=0.18.0

  run:
    - python
    - urwid >=1.1.1,<=1.2.1
    - jujuclient >=0.17.5,<=0.50.1
    - pyyaml >=3.10,<=3.11
    - websocket-client >=0.12.0,<=0.18.0

test:
  # Python imports
  imports:
    - quickstart
    - quickstart.cli
    - quickstart.models
    - quickstart.tests
    - quickstart.tests.cli
    - quickstart.tests.functional
    - quickstart.tests.models

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://launchpad.net/juju-quickstart
  license: GNU Affero General Public License v3
  summary: u'Juju Quickstart is a Juju plugin which allows for easily setting up a Juju\nenvironment in very few steps. The environment is bootstrapped and set up so\nthat it can be managed using a Web interface (the Juju GUI).\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
