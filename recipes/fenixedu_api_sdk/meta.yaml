package:
  name: fenixedu_api_sdk
  version: "2.0.1"

source:
  fn: fenixedu_api_sdk-2.0.1.tar.gz
  url: https://pypi.python.org/packages/source/f/fenixedu_api_sdk/fenixedu_api_sdk-2.0.1.tar.gz
  md5: 193ad25889a6655dd996b7973d5fd245
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fenixedu_api_sdk = fenixedu_api_sdk:main
    #
    # Would create an entry point called fenixedu_api_sdk that calls fenixedu_api_sdk.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - fenixedu
    - fenixedu_api_sdk

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/samfcmc/fenixedu-python-sdk
  license: UNKNOWN
  summary: 'FenixEdu API SDK for python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
