package:
  name: ldapgroups
  version: "0.2"

source:
  fn: ldapgroups-0.2.zip
  url: https://pypi.python.org/packages/source/l/ldapgroups/ldapgroups-0.2.zip
  md5: 8985a1f14b8e17e37276a474ccc45537
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ldapgroups = ldapgroups:main
    #
    # Would create an entry point called ldapgroups that calls ldapgroups.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - ldappas >=0.8

  run:
    - python
    - ldappas >=0.8

test:
  # Python imports
  imports:
    - ldapgroups
    - ldapgroups.browser
    - ldapgroups.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/ldapgroups
  license: MIT
  summary: 'A read-only Zope3 GroupFolder implementation that reflects groups on an LDAP server.\nNeeds ldappas\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
