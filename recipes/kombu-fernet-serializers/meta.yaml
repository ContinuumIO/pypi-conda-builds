package:
  name: kombu-fernet-serializers
  version: "0.0.5"

source:
  fn: kombu-fernet-serializers-0.0.5.tar.gz
  url: https://pypi.python.org/packages/source/k/kombu-fernet-serializers/kombu-fernet-serializers-0.0.5.tar.gz
  md5: 1b1482fbbe703f5047a1259882a570ce
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kombu-fernet-serializers = kombu-fernet-serializers:main
    #
    # Would create an entry point called kombu-fernet-serializers that calls kombu-fernet-serializers.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - anyjson >=0.3.3
    - kombu >=3.0.16
    - cryptography >=0.6.1

  run:
    - python
    - setuptools
    - anyjson >=0.3.3
    - kombu >=3.0.16
    - cryptography >=0.6.1

test:
  # Python imports
  imports:
    - kombu_fernet
    - kombu_fernet.serializers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/heroku/kombu-fernet-serializers
  license: MIT License
  summary: 'Symmetrically encrypted serializers for Kombu'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
