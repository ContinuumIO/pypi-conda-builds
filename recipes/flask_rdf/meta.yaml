package:
  name: flask_rdf
  version: "0.1.6"

source:
  fn: flask_rdf-0.1.6.tar.gz
  url: https://pypi.python.org/packages/source/f/flask_rdf/flask_rdf-0.1.6.tar.gz
  md5: 75013ff9956832d229562df61634d368
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask_rdf = flask_rdf:main
    #
    # Would create an entry point called flask_rdf that calls flask_rdf.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - python-mimeparse ==0.1.4

  run:
    - python
    - flask
    - python-mimeparse ==0.1.4

test:
  # Python imports
  imports:
    - flask_rdf

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/hufman/flask_rdf
  license: BSD License
  summary: 'Flask decorator to output RDF using content negotiation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
