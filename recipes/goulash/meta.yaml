package:
  name: goulash
  version: "0.57"

source:
  fn: goulash-0.57.tar.gz
  url: https://pypi.python.org/packages/source/g/goulash/goulash-0.57.tar.gz
  md5: 2395c789afb0b7b14616f1e11e335fcb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - goulash = goulash:main
    #
    # Would create an entry point called goulash that calls goulash.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - ansi2html
    - configparser
    - addict
    - argparse

  run:
    - python
    - ansi2html
    - configparser
    - addict
    - argparse

test:
  # Python imports
  imports:
    - goulash

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mattvonrocketstein/goulash/
  license: UNKNOWN
  summary: 'toolbox, random shared stuff from my other projects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
