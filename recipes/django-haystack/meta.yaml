package:
  name: django-haystack
  version: "2.3.1"

source:
  fn: django-haystack-2.3.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-haystack/django-haystack-2.3.1.tar.gz
  md5: 4c5dc665c1e82701a95168442e2510cd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-haystack = django-haystack:main
    #
    # Would create an entry point called django-haystack that calls django-haystack.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - haystack
    - haystack.backends
    - haystack.management
    - haystack.management.commands
    - haystack.templatetags
    - haystack.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://haystacksearch.org/
  license: BSD License
  summary: 'Pluggable search for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
