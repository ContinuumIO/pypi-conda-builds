package:
  name: django-haystack
  version: "2.4.0"

source:
  fn: django-haystack-2.4.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-haystack/django-haystack-2.4.0.tar.gz
  md5: 764e652d2fc5ff95b0531ff4e37d5c47
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-haystack = django-haystack:main
    #
    # Would create an entry point called django-haystack that calls django-haystack.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - haystack
    - haystack.backends
    - haystack.management
    - haystack.management.commands
    - haystack.templatetags
    - haystack.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  requires:
    - coverage
    - elasticsearch >=1.0.0,<2.0.0
    - geopy ==0.95.1
    - mock
    - nose
    - pysolr >=3.3.2
    - python-dateutil
    - whoosh ==2.5.4
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://haystacksearch.org/
  license: BSD License
  summary: 'Pluggable search for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
