package:
  name: mongodict
  version: "0.3.1"

source:
  fn: mongodict-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/m/mongodict/mongodict-0.3.1.tar.gz
  md5: 57f8bb308156c1891d790bb107eb4f9d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mongodict = mongodict:main
    #
    # Would create an entry point called mongodict that calls mongodict.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - pymongo

  run:
    - python
    - pymongo

test:
  # Python imports
  imports:
    - mongodict

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/turicas/mongodict/
  license: GNU General Public License (GPL)
  summary: 'MongoDB-backed Python dict-like interface'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
