package:
  name: hgdistver
  version: "0.25"

source:
  fn: hgdistver-0.25.tar.gz
  url: https://pypi.python.org/packages/source/h/hgdistver/hgdistver-0.25.tar.gz
  md5: 3ac7984dbfc2bdbb85da4a656aee15dd
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hgdistver = hgdistver:main
    #
    # Would create an entry point called hgdistver that calls hgdistver.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - hgdistver

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/RonnyPfannschmidt/hgdistver/
  license: MIT License
  summary: 'utility to generate python package version infos from mercurial/git tags'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
