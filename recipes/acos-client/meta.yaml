package:
  name: acos-client
  version: "1.2.4"

source:
  fn: acos-client-1.2.4.tar.gz
  url: https://pypi.python.org/packages/source/a/acos-client/acos-client-1.2.4.tar.gz
  md5: 6831804688fd2c52e037f8eee657e95b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - acos-client = acos-client:main
    #
    # Would create an entry point called acos-client that calls acos-client.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - hash_ring >=1.3.1
    - requests >=2.3.0

  run:
    - python
    - hash_ring >=1.3.1
    - requests >=2.3.0

test:
  # Python imports
  imports:
    - acos_client
    - acos_client.v21
    - acos_client.v21.slb
    - acos_client.v21.slb.template
    - acos_client.v30
    - acos_client.v30.file
    - acos_client.v30.slb
    - acos_client.v30.slb.template
    - tests
    - tests.unit

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/a10networks/acos-client
  license: Apache Software License
  summary: 'A10 Networks ACOS API Client'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
