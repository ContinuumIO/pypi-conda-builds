package:
  name: cufflinks
  version: "0.3"

source:
  fn: cufflinks-0.3.tar.gz
  url: https://pypi.python.org/packages/source/c/cufflinks/cufflinks-0.3.tar.gz
  md5: bcfa36336c29170a31b4dc7c3928e4ba
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cufflinks = cufflinks:main
    #
    # Would create an entry point called cufflinks that calls cufflinks.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pandas
    - plotly >=1.6.16
    - colorlover >=0.2

  run:
    - python
    - pandas
    - plotly >=1.6.16
    - colorlover >=0.2

test:
  # Python imports
  imports:
    - cufflinks

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/santosjorge/cufflinks
  license: MIT
  summary: 'Productivity Tools for Plotly + Pandas'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
