package:
  name: django-tastypie-swagger
  version: "0.1.4"

source:
  fn: django-tastypie-swagger-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-tastypie-swagger/django-tastypie-swagger-0.1.4.tar.gz
  md5: f90cef9ccab600bda8e5359a7feb3651
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-tastypie-swagger = django-tastypie-swagger:main
    #
    # Would create an entry point called django-tastypie-swagger that calls django-tastypie-swagger.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - tastypie_swagger

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/concentricsky/django-tastypie-swagger
  license: BSD
  summary: 'An adapter to use swagger-ui with django-tastypie'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
