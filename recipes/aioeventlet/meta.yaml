package:
  name: aioeventlet
  version: "0.4"

source:
  fn: aioeventlet-0.4.tar.gz
  url: https://pypi.python.org/packages/source/a/aioeventlet/aioeventlet-0.4.tar.gz
  md5: 678ea30265ae0326bddc767f80efd144
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - aioeventlet = aioeventlet:main
    #
    # Would create an entry point called aioeventlet that calls aioeventlet.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - eventlet
    - trollius >=0.3

  run:
    - python
    - eventlet
    - trollius >=0.3

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

# requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://aioeventlet.readthedocs.org/
  license: Apache Software License
  summary: 'asyncio event loop scheduling callbacks in eventlet.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
