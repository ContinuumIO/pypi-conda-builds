package:
  name: collective.pfg.creditcardfields
  version: "1.3"

source:
  fn: collective.pfg.creditcardfields-1.3.zip
  url: https://pypi.python.org/packages/source/c/collective.pfg.creditcardfields/collective.pfg.creditcardfields-1.3.zip
  md5: 869b1ec9fa508a7aefcf8c9789366403
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.pfg.creditcardfields = collective.pfg.creditcardfields:main
    #
    # Would create an entry point called collective.pfg.creditcardfields that calls collective.pfg.creditcardfields.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.archetypes >=1.7.7

  run:
    - python
    - setuptools
    - setuptools
    - products.archetypes >=1.7.7

test:
  # Python imports
  imports:
    - collective
    - collective.pfg
    - collective.pfg.creditcardfields

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://sixfeetup.com
  license: GPL
  summary: 'PFG extension, to be able to use fields specific to credit cards'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
