package:
  name: django-subscribe
  version: "0.1.4"

source:
  fn: django-subscribe-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-subscribe/django-subscribe-0.1.4.tar.gz
  md5: 0620506a302eb7800b1ac0107656c11e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-subscribe = django-subscribe:main
    #
    # Would create an entry point called django-subscribe that calls django-subscribe.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - subscribe
    - subscribe.migrations
    - subscribe.templatetags
    - subscribe.tests
    - subscribe.tests.integration_tests
    - subscribe.tests.test_app

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bitmazk/django-subscribe
  license: The MIT License
  summary: 'A simple subscription app for Django. Allows a user to subscribe to any object.\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
