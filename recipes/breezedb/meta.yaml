package:
  name: breezedb
  version: "1.2.0"

source:
  fn: breezedb-1.2.0.tar.gz
  url: https://pypi.python.org/packages/source/b/breezedb/breezedb-1.2.0.tar.gz
  md5: 5e446f8342af9e996fdc10405c53970f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - breezedb = breezedb:main
    #
    # Would create an entry point called breezedb that calls breezedb.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - breezedb

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/RMed/breezedb_python
  license: GNU General Public License v2 (GPLv2)
  summary: 'A simple file-based database engine'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
