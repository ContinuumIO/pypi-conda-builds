package:
  name: collective.media
  version: "0.21"

source:
  fn: collective.media-0.21.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.media/collective.media-0.21.tar.gz
  md5: c992e4299eb45500e4c195c63263154d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.media = collective.media:main
    #
    # Would create an entry point called collective.media that calls collective.media.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.media
    - collective.media.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/intk/collective.media
  license: GPL
  summary: 'Adds functionality to retrieve and prioritize media inside of folderish items.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
