package:
  name: user-agents
  version: "0.3.2"

source:
  fn: user-agents-0.3.2.tar.gz
  url: https://pypi.python.org/packages/source/u/user-agents/user-agents-0.3.2.tar.gz
  md5: 35801774d798dd362148da1841dc278e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - user-agents = user-agents:main
    #
    # Would create an entry point called user-agents that calls user-agents.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - ua-parser

  run:
    - python
    - ua-parser

test:
  # Python imports
  imports:
    - user_agents

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/selwin/python-user-agents
  license: MIT License
  summary: 'A library to identify devices (phones, tablets) and their capabilities by parsing (browser/HTTP) user agent strings'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
