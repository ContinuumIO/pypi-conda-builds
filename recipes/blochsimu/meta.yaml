package:
  name: blochsimu
  version: "0.1.2"

source:
  fn: blochsimu-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/b/blochsimu/blochsimu-0.1.2.tar.gz
  md5: b3a92cf64ec72cff91085c8f273a8321
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - blochsimu = blochsimu:main
    #
    # Would create an entry point called blochsimu that calls blochsimu.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy

  run:
    - python
    - numpy

test:
  # Python imports
  imports:
    - blochsimu

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/k7hoven/blochsimu
  license: BSD License
  summary: 'Bloch equation simulator for e.g. NMR-related simulations'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
