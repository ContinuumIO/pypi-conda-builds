package:
  name: django-coop
  version: "0.7.3"

source:
  fn: django-coop-0.7.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-coop/django-coop-0.7.3.tar.gz
  md5: 3ab9d26ba1eb3003fa76695845baa4e9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-coop = django-coop:main
    #
    # Would create an entry point called django-coop that calls django-coop.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - coop
    - coop.agenda
    - coop.agenda.conf
    - coop.article
    - coop.bin
    - coop.doc
    - coop.exchange
    - coop.link
    - coop.mailing
    - coop.management
    - coop.management.commands
    - coop.org
    - coop.person
    - coop.prefs
    - coop.project
    - coop.rdf
    - coop.tag
    - coop.templatetags
    - coop.ui
    - coop.utils
    - coop.webid

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/quinode/django-coop/
  license: BSD License
  summary: 'A basis for a cooperative organization directory, with people, organization, offers.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
