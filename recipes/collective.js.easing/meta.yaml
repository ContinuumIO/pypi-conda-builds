package:
  name: collective.js.easing
  version: "1.0"

source:
  fn: collective.js.easing-1.0.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.js.easing/collective.js.easing-1.0.tar.gz
  md5: 89bac1f4181848c71100b91e817e73e1
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.js.easing = collective.js.easing:main
    #
    # Would create an entry point called collective.js.easing that calls collective.js.easing.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.js
    - collective.js.easing

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.js.easing
  license: GPL
  summary: 'JQuery easing plugin registration for the Plone CMS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
