package:
  name: anidado_sinnick
  version: "1.3.0"

source:
  fn: anidado_sinnick-1.3.0.tar.gz
  url: https://pypi.python.org/packages/source/a/anidado_sinnick/anidado_sinnick-1.3.0.tar.gz
  md5: 1b80d25720db9487829d83027d2daf9e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - anidado_sinnick = anidado_sinnick:main
    #
    # Would create an entry point called anidado_sinnick that calls anidado_sinnick.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - anidado_sinnick

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.google.com.ar
  license: UNKNOWN
  summary: 'Un modulo que imprime listas anidadas'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
