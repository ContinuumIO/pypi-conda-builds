package:
  name: django-shop-configurableproduct
  version: "0.3.1"

source:
  fn: django-shop-configurableproduct-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-shop-configurableproduct/django-shop-configurableproduct-0.3.1.tar.gz
  md5: 5bc2a2831121e41a7e562f83091e9d69
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-shop-configurableproduct = django-shop-configurableproduct:main
    #
    # Would create an entry point called django-shop-configurableproduct that calls django-shop-configurableproduct.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sorl-thumbnail
    - django-shop

  run:
    - python
    - sorl-thumbnail
    - django-shop

test:
  # Python imports
  imports:
    - configurableproduct
    - configurableproduct.migrations
    - configurableproduct.models
    - configurableproduct.models.fields

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/zeus/django-shop-configurableproduct
  license: GPL
  summary: 'Configurable product for django-shop'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
