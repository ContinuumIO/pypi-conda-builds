package:
  name: collective.subscribablesections
  version: "0.1"

source:
  fn: collective.subscribablesections-0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.subscribablesections/collective.subscribablesections-0.1.tar.gz
  md5: 6a1082a8e11e14a9b5525d8c44a48e4b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.subscribablesections = collective.subscribablesections:main
    #
    # Would create an entry point called collective.subscribablesections that calls collective.subscribablesections.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - p4a.subtyper

  run:
    - python
    - setuptools
    - setuptools
    - p4a.subtyper

test:
  # Python imports
  imports:
    - collective
    - collective.subscribablesections
    - collective.subscribablesections.browser
    - collective.subscribablesections.browser.views
    - collective.subscribablesections.portlets

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/collective.subscribablesections
  license: GPL
  summary: 'Allow Members to request access to private Plone Folders.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
