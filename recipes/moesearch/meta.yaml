package:
  name: moesearch
  version: "0.1.8"

source:
  fn: moesearch-0.1.8.tar.gz
  url: https://pypi.python.org/packages/source/m/moesearch/moesearch-0.1.8.tar.gz
  md5: c7ad1c0e47fbb54d2f0b831420dde001
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - moesearch = moesearch:main
    #
    # Would create an entry point called moesearch that calls moesearch.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=2.5.0

  run:
    - python
    - requests >=2.5.0

test:
  # Python imports
  imports:
    - moesearch

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/alfateam123/moesearch
  license: UNKNOWN
  summary: 'archive.moe Search REST API Library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
