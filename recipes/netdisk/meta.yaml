package:
  name: netdisk
  version: "1.1.1"

source:
  fn: netdisk-1.1.1.tar.gz
  url: https://pypi.python.org/packages/source/n/netdisk/netdisk-1.1.1.tar.gz
  md5: c241c0ce681c8a1548755dc231e80832
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - netdisk = netdisk:main
    #
    # Would create an entry point called netdisk that calls netdisk.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - netdisk
    - netdisk.dropbox
    - netdisk.kuaipan
    - netdisk.kuaipan.oauth
    - netdisk.kuaipan.poster

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/liuyug/netdisk
  license: GPLv3
  summary: 'Command Line Tool for Network Disk'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
