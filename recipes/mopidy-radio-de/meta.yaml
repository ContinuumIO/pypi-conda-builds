package:
  name: mopidy-radio-de
  version: "0.2.0"

source:
  fn: Mopidy-radio-de-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/M/Mopidy-radio-de/Mopidy-radio-de-0.2.0.tar.gz
  md5: 6c4974d2d9530104599b51e7d2b052bb
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mopidy-radio-de = mopidy-radio-de:main
    #
    # Would create an entry point called mopidy-radio-de that calls mopidy-radio-de.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - mopidy >=0.18
    - pykka >=1.1
    - python-dateutil

  run:
    - python
    - setuptools
    - setuptools
    - mopidy >=0.18
    - pykka >=1.1
    - python-dateutil

test:
  # Python imports
  imports:
    - mopidy_radio_de

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/hechtus/mopidy-radio-de/
  license: MIT License
  summary: u'radio.de extension for Mopidy'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
