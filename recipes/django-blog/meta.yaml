package:
  name: django-blog
  version: "0.1.1"

source:
  fn: django-blog-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-blog/django-blog-0.1.1.tar.gz
  md5: 1105c2e45cdbe9f9a476e02fcea2c329
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-blog = django-blog:main
    #
    # Would create an entry point called django-blog that calls django-blog.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - blog
    - blog.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/djangoware/django-blog
  license: UNKNOWN
  summary: 'A simple blog app'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
