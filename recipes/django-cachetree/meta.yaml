package:
  name: django-cachetree
  version: "0.1.3"

source:
  fn: django-cachetree-0.1.3.zip
  url: https://pypi.python.org/packages/source/d/django-cachetree/django-cachetree-0.1.3.zip
  md5: 6272d8c742ddebe28fc6c387b7df2fe9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-cachetree = django-cachetree:main
    #
    # Would create an entry point called django-cachetree that calls django-cachetree.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - cachetree

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/brianjaystanley/django-cachetree
  license: MIT License
  summary: 'cache configurable trees of related model instances in Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
