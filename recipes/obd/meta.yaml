package:
  name: obd
  version: "0.4.0"

source:
  fn: obd-0.4.0.tar.gz
  url: https://pypi.python.org/packages/source/o/obd/obd-0.4.0.tar.gz
  md5: 80bb454b7edc58edb167e69852b468d5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - obd = obd:main
    #
    # Would create an entry point called obd that calls obd.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyserial

  run:
    - python
    - pyserial

test:
  # Python imports
  imports:
    - obd
    - obd.protocols

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/brendanwhitfield/python-OBD
  license: GNU General Public License v2 (GPLv2)
  summary: "Serial module for handling live sensor data from a vehicle's OBD-II port"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
