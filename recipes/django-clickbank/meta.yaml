package:
  name: django-clickbank
  version: "0.2.8"

source:
  fn: django-clickbank-0.2.8.tar.gz
  url: https://pypi.python.org/packages/source/d/django-clickbank/django-clickbank-0.2.8.tar.gz
  md5: bcba960bd0b0b40bf0479c4055025ae9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-clickbank = django-clickbank:main
    #
    # Would create an entry point called django-clickbank that calls django-clickbank.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_clickbank
    - django_clickbank.migrations
    - django_clickbank.tests
    - django_clickbank.util

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/sureiya/django-clickbank/
  license: MIT
  summary: "A pluggable application for receiving data from ClickBank's Instant Payment Notifications"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
