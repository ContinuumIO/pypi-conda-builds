package:
  name: django-debugging
  version: "0.1a2"

source:
  fn: django-debugging-0.1a2.zip
  url: https://pypi.python.org/packages/source/d/django-debugging/django-debugging-0.1a2.zip
  md5: aa3e714be93c2c67cb309cabbdf89e40
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-debugging = django-debugging:main
    #
    # Would create an entry point called django-debugging that calls django-debugging.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_debugging
    - django_debugging.management
    - django_debugging.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/nextscreenlabs/django-debugging
  license: BSD License
  summary: 'Debugging utilities for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
