package:
  name: demjson
  version: "2.2.3"

source:
  fn: demjson-2.2.3.tar.gz
  url: http://deron.meranda.us/python/demjson/dist/demjson-2.2.3.tar.gz
  #md5: 
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - demjson = demjson:main
    #
    # Would create an entry point called demjson that calls demjson.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://deron.meranda.us/python/demjson/
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'encoder, decoder, and lint/validator for JSON (JavaScript Object Notation) compliant with RFC 7159'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
