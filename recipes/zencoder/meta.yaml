package:
  name: zencoder
  version: "0.6.5"

source:
  fn: zencoder-0.6.5.tar.gz
  url: https://pypi.python.org/packages/source/z/zencoder/zencoder-0.6.5.tar.gz
  md5: 1b9ca5064d603e83ffc6224761d81ed9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zencoder = zencoder:main
    #
    # Would create an entry point called zencoder that calls zencoder.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=1.0

  run:
    - python
    - requests >=1.0

test:
  # Python imports
  imports:
    - zencoder

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/schworer/zencoder-py
  license: MIT License
  summary: 'Integration library for Zencoder'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
