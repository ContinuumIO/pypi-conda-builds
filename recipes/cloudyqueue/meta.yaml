package:
  name: cloudyqueue
  version: "0.0.0"

source:
  fn: cloudyqueue-0.0.0.tar.gz
  url: https://pypi.python.org/packages/source/c/cloudyqueue/cloudyqueue-0.0.0.tar.gz
  md5: e2a4f009861d2ec87e62b3cee83ea7df
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cloudyqueue = cloudyqueue:main
    #
    # Would create an entry point called cloudyqueue that calls cloudyqueue.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - boto

  run:
    - python
    - boto

test:
  # Python imports
  imports:
    - cloudyqueue

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/wnyc/cloudyqueue 
  license: GPL
  summary: 'Dictionary interface to cloud providers.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
