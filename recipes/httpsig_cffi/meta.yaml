package:
  name: httpsig_cffi
  version: "15.0.0"

source:
  fn: httpsig_cffi-15.0.0.tar.gz
  url: https://pypi.python.org/packages/source/h/httpsig_cffi/httpsig_cffi-15.0.0.tar.gz
  md5: 1558db5224cc2622986f0f611fdecadc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - httpsig_cffi = httpsig_cffi:main
    #
    # Would create an entry point called httpsig_cffi that calls httpsig_cffi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six
    - cryptography

  run:
    - python
    - six
    - cryptography

test:
  # Python imports
  imports:
    - httpsig_cffi
    - httpsig_cffi.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/hawkowl/httpsig_cffi
  license: MIT License
  summary: 'Secure HTTP request signing using the HTTP Signature draft specification'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
