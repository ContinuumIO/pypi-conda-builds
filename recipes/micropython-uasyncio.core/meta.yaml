package:
  name: micropython-uasyncio.core
  version: "0.8.3"

source:
  fn: micropython-uasyncio.core-0.8.3.tar.gz
  url: https://pypi.python.org/packages/source/m/micropython-uasyncio.core/micropython-uasyncio.core-0.8.3.tar.gz
  md5: 28c519cf0c73b85c833f76fac2889a09
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - micropython-uasyncio.core = micropython-uasyncio.core:main
    #
    # Would create an entry point called micropython-uasyncio.core that calls micropython-uasyncio.core.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - micropython-logging

  run:
    - python
    - micropython-logging

test:
  # Python imports
  imports:
    - uasyncio

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/micropython/micropython/issues/405
  license: MIT
  summary: 'uasyncio.core module for MicroPython'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
