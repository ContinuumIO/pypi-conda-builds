package:
  name: clientcookie
  version: "1.3.0"

source:
  fn: ClientCookie-1.3.0.tar.gz
  url: http://wwwsearch.sourceforge.net/ClientCookie/src/ClientCookie-1.3.0.tar.gz
  #md5: 
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - clientcookie = clientcookie:main
    #
    # Would create an entry point called clientcookie that calls clientcookie.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - ClientCookie
    - clientcookie

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://wwwsearch.sourceforge.net/ClientCookie/
  license: BSD License or Zope Public License
  summary: 'Client-side HTTP cookie handling.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
