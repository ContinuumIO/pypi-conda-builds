package:
  name: django-disqus
  version: "0.5"

source:
  fn: django-disqus-0.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-disqus/django-disqus-0.5.tar.gz
  md5: acdc010b70611b25893037ccf119ee28
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-disqus = django-disqus:main
    #
    # Would create an entry point called django-disqus that calls django-disqus.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4
    - mock >=1.0.1

  run:
    - python
    - django >=1.4
    - mock >=1.0.1

test:
  # Python imports
  imports:
    - disqus
    - disqus.management
    - disqus.management.commands
    - disqus.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://arthurk.github.com/django-disqus/
  license: BSD License
  summary: 'Export comments and integrate DISQUS into your Django website'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
