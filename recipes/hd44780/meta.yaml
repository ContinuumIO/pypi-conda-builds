package:
  name: hd44780
  version: "1.1.2"

source:
  fn: HD44780-1.1.2.tar.gz
  url: https://pypi.python.org/packages/source/H/HD44780/HD44780-1.1.2.tar.gz
  md5: 9ce2f7bf791cc29ced68529536f32ea1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hd44780 = hd44780:main
    #
    # Would create an entry point called hd44780 that calls hd44780.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - hd44780

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Mezgrman/HD44780
  license: AGPLv3
  summary: 'A library for controlling HD44780 compatible character LCDs'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
