package:
  name: django-quiet-runserver
  version: "0.2.0"

source:
  fn: django-quiet-runserver-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-quiet-runserver/django-quiet-runserver-0.2.0.tar.gz
  md5: ef918be7e48f15c3fa684cf6ce06f0ba
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-quiet-runserver = django-quiet-runserver:main
    #
    # Would create an entry point called django-quiet-runserver that calls django-quiet-runserver.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6

  run:
    - python
    - django >=1.6

test:
  # Python imports
  imports:
    - djangoquietrunserver
    - djangoquietrunserver.management
    - djangoquietrunserver.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/tim_heap/django-quiet-runserver
  license: UNKNOWN
  summary: 'Quieter runserver for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
