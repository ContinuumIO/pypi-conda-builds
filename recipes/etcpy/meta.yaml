package:
  name: etcpy
  version: "0.1.1"

source:
  fn: etcpy-0.1.1.tar.bz2
  url: https://pypi.python.org/packages/source/e/etcpy/etcpy-0.1.1.tar.bz2
  md5: 60536b682947bc229bb92e34d0b6872c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - etcpy = etcpy:main
    #
    # Would create an entry point called etcpy that calls etcpy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - distribute
    - requests

  run:
    - python
    - distribute
    - requests

test:
  # Python imports
  imports:
    - etcpy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.pocketnix.org/etcpy/file/tip
  license: MIT BSD
  summary: 'etcd interface library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
