package:
  name: atreal.usersinout
  version: "1.2.2"

source:
  fn: atreal.usersinout-1.2.2.zip
  url: https://pypi.python.org/packages/source/a/atreal.usersinout/atreal.usersinout-1.2.2.zip
  md5: 03d11c25574a926412fc577f0ff1b8b7
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - atreal.usersinout = atreal.usersinout:main
    #
    # Would create an entry point called atreal.usersinout that calls atreal.usersinout.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - atreal
    - atreal.usersinout
    - atreal.usersinout.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.atreal.net
  license: GPL
  summary: 'Import / export users via CSV files'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
