package:
  name: django-celery-transactions2
  version: "0.1.6"

source:
  fn: django-celery-transactions2-0.1.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-celery-transactions2/django-celery-transactions2-0.1.6.tar.gz
  md5: bb83866ef0a0aceb5ca9aa010b5aeda1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-celery-transactions2 = django-celery-transactions2:main
    #
    # Would create an entry point called django-celery-transactions2 that calls django-celery-transactions2.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - djcelery_transactions

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/chrisdoble/django-celery-transactions
  license: BSD License
  summary: 'Django transaction support for Celery tasks.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
