package:
  name: django-redis-metrics
  version: "0.8.3"

source:
  fn: django-redis-metrics-0.8.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-redis-metrics/django-redis-metrics-0.8.3.tar.gz
  md5: f74e36dba185b284cdbac28a11960479
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-redis-metrics = django-redis-metrics:main
    #
    # Would create an entry point called django-redis-metrics that calls django-redis-metrics.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - redis

  run:
    - python
    - django
    - redis

test:
  # Python imports
  imports:
    - redis_metrics
    - redis_metrics.management
    - redis_metrics.management.commands
    - redis_metrics.templatetags
    - redis_metrics.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bradmontgomery/django-redis-metrics
  license: MIT License
  summary: 'django-redis-metrics is a Django application for tracking application metrics backed by Redis.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
