package:
  name: plone.app.ldap
  version: "1.3.2"

source:
  fn: plone.app.ldap-1.3.2.zip
  url: https://pypi.python.org/packages/source/p/plone.app.ldap/plone.app.ldap-1.3.2.zip
  md5: 79bd36c04b4448bd8d382e839d9a9ea6
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.app.ldap = plone.app.ldap:main
    #
    # Would create an entry point called plone.app.ldap that calls plone.app.ldap.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - plone.memoize
    - products.cmfcore
    - products.genericsetup
    - products.ploneldap
    - python-ldap
    - setuptools
    - zope.component
    - zope.i18nmessageid
    - zope.interface
    - zope.lifecycleevent
    - zope.schema

  run:
    - python
    - setuptools
    - plone.memoize
    - products.cmfcore
    - products.genericsetup
    - products.ploneldap
    - python-ldap
    - setuptools
    - zope.component
    - zope.i18nmessageid
    - zope.interface
    - zope.lifecycleevent
    - zope.schema

test:
  # Python imports
  imports:
    - plone
    - plone.app
    - plone.app.ldap
    - plone.app.ldap.browser
    - plone.app.ldap.engine
    - plone.app.ldap.ploneldap

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/plone.app.ldap
  license: GPL
  summary: 'LDAP control panel for Plone 4.1 and higher'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
