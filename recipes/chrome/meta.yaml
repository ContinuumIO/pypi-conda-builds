package:
  name: chrome
  version: "0.0.1"

source:
  fn: chrome-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/chrome/chrome-0.0.1.tar.gz
  md5: cb038bca100751678b3ea1ee888a86c9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - chrome = chrome:main
    #
    # Would create an entry point called chrome that calls chrome.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - appscript
    - app
    - osascript

  run:
    - python
    - appscript
    - app
    - osascript

test:
  # Python imports
  imports:
    - chrome

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/cancerhermit/PyChrome/
  license: GNU General Public License (GPL)
  summary: 'Chrome (Mac OS) Python wrapper'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
