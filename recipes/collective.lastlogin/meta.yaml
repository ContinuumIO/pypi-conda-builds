package:
  name: collective.lastlogin
  version: "0.1"

source:
  fn: collective.lastlogin-0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.lastlogin/collective.lastlogin-0.1.tar.gz
  md5: fbc12aee72092ab3a2a4c0aa9297eb3c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.lastlogin = collective.lastlogin:main
    #
    # Would create an entry point called collective.lastlogin that calls collective.lastlogin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.lastlogin

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/collective.lastlogin
  license: GPL
  summary: 'Show the list of Plone users with the last login date.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
