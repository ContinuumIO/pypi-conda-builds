package:
  name: django-email-registration
  version: "0.3.1"

source:
  fn: django-email-registration-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-email-registration/django-email-registration-0.3.1.tar.gz
  md5: f056cd1b166fa4a46bf7e7eb7eb3346b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-email-registration = django-email-registration:main
    #
    # Would create an entry point called django-email-registration that calls django-email-registration.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4.2

  run:
    - python
    - django >=1.4.2

test:
  # Python imports
  imports:
    - email_registration

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/matthiask/django-email-registration/
  license: BSD License
  summary: "So simple you'll burst into tears right away."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
