package:
  name: django-pagination-bootstrap
  version: "1.0.7"

source:
  fn: django-pagination-bootstrap-1.0.7.tar.gz
  url: https://pypi.python.org/packages/source/d/django-pagination-bootstrap/django-pagination-bootstrap-1.0.7.tar.gz
  md5: c9f65f7d3fc3d6e2d905c36c266981d3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-pagination-bootstrap = django-pagination-bootstrap:main
    #
    # Would create an entry point called django-pagination-bootstrap that calls django-pagination-bootstrap.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4.0

  run:
    - python
    - django >=1.4.0

test:
  # Python imports
  imports:
    - pagination_bootstrap
    - pagination_bootstrap.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/staticdev/django-pagination-bootstrap
  license: MIT License
  summary: "Easy add pagination in Django, using Bootstrap's layout."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
