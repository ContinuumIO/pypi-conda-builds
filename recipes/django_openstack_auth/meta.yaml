package:
  name: django_openstack_auth
  version: "1.3.1"

source:
  fn: django_openstack_auth-1.3.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django_openstack_auth/django_openstack_auth-1.3.1.tar.gz
  md5: e3baf9119efec6c3a592161b97085b19
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_openstack_auth = django_openstack_auth:main
    #
    # Would create an entry point called django_openstack_auth that calls django_openstack_auth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pbr

  run:
    - python
    - pbr

test:
  # Python imports
  imports:
    - django_openstack_auth

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Apache Software License
  summary: 'Django authentication backend for use with OpenStack Identity'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
