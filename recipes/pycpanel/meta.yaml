package:
  name: pycpanel
  version: "0.1.5"

source:
  fn: pycpanel-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/p/pycpanel/pycpanel-0.1.5.tar.gz
  md5: 6e74423245cdb7313f4a5f2676f03294
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pycpanel = pycpanel:main
    #
    # Would create an entry point called pycpanel that calls pycpanel.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - requests >=2.0.0

  run:
    - python
    - requests >=2.0.0

test:
  # Python imports
  imports:
    - pycpanel

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/oznu/pycpanel
  license: Apache License
  summary: 'Python Client for the cPanel API and CSF.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
