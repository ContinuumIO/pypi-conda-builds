package:
  name: tapi
  version: "0.1.7"

source:
  fn: Tapi-0.1.7.tar.gz
  url: https://pypi.python.org/packages/source/T/Tapi/Tapi-0.1.7.tar.gz
  md5: afaef60db11d100420bd8e046bf39fd9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tapi = tapi:main
    #
    # Would create an entry point called tapi that calls tapi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests
    - jsonpath_rw

  run:
    - python
    - requests
    - jsonpath_rw

test:
  # Python imports
  imports:
    - tapi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://tapi.readthedocs.org/en/latest/index.html
  license: MIT License
  summary: 'Framework for testing APIs'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
