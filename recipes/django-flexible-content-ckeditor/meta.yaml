package:
  name: django-flexible-content-ckeditor
  version: "1.0.0"

source:
  fn: django-flexible-content-ckeditor-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-flexible-content-ckeditor/django-flexible-content-ckeditor-1.0.0.tar.gz
  md5: 08c9201167e9048322d02ec70a0b3423
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-flexible-content-ckeditor = django-flexible-content-ckeditor:main
    #
    # Would create an entry point called django-flexible-content-ckeditor that calls django-flexible-content-ckeditor.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-flexible-content

  run:
    - python
    - django-flexible-content

test:
  # Python imports
  imports:
    - flexible_ckeditor

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/hjkelly/django-flexible-content-ckeditor
  license: UNKNOWN
  summary: 'Rich text (WYSIWYG) support for django-flexible-content'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
