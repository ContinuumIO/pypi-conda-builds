package:
  name: django-redcap
  version: "0.1b2"

source:
  fn: django-redcap-0.1b2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-redcap/django-redcap-0.1b2.tar.gz
  md5: 67dde7771f983add89ba5a4b0611d2e1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-redcap = django-redcap:main
    #
    # Would create an entry point called django-redcap that calls django-redcap.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - distribute
    - django

  run:
    - python
    - distribute
    - django

test:
  # Python imports
  imports:
    - djredcap
    - djredcap.management
    - djredcap.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/cbmi/django-redcap/
  license: BSD License
  summary: 'Utilities for porting REDCap projects to Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
