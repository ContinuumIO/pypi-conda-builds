package:
  name: logstash_handler
  version: "0.1.0"

source:
  fn: logstash_handler-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/l/logstash_handler/logstash_handler-0.1.0.tar.gz
  md5: 1cfe11ce07bc7b86c8b619c719fc19f0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - logstash_handler = logstash_handler:main
    #
    # Would create an entry point called logstash_handler that calls logstash_handler.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - logstash_formatter >=0.5.8

  run:
    - python
    - logstash_formatter >=0.5.8

test:
  # Python imports
  imports:
    - logstash_handler

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/klynch/python-logstash-handler
  license: MIT License
  summary: 'Log handler meant for logstash'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
