package:
  name: offlinelibrary
  version: "0.1.1"

source:
  fn: OfflineLibrary-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/O/OfflineLibrary/OfflineLibrary-0.1.1.tar.gz
  md5: 97a21e7b86c72e59c26fd792e9d89118
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - offlinelibrary = offlinelibrary:main
    #
    # Would create an entry point called offlinelibrary that calls offlinelibrary.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - offlinelibrary
    - offlinelibrary.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/OfflineLibrary/
  license: LICENSE.txt
  summary: 'offline library for web development'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
