package:
  name: google-analytics-api-wrapper
  version: "0.1.7"

source:
  fn: google-analytics-api-wrapper-0.1.7.tar.gz
  url: https://pypi.python.org/packages/source/g/google-analytics-api-wrapper/google-analytics-api-wrapper-0.1.7.tar.gz
  md5: 1f0c11af2bf146b4008dabb6d4cc2851
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - google-analytics-api-wrapper = google-analytics-api-wrapper:main
    #
    # Would create an entry point called google-analytics-api-wrapper that calls google-analytics-api-wrapper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - analytics_query

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tikazyq/google-analytics-api-wrapper
  license: http://opensource.org/licenses/MIT
  summary: 'The Goolge Analytics wrapper is a convenient tool to extract data from GA via API. It is especially useful when the user has many GA profiles / web properties.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
