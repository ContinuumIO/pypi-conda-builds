package:
  name: aldryn-apphooks-config
  version: "0.2.4"

source:
  fn: aldryn-apphooks-config-0.2.4.tar.gz
  url: https://pypi.python.org/packages/source/a/aldryn-apphooks-config/aldryn-apphooks-config-0.2.4.tar.gz
  md5: e2498d51a409ce6ed2c88400d70bdf9b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - aldryn-apphooks-config = aldryn-apphooks-config:main
    #
    # Would create an entry point called aldryn-apphooks-config that calls aldryn-apphooks-config.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-appdata >=0.1.3
    - django-cms >=3.0.11

  run:
    - python
    - django-appdata >=0.1.3
    - django-cms >=3.0.11

test:
  # Python imports
  imports:
    - aldryn_apphooks_config

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/aldryn/aldryn-apphooks-config
  license: BSD License
  summary: 'Namespaces based configuration for Apphooks'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
