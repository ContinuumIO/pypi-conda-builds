package:
  name: collective.examples.diazo
  version: "0.2"

source:
  fn: collective.examples.diazo-0.2.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.examples.diazo/collective.examples.diazo-0.2.tar.gz
  md5: 4dfb8c6a88f7737cce1ea4969579bbf6
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.examples.diazo = collective.examples.diazo:main
    #
    # Would create an entry point called collective.examples.diazo that calls collective.examples.diazo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.xdv

  run:
    - python
    - setuptools
    - setuptools
    - collective.xdv

test:
  # Python imports
  imports:
    - collective
    - collective.examples
    - collective.examples.diazo

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/
  license: GPL
  summary: 'Example of Diazo (xdv) usage in Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
