package:
  name: django-jstemplate
  version: "1.3.7"

source:
  fn: django-jstemplate-1.3.7.tar.gz
  url: https://pypi.python.org/packages/source/d/django-jstemplate/django-jstemplate-1.3.7.tar.gz
  md5: 128f4f10d6cc24fab0229d3bbec47a53
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-jstemplate = django-jstemplate:main
    #
    # Would create an entry point called django-jstemplate that calls django-jstemplate.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3
    - six

  run:
    - python
    - django >=1.3
    - six

test:
  # Python imports
  imports:
    - jstemplate
    - jstemplate.management
    - jstemplate.management.commands
    - jstemplate.templatetags
    - jstemplate.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mjumbewu/django-jstemplate/
  license: BSD License
  summary: 'A Django template tag for embedding Mustache.js templates -- or other JavaScript templates -- safely.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
