package:
  name: caffeine
  version: "0.3"

source:
  fn: caffeine-0.3.tar.gz
  url: https://pypi.python.org/packages/source/c/caffeine/caffeine-0.3.tar.gz
  md5: 319ac85798cc1b29f42df244ba1ce021
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - caffeine = caffeine:main
    #
    # Would create an entry point called caffeine that calls caffeine.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyobjc-core >=3.0.4

  run:
    - python
    - pyobjc-core >=3.0.4

test:
  # Python imports
  imports:
    - caffeine

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jpn--/caffeine
  license: MIT License
  summary: 'A tool to keep Mac OS X from sleeping'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
