package:
  name: bazinga
  version: "0.2.4"

source:
  fn: Bazinga-0.2.4.tar.gz
  url: https://pypi.python.org/packages/source/B/Bazinga/Bazinga-0.2.4.tar.gz
  md5: 736e52ec277eabd5cb554c2ac14d75be
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bazinga = bazinga:main
    #
    # Would create an entry point called bazinga that calls bazinga.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - nose
    - snakefood

  run:
    - python
    - setuptools
    - nose
    - snakefood

test:
  # Python imports
  imports:
    - bazinga

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jbochi/bazinga
  license: MIT License
  summary: 'Bazinga is a nose plugin to run tests only if their dependencies were modified'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
