package:
  name: html_jumping
  version: "0.2.4"

source:
  fn: html_jumping-0.2.4.tar.gz
  url: https://pypi.python.org/packages/source/h/html_jumping/html_jumping-0.2.4.tar.gz
  md5: e1b850e7a3e0c175c01ea53305bddf1d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - html_jumping = html_jumping:main
    #
    # Would create an entry point called html_jumping that calls html_jumping.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - httplib2

  run:
    - python
    - httplib2

test:
  # Python imports
  imports:
    - html_jumping

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dperezrada/html-jumping
  license: GNU General Public License (GPL)
  summary: 'Allows to get an HTML, coming from several previous URLs. Sometimes this is needed to get webpages that requires cookies or a HTTP referrer to get a certain page.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
