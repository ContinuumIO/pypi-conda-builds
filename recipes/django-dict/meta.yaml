package:
  name: django-dict
  version: "1.0"

source:
  fn: django-dict-1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-dict/django-dict-1.0.tar.gz
  md5: 03a4f9bdebd109b3be13e03ddfa2f0c4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-dict = django-dict:main
    #
    # Would create an entry point called django-dict that calls django-dict.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - django_dict

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jbzdak/django-dict
  license: BSD License
  summary: 'Reusable app that adds simple tagging'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
