package:
  name: zope.app.keyreference
  version: "3.6.1"

source:
  fn: zope.app.keyreference-3.6.1.tar.gz
  url: https://pypi.python.org/packages/source/z/zope.app.keyreference/zope.app.keyreference-3.6.1.tar.gz
  md5: 78539e472016a8ca57b34b6ea0ab7d9d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zope.app.keyreference = zope.app.keyreference:main
    #
    # Would create an entry point called zope.app.keyreference that calls zope.app.keyreference.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.keyreference

  run:
    - python
    - setuptools
    - zope.keyreference

test:
  # Python imports
  imports:
    - zope
    - zope.app
    - zope.app.keyreference

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/zope.app.keyreference
  license: Zope Public License
  summary: 'Object key references'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
