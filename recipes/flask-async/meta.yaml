package:
  name: flask-async
  version: "0.11-dev-20140215"

source:
  fn: Flask-Async-0.11-dev-20140215.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Async/Flask-Async-0.11-dev-20140215.tar.gz
  md5: 8c79e24e44b575cd236df97f9e972ecf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-async = flask-async:main
    #
    # Would create an entry point called flask-async that calls flask-async.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - jinja2 >=2.4
    - itsdangerous >=0.21

  run:
    - python
    - jinja2 >=2.4
    - itsdangerous >=0.21

test:
  # Python imports
  imports:
    - flask
    - flask.ext
    - flask.testsuite
    - werkzeug

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/mrdon/flask/
  license: BSD License
  summary: 'A fork of Flask to support asyncio'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
