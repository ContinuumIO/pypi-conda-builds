package:
  name: cns.recipe.zmysqlda
  version: "2.0.9"

source:
  fn: cns.recipe.zmysqlda-2.0.9.tar.gz
  url: https://pypi.python.org/packages/source/c/cns.recipe.zmysqlda/cns.recipe.zmysqlda-2.0.9.tar.gz
  md5: 250de776fe5ada40f226e6606bc4160b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cns.recipe.zmysqlda = cns.recipe.zmysqlda:main
    #
    # Would create an entry point called cns.recipe.zmysqlda that calls cns.recipe.zmysqlda.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zc.buildout
    - setuptools

  run:
    - python
    - setuptools
    - zc.buildout
    - setuptools

test:
  # Python imports
  imports:
    - cns
    - cns.recipe
    - cns.recipe.zmysqlda

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://corenet-int.com
  license: GPL
  summary: 'Recipe for installing ZMySQLDA'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
