package:
  name: json-cfg
  version: "0.3.4"

source:
  fn: json-cfg-0.3.4.zip
  url: https://pypi.python.org/packages/source/j/json-cfg/json-cfg-0.3.4.zip
  md5: 58d268aceafaf41ca1dbd8fe2c4a07d4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - json-cfg = json-cfg:main
    #
    # Would create an entry point called json-cfg that calls json-cfg.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - jsoncfg
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/pasztorpisti/json-cfg
  license: MIT License
  summary: 'JSON config file parser with extended syntax (e.g.: comments), line/column numbers and other extras.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
