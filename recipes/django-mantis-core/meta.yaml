package:
  name: django-mantis-core
  version: "0.2.0"

source:
  fn: django-mantis-core-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-mantis-core/django-mantis-core-0.2.0.tar.gz
  md5: 1769694bdf8b2bac2c0c7285efe051aa
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-mantis-core = django-mantis-core:main
    #
    # Would create an entry point called django-mantis-core that calls django-mantis-core.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-dingos >=0.1.0

  run:
    - python
    - django-dingos >=0.1.0

test:
  # Python imports
  imports:
    - mantis_core

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/siemens/django-mantis-core
  license: GNU General Public License v2 or later (GPLv2+)
  summary: 'A wrapper around the Django Dingos app for the Mantis Cyber Threat Intelligence Mgmt. Framework.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
