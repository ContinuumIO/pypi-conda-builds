package:
  name: python-zimbra
  version: "2.0"

source:
  fn: python-zimbra-2.0.tar.gz
  url: https://pypi.python.org/packages/source/p/python-zimbra/python-zimbra-2.0.tar.gz
  md5: da82f5cfa0fc4c41997ae05abd3fefe9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-zimbra = python-zimbra:main
    #
    # Would create an entry point called python-zimbra that calls python-zimbra.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - pythonzimbra

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Zimbra-Community/python-zimbra
  license: BSD License
  summary: 'Python classes to access Zimbra SOAP backend with a few utilities.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
