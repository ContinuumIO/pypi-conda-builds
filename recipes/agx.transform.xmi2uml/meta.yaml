package:
  name: agx.transform.xmi2uml
  version: "1.0a1"

source:
  fn: agx.transform.xmi2uml-1.0a1.tar.gz
  url: https://pypi.python.org/packages/source/a/agx.transform.xmi2uml/agx.transform.xmi2uml-1.0a1.tar.gz
  md5: 21d085c73f19a9fb0abd73f36494c83b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - agx.transform.xmi2uml = agx.transform.xmi2uml:main
    #
    # Would create an entry point called agx.transform.xmi2uml that calls agx.transform.xmi2uml.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - agx.core
    - node.ext.xmi
    - node.ext.uml

  run:
    - python
    - setuptools
    - setuptools
    - agx.core
    - node.ext.xmi
    - node.ext.uml

test:
  # Python imports
  imports:
    - agx
    - agx.transform
    - agx.transform.xmi2uml
    - agx.transform.xmi2uml.flavours
    - agx.transform.xmi2uml.testing

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/bluedynamics/agx.transform.xmi2uml
  license: GNU General Public License (GPL)
  summary: 'AGX XMI to UML Transform'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
