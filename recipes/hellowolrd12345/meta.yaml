package:
  name: hellowolrd12345
  version: "1.0.1"

source:
  fn: helloWolrd12345-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/h/helloWolrd12345/helloWolrd12345-1.0.1.tar.gz
  md5: 7fb8221814ada1810e3d5132efdc4115
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hellowolrd12345 = hellowolrd12345:main
    #
    # Would create an entry point called hellowolrd12345 that calls hellowolrd12345.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - hellowolrd12345

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.epgauslabs.com
  license: UNKNOWN
  summary: 'A simple hellower'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
