package:
  name: django-supertools
  version: "0.3"

source:
  fn: django-supertools-0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-supertools/django-supertools-0.3.tar.gz
  md5: 18cc028110e0c2f6890e07707ab95a45
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-supertools = django-supertools:main
    #
    # Would create an entry point called django-supertools that calls django-supertools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pytz

  run:
    - python
    - pytz

test:
  # Python imports
  imports:
    - supertools
    - supertools.middlewate
    - supertools.views

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kaleidos/django-supertools
  license: BSD License
  summary: 'Generic tools for django apps.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
