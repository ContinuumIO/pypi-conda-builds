package:
  name: django-simple-api-key
  version: "0.1.1"

source:
  fn: django-simple-api-key-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-simple-api-key/django-simple-api-key-0.1.1.tar.gz
  md5: d07ab9402dec9cef0ef1f6572562d2b7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-simple-api-key = django-simple-api-key:main
    #
    # Would create an entry point called django-simple-api-key that calls django-simple-api-key.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - south >=0.8.1

  run:
    - python
    - south >=0.8.1

test:
  # Python imports
  imports:
    - simple_api_key

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/CIGIHub/django-simple-api-key/
  license: MIT License
  summary: 'A simple Django app to provide users api keys.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
