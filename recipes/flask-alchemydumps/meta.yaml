package:
  name: flask-alchemydumps
  version: "0.0.6"

source:
  fn: Flask-AlchemyDumps-0.0.6.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-AlchemyDumps/Flask-AlchemyDumps-0.0.6.tar.gz
  md5: 30ba36c58eaccf602e5e48c9cf7944ed
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-alchemydumps = flask-alchemydumps:main
    #
    # Would create an entry point called flask-alchemydumps that calls flask-alchemydumps.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - flask-script
    - flask-sqlalchemy
    - sqlalchemy
    - unipath

  run:
    - python
    - flask
    - flask-script
    - flask-sqlalchemy
    - sqlalchemy
    - unipath

test:
  # Python imports
  imports:
    - flask_alchemydumps
    - flask_alchemydumps.helpers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/cuducos/alchemydumps
  license: MIT License
  summary: 'SQLAlchemy backup/dump tool for Flask'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
