package:
  name: minishift-python
  version: "0.1.4"

source:
  fn: minishift-python-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/m/minishift-python/minishift-python-0.1.4.tar.gz
  md5: ac55353300eec1b1553c44260ae8c3a6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - minishift-python = minishift-python:main
    #
    # Would create an entry point called minishift-python that calls minishift-python.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - mcp2210
    - python-daemon

  run:
    - python
    - mcp2210
    - python-daemon

test:
  # Python imports
  imports:
    - minishift

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/arachnidlabs/minishift-python/
  license: UNKNOWN
  summary: 'Python interface for the minishift'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
