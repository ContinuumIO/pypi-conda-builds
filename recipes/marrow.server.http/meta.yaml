package:
  name: marrow.server.http
  version: "0.9"

source:
  fn: marrow.server.http-0.9.tar.gz
  url: https://pypi.python.org/packages/source/m/marrow.server.http/marrow.server.http-0.9.tar.gz
  md5: 1a973eb32699243837393fab8bba0129
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - marrow.server.http = marrow.server.http:main
    #
    # Would create an entry point called marrow.server.http that calls marrow.server.http.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - marrow.util
    - marrow.server

  run:
    - python
    - setuptools
    - marrow.util
    - marrow.server

test:
  # Python imports
  imports:
    - marrow
    - marrow.server
    - marrow.server.http

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/pulp/marrow.server.http
  license: MIT License
  summary: 'A powerful HTTP/1.1 server for WSGI 2 applications in both Python 2.x and 3.x.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
