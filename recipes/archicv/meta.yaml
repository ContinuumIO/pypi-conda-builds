package:
  name: archicv
  version: "0.0.2.1"

source:
  fn: archicv-0.0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/a/archicv/archicv-0.0.2.1.tar.gz
  md5: 5b40dd28051a7765e0ad8e661b29d492
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - archicv = archicv:main
    #
    # Would create an entry point called archicv that calls archicv.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - archicv
    - archicv.draw

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/zhangxiansheng/-archi
  license: MIT License
  summary: 'Computer Vision on Landscape Architecture'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
