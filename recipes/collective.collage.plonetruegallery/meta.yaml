package:
  name: collective.collage.plonetruegallery
  version: "1.0b1"

source:
  fn: collective.collage.plonetruegallery-1.0b1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.collage.plonetruegallery/collective.collage.plonetruegallery-1.0b1.tar.gz
  md5: bf42afbbb99ee43f0500a898b2f0ca35
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.collage.plonetruegallery = collective.collage.plonetruegallery:main
    #
    # Would create an entry point called collective.collage.plonetruegallery that calls collective.collage.plonetruegallery.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.plonetruegallery
    - products.collage

  run:
    - python
    - setuptools
    - setuptools
    - collective.plonetruegallery
    - products.collage

test:
  # Python imports
  imports:
    - collective
    - collective.collage
    - collective.collage.plonetruegallery
    - collective.collage.plonetruegallery.patches

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/collective.collage.plonetruegallery
  license: GPL
  summary: 'Show a PloneTrueGallery slideshow in a Collage'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
