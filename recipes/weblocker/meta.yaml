package:
  name: weblocker
  version: "0.5"

source:
  fn: WebLocker-0.5.tar.gz
  url: https://pypi.python.org/packages/source/W/WebLocker/WebLocker-0.5.tar.gz
  md5: a71dba15656ecd5483211ae28f774eb0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - weblocker = weblocker:main
    #
    # Would create an entry point called weblocker that calls weblocker.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - weblocker

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/nsmgr8/weblocker
  license: MIT License
  summary: 'A simple GUI for adding/removing sites into /etc/hosts file to block websites'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
