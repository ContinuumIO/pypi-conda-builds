package:
  name: django_html_comments
  version: "0.1dev"

source:
  fn: master.zip
  url: https://github.com/seanodonnell/django_html_comments/archive/master.zip
  #md5: 
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_html_comments = django_html_comments:main
    #
    # Would create an entry point called django_html_comments that calls django_html_comments.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_html_comments

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/seanodonnell/django_html_comments
  license: GNU Lesser General Public License v2 (LGPLv2)
  summary: 'Django app that provides views to process limited sanitized html comments and a sample tinymce configuration. Compatible with django.contrib.comments and Mezzanines ThreadedComments'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
