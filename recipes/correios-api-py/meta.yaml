package:
  name: correios-api-py
  version: "0.1.1"

source:
  fn: correios-api-py-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/c/correios-api-py/correios-api-py-0.1.1.tar.gz
  md5: 8f1ea5c7104c52dda41b7d0d68316aac
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - correios-api-py = correios-api-py:main
    #
    # Would create an entry point called correios-api-py that calls correios-api-py.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - beautifulsoup >=3.1.0

  run:
    - python
    - beautifulsoup >=3.1.0

test:
  # Python imports
  imports:
    - correios

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/guilhermechapiewski/correios-api-py/
  license: Apache License 2.0
  summary: 'API Python para obter informacoes de encomendas/Sedex dos Correios.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
