package:
  name: medialog.mobilethemeone
  version: "0.1.3"

source:
  fn: medialog.mobilethemeOne-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/m/medialog.mobilethemeOne/medialog.mobilethemeOne-0.1.3.tar.gz
  md5: 46186019cacd778284401b31d467a955
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - medialog.mobilethemeone = medialog.mobilethemeone:main
    #
    # Would create an entry point called medialog.mobilethemeone that calls medialog.mobilethemeone.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - ftw.mobilenavigation

  run:
    - python
    - setuptools
    - setuptools
    - ftw.mobilenavigation

test:
  # Python imports
  imports:
    - medialog
    - medialog.mobilethemeOne

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/espenmn/medialog.mobilethemeOne
  license: gpl
  summary: 'A diazo theme for zettwerk.mobiletheming'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
