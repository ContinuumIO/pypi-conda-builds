package:
  name: exactonline
  version: "0.1.2"

source:
  fn: exactonline-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/e/exactonline/exactonline-0.1.2.tar.gz
  md5: e6429bf46e0bb2ab8aeb5494c93fde03
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - exactonline = exactonline:main
    #
    # Would create an entry point called exactonline that calls exactonline.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - exactonline
    - exactonline.api

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ossobv/exactonline
  license: GNU Lesser General Public License v3 or later (LGPLv3+)
  summary: 'Exact Online REST API Library in Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
