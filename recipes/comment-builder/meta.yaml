package:
  name: comment-builder
  version: "0.1"

source:
  fn: comment-builder-0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/comment-builder/comment-builder-0.1.tar.gz
  md5: f5960a851476d14d1f59cd29dd548a19
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - comment-builder = comment-builder:main
    #
    # Would create an entry point called comment-builder that calls comment-builder.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/adamatan/comment-builder
  license: MIT
  summary: 'A python script for displaying aligned comments'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
