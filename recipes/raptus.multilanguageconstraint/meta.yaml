package:
  name: raptus.multilanguageconstraint
  version: "1.0a3"

source:
  fn: raptus.multilanguageconstraint-1.0a3.zip
  url: https://pypi.python.org/packages/source/r/raptus.multilanguageconstraint/raptus.multilanguageconstraint-1.0a3.zip
  md5: 557261473f393682408272aabc692e0a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - raptus.multilanguageconstraint = raptus.multilanguageconstraint:main
    #
    # Would create an entry point called raptus.multilanguageconstraint that calls raptus.multilanguageconstraint.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - archetypes.schemaextender
    - collective.monkeypatcher
    - raptus.multilanguageplone

  run:
    - python
    - setuptools
    - setuptools
    - archetypes.schemaextender
    - collective.monkeypatcher
    - raptus.multilanguageplone

test:
  # Python imports
  imports:
    - raptus
    - raptus.multilanguageconstraint
    - raptus.multilanguageconstraint.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Raptus/raptus.multilanguageconstraint
  license: gpl
  summary: 'Provides a language selection field on all content objects to display it only in selected languages'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
