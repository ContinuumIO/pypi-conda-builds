package:
  name: topiary
  version: "0.0.7"

source:
  fn: topiary-0.0.7.tar.gz
  url: https://pypi.python.org/packages/source/t/topiary/topiary-0.0.7.tar.gz
  md5: f6c1d84f110c6f0c9898701f9732ae8d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - topiary = topiary:main
    #
    # Would create an entry point called topiary that calls topiary.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy >=1.7,<2.0
    - pandas >=0.13.1,<0.17.0
    - mhctools >=0.0.0,<0.1.0
    - varcode >=0.3.1,<0.4.0
    - nose >=1.3.6,<1.4

  run:
    - python
    - numpy >=1.7,<2.0
    - pandas >=0.13.1,<0.17.0
    - mhctools >=0.0.0,<0.1.0
    - varcode >=0.3.1,<0.4.0
    - nose >=1.3.6,<1.4

test:
  # Python imports
  imports:
    - test
    - topiary
    - topiary.rna

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/hammerlab/topiary
  license: Apache Software License
  summary: 'Predict cancer epitopes from cancer sequence data'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
