package:
  name: foxtrot
  version: "0.1.9"

source:
  fn: foxtrot-0.1.9.tar.gz
  url: https://pypi.python.org/packages/source/f/foxtrot/foxtrot-0.1.9.tar.gz
  md5: ac97bb8c6a20ae4153190e7839335fb1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - foxtrot = foxtrot:main
    #
    # Would create an entry point called foxtrot that calls foxtrot.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - foxtrot

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Package for Python code analyzing'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
