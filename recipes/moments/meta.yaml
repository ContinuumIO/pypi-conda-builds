package:
  name: moments
  version: "2.0"

source:
  fn: Moments-2.0.tar.gz
  url: https://pypi.python.org/packages/source/M/Moments/Moments-2.0.tar.gz
  md5: 7a5aa41d2079086349ed52f31dae3318
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - moments = moments:main
    #
    # Would create an entry point called moments that calls moments.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - moments

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/cbrandt/moments
  license: MIT License
  summary: 'Python package to process moments log files'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
