package:
  name: aspen-jinja2
  version: "0.4"

source:
  fn: aspen-jinja2-0.4.tar.bz2
  url: https://pypi.python.org/packages/source/a/aspen-jinja2/aspen-jinja2-0.4.tar.bz2
  md5: cb3e8bfa5e219d671c03c303a3bbc692
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - aspen-jinja2 = aspen-jinja2:main
    #
    # Would create an entry point called aspen-jinja2 that calls aspen-jinja2.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - aspen >=0.23
    - jinja2

  run:
    - python
    - setuptools
    - aspen >=0.23
    - jinja2

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://aspen.io/
  license: MIT License
  summary: 'jinja2 plugin for Aspen'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
