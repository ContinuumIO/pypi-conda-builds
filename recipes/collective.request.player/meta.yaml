package:
  name: collective.request.player
  version: "0.1.0b1"

source:
  fn: collective.request.player-0.1.0b1.zip
  url: https://pypi.python.org/packages/source/c/collective.request.player/collective.request.player-0.1.0b1.zip
  md5: 26dedd330d0d58f013df878996c8b018
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.request.player = collective.request.player:main
    #
    # Would create an entry point called collective.request.player that calls collective.request.player.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - requests

  run:
    - python
    - setuptools
    - setuptools
    - requests

test:
  # Python imports
  imports:
    - collective
    - collective.request
    - collective.request.player
    - collective.request.player.browser
    - collective.request.player.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/potar/collective.request.player
  license: gpl
  summary: "The package replays requests. It's a part of https://github.com/potar/dagger"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
