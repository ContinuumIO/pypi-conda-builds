package:
  name: django-site-utils
  version: "0.1a3"

source:
  fn: django-site-utils-0.1a3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-site-utils/django-site-utils-0.1a3.tar.gz
  md5: 50ab27348bdea33aac65fbc7e1e91735
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-site-utils = django-site-utils:main
    #
    # Would create an entry point called django-site-utils that calls django-site-utils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3

  run:
    - python
    - django >=1.3

test:
  # Python imports
  imports:
    - site_utils
    - site_utils.management
    - site_utils.management.commands
    - test_project.test_app
    - test_project.test_app.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://projects.ninemoreminutes.com/projects/django-site-utils/
  license: BSD License
  summary: 'Django site-wide management commands and utilities.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
