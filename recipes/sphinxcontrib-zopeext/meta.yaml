package:
  name: sphinxcontrib-zopeext
  version: "0.2.1"

source:
  fn: sphinxcontrib-zopeext-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/s/sphinxcontrib-zopeext/sphinxcontrib-zopeext-0.2.1.tar.gz
  md5: e3c5ca4812ca00db1510af655b0d73e2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sphinxcontrib-zopeext = sphinxcontrib-zopeext:main
    #
    # Would create an entry point called sphinxcontrib-zopeext that calls sphinxcontrib-zopeext.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sphinx >=0.6
    - zope.interface

  run:
    - python
    - sphinx >=0.6
    - zope.interface

test:
  # Python imports
  imports:
    - sphinxcontrib
    - sphinxcontrib.zopeext

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/birkenfeld/sphinx-contrib
  license: BSD License
  summary: 'Sphinx extension zopeext'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
