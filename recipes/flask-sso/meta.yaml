package:
  name: flask-sso
  version: "0.2.0"

source:
  fn: Flask-SSO-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-SSO/Flask-SSO-0.2.0.tar.gz
  md5: 2f1d329185493bd57861cefeab199a73
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-sso = flask-sso:main
    #
    # Would create an entry point called flask-sso that calls flask-sso.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - blinker
    - six

  run:
    - python
    - flask
    - blinker
    - six

test:
  # Python imports
  imports:
    - flask_sso

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/inveniosoftware/flask-sso/
  license: GNU General Public License v2 (GPLv2)
  summary: 'Flask-SSO extension that eases Shibboleth authentication.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
