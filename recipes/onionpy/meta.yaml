package:
  name: onionpy
  version: "0.3.0"

source:
  fn: OnionPy-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/O/OnionPy/OnionPy-0.3.0.tar.gz
  md5: 54cf7c127766327d34dec26da64b2b1d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - onionpy = onionpy:main
    #
    # Would create an entry point called onionpy that calls onionpy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=2.0.0

  run:
    - python
    - requests >=2.0.0

test:
  # Python imports
  imports:
    - onion_py
    - onionpy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/duk3luk3/onion-py
  license: onkyo-eiscp
  summary: 'Python wrapper for the OnionOO tor status API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
