package:
  name: django-view-as
  version: "0.3.1"

source:
  fn: django-view-as-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-view-as/django-view-as-0.3.1.tar.gz
  md5: 6d37d52ad660d5e617c42b3f9b422b9a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-view-as = django-view-as:main
    #
    # Would create an entry point called django-view-as that calls django-view-as.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - viewas

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Apache License 2.0
  summary: 'A Django middleware which allows you to view the site on behalf of a user.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
