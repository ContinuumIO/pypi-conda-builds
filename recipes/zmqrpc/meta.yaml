package:
  name: zmqrpc
  version: "0.1f"

source:
  fn: zmqrpc-0.1f.tar.gz
  url: https://pypi.python.org/packages/source/z/zmqrpc/zmqrpc-0.1f.tar.gz
  md5: 47c2fc1c7ec2a5f6dbd500e5016cb38c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zmqrpc = zmqrpc:main
    #
    # Would create an entry point called zmqrpc that calls zmqrpc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyzmq-static
    - pymongo

  run:
    - python
    - pyzmq-static
    - pymongo

test:
  # Python imports
  imports:
    - zmqrpc

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/geoffwatts/zmqrpc
  license: UNKNOWN
  summary: 'A Python library that exports a class for RPC via zmq, using BSON for data interchange.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
