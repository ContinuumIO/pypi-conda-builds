package:
  name: django-revcanonical
  version: "1.0"

source:
  fn: django-revcanonical-1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-revcanonical/django-revcanonical-1.0.tar.gz
  md5: 661981aa17e1c9fa5dc18286ed8cba5c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-revcanonical = django-revcanonical:main
    #
    # Would create an entry point called django-revcanonical that calls django-revcanonical.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - revcanonical
    - revcanonical.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://hg.piranha.org.ua/django-revcanonical/
  license: BSD License
  summary: 'An implementation of rev=canonical url shortening for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
