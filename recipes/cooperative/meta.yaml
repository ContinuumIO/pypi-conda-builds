package:
  name: cooperative
  version: "0.1.10"

source:
  fn: cooperative-0.1.10.tar.gz
  url: https://pypi.python.org/packages/source/c/cooperative/cooperative-0.1.10.tar.gz
  md5: 9c3756bd76e111fe2c5604360f3a2409
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cooperative = cooperative:main
    #
    # Would create an entry point called cooperative that calls cooperative.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - iter-karld-tools ==0.9.2
    - stream-tap ==0.9.3
    - twisted ==15.0.0

  run:
    - python
    - iter-karld-tools ==0.9.2
    - stream-tap ==0.9.3
    - twisted ==15.0.0

test:
  # Python imports
  imports:
    - cooperative
    - cooperative.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/johnwlockwood/cooperative
  license: Apache Software License
  summary: 'Write computationally intensive non-blocking code without callbacks'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
