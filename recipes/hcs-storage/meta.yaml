package:
  name: hcs-storage
  version: "1.1"

source:
  fn: hcs-storage-1.1.tar.gz
  url: https://pypi.python.org/packages/source/h/hcs-storage/hcs-storage-1.1.tar.gz
  md5: d23c28c3f59d5bde7ea5ed90ea1bb9e8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hcs-storage = hcs-storage:main
    #
    # Would create an entry point called hcs-storage that calls hcs-storage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six

  run:
    - python
    - six

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/hcs-storage
  license: ISC License (ISCL)
  summary: 'My personal library collecting some useful snippets.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
