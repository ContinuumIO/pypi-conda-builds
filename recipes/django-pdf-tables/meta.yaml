package:
  name: django-pdf-tables
  version: "0.2.0"

source:
  fn: django-pdf-tables-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-pdf-tables/django-pdf-tables-0.2.0.tar.gz
  md5: ae2e7ecf0b4f5948db84e57f0f89266f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-pdf-tables = django-pdf-tables:main
    #
    # Would create an entry point called django-pdf-tables that calls django-pdf-tables.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fpdf
    - django

  run:
    - python
    - fpdf
    - django

test:
  # Python imports
  imports:
    - pdf_tables

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/PragmaticMates/django-pdf-tables
  license: BSD License
  summary: 'A Django app for processing PDF documents with tables'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
