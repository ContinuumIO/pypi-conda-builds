package:
  name: sqlalchemy-enum-dict
  version: "0.1.2"

source:
  fn: SQLAlchemy-Enum-Dict-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/S/SQLAlchemy-Enum-Dict/SQLAlchemy-Enum-Dict-0.1.2.tar.gz
  md5: 48c5290b0dab76d9837c7e88b3cdd9c4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sqlalchemy-enum-dict = sqlalchemy-enum-dict:main
    #
    # Would create an entry point called sqlalchemy-enum-dict that calls sqlalchemy-enum-dict.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sqlalchemy

  run:
    - python
    - sqlalchemy

test:
  # Python imports
  imports:
    - sqlalchemy_enum_dict

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/ponomar/sqlalchemy-enum-dict
  license: BSD License
  summary: 'Adds convinient EnumDict column to sqlalchemy'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
