package:
  name: aiclib
  version: "0.84"

source:
  fn: aiclib-0.84.tar.gz
  url: https://pypi.python.org/packages/source/a/aiclib/aiclib-0.84.tar.gz
  md5: 552077d1dfb3ba7df207b4c8110cf7b1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - aiclib = aiclib:main
    #
    # Would create an entry point called aiclib that calls aiclib.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - urllib3

  run:
    - python
    - urllib3

test:
  # Python imports
  imports:
    - aiclib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rackerlabs/aiclib
  license: Apache Software License
  summary: 'A declarative system to consume the NVP api.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
