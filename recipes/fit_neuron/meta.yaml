package:
  name: fit_neuron
  version: "0.0.6"

source:
  fn: fit_neuron-0.0.6.tar.gz
  url: https://pypi.python.org/packages/source/f/fit_neuron/fit_neuron-0.0.6.tar.gz
  md5: 4c0db9849a1d78b173561f815efaad91
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fit_neuron = fit_neuron:main
    #
    # Would create an entry point called fit_neuron that calls fit_neuron.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - fit_neuron
    - fit_neuron.data
    - fit_neuron.evaluate
    - fit_neuron.optimize
    - fit_neuron.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pythonhosted.org/fit_neuron
  license: Apache
  summary: 'Package for estimation and evaluation of neural models from patch clamp neural recordings.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
