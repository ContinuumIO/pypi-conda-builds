package:
  name: ifbyphone-api-module
  version: "0.0.5"

source:
  fn: Ifbyphone-API-Module-0.0.5.tar.gz
  url: https://pypi.python.org/packages/source/I/Ifbyphone-API-Module/Ifbyphone-API-Module-0.0.5.tar.gz
  md5: 5290ad479532c538de46228312d40ec3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ifbyphone-api-module = ifbyphone-api-module:main
    #
    # Would create an entry point called ifbyphone-api-module that calls ifbyphone-api-module.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - Ifbyphone
    - Ifbyphone.api

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Opus1no2/Ifbyphone-API-Module
  license: MIT
  summary: 'A Python client for interacting with Ifbyphone REST API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
