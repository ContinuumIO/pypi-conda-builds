package:
  name: cliargs
  version: "1.0"

source:
  fn: CLIArgs-1.0.tar.gz
  url: https://pypi.python.org/packages/source/C/CLIArgs/CLIArgs-1.0.tar.gz
  md5: c0ba2f9c38b0f15502995936250f031d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cliargs = cliargs:main
    #
    # Would create an entry point called cliargs that calls cliargs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - cliargs

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/CLIArgs/
  license: GNU General Public License (GPL)
  summary: 'Absolutely trivial command line arguments'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
