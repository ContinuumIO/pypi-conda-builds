package:
  name: app
  version: "0.0.2"

source:
  fn: app-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/a/app/app-0.0.2.tar.gz
  md5: 548c2d56d35bb507d7d1c55a7a5529b8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - app = app:main
    #
    # Would create an entry point called app that calls app.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - appscript
    - envoy
    - osascript

  run:
    - python
    - appscript
    - envoy
    - osascript

test:
  # Python imports
  imports:
    - app

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/cancerhermit/PyApp/
  license: GNU General Public License (GPL)
  summary: 'Python base class for Mac OS Applications'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
