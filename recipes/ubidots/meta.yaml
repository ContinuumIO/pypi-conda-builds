package:
  name: ubidots
  version: "1.6.2"

source:
  fn: ubidots-1.6.2.tar.gz
  url: https://pypi.python.org/packages/source/u/ubidots/ubidots-1.6.2.tar.gz
  md5: 39bd521cc8e7f242cd28c0b7877ebe58
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ubidots = ubidots:main
    #
    # Would create an entry point called ubidots that calls ubidots.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=2.5.1

  run:
    - python
    - requests >=2.5.1

test:
  # Python imports
  imports:
    - ubidots

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ubidots/ubidots-python/
  license: MIT License
  summary: 'Api Client to connect to ubidots.com api version 1.6'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
