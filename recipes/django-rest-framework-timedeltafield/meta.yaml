package:
  name: django-rest-framework-timedeltafield
  version: "0.0.2"

source:
  fn: django-rest-framework-timedeltafield-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-rest-framework-timedeltafield/django-rest-framework-timedeltafield-0.0.2.tar.gz
  md5: bd1fd84f52f0f4ad7b2aead1611d0c3e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-rest-framework-timedeltafield = django-rest-framework-timedeltafield:main
    #
    # Would create an entry point called django-rest-framework-timedeltafield that calls django-rest-framework-timedeltafield.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - timedelta_drf

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/RyanPineo/django-rest-framework-timedelta-field
  license: UNKNOWN
  summary: 'Django Rest Framework serializer field for timedelta fields.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
