package:
  name: cache_toolbar
  version: "1.0.1"

source:
  fn: cache_toolbar-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/cache_toolbar/cache_toolbar-1.0.1.tar.gz
  md5: 579f06c5c9a08ef7c09594f9f9f51cfe
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cache_toolbar = cache_toolbar:main
    #
    # Would create an entry point called cache_toolbar that calls cache_toolbar.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - cache_toolbar
    - cache_toolbar.panels

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/WoLpH/Cache-Debug-Toolbar
  license: UNKNOWN
  summary: 'Django Cache Debug Toolbar for Redis, Memcache and PyLibMC'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
