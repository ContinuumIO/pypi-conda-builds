package:
  name: monolith.client
  version: "0.9"

source:
  fn: monolith.client-0.9.tar.gz
  url: https://pypi.python.org/packages/source/m/monolith.client/monolith.client-0.9.tar.gz
  md5: 83ef6c3272a6f6ee56a6d6ab3185fa00
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - monolith.client = monolith.client:main
    #
    # Would create an entry point called monolith.client that calls monolith.client.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests
    - statsd

  run:
    - python
    - requests
    - statsd

test:
  # Python imports
  imports:
    - monolith
    - monolith.client
    - monolith.client.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mozilla/monolith-client
  license: Apache Software License
  summary: 'Mozilla Monolith Client'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
