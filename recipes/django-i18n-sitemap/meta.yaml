package:
  name: django-i18n-sitemap
  version: "0.1"

source:
  fn: django-i18n-sitemap-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-i18n-sitemap/django-i18n-sitemap-0.1.tar.gz
  md5: 5de8620ef689b48dc3c76b3dfa6ed6d4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-i18n-sitemap = django-i18n-sitemap:main
    #
    # Would create an entry point called django-i18n-sitemap that calls django-i18n-sitemap.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - i18nsitemap

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/TracyWebTech/django-i18n-sitemap
  license: GNU General Public License v2 (GPLv2)
  summary: 'Adds the possibility to have i18n urls in your sitemap'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
