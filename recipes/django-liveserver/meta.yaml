package:
  name: django-liveserver
  version: "0.1a-2"

source:
  fn: django-liveserver-0.1a-2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-liveserver/django-liveserver-0.1a-2.tar.gz
  md5: 1dbcd15ca1709fb4cd948b1cddfff5e1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-liveserver = django-liveserver:main
    #
    # Would create an entry point called django-liveserver that calls django-liveserver.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3

  run:
    - python
    - django >=1.3

test:
  # Python imports
  imports:
    - django_liveserver

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'A backport of the Django 1.4 LiveServerTestCase for 1.3'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
