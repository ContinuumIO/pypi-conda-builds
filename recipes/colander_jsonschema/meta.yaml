package:
  name: colander_jsonschema
  version: "0.2"

source:
  fn: colander_jsonschema-0.2.tar.gz
  url: https://pypi.python.org/packages/source/c/colander_jsonschema/colander_jsonschema-0.2.tar.gz
  md5: ce0814d00ebbceca52af3a25594b7f16
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - colander_jsonschema = colander_jsonschema:main
    #
    # Would create an entry point called colander_jsonschema that calls colander_jsonschema.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - colander

  run:
    - python
    - colander

test:
  # Python imports
  imports:
    - colander_jsonschema
    - colander_jsonschema.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/gjo/colander_jsonschema
  license: BSD License
  summary: 'JSON-Schema converter for colander'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
