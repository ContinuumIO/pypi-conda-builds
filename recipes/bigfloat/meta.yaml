package:
  name: bigfloat
  version: "0.3.0"

source:
  fn: bigfloat-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/b/bigfloat/bigfloat-0.3.0.tar.gz
  md5: 7343f72b02bad33676df57e10a13508a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bigfloat = bigfloat:main
    #
    # Would create an entry point called bigfloat that calls bigfloat.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - bigfloat
    - bigfloat.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/mdickinson/bigfloat
  license: GNU Lesser General Public License v3 (LGPLv3)
  summary: 'Arbitrary-precision correctly-rounded floating-point arithmetic, via MPFR.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
