package:
  name: django-subcommander
  version: "0.1"

source:
  fn: django-subcommander-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-subcommander/django-subcommander-0.1.tar.gz
  md5: d980872821db68f1b66b0db798b606e3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-subcommander = django-subcommander:main
    #
    # Would create an entry point called django-subcommander that calls django-subcommander.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_subcommander

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/votizen/django-subcommander
  license: MIT License
  summary: 'Write Django management commands that have subcommands.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
