package:
  name: digimat.wink
  version: "0.1.2"

source:
  fn: digimat.wink-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/digimat.wink/digimat.wink-0.1.2.tar.gz
  md5: f14cf842e95ec24eae3fde3124e5fa1e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - digimat.wink = digimat.wink:main
    #
    # Would create an entry point called digimat.wink that calls digimat.wink.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - requests
    - setuptools

  run:
    - python
    - requests
    - setuptools

test:
  # Python imports
  imports:
    - digimat
    - digimat.wink

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: PSF
  summary: 'Digimat Wink'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
