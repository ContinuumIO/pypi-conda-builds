package:
  name: blug
  version: "0.2.0"

source:
  fn: blug-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/b/blug/blug-0.2.0.tar.gz
  md5: 39f50e2f629bbe153d40b9bbb5c67e58
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - blug = blug:main
    #
    # Would create an entry point called blug that calls blug.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - blug
    - lib
    - test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.github.com/jeffknupp/blug
  license: MIT License
  summary: 'Static site generator for Markdown based blogs, with a built-in webserver'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
