package:
  name: clusterprocessing
  version: "0.0.0.dev"

source:
  fn: clusterprocessing-0.0.0.dev.tar.gz
  url: https://pypi.python.org/packages/source/c/clusterprocessing/clusterprocessing-0.0.0.dev.tar.gz
  md5: adf8a3f8969aa00db178926d4a32c69f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - clusterprocessing = clusterprocessing:main
    #
    # Would create an entry point called clusterprocessing that calls clusterprocessing.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - clusterprocessing
    - fabfile
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: TBA
  license: TBA
  summary: 'TBA'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
