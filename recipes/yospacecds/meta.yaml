package:
  name: yospacecds
  version: "1.1.0"

source:
  fn: yospaceCDS-1.1.0.tar.gz
  url: https://pypi.python.org/packages/source/y/yospaceCDS/yospaceCDS-1.1.0.tar.gz
  md5: cceb0e19bfed6c624bf029a7773e39a3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - yospacecds = yospacecds:main
    #
    # Would create an entry point called yospacecds that calls yospacecds.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - suds >=0.4

  run:
    - python
    - suds >=0.4

test:
  # Python imports
  imports:
    - yospaceCDS
    - yospacecds

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.yospace.com/index.php/cds_main.html
  license: Distribute Freely
  summary: 'Yospace CDS Webservice Client'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
