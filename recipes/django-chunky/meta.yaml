package:
  name: django-chunky
  version: "0.1.7"

source:
  fn: django-chunky-0.1.7.tar.gz
  url: https://pypi.python.org/packages/source/d/django-chunky/django-chunky-0.1.7.tar.gz
  md5: 336d4591755237c622cc175334ea24c6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-chunky = django-chunky:main
    #
    # Would create an entry point called django-chunky that calls django-chunky.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - chunky

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/freyley/django-chunky
  license: BSD License
  summary: 'Editable content chunks for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
