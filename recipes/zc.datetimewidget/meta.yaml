package:
  name: zc.datetimewidget
  version: "0.7.0"

source:
  fn: zc.datetimewidget-0.7.0.tar.gz
  url: https://pypi.python.org/packages/source/z/zc.datetimewidget/zc.datetimewidget-0.7.0.tar.gz
  md5: cfb12768d4d55134e8be3f4ceef7d2ca
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zc.datetimewidget = zc.datetimewidget:main
    #
    # Would create an entry point called zc.datetimewidget that calls zc.datetimewidget.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - pytz
    - setuptools
    - zc.i18n
    - zc.resourcelibrary
    - zope.app.form
    - zope.component
    - zope.datetime
    - zope.interface
    - zope.publisher >=3.12
    - zope.schema

  run:
    - python
    - pytz
    - setuptools
    - zc.i18n
    - zc.resourcelibrary
    - zope.app.form
    - zope.component
    - zope.datetime
    - zope.interface
    - zope.publisher >=3.12
    - zope.schema

test:
  # Python imports
  imports:
    - zc
    - zc.datetimewidget
    - zc.datetimewidget.demo

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/zc.datetimewidget
  license: Zope Public License
  summary: 'Javascript-based widgets for date and datetime fields.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
