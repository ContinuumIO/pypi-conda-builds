package:
  name: django-thumbnails
  version: "0.1.1"

source:
  fn: django-thumbnails-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-thumbnails/django-thumbnails-0.1.1.tar.gz
  md5: 28a066ed5d2725d1312b5897a2be1a99
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-thumbnails = django-thumbnails:main
    #
    # Would create an entry point called django-thumbnails that calls django-thumbnails.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - da-vinci
    - shortuuid

  run:
    - python
    - django
    - da-vinci
    - shortuuid

test:
  # Python imports
  imports:
    - thumbnails

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ui/django-thumbnails
  license: MIT License
  summary: 'A simple Django app to manage image/photo thumbnails. Supports remote/cloud storage systems like Amazon S3.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
