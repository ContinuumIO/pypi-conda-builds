package:
  name: transdate
  version: "1.1.1"

source:
  fn: transdate-1.1.1.tar.gz
  url: https://pypi.python.org/packages/source/t/transdate/transdate-1.1.1.tar.gz
  md5: ec15374c4b1f3ab58674855824ff84a1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - transdate = transdate:main
    #
    # Would create an entry point called transdate that calls transdate.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - transdate

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://mearie.org/projects/transdate/
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Python implementation of Asian lunisolar calendar'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
