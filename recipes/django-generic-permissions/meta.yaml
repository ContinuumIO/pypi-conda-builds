package:
  name: django-generic-permissions
  version: "0.1.0"

source:
  fn: django-generic-permissions-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-generic-permissions/django-generic-permissions-0.1.0.tar.gz
  md5: b676f7215c11b7e8be369617ffd6694c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-generic-permissions = django-generic-permissions:main
    #
    # Would create an entry point called django-generic-permissions that calls django-generic-permissions.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4

  run:
    - python
    - django >=1.4

test:
  # Python imports
  imports:
    - django_generic_permissions

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/garrypolley/django_generic_permissions
  license: UNKNOWN
  summary: 'A generic permission framework for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
