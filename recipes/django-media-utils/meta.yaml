package:
  name: django-media-utils
  version: "0.1.0"

source:
  fn: django-media-utils-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-media-utils/django-media-utils-0.1.0.tar.gz
  md5: 5448513ebb20f415f03248cd34180a66
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-media-utils = django-media-utils:main
    #
    # Would create an entry point called django-media-utils that calls django-media-utils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - media_utils
    - media_utils.management
    - media_utils.management.commands
    - media_utils.tests
    - media_utils.tests.no_label_app
    - media_utils.tests.skip_app
    - media_utils.tests.test_app

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://launchpad.net/django-media-utils
  license: BSD License
  summary: 'Django utilities for managing static assets'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
