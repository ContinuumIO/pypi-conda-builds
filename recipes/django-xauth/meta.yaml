package:
  name: django-xauth
  version: "0.0.9"

source:
  fn: django-xauth-0.0.9.tar.gz
  url: https://pypi.python.org/packages/source/d/django-xauth/django-xauth-0.0.9.tar.gz
  md5: c1dc85b6ea7e4bef2fbd65d0ae4885fb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-xauth = django-xauth:main
    #
    # Would create an entry point called django-xauth that calls django-xauth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - xauth
    - xauth.management
    - xauth.management.commands
    - xauth.service
    - xauth.service.oauth1
    - xauth.service.oauth2
    - xauth.service.openid
    - xauth.service.openid.commands
    - xauth.service.simple

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/lorien/django-xauth/
  license: BSD License
  summary: 'All-in-one django registration application'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
