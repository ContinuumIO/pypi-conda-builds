package:
  name: indicngram
  version: "0.4"

source:
  fn: indicngram-0.4.tar.gz
  url: https://pypi.python.org/packages/source/i/indicngram/indicngram-0.4.tar.gz
  md5: 4f59a642aca70c2fff5d021b26b639ac
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - indicngram = indicngram:main
    #
    # Would create an entry point called indicngram that calls indicngram.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - indicsyllabifier

  run:
    - python
    - setuptools
    - indicsyllabifier

test:
  # Python imports
  imports:
    - indicngram
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://silpa.org.in/NGram
  license: LGPL-3.0
  summary: 'n-gram genereator for indic languages'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
