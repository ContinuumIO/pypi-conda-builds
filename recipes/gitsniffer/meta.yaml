package:
  name: gitsniffer
  version: "c3ed446"

source:
  fn: gitsniffer-c3ed446.tar.gz
  url: https://pypi.python.org/packages/source/g/gitsniffer/gitsniffer-c3ed446.tar.gz
  md5: 982aca8cfbb2c98f8fb791cd175204c4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gitsniffer = gitsniffer:main
    #
    # Would create an entry point called gitsniffer that calls gitsniffer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - gitsniffer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: GPLv3+
  summary: 'Find git repos where they live. In the CLOOUUD'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
