package:
  name: aldryn-reversion
  version: "0.0.3"

source:
  fn: aldryn-reversion-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/a/aldryn-reversion/aldryn-reversion-0.0.3.tar.gz
  md5: 3f0a3b99233e9519799529b25962dcd6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - aldryn-reversion = aldryn-reversion:main
    #
    # Would create an entry point called aldryn-reversion that calls aldryn-reversion.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-cms >=3.0.9
    - django-reversion >=1.8.2,<1.9

  run:
    - python
    - django-cms >=3.0.9
    - django-reversion >=1.8.2,<1.9

test:
  # Python imports
  imports:
    - aldryn_reversion

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/aldryn/aldryn-reversion
  license: BSD License
  summary: 'Support for django-reversion on models with translatable fields and django-cms placeholder fields.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
