package:
  name: sflvault-common
  version: "0.7.8.1"

source:
  fn: SFLvault-common-0.7.8.1.tar.gz
  url: https://pypi.python.org/packages/source/S/SFLvault-common/SFLvault-common-0.7.8.1.tar.gz
  md5: 9dbb168e5aa68d008e77a51bfc289e19
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sflvault-common = sflvault-common:main
    #
    # Would create an entry point called sflvault-common that calls sflvault-common.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pycrypto

  run:
    - python
    - setuptools
    - pycrypto

test:
  # Python imports
  imports:
    - sflvault
    - sflvault.common

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.sflvault.org
  license: GPLv3
  summary: 'Networked credentials store and authentication manager - Common'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
