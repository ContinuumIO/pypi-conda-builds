package:
  name: plone.app.redirector
  version: "1.2.1"

source:
  fn: plone.app.redirector-1.2.1.zip
  url: https://pypi.python.org/packages/source/p/plone.app.redirector/plone.app.redirector-1.2.1.zip
  md5: 4bc01dcfe6db8550996488df0b713518
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.app.redirector = plone.app.redirector:main
    #
    # Would create an entry point called plone.app.redirector that calls plone.app.redirector.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - plone.memoize

  run:
    - python
    - setuptools
    - plone.memoize

test:
  # Python imports
  imports:
    - plone
    - plone.app
    - plone.app.redirector
    - plone.app.redirector.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/plone.app.redirector
  license: GNU General Public License (GPL)
  summary: 'redirection tool'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
