package:
  name: kippo-extra
  version: "1.0.0"

source:
  fn: kippo-extra-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/k/kippo-extra/kippo-extra-1.0.0.tar.gz
  md5: d8e23b9f1ecb0e31ce9471b25fa61ee0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kippo-extra = kippo-extra:main
    #
    # Would create an entry point called kippo-extra that calls kippo-extra.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - kippo_extra
    - kippo_extra.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/basilfx/kippo-extra
  license: MIT License
  summary: 'Set of extra commands for the kippo SSH honeypot daemon'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
