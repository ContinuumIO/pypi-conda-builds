package:
  name: django-smart-manager
  version: "0.4.1"

source:
  fn: django-smart-manager-0.4.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-smart-manager/django-smart-manager-0.4.1.tar.gz
  md5: e3c8a3fbf81317cf9c76838fada4d1c8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-smart-manager = django-smart-manager:main
    #
    # Would create an entry point called django-smart-manager that calls django-smart-manager.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.7
    - django-manager-utils >=0.8.2
    - jsonfield >=0.9.20

  run:
    - python
    - django >=1.7
    - django-manager-utils >=0.8.2
    - jsonfield >=0.9.20

test:
  # Python imports
  imports:
    - smart_manager
    - smart_manager.migrations
    - smart_manager.tests
    - smart_manager.tests.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ambitioninc/django-smart-manager
  license: MIT License
  summary: 'Make templates that manage models'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
