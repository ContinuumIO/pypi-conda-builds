package:
  name: django-ajax-crawler
  version: "0.2dev"

source:
  fn: django-ajax-crawler-0.2dev.tar.gz
  url: https://pypi.python.org/packages/source/d/django-ajax-crawler/django-ajax-crawler-0.2dev.tar.gz
  md5: 789c7a43fd1cde1b9f314fc676496fdd
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-ajax-crawler = django-ajax-crawler:main
    #
    # Would create an entry point called django-ajax-crawler that calls django-ajax-crawler.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - selenium

  run:
    - python
    - setuptools
    - django
    - selenium

test:
  # Python imports
  imports:
    - ajaxcrawler

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: alex-web.ru
  license: MIT License
  summary: 'Handle page with dynamic data.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
