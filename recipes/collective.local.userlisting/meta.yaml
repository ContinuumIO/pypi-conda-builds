package:
  name: collective.local.userlisting
  version: "1.3"

source:
  fn: collective.local.userlisting-1.3.zip
  url: https://pypi.python.org/packages/source/c/collective.local.userlisting/collective.local.userlisting-1.3.zip
  md5: d3cbd296dcfacd91f03d351c531fed71
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.local.userlisting = collective.local.userlisting:main
    #
    # Would create an entry point called collective.local.userlisting that calls collective.local.userlisting.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.local
    - collective.local.userlisting

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/collective.local.userlisting
  license: GPL
  summary: 'Provides a view on contents that displays the list of users having a role on it.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
