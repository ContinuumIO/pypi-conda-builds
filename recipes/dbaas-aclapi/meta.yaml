package:
  name: dbaas-aclapi
  version: "0.0.40"

source:
  fn: dbaas-aclapi-0.0.40.tar.gz
  url: https://pypi.python.org/packages/source/d/dbaas-aclapi/dbaas-aclapi-0.0.40.tar.gz
  md5: 1a6d6e7c5a054e7d43411663764de630
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dbaas-aclapi = dbaas-aclapi:main
    #
    # Would create an entry point called dbaas-aclapi that calls dbaas-aclapi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - urllib3
    - dbaas-credentials

  run:
    - python
    - django
    - urllib3
    - dbaas-credentials

test:
  # Python imports
  imports:
    - dbaas_aclapi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/felippemr/dbaas-aclapi
  license: BSD License
  summary: 'ACL API integration for DBaaS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
