package:
  name: zope.app.localpermission
  version: "3.7.2"

source:
  fn: zope.app.localpermission-3.7.2.tar.gz
  url: https://pypi.python.org/packages/source/z/zope.app.localpermission/zope.app.localpermission-3.7.2.tar.gz
  md5: 121509781b19ce55ebe890fa408702fc
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zope.app.localpermission = zope.app.localpermission:main
    #
    # Would create an entry point called zope.app.localpermission that calls zope.app.localpermission.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zodb3
    - zope.component
    - zope.i18nmessageid
    - zope.interface
    - zope.location
    - zope.security

  run:
    - python
    - setuptools
    - zodb3
    - zope.component
    - zope.i18nmessageid
    - zope.interface
    - zope.location
    - zope.security

test:
  # Python imports
  imports:
    - zope
    - zope.app
    - zope.app.localpermission

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/zope.app.localpermission
  license: Zope Public License
  summary: 'Local Persistent Permissions for zope.security'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
