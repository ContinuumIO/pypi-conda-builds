package:
  name: filedownloader.py
  version: "0.4.0"

source:
  fn: fileDownloader.py-0.4.0.zip
  url: https://pypi.python.org/packages/source/f/fileDownloader.py/fileDownloader.py-0.4.0.zip
  md5: 487287a8a75be4cfc3ad015ca4008a58
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - filedownloader.py = filedownloader.py:main
    #
    # Would create an entry point called filedownloader.py that calls filedownloader.py.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - fileDownloader

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bantonj.wordpress.com/software/open-source/
  license: MIT
  summary: 'Downloads files via HTTP or FTP'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
