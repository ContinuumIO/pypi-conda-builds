package:
  name: djaodjin-signup
  version: "0.1.6"

source:
  fn: djaodjin-signup-0.1.6.tar.gz
  url: https://pypi.python.org/packages/source/d/djaodjin-signup/djaodjin-signup-0.1.6.tar.gz
  md5: 0e2093ea3f61a5877e7f829807daff97
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djaodjin-signup = djaodjin-signup:main
    #
    # Would create an entry point called djaodjin-signup that calls djaodjin-signup.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - signup
    - signup.backends
    - signup.urls

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/djaodjin/djaodjin-signup/
  license: BSD
  summary: "DjaoDjin's Implementation of Frictionless Sign Up"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
