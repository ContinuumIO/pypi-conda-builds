package:
  name: tinytag
  version: "0.8.0"

source:
  fn: tinytag-0.8.0.tar.gz
  url: https://pypi.python.org/packages/source/t/tinytag/tinytag-0.8.0.tar.gz
  md5: b76520de844e4d1fb83d5cffc7dd4632
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tinytag = tinytag:main
    #
    # Would create an entry point called tinytag that calls tinytag.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - audiotranscode
    - audiotranscode.test
    - tinytag
    - tinytag.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/devsnd/tinytag/
  license: GNU General Public License v3 (GPLv3)
  summary: 'Read music meta data and length of MP3, OGG, FLAC and Wave files'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
