package:
  name: markgen
  version: "0.9.6"

source:
  fn: markgen-0.9.6.tar.gz
  url: https://pypi.python.org/packages/source/m/markgen/markgen-0.9.6.tar.gz
  md5: c5bfc72bd3fa7784368089c16e4b6f56
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - markgen = markgen:main
    #
    # Would create an entry point called markgen that calls markgen.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - markgen

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/1stvamp/markgen.py
  license: BSD
  summary: 'Python library for the programmatic generation of markdown text'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
