package:
  name: odpdown
  version: "0.4.1"

source:
  fn: odpdown-0.4.1.tar.gz
  url: https://pypi.python.org/packages/source/o/odpdown/odpdown-0.4.1.tar.gz
  md5: 78f5d2f28ef4f7bde9c378fa38fe8261
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - odpdown = odpdown:main
    #
    # Would create an entry point called odpdown that calls odpdown.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - mistune >=0.5
    - lpod-python >=1.1.6
    - pygments >=1.6
    - pillow >=2.0

  run:
    - python
    - mistune >=0.5
    - lpod-python >=1.1.6
    - pygments >=1.6
    - pillow >=2.0

test:
  # Python imports
  imports:
    - odpdown

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/thorstenb/odpdown.git
  license: BSD License
  summary: 'Generate OpenDocument Presentation (odp) files from markdown'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
