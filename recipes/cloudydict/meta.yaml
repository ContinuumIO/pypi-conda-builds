package:
  name: cloudydict
  version: "0.0.19"

source:
  fn: cloudydict-0.0.19.tar.gz
  url: https://pypi.python.org/packages/source/c/cloudydict/cloudydict-0.0.19.tar.gz
  md5: 4154a95ef9c95529e8649b0b6286a4e7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cloudydict = cloudydict:main
    #
    # Would create an entry point called cloudydict that calls cloudydict.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - boto
    - python-cloudfiles

  run:
    - python
    - boto
    - python-cloudfiles

test:
  # Python imports
  imports:
    - cloudydict

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://wnyc.github.io/cloudydict/
  license: MIT License
  summary: 'Dictionary interface to cloud providers.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
