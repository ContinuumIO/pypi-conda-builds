package:
  name: infrae.fileupload
  version: "1.0.1"

source:
  fn: infrae.fileupload-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/i/infrae.fileupload/infrae.fileupload-1.0.1.tar.gz
  md5: cd10f604d30635c83feb0dac8a7f9333
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - infrae.fileupload = infrae.fileupload:main
    #
    # Would create an entry point called infrae.fileupload that calls infrae.fileupload.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - webob

  run:
    - python
    - setuptools
    - setuptools
    - webob

test:
  # Python imports
  imports:
    - infrae
    - infrae.fileupload

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://infrae.com/products/silva
  license: BSD License
  summary: 'WSGI middleware to manage upload of files'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
