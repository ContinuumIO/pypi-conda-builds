package:
  name: pyautocad
  version: "0.1.3"

source:
  fn: pyautocad-0.1.3.zip
  url: https://pypi.python.org/packages/source/p/pyautocad/pyautocad-0.1.3.zip
  md5: bba0a60a64915e3d9fb2d797f1245ad9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pyautocad = pyautocad:main
    #
    # Would create an entry point called pyautocad that calls pyautocad.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - comtypes >=1.1.1

  run:
    - python
    - comtypes >=1.1.1

test:
  # Python imports
  imports:
    - pyautocad
    - pyautocad.contrib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/reclosedev/pyautocad
  license: BSD License
  summary: 'AutoCAD Automation for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
