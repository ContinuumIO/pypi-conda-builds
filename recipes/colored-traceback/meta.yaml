package:
  name: colored-traceback
  version: "0.2.1"

source:
  fn: colored-traceback-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/c/colored-traceback/colored-traceback-0.2.1.tar.gz
  md5: 3a893494601ef1f4181293ba5ef13eba
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - colored-traceback = colored-traceback:main
    #
    # Would create an entry point called colored-traceback that calls colored-traceback.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pygments

  run:
    - python
    - pygments

test:
  # Python imports
  imports:
    - colored_traceback
    - colored_traceback.always
    - colored_traceback.auto

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.github.com/staticshock/colored-traceback.py
  license: ISC License (ISCL)
  summary: 'Automatically color uncaught exception tracebacks'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
