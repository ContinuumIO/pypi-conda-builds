package:
  name: act_as_executable
  version: "1.0"

source:
  fn: act_as_executable-1.0.tar.gz
  url: https://pypi.python.org/packages/source/a/act_as_executable/act_as_executable-1.0.tar.gz
  md5: 7d317e18dfbd32b2b35654fbb145e1f1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - act_as_executable = act_as_executable:main
    #
    # Would create an entry point called act_as_executable that calls act_as_executable.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - envoy

  run:
    - python
    - envoy

test:
  # Python imports
  imports:
    - act_as_executable

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD
  summary: 'A Python wrapper to build CLI wrappers'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
