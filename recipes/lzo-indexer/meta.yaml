package:
  name: lzo-indexer
  version: "0.0.1"

source:
  fn: lzo-indexer-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/l/lzo-indexer/lzo-indexer-0.0.1.tar.gz
  md5: 0f32b5e513ecd7fda11138c55f3599f8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lzo-indexer = lzo-indexer:main
    #
    # Would create an entry point called lzo-indexer that calls lzo-indexer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - lzo_indexer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/duedil-ltd/python-lzo-indexer
  license: lzw
  summary: 'Library for indexing LZO compressed files'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
