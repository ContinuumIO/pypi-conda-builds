package:
  name: pelican-extended-sitemap
  version: "1.0.2"

source:
  fn: pelican-extended-sitemap-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/p/pelican-extended-sitemap/pelican-extended-sitemap-1.0.2.tar.gz
  md5: 1106f712c2c59d37dc91107687225b30
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pelican-extended-sitemap = pelican-extended-sitemap:main
    #
    # Would create an entry point called pelican-extended-sitemap that calls pelican-extended-sitemap.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - extended_sitemap
    - extended_sitemap.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dArignac/pelican-extended-sitemap
  license: MIT License
  summary: 'sitemap generator plugin for pelican'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
