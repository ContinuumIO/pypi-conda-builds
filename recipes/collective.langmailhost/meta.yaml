package:
  name: collective.langmailhost
  version: "0.3.5"

source:
  fn: collective.langMailHost-0.3.5.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.langMailHost/collective.langMailHost-0.3.5.tar.gz
  md5: 3b0b945bf7f1c07a88cccbe5ffaff9ed
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.langmailhost = collective.langmailhost:main
    #
    # Would create an entry point called collective.langmailhost that calls collective.langmailhost.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.langMailHost
    - collective.langMailHost.Extensions
    - collective.langMailHost.adapter
    - collective.langMailHost.browser
    - collective.langMailHost.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/collective.langMailHost
  license: GPL
  summary: 'This package aims to provide language dependent character set for sending emails from Plone site.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
