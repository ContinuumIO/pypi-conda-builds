package:
  name: djangopypi
  version: "0.4.4"

source:
  fn: djangopypi-0.4.4.tar.gz
  url: https://pypi.python.org/packages/source/d/djangopypi/djangopypi-0.4.4.tar.gz
  md5: c5c2cd2fbb704d41a86856ce30d5b2d8
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangopypi = djangopypi:main
    #
    # Would create an entry point called djangopypi that calls djangopypi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pkginfo
    - docutils

  run:
    - python
    - setuptools
    - pkginfo
    - docutils

test:
  # Python imports
  imports:
    - djangopypi
    - djangopypi.management
    - djangopypi.management.commands
    - djangopypi.migrations
    - djangopypi.templatetags
    - djangopypi.tests
    - djangopypi.views

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/benliles/djangopypi
  license: BSD License
  summary: 'A Django application that emulates the Python Package Index.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
