package:
  name: django-orderable
  version: "3.1.0"

source:
  fn: django-orderable-3.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-orderable/django-orderable-3.1.0.tar.gz
  md5: 6d3b2b8e8c95e0fdca526536610df268
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-orderable = django-orderable:main
    #
    # Would create an entry point called django-orderable that calls django-orderable.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - orderable
    - orderable.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/incuna/django-orderable
  license: BSD License
  summary: 'Orderable model and admin'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
