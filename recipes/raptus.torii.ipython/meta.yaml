package:
  name: raptus.torii.ipython
  version: "1.0b1"

source:
  fn: raptus.torii.ipython-1.0b1.tar.gz
  url: https://pypi.python.org/packages/source/r/raptus.torii.ipython/raptus.torii.ipython-1.0b1.tar.gz
  md5: 08858bcbf93e5bedf7ae1dd1d30b748d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - raptus.torii.ipython = raptus.torii.ipython:main
    #
    # Would create an entry point called raptus.torii.ipython that calls raptus.torii.ipython.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - ipython
    - twisted >=8.0.1

  run:
    - python
    - setuptools
    - setuptools
    - ipython
    - twisted >=8.0.1

test:
  # Python imports
  imports:
    - raptus
    - raptus.torii
    - raptus.torii.ipython

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/raptus.ipython
  license: GPL
  summary: 'extends raptus.torii with ipython'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
