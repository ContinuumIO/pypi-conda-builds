package:
  name: django-envelope
  version: "1.0"

source:
  fn: django-envelope-1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-envelope/django-envelope-1.0.tar.gz
  md5: 3d3a9befbb23c0b20f2e3062939f7a03
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-envelope = django-envelope:main
    #
    # Would create an entry point called django-envelope that calls django-envelope.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5

  run:
    - python
    - django >=1.5

test:
  # Python imports
  imports:
    - envelope
    - envelope.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/zsiciarz/django-envelope
  license: MIT License
  summary: 'A contact form app for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
