package:
  name: biryani
  version: "0.10.4"

source:
  fn: Biryani-0.10.4.tar.gz
  url: https://pypi.python.org/packages/source/B/Biryani/Biryani-0.10.4.tar.gz
  md5: 3b419cf78e07f7135e45a5592ba8b275
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - biryani = biryani:main
    #
    # Would create an entry point called biryani that calls biryani.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - babel >=0.9.4

  run:
    - python
    - babel >=0.9.4

test:
  # Python imports
  imports:
    - biryani
    - biryani.nonstandard

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://biryani.readthedocs.org/
  license: Apache Software License
  summary: 'Toolbox to convert and validate data (for web forms, CSV files, XML files,'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
