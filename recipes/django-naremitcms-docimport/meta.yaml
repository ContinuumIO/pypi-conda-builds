package:
  name: django-naremitcms-docimport
  version: "0.0.5"

source:
  fn: django-naremitcms-docimport-0.0.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-naremitcms-docimport/django-naremitcms-docimport-0.0.5.tar.gz
  md5: ae9042dd57725efa4cae04d728639cf2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-naremitcms-docimport = django-naremitcms-docimport:main
    #
    # Would create an entry point called django-naremitcms-docimport that calls django-naremitcms-docimport.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - naremitcms_docimport
    - naremitcms_docimport.management
    - naremitcms_docimport.management.commands
    - naremitcms_docimport.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://naremitcms.com
  license: BSD License
  summary: 'A static text file import tool for NaremitCMS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
