package:
  name: django-emailauth
  version: "0.1"

source:
  fn: django-emailauth-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-emailauth/django-emailauth-0.1.tar.gz
  md5: 3141e80f00a5d45095b034ad1f6fec5d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-emailauth = django-emailauth:main
    #
    # Would create an entry point called django-emailauth that calls django-emailauth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - emailauth
    - emailauth.management
    - emailauth.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/redvasily/django-emailauth/tree/master/
  license: BSD License
  summary: 'User email authentication application for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
