package:
  name: googlefinance
  version: "0.7"

source:
  fn: googlefinance-0.7.tar.gz
  url: https://pypi.python.org/packages/source/g/googlefinance/googlefinance-0.7.tar.gz
  md5: 5fb4dc76b5f6bb6a0e75301b16ae11a8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - googlefinance = googlefinance:main
    #
    # Would create an entry point called googlefinance that calls googlefinance.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - googlefinance

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/hongtaocai/googlefinance
  license: MIT License
  summary: 'Python module to get real-time (no delay) stock data from Google Finance API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
