package:
  name: hyperspectral
  version: "0.1.8"

source:
  fn: hyperspectral-0.1.8.tar.gz
  url: https://pypi.python.org/packages/source/h/hyperspectral/hyperspectral-0.1.8.tar.gz
  md5: 099bd2b281fd23acd3d8421673fe3856
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hyperspectral = hyperspectral:main
    #
    # Would create an entry point called hyperspectral that calls hyperspectral.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - hyperspectral

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/irap-omp/hyperspectral
  license: Science!
  summary: 'A frugal python package with some hyperspectral data goodies.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
