package:
  name: lazylights
  version: "2.0.0rc1"

source:
  fn: lazylights-2.0.0rc1.tar.gz
  url: https://pypi.python.org/packages/source/l/lazylights/lazylights-2.0.0rc1.tar.gz
  md5: 9b5a8d03802a88e8a7d68286fc5da072
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lazylights = lazylights:main
    #
    # Would create an entry point called lazylights that calls lazylights.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - lazylights

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/mpapi/lazylights/
  license: LICENSE
  summary: 'Python tools for controlling Lifx bulbs.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
