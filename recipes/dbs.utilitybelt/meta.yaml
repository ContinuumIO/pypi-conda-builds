package:
  name: dbs.utilitybelt
  version: "0.1.2"

source:
  fn: dbs.utilitybelt-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/dbs.utilitybelt/dbs.utilitybelt-0.1.2.tar.gz
  md5: da6611b1d679f83f355b46d316b75243
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dbs.utilitybelt = dbs.utilitybelt:main
    #
    # Would create an entry point called dbs.utilitybelt that calls dbs.utilitybelt.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - dbs
    - dbs.utilitybelt

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.dreambitstudios.com
  license: BSD License
  summary: 'Utilities package collected in Dream Bit Studios.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
