package:
  name: lpdistutils
  version: "0.1"

source:
  fn: lpdistutils-0.1.tar.gz
  url: https://pypi.python.org/packages/source/l/lpdistutils/lpdistutils-0.1.tar.gz
  md5: ec2f2477f420bc875f15aeaf858f36d1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lpdistutils = lpdistutils:main
    #
    # Would create an entry point called lpdistutils that calls lpdistutils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - launchpadlib
    - bzr

  run:
    - python
    - launchpadlib
    - bzr

test:
  # Python imports
  imports:
    - lpdistutils
    - lpdistutils.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://launchpad.net/lpdistutils
  license: GNU General Public License (GPL)
  summary: 'Distutils commands for working with Launchpad and Bazaar.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
