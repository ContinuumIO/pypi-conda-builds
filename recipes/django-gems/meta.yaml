package:
  name: django-gems
  version: "0.3.0"

source:
  fn: django-gems-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-gems/django-gems-0.3.0.tar.gz
  md5: c1072778e6ac44cf5ee0c4b25713fa75
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-gems = django-gems:main
    #
    # Would create an entry point called django-gems that calls django-gems.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_gems
    - django_gems.db
    - django_gems.db.models
    - django_gems.templatetags
    - django_gems.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plfiorini.github.com/django-gems
  license: BSD License
  summary: 'Django utilities library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
