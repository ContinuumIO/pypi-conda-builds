package:
  name: pyramid-classy
  version: "0.4.2"

source:
  fn: Pyramid-Classy-0.4.2.zip
  url: https://pypi.python.org/packages/source/P/Pyramid-Classy/Pyramid-Classy-0.4.2.zip
  md5: 3b7b264a1c6fc28d5b77503e5092d9ba
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pyramid-classy = pyramid-classy:main
    #
    # Would create an entry point called pyramid-classy that calls pyramid-classy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyramid

  run:
    - python
    - pyramid

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tark-hidden/pyramid_classy
  license: BSD License
  summary: 'Class-based views for Pyramid'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
