package:
  name: django-paginationlinks
  version: "0.1"

source:
  fn: django-paginationlinks-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-paginationlinks/django-paginationlinks-0.1.tar.gz
  md5: f42a848802b6d96391494c37a754d9f4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-paginationlinks = django-paginationlinks:main
    #
    # Would create an entry point called django-paginationlinks that calls django-paginationlinks.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - paginationlinks
    - paginationlinks.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/blancltd/django-paginationlinks
  license: BSD License
  summary: 'Django Pagination Links'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
