package:
  name: cmph-cffi
  version: "0.3.0"

source:
  fn: cmph-cffi-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/c/cmph-cffi/cmph-cffi-0.3.0.tar.gz
  md5: 70d8295ba9b081cfdfce4b28193c6349
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cmph-cffi = cmph-cffi:main
    #
    # Would create an entry point called cmph-cffi that calls cmph-cffi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cffi >=0.8
    - six

  run:
    - python
    - cffi >=0.8
    - six

test:
  # Python imports
  imports:
    - cmph

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/URXtech/cmph-cffi/
  license: GNU Lesser General Public License v2 or later (LGPLv2+) or Mozilla Public License 1.1 (MPL 1.1)
  summary: 'CFFI enabled bindings to the CMPH library forcreating and using minimal perfect hashes'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
