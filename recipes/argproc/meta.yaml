package:
  name: argproc
  version: "1.4"

source:
  fn: argproc-1.4.tar.gz
  url: https://pypi.python.org/packages/source/a/argproc/argproc-1.4.tar.gz
  md5: 14453d3861ce8ba9d471e50d4783806b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - argproc = argproc:main
    #
    # Would create an entry point called argproc that calls argproc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - ply >=3.3
    - nose

  run:
    - python
    - ply >=3.3
    - nose

test:
  # Python imports
  imports:
    - argproc
    - argproc.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/geertj/argproc
  license: MIT License
  summary: 'A rule-based arguments processor'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
