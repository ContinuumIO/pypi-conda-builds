package:
  name: django-birthday
  version: "0.1.1"

source:
  fn: django-birthday-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-birthday/django-birthday-0.1.1.tar.gz
  md5: 9d9a40e16467c77aaaffda52a954d744
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-birthday = django-birthday:main
    #
    # Would create an entry point called django-birthday that calls django-birthday.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - birthday
    - testapp

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/ojii/django-birthday
  license: UNKNOWN
  summary: 'Helper field and manager for working with birthdays'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
