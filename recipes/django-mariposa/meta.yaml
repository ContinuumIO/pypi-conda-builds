package:
  name: django-mariposa
  version: "0.1"

source:
  fn: django-mariposa-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-mariposa/django-mariposa-0.1.tar.gz
  md5: 686196b9ad470d3adcabe5ba7eb72006
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-mariposa = django-mariposa:main
    #
    # Would create an entry point called django-mariposa that calls django-mariposa.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - mariposa ==0.1

  run:
    - python
    - mariposa ==0.1

test:
  # Python imports
  imports:
    - django_mariposa
    - django_mariposa.management
    - django_mariposa.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/skyl/django-mariposa
  license: MIT
  summary: 'django-mariposa'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
