package:
  name: collective.collage.megamenu
  version: "1.1.2"

source:
  fn: collective.collage.megamenu-1.1.2.zip
  url: https://pypi.python.org/packages/source/c/collective.collage.megamenu/collective.collage.megamenu-1.1.2.zip
  md5: bf8fc5265d9a7ed124ab6935a1d87833
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.collage.megamenu = collective.collage.megamenu:main
    #
    # Would create an entry point called collective.collage.megamenu that calls collective.collage.megamenu.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.collage
    - plone.browserlayer
    - plone.app.registry
    - collective.monkeypatcher
    - z3c.form
    - collective.collage.nested

  run:
    - python
    - setuptools
    - setuptools
    - products.collage
    - plone.browserlayer
    - plone.app.registry
    - collective.monkeypatcher
    - z3c.form
    - collective.collage.nested

test:
  # Python imports
  imports:
    - collective
    - collective.collage
    - collective.collage.megamenu
    - collective.collage.megamenu.browser
    - collective.collage.megamenu.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/Products.Collage/addons/collective.collage.megamenu
  license: GPL
  summary: 'A mega drop down menu implementation using Products.Collage'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
