package:
  name: blanc-basic-pages
  version: "0.3.2"

source:
  fn: blanc-basic-pages-0.3.2.tar.gz
  url: https://pypi.python.org/packages/source/b/blanc-basic-pages/blanc-basic-pages-0.3.2.tar.gz
  md5: 224ec28dce92822983d67d09b3ca92f5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - blanc-basic-pages = blanc-basic-pages:main
    #
    # Would create an entry point called blanc-basic-pages that calls blanc-basic-pages.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-mptt >=0.6.1
    - django-mptt-admin >=0.1.8

  run:
    - python
    - django-mptt >=0.6.1
    - django-mptt-admin >=0.1.8

test:
  # Python imports
  imports:
    - blanc_basic_pages
    - blanc_basic_pages.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/blancltd/blanc-basic-pages
  license: BSD License
  summary: 'Blanc Basic Pages for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
