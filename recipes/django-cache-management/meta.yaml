package:
  name: django-cache-management
  version: "1.0.0"

source:
  fn: django-cache-management-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-cache-management/django-cache-management-1.0.0.tar.gz
  md5: 66d81a971b90ebe359ace0992e102ed0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-cache-management = django-cache-management:main
    #
    # Would create an entry point called django-cache-management that calls django-cache-management.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6,<1.9
    - django-nose >=1.3,<2
    - django-redis >=3.8.1,<4
    - six

  run:
    - python
    - django >=1.6,<1.9
    - django-nose >=1.3,<2
    - django-redis >=3.8.1,<4
    - six

test:
  # Python imports
  imports:
    - django_cache_management
    - django_cache_management.management
    - django_cache_management.management.commands
    - settings

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lddubeau/django-cache-management
  license: Mozilla Public License 2.0 (MPL 2.0)
  summary: 'Cache management for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
