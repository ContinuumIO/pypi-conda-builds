package:
  name: bob.db.replay
  version: "2.0.2"

source:
  fn: bob.db.replay-2.0.2.zip
  url: https://pypi.python.org/packages/source/b/bob.db.replay/bob.db.replay-2.0.2.zip
  md5: 0fef775d7d4c31fe2b2735112f594862
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bob.db.replay = bob.db.replay:main
    #
    # Would create an entry point called bob.db.replay that calls bob.db.replay.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - six
    - sqlalchemy
    - antispoofing.utils
    - bob.db.base

  run:
    - python
    - setuptools
    - setuptools
    - six
    - sqlalchemy
    - antispoofing.utils
    - bob.db.base

test:
  # Python imports
  imports:
    - bob
    - bob.db
    - bob.db.replay

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/bob.db.replay
  license: GNU General Public License v3 (GPLv3)
  summary: 'Replay Attack Database Access API for Bob'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
