package:
  name: jujuclient
  version: "0.50.1"

source:
  fn: jujuclient-0.50.1.tar.gz
  url: https://pypi.python.org/packages/source/j/jujuclient/jujuclient-0.50.1.tar.gz
  md5: c164a9b94017777f035f30b7f8e8577b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jujuclient = jujuclient:main
    #
    # Would create an entry point called jujuclient that calls jujuclient.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - websocket-client >=0.18.0

  run:
    - python
    - websocket-client >=0.18.0

test:
  # Python imports
  imports:
    - jujuclient

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://juju.ubuntu.com
  license: UNKNOWN
  summary: 'A juju-core/gojuju simple synchronous python api client.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
