package:
  name: python-iqm
  version: "0.2.1"

source:
  fn: python-iqm-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/p/python-iqm/python-iqm-0.2.1.tar.gz
  md5: dcb3a3633e07d6c57da910876d5d81a8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-iqm = python-iqm:main
    #
    # Would create an entry point called python-iqm that calls python-iqm.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ClockworkNet/python-iqm
  license: MIT License
  summary: 'Interquartile Mean pure-Python module'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
