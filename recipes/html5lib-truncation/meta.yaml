package:
  name: html5lib-truncation
  version: "0.1.0"

source:
  fn: html5lib-truncation-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/h/html5lib-truncation/html5lib-truncation-0.1.0.tar.gz
  md5: 7e41e3c92ce9fdd8590c7899415dc056
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - html5lib-truncation = html5lib-truncation:main
    #
    # Would create an entry point called html5lib-truncation that calls html5lib-truncation.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - html5lib

  run:
    - python
    - html5lib

test:
  # Python imports
  imports:
    - html5lib_truncation
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tonyseek/html5lib-truncation
  license: MIT License
  summary: 'Truncating HTML with html5lib filter'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
