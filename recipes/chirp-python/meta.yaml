package:
  name: chirp-python
  version: "1.0.0"

source:
  fn: chirp-python-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/c/chirp-python/chirp-python-1.0.0.tar.gz
  md5: 1c400d1e86326e08834a729c80d77daa
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - chirp-python = chirp-python:main
    #
    # Would create an entry point called chirp-python that calls chirp-python.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - chirp

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rahulmadhavan/chirp-python
  license: LICENSE
  summary: 'to discover services using chirp on the same network'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
