package:
  name: cubicweb-intranet
  version: "1.1.0"

source:
  fn: cubicweb-intranet-1.1.0.tar.gz
  url: https://pypi.python.org/packages/source/c/cubicweb-intranet/cubicweb-intranet-1.1.0.tar.gz
  md5: 141c2f5b4543123e0833f7e554d40b4f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cubicweb-intranet = cubicweb-intranet:main
    #
    # Would create an entry point called cubicweb-intranet that calls cubicweb-intranet.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cubicweb-tag
    - cubicweb-link
    - cubicweb >=3.8.0
    - cubicweb-event
    - cubicweb-book
    - cubicweb-folder
    - cubicweb-comment
    - cubicweb-card
    - cubicweb-task
    - cubicweb-blog
    - cubicweb-file >=1.9.0

  run:
    - python
    - cubicweb-tag
    - cubicweb-link
    - cubicweb >=3.8.0
    - cubicweb-event
    - cubicweb-book
    - cubicweb-folder
    - cubicweb-comment
    - cubicweb-card
    - cubicweb-task
    - cubicweb-blog
    - cubicweb-file >=1.9.0

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://cubicweb.org/project/cubicweb-intranet
  license: LGPL
  summary: 'an intranet built on the CubicWeb framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
