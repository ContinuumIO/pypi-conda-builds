package:
  name: hyperkitty
  version: "1.0.1"

source:
  fn: HyperKitty-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/H/HyperKitty/HyperKitty-1.0.1.tar.gz
  md5: 1e8ceeea80520edaa41f50a4e3f4ada1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hyperkitty = hyperkitty:main
    #
    # Would create an entry point called hyperkitty that calls hyperkitty.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6
    - django-gravatar2 >=1.0.6
    - python-social-auth >=0.2.3
    - djangorestframework >=3.0.0
    - django-crispy-forms >=1.4.0
    - rjsmin >=1.0.6
    - cssmin >=0.2.0
    - robot-detection >=0.3
    - pytz >=2012
    - django-paintstore >=0.1.2
    - django-compressor >=1.3
    - django-browserid >=0.11.1
    - mailmanclient >=1.0.0b1
    - python-dateutil <2.0
    - networkx >=1.9.1
    - enum34 >=1.0
    - django-haystack >=2.1.0
    - django-extensions >=1.3.7
    - lockfile >=0.9.1

  run:
    - python
    - django >=1.6
    - django-gravatar2 >=1.0.6
    - python-social-auth >=0.2.3
    - djangorestframework >=3.0.0
    - django-crispy-forms >=1.4.0
    - rjsmin >=1.0.6
    - cssmin >=0.2.0
    - robot-detection >=0.3
    - pytz >=2012
    - django-paintstore >=0.1.2
    - django-compressor >=1.3
    - django-browserid >=0.11.1
    - mailmanclient >=1.0.0b1
    - python-dateutil <2.0
    - networkx >=1.9.1
    - enum34 >=1.0
    - django-haystack >=2.1.0
    - django-extensions >=1.3.7
    - lockfile >=0.9.1

test:
  # Python imports
  imports:
    - hyperkitty
    - hyperkitty.api
    - hyperkitty.jobs
    - hyperkitty.lib
    - hyperkitty.management
    - hyperkitty.management.commands
    - hyperkitty.migrations
    - hyperkitty.south_migrations
    - hyperkitty.templatetags
    - hyperkitty.tests
    - hyperkitty.tests.commands
    - hyperkitty.tests.lib
    - hyperkitty.tests.views
    - hyperkitty.views

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://gitlab.com/mailman/hyperkitty
  license: GNU General Public License v3 (GPLv3)
  summary: 'A web interface to access GNU Mailman v3 archives'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
