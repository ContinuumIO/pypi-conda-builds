package:
  name: asyncirc
  version: "0.0.3"

source:
  fn: AsyncIRC-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/A/AsyncIRC/AsyncIRC-0.0.3.tar.gz
  md5: f601b6ab15042757fdf79e250fa90d77
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - asyncirc = asyncirc:main
    #
    # Would create an entry point called asyncirc that calls asyncirc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - asyncirc

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kageurufu/asyncirc
  license: UNKNOWN
  summary: 'Dependancy-free Asynchronous IRC Library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
