package:
  name: theslasher
  version: "0.1"

source:
  fn: theslasher-0.1.tar.gz
  url: https://pypi.python.org/packages/source/t/theslasher/theslasher-0.1.tar.gz
  md5: 52fa41bcb141a8dad6bc99535dd0998f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - theslasher = theslasher:main
    #
    # Would create an entry point called theslasher that calls theslasher.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - webob
    - paste
    - pastescript

  run:
    - python
    - setuptools
    - webob
    - paste
    - pastescript

test:
  # Python imports
  imports:
    - theslasher

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://k0s.org
  license: UNKNOWN
  summary: 'removes trailing slashes'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
