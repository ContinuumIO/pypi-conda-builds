package:
  name: django-pipeline
  version: "1.5.0"

source:
  fn: django-pipeline-1.5.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-pipeline/django-pipeline-1.5.0.tar.gz
  md5: a15f21e72767d15d91dd781e009dbddb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-pipeline = django-pipeline:main
    #
    # Would create an entry point called django-pipeline that calls django-pipeline.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - futures >=2.1.3

  run:
    - python
    - futures >=2.1.3

test:
  # Python imports
  imports:
    - pipeline
    - pipeline.compilers
    - pipeline.compressors
    - pipeline.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/cyberdelia/django-pipeline
  license: MIT License
  summary: 'Pipeline is an asset packaging library for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
