package:
  name: kss.plugin.yuidnd
  version: "1.1"

source:
  fn: kss.plugin.yuidnd-1.1.zip
  url: https://pypi.python.org/packages/source/k/kss.plugin.yuidnd/kss.plugin.yuidnd-1.1.zip
  md5: 9771b2c3a37d66024e810f4cce94dce5
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kss.plugin.yuidnd = kss.plugin.yuidnd:main
    #
    # Would create an entry point called kss.plugin.yuidnd that calls kss.plugin.yuidnd.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - kss
    - kss.plugin
    - kss.plugin.yuidnd
    - kss.plugin.yuidnd.demo

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://plone.org/products/extreme-management-tool/
  license: GPL
  summary: 'A plugin for KSS that provides drag-and-drop based on yahoo.ui'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
