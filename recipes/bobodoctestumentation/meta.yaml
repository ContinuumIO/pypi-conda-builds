package:
  name: bobodoctestumentation
  version: "2.2.0"

source:
  fn: bobodoctestumentation-2.2.0.tar.gz
  url: https://pypi.python.org/packages/source/b/bobodoctestumentation/bobodoctestumentation-2.2.0.tar.gz
  md5: 3995e88d25b5fbea6ee6fca9442b6c34
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bobodoctestumentation = bobodoctestumentation:main
    #
    # Would create an entry point called bobodoctestumentation that calls bobodoctestumentation.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - manuel
    - six
    - webtest
    - zope.testing

  run:
    - python
    - manuel
    - six
    - webtest
    - zope.testing

test:
  # Python imports
  imports:
    - bobodoctestumentation

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.python.org/pypi/bobodoctestumentation
  license: ZPL 2.1
  summary: 'Bobo tests and documentation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
