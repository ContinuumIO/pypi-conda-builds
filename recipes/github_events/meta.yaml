package:
  name: github_events
  version: "0.0.3"

source:
  fn: github_events-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/g/github_events/github_events-0.0.3.tar.gz
  md5: 2d838e7e5fa47bce0ecf3b2b8906f7d6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - github_events = github_events:main
    #
    # Would create an entry point called github_events that calls github_events.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - github

  run:
    - python
    - github

test:
  # Python imports
  imports:
    - github_events

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/cancerhermit/PyGithubEvents/
  license: GNU General Public License (GPL)
  summary: 'Shorthand way to get user github events'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
