package:
  name: products.simplecalendar
  version: "1.0.1b4"

source:
  fn: Products.SimpleCalendar-1.0.1b4.tar.gz
  url: https://pypi.python.org/packages/source/P/Products.SimpleCalendar/Products.SimpleCalendar-1.0.1b4.tar.gz
  md5: ec2ef700f3107a527a7a0b4a19238207
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - products.simplecalendar = products.simplecalendar:main
    #
    # Would create an entry point called products.simplecalendar that calls products.simplecalendar.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - dateable.chronos ==0.8
    - dateable.kalends ==0.5

  run:
    - python
    - setuptools
    - setuptools
    - dateable.chronos ==0.8
    - dateable.kalends ==0.5

test:
  # Python imports
  imports:
    - Products
    - Products.SimpleCalendar
    - Products.SimpleCalendar.content

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.nidelven-it.no/d
  license: GPL
  summary: 'Simple to install and use calendaring solution'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
