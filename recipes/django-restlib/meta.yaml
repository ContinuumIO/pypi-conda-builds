package:
  name: django-restlib
  version: "1.0"

source:
  fn: django-restlib-1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-restlib/django-restlib-1.0.tar.gz
  md5: 7f442053b0b1e8168f699c6af0efd869
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-restlib = django-restlib:main
    #
    # Would create an entry point called django-restlib that calls django-restlib.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - restlib
    - restlib.conf
    - restlib.representations
    - restlib.resources
    - restlib.resources.middleware
    - restlib.resources.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bruth/django-restlib
  license: BSD License
  summary: 'Django REST API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
