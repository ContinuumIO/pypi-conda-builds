package:
  name: coopr.plugins
  version: "3.1"

source:
  fn: coopr.plugins-3.1.tar.gz
  url: https://pypi.python.org/packages/source/c/coopr.plugins/coopr.plugins-3.1.tar.gz
  md5: 3abf520bd9c162618e3d13ca83f84e1a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - coopr.plugins = coopr.plugins:main
    #
    # Would create an entry point called coopr.plugins that calls coopr.plugins.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - coopr
    - coopr.plugins
    - coopr.plugins.converter
    - coopr.plugins.smanager
    - coopr.plugins.solvers
    - coopr.plugins.testdriver
    - coopr.plugins.tests
    - coopr.plugins.tests.mip
    - coopr.plugins.tests.pickling

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://software.sandia.gov/svn/public/coopr/coopr.plugins
  license: BSD License
  summary: 'Plugins that are typically bundled with Coopr'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
