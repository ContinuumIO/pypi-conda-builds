package:
  name: configen
  version: "0.0.1"

source:
  fn: configen-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/configen/configen-0.0.1.tar.gz
  md5: c24fc50bca5aa2099f488d0ddc22363f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - configen = configen:main
    #
    # Would create an entry point called configen that calls configen.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - configen

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tengu/configen
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'instantiate a template from various parameter representations.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
