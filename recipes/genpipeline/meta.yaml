package:
  name: genpipeline
  version: "0.1.2"

source:
  fn: genpipeline-0.1.2.zip
  url: https://pypi.python.org/packages/source/g/genpipeline/genpipeline-0.1.2.zip
  md5: ea53f3a059358f1f51fe1820820bc11b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - genpipeline = genpipeline:main
    #
    # Would create an entry point called genpipeline that calls genpipeline.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - greenlet >=0.4.0
    - sqlalchemy >=0.7.0

  run:
    - python
    - greenlet >=0.4.0
    - sqlalchemy >=0.7.0

test:
  # Python imports
  imports:
    - genpipeline

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/renshawbay/genpipeline
  license: MIT License
  summary: 'A simple Python coroutine-based method for creating data processing pipelines'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
