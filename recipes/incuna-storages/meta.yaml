package:
  name: incuna-storages
  version: "1.1"

source:
  fn: incuna-storages-1.1.tar.gz
  url: https://pypi.python.org/packages/source/i/incuna-storages/incuna-storages-1.1.tar.gz
  md5: da0151f4ee71891a9b1d2cf781da114f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - incuna-storages = incuna-storages:main
    #
    # Would create an entry point called incuna-storages that calls incuna-storages.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - incuna_storages
    - incuna_storages.backends

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/incuna/incuna-storages/
  license: UNKNOWN
  summary: 'A collection of django storage backends.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
