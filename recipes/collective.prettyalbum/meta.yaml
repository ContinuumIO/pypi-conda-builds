package:
  name: collective.prettyalbum
  version: "0.5"

source:
  fn: collective.prettyalbum-0.5.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.prettyalbum/collective.prettyalbum-0.5.tar.gz
  md5: 74de392f8725387f60e785e2bd0867ae
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.prettyalbum = collective.prettyalbum:main
    #
    # Would create an entry point called collective.prettyalbum that calls collective.prettyalbum.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - archetypes.schemaextender
    - collective.prettyphoto
    - plone.app.jquerytools

  run:
    - python
    - setuptools
    - archetypes.schemaextender
    - collective.prettyphoto
    - plone.app.jquerytools

test:
  # Python imports
  imports:
    - collective
    - collective.prettyalbum
    - collective.prettyalbum.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: GPL
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
