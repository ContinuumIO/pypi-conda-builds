package:
  name: feincms-extensions
  version: "0.4.1"

source:
  fn: feincms-extensions-0.4.1.tar.gz
  url: https://pypi.python.org/packages/source/f/feincms-extensions/feincms-extensions-0.4.1.tar.gz
  md5: 6c93532f47de7712d4936aa3e0e1db20
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - feincms-extensions = feincms-extensions:main
    #
    # Would create an entry point called feincms-extensions that calls feincms-extensions.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - feincms >=1.9,<1.11

  run:
    - python
    - feincms >=1.9,<1.11

test:
  # Python imports
  imports:
    - feincms_extensions
    - feincms_extensions.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/incuna/feincms-extensions/
  license: UNKNOWN
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
