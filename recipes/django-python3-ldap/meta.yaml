package:
  name: django-python3-ldap
  version: "0.9.3"

source:
  fn: django-python3-ldap-0.9.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-python3-ldap/django-python3-ldap-0.9.3.tar.gz
  md5: 3e572144839a63c3575888612244ce31
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-python3-ldap = django-python3-ldap:main
    #
    # Would create an entry point called django-python3-ldap that calls django-python3-ldap.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.7
    - ldap3 ==0.9.8.2

  run:
    - python
    - django >=1.7
    - ldap3 ==0.9.8.2

test:
  # Python imports
  imports:
    - django_python3_ldap
    - django_python3_ldap.management
    - django_python3_ldap.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/etianen/django-python3-ldap
  license: BSD License
  summary: 'Django LDAP user authentication backend for Python 3.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
