package:
  name: sphinxjp-tk0miya
  version: "0.0.4"

source:
  fn: sphinxjp-tk0miya-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/s/sphinxjp-tk0miya/sphinxjp-tk0miya-0.0.4.tar.gz
  md5: 8818619ab4cc823b51056a4b750eb366
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sphinxjp-tk0miya = sphinxjp-tk0miya:main
    #
    # Would create an entry point called sphinxjp-tk0miya that calls sphinxjp-tk0miya.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - sphinx >=0.6
    - setuptools
    - flickr_api

  run:
    - python
    - sphinx >=0.6
    - setuptools
    - flickr_api

test:
  # Python imports
  imports:
    - sphinxjp

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/r_rudi/sphinxjp.tk0miya
  license: BSD License
  summary: 'Sphinx flicker API extention'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
