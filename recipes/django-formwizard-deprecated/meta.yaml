package:
  name: django-formwizard-deprecated
  version: "0.1.0"

source:
  fn: django-formwizard-deprecated-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-formwizard-deprecated/django-formwizard-deprecated-0.1.0.tar.gz
  md5: b1a4ad397a777b762cd51aa870b36150
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-formwizard-deprecated = django-formwizard-deprecated:main
    #
    # Would create an entry point called django-formwizard-deprecated that calls django-formwizard-deprecated.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - formwizard_deprecated

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/fcurella/django-formwizard-deprecated
  license: BSD License
  summary: 'Original django.contrib.formtools.legacy.FormWizard from Django 1.5.12'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
