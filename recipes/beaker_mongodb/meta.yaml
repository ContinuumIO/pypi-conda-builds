package:
  name: beaker_mongodb
  version: "0.3"

source:
  fn: beaker_mongodb-0.3.tar.gz
  url: https://pypi.python.org/packages/source/b/beaker_mongodb/beaker_mongodb-0.3.tar.gz
  md5: 8f440066d1fad1240c90212fec256df4
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - beaker_mongodb = beaker_mongodb:main
    #
    # Would create an entry point called beaker_mongodb that calls beaker_mongodb.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pymongo >=1.9
    - beaker >=1.5

  run:
    - python
    - setuptools
    - pymongo >=1.9
    - beaker >=1.5

test:
  # Python imports
  imports:
    - beaker_mongodb
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/BlueDragonX/beaker_mongodb/
  license: BSD License
  summary: 'Beaker backend to write sessions and caches to a MongoDB schemaless database.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
