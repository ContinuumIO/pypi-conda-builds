package:
  name: html-purifier
  version: "0.1.9"

source:
  fn: html-purifier-0.1.9.zip
  url: https://pypi.python.org/packages/source/h/html-purifier/html-purifier-0.1.9.zip
  md5: 71b6fe1c765aa7b2bc493043d08af1bf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - html-purifier = html-purifier:main
    #
    # Would create an entry point called html-purifier that calls html-purifier.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - purifier

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/PixxxeL/python-html-purifier
  license: GPL3
  summary: 'Cuts the tags and attributes from HTML that are not in the whitelist. Their content is left.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
