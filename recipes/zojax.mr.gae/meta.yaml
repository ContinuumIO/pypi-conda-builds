package:
  name: zojax.mr.gae
  version: "0.3"

source:
  fn: zojax.mr.gae-0.3.tar.gz
  url: https://pypi.python.org/packages/source/z/zojax.mr.gae/zojax.mr.gae-0.3.tar.gz
  md5: 370215428dae5f8499a1fa41d81ce784
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zojax.mr.gae = zojax.mr.gae:main
    #
    # Would create an entry point called zojax.mr.gae that calls zojax.mr.gae.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout

test:
  # Python imports
  imports:
    - zojax
    - zojax.mr
    - zojax.mr.gae

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Zojax/zojax.mr.gae
  license: GPL
  summary: 'Simple GAE-extension for buildout'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
