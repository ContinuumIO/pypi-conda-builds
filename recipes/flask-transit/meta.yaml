package:
  name: flask-transit
  version: "0.1.0-pre"

source:
  fn: Flask-Transit-0.1.0-pre.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Transit/Flask-Transit-0.1.0-pre.tar.gz
  md5: f804310062dc728650e7a1118d2cd2b4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-transit = flask-transit:main
    #
    # Would create an entry point called flask-transit that calls flask-transit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - transit-python

  run:
    - python
    - flask
    - transit-python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/obmarg/flask-transit
  license: BSD License
  summary: 'A flask extension for working with transit data.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
