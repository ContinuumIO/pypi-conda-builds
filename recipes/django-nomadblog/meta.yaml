package:
  name: django-nomadblog
  version: "2.0.0"

source:
  fn: django-nomadblog-2.0.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-nomadblog/django-nomadblog-2.0.0.tar.gz
  md5: 3e46b9d934244fbfa7ff9f904eda1541
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-nomadblog = django-nomadblog:main
    #
    # Would create an entry point called django-nomadblog that calls django-nomadblog.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - nomadblog
    - nomadblog.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/nabucosound/django-nomadblog/
  license: BSD License
  summary: 'A minimalist Django blogging system.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
