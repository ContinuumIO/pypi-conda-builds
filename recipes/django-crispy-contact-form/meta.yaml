package:
  name: django-crispy-contact-form
  version: "0.1.12"

source:
  fn: django-crispy-contact-form-0.1.12.tar.gz
  url: https://pypi.python.org/packages/source/d/django-crispy-contact-form/django-crispy-contact-form-0.1.12.tar.gz
  md5: 528d04e01b5cd86186a8ca93c3b20cb0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-crispy-contact-form = django-crispy-contact-form:main
    #
    # Would create an entry point called django-crispy-contact-form that calls django-crispy-contact-form.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5
    - django-appcore ==0.0.4
    - django-simple-captcha >=0.4.5
    - django-crispy-forms >=1.4.0
    - django-ipware >=0.1.1
    - bleach >=1.4

  run:
    - python
    - django >=1.5
    - django-appcore ==0.0.4
    - django-simple-captcha >=0.4.5
    - django-crispy-forms >=1.4.0
    - django-ipware >=0.1.1
    - bleach >=1.4

test:
  # Python imports
  imports:
    - contact_form
    - contact_form.admin
    - contact_form.migrations
    - contact_form.models
    - contact_form.south_migrations
    - contact_form.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dlancer/django-crispy-contact-form
  license: BSD License
  summary: 'Customizable contact form based on crispy-forms with captcha support for Django 1.5+'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
