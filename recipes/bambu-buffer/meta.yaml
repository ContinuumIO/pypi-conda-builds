package:
  name: bambu-buffer
  version: "2.2"

source:
  fn: bambu-buffer-2.2.tar.gz
  url: https://pypi.python.org/packages/source/b/bambu-buffer/bambu-buffer-2.2.tar.gz
  md5: 187030a5e2c64aa9e6dae0e6bed3e9b2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bambu-buffer = bambu-buffer:main
    #
    # Would create an entry point called bambu-buffer that calls bambu-buffer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6
    - requests >=2.0

  run:
    - python
    - django >=1.6
    - requests >=2.0

test:
  # Python imports
  imports:
    - bambu_buffer
    - bambu_buffer.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/iamsteadman/bambu-buffer
  license: UNKNOWN
  summary: 'Post to Buffer and manage profile settings through a Django-powered site'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
