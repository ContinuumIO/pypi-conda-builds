package:
  name: django-rest-framework-multi-slug-field
  version: "0.2.0"

source:
  fn: django-rest-framework-multi-slug-field-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-rest-framework-multi-slug-field/django-rest-framework-multi-slug-field-0.2.0.tar.gz
  md5: 4ae718be6eaf84cc62e692b564cb6ffd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-rest-framework-multi-slug-field = django-rest-framework-multi-slug-field:main
    #
    # Would create an entry point called django-rest-framework-multi-slug-field that calls django-rest-framework-multi-slug-field.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - djangorestframework >=2.3.14

  run:
    - python
    - djangorestframework >=2.3.14

test:
  # Python imports
  imports:
    - rest_framework_msf

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/simpleenergy/django-rest-framework-multi-slug-field
  license: BSD License
  summary: 'A field for representing a relationship via multiple fields on the target'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
