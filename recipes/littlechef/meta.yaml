package:
  name: littlechef
  version: "1.8.0"

source:
  fn: littlechef-1.8.0.tar.gz
  url: https://pypi.python.org/packages/source/l/littlechef/littlechef-1.8.0.tar.gz
  md5: 24c8b2a1d229f865f91fa20d359836f4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - littlechef = littlechef:main
    #
    # Would create an entry point called littlechef that calls littlechef.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fabric >=1.5.4
    - argparse
    - jinja2 >=2.7.3

  run:
    - python
    - fabric >=1.5.4
    - argparse
    - jinja2 >=2.7.3

test:
  # Python imports
  imports:
    - littlechef

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/tobami/littlechef
  license: Apache Software License
  summary: 'Cook with Chef without a Chef Server'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
