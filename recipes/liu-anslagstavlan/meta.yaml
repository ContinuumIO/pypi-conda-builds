package:
  name: liu-anslagstavlan
  version: "0.9.1-20110113"

source:
  fn: liu-anslagstavlan-0.9.1-20110113.tar.gz
  url: https://pypi.python.org/packages/source/l/liu-anslagstavlan/liu-anslagstavlan-0.9.1-20110113.tar.gz
  md5: 4dd03215728b1f2fa8764b419e3319bf
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - liu-anslagstavlan = liu-anslagstavlan:main
    #
    # Would create an entry point called liu-anslagstavlan that calls liu-anslagstavlan.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - liuanslagstavlan

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/pilt/liu-anslagstavlan
  license: BSD License
  summary: 'Unoffical library for working with the bulletin board of Link\xc3\xb6ping University.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
