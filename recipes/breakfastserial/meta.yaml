package:
  name: breakfastserial
  version: "0.0.9"

source:
  fn: BreakfastSerial-0.0.9.tar.gz
  url: https://pypi.python.org/packages/source/B/BreakfastSerial/BreakfastSerial-0.0.9.tar.gz
  md5: f8e46a5e4852d81053fc3aea69727a60
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - breakfastserial = breakfastserial:main
    #
    # Would create an entry point called breakfastserial that calls breakfastserial.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyfirmata

  run:
    - python
    - pyfirmata

test:
  # Python imports
  imports:
    - BreakfastSerial
    - breakfastserial

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/theycallmeswift/BreakfastSerial/
  license: UNKNOWN
  summary: 'Python Framework for interacting with Arduino'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
