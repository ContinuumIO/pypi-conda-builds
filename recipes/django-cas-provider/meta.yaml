package:
  name: django-cas-provider
  version: "0.3.1"

source:
  fn: django-cas-provider-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-cas-provider/django-cas-provider-0.3.1.tar.gz
  md5: 027369301ebbd3d335748c0bdca923d3
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-cas-provider = django-cas-provider:main
    #
    # Would create an entry point called django-cas-provider that calls django-cas-provider.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - south >=0.7.2

  run:
    - python
    - setuptools
    - south >=0.7.2

test:
  # Python imports
  imports:
    - cas_provider
    - cas_provider.management
    - cas_provider.management.commands
    - cas_provider.migrations
    - cas_provider_examples
    - cas_provider_examples.simple

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/castlabs/django-cas-provider
  license: MIT License
  summary: 'A "provider" for the Central Authentication Service (http://jasig.org/cas)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
