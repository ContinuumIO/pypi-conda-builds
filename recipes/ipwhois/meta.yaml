package:
  name: ipwhois
  version: "0.10.1"

source:
  fn: ipwhois-0.10.1.zip
  url: https://pypi.python.org/packages/source/i/ipwhois/ipwhois-0.10.1.zip
  md5: e878ec0a827ab4a4d4db4096fa94d89a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ipwhois = ipwhois:main
    #
    # Would create an entry point called ipwhois that calls ipwhois.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - dnspython
    - ipaddr

  run:
    - python
    - dnspython
    - ipaddr

test:
  # Python imports
  imports:
    - ipwhois

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/secynic/ipwhois
  license: BSD License
  summary: 'IP Whois Resolution and Parsing'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
