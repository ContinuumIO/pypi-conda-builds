package:
  name: python-boxview
  version: "1.1.3beta1"

source:
  fn: python-boxview-1.1.3beta1.tar.gz
  url: https://pypi.python.org/packages/source/p/python-boxview/python-boxview-1.1.3beta1.tar.gz
  md5: 43ed691a7c4590711ee7f5d047cc22d2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-boxview = python-boxview:main
    #
    # Would create an entry point called python-boxview that calls python-boxview.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests
    - six

  run:
    - python
    - requests
    - six

test:
  # Python imports
  imports:
    - boxview

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/caxap/python-boxview
  license: MIT License
  summary: 'Python client library for Box View API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
