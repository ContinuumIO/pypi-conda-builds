package:
  name: hostery
  version: "0.1.4"

source:
  fn: Hostery-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/H/Hostery/Hostery-0.1.4.tar.gz
  md5: 22f397ab371d84ebb5741c87516f7b22
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hostery = hostery:main
    #
    # Would create an entry point called hostery that calls hostery.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - jinja2 >=2.5

  run:
    - python
    - jinja2 >=2.5

test:
  # Python imports
  imports:
    - hostery

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.hostery.georgemichaelbrower.com/
  license: LICENSE.txt
  summary: 'Pushes git snapshots to a webhost.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
