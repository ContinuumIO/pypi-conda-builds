package:
  name: django-easy-thumbnails-watermark
  version: "0.6.9"

source:
  fn: django-easy-thumbnails-watermark-0.6.9.tar.gz
  url: https://pypi.python.org/packages/source/d/django-easy-thumbnails-watermark/django-easy-thumbnails-watermark-0.6.9.tar.gz
  md5: b53278a15c880155bce027993fbfeef7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-easy-thumbnails-watermark = django-easy-thumbnails-watermark:main
    #
    # Would create an entry point called django-easy-thumbnails-watermark that calls django-easy-thumbnails-watermark.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - easy_thumbnails

  run:
    - python
    - easy_thumbnails

test:
  # Python imports
  imports:
    - easy_thumbnails_watermark

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://v.licheni.net/drc/django-easy-thumbnails-watermark.git
  license: GNU General Public License v3 (GPLv3)
  summary: 'A watermark filter for easy_thumbnails'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
