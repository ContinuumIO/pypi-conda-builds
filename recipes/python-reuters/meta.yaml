package:
  name: python-reuters
  version: "0.2"

source:
  fn: python-reuters-0.2.tar.gz
  url: https://pypi.python.org/packages/source/p/python-reuters/python-reuters-0.2.tar.gz
  md5: e40733c4f8c2cc0218a9b429774ea7bb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-reuters = python-reuters:main
    #
    # Would create an entry point called python-reuters that calls python-reuters.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - jinja2

  run:
    - python
    - jinja2

test:
  # Python imports
  imports:
    - reuters

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/YACOWS/python-reuters
  license: UNKNOWN
  summary: 'Python library to consume Reuters Soap Web services'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
