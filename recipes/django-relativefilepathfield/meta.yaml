package:
  name: django-relativefilepathfield
  version: "1.0.2"

source:
  fn: django-relativefilepathfield-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-relativefilepathfield/django-relativefilepathfield-1.0.2.tar.gz
  md5: fd015e0f55f7460e2f3472116472af80
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-relativefilepathfield = django-relativefilepathfield:main
    #
    # Would create an entry point called django-relativefilepathfield that calls django-relativefilepathfield.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - relativefilepathfield

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/alexhayes/django-relativefilepathfield
  license: UNKNOWN
  summary: 'A Django FilePathField which stores a relative value to the file.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
