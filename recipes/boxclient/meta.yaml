package:
  name: boxclient
  version: "1.2.1"

source:
  fn: boxclient-1.2.1.tar.gz
  url: https://pypi.python.org/packages/source/b/boxclient/boxclient-1.2.1.tar.gz
  md5: 92ca4742acc2a1e84fbaba19fb0c52da
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - boxclient = boxclient:main
    #
    # Would create an entry point called boxclient that calls boxclient.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - oauthclient ==1.0.1

  run:
    - python
    - oauthclient ==1.0.1

test:
  # Python imports
  imports:
    - box
    - boxclient

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/rca/box
  license: LICENSE
  summary: 'Box API v2.0 client'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
