package:
  name: boozelib
  version: "0.4"

source:
  fn: BoozeLib-0.4.tar.gz
  url: https://pypi.python.org/packages/source/B/BoozeLib/BoozeLib-0.4.tar.gz
  md5: c16d8618c5f2a8f15e377d1a124782c9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - boozelib = boozelib:main
    #
    # Would create an entry point called boozelib that calls boozelib.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - boozelib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/brutus/boozelib
  license: GNU General Public License (GPL)
  summary: 'A Python module containing a couple of functions to calculate the *blood alcohol content* of people.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
