package:
  name: django-icons-twitterjoy
  version: "1.0"

source:
  fn: django-icons-twitterjoy-1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-icons-twitterjoy/django-icons-twitterjoy-1.0.tar.gz
  md5: e6cd5232c51992c3415c4c7319eaba55
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-icons-twitterjoy = django-icons-twitterjoy:main
    #
    # Would create an entry point called django-icons-twitterjoy that calls django-icons-twitterjoy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - distribute

  run:
    - python
    - distribute

test:
  # Python imports
  imports:
    - icons_twitterjoy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/kmike/django-icons/
  license: MIT License
  summary: 'twitterjoy icons pack for django.contrib.staticfiles'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
