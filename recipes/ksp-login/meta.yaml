package:
  name: ksp-login
  version: "0.3.0"

source:
  fn: ksp-login-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/k/ksp-login/ksp-login-0.3.0.tar.gz
  md5: 538f0cf67615c6ff3e0d946c23b6793a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ksp-login = ksp-login:main
    #
    # Would create an entry point called ksp-login that calls ksp-login.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django >=1.4
    - python-social-auth

  run:
    - python
    - django >=1.4
    - python-social-auth

test:
  # Python imports
  imports:
    - ksp_login
    - ksp_login.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/koniiiik/ksp_login
  license: LICENSE
  summary: 'A Django app for both traditional and social authentication'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
