package:
  name: flask-googlefed
  version: "0.1"

source:
  fn: Flask-GoogleFed-0.1.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-GoogleFed/Flask-GoogleFed-0.1.tar.gz
  md5: d7ef891e48667a5d2413d08cab5c61ee
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-googlefed = flask-googlefed:main
    #
    # Would create an entry point called flask-googlefed that calls flask-googlefed.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - flask-openid

  run:
    - python
    - flask
    - flask-openid

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kennethreitz/flask-googlefed
  license: BSD License
  summary: 'Google Federated Logins for Flask.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
