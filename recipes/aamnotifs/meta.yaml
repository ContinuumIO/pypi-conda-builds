package:
  name: aamnotifs
  version: "0.1.0"

source:
  fn: aamnotifs-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/a/aamnotifs/aamnotifs-0.1.0.tar.gz
  md5: 460ee0d635167b967fcdfc49b3accd22
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - aamnotifs = aamnotifs:main
    #
    # Would create an entry point called aamnotifs that calls aamnotifs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - pika

  run:
    - python
    - pika

test:
  # Python imports
  imports:
    - aamnotifs

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/andreimarcu/aamnotifs
  license: LICENSE.txt
  summary: 'Simple notifications implementation with RabbitMQ using pika.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
