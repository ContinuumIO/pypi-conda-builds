package:
  name: tornado-alf
  version: "0.4.2"

source:
  fn: tornado-alf-0.4.2.tar.gz
  url: https://pypi.python.org/packages/source/t/tornado-alf/tornado-alf-0.4.2.tar.gz
  md5: ba6b915a7e0bc73a8874b9d188e5824e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tornado-alf = tornado-alf:main
    #
    # Would create an entry point called tornado-alf that calls tornado-alf.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - tornado >=3.0

  run:
    - python
    - tornado >=3.0

test:
  # Python imports
  imports:
    - tornadoalf

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/globocom/tornado-alf
  license: MIT License
  summary: 'OAuth Client For Tornado'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
