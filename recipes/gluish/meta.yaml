package:
  name: gluish
  version: "0.1.73"

source:
  fn: gluish-0.1.73.tar.gz
  url: https://pypi.python.org/packages/source/g/gluish/gluish-0.1.73.tar.gz
  md5: f5f3d5512fb49da9d0c3afbebd27624a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gluish = gluish:main
    #
    # Would create an entry point called gluish that calls gluish.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - beautifulsoup ==3.2.1
    - astroid >=1.0.1
    - colorama ==0.3.3
    - elasticsearch ==1.3.0
    - logilab-common ==0.61.0
    - luigi >=1.0.20
    - nose ==1.3.3
    - pyisbn ==1.0.0
    - python-dateutil ==2.2
    - pytz ==2014.4
    - requests ==2.5.1
    - six ==1.9.0
    - urllib3 ==1.10
    - wsgiref ==0.1.2

  run:
    - python
    - beautifulsoup ==3.2.1
    - astroid >=1.0.1
    - colorama ==0.3.3
    - elasticsearch ==1.3.0
    - logilab-common ==0.61.0
    - luigi >=1.0.20
    - nose ==1.3.3
    - pyisbn ==1.0.0
    - python-dateutil ==2.2
    - pytz ==2014.4
    - requests ==2.5.1
    - six ==1.9.0
    - urllib3 ==1.10
    - wsgiref ==0.1.2

test:
  # Python imports
  imports:
    - gluish

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/miku/gluish
  license: UNKNOWN
  summary: 'Utils around Luigi.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
