package:
  name: builtin-password-reset
  version: "0.0.1"

source:
  fn: builtin-password-reset-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/b/builtin-password-reset/builtin-password-reset-0.0.1.tar.gz
  md5: 6f93f19dd5d7d2f9ea384fde2d9ffded
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - builtin-password-reset = builtin-password-reset:main
    #
    # Would create an entry point called builtin-password-reset that calls builtin-password-reset.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - builtin_password_reset

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://albertoconnor.ca
  license: MIT License
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
