package:
  name: django-sencha
  version: "1.3.5"

source:
  fn: django-sencha-1.3.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-sencha/django-sencha-1.3.5.tar.gz
  md5: ccf50106a09019b04099d4c40198c2d2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-sencha = django-sencha:main
    #
    # Would create an entry point called django-sencha that calls django-sencha.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - sencha

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://cellarosi@bitbucket.org/cellarosi/django-sencha
  license: MIT License
  summary: 'Django project contain static files about extjs 4.2'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
