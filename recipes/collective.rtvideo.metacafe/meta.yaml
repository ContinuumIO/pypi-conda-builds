package:
  name: collective.rtvideo.metacafe
  version: "0.1.0"

source:
  fn: collective.rtvideo.metacafe-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.rtvideo.metacafe/collective.rtvideo.metacafe-0.1.0.tar.gz
  md5: 89bd6088ea3fafe16e843acdf751e880
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.rtvideo.metacafe = collective.rtvideo.metacafe:main
    #
    # Would create an entry point called collective.rtvideo.metacafe that calls collective.rtvideo.metacafe.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - redturtle.video >=0.6.0

  run:
    - python
    - setuptools
    - setuptools
    - redturtle.video >=0.6.0

test:
  # Python imports
  imports:
    - collective
    - collective.rtvideo
    - collective.rtvideo.metacafe
    - collective.rtvideo.metacafe.browser
    - collective.rtvideo.metacafe.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/redturtle.video
  license: GNU General Public License (GPL)
  summary: 'The Metacafe Plone support for RedTurtle Video'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
