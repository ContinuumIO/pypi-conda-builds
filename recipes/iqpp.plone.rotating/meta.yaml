package:
  name: iqpp.plone.rotating
  version: "0.1b1"

source:
  fn: iqpp.plone.rotating-0.1b1.tar.gz
  url: https://pypi.python.org/packages/source/i/iqpp.plone.rotating/iqpp.plone.rotating-0.1b1.tar.gz
  md5: 4f3d4e1a657fec6ca2488c1858826566
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - iqpp.plone.rotating = iqpp.plone.rotating:main
    #
    # Would create an entry point called iqpp.plone.rotating that calls iqpp.plone.rotating.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - iqpp
    - iqpp.plone
    - iqpp.plone.rotating
    - iqpp.plone.rotating.adapters
    - iqpp.plone.rotating.browser
    - iqpp.plone.rotating.portlets
    - iqpp.plone.rotating.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.iqpp.de
  license: GPL
  summary: 'Rotating content for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
