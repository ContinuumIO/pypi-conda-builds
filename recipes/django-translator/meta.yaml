package:
  name: django-translator
  version: "1.0.3"

source:
  fn: django-translator-1.0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-translator/django-translator-1.0.3.tar.gz
  md5: e8a5118e79d3f4946928281f1c5a0393
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-translator = django-translator:main
    #
    # Would create an entry point called django-translator that calls django-translator.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - south >=0.8.4
    - django-modeltranslation >=0.7.3
    - django-taggit >=0.11.2

  run:
    - python
    - south >=0.8.4
    - django-modeltranslation >=0.7.3
    - django-taggit >=0.11.2

test:
  # Python imports
  imports:
    - translator

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.dreipol.ch/
  license: MIT License
  summary: 'Translator is an app for collecting translations for specified keys in django admin.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
