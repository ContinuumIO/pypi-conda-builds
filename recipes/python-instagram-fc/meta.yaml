package:
  name: python-instagram-fc
  version: "0.8.0"

source:
  fn: python-instagram-fc-0.8.0.zip
  url: https://pypi.python.org/packages/source/p/python-instagram-fc/python-instagram-fc-0.8.0.zip
  md5: 51780ee72f94a01cc55e695f252afb30
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-instagram-fc = python-instagram-fc:main
    #
    # Would create an entry point called python-instagram-fc that calls python-instagram-fc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - simplejson
    - httplib2

  run:
    - python
    - simplejson
    - httplib2

test:
  # Python imports
  imports:
    - instagram

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/futurecolors/python-instagram
  license: MIT
  summary: 'Instagram API client'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
