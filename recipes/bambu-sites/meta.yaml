package:
  name: bambu-sites
  version: "0.1"

source:
  fn: bambu-sites-0.1.tar.gz
  url: https://pypi.python.org/packages/source/b/bambu-sites/bambu-sites-0.1.tar.gz
  md5: ed8d535693e8a9c11c2e0fda87114ac0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bambu-sites = bambu-sites:main
    #
    # Would create an entry point called bambu-sites that calls bambu-sites.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django >=1

  run:
    - python
    - django >=1

test:
  # Python imports
  imports:
    - bambu.sites

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/bambu-sites
  license: UNKNOWN
  summary: "Middleware that redirects users to a site's primary domain, based on the `django.contrib.sites` framework"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
