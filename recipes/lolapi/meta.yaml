package:
  name: lolapi
  version: "0.9.94"

source:
  fn: lolapi-0.9.94.zip
  url: https://pypi.python.org/packages/source/l/lolapi/lolapi-0.9.94.zip
  md5: 3f1cb152cf299c8f4d0b3094e1cada48
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lolapi = lolapi:main
    #
    # Would create an entry point called lolapi that calls lolapi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - lolapi
    - lolapi.resources

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/smotes/lolapi
  license: MIT
  summary: 'Python wrapper for the League of Legends RESTful API by Riot Games'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
