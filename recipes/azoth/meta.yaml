package:
  name: azoth
  version: "0.3.0"

source:
  fn: azoth-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/a/azoth/azoth-0.3.0.tar.gz
  md5: 354365a26c4f9301e352b0e7b4d6ada5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - azoth = azoth:main
    #
    # Would create an entry point called azoth that calls azoth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six
    - transaction
    - sqlalchemy
    - zope.interface
    - zope.sqlalchemy

  run:
    - python
    - six
    - transaction
    - sqlalchemy
    - zope.interface
    - zope.sqlalchemy

test:
  # Python imports
  imports:
    - azoth
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/TakesxiSximada/azoth
  license: BSD License
  summary: 'Sqlalchemy utilities'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
