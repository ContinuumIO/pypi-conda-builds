package:
  name: hfilesize
  version: "0.1.0"

source:
  fn: hfilesize-0.1.0.zip
  url: https://pypi.python.org/packages/source/h/hfilesize/hfilesize-0.1.0.zip
  md5: 598f5440be97223d529c41f5be6879a7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hfilesize = hfilesize:main
    #
    # Would create an entry point called hfilesize that calls hfilesize.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - hfilesize

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/simonzack/hfilesize
  license: GNU General Public License v3 (GPLv3)
  summary: 'Human Readable File Sizes'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
