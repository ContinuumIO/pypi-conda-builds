package:
  name: megrok.kss
  version: "0.2.3"

source:
  fn: megrok.kss-0.2.3.tar.gz
  url: https://pypi.python.org/packages/source/m/megrok.kss/megrok.kss-0.2.3.tar.gz
  md5: 0a03890415bfd54150c16010cff5b7ac
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - megrok.kss = megrok.kss:main
    #
    # Would create an entry point called megrok.kss that calls megrok.kss.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - grok >=0.13
    - kss.core >=1.4,<1.4.99

  run:
    - python
    - setuptools
    - setuptools
    - grok >=0.13
    - kss.core >=1.4,<1.4.99

test:
  # Python imports
  imports:
    - megrok
    - megrok.kss
    - megrok.kss.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/megrok.kss
  license: ZPL
  summary: 'KSS for Grok. Ajax with Style, see http://kssproject.org'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
