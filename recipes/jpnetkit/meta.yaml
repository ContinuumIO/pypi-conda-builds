package:
  name: jpnetkit
  version: "0.1dev"

source:
  fn: jpNetKit-0.1dev.tar.gz
  url: https://pypi.python.org/packages/source/j/jpNetKit/jpNetKit-0.1dev.tar.gz
  md5: ef5b6a0218b519eeffad4a660009a1d6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jpnetkit = jpnetkit:main
    #
    # Would create an entry point called jpnetkit that calls jpnetkit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - requests >=1.1.0
    - beautifulsoup4 >=4.1.3
    - jcconv >=0.2.3
    - lxml >=2.3.6

  run:
    - python
    - requests >=1.1.0
    - beautifulsoup4 >=4.1.3
    - jcconv >=0.2.3
    - lxml >=2.3.6

test:
  # Python imports
  imports:
    - jpnetkit
    - jpnetkit.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/jp-net-kit/
  license: LICENSE.txt
  summary: 'jp2en web services api collection'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
