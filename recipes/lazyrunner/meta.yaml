package:
  name: lazyrunner
  version: "0.01v3"

source:
  fn: lazyrunner-0.01v3.tar.gz
  url: https://pypi.python.org/packages/source/l/lazyrunner/lazyrunner-0.01v3.tar.gz
  md5: 21d20f948c044f29016feb3a5afae382
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lazyrunner = lazyrunner:main
    #
    # Would create an entry point called lazyrunner that calls lazyrunner.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - treedict

  run:
    - python
    - treedict

test:
  # Python imports
  imports:
    - lazyrunner

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.stat.washington.edu/~hoytak/code/lazyrunner/
  license: BSD License
  summary: 'LazyRunner: Module based scientific experiment framework with lazy evaluation.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
