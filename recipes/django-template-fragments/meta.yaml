package:
  name: django-template-fragments
  version: "0.1.3"

source:
  fn: django-template-fragments-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-template-fragments/django-template-fragments-0.1.3.tar.gz
  md5: 730a3d9fe38489d03b7023fd7b8931d3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-template-fragments = django-template-fragments:main
    #
    # Would create an entry point called django-template-fragments that calls django-template-fragments.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - fragments
    - fragments.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Psycojoker/django-template-fragments
  license: MIT
  summary: 'helper for templates used in javascript client framework: allow to store small django templates in a dir and generate a javascript object that contains them all as strings'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
