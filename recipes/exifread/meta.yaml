package:
  name: exifread
  version: "2.0.2"

source:
  fn: ExifRead-2.0.2.tar.gz
  url: https://pypi.python.org/packages/source/E/ExifRead/ExifRead-2.0.2.tar.gz
  md5: 5d70c0614d4d575bc05c61c27b664cf8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - exifread = exifread:main
    #
    # Would create an entry point called exifread that calls exifread.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - exifread
    - exifread.tags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ianare/exif-py
  license: BSD License
  summary: 'Read Exif metadata from tiff and jpeg files.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
