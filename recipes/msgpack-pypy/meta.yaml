package:
  name: msgpack-pypy
  version: "0.0.2"

source:
  fn: msgpack-pypy-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/m/msgpack-pypy/msgpack-pypy-0.0.2.tar.gz
  md5: 4b5f113d07aa87af4b264e1b7a6fe251
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - msgpack-pypy = msgpack-pypy:main
    #
    # Would create an entry point called msgpack-pypy that calls msgpack-pypy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - msgpack-python >=0.4

  run:
    - python
    - msgpack-python >=0.4

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/antocuni/msgpack-pypy
  license: BSD License
  summary: 'A msgpack extension with special optimizations for PyPy'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
