package:
  name: exemelopy
  version: "0.0.9"

source:
  fn: exemelopy-0.0.9.tar.gz
  url: https://pypi.python.org/packages/source/e/exemelopy/exemelopy-0.0.9.tar.gz
  md5: 25a4254bac384a0c59b0e32fac3fc99b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - exemelopy = exemelopy:main
    #
    # Would create an entry point called exemelopy that calls exemelopy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - lxml
    - ordereddict

  run:
    - python
    - lxml
    - ordereddict

test:
  # Python imports
  imports:
    - exemelopy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/OldhamMade/exemelopy
  license: UNKNOWN
  summary: 'exemelopy is a tool for building XML from native Python data-types, similiar to the json/simplejson modules'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
