package:
  name: dyndnsimple
  version: "0.0.11"

source:
  fn: dyndnsimple-0.0.11.tar.gz
  url: https://pypi.python.org/packages/source/d/dyndnsimple/dyndnsimple-0.0.11.tar.gz
  md5: d97333da83bd143b4bc47109b7e2cbd2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dyndnsimple = dyndnsimple:main
    #
    # Would create an entry point called dyndnsimple that calls dyndnsimple.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests ==0.14.1

  run:
    - python
    - requests ==0.14.1

test:
  # Python imports
  imports:
    - dyndnsimple

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bwghughes/dyndnsimple
  license: ISC License (ISCL)
  summary: 'Package for updating DNSimple domain with a WAN IP address.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
