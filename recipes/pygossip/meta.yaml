package:
  name: pygossip
  version: "0.5"

source:
  fn: pygossip-0.5.tar.gz
  url: https://pypi.python.org/packages/source/p/pygossip/pygossip-0.5.tar.gz
  md5: 83af8a0534b46675f03639227427bcbb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pygossip = pygossip:main
    #
    # Would create an entry point called pygossip that calls pygossip.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - gossip
    - pygossip

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bmsi.com/python/pygossip.html
  license: GNU General Public License (GPL)
  summary: 'Python GOSSiP distributed domain reputation service'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
