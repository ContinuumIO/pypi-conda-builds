package:
  name: grumblr
  version: "0.0.3"

source:
  fn: grumblr-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/g/grumblr/grumblr-0.0.3.tar.gz
  md5: 9821ee84e4418fd8608cf9e57cac2bc7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - grumblr = grumblr:main
    #
    # Would create an entry point called grumblr that calls grumblr.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - ashes ==0.7.3
    - boltons ==0.4.2
    - gevent ==1.0.1
    - progressbar ==2.3
    - pytumblr ==0.0.6
    - pyyaml ==3.11

  run:
    - python
    - ashes ==0.7.3
    - boltons ==0.4.2
    - gevent ==1.0.1
    - progressbar ==2.3
    - pytumblr ==0.0.6
    - pyyaml ==3.11

test:
  # Python imports
  imports:
    - grumblr

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mahmoud/grumblr
  license: BSD
  summary: 'Maintenance utility for tumblr blogs.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
