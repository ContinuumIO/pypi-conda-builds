package:
  name: kabaret.naming
  version: "0.1.0b1"

source:
  fn: kabaret.naming-0.1.0b1.zip
  url: https://pypi.python.org/packages/source/k/kabaret.naming/kabaret.naming-0.1.0b1.zip
  md5: 64dff9589b53672171616503ef61f91f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kabaret.naming = kabaret.naming:main
    #
    # Would create an entry point called kabaret.naming that calls kabaret.naming.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - kabaret
    - kabaret.naming
    - kabaret.naming.examples
    - kabaret.naming.fields

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://packages.python.org/kabaret/naming.html
  license: GNU Lesser General Public License v3 or later (LGPLv3+)
  summary: 'File and Folder naming convention modeling and exploitation.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
