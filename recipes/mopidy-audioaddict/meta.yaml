package:
  name: mopidy-audioaddict
  version: "0.2.7"

source:
  fn: Mopidy-AudioAddict-0.2.7.tar.gz
  url: https://pypi.python.org/packages/source/M/Mopidy-AudioAddict/Mopidy-AudioAddict-0.2.7.tar.gz
  md5: bb56aadd7df6d0a8ce35ec0b60c47db8
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mopidy-audioaddict = mopidy-audioaddict:main
    #
    # Would create an entry point called mopidy-audioaddict that calls mopidy-audioaddict.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - mopidy >=0.18
    - pykka >=1.1
    - pycountry

  run:
    - python
    - setuptools
    - setuptools
    - mopidy >=0.18
    - pykka >=1.1
    - pycountry

test:
  # Python imports
  imports:
    - mopidy_audioaddict

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/nilicule/mopidy-audioaddict
  license: Apache Software License
  summary: u'Mopidy extension for the AudioAddict network of radiostations'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
