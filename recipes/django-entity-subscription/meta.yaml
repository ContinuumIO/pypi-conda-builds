package:
  name: django-entity-subscription
  version: "0.4.1"

source:
  fn: django-entity-subscription-0.4.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-entity-subscription/django-entity-subscription-0.4.1.tar.gz
  md5: bff4ea96b7d842fb82ca0bf9b561ba4a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-entity-subscription = django-entity-subscription:main
    #
    # Would create an entry point called django-entity-subscription that calls django-entity-subscription.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6,<1.7
    - django-entity >=1.5.0

  run:
    - python
    - django >=1.6,<1.7
    - django-entity >=1.5.0

test:
  # Python imports
  imports:
    - entity_subscription
    - entity_subscription.migrations
    - entity_subscription.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ambitioninc/django-entity-subscription
  license: MIT License
  summary: 'Make subscription management easy and entity-based.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
