package:
  name: babtools_misc
  version: "0.1.3"

source:
  fn: babtools_misc-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/b/babtools_misc/babtools_misc-0.1.3.tar.gz
  md5: cc596f0187e3db3b54b1c9df6f40ea77
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - babtools_misc = babtools_misc:main
    #
    # Would create an entry point called babtools_misc that calls babtools_misc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - babtools_misc

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/ArneBab/babtools-misc
  license: GNU General Public License (GPL)
  summary: ['several small tools.']

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
