package:
  name: collective.eclipsescripts
  version: "0.1"

source:
  fn: collective.eclipsescripts-0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.eclipsescripts/collective.eclipsescripts-0.1.tar.gz
  md5: 495408e10fe5ce30654f204a6e24e7b1
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.eclipsescripts = collective.eclipsescripts:main
    #
    # Would create an entry point called collective.eclipsescripts that calls collective.eclipsescripts.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://mfabrik.com
  license: GPL
  summary: 'Plone plug-in for Eclipse development'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
