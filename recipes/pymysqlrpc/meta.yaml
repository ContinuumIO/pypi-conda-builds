package:
  name: pymysqlrpc
  version: "0.1.4"

source:
  fn: pymysqlrpc-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/p/pymysqlrpc/pymysqlrpc-0.1.4.tar.gz
  md5: 818107cc35a66426f25f04d6926c5405
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pymysqlrpc = pymysqlrpc:main
    #
    # Would create an entry point called pymysqlrpc that calls pymysqlrpc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - pymysqlrpc

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/feihuroger/pymysqlrpc
  license: MIT
  summary: 'a rpc framework implement mysql server protocol on top of gevent'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
