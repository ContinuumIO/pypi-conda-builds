package:
  name: django-cache-toolbox-modified
  version: "0.1.1"

source:
  fn: django-cache-toolbox-modified-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-cache-toolbox-modified/django-cache-toolbox-modified-0.1.1.tar.gz
  md5: ea7296af9bcd58c68d243316bd45bd40
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-cache-toolbox-modified = django-cache-toolbox-modified:main
    #
    # Would create an entry point called django-cache-toolbox-modified that calls django-cache-toolbox-modified.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3.0

  run:
    - python
    - django >=1.3.0

test:
  # Python imports
  imports:
    - cache_toolbox
    - cache_toolbox.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/django-cache-toolbox-modified/
  license: BSD
  summary: 'Non-magical object caching for Django. Based on django-cache-toolbox by Playfire.com'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
