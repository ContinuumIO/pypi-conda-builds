package:
  name: django-pure-pagination-fork
  version: "0.2.2"

source:
  fn: django-pure-pagination-fork-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-pure-pagination-fork/django-pure-pagination-fork-0.2.2.tar.gz
  md5: ed7aee5b60c998b5ed0305a859f4956c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-pure-pagination-fork = django-pure-pagination-fork:main
    #
    # Would create an entry point called django-pure-pagination-fork that calls django-pure-pagination-fork.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - pure_pagination

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jamespacileo/django-pure-pagination/
  license: BSD
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
