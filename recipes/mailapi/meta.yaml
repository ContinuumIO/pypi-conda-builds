package:
  name: mailapi
  version: "0.1.8"

source:
  fn: MailApi-0.1.8.tar.gz
  url: https://pypi.python.org/packages/source/M/MailApi/MailApi-0.1.8.tar.gz
  md5: 54caf2713e1ebbe4ef7a35c66535bdcf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mailapi = mailapi:main
    #
    # Would create an entry point called mailapi that calls mailapi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sqlalchemy
    - mysql-python
    - pep8

  run:
    - python
    - sqlalchemy
    - mysql-python
    - pep8

test:
  # Python imports
  imports:
    - mailapi
    - mailapi.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/paszabo/mailapi
  license: mailbot
  summary: 'An interface to iredadmin'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
