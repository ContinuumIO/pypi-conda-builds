package:
  name: arkpy
  version: "0.6"

source:
  fn: arkpy-0.6.tar.gz
  url: https://pypi.python.org/packages/source/a/arkpy/arkpy-0.6.tar.gz
  md5: 9ec927c8b0c8a66a2f4bb908ea8ae2b0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - arkpy = arkpy:main
    #
    # Would create an entry point called arkpy that calls arkpy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - arkpy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/mjgiarlo/arkpy
  license: UNKNOWN
  summary: 'Simple tools built around the Archival Resource Key\n    and Noid specifications, allowing minting and validating of ARKs\n    via code and the command-line.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
