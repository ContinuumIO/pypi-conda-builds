package:
  name: pygnetic
  version: "1.0"

source:
  fn: pygnetic-1.0.zip
  url: https://pypi.python.org/packages/source/p/pygnetic/pygnetic-1.0.zip
  md5: a5c7592040e0f47e2bf5b2c4b02d73d5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pygnetic = pygnetic:main
    #
    # Would create an entry point called pygnetic that calls pygnetic.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - pygnetic
    - pygnetic.network
    - pygnetic.serialization

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/bluex/pygame_network
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Network library for Pygame'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
