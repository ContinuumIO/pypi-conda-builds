package:
  name: collective.allowsearch
  version: "0.1.4"

source:
  fn: collective.allowsearch-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.allowsearch/collective.allowsearch-0.1.4.tar.gz
  md5: 548046117b0f4fbba638de2e4fbdf581
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.allowsearch = collective.allowsearch:main
    #
    # Would create an entry point called collective.allowsearch that calls collective.allowsearch.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.allowsearch

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://svn.plone.org/svn/collective/collective.allowsearch/trunk
  license: GPL
  summary: 'Provides an interface and a default adapter to allow users to search the catalog independent of the View permission.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
