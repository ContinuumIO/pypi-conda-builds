package:
  name: bakemate
  version: "0.5"

source:
  fn: bakemate-0.5.tar.gz
  url: https://pypi.python.org/packages/source/b/bakemate/bakemate-0.5.tar.gz
  md5: bb7af5f0f05fe57dffbeb0a1b86a7642
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bakemate = bakemate:main
    #
    # Would create an entry point called bakemate that calls bakemate.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - bakemate

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tengu/py-bakemate
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'a baker helper'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
