package:
  name: flask-email
  version: "1.4.4"

source:
  fn: Flask-Email-1.4.4.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Email/Flask-Email-1.4.4.tar.gz
  md5: 297ca9dfbe95028c7ea3819287806c45
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-email = flask-email:main
    #
    # Would create an entry point called flask-email that calls flask-email.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - blinker

  run:
    - python
    - flask
    - blinker

test:
  # Python imports
  imports:
    - flask_email
    - flask_email.backends
    - flask_email.backends.rest
    - flask_email.contrib
    - flask_email.contrib.mailgun
    - flask_email.contrib.mailgun.rest
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/allanlei/flask-email
  license: BSD License
  summary: 'Flask extension for sending email'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
