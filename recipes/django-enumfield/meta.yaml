package:
  name: django-enumfield
  version: "1.2"

source:
  fn: django-enumfield-1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-enumfield/django-enumfield-1.2.tar.gz
  md5: 14ac59601dd3ec2546ba1d875028f701
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-enumfield = django-enumfield:main
    #
    # Would create an entry point called django-enumfield that calls django-enumfield.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_enumfield
    - django_enumfield.db
    - django_enumfield.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/5monkeys/django-enumfield
  license: MIT License
  summary: 'Custom Django field for using enumerations of named constants'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
