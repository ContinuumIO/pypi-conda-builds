package:
  name: django_choices_flow
  version: "0.9.3"

source:
  fn: django_choices_flow-0.9.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django_choices_flow/django_choices_flow-0.9.3.tar.gz
  md5: 225088df009ab18fd2504176d9aa4dc1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_choices_flow = django_choices_flow:main
    #
    # Would create an entry point called django_choices_flow that calls django_choices_flow.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.2

  run:
    - python
    - django >=1.2

test:
  # Python imports
  imports:
    - django_choices_flow

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/valdergallo/django-choices-flow
  license: FREEBSD
  summary: 'Simple library with flow in choices values for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
