package:
  name: logging-prometheus
  version: "0.0.1"

source:
  fn: logging-prometheus-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/l/logging-prometheus/logging-prometheus-0.0.1.tar.gz
  md5: e04615a946db356bbc2b0bb294efad20
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - logging-prometheus = logging-prometheus:main
    #
    # Would create an entry point called logging-prometheus that calls logging-prometheus.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - prometheus_client >=0.0.8

  run:
    - python
    - prometheus_client >=0.0.8

test:
  # Python imports
  imports:
    - logging_prometheus

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/korfuri/python-logging-prometheus
  license: Apache Software License
  summary: 'Exports logging metrics for Prometheus.io.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
