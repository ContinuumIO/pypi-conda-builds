package:
  name: django-lifestreams
  version: "0.1.0"

source:
  fn: django-lifestreams-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-lifestreams/django-lifestreams-0.1.0.tar.gz
  md5: 8d9975ae9f886e8476fe64f0a8d24886
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-lifestreams = django-lifestreams:main
    #
    # Would create an entry point called django-lifestreams that calls django-lifestreams.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4
    - pytz >=2013b

  run:
    - python
    - django >=1.4
    - pytz >=2013b

test:
  # Python imports
  imports:
    - lifestreams
    - lifestreams.management
    - lifestreams.management.commands
    - lifestreams.plugins
    - lifestreams.plugins.lifestream_instagram
    - lifestreams.plugins.lifestream_rss
    - lifestreams.plugins.lifestream_twitter
    - lifestreams.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/witoi/django-lifestreams
  license: UNKNOWN
  summary: 'A lifestreams application for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
