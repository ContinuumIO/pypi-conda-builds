package:
  name: invisibleroads-scaffolds
  version: "1.8.0"

source:
  fn: invisibleroads-scaffolds-1.8.0.tar.gz
  url: https://pypi.python.org/packages/source/i/invisibleroads-scaffolds/invisibleroads-scaffolds-1.8.0.tar.gz
  md5: 256d916f17b835285d3267207b699cb8
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - invisibleroads-scaffolds = invisibleroads-scaffolds:main
    #
    # Would create an entry point called invisibleroads-scaffolds that calls invisibleroads-scaffolds.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyramid

  run:
    - python
    - setuptools
    - pyramid

test:
  # Python imports
  imports:
    - invisibleroads_scaffolds

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/invisibleroads/invisibleroads-scaffolds
  license: MIT License
  summary: 'Pyramid application scaffolds based on invisibleroads.com'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
