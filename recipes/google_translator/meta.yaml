package:
  name: google_translator
  version: "0.2"

source:
  fn: google_translator-0.2.tar.gz
  url: https://pypi.python.org/packages/source/g/google_translator/google_translator-0.2.tar.gz
  md5: f5ed171f258500443bf77cca8b4cb00d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - google_translator = google_translator:main
    #
    # Would create an entry point called google_translator that calls google_translator.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - google_translator

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lyrl/google_translator
  license: MIT License
  summary: 'google translate api'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
