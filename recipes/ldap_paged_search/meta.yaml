package:
  name: ldap_paged_search
  version: "0.4.2"

source:
  fn: ldap_paged_search-0.4.2.tar.gz
  url: https://pypi.python.org/packages/source/l/ldap_paged_search/ldap_paged_search-0.4.2.tar.gz
  md5: 15c15ed08ed0a0b9d162597b9991301b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ldap_paged_search = ldap_paged_search:main
    #
    # Would create an entry point called ldap_paged_search that calls ldap_paged_search.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - ldap_paged_search

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/neoCrimeLabs/python-ldap_paged_search
  license: GNU Lesser General Public License v2 or later (LGPLv2+)
  summary: 'Easily perform LDAP queries with more than 1000 results'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
