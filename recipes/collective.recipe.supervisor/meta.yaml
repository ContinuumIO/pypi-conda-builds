package:
  name: collective.recipe.supervisor
  version: "0.19"

source:
  fn: collective.recipe.supervisor-0.19.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.recipe.supervisor/collective.recipe.supervisor-0.19.tar.gz
  md5: 8a5303791693dd32b2b1cf98dc3f3a92
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.recipe.supervisor = collective.recipe.supervisor:main
    #
    # Would create an entry point called collective.recipe.supervisor that calls collective.recipe.supervisor.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - zc.recipe.egg

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - zc.recipe.egg

test:
  # Python imports
  imports:
    - collective
    - collective.recipe
    - collective.recipe.supervisor
    - collective.recipe.supervisor.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/collective.recipe.supervisor
  license: Zope Public License
  summary: 'A buildout recipe to install supervisor'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
