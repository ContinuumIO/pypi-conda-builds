package:
  name: flask-odesk
  version: "0.4.1.1"

source:
  fn: Flask-oDesk-0.4.1.1.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-oDesk/Flask-oDesk-0.4.1.1.tar.gz
  md5: e2581d230c3b8219b0f3d666cbe74e56
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-odesk = flask-odesk:main
    #
    # Would create an entry point called flask-odesk that calls flask-odesk.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - oauth2
    - python-odesk >=0.4

  run:
    - python
    - flask
    - oauth2
    - python-odesk >=0.4

test:
  # Python imports
  imports:
    - flaskext

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/odesk/flask-odesk
  license: BSD License
  summary: 'Adds oDesk API support to Flask'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
