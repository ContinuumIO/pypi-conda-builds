package:
  name: djinn_profiles
  version: "1.0.2"

source:
  fn: djinn_profiles-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/djinn_profiles/djinn_profiles-1.0.2.tar.gz
  md5: 99288fb44fdd8dba206e79ef3d77f555
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djinn_profiles = djinn_profiles:main
    #
    # Would create an entry point called djinn_profiles that calls djinn_profiles.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - djinn_core
    - django-markupfield

  run:
    - python
    - setuptools
    - django
    - djinn_core
    - django-markupfield

test:
  # Python imports
  imports:
    - djinn_profiles
    - djinn_profiles.forms
    - djinn_profiles.models

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/PythonUnited/djinn_profiles
  license: beer-ware
  summary: 'Djinn Intranet user and group profiles'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
