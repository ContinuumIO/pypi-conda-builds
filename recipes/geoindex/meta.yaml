package:
  name: geoindex
  version: "0.0.1"

source:
  fn: geoindex-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/g/geoindex/geoindex-0.0.1.tar.gz
  md5: b4c8eeefdd08c8c7e298e21d6d5c6e62
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - geoindex = geoindex:main
    #
    # Would create an entry point called geoindex that calls geoindex.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python-geohash >=0.8.4

  run:
    - python
    - python-geohash >=0.8.4

test:
  # Python imports
  imports:
    - geoindex

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/gusdan/geoindex
  license: BSD License
  summary: 'Simple library for perform quick nearby search for geo spatial data.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
