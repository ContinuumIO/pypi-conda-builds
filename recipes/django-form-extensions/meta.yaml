package:
  name: django-form-extensions
  version: "0.1.2b4"

source:
  fn: django-form-extensions-0.1.2b4.zip
  url: https://pypi.python.org/packages/source/d/django-form-extensions/django-form-extensions-0.1.2b4.zip
  md5: dc86a5f7ba845df97adcc1717b0ad320
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-form-extensions = django-form-extensions:main
    #
    # Would create an entry point called django-form-extensions that calls django-form-extensions.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.1
    - lxml >=2.0

  run:
    - python
    - django >=1.1
    - lxml >=2.0

test:
  # Python imports
  imports:
    - form_extensions

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/nextscreenlabs/django-form-extensions
  license: BSD License
  summary: 'Useful fields and widgets for Django forms.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
