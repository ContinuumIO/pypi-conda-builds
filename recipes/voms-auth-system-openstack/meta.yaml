package:
  name: voms-auth-system-openstack
  version: "1.0"

source:
  fn: voms-auth-system-openstack-1.0.tar.gz
  url: https://pypi.python.org/packages/source/v/voms-auth-system-openstack/voms-auth-system-openstack-1.0.tar.gz
  md5: 8c0c766bff0855805b88b6e92a77e94f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - voms-auth-system-openstack = voms-auth-system-openstack:main
    #
    # Would create an entry point called voms-auth-system-openstack that calls voms-auth-system-openstack.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - voms_auth_system_openstack

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/IFCA/voms-auth-system-openstack
  license: Apache Software License
  summary: 'VOMS-based authentication for Openstack'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
