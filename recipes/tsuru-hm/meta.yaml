package:
  name: tsuru-hm
  version: "0.2.2"

source:
  fn: tsuru-hm-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/t/tsuru-hm/tsuru-hm-0.2.2.tar.gz
  md5: 0df976f9afe2acc8230dd00f2b073014
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tsuru-hm = tsuru-hm:main
    #
    # Would create an entry point called tsuru-hm that calls tsuru-hm.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - boto ==2.25.0
    - pymongo ==2.6.3
    - requests ==2.4.3

  run:
    - python
    - boto ==2.25.0
    - pymongo ==2.6.3
    - requests ==2.4.3

test:
  # Python imports
  imports:
    - hm
    - hm.iaas
    - hm.lb_managers
    - hm.managers
    - hm.model

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tsuru/hm
  license: UNKNOWN
  summary: 'Host manager library for tsuru PaaS services'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
