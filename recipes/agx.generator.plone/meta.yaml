package:
  name: agx.generator.plone
  version: "1.0a1"

source:
  fn: agx.generator.plone-1.0a1.tar.gz
  url: https://pypi.python.org/packages/source/a/agx.generator.plone/agx.generator.plone-1.0a1.tar.gz
  md5: b71cc158b296233f7828bb7b1b744627
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - agx.generator.plone = agx.generator.plone:main
    #
    # Would create an entry point called agx.generator.plone that calls agx.generator.plone.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - agx.generator.zca
    - agx.generator.buildout

  run:
    - python
    - setuptools
    - setuptools
    - agx.generator.zca
    - agx.generator.buildout

test:
  # Python imports
  imports:
    - agx
    - agx.generator
    - agx.generator.plone
    - agx.generator.plone.testing

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/bluedynamics/agx.generator.plone
  license: GNU General Public License (GPL)
  summary: 'AGX generator for plone products'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
