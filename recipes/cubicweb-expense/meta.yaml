package:
  name: cubicweb-expense
  version: "0.7.1"

source:
  fn: cubicweb-expense-0.7.1.tar.gz
  url: https://pypi.python.org/packages/source/c/cubicweb-expense/cubicweb-expense-0.7.1.tar.gz
  md5: 49f6ca9d5d9168cb2dad5473b23efde6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cubicweb-expense = cubicweb-expense:main
    #
    # Would create an entry point called cubicweb-expense that calls cubicweb-expense.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cubicweb >=3.15.0
    - cubicweb-addressbook
    - cubicweb-file

  run:
    - python
    - cubicweb >=3.15.0
    - cubicweb-addressbook
    - cubicweb-file

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.cubicweb.org/project/cubicweb-expense
  license: LGPL
  summary: 'expense component for the CubicWeb framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
