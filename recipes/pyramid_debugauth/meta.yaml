package:
  name: pyramid_debugauth
  version: "0.1.2"

source:
  fn: pyramid_debugauth-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/p/pyramid_debugauth/pyramid_debugauth-0.1.2.tar.gz
  md5: 1acb1e0027565970151b98f8ed2de2ef
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pyramid_debugauth = pyramid_debugauth:main
    #
    # Would create an entry point called pyramid_debugauth that calls pyramid_debugauth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - pyramid_debugauth

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD-derived (http://www.repoze.org/LICENSE.txt)
  summary: 'Debug Authentication Policy for Pyramid'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
