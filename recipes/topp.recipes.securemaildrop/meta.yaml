package:
  name: topp.recipes.securemaildrop
  version: "0.1.4"

source:
  fn: topp.recipes.securemaildrop-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/t/topp.recipes.securemaildrop/topp.recipes.securemaildrop-0.1.4.tar.gz
  md5: 3ffa3a4bb027b3d254afe39a68c0e5bb
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - topp.recipes.securemaildrop = topp.recipes.securemaildrop:main
    #
    # Would create an entry point called topp.recipes.securemaildrop that calls topp.recipes.securemaildrop.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - zc.recipe.egg

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - zc.recipe.egg

test:
  # Python imports
  imports:
    - topp
    - topp.recipes
    - topp.recipes.securemaildrop
    - topp.recipes.securemaildrop.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: Zope Public License
  summary: 'Install MaildropHost and SecureMaildrop Zope products and maildrop daemon'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
