package:
  name: funsizer
  version: "0.1"

source:
  fn: funsizer-0.1.tar.gz
  url: https://pypi.python.org/packages/source/f/funsizer/funsizer-0.1.tar.gz
  md5: 1c64ccac73f889b3b689192e86d32b94
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - funsizer = funsizer:main
    #
    # Would create an entry point called funsizer that calls funsizer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - requests ==2.2.1

  run:
    - python
    - requests ==2.2.1

test:
  # Python imports
  imports:
    - funsizer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/MihaiTabara/funsizer
  license: MPL
  summary: 'Command line client to retrieve partials from https://github.com/mozilla/build-funsize'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
