package:
  name: django-raven-metlog
  version: "0.2"

source:
  fn: django-raven-metlog-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-raven-metlog/django-raven-metlog-0.2.tar.gz
  md5: 0b1b14c4a0bf9dababf6ac32dd5f01bf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-raven-metlog = django-raven-metlog:main
    #
    # Would create an entry point called django-raven-metlog that calls django-raven-metlog.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - docopt
    - raven
    - metlog-raven >=0.4

  run:
    - python
    - docopt
    - raven
    - metlog-raven >=0.4

test:
  # Python imports
  imports:
    - djangoraven

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mozilla-services/django-raven-metlog
  license: Mozilla Public License 2.0 (MPL 2.0)
  summary: 'Django+Raven+Meltog integration'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
