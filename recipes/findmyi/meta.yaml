package:
  name: findmyi
  version: "0.2"

source:
  fn: findmyi-0.2.tar.gz
  url: https://pypi.python.org/packages/source/f/findmyi/findmyi-0.2.tar.gz
  md5: c567eb0dece2731310d1990c26f323cb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - findmyi = findmyi:main
    #
    # Would create an entry point called findmyi that calls findmyi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests ==1.1.0

  run:
    - python
    - requests ==1.1.0

test:
  # Python imports
  imports:
    - findmyi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/raphaelmutschler/fmi
  license: UNKNOWN
  summary: 'A simple Python interface to Find My iPhone over the iCloud service'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
