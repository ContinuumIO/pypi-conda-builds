package:
  name: atelier
  version: "0.0.14"

source:
  fn: atelier-0.0.14.tar.gz
  url: https://pypi.python.org/packages/source/a/atelier/atelier-0.0.14.tar.gz
  md5: b17294d4f6a1b460ad8b4676cdddc52b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - atelier = atelier:main
    #
    # Would create an entry point called atelier that calls atelier.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fabric
    - sphinx
    - babel
    - unipath
    - python_dateutil

  run:
    - python
    - fabric
    - sphinx
    - babel
    - unipath
    - python_dateutil

test:
  # Python imports
  imports:
    - atelier
    - atelier.sphinxconf

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://atelier.lino-framework.org
  license: BSD License
  summary: 'A collection of tools for software artists'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
