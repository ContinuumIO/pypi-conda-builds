package:
  name: django-versioning
  version: "0.7.5.0"

source:
  fn: django-versioning-0.7.5.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-versioning/django-versioning-0.7.5.0.tar.gz
  md5: 89977dfd401f3e76805936d97df11cf3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-versioning = django-versioning:main
    #
    # Would create an entry point called django-versioning that calls django-versioning.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - versioning
    - versioning.management
    - versioning.management.commands
    - versioning.templatetags
    - versioning.vendor
    - versioning.vendor.diff_match_patch
    - versioning.vendor.diff_match_patch.python2
    - versioning.vendor.diff_match_patch.python3

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/emacsway/django-versioning-0.7.5.0
  license: BSD License
  summary: 'Django-versioning allows you to version the data stored in django models, and stores only diff, not content copy.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
