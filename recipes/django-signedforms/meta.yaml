package:
  name: django-signedforms
  version: "0.2"

source:
  fn: django-signedforms-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-signedforms/django-signedforms-0.2.tar.gz
  md5: 7b46610813e4281551a7347af091d7fe
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-signedforms = django-signedforms:main
    #
    # Would create an entry point called django-signedforms that calls django-signedforms.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >1.3

  run:
    - python
    - django >1.3

test:
  # Python imports
  imports:
    - signedforms

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/piquadrat/django-signedforms
  license: BSD License
  summary: 'A signed Django form'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
