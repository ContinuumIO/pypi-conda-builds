package:
  name: django-localcrawler
  version: "0.1.1"

source:
  fn: django-localcrawler-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-localcrawler/django-localcrawler-0.1.1.tar.gz
  md5: 1f453f293de7ba5748aa4e14cafb2ef5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-localcrawler = django-localcrawler:main
    #
    # Would create an entry point called django-localcrawler that calls django-localcrawler.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.2
    - beautifulsoup >=3.0.8.1

  run:
    - python
    - django >=1.2
    - beautifulsoup >=3.0.8.1

test:
  # Python imports
  imports:
    - localcrawler
    - localcrawler.management
    - localcrawler.management.commands
    - testapp

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/ojii/django-localcrawler
  license: BSD
  summary: 'django-localcrawler'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
