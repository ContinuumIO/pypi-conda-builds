package:
  name: hgsubversion
  version: "1.8.1"

source:
  fn: hgsubversion-1.8.1.tar.gz
  url: https://pypi.python.org/packages/source/h/hgsubversion/hgsubversion-1.8.1.tar.gz
  md5: 7ad0e4cd7dc1acabdd076511d485b247
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hgsubversion = hgsubversion:main
    #
    # Would create an entry point called hgsubversion that calls hgsubversion.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - mercurial
    - subvertpy >=0.7.4

  run:
    - python
    - mercurial
    - subvertpy >=0.7.4

test:
  # Python imports
  imports:
    - hgsubversion
    - hgsubversion.hooks
    - hgsubversion.layouts
    - hgsubversion.svnwrap

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/durin42/hgsubversion
  license: GNU General Public License (GPL)
  summary: 'hgsubversion is a Mercurial extension for working with Subversion repositories.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
