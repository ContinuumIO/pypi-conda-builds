package:
  name: mollie-api-python
  version: "1.0.0"

source:
  fn: mollie-api-python-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/m/mollie-api-python/mollie-api-python-1.0.0.tar.gz
  md5: 8209bfc7395b1a3ffdc9960b93f58937
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mollie-api-python = mollie-api-python:main
    #
    # Would create an entry point called mollie-api-python that calls mollie-api-python.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - Mollie
    - Mollie.API
    - Mollie.API.Object
    - Mollie.API.Resource

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mollie/mollie-api-python
  license: BSD
  summary: 'Mollie API client for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
