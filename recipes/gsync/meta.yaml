package:
  name: gsync
  version: "0.1.14"

source:
  fn: gsync-0.1.14.tar.gz
  url: https://pypi.python.org/packages/source/g/gsync/gsync-0.1.14.tar.gz
  md5: 86a8b88d5cbe66789fdb67552ae86ac5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gsync = gsync:main
    #
    # Would create an entry point called gsync that calls gsync.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - google-api-python-client >=1.2
    - docopt >=0.6.0
    - httplib2 >=0.8
    - oauth2client >=1.1
    - python-dateutil >=1.5
    - urllib3 >=1.5

  run:
    - python
    - google-api-python-client >=1.2
    - docopt >=0.6.0
    - httplib2 >=0.8
    - oauth2client >=1.1
    - python-dateutil >=1.5
    - urllib3 >=1.5

test:
  # Python imports
  imports:
    - gsync
    - libgsync
    - libgsync.drive
    - libgsync.options
    - libgsync.sync
    - libgsync.sync.file
    - libgsync.sync.file.local
    - libgsync.sync.file.remote

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/iwonbigbro/gsync
  license: BSD License
  summary: 'GSync - RSync for Google Drive'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
