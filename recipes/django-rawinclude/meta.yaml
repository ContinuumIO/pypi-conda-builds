package:
  name: django-rawinclude
  version: "0.1"

source:
  fn: django-rawinclude-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-rawinclude/django-rawinclude-0.1.tar.gz
  md5: b27e18beb8a5e8dcb02f233a535dafc2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-rawinclude = django-rawinclude:main
    #
    # Would create an entry point called django-rawinclude that calls django-rawinclude.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - distribute

  run:
    - python
    - distribute

test:
  # Python imports
  imports:
    - rawinclude
    - rawinclude.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/niwibe/django-rawinclude
  license: BSD License
  summary: 'Small module for django that gives the ease of loading templates in raw.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
