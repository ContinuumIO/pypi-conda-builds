package:
  name: geoformalchemy
  version: "0.4"

source:
  fn: GeoFormAlchemy-0.4.tar.gz
  url: https://pypi.python.org/packages/source/G/GeoFormAlchemy/GeoFormAlchemy-0.4.tar.gz
  md5: 1ee72e734040017f4ef7f194f24c4f4c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - geoformalchemy = geoformalchemy:main
    #
    # Would create an entry point called geoformalchemy that calls geoformalchemy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - formalchemy >=1.3.0
    - geoalchemy >=0.4

  run:
    - python
    - setuptools
    - formalchemy >=1.3.0
    - geoalchemy >=0.4

test:
  # Python imports
  imports:
    - geoformalchemy
    - geoformalchemy.pylons

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/camptocamp/GeoFormAlchemy
  license: MIT License
  summary: 'GeoFormAlchemy - a extension for FormAlchemy that adds support for spatial databases'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
