package:
  name: plone.app.hud
  version: "1.0a1"

source:
  fn: plone.app.hud-1.0a1.zip
  url: https://pypi.python.org/packages/source/p/plone.app.hud/plone.app.hud-1.0a1.zip
  md5: f16e1d23803e9ac78bc6946c294009e5
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.app.hud = plone.app.hud:main
    #
    # Would create an entry point called plone.app.hud that calls plone.app.hud.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pillow
    - plone
    - plone.api
    - plone.hud
    - setuptools

  run:
    - python
    - setuptools
    - pillow
    - plone
    - plone.api
    - plone.hud
    - setuptools

test:
  # Python imports
  imports:
    - plone
    - plone.app
    - plone.app.hud
    - plone.app.hud.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/plone.app.hud
  license: GPL version 2
  summary: 'Plone Heads Up Display Panels'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
