package:
  name: django-laconicurls
  version: "0.1.1"

source:
  fn: django-laconicurls-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-laconicurls/django-laconicurls-0.1.1.tar.gz
  md5: 9b1f1e780e029c757d83bb74bda0ee87
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-laconicurls = django-laconicurls:main
    #
    # Would create an entry point called django-laconicurls that calls django-laconicurls.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - laconicurls

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/robert-b-clarke/django-laconicurls
  license: BSD License
  summary: 'A generic url shortner for content in a Django ORM'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
