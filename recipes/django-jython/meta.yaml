package:
  name: django-jython
  version: "1.7.0b2"

source:
  fn: django-jython-1.7.0b2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-jython/django-jython-1.7.0b2.tar.gz
  md5: 28c2b5e28b0145b99626e9f8977cf116
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-jython = django-jython:main
    #
    # Would create an entry point called django-jython that calls django-jython.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - doj
    - doj.db
    - doj.db.backends
    - doj.db.backends.mssql
    - doj.db.backends.mysql
    - doj.db.backends.postgresql
    - doj.db.backends.sqlite
    - doj.management
    - doj.management.commands
    - doj.monkey
    - doj.tests
    - doj.tests.db
    - doj.tests.db.migrations
    - doj.tests.monkey

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/django-jython/
  license: BSD License
  summary: u'Database backends and management commands, for development under Django/Jython'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
