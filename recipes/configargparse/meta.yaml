package:
  name: configargparse
  version: "0.9.3"

source:
  fn: ConfigArgParse-0.9.3.tar.gz
  url: https://pypi.python.org/packages/source/C/ConfigArgParse/ConfigArgParse-0.9.3.tar.gz
  md5: 69273e8099661cd12985b85d795ab73e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - configargparse = configargparse:main
    #
    # Would create an entry point called configargparse that calls configargparse.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - configargparse

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/zorro3/ConfigArgParse
  license: MIT License
  summary: 'A drop-in replacement for argparse that allows options to also be set via config files and/or environment variables.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
