package:
  name: django-qunit2
  version: "20130822"

source:
  fn: django-qunit2-20130822.zip
  url: https://pypi.python.org/packages/source/d/django-qunit2/django-qunit2-20130822.zip
  md5: c062fde25305952667e4b8b85260dd11
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-qunit2 = django-qunit2:main
    #
    # Would create an entry point called django-qunit2 that calls django-qunit2.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_qunit

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/turtlemonvh/django-qunit/
  license: MIT License
  summary: 'QUnit Javascript testing integration for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
