package:
  name: bowerrecipe
  version: "0.1.1"

source:
  fn: bowerrecipe-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/b/bowerrecipe/bowerrecipe-0.1.1.tar.gz
  md5: 5d943831d660d508fb2eed09927c6023
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bowerrecipe = bowerrecipe:main
    #
    # Would create an entry point called bowerrecipe that calls bowerrecipe.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout

test:
  # Python imports
  imports:
    - bowerrecipe
    - bowerrecipe.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/anshumanb/bowerrecipe
  license: MIT License
  summary: 'zc.buildout recipe to install static resources using Twitter Bower.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
