package:
  name: django-serene
  version: "0.0.9"

source:
  fn: django-serene-0.0.9.zip
  url: https://pypi.python.org/packages/source/d/django-serene/django-serene-0.0.9.zip
  md5: f5cecc3a2fa9105e631a99817e1cb4b3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-serene = django-serene:main
    #
    # Would create an entry point called django-serene that calls django-serene.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - djangorestframework ==0.3.3

  run:
    - python
    - djangorestframework ==0.3.3

test:
  # Python imports
  imports:
    - serene
    - serene.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/django-serene/
  license: GNU General Public License (GPL)
  summary: 'An enhanced djangorestframework. Serene is a RESTful framework with a touch of sirisanthana'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
