package:
  name: django-authentication
  version: "0.1.5"

source:
  fn: django-authentication-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-authentication/django-authentication-0.1.5.tar.gz
  md5: e70254b1ebe0b4f12f2739d2019febb4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-authentication = django-authentication:main
    #
    # Would create an entry point called django-authentication that calls django-authentication.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django ==1.5.1
    - django-braces ==1.0.0
    - django-extensions ==1.1.1

  run:
    - python
    - django ==1.5.1
    - django-braces ==1.0.0
    - django-extensions ==1.1.1

test:
  # Python imports
  imports:
    - authentication
    - authentication.tests
    - authentication.views

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT License
  summary: 'Django authentication components.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
