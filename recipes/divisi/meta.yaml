package:
  name: divisi
  version: "0.6.10"

source:
  fn: Divisi-0.6.10.tar.gz
  url: https://pypi.python.org/packages/source/D/Divisi/Divisi-0.6.10.tar.gz
  md5: d689e3a1a8173492b130986d779d9586
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - divisi = divisi:main
    #
    # Would create an entry point called divisi that calls divisi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - csc-utils >=0.4.1

  run:
    - python
    - csc-utils >=0.4.1

test:
  # Python imports
  imports:
    - csc
    - csc.divisi
    - divisi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://divisi.media.mit.edu/
  license: GNU General Public License (GPL)
  summary: 'Divisi: Commonsense Reasoning over Semantic Networks'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
