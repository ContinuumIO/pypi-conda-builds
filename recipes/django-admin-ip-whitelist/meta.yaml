package:
  name: django-admin-ip-whitelist
  version: "0.1"

source:
  fn: django-admin-ip-whitelist-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-admin-ip-whitelist/django-admin-ip-whitelist-0.1.tar.gz
  md5: cda2dab0064db298fcca4297e846a249
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-admin-ip-whitelist = django-admin-ip-whitelist:main
    #
    # Would create an entry point called django-admin-ip-whitelist that calls django-admin-ip-whitelist.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - admin_ip_whitelist

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/dvska/django-admin-ip-whitelist
  license: Apache Software License
  summary: 'Django middleware to allow access to /admin only for users, whose IPs are in the white list'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
