package:
  name: flask-cache-latest
  version: "0.12"

source:
  fn: Flask-Cache-Latest-0.12.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Cache-Latest/Flask-Cache-Latest-0.12.tar.gz
  md5: 5cdbc4d0f24274e367ea446d5086f903
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-cache-latest = flask-cache-latest:main
    #
    # Would create an entry point called flask-cache-latest that calls flask-cache-latest.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask

  run:
    - python
    - flask

test:
  # Python imports
  imports:
    - flask_cache

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/thadeusb/flask-cache/tree/18cd9ebdb20e4d0f8f0900b971fcb8d48e27737d
  license: BSD License
  summary: 'Adds cache support to your Flask application'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
