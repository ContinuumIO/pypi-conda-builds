package:
  name: collective.barcamp
  version: "0.1"

source:
  fn: collective.barcamp-0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.barcamp/collective.barcamp-0.1.tar.gz
  md5: 6960fa5249b0aa9620b3630ba7e86cc1
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.barcamp = collective.barcamp:main
    #
    # Would create an entry point called collective.barcamp that calls collective.barcamp.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone
    - plone.z3cform

  run:
    - python
    - setuptools
    - setuptools
    - plone
    - plone.z3cform

test:
  # Python imports
  imports:
    - collective
    - collective.barcamp
    - collective.barcamp.browser
    - collective.barcamp.content
    - collective.barcamp.interfaces
    - collective.barcamp.portlets
    - collective.barcamp.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/
  license: GNU General Public License (GPL)
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
