package:
  name: junitxml
  version: "0.7"

source:
  fn: junitxml-0.7.tar.gz
  url: https://pypi.python.org/packages/source/j/junitxml/junitxml-0.7.tar.gz
  md5: 20d17c015fc11781f9d30bf89b862590
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - junitxml = junitxml:main
    #
    # Would create an entry point called junitxml that calls junitxml.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - junitxml
    - junitxml.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://launchpad.net/pyjunitxml
  license: LGPL-3
  summary: 'PyJUnitXML, a pyunit extension to output JUnit compatible XML.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
