package:
  name: django-sortable-fc
  version: "1.0"

source:
  fn: django-sortable-fc-1.0.zip
  url: https://pypi.python.org/packages/source/d/django-sortable-fc/django-sortable-fc-1.0.zip
  md5: 9265167be3dee391c40fa6eb00632bca
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-sortable-fc = django-sortable-fc:main
    #
    # Would create an entry point called django-sortable-fc that calls django-sortable-fc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - sortable

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/futurecolors/django-sortable/
  license: BSD License
  summary: 'An app to add drag-and-drop to admin to reorder instances of models.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
