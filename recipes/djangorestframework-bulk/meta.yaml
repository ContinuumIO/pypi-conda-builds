package:
  name: djangorestframework-bulk
  version: "0.2.1"

source:
  fn: djangorestframework-bulk-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/djangorestframework-bulk/djangorestframework-bulk-0.2.1.tar.gz
  md5: 1f926143ebccaf25699cab77dc5e9bf4
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangorestframework-bulk = djangorestframework-bulk:main
    #
    # Would create an entry point called djangorestframework-bulk that calls djangorestframework-bulk.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django
    - djangorestframework
    - setuptools

  run:
    - python
    - django
    - djangorestframework
    - setuptools

test:
  # Python imports
  imports:
    - rest_framework_bulk
    - rest_framework_bulk.drf2
    - rest_framework_bulk.drf3
    - rest_framework_bulk.tests
    - rest_framework_bulk.tests.simple_app

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/miki725/django-rest-framework-bulk
  license: MIT License
  summary: u'Django REST Framework bulk CRUD view mixins'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
