package:
  name: metarace
  version: "1.6.64"

source:
  fn: metarace-1.6.64.tar.gz
  url: https://pypi.python.org/packages/source/m/metarace/metarace-1.6.64.tar.gz
  md5: dba60d80f7885e28caa2ac1b630b9f13
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - metarace = metarace:main
    #
    # Would create an entry point called metarace that calls metarace.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - metarace

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://metarace.com.au/_about/software
  license: GNU General Public License (GPL)
  summary: 'Cycle race abstractions'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
