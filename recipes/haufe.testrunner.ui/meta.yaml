package:
  name: haufe.testrunner.ui
  version: "0.1.5"

source:
  fn: haufe.testrunner.ui-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/h/haufe.testrunner.ui/haufe.testrunner.ui-0.1.5.tar.gz
  md5: 8766b79f2c4cb35facf8f99ab3ac69f0
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - haufe.testrunner.ui = haufe.testrunner.ui:main
    #
    # Would create an entry point called haufe.testrunner.ui that calls haufe.testrunner.ui.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - grok
    - sqlalchemy
    - zope.sqlalchemy >=0.2.0
    - haufe.testrunner >=0.6.1

  run:
    - python
    - setuptools
    - setuptools
    - grok
    - sqlalchemy
    - zope.sqlalchemy >=0.2.0
    - haufe.testrunner >=0.6.1

test:
  # Python imports
  imports:
    - haufe_testrunner_ui

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: ZPL
  summary: 'Webfrontend for haufe.testrunner results'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
