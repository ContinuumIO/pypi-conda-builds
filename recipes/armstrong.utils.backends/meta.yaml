package:
  name: armstrong.utils.backends
  version: "1.1.1"

source:
  fn: armstrong.utils.backends-1.1.1.tar.gz
  url: https://pypi.python.org/packages/source/a/armstrong.utils.backends/armstrong.utils.backends-1.1.1.tar.gz
  md5: a7d1544fe015503d36fbf257c0475268
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - armstrong.utils.backends = armstrong.utils.backends:main
    #
    # Would create an entry point called armstrong.utils.backends that calls armstrong.utils.backends.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - armstrong
    - armstrong.utils
    - armstrong.utils.backends

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/armstrong/armstrong.utils.backends/
  license: Apache Software License
  summary: u'Generic backend system to use throughout Armstrong'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
