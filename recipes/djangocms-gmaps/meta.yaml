package:
  name: djangocms-gmaps
  version: "0.1.3"

source:
  fn: djangocms-gmaps-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/d/djangocms-gmaps/djangocms-gmaps-0.1.3.tar.gz
  md5: 70e8e9cea7832c64ea3fd27f2edb7ea4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangocms-gmaps = djangocms-gmaps:main
    #
    # Would create an entry point called djangocms-gmaps that calls djangocms-gmaps.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-appconf
    - jsonfield
    - django-cms >=3.0
    - django-filer >=0.9
    - easy-thumbnails >=1.0
    - django-sekizai >=0.7

  run:
    - python
    - django-appconf
    - jsonfield
    - django-cms >=3.0
    - django-filer >=0.9
    - easy-thumbnails >=1.0
    - django-sekizai >=0.7

test:
  # Python imports
  imports:
    - djangocms_gmaps

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mishbahr/djangocms-gmaps
  license: BSD License
  summary: 'The easiest way to embed Google Maps for your django-cms powered site. This is a great way to display the location of your business or event.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
