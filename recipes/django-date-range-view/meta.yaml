package:
  name: django-date-range-view
  version: "0.1.1"

source:
  fn: django-date-range-view-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-date-range-view/django-date-range-view-0.1.1.tar.gz
  md5: 714636cb3e06c9c47d923244d088840b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-date-range-view = django-date-range-view:main
    #
    # Would create an entry point called django-date-range-view that calls django-date-range-view.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python-dateutil

  run:
    - python
    - python-dateutil

test:
  # Python imports
  imports:
    - django_date_range_view

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/freakypie/django-date-range-view
  license: BSD License
  summary: 'provides a simple date range parsing django view'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
