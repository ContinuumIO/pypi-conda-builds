package:
  name: collective.mathjax
  version: "1.1.1"

source:
  fn: collective.mathjax-1.1.1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.mathjax/collective.mathjax-1.1.1.tar.gz
  md5: bb5d50290ee471b7333494751b28d232
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.mathjax = collective.mathjax:main
    #
    # Would create an entry point called collective.mathjax that calls collective.mathjax.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.mathjax

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/collective/collective.mathjax
  license: GPL
  summary: 'MathJax integration for Plone.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
