package:
  name: django-cachebuster
  version: "0.2.1"

source:
  fn: django-cachebuster-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-cachebuster/django-cachebuster-0.2.1.tar.gz
  md5: 62dad73c9d15bc8a6f01894c64d3cc11
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-cachebuster = django-cachebuster:main
    #
    # Would create an entry point called django-cachebuster that calls django-cachebuster.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - cachebuster
    - cachebuster.detectors
    - cachebuster.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/jaddison/django-cachebuster
  license: BSD License
  summary: 'Django 1.3 ready cache busting app'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
