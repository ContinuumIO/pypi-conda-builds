package:
  name: bridgepython
  version: "0.2.2"

source:
  fn: BridgePython-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/B/BridgePython/BridgePython-0.2.2.tar.gz
  md5: 0a08da7ad97f92060936fccb62001c2c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bridgepython = bridgepython:main
    #
    # Would create an entry point called bridgepython that calls bridgepython.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - BridgePython
    - BridgePython.data
    - bridgepython

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/BridgePython/
  license: LICENSE.txt
  summary: 'A Python API for the Bridge service.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
