package:
  name: linkchecker
  version: "9.3"

source:
  fn: LinkChecker-9.3.tar.gz
  url: https://pypi.python.org/packages/source/L/LinkChecker/LinkChecker-9.3.tar.gz
  md5: d7ba7ff04715022af5574b4e6cccbd27
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - linkchecker = linkchecker:main
    #
    # Would create an entry point called linkchecker that calls linkchecker.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=2.2.0

  run:
    - python
    - requests >=2.2.0

test:
  # Python imports
  imports:
    - linkcheck
    - linkcheck.HtmlParser
    - linkcheck.bookmarks
    - linkcheck.cache
    - linkcheck.checker
    - linkcheck.configuration
    - linkcheck.director
    - linkcheck.gui
    - linkcheck.htmlutil
    - linkcheck.logger
    - linkcheck.network
    - linkcheck.parser
    - linkcheck.plugins
    - linkcheck_dns.dns
    - linkcheck_dns.dns.rdtypes
    - linkcheck_dns.dns.rdtypes.ANY
    - linkcheck_dns.dns.rdtypes.IN
    - linkchecker

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://wummel.github.io/linkchecker/
  license: GNU General Public License (GPL)
  summary: 'check links in web documents or full websites'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
