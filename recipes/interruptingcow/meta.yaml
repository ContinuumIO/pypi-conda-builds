package:
  name: interruptingcow
  version: "0.6"

source:
  fn: interruptingcow-0.6.tar.gz
  url: https://pypi.python.org/packages/source/i/interruptingcow/interruptingcow-0.6.tar.gz
  md5: f31b238e72ce40239dd2275ccb73945f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - interruptingcow = interruptingcow:main
    #
    # Would create an entry point called interruptingcow that calls interruptingcow.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - interruptingcow

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/evzijst/interruptingcow
  license: MIT License
  summary: 'A watchdog that interrupts long running code.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
