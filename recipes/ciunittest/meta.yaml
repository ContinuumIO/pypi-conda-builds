package:
  name: ciunittest
  version: "1.0.6"

source:
  fn: CIUnitTest-1.0.6.tar.gz
  url: https://pypi.python.org/packages/source/C/CIUnitTest/CIUnitTest-1.0.6.tar.gz
  md5: 850b1b852756e5d68782155dbb37fd0d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ciunittest = ciunittest:main
    #
    # Would create an entry point called ciunittest that calls ciunittest.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - ciunittest

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://go.pelicandd.com/n/python-ciunittest
  license: MS-PL
  summary: 'Provides test results of unittest in JSON format, in order to be able to use the results programmatically.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
