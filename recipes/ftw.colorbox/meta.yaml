package:
  name: ftw.colorbox
  version: "1.2.0"

source:
  fn: ftw.colorbox-1.2.0.zip
  url: https://pypi.python.org/packages/source/f/ftw.colorbox/ftw.colorbox-1.2.0.zip
  md5: 80a2e1cc1c3a8a901f892e7dde05e582
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ftw.colorbox = ftw.colorbox:main
    #
    # Would create an entry point called ftw.colorbox that calls ftw.colorbox.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - ftw
    - ftw.colorbox
    - ftw.colorbox.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/4teamwork/ftw.colorbox
  license: GPL2
  summary: 'An image gallery for Plone using ColorBox'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
