package:
  name: django-briefcase
  version: "0.1b2"

source:
  fn: django-briefcase-0.1b2.zip
  url: https://pypi.python.org/packages/source/d/django-briefcase/django-briefcase-0.1b2.zip
  md5: 090c7fac8d733198916536cc161cb9cb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-briefcase = django-briefcase:main
    #
    # Would create an entry point called django-briefcase that calls django-briefcase.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - south

  run:
    - python
    - django
    - south

test:
  # Python imports
  imports:
    - briefcase
    - briefcase.migrations
    - briefcase.templatetags
    - briefcase.views

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/zsiciarz/django-briefcase
  license: MIT License
  summary: 'Yet another document management app for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
