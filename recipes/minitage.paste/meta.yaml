package:
  name: minitage.paste
  version: "1.4.6"

source:
  fn: minitage.paste-1.4.6.zip
  url: https://pypi.python.org/packages/source/m/minitage.paste/minitage.paste-1.4.6.zip
  md5: 19408106b94736dedc0553832d737b4e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - minitage.paste = minitage.paste:main
    #
    # Would create an entry point called minitage.paste that calls minitage.paste.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - pastescript
    - iniparse

  run:
    - python
    - setuptools
    - setuptools
    - pastescript
    - iniparse

test:
  # Python imports
  imports:
    - minitage
    - minitage.paste
    - minitage.paste.common
    - minitage.paste.instances
    - minitage.paste.instances.apache
    - minitage.paste.instances.common
    - minitage.paste.instances.env
    - minitage.paste.instances.mysql
    - minitage.paste.instances.nginx
    - minitage.paste.instances.pasteinitd
    - minitage.paste.instances.postgresql
    - minitage.paste.instances.varnish
    - minitage.paste.instances.varnish2
    - minitage.paste.projects
    - minitage.paste.projects.common
    - minitage.paste.projects.django
    - minitage.paste.projects.plone25
    - minitage.paste.projects.plone3
    - minitage.paste.projects.plone4
    - minitage.paste.projects.plone41
    - minitage.paste.projects.plone42
    - minitage.paste.projects.plone43
    - minitage.paste.projects.plone44
    - minitage.paste.projects.pyramid
    - minitage.paste.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://cheeseshop.python.org/pypi/minitage.paste
  license: BSD License
  summary: 'PasteScripts to facilitate use of minitage and creation of minitage based projects sponsored by Makina Corpus.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
