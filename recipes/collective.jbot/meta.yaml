package:
  name: collective.jbot
  version: "1.0a3"

source:
  fn: collective.jbot-1.0a3.zip
  url: https://pypi.python.org/packages/source/c/collective.jbot/collective.jbot-1.0a3.zip
  md5: c3b6f4ab9e8cefe921065da40090534b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.jbot = collective.jbot:main
    #
    # Would create an entry point called collective.jbot that calls collective.jbot.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - z3c.jbot
    - five.globalrequest
    - plone.app.theming >=1.1
    - plone.resource

  run:
    - python
    - setuptools
    - setuptools
    - z3c.jbot
    - five.globalrequest
    - plone.app.theming >=1.1
    - plone.resource

test:
  # Python imports
  imports:
    - collective
    - collective.jbot
    - collective.jbot.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.jbot
  license: GPL
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
