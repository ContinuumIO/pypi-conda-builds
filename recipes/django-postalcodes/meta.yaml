package:
  name: django-postalcodes
  version: "0.3.0"

source:
  fn: django-postalcodes-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-postalcodes/django-postalcodes-0.3.0.tar.gz
  md5: 8268ef5dbc4ab0ac5c595914c847599b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-postalcodes = django-postalcodes:main
    #
    # Would create an entry point called django-postalcodes that calls django-postalcodes.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3

  run:
    - python
    - django >=1.3

test:
  # Python imports
  imports:
    - postalcodes
    - postalcodes.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bennylope/django-postalcodes/
  license: BSD License
  summary: 'Postal code management.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
