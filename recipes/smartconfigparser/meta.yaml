package:
  name: smartconfigparser
  version: "0.0.4"

source:
  fn: smartconfigparser-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/s/smartconfigparser/smartconfigparser-0.0.4.tar.gz
  md5: 73651f40ba5d59d4aabfb4e9b224f324
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - smartconfigparser = smartconfigparser:main
    #
    # Would create an entry point called smartconfigparser that calls smartconfigparser.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - smartconfigparser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/guillaumevincent/smartconfigparser.git
  license: wtfpl - Do what the fuck you want to public license
  summary: 'SmartConfigParser add missing default value in ConfigParser module'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
