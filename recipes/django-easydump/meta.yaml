package:
  name: django-easydump
  version: "0.2.6"

source:
  fn: django-easydump-0.2.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-easydump/django-easydump-0.2.6.tar.gz
  md5: fc404faa717c2a79ffa46fdec4ca4e93
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-easydump = django-easydump:main
    #
    # Would create an entry point called django-easydump that calls django-easydump.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - python-dateutil
    - boto

  run:
    - python
    - django
    - python-dateutil
    - boto

test:
  # Python imports
  imports:
    - easydump
    - easydump.management
    - easydump.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://priestc.github.com/django-easydump/
  license: UNKNOWN
  summary: 'Easily load database snapshots across deployments'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
