package:
  name: incuna-countries
  version: "1.0.3"

source:
  fn: incuna-countries-1.0.3.tar.gz
  url: https://pypi.python.org/packages/source/i/incuna-countries/incuna-countries-1.0.3.tar.gz
  md5: d399c2e03fdc8856af0a750e971c8ad6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - incuna-countries = incuna-countries:main
    #
    # Would create an entry point called incuna-countries that calls incuna-countries.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - countries

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://incuna.com/
  license: UNKNOWN
  summary: 'Countries model and fixtures.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
