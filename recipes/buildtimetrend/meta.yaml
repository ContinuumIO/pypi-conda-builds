package:
  name: buildtimetrend
  version: "0.2"

source:
  fn: buildtimetrend-0.2.tar.gz
  url: https://pypi.python.org/packages/source/b/buildtimetrend/buildtimetrend-0.2.tar.gz
  md5: e23ff9ea62368179c1bbb7c2b16635c4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - buildtimetrend = buildtimetrend:main
    #
    # Would create an entry point called buildtimetrend that calls buildtimetrend.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - keen >=0.3.9
    - lxml
    - pyyaml
    - python-dateutil

  run:
    - python
    - keen >=0.3.9
    - lxml
    - pyyaml
    - python-dateutil

test:
  # Python imports
  imports:
    - buildtimetrend

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://buildtimetrend.github.io/
  license: GNU Affero General Public License v3 or later (AGPLv3+)
  summary: "Visualise what's trending in your build process"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
