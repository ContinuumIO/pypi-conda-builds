package:
  name: linkscrubber
  version: "1.2.0"

source:
  fn: linkscrubber-1.2.0.tar.gz
  url: https://pypi.python.org/packages/source/l/linkscrubber/linkscrubber-1.2.0.tar.gz
  md5: a61cad39907a20ff452808482ad498b6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - linkscrubber = linkscrubber:main
    #
    # Would create an entry point called linkscrubber that calls linkscrubber.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - linkscrubber

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT License
  summary: 'pinboard.in bookmark cleaner'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
