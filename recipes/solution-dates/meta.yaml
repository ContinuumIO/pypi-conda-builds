package:
  name: solution-dates
  version: "0.6.2"

source:
  fn: Solution-Dates-0.6.2.tar.gz
  url: https://pypi.python.org/packages/source/S/Solution-Dates/Solution-Dates-0.6.2.tar.gz
  md5: 5bcee2887c4c1a96c87ddeed33878438
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - solution-dates = solution-dates:main
    #
    # Would create an entry point called solution-dates that calls solution-dates.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - babel >=1.3
    - pytz >=0a
    - solution >=2.3.4

  run:
    - python
    - babel >=1.3
    - pytz >=0a
    - solution >=2.3.4

test:
  # Python imports
  imports:
    - solution_dates
    - solution_dates.fields
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/lucuma/solution_dates
  license: BSD License
  summary: 'A dates-handling extension for the Solution library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
