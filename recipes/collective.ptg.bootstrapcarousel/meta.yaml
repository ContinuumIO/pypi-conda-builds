package:
  name: collective.ptg.bootstrapcarousel
  version: "1.0.1"

source:
  fn: collective.ptg.bootstrapcarousel-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.ptg.bootstrapcarousel/collective.ptg.bootstrapcarousel-1.0.1.tar.gz
  md5: e72f6d7165cd3901f986f78d3ba6177c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.ptg.bootstrapcarousel = collective.ptg.bootstrapcarousel:main
    #
    # Would create an entry point called collective.ptg.bootstrapcarousel that calls collective.ptg.bootstrapcarousel.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.plonetruegallery

  run:
    - python
    - setuptools
    - setuptools
    - collective.plonetruegallery

test:
  # Python imports
  imports:
    - collective
    - collective.ptg
    - collective.ptg.bootstrapcarousel

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jsonmurphy/collective.ptg.boostrapcarousel
  license: GPL
  summary: 'Add Bootstraps Carousel to collective.plonetruegallery'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
