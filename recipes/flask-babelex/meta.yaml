package:
  name: flask-babelex
  version: "0.9.2"

source:
  fn: Flask-BabelEx-0.9.2.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-BabelEx/Flask-BabelEx-0.9.2.tar.gz
  md5: 9db1b9afac2faf0277247af2154b38df
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-babelex = flask-babelex:main
    #
    # Would create an entry point called flask-babelex that calls flask-babelex.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - babel >=1.0
    - speaklater >=1.2
    - jinja2 >=2.5

  run:
    - python
    - flask
    - babel >=1.0
    - speaklater >=1.2
    - jinja2 >=2.5

test:
  # Python imports
  imports:
    - flask_babelex

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/mrjoes/flask-babelex
  license: BSD License
  summary: 'Adds i18n/l10n support to Flask applications'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
