package:
  name: cs.migration.folder2donedukia
  version: "1.8"

source:
  fn: cs.migration.folder2donedukia-1.8.zip
  url: https://pypi.python.org/packages/source/c/cs.migration.folder2donedukia/cs.migration.folder2donedukia-1.8.zip
  md5: 441bd0a1df343927b8b0b68d57d6210e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cs.migration.folder2donedukia = cs.migration.folder2donedukia:main
    #
    # Would create an entry point called cs.migration.folder2donedukia that calls cs.migration.folder2donedukia.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - cs.donedukia

  run:
    - python
    - setuptools
    - setuptools
    - cs.donedukia

test:
  # Python imports
  imports:
    - cs
    - cs.migration
    - cs.migration.folder2donedukia
    - cs.migration.folder2donedukia.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/plone/plone.app.example
  license: GPL
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
