package:
  name: django-tag-tools
  version: "0.2.0-alpha"

source:
  fn: django-tag-tools-0.2.0-alpha.tar.gz
  url: https://pypi.python.org/packages/source/d/django-tag-tools/django-tag-tools-0.2.0-alpha.tar.gz
  md5: 972d2075a9a88ba5c16b00192a4e9b11
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-tag-tools = django-tag-tools:main
    #
    # Would create an entry point called django-tag-tools that calls django-tag-tools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - tagtools

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kaleissin/django-tag-tools
  license: MIT License
  summary: 'Tag cloud for django tagging and django-taggit.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
