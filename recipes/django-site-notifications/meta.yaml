package:
  name: django-site-notifications
  version: "1.0.0"

source:
  fn: django-site-notifications-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-site-notifications/django-site-notifications-1.0.0.tar.gz
  md5: 6505ae36a33f08c6d1b81f3dce1f25cb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-site-notifications = django-site-notifications:main
    #
    # Would create an entry point called django-site-notifications that calls django-site-notifications.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - site_notifications
    - site_notifications.migrations
    - site_notifications.south_migrations
    - site_notifications.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/dstegelman/django-site-notifications
  license: MIT License
  summary: 'Enable a message on every page load.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
