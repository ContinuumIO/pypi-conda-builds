package:
  name: meteor-ejson
  version: "1.0.0"

source:
  fn: meteor-ejson-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/m/meteor-ejson/meteor-ejson-1.0.0.tar.gz
  md5: 90952e08f858e936ea6703e503c6f01c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - meteor-ejson = meteor-ejson:main
    #
    # Would create an entry point called meteor-ejson that calls meteor-ejson.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six

  run:
    - python
    - six

test:
  # Python imports
  imports:
    - ejson

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: MIT License
  summary: u'Encoder and Decoder for Extended JSON (EJSON) as used in Meteor and DDP.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
