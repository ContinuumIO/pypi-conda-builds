package:
  name: morfessor
  version: "2.0.2alpha1"

source:
  fn: Morfessor-2.0.2alpha1.tar.gz
  url: https://pypi.python.org/packages/source/M/Morfessor/Morfessor-2.0.2alpha1.tar.gz
  md5: c0400410ae1fd4d664cbc37ffb29f70c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - morfessor = morfessor:main
    #
    # Would create an entry point called morfessor that calls morfessor.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - morfessor
    - morfessor.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.cis.hut.fi/projects/morpho/
  license: BSD License
  summary: 'Morfessor'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
