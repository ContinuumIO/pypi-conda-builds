package:
  name: mopidy-mobile
  version: "0.9.3"

source:
  fn: Mopidy-Mobile-0.9.3.tar.gz
  url: https://pypi.python.org/packages/source/M/Mopidy-Mobile/Mopidy-Mobile-0.9.3.tar.gz
  md5: 9eac4658fee3b57fbc1821773a6ec45e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mopidy-mobile = mopidy-mobile:main
    #
    # Would create an entry point called mopidy-mobile that calls mopidy-mobile.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - mopidy >=0.19

  run:
    - python
    - setuptools
    - setuptools
    - mopidy >=0.19

test:
  # Python imports
  imports:
    - mopidy_mobile

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tkem/mopidy-mobile
  license: Apache Software License
  summary: u'Mopidy Web client extension for mobile devices'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
