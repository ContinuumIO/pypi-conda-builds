package:
  name: hangulize
  version: "0.0.7"

source:
  fn: hangulize-0.0.7.tar.gz
  url: https://pypi.python.org/packages/source/h/hangulize/hangulize-0.0.7.tar.gz
  md5: 0cc40b1e0e3416da6c0ee98c55025e38
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hangulize = hangulize:main
    #
    # Would create an entry point called hangulize that calls hangulize.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - cmds
    - hangulize
    - hangulize.langs
    - hangulize.langs.aze
    - hangulize.langs.bel
    - hangulize.langs.bul
    - hangulize.langs.cat
    - hangulize.langs.ces
    - hangulize.langs.cym
    - hangulize.langs.deu
    - hangulize.langs.ell
    - hangulize.langs.epo
    - hangulize.langs.est
    - hangulize.langs.fin
    - hangulize.langs.grc
    - hangulize.langs.hbs
    - hangulize.langs.hun
    - hangulize.langs.isl
    - hangulize.langs.ita
    - hangulize.langs.jpn
    - hangulize.langs.kat
    - hangulize.langs.lat
    - hangulize.langs.lav
    - hangulize.langs.lit
    - hangulize.langs.mkd
    - hangulize.langs.nld
    - hangulize.langs.pol
    - hangulize.langs.por
    - hangulize.langs.ron
    - hangulize.langs.rus
    - hangulize.langs.slk
    - hangulize.langs.slv
    - hangulize.langs.spa
    - hangulize.langs.sqi
    - hangulize.langs.swe
    - hangulize.langs.tes
    - hangulize.langs.tur
    - hangulize.langs.ukr
    - hangulize.langs.vie
    - hangulize.langs.wlm
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://hangulize.org/
  license: BSD License
  summary: 'Korean Alphabet Transcription'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
