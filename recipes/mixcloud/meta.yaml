package:
  name: mixcloud
  version: "0.0.3"

source:
  fn: mixcloud-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/m/mixcloud/mixcloud-0.0.3.tar.gz
  md5: 917ff82fc254616f493e9f473aec81d0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mixcloud = mixcloud:main
    #
    # Would create an entry point called mixcloud that calls mixcloud.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python-dateutil
    - requests
    - unidecode
    - pyyaml

  run:
    - python
    - python-dateutil
    - requests
    - unidecode
    - pyyaml

test:
  # Python imports
  imports:
    - mixcloud

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/emillon/mixcloud
  license: BSD License
  summary: 'Bindings for the mixcloud.com API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
