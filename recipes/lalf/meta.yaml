package:
  name: lalf
  version: "3.0a0"

source:
  fn: lalf-3.0a0.tar.gz
  url: https://pypi.python.org/packages/source/l/lalf/lalf-3.0a0.tar.gz
  md5: bb588fa75417a0d54feba6c0d428ae0e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lalf = lalf:main
    #
    # Would create an entry point called lalf that calls lalf.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - pyquery
    - requests
    - pypng

  run:
    - python
    - pyquery
    - requests
    - pypng

test:
  # Python imports
  imports:
    - lalf

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Roromis/Lalf-Forumactif
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'Forumactif to phpbb converter'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
