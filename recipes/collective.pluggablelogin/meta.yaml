package:
  name: collective.pluggablelogin
  version: "1.2"

source:
  fn: collective.pluggablelogin-1.2.zip
  url: https://pypi.python.org/packages/source/c/collective.pluggablelogin/collective.pluggablelogin-1.2.zip
  md5: 63f42be1ec2c0bf95ac01aa4f781ca09
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.pluggablelogin = collective.pluggablelogin:main
    #
    # Would create an entry point called collective.pluggablelogin that calls collective.pluggablelogin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.cmfplone

  run:
    - python
    - setuptools
    - setuptools
    - products.cmfplone

test:
  # Python imports
  imports:
    - collective
    - collective.pluggablelogin
    - collective.pluggablelogin.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/collective/collective.pluggablelogin
  license: GPL
  summary: 'Turns the login form into a portlet manager for easier customization'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
