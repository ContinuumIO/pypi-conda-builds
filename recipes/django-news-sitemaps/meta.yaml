package:
  name: django-news-sitemaps
  version: "0.1.5"

source:
  fn: django-news-sitemaps-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-news-sitemaps/django-news-sitemaps-0.1.5.tar.gz
  md5: 9c1df52ab23eb0b01d4899db14e7762a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-news-sitemaps = django-news-sitemaps:main
    #
    # Would create an entry point called django-news-sitemaps that calls django-news-sitemaps.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - news_sitemaps

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/washingtontimes/django-news-sitemaps/
  license: Apache Software License
  summary: 'Generates sitemaps compatible with the Google News schema'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
