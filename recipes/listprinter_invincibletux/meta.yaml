package:
  name: listprinter_invincibletux
  version: "1.3.0"

source:
  fn: ListPrinter_InvincibleTux-1.3.0.tar.gz
  url: https://pypi.python.org/packages/source/L/ListPrinter_InvincibleTux/ListPrinter_InvincibleTux-1.3.0.tar.gz
  md5: fee05ea4c99b41ac364d1052c2e7faa5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - listprinter_invincibletux = listprinter_invincibletux:main
    #
    # Would create an entry point called listprinter_invincibletux that calls listprinter_invincibletux.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - listprinter_invincibletux

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://invincibletux.blogspot.com/
  license: UNKNOWN
  summary: 'Simple List printer'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
