package:
  name: carafe
  version: "0.4.7"

source:
  fn: carafe-0.4.7.tar.gz
  url: https://pypi.python.org/packages/source/c/carafe/carafe-0.4.7.tar.gz
  md5: 5d3a79687775ad30e1df4d319f945d3a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - carafe = carafe:main
    #
    # Would create an entry point called carafe that calls carafe.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.10.1
    - flask-cache >=0.12
    - flask-principal >=0.4.0
    - flask-testing >=0.4
    - blinker >=1.3

  run:
    - python
    - flask >=0.10.1
    - flask-cache >=0.12
    - flask-principal >=0.4.0
    - flask-testing >=0.4
    - blinker >=1.3

test:
  # Python imports
  imports:
    - carafe
    - carafe.ext

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dgilland/carafe
  license: MIT License
  summary: 'Collection of Flask extensions geared towards JSON APIs'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
