package:
  name: collective.facebook.accounts
  version: "1.0b4"

source:
  fn: collective.facebook.accounts-1.0b4.zip
  url: https://pypi.python.org/packages/source/c/collective.facebook.accounts/collective.facebook.accounts-1.0b4.zip
  md5: 01f0d547cf57750056f7c402464aae18
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.facebook.accounts = collective.facebook.accounts:main
    #
    # Would create an entry point called collective.facebook.accounts that calls collective.facebook.accounts.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.facebook
    - collective.facebook.accounts
    - collective.facebook.accounts.Extensions
    - collective.facebook.accounts.browser
    - collective.facebook.accounts.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.facebook.accounts
  license: GNU General Public License (GPL)
  summary: 'This product allows you to associate Facebook accounts with a Plone site using OAuth authentication.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
