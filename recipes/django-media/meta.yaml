package:
  name: django-media
  version: "1.0.1"

source:
  fn: django-media-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-media/django-media-1.0.1.tar.gz
  md5: 4c72c4e0dbb4e89f4775287b7268f3f6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-media = django-media:main
    #
    # Would create an entry point called django-media that calls django-media.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pil

  run:
    - python
    - pil

test:
  # Python imports
  imports:
    - media
    - media.urls

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/pigmonkey/django-media
  license: BSD License
  summary: 'A basic Django application to store and manage media.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
