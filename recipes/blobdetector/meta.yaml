package:
  name: blobdetector
  version: "1.0.0"

source:
  fn: BlobDetector-1.0.0.tar.gz
  url: https://github.com/downloads/migurski/Blobdetector/BlobDetector-1.0.0.tar.gz
  #md5: 
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - blobdetector = blobdetector:main
    #
    # Would create an entry point called blobdetector that calls blobdetector.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - BlobDetector
    - blobdetector

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/migurski/Blobdetector
  license: BSD
  summary: 'A small package that detects and categorizes blobs in images.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
