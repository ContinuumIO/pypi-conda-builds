package:
  name: flydraanalysistools
  version: "0.0.1"

source:
  fn: FlydraAnalysisTools-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/F/FlydraAnalysisTools/FlydraAnalysisTools-0.0.1.tar.gz
  md5: d53c463cf89308a4f6ac82b5fd73747b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flydraanalysistools = flydraanalysistools:main
    #
    # Would create an entry point called flydraanalysistools that calls flydraanalysistools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - flydra_analysis_tools
    - flydraanalysistools

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/FlydraAnalysisTools/
  license: LICENSE.txt
  summary: 'Analysis scripts for analyzing and plotting flydra data'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
