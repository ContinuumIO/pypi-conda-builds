package:
  name: chordbook
  version: "0.0.12"

source:
  fn: ChordBook-0.0.12.tar.gz
  url: https://pypi.python.org/packages/source/C/ChordBook/ChordBook-0.0.12.tar.gz
  md5: 2ef6c0f1f666a8ea6ad972938e3d2ba7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - chordbook = chordbook:main
    #
    # Would create an entry point called chordbook that calls chordbook.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - chordbook
    - chordbook.examples
    - chordbook.output
    - chordbook.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/wgmyers/chordbook
  license: COPYING
  summary: 'A tool to manage books of chords for bands.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
