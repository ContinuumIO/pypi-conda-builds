package:
  name: metlog-raven
  version: "0.5"

source:
  fn: metlog-raven-0.5.tar.gz
  url: https://pypi.python.org/packages/source/m/metlog-raven/metlog-raven-0.5.tar.gz
  md5: 2baa7e38e895594a8f8aaf7e9b737e8f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - metlog-raven = metlog-raven:main
    #
    # Would create an entry point called metlog-raven that calls metlog-raven.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - metlog-py >=0.9.4
    - raven ==3.1.16

  run:
    - python
    - metlog-py >=0.9.4
    - raven ==3.1.16

test:
  # Python imports
  imports:
    - metlog_raven

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/mozilla-services/metlog-raven
  license: Mozilla Public License 2.0 (MPL 2.0)
  summary: 'A Metlog plugin to send exceptions to Sentry'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
