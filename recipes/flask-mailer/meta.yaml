package:
  name: flask-mailer
  version: "0.3.7"

source:
  fn: Flask-Mailer-0.3.7.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Mailer/Flask-Mailer-0.3.7.tar.gz
  md5: e9d3cf7447767a8616bb864dffbddbf3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-mailer = flask-mailer:main
    #
    # Would create an entry point called flask-mailer that calls flask-mailer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask

  run:
    - python
    - flask

test:
  # Python imports
  imports:
    - flaskext
    - flaskext.mailer
    - flaskext.mailer.backends

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/vitalk/flask-mailer
  license: BSD License
  summary: 'A Flask extension for sending emails with pluggable backends.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
