package:
  name: hurricane-web
  version: "0.1a1"

source:
  fn: Hurricane-web-0.1a1.tar.gz
  url: https://pypi.python.org/packages/source/H/Hurricane-web/Hurricane-web-0.1a1.tar.gz
  md5: ecf5c783db52dad1c595d50ca6921845
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hurricane-web = hurricane-web:main
    #
    # Would create an entry point called hurricane-web that calls hurricane-web.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - tornado ==4.1

  run:
    - python
    - setuptools
    - tornado ==4.1

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/majia-tech/Hurricane
  license: MIT License
  summary: 'a simple web framework based on Tornado.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
