package:
  name: iterpipes
  version: "0.4"

source:
  fn: iterpipes-0.4.tar.gz
  url: https://pypi.python.org/packages/source/i/iterpipes/iterpipes-0.4.tar.gz
  md5: 49273f11b8888d565eed58b6f2702a06
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - iterpipes = iterpipes:main
    #
    # Would create an entry point called iterpipes that calls iterpipes.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - iterpipes

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/vlasovskikh/iterpipes/
  license: MIT License
  summary: 'A library for running shell pipelines using shell-like syntax'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
