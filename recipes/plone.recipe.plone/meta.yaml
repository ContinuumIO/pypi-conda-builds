package:
  name: plone.recipe.plone
  version: "3.1.7"

source:
  fn: plone.recipe.plone-3.1.7.tar.gz
  url: https://pypi.python.org/packages/source/p/plone.recipe.plone/plone.recipe.plone-3.1.7.tar.gz
  md5: 30a95af00e8b11244a5421c752bab121
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.recipe.plone = plone.recipe.plone:main
    #
    # Would create an entry point called plone.recipe.plone that calls plone.recipe.plone.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zc.buildout
    - setuptools
    - zc.recipe.egg
    - plone.recipe.distros

  run:
    - python
    - setuptools
    - zc.buildout
    - setuptools
    - zc.recipe.egg
    - plone.recipe.distros

test:
  # Python imports
  imports:
    - dist_plone
    - dist_plone.platforms
    - plone
    - plone.recipe
    - plone.recipe.plone

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/plone/dist/plone.recipe.plone
  license: Zope Public License
  summary: 'Install Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
