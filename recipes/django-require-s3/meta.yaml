package:
  name: django-require-s3
  version: "1.0.0"

source:
  fn: django-require-s3-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-require-s3/django-require-s3-1.0.0.tar.gz
  md5: 2064ca338f6b1c520ce6a54f40bee98b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-require-s3 = django-require-s3:main
    #
    # Would create an entry point called django-require-s3 that calls django-require-s3.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-require
    - django-storages
    - boto

  run:
    - python
    - django-require
    - django-storages
    - boto

test:
  # Python imports
  imports:
    - require_s3

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/etianen/django-require-s3
  license: BSD License
  summary: 'An integration between django-require and the S3 storage backend from django-storages.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
