package:
  name: cs.htmlmailer
  version: "1.0.1"

source:
  fn: cs.htmlmailer-1.0.1.zip
  url: https://pypi.python.org/packages/source/c/cs.htmlmailer/cs.htmlmailer-1.0.1.zip
  md5: 09e4a4ac07d2e9e8e9ead82a6cce0955
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cs.htmlmailer = cs.htmlmailer:main
    #
    # Would create an entry point called cs.htmlmailer that calls cs.htmlmailer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - cs
    - cs.htmlmailer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.codesyntax.com/private/cs.htmlmailer/
  license: GPL
  summary: 'A library to send emails with HTML and Text mixed content'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
