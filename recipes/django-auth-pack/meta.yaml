package:
  name: django-auth-pack
  version: "0.1.13"

source:
  fn: django-auth-pack-0.1.13.tar.gz
  url: https://pypi.python.org/packages/source/d/django-auth-pack/django-auth-pack-0.1.13.tar.gz
  md5: bc14f918debd2cd29362489b6d08ee54
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-auth-pack = django-auth-pack:main
    #
    # Would create an entry point called django-auth-pack that calls django-auth-pack.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5
    - django-crispy-forms >=1.3.2
    - django-jsonview >=0.2.1

  run:
    - python
    - django >=1.5
    - django-crispy-forms >=1.3.2
    - django-jsonview >=0.2.1

test:
  # Python imports
  imports:
    - auth_pack

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/DigitalDemiurge/django-auth-pack
  license: MIT license
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
