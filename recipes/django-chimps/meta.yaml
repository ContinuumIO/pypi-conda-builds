package:
  name: django-chimps
  version: "0.0.5"

source:
  fn: django-chimps-0.0.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-chimps/django-chimps-0.0.5.tar.gz
  md5: 4a3637fd23c24e400053ca38265e29c7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-chimps = django-chimps:main
    #
    # Would create an entry point called django-chimps that calls django-chimps.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - mailsnake

  run:
    - python
    - mailsnake

test:
  # Python imports
  imports:
    - chimps
    - chimps/management
    - chimps/management/commands
    - chimps/runtests
    - chimps/templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/simonluijk/django-chimps
  license: BSD License
  summary: 'Django app to allow users to subscribe to MailChimp lists.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
