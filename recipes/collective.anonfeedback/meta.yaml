package:
  name: collective.anonfeedback
  version: "1.0"

source:
  fn: collective.anonfeedback-1.0.zip
  url: https://pypi.python.org/packages/source/c/collective.anonfeedback/collective.anonfeedback-1.0.zip
  md5: 833b208f8ced2422276875ce7e9a5e30
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.anonfeedback = collective.anonfeedback:main
    #
    # Would create an entry point called collective.anonfeedback that calls collective.anonfeedback.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.cmfcore

  run:
    - python
    - setuptools
    - setuptools
    - products.cmfcore

test:
  # Python imports
  imports:
    - collective
    - collective.anonfeedback
    - collective.anonfeedback.browser
    - collective.anonfeedback.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.anonfeedback
  license: MIT
  summary: 'An anonymous feedback form'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
