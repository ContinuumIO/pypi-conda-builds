package:
  name: django-sync-server
  version: "0.4.2"

source:
  fn: django-sync-server-0.4.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-sync-server/django-sync-server-0.4.2.tar.gz
  md5: 8bc47770cf7410d2e083d4b3590cf8c7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-sync-server = django-sync-server:main
    #
    # Would create an entry point called django-sync-server that calls django-sync-server.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - south

  run:
    - python
    - django
    - south

test:
  # Python imports
  imports:
    - weave
    - weave.migrations
    - weave.views

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/django-sync-server/
  license: GNU General Public License (GPL)
  summary: 'django-sync-server is a Django reusable application witch implements a Firefox weave server.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
