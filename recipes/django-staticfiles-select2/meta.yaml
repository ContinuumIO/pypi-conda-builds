package:
  name: django-staticfiles-select2
  version: "3.4.5"

source:
  fn: django-staticfiles-select2-3.4.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-staticfiles-select2/django-staticfiles-select2-3.4.5.tar.gz
  md5: 0180a00ce39d495f9e564e695fb22f4e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-staticfiles-select2 = django-staticfiles-select2:main
    #
    # Would create an entry point called django-staticfiles-select2 that calls django-staticfiles-select2.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - staticfiles_select2

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/slafs/django-staticfiles-select2/
  license: BSD License
  summary: 'select2 meets Django staticfiles'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
