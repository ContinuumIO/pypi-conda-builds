package:
  name: mopidy-simple-webclient
  version: "0.1"

source:
  fn: Mopidy-Simple-Webclient-0.1.tar.gz
  url: https://pypi.python.org/packages/source/M/Mopidy-Simple-Webclient/Mopidy-Simple-Webclient-0.1.tar.gz
  md5: 74c8e7201904cbb0ec8589cc049ca9f4
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mopidy-simple-webclient = mopidy-simple-webclient:main
    #
    # Would create an entry point called mopidy-simple-webclient that calls mopidy-simple-webclient.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - mopidy >=0.19.4
    - setuptools

  run:
    - python
    - setuptools
    - mopidy >=0.19.4
    - setuptools

test:
  # Python imports
  imports:
    - mopidy_simple_webclient

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/xolox/mopidy-simple-webclient
  license: MIT License
  summary: 'Very simple and mobile friendly web interface for the Mopidy music server'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
