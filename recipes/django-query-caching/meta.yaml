package:
  name: django-query-caching
  version: "0.1.1"

source:
  fn: django-query-caching-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-query-caching/django-query-caching-0.1.1.tar.gz
  md5: e63d246a21c65af044c82a0684f51194
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-query-caching = django-query-caching:main
    #
    # Would create an entry point called django-query-caching that calls django-query-caching.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - django_query_caching

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/SeanHayes/django-query-caching
  license: BSD License
  summary: 'Caches the results of SQL queries transparently.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
