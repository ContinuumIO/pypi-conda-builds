package:
  name: lockserver
  version: "0.36"

source:
  fn: lockserver-0.36.tar.gz
  url: https://pypi.python.org/packages/source/l/lockserver/lockserver-0.36.tar.gz
  md5: 845f21613331bb638c3984c58ce87177
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lockserver = lockserver:main
    #
    # Would create an entry point called lockserver that calls lockserver.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - web.py >=0.37

  run:
    - python
    - web.py >=0.37

test:
  # Python imports
  imports:
    - lockserver

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.grycap.upv.es
  license: MIT
  summary: 'Simple lock server and client api for distributed coordination using a simple centralized python lock server'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
