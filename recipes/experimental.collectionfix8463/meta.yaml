package:
  name: experimental.collectionfix8463
  version: "0.2.0"

source:
  fn: experimental.collectionfix8463-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/e/experimental.collectionfix8463/experimental.collectionfix8463-0.2.0.tar.gz
  md5: d206c495ac5933f63d0e4bb5568e9167
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - experimental.collectionfix8463 = experimental.collectionfix8463:main
    #
    # Would create an entry point called experimental.collectionfix8463 that calls experimental.collectionfix8463.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone <3.3.6dev

  run:
    - python
    - setuptools
    - setuptools
    - plone <3.3.6dev

test:
  # Python imports
  imports:
    - experimental
    - experimental.collectionfix8463

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/experimental.collectionfix8463
  license: GPL
  summary: 'A quick and dirty fix for the #8463 Plone issue with collections. Use only for versions before 3.3.6'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
