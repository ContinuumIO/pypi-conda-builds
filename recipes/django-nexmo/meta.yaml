package:
  name: django-nexmo
  version: "1.0.1"

source:
  fn: django-nexmo-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-nexmo/django-nexmo-1.0.1.tar.gz
  md5: 9f186b0635c9166c00948bd86e64fa86
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-nexmo = django-nexmo:main
    #
    # Would create an entry point called django-nexmo that calls django-nexmo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - nexmo

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/thibault/django-nexmo
  license: WTFPL
  summary: 'A simple Django app to send text messages using the Nexmo api.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
