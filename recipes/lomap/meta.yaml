package:
  name: lomap
  version: "0.1.1.post1"

source:
  fn: lomap-0.1.1.post1.tar.gz
  url: https://pypi.python.org/packages/source/l/lomap/lomap-0.1.1.post1.tar.gz
  md5: 954fb1264890d669938c9737a7f44a06
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lomap = lomap:main
    #
    # Would create an entry point called lomap that calls lomap.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - networkx >=1.6
    - pp >=1.6.2
    - matplotlib >=1.3.1
    - setuptools >=1.1.6

  run:
    - python
    - networkx >=1.6
    - pp >=1.6.2
    - matplotlib >=1.3.1
    - setuptools >=1.1.6

test:
  # Python imports
  imports:
    - lomap
    - lomap.algorithms
    - lomap.classes

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://hyness.bu.edu/lomap
  license: GNU GPL
  summary: 'LTL Optimal Multi-Agent Planner (LOMAP)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
