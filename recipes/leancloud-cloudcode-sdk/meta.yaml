package:
  name: leancloud-cloudcode-sdk
  version: "1.0.0"

source:
  fn: leancloud-cloudcode-sdk-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/l/leancloud-cloudcode-sdk/leancloud-cloudcode-sdk-1.0.0.tar.gz
  md5: 39ce153745bad96431c45fe7d696d821
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - leancloud-cloudcode-sdk = leancloud-cloudcode-sdk:main
    #
    # Would create an entry point called leancloud-cloudcode-sdk that calls leancloud-cloudcode-sdk.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - werkzeug

  run:
    - python
    - werkzeug

test:
  # Python imports
  imports:
    - cloudcode

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://leancloud.cn/
  license: LGPL
  summary: 'LeanCloud Cloudcode Python SDK'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
