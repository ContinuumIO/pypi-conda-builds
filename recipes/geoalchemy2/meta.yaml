package:
  name: geoalchemy2
  version: "0.2.4"

source:
  fn: GeoAlchemy2-0.2.4.tar.gz
  url: https://pypi.python.org/packages/source/G/GeoAlchemy2/GeoAlchemy2-0.2.4.tar.gz
  md5: 2a7203f3ac614bc8758afa8f43a99154
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - geoalchemy2 = geoalchemy2:main
    #
    # Would create an entry point called geoalchemy2 that calls geoalchemy2.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sqlalchemy >=0.8

  run:
    - python
    - setuptools
    - sqlalchemy >=0.8

test:
  # Python imports
  imports:
    - geoalchemy2

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://geoalchemy.org/
  license: MIT License
  summary: 'Using SQLAlchemy with Spatial Databases'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
