package:
  name: django-bootstrap3-validator
  version: "0.3"

source:
  fn: Django-Bootstrap3-Validator-0.3.3.zip
  url: https://pypi.python.org/packages/source/D/Django-Bootstrap3-Validator/Django-Bootstrap3-Validator-0.3.3.zip
  md5: 01cbbcebd0d2b7879e02aedfcd1a9512
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-bootstrap3-validator = django-bootstrap3-validator:main
    #
    # Would create an entry point called django-bootstrap3-validator that calls django-bootstrap3-validator.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - bootstrap_validator
    - bootstrap_validator.migrations
    - bootstrap_validator.templatetags
    - demo
    - demo.demo

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/yuanxu/django-bootstrap3-validator
  license: Apache Software License
  summary: 'BootstrapValidator support for Django projects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
