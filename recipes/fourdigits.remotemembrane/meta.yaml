package:
  name: fourdigits.remotemembrane
  version: "0.6"

source:
  fn: fourdigits.remotemembrane-0.6.zip
  url: https://pypi.python.org/packages/source/f/fourdigits.remotemembrane/fourdigits.remotemembrane-0.6.zip
  md5: f78e300122cd3a33da29d52d8428f20a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fourdigits.remotemembrane = fourdigits.remotemembrane:main
    #
    # Would create an entry point called fourdigits.remotemembrane that calls fourdigits.remotemembrane.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.membrane

  run:
    - python
    - setuptools
    - setuptools
    - products.membrane

test:
  # Python imports
  imports:
    - fourdigits
    - fourdigits.remotemembrane
    - fourdigits.remotemembrane.browser
    - fourdigits.remotemembrane.plugins

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.fourdigits.nl
  license: GPL version 2
  summary: 'A set of PAS plugins so users\n                   are  authenticated from another Plone site'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
