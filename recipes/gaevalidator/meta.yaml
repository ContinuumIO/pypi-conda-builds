package:
  name: gaevalidator
  version: "0.1"

source:
  fn: gaevalidator-0.1.tar.gz
  url: https://pypi.python.org/packages/source/g/gaevalidator/gaevalidator-0.1.tar.gz
  md5: c6adc1faea91abf1b64a8814b64e8663
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gaevalidator = gaevalidator:main
    #
    # Would create an entry point called gaevalidator that calls gaevalidator.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - gaevalidator
    - gaevalidator.ndb

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/renzon/gavalidator
  license: BSD License
  summary: 'A automate validation based on App Engine Models'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
