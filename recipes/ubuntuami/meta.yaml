package:
  name: ubuntuami
  version: "0.1.0"

source:
  fn: UbuntuAMI-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/U/UbuntuAMI/UbuntuAMI-0.1.0.tar.gz
  md5: 15b00e1806ca46349c780c6550b60be3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ubuntuami = ubuntuami:main
    #
    # Would create an entry point called ubuntuami that calls ubuntuami.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - cssselect
    - lxml
    - requests

  run:
    - python
    - cssselect
    - lxml
    - requests

test:
  # Python imports
  imports:
    - ubuntuami

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://lxyu.github.com/ubuntuami/
  license: ubuntudesign-asset-mapper
  summary: 'Get the daily build ami of Ubuntu Cloud Image'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
