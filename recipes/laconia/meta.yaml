package:
  name: laconia
  version: "0.3.0"

source:
  fn: laconia-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/l/laconia/laconia-0.3.0.tar.gz
  md5: f80952c5ad4c3a09c26ca2b5b363c599
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - laconia = laconia:main
    #
    # Would create an entry point called laconia that calls laconia.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - laconia

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/avengerpenguin/laconia
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'Simple API for RDF'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
