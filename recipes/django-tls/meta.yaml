package:
  name: django-tls
  version: "0.0.2"

source:
  fn: django-tls-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-tls/django-tls-0.0.2.tar.gz
  md5: a8fab88beda91913b31ea515bdb25f36
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-tls = django-tls:main
    #
    # Would create an entry point called django-tls that calls django-tls.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - werkzeug >=0.6.2

  run:
    - python
    - werkzeug >=0.6.2

test:
  # Python imports
  imports:
    - tls

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/aino/django-tls
  license: BSD License
  summary: 'Stores the current request in Thread Local Storage using Werkzeug'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
