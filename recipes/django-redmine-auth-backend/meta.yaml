package:
  name: django-redmine-auth-backend
  version: "0.1"

source:
  fn: django-redmine-auth-backend-0.1.tar.gz
  url: https://pypi.python.org/packages/2.7/d/django-redmine-auth-backend/django-redmine-auth-backend-0.1.tar.gz
  #md5: 
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-redmine-auth-backend = django-redmine-auth-backend:main
    #
    # Would create an entry point called django-redmine-auth-backend that calls django-redmine-auth-backend.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - redmine_auth

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/daniel-yavorovich/django-redmine-auth-backend
  license: Apache V2 License
  summary: 'A Django authentication backend for use with the Redmine'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
