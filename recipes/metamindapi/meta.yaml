package:
  name: metamindapi
  version: "1.2"

source:
  fn: MetaMindApi-1.2.tar.gz
  url: https://pypi.python.org/packages/source/M/MetaMindApi/MetaMindApi-1.2.tar.gz
  md5: 7682f04aadf3dabca00f99eea497c84c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - metamindapi = metamindapi:main
    #
    # Would create an entry point called metamindapi that calls metamindapi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests
    - simplejson

  run:
    - python
    - requests
    - simplejson

test:
  # Python imports
  imports:
    - metamind
    - metamind.api
    - metamindapi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://www.metamind.io
  license: UNKNOWN
  summary: 'MetaMind client for text and image classification'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
