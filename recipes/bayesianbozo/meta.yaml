package:
  name: bayesianbozo
  version: "0.1.0"

source:
  fn: BayesianBozo-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/B/BayesianBozo/BayesianBozo-0.1.0.tar.gz
  md5: 1d002f3b3211ceced2c4f9bd32de28b3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bayesianbozo = bayesianbozo:main
    #
    # Would create an entry point called bayesianbozo that calls bayesianbozo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - bayesian_bozo
    - bayesianbozo

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jcbozonier/BayesianBozo
  license: bayesnet
  summary: 'Bayesian statistical functions for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
