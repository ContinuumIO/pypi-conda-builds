package:
  name: django-readonly
  version: "0.1.2"

source:
  fn: django-readonly-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-readonly/django-readonly-0.1.2.tar.gz
  md5: c027518cbe14ee71698f5ebe9cb71667
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-readonly = django-readonly:main
    #
    # Would create an entry point called django-readonly that calls django-readonly.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - readonly

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/zalew/django-readonly
  license: BSD License
  summary: 'Put website in read-only mode for maintenance.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
