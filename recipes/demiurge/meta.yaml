package:
  name: demiurge
  version: "0.2"

source:
  fn: demiurge-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/demiurge/demiurge-0.2.tar.gz
  md5: 2cdbfcd8bf629f2158847c4a4df7351c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - demiurge = demiurge:main
    #
    # Would create an entry point called demiurge that calls demiurge.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyquery >=1.2.8

  run:
    - python
    - pyquery >=1.2.8

test:
  # Python imports
  imports:
    - demiurge

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/matiasb/demiurge
  license: BSD License
  summary: 'Scraping micro-framework based on pyquery.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
