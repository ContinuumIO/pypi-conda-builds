package:
  name: xstatic-jquery.quicksearch
  version: "2.0.3.1"

source:
  fn: XStatic-JQuery.quicksearch-2.0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/X/XStatic-JQuery.quicksearch/XStatic-JQuery.quicksearch-2.0.3.1.tar.gz
  md5: 0dc4bd1882cf35dc7b19a236ba09b89d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - xstatic-jquery.quicksearch = xstatic-jquery.quicksearch:main
    #
    # Would create an entry point called xstatic-jquery.quicksearch that calls xstatic-jquery.quicksearch.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - xstatic
    - xstatic.pkg
    - xstatic.pkg.jquery_quicksearch

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plugins.jquery.com/jquery.quicksearch/
  license: (same as JQuery.quicksearch)
  summary: 'JQuery.quicksearch 2.0.3 (XStatic packaging standard)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
