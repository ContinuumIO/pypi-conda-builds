package:
  name: amber-python-common
  version: "0.2"

source:
  fn: amber-python-common-0.2.tar.gz
  url: https://pypi.python.org/packages/source/a/amber-python-common/amber-python-common-0.2.tar.gz
  md5: 3b5f036fd93d26fe42ae6305ad7142dd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - amber-python-common = amber-python-common:main
    #
    # Would create an entry point called amber-python-common that calls amber-python-common.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - ambercommon
    - ambercommon.common

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://project-capo.github.io/
  license: UNKNOWN
  summary: 'Amber clients in python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
