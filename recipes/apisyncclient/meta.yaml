package:
  name: apisyncclient
  version: "1.0"

source:
  fn: ApiSyncClient-1.0.tar.gz
  url: https://pypi.python.org/packages/source/A/ApiSyncClient/ApiSyncClient-1.0.tar.gz
  md5: db60887a1f792020b80a3dc0be1ff157
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - apisyncclient = apisyncclient:main
    #
    # Would create an entry point called apisyncclient that calls apisyncclient.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - ApiSyncClient
    - apisyncclient

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tsyganov-ivan/ApiDaemon
  license: MIT-license
  summary: 'Client for ApiDaemon-service'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
