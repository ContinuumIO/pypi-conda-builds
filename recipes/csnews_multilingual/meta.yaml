package:
  name: csnews_multilingual
  version: "3.2"

source:
  fn: csnews_multilingual-3.2.zip
  url: https://pypi.python.org/packages/source/c/csnews_multilingual/csnews_multilingual-3.2.zip
  md5: 54566eff3a5804f1b74e2f53137f09f6
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - csnews_multilingual = csnews_multilingual:main
    #
    # Would create an entry point called csnews_multilingual that calls csnews_multilingual.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-photologue
    - django-tinymce
    - django-transmeta

  run:
    - python
    - setuptools
    - django-photologue
    - django-tinymce
    - django-transmeta

test:
  # Python imports
  imports:
    - csnews_multilingual

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.codesyntax.com
  license: GPL
  summary: 'Simple news module'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
