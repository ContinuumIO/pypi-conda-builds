package:
  name: sharedarray
  version: "0.2"

source:
  fn: SharedArray-0.2.tar.gz
  url: https://pypi.python.org/packages/source/S/SharedArray/SharedArray-0.2.tar.gz
  md5: 7c36aca5a78d54898bd14a8842754e98
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sharedarray = sharedarray:main
    #
    # Would create an entry point called sharedarray that calls sharedarray.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - sharedarray

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://parad0x.org/git/python/shared-array/about
  license: GNU General Public License v2 (GPLv2)
  summary: 'Share numpy arrays between processes'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
