package:
  name: salesforce-bulk
  version: "1.0.7"

source:
  fn: salesforce-bulk-1.0.7.tar.gz
  url: https://pypi.python.org/packages/source/s/salesforce-bulk/salesforce-bulk-1.0.7.tar.gz
  md5: 0f78cb8c69b09b5289eb7e29f6507016
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - salesforce-bulk = salesforce-bulk:main
    #
    # Would create an entry point called salesforce-bulk that calls salesforce-bulk.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - httplib2 >=0.7.5
    - requests >=2.2.1

  run:
    - python
    - httplib2 >=0.7.5
    - requests >=2.2.1

test:
  # Python imports
  imports:
    - salesforce_bulk

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/heroku/salesforce-bulk
  license: MIT License
  summary: 'Python interface to the Salesforce.com Bulk API.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
