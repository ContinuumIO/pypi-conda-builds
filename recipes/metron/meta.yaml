package:
  name: metron
  version: "1.3.5"

source:
  fn: metron-1.3.5.tar.gz
  url: https://pypi.python.org/packages/source/m/metron/metron-1.3.5.tar.gz
  md5: 994089f22459c6c46caee88252802a56
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - metron = metron:main
    #
    # Would create an entry point called metron that calls metron.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - metron
    - metron.templatetags
    - metron.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://metron.rtfd.org/
  license: MIT License
  summary: 'analytics and metrics integration for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
