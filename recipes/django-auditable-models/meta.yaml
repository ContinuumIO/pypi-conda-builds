package:
  name: django-auditable-models
  version: "1.0"

source:
  fn: django-auditable-models-1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-auditable-models/django-auditable-models-1.0.tar.gz
  md5: bd8ecc6b059ad106f6e21efebe4cdd5d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-auditable-models = django-auditable-models:main
    #
    # Would create an entry point called django-auditable-models that calls django-auditable-models.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django
    - django-cached-user

  run:
    - python
    - django
    - django-cached-user

test:
  # Python imports
  imports:
    - auditable_models

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/bitzeppelin/django-auditable-models
  license: BSD License
  summary: 'Auditable model classes for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
