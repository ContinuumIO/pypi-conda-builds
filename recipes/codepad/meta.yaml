package:
  name: codepad
  version: "0.1"

source:
  fn: codepad-0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/codepad/codepad-0.1.tar.gz
  md5: 484c91b6ff6b043ab0747f731ff85ed3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - codepad = codepad:main
    #
    # Would create an entry point called codepad that calls codepad.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - codepad

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/daltonmatos/codepad
  license: 3-clause BSD
  summary: 'A Command line interface for codepad.org'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
