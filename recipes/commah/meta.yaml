package:
  name: commah
  version: "0.2.1"

source:
  fn: commah-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/c/commah/commah-0.2.1.tar.gz
  md5: 7f3356c52def850ed2f0a5402cc65736
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - commah = commah:main
    #
    # Would create an entry point called commah that calls commah.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy >=1.9
    - cosmolopy
    - scipy >=0.14

  run:
    - python
    - numpy >=1.9
    - cosmolopy
    - scipy >=0.14

test:
  # Python imports
  imports:
    - commah

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/astroduff/commah
  license: BSD License
  summary: 'Get conc - mass or accretion history for any cosmology'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
