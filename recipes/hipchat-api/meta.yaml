package:
  name: hipchat-api
  version: "0.0.2"

source:
  fn: hipchat-api-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/h/hipchat-api/hipchat-api-0.0.2.tar.gz
  md5: 8ddfbc5570de5cc8b0c60a0a6ade2e4d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hipchat-api = hipchat-api:main
    #
    # Would create an entry point called hipchat-api that calls hipchat-api.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - hipchat

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://goodcode.io/
  license: MIT License
  summary: 'A Pythonic wrapper for the HipChat API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
