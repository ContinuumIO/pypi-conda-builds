package:
  name: flask-canvas
  version: "0.1"

source:
  fn: flask-canvas-0.1.tar.gz
  url: https://pypi.python.org/packages/source/f/flask-canvas/flask-canvas-0.1.tar.gz
  md5: c69517b4900bd2498bf21da9e355ffb3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-canvas = flask-canvas:main
    #
    # Would create an entry point called flask-canvas that calls flask-canvas.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask

  run:
    - python
    - flask

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/demianbrecht/flask-canvas
  license: MIT License
  summary: 'A Flask extension for Facebook canvas-based apps'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
