package:
  name: gocept.sequence
  version: "0.31"

source:
  fn: gocept.sequence-0.31.tar.gz
  url: https://pypi.python.org/packages/source/g/gocept.sequence/gocept.sequence-0.31.tar.gz
  md5: efc55e1f5e092e91b89b3cff0492b9c9
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gocept.sequence = gocept.sequence:main
    #
    # Would create an entry point called gocept.sequence that calls gocept.sequence.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - gocept
    - gocept.sequence

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/gocept.sequence/
  license: Zope Public License
  summary: 'Generates a sequence'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
