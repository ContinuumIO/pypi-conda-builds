package:
  name: iron-core
  version: "1.1.6"

source:
  fn: iron-core-1.1.6.tar.gz
  url: https://pypi.python.org/packages/source/i/iron-core/iron-core-1.1.6.tar.gz
  md5: 2c5dfdee49ad2ffee95ae90cdabc5a8c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - iron-core = iron-core:main
    #
    # Would create an entry point called iron-core that calls iron-core.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=1.1.0
    - python-dateutil

  run:
    - python
    - requests >=1.1.0
    - python-dateutil

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://www.github.com/iron-io/iron_core_python
  license: BSD License
  summary: 'Universal classes and methods for Iron.io API wrappers to build on.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
