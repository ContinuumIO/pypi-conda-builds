package:
  name: zc.relation
  version: "1.0"

source:
  fn: zc.relation-1.0.tar.gz
  url: https://pypi.python.org/packages/source/z/zc.relation/zc.relation-1.0.tar.gz
  md5: 7e479095954fc6d8f648951434695837
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zc.relation = zc.relation:main
    #
    # Would create an entry point called zc.relation that calls zc.relation.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - zodb3 >=3.8dev
    - zope.interface
    - setuptools
    - zope.testing

  run:
    - python
    - zodb3 >=3.8dev
    - zope.interface
    - setuptools
    - zope.testing

test:
  # Python imports
  imports:
    - zc
    - zc.relation

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: ZPL 2.1
  summary: 'Index intransitive and transitive n-ary relationships.\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
