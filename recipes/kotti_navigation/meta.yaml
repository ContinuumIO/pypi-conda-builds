package:
  name: kotti_navigation
  version: "0.3.1"

source:
  fn: kotti_navigation-0.3.1.zip
  url: https://pypi.python.org/packages/source/k/kotti_navigation/kotti_navigation-0.3.1.zip
  md5: eba8c48de7bbe73af9a0355738f9a756
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kotti_navigation = kotti_navigation:main
    #
    # Would create an entry point called kotti_navigation that calls kotti_navigation.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - kotti >=0.9.2

  run:
    - python
    - setuptools
    - kotti >=0.9.2

test:
  # Python imports
  imports:
    - kotti_navigation
    - kotti_navigation.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/kotti_navigation
  license: BSD-derived (http://www.repoze.org/LICENSE.txt)
  summary: 'Add a configurable navigation to your Kotti site'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
