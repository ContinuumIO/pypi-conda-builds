package:
  name: cstyle
  version: "0.1.0"

source:
  fn: cstyle-0.1.0.zip
  url: https://pypi.python.org/packages/source/c/cstyle/cstyle-0.1.0.zip
  md5: b9c2445cf57ae79e83142c2fe6486c79
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cstyle = cstyle:main
    #
    # Would create an entry point called cstyle that calls cstyle.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - cstyle

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/pooya-eghbali/cstyle
  license: GNU Lesser General Public License v3 (LGPLv3)
  summary: 'Use c-style braces instead of indentation.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
