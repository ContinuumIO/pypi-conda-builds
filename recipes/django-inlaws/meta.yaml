package:
  name: django-inlaws
  version: "0.1.1beta1"

source:
  fn: django-inlaws-0.1.1beta1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-inlaws/django-inlaws-0.1.1beta1.tar.gz
  md5: ae0890976c93f6e28f1a810f4cf114d6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-inlaws = django-inlaws:main
    #
    # Would create an entry point called django-inlaws that calls django-inlaws.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - example
    - example.blog
    - inlaws
    - inlaws.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/washingtontimes/django-inlaws
  license: UNKNOWN
  summary: 'Shows related objects in the Django admin.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
