package:
  name: django-plainpasswordhasher
  version: "0.3"

source:
  fn: django-plainpasswordhasher-0.3.zip
  url: https://pypi.python.org/packages/source/d/django-plainpasswordhasher/django-plainpasswordhasher-0.3.zip
  md5: 9b359a7912906bd4928fdfbe11ef210d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-plainpasswordhasher = django-plainpasswordhasher:main
    #
    # Would create an entry point called django-plainpasswordhasher that calls django-plainpasswordhasher.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django
    - setuptools

  run:
    - python
    - django
    - setuptools

test:
  # Python imports
  imports:
    - django_plainpasswordhasher

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/novapost/django-plainpasswordhasher
  license: BSD License
  summary: u'Dummy (plain text) password hashing for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
