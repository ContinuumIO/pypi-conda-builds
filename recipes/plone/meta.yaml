package:
  name: plone
  version: "5.0b1"

source:
  fn: Plone-5.0b1.zip
  url: https://pypi.python.org/packages/source/P/Plone/Plone-5.0b1.zip
  md5: 15064dea7ca453268b44a8a80901dcd5
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone = plone:main
    #
    # Would create an entry point called plone that calls plone.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - products.archetypes
    - products.atcontenttypes >=2.1.3
    - products.cmfplacefulworkflow
    - products.cmfplone
    - plone.app.caching
    - plone.app.dexterity
    - plone.app.iterate
    - plone.app.openid
    - plone.app.upgrade

  run:
    - python
    - setuptools
    - products.archetypes
    - products.atcontenttypes >=2.1.3
    - products.cmfplacefulworkflow
    - products.cmfplone
    - plone.app.caching
    - plone.app.dexterity
    - plone.app.iterate
    - plone.app.openid
    - plone.app.upgrade

test:
  # Python imports
  imports:
    - plone

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/
  license: GNU General Public License (GPL)
  summary: 'The Plone Content Management System'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
