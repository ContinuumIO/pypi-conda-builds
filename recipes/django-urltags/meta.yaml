package:
  name: django-urltags
  version: "0.1"

source:
  fn: django-urltags-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-urltags/django-urltags-0.1.tar.gz
  md5: 6fcef74b15d8d37501260d4f45632181
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-urltags = django-urltags:main
    #
    # Would create an entry point called django-urltags that calls django-urltags.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - urltags
    - urltags.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://callowayproject.com/
  license: Apache Software License
  summary: 'django-urltags contains several template tags and filters for messing with URLs'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
