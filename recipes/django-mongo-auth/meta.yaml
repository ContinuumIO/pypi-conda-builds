package:
  name: django-mongo-auth
  version: "0.1.2"

source:
  fn: django-mongo-auth-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-mongo-auth/django-mongo-auth-0.1.2.tar.gz
  md5: a17adb2e2d99d7095e51dd0b73e22b96
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-mongo-auth = django-mongo-auth:main
    #
    # Would create an entry point called django-mongo-auth that calls django-mongo-auth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3
    - mongoengine >=0.6.11
    - tweepy >=1.9
    - django-browserid >=0.6
    - django-missing >=0.1.10
    - django-sekizai >=0.5,

  run:
    - python
    - django >=1.3
    - mongoengine >=0.6.11
    - tweepy >=1.9
    - django-browserid >=0.6
    - django-missing >=0.1.10
    - django-sekizai >=0.5,

test:
  # Python imports
  imports:
    - mongo_auth
    - mongo_auth.contrib
    - mongo_auth.contrib.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mitar/django-mongo-auth
  license: GNU Affero General Public License v3
  summary: 'Django authentication based on an extensible MongoEngine user class.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
