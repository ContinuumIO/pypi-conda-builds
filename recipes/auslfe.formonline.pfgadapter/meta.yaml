package:
  name: auslfe.formonline.pfgadapter
  version: "0.7.2"

source:
  fn: auslfe.formonline.pfgadapter-0.7.2.zip
  url: https://pypi.python.org/packages/source/a/auslfe.formonline.pfgadapter/auslfe.formonline.pfgadapter-0.7.2.zip
  md5: 1a7d9b02789203cdba5e1507287aae91
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - auslfe.formonline.pfgadapter = auslfe.formonline.pfgadapter:main
    #
    # Would create an entry point called auslfe.formonline.pfgadapter that calls auslfe.formonline.pfgadapter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.ploneformgen
    - auslfe.formonline.content >=0.8.0

  run:
    - python
    - setuptools
    - setuptools
    - products.ploneformgen
    - auslfe.formonline.content >=0.8.0

test:
  # Python imports
  imports:
    - auslfe
    - auslfe.formonline
    - auslfe.formonline.pfgadapter
    - auslfe.formonline.pfgadapter.browser
    - auslfe.formonline.pfgadapter.content
    - auslfe.formonline.pfgadapter.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/auslfe.formonline.pfgadapter
  license: GNU General Public License (GPL)
  summary: 'An adapter for PloneFormGen that add to Plone a way for manage online forms submission'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
