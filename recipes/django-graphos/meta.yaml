package:
  name: django-graphos
  version: "0.0.2a0"

source:
  fn: django-graphos-0.0.2a0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-graphos/django-graphos-0.0.2a0.tar.gz
  md5: 0ef2a4d9882972148df66b2332c1a6f1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-graphos = django-graphos:main
    #
    # Would create an entry point called django-graphos that calls django-graphos.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - graphos
    - graphos.renderers
    - graphos.sources

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/agiliq/django-graphos
  license: BSD License
  summary: 'Django app to provide a JS agnostic way to work with charts.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
