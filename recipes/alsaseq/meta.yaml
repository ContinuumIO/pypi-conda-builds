package:
  name: alsaseq
  version: "0.4.1"

source:
  fn: alsaseq-0.4.1.tar.gz
  url: https://pypi.python.org/packages/source/a/alsaseq/alsaseq-0.4.1.tar.gz
  md5: f72527750ed11ebe7f5af74ba08ec4c2
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - alsaseq = alsaseq:main
    #
    # Would create an entry point called alsaseq that calls alsaseq.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - alsaseq

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pp.com.mx/python/alsaseq/
  license: GNU GPL v2 or later
  summary: 'ALSA sequencer bindings for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
