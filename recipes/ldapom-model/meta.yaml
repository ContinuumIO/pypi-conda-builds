package:
  name: ldapom-model
  version: "0.1.0"

source:
  fn: ldapom-model-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/l/ldapom-model/ldapom-model-0.1.0.tar.gz
  md5: e1662c40e877ab0026b56412812469f3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ldapom-model = ldapom-model:main
    #
    # Would create an entry point called ldapom-model that calls ldapom-model.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - ldapom ==0.12.0

  run:
    - python
    - ldapom ==0.12.0

test:
  # Python imports
  imports:
    - ldapom_model
    - test_server

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/maethor/ldapom-model
  license: WTFPL
  summary: 'Base class to manage models with ldapom.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
