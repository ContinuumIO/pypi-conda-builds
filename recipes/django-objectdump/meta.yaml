package:
  name: django-objectdump
  version: "0.4.2"

source:
  fn: django-objectdump-0.4.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-objectdump/django-objectdump-0.4.2.tar.gz
  md5: 6190e8c4662eb25653a7026633411550
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-objectdump = django-objectdump:main
    #
    # Would create an entry point called django-objectdump that calls django-objectdump.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - example
    - example.simpleapp
    - objectdump
    - objectdump.management
    - objectdump.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/callowayproject/django-objectdump
  license: UNKNOWN
  summary: 'Export one or more objects and their related objects. Allows for fine-tuned control over which related objects are exported.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
