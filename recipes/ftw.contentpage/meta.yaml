package:
  name: ftw.contentpage
  version: "1.11.3"

source:
  fn: ftw.contentpage-1.11.3.zip
  url: https://pypi.python.org/packages/source/f/ftw.contentpage/ftw.contentpage-1.11.3.zip
  md5: 61b68aa7dcf6d7ce299740feee7e4860
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ftw.contentpage = ftw.contentpage:main
    #
    # Would create an entry point called ftw.contentpage that calls ftw.contentpage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - archetypes.schemaextender
    - collective.quickupload
    - ftw.calendarwidget
    - ftw.colorbox
    - ftw.geo
    - ftw.profilehook
    - ftw.table
    - ftw.upgrade
    - plone.api
    - plone.formwidget.contenttree
    - plone.formwidget.recaptcha
    - setuptools
    - simplelayout.base >=4.0.2
    - simplelayout.ui.base >=3.0.3
    - simplelayout.portlet.dropzone
    - plone.dexterity
    - plone.directives.form

  run:
    - python
    - setuptools
    - archetypes.schemaextender
    - collective.quickupload
    - ftw.calendarwidget
    - ftw.colorbox
    - ftw.geo
    - ftw.profilehook
    - ftw.table
    - ftw.upgrade
    - plone.api
    - plone.formwidget.contenttree
    - plone.formwidget.recaptcha
    - setuptools
    - simplelayout.base >=4.0.2
    - simplelayout.ui.base >=3.0.3
    - simplelayout.portlet.dropzone
    - plone.dexterity
    - plone.directives.form

test:
  # Python imports
  imports:
    - ftw
    - ftw.contentpage
    - ftw.contentpage.Extensions
    - ftw.contentpage.behaviors
    - ftw.contentpage.browser
    - ftw.contentpage.content
    - ftw.contentpage.handlers
    - ftw.contentpage.portlets
    - ftw.contentpage.tests
    - ftw.contentpage.upgrades
    - ftw.contentpage.viewlets

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/4teamwork/ftw.contentpage
  license: GPL2
  summary: 'Contentpage based on Simplelayout for for web/intranet'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
