package:
  name: django-countries
  version: "3.3"

source:
  fn: django-countries-3.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-countries/django-countries-3.3.tar.gz
  md5: a981d1ae636ef8b5f05a0c73f7a7806e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-countries = django-countries:main
    #
    # Would create an entry point called django-countries that calls django-countries.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_countries
    - django_countries.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/SmileyChris/django-countries/
  license: MIT License
  summary: 'Provides a country field for Django models.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
