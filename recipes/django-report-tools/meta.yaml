package:
  name: django-report-tools
  version: "0.2.2"

source:
  fn: django-report-tools-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-report-tools/django-report-tools-0.2.2.tar.gz
  md5: 1de17b79e4089a5a46088d416d4e7ae6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-report-tools = django-report-tools:main
    #
    # Would create an entry point called django-report-tools that calls django-report-tools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - report_tools
    - report_tools.renderers
    - report_tools.renderers.googlecharts
    - report_tools.renderers.googlecharts.gviz_api
    - report_tools.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/evanbrumley/django-report-tools
  license: BSD License
  summary: 'Class-based reports for elegant data views.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
