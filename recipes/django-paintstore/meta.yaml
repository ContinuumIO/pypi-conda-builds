package:
  name: django-paintstore
  version: "0.2"

source:
  fn: django-paintstore-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-paintstore/django-paintstore-0.2.tar.gz
  md5: 759458c80d319d1412af759c1a200cfb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-paintstore = django-paintstore:main
    #
    # Would create an entry point called django-paintstore that calls django-paintstore.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - paintstore

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/gsiegman/django-paintstore
  license: MIT License
  summary: 'A Django app that integrates jQuery ColorPicker with the Django admin.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
