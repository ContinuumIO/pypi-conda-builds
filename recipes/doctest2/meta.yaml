package:
  name: doctest2
  version: "0.1"

source:
  fn: doctest2-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/doctest2/doctest2-0.1.tar.gz
  md5: 21eb87ffc44c9cebcd7df5fc74e7103f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - doctest2 = doctest2:main
    #
    # Would create an entry point called doctest2 that calls doctest2.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - doctest2
    - doctest2.languages
    - doctest2.scripts
    - doctest2.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/devin.jeanpierre/doctest2/
  license: MIT License
  summary: 'Enhanced doctest library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
