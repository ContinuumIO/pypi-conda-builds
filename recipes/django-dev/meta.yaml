package:
  name: django-dev
  version: "0.1.4"

source:
  fn: django-dev-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-dev/django-dev-0.1.4.tar.gz
  md5: 4f4320b70a0ad719e03d7b9a5a338cba
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-dev = django-dev:main
    #
    # Would create an entry point called django-dev that calls django-dev.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_dev

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/idlesign/django-dev
  license: BSD License
  summary: 'Tools to facilitate application development for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
