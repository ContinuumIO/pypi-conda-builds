package:
  name: django-userpreferences
  version: "0.1"

source:
  fn: django-userpreferences-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-userpreferences/django-userpreferences-0.1.tar.gz
  md5: e1ac68e1425d43c88ac7d688428d79a7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-userpreferences = django-userpreferences:main
    #
    # Would create an entry point called django-userpreferences that calls django-userpreferences.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django_picklefield >=0.1.9

  run:
    - python
    - django_picklefield >=0.1.9

test:
  # Python imports
  imports:
    - preferences

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/Narsil/django-userpreferences
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Django application that allows you to easily store\n    preferences for your users to choose from. Sending emails but want to let\n    the user choose the frequency ? Do it by adding a preferences.py file in\n    your email app.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
