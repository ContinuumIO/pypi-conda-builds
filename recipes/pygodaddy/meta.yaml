package:
  name: pygodaddy
  version: "0.2.2"

source:
  fn: pygodaddy-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/p/pygodaddy/pygodaddy-0.2.2.tar.gz
  md5: e99157dd67fc36a7a381cce7f2457f83
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pygodaddy = pygodaddy:main
    #
    # Would create an entry point called pygodaddy that calls pygodaddy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=1.2.3
    - tldextract >=1.5.1

  run:
    - python
    - requests >=1.2.3
    - tldextract >=1.5.1

test:
  # Python imports
  imports:
    - pygodaddy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/observerss/pygodaddy
  license: Apache Software License
  summary: '3rd Party Client Library for Manipulating Go Daddy DNS Records.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
