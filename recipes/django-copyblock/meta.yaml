package:
  name: django-copyblock
  version: "0.5.3"

source:
  fn: django-copyblock-0.5.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-copyblock/django-copyblock-0.5.3.tar.gz
  md5: 7f80eb1b704fdefd81898e5ed54ad292
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-copyblock = django-copyblock:main
    #
    # Would create an entry point called django-copyblock that calls django-copyblock.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - copyblock
    - copyblock.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/sivy/django-copyblock/
  license: MIT License
  summary: 'Manage website copy as a directory of markdown-formatted files. Insert files as copy into your Django templates.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
