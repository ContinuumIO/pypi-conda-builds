package:
  name: backitup
  version: "0.9.6"

source:
  fn: backitup-0.9.6.zip
  url: https://pypi.python.org/packages/source/b/backitup/backitup-0.9.6.zip
  md5: 4ba2dfe41b051f1c8dfb512ec87d5fd2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - backitup = backitup:main
    #
    # Would create an entry point called backitup that calls backitup.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - backitup

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/backitup/
  license: MIT License
  summary: 'Simple library for easily customized backup and uploading of your files'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
