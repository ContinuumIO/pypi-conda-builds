package:
  name: django-restapi-op
  version: "0.1.0"

source:
  fn: django-restapi-op-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-restapi-op/django-restapi-op-0.1.0.tar.gz
  md5: fd0393dc13fa1989d07f391205017952
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-restapi-op = django-restapi-op:main
    #
    # Would create an entry point called django-restapi-op that calls django-restapi-op.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - django_restapi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/OpenProximity/django_restapi
  license: Apache V2
  summary: 'Django REST API modified for OpenProximity'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
