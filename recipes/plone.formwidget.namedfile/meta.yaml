package:
  name: plone.formwidget.namedfile
  version: "1.0.13"

source:
  fn: plone.formwidget.namedfile-1.0.13.zip
  url: https://pypi.python.org/packages/source/p/plone.formwidget.namedfile/plone.formwidget.namedfile-1.0.13.zip
  md5: cb2d1360f7a3974a39b9f5e3f3dda33c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.formwidget.namedfile = plone.formwidget.namedfile:main
    #
    # Would create an entry point called plone.formwidget.namedfile that calls plone.formwidget.namedfile.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.namedfile
    - plone.z3cform >=0.7.4
    - z3c.form

  run:
    - python
    - setuptools
    - setuptools
    - plone.namedfile
    - plone.z3cform >=0.7.4
    - z3c.form

test:
  # Python imports
  imports:
    - plone
    - plone.formwidget
    - plone.formwidget.namedfile

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/plone.formwidget.namedfile
  license: GPL
  summary: 'Image widget for z3c.form and Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
