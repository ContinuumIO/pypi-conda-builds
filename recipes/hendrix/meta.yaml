package:
  name: hendrix
  version: "0.1.15"

source:
  fn: hendrix-0.1.15.tar.gz
  url: https://pypi.python.org/packages/source/h/hendrix/hendrix-0.1.15.tar.gz
  md5: a1563584356b2ac8b5a8c45b68836da8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hendrix = hendrix:main
    #
    # Would create an entry point called hendrix that calls hendrix.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - twisted
    - txsockjs
    - zope.interface
    - watchdog
    - jinja2
    - pychalk ==0.0.5
    - service_identity

  run:
    - python
    - twisted
    - txsockjs
    - zope.interface
    - watchdog
    - jinja2
    - pychalk ==0.0.5
    - service_identity

test:
  # Python imports
  imports:
    - hendrix
    - hendrix.contrib
    - hendrix.contrib.async
    - hendrix.contrib.cache
    - hendrix.contrib.cache.backends
    - hendrix.contrib.resources
    - hendrix.contrib.services
    - hendrix.contrib.ssl
    - hendrix.deploy
    - hendrix.management
    - hendrix.management.commands
    - hendrix.test
    - hendrix.test.testproject
    - hendrix.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/hangarunderground/hendrix
  license: MIT License
  summary: 'A deployment module for Django that uses Twisted.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
