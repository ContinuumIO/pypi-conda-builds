package:
  name: collective.blog.portlets
  version: "1.6"

source:
  fn: collective.blog.portlets-1.6.zip
  url: https://pypi.python.org/packages/source/c/collective.blog.portlets/collective.blog.portlets-1.6.zip
  md5: 5a912592545e8cadc20747d0b543c467
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.blog.portlets = collective.blog.portlets:main
    #
    # Would create an entry point called collective.blog.portlets that calls collective.blog.portlets.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone

  run:
    - python
    - setuptools
    - setuptools
    - plone

test:
  # Python imports
  imports:
    - collective
    - collective.blog
    - collective.blog.portlets

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/collective/collective.blog.portlets
  license: GPL
  summary: 'Archive view and portlet'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
