package:
  name: easypost
  version: "2.0.14"

source:
  fn: easypost-2.0.14.tar.gz
  url: https://pypi.python.org/packages/source/e/easypost/easypost-2.0.14.tar.gz
  md5: 7430012d9f7dc1f5130d0a22983531a2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - easypost = easypost:main
    #
    # Would create an entry point called easypost that calls easypost.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=1.0.0
    - six

  run:
    - python
    - requests >=1.0.0
    - six

test:
  # Python imports
  imports:
    - easypost

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://easypost.com/
  license: UNKNOWN
  summary: 'EasyPost Shipping API Client Library for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
