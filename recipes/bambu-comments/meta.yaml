package:
  name: bambu-comments
  version: "2.0.1"

source:
  fn: bambu-comments-2.0.1.tar.gz
  url: https://pypi.python.org/packages/source/b/bambu-comments/bambu-comments-2.0.1.tar.gz
  md5: 586ddfbcd2eab9420e2e320bece7104d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bambu-comments = bambu-comments:main
    #
    # Would create an entry point called bambu-comments that calls bambu-comments.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4
    - pyquery
    - html2text
    - markdown
    - bambu-mail >=2.0
    - requests >=2.2.1

  run:
    - python
    - django >=1.4
    - pyquery
    - html2text
    - markdown
    - bambu-mail >=2.0
    - requests >=2.2.1

test:
  # Python imports
  imports:
    - bambu_comments
    - bambu_comments.migrations
    - bambu_comments.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/iamsteadman/bambu-comments
  license: UNKNOWN
  summary: 'Generic model commenting'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
