package:
  name: django-template-email
  version: "0.1"

source:
  fn: django-template-email-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-template-email/django-template-email-0.1.tar.gz
  md5: e060727cf234c52f38f8444077a750c5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-template-email = django-template-email:main
    #
    # Would create an entry point called django-template-email that calls django-template-email.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - template_email
    - template_email.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/bendavis78/django-template-email
  license: UNKNOWN
  summary: 'A useful tool for building email messages using django templates'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
