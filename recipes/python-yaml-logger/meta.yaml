package:
  name: python-yaml-logger
  version: "0.3.1"

source:
  fn: python-yaml-logger-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/p/python-yaml-logger/python-yaml-logger-0.3.1.tar.gz
  md5: f5f6d1402c24fb7f5f003976d694b7be
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-yaml-logger = python-yaml-logger:main
    #
    # Would create an entry point called python-yaml-logger that calls python-yaml-logger.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyyaml

  run:
    - python
    - setuptools
    - pyyaml

test:
  # Python imports
  imports:
    - yamlformatter

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/OldhamMade/python-yaml-logger
  license: MIT License
  summary: 'YAML formatter for the standard Python logging module'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
