package:
  name: django-clever-selects
  version: "0.6.9"

source:
  fn: django-clever-selects-0.6.9.tar.gz
  url: https://pypi.python.org/packages/source/d/django-clever-selects/django-clever-selects-0.6.9.tar.gz
  md5: a73f489f2a23e210e06a7c80da30d508
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-clever-selects = django-clever-selects:main
    #
    # Would create an entry point called django-clever-selects that calls django-clever-selects.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - clever_selects
    - clever_selects.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/PragmaticMates/django-clever-selects
  license: BSD License
  summary: 'Chained select box widget for Django framework using AJAX requests.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
