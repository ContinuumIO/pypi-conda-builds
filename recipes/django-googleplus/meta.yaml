package:
  name: django-googleplus
  version: "0.1"

source:
  fn: django-googleplus-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-googleplus/django-googleplus-0.1.tar.gz
  md5: 0601f22c844df5f39c984dee0c933e36
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-googleplus = django-googleplus:main
    #
    # Would create an entry point called django-googleplus that calls django-googleplus.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - account
    - googleplus
    - googleplus.backends
    - googleplus.backends.default

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jonasgeiregat/django-googleplus
  license: UNKNOWN
  summary: ' A plugable registration backend for googleplus. Closely modeled after django-registration'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
