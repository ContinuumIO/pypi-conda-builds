package:
  name: flask-sqlalchemy-session
  version: "1.0"

source:
  fn: Flask-SQLAlchemy-Session-1.0.zip
  url: https://pypi.python.org/packages/source/F/Flask-SQLAlchemy-Session/Flask-SQLAlchemy-Session-1.0.zip
  md5: fcad45e73c1159b6e7983a16bc4e6fff
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-sqlalchemy-session = flask-sqlalchemy-session:main
    #
    # Would create an entry point called flask-sqlalchemy-session that calls flask-sqlalchemy-session.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sqlalchemy >=0.9
    - flask >=0.9
    - werkzeug >=0.6.1

  run:
    - python
    - sqlalchemy >=0.9
    - flask >=0.9
    - werkzeug >=0.6.1

test:
  # Python imports
  imports:
    - flask_sqlalchemy_session

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github/dtheodor/flask-sqlalchemy-session
  license: MIT License
  summary: 'SQL Alchemy session scoped on Flask requests.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
