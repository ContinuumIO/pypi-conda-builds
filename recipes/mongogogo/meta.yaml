package:
  name: mongogogo
  version: "0.1"

source:
  fn: mongogogo-0.1.tar.gz
  url: https://pypi.python.org/packages/source/m/mongogogo/mongogogo-0.1.tar.gz
  md5: 40e196a58d6e5fc70321b73260a43502
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mongogogo = mongogogo:main
    #
    # Would create an entry point called mongogogo that calls mongogogo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pymongo
    - colander

  run:
    - python
    - setuptools
    - pymongo
    - colander

test:
  # Python imports
  imports:
    - mongogogo
    - mongogogo.query
    - mongogogo.schema
    - mongogogo.schema.tests
    - mongogogo.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: MIT
  summary: 'Mongo Schema and Query Library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
