package:
  name: setuputils
  version: "0.9.4"

source:
  fn: setuputils-0.9.4.tar.gz
  url: https://pypi.python.org/packages/source/s/setuputils/setuputils-0.9.4.tar.gz
  md5: be52c8335a9f9a8f8063377f660aa7be
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - setuputils = setuputils:main
    #
    # Would create an entry point called setuputils that calls setuputils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - setuputils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/setuputils
  license: Python Software Foundation License
  summary: 'A utility to automate away boilerplate in Python setup scripts.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
