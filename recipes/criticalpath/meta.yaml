package:
  name: criticalpath
  version: "0.1.0"

source:
  fn: criticalpath-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/c/criticalpath/criticalpath-0.1.0.tar.gz
  md5: 6544e1c8dea2eabb451cf3bec51c8bf3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - criticalpath = criticalpath:main
    #
    # Would create an entry point called criticalpath that calls criticalpath.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - criticalpath

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/chrisspen/criticalpath
  license: GNU Lesser General Public License v3 (LGPLv3)
  summary: 'Calculates the critical path in a task network.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
