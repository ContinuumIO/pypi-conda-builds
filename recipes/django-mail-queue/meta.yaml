package:
  name: django-mail-queue
  version: "2.2.1"

source:
  fn: django-mail-queue-2.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-mail-queue/django-mail-queue-2.2.1.tar.gz
  md5: a82567acf18e25990c4e282804335f38
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-mail-queue = django-mail-queue:main
    #
    # Would create an entry point called django-mail-queue that calls django-mail-queue.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - mailqueue
    - mailqueue.management
    - mailqueue.management.commands
    - mailqueue.migrations
    - mailqueue.south_migrations
    - mailqueue.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/dstegelman/django-mail-queue
  license: MIT License
  summary: 'Simple Mail Queuing for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
