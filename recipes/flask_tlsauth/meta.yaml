package:
  name: flask_tlsauth
  version: "0.1.3"

source:
  fn: flask_tlsauth-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/f/flask_tlsauth/flask_tlsauth-0.1.3.tar.gz
  md5: 3eb5ceda23cc80173c950dbabefc6885
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask_tlsauth = flask_tlsauth:main
    #
    # Would create an entry point called flask_tlsauth that calls flask_tlsauth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - tlsauth
    - flask

  run:
    - python
    - tlsauth
    - flask

test:
  # Python imports
  imports:
    - flask_tlsauth

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://packages.python.org/flask_tlsauth
  license: BSD License
  summary: 'Flask extension implementing TLS Authentication - simple client certificate CA inclusive'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
