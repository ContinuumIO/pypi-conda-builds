package:
  name: tornado_content_negotiation
  version: "0.2.2"

source:
  fn: tornado_content_negotiation-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/t/tornado_content_negotiation/tornado_content_negotiation-0.2.2.tar.gz
  md5: ebb1636872de268fb296a1974abb7229
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tornado_content_negotiation = tornado_content_negotiation:main
    #
    # Would create an entry point called tornado_content_negotiation that calls tornado_content_negotiation.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python-mimeparse
    - tornado

  run:
    - python
    - python-mimeparse
    - tornado

test:
  # Python imports
  imports:
    - tornado_content_negotiation

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/globocom/tornado_content_negotiation
  license: MIT License
  summary: 'Content Negotiation module for Tornado.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
