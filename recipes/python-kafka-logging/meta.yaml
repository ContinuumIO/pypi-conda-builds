package:
  name: python-kafka-logging
  version: "0.2"

source:
  fn: python-kafka-logging-0.2.tar.gz
  url: https://pypi.python.org/packages/source/p/python-kafka-logging/python-kafka-logging-0.2.tar.gz
  md5: a772c530c6e989051b894fc95cd88687
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-kafka-logging = python-kafka-logging:main
    #
    # Would create an entry point called python-kafka-logging that calls python-kafka-logging.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - kafka-python ==0.9.2
    - logstash-formatter ==0.5.8

  run:
    - python
    - kafka-python ==0.9.2
    - logstash-formatter ==0.5.8

test:
  # Python imports
  imports:
    - python-kafka-logging

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/taykey/python-kafka-logging
  license: Apache Software License
  summary: 'Simple python logging handler for forwarding logs to a kafka server.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
