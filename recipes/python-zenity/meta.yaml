package:
  name: python-zenity
  version: "0.1"

source:
  fn: python-zenity-0.1.tar.gz
  url: https://pypi.python.org/packages/source/p/python-zenity/python-zenity-0.1.tar.gz
  md5: 34f4e7ff6e97f34cda037d38635a26e9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-zenity = python-zenity:main
    #
    # Would create an entry point called python-zenity that calls python-zenity.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - pythonzenity

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/poulp/python-zenity
  license: MIT
  summary: 'Simple dialog with Python and Gtk'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
