package:
  name: plone.app.jqtools
  version: "1.0"

source:
  fn: plone.app.jqtools-1.0.zip
  url: https://pypi.python.org/packages/source/p/plone.app.jqtools/plone.app.jqtools-1.0.zip
  md5: ec051faf542279e4cd37d4034aa576aa
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.app.jqtools = plone.app.jqtools:main
    #
    # Would create an entry point called plone.app.jqtools that calls plone.app.jqtools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.jquerytools

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.jquerytools

test:
  # Python imports
  imports:
    - plone
    - plone.app
    - plone.app.jqtools

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/plone/plone.app.jquerytools
  license: GPL
  summary: 'JQuery Tools Integration for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
