package:
  name: django-mobilvest
  version: "0.1.1"

source:
  fn: django-mobilvest-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-mobilvest/django-mobilvest-0.1.1.tar.gz
  md5: 2515e5fe08a802568624f1b4032afa0b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-mobilvest = django-mobilvest:main
    #
    # Would create an entry point called django-mobilvest that calls django-mobilvest.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - mobilvest

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/ir4y/django-mobilvest
  license: BSD License
  summary: 'This Django is a sms-sender backend for http://mobilvest.ru service'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
