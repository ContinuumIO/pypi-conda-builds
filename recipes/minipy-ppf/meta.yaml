package:
  name: minipy-ppf
  version: "0.2"

source:
  fn: minipy-ppf-0.2.tar.gz
  url: https://pypi.python.org/packages/source/m/minipy-ppf/minipy-ppf-0.2.tar.gz
  md5: 298fbf2996af20ff6fad5ee29adc2a13
#  patches:
   # List any patch files here
   # - fix.patch

build:
  # preserve_egg_dir: True
  entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - minipy-ppf = minipy-ppf:main
    #
    # Would create an entry point called minipy-ppf that calls minipy-ppf.main()

    - minipy = minipy:main

  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  # imports:

  commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.

    - minipy --help

  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/cpbotha/minipy
  license: GNU General Public License (GPL)
  summary: 'Minify Python 2 source code - PyPI Fix'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
