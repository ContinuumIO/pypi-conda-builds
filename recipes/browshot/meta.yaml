package:
  name: browshot
  version: "1.14.0"

source:
  fn: Browshot-1.14.0.tar.gz
  url: https://pypi.python.org/packages/source/B/Browshot/Browshot-1.14.0.tar.gz
  md5: 6a410ee93d4efa50b3fcc76a4442ae22
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - browshot = browshot:main
    #
    # Would create an entry point called browshot that calls browshot.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - simplejson
    - requests

  run:
    - python
    - simplejson
    - requests

test:
  # Python imports
  imports:
    - browshot
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/juliensobrier/browshot-python
  license: BSD License
  summary: 'Client to interact with Browshot'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
