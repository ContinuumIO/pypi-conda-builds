package:
  name: django-shop-postfinance
  version: "0.0.3"

source:
  fn: django-shop-postfinance-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-shop-postfinance/django-shop-postfinance-0.0.3.tar.gz
  md5: 0f67b5d8ffc10afd893932e1bb0b375e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-shop-postfinance = django-shop-postfinance:main
    #
    # Would create an entry point called django-shop-postfinance that calls django-shop-postfinance.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.2

  run:
    - python
    - django >=1.2

test:
  # Python imports
  imports:
    - shop_postfinance
    - shop_postfinance.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.django-shop.org/
  license: BSD License
  summary: 'An postfinance payment backend'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
