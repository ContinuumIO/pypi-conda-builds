package:
  name: supercell
  version: "0.6.3"

source:
  fn: supercell-0.6.3.tar.gz
  url: https://pypi.python.org/packages/source/s/supercell/supercell-0.6.3.tar.gz
  md5: e2a898fd0f44ad103abb09a20e7cf7f9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - supercell = supercell:main
    #
    # Would create an entry point called supercell that calls supercell.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - tornado >=3.1.0,<=3.2.0
    - schematics >=0.9-4
    - scales >=1.0.3

  run:
    - python
    - tornado >=3.1.0,<=3.2.0
    - schematics >=0.9-4
    - scales >=1.0.3

test:
  # Python imports
  imports:
    - supercell

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://supercell.readthedocs.org/
  license: Apache Software License
  summary: 'Supercell is a framework for creating RESTful APIs that loosely follow the idea of domain driven design.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
