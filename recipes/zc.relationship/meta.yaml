package:
  name: zc.relationship
  version: "1.1.1"

source:
  fn: zc.relationship-1.1.1.tar.gz
  url: https://pypi.python.org/packages/source/z/zc.relationship/zc.relationship-1.1.1.tar.gz
  md5: b6461c8378603aceaa848294f92457e9
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zc.relationship = zc.relationship:main
    #
    # Would create an entry point called zc.relationship that calls zc.relationship.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - zodb3
    - zope.app.container
    - zope.app.folder
    - zope.app.intid
    - zope.interface
    - zope.component
    - zope.app.keyreference
    - zope.location
    - zope.index
    - zope.app.testing
    - zope.app.component
    - zope.testing
    - setuptools

  run:
    - python
    - zodb3
    - zope.app.container
    - zope.app.folder
    - zope.app.intid
    - zope.interface
    - zope.component
    - zope.app.keyreference
    - zope.location
    - zope.index
    - zope.app.testing
    - zope.app.component
    - zope.testing
    - setuptools

test:
  # Python imports
  imports:
    - zc
    - zc.relationship

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: ZPL 2.1
  summary: 'Low-level ZODB relationship index: supports intransitive and transitive n-ary\nrelationships.  Example usage of "relationship containers".\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
