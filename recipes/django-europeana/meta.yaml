package:
  name: django-europeana
  version: "0.1.5"

source:
  fn: django-europeana-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-europeana/django-europeana-0.1.5.tar.gz
  md5: d73abc86ba5d02a58769dbae8d813717
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-europeana = django-europeana:main
    #
    # Would create an entry point called django-europeana that calls django-europeana.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - europeana

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.aksprendimai.lt/
  license: GNU Affero General Public License v3 or later (AGPLv3+)
  summary: 'Application providing Europeana widget integration.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
