package:
  name: django-celery-transactions
  version: "0.3.1"

source:
  fn: django-celery-transactions-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-celery-transactions/django-celery-transactions-0.3.1.tar.gz
  md5: a925bb7737bc5d06c904ef2d7521cbb0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-celery-transactions = django-celery-transactions:main
    #
    # Would create an entry point called django-celery-transactions that calls django-celery-transactions.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - celery >=2.2.7
    - django >=1.2.4

  run:
    - python
    - celery >=2.2.7
    - django >=1.2.4

test:
  # Python imports
  imports:
    - djcelery_transactions

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/fellowshipofone/django-celery-transactions
  license: BSD License
  summary: 'Django transaction support for Celery tasks.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
