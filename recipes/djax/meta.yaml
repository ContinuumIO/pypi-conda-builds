package:
  name: djax
  version: "0.8.3"

source:
  fn: Djax-0.8.3.tar.gz
  url: https://pypi.python.org/packages/source/D/Djax/Djax-0.8.3.tar.gz
  md5: 95036959ab5ee1443d1c7352b5bfcbdd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djax = djax:main
    #
    # Would create an entry point called djax that calls djax.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sharrock-client
    - django
    - python-dateutil

  run:
    - python
    - sharrock-client
    - django
    - python-dateutil

test:
  # Python imports
  imports:
    - djax
    - djax.management
    - djax.management.commands
    - pax

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/Axilent/Djax
  license: BSD License
  summary: 'Integrates Django projects with Axilent.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
