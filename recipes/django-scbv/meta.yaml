package:
  name: django-scbv
  version: "2011.7"

source:
  fn: django-scbv-2011.7.tar.gz
  url: https://pypi.python.org/packages/source/d/django-scbv/django-scbv-2011.7.tar.gz
  md5: a9266e210a49bccacfd6bc43d7b83a0d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-scbv = django-scbv:main
    #
    # Would create an entry point called django-scbv that calls django-scbv.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3

  run:
    - python
    - django >=1.3

test:
  # Python imports
  imports:
    - scbv
    - scbv.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD License
  summary: 'Simple (and sane) class-based views for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
