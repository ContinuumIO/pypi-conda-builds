package:
  name: django-money-rates
  version: "0.3.1"

source:
  fn: django-money-rates-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-money-rates/django-money-rates-0.3.1.tar.gz
  md5: 4146fd175f87eacb88d004f065fc285d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-money-rates = django-money-rates:main
    #
    # Would create an entry point called django-money-rates that calls django-money-rates.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - djmoney_rates

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/evonove/django-money-rates
  license: BSD License
  summary: 'Currency conversion for money'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
