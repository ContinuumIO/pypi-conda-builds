package:
  name: django-redis-sentinel
  version: "1.0rc4"

source:
  fn: django-redis-sentinel-1.0rc4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-redis-sentinel/django-redis-sentinel-1.0rc4.tar.gz
  md5: e0e8ef49affd946aa3e71858b6617659
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-redis-sentinel = django-redis-sentinel:main
    #
    # Would create an entry point called django-redis-sentinel that calls django-redis-sentinel.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-redis ==3.8.3

  run:
    - python
    - django-redis ==3.8.3

test:
  # Python imports
  imports:
    - django_redis_sentinel

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/KabbageInc/django-redis-sentinel
  license: MIT License
  summary: 'Plugin for django-redis that supports Redis Sentinel'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
