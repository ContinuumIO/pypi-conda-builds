package:
  name: django-beanstalk
  version: "0.1"

source:
  fn: django-beanstalk-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-beanstalk/django-beanstalk-0.1.tar.gz
  md5: eefc93b182593676a122767e159a54d6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-beanstalk = django-beanstalk:main
    #
    # Would create an entry point called django-beanstalk that calls django-beanstalk.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - django_beanstalk

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/humanfromearth/django-beanstalk
  license: GNU General Public License (GPL)
  summary: 'Django administration for beanstalk daemons'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
