package:
  name: mogli
  version: "0.2.3"

source:
  fn: mogli-0.2.3.tar.gz
  url: https://pypi.python.org/packages/source/m/mogli/mogli-0.2.3.tar.gz
  md5: 8a50349ea80de47da9bdad11f9c0169b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mogli = mogli:main
    #
    # Would create an entry point called mogli that calls mogli.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - glfw
    - gr
    - numpy
    - pyopengl

  run:
    - python
    - glfw
    - gr
    - numpy
    - pyopengl

test:
  # Python imports
  imports:
    - mogli

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/FlorianRhiem/mogli
  license: MIT License
  summary: 'Simple visualization of molecules in python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
