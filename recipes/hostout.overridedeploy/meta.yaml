package:
  name: hostout.overridedeploy
  version: "1.0.1"

source:
  fn: hostout.overridedeploy-1.0.1.zip
  url: https://pypi.python.org/packages/source/h/hostout.overridedeploy/hostout.overridedeploy-1.0.1.zip
  md5: c5fe7f375c166081d531539f4731d80e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hostout.overridedeploy = hostout.overridedeploy:main
    #
    # Would create an entry point called hostout.overridedeploy that calls hostout.overridedeploy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zc.recipe.egg
    - setuptools
    - collective.hostout >=1.0a5

  run:
    - python
    - setuptools
    - zc.recipe.egg
    - setuptools
    - collective.hostout >=1.0a5

test:
  # Python imports
  imports:
    - hostout
    - hostout.overridedeploy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ianderso/hostout.overridedeployhostout.overridedeploy
  license: GPL
  summary: 'override default deploy of hostout'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
