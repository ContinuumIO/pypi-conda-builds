package:
  name: django-debugmail
  version: "1.0.4"

source:
  fn: django-debugmail-1.0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-debugmail/django-debugmail-1.0.4.tar.gz
  md5: 41350a5b5cad3b3adb8620208b09dfe5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-debugmail = django-debugmail:main
    #
    # Would create an entry point called django-debugmail that calls django-debugmail.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4.2

  run:
    - python
    - django >=1.4.2

test:
  # Python imports
  imports:
    - debugmail

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/radiosilence/django-debugmail.git
  license: BSD License
  summary: 'Debug based e-mail backend.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
