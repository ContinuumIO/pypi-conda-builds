package:
  name: fudgemsg
  version: "0.1"

source:
  fn: fudgemsg-0.1.tar.gz
  url: https://pypi.python.org/packages/source/f/fudgemsg/fudgemsg-0.1.tar.gz
  md5: 906003727ce9c5547bf3b7c4e85240df
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fudgemsg = fudgemsg:main
    #
    # Would create an entry point called fudgemsg that calls fudgemsg.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - fudgemsg
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/joequant/Fudge-Python
  license: APL2
  summary: 'Fudge is a hierarchical, typesafe, binary, self-describing message encoding system.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
