package:
  name: gauge
  version: "0.3.3"

source:
  fn: gauge-0.3.3.tar.gz
  url: https://pypi.python.org/packages/source/g/gauge/gauge-0.3.3.tar.gz
  md5: 9f5b2b0f3bfb0bbec510dd540609d631
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gauge = gauge:main
    #
    # Would create an entry point called gauge that calls gauge.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six >=1.8.0
    - sortedcontainers >=0.8.2

  run:
    - python
    - six >=1.8.0
    - sortedcontainers >=0.8.2

test:
  # Python imports
  imports:
    - gauge

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/what-studio/gauge
  license: BSD License
  summary: 'Deterministic linear gauge library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
