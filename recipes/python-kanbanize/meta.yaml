package:
  name: python-kanbanize
  version: "0.2.0"

source:
  fn: python-kanbanize-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/p/python-kanbanize/python-kanbanize-0.2.0.tar.gz
  md5: 9b573b71b78e73e0de30c8ad6399a295
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-kanbanize = python-kanbanize:main
    #
    # Would create an entry point called python-kanbanize that calls python-kanbanize.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - python_kanbanize

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/guandalf/python-kanbanize
  license: GNU GPL
  summary: 'Kanbanize.com Python API Wrapper\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
