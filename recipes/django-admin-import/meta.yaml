package:
  name: django-admin-import
  version: "0.3.0"

source:
  fn: django-admin-import-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-admin-import/django-admin-import-0.3.0.tar.gz
  md5: 78c490db3c76a87bc422614ffb82ffa7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-admin-import = django-admin-import:main
    #
    # Would create an entry point called django-admin-import that calls django-admin-import.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - admin_import

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/allink/django-admin-import/
  license: BSD License
  summary: 'Import tool attachable to almost every Django admin'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
