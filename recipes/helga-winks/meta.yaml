package:
  name: helga-winks
  version: "1.1.0"

source:
  fn: helga-winks-1.1.0.tar.gz
  url: https://pypi.python.org/packages/source/h/helga-winks/helga-winks-1.1.0.tar.gz
  md5: 295254124752f757274ec01629c6d857
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - helga-winks = helga-winks:main
    #
    # Would create an entry point called helga-winks that calls helga-winks.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/michaelorr/helga-winks
  license: GNU General Public License v3 (GPLv3)
  summary: 'winks @ u'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
