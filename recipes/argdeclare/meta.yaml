package:
  name: argdeclare
  version: "0.5.6"

source:
  fn: argdeclare-0.5.6.tar.gz
  url: https://pypi.python.org/packages/source/a/argdeclare/argdeclare-0.5.6.tar.gz
  md5: 5477ec57bfe3206480e034feb2feff21
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - argdeclare = argdeclare:main
    #
    # Would create an entry point called argdeclare that calls argdeclare.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - argdeclare

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ixmatus/argdeclare
  license: Python Software Foundation License
  summary: "Argdeclare is a declarative argument configurator for python's argparse."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
