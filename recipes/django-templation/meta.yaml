package:
  name: django-templation
  version: "0.1.1"

source:
  fn: django-templation-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-templation/django-templation-0.1.1.tar.gz
  md5: 3200733399cd18e3b0791a2a5023da40
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-templation = django-templation:main
    #
    # Would create an entry point called django-templation that calls django-templation.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - wsgidav >=1.1.0
    - uwsgi >=1.9.5

  run:
    - python
    - wsgidav >=1.1.0
    - uwsgi >=1.9.5

test:
  # Python imports
  imports:
    - templation

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/qdqmedia/django-templation
  license: BSD License
  summary: 'The easy way to allow designers edit templates and assets.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
