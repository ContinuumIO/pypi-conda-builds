package:
  name: pyelliptic
  version: "1.5.6"

source:
  fn: pyelliptic-1.5.6.tar.gz
  url: https://pypi.python.org/packages/source/p/pyelliptic/pyelliptic-1.5.6.tar.gz
  md5: b9318252f4cf8baea0f62c81acc9db9e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pyelliptic = pyelliptic:main
    #
    # Would create an entry point called pyelliptic that calls pyelliptic.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - pyelliptic

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/yann2192/pyelliptic
  license: BSD
  summary: 'Python OpenSSL wrapper for modern cryptography with ECC, AES, HMAC, Blowfish, ...'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
