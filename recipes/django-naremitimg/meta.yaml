package:
  name: django-naremitimg
  version: "0.1.2"

source:
  fn: django-naremitimg-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-naremitimg/django-naremitimg-0.1.2.tar.gz
  md5: c95c9f515c53a281341db6dfb2a282bb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-naremitimg = django-naremitimg:main
    #
    # Would create an entry point called django-naremitimg that calls django-naremitimg.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - naremitimg

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/naremit/NaremitIMG
  license: BSD License
  summary: 'A tiny Django app for performing image manipulations on the fly.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
