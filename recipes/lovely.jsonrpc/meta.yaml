package:
  name: lovely.jsonrpc
  version: "0.2.1"

source:
  fn: lovely.jsonrpc-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/l/lovely.jsonrpc/lovely.jsonrpc-0.2.1.tar.gz
  md5: 0cacae307ce355db102c9aec7fb472d8
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lovely.jsonrpc = lovely.jsonrpc:main
    #
    # Would create an entry point called lovely.jsonrpc that calls lovely.jsonrpc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - lovely
    - lovely.jsonrpc

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/lovely-jsonrpc
  license: Apache License 2.0
  summary: 'Python JSONRPC package'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
