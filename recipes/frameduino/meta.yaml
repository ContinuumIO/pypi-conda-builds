package:
  name: frameduino
  version: "0.2.1"

source:
  fn: FrameDuino-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/F/FrameDuino/FrameDuino-0.2.1.tar.gz
  md5: ba1cffdd7c4ff2621b2eb6c784d2ad13
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - frameduino = frameduino:main
    #
    # Would create an entry point called frameduino that calls frameduino.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - frameduino
    - frameduino.drivers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/adanmauri/frameduino
  license: GPLv3
  summary: 'Pinguino Board Framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
