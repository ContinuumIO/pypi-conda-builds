package:
  name: intacct
  version: "0.0.9"

source:
  fn: intacct-0.0.9.tar.gz
  url: https://pypi.python.org/packages/source/i/intacct/intacct-0.0.9.tar.gz
  md5: fcfd7c645deecda244365f4a4d378700
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - intacct = intacct:main
    #
    # Would create an entry point called intacct that calls intacct.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - intacct

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jnalley/intacctws-py
  license: MIT License
  summary: 'Python client for Intacct API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
