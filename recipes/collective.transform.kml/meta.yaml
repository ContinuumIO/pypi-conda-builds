package:
  name: collective.transform.kml
  version: "0.1"

source:
  fn: collective.transform.kml-0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.transform.kml/collective.transform.kml-0.1.tar.gz
  md5: 9e2282c6fcddc85b6d0e0e1623d96bb2
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.transform.kml = collective.transform.kml:main
    #
    # Would create an entry point called collective.transform.kml that calls collective.transform.kml.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - htmllaundry

  run:
    - python
    - setuptools
    - setuptools
    - htmllaundry

test:
  # Python imports
  imports:
    - collective
    - collective.transform
    - collective.transform.kml

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.transform.kml
  license: GPL
  summary: 'Transform KML to HTML'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
