package:
  name: labmtsimple
  version: "2.3.4.4"

source:
  fn: labMTsimple-2.3.4.4.tar.gz
  url: https://pypi.python.org/packages/source/l/labMTsimple/labMTsimple-2.3.4.4.tar.gz
  md5: bf6f95d346706c139b2a9a884808139e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - labmtsimple = labmtsimple:main
    #
    # Would create an entry point called labmtsimple that calls labmtsimple.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - labMTsimple
    - labmtsimple

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/andyreagan/labMT-simple
  license: UNKNOWN
  summary: 'Basic usage script for dictionary-based sentiment analysis. Intended use with labMT data'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
