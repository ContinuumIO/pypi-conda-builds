package:
  name: ckanext-showcase
  version: "1.0.0b1"

source:
  fn: ckanext-showcase-1.0.0b1.tar.gz
  url: https://pypi.python.org/packages/source/c/ckanext-showcase/ckanext-showcase-1.0.0b1.tar.gz
  md5: 5a3e13a056da4969c1350b30ca4d6964
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ckanext-showcase = ckanext-showcase:main
    #
    # Would create an entry point called ckanext-showcase that calls ckanext-showcase.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - ckanext
    - ckanext.showcase
    - ckanext.showcase.commands
    - ckanext.showcase.logic
    - ckanext.showcase.logic.action
    - ckanext.showcase.model
    - ckanext.showcase.tests
    - ckanext.showcase.tests.action

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ckan/ckanext-showcase
  license: GNU Affero General Public License v3 or later (AGPLv3+)
  summary: 'A ckan extension to showcase datasets in use'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
