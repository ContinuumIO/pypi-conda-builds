package:
  name: plone.app.controlpanel
  version: "3.0.2"

source:
  fn: plone.app.controlpanel-3.0.2.tar.gz
  url: https://pypi.python.org/packages/source/p/plone.app.controlpanel/plone.app.controlpanel-3.0.2.tar.gz
  md5: 18df189b68efffa93e3ac047c1ec2e9f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.app.controlpanel = plone.app.controlpanel:main
    #
    # Would create an entry point called plone.app.controlpanel that calls plone.app.controlpanel.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - plone.app.form
    - plone.app.vocabularies
    - plone.app.workflow
    - plone.fieldsets
    - plone.memoize
    - plone.protect
    - plone.locking
    - zope.annotation
    - zope.cachedescriptors
    - zope.component
    - zope.event
    - zope.formlib
    - zope.i18n
    - zope.interface
    - zope.ramcache
    - zope.publisher
    - zope.schema
    - zope.site
    - zope.testing
    - acquisition
    - products.cmfplone
    - products.cmfcore
    - products.plonepas
    - products.portaltransforms
    - products.statusmessages
    - zope2 >=2.13.0
    - zodb3

  run:
    - python
    - setuptools
    - plone.app.form
    - plone.app.vocabularies
    - plone.app.workflow
    - plone.fieldsets
    - plone.memoize
    - plone.protect
    - plone.locking
    - zope.annotation
    - zope.cachedescriptors
    - zope.component
    - zope.event
    - zope.formlib
    - zope.i18n
    - zope.interface
    - zope.ramcache
    - zope.publisher
    - zope.schema
    - zope.site
    - zope.testing
    - acquisition
    - products.cmfplone
    - products.cmfcore
    - products.plonepas
    - products.portaltransforms
    - products.statusmessages
    - zope2 >=2.13.0
    - zodb3

test:
  # Python imports
  imports:
    - plone
    - plone.app
    - plone.app.controlpanel

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/plone.app.controlpanel
  license: GNU General Public License (GPL)
  summary: 'Formlib-based controlpanels for Plone.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
