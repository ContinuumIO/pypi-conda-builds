package:
  name: django-shop-catalog
  version: "0.0.3"

source:
  fn: django-shop-catalog-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-shop-catalog/django-shop-catalog-0.0.3.tar.gz
  md5: 022af3bd76ded383e799b4dce9ca235d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-shop-catalog = django-shop-catalog:main
    #
    # Would create an entry point called django-shop-catalog that calls django-shop-catalog.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6,<1.8
    - django-cms >=3.0.1
    - django-shop >=0.2.0
    - django-filer >=0.9.5
    - django-mptt >=0.6.0
    - django-hvad >=0.4.0
    - measurement >=1.7.2
    - django-currencies >=0.3.2

  run:
    - python
    - django >=1.6,<1.8
    - django-cms >=3.0.1
    - django-shop >=0.2.0
    - django-filer >=0.9.5
    - django-mptt >=0.6.0
    - django-hvad >=0.4.0
    - measurement >=1.7.2
    - django-currencies >=0.3.2

test:
  # Python imports
  imports:
    - catalog
    - catalog.addresses
    - catalog.addresses.management
    - catalog.addresses.management.commands
    - catalog.migrations
    - catalog.orders
    - catalog.reviews
    - catalog.templatetags
    - catalog.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/django-shop-catalog/
  license: BSD
  summary: u'Catalog app for django SHOP'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
