package:
  name: mk.recipe.modwsgi
  version: "0.1"

source:
  fn: mk.recipe.modwsgi-0.1.tar.gz
  url: https://pypi.python.org/packages/source/m/mk.recipe.modwsgi/mk.recipe.modwsgi-0.1.tar.gz
  md5: cf93b0efe145cd84f4d4c750fe0cd1a2
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mk.recipe.modwsgi = mk.recipe.modwsgi:main
    #
    # Would create an entry point called mk.recipe.modwsgi that calls mk.recipe.modwsgi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.recipe.egg

  run:
    - python
    - setuptools
    - setuptools
    - zc.recipe.egg

test:
  # Python imports
  imports:
    - mk
    - mk.recipe
    - mk.recipe.modwsgi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/k4ml/mk.recipe.modwsgi
  license: Zope Public License
  summary: 'WSGI from buildout'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
