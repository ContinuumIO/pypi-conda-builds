package:
  name: django-scss
  version: "0.6.1"

source:
  fn: django-scss-0.6.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-scss/django-scss-0.6.1.tar.gz
  md5: 99f19560f659767416b1e4f2b21043d5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-scss = django-scss:main
    #
    # Would create an entry point called django-scss that calls django-scss.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_scss
    - django_scss.templatetags
    - django_scss.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/andreyfedoseev/django-scss
  license: BSD License
  summary: 'Django template tags to compile SCSS into CSS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
