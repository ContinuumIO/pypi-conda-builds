package:
  name: echoplot
  version: "0.1.1"

source:
  fn: echoplot-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/e/echoplot/echoplot-0.1.1.tar.gz
  md5: 0c5ff68acaf01e75b2f790dee9a311a2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - echoplot = echoplot:main
    #
    # Would create an entry point called echoplot that calls echoplot.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - matplotlib >=1.3.1
    - numpy >=1.8.1
    - pyechonest >=8.0.1

  run:
    - python
    - matplotlib >=1.3.1
    - numpy >=1.8.1
    - pyechonest >=8.0.1

test:
  # Python imports
  imports:
    - echoplot

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/echoplot
  license: LICENSE.txt
  summary: 'Plot song data using the EchoNest API.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
