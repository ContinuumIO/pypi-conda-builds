package:
  name: gitzebo
  version: "0.0.10"

source:
  fn: gitzebo-0.0.10.tar.gz
  url: https://pypi.python.org/packages/source/g/gitzebo/gitzebo-0.0.10.tar.gz
  md5: 7bbf43e29ad85396146e8f8032d74447
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gitzebo = gitzebo:main
    #
    # Would create an entry point called gitzebo that calls gitzebo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sqlalchemy
    - flask
    - argparse
    - sh

  run:
    - python
    - sqlalchemy
    - flask
    - argparse
    - sh

test:
  # Python imports
  imports:
    - gitzebo

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://jgilik.com/gitzebo/
  license: 2-Clause BSD License
  summary: 'A minimal git management web application.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
