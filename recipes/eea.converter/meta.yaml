package:
  name: eea.converter
  version: "9.2"

source:
  fn: eea.converter-9.2.zip
  url: https://pypi.python.org/packages/source/e/eea.converter/eea.converter-9.2.zip
  md5: 4bd2a810f2a2d0707836d3e8d4524d34
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - eea.converter = eea.converter:main
    #
    # Would create an entry point called eea.converter that calls eea.converter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - kv
    - pillow
    - products.statusmessages

  run:
    - python
    - setuptools
    - setuptools
    - kv
    - pillow
    - products.statusmessages

test:
  # Python imports
  imports:
    - eea
    - eea.converter
    - eea.converter.browser
    - eea.converter.browser.app
    - eea.converter.events
    - eea.converter.pdf
    - eea.converter.rules
    - eea.converter.tests
    - eea.converter.upgrades

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://eea.github.com/docs/eea.converter
  license: GNU General Public License (GPL) or Mozilla Public License 1.0 (MPL)
  summary: 'SVG, PNG, PDF converters using external tools as ImageMagick'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
