package:
  name: landmarkerio-server
  version: "0.0.7"

source:
  fn: landmarkerio-server-0.0.7.tar.gz
  url: https://pypi.python.org/packages/source/l/landmarkerio-server/landmarkerio-server-0.0.7.tar.gz
  md5: 7a01e404731347461b4ad230f3322989
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - landmarkerio-server = landmarkerio-server:main
    #
    # Would create an entry point called landmarkerio-server that calls landmarkerio-server.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - menpo >=0.2.5
    - flask >=0.10.1
    - flask-restful >=0.2.11
    - flask-compress >=1.0.2

  run:
    - python
    - menpo >=0.2.5
    - flask >=0.10.1
    - flask-restful >=0.2.11
    - flask-compress >=1.0.2

test:
  # Python imports
  imports:
    - landmarkerioserver

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/menpo/landmarkerio-server/
  license: BSD License
  summary: 'Menpo-based server for www.landmarker.io'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
