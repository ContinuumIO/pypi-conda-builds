package:
  name: django-system-monitor
  version: "0.5"

source:
  fn: django-system-monitor-0.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-system-monitor/django-system-monitor-0.5.tar.gz
  md5: d3ae93dbb3889948821ef75f0c3f7dbe
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-system-monitor = django-system-monitor:main
    #
    # Would create an entry point called django-system-monitor that calls django-system-monitor.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - psutil ==1.2.1

  run:
    - python
    - psutil ==1.2.1

test:
  # Python imports
  imports:
    - sysmon
    - sysmon.templatetags
    - sysmon.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/hakanzy/django-system-monitor
  license: BSD License
  summary: 'View simple system statistics in django admin panel'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
