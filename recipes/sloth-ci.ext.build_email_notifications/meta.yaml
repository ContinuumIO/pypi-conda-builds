package:
  name: sloth-ci.ext.build_email_notifications
  version: "1.0.5"

source:
  fn: sloth-ci.ext.build_email_notifications-1.0.5.zip
  url: https://pypi.python.org/packages/source/s/sloth-ci.ext.build_email_notifications/sloth-ci.ext.build_email_notifications-1.0.5.zip
  md5: d717f754a26f4851449410970655ce1a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sloth-ci.ext.build_email_notifications = sloth-ci.ext.build_email_notifications:main
    #
    # Would create an entry point called sloth-ci.ext.build_email_notifications that calls sloth-ci.ext.build_email_notifications.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sloth_ci >=1.2.3

  run:
    - python
    - sloth_ci >=1.2.3

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/moigagoo/sloth-ci-extensions
  license: MIT
  summary: 'Build email notifications for Sloth CI apps'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
