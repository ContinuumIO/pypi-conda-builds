package:
  name: django-strategy-field
  version: "0.1"

source:
  fn: django-strategy-field-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-strategy-field/django-strategy-field-0.1.tar.gz
  md5: 2724b2d0a1f10297c363e7aadd7410cd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-strategy-field = django-strategy-field:main
    #
    # Would create an entry point called django-strategy-field that calls django-strategy-field.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - strategy_field

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/saxix/django-strategy-field
  license: BSD
  summary: 'Django custom field to implement the pattern strategy'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
