package:
  name: sqlalchemy-fulltext-search
  version: "0.2.2"

source:
  fn: SQLAlchemy-FullText-Search-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/S/SQLAlchemy-FullText-Search/SQLAlchemy-FullText-Search-0.2.2.tar.gz
  md5: cd3dea9cebc25ae8b4a1edbd5d7ebb19
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sqlalchemy-fulltext-search = sqlalchemy-fulltext-search:main
    #
    # Would create an entry point called sqlalchemy-fulltext-search that calls sqlalchemy-fulltext-search.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sqlalchemy >=0.8

  run:
    - python
    - sqlalchemy >=0.8

test:
  # Python imports
  imports:
    - sqlalchemy_fulltext

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mengzhuo/sqlalchemy-fulltext-search
  license: BSD License
  summary: 'Provide FullText for MYSQL & SQLAlchemy model'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
