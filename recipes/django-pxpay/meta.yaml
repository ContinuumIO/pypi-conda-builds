package:
  name: django-pxpay
  version: "0.2.6"

source:
  fn: django-pxpay-0.2.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-pxpay/django-pxpay-0.2.6.tar.gz
  md5: e677ed2ba12a469b10e2dcbe2f936e19
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-pxpay = django-pxpay:main
    #
    # Would create an entry point called django-pxpay that calls django-pxpay.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=0.13.5

  run:
    - python
    - requests >=0.13.5

test:
  # Python imports
  imports:
    - pxpay

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/pavel-shpilev/django-pxpay
  license: BSD License
  summary: 'PaymentExpress PxPay Gateway for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
