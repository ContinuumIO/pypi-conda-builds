package:
  name: django-registration-names
  version: "0.1b"

source:
  fn: django-registration-names-0.1b.tar.gz
  url: https://pypi.python.org/packages/source/d/django-registration-names/django-registration-names-0.1b.tar.gz
  md5: 96ce64fee5ecec1455a11ae9cdd4ce77
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-registration-names = django-registration-names:main
    #
    # Would create an entry point called django-registration-names that calls django-registration-names.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - django-registration

  run:
    - python
    - django
    - django-registration

test:
  # Python imports
  imports:
    - registration_names
    - registration_names.backends
    - registration_names.backends.default
    - registration_names.backends.simple

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ivanyu/django-registration-names
  license: MIT License
  summary: 'A way to control allowed and prohibited user names for registration with django-registration.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
