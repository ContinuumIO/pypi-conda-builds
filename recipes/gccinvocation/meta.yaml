package:
  name: gccinvocation
  version: "0.1"

source:
  fn: gccinvocation-0.1.tar.gz
  url: https://pypi.python.org/packages/source/g/gccinvocation/gccinvocation-0.1.tar.gz
  md5: eae9d1e3eab101a66c5540bc67a300bc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gccinvocation = gccinvocation:main
    #
    # Would create an entry point called gccinvocation that calls gccinvocation.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - gccinvocation

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/fedora-static-analysis/gccinvocation
  license: LGPLv2.1+
  summary: 'Library for parsing GCC command-line options'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
