package:
  name: atomia-client
  version: "12.5.0"

source:
  fn: atomia-client-12.5.0.zip
  url: https://pypi.python.org/packages/source/a/atomia-client/atomia-client-12.5.0.zip
  md5: b2ab89b96bf6db6c919cc2bc6215020f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - atomia-client = atomia-client:main
    #
    # Would create an entry point called atomia-client that calls atomia-client.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - atomia_client
    - atomia_client.pysimplesoap_atomia

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.atomia.com/
  license: Atomia
  summary: 'Client library for Atomia Automation Server.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
