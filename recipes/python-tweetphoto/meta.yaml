package:
  name: python-tweetphoto
  version: "0.2"

source:
  fn: python-tweetphoto-0.2.tar.gz
  url: https://pypi.python.org/packages/source/p/python-tweetphoto/python-tweetphoto-0.2.tar.gz
  md5: 2cee7eb134136808d8f561b75bc953a3
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-tweetphoto = python-tweetphoto:main
    #
    # Would create an entry point called python-tweetphoto that calls python-tweetphoto.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - simplejson

  run:
    - python
    - setuptools
    - simplejson

test:
  # Python imports
  imports:
    - pyTweetPhoto

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/python-tweetphoto/
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'A python wrapper around the TweetPhoto API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
