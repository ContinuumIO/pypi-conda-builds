package:
  name: dodostorage
  version: "0.3.dev-r2016"

source:
  fn: DoDoStorage-0.3.dev-r2016.tar.gz
  url: https://pypi.python.org/packages/source/D/DoDoStorage/DoDoStorage-0.3.dev-r2016.tar.gz
  md5: ab78bd89eb63b564cf22dc680ecb4d9b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dodostorage = dodostorage:main
    #
    # Would create an entry point called dodostorage that calls dodostorage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - hutools
    - httplib2
    - simplejson
    - selector
    - sqlalchemy >=0.3

  run:
    - python
    - hutools
    - httplib2
    - simplejson
    - selector
    - sqlalchemy >=0.3

test:
  # Python imports
  imports:
    - dodostorage

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.hudora.de/code/
  license: BSD
  summary: 'User-Space write-once Network Filesystem'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
