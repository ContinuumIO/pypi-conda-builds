package:
  name: javaobj
  version: "0.1.0"

source:
  fn: javaobj-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/j/javaobj/javaobj-0.1.0.tar.gz
  md5: 5608c2c76ed2fe76a35517a6f600a469
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - javaobj = javaobj:main
    #
    # Would create an entry point called javaobj that calls javaobj.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - javaobj

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/python-javaobj
  license: Apache Software License
  summary: 'Module for serializing and de-serializing Java objects.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
