package:
  name: tzwhere
  version: "1.0"

source:
  fn: tzwhere-1.0.tar.gz
  url: https://pypi.python.org/packages/source/t/tzwhere/tzwhere-1.0.tar.gz
  md5: bfc694a6fb1e138e655bb72a24ca5a78
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tzwhere = tzwhere:main
    #
    # Would create an entry point called tzwhere that calls tzwhere.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - tzwhere

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/pegler/pytzwhere
  license: MIT License
  summary: 'Python library to look up timezone from lat / long offline'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
