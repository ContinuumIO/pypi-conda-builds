package:
  name: django-oppia
  version: "0.7.2"

source:
  fn: django-oppia-0.7.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-oppia/django-oppia-0.7.2.tar.gz
  md5: 181e382e07a008781520f6bd9097b31a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-oppia = django-oppia:main
    #
    # Would create an entry point called django-oppia that calls django-oppia.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.7.3
    - django-tastypie >=0.11.0
    - django-tablib >=0.9.11
    - django-crispy-forms >=1.4.0

  run:
    - python
    - django >=1.7.3
    - django-tastypie >=0.11.0
    - django-tablib >=0.9.11
    - django-crispy-forms >=1.4.0

test:
  # Python imports
  imports:
    - oppia
    - oppia.api
    - oppia.fixtures
    - oppia.migrations
    - oppia.mobile
    - oppia.profile
    - oppia.quiz
    - oppia.quiz.api
    - oppia.quiz.migrations
    - oppia.viz
    - oppia.viz.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://digital-campus.org/
  license: GNU General Public License v3 (GPLv3)
  summary: 'Server side component of OppiaMobile learning platform'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
