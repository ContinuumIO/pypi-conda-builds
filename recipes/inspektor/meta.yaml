package:
  name: inspektor
  version: "0.1.15"

source:
  fn: inspektor-0.1.15.tar.gz
  url: https://pypi.python.org/packages/source/i/inspektor/inspektor-0.1.15.tar.gz
  md5: 5313a17a4feee8d8af49b0ae2d95df48
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - inspektor = inspektor:main
    #
    # Would create an entry point called inspektor that calls inspektor.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - pylint >=1.3

  run:
    - python
    - pylint >=1.3

test:
  # Python imports
  imports:
    - inspektor
    - inspektor.cli
    - inspektor.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/autotest/inspektor
  license: UNKNOWN
  summary: 'Inspektor code checker'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
