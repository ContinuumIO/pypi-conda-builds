package:
  name: argent
  version: "0.01.1"

source:
  fn: argent-0.01.1.tar.gz
  url: https://pypi.python.org/packages/source/a/argent/argent-0.01.1.tar.gz
  md5: 8e8ed508065a622ce6949590b74f6e89
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - argent = argent:main
    #
    # Would create an entry point called argent that calls argent.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - clint

  run:
    - python
    - clint

test:
  # Python imports
  imports:
    - argent

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/startling/argent
  license: UNKNOWN
  summary: 'Parse command-line arguments using introspective magic.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
