package:
  name: django-config
  version: "0.1.4"

source:
  fn: django-config-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-config/django-config-0.1.4.tar.gz
  md5: f3d3798deebe454342bfb73d62f5d347
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-config = django-config:main
    #
    # Would create an entry point called django-config that calls django-config.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - djangoconfig

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/tarequeh/django-config/
  license: MIT License
  summary: 'An architecture for maintaining multiple settings files in Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
