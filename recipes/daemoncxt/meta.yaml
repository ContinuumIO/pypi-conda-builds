package:
  name: daemoncxt
  version: "1.5.7"

source:
  fn: DaemonCxt-1.5.7.tar.gz
  url: https://pypi.python.org/packages/source/D/DaemonCxt/DaemonCxt-1.5.7.tar.gz
  md5: 2e3e3aa6f79f07dd660d8796d2312d59
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - daemoncxt = daemoncxt:main
    #
    # Would create an entry point called daemoncxt that calls daemoncxt.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - daemoncxt
    - daemoncxt.version

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ewiger/daemoncxt
  license: PSF
  summary: 'Library to implement a well-behaved Unix daemon process'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
