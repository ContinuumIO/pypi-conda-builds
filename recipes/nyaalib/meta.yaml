package:
  name: nyaalib
  version: "0.0.6"

source:
  fn: nyaalib-0.0.6.tar.gz
  url: https://pypi.python.org/packages/source/n/nyaalib/nyaalib-0.0.6.tar.gz
  md5: 47c670ba0aae63061e03812ed623794a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - nyaalib = nyaalib:main
    #
    # Would create an entry point called nyaalib that calls nyaalib.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - enum34
    - html5lib
    - requests

  run:
    - python
    - enum34
    - html5lib
    - requests

test:
  # Python imports
  imports:
    - nyaalib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kennydo/nyaalib
  license: MIT License
  summary: 'Python library for Nyaa'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
