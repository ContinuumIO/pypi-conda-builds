package:
  name: dbwrapper
  version: "1.1"

source:
  fn: DBWrapper-1.1.tar.gz
  url: https://pypi.python.org/packages/source/D/DBWrapper/DBWrapper-1.1.tar.gz
  md5: 88ad0ec44096297055ceaebc9cf7bd1b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dbwrapper = dbwrapper:main
    #
    # Would create an entry point called dbwrapper that calls dbwrapper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - dbwrapper

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/SvenFestersen/DBWrapper
  license: GNU General Public License v3 (GPLv3)
  summary: 'Thread-safe wrapper for Python sqlite3 bindings.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
