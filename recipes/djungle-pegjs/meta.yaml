package:
  name: djungle-pegjs
  version: "0.0.2"

source:
  fn: djungle-pegjs-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/djungle-pegjs/djungle-pegjs-0.0.2.tar.gz
  md5: 0b20a0c8ae3752b8d0160fce735c281c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djungle-pegjs = djungle-pegjs:main
    #
    # Would create an entry point called djungle-pegjs that calls djungle-pegjs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4

  run:
    - python
    - django >=1.4

test:
  # Python imports
  imports:
    - djungle
    - djungle.pegjs

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/djungle/djungle-pegjs
  license: GNU General Public License (GPL)
  summary: 'PEG.js as Django app.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
