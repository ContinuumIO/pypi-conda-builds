package:
  name: django-copyright
  version: "1.0.0"

source:
  fn: django-copyright-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-copyright/django-copyright-1.0.0.tar.gz
  md5: 12dcff1b1e4f710b350e9fda98d8f778
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-copyright = django-copyright:main
    #
    # Would create an entry point called django-copyright that calls django-copyright.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - copyright
    - copyright.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD
  summary: 'Copyright django app'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
