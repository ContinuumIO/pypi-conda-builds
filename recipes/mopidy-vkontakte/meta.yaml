package:
  name: mopidy-vkontakte
  version: "0.3.1"

source:
  fn: Mopidy-VKontakte-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/M/Mopidy-VKontakte/Mopidy-VKontakte-0.3.1.tar.gz
  md5: 4dc997f7dfe69f1186641f543726f889
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mopidy-vkontakte = mopidy-vkontakte:main
    #
    # Would create an entry point called mopidy-vkontakte that calls mopidy-vkontakte.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - mopidy >=0.18
    - pykka >=1.1

  run:
    - python
    - setuptools
    - setuptools
    - mopidy >=0.18
    - pykka >=1.1

test:
  # Python imports
  imports:
    - mopidy_vkontakte

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/sibuser/mopidy-vkontakte
  license: Apache Software License
  summary: u'Mopidy extension for VKontakte allows         to listen to music from VKontakte social network.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
