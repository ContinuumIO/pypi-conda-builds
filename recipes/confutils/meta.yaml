package:
  name: confutils
  version: "0.3.7"

source:
  fn: confutils-0.3.7.tar.gz
  url: https://pypi.python.org/packages/source/c/confutils/confutils-0.3.7.tar.gz
  md5: 67e2641cd09a0b0bbc9e32f77f33289a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - confutils = confutils:main
    #
    # Would create an entry point called confutils that calls confutils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - confutils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/rbarrois/confutils
  license: BSD License
  summary: 'Advanced configuration file utilities.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
