package:
  name: dataview
  version: "1.0.5"

source:
  fn: dataview-1.0.5.zip
  url: https://pypi.python.org/packages/source/d/dataview/dataview-1.0.5.zip
  md5: a3fbe1e9b864e7583df7ed1cd06ee76d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dataview = dataview:main
    #
    # Would create an entry point called dataview that calls dataview.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - dataview

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/damamaty/dataview
  license: MIT License
  summary: 'A module that allows you to create views of your sequences or its slices'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
