package:
  name: gum-audio
  version: "0.8.2"

source:
  fn: gum-audio-0.8.2.tar.gz
  url: https://pypi.python.org/packages/source/g/gum-audio/gum-audio-0.8.2.tar.gz
  md5: dff18e12f89b5f22df9e18c0715912c2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gum-audio = gum-audio:main
    #
    # Would create an entry point called gum-audio that calls gum-audio.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyalsaaudio >=0.6
    - scikits.samplerate

  run:
    - python
    - pyalsaaudio >=0.6
    - scikits.samplerate

test:
  # Python imports
  imports:
    - gum
    - gum.controllers
    - gum.fx
    - gum.lib
    - gum.models
    - gum.views

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/stackp/Gum
  license: BSD License
  summary: 'Gum Audio Editor'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
