package:
  name: mrpython
  version: "0.0.17"

source:
  fn: mrpython-0.0.17.tar.gz
  url: https://pypi.python.org/packages/source/m/mrpython/mrpython-0.0.17.tar.gz
  md5: f262603db8aa267fa1a9ca0b3e061938
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mrpython = mrpython:main
    #
    # Would create an entry point called mrpython that calls mrpython.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - mrpython
    - mrpython.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/petermelias/mrpython
  license: MIT License
  summary: 'Collection of things for Python that dont warrant their own libraries.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
