package:
  name: django-static-root-finder
  version: "0.2.0"

source:
  fn: django-static-root-finder-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-static-root-finder/django-static-root-finder-0.2.0.tar.gz
  md5: 2352dfd924ddc0b972d9371671d52172
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-static-root-finder = django-static-root-finder:main
    #
    # Would create an entry point called django-static-root-finder that calls django-static-root-finder.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3

  run:
    - python
    - django >=1.3

test:
  # Python imports
  imports:
    - django_static_root_finder

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ubuntudesign/django-static-root-finder
  license: UNKNOWN
  summary: 'A static files finder to find files in the STATIC_ROOT directory.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
