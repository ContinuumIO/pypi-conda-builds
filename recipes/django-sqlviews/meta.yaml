package:
  name: django-sqlviews
  version: "1.0"

source:
  fn: django-sqlviews-1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-sqlviews/django-sqlviews-1.0.tar.gz
  md5: 03e448f3c87b07886381cf33d1a07571
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-sqlviews = django-sqlviews:main
    #
    # Would create an entry point called django-sqlviews that calls django-sqlviews.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - sqlviews
    - sqlviews.management
    - sqlviews.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/seveas/django-sqlviews
  license: UNKNOWN
  summary: 'Management command for creating views of multi-table models'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
