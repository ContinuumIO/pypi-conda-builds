package:
  name: buildout.locallib
  version: "0.3.1"

source:
  fn: buildout.locallib-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/b/buildout.locallib/buildout.locallib-0.3.1.tar.gz
  md5: af87673d00fee80eb69bb93b75696be4
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - buildout.locallib = buildout.locallib:main
    #
    # Would create an entry point called buildout.locallib that calls buildout.locallib.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - buildout
    - buildout.locallib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/shimizukawa/buildout.locallib/
  license: Python Software Foundation License
  summary: 'buildout.locallib use egg-packages installed on site-packages folder.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
