package:
  name: avroknife
  version: "1.0.4"

source:
  fn: avroknife-1.0.4.tar.gz
  url: https://pypi.python.org/packages/source/a/avroknife/avroknife-1.0.4.tar.gz
  md5: 383e4ae32ddcfdf8cc4540f45177eb29
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - avroknife = avroknife:main
    #
    # Would create an entry point called avroknife that calls avroknife.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - avro >=1.7.5
    - nose

  run:
    - python
    - avro >=1.7.5
    - nose

test:
  # Python imports
  imports:
    - avroknife
    - avroknife.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/CeON/avroknife
  license: Apache Software License
  summary: 'Utility for browsing and simple manipulation of Avro-based files'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
