package:
  name: playwhe
  version: "0.5"

source:
  fn: playwhe-0.5.tar.gz
  url: https://pypi.python.org/packages/source/p/playwhe/playwhe-0.5.tar.gz
  md5: 42364396284cfddea16421661c2a3581
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - playwhe = playwhe:main
    #
    # Would create an entry point called playwhe that calls playwhe.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - playwhe

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/playwhe
  license: License :: Public Domain
  summary: 'A Python API and script for retrieving and storing Play Whe results.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
