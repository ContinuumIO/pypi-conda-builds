package:
  name: archetypes.recurringdate
  version: "0.2"

source:
  fn: archetypes.recurringdate-0.2.tar.gz
  url: https://pypi.python.org/packages/source/a/archetypes.recurringdate/archetypes.recurringdate-0.2.tar.gz
  md5: fd2ad62219ff924d03a3a58ececdf551
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - archetypes.recurringdate = archetypes.recurringdate:main
    #
    # Would create an entry point called archetypes.recurringdate that calls archetypes.recurringdate.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - python-dateutil
    - p4a.common

  run:
    - python
    - setuptools
    - setuptools
    - python-dateutil
    - p4a.common

test:
  # Python imports
  imports:
    - archetypes
    - archetypes.recurringdate

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://dev.plone.org/archetypes/log/MoreFieldsAndWidgets/archetypes.recurringdate
  license: GNU General Public License (GPL)
  summary: 'Field/Widget to support recurring dates.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
