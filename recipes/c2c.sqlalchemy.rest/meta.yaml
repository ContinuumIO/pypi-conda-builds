package:
  name: c2c.sqlalchemy.rest
  version: "0.3rc3"

source:
  fn: c2c.sqlalchemy.rest-0.3rc3.tar.gz
  url: https://pypi.python.org/packages/source/c/c2c.sqlalchemy.rest/c2c.sqlalchemy.rest-0.3rc3.tar.gz
  md5: 05fd7b3f59b3e73948d2278789198b26
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - c2c.sqlalchemy.rest = c2c.sqlalchemy.rest:main
    #
    # Would create an entry point called c2c.sqlalchemy.rest that calls c2c.sqlalchemy.rest.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyramid
    - sqlalchemy
    - geoalchemy
    - shapely

  run:
    - python
    - pyramid
    - sqlalchemy
    - geoalchemy
    - shapely

test:
  # Python imports
  imports:
    - c2c
    - c2c.sqlalchemy
    - c2c.sqlalchemy.rest

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/sbrunner/c2c.sqlalchemy.rest
  license: BSD License
  summary: 'Add a REST interface to simple SQLAlchemy object'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
