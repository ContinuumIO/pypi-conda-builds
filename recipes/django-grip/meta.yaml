package:
  name: django-grip
  version: "1.6.1"

source:
  fn: django-grip-1.6.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-grip/django-grip-1.6.1.tar.gz
  md5: daf14fa4827fb81fd8b3bc6cc2c99026
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-grip = django-grip:main
    #
    # Would create an entry point called django-grip that calls django-grip.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pubcontrol >=2,<3
    - gripcontrol >=2.3,<3

  run:
    - python
    - pubcontrol >=2,<3
    - gripcontrol >=2.3,<3

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/fanout/django-grip
  license: MIT License
  summary: 'Django GRIP library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
