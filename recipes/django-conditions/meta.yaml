package:
  name: django-conditions
  version: "0.9.0"

source:
  fn: django-conditions-0.9.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-conditions/django-conditions-0.9.0.tar.gz
  md5: 98d5b2029aafb1179aaacab564d5ed1a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-conditions = django-conditions:main
    #
    # Would create an entry point called django-conditions that calls django-conditions.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4.3
    - django-jsonfield >=0.9.13

  run:
    - python
    - django >=1.4.3
    - django-jsonfield >=0.9.13

test:
  # Python imports
  imports:
    - conditions

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/RevolutionTech/django-conditions/
  license: BSD License
  summary: 'A Django app that allows creation of conditional logic in admin.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
