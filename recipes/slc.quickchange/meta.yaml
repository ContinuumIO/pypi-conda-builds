package:
  name: slc.quickchange
  version: "2.0.1"

source:
  fn: slc.quickchange-2.0.1.tar.gz
  url: https://pypi.python.org/packages/source/s/slc.quickchange/slc.quickchange-2.0.1.tar.gz
  md5: e12438346ac8d21295c45e94e4c7f94f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - slc.quickchange = slc.quickchange:main
    #
    # Would create an entry point called slc.quickchange that calls slc.quickchange.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.linguaplone

  run:
    - python
    - setuptools
    - setuptools
    - products.linguaplone

test:
  # Python imports
  imports:
    - slc
    - slc.quickchange
    - slc.quickchange.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://svn.syslab.com/svn/syslabcom/slc.quickchange/
  license: GNU General Public License (GPL) or European Union Public Licence 1.1 (EUPL 1.1)
  summary: 'A tool that adds Search and Replace functionality for the content of your objects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
