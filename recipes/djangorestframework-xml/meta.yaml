package:
  name: djangorestframework-xml
  version: "1.0.1"

source:
  fn: djangorestframework-xml-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/djangorestframework-xml/djangorestframework-xml-1.0.1.tar.gz
  md5: 0e7fb7d907e139148944953a19bf7969
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangorestframework-xml = djangorestframework-xml:main
    #
    # Would create an entry point called djangorestframework-xml that calls djangorestframework-xml.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - defusedxml >=0.3

  run:
    - python
    - defusedxml >=0.3

test:
  # Python imports
  imports:
    - rest_framework_xml

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jpadilla/django-rest-framework-xml
  license: BSD License
  summary: 'XML support for Django REST Framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
