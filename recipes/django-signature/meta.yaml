package:
  name: django-signature
  version: "0.3.1"

source:
  fn: django-signature-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-signature/django-signature-0.3.1.tar.gz
  md5: 371ddbeb9e9a21b4e80a1403216408fd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-signature = django-signature:main
    #
    # Would create an entry point called django-signature that calls django-signature.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - signature

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/bearstech/django-signature
  license: GPL v3
  summary: 'Django application to generate and sign Models.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
