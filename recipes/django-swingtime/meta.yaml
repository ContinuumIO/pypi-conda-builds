package:
  name: django-swingtime
  version: "0.6"

source:
  fn: django-swingtime-0.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-swingtime/django-swingtime-0.6.tar.gz
  md5: af7746991c9eb20114451680c311bc17
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-swingtime = django-swingtime:main
    #
    # Would create an entry point called django-swingtime that calls django-swingtime.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - swingtime
    - swingtime.conf

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dakrauth/django-swingtime
  license: MIT License
  summary: 'A Django calendaring application.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
