package:
  name: collective.blog.feeds
  version: "2.1"

source:
  fn: collective.blog.feeds-2.1.zip
  url: https://pypi.python.org/packages/source/c/collective.blog.feeds/collective.blog.feeds-2.1.zip
  md5: 249b27cc8550023d49114a10ea6a572a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.blog.feeds = collective.blog.feeds:main
    #
    # Would create an entry point called collective.blog.feeds that calls collective.blog.feeds.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.fatsyndication
    - plone

  run:
    - python
    - setuptools
    - setuptools
    - products.fatsyndication
    - plone

test:
  # Python imports
  imports:
    - collective
    - collective.blog
    - collective.blog.feeds

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/collective.blog.feeds
  license: GPL
  summary: 'Provides several syndication feed types for standard folders.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
