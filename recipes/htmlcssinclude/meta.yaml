package:
  name: htmlcssinclude
  version: "0.1.2"

source:
  fn: HtmlCssInclude-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/H/HtmlCssInclude/HtmlCssInclude-0.1.2.tar.gz
  md5: 0b4438bf62ae66842a514dcff67c0cd7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - htmlcssinclude = htmlcssinclude:main
    #
    # Would create an entry point called htmlcssinclude that calls htmlcssinclude.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - htmlcssinclude

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Python Software Foundation License
  summary: 'HtmlCssInclude convert css to inline style for mobile'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
