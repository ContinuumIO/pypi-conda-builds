package:
  name: ax_handoff
  version: "1.1.3"

source:
  fn: Ax_Handoff-1.1.3.tar.gz
  url: https://pypi.python.org/packages/source/A/Ax_Handoff/Ax_Handoff-1.1.3.tar.gz
  md5: 25f1685f172cd49776d8e3c96aed9cdb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ax_handoff = ax_handoff:main
    #
    # Would create an entry point called ax_handoff that calls ax_handoff.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - ax_handoff
    - axonchisel
    - axonchisel.handoff
    - axonchisel.handoff.protocol

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/dkamins/ax_handoff/
  license: MIT License
  summary: 'Easy secure protocol for passing encrypted structured data over unencrypted channels (such as URLs) while maintaining tamper-proof integrity.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
