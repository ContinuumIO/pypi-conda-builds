package:
  name: frasco-sentry
  version: "0.1"

source:
  fn: frasco-sentry-0.1.tar.gz
  url: https://pypi.python.org/packages/source/f/frasco-sentry/frasco-sentry-0.1.tar.gz
  md5: 94f36ff513645548285f544fc0f60ce3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - frasco-sentry = frasco-sentry:main
    #
    # Would create an entry point called frasco-sentry that calls frasco-sentry.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - frasco
    - raven >=5.0.0

  run:
    - python
    - frasco
    - raven >=5.0.0

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/frascoweb/frasco-sentry
  license: MIT
  summary: 'Sentry integration for Frasco'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
