package:
  name: jsonrpc_wrapper
  version: "0.1"

source:
  fn: jsonrpc_wrapper-0.1.tar.gz
  url: https://pypi.python.org/packages/source/j/jsonrpc_wrapper/jsonrpc_wrapper-0.1.tar.gz
  md5: 5532f3824e65ad7374d5655ca78a734c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jsonrpc_wrapper = jsonrpc_wrapper:main
    #
    # Would create an entry point called jsonrpc_wrapper that calls jsonrpc_wrapper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - jsonrpc_wrapper

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/abraxas/jsonrpc_wrapper
  license: UNKNOWN
  summary: 'A simple library to create a json_rpc interface around classes *without* mandating protocol or controlling flow.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
