package:
  name: fortune3py
  version: "2.0.1"

source:
  fn: fortune3py-2.0.1.zip
  url: https://pypi.python.org/packages/source/f/fortune3py/fortune3py-2.0.1.zip
  md5: 7bd242fbac1cabaad29ccfa90ddf2329
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fortune3py = fortune3py:main
    #
    # Would create an entry point called fortune3py that calls fortune3py.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - fortune3py

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jbahuleyan/fortune3py
  license: Apache Software License
  summary: 'Fortune cookies for Python3'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
