package:
  name: imhotep_pylint
  version: "0.0.4"

source:
  fn: imhotep_pylint-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/i/imhotep_pylint/imhotep_pylint-0.0.4.tar.gz
  md5: 445fb04592c54367b26d32fc9ddf6316
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - imhotep_pylint = imhotep_pylint:main
    #
    # Would create an entry point called imhotep_pylint that calls imhotep_pylint.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - imhotep_pylint

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/justinabrahms/imhotep_pylint
  license: MIT
  summary: 'An imhotep plugin for pylint validation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
