package:
  name: fileconfig
  version: "0.5.2"

source:
  fn: fileconfig-0.5.2.zip
  url: https://pypi.python.org/packages/source/f/fileconfig/fileconfig-0.5.2.zip
  md5: 6da53ccc644fa512dc4c26e7ae38f3a8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fileconfig = fileconfig:main
    #
    # Would create an entry point called fileconfig that calls fileconfig.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - fileconfig

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/xflr6/fileconfig
  license: MIT License
  summary: 'Config file sections as objects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
