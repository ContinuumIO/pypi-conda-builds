package:
  name: ilrt.formalworkflow
  version: "1.9"

source:
  fn: ilrt.formalworkflow-1.9.tar.gz
  url: https://pypi.python.org/packages/source/i/ilrt.formalworkflow/ilrt.formalworkflow-1.9.tar.gz
  md5: d41ba9a69cff7f4f5038d1db73a0bba4
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ilrt.formalworkflow = ilrt.formalworkflow:main
    #
    # Would create an entry point called ilrt.formalworkflow that calls ilrt.formalworkflow.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.iterate

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.iterate

test:
  # Python imports
  imports:
    - ilrt
    - ilrt.formalworkflow
    - ilrt.formalworkflow.browser
    - ilrt.formalworkflow.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/edcrewe/ilrt.formalworkflow
  license: BSD License
  summary: 'Formal workflow is designed to prevent editing, deletion or reversion of published content from skipping review'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
