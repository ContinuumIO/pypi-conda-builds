package:
  name: js.jquery_spin
  version: "1.0"

source:
  fn: js.jquery_spin-1.0.zip
  url: https://pypi.python.org/packages/source/j/js.jquery_spin/js.jquery_spin-1.0.zip
  md5: 0fd8741112fcf9226be19a2c0aabf999
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - js.jquery_spin = js.jquery_spin:main
    #
    # Would create an entry point called js.jquery_spin that calls js.jquery_spin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fanstatic
    - js.spin
    - setuptools

  run:
    - python
    - setuptools
    - fanstatic
    - js.spin
    - setuptools

test:
  # Python imports
  imports:
    - js
    - js.jquery_spin

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD
  summary: 'Fanstatic packaging of jquery.spin'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
