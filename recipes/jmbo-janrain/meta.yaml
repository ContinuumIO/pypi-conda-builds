package:
  name: jmbo-janrain
  version: "0.1"

source:
  fn: jmbo-janrain-0.1.tar.gz
  url: https://pypi.python.org/packages/source/j/jmbo-janrain/jmbo-janrain-0.1.tar.gz
  md5: dae19ba95a48568054515f2540b76f9f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jmbo-janrain = jmbo-janrain:main
    #
    # Would create an entry point called jmbo-janrain that calls jmbo-janrain.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - jmbo-foundry >=0.7
    - requests >=1.1.0

  run:
    - python
    - jmbo-foundry >=0.7
    - requests >=1.1.0

test:
  # Python imports
  imports:
    - janrain
    - janrain.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/praekelt/jmbo-janrain
  license: BSD License
  summary: 'Jmbo Janrain integration.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
