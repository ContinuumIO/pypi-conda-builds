package:
  name: twisted
  version: "15.2.0"

source:
  fn: Twisted-15.2.0.tar.bz2
  url: https://pypi.python.org/packages/source/T/Twisted/Twisted-15.2.0.tar.bz2
  md5: 84a4087401f6fa1dc022025139db1fec
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - twisted = twisted:main
    #
    # Would create an entry point called twisted that calls twisted.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.interface >=3.6.0

  run:
    - python
    - zope.interface >=3.6.0

test:
  # Python imports
  imports:
    - twisted
    - twisted.application
    - twisted.application.test
    - twisted.conch
    - twisted.conch.client
    - twisted.conch.insults
    - twisted.conch.openssh_compat
    - twisted.conch.scripts
    - twisted.conch.ssh
    - twisted.conch.test
    - twisted.conch.ui
    - twisted.cred
    - twisted.cred.test
    - twisted.enterprise
    - twisted.internet
    - twisted.internet.iocpreactor
    - twisted.internet.test
    - twisted.logger
    - twisted.logger.test
    - twisted.lore
    - twisted.lore.scripts
    - twisted.lore.test
    - twisted.mail
    - twisted.mail.scripts
    - twisted.mail.test
    - twisted.manhole
    - twisted.manhole.test
    - twisted.manhole.ui
    - twisted.manhole.ui.test
    - twisted.names
    - twisted.names.test
    - twisted.news
    - twisted.news.test
    - twisted.pair
    - twisted.pair.test
    - twisted.persisted
    - twisted.persisted.test
    - twisted.plugins
    - twisted.positioning
    - twisted.positioning.test
    - twisted.protocols
    - twisted.protocols.gps
    - twisted.protocols.mice
    - twisted.protocols.test
    - twisted.python
    - twisted.python.test
    - twisted.runner
    - twisted.runner.test
    - twisted.scripts
    - twisted.scripts.test
    - twisted.spread
    - twisted.spread.ui
    - twisted.tap
    - twisted.test
    - twisted.trial
    - twisted.trial._dist
    - twisted.trial._dist.test
    - twisted.trial.test
    - twisted.web
    - twisted.web._auth
    - twisted.web.test
    - twisted.words
    - twisted.words.im
    - twisted.words.protocols
    - twisted.words.protocols.jabber
    - twisted.words.test
    - twisted.words.xish

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://twistedmatrix.com/
  license: MIT
  summary: 'An asynchronous networking framework written in Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
