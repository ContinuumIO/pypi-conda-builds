package:
  name: betfairng
  version: "0.1.6"

source:
  fn: betfairng-0.1.6.tar.gz
  url: https://pypi.python.org/packages/source/b/betfairng/betfairng-0.1.6.tar.gz
  md5: 13cd79c0c5362a474ca7b59a32c8da37
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - betfairng = betfairng:main
    #
    # Would create an entry point called betfairng that calls betfairng.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - betfairng

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.tagwager.com/
  license: BSD License
  summary: 'Client library for Betfair API Next-Generation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
