package:
  name: canari
  version: "1.1"

source:
  fn: canari-1.1.tar.gz
  url: https://pypi.python.org/packages/source/c/canari/canari-1.1.tar.gz
  md5: d535ff12b818d9c9502b49999518740d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - canari = canari:main
    #
    # Would create an entry point called canari that calls canari.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - argparse
    - flask
    - twisted
    - safedexml

  run:
    - python
    - argparse
    - flask
    - twisted
    - safedexml

test:
  # Python imports
  imports:
    - canari
    - canari.commands
    - canari.maltego
    - canari.pkgutils
    - canari.resources
    - canari.resources.etc
    - canari.resources.template
    - canari.tds
    - canari.utils
    - canari.xmltools

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: GPL
  summary: 'Rapid transform development and transform execution framework for Maltego.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
