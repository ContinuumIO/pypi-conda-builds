package:
  name: gamereportcommon
  version: "1.2"

source:
  fn: GameReportCommon-1.2.zip
  url: https://pypi.python.org/packages/source/G/GameReportCommon/GameReportCommon-1.2.zip
  md5: 4ad7b2ab91d0eaba09a6bf75902022ab
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gamereportcommon = gamereportcommon:main
    #
    # Would create an entry point called gamereportcommon that calls gamereportcommon.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - GameReportCommon
    - gamereportcommon

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/GameReportCommon/
  license: LICENSE.txt
  summary: 'Common library for GameReport project'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
