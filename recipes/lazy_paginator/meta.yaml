package:
  name: lazy_paginator
  version: "0.1"

source:
  fn: lazy_paginator-0.1.tar.gz
  url: https://pypi.python.org/packages/source/l/lazy_paginator/lazy_paginator-0.1.tar.gz
  md5: 0f5aab7feaf76008e8d683ce4da005a2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lazy_paginator = lazy_paginator:main
    #
    # Would create an entry point called lazy_paginator that calls lazy_paginator.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - lazy_paginator

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'Lazy Paginator for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
