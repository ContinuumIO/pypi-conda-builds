package:
  name: django-alphafilter
  version: "0.8"

source:
  fn: django-alphafilter-0.8.tar.gz
  url: https://pypi.python.org/packages/source/d/django-alphafilter/django-alphafilter-0.8.tar.gz
  md5: 6a962f9ac9b2c782c51007771cd2c54c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-alphafilter = django-alphafilter:main
    #
    # Would create an entry point called django-alphafilter that calls django-alphafilter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - alphafilter
    - alphafilter.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/coordt/django-alphabetfilter
  license: Apache 2.0
  summary: 'Django-AlphaFilter provides an admin widget for alphabetical filtering that works like date_hierarchy and an template tag for use elsewhere.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
