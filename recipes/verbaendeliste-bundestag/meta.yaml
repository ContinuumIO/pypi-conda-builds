package:
  name: verbaendeliste-bundestag
  version: "0.1.0"

source:
  fn: verbaendeliste-bundestag-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/v/verbaendeliste-bundestag/verbaendeliste-bundestag-0.1.0.tar.gz
  md5: c5a8f33a6de4b4718094a42cea20c97e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - verbaendeliste-bundestag = verbaendeliste-bundestag:main
    #
    # Would create an entry point called verbaendeliste-bundestag that calls verbaendeliste-bundestag.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/stefanw/verbaendeliste-bundestag
  license: MIT License
  summary: 'Parse PDF-to-XML converted lobby list of German Bundestag'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
