package:
  name: djangocms-simple-gallery
  version: "1.1.1"

source:
  fn: djangocms-simple-gallery-1.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/djangocms-simple-gallery/djangocms-simple-gallery-1.1.1.tar.gz
  md5: bd2816dc7d7d0c59b0edde64d8b49250
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangocms-simple-gallery = djangocms-simple-gallery:main
    #
    # Would create an entry point called djangocms-simple-gallery that calls djangocms-simple-gallery.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-filer

  run:
    - python
    - django-filer

test:
  # Python imports
  imports:
    - djangocms_simple_gallery
    - djangocms_simple_gallery.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/divio/djangocms-simple-gallery
  license: BSD License
  summary: 'Simple gallery plugin for the django-cms.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
