package:
  name: chrome-webstore-deploy
  version: "0.0.2"

source:
  fn: chrome-webstore-deploy-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/c/chrome-webstore-deploy/chrome-webstore-deploy-0.0.2.tar.gz
  md5: 0006e98b6f573e63449ad33976c76942
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - chrome-webstore-deploy = chrome-webstore-deploy:main
    #
    # Would create an entry point called chrome-webstore-deploy that calls chrome-webstore-deploy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - oauth2client >=1.2
    - httplib2 >=0.9

  run:
    - python
    - oauth2client >=1.2
    - httplib2 >=0.9

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/jonnor/chrome-webstore-deploy/
  license: MIT License
  summary: 'Automate deployment of Chrome extensions/apps to Chrome Web Store.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
