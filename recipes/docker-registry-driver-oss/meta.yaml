package:
  name: docker-registry-driver-oss
  version: "0.1.3"

source:
  fn: docker-registry-driver-oss-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/d/docker-registry-driver-oss/docker-registry-driver-oss-0.1.3.tar.gz
  md5: b14e70924ceac590aabaf501c469b833
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - docker-registry-driver-oss = docker-registry-driver-oss:main
    #
    # Would create an entry point called docker-registry-driver-oss that calls docker-registry-driver-oss.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - docker-registry-core >=2,<3
    - oss ==0.1.3

  run:
    - python
    - docker-registry-core >=2,<3
    - oss ==0.1.3

test:
  # Python imports
  imports:
    - docker_registry
    - docker_registry.drivers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/chris-jin/docker-registry-driver-oss
  license: Apache Software License
  summary: 'Docker registry ali oss driver'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
