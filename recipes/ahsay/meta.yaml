package:
  name: ahsay
  version: "0.2.0"

source:
  fn: ahsay-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/a/ahsay/ahsay-0.2.0.tar.gz
  md5: 32a702eb9da18dddcce3af659db83d30
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ahsay = ahsay:main
    #
    # Would create an entry point called ahsay that calls ahsay.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - docopt ==0.6.2
    - requests ==2.4.1

  run:
    - python
    - docopt ==0.6.2
    - requests ==2.4.1

test:
  # Python imports
  imports:
    - ahsay

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/koddsson/ahsay-python-api
  license: UNKNOWN
  summary: 'A simple requests wrapper for the Ahsay API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
