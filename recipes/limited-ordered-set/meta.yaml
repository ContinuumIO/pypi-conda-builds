package:
  name: limited-ordered-set
  version: "0.1.0"

source:
  fn: limited-ordered-set-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/l/limited-ordered-set/limited-ordered-set-0.1.0.tar.gz
  md5: e059404f55d78f58fd0aa0ffe6e546ec
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - limited-ordered-set = limited-ordered-set:main
    #
    # Would create an entry point called limited-ordered-set that calls limited-ordered-set.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - ordered-set

  run:
    - python
    - ordered-set

test:
  # Python imports
  imports:
    - limited_ordered_set

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/phpdude/limited-ordered-set
  license: BSD License
  summary: 'Limited Ordered Set object. Data type for storing actions history lists.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
