package:
  name: zope.app.authentication
  version: "3.9"

source:
  fn: zope.app.authentication-3.9.tar.gz
  url: https://pypi.python.org/packages/source/z/zope.app.authentication/zope.app.authentication-3.9.tar.gz
  md5: 55881ac16a852d756f39233ec2ee25cc
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zope.app.authentication = zope.app.authentication:main
    #
    # Would create an entry point called zope.app.authentication that calls zope.app.authentication.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zodb3
    - zope.authentication
    - zope.component
    - zope.container
    - zope.dublincore
    - zope.event
    - zope.exceptions
    - zope.formlib >=4.0.2
    - zope.i18n
    - zope.i18nmessageid
    - zope.interface
    - zope.location
    - zope.password >=3.5.1
    - zope.pluggableauth >=1.1
    - zope.schema
    - zope.security
    - zope.traversing
    - zope.app.container
    - zope.app.component

  run:
    - python
    - setuptools
    - zodb3
    - zope.authentication
    - zope.component
    - zope.container
    - zope.dublincore
    - zope.event
    - zope.exceptions
    - zope.formlib >=4.0.2
    - zope.i18n
    - zope.i18nmessageid
    - zope.interface
    - zope.location
    - zope.password >=3.5.1
    - zope.pluggableauth >=1.1
    - zope.schema
    - zope.security
    - zope.traversing
    - zope.app.container
    - zope.app.component

test:
  # Python imports
  imports:
    - zope
    - zope.app
    - zope.app.authentication
    - zope.app.authentication.browser
    - zope.app.authentication.browser.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/zope.app.authentication
  license: Zope Public License
  summary: 'Principals and groups management for the pluggable authentication utility'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
