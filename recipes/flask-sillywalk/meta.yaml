package:
  name: flask-sillywalk
  version: "2.1"

source:
  fn: Flask-Sillywalk-2.1.zip
  url: https://pypi.python.org/packages/source/F/Flask-Sillywalk/Flask-Sillywalk-2.1.zip
  md5: 8e8af1b9281df825b91e4f1c803d96a6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-sillywalk = flask-sillywalk:main
    #
    # Would create an entry point called flask-sillywalk that calls flask-sillywalk.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - pytest-cov
    - coverage

  run:
    - python
    - flask
    - pytest-cov
    - coverage

test:
  # Python imports
  imports:
    - flask_sillywalk
    - flask_sillywalk.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/hobbeswalsh/flask-sillywalk
  license: BSD License
  summary: 'So you want to implement an auto-documenting API?'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
