package:
  name: gandalf-client
  version: "0.5.2"

source:
  fn: gandalf-client-0.5.2.tar.gz
  url: https://pypi.python.org/packages/source/g/gandalf-client/gandalf-client-0.5.2.tar.gz
  md5: 5b6d619130baa1f8a7439b32075528d8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gandalf-client = gandalf-client:main
    #
    # Would create an entry point called gandalf-client that calls gandalf-client.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - gandalf
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rfloriano/gandalf-client
  license: MIT License
  summary: 'an incredible python package'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
