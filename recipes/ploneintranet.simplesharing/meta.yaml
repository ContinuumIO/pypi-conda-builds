package:
  name: ploneintranet.simplesharing
  version: "0.2"

source:
  fn: ploneintranet.simplesharing-0.2.zip
  url: https://pypi.python.org/packages/source/p/ploneintranet.simplesharing/ploneintranet.simplesharing-0.2.zip
  md5: b3fe62a435587678551188caec8a1d35
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ploneintranet.simplesharing = ploneintranet.simplesharing:main
    #
    # Would create an entry point called ploneintranet.simplesharing that calls ploneintranet.simplesharing.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.behavior
    - plone.directives.form
    - zope.schema
    - zope.interface
    - zope.component
    - plone.api >=1.2.1
    - collective.workspace
    - collective.z3cform.chosen

  run:
    - python
    - setuptools
    - setuptools
    - plone.behavior
    - plone.directives.form
    - zope.schema
    - zope.interface
    - zope.component
    - plone.api >=1.2.1
    - collective.workspace
    - collective.z3cform.chosen

test:
  # Python imports
  imports:
    - ploneintranet
    - ploneintranet.simplesharing
    - ploneintranet.simplesharing.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ploneintranet/ploneintranet.simplesharing/
  license: gpl
  summary: 'In-line sharing/collaboration controls whilst editing content'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
