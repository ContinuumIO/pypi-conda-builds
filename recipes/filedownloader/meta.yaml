package:
  name: filedownloader
  version: "0.0.2"

source:
  fn: FileDownloader-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/F/FileDownloader/FileDownloader-0.0.2.tar.gz
  md5: a73fecb8fa71d3aad8e1469a6235903c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - filedownloader = filedownloader:main
    #
    # Would create an entry point called filedownloader that calls filedownloader.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - FileDownloader
    - filedownloader

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/yangsibai/py-FileDownloader
  license: MIT License
  summary: 'download file in multi threads'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
