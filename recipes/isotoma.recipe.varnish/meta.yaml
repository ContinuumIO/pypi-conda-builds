package:
  name: isotoma.recipe.varnish
  version: "3.0.0"

source:
  fn: isotoma.recipe.varnish-3.0.0.zip
  url: https://pypi.python.org/packages/source/i/isotoma.recipe.varnish/isotoma.recipe.varnish-3.0.0.zip
  md5: 95e47d670d6e55e84006242da77b1edd
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - isotoma.recipe.varnish = isotoma.recipe.varnish:main
    #
    # Would create an entry point called isotoma.recipe.varnish that calls isotoma.recipe.varnish.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - cheetah
    - isotoma.recipe.gocaptain

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - cheetah
    - isotoma.recipe.gocaptain

test:
  # Python imports
  imports:
    - isotoma
    - isotoma.recipe
    - isotoma.recipe.varnish

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Apache Software License
  summary: 'Set up varnish and varnish logging'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
