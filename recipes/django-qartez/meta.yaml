package:
  name: django-qartez
  version: "0.6"

source:
  fn: django-qartez-0.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-qartez/django-qartez-0.6.tar.gz
  md5: 7363ecc32365ac9c5baa96801d426a41
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-qartez = django-qartez:main
    #
    # Would create an entry point called django-qartez that calls django-qartez.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six >=1.1.0

  run:
    - python
    - six >=1.1.0

test:
  # Python imports
  imports:
    - qartez
    - qartez.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/barseghyanartur/django-qartez
  license: GNU General Public License v2 (GPLv2) or GNU Lesser General Public License v2 or later (LGPLv2+)
  summary: 'Additional XML sitemap functionality for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
