package:
  name: sales_menu
  version: "0.1.1"

source:
  fn: sales_menu-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/s/sales_menu/sales_menu-0.1.1.tar.gz
  md5: 11e7003d0600cc39f4376837734252f1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sales_menu = sales_menu:main
    #
    # Would create an entry point called sales_menu that calls sales_menu.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - sales_menu

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/s-m-i-t-a/sales_menu
  license: BSD License
  summary: 'The simple menu for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
