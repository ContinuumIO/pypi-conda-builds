package:
  name: hashextender
  version: "1.0.0"

source:
  fn: HashExtender-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/H/HashExtender/HashExtender-1.0.0.tar.gz
  md5: 7309a0a3016643a402301f0c08f764a6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hashextender = hashextender:main
    #
    # Would create an entry point called hashextender that calls hashextender.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - hashextender

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'Module implements hash length extension attack against MD5 algorithm'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
