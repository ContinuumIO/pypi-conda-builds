package:
  name: django-css
  version: "2.3.1"

source:
  fn: django-css-2.3.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-css/django-css-2.3.1.tar.gz
  md5: dd43941221e016dfd4dee79831f1c1a4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-css = django-css:main
    #
    # Would create an entry point called django-css that calls django-css.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - compressor
    - compressor.conf
    - compressor.filters
    - compressor.filters.jsmin
    - compressor.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/dziegler/django-css
  license: BSD License
  summary: 'django-css provides an easy way to use CSS compilers with Django projects, and an automated system for compressing CSS and JavaScript files'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
