package:
  name: django-voting
  version: "0.2"

source:
  fn: django-voting-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-voting/django-voting-0.2.tar.gz
  md5: d4dbc8c0530f127027f6af6e76f91ead
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-voting = django-voting:main
    #
    # Would create an entry point called django-voting that calls django-voting.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - voting
    - voting.migrations
    - voting.templatetags
    - voting.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/jezdez/django-voting/
  license: BSD License
  summary: 'Generic voting application for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
