package:
  name: django-octopus
  version: "0.3"

source:
  fn: django-octopus-0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-octopus/django-octopus-0.3.tar.gz
  md5: 01d05053605ade1f737e0667215dc30c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-octopus = django-octopus:main
    #
    # Would create an entry point called django-octopus that calls django-octopus.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - octopus
    - octopus.static
    - octopus.templatetags
    - octopus.tools
    - tests
    - tests.octopus
    - tests.octopus.static
    - tests.octopus.templatetags
    - tests.octopus.tools
    - tests.test_app
    - tests.test_app.templates
    - tests.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/brmc/django-octopus
  license: MIT License
  summary: 'A simple AJAX pull framework for django, now with fullfeatured demo'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
