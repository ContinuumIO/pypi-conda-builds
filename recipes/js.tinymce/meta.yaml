package:
  name: js.tinymce
  version: "4.1.5"

source:
  fn: js.tinymce-4.1.5.zip
  url: https://pypi.python.org/packages/source/j/js.tinymce/js.tinymce-4.1.5.zip
  md5: 5b3917391b40edde4a81fbbdf8fa7e83
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - js.tinymce = js.tinymce:main
    #
    # Would create an entry point called js.tinymce that calls js.tinymce.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - fanstatic
    - setuptools

  run:
    - python
    - setuptools
    - fanstatic
    - setuptools

test:
  # Python imports
  imports:
    - js
    - js.tinymce

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD
  summary: 'Fanstatic packaging of TinyMCE'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
