package:
  name: svargaext.sqlamigrate
  version: "0.4"

source:
  fn: svargaext.sqlamigrate-0.4.tar.gz
  url: https://pypi.python.org/packages/source/s/svargaext.sqlamigrate/svargaext.sqlamigrate-0.4.tar.gz
  md5: 43b68458250be8b87e6c6e63dbedb123
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - svargaext.sqlamigrate = svargaext.sqlamigrate:main
    #
    # Would create an entry point called svargaext.sqlamigrate that calls svargaext.sqlamigrate.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sqlalchemy-migrate

  run:
    - python
    - sqlalchemy-migrate

test:
  # Python imports
  imports:
    - svargaext.sqlamigrate

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/joes/svargaext.sqlamigrate/
  license: BSD License
  summary: 'SQLAlchemy migrate module for Svarga framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
