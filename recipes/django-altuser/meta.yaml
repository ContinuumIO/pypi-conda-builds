package:
  name: django-altuser
  version: "0.9.5"

source:
  fn: django-altuser-0.9.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-altuser/django-altuser-0.9.5.tar.gz
  md5: 537527040d7952ad8813625487faa5c4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-altuser = django-altuser:main
    #
    # Would create an entry point called django-altuser that calls django-altuser.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - altuser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://v.licheni.net/drc/django-altuser.git
  license: GNU General Public License v3 (GPLv3)
  summary: 'Alternative user models for django >= 1.5, with email field and other features'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
