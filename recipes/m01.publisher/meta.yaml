package:
  name: m01.publisher
  version: "0.5.0"

source:
  fn: m01.publisher-0.5.0.zip
  url: https://pypi.python.org/packages/source/m/m01.publisher/m01.publisher-0.5.0.zip
  md5: 3aa4e7ca8497b8087e40eab4ac8e654c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - m01.publisher = m01.publisher:main
    #
    # Would create an entry point called m01.publisher that calls m01.publisher.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - p01.cgi
    - transaction
    - z3c.jsonrpc
    - zope.app.appsetup
    - zope.app.publication
    - zope.authentication
    - zope.component
    - zope.event
    - zope.interface
    - zope.location
    - zope.publisher
    - zope.security
    - zope.site

  run:
    - python
    - setuptools
    - setuptools
    - p01.cgi
    - transaction
    - z3c.jsonrpc
    - zope.app.appsetup
    - zope.app.publication
    - zope.authentication
    - zope.component
    - zope.event
    - zope.interface
    - zope.location
    - zope.publisher
    - zope.security
    - zope.site

test:
  # Python imports
  imports:
    - m01
    - m01.publisher

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://cheeseshop.python.org/pypi/m01.publisher
  license: Zope Public License
  summary: 'NO ZODB publisher components for Zope3'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
