package:
  name: intbitset
  version: "2.1.1"

source:
  fn: intbitset-2.1.1.tar.gz
  url: https://pypi.python.org/packages/source/i/intbitset/intbitset-2.1.1.tar.gz
  md5: 19edfdfaec9fabdfafe1ee28a1e32d3b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - intbitset = intbitset:main
    #
    # Would create an entry point called intbitset that calls intbitset.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six

  run:
    - python
    - six

test:
  # Python imports
  imports:
    - intbitset

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/inveniosoftware/intbitset/
  license: GNU General Public License v2 (GPLv2)
  summary: 'C-based extension implementing fast integer bit sets'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
