package:
  name: implib2
  version: "0.9.2"

source:
  fn: IMPLib2-0.9.2.tar.gz
  url: https://pypi.python.org/packages/source/I/IMPLib2/IMPLib2-0.9.2.tar.gz
  md5: 1183da4cb4fbaa2cc0d4ecfb46f6bac3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - implib2 = implib2:main
    #
    # Would create an entry point called implib2 that calls implib2.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyserial ==2.6

  run:
    - python
    - pyserial ==2.6

test:
  # Python imports
  imports:
    - implib2

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mhubig/implib2
  license: GNU Lesser General Public License v3 or later (LGPLv3+)
  summary: 'Python implementation of the IMPBUS-2 data transmission protocol.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
