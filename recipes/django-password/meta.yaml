package:
  name: django-password
  version: "3.0"

source:
  fn: django-password-3.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-password/django-password-3.0.tar.gz
  md5: 4747d1f112a9fd92552c401b3566f613
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-password = django-password:main
    #
    # Would create an entry point called django-password that calls django-password.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - django_simple_aes_field
    - south

  run:
    - python
    - django
    - django_simple_aes_field
    - south

test:
  # Python imports
  imports:
    - password
    - password.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/ghickman/django-password
  license: MIT License
  summary: 'A Django application to store communal passwords.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
