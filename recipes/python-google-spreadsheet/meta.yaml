package:
  name: python-google-spreadsheet
  version: "2.0.0"

source:
  fn: python-google-spreadsheet-2.0.0.tar.gz
  url: https://pypi.python.org/packages/source/p/python-google-spreadsheet/python-google-spreadsheet-2.0.0.tar.gz
  md5: 03fffb8ab6ccf4b83bdd34bd57818da7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-google-spreadsheet = python-google-spreadsheet:main
    #
    # Would create an entry point called python-google-spreadsheet that calls python-google-spreadsheet.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - gdata

  run:
    - python
    - gdata

test:
  # Python imports
  imports:
    - google_spreadsheet

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/yoavaviram/python-google-spreadsheet
  license: BSD License
  summary: 'A simple Python wrapper for the Google Spreadsheets API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
