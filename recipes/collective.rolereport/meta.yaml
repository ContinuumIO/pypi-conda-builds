package:
  name: collective.rolereport
  version: "1.1"

source:
  fn: collective.rolereport-1.1.zip
  url: https://pypi.python.org/packages/source/c/collective.rolereport/collective.rolereport-1.1.zip
  md5: 363964cb9a7820f142b25a89e3c510e4
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.rolereport = collective.rolereport:main
    #
    # Would create an entry point called collective.rolereport that calls collective.rolereport.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.rolereport

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.colliberty.com/
  license: GPL
  summary: 'Creates a report on what users have which roles'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
