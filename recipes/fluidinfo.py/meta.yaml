package:
  name: fluidinfo.py
  version: "1.2.0"

source:
  fn: fluidinfo.py-1.2.0.tar.gz
  url: https://pypi.python.org/packages/source/f/fluidinfo.py/fluidinfo.py-1.2.0.tar.gz
  md5: 90d965b2fcd24306648c7e145caaf218
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fluidinfo.py = fluidinfo.py:main
    #
    # Would create an entry point called fluidinfo.py that calls fluidinfo.py.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://fluidinfo.com
  license: MIT License
  summary: "A thin wrapper for Fluidinfo's RESTful API"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
