package:
  name: linotp
  version: "2.7.1.2"

source:
  fn: LinOTP-2.7.1.2.tar.gz
  url: https://pypi.python.org/packages/source/L/LinOTP/LinOTP-2.7.1.2.tar.gz
  md5: 60700a5f3408d73959921d58b98b3de6
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - linotp = linotp:main
    #
    # Would create an entry point called linotp that calls linotp.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pylons >=0.9.7,<=1.0
    - webob <1.4
    - sqlalchemy >=0.6
    - docutils >=0.4
    - simplejson >=2.0
    - pycrypto >=1.0
    - repoze.who <=1.1
    - pyrad >=1.1
    - linotpuseridresolver >=2.7.dev
    - netaddr
    - qrcode >=2.4
    - configobj >=4.6.0
    - httplib2
    - sqlalchemy-migrate

  run:
    - python
    - setuptools
    - pylons >=0.9.7,<=1.0
    - webob <1.4
    - sqlalchemy >=0.6
    - docutils >=0.4
    - simplejson >=2.0
    - pycrypto >=1.0
    - repoze.who <=1.1
    - pyrad >=1.1
    - linotpuseridresolver >=2.7.dev
    - netaddr
    - qrcode >=2.4
    - configobj >=4.6.0
    - httplib2
    - sqlalchemy-migrate

test:
  # Python imports
  imports:
    - linotp
    - linotp.config
    - linotp.controllers
    - linotp.lib
    - linotp.lib.ImportOTP
    - linotp.lib.audit
    - linotp.lib.ext
    - linotp.lib.security
    - linotp.lib.tokens
    - linotp.model
    - linotp.tests
    - linotp.tests.functional
    - upgrades

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.linotp.org
  license: GNU Affero General Public License v3
  summary: 'LinOTP Service'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
