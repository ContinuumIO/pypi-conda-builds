package:
  name: rusty-web-framework
  version: "0.1"

source:
  fn: rusty-web-framework-0.1.tar.gz
  url: https://pypi.python.org/packages/source/r/rusty-web-framework/rusty-web-framework-0.1.tar.gz
  md5: 2bf86130e2ecbfbf2b16cc3817dd2d5f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - rusty-web-framework = rusty-web-framework:main
    #
    # Would create an entry point called rusty-web-framework that calls rusty-web-framework.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - rusty

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/lauri.vosandi/rusty-web-framework/
  license: The MIT Licence
  summary: 'Experimental web framework for Python enthusiasts'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
