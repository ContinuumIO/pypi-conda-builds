package:
  name: cexprtk
  version: "0.2.0"

source:
  fn: cexprtk-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/c/cexprtk/cexprtk-0.2.0.tar.gz
  md5: 672c97ff9f468adf309d8bf2cdec9e1a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cexprtk = cexprtk:main
    #
    # Would create an entry point called cexprtk that calls cexprtk.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - cexprtk

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/mjdr/cexprtk
  license: Common Public License
  summary: "Mathematical expression parser: cython wrapper around the 'C++ Mathematical Expression Toolkit Library' "

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
