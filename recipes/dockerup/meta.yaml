package:
  name: dockerup
  version: "1.0.43"

source:
  fn: dockerup-1.0.43.tar.gz
  url: https://pypi.python.org/packages/source/d/dockerup/dockerup-1.0.43.tar.gz
  md5: bc9285662b7a930e0461a05778240b53
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dockerup = dockerup:main
    #
    # Would create an entry point called dockerup that calls dockerup.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - docker-py >=0.7.0

  run:
    - python
    - docker-py >=0.7.0

test:
  # Python imports
  imports:
    - dockerup

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/barchart/dockerup
  license: UNKNOWN
  summary: 'Docker container bootstrapper'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
