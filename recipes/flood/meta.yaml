package:
  name: flood
  version: "0.5"

source:
  fn: flood-0.5.tar.gz
  url: https://pypi.python.org/packages/source/f/flood/flood-0.5.tar.gz
  md5: 4a1b745fae5c2d66d10591473ac8fb8a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flood = flood:main
    #
    # Would create an entry point called flood that calls flood.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - beautifulsoup4 ==4.3.2
    - lxml ==3.2.4
    - requests ==2.1.0

  run:
    - python
    - beautifulsoup4 ==4.3.2
    - lxml ==3.2.4
    - requests ==2.1.0

test:
  # Python imports
  imports:
    - flood
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/DandyDev/flood
  license: WTFPL 2
  summary: 'Python search APIs to various Torrent trackers'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
