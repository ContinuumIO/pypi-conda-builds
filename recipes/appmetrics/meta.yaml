package:
  name: appmetrics
  version: "0.4.2"

source:
  fn: AppMetrics-0.4.2.tar.gz
  url: https://pypi.python.org/packages/source/A/AppMetrics/AppMetrics-0.4.2.tar.gz
  md5: 02b65cb81e662c9aaa3c1327527e1ec9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - appmetrics = appmetrics:main
    #
    # Would create an entry point called appmetrics that calls appmetrics.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - appmetrics
    - appmetrics.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/avalente/appmetrics
  license: Apache Software License
  summary: 'Application metrics collector'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
