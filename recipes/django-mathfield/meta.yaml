package:
  name: django-mathfield
  version: "0.1.0"

source:
  fn: django-mathfield-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-mathfield/django-mathfield-0.1.0.tar.gz
  md5: d5e36379c97f8dd11f4ee47a7dd4344c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-mathfield = django-mathfield:main
    #
    # Would create an entry point called django-mathfield that calls django-mathfield.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - mathfield

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jongoodnow/django-mathfield
  license: BSD License
  summary: 'A Django model field for writing and displaying LaTeX'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
