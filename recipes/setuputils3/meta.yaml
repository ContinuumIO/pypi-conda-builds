package:
  name: setuputils3
  version: "0.9.4"

source:
  fn: setuputils3-0.9.4.tar.gz
  url: https://pypi.python.org/packages/source/s/setuputils3/setuputils3-0.9.4.tar.gz
  md5: b15fc5839a24bbd6e4080bbcf75e81b1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - setuputils3 = setuputils3:main
    #
    # Would create an entry point called setuputils3 that calls setuputils3.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - setuputils3

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/setuputils3
  license: Python Software Foundation License
  summary: 'A utility to automate away boilerplate in Python 3 setup scripts.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
