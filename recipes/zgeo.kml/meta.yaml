package:
  name: zgeo.kml
  version: "0.4"

source:
  fn: zgeo.kml-0.4.tar.gz
  url: https://pypi.python.org/packages/source/z/zgeo.kml/zgeo.kml-0.4.tar.gz
  md5: 582b103f623844156a8d23805d86506d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zgeo.kml = zgeo.kml:main
    #
    # Would create an entry point called zgeo.kml that calls zgeo.kml.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zgeo.geographer >=0.3

  run:
    - python
    - setuptools
    - setuptools
    - zgeo.geographer >=0.3

test:
  # Python imports
  imports:
    - zgeo
    - zgeo.kml

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://trac.gispython.org/projects/PrimaGIS/wiki/zgeo.kml
  license: BSD License
  summary: 'Google Earth KML for Zope'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
