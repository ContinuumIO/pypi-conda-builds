package:
  name: mongobackup
  version: "0.1.2"

source:
  fn: mongobackup-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/m/mongobackup/mongobackup-0.1.2.tar.gz
  md5: 64939634593d586b3e4b323da79c52d9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mongobackup = mongobackup:main
    #
    # Would create an entry point called mongobackup that calls mongobackup.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - boto >=2.25

  run:
    - python
    - boto >=2.25

test:
  # Python imports
  imports:
    - mongobackup

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/zagaran/mongobackup
  license: MIT License
  summary: 'A library to manage mongo backups and restoration from backup'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
