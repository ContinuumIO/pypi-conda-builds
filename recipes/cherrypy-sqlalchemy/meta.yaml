package:
  name: cherrypy-sqlalchemy
  version: "0.5.2"

source:
  fn: CherryPy-SQLAlchemy-0.5.2.tar.gz
  url: https://pypi.python.org/packages/source/C/CherryPy-SQLAlchemy/CherryPy-SQLAlchemy-0.5.2.tar.gz
  md5: 093793b627496f8b0b6a327abb77577e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cherrypy-sqlalchemy = cherrypy-sqlalchemy:main
    #
    # Would create an entry point called cherrypy-sqlalchemy that calls cherrypy-sqlalchemy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cherrypy
    - sqlalchemy

  run:
    - python
    - cherrypy
    - sqlalchemy

test:
  # Python imports
  imports:
    - cp_sqlalchemy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ionrock/cp_sqlalchemy
  license: BSD License
  summary: 'Use SQLAlchemy with CherryPy'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
