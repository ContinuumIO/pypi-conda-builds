package:
  name: plone.recipe.unifiedinstaller
  version: "4.3.1"

source:
  fn: plone.recipe.unifiedinstaller-4.3.1.zip
  url: https://pypi.python.org/packages/source/p/plone.recipe.unifiedinstaller/plone.recipe.unifiedinstaller-4.3.1.zip
  md5: 9c457e7086639f65bf0aafb9659d3e45
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.recipe.unifiedinstaller = plone.recipe.unifiedinstaller:main
    #
    # Would create an entry point called plone.recipe.unifiedinstaller that calls plone.recipe.unifiedinstaller.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - zc.recipe.egg

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - zc.recipe.egg

test:
  # Python imports
  imports:
    - plone
    - plone.recipe
    - plone.recipe.unifiedinstaller

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://svn.plone.org/svn/collective/buildout/plone.recipe.unifiedinstaller/trunk
  license: Zope Public License
  summary: 'ZC Buildout recipe for Plone Unified Installer install finalization'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
