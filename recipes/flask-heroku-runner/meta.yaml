package:
  name: flask-heroku-runner
  version: "2"

source:
  fn: Flask-Heroku-Runner-2.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Heroku-Runner/Flask-Heroku-Runner-2.tar.gz
  md5: 7ea9998d347381d0a82dea7207735753
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-heroku-runner = flask-heroku-runner:main
    #
    # Would create an entry point called flask-heroku-runner that calls flask-heroku-runner.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.8

  run:
    - python
    - flask >=0.8

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/daveshawley/flask-heroku-runner/
  license: BSD License
  summary: 'Minimalist Heroku bootstrap for Flask'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
