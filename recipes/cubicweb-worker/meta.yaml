package:
  name: cubicweb-worker
  version: "3.0.5"

source:
  fn: cubicweb-worker-3.0.5.tar.gz
  url: https://pypi.python.org/packages/source/c/cubicweb-worker/cubicweb-worker-3.0.5.tar.gz
  md5: bf9299994c37d3e76ef2a85cfeb3ab7a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cubicweb-worker = cubicweb-worker:main
    #
    # Would create an entry point called cubicweb-worker that calls cubicweb-worker.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cubicweb >=3.17.0

  run:
    - python
    - cubicweb >=3.17.0

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.cubicweb.org/project/cubicweb-worker
  license: LGPL
  summary: 'asynchronous workers in your instance'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
