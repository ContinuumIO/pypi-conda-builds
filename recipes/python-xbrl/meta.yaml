package:
  name: python-xbrl
  version: "1.1.0"

source:
  fn: python-xbrl-1.1.0.tar.gz
  url: https://pypi.python.org/packages/source/p/python-xbrl/python-xbrl-1.1.0.tar.gz
  md5: 50d697c92f8012ddc7abcd267ee60cb3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-xbrl = python-xbrl:main
    #
    # Would create an entry point called python-xbrl that calls python-xbrl.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pytest
    - pep8
    - marshmallow
    - beautifulsoup4
    - ordereddict
    - lxml
    - six

  run:
    - python
    - pytest
    - pep8
    - marshmallow
    - beautifulsoup4
    - ordereddict
    - lxml
    - six

test:
  # Python imports
  imports:
    - xbrl

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/greedo/python-xbrl/
  license: Apache License
  summary: 'library for parsing xbrl documents'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
