package:
  name: frasco-babel
  version: "0.2.2"

source:
  fn: frasco-babel-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/f/frasco-babel/frasco-babel-0.2.2.tar.gz
  md5: f867ff1c3b74bc47d806459f41e64f84
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - frasco-babel = frasco-babel:main
    #
    # Would create an entry point called frasco-babel that calls frasco-babel.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - frasco
    - flask-babel >=0.9
    - goslate >=1.3.0

  run:
    - python
    - frasco
    - flask-babel >=0.9
    - goslate >=1.3.0

test:
  # Python imports
  imports:
    - frasco_babel

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/frascoweb/frasco-babel
  license: MIT
  summary: 'I18n and L10n support for Frasco'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
