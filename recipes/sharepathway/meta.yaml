package:
  name: sharepathway
  version: "0.5.0"

source:
  fn: sharepathway-0.5.0.zip
  url: https://pypi.python.org/packages/source/s/sharepathway/sharepathway-0.5.0.zip
  md5: d9a7dd0e32922148f62a78560e4b3dfc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sharepathway = sharepathway:main
    #
    # Would create an entry point called sharepathway that calls sharepathway.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy >=1.4.0
    - scipy >=0.9.0

  run:
    - python
    - numpy >=1.4.0
    - scipy >=0.9.0

test:
  # Python imports
  imports:
    - sharepathway

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/GuipengLi/sharepathway
  license: UNKNOWN
  summary: 'A Python package for KEGG pathway enrichment analysis with multiple gene lists'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
