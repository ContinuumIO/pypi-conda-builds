package:
  name: abe-mocks
  version: "0.1.0"

source:
  fn: abe-mocks-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/a/abe-mocks/abe-mocks-0.1.0.tar.gz
  md5: 1ed6d22b72c0815f8a3627d56e6d4c70
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - abe-mocks = abe-mocks:main
    #
    # Would create an entry point called abe-mocks that calls abe-mocks.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - supermutes >=0.2.5

  run:
    - python
    - supermutes >=0.2.5

test:
  # Python imports
  imports:
    - abe

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/apibyexample/abe-python
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Parse ABE files for usage within python tests'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
