package:
  name: django-floppymodelforms
  version: "0.1.0"

source:
  fn: django-floppymodelforms-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-floppymodelforms/django-floppymodelforms-0.1.0.tar.gz
  md5: c8957657567d04cc21ea7ba4caa323d3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-floppymodelforms = django-floppymodelforms:main
    #
    # Would create an entry point called django-floppymodelforms that calls django-floppymodelforms.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - django-floppyforms

  run:
    - python
    - django
    - django-floppyforms

test:
  # Python imports
  imports:
    - floppymodelforms

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/henriquebastos/django-floppymodelforms
  license: BSD License
  summary: "A hack to force Django's ModelForm to use floppyforms' fields."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
