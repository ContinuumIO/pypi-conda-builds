package:
  name: collective.cart.stock
  version: "0.5"

source:
  fn: collective.cart.stock-0.5.zip
  url: https://pypi.python.org/packages/source/c/collective.cart.stock/collective.cart.stock-0.5.zip
  md5: 1e5fd0bb302b2bd27ab35c13006d7b42
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.cart.stock = collective.cart.stock:main
    #
    # Would create an entry point called collective.cart.stock that calls collective.cart.stock.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - plone.app.dexterity
    - setuptools

  run:
    - python
    - setuptools
    - plone.app.dexterity
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.cart
    - collective.cart.stock
    - collective.cart.stock.browser
    - collective.cart.stock.tests
    - collective.cart.stock.tests.dexterity

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.cart.stock/
  license: BSD License
  summary: 'Adds stock content type.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
