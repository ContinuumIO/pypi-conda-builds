package:
  name: ftw.calendar
  version: "2.1.0"

source:
  fn: ftw.calendar-2.1.0.zip
  url: https://pypi.python.org/packages/source/f/ftw.calendar/ftw.calendar-2.1.0.zip
  md5: 7fd0f0a53ff5a6b04113cb1649e220f2
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ftw.calendar = ftw.calendar:main
    #
    # Would create an entry point called ftw.calendar that calls ftw.calendar.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - simplejson

  run:
    - python
    - setuptools
    - setuptools
    - simplejson

test:
  # Python imports
  imports:
    - ftw
    - ftw.calendar
    - ftw.calendar.browser
    - ftw.calendar.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/4teamwork/ftw.calendar
  license: GPL2
  summary: 'Calendar view based on fullcalendar'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
