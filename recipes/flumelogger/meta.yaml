package:
  name: flumelogger
  version: "0.3.3"

source:
  fn: flumelogger-0.3.3.tar.gz
  url: https://pypi.python.org/packages/source/f/flumelogger/flumelogger-0.3.3.tar.gz
  md5: a7ed21358de38cc84f82f9e6fb56176a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flumelogger = flumelogger:main
    #
    # Would create an entry point called flumelogger that calls flumelogger.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - thrift >=0.9.0

  run:
    - python
    - thrift >=0.9.0

test:
  # Python imports
  imports:
    - flumelogger
    - flumelogger.flumeng
    - flumelogger.flumeog

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/lsjostro/flumelogger
  license: BSD License
  summary: 'Flume logging handler for sending log events to Flume'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
