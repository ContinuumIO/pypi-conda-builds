package:
  name: pelican_publications
  version: "0.2.0"

source:
  fn: pelican_publications-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/p/pelican_publications/pelican_publications-0.2.0.tar.gz
  md5: a2c2fe03beb46dca971a5762038e6c79
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pelican_publications = pelican_publications:main
    #
    # Would create an entry point called pelican_publications that calls pelican_publications.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - bibtexparser

  run:
    - python
    - bibtexparser

test:
  # Python imports
  imports:
    - pelican_publications

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/adamreeve/pelican_publications
  license: MIT License
  summary: 'A Pelican plugin that adds an RST directive for including a BibTeX publication list.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
