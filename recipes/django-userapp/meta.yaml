package:
  name: django-userapp
  version: "0.0.5"

source:
  fn: django-userapp-0.0.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-userapp/django-userapp-0.0.5.tar.gz
  md5: 1768e743741c662add55576cd21ad407
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-userapp = django-userapp:main
    #
    # Would create an entry point called django-userapp that calls django-userapp.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - mock
    - userapp
    - distribute

  run:
    - python
    - mock
    - userapp
    - distribute

test:
  # Python imports
  imports:
    - django_userapp
    - django_userapp.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/imanhodjaev/django-userapp
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Django userapp integration'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
