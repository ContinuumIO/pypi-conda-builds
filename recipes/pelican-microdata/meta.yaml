package:
  name: pelican-microdata
  version: "0.2"

source:
  fn: pelican-microdata-0.2.tar.gz
  url: https://pypi.python.org/packages/source/p/pelican-microdata/pelican-microdata-0.2.tar.gz
  md5: 17758219339b7ee5728b7f72d019b8dc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pelican-microdata = pelican-microdata:main
    #
    # Would create an entry point called pelican-microdata that calls pelican-microdata.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pelican

  run:
    - python
    - pelican

test:
  # Python imports
  imports:
    - microdata

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/noirbizarre/pelican-microdata
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Microdata semantic markups support for Pelican Blog Generator'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
