package:
  name: redis_triplestore
  version: "0.1"

source:
  fn: redis_triplestore-0.1.tar.gz
  url: https://pypi.python.org/packages/source/r/redis_triplestore/redis_triplestore-0.1.tar.gz
  md5: 6e28490a327bba1acd76124e9cc37f6d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - redis_triplestore = redis_triplestore:main
    #
    # Would create an entry point called redis_triplestore that calls redis_triplestore.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - redis_triplestore

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.welldev.org/redis_triplestore/
  license: BSD License
  summary: 'Implements a triplestore using Redis to store relations, in Python.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
