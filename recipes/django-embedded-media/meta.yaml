package:
  name: django-embedded-media
  version: "0.1.0"

source:
  fn: django-embedded-media-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-embedded-media/django-embedded-media-0.1.0.tar.gz
  md5: 039a95b375a2718796c3dfc552f9717d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-embedded-media = django-embedded-media:main
    #
    # Would create an entry point called django-embedded-media that calls django-embedded-media.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - embedded_media

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dmpayton/django-embedded-media
  license: MIT License
  summary: 'Render inline CSS and JS in Django forms media.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
