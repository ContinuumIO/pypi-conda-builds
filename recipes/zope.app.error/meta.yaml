package:
  name: zope.app.error
  version: "3.5.3"

source:
  fn: zope.app.error-3.5.3.tar.gz
  url: https://pypi.python.org/packages/source/z/zope.app.error/zope.app.error-3.5.3.tar.gz
  md5: bab82dd06233e9b5e34e9709e8993ace
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zope.app.error = zope.app.error:main
    #
    # Would create an entry point called zope.app.error that calls zope.app.error.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.component
    - zope.error
    - zope.publisher
    - zope.traversing

  run:
    - python
    - setuptools
    - zope.component
    - zope.error
    - zope.publisher
    - zope.traversing

test:
  # Python imports
  imports:
    - zope
    - zope.app
    - zope.app.error
    - zope.app.error.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/zope.app.error
  license: Zope Public License
  summary: 'Error reporting utility management UI for Zope3'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
