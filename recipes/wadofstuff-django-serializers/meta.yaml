package:
  name: wadofstuff-django-serializers
  version: "1.1.0"

source:
  fn: wadofstuff-django-serializers-1.1.0.tar.gz
  url: http://wadofstuff.googlecode.com/files/wadofstuff-django-serializers-1.1.0.tar.gz
  #md5: 
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - wadofstuff-django-serializers = wadofstuff-django-serializers:main
    #
    # Would create an entry point called wadofstuff-django-serializers that calls wadofstuff-django-serializers.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - wadofstuff
    - wadofstuff.django
    - wadofstuff.django.serializers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/wadofstuff/
  license: BSD License
  summary: 'Extended serializers for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
