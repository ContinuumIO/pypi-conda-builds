package:
  name: django-bento
  version: "1.0.1"

source:
  fn: django-bento-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-bento/django-bento-1.0.1.tar.gz
  md5: 528ff5b39a94ac1acacf6b9be2f376e3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-bento = django-bento:main
    #
    # Would create an entry point called django-bento that calls django-bento.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - bento
    - bento.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/lazybird/django-bento/
  license: Creative Commons Attribution 3.0 Unported
  summary: 'django-bento helps you adding editable text and image content areas on\nyour site.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
