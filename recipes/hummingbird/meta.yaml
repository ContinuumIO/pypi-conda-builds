package:
  name: hummingbird
  version: "0.0.2-r1"

source:
  fn: Hummingbird-0.0.2-r1.tar.gz
  url: https://pypi.python.org/packages/source/H/Hummingbird/Hummingbird-0.0.2-r1.tar.gz
  md5: 5822be96d109a49b4ab45b0c6c2b6cb4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hummingbird = hummingbird:main
    #
    # Would create an entry point called hummingbird that calls hummingbird.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=2.4.0

  run:
    - python
    - requests >=2.4.0

test:
  # Python imports
  imports:
    - hummingbird
    - hummingbird.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/KnightHawk3/hummingbird
  license: MIT License
  summary: 'An API Wrapper for Hummingbird.me'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
