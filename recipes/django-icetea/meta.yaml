package:
  name: django-icetea
  version: "0.5"

source:
  fn: django-icetea-0.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-icetea/django-icetea-0.5.tar.gz
  md5: 9de4f8ebb26b19a10abf23cdb48a7957
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-icetea = django-icetea:main
    #
    # Would create an entry point called django-icetea that calls django-icetea.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django ==1.3.1
    - xlwt

  run:
    - python
    - django ==1.3.1
    - xlwt

test:
  # Python imports
  imports:
    - icetea

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/stargazer/django-icetea
  license: WTFPL
  summary: 'REST API Framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
