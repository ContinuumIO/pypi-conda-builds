package:
  name: maabara
  version: "1.0.0"

source:
  fn: maabara-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/m/maabara/maabara-1.0.0.tar.gz
  md5: 552c39176e7072ddd0c254a9d98db912
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - maabara = maabara:main
    #
    # Would create an entry point called maabara that calls maabara.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy
    - sympy
    - uncertainties
    - scipy

  run:
    - python
    - numpy
    - sympy
    - uncertainties
    - scipy

test:
  # Python imports
  imports:
    - maabara

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dudheit314/maabara
  license: BSD License
  summary: 'Maabara allows you to compute and document error propagation fast and easy through Latex'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
