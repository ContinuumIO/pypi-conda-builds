package:
  name: kayako
  version: "1.1.5"

source:
  fn: kayako-1.1.5.tar.gz
  url: https://pypi.python.org/packages/source/k/kayako/kayako-1.1.5.tar.gz
  md5: 04af6d8a7769bbac403823fd4f3e4fce
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kayako = kayako:main
    #
    # Would create an entry point called kayako that calls kayako.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - lxml

  run:
    - python
    - setuptools
    - lxml

test:
  # Python imports
  imports:
    - kayako
    - kayako.core
    - kayako.objects
    - kayako.objects.ticket
    - kayako.tests
    - kayako.tests.core
    - kayako.tests.object

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Python API Wrapper for Kayako 4.01.240'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
