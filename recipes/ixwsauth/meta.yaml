package:
  name: ixwsauth
  version: "0.1.1"

source:
  fn: IXWSAuth-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/I/IXWSAuth/IXWSAuth-0.1.1.tar.gz
  md5: 2c1c76effee2df09aec95f9069d6eba8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ixwsauth = ixwsauth:main
    #
    # Would create an entry point called ixwsauth that calls ixwsauth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3.0
    - pep8 >=1.0.1
    - pylint >=0.25.1
    - ixdjango >=0.1.1

  run:
    - python
    - django >=1.3.0
    - pep8 >=1.0.1
    - pylint >=0.25.1
    - ixdjango >=0.1.1

test:
  # Python imports
  imports:
    - ixwsauth
    - ixwsauth.test_suite

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/IXWSAuth/
  license: MIT
  summary: 'Authentication libraries for IX web services'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
