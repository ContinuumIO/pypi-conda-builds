package:
  name: ipasn-web
  version: "1.0"

source:
  fn: ipasn-web-1.0.tar.gz
  url: https://pypi.python.org/packages/source/i/ipasn-web/ipasn-web-1.0.tar.gz
  md5: d0566135b7faed79747df66eb5125429
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ipasn-web = ipasn-web:main
    #
    # Would create an entry point called ipasn-web that calls ipasn-web.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - ipasn
    - ipasn.web

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/CIRCL/IP-ASN-history
  license: GNU General Public License v3 (GPLv3)
  summary: 'Library to access the IP ASN History REST API.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
