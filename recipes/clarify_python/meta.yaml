package:
  name: clarify_python
  version: "2.0.0"

source:
  fn: clarify_python-2.0.0.tar.gz
  url: https://pypi.python.org/packages/source/c/clarify_python/clarify_python-2.0.0.tar.gz
  md5: cc9e0f0688f1b96abb683b576728dccf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - clarify_python = clarify_python:main
    #
    # Would create an entry point called clarify_python that calls clarify_python.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - urllib3
    - certifi

  run:
    - python
    - urllib3
    - certifi

test:
  # Python imports
  imports:
    - clarify_python

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Clarify/clarify_python
  license: MIT License
  summary: 'The Clarify Python 3 Helper Library wraps the entire Clarify API in a Python 3.x Client class.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
