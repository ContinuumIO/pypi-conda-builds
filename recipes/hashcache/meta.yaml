package:
  name: hashcache
  version: "0.0.1"

source:
  fn: hashcache-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/h/hashcache/hashcache-0.0.1.tar.gz
  md5: c343dff852eaa3603cc52ea192d244e2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hashcache = hashcache:main
    #
    # Would create an entry point called hashcache that calls hashcache.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - hashcache
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/yola/hashcache
  license: MIT
  summary: 'Wrapper for the django cache api to gracefully handle long or non-ascii keys'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
