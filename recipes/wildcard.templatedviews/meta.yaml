package:
  name: wildcard.templatedviews
  version: "1.0b4"

source:
  fn: wildcard.templatedviews-1.0b4.zip
  url: https://pypi.python.org/packages/source/w/wildcard.templatedviews/wildcard.templatedviews-1.0b4.zip
  md5: 4f0b7421280a11c2a6edcf86fad34bd8
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - wildcard.templatedviews = wildcard.templatedviews:main
    #
    # Would create an entry point called wildcard.templatedviews that calls wildcard.templatedviews.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - wildcard
    - wildcard.templatedviews

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/
  license: GPL
  summary: 'Provide templated views with schema settings associated with them.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
