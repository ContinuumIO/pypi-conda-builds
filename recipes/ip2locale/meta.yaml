package:
  name: ip2locale
  version: "0.1.0"

source:
  fn: ip2locale-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/i/ip2locale/ip2locale-0.1.0.tar.gz
  md5: 1b36a25f5fa3a122de6f57790f8c6260
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ip2locale = ip2locale:main
    #
    # Would create an entry point called ip2locale that calls ip2locale.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - ip2locale
    - ip2locale.Provider
    - ip2locale.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/pengyao/ip2locale
  license: Apache License V2.0
  summary: 'ipv4 to locale'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
