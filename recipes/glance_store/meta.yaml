package:
  name: glance_store
  version: "0.6.0"

source:
  fn: glance_store-0.6.0.tar.gz
  url: https://pypi.python.org/packages/source/g/glance_store/glance_store-0.6.0.tar.gz
  md5: 3bcfa5242bb1cc77c38d20378f31e0e2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - glance_store = glance_store:main
    #
    # Would create an entry point called glance_store that calls glance_store.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

# requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Apache Software License
  summary: 'OpenStack Image Service Store Library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
