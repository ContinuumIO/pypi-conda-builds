package:
  name: zanshin
  version: "0.6"

source:
  fn: zanshin-0.6.tar.gz
  url: https://pypi.python.org/packages/source/z/zanshin/zanshin-0.6.tar.gz
  md5: a189f27e26945741042b4172c89136b4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zanshin = zanshin:main
    #
    # Would create an entry point called zanshin that calls zanshin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - zanshin

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://chandlerproject.org/Projects/ZanshinProject
  license: MIT License
  summary: 'High-level library for HTTP, WebDAV and CalDAV operations'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
