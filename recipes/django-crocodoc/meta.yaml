package:
  name: django-crocodoc
  version: "0.1.5"

source:
  fn: django-crocodoc-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-crocodoc/django-crocodoc-0.1.5.tar.gz
  md5: 82bee3536a397b26bff77639e4b25705
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-crocodoc = django-crocodoc:main
    #
    # Would create an entry point called django-crocodoc that calls django-crocodoc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - crocodoc
    - django-braces
    - django-jsonfield
    - django-uuidfield
    - bunch
    - httpretty

  run:
    - python
    - crocodoc
    - django-braces
    - django-jsonfield
    - django-uuidfield
    - bunch
    - httpretty

test:
  # Python imports
  imports:
    - dj_crocodoc

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rosscdh/django-crocodoc
  license: MIT
  summary: 'Django app for integrating with CrocoDoc'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
