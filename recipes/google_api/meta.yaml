package:
  name: google_api
  version: "0.1.0"

source:
  fn: google_api-0.1.0.zip
  url: https://pypi.python.org/packages/source/g/google_api/google_api-0.1.0.zip
  md5: 3cdaca40d052481cc376b9c6db8e7a7d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - google_api = google_api:main
    #
    # Would create an entry point called google_api that calls google_api.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - appier

  run:
    - python
    - appier

test:
  # Python imports
  imports:
    - google
    - google_api

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://google_api.hive.pt
  license: GNU General Public License (GPL)
  summary: 'Google API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
