package:
  name: django-skebby
  version: "0.3.0"

source:
  fn: django-skebby-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-skebby/django-skebby-0.3.0.tar.gz
  md5: 3f844136408c5c996aeb29d83c3d0476
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-skebby = django-skebby:main
    #
    # Would create an entry point called django-skebby that calls django-skebby.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4
    - requests >=2

  run:
    - python
    - django >=1.4
    - requests >=2

test:
  # Python imports
  imports:
    - django_skebby

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/xrmx/django-skebby
  license: BSD License
  summary: 'A Django app to send sms with skebby'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
