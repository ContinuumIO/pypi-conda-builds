package:
  name: deltacloud_heat
  version: "0.1.0"

source:
  fn: deltacloud_heat-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/deltacloud_heat/deltacloud_heat-0.1.0.tar.gz
  md5: a4c5f3520fc2c5896944e880a31d9a11
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - deltacloud_heat = deltacloud_heat:main
    #
    # Would create an entry point called deltacloud_heat that calls deltacloud_heat.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - deltacloud >=0.9.1
    - python-novaclient

  run:
    - python
    - deltacloud >=0.9.1
    - python-novaclient

test:
  # Python imports
  imports:
    - deltacloud_heat

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tomassedovic/deltacloud_heat
  license: Apache Software License
  summary: 'Deltacloud backend for Heat'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
