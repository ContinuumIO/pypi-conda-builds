package:
  name: git_logparser
  version: "0.1.0"

source:
  fn: git_logparser-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/g/git_logparser/git_logparser-0.1.0.tar.gz
  md5: 8a7374fcec83a97835cb3ef58e770610
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - git_logparser = git_logparser:main
    #
    # Would create an entry point called git_logparser that calls git_logparser.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - git
    - git.test
    - git_logparser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://pypi.python.org/pypi?r6_commit
  license: Be Real
  summary: 'Library to parse Git Logs'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
