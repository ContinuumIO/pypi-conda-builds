package:
  name: silvuple
  version: "1.2"

source:
  fn: silvuple-1.2.zip
  url: https://pypi.python.org/packages/source/s/silvuple/silvuple-1.2.zip
  md5: 652d038eec662d979501306ee845dbbf
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - silvuple = silvuple:main
    #
    # Would create an entry point called silvuple that calls silvuple.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - plone
    - products.linguaplone
    - five.grok
    - plone.app.dexterity
    - plone.app.registry
    - plone.directives.form
    - odict

  run:
    - python
    - setuptools
    - plone
    - products.linguaplone
    - five.grok
    - plone.app.dexterity
    - plone.app.registry
    - plone.directives.form
    - odict

test:
  # Python imports
  imports:
    - silvuple

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://opensourcehacker.com
  license: GPL2
  summary: 'Translation manager for Plone / LinguaPlone websites'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
