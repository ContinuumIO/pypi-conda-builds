package:
  name: flask-babelpkg
  version: "0.9.6"

source:
  fn: Flask-BabelPkg-0.9.6.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-BabelPkg/Flask-BabelPkg-0.9.6.tar.gz
  md5: 6c1a13a2eae770ca2e85178e34d59c78
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-babelpkg = flask-babelpkg:main
    #
    # Would create an entry point called flask-babelpkg that calls flask-babelpkg.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - babel >=1.0
    - speaklater >=1.2
    - jinja2 >=2.5

  run:
    - python
    - flask
    - babel >=1.0
    - speaklater >=1.2
    - jinja2 >=2.5

test:
  # Python imports
  imports:
    - flask_babelpkg

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/dpgaspar/flask-babelPkg
  license: BSD License
  summary: 'Adds i18n/l10n support to Flask applications and extensions'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
