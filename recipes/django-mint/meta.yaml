package:
  name: django-mint
  version: "0.5"

source:
  fn: django-mint-0.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-mint/django-mint-0.5.tar.gz
  md5: 6914f598da1b8945311261aafc236f3a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-mint = django-mint:main
    #
    # Would create an entry point called django-mint that calls django-mint.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - simplejson
    - python-dateutil

  run:
    - python
    - simplejson
    - python-dateutil

test:
  # Python imports
  imports:
    - mint

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/pastapreneur/mint
  license: UNKNOWN
  summary: 'RESTful API for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
