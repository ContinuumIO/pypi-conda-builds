package:
  name: django-jsonapi-org
  version: "0.5.1"

source:
  fn: django-jsonapi-org-0.5.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-jsonapi-org/django-jsonapi-org-0.5.1.tar.gz
  md5: f2ba547f56a98e43e3b7897062f7d63e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-jsonapi-org = django-jsonapi-org:main
    #
    # Would create an entry point called django-jsonapi-org that calls django-jsonapi-org.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - jsonapi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/chibiegg/django-jsonapi-org
  license: MIT License
  summary: 'JSON API implementation for use with django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
