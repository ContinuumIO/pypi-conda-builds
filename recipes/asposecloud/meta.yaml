package:
  name: asposecloud
  version: "1.0.11"

source:
  fn: asposecloud-1.0.11.tar.gz
  url: https://pypi.python.org/packages/source/a/asposecloud/asposecloud-1.0.11.tar.gz
  md5: af1bb5f65acf3e82487d4d605b211380
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - asposecloud = asposecloud:main
    #
    # Would create an entry point called asposecloud that calls asposecloud.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - asposecloud
    - asposecloud.barcode
    - asposecloud.cells
    - asposecloud.email
    - asposecloud.imaging
    - asposecloud.ocr
    - asposecloud.pdf
    - asposecloud.slides
    - asposecloud.tasks
    - asposecloud.words

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/asposeforcloud/Aspose_Cloud_SDK_For_Python
  license: MIT License
  summary: 'Aspose Cloud SDK for Python allows you to use Aspose API in your Python applications'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
