package:
  name: ghdiff_class
  version: "0.1"

source:
  fn: ghdiff_class-0.1.tar.gz
  url: https://pypi.python.org/packages/source/g/ghdiff_class/ghdiff_class-0.1.tar.gz
  md5: 9c73e69c7daf5f2e4f08c4f752a32788
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ghdiff_class = ghdiff_class:main
    #
    # Would create an entry point called ghdiff_class that calls ghdiff_class.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six
    - chardet

  run:
    - python
    - six
    - chardet

test:
  # Python imports
  imports:
    - ghdiff_class

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github/twidi/ghdiff
  license: MIT
  summary: 'Generate pretty Github-style diffs (class version, see ghdiff for origin)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
