package:
  name: python-xen
  version: "0.0.3"

source:
  fn: Python-Xen-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/P/Python-Xen/Python-Xen-0.0.3.tar.gz
  md5: 75b0689146dfbbe493769f15871bf853
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-xen = python-xen:main
    #
    # Would create an entry point called python-xen that calls python-xen.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - pyxen

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.bitbucket.org/tortoiselabs/python-xen
  license: ISC License (ISCL)
  summary: 'Python Xen bindings using bits of libxc and libxs'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
