package:
  name: zope.app.zcmlfiles
  version: "3.8.0"

source:
  fn: zope.app.zcmlfiles-3.8.0.zip
  url: https://pypi.python.org/packages/source/z/zope.app.zcmlfiles/zope.app.zcmlfiles-3.8.0.zip
  md5: 1e15b14d7f9f7925d69d5a3a46eabc30
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zope.app.zcmlfiles = zope.app.zcmlfiles:main
    #
    # Would create an entry point called zope.app.zcmlfiles that calls zope.app.zcmlfiles.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.annotation
    - zope.component
    - zope.componentvocabulary
    - zope.copypastemove
    - zope.dublincore
    - zope.formlib
    - zope.i18n
    - zope.location
    - zope.publisher
    - zope.size
    - zope.traversing
    - zope.app.applicationcontrol
    - zope.app.appsetup
    - zope.app.basicskin
    - zope.app.broken
    - zope.app.component
    - zope.app.container
    - zope.app.content
    - zope.app.dependable
    - zope.app.error
    - zope.app.exception
    - zope.app.folder
    - zope.app.form
    - zope.app.generations
    - zope.app.http
    - zope.app.i18n
    - zope.app.locales >=3.6.0
    - zope.app.pagetemplate
    - zope.app.principalannotation
    - zope.app.publication
    - zope.app.publisher
    - zope.app.rotterdam
    - zope.app.schema
    - zope.app.security
    - zope.app.wsgi

  run:
    - python
    - setuptools
    - zope.annotation
    - zope.component
    - zope.componentvocabulary
    - zope.copypastemove
    - zope.dublincore
    - zope.formlib
    - zope.i18n
    - zope.location
    - zope.publisher
    - zope.size
    - zope.traversing
    - zope.app.applicationcontrol
    - zope.app.appsetup
    - zope.app.basicskin
    - zope.app.broken
    - zope.app.component
    - zope.app.container
    - zope.app.content
    - zope.app.dependable
    - zope.app.error
    - zope.app.exception
    - zope.app.folder
    - zope.app.form
    - zope.app.generations
    - zope.app.http
    - zope.app.i18n
    - zope.app.locales >=3.6.0
    - zope.app.pagetemplate
    - zope.app.principalannotation
    - zope.app.publication
    - zope.app.publisher
    - zope.app.rotterdam
    - zope.app.schema
    - zope.app.security
    - zope.app.wsgi

test:
  # Python imports
  imports:
    - zope
    - zope.app
    - zope.app.zcmlfiles

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/zope.app.zcmlfiles
  license: Zope Public License
  summary: 'Zope application server ZCML files'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
