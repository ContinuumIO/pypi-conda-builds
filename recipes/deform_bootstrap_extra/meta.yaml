package:
  name: deform_bootstrap_extra
  version: "0.2.8"

source:
  fn: deform_bootstrap_extra-0.2.8.tar.gz
  url: https://pypi.python.org/packages/source/d/deform_bootstrap_extra/deform_bootstrap_extra-0.2.8.tar.gz
  md5: 0c97dafd2f8335f41041aff121354d67
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - deform_bootstrap_extra = deform_bootstrap_extra:main
    #
    # Would create an entry point called deform_bootstrap_extra that calls deform_bootstrap_extra.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - nine
    - bag >=0.3.4
    - deform_bootstrap ==0.2.8
    - pyramid_deform

  run:
    - python
    - nine
    - bag >=0.3.4
    - deform_bootstrap ==0.2.8
    - pyramid_deform

test:
  # Python imports
  imports:
    - deform_bootstrap_extra
    - deform_bootstrap_extra.jests
    - deform_bootstrap_extra.pyramid

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/nandoflorestan/deform_bootstrap_extra
  license: BSD License
  summary: 'A complement to deform_bootstrap, which offers Twitter Bootstrap compatible widgets, templates and styles for the deform form library.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
