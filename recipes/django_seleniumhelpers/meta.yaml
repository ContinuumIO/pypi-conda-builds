package:
  name: django_seleniumhelpers
  version: "1.1.1"

source:
  fn: django_seleniumhelpers-1.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django_seleniumhelpers/django_seleniumhelpers-1.1.1.tar.gz
  md5: e0d0914792ce98f51f7385e6946fd006
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_seleniumhelpers = django_seleniumhelpers:main
    #
    # Would create an entry point called django_seleniumhelpers that calls django_seleniumhelpers.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - distribute
    - django
    - selenium

  run:
    - python
    - distribute
    - django
    - selenium

test:
  # Python imports
  imports:
    - django_seleniumhelpers
    - seleniumhelpers
    - seleniumhelpers.management
    - seleniumhelpers.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/espenak/django_seleniumhelpers
  license: BSD
  summary: 'Extends django.test.LiveServerTestCase to make selenium testing even easier.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
