package:
  name: again
  version: "1.1"

source:
  fn: again-1.1.tar.gz
  url: https://pypi.python.org/packages/source/a/again/again-1.1.tar.gz
  md5: d28abdaddb2ae50426b3f5967414f4a5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - again = again:main
    #
    # Would create an entry point called again that calls again.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - again

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kashifrazzaqui/again
  license: UNKNOWN
  summary: 'Python decorators for type and value checking at runtime. Also, some boilerplate code for making classes that support event registration and firing.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
