package:
  name: mozsvc
  version: "0.8"

source:
  fn: mozsvc-0.8.tar.gz
  url: https://pypi.python.org/packages/source/m/mozsvc/mozsvc-0.8.tar.gz
  md5: 8a5f654aaaae4c7ceecc1d06914ad175
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mozsvc = mozsvc:main
    #
    # Would create an entry point called mozsvc that calls mozsvc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyramid >=1.5
    - simplejson
    - konfig

  run:
    - python
    - pyramid >=1.5
    - simplejson
    - konfig

test:
  # Python imports
  imports:
    - mozsvc
    - mozsvc.storage
    - mozsvc.tests
    - mozsvc.user

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mozilla-services/mozservices
  license: UNKNOWN
  summary: 'Various utilities for Mozilla apps'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
