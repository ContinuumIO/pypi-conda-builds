package:
  name: django-partitions
  version: "0.1"

source:
  fn: django-partitions-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-partitions/django-partitions-0.1.tar.gz
  md5: 5a8bab15a7ac0b6dcd862b072219eba6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-partitions = django-partitions:main
    #
    # Would create an entry point called django-partitions that calls django-partitions.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - partitions
    - partitions.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/eldarion/django-partitions
  license: BSD License
  summary: 'an app for partitions querysets based on indexed Q expressions in settings'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
