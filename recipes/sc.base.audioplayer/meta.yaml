package:
  name: sc.base.audioplayer
  version: "1.0dev-r12813"

source:
  fn: sc.base.audioplayer-1.0dev-r12813.tar.gz
  url: https://pypi.python.org/packages/source/s/sc.base.audioplayer/sc.base.audioplayer-1.0dev-r12813.tar.gz
  md5: f44a70da090566984d8c8bd4dc24be20
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sc.base.audioplayer = sc.base.audioplayer:main
    #
    # Would create an entry point called sc.base.audioplayer that calls sc.base.audioplayer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - sc
    - sc.base
    - sc.base.audioplayer
    - sc.base.audioplayer.browser
    - sc.base.audioplayer.doc

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.simplesconsultoria.com.br/
  license: GPL
  summary: 'Skin feita pela Simples Consultoria'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
