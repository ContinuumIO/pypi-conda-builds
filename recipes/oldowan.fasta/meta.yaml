package:
  name: oldowan.fasta
  version: "1.0.2"

source:
  fn: oldowan.fasta-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/o/oldowan.fasta/oldowan.fasta-1.0.2.tar.gz
  md5: e77187389a22a7aae0f97e60ff879101
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - oldowan.fasta = oldowan.fasta:main
    #
    # Would create an entry point called oldowan.fasta that calls oldowan.fasta.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - oldowan
    - oldowan.fasta

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.raaum.org/software/oldowan
  license: MIT License
  summary: 'Read and write FASTA format.\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
