package:
  name: django-predicate
  version: "0.1.1"

source:
  fn: django-predicate-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-predicate/django-predicate-0.1.1.tar.gz
  md5: a27061cd3d739b8496f7777f19a336b9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-predicate = django-predicate:main
    #
    # Would create an entry point called django-predicate that calls django-predicate.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - predicate
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/ptone/django-predicate
  license: BSD License
  summary: 'A predicate class constructed like Django Q objects, used to test whether a new or modified model would match a query'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
