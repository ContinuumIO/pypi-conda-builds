package:
  name: collective.ui.ie6nomore
  version: "1.0"

source:
  fn: collective.ui.ie6nomore-1.0.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.ui.ie6nomore/collective.ui.ie6nomore-1.0.tar.gz
  md5: b458021bb3c4f1b126f27e9480e4c263
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.ui.ie6nomore = collective.ui.ie6nomore:main
    #
    # Would create an entry point called collective.ui.ie6nomore that calls collective.ui.ie6nomore.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.ui
    - collective.ui.ie6nomore
    - collective.ui.ie6nomore.browser
    - collective.ui.ie6nomore.doc

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.simplesconsultoria.com.br/
  license: GPL
  summary: 'Enables a IE6 warning message instructing the user to download a newer (and better) browser.(Based on efforts initiated by http://www.ie6nomore.com/)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
