package:
  name: sqlalchemy_traversal
  version: "0.5.2"

source:
  fn: sqlalchemy_traversal-0.5.2.tar.gz
  url: https://pypi.python.org/packages/source/s/sqlalchemy_traversal/sqlalchemy_traversal-0.5.2.tar.gz
  md5: 6ea47ede36b54010ba3a13e5f4e24529
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sqlalchemy_traversal = sqlalchemy_traversal:main
    #
    # Would create an entry point called sqlalchemy_traversal that calls sqlalchemy_traversal.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyramid
    - sqlalchemy
    - zope.interface
    - colander
    - venusian

  run:
    - python
    - setuptools
    - pyramid
    - sqlalchemy
    - zope.interface
    - colander
    - venusian

test:
  # Python imports
  imports:
    - sqlalchemy_traversal
    - sqlalchemy_traversal.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/eventray/sqlalchemy_traversal
  license: MIT
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
