package:
  name: django-getenv
  version: "1.3.1"

source:
  fn: django-getenv-1.3.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-getenv/django-getenv-1.3.1.tar.gz
  md5: 08d1f0be0c93df9ed0338a40e609d1ad
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-getenv = django-getenv:main
    #
    # Would create an entry point called django-getenv that calls django-getenv.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - getenv

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/schwuk/django-getenv
  license: BSD License
  summary: 'Read settings from environment variables.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
