package:
  name: logentries-cli
  version: "1.2"

source:
  fn: logentries-cli-1.2.tar.gz
  url: https://pypi.python.org/packages/source/l/logentries-cli/logentries-cli-1.2.tar.gz
  md5: bfdd145f3f601f72d2e0f9f521159f0d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - logentries-cli = logentries-cli:main
    #
    # Would create an entry point called logentries-cli that calls logentries-cli.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - parsedatetime >=1.4
    - python-dateutil >=2.4.0
    - requests >=2.5.1

  run:
    - python
    - parsedatetime >=1.4
    - python-dateutil >=2.4.0
    - requests >=2.5.1

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/adamchainz/logentries-cli/
  license: Apache Software License
  summary: 'Get your logs from Logentries on the comandline'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
