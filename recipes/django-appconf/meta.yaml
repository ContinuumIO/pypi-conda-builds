package:
  name: django-appconf
  version: "1.0.1"

source:
  fn: django-appconf-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-appconf/django-appconf-1.0.1.tar.gz
  md5: 29c87a00f0d098b90f3ac6113ae6e52d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-appconf = django-appconf:main
    #
    # Would create an entry point called django-appconf that calls django-appconf.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six

  run:
    - python
    - six

test:
  # Python imports
  imports:
    - appconf

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

# requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://django-appconf.readthedocs.org/
  license: BSD License
  summary: 'A helper class for handling configuration defaults of packaged apps gracefully.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
