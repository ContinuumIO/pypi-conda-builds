package:
  name: coderwall
  version: "1.2.0"

source:
  fn: coderwall-1.2.0.tar.gz
  url: https://pypi.python.org/packages/source/c/coderwall/coderwall-1.2.0.tar.gz
  md5: 34c8a6102ea100304425685eaa5b2395
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - coderwall = coderwall:main
    #
    # Would create an entry point called coderwall that calls coderwall.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - coderwall

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/cwc/coderwall-python
  license: MIT License
  summary: 'A Python module for accessing user data at http://coderwall.com'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
