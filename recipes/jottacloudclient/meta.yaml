package:
  name: jottacloudclient
  version: "0.2.5-1"

source:
  fn: jottacloudclient-0.2.5-1.tar.gz
  url: https://pypi.python.org/packages/source/j/jottacloudclient/jottacloudclient-0.2.5-1.tar.gz
  md5: e1529f5b7c9d5a217a3cfd6e566f1ed7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jottacloudclient = jottacloudclient:main
    #
    # Would create an entry point called jottacloudclient that calls jottacloudclient.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - jottalib >=0.2.10
    - clint

  run:
    - python
    - jottalib >=0.2.10
    - clint

test:
  # Python imports
  imports:
    - jottacloudclient

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://gitorious.org/jottafs/jottacloudclient.git
  license: GNU General Public License v3 (GPLv3)
  summary: 'A cli friendly sync client for JottaCloud'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
