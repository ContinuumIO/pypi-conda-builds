package:
  name: collective.ixedit
  version: "1.0"

source:
  fn: collective.ixedit-1.0.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.ixedit/collective.ixedit-1.0.tar.gz
  md5: 0f549d8692e5e30fdad52a6342f253b7
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.ixedit = collective.ixedit:main
    #
    # Would create an entry point called collective.ixedit that calls collective.ixedit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.ixedit
    - collective.ixedit.browser
    - collective.ixedit.interfaces
    - collective.ixedit.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/collective.ixedit
  license: GPLv3+
  summary: 'IXEdit Javascript Interaction Design Tool'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
