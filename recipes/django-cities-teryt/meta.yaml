package:
  name: django-cities-teryt
  version: "1.0.1"

source:
  fn: django-cities-teryt-1.0.1.zip
  url: https://pypi.python.org/packages/source/d/django-cities-teryt/django-cities-teryt-1.0.1.zip
  md5: 8942f07e7b2531eddfbb219f541ba655
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-cities-teryt = django-cities-teryt:main
    #
    # Would create an entry point called django-cities-teryt that calls django-cities-teryt.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-autoslug
    - lxml
    - unidecode

  run:
    - python
    - django-autoslug
    - lxml
    - unidecode

test:
  # Python imports
  imports:
    - cities_teryt
    - cities_teryt.management
    - cities_teryt.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lukaszbanasiak/django-cities-teryt
  license: MIT License
  summary: 'Polish region and city data for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
