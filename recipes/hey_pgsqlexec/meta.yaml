package:
  name: hey_pgsqlexec
  version: "1.0.2"

source:
  fn: hey_pgsqlexec-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/h/hey_pgsqlexec/hey_pgsqlexec-1.0.2.tar.gz
  md5: 19bc152d33d40f22bd114a8d54788c92
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hey_pgsqlexec = hey_pgsqlexec:main
    #
    # Would create an entry point called hey_pgsqlexec that calls hey_pgsqlexec.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - psycopg2

  run:
    - python
    - psycopg2

test:
  # Python imports
  imports:
    - hey_pgsqlexec

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.pybrid.com/
  license: LICENSE.txt
  summary: 'Hey! PostgreSQL SQL Executable Object'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
