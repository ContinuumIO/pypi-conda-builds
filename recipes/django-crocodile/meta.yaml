package:
  name: django-crocodile
  version: "0.4.1"

source:
  fn: django-crocodile-0.4.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-crocodile/django-crocodile-0.4.1.tar.gz
  md5: bcb42f594b18c90627231e4cbb4b8f9a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-crocodile = django-crocodile:main
    #
    # Would create an entry point called django-crocodile that calls django-crocodile.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cssmin >=0.1.4

  run:
    - python
    - cssmin >=0.1.4

test:
  # Python imports
  imports:
    - crocodile

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/danielquinn/django-crocodile
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'A simple CSS and Javascript aggregator for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
