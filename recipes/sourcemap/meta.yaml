package:
  name: sourcemap
  version: "0.1.8"

source:
  fn: sourcemap-0.1.8.tar.gz
  url: https://pypi.python.org/packages/source/s/sourcemap/sourcemap-0.1.8.tar.gz
  md5: 95463a39246aa88e460d78c0299cff69
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sourcemap = sourcemap:main
    #
    # Would create an entry point called sourcemap that calls sourcemap.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - sourcemap

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mattrobenolt/python-sourcemap
  license: UNKNOWN
  summary: 'Parse JavaScript source maps.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
