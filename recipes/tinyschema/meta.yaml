package:
  name: tinyschema
  version: "0.2.1"

source:
  fn: tinyschema-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/t/tinyschema/tinyschema-0.2.1.tar.gz
  md5: 83cb59ab8a46cd48f7e891307add7169
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tinyschema = tinyschema:main
    #
    # Would create an entry point called tinyschema that calls tinyschema.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - translationstring

  run:
    - python
    - setuptools
    - setuptools
    - translationstring

test:
  # Python imports
  imports:
    - tinyschema
    - tinyschema.renderer
    - tinyschema.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/podhmo/tinyschema
  license: UNKNOWN
  summary: 'tiny schema implementation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
