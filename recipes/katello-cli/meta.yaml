package:
  name: katello-cli
  version: "1.3"

source:
  fn: katello-cli-1.3.tar.gz
  url: https://pypi.python.org/packages/source/k/katello-cli/katello-cli-1.3.tar.gz
  md5: cb6e65cbfc0bbb4fcb80b154d718f8d8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - katello-cli = katello-cli:main
    #
    # Would create an entry point called katello-cli that calls katello-cli.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - katello
    - katello.client
    - katello.client.api
    - katello.client.cli
    - katello.client.core
    - katello.client.lib
    - katello.client.lib.ui
    - katello.client.lib.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: GPL
  summary: "Command line interface for the Katello System's Management Project."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
