package:
  name: django-taggit-live
  version: "0.0.6"

source:
  fn: django-taggit-live-0.0.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-taggit-live/django-taggit-live-0.0.6.tar.gz
  md5: ca1421dfa1653c240772dba9e393e9fa
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-taggit-live = django-taggit-live:main
    #
    # Would create an entry point called django-taggit-live that calls django-taggit-live.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6
    - django-taggit

  run:
    - python
    - django >=1.6
    - django-taggit

test:
  # Python imports
  imports:
    - taggit_live

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/20tab/django-taggit-live
  license: Mit License
  summary: "It's an autocomplete widget for django-taggit TagField"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
