package:
  name: disabledoc
  version: "0.1"

source:
  fn: disabledoc-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/disabledoc/disabledoc-0.1.tar.gz
  md5: 13b770c7b9996aafddf6c7d88a6fa47f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - disabledoc = disabledoc:main
    #
    # Would create an entry point called disabledoc that calls disabledoc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - nose

  run:
    - python
    - setuptools
    - nose

test:
  # Python imports
  imports:
    - disabledoc

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kumar303/disable-docstring
  license: Apache License
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
