package:
  name: django-urli18n
  version: "0.1"

source:
  fn: django-urli18n-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-urli18n/django-urli18n-0.1.tar.gz
  md5: c834f41a006027aa3628d6a2a706c78a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-urli18n = django-urli18n:main
    #
    # Would create an entry point called django-urli18n that calls django-urli18n.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - urli18n
    - urli18n.templatetags
    - urli18n.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Torte/django-urli18n
  license: UNKNOWN
  summary: 'A reusable Django app to display the current activated language in the URL'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
