package:
  name: generateds
  version: "2.15b"

source:
  fn: generateDS-2.15b.tar.gz
  url: https://pypi.python.org/packages/source/g/generateDS/generateDS-2.15b.tar.gz
  md5: 62c300914747cb2ee213171675b4dc17
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - generateds = generateds:main
    #
    # Would create an entry point called generateds that calls generateds.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - generateds
    - libgenerateDS
    - libgenerateDS.gui

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.davekuhlman.org/generateDS.html
  license: http://www.opensource.org/licenses/mit-license.php
  summary: 'Generate Python data structures and XML parser from Xschema'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
