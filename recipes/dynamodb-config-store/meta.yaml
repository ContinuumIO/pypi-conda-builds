package:
  name: dynamodb-config-store
  version: "0.2.2"

source:
  fn: dynamodb-config-store-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/d/dynamodb-config-store/dynamodb-config-store-0.2.2.tar.gz
  md5: 8192f3cb942eec8eaf4ce2d117a27ec4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dynamodb-config-store = dynamodb-config-store:main
    #
    # Would create an entry point called dynamodb-config-store that calls dynamodb-config-store.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - boto >=2.29.1

  run:
    - python
    - boto >=2.29.1

test:
  # Python imports
  imports:
    - dynamodb_config_store

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/sebdah/dynamodb-config-store/
  license: Apache Software License
  summary: 'Store configuration details in DynamoDB'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
