package:
  name: accost
  version: "dev"

source:
  fn: default.tar.gz
  url: https://bitbucket.org/guyingbo/accost/get/default.tar.gz
  #md5: 
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - accost = accost:main
    #
    # Would create an entry point called accost that calls accost.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - werkzeug >=0.8.3
    - jinja2 >=2.4

  run:
    - python
    - werkzeug >=0.8.3
    - jinja2 >=2.4

test:
  # Python imports
  imports:
    - accost
    - accost.log4mongo
    - accost.middlewares
    - accost.utils
    - accost.wsgiapps

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/guyingbo/accost
  license: BSD License
  summary: 'Fast and easy python web framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
