package:
  name: coopr.bilevel
  version: "1.0"

source:
  fn: coopr.bilevel-1.0.tar.gz
  url: https://pypi.python.org/packages/source/c/coopr.bilevel/coopr.bilevel-1.0.tar.gz
  md5: cbe5380320839ac52c0d32d773711ca8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - coopr.bilevel = coopr.bilevel:main
    #
    # Would create an entry point called coopr.bilevel that calls coopr.bilevel.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - coopr
    - coopr.bilevel
    - coopr.bilevel.plugins
    - coopr.bilevel.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://software.sandia.gov/svn/public/coopr/coopr.bilevel
  license: BSD License
  summary: 'A Coopr package for modeling bilevel programs'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
