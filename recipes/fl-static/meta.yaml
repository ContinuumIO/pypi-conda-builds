package:
  name: fl-static
  version: "0.0.2"

source:
  fn: fl-static-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/f/fl-static/fl-static-0.0.2.tar.gz
  md5: 19f1b1049fc06a479f1a88251284014c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fl-static = fl-static:main
    #
    # Would create an entry point called fl-static that calls fl-static.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - static

  run:
    - python
    - flask
    - static

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/marksteve/fl-static
  license: MIT License
  summary: 'Serve production static files with Flask.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
