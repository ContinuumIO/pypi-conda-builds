package:
  name: collective.virtualtreecategories
  version: "0.3.3"

source:
  fn: collective.virtualtreecategories-0.3.3.zip
  url: https://pypi.python.org/packages/source/c/collective.virtualtreecategories/collective.virtualtreecategories-0.3.3.zip
  md5: 42715753c0b06d2bba3f53589b3b64a1
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.virtualtreecategories = collective.virtualtreecategories:main
    #
    # Would create an entry point called collective.virtualtreecategories that calls collective.virtualtreecategories.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - simplejson
    - products.addremovewidget
    - archetypes.schemaextender

  run:
    - python
    - setuptools
    - setuptools
    - simplejson
    - products.addremovewidget
    - archetypes.schemaextender

test:
  # Python imports
  imports:
    - collective
    - collective.virtualtreecategories
    - collective.virtualtreecategories.browser
    - collective.virtualtreecategories.dexterity
    - collective.virtualtreecategories.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/collective.virtualtreecategories
  license: GPL
  summary: 'Virtual tree categories/keywords for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
