package:
  name: delaunator
  version: "1.0.3"

source:
  fn: delaunator-1.0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/delaunator/delaunator-1.0.3.tar.gz
  md5: bdb5de5698937f64ff92dee557ae6fb8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - delaunator = delaunator:main
    #
    # Would create an entry point called delaunator that calls delaunator.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - delaunator
    - delaunator.libdelaunator_src

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Aluriak/Delaunator
  license: GNU General Public License (GPL)
  summary: '2D Delaunay Triangulation in C++ with Python wrapper'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
