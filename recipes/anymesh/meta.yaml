package:
  name: anymesh
  version: "0.4.0"

source:
  fn: anymesh-0.4.0.tar.gz
  url: https://pypi.python.org/packages/source/a/anymesh/anymesh-0.4.0.tar.gz
  md5: f1d94770161c81a3b43b90cadb3aacc0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - anymesh = anymesh:main
    #
    # Would create an entry point called anymesh that calls anymesh.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - twisted

  run:
    - python
    - twisted

test:
  # Python imports
  imports:
    - anymesh

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/AnyMesh/anyMesh-Python.git
  license: MIT
  summary: 'AnyMesh is a multi-platform, decentralized, auto-discovery, auto-connect mesh networking API.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
