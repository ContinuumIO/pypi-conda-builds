package:
  name: conchoctopus
  version: "0.1a1"

source:
  fn: conchoctopus-0.1a1.tar.gz
  url: https://pypi.python.org/packages/source/c/conchoctopus/conchoctopus-0.1a1.tar.gz
  md5: 96566c4c10c1cbcac1b6bd4d2aafaabd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - conchoctopus = conchoctopus:main
    #
    # Would create an entry point called conchoctopus that calls conchoctopus.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - conchoctopus
    - conchoctopus.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/jakamkon/conchoctopus/
  license: MIT License
  summary: 'An environment and API that simplifies workingwith multiple SSH servers in an asynchronous way.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
