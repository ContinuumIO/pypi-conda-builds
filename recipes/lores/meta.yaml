package:
  name: lores
  version: "0.0.5"

source:
  fn: lores-0.0.5.tar.gz
  url: https://pypi.python.org/packages/source/l/lores/lores-0.0.5.tar.gz
  md5: 25e22944fd942e3880e10d7a548fcfe1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lores = lores:main
    #
    # Would create an entry point called lores that calls lores.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - pillow
    - requests
    - vari >=0.0.2

  run:
    - python
    - pillow
    - requests
    - vari >=0.0.2

test:
  # Python imports
  imports:
    - lores

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/philadams/lores
  license: LICENSE.txt
  summary: 'Render image files in the terminal'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
