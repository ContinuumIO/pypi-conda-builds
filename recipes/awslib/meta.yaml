package:
  name: awslib
  version: "0.6"

source:
  fn: awslib-0.6.tar.gz
  url: https://pypi.python.org/packages/source/a/awslib/awslib-0.6.tar.gz
  md5: 6cd16313978b93e02e81e50200a06f31
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - awslib = awslib:main
    #
    # Would create an entry point called awslib that calls awslib.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - boto

  run:
    - python
    - boto

test:
  # Python imports
  imports:
    - awslib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT License
  summary: 'Small library providing Amazon Web Services utility functions on top of boto'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
