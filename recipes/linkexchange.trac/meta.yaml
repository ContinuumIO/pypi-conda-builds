package:
  name: linkexchange.trac
  version: "0.1"

source:
  fn: LinkExchange.Trac-0.1.tar.gz
  url: https://pypi.python.org/packages/source/L/LinkExchange.Trac/LinkExchange.Trac-0.1.tar.gz
  md5: 7435ce169bf29e39ae9f93c0ce5e7285
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - linkexchange.trac = linkexchange.trac:main
    #
    # Would create an entry point called linkexchange.trac that calls linkexchange.trac.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - linkexchange >=0.4dev
    - trac >=0.11

  run:
    - python
    - setuptools
    - linkexchange >=0.4dev
    - trac >=0.11

test:
  # Python imports
  imports:
    - linkexchange
    - linkexchange.trac

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://linkexchange.org.ua
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Trac integration with LinkExchange library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
