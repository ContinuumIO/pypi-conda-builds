package:
  name: githubremote
  version: "0.1.1"

source:
  fn: GithubRemote-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/G/GithubRemote/GithubRemote-0.1.1.tar.gz
  md5: d6e01e1b74a423c07932321d90dc542e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - githubremote = githubremote:main
    #
    # Would create an entry point called githubremote that calls githubremote.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pygithub

  run:
    - python
    - pygithub

test:
  # Python imports
  imports:
    - GithubRemote
    - GithubRemote.Gui
    - githubremote

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/cameronbwhite/GithubRemote
  license: BSD License
  summary: 'Access remote github repos'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
