package:
  name: python-gsmmodem
  version: "0.9"

source:
  fn: python-gsmmodem-0.9.tar.gz
  url: https://pypi.python.org/packages/source/p/python-gsmmodem/python-gsmmodem-0.9.tar.gz
  md5: 1708832d3655451f6e8d8bd86621a2e3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-gsmmodem = python-gsmmodem:main
    #
    # Would create an entry point called python-gsmmodem that calls python-gsmmodem.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyserial >=2.6

  run:
    - python
    - pyserial >=2.6

test:
  # Python imports
  imports:
    - gsmmodem
    - gsmtermlib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/faucamp/python-gsmmodem
  license: GNU Lesser General Public License v3 or later (LGPLv3+)
  summary: 'Control an attached GSM modem: send/receive SMS messages, handle calls, etc'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
