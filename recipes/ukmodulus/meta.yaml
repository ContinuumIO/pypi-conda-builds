package:
  name: ukmodulus
  version: "0.1.1"

source:
  fn: ukmodulus-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/u/ukmodulus/ukmodulus-0.1.1.tar.gz
  md5: e468965819733ef51fc527450984b347
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ukmodulus = ukmodulus:main
    #
    # Would create an entry point called ukmodulus that calls ukmodulus.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - ukmodulus

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bazerk/uk-modulus-checking
  license: UNKNOWN
  summary: 'Implementation of VocaLinks UK Modulus checking for UK account numbers and sort codes'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
