package:
  name: django-achievements
  version: "0.1.1"

source:
  fn: django-achievements-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-achievements/django-achievements-0.1.1.tar.gz
  md5: c259685d29def66c93535aac0403388a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-achievements = django-achievements:main
    #
    # Would create an entry point called django-achievements that calls django-achievements.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django-appconf >=0.4.1

  run:
    - python
    - django-appconf >=0.4.1

test:
  # Python imports
  imports:
    - achievements
    - achievements.migrations
    - achievements.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ssaboum/django-achievements
  license: BSD License
  summary: 'Django application to deal with achievements and rewards unlocked by users'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
