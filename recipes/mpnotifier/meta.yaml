package:
  name: mpnotifier
  version: "1.1"

source:
  fn: MPNotifier-1.1.tar.gz
  url: https://pypi.python.org/packages/source/M/MPNotifier/MPNotifier-1.1.tar.gz
  md5: b7c328602b1004359aae031122dc95ef
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mpnotifier = mpnotifier:main
    #
    # Would create an entry point called mpnotifier that calls mpnotifier.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - mpnotifier

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://chadok.info/mpn
  license: GNU General Public License (GPL)
  summary: 'A lightweigh notifier for MPD'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
