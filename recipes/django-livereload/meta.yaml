package:
  name: django-livereload
  version: "1.2"

source:
  fn: django-livereload-1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-livereload/django-livereload-1.2.tar.gz
  md5: acf476edacde14b42b3f7163892fdd9f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-livereload = django-livereload:main
    #
    # Would create an entry point called django-livereload that calls django-livereload.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - beautifulsoup4 >=4.3.2

  run:
    - python
    - beautifulsoup4 >=4.3.2

test:
  # Python imports
  imports:
    - livereload
    - livereload.management
    - livereload.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Fantomas42/django-livereload
  license: BSD License
  summary: 'LiveReload with the Django development server'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
