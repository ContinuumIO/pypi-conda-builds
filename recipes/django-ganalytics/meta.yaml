package:
  name: django-ganalytics
  version: "0.4"

source:
  fn: django-ganalytics-0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-ganalytics/django-ganalytics-0.4.tar.gz
  md5: a151f17471c04604d543a625b9114f16
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-ganalytics = django-ganalytics:main
    #
    # Would create an entry point called django-ganalytics that calls django-ganalytics.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3.1

  run:
    - python
    - django >=1.3.1

test:
  # Python imports
  imports:
    - ganalytics

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rdegges/django-ganalytics
  license: UNLICENSE
  summary: 'Simple Google Analytics integration for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
