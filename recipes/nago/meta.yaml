package:
  name: nago
  version: "1.7"

source:
  fn: nago-1.7.tar.gz
  url: https://pypi.python.org/packages/source/n/nago/nago-1.7.tar.gz
  md5: 6e4d8f2c72d60b0ba95760d03c5a4a82
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - nago = nago:main
    #
    # Would create an entry point called nago that calls nago.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - nago
    - nago.core
    - nago.extensions
    - nago.protocols
    - nago.protocols.httpserver
    - nago.settings

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/opinkerfi/nago
  license: GPL
  summary: 'General purpose agent, mostly used with nagios'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
