package:
  name: common-python-difio
  version: "1.3.1"

source:
  fn: common-python-difio-1.3.1.tar.gz
  url: https://pypi.python.org/packages/source/c/common-python-difio/common-python-difio-1.3.1.tar.gz
  md5: f4d11bf2e3e71ee558a6a2f65d3b9bcb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - common-python-difio = common-python-difio:main
    #
    # Would create an entry point called common-python-difio that calls common-python-difio.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - pip

  run:
    - python
    - pip

test:
  # Python imports
  imports:
    - common_difio

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/difio/common-difio-python
  license: MIT License
  summary: 'Common module for Difio Python clients'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
