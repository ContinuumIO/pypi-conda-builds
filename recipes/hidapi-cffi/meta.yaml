package:
  name: hidapi-cffi
  version: "0.2.1"

source:
  fn: hidapi-cffi-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/h/hidapi-cffi/hidapi-cffi-0.2.1.tar.gz
  md5: b3e99f7423e950789dc548fb7c5e2ef0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hidapi-cffi = hidapi-cffi:main
    #
    # Would create an entry point called hidapi-cffi that calls hidapi-cffi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/jbaiter/hidapi-cffi.git
  license: BSD
  summary: 'CFFI wrapper for hidapi'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
