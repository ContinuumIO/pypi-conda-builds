package:
  name: zope.app.cache
  version: "3.7.0"

source:
  fn: zope.app.cache-3.7.0.zip
  url: https://pypi.python.org/packages/source/z/zope.app.cache/zope.app.cache-3.7.0.zip
  md5: 8dd74574e869ce236ced0de7e349bb5c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zope.app.cache = zope.app.cache:main
    #
    # Would create an entry point called zope.app.cache that calls zope.app.cache.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zodb3
    - zope.annotation
    - zope.app.form
    - zope.app.pagetemplate
    - zope.component
    - zope.componentvocabulary
    - zope.interface
    - zope.proxy
    - zope.publisher
    - zope.ramcache
    - zope.schema
    - zope.traversing

  run:
    - python
    - setuptools
    - zodb3
    - zope.annotation
    - zope.app.form
    - zope.app.pagetemplate
    - zope.component
    - zope.componentvocabulary
    - zope.interface
    - zope.proxy
    - zope.publisher
    - zope.ramcache
    - zope.schema
    - zope.traversing

test:
  # Python imports
  imports:
    - zope
    - zope.app
    - zope.app.cache
    - zope.app.cache.browser
    - zope.app.cache.interfaces
    - zope.app.cache.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/zope.app.cache
  license: Zope Public License
  summary: 'Zope Caching Framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
