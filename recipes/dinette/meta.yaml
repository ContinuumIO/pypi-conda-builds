package:
  name: dinette
  version: "1.4e"

source:
  fn: dinette-1.4e.tar.gz
  url: https://pypi.python.org/packages/source/d/dinette/dinette-1.4e.tar.gz
  md5: 7e28d4427c8f2c8fc7a532632092f08a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dinette = dinette:main
    #
    # Would create an entry point called dinette that calls dinette.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - dinette
    - dinette.libs
    - dinette.management
    - dinette.management.commands
    - dinette.migrations
    - dinette.templatetags
    - dinette.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.agiliq.com/
  license: GNU General Public License (GPL)
  summary: 'Dinette is a forum application in the spirit of PunBB.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
