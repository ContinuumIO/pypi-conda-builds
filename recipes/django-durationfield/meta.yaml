package:
  name: django-durationfield
  version: "0.5.1"

source:
  fn: django-durationfield-0.5.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-durationfield/django-durationfield-0.5.1.tar.gz
  md5: 4c6af374c4b024cef0e1982cf0b5d364
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-durationfield = django-durationfield:main
    #
    # Would create an entry point called django-durationfield that calls django-durationfield.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - durationfield
    - durationfield.db
    - durationfield.db.models
    - durationfield.db.models.fields
    - durationfield.forms
    - durationfield.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://django-durationfield.readthedocs.org
  license: BSD License
  summary: 'Reusable app for adding a DurationField to Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
