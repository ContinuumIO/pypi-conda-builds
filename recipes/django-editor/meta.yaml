package:
  name: django-editor
  version: "0.1"

source:
  fn: django-editor-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-editor/django-editor-0.1.tar.gz
  md5: 420f2d17168bc3f799e827d120b62f2a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-editor = django-editor:main
    #
    # Would create an entry point called django-editor that calls django-editor.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - editor

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/littlepea/django-editor
  license: BSD
  summary: 'Allows pluggable WYSIWYG editors in django admin without hard dependencies'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
