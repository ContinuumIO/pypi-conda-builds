package:
  name: django-ucamwebauth
  version: "1.3"

source:
  fn: django-ucamwebauth-1.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-ucamwebauth/django-ucamwebauth-1.3.tar.gz
  md5: d43304c5e916e4d9972bf23d695ca528
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-ucamwebauth = django-ucamwebauth:main
    #
    # Would create an entry point called django-ucamwebauth that calls django-ucamwebauth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyopenssl

  run:
    - python
    - pyopenssl

test:
  # Python imports
  imports:
    - ucamwebauth

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://git.csx.cam.ac.uk/x/ucs/raven/django-ucamwebauth.git
  license: MIT License
  summary: 'A Django authentication backend for Ucam-WebAuth a.k.a. Raven'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
