package:
  name: django-gzipping-cache
  version: "0.1.0"

source:
  fn: django-gzipping-cache-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-gzipping-cache/django-gzipping-cache-0.1.0.tar.gz
  md5: b124d264b6d8209aa0abbe751c54c895
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-gzipping-cache = django-gzipping-cache:main
    #
    # Would create an entry point called django-gzipping-cache that calls django-gzipping-cache.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3

  run:
    - python
    - django >=1.3

test:
  # Python imports
  imports:
    - django_gzipping_cache
    - django_gzipping_cache.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mapmyfitness/django-gzipping-cache
  license: BSD License
  summary: 'A wrapper for django caches that gzips cached values'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
