package:
  name: rdbhdb
  version: "0.11.0"

source:
  fn: rdbhdb-0.11.0.zip
  url: https://pypi.python.org/packages/source/r/rdbhdb/rdbhdb-0.11.0.zip
  md5: 3c84b83f5ec6778f94908b177a9b105e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - rdbhdb = rdbhdb:main
    #
    # Would create an entry point called rdbhdb that calls rdbhdb.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - urllib3 >=1.5

  run:
    - python
    - urllib3 >=1.5

test:
  # Python imports
  imports:
    - rdbhdb

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.rdbhost.com/
  license: MIT License
  summary: 'DB API module for accessing hosted databases at www.rdbhost.com'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
