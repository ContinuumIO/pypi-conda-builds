package:
  name: django-pybrowscap
  version: "1.0.0"

source:
  fn: django-pybrowscap-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-pybrowscap/django-pybrowscap-1.0.0.tar.gz
  md5: 07c7ec68ebcc14989b8de397ca78eea6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-pybrowscap = django-pybrowscap:main
    #
    # Would create an entry point called django-pybrowscap that calls django-pybrowscap.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - pybrowscap
    - requests

  run:
    - python
    - django
    - pybrowscap
    - requests

test:
  # Python imports
  imports:
    - django_pybrowscap
    - django_pybrowscap.management
    - django_pybrowscap.management.commands
    - django_pybrowscap.tests
    - django_pybrowscap.tests.management
    - django_pybrowscap.tests.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.codescale.net/en/community#django-pybrowscap
  license: BSD License
  summary: 'django-pybrowscap is django middleware with support for pybrowscap'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
