package:
  name: python-libdrizzle
  version: "0.08.2"

source:
  fn: python-libdrizzle-0.08.2.tar.gz
  url: https://pypi.python.org/packages/source/p/python-libdrizzle/python-libdrizzle-0.08.2.tar.gz
  md5: 796f54e88675f57ae4cfb9a094832977
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-libdrizzle = python-libdrizzle:main
    #
    # Would create an entry point called python-libdrizzle that calls python-libdrizzle.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - drizzle

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://launchpad.net/drizzle-interface
  license: BSD License
  summary: 'Python wrapper of libdrizzle'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
