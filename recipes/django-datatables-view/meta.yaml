package:
  name: django-datatables-view
  version: "1.12.1"

source:
  fn: django-datatables-view-1.12.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-datatables-view/django-datatables-view-1.12.1.tar.gz
  md5: 64c71e833b680b1fef3ff28c961f787f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-datatables-view = django-datatables-view:main
    #
    # Would create an entry point called django-datatables-view that calls django-datatables-view.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - django_datatables_view

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/pigletto/django-datatables-view
  license: BSD License
  summary: 'Django datatables view'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
