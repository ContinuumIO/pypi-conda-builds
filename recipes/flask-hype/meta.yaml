package:
  name: flask-hype
  version: "0.1.4"

source:
  fn: flask-hype-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/f/flask-hype/flask-hype-0.1.4.tar.gz
  md5: d88b45e6f24995d6ce9992c3405a4c46
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-hype = flask-hype:main
    #
    # Would create an entry point called flask-hype that calls flask-hype.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.10,<0.11
    - pilo >=0.4,<0.5

  run:
    - python
    - flask >=0.10,<0.11
    - pilo >=0.4,<0.5

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bninja/flask-hype
  license: BSD License
  summary: 'Flask extension for hype'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
