package:
  name: django-xlink
  version: "0.1.1"

source:
  fn: django-xlink-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-xlink/django-xlink-0.1.1.tar.gz
  md5: a9a3427bc021b3dbc74e0bdfb04d460c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-xlink = django-xlink:main
    #
    # Would create an entry point called django-xlink that calls django-xlink.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - xlink
    - xlink.management
    - xlink.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/justquick/django-xlink
  license: BSD License
  summary: 'Django cross link searches particular sites for links back to your site and stores them'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
