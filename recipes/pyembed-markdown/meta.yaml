package:
  name: pyembed-markdown
  version: "1.1.0"

source:
  fn: pyembed-markdown-1.1.0.tar.gz
  url: https://pypi.python.org/packages/source/p/pyembed-markdown/pyembed-markdown-1.1.0.tar.gz
  md5: ac0ab0e1502d5803695667511625930b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pyembed-markdown = pyembed-markdown:main
    #
    # Would create an entry point called pyembed-markdown that calls pyembed-markdown.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyembed
    - markdown

  run:
    - python
    - pyembed
    - markdown

test:
  # Python imports
  imports:
    - pyembed.markdown

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pyembed.github.io
  license: MIT License
  summary: 'Python Markdown extension for embedding content using OEmbed'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
