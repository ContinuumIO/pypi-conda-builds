package:
  name: duinobot
  version: "0.13-8"

source:
  fn: duinobot-0.13-8.tar.gz
  url: https://pypi.python.org/packages/source/d/duinobot/duinobot-0.13-8.tar.gz
  md5: cff1fa351143c3ce349801aeb83708a8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - duinobot = duinobot:main
    #
    # Would create an entry point called duinobot that calls duinobot.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - duinobot
    - duinobot.pyfirmata

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://lihuen.linti.unlp.edu.ar
  license: UNKNOWN
  summary: 'API en Python para el robot Multiplo N6'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
