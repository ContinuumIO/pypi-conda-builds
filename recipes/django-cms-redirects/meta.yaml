package:
  name: django-cms-redirects
  version: "1.0.3"

source:
  fn: django-cms-redirects-1.0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-cms-redirects/django-cms-redirects-1.0.3.tar.gz
  md5: a06242ef951721245ee5a3739ec0d519
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-cms-redirects = django-cms-redirects:main
    #
    # Would create an entry point called django-cms-redirects that calls django-cms-redirects.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - django-cms

  run:
    - python
    - setuptools
    - django
    - django-cms

test:
  # Python imports
  imports:
    - cms_redirects
    - cms_redirects.migrations
    - redirect_demo

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/salvaorenick/django-cms-redirects
  license: BSD License
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
