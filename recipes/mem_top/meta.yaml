package:
  name: mem_top
  version: "0.1.0"

source:
  fn: mem_top-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/m/mem_top/mem_top-0.1.0.tar.gz
  md5: 18a5af60a4821de5ef6e20e7ca41bf3d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mem_top = mem_top:main
    #
    # Would create an entry point called mem_top that calls mem_top.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - mem_top

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/denis-ryzhkov/mem_top
  license: MIT License
  summary: 'Shows top suspects for memory leaks in your Python program.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
