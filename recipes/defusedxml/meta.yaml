package:
  name: defusedxml
  version: "0.4.1"

source:
  fn: defusedxml-0.4.1.tar.gz
  url: https://pypi.python.org/packages/source/d/defusedxml/defusedxml-0.4.1.tar.gz
  md5: 230a5eff64f878b392478e30376d673a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - defusedxml = defusedxml:main
    #
    # Would create an entry point called defusedxml that calls defusedxml.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - defusedxml

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/tiran/defusedxml
  license: Python Software Foundation License
  summary: 'XML bomb protection for Python stdlib modules'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
