package:
  name: django-cepfacil
  version: "0.0.1"

source:
  fn: django-cepfacil-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-cepfacil/django-cepfacil-0.0.1.tar.gz
  md5: 2329d4b37db1814e91954c062c8727d9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-cepfacil = django-cepfacil:main
    #
    # Would create an entry point called django-cepfacil that calls django-cepfacil.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - cepfacil

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/adonescunha/django-cepfacil/
  license: MIT License
  summary: 'A Django app that provides a wrapper to CEP F\xc3\xa1cil web service.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
