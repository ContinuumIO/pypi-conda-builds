package:
  name: dexcell
  version: "0.4.3.5"

source:
  fn: dexcell-0.4.3.5.tar.gz
  url: https://pypi.python.org/packages/source/d/dexcell/dexcell-0.4.3.5.tar.gz
  md5: 12ae53c259fd727d1413c7b951188fba
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dexcell = dexcell:main
    #
    # Would create an entry point called dexcell that calls dexcell.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - dexcell
    - dexma

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dexma/dexcell-python/
  license: BSD License
  summary: 'DEXCell Energy Manager HTTP/JSON REST API for python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
