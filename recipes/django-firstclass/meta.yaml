package:
  name: django-firstclass
  version: "0.9.1"

source:
  fn: django-firstclass-0.9.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-firstclass/django-firstclass-0.9.1.tar.gz
  md5: e9da9b69847c45d6751df3ed46ce044f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-firstclass = django-firstclass:main
    #
    # Would create an entry point called django-firstclass that calls django-firstclass.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - beautifulsoup ==3.2.1
    - django-extensions ==0.8

  run:
    - python
    - beautifulsoup ==3.2.1
    - django-extensions ==0.8

test:
  # Python imports
  imports:
    - firstclass
    - firstclass.middleware
    - firstclass.middleware.alternative
    - firstclass.middleware.online
    - firstclass.middleware.text

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/amccloud/django-firstclass/
  license: MIT
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
