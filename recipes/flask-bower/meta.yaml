package:
  name: flask-bower
  version: "1.1.1"

source:
  fn: Flask-Bower-1.1.1.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Bower/Flask-Bower-1.1.1.tar.gz
  md5: e55c06aa5d0da2e6183a5cbc9b2fcc04
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-bower = flask-bower:main
    #
    # Would create an entry point called flask-bower that calls flask-bower.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.9

  run:
    - python
    - flask >=0.9

test:
  # Python imports
  imports:
    - flask_bower

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/lobeck/flask-bower
  license: GNU General Public License v2 (GPLv2)
  summary: 'An extension to manage and serve your javascript assets with bower'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
