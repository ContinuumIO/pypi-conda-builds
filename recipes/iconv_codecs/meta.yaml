package:
  name: iconv_codecs
  version: "0.2a1"

source:
  fn: iconv_codecs-0.2a1.tar.gz
  url: https://pypi.python.org/packages/source/i/iconv_codecs/iconv_codecs-0.2a1.tar.gz
  md5: e314a0e209a0deacc43277388eb400b3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - iconv_codecs = iconv_codecs:main
    #
    # Would create an entry point called iconv_codecs that calls iconv_codecs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - iconv_codecs

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pythonlog.wordpress.com/2009/03/03/ajuste-a-agua-conforme-o-foobar/
  license: GNU General Public License (GPL)
  summary: 'Python module to register all supported iconv codecs'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
