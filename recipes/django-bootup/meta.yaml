package:
  name: django-bootup
  version: "1.0.1"

source:
  fn: django-bootup-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-bootup/django-bootup-1.0.1.tar.gz
  md5: eb68f4d494492e89afb19b69a094bdcf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-bootup = django-bootup:main
    #
    # Would create an entry point called django-bootup that calls django-bootup.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - bootup

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/un33k/django-bootup
  license: BSD License
  summary: 'A Django bootup application that helps with quick and secure bootup of your django project.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
