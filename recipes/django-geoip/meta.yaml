package:
  name: django-geoip
  version: "0.5.2"

source:
  fn: django-geoip-0.5.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-geoip/django-geoip-0.5.2.tar.gz
  md5: 38370f9fe3f085f5cb9bdade8b54e064
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-geoip = django-geoip:main
    #
    # Would create an entry point called django-geoip that calls django-geoip.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-appconf >=0.6
    - requests >=1.0.4

  run:
    - python
    - django-appconf >=0.6
    - requests >=1.0.4

test:
  # Python imports
  imports:
    - django_geoip
    - django_geoip.management
    - django_geoip.management.commands
    - django_geoip.migrations
    - django_geoip.settings
    - django_geoip.south_migrations
    - django_geoip.vendor
    - django_geoip.vendor.progressbar

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/futurecolors/django-geoip
  license: MIT License
  summary: 'App to figure out where your visitors are from by their IP address.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
