package:
  name: django-renderit
  version: "1.0.2"

source:
  fn: django-renderit-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-renderit/django-renderit-1.0.2.tar.gz
  md5: 294b7433ca3eed8ab50686348b06a706
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-renderit = django-renderit:main
    #
    # Would create an entry point called django-renderit that calls django-renderit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - renderit
    - renderit.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/jsoa/django-renderit/
  license: Apache Software License
  summary: 'Application that can render any object'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
