package:
  name: tsuru-hcaas
  version: "0.2.3"

source:
  fn: tsuru-hcaas-0.2.3.tar.gz
  url: https://pypi.python.org/packages/source/t/tsuru-hcaas/tsuru-hcaas-0.2.3.tar.gz
  md5: 92fd7ab1af3cbbb06d955497393bdf28
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tsuru-hcaas = tsuru-hcaas:main
    #
    # Would create an entry point called tsuru-hcaas that calls tsuru-hcaas.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask ==0.10.1
    - pyzabbix ==0.6
    - pymongo ==2.6.3

  run:
    - python
    - flask ==0.10.1
    - pyzabbix ==0.6
    - pymongo ==2.6.3

test:
  # Python imports
  imports:
    - healthcheck
    - healthcheck.backends

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'Healthcheck as a service API for Tsuru PaaS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
