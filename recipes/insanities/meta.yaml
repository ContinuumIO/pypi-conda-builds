package:
  name: insanities
  version: "0.3"

source:
  fn: insanities-0.3.tar.gz
  url: https://pypi.python.org/packages/source/i/insanities/insanities-0.3.tar.gz
  md5: ea9b4908b5a31dc841db9670ca57f03c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - insanities = insanities:main
    #
    # Would create an entry point called insanities that calls insanities.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - insanities
    - insanities.forms
    - insanities.templates
    - insanities.templates.jinja2
    - insanities.templates.mint
    - insanities.utils
    - insanities.web

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/riffm/insanities-testing/
  license: MIT
  summary: 'A set of insanities of several geeks to show ourselves our coolness.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
