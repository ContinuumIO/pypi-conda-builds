package:
  name: django-elasticsearch-debug-toolbar
  version: "0.1.15"

source:
  fn: django-elasticsearch-debug-toolbar-0.1.15.tar.gz
  url: https://pypi.python.org/packages/source/d/django-elasticsearch-debug-toolbar/django-elasticsearch-debug-toolbar-0.1.15.tar.gz
  md5: 3d356f23a73883c4393dd35dab504dd7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-elasticsearch-debug-toolbar = django-elasticsearch-debug-toolbar:main
    #
    # Would create an entry point called django-elasticsearch-debug-toolbar that calls django-elasticsearch-debug-toolbar.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - elasticsearch >=1.0
    - django-debug-toolbar >=1.2

  run:
    - python
    - elasticsearch >=1.0
    - django-debug-toolbar >=1.2

test:
  # Python imports
  imports:
    - elastic_panel

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/Benoss/django-elasticsearch-debug-toolbar
  license: MIT License
  summary: 'A Django Debug Toolbar panel for Elasticsearch'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
