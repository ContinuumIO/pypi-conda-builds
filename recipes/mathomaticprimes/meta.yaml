package:
  name: mathomaticprimes
  version: "0.0.1"

source:
  fn: mathomaticprimes-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/m/mathomaticprimes/mathomaticprimes-0.0.1.tar.gz
  md5: ede7a4ad60033b640795bc11463b7d36
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mathomaticprimes = mathomaticprimes:main
    #
    # Would create an entry point called mathomaticprimes that calls mathomaticprimes.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - mathomatic
    - mathomaticprimes

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://gitorious.org/python-service-eggs
  license: GNU Affero General Public License v3
  summary: "Python library and web service interface (as service egg) for Mathomatic's prime number generator."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
