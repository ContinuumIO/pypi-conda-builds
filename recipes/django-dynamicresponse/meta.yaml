package:
  name: django-dynamicresponse
  version: "0.5.0"

source:
  fn: django-dynamicresponse-0.5.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-dynamicresponse/django-dynamicresponse-0.5.0.tar.gz
  md5: b9f670cc2f3434a00e39b6e979ca58de
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-dynamicresponse = django-dynamicresponse:main
    #
    # Would create an entry point called django-dynamicresponse that calls django-dynamicresponse.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - dynamicresponse
    - dynamicresponse.middleware

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/funkbit/django-dynamicresponse
  license: BSD License
  summary: 'Lightweight framework for easily providing REST APIs for web apps built with Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
