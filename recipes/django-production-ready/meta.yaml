package:
  name: django-production-ready
  version: "0.0.3a0"

source:
  fn: django-production-ready-0.0.3a0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-production-ready/django-production-ready-0.0.3a0.tar.gz
  md5: 104e66935a45c962a35d3696865eb6bb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-production-ready = django-production-ready:main
    #
    # Would create an entry point called django-production-ready that calls django-production-ready.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - prodready
    - prodready.management
    - prodready.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/agiliq/django-production-ready
  license: BSD License
  summary: 'A Django app that runs simple tests to check if your app is production ready.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
