package:
  name: collective.behavior.sku
  version: "0.4"

source:
  fn: collective.behavior.sku-0.4.zip
  url: https://pypi.python.org/packages/source/c/collective.behavior.sku/collective.behavior.sku-0.4.zip
  md5: bb7dce45144ee49da8a609df5e7e309d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.behavior.sku = collective.behavior.sku:main
    #
    # Would create an entry point called collective.behavior.sku that calls collective.behavior.sku.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - plone.behavior
    - setuptools

  run:
    - python
    - setuptools
    - plone.behavior
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.behavior
    - collective.behavior.sku
    - collective.behavior.sku.tests
    - collective.behavior.sku.tests.dexterity

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.behavior.sku/
  license: BSD License
  summary: 'Behavior to add sku field.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
