package:
  name: distprocessing
  version: "0.0.9"

source:
  fn: distprocessing-0.0.9.tar.gz
  url: https://pypi.python.org/packages/source/d/distprocessing/distprocessing-0.0.9.tar.gz
  md5: 2df7b0846db1dae36852116557bfd204
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - distprocessing = distprocessing:main
    #
    # Would create an entry point called distprocessing that calls distprocessing.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - distprocessing
    - example

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/thinkingpotato/distprocessing
  license: UNKNOWN
  summary: 'Distprocessing lets you set up a cluster and distribute workload between several workers using multiprocessing approach.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
