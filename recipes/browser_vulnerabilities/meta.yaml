package:
  name: browser_vulnerabilities
  version: "0.1.0"

source:
  fn: browser_vulnerabilities-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/b/browser_vulnerabilities/browser_vulnerabilities-0.1.0.tar.gz
  md5: 1d84c1745dfa60492f1576f782dc82ef
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - browser_vulnerabilities = browser_vulnerabilities:main
    #
    # Would create an entry point called browser_vulnerabilities that calls browser_vulnerabilities.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - httpagentparser >=1.6.0

  run:
    - python
    - httpagentparser >=1.6.0

test:
  # Python imports
  imports:
    - browser_vulnerabilities

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ojarva/browser-vulnerabilities
  license: MIT License
  summary: 'Library for detecting vulnerable browsers'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
