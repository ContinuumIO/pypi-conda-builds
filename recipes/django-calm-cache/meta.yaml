package:
  name: django-calm-cache
  version: "0.9.2"

source:
  fn: django-calm-cache-0.9.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-calm-cache/django-calm-cache-0.9.2.tar.gz
  md5: b26c37a565ed5e928205ea3842fe9e5c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-calm-cache = django-calm-cache:main
    #
    # Would create an entry point called django-calm-cache that calls django-calm-cache.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - calm_cache
    - calm_cache.backends
    - calm_cache.decorators

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/pitcrews/django-calm-cache
  license: BSD License
  summary: 'A set of useful tools that enhance the standard Django cache experience'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
