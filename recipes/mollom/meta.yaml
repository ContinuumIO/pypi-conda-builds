package:
  name: mollom
  version: "0.1.2"

source:
  fn: mollom-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/m/mollom/mollom-0.1.2.tar.gz
  md5: c0b7474e6d4f80e2ec3e84b0563aa2c6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mollom = mollom:main
    #
    # Would create an entry point called mollom that calls mollom.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests-oauthlib

  run:
    - python
    - requests-oauthlib

test:
  # Python imports
  imports:
    - mollom

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Mollom/mollom_python
  license: MIT License
  summary: 'Mollom client library.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
