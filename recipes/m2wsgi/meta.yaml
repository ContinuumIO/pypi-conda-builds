package:
  name: m2wsgi
  version: "0.5.2"

source:
  fn: m2wsgi-0.5.2.tar.gz
  url: https://pypi.python.org/packages/source/m/m2wsgi/m2wsgi-0.5.2.tar.gz
  md5: 3a3bc0b3466ec2457b49c4a21c4b883f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - m2wsgi = m2wsgi:main
    #
    # Would create an entry point called m2wsgi that calls m2wsgi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - m2wsgi
    - m2wsgi.device
    - m2wsgi.io
    - m2wsgi.middleware
    - m2wsgi.tests
    - m2wsgi.util

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/rfk/m2wsgi/
  license: MIT License
  summary: 'a mongrel2 => wsgi gateway and helper tools'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
