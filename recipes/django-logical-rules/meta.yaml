package:
  name: django-logical-rules
  version: "1.0"

source:
  fn: django-logical-rules-1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-logical-rules/django-logical-rules-1.0.tar.gz
  md5: 40ae2b01616dc17e39d8a5fed35da504
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-logical-rules = django-logical-rules:main
    #
    # Would create an entry point called django-logical-rules that calls django-logical-rules.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4

  run:
    - python
    - django >=1.4

test:
  # Python imports
  imports:
    - logical_rules
    - logical_rules.templatetags
    - logical_rules.tests
    - logical_rules.tests.test_app

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/aashe/django-logical-rules
  license: BSD License
  summary: 'A rule engine for Django apps.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
