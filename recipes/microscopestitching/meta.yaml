package:
  name: microscopestitching
  version: "0.0.1"

source:
  fn: microscopestitching-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/m/microscopestitching/microscopestitching-0.0.1.tar.gz
  md5: 20c75bdfb74ae581ad20b36ab9c8586a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - microscopestitching = microscopestitching:main
    #
    # Would create an entry point called microscopestitching that calls microscopestitching.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - imreg_dft
    - scikit-imagenumpy
    - joblib

  run:
    - python
    - imreg_dft
    - scikit-imagenumpy
    - joblib

test:
  # Python imports
  imports:
    - microscopestitching

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/arve0/microscopestitching
  license: MIT License
  summary: 'Automatic merge/stitching of regular spaced images'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
