package:
  name: quintagroup.slidertemplates
  version: "1.3.1"

source:
  fn: quintagroup.slidertemplates-1.3.1.zip
  url: https://pypi.python.org/packages/source/q/quintagroup.slidertemplates/quintagroup.slidertemplates-1.3.1.zip
  md5: 1bc124140e522d8ef00290431a32c3b4
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - quintagroup.slidertemplates = quintagroup.slidertemplates:main
    #
    # Would create an entry point called quintagroup.slidertemplates that calls quintagroup.slidertemplates.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.portlet.ngcollection

  run:
    - python
    - setuptools
    - setuptools
    - collective.portlet.ngcollection

test:
  # Python imports
  imports:
    - quintagroup
    - quintagroup.slidertemplates
    - quintagroup.slidertemplates.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/
  license: GPL
  summary: 'Different Templates to display NGCollection Portlet'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
