package:
  name: tests-always-pass
  version: "0.1.0"

source:
  fn: tests-always-pass-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/t/tests-always-pass/tests-always-pass-0.1.0.tar.gz
  md5: e6ecc8cf9971dcb32ceea164f9261757
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tests-always-pass = tests-always-pass:main
    #
    # Would create an entry point called tests-always-pass that calls tests-always-pass.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - tests-always-pass

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/borntyping/cookiecutter-pypackage-minimal
  license: UNKNOWN
  summary: 'I hate tests that always pass'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
