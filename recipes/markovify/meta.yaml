package:
  name: markovify
  version: "0.0.0"

source:
  fn: markovify-0.0.0.tar.gz
  url: https://pypi.python.org/packages/source/m/markovify/markovify-0.0.0.tar.gz
  md5: d324b8265f443fb0cd75f2690aa598b5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - markovify = markovify:main
    #
    # Would create an entry point called markovify that calls markovify.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - unidecode

  run:
    - python
    - unidecode

test:
  # Python imports
  imports:
    - markovify

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/jsvine/markovify
  license: MIT License
  summary: 'Use Markov chains to generate random semi-plausible sentences based on an existing text.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
