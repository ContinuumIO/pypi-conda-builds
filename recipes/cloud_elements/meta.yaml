package:
  name: cloud_elements
  version: "0.2.2"

source:
  fn: cloud_elements-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/c/cloud_elements/cloud_elements-0.2.2.tar.gz
  md5: 87e20bb0584dc80f57255e491703a655
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cloud_elements = cloud_elements:main
    #
    # Would create an entry point called cloud_elements that calls cloud_elements.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - cloud_elements

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/cloud-elements/elements-connector-python.git
  license: UNKNOWN
  summary: 'Cloud Elements Connector for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
