package:
  name: movielist
  version: "1.4"

source:
  fn: MovieList-1.4.tar.gz
  url: https://pypi.python.org/packages/source/M/MovieList/MovieList-1.4.tar.gz
  md5: 5ae9f669f60ea2e1f6b80bbb1e6e568b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - movielist = movielist:main
    #
    # Would create an entry point called movielist that calls movielist.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - MovieList
    - movielist

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/MovieList/
  license: GNU General Public License v3 (GPLv3)
  summary: 'A utility to catalogue and play media.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
