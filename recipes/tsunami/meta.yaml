package:
  name: tsunami
  version: "0.3"

source:
  fn: tsunami-0.3.tar.gz
  url: https://pypi.python.org/packages/source/t/tsunami/tsunami-0.3.tar.gz
  md5: 83f3568fa00e1d0df38f77f1fb3c73e6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tsunami = tsunami:main
    #
    # Would create an entry point called tsunami that calls tsunami.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - tornado
    - sqlalchemy
    - werkzeug

  run:
    - python
    - tornado
    - sqlalchemy
    - werkzeug

test:
  # Python imports
  imports:
    - tsunami
    - tsunami.clients
    - tsunami.runtime
    - tsunami.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.tsangpo.net/
  license: UNKNOWN
  summary: 'Web framework based tornado'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
