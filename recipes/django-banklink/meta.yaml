package:
  name: django-banklink
  version: "0.14"

source:
  fn: django-banklink-0.14.tar.gz
  url: https://pypi.python.org/packages/source/d/django-banklink/django-banklink-0.14.tar.gz
  md5: 41a629e11585e82461fb555e95ce47d0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-banklink = django-banklink:main
    #
    # Would create an entry point called django-banklink that calls django-banklink.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - django_banklink

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.github.com/truevision/django_banklink
  license: BSD License
  summary: 'django application for Swedbank payment gateway'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
