package:
  name: hedgeye_analysis
  version: "0.2.1"

source:
  fn: hedgeye_analysis-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/h/hedgeye_analysis/hedgeye_analysis-0.2.1.tar.gz
  md5: cd6ebd1fae0e144b41c4c5eb10bdf2df
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hedgeye_analysis = hedgeye_analysis:main
    #
    # Would create an entry point called hedgeye_analysis that calls hedgeye_analysis.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - ipython
    - matplotlib
    - numpy
    - pandas
    - seaborn

  run:
    - python
    - ipython
    - matplotlib
    - numpy
    - pandas
    - seaborn

test:
  # Python imports
  imports:
    - hedgeye_analysis

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/hedgeyedev/hedgeye-python-analysis/hedgeye_analysis
  license: MIT License
  summary: 'Python analysis tools used by Hedgeye'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
