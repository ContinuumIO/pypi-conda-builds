package:
  name: django-epic-sample
  version: "0.2.3"

source:
  fn: django-epic-sample-0.2.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-epic-sample/django-epic-sample-0.2.3.tar.gz
  md5: 63e7fa0a2ecf01e5e594904eb5ad51c6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-epic-sample = django-epic-sample:main
    #
    # Would create an entry point called django-epic-sample that calls django-epic-sample.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-epic

  run:
    - python
    - django-epic

test:
  # Python imports
  imports:
    - epic-sample

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/egauge/epic-sample/
  license: MIT License
  summary: 'A minimal Django project to demonstrate EPIC functionality.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
