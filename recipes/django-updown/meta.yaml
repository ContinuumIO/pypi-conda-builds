package:
  name: django-updown
  version: "0.5.0"

source:
  fn: django-updown-0.5.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-updown/django-updown-0.5.0.tar.gz
  md5: 78a0c6f319f19b3a4c4cad7036122ab3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-updown = django-updown:main
    #
    # Would create an entry point called django-updown that calls django-updown.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - updown
    - updown.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/weluse/django-updown/tree/master
  license: BSD License
  summary: 'django-updown is a reusable Django application for youtube     like up and down voting.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
