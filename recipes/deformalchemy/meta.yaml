package:
  name: deformalchemy
  version: "0.1dev"

source:
  fn: DeformAlchemy-0.1dev.tar.gz
  url: https://pypi.python.org/packages/source/D/DeformAlchemy/DeformAlchemy-0.1dev.tar.gz
  md5: de77340a904228bd5e9abf3785e52902
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - deformalchemy = deformalchemy:main
    #
    # Would create an entry point called deformalchemy that calls deformalchemy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - deform
    - deform_bootstrap
    - colanderalchemy >=0.2dev
    - pyramid

  run:
    - python
    - deform
    - deform_bootstrap
    - colanderalchemy >=0.2dev
    - pyramid

test:
  # Python imports
  imports:
    - deformalchemy
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: MIT
  summary: 'DeformAlchemy'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
