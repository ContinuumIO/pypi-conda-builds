package:
  name: aptop
  version: "0.3.2b"

source:
  fn: aptop-0.3.2b.tar.gz
  url: https://pypi.python.org/packages/source/a/aptop/aptop-0.3.2b.tar.gz
  md5: a683f70c3182304d29408a2354bc2f93
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - aptop = aptop:main
    #
    # Would create an entry point called aptop that calls aptop.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - lxml

  run:
    - python
    - lxml

test:
  # Python imports
  imports:
    - ApTop
    - aptop

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/btoic/aptop/get/master.tar.gz
  license: GPLv2
  summary: 'ApTop is top like clone for apaache geting info from apache mod_status'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
