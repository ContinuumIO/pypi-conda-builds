package:
  name: church_encoding
  version: "0.1.1"

source:
  fn: church_encoding-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/c/church_encoding/church_encoding-0.1.1.tar.gz
  md5: ae6fae0dd6b5aa2d4ccf7d3dfd23cb16
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - church_encoding = church_encoding:main
    #
    # Would create an entry point called church_encoding that calls church_encoding.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - church_encoding
    - church_encoding.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/Risto-Stevcev/python-church-encodings
  license: BSD License
  summary: 'Church encodings written in Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
