package:
  name: collective.collage.contentleadimage
  version: "0.1"

source:
  fn: collective.collage.contentleadimage-0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.collage.contentleadimage/collective.collage.contentleadimage-0.1.tar.gz
  md5: 5cf0e33dcc301499fae6e6e74ac489e9
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.collage.contentleadimage = collective.collage.contentleadimage:main
    #
    # Would create an entry point called collective.collage.contentleadimage that calls collective.collage.contentleadimage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.collage
    - collective.contentleadimage
    - collective.fastview

  run:
    - python
    - setuptools
    - setuptools
    - products.collage
    - collective.contentleadimage
    - collective.fastview

test:
  # Python imports
  imports:
    - collective
    - collective.collage
    - collective.collage.contentleadimage

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://svn.plone.org/svn/collective/Products.Collage/addons/collective.collage.contentleadimage
  license: GPL
  summary: 'Views for Products.Collage that display content with images from collective.contentleadimage'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
