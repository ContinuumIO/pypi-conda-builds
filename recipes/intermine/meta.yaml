package:
  name: intermine
  version: "1.09.05"

source:
  fn: intermine-1.09.05.tar.gz
  url: https://pypi.python.org/packages/source/i/intermine/intermine-1.09.05.tar.gz
  md5: be09743432527f2c49951981f8de3dbb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - intermine = intermine:main
    #
    # Would create an entry point called intermine that calls intermine.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - intermine
    - intermine.lists

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.intermine.org
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'InterMine WebService client'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
