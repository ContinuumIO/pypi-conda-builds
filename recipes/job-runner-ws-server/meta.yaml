package:
  name: job-runner-ws-server
  version: "1.0.0"

source:
  fn: job-runner-ws-server-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/j/job-runner-ws-server/job-runner-ws-server-1.0.0.tar.gz
  md5: 97692e55a88454aa42ff9fb308a131a7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - job-runner-ws-server = job-runner-ws-server:main
    #
    # Would create an entry point called job-runner-ws-server that calls job-runner-ws-server.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - argparse
    - gevent
    - gevent_subprocess
    - gevent-websocket
    - pyzmq

  run:
    - python
    - argparse
    - gevent
    - gevent_subprocess
    - gevent-websocket
    - pyzmq

test:
  # Python imports
  imports:
    - job_runner_ws_server

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/spilgames/job_runner_ws_server/
  license: UNKNOWN
  summary: 'Job-Runner WebSocket Server'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
