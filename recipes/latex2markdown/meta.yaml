package:
  name: latex2markdown
  version: "0.2.1"

source:
  fn: latex2markdown-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/l/latex2markdown/latex2markdown-0.2.1.tar.gz
  md5: cbc35460a6ec1f5c0ba1b3b5820a637b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - latex2markdown = latex2markdown:main
    #
    # Would create an entry point called latex2markdown that calls latex2markdown.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - latex2markdown

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ajtulloch/LaTeX2Markdown
  license: UNKNOWN
  summary: 'An AMS-LaTeX compatible converter that maps a subset of LaTeX to Markdown/MathJaX.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
