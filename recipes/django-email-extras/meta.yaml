package:
  name: django-email-extras
  version: "0.3.1"

source:
  fn: django-email-extras-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-email-extras/django-email-extras-0.3.1.tar.gz
  md5: b03c8dc6ab9597eee27ff00cf95c3d55
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-email-extras = django-email-extras:main
    #
    # Would create an entry point called django-email-extras that calls django-email-extras.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python-gnupg
    - sphinx-me

  run:
    - python
    - python-gnupg
    - sphinx-me

test:
  # Python imports
  imports:
    - email_extras

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/stephenmcd/django-email-extras
  license: UNKNOWN
  summary: 'A Django reusable app providing the ability to send PGP encrypted and multipart emails using the Django templating system.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
