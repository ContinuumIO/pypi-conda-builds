package:
  name: estraiernative
  version: "0.2"

source:
  fn: estraiernative-0.2.tar.gz
  url: https://pypi.python.org/packages/source/e/estraiernative/estraiernative-0.2.tar.gz
  md5: ffc28ca5151831d3f383d1022980b1a8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - estraiernative = estraiernative:main
    #
    # Would create an entry point called estraiernative that calls estraiernative.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - estraiernative

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Python native binding for Hyper Estraier'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
