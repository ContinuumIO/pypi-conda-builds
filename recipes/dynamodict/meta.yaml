package:
  name: dynamodict
  version: "0.2.1"

source:
  fn: dynamodict-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/dynamodict/dynamodict-0.2.1.tar.gz
  md5: 55a60d4b92322fe626ba9f65a4c89455
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dynamodict = dynamodict:main
    #
    # Would create an entry point called dynamodict that calls dynamodict.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - boto >=2.25

  run:
    - python
    - boto >=2.25

test:
  # Python imports
  imports:
    - ddbd
    - dynamodict

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/witoff/DynamoDict
  license: UNKNOWN
  summary: 'A simple ORM that maps DynamoDB Tables to native python dicts'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
