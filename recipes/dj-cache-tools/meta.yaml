package:
  name: dj-cache-tools
  version: "0.5.2"

source:
  fn: dj-cache-tools-0.5.2.tar.gz
  url: https://pypi.python.org/packages/source/d/dj-cache-tools/dj-cache-tools-0.5.2.tar.gz
  md5: be2ee36e7b2a3ea8f6c37a5dcaa1c6e4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dj-cache-tools = dj-cache-tools:main
    #
    # Would create an entry point called dj-cache-tools that calls dj-cache-tools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3

  run:
    - python
    - django >=1.3

test:
  # Python imports
  imports:
    - cache_tools

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/creatiweb/django-cache-tools
  license: BSD License
  summary: 'Django cache tools originally developed for Ella CMS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
