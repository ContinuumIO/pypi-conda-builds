package:
  name: products.plonelanguagetool
  version: "3.2.7"

source:
  fn: Products.PloneLanguageTool-3.2.7.zip
  url: https://pypi.python.org/packages/source/P/Products.PloneLanguageTool/Products.PloneLanguageTool-3.2.7.zip
  md5: bd9eb6278bf76e8cbce99437ca362164
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - products.plonelanguagetool = products.plonelanguagetool:main
    #
    # Would create an entry point called products.plonelanguagetool that calls products.plonelanguagetool.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - Products
    - Products.PloneLanguageTool
    - Products.PloneLanguageTool.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/Products.PloneLanguageTool
  license: GNU General Public License (GPL)
  summary: 'PloneLanguageTool allows you to set the available languages in your Plone site, select various fallback mechanisms, and control the use of flags for language selection and translations.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
