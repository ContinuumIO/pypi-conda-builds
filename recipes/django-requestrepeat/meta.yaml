package:
  name: django-requestrepeat
  version: "0.5"

source:
  fn: django-requestrepeat-0.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-requestrepeat/django-requestrepeat-0.5.tar.gz
  md5: f52208f768b4e58cfe9f94f9f0cda8b2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-requestrepeat = django-requestrepeat:main
    #
    # Would create an entry point called django-requestrepeat that calls django-requestrepeat.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.1.1
    - requests ==2.5.1

  run:
    - python
    - django >=1.1.1
    - requests ==2.5.1

test:
  # Python imports
  imports:
    - requestrepeat
    - requestrepeat.migrations
    - requestrepeat.rr

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/clickyspinny/django-requestrepeat/tree/master/requestrepeat
  license: BSD License
  summary: 'A django app that repeats one http POST into several.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
