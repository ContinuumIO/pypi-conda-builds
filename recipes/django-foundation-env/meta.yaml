package:
  name: django-foundation-env
  version: "0.2"

source:
  fn: django-foundation-env-0.2.zip
  url: https://pypi.python.org/packages/source/d/django-foundation-env/django-foundation-env-0.2.zip
  md5: 112b742fd3cfec819875734c5ee66d2e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-foundation-env = django-foundation-env:main
    #
    # Would create an entry point called django-foundation-env that calls django-foundation-env.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - foundation
    - foundation.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://pypass.gr
  license: GPLv3
  summary: 'Zurb Foundation Framework for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
