package:
  name: jcrack
  version: "0.0.2"

source:
  fn: jcrack-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/j/jcrack/jcrack-0.0.2.tar.gz
  md5: 4a407b7459664b05a0e8e08c10d40d8c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jcrack = jcrack:main
    #
    # Would create an entry point called jcrack that calls jcrack.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - celery >=2.4.6
    - sqlalchemy >=0.7.5
    - configparser >=3.1.0

  run:
    - python
    - celery >=2.4.6
    - sqlalchemy >=0.7.5
    - configparser >=3.1.0

test:
  # Python imports
  imports:
    - jcrack

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://awgh.org
  license: GPL
  summary: 'Jabbercracky: A Hash-Cracking Web Service'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
