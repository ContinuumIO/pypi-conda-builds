package:
  name: oauth2-utf8
  version: "1.5.170"

source:
  fn: oauth2-utf8-1.5.170.tar.gz
  url: https://pypi.python.org/packages/source/o/oauth2-utf8/oauth2-utf8-1.5.170.tar.gz
  md5: 59118554d74caad6e5a7d79ed48b3033
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - oauth2-utf8 = oauth2-utf8:main
    #
    # Would create an entry point called oauth2-utf8 that calls oauth2-utf8.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - httplib2

  run:
    - python
    - httplib2

test:
  # Python imports
  imports:
    - oauth2
    - oauth2.clients
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bxm156/python-oauth2
  license: MIT License
  summary: 'library for OAuth version 1.0 with to_url utf8 fix'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
