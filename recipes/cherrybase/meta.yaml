package:
  name: cherrybase
  version: "0.4.5"

source:
  fn: cherrybase-0.4.5.tar.gz
  url: https://pypi.python.org/packages/source/c/cherrybase/cherrybase-0.4.5.tar.gz
  md5: 0a5c90f72ecb560e916591ece6da27a6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cherrybase = cherrybase:main
    #
    # Would create an entry point called cherrybase that calls cherrybase.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cherrypy >=3.2

  run:
    - python
    - cherrypy >=3.2

test:
  # Python imports
  imports:
    - cherrybase
    - cherrybase.db
    - cherrybase.db.drivers
    - cherrybase.orm
    - cherrybase.orm.drivers
    - cherrybase.tools
    - cherrybase.tools.auth

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/UncleRus/cherryBase
  license: LGPLv3
  summary: 'Wrapper around CherryPy'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
