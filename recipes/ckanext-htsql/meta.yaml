package:
  name: ckanext-htsql
  version: "0.1.1"

source:
  fn: ckanext-htsql-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/c/ckanext-htsql/ckanext-htsql-0.1.1.tar.gz
  md5: 5c4a4860e6a892380fde31c43a068a72
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ckanext-htsql = ckanext-htsql:main
    #
    # Would create an entry point called ckanext-htsql that calls ckanext-htsql.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - htsql

  run:
    - python
    - setuptools
    - htsql

test:
  # Python imports
  imports:
    - ckanext
    - ckanext.htsql
    - ckanext.htsql.logic

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: AGPL
  summary: 'Enables HTSQL queries in the new datastore in CKAN.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
