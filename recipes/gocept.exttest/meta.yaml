package:
  name: gocept.exttest
  version: "1.0"

source:
  fn: gocept.exttest-1.0.tar.gz
  url: https://pypi.python.org/packages/source/g/gocept.exttest/gocept.exttest-1.0.tar.gz
  md5: f0e99d93a0d0eb1ce69844d7b3434756
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gocept.exttest = gocept.exttest:main
    #
    # Would create an entry point called gocept.exttest that calls gocept.exttest.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zc.recipe.testrunner

  run:
    - python
    - setuptools
    - zc.recipe.testrunner

test:
  # Python imports
  imports:
    - gocept
    - gocept.exttest
    - gocept.exttest.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.gocept.com/hg/public/gocept.exttest
  license: ZPL
  summary: 'Helper to integrate external tests with python unittests.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
