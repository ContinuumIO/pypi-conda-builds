package:
  name: noolite
  version: "1.1.3"

source:
  fn: noolite-1.1.3.tar.gz
  url: https://pypi.python.org/packages/source/n/noolite/noolite-1.1.3.tar.gz
  md5: 506f721c9a1d23646b014380d8520734
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - noolite = noolite:main
    #
    # Would create an entry point called noolite that calls noolite.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyusb

  run:
    - python
    - pyusb

test:
  # Python imports
  imports:
    - noolite

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Sicness/pyNooLite
  license: GNU General Public License v3 (GPLv3)
  summary: 'Class for NooLite USB stick'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
