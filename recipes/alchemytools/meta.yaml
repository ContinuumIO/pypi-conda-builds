package:
  name: alchemytools
  version: "0.4"

source:
  fn: Alchemytools-0.4.tar.gz
  url: https://pypi.python.org/packages/source/A/Alchemytools/Alchemytools-0.4.tar.gz
  md5: f7819d7ad5e180d7c289443eaed90c42
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - alchemytools = alchemytools:main
    #
    # Would create an entry point called alchemytools that calls alchemytools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sqlalchemy >=0.7.8,<=0.9.2

  run:
    - python
    - sqlalchemy >=0.7.8,<=0.9.2

test:
  # Python imports
  imports:
    - alchemytools

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/daltonmatos/alchemytools
  license: BSD License
  summary: 'Alchemytools is a set of helpers to be used in any SQLAlchemy project'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
