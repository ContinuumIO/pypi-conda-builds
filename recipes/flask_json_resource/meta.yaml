package:
  name: flask_json_resource
  version: "0.2.18"

source:
  fn: flask_json_resource-0.2.18.tar.gz
  url: https://pypi.python.org/packages/source/f/flask_json_resource/flask_json_resource-0.2.18.tar.gz
  md5: 26e37d0de940193f60e4ca5c2a19e235
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask_json_resource = flask_json_resource:main
    #
    # Would create an entry point called flask_json_resource that calls flask_json_resource.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - json_resource
    - flask-pymongo

  run:
    - python
    - flask
    - json_resource
    - flask-pymongo

test:
  # Python imports
  imports:
    - flask_json_resource
    - flask_json_resource.features

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
