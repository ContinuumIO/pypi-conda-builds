package:
  name: timeparser
  version: "0.7.4"

source:
  fn: timeparser-0.7.4.tar.gz
  url: https://pypi.python.org/packages/source/t/timeparser/timeparser-0.7.4.tar.gz
  md5: 8132f4edba4c17be19cc571527b3af08
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - timeparser = timeparser:main
    #
    # Would create an entry point called timeparser that calls timeparser.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - timeparser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/thomst/timeparser
  license: GNU General Public License (GPL)
  summary: 'A python-module to parse strings to time-, date-, datetime- or timedelta-objects.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
