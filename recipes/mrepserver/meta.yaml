package:
  name: mrepserver
  version: "0.1.0"

source:
  fn: mrepserver-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/m/mrepserver/mrepserver-0.1.0.tar.gz
  md5: 8721a08aa047d446dc2a7b78e88b8f7a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mrepserver = mrepserver:main
    #
    # Would create an entry point called mrepserver that calls mrepserver.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - mrep
    - tornado

  run:
    - python
    - mrep
    - tornado

test:
  # Python imports
  imports:
    - mrepserver

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/unnonouno/mrepserver
  license: MIT License
  summary: 'MREP server: morpheme regular expression printer'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
