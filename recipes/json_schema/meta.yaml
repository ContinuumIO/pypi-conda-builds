package:
  name: json_schema
  version: "0.3"

source:
  fn: json_schema-0.3.tar.gz
  url: https://pypi.python.org/packages/source/j/json_schema/json_schema-0.3.tar.gz
  md5: 59e75cb6b7f192ea01507512a7e7ede8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - json_schema = json_schema:main
    #
    # Would create an entry point called json_schema that calls json_schema.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - json_schema

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://lericson.blogg.se/code/category/json_schema.html
  license: UNKNOWN
  summary: 'JSON schema definition and validation library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
