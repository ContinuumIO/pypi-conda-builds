package:
  name: mangopi
  version: "0.2.1.1"

source:
  fn: mangopi-0.2.1.1.tar.gz
  url: https://pypi.python.org/packages/source/m/mangopi/mangopi-0.2.1.1.tar.gz
  md5: 4a27a5c2e2e2e5e90f504c72915952d3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mangopi = mangopi:main
    #
    # Would create an entry point called mangopi that calls mangopi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - underscore.py ==0.1.6

  run:
    - python
    - underscore.py ==0.1.6

test:
  # Python imports
  imports:
    - mangopi
    - mangopi.helper
    - mangopi.helper.traits
    - mangopi.site

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jiaweihli/mangopi/
  license: MIT
  summary: 'A manga API with a pluggable site architecture.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
