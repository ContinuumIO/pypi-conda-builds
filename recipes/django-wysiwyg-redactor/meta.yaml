package:
  name: django-wysiwyg-redactor
  version: "0.4.7"

source:
  fn: django-wysiwyg-redactor-0.4.7.tar.gz
  url: https://pypi.python.org/packages/source/d/django-wysiwyg-redactor/django-wysiwyg-redactor-0.4.7.tar.gz
  md5: b580cb5822166bf412b5333e7e59b6d6
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-wysiwyg-redactor = django-wysiwyg-redactor:main
    #
    # Would create an entry point called django-wysiwyg-redactor that calls django-wysiwyg-redactor.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - redactor

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/douglasmiranda/django-wysiwyg-redactor
  license: BSD License
  summary: 'django-wysiwyg-redactor is a lightweight responsive wysiwyg editor for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
