package:
  name: django-stockandflow
  version: "0.0.4"

source:
  fn: django-stockandflow-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-stockandflow/django-stockandflow-0.0.4.tar.gz
  md5: 9b4c23f8086618bdd86883cbf5a528da
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-stockandflow = django-stockandflow:main
    #
    # Would create an entry point called django-stockandflow that calls django-stockandflow.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - stockandflow

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/jesseh/django-stockandflow/
  license: LICENSE.txt
  summary: 'Django stock and flow tracking for business intelligence metrics'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
