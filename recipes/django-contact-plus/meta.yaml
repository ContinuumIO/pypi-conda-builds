package:
  name: django-contact-plus
  version: "0.0.2"

source:
  fn: django-contact-plus-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-contact-plus/django-contact-plus-0.0.2.tar.gz
  md5: 079adc272aabc773be7b6eb1582b643a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-contact-plus = django-contact-plus:main
    #
    # Would create an entry point called django-contact-plus that calls django-contact-plus.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'Core of cmsplugin_contact_plus extracted as a standalone Django app'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
