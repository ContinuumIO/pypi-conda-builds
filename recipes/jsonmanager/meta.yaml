package:
  name: jsonmanager
  version: "0.2.4"

source:
  fn: jsonmanager-0.2.4.tar.gz
  url: https://pypi.python.org/packages/source/j/jsonmanager/jsonmanager-0.2.4.tar.gz
  md5: 76d0e749906582d508e51850a77266fd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jsonmanager = jsonmanager:main
    #
    # Would create an entry point called jsonmanager that calls jsonmanager.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - jsonmanager
    - jsonmanager.validation_tools

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jmatthias/jsonmanager
  license: LICENSE.txt
  summary: 'Validation, coercion, and forms for JSON.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
