package:
  name: cheetah
  version: "2.4.4"

source:
  fn: Cheetah-2.4.4.tar.gz
  url: https://pypi.python.org/packages/source/C/Cheetah/Cheetah-2.4.4.tar.gz
  md5: 853917116e731afbc8c8a43c37e6ddba
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cheetah = cheetah:main
    #
    # Would create an entry point called cheetah that calls cheetah.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - markdown >=2.0.1

  run:
    - python
    - markdown >=2.0.1

test:
  # Python imports
  imports:
    - Cheetah
    - Cheetah.Macros
    - Cheetah.Templates
    - Cheetah.Tests
    - Cheetah.Tools
    - Cheetah.Utils
    - cheetah

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.cheetahtemplate.org/
  license: MIT License
  summary: 'Cheetah is a template engine and code generation tool.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
