package:
  name: verboselogs
  version: "1.0.1"

source:
  fn: verboselogs-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/v/verboselogs/verboselogs-1.0.1.tar.gz
  md5: ddff692ca9235fe4bae05c17e163f454
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - verboselogs = verboselogs:main
    #
    # Would create an entry point called verboselogs that calls verboselogs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - verboselogs

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://pypi.python.org/pypi/verboselogs
  license: UNKNOWN
  summary: "Verbose logging for Python's logging module"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
