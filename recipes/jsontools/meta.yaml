package:
  name: jsontools
  version: "0.1"

source:
  fn: jsontools-0.1.tar.gz
  url: https://pypi.python.org/packages/source/j/jsontools/jsontools-0.1.tar.gz
  md5: 90867f4090d16cba0268edf78b938da8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jsontools = jsontools:main
    #
    # Would create an entry point called jsontools that calls jsontools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - jsontools

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.davispj.com/svn/projects/jsontools
  license: MIT License
  summary: 'jsontools - Useful utilities for JSON'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
