package:
  name: fiabilipy
  version: "2.7"

source:
  fn: fiabilipy-2.7.tar.gz
  url: https://pypi.python.org/packages/source/f/fiabilipy/fiabilipy-2.7.tar.gz
  md5: c8de53a3b47944ca47b994c89060faf6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fiabilipy = fiabilipy:main
    #
    # Would create an entry point called fiabilipy that calls fiabilipy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - fiabilipy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://fiabilipy.org
  license: GNU General Public License v2 or later (GPLv2+)
  summary: 'Learn engineering reliability with python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
