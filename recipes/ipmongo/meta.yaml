package:
  name: ipmongo
  version: "0.1.3"

source:
  fn: ipmongo-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/i/ipmongo/ipmongo-0.1.3.tar.gz
  md5: e33227368d5dedd1a35d62cf938231fb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ipmongo = ipmongo:main
    #
    # Would create an entry point called ipmongo that calls ipmongo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pymongo
    - ipaddr

  run:
    - python
    - pymongo
    - ipaddr

test:
  # Python imports
  imports:
    - ipmongo

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/wal1ybot/ipmongo
  license: MIT License
  summary: 'Provide conversion of ipaddr object for MongoDB'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
