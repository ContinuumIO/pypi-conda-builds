package:
  name: configify
  version: "0.0.3"

source:
  fn: Configify-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/C/Configify/Configify-0.0.3.tar.gz
  md5: 573c28dea6c3cc5d24ea2cafa92db0cb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - configify = configify:main
    #
    # Would create an entry point called configify that calls configify.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - jinja2 ==2.7.3
    - markupsafe ==0.23
    - pyyaml ==3.11

  run:
    - python
    - jinja2 ==2.7.3
    - markupsafe ==0.23
    - pyyaml ==3.11

test:
  # Python imports
  imports:
    - configify

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/RickyCook/configify
  license: UNKNOWN
  summary: 'Templated output, from templated context parameters'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
