package:
  name: isaac
  version: "0.1.0"

source:
  fn: isaac-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/i/isaac/isaac-0.1.0.tar.gz
  md5: c2fc518c036524a30c0af963fe445d2e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - isaac = isaac:main
    #
    # Would create an entry point called isaac that calls isaac.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - isaac

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/danielquinn/isaac/
  license: GNU Affero General Public License v3
  summary: 'A password checker that uses the natural rainbow table that is Google Search for validation.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
