package:
  name: bootstrap_admin
  version: "0.3.6"

source:
  fn: bootstrap_admin-0.3.6.tar.gz
  url: https://pypi.python.org/packages/source/b/bootstrap_admin/bootstrap_admin-0.3.6.tar.gz
  md5: ca5ea57e0d0ff38d36575fa28b4fcc2e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bootstrap_admin = bootstrap_admin:main
    #
    # Would create an entry point called bootstrap_admin that calls bootstrap_admin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - bootstrap_admin
    - bootstrap_admin.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/douglasmiranda/django-admin-bootstrap
  license: BSD License
  summary: 'Responsive Theme for Django Admin (Django 1.7)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
