package:
  name: youku
  version: "0.0.7"

source:
  fn: youku-0.0.7.tar.gz
  url: https://pypi.python.org/packages/source/y/youku/youku-0.0.7.tar.gz
  md5: f12c31120dd17cc1827acb8c3978fcc3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - youku = youku:main
    #
    # Would create an entry point called youku that calls youku.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - youku

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/hanguokai/youku
  license: Apache Software License
  summary: 'Youku open api python client and video uploader'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
