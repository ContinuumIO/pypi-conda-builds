package:
  name: django-importer
  version: "0.6"

source:
  fn: django-importer-0.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-importer/django-importer-0.6.tar.gz
  md5: 6486ea669c8845c903080f33241b5c2b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-importer = django-importer:main
    #
    # Would create an entry point called django-importer that calls django-importer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_importer
    - django_importer.importers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/ricobl/django-importer/
  license: UNKNOWN
  summary: 'Data importers for Django models'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
