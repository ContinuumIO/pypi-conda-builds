package:
  name: flask-gae_tests
  version: "1.0.2"

source:
  fn: flask-gae_tests-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/f/flask-gae_tests/flask-gae_tests-1.0.2.tar.gz
  md5: 7786d3579574dd6b7770649bb849edd3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-gae_tests = flask-gae_tests:main
    #
    # Would create an entry point called flask-gae_tests that calls flask-gae_tests.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask

  run:
    - python
    - flask

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/gregorynicholas/flask-gae_tests
  license: MIT License
  summary: 'Flask Extension with base test cases to simplify testing Flask applications on App Engine.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
