package:
  name: amplecode.recipe.template
  version: "1.1"

source:
  fn: amplecode.recipe.template-1.1.tar.gz
  url: https://pypi.python.org/packages/source/a/amplecode.recipe.template/amplecode.recipe.template-1.1.tar.gz
  md5: adf8d767ad5d3a73a02bec7baef0a47e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - amplecode.recipe.template = amplecode.recipe.template:main
    #
    # Would create an entry point called amplecode.recipe.template that calls amplecode.recipe.template.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.recipe.egg
    - jinja2

  run:
    - python
    - setuptools
    - setuptools
    - zc.recipe.egg
    - jinja2

test:
  # Python imports
  imports:
    - amplecode
    - amplecode.recipe
    - amplecode.recipe.template

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/torgeilo/amplecode.recipe.template
  license: BSD License
  summary: 'Buildout recipe for making files out of Jinja2 templates'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
