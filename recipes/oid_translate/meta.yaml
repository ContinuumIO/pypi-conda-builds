package:
  name: oid_translate
  version: "0.2.3"

source:
  fn: oid_translate-0.2.3.tar.gz
  url: https://pypi.python.org/packages/source/o/oid_translate/oid_translate-0.2.3.tar.gz
  md5: fc86525e84892f5db3bf78b75dbff34b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - oid_translate = oid_translate:main
    #
    # Would create an entry point called oid_translate that calls oid_translate.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - oid_translate

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dropbox/oid_translate
  license: Apache
  summary: 'Python OID/MIB Name Translator'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
