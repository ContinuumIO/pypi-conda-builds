package:
  name: django-iphone-push
  version: "1.0"

source:
  fn: django-iphone-push-1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-iphone-push/django-iphone-push-1.0.tar.gz
  md5: ad7e8bb54024aa3125c36f004bb2da1e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-iphone-push = django-iphone-push:main
    #
    # Would create an entry point called django-iphone-push that calls django-iphone-push.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - iphonepush
    - iphonepush.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://leepa.github.com/django-iphone-push/
  license: See LICENSE.txt
  summary: 'Add iPhone Push notifications to a Django Project quickly and easily'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
