package:
  name: git-pylint-commit-hook
  version: "2.0.7"

source:
  fn: git-pylint-commit-hook-2.0.7.tar.gz
  url: https://pypi.python.org/packages/source/g/git-pylint-commit-hook/git-pylint-commit-hook-2.0.7.tar.gz
  md5: 269ca01a5cc972414d4eb8cbf6376888
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - git-pylint-commit-hook = git-pylint-commit-hook:main
    #
    # Would create an entry point called git-pylint-commit-hook that calls git-pylint-commit-hook.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pylint

  run:
    - python
    - pylint

test:
  # Python imports
  imports:
    - git_pylint_commit_hook

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.sebastiandahlgren.se
  license: Apache Software License
  summary: 'Git commit hook that checks Python files with pylint'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
