package:
  name: django-publications
  version: "0.6.1"

source:
  fn: django-publications-0.6.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-publications/django-publications-0.6.1.tar.gz
  md5: 54cbbfe44a54f52c5602239bd716a6ed
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-publications = django-publications:main
    #
    # Would create an entry point called django-publications that calls django-publications.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5.0
    - pillow >=2.3.0

  run:
    - python
    - django >=1.5.0
    - pillow >=2.3.0

test:
  # Python imports
  imports:
    - publications
    - publications.admin
    - publications.admin_views
    - publications.models
    - publications.templatetags
    - publications.tests
    - publications.views

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lucastheis/django-publications
  license: MIT License
  summary: 'A Django app for managing publications.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
