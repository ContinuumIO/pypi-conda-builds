package:
  name: mongodb-log-je
  version: "0.1.0"

source:
  fn: mongodb-log-je-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/m/mongodb-log-je/mongodb-log-je-0.1.0.tar.gz
  md5: 9d798ce0f92f94fc6f364b35bcc27ee2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mongodb-log-je = mongodb-log-je:main
    #
    # Would create an entry point called mongodb-log-je that calls mongodb-log-je.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pymongo

  run:
    - python
    - pymongo

test:
  # Python imports
  imports:
    - mongolog

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/andreisavu/mongodb-log/tree/master
  license: UNKNOWN
  summary: 'Centralized logging made simple using mongodb'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
