package:
  name: megrok.z3ctable
  version: "1.6.0"

source:
  fn: megrok.z3ctable-1.6.0.tar.gz
  url: https://pypi.python.org/packages/source/m/megrok.z3ctable/megrok.z3ctable-1.6.0.tar.gz
  md5: b131409ccba56c444dc61ec183d644e9
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - megrok.z3ctable = megrok.z3ctable:main
    #
    # Would create an entry point called megrok.z3ctable that calls megrok.z3ctable.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - grokcore.component
    - grokcore.layout
    - grokcore.view >=1.12
    - martian
    - setuptools
    - z3c.table >=0.8
    - zope.component >=3.9.1
    - zope.publisher

  run:
    - python
    - setuptools
    - grokcore.component
    - grokcore.layout
    - grokcore.view >=1.12
    - martian
    - setuptools
    - z3c.table >=0.8
    - zope.component >=3.9.1
    - zope.publisher

test:
  # Python imports
  imports:
    - megrok
    - megrok.z3ctable
    - megrok.z3ctable.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/megrok.z3ctable
  license: ZPL
  summary: 'A table component package for Grok based on z3c.table.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
