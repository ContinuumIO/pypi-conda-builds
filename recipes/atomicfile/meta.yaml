package:
  name: atomicfile
  version: "1.0"

source:
  fn: atomicfile-1.0.tar.gz
  url: https://pypi.python.org/packages/source/a/atomicfile/atomicfile-1.0.tar.gz
  md5: ba355b6a5c56a9056f0c234cea8cf5fa
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - atomicfile = atomicfile:main
    #
    # Would create an entry point called atomicfile that calls atomicfile.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - atomicfile

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/sashka/atomicfile
  license: MIT
  summary: 'Writeable file object that atomically updates a file.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
