package:
  name: condorpy
  version: "0.0.0"

source:
  fn: condorpy-0.0.0.tar.gz
  url: https://pypi.python.org/packages/source/c/condorpy/condorpy-0.0.0.tar.gz
  md5: 9868d571c77749ea3034a7ee947824c7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - condorpy = condorpy:main
    #
    # Would create an entry point called condorpy that calls condorpy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - condorpy
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/sdc50/condorpy/wiki/Home
  license: BSD 2-Clause License
  summary: "A Python wrapper for HTCondor's cli"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
