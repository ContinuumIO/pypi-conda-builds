package:
  name: glamkit-performance
  version: "0.5.3"

source:
  fn: glamkit-performance-0.5.3.tar.gz
  url: https://pypi.python.org/packages/source/g/glamkit-performance/glamkit-performance-0.5.3.tar.gz
  md5: b039803ef22ed63e5f651d7c8b81d287
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - glamkit-performance = glamkit-performance:main
    #
    # Would create an entry point called glamkit-performance that calls glamkit-performance.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - mediahash
    - mediahash.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://glamkit.org/
  license: BSD License
  summary: 'A template tag to help with concurrent media serving from Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
