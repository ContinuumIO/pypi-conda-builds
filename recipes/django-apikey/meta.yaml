package:
  name: django-apikey
  version: "0.2.0"

source:
  fn: django-apikey-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-apikey/django-apikey-0.2.0.tar.gz
  md5: 186d9ecd3f7e2717cedc0e87711eb71e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-apikey = django-apikey:main
    #
    # Would create an entry point called django-apikey that calls django-apikey.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - apikey

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://www.github.com/jorgeecardona/django-apikey
  license: BSD License
  summary: 'Simple Key and Token authentication for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
