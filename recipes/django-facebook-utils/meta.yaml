package:
  name: django-facebook-utils
  version: "1.0.4"

source:
  fn: django-facebook-utils-1.0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-facebook-utils/django-facebook-utils-1.0.4.tar.gz
  md5: 6b1419f9eceab94002fef6e43b0a9d89
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-facebook-utils = django-facebook-utils:main
    #
    # Would create an entry point called django-facebook-utils that calls django-facebook-utils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - requests >=1.1.0

  run:
    - python
    - requests >=1.1.0

test:
  # Python imports
  imports:
    - facebook_utils
    - facebook_utils.management
    - facebook_utils.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/caioariede/django-facebook-utils/
  license: MIT License
  summary: 'Some Facebook utilities to use in Django projects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
