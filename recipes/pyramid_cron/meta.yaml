package:
  name: pyramid_cron
  version: "0.1"

source:
  fn: pyramid_cron-0.1.tar.gz
  url: https://pypi.python.org/packages/source/p/pyramid_cron/pyramid_cron-0.1.tar.gz
  md5: 50e285d76d409e9fe939356e01daa947
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pyramid_cron = pyramid_cron:main
    #
    # Would create an entry point called pyramid_cron that calls pyramid_cron.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyramid >=1.4.5
    - six

  run:
    - python
    - pyramid >=1.4.5
    - six

test:
  # Python imports
  imports:
    - pyramid_cron
    - pyramid_cron.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/cartlogic/pyramid_cron
  license: MIT License
  summary: 'Simple scheduled tasks for Pyramid.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
