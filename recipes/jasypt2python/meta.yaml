package:
  name: jasypt2python
  version: "1.0.1"

source:
  fn: Jasypt2Python-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/J/Jasypt2Python/Jasypt2Python-1.0.1.tar.gz
  md5: 128deab344600bc0de6b8d8036171fbd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jasypt2python = jasypt2python:main
    #
    # Would create an entry point called jasypt2python that calls jasypt2python.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pycrypto ==2.6.1

  run:
    - python
    - pycrypto ==2.6.1

test:
  # Python imports
  imports:
    - j2p
    - jasypt2python
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/TelmedIQ/jasypt-2-python
  license: UNKNOWN
  summary: 'Jasypt decryption library in Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
