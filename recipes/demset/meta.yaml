package:
  name: demset
  version: "1.0"

source:
  fn: demset-1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/demset/demset-1.0.tar.gz
  md5: b6f751ed75f6642d84e6fee3ee38d1db
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - demset = demset:main
    #
    # Would create an entry point called demset that calls demset.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - demset

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://deron.meranda.us/python/demset/
  license: Python Software Foundation License
  summary: 'set and frozenset classes for Python 2.2 and 2.3 compatible with Python 2.4 builtin types'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
