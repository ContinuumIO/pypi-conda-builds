package:
  name: dataspyre
  version: "0.1.9"

source:
  fn: DataSpyre-0.1.9.tar.gz
  url: https://pypi.python.org/packages/source/D/DataSpyre/DataSpyre-0.1.9.tar.gz
  md5: ddcb1e7e7baded30182ffc38736b2d71
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dataspyre = dataspyre:main
    #
    # Would create an entry point called dataspyre that calls dataspyre.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy
    - pandas
    - cherrypy
    - jinja2
    - matplotlib

  run:
    - python
    - numpy
    - pandas
    - cherrypy
    - jinja2
    - matplotlib

test:
  # Python imports
  imports:
    - dataspyre
    - spyre

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/adamhajari/spyre
  license: MIT License
  summary: 'Spyre makes it easy to build interactive web applications, and requires no knowledge of HTML, CSS, or Javascript.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
