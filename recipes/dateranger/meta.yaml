package:
  name: dateranger
  version: "0.3.0.alpha"

source:
  fn: DateRanger-0.3.0.alpha.tar.gz
  url: https://pypi.python.org/packages/source/D/DateRanger/DateRanger-0.3.0.alpha.tar.gz
  md5: 59d4b2a0a0831e3ea57128640aa9c44d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dateranger = dateranger:main
    #
    # Would create an entry point called dateranger that calls dateranger.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - DateRanger
    - dateranger

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/spitfire-sidra/DateRanger
  license: MIT License
  summary: 'Useful business date ranges'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
