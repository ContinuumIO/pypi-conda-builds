package:
  name: bambu-mail
  version: "2.1"

source:
  fn: bambu-mail-2.1.tar.gz
  url: https://pypi.python.org/packages/source/b/bambu-mail/bambu-mail-2.1.tar.gz
  md5: 68910b8581a5ec6940b2060ef64642c5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bambu-mail = bambu-mail:main
    #
    # Would create an entry point called bambu-mail that calls bambu-mail.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4
    - bambu-markup >=2.0
    - httplib2

  run:
    - python
    - django >=1.4
    - bambu-markup >=2.0
    - httplib2

test:
  # Python imports
  imports:
    - bambu_mail
    - bambu_mail.backends
    - bambu_mail.newsletter

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/iamsteadman/bambu-mail
  license: UNKNOWN
  summary: 'A shortcut function for sending template-based emails in HTML and plain-text format'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
