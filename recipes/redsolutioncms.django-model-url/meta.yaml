package:
  name: redsolutioncms.django-model-url
  version: "0.1.0"

source:
  fn: redsolutioncms.django-model-url-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/r/redsolutioncms.django-model-url/redsolutioncms.django-model-url-0.1.0.tar.gz
  md5: 3c9f428ed7984ccf1c2b63a106bed134
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - redsolutioncms.django-model-url = redsolutioncms.django-model-url:main
    #
    # Would create an entry point called redsolutioncms.django-model-url that calls redsolutioncms.django-model-url.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-url-methods ==0.1.0

  run:
    - python
    - setuptools
    - django-url-methods ==0.1.0

test:
  # Python imports
  imports:
    - modelurl
    - modelurl.redsolution_setup

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: LGPL
  summary: "Django model url keeps track your objects' urls and inserts dynamic links in content.\nDo not worry, that page has changed address. All the old links will continue to work."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
