package:
  name: wincertstore
  version: "0.2"

source:
  fn: wincertstore-0.2.zip
  url: https://pypi.python.org/packages/source/w/wincertstore/wincertstore-0.2.zip
  md5: ae728f2f007185648d0c7a8679b361e2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - wincertstore = wincertstore:main
    #
    # Would create an entry point called wincertstore that calls wincertstore.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/tiran/wincertstore
  license: Python Software Foundation License
  summary: "Python module to extract CA and CRL certs from Windows' cert store (ctypes based)."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
