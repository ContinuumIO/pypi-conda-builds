package:
  name: django-image-cropping
  version: "1.0.1"

source:
  fn: django-image-cropping-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-image-cropping/django-image-cropping-1.0.1.tar.gz
  md5: d069b16b453bebf44511487ad52e7e99
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-image-cropping = django-image-cropping:main
    #
    # Would create an entry point called django-image-cropping that calls django-image-cropping.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-appconf ==1.0.1

  run:
    - python
    - django-appconf ==1.0.1

test:
  # Python imports
  imports:
    - image_cropping
    - image_cropping.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/jonasundderwolf/django-image-cropping
  license: UNKNOWN
  summary: 'A reusable app for cropping images easily and non-destructively in Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
