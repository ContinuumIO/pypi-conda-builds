package:
  name: django-formfieldset
  version: "1.1"

source:
  fn: django-formfieldset-1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-formfieldset/django-formfieldset-1.1.tar.gz
  md5: 969bd0cf86828c1759d10c6c305c8788
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-formfieldset = django-formfieldset:main
    #
    # Would create an entry point called django-formfieldset that calls django-formfieldset.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - formfieldset
    - formfieldset.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/muhuk/django-formfieldset/tree/master
  license: BSD License
  summary: 'Fieldset Rendering For Non-Admin Forms'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
