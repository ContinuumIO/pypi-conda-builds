package:
  name: django-defaultsite
  version: "1.1"

source:
  fn: django-defaultsite-1.1.zip
  url: https://pypi.python.org/packages/source/d/django-defaultsite/django-defaultsite-1.1.zip
  md5: f9ce8d8066f613f81283237faaf91923
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-defaultsite = django-defaultsite:main
    #
    # Would create an entry point called django-defaultsite that calls django-defaultsite.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - defaultsite

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://oppian.com/labs/django-defaultsite/
  license: BSD License
  summary: 'django-defaultsiteSets the Site object in django to something better then example.com.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
