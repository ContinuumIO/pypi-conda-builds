package:
  name: frozen-flask
  version: "0.11"

source:
  fn: Frozen-Flask-0.11.tar.gz
  url: https://pypi.python.org/packages/source/F/Frozen-Flask/Frozen-Flask-0.11.tar.gz
  md5: bb1f2cf7d752868a0196070d9cd65b21
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - frozen-flask = frozen-flask:main
    #
    # Would create an entry point called frozen-flask that calls frozen-flask.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.7

  run:
    - python
    - flask >=0.7

test:
  # Python imports
  imports:
    - flask_frozen
    - flask_frozen.test_app
    - flask_frozen.test_app.admin

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/SimonSapin/Frozen-Flask
  license: BSD License
  summary: 'Freezes a Flask application into a set of static files.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
