package:
  name: cmsplugin-iframe
  version: "0.1.1"

source:
  fn: cmsplugin-iframe-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/c/cmsplugin-iframe/cmsplugin-iframe-0.1.1.tar.gz
  md5: e3d203573f6dedafb71eb6cc2244c688
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cmsplugin-iframe = cmsplugin-iframe:main
    #
    # Would create an entry point called cmsplugin-iframe that calls cmsplugin-iframe.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-cms
    - beautifulsoup4

  run:
    - python
    - django-cms
    - beautifulsoup4

test:
  # Python imports
  imports:
    - cmsplugin_iframe
    - cmsplugin_iframe.migrations
    - cmsplugin_iframe.south_migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/satyrius/cmsplugin-iframe
  license: MIT License
  summary: 'Django CMS page iframe plugin'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
