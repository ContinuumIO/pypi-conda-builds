package:
  name: buildout.disablessl
  version: "1.1"

source:
  fn: buildout.disablessl-1.1.tar.gz
  url: https://pypi.python.org/packages/source/b/buildout.disablessl/buildout.disablessl-1.1.tar.gz
  md5: 775fd2b6f878c4d5320d77f58d05e30c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - buildout.disablessl = buildout.disablessl:main
    #
    # Would create an entry point called buildout.disablessl that calls buildout.disablessl.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - buildout
    - buildout.disablessl

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/gocept/buildout.disablessl/
  license: ZPL 2.1
  summary: 'Disable SSL verification of setuptools for buildout'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
