package:
  name: django-qourisman
  version: "0.0.7"

source:
  fn: django-qourisman-0.0.7.tar.gz
  url: https://pypi.python.org/packages/source/d/django-qourisman/django-qourisman-0.0.7.tar.gz
  md5: 2ff07e5d27c9bc11daf0044c68a0693e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-qourisman = django-qourisman:main
    #
    # Would create an entry point called django-qourisman that calls django-qourisman.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - qourisman
    - qourisman.auth
    - qourisman.filterspecs
    - qourisman.listfilters
    - qourisman.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/django-district/django-qourisman
  license: http://code.djangoproject.com/browser/django/trunk/LICENSE
  summary: 'A collection of Django admin extensions'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
