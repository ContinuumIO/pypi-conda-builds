package:
  name: sphinx-epytext
  version: "0.0.3"

source:
  fn: sphinx-epytext-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/s/sphinx-epytext/sphinx-epytext-0.0.3.tar.gz
  md5: 0e74f557e23e676da3de8f77786196a9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sphinx-epytext = sphinx-epytext:main
    #
    # Would create an entry point called sphinx-epytext that calls sphinx-epytext.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sphinx

  run:
    - python
    - sphinx

test:
  # Python imports
  imports:
    - sphinx_epytext

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jayvdb/sphinx-epytext
  license: MIT License
  summary: 'Sphinx "epytext" extension.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
