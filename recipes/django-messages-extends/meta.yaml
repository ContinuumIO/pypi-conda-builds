package:
  name: django-messages-extends
  version: "0.4"

source:
  fn: django-messages-extends-0.4.zip
  url: https://pypi.python.org/packages/source/d/django-messages-extends/django-messages-extends-0.4.zip
  md5: 86f00831e123183ccfc46d5d7e5e7155
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-messages-extends = django-messages-extends:main
    #
    # Would create an entry point called django-messages-extends that calls django-messages-extends.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - messages_extends

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/AliLozano/django-messages-extends/
  license: MIT License
  summary: "A Django app for extends Django's messages framework, adds sticky messages and persistent messages"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
