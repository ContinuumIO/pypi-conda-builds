package:
  name: diffscuss
  version: "2.0.0"

source:
  fn: diffscuss-2.0.0.tar.gz
  url: https://pypi.python.org/packages/source/d/diffscuss/diffscuss-2.0.0.tar.gz
  md5: d2982608b1fbd27e4e990616bd333ac4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - diffscuss = diffscuss:main
    #
    # Would create an entry point called diffscuss that calls diffscuss.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - pygithub ==1.14.2
    - argparse ==1.2.1
    - requests ==1.2.0

  run:
    - python
    - pygithub ==1.14.2
    - argparse ==1.2.1
    - requests ==1.2.0

test:
  # Python imports
  imports:
    - diffscuss
    - diffscuss.mailbox
    - diffscuss.support

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/hut8labs/diffscuss/
  license: LICENSE.txt
  summary: 'Plain-text code review format and tools.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
