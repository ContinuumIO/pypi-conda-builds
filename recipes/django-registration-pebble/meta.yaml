package:
  name: django-registration-pebble
  version: "1.0.1a0"

source:
  fn: django-registration-pebble-1.0.1a0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-registration-pebble/django-registration-pebble-1.0.1a0.tar.gz
  md5: c082d815fe53448e58e9b447edaeb686
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-registration-pebble = django-registration-pebble:main
    #
    # Would create an entry point called django-registration-pebble that calls django-registration-pebble.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - registration
    - registration.backends
    - registration.backends.default
    - registration.backends.simple
    - registration.management
    - registration.management.commands
    - registration.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.bitbucket.org/scott_walton/django-registration/
  license: BSD License
  summary: 'Pebble experimental release of django-registration'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
