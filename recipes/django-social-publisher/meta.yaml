package:
  name: django-social-publisher
  version: "0.2.2"

source:
  fn: django-social-publisher-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-social-publisher/django-social-publisher-0.2.2.tar.gz
  md5: 3daef4f7726d38e3b2ed99cafc0c3c44
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-social-publisher = django-social-publisher:main
    #
    # Would create an entry point called django-social-publisher that calls django-social-publisher.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python-social-auth >=0.2

  run:
    - python
    - python-social-auth >=0.2

test:
  # Python imports
  imports:
    - social_publisher
    - social_publisher.auth
    - social_publisher.backends
    - social_publisher.handlers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/scailer/django-social-publisher/
  license: MIT License
  summary: 'Server-side content publishing extension for django-social-auth.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
