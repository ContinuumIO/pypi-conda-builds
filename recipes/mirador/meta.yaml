package:
  name: mirador
  version: "0.3.3"

source:
  fn: Mirador-0.3.3.tar.gz
  url: https://pypi.python.org/packages/source/M/Mirador/Mirador-0.3.3.tar.gz
  md5: ff65fc6330aa4f0710fd6f0f3af45eca
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mirador = mirador:main
    #
    # Would create an entry point called mirador that calls mirador.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - requests >=2.3.0

  run:
    - python
    - requests >=2.3.0

test:
  # Python imports
  imports:
    - mirador
    - mirador.errors
    - mirador.ext
    - mirador.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/mirador-cv/mirador-py
  license: LICENSE.txt
  summary: 'client for the Mirador image moderation API. (mirador.im)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
