package:
  name: aliyun
  version: "0.1.1"

source:
  fn: Aliyun-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/A/Aliyun/Aliyun-0.1.1.tar.gz
  md5: 3c8909aa6af4d02385b2623f8c9ce5c0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - aliyun = aliyun:main
    #
    # Would create an entry point called aliyun that calls aliyun.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - httplib2

  run:
    - python
    - httplib2

test:
  # Python imports
  imports:
    - aliyun
    - aliyun.modules
    - aliyun.modules.moye
    - aliyun.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://ods.aliyun.com/
  license: BSD License
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
