package:
  name: html5charref
  version: "0.1.0"

source:
  fn: html5charref-0.1.0.zip
  url: https://pypi.python.org/packages/source/h/html5charref/html5charref-0.1.0.zip
  md5: b101397cdad851c016c40a6867952bd4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - html5charref = html5charref:main
    #
    # Would create an entry point called html5charref that calls html5charref.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=2.2.0
    - beautifulsoup >=3.2.1

  run:
    - python
    - requests >=2.2.0
    - beautifulsoup >=3.2.1

test:
  # Python imports
  imports:
    - html5charref

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/bpabel/html5charref/
  license: MIT License
  summary: 'Python library for escaping/unescaping HTML5 Named Character References.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
