package:
  name: cubicweb-transactionlog
  version: "0.3.0"

source:
  fn: cubicweb-transactionlog-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/c/cubicweb-transactionlog/cubicweb-transactionlog-0.3.0.tar.gz
  md5: 5171b9a6590d22f8511f91dc06875b9d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cubicweb-transactionlog = cubicweb-transactionlog:main
    #
    # Would create an entry point called cubicweb-transactionlog that calls cubicweb-transactionlog.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cubicweb >=3.19.0

  run:
    - python
    - cubicweb >=3.19.0

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.cubicweb.org/project/cubicweb-transactionlog
  license: LGPL
  summary: 'Collect entities and relations modifications happening in transactions.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
