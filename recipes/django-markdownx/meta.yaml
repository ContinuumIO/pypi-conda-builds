package:
  name: django-markdownx
  version: "0.4.2"

source:
  fn: django-markdownx-0.4.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-markdownx/django-markdownx-0.4.2.tar.gz
  md5: f7da0cc9a0ccaec4f801682b6537edb3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-markdownx = django-markdownx:main
    #
    # Would create an entry point called django-markdownx that calls django-markdownx.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pillow
    - markdown

  run:
    - python
    - pillow
    - markdown

test:
  # Python imports
  imports:
    - markdownx

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/adi-/django-markdownx
  license: BSD License
  summary: 'Simple markdown editor (with live preview and images uploads) built for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
