package:
  name: docbucket
  version: "0.1"

source:
  fn: docbucket-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/docbucket/docbucket-0.1.tar.gz
  md5: 4a342d900064fecfcae21f651a9e8851
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - docbucket = docbucket:main
    #
    # Would create an entry point called docbucket that calls docbucket.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - docbucket
    - docbucket.management
    - docbucket.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://idevelop.org/p/docbucket/
  license: GPL3
  summary: 'A simple document manager for individuals written with Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
