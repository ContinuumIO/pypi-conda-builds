package:
  name: django-moneyfield
  version: "0.2.1"

source:
  fn: django-moneyfield-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-moneyfield/django-moneyfield-0.2.1.tar.gz
  md5: 104f2bcf8e782934594e61379b098fa9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-moneyfield = django-moneyfield:main
    #
    # Would create an entry point called django-moneyfield that calls django-moneyfield.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - moneyfield

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/carlospalol/django-moneyfield
  license: MIT License
  summary: 'Django Money Model Field'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
