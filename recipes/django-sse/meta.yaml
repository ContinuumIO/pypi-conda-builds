package:
  name: django-sse
  version: "0.4.1"

source:
  fn: django-sse-0.4.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-sse/django-sse-0.4.1.tar.gz
  md5: 58b0d663aac65b3757e123f09f274665
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-sse = django-sse:main
    #
    # Would create an entry point called django-sse that calls django-sse.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - django_sse

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/niwibe/django-sse
  license: BSD License
  summary: 'HTML5 Server-Side Events for Django 1.3+'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
