package:
  name: plone.session
  version: "3.5.5"

source:
  fn: plone.session-3.5.5.tar.gz
  url: https://pypi.python.org/packages/source/p/plone.session/plone.session-3.5.5.tar.gz
  md5: 673858b7f96d4d5c1a3c1fd830a22fa9
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.session = plone.session:main
    #
    # Would create an entry point called plone.session that calls plone.session.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - plone.keyring
    - plone.protect
    - zope.component
    - zope.interface
    - products.pluggableauthservice
    - zope2

  run:
    - python
    - setuptools
    - plone.keyring
    - plone.protect
    - zope.component
    - zope.interface
    - products.pluggableauthservice
    - zope2

test:
  # Python imports
  imports:
    - plone
    - plone.session
    - plone.session.plugins
    - plone.session.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/plone.session
  license: BSD License
  summary: 'Session based authentication for Zope'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
