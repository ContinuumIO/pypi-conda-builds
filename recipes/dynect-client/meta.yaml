package:
  name: dynect-client
  version: "0.2.15"

source:
  fn: dynect-client-0.2.15.tar.gz
  url: https://pypi.python.org/packages/source/d/dynect-client/dynect-client-0.2.15.tar.gz
  md5: d2c35afa2f84886fbc297b25f9cb7458
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dynect-client = dynect-client:main
    #
    # Would create an entry point called dynect-client that calls dynect-client.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - dynect_client

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ZachGoldberg/Dynect-REST-Python-Client
  license: UNKNOWN
  summary: 'Dynect REST API Client for adding/removing domains'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
