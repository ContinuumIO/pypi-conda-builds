package:
  name: cmsplugin-ink-news
  version: "0.9.1"

source:
  fn: cmsplugin-ink-news-0.9.1.tar.gz
  url: https://pypi.python.org/packages/source/c/cmsplugin-ink-news/cmsplugin-ink-news-0.9.1.tar.gz
  md5: 7545962d62cebb3ecdc0d8a58b789d4b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cmsplugin-ink-news = cmsplugin-ink-news:main
    #
    # Would create an entry point called cmsplugin-ink-news that calls cmsplugin-ink-news.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django-cms >=3.0
    - pytz

  run:
    - python
    - django-cms >=3.0
    - pytz

test:
  # Python imports
  imports:
    - cmsplugin_news

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://code.launchpad.net/~doctormo
  license: AGPLv3
  summary: "The modified news plugin used for inkscape's website."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
