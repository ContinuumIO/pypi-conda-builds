package:
  name: armory
  version: "0.1.0"

source:
  fn: armory-0.1.0.zip
  url: https://pypi.python.org/packages/source/a/armory/armory-0.1.0.zip
  md5: a26a80efd56d0f5f078018ce55ead665
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - armory = armory:main
    #
    # Would create an entry point called armory that calls armory.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - armory
    - armory.classviews
    - armory.db
    - armory.regex
    - armory.utils
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://packages.python.org/armory
  license: Apache Software License
  summary: 'A library of python and django tools and scripts.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
