package:
  name: django-request
  version: "1.3.0"

source:
  fn: django-request-1.3.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-request/django-request-1.3.0.tar.gz
  md5: 1edfa8e6baeca31cdfcfbcd821e96714
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-request = django-request:main
    #
    # Would create an entry point called django-request that calls django-request.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - request
    - request.management
    - request.management.commands
    - request.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://django-request.readthedocs.org/en/latest/
  license: BSD License
  summary: 'django-request is a statistics module for django. It stores requests in a database for admins to see, it can also be used to get statistics on who is online etc.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
