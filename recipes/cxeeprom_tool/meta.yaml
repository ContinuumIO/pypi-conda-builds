package:
  name: cxeeprom_tool
  version: "3.0"

source:
  fn: cxeeprom_tool-3.0.tar.gz
  url: https://pypi.python.org/packages/source/c/cxeeprom_tool/cxeeprom_tool-3.0.tar.gz
  md5: ca066c7d1ccf7538ea0edef4b49ec8ab
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cxeeprom_tool = cxeeprom_tool:main
    #
    # Would create an entry point called cxeeprom_tool that calls cxeeprom_tool.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - argparse

  run:
    - python
    - argparse

test:
  # Python imports
  imports:
    - cxeeprom_tool
    - cxeeprom_tool/defs

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD License
  summary: 'Calxeda Configuration EEPROM image tool'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
