package:
  name: etym
  version: "0.0.2"

source:
  fn: etym-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/e/etym/etym-0.0.2.tar.gz
  md5: 63bc33e2fe173f0d79c2dc1350a651c9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - etym = etym:main
    #
    # Would create an entry point called etym that calls etym.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - beautifulsoup >=3.2.1
    - blessings >=1.6
    - docopt >=0.6.2
    - requests >=2.6.0

  run:
    - python
    - beautifulsoup >=3.2.1
    - blessings >=1.6
    - docopt >=0.6.2
    - requests >=2.6.0

test:
  # Python imports
  imports:
    - etym

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/conorsch/etym
  license: UNKNOWN
  summary: 'Command-line interface for EtymOnline.com.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
