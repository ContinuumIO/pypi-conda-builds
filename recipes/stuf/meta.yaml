package:
  name: stuf
  version: "0.9.16"

source:
  fn: stuf-0.9.16.tar.bz2
  url: https://pypi.python.org/packages/source/s/stuf/stuf-0.9.16.tar.bz2
  md5: da0cee8c819315073e1b424cb4d07639
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - stuf = stuf:main
    #
    # Would create an entry point called stuf that calls stuf.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - parse >=1.6.6

  run:
    - python
    - parse >=1.6.6

test:
  # Python imports
  imports:
    - stuf
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/lcrees/stuf
  license: BSD License
  summary: 'Normal, default, ordered, chained, restricted, counter, and frozen dictionaries with attribute-style access.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
