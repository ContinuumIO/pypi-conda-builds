package:
  name: django-rainbowtests
  version: "0.3.0"

source:
  fn: django-rainbowtests-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-rainbowtests/django-rainbowtests-0.3.0.tar.gz
  md5: 8d1a5bcdf8bf699a71f103ab7cbff1fd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-rainbowtests = django-rainbowtests:main
    #
    # Would create an entry point called django-rainbowtests that calls django-rainbowtests.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - rainbowtests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bradmontgomery/django-rainbowtests
  license: MIT License
  summary: 'A colorful Django Test Runner.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
