package:
  name: jenkins_tools
  version: "0.0.69"

source:
  fn: jenkins_tools-0.0.69.tar.gz
  url: https://pypi.python.org/packages/source/j/jenkins_tools/jenkins_tools-0.0.69.tar.gz
  md5: 210fb7c2a57ed2e36ee6753611ff4630
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jenkins_tools = jenkins_tools:main
    #
    # Would create an entry point called jenkins_tools that calls jenkins_tools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - empy
    - pyyaml
    - jenkins
    - argparse
    - rosdep
    - rosdistro >=0.2.7
    - rospkg
    - catkin-pkg
    - distribute

  run:
    - python
    - empy
    - pyyaml
    - jenkins
    - argparse
    - rosdep
    - rosdistro >=0.2.7
    - rospkg
    - catkin-pkg
    - distribute

test:
  # Python imports
  imports:
    - jenkins_tools

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://wiki.ros.org/jenkins_tools
  license: BSD License
  summary: 'A tool for running scripts in a chroot environment on Jenkins or locally'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
