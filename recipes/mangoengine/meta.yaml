package:
  name: mangoengine
  version: "v1.0.0-rc.2"

source:
  fn: MangoEngine-v1.0.0-rc.2.tar.gz
  url: https://pypi.python.org/packages/source/M/MangoEngine/MangoEngine-v1.0.0-rc.2.tar.gz
  md5: a71387214319838f2e59dd82a53107b5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mangoengine = mangoengine:main
    #
    # Would create an entry point called mangoengine that calls mangoengine.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - mangoengine
    - mangoengine.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://www.github.com/brownhead/mangoengine
  license: Unlicense
  summary: 'MangoEngine is a lightweight library for creating generic data models in Python.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
