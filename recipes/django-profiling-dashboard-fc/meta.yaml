package:
  name: django-profiling-dashboard-fc
  version: "1.0"

source:
  fn: django-profiling-dashboard-fc-1.0.zip
  url: https://pypi.python.org/packages/source/d/django-profiling-dashboard-fc/django-profiling-dashboard-fc-1.0.zip
  md5: 4aac27526ceadb6f2a9caabb26451a1b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-profiling-dashboard-fc = django-profiling-dashboard-fc:main
    #
    # Would create an entry point called django-profiling-dashboard-fc that calls django-profiling-dashboard-fc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3
    - yappi ==0.62
    - psutil ==0.7.0
    - pympler ==0.3.1
    - django-query-exchange-fc ==1.0

  run:
    - python
    - django >=1.3
    - yappi ==0.62
    - psutil ==0.7.0
    - pympler ==0.3.1
    - django-query-exchange-fc ==1.0

test:
  # Python imports
  imports:
    - profiling_dashboard

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kmike/django-profiling-dashboard
  license: MIT License
  summary: ' Django profiling dashboard for debugging CPU, memory and other resources usage in live servers '

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
