package:
  name: smartbus-client-python
  version: "1.7.1"

source:
  fn: smartbus-client-python-1.7.1.zip
  url: https://pypi.python.org/packages/source/s/smartbus-client-python/smartbus-client-python-1.7.1.zip
  md5: 77fdbe0be7dfe83d64485d1bb4224d6d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - smartbus-client-python = smartbus-client-python:main
    #
    # Would create an entry point called smartbus-client-python that calls smartbus-client-python.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - smartbus
    - smartbus.ipcclient
    - smartbus.netclient

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Hesong-OpenSource/smartbus-client-python
  license: MIT License
  summary: "Python wrapper for Hesong(GuangZhou)'s IPSC-Smartbus client"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
