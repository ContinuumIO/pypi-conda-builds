package:
  name: zope.app.dublincore
  version: "3.4.0"

source:
  fn: zope.app.dublincore-3.4.0.tar.gz
  url: https://pypi.python.org/packages/source/z/zope.app.dublincore/zope.app.dublincore-3.4.0.tar.gz
  md5: 1fe54a1b1e22dd8a4c77ff9f27310db4
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zope.app.dublincore = zope.app.dublincore:main
    #
    # Would create an entry point called zope.app.dublincore that calls zope.app.dublincore.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.dublincore
    - zope.deprecation

  run:
    - python
    - setuptools
    - zope.dublincore
    - zope.deprecation

test:
  # Python imports
  imports:
    - zope
    - zope.app
    - zope.app.dublincore

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/zope.app.dublincore
  license: ZPL 2.1
  summary: 'A deprecated backwards compatibility package\n                     which will go away in 3.5'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
