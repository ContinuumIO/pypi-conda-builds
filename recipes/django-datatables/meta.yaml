package:
  name: django-datatables
  version: "0.1.0-r35"

source:
  fn: django-datatables-0.1.0-r35.tar.gz
  url: https://pypi.python.org/packages/source/d/django-datatables/django-datatables-0.1.0-r35.tar.gz
  md5: 2c65b05cf99b58ec9486c75deb41670d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-datatables = django-datatables:main
    #
    # Would create an entry point called django-datatables that calls django-datatables.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3
    - decorator

  run:
    - python
    - django >=1.3
    - decorator

test:
  # Python imports
  imports:
    - datatables
    - datatables.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://projects.ninemoreminutes.com/projects/django-datatables/
  license: BSD License
  summary: 'Django integration of DataTables jQuery plugin.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
