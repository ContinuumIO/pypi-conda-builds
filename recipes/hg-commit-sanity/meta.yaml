package:
  name: hg-commit-sanity
  version: "0.0.1"

source:
  fn: hg-commit-sanity-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/h/hg-commit-sanity/hg-commit-sanity-0.0.1.tar.gz
  md5: ab11373c4dbf2a30f9120e1050ee6263
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hg-commit-sanity = hg-commit-sanity:main
    #
    # Would create an entry point called hg-commit-sanity that calls hg-commit-sanity.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - mercurial

  run:
    - python
    - mercurial

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/paylogic/hg-commit-sanity
  license: MIT License
  summary: 'Mercurial extension to do sanity checks on commits.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
