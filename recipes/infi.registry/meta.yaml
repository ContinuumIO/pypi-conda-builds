package:
  name: infi.registry
  version: "0.1.17"

source:
  fn: infi.registry-0.1.17.tar.gz
  url: https://pypi.python.org/packages/source/i/infi.registry/infi.registry-0.1.17.tar.gz
  md5: e865b1839decb1f93c353d998f5e791d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - infi.registry = infi.registry:main
    #
    # Would create an entry point called infi.registry that calls infi.registry.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - munch
    - setuptools

  run:
    - python
    - munch
    - setuptools

test:
  # Python imports
  imports:
    - infi
    - infi.registry
    - infi.registry.interface

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Infinidat/infi.registry
  license: Python Software Foundation License
  summary: 'a Python dictionary-like interface to the Windows Registry'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
