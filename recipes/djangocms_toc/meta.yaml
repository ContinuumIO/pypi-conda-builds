package:
  name: djangocms_toc
  version: "1.0.1"

source:
  fn: djangocms_toc-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/djangocms_toc/djangocms_toc-1.0.1.tar.gz
  md5: bc6b769f4ad29d57f855d9ca78e45236
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangocms_toc = djangocms_toc:main
    #
    # Would create an entry point called djangocms_toc that calls djangocms_toc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - djangocms_toc
    - djangocms_toc.migrations
    - djangocms_toc.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: None
  license: AGPLv3
  summary: 'Table of Contents Plugin for django CMS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
