package:
  name: django_rungevent
  version: "0.3.3"

source:
  fn: django_rungevent-0.3.3.zip
  url: https://pypi.python.org/packages/source/d/django_rungevent/django_rungevent-0.3.3.zip
  md5: 7be53173ab97f4c3353aeb7cbef1f800
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_rungevent = django_rungevent:main
    #
    # Would create an entry point called django_rungevent that calls django_rungevent.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - django_rungevent
    - django_rungevent.management
    - django_rungevent.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tr4n2uil/django-rungevent
  license: MIT
  summary: 'Django Gevent Run Command'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
