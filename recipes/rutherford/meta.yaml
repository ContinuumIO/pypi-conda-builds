package:
  name: rutherford
  version: "0.7.1"

source:
  fn: rutherford-0.7.1.tar.gz
  url: https://pypi.python.org/packages/source/r/rutherford/rutherford-0.7.1.tar.gz
  md5: 6b01551bc17c14032964187ee54d7751
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - rutherford = rutherford:main
    #
    # Would create an entry point called rutherford that calls rutherford.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pytz
    - tzlocal

  run:
    - python
    - setuptools
    - pytz
    - tzlocal

test:
  # Python imports
  imports:
    - rutherford

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/sgillies/rutherford
  license: BSD
  summary: 'Atom feed for Tinkerer'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
