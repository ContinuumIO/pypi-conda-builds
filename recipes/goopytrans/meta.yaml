package:
  name: goopytrans
  version: "0.2"

source:
  fn: goopytrans-0.2.tar.gz
  url: https://pypi.python.org/packages/source/g/goopytrans/goopytrans-0.2.tar.gz
  md5: 91398e1478fdeb9fce9ee674690cb729
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - goopytrans = goopytrans:main
    #
    # Would create an entry point called goopytrans that calls goopytrans.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - goopytrans

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/j2labs/goopytrans
  license: UNKNOWN
  summary: "Python Client Library for Google's translation API"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
