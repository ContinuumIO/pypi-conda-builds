package:
  name: flatty
  version: "0.1.2"

source:
  fn: flatty-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/f/flatty/flatty-0.1.2.tar.gz
  md5: 6881439cbfbbce669c67e40a34742cd5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flatty = flatty:main
    #
    # Would create an entry point called flatty that calls flatty.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - flatty
    - flatty.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://packages.python.org/flatty
  license: BSD License
  summary: 'flatty - marshaller/unmarshaller for light-schema python objects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
