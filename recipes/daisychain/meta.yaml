package:
  name: daisychain
  version: "0.1.1"

source:
  fn: daisychain-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/daisychain/daisychain-0.1.1.tar.gz
  md5: 71cd1931523939f4d5ebada5ba06abe8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - daisychain = daisychain:main
    #
    # Would create an entry point called daisychain that calls daisychain.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - py3compat >=0.2

  run:
    - python
    - py3compat >=0.2

test:
  # Python imports
  imports:
    - daisychain
    - daisychain.steps
    - daisychain.steps.authentication
    - daisychain.steps.compilers
    - daisychain.steps.inputs
    - daisychain.steps.outputs
    - daisychain.steps.pipes
    - daisychain.steps.system
    - test
    - test.mock_requests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/python-daisychain/daisychain
  license: MIT License
  summary: 'Configuration-based OO-dependency resolution workflow engine'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
