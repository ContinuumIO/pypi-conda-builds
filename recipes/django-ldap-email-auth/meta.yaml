package:
  name: django-ldap-email-auth
  version: "0.1.1"

source:
  fn: django-ldap-email-auth-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-ldap-email-auth/django-ldap-email-auth-0.1.1.tar.gz
  md5: 5824c69b7dfc290f2280dedea715a337
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-ldap-email-auth = django-ldap-email-auth:main
    #
    # Would create an entry point called django-ldap-email-auth that calls django-ldap-email-auth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6
    - python-ldap >=2.4.16
    - django-auth-ldap >=1.2.3

  run:
    - python
    - django >=1.6
    - python-ldap >=2.4.16
    - django-auth-ldap >=1.2.3

test:
  # Python imports
  imports:
    - ldap_email_auth

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/dpaw/django-ldap-email-auth
  license: Apache Software License
  summary: 'Active Directory LDAP email authentication settings for Django projects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
