package:
  name: backslant
  version: "0.2.8"

source:
  fn: backslant-0.2.8.tar.gz
  url: https://pypi.python.org/packages/source/b/backslant/backslant-0.2.8.tar.gz
  md5: 6fd265ca5cea88fb51ee74076088d8ef
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - backslant = backslant:main
    #
    # Would create an entry point called backslant that calls backslant.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - funcparserlib
    - markupsafe

  run:
    - python
    - funcparserlib
    - markupsafe

test:
  # Python imports
  imports:
    - backslant

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Deepwalker/backslant/
  license: BSD License
  summary: 'Python template engine.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
