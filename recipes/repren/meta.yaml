package:
  name: repren
  version: "0.3.4"

source:
  fn: repren-0.3.4.tar.gz
  url: https://pypi.python.org/packages/source/r/repren/repren-0.3.4.tar.gz
  md5: a24501bb04a06a33fd621f25f3fac63d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - repren = repren:main
    #
    # Would create an entry point called repren that calls repren.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - repren

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jlevy/repren
  license: Apache Software License
  summary: 'repren: Multi-pattern string replacement and file renaming'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
