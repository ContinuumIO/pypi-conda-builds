package:
  name: eaternet-adapters
  version: "0.1.2"

source:
  fn: eaternet-adapters-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/e/eaternet-adapters/eaternet-adapters-0.1.2.tar.gz
  md5: 170cb98532b835f4d036f03a4377d2fc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - eaternet-adapters = eaternet-adapters:main
    #
    # Would create an entry point called eaternet-adapters that calls eaternet-adapters.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - eaternet.adapters
    - eaternet.adapters.agencies
    - eaternet.adapters.framework

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/eaternet/adapters-python
  license: Apache Software License
  summary: 'Python plugins for restaurant inspection data'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
