package:
  name: sqla_mixins
  version: "0.6"

source:
  fn: sqla_mixins-0.6.tar.gz
  url: https://pypi.python.org/packages/source/s/sqla_mixins/sqla_mixins-0.6.tar.gz
  md5: ea56f3d924e99f92ae5b59c53ab2d1c6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sqla_mixins = sqla_mixins:main
    #
    # Would create an entry point called sqla_mixins that calls sqla_mixins.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - passlib
    - sqlalchemy

  run:
    - python
    - passlib
    - sqlalchemy

test:
  # Python imports
  imports:
    - sqla_mixins

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bboe/sqla_mixins
  license: BSD License
  summary: 'A python module that contains a number of simple declarative sqlalchemy mixins.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
