package:
  name: lovely.responsecache
  version: "1.4.1"

source:
  fn: lovely.responsecache-1.4.1.tar.gz
  url: https://pypi.python.org/packages/source/l/lovely.responsecache/lovely.responsecache-1.4.1.tar.gz
  md5: 1aadac776f30c3748482053deedc4206
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lovely.responsecache = lovely.responsecache:main
    #
    # Would create an entry point called lovely.responsecache that calls lovely.responsecache.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - lovely.memcached
    - zope.app.publication
    - zope.app.security
    - zope.contentprovider
    - zope.formlib
    - zope.publisher
    - zope.schema
    - zope.security
    - zope.traversing

  run:
    - python
    - setuptools
    - lovely.memcached
    - zope.app.publication
    - zope.app.security
    - zope.contentprovider
    - zope.formlib
    - zope.publisher
    - zope.schema
    - zope.security
    - zope.traversing

test:
  # Python imports
  imports:
    - lovely
    - lovely.responsecache
    - lovely.responsecache.testing

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.zope.org/lovely.responsecache
  license: ZPL 2.1
  summary: 'Cache results of ContentProviders'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
