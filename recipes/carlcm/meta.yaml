package:
  name: carlcm
  version: "0.1.12"

source:
  fn: carlcm-0.1.12.tar.gz
  url: https://pypi.python.org/packages/source/c/carlcm/carlcm-0.1.12.tar.gz
  md5: c8f25dd086271dec8c13349d534c39be
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - carlcm = carlcm:main
    #
    # Would create an entry point called carlcm that calls carlcm.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - jinja2

  run:
    - python
    - jinja2

test:
  # Python imports
  imports:
    - carlcm
    - carlcm.actions
    - carlcm.modules
    - carlcm.roles

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/cjmackey/carlcm
  license: GNU General Public License v3 (GPLv3)
  summary: 'CarlCM'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
