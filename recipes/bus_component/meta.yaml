package:
  name: bus_component
  version: "0.1.0"

source:
  fn: bus_component-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/b/bus_component/bus_component-0.1.0.tar.gz
  md5: 555f6750297c48b5fcea458427116ece
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bus_component = bus_component:main
    #
    # Would create an entry point called bus_component that calls bus_component.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - bus_component

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: git://github.com/borrey/bus-component.git
  license: UNKNOWN
  summary: 'amqp bus communication lirbrary to pass infromation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
