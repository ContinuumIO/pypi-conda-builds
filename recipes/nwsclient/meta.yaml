package:
  name: nwsclient
  version: "1.6.3"

source:
  fn: nwsclient-1.6.3.tar.gz
  url: https://pypi.python.org/packages/source/n/nwsclient/nwsclient-1.6.3.tar.gz
  md5: 6087efcc26ec94fa39d8d816c3cf09bd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - nwsclient = nwsclient:main
    #
    # Would create an entry point called nwsclient that calls nwsclient.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - nws
    - nwsclient

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://nws-py.sourceforge.net/
  license: GNU General Public License (GPL)
  summary: 'Python NetWorkSpaces'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
