package:
  name: hgvs
  version: "0.3.3"

source:
  fn: hgvs-0.3.3.tar.gz
  url: https://pypi.python.org/packages/source/h/hgvs/hgvs-0.3.3.tar.gz
  md5: 0454aa4a68333c5be4cff8a6e3533de3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hgvs = hgvs:main
    #
    # Would create an entry point called hgvs that calls hgvs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - psycopg2
    - pysam
    - biopython
    - parsley
    - recordtype

  run:
    - python
    - psycopg2
    - pysam
    - biopython
    - parsley
    - recordtype

test:
  # Python imports
  imports:
    - hgvs
    - hgvs.dataproviders
    - hgvs.decorators
    - hgvs.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/hgvs/hgvs
  license: Apache Software License
  summary: 'HGVS Parser, Formatter, Mapper, Validator'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
