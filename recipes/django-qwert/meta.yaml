package:
  name: django-qwert
  version: "0.2.2"

source:
  fn: django-qwert-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-qwert/django-qwert-0.2.2.tar.gz
  md5: d00c43bab80842a507b570f6b0b6d67b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-qwert = django-qwert:main
    #
    # Would create an entry point called django-qwert that calls django-qwert.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - distribute
    - setuptools-git
    - mock
    - django >=1.3

  run:
    - python
    - distribute
    - setuptools-git
    - mock
    - django >=1.3

test:
  # Python imports
  imports:
    - qwert
    - qwert.decorators
    - qwert.http
    - qwert.management
    - qwert.middleware
    - qwert.templatetags
    - qwert.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lambdalisue/django-qwert
  license: MIT License
  summary: 'A trivia collection framework for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
