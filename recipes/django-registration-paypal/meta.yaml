package:
  name: django-registration-paypal
  version: "0.1.2"

source:
  fn: django-registration-paypal-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-registration-paypal/django-registration-paypal-0.1.2.tar.gz
  md5: 95d9caaebe98ee4205636728acccfd9a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-registration-paypal = django-registration-paypal:main
    #
    # Would create an entry point called django-registration-paypal that calls django-registration-paypal.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - paypal_registration

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/buchuki/django-registration-paypal
  license: LICENSE.txt
  summary: 'django-registration backend to support paypal payments'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
