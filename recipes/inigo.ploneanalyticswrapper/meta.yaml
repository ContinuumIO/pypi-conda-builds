package:
  name: inigo.ploneanalyticswrapper
  version: "1.0"

source:
  fn: inigo.ploneanalyticswrapper-1.0.zip
  url: https://pypi.python.org/packages/source/i/inigo.ploneanalyticswrapper/inigo.ploneanalyticswrapper-1.0.zip
  md5: 482c86849993b8d2c45e4d0e261e235a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - inigo.ploneanalyticswrapper = inigo.ploneanalyticswrapper:main
    #
    # Would create an entry point called inigo.ploneanalyticswrapper that calls inigo.ploneanalyticswrapper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - inigo
    - inigo.ploneanalyticswrapper

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/inigoconsulting/inigo.ploneanalyticswrapper
  license: gpl
  summary: 'A wrapper for plone.analytics viewlet which adds a selectable DIV around it'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
