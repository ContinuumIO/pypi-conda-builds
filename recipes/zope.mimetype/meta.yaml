package:
  name: zope.mimetype
  version: "2.0.0"

source:
  fn: zope.mimetype-2.0.0.tar.gz
  url: https://pypi.python.org/packages/source/z/zope.mimetype/zope.mimetype-2.0.0.tar.gz
  md5: 8f94843ea606189b58e8e1565e95c54d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zope.mimetype = zope.mimetype:main
    #
    # Would create an entry point called zope.mimetype that calls zope.mimetype.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.browser
    - zope.browserresource
    - zope.component
    - zope.configuration
    - zope.contenttype >=3.5
    - zope.event
    - zope.formlib >=4.0
    - zope.i18n
    - zope.i18nmessageid
    - zope.interface
    - zope.publisher
    - zope.schema
    - zope.security

  run:
    - python
    - setuptools
    - zope.browser
    - zope.browserresource
    - zope.component
    - zope.configuration
    - zope.contenttype >=3.5
    - zope.event
    - zope.formlib >=4.0
    - zope.i18n
    - zope.i18nmessageid
    - zope.interface
    - zope.publisher
    - zope.schema
    - zope.security

test:
  # Python imports
  imports:
    - zope
    - zope.mimetype

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/zope.mimetype
  license: Zope Public License
  summary: 'A simple package for working with MIME content types'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
