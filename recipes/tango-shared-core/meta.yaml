package:
  name: tango-shared-core
  version: "0.14.2"

source:
  fn: tango-shared-core-0.14.2.tar.gz
  url: https://pypi.python.org/packages/source/t/tango-shared-core/tango-shared-core-0.14.2.tar.gz
  md5: 5bf0e7a853e102bc6a3c38b4bb4db8e6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tango-shared-core = tango-shared-core:main
    #
    # Would create an entry point called tango-shared-core that calls tango-shared-core.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6.0,<1.8
    - bleach >=1.2.2
    - django-filter >=0.6
    - docutils >=0.10
    - easy-thumbnails >=1.3
    - markdown >=2.3.1
    - pillow >=2.1.0
    - six
    - smartypants >=1.6.0.3
    - tango-admin >=0.2
    - tango-voting >=0.2
    - typogrify >=2.0.0
    - xmltramp2

  run:
    - python
    - django >=1.6.0,<1.8
    - bleach >=1.2.2
    - django-filter >=0.6
    - docutils >=0.10
    - easy-thumbnails >=1.3
    - markdown >=2.3.1
    - pillow >=2.1.0
    - six
    - smartypants >=1.6.0.3
    - tango-admin >=0.2
    - tango-voting >=0.2
    - typogrify >=2.0.0
    - xmltramp2

test:
  # Python imports
  imports:
    - tango_shared
    - tango_shared.templatetags
    - tango_shared.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tBaxter/tango-shared-core
  license: MIT License
  summary: 'Tango shared/core functionality.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
