package:
  name: django-bookmarks
  version: "0.1.0"

source:
  fn: django-bookmarks-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-bookmarks/django-bookmarks-0.1.0.tar.gz
  md5: 08257a840d34a8befefa23ed53e2f08b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-bookmarks = django-bookmarks:main
    #
    # Would create an entry point called django-bookmarks that calls django-bookmarks.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - bookmarks

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/django-bookmarks/
  license: MIT License
  summary: 'A reusable Django app for bookmark management.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
