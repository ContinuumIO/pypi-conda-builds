package:
  name: cyberoam-candy
  version: "0.23"

source:
  fn: cyberoam-candy-0.23.tar.gz
  url: https://pypi.python.org/packages/source/c/cyberoam-candy/cyberoam-candy-0.23.tar.gz
  md5: 02cfc09cbe8f86154cecdd3e2d867b94
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cyberoam-candy = cyberoam-candy:main
    #
    # Would create an entry point called cyberoam-candy that calls cyberoam-candy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - cyberoam_candy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/bhavul/cyberoam-candy
  license: MIT
  summary: 'Basic Cyberoam login client. Command line interface.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
