package:
  name: arsenal
  version: "1.01"

source:
  fn: arsenal-1.01.tar.gz
  url: https://pypi.python.org/packages/source/a/arsenal/arsenal-1.01.tar.gz
  md5: 93727d3590f1a0df78a8aeb3422b0e17
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - arsenal = arsenal:main
    #
    # Would create an entry point called arsenal that calls arsenal.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy

  run:
    - python
    - numpy

test:
  # Python imports
  imports:
    - arsenal
    - arsenal.cache
    - arsenal.datastructures
    - arsenal.debug
    - arsenal.diagram
    - arsenal.math
    - arsenal.nlp
    - arsenal.nlp.data
    - arsenal.nlp.lexicon
    - arsenal.nlp.lexicon.names
    - arsenal.nlp.similarity
    - arsenal.nlp.tests
    - arsenal.plot
    - arsenal.profiling
    - arsenal.recipes
    - arsenal.sendmail
    - arsenal.terminal
    - arsenal.testing
    - arsenal.text
    - arsenal.unit_conversion
    - arsenal.useless
    - arsenal.web

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/timvieira/arsenal/
  license: UNKNOWN
  summary: 'Utils.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
