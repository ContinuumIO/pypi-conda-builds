package:
  name: django-push-notify
  version: "0.1.0"

source:
  fn: django-push-notify-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-push-notify/django-push-notify-0.1.0.tar.gz
  md5: 819488e05f2b7b72798d287e74127bbb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-push-notify = django-push-notify:main
    #
    # Would create an entry point called django-push-notify that calls django-push-notify.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django >=1.4.0
    - django-json-field ==0.5.1

  run:
    - python
    - django >=1.4.0
    - django-json-field ==0.5.1

test:
  # Python imports
  imports:
    - push_notify

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/n8j/django-push-notify
  license: MIT
  summary: 'Django app for sending ios push notifications'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
