package:
  name: collective.periodcriterion
  version: "0.2.0"

source:
  fn: collective.periodcriterion-0.2.0.zip
  url: https://pypi.python.org/packages/source/c/collective.periodcriterion/collective.periodcriterion-0.2.0.zip
  md5: 4526968698cb959b147aeda5137267f1
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.periodcriterion = collective.periodcriterion:main
    #
    # Would create an entry point called collective.periodcriterion that calls collective.periodcriterion.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.cmfplone >=4.2
    - plone.app.querystring

  run:
    - python
    - setuptools
    - setuptools
    - products.cmfplone >=4.2
    - plone.app.querystring

test:
  # Python imports
  imports:
    - collective
    - collective.periodcriterion

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/collective.periodcriterion
  license: GPL
  summary: 'Add to Plone Collection content type the "this month" and "this year" criteria'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
