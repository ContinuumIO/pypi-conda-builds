package:
  name: investigate
  version: "1.0.0"

source:
  fn: investigate-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/i/investigate/investigate-1.0.0.tar.gz
  md5: 64f5e47815ec68af59b6251b64fe261f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - investigate = investigate:main
    #
    # Would create an entry point called investigate that calls investigate.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - investigate

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dead10ck/pyinvestigate.git
  license: MIT
  summary: 'Python interface for the OpenDNS Investigate API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
