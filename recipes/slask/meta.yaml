package:
  name: slask
  version: "0.1.1"

source:
  fn: slask-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/s/slask/slask-0.1.1.tar.gz
  md5: bc1bf065a78de7754dd93dae145bd24e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - slask = slask:main
    #
    # Would create an entry point called slask that calls slask.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.0.0
    - requests >=2.0.0

  run:
    - python
    - flask >=0.0.0
    - requests >=2.0.0

test:
  # Python imports
  imports:
    - slask

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/benkershner/slask
  license: MIT
  summary: 'A Flask app to republish to Slack'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
