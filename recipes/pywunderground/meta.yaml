package:
  name: pywunderground
  version: "0.2"

source:
  fn: pywunderground-0.2.tar.gz
  url: https://pypi.python.org/packages/source/p/pywunderground/pywunderground-0.2.tar.gz
  md5: 42fad040a5c425af6863646a4dc71746
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pywunderground = pywunderground:main
    #
    # Would create an entry point called pywunderground that calls pywunderground.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=0.9.1

  run:
    - python
    - requests >=0.9.1

test:
  # Python imports
  imports:
    - pywunderground

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Diaoul/pywunderground
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Simple interface to retrieve weather data from wunderground'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
