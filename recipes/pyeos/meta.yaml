package:
  name: pyeos
  version: "0.56"

source:
  fn: pyEOS-0.56.tar.gz
  url: https://pypi.python.org/packages/source/p/pyEOS/pyEOS-0.56.tar.gz
  md5: 1f4aa78c52479ec6614ed8e967d00d00
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pyeos = pyeos:main
    #
    # Would create an entry point called pyeos that calls pyeos.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - jsonrpclib

  run:
    - python
    - jsonrpclib

test:
  # Python imports
  imports:
    - pyEOS
    - pyeos

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/spotify/pyeos/
  license: UNKNOWN
  summary: 'Python API to interact with network devices running EOS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
