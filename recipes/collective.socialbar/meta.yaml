package:
  name: collective.socialbar
  version: "1.1"

source:
  fn: collective.socialbar-1.1.zip
  url: https://pypi.python.org/packages/source/c/collective.socialbar/collective.socialbar-1.1.zip
  md5: 37e36cea13e18c8757de16d19a440084
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.socialbar = collective.socialbar:main
    #
    # Would create an entry point called collective.socialbar that calls collective.socialbar.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.socialbar

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/
  license: GPL
  summary: 'socialbar viewlet'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
