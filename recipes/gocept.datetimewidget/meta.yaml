package:
  name: gocept.datetimewidget
  version: "1.4.1"

source:
  fn: gocept.datetimewidget-1.4.1.tar.gz
  url: https://pypi.python.org/packages/source/g/gocept.datetimewidget/gocept.datetimewidget-1.4.1.tar.gz
  md5: e2df26958a6dfd34ec0ada48edcd1006
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gocept.datetimewidget = gocept.datetimewidget:main
    #
    # Would create an entry point called gocept.datetimewidget that calls gocept.datetimewidget.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.datetimewidget
    - zope.app.publisher

  run:
    - python
    - setuptools
    - setuptools
    - zc.datetimewidget
    - zope.app.publisher

test:
  # Python imports
  imports:
    - gocept
    - gocept.datetimewidget

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: Zope Public License
  summary: 'Integration package for using zc.datetimewidget in Zope 2'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
