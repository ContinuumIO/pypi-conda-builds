package:
  name: onespacemedia-server-management
  version: "0.1.10"

source:
  fn: onespacemedia-server-management-0.1.10.tar.gz
  url: https://pypi.python.org/packages/source/o/onespacemedia-server-management/onespacemedia-server-management-0.1.10.tar.gz
  md5: a7d9ed2994707035e5bd63403f85d73f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - onespacemedia-server-management = onespacemedia-server-management:main
    #
    # Would create an entry point called onespacemedia-server-management that calls onespacemedia-server-management.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - fabric
    - ansible
    - requests
    - fabric-virtualenv

  run:
    - python
    - django
    - fabric
    - ansible
    - requests
    - fabric-virtualenv

test:
  # Python imports
  imports:
    - server_management
    - server_management.management
    - server_management.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/onespacemedia/server-management/
  license: UNKNOWN
  summary: 'A set of server management tools used by Onespacemedia.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
