package:
  name: django-extramimetypes
  version: "0.1.0"

source:
  fn: django-extramimetypes-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-extramimetypes/django-extramimetypes-0.1.0.tar.gz
  md5: 53e5f72b57747f4f9bcf8c52ad1e8455
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-extramimetypes = django-extramimetypes:main
    #
    # Would create an entry point called django-extramimetypes that calls django-extramimetypes.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.2

  run:
    - python
    - django >=1.2

test:
  # Python imports
  imports:
    - extramimetypes

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bradleyayers/django-extramimetypes/
  license: UNKNOWN
  summary: "Hooks into Python's mimetypes module to allow extra mimetype guesses to be defined."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
