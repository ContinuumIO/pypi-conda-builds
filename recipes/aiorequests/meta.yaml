package:
  name: aiorequests
  version: "0.1.1"

source:
  fn: aiorequests-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/a/aiorequests/aiorequests-0.1.1.tar.gz
  md5: 35cdf83dfb453ce01bc94828fcbe1b7e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - aiorequests = aiorequests:main
    #
    # Would create an entry point called aiorequests that calls aiorequests.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - aiohttp
    - requests

  run:
    - python
    - aiohttp
    - requests

test:
  # Python imports
  imports:
    - aiorequests
    - aiorequests.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/jsandovalc/aiorequests
  license: MIT License
  summary: 'A requests-like API built on top of aiohttp client'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
