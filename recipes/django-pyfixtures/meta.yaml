package:
  name: django-pyfixtures
  version: "0.1.1"

source:
  fn: django-pyfixtures-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-pyfixtures/django-pyfixtures-0.1.1.tar.gz
  md5: 1a37a72e7cf2a0ed545f9cd605f037da
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-pyfixtures = django-pyfixtures:main
    #
    # Would create an entry point called django-pyfixtures that calls django-pyfixtures.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - pyfixtures

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/chase-seibert/django-pyfixtures
  license: LICENSE.txt
  summary: 'dumpdata generates python source code files as the fixtures'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
