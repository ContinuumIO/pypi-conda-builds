package:
  name: mopidy-evtdev
  version: "0.1.1"

source:
  fn: Mopidy-EvtDev-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/M/Mopidy-EvtDev/Mopidy-EvtDev-0.1.1.tar.gz
  md5: 405244a0295cde328822b4d2cdd44595
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mopidy-evtdev = mopidy-evtdev:main
    #
    # Would create an entry point called mopidy-evtdev that calls mopidy-evtdev.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - mopidy >=0.18
    - pykka >=1.1
    - evdev >=0.4.3

  run:
    - python
    - setuptools
    - setuptools
    - mopidy >=0.18
    - pykka >=1.1
    - evdev >=0.4.3

test:
  # Python imports
  imports:
    - mopidy_evtdev

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/liamw9534/mopidy-evtdev
  license: Apache Software License
  summary: u'Mopidy extension for virtual input devices'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
