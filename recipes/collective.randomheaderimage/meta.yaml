package:
  name: collective.randomheaderimage
  version: "0.1.0"

source:
  fn: collective.randomheaderimage-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.randomheaderimage/collective.randomheaderimage-0.1.0.tar.gz
  md5: 929c1c136f485feebddebb1e0da835e7
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.randomheaderimage = collective.randomheaderimage:main
    #
    # Would create an entry point called collective.randomheaderimage that calls collective.randomheaderimage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.randomheaderimage
    - collective.randomheaderimage.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/collective.randomheaderimage/
  license: GPL
  summary: 'Provide a random header to display into the header'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
