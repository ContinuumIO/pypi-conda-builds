package:
  name: zope.contentprovider
  version: "4.0.0"

source:
  fn: zope.contentprovider-4.0.0.tar.gz
  url: https://pypi.python.org/packages/source/z/zope.contentprovider/zope.contentprovider-4.0.0.tar.gz
  md5: 2c5ba23a74b4f2fd5bd2ee5fcb67ad9f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zope.contentprovider = zope.contentprovider:main
    #
    # Would create an entry point called zope.contentprovider that calls zope.contentprovider.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.component
    - zope.event
    - zope.interface
    - zope.location
    - zope.publisher
    - zope.schema
    - zope.tales

  run:
    - python
    - setuptools
    - zope.component
    - zope.event
    - zope.interface
    - zope.location
    - zope.publisher
    - zope.schema
    - zope.tales

test:
  # Python imports
  imports:
    - zope
    - zope.contentprovider

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/zope.contentprovider
  license: Zope Public License
  summary: 'Content Provider Framework for Zope Templates'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
