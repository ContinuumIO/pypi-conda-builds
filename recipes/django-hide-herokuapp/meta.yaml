package:
  name: django-hide-herokuapp
  version: "0.1"

source:
  fn: django-hide-herokuapp-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-hide-herokuapp/django-hide-herokuapp-0.1.tar.gz
  md5: 9680e33722caecd120df0b6a38379980
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-hide-herokuapp = django-hide-herokuapp:main
    #
    # Would create an entry point called django-hide-herokuapp that calls django-hide-herokuapp.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - hide_herokuapp

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/isms/django-hide-herokuapp/
  license: MIT License
  summary: 'Direct search engines not to index *.herokuapp.com URLs.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
