package:
  name: lp-helpers
  version: "0.11"

source:
  fn: lp-helpers-0.11.tar.gz
  url: https://pypi.python.org/packages/source/l/lp-helpers/lp-helpers-0.11.tar.gz
  md5: 6b00386088d72b94c13c4b3e26bece36
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lp-helpers = lp-helpers:main
    #
    # Would create an entry point called lp-helpers that calls lp-helpers.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - bzr
    - launchpadlib

  run:
    - python
    - bzr
    - launchpadlib

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: GPL
  summary: 'Helper scripts for working with Launchpad and Bazaar.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
