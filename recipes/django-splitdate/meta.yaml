package:
  name: django-splitdate
  version: "0.1.8"

source:
  fn: django-splitdate-0.1.8.tar.gz
  url: https://pypi.python.org/packages/source/d/django-splitdate/django-splitdate-0.1.8.tar.gz
  md5: b1f35a185504ed6d94691d85ecaa4f21
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-splitdate = django-splitdate:main
    #
    # Would create an entry point called django-splitdate that calls django-splitdate.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.7

  run:
    - python
    - django >=1.7

test:
  # Python imports
  imports:
    - django_splitdate

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/NB-Dev/django-splitdate
  license: MIT License
  summary: 'A widget for django form date fields that displays three inputs (day, month, year).'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
