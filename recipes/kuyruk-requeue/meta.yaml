package:
  name: kuyruk-requeue
  version: "1.1.1"

source:
  fn: Kuyruk-Requeue-1.1.1.tar.gz
  url: https://pypi.python.org/packages/source/K/Kuyruk-Requeue/Kuyruk-Requeue-1.1.1.tar.gz
  md5: 152969b5965bef13104c0fbd8672f5df
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kuyruk-requeue = kuyruk-requeue:main
    #
    # Would create an entry point called kuyruk-requeue that calls kuyruk-requeue.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - kuyruk >=3.0.0
    - redis >=2.10.3

  run:
    - python
    - setuptools
    - kuyruk >=3.0.0
    - redis >=2.10.3

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/cenkalti/kuyruk-requeue
  license: UNKNOWN
  summary: 'Save failed tasks to Redis and requeue them.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
