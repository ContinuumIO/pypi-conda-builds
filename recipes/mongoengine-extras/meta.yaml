package:
  name: mongoengine-extras
  version: "0.0.1"

source:
  fn: mongoengine-extras-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/m/mongoengine-extras/mongoengine-extras-0.0.1.tar.gz
  md5: 2a29c22538ea41fdf027a5354f072ff1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mongoengine-extras = mongoengine-extras:main
    #
    # Would create an entry point called mongoengine-extras that calls mongoengine-extras.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - mongoengine

  run:
    - python
    - mongoengine

test:
  # Python imports
  imports:
    - mongoengine_extras
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bennylope/mongoengine-extras
  license: Public Domain
  summary: 'Extra fields and utilities for mongoengine.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
