package:
  name: edrn.theme
  version: "1.2.5"

source:
  fn: edrn.theme-1.2.5.zip
  url: https://pypi.python.org/packages/source/e/edrn.theme/edrn.theme-1.2.5.zip
  md5: 11c738a021dd94d1cd2f4d1d4bb7aef7
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - edrn.theme = edrn.theme:main
    #
    # Would create an entry point called edrn.theme that calls edrn.theme.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.cmfplone
    - plone.app.layout
    - zope.globalrequest

  run:
    - python
    - setuptools
    - setuptools
    - products.cmfplone
    - plone.app.layout
    - zope.globalrequest

test:
  # Python imports
  imports:
    - edrn
    - edrn.theme
    - edrn.theme.browser
    - edrn.theme.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://pypi.python.org/pypi/edrn.theme
  license: Apache Software License
  summary: 'Look and feel for websites of the Early Detection Research Network (EDRN).'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
