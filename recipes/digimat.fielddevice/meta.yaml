package:
  name: digimat.fielddevice
  version: "0.1.0"

source:
  fn: digimat.fielddevice-0.1.0.zip
  url: https://pypi.python.org/packages/source/d/digimat.fielddevice/digimat.fielddevice-0.1.0.zip
  md5: ac45c7feffe7fa38b62a3adb61570453
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - digimat.fielddevice = digimat.fielddevice:main
    #
    # Would create an entry point called digimat.fielddevice that calls digimat.fielddevice.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - requests
    - setuptools

  run:
    - python
    - requests
    - setuptools

test:
  # Python imports
  imports:
    - digimat
    - digimat.fielddevice

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: PSF
  summary: 'Digimat RWS FieldDevice'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
