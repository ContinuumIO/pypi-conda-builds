package:
  name: flatland-fork
  version: "0.4"

source:
  fn: flatland-fork-0.4.zip
  url: https://pypi.python.org/packages/source/f/flatland-fork/flatland-fork-0.4.zip
  md5: 83dbc328ea0a613685358b02d6b0fa60
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flatland-fork = flatland-fork:main
    #
    # Would create an entry point called flatland-fork that calls flatland-fork.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - blinker

  run:
    - python
    - blinker

test:
  # Python imports
  imports:
    - flatland
    - flatland.ext
    - flatland.out
    - flatland.schema
    - flatland.util
    - flatland.validation

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://discorporate.us/jek/projects/flatland/
  license: MIT License
  summary: 'HTML form management and validation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
