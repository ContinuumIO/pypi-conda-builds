package:
  name: boodebr
  version: "1.5.1"

source:
  fn: boodebr-1.5.1.tar.gz
  url: http://boodebr.googlecode.com/files/boodebr-1.5.1.tar.gz
  #md5: 
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - boodebr = boodebr:main
    #
    # Would create an entry point called boodebr that calls boodebr.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - boodebr
    - boodebr.config
    - boodebr.disthelper
    - boodebr.disthelper.samples
    - boodebr.disthelper.samples.subpkg
    - boodebr.disthelper.scripts
    - boodebr.disthelper.treeops
    - boodebr.disthelper.util
    - boodebr.gui
    - boodebr.ion
    - boodebr.ion.hooks
    - boodebr.sql
    - boodebr.sql2
    - boodebr.test
    - boodebr.test.gui
    - boodebr.util

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://boodebr.org/pages/boodebr
  license: MIT License
  summary: 'A collection of modules from boodebr.org for JSON, XML, config files, SQL, locking, thread queues, and more.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
