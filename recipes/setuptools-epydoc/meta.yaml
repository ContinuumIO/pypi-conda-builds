package:
  name: setuptools-epydoc
  version: "0.2"

source:
  fn: setuptools-epydoc-0.2.tar.gz
  url: https://pypi.python.org/packages/source/s/setuptools-epydoc/setuptools-epydoc-0.2.tar.gz
  md5: cbc8268ac7687533560e671fc4d753e4
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - setuptools-epydoc = setuptools-epydoc:main
    #
    # Would create an entry point called setuptools-epydoc that calls setuptools-epydoc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - epydoc

  run:
    - python
    - setuptools
    - epydoc

test:
  # Python imports
  imports:
    - setuptools_epydoc

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/languitar/setuptools-epydoc
  license: Mozilla Public License 2.0 (MPL 2.0)
  summary: 'Epydoc API documentation generation command for pylint'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
