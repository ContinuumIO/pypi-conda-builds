package:
  name: langutil
  version: "0.1.5"

source:
  fn: LangUtil-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/L/LangUtil/LangUtil-0.1.5.tar.gz
  md5: eb6e9b835976ba387e99df3fa6089e29
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - langutil = langutil:main
    #
    # Would create an entry point called langutil that calls langutil.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - phpserialize ==1.3

  run:
    - python
    - phpserialize ==1.3

test:
  # Python imports
  imports:
    - langutil

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/LangUtil/
  license: LICENSE.txt
  summary: 'Helpers for generating code.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
