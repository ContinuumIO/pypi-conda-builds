package:
  name: collective.base64imagepatch
  version: "0.12"

source:
  fn: collective.base64imagepatch-0.12.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.base64imagepatch/collective.base64imagepatch-0.12.tar.gz
  md5: d5c19f7f123af101b0a4d4bab7536b71
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.base64imagepatch = collective.base64imagepatch:main
    #
    # Would create an entry point called collective.base64imagepatch that calls collective.base64imagepatch.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - beautifulsoup4

  run:
    - python
    - setuptools
    - setuptools
    - beautifulsoup4

test:
  # Python imports
  imports:
    - collective
    - collective.base64imagepatch

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.base64imagepatch
  license: GPL
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
