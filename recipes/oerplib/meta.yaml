package:
  name: oerplib
  version: "0.8.4"

source:
  fn: OERPLib-0.8.4.tar.gz
  url: https://pypi.python.org/packages/source/O/OERPLib/OERPLib-0.8.4.tar.gz
  md5: e075ed3ac0d49b8fc1f02a6cf9325cd0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - oerplib = oerplib:main
    #
    # Would create an entry point called oerplib that calls oerplib.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - oerplib
    - oerplib.rpc
    - oerplib.service
    - oerplib.service.inspect
    - oerplib.service.osv
    - oerplib.tools

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pythonhosted.org/OERPLib/
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'OERPLib is a Python module providing an easy way to pilot your OpenERP and Odoo servers through RPC.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
