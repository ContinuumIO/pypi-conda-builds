package:
  name: lisa-plugin-domoticz
  version: "0.8.9.94"

source:
  fn: lisa-plugin-Domoticz-0.8.9.94.tar.gz
  url: https://pypi.python.org/packages/source/l/lisa-plugin-Domoticz/lisa-plugin-Domoticz-0.8.9.94.tar.gz
  md5: ab77d5fc8f1b806b7af8076082b4b5fb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lisa-plugin-domoticz = lisa-plugin-domoticz:main
    #
    # Would create an entry point called lisa-plugin-domoticz that calls lisa-plugin-domoticz.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6.2,<1.7
    - django-tastypie >=0.11.0
    - pysnmp
    - lisa-server

  run:
    - python
    - django >=1.6.2,<1.7
    - django-tastypie >=0.11.0
    - pysnmp
    - lisa-server

test:
  # Python imports
  imports:
    - lisa.plugins

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/jfcjfc/LISA-PLUGINS-Domoticz
  license: MIT License
  summary: 'LISA home automation system - Plugin'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
