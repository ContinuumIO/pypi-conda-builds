package:
  name: coincheck
  version: "0.1.6"

source:
  fn: coincheck-0.1.6.tar.gz
  url: https://pypi.python.org/packages/source/c/coincheck/coincheck-0.1.6.tar.gz
  md5: 80e1a45034d2385b4eb6041b76ee8a8a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - coincheck = coincheck:main
    #
    # Would create an entry point called coincheck that calls coincheck.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests ==2.6.0
    - pytz >2

  run:
    - python
    - requests ==2.6.0
    - pytz >2

test:
  # Python imports
  imports:
    - coincheck

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kmn/coincheck
  license: MIT License
  summary: 'Coincheck API Library for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
