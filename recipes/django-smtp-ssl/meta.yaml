package:
  name: django-smtp-ssl
  version: "1.0"

source:
  fn: django-smtp-ssl-1.0.zip
  url: https://pypi.python.org/packages/source/d/django-smtp-ssl/django-smtp-ssl-1.0.zip
  md5: a05e766a03fa2f1ec0b323662e455b89
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-smtp-ssl = django-smtp-ssl:main
    #
    # Would create an entry point called django-smtp-ssl that calls django-smtp-ssl.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bancek/django-smtp-ssl
  license: MIT License
  summary: 'SMTP SSL email backend for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
