package:
  name: ibme.persondirectory
  version: "1.1"

source:
  fn: ibme.persondirectory-1.1.zip
  url: https://pypi.python.org/packages/source/i/ibme.persondirectory/ibme.persondirectory-1.1.zip
  md5: 2b709935d90f15fc74030934a09d52fb
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ibme.persondirectory = ibme.persondirectory:main
    #
    # Would create an entry point called ibme.persondirectory that calls ibme.persondirectory.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.contentlisting
    - plone.app.dexterity
    - plone.indexer

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.contentlisting
    - plone.app.dexterity
    - plone.indexer

test:
  # Python imports
  imports:
    - ibme
    - ibme.persondirectory
    - ibme.persondirectory.browser
    - ibme.persondirectory.models
    - ibme.persondirectory.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://shuttlethread.com
  license: UNKNOWN
  summary: 'Directory of people'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
