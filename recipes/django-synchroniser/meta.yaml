package:
  name: django-synchroniser
  version: "0.2.0"

source:
  fn: django-synchroniser-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-synchroniser/django-synchroniser-0.2.0.tar.gz
  md5: a90c884c4bc96d31532b9ae0fd1ae820
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-synchroniser = django-synchroniser:main
    #
    # Would create an entry point called django-synchroniser that calls django-synchroniser.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6,<1.7

  run:
    - python
    - django >=1.6,<1.7

test:
  # Python imports
  imports:
    - example
    - synchronise

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/elevenbits/django-synchroniser
  license: GNU General Public License v3 (GPLv3)
  summary: 'django-synchroniser automagically synchronises Bitbucket Mecurial base projects to GitHub (Git based) projects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
