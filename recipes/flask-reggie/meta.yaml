package:
  name: flask-reggie
  version: "0.0.2"

source:
  fn: Flask-Reggie-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Reggie/Flask-Reggie-0.0.2.tar.gz
  md5: 5a7d6dace2d285956f5cd633f1204ba6
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-reggie = flask-reggie:main
    #
    # Would create an entry point called flask-reggie that calls flask-reggie.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask

  run:
    - python
    - setuptools
    - flask

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/rhyselsmore/flask-reggie
  license: Flask-Registry
  summary: 'Flask Regex Routes.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
