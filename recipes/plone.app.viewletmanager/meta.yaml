package:
  name: plone.app.viewletmanager
  version: "2.0.8"

source:
  fn: plone.app.viewletmanager-2.0.8.zip
  url: https://pypi.python.org/packages/source/p/plone.app.viewletmanager/plone.app.viewletmanager-2.0.8.zip
  md5: 52e068eee48dffd82508bd4cdbbd664f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.app.viewletmanager = plone.app.viewletmanager:main
    #
    # Would create an entry point called plone.app.viewletmanager that calls plone.app.viewletmanager.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - acquisition
    - products.genericsetup
    - zodb3
    - zope2
    - plone.app.vocabularies
    - setuptools
    - zope.component
    - zope.contentprovider
    - zope.interface
    - zope.site
    - zope.viewlet

  run:
    - python
    - acquisition
    - products.genericsetup
    - zodb3
    - zope2
    - plone.app.vocabularies
    - setuptools
    - zope.component
    - zope.contentprovider
    - zope.interface
    - zope.site
    - zope.viewlet

test:
  # Python imports
  imports:
    - plone
    - plone.app
    - plone.app.viewletmanager
    - plone.app.viewletmanager.exportimport
    - plone.app.viewletmanager.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/plone.app.viewletmanager
  license: GNU General Public License (GPL)
  summary: 'Configurable viewlet manager'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
