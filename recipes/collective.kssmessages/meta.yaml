package:
  name: collective.kssmessages
  version: "0.1"

source:
  fn: collective.kssmessages-0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.kssmessages/collective.kssmessages-0.1.tar.gz
  md5: fb99fec84b97ab5f476d4f0a7e4431fa
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.kssmessages = collective.kssmessages:main
    #
    # Would create an entry point called collective.kssmessages that calls collective.kssmessages.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.kssmessages
    - collective.kssmessages.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: GPL
  summary: 'Show human-readable error messages when KSS timeouts or errors occur'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
