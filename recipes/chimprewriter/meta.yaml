package:
  name: chimprewriter
  version: "0.1"

source:
  fn: chimprewriter-0.1.zip
  url: https://pypi.python.org/packages/source/c/chimprewriter/chimprewriter-0.1.zip
  md5: 5f79fa6533460ce4e919435897baac1b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - chimprewriter = chimprewriter:main
    #
    # Would create an entry point called chimprewriter that calls chimprewriter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - chimprewriter
    - chimprewriter.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.niteoweb.com
  license: BSD
  summary: 'Python bindings for ChimpRewriter API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
