package:
  name: products.plonestatcounter
  version: "0.1"

source:
  fn: Products.PloneStatCounter-0.1.zip
  url: https://pypi.python.org/packages/source/P/Products.PloneStatCounter/Products.PloneStatCounter-0.1.zip
  md5: 08ee33fb57783fb9cd33ed8046ea0953
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - products.plonestatcounter = products.plonestatcounter:main
    #
    # Would create an entry point called products.plonestatcounter that calls products.plonestatcounter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - Products
    - Products.PloneStatCounter

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/Products.PloneStatCounter
  license: GPL
  summary: 'A package to ease use of StatCounter in Plone.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
