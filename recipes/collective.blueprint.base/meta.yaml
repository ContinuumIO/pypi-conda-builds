package:
  name: collective.blueprint.base
  version: "1.0"

source:
  fn: collective.blueprint.base-1.0.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.blueprint.base/collective.blueprint.base-1.0.tar.gz
  md5: 2f1310b8d13eff3fc4b6e7cadad506dc
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.blueprint.base = collective.blueprint.base:main
    #
    # Would create an entry point called collective.blueprint.base that calls collective.blueprint.base.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.transmogrifier

  run:
    - python
    - setuptools
    - setuptools
    - collective.transmogrifier

test:
  # Python imports
  imports:
    - collective
    - collective.blueprint
    - collective.blueprint.base
    - collective.blueprint.base.configsource
    - collective.blueprint.base.delete
    - collective.blueprint.base.keysplitter
    - collective.blueprint.base.recurser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/collective.blueprint.base
  license: GPL
  summary: 'Some core transmogrifier blueprints and base classes'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
