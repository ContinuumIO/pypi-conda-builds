package:
  name: tornado-cors
  version: "0.6.0"

source:
  fn: tornado-cors-0.6.0.tar.gz
  url: https://pypi.python.org/packages/source/t/tornado-cors/tornado-cors-0.6.0.tar.gz
  md5: 125a518734859cc5596a2c25d9f5f5f1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tornado-cors = tornado-cors:main
    #
    # Would create an entry point called tornado-cors that calls tornado-cors.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - tornado

  run:
    - python
    - tornado

test:
  # Python imports
  imports:
    - tornado_cors

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/globocom/tornado-cors
  license: MIT License
  summary: 'CORS support for Tornado'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
