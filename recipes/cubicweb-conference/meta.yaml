package:
  name: cubicweb-conference
  version: "0.13.0"

source:
  fn: cubicweb-conference-0.13.0.tar.gz
  url: https://pypi.python.org/packages/source/c/cubicweb-conference/cubicweb-conference-0.13.0.tar.gz
  md5: 9e3e09da34376f16e366e07628b36ea0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cubicweb-conference = cubicweb-conference:main
    #
    # Would create an entry point called cubicweb-conference that calls cubicweb-conference.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cubicweb-comment
    - cubicweb-seo
    - cubicweb-tag
    - cubicweb-card >=0.4.0
    - cubicweb >=3.10.0
    - cubicweb-addressbook
    - cubicweb-file

  run:
    - python
    - cubicweb-comment
    - cubicweb-seo
    - cubicweb-tag
    - cubicweb-card >=0.4.0
    - cubicweb >=3.10.0
    - cubicweb-addressbook
    - cubicweb-file

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.cubicweb.org/project/cubicweb-conference
  license: LGPL
  summary: 'conference component for the CubicWeb framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
