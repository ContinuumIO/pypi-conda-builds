package:
  name: tornfoursquare
  version: "0.0.1"

source:
  fn: tornfoursquare-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/t/tornfoursquare/tornfoursquare-0.0.1.tar.gz
  md5: 4256d8af65bc0b2eb9325d84125f3704
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tornfoursquare = tornfoursquare:main
    #
    # Would create an entry point called tornfoursquare that calls tornfoursquare.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - tornfoursquare

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/stevepeak/tornfoursquare
  license: Apache v2
  summary: 'Foursquare auth wrapper for Tornado Web'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
