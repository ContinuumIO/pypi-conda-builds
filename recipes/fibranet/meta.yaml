package:
  name: fibranet
  version: "10"

source:
  fn: FibraNet-10.tar.gz
  url: https://pypi.python.org/packages/source/F/FibraNet/FibraNet-10.tar.gz
  md5: 03524d3abc6e64ecbe24eb1a2db9c7ff
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fibranet = fibranet:main
    #
    # Would create an entry point called fibranet that calls fibranet.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - eventnet
    - fibranet

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/fibranet/
  license: BSD
  summary: 'A cooperative threading and event driven framework with simple network capabilities.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
