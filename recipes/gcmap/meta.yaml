package:
  name: gcmap
  version: "0.0.3"

source:
  fn: gcmap-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/g/gcmap/gcmap-0.0.3.tar.gz
  md5: 74ca7e87e81a4f4e8739ad575b80b49a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gcmap = gcmap:main
    #
    # Would create an entry point called gcmap that calls gcmap.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - aggdraw ==1.1-64bits
    - numpy >=1.7.1
    - pillow >=2.0.0
    - pyproj >=1.9.3

  run:
    - python
    - aggdraw ==1.1-64bits
    - numpy >=1.7.1
    - pillow >=2.0.0
    - pyproj >=1.9.3

test:
  # Python imports
  imports:
    - gcmap

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/paulgb/gcmap
  license: UNKNOWN
  summary: 'Great circle visualization of many coordinate pairs'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
