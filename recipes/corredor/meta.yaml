package:
  name: corredor
  version: "0.1"

source:
  fn: corredor-0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/corredor/corredor-0.1.tar.gz
  md5: 4b8564d6868d0a43377d74557c11fc26
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - corredor = corredor:main
    #
    # Would create an entry point called corredor that calls corredor.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - mozlog >=1.7
    - tornado
    - pyzmq

  run:
    - python
    - mozlog >=1.7
    - tornado
    - pyzmq

test:
  # Python imports
  imports:
    - corredor
    - corredor.testing
    - tests
    - tests.client

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ahal/corredor
  license: Mozilla Public License 2.0 (MPL 2.0)
  summary: 'A network transport for running distributed tests'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
