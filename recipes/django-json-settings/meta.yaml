package:
  name: django-json-settings
  version: "0.1"

source:
  fn: django-json-settings-0.1.zip
  url: https://pypi.python.org/packages/source/d/django-json-settings/django-json-settings-0.1.zip
  md5: c3be67737332a5324282721b840129a7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-json-settings = django-json-settings:main
    #
    # Would create an entry point called django-json-settings that calls django-json-settings.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - json_settings

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/isotoma/django-json-settings
  license: Apache Software License
  summary: 'A Django application to let you provide local settings in json format'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
