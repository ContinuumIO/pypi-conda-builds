package:
  name: django-image-helper
  version: "0.1.1"

source:
  fn: django-image-helper-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-image-helper/django-image-helper-0.1.1.tar.gz
  md5: 5587be7f46ab83f99dbd2ff1131061af
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-image-helper = django-image-helper:main
    #
    # Would create an entry point called django-image-helper that calls django-image-helper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3
    - pillow ==1.7.6

  run:
    - python
    - django >=1.3
    - pillow ==1.7.6

test:
  # Python imports
  imports:
    - image_helper

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/madisona/django-image-helper
  license: BSD License
  summary: 'Django helpers for working with images.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
