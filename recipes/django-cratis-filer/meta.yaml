package:
  name: django-cratis-filer
  version: "0.1.0"

source:
  fn: django-cratis-filer-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-cratis-filer/django-cratis-filer-0.1.0.tar.gz
  md5: 2beb1b0d1aae62d493cb2176ce3a27a3
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-cratis-filer = django-cratis-filer:main
    #
    # Would create an entry point called django-cratis-filer that calls django-cratis-filer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django-cratis
    - setuptools
    - django-cms
    - pillow
    - easy-thumbnails
    - pillow
    - django-filer
    - cmsplugin-filer

  run:
    - python
    - django-cratis
    - setuptools
    - django-cms
    - pillow
    - easy-thumbnails
    - pillow
    - django-filer
    - cmsplugin-filer

test:
  # Python imports
  imports:
    - cratis_filer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: MIT
  summary: 'Integrates django-cms into cratis application'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
