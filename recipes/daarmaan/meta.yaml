package:
  name: daarmaan
  version: "0.2.2"

source:
  fn: Daarmaan-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/D/Daarmaan/Daarmaan-0.2.2.tar.gz
  md5: 166d99ed67e35eea467fa0f012e93601
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - daarmaan = daarmaan:main
    #
    # Would create an entry point called daarmaan that calls daarmaan.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - daarmaan
    - daarmaan.client
    - daarmaan.server

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://daarmaan.yellowen.com/
  license: GNU General Public License (GPL)
  summary: 'Single Sign On Service for Yellowen'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
