package:
  name: frag2text
  version: "0.0.5"

source:
  fn: frag2text-0.0.5.tar.gz
  url: https://pypi.python.org/packages/source/f/frag2text/frag2text-0.0.5.tar.gz
  md5: 5aed1c286bc22b30a5c36db67ff974ae
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - frag2text = frag2text:main
    #
    # Would create an entry point called frag2text that calls frag2text.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cssselect
    - html2text
    - html5lib
    - lxml
    - requests

  run:
    - python
    - cssselect
    - html2text
    - html5lib
    - lxml
    - requests

test:
  # Python imports
  imports:
    - frag2text

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/siznax/frag2text/
  license: MIT License
  summary: 'Select and reverse-Markdown (html2text) web page fragments.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
