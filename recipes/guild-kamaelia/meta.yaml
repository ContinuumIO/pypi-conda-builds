package:
  name: guild-kamaelia
  version: "0.0.2"

source:
  fn: guild-kamaelia-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/g/guild-kamaelia/guild-kamaelia-0.0.2.tar.gz
  md5: df8adcf0a68cf0b321f4cbf87e13b51b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - guild-kamaelia = guild-kamaelia:main
    #
    # Would create an entry point called guild-kamaelia that calls guild-kamaelia.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - guild.kamaelia

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.sparkslabs.com/michael/
  license: Apache Software License
  summary: 'Kamaelia partial-compatibility layer for guild'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
