package:
  name: python-hcalendar
  version: "0.2"

source:
  fn: python-hcalendar-0.2.tar.gz
  url: https://pypi.python.org/packages/source/p/python-hcalendar/python-hcalendar-0.2.tar.gz
  md5: 68de84038994044491753e9e733fdaca
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-hcalendar = python-hcalendar:main
    #
    # Would create an entry point called python-hcalendar that calls python-hcalendar.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - isodate >=0.5.0
    - beautifulsoup4 >=4.3.2

  run:
    - python
    - isodate >=0.5.0
    - beautifulsoup4 >=4.3.2

test:
  # Python imports
  imports:
    - hcalendar
    - unittests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mback2k/python-hcalendar
  license: MIT License
  summary: 'python-hcalendar is a basic hCalendar parser'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
