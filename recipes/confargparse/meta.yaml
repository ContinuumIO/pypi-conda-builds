package:
  name: confargparse
  version: "1.1.20"

source:
  fn: ConfArgParse-1.1.20.tar.gz
  url: https://pypi.python.org/packages/source/C/ConfArgParse/ConfArgParse-1.1.20.tar.gz
  md5: fe50f860d048949933c1cd771cfba90f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - confargparse = confargparse:main
    #
    # Would create an entry point called confargparse that calls confargparse.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - hgtools

  run:
    - python
    - hgtools

test:
  # Python imports
  imports:
    - confargparse

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/swamidass/confargparse/
  license: MIT License
  summary: 'An integrated argument/configuration file parser that follows the syntax of argparser'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
