package:
  name: kwplayer
  version: "3.5.2"

source:
  fn: kwplayer-3.5.2.tar.gz
  url: https://pypi.python.org/packages/source/k/kwplayer/kwplayer-3.5.2.tar.gz
  md5: 3b446246b7a690e9e91fd5380053d11e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kwplayer = kwplayer:main
    #
    # Would create an entry point called kwplayer that calls kwplayer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - kuwo
    - kwplayer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/LiuLang/kwplayer
  license: GNU General Public License v3 (GPLv3)
  summary: 'Music player for linux users'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
