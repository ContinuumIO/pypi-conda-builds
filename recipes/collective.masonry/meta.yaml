package:
  name: collective.masonry
  version: "1.2.2"

source:
  fn: collective.masonry-1.2.2.zip
  url: https://pypi.python.org/packages/source/c/collective.masonry/collective.masonry-1.2.2.zip
  md5: 52d07afc8ce06aa3facd979e69ddc926
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.masonry = collective.masonry:main
    #
    # Would create an entry point called collective.masonry that calls collective.masonry.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.js.masonry
    - collective.js.imagesloaded
    - collective.registry

  run:
    - python
    - setuptools
    - setuptools
    - collective.js.masonry
    - collective.js.imagesloaded
    - collective.registry

test:
  # Python imports
  imports:
    - collective
    - collective.masonry

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.masonry
  license: GPL
  summary: 'Integrate Masonry to Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
