package:
  name: ftw.bridge.proxy
  version: "1.1.1"

source:
  fn: ftw.bridge.proxy-1.1.1.tar.gz
  url: https://pypi.python.org/packages/source/f/ftw.bridge.proxy/ftw.bridge.proxy-1.1.1.tar.gz
  md5: 632048bf9a2eed8b841b960296c42da1
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ftw.bridge.proxy = ftw.bridge.proxy:main
    #
    # Would create an entry point called ftw.bridge.proxy that calls ftw.bridge.proxy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - pyramid
    - pyramid_zcml
    - zope.component
    - requests

  run:
    - python
    - setuptools
    - setuptools
    - pyramid
    - pyramid_zcml
    - zope.component
    - requests

test:
  # Python imports
  imports:
    - ftw
    - ftw.bridge
    - ftw.bridge.proxy
    - ftw.bridge.proxy.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/4teamwork/ftw.bridge.proxy
  license: GNU General Public License (GPL)
  summary: 'A pyramid based proxy application for proxying requests between plone instances.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
