package:
  name: mezzanine-buffer
  version: "0.1.0"

source:
  fn: mezzanine-buffer-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/m/mezzanine-buffer/mezzanine-buffer-0.1.0.tar.gz
  md5: 550d54d4d4c47cc3363ed4bb86206c3f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mezzanine-buffer = mezzanine-buffer:main
    #
    # Would create an entry point called mezzanine-buffer that calls mezzanine-buffer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=2.1.0
    - buffer-python >=1.08

  run:
    - python
    - requests >=2.1.0
    - buffer-python >=1.08

test:
  # Python imports
  imports:
    - mezzanine_buffer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/caffodian/mezzanine-buffer
  license: BSD License
  summary: 'Buffer integration for Mezzanine CMS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
