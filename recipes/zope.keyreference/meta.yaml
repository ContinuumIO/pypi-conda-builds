package:
  name: zope.keyreference
  version: "4.1.0"

source:
  fn: zope.keyreference-4.1.0.tar.gz
  url: https://pypi.python.org/packages/source/z/zope.keyreference/zope.keyreference-4.1.0.tar.gz
  md5: 09eb4f63d5c83822add0cd4cd76c35db
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zope.keyreference = zope.keyreference:main
    #
    # Would create an entry point called zope.keyreference that calls zope.keyreference.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zodb
    - zope.component
    - zope.i18nmessageid
    - zope.interface
    - zope.schema

  run:
    - python
    - setuptools
    - zodb
    - zope.component
    - zope.i18nmessageid
    - zope.interface
    - zope.schema

test:
  # Python imports
  imports:
    - zope
    - zope.keyreference

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/zope.keyreference
  license: Zope Public License
  summary: 'Key References'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
