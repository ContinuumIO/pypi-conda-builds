package:
  name: infrae.paster
  version: "0.1dev-r27752"

source:
  fn: infrae.paster-0.1dev-r27752.tar.gz
  url: https://pypi.python.org/packages/source/i/infrae.paster/infrae.paster-0.1dev-r27752.tar.gz
  md5: 91fe92359e7fb47c44e56dc98ab03dcf
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - infrae.paster = infrae.paster:main
    #
    # Would create an entry point called infrae.paster that calls infrae.paster.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zc.buildout
    - setuptools
    - paste
    - zopeskel

  run:
    - python
    - setuptools
    - zc.buildout
    - setuptools
    - paste
    - zopeskel

test:
  # Python imports
  imports:
    - infrae
    - infrae.paster

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://svn.infrae.com/buildout/infrae.paster
  license: Zope Public License
  summary: 'Buildout recipe to create a part using a paster template'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
