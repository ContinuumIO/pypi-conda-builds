package:
  name: setuptools-flakes
  version: "0.1"

source:
  fn: setuptools-flakes-0.1.tar.gz
  url: https://pypi.python.org/packages/source/s/setuptools-flakes/setuptools-flakes-0.1.tar.gz
  md5: 9abc8a51ad12e47cf7c5637db0289a47
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - setuptools-flakes = setuptools-flakes:main
    #
    # Would create an entry point called setuptools-flakes that calls setuptools-flakes.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyflakes

  run:
    - python
    - setuptools
    - pyflakes

test:
  # Python imports
  imports:
    - setuptools_flakes

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/johnnoone/setuptools-pyflakes.git
  license: BSD License
  summary: 'Setup tools command for pyflakes'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
