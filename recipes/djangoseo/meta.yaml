package:
  name: djangoseo
  version: "1.0"

source:
  fn: DjangoSEO-1.0.tar.gz
  url: https://pypi.python.org/packages/source/D/DjangoSEO/DjangoSEO-1.0.tar.gz
  md5: 69cd65eb3a9d716b5ba067b1a4e63735
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djangoseo = djangoseo:main
    #
    # Would create an entry point called djangoseo that calls djangoseo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.1

  run:
    - python
    - django >=1.1

test:
  # Python imports
  imports:
    - djangoseo
    - rollyourown
    - rollyourown.seo
    - rollyourown.seo.management
    - rollyourown.seo.management.commands
    - rollyourown.seo.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/willhardy/django-seo
  license: Apache Software License or BSD License
  summary: 'A framework for managing SEO metadata in Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
