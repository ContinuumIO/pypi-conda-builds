package:
  name: dcitools
  version: "0.4.2a"

source:
  fn: dcitools-0.4.2a.tar.gz
  url: https://pypi.python.org/packages/source/d/dcitools/dcitools-0.4.2a.tar.gz
  md5: 57ab42ce5b21e26a860791cc49378fda
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dcitools = dcitools:main
    #
    # Would create an entry point called dcitools that calls dcitools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - tbx >=1.3.3
    - six >=1.8.0

  run:
    - python
    - tbx >=1.3.3
    - six >=1.8.0

test:
  # Python imports
  imports:
    - dcitools
    - dcitools.devices
    - dcitools.devices.doremi
    - dcitools.parsers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ronhanson/python-dcitools
  license: MIT License
  summary: 'Digital Cinema related collection of tools for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
