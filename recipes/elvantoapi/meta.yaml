package:
  name: elvantoapi
  version: "1.3.2"

source:
  fn: ElvantoAPI-1.3.2.tar.gz
  url: https://pypi.python.org/packages/source/E/ElvantoAPI/ElvantoAPI-1.3.2.tar.gz
  md5: a1204f1ffe7d11f6fc638038a3bce974
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - elvantoapi = elvantoapi:main
    #
    # Would create an entry point called elvantoapi that calls elvantoapi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - ElvantoAPI
    - elvantoapi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/elvanto/api-py
  license: elves
  summary: 'Python API Wrapper for Elvanto Systems'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
