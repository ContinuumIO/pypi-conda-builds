package:
  name: mergedict
  version: "0.2.0"

source:
  fn: mergedict-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/m/mergedict/mergedict-0.2.0.tar.gz
  md5: 0a52ba99f7e56f5c88670ecfe422c5f7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mergedict = mergedict:main
    #
    # Would create an entry point called mergedict that calls mergedict.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - singledispatch

  run:
    - python
    - singledispatch

test:
  # Python imports
  imports:
    - mergedict

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/schettino72/mergedict/
  license: MIT License
  summary: 'A Python `dict` with a merge() method.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
