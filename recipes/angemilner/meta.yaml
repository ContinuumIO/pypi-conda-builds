package:
  name: angemilner
  version: "0.2.0"

source:
  fn: angemilner-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/a/angemilner/angemilner-0.2.0.tar.gz
  md5: 9d7f770f92c55d5be25d83afd23eaf73
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - angemilner = angemilner:main
    #
    # Would create an entry point called angemilner that calls angemilner.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pymongo >=2.7.2

  run:
    - python
    - setuptools
    - pymongo >=2.7.2

test:
  # Python imports
  imports:
    - angemilner

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rawkintrevo/API-Key-Librarian
  license: Apache Software License
  summary: 'Manages a library of API keys using MongoDB to prevent rate limiting'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
