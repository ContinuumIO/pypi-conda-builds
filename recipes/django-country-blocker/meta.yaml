package:
  name: django-country-blocker
  version: "1.1.13"

source:
  fn: django-country-blocker-1.1.13.tar.gz
  url: https://pypi.python.org/packages/source/d/django-country-blocker/django-country-blocker-1.1.13.tar.gz
  md5: a7573e7acc63ceb8d0d95e29b92d2fb1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-country-blocker = django-country-blocker:main
    #
    # Would create an entry point called django-country-blocker that calls django-country-blocker.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - country_block
    - country_block.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.github.com/jslootbeek/django-country-blocker
  license: BSD License
  summary: 'Django app introducing a Context Processor that adds users location information to the context.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
