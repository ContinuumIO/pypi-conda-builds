package:
  name: device42api
  version: "1.2.1"

source:
  fn: device42api-1.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/device42api/device42api-1.2.1.tar.gz
  md5: 257d0dbb489aba491e9fd75d909d4917
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - device42api = device42api:main
    #
    # Would create an entry point called device42api that calls device42api.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python-httplib2 >=0.8
    - python-simplejson >=1.4.6

  run:
    - python
    - python-httplib2 >=0.8
    - python-simplejson >=1.4.6

test:
  # Python imports
  imports:
    - device42api

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://python-device42api.readthedocs.org/en/latest/
  license: GNU General Public License v2 (GPLv2)
  summary: 'Device42.com API module for object oriented API handling'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
