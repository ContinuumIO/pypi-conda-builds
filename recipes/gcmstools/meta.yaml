package:
  name: gcmstools
  version: "0.1.1"

source:
  fn: gcmstools-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/g/gcmstools/gcmstools-0.1.1.tar.gz
  md5: 41042c98215db082d3df81e2fe03165f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gcmstools = gcmstools:main
    #
    # Would create an entry point called gcmstools that calls gcmstools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy >=1.9.1
    - matplotlib >=1.4.2
    - tables >=3.1.1
    - scipy >=0.14.0
    - pandas >=0.15.2
    - ipython >=2.3.1

  run:
    - python
    - numpy >=1.9.1
    - matplotlib >=1.4.2
    - tables >=3.1.1
    - scipy >=0.14.0
    - pandas >=0.15.2
    - ipython >=2.3.1

test:
  # Python imports
  imports:
    - gcmstools

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rnelsonchem/gcms_nnls
  license: BSD License
  summary: 'Tools for working with GCMS files.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
