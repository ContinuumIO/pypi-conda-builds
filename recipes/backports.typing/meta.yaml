package:
  name: backports.typing
  version: "1.0"

source:
  fn: backports.typing-1.0.zip
  url: https://pypi.python.org/packages/source/b/backports.typing/backports.typing-1.0.zip
  md5: 53a256347bc651da8db1f6f57ac821cb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - backports.typing = backports.typing:main
    #
    # Would create an entry point called backports.typing that calls backports.typing.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - backports
    - backports.typing

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/jaraco/backports.typing
  license: MIT License
  summary: 'backports.typing'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
