package:
  name: zest.commentcleanup
  version: "1.6"

source:
  fn: zest.commentcleanup-1.6.zip
  url: https://pypi.python.org/packages/source/z/zest.commentcleanup/zest.commentcleanup-1.6.zip
  md5: 3ab858e463762e3a012aad6a6991257b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zest.commentcleanup = zest.commentcleanup:main
    #
    # Would create an entry point called zest.commentcleanup that calls zest.commentcleanup.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - zest
    - zest.commentcleanup

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/zestsoftware/zest.commentcleanup
  license: GPL
  summary: 'Quickly remove lots of spam comments from your Plone Site'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
