package:
  name: django-thumborize
  version: "1.0"

source:
  fn: django-thumborize-1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-thumborize/django-thumborize-1.0.tar.gz
  md5: 59a0ae9da6dc81db0d6840ed1d5b9da5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-thumborize = django-thumborize:main
    #
    # Would create an entry point called django-thumborize that calls django-thumborize.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - libthumbor

  run:
    - python
    - django
    - libthumbor

test:
  # Python imports
  imports:
    - thumborize
    - thumborize.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tpisani/django-thumborize
  license: BSD
  summary: 'Minimal interface for thumbor imaging service'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
