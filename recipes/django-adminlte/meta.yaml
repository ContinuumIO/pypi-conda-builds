package:
  name: django-adminlte
  version: "1.2"

source:
  fn: django-adminlte-1.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-adminlte/django-adminlte-1.2.tar.gz
  md5: 37b7aec76e6bbce1bff7482649ab2a34
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-adminlte = django-adminlte:main
    #
    # Would create an entry point called django-adminlte that calls django-adminlte.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4

  run:
    - python
    - django >=1.4

test:
  # Python imports
  imports:
    - django_adminlte

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ricardotun/django-adminlte
  license: MIT License
  summary: 'AdminLTE Bootstrap Theme packaged for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
