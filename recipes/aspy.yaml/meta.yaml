package:
  name: aspy.yaml
  version: "0.2.1"

source:
  fn: aspy.yaml-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/a/aspy.yaml/aspy.yaml-0.2.1.tar.gz
  md5: ac9b562a77492ac6eaf196f305c3a17d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - aspy.yaml = aspy.yaml:main
    #
    # Would create an entry point called aspy.yaml that calls aspy.yaml.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - ordereddict
    - pyyaml

  run:
    - python
    - ordereddict
    - pyyaml

test:
  # Python imports
  imports:
    - aspy
    - aspy.yaml

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/asottile/aspy.yaml
  license: MIT License
  summary: 'A few extensions to pyyaml.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
