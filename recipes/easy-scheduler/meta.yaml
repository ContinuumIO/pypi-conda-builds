package:
  name: easy-scheduler
  version: "0.1.2"

source:
  fn: easy-scheduler-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/e/easy-scheduler/easy-scheduler-0.1.2.tar.gz
  md5: 7172ab10b7142fb7f375497a61c04b9a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - easy-scheduler = easy-scheduler:main
    #
    # Would create an entry point called easy-scheduler that calls easy-scheduler.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - easy_scheduler

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/psycho-ir/easy-scheduler
  license: UNKNOWN
  summary: 'An easy and lighweight scheduler'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
