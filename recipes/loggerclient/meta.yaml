package:
  name: loggerclient
  version: "1.0.1"

source:
  fn: loggerclient-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/l/loggerclient/loggerclient-1.0.1.tar.gz
  md5: 4b99e630f903f8f6276838d394c7f3e9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - loggerclient = loggerclient:main
    #
    # Would create an entry point called loggerclient that calls loggerclient.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - loggerclient

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://getlogger.com/
  license: BSD-2-Clause
  summary: 'Client for Logger - an easy way to log errors'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
