package:
  name: django-kungfu
  version: "0.2"

source:
  fn: django-kungfu-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-kungfu/django-kungfu-0.2.tar.gz
  md5: 4547c306137eb76ae5a9a39b3c36a65a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-kungfu = django-kungfu:main
    #
    # Would create an entry point called django-kungfu that calls django-kungfu.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_kungfu

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/delinhabit/django-kungfu
  license: BSD License
  summary: 'A Flasky approach to distributed Django configuration'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
