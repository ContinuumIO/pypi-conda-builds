package:
  name: collective.templateengines
  version: "0.3.5"

source:
  fn: collective.templateengines-0.3.5.zip
  url: https://pypi.python.org/packages/source/c/collective.templateengines/collective.templateengines-0.3.5.zip
  md5: 2f7a3108640c06d506cc7595d6a60e93
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.templateengines = collective.templateengines:main
    #
    # Would create an entry point called collective.templateengines that calls collective.templateengines.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zope.interface

  run:
    - python
    - setuptools
    - setuptools
    - zope.interface

test:
  # Python imports
  imports:
    - collective
    - collective.templateengines
    - collective.templateengines.backends
    - collective.templateengines.context
    - collective.templateengines.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.twinapex.com/for-developers/open-source/for-plone/easy-template
  license: License :: OSI Approved :: BSD License
  summary: 'Template engine abstraction layer for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
