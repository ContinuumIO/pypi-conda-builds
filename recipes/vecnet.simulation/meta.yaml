package:
  name: vecnet.simulation
  version: "0.1"

source:
  fn: vecnet.simulation-0.1.tar.gz
  url: https://pypi.python.org/packages/source/v/vecnet.simulation/vecnet.simulation-0.1.tar.gz
  md5: 596ee553d243fa0b1666def238a6f251
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - vecnet.simulation = vecnet.simulation:main
    #
    # Would create an entry point called vecnet.simulation that calls vecnet.simulation.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - vecnet
    - vecnet.simulation
    - vecnet.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jimm-domingo/vecnet.simulation
  license: Mozilla Public License 2.0 (MPL 2.0)
  summary: 'Library for simulation models in VecNet'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
