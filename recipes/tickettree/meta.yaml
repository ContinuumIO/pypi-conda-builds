package:
  name: tickettree
  version: "0.9"

source:
  fn: tickettree.0.9.src.tar.gz
  url: https://pypi.python.org/packages/source/T/TicketTree/tickettree.0.9.src.tar.gz
  md5: 93577d6bd1dfe91bb6d4423e94f5fe22
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - tickettree = tickettree:main
    #
    # Would create an entry point called tickettree that calls tickettree.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - tickettree

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://trac-hacks.org/wiki/TicketTree
  license: BSD License
  summary: 'Ticket Tree'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
