package:
  name: extprot
  version: "0.2.4"

source:
  fn: extprot-0.2.4.tar.gz
  url: https://pypi.python.org/packages/source/e/extprot/extprot-0.2.4.tar.gz
  md5: 00304550a8530e0d3027ebe37a6ec0dd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - extprot = extprot:main
    #
    # Would create an entry point called extprot that calls extprot.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - extprot
    - extprot.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/rfk/extprot/tree/master
  license: MIT License
  summary: 'compact, efficient, extensible binary serialization format'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
