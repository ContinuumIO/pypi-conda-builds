package:
  name: flask-couchdb-schematics
  version: "1.0.0"

source:
  fn: Flask-CouchDB-Schematics-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-CouchDB-Schematics/Flask-CouchDB-Schematics-1.0.0.tar.gz
  md5: b7c1640f59bcb676038abee9f8030800
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-couchdb-schematics = flask-couchdb-schematics:main
    #
    # Would create an entry point called flask-couchdb-schematics that calls flask-couchdb-schematics.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - couchdb-schematics
    - pytest

  run:
    - python
    - flask
    - couchdb-schematics
    - pytest

test:
  # Python imports
  imports:
    - flask_couchdb

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/ryanolson/flask-couchdb-schematics/
  license: MIT
  summary: 'Provides utilities for using CouchDB + Schematics with Flask'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
