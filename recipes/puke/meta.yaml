package:
  name: puke
  version: "1.5.22"

source:
  fn: puke-1.5.22.tar.gz
  url: https://pypi.python.org/packages/source/p/puke/puke-1.5.22.tar.gz
  md5: 65a74ef5950018f56b4d2016ef220c5f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - puke = puke:main
    #
    # Would create an entry point called puke that calls puke.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyscss
    - closure_linter
    - colorama
    - pyyaml
    - paramiko
    - requests ==1.2.1

  run:
    - python
    - pyscss
    - closure_linter
    - colorama
    - pyyaml
    - paramiko
    - requests ==1.2.1

test:
  # Python imports
  imports:
    - puke

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/webitup/puke
  license: http://www.gnu.org/copyleft/gpl.html
  summary: 'Puke is a straightforward build system'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
