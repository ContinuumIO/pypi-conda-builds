package:
  name: django-shop-viveum
  version: "0.3.4"

source:
  fn: django-shop-viveum-0.3.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-shop-viveum/django-shop-viveum-0.3.4.tar.gz
  md5: ee5f5e95d574e4fc94ef21faeb4095a5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-shop-viveum = django-shop-viveum:main
    #
    # Would create an entry point called django-shop-viveum that calls django-shop-viveum.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4
    - django-shop
    - requests

  run:
    - python
    - django >=1.4
    - django-shop
    - requests

test:
  # Python imports
  imports:
    - viveum
    - viveum.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jrief/django-shop-viveum
  license: BSD License
  summary: 'A payment backend module for django-SHOP, using Viveum (https://viveum.v-psp.com) as PSP.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
