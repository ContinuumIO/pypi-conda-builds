package:
  name: django-cli-query
  version: "1.3"

source:
  fn: django-cli-query-1.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-cli-query/django-cli-query-1.3.tar.gz
  md5: bc0c8ddb9b9afcdc10178e03cfa6949a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-cli-query = django-cli-query:main
    #
    # Would create an entry point called django-cli-query that calls django-cli-query.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - cli_query
    - cli_query.management
    - cli_query.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/seveas/django-cli-query
  license: UNKNOWN
  summary: 'Django management command for commandline querying'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
