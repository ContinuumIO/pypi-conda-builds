package:
  name: jldssdp
  version: "0.2"

source:
  fn: jldssdp-0.2.tar.gz
  url: https://pypi.python.org/packages/source/j/jldssdp/jldssdp-0.2.tar.gz
  md5: 88c42d2c06e2e95170e326f1bca12403
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jldssdp = jldssdp:main
    #
    # Would create an entry point called jldssdp that calls jldssdp.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - argparse
    - twisted
    - netifaces

  run:
    - python
    - argparse
    - twisted
    - netifaces

test:
  # Python imports
  imports:
    - jldssdp

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jldupont/jldssdp
  license: UNKNOWN
  summary: 'Collection of SSDP related tools'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
