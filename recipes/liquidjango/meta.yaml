package:
  name: liquidjango
  version: "0.2.4"

source:
  fn: liquidjango-0.2.4.tar.gz
  url: https://pypi.python.org/packages/source/l/liquidjango/liquidjango-0.2.4.tar.gz
  md5: 6052157f3f890d59407794e7fd778889
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - liquidjango = liquidjango:main
    #
    # Would create an entry point called liquidjango that calls liquidjango.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - distribute
    - docutils
    - mutagen
    - pil
    - djapian

  run:
    - python
    - distribute
    - docutils
    - mutagen
    - pil
    - djapian

test:
  # Python imports
  imports:
    - liquidjango

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/adalovelace/liquidjango/overview
  license: GNU General Public License (GPL)
  summary: 'an interface to control a radio with liquidsoap'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
