package:
  name: django-sitemessage
  version: "0.6.0"

source:
  fn: django-sitemessage-0.6.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-sitemessage/django-sitemessage-0.6.0.tar.gz
  md5: d6b87e4029085a9d93ff2991cb10adec
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-sitemessage = django-sitemessage:main
    #
    # Would create an entry point called django-sitemessage that calls django-sitemessage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-etc

  run:
    - python
    - django-etc

test:
  # Python imports
  imports:
    - sitemessage

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/idlesign/django-sitemessage
  license: BSD License
  summary: 'Reusable application for Django introducing a message delivery framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
