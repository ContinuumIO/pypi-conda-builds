package:
  name: isotoma.recipe.django
  version: "3.1.5"

source:
  fn: isotoma.recipe.django-3.1.5.tar.gz
  url: https://pypi.python.org/packages/source/i/isotoma.recipe.django/isotoma.recipe.django-3.1.5.tar.gz
  md5: b036167c40061dea0f16117057457c1f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - isotoma.recipe.django = isotoma.recipe.django:main
    #
    # Would create an entry point called isotoma.recipe.django that calls isotoma.recipe.django.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zc.buildout
    - zc.recipe.egg
    - django

  run:
    - python
    - setuptools
    - zc.buildout
    - zc.recipe.egg
    - django

test:
  # Python imports
  imports:
    - isotoma
    - isotoma.recipe
    - isotoma.recipe.django

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/isotoma
  license: BSD License
  summary: 'Buildout recipe for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
