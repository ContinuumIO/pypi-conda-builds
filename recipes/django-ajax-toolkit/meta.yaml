package:
  name: django-ajax-toolkit
  version: "0.3.0"

source:
  fn: django-ajax-toolkit-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-ajax-toolkit/django-ajax-toolkit-0.3.0.tar.gz
  md5: 217c5a821738c7c710ea4d8dda8a16fe
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-ajax-toolkit = django-ajax-toolkit:main
    #
    # Would create an entry point called django-ajax-toolkit that calls django-ajax-toolkit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - msgpack-python ==0.3.0

  run:
    - python
    - msgpack-python ==0.3.0

test:
  # Python imports
  imports:
    - ajaxtoolkit

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Geekfish/django-ajax-toolkit
  license: BSD License
  summary: 'Ajax goodies for django projects.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
