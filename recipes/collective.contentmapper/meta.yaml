package:
  name: collective.contentmapper
  version: "0.12"

source:
  fn: collective.contentMapper-0.12.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.contentMapper/collective.contentMapper-0.12.tar.gz
  md5: c8d963133b9fe2a9d7c6c2344ee621a0
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.contentmapper = collective.contentmapper:main
    #
    # Would create an entry point called collective.contentmapper that calls collective.contentmapper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.tagnavigator

  run:
    - python
    - setuptools
    - setuptools
    - products.tagnavigator

test:
  # Python imports
  imports:
    - collective
    - collective.contentMapper
    - collective.contentMapper.browser
    - collective.contentMapper.portlets

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/
  license: GPL
  summary: 'Overrides collective.tagNavigator and adds a map interface instead of the menu'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
