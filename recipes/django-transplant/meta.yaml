package:
  name: django-transplant
  version: "0.0.2"

source:
  fn: django-transplant-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-transplant/django-transplant-0.0.2.tar.gz
  md5: f776da154a79e352f8e0fdd85121fbf5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-transplant = django-transplant:main
    #
    # Would create an entry point called django-transplant that calls django-transplant.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.2.5

  run:
    - python
    - django >=1.2.5

test:
  # Python imports
  imports:
    - transplant
    - transplant.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/lolek09/django-transplant
  license: BSD License
  summary: 'Automated merges of User accounts.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
