package:
  name: django-statistic
  version: "1.0"

source:
  fn: django-statistic-1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-statistic/django-statistic-1.0.tar.gz
  md5: 7f0e1b813b5eeefa695b532a7bee2f63
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-statistic = django-statistic:main
    #
    # Would create an entry point called django-statistic that calls django-statistic.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - statistic

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/unk2k/django-statistic
  license: BSD License
  summary: 'Statistic for django models'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
