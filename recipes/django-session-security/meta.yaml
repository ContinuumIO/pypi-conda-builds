package:
  name: django-session-security
  version: "2.2.3"

source:
  fn: django-session-security-2.2.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-session-security/django-session-security-2.2.3.tar.gz
  md5: 403e0bf916bf61f16ab2279f53853c10
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-session-security = django-session-security:main
    #
    # Would create an entry point called django-session-security that calls django-session-security.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - session_security
    - session_security.templatetags
    - session_security.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/yourlabs/django-session-security
  license: MIT License
  summary: 'Let the user secure his session for usage in public computers'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
