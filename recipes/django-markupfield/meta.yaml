package:
  name: django-markupfield
  version: "1.3.4"

source:
  fn: django-markupfield-1.3.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-markupfield/django-markupfield-1.3.4.tar.gz
  md5: e03aee5fc2b0d7b00f11c4a38bd0f5be
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-markupfield = django-markupfield:main
    #
    # Would create an entry point called django-markupfield that calls django-markupfield.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - markupfield
    - markupfield.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/jamesturk/django-markupfield/
  license: BSD License
  summary: 'Custom Django field for easy use of markup in text fields'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
