package:
  name: largeman
  version: "0.1.0"

source:
  fn: largeman-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/l/largeman/largeman-0.1.0.tar.gz
  md5: 2ace232dd0d729dcbd461cbc0118e9be
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - largeman = largeman:main
    #
    # Would create an entry point called largeman that calls largeman.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - largeman

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/sneeu/largeman
  license: MIT License
  summary: 'A little library for making, and using state machines.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
