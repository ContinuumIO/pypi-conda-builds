package:
  name: python-hessian
  version: "1.0.1"

source:
  fn: python-hessian-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/p/python-hessian/python-hessian-1.0.1.tar.gz
  md5: edcb86b76b6080ea833f9efe47b92619
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-hessian = python-hessian:main
    #
    # Would create an entry point called python-hessian that calls python-hessian.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - pyhessian
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/theatlantic/python-hessian
  license: BSD License
  summary: 'Hessian RPC Library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
