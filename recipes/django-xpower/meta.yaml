package:
  name: django-xpower
  version: "0.1.0"

source:
  fn: django-xpower-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-xpower/django-xpower-0.1.0.tar.gz
  md5: 040d4557f518889b904f62a9a15320d9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-xpower = django-xpower:main
    #
    # Would create an entry point called django-xpower that calls django-xpower.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - xpower

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/justquick/django-xpower
  license: BSD License
  summary: 'Adds the X-Powered-By header to your Django site'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
