package:
  name: django_reportview
  version: "0.1"

source:
  fn: django_reportview-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django_reportview/django_reportview-0.1.tar.gz
  md5: 0f84db1080e0c97d7d6fc9971576bf65
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_reportview = django_reportview:main
    #
    # Would create an entry point called django_reportview that calls django_reportview.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - reportlab

  run:
    - python
    - reportlab

test:
  # Python imports
  imports:
    - django_reportview
    - reportview

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://dashdrum.com/reportview/
  license: NCSA
  summary: 'Class-based views for PDF output'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
