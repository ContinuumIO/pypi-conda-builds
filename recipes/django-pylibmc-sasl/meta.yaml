package:
  name: django-pylibmc-sasl
  version: "0.2.4"

source:
  fn: django-pylibmc-sasl-0.2.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-pylibmc-sasl/django-pylibmc-sasl-0.2.4.tar.gz
  md5: e56bfc8ed4e574c2c0b5fdea3c8c2b2b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-pylibmc-sasl = django-pylibmc-sasl:main
    #
    # Would create an entry point called django-pylibmc-sasl that calls django-pylibmc-sasl.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pylibmc
    - django >=1.2

  run:
    - python
    - pylibmc
    - django >=1.2

test:
  # Python imports
  imports:
    - django_pylibmc

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/kennethreitz/django-pylibmc-sasl
  license: BSD License
  summary: 'Django cache backend using pylibmc-sasl'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
