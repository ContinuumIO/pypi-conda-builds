package:
  name: zope.security
  version: "4.0.1"

source:
  fn: zope.security-4.0.1.tar.gz
  url: https://pypi.python.org/packages/source/z/zope.security/zope.security-4.0.1.tar.gz
  md5: 27d1f2873a0ee9c1f485f7b8f22d8e1c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zope.security = zope.security:main
    #
    # Would create an entry point called zope.security that calls zope.security.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.component
    - zope.i18nmessageid
    - zope.interface
    - zope.location
    - zope.proxy >=4.1.0
    - zope.schema

  run:
    - python
    - setuptools
    - zope.component
    - zope.i18nmessageid
    - zope.interface
    - zope.location
    - zope.proxy >=4.1.0
    - zope.schema

test:
  # Python imports
  imports:
    - zope
    - zope.security
    - zope.security.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/zope.security
  license: Zope Public License
  summary: 'Zope Security Framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
