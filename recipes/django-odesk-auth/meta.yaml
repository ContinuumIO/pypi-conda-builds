package:
  name: django-odesk-auth
  version: "0.1.0"

source:
  fn: django-odesk-auth-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-odesk-auth/django-odesk-auth-0.1.0.tar.gz
  md5: a29b1c93d71701ca217c5c57b955d66c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-odesk-auth = django-odesk-auth:main
    #
    # Would create an entry point called django-odesk-auth that calls django-odesk-auth.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_odesk_auth

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: BSD License
  summary: 'A simple Django app for basic \xe2\x80\x9cLog in via oDesk\xe2\x80\x9d functionality.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
