package:
  name: django-admin-user-stats
  version: "0.2"

source:
  fn: django-admin-user-stats-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-admin-user-stats/django-admin-user-stats-0.2.tar.gz
  md5: 9083a16ee1ed4f75f2dcdd8692b6298f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-admin-user-stats = django-admin-user-stats:main
    #
    # Would create an entry point called django-admin-user-stats that calls django-admin-user-stats.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - admin_user_stats

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kmike/django-admin-user-stats/
  license: MIT License
  summary: 'django-admin-tools dashboard modules with user registration stats'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
