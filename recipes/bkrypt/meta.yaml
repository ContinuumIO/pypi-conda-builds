package:
  name: bkrypt
  version: "0.1.1"

source:
  fn: bkrypt-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/b/bkrypt/bkrypt-0.1.1.tar.gz
  md5: 3807087e4cf90bd4bc3413cb7a346965
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bkrypt = bkrypt:main
    #
    # Would create an entry point called bkrypt that calls bkrypt.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - py-bcrypt

  run:
    - python
    - py-bcrypt

test:
  # Python imports
  imports:
    - bkrypt

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/nvie/bkrypt
  license: BSD License
  summary: 'bkrypt: Easy Password wrapper around BCrypt.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
