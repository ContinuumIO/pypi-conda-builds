package:
  name: django-roberts
  version: "0.1.3"

source:
  fn: django-roberts-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-roberts/django-roberts-0.1.3.tar.gz
  md5: 9d43930a45e3df6ecb9eac2dbe2e2115
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-roberts = django-roberts:main
    #
    # Would create an entry point called django-roberts that calls django-roberts.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - robots

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/mhurt/django-roberts
  license: MIT License
  summary: 'A simple Django app to provide access to a robots.txt file'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
