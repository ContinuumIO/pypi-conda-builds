package:
  name: django-announce
  version: "0.1.5"

source:
  fn: django-announce-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-announce/django-announce-0.1.5.tar.gz
  md5: 878f75b7f4e488b541be41fd8e7717df
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-announce = django-announce:main
    #
    # Would create an entry point called django-announce that calls django-announce.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - announce
    - announce.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/ozkatz/django-announce/
  license: MIT License
  summary: 'an Announce.js client for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
