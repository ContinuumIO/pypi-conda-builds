package:
  name: django-staticfiles-jquery-ui
  version: "1.8.24"

source:
  fn: django-staticfiles-jquery-ui-1.8.24.tar.gz
  url: https://pypi.python.org/packages/source/d/django-staticfiles-jquery-ui/django-staticfiles-jquery-ui-1.8.24.tar.gz
  md5: d7cb2a4815401633583f59c1c4b7f5ad
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-staticfiles-jquery-ui = django-staticfiles-jquery-ui:main
    #
    # Would create an entry point called django-staticfiles-jquery-ui that calls django-staticfiles-jquery-ui.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - jquery_ui

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://byteorbit.com/
  license: BSD
  summary: 'jquery-ui packaged for django-staticfiles'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
