package:
  name: metals
  version: "0.1.1dev"

source:
  fn: metals-0.1.1dev.tar.gz
  url: https://pypi.python.org/packages/source/m/metals/metals-0.1.1dev.tar.gz
  md5: ffef39210254dcc2ec144a39362e890c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - metals = metals:main
    #
    # Would create an entry point called metals that calls metals.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sqlalchemy
    - imdbpy
    - termcolor

  run:
    - python
    - setuptools
    - sqlalchemy
    - imdbpy
    - termcolor

test:
  # Python imports
  imports:
    - metals

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/metals/
  license: MIT License
  summary: 'list metadata for (movie) files on the command line'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
