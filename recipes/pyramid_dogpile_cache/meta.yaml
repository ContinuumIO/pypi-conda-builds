package:
  name: pyramid_dogpile_cache
  version: "0.0.4"

source:
  fn: pyramid_dogpile_cache-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/p/pyramid_dogpile_cache/pyramid_dogpile_cache-0.0.4.tar.gz
  md5: 2184aeb42c78ad9b4e7f38c6b8d2942e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pyramid_dogpile_cache = pyramid_dogpile_cache:main
    #
    # Would create an entry point called pyramid_dogpile_cache that calls pyramid_dogpile_cache.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyramid
    - dogpile.cache

  run:
    - python
    - setuptools
    - pyramid
    - dogpile.cache

test:
  # Python imports
  imports:
    - pyramid_dogpile_cache

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/moriyoshi/pyramid_dogpile_cache
  license: MIT License
  summary: 'dogpile.cache factory for Pyramid'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
