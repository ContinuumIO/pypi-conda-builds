package:
  name: collective.transform.creole
  version: "1.1"

source:
  fn: collective.transform.creole-1.1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.transform.creole/collective.transform.creole-1.1.tar.gz
  md5: 85bcefe9ce6ebdcaf72c9cc56db38dc3
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.transform.creole = collective.transform.creole:main
    #
    # Would create an entry point called collective.transform.creole that calls collective.transform.creole.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - creoleparser

  run:
    - python
    - setuptools
    - setuptools
    - creoleparser

test:
  # Python imports
  imports:
    - collective
    - collective.transform
    - collective.transform.creole

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://svn.plone.org/svn/collective/collective.transform.creole
  license: GPL
  summary: 'Creole wiki text transform for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
