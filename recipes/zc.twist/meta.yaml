package:
  name: zc.twist
  version: "1.3.1"

source:
  fn: zc.twist-1.3.1.tar.gz
  url: https://pypi.python.org/packages/source/z/zc.twist/zc.twist-1.3.1.tar.gz
  md5: 60bc9506455d9675a280880b74cd4427
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zc.twist = zc.twist:main
    #
    # Would create an entry point called zc.twist that calls zc.twist.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - zodb3
    - twisted >=8.0.1
    - zope.component
    - setuptools
    - zope.testing

  run:
    - python
    - zodb3
    - twisted >=8.0.1
    - zope.component
    - setuptools
    - zope.testing

test:
  # Python imports
  imports:
    - zc
    - zc.twist

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/zc.twist
  license: ZPL
  summary: 'Mixing Twisted and ZODB\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
