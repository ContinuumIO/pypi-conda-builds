package:
  name: collective.slideshowfolder
  version: "0.8"

source:
  fn: collective.slideshowfolder-0.8.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.slideshowfolder/collective.slideshowfolder-0.8.tar.gz
  md5: c54f19d19efdc8ec91fadbcf3be2d33c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.slideshowfolder = collective.slideshowfolder:main
    #
    # Would create an entry point called collective.slideshowfolder that calls collective.slideshowfolder.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.slideshowfolder

  run:
    - python
    - setuptools
    - setuptools
    - products.slideshowfolder

test:
  # Python imports
  imports:
    - collective
    - collective.slideshowfolder
    - collective.slideshowfolder.portlets

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/collective.slideshowfolder
  license: GPL
  summary: 'Somtimes useful extensions to Products.slideshowfolder'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
