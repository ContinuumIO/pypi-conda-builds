package:
  name: ariane
  version: "0.0.5"

source:
  fn: Ariane-0.0.5.tar.gz
  url: https://pypi.python.org/packages/source/A/Ariane/Ariane-0.0.5.tar.gz
  md5: a78dcef245e760b7cad7e53da96e6085
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ariane = ariane:main
    #
    # Would create an entry point called ariane that calls ariane.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - jinja2 >=0.7.6

  run:
    - python
    - jinja2 >=0.7.6

test:
  # Python imports
  imports:
    - ariane
    - ariane.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/guillaumevincent/Ariane
  license: wtfpl - Do what the fuck you want to public license
  summary: 'Ariane is a python package for describing, producing and visualizing RESTful APIs'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
