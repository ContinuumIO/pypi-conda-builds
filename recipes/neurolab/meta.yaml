package:
  name: neurolab
  version: "0.3.5"

source:
  fn: neurolab-0.3.5.tar.gz
  url: https://pypi.python.org/packages/source/n/neurolab/neurolab-0.3.5.tar.gz
  md5: 3eedda67bacb6bf63ed8448ff5b790d5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - neurolab = neurolab:main
    #
    # Would create an entry point called neurolab that calls neurolab.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - neurolab
    - neurolab.train

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://neurolab.googlecode.com
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Simple and powerfull neural network library for python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
