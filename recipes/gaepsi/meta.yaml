package:
  name: gaepsi
  version: "0.2"

source:
  fn: gaepsi-0.2.tar.gz
  url: http://web.phys.cmu.edu/~yfeng1/gaepsi/gaepsi-0.2.tar.gz
  #md5: 
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gaepsi = gaepsi:main
    #
    # Would create an entry point called gaepsi that calls gaepsi.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - numpy
    - sharedmem
    - chealpy

  run:
    - python
    - numpy
    - sharedmem
    - chealpy

test:
  # Python imports
  imports:
    - gaepsi
    - gaepsi.cosmology
    - gaepsi.cython
    - gaepsi.readers
    - gaepsi.tools

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/rainwoodman/gaepsi
  license: UNKNOWN
  summary: 'Visualization and Analysis toolkit for SPH Cosmology code GADGET'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
