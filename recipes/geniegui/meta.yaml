package:
  name: geniegui
  version: "0.99"

source:
  fn: geniegui-0.99.zip
  url: https://pypi.python.org/packages/source/g/geniegui/geniegui-0.99.zip
  md5: 06eeed47a9dcfcae062908fd34ee9255
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - geniegui = geniegui:main
    #
    # Would create an entry point called geniegui that calls geniegui.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - geniegui

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/svenrahmann/geniegui/
  license: MIT License
  summary: 'generate and run a GUI from an argparse.ArgumentParser object of a console application'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
