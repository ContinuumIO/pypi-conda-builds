package:
  name: django-bitcoin
  version: "0.2"

source:
  fn: django-bitcoin-0.2.zip
  url: https://pypi.python.org/packages/source/d/django-bitcoin/django-bitcoin-0.2.zip
  md5: 22f1f2e69b97abe0b12d94c9cdc6de68
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-bitcoin = django-bitcoin:main
    #
    # Would create an entry point called django-bitcoin that calls django-bitcoin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_bitcoin
    - django_bitcoin.jsonrpc
    - django_bitcoin.management
    - django_bitcoin.management.commands
    - django_bitcoin.migrations
    - django_bitcoin.templates
    - django_bitcoin.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kangasbros/django-bitcoin
  license: MIT
  summary: 'Bitcoin application integration for Django web framework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
