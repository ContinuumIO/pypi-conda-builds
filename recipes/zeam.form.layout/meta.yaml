package:
  name: zeam.form.layout
  version: "0.3.1"

source:
  fn: zeam.form.layout-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/z/zeam.form.layout/zeam.form.layout-0.3.1.tar.gz
  md5: 6f8241d15363087979d97d36ed3b74b6
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zeam.form.layout = zeam.form.layout:main
    #
    # Would create an entry point called zeam.form.layout that calls zeam.form.layout.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.component
    - zope.publisher
    - grokcore.component
    - grokcore.layout
    - grokcore.chameleon
    - zeam.form.base >=1.0
    - zeam.form.composed >=1.2
    - megrok.pagetemplate

  run:
    - python
    - setuptools
    - zope.component
    - zope.publisher
    - grokcore.component
    - grokcore.layout
    - grokcore.chameleon
    - zeam.form.base >=1.0
    - zeam.form.composed >=1.2
    - megrok.pagetemplate

test:
  # Python imports
  imports:
    - zeam
    - zeam.form
    - zeam.form.layout
    - zeam.form.layout.ftests
    - zeam.form.layout.ftests.layout
    - zeam.form.layout.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: BSD
  summary: 'grokcore.layout support for zeam.form'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
