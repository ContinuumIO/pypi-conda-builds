package:
  name: collective.hootsuite
  version: "1.1.2"

source:
  fn: collective.hootsuite-1.1.2.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.hootsuite/collective.hootsuite-1.1.2.tar.gz
  md5: 202288ddc04f6a3cdfaf31d83c8f20c7
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.hootsuite = collective.hootsuite:main
    #
    # Would create an entry point called collective.hootsuite that calls collective.hootsuite.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.hootsuite
    - collective.hootsuite.browser
    - collective.hootsuite.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/
  license: gpl
  summary: 'Hootsuite Plone connector'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
