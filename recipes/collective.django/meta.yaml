package:
  name: collective.django
  version: "1.0"

source:
  fn: collective.django-1.0.zip
  url: https://pypi.python.org/packages/source/c/collective.django/collective.django-1.0.zip
  md5: 20cb83d2ad850e87afcdd8924462ec6c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.django = collective.django:main
    #
    # Would create an entry point called collective.django that calls collective.django.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - distribute
    - django
    - zope.interface
    - transaction
    - zope.configuration
    - zope.i18nmessageid

  run:
    - python
    - setuptools
    - setuptools
    - distribute
    - django
    - zope.interface
    - transaction
    - zope.configuration
    - zope.i18nmessageid

test:
  # Python imports
  imports:
    - collective
    - collective.django
    - collective.django.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/collective/collective.django
  license: BSD License
  summary: 'Django transaction integration for Plone'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
