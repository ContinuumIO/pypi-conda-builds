package:
  name: collective.pfg.sslfield
  version: "0.5"

source:
  fn: collective.pfg.sslfield-0.5.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.pfg.sslfield/collective.pfg.sslfield-0.5.tar.gz
  md5: bf883e716e24c4af07beaad164461055
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.pfg.sslfield = collective.pfg.sslfield:main
    #
    # Would create an entry point called collective.pfg.sslfield that calls collective.pfg.sslfield.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.pfg
    - collective.pfg.sslfield
    - collective.pfg.sslfield.Extensions
    - collective.pfg.sslfield.content

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/collective.pfg.sslfield
  license: GPL
  summary: 'New field for PloneFormGen to extract SSL data from the request'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
