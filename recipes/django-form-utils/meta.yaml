package:
  name: django-form-utils
  version: "1.0.2"

source:
  fn: django-form-utils-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-form-utils/django-form-utils-1.0.2.tar.gz
  md5: 83befed61328aacc431481e3a00fc81a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-form-utils = django-form-utils:main
    #
    # Would create an entry point called django-form-utils that calls django-form-utils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - form_utils
    - form_utils.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/carljm/django-form-utils/
  license: BSD License
  summary: 'Form utilities for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
