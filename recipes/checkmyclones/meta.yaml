package:
  name: checkmyclones
  version: "0.0.10"

source:
  fn: checkmyclones-0.0.10.tar.gz
  url: https://pypi.python.org/packages/source/c/checkmyclones/checkmyclones-0.0.10.tar.gz
  md5: a0294d684c0a3e33e5b6d1583c564aaf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - checkmyclones = checkmyclones:main
    #
    # Would create an entry point called checkmyclones that calls checkmyclones.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - checkmyclones
    - clonechecker

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/benjschiller/checkmyclones
  license: UNKNOWN
  summary: 'Provides tools to check Sanger sequencing results\n\t      (or any plain-text or FASTQ files) against a set of reference\n\t      sequences, provided in any reasonable format (including coordinates)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
