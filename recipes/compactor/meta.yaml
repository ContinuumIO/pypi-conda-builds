package:
  name: compactor
  version: "0.3.0"

source:
  fn: compactor-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/c/compactor/compactor-0.3.0.tar.gz
  md5: 3e126fe8e00267af2399c7a40fcf3f67
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - compactor = compactor:main
    #
    # Would create an entry point called compactor that calls compactor.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - trollius
    - tornado ==4.1
    - twitter.common.lang

  run:
    - python
    - trollius
    - tornado ==4.1
    - twitter.common.lang

test:
  # Python imports
  imports:
    - compactor

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/wickman/compactor
  license: Apache License 2.0
  summary: 'Pure python implementation of libprocess actors'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
