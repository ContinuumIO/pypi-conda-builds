package:
  name: geeprog
  version: "2.0.1"

source:
  fn: gEEProg-2.0.1.tar.gz
  url: https://pypi.python.org/packages/source/g/gEEProg/gEEProg-2.0.1.tar.gz
  md5: 5408fc43f6daa971ebcbb37c082beee4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - geeprog = geeprog:main
    #
    # Would create an entry point called geeprog that calls geeprog.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyserial >=2.7

  run:
    - python
    - pyserial >=2.7

test:
  # Python imports
  imports:
    - geeprog

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.dasarodesigns.com/
  license: GPL
  summary: "GUI to control D'Asaro Designs EEPROM programmers."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
