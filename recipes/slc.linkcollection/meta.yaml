package:
  name: slc.linkcollection
  version: "1.4.2"

source:
  fn: slc.linkcollection-1.4.2.zip
  url: https://pypi.python.org/packages/source/s/slc.linkcollection/slc.linkcollection-1.4.2.zip
  md5: 8c8db6aaa84b8e2ac4f0f2f11c78474e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - slc.linkcollection = slc.linkcollection:main
    #
    # Would create an entry point called slc.linkcollection that calls slc.linkcollection.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.browserlayer

  run:
    - python
    - setuptools
    - setuptools
    - plone.browserlayer

test:
  # Python imports
  imports:
    - slc
    - slc.linkcollection
    - slc.linkcollection.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/plone/plone.example
  license: GNU General Public License (GPL) or European Union Public Licence 1.1 (EUPL 1.1)
  summary: 'A viewlet displaying links to subdocuments and quickly loading their content via jquery'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
