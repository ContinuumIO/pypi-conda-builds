package:
  name: django-fs-email-helper
  version: "0.2.1"

source:
  fn: django-fs-email-helper-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-fs-email-helper/django-fs-email-helper-0.2.1.tar.gz
  md5: 3836fba596d9b922da4ab9166da65afb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-fs-email-helper = django-fs-email-helper:main
    #
    # Would create an entry point called django-fs-email-helper that calls django-fs-email-helper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - html2text

  run:
    - python
    - html2text

test:
  # Python imports
  imports:
    - email_helper

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/fogstream/django-fs-email-helper
  license: The MIT License (MIT)
  summary: 'The Django-related reusable app provides the ability to send multipart emails and store them in a database.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
