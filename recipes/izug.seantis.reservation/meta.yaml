package:
  name: izug.seantis.reservation
  version: "0.7"

source:
  fn: izug.seantis.reservation-0.7.tar.gz
  url: https://pypi.python.org/packages/source/i/izug.seantis.reservation/izug.seantis.reservation-0.7.tar.gz
  md5: e91cc0e851c7ff0aaf38dae4d46b1551
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - izug.seantis.reservation = izug.seantis.reservation:main
    #
    # Would create an entry point called izug.seantis.reservation that calls izug.seantis.reservation.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - plone >=4.1setuptools
    - seantis.reservation
    - izug.basetheme
    - collective.geo.zugmap

  run:
    - python
    - setuptools
    - plone >=4.1setuptools
    - seantis.reservation
    - izug.basetheme
    - collective.geo.zugmap

test:
  # Python imports
  imports:
    - izug
    - izug.seantis
    - izug.seantis.reservation

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/
  license: GPL
  summary: 'iZug theme adjustments for seantis.reservation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
