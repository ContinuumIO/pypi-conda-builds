package:
  name: django-oraclepool
  version: "1.4"

source:
  fn: django-oraclepool-1.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-oraclepool/django-oraclepool-1.4.tar.gz
  md5: c0b10abf7e8c55a844e6bc7ed922109a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-oraclepool = django-oraclepool:main
    #
    # Would create an entry point called django-oraclepool that calls django-oraclepool.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - oraclepool

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/edcrewe/django-oraclepool
  license: Apache Software License
  summary: 'django database backend that uses cx_Oracle session pooling for connections'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
