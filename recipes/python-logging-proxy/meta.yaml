package:
  name: python-logging-proxy
  version: "1.0.7.7"

source:
  fn: python-logging-proxy-1.0.7.7.tar.gz
  url: https://pypi.python.org/packages/source/p/python-logging-proxy/python-logging-proxy-1.0.7.7.tar.gz
  md5: a60207fc0dc4f0bf708538fe79800425
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-logging-proxy = python-logging-proxy:main
    #
    # Would create an entry point called python-logging-proxy that calls python-logging-proxy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyopenssl
    - pymiproxy

  run:
    - python
    - pyopenssl
    - pymiproxy

test:
  # Python imports
  imports:
    - python_logging_proxy

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/alistair-broomhead/python-logging-proxy
  license: MIT
  summary: 'Logging Proxy adapting pymiproxy'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
