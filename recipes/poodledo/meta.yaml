package:
  name: poodledo
  version: "0.2"

source:
  fn: poodledo-0.2.tar.gz
  url: https://pypi.python.org/packages/source/p/poodledo/poodledo-0.2.tar.gz
  md5: a8a47fe9cb0199064ea3e53318f7e329
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - poodledo = poodledo:main
    #
    # Would create an entry point called poodledo that calls poodledo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - parsedatetime
    - python-dateutil

  run:
    - python
    - parsedatetime
    - python-dateutil

test:
  # Python imports
  imports:
    - poodledo

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/handyman5/poodledo
  license: BSD License
  summary: 'a Python library for working with Toodledo'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
