package:
  name: happy-blog
  version: "0.2.2"

source:
  fn: happy-blog-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/h/happy-blog/happy-blog-0.2.2.tar.gz
  md5: 539f2dcd2f3b110ac58b98fb17edc979
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - happy-blog = happy-blog:main
    #
    # Would create an entry point called happy-blog that calls happy-blog.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.2
    - django-cms >=2.0
    - django-hvad
    - django-tinymce
    - django-simplegallery-hvad

  run:
    - python
    - django >=1.2
    - django-cms >=2.0
    - django-hvad
    - django-tinymce
    - django-simplegallery-hvad

test:
  # Python imports
  imports:
    - happy_blog
    - happy_blog.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/neo64bit/happy-blog
  license: BSD License
  summary: 'A simple django blog app that uses django-hvad for translations.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
