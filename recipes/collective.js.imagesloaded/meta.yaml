package:
  name: collective.js.imagesloaded
  version: "2.1"

source:
  fn: collective.js.imagesloaded-2.1.zip
  url: https://pypi.python.org/packages/source/c/collective.js.imagesloaded/collective.js.imagesloaded-2.1.zip
  md5: 2b1b8e13ae3c2c3c79f601bfbb836e18
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.js.imagesloaded = collective.js.imagesloaded:main
    #
    # Would create an entry point called collective.js.imagesloaded that calls collective.js.imagesloaded.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.js
    - collective.js.imagesloaded

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.js.imagesloaded
  license: GNU General Public License (GPL)
  summary: 'Register JQuery imagesloaded plugin in Plone resource registry'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
