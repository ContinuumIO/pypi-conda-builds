package:
  name: django_jalali
  version: "1.1"

source:
  fn: django-jalali-1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django_jalali/django-jalali-1.1.tar.gz
  md5: fe39d13f37af9fba2bd35784d506b4bf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_jalali = django_jalali:main
    #
    # Would create an entry point called django_jalali that calls django_jalali.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - jdatetime
    - django

  run:
    - python
    - jdatetime
    - django

test:
  # Python imports
  imports:
    - django_jalali
    - django_jalali.admin
    - django_jalali.db
    - django_jalali.forms
    - django_jalali.templatetags
    - django_jalali.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/slashmili/django-jalali
  license: Python Software Foundation License
  summary: 'Jalali Date support for Django model and admin interface'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
