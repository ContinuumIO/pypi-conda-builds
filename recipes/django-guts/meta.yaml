package:
  name: django-guts
  version: "0.1.6"

source:
  fn: django-guts-0.1.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-guts/django-guts-0.1.6.tar.gz
  md5: 0e4da11f34d0063fc4b268a63075739b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-guts = django-guts:main
    #
    # Would create an entry point called django-guts that calls django-guts.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_guts

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/svetlyak40wt/django-guts/
  license: BSD License
  summary: 'Funny application to show sources of your Django project.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
