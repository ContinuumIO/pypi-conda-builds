package:
  name: sqlbean
  version: "0.603"

source:
  fn: sqlbean-0.603.tar.gz
  url: https://pypi.python.org/packages/source/s/sqlbean/sqlbean-0.603.tar.gz
  md5: fd3262e18eadb5eb14da6615af4db032
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sqlbean = sqlbean:main
    #
    # Would create an entry point called sqlbean that calls sqlbean.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - dbutils
    - decorator

  run:
    - python
    - dbutils
    - decorator

test:
  # Python imports
  imports:
    - sqlbean
    - sqlbean.db
    - sqlbean.example

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'A auto maping ORM for MYSQL and can bind with memcached'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
