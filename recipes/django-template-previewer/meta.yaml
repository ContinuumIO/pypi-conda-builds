package:
  name: django-template-previewer
  version: "0.1.3"

source:
  fn: django-template-previewer-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-template-previewer/django-template-previewer-0.1.3.tar.gz
  md5: 6f97b8c21d17f41674cee9c4668df06a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-template-previewer = django-template-previewer:main
    #
    # Would create an entry point called django-template-previewer that calls django-template-previewer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - template_previewer
    - template_previewer.template_parser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/machinalis/django-template-previewer
  license: BSD License
  summary: u'A Django app to allow developers preview templates'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
