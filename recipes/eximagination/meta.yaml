package:
  name: eximagination
  version: "0.7"

source:
  fn: eximagination-0.7.tar.gz
  url: https://pypi.python.org/packages/source/e/eximagination/eximagination-0.7.tar.gz
  md5: 3b89a178ace7c16211e86bfb8e42da6c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - eximagination = eximagination:main
    #
    # Would create an entry point called eximagination that calls eximagination.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six ==1.4.1
    - pillow ==2.2.1

  run:
    - python
    - six ==1.4.1
    - pillow ==2.2.1

test:
  # Python imports
  imports:
    - eximagination
    - eximagination.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/barseghyanartur/eximagination
  license: GPL 2.0/LGPL 2.1
  summary: 'Eximagination package for copying external images in tempalate tags and storing them locally.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
