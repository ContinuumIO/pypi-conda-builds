package:
  name: jaws-scraper
  version: "0.1.0"

source:
  fn: jaws-scraper-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/j/jaws-scraper/jaws-scraper-0.1.0.tar.gz
  md5: 99eda24ccc889d2dad41aa47c4dd547c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jaws-scraper = jaws-scraper:main
    #
    # Would create an entry point called jaws-scraper that calls jaws-scraper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - mechanize ==0.2.5
    - requests ==2.2.1

  run:
    - python
    - mechanize ==0.2.5
    - requests ==2.2.1

test:
  # Python imports
  imports:
    - jaws

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/iccelou91/JAWS
  license: LICENSE.txt
  summary: 'Just Another Web Scraper.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
