package:
  name: duolingo-graph
  version: "0.3.0"

source:
  fn: duolingo-graph-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/d/duolingo-graph/duolingo-graph-0.3.0.tar.gz
  md5: b1991b71b69a277d2c413de4a901b776
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - duolingo-graph = duolingo-graph:main
    #
    # Would create an entry point called duolingo-graph that calls duolingo-graph.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - duolingo_graph

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Undeterminant/duolingo-graph
  license: CC0
  summary: 'Utilities to graph the courses on Duolingo.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
