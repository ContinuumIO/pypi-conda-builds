package:
  name: django-rest-hooks-ng
  version: "1.1.1"

source:
  fn: django-rest-hooks-ng-1.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-rest-hooks-ng/django-rest-hooks-ng-1.1.1.tar.gz
  md5: 1b8dd83f8dc41e7ff5804396c6000ae4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-rest-hooks-ng = django-rest-hooks-ng:main
    #
    # Would create an entry point called django-rest-hooks-ng that calls django-rest-hooks-ng.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django >=1.4
    - requests

  run:
    - python
    - django >=1.4
    - requests

test:
  # Python imports
  imports:
    - rest_hooks

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/PressLabs/django-rest-hooks-ng
  license: BSD License
  summary: 'A powerful mechanism for sending real time API notifications via a new subscription model.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
