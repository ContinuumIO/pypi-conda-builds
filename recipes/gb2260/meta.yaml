package:
  name: gb2260
  version: "0.3.1"

source:
  fn: GB2260-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/G/GB2260/GB2260-0.3.1.tar.gz
  md5: e966836273ef741c453a72ce3539f42c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gb2260 = gb2260:main
    #
    # Would create an entry point called gb2260 that calls gb2260.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - gb2260

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/cn/GB2260.py
  license: BSD License
  summary: 'The Python implementation for looking up the Chinese administrative divisions.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
