package:
  name: django-localflavor-sl
  version: "0.3"

source:
  fn: django-localflavor-sl-0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-localflavor-sl/django-localflavor-sl-0.3.tar.gz
  md5: 3747353c0b7cc529ad29575a03807ac3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-localflavor-sl = django-localflavor-sl:main
    #
    # Would create an entry point called django-localflavor-sl that calls django-localflavor-sl.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - localflavor
    - localflavor.sl

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'Slovenian Django localflavor'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
