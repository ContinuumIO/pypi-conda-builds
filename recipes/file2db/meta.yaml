package:
  name: file2db
  version: "0.5.0"

source:
  fn: file2db-0.5.0.tar.gz
  url: https://pypi.python.org/packages/source/f/file2db/file2db-0.5.0.tar.gz
  md5: 472d9f33980ead4b07c979aeee60c036
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - file2db = file2db:main
    #
    # Would create an entry point called file2db that calls file2db.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - file2db

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/moosejaw/file2db
  license: GNU General Public License v3 (GPLv3)
  summary: 'Simple file to database manipulation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
