package:
  name: aboutyou
  version: "1.0.1"

source:
  fn: aboutyou-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/a/aboutyou/aboutyou-1.0.1.tar.gz
  md5: 4a98d7d32121ae3461d34f87ddbc7754
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - aboutyou = aboutyou:main
    #
    # Would create an entry point called aboutyou that calls aboutyou.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pylibmc >=1.3.0
    - pyyaml

  run:
    - python
    - pylibmc >=1.3.0
    - pyyaml

test:
  # Python imports
  imports:
    - aboutyou
    - aboutyou.django

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/slicedice/aboutyou-shop-sdk-python/overview
  license: MIT
  summary: 'A connection to the aboutyou.de shop.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
