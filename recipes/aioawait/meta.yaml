package:
  name: aioawait
  version: "8"

source:
  fn: aioawait-8.tar.gz
  url: https://pypi.python.org/packages/source/a/aioawait/aioawait-8.tar.gz
  md5: f3f9e667461b19eb76599b7194a061fa
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - aioawait = aioawait:main
    #
    # Would create an entry point called aioawait that calls aioawait.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - aioawait

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/carlopires/aioawait
  license: MIT License
  summary: 'Call asynchronous functions of Python 3.4.3 asyncio infrastructure from synchronous code'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
