package:
  name: iccommunity.mailman
  version: "1.0-dev-r371"

source:
  fn: iccommunity.mailman-1.0-dev-r371.tar.gz
  url: https://pypi.python.org/packages/source/i/iccommunity.mailman/iccommunity.mailman-1.0-dev-r371.tar.gz
  md5: a0dc7aea4a4ffec870ce88afa4336ae6
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - iccommunity.mailman = iccommunity.mailman:main
    #
    # Would create an entry point called iccommunity.mailman that calls iccommunity.mailman.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - iccommunity
    - iccommunity.mailman
    - iccommunity.mailman.Extensions
    - iccommunity.mailman.browser
    - iccommunity.mailman.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.platecom.com/productos/iccommunity.mailman/
  license: GNU General Public License (GPL)
  summary: 'icSemantic.Mailman'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
