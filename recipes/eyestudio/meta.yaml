package:
  name: eyestudio
  version: "1.0.0"

source:
  fn: eyestudio-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/e/eyestudio/eyestudio-1.0.0.tar.gz
  md5: 73c7b20c8e1e2432296b02027f6f97dd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - eyestudio = eyestudio:main
    #
    # Would create an entry point called eyestudio that calls eyestudio.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - eyestudio
    - eyestudio.Analysis
    - eyestudio.Engine
    - eyestudio.Filtering
    - eyestudio.Filters
    - eyestudio.Metrics
    - eyestudio.PyQt

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/eyestudio/
  license: UNKNOWN
  summary: 'Tool for evaluating and analysing eye movement classification algorithms.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
