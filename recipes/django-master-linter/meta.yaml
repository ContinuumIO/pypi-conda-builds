package:
  name: django-master-linter
  version: "0.0.7"

source:
  fn: django-master-linter-0.0.7.tar.gz
  url: https://pypi.python.org/packages/source/d/django-master-linter/django-master-linter-0.0.7.tar.gz
  md5: c7e3a760e3948f1a950005999edebc84
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-master-linter = django-master-linter:main
    #
    # Would create an entry point called django-master-linter that calls django-master-linter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-master

  run:
    - python
    - django-master

test:
  # Python imports
  imports:
    - djangomaster_linter
    - djangomaster_linter.master

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/django-master-linter/
  license: LICENSE
  summary: 'django master linter'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
