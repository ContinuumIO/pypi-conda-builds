package:
  name: flasfka
  version: "1.1.6"

source:
  fn: flasfka-1.1.6.tar.gz
  url: https://pypi.python.org/packages/source/f/flasfka/flasfka-1.1.6.tar.gz
  md5: 3d03c0e8ec25ac1475b5cf5f0de09ac1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flasfka = flasfka:main
    #
    # Would create an entry point called flasfka that calls flasfka.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.10
    - kafka-python >=0.9.2

  run:
    - python
    - flask >=0.10
    - kafka-python >=0.9.2

test:
  # Python imports
  imports:
    - flasfka

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/travel-intelligence/flasfka
  license: MIT License
  summary: '\nPush/Pull on Kafka over HTTP\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
