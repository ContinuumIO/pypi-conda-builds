package:
  name: django-facebook-posts
  version: "0.5.0"

source:
  fn: django-facebook-posts-0.5.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-facebook-posts/django-facebook-posts-0.5.0.tar.gz
  md5: db15c9454776c649bb617211598d33c9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-facebook-posts = django-facebook-posts:main
    #
    # Would create an entry point called django-facebook-posts that calls django-facebook-posts.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-facebook-api >=0.5.0
    - django-facebook-applications >=0.1.0
    - django-facebook-users >=0.3.0
    - django-facebook-pages >=0.3.0
    - django-m2m-history >=0.1.2

  run:
    - python
    - django-facebook-api >=0.5.0
    - django-facebook-applications >=0.1.0
    - django-facebook-users >=0.3.0
    - django-facebook-pages >=0.3.0
    - django-m2m-history >=0.1.2

test:
  # Python imports
  imports:
    - facebook_posts
    - facebook_posts.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ramusus/django-facebook-posts
  license: BSD License
  summary: 'Django implementation for Facebook Graph API Posts'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
