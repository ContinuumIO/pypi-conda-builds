package:
  name: fontaine
  version: "1.3.0"

source:
  fn: fontaine-1.3.0.tar.gz
  url: https://pypi.python.org/packages/source/f/fontaine/fontaine-1.3.0.tar.gz
  md5: 46e6a93af70a49f4d060ec0309ea452c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fontaine = fontaine:main
    #
    # Would create an entry point called fontaine that calls fontaine.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - lxml
    - requests

  run:
    - python
    - lxml
    - requests

test:
  # Python imports
  imports:
    - fontaine
    - fontaine.charmaps
    - fontaine.charmaps.internals
    - fontaine.ext
    - fontaine.structures

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/davelab6/pyfontaine
  license: GNU GPLv3
  summary: 'Fontaine font tool'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
