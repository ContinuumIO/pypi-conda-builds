package:
  name: mpltex
  version: "0.4"

source:
  fn: mpltex-0.4.tar.gz
  url: https://pypi.python.org/packages/source/m/mpltex/mpltex-0.4.tar.gz
  md5: 859a26e86e4f2c902ba644a446c5b0dd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mpltex = mpltex:main
    #
    # Would create an entry point called mpltex that calls mpltex.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - matplotlib
    - brewer2mpl

  run:
    - python
    - matplotlib
    - brewer2mpl

test:
  # Python imports
  imports:
    - mpltex

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/liuyxpp/mpltex
  license: BSD License
  summary: 'mpltex is a python package for creating publication-quality plots using matplotlib.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
