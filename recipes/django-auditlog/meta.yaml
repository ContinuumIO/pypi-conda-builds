package:
  name: django-auditlog
  version: "0.2.1"

source:
  fn: django-auditlog-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-auditlog/django-auditlog-0.2.1.tar.gz
  md5: 11092c2a32811fe8883923bdf111ec4a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-auditlog = django-auditlog:main
    #
    # Would create an entry point called django-auditlog that calls django-auditlog.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - django >=1.5

  run:
    - python
    - django >=1.5

test:
  # Python imports
  imports:
    - auditlog

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jjkester/django-auditlog
  license: MIT
  summary: 'Audit log app for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
