package:
  name: django-detective
  version: "0.4.0"

source:
  fn: django-detective-0.4.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-detective/django-detective-0.4.0.tar.gz
  md5: c51a4633c6dd347172f56bb1858f0337
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-detective = django-detective:main
    #
    # Would create an entry point called django-detective that calls django-detective.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - detective

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/PragmaticMates/django-detective
  license: BSD License
  summary: 'Django app that tracks all user requests.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
