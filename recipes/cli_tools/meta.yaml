package:
  name: cli_tools
  version: "0.3.0"

source:
  fn: cli_tools-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/c/cli_tools/cli_tools-0.3.0.tar.gz
  md5: 2c90dc3e43293dd303a7a6cea43ef1fa
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cli_tools = cli_tools:main
    #
    # Would create an entry point called cli_tools that calls cli_tools.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - argparse
    - setuptools
    - six

  run:
    - python
    - argparse
    - setuptools
    - six

test:
  # Python imports
  imports:
    - cli_tools

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/klmitch/cli_tools
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'Command Line Interface Tools'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
