package:
  name: flake8-pep257
  version: "1.0.2"

source:
  fn: flake8-pep257-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/f/flake8-pep257/flake8-pep257-1.0.2.tar.gz
  md5: a22908454a8f41de20209e464e9e5f5a
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flake8-pep257 = flake8-pep257:main
    #
    # Would create an entry point called flake8-pep257 that calls flake8-pep257.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flake8
    - pep257

  run:
    - python
    - setuptools
    - flake8
    - pep257

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Robpol86/flake8-pep257
  license: MIT License
  summary: 'Flake8 plugin for the pep257 Python utility.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
