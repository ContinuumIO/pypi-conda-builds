package:
  name: aldryn-snake
  version: "0.0.2"

source:
  fn: aldryn-snake-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/a/aldryn-snake/aldryn-snake-0.0.2.tar.gz
  md5: 35f6081bc4ecd213926b7970f6520546
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - aldryn-snake = aldryn-snake:main
    #
    # Would create an entry point called aldryn-snake that calls aldryn-snake.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - aldryn_snake

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/aldryn/aldryn-snake
  license: BSD License
  summary: 'Adds header and tail scripts from addons'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
