package:
  name: coopr.environ
  version: "1.0.1"

source:
  fn: coopr.environ-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/c/coopr.environ/coopr.environ-1.0.1.tar.gz
  md5: 804968c286b359b1523c21b47641f630
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - coopr.environ = coopr.environ:main
    #
    # Would create an entry point called coopr.environ that calls coopr.environ.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - coopr
    - coopr.environ

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://software.sandia.gov/svn/public/coopr/coopr.core
  license: BSD License
  summary: 'A Coopr package that loads plugins from all Coopr packages.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
