package:
  name: rhodecode
  version: "2.2.6"

source:
  fn: RhodeCode-2.2.6.tar.gz
  url: https://pypi.python.org/packages/source/R/RhodeCode/RhodeCode-2.2.6.tar.gz
  md5: c7e474dbbc2032f79980150265b19867
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - rhodecode = rhodecode:main
    #
    # Would create an entry point called rhodecode that calls rhodecode.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - rhodecode

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://rhodecode.com
  license: UNKNOWN
  summary: 'RhodeCode is a fast and powerful management tool for Mercurial and GIT with a built in push/pull server, full text search and code-review.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
