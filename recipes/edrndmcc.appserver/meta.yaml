package:
  name: edrndmcc.appserver
  version: "1.2.1"

source:
  fn: edrndmcc.appserver-1.2.1.zip
  url: https://pypi.python.org/packages/source/e/edrndmcc.appserver/edrndmcc.appserver-1.2.1.zip
  md5: aa6d493113bd8faf57879c64b062586d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - edrndmcc.appserver = edrndmcc.appserver:main
    #
    # Would create an entry point called edrndmcc.appserver that calls edrndmcc.appserver.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - edrn.rdf
    - edrn.theme
    - plone
    - plone.app.ldap
    - setuptools

  run:
    - python
    - setuptools
    - edrn.rdf
    - edrn.theme
    - plone
    - plone.app.ldap
    - setuptools

test:
  # Python imports
  imports:
    - edrndmcc
    - edrndmcc.appserver
    - edrndmcc.appserver.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/EDRN/edrndmcc.appserver
  license: Apache Software License
  summary: 'EDRN DMCC Application Server policy'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
