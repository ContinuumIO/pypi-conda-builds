package:
  name: coopr.doc
  version: "1.3.2"

source:
  fn: coopr.doc-1.3.2.tar.gz
  url: https://pypi.python.org/packages/source/c/coopr.doc/coopr.doc-1.3.2.tar.gz
  md5: 9a88b4e535d885b5fe6945cb841f3990
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - coopr.doc = coopr.doc:main
    #
    # Would create an entry point called coopr.doc that calls coopr.doc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - coopr

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://software.sandia.gov/svn/public/coopr/coopr.doc
  license: BSD License
  summary: "Coopr's online books and documentation"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
