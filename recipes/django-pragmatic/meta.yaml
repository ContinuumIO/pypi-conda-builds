package:
  name: django-pragmatic
  version: "1.5.0"

source:
  fn: django-pragmatic-1.5.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-pragmatic/django-pragmatic-1.5.0.tar.gz
  md5: 180919d64f870c16e617ed54edd43efd
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-pragmatic = django-pragmatic:main
    #
    # Would create an entry point called django-pragmatic that calls django-pragmatic.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - south
    - django-filter
    - django
    - python-pragmatic

  run:
    - python
    - south
    - django-filter
    - django
    - python-pragmatic

test:
  # Python imports
  imports:
    - pragmatic
    - pragmatic.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/PragmaticMates/django-pragmatic
  license: BSD License
  summary: 'Pragmatic tools and utilities for Django projects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
