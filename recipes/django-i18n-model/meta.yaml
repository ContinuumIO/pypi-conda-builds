package:
  name: django-i18n-model
  version: "0.0.9"

source:
  fn: django-i18n-model-0.0.9.tar.gz
  url: https://pypi.python.org/packages/source/d/django-i18n-model/django-i18n-model-0.0.9.tar.gz
  md5: 67829dd1dbccca8a56fc7abff80f9107
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-i18n-model = django-i18n-model:main
    #
    # Would create an entry point called django-i18n-model that calls django-i18n-model.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - i18n_model
    - i18n_model.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/Lacrymology/django-i18n-model
  license: BSD License
  summary: 'Translations for Django models'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
