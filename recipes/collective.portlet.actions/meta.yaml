package:
  name: collective.portlet.actions
  version: "1.2.2"

source:
  fn: collective.portlet.actions-1.2.2.zip
  url: https://pypi.python.org/packages/source/c/collective.portlet.actions/collective.portlet.actions-1.2.2.zip
  md5: 699b78ce8b2944a1407d2a81c35f91cd
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.portlet.actions = collective.portlet.actions:main
    #
    # Would create an entry point called collective.portlet.actions that calls collective.portlet.actions.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - acquisition
    - products.cmfcore
    - zope2
    - plone.app.portlets
    - plone.memoize
    - plone.portlets
    - zope.formlib
    - zope.schema

  run:
    - python
    - setuptools
    - setuptools
    - acquisition
    - products.cmfcore
    - zope2
    - plone.app.portlets
    - plone.memoize
    - plone.portlets
    - zope.formlib
    - zope.schema

test:
  # Python imports
  imports:
    - collective
    - collective.portlet
    - collective.portlet.actions
    - collective.portlet.actions.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/collective-portlet-actions
  license: GPL
  summary: 'A portlet that provides the links of an action category'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
