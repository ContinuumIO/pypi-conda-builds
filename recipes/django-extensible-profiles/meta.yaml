package:
  name: django-extensible-profiles
  version: "1.3.6"

source:
  fn: django-extensible-profiles-1.3.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-extensible-profiles/django-extensible-profiles-1.3.6.tar.gz
  md5: 9a0c89a993776652c0d0cdb5c642ad60
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-extensible-profiles = django-extensible-profiles:main
    #
    # Would create an entry point called django-extensible-profiles that calls django-extensible-profiles.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - profiles
    - profiles.extensions
    - profiles.management
    - profiles.management.commands
    - profiles.modules
    - profiles.modules.addresses
    - profiles.modules.addresses.extensions
    - profiles.modules.codes
    - profiles.modules.codes.extensions
    - profiles.modules.notifications
    - profiles.modules.notifications.extensions
    - profiles.modules.options
    - profiles.modules.options.extensions
    - profiles.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://incuna.com/
  license: UNKNOWN
  summary: 'Generic extensible django user profiles.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
