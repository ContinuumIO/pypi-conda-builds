package:
  name: django-uploadstatic
  version: "0.3.0"

source:
  fn: django-uploadstatic-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-uploadstatic/django-uploadstatic-0.3.0.tar.gz
  md5: 7820a01c08973492b0deaec4888ba36a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-uploadstatic = django-uploadstatic:main
    #
    # Would create an entry point called django-uploadstatic that calls django-uploadstatic.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - uploadstatic
    - uploadstatic.management
    - uploadstatic.management.commands
    - uploadstatic.tests
    - uploadstatic.tests.apps
    - uploadstatic.tests.apps.test
    - uploadstatic.tests.urls

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/streeter/django-uploadstatic
  license: BSD License
  summary: 'A Django app that provides helpers for uploading static files.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
