package:
  name: distdrone
  version: "1.28"

source:
  fn: distdrone-1.28.tar.gz
  url: https://pypi.python.org/packages/source/d/distdrone/distdrone-1.28.tar.gz
  md5: bbb89eed5d652ea3475ce39179c3db0d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - distdrone = distdrone:main
    #
    # Would create an entry point called distdrone that calls distdrone.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - distdrone
    - distdrone.imgproc
    - distdrone.motion

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/isaacrob/distdrone
  license: MIT
  summary: 'package to drive parallel drone swarm'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
