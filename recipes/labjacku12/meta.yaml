package:
  name: labjacku12
  version: "0.4"

source:
  fn: labjacku12-0.4.tar.gz
  url: https://pypi.python.org/packages/source/l/labjacku12/labjacku12-0.4.tar.gz
  md5: 495a44829c4a4d5d315cd79d193430a9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - labjacku12 = labjacku12:main
    #
    # Would create an entry point called labjacku12 that calls labjacku12.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - labjack
    - labjacku12
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.phys.ethz.ch/~robertjo/labjacku12
  license: BSD
  summary: 'LabJack U12 driver'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
