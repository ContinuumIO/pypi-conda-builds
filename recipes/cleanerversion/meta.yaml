package:
  name: cleanerversion
  version: "1.4.7"

source:
  fn: CleanerVersion-1.4.7.tar.gz
  url: https://pypi.python.org/packages/source/C/CleanerVersion/CleanerVersion-1.4.7.tar.gz
  md5: a3d2bbc3aa9f5f0919402f80302a5e67
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cleanerversion = cleanerversion:main
    #
    # Would create an entry point called cleanerversion that calls cleanerversion.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - cleanerversion
    - versions
    - versions.util
    - versions_tests
    - versions_tests.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/swisscom/cleanerversion
  license: Apache License 2.0
  summary: 'A versioning solution for relational data models using the Django ORM'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
