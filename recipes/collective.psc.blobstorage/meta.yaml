package:
  name: collective.psc.blobstorage
  version: "0.1.1"

source:
  fn: collective.psc.blobstorage-0.1.1.zip
  url: https://pypi.python.org/packages/source/c/collective.psc.blobstorage/collective.psc.blobstorage-0.1.1.zip
  md5: 252f475f34099dfb20e0dd6f5acab514
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.psc.blobstorage = collective.psc.blobstorage:main
    #
    # Would create an entry point called collective.psc.blobstorage that calls collective.psc.blobstorage.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.blob

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.blob

test:
  # Python imports
  imports:
    - collective
    - collective.psc
    - collective.psc.blobstorage
    - collective.psc.blobstorage.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/collective.psc.blobstorage
  license: GPL
  summary: 'Blob storage for PloneSoftwareCenter'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
