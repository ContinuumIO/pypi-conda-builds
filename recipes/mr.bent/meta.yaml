package:
  name: mr.bent
  version: "1.0a1"

source:
  fn: mr.bent-1.0a1.zip
  url: https://pypi.python.org/packages/source/m/mr.bent/mr.bent-1.0a1.zip
  md5: 0422d8a28383df9c97ec11c6216aac3b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mr.bent = mr.bent:main
    #
    # Would create an entry point called mr.bent that calls mr.bent.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - mr
    - mr.bent
    - mr.bent.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://dev.plone.org/collective/browser/mr.bent
  license: BSD License
  summary: 'Mr. Bent knows his numbers.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
