package:
  name: zope.app.debugskin
  version: "3.4.1"

source:
  fn: zope.app.debugskin-3.4.1.tar.gz
  url: https://pypi.python.org/packages/source/z/zope.app.debugskin/zope.app.debugskin-3.4.1.tar.gz
  md5: bd95d2848aa3108e53717d13b3c0924d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zope.app.debugskin = zope.app.debugskin:main
    #
    # Would create an entry point called zope.app.debugskin that calls zope.app.debugskin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.app.rotterdam
    - zope.app.skins
    - zope.publisher

  run:
    - python
    - setuptools
    - zope.app.rotterdam
    - zope.app.skins
    - zope.publisher

test:
  # Python imports
  imports:
    - zope
    - zope.app
    - zope.app.debugskin

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://cheeseshop.python.org/pypi/zope.app.debugskin
  license: Zope Public License
  summary: 'Debug -- A Zope 3 ZMI Skin'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
