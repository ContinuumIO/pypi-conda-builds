package:
  name: django-hresume
  version: "0.1"

source:
  fn: django-hresume-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-hresume/django-hresume-0.1.tar.gz
  md5: fc7f8cd9117a4a0b89fd005529232afd
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-hresume = django-hresume:main
    #
    # Would create an entry point called django-hresume that calls django-hresume.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python
    - setuptools

test:
  # Python imports
  imports:
    - hresume

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/wscoble/django-hresume
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'A resume application for django, presented using the hresume microformat'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
