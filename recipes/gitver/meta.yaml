package:
  name: gitver
  version: "0.3.1"

source:
  fn: gitver-0.3.1.tar.gz
  url: https://pypi.python.org/packages/source/g/gitver/gitver-0.3.1.tar.gz
  md5: 4dd21a2dfe3d429f0bb2b9a4be428071
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gitver = gitver:main
    #
    # Would create an entry point called gitver that calls gitver.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - ansicolors
    - sh

  run:
    - python
    - ansicolors
    - sh

test:
  # Python imports
  imports:
    - gitver

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/manuelbua/gitver
  license: Apache Software License
  summary: 'Simple version string management for git'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
