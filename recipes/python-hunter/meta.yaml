package:
  name: python-hunter
  version: "0.0"

source:
  fn: python-hunter-0.0.tar.gz
  url: https://pypi.python.org/packages/source/p/python-hunter/python-hunter-0.0.tar.gz
  md5: bd4d0d388bfbc040f77fee34b2bfa7fb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-hunter = python-hunter:main
    #
    # Would create an entry point called python-hunter that calls python-hunter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - hunter

  run:
    - python
    - hunter

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://pypi.python.org/pypi/hunter/
  license: UNKNOWN
  summary: 'Hunter is a flexible code tracing toolkit.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
