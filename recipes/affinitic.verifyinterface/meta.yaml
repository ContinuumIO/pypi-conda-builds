package:
  name: affinitic.verifyinterface
  version: "0.1"

source:
  fn: affinitic.verifyinterface-0.1.tar.gz
  url: https://pypi.python.org/packages/source/a/affinitic.verifyinterface/affinitic.verifyinterface-0.1.tar.gz
  md5: 484e82a7e2774ad0cbdf146845311d08
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - affinitic.verifyinterface = affinitic.verifyinterface:main
    #
    # Would create an entry point called affinitic.verifyinterface that calls affinitic.verifyinterface.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zope.interface

  run:
    - python
    - setuptools
    - setuptools
    - zope.interface

test:
  # Python imports
  imports:
    - affinitic
    - affinitic.verifyinterface
    - affinitic.verifyinterface.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://hg.affinitic.be/affinitic.verifyinterface
  license: GPL
  summary: 'Verify interface contract for all implements/classImplements declaration'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
