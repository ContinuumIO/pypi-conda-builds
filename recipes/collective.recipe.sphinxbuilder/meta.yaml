package:
  name: collective.recipe.sphinxbuilder
  version: "0.8.2"

source:
  fn: collective.recipe.sphinxbuilder-0.8.2.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.recipe.sphinxbuilder/collective.recipe.sphinxbuilder-0.8.2.tar.gz
  md5: ff464f98eb5a375d51ca885deb8db9dd
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.recipe.sphinxbuilder = collective.recipe.sphinxbuilder:main
    #
    # Would create an entry point called collective.recipe.sphinxbuilder that calls collective.recipe.sphinxbuilder.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - zc.recipe.egg
    - docutils
    - sphinx >=1.1

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - zc.recipe.egg
    - docutils
    - sphinx >=1.1

test:
  # Python imports
  imports:
    - collective
    - collective.recipe
    - collective.recipe.sphinxbuilder
    - collective.recipe.sphinxbuilder.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/sdouche/collective.recipe.sphinxbuilder
  license: Zope Public License
  summary: 'ZC.buildout recipe to generate and build Sphinx-based documentation in the buildout.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
