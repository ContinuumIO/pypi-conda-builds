package:
  name: django-tinymcewrapper
  version: "0.4.2"

source:
  fn: django-tinymcewrapper-0.4.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-tinymcewrapper/django-tinymcewrapper-0.4.2.tar.gz
  md5: 2d3e854d81a3a8e915d44acdf90fc9c9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-tinymcewrapper = django-tinymcewrapper:main
    #
    # Would create an entry point called django-tinymcewrapper that calls django-tinymcewrapper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-tinymce

  run:
    - python
    - django-tinymce

test:
  # Python imports
  imports:
    - tinymcewrapper

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://callowayproject.com/
  license: Apache Software License
  summary: 'django-tinymcewrapper allows you to easily add tinymce widgets to other apps admin forms without modification of the other app.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
