package:
  name: motmot.fastimage_ipp
  version: "0.5.5.ipp"

source:
  fn: motmot.FastImage_ipp-0.5.5.ipp.tar.gz
  url: https://pypi.python.org/packages/source/m/motmot.FastImage_ipp/motmot.FastImage_ipp-0.5.5.ipp.tar.gz
  md5: 112fc1e2c9ea7d23bb1486fd0d9ce504
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - motmot.fastimage_ipp = motmot.fastimage_ipp:main
    #
    # Would create an entry point called motmot.fastimage_ipp that calls motmot.fastimage_ipp.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - motmot
    - motmot.FastImage

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.astraw.com/projects/motmot
  license: BSD
  summary: 'Pythonic API for a subset of the Intel Integrated Performance Primitives (Intel IPP) library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
