package:
  name: collective.portlet.map
  version: "1.0dev-r75856"

source:
  fn: collective.portlet.map-1.0dev-r75856.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.portlet.map/collective.portlet.map-1.0dev-r75856.tar.gz
  md5: cfe87682c5afeba7a91286293e4bf78d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.portlet.map = collective.portlet.map:main
    #
    # Would create an entry point called collective.portlet.map that calls collective.portlet.map.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.maps

  run:
    - python
    - setuptools
    - setuptools
    - products.maps

test:
  # Python imports
  imports:
    - collective
    - collective.portlet
    - collective.portlet.map
    - collective.portlet.map.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.reflab.it
  license: GPL
  summary: 'Google Map portlet'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
