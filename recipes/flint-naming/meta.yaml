package:
  name: flint-naming
  version: "0.2"

source:
  fn: flint-naming-0.2.tar.gz
  url: https://pypi.python.org/packages/source/f/flint-naming/flint-naming-0.2.tar.gz
  md5: ba0f4218ccea14719ee6493e8a436aa4
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flint-naming = flint-naming:main
    #
    # Would create an entry point called flint-naming that calls flint-naming.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pep8-naming

  run:
    - python
    - setuptools
    - pep8-naming

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/flintwork/pep8-naming
  license: MIT License
  summary: 'Check PEP-8 naming conventions, plugin for flint'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
