package:
  name: geocluster
  version: "1.0.1"

source:
  fn: geocluster-1.0.1.zip
  url: https://pypi.python.org/packages/source/g/geocluster/geocluster-1.0.1.zip
  md5: acd94b84f2ea3468e5ee593e4760fc00
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - geocluster = geocluster:main
    #
    # Would create an entry point called geocluster that calls geocluster.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - geocluster

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.regisblog.fr/geocluster/
  license: MIT License
  summary: 'GeoCluster is a framework agnostic library to help map clusterization'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
