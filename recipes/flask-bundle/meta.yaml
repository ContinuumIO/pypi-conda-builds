package:
  name: flask-bundle
  version: "0.4"

source:
  fn: Flask-Bundle-0.4.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Bundle/Flask-Bundle-0.4.tar.gz
  md5: 79048e93dcbbe57d613805eeec22ce9f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-bundle = flask-bundle:main
    #
    # Would create an entry point called flask-bundle that calls flask-bundle.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask

  run:
    - python
    - flask

test:
  # Python imports
  imports:
    - flask_bundle

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Deepwalker/Flask-Bundle/
  license: BSD License
  summary: 'Class based tool that behaves like blueprints'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
