package:
  name: cisco_pass
  version: "0.2"

source:
  fn: cisco_pass-0.2.tar.gz
  url: https://pypi.python.org/packages/source/c/cisco_pass/cisco_pass-0.2.tar.gz
  md5: 4dc8f1a6533dc275183da155b5805029
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cisco_pass = cisco_pass:main
    #
    # Would create an entry point called cisco_pass that calls cisco_pass.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - passlib

  run:
    - python
    - passlib

test:
  # Python imports
  imports:
    - cisco_pass

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/rcaldwel/cisco_pass
  license: UNKNOWN
  summary: 'Generate Cisco password strings.  Decrypt type7.  Devices: IOS, NXOS, ACE, CatOS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
