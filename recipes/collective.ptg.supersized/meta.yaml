package:
  name: collective.ptg.supersized
  version: "1.2"

source:
  fn: collective.ptg.supersized-1.2.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.ptg.supersized/collective.ptg.supersized-1.2.tar.gz
  md5: fc4c3d8c541252e4a897672c230ccb69
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.ptg.supersized = collective.ptg.supersized:main
    #
    # Would create an entry point called collective.ptg.supersized that calls collective.ptg.supersized.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.plonetruegallery

  run:
    - python
    - setuptools
    - setuptools
    - collective.plonetruegallery

test:
  # Python imports
  imports:
    - collective
    - collective.ptg
    - collective.ptg.supersized

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.ptg.supersized
  license: GPL
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
