package:
  name: django-unchained
  version: "1.0.1"

source:
  fn: django-unchained-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-unchained/django-unchained-1.0.1.tar.gz
  md5: 80011deea994c9d78b54adb748c95033
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-unchained = django-unchained:main
    #
    # Would create an entry point called django-unchained that calls django-unchained.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://youtube.com/v/rC8VJ9aeB_g?hd=1&autoplay=1
  license: UNKNOWN
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
