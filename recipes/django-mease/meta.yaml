package:
  name: django-mease
  version: "0.2.0"

source:
  fn: django-mease-0.2.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-mease/django-mease-0.2.0.tar.gz
  md5: 097d151f7e6ccec891e5e7ec00211440
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-mease = django-mease:main
    #
    # Would create an entry point called django-mease that calls django-mease.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - mease ==0.2.0

  run:
    - python
    - mease ==0.2.0

test:
  # Python imports
  imports:
    - djmease
    - djmease.management
    - djmease.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/florianpaquet/django-mease
  license: MIT License
  summary: 'django-mease: Websocket integration made easy for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
