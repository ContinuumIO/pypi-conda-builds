package:
  name: ipdbplugin
  version: "1.4.2"

source:
  fn: ipdbplugin-1.4.2.tar.gz
  url: https://pypi.python.org/packages/source/i/ipdbplugin/ipdbplugin-1.4.2.tar.gz
  md5: 9cdf6ada252cc787337c3ebf73f677e2
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ipdbplugin = ipdbplugin:main
    #
    # Would create an entry point called ipdbplugin that calls ipdbplugin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - nose
    - ipython

  run:
    - python
    - setuptools
    - nose
    - ipython

test:
  # Python imports
  imports:
    - ipdbplugin

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/flavioamieiro/nose-ipdb/tree/master
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Nose plugin to use iPdb instead of Pdb when tests fail'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
