package:
  name: django-sermon
  version: "1.0.3"

source:
  fn: django-sermon-1.0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-sermon/django-sermon-1.0.3.tar.gz
  md5: 785ca2f83775e8db45ebd43187179f0c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-sermon = django-sermon:main
    #
    # Would create an entry point called django-sermon that calls django-sermon.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-markup-mixin
    - django-extensions

  run:
    - python
    - django-markup-mixin
    - django-extensions

test:
  # Python imports
  imports:
    - sermon

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/powellc/django-sermon
  license: BSD License
  summary: 'A simple reusable application for managing sermons in a Django application.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
