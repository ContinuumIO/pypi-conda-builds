package:
  name: dalmatian
  version: "0.1.4"

source:
  fn: dalmatian-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/d/dalmatian/dalmatian-0.1.4.tar.gz
  md5: 2f87917e7954276f496c6533310ceedf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dalmatian = dalmatian:main
    #
    # Would create an entry point called dalmatian that calls dalmatian.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - numpy
    - reportlab

  run:
    - python
    - numpy
    - reportlab

test:
  # Python imports
  imports:
    - dalmatian

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/shoya140/dalmatian
  license: MIT License
  summary: 'Visualizing a confusion matrix with gradations'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
