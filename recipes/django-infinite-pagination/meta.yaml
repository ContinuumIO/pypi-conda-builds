package:
  name: django-infinite-pagination
  version: "1.0"

source:
  fn: django-infinite-pagination-1.0.zip
  url: https://pypi.python.org/packages/source/d/django-infinite-pagination/django-infinite-pagination-1.0.zip
  md5: d5dce5d3a770aeaabad1bd820feb1b1a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-infinite-pagination = django-infinite-pagination:main
    #
    # Would create an entry point called django-infinite-pagination that calls django-infinite-pagination.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - infinite_pagination
    - infinite_pagination.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/nigma/django-infinite-pagination
  license: BSD License
  summary: 'Infinite pagination for Django.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
