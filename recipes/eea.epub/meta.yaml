package:
  name: eea.epub
  version: "6.2"

source:
  fn: eea.epub-6.2.zip
  url: https://pypi.python.org/packages/source/e/eea.epub/eea.epub-6.2.zip
  md5: 28c62aa7fc9342235fa96c206bd7a512
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - eea.epub = eea.epub:main
    #
    # Would create an entry point called eea.epub that calls eea.epub.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - beautifulsoup4
    - lxml
    - requests
    - plone.app.async
    - eea.converter >8.9
    - eea.downloads >1.0

  run:
    - python
    - setuptools
    - setuptools
    - beautifulsoup4
    - lxml
    - requests
    - plone.app.async
    - eea.converter >8.9
    - eea.downloads >1.0

test:
  # Python imports
  imports:
    - eea
    - eea.epub
    - eea.epub.browser
    - eea.epub.browser.app
    - eea.epub.cache
    - eea.epub.events
    - eea.epub.export
    - eea.epub.rules
    - eea.epub.subtypes
    - eea.epub.tests
    - eea.epub.upgrades

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://svn.eionet.europa.eu/projects/Zope
  license: GPL
  summary: 'Publish Plone content in epub form'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
