package:
  name: django-beta
  version: "0.1.0"

source:
  fn: django-beta-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-beta/django-beta-0.1.0.tar.gz
  md5: dbf1972e46e625d48a54789c1b9a98c5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-beta = django-beta:main
    #
    # Would create an entry point called django-beta that calls django-beta.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - beta
    - beta.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/revsys/django-beta
  license: BSD License
  summary: 'django-beta is a reusable Django application for handling pre-beta signups.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
