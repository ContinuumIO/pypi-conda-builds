package:
  name: obhm
  version: "0.0.1"

source:
  fn: obhm-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/o/obhm/obhm-0.0.1.tar.gz
  md5: 5a4b51dd6118461ddde188079ab0531a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - obhm = obhm:main
    #
    # Would create an entry point called obhm that calls obhm.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python-gflags

  run:
    - python
    - python-gflags

test:
  # Python imports
  imports:
    - obhm

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ob/obhm
  license: UNKNOWN
  summary: 'One Banana Host Monitoring for people with other things to think about'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
