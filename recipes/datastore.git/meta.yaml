package:
  name: datastore.git
  version: "0.1.1"

source:
  fn: datastore.git-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/datastore.git/datastore.git-0.1.1.tar.gz
  md5: 2daed55e795f4cc4a676110361aac5c6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - datastore.git = datastore.git:main
    #
    # Would create an entry point called datastore.git that calls datastore.git.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - datastore >=0.3.3
    - dulwich >=0.8.7

  run:
    - python
    - datastore >=0.3.3
    - dulwich >=0.8.7

test:
  # Python imports
  imports:
    - datastore
    - datastore.git

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/datastore/datastore.git
  license: MIT License
  summary: 'git datastore implementation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
