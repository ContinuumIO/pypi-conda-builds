package:
  name: kr-gevent-websocket
  version: "0.4"

source:
  fn: kr-gevent-websocket-0.4.tar.gz
  url: https://pypi.python.org/packages/source/k/kr-gevent-websocket/kr-gevent-websocket-0.4.tar.gz
  md5: 99ed7777563e5bcbbac3d72d2b0ae861
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kr-gevent-websocket = kr-gevent-websocket:main
    #
    # Would create an entry point called kr-gevent-websocket that calls kr-gevent-websocket.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - gevent
    - greenlet

  run:
    - python
    - gevent
    - greenlet

test:
  # Python imports
  imports:
    - geventwebsocket
    - geventwebsocket.gunicorn
    - geventwebsocket.protocols

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/Jeffrey/gevent-websocket
  license: Apache Software License
  summary: 'Websocket handler for the gevent pywsgi server, a Python network library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
