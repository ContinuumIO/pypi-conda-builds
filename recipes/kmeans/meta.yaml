package:
  name: kmeans
  version: "1.0.2"

source:
  fn: kmeans-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/k/kmeans/kmeans-1.0.2.tar.gz
  md5: 7b042b9ea3fbe111afe920b70ee15b36
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - kmeans = kmeans:main
    #
    # Would create an entry point called kmeans that calls kmeans.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - kmeans

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/numberoverzero/kmeans/
  license: MIT License
  summary: 'python wrapper for basic c implementation of kmeans'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
