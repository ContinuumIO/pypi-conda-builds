package:
  name: mipp
  version: "0.9.2"

source:
  fn: mipp-0.9.2.tar.gz
  url: https://pypi.python.org/packages/source/m/mipp/mipp-0.9.2.tar.gz
  md5: b03c45eb9dfccee051caec78a62f2da3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mipp = mipp:main
    #
    # Would create an entry point called mipp that calls mipp.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - mipp
    - mipp.xrit
    - mipp.xsar

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/loerum/mipp
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'Meteorological ingest processing package'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
