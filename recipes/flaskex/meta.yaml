package:
  name: flaskex
  version: "0.0.66"

source:
  fn: FlaskEx-0.0.66.tar.gz
  url: https://pypi.python.org/packages/source/F/FlaskEx/FlaskEx-0.0.66.tar.gz
  md5: d25dd3fb1a7010cf9a701ba764af9621
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flaskex = flaskex:main
    #
    # Would create an entry point called flaskex that calls flaskex.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - flask-wtf
    - flask-sqlalchemy
    - flask-assets
    - alembic
    - pyjade ==2.0.1b
    - pytz
    - iso8601
    - flask-debugtoolbar
    - facebook-sdk
    - glob2
    - simplejson
    - lepl
    - flask-script <0.6

  run:
    - python
    - flask
    - flask-wtf
    - flask-sqlalchemy
    - flask-assets
    - alembic
    - pyjade ==2.0.1b
    - pytz
    - iso8601
    - flask-debugtoolbar
    - facebook-sdk
    - glob2
    - simplejson
    - lepl
    - flask-script <0.6

test:
  # Python imports
  imports:
    - flaskex
    - flaskex.assets
    - flaskex.ex
    - flaskex.ext
    - flaskex.forms
    - flaskex.sns

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
