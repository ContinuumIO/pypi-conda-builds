package:
  name: django-debug-toolbar
  version: "1.3.0"

source:
  fn: django-debug-toolbar-1.3.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-debug-toolbar/django-debug-toolbar-1.3.0.tar.gz
  md5: a43c3232545e596fc5947b6454109874
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-debug-toolbar = django-debug-toolbar:main
    #
    # Would create an entry point called django-debug-toolbar that calls django-debug-toolbar.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4.2
    - sqlparse

  run:
    - python
    - django >=1.4.2
    - sqlparse

test:
  # Python imports
  imports:
    - debug_toolbar
    - debug_toolbar.management
    - debug_toolbar.management.commands
    - debug_toolbar.panels
    - debug_toolbar.panels.sql
    - debug_toolbar.panels.templates

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

# requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/django-debug-toolbar/django-debug-toolbar
  license: BSD License
  summary: 'A configurable set of panels that display various debug information about the current request/response.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
