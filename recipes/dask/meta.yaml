package:
  name: dask
  version: "0.4.0"

source:
  fn: dask-0.4.0.tar.gz
  url: https://pypi.python.org/packages/source/d/dask/dask-0.4.0.tar.gz
  md5: 40569863d28316305d8abc1f8d97af1b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dask = dask:main
    #
    # Would create an entry point called dask that calls dask.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - toolz
    - psutil
    - chest

  run:
    - python
    - toolz
    - psutil
    - chest

test:
  # Python imports
  imports:
    - dask
    - dask.array
    - dask.bag
    - dask.dataframe
    - dask.store
    - pbag
    - pframe

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/ContinuumIO/dask/
  license: BSD
  summary: 'Minimal task scheduling abstraction'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
