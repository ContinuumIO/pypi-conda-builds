package:
  name: django-klingon
  version: "0.0.4"

source:
  fn: django-klingon-0.0.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-klingon/django-klingon-0.0.4.tar.gz
  md5: 00284e3c63917fbfecf3ed4229fd4a31
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-klingon = django-klingon:main
    #
    # Would create an entry point called django-klingon that calls django-klingon.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - klingon
    - klingon.management
    - klingon.management.commands
    - klingon.tests
    - klingon.tests.testapp

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/RouteAtlas/django-klingon
  license: GPL
  summary: 'django-klingon is an attempt to make django model translation\n    suck but with no integrations pain in your app!'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
