package:
  name: django-easyfilters
  version: "0.5"

source:
  fn: django-easyfilters-0.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-easyfilters/django-easyfilters-0.5.tar.gz
  md5: 8355922d493e662841ff9b2b255c2517
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-easyfilters = django-easyfilters:main
    #
    # Would create an entry point called django-easyfilters that calls django-easyfilters.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3
    - python-dateutil
    - six

  run:
    - python
    - django >=1.3
    - python-dateutil
    - six

test:
  # Python imports
  imports:
    - django_easyfilters
    - django_easyfilters.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/spookylukey/django-easyfilters/
  license: MIT License
  summary: 'Easy creation of link-based filtering for a list of Django model objects.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
