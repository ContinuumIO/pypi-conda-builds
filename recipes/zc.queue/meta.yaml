package:
  name: zc.queue
  version: "2.0.0a1"

source:
  fn: zc.queue-2.0.0a1.tar.gz
  url: https://pypi.python.org/packages/source/z/zc.queue/zc.queue-2.0.0a1.tar.gz
  md5: e17379d412be5fc44f9fe55c99d77537
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zc.queue = zc.queue:main
    #
    # Would create an entry point called zc.queue that calls zc.queue.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zodb
    - persistent
    - zope.interface

  run:
    - python
    - setuptools
    - zodb
    - persistent
    - zope.interface

test:
  # Python imports
  imports:
    - zc
    - zc.queue

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/zc.queue
  license: Zope Public License
  summary: 'Queues that are optimized for persistency via the ZODB.\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
