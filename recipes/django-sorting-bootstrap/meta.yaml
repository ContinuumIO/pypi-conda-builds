package:
  name: django-sorting-bootstrap
  version: "1.1"

source:
  fn: django-sorting-bootstrap-1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-sorting-bootstrap/django-sorting-bootstrap-1.1.tar.gz
  md5: 708cdc26ac3ba3aad4d5ab9f49f5de25
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-sorting-bootstrap = django-sorting-bootstrap:main
    #
    # Would create an entry point called django-sorting-bootstrap that calls django-sorting-bootstrap.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4.0

  run:
    - python
    - django >=1.4.0

test:
  # Python imports
  imports:
    - sorting_bootstrap
    - sorting_bootstrap.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/staticdev/django-sorting-bootstrap
  license: MIT License
  summary: 'Sorting templates API using sorting-bootstrap templatetags and Bootstrap classes.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
