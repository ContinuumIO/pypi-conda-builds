package:
  name: alchemist-armet
  version: "0.1.2"

source:
  fn: alchemist-armet-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/a/alchemist-armet/alchemist-armet-0.1.2.tar.gz
  md5: 2eb74171540d687cd5f450d49b5a773e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - alchemist-armet = alchemist-armet:main
    #
    # Would create an entry point called alchemist-armet that calls alchemist-armet.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - alchemist >=0.3.0
    - armet >=0.4.0

  run:
    - python
    - alchemist >=0.3.0
    - armet >=0.4.0

test:
  # Python imports
  imports:
    - alchemist_armet

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/concordusapps/alchemist-armet
  license: MIT License
  summary: 'Tight integration of armet with alchemist.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
