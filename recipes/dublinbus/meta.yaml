package:
  name: dublinbus
  version: "0.1a0"

source:
  fn: dublinbus-0.1a0.zip
  url: https://pypi.python.org/packages/source/d/dublinbus/dublinbus-0.1a0.zip
  md5: d38af5cc9c9c1ff0e098e09bb4119b00
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dublinbus = dublinbus:main
    #
    # Would create an entry point called dublinbus that calls dublinbus.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - dublinbus

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/vasyl-purchel/dublinbus
  license: MIT License
  summary: 'Dublin bus real time information from rtpi.ie site'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
