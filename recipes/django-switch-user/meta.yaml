package:
  name: django-switch-user
  version: "0.1.0"

source:
  fn: django-switch-user-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-switch-user/django-switch-user-0.1.0.tar.gz
  md5: 540f985f93b9ff3e1aed406f0ed21cda
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-switch-user = django-switch-user:main
    #
    # Would create an entry point called django-switch-user that calls django-switch-user.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - su

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://michaeltrier.com
  license: MIT
  summary: 'Django Switch User allows you to assume the identity of another user.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
