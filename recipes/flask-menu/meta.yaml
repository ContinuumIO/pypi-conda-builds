package:
  name: flask-menu
  version: "0.3.0"

source:
  fn: Flask-Menu-0.3.0.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Menu/Flask-Menu-0.3.0.tar.gz
  md5: 40188884fc6b6b86bec237f490ff2145
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-menu = flask-menu:main
    #
    # Would create an entry point called flask-menu that calls flask-menu.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - six

  run:
    - python
    - flask
    - six

test:
  # Python imports
  imports:
    - flask_menu

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/inveniosoftware/flask-menu
  license: BSD License
  summary: 'Flask-Menu is a Flask extension that adds support for generating menus.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
