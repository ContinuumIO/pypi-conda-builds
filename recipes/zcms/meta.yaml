package:
  name: zcms
  version: "0.6.4"

source:
  fn: zcms-0.6.4.tar.gz
  url: https://pypi.python.org/packages/source/z/zcms/zcms-0.6.4.tar.gz
  md5: 18fd3a8c20c398edfa831c54c31a52df
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zcms = zcms:main
    #
    # Would create an entry point called zcms that calls zcms.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - paste
    - pyramid <=1.4.4
    - pygments
    - docutils
    - chardet
    - webhelpers
    - markdown
    - pyyaml
    - rst2html5

  run:
    - python
    - setuptools
    - paste
    - pyramid <=1.4.4
    - pygments
    - docutils
    - chardet
    - webhelpers
    - markdown
    - pyyaml
    - rst2html5

test:
  # Python imports
  imports:
    - zcms
    - zcms.directives

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://zopen.cn
  license: BSD-derived (http://www.repoze.org/LICENSE.txt)
  summary: 'Serve filesystem content via repoze.bfg'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
