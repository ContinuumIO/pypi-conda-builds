package:
  name: products.textindexng3
  version: "3.4.9"

source:
  fn: Products.TextIndexNG3-3.4.9.zip
  url: https://pypi.python.org/packages/source/P/Products.TextIndexNG3/Products.TextIndexNG3-3.4.9.zip
  md5: bd28ba0d7de5939f2399147181b5cc76
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - products.textindexng3 = products.textindexng3:main
    #
    # Would create an entry point called products.textindexng3 that calls products.textindexng3.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope2 >=2.12.18
    - zopyx.txng3.core
    - zopyx.txng3.ext

  run:
    - python
    - setuptools
    - zope2 >=2.12.18
    - zopyx.txng3.core
    - zopyx.txng3.ext

test:
  # Python imports
  imports:
    - Products
    - Products.TextIndexNG3

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.zopyx.com/pypi/Products.TextIndexNG3
  license: ZPL (see LICENSE.txt)
  summary: 'Pluggable fulltext indexing solution for Zope 2 and Zope 3'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
