package:
  name: fluentcms-countdown
  version: "1.0.2"

source:
  fn: fluentcms-countdown-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/f/fluentcms-countdown/fluentcms-countdown-1.0.2.tar.gz
  md5: 2860a9e6df8c33b806e25daa48f5c789
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fluentcms-countdown = fluentcms-countdown:main
    #
    # Would create an entry point called fluentcms-countdown that calls fluentcms-countdown.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-fluent-contents >=v1.0c2

  run:
    - python
    - django-fluent-contents >=v1.0c2

test:
  # Python imports
  imports:
    - fluentcms_countdown
    - fluentcms_countdown.south_migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/edoburu/fluentcms-countdown
  license: Apache Software License
  summary: 'A countdown timer for django-fluent-contents'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
