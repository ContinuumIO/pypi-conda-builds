package:
  name: django-loginza
  version: "0.3.9"

source:
  fn: django-loginza-0.3.9.tar.gz
  url: https://pypi.python.org/packages/source/d/django-loginza/django-loginza-0.3.9.tar.gz
  md5: bf950ddaa4e5eab6a924634c814f2bd6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-loginza = django-loginza:main
    #
    # Would create an entry point called django-loginza that calls django-loginza.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - loginza
    - loginza.conf
    - loginza.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/vgarvardt/django-loginza
  license: BSD License
  summary: 'Django application for Loginza service'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
