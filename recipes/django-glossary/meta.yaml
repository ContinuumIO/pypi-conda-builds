package:
  name: django-glossary
  version: "0.1.6"

source:
  fn: django-glossary-0.1.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-glossary/django-glossary-0.1.6.tar.gz
  md5: ff1249c8b6aa5b1022c62951094baf40
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-glossary = django-glossary:main
    #
    # Would create an entry point called django-glossary that calls django-glossary.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - glossary
    - glossary.templatetags
    - test_project

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/kcunning/django-glossary
  license: MIT License
  summary: 'A simple glossary system for Django-powered sites'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
