package:
  name: pyes
  version: "0.99.5"

source:
  fn: pyes-0.99.5.tar.gz
  url: https://pypi.python.org/packages/source/p/pyes/pyes-0.99.5.tar.gz
  md5: 5299b4b81721938de47a7905d63ca803
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pyes = pyes:main
    #
    # Would create an entry point called pyes that calls pyes.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - urllib3 >=1.7
    - six >=1.5.2

  run:
    - python
    - urllib3 >=1.7
    - six >=1.5.2

test:
  # Python imports
  imports:
    - pyes
    - pyes.contrib
    - pyes.orm
    - pyes.pyesthrift
    - pyes.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/aparo/pyes/
  license: BSD License
  summary: 'Python Elastic Search driver'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
