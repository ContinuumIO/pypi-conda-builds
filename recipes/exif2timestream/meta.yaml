package:
  name: exif2timestream
  version: "0.2.12"

source:
  fn: exif2timestream-0.2.12.tar.gz
  url: https://pypi.python.org/packages/source/e/exif2timestream/exif2timestream-0.2.12.tar.gz
  md5: 1343a7b3aece93a6b86b9e67cbb625d8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - exif2timestream = exif2timestream:main
    #
    # Would create an entry point called exif2timestream that calls exif2timestream.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - exifread ==1.4.2
    - docopt ==0.6.1
    - voluptuous ==0.8.4

  run:
    - python
    - exifread ==1.4.2
    - docopt ==0.6.1
    - voluptuous ==0.8.4

test:
  # Python imports
  imports:
    - exif2timestream

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/borevitzlab/traitcapture-bin
  license: GNU General Public License v3 or later (GPLv3+)
  summary: '\nexif2timestream: Rename image files into a timestream from exif data.\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
