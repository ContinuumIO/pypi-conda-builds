package:
  name: medialog.tinymceplugins.imagestyles
  version: "0.2"

source:
  fn: medialog.tinymceplugins.imagestyles-0.2.tar.gz
  url: https://pypi.python.org/packages/source/m/medialog.tinymceplugins.imagestyles/medialog.tinymceplugins.imagestyles-0.2.tar.gz
  md5: 1da41ebcdee7bb67deb8aa837fba295f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - medialog.tinymceplugins.imagestyles = medialog.tinymceplugins.imagestyles:main
    #
    # Would create an entry point called medialog.tinymceplugins.imagestyles that calls medialog.tinymceplugins.imagestyles.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.tinymce

  run:
    - python
    - setuptools
    - setuptools
    - products.tinymce

test:
  # Python imports
  imports:
    - medialog
    - medialog.tinymceplugins
    - medialog.tinymceplugins.imagestyles

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/medialog.tinymceplugins.imagestyles/
  license: GPL
  summary: 'A TinyMCE plugin for having more styles (classes) for images'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
