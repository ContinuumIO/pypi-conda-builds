package:
  name: products.siteerrorlog
  version: "2.13.2"

source:
  fn: Products.SiteErrorLog-2.13.2.zip
  url: https://pypi.python.org/packages/source/P/Products.SiteErrorLog/Products.SiteErrorLog-2.13.2.zip
  md5: 369791776e73e2dea0bc440a8bd8630d
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - products.siteerrorlog = products.siteerrorlog:main
    #
    # Would create an entry point called products.siteerrorlog that calls products.siteerrorlog.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - acquisition

  run:
    - python
    - setuptools
    - acquisition

test:
  # Python imports
  imports:
    - Products
    - Products.SiteErrorLog
    - Products.SiteErrorLog.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/Products.SiteErrorLog
  license: ZPL 2.1
  summary: 'zope.sendmail integration for Zope 2.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
