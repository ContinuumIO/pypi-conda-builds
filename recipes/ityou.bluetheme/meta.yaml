package:
  name: ityou.bluetheme
  version: "0.1"

source:
  fn: ityou.bluetheme-0.1.tar.gz
  url: https://pypi.python.org/packages/source/i/ityou.bluetheme/ityou.bluetheme-0.1.tar.gz
  md5: 3dbfc725655619a7be6ad306451d70ea
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ityou.bluetheme = ityou.bluetheme:main
    #
    # Would create an entry point called ityou.bluetheme that calls ityou.bluetheme.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - ityou
    - ityou.bluetheme
    - ityou.bluetheme.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.ityou.de/
  license: GPL
  summary: 'An installable theme for Plone 4'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
