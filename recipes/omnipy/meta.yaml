package:
  name: omnipy
  version: "0.1.2"

source:
  fn: omnipy-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/o/omnipy/omnipy-0.1.2.tar.gz
  md5: 6925b36f89d621d8ea3cf958e471377e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - omnipy = omnipy:main
    #
    # Would create an entry point called omnipy that calls omnipy.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - treelib
    - tldextract

  run:
    - python
    - treelib
    - tldextract

test:
  # Python imports
  imports:
    - omnipy
    - omnipy.data
    - omnipy.data.reader
    - omnipy.model
    - omnipy.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/caesar0301/omnipy
  license: GPLv3
  summary: 'My misc python library kit in daily data processing.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
