package:
  name: gilt-python
  version: "0.7.1"

source:
  fn: gilt-python-0.7.1.tar.gz
  url: https://pypi.python.org/packages/source/g/gilt-python/gilt-python-0.7.1.tar.gz
  md5: 75fa23c31e65310bf5df1e4cb75001a2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gilt-python = gilt-python:main
    #
    # Would create an entry point called gilt-python that calls gilt-python.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - simplejson >=2.3.3
    - httplib2 >=0.7.2
    - iso8601 >=0.1.4

  run:
    - python
    - simplejson >=2.3.3
    - httplib2 >=0.7.2
    - iso8601 >=0.1.4

test:
  # Python imports
  imports:
    - gilt
    - gilt.rest

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/aellerton/gilt-python
  license: Apache Software License
  summary: 'Gilt API client'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
