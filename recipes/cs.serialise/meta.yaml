package:
  name: cs.serialise
  version: "20150116"

source:
  fn: cs.serialise-20150116.tar.gz
  url: https://pypi.python.org/packages/source/c/cs.serialise/cs.serialise-20150116.tar.gz
  md5: df8f81166ddec176847e021febe11f07
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cs.serialise = cs.serialise:main
    #
    # Would create an entry point called cs.serialise that calls cs.serialise.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/cameron_simpson/css/commits/all
  license: GNU General Public License v3 (GPLv3)
  summary: 'some serialisation functions'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
