package:
  name: fso
  version: "0.3"

source:
  fn: fso-0.3.tar.gz
  url: https://pypi.python.org/packages/source/f/fso/fso-0.3.tar.gz
  md5: f9006cdd7c57931403d6e98344206927
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - fso = fso:main
    #
    # Would create an entry point called fso that calls fso.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - distribute >=0.6.24
    - six >=1.4.1
    - aadict >=0.2.0
    - globre >=0.0.5
    - asset >=0.0.4
    - morph >=0.1.1

  run:
    - python
    - distribute >=0.6.24
    - six >=1.4.1
    - aadict >=0.2.0
    - globre >=0.0.5
    - asset >=0.0.4
    - morph >=0.1.1

test:
  # Python imports
  imports:
    - fso

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/metagriffin/fso
  license: GNU General Public License v3 or later (GPLv3+)
  summary: 'File System Overlay - filesystem layering for unit testing'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
