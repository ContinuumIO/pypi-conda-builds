package:
  name: example.archetype
  version: "1.0"

source:
  fn: example.archetype-1.0.tar.gz
  url: https://pypi.python.org/packages/source/e/example.archetype/example.archetype-1.0.tar.gz
  md5: 8af612bf0a4ac120f501128d4dd3ca9f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - example.archetype = example.archetype:main
    #
    # Would create an entry point called example.archetype that calls example.archetype.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - example
    - example.archetype
    - example.archetype.browser
    - example.archetype.content
    - example.archetype.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/example.archetype
  license: GPL
  summary: 'An example Archetypes-based product.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
