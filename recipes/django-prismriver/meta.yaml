package:
  name: django-prismriver
  version: "0.12"

source:
  fn: django-prismriver-0.12.tar.gz
  url: https://pypi.python.org/packages/source/d/django-prismriver/django-prismriver-0.12.tar.gz
  md5: 29765d01613da505c8d21dd51d676917
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-prismriver = django-prismriver:main
    #
    # Would create an entry point called django-prismriver that calls django-prismriver.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - prismriver
    - prismriver.dashboard
    - prismriver.dashboard.plugins
    - prismriver.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/PolCPP/django-prismriver
  license: BSD licence, see LICENCE.txt
  summary: 'A light but cool Django admin theme'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
