package:
  name: etl_utils
  version: "0.1.10"

source:
  fn: etl_utils-0.1.10.tar.gz
  url: https://pypi.python.org/packages/source/e/etl_utils/etl_utils-0.1.10.tar.gz
  md5: 4b0a7e078eee3f0920fbdf636126440f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - etl_utils = etl_utils:main
    #
    # Would create an entry point called etl_utils that calls etl_utils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - werkzeug
    - progressbar ==2.2
    - termcolor
    - pysingleton
    - humanize ==0.5.1

  run:
    - python
    - werkzeug
    - progressbar ==2.2
    - termcolor
    - pysingleton
    - humanize ==0.5.1

test:
  # Python imports
  imports:
    - etl_utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://17zuoye.github.io/etl_utils/
  license: MIT
  summary: 'etl utils'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
