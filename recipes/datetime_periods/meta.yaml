package:
  name: datetime_periods
  version: "1.1.1"

source:
  fn: datetime_periods-1.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/datetime_periods/datetime_periods-1.1.1.tar.gz
  md5: 7a2840ab6122c1d4cdc9db16c3cada71
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - datetime_periods = datetime_periods:main
    #
    # Would create an entry point called datetime_periods that calls datetime_periods.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python-dateutil >=2.1
    - datetime_truncate >=1.0.0

  run:
    - python
    - python-dateutil >=2.1
    - datetime_truncate >=1.0.0

test:
  # Python imports
  imports:
    - datetime_periods

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/gaqzi/datetime_periods
  license: BSD License
  summary: 'Easily create time periods from timestamps'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
