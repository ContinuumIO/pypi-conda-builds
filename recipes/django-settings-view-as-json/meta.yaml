package:
  name: django-settings-view-as-json
  version: "0.0.6"

source:
  fn: django-settings-view-as-json-0.0.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-settings-view-as-json/django-settings-view-as-json-0.0.6.tar.gz
  md5: 273924715556a14210412264572937c9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-settings-view-as-json = django-settings-view-as-json:main
    #
    # Would create an entry point called django-settings-view-as-json that calls django-settings-view-as-json.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django
    - django-braces
    - six

  run:
    - python
    - django
    - django-braces
    - six

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/hampsterx/django-settings-view-as-json
  license: django-setup
  summary: 'View Django Settings at a URL'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
