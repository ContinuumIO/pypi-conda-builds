package:
  name: python-lirc
  version: "1.2.1"

source:
  fn: python-lirc-1.2.1.tar.gz
  url: https://pypi.python.org/packages/source/p/python-lirc/python-lirc-1.2.1.tar.gz
  md5: 87976c6260eda2d3d04340b627c4b5b2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-lirc = python-lirc:main
    #
    # Would create an entry point called python-lirc that calls python-lirc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/tompreston/python-lirc
  license: GNU Affero General Public License v3 or later (AGPLv3+)
  summary: 'Python bindings for LIRC.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
