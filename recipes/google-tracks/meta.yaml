package:
  name: google-tracks
  version: "0.1.4"

source:
  fn: google-tracks-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/g/google-tracks/google-tracks-0.1.4.tar.gz
  md5: 6fd37b8b9cde00092ca2b40d132137a0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - google-tracks = google-tracks:main
    #
    # Would create an entry point called google-tracks that calls google-tracks.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - httplib2
    - oauth2client
    - pyopenssl

  run:
    - python
    - httplib2
    - oauth2client
    - pyopenssl

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/TDispatch/google-tracks-api
  license: BSD License
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
