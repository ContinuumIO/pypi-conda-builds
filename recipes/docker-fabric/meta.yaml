package:
  name: docker-fabric
  version: "0.3.5"

source:
  fn: docker-fabric-0.3.5.tar.gz
  url: https://pypi.python.org/packages/source/d/docker-fabric/docker-fabric-0.3.5.tar.gz
  md5: 3b75b223f539c6da6246dc0d2c68743a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - docker-fabric = docker-fabric:main
    #
    # Would create an entry point called docker-fabric that calls docker-fabric.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six
    - fabric >=1.8.0
    - docker-py >=0.5.0
    - docker-map >=0.3.2

  run:
    - python
    - six
    - fabric >=1.8.0
    - docker-py >=0.5.0
    - docker-map >=0.3.2

test:
  # Python imports
  imports:
    - dockerfabric
    - dockerfabric.utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/merll/docker-fabric
  license: MIT License
  summary: 'Integration of Docker deployments into Fabric.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
