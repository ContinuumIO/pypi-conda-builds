package:
  name: collective.groupspace.roles
  version: "1.0.3"

source:
  fn: collective.groupspace.roles-1.0.3.zip
  url: https://pypi.python.org/packages/source/c/collective.groupspace.roles/collective.groupspace.roles-1.0.3.zip
  md5: 44dc58ae2523bc4da819bad2ad308d7b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.groupspace.roles = collective.groupspace.roles:main
    #
    # Would create an entry point called collective.groupspace.roles that calls collective.groupspace.roles.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.groupspace
    - collective.groupspace.roles
    - collective.groupspace.roles.browser
    - collective.groupspace.roles.portlets
    - collective.groupspace.roles.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/collective.groupspace.roles
  license: GNU General Public License (GPL)
  summary: 'Plone collective.groupspace view for assigning roles to participants'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
