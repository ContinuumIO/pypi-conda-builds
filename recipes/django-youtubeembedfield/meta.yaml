package:
  name: django-youtubeembedfield
  version: "0.1.3"

source:
  fn: django-youtubeembedfield-0.1.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-youtubeembedfield/django-youtubeembedfield-0.1.3.tar.gz
  md5: 0d157889465cee9cc864d3c092384240
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-youtubeembedfield = django-youtubeembedfield:main
    #
    # Would create an entry point called django-youtubeembedfield that calls django-youtubeembedfield.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - youtubeembedfield

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/Celc/django-youtubeembedfield
  license: The MIT License
  summary: 'A field that accepts regular YouTube links and saves the embed ones.\n'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
