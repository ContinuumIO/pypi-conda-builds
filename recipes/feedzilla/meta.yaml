package:
  name: feedzilla
  version: "0.24"

source:
  fn: feedzilla-0.24.tar.gz
  url: https://pypi.python.org/packages/source/f/feedzilla/feedzilla-0.24.tar.gz
  md5: 1e51bcf395540e48ecba2b8d8a07c9ad
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - feedzilla = feedzilla:main
    #
    # Would create an entry point called feedzilla that calls feedzilla.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - demo
    - feedzilla
    - feedzilla.management
    - feedzilla.management.commands
    - feedzilla.migrations
    - feedzilla.processors
    - feedzilla.templatetags
    - feedzilla.util

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/lorien/feedzilla
  license: BSD License
  summary: 'Django application for ATOM/RSS feeds aggregation i.e. planet engine'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
