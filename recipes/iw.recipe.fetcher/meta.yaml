package:
  name: iw.recipe.fetcher
  version: "0.2"

source:
  fn: iw.recipe.fetcher-0.2.tar.gz
  url: https://pypi.python.org/packages/source/i/iw.recipe.fetcher/iw.recipe.fetcher-0.2.tar.gz
  md5: c200b73c998e54a963bd0cd7248ab298
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - iw.recipe.fetcher = iw.recipe.fetcher:main
    #
    # Would create an entry point called iw.recipe.fetcher that calls iw.recipe.fetcher.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout

test:
  # Python imports
  imports:
    - iw
    - iw.recipe
    - iw.recipe.fetcher
    - iw.recipe.fetcher.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: GPL
  summary: 'ZC buildout recipe to fetch urls'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
