package:
  name: flask-bouncer
  version: "0.1.13"

source:
  fn: flask-bouncer-0.1.13.tar.gz
  url: https://pypi.python.org/packages/source/f/flask-bouncer/flask-bouncer-0.1.13.tar.gz
  md5: ce698595da947dde9600300bdb00fbcc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-bouncer = flask-bouncer:main
    #
    # Would create an entry point called flask-bouncer that calls flask-bouncer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - bouncer >=0.1.8
    - flask >=0.9
    - blinker

  run:
    - python
    - bouncer >=0.1.8
    - flask >=0.9
    - blinker

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/jtushman/flask-bouncer
  license: BSD License
  summary: 'Flask Simple Declarative Authentication based on Ryan Bates excellent cancan library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
