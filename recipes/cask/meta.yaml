package:
  name: cask
  version: "0.1.1"

source:
  fn: cask-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/c/cask/cask-0.1.1.tar.gz
  md5: 2cfb27a3bc4f737881770bec89f3f967
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cask = cask:main
    #
    # Would create an entry point called cask that calls cask.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - injector >=0.7.4
    - six

  run:
    - python
    - injector >=0.7.4
    - six

test:
  # Python imports
  imports:
    - cask

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jstasiak/cask
  license: MIT License
  summary: 'Injector-enabled Python application microframework'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
