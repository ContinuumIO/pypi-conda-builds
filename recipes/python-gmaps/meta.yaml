package:
  name: python-gmaps
  version: "0.2.1"

source:
  fn: python-gmaps-0.2.1.zip
  url: https://pypi.python.org/packages/source/p/python-gmaps/python-gmaps-0.2.1.zip
  md5: 10876b5f133fa3047701409368a4c37e
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-gmaps = python-gmaps:main
    #
    # Would create an entry point called python-gmaps that calls python-gmaps.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests
    - pytz

  run:
    - python
    - requests
    - pytz

test:
  # Python imports
  imports:
    - gmaps

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/swistakm/python-gmaps
  license: BSD License
  summary: 'Google Maps API client'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
