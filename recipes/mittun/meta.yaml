package:
  name: mittun
  version: "0.1.2.8"

source:
  fn: mittun-0.1.2.8.tar.gz
  url: https://pypi.python.org/packages/source/m/mittun/mittun-0.1.2.8.tar.gz
  md5: 5e5a6b81e90b266ccb4cb66056915462
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mittun = mittun:main
    #
    # Would create an entry point called mittun that calls mittun.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - mysql-python ==1.2.3
    - django ==1.4.2
    - south ==0.7.3
    - pil ==1.1.7

  run:
    - python
    - mysql-python ==1.2.3
    - django ==1.4.2
    - south ==0.7.3
    - pil ==1.1.7

test:
  # Python imports
  imports:
    - mittun
    - mittun.events
    - mittun.events.management
    - mittun.events.management.commands
    - mittun.events.migrations
    - mittun.registration
    - mittun.registration.migrations
    - mittun.sponsors
    - mittun.sponsors.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'conference manager'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
