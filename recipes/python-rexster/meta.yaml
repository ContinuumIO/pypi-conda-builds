package:
  name: python-rexster
  version: "0.1.1"

source:
  fn: python-rexster-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/p/python-rexster/python-rexster-0.1.1.tar.gz
  md5: ba817806a985e7e5f1e4d1be34f5a99b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - python-rexster = python-rexster:main
    #
    # Would create an entry point called python-rexster that calls python-rexster.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - requests
    - simplejson

  run:
    - python
    - requests
    - simplejson

test:
  # Python imports
  imports:
    - rexster

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/CulturePlex/python-rexster
  license: GNU General Public License (GPL)
  summary: 'A Python client for Rexster following\n                    the Blueprints property graph model\n                    interface'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
