package:
  name: coopr.pysos
  version: "2.0.9"

source:
  fn: coopr.pysos-2.0.9.tar.gz
  url: https://pypi.python.org/packages/source/c/coopr.pysos/coopr.pysos-2.0.9.tar.gz
  md5: debceea17575b506cf9fc445eaaec429
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - coopr.pysos = coopr.pysos:main
    #
    # Would create an entry point called coopr.pysos that calls coopr.pysos.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - coopr
    - coopr.pysos

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://software.sandia.gov/svn/public/coopr/coopr.pysos
  license: BSD License
  summary: 'Coopr utilities for composing heterogeous models'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
