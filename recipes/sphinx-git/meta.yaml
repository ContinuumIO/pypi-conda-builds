package:
  name: sphinx-git
  version: "9"

source:
  fn: sphinx-git-9.tar.gz
  url: https://pypi.python.org/packages/source/s/sphinx-git/sphinx-git-9.tar.gz
  md5: b42a394031efbd11c4246c27b9112a51
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sphinx-git = sphinx-git:main
    #
    # Would create an entry point called sphinx-git that calls sphinx-git.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - sphinx
    - gitpython >=0.3.6

  run:
    - python
    - sphinx
    - gitpython >=0.3.6

test:
  # Python imports
  imports:
    - sphinx_git

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/OddBloke/sphinx-git
  license: UNKNOWN
  summary: 'git Changelog for Sphinx'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
