package:
  name: zope.html
  version: "2.4.2"

source:
  fn: zope.html-2.4.2.zip
  url: https://pypi.python.org/packages/source/z/zope.html/zope.html-2.4.2.zip
  md5: b47564cef47997cc4dab03ff66111ef0
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zope.html = zope.html:main
    #
    # Would create an entry point called zope.html that calls zope.html.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - pytz
    - setuptools
    - zc.resourcelibrary
    - zodb3
    - zope.annotation
    - zope.component
    - zope.event
    - zope.file
    - zope.formlib >=4
    - zope.i18nmessageid
    - zope.interface
    - zope.lifecycleevent
    - zope.mimetype
    - zope.publisher
    - zope.schema

  run:
    - python
    - pytz
    - setuptools
    - zc.resourcelibrary
    - zodb3
    - zope.annotation
    - zope.component
    - zope.event
    - zope.file
    - zope.formlib >=4
    - zope.i18nmessageid
    - zope.interface
    - zope.lifecycleevent
    - zope.mimetype
    - zope.publisher
    - zope.schema

test:
  # Python imports
  imports:
    - zope
    - zope.html

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://cheeseshop.python.org/pypi/zope.html
  license: Zope Public License
  summary: 'HTML and XHTML Editing Support'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
