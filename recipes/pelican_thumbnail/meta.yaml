package:
  name: pelican_thumbnail
  version: "0.2"

source:
  fn: pelican_thumbnail-0.2.tar.gz
  url: https://pypi.python.org/packages/source/p/pelican_thumbnail/pelican_thumbnail-0.2.tar.gz
  md5: 724fd94811b7c580e39288f6f6a90b57
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pelican_thumbnail = pelican_thumbnail:main
    #
    # Would create an entry point called pelican_thumbnail that calls pelican_thumbnail.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pelican

  run:
    - python
    - pelican

test:
  # Python imports
  imports:
    - pelican_thumbnail

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/wutali/pelican_thumbnail
  license: UNKNOWN
  summary: 'A Pelican plugin to add a thumbnail for the article.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
