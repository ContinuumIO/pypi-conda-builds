package:
  name: motorway
  version: "1.1.0"

source:
  fn: motorway-1.1.0.tar.gz
  url: https://pypi.python.org/packages/source/m/motorway/motorway-1.1.0.tar.gz
  md5: 8451f24f5ee9ed1abf7b0c4ef123f0e0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - motorway = motorway:main
    #
    # Would create an entry point called motorway that calls motorway.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - hash_ring ==1.3.1
    - flask
    - setproctitle ==1.1.8
    - pyzmq ==14.3.1
    - colorlog ==2.3.1
    - isodate
    - nose
    - freezegun
    - mock
    - coverage

  run:
    - python
    - hash_ring ==1.3.1
    - flask
    - setproctitle ==1.1.8
    - pyzmq ==14.3.1
    - colorlog ==2.3.1
    - isodate
    - nose
    - freezegun
    - mock
    - coverage

test:
  # Python imports
  imports:
    - motorway
    - motorway.contrib
    - motorway.contrib.amazon_kinesis
    - motorway.contrib.amazon_sqs
    - motorway.discovery
    - motorway.tests
    - tutorial

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'Real-time ZMQ-based pure python data pipeline'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
