package:
  name: django-template-finder
  version: "0.6.2"

source:
  fn: django-template-finder-0.6.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-template-finder/django-template-finder-0.6.2.tar.gz
  md5: 98657ed009d686abcbaf145231b8ffdf
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-template-finder = django-template-finder:main
    #
    # Would create an entry point called django-template-finder that calls django-template-finder.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - templatefinder
    - templatefinder.test_project
    - templatefinder.test_project.testapp

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/TyMaszWeb/django-template-finder
  license: BSD License
  summary: 'Simple Django utility that allows you to find templates with names matching given pattern'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
