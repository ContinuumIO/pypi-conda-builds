package:
  name: django-basket
  version: "0.4.5"

source:
  fn: django-basket-0.4.5.tar.gz
  url: https://pypi.python.org/packages/source/d/django-basket/django-basket-0.4.5.tar.gz
  md5: a191da925126ee0922ad98afb8199119
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-basket = django-basket:main
    #
    # Would create an entry point called django-basket that calls django-basket.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django-classy-tags

  run:
    - python
    - django-classy-tags

test:
  # Python imports
  imports:
    - basket
    - basket.contrib
    - basket.contrib.orderinfo
    - basket.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: GNU General Public License (GPL)
  summary: 'Django generic basket application. Allow to put in basket any object and view orders in the Django admin interface'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
