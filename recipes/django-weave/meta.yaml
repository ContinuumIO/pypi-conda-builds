package:
  name: django-weave
  version: "0.1.6.svn96"

source:
  fn: django-weave-0.1.6.svn96.tar.gz
  url: https://pypi.python.org/packages/source/d/django-weave/django-weave-0.1.6.svn96.tar.gz
  md5: 1543288d09b2a4e5fa21cb392f0f01ab
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-weave = django-weave:main
    #
    # Would create an entry point called django-weave that calls django-weave.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - weave
    - weave.views

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/django-weave/
  license: GNU General Public License (GPL)
  summary: 'django-weave is a Django reusable application witch implements a Firefox weave server.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
