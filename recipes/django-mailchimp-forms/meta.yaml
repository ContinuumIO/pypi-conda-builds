package:
  name: django-mailchimp-forms
  version: "0.2"

source:
  fn: django-mailchimp-forms-0.2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-mailchimp-forms/django-mailchimp-forms-0.2.tar.gz
  md5: 818ba2147bfc1d98f987f299c2ff9849
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-mailchimp-forms = django-mailchimp-forms:main
    #
    # Would create an entry point called django-mailchimp-forms that calls django-mailchimp-forms.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.2.3

  run:
    - python
    - django >=1.2.3

test:
  # Python imports
  imports:
    - django_mailchimp_forms

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.indifex.com/django-mailchimp-forms/
  license: BSD License
  summary: 'Mailchimp registration framework for Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
