package:
  name: pongo
  version: "0.1.2"

source:
  fn: pongo-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/p/pongo/pongo-0.1.2.tar.gz
  md5: d0c8bfac84803a5fd4bc14c198bdecc5
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pongo = pongo:main
    #
    # Would create an entry point called pongo that calls pongo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - bottle >=0.8.0
    - pymongo >=1.8
    - mako >=0.3.0

  run:
    - python
    - bottle >=0.8.0
    - pymongo >=1.8
    - mako >=0.3.0

test:
  # Python imports
  imports:
    - pongo

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://bitbucket.org/vrde/pongo
  license: BSD License
  summary: 'A Mongo database explorer'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
