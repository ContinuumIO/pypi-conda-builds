package:
  name: flask-holster
  version: "0.3.4"

source:
  fn: Flask-Holster-0.3.4.tar.gz
  url: https://pypi.python.org/packages/source/F/Flask-Holster/Flask-Holster-0.3.4.tar.gz
  md5: 5868f559271dd3b3feb5619ad92e59dc
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-holster = flask-holster:main
    #
    # Would create an entry point called flask-holster that calls flask-holster.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask

  run:
    - python
    - flask

test:
  # Python imports
  imports:
    - flask_holster

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/MostAwesomeDude/flask-holster
  license: MIT License
  summary: 'Rigid MVC content negotiation for Flask'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
