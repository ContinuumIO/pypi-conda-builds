package:
  name: logmongo
  version: "0.1.4"

source:
  fn: logmongo-0.1.4.tar.gz
  url: https://pypi.python.org/packages/source/l/logmongo/logmongo-0.1.4.tar.gz
  md5: 0b218bce9d2fe5533afc7fd101f4cf67
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - logmongo = logmongo:main
    #
    # Would create an entry point called logmongo that calls logmongo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pymongo
    - prettyprint

  run:
    - python
    - pymongo
    - prettyprint

test:
  # Python imports
  imports:
    - logmongo

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/russellballestrini/logmongo
  license: Public Domain
  summary: 'Logmongo: Log messages to a capped MongoDB collections'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
