package:
  name: django-disposable-email-checker
  version: "0.2.3"

source:
  fn: django-disposable-email-checker-0.2.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-disposable-email-checker/django-disposable-email-checker-0.2.3.tar.gz
  md5: cc83f87ede639877aed59c8c81b1bf44
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-disposable-email-checker = django-disposable-email-checker:main
    #
    # Would create an entry point called django-disposable-email-checker that calls django-disposable-email-checker.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - disposable_email_checker

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/aaronbassett/DisposableEmailChecker
  license: MIT License
  summary: 'Python class for use with Django to detect Disposable Emails'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
