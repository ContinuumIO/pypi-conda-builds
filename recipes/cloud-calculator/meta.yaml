package:
  name: cloud-calculator
  version: "0.1.0"

source:
  fn: cloud-calculator-0.1.0.zip
  url: https://pypi.python.org/packages/source/c/cloud-calculator/cloud-calculator-0.1.0.zip
  md5: e8061b26c9f1890a54585d1185d0ee4c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cloud-calculator = cloud-calculator:main
    #
    # Would create an entry point called cloud-calculator that calls cloud-calculator.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - sitio
    - sitio.adapters
    - sitio.analyser
    - sitio.common
    - sitio.model

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/HEP-KBFI/ccc
  license: BSD (3-clause)
  summary: 'Cost calculator for public cloud services.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
