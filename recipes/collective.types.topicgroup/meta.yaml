package:
  name: collective.types.topicgroup
  version: "9.06.02"

source:
  fn: collective.types.topicgroup-9.06.02.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.types.topicgroup/collective.types.topicgroup-9.06.02.tar.gz
  md5: b63915af36bc667f9a591b829836803f
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.types.topicgroup = collective.types.topicgroup:main
    #
    # Would create an entry point called collective.types.topicgroup that calls collective.types.topicgroup.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - collective
    - collective.types
    - collective.types.topicgroup
    - collective.types.topicgroup.browser
    - collective.types.topicgroup.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://cphi.washington.edu
  license: GPL 2
  summary: 'TopicGroup content type'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
