package:
  name: django-formalizr
  version: "1"

source:
  fn: django-formalizr-1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-formalizr/django-formalizr-1.tar.gz
  md5: 904e0f34da716ec7baf61ba3142cca8b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-formalizr = django-formalizr:main
    #
    # Would create an entry point called django-formalizr that calls django-formalizr.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - formalizr

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/krasnoperov/django-formalizr
  license: BSD License
  summary: 'Django AJAX Class Based Views'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
