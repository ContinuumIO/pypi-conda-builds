package:
  name: django-hashed-url
  version: "0.1.1"

source:
  fn: django-hashed-url-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-hashed-url/django-hashed-url-0.1.1.tar.gz
  md5: 68fe5fbd03cc2a30c5c5b3d853fc8481
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-hashed-url = django-hashed-url:main
    #
    # Would create an entry point called django-hashed-url that calls django-hashed-url.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - hashed_url
    - hashed_url.management
    - hashed_url.management.commands
    - hashed_url.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/continuous/django-hashed-url
  license: Apache Software License
  summary: 'Package for pre-authorising URLs using a hash parameter'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
