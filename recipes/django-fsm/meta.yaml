package:
  name: django-fsm
  version: "2.2.1"

source:
  fn: django-fsm-2.2.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-fsm/django-fsm-2.2.1.tar.gz
  md5: 42c0a9a3a2f27925a3f5cd244665a6e0
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-fsm = django-fsm:main
    #
    # Would create an entry point called django-fsm that calls django-fsm.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_fsm
    - django_fsm.management
    - django_fsm.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/kmmbvnr/django-fsm
  license: MIT License
  summary: 'Django friendly finite state machine support.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
