package:
  name: django-absoluteurl
  version: "1.0"

source:
  fn: django-absoluteurl-1.0.tar.gz
  url: https://pypi.python.org/packages/source/d/django-absoluteurl/django-absoluteurl-1.0.tar.gz
  md5: 5a0c6b9210a6caba5c9b8b66f50df750
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-absoluteurl = django-absoluteurl:main
    #
    # Would create an entry point called django-absoluteurl that calls django-absoluteurl.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - absoluteurl
    - absoluteurl.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bgryszko/django-absoluteurl
  license: MIT License
  summary: 'Absolute Url is an app for creating absolute urls in Django templates.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
