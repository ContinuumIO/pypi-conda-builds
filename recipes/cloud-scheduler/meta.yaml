package:
  name: cloud-scheduler
  version: "1.8.1"

source:
  fn: cloud-scheduler-1.8.1.tar.gz
  url: https://pypi.python.org/packages/source/c/cloud-scheduler/cloud-scheduler-1.8.1.tar.gz
  md5: a4c39339df6bcd84f94cee7ab05a9128
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cloud-scheduler = cloud-scheduler:main
    #
    # Would create an entry point called cloud-scheduler that calls cloud-scheduler.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - boto >=2.0b3

  run:
    - python
    - boto >=2.0b3

test:
  # Python imports
  imports:
    - cloudscheduler

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/hep-gc/cloud-scheduler
  license: 'GPL3' or 'Apache 2'
  summary: 'A cloud-enabled distributed resource manager'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
