package:
  name: django-mysql
  version: "0.1.10"

source:
  fn: django-mysql-0.1.10.tar.gz
  url: https://pypi.python.org/packages/source/d/django-mysql/django-mysql-0.1.10.tar.gz
  md5: 8ba29ef851529e13f9e41f4ca342dabe
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-mysql = django-mysql:main
    #
    # Would create an entry point called django-mysql that calls django-mysql.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.7

  run:
    - python
    - django >=1.7

test:
  # Python imports
  imports:
    - django_mysql
    - django_mysql/management
    - django_mysql/management/commands
    - django_mysql/models
    - django_mysql/models/fields
    - django_mysql/shims
    - django_mysql/test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/adamchainz/django-mysql
  license: BSD License
  summary: 'Extensions to Django for use with MySQL/MariaDB'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
