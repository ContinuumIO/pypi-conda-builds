package:
  name: latimes-qiklog
  version: "0.9.04"

source:
  fn: latimes-qiklog-0.9.04.tar.gz
  url: https://pypi.python.org/packages/source/l/latimes-qiklog/latimes-qiklog-0.9.04.tar.gz
  md5: 8d09717c23daca1b74e4e77f3704242c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - latimes-qiklog = latimes-qiklog:main
    #
    # Would create an entry point called latimes-qiklog that calls latimes-qiklog.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - qiklog

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/datadesk/latimes-qiklog
  license: MIT License
  summary: "A simplified wrapper for Python's logging module"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
