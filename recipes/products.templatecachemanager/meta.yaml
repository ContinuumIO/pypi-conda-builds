package:
  name: products.templatecachemanager
  version: "0.1dev-r67896"

source:
  fn: Products.TemplateCacheManager-0.1dev-r67896.tar.gz
  url: https://pypi.python.org/packages/source/P/Products.TemplateCacheManager/Products.TemplateCacheManager-0.1dev-r67896.tar.gz
  md5: 1a06f4b4d2aa43f32168d25b54ce7d5c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - products.templatecachemanager = products.templatecachemanager:main
    #
    # Would create an entry point called products.templatecachemanager that calls products.templatecachemanager.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - Products
    - Products.TemplateCacheManager

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/Products.TemplateCacheManager
  license: ZPL
  summary: 'Cache rendered pages including headers with ETag support'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
