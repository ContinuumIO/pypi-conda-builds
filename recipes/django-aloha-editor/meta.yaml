package:
  name: django-aloha-editor
  version: "1.1.0"

source:
  fn: django-aloha-editor-1.1.0.zip
  url: https://pypi.python.org/packages/source/d/django-aloha-editor/django-aloha-editor-1.1.0.zip
  md5: 30d58006adf64ed3948e20462a69e796
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-aloha-editor = django-aloha-editor:main
    #
    # Would create an entry point called django-aloha-editor that calls django-aloha-editor.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - aloha_editor
    - aloha_editor.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/baskoopmans/django-aloha-editor/
  license: BSD License
  summary: 'A set of project independent reusable features.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
