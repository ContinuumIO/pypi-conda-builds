package:
  name: deltacloud
  version: "0.9.1"

source:
  fn: deltacloud-0.9.1.tar.gz
  url: https://pypi.python.org/packages/source/d/deltacloud/deltacloud-0.9.1.tar.gz
  md5: acd1cc80f3ae5ceda72b65b8345d7ebe
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - deltacloud = deltacloud:main
    #
    # Would create an entry point called deltacloud that calls deltacloud.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests >=1.0

  run:
    - python
    - requests >=1.0

test:
  # Python imports
  imports:
    - deltacloud

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://deltacloud.apache.org/
  license: Apache Software License
  summary: 'Python client library for Deltacloud API'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
