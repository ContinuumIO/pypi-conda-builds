package:
  name: backgroundr
  version: "1.1.11"

source:
  fn: Backgroundr-1.1.11.tar.gz
  url: https://pypi.python.org/packages/source/B/Backgroundr/Backgroundr-1.1.11.tar.gz
  md5: 452c47ba7fea872cf0fb37ce3274a042
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - backgroundr = backgroundr:main
    #
    # Would create an entry point called backgroundr that calls backgroundr.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pillow

  run:
    - python
    - pillow

test:
  # Python imports
  imports:
    - backgroundr

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/smg247/backgroundr
  license: backitup
  summary: 'A simple way to create backgrounds for your computer with custom colors and text'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
