package:
  name: flask-macros
  version: "0.1.5"

source:
  fn: flask-macros-0.1.5.tar.gz
  url: https://pypi.python.org/packages/source/f/flask-macros/flask-macros-0.1.5.tar.gz
  md5: 556256062130dec367cdf0de80a43690
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-macros = flask-macros:main
    #
    # Would create an entry point called flask-macros that calls flask-macros.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask ==0.10.1
    - jinja2 ==2.7.3
    - wtforms ==2.0.1

  run:
    - python
    - flask ==0.10.1
    - jinja2 ==2.7.3
    - wtforms ==2.0.1

test:
  # Python imports
  imports:
    - flask_macros

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'macros for flask projects'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
