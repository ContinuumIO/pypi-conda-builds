package:
  name: collective.ptg.scrollable
  version: "0.3"

source:
  fn: collective.ptg.scrollable-0.3.zip
  url: https://pypi.python.org/packages/source/c/collective.ptg.scrollable/collective.ptg.scrollable-0.3.zip
  md5: 19fbb78f4c06ffdcaa3b6fba1dfe92b2
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.ptg.scrollable = collective.ptg.scrollable:main
    #
    # Would create an entry point called collective.ptg.scrollable that calls collective.ptg.scrollable.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - collective.plonetruegallery

  run:
    - python
    - setuptools
    - setuptools
    - collective.plonetruegallery

test:
  # Python imports
  imports:
    - collective
    - collective.ptg
    - collective.ptg.scrollable

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/collective/collective.ptg.scrollable
  license: GPL
  summary: 'scrollable gallery type for plonetruegallery'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
