package:
  name: plonetheme.blueblog
  version: "0.1dev-r60739"

source:
  fn: plonetheme.blueblog-0.1dev-r60739.tar.gz
  url: https://pypi.python.org/packages/source/p/plonetheme.blueblog/plonetheme.blueblog-0.1dev-r60739.tar.gz
  md5: 9767477f3b5e94b3f150aa1a69e177d9
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plonetheme.blueblog = plonetheme.blueblog:main
    #
    # Would create an entry point called plonetheme.blueblog that calls plonetheme.blueblog.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - plonetheme
    - plonetheme.blueblog
    - plonetheme.blueblog.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/
  license: GPL
  summary: 'An installable theme for Plone 3.0'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
