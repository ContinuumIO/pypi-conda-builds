package:
  name: lunar
  version: "0.0.1"

source:
  fn: lunar-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/l/lunar/lunar-0.0.1.tar.gz
  md5: 851924af8e9e41195fab7c45b9be99d2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lunar = lunar:main
    #
    # Would create an entry point called lunar that calls lunar.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - lunar

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'lunar is a WSGI based webframework in pure Python, without any third-party dependency.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
