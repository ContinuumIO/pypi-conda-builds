package:
  name: djcroco
  version: "0.3.2"

source:
  fn: djcroco-0.3.2.tar.gz
  url: https://pypi.python.org/packages/source/d/djcroco/djcroco-0.3.2.tar.gz
  md5: 6ee58214c2051d55fdf8f3435fdddb5a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - djcroco = djcroco:main
    #
    # Would create an entry point called djcroco that calls djcroco.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - crocodoc

  run:
    - python
    - crocodoc

test:
  # Python imports
  imports:
    - djcroco
    - djcroco.templatetags
    - djcroco.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mattack108/djcroco/
  license: UNKNOWN
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
