package:
  name: koansys.recipe.hooks
  version: "0.1-dev-r8"

source:
  fn: koansys.recipe.hooks-0.1-dev-r8.tar.gz
  url: https://pypi.python.org/packages/source/k/koansys.recipe.hooks/koansys.recipe.hooks-0.1-dev-r8.tar.gz
  md5: c1691d25b5c6d53980b1aa052a0ca165
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - koansys.recipe.hooks = koansys.recipe.hooks:main
    #
    # Would create an entry point called koansys.recipe.hooks that calls koansys.recipe.hooks.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zc.buildout
    - setuptools

  run:
    - python
    - setuptools
    - zc.buildout
    - setuptools

test:
  # Python imports
  imports:
    - koansys
    - koansys.recipe
    - koansys.recipe.hooks

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://koansys-recipe-hooks.googlecode.com/svn/trunk
  license: GPL
  summary: 'zc.buildout recipe to run python methods as hooks'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
