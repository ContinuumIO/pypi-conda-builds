package:
  name: pyeventemitter
  version: "1.0.2"

source:
  fn: PyEventEmitter-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/P/PyEventEmitter/PyEventEmitter-1.0.2.tar.gz
  md5: 35afd0ef0ea30c32416a2c412b77f17a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - pyeventemitter = pyeventemitter:main
    #
    # Would create an entry point called pyeventemitter that calls pyeventemitter.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - event_emitter
    - pyeventemitter

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/etissieres/PyEventEmitter
  license: MIT
  summary: 'Simple python events library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
