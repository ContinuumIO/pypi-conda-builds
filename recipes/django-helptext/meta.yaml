package:
  name: django-helptext
  version: "0.3"

source:
  fn: django-helptext-0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-helptext/django-helptext-0.3.tar.gz
  md5: 813bc84bb162e033b25ff8421e3529d8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-helptext = django-helptext:main
    #
    # Would create an entry point called django-helptext that calls django-helptext.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - helptext

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.bitbucket.org/smulloni/django-helptext/
  license: BSD License
  summary: 'A django application for editing django model field help text in the django admin'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
