package:
  name: django-reportmail
  version: "1.2.4"

source:
  fn: django-reportmail-1.2.4.tar.gz
  url: https://pypi.python.org/packages/source/d/django-reportmail/django-reportmail-1.2.4.tar.gz
  md5: 60e28657dbe50afcf934e162beb36947
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-reportmail = django-reportmail:main
    #
    # Would create an entry point called django-reportmail that calls django-reportmail.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.6,<1.8

  run:
    - python
    - django >=1.6,<1.8

test:
  # Python imports
  imports:
    - reportmail

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/hirokiky/django-reportmail
  license: MIT License
  summary: 'django library to render and send report mail. '

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
