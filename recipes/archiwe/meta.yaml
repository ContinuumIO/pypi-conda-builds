package:
  name: archiwe
  version: "0.9.0"

source:
  fn: archiwe-0.9.0.tar.gz
  url: https://pypi.python.org/packages/source/a/archiwe/archiwe-0.9.0.tar.gz
  md5: 2e06340bb0c0eac286aa59cfab96060d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - archiwe = archiwe:main
    #
    # Would create an entry point called archiwe that calls archiwe.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - archiwe
    - archiwe/store
    - archiwe/store/read
    - archiwe/store/write

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/archiwe
  license: MIT License
  summary: 'Webification of archive files'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
