package:
  name: django-sampledatahelper
  version: "0.3"

source:
  fn: django-sampledatahelper-0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-sampledatahelper/django-sampledatahelper-0.3.tar.gz
  md5: 2a5bf3c10466dfbb8021a8a248368248
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-sampledatahelper = django-sampledatahelper:main
    #
    # Would create an entry point called django-sampledatahelper that calls django-sampledatahelper.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - six
    - django
    - sampledata

  run:
    - python
    - six
    - django
    - sampledata

test:
  # Python imports
  imports:
    - sampledatahelper
    - sampledatahelper.management
    - sampledatahelper.management.commands
    - tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kaleidos/django-sampledatahelper
  license: BSD License
  summary: 'Helper class for generate sampledata'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
