package:
  name: django-aloha
  version: "0.0.1"

source:
  fn: django-aloha-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-aloha/django-aloha-0.0.1.tar.gz
  md5: 82794148856c935a115c538771a123c6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-aloha = django-aloha:main
    #
    # Would create an entry point called django-aloha that calls django-aloha.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - aloha
    - aloha.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/descent/django-aloha
  license: GNU General Public License (GPL)
  summary: 'django app to easily integrate editing with Aloha WYSIWIG editor in django apps.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
