package:
  name: modoboa-stats
  version: "1.0.1"

source:
  fn: modoboa-stats-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/m/modoboa-stats/modoboa-stats-1.0.1.tar.gz
  md5: 790aba0dd942f9150aab62606a08a39b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - modoboa-stats = modoboa-stats:main
    #
    # Would create an entry point called modoboa-stats that calls modoboa-stats.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - modoboa-admin
    - lxml

  run:
    - python
    - modoboa-admin
    - lxml

test:
  # Python imports
  imports:
    - modoboa_stats
    - modoboa_stats.management
    - modoboa_stats.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://modoboa.org/
  license: MIT License
  summary: 'Graphical statistics for Modoboa'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
