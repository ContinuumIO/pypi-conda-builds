package:
  name: gocept.nginx
  version: "0.9.4"

source:
  fn: gocept.nginx-0.9.4.tar.gz
  url: https://pypi.python.org/packages/source/g/gocept.nginx/gocept.nginx-0.9.4.tar.gz
  md5: cb4dfab84dba1e155a2ab1baaaaec576
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gocept.nginx = gocept.nginx:main
    #
    # Would create an entry point called gocept.nginx that calls gocept.nginx.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout

test:
  # Python imports
  imports:
    - gocept
    - gocept.nginx

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: ZPL 2.1
  summary: 'zc.buildout recipe for configuring an nginx server'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
