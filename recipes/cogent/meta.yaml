package:
  name: cogent
  version: "1.5.3"

source:
  fn: cogent-1.5.3.tgz
  url: https://pypi.python.org/packages/source/c/cogent/cogent-1.5.3.tgz
  md5: ecdfa0bc89e0c04c9d4d39cac9984498
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cogent = cogent:main
    #
    # Would create an entry point called cogent that calls cogent.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - cogent
    - cogent.align
    - cogent.align.weights
    - cogent.app
    - cogent.cluster
    - cogent.core
    - cogent.data
    - cogent.db
    - cogent.db.ensembl
    - cogent.draw
    - cogent.evolve
    - cogent.format
    - cogent.maths
    - cogent.maths.matrix
    - cogent.maths.spatial
    - cogent.maths.stats
    - cogent.maths.stats.cai
    - cogent.maths.unifrac
    - cogent.motif
    - cogent.parse
    - cogent.phylo
    - cogent.recalculation
    - cogent.seqsim
    - cogent.struct
    - cogent.util

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://sourceforge.net/projects/pycogent
  license: GNU General Public License (GPL)
  summary: 'COmparative GENomics Toolkit'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
