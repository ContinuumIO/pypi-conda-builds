package:
  name: products.sampleremember
  version: "1.1b2"

source:
  fn: Products.sampleremember-1.1b2.tar.gz
  url: https://pypi.python.org/packages/source/P/Products.sampleremember/Products.sampleremember-1.1b2.tar.gz
  md5: f619a9327addf55d1f02eb82fd7ee4b7
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - products.sampleremember = products.sampleremember:main
    #
    # Would create an entry point called products.sampleremember that calls products.sampleremember.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - products.remember

  run:
    - python
    - setuptools
    - setuptools
    - products.remember

test:
  # Python imports
  imports:
    - Products
    - Products.sampleremember
    - Products.sampleremember.content
    - Products.sampleremember.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://plone.org/products/remember
  license: GPL
  summary: 'Sample remember based custom member implementation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
