package:
  name: nonofusedmadd
  version: "0.0.2"

source:
  fn: nonofusedmadd-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/n/nonofusedmadd/nonofusedmadd-0.0.2.tar.gz
  md5: 5792fbfdcf172f477a99282130f38bea
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - nonofusedmadd = nonofusedmadd:main
    #
    # Would create an entry point called nonofusedmadd that calls nonofusedmadd.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - nonofusedmadd

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/minrk/nonofusedmadd
  license: BSD License
  summary: 'Remove `-mno-fused-madd` from /System/.../_sysconfigdata.py'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
