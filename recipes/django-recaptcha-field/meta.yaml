package:
  name: django-recaptcha-field
  version: "1.0b2"

source:
  fn: django-recaptcha-field-1.0b2.tar.gz
  url: https://pypi.python.org/packages/source/d/django-recaptcha-field/django-recaptcha-field-1.0b2.tar.gz
  md5: 0cba97f2db0859b366629d926c25a52a
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-recaptcha-field = django-recaptcha-field:main
    #
    # Would create an entry point called django-recaptcha-field that calls django-recaptcha-field.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3
    - recaptcha >=1.0rc1

  run:
    - python
    - django >=1.3
    - recaptcha >=1.0rc1

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://packages.python.org/django-recaptcha-field/
  license: BSD License
  summary: 'Easy-to-use, highly extensible and well documented reCAPTCHA plugin for Django forms'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
