package:
  name: redomino.redirectparent
  version: "0.3"

source:
  fn: redomino.redirectparent-0.3.zip
  url: https://pypi.python.org/packages/source/r/redomino.redirectparent/redomino.redirectparent-0.3.zip
  md5: bbb3dfa8ad923cb1250e2476591c79a6
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - redomino.redirectparent = redomino.redirectparent:main
    #
    # Would create an entry point called redomino.redirectparent that calls redomino.redirectparent.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - redomino
    - redomino.redirectparent
    - redomino.redirectparent.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/
  license: GPL
  summary: 'UNKNOWN'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
