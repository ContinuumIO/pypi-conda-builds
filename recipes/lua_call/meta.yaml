package:
  name: lua_call
  version: "0.10.0"

source:
  fn: lua_call-0.10.0.tar.gz
  url: https://pypi.python.org/packages/source/l/lua_call/lua_call-0.10.0.tar.gz
  md5: 39da6da144c219cc667df790caf28bd1
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - lua_call = lua_call:main
    #
    # Would create an entry point called lua_call that calls lua_call.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - lua_call

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/josiahcarlson/lua-call/
  license: MIT License
  summary: 'Call Lua scripts from other Lua scripts in Redis'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
