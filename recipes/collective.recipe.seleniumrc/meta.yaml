package:
  name: collective.recipe.seleniumrc
  version: "0.6.1.1"

source:
  fn: collective.recipe.seleniumrc-0.6.1.1.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.recipe.seleniumrc/collective.recipe.seleniumrc-0.6.1.1.tar.gz
  md5: 4a2dbde27c94f5efe93efdaa37597a96
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.recipe.seleniumrc = collective.recipe.seleniumrc:main
    #
    # Would create an entry point called collective.recipe.seleniumrc that calls collective.recipe.seleniumrc.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zc.buildout
    - setuptools
    - hexagonit.recipe.download

  run:
    - python
    - setuptools
    - zc.buildout
    - setuptools
    - hexagonit.recipe.download

test:
  # Python imports
  imports:
    - collective
    - collective.recipe
    - collective.recipe.seleniumrc

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/buildout/collective.recipe.seleniumrc
  license: ZPL 2.1
  summary: 'zc.buildout recipe for installing the Selenium RC distribution.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
