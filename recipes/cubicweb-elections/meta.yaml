package:
  name: cubicweb-elections
  version: "0.2.1"

source:
  fn: cubicweb-elections-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/c/cubicweb-elections/cubicweb-elections-0.2.1.tar.gz
  md5: fb054dcc936bcaeb5d3c1c793d844b05
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cubicweb-elections = cubicweb-elections:main
    #
    # Would create an entry point called cubicweb-elections that calls cubicweb-elections.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - cubicweb-dataio >=0.3.0
    - cubicweb-squareui >=0.3.0
    - cubicweb >=3.17.8
    - cubicweb-postgis
    - cubicweb-bootstrap >=0.5.0
    - cubicweb-leaflet >=0.1.0
    - cubicweb-osmfrance >=0.2.2

  run:
    - python
    - cubicweb-dataio >=0.3.0
    - cubicweb-squareui >=0.3.0
    - cubicweb >=3.17.8
    - cubicweb-postgis
    - cubicweb-bootstrap >=0.5.0
    - cubicweb-leaflet >=0.1.0
    - cubicweb-osmfrance >=0.2.2

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.cubicweb.org/project/cubicweb-elections
  license: LGPL
  summary: 'Cube for elections data from data.gouv.fr'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
