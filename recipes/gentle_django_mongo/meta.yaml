package:
  name: gentle_django_mongo
  version: "0.0.3"

source:
  fn: gentle_django_mongo-0.0.3.tar.gz
  url: https://pypi.python.org/packages/source/g/gentle_django_mongo/gentle_django_mongo-0.0.3.tar.gz
  md5: 360aefc8b29e5d7d7566c7f73d19a066
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - gentle_django_mongo = gentle_django_mongo:main
    #
    # Would create an entry point called gentle_django_mongo that calls gentle_django_mongo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4.1
    - pymongo >=2.7.1

  run:
    - python
    - django >=1.4.1
    - pymongo >=2.7.1

test:
  # Python imports
  imports:
    - gentle_django_mongo

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'Django Model interface for mongo models'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
