package:
  name: simple-models
  version: "0.2.4"

source:
  fn: simple-models-0.2.4.tar.gz
  url: https://pypi.python.org/packages/source/s/simple-models/simple-models-0.2.4.tar.gz
  md5: 8a7b6a8922d3499dcab89d34529387e7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - simple-models = simple-models:main
    #
    # Would create an entry point called simple-models that calls simple-models.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - simplemodels

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/prawn-cake/simple-models
  license: MIT
  summary: 'Library for building declarative dict-like structures to get more power and safety in your API applications'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
