package:
  name: redturtle.bent
  version: "0.0.2"

source:
  fn: redturtle.bent-0.0.2.tar.gz
  url: https://pypi.python.org/packages/source/r/redturtle.bent/redturtle.bent-0.0.2.tar.gz
  md5: d85e44c42e541f7ff7dc156018a8bbe5
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - redturtle.bent = redturtle.bent:main
    #
    # Would create an entry point called redturtle.bent that calls redturtle.bent.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - mr.bent

  run:
    - python
    - setuptools
    - setuptools
    - mr.bent

test:
  # Python imports
  imports:
    - redturtle
    - redturtle.bent

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.redturtle.net
  license: GPL
  summary: 'RedTurtle plugins for mr.bent'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
