package:
  name: acme.hello
  version: "0.1"

source:
  fn: acme.hello-0.1.tar.gz
  url: https://pypi.python.org/packages/source/a/acme.hello/acme.hello-0.1.tar.gz
  md5: bb38685069869db80e77773534cd4e5b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - acme.hello = acme.hello:main
    #
    # Would create an entry point called acme.hello that calls acme.hello.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - acme

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jptomo/acme.hello
  license: Apache Software License
  summary: 'This is a test program for namespace_packages.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
