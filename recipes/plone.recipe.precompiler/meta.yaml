package:
  name: plone.recipe.precompiler
  version: "0.6"

source:
  fn: plone.recipe.precompiler-0.6.zip
  url: https://pypi.python.org/packages/source/p/plone.recipe.precompiler/plone.recipe.precompiler-0.6.zip
  md5: 10ded1097f769064a2893e3624418cd6
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.recipe.precompiler = plone.recipe.precompiler:main
    #
    # Would create an entry point called plone.recipe.precompiler that calls plone.recipe.precompiler.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zc.buildout
    - setuptools
    - zc.recipe.egg
    - python_gettext

  run:
    - python
    - setuptools
    - zc.buildout
    - setuptools
    - zc.recipe.egg
    - python_gettext

test:
  # Python imports
  imports:
    - plone
    - plone.recipe
    - plone.recipe.precompiler

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/plone/plone.recipe.precompiler
  license: GNU General Public License (GPL)
  summary: 'zc.buildout recipe to precompile python and po files.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
