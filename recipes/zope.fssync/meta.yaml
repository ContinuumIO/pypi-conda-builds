package:
  name: zope.fssync
  version: "3.6.1"

source:
  fn: zope.fssync-3.6.1.zip
  url: https://pypi.python.org/packages/source/z/zope.fssync/zope.fssync-3.6.1.zip
  md5: f62812c50707178c95dc6c4c22b8572b
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - zope.fssync = zope.fssync:main
    #
    # Would create an entry point called zope.fssync that calls zope.fssync.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.annotation
    - zope.component
    - zope.dottedname
    - zope.filerepresentation
    - zope.interface
    - zope.lifecycleevent
    - zope.location
    - zope.proxy
    - zope.traversing
    - zope.xmlpickle

  run:
    - python
    - setuptools
    - zope.annotation
    - zope.component
    - zope.dottedname
    - zope.filerepresentation
    - zope.interface
    - zope.lifecycleevent
    - zope.location
    - zope.proxy
    - zope.traversing
    - zope.xmlpickle

test:
  # Python imports
  imports:
    - zope
    - zope.fssync
    - zope.fssync.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/zope.fssync
  license: Zope Public License
  summary: 'Filesystem synchronization utility for Zope 3.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
