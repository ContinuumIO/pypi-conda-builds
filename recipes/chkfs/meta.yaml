package:
  name: chkfs
  version: "0.990c613daa19960e8a900cbffc5472e44282b6f1"

source:
  fn: chkfs-0.990c613daa19960e8a900cbffc5472e44282b6f1.tar.gz
  url: https://pypi.python.org/packages/source/c/chkfs/chkfs-0.990c613daa19960e8a900cbffc5472e44282b6f1.tar.gz
  md5: 0e81b5fe4aabdf5466ff18c6c4509106
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - chkfs = chkfs:main
    #
    # Would create an entry point called chkfs that calls chkfs.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - jsonchkstore >=0
    - chkstore >=0

  run:
    - python
    - jsonchkstore >=0
    - chkstore >=0

test:
  # Python imports
  imports:
    - chkfs

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/nejucomo/chkfs
  license: GPLv3
  summary: 'A commandline tool for storing filesystems inside a chkstore.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
