package:
  name: id_phonenumbers
  version: "0.1.0"

source:
  fn: id_phonenumbers-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/i/id_phonenumbers/id_phonenumbers-0.1.0.tar.gz
  md5: 383a966fe513477f3379f7da1d15c33b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - id_phonenumbers = id_phonenumbers:main
    #
    # Would create an entry point called id_phonenumbers that calls id_phonenumbers.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - phonenumbers

  run:
    - python
    - phonenumbers

test:
  # Python imports
  imports:
    - id_phonenumbers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/SeiryuZ/id_phonenumbers
  license: MIT License
  summary: 'Indonesian Phonenumbers Parser'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
