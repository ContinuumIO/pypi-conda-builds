package:
  name: cornerstone.cssvar
  version: "1.0"

source:
  fn: cornerstone.cssvar-1.0.tar.gz
  url: https://pypi.python.org/packages/source/c/cornerstone.cssvar/cornerstone.cssvar-1.0.tar.gz
  md5: c416dbe749313e76f92c5933357ee4f4
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cornerstone.cssvar = cornerstone.cssvar:main
    #
    # Would create an entry point called cornerstone.cssvar that calls cornerstone.cssvar.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - zope.component
    - zope.app.publisher
    - zope.configuration

  run:
    - python
    - setuptools
    - zope.component
    - zope.app.publisher
    - zope.configuration

test:
  # Python imports
  imports:
    - cornerstone
    - cornerstone.cssvar

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: BSD License
  summary: 'CSS Variables Support for Zope.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
