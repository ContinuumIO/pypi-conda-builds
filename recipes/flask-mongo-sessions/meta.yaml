package:
  name: flask-mongo-sessions
  version: "0.2.1"

source:
  fn: flask-mongo-sessions-0.2.1.tar.gz
  url: https://pypi.python.org/packages/source/f/flask-mongo-sessions/flask-mongo-sessions-0.2.1.tar.gz
  md5: 86abb21f2c38efc553e5a66c1eae5be4
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-mongo-sessions = flask-mongo-sessions:main
    #
    # Would create an entry point called flask-mongo-sessions that calls flask-mongo-sessions.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask >=0.8
    - flask-pymongo

  run:
    - python
    - flask >=0.8
    - flask-pymongo

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ivanyu/flask-mongo-sessions
  license: MIT License
  summary: 'Server-side sessions for Flask with MongoDB'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
