package:
  name: django_hits
  version: "0.2.0-2"

source:
  fn: django_hits-0.2.0-2.tar.gz
  url: https://pypi.python.org/packages/source/d/django_hits/django_hits-0.2.0-2.tar.gz
  md5: 6fae86cb8f282408d8adada47c439828
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django_hits = django_hits:main
    #
    # Would create an entry point called django_hits that calls django_hits.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.3

  run:
    - python
    - django >=1.3

test:
  # Python imports
  imports:
    - django_hits
    - django_hits.management
    - django_hits.management.commands
    - django_hits.migrations
    - django_hits.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/ddanier/django_hits
  license: BSD License
  summary: 'Count visitors/views for your page'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
