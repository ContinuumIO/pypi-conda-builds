package:
  name: flotypebridge
  version: "0.2.4"

source:
  fn: FlotypeBridge-0.2.4.tar.gz
  url: https://pypi.python.org/packages/source/F/FlotypeBridge/FlotypeBridge-0.2.4.tar.gz
  md5: 369be72e289650d36b634987083f4eab
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flotypebridge = flotypebridge:main
    #
    # Would create an entry point called flotypebridge that calls flotypebridge.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - flotype
    - flotypebridge

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/FlotypeBridge/
  license: LICENSE.txt
  summary: 'A Python API for the Flotype Bridge service.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
