package:
  name: seantis.dir.facility
  version: "1.7.2"

source:
  fn: seantis.dir.facility-1.7.2.tar.gz
  url: https://pypi.python.org/packages/source/s/seantis.dir.facility/seantis.dir.facility-1.7.2.tar.gz
  md5: c78080388906ab3f7c66065162129a51
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - seantis.dir.facility = seantis.dir.facility:main
    #
    # Would create an entry point called seantis.dir.facility that calls seantis.dir.facility.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone >=4.3
    - plone.app.dexterity
    - seantis.dir.base >=1.7
    - seantis.reservation
    - collective.js.underscore
    - collective.js.fullcalendar

  run:
    - python
    - setuptools
    - setuptools
    - plone >=4.3
    - plone.app.dexterity
    - seantis.dir.base >=1.7
    - seantis.reservation
    - collective.js.underscore
    - collective.js.fullcalendar

test:
  # Python imports
  imports:
    - seantis
    - seantis.dir
    - seantis.dir.facility
    - seantis.dir.facility.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://svn.plone.org/svn/collective/
  license: GPL
  summary: 'Facility Directory for Seantis Reservation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
