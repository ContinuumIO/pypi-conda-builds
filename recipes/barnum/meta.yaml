package:
  name: barnum
  version: "0.2"

source:
  fn: barnum-0.2.tar.gz
  url: https://pypi.python.org/packages/source/b/barnum/barnum-0.2.tar.gz
  md5: 529eb75e1d2f3b0d86d26a0b52359fb3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - barnum = barnum:main
    #
    # Would create an entry point called barnum that calls barnum.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - barnum

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/chris1610/barnum-proj
  license: GNU General Public License (GPL)
  summary: 'Create random data for your applications'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
