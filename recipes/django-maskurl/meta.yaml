package:
  name: django-maskurl
  version: "0.1"

source:
  fn: django-maskurl-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-maskurl/django-maskurl-0.1.tar.gz
  md5: 6d891f7d2bd9cd82c2073c25c2eb5931
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-maskurl = django-maskurl:main
    #
    # Would create an entry point called django-maskurl that calls django-maskurl.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - maskurl

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://redxbeard.github.io/django_maskurl
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'To mask django url paths, instead of built in template tag'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
