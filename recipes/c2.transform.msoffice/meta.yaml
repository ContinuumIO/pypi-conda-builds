package:
  name: c2.transform.msoffice
  version: "1.0b4"

source:
  fn: c2.transform.msoffice-1.0b4.tar.gz
  url: https://pypi.python.org/packages/source/c/c2.transform.msoffice/c2.transform.msoffice-1.0b4.tar.gz
  md5: df110e03c49ff266e862484c7df0c240
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - c2.transform.msoffice = c2.transform.msoffice:main
    #
    # Would create an entry point called c2.transform.msoffice that calls c2.transform.msoffice.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - c2
    - c2.transform
    - c2.transform.msoffice
    - c2.transform.msoffice.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.cmscom.jp
  license: GPL
  summary: 'This product enables to search MS-Office files on Plone.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
