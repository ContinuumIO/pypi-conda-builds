package:
  name: dynectdns
  version: "0.3"

source:
  fn: DynectDNS-0.3.tar.gz
  url: https://pypi.python.org/packages/source/D/DynectDNS/DynectDNS-0.3.tar.gz
  md5: 3cf5b6278eb766fe15c539d9e8d90114
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - dynectdns = dynectdns:main
    #
    # Would create an entry point called dynectdns that calls dynectdns.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - dynect
    - dynectdns

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/dyninc/Dynect-API-Python-Library
  license: UNKNOWN
  summary: 'Dynect REST API wrapper'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
