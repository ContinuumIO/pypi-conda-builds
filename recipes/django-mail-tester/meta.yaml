package:
  name: django-mail-tester
  version: "0.0.1"

source:
  fn: django-mail-tester-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-mail-tester/django-mail-tester-0.0.1.tar.gz
  md5: bf372ab30e41a2bd41c476e4f6979b8c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-mail-tester = django-mail-tester:main
    #
    # Would create an entry point called django-mail-tester that calls django-mail-tester.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - django_mail_tester
    - django_mail_tester.management
    - django_mail_tester.management.commands

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/satyrius/django-mail-tester
  license: MIT License
  summary: 'Django management command to send email'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
