package:
  name: plone.rfc822
  version: "1.1.1"

source:
  fn: plone.rfc822-1.1.1.zip
  url: https://pypi.python.org/packages/source/p/plone.rfc822/plone.rfc822-1.1.1.zip
  md5: 09d7ad2cb82a367f69ade4ab81bf63f8
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - plone.rfc822 = plone.rfc822:main
    #
    # Would create an entry point called plone.rfc822 that calls plone.rfc822.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zope.schema
    - zope.component
    - zope.interface
    - python-dateutil

  run:
    - python
    - setuptools
    - setuptools
    - zope.schema
    - zope.component
    - zope.interface
    - python-dateutil

test:
  # Python imports
  imports:
    - plone
    - plone.rfc822

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/plone.rfc822
  license: BSD License
  summary: 'RFC822 marshalling for zope.schema fields'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
