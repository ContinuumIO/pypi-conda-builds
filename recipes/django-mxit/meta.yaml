package:
  name: django-mxit
  version: "0.0.1"

source:
  fn: django-mxit-0.0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-mxit/django-mxit-0.0.1.tar.gz
  md5: cfefbbc394857072bf9480a836dd8094
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-mxit = django-mxit:main
    #
    # Would create an entry point called django-mxit that calls django-mxit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django

  run:
    - python
    - django

test:
  # Python imports
  imports:
    - mxit

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/praekelt/django-mxit
  license: BSD License
  summary: 'Simple helpers for writing Mxit apps with Django'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
