package:
  name: beetle-s3uploader
  version: "0.1.0"

source:
  fn: Beetle-S3Uploader-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/B/Beetle-S3Uploader/Beetle-S3Uploader-0.1.0.tar.gz
  md5: 4e101648bfaf2a1898c813a37e9d004d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - beetle-s3uploader = beetle-s3uploader:main
    #
    # Would create an entry point called beetle-s3uploader that calls beetle-s3uploader.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - boto

  run:
    - python
    - boto

test:
  # Python imports
  imports:
    - beetle_s3uploader

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/cknv/beetle-s3uploader
  license: MIT License
  summary: 'Beetle plugin to upload the site to S3'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
