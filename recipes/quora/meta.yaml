package:
  name: quora
  version: "0.1.21"

source:
  fn: quora-0.1.21.tar.gz
  url: https://pypi.python.org/packages/source/q/quora/quora-0.1.21.tar.gz
  md5: f24f46a6b30b1ecf9ad6be19dec0024f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - quora = quora:main
    #
    # Would create an entry point called quora that calls quora.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - beautifulsoup4 ==4.3.2
    - feedparser ==5.1.3
    - requests ==2.5.0

  run:
    - python
    - beautifulsoup4 ==4.3.2
    - feedparser ==5.1.3
    - requests ==2.5.0

test:
  # Python imports
  imports:
    - quora

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/csu/pyquora
  license: UNKNOWN
  summary: 'Fetches and parses data from Quora.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
