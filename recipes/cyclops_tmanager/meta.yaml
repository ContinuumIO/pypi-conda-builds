package:
  name: cyclops_tmanager
  version: "1.9dev"

source:
  fn: cyclops_tmanager-1.9dev.tar.gz
  url: https://pypi.python.org/packages/source/c/cyclops_tmanager/cyclops_tmanager-1.9dev.tar.gz
  md5: 13a7d96a248b7c54d154040b8ac3987b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cyclops_tmanager = cyclops_tmanager:main
    #
    # Would create an entry point called cyclops_tmanager that calls cyclops_tmanager.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - sympy
    - httplib2

  run:
    - python
    - sympy
    - httplib2

test:
  # Python imports
  imports:
    - cyclops_tmanager
    - cyclops_tmanager/logs
    - cyclops_tmanager/os_api

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: UNKNOWN
  summary: 'Rating Charging and Billing engine for the cloud service providers'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
