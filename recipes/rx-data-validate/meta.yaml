package:
  name: rx-data-validate
  version: "0.1.0"

source:
  fn: rx-data-validate-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/r/rx-data-validate/rx-data-validate-0.1.0.tar.gz
  md5: 9238b6a6e3f5c75e8a9c2dd1968ff542
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - rx-data-validate = rx-data-validate:main
    #
    # Would create an entry point called rx-data-validate that calls rx-data-validate.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyrx
    - pyyaml

  run:
    - python
    - pyrx
    - pyyaml

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: MIT
  summary: 'Validate Qcumber Data'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
