package:
  name: rbco.wfdocumentator
  version: "0.0.4"

source:
  fn: rbco.wfdocumentator-0.0.4.zip
  url: https://pypi.python.org/packages/source/r/rbco.wfdocumentator/rbco.wfdocumentator-0.0.4.zip
  md5: 03fc2d2a1a215d320fda29c784ba210c
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - rbco.wfdocumentator = rbco.wfdocumentator:main
    #
    # Would create an entry point called rbco.wfdocumentator that calls rbco.wfdocumentator.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - rbco.commandwrap <=0.0.99
    - prdg.util <=0.0.99

  run:
    - python
    - setuptools
    - setuptools
    - rbco.commandwrap <=0.0.99
    - prdg.util <=0.0.99

test:
  # Python imports
  imports:
    - rbco
    - rbco.wfdocumentator
    - rbco.wfdocumentator.browser

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://code.google.com/p/rbco-wfdocumentator/
  license: GPL
  summary: 'An add-on for Zope and Plone which aims to provide user-friendly automatically generated documentation about workflow definitions.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
