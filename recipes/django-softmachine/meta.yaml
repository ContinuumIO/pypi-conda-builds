package:
  name: django-softmachine
  version: "1.0.6"

source:
  fn: django-softmachine-1.0.6.tar.gz
  url: https://pypi.python.org/packages/source/d/django-softmachine/django-softmachine-1.0.6.tar.gz
  md5: 30a7fe6da446084c56c1dd8ed38405f6
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-softmachine = django-softmachine:main
    #
    # Would create an entry point called django-softmachine that calls django-softmachine.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5

  run:
    - python
    - django >=1.5

test:
  # Python imports
  imports:
    - dbDesigner
    - dbDesigner.service
    - protoLib
    - protoLib.actions
    - protoLib.admin
    - protoLib.localisation
    - protoLib.management
    - protoLib.utils
    - protobase
    - prototype
    - prototype.actions
    - prototype.actions.exportProto
    - prototype.tools

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/certae/django-softmachine
  license: GNU General Public License (GPL)
  summary: 'CeRTAE SoftMachine est une application web qui consiste \xc3\xa0 r\xc3\xa9aliser les interfaces d\xe2\x80\x99une application par la construction de vues \xc3\xa0 partir d\xe2\x80\x99un mod\xc3\xa8le de donn\xc3\xa9es normalis\xc3\xa9e.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
