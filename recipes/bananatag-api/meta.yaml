package:
  name: bananatag-api
  version: "0.1.0"

source:
  fn: bananatag-api-0.1.0.zip
  url: https://pypi.python.org/packages/source/b/bananatag-api/bananatag-api-0.1.0.zip
  md5: ffb14556a83c6de7d61de8b165300f39
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bananatag-api = bananatag-api:main
    #
    # Would create an entry point called bananatag-api that calls bananatag-api.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - btapi

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/bananatag/bananatag-api-python
  license: MIT License
  summary: "The Bananatag API Python Library is used in conjunction with Bananatag's REST API. The Bananatag REST API allows users access to all data associated with their account and sub-accounts."

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
