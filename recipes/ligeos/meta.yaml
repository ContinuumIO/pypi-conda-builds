package:
  name: ligeos
  version: "0.1.1"

source:
  fn: ligeos-0.1.1.tar.gz
  url: https://pypi.python.org/packages/source/l/ligeos/ligeos-0.1.1.tar.gz
  md5: 42b1bbf06b0fe9364e50285186fc700c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ligeos = ligeos:main
    #
    # Would create an entry point called ligeos that calls ligeos.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - ligeos

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/umidev/ligeos
  license: MIT License
  summary: 'A library for linear referencing on linestrings, including geographic calculations.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
