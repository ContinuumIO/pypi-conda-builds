package:
  name: collective.folderposition
  version: "1.0"

source:
  fn: collective.folderposition-1.0.tar.gz
  url: https://pypi.python.org/packages/source/c/collective.folderposition/collective.folderposition-1.0.tar.gz
  md5: 915bd0dbc52edd1bc6af961e967cf1ee
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.folderposition = collective.folderposition:main
    #
    # Would create an entry point called collective.folderposition that calls collective.folderposition.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - plone.app.content

  run:
    - python
    - setuptools
    - setuptools
    - plone.app.content

test:
  # Python imports
  imports:
    - collective
    - collective.folderposition

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/collective.folderposition
  license: GNU General Public License (GPL)
  summary: 'Extended ordering controls for folder contents.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
