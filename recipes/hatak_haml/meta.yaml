package:
  name: hatak_haml
  version: "0.1"

source:
  fn: Hatak_Haml-0.1.tar.gz
  url: https://pypi.python.org/packages/source/H/Hatak_Haml/Hatak_Haml-0.1.tar.gz
  md5: a14063583ef18b4b6be2dd84ba0e2573
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hatak_haml = hatak_haml:main
    #
    # Would create an entry point called hatak_haml that calls hatak_haml.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - hatak >=0.2
    - hatak_jinja2
    - hamlish_jinja

  run:
    - python
    - hatak >=0.2
    - hatak_jinja2
    - hamlish_jinja

test:
  # Python imports
  imports:
    - haplugin
    - haplugin.haml
    - hatak_haml

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  #home: The package home page
  license: Apache License 2.0
  summary: 'Haml plugin for Hatak.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
