package:
  name: hachoir-subfile
  version: "0.5.3"

source:
  fn: hachoir-subfile-0.5.3.tar.gz
  url: https://pypi.python.org/packages/source/h/hachoir-subfile/hachoir-subfile-0.5.3.tar.gz
  md5: 74ff4f86f8ecbc864b6c8a84cbc8164f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hachoir-subfile = hachoir-subfile:main
    #
    # Would create an entry point called hachoir-subfile that calls hachoir-subfile.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - hachoir_subfile

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://hachoir.org/wiki/hachoir-subfile
  license: GNU General Public License (GPL)
  summary: 'Find subfile in any binary stream'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
