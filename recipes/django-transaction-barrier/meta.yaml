package:
  name: django-transaction-barrier
  version: "0.3"

source:
  fn: django-transaction-barrier-0.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-transaction-barrier/django-transaction-barrier-0.3.tar.gz
  md5: 6b5d1345be49e66a7e7e2586c9d50957
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-transaction-barrier = django-transaction-barrier:main
    #
    # Would create an entry point called django-transaction-barrier that calls django-transaction-barrier.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4.0,<1.8.0
    - celery >=3.0.0,<4.0.0

  run:
    - python
    - django >=1.4.0,<1.8.0
    - celery >=3.0.0,<4.0.0

test:
  # Python imports
  imports:
    - django_transaction_barrier
    - django_transaction_barrier.migrations

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/godaddy/django_transaction_barrier
  license: MIT License
  summary: 'Transaction barriers for Django and Celery.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
