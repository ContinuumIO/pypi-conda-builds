package:
  name: eventlog-writer
  version: "0.4.5"

source:
  fn: eventlog-writer-0.4.5.tar.gz
  url: https://pypi.python.org/packages/source/e/eventlog-writer/eventlog-writer-0.4.5.tar.gz
  md5: 6c996cc1e19e07a3b3499d422a9f8f04
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - eventlog-writer = eventlog-writer:main
    #
    # Would create an entry point called eventlog-writer that calls eventlog-writer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - concurrentloghandler

  run:
    - python
    - concurrentloghandler

# test:
  # Python imports
  # imports:

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/zalando/python-eventlog-writer
  license: Apache Software License
  summary: 'Python module to write business events'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
