package:
  name: linkexchange.zope
  version: "0.1"

source:
  fn: LinkExchange.Zope-0.1.tar.gz
  url: https://pypi.python.org/packages/source/L/LinkExchange.Zope/LinkExchange.Zope-0.1.tar.gz
  md5: b7ca0695fa97ec57cb2ea2ea86af0b27
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - linkexchange.zope = linkexchange.zope:main
    #
    # Would create an entry point called linkexchange.zope that calls linkexchange.zope.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - linkexchange >=0.4dev

  run:
    - python
    - setuptools
    - linkexchange >=0.4dev

test:
  # Python imports
  imports:
    - linkexchange
    - linkexchange.zope
    - linkexchange.zope.Extensions

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://linkexchange.org.ua
  license: GNU Library or Lesser General Public License (LGPL)
  summary: 'Zope integration with LinkExchange library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
