package:
  name: django-responsive-admin
  version: "0.2.8"

source:
  fn: django-responsive-admin-0.2.8.tar.gz
  url: https://pypi.python.org/packages/source/d/django-responsive-admin/django-responsive-admin-0.2.8.tar.gz
  md5: 1d8353e9eb39d1dc0b00f12b702b4f33
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-responsive-admin = django-responsive-admin:main
    #
    # Would create an entry point called django-responsive-admin that calls django-responsive-admin.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - responsive_admin
    - responsive_admin.conf
    - responsive_admin.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/pdr/django-responsive-admin
  license: MIT License
  summary: 'A responsive admin for Django using Kube CSS'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
