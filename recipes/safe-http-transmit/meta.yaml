package:
  name: safe-http-transmit
  version: "1.1.4"

source:
  fn: safe-http-transmit-1.1.4.tar.gz
  url: https://pypi.python.org/packages/source/s/safe-http-transmit/safe-http-transmit-1.1.4.tar.gz
  md5: 7c1b2dd0582e4c3eadfe704e0b0edf27
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - safe-http-transmit = safe-http-transmit:main
    #
    # Would create an entry point called safe-http-transmit that calls safe-http-transmit.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - safe_http_transmit

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/defcube/safe-http-transmit
  license: UNKNOWN
  summary: 'A safe HTTP transmitter.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
