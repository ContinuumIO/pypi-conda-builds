package:
  name: django-environment
  version: "0.3.8"

source:
  fn: django-environment-0.3.8.tar.gz
  url: https://pypi.python.org/packages/source/d/django-environment/django-environment-0.3.8.tar.gz
  md5: 8ff9bb4b1d54cc85d73973a18d427db7
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-environment = django-environment:main
    #
    # Would create an entry point called django-environment that calls django-environment.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - virtualenvwrapper

  run:
    - python
    - virtualenvwrapper

test:
  # Python imports
  imports:
    - django_env

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/epicserve/django-environment
  license: UNKNOWN
  summary: 'A plugin for virtualenvwrapper that makes setting up and creating new Django environments easier.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
