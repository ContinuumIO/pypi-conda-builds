package:
  name: django-intercom
  version: "0.0.11"

source:
  fn: django-intercom-0.0.11.tar.gz
  url: https://pypi.python.org/packages/source/d/django-intercom/django-intercom-0.0.11.tar.gz
  md5: e42eddf3766aa6cce3133b7b6754d203
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-intercom = django-intercom:main
    #
    # Would create an entry point called django-intercom that calls django-intercom.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - intercom
    - intercom.templatetags

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kencochrane/django-intercom/
  license: UNKNOWN
  summary: 'Django App for integrating with intercom.io'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
