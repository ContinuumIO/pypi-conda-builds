package:
  name: django-spoolgore
  version: "0.1"

source:
  fn: django-spoolgore-0.1.tar.gz
  url: https://pypi.python.org/packages/source/d/django-spoolgore/django-spoolgore-0.1.tar.gz
  md5: 8053e3f6b2f8d7fbbdcf9a1cb7da46f9
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-spoolgore = django-spoolgore:main
    #
    # Would create an entry point called django-spoolgore that calls django-spoolgore.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - spoolgore

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/20tab/django-spoolgore
  license: UNKNOWN
  summary: 'A django EMAIL_BACKEND for enqueuing mail in the Spoolgore daemon.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
