package:
  name: ftw.footer
  version: "1.0.2"

source:
  fn: ftw.footer-1.0.2.zip
  url: https://pypi.python.org/packages/source/f/ftw.footer/ftw.footer-1.0.2.zip
  md5: 452158818b7adc80e4fb28052c8f175e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ftw.footer = ftw.footer:main
    #
    # Would create an entry point called ftw.footer that calls ftw.footer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - ftw
    - ftw.footer
    - ftw.footer.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/4teamwork/ftw.footer
  license: GPL2
  summary: 'Adds portlet columns as footer to Plone.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
