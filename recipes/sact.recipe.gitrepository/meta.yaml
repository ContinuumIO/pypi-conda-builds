package:
  name: sact.recipe.gitrepository
  version: "1.2"

source:
  fn: sact.recipe.gitrepository-1.2.tar.gz
  url: https://pypi.python.org/packages/source/s/sact.recipe.gitrepository/sact.recipe.gitrepository-1.2.tar.gz
  md5: 38904c9e055a2b5cc579b0dc10b7d245
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - sact.recipe.gitrepository = sact.recipe.gitrepository:main
    #
    # Would create an entry point called sact.recipe.gitrepository that calls sact.recipe.gitrepository.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.recipe.egg

  run:
    - python
    - setuptools
    - setuptools
    - zc.recipe.egg

test:
  # Python imports
  imports:
    - sact
    - sact.recipe
    - sact.recipe.gitrepository

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/securactive/sact.recipe.gitrepository
  license: BSD License
  summary: 'Buildout recipe for fetching sources in a git repository'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
