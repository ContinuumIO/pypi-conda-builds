package:
  name: mplstyle
  version: "0.1.2"

source:
  fn: mplstyle-0.1.2.tar.gz
  url: https://pypi.python.org/packages/source/m/mplstyle/mplstyle-0.1.2.tar.gz
  md5: 61165a8b9aad9e8758428b738856ee23
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - mplstyle = mplstyle:main
    #
    # Would create an entry point called mplstyle that calls mplstyle.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - mplstyle
    - mplstyle.styles

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/jsvine/mplstyle
  license: MIT License
  summary: 'A simple API for setting matplotlib styles in matplotlib.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
