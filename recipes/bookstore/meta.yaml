package:
  name: bookstore
  version: "1.0.0"

source:
  fn: bookstore-1.0.0.tar.gz
  url: https://pypi.python.org/packages/source/b/bookstore/bookstore-1.0.0.tar.gz
  md5: a3c86d6b427376896d7d0fb29eae828f
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - bookstore = bookstore:main
    #
    # Would create an entry point called bookstore that calls bookstore.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyrax >=1.4.10
    - ipython[notebook] >=1.1.0

  run:
    - python
    - pyrax >=1.4.10
    - ipython[notebook] >=1.1.0

test:
  # Python imports
  imports:
    - bookstore

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/rgbkrk/bookstore
  license: Apache Software License
  summary: 'IPython notebook storage on OpenStack Swift + Rackspace.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
