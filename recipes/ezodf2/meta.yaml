package:
  name: ezodf2
  version: "0.2.10"

source:
  fn: ezodf2-0.2.10.tar.gz
  url: https://pypi.python.org/packages/source/e/ezodf2/ezodf2-0.2.10.tar.gz
  md5: b66007f590a23528170831b4a4a1a07d
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - ezodf2 = ezodf2:main
    #
    # Would create an entry point called ezodf2 that calls ezodf2.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - lxml

  run:
    - python
    - lxml

test:
  # Python imports
  imports:
    - ezodf2

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/iwschris/ezodf2
  license: MIT License
  summary: 'A Python package to create/manipulate OpenDocumentFormat files.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
