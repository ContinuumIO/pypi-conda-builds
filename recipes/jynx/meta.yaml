package:
  name: jynx
  version: "0.4.2-jython-py2.5"

source:
  fn: Jynx-0.4.2-jython-py2.5.tar.gz
  url: https://pypi.python.org/packages/source/J/Jynx/Jynx-0.4.2-jython-py2.5.tar.gz
  md5: b2a9fb69fda37d367f2bbbec76af9a71
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - jynx = jynx:main
    #
    # Would create an entry point called jynx that calls jynx.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - jynx
    - jynx.lib
    - jynx.lib.hibernate
    - jynx.lib.hibernate.tests
    - jynx.lib.javaparser
    - jynx.lib.javaparser.tests
    - jynx.lib.junit
    - jynx.lib.util
    - jynx.tests
    - org
    - org.javaparser
    - org.junit
    - org.jynx
    - org.jynx.gen
    - org.slf4j

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.fiber-space.de/
  license: MIT License
  summary: 'Jython library for Python integration with modern Java using dynamic Java compilation'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
