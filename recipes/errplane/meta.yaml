package:
  name: errplane
  version: "0.1.6"

source:
  fn: errplane-0.1.6.tar.gz
  url: https://pypi.python.org/packages/source/e/errplane/errplane-0.1.6.tar.gz
  md5: 13d59120a34a3f5a6811b783f82cdbf3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - errplane = errplane:main
    #
    # Would create an entry point called errplane that calls errplane.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - requests

  run:
    - python
    - requests

test:
  # Python imports
  imports:
    - errplane

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/errplane/errplane-python
  license: LICENSE.txt
  summary: 'Python library for use with Errplane (https://errplane.com)'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
