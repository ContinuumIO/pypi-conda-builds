package:
  name: django-terms
  version: "1.1.3"

source:
  fn: django-terms-1.1.3.tar.gz
  url: https://pypi.python.org/packages/source/d/django-terms/django-terms-1.1.3.tar.gz
  md5: be2ceafc9805e96c69f5572c8beb3f5b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - django-terms = django-terms:main
    #
    # Would create an entry point called django-terms that calls django-terms.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.4,<1.8
    - lxml

  run:
    - python
    - django >=1.4,<1.8
    - lxml

test:
  # Python imports
  imports:
    - terms
    - terms.south_migrations
    - terms.templatetags
    - terms.tests
    - terms.tests.html
    - terms.tests.terms

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/BertrandBordage/django-terms
  license: BSD License
  summary: 'Site-wide adds a definition or a link for specialized terms.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
