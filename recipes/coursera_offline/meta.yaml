package:
  name: coursera_offline
  version: "1.0.2"

source:
  fn: coursera_offline-1.0.2.zip
  url: https://pypi.python.org/packages/source/c/coursera_offline/coursera_offline-1.0.2.zip
  md5: 5d8628be664747a145a6aadc6ab939c3
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - coursera_offline = coursera_offline:main
    #
    # Would create an entry point called coursera_offline that calls coursera_offline.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - pyquery >=1.2.9
    - python-crontab >=1.8.1
    - docutils >=0.3

  run:
    - python
    - pyquery >=1.2.9
    - python-crontab >=1.8.1
    - docutils >=0.3

test:
  # Python imports
  imports:
    - coursera_offline

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/sanketh95/coursera-offline
  license: GPLv3
  summary: 'Download Coursera videos for offline viewing'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
