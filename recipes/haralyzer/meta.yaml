package:
  name: haralyzer
  version: "1.1.1"

source:
  fn: haralyzer-1.1.1.tar.gz
  url: https://pypi.python.org/packages/source/h/haralyzer/haralyzer-1.1.1.tar.gz
  md5: 6f13c9048763af9b143f93e304949139
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - haralyzer = haralyzer:main
    #
    # Would create an entry point called haralyzer that calls haralyzer.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - python-dateutil
    - cached_property

  run:
    - python
    - python-dateutil
    - cached_property

test:
  # Python imports
  imports:
    - haralyzer

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mrname/haralyzer
  license: GNU General Public License (GPL)
  summary: 'A python framework for getting useful stuff out of HAR files'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
