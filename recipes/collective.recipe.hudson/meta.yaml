package:
  name: collective.recipe.hudson
  version: "1.0"

source:
  fn: collective.recipe.hudson-1.0.zip
  url: https://pypi.python.org/packages/source/c/collective.recipe.hudson/collective.recipe.hudson-1.0.zip
  md5: c44ce2e28bf800a2189738ab9683dd12
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - collective.recipe.hudson = collective.recipe.hudson:main
    #
    # Would create an entry point called collective.recipe.hudson that calls collective.recipe.hudson.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - iw.recipe.template
    - setuptools
    - zc.buildout
    - zc.recipe.egg

  run:
    - python
    - setuptools
    - iw.recipe.template
    - setuptools
    - zc.buildout
    - zc.recipe.egg

test:
  # Python imports
  imports:
    - collective
    - collective.recipe
    - collective.recipe.hudson

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://pypi.python.org/pypi/collective.recipe.hudson
  license: Zope Public License
  summary: 'Buildout recipe for installing a Hudson instance'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
