package:
  name: memoized
  version: "0.2"

source:
  fn: memoized-0.2.tar.gz
  url: https://pypi.python.org/packages/source/m/memoized/memoized-0.2.tar.gz
  md5: ab3c44f629e94173f2117fba3eac95b8
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - memoized = memoized:main
    #
    # Would create an entry point called memoized that calls memoized.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  # Python imports
  imports:
    - memoized

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://bitbucket.org/gsakkis/memoized
  license: MIT License
  summary: 'General purpose efficient memoization'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
