package:
  name: simplegeo
  version: "3.0.127"

source:
  fn: simplegeo-3.0.127.tar.gz
  url: https://pypi.python.org/packages/source/s/simplegeo/simplegeo-3.0.127.tar.gz
  md5: 76a9ab5272599d882178fc1a7b50cc7c
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - simplegeo = simplegeo:main
    #
    # Would create an entry point called simplegeo that calls simplegeo.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - httplib2 >=0.6.0
    - oauth2 >=1.5
    - ipaddr >=2.0.0
    - simplejson >=2.1.0

  run:
    - python
    - httplib2 >=0.6.0
    - oauth2 >=1.5
    - ipaddr >=2.0.0
    - simplejson >=2.1.0

test:
  # Python imports
  imports:
    - simplegeo
    - simplegeo.context
    - simplegeo.contrib
    - simplegeo.places
    - simplegeo.shared
    - simplegeo.storage
    - simplegeo.test
    - simplegeo.test.client
    - simplegeo.test.models

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://github.com/simplegeo/python-simplegeo
  license: MIT License
  summary: "Library for interfacing with SimpleGeo's API"

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
