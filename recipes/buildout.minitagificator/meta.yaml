package:
  name: buildout.minitagificator
  version: "2.4.29"

source:
  fn: buildout.minitagificator-2.4.29.zip
  url: https://pypi.python.org/packages/source/b/buildout.minitagificator/buildout.minitagificator-2.4.29.zip
  md5: e9e98dda7e4ccbafd3212412f7784e3e
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - buildout.minitagificator = buildout.minitagificator:main
    #
    # Would create an entry point called buildout.minitagificator that calls buildout.minitagificator.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - minitage.recipe.egg >=1.88
    - minitage.recipe.cmmi
    - minitage.recipe.scripts
    - minitage.recipe.common

  run:
    - python
    - setuptools
    - setuptools
    - zc.buildout
    - minitage.recipe.egg >=1.88
    - minitage.recipe.cmmi
    - minitage.recipe.scripts
    - minitage.recipe.common

test:
  # Python imports
  imports:
    - buildout
    - buildout.minitagificator

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/minitage/buildout.minitagificator
  license: GPL
  summary: 'A buildout extension to replace buildout part by minitage interesant ones sponsored by Makina Corpus'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
