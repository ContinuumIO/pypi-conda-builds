package:
  name: flask-boilerplate-utils
  version: "0.1.58"

source:
  fn: flask-boilerplate-utils-0.1.58.tar.gz
  url: https://pypi.python.org/packages/source/f/flask-boilerplate-utils/flask-boilerplate-utils-0.1.58.tar.gz
  md5: 7aa66a243e6a0539f9c799fb6fa0a91b
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - flask-boilerplate-utils = flask-boilerplate-utils:main
    #
    # Would create an entry point called flask-boilerplate-utils that calls flask-boilerplate-utils.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - flask
    - raven
    - flask-wtf
    - flask-babel
    - flask-script
    - meinheld
    - flask-classy

  run:
    - python
    - flask
    - raven
    - flask-wtf
    - flask-babel
    - flask-script
    - meinheld
    - flask-classy

test:
  # Python imports
  imports:
    - flask_boilerplate_utils

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/nickw444/flask-boilerplate-utils
  license: UNKNOWN
  summary: 'Additional functionality with easy upgrading for the flask-boilerplate.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
