package:
  name: supervisor_cache
  version: "1.1.0"

source:
  fn: supervisor_cache-1.1.0.tar.gz
  url: https://pypi.python.org/packages/source/s/supervisor_cache/supervisor_cache-1.1.0.tar.gz
  md5: b4854d2fa3f5dbaaa891915216141ef2
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - supervisor_cache = supervisor_cache:main
    #
    # Would create an entry point called supervisor_cache that calls supervisor_cache.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - supervisor >=3.0a6

  run:
    - python
    - supervisor >=3.0a6

test:
  # Python imports
  imports:
    - supervisor_cache
    - supervisor_cache.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/mnaberez/supervisor_cache
  license: BSD License
  summary: 'supervisor_cache RPC extension for supervisor'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
