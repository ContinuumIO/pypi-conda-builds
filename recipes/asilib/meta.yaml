package:
  name: asilib
  version: "1.0.2"

source:
  fn: asilib-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/a/asilib/asilib-1.0.2.tar.gz
  md5: 5a0cb486f0dbb8f28efcee8b930ebd71
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - asilib = asilib:main
    #
    # Would create an entry point called asilib that calls asilib.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - restlib >=0.9
    - opensocial.py

  run:
    - python
    - setuptools
    - restlib >=0.9
    - opensocial.py

test:
  # Python imports
  imports:
    - asilib

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://asibsync.sourceforge.net
  license: BSD License
  summary: 'Aalto Social Interface (ASI) Python interface library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
