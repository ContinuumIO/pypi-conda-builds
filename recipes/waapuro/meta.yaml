package:
  name: waapuro
  version: "1.0.1"

source:
  fn: waapuro-1.0.1.tar.gz
  url: https://pypi.python.org/packages/source/w/waapuro/waapuro-1.0.1.tar.gz
  md5: d7c25e3ab4f2023436c59bc22ad33f13
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - waapuro = waapuro:main
    #
    # Would create an entry point called waapuro that calls waapuro.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - future

  run:
    - python
    - future

test:
  # Python imports
  imports:
    - waapuro
    - waapuro.test

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/kxz/waapuro
  license: MIT License
  summary: 'A dead-simple hiragana and katakana romanization library'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
