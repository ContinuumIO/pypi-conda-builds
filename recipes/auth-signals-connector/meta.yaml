package:
  name: auth-signals-connector
  version: "0.1.0"

source:
  fn: auth-signals-connector-0.1.0.tar.gz
  url: https://pypi.python.org/packages/source/a/auth-signals-connector/auth-signals-connector-0.1.0.tar.gz
  md5: 97266f1e9b5cfa5f772e98a3cb382649
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - auth-signals-connector = auth-signals-connector:main
    #
    # Would create an entry point called auth-signals-connector that calls auth-signals-connector.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - django >=1.5

  run:
    - python
    - django >=1.5

test:
  # Python imports
  imports:
    - auth_signals_connector

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/e0ne/auth-signals-connector
  license: MIT License
  summary: 'Simple abstraction layer for django.contrib.auth.signals'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
