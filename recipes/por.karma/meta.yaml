package:
  name: por.karma
  version: "1.0"

source:
  fn: por.karma-1.0.zip
  url: https://pypi.python.org/packages/source/p/por.karma/por.karma-1.0.zip
  md5: 02ff835e48d616486dd1d519f653b216
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - por.karma = por.karma:main
    #
    # Would create an entry point called por.karma that calls por.karma.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - setuptools

  run:
    - python
    - setuptools
    - setuptools

test:
  # Python imports
  imports:
    - por
    - por.karma

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://www.redturtle.it
  license: GPL
  summary: 'Karma integration in Penelope'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
