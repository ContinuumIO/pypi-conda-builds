package:
  name: html2rest
  version: "0.2.2"

source:
  fn: html2rest-0.2.2.tar.gz
  url: https://pypi.python.org/packages/source/h/html2rest/html2rest-0.2.2.tar.gz
  md5: e5fc78ce17cf1d8d29592862995f03bb
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - html2rest = html2rest:main
    #
    # Would create an entry point called html2rest that calls html2rest.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - html2rest

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/podados/python-html2rest
  license: BSD License
  summary: 'Convert HTML to restructuredText'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
