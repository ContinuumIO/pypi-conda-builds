package:
  name: hostexpand
  version: "1.0.2"

source:
  fn: hostexpand-1.0.2.tar.gz
  url: https://pypi.python.org/packages/source/h/hostexpand/hostexpand-1.0.2.tar.gz
  md5: 52bade8514401ba99b2192744668de25
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - hostexpand = hostexpand:main
    #
    # Would create an entry point called hostexpand that calls hostexpand.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - dnspython

  run:
    - python
    - dnspython

test:
  # Python imports
  imports:
    - hostexpand

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/yadt/hostexpand
  license: GNU General Public License (GPL)
  summary: 'A tool to expand hostnames based on a pattern language and DNS resolution'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
