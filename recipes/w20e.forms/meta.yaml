package:
  name: w20e.forms
  version: "1.0.2b"

source:
  fn: w20e.forms-1.0.2b.tar.gz
  url: https://pypi.python.org/packages/source/w/w20e.forms/w20e.forms-1.0.2b.tar.gz
  md5: fe34744c9fc3e7e00898b49ef886b7c8
#  patches:
   # List any patch files here
   # - fix.patch

build:
  preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - w20e.forms = w20e.forms:main
    #
    # Would create an entry point called w20e.forms that calls w20e.forms.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - lxml
    - zope.interface
    - ordereddict
    - python-dateutil
    - chameleon

  run:
    - python
    - setuptools
    - lxml
    - zope.interface
    - ordereddict
    - python-dateutil
    - chameleon

test:
  # Python imports
  imports:
    - w20e
    - w20e.forms
    - w20e.forms.data
    - w20e.forms.model
    - w20e.forms.plone
    - w20e.forms.pyramid
    - w20e.forms.rendering
    - w20e.forms.rendering.html
    - w20e.forms.submission
    - w20e.forms.tests
    - w20e.forms.xml

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: 
  license: GNU General Public License (GPL)
  summary: 'Python API for creating and handling forms'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
